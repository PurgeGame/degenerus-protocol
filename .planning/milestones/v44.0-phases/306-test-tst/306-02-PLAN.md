---
phase: 306-test-tst
plan: 02
type: execute
wave: 1
subsystem: Edge fuzz coverage — RedemptionEdgeCases
tags: [TST, EDGE, fuzz, sStonk, V-184-reproduction, EDGE-07, EDGE-19, EDGE-20, dust-floor, multi-day-stall, v44.0]
depends_on: []
requirements: [TST-03, TST-04, EDGE-01, EDGE-02, EDGE-03, EDGE-04, EDGE-05, EDGE-06, EDGE-07, EDGE-08, EDGE-09, EDGE-10, EDGE-11, EDGE-12, EDGE-13, EDGE-14, EDGE-15, EDGE-16, EDGE-17, EDGE-18, EDGE-19, EDGE-20]
files_modified:
  - test/fuzz/RedemptionEdgeCases.t.sol
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the new edge-case fuzz file"
    - "`FOUNDRY_PROFILE=deep forge test --match-path 'test/fuzz/RedemptionEdgeCases.t.sol'` PASSES at 10k runs per case"
    - "20 testFuzz_EDGE_NN_* functions present (18 SPEC-locked + EDGE-19 multi-day stall + EDGE-20 dust floor): testFuzz_EDGE_01_PreAdvanceGapBurnLandsInCurrentDayPool, testFuzz_EDGE_02_TwoPendingDaysSimultaneous, testFuzz_EDGE_03_SinglePlayerMultiDayClaimsIndependent, testFuzz_EDGE_04_MultiplePlayersSameDay, testFuzz_EDGE_05_ClaimBeforeResolveReverts, testFuzz_EDGE_06_SkippedAdvanceLongStallEventualResolution, testFuzz_EDGE_07_V184AttackReproductionStructuralClosure, testFuzz_EDGE_08_BurnGameOverClaimBothVariants, testFuzz_EDGE_09_NPlayersConcurrentClaimsSum, testFuzz_EDGE_10_ReentrancyOnPayEthBlocked, testFuzz_EDGE_11_BurnDuringRngLockedReverts, testFuzz_EDGE_12_BurnDuringLivenessReverts, testFuzz_EDGE_13_ZeroRoundedEthValueOwedBurnProceeds, testFuzz_EDGE_14_SupplyCapExactOneWeiOverAndLazyInit, testFuzz_EDGE_15_EvCapExactOneWeiOver, testFuzz_EDGE_16_CrossDayCapResetStructural, testFuzz_EDGE_17_LateDayBurnPostResolveLegitimate, testFuzz_EDGE_18_BurniePoolInsufficientFallback, testFuzz_EDGE_19_MultiDayRngStallStaleClaimRecovery, testFuzz_EDGE_20_BurnTooSmall"
    - "EDGE-07 V-184 reproduction asserts NEGATIVE PATH: same-day post-resolve re-burn attempt either (a) reverts `PriorDayUnresolved` (re-burn lands while sentinel still names a different already-resolved day — INV-13 enforcement) OR (b) lands in a fresh `pendingByDay[D]` slot keyed to a DIFFERENT mapping key than the already-resolved `redemptionPeriods[D].roll` value; captures `redemptionPeriods[D].roll` before AND after the attack sequence; asserts byte-identity"
    - "EDGE-19 multi-day RNG stall: burns on day D, advance skipped for days D+1..D+k (k bounded fuzz [2, 5]), eventual advance on day D+k+1 fires and reads `sdgnrs.pendingResolveDay() == D` (NOT D+k); resolves `redemptionPeriods[D].roll` correctly; `claimRedemption(D)` succeeds for the burner"
    - "EDGE-20 dust floor: `burn(amount)` where `amount < MIN_BURN_AMOUNT = 1e18` reverts `BurnTooSmall`; `burn(MIN_BURN_AMOUNT)` succeeds; `burn(MIN_BURN_AMOUNT - 1)` reverts"
    - "AGENT-COMMITTED test-tree commit per `D-43N-TEST-COMMITS-AUTO-01` lineage; zero `contracts/*.sol` mutations"
  artifacts:
    - path: "test/fuzz/RedemptionEdgeCases.t.sol"
      provides: "20 testFuzz_EDGE_NN_* fuzz functions covering EDGE-01..20 with positive + negative assertions per 304-SPEC §3 + Phase 305 additions"
      contains: "testFuzz_EDGE_07_V184AttackReproductionStructuralClosure"
  key_links:
    - from: "test/fuzz/RedemptionEdgeCases.t.sol::testFuzz_EDGE_07_V184AttackReproductionStructuralClosure"
      to: "redemptionPeriods[D].roll byte-identity assertion"
      via: "captures roll pre-attack and post-attack; assertEq byte-identical across the entire attack sequence"
      pattern: "assertEq\\(uint256\\(rollPostAttack\\), uint256\\(rollPreAttack\\)"
    - from: "test/fuzz/RedemptionEdgeCases.t.sol::testFuzz_EDGE_19_MultiDayRngStallStaleClaimRecovery"
      to: "sdgnrs.pendingResolveDay()"
      via: "asserts sentinel correctly names the stuck day after k-day stall"
      pattern: "sdgnrs\\.pendingResolveDay\\(\\)"
    - from: "test/fuzz/RedemptionEdgeCases.t.sol::testFuzz_EDGE_20_BurnTooSmall"
      to: "BurnTooSmall revert"
      via: "vm.expectRevert(StakedDegenerusStonk.BurnTooSmall.selector)"
      pattern: "BurnTooSmall\\.selector"
---

<objective>
Ship the edge-case fuzz suite that mechanizes EDGE-01..20 (18 SPEC-locked + EDGE-19 multi-day stall + EDGE-20 dust floor added in Phase 305) against the post-refactor sStonk source. New file `test/fuzz/RedemptionEdgeCases.t.sol` contains one `testFuzz_EDGE_NN_*` fuzz function per scenario, each with positive (correct-behavior outcome) AND negative (revert OR byte-identity attestation) assertions per the 304-SPEC §3 enumeration. EDGE-07 is the HEADLINE — explicit V-184 attack-vector reproduction asserting `redemptionPeriods[D].roll` byte-identity across the entire attack sequence; HANDOFF-111..117 close structurally via this single test (the 7 catalog rows V-184/V-186/V-188/V-190/V-191/V-192/V-193 collapse per FIXREC §0.6).

Purpose: TST-03 + TST-04 + EDGE-01..20 closure for v44.0. The 20 fuzz functions are what FINDINGS-v44.0.md §3.C conservation re-proof matrix (Phase 308) cites as the EDGE-NN → test_id mapping. EDGE-07 specifically is cited by §3.D V-184 disposition RESOLVED-AT-V44 attestation. Plan 02 has no contract dependencies and runs in Wave 1 fully parallel with Plans 01/03/04/05 — modifies only test/fuzz/RedemptionEdgeCases.t.sol (a new file, no overlap).

Output: One new file `test/fuzz/RedemptionEdgeCases.t.sol` (~1100-1400 lines: setUp + 20 testFuzz_* functions). AGENT-COMMITTED test-tree commit envelope per `D-43N-TEST-COMMITS-AUTO-01`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/305-implementation-impl/305-01-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@test/fuzz/RedemptionGas.t.sol
@test/fuzz/helpers/DeployProtocol.sol
@test/fuzz/CoverageGap222.t.sol

<interfaces>
<!-- Errors the executor uses with vm.expectRevert (selector form). All defined in contracts/StakedDegenerusStonk.sol post-Phase-305. -->

```solidity
StakedDegenerusStonk.Insufficient.selector             // amount == 0 || amount > bal || cap breach
StakedDegenerusStonk.BurnsBlockedDuringRng.selector    // game.rngLocked() == true
StakedDegenerusStonk.BurnsBlockedDuringLiveness.selector // game.livenessTriggered() && !game.gameOver()
StakedDegenerusStonk.NoClaim.selector                  // empty composite-keyed claim slot
StakedDegenerusStonk.NotResolved.selector              // redemptionPeriods[day].roll == 0
StakedDegenerusStonk.ExceedsDailyRedemptionCap.selector // claim.ethValueOwed + ethValueOwed > 160e18
StakedDegenerusStonk.PriorDayUnresolved.selector       // sentinel != 0 && sentinel != currentPeriod (INV-13)
StakedDegenerusStonk.BurnTooSmall.selector             // amount < MIN_BURN_AMOUNT (1e18)
StakedDegenerusStonk.TransferFailed.selector           // .call / .transfer return false
```

Key constants the fuzz suite uses:

```solidity
uint256 constant MIN_BURN_AMOUNT = 1e18;
uint256 constant MAX_DAILY_REDEMPTION_EV = 160 ether;
// Roll range per resolveRedemptionPeriod inputs: 25-175 (uint16); 100 = neutral
```

Public view functions invoked by edge tests:

```solidity
sdgnrs.pendingResolveDay() returns (uint32)                            // INV-13 sentinel
sdgnrs.hasPendingRedemptions(uint32 day) returns (bool)
sdgnrs.pendingRedemptions(address, uint32) returns (uint96, uint96, uint16)
sdgnrs.redemptionPeriods(uint32) returns (uint16, uint32)
sdgnrs.pendingRedemptionEthValue() returns (uint256)
sdgnrs.balanceOf(address) returns (uint256)
sdgnrs.totalSupply() returns (uint256)
sdgnrs.burnieReserve() returns (uint256)
game.currentDayView() returns (uint32)
game.gameOver() returns (bool)
game.rngLocked() returns (bool)
game.livenessTriggered() returns (bool)
```

Mocked external behavior for full-claim coverage (precedent in RedemptionGas.t.sol:97-109):

```solidity
vm.mockCall(
    address(coinflip),
    abi.encodeWithSelector(coinflip.getCoinflipDayResult.selector, anyDay),
    abi.encode(uint16(100), true)  // rewardPercent != 0, flipWon = true → flipResolved = true, full-claim path
);
vm.mockCall(
    address(coinflip),
    abi.encodeWithSelector(coinflip.claimCoinflipsForRedemption.selector),
    abi.encode(uint256(0))
);
```
</interfaces>

<patterns>
1. **DeployProtocol scaffold** — inherit + `_deployProtocol()` in setUp.

2. **Bounded fuzz amount** — `amount = bound(amountSeed, MIN_BURN_AMOUNT, playerBalance)`. NEVER fuzz amount unbounded — sub-MIN_BURN_AMOUNT reverts BurnTooSmall and is tested only in EDGE-20.

3. **Day-boundary control** — `vm.warp(block.timestamp + 1 days)` then `try game.advanceGame() {} catch {}` + VRF fulfill via mockVRF + second `advanceGame()`. Precedent at `test/fuzz/RedemptionGas.t.sol`'s setUp + the RedemptionHandler `action_advanceDay`.

4. **Resolve directly from test** — instead of running the full advanceGame VRF cycle, edge tests that need a deterministic roll can `vm.prank(address(game)); sdgnrs.resolveRedemptionPeriod(roll, flipDay, dayToResolve);` — precedent at `test/fuzz/RedemptionGas.t.sol:77-78`. Use roll ∈ [25, 175].

5. **vm.expectRevert with selector** — `vm.expectRevert(StakedDegenerusStonk.BurnTooSmall.selector);` precedent at `test/fuzz/CoverageGap222.t.sol` ACL-rejection patterns.

6. **Multi-actor seeding** — `vm.prank(address(game)); sdgnrs.transferFromPool(StakedDegenerusStonk.Pool.Reward, actor, 1_000_000 ether);` precedent at `test/fuzz/RedemptionGas.t.sol:25-27`.

7. **Per-function FOUNDRY_PROFILE=deep** — adds `/// forge-config: deep.fuzz.runs = 10000` NatSpec comment ABOVE each `testFuzz_*` to override default 256 → 10000 runs. Verify the comment placement matches the Foundry recognition format (NatSpec immediately preceding function declaration).

8. **EDGE-07 attack reproduction precedent** — the existing `testFuzz_RngLockDeterminism_StakedStonkRedemption` at `test/fuzz/RngLockDeterminism.t.sol:1272-1334` is the rough shape; for EDGE-07 the test must additionally TRIGGER the re-burn attempt + assert `redemptionPeriods[D].roll` byte-identical AND assert the re-burn either reverts `PriorDayUnresolved` (if sentinel still names the resolved day's pool — actually no, sentinel is CLEARED at resolve) OR lands in `pendingByDay[currentDayView()]` which is a FRESH slot since `pendingResolveDay = 0` post-resolve. Trace carefully: after resolve, sentinel == 0, so a new burn sets sentinel to `currentDayView()` and writes a new pool. The KEY assertion is that this new pool's eventual resolve writes `redemptionPeriods[currentDayView()]`, NOT `redemptionPeriods[D]` (where D is the already-resolved day). Walk this in the test natspec.
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EDGE-01 through EDGE-10 — positive/negative pairs + V-184 headline</name>
  <files>test/fuzz/RedemptionEdgeCases.t.sol</files>
  <read_first>
    Read `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` §3 lines 401-557 (EDGE-01..10 scenarios + positive/negative assertions + Foundry function names verbatim from each EDGE-NN's "Foundry function name" field) + `test/fuzz/RedemptionGas.t.sol` for setUp + mock patterns + `contracts/StakedDegenerusStonk.sol` lines 615-740 (burn + resolve + claim paths).
  </read_first>
  <behavior>
    For each EDGE-NN function, the body must execute the exact scenario sequence + assert BOTH positive + negative outcomes per 304-SPEC §3. Per-function behavior contracts:

    - testFuzz_EDGE_01: warp to day D, burn from player A pre-day-D-advance; assert `pendingByDay[D].ethBase != 0` (gwei units > 0) AND `pendingByDay[D-1]` byte-identical pre/post (capture via vm.load).
    - testFuzz_EDGE_02: pre-populate pendingByDay[D-1] via day-(D-1) burn + advance not-yet-fired; warp to day D + day-D advance fires + resolves D-1; assert `redemptionPeriods[D-1].roll != 0` + `pendingByDay[D-1] == 0` (delete-at-resolve) + `pendingByDay[D]` unchanged (zero, since no day-D burn yet).
    - testFuzz_EDGE_03: single player A burns on D, D+1, D+2 (with day advances between); assert composite-key slots `pendingRedemptions[A][D]`, `pendingRedemptions[A][D+1]`, `pendingRedemptions[A][D+2]` all independently populated; claim D+2 first; assert `pendingRedemptions[A][D]` byte-identical post-claim-of-D+2; then claim D and D+1 in order.
    - testFuzz_EDGE_04: 2 players A + B burn same day D at different points relative to advance; resolve day D; both claim; assert sum(payouts) == (pendingByDay[D].ethBase * 1e9 * roll / 100) (gwei → wei conversion).
    - testFuzz_EDGE_05: player A burns day D; pre-advance, attempt claimRedemption(D); vm.expectRevert(StakedDegenerusStonk.NotResolved.selector); assert pendingRedemptions[A][D] byte-identical pre/post failed-claim.
    - testFuzz_EDGE_06: burn day D; warp 2-12 days forward (fuzz the stall); eventual advance fires; assert pendingResolveDay = D BEFORE the eventual advance, then == 0 AFTER; claim succeeds with the eventual roll.
    - testFuzz_EDGE_07 (HEADLINE): player A burns day D; trigger day-(D+1) advance → resolves redemptionPeriods[D] with roll R1; capture R1 via `(uint16 rollPre, ) = sdgnrs.redemptionPeriods(D);` IMMEDIATELY after resolve; now attempt the V-184 re-burn vector — player B burns 1 wholeSDGNRS (MIN_BURN_AMOUNT) on same wall day D (post-resolve, pre-day-boundary) — assert this burn either (a) lands in `pendingByDay[D]` as a FRESH pool (sentinel was 0 post-resolve, B's burn sets it to D again) OR (b) reverts if some intervening state blocks it; trigger another day-(D+1) advance attempt (the AdvanceModule will read `pendingResolveDay() == D` again and call `resolveRedemptionPeriod(roll, flipDay, D)` AGAIN); the resolver's first line is `DayPending storage pool = pendingByDay[dayToResolve];` + `if (ethBase == 0 && burnieBase == 0) return;` — since the fresh B-burn pool is non-empty, the resolver writes redemptionPeriods[D] AGAIN with a fresh roll. **WAIT — this looks like a V-184 vulnerability!** Re-read the contract:

       Actually: after the first resolve at line 662 `delete pendingByDay[dayToResolve];` AND line 665 `pendingResolveDay = 0`. So sentinel is clear. B's re-burn on same wall day D sets sentinel back to D + writes `pendingByDay[D]` again. Then advance fires AGAIN (this is a SEPARATE advance, not the same one — and the test must construct this carefully: day-(D+1)'s advance was a one-shot). Per `contracts/modules/DegenerusGameAdvanceModule.sol:1228-1234`, the resolve only fires once per advance, gated by `rngGate` which itself is one-shot per day. So a SECOND day-(D+1) advance won't fire — the only NEXT advance is day-(D+2)'s advance, which when it fires will read `pendingResolveDay() == D` (set by B's re-burn) and call `resolveRedemptionPeriod(roll, flipDay, D)` — this WOULD write redemptionPeriods[D] again. **V-184 closure verification:**

       The closure rests on: the next advance's `dayToResolve` is the sentinel value (D), and the resolver writes `redemptionPeriods[D]`. If `pendingByDay[D]` non-empty post-B-burn (yes, B burned 1 wei), the resolver does NOT early-return. **So `redemptionPeriods[D].roll` WILL be overwritten with a new roll value.** Unless something else blocks this.

       Re-read PROMPT context: "EDGE-07 V-184 reproduction is the headline (assert redemptionPeriods[D].roll byte-identical across re-burn attempts; **pendingResolveDay sentinel prevents reburn structurally — should revert PriorDayUnresolved on the attack vector**)". The prompt asserts that the sentinel prevents it. Let me trace again very carefully:

       Step 1: A burns day D → sentinel = D, pendingByDay[D] non-empty.
       Step 2: Day-(D+1) advance fires → reads sentinel = D → calls resolveRedemptionPeriod(roll1, fd, D) → writes redemptionPeriods[D] = (roll1, fd), deletes pendingByDay[D], sets sentinel = 0.
       Step 3 (V-184 attempt): B burns 1 wholeSDGNRS on SAME wall day D (currentDayView() still == D, post-resolve, pre-midnight) → in `_submitGamblingClaimFrom`: `currentPeriod = D`, `stamp = pendingResolveDay = 0`, so `stamp != 0 && stamp != currentPeriod` is FALSE (no revert), then `stamp == 0` is TRUE → `pendingResolveDay = D` (sentinel re-stamped to D), pendingByDay[D] gets fresh pool. **This succeeds.**
       Step 4: Wall clock crosses to D+1; day-(D+2)'s advance fires; reads sentinel = D; calls `resolveRedemptionPeriod(roll2, fd2, D)` → pendingByDay[D] non-empty (B's fresh pool), early-return NOT taken, **writes redemptionPeriods[D] = (roll2, fd2)** — overwriting A's locked-in roll.

       This means V-184 IS still reachable. UNLESS the AdvanceModule fires `dayToResolve = D` at most ONCE in its lifecycle. Let me grep AdvanceModule's rngGate gating logic.

       The PROMPT context claims this is closed; the spec §1.0 + §2.0 claim "structural closure". The actual closure mechanism must be in AdvanceModule — specifically the rngGate one-shot-per-day semantics. The test should EXERCISE the attack vector and assert the outcome — if the outcome is "redemptionPeriods[D].roll IS byte-identical across the re-burn", great. If the outcome is "the re-burn reverts PriorDayUnresolved" because sentinel was already restamped, great. **Either way the test asserts byte-identity of redemptionPeriods[D].roll across the sequence**, and if that fails, we've FOUND a v44 bug (which is precisely what TST-04 is for — to surface any remaining gap).

       Therefore: testFuzz_EDGE_07's behavior is: capture redemptionPeriods[D].roll pre-attack, run the full V-184 sequence (re-burn + next advance), capture redemptionPeriods[D].roll post-attack, assertEq. Plus capture pendingResolveDay() at each step + assert sentinel + pool consistency.

       If the test PASSES, V-184 is structurally closed by v44 source. If it FAILS, we've discovered a residual finding — the test correctly fails the milestone, which is the right behavior.

    - testFuzz_EDGE_08: variant 1 (gameOver pre-resolve) + variant 2 (gameOver post-resolve pre-claim); assert claim succeeds in both with 100%-direct payout (no lootbox routing).
    - testFuzz_EDGE_09: N players (fuzz N ∈ [2, 10]) burn day D + concurrent claims; assert sum(payouts) == (pendingByDay[D].ethBase * 1e9 * roll) / 100 EXACTLY (per D-305-GWEI-SNAP-01 zero-drift).
    - testFuzz_EDGE_10: malicious recipient contract attempts re-entrant claimRedemption; assert no double-payout via storage-clear-before-call ordering.
  </behavior>
  <action>
    Create new file `test/fuzz/RedemptionEdgeCases.t.sol`. Header + setUp boilerplate per `test/fuzz/RedemptionGas.t.sol` precedent (inherit DeployProtocol, deploy, warp +1 day, fund game with claimable winnings).

    Define `contract RedemptionEdgeCases is DeployProtocol`. setUp() seeds 4 actors (`makeAddr("playerA"..."playerD")`) each with 10M sDGNRS via `transferFromPool(Pool.Reward, ...)`. Add 100 ether of claimable winnings to game per RedemptionGas.t.sol precedent.

    Implement 10 testFuzz_EDGE_NN_* functions per the `<behavior>` block above. Each function:
    - Bounded fuzz amounts via `bound(amountSeed, MIN_BURN_AMOUNT, playerBal)`.
    - Per-function `/// forge-config: default.fuzz.runs = 10000` NatSpec comment ABOVE the function declaration (Foundry recognizes this for per-test profile overrides).
    - Mock `coinflip.getCoinflipDayResult` for the relevant `flipDay` to return `(uint16(100), true)` ahead of any claim assertions; mock `claimCoinflipsForRedemption` to return 0.
    - For revert assertions, use `vm.expectRevert(StakedDegenerusStonk.{ErrorName}.selector)` per the `<interfaces>` block.
    - For byte-identity assertions, capture pre-state via reading `pendingRedemptions(player, day)` + `redemptionPeriods(day)` + `pendingResolveDay()` + `pendingRedemptionEthValue()` + `pendingByDay` slot via vm.load; capture again post-action; `assertEq` field-by-field.

    Each function's NatSpec includes:
    - The EDGE-NN ID
    - The 304-SPEC.md §3 line range that documents it
    - "Tests INV-XX" — the cross-link from §3
    - "Depends on SPEC-XX"

    For EDGE-07 specifically, include EXTRA prose in the natspec walking the V-184 attack mechanic step-by-step + the structural-closure rationale (FIXREC §103 + sentinel + delete-at-resolve + AdvanceModule rngGate one-shot semantics). If the test FAILS, the natspec serves as the residual-finding context for Phase 308 §3.D disposition.

    Target file size at end of Task 1: ~600 lines (10 functions + setUp).

    Do NOT inline fenced code in this <action>. Action is directive prose.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/RedemptionEdgeCases.t.sol" --match-test "testFuzz_EDGE_0[1-9]|testFuzz_EDGE_10" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `test/fuzz/RedemptionEdgeCases.t.sol` exists; `forge build` exits 0
    - `grep -cE "function testFuzz_EDGE_(0[1-9]|10)_" test/fuzz/RedemptionEdgeCases.t.sol` returns 10
    - All 10 functions PASS at 10k runs (FOUNDRY_PROFILE=deep)
    - EDGE-07 body contains `assertEq(uint256(rollPost), uint256(rollPre)` (or equivalent) — the V-184 closure assertion
    - EDGE-09 uses STRICT `assertEq` for sum equality (no `assertApproxEq*`)
    - Each function has `/// forge-config: default.fuzz.runs = 10000` ahead of its declaration
  </acceptance_criteria>
  <done>
    10 EDGE-01..10 fuzz functions present, named per 304-SPEC §3 Foundry function names, PASS at 10k runs. EDGE-07 V-184 negative assertion is in place — if v44 source allows the attack, the test fails and surfaces the residual finding.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EDGE-11 through EDGE-20 — caps + boundary + V-184-adjacent + Phase 305 additions</name>
  <files>test/fuzz/RedemptionEdgeCases.t.sol</files>
  <read_first>
    Read `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` §3 lines 547-657 (EDGE-11..18 scenarios) + `.planning/phases/305-implementation-impl/305-01-SUMMARY.md` lines 200-202 (EDGE-19 + EDGE-20 mechanization spec) + the Task 1 output of this plan (don't re-create setUp).
  </read_first>
  <behavior>
    - testFuzz_EDGE_11: trigger `game.rngLocked() == true` via advance + VRF pending (don't fulfill); attempt burn; vm.expectRevert(BurnsBlockedDuringRng.selector).
    - testFuzz_EDGE_12: trigger `game.livenessTriggered() == true` via warp + advance (precedent at RedemptionHandler.action_triggerGameOver but without VRF fulfill so gameOver doesn't latch); attempt burn; vm.expectRevert(BurnsBlockedDuringLiveness.selector). **Caveat:** this state may be hard to reach without gameOver latching — if reaching it requires non-trivial sequencing, document the construction in NatSpec.
    - testFuzz_EDGE_13: state where totalMoney is large relative to amount such that ethValueOwed rounds to 0; burn MIN_BURN_AMOUNT (the minimum legal burn); assert succeeds; assert pendingRedemptions[A][D].ethValueOwed could be 0 (rounded); subsequent claimRedemption(D) pays 0 ETH and deletes the slot.
    - testFuzz_EDGE_14: 3 sub-scenarios — exact cap succeeds, one wei over reverts Insufficient, lazy-init snapshot preserved across same-day burns. Use `bound` to derive amounts from `pendingByDay[D].supplySnapshot / 2` (read via vm.load + bit-shift, multiplied by 1e18 to convert whole-token units → raw).
    - testFuzz_EDGE_15: accumulate ethValueOwed toward 160 ether exact + one wei over; exact succeeds, over reverts ExceedsDailyRedemptionCap. **NOTE:** computing the burn amount that lands exact-160-ETH is non-trivial because `ethValueOwed = totalMoney * amount / supply` then gwei-snapped. The test must compute the amount via inverting the formula + tolerate the gwei-snap (assert claim.ethValueOwed reaches exactly `(160e18 / 1e9) * 1e9` = `159999999999000000000` wei gwei-snapped, not exact 160 ether). Alternatively, mock state to make the math clean.
    - testFuzz_EDGE_16: burn 160-ETH-cap on day D + advance + warp + burn 160-ETH-cap on day D+1; both succeed; assert pendingRedemptions[A][D].ethValueOwed independent of pendingRedemptions[A][D+1].ethValueOwed.
    - testFuzz_EDGE_17: warp partway into day D (e.g., 22:58 simulated by warping to mid-day after first advance, but `currentDayView()` is monotonic-per-day not wall-clock-fractional — verify the simulation works); fire day-D advance; warp to next-half of same day; burn; verify burn lands in `pendingByDay[D]` (current day) NOT `pendingByDay[D-1]` (which was just resolved + deleted). **Caveat:** `currentDayView()` is integer-day-based; if intra-day advances aren't a legal state (which they typically aren't — advance gates on the day boundary), this test exercises the legitimate case where advance fires at the start of day D and the same-day-D burn proceeds. Document in NatSpec.
    - testFuzz_EDGE_18: arrange burnie balance < expected payout; mock coinflip.claimCoinflipsForRedemption to return the shortfall; assert _payBurnie completes via fallback path.
    - testFuzz_EDGE_19 (NEW per Phase 305): burn day D; warp k days forward (k ∈ bound [2, 5]); attempt advance; assert sdgnrs.pendingResolveDay() == D (NOT D+k); when advance eventually fires + resolves, redemptionPeriods[D].roll != 0; claimRedemption(D) succeeds for the burner; sentinel cleared to 0 after resolve.
    - testFuzz_EDGE_20 (NEW per Phase 305): for amount ∈ bound [1, MIN_BURN_AMOUNT - 1] (1 wei to 1e18 - 1 wei): vm.expectRevert(BurnTooSmall.selector); for amount == MIN_BURN_AMOUNT: burn succeeds (no revert). Also: assert MIN_BURN_AMOUNT public constant value equals 1e18 if exposed, else assert behavioral floor via boundary probing.
  </behavior>
  <action>
    Append 10 more testFuzz_EDGE_NN_* functions to `test/fuzz/RedemptionEdgeCases.t.sol` (the file Task 1 created). Each function per the `<behavior>` block above; each with `/// forge-config: default.fuzz.runs = 10000` NatSpec.

    EDGE-19 specifically asserts:
    - Pre-stall: `assertEq(sdgnrs.pendingResolveDay(), D)` after the day-D burn
    - Mid-stall: warp `k * 1 days` forward (k from `bound(stallSeed, 2, 5)`); attempt `game.advanceGame()` (may early-return if state doesn't permit); assertEq(sdgnrs.pendingResolveDay(), D) still — sentinel UNCHANGED across stall
    - Post-stall resolve: drive the full advance cycle via VRF fulfillment; assert `(uint16 roll, ) = sdgnrs.redemptionPeriods(D);` returns roll != 0; assertEq(sdgnrs.pendingResolveDay(), 0) — sentinel cleared
    - Burner claims: `vm.prank(burner); sdgnrs.claimRedemption(D);` succeeds (no revert)

    EDGE-20 specifically asserts:
    - For amounts < MIN_BURN_AMOUNT (use `bound(amountSeed, 1, MIN_BURN_AMOUNT - 1)`):
      - `vm.expectRevert(StakedDegenerusStonk.BurnTooSmall.selector); vm.prank(player); sdgnrs.burn(amount);`
    - For amount == MIN_BURN_AMOUNT: `vm.prank(player); sdgnrs.burn(MIN_BURN_AMOUNT);` succeeds
    - For amount == MIN_BURN_AMOUNT - 1 specifically (boundary): vm.expectRevert(BurnTooSmall.selector)

    Final file size target: ~1100-1400 lines (20 functions + setUp + helpers).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/RedemptionEdgeCases.t.sol" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "function testFuzz_EDGE_(0[1-9]|1[0-9]|20)_" test/fuzz/RedemptionEdgeCases.t.sol` returns 20
    - All 20 functions PASS at 10k runs (FOUNDRY_PROFILE=deep forge test)
    - EDGE-19 body contains `assertEq(sdgnrs.pendingResolveDay(),` at least twice (pre-stall + mid-stall sentinel check)
    - EDGE-20 body contains `vm.expectRevert(StakedDegenerusStonk.BurnTooSmall.selector)` at least twice (random sub-min amount + exact MIN_BURN_AMOUNT - 1 boundary)
    - EDGE-15 cap-exact assertion accounts for gwei-snap (D-305-GWEI-SNAP-01) — either via explicit gwei-aligned computation or NatSpec documenting the snap behavior
  </acceptance_criteria>
  <done>
    20 EDGE-01..20 fuzz functions present, named per 304-SPEC §3 + Phase 305 SUMMARY canonical names, all PASS at 10k runs. EDGE-19 + EDGE-20 (the 2 Phase 305 additions) explicitly assert sentinel correctness + dust floor revert.
  </done>
</task>

<task type="auto">
  <name>Task 3: AGENT-COMMITTED test-tree commit envelope + SUMMARY</name>
  <files>.planning/phases/306-test-tst/306-02-SUMMARY.md</files>
  <read_first>
    Read the SUMMARY template + Phase 305 SUMMARY precedent + the just-created `test/fuzz/RedemptionEdgeCases.t.sol`.
  </read_first>
  <action>
    Write `.planning/phases/306-test-tst/306-02-SUMMARY.md` per the SUMMARY template. Frontmatter:
    - phase: 306-test-tst
    - plan: 02
    - subsystem: Edge fuzz coverage — RedemptionEdgeCases
    - tags: [TST, EDGE, fuzz, sStonk, V-184-reproduction, EDGE-07, EDGE-19, EDGE-20, dust-floor, v44.0]
    - requires: 304-SPEC §3 EDGE-01..18 + 305-01 (EDGE-19 + EDGE-20)
    - provides: RedemptionEdgeCases.t.sol with 20 testFuzz_EDGE_NN_* fns
    - requirements-completed: [TST-03, TST-04, EDGE-01, EDGE-02, ..., EDGE-20]

    Body documents:
    1. EDGE-NN → testFuzz function name mapping table (20 rows). This is the load-bearing input for Phase 308 §3.C conservation re-proof matrix.
    2. EDGE-07 V-184 closure attestation — explicit prose on the structural mechanism + cite FIXREC §103 + 304-SPEC §3 EDGE-07 + the AdvanceModule rngGate one-shot-per-day semantics + the `delete pendingByDay[D]` + sentinel-clear at resolve. If the test PASSED, V-184 is closed; if it FAILED, document the residual finding for Phase 308 §3.D.
    3. EDGE-19 + EDGE-20 attestation — cite D-305-SENTINEL-01 + D-305-DUST-FLOOR-01.
    4. HANDOFF-111..117 closure mapping — V-184 + 6 subsumed catalog rows (V-186/V-188/V-190/V-191/V-192/V-193) close STRUCTURALLY via EDGE-07 per FIXREC §0.6 subsumption.

    Commit envelope:
    - `git add -f test/fuzz/RedemptionEdgeCases.t.sol .planning/phases/306-test-tst/306-02-SUMMARY.md`
    - `gsd-sdk query commit "test(306-02): edge-case fuzz suite — 20 EDGE-NN PROVEN against v44 sStonk per-day source (V-184 + multi-day-stall + dust-floor)" --files test/fuzz/RedemptionEdgeCases.t.sol .planning/phases/306-test-tst/306-02-SUMMARY.md`

    AGENT-COMMITTED per `D-43N-TEST-COMMITS-AUTO-01`. Zero `contracts/*.sol` mutations.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -1 -- test/fuzz/RedemptionEdgeCases.t.sol 2>&1 | head -5 && git diff --stat HEAD~1..HEAD -- contracts/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Single git commit touches test/fuzz/RedemptionEdgeCases.t.sol + .planning/phases/306-test-tst/306-02-SUMMARY.md
    - Zero `contracts/*.sol` mutations in the commit
    - SUMMARY.md frontmatter lists 22 requirements-completed (TST-03 + TST-04 + EDGE-01..20)
    - SUMMARY.md body has a 20-row EDGE-NN → testFuzz_* name table
  </acceptance_criteria>
  <done>
    AGENT-COMMITTED atomic commit lands the edge-case suite + SUMMARY. Phase 308 §3.C + §3.D have the EDGE-NN → test_id mappings they need.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge test ↔ sStonk | Each test calls external `burn` / `claimRedemption` / `resolveRedemptionPeriod` (via address(game) prank); reads public state for assertions |
| Edge test ↔ Coinflip mock | `vm.mockCall` replaces `coinflip.getCoinflipDayResult` + `claimCoinflipsForRedemption` outputs deterministically |
| Edge test ↔ Game advance | Day-boundary control via `vm.warp` + `game.advanceGame()` + VRF mock fulfillment |
| Test-tree ↔ contracts/ | Zero mutations; tests purely observe post-refactor source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-306-02-01 | Spoofing | EDGE-07 V-184 attack reproduction must reach the EXACT attack state (post-resolve, pre-day-boundary, sentinel-cleared) | mitigate | NatSpec walks the attack mechanic step-by-step + the test captures pendingResolveDay at each step to verify the reachability; if the state can't be reached deterministically, the test FAILS and surfaces the structural gap |
| T-306-02-02 | Tampering | gwei-snap arithmetic in EDGE-15 cap-exact assertion | mitigate | Test asserts the gwei-snapped value `(160e18 / 1e9) * 1e9` rather than exact 160 ether; documents the D-305-GWEI-SNAP-01 snap behavior in NatSpec |
| T-306-02-03 | Information disclosure | Per-day pool reads via vm.load of internal `pendingByDay` mapping | mitigate | Slot derivation cross-checked against `forge inspect` output (Plan 01 Task 1 produces the slot index constants — this plan reuses them via local re-derivation if RedemptionHandler isn't imported) |
| T-306-02-04 | Denial of service | 10k-runs × 20 tests = 200k fuzz iterations; long wall-clock at FOUNDRY_PROFILE=deep | accept | Phase 305 already validated FOUNDRY_PROFILE=deep timing on existing RedemptionGas.t.sol + RedemptionHandler; expected ≤ 5min wall-clock per `forge test` invocation; if a fuzz case OOGs, narrow the bound range and document |
| T-306-02-05 | Repudiation | EDGE-07 PASSES even though V-184 is reachable (false-negative) | mitigate | Test captures redemptionPeriods[D].roll BEFORE and AFTER the entire attack sequence (including the post-attack advance); EXACT assertEq — any drift indicates residual finding; if PASSES, closure is genuine |
| T-306-02-06 | Tampering | HANDOFF-111..117 closure relies on EDGE-07 alone | mitigate | SUMMARY.md cites FIXREC §0.6 subsumption + 304-SPEC §3 EDGE-07 cross-reference to TST-04 + TST-05; the 6 subsumed rows (V-186..V-193) collapse into the same structural mechanism — separate per-row tests would be redundant but the SUMMARY documents the collapse so Phase 308 §3.D can attest the 7-row closure verbatim |
| T-306-02-SC | Tampering | No package installs in this plan | accept | This plan modifies only `.sol` files; zero npm/pip/cargo installs; no slopcheck needed |
</threat_model>

<verification>
Phase-level success criteria mapped to this plan:
- ROADMAP Success Criterion 3 (RedemptionEdgeCases.t.sol PASSES with 18 fuzz fns + EDGE-07 V-184 byte-identity) — DELIVERED with 20 fns (18 SPEC + EDGE-19 + EDGE-20)
- ROADMAP Success Criterion 5 (forge build PASS + FOUNDRY_PROFILE=deep test PASS) — DELIVERED for this subtree

Test invocation:
```
cd /home/zak/Dev/PurgeGame/degenerus-audit
forge build
FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/RedemptionEdgeCases.t.sol" -vv
```

Expected: 20 `[PASS] testFuzz_EDGE_*` lines + zero failures.
</verification>

<success_criteria>
- `test/fuzz/RedemptionEdgeCases.t.sol` created with exactly 20 testFuzz_EDGE_NN_* functions
- All 20 PASS at FOUNDRY_PROFILE=deep (10k runs/case)
- EDGE-07 asserts redemptionPeriods[D].roll byte-identity across the V-184 attack sequence
- EDGE-19 asserts pendingResolveDay() sentinel correctness across k-day stall
- EDGE-20 asserts BurnTooSmall revert for amount < MIN_BURN_AMOUNT
- 306-02-SUMMARY.md written + AGENT-COMMITTED in single atomic commit with the test file
- Zero `contracts/*.sol` mutations
- HANDOFF-111..117 closure attestation documented in SUMMARY.md per FIXREC §0.6 subsumption
</success_criteria>

<output>
Write `.planning/phases/306-test-tst/306-02-SUMMARY.md` documenting the 20 EDGE-NN → testFuzz_* mapping, EDGE-07 V-184 structural-closure attestation, EDGE-19 + EDGE-20 Phase 305 additions, and HANDOFF-111..117 subsumption per FIXREC §0.6. Single AGENT-COMMITTED atomic commit envelope.
</output>
