---
phase: 306-test-tst
plan: 01
type: execute
wave: 1
subsystem: Foundry invariant harness — RedemptionAccounting
tags: [TST, INV, invariant-harness, sStonk, per-day-keying, single-pool-INV-13, ghost-vars, storage-slots-v44, v44.0]
depends_on: []
requirements: [TST-02, TST-07, INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, INV-09, INV-10, INV-11, INV-12, INV-13]
files_modified:
  - test/invariant/RedemptionAccounting.t.sol
  - test/fuzz/handlers/RedemptionHandler.sol
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the harness file at HEAD"
    - "`FOUNDRY_PROFILE=deep forge test --match-path 'test/invariant/RedemptionAccounting.t.sol'` PASSES at default invariant depth + runs (depth >= 15 calls per sequence; >= 256 runs)"
    - "13 invariant functions present and asserted after every handler call: invariant_INV_01_WriteOnceRoll, invariant_INV_02_EthConservationExact, invariant_INV_03_BurnieConservationExact, invariant_INV_04_PerDayBaseCorrectness, invariant_INV_05_PerDayCumulativeCorrectness, invariant_INV_06_NoCrossPlayerRollManipulation, invariant_INV_07_NoSelfRollManipulation, invariant_INV_08_PreAdvanceGapBurnSafety, invariant_INV_09_SkippedAdvanceRecovery, invariant_INV_10_PerDaySupplyCap, invariant_INV_11_PerPlayerPerDayEvCap, invariant_INV_12_GameOverMidPending, invariant_INV_13_SinglePoolPending"
    - "Handler exposes random-action emitter across {action_burn, action_advanceDay, action_claim, action_triggerGameOver} with multi-actor support (>= 4 actors seeded with sDGNRS + ETH); selectors registered via targetContract + targetSelector"
    - "INV-02 + INV-03 assert EXACT equality (no dust tolerance) per D-305-GWEI-SNAP-01 (gcd(1e9, 100) = 100 means × roll / 100 is exact for gwei-aligned wei values); cumulative scalar drift is structurally zero"
    - "INV-13 single-pool invariant asserted: for all days D scanned across emitted action history, at most one D has `pendingByDay[D].ethBase != 0 || pendingByDay[D].burnieBase != 0`; matches `pendingResolveDay` sentinel if non-zero"
    - "Per-day ghost storage tracks (player, day) burn events + per-day pool aggregate + first-write redemptionPeriods[D].roll value; allows write-once + per-day-sum + cross-player-roll assertions"
    - "Storage slot constants in `RedemptionHandler.sol` refreshed against v44 layout (deriving via `forge inspect contracts/StakedDegenerusStonk.sol:StakedDegenerusStonk storage-layout` once during execution); stale v43 constants (SLOT_PENDING_BURNIE=10, SLOT_PERIOD_INDEX=14, SLOT_PERIOD_BURNED=15, SLOT_SUPPLY_SNAPSHOT=13) replaced with v44 equivalents"
    - "AGENT-COMMITTED test-tree commit per `D-43N-TEST-COMMITS-AUTO-01` lineage; zero `contracts/*.sol` mutations"
  artifacts:
    - path: "test/invariant/RedemptionAccounting.t.sol"
      provides: "Foundry invariant harness with 13 invariant_INV_NN_* functions + stateful handler bound to RedemptionHandler"
      contains: "invariant_INV_01_WriteOnceRoll"
    - path: "test/fuzz/handlers/RedemptionHandler.sol"
      provides: "Refreshed v44-compatible handler — per-(player, day) ghost vars; per-day pool ghost; single-pool sentinel ghost; v44 storage slot constants; multi-day claim site (claims arbitrary resolved days from emitted history)"
      contains: "ghost_perDay_ethBase"
  key_links:
    - from: "test/invariant/RedemptionAccounting.t.sol::setUp"
      to: "RedemptionHandler + targetContract + targetSelector(s)"
      via: "Foundry invariant-test selector registration"
      pattern: "targetContract\\(address\\(handler\\)\\)"
    - from: "test/invariant/RedemptionAccounting.t.sol::invariant_INV_01_WriteOnceRoll"
      to: "handler.ghost_perDay_firstRoll[D]"
      via: "asserts redemptionPeriods[D].roll == ghost_perDay_firstRoll[D] for every D handler has written"
      pattern: "assertEq\\(uint256\\(roll\\), uint256\\(ghost_perDay_firstRoll\\["
    - from: "test/invariant/RedemptionAccounting.t.sol::invariant_INV_13_SinglePoolPending"
      to: "sdgnrs.pendingResolveDay()"
      via: "scans handler.daysWritten[] + asserts at most one has non-zero pool + matches sentinel"
      pattern: "sdgnrs\\.pendingResolveDay\\(\\)"
    - from: "test/fuzz/handlers/RedemptionHandler.sol::action_burn"
      to: "ghost_perDay_ethBase[day] + ghost_perDay_pendingResolveDay"
      via: "ghost update on every successful burn (composite-keyed by (player, day))"
      pattern: "ghost_perDay_ethBase\\[currentDay\\] \\+= ethValueOwed"
---

<objective>
Ship the Foundry invariant harness that mechanizes all 13 INV-NN formal accounting properties (12 SPEC-locked + INV-13 added in Phase 305) against the post-refactor `contracts/StakedDegenerusStonk.sol` per the 304-SPEC §1 invariant model verbatim. New file `test/invariant/RedemptionAccounting.t.sol` wires a stateful `RedemptionHandler` (extended this plan with per-day ghost storage + v44 slot constants) into Foundry's invariant fuzzer; one `invariant_INV_NN_*` function per INV-NN is asserted after every random action drawn from {burn, advance, claim, gameOver, transfer, approve, admin-action}. INV-02 + INV-03 assert EXACT equality (no dust tolerance) per the D-305-GWEI-SNAP-01 zero-drift accounting result — tightening the SPEC §1's original "dust-bounded" framing. INV-13 single-pool invariant is the load-bearing v44.0 closure assertion alongside INV-01: at most one `pendingByDay[D]` may be non-empty at any reachable state, structurally enforced by the `pendingResolveDay` sentinel + `PriorDayUnresolved` revert in `_submitGamblingClaimFrom`.

Purpose: TST-02 + INV-01..13 closure for the v44.0 milestone. The 13 invariant assertions are what FINDINGS-v44.0.md §3.F formal invariant attestation matrix (Phase 308) cites verbatim as the "proven by Foundry harness" attestation for each INV-NN row. Without this plan landing, §3.F has no test_id to cite and Phase 308 closure-verdict math fails. Plan 01 has no downstream contract dependencies and runs in Wave 1 fully parallel with Plans 02/03/04/05 — modifies only test/invariant/ + test/fuzz/handlers/ paths.

Output: One new file `test/invariant/RedemptionAccounting.t.sol` (~700-900 lines: setUp + 13 invariant functions + ghost var readers). One updated file `test/fuzz/handlers/RedemptionHandler.sol` (~450-550 lines after edits — per-day ghost vars + v44 slot constants + multi-day claim selector + handler getter helpers for ghosts read by invariant fns). AGENT-COMMITTED test-tree commit envelope per `D-43N-TEST-COMMITS-AUTO-01`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/305-implementation-impl/305-01-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/interfaces/IStakedDegenerusStonk.sol
@test/fuzz/handlers/RedemptionHandler.sol
@test/fuzz/helpers/DeployProtocol.sol
@test/fuzz/RedemptionGas.t.sol

<interfaces>
<!-- Key contracts the executor must use. Extracted from contracts/StakedDegenerusStonk.sol post-Phase-305-refactor (HEAD commit 213f9184). -->

Storage layout (v44.0) — first-class slots the invariant harness reads via getters or vm.load:

```solidity
// In StakedDegenerusStonk:
uint256 public totalSupply;
mapping(address => uint256) public balanceOf;
uint256[5] private poolBalances;
struct PendingRedemption { uint96 ethValueOwed; uint96 burnieOwed; uint16 activityScore; } // 1 slot per (player, day)
struct RedemptionPeriod  { uint16 roll; uint32 flipDay; }                                   // 1 slot per day; roll==0 means unresolved
struct DayPending        { uint64 ethBase; uint64 burnieBase; uint64 supplySnapshot; uint64 burned; } // 1 slot; ethBase + burnieBase in GWEI; supplySnapshot + burned in WHOLE TOKENS

mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions; // composite key per SPEC-02
mapping(uint32 => RedemptionPeriod) public redemptionPeriods;                       // public getter exposed
uint256 public pendingRedemptionEthValue;                                            // public; wei
uint256 internal pendingRedemptionBurnie;                                            // INTERNAL — read via storage-slot derivation; raw BURNIE units (no divisor)
mapping(uint32 => DayPending) internal pendingByDay;                                 // INTERNAL — read via storage-slot derivation
uint32 public pendingResolveDay;                                                     // public; INV-13 sentinel (0 = none)

uint256 private constant MAX_DAILY_REDEMPTION_EV = 160 ether;
uint256 private constant MIN_BURN_AMOUNT = 1e18;
```

Public/external functions the harness + handler invoke:

```solidity
function burn(uint256 amount) external returns (uint256, uint256, uint256);
function claimRedemption(uint32 day) external;
function hasPendingRedemptions(uint32 day) external view returns (bool);
function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external; // onlyGame
function pendingResolveDay() external view returns (uint32);
function pendingRedemptionEthValue() external view returns (uint256);
// Public auto-getters:
//   pendingRedemptions(address, uint32) returns (uint96 ethValueOwed, uint96 burnieOwed, uint16 activityScore)
//   redemptionPeriods(uint32) returns (uint16 roll, uint32 flipDay)
```

Errors the harness asserts revert paths against:

```solidity
error Unauthorized();
error Insufficient();
error TransferFailed();
error BurnsBlockedDuringRng();
error BurnsBlockedDuringLiveness();
error NoClaim();
error NotResolved();
error ExceedsDailyRedemptionCap();
error PriorDayUnresolved();   // INV-13 enforcement (D-305-SENTINEL-01)
error BurnTooSmall();          // MIN_BURN_AMOUNT floor (D-305-DUST-FLOOR-01)
```

AdvanceModule call shape (the handler's advanceDay action drives this):

```solidity
// In DegenerusGameAdvanceModule (3 sites at :1228+:1234, :1294+:1300, :1327+:1333):
uint32 toResolve = sdgnrs.pendingResolveDay();
if (toResolve != 0) {
    sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, toResolve);
}
```

DegenerusGame view surface (the handler reads these for state):

```solidity
function currentDayView() external view returns (uint32);
function rngLocked() external view returns (bool);
function gameOver() external view returns (bool);
function livenessTriggered() external view returns (bool);
function rngWordForDay(uint32 day) external view returns (uint256);
function playerActivityScore(address player) external view returns (uint256);
function claimableWinningsOf(address account) external view returns (uint256);
function advanceGame() external;
```
</interfaces>

<patterns>
<!-- Patterns the harness MUST follow per existing v43 audit-repo conventions. -->

1. **DeployProtocol scaffold** — `test/fuzz/helpers/DeployProtocol.sol` deploys 4 mocks + 23 protocol contracts in `_deployProtocol()`; inherit + call `_deployProtocol()` in setUp. NO ad-hoc deployment.

2. **vm.warp + vm.prank** — `test/fuzz/RedemptionGas.t.sol` precedent for day-boundary control: `vm.warp(block.timestamp + 1 days)` then `try game.advanceGame() {} catch {}`. VRF fulfillment via `vrf.fulfillRandomWords(reqId, randomWord)` on mocks.

3. **Mock claimable winnings + sDGNRS funding** — `test_gas_burn_gambling` precedent at `test/fuzz/RedemptionGas.t.sol:31-38`:
   ```
   vm.deal(address(game), 100 ether);
   bytes32 claimableSlot = keccak256(abi.encode(address(sdgnrs), uint256(7)));
   vm.store(address(game), claimableSlot, bytes32(uint256(100 ether)));
   uint256 slot1Val = uint256(vm.load(address(game), bytes32(uint256(1))));
   slot1Val = (slot1Val & type(uint128).max) | (uint256(100 ether) << 128);
   vm.store(address(game), bytes32(uint256(1)), bytes32(slot1Val));
   ```

4. **Multi-actor handler** — `RedemptionHandler.sol` precedent: array of N actors seeded via `transferFromPool(Pool.Reward, actor, X)` from `address(game)` prank.

5. **vm.load for internal storage** — `pendingByDay[D]` is INTERNAL. Use `keccak256(abi.encode(uint256(D), uint256(SLOT_PENDING_BY_DAY)))` then `vm.load` to read the packed slot; decode 4×uint64 via bit-shifts (ethBase = raw & type(uint64).max; burnieBase = (raw >> 64) & ..; supplySnapshot = (raw >> 128) & ..; burned = (raw >> 192) & ..). `pendingResolveDay` is PUBLIC — call the getter directly. `pendingRedemptionBurnie` is INTERNAL — read via `vm.load(address(sdgnrs), bytes32(SLOT_PENDING_REDEMPTION_BURNIE))`.

6. **Slot constant derivation** — Run `forge inspect contracts/StakedDegenerusStonk.sol:StakedDegenerusStonk storage-layout` ONCE during Task 1 execution; record the exact slot indices for `pendingRedemptionBurnie` + `pendingByDay` + `pendingResolveDay` in the handler's constants block. DO NOT GUESS slot numbers.

7. **Mock coinflip resolution** — For claim tests that must succeed, mock `coinflip.getCoinflipDayResult` to return `(uint16(100), true)` so `flipResolved == true` + `flipWon == true` triggers the full-claim path including `delete pendingRedemptions[msg.sender][day]`. Precedent at `test/fuzz/RedemptionGas.t.sol:97-109`.

8. **Foundry invariant config** — `foundry.toml` `deep` profile should already configure `runs = 256` + `depth = 15`+ for invariants. If not present in `foundry.toml`, the harness's contract-level `/// forge-config: deep.invariant.runs = 256` natspec-style comments configure per-test.
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh RedemptionHandler with v44 storage + per-day ghost vars + multi-actor + multi-day claim</name>
  <files>test/fuzz/handlers/RedemptionHandler.sol</files>
  <read_first>
    Read `test/fuzz/handlers/RedemptionHandler.sol` (currently 328 lines, v43-style ghost vars + stale slot constants) + `contracts/StakedDegenerusStonk.sol` lines 217-300 (struct shapes + slot order) + `test/fuzz/helpers/DeployProtocol.sol` (full deployment scaffold).
  </read_first>
  <action>
    Refactor `test/fuzz/handlers/RedemptionHandler.sol` for the v44 per-day-keyed surface. Specific deltas:

    1. **Derive v44 storage slot constants.** Run `forge inspect contracts/StakedDegenerusStonk.sol:StakedDegenerusStonk storage-layout` once. Replace the existing constants block at lines 64-70 with v44 equivalents: `SLOT_PENDING_REDEMPTION_ETH_VALUE` (the public uint256 cumulative-scalar), `SLOT_PENDING_REDEMPTION_BURNIE` (internal uint256), `SLOT_PENDING_BY_DAY` (the mapping base for `pendingByDay`), `SLOT_PENDING_REDEMPTIONS` (the outer mapping base for `pendingRedemptions`), `SLOT_REDEMPTION_PERIODS` (mapping base for `redemptionPeriods`), `SLOT_PENDING_RESOLVE_DAY` (the sentinel — also public, prefer the getter). Document each constant with the slot index from forge inspect output. Remove the stale `SLOT_PERIOD_INDEX = 14` + `SLOT_PERIOD_BURNED = 15` + `SLOT_SUPPLY_SNAPSHOT = 13` (the v43 storage layout these reference is GONE in v44).

    2. **Add per-day ghost vars.** Add mappings:
       - `mapping(uint32 => uint256) public ghost_perDay_ethBase` — running sum of `ethValueOwed` written into `pendingByDay[D]` by handler-triggered burns (kept in WEI, not gwei, to reconcile against the cumulative scalar exactly).
       - `mapping(uint32 => uint256) public ghost_perDay_burnieBase` — symmetric for BURNIE (in RAW BURNIE units, no divisor).
       - `mapping(uint32 => mapping(address => uint256)) public ghost_perDay_perPlayer_ethValueOwed` — per-(player, day) running sum for INV-04 + INV-11 reconciliation.
       - `mapping(uint32 => mapping(address => uint256)) public ghost_perDay_perPlayer_burnieOwed` — symmetric.
       - `mapping(uint32 => uint16) public ghost_perDay_firstRoll` — first-write value of `redemptionPeriods[D].roll`; written from `_checkResolvedPeriods` upon detecting a fresh resolve; once non-zero, NEVER overwritten in the ghost (INV-01 anchor).
       - `mapping(uint32 => uint32) public ghost_perDay_firstFlipDay` — symmetric for `flipDay`.
       - `uint32[] public ghost_daysWritten` — append-only set of days that ever received a burn (used by invariant fns to bound the cross-day scan). Plus a `mapping(uint32 => bool) ghost_dayWritten` for set-membership.
       - `mapping(uint32 => bool) public ghost_dayResolved` — flips true on first resolve detection per day.
       - `mapping(uint32 => mapping(address => bool)) public ghost_claimDone` — flips true on full-claim per (player, day); flips back to false would indicate INV-07 violation (but it never should — assertion is INSERT-ONLY).
       - `mapping(uint32 => mapping(address => uint96)) public ghost_perPlayer_locked_ethValueOwed` — snapshot of `claim.ethValueOwed` at the LAST burn of the day for (player, D); INV-07 reads this and asserts byte-identity at every subsequent reachable state until `ghost_claimDone[D][player] == true`.

    3. **Update action_burn.** After the `try sdgnrs.burn(amount) {} catch {}` block, on success:
       - Read `uint32 currentDay = game.currentDayView();`.
       - Read the post-burn composite-keyed claim: `(uint96 ethOwed, uint96 burnieOwed, uint16 score) = sdgnrs.pendingRedemptions(currentActor, currentDay);`.
       - Compute the per-burn delta: `uint256 ethDelta = uint256(ethOwed) - ghost_perDay_perPlayer_ethValueOwed[currentDay][currentActor];` (and symmetric for burnie).
       - Update per-day ghosts: `ghost_perDay_ethBase[currentDay] += ethDelta; ghost_perDay_burnieBase[currentDay] += burnieDelta;`.
       - Update per-(player, day) ghosts: `ghost_perDay_perPlayer_ethValueOwed[currentDay][currentActor] = uint256(ethOwed); ghost_perDay_perPlayer_burnieOwed[...] = uint256(burnieOwed);`.
       - Update locked-snapshot: `ghost_perPlayer_locked_ethValueOwed[currentDay][currentActor] = ethOwed;` (INV-07 anchor — re-stamped on every same-day burn so the snapshot lands on the LAST burn).
       - Append `currentDay` to `ghost_daysWritten` if not already present.

    4. **Update action_advanceDay.** After the second `try game.advanceGame() {} catch {}`, scan all `D` in `ghost_daysWritten` and detect newly-resolved days by reading `(uint16 roll, uint32 flipDay) = sdgnrs.redemptionPeriods(D);` — if `roll != 0 && !ghost_dayResolved[D]`, then assert `25 <= roll && roll <= 175` (defensive), set `ghost_perDay_firstRoll[D] = roll; ghost_perDay_firstFlipDay[D] = flipDay; ghost_dayResolved[D] = true; ghost_periodsResolved++;`. Remove the v43-only `_checkResolvedPeriods` body — replace with this new per-day variant.

    5. **Update action_claim to randomize claim day.** Replace the hardcoded `claimDay = today - 1` with a fuzz-driven random pick from `ghost_daysWritten` filtered by `ghost_dayResolved[D] == true && !ghost_claimDone[D][currentActor]`. Take an additional `uint256 daySeed` arg to action_claim (signature: `action_claim(uint256 actorSeed, uint256 daySeed)`). If no claimable day exists for the chosen actor, early-return. On successful claim, set `ghost_claimDone[claimDay][currentActor] = true;`.

    6. **Add invariant-mode adversarial advance.** Add a new action `action_burnOnPreviousDay(uint256 actorSeed)` that attempts to burn after an advance has shifted `currentDayView()` — this exercises INV-08 + INV-13 (`PriorDayUnresolved` revert). The handler simply tries `sdgnrs.burn(MIN_BURN_AMOUNT)` and tracks the revert via try/catch — no assertion in the handler (the assertion lives in the invariant fn).

    7. **Mock VRF + coinflip + claimable winnings.** In the constructor, ensure VRF mock works (action_advanceDay already does this); additionally mock `coinflip.getCoinflipDayResult` to return `(uint16(100), true)` for ANY day so claims that target resolved days complete the full-payout path. Use `vm.mockCall(address(coinflip), abi.encodeWithSelector(IBurnieCoinflipPlayer.getCoinflipDayResult.selector), abi.encode(uint16(100), true));` once in the constructor.

    8. **Expose ghost-getter helpers** required by invariant fns:
       - `function getDaysWrittenCount() external view returns (uint256) { return ghost_daysWritten.length; }`
       - `function getDayWritten(uint256 i) external view returns (uint32) { return ghost_daysWritten[i]; }`
       - `function getActorCount()` + `getActor(uint256 i)` — already present, preserve.

    Preserve `_trackSupplyDelta` + existing call counters. Do NOT remove the existing `ghost_*` counters (totalEthClaimed / totalBurnieClaimed / etc.) — invariant fns may still use them, and removing risks breaking unrelated coverage.

    The file should be ~450-550 lines after edits. Maintain `// SPDX-License-Identifier: AGPL-3.0-only` + `pragma solidity ^0.8.26;` header.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && grep -cE "^    function action_(burn|advanceDay|claim|triggerGameOver|burnOnPreviousDay)" test/fuzz/handlers/RedemptionHandler.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (handler compiles against v44 sStonk + DeployProtocol)
    - Action selector count >= 5 (burn + advanceDay + claim + triggerGameOver + burnOnPreviousDay)
    - All 8 per-day ghost mappings present (grep verifiable)
    - No reference to `redemptionPeriodIndex` / `pendingRedemptionEthBase` / `pendingRedemptionBurnieBase` / `redemptionPeriodSupplySnapshot` / `redemptionPeriodBurned` (the 5 deleted v43 slots) anywhere in the handler
    - `forge inspect` slot constants documented inline above each `SLOT_*` declaration
  </acceptance_criteria>
  <done>
    RedemptionHandler.sol compiles against v44 sStonk, exposes ≥ 5 action selectors and the 8 per-day ghost mappings + ghost getters, and emits `PriorDayUnresolved`-triggering action_burnOnPreviousDay for INV-08/INV-13 coverage. Zero references to deleted v43 slot names remain.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RedemptionAccounting.t.sol with 13 invariant_INV_NN_* functions</name>
  <files>test/invariant/RedemptionAccounting.t.sol</files>
  <read_first>
    Read `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` §1 INV-01..12 (lines 78-296) + `.planning/phases/305-implementation-impl/305-01-SUMMARY.md` (INV-13 + zero-drift accounting result) + the refreshed `test/fuzz/handlers/RedemptionHandler.sol` from Task 1.
  </read_first>
  <behavior>
    For every invariant fn, the assertion must hold after EVERY handler action — that's Foundry's default semantics for `invariant_*`. Per-INV behavior:
    - invariant_INV_01_WriteOnceRoll: for every D in handler.ghost_daysWritten where ghost_dayResolved[D] == true, assert `sdgnrs.redemptionPeriods(D).roll == ghost_perDay_firstRoll[D]` and `.flipDay == ghost_perDay_firstFlipDay[D]`.
    - invariant_INV_02_EthConservationExact: assert `pendingRedemptionEthValue == (sum over D in ghost_daysWritten with !ghost_dayResolved[D] of pendingByDay[D].ethBase * 1e9) + (sum over D in ghost_daysWritten with ghost_dayResolved[D] of (sum over players P with !ghost_claimDone[D][P] of pendingRedemptions[P][D].ethValueOwed * redemptionPeriods[D].roll / 100))`. **EXACT EQUALITY** — no dust tolerance, per D-305-GWEI-SNAP-01.
    - invariant_INV_03_BurnieConservationExact: assert `pendingRedemptionBurnie == sum over D in ghost_daysWritten with !ghost_dayResolved[D] of pendingByDay[D].burnieBase * 1e9` (BURNIE netted at resolve per existing v43 semantics, so the resolved-but-unclaimed term is structurally zero — verify EXACT equality).
    - invariant_INV_04_PerDayBaseCorrectness: for every D in ghost_daysWritten where !ghost_dayResolved[D] && pendingByDay[D].ethBase != 0: assert `pendingByDay[D].ethBase * 1e9 == sum over actors A of pendingRedemptions[A][D].ethValueOwed`. EXACT.
    - invariant_INV_05_PerDayCumulativeCorrectness: same as INV-02 but reorganized to test the cumulative-vs-per-day-sum identity directly.
    - invariant_INV_06_NoCrossPlayerRollManipulation: for every (P, D) where ghost_perDay_perPlayer_ethValueOwed[D][P] != 0 && ghost_dayResolved[D]: assert `redemptionPeriods[D].roll == ghost_perDay_firstRoll[D]` (subsumed by INV-01; included as a cross-link assertion for §3.F).
    - invariant_INV_07_NoSelfRollManipulation: for every (P, D) where ghost_perPlayer_locked_ethValueOwed[D][P] != 0 && !ghost_claimDone[D][P]: assert `pendingRedemptions[P][D].ethValueOwed == ghost_perPlayer_locked_ethValueOwed[D][P]`. EXACT byte-identity until claim.
    - invariant_INV_08_PreAdvanceGapBurnSafety: subsumed by INV-13 + INV-04 — exercised by action_burnOnPreviousDay. The invariant body asserts: for all D < currentDayView() where ghost_dayResolved[D] == true: `pendingByDay[D].ethBase == 0 && pendingByDay[D].burnieBase == 0 && pendingByDay[D].supplySnapshot == 0 && pendingByDay[D].burned == 0` (delete-at-resolve per SPEC-04 (c) cleared the slot).
    - invariant_INV_09_SkippedAdvanceRecovery: trivialized by INV-13 — at most one D has non-empty pendingByDay; the sentinel always names it; no overwrite possible. Assertion: `if (pendingResolveDay != 0) { for every D != pendingResolveDay in ghost_daysWritten: assert pendingByDay[D].ethBase == 0; }`.
    - invariant_INV_10_PerDaySupplyCap: for every D in ghost_daysWritten where pendingByDay[D].supplySnapshot != 0: assert `pendingByDay[D].burned * 1e18 <= (pendingByDay[D].supplySnapshot * 1e18) / 2`. The `*1e18` is because pool fields are in whole-token units (D-305-STRUCT-TIGHTEN-01).
    - invariant_INV_11_PerPlayerPerDayEvCap: for every (P, D) where pendingRedemptions[P][D].ethValueOwed != 0: assert `pendingRedemptions[P][D].ethValueOwed <= MAX_DAILY_REDEMPTION_EV` (160 ether literal).
    - invariant_INV_12_GameOverMidPending: per SPEC-04 (a) gracefully-resolve lock — even after game.gameOver() == true, claims for resolved days succeed; the invariant asserts no orphaned (non-zero pendingByDay where dayResolved == false) exists post-gameOver — the eventual advance still resolves them. Body: `if (game.gameOver()) { for every D in ghost_daysWritten where pendingByDay[D].ethBase != 0 && !ghost_dayResolved[D]: assert D == pendingResolveDay; }` (the one remaining pool is still validly named by sentinel).
    - invariant_INV_13_SinglePoolPending: count days D in ghost_daysWritten where (pendingByDay[D].ethBase != 0 || pendingByDay[D].burnieBase != 0); assert count <= 1; if count == 1, the one non-empty D MUST equal sdgnrs.pendingResolveDay(). If pendingResolveDay == 0, count MUST be 0.

    For INV-04 + INV-05 + INV-09 + INV-13's per-D scans, bound the loop by `min(handler.getDaysWrittenCount(), 100)` to avoid OOG at deep invariant runs.
  </behavior>
  <action>
    Create new file `test/invariant/RedemptionAccounting.t.sol`. Header:
    - `// SPDX-License-Identifier: AGPL-3.0-only` + `pragma solidity ^0.8.26;`
    - Import `DeployProtocol` from `../fuzz/helpers/DeployProtocol.sol` + `RedemptionHandler` from `../fuzz/handlers/RedemptionHandler.sol` + `StakedDegenerusStonk` from `../../contracts/StakedDegenerusStonk.sol`.

    Define `contract RedemptionAccounting is DeployProtocol`. In `setUp()`:
    - `_deployProtocol();`
    - Warp + fund game per RedemptionGas.t.sol precedent (vm.deal + vm.store for game.claimableWinnings).
    - `handler = new RedemptionHandler(sdgnrs, game, mockVRF, coin, 4);` (4 actors).
    - Register handler selectors via `bytes4[] memory selectors = new bytes4[](5);` listing `action_burn / action_advanceDay / action_claim / action_triggerGameOver / action_burnOnPreviousDay`; `targetSelector(FuzzSelector({addr: address(handler), selectors: selectors}));`.
    - `targetContract(address(handler));`

    Implement 13 invariant functions per the `<behavior>` block above. Each fn body executes the assertion logic per its INV-NN definition. Use `MIN_BURN_AMOUNT = 1e18` + `MAX_DAILY_REDEMPTION_EV = 160 ether` as local constants for INV-10 + INV-11 (mirroring sStonk).

    For each invariant fn, include a NatSpec comment block citing:
    - The INV-NN ID
    - The 304-SPEC.md §1 line range that documents it
    - The Phase 305 source mechanism that makes it hold (D-305-* IDs for INV-13 + INV-02 exact + INV-03 exact + INV-10 ceiling-divide)

    Add a final helper section at the bottom: `function _readPendingByDay(uint32 day) internal view returns (uint64 ethBase, uint64 burnieBase, uint64 supplySnapshot, uint64 burned)` reads the packed slot via `keccak256(abi.encode(uint256(day), uint256(SLOT_PENDING_BY_DAY)))` + `vm.load` + bit-shifts. Use the slot constant from Task 1's `RedemptionHandler.SLOT_PENDING_BY_DAY` exposure (add it as a public constant in the handler OR derive locally — pick one and document).

    Add `function _readPendingRedemptionBurnie() internal view returns (uint256)` for the internal scalar.

    File length target: ~700-900 lines.

    Do NOT write fenced code in this <action> — the action is directive prose only; the inline code shown above is reference for the executor.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && FOUNDRY_PROFILE=deep forge test --match-path "test/invariant/RedemptionAccounting.t.sol" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `test/invariant/RedemptionAccounting.t.sol` exists and `forge build` exits 0
    - `grep -c "^    function invariant_INV_" test/invariant/RedemptionAccounting.t.sol` returns 13
    - Each `invariant_INV_NN_*` function name matches the canonical list in `must_haves.truths` line 3 (verify with `grep -oE "invariant_INV_[0-9]{2}_[A-Za-z]+"` returning all 13 unique names)
    - `FOUNDRY_PROFILE=deep forge test --match-path "test/invariant/RedemptionAccounting.t.sol"` exits 0 (all 13 invariants pass at default invariant depth/runs)
    - No `assertApproxEq*` calls in INV-02 / INV-03 / INV-04 bodies — strict `assertEq` only (D-305-GWEI-SNAP-01 zero-drift)
    - For INV-10, the assertion uses `*1e18` to convert pool whole-token units back to raw before comparing
  </acceptance_criteria>
  <done>
    13 INV-01..13 invariant functions present, named per the canonical list, and PASS at FOUNDRY_PROFILE=deep against the v44 sStonk source. EXACT equality assertions (no dust tolerance) for INV-02..05 per D-305-GWEI-SNAP-01.
  </done>
</task>

<task type="auto">
  <name>Task 3: AGENT-COMMITTED test-tree commit envelope</name>
  <files>.planning/phases/306-test-tst/306-01-SUMMARY.md</files>
  <read_first>
    Read `.planning/phases/305-implementation-impl/305-01-SUMMARY.md` for the AGENT-COMMITTED commit format precedent + the SPEC §3.F + §3.C citations the Phase 308 TERMINAL will consume.
  </read_first>
  <action>
    Write `.planning/phases/306-test-tst/306-01-SUMMARY.md` per the @$HOME/.claude/get-shit-done/templates/summary.md template + audit-repo conventions. Frontmatter must include:
    - `phase: 306-test-tst`
    - `plan: 01`
    - `subsystem: Foundry invariant harness — RedemptionAccounting`
    - `tags: [TST, INV, invariant-harness, sStonk, v44.0]`
    - `requires`: 304-SPEC §1 INV-01..12 + 305-01 (INV-13 + zero-drift)
    - `provides`: RedemptionAccounting.t.sol invariant harness with 13 PROVEN INV-NN functions
    - `requirements-completed: [TST-02, INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, INV-09, INV-10, INV-11, INV-12, INV-13]`

    Body documents:
    1. The 13 invariant function names + INV-NN → test_id mapping (this is the load-bearing input for Phase 308 §3.F attestation matrix).
    2. The 8 ghost-var mappings added to RedemptionHandler + which INV each anchors.
    3. The 5 handler action selectors registered + behavior summary.
    4. The 4 SLOT_* constants derived from `forge inspect` output (record exact slot indices).
    5. EXACT-equality attestation for INV-02..05 — cite D-305-GWEI-SNAP-01 as the source mechanism.
    6. INV-13 single-pool attestation — cite D-305-SENTINEL-01 + `PriorDayUnresolved` revert.

    Commit both files (handler + harness + this SUMMARY) in a single atomic envelope:
    - `git add -f test/invariant/RedemptionAccounting.t.sol test/fuzz/handlers/RedemptionHandler.sol .planning/phases/306-test-tst/306-01-SUMMARY.md`
    - `gsd-sdk query commit "test(306-01): Foundry invariant harness — 13 INV-NN PROVEN against v44 sStonk per-day source" --files test/invariant/RedemptionAccounting.t.sol test/fuzz/handlers/RedemptionHandler.sol .planning/phases/306-test-tst/306-01-SUMMARY.md`

    Per `feedback_no_contract_commits.md` clarified policy + `D-43N-TEST-COMMITS-AUTO-01` lineage — test/ + .planning/ commit autonomously (NO user approval gate). Zero `contracts/*.sol` mutations in this plan envelope.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -1 -- test/invariant/RedemptionAccounting.t.sol 2>&1 | head -5 && git diff --stat HEAD~1..HEAD -- contracts/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Single git commit exists touching: test/invariant/RedemptionAccounting.t.sol + test/fuzz/handlers/RedemptionHandler.sol + .planning/phases/306-test-tst/306-01-SUMMARY.md
    - The same commit touches ZERO files under `contracts/` (verify: `git diff --stat HEAD~1..HEAD -- contracts/` returns empty / no rows)
    - SUMMARY.md frontmatter lists all 14 requirements-completed IDs (TST-02 + INV-01..13)
    - SUMMARY.md body cites the exact 13 invariant function names so Phase 308 §3.F can grep them directly
  </acceptance_criteria>
  <done>
    AGENT-COMMITTED atomic commit lands the invariant harness + handler refresh + SUMMARY artifact in one envelope. Phase 308 §3.F attestation matrix has 14 (INV-NN, test_id) rows to cite verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler ↔ sStonk | Handler calls external `burn` / `claimRedemption` via prank; assertions read public state + vm.load internal slots |
| Handler ↔ Game | Handler triggers `advanceGame` + VRF mock fulfillment; reads `currentDayView` + `gameOver` + `rngLocked` |
| Harness ↔ Handler | Invariant fns read ghost vars exposed via getters; ghost data is in handler-internal memory only — not adversarial |
| Test-tree ↔ contracts/ | Zero mutations; harness purely observes post-refactor source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-306-01-01 | Tampering | Ghost-var update logic in handler | mitigate | Ghost updates derived ONLY from public sStonk reads post-action (pendingRedemptions auto-getter + redemptionPeriods auto-getter); no shadowing of internal state that could drift from reality |
| T-306-01-02 | Spoofing | Per-day sum mis-attribution | mitigate | `ghost_perDay_perPlayer_*` mappings update only on successful try/catch — failed burns don't pollute ghosts; per-(player, day) composite key prevents cross-day aliasing |
| T-306-01-03 | Information disclosure | Slot derivation drift from v44 layout | mitigate | Task 1 runs `forge inspect` ONCE and records exact slot indices inline; future v45 layout changes detected by compile-time decode failures (uint64 unpacking + slot reads use unpacked indices visible in the constant block) |
| T-306-01-04 | Denial of service | Per-D scan loops in invariant fns OOG at deep runs | mitigate | All loops bounded by `min(handler.getDaysWrittenCount(), 100)`; handler emits ≤ 15-call sequences default, well within bound |
| T-306-01-05 | Repudiation | EXACT-equality assertions falsely accepting dust drift | mitigate | INV-02..05 use strict `assertEq` (not `assertApproxEqAbs`); any drift > 0 fails immediately; the D-305-GWEI-SNAP-01 zero-drift proof (gcd(1e9, 100) = 100) is the structural guarantee — invariant FAILING here is a real bug discovery |
| T-306-01-06 | Elevation of privilege | Handler calls onlyGame functions directly | mitigate | Handler does NOT directly call `resolveRedemptionPeriod` — it triggers it indirectly via `advanceGame` flow; `transferFromPool` calls are pranked as `address(game)` per existing v43 pattern, no privilege bypass beyond the established test idiom |
| T-306-01-SC | Tampering | No package installs in this plan | accept | This plan modifies only `.sol` files; zero npm/pip/cargo installs; no slopcheck needed |
</threat_model>

<verification>
Phase-level success criteria mapped to this plan:
- ROADMAP Success Criterion 2 (RedemptionAccounting.t.sol PASSES with 12 invariant_* fns) — DELIVERED with 13 fns (12 SPEC-locked + INV-13 from 305)
- ROADMAP Success Criterion 5 (forge build PASS + FOUNDRY_PROFILE=deep forge test --match-path "test/{fuzz,invariant}/**" PASS) — DELIVERED for the invariant subtree; the fuzz subtree comes from Plans 02/03/04/05

Test invocation for the executor:
```
cd /home/zak/Dev/PurgeGame/degenerus-audit
forge build
FOUNDRY_PROFILE=deep forge test --match-path "test/invariant/RedemptionAccounting.t.sol" -vv
```

Expected output: 13 `[PASS] invariant_INV_NN_*` lines + zero failures.
</verification>

<success_criteria>
- `test/invariant/RedemptionAccounting.t.sol` created with exactly 13 invariant functions (one per INV-01..13)
- `test/fuzz/handlers/RedemptionHandler.sol` refreshed: v44 slot constants derived from `forge inspect`, 8 per-day ghost mappings added, 5 action selectors registered, multi-day claim
- `forge build` exits 0
- `FOUNDRY_PROFILE=deep forge test --match-path "test/invariant/RedemptionAccounting.t.sol"` exits 0
- INV-02 + INV-03 + INV-04 + INV-05 use STRICT `assertEq` (no dust tolerance) per D-305-GWEI-SNAP-01
- INV-13 asserts at-most-one non-empty pendingByDay AND matches `pendingResolveDay` sentinel
- 306-01-SUMMARY.md written + AGENT-COMMITTED in single atomic commit with the test files
- Zero `contracts/*.sol` mutations in the commit envelope
</success_criteria>

<output>
Write `.planning/phases/306-test-tst/306-01-SUMMARY.md` with frontmatter listing requirements-completed (TST-02 + INV-01..13), the 13 invariant fn names verbatim, the 8 ghost mappings + INV anchors, the 4 v44 SLOT_* constants with exact slot indices from forge inspect, and EXACT-equality attestation citing D-305-GWEI-SNAP-01 + D-305-SENTINEL-01. Commit envelope is a single AGENT-COMMITTED atomic commit covering the 3 files.
</output>
