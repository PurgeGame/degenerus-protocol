---
phase: 305-implementation-impl
plan: 01
type: execute
wave: 1
subsystem: sStonk per-day redemption refactor IMPL
tags: [IMPL, sStonk, per-day-keying, V-184-structural-elimination, v44.0, batched-contract-diff]
depends_on: []
requirements: [IMPL-01, IMPL-02, IMPL-03, IMPL-04]
files_modified:
  - contracts/StakedDegenerusStonk.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/interfaces/IStakedDegenerusStonk.sol
  - test/fuzz/RedemptionGas.t.sol
  - test/fuzz/CoverageGap222.t.sol
  - .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the patched contract tree"
    - "Storage layout shows the 5 pre-refactor slots removed (`pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodIndex`, `redemptionPeriodBurned`) + `mapping(uint32 => DayPending) internal pendingByDay` added + `pendingRedemptions` re-keyed to `mapping(address => mapping(uint32 => PendingRedemption))`"
    - "`_submitGamblingClaimFrom` writes burns to `pendingByDay[currentDayView()]` per-day pool with SPEC-05 lazy-init supply snapshot; cumulative `pendingRedemptionEthValue` global continues to track cross-day sum unchanged"
    - "`resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` accepts the day arg + writes `redemptionPeriods[dayToResolve]` + emits `RedemptionResolved` + then `delete pendingByDay[dayToResolve]` for storage refund; all 3 AdvanceModule call sites (`:1230`, `:1293`, `:1323`) pass `dayToResolve = day - 1`"
    - "`claimRedemption(uint32 day)` reads composite-keyed `pendingRedemptions[msg.sender][day]` + `redemptionPeriods[day]`; `UnresolvedClaim` revert at `:796-797` REMOVED; `delete pendingRedemptions[msg.sender][day]` on full-claim path; partial-claim branch at `:659-665` preserved verbatim"
    - "All `file:line` citations grep-verified against working-tree HEAD BEFORE source mutation lands (per D-305-GREP-01); any drift documented in `305-01-GREP-VERIFICATION.md` artifact before patching"
    - "USER-APPROVED diff committed exactly once per `feedback_batch_contract_approval.md` (single atomic commit covering all 5 source files); manual diff review per `feedback_manual_review_before_push.md` before commit lands; no `git push` until separate approval"
  artifacts:
    - path: ".planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md"
      provides: "Pre-patch grep-verification artifact — per-citation status table (VERIFIED / DRIFTED-WITH-CORRECTION) for every cited file:line in 304-SPEC.md §5"
      contains: "61-row manifest re-verification at HEAD"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "Per-day-keyed redemption refactor (`DayPending` struct + `pendingByDay` mapping + composite-key `pendingRedemptions` + 3-arg `resolveRedemptionPeriod` + 1-arg `claimRedemption` + 1-arg `hasPendingRedemptions`)"
      contains: "struct DayPending"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "3 call-site updates at `:1225+:1230`, `:1288+:1293`, `:1318+:1323` — all 3 pass `dayToResolve = day - 1`"
      contains: "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)"
    - path: "contracts/interfaces/IStakedDegenerusStonk.sol"
      provides: "Interface signature delta — 1-arg `hasPendingRedemptions(uint32 day)` + 3-arg `resolveRedemptionPeriod(uint16, uint32, uint32)`"
      contains: "function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external"
    - path: "test/fuzz/RedemptionGas.t.sol"
      provides: "Compile-break fix: all `resolveRedemptionPeriod(100, currentDay)` → 3-arg form; all `hasPendingRedemptions()` → 1-arg form"
      contains: "sdgnrs.resolveRedemptionPeriod(100, currentDay, currentDay - 1)"
    - path: "test/fuzz/CoverageGap222.t.sol"
      provides: "Compile-break fix: function-selector string at `:948` updated to 3-arg form; assertion at `:973` byte-identical"
      contains: "resolveRedemptionPeriod(uint16,uint32,uint32)"
  key_links:
    - from: "contracts/StakedDegenerusStonk.sol::_submitGamblingClaimFrom"
      to: "pendingByDay[currentPeriod]"
      via: "per-day-keyed write of ethBase/burnieBase/burned/supplySnapshot"
      pattern: "pendingByDay\\[currentPeriod\\]\\.(ethBase|burnieBase|burned|supplySnapshot)"
    - from: "contracts/StakedDegenerusStonk.sol::resolveRedemptionPeriod"
      to: "pendingByDay[dayToResolve]"
      via: "per-day-keyed read + delete after write to redemptionPeriods[dayToResolve]"
      pattern: "delete pendingByDay\\[dayToResolve\\]"
    - from: "contracts/StakedDegenerusStonk.sol::claimRedemption"
      to: "pendingRedemptions[msg.sender][day]"
      via: "composite-key read + delete on full-claim path"
      pattern: "delete pendingRedemptions\\[msg.sender\\]\\[day\\]"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol::_advanceGame"
      to: "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)"
      via: "3-arg call at :1230 with explicit dayToResolve"
      pattern: "sdgnrs.resolveRedemptionPeriod\\(redemptionRoll, flipDay, day - 1\\)"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol::_gameOverEntropy"
      to: "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)"
      via: "3-arg call at :1293 + :1323 (two distinct paths inside _gameOverEntropy)"
      pattern: "sdgnrs.resolveRedemptionPeriod\\(redemptionRoll, flipDay, day - 1\\)"
---

<objective>
Land the v44.0 sStonk per-day redemption refactor as a single batched USER-APPROVED contract diff per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`. Mutate `contracts/StakedDegenerusStonk.sol` per the LOCKED Phase 304 SPEC §2 SPEC-01..05 + §2.7 7 deletions; update the 3 call sites in `contracts/modules/DegenerusGameAdvanceModule.sol`; update `contracts/interfaces/IStakedDegenerusStonk.sol` to match the new signatures; fix the 2 test files that compile-break under the new interface. Storage layout breaks atomically per pre-launch frozen-at-deploy posture (`feedback_frozen_contracts_no_future_proofing.md`); no migration scaffolding; no compatibility shim; no future-proofing comments. Before any source edit, re-verify every cited `file:line` in 304-SPEC.md §5 against working-tree HEAD per `feedback_verify_call_graph_against_source.md` (D-305-GREP-01).

Purpose: V-184 cross-day re-roll catastrophe + its 6 subsumed catalog rows close STRUCTURALLY (not by gating) at v44.0 via per-day storage-key separation. The refactor makes the overwrite primitive that V-184 exploits physically unreachable: every day's resolve writes a distinct mapping slot keyed by `uint32 day`, and no later action can retroactively rewrite a player's day-D claim values. INV-01..12 the Phase 306 invariant harness will prove all depend on this storage shape being correct. Phase 305 produces zero findings — it produces source mutations the LOCKED SPEC dictates verbatim.

Output:
1. `.planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md` AGENT-COMMITTED state artifact (Task 1 output — pre-patch citation manifest re-verification).
2. Single atomic USER-APPROVED commit covering `contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` + `contracts/interfaces/IStakedDegenerusStonk.sol` + `test/fuzz/RedemptionGas.t.sol` + `test/fuzz/CoverageGap222.t.sol` (Task 2 output — the contract diff). Test files batch into the SAME commit envelope as the contracts per `feedback_no_contract_commits.md` clarified policy + `D-43N-TEST-COMMITS-AUTO-01` lineage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/305-implementation-impl/305-CONTEXT.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/304-spec-invariant-model-spec/304-05-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/interfaces/IStakedDegenerusStonk.sol
@test/fuzz/RedemptionGas.t.sol
@test/fuzz/CoverageGap222.t.sol

<interfaces>
<!-- Pre-refactor signatures the executor mutates. Extracted from source HEAD at planning time. -->
<!-- Executor must consume these directly — no codebase exploration needed for signature shapes. -->

From contracts/StakedDegenerusStonk.sol (PRE-REFACTOR — what currently exists):

Storage block (`:209-231`):
```
struct PendingRedemption {
    uint96  ethValueOwed;
    uint96  burnieOwed;
    uint32  periodIndex;    // REMOVED in refactor — composite outer key carries the day
    uint16  activityScore;
}
struct RedemptionPeriod {
    uint16  roll;
    uint32  flipDay;
}
mapping(address => PendingRedemption) public pendingRedemptions;             // :221 — RE-KEY to composite
mapping(uint32 => RedemptionPeriod) public redemptionPeriods;                 // :222 — UNCHANGED
uint256 public pendingRedemptionEthValue;                                     // :224 — UNCHANGED (cumulative scalar)
uint256 internal pendingRedemptionBurnie;                                     // :225 — UNCHANGED (cumulative scalar)
uint256 internal pendingRedemptionEthBase;                                    // :226 — DELETE
uint256 internal pendingRedemptionBurnieBase;                                 // :227 — DELETE
uint256 internal redemptionPeriodSupplySnapshot;                              // :229 — DELETE
uint32  internal redemptionPeriodIndex;                                       // :230 — DELETE
uint256 internal redemptionPeriodBurned;                                      // :231 — DELETE
```

Function signatures (PRE-REFACTOR):
```
function hasPendingRedemptions() external view returns (bool);                                  // :577 — ADD uint32 day arg
function resolveRedemptionPeriod(uint16 roll, uint32 flipDay) external;                         // :585 — ADD uint32 dayToResolve arg
function claimRedemption() external;                                                            // :618 — ADD uint32 day arg
function _submitGamblingClaimFrom(address beneficiary, address burnFrom, uint256 amount) private;  // :752 — rewrite body for per-day keying
```

Errors block:
```
error UnresolvedClaim();                                                                        // :108 — DELETE (no callers post-refactor)
error NotResolved();                                                                            // :114 — PRESERVE
error NoClaim();                                                                                // :111 — PRESERVE
```

Events block (`:158`, `:161`, `:164`):
```
event RedemptionSubmitted(address indexed player, uint256 sdgnrsAmount, uint256 ethValueOwed, uint256 burnieOwed, uint32 periodIndex);  // UNCHANGED topic hash; `periodIndex` arg name retained — semantically the day under per-day keying
event RedemptionResolved(uint32 indexed periodIndex, uint16 roll, uint256 rolledBurnie, uint32 flipDay);                                  // UNCHANGED topic hash
event RedemptionClaimed(address indexed player, uint16 roll, bool flipResolved, uint256 ethPayout, uint256 burniePayout, uint256 lootboxEth);  // UNCHANGED
```

POST-REFACTOR target shape (what executor writes):

Storage block additions:
```
struct DayPending { uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned; }   // SPEC-01: 3 slots per active day
mapping(uint32 => DayPending) internal pendingByDay;                                                 // SPEC-01: replaces the 5 deleted slots at :226-231
mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions;                  // SPEC-02: composite key
```

Function signatures (POST-REFACTOR):
```
function hasPendingRedemptions(uint32 day) external view returns (bool);                         // SPEC-03 secondary lock
function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external;     // SPEC-03 primary lock
function claimRedemption(uint32 day) external;                                                    // SPEC-02
```

PendingRedemption struct (POST-REFACTOR — `periodIndex` field removed):
```
struct PendingRedemption {
    uint96  ethValueOwed;
    uint96  burnieOwed;
    uint16  activityScore;   // outer composite key (player, day) carries the day reference
}
```

From contracts/modules/DegenerusGameAdvanceModule.sol — 3 call sites (PRE-REFACTOR):

`:1216` `_advanceGame(uint48 ts, uint32 day, ...)` — `day` is local in scope as the wall-clock day.

Site 1 — `_advanceGame` primary path (rngGate / fresh VRF word):
```
1222:                IStakedDegenerusStonk sdgnrs = IStakedDegenerusStonk(
1223:                    ContractAddresses.SDGNRS
1224:                );
1225:                if (sdgnrs.hasPendingRedemptions()) {                                  // GATE — ADD `day - 1` arg
1226:                    uint16 redemptionRoll = uint16(((currentWord >> 8) % 151) + 25);   // UNCHANGED (RNG-derivation byte-identical)
1227:                    uint32 flipDay = day + 1;
1228:                    sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);            // CALL — ADD third arg `day - 1`
1229:                }
```

Site 2 — `_gameOverEntropy` stale-VRF path (`:1267 _gameOverEntropy(uint48 ts, uint32 day, ...)`):
```
1285:                IStakedDegenerusStonk sdgnrs = IStakedDegenerusStonk(
1286:                    ContractAddresses.SDGNRS
1287:                );
1288:                if (sdgnrs.hasPendingRedemptions()) {                                   // GATE — ADD `day - 1` arg
1289:                    uint16 redemptionRoll = uint16(((currentWord >> 8) % 151) + 25);
1290:                    uint32 flipDay = day + 1;
1291:                    sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);             // CALL — ADD third arg `day - 1`
1292:                }
```

Site 3 — `_gameOverEntropy` fallback path (uses `fallbackWord` not `currentWord`):
```
1315:                    IStakedDegenerusStonk sdgnrs = IStakedDegenerusStonk(
1316:                        ContractAddresses.SDGNRS
1317:                    );
1318:                    if (sdgnrs.hasPendingRedemptions()) {                                // GATE — ADD `day - 1` arg
1319:                        uint16 redemptionRoll = uint16(((fallbackWord >> 8) % 151) + 25);
1320:                        uint32 flipDay = day + 1;
1321:                        sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);         // CALL — ADD third arg `day - 1`
1322:                    }
```

Symmetry attestation per Phase 304 Plan 05 §5.2: all 3 sites pass IDENTICAL `dayToResolve = day - 1` expression so a future grep enumerates all 3 cleanly per `feedback_verify_call_graph_against_source.md` Phase 294 BURNIE-gap precedent.

From contracts/interfaces/IStakedDegenerusStonk.sol (PRE-REFACTOR):
```
:86: function hasPendingRedemptions() external view returns (bool);
:96: function resolveRedemptionPeriod(uint16 roll, uint32 flipDay) external;
```

POST-REFACTOR:
```
function hasPendingRedemptions(uint32 day) external view returns (bool);
function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external;
```

From test/fuzz/RedemptionGas.t.sol — compile-break sites:
```
:78:        sdgnrs.resolveRedemptionPeriod(100, currentDay);             // → 3-arg: (100, currentDay, currentDay - 1)
:94:        sdgnrs.resolveRedemptionPeriod(100, currentDay);             // → 3-arg: (100, currentDay, currentDay - 1)
:113:        sdgnrs.claimRedemption();                                    // → 1-arg: (currentDay - 1)
:127:        bool pending = sdgnrs.hasPendingRedemptions();                // → 1-arg: (currentDay)  -- queries today's pool
:134:        bool pending = sdgnrs.hasPendingRedemptions();                // → 1-arg: (currentDay)  -- queries today's pool
```

Note `:113` is a NEW compile-break point not in CONTEXT.md's enumeration: `claimRedemption()` becomes `claimRedemption(uint32 day)` per SPEC-02. The executor MUST update `:113` to pass the correct day (matching the day the resolve was performed against, i.e. the burn day = `currentDay` captured at `:92`). Per D-305-TESTBREAK-01 spirit, all sites that compile-break update inside the batched diff.

From test/fuzz/CoverageGap222.t.sol — compile-break site:
```
:948:                "resolveRedemptionPeriod(uint16,uint32)",          // → "resolveRedemptionPeriod(uint16,uint32,uint32)"
:951:                uint16(0),
:952:                uint32(0)
                                                                       // ADD uint32(0) third arg
:973: assertFalse(o1, "sdgnrs.resolveRedemptionPeriod rejected non-authorized caller");   // BYTE-IDENTICAL — preserve
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-patch grep re-verification of 304-SPEC.md §5 citation manifest against working-tree HEAD</name>
  <files>.planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md (§5.1 sStonk manifest 50 rows + §5.2 AdvanceModule manifest 11 rows — every cited `file:line`)
    - .planning/phases/304-spec-invariant-model-spec/304-05-SUMMARY.md ("Citations that were CORRECTED" section + "All-three-call-site attestation" section — 61 VERIFIED / 0 CORRECTED / 0 ABSENT at v43.0 baseline HEAD `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`)
    - .planning/phases/305-implementation-impl/305-CONTEXT.md (D-305-GREP-01 + D-305-DAYTORESOLVE-01)
    - contracts/StakedDegenerusStonk.sol (current working-tree HEAD — lines `:88-117`, `:209-231`, `:254`, `:486-495`, `:577-610`, `:618-684`, `:752-814`, `:817-852`)
    - contracts/modules/DegenerusGameAdvanceModule.sol (current working-tree HEAD — lines `:19`, `:1180-1235`, `:1265-1325`)
  </read_first>
  <action>
Re-verify every cited `file:line` in 304-SPEC.md §5 against the CURRENT working-tree HEAD per D-305-GREP-01 + `feedback_verify_call_graph_against_source.md`. The Phase 304 Plan 05 manifest verified 61 citations at v43.0 baseline HEAD `8111cfc5189f628b64b500c881f9995c3edf0ed2`; the working tree at Phase 305 start should be byte-identical for these citations (Phase 304 itself produced 5 SPEC-doc commits but zero source-tree mutations — drift is structurally bounded, but verify, don't assume).

Create `.planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md` with this structure:

1. **Header section** — baseline HEAD reference, current working-tree HEAD SHA (from `git rev-parse HEAD`), Phase 305 Plan 01 plan reference, scope statement (re-verify 50 sStonk + 11 AdvanceModule citations + the 2 test-file compile-break sites added in CONTEXT.md `<domain>`).

2. **§A — `contracts/StakedDegenerusStonk.sol` re-verification table (50 rows + any added)** — columns: `Cited at §N (304-SPEC)` / `Cited line` / `Status at v43.0 baseline (Plan 05)` / `Status at Phase 305 start (this verification)` / `Drift note (if any)`. Status values: `VERIFIED-NO-DRIFT` (line content byte-identical) / `DRIFTED-WITH-CORRECTION` (line moved but content still present at new line — record old/new line numbers) / `ABSENT` (citation invalidated — BLOCKS Task 2). For VERIFIED-NO-DRIFT rows, the Status column is sufficient; no per-row snippet needed.

3. **§B — `contracts/modules/DegenerusGameAdvanceModule.sol` re-verification table (11 rows)** — same column structure. Explicitly re-verify all THREE `sdgnrs.resolveRedemptionPeriod` call sites at `:1230` + `:1293` + `:1323` and all THREE `sdgnrs.hasPendingRedemptions` gate sites at `:1225` + `:1288` + `:1318` per the §5.2 "EXHAUSTIVE CALL-GRAPH ATTESTATION" lock. Use `grep -n "sdgnrs.resolveRedemptionPeriod\|sdgnrs.hasPendingRedemptions" contracts/modules/DegenerusGameAdvanceModule.sol` — must return exactly 6 hits (3 + 3) at the expected lines.

4. **§C — Test-file compile-break site verification** — re-verify the 5 compile-break sites enumerated in CONTEXT.md `<domain>` + `<interfaces>` block of this PLAN: `test/fuzz/RedemptionGas.t.sol:78`, `:94`, `:113` (claimRedemption call — added to executor's working set in this plan), `:127`, `:134` + `test/fuzz/CoverageGap222.t.sol:948` + `:973`. Use `grep -n "resolveRedemptionPeriod\|hasPendingRedemptions\|claimRedemption" test/fuzz/RedemptionGas.t.sol test/fuzz/CoverageGap222.t.sol` and assert each cited line shows the expected pre-refactor signature.

5. **§D — Aggregate status attestation** — final paragraph: total citations re-verified (61 from SPEC manifest + 7 test-file compile-break sites = 68); count of VERIFIED-NO-DRIFT / DRIFTED-WITH-CORRECTION / ABSENT. If ALL = VERIFIED-NO-DRIFT, attest: "Working-tree HEAD is byte-identical to v43.0 baseline HEAD for all cited file:line locations. Task 2 proceeds with line numbers verbatim from 304-SPEC.md §5 + 305-CONTEXT.md `<domain>`." If ANY = DRIFTED-WITH-CORRECTION, list each drift + the post-correction line number; Task 2 uses corrected line numbers. If ANY = ABSENT, STOP — do not proceed to Task 2; surface the absent citation to the user and request guidance (per D-305-GREP-01 "the plan does NOT proceed to patching with stale line numbers").

6. **§E — Per-mutation work-item enumeration (pre-patch)** — list every source mutation Task 2 will perform, in execution order:
   - Mutation 1: DELETE `error UnresolvedClaim();` at `:108`
   - Mutation 2: REMOVE `uint32 periodIndex` field from `PendingRedemption` struct at `:212`
   - Mutation 3: ADD `struct DayPending { ... }` declaration near `:215-218` region (alongside existing `RedemptionPeriod` struct)
   - Mutation 4: CHANGE `mapping(address => PendingRedemption) public pendingRedemptions` at `:221` to composite key shape
   - Mutation 5: DELETE 5 storage slots at `:226-231` (`pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodIndex`, `redemptionPeriodBurned`)
   - Mutation 6: ADD `mapping(uint32 => DayPending) internal pendingByDay;` declaration immediately after `pendingRedemptionBurnie` at `:225` (per D-305-STORAGE-01)
   - Mutation 7: REWRITE `hasPendingRedemptions()` at `:577` to take `uint32 day` arg; body returns `pendingByDay[day].ethBase != 0 || pendingByDay[day].burnieBase != 0`
   - Mutation 8: REWRITE `resolveRedemptionPeriod(uint16 roll, uint32 flipDay)` at `:585` to 3-arg `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)`; rewrite body for per-day keying + delete-at-resolve per SPEC-03 + SPEC-04 (c)
   - Mutation 9: REWRITE `claimRedemption()` at `:618` to `claimRedemption(uint32 day)` taking day arg; rewrite body for composite-key reads + delete-on-full-claim per SPEC-02 + SPEC-04 (d); partial-claim branch at `:659-665` preserved verbatim
   - Mutation 10: REWRITE `_submitGamblingClaimFrom` body — replace `:758-762` reset block with SPEC-05 lazy-init (keep `:757 currentPeriod = game.currentDayView();` local — consumed downstream); re-key all writes at `:789-792` + the per-claim storage pointer at `:795` + the cap check at `:763`; DELETE `:796-797` `UnresolvedClaim` revert block; DELETE `:806` `claim.periodIndex = currentPeriod;` line (field is removed in Mutation 2)
   - Mutation 11: UPDATE `IStakedDegenerusStonk` interface at `:86` (`hasPendingRedemptions`) + `:96` (`resolveRedemptionPeriod`) for new signatures
   - Mutation 12: UPDATE `DegenerusGameAdvanceModule.sol` 3 call sites (`:1225+:1230` rngGate primary; `:1288+:1293` _gameOverEntropy stale-VRF; `:1318+:1323` _gameOverEntropy fallback) — each site gets BOTH the gate (`hasPendingRedemptions(day - 1)`) AND the call (`resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)`) updated to the new arity per D-305-DAYTORESOLVE-01
   - Mutation 13: FIX `test/fuzz/RedemptionGas.t.sol` 5 compile-break sites (`:78`, `:94`, `:113`, `:127`, `:134`)
   - Mutation 14: FIX `test/fuzz/CoverageGap222.t.sol:948` selector string + `:951-952` arg list

Execute this in a single Bash session of grep commands; emit the artifact as a single Write operation. Commit the artifact AGENT-COMMITTED autonomously per `.planning/` policy (planner orchestration handles the commit, not the executor task). Do NOT touch `contracts/` or `test/` source files in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md && grep -c "VERIFIED-NO-DRIFT\|DRIFTED-WITH-CORRECTION\|ABSENT" .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md | awk '$1 >= 61 {exit 0} {exit 1}' && grep -q "^## §D — Aggregate status attestation" .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md && grep -q "^## §E — Per-mutation work-item enumeration" .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md` exists.
    - `grep -c "^## §A\|^## §B\|^## §C\|^## §D\|^## §E" 305-01-GREP-VERIFICATION.md` returns exactly 5 (five sections present).
    - The §A sStonk manifest table has at minimum 50 rows of citation status (one per Phase 304 Plan 05 §5.1 row).
    - The §B AdvanceModule manifest table has at minimum 11 rows + explicitly cites `:1225`, `:1230`, `:1288`, `:1293`, `:1318`, `:1323`.
    - `grep -n "sdgnrs.resolveRedemptionPeriod" contracts/modules/DegenerusGameAdvanceModule.sol` returns exactly 3 hits at lines `1230`, `1293`, `1323` (verified by Task 1's §B table); `grep -n "sdgnrs.hasPendingRedemptions" contracts/modules/DegenerusGameAdvanceModule.sol` returns exactly 3 hits at lines `1225`, `1288`, `1318`.
    - The §D aggregate status attestation explicitly states either "all VERIFIED-NO-DRIFT" (Task 2 proceeds with verbatim lines) OR enumerates DRIFTED-WITH-CORRECTION corrections OR signals ABSENT (Task 2 blocked).
    - The §E work-item enumeration lists at least 14 source mutations in execution order, each annotated with the SPEC-NN cross-reference.
    - No `contracts/*.sol` or `test/*.sol` files modified by Task 1 (`git diff --name-only HEAD -- contracts/ test/` returns nothing).
  </acceptance_criteria>
  <done>Task 1 complete when the GREP-VERIFICATION.md artifact exists with all 5 sections, the §D attestation is final (one of: all-verified / drifted-with-correction-table / absent-blocker), the §E work-item list enumerates the mutations Task 2 executes, and no source-tree files have been modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Land the batched contract+interface+test diff per SPEC, present for USER review and approval, commit as single atomic commit</name>
  <files>contracts/StakedDegenerusStonk.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/interfaces/IStakedDegenerusStonk.sol, test/fuzz/RedemptionGas.t.sol, test/fuzz/CoverageGap222.t.sol</files>
  <read_first>
    - .planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md (Task 1 output — line numbers used verbatim if §D attests "all VERIFIED-NO-DRIFT"; corrected line numbers used if any §A/§B rows are DRIFTED-WITH-CORRECTION)
    - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md (§1 INV-01..12 + §2 SPEC-01..05 with sub-locks a–d + §2.7 7 deletions + §3 EDGE-01..18 + §4 design-intent walks + §5 source-verified citation manifest)
    - .planning/phases/305-implementation-impl/305-CONTEXT.md (D-305-DAYTORESOLVE-01 + D-305-STORAGE-01 + D-305-APPROVAL-01..04 + Claude's Discretion block)
    - contracts/StakedDegenerusStonk.sol (current pre-refactor state — full file)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines `:1180-1235` + `:1265-1325` for the 3 call sites)
    - contracts/interfaces/IStakedDegenerusStonk.sol (full file — interface delta)
    - test/fuzz/RedemptionGas.t.sol (full file — 5 compile-break sites + the gas-benchmark intent)
    - test/fuzz/CoverageGap222.t.sol (lines `:940-980` — the selector-rejection test)
  </read_first>
  <action>See `<what-built>` below for the 14-mutation work-item list (executed verbatim against the line numbers attested in Task 1's §D), the `forge build` pre-presentation gate, the diff-summary generation, the user-approval wait, the post-approval commit message format, and the explicit no-`git push` instruction.</action>
  <verify>
    <automated>forge build 2>&1 | tail -5 | grep -qE "Compiler run successful|Compilation finished" &amp;&amp; git diff --name-only HEAD~1 HEAD 2>/dev/null | sort | diff - &lt;(printf 'contracts/StakedDegenerusStonk.sol\ncontracts/interfaces/IStakedDegenerusStonk.sol\ncontracts/modules/DegenerusGameAdvanceModule.sol\ntest/fuzz/CoverageGap222.t.sol\ntest/fuzz/RedemptionGas.t.sol\n') &amp;&amp; [ "$(grep -c 'sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)' contracts/modules/DegenerusGameAdvanceModule.sol)" = "3" ] &amp;&amp; [ "$(grep -c 'sdgnrs.hasPendingRedemptions(day - 1)' contracts/modules/DegenerusGameAdvanceModule.sol)" = "3" ] &amp;&amp; [ "$(grep -c 'mapping(uint32 =&gt; DayPending) internal pendingByDay' contracts/StakedDegenerusStonk.sol)" = "1" ] &amp;&amp; [ "$(grep -c 'error UnresolvedClaim' contracts/StakedDegenerusStonk.sol)" = "0" ]</automated>
    <human-check>See `<how-to-verify>` below for the full diff-review checklist (per-file SPEC-NN cross-reference + no-history-narration + no-future-proofing + approval-signal verification).</human-check>
  </verify>
  <what-built>
The batched contract + interface + test diff per the LOCKED Phase 304 SPEC, presented for USER review as a single atomic commit covering 5 files. The executor performs all 14 mutations enumerated in Task 1's §E work-item list, then presents the diff (`git diff` output) to the user for explicit review and approval per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` + `feedback_wait_for_approval.md`. NO commit lands without user approval. NO `git push` after commit lands (separate approval required per `feedback_manual_review_before_push.md`).

**The 14 mutations (executed verbatim against the line numbers attested in Task 1's §D):**

**Mutation 1 — Delete `UnresolvedClaim` error declaration (`:108` per SPEC §2.7 deletion 6).** Per SPEC-02: under composite keying a player may legitimately hold concurrent unresolved entries across multiple days; the revert was structurally required only under the pre-refactor single-pool design where concurrent unresolved player claims aliased into one slot. POST-state: no `UnresolvedClaim` error in the contract; no callers exist post-refactor. Discretion: the `///` natspec block immediately above the error (`:107`) is deleted along with the error declaration line.

**Mutation 2 — Remove `uint32 periodIndex` field from `PendingRedemption` struct (`:212` per SPEC §2.7 deletion partial of #6 + SPEC-02).** Per SPEC-02: the outer composite mapping key now carries the day reference, so the inner `periodIndex` field becomes redundant. POST-state struct shape: `struct PendingRedemption { uint96 ethValueOwed; uint96 burnieOwed; uint16 activityScore; }` — 96 + 96 + 16 = 208 bits, still 1 slot. The inline `//` field comment for `periodIndex` (`:212`) is deleted along with the field. The slot-comment line `:213` may be retained or updated per executor's discretion (per Claude's Discretion block — comment verbosity left to planner/executor).

**Mutation 3 — Add `DayPending` struct declaration (per SPEC-01).** Place immediately adjacent to the existing `RedemptionPeriod` struct (`:216-218` region). Exact shape per SPEC-01:
```
struct DayPending {
    uint256 ethBase;
    uint256 burnieBase;
    uint128 supplySnapshot;
    uint128 burned;
}
```
3 slots per active day: slot 1 holds `ethBase`; slot 2 holds `burnieBase`; slot 3 packs `supplySnapshot` + `burned`. Per Claude's Discretion: header `///` natspec describing POST-state semantics permitted (cross-references to SPEC-01 / SPEC-05 / INV-04 / INV-05 / INV-10 as informational anchors permitted per D-305-APPROVAL-02); NO history narration ("previously was X" forbidden per `feedback_no_history_in_comments.md`).

**Mutation 4 — Re-key `pendingRedemptions` to composite (`:221` per SPEC-02).** PRE-state `mapping(address => PendingRedemption) public pendingRedemptions;` → POST-state `mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions;`. The mapping visibility (`public`) is preserved. The inline `///` natspec at `:220` (if present in the working-tree HEAD context) is updated to reflect the composite-key semantic; per Claude's Discretion, the executor picks comment verbosity.

**Mutation 5 — Delete 5 storage slots at `:226-231` (per SPEC §2.7 deletions 1-5).**
- `:226` `uint256 internal pendingRedemptionEthBase;` (subsumed by `pendingByDay[D].ethBase`)
- `:227` `uint256 internal pendingRedemptionBurnieBase;` (subsumed by `pendingByDay[D].burnieBase`)
- `:228` blank line (or comment) — preserved per executor discretion
- `:229` `uint256 internal redemptionPeriodSupplySnapshot;` (subsumed by `pendingByDay[D].supplySnapshot`)
- `:230` `uint32  internal redemptionPeriodIndex;` (subsumed by per-day mapping keys)
- `:231` `uint256 internal redemptionPeriodBurned;` (subsumed by `pendingByDay[D].burned`)
The two cumulative scalars `:224 pendingRedemptionEthValue` (public) + `:225 pendingRedemptionBurnie` (internal) MUST be PRESERVED per SPEC-01 explicit unchanged-list.

**Mutation 6 — Add `pendingByDay` mapping declaration per SPEC-01 + D-305-STORAGE-01.** Placement: immediately after `pendingRedemptionBurnie` at `:225` (the surviving cumulative scalar), replacing the 5-slot run at `:226-231` per D-305-STORAGE-01 cosmetic-ordering decision. Exact line:
```
mapping(uint32 => DayPending) internal pendingByDay;
```
Per Claude's Discretion: inline `///` natspec describing POST-state semantics permitted (SPEC-NN cross-references as informational anchors); NO history narration.

**Mutation 7 — Rewrite `hasPendingRedemptions()` body (`:577-579`) per SPEC-03 secondary lock.** PRE-state signature `function hasPendingRedemptions() external view returns (bool)`; PRE-state body `return pendingRedemptionEthBase != 0 || pendingRedemptionBurnieBase != 0;`. POST-state signature `function hasPendingRedemptions(uint32 day) external view returns (bool)`; POST-state body `return pendingByDay[day].ethBase != 0 || pendingByDay[day].burnieBase != 0;`. The `///` natspec block above the function (lines `:574-576` region per the §5.1 manifest) is updated to describe the per-day query semantic; per Claude's Discretion: short rewrite — single-line natspec acceptable (e.g. `/// @notice Check if day D has unresolved gambling burn pool entries / @param day The day to query / @return True if pendingByDay[day] has ethBase or burnieBase != 0`).

**Mutation 8 — Rewrite `resolveRedemptionPeriod` per SPEC-03 + SPEC-04 (a) + SPEC-04 (c).**
PRE-state signature at `:585`: `function resolveRedemptionPeriod(uint16 roll, uint32 flipDay) external`.
POST-state signature: `function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external`.

POST-state body:
1. ACL check preserved verbatim from `:586`: `if (msg.sender != ContractAddresses.GAME) revert Unauthorized();`
2. DELETE `:588 uint32 period = redemptionPeriodIndex;` — the implicit period read is replaced by the explicit `dayToResolve` arg.
3. Replace `:589` early-return short-circuit with per-day-keyed form: `if (pendingByDay[dayToResolve].ethBase == 0 && pendingByDay[dayToResolve].burnieBase == 0) return;`. Per SPEC-04 (c) rationale: short-circuit preserved verbatim, just re-keyed.
4. RE-KEY ETH segregation block (`:591-594` region). Per Claude's Discretion: optionally introduce a `DayPending storage pool = pendingByDay[dayToResolve];` alias for readability. Semantics (locked):
   - Compute `uint256 rolledEth = (pendingByDay[dayToResolve].ethBase * roll) / 100;` (or via alias)
   - Update cumulative: `pendingRedemptionEthValue = pendingRedemptionEthValue - pendingByDay[dayToResolve].ethBase + rolledEth;`
   - Note: no per-day-base zeroing needed here — Mutation 8 step 8 `delete pendingByDay[dayToResolve]` performs the storage-refund atomically; the cumulative update happens BEFORE the delete so it reads the live base value.
5. Compute `uint256 burnieToCredit = (pendingByDay[dayToResolve].burnieBase * roll) / 100;` (re-keyed from pre-refactor `:597`).
6. Update cumulative BURNIE: `pendingRedemptionBurnie -= pendingByDay[dayToResolve].burnieBase;` (re-keyed from pre-refactor `:600`).
7. Write per-day result (re-keyed from pre-refactor `:604-607`): `redemptionPeriods[dayToResolve] = RedemptionPeriod({ roll: roll, flipDay: flipDay });`.
8. Emit `RedemptionResolved` (per SPEC-04 (c) — fires AFTER write): `emit RedemptionResolved(dayToResolve, roll, burnieToCredit, flipDay);`. Note the first event arg uses `dayToResolve` (no separate `period` local needed).
9. Per SPEC-04 (c) storage refund: `delete pendingByDay[dayToResolve];` — fires AFTER `redemptionPeriods[dayToResolve]` is written AND `RedemptionResolved` has emitted. This refunds the 3 slots written in `_submitGamblingClaimFrom`'s per-day pool.

Header `///` natspec above the function (`:580-584` region) updated to describe POST-state semantics per Claude's Discretion. Reference SPEC-03 + SPEC-04 (c) as informational anchors permitted. Per `feedback_no_history_in_comments.md` D-305-APPROVAL-02: no "previously did X" prose.

Per SPEC-04 (a) — NO separate `gameOver` branch is added inside `resolveRedemptionPeriod`. The existing `:638-643` 50/50-vs-100% split logic in `claimRedemption` handles post-`gameOver` payout via the `isGameOver` predicate at `:635`. Reading per SPEC-04 (a) lock: "no separate gameOver branch in resolveRedemptionPeriod; existing :638-643 split logic handles post-gameOver payout".

**Mutation 9 — Rewrite `claimRedemption` per SPEC-02 + SPEC-04 (d).**
PRE-state signature at `:618`: `function claimRedemption() external`.
POST-state signature: `function claimRedemption(uint32 day) external`.

POST-state body:
1. `address player = msg.sender;` preserved from `:619`.
2. Re-key claim read (replacing `:620 PendingRedemption storage claim = pendingRedemptions[player];`): `PendingRedemption storage claim = pendingRedemptions[player][day];`.
3. REPLACE `:621 if (claim.periodIndex == 0) revert NoClaim();` — the `periodIndex` field no longer exists post-Mutation 2. The semantic equivalent under composite keying: a non-existent `(player, day)` claim has `ethValueOwed == 0 && burnieOwed == 0`. New check: `if (claim.ethValueOwed == 0 && claim.burnieOwed == 0) revert NoClaim();`. (Per Claude's Discretion: alternative form `if (claim.activityScore == 0) revert NoClaim();` is also a valid no-claim sentinel since `activityScore` is stored as `score + 1` (`:809-810`) and is never zero post-submission for a real claim — executor picks; semantic equivalent.)
4. Re-key period read (`:623`): `RedemptionPeriod storage period = redemptionPeriods[day];`.
5. PRESERVE VERBATIM `:624 if (period.roll == 0) revert NotResolved();` per SPEC-02 explicit preservation.
6. DELETE `:626-627 uint32 claimPeriodIndex = claim.periodIndex;` — `periodIndex` field is removed; the `day` arg replaces this local. PRESERVE `:625 uint16 roll = period.roll;` and `:627 uint16 claimActivityScore = claim.activityScore;` (rename arithmetic accordingly).
7. PRESERVE `:631 uint256 totalRolledEth = (claim.ethValueOwed * roll) / 100;`.
8. PRESERVE the 50/50 split block at `:634-643` VERBATIM (per SPEC-04 (a) — the `isGameOver = game.gameOver()` predicate at `:635` already correctly selects post-`gameOver` 100% direct payout).
9. PRESERVE the BURNIE-payout block at `:647-655` VERBATIM (per SPEC-04 (d) — `(rewardPercent, flipWon) = coinflip.getCoinflipDayResult(period.flipDay)` unchanged; `flipResolved` computation unchanged; burniePayout formula unchanged).
10. PRESERVE `:657 pendingRedemptionEthValue -= totalRolledEth;`.
11. REWRITE the partial-claim branch at `:659-665` for composite keying per SPEC-04 (d):
    - PRE-state: `delete pendingRedemptions[player];` (under single-key shape)
    - POST-state: `delete pendingRedemptions[player][day];` (composite-keyed)
    - Partial branch (`flipResolved == false`): `claim.ethValueOwed = 0;` PRESERVED VERBATIM (the storage pointer `claim` now points to `pendingRedemptions[player][day]` per Mutation 9 step 2, so the assignment writes the correct composite slot).
12. PRESERVE the lootbox-eth block at `:667-673` VERBATIM (per `<code_context>` Reusable Assets note — `game.resolveRedemptionLootbox` external call unchanged).
   - One required rename inside this block: `uint256 rngWord = game.rngWordForDay(claimPeriodIndex);` at `:670` becomes `uint256 rngWord = game.rngWordForDay(day);` (the `claimPeriodIndex` local is deleted in Mutation 9 step 6).
13. PRESERVE the BURNIE payout call at `:677` VERBATIM: `if (burniePayout != 0) { _payBurnie(player, burniePayout); }`.
14. PRESERVE the `RedemptionClaimed` emit at `:680` VERBATIM.
15. PRESERVE the trailing `_payEth(player, ethDirect);` at `:683` VERBATIM.

Header `///` natspec above the function (`:613-617` region per the §5.1 manifest) updated to describe POST-state day-arg semantic per Claude's Discretion. Permitted: SPEC-02 + SPEC-04 (d) cross-references. Forbidden: history narration.

**Mutation 10 — Rewrite `_submitGamblingClaimFrom` body per SPEC-01 + SPEC-02 + SPEC-04 (b) + SPEC-05.**
Signature unchanged: `function _submitGamblingClaimFrom(address beneficiary, address burnFrom, uint256 amount) private`.

POST-state body changes:
1. PRESERVE `:753 uint256 bal = balanceOf[burnFrom];` + `:754 if (amount == 0 || amount > bal) revert Insufficient();` per SPEC-04 (b) — existing zero-amount revert preserved as the only zero-guard.
2. PRESERVE `:757 uint32 currentPeriod = game.currentDayView();` — the local IS consumed downstream at the cap-check (Mutation 10 step 3) and the per-day write (Mutation 10 step 4) and the per-claim composite key (Mutation 10 step 5) per Plan 05 SUMMARY's disambiguation. (Per Claude's Discretion: rename to `currentDay` for symmetry with the post-refactor mental model; semantic locked.)
3. REPLACE the `:758-762` reset block with SPEC-05 lazy-init:
   ```
   if (pendingByDay[currentPeriod].supplySnapshot == 0 && pendingByDay[currentPeriod].burned == 0) {
       pendingByDay[currentPeriod].supplySnapshot = uint128(totalSupply);
   }
   ```
   Per SPEC-05 lock: predicate `supplySnapshot == 0 && burned == 0` is true exactly once per day (first burn of day D); initialization value is `totalSupply` AT THAT MOMENT (pre-decrement); cast to `uint128` is safe per SPEC-01 rationale (total supply bounded by `INITIAL_SUPPLY ≈ 1e30` well below `uint128.max ≈ 3.4e38`).
4. REPLACE the cap check at `:763` for per-day keying:
   ```
   if (pendingByDay[currentPeriod].burned + amount > pendingByDay[currentPeriod].supplySnapshot / 2) revert Insufficient();
   pendingByDay[currentPeriod].burned += uint128(amount);
   ```
   Cast to `uint128` is safe (amount is bounded by `totalSupply` which is bounded as above).
5. PRESERVE `:766 uint256 supplyBefore = totalSupply;` per SPEC-05 pre-decrement semantic.
6. PRESERVE the ETH+BURNIE value computation at `:768-779` VERBATIM (cumulative subtractions of `pendingRedemptionEthValue` + `pendingRedemptionBurnie` unchanged — both globals are preserved per SPEC-01 explicit unchanged-list).
7. PRESERVE the burn block at `:781-786` VERBATIM (`balanceOf[burnFrom] = bal - amount; totalSupply -= amount;` + `emit Transfer`).
8. REWRITE the segregate block at `:789-792` for per-day keying:
   ```
   pendingRedemptionEthValue += ethValueOwed;             // PRESERVED — cumulative
   pendingByDay[currentPeriod].ethBase += ethValueOwed;   // REPLACES pendingRedemptionEthBase += ethValueOwed at :790
   pendingRedemptionBurnie += burnieOwed;                  // PRESERVED — cumulative
   pendingByDay[currentPeriod].burnieBase += burnieOwed;  // REPLACES pendingRedemptionBurnieBase += burnieOwed at :792
   ```
   Per Claude's Discretion: optionally introduce a `DayPending storage pool = pendingByDay[currentPeriod];` alias to make all 3 writes (lazy-init + cap accumulator at step 4 + ethBase/burnieBase at step 8) share one storage-pointer read; gas-neutral/cosmetic.
9. REWRITE the per-claim storage-pointer read at `:795` for composite keying:
   ```
   PendingRedemption storage claim = pendingRedemptions[beneficiary][currentPeriod];
   ```
   This replaces `pendingRedemptions[beneficiary]` with the composite form per SPEC-02.
10. DELETE the `:796-797` `UnresolvedClaim` revert block ENTIRELY per SPEC-02 + SPEC §2.7 deletion 6. Under composite keying the multi-day pending stack is structurally permitted (each `(player, day)` is a distinct slot).
11. PRESERVE the EV-cap check at `:801` VERBATIM:
    ```
    if (claim.ethValueOwed + ethValueOwed > MAX_DAILY_REDEMPTION_EV) revert ExceedsDailyRedemptionCap();
    ```
    Per SPEC-02 + INV-11 (per-(player, day) EV cap). Note: under composite keying, this cap now resets per-day naturally (a different `day` arg points to a different `claim` slot with `ethValueOwed == 0` fresh).
12. PRESERVE `:803 claim.ethValueOwed += uint96(ethValueOwed);` + `:805 claim.burnieOwed += uint96(burnieOwed);` VERBATIM.
13. DELETE `:806 claim.periodIndex = currentPeriod;` — the field is removed in Mutation 2.
14. PRESERVE the activity-score snapshot block at `:808-811` VERBATIM (depends only on `claim.activityScore`, which is preserved).
15. PRESERVE the `RedemptionSubmitted` event emit at `:813` VERBATIM:
    ```
    emit RedemptionSubmitted(beneficiary, amount, ethValueOwed, burnieOwed, currentPeriod);
    ```
    The event signature is unchanged (the param name `periodIndex` in the event declaration at `:158` is semantically the day under per-day keying; topic hash byte-identical; renaming the event param is out of scope — events not flagged in SPEC §2.7).

Header `///` natspec block above `_submitGamblingClaimFrom` (`:748-751` region per the §5.1 manifest) updated to describe POST-state per-day-keyed write semantic per Claude's Discretion. Cross-references to SPEC-01 / SPEC-02 / SPEC-04 (b) / SPEC-05 permitted as informational anchors. Forbidden: history narration.

**Mutation 11 — Update `IStakedDegenerusStonk` interface (`:86` + `:96`).** PRE-state at `:86`: `function hasPendingRedemptions() external view returns (bool);`. POST-state: `function hasPendingRedemptions(uint32 day) external view returns (bool);`. PRE-state at `:96`: `function resolveRedemptionPeriod(uint16 roll, uint32 flipDay) external;`. POST-state: `function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external;`. Per Claude's Discretion: the natspec blocks at `:84-85` and `:92-95` updated to reflect new signatures; cross-references to SPEC-03 permitted; history narration forbidden.

Note: `claimRedemption(uint32 day)` is NOT in the existing interface (`grep -n "claimRedemption" contracts/interfaces/IStakedDegenerusStonk.sol` returns no hit at the working-tree HEAD — verify via Task 1 §C). Per CONTEXT.md `<scope_constraints>` "Out of Scope" + `<canonical_refs>` "Add only what `_submitGamblingClaimFrom` + `resolveRedemptionPeriod` require": `IDegenerusGamePlayer` interface is UNCHANGED (`currentDayView` already exposed at `:32` of sStonk's import). `IStakedDegenerusStonk` is also kept minimal — only the two signatures cited in CONTEXT.md `<domain>` `<interfaces>` (the 2 contract-to-contract calls from AdvanceModule) update; the EOA-callable `claimRedemption(day)` need not be added to the interface (it was not in the v43 interface either).

**Mutation 12 — Update `DegenerusGameAdvanceModule.sol` 3 call sites per D-305-DAYTORESOLVE-01.**

Each of the 3 sites gets BOTH the gate AND the call updated to the new arity, passing `day - 1` as the explicit `dayToResolve` value per D-305-DAYTORESOLVE-01. Identical expression at all 3 sites for future-grep enumeration:

Site 1 — `_advanceGame` rngGate primary path (`:1225 + :1230`):
```
PRE:  if (sdgnrs.hasPendingRedemptions()) {
POST: if (sdgnrs.hasPendingRedemptions(day - 1)) {
PRE:  sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);
POST: sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1);
```

Site 2 — `_gameOverEntropy` stale-VRF path (`:1288 + :1293`):
```
PRE:  if (sdgnrs.hasPendingRedemptions()) {
POST: if (sdgnrs.hasPendingRedemptions(day - 1)) {
PRE:  sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);
POST: sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1);
```

Site 3 — `_gameOverEntropy` fallback path (`:1318 + :1323`):
```
PRE:  if (sdgnrs.hasPendingRedemptions()) {
POST: if (sdgnrs.hasPendingRedemptions(day - 1)) {
PRE:  sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay);
POST: sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1);
```

The RNG derivation lines (`:1226`, `:1289`, `:1319`) and the `flipDay` computations (`:1227`, `:1290`, `:1320`) remain BYTE-IDENTICAL per `feedback_rng_backward_trace.md` — Phase 305 IMPL does not change RNG inputs. The `currentWord` vs `fallbackWord` difference at Sites 1/2 vs Site 3 is in the roll derivation only, not in the call arity.

Per `feedback_no_history_in_comments.md`: do NOT add a "// updated to pass day - 1" or similar history comment. The existing `// Resolve gambling burn period if pending` comment at `:1221` (and the mirror at `:1284` `// Resolve gambling burn period if pending (mirrors rngGate redemption resolution)`) is preserved or updated per executor's discretion to describe POST-state semantics (e.g., "Resolve day-(D-1) gambling burn pool if pending"); cross-references to SPEC-03 permitted as anchors.

The doc comment at `:1772` (`"resolveRedemptionPeriod is NOT called for backfilled gap days"`) is UNCHANGED per CONTEXT.md `<code_context>` Integration Points — the backfill path does not call the resolver, so per-day refactor does not affect gap-day behavior.

**Mutation 13 — Fix `test/fuzz/RedemptionGas.t.sol` 5 compile-break sites.**

`:78` (PRE: `sdgnrs.resolveRedemptionPeriod(100, currentDay);` → POST: `sdgnrs.resolveRedemptionPeriod(100, currentDay, currentDay - 1);`)
`:94` (same PRE/POST as `:78`)
`:113` (PRE: `sdgnrs.claimRedemption();` → POST: `sdgnrs.claimRedemption(currentDay - 1);` — the test resolves day `currentDay - 1`'s pool against day `currentDay`'s VRF word, then the player claims their day-(`currentDay - 1`) entry. NOTE: at `:92`, `currentDay = game.currentDayView();` is captured; this `currentDay` is the wall-day at the time of the resolve, so the burn happened the same wall-day; for the gas benchmark to remain meaningful under per-day keying, the burn at `:89` lands in `pendingByDay[currentDay]` and the resolve at `:94` resolves day `currentDay - 1`'s pool — which is EMPTY pre-test. The test currently relies on `redemptionPeriodIndex` indirection where the burn lands in "the current period" and the resolve resolves "whatever period is current". Under per-day keying, the burn lands in `pendingByDay[currentDay]` but the resolve resolves `pendingByDay[currentDay - 1]`. To preserve gas-benchmark intent (a non-trivial resolve + claim), the test must EITHER (a) bump the wall day between burn and resolve so the resolve picks up the burn's pool, OR (b) pass `currentDay` as `dayToResolve` so the resolve targets the burn's pool. Per CONTEXT.md `<domain>` "Function bodies otherwise byte-identical; gas-benchmark intent preserved" + `<decisions>` D-305-TESTBREAK-01 "Update inline": the simplest preserve-intent approach is option (b) — pass `currentDay` as both the second AND the third arg: `sdgnrs.resolveRedemptionPeriod(100, currentDay, currentDay);` and `sdgnrs.claimRedemption(currentDay);`. The test becomes "resolve and claim the burn's same-wall-day pool" — semantically slightly different from the v43 test (v43 implicitly resolved whatever was in `redemptionPeriodIndex`), but bench-intent-preserving (real resolve write + real claim work). Per Claude's Discretion: executor picks (a) or (b); recommend (b) as minimal-surface-area.)
`:127` (PRE: `bool pending = sdgnrs.hasPendingRedemptions();` → POST: `bool pending = sdgnrs.hasPendingRedemptions(currentDay);` — at `:121` the burn lands in `pendingByDay[currentDay]`, the gate query must read the same key. Per executor discretion: capture `uint32 currentDay = game.currentDayView();` at top of `test_gas_hasPendingRedemptions_true` before the burn at `:123`.)
`:134` (PRE: `bool pending = sdgnrs.hasPendingRedemptions();` → POST: `bool pending = sdgnrs.hasPendingRedemptions(game.currentDayView());` — `view` test, no burn, asserts false on an empty pool.)

The doc comment block at `:9-10` (`/// @notice Exercises burn, burnWrapped, resolveRedemptionPeriod, claimRedemption, hasPendingRedemptions, ...`) does not require change — the function-name list is byte-identical.

The `claimRedemption()` call at `:113` IS a compile-break site CONTEXT.md did not explicitly enumerate (CONTEXT.md `<domain>` lists `:78`, `:94`, `:127`, `:134` plus `:9-10`; this plan's pre-task work identifies `:113` as a 5th break site). Executor updates per the analysis above.

**Mutation 14 — Fix `test/fuzz/CoverageGap222.t.sol:948` selector + arg list.**

PRE-state at `:944-953`:
```
function test_gap_sdgnrs_redemption_and_gameover_guards() public {
    vm.prank(buyer);
    (bool o1, ) = address(sdgnrs).call(
        abi.encodeWithSignature(
            "resolveRedemptionPeriod(uint16,uint32)",
            uint16(0),
            uint32(0)
        )
    );
```

POST-state at the same lines:
```
function test_gap_sdgnrs_redemption_and_gameover_guards() public {
    vm.prank(buyer);
    (bool o1, ) = address(sdgnrs).call(
        abi.encodeWithSignature(
            "resolveRedemptionPeriod(uint16,uint32,uint32)",
            uint16(0),
            uint32(0),
            uint32(0)
        )
    );
```

Two changes: selector string at `:948` adds `,uint32`; arg list at `:951-952` adds a third `uint32(0)` arg.

The assertion text at `:973` `"sdgnrs.resolveRedemptionPeriod rejected non-authorized caller"` is BYTE-IDENTICAL per CONTEXT.md `<domain>` lock — the test continues to assert the ACL `revert Unauthorized()` rejection at sStonk's `:586` (preserved verbatim in Mutation 8). The other selector-based calls at `:957` `"claimRedemption()"` MUST also be updated to `"claimRedemption(uint32)"` with a `uint32(0)` arg — this is a 7th compile-break point CONTEXT.md `<domain>` did not explicitly enumerate but which Task 1 §C will surface (the `claimRedemption` selector change is symmetric to the resolve selector change). Executor adds this fix; the assertion at `:974` `"sdgnrs.claimRedemption rejected caller with no pending redemption"` remains semantically valid (no-claim revert preserved at sStonk's `:621` equivalent in Mutation 9 step 3).

The other selector calls at `:962` (`burnAtGameOver()`), `:966` (`depositSteth(uint256)`), `:970` (`gameAdvance()`), and `:974` (`gameClaimWhalePass()`) are byte-identical post-refactor (their signatures don't change) — UNCHANGED.

**After all 14 mutations:**

1. Run `forge build` to verify compile success. If compile fails, fix the failure (the most likely failures are missed line-number updates from Task 1 drift, or a missed compile-break site Task 1 §C surfaced); do NOT advance to the user-approval step with a broken build.
2. Run `git diff` to capture the full diff (5 files).
3. Generate a brief diff summary: count of additions/deletions per file; mutation count (14); SPEC-NN cross-reference per file:
   - `contracts/StakedDegenerusStonk.sol` — Mutations 1-10 (SPEC-01 + SPEC-02 + SPEC-03 + SPEC-04 (a-d) + SPEC-05 + §2.7 deletions 1-7)
   - `contracts/modules/DegenerusGameAdvanceModule.sol` — Mutation 12 (SPEC-03 + D-305-DAYTORESOLVE-01)
   - `contracts/interfaces/IStakedDegenerusStonk.sol` — Mutation 11 (SPEC-03)
   - `test/fuzz/RedemptionGas.t.sol` — Mutation 13 (D-305-TESTBREAK-01)
   - `test/fuzz/CoverageGap222.t.sol` — Mutation 14 (D-305-TESTBREAK-01)
4. Present the diff to the user for review. State explicitly:
   - "Phase 305 IMPL batched diff ready for review (5 files, 14 mutations per Phase 304 SPEC §2 SPEC-01..05 + §2.7 7 deletions)."
   - "Per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`: NOT pre-approved. Awaiting explicit USER approval before commit lands."
   - "Per `feedback_manual_review_before_push.md`: post-commit `git push` requires SEPARATE approval. This task ends at commit-landed state."
5. WAIT for explicit user approval. Acceptable approval signals: "approved", "ship it", "commit", "looks good — commit", or equivalent affirmative. Anything else (including silence, requests for changes, or partial questions) is NOT approval.
6. On approval: stage all 5 files (`git add contracts/StakedDegenerusStonk.sol contracts/modules/DegenerusGameAdvanceModule.sol contracts/interfaces/IStakedDegenerusStonk.sol test/fuzz/RedemptionGas.t.sol test/fuzz/CoverageGap222.t.sol`), commit with the message format:
   ```
   feat(305-01): v44.0 sStonk per-day redemption refactor per Phase 304 SPEC

   Single batched USER-APPROVED diff per feedback_batch_contract_approval.md.

   contracts/StakedDegenerusStonk.sol:
   - DELETE 5 storage slots (pendingRedemptionEthBase, pendingRedemptionBurnieBase,
     redemptionPeriodSupplySnapshot, redemptionPeriodIndex, redemptionPeriodBurned)
     per SPEC §2.7 deletions 1-5
   - ADD DayPending struct + pendingByDay mapping per SPEC-01
   - RE-KEY pendingRedemptions to composite (player, day) per SPEC-02
   - REMOVE inner periodIndex field from PendingRedemption per SPEC-02
   - DELETE UnresolvedClaim error per SPEC §2.7 deletion 6
   - REWRITE _submitGamblingClaimFrom for per-day keying + SPEC-05 lazy-init
     supply snapshot per SPEC-01 + SPEC-02 + SPEC-04 (b) + SPEC-05
   - REWRITE hasPendingRedemptions(uint32 day) per SPEC-03 secondary lock
   - REWRITE resolveRedemptionPeriod(uint16, uint32, uint32 dayToResolve) per
     SPEC-03 + SPEC-04 (a) + SPEC-04 (c); delete pendingByDay[dayToResolve]
     at resolve for storage refund
   - REWRITE claimRedemption(uint32 day) per SPEC-02 + SPEC-04 (d); composite-
     key reads; delete pendingRedemptions[msg.sender][day] on full-claim;
     partial-claim branch preserved verbatim

   contracts/modules/DegenerusGameAdvanceModule.sol:
   - 3 call-site updates per D-305-DAYTORESOLVE-01: rngGate primary (:1225+:1230),
     _gameOverEntropy stale-VRF (:1288+:1293), _gameOverEntropy fallback
     (:1318+:1323). All sites pass dayToResolve = day - 1.

   contracts/interfaces/IStakedDegenerusStonk.sol:
   - Update hasPendingRedemptions(uint32 day) + resolveRedemptionPeriod 3-arg
     signatures per SPEC-03.

   test/fuzz/RedemptionGas.t.sol + test/fuzz/CoverageGap222.t.sol:
   - Compile-break fixes (5 sites + 7th selector-string update) per D-305-TESTBREAK-01.
     AGENT-COMMITTED inside the same atomic commit envelope per feedback_no_contract_commits.md
     clarified policy + D-43N-TEST-COMMITS-AUTO-01 lineage.

   V-184 catastrophe + HANDOFF-111..117 close STRUCTURALLY by absence of the
   overwrite primitive (per SPEC §2.0 Priority Statement clause 1).

   Storage layout breaks atomically (pre-launch redeploy-fresh posture per
   feedback_frozen_contracts_no_future_proofing.md). Phase 306 TST proves
   INV-01..12 via Foundry invariant harness + 18 EDGE-NN fuzz functions.
   ```
7. Do NOT run `git push`. Per `feedback_manual_review_before_push.md` D-305-APPROVAL-04: post-commit push requires separate user approval.
  </what-built>
  <how-to-verify>
After Task 1 (grep-verification) AGENT-COMMITS its artifact, you (the user) will receive the diff for review.

Verification steps for the user:

1. **Build verification (automated, Claude runs before presenting diff):** Confirm `forge build` exits 0. If the build fails, the diff is NOT presented — Claude fixes the failure and re-attempts.

2. **Diff review (manual, you review):**
   - **`contracts/StakedDegenerusStonk.sol`:** Verify SPEC-01 struct shape: `struct DayPending { uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned; }`. Verify SPEC-02 composite key: `mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions`. Verify SPEC-03 3-arg signature: `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)`. Verify SPEC-04 (c) delete-at-resolve: `delete pendingByDay[dayToResolve];` AFTER `redemptionPeriods[dayToResolve] = ...` AND AFTER `emit RedemptionResolved(...)`. Verify SPEC-04 (d) delete-on-full-claim: `delete pendingRedemptions[player][day];` AFTER `_payBurnie` AND AFTER `emit RedemptionClaimed`. Verify partial-claim branch preserved verbatim at the equivalent of `:659-665` (only `claim.ethValueOwed = 0;` on `!flipResolved`). Verify SPEC-05 lazy-init predicate: `if (pendingByDay[currentPeriod].supplySnapshot == 0 && pendingByDay[currentPeriod].burned == 0)`. Verify 5 storage slot deletions (`pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodIndex`, `redemptionPeriodBurned`). Verify `UnresolvedClaim` error deleted. Verify `PendingRedemption.periodIndex` field deleted. Verify `pendingRedemptionEthValue` + `pendingRedemptionBurnie` cumulative scalars preserved unchanged.
   - **`contracts/modules/DegenerusGameAdvanceModule.sol`:** Verify all 3 call sites pass `day - 1` as `dayToResolve` (search for `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)` — must return exactly 3 hits). Verify all 3 gate sites pass `day - 1` to `hasPendingRedemptions` (search for `sdgnrs.hasPendingRedemptions(day - 1)` — must return exactly 3 hits). Verify the `redemptionRoll` derivation lines (`((currentWord >> 8) % 151) + 25` at Sites 1/2 and `((fallbackWord >> 8) % 151) + 25` at Site 3) are BYTE-IDENTICAL — no RNG-input change per `feedback_rng_backward_trace.md`.
   - **`contracts/interfaces/IStakedDegenerusStonk.sol`:** Verify the 2 signature changes match SPEC-03 verbatim. Verify no other signatures changed.
   - **`test/fuzz/RedemptionGas.t.sol`:** Verify 5 compile-break sites updated. Verify `forge test --match-path "test/fuzz/RedemptionGas.t.sol" --no-match-test "PLACEHOLDER"` either passes or fails identically to the v43 baseline test (gas-benchmark intent preserved; the test exercises the same code path under per-day keying, with semantics adjusted per Mutation 13's per-day-keyed lookup).
   - **`test/fuzz/CoverageGap222.t.sol`:** Verify the selector string at `:948` is `"resolveRedemptionPeriod(uint16,uint32,uint32)"`. Verify the arg list has 3 `uint32(0)` args. Verify the assertion at `:973` is byte-identical to pre-refactor. Verify the `claimRedemption()` selector update (if Mutation 14 applied the 7th-site fix per Task 1 §C surfacing).

3. **No history comments check:** Visually scan all 5 files for forbidden lexicon: `previously`, `formerly`, `used to be`, `changed from`, `v43.0 used to`, `was X now Y`. Should find ZERO matches. Cross-references to `SPEC-NN` / `INV-NN` / `EDGE-NN` IDs are PERMITTED as informational anchors (e.g., "per SPEC-01" inside a header natspec block).

4. **No future-proofing scaffolding check:** Verify the diff does NOT contain: a migration shim, an `IStakedDegenerusStonkLegacy` interface, compatibility branches on the changed signatures, "TODO: migrate storage" prose, or "future v45 will" comments.

5. **No `git push` check:** Confirm Claude states it will NOT push and that push requires separate approval per `feedback_manual_review_before_push.md`.

6. **Approval signal:** Reply "approved" (or "ship it" / "commit" / equivalent) to authorize the commit. Reply with specific change requests if any aspect of the diff needs adjustment. Reply with "deny" / "abort" / "stop" to abort the phase.
  </how-to-verify>
  <resume-signal>Reply "approved" (or "ship it" / "commit") to land the single atomic commit. Reply with specific change requests for amendments. Reply "deny" / "abort" to halt Phase 305.</resume-signal>
  <acceptance_criteria>
    - `forge build` exits 0 BEFORE the diff is presented to the user (executor verifies internally; failure blocks user-approval step).
    - `git diff --name-only HEAD` returns exactly these 5 files: `contracts/StakedDegenerusStonk.sol`, `contracts/modules/DegenerusGameAdvanceModule.sol`, `contracts/interfaces/IStakedDegenerusStonk.sol`, `test/fuzz/RedemptionGas.t.sol`, `test/fuzz/CoverageGap222.t.sol` (5 files, no more, no less; if any other file appears in the diff, the executor STOPS and surfaces the over-scope to the user).
    - `grep -c "pendingRedemptionEthBase\|pendingRedemptionBurnieBase\|redemptionPeriodSupplySnapshot\|redemptionPeriodIndex\|redemptionPeriodBurned" contracts/StakedDegenerusStonk.sol` returns 0 (all 5 deleted slots are absent from the contract).
    - `grep -c "struct DayPending" contracts/StakedDegenerusStonk.sol` returns at least 1; `grep -c "mapping(uint32 => DayPending) internal pendingByDay" contracts/StakedDegenerusStonk.sol` returns exactly 1.
    - `grep -c "mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions" contracts/StakedDegenerusStonk.sol` returns exactly 1.
    - `grep -c "error UnresolvedClaim" contracts/StakedDegenerusStonk.sol` returns 0.
    - `grep -nE "function (hasPendingRedemptions|resolveRedemptionPeriod|claimRedemption)\(" contracts/StakedDegenerusStonk.sol` shows: `hasPendingRedemptions(uint32 day)`, `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)`, `claimRedemption(uint32 day)` — three matches with the new arity.
    - `grep -c "uint32  periodIndex" contracts/StakedDegenerusStonk.sol` returns 0 (the field is removed from `PendingRedemption`); however `grep -c "uint32 periodIndex" contracts/StakedDegenerusStonk.sol` may still return >0 because the event declaration at `:158`/`:161` uses `uint32 periodIndex` as an event arg name — that is PRESERVED (events out of SPEC §2.7 scope).
    - `grep -c "delete pendingByDay\[dayToResolve\]" contracts/StakedDegenerusStonk.sol` returns exactly 1 (SPEC-04 (c)).
    - `grep -c "delete pendingRedemptions\[" contracts/StakedDegenerusStonk.sol` returns exactly 1 (SPEC-04 (d) full-claim path); inspect that the delete is at the equivalent of `:660-661` (full-claim branch, not partial).
    - `grep -c "claim.ethValueOwed = 0" contracts/StakedDegenerusStonk.sol` returns exactly 1 (SPEC-04 (d) partial-claim branch preserved).
    - `grep -nc "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)" contracts/modules/DegenerusGameAdvanceModule.sol` returns exactly 3 (per D-305-DAYTORESOLVE-01 — all 3 sites pass IDENTICAL expression for future-grep enumeration).
    - `grep -nc "sdgnrs.hasPendingRedemptions(day - 1)" contracts/modules/DegenerusGameAdvanceModule.sol` returns exactly 3 (3 gate sites updated symmetrically).
    - `grep -nc "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)" contracts/modules/DegenerusGameAdvanceModule.sol` returns 0 (no pre-refactor 2-arg form survives).
    - `grep -nc "sdgnrs.hasPendingRedemptions()" contracts/modules/DegenerusGameAdvanceModule.sol` returns 0 (no pre-refactor 0-arg form survives).
    - `grep -c "function hasPendingRedemptions(uint32 day)\|function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)" contracts/interfaces/IStakedDegenerusStonk.sol` returns exactly 2 (both updated signatures present).
    - `grep -c "resolveRedemptionPeriod(uint16,uint32,uint32)" test/fuzz/CoverageGap222.t.sol` returns at least 1 (selector string updated).
    - `grep -c "resolveRedemptionPeriod(uint16,uint32)\"" test/fuzz/CoverageGap222.t.sol` returns 0 (no pre-refactor selector string survives).
    - `grep -v '^#\|^//\|^ \*' contracts/StakedDegenerusStonk.sol contracts/modules/DegenerusGameAdvanceModule.sol contracts/interfaces/IStakedDegenerusStonk.sol | grep -ciE "previously|formerly|used to be|changed from|v43\.0 used to|v44\.0 will|migration" returns 0 (no history-narration or future-proofing in the contract diff per D-305-APPROVAL-02 + D-305-APPROVAL-03).
    - The commit message ends with the conventional GSD trailer (`Co-Authored-By: Claude...`) or omits it per project convention — executor preserves the existing commit-message style of recent commits in the repo (`git log --oneline -10` for reference).
    - `git log -1 --format=%s` (post-commit) shows the commit subject begins with `feat(305-01):`.
    - `git diff HEAD~1 HEAD --stat` shows exactly the 5 expected files modified.
    - `git status` post-commit shows clean working tree (or only AGENT-COMMITTED `.planning/` artifacts that landed alongside per planner orchestration).
    - NO `git push` was executed (`git log origin/main..HEAD` shows at least one unpushed commit).
  </acceptance_criteria>
  <done>Task 2 complete when: (1) `forge build` PASS post-mutation; (2) USER explicitly approved the diff; (3) the single atomic commit landed with the conventional message; (4) all acceptance-criteria grep assertions hold; (5) no `git push` executed (separate approval required).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EOA → `burn(amount)` (`StakedDegenerusStonk.sol:486`) | Untrusted player input (sDGNRS amount) crosses into `_submitGamblingClaimFrom` writer of per-day pool. |
| EOA → `claimRedemption(uint32 day)` (`:618` post-refactor) | Untrusted player input (day arg) crosses into composite-keyed read + `_payEth` / `_payBurnie` external paths. |
| AdvanceModule → `resolveRedemptionPeriod(uint16, uint32, uint32 dayToResolve)` (`:585` post-refactor) | Trusted-but-authority-bounded input (`day - 1` from AdvanceModule's `_advanceGame` / `_gameOverEntropy` locals). The ACL check `msg.sender != ContractAddresses.GAME` at `:586` gates the boundary. |
| AdvanceModule → `hasPendingRedemptions(uint32 day)` (`:577` post-refactor) | Read-only query; no authority check; any caller can probe pool existence per-day. |
| sStonk → `coinflip.getCoinflipDayResult(period.flipDay)` (`:649-650`) | External read; trusted oracle; UNCHANGED post-refactor. |
| sStonk → `game.resolveRedemptionLootbox(player, lootboxEth, entropy, actScore)` (`:672`) | External write; trusted Game module; UNCHANGED post-refactor. |
| Storage layout boundary | V43 layout → V44 layout breaks intentionally at this commit per `feedback_frozen_contracts_no_future_proofing.md` pre-launch posture; no migration scaffolding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305-01 | Tampering | `resolveRedemptionPeriod`'s `dayToResolve` arg | mitigate | ACL check at `:586` `if (msg.sender != ContractAddresses.GAME) revert Unauthorized();` PRESERVED VERBATIM (Mutation 8 step 1). Only AdvanceModule (the bounded-authority caller) sets `dayToResolve`; AdvanceModule's own value (`day - 1`) is computed from its `_advanceGame(uint48 ts, uint32 day, ...)` / `_gameOverEntropy(uint48 ts, uint32 day, ...)` local-variable `day` which is the wall-clock day passed from a higher-level frame (no player-controllable input to that `day` derivation per `feedback_rng_commitment_window.md`). V-184 catastrophic cross-day re-roll exploit and its 6 subsumed catalog rows (V-186/V-188/V-190/V-191/V-192/V-193) close STRUCTURALLY at v44.0 (per FIXREC §103.A.4 + SPEC §2.0 clause 1) by ABSENCE of the overwrite primitive: each day's `redemptionPeriods[D]` is a distinct mapping slot; no later action can re-write a resolved day's roll because no code path passes a `dayToResolve` value equal to an already-resolved day (D-305-DAYTORESOLVE-01 + SPEC-03 lock `dayToResolve = day - 1` + AdvanceModule's catch-up loop walks strictly forward by wall-day). |
| T-305-02 | Tampering | `claimRedemption(uint32 day)`'s `day` arg | mitigate | Composite-key read `pendingRedemptions[msg.sender][day]` enforces per-(player, day) isolation. A malicious player passing a `day` value other than their own burn day reads a slot that is `ethValueOwed == 0 && burnieOwed == 0` → triggers `revert NoClaim()` at the equivalent of `:621` (Mutation 9 step 3). No payout is possible without a corresponding burn writing to `(msg.sender, day)`. Per INV-07 — no self-roll manipulation via timing: the window `[T_burn, T_full_claim]` cannot be retroactively re-written because subsequent burns target `(msg.sender, currentDay != day)` — a distinct composite slot. |
| T-305-03 | Elevation of Privilege | Storage-layout admin paths | accept | No new admin paths introduced. Writer set for `redemptionPeriods[D].roll` UNCHANGED — still AdvanceModule exclusive via `resolveRedemptionPeriod`'s ACL check. Phase 300 ADMA audit (v43.0) attested zero exempt-writer-slot widening at v43 close HEAD; this refactor inherits that attestation by symmetry (the writer SET is unchanged; only the storage SHAPE the writers act on changed). Phase 307 SWEEP `/contract-auditor` will re-verify under the v44 audit subject. |
| T-305-04 | Denial of Service | Per-day mapping unbounded growth | accept | A malicious player burning every day grows `pendingByDay[day]` storage linearly with day count (per EDGE-03). Per SPEC-04 (c) `delete pendingByDay[dayToResolve]` fires at resolve so RESOLVED days refund their 3 slots immediately. Only unresolved + unclaimed days persist — bounded by the actual wall-day count in the gap window between burn and full-claim. Player pays own storage cost (gas of writes). No unbounded loop iterates over `pendingByDay`. `resolveRedemptionPeriod` is O(1) — one mapping read + one mapping write + one mapping delete. `claimRedemption` is O(1). `_submitGamblingClaimFrom` is O(1). Phase 306 TST-06 gas regression bench (Phase 306, OUT OF SCOPE for 305) will quantify burn ≤ +5% / claim ≤ +0% per the SPEC §2.0 soft target. |
| T-305-05 | Repudiation | Event emission ordering | mitigate | `RedemptionResolved` event emission preserved at the same site per SPEC-04 (c) — fires AFTER `redemptionPeriods[dayToResolve]` is written AND BEFORE `delete pendingByDay[dayToResolve]`. `RedemptionSubmitted` and `RedemptionClaimed` event topic hashes are BYTE-IDENTICAL (no signature change; param name `periodIndex` semantically the day; not renamed per SPEC §2.7 events-not-flagged scope). Indexer migration not required. |
| T-305-06 | Spoofing | `msg.sender` semantics on `claimRedemption(uint32 day)` | accept | `msg.sender` identity check preserved; under composite keying `pendingRedemptions[msg.sender][day]` ensures only the player whose address keyed the burn can read it. No new spoofing surface. |
| T-305-07 | Information Disclosure | `pendingRedemptions` public visibility | accept | `pendingRedemptions` mapping retains `public` visibility (Mutation 4) → Solidity auto-generates the composite-key getter `function pendingRedemptions(address, uint32) external view returns (uint96, uint96, uint16)` exposing the 3-field struct (the `periodIndex` field is gone so the getter return tuple is now 3-arity instead of 4). Pre-launch posture means external indexers depending on the auto-getter break; this is an INTENDED ABI break per `feedback_frozen_contracts_no_future_proofing.md` — no compatibility shim. Phase 307 SWEEP `/contract-auditor` may flag indexer-migration as a doc item; Phase 308 §3.A delta-surface attests the break. |
| T-305-08 | Tampering | RNG commitment window between resolve gate and resolve write | accept | Per `feedback_rng_commitment_window.md` + Phase 304 SPEC INV-06: the RNG derivation `redemptionRoll = uint16(((currentWord >> 8) % 151) + 25)` at AdvanceModule `:1226` / `:1289` / `:1319` consumes `currentWord` (or `fallbackWord`) which was committed by VRF at an earlier moment. Phase 305 IMPL adds NO new SLOADs inside the RNG-derivation window of the AdvanceModule writer (`_applyDailyRng` body at `:1216`). The new `hasPendingRedemptions(day - 1)` gate at `:1225` reads `pendingByDay[day - 1]` slot AFTER `_applyDailyRng` returns `currentWord`, not inside the RNG-derivation window — relative ordering preserved from v43 (v43's zero-arg gate at the same source line had the same ordering). F-41-02/03 class of bugs not reintroduced. Phase 307 SWEEP `/contract-auditor` re-verifies per `feedback_rng_window_storage_read_freshness.md`. |
| T-305-09 | Tampering | Inline-duplicated business logic across 3 AdvanceModule call sites | mitigate | Per `feedback_verify_call_graph_against_source.md` Phase 294 BURNIE-gap precedent — recurring failure mode where partial fix modifies only one of N duplicates. Phase 304 Plan 05 §5.2 "EXHAUSTIVE CALL-GRAPH ATTESTATION" + Task 1 §B re-verification + Task 2 acceptance criterion grep counts (`exactly 3` hits at `:1230`/`:1293`/`:1323` for resolve; `exactly 3` at `:1225`/`:1288`/`:1318` for gate; symmetric `IDENTICAL day - 1` expression at all 3 sites) prevent the partial-fix failure mode. |

**Security-first hard floor per SPEC §2.0 + `feedback_security_over_gas.md`:** No optimization, packing, slot-reuse, or shortcut may collapse the `(player, day)` key space or alias rolls across days. The V-184 catastrophe class closes STRUCTURALLY at v44.0 — not by gating, not by an ordering check, but by absence of the overwrite primitive. Phase 305 IMPL produces the source mutations; Phase 306 TST proves INV-01..12; Phase 307 SWEEP adversarially re-validates; Phase 308 TERMINAL §3.D RESOLVED-AT-V44 attests the closure.

**Phase 307 SWEEP scope (NOT this phase):** 3-skill HYBRID `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` re-validates against the post-refactor source. Skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md` BEFORE any user-pause. If SWEEP surfaces a Tier-1 or Tier-2 finding, a FIXREC-augment commit per `D-284-ADVERSARIAL-RE-PASS-01` lands separately (NOT in this phase).
</threat_model>

<verification>

## Phase-Level Success Criteria (from ROADMAP.md §"Phase 305")

Verify after Task 2 lands the commit:

1. **`forge build` PASS against the patched contract tree.** Run: `forge build 2>&1 | tail -20`. Expected: build succeeds; zero compile errors; warnings unchanged from v43 baseline (no new warnings beyond struct-field-removal or signature-arity-change cascading effects on existing callers).

2. **Storage layout diff vs v43.0 baseline shows the 5 slots removed + `pendingByDay` mapping added + `pendingRedemptions` re-keyed; storage breaks ACCEPTED per pre-launch posture.** Run: `forge inspect contracts/StakedDegenerusStonk.sol:StakedDegenerusStonk storageLayout > /tmp/v44-storage.json && diff <(git show MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2:contracts/StakedDegenerusStonk.sol > /tmp/v43.sol && forge inspect /tmp/v43.sol:StakedDegenerusStonk storageLayout 2>/dev/null) /tmp/v44-storage.json | head -50`. Expected: diff shows 5 deletions (the 5 named slots) + 1 mapping addition (`pendingByDay`) + 1 mapping re-keying (`pendingRedemptions` outer→inner shape change). No other storage delta. If the v43 baseline isn't reachable via tag, the human verification step accepts a manual reading of the diff against the 304-SPEC.md §1 storage table.

3. **`_submitGamblingClaimFrom` writes burns to `pendingByDay[currentDayView()]` (per-day pool); cumulative `pendingRedemptionEthValue` global continues to track the cross-day sum unchanged.** Run: `grep -n "pendingByDay\[currentPeriod\]\.\(ethBase\|burnieBase\|burned\|supplySnapshot\)" contracts/StakedDegenerusStonk.sol`. Expected: at least 5 hits — lazy-init write (`supplySnapshot`), cap-check read (`burned + amount > supplySnapshot / 2`), cap accumulator (`burned +=`), segregate writes (`ethBase +=`, `burnieBase +=`). Also `grep -n "pendingRedemptionEthValue +=" contracts/StakedDegenerusStonk.sol`. Expected: exactly 1 hit (the cumulative `+=` in `_submitGamblingClaimFrom` preserved per SPEC-01 unchanged-list).

4. **`resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` accepts the day arg + writes `redemptionPeriods[dayToResolve]` + deletes `pendingByDay[dayToResolve]`; `AdvanceModule` 3 call sites pass `dayToResolve = day - 1`.** Run: `grep -A 30 "function resolveRedemptionPeriod" contracts/StakedDegenerusStonk.sol | grep -E "redemptionPeriods\[dayToResolve\]|delete pendingByDay\[dayToResolve\]|emit RedemptionResolved\(dayToResolve"`. Expected: at least 3 hits (write, emit, delete). Also `grep -nc "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay, day - 1)" contracts/modules/DegenerusGameAdvanceModule.sol`. Expected: exactly 3.

5. **`claimRedemption(uint32 day)` reads composite-keyed `pendingRedemptions[msg.sender][day]` + `redemptionPeriods[day]`; `UnresolvedClaim` revert at `:796-797` REMOVED; `delete pendingRedemptions[msg.sender][day]` after payout for storage refund. USER-APPROVED diff committed exactly once.** Run: `grep -n "pendingRedemptions\[" contracts/StakedDegenerusStonk.sol`. Expected hits include: declaration `mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions;`, `_submitGamblingClaimFrom`'s composite read `pendingRedemptions[beneficiary][currentPeriod]`, `claimRedemption`'s composite read `pendingRedemptions[player][day]`, and the full-claim `delete pendingRedemptions[player][day]`. Also `grep -c "UnresolvedClaim" contracts/StakedDegenerusStonk.sol`. Expected: 0 (error declaration AND revert site both deleted). Also `git log --oneline -1 | grep -c "305-01"`. Expected: 1 (single commit covering the batched diff).

## Threat Model Gates

After Task 2:
- `grep -v '^//\|^ \*' contracts/StakedDegenerusStonk.sol contracts/modules/DegenerusGameAdvanceModule.sol contracts/interfaces/IStakedDegenerusStonk.sol | grep -ciE "previously|formerly|used to be|changed from|v43\.0 used|v44\.0 will|migration"` returns 0. (No history narration; no future-proofing prose; D-305-APPROVAL-02 + D-305-APPROVAL-03.)
- `grep -c "IStakedDegenerusStonkLegacy" contracts/` returns 0 (no legacy interface introduced).
- `grep -nc "sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)" contracts/modules/DegenerusGameAdvanceModule.sol` returns 0 (no pre-refactor 2-arg form survives — partial-fix failure mode prevented per Phase 294 BURNIE-gap precedent + `feedback_verify_call_graph_against_source.md`).
- `grep -c "function resolveRedemptionPeriod(uint16 roll, uint32 flipDay)" contracts/StakedDegenerusStonk.sol` returns 0 (the 2-arg signature has been fully replaced).

## Manual Approval Gate Verification

- The commit landed AFTER explicit user approval (Task 2 is a `checkpoint:human-verify` task; the executor does NOT commit without an "approved"-class signal per `feedback_wait_for_approval.md`).
- `git log origin/main..HEAD` shows the commit is NOT pushed (push requires separate approval per `feedback_manual_review_before_push.md` D-305-APPROVAL-04).

</verification>

<success_criteria>

Phase 305 IMPL is complete when:

1. Task 1's `.planning/phases/305-implementation-impl/305-01-GREP-VERIFICATION.md` artifact exists with §A/§B/§C/§D/§E sections; §D attests "all VERIFIED-NO-DRIFT" (the working tree is byte-identical to v43.0 baseline for cited locations) or enumerates corrections; §E lists 14 mutations in execution order.

2. Task 2's single atomic commit lands covering exactly 5 files: `contracts/StakedDegenerusStonk.sol`, `contracts/modules/DegenerusGameAdvanceModule.sol`, `contracts/interfaces/IStakedDegenerusStonk.sol`, `test/fuzz/RedemptionGas.t.sol`, `test/fuzz/CoverageGap222.t.sol`. The commit was USER-APPROVED per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` + `feedback_wait_for_approval.md`. No `git push` executed.

3. `forge build` PASS post-commit. Storage layout diff vs v43 baseline shows the 5 slot deletions + `pendingByDay` mapping addition + `pendingRedemptions` composite re-keying (and nothing else).

4. `_submitGamblingClaimFrom` writes burns to `pendingByDay[currentDayView()]` with SPEC-05 lazy-init; cumulative `pendingRedemptionEthValue` global continues to track the cross-day sum unchanged.

5. `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` accepts the day arg + writes `redemptionPeriods[dayToResolve]` + emits `RedemptionResolved` + deletes `pendingByDay[dayToResolve]`. All 3 AdvanceModule call sites (`:1230` rngGate primary, `:1293` _gameOverEntropy stale-VRF, `:1323` _gameOverEntropy fallback) pass `day - 1` as `dayToResolve`. All 3 gate sites (`:1225`, `:1288`, `:1318`) pass `day - 1` to `hasPendingRedemptions(uint32 day)`.

6. `claimRedemption(uint32 day)` reads composite-keyed `pendingRedemptions[msg.sender][day]` + `redemptionPeriods[day]`; existing `NotResolved` revert preserved (`period.roll == 0`); `delete pendingRedemptions[msg.sender][day]` on full-claim; partial-claim branch preserved verbatim. `UnresolvedClaim` error declaration + revert site both DELETED.

7. No history-narration comments in any of the 3 mutated contract files (`feedback_no_history_in_comments.md` + D-305-APPROVAL-02). No future-proofing scaffolding (`feedback_frozen_contracts_no_future_proofing.md` + D-305-APPROVAL-03).

8. All 4 of IMPL-01..04 satisfied:
   - IMPL-01 (storage refactor): Mutations 1, 2, 3, 4, 5, 6, 10 step 3.
   - IMPL-02 (`_submitGamblingClaimFrom` rewrite): Mutation 10 + the SPEC-05 lazy-init at step 3.
   - IMPL-03 (`resolveRedemptionPeriod` 3-arg + AdvanceModule call sites): Mutations 8 + 12.
   - IMPL-04 (`claimRedemption(uint32 day)` + composite read + delete-on-full-claim): Mutation 9.

</success_criteria>

<output>
After Task 2 commit lands, create `.planning/phases/305-implementation-impl/305-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md` with these specific fields:

- `phase: 305-implementation-impl`
- `plan: 01`
- `subsystem: sStonk per-day redemption refactor IMPL`
- `requires: [IMPL-01, IMPL-02, IMPL-03, IMPL-04, SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05, INV-01..12, EDGE-01..18]` (consumes the LOCKED SPEC + invariants the refactor must satisfy)
- `provides: [v44.0-storage-layout-break, per-day-keyed-redemption, V-184-structural-elimination, HANDOFF-111..117-structural-closure, dayToResolve-explicit-arg, composite-key-pendingRedemptions]`
- `affects: [contracts/StakedDegenerusStonk.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/interfaces/IStakedDegenerusStonk.sol, test/fuzz/RedemptionGas.t.sol, test/fuzz/CoverageGap222.t.sol]`
- `decisions:` document any executor-discretion picks (alias name choice for `DayPending storage`, NoClaim sentinel choice, RedemptionGas.t.sol option (a) vs (b) for the resolve-day semantic, 7th compile-break site fix in CoverageGap222.t.sol).
- `metrics:` commit SHA, file count (5), mutation count (14), line counts.

Summary body sections:
- **What was built** — enumerate the 14 mutations actually executed; cite SPEC-NN anchors; reference the USER-approved commit SHA.
- **Citations re-verified** — link to `305-01-GREP-VERIFICATION.md`; summarize §D verdict (all-verified vs corrections).
- **V-184 closure attestation** — explicit statement: "V-184 catastrophe + HANDOFF-111..117 close STRUCTURALLY by absence of the overwrite primitive (per SPEC §2.0 Priority Statement clause 1). Phase 306 TST mechanizes the closure assertion via EDGE-07 + TST-04 + TST-05. Phase 308 §3.D attests RESOLVED-AT-V44."
- **Self-check: PASSED** — bullet list against the success_criteria above. Re-run the key grep assertions and paste output.
- **Deviations from Plan** — any auto-fixed issues (e.g., a 7th compile-break site Task 1 §C surfaced beyond CONTEXT.md's enumeration, or a Task 1 §A drift correction).
- **Handoff to Phase 306 TST** — explicit statement that the refactor is ready for Foundry invariant + fuzz coverage; INV-01..12 mechanization is the Phase 306 TST primary deliverable.
</output>
