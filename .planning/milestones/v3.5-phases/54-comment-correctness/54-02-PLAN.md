---
phase: 54-comment-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.5-comment-findings-54-02-high-risk-modules.md
autonomous: true
requirements:
  - CMT-01
  - CMT-02
  - CMT-03
  - CMT-04
must_haves:
  truths:
    - "Every NatSpec tag in AdvanceModule.sol and LootboxModule.sol is verified against current code"
    - "No stale references to pre-skim-redesign or pre-lootbox-split behavior exist"
    - "Inline comments accurately describe the skim pipeline and lootbox routing code"
    - "All findings are documented with ID, severity, contract, line ref, and recommendation"
  artifacts:
    - path: "audit/v3.5-comment-findings-54-02-high-risk-modules.md"
      provides: "Comment correctness findings for AdvanceModule.sol and LootboxModule.sol"
      contains: "CMT-V35-"
  key_links:
    - from: "audit/v3.5-comment-findings-54-02-high-risk-modules.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "Line references in findings"
      pattern: "AdvanceModule\\.sol"
    - from: "audit/v3.5-comment-findings-54-02-high-risk-modules.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "Line references in findings"
      pattern: "LootboxModule\\.sol"
---

<objective>
Audit every NatSpec tag and inline comment in the two highest-risk game modules: DegenerusGameAdvanceModule.sol (~1,453 lines) and DegenerusGameLootboxModule.sol (~1,814 lines). AdvanceModule was rewritten for the v3.4 skim pipeline redesign and received fba43f2c NatSpec fixes. LootboxModule received the redemption lootbox 50/50 split in v3.4.

Purpose: These modules contain the newest code in the protocol. AdvanceModule's skim pipeline was redesigned in v3.4 with new bit-field consumption, overshoot surcharge, and take cap logic. LootboxModule got the redemption lootbox routing. Both are prime targets for NatSpec drift.

Output: `audit/v3.5-comment-findings-54-02-high-risk-modules.md` -- findings table with CMT-V35-NNN IDs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@audit/v3.2-findings-consolidated.md
@audit/v3.4-findings-consolidated.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment correctness audit of AdvanceModule.sol and LootboxModule.sol</name>
  <files>audit/v3.5-comment-findings-54-02-high-risk-modules.md</files>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/modules/DegenerusGameLootboxModule.sol
    audit/v3.2-findings-consolidated.md
    audit/v3.4-findings-consolidated.md
    audit/KNOWN-ISSUES.md
  </read_first>
  <action>
Read each of the two module contracts in full. For EVERY function, event, error, and state variable:

1. **NatSpec tags**: Verify every @notice, @dev, @param, @return, @custom:reverts, @inheritdoc tag against the actual implementation. Check that:
   - @param names match function parameter names exactly
   - @param descriptions match the parameter's actual usage
   - @return descriptions match actual return values and semantics
   - @dev notes accurately describe implementation details, caller lists, CEI order
   - @notice one-liners accurately summarize function purpose
   - @custom:reverts tags have matching revert paths in the implementation
   - Block comments and section headers accurately describe their sections

2. **AdvanceModule-specific checks**:
   - Skim pipeline comments: verify 5-step pipeline description matches code (overshoot surcharge, ratio adjustment, bit-field consumption, triangular variance, take cap)
   - Bit-field allocation NatSpec: verify documented bit ranges match actual rngWord shifts (the fba43f2c fix updated these -- verify the fix text is correct)
   - Overshoot surcharge formula comments: verify math expressions match code
   - wireVrf NatSpec: verify all @param tags present (CMT-V32-004 flagged missing @param keyHash_)

3. **LootboxModule-specific checks**:
   - Redemption lootbox 50/50 split routing comments: verify they match the actual split logic
   - PlayerCredited event NatSpec: verify all @param tags present (CMT-V32-003 flagged missing @param recipient)
   - Lootbox resolution paths: verify inline comments match ETH/BURNIE flow
   - Reclassification logic comments: verify they match actual accounting-only reclassification (no ETH transfer)

4. **v3.2/v3.4 "accept as known" findings to re-verify in this batch**:
   - CMT-V32-003: LootboxModule PlayerCredited event missing @param recipient -- check if now fixed
   - CMT-V32-004: AdvanceModule wireVrf missing @param keyHash_ -- check if now fixed
   - F-50-01: AdvanceModule full 256-bit modulo documented -- verify NatSpec update from fba43f2c is accurate
   - F-50-02: AdvanceModule bit overlap documented -- verify NatSpec update from fba43f2c is accurate

5. **fba43f2c fix commit verification**: For AdvanceModule lines modified in the fix commit (bit-field NatSpec at lines ~1023, ~1036-1037), verify the NEW text accurately describes the actual bit consumption pattern. Prior v3.1 experience showed fix text can introduce new errors.

6. **Deduplication**: Before reporting any finding, check against v3.1, v3.2, and v3.4 consolidated findings. Skip if already fixed. Only report if issue persists or fix is wrong.

**Output format**: Create `audit/v3.5-comment-findings-54-02-high-risk-modules.md` with:
- Header: title, date, scope (2 modules, ~3,267 lines), pass (v3.5 comment correctness), mode (flag-only)
- Summary table: contract | new findings | prior findings verified | total
- Per-contract sections with:
  - Review scope: line count, NatSpec tag count, function count
  - Prior fix verification status for v3.2/v3.4 findings in this contract
  - New findings in format: `| CMT-V35-NNN | LOW or INFO | Contract.sol | line | Summary | Recommendation |`
  - Fresh scan notes
- If zero findings for a contract, state "No new findings. All NatSpec verified accurate."

**Severity guidelines**:
- LOW: A C4A warden would likely file this (wrong @param name, stale @custom:reverts, factually incorrect @dev)
- INFO: Cosmetic improvement only (could be more precise, minor wording, missing optional tag)
  </action>
  <verify>
    <automated>test -f audit/v3.5-comment-findings-54-02-high-risk-modules.md && grep -c "CMT-V35-" audit/v3.5-comment-findings-54-02-high-risk-modules.md; grep -q "AdvanceModule" audit/v3.5-comment-findings-54-02-high-risk-modules.md && grep -q "LootboxModule" audit/v3.5-comment-findings-54-02-high-risk-modules.md && echo "Both modules covered"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-comment-findings-54-02-high-risk-modules.md exists and is non-empty
    - File contains "AdvanceModule" section with review scope stated
    - File contains "LootboxModule" section with review scope stated
    - Every finding row contains: CMT-V35-NNN | severity | Contract.sol | line number | summary | recommendation
    - File contains verification status for CMT-V32-003 (LootboxModule PlayerCredited event)
    - File contains verification status for CMT-V32-004 (AdvanceModule wireVrf @param)
    - File contains verification status for F-50-01 (AdvanceModule 256-bit modulo NatSpec)
    - File contains verification status for F-50-02 (AdvanceModule bit overlap NatSpec)
    - No finding ID duplicates an existing v3.1/v3.2/v3.4 finding that was already fixed
  </acceptance_criteria>
  <done>All NatSpec tags and inline comments in AdvanceModule.sol and LootboxModule.sol verified. Findings documented with line references. Prior v3.2/v3.4 findings in these contracts re-verified.</done>
</task>

</tasks>

<verification>
- audit/v3.5-comment-findings-54-02-high-risk-modules.md exists with findings table
- Both modules have dedicated review sections
- Prior findings (CMT-V32-003, CMT-V32-004, F-50-01, F-50-02) have verification status
- No duplicate findings against v3.1/v3.2/v3.4 consolidated reports
</verification>

<success_criteria>
Every @param, @return, @dev, @notice, @custom:reverts, @inheritdoc tag and every inline comment in AdvanceModule.sol and LootboxModule.sol has been checked against the implementation. All findings are documented with contract, line number, and recommendation. Prior "accept as known" findings from v3.2/v3.4 have been re-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/54-comment-correctness/54-02-SUMMARY.md`
</output>
