---
phase: 54-comment-correctness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.5-comment-findings-54-05-game-modules.md
autonomous: true
requirements:
  - CMT-01
  - CMT-02
  - CMT-03
  - CMT-04
must_haves:
  truths:
    - "Every NatSpec tag in all 10 game module contracts is verified against current code"
    - "No stale references to removed features exist in module contracts"
    - "Inline comments accurately describe module logic"
    - "All findings are documented with ID, severity, contract, line ref, and recommendation"
  artifacts:
    - path: "audit/v3.5-comment-findings-54-05-game-modules.md"
      provides: "Comment correctness findings for JackpotModule, MintModule, DegeneretteModule, DecimatorModule, WhaleModule, EndgameModule, GameOverModule, BoonModule, PayoutUtils, MintStreakUtils"
      contains: "## "
  key_links:
    - from: "audit/v3.5-comment-findings-54-05-game-modules.md"
      to: "contracts/modules/"
      via: "Line references in findings"
      pattern: "Module\\.sol"
---

<objective>
Audit every NatSpec tag and inline comment in the 10 game module contracts not covered by Plan 54-02: DegenerusGameJackpotModule.sol (~2,795 lines), DegenerusGameMintModule.sol (~1,199 lines), DegenerusGameDegeneretteModule.sol (~1,179 lines), DegenerusGameDecimatorModule.sol (~1,024 lines), DegenerusGameWhaleModule.sol (~840 lines), DegenerusGameEndgameModule.sol (~540 lines), DegenerusGameBoonModule.sol (~359 lines), DegenerusGameGameOverModule.sol (~235 lines), DegenerusGamePayoutUtils.sol (~94 lines), DegenerusGameMintStreakUtils.sol (~62 lines). Total ~8,327 lines.

Purpose: These modules were NOT modified since v3.2 and were fully audited then. This is a verification pass to confirm no drift occurred from indirect changes. Several v3.2 "accept as known" findings need re-verification. Despite the large line count, most of this is confirmation of already-verified NatSpec.

Output: `audit/v3.5-comment-findings-54-05-game-modules.md` -- findings table with CMT-V35-NNN IDs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@audit/v3.2-findings-consolidated.md
@audit/v3.4-findings-consolidated.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment correctness audit of 10 game module contracts</name>
  <files>audit/v3.5-comment-findings-54-05-game-modules.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/modules/DegenerusGameMintModule.sol
    contracts/modules/DegenerusGameDegeneretteModule.sol
    contracts/modules/DegenerusGameDecimatorModule.sol
    contracts/modules/DegenerusGameWhaleModule.sol
    contracts/modules/DegenerusGameEndgameModule.sol
    contracts/modules/DegenerusGameBoonModule.sol
    contracts/modules/DegenerusGameGameOverModule.sol
    contracts/modules/DegenerusGamePayoutUtils.sol
    contracts/modules/DegenerusGameMintStreakUtils.sol
    audit/v3.2-findings-consolidated.md
    audit/KNOWN-ISSUES.md
  </read_first>
  <action>
Read each of the 10 module contracts. These were NOT modified since v3.2, so the primary task is:
(a) Re-verify v3.2 "accept as known" findings for current status
(b) Spot-check all NatSpec for any issues missed in prior passes

For EVERY function, event, error, and state variable, verify NatSpec accuracy. Pay special attention to:

1. **v3.2 "accept as known" findings to re-verify**:
   - CMT-104 (DegenerusJackpots, but checked via JackpotModule): OnlyCoin says "coinflip contract" but accepts COIN+COINFLIP -- NOTE: CMT-104 is in DegenerusJackpots.sol, not a module. If this contract is NOT in this batch, note in the report that it will be checked in Plan 54-06.
   - CMT-V32-002 (JackpotModule): "BURNIE only, no ETH bonuses" inline comment -- verify if this accurately describes the payout logic
   - CMT-V32-005 (MintModule): processFutureTicketBatch @return writesUsed "queue entries" -- verify if the return description matches actual semantics
   - CMT-V32-006 (DecimatorModule): decClaimable @return "or expired" -- verify if expiry concept still exists in implementation
   - CMT-V32-001 (JackpotModule): @return ticketSpent -- verify current accuracy
   - DRIFT-V32-001 (GameOverModule): _sendToVault hard-revert undocumented -- check if NatSpec now documents the hard revert behavior. Cross-reference KNOWN-ISSUES.md entry about _sendToVault hard reverts being intentional design.

2. **NatSpec tag verification**: For each function:
   - @param names match parameter names
   - @return descriptions match return semantics
   - @dev caller lists are complete
   - @custom:reverts tags match actual revert paths
   - Block comments and section headers match their sections

3. **Inline comments**: Verify // comments describe adjacent code accurately, particularly in:
   - Jackpot distribution logic
   - Mint queue processing
   - Decimator resolution paths
   - Payout calculations

4. **Deduplication**: Before reporting any finding, check against v3.1, v3.2, and v3.4 consolidated findings. Skip if already fixed. Only report if issue persists or fix is wrong.

**Output format**: Create `audit/v3.5-comment-findings-54-05-game-modules.md` with:
- Header: title, date, scope (10 modules, ~8,327 lines), pass (v3.5 comment correctness), mode (flag-only)
- Summary table: contract | new findings | prior findings verified | total
- Per-contract sections with:
  - Review scope: line count, NatSpec tag count, function count
  - Prior fix verification status for v3.2 findings in this contract
  - New findings in format: `| CMT-V35-NNN | LOW or INFO | Contract.sol | line | Summary | Recommendation |`
  - Fresh scan notes
- If zero findings for a contract, state "No new findings. All NatSpec verified accurate."

**Severity guidelines**:
- LOW: A C4A warden would likely file this (wrong @param name, stale @custom:reverts, factually incorrect @dev)
- INFO: Cosmetic improvement only (could be more precise, minor wording, missing optional tag)

**Context budget note**: This batch has ~8,327 lines across 10 files. Read the larger files (JackpotModule 2,795 lines, MintModule 1,199 lines, DegeneretteModule 1,179 lines) in sections if needed. Prioritize the v3.2 re-verification items, then do a systematic sweep of remaining NatSpec.
  </action>
  <verify>
    <automated>test -f audit/v3.5-comment-findings-54-05-game-modules.md && grep -c "CMT-V35-\|No new findings" audit/v3.5-comment-findings-54-05-game-modules.md; grep -q "JackpotModule" audit/v3.5-comment-findings-54-05-game-modules.md && grep -q "MintModule" audit/v3.5-comment-findings-54-05-game-modules.md && grep -q "DecimatorModule" audit/v3.5-comment-findings-54-05-game-modules.md && grep -q "GameOverModule" audit/v3.5-comment-findings-54-05-game-modules.md && echo "Key modules covered"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-comment-findings-54-05-game-modules.md exists and is non-empty
    - File contains a section for each of the 10 module contracts
    - Every finding row contains: CMT-V35-NNN | severity | Contract.sol | line number | summary | recommendation
    - File contains verification status for CMT-V32-001 (JackpotModule @return ticketSpent)
    - File contains verification status for CMT-V32-002 (JackpotModule "BURNIE only" inline)
    - File contains verification status for CMT-V32-005 (MintModule @return writesUsed)
    - File contains verification status for CMT-V32-006 (DecimatorModule @return "or expired")
    - File contains verification status for DRIFT-V32-001 (GameOverModule _sendToVault)
    - No finding ID duplicates an existing v3.1/v3.2/v3.4 finding that was already fixed
  </acceptance_criteria>
  <done>All NatSpec tags and inline comments in all 10 game module contracts verified. Findings documented with line references. Prior v3.2 "accept as known" findings re-verified for current status.</done>
</task>

</tasks>

<verification>
- audit/v3.5-comment-findings-54-05-game-modules.md exists with findings table
- All 10 module contracts have dedicated review sections
- Prior findings (CMT-V32-001, CMT-V32-002, CMT-V32-005, CMT-V32-006, DRIFT-V32-001) have verification status
- No duplicate findings against v3.1/v3.2/v3.4 consolidated reports
</verification>

<success_criteria>
Every @param, @return, @dev, @notice, @custom:reverts tag and every inline comment in all 10 game module contracts has been checked against the implementation. All findings documented. Prior v3.2 "accept as known" findings re-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/54-comment-correctness/54-05-SUMMARY.md`
</output>
