---
phase: 54-comment-correctness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.5-comment-findings-54-04-core-storage.md
autonomous: true
requirements:
  - CMT-01
  - CMT-02
  - CMT-03
  - CMT-04
must_haves:
  truths:
    - "Every NatSpec tag in DegenerusAdmin.sol, DegenerusVault.sol, and DegenerusGameStorage.sol is verified against current code"
    - "No stale references to removed features or incorrect slot boundary comments exist"
    - "Inline comments accurately describe admin governance, vault mechanics, and storage layout"
    - "All findings are documented with ID, severity, contract, line ref, and recommendation"
  artifacts:
    - path: "audit/v3.5-comment-findings-54-04-core-storage.md"
      provides: "Comment correctness findings for DegenerusAdmin.sol, DegenerusVault.sol, DegenerusGameStorage.sol"
      contains: "CMT-V35-"
  key_links:
    - from: "audit/v3.5-comment-findings-54-04-core-storage.md"
      to: "contracts/DegenerusAdmin.sol"
      via: "Line references in findings"
      pattern: "DegenerusAdmin\\.sol"
    - from: "audit/v3.5-comment-findings-54-04-core-storage.md"
      to: "contracts/DegenerusVault.sol"
      via: "Line references in findings"
      pattern: "DegenerusVault\\.sol"
    - from: "audit/v3.5-comment-findings-54-04-core-storage.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "Line references in findings"
      pattern: "GameStorage\\.sol"
---

<objective>
Audit every NatSpec tag and inline comment in DegenerusAdmin.sol (~804 lines), DegenerusVault.sol (~1,050 lines), and DegenerusGameStorage.sol (~1,599 lines). All three received fba43f2c fix commit changes. GameStorage has the critical storage slot layout diagram that must match actual variable declarations.

Purpose: These three contracts form the administrative and storage backbone of the protocol. The fba43f2c fix commit modified NatSpec in all three, and the fixes themselves need verification (prior experience showed fix text can be wrong). GameStorage's slot boundary comments are critical for wardens auditing storage layout.

Output: `audit/v3.5-comment-findings-54-04-core-storage.md` -- findings table with CMT-V35-NNN IDs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@audit/v3.2-findings-consolidated.md
@audit/v3.4-findings-consolidated.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment correctness audit of DegenerusAdmin.sol, DegenerusVault.sol, DegenerusGameStorage.sol</name>
  <files>audit/v3.5-comment-findings-54-04-core-storage.md</files>
  <read_first>
    contracts/DegenerusAdmin.sol
    contracts/DegenerusVault.sol
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.2-findings-consolidated.md
    audit/v3.4-findings-consolidated.md
    audit/KNOWN-ISSUES.md
  </read_first>
  <action>
Read each of the three contracts in full. For EVERY function, event, error, and state variable:

1. **NatSpec tags**: Verify every @notice, @dev, @param, @return, @custom:reverts, @inheritdoc tag against the actual implementation. Check that:
   - @param names match function parameter names exactly
   - @param descriptions match the parameter's actual usage
   - @return descriptions match actual return values and semantics
   - @dev notes accurately describe implementation details, caller lists
   - @custom:reverts tags have matching revert paths in the implementation

2. **DegenerusAdmin.sol-specific checks**:
   - NEW-001: IDegenerusGameAdmin interface @dev mentions "liveness" -- check if fix was applied (change to "VRF + liquidity management")
   - OQ-1: lastVrfProcessedTimestamp non-reset documented as intentional (fba43f2c fix) -- verify fix text is accurate
   - Governance NatSpec: verify threshold decay description (50% -> 5% over 7 days) matches threshold() function
   - VRF swap governance NatSpec: verify process description matches actual governance flow

3. **DegenerusVault.sol-specific checks**:
   - CMT-201: _transfer @dev plural "checks" changed to singular (fba43f2c fix) -- verify fix text is accurate
   - Fund routing NatSpec: verify @dev descriptions of ETH/stETH/BURNIE flows match actual implementation
   - KNOWN-ISSUES.md cross-reference: _sendToVault hard-revert is documented as intentional design -- do not flag comments about this behavior

4. **DegenerusGameStorage.sol-specific checks**:
   - CMT-003: Slot 0 tail vs Slot 1 boundary comment (fba43f2c fix) -- verify the slot boundary comment is now accurate
   - Storage layout diagram: verify the header diagram matches actual variable declarations, types, and slot positions
   - Bit packing comments: verify that every packed struct/variable has accurate size annotations
   - Section headers: verify they accurately describe the variable groups they precede

5. **fba43f2c fix commit verification**: For all three contracts, verify the NEW text introduced by the fix commit is accurate. Check:
   - DegenerusAdmin.sol: OQ-1 fix text
   - DegenerusVault.sol: CMT-201 fix text
   - DegenerusGameStorage.sol: CMT-003 fix text

6. **Deduplication**: Before reporting any finding, check against v3.1, v3.2, and v3.4 consolidated findings. Skip if already fixed. Only report if issue persists or fix is wrong.

**Output format**: Create `audit/v3.5-comment-findings-54-04-core-storage.md` with:
- Header: title, date, scope (3 contracts, ~3,453 lines), pass (v3.5 comment correctness), mode (flag-only)
- Summary table: contract | new findings | prior findings verified | total
- Per-contract sections with:
  - Review scope: line count, NatSpec tag count, function count
  - Prior fix verification status for v3.2 findings in this contract
  - New findings in format: `| CMT-V35-NNN | LOW or INFO | Contract.sol | line | Summary | Recommendation |`
  - Fresh scan notes
- If zero findings for a contract, state "No new findings. All NatSpec verified accurate."

**Severity guidelines**:
- LOW: A C4A warden would likely file this (wrong @param name, stale @custom:reverts, factually incorrect @dev, wrong slot boundary)
- INFO: Cosmetic improvement only (could be more precise, minor wording, missing optional tag)
  </action>
  <verify>
    <automated>test -f audit/v3.5-comment-findings-54-04-core-storage.md && grep -c "CMT-V35-" audit/v3.5-comment-findings-54-04-core-storage.md; grep -q "DegenerusAdmin" audit/v3.5-comment-findings-54-04-core-storage.md && grep -q "DegenerusVault" audit/v3.5-comment-findings-54-04-core-storage.md && grep -q "GameStorage" audit/v3.5-comment-findings-54-04-core-storage.md && echo "All 3 contracts covered"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-comment-findings-54-04-core-storage.md exists and is non-empty
    - File contains "DegenerusAdmin.sol" section with review scope stated
    - File contains "DegenerusVault.sol" section with review scope stated
    - File contains "DegenerusGameStorage.sol" section with review scope stated
    - Every finding row contains: CMT-V35-NNN | severity | Contract.sol | line number | summary | recommendation
    - File contains verification status for NEW-001 (DegenerusAdmin "liveness" in interface @dev)
    - File contains verification status for OQ-1 (DegenerusAdmin lastVrfProcessedTimestamp)
    - File contains verification status for CMT-201 (DegenerusVault _transfer singular)
    - File contains verification status for CMT-003 (GameStorage slot boundary)
    - No finding ID duplicates an existing v3.1/v3.2/v3.4 finding that was already fixed
  </acceptance_criteria>
  <done>All NatSpec tags and inline comments in DegenerusAdmin.sol, DegenerusVault.sol, and DegenerusGameStorage.sol verified. Findings documented with line references. Prior v3.2 findings in these contracts re-verified. Storage layout diagram verified against actual variable declarations.</done>
</task>

</tasks>

<verification>
- audit/v3.5-comment-findings-54-04-core-storage.md exists with findings table
- All 3 contracts have dedicated review sections
- Prior findings (NEW-001, OQ-1, CMT-201, CMT-003) have verification status
- Storage layout diagram verified
- No duplicate findings against v3.1/v3.2/v3.4 consolidated reports
</verification>

<success_criteria>
Every @param, @return, @dev, @notice, @custom:reverts tag and every inline comment in DegenerusAdmin.sol, DegenerusVault.sol, and DegenerusGameStorage.sol has been checked against the implementation. Storage layout comments verified against actual variable declarations. All findings documented. Prior "accept as known" findings from v3.2 re-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/54-comment-correctness/54-04-SUMMARY.md`
</output>
