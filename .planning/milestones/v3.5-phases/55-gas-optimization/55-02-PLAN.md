---
phase: 55-gas-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.5-gas-storage-liveness-extended.md
autonomous: true
requirements:
  - GAS-01
  - GAS-04
must_haves:
  truths:
    - "Every DegenerusGameStorage variable in Slots 25-109 has a liveness verdict (ALIVE or DEAD) with read+write evidence"
    - "Boon mappings, deity boon mappings, decimator entries, VRF fields, lootbox RNG fields all analyzed"
    - "Liveness analysis searched ALL 13 inheriting contracts for each variable"
  artifacts:
    - path: "audit/v3.5-gas-storage-liveness-extended.md"
      provides: "Storage liveness verdicts for DegenerusGameStorage Slots 25-109"
      contains: "## Slot 2"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "audit/v3.5-gas-storage-liveness-extended.md"
      via: "variable declaration -> liveness verdict"
      pattern: "ALIVE|DEAD"
---

<objective>
Analyze liveness of all DegenerusGameStorage variables in Slots 25 through 109 (boon mappings, deity boons, VRF config, lootbox RNG, degenerette bets, decimator entries, century bonus, yield accumulator, terminal decimator).

Purpose: GAS-01 requires confirming every storage variable has read+write in reachable code paths. This plan covers the remaining ~60+ variables (Slots 25-109) which contain boon state, VRF infrastructure, lootbox RNG, and late-addition features.

Output: `audit/v3.5-gas-storage-liveness-extended.md` with per-variable liveness verdicts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-gas-optimization/55-RESEARCH.md

@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liveness analysis for Slots 25-61 (Boon mappings, Deity boons, Last jackpot info, Operator approvals, Level pools, Affiliate, Deity pass, Earlybird, VRF config)</name>
  <files>audit/v3.5-gas-storage-liveness-extended.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Create `audit/v3.5-gas-storage-liveness-extended.md` with header:

```markdown
# v3.5 Gas Optimization: Storage Variable Liveness — DegenerusGameStorage Slots 25-109

**Date:** 2026-03-21
**Phase:** 55 (Gas Optimization)
**Scope:** DegenerusGameStorage.sol Slots 25-109 — boons, VRF, lootbox RNG, decimator, terminal state
**Method:** forge inspect storageLayout + rg reference tracing across ALL 13 inheriting contracts
**Contracts searched:** DegenerusGame.sol, DegenerusGameAdvanceModule.sol, DegenerusGameBoonModule.sol, DegenerusGameDecimatorModule.sol, DegenerusGameDegeneretteModule.sol, DegenerusGameEndgameModule.sol, DegenerusGameGameOverModule.sol, DegenerusGameJackpotModule.sol, DegenerusGameLootboxModule.sol, DegenerusGameMintModule.sol, DegenerusGameMintStreakUtils.sol, DegenerusGamePayoutUtils.sol, DegenerusGameWhaleModule.sol

---
```

For EACH variable, run `rg '<varName>' contracts/ --type sol` to find ALL read and write sites. Record:
1. Variable name and type
2. Slot (from NatSpec or `forge inspect DegenerusGameStorage storageLayout`)
3. Write sites: file:line for every assignment
4. Read sites: file:line for every read
5. Verdict: ALIVE or DEAD

**Variables to analyze (Slots 25-61):**

Boon mappings (Slots ~26-36):
- `coinflipBoonDay` (mapping(address => uint48))
- `lootboxBoon5Active` (mapping(address => bool))
- `lootboxBoon5Day` (mapping(address => uint48))
- `lootboxBoon15Active` (mapping(address => bool))
- `lootboxBoon15Day` (mapping(address => uint48))
- `lootboxBoon25Active` (mapping(address => bool))
- `lootboxBoon25Day` (mapping(address => uint48))
- `whaleBoonDay` (mapping(address => uint48))
- `whaleBoonDiscountBps` (mapping(address => uint16))
- `activityBoonPending` (mapping(address => uint24))
- `activityBoonDay` (mapping(address => uint48))
- `autoRebuyState` (mapping(address => AutoRebuyState))
- `decimatorAutoRebuyDisabled` (mapping(address => bool))
- `purchaseBoostBps` (mapping(address => uint16))
- `purchaseBoostDay` (mapping(address => uint48))
- `decimatorBoostBps` (mapping(address => uint16))
- `coinflipBoonBps` (mapping(address => uint16))

Last jackpot info (Slot ~43, 3 vars):
- `lastDailyJackpotWinningTraits` (uint32)
- `lastDailyJackpotLevel` (uint24)
- `lastDailyJackpotDay` (uint48)

Lootbox extended:
- `lootboxEthBase` (mapping(uint48 => mapping(address => uint256)))
- `operatorApprovals` (mapping(address => mapping(address => bool)))

Level pools and allocation:
- `levelPrizePool` (mapping(uint24 => uint256))
- `affiliateDgnrsClaimedBy` (mapping)
- `levelDgnrsAllocation` (mapping(uint24 => uint256))
- `levelDgnrsClaimed` (mapping(uint24 => uint256))

Deity pass:
- `deityPassCount` (mapping(address => uint16))
- `deityPassPurchasedCount` (mapping(address => uint16))
- `deityPassPaidTotal` (mapping(address => uint256))
- `deityPassOwners` (address[])
- `deityPassSymbol` (mapping(address => uint8))
- `deityBySymbol` (mapping(uint8 => address))

Earlybird:
- `earlybirdDgnrsPoolStart` (uint256)
- `earlybirdEthIn` (uint256)

VRF config (Slot ~58-60):
- `vrfCoordinator` (IVRFCoordinator, address — 20 bytes)
- `vrfKeyHash` (bytes32)
- `vrfSubscriptionId` (uint256)

Lootbox RNG (Slot ~61):
- `lootboxRngIndex` (uint48)

Format each as:
```markdown
### `variableName` (type) — Slot N
**Write:** file.sol:NN (description)
**Read:** file.sol:NN (description)
**Verdict:** ALIVE
```

IMPORTANT: For mappings, check that both read and write exist across all 13 contracts.
IMPORTANT: Constants and immutables must NOT be included.
  </action>
  <verify>
    <automated>grep -c "Verdict:" audit/v3.5-gas-storage-liveness-extended.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-gas-storage-liveness-extended.md exists
    - File contains verdicts for all boon mappings: `coinflipBoonDay`, `lootboxBoon5Active`, `lootboxBoon5Day`, `lootboxBoon15Active`, `lootboxBoon15Day`, `lootboxBoon25Active`, `lootboxBoon25Day`, `whaleBoonDay`, `whaleBoonDiscountBps`, `activityBoonPending`, `activityBoonDay`
    - File contains verdicts for `autoRebuyState`, `decimatorAutoRebuyDisabled`, `purchaseBoostBps`, `purchaseBoostDay`, `decimatorBoostBps`, `coinflipBoonBps`
    - File contains verdicts for `lastDailyJackpotWinningTraits`, `lastDailyJackpotLevel`, `lastDailyJackpotDay`
    - File contains verdicts for `lootboxEthBase`, `operatorApprovals`, `levelPrizePool`, `affiliateDgnrsClaimedBy`, `levelDgnrsAllocation`, `levelDgnrsClaimed`
    - File contains verdicts for all deity pass vars and earlybird vars
    - File contains verdicts for `vrfCoordinator`, `vrfKeyHash`, `vrfSubscriptionId`, `lootboxRngIndex`
    - grep "Verdict:" returns at least 40 matches
  </acceptance_criteria>
  <done>All DegenerusGameStorage variables in Slots 25-61 have liveness verdicts with file:line evidence</done>
</task>

<task type="auto">
  <name>Task 2: Liveness analysis for Slots 62-109 (Lootbox RNG extended, deity boon days, degenerette, decimator, lazy pass, daily hero, distress lootbox, yield, century bonus, terminal decimator)</name>
  <files>audit/v3.5-gas-storage-liveness-extended.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.5-gas-storage-liveness-extended.md
  </read_first>
  <action>
Append to `audit/v3.5-gas-storage-liveness-extended.md` with liveness analysis for Slots 62-109.

**Variables to analyze:**

Lootbox RNG extended:
- `lootboxRngPendingEth` (uint256)
- `lootboxRngThreshold` (uint256)
- `lootboxRngMinLinkBalance` (uint256)
- `lootboxRngWordByIndex` (mapping(uint48 => uint256))
- `lootboxRngRequestIndexById` (mapping(uint256 => uint48))
- `lootboxDay` (mapping(uint48 => mapping(address => uint48)))
- `lootboxBaseLevelPacked` (mapping)
- `lootboxEvScorePacked` (mapping(uint48 => mapping(address => uint16)))
- `lootboxIndexQueue` (mapping(address => uint48[]))
- `lootboxBurnie` (mapping(uint48 => mapping(address => uint256)))
- `lootboxRngPendingBurnie` (uint256)
- `lastLootboxRngWord` (uint256)
- `midDayTicketRngPending` (bool) — Slot 74

Deity boon day mappings (Slots ~75-85):
- `deityBoonDay` (mapping(address => uint48))
- `deityBoonUsedMask` (mapping(address => uint8))
- `deityBoonRecipientDay` (mapping(address => uint48))
- `deityCoinflipBoonDay` (mapping(address => uint48))
- `deityLootboxBoon5Day` (mapping(address => uint48))
- `deityLootboxBoon15Day` (mapping(address => uint48))
- `deityLootboxBoon25Day` (mapping(address => uint48))
- `deityPurchaseBoostDay` (mapping(address => uint48))
- `deityDecimatorBoostDay` (mapping(address => uint48))
- `deityWhaleBoonDay` (mapping(address => uint48))
- `deityActivityBoonDay` (mapping(address => uint48))

Degenerette:
- `degeneretteBets` (mapping(address => mapping(uint64 => uint256)))
- `degeneretteBetNonce` (mapping(address => uint64))

Deity pass boon:
- `deityPassBoonTier` (mapping(address => uint8))
- `deityPassBoonDay` (mapping(address => uint48))
- `deityDeityPassBoonDay` (mapping(address => uint48))

Lootbox EV benefit:
- `lootboxEvBenefitUsedByLevel` (mapping)

Decimator entries:
- `decBurn` (mapping(uint24 => mapping(address => DecEntry)))
- `decBucketBurnTotal` (mapping(uint24 => uint256[13][13]))
- `decClaimRounds` (mapping(uint24 => DecClaimRound))
- `decBucketOffsetPacked` (mapping(uint24 => uint64))

Lazy pass boon:
- `lazyPassBoonDay` (mapping(address => uint48))
- `lazyPassBoonDiscountBps` (mapping(address => uint16))
- `deityLazyPassBoonDay` (mapping(address => uint48))

Daily hero / degenerette wagers:
- `dailyHeroWagers` (mapping(uint48 => uint256[4]))
- `playerDegeneretteEthWagered` (mapping)
- `topDegeneretteByLevel` (mapping(uint24 => uint256))

Distress lootbox:
- `lootboxDistressEth` (mapping(uint48 => mapping(address => uint256)))

Yield and century bonus:
- `yieldAccumulator` (uint256) — Slot 103
- `centuryBonusLevel` (uint24) — Slot 104
- `centuryBonusUsed` (mapping(address => uint256))

Late additions:
- `lastVrfProcessedTimestamp` (uint48) — Slot 106
- `terminalDecEntries` (mapping(address => TerminalDecEntry))
- `terminalDecBucketBurnTotal` (mapping(bytes32 => uint256))
- `lastTerminalDecClaimRound` (TerminalDecClaimRound) — Slot 109

At the end, add summary table covering ALL Slots 25-109:

```markdown
## Summary — Slots 25-109

| Slot | Variable | Type | Verdict |
|------|----------|------|---------|
| ... | ... | ... | ... |

**Total variables analyzed:** NN
**ALIVE:** NN
**DEAD:** NN
```
  </action>
  <verify>
    <automated>grep -c "Verdict:" audit/v3.5-gas-storage-liveness-extended.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains verdicts for all lootbox RNG vars: `lootboxRngPendingEth`, `lootboxRngThreshold`, `lootboxRngMinLinkBalance`, `lootboxRngWordByIndex`, `lootboxRngRequestIndexById`, `lootboxDay`, `lootboxBaseLevelPacked`, `lootboxEvScorePacked`, `lootboxIndexQueue`, `lootboxBurnie`, `lootboxRngPendingBurnie`, `lastLootboxRngWord`, `midDayTicketRngPending`
    - File contains verdicts for all 11 deity boon day mappings
    - File contains verdicts for `degeneretteBets`, `degeneretteBetNonce`
    - File contains verdicts for all decimator entries: `decBurn`, `decBucketBurnTotal`, `decClaimRounds`, `decBucketOffsetPacked`
    - File contains verdicts for `yieldAccumulator`, `centuryBonusLevel`, `centuryBonusUsed`
    - File contains verdicts for `lastVrfProcessedTimestamp`, `terminalDecEntries`, `terminalDecBucketBurnTotal`, `lastTerminalDecClaimRound`
    - File contains "## Summary" section with markdown table
    - grep "Verdict:" returns at least 90 matches (40+ from Task 1 + 50+ from Task 2)
  </acceptance_criteria>
  <done>All DegenerusGameStorage variables in Slots 25-109 have liveness verdicts. Summary table shows total ALIVE vs DEAD counts for extended range.</done>
</task>

</tasks>

<verification>
- `audit/v3.5-gas-storage-liveness-extended.md` exists and contains 90+ "Verdict:" lines
- Every variable in Slots 25-109 of DegenerusGameStorage has been analyzed
- All 13 inheriting contracts were searched for each variable
- Summary table at end shows total counts
</verification>

<success_criteria>
- Every storage variable declared in DegenerusGameStorage.sol Slots 25-109 has a liveness verdict
- Each verdict backed by specific file:line references for both reads and writes
- Any DEAD variable flagged as a GAS finding
- Analysis covered all 13 inheriting contracts
</success_criteria>

<output>
After completion, create `.planning/phases/55-gas-optimization/55-02-SUMMARY.md`
</output>
