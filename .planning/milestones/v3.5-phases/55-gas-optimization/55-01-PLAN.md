---
phase: 55-gas-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.5-gas-storage-liveness-core.md
autonomous: true
requirements:
  - GAS-01
  - GAS-04
must_haves:
  truths:
    - "Every DegenerusGameStorage scalar variable in Slots 0-24 has a liveness verdict (ALIVE or DEAD) with read+write evidence"
    - "Every DegenerusGameStorage mapping variable in Slots 3-24 has a liveness verdict with read+write evidence"
    - "Liveness analysis searched ALL 13 inheriting contracts (DegenerusGame + 12 modules) for each variable"
  artifacts:
    - path: "audit/v3.5-gas-storage-liveness-core.md"
      provides: "Storage liveness verdicts for DegenerusGameStorage Slots 0-24"
      contains: "## Slot 0"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "audit/v3.5-gas-storage-liveness-core.md"
      via: "variable declaration -> liveness verdict"
      pattern: "ALIVE|DEAD"
---

<objective>
Analyze liveness of all DegenerusGameStorage variables in Slots 0 through 24 (core game mechanics: timing/FSM, price, prize pools, RNG, tickets, daily jackpot, lootbox basics, gameover flags).

Purpose: GAS-01 requires confirming every storage variable has read+write in reachable code paths. This plan covers the first ~45 variables (Slots 0-24) which contain the core game loop state.

Output: `audit/v3.5-gas-storage-liveness-core.md` with per-variable liveness verdicts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-gas-optimization/55-RESEARCH.md

@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liveness analysis for DegenerusGameStorage Slots 0-12 (Timing, FSM, Price, Prize Pools, RNG, Flip Reversals, Daily Budgets, Claimable)</name>
  <files>audit/v3.5-gas-storage-liveness-core.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Create `audit/v3.5-gas-storage-liveness-core.md` with header:

```markdown
# v3.5 Gas Optimization: Storage Variable Liveness — DegenerusGameStorage Slots 0-24

**Date:** 2026-03-21
**Phase:** 55 (Gas Optimization)
**Scope:** DegenerusGameStorage.sol Slots 0-24 — core game state
**Method:** forge inspect storageLayout + rg reference tracing across ALL 13 inheriting contracts
**Contracts searched:** DegenerusGame.sol, DegenerusGameAdvanceModule.sol, DegenerusGameBoonModule.sol, DegenerusGameDecimatorModule.sol, DegenerusGameDegeneretteModule.sol, DegenerusGameEndgameModule.sol, DegenerusGameGameOverModule.sol, DegenerusGameJackpotModule.sol, DegenerusGameLootboxModule.sol, DegenerusGameMintModule.sol, DegenerusGameMintStreakUtils.sol, DegenerusGamePayoutUtils.sol, DegenerusGameWhaleModule.sol

---
```

For EACH variable below, run `rg '<varName>' contracts/ --type sol` to find ALL read and write sites across ALL 13 inheriting contracts plus DegenerusGameStorage.sol itself. Then record:

1. **Variable name** and type
2. **Slot/offset** (from the NatSpec in DegenerusGameStorage.sol; verify with `forge inspect DegenerusGameStorage storageLayout` if uncertain)
3. **Write sites**: every file:line where the variable is assigned (=, +=, -=, delete)
4. **Read sites**: every file:line where the variable is read (conditions, computations, returns, function args)
5. **Verdict**: ALIVE (has both read and write in reachable code) or DEAD

**Variables to analyze (Slots 0-12):**

Slot 0 (15 vars, 32/32 bytes):
- `levelStartTime` (uint48)
- `dailyIdx` (uint48)
- `rngRequestTime` (uint48)
- `level` (uint24)
- `jackpotPhaseFlag` (bool)
- `jackpotCounter` (uint8)
- `earlyBurnPercent` (uint8)
- `poolConsolidationDone` (bool)
- `lastPurchaseDay` (bool)
- `decWindowOpen` (bool)
- `rngLockedFlag` (bool)
- `phaseTransitionActive` (bool)
- `gameOver` (bool)
- `dailyJackpotCoinTicketsPending` (bool)
- `dailyEthBucketCursor` (uint8)

Slot 1 (7 vars, 27/32 bytes):
- `dailyEthPhase` (uint8)
- `compressedJackpotFlag` (uint8)
- `purchaseStartDay` (uint48)
- `price` (uint128)
- `ticketWriteSlot` (uint8)
- `ticketsFullyProcessed` (bool)
- `prizePoolFrozen` (bool)

Slot 2: `currentPrizePool` (uint256)
Slot 3: `prizePoolsPacked` (uint256)
Slot 4: `rngWordCurrent` (uint256)
Slot 5: `vrfRequestId` (uint256)
Slot 6: `totalFlipReversals` (uint256)
Slot 7: `dailyTicketBudgetsPacked` (uint256)
Slot 8: `dailyEthPoolBudget` (uint256)
Slot 9: `claimableWinnings` (mapping)
Slot 10: `claimablePool` (uint256)
Slot 11: `traitBurnTicket` (mapping)
Slot 12: `mintPacked_` (mapping)

Format each variable as:

```markdown
### `variableName` (type) — Slot N
**Write:** file.sol:NN (description), file2.sol:NN (description)
**Read:** file.sol:NN (description), file2.sol:NN (description)
**Verdict:** ALIVE
```

If a variable has NO write sites or NO read sites, verdict is DEAD and flag as a GAS finding.

IMPORTANT: For mappings, check that both read and write exist. The mapping declaration itself does not count as a write.
IMPORTANT: Constants (declared with `constant` or `immutable`) must NOT be included — they occupy no storage slots.
IMPORTANT: Internal functions defined in DegenerusGameStorage.sol are NOT storage variables — skip them.
  </action>
  <verify>
    <automated>grep -c "Verdict:" audit/v3.5-gas-storage-liveness-core.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-gas-storage-liveness-core.md exists
    - File contains "## Slot 0" section
    - File contains verdicts for `levelStartTime`, `dailyIdx`, `rngRequestTime`, `level`, `jackpotPhaseFlag`, `jackpotCounter`, `earlyBurnPercent`, `poolConsolidationDone`, `lastPurchaseDay`, `decWindowOpen`, `rngLockedFlag`, `phaseTransitionActive`, `gameOver`, `dailyJackpotCoinTicketsPending`, `dailyEthBucketCursor`
    - File contains verdicts for `dailyEthPhase`, `compressedJackpotFlag`, `purchaseStartDay`, `price`, `ticketWriteSlot`, `ticketsFullyProcessed`, `prizePoolFrozen`
    - File contains verdicts for `currentPrizePool`, `prizePoolsPacked`, `rngWordCurrent`, `vrfRequestId`, `totalFlipReversals`, `dailyTicketBudgetsPacked`, `dailyEthPoolBudget`, `claimableWinnings`, `claimablePool`, `traitBurnTicket`, `mintPacked_`
    - Every verdict line contains "ALIVE" or "DEAD"
    - Every variable entry has at least one "Write:" line and one "Read:" line (or explicit "NONE" if dead)
    - grep "Verdict:" audit/v3.5-gas-storage-liveness-core.md returns at least 29 matches (15 slot0 + 7 slot1 + 7 slots2-12)
  </acceptance_criteria>
  <done>All DegenerusGameStorage variables in Slots 0-12 have liveness verdicts with file:line evidence for reads and writes across all 13 inheriting contracts</done>
</task>

<task type="auto">
  <name>Task 2: Liveness analysis for DegenerusGameStorage Slots 13-24 (RNG by day, pending pools, ticket queue, daily jackpot cursors, lootbox core, gameover flags, whale claims)</name>
  <files>audit/v3.5-gas-storage-liveness-core.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.5-gas-storage-liveness-core.md
  </read_first>
  <action>
Append to `audit/v3.5-gas-storage-liveness-core.md` with liveness analysis for Slots 13-24.

Use the same method: `rg '<varName>' contracts/ --type sol` for each variable, searching ALL 13 inheriting contracts.

**Variables to analyze (Slots 13-24):**

Slot 13: `rngWordByDay` (mapping(uint48 => uint256))
Slot 14: `prizePoolPendingPacked` (uint256)
Slot 15: `ticketQueue` (mapping(uint24 => address[]))
Slot 16: `ticketsOwedPacked` (mapping(uint24 => mapping(address => uint40)))
Slot 17 (3 vars, 9/32 bytes):
- `ticketCursor` (uint32)
- `ticketLevel` (uint24)
- `dailyEthWinnerCursor` (uint16)

Slot 18: `dailyCarryoverEthPool` (uint256)
Slot 19: `dailyCarryoverWinnerCap` (uint16, 2/32 bytes — 30 bytes wasted)
Slot 20: `lootboxEth` (mapping(uint48 => mapping(address => uint256)))
Slot 21: `lootboxPresaleActive` (bool)
Slot 22: `lootboxEthTotal` (uint256)
Slot 23: `lootboxPresaleMintEth` (uint256)
Slot 24 (3 vars, 8/32 bytes):
- `gameOverTime` (uint48)
- `gameOverFinalJackpotPaid` (bool)
- `finalSwept` (bool)

Also include:
Slot 25 (or wherever it falls): `whalePassClaims` (mapping(address => uint256))

Format identical to Task 1. For each variable record Write sites, Read sites, Verdict.

At the end of the file, add a summary table:

```markdown
## Summary — Slots 0-24

| Slot | Variable | Type | Verdict |
|------|----------|------|---------|
| 0 | levelStartTime | uint48 | ALIVE |
| ... | ... | ... | ... |

**Total variables analyzed:** NN
**ALIVE:** NN
**DEAD:** NN
```
  </action>
  <verify>
    <automated>grep -c "Verdict:" audit/v3.5-gas-storage-liveness-core.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.5-gas-storage-liveness-core.md contains verdicts for `rngWordByDay`, `prizePoolPendingPacked`, `ticketQueue`, `ticketsOwedPacked`, `ticketCursor`, `ticketLevel`, `dailyEthWinnerCursor`
    - File contains verdicts for `dailyCarryoverEthPool`, `dailyCarryoverWinnerCap`, `lootboxEth`, `lootboxPresaleActive`, `lootboxEthTotal`, `lootboxPresaleMintEth`
    - File contains verdicts for `gameOverTime`, `gameOverFinalJackpotPaid`, `finalSwept`, `whalePassClaims`
    - File contains "## Summary" section with a markdown table
    - grep "Verdict:" returns at least 46 matches (29 from Task 1 + 17 from Task 2)
    - Summary table has "Total variables analyzed:" line
  </acceptance_criteria>
  <done>All DegenerusGameStorage variables in Slots 0-24 have liveness verdicts. Summary table shows total ALIVE vs DEAD counts.</done>
</task>

</tasks>

<verification>
- `audit/v3.5-gas-storage-liveness-core.md` exists and contains at least 46 "Verdict:" lines
- Every variable in Slots 0-24 of DegenerusGameStorage has been analyzed
- All 13 inheriting contracts were searched for each variable (documented in file header)
- Summary table at end shows total counts
</verification>

<success_criteria>
- Every storage variable declared in DegenerusGameStorage.sol Slots 0-24 has a liveness verdict
- Each verdict backed by specific file:line references for both reads and writes
- Any DEAD variable flagged as a GAS finding
- Analysis covered all 13 inheriting contracts (not just DegenerusGame.sol)
</success_criteria>

<output>
After completion, create `.planning/phases/55-gas-optimization/55-01-SUMMARY.md`
</output>
