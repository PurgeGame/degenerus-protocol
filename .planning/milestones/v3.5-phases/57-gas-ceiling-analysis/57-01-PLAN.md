---
phase: 57-gas-ceiling-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md
autonomous: true
requirements:
  - CEIL-01
  - CEIL-02

must_haves:
  truths:
    - "Every one of the 12 advanceGame stages has a worst-case gas estimate with per-operation breakdown"
    - "The 5 heavy-hitter stages (JACKPOT_PHASE_ENDED, ENTERED_JACKPOT, JACKPOT_DAILY_STARTED, PURCHASE_DAILY, GAMEOVER) have detailed loop-by-loop gas accounting"
    - "Maximum jackpot payout count per path is computed from the 14M ceiling and per-winner gas cost"
    - "The deity pass loop in GAMEOVER is analyzed for unbounded growth with an economic bound estimate"
    - "Cold storage preamble cost (~15-20 SLOADs at 2100 gas each) is accounted for in every path"
  artifacts:
    - path: ".planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md"
      provides: "Stage-by-stage worst-case gas profiling for all 12 advanceGame paths"
      contains: "CEIL-01"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: ".planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md"
      via: "Manual gas analysis of each stage dispatch"
      pattern: "STAGE_"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: ".planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md"
      via: "Per-winner gas cost breakdown for processTicketBatch and payDailyJackpot"
      pattern: "WRITES_BUDGET_SAFE"
---

<objective>
Profile worst-case gas consumption for every advanceGame() code path (all 12 stages) and compute maximum jackpot payout counts that fit under the 14M gas ceiling.

Purpose: Prove that no single advanceGame() call can exceed the 14M block gas budget under worst-case conditions, or identify which paths are at risk. This is the highest-risk analysis in the phase because advanceGame spans multiple delegatecall modules with loops bounded by different constants.

Output: `57-01-advancegame-gas-analysis.md` containing per-stage gas breakdowns and maximum payout computations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-gas-ceiling-analysis/57-RESEARCH.md

@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameEndgameModule.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGamePayoutUtils.sol
@contracts/modules/DegenerusGameDecimatorModule.sol
@contracts/DegenerusGame.sol
@contracts/DegenerusJackpots.sol
@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile the 5 heavy-hitter advanceGame stages (CEIL-01 partial, CEIL-02)</name>
  <files>.planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (full file -- 1453 lines; the do-while dispatch starts at line ~126; stage constants at top)
    - contracts/modules/DegenerusGameJackpotModule.sol (processTicketBatch at ~line 1910; payDailyJackpot at ~line 1420; payDailyCoinJackpot; runEarlyBirdLootboxJackpot; _addClaimableEth in PayoutUtils)
    - contracts/modules/DegenerusGameEndgameModule.sol (runRewardJackpots, _endPhase)
    - contracts/modules/DegenerusGameGameOverModule.sol (handleGameOverDrain -- deity pass loop at ~line 82; handleFinalSweep)
    - contracts/modules/DegenerusGamePayoutUtils.sol (_addClaimableEth -- the hot inner loop function)
    - contracts/DegenerusJackpots.sol (runBafJackpot -- external call, up to 107 winners)
    - contracts/modules/DegenerusGameDecimatorModule.sol (decimator logic called from endgame paths)
    - .planning/phases/57-gas-ceiling-analysis/57-RESEARCH.md (per-winner gas cost estimates, key constants table, pitfalls)
  </read_first>
  <action>
Create the analysis document `57-01-advancegame-gas-analysis.md` with a header containing phase/plan/date metadata and a methodology note explaining:
- Gas ceiling target: 14,000,000 (conservative, Ethereum mainnet is 30M)
- Compiler: via_ir=true, optimizer_runs=2 (optimizes for code size, not runtime -- actual gas may be higher than typical)
- Each advanceGame() call executes exactly ONE stage via do-while(false) dispatch
- Cold storage preamble: the AdvanceModule reads ~15-20 storage slots at start (level, jackpotPhaseFlag, lastPurchaseDay, rngLockedFlag, dailyIdx, etc.) = ~31,500-42,000 gas in cold SLOADs
- Delegatecall overhead: ~2,600 gas per module hop (cold), ~100 (warm)
- Base tx cost: 21,000 gas
- Advance bounty creditFlip external call: ~5,000 gas
- Event emission (Advance event): ~1,500 gas

Then produce detailed gas breakdowns for these 5 heavy-hitter stages, in priority order:

**Stage 10: JACKPOT_PHASE_ENDED** (highest risk)
- Call chain: payDailyJackpotCoinAndTickets -> if counter >= 5: _awardFinalDayDgnrsReward + _rewardTopAffiliate -> _runRewardJackpots -> delegatecall EndgameModule.runRewardJackpots() -> _endPhase
- runRewardJackpots calls: jackpots.runBafJackpot() (external, up to 107 winners in BAF), decimator execution, coin+ticket distribution
- Profile: BAF jackpot (107 winners x per-winner cost), decimator module, coinJackpot (50 winners), endPhase storage writes
- Per-winner gas for BAF: memory array allocation + winner selection + _addClaimableEth per winner. _addClaimableEth: autoRebuyState SLOAD (2100 cold) + conditional auto-rebuy path (~15,000-25,000) or normal path (~10,000-12,000) + event (~3,500). Worst case (all auto-rebuy): ~25,000/winner. 107 winners = ~2,675,000 gas just for BAF distribution
- Sum: base overhead (~70,000) + cold preamble (~40,000) + payDailyJackpotCoinAndTickets (~200,000 for coin jackpot 50 winners) + runRewardJackpots: BAF 107 (~2,675,000) + decimator (~100,000) + endPhase storage (~50,000) + bounty (~5,000) + event (~1,500)
- Document the total worst-case estimate and headroom = 14M - total

**Stage 7: ENTERED_JACKPOT** (second highest risk)
- Call chain: _consolidatePrizePools (x00 special case), _applyTimeBasedFutureTake (skim pipeline), _drawDownFuturePrizePool, _runEarlyBirdLootboxJackpot (100 winners), flag updates
- Early bird lootbox: 100 iterations of winner selection + _addClaimableEth per winner
- Profile: consolidation storage (~10 SSTOREs), skim pipeline arithmetic (~5,000), earlybird loop (100 x ~25,000 per winner worst case = ~2,500,000), drawdown (~5,000), flag writes (~20,000)
- Sum and document headroom

**Stage 11: JACKPOT_DAILY_STARTED** (third highest risk)
- Call chain: payDailyJackpot(true, ...) -- full daily init + Phase 0 ETH chunk
- Phase 0 chunk: _processDailyEthChunk with unitsBudget=1000. With auto-rebuy (3 units/winner), max ~333 winners per chunk. Per-winner: ~25,000 (auto-rebuy worst case)
- Total winner distribution: 333 x 25,000 = ~8,325,000
- Plus: daily init storage (~50,000), winner array construction (~100,000), bucket resolution
- Sum and document headroom

**Stage 6: PURCHASE_DAILY** (daily during purchase phase)
- Call chain: payDailyJackpot(false) + _payDailyCoinJackpot
- payDailyJackpot(false) distributes ETH to up to DAILY_ETH_MAX_WINNERS=321 winners, but chunked by unitsBudget=1000
- First chunk covers Phase 0 winners. Same chunking as Stage 11
- _payDailyCoinJackpot: up to DAILY_COIN_MAX_WINNERS=50 coin winners
- Profile the combined worst case and document headroom

**Stage 0: GAMEOVER** (unbounded risk)
- Call chain: _handleGameOverPath -> delegatecall GameOverModule.handleGameOverDrain
- handleGameOverDrain: iterates deityPassOwners array (dynamic, potentially unbounded)
- Per deity pass owner: SLOAD owner address + ETH transfer (~9,000-30,000 gas per iteration depending on cold/warm and whether transfer succeeds)
- Economic bound: compute maximum deity pass count from purchase price. If each deity pass costs X ETH and total ETH in the protocol is bounded, compute max owners. If the array has no hard cap in code, this is a FINDING (potential DoS vector)
- Also profiles: handleFinalSweep (one-time finalization), decimator, terminal jackpot
- Document: if unbounded, flag as potential HIGH/MEDIUM finding. If bounded economically, compute max iterations and worst-case gas

For each stage, structure the analysis as:
1. Entry conditions (what game state triggers this stage)
2. Call graph (every function call with contract and line reference)
3. Loop analysis (bound, per-iteration cost, worst-case total)
4. Storage operations (cold SLOADs, cold SSTOREs, count and cost)
5. External calls (target, cold/warm, estimated cost)
6. Events (count, topic count, data size)
7. **Worst-case total** (sum of all above)
8. **Headroom** (14,000,000 - worst_case)
9. **Max payouts** (for stages with winner distribution: max winners at 14M / per-winner cost)

At the end, include a summary table:

| Stage | Name | Worst-Case Gas | Headroom | Max Payouts | Risk Level |
|-------|------|----------------|----------|-------------|------------|

Risk levels: SAFE (>3M headroom), TIGHT (1-3M headroom), AT_RISK (<1M headroom), BREACH (exceeds 14M)
  </action>
  <verify>
    <automated>grep -c "STAGE_\|Stage " .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md</automated>
  </verify>
  <acceptance_criteria>
    - 57-01-advancegame-gas-analysis.md exists in .planning/phases/57-gas-ceiling-analysis/
    - File contains "14,000,000" or "14M" (gas ceiling reference)
    - File contains "JACKPOT_PHASE_ENDED" with a numeric gas estimate
    - File contains "ENTERED_JACKPOT" with a numeric gas estimate
    - File contains "JACKPOT_DAILY_STARTED" with a numeric gas estimate
    - File contains "PURCHASE_DAILY" with a numeric gas estimate
    - File contains "GAMEOVER" with either a numeric gas estimate or "FINDING" flag for unbounded loop
    - File contains "deityPassOwners" (deity pass loop analysis)
    - File contains "WRITES_BUDGET_SAFE" or "550" (processTicketBatch bound)
    - File contains "unitsBudget" or "1000" (daily ETH chunk bound)
    - File contains "107" (BAF jackpot max winners)
    - File contains "Headroom" in at least 5 locations (one per heavy-hitter stage)
    - File contains a summary table with "Risk Level" or "Risk" column
    - grep -c "Worst-Case\|worst.case\|worst case" returns at least 5
  </acceptance_criteria>
  <done>5 heavy-hitter advanceGame stages fully profiled with per-operation gas breakdowns, max payout computations, and headroom calculations. Deity pass loop analyzed for bounded/unbounded status. Summary table with risk levels present.</done>
</task>

<task type="auto">
  <name>Task 2: Profile remaining 7 advanceGame stages and complete CEIL-01 (CEIL-01 complete)</name>
  <files>.planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md</files>
  <read_first>
    - .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md (current state from Task 1 -- the 5 heavy-hitter analyses)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 126-420 for stage dispatch; lines 234-269 for RNG/transition/future ticket paths)
    - contracts/modules/DegenerusGameJackpotModule.sol (processTicketBatch at ~line 1910 for TICKETS_WORKING and FUTURE_TICKETS; _processDailyEthChunk for ETH_RESUME; payDailyJackpotCoinAndTickets for COIN_TICKETS)
    - contracts/storage/DegenerusGameStorage.sol (_queueTickets, _swapTicketSlot for ticket queue operations)
  </read_first>
  <action>
Append analysis sections for the remaining 7 lighter advanceGame stages to the existing document:

**Stage 1: RNG_REQUESTED**
- Call chain: _swapAndFreeze() + rngGate() -> VRF requestRandomWords external call
- Profile: _swapAndFreeze storage writes (~5 SSTOREs = ~25,000-110,000 depending on cold/warm), rngGate logic (~5,000), VRF external call (~50,000)
- These are one-time operations per level transition, no loops
- Estimate worst case and headroom

**Stage 2: TRANSITION_WORKING**
- Call chain: _processPhaseTransition (queues 32 tickets + stETH stake)
- Profile: _queueTickets for 32 tickets (O(1) append = ~5,000 per queue write), steth.submit{value}() external call (~30,000)
- Light path, no winner distribution loops
- Estimate worst case and headroom

**Stage 3: TRANSITION_DONE**
- Same as TRANSITION_WORKING (single call pattern) or flag-update only
- Document if this is a continuation of TRANSITION_WORKING or a separate light operation
- Estimate worst case and headroom

**Stage 4: FUTURE_TICKETS_WORKING**
- Call chain: _prepareFutureTickets -> _processFutureTicketBatch x up to 5 levels
- Each _processFutureTicketBatch calls processTicketBatch (WRITES_BUDGET_SAFE=550)
- Profile: 5 potential processTicketBatch calls, each bounded at 550 writes
- Per write: ~5,000 gas (cold SSTORE). 550 writes = ~2,750,000 per batch. But research notes first batch uses 65% (357 writes) for cold storage headroom
- Document whether all 5 levels can execute in a single call or whether the chunking forces multiple advanceGame calls
- Estimate worst case and headroom

**Stage 5: TICKETS_WORKING**
- Call chain: _runProcessTicketBatch -> delegatecall JackpotModule.processTicketBatch()
- processTicketBatch bounded by WRITES_BUDGET_SAFE=550 (357 on first call)
- Per iteration: 1 SLOAD ticket data + conditional winner check + 1 SSTORE update + potential winner event
- Profile: 550 iterations x per-iteration cost
- Estimate worst case and headroom

**Stage 8: JACKPOT_ETH_RESUME**
- Call chain: payDailyJackpot(true, ...) resuming from cursor
- _processDailyEthChunk with unitsBudget=1000, but starting from saved cursor position
- Same per-winner analysis as Stage 11 but potentially fewer winners (resuming mid-distribution)
- Worst case is same as Stage 11's chunk: 333 auto-rebuy winners at 25,000 each = ~8,325,000
- Estimate worst case and headroom

**Stage 9: JACKPOT_COIN_TICKETS**
- Call chain: payDailyJackpotCoinAndTickets (without the endgame path -- that's Stage 10)
- Coin jackpot: up to DAILY_COIN_MAX_WINNERS=50 winners
- Ticket distribution: bounded storage writes
- No BAF or decimator in this stage (those are Stage 10 only)
- Estimate worst case and headroom

For each stage, use the same structure as Task 1:
1. Entry conditions
2. Call graph with line references
3. Loop analysis
4. Storage operations
5. External calls
6. Events
7. Worst-case total
8. Headroom

Update the summary table to include ALL 12 stages. Verify every stage from the constant list is accounted for:
- STAGE_GAMEOVER (0)
- STAGE_RNG_REQUESTED (1)
- STAGE_TRANSITION_WORKING (2)
- STAGE_TRANSITION_DONE (3)
- STAGE_FUTURE_TICKETS_WORKING (4)
- STAGE_TICKETS_WORKING (5)
- STAGE_PURCHASE_DAILY (6)
- STAGE_ENTERED_JACKPOT (7)
- STAGE_JACKPOT_ETH_RESUME (8)
- STAGE_JACKPOT_COIN_TICKETS (9)
- STAGE_JACKPOT_PHASE_ENDED (10)
- STAGE_JACKPOT_DAILY_STARTED (11)

Add a section at the end titled "CEIL-02: Maximum Jackpot Payouts Under 14M" that consolidates max payout counts across all winner-distributing stages into a single reference table:

| Stage | Distribution Type | Max Winners (Code Bound) | Max Winners (14M Budget) | Binding Constraint |
|-------|-------------------|--------------------------|--------------------------|-------------------|

The "Binding Constraint" column states whether the code constant or the 14M gas ceiling is the actual limiter.
  </action>
  <verify>
    <automated>grep -c "Stage\|STAGE_" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md</automated>
  </verify>
  <acceptance_criteria>
    - 57-01-advancegame-gas-analysis.md contains all 12 stage names: GAMEOVER, RNG_REQUESTED, TRANSITION_WORKING, TRANSITION_DONE, FUTURE_TICKETS_WORKING, TICKETS_WORKING, PURCHASE_DAILY, ENTERED_JACKPOT, JACKPOT_ETH_RESUME, JACKPOT_COIN_TICKETS, JACKPOT_PHASE_ENDED, JACKPOT_DAILY_STARTED
    - File contains "CEIL-01" reference
    - File contains "CEIL-02" section header
    - File contains "Maximum Jackpot Payouts" or equivalent section
    - File contains "Binding Constraint" or equivalent analysis of code-bound vs gas-bound limits
    - File contains "processTicketBatch" (ticket processing analysis)
    - File contains "VRF" or "requestRandomWords" (RNG stage)
    - File contains "stETH" or "steth.submit" (transition stage)
    - Summary table has entries for at least 12 stages (grep for table row markers)
    - grep -c "Headroom" returns at least 12 (one per stage)
    - grep -c "Worst-Case\|worst.case\|worst case" returns at least 12
  </acceptance_criteria>
  <done>All 12 advanceGame stages profiled with worst-case gas, headroom, and max payout calculations. CEIL-01 (all paths profiled) and CEIL-02 (max payouts computed) are fully addressed. Complete summary table and CEIL-02 consolidation table present.</done>
</task>

</tasks>

<verification>
- `grep -c "STAGE_" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md` returns >= 12 (all stages mentioned)
- `grep "CEIL-01\|CEIL-02" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md` shows both requirement IDs
- `grep "14,000,000\|14M" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md` shows gas ceiling referenced
- `grep -c "Headroom" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md` returns >= 12
- `grep "SAFE\|TIGHT\|AT_RISK\|BREACH" .planning/phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md` shows risk classifications
</verification>

<success_criteria>
Every advanceGame() code path (12 stages) has a worst-case gas profile with per-operation breakdowns. Maximum jackpot payouts per distribution path are computed under the 14M ceiling. Risk levels assigned to every stage. Deity pass loop analyzed for bounded/unbounded behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/57-gas-ceiling-analysis/57-01-SUMMARY.md`
</output>
