---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 05
type: execute
wave: 4
depends_on: ["354-01", "354-03", "354-04"]
files_modified:
  - contracts/modules/GameAfkingModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/ContractAddresses.sol
autonomous: false
requirements: [TKT-01, TKT-02, OPEN-01, OPEN-02]
must_haves:
  truths:
    - "The afking ticket per-buy path no longer delegatecalls MintModule.purchaseWith; it uses a custom minimal-write primitive that queues resolution-equivalent ticket entries (placement/trait/quantity)"
    - "The ticket primitive accrues the 10%/20% buyer-bonus per buy into Sub.buyerOwedBurnie (10% of the BURNIE-equiv ticket spend, doubling to 20% for ≥10-ticket buys — live :1655-1659 magnitude, minus the kickback leg)"
    - "The ticket primitive KEEPS the century/x00 quantity bonus at parity (reusing centuryBonusLevel / centuryBonusUsed, no new slot) and applies the same questProgress accrue as the lootbox leg (AGG-04 mode-agnostic)"
    - "The ticket primitive is boons/boost-OFF (does NOT call consumePurchaseBoost) — the LOCKED v55 afking boons-OFF design, NOT a silent drop"
    - "The afking open path stays unchanged in its freeze/parity: live-level resolve, frozen stamp-day seed+word, lastOpenedDay monotone no-double-open, EV-cap exactly-once on [player][currentLevel] — re-verified after the accrual/settle refactor"
    - "buyerOwedBurnie applies the 100M whole-BURNIE saturating clamp before the += (can only under-credit)"
  artifacts:
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the ticket minimal-write primitive + buyerOwedBurnie accrual + century parity; the OPEN path re-verified unchanged"
      contains: "buyerOwedBurnie"
  key_links:
    - from: "GameAfkingModule ticket primitive"
      to: "the ticket queue (resolution-equivalent) + Sub.buyerOwedBurnie + Sub.questProgress + centuryBonusLevel/centuryBonusUsed"
      via: "minimal queue-write + in-slot accrue, no purchaseWith, no consumePurchaseBoost"
      pattern: "buyerOwedBurnie|_queueTickets|centuryBonus"
---

<objective>
Replace the afking ticket-mode per-day `purchaseWith` heavyweight (`GameAfkingModule.sol:713-731`, ~262k) with a custom minimal-write primitive that mirrors the lootbox box-stamp shape: queue resolution-equivalent ticket entries, defer the affiliate/quest to the aggregator, ACCRUE the 10%/20% ticket buyer-bonus into `Sub.buyerOwedBurnie` (closing the v55-style dropped-bonus regression), keep the century/x00 quantity bonus at parity, and stay boons/boost-OFF. Then RE-VERIFY (not modify) the afking open-end (`_openAfkingBox`→`resolveAfkingBox`) stays completely unmanipulable after the accrual/settle refactor. Finally wire the interface/ContractAddresses touches.

Purpose: TKT-01/02 close the dropped-bonus regression at live parity while removing the ~262k heavyweight. OPEN-01/02 confirm the open path (already lean, "no fat to cut") keeps its freeze/parity guarantees — the accumulator fields are disjoint from the open markers. This plan touches the SAME GameAfkingModule file as 354-03, so it is Wave 4 (sequential after the aggregator + the affiliate PULL land).
Output: the ticket primitive + buyerOwedBurnie accrual + century parity, the OPEN re-verification, and the wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md
@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-AFKING-READS-WRITES.md

# Producers this plan consumes:
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-01-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-03-SUMMARY.md

<interfaces>
<!-- Exact anchors the executor edits — DO NOT re-explore. -->

// GameAfkingModule ticket branch (:713-731) — the purchaseWith delegatecall to REPLACE:
//   if (isTicket) { ... GAME_MINT_MODULE.delegatecall(purchaseWith.selector, player, amount, 0,
//                       bytes32("DGNRS"), payKind, ethValue) ... sub.lastOpenedDay = processDay; (:734) }
// The lootbox stamp branch (:735-833) is the SHAPE to mirror (minimal write + in-slot accrue, 354-03).

// MintModule ticket buyer-bonus (:1652-1660) — the magnitude to ACCRUE (minus kickback):
//   uint256 coinCost = (quantity * (PRICE_COIN_UNIT / 4)) / TICKET_SCALE;
//   bonusCredit += coinCost / 10;                                   // flat 10%
//   if (quantity >= 10 * 4 * TICKET_SCALE)                          // ≥10 tickets
//       bonusCredit += (quantity * PRICE_COIN_UNIT) / (40 * TICKET_SCALE);   // +10% → 20% total
// MintModule century/x00 bonus (:1243-1259) — REUSE centuryBonusLevel(:1563)/centuryBonusUsed(:1567):
//   if (ticketCost != 0 && targetLevel % 100 == 0 && cachedScore != 0) { ... bonusQty ... adjustedQty += }
// MintModule._queueTicketsScaled(buyer, targetLevel, adjustedQty, false) (:1263) — the queue primitive
//   the afking ticket-write must mirror (resolution-equivalent placement/trait/quantity).
// MintModule consumePurchaseBoost (applied on the manual leg via _callTicketPurchase, :1544) — NOT applied
//   by the afking primitive (boons/boost-OFF by design, GameAfkingModule:524/742/894).

// OPEN path (RE-VERIFY, do NOT modify the freeze/parity):
//   _openAfkingBox (:888-910): sub.lastOpenedDay = day (:892, effects-first);
//     resolveAfkingBox.selector with uint256(sub.amount) [now milli-ETH → widen+rescale to wei for the box],
//     day, rngWordByDay[day] (:905, frozen stamp-day word), uint16(sub.scorePlus1)-1 (:906).
//   _afkingBoxReady (:918-922): lastOpenedDay < lastAutoBoughtDay && rngWordByDay[lastAutoBoughtDay] != 0.
//   resolveAfkingBox (DegenerusGameLootboxModule.sol:877+): currentLevel = level+1 LIVE (:894);
//     _applyEvMultiplierWithCap(player, currentLevel, ...) RMW (:902), keyed [player][currentLevel].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticket minimal-write primitive + buyerOwedBurnie accrual + century parity (TKT-01/TKT-02)</name>
  <files>contracts/modules/GameAfkingModule.sol, contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    `contracts/modules/GameAfkingModule.sol:713-734` (the ticket purchaseWith branch to replace), `:735-833` (the lootbox stamp+accrue shape to mirror, as landed by 354-03), `:1041` (`_ethToBurnie`). `contracts/modules/DegenerusGameMintModule.sol:1243-1264` (the century bonus + `_queueTicketsScaled` call), `:1652-1660` (the 10%/20% buyer-bonus magnitude). SPEC `## TKT` (353-SPEC.md:193-204) — the minimal-write primitive mirroring the box-stamp; the buyer-bonus ACCRUE into `buyerOwedBurnie` (R2); century KEEP at parity (D-10, reuse `centuryBonusLevel`/`centuryBonusUsed`, no new slot, reuse the already-computed activity score); purchase-BOOST explicitly OFF (XMODEL C3-b — boons/boost-OFF by design, NOT a silent drop). SPEC `## Accumulator Layout` R2 buyer-bonus magnitude (353-SPEC.md:145) + guardrail 2 (353-SPEC.md:313, the 100M clamp before the +=). SPEC `## QST` "The ticket buyer-bonus rides this PUSH settle" (353-SPEC.md:212) — `buyerOwedBurnie` pays the SUB via the quest PUSH (`_settleQuest` from 354-03), not the affiliate PULL.
  </read_first>
  <action>
    Replace the `if (isTicket)` `purchaseWith` delegatecall (`:713-731`) with a custom minimal-write primitive that mirrors the lootbox-branch shape (354-03): (1) QUEUE the ticket entries directly via a `_queueTicketsScaled`-equivalent write (resolution-equivalent placement/trait/quantity — reuse the MintModule queue mechanic; reach `_queueTicketsScaled` or replicate its queue write so the entries are byte-equivalent to `purchaseWith`'s ticket leg). (2) KEEP the century/x00 quantity bonus at PARITY (TKT-02 / D-10): replicate the `targetLevel % 100 == 0` quantity bonus (`MintModule:1243-1259`) BEFORE queuing, REUSING the existing `centuryBonusLevel` (uint24, `:1563`) + `centuryBonusUsed` (mapping, `:1567`) storage — NO new slot — and reusing the per-buy activity score already computed for the box stamp (no extra `_playerActivityScore` call). (3) Do NOT apply `consumePurchaseBoost` (boons/boost-OFF by design — the LOCKED v55 afking design, consistent with `:524`/`:742`/`:894`; this is an intentional documented omission, NOT a bug — TST-356 asserts afking-ticket quantity == manual quantity MINUS the boon-boost, century bonus present). (4) ACCRUE the 10%/20% buyer-bonus per buy into `Sub.buyerOwedBurnie` (TKT-01): compute `coinCost = (quantity × (PRICE_COIN_UNIT/4)) / TICKET_SCALE`; `bonus = coinCost / 10` (flat 10%); if `quantity >= 10 × 4 × TICKET_SCALE` add `(quantity × PRICE_COIN_UNIT) / (40 × TICKET_SCALE)` (→20% on ≥10 tickets) — the live `:1655-1659` magnitude MINUS the affiliate-kickback leg (the affiliate is deferred to the aggregator/PULL); value it in WHOLE BURNIE and apply the 100M saturating clamp BEFORE the write (`sub.buyerOwedBurnie = min(uint256(sub.buyerOwedBurnie) + bonusWhole, 100_000_000)`, guardrail 2 — can only under-credit). (5) Advance the SAME `questProgress` delivered-day counter the lootbox leg advances (AGG-04 mode-agnostic — `buyerOwedBurnie` + `questProgress` settle together via `_settleQuest`, 354-03). (6) Keep `sub.lastOpenedDay = processDay` (`:734`) so the open-leg never treats a ticket sub as box-pending. The SOLVENCY-01 debit at `:708-711` stays byte-unchanged. `buyerOwedBurnie` is ticket-mode-specific — the lootbox leg accrues NONE (it gets the lootbox boon path; do NOT accrue buyerOwedBurnie in the lootbox branch).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The afking ticket path no longer delegatecalls `purchaseWith`: `sed -n '700,760p' contracts/modules/GameAfkingModule.sol | grep -c "purchaseWith.selector"` returns 0.
    - The ticket path accrues `buyerOwedBurnie` and advances `questProgress`: `sed -n '539,851p' contracts/modules/GameAfkingModule.sol | grep -cE "buyerOwedBurnie"` ≥ 1.
    - The century bonus reuses the existing storage (no new slot added): `grep -cE "centuryBonusUsed|centuryBonusLevel" contracts/modules/GameAfkingModule.sol` ≥ 1 AND no new `centuryBonus*` declaration is added to `DegenerusGameStorage.sol` (it already exists).
    - The ticket primitive does NOT call `consumePurchaseBoost`: `sed -n '539,851p' contracts/modules/GameAfkingModule.sol | grep -c "consumePurchaseBoost"` returns 0.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The afking ticket primitive queues resolution-equivalent entries with no purchaseWith, accrues the 10%/20% buyer-bonus at live parity into buyerOwedBurnie (clamped), keeps the century bonus at parity reusing the existing storage, stays boons/boost-OFF, and advances the shared questProgress counter; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the afking open-end stays unmanipulable + wire interfaces/ContractAddresses (OPEN-01/OPEN-02)</name>
  <files>contracts/modules/GameAfkingModule.sol, contracts/ContractAddresses.sol</files>
  <read_first>
    `contracts/modules/GameAfkingModule.sol:888-922` (`_openAfkingBox` + `_afkingBoxReady`), `:938-966` (`_autoOpen`), `:985+` (`mintBurnie`). `contracts/modules/DegenerusGameLootboxModule.sol:877-908` (`resolveAfkingBox` — currentLevel=level+1 LIVE :894, frozen seed/word :904-905, EV-cap RMW :902). SPEC `## OPEN` (353-SPEC.md:248-257) — OPEN-01 the path is already the cheapest viable shared materialization ("no fat to cut"); OPEN-02 re-verify AFTER the refactor: live-level parity, frozen stamp-day seed+word, lastOpenedDay monotone no-double-open, EV-cap exactly-once on `[player][currentLevel]`, no shared-state hazard (the accumulator fields are disjoint from the open markers in the shared slot). SPEC `## Threat Model Re-Attestation` OPEN-02 + RNG-freeze rows (353-SPEC.md:165/167/171). `353-AFKING-READS-WRITES.md` §2 (the OPEN is a re-verification target, NOT an optimization target). `contracts/ContractAddresses.sol` (freely modifiable wiring — NOT part of the held diff).
  </read_first>
  <action>
    OPEN re-verification (OPEN-01/OPEN-02): do NOT touch the open's freeze/parity. CONFIRM (and adjust ONLY if the 354-01 milli-ETH `amount` re-denomination requires it) that `_openAfkingBox` (`:888-910`) still passes the box `amount` correctly to `resolveAfkingBox` — since `amount` is now milli-ETH (354-01), the open must RESCALE the stamped milli-ETH `amount` back to wei before the box seed / EV-cap payout (the box's spend is in wei; the milli-ETH stamp must be widened AND multiplied by the milli-ETH unit). This is the ONE open-path change the refactor forces; everything else stays byte-identical: `lastOpenedDay = day` effects-first (`:892`), `rngWordByDay[day]` frozen stamp-day word (`:905`), `uint16(sub.scorePlus1) - 1` (`:906`), `_afkingBoxReady` gate (`:918-922`), `currentLevel = level + 1` LIVE in `resolveAfkingBox` (`:894`), the single `_applyEvMultiplierWithCap(player, currentLevel, ...)` RMW (`:902`). Re-confirm the accumulator fields (`affiliateBase`/`questProgress`/`buyerOwedBurnie`) are DISJOINT from the open markers (`lastOpenedDay`/`lastAutoBoughtDay`) in the shared Sub slot — different fields, different write paths, no collision (EVM SLOAD-mask-SSTORE per-field). Document the re-verification in the SUMMARY (the open is NOT an optimization target — the C5 gas micro-opts are 355 GAS, NOT here). Wiring: update `contracts/ContractAddresses.sol` and any interface file as needed for the new `claim`/`withdraw`/`claimQuest` cross-contract calls to resolve (e.g. if the affiliate `drainAffiliateBase` accessor or the `claimQuest`/`claim` selectors need a registered address constant) — `ContractAddresses.sol` is FREELY modifiable and NOT part of the USER-held contract diff.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_openAfkingBox` still advances `lastOpenedDay = day` BEFORE the resolve (effects-first): `sed -n '888,910p' contracts/modules/GameAfkingModule.sol | grep -n "sub.lastOpenedDay = day"` precedes the `delegatecall`.
    - `_openAfkingBox` still sources the word from `rngWordByDay[day]` (frozen stamp-day word) — grep present, unchanged.
    - `resolveAfkingBox` still uses `currentLevel = level + 1` LIVE and the single EV-cap RMW (`DegenerusGameLootboxModule.sol` :894/:902 unchanged — grep present).
    - The open's box `amount` is rescaled from milli-ETH to wei (the one forced change): the open path multiplies/rescales `sub.amount` before passing it to `resolveAfkingBox`.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The afking open-end is re-verified unmanipulable (freeze/parity intact, only the milli-ETH→wei rescale forced by 354-01), the accumulator fields are disjoint from the open markers, and the wiring resolves; forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| afking ticket buy → the ticket queue | the minimal-write primitive must produce resolution-equivalent entries (no shortcut that diverges from purchaseWith's placement/trait) |
| afking open → the shared EV-cap budget | the open shares the per-(player,level) EV-cap with the human path; must be exactly-once, no double-draw |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-05-TKT-REGRESSION | Repudiation (silent drop) | the ticket buyer-bonus | mitigate | SPEC TKT-01/02 (353-SPEC.md:197/334): the primitive ACCRUES the 10%/20% buyer-bonus into `buyerOwedBurnie` at live parity (closing the v55-style dropped-bonus regression). **TST-356:** afking-ticket buyer-bonus at live parity (minus kickback). |
| T-354-05-TKT-BOOST | Information Disclosure (asymmetry) | the boons/boost-OFF omission | accept-by-design | SPEC TKT boost-parity (353-SPEC.md:203-204, XMODEL C3-b): boons/boost-OFF is the LOCKED v55 design, NOT a silent drop; TST-356 asserts the boost-OFF baseline with the century bonus present. |
| T-354-05-OPEN02 | Tampering / Elevation (open manipulation) | the afking open after the refactor | mitigate (re-verified) | SPEC OPEN-02 (353-SPEC.md:252-257): live-level parity, frozen stamp-day seed+word, lastOpenedDay monotone, EV-cap exactly-once on `[player][currentLevel]`, accumulator fields disjoint from open markers. **TST-356 OPEN-02 + the two-path coexistence.** |
| T-354-05-SEC02-RNG | Tampering | the open's RNG-freeze after the milli-ETH rescale | mitigate | SPEC RNG-freeze row (353-SPEC.md:165/171): the open keeps the frozen stamp-day word; the milli-ETH→wei rescale is on the `amount` (the spend), NOT the seed/word/day. **TST-356:** the open seed is unchanged. |

No package installs in this plan (no `T-{phase}-SC` row).
</threat_model>

<verification>
- `forge build` clean.
- The afking ticket path: no purchaseWith, accrues buyerOwedBurnie (clamped) at live parity, century bonus at parity reusing existing storage, boons/boost-OFF, advances the shared questProgress.
- The open path: freeze/parity intact; only the milli-ETH→wei rescale forced; accumulator fields disjoint from open markers.
- ContractAddresses.sol wiring resolves (freely modifiable).
</verification>

<success_criteria>
The ticket minimal-write primitive lands at parity (TKT-01/02) — resolution-equivalent entries, the 10%/20% buyer-bonus accrued into buyerOwedBurnie at live parity, century bonus kept, boons/boost-OFF. The open-end is re-verified completely unmanipulable (OPEN-01/02) with only the milli-ETH→wei rescale forced by 354-01.
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-05-SUMMARY.md` when done.
</output>
