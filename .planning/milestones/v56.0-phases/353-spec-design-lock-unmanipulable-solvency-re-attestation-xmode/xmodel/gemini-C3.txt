```
VERDICT: EXPLOITABLE
RATIONALE: Deferring the ticket quest to the batched aggregator introduces an exploitable streak-penalty dodge. The batched "±10 streak per window" model relies on explicit unsubscriptions to penalize users. If an AFK player leaves their `afkingFunding` empty, the daily keeper skips their buy but leaves the sub active; at the window's end, the settle awards the +10 streak regardless. This allows them to maintain a falsely high activity score without spending daily ETH, entirely dodging the streak-reset-to-zero penalty that `_questSyncState` strictly enforces on manual buyers who miss a day. Furthermore, replacing `_callTicketPurchase` with a minimal `_queueTicketsScaled` primitive creates a quantity resolution mismatch by silently dropping the `consumePurchaseBoost` multiplier that the manual leg applies. 
MISMATCH-OR-EXPLOIT: Streak penalty dodge (unfunded AFK subs skip daily buys but still receive the batched +10 streak at settle, bypassing the zero-reset penalty manual buyers suffer) AND resolution mismatch (the minimal-write primitive fails to consume and apply the buyer's purchase boost to their ticket quantity). The century bonus deduplication itself is safe, as both paths synchronously check and update the shared `centuryBonusUsed` state.
```
