MODEL-UNAVAILABLE

Model: codex (`@openai/codex`, gpt-5.5), invoked via the verified read-only template
  codex exec --sandbox read-only --skip-git-repo-check -C /tmp -o <out> -  (prompt on stdin)

Concern: C5 (long-run gas optimization — prompt-C5-long-run-gas.md).

Outcome: the codex CLI HUNG on this large prompt (same pattern as C3/C4). The CLI itself is
healthy (trivial smoke prompt returned `SMOKE_OK` in seconds, exit 0; codex-C1 + codex-C2
completed earlier this session) but consistently hangs on the large C3/C4/C5 prompts. Two
invocation attempts were made for C5, each killed by a hard `timeout`:
  - attempt 1: stdin pipe, `-C /tmp` workdir, 420s timeout → no output
  - attempt 2 (retry): stdin pipe, `-C /tmp -m gpt-5.5`, no-exploration preamble, 420s timeout → no output
No partial model output was ever produced (the -o file is only written at completion). Nothing
was fabricated. Per the plan's robustness clause (retry once, then record MODEL-UNAVAILABLE),
C5 is recorded as MODEL-UNAVAILABLE for codex.

Coverage note: concern C5 is STILL covered by the gemini-C5 artifact (gemini answered C5 —
VERDICT: OPTIMIZATIONS-FOUND, suggesting two safe marginal warm-SLOAD savings [hoist
`_goRead(swept)` out of the per-buy loop; 1-deep memoize `rngWordByDay[lastAutoBoughtDay]`
during OPEN_BATCH] and REJECTING three invariant-weakening "optimizations" [batching the
solvency debit, deferring the EV-cap RMW, using live block data for the seed]). That
disposition is recorded in the 353-SPEC.md XMODEL table.

Probable cause: codex (gpt-5.5) throttling / extended-reasoning timeout on the large
prompts after a burst of successful calls — NOT a contracts or design problem, and NOT a CLI
authentication failure.
