---
phase: 357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 00b
type: execute
wave: 1
depends_on: ["357-00"]
files_modified:
  - test/fuzz/V56SubHardening.t.sol
  - test/REGRESSION-BASELINE-v56.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "After 357-00 (HEAD'), forge test is re-run whole-tree and test/REGRESSION-BASELINE-v56.md is reconciled: the F-356-01 stub turns a reverting affiliate-claim assertion GREEN or is regression-neutral (NON-WIDENING HOLDS or NARROWS); the D-11/D-12 reverts turn some currently-green passless/unfunded-subscribe fixtures RED — these are LEGITIMATE behavior-supersession reds, dropped via vm.skip-with-reason exactly like the 356-07 expanded-mandate drops"
    - "NEW positive proofs are authored (V56SubHardening suite OR folded into V56Sec*): pass-required revert (NoPass) fires for a passless EOA; grounding revert (MustPurchaseToBeginAfking) fires for an unfunded EOA; VAULT/sDGNRS exempt still subscribe; a deity holder bypasses D-11; a pass covering currentLevel passes; the crossing eviction still evicts an outgrown pass; the drainAffiliateBase Game dispatch stub is now reachable from the affiliate path AND still AFFILIATE-only"
    - "The post-reconcile ledger satisfies live − union == ∅ (NON-WIDENING) before the subject re-freezes at HEAD'; the SOLVENCY-01 leg-1 byte-diff anchor is re-confirmed (the ETH/claimablePool debit is byte-unchanged by the 357-00 changes — BURNIE-only / revert-only)"
    - "The audit subject is re-frozen at HEAD' (the 357-00 commit); git diff HEAD' HEAD -- contracts/ is empty after this plan (test-only writes; no further contracts/ mutation)"
  artifacts:
    - path: "test/fuzz/V56SubHardening.t.sol"
      provides: "the D-14 new positive proofs for the D-11/D-12/D-13 hardening + the drainAffiliateBase stub reachability"
      contains: "MustPurchaseToBeginAfking"
    - path: "test/REGRESSION-BASELINE-v56.md"
      provides: "the reconciled NON-WIDENING ledger at HEAD' (F-356-01 narrows; the D-11/D-12 supersession reds vm.skip-dropped + re-proven)"
      contains: "NON-WIDENING"
  key_links:
    - from: "test/fuzz/V56SubHardening.t.sol"
      to: "contracts/modules/GameAfkingModule.sol subscribe gates + contracts/DegenerusGame.sol drainAffiliateBase stub"
      via: "vm.expectRevert(abi.encodeWithSignature(...)) + subscribe + the drainAffiliateBase-via-claim reachability"
      pattern: "NoPass|MustPurchaseToBeginAfking|drainAffiliateBase"
    - from: "test/REGRESSION-BASELINE-v56.md"
      to: "the empirical 453f8073 baseline union BY NAME"
      via: "the live − union == ∅ subset gate re-run at HEAD'"
      pattern: "NON-WIDENING|453f8073"
---

<objective>
The D-14 test reconciliation (the second Wave-1 plan, depends on 357-00): after the 357-00 contract gate commits at HEAD', (1) re-run `forge test` whole-tree at HEAD' and reconcile `test/REGRESSION-BASELINE-v56.md` — the F-356-01 stub can only turn a reverting affiliate-`claim` assertion GREEN or be regression-neutral (NON-WIDENING HOLDS/NARROWS); the D-11/D-12 reverts WILL turn some currently-green passless/unfunded-subscribe fixtures RED, which are LEGITIMATE behavior-supersession reds handled exactly like the 356-07 expanded-mandate drops (`vm.skip`-with-reason DROP); (2) author the NEW positive proofs that the hardened behavior is correct; (3) confirm the post-reconcile ledger still satisfies `live − union == ∅` (NON-WIDENING) and re-freeze the audit subject at HEAD'.

Purpose: 357-00 is NOT a pure "contract gate + re-run" — the D-11/D-12 reverts intentionally supersede some existing fixtures, so the NON-WIDENING gate must be reconciled (drop the superseded reds by NAME + re-prove the new behavior GREEN) before the subject re-freezes at HEAD' and the read-only delta-audit (357-01) ∥ sweep (357-02) run against it. This makes "audited == shipped" true at HEAD'.
Output: `test/fuzz/V56SubHardening.t.sol` (the new positive proofs) + the reconciled `test/REGRESSION-BASELINE-v56.md` at HEAD'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md
@test/REGRESSION-BASELINE-v56.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md

<interfaces>
<!-- HEAD' = the 357-00 commit SHA (read it from 357-00-SUMMARY.md). The audit subject re-freezes at HEAD'. -->
- HEAD' = the 357-00 .sol commit (read the resolved SHA from 357-00-SUMMARY.md). All assertions in this plan are against HEAD'; after this plan, `git diff HEAD' HEAD -- contracts/` MUST be empty (test-only writes).
- The reconciliation precedent (356-07 expanded-mandate drops, commit `f23b010e`): a `vm.skip(true, "reason")` DROP for each fixture that the audited v56 diff intentionally supersedes, each re-proven GREEN by a positive proof. Mirror EXACTLY: the D-11/D-12 reverts will turn passless/unfunded-subscribe fixtures RED — drop them BY NAME with a reason string naming the superseding gate (NoPass / MustPurchaseToBeginAfking), and re-prove the new behavior.
- The F-356-01 effect: the drainAffiliateBase stub can ONLY turn a currently-reverting affiliate-`claim` assertion GREEN or be regression-neutral. NON-WIDENING HOLDS (no new red from the stub) or NARROWS (a red goes green). If any test goes RED from the stub, that is a genuine regression — STOP and reconcile.
- The NON-WIDENING gate (AUTHORITATIVE — test/REGRESSION-BASELINE-v56.md, 449 lines): live v56 624/134/30 == the empirical `453f8073` baseline union BY NAME (`live − union == ∅` AND `union − live == ∅`). The 453f8073 baseline corpus is UNCOMPILABLE (AfKing.sol deleted but DeployProtocol+5 files still deploy it at a load-bearing nonce) → the baseline union was established via the byte-identical-contracts commit `83a6a9ca` (git diff 453f8073 83a6a9ca -- contracts/ EMPTY). After the 357-00 reverts + drops, RE-RUN whole-tree forge test at HEAD' and re-confirm `live − union == ∅` (the post-drop live set minus the 453f8073 union is empty). The SOLVENCY-01 leg-1 byte-diff anchor (GameAfkingModule 709-710 ↔ 663-664 byte-identical to 453f8073) must STILL hold — the 357-00 changes are BURNIE-only (drainAffiliateBase) + revert-only (D-11/D-12), so the ETH/claimablePool debit is byte-unchanged.
- The TEST ANALOGS to fork for the new positive proofs (CONTEXT D-14 / pattern map):
  - `test/fuzz/AfKingSubscription.t.sol` — the `vm.expectRevert(abi.encodeWithSignature("NotApproved()"))` + `game.subscribe(...)` revert-then-honor idiom (`testUnapprovedFundingSourceRefusedThenHonored` ~`:241`) + the crossing-eviction proofs (`testCrossingPassHolderRefreshedNotEvicted` ~`:90`, `testCrossingNoPassEvictedViaTombstone` ~`:125` "MUST NOT revert despite the eviction"). Mirror the revert-signature shape for `NoPass()` / `MustPurchaseToBeginAfking()`.
  - `test/fuzz/V56SecUnmanipulable.t.sol` (extends DeployProtocol) — copy its harness: `_subscribeLootbox(who,q)` (vm.prank + subscribe), `_grantDeityPass(who)` / `_clearDeityPass(who)` (vm.store the deity bit), `_setValidThroughLevel(who,lvl)` + `_setLevel(lvl)` (drive the crossing), `_fundPool(who,amount)` / `_drainAllFunding(who)` (funded-vs-unfunded D-12 proofs), the v56 Sub-slot offset block (VERBATIM v56 offsets — NOT stale v55), `testAffiliateBaseDrainAffiliateOnly` (the `vm.expectRevert()` + `IGameAfkingModule(address(game)).drainAffiliateBase(p)` AFFILIATE-only shape — extend it to prove the NEW Game dispatch stub is reachable from the affiliate path and still AFFILIATE-only).
  - `test/fuzz/helpers/DeployProtocol.sol` exposes `vault` + `sdgnrs` (`:75-76`) → the D-13 "VAULT/sDGNRS exempt still subscribe" proof can `vm.prank(address(vault))` / `vm.prank(address(sdgnrs))` and assert subscribe succeeds (no NoPass/MustPurchase revert).
- The 6 NEW positive proofs to author (CONTEXT D-14): (1) NoPass revert fires for a passless EOA on UPSERT subscribe; (2) MustPurchaseToBeginAfking revert fires for an unfunded EOA on UPSERT subscribe; (3) VAULT + sDGNRS exempt still subscribe (no revert); (4) a deity holder bypasses D-11; (5) a pass covering currentLevel passes D-11; (6) the crossing eviction still evicts an outgrown pass. PLUS the drainAffiliateBase reachability/AFFILIATE-only extension (the F-356-01 positive proof). Fold into a NEW `test/fuzz/V56SubHardening.t.sol` suite (CONTEXT leaves the file choice to the planner — a dedicated suite keeps the hardening proofs grouped and reviewable).
- ZERO contracts/*.sol mutation in this plan — test + ledger writes only. The .sol commit was 357-00.
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new contract code or attack surface — it authors test proofs + reconciles the regression ledger against the 357-00 subject (frozen at HEAD'). The threats are the D-11/D-12/D-13 hardening behaviors being PROVEN (positive proofs), not modified. ZERO contracts/*.sol edits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-00b-MASKDROP | Tampering | the vm.skip drops of superseded reds | mitigate | Each dropped fixture carries a reason string naming the superseding gate (NoPass / MustPurchaseToBeginAfking) AND is re-proven GREEN by a positive proof — a drop is NEVER a silent mask. The post-drop ledger is re-run and `live − union == ∅` re-confirmed (a genuine regression cannot hide behind a drop). Mirrors the 356-07 f23b010e discipline. |
| T-357-00b-WIDEN | Elevation | a 357-00 contract change widening the regression | mitigate | The F-356-01 stub can only NARROW or be neutral; the D-11/D-12 reverts are reconciled by drop+re-prove. The whole-tree forge test re-run at HEAD' + the `live − union == ∅` subset gate is the binding NON-WIDENING check before re-freeze. |
| T-357-00b-SOLVENCY | Tampering | the SOLVENCY-01 leg-1 byte anchor | mitigate | Re-confirm the GameAfkingModule ETH/claimablePool debit two-liner is byte-unchanged by the 357-00 changes (drainAffiliateBase is BURNIE-only; D-11/D-12 are revert-only) — the leg-1 byte-diff anchor in the ledger §7 holds. |
| T-357-00b-RO | Tampering | the frozen subject | accept | Test-only + ledger writes; `git diff HEAD' HEAD -- contracts/` verified empty after this plan. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the D-14 new positive proofs (V56SubHardening suite) — D-11/D-12/D-13 + the drainAffiliateBase stub reachability</name>
  <read_first>
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md (HEAD' — the subject SHA these proofs run against)
    - test/fuzz/V56SecUnmanipulable.t.sol (the harness to copy: _subscribeLootbox / _grantDeityPass / _clearDeityPass / _setValidThroughLevel / _setLevel / _fundPool / _drainAllFunding / the v56 Sub-slot offset block / testAffiliateBaseDrainAffiliateOnly — read the helper region + the offset block + the drain-affiliate-only test)
    - test/fuzz/AfKingSubscription.t.sol (the vm.expectRevert + subscribe revert-then-honor idiom + the crossing-eviction proofs — read testUnapprovedFundingSourceRefusedThenHonored + testCrossingPassHolderRefreshedNotEvicted + testCrossingNoPassEvictedViaTombstone)
    - test/fuzz/helpers/DeployProtocol.sol (the vault + sdgnrs handles ~`:75-76` for the D-13 exempt proof)
    - contracts/modules/GameAfkingModule.sol (the HEAD' gates — grep "NoPass\|MustPurchaseToBeginAfking\|_passHorizonOf\|currentLevel > sub.validThroughLevel" to read the as-built revert conditions the proofs assert against)
    - contracts/DegenerusGame.sol (the HEAD' drainAffiliateBase stub — grep "function drainAffiliateBase")
  </read_first>
  <behavior>
    - Test (D-11 negative): an EOA with no pass and `_passHorizonOf < level` reverts NoPass() on an UPSERT subscribe (dailyQuantity >= 1).
    - Test (D-11 positive): an EOA whose pass horizon covers currentLevel subscribes successfully; a deity holder (deity bit set → horizon type(uint24).max) subscribes successfully.
    - Test (D-12 negative): a passless-but-deity-OR-pass-holding EOA that is UNFUNDED (no bought-today, no funded cover-buy) reverts MustPurchaseToBeginAfking() on UPSERT subscribe.
    - Test (D-12 positive): an EOA that is funded (a cover-buy executes) OR already-bought-today subscribes successfully (no MustPurchase revert).
    - Test (D-13 exempt): vm.prank(address(vault)) and vm.prank(address(sdgnrs)) subscribe(address(this), ...) succeed with no pass + unfunded (no NoPass / no MustPurchaseToBeginAfking).
    - Test (crossing eviction KEPT): a sub with a pass valid at subscribe is evicted (via the :942 crossing path, tombstone, MUST NOT revert) when currentLevel advances past its validThroughLevel.
    - Test (F-356-01 reachability): drainAffiliateBase routed through the affiliate path (or the Game dispatch stub directly with msg.sender == AFFILIATE) succeeds and zeroes affiliateBase; called by a non-affiliate it reverts NotApproved().
  </behavior>
  <files>test/fuzz/V56SubHardening.t.sol</files>
  <action>
    Create test/fuzz/V56SubHardening.t.sol extending DeployProtocol, copying the V56SecUnmanipulable harness (the helpers + the VERBATIM v56 Sub-slot offset block — NOT stale v55 offsets). Author the 6 new positive proofs (D-11 negative+positive incl. deity bypass + pass-covers-currentLevel; D-12 negative+positive funded/bought-today; D-13 VAULT+sDGNRS exempt; crossing-eviction-still-evicts) using the `vm.expectRevert(abi.encodeWithSignature("NoPass()"))` / `abi.encodeWithSignature("MustPurchaseToBeginAfking()")` idiom from AfKingSubscription.t.sol, plus the drainAffiliateBase reachability/AFFILIATE-only proof (extend the testAffiliateBaseDrainAffiliateOnly shape — prove the NEW Game dispatch stub is reachable from the affiliate path and still reverts NotApproved() for a non-affiliate caller). Use vm.prank(address(vault))/vm.prank(address(sdgnrs)) for the D-13 exempt proofs. Run `forge test --match-contract V56SubHardening` and confirm all GREEN. Test-only — ZERO contracts/*.sol edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -f test/fuzz/V56SubHardening.t.sol && grep -q "MustPurchaseToBeginAfking" test/fuzz/V56SubHardening.t.sol && grep -q "NoPass" test/fuzz/V56SubHardening.t.sol && grep -q "drainAffiliateBase" test/fuzz/V56SubHardening.t.sol && forge test --match-contract V56SubHardening 2>&1 | tail -8 | grep -qiE "Suite result: ok|passed" && git diff --quiet HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/V56SubHardening.t.sol exists, extends DeployProtocol, and copies the V56SecUnmanipulable harness with the VERBATIM v56 Sub-slot offsets.
    - The 6 positive proofs (D-11 negative+positive+deity, D-12 negative+positive, D-13 VAULT+sDGNRS exempt, crossing-eviction-still-evicts) + the drainAffiliateBase reachability/AFFILIATE-only proof are present and GREEN.
    - The reverts assert `NoPass()` / `MustPurchaseToBeginAfking()` via abi.encodeWithSignature; the D-13 proofs use vm.prank(vault)/vm.prank(sdgnrs).
    - `forge test --match-contract V56SubHardening` passes; `git diff HEAD -- contracts/` is empty (test-only).
  </acceptance_criteria>
  <done>V56SubHardening.t.sol authored with the 6 hardening positive proofs + the drainAffiliateBase reachability proof, all GREEN against HEAD'; zero contract mutation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-run forge test whole-tree at HEAD', reconcile the NON-WIDENING ledger (drop the D-11/D-12 supersession reds + record the F-356-01 narrowing)</name>
  <read_first>
    - test/REGRESSION-BASELINE-v56.md (the AUTHORITATIVE ledger to reconcile — read the §-header / the live-624/134/30 baseline / the live − union == ∅ subset gate / the 356-07 f23b010e vm.skip-drop precedent / the SOLVENCY-01 leg-1 byte anchor §7)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md (HEAD' SHA)
    - test/fuzz/V56SubHardening.t.sol (the new proofs from Task 1 — the re-prove side of each drop)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md (the D-14 reconciliation rules — drop+re-prove, live − union == ∅ HOLDS/NARROWS)
  </read_first>
  <files>test/REGRESSION-BASELINE-v56.md</files>
  <action>
    Re-run whole-tree `forge test` at HEAD' and reconcile test/REGRESSION-BASELINE-v56.md:
    1. Identify the fixtures the D-11/D-12 reverts turned RED (passless/unfunded-subscribe tests that previously subscribed without a pass or unfunded). For each, apply a `vm.skip(true, "reason")` DROP in its test file (the file owning the now-superseded assertion) with a reason string naming the superseding gate (NoPass / MustPurchaseToBeginAfking) — mirror the 356-07 f23b010e discipline; each drop is re-proven GREEN by the corresponding V56SubHardening positive proof (Task 1). NOTE which test files are touched (the drops are test/*.sol edits, NOT contracts/).
    2. Record the F-356-01 effect: confirm the drainAffiliateBase stub turned a reverting affiliate-claim assertion GREEN (a NARROWING) or was regression-neutral — NEVER a new red. If any test goes RED from the stub, STOP (genuine regression).
    3. Re-confirm `live − union == ∅` (the post-drop live failing-NAME set minus the empirical 453f8073 baseline union is EMPTY) — NON-WIDENING HOLDS or NARROWS at HEAD'. Update the ledger's live counts + the §-header to name the 357-00 contract changes (the F-356-01 stub + D-11/D-12/D-13) + HEAD' + the new V56SubHardening proofs + the drops BY NAME.
    4. Re-confirm the SOLVENCY-01 leg-1 byte-diff anchor (the GameAfkingModule ETH/claimablePool debit two-liner byte-unchanged by the 357-00 changes — drainAffiliateBase is BURNIE-only, D-11/D-12 are revert-only). Record it in the ledger.
    5. Confirm `git diff HEAD' HEAD -- contracts/` is empty (the subject re-freezes at HEAD' — only test/ + ledger writes downstream).
    Test + ledger writes only — ZERO contracts/*.sol edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "NON-WIDENING" test/REGRESSION-BASELINE-v56.md && grep -qE "NoPass|MustPurchaseToBeginAfking" test/REGRESSION-BASELINE-v56.md && grep -q "V56SubHardening" test/REGRESSION-BASELINE-v56.md && grep -q "SOLVENCY-01" test/REGRESSION-BASELINE-v56.md && git diff --quiet HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - The whole-tree forge test is re-run at HEAD'; the ledger records the updated live counts at HEAD'.
    - Each D-11/D-12 supersession red is dropped via vm.skip-with-reason (naming NoPass / MustPurchaseToBeginAfking) AND re-proven GREEN by a V56SubHardening positive proof — attributed BY NAME, never silently masked.
    - The F-356-01 narrowing (or regression-neutrality) is recorded; no new red from the stub.
    - `live − union == ∅` (NON-WIDENING) is re-confirmed at HEAD'; the ledger §-header names the 357-00 changes + HEAD' + V56SubHardening + the drops.
    - The SOLVENCY-01 leg-1 byte-diff anchor is re-confirmed (the 357-00 changes are BURNIE-only / revert-only).
    - `git diff HEAD' HEAD -- contracts/` is empty (the subject re-freezes at HEAD'; only test/ + ledger written).
  </acceptance_criteria>
  <done>The NON-WIDENING ledger is reconciled at HEAD': the D-11/D-12 supersession reds dropped+re-proven, the F-356-01 narrowing recorded, live − union == ∅ re-confirmed, the SOLVENCY-01 anchor held; the subject is re-frozen at HEAD'.</done>
</task>

</tasks>

<verification>
- All 6 D-14 positive proofs + the drainAffiliateBase reachability proof are GREEN against HEAD' (V56SubHardening suite).
- The NON-WIDENING ledger is reconciled at HEAD': the D-11/D-12 supersession reds dropped via vm.skip-with-reason + re-proven; the F-356-01 narrowing recorded; live − union == ∅ re-confirmed; the SOLVENCY-01 leg-1 byte anchor held.
- The subject re-freezes at HEAD' — `git diff HEAD' HEAD -- contracts/` is empty (test + ledger writes only; zero contract mutation in this plan).
</verification>

<success_criteria>
The D-14 reconciliation satisfied: the new positive proofs prove the D-11/D-12/D-13 hardening + the F-356-01 stub reachability GREEN; the regression ledger is reconciled NON-WIDENING (`live − union == ∅`) at HEAD' with the supersession reds dropped+re-proven and the SOLVENCY-01 anchor held; the audit subject is re-frozen at HEAD' for the downstream read-only delta-audit (357-01) ∥ sweep (357-02).
</success_criteria>

<output>
Create `.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00b-SUMMARY.md` when done — confirm HEAD' as the re-frozen subject SHA.
</output>
