---
phase: 357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 04
type: execute
wave: 4
depends_on: ["357-03"]
files_modified:
  - audit/FINDINGS-v56.0.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [AUDIT-01]

must_haves:
  truths:
    - "A single blocking USER checkpoint approves the closure verdict + the MILESTONE_V56_AT_HEAD_<sha> signal FORMAT + the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome + F-356-01-RESOLVED + any 357-02 new_findings disposition BEFORE the signal is emitted"
    - "MILESTONE_V56_AT_HEAD_<sha> is resolved to the concrete closure-HEAD SHA (the closure commit's own SHA, self-referential) and propagated verbatim across all targets; a final grep confirms ZERO unresolved <sha> placeholders remain"
    - "The atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied with the CURRENT v56.0 requirement set re-attested at closure (AUDIT-01 flipped to Complete); the stale ROADMAP progress table row for 356 [In Progress 5/7] is reconciled to Complete"
    - "audit/FINDINGS-v56.0.md is chmod 444 at the resolved closure HEAD; the closure commit has NO contracts/*.sol (the 357-00 fix commit is the SOLE .sol commit); .planning/ force-added (gitignored)"
    - "git diff HEAD' HEAD -- contracts/ is empty (closure is doc-only; subject frozen at HEAD'); the closure is applied LOCALLY — nothing is auto-pushed"
    - "AFTER the flip: the close-then-prompt-to-push offer (D-02) explicitly surfaces that the LIVE public contract on origin/main is BROKEN until pushed, OFFERS to push the bundle (the F-356-01 fix HEAD' + the unpushed 5cb707f2 + the closure flip), and WAITS for the USER's explicit GO — pushing is never a close side-effect"
  artifacts:
    - path: "audit/FINDINGS-v56.0.md"
      provides: "The locked (chmod 444) v56.0 findings deliverable with the resolved closure-signal SHA"
      contains: "MILESTONE_V56_AT_HEAD_"
    - path: ".planning/REQUIREMENTS.md"
      provides: "The CURRENT v56.0 REQ rows flipped to a closure attestation (AUDIT-01 Complete)"
      contains: "AUDIT-01"
  key_links:
    - from: ".planning/STATE.md"
      to: "audit/FINDINGS-v56.0.md"
      via: "verbatim closure-signal propagation"
      pattern: "MILESTONE_V56_AT_HEAD_"
    - from: ".planning/REQUIREMENTS.md"
      to: "the CURRENT v56.0 REQ-IDs"
      via: "closure re-attestation"
      pattern: "Complete|RESOLVED|NEGATIVE-VERIFIED"
---

<objective>
Author the AUDIT-01 CLOSURE FLIP: gate on a single USER approval of the closure verdict + the `MILESTONE_V56_AT_HEAD_<sha>` signal FORMAT + the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome + F-356-01-RESOLVED + any 357-02 new_findings disposition, then resolve the placeholder SHA, apply the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) with the CURRENT v56.0 requirement set re-attested (AUDIT-01 → Complete), reconcile the stale ROADMAP progress row for 356, and chmod 444 the findings deliverable. THEN surface the close-then-prompt-to-push offer (D-02). Closure is LAST and `autonomous: false` (the SECOND of the phase's two blocking USER gates; the 357-00 contract gate was the first). Doc-only — ZERO `contracts/*.sol` edits (the 357-00 fix is the SOLE .sol commit; the subject stays frozen at HEAD'). NOTHING is auto-pushed.

Purpose: the SC3 closure flip of the FULL-CLOSE terminal phase (AUDIT-01) — the milestone close. `autonomous: false` per the v45/v47/v48/v49/v55 USER-APPROVED-closure precedent + the review-before-closure discipline. Auto-advance is HELD at this boundary. The v56-specific twist: the LIVE public contract on origin/main is currently BROKEN (the affiliate-claim bug, fixed at HEAD' but unpushed) → the close-then-prompt-to-push offer (D-02) is a mandatory closure-gate item, but the push WAITS for explicit USER GO.
Output: the resolved `MILESTONE_V56_AT_HEAD_<sha>` signal propagated verbatim; the 5-doc flip applied with the CURRENT v56.0 reqs re-attested; audit/FINDINGS-v56.0.md locked chmod 444; the close-then-prompt-to-push offer surfaced; nothing pushed without explicit GO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v56.0.md
@audit/FINDINGS-v55.0.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md

<interfaces>
<!-- The closure-flip mechanics (from the v55 Phase 352 / v49 Phase 333 terminal precedent). Executor follows exactly. -->
- The 5 atomic flip targets: (1) ROADMAP.md — flip Phase 357 to Complete + the v56.0 milestone to shipped + the Progress table row 357 + RECONCILE the stale Progress row 356 (it still shows "In Progress 5/7" though 356 is verified-closed 7/7 — fix to Complete) + flip the milestone bullet at the top (▶ → ✅); (2) STATE.md — move v56.0 from the active "Current Milestone" / "Active Milestone Roadmap" position to "Last Shipped Milestone" with the closure-signal + verdict, update progress (5/5 phases, percent 100), clear the stale "Current focus: Phase 356" / "NEXT = /gsd:plan-phase 357" / Current-Position notes; (3) MILESTONES.md — prepend the v56.0 archive entry (mirror the v55.0 block at the top, `:3`); (4) PROJECT.md — flip the Current Milestone section (v56.0 → Completed); (5) REQUIREMENTS.md — flip the CURRENT v56.0 REQ rows in the Traceability table to a closure attestation (AUDIT-01 → Complete/attested-at-closure; the already-Complete rows stay).
- The closure signal: `MILESTONE_V56_AT_HEAD_<sha>` where `<sha>` is the v56.0 closure HEAD. The placeholder `<sha>` resolves to the closure commit's OWN SHA (self-referential). v48/v49/v55 used a SINGLE-commit self-referential orchestration (emit + flip + chmod 444 in one commit; the signal = the commit's own SHA) — mirror the single-commit actual. The signal propagates VERBATIM to: audit/FINDINGS-v56.0.md frontmatter (closure_signal + audit_subject_head) + §1 + §9b/§9c; ROADMAP.md; STATE.md (Last Shipped Milestone); MILESTONES.md (archive); PROJECT.md; REQUIREMENTS.md. Replace every `MILESTONE_V56_AT_HEAD_<sha>` placeholder string with the resolved value in one pass. NOTE: audit/FINDINGS-v56.0.md's `source_tree_frozen_ref` is the concrete HEAD' SHA (NOT a placeholder) — only the closure SIGNAL is resolved.
- The audit subject is FROZEN at HEAD' (the 357-00 commit); the closure is a DOC-ONLY flip — no contracts/*.sol edits. The 357-00 fix commit is the SOLE .sol commit of the phase; this closure commit has NO .sol. `.planning/` is gitignored → force-add the planning docs at commit time; audit/FINDINGS-v56.0.md is tracked. The commit-guard hook does NOT block (no .sol in the diff). Stage ONLY the 5 flip docs + the findings deliverable + the 357-00b/357-01/357-02/357-03 + SUMMARY logs; do NOT stage any held working-tree edit unrelated to closure.
- chmod 444: applied to audit/FINDINGS-v56.0.md at closure (FINAL READ-only at the closure HEAD; the v44/v46/v47/v48/v49/v55 precedent).
- The USER gate (autonomous: false): present the closure verdict (from FINDINGS-v56.0 §9a) + the proposed `MILESTONE_V56_AT_HEAD_<sha>` signal FORMAT + the SEC-01 (strategic sub/unsub unmanipulable + the D-11/D-12/D-13 hardening — the passless cap-occupancy + unfunded free-rider vectors CLOSED) + SEC-02 (SOLVENCY-01 byte-unchanged + RNG-freeze) re-attestation outcome + F-356-01-RESOLVED + the new_findings disposition (from 357-02) and WAIT for explicit USER approval BEFORE resolving the SHA / propagating / flipping. Do NOT auto-advance even if auto_advance is on (the closure-verdict gate is the separate review-before-closure discipline; closure is doc-only so the commit is hands-off AFTER the verdict approval, but the verdict + signal-format approval IS the gate).
- new_findings: if 357-02 surfaced a FINDING_CANDIDATE beyond F-356-01-RESOLVED, the USER adjudicates its disposition (DEFER→v57 [default leaning, fix design locked] / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED) at THIS gate. F-356-01 is already RESOLVED-AT-357 (the fix landed at the 357-00 gate). If a candidate is FIX-adjudicated, closure HALTS and a new contract phase is planned. If zero surviving candidates beyond F-356-01-RESOLVED + all re-attestations hold, the clean-closure verdict holds.
- THE CLOSE-THEN-PROMPT-TO-PUSH OFFER (D-02 — MANDATORY, AFTER the flip): v56 DIVERGES from every prior milestone's "NOTHING pushed at close" rule because the v56 contract subject is ALREADY PUBLIC on origin/main (354 e18af451 + the 355 GAS diff are pushed) — so the *broken* affiliate-claim contract is currently LIVE. Plus there is an UNPUSHED 8-line contract change going into close: `5cb707f2 feat(advance): active AfKing sub bypasses mustMintToday with no time delay`. Posture: (1) apply the closure flip LOCALLY — nothing is auto-pushed; (2) at this gate, EXPLICITLY SURFACE that the live public contract is broken until pushed, and OFFER to push the bundle (the F-356-01 fix commit HEAD' + 5cb707f2 + the closure flip) — but WAIT for the USER's explicit GO before any push; (3) pushing is outward-facing/publishing → a deliberate, explicitly-authorized step, NEVER a side effect of closing. If the USER declines/defers the push, record "closed locally; push deferred — live contract remains broken until pushed" in the SUMMARY.
- Commit discipline: planning docs + audit/FINDINGS-v56.0.md are AGENT-committable (no contracts/*.sol → the commit-guard hook does not block; verify the frozen-subject check HEAD'-to-HEAD empty before committing). End the commit message with the Co-Authored-By trailer per the global convention.
- v56 = the 5-phase shape: 353 SPEC / 354 IMPL / 355 GAS (net diff) / 356 TST / 357 TERMINAL. The §9b wave summary + the MILESTONES archive entry record all 5 (357 incl. the 357-00 hardening + 357-00b + the delta-audit + sweep + closure flip — the FIRST TERMINAL with a contract footprint).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is the doc-only milestone closure flip + the close-then-prompt-to-push offer. The audit subject stays frozen at HEAD'. The boundaries are the human review gate before the closure signal is emitted AND the explicit-GO gate before any push (the live public contract is currently broken). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-04-PREMATURE | Elevation | emitting the closure signal before review | mitigate | The plan is autonomous: false — a single blocking USER checkpoint approves the verdict + signal + the SEC/hardening re-attestation outcome + F-356-01-RESOLVED + any new_findings disposition BEFORE resolution/propagation (the v45/v47/v48/v49/v55 USER-APPROVED-CLOSE precedent; does not auto-advance even with auto_advance on). |
| T-357-04-SHADRIFT | Tampering | closure-signal SHA propagation | mitigate | The MILESTONE_V56_AT_HEAD_<sha> placeholder is resolved + propagated VERBATIM in one pass across all targets; a final grep confirms ZERO unresolved <sha> placeholders remain (the literal <sha> token must be gone). source_tree_frozen_ref (the concrete HEAD') is distinct and untouched. |
| T-357-04-CONTRACTS | Tampering | frozen subject | mitigate | The frozen-subject check (HEAD' to HEAD) verified empty before the closure commit; closure is doc-only; the commit-guard hook does not block (no .sol in the diff); only the closure docs are staged. |
| T-357-04-PUSH | Elevation | pushing the broken-then-fixed live contract | mitigate | The push is NEVER a close side-effect — it is OFFERED and WAITS for explicit USER GO (D-02). The closure flip is applied LOCALLY first. The offer explicitly surfaces that the live public contract is broken until pushed so the USER decides deliberately. If declined, the SUMMARY records push-deferred. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: USER closure-verdict approval gate (before emitting MILESTONE_V56_AT_HEAD)</name>
  <files>(none — this is a blocking human-verify gate; the doc flips happen in Task 2 after approval)</files>
  <what-built>
    The full v56.0 audit substance is complete and consolidated: 357-00 (the THREE-change contract gate — F-356-01 drainAffiliateBase stub + D-11 pass-required subscribe + D-12 purchase-grounded subscribe + D-13 VAULT/SDGNRS exemption, committed at HEAD' after USER hand-review), 357-00b (the D-14 NON-WIDENING reconciliation + the V56SubHardening positive proofs, live − union == ∅ at HEAD'), 357-01 delta-audit (every v56 surface NON-WIDENING vs 453f8073, zero orphan hunks, SOLVENCY-01 byte-unchanged + RNG-freeze + the affiliate flat-7% deterministic-split-PULL non-gameability + the open two-path + LIVE-01 + GAS-06 + the shared-quest non-perturbation + the 5cb707f2 + the 357-00 hardening attributed), 357-02 adversarial sweep (the 3-skill genuine-parallel disposition + the XMODEL augmentation + the /degen-skeptic dual-gate on the strategic sub/unsub edge + the D-11/D-12 gates + the now-sound 5cb707f2 bypass + the drainAffiliateBase stub + the affiliate claim CEI — the passless cap-occupancy + unfunded free-rider vectors CLOSED), and audit/FINDINGS-v56.0.md (the 9-section deliverable, F-356-01 RESOLVED-AT-357). The closure verdict + the proposed closure signal are ready but NOT yet emitted.
  </what-built>
  <how-to-verify>
    1. Read audit/FINDINGS-v56.0.md §9a (the closure verdict — locked-target + actual) and §4.1 (the adversarial sweep outcome: row counts + the XMODEL augmentation + any surviving FINDING_CANDIDATE beyond F-356-01-RESOLVED).
    2. Confirm the delta-audit (357-01) attests all changed surfaces NON-WIDENING + SOLVENCY-01 byte-unchanged + RNG-freeze + the affiliate non-gameability + the open two-path/no-double-open + LIVE-01 + GAS-06 + the shared-quest non-perturbation; the regression is live − union == ∅ BY NAME at HEAD'; KNOWN-ISSUES.md byte-unmodified vs v55.
    3. Confirm the D-11/D-12/D-13 hardening re-attestation (357-02 §C/§D): the passless cap-occupancy + unfunded free-rider vectors CLOSED, the 5cb707f2 bypass now SOUND, F-356-01 RESOLVED-AT-357.
    4. Approve (a) the closure verdict text, (b) the MILESTONE_V56_AT_HEAD_<sha> signal FORMAT, (c) the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome + F-356-01-RESOLVED, and (d) the disposition of any surviving FINDING_CANDIDATE (DEFER→v57 [default] / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED). If a candidate is FIX-adjudicated, closure HALTS and a new contract phase is planned.
  </how-to-verify>
  <read_first>
    - audit/FINDINGS-v56.0.md (§9a verdict + §4 adversarial disposition + §3 delta-surface + §3.B the composition matrix + §3.C the req re-attestation)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md
    - .planning/ROADMAP.md (the Phase 357 goal + the 3 Success Criteria — the locked-target verdict source)
  </read_first>
  <action>
    Present the closure verdict (FINDINGS-v56.0 §9a), the adversarial sweep outcome (§4.1 row counts + the XMODEL augmentation + any FINDING_CANDIDATE), the delta-audit NON-WIDENING attestation (zero orphan hunks), the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome (the passless cap-occupancy + unfunded free-rider vectors CLOSED; the 5cb707f2 bypass SOUND), F-356-01-RESOLVED-AT-357, and the regression live − union == ∅ to the USER, then BLOCK for explicit approval of the verdict text + the MILESTONE_V56_AT_HEAD signal FORMAT + the re-attestation outcomes + any FINDING_CANDIDATE disposition. Resolve / propagate / flip nothing until approval. This gate must NOT auto-advance even with auto_advance on. (The push offer is a SEPARATE step in Task 2 AFTER the flip — do NOT push at this gate.)
  </action>
  <verify>
    <human-check>The USER reviews FINDINGS-v56.0 §9a + §4.1 + §3.B + the 357-01/357-02 logs and explicitly approves the verdict + the signal FORMAT + the SEC/hardening re-attestation outcomes + F-356-01-RESOLVED + any FINDING_CANDIDATE disposition.</human-check>
  </verify>
  <acceptance_criteria>
    - The closure verdict (FINDINGS §9a), the sweep outcome (§4.1 + XMODEL), the delta-audit NON-WIDENING + the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome + F-356-01-RESOLVED, and any FINDING_CANDIDATE disposition are presented to the USER.
    - Execution BLOCKS until the USER explicitly approves the verdict + the signal FORMAT + the re-attestation outcomes + the FINDING_CANDIDATE disposition.
    - No closure-signal SHA is resolved or propagated, and no doc is flipped, before approval; no push happens at this gate.
    - This gate does NOT auto-advance even if auto_advance is on.
  </acceptance_criteria>
  <done>The USER has explicitly approved the closure verdict + the MILESTONE_V56_AT_HEAD signal FORMAT + the SEC/hardening re-attestation outcome + F-356-01-RESOLVED + any FINDING_CANDIDATE disposition; execution is cleared to proceed to Task 2.</done>
  <resume-signal>Type "approved" to emit MILESTONE_V56_AT_HEAD and apply the 5-doc flip, or describe required changes / the FINDING_CANDIDATE disposition / a re-attestation-failure halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve the closure-signal SHA, apply the atomic 5-doc flip (reconcile the stale 356 row), chmod 444, commit (local) — THEN surface the close-then-prompt-to-push offer</name>
  <files>audit/FINDINGS-v56.0.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - audit/FINDINGS-v55.0.md §9 (the single-commit self-referential SHA closure orchestration + the verbatim propagation-target list to mirror — read the §9 region ~`:795`)
    - .planning/milestones/v55.0-phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-04-PLAN.md (the closure-flip orchestration to mirror — SHA resolution + the atomic flip + the final placeholder grep)
    - audit/FINDINGS-v56.0.md (the placeholder targets to resolve: frontmatter + §1 + §9b/§9c)
    - .planning/ROADMAP.md (Phase 357 + the v56.0 milestone flip points + the Progress table — the stale 356 "In Progress 5/7" row to reconcile + the ▶ → ✅ milestone bullet)
    - .planning/STATE.md (the Current Milestone → Last Shipped Milestone move + the stale "Current focus: Phase 356" / "NEXT = /gsd:plan-phase 357" / Current-Position notes to clear + the progress block)
    - .planning/MILESTONES.md (the v55.0 archive block at the top, `:3` — the format to mirror for the v56.0 entry, prepended)
    - .planning/PROJECT.md (the Current Milestone section)
    - .planning/REQUIREMENTS.md (the Traceability table — the CURRENT v56.0 REQ rows, AUDIT-01 the only Pending)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md (HEAD' — for the push-bundle description)
  </read_first>
  <action>
    After USER approval (Task 1), execute the closure:
    1. Verify the frozen-subject check (HEAD' to HEAD) is empty (the subject is frozen; closure is doc-only).
    2. Resolve the MILESTONE_V56_AT_HEAD_<sha> placeholder to the closure-HEAD SHA (the closure commit's own SHA, self-referential; mirror the v48/v49/v55 single-commit actual). Replace EVERY MILESTONE_V56_AT_HEAD_<sha> placeholder string in audit/FINDINGS-v56.0.md (frontmatter closure_signal + audit_subject_head, §1, §9b, §9c) with the resolved value. Leave source_tree_frozen_ref (the concrete HEAD' SHA) untouched.
    3. Apply the atomic 5-doc flip, propagating the resolved signal VERBATIM:
       - ROADMAP.md: Phase 357 → Complete; the v56.0 milestone → shipped (closure signal + verdict; flip the top milestone bullet ▶ → ✅); the Progress table row 357 → Complete; RECONCILE the stale Progress row 356 (it shows "In Progress 5/7" — fix to Complete 7/7, 356 is verified-closed).
       - STATE.md: move v56.0 to "Last Shipped Milestone" (closure signal + the 5-phase summary + verdict + the F-356-01-RESOLVED + the D-11/D-12/D-13 hardening outcome + the new_findings disposition); update progress (5/5 phases, percent 100); CLEAR the stale "Current focus: Phase 356" / "NEXT = /gsd:plan-phase 357" / Current-Position "Phase 357 ... ready to plan" notes (it is DONE).
       - MILESTONES.md: prepend the v56.0 archive entry mirroring the v55.0 block (subject HEAD', baseline 453f8073, the 5-phase shape 353-357, closure signal, the aggregator + ticket primitive + affiliate PULL + quest non-perturbation + open-end + GAS wins + SEC-01/02 + the D-11/D-12/D-13 hardening + LIVE-01 + GAS-06 + F-356-01-RESOLVED, regression live − union == ∅ NON-WIDENING, the close-then-prompt-to-push posture, any FINDING_CANDIDATE disposition).
       - PROJECT.md: flip the Current Milestone section (v56.0 → Completed).
       - REQUIREMENTS.md: flip the CURRENT v56.0 REQ rows in the Traceability table to a closure attestation (AUDIT-01 → Complete/attested-at-closure; the already-Complete rows stay).
    4. chmod 444 audit/FINDINGS-v56.0.md (FINAL READ-only at the closure HEAD).
    5. Commit the closure flip (planning docs force-added since .planning/ is gitignored; audit/FINDINGS-v56.0.md tracked; NO contracts/*.sol in the diff so the commit-guard hook does not block; stage ONLY the closure docs + the 357 logs/SUMMARYs). End the commit message with the Co-Authored-By trailer. This commit has NO .sol (the 357-00 fix is the sole .sol commit). DO NOT push.
    6. Confirm a final grep finds ZERO unresolved MILESTONE_V56_AT_HEAD_<sha> placeholders remaining (the literal <sha> token must be gone).
    7. THE CLOSE-THEN-PROMPT-TO-PUSH OFFER (D-02): AFTER the local closure commit, EXPLICITLY surface to the USER that the LIVE public contract on origin/main is BROKEN until pushed (the affiliate-claim bug — fixed at HEAD' but unpushed), and OFFER to push the bundle (the F-356-01 fix commit HEAD' + the unpushed 5cb707f2 advance-gate + the closure flip). WAIT for the USER's explicit GO before any push — pushing is NEVER a close side-effect. If the USER says GO, push; if declined/deferred, record "closed locally; push deferred — live contract remains broken until pushed" in the SUMMARY. Doc-only — no source edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && HEADP=$(grep -oE "[0-9a-f]{40}" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md | head -1); git diff --quiet "$HEADP" HEAD -- contracts/ && test "$(stat -c '%a' audit/FINDINGS-v56.0.md)" = "444" && ! grep -q "MILESTONE_V56_AT_HEAD_<sha>" audit/FINDINGS-v56.0.md && grep -q "MILESTONE_V56_AT_HEAD_" audit/FINDINGS-v56.0.md && grep -q "MILESTONE_V56_AT_HEAD_" .planning/STATE.md && ! grep -qE "In Progress.*356|356.*In Progress" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The frozen-subject check (HEAD' to HEAD) is empty (closure is doc-only; subject frozen).
    - The MILESTONE_V56_AT_HEAD_<sha> placeholder is resolved to the concrete closure SHA and propagated verbatim to all FINDINGS targets (frontmatter + §1 + §9b/§9c); ZERO unresolved <sha> placeholders remain; source_tree_frozen_ref (the concrete HEAD') is untouched.
    - The atomic 5-doc flip is applied: ROADMAP (Phase 357 Complete + milestone shipped + Progress row 357 + the stale 356 row reconciled to Complete + the ▶ → ✅ bullet), STATE (Last Shipped Milestone + signal + verdict + the stale focus/RESUME notes cleared + 5/5 phases 100%), MILESTONES (v56.0 archive entry prepended), PROJECT (Current Milestone flipped), REQUIREMENTS (the CURRENT v56.0 rows attested at closure, AUDIT-01 Complete).
    - audit/FINDINGS-v56.0.md is chmod 444.
    - The closure flip is committed with the Co-Authored-By trailer; the commit-guard hook did not block (no contracts/*.sol in the diff); nothing is auto-pushed.
    - The close-then-prompt-to-push offer (D-02) is surfaced (the live contract is broken until pushed; offer the bundle HEAD' + 5cb707f2 + closure flip) and WAITS for explicit USER GO; the push outcome (pushed / deferred) is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Closure signal resolved + propagated verbatim; the atomic 5-doc flip applied with the CURRENT v56.0 reqs re-attested + the stale 356 row reconciled; FINDINGS-v56.0.md chmod 444; closure committed LOCALLY (no .sol); zero unresolved placeholders; the close-then-prompt-to-push offer surfaced + the push outcome (gated on explicit GO) recorded; the frozen subject is byte-identical to HEAD'.</done>
</task>

</tasks>

<verification>
- The frozen-subject check (HEAD' to HEAD) is empty (closure is doc-only; subject frozen throughout).
- audit/FINDINGS-v56.0.md is chmod 444 with the resolved closure SHA (no <sha> placeholder remaining); source_tree_frozen_ref is the concrete HEAD'.
- The 5 docs (ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS) are flipped atomically; the CURRENT v56.0 REQ rows attested at closure (AUDIT-01 Complete); the stale ROADMAP 356 progress row reconciled.
- The USER approved the verdict + signal FORMAT + the SEC/hardening re-attestation outcome + F-356-01-RESOLVED at the Task 1 blocking gate before any propagation; the close-then-prompt-to-push offer (D-02) was surfaced AND the push waited for explicit GO (never a close side-effect); nothing pushed without GO.
</verification>

<success_criteria>
The SC3 closure flip of AUDIT-01 satisfied: the USER approves the closure verdict + the MILESTONE_V56_AT_HEAD signal FORMAT + the SEC-01/SEC-02 + D-11/D-12/D-13 hardening re-attestation outcome + F-356-01-RESOLVED at a single blocking gate; the signal is resolved (self-referential closure-commit SHA) + propagated verbatim; the atomic 5-doc closure flip is applied with the CURRENT v56.0 requirements re-attested + the stale 356 row reconciled; FINDINGS-v56.0.md is locked chmod 444; the closure commit has no .sol (the 357-00 fix is the sole .sol commit); zero unresolved placeholders; the close-then-prompt-to-push offer (D-02) is surfaced + waits for explicit USER GO; nothing pushed without GO. The frozen subject is byte-identical to HEAD'.
</success_criteria>

<output>
Create `.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-04-SUMMARY.md` when done — record the resolved closure SHA + the push outcome (pushed / deferred).
</output>
