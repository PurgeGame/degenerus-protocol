---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 04
type: execute
wave: 1
depends_on: ["356-02"]
files_modified:
  - test/fuzz/V56FreezeSolvency.t.sol
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "The ETH/claimablePool debit path is byte-unchanged vs 453f8073 (the SOLVENCY-01 site GameAfkingModule:744-745)"
    - "balance + steth.balanceOf(this) >= claimablePool holds across churn/accrue/pendingBurnie-claim sequences"
    - "The subscribe min-buy STAMPS for-later-open and never inline-resolves pre-RNG; single-roll open and pendingBurnie credit consume ONLY the frozen rngWordByDay[stampDay]"
    - "Two opens of the same stamp at different blocks (prevrandao/coinbase/roll perturbed) produce byte-identical materialized boxes"
    - "D-05: SEC-02 is proven in three legs — the ETH/claimablePool byte-unchanged anchor, the solvency invariant fuzz, and the RNG-freeze determinism fuzz"
  artifacts:
    - path: "test/fuzz/V56FreezeSolvency.t.sol"
      provides: "SEC-02 solvency invariant fuzz + RNG-freeze determinism fuzz"
      contains: "contract V56FreezeSolvency"
      min_lines: 150
  key_links:
    - from: "the solvency invariant"
      to: "claimablePool (slot 1 byte 16, uint128) + steth.balanceOf"
      via: "vm.load(game, slot 1) >> (16*8) masked uint128"
      pattern: "claimablePool|CLAIMABLE_POOL"
    - from: "the freeze-determinism oracle"
      to: "rngWordByDay[stampDay] seed (no block.* entropy)"
      via: "keccak256(abi.encode(rngWordByDay[stampDay], player, stampDay, amount))"
      pattern: "rngWordByDay|LootBoxOpened"
---

<objective>
Author `test/fuzz/V56FreezeSolvency.t.sol` — the SEC-02 proof in three legs (D-05): (1) the ETH/`claimablePool` debit path byte-unchanged vs `453f8073` (a grep/diff anchor recorded in the 356-07 ledger; the forge side asserts the debit math is the v55 SOLVENCY-01 site untouched); (2) a solvency invariant fuzz (`balance + steth.balanceOf(this) >= claimablePool` across churn/accrue/claim); (3) an RNG-freeze determinism fuzz (the subscribe min-buy STAMPS-for-later-open and never inline-resolves pre-RNG; single-roll open; `pendingBurnie` credit consume ONLY the frozen day-word). ADAPT the now-v56-migrated `V55FreezeDeterminism` (the materialized-box byte-identity oracle) + the solvency reads from `V55RevertFreeEvCap`.

Purpose: SEC-02 proves the v56 accrual/settle redesign is a BURNIE-emission-timing change only — it touches no frozen RNG-window slot and the ETH/pool debit is byte-frozen. The affiliate/quest rewards stay BURNIE flip-credit OFF the ETH path.
Output: one new forge fuzz suite proving SEC-02 empirically (legs 2+3); the leg-1 byte-diff anchor is recorded in 356-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md

<interfaces>
<!-- Shipped surface under test (read-only; verified anchors). -->

GameAfkingModule.sol:
  _deliverAfkingBuy (the SOLVENCY-01 debit site, byte-frozen from v55): :744-745
    `afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue);`
  the subscribe min-buy STAMPS for-later-open (never inline-resolves pre-RNG); single-roll open;
  pendingBurnie credit via claimAfkingBurnie :1270 (CEI :1277)
  the afking open is reached via mintBurnie() (the autoOpen selector was dropped — NOT re-exposed on the Game)

The freeze-determinism oracle (from V55FreezeDeterminism.t.sol:50-72, NOW v56-migrated by 356-02):
  bytes32 LOOTBOX_OPENED_SIG = keccak256("LootBoxOpened(address,uint48,uint32,uint256,uint24,uint32,uint256,bool)");
  struct Box { bool present; uint48 lootboxIndex; uint32 day; uint256 amount; uint24 futureLevel; uint32 futureTickets; uint256 burnie; bool roundedUp; }
  Seed = keccak256(abi.encode(rngWordByDay[stampDay], player, stampDay, amount)) -- carries NO block.* entropy.
  Two opens of the SAME stamp at DIFFERENT blocks (vm.roll/warp/prevrandao/coinbase perturbed) -> byte-identical Box.
  The two-block determinism test to adapt: testStampedDayDeterminismOpenAtTwoBlocks (V55FreezeDeterminism:91+).

Solvency reads (from V55RevertFreeEvCap.t.sol:46-50, NOW v56-migrated):
  CLAIMABLE_POOL_SLOT = 1 (claimablePool at byte offset 16, uint128)
  AFKING_FUNDING_SLOT = 8 (afkingFunding mapping)
  Class-B fail-loud pattern: testClassB_StageDebitSolvencyFailsLoud (V55RevertFreeEvCap:196)

The v56 Sub-slot offset block: copy from V56AfkingGasMarginal:68-89.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solvency invariant fuzz (leg 2) + the SOLVENCY-01 debit-site forge assertion (leg 1 forge arm)</name>
  <behavior>
    - Solvency invariant: across random {sub, unsub, buy, accrue, claimAfkingBurnie} sequences, `address(game).balance + steth.balanceOf(address(game)) >= claimablePool` ALWAYS holds.
    - The pendingBurnie accrual + claim NEVER debits ETH/claimablePool (the affiliate/quest rewards are BURNIE flip-credit OFF the ETH path — claiming BURNIE leaves claimablePool unchanged).
    - The ETH/claimablePool debit happens ONLY at the buy-delivery site (the v55 SOLVENCY-01 site), and the debit equals the delivered ethValue exactly.
  </behavior>
  <read_first>
    - test/fuzz/V55RevertFreeEvCap.t.sol (the solvency reads ~46-50, the Class-B fail-loud pattern ~196 — NOW v56-migrated)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89,579-614 (the offset block, the _settleGame VRF-drain harness)
    - contracts/modules/GameAfkingModule.sol:744-745 (the byte-frozen SOLVENCY-01 debit), :1270-1284 (the BURNIE-credit claim, OFF the ETH path)
    - test/fuzz/helpers/DeployProtocol.sol (the fixture; the steth mock + claimablePool wiring)
  </read_first>
  <action>
    Create `test/fuzz/V56FreezeSolvency.t.sol` (contract `V56FreezeSolvency is DeployProtocol`). Copy the v56 offset block + the solvency-read constants (CLAIMABLE_POOL_SLOT=1 byte16 uint128, AFKING_FUNDING_SLOT=8) from the migrated analogs. Write a stateful solvency invariant driving random {sub, unsub, buy, claimAfkingBurnie} actions and asserting `game.balance + steth.balanceOf(game) >= claimablePool` after every action (read claimablePool via vm.load slot 1 >> 16*8 masked uint128). Add a focused unit asserting a `claimAfkingBurnie` call leaves `claimablePool` byte-unchanged (BURNIE credit is OFF the ETH path) and that the buy-delivery debit equals the delivered ethValue exactly (the v55 SOLVENCY-01 site behavior). Use the Class-B fail-loud pattern for the debit assertion. Seeded-fuzz determinism (`[fuzz] seed=0xdeadbeef`); carry the unseeded-`[invariant]` ⊆ caveat (Pitfall 5). Lean comments only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56FreezeSolvency --match-test "Solvency|Debit|ClaimablePool|Burnie" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56FreezeSolvency --match-test "Solvency|Debit|ClaimablePool|Burnie"` exits 0; the solvency invariant holds across the fuzzed churn/accrue/claim sequence; the BURNIE-claim-leaves-claimablePool-unchanged unit passes (an exact equality on the pool before/after claim); `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>The solvency invariant holds; the BURNIE credit is proven OFF the ETH/claimablePool path; the debit equals delivered value; no contract touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RNG-freeze determinism fuzz (leg 3) — STAMP-not-resolve + single-roll open + frozen-word-only</name>
  <behavior>
    - The subscribe min-buy STAMPS a box for-later-open and NEVER inline-resolves pre-RNG (no LootBoxOpened at subscribe time; the materialization is deferred to the open).
    - Two opens of the SAME stamp at DIFFERENT blocks (vm.roll/warp + perturbed block.prevrandao/coinbase) produce byte-identical materialized boxes (the seed = keccak(rngWordByDay[stampDay], player, stampDay, amount) carries no block.* entropy).
    - The single-roll open + the pendingBurnie credit consume ONLY the frozen rngWordByDay[stampDay] — no entropy added between RNG request and unlock.
  </behavior>
  <read_first>
    - test/fuzz/V55FreezeDeterminism.t.sol (the materialized-box oracle ~50-72, the two-block determinism test testStampedDayDeterminismOpenAtTwoBlocks ~91+ — NOW v56-migrated by 356-02)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89,594-614 (the offset block, _settleClean — DEMAND clean before a mintBurnie open-leg measure)
    - contracts/modules/GameAfkingModule.sol (the subscribe min-buy stamp path, mintBurnie :1193 the afking-open entry, the single-roll open, the cachedDay/cachedWord short-circuit :1157-1163)
    - contracts/modules/DegenerusGameAdvanceModule.sol:1262 (rngGate — the rngWordByDay[day] frozen word the open consumes)
  </read_first>
  <action>
    Extend `V56FreezeSolvency.t.sol`. Adapt the two-block determinism test: subscribe + STAGE to land a stamped box on a known stampDay (rngWordByDay[stampDay] set), then open the SAME stamp at two different blocks with perturbed block.prevrandao/coinbase/number/timestamp (vm.roll/warp/setBlockprevrandao) and assert the two materialized Box structs (decoded from the LOOTBOX_OPENED_SIG event) are byte-identical. The afking open is reached via `mintBurnie()` (NOT a Game-exposed autoOpen — the selector was dropped). Add a STAMP-not-resolve assertion: a subscribe min-buy emits NO LootBoxOpened pre-RNG (the box is stamped, not resolved). Use `_settleClean` before the open-leg to avoid the RngNotReady second-cycle trip (Don't-Hand-Roll). Seeded-fuzz determinism. Lean comments only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56FreezeSolvency --match-test "Freeze|Determinism|TwoBlock|Stamp|Frozen" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56FreezeSolvency --match-test "Freeze|Determinism|TwoBlock|Stamp|Frozen"` exits 0; the two-block open produces byte-identical Box structs (the assertion compares all 8 fields); the STAMP-not-resolve assertion confirms no LootBoxOpened at subscribe time; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>The RNG-freeze determinism holds (same stamp, different blocks -> identical box); STAMP-not-resolve confirmed; the open consumes only the frozen day-word; no contract touched.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (SEC-02 — solvency + RNG-freeze)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| afking accrual/settle -> ETH/claimablePool ledger | The new pendingBurnie accrual must NOT debit ETH/claimablePool (a divergence would break SOLVENCY-01). |
| RNG request -> unlock window | Any entropy added between request and unlock (block.prevrandao/coinbase leaking into the open) would let a player grind the open outcome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-04-SOL | Tampering | _deliverAfkingBuy debit (:744-745) | mitigate | Leg 1+2: the ETH/claimablePool debit is byte-frozen from v55; affiliate/quest rewards are BURNIE flip-credit OFF the ETH path. Assert the solvency invariant + BURNIE-claim-leaves-pool-unchanged + (356-07) the byte-diff anchor. |
| T-356-04-FRZ | Tampering (RNG) | the open materialization seed | mitigate | Leg 3: the open seed = keccak(rngWordByDay[stampDay], player, stampDay, amount) — NO block.* entropy. Assert two-block byte-identity + STAMP-not-resolve. |
| T-356-04-DIV | Tampering | the new accrue/settle surfaces | mitigate | The accrue/settle touches no frozen RNG-window slot; the single-roll open consumes only the frozen day-word. The determinism fuzz proves it. |
| T-356-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `forge build` EXIT 0.
- `forge test --match-contract V56FreezeSolvency` green (solvency invariant + freeze determinism + STAMP-not-resolve).
- The leg-1 byte-diff anchor (`git diff 453f8073 HEAD -- contracts/modules/GameAfkingModule.sol` shows the debit site :744-745 unchanged) is handed to 356-07 for the ledger record.
- ZERO `contracts/*.sol` mutation (ContractAddresses.sol restored each round-trip).
</verification>

<success_criteria>
- The solvency invariant holds across churn/accrue/claim (leg 2).
- The BURNIE credit is proven OFF the ETH/claimablePool path (leg 1 forge arm); the byte-diff anchor recorded in 356-07.
- The RNG-freeze determinism holds: same stamp at different blocks -> byte-identical box; STAMP-not-resolve confirmed (leg 3).
- No contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-04-SUMMARY.md` when done
</output>
