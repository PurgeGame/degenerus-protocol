---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - test/fuzz/V55FreezeDeterminism.t.sol
  - test/fuzz/V55RevertFreeEvCap.t.sol
  - test/fuzz/V55SetMutationOpenE.t.sol
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "The 3 v55-proof fuzz files read AND write the v56-repacked Sub slot (offsets + write-mask helpers in lockstep)"
    - "The _setStamp / _setScorePlus1 write helpers mask the v56 layout (amount uint24@8, scorePlus1 uint16@6, day markers uint24), not the old uint96/uint32 layout"
    - "These 3 files are valid ADAPT-source templates for the Wave-1 v56 successors (no stale-layout writes left to carry forward)"
  artifacts:
    - path: "test/fuzz/V55FreezeDeterminism.t.sol"
      provides: "v56-layout freeze-determinism oracle (offsets + _setStamp migrated)"
      contains: "OFF_LASTBOUGHT = 11"
    - path: "test/fuzz/V55SetMutationOpenE.t.sol"
      provides: "v56-layout set-mutation/two-path oracle (offsets + _setScorePlus1 migrated)"
      contains: "OFF_LASTBOUGHT = 11"
  key_links:
    - from: "_setStamp / _setScorePlus1 write helpers"
      to: "the v56-repacked Sub slot (amount uint24@8, scorePlus1 uint16@6)"
      via: "packed &= ~mask << off*8 ; packed |= value << off*8"
      pattern: "OFF_AMOUNT *= *8"
---

<objective>
Wave-0 precondition (part 2 of 2). Migrate the 3 v55-PROOF fuzz files (`V55FreezeDeterminism`, `V55RevertFreeEvCap`, `V55SetMutationOpenE`) from the stale `OFF_LASTBOUGHT=21`/uint32 + `OFF_AMOUNT=9`/uint96 + `OFF_SCOREPLUS1=7`/uint16 layout to the v56 re-pack — BOTH the read offsets AND the `_setStamp`/`_setScorePlus1` WRITE-mask helpers, in lockstep. Unlike the 7 read-only files in 356-01, these 3 carry write helpers that compose the OLD packed layout; a read-only constant swap would corrupt their stamp writes (the masks must shift too).

Purpose: D-10 (migrate ALL stale-offset files) + a hard precondition for Wave 1 — `V55RevertFreeEvCap` + `V55SetMutationOpenE` are the ADAPT-source templates for the SEC-01 suite (356-03) and `V55FreezeDeterminism` for SEC-02 (356-04); they MUST be fully green on the v56 layout before they are adapted, or the new suites inherit corrupt stamp masks. RESEARCH Pitfall + PATTERNS "EXTRA care for 3 of the 10" flag this lockstep explicitly.
Output: 3 v55-proof files reading and writing the v56 Sub slot correctly; valid ADAPT templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md

<interfaces>
<!-- The CANONICAL v56 Sub-slot offset block (from V56AfkingGasMarginal.t.sol:68-89). -->
<!-- These 3 files have BOTH read offsets AND write-mask helpers to migrate. -->

v56 Sub slot (SUBOF_SLOT = 66) — the WRITE-relevant fields:
  scorePlus1 u16 @6   (was OFF_SCOREPLUS1 = 7)
  amount     u24 @8   (was OFF_AMOUNT = 9, uint96)   <-- WIDTH CHANGE 96 -> 24
  lastAutoBoughtDay u24 @11 (was OFF_LASTBOUGHT = 21, uint32)
  lastOpenedDay     u24 @14 (was OFF_LASTOPENED = 25, uint32)
  afkCoveredThroughDay u24 @17 · afkingStartDay u24 @20
  affiliateBase u32 @23 · pendingBurnie u32 @27 · subStreakLatch u8 @31

The verified stale write helpers to migrate (from grep this session):
  V55FreezeDeterminism._setStamp (lines ~422-427): masks OFF_SCOREPLUS1 u16 + OFF_AMOUNT u96 + day markers u32
  V55RevertFreeEvCap stamp-write  (lines ~559-564): same mask shape
  V55SetMutationOpenE: OFF_SCOREPLUS1=7 / OFF_AMOUNT=9(uint96) + _setScorePlus1 (lines ~473-477) masking byte 7
    (its OFF_DAILY=0 / OFF_VALIDTHROUGH=1 are UNCHANGED — do not touch)

The 08e59a4a transform PLUS the write-mask shift:
  read: OFF_LASTBOUGHT 21->11, OFF_LASTOPENED 25->14, _subField(...,32)->...,24
  write: in _setStamp/_setScorePlus1, re-target the mask offsets to scorePlus1@6 / amount@8 and
         narrow the amount mask from (1<<96)-1 to (1<<24)-1 (uint24); day-marker write masks to uint24.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate V55FreezeDeterminism + V55RevertFreeEvCap (offsets + _setStamp masks)</name>
  <read_first>
    - test/fuzz/V55FreezeDeterminism.t.sol (offsets lines ~54-56; _setStamp lines ~422-429)
    - test/fuzz/V55RevertFreeEvCap.t.sol (offsets lines ~63-65; stamp-write lines ~559-566; CLAIMABLE_POOL_SLOT=1 byte16 + AFKING_FUNDING_SLOT=8 reads ~46-50)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89 (the canonical v56 offset block to match)
    - contracts/storage/DegenerusGameStorage.sol (confirm amount is uint24@8, scorePlus1 uint16@6 in the shipped re-pack)
  </read_first>
  <action>
    In BOTH files: migrate the read offsets (`OFF_SCOREPLUS1 = 7` -> `6`; `OFF_AMOUNT = 9` -> `8` and treat as uint24; `OFF_LASTBOUGHT = 21` -> `11`; `OFF_LASTOPENED = 25` -> `14`; add/correct `OFF_AFKCOVERED=17`, `OFF_AFKINGSTART=20`, `OFF_AFFBASE=23`, `OFF_PENDINGBURNIE=27`, `OFF_STREAKLATCH=31` to match the canonical block). Change every `_subField(..., 32)` day-marker read to width `24`. Then migrate the WRITE-mask helpers in lockstep: in `_setStamp` (and the RevertFreeEvCap stamp-write), re-target the scorePlus1 mask to byte 6, NARROW the amount mask from `(uint256(1) << 96) - 1` to `(uint256(1) << 24) - 1` at offset 8, and narrow any day-marker write mask to uint24. The freeze-determinism + EV-cap LOGIC stays byte-identical — this is a layout migration only (no behavior change, lean-comments rule). Confirm the shipped widths via `forge inspect DegenerusGame storageLayout` if ambiguous.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-path 'test/fuzz/V55FreezeDeterminism.t.sol' --match-path 'test/fuzz/V55RevertFreeEvCap.t.sol' 2>&1 | grep -E "Suite result|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    Both files contain `OFF_LASTBOUGHT = 11` and `OFF_AMOUNT = 8` and `OFF_SCOREPLUS1 = 6`; `grep -n '(uint256(1) << 96)' test/fuzz/V55FreezeDeterminism.t.sol test/fuzz/V55RevertFreeEvCap.t.sol` returns empty (the uint96 amount mask is gone); the previously garbage-reading stamp-determinism assertions no longer red with `6555125 != 3774873600`-shaped messages (any still-red recorded PRE-EXISTING for the 356-07 union); `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>Both freeze/EV-cap proofs read AND write the v56 Sub slot; no uint96 amount mask remains; logic unchanged; ContractAddresses.sol restored.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate V55SetMutationOpenE (offsets + _setScorePlus1 mask; preserve OFF_DAILY/OFF_VALIDTHROUGH)</name>
  <read_first>
    - test/fuzz/V55SetMutationOpenE.t.sol (OFF_SCOREPLUS1=7/OFF_AMOUNT=9 lines ~54-56; _setScorePlus1 lines ~473-479; the swap-pop streak arm ~213-266; the no-orphan control/removed pair ~124-169; the two-path coexistence ~82-116)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89 (the canonical v56 offset block)
    - contracts/storage/DegenerusGameStorage.sol (confirm scorePlus1 uint16@6, amount uint24@8)
  </read_first>
  <action>
    Migrate the read offsets: `OFF_SCOREPLUS1 = 7` -> `6`; `OFF_AMOUNT = 9` -> `8` (uint24); `OFF_LASTBOUGHT = 21` -> `11`; `OFF_LASTOPENED = 25` -> `14`; add/correct the afking-marker + accumulator + latch offsets to the canonical block. Leave `OFF_DAILY = 0` and `OFF_VALIDTHROUGH = 1` UNCHANGED (those bytes did not move). Change day-marker `_subField(..., 32)` reads to width `24`. Migrate `_setScorePlus1` (lines ~473-477) to mask byte 6 (`<< (6*8)`) with a uint16 mask. The set-mutation / swap-pop / two-path / no-orphan LOGIC stays byte-identical — layout migration only. This file is the ADAPT-source for the SEC-01 two-path + swap-pop arms (356-03) and the QST-04 fixture (356-05), so it MUST be fully green on the v56 layout.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-path 'test/fuzz/V55SetMutationOpenE.t.sol' 2>&1 | grep -E "Suite result|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    File contains `OFF_SCOREPLUS1 = 6`, `OFF_AMOUNT = 8`, `OFF_LASTBOUGHT = 11`, and still contains `OFF_DAILY = 0` + `OFF_VALIDTHROUGH = 1` (unchanged); `grep -n 'OFF_SCOREPLUS1 *= *7\|OFF_AMOUNT *= *9\|OFF_LASTBOUGHT *= *21' test/fuzz/V55SetMutationOpenE.t.sol` returns empty; `_setScorePlus1` masks `(OFF_SCOREPLUS1 * 8)` with `OFF_SCOREPLUS1 == 6`; the swap-pop/two-path arms run without stale-layout garbage reds; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>V55SetMutationOpenE reads AND writes the v56 Sub slot; OFF_DAILY/OFF_VALIDTHROUGH preserved; the _setScorePlus1 mask targets byte 6; logic unchanged; valid ADAPT template; ContractAddresses.sol restored.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (356 IS the security proof)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test write-helper -> shipped Sub slot | A stale write-mask (`_setStamp`/`_setScorePlus1`) composes the OLD packed layout and CORRUPTS adjacent v56 fields when it writes — producing both false greens AND spurious reds, and poisoning the Wave-1 ADAPT templates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-02-WM | Tampering (test integrity) | _setStamp / _setScorePlus1 write masks | mitigate | Migrate read offsets AND write masks in lockstep (the uint96 amount mask -> uint24@8; scorePlus1 byte7 -> byte6); a partial migration corrupts the stamp write and the adapted suites inherit it. |
| T-356-02-AT | Tampering (template integrity) | the 3 ADAPT-source files | mitigate | These are the SEC-01/SEC-02/QST-04 templates; they MUST be fully green on v56 before Wave 1 adapts them (the depends_on ordering enforces this). |
| T-356-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `forge build` EXIT 0 after the migration.
- No `(uint256(1) << 96)` amount mask remains in any of the 3 files.
- The 3 proofs run without stale-layout garbage-read reds (remaining reds recorded PRE-EXISTING for the 356-07 union).
- ZERO `contracts/*.sol` mutation (ContractAddresses.sol restored each round-trip).
</verification>

<success_criteria>
- All 3 v55-proof files at `OFF_LASTBOUGHT = 11`/uint24, `OFF_AMOUNT = 8`/uint24, `OFF_SCOREPLUS1 = 6`/uint16.
- The write-mask helpers (`_setStamp`, `_setScorePlus1`) target the v56 offsets/widths.
- `grep -rn 'OFF_LASTBOUGHT *= *21\|OFF_AMOUNT *= *9\|(uint256(1) << 96)' test/fuzz/V55*.t.sol` returns empty.
- No test logic changed; no contract file touched; the 3 files are valid Wave-1 ADAPT templates.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-02-SUMMARY.md` when done
</output>
