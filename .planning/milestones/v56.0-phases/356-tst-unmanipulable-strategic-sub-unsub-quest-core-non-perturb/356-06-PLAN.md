---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gas/V56AfkingGasMarginal.t.sol
autonomous: true
requirements: [LIVE-01, GAS-06]
must_haves:
  truths:
    - "The harness SUBSCRIBER_CAP is 1000 (matching the shipped GameAfkingModule:165), not the stale 500"
    - "Each advanceGame tx in a worst-case multi-day VRF-stall resume is < 16,777,216 INDIVIDUALLY (the gap-backfill advance N AND the jackpot advance N+1)"
    - "advance N sets STAGE_GAP_BACKFILLED + pays no jackpot; advance N+1 pays the jackpot with the same frozen word; rngGate returns gapDays==0 on re-entry; purchaseStartDay bumped exactly once; dailyIdx not advanced so advanceDue stays true"
    - "openBoxes(maxCount) drains EITHER box type in <16.7M chunks, afking-first-then-human, both cursors advance; lastOpenedDay monotone no-double-open; drainAfkingBoxes reachable only via the openBoxes delegatecall"
    - "D-08: LIVE-01 openBoxes valve proven (drain + bound + two-path coexist + individual-paths byte-unchanged + selector-isolation); D-07: the decouple full idempotent-resume invariants"
    - "The GAS-01..04 per-buy/per-open marginals are re-asserted against a recorded LOOSE ceiling bound; the 4 proof residuals are empirically pinned"
  artifacts:
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "per-tx gap-resume ceiling + GAS-06 decouple + LIVE-01 valve + D-06 residuals + D-09 regression locks"
      contains: "SUBSCRIBER_CAP = 1000"
  key_links:
    - from: "the per-advance ceiling assertion"
      to: "the decouple break (DegenerusGameAdvanceModule:369-372) + rngGate idempotency (:1262/:1271/:1284)"
      via: "advanceGame gas measured per-tx; STAGE_GAP_BACKFILLED observed; gapDays==0 on re-entry"
      pattern: "16_777_216|STAGE_GAP_BACKFILLED"
    - from: "the openBoxes valve test"
      to: "DegenerusGame.openBoxes:1800 (afking-first delegatecall then _openHumanBoxes:1829)"
      via: "openBoxes(maxCount) drains both cursors; drainAfkingBoxes selector-isolated"
      pattern: "openBoxes|drainAfkingBoxes"
---

<objective>
EXTEND `test/gas/V56AfkingGasMarginal.t.sol` (per D-09 — do NOT author a parallel new gas suite) to land: (1) the REQUIRED harness fix `SUBSCRIBER_CAP` 500 -> 1000 (Pitfall 1, a HARNESS BUG that 2x under-states the worst-case STAGE/open chunk); (2) D-06 — a worst-case multi-day VRF-stall resume asserting EACH `advanceGame` tx is `< 16,777,216` INDIVIDUALLY (the gap-backfill advance N AND the jackpot advance N+1), plus empirically pinning the 4 proof residuals; (3) GAS-06/D-07 — the full idempotent-resume invariants for the gap/jackpot decouple; (4) LIVE-01/D-08 — the `openBoxes` valve (drain + bound + coexist + selector-isolation); (5) D-09 — re-assert the GAS-01..04 marginals as LOOSE-bound regression locks. Reuse the file's own marginal idiom, the VRF-drain harness, and the dual-bound helpers.

Purpose: GAS-06 closes the Codex-found protocol-forced 16.7M composition breach (gap-backfill + jackpot in one tx). LIVE-01 proves the unified `openBoxes` valve clears any backlog. D-06 turns the proof's per-tx gap-resume ESTIMATE (~15.8M) into an empirical per-advance assertion at the real cap of 1000.
Output: the extended gas harness with the per-tx ceiling, the decouple regression, the valve proof, and the regression locks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md
@audit/PROOF-V56-16P7M-GAS-CEILING.md

<interfaces>
<!-- Shipped surface under test (read-only; verified anchors). -->

DegenerusGameAdvanceModule.sol:
  the decouple break :369-372  if (gapDays != 0) { stage = STAGE_GAP_BACKFILLED; break; }  (STAGE_GAP_BACKFILLED = 12 @:81)
  rngGate(...) returns(word, gapDays) :1262
    idempotency :1271  if (rngWordByDay[day] != 0) return (rngWordByDay[day], 0);
    gap guard   :1284  if (day > idx + 1 && rngWordByDay[idx + 1] == 0) { ... purchaseStartDay += gapCount; gapDays = gapCount; }
  SUB_STAGE_WEIGHT_BUDGET = 1000 :166 (lootbox=1 / ticket=8 / evict=2); processTicketBatch write-budget 550

GameAfkingModule.sol:
  SUBSCRIBER_CAP = 1000 :165  (the harness has stale 500 @:127 — FIX IT; the contract's :499/:505 are stale COMMENTS only)
  drainAfkingBoxes(uint256 count) returns(opened) :1234 -> _autoOpen  (reached ONLY via the openBoxes delegatecall)
  mintBurnie() :1193 -- the rewarded one-category router; _autoOpen(OPEN_BATCH) + one CEI-last bounty creditFlip (byte-unchanged)
  OPEN_BATCH = 130 :212; the cachedDay/cachedWord short-circuit :1157-1163 (mixed-day defeats it)
  the afking-open skip :1154 if (sub.lastOpenedDay >= stampDay) continue; (lastOpenedDay monotone no-double-open)

DegenerusGame.sol:
  openBoxes(uint256 maxCount) returns(opened) :1800  -- drainAfkingBoxes(maxCount) FIRST (delegatecall),
    then if (openedAfking < maxCount) opened = _openHumanBoxes(maxCount - openedAfking); opened += openedAfking;
  _openHumanBoxes(maxCount) :1829 -- human leg; rngLock+liveness entry-gate, orphan-index skip, uniform O(1)

The harness assets to REUSE (all in V56AfkingGasMarginal.t.sol):
  the offset block :68-89; the MARGINAL idiom (vm.snapshotState/vm.revertToState) :181-208;
  the VRF-drain harness _settleGame/_settleClean/_fulfillPending :579-614; _warpToBoundary :477-487 (explicit accumulating t);
  the dual-bound helpers GAS_TARGET=10_000_000 / EFFECTIVE_GAS_CEILING=16_700_000 / _maxSafeBatch :98-103,164-168;
  SETTLE_PERIOD :124 is a harness-local warp helper ONLY (there is no settle day in the shipped code).

The 4 D-06 residuals to pin (audit/PROOF-V56-16P7M-GAS-CEILING.md residual list):
  R1 STAGE weight-model fidelity: a level-crossing pass refresh-or-evict iter AND a gap-resumed streak rebase iter, each true per-iter gas <= its weight allocation, inside a SATURATED weight chunk.
  R2 processTicketBatch single-entry worst case: the heaviest single entry (take up to maxT at a full budget) at the cap.
  R3 mixed-stamp-day OPEN_BATCH: 130 boxes spanning 130 DISTINCT stamp days (defeats the cachedDay/cachedWord short-circuit :1157-1163).
  R4 heaviest reachable per-iter state: max streak hand-back / level crossing vs the fixture's 5-ETH-sub + deity-pass states.

The hard per-tx bar is 16,777,216 (EIP-7825); the harness EFFECTIVE_GAS_CEILING constant is 16,700,000. Use the EIP value 16_777_216 for the D-06 per-advance asserts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SUBSCRIBER_CAP (500->1000) + the per-tx gap-resume ceiling (D-06) + the GAS-06 decouple idempotent-resume invariants (D-07)</name>
  <read_first>
    - test/gas/V56AfkingGasMarginal.t.sol (the WHOLE file: :68-89 offsets, :98-103/:164-168 dual-bound, :124-127 SETTLE_PERIOD+SUBSCRIBER_CAP, :181-208 marginal idiom, :477-487 warp, :579-614 VRF-drain)
    - contracts/modules/DegenerusGameAdvanceModule.sol:81,166,347-372,1262,1271,1284 (STAGE_GAP_BACKFILLED, the weight budget, the decouple break, rngGate idempotency + gap guard)
    - contracts/modules/GameAfkingModule.sol:165 (the shipped SUBSCRIBER_CAP=1000)
    - audit/PROOF-V56-16P7M-GAS-CEILING.md (the GAS-06 composition breach + the per-tx estimate the harness makes empirical)
  </read_first>
  <action>
    Correct line 127 SUBSCRIBER_CAP=500 to 1000 and update its comment to reference the shipped :165 (and re-derive any existing "at the cap" assertion against 1000 — a 2x worst-case correction). Add a dedicated heavy-state method (its own setup, NOT a cheap marginal) driving a worst-case multi-day VRF-stall resume at the real cap: a 120-day gap backfill (advance N) followed by the up-to-305-winner jackpot (advance N+1). Bracket EACH advanceGame tx SEPARATELY (gasleft before/after a single advanceGame call) and assert EACH less than 16_777_216 (NOT the ~25M total). Then assert the D-07 idempotent-resume invariants: advance N sets the stage to STAGE_GAP_BACKFILLED (=12) and pays NO jackpot that tx; advance N+1 pays the day's jackpot with the SAME frozen rngWordByDay word; on re-entry rngGate returns gapDays==0 (probe via rngWordByDay[day] != 0); dailyIdx NOT advanced so advanceDue() stays true between N and N+1; purchaseStartDay bumped EXACTLY ONCE across the resume; no double jackpot, no skipped day. Budget block_gas_limit=30e9 (already foundry.toml:16). Reuse _settleGame/_fulfillPending for the drive; reuse _warpToBoundary for the multi-day stall. Lean comments only; no logic change to the existing marginal tests.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56AfkingGasMarginal --match-test "GapResume|PerAdvance|Ceiling|Decouple|Idempot|GapBackfill" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `grep -n 'SUBSCRIBER_CAP *= *1000' test/gas/V56AfkingGasMarginal.t.sol` matches (the stale 500 is gone); the per-tx test brackets advance N and advance N+1 separately and asserts EACH `< 16_777_216`; the decouple test asserts STAGE_GAP_BACKFILLED on advance N, no jackpot on N, jackpot-with-same-word on N+1, gapDays==0 on re-entry, purchaseStartDay bumped exactly once; `forge test --match-contract V56AfkingGasMarginal` exits 0; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>SUBSCRIBER_CAP is 1000; each advanceGame tx in the worst-case resume is < 16,777,216; the gap/jackpot decouple is proven idempotent (defer-then-next-pays, no double jackpot, no skipped day); no contract touched.</done>
</task>

<task type="auto">
  <name>Task 2: The 4 D-06 residuals + the LIVE-01 openBoxes valve (drain/bound/coexist/selector-isolation) + the D-09 GAS-01..04 regression locks</name>
  <read_first>
    - test/gas/V56AfkingGasMarginal.t.sol (the marginal idiom :181-208, _settleClean :594-602, the dual-bound helpers; the existing per-buy/per-open marginal tests to add LOOSE-bound asserts onto)
    - test/gas/KeeperOpenBoxWorstCaseGas.t.sol (the open worst-case analog — already uint24-migrated; reference for the open-leg shape if a case lands there instead)
    - contracts/DegenerusGame.sol:1800-1819,1829 (openBoxes valve: afking-first delegatecall then _openHumanBoxes remainder; the human leg)
    - contracts/modules/GameAfkingModule.sol:1146-1163,1234,1193,212 (drainAfkingBoxes -> _autoOpen, the cachedDay/cachedWord short-circuit, mintBurnie byte-unchanged, OPEN_BATCH=130, the lastOpenedDay skip :1154)
    - audit/PROOF-V56-16P7M-GAS-CEILING.md (the 4 residuals R1-R4)
  </read_first>
  <action>
    Add the 4 D-06 residual cases (read R1-R4 from the proof): R1 — force a level-crossing pass refresh-or-evict iteration AND a gap-resumed streak rebase iteration INSIDE a saturated weight chunk; assert each true per-iter gas <= its assigned weight allocation. R2 — measure the heaviest single processTicketBatch entry (take up to maxT at a full write-budget) at the cap and assert it is bounded. R3 — measure an OPEN_BATCH of 130 boxes spanning 130 DISTINCT stamp days (each box re-reads rngWordByDay, defeating the cachedDay/cachedWord short-circuit) and assert the chunk is < the ceiling. R4 — re-measure the per-iter marginals at the heaviest reachable state (max streak hand-back / level crossing) vs the fixture's 5-ETH-sub+deity-pass states. Add the LIVE-01 valve cases: (a) afking-first ordering — populate both backlogs, call openBoxes(maxCount), assert the human leg consumes ONLY maxCount - openedAfking; (b) repeated bounded openBoxes calls advance BOTH _subOpenCursor (slot 70 byte 2) AND boxCursor until both backlogs DRAIN (no stuck box); (c) each openBoxes(maxCount) chunk < 16_777_216; (d) lastOpenedDay monotone no-double-open (the skip :1154); (e) selector isolation — calling drainAfkingBoxes directly on the module address hits empty storage (reached only via the Game delegatecall); (f) individual openLootBox(player,index) + the rewarded mintBurnie open path byte-unchanged (assert the materialized box matches the valve path). Add the D-09 regression locks: re-assert the existing per-buy (lootbox ~130-140k; ticket off the ~262k purchaseWith heavyweight) + per-open marginals against a RECORDED LOOSE ceiling bound (a generous ceiling, not a brittle exact number) so a future regression fails the gate. Reuse the MARGINAL idiom (vm.snapshotState/vm.revertToState) for every gas number; _settleClean before any mintBurnie open-leg measure. Lean comments only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56AfkingGasMarginal --match-test "Residual|LevelCross|TicketEntry|MixedDay|HeaviestState|OpenBoxes|Valve|Drain|Coexist|Selector|RegressionLock|Marginal" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56AfkingGasMarginal` exits 0 with the 4 residual cases (R1 level-cross/gap-rebase weight-fidelity, R2 heaviest ticket entry, R3 mixed-130-day open, R4 heaviest-state marginal) all asserting bounds; the openBoxes valve cases assert afking-first ordering + both-cursors-drain + each-chunk < 16_777_216 + lastOpenedDay-monotone + drainAfkingBoxes-selector-isolated + mintBurnie/openLootBox byte-unchanged; the per-buy/per-open marginals assert against a recorded LOOSE ceiling bound; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>The 4 proof residuals are empirically pinned; the openBoxes valve drains either box type in <16.7M chunks afking-first with both cursors advancing and selector isolation; the individual open path is byte-unchanged; the GAS-01..04 marginals are locked against a loose ceiling; no contract touched.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (LIVE-01 + GAS-06 + the 16.7M ceiling)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third party -> advanceGame chain (protocol-forced loops) | A multi-day VRF stall must not force a single advanceGame tx over the 16.7M EIP-7825 per-tx ceiling (a gas-DoS that bricks the daily advance). |
| afking-open path <-> human-open path (shared Sub slot) | The two open routes share the Sub record; one must not corrupt or double-open the other. |
| caller -> openBoxes valve (permissionless) | The valve must drain any backlog in bounded chunks without a stuck box, and drainAfkingBoxes must not be a re-exposed standalone selector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-06-COMP | Denial (gas-DoS) | the gap-backfill + jackpot composition | mitigate | GAS-06 decouple :369-372: defer the jackpot to advance N+1; rngGate idempotent; dailyIdx not advanced. Assert EACH advanceGame tx < 16_777_216 (D-06) + the idempotent-resume invariants (D-07). |
| T-356-06-CEIL | Denial (gas-DoS) | the STAGE / open / ticket worst-case chunks | mitigate | Pin the 4 proof residuals (R1-R4) at the REAL cap of 1000 (SUBSCRIBER_CAP fix); each chunk bounded < the ceiling. |
| T-356-06-2PATH | Tampering | the two-path open coexistence | mitigate | LIVE-01: lastOpenedDay monotone no-double-open (:1154); independent cursors (_subOpenCursor vs boxCursor); afking-first-then-human budget split; assert both drain. |
| T-356-06-SEL | Elevation | drainAfkingBoxes selector | mitigate | drainAfkingBoxes reachable ONLY via the openBoxes delegatecall (no autoOpen collision — the dead autoOpen dropped in 86a2d6c8). Assert direct call hits empty storage. |
| T-356-06-BYTE | Tampering | individual openLootBox / mintBurnie open | mitigate | The individual + rewarded open path byte-unchanged; assert the materialized box matches the valve path. |
| T-356-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `forge build` EXIT 0.
- `forge test --match-contract V56AfkingGasMarginal` green (the existing marginals + the per-tx ceiling + the decouple + the 4 residuals + the valve + the regression locks).
- `grep -n 'SUBSCRIBER_CAP *= *1000' test/gas/V56AfkingGasMarginal.t.sol` matches; the stale 500 is gone.
- ZERO `contracts/*.sol` mutation (ContractAddresses.sol restored each round-trip).
</verification>

<success_criteria>
- SUBSCRIBER_CAP is 1000 (the harness bug fixed).
- Each advanceGame tx in the worst-case multi-day VRF-stall resume is < 16,777,216 individually (gap-backfill N AND jackpot N+1).
- The GAS-06 decouple idempotent-resume invariants hold (defer-then-next-pays, no double jackpot, no skipped day).
- The openBoxes valve drains either box type in <16.7M afking-first chunks with both cursors advancing + selector isolation + byte-unchanged individual open.
- The 4 proof residuals are empirically pinned; the GAS-01..04 marginals are locked against a loose ceiling.
- No contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-06-SUMMARY.md` when done
</output>
