---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 03
type: execute
wave: 1
depends_on: ["356-02"]
files_modified:
  - test/fuzz/V56SecUnmanipulable.t.sol
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "A random sub/unsub/buy/claim/open churn sequence credits no more BURNIE/affiliate than honest continuous play"
    - "affiliateBase persists across unsub — churn forfeits nothing and gains nothing (total drained == honest continuous accrual)"
    - "Missing one funded day reads effective streak 0 (decay-on-read); the per-window streak advances only on debit-delivered days"
    - "claimAfkingBurnie pays the accrued pendingBurnie EXACTLY ONCE (CEI zero-before-credit), idempotent under double-call / claim->unsub->claim"
    - "Each of the 4 sub-ending paths writes the decay-applied final streak to DegenerusQuests BEFORE the slot is deleted/tombstoned; funding-kill zeroes only when a full prior day was missed with no valid mint"
  artifacts:
    - path: "test/fuzz/V56SecUnmanipulable.t.sol"
      provides: "SEC-01 stateful churn-fuzz invariants + the 4 named repros"
      contains: "contract V56SecUnmanipulable"
      min_lines: 200
  key_links:
    - from: "the churn-fuzz invariant"
      to: "affiliateBase / pendingBurnie / the streak latch in the Sub slot"
      via: "_subField(who, OFF_AFFBASE/OFF_PENDINGBURNIE/OFF_STREAKLATCH, ...)"
      pattern: "OFF_AFFBASE|OFF_PENDINGBURNIE|OFF_STREAKLATCH"
    - from: "claimAfkingBurnie double-call repro"
      to: "the CEI zero-before-credit at GameAfkingModule:1277"
      via: "s.pendingBurnie = 0 precedes coinflip.creditFlip"
      pattern: "claimAfkingBurnie"
---

<objective>
Author `test/fuzz/V56SecUnmanipulable.t.sol` — the SEC-01 PRIMARY proof: the afking system (buy + open) is unmanipulable via strategic sub/unsub churn. Per D-01, prove the property with BOTH a stateful property-fuzz invariant AND all FOUR named repro tests (D-02). The shipped streak is compute-on-read with decay (NO settle day); reward is the per-day `pendingBurnie` pulled via the CEI `claimAfkingBurnie`. ADAPT the now-v56-migrated `V55RevertFreeEvCap` + `V55SetMutationOpenE` (fuzz+repro structure, solvency reads, swap-pop/two-path/no-orphan arms); copy the v56 offset block + the SEC-01 probe accessors from `V56AfkingGasMarginal:68-89,631-649`.

Purpose: SEC-01 is the USER-flagged PRIMARY concern. The four designed-against vectors (affiliate re-claim churn; streak decay/gap dodge; pendingBurnie double-claim; the 4 finalize hooks before delete) each become a legible "this exact vector is closed" regression.
Output: one new forge fuzz suite proving SEC-01 empirically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md

<interfaces>
<!-- Shipped surface under test (read-only; verified anchors). Assert behavior through these. -->

GameAfkingModule.sol:
  subscribe(player, drainFirst, useTickets, dailyQuantity, reinvestPct, fundingSource) payable :255
    -- dailyQuantity == 0 is the explicit cancel; FREEZE-01 blocks during rngLock
  affiliateBase: accrue at :753-755 (sub.affiliateBase = uint32(newBase)); persists across unsub (:315 comment);
    read-and-zero AFFILIATE-only at :1303-1304 (base = s.affiliateBase; s.affiliateBase = 0)
  _afkingStreak(sub, currentDay) :778-786  -- decay-on-read: `if (currentDay==0 || covered+1 < currentDay) return 0;`
    else `_streakBaseOf(sub) + (covered - afkingStartDay)`  (NOTE: the read fn is _afkingStreak, NOT _streakOf — Pitfall 3)
  claimAfkingBurnie(address[] subs) :1270  -- CEI: `s.pendingBurnie = 0;` at :1277 precedes `coinflip.creditFlip`
  _finalizeAfking(player, sub, currentDay) :799-816  -- computes earned = streakBase + (covered - afkingStartDay),
    calls quests.finalizeAfking(...), then zeroes afkingStartDay + streak base
  The 4 finalize hooks (ALL call _finalizeAfking BEFORE delete/tombstone):
    (A) explicit cancel subscribe(_,0):  :318 _finalizeAfking(...)  then :319 c.dailyQuantity = 0;
    (B) cancel-reclaim (load-bearing):    :912 _finalizeAfking(...)  then :915 delete _subOf[player]; :916 _removeFromSet
    (C) pass-eviction crossing:           :952 _finalizeAfking(...)  then :953 sub.dailyQuantity = 0; :954 _removeFromSet
    (D) funding-kill (NORMAL underfunded): :1010 _finalizeAfking(...) then :1011 sub.dailyQuantity = 0; :1012 _removeFromSet
  no-orphan guard :876-881 (a pending unopened-box sub `lastOpenedDay < lastAutoBoughtDay` left ENTIRELY untouched)

DegenerusQuests.sol:
  finalizeAfking(player, uint24 earnedStreak, uint32 afkingCoveredDay, uint32 currentDay) :463 onlyGame
    -- idempotent (`if (!state.afkingActive) return;`); funding-kill guard :474
       `lastValid = max(afkingCoveredDay, state.lastActiveDay); finalStreak = (currentDay==0 || lastValid+1 >= currentDay) ? earned : 0`
    -- Pitfall 4: the guard is `lastValid + 1 >= currentDay`, NOT `<= currentDay-2`; a sub that lapsed afking but kept
       minting MANUALLY (bumps lastActiveDay) is NOT wrongly zeroed (D-02.4).

The v56 Sub-slot offset block + SEC-01 probe accessors (copy from V56AfkingGasMarginal:68-89, 631-649):
  SUBOF_SLOT=66; OFF_AFFBASE=23(u32), OFF_PENDINGBURNIE=27(u32), OFF_STREAKLATCH=31(u8 & 0x7f),
  OFF_AFKCOVERED=17(u24), OFF_AFKINGSTART=20(u24), OFF_LASTBOUGHT=11(u24), OFF_LASTOPENED=14(u24)
  _affiliateBaseOf / _pendingBurnieOf (width 32) / _streakBaseOf (byte31 & 0x7f) / _afkCoveredOf / _afkingStartOf (width 24)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stateful churn-fuzz invariants + repro 1 (affiliate re-claim churn) + repro 2 (streak decay/gap dodge)</name>
  <behavior>
    - Invariant (fuzz {sub,unsub,buy,claim,open}): total credited BURNIE + total affiliate drained over ANY churn sequence <= the honest continuous-sub accrual over the same day span.
    - Invariant: the effective streak read never exceeds the funded-delivered-day span (never credits a non-delivered day).
    - Repro 1 (affiliate re-claim churn): sub -> accrue affiliateBase -> unsub -> re-sub repeatedly; the total drained to uplines EQUALS honest continuous accrual (affiliateBase persists across unsub: forfeit-nothing-gain-nothing).
    - Repro 2 (streak decay/gap dodge): miss ONE funded day -> _afkingStreak reads 0 (decay `covered+1 < currentDay`); resume after a gap -> afkingStartDay/streakAtAfkingStart reset on the delivered day (no stale-span credit); the per-window streak advances ONLY on debit-delivered days (the C3-a non-funded dodge stays closed).
  </behavior>
  <read_first>
    - test/fuzz/V55RevertFreeEvCap.t.sol (the fuzz+repro structure, the solvency/accumulator reads — NOW v56-migrated by 356-02)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89,579-614,631-649 (the offset block, the _settleGame VRF-drain harness, the SEC-01 probe accessors)
    - test/fuzz/helpers/DeployProtocol.sol (the fixture every proof inherits)
    - contracts/modules/GameAfkingModule.sol:255,315,753-755,778-786,1270-1284,1303-1304 (subscribe, affiliateBase persist+accrue+drain, _afkingStreak decay, claimAfkingBurnie)
  </read_first>
  <action>
    Create `test/fuzz/V56SecUnmanipulable.t.sol` (contract `V56SecUnmanipulable is DeployProtocol`). Copy the v56 offset block + the probe accessors from V56AfkingGasMarginal verbatim (NOT the v55 offsets). Reuse the funded-sub + deity-pass + new-day STAGE harness from the migrated V55SetMutationOpenE (`_runStageNewDay`, `_subscribeLootbox`, `_fundPool`, `_grantDeityPass`) and the `_settleGame`/`_fulfillPending`/`_warpToBoundary` VRF-drain helpers. Write a stateful fuzz invariant driving random {sub, unsub via subscribe(_,0), buy, claimAfkingBurnie, openBoxes} actions and asserting (a) cumulative affiliate-drained + BURNIE-credited <= honest continuous accrual baseline, (b) the effective streak (probed via the activity-score read / finalize path + the latch byte _streakBaseOf) never exceeds the funded-delivered-day count. Write repro 1 (sub->accrue->unsub->re-sub loop; assert total drained == honest continuous via the AFFILIATE-only drain path, probing affiliateBase = _subField(who, OFF_AFFBASE, 32)). Write repro 2 (decay/gap dodge: warp past one funded day, assert the streak reads 0 via the finalize write / latch; resume and assert afkingStartDay resets, probing OFF_AFKINGSTART width 24). Carry the unseeded-invariant ⊆ caveat (Pitfall 5) — assertions must be seeded-fuzz-deterministic (`[fuzz] seed=0xdeadbeef`). No fenced code in the test comments beyond functional explanation (lean-comments rule).
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56SecUnmanipulable --match-test "Churn|Affiliate|Decay|Streak" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56SecUnmanipulable --match-test "Churn|Affiliate|Decay|Streak"` exits 0 with the churn invariant + repro 1 + repro 2 all PASS; the affiliate-churn repro asserts `totalDrained == honestContinuousAccrual` (an exact equality, not an inequality); the decay repro asserts the streak reads 0 after one missed funded day; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>The churn-fuzz invariant + the affiliate-persistence + streak-decay repros pass; no positive-EV from churn; no contract touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repro 3 (pendingBurnie double-claim CEI idempotency) + repro 4 (4 finalize hooks before delete) + no-orphan arm</name>
  <behavior>
    - Repro 3 (double-claim idempotency): claimAfkingBurnie double-call in one block / claim->unsub->claim pays the accrued pendingBurnie EXACTLY ONCE; pendingBurnie zeroed CEI-before-credit (the second call sees 0 owed, credits nothing).
    - Repro 4 (4 finalize hooks): for EACH ending path (explicit cancel subscribe(_,0); cancel-reclaim that deletes _subOf; pass-eviction crossing; funding-kill), the decay-applied final streak is written to DegenerusQuests BEFORE the slot is deleted/tombstoned.
    - Funding-kill boundary: lastValid == currentDay-1 (delivered yesterday OR a manual mint bumped lastActiveDay) -> streak KEPT; lastValid <= currentDay-2 (full prior day missed, no valid mint) -> streak ZEROED.
    - No-orphan: a sub with a pending unopened box (lastOpenedDay < lastAutoBoughtDay) is left ENTIRELY untouched by the STAGE (no reclaim/evict/funding-kill/re-stamp).
  </behavior>
  <read_first>
    - test/fuzz/V55SetMutationOpenE.t.sol (the swap-pop streak arm ~213-266, the no-orphan control/removed pair ~124-169, the event-drain helper ~491-508 — NOW v56-migrated by 356-02)
    - contracts/modules/GameAfkingModule.sol:318,799-816,876-881,912-916,952-954,1010-1012,1270-1284 (the 4 hooks, _finalizeAfking, the no-orphan guard, the CEI claim)
    - contracts/DegenerusQuests.sol:463-483 (finalizeAfking idempotency + the funding-kill guard :474 `lastValid+1 >= currentDay`)
    - test/gas/V56AfkingGasMarginal.t.sol:579-614 (the _settleGame harness to drive the STAGE that triggers cancel-reclaim/pass-evict/funding-kill)
  </read_first>
  <action>
    Extend `V56SecUnmanipulable.t.sol`. Repro 3: accrue a known pendingBurnie, call `claimAfkingBurnie([who])` twice in one block (and a claim->unsub->claim variant); assert the coinflip credit fired exactly once (capture via the credit event or the recipient's flip balance delta) and the second call is a no-op; probe pendingBurnie = _subField(who, OFF_PENDINGBURNIE, 32) reads 0 after the first. Repro 4: drive each of the 4 ending paths and assert `quests.finalizeAfking` ran with the decay-applied earned streak BEFORE the slot delete/tombstone — for (B) cancel-reclaim use the event-drain helper to confirm ordering (finalize precedes `delete _subOf`); for (D) funding-kill write BOTH boundary cases explicitly (Pitfall 4): `lastValid == currentDay-1` -> kept (incl. the manual-mint-keeps-alive case bumping lastActiveDay) AND `lastValid <= currentDay-2` -> zeroed. Add the no-orphan arm (copy the control/removed pair): a pending-box sub is left untouched across a STAGE cycle. Carry the seeded-fuzz determinism. No fenced code in comments (lean rule).
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56SecUnmanipulable --match-test "DoubleClaim|Idempot|Finalize|FundingKill|Orphan|Cancel|Evict" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56SecUnmanipulable --match-test "DoubleClaim|Idempot|Finalize|FundingKill|Orphan|Cancel|Evict"` exits 0; the double-claim repro asserts EXACTLY ONE credit fired (the second call credits 0); all 4 finalize-hook cases assert finalize-before-delete; the funding-kill test has BOTH the kept (`lastValid+1 >= currentDay`, incl. manual-mint) and zeroed (`lastValid <= currentDay-2`) boundary cases; the no-orphan arm passes; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>All 4 named repros pass; double-claim is idempotent (CEI); the 4 finalize hooks run before delete; the funding-kill boundary is asserted both ways; no-orphan holds; no contract touched.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (SEC-01 — the PRIMARY attack matrix)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player -> GameAfkingModule (permissionless sub/unsub/claim) | An untrusted player drives subscribe/cancel/claim sequences attempting positive-EV via churn, decay-dodge, or double-settlement. |
| GameAfkingModule (Sub slot) -> DegenerusQuests (finalize write) | The afking streak state lives in the Sub slot and is written back across the contract boundary on the 4 ending paths — a missed finalize-before-delete would lose or duplicate streak credit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-03-CHURN | Elevation (econ) | subscribe / unsub / affiliateBase | mitigate | Repro 1 + the churn invariant: affiliateBase PERSISTS across unsub (not flushed on mutation, :315); the AFFILIATE-only drain is read-and-zero -> churn neither forfeits nor duplicates. Assert total drained == honest continuous. |
| T-356-03-GAP | Tampering | _afkingStreak decay-on-read | mitigate | Repro 2: decay `covered+1 < currentDay -> 0` (:784); streak advances ONLY on debit-delivered days; gap-reset-on-resume. Assert the streak reads 0 after a missed funded day. |
| T-356-03-DBL | Tampering | claimAfkingBurnie | mitigate | Repro 3: CEI zero-before-credit (:1277); creditFlip recordAmount makes reentrancy a non-issue. Assert pays EXACTLY ONCE. |
| T-356-03-FIN | Tampering | the 4 finalize hooks | mitigate | Repro 4: each ending path writes the decay-applied streak BEFORE delete (:318/:912/:952/:1010); funding-kill guard `lastValid+1 >= currentDay` (:474). Assert finalize-before-delete + both guard boundaries. |
| T-356-03-ORPH | Denial | the no-orphan STAGE guard | mitigate | The no-orphan arm: a pending-box sub (:876-881) is left untouched (no reclaim/evict/funding-kill/re-stamp). |
| T-356-03-D03 | (accepted by design) | first-sub `+0..+9` head-start (QST-02) | accept | D-03: USER-ACCEPTED-BY-DESIGN; the activity-score still reads the actual state.streak. Do NOT flag as a missed control. |
| T-356-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `forge build` EXIT 0.
- `forge test --match-contract V56SecUnmanipulable` green (the churn invariant + all 4 repros + the no-orphan arm).
- ZERO `contracts/*.sol` mutation (ContractAddresses.sol restored each round-trip).
</verification>

<success_criteria>
- The stateful churn-fuzz invariant proves no churn sequence beats honest continuous accrual.
- All 4 named repros (affiliate re-claim churn; streak decay/gap dodge; pendingBurnie double-claim CEI; the 4 finalize hooks before delete) pass.
- The funding-kill boundary is asserted both kept and zeroed (incl. the manual-mint-keeps-alive case).
- D-03 head-start is treated as accepted, not flagged.
- No contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-03-SUMMARY.md` when done
</output>
