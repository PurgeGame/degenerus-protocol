---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/AfKingFundingWaterfall.t.sol
  - test/fuzz/AfKingSubscription.t.sol
  - test/fuzz/KeeperRouterOneCategory.t.sol
  - test/fuzz/KeeperFaucetResistance.t.sol
  - test/fuzz/KeeperRewardRoutingSameResults.t.sol
  - test/fuzz/KeeperNonBrick.t.sol
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "The DeployProtocol forge fixture is green at setUp before the proof waves run"
    - "The 7 read-only keeper/afking fuzz files read the v56-repacked Sub slot (no garbage-read reds)"
    - "Every day-marker read in these 7 files uses width 24, not 32"
  artifacts:
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "uint24/OFF_LASTBOUGHT=11 Sub-slot probes"
      contains: "OFF_LASTBOUGHT = 11"
    - path: "test/fuzz/KeeperNonBrick.t.sol"
      provides: "uint24/OFF_LASTBOUGHT=11 Sub-slot probes"
      contains: "OFF_LASTBOUGHT = 11"
  key_links:
    - from: "the 7 migrated files"
      to: "the v56-repacked Sub slot in DegenerusGameStorage"
      via: "vm.load(game, keccak256(abi.encode(who, SUBOF_SLOT))) >> (off*8)"
      pattern: "OFF_LASTBOUGHT *= *11"
---

<objective>
Wave-0 precondition (part 1 of 2). Confirm the DeployProtocol forge fixture is green at setUp (RESEARCH Open Question 1 — the 355-CONTEXT noted it was down for vanity-address realignment; a probe this session passed 5/5 on V56AfkingGasMarginal, but the executor re-confirms first), then migrate the 7 READ-ONLY keeper/afking fuzz files from the stale `OFF_LASTBOUGHT=21`/uint32 layout to the shipped `11`/uint24 re-pack — the exact mechanical transform already applied to the gas suites in `08e59a4a`. These 7 files carry probe-only reads (no write-mask helpers), so a pure offset-constant + read-width swap is sufficient.

Purpose: D-10 mandates migrating ALL ~10 stale-offset fuzz files; this is the NON-WIDENING precondition (the `6555125 != 3774873600` garbage-read reds flip green → a NARROWING, never a widening). Splitting the 7 simple files from the 3 v55-proof files (356-02, which need write-mask migration in lockstep) keeps each plan within budget and isolates risk.
Output: 7 migrated forge test files reading the v56 Sub slot correctly; a confirmed-green fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md

<interfaces>
<!-- The CANONICAL v56 Sub-slot offset block (from V56AfkingGasMarginal.t.sol:68-89, already migrated). -->
<!-- The target layout every migrated file must match. Confirm against HEAD via `forge inspect DegenerusGame storageLayout`. -->

The v56 single-256-bit Sub slot (SUBOF_SLOT = 66):
  dailyQuantity u8 @0 · validThroughLevel u24 @1 · reinvestPct u8 @4 · flags u8 @5
  scorePlus1 u16 @6 · amount u24 @8
  lastAutoBoughtDay u24 @11 · lastOpenedDay u24 @14 · afkCoveredThroughDay u24 @17 · afkingStartDay u24 @20
  affiliateBase u32 @23 · pendingBurnie u32 @27 · subStreakLatch u8 @31

  OFF_LASTBOUGHT    = 11  (uint24, width 24)   was 21/uint32
  OFF_LASTOPENED    = 14  (uint24, width 24)   was 25/uint32
  OFF_AFKCOVERED    = 17  (uint24, width 24)
  OFF_AFKINGSTART   = 20  (uint24, width 24)
  OFF_AFFBASE       = 23  (uint32, width 32)
  OFF_PENDINGBURNIE = 27  (uint32, width 32)
  OFF_STREAKLATCH   = 31  (uint8,  byte 31; bit7 ever-sub, bits0-6 streak; mask & 0x7f)

The `08e59a4a` mechanical transform (verified at git show 08e59a4a -- test/gas/RouterWorstCaseGas.t.sol):
  OFF_LASTBOUGHT 21 -> 11 ; OFF_LASTOPENED 25 -> 14
  every `_subField(who, OFF_LASTBOUGHT, 32)` day-marker read -> `..., 24`
  any inline `p & 0xFFFFFFFF` mask on a day marker -> `p & 0xFFFFFF` (uint24)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture-sanity gate — confirm the forge fixture is green at setUp</name>
  <read_first>
    - test/gas/V56AfkingGasMarginal.t.sol (the already-migrated harness; its setUp is the canonical fixture entry)
    - test/fuzz/helpers/DeployProtocol.sol (the live deploy fixture every forge proof inherits)
    - scripts/lib/patchForFoundry.js (predicts CREATE addresses before a whole-tree run; no pretest hook)
    - .planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md (Open Question 1 + Pitfall 6: ContractAddresses.sol drift breaks the tree)
  </read_first>
  <action>
    Run `node scripts/lib/patchForFoundry.js` then `forge test --match-path 'test/gas/V56AfkingGasMarginal.t.sol' -vv`, then ALWAYS `git checkout -- contracts/ContractAddresses.sol` to restore the frozen fixture file (sha256 80fe0dac…; Pitfall 6 — drift reds the whole tree). If setUp reverts (fixture down per the 355-CONTEXT vanity-address note), STOP and repair the fixture first (the v55 351-01 precedent) before any migration. Do NOT run `hardhat compile --force` (the known landmine that regenerates ContractAddresses.sol). This task mutates NO files — it is a gate.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-path 'test/gas/V56AfkingGasMarginal.t.sol' 2>&1 | grep -E "5 passed|Suite result: ok"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-path 'test/gas/V56AfkingGasMarginal.t.sol'` reports `Suite result: ok. 5 passed; 0 failed` (setUp green); `git checkout -- contracts/ContractAddresses.sol` leaves the file byte-identical to HEAD (`git diff --quiet HEAD -- contracts/ContractAddresses.sol` exits 0).
  </acceptance_criteria>
  <done>The fixture is confirmed green at setUp; ContractAddresses.sol is restored; the proof waves are unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 7 read-only keeper/afking fuzz files to the uint24 re-pack</name>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol, test/fuzz/AfKingFundingWaterfall.t.sol, test/fuzz/AfKingSubscription.t.sol (the 3 afking probe files at stale 21/uint32)
    - test/fuzz/KeeperRouterOneCategory.t.sol, test/fuzz/KeeperFaucetResistance.t.sol, test/fuzz/KeeperRewardRoutingSameResults.t.sol, test/fuzz/KeeperNonBrick.t.sol (the 4 keeper probe files at stale 21/uint32)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89 (the CANONICAL target offset block to copy verbatim)
    - test/gas/RouterWorstCaseGas.t.sol (an already-migrated gas suite — the 08e59a4a transform applied; reference the result shape)
    - contracts/storage/DegenerusGameStorage.sol (the shipped 32-byte/13-field Sub slot — confirm offsets)
  </read_first>
  <action>
    In each of the 7 files apply the `08e59a4a` mechanical transform: change `OFF_LASTBOUGHT = 21` -> `11`, `OFF_LASTOPENED = 25` -> `14`; for any other day-marker offset constants present, set `OFF_AFKCOVERED = 17`, `OFF_AFKINGSTART = 20` (uint24); accumulators `OFF_AFFBASE = 23`, `OFF_PENDINGBURNIE = 27` (uint32); latch `OFF_STREAKLATCH = 31`. Change EVERY `_subField(who, OFF_<day-marker>, 32)` day-marker read to width `24`; change any inline `p & 0xFFFFFFFF` mask on a day marker to `p & 0xFFFFFF`. Update the inline byte-range comments to match (bytes 11..13 etc.). These 7 files are READ-ONLY probes (no `_setStamp`/`_setScorePlus1` write helpers — verify with grep first; if any file unexpectedly has a write mask, defer it to 356-02 and note it). Leave the keeper-router test LOGIC untouched — this is a layout-read fix only, never a behavior change (per the lean-comments + zero-logic-change rule). Confirm the canonical offsets via `forge inspect DegenerusGame storageLayout` against HEAD if any offset is ambiguous.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-path 'test/fuzz/AfKing*.t.sol' --match-path 'test/fuzz/Keeper*.t.sol' 2>&1 | grep -E "Suite result|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `grep -L 'OFF_LASTBOUGHT *= *11' test/fuzz/AfKingConcurrency.t.sol test/fuzz/AfKingFundingWaterfall.t.sol test/fuzz/AfKingSubscription.t.sol test/fuzz/KeeperRouterOneCategory.t.sol test/fuzz/KeeperFaucetResistance.t.sol test/fuzz/KeeperRewardRoutingSameResults.t.sol test/fuzz/KeeperNonBrick.t.sol` returns empty (all 7 migrated); `grep -rn 'OFF_LASTBOUGHT *= *21' test/fuzz/AfKing*.t.sol test/fuzz/Keeper*.t.sol` returns empty; the previously garbage-reading assertions in these files no longer red with `6555125 != 3774873600`-shaped messages (record any still-red as PRE-EXISTING for the 356-07 baseline union, NOT a new red); `git diff --quiet HEAD -- contracts/ContractAddresses.sol` exits 0.
  </acceptance_criteria>
  <done>All 7 read-only files read the v56 Sub slot at uint24/OFF_LASTBOUGHT=11; no garbage-read reds remain attributable to the stale layout; no test logic changed; ContractAddresses.sol restored.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (356 IS the security proof — the threat model is the attack matrix this plan empirically closes)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> shipped Sub slot | A stale-offset probe reads the WRONG bytes of the packed Sub slot, producing false greens (a passing test that asserts on garbage) — masking a real economic regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-01-FG | Tampering (test integrity) | the 7 stale-offset probe files | mitigate | Migrate every day-marker read to uint24/OFF_LASTBOUGHT=11 (the 08e59a4a transform); a false green (SweepPerPlayer was one) is the risk — assert the migrated reads return real day values, not `6555125 != 3774873600` garbage. |
| T-356-01-FX | Denial (fixture) | DeployProtocol fixture / ContractAddresses.sol | mitigate | Task 1 fixture-sanity gate confirms setUp green; every patch round-trip ends with `git checkout -- contracts/ContractAddresses.sol` (Pitfall 6 drift = whole-tree red). |
| T-356-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH Package Legitimacy Audit: vacuously satisfied — vendored forge-std only). |
</threat_model>

<verification>
- `forge build` EXIT 0 after the migration.
- The 7 migrated suites run without stale-layout garbage-read reds (any remaining red is recorded as PRE-EXISTING for the 356-07 baseline union, not a new regression).
- ZERO `contracts/*.sol` mutation (`git diff --quiet HEAD -- contracts/` exits 0 after every patch round-trip; ContractAddresses.sol restored each time).
</verification>

<success_criteria>
- The fixture is green at setUp (Task 1 gate passes).
- All 7 read-only keeper/afking fuzz files are at `OFF_LASTBOUGHT = 11`/uint24 with width-24 day-marker reads.
- `grep -rn 'OFF_LASTBOUGHT *= *21' test/fuzz/AfKing*.t.sol test/fuzz/Keeper*.t.sol` returns empty.
- No test logic changed (layout-read fix only); no contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-01-SUMMARY.md` when done
</output>
