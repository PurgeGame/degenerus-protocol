---
phase: 355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum
plan: 03
type: execute
wave: 3
depends_on: ["355-01", "355-02"]
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/GameAfkingModule.sol
  - test/gas/V56AfkingGasMarginal.t.sol
autonomous: false
requirements: [GAS-03, GAS-04]
user_setup: []
must_haves:
  truths:
    - "SUB_STAGE_BATCH is SET to the COMPUTED OPTIMAL (GAS-03) — the LARGEST N from 355-01's derived max-safe batch such that the settle-day STAGE chunk (the heaviest day, every sub fires _settleQuest) worst-case TARGETS < 10,000,000 at the SUBSCRIBER_CAP for the binding (heavier) of the ticket and lootbox modes, AND provably NEVER EXCEEDS 16,700,000"
    - "OPEN_BATCH is RE-CONFIRMED / RE-TUNED to its COMPUTED optimal the same way — the largest N keeping the OPEN_BATCH open chunk worst-case < 10,000,000 and provably ≤ 16,700,000 — using 355-01's measured per-open marginal"
    - "Every batched per-tx advance/afking chunk (the settle-day STAGE at the chosen SUB_STAGE_BATCH, both modes; the open chunk at the chosen OPEN_BATCH) is PROVEN < 10,000,000 (target) and provably ≤ 16,700,000 (hard ceiling) at SUBSCRIBER_CAP via the 355-01 worst-case harness re-run against the chosen values"
    - "Every skeptic-APPROVED GAS-04 mode/SLOAD-collapse change from 355-02 is landed (and ONLY those) — unmanipulability + SOLVENCY-01 byte-frozen + RNG-freeze preserved; OR if 355-02's APPROVED set is empty AND the current constants already measure as the computed optimal, Outcome-A is recorded (no net diff)"
    - "Any contract change is applied to contracts/ + forge build clean, then HELD at the contract-commit boundary for explicit USER hand-review (NEVER auto-committed)"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "the computed-optimal SUB_STAGE_BATCH constant (GAS-03) — largest N keeping the settle-day chunk < 10M and ≤ 16.7M"
      contains: "SUB_STAGE_BATCH"
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the re-confirmed / re-tuned OPEN_BATCH constant — largest N keeping the open chunk < 10M and ≤ 16.7M"
      contains: "OPEN_BATCH"
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "the settle-day STAGE-at-cap + open-chunk re-proof against the chosen SUB_STAGE_BATCH / OPEN_BATCH (dual bound: < 10M target / ≤ 16.7M ceiling)"
      contains: "GAS_TARGET"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol SUB_STAGE_BATCH"
      to: "GameAfkingModule.processSubscriberStage settle-day chunk"
      via: "_runSubscriberStage passes SUB_STAGE_BATCH to the STAGE"
      pattern: "SUB_STAGE_BATCH"
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "the chosen SUB_STAGE_BATCH / OPEN_BATCH"
      via: "settle-day full-chunk + open-chunk advance asserted < 10M target, ≤ 16.7M ceiling"
      pattern: "GAS_TARGET"
---

<objective>
Land the residual GAS tune as the v56 contract-boundary HARD STOP: SET `SUB_STAGE_BATCH` (GAS-03, the number DEFERRED from 354 to here) to the COMPUTED OPTIMAL — the largest batch keeping the settle-day STAGE chunk worst-case < 10M (the comfort target) and provably ≤ 16.7M (the hard kill ceiling) at the cap — RE-CONFIRM / RE-TUNE `OPEN_BATCH` the same way against the measured per-open marginal, apply ONLY the skeptic-APPROVED GAS-04 mode/SLOAD-collapse changes from 355-02 (if any), prove every batched per-tx chunk under the dual bound, then HOLD at the contract-commit boundary for USER hand-review. If 355-02's APPROVED set is empty AND the current constants already measure as the computed optimal (already-largest-N-under-10M with full headroom to 16.7M), record Outcome-A (no net contract diff) per the v55 Phase 350 precedent.

Purpose: `SUB_STAGE_BATCH` and `OPEN_BATCH` are the load-bearing throughput/headroom decisions. The USER constraint is now a DUAL BOUND: too large and the settle-day STAGE / open chunk breaches 16.7M (advanceGame DoS / forced game-over); too small wastes throughput / forces more advance txs. "Optimal" = the LARGEST N that still lands the worst-case chunk < 10M (the target) — which leaves the 10M→16.7M headroom as the never-exceed safety margin. These constants MUST be set from the MEASURED 355-01 per-item marginals + derived max-safe batches, not guessed. The GAS-04 collapse only ever lands what the skeptic cleared and what does not erode that headroom.

Output: the computed-optimal `SUB_STAGE_BATCH` in `DegenerusGameAdvanceModule.sol`, the re-confirmed/re-tuned `OPEN_BATCH` in `GameAfkingModule.sol` (+ any APPROVED GAS-04 edit in `GameAfkingModule.sol`), the settle-day-at-cap + open-chunk re-proof under the dual bound in the harness, and a diff-review record — applied + `forge build` clean, HELD at the contract-commit boundary. This is the ONE contract gate in Phase 355.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-SUMMARY.md

<interfaces>
<!-- The ONE Phase-355 contract gate. Per project rules feedback_batch_contract_approval / feedback_never_preapprove_contracts / feedback_no_contract_commits: a contracts/*.sol commit is the ONLY action needing approval. ContractAddresses.sol is freely modifiable; tests/docs are agent-committable. Sequential-on-main, worktrees OFF (submodule + node_modules). Pre-launch redeploy-fresh (storage break fine). -->

The DUAL BOUND (USER-LOCKED this phase): every batched per-tx loop in the daily advance / afking chain must be sized so its WORST-CASE gas TARGETS < 10,000,000 (the comfort target) AND PROVABLY NEVER EXCEEDS 16,700,000 (the hard kill ceiling — a breach = advanceGame DoS / forced game-over). "Optimal" = the LARGEST N keeping the worst-case chunk < 10M. The 355-01 harness already measured each batched loop's worst-case chunk + emitted its derived max-safe batch (the largest N keeping fixed_overhead + N×worst_case_per_item_marginal < 10M). This plan SETS the constants to those derived optima and re-proves.

The batched per-tx loops to bound (RE-CONFIRM by reading the source — there is NO standalone BUY_BATCH constant; "BUY_BATCH-style" is only a comment idiom, and SUB_STAGE_BATCH IS the per-day buy/stage chunk):
- SUB_STAGE_BATCH lives in DegenerusGameAdvanceModule.sol:149 (uint256 private constant SUB_STAGE_BATCH = 50;). DegenerusGameAdvanceModule._runSubscriberStage (~:754-765) passes it to GameAfkingModule.processSubscriberStage as the chunk size. This is the constant to SET (GAS-03). It is the v55-era value (50) carried forward with a STALE comment (~262k/buy → 13.1M; the v56 per-buy storm is GONE, settle-day is the new binding case). SIZE it from the 355-01 derived_max_safe_sub_stage_batch (the binding settle-day per-sub marginal) — the largest N keeping the settle-day chunk < 10M and ≤ 16.7M for the heavier mode. When you change the value, REWRITE the stale comment to the v56 reality (settle-day per-sub marginal × N < 10M target, ≤ 16.7M ceiling) — lean style, no plan-IDs/spec-lines.
- OPEN_BATCH lives in GameAfkingModule.sol:206 (uint256 internal constant OPEN_BATCH = 200;). RE-CONFIRM / RE-TUNE it from the 355-01 derived_max_safe_open_batch (the per-open marginal) — the largest N keeping the open chunk < 10M and ≤ 16.7M. If 355-01 confirmed 200 is already within the < 10M-safe range with full headroom, leave it (record the confirmation); if 355-01 derived a smaller max-safe N, SHRINK it to the optimal; if a larger N is safe AND there is a throughput reason, it MAY be raised but NEVER above the derived max-safe N.
- SETTLE_PERIOD = 10, SUBSCRIBER_CAP = 500 (GameAfkingModule.sol). The settle-day chunk is the heaviest STAGE day: when processDay % SETTLE_PERIOD == 0, each of the up-to-SUB_STAGE_BATCH subs in the chunk runs _settleQuest.

The SOLVENCY-01 byte-frozen debit: GameAfkingModule.sol:744-745 (afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue)). NEVER touched by this plan — assert byte-unchanged vs 453f8073 at the gate.

Outcome-A precedent (v55 Phase 350): if the gas-skeptic rejected every GAS-04 candidate and the savings are structural to the 354 refactor, the phase records Outcome-A — no net contract diff. NOTE: setting SUB_STAGE_BATCH / OPEN_BATCH IS a constant edit, so any value change rides the gate. Outcome-A (doc-only, no contracts/ edit) applies ONLY when the 355-02 APPROVED set is empty AND the 355-01 measurement confirms the CURRENT SUB_STAGE_BATCH=50 and OPEN_BATCH=200 are ALREADY the computed optima (already the largest-N-under-10M with full headroom to 16.7M for the binding mode). Otherwise SET the value(s) to the derived optimum (a contracts/ edit, so the gate fires). The executor MUST determine which case applies from the 355-01 derived max-safe batches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set SUB_STAGE_BATCH + OPEN_BATCH to the computed optimum (GAS-03) + apply the skeptic-APPROVED GAS-04 collapse (or record Outcome-A)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md (the MEASURED settle-day full-chunk gas at 50, both modes, the per-open marginal + open chunk at 200, AND the derived max-safe batches derived_max_safe_sub_stage_batch / derived_max_safe_open_batch — the load-bearing numbers for the SUB_STAGE_BATCH / OPEN_BATCH decision)
    - .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md (the `## Decision for 355-03` section — the APPROVED GAS-04 set with file:line + implementation notes + binding-marginal effects, and the REJECTED set)
    - contracts/modules/DegenerusGameAdvanceModule.sol:145-160, :743-765 (the SUB_STAGE_BATCH constant + its stale comment + _runSubscriberStage that consumes it)
    - contracts/modules/GameAfkingModule.sol:200-210 (the OPEN_BATCH constant)
    - contracts/modules/GameAfkingModule.sol:660-929, :1141-1190 (the STAGE hot path + settle-day ride + _settleQuest — the GAS-04 edit target, IF any APPROVED candidate touches it)
    - contracts/modules/GameAfkingModule.sol:744-745 (the SOLVENCY-01 debit — must stay byte-frozen)
  </read_first>
  <action>
    Set BOTH batch constants to their COMPUTED OPTIMUM from the 355-01 derived max-safe batches (the largest N keeping each loop's worst-case chunk < 10M and provably ≤ 16.7M at the cap):
    - SUB_STAGE_BATCH: pick the LARGEST N ≤ derived_max_safe_sub_stage_batch (from the binding-mode settle-day per-sub marginal). The current 50 may stay, shrink, or rise — the measurement decides. If the settle-day per-sub marginal is lower than the old per-buy storm, the safe N may be larger (more throughput, fewer advance txs); if the settle-day ride pushes it higher, N must drop. Set it in DegenerusGameAdvanceModule.sol:149 and REWRITE the stale ~262k/13.1M comment to the v56 reality (settle-day per-sub marginal × N < 10M target / ≤ 16.7M ceiling), lean style — no plan-IDs/requirement-tags/spec-lines.
    - OPEN_BATCH: pick the LARGEST N ≤ derived_max_safe_open_batch (from the per-open marginal) keeping the open chunk < 10M and ≤ 16.7M. Set it in GameAfkingModule.sol:206 if it differs from 200; if 200 is already the confirmed optimum with full headroom, leave it.
    - THEN branch on the 355-02 decision: IF 355-02's APPROVED set is non-empty, apply ONLY those GAS-04 changes to GameAfkingModule.sol exactly per the implementation notes (hoist a repeated SLOAD, collapse a redundant mode branch, share a clamp helper — whatever was APPROVED). Apply each verbatim to the cited file:line; do NOT improvise extra changes. If an APPROVED change altered a binding marginal, the new max-safe batch must be RE-DERIVED (Task 2 re-runs the harness — adjust the batch value if the re-derivation moves it).
    - Outcome-A (doc-only, NO contracts/ edit) applies ONLY when 355-02's APPROVED set is empty AND 355-01 confirms BOTH current constants (SUB_STAGE_BATCH=50, OPEN_BATCH=200) are ALREADY the computed optima (largest-N-under-10M with full headroom to 16.7M for the binding mode). In that case make NO contracts/ edit and proceed to Task 2 to RE-PROVE both fit the dual bound (the measurement IS the deliverable).
    - Comment style (USER rule lean-code-comments-no-procedural-meta): comments explain how the code works — no plan-IDs, requirement-tags, or spec-line citations.
    - In ALL cases: the SOLVENCY-01 debit (:744-745) stays byte-frozen; the open freeze shape, the self-marking settle balances, and the double-credit guards are untouched. `forge build` must exit 0.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5; grep -n "SUB_STAGE_BATCH = " contracts/modules/DegenerusGameAdvanceModule.sol; grep -n "OPEN_BATCH = " contracts/modules/GameAfkingModule.sol; git diff 453f8073 -- contracts/modules/GameAfkingModule.sol | grep -E "^[-+].*(afkingFunding\[src\] -=|claimablePool -= uint128)"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - SUB_STAGE_BATCH is set to the largest-N-under-10M value justified by the 355-01 derived max-safe batch (either re-tuned, or 50 confirmed as the optimum with full headroom to 16.7M at the cap for the binding mode) and its stale ~262k comment is rewritten to the v56 dual-bound reality.
    - OPEN_BATCH is set to the largest-N-under-10M value justified by the 355-01 derived max-safe open batch (re-tuned, or 200 confirmed as the optimum with full headroom).
    - If 355-02 APPROVED set non-empty: each APPROVED GAS-04 change is present at its cited file:line and NO un-approved change is introduced. Outcome-A (no contracts/ edit) ONLY when the APPROVED set is empty AND both constants were already the computed optima.
    - The SOLVENCY-01 debit lines (:744-745) show NO +/- in `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` (the grep returns no added/removed lines on those two statements).
  </acceptance_criteria>
  <done>SUB_STAGE_BATCH and OPEN_BATCH are set to their computed optima (largest N under the 10M target, provably ≤ 16.7M) against the measured marginals; only skeptic-APPROVED GAS-04 changes are landed (or Outcome-A recorded); SOLVENCY-01 byte-frozen; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove every batched chunk under the dual bound (< 10M target, ≤ 16.7M ceiling) at the chosen SUB_STAGE_BATCH / OPEN_BATCH</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V56AfkingGasMarginal.t.sol (the 355-01 worst-case settle-day chunk + open-chunk tests, GAS_TARGET / EFFECTIVE_GAS_CEILING constants, the derived-max-safe-batch logs — update the local SUB_STAGE_BATCH / OPEN_BATCH constants + re-run against the chosen values)
    - .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md (the measured baseline + derived max-safe batches to compare against)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the SUB_STAGE_BATCH value just set)
    - contracts/modules/GameAfkingModule.sol (the OPEN_BATCH value just set)
  </read_first>
  <action>
    Update the harness's SUB_STAGE_BATCH and OPEN_BATCH local constants to mirror the values set in Task 1, and re-run the dual-bound proofs:
    - The worst-case settle-day STAGE chunk test (both lootbox and ticket modes) proves the FULL chunk on a settle-day boundary at the chosen SUB_STAGE_BATCH < GAS_TARGET (10M, the target) AND logs the headroom up to EFFECTIVE_GAS_CEILING (16.7M, the never-exceed proof) for the binding mode. Keep the cross-check assert: SUB_STAGE_BATCH ≤ the freshly derived derived_max_safe_sub_stage_batch (the chosen value IS the optimum, not above it).
    - The OPEN_BATCH open chunk proof: the open chunk at the chosen OPEN_BATCH < GAS_TARGET (10M) AND headroom to 16.7M logged; cross-check OPEN_BATCH ≤ derived_max_safe_open_batch.
    - If any APPROVED GAS-04 change altered a per-buy/per-settle/per-open marginal, re-run the affected marginal tests, re-derive the affected max-safe batch, and update the logged-comparison notes (the measured number is the deliverable — re-record it; if the re-derivation moved the optimal batch, the Task-1 value must match).
    - Keep the non-vacuity assertions (full chunk advanced, ≥ cap−2 newly stamped, ≥1 settle fired drained questProgress to 0). If the chosen batch is larger than the harness's prior N_HI staging supported, extend the funded-set seeding so a full SUB_STAGE_BATCH chunk is exercised in one advance.
  </action>
  <verify>
    <automated>forge test --match-contract V56AfkingGasMarginal -vv 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract V56AfkingGasMarginal` exits 0.
    - The worst-case settle-day chunk test asserts the full SUB_STAGE_BATCH advance (at the value set in Task 1) < 10_000_000 (the TARGET) for BOTH lootbox and ticket modes, logs the headroom up to 16_700_000, and cross-checks SUB_STAGE_BATCH ≤ its derived max-safe N; non-vacuity intact.
    - The OPEN_BATCH open chunk is asserted < 10_000_000 (the TARGET) at the chosen OPEN_BATCH, headroom to 16_700_000 logged, OPEN_BATCH ≤ its derived max-safe N.
    - The harness SUB_STAGE_BATCH and OPEN_BATCH local constants match DegenerusGameAdvanceModule.sol / GameAfkingModule.sol.
    - Any re-measured marginal affected by an APPROVED GAS-04 change is re-logged and its derived max-safe batch re-derived.
  </acceptance_criteria>
  <done>Every batched per-tx chunk (the settle-day STAGE at the chosen SUB_STAGE_BATCH for both modes; the open chunk at the chosen OPEN_BATCH) is proven < 10M (target) and provably ≤ 16.7M (ceiling) at SUBSCRIBER_CAP; GAS-03 is empirically satisfied under the dual bound.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Contract-commit HARD STOP — USER hand-review + commit the net diff (or confirm Outcome-A)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/GameAfkingModule.sol</files>
  <action>
    HOLD at the contract-commit boundary. Do NOT auto-commit any `contracts/*.sol` change. Assemble the diff-review artifact, run the build + harness, present the diff to the USER, and commit ONLY on explicit USER authorization (or, under Outcome-A, make no contract commit). This task is a blocking human gate, not autonomous work — its concrete steps are in `<how-to-verify>`.
  </action>
  <what-built>
    The Phase 355 GAS tune: SUB_STAGE_BATCH set to the computed optimum against the measured settle-day chunk (GAS-03) + OPEN_BATCH re-confirmed/re-tuned to its computed optimum + any skeptic-APPROVED GAS-04 mode/SLOAD-collapse change, applied to contracts/ and locally compiling (forge build clean), with every batched per-tx chunk proven < 10M (target) and provably ≤ 16.7M (ceiling) at the cap. The SOLVENCY-01 debit (GameAfkingModule.sol:744-745) is byte-frozen vs 453f8073; the open freeze shape + self-marking settle balances + double-credit guards are untouched. If 355-02 rejected every candidate AND both constants were confirmed already-optimal, this is Outcome-A (no contracts/ diff — doc-only).
  </what-built>
  <how-to-verify>
    1. Author a `355-03-DIFF-REVIEW.md` (gitignored `*REVIEW*` artifact, on disk only): the full `git diff 453f8073 -- contracts/` for Phase 355, the per-requirement mapping (GAS-03 -> SUB_STAGE_BATCH line + OPEN_BATCH line, each with its derived max-safe N + the worst-case chunk gas + the headroom to 16.7M; GAS-04 -> each APPROVED change line or "Outcome-A, none"), the SOLVENCY-01 byte-unchanged attestation, and the settle-day-at-cap + open-chunk measured numbers showing < 10M and ≤ 16.7M.
    2. Run `forge build` (exit 0) and `forge test --match-contract V56AfkingGasMarginal` (exit 0) and paste the settle-day-at-cap + open-chunk results (the < 10M target hit + the 16.7M headroom).
    3. Present the diff to the USER. If Outcome-A (no contracts/ diff), state that explicitly and ask the USER to confirm the no-diff close.
    4. The USER hand-reviews. ONLY on explicit USER "approved" (or equivalent authorization) does the agent commit the single batched `contracts/*.sol` change as `feat(355): ...` (rides the same v56 contract boundary). NEVER auto-commit contracts. If Outcome-A, no contract commit is made — only the docs/SUMMARY commit.
  </how-to-verify>
  <resume-signal>USER types "approved" (commit the contract diff) or "outcome-a confirmed" (no contract commit) or describes required changes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper/advanceGame -> STAGE | The per-day STAGE runs in the advanceGame chain; a too-large SUB_STAGE_BATCH on the settle day breaches 16.7M -> the advance reverts -> the game cannot advance -> DoS / forced game-over. This is the HIGH-severity threat (the only gas-DoS that matters per the STATE.md weighting). The 10M comfort target (deliberately below the 16.7M kill ceiling) is the mitigation: it leaves headroom for measurement variance + worst-case outliers so a real settle-day tx never reaches 16.7M. |
| keeper/advanceGame -> open chunk | The OPEN_BATCH open chunk runs in the same chain; a too-large OPEN_BATCH breaches 16.7M the same way. Same dual-bound mitigation. |
| any GAS-04 edit -> the afking buy/open/settle | A "gas win" that silently widens an attack surface (unmanipulability, double-credit, RNG-freeze, SOLVENCY-01) OR raises a binding per-item marginal (shrinking the safe batch) is the security/headroom violation; gated by the 355-02 skeptic + this gate's byte-frozen attestation + the Task-2 dual-bound re-proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-03-BATCHDOS | Denial of service | SUB_STAGE_BATCH / OPEN_BATCH too large for an under-measured marginal | mitigate | size BOTH constants to the COMPUTED max-safe N from the MEASURED worst-case per-item marginal (settle-day per-sub for SUB_STAGE_BATCH, per-open for OPEN_BATCH) so the worst-case chunk TARGETS < 10M; an under-measured marginal that picked too large an N → the settle-day STAGE chunk breaches 16.7M → advanceGame DoS / forced game-over. The 10M comfort target IS the mitigation: it sits below the 16.7M kill ceiling so the headroom absorbs measurement variance + worst-case outliers. Task 2 re-proves the full chunk < 10M with the headroom to 16.7M logged + the cross-check that the chosen N ≤ the derived max-safe N. HIGH severity — block on a breach |
| T-355-03-SOLVENCY | Tampering | the ETH/claimablePool debit | mitigate | the SOLVENCY-01 debit (:744-745) is byte-frozen; the gate attests `git diff 453f8073` shows no +/- on those two lines; never a candidate |
| T-355-03-UNAPPROVED | Elevation of privilege (unvetted change) | a GAS-04 edit | mitigate | ONLY the 355-02 skeptic-APPROVED set is landed, verbatim at the cited file:line; no improvised edit; no edit that raises a binding marginal (which would shrink the safe batch); the USER hand-review at Task 3 is the final gate |
| T-355-03-FREEZE | Tampering | the RNG-freeze open shape / self-marking settle | accept | this plan does NOT touch the open seed (rngWordByDay[lastAutoBoughtDay]), the frozen scorePlus1, or the self-marking settle balances — confirmed unchanged in the diff review (the SUB_STAGE_BATCH / OPEN_BATCH constants + an APPROVED SLOAD/branch collapse cannot reach them) |
| T-355-03-SC | Tampering | npm/forge installs | mitigate | no package installs; the only contract change is two constants + an APPROVED collapse; the USER hand-review gate (blocking-human, never auto-approved) covers the contract-commit boundary |
</threat_model>

<verification>
- `forge build` exits 0 with the Phase-355 contract change (or no change under Outcome-A) applied.
- `forge test --match-contract V56AfkingGasMarginal` exits 0; every batched per-tx chunk at the chosen constants is proven under the dual bound.
- INVARIANT (USER-LOCKED): every per-tx advance/afking chunk — the settle-day STAGE at the chosen SUB_STAGE_BATCH (both modes) AND the open chunk at the chosen OPEN_BATCH — has its worst-case < 10,000,000 (the comfort TARGET) and provably ≤ 16,700,000 (the hard ceiling) at SUBSCRIBER_CAP, with the chosen N being the LARGEST that still satisfies < 10M (the optimum). The headroom from the measured chunk up to 16.7M is logged as the never-exceed argument.
- `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` shows NO +/- on the SOLVENCY-01 debit lines (:744-745).
- Only skeptic-APPROVED GAS-04 changes are present (or none under Outcome-A); the open freeze shape + self-marking settle balances + double-credit guards are untouched.
- The contract change is HELD at the commit boundary — committed ONLY on explicit USER hand-review (or no contract commit under Outcome-A).
</verification>

<success_criteria>
- GAS-03: SUB_STAGE_BATCH is set to the computed optimum (largest N under the 10M target) against the measured settle-day chunk and the settle-day STAGE is proven < 10M (target) and provably ≤ 16.7M (ceiling) at SUBSCRIBER_CAP for the binding mode.
- OPEN_BATCH is re-confirmed/re-tuned to its computed optimum and the open chunk is proven under the same dual bound.
- GAS-04: every skeptic-APPROVED mode/SLOAD-collapse change is landed (and only those), proven unmanipulable and headroom-preserving; OR Outcome-A recorded (no net diff, both constants confirmed already-optimal).
- The SOLVENCY-01 debit is byte-frozen; RNG-freeze + unmanipulability + double-credit guards intact.
- The contract change rides the single v56 USER-approved diff, HELD at the contract-commit boundary (the ONE Phase-355 gate), or recorded as Outcome-A no-diff.
</success_criteria>

<output>
Create `.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-03-SUMMARY.md` when done — record: the chosen SUB_STAGE_BATCH + OPEN_BATCH + their justification (the derived max-safe N + the measured worst-case chunk + the headroom to 16.7M for each), the landed GAS-04 changes (or Outcome-A), the SOLVENCY-01 byte-frozen attestation, the settle-day-at-cap + open-chunk measured numbers (< 10M target / ≤ 16.7M ceiling), and the commit hash (or "Outcome-A — no contract commit").
</output>
</output>
