---
phase: 355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gas/V56AfkingGasMarginal.t.sol
autonomous: true
requirements: [GAS-01, GAS-02]
must_haves:
  truths:
    - "A v56 gas-marginal harness measures the per-buy lootbox marginal, the per-buy ticket marginal, the per-open marginal, the per-settle marginal, and the WORST-CASE per-tx chunk for every batched advance/afking loop — the settle-day STAGE chunk at SUB_STAGE_BATCH (the binding case, where every sub fires _settleQuest) and the OPEN_BATCH open chunk — each via the (gasN − gasN−1) snapshot/revert loop-N-divide MARGINAL (never a single-item total)"
    - "From each measured worst-case per-item marginal the harness DERIVES the max safe batch size as the largest N with fixed_tx_overhead + N×worst_case_per_item_marginal < 10,000,000 (the design comfort TARGET), and emits that derived N as a named-uint log alongside the measured chunk gas"
    - "Every measured per-tx chunk (settle-day STAGE at SUB_STAGE_BATCH, both modes; OPEN_BATCH open chunk) is recorded as a named-uint log, asserted < 10,000,000 (the TARGET) at its CURRENT batch constant, AND the headroom from that value up to 16,700,000 (the HARD never-exceed ceiling) is computed and logged — the dual bound: target < 10M, provably ≤ 16.7M"
    - "The harness proves GAS-02: the per-buy accrue makes NO new cold per-buy SSTORE (the affiliateBase/questProgress/buyerOwedBurnie accumulator lives in the already-warm Sub slot), confirmed via a state-diff trace over a per-buy STAGE iteration"
  artifacts:
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "the v56 per-buy / per-open / per-settle marginal-gas harness, the per-tx worst-case chunk gas + derived-max-safe-batch + dual-bound (10M target / 16.7M ceiling) report, and the GAS-02 warm-slot/no-new-cold-SSTORE trace, on the e18af451 applied tree"
      contains: "EFFECTIVE_GAS_CEILING"
      min_lines: 200
  key_links:
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "GameAfkingModule.processSubscriberStage"
      via: "new-day advanceGame() STAGE over N funded subs"
      pattern: "advanceGame"
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "GameAfkingModule._settleQuest settle-day ride"
      via: "warp the STAGE onto a processDay % SETTLE_PERIOD == 0 boundary"
      pattern: "SETTLE_PERIOD"
---

<objective>
Build the v56 gas-marginal measurement harness on the IMPL-applied tree (commit `e18af451`, baseline `453f8073`) and measure every everyday-afking marginal the GAS phase needs: the per-buy LOOTBOX marginal, the per-buy TICKET marginal (the new minimal-write primitive, off the old ~262k `purchaseWith`), the per-open marginal, the per-SETTLE marginal (the settle-day chunk that rides the daily buy STAGE), and the WORST-CASE per-tx chunk for EVERY batched advance/afking loop — the settle-day STAGE chunk at `SUB_STAGE_BATCH` (the binding case) and the `OPEN_BATCH` open chunk. From each measured worst-case per-item marginal, DERIVE the max safe batch size for the < 10M design target and report the dual bound (< 10M target, provably ≤ 16.7M hard ceiling). Also prove GAS-02 empirically: the per-buy in-slot accrue makes NO new cold per-buy SSTORE.

Purpose: every downstream GAS decision (the `SUB_STAGE_BATCH` / `OPEN_BATCH` (re-)tune in 355-03, the scavenger candidate sizing in 355-02, the Outcome-A-vs-net-tune call) is grounded in MEASURED numbers, not SPEC placeholders. The USER constraint is now SHARPER than "under 16.7M": every batched per-tx loop must be sized so its WORST-CASE gas TARGETS < 10,000,000 (the comfort target) AND PROVABLY NEVER EXCEEDS 16,700,000 (the hard kill ceiling — a breach = advanceGame DoS / forced game-over). The headroom between 10M and 16.7M absorbs measurement variance + worst-case outliers. The measured per-item marginals and the derived max-safe batch sizes ARE the GAS-01/GAS-02 deliverable.

Output: `test/gas/V56AfkingGasMarginal.t.sol` — the measured marginals as named-uint logs, the per-tx worst-case chunk gas for each batched loop with its DERIVED max-safe batch size (the largest N keeping the chunk < 10M) and its dual-bound report (< 10M target / ≤ 16.7M ceiling headroom), plus the GAS-02 no-new-cold-SSTORE trace. Test-only; agent-committable (no contract gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-SUMMARY.md

<interfaces>
<!-- The v55 harness (test/gas/V55AfkingGasMarginal.t.sol) is the PATTERN to fork. Its comment header describes the OLD v55/349.2 tree (the per-buy cross-contract storm, the 232-bit Sub slot) — those facts are STALE under v56 and MUST be re-derived against the applied tree, not copied. Reuse its proven driving harness (DeployProtocol fixture, _settleGame/_settleClean VRF-drain, _setupFundedLootboxSubs, depositAfkingFunding, _grantDeityPass, the Sub-stamp slot reads, the snapshot/revert two-near-N marginal form). -->

The CR-01 / 350-SPEC §0 MARGINAL rule (load-bearing): every per-item number is the loop-N-divide MARGINAL — (gas for N items − gas for N−1 items) measured from ONE clean baseline via vm.snapshotState()/vm.revertToState() — NEVER a single-item TOTAL (a single-item total over-states cost and re-introduces the Phase-319 self-crank faucet).

The DUAL BOUND (USER-LOCKED this phase): every batched per-tx loop in the daily advance / afking chain must be sized so its WORST-CASE gas TARGETS < 10,000,000 AND PROVABLY NEVER EXCEEDS 16,700,000. Two constants in the harness:
- GAS_TARGET = 10_000_000 — the design comfort TARGET; the worst-case chunk should land below this with the chosen batch.
- EFFECTIVE_GAS_CEILING = 16_700_000 — the HARD never-exceed kill ceiling (foundry.toml inflates block_gas_limit to 30e9 for the harness; the bar is the 16.7M). A breach = advanceGame DoS / forced game-over.
The headroom (16.7M − the measured-at-target chunk) is the safety margin that absorbs measurement variance + worst-case outliers; it MUST be computed and logged so 355-03 can show the never-exceed argument.

Sizing method (the harness DERIVES this, it is not guessed): for each batched loop, measure the worst-case per-item marginal at the worst realistic state (settle day, SUBSCRIBER_CAP subs / full open queue), then the max safe batch N = the largest integer with fixed_tx_overhead + N×worst_case_per_item_marginal < GAS_TARGET. "Optimal" = that largest N (maximize throughput/headroom without breaching 10M). Emit N as a derived-max-safe-batch named-uint log.

The batched per-tx loops to bound (RE-CONFIRM by reading the source — there is NO standalone BUY_BATCH constant; "BUY_BATCH-style" is only a comment idiom and SUB_STAGE_BATCH IS the per-day buy/stage chunk):
- SUB_STAGE_BATCH = 50 — DegenerusGameAdvanceModule.sol:149 (the per-day STAGE/buy chunk; the STAGE caller passes this to processSubscriberStage). The BINDING worst case is the settle-day STAGE chunk (processDay % SETTLE_PERIOD == 0, every sub fires _settleQuest).
- OPEN_BATCH = 200 — GameAfkingModule.sol:206 (the box-open chunk).
- SETTLE_PERIOD = 10 — GameAfkingModule.sol:172 (the quest leg settles inline on processDay % SETTLE_PERIOD == 0).
- SUBSCRIBER_CAP = 500 — GameAfkingModule.sol:164 (the worst-case sub count).

The v56 per-buy hot path (GameAfkingModule.sol ~:743-918): SOLVENCY-01 debit (afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue), byte-frozen) → ticket minimal-write primitive (_queueTicketsScaled + buyerOwedBurnie accrue) OR lootbox box-stamp → the mode-agnostic in-slot accrue (affiliateBase flat-7% += / ++questProgress / afkCoveredThroughDay = processDay) → lastAutoBoughtDay marker → IF processDay % SETTLE_PERIOD == 0 THEN _settleQuest (the per-settle cost: questProgress×QUEST_SLOT0_REWARD + buyerOwedBurnie in ONE coinflip.creditFlip + quests.settleAfkingQuest streak-advance).

The re-packed Sub slot (DegenerusGameStorage.sol:1894-1962) is a SINGLE 256-bit slot, 241/256 bits used (15 spare): config 48 + stamp 48 (scorePlus1 uint16 / amount uint32 milli-ETH) + markers 72 (lastAutoBoughtDay/lastOpenedDay/afkCoveredThroughDay uint24 each) + accumulator 73 (affiliateBase uint32 / questProgress uint8 / buyerOwedBurnie uint32 / hasEverSubscribed bool). The accumulator is IN-SLOT — the per-buy accrue is a warm SLOAD-mask-SSTORE on the same slot the stamp already dirtied, NOT a new cold slot. THIS is the GAS-02 property to prove.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork the v55 harness into V56AfkingGasMarginal and re-derive the applied-tree facts</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol (the PATTERN: the DeployProtocol fixture, the snapshot/revert two-near-N marginal form, _settleGame/_settleClean, _setupFundedLootboxSubs, depositAfkingFunding, _grantDeityPass, the Sub-stamp slot reads — fork these helpers)
    - contracts/storage/DegenerusGameStorage.sol:1894-2002 (the re-packed Sub slot field order + the cursor slot — the byte offsets for lastAutoBoughtDay/lastOpenedDay shifted vs v55 because amount narrowed to uint32 milli-ETH; RE-DERIVE the offsets)
    - contracts/modules/GameAfkingModule.sol:700-929 (the v56 per-buy hot path + the settle-day ride — the exact accrue/settle shape being measured)
    - contracts/modules/DegenerusGameAdvanceModule.sol:149, :754-765 (SUB_STAGE_BATCH + _runSubscriberStage)
  </read_first>
  <action>
    Create test/gas/V56AfkingGasMarginal.t.sol as a fork of V55AfkingGasMarginal.t.sol's driving harness (NOT a copy of its stale comment header). RE-DERIVE the game-resident storage slots via `forge inspect storage DegenerusGame` (the Sub slot's field byte offsets changed under the v56 re-pack — amount is now uint32 milli-ETH, the day markers are uint24, the accumulator fields are new; do not trust the v55 OFF_LASTBOUGHT=21 / OFF_LASTOPENED=25 constants). Port the proven helpers: _settleGame/_settleClean VRF-drain, _setupFundedLootboxSubs, _fundPool/depositAfkingFunding, _grantDeityPass, _measureStageAdvanceGas, _readStampDay, rngWordByDay/_injectRngWordByDay, the snapshot/revert two-near-N form, _u. Add a _setupFundedTicketSubs sibling (subscribe with useTickets=true — the `subscribe(..., useTickets, ...)` flag, RE-DERIVE the exact subscribe signature from the contract) so the ticket-mode marginal can be measured against the new minimal-write primitive. Declare BOTH bounds as harness constants: GAS_TARGET = 10_000_000 (the design comfort target) and EFFECTIVE_GAS_CEILING = 16_700_000 (the HARD never-exceed ceiling). Read SUB_STAGE_BATCH/SETTLE_PERIOD/OPEN_BATCH/SUBSCRIBER_CAP as local constants mirroring the source. Add a pure helper `_maxSafeBatch(uint256 fixedOverhead, uint256 perItemMarginal) returns (uint256)` that returns the largest N with fixedOverhead + N×perItemMarginal < GAS_TARGET (the derived optimal batch). Write the contract header to describe the v56 applied tree (in-slot accumulator + deferred settle) AND the dual-bound sizing contract (< 10M target / ≤ 16.7M never-exceed), explicitly noting the v55 storm is GONE.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5 && forge test --match-contract V56AfkingGasMarginal --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 with the new test file present.
    - `forge test --match-contract V56AfkingGasMarginal --list` lists the contract and its test fns (compiles + is discoverable).
    - The file declares BOTH GAS_TARGET = 10_000_000 and EFFECTIVE_GAS_CEILING = 16_700_000, plus re-derived slot/offset constants (no copied stale v55 232-bit / OFF_LASTBOUGHT=21 premise).
    - A `_maxSafeBatch(fixedOverhead, perItemMarginal)` helper returns the largest N keeping fixedOverhead + N×perItemMarginal < GAS_TARGET.
    - A _setupFundedTicketSubs helper exists (useTickets=true subscribe) alongside the lootbox helper.
  </acceptance_criteria>
  <done>The v56 harness compiles, is test-discoverable, carries the re-derived applied-tree slot facts, both lootbox and ticket funded-sub setup helpers, both bounds (10M target / 16.7M ceiling), and the max-safe-batch derivation helper.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the four marginals + every batched-loop worst-case chunk, derive max-safe batch, report the dual bound</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol:136-304 (the per-buy / per-open / 16.7M-ceiling test bodies — the shape to adapt for the four v56 marginals)
    - contracts/modules/GameAfkingModule.sol:908-929 (the settle-day ride: processDay % SETTLE_PERIOD == 0 → _settleQuest — the per-settle cost the new per-settle test must warp onto)
    - contracts/modules/GameAfkingModule.sol:1141-1190 (_settleQuest + claimQuest — what the per-settle marginal includes)
    - contracts/modules/GameAfkingModule.sol:1000-1097 (the OPEN_BATCH open leg — the open chunk to bound)
  </read_first>
  <action>
    Add the marginal measurements + the per-tx worst-case chunk + derived-batch + dual-bound reports, each via the snapshot/revert (gasN − gasN−1) form:
    (a) testPerBuyLootboxMarginal — N vs N−1 funded LOOTBOX subs over a NON-settle-day STAGE advance (warp so processDay % SETTLE_PERIOD != 0); record per_buy_lootbox_marginal_gas; assert < EFFECTIVE_GAS_CEILING (per-item must never alone near the ceiling). Emit a comparison log against the v55 ~206k reference and the ~130-140k GAS-01 target (informational, NOT a hard pin — the measured number is the deliverable).
    (b) testPerBuyTicketMarginal — same, funded TICKET subs (the minimal-write primitive); record per_buy_ticket_marginal_gas; assert under the ceiling. Emit a log vs the old ~262k purchaseWith reference (the structural win).
    (c) testPerOpenMarginal — N vs N−1 ready stamped boxes via mintBurnie() open leg (port _measureOpenLegGas); record per_open_marginal_gas; assert uniform-O(1) under a generous bound. THEN derive the open chunk: compute open_chunk_at_OPEN_BATCH = fixed_open_overhead + OPEN_BATCH×per_open_marginal_gas, assert it < GAS_TARGET (the 10M comfort target), log the headroom (EFFECTIVE_GAS_CEILING − open_chunk_at_OPEN_BATCH), and emit derived_max_safe_open_batch = _maxSafeBatch(fixed_open_overhead, per_open_marginal_gas). Cross-check: assert OPEN_BATCH ≤ derived_max_safe_open_batch (the current constant is within the < 10M-safe range) — if not, the test FAILS, signaling 355-03 must shrink OPEN_BATCH.
    (d) testPerSettleMarginal — measure the ADDED cost of the settle-day ride: gasN = a STAGE advance landing on a settle boundary (processDay % SETTLE_PERIOD == 0) over N subs that have accrued questProgress+buyerOwedBurnie; gasNm1 the same minus one sub — so (gasN − gasNm1) isolates ONE sub's _settleQuest cost (the creditFlip + settleAfkingQuest). Record per_settle_marginal_gas; assert under the ceiling. To accrue questProgress before the settle day, run prior non-settle STAGE days for the funded set. This per-sub settle-day marginal is the BINDING per-item marginal for SUB_STAGE_BATCH.
    (e) testWorstCaseSettleDayStageChunkAtCap — seed SUB_STAGE_BATCH funded subs (the binding case is whichever mode is heavier — measure BOTH a full lootbox chunk and a full ticket chunk on a settle-day boundary), drive ONE settle-day advance, record stage_settle_day_full_chunk_whole_gas for BOTH modes. For the BINDING (heavier) mode: (1) assert the whole settle-day advance (which BOUNDS the chunk) < GAS_TARGET (the 10M comfort target at the CURRENT SUB_STAGE_BATCH); (2) log the headroom (EFFECTIVE_GAS_CEILING − the measured chunk) — this is the never-exceed margin; (3) emit derived_max_safe_sub_stage_batch = _maxSafeBatch(fixed_stage_overhead, per_settle_marginal_gas) using the (d) binding-mode per-sub settle-day marginal; (4) cross-check assert SUB_STAGE_BATCH ≤ derived_max_safe_sub_stage_batch (the current 50 is within the < 10M-safe range) — if not, the test FAILS, signaling 355-03 must shrink SUB_STAGE_BATCH. Non-vacuity: assert the cursor advanced a full SUB_STAGE_BATCH chunk AND ≥ SUB_STAGE_BATCH−2 subs newly stamped AND the settle actually fired (questProgress drained to 0 on the settled subs).
    Every measured number AND every derived max-safe batch AND every headroom is an emit log_named_uint. The settle-day chunk number + the derived max-safe SUB_STAGE_BATCH are the load-bearing inputs to GAS-03 (355-03).
  </action>
  <verify>
    <automated>forge test --match-contract V56AfkingGasMarginal -vv 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - All five tests pass (`forge test --match-contract V56AfkingGasMarginal` exits 0).
    - Each test emits a named-uint log of its measured marginal (per_buy_lootbox_marginal_gas, per_buy_ticket_marginal_gas, per_open_marginal_gas, per_settle_marginal_gas, stage_settle_day_full_chunk_whole_gas for both lootbox and ticket modes).
    - For every batched loop, the harness emits BOTH a derived max-safe batch log (derived_max_safe_sub_stage_batch, derived_max_safe_open_batch) AND a headroom-to-16.7M log, and cross-checks the current constant (SUB_STAGE_BATCH, OPEN_BATCH) ≤ its derived max-safe value.
    - testWorstCaseSettleDayStageChunkAtCap asserts the BINDING-mode full SUB_STAGE_BATCH settle-day advance < 10_000_000 (the TARGET) at the current constant, logs the headroom up to 16_700_000, and emits the derived max-safe SUB_STAGE_BATCH; non-vacuity holds (full chunk advanced, ≥ cap−2 newly stamped, ≥1 settle fired drained questProgress to 0).
    - testPerOpenMarginal asserts the OPEN_BATCH open chunk < 10_000_000 (the TARGET), logs the headroom up to 16_700_000, and emits the derived max-safe OPEN_BATCH.
    - Each per-item marginal is the (gasN − gasN−1) snapshot/revert form (no single-item total used as a per-item number).
  </acceptance_criteria>
  <done>The harness reports all four marginals + every batched-loop worst-case chunk (settle-day STAGE both modes, OPEN_BATCH open chunk), each asserted < 10M (target) at its current constant with the headroom to 16.7M logged AND its derived max-safe batch size emitted; the settle-day chunk number + the derived max-safe SUB_STAGE_BATCH are available to GAS-03.</done>
</task>

<task type="auto">
  <name>Task 3: Prove GAS-02 — the per-buy accrue makes no new cold per-buy SSTORE (warm in-slot)</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol:335-412, :603-628 (the vm.startStateDiffRecording / stopAndReturnStateDiff trace + the StorageAccess filtering helpers — the pattern for a per-buy SSTORE trace)
    - contracts/modules/GameAfkingModule.sol:873-907 (the mode-agnostic accrue + the lastAutoBoughtDay marker — the writes that must all land on the ONE warm Sub slot)
    - contracts/storage/DegenerusGameStorage.sol:1894-1967 (the single-slot Sub layout — _subOf is the slot whose writes the trace must confirm are all the same slot)
  </read_first>
  <action>
    Add testGas02AccrueIsWarmInSlotNoNewColdSstore. Drive a per-buy STAGE iteration (a new-day advanceGame over a small funded set) under vm.startStateDiffRecording / vm.stopAndReturnStateDiff. RE-DERIVE the _subOf slot for the buyer (keccak256(abi.encode(player, SUBOF_SLOT))). Assert: (1) all StorageAccess writes attributed to account == game on the buyer's Sub record target the SAME single slot (the stamp + the accumulator accrue + the markers are co-located — no second distinct cold slot is written for affiliateBase/questProgress/buyerOwedBurnie). Concretely, filter the per-buy iteration's write set for the buyer's Sub slot and assert the accumulator fields do not introduce a write to any slot other than the buyer's single _subOf slot. (2) Non-vacuity: assert ≥1 write DID land on the buyer's Sub slot (the accrue ran). Emit the per-buy distinct-Sub-slot-write count as a named-uint log (must be 1 for the buyer's record). Record a short note in the test NatSpec that the off-slot affiliate ledger is touched only at claim (DegenerusAffiliate.claim mints A/U1/U2 directly — single-step, no pendingClaim), NEVER per-buy.
  </action>
  <verify>
    <automated>forge test --match-test testGas02AccrueIsWarmInSlotNoNewColdSstore -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - testGas02AccrueIsWarmInSlotNoNewColdSstore passes.
    - The test asserts the per-buy accrue writes only the buyer's single _subOf slot (no new distinct cold slot for the accumulator fields) AND non-vacuously that ≥1 write landed on that slot.
    - The test emits a named-uint distinct-Sub-slot-write count (= 1 for the buyer's record).
    - Whole-contract run still green: `forge test --match-contract V56AfkingGasMarginal` exits 0.
  </acceptance_criteria>
  <done>GAS-02 is proven empirically: the per-buy accumulator accrue is a warm in-slot write on the same Sub slot the stamp dirties, introducing no new cold per-buy SSTORE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → contracts | The harness only READS gas + state via the live DeployProtocol fixture and vm cheatcodes; it MUTATES no `contracts/*.sol`. The risk is a MIS-MEASUREMENT that mis-informs the GAS-03 sizing decision (an under-measured per-item marginal → a too-large SUB_STAGE_BATCH / OPEN_BATCH → settle-day STAGE breaches 16.7M → advanceGame DoS / forced game-over). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-01-MEASURE | Information disclosure (wrong measurement) | the per-item marginal | mitigate | enforce the CR-01 (gasN − gasN−1) snapshot/revert MARGINAL form — never a single-item total; assert non-vacuity (cursor advanced a full chunk, ≥cap−2 newly stamped) so a vacuous/skip advance cannot pass as a measurement |
| T-355-01-SETTLEDAY | Denial of service (under-measured worst case) | the settle-day STAGE chunk | mitigate | measure the SETTLE-DAY chunk (processDay % SETTLE_PERIOD == 0, where every sub fires _settleQuest) as the binding case for both modes, NOT a cheap non-settle day; assert the full SUB_STAGE_BATCH advance < 10M (target) with the settle actually fired (questProgress drained) and log the headroom up to 16.7M; the 10M target IS the mitigation — it is deliberately below the 16.7M kill ceiling so measurement variance + worst-case outliers cannot push a real settle-day tx past 16.7M |
| T-355-01-UNDERSIZE | Denial of service (under-derived batch) | the derived max-safe batch | mitigate | derive each batch's max-safe N from the MEASURED worst-case per-item marginal (settle-day per-sub marginal for SUB_STAGE_BATCH; per-open marginal for OPEN_BATCH) via fixed_overhead + N×marginal < 10M, and FAIL the test if the current constant exceeds its derived max-safe N — so an over-large constant cannot silently pass measurement |
| T-355-01-GAS02VAC | Tampering (vacuous proof) | the GAS-02 no-cold-SSTORE trace | mitigate | the trace asserts non-vacuously that ≥1 write landed on the buyer's Sub slot (the accrue ran) AND no second distinct cold slot is written — a no-op advance cannot satisfy the no-new-cold-slot claim vacuously |
| T-355-01-SC | Tampering | npm/forge installs | accept | no package installs in this plan — pure test authoring against the existing Foundry toolchain (foundry.toml present); no new dependency surface |
</threat_model>

<verification>
- `forge build` exits 0 with `test/gas/V56AfkingGasMarginal.t.sol` present.
- `forge test --match-contract V56AfkingGasMarginal` exits 0 (all six+ tests green).
- All four marginals (per-buy lootbox, per-buy ticket, per-open, per-settle) are emitted as named-uint logs and asserted under the 16.7M ceiling.
- INVARIANT (USER-LOCKED): every per-tx advance/afking chunk — the settle-day STAGE at SUB_STAGE_BATCH (both modes) AND the OPEN_BATCH open chunk — has its worst-case at the current constant asserted < 10M (the comfort TARGET), its headroom up to 16.7M logged, and its derived max-safe batch (largest N keeping the chunk < 10M) emitted; the current constant is cross-checked ≤ its derived max-safe N. Worst case is measured at SUBSCRIBER_CAP / full queue.
- GAS-02 proven: per-buy accrue is a warm in-slot write, no new cold per-buy SSTORE.
- ZERO `contracts/*.sol` mutation (`git diff e18af451 -- contracts/` empty).
</verification>

<success_criteria>
- The v56 gas-marginal harness exists and measures the per-buy (both modes) + per-open + per-settle marginals + the worst-case chunk for every batched loop (settle-day STAGE at SUB_STAGE_BATCH; OPEN_BATCH open chunk), each as the CR-01 (gasN − gasN−1) MARGINAL.
- For every batched loop, the harness reports the dual bound — the worst-case chunk at the current constant < 10M (target) with the headroom to 16.7M (the never-exceed proof) logged — and the DERIVED optimal (largest-N-under-10M) max-safe batch.
- The measured numbers + derived max-safe batches are logged and available as the load-bearing input to 355-02 (scavenger sizing) and 355-03 (the SUB_STAGE_BATCH / OPEN_BATCH (re-)tune / Outcome-A call).
- GAS-01 measurement requirement satisfied (the per-buy + per-settle marginals + the per-tx worst-case chunks measured + the optimal batches derived). GAS-02 packing/no-new-cold-SSTORE proven empirically.
- Agent-committable (test only); no contract-commit gate.
</success_criteria>

<output>
Create `.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md` when done — record the MEASURED numbers (per_buy_lootbox, per_buy_ticket, per_open, per_settle, settle_day_full_chunk for both modes) verbatim, PLUS for every batched loop the worst-case chunk gas at its current constant, the headroom to 16.7M, and the DERIVED max-safe batch size (the largest N under the 10M target), since 355-02 and 355-03 consume them.
</output>
</output>
