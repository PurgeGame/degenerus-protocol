---
phase: 298-vrf-read-graph-catalog-catalog
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §11 (BurnieCoinflip._resolveFlip at :807 + win-decode at :837) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated"
    - "Every participating slot has writer enumeration (cross-contract into BurnieCoin where applicable)"
    - "Every tuple classified"
    - "Every VIOLATION has tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md"
      provides: "§11 catalog content for BurnieCoinflip resolution"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `BurnieCoinflip._resolveFlip` (`contracts/BurnieCoinflip.sol:807`) including the `(rngWord & 1)` win-decode at `:837` per D-298-CONSUMER-LIST-01 entry §11. Produce the §11 catalog section.

Purpose: CAT-01..06 coverage for BURNIE coin-flip resolution consumer. Extends coverage beyond Phase 287 JPSURF's jackpot-only scope per D-298-CONSUMER-LIST-01 prose.

Output: `298-11-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/BurnieCoinflip.sol
@contracts/BurnieCoin.sol
</context>

<tasks>

<task type="auto">
  <name>Task 11.1: Sub-agent backward-trace from BurnieCoinflip._resolveFlip</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §11; all D-298-* decisions; BurnieCoinflip:730 gated-revert convention reference)
    - contracts/BurnieCoinflip.sol (consumer at :807; win-decode at :837)
    - contracts/BurnieCoin.sol (cross-contract ERC20 with OZ-inherited transfer/transferFrom/approve/_mint/_burn writers)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at `BurnieCoinflip._resolveFlip` at `:807` AND the `(rngWord & 1)` win-decode at `:837`. Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01 — including cross-contract hops into `BurnieCoin` for balance/transfer SLOADs in payout paths.

    Special trace focus:
    - Win-decode SLOAD chain at `:837`: every storage read consumed alongside `(rngWord & 1)` to determine payout
    - Cross-contract `BurnieCoin` SLOADs: balance / allowance / supply if reached during payout
    - OZ-inherited writers of any participating slot must be enumerated in §C (transfer / transferFrom / approve / _mint / _burn on BurnieCoin if any of its slots are participating)

    Produces §A + §B + §C + §D + §E. Explicit-enumeration + ALL-SLOADs + NO SAFE_BY_DESIGN.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md`. Heading `# §11 — BurnieCoinflip._resolveFlip + win-decode (file:line 807 / 837)` + 5 CAT sub-headings.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-11-CATALOG-section.md` exists with all 5 CAT sub-headings
    - §B Participating? column populated; NO rows attested
    - §C covers every YES slot including any OZ-inherited writers for participating BurnieCoin slots
    - Every §D row classified (no SAFE_BY_DESIGN)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§11 section file complete; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §11 catalog section authored (BURNIE coin-flip resolution).
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-11-SUMMARY.md` when done.
</output>
