---
phase: 298-vrf-read-graph-catalog-catalog
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §4 (DecimatorModule.runTerminalDecimatorJackpot at DecimatorModule.sol:755) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated"
    - "Every participating slot has writer enumeration"
    - "Every (slot × writer × callsite) classified ∈ exemption set or VIOLATION"
    - "Every VIOLATION has remediation tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md"
      provides: "§4 catalog content for runTerminalDecimatorJackpot"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `DecimatorModule.runTerminalDecimatorJackpot` (`contracts/modules/DegenerusGameDecimatorModule.sol:755`) per D-298-CONSUMER-LIST-01 entry §4. Produce the §4 catalog section.

Purpose: CAT-01..06 coverage for game-over decimator-terminal consumer. Extends Phase 287 JPSURF beyond jackpot-only scope.

Output: `298-04-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameDecimatorModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Sub-agent backward-trace from runTerminalDecimatorJackpot</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §4; D-298-TRACE-DEPTH-01; D-298-SLOT-CLASSIFICATION-01; D-298-EXEMPT-REACH-01; D-298-EXEMPT-CROSSCONTRACT-01; D-298-RECOMMEND-DEPTH-01)
    - contracts/modules/DegenerusGameDecimatorModule.sol (consumer entry at :755)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at `runTerminalDecimatorJackpot` (`DecimatorModule.sol:755`). Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01; produces §A traced-fn-set + §B full-SLOAD-table with Participating? column + §C writer enumeration for participating slots (OZ-inherited transfer/transferFrom/approve/_mint/_burn included where applicable) + §D per-tuple verdict matrix (NO SAFE_BY_DESIGN) + §E per-VIOLATION tactic-from-menu + ≤80-char rationale per D-298-RECOMMEND-DEPTH-01.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md`. Heading `# §4 — DecimatorModule.runTerminalDecimatorJackpot (file:line 755)` + `## CAT-01 (§A)` + `## CAT-02 (§B)` + `## CAT-03 (§C)` + `## CAT-04 (§D)` + `## CAT-06 (§E)`.

    Explicit-enumeration discipline per `feedback_verify_call_graph_against_source.md` — no "by construction" / "covered by single fn" shortcuts.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-04-CATALOG-section.md` exists
    - All 5 CAT sub-headings present
    - §B Participating? column populated; NO rows attested
    - §C covers every YES slot
    - Every §D row classified (no SAFE_BY_DESIGN)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§4 section file complete; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §4 catalog section authored.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-04-SUMMARY.md` when done.
</output>
