---
phase: 298-vrf-read-graph-catalog-catalog
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §7 (LootboxModule._resolveLootboxCommon at :960 + _resolveLootboxRoll at :1623) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated"
    - "Every participating slot has writer enumeration"
    - "Every tuple classified"
    - "Every VIOLATION has tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md"
      provides: "§7 catalog content for manual-path lootbox roll"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `LootboxModule._resolveLootboxCommon` (`contracts/modules/DegenerusGameLootboxModule.sol:960`) and `LootboxModule._resolveLootboxRoll` (`:1623`) per D-298-CONSUMER-LIST-01 entry §7. Produce the §7 catalog section.

Purpose: CAT-01..06 coverage for manual-path lootbox-roll consumer cluster. Both internal helpers traced together as one consumer entry since they share the same RNG-consumption code path with bits[152..167] Bernoulli per Phase 274 LBX-WT precedent.

Output: `298-07-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameLootboxModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 7.1: Sub-agent backward-trace from _resolveLootboxCommon + _resolveLootboxRoll</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §7; all D-298-* decisions)
    - contracts/modules/DegenerusGameLootboxModule.sol (consumer entries at :960 + :1623)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at both `_resolveLootboxCommon` (`:960`) and `_resolveLootboxRoll` (`:1623`). Both internal helpers are RNG-consumption sites for the manual lootbox-roll path. Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01.

    Per D-298-EXEMPT-REACH-01 per-callsite classification: the same internal helper reached from BOTH a manual EOA entry (`openLootBox` / `openBurnieLootBox`) AND auto-resolve entry (Phase 274 manual-only scope per D-274-MANUAL-ONLY-01; auto-resolve callers in §6 covered by `resolveRedemptionLootbox`). The trace must explicitly enumerate every external entry point that reaches `:960` / `:1623` and classify each callsite separately.

    Produces §A + §B + §C + §D + §E. Explicit-enumeration + ALL-SLOADs + NO SAFE_BY_DESIGN.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md`. Heading `# §7 — LootboxModule._resolveLootboxCommon / _resolveLootboxRoll (file:line 960 / 1623)` + 5 CAT sub-headings.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-07-CATALOG-section.md` exists
    - All 5 CAT sub-headings present
    - §B Participating? column populated; NO rows attested
    - §C covers every YES slot
    - Every §D row classified (no SAFE_BY_DESIGN)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§7 section file complete; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §7 catalog section authored.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-07-SUMMARY.md` when done.
</output>
