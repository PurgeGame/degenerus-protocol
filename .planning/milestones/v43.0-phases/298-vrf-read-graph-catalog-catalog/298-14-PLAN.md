---
phase: 298-vrf-read-graph-catalog-catalog
plan: 14
type: execute
wave: 2
depends_on: [298-01, 298-02, 298-03, 298-04, 298-05, 298-06, 298-07, 298-08, 298-09, 298-10, 298-11, 298-12, 298-13]
files_modified:
  - .planning/RNGLOCK-CATALOG.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06]
must_haves:
  truths:
    - "`.planning/RNGLOCK-CATALOG.md` exists at phase close with §0 executive summary + §1..§13 per-consumer sections + §14 unique-slot index + §15 per-slot writer table + §16 verdict matrix + §17 CAT-06 grep-gate attestation"
    - "Every one of the 13 consumer entries from D-298-CONSUMER-LIST-01 appears verbatim with file:line citation in §1..§13"
    - "§14 unique-slot index deduplicates every participating slot from the 13 per-consumer SLOAD tables"
    - "§15 per-slot writer enumeration aggregates every (slot × writer × callsite) tuple from the 13 sections + adds writers found only via the fresh-sweep grep"
    - "§16 verdict matrix classifies every (slot × writer × callsite) tuple ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION} — zero SAFE_BY_DESIGN classifications"
    - "Every §16 VIOLATION row has a §16 Recommended-tactic column entry ∈ {(a), (b), (c), (d)} + ≤80-char Rationale column entry"
    - "§17 records 5 grep pattern hit counts + per-hit cross-reference to a catalog row OR a deliberate-exclusion attestation"
    - "Zero `contracts/` + zero `test/` files modified across all 14 plans"
  artifacts:
    - path: ".planning/RNGLOCK-CATALOG.md"
      provides: "Aggregated v43.0 VRF read-graph catalog deliverable (CAT-01..06)"
      contains: "## §0"
    - path: ".planning/RNGLOCK-CATALOG.md"
      provides: "Per-consumer sections §1..§13 + unique-slot index §14 + writer table §15 + verdict matrix §16 + grep-gate attestation §17"
      contains: "## §17"
  key_links:
    - from: ".planning/RNGLOCK-CATALOG.md §16 verdict matrix"
      to: "v44.0 FIX-MILESTONE planning"
      via: "Per-VIOLATION row carries v44.0 handoff anchor `D-43N-V44-HANDOFF-NN` (locked-decision ID placeholder per REQUIREMENTS FIXREC-05)"
      pattern: "D-43N-V44-HANDOFF-"
    - from: ".planning/RNGLOCK-CATALOG.md §17 CAT-06 attestation"
      to: ".planning/RNGLOCK-CATALOG.md §15 writer table"
      via: "Each of the 5 grep patterns reports hit count + cross-reference (every external/public function + every inline-assembly slot directive + every raw sstore + every storage variable declaration accounted for)"
      pattern: "Pattern [1-5]:"
---

<objective>
Aggregate the 13 per-consumer §1..§13 catalog sections (Wave-1 outputs from plans 298-01..298-13) into the canonical `.planning/RNGLOCK-CATALOG.md` artifact per D-298-CATALOG-LAYOUT-01. Author §0 executive summary, §14 unique-slot index, §15 per-slot writer table, §16 (slot × writer × callsite) verdict matrix with classifications + remediation-tactic recommendations, and §17 CAT-06 grep-gate completeness attestation as a FRESH-SWEEP independent grep audit of `contracts/` (no reliance on the per-consumer sub-agent outputs for completeness coverage).

Purpose: This is the load-bearing v43.0 catalog deliverable. Phase 299 FIXREC consumes §16 verdict matrix to author per-VIOLATION FIXREC entries. Phase 300 ADMA consumes §15 writer enumeration to identify admin functions writing participating slots. Phase 301 FUZZ consumes §1..§13 consumer surface enumeration for ≥1-fuzz-case-per-consumer coverage attestation. Phase 303 TERMINAL cross-references §16 in FINDINGS-v43.0.md §3.A.

Output: `.planning/RNGLOCK-CATALOG.md` (single canonical artifact) — 18 sections (§0 + §1..§13 + §14 + §15 + §16 + §17).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-04-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-06-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-07-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-08-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-10-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-11-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-12-CATALOG-section.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md
</context>

<tasks>

<task type="auto">
  <name>Task 14.1: Aggregate 13 per-consumer sections into §1..§13 + author §14 unique-slot index + §15 per-slot writer table</name>
  <files>.planning/RNGLOCK-CATALOG.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CATALOG-LAYOUT-01 18-section structure; D-298-EXEMPT-REACH-01 per-callsite verdict; D-298-EXEMPT-CROSSCONTRACT-01 cross-contract EXEMPT propagation; D-298-RECOMMEND-DEPTH-01 ≤80-char rationale)
    - All 13 per-consumer section files: `.planning/phases/298-vrf-read-graph-catalog-catalog/298-{01..13}-CATALOG-section.md`
    - .planning/milestones/v41.0-phases/287-jackpot-influence-surface-closure-jpsurf/287-01-JPSURF-AUDIT.md (Phase 287 JPSURF format precedent at scaled-up granularity)
    - feedback_verify_call_graph_against_source.md (explicit-enumeration discipline carries into aggregation)
  </read_first>
  <action>
    Read all 13 per-consumer section files. Author `.planning/RNGLOCK-CATALOG.md` with the following structure:

    **§0 — Executive Summary** (authored AFTER §16 + §17 complete; emits metrics): total VRF consumers (= 13 per D-298-CONSUMER-LIST-01); total participating slots (deduped count from §14); total writers enumerated (count from §15); total VIOLATION rows in §16; total EXEMPT rows in §16 grouped by class (EXEMPT-ADVANCEGAME / EXEMPT-VRFCALLBACK / EXEMPT-RETRYLOOTBOXRNG); top-line recommendation if any structural pattern is visible across multiple consumers. Inline metrics paragraph; no headline-only.

    **§1..§13 — Per-consumer sections.** For each consumer entry NN, copy the contents of `298-{NN}-CATALOG-section.md` verbatim under heading `## §{NN} — {consumer entry name + file:line}`. Preserve the embedded `## CAT-01 (§A)` / `## CAT-02 (§B)` / `## CAT-03 (§C)` / `## CAT-04 (§D)` / `## CAT-06 (§E)` sub-headings as `### CAT-01` / etc. so aggregation produces a single hierarchical document.

    **§14 — Unique-slot index.** Deduplicate every `Participating? = YES` slot across §1..§13's §B tables into one master slot list. Each row: `Slot | Module/Contract | Storage layout type | Consumers (§1..§13 backref list — which consumer sections SLOAD this slot)`. Backref column is `§1, §2, §10` style; a single slot referenced by multiple consumers appears once with multi-consumer backref.

    **§15 — Per-slot writer enumeration.** For each unique participating slot in §14, merge writer rows from §1..§13's §C tables. Add a `Source consumers` column listing which §1..§13 §C tables surfaced that writer. Each row: `Slot | Writer fn | Writer fn file:line | Callsite file:line | Source consumers (§N list)`. The per-callsite granularity from D-298-EXEMPT-REACH-01 is preserved — same writer fn at different callsites = separate rows.

    Do NOT author §16 or §17 in this task — those are Tasks 14.2 + 14.3.

    Important: Do NOT modify any `contracts/` or `test/` files. The aggregator is read-only on source.
  </action>
  <verify>
    <automated>test -f .planning/RNGLOCK-CATALOG.md && grep -q "## §0" .planning/RNGLOCK-CATALOG.md && grep -q "## §1 " .planning/RNGLOCK-CATALOG.md && grep -q "## §2 " .planning/RNGLOCK-CATALOG.md && grep -q "## §3 " .planning/RNGLOCK-CATALOG.md && grep -q "## §4 " .planning/RNGLOCK-CATALOG.md && grep -q "## §5 " .planning/RNGLOCK-CATALOG.md && grep -q "## §6 " .planning/RNGLOCK-CATALOG.md && grep -q "## §7 " .planning/RNGLOCK-CATALOG.md && grep -q "## §8 " .planning/RNGLOCK-CATALOG.md && grep -q "## §9 " .planning/RNGLOCK-CATALOG.md && grep -q "## §10 " .planning/RNGLOCK-CATALOG.md && grep -q "## §11 " .planning/RNGLOCK-CATALOG.md && grep -q "## §12 " .planning/RNGLOCK-CATALOG.md && grep -q "## §13 " .planning/RNGLOCK-CATALOG.md && grep -q "## §14" .planning/RNGLOCK-CATALOG.md && grep -q "## §15" .planning/RNGLOCK-CATALOG.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/RNGLOCK-CATALOG.md` exists
    - File contains `## §0` heading
    - File contains all 13 per-consumer `## §1 ` through `## §13 ` headings
    - File contains `## §14` unique-slot index heading
    - File contains `## §15` per-slot writer table heading
    - §14 contains a deduplicated slot list with `Consumers (§N backref)` column populated for every row
    - §15 covers every slot listed in §14 (no missing slot)
    - §15 preserves per-callsite granularity (same writer fn at different callsites = separate rows)
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>RNGLOCK-CATALOG.md exists with §0 + §1..§13 + §14 + §15 sections populated.</done>
</task>

<task type="auto">
  <name>Task 14.2: Author §16 verdict matrix with per-tuple classification + remediation-tactic recommendations</name>
  <files>.planning/RNGLOCK-CATALOG.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-EXEMPT-REACH-01 + D-298-EXEMPT-CROSSCONTRACT-01 classification rules; D-298-RECOMMEND-DEPTH-01 single-tactic + ≤80-char rationale)
    - .planning/RNGLOCK-CATALOG.md (§14 + §15 just authored)
    - .planning/REQUIREMENTS.md (FIXREC-05 D-43N-V44-HANDOFF-NN locked-decision-ID placeholder convention; v44.0 FIX-MILESTONE handoff anchor format)
    - contracts/modules/DegenerusGameMintModule.sol (MintModule:1221 rngLockedFlag gate convention — reference pattern for tactic (a) gated-revert rationale)
    - contracts/BurnieCoinflip.sol (BurnieCoinflip:730 rngLockedFlag gate convention)
    - contracts/StakedDegenerusStonk.sol (sStonk:492 rngLockedFlag gate convention)
  </read_first>
  <action>
    Append `## §16 — Verdict Matrix (slot × writer × callsite)` to `.planning/RNGLOCK-CATALOG.md`.

    For every (slot × writer × callsite) tuple in §15, emit one row in §16 with columns:
    `Slot | Writer fn | Callsite (file:line) | Reached from EXEMPT stack? | Classification | Recommended tactic | Rationale | v44.0 handoff anchor`

    Classification rules per D-298-EXEMPT-REACH-01 + D-298-EXEMPT-CROSSCONTRACT-01:
    - Callsite reachable from `advanceGame()` resolution stack → `EXEMPT-ADVANCEGAME`
    - Callsite reachable from VRF coordinator callback stack → `EXEMPT-VRFCALLBACK`
    - Callsite reachable from `retryLootboxRng` failsafe stack → `EXEMPT-RETRYLOOTBOXRNG`
    - Callsite NOT reached from any of the 3 EXEMPT stacks → `VIOLATION`
    - The SAME writer fn at different callsites can carry different classifications (per-callsite granularity)
    - NO SAFE_BY_DESIGN classification (v43.0 milestone goal explicitly precludes it per D-43N-AUDIT-ONLY-01)

    Cross-classify per D-298-EXEMPT-CROSSCONTRACT-01: a writer in contract Y reached from contract X's EXEMPT stack propagates the EXEMPT class across the contract boundary.

    For every row classified `VIOLATION`:
    - `Recommended tactic` column = one of `(a)`, `(b)`, `(c)`, `(d)`:
      * `(a)` rngLockedFlag-gated revert — recommended for admin/external-only writers; cite existing MintModule:1221 / BurnieCoinflip:730 / sStonk:492 convention
      * `(b)` snapshot/anchor pattern — recommended when the slot semantically must remain unmodified during resolution but the value itself can advance asynchronously; cite Phase 281 owed-salt or Phase 288 dailyIdx precedent
      * `(c)` pre-lock reorder — recommended when the write naturally happens before rngLock anyway; restructure to make it explicit
      * `(d)` immutable — recommended when the slot value is effectively constant post-deployment
    - `Rationale` column ≤80 chars (e.g. `"eliminates ALL cross-call re-derivation; zero new storage slot"`) per D-298-RECOMMEND-DEPTH-01
    - `v44.0 handoff anchor` column = `D-43N-V44-HANDOFF-NN` placeholder where NN is the per-VIOLATION ordinal (`01`, `02`, ...) numbered sequentially across the §16 verdict matrix

    EXEMPT-class rows have blank `Recommended tactic` / `Rationale` / `v44.0 handoff anchor` columns (no tactic recommendation needed).

    Sort §16 by slot (alphabetical / storage-layout order). Within each slot, sort by writer fn then callsite.

    No SAFE_BY_DESIGN, no blank Classification, no missing tactic on VIOLATION rows.
  </action>
  <verify>
    <automated>grep -q "## §16" .planning/RNGLOCK-CATALOG.md && ! grep -q "SAFE_BY_DESIGN" .planning/RNGLOCK-CATALOG.md && grep -qE "EXEMPT-(ADVANCEGAME|VRFCALLBACK|RETRYLOOTBOXRNG)|VIOLATION" .planning/RNGLOCK-CATALOG.md && bash -c '! grep -q "VIOLATION" .planning/RNGLOCK-CATALOG.md || (grep -c "D-43N-V44-HANDOFF-" .planning/RNGLOCK-CATALOG.md | awk "{ exit !(\$1 >= 1) }")' && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `## §16` heading present in `.planning/RNGLOCK-CATALOG.md`
    - Zero `SAFE_BY_DESIGN` tokens anywhere in the file
    - At least one of each classification token present (EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG appear since the 3 exempt entry points each touch slots; VIOLATION may be zero only if every tuple is structurally exempt)
    - Every row classified as `VIOLATION` carries: Recommended tactic ∈ {(a), (b), (c), (d)}, ≤80-char Rationale, `D-43N-V44-HANDOFF-NN` anchor
    - VIOLATION row handoff anchors numbered sequentially (01, 02, ...) — at least 1 anchor if any VIOLATION exists
    - Per-callsite granularity preserved (same writer fn at different callsites = separate rows)
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§16 verdict matrix appended with classifications + remediation tactics + v44.0 handoff anchors.</done>
</task>

<task type="auto">
  <name>Task 14.3: Author §17 CAT-06 grep-gate completeness attestation (fresh-sweep)</name>
  <files>.planning/RNGLOCK-CATALOG.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-GREP-GATE-01: 5 grep patterns + main-context fresh-sweep self-attestation)
    - .planning/REQUIREMENTS.md (CAT-06 verbatim — grep patterns: `function .*external`, `function .*public`, `slot:`, `assembly { sstore`, every storage variable declaration)
    - .planning/RNGLOCK-CATALOG.md (§15 writer table just authored — for cross-referencing grep hits)
  </read_first>
  <action>
    Append `## §17 — CAT-06 Grep-Gate Completeness Attestation` to `.planning/RNGLOCK-CATALOG.md`.

    Run all 5 grep patterns FRESH (do NOT rely on sub-agent outputs for completeness coverage). For each pattern: record literal command, hit count, then per-hit disposition.

    Pattern 1: External-function sweep
    ```
    grep -rn "function .*external" contracts/ --include="*.sol"
    ```

    Pattern 2: Public-function sweep
    ```
    grep -rn "function .*public" contracts/ --include="*.sol"
    ```

    Pattern 3: Inline-assembly slot-directive sweep
    ```
    grep -rn "slot:" contracts/ --include="*.sol"
    ```

    Pattern 4: Inline-assembly raw sstore sweep
    ```
    grep -rn "assembly { sstore" contracts/ --include="*.sol"
    ```

    Pattern 5: Storage variable declaration sweep — per top-level + per-module contract file, grep for top-level state-variable declarations matching:
    ```
    grep -rnE '^\s*(mapping|uint|int|address|bool|bytes|string|struct)\s+\w' contracts/ --include="*.sol"
    ```

    For each pattern, record in §17:
    - The literal grep command used
    - Total hit count (integer)
    - Per-hit disposition table with columns: `File:Line | Match text | Disposition`. Disposition is one of:
      * `Catalog row §15:{slot}:{writer-fn}` — hit corresponds to a writer enumerated in §15
      * `Catalog row §B:{consumer-§N}:{slot}` — hit is a SLOAD already captured in §B of consumer §N (only applies to inline-asm slot-directive Pattern 3 hits when the asm reads a slot)
      * `Deliberate exclusion: view function` — pure view function under Pattern 1/2 that writes nothing
      * `Deliberate exclusion: not a writer of any participating slot` — function/declaration that does not touch the participating-slot set
      * `Deliberate exclusion: non-participating slot declaration` — Pattern 5 hit for a storage var that is not a participating slot per §B classification
    - NO hit may be left undispositioned. Every grep hit must point to a catalog row OR a deliberate-exclusion attestation.

    Cross-coverage check (final §17 paragraph): for every writer in §15, verify the corresponding `function .*external` or `function .*public` declaration appears in Pattern 1 or Pattern 2 hits.

    **OZ-inherited writer carve-out (D-298-OZ-CARVEOUT-01, applied here per plan-checker W-02 fix):** OpenZeppelin-inherited writers (`_mint`, `_burn`, `transfer`, `transferFrom`, `approve`, `permit`, `_transfer`, `_approve`, `_spendAllowance`, and other ERC20/ERC721 standard methods sourced from `node_modules/@openzeppelin/` or equivalent lib path) live outside `contracts/` and will structurally NOT appear in Pattern 1/2 hits. For these writers, emit a dedicated `OZ-inherited writer (expected absence from contracts/ grep)` disposition row for the cross-coverage check — these are NOT missing-writer discrepancies. The §15 row's `Writer fn file:line` column already carries the inherited-method source path (lib/node_modules) for traceability.

    Emit `Cross-coverage: PASS` if every §15 writer is matched by either (a) a Pattern 1/2 hit OR (b) the OZ-inherited-writer carve-out. Otherwise list the actual missing writers (those NOT covered by either path) and emit `Cross-coverage: discrepancy found` with the list.

    If the cross-coverage check produces actual missing writers (post OZ-carve-out), this indicates either a missed grep pattern OR a writer enumeration error. Stop, surface the discrepancy for the planner to investigate, do NOT silently continue.

    Once §17 is complete, return to §0 and author the executive summary using the final §16 + §17 metrics.
  </action>
  <verify>
    <automated>grep -q "## §17" .planning/RNGLOCK-CATALOG.md && grep -q "Pattern 1:" .planning/RNGLOCK-CATALOG.md && grep -q "Pattern 2:" .planning/RNGLOCK-CATALOG.md && grep -q "Pattern 3:" .planning/RNGLOCK-CATALOG.md && grep -q "Pattern 4:" .planning/RNGLOCK-CATALOG.md && grep -q "Pattern 5:" .planning/RNGLOCK-CATALOG.md && grep -q "Cross-coverage: PASS" .planning/RNGLOCK-CATALOG.md && ! grep -q "SAFE_BY_DESIGN" .planning/RNGLOCK-CATALOG.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `## §17` heading present
    - All 5 grep patterns referenced as `Pattern 1:` through `Pattern 5:`
    - Each pattern records: literal command + hit count + per-hit disposition table
    - Cross-coverage check authored as final §17 paragraph; PASS verdict (every §15 writer appears in Pattern 1 or Pattern 2 hits) OR discrepancy listed
    - §0 executive summary updated with final §16 + §17 metrics (total VRF consumers / participating slots / writers / VIOLATIONs / EXEMPT rows by class)
    - Zero `SAFE_BY_DESIGN` tokens anywhere
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§17 attestation complete + cross-coverage PASS + §0 executive summary populated.</done>
</task>

</tasks>

<verification>
- `.planning/RNGLOCK-CATALOG.md` exists with §0 + §1..§13 + §14 + §15 + §16 + §17 sections
- §16 contains classifications ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION}; zero SAFE_BY_DESIGN
- Every §16 VIOLATION row has tactic + rationale + `D-43N-V44-HANDOFF-NN` anchor
- §17 records 5 grep patterns + hit counts + per-hit disposition + cross-coverage PASS verdict
- §0 executive summary populated with final metrics
- Zero `contracts/` + zero `test/` files modified across all 14 plans (`git diff --name-only HEAD~14 HEAD -- contracts/ test/` returns empty for the full phase delta)
</verification>

<success_criteria>
The canonical `.planning/RNGLOCK-CATALOG.md` artifact ships with all 6 CAT requirements (CAT-01..06) satisfied. Phase 299 FIXREC + Phase 300 ADMA + Phase 301 FUZZ can consume the artifact as load-bearing input per the v43.0 milestone phase dependency graph.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-14-SUMMARY.md` when done. Summary records: total participating slot count; total VIOLATION count; CAT-05 grep-gate verdict (PASS/discrepancy); v44.0 handoff anchor count.
</output>
