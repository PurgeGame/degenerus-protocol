---
phase: 298-vrf-read-graph-catalog-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §1 (JackpotModule.payDailyJackpot at JackpotModule.sol:339) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD inside the resolution path is enumerated with file:line citation (no 'by construction' / 'covered by single fn' shortcuts)"
    - "Every participating slot (Participating? = YES) has its full external/public writer set enumerated"
    - "Every (slot × writer × callsite) tuple under §1's slot set carries a classification ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION}"
    - "Every VIOLATION row carries a remediation tactic ∈ {gated-revert, snapshot-anchor, pre-lock-reorder, immutable} + 1-line rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md"
      provides: "Per-consumer §1 catalog content (CAT-01+CAT-02+CAT-03+CAT-04+CAT-06 rows for payDailyJackpot)"
      contains: "## CAT-01"
  key_links:
    - from: "298-01-CATALOG-section.md §B SLOAD table"
      to: "298-01-CATALOG-section.md §C writer enumeration"
      via: "Participating? = YES slots forward into §C; NON-PARTICIPATING attestation rows do not"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `JackpotModule.payDailyJackpot` (`contracts/modules/DegenerusGameJackpotModule.sol:339`) per D-298-CONSUMER-LIST-01 entry §1. Produce the §1 catalog section.

Purpose: Phase 298 CAT-01..06 coverage for VRF-consumer entry §1 (daily ETH/whalepass distribution; Phase 287 JPSURF prior coverage at scaled granularity). Output is one of 13 per-consumer section files that Wave-2 aggregates into `.planning/RNGLOCK-CATALOG.md`.

Output: `.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md` with §A traced function set + §B SLOAD table (all SLOADs reached, Participating? YES/NO + attestation if NO) + §C per-participating-slot writer enumeration + §D verdict-matrix rows + §E remediation tactic per VIOLATION row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameJackpotModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Sub-agent backward-trace from payDailyJackpot</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (LOAD-BEARING decision IDs: D-298-CONSUMER-LIST-01 entry §1; D-298-TRACE-DEPTH-01 all-source contracts; D-298-SLOT-CLASSIFICATION-01 two-tier; D-298-EXEMPT-REACH-01 stack-rooted per-callsite; D-298-EXEMPT-CROSSCONTRACT-01 cross-contract EXEMPT propagation; D-298-RECOMMEND-DEPTH-01 one tactic + ≤80-char rationale; D-298-SUB-AGENT-PROMPT-01 prompt skeleton)
    - contracts/modules/DegenerusGameJackpotModule.sol (consumer entry at :339)
    - .planning/milestones/v41.0-phases/287-jackpot-influence-surface-closure-jpsurf/287-01-JPSURF-AUDIT.md (Phase 287 JPSURF format precedent: SLOAD table column shape + Load-bearing-for-winner-selection column → Participating? column)
    - feedback_rng_backward_trace.md (every RNG audit traces BACKWARD from each consumer)
    - feedback_rng_window_storage_read_freshness.md (enumerate ALL SLOADs, not just VRF-derived seeds; F-41-02/03 precedent)
    - feedback_rng_commitment_window.md (check what player-controllable state can change between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md (grep-verified enumeration; no 'by construction' / 'single fn reaches all paths' claims; Phase 294 BURNIE gap precedent)
  </read_first>
  <action>
    Dispatch a single general-purpose sub-agent (Task tool) with the per-consumer prompt instantiated for §1 entry. The sub-agent's job:

    (1) Read `contracts/modules/DegenerusGameJackpotModule.sol` and locate the `payDailyJackpot` function at line 339. Walk transitively into every internal/external function reached from that entry, walking across module/contract boundaries into anything under `contracts/` per D-298-TRACE-DEPTH-01. Stop only at external interfaces with no source available (Chainlink VRF coordinator, opaque oracle mocks).

    (2) Build §A — "Traced function set". List every internal/external function reached transitively from `payDailyJackpot` with file:line citation. Include cross-module hops (e.g., into `DegenerusVault`, `BurnieCoinflip`, `JackpotBucketLib`, `EntropyLib`, `TraitUtils`). Explicit enumeration only — NO 'by construction' / 'covered by single fn' shortcuts (Phase 294 BURNIE gap precedent).

    (3) Build §B — "SLOAD table". For every storage read reached anywhere in §A's function set, emit one row with columns: `Slot | Read-site (file:line) | Read context | Participating? (YES/NO) | Attestation if NO`. Participating = the value influences any VRF-derived output (jackpot recipient, jackpot amount, trait award, lootbox ticket, hero-override outcome, etc.). NON-PARTICIPATING rows (accounting SLOADs like `claimablePool` inside `_addClaimableEth`, `yieldAccumulator` for yield obligation calc) carry a 1-line attestation explaining why the value drives no VRF output per D-298-SLOT-CLASSIFICATION-01. Enumerate ALL SLOADs per `feedback_rng_window_storage_read_freshness.md` (F-41-02/03 precedent) — DO NOT filter to VRF-derived seeds.

    (4) Build §C — "Per-participating-slot writer enumeration". For each `Participating? = YES` slot in §B, enumerate EVERY external/public function across all `contracts/` that writes the slot. Include OZ-inherited writers (`transfer`, `transferFrom`, `approve`, `_mint`, `_burn`) where applicable; admin/owner functions; affiliate-registration writers; anything reachable from a non-internal entry point. Each writer entry: `Slot | Writer fn | Writer file:line | Callsite file:line` (per-callsite per D-298-EXEMPT-REACH-01).

    (5) Build §D — "Verdict-matrix rows". For each (slot × writer × callsite) tuple emitted in §C, classify into one of `EXEMPT-ADVANCEGAME | EXEMPT-VRFCALLBACK | EXEMPT-RETRYLOOTBOXRNG | VIOLATION` per D-298-EXEMPT-REACH-01 stack-rooted strict + per-callsite + D-298-EXEMPT-CROSSCONTRACT-01 cross-contract EXEMPT propagation. NO SAFE_BY_DESIGN escape hatch per v43.0 milestone goal + D-43N-AUDIT-ONLY-01. Any callsite NOT reached from one of the 3 EXEMPT stacks (advanceGame / VRF coordinator callback / retryLootboxRng) = VIOLATION.

    (6) Build §E — "Remediation tactic per VIOLATION row". For each VIOLATION row in §D, emit one tactic from the menu `(a) rngLockedFlag-gated revert | (b) snapshot/anchor pattern | (c) pre-lock reorder | (d) immutable` + ≤80-char rationale per D-298-RECOMMEND-DEPTH-01. Reference existing patterns where appropriate (MintModule:1221 / BurnieCoinflip:730 / sStonk:492 for tactic (a); Phase 281 owed-salt or Phase 288 dailyIdx for tactic (b)).

    Write the output to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md` with the heading `# §1 — JackpotModule.payDailyJackpot (file:line 339)` and inline sub-sections `## CAT-01 (§A)`, `## CAT-02 (§B)`, `## CAT-03 (§C)`, `## CAT-04 (§D)`, `## CAT-06 (§E)`.

    Sub-agent prompt skeleton per D-298-SUB-AGENT-PROMPT-01 — adapt prose; the planner-finalized version is:

    > Backward-trace VRF-derived entropy from `JackpotModule.payDailyJackpot` at `contracts/modules/DegenerusGameJackpotModule.sol:339`. Walk every reachable SLOAD inside the resolution code path with EXPLICIT file:line citation per `feedback_verify_call_graph_against_source.md` — NO "by construction" / "covered by single fn" claims. Trace walks into every source contract under `contracts/`; stops only at external interfaces with no source available. Produce §A (traced function set) + §B (full SLOAD table with Participating? column + attestation for NO rows per `feedback_rng_window_storage_read_freshness.md`) + §C (per-participating-slot writer enumeration; include OZ-inherited transfer/transferFrom/approve/_mint/_burn where applicable; per-callsite) + §D (per-tuple verdict matrix; classifications ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION}; NO SAFE_BY_DESIGN) + §E (per-VIOLATION tactic ∈ {a, b, c, d} + ≤80-char rationale per D-298-RECOMMEND-DEPTH-01). Read-only: do NOT modify `contracts/` or `test/`. Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md`.

    Note: Phase 287 JPSURF audit covered jackpot SLOADs with 27 rows (`Load-bearing for winner-selection?` column). The §1 SLOAD table will likely overlap with that prior catalog but at v43 granularity may add rows that Phase 287 marked NEGATIVE-scope (the freeze invariant is strictly broader than winner-selection load-bearing).

    After the sub-agent writes the file, verify the file exists + contains all 5 CAT headings + every §D row has a classification token + every §E row has a tactic token.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-CATALOG-section.md` exists
    - File contains `## CAT-01`, `## CAT-02`, `## CAT-03`, `## CAT-04`, `## CAT-06` sub-headings
    - §B SLOAD table is a single contiguous markdown table with `Participating?` column populated YES/NO on every row; every NO row carries an attestation
    - §C writer enumeration covers every `Participating? = YES` slot from §B (no skipped slot)
    - Every §D row carries a classification token ∈ {`EXEMPT-ADVANCEGAME`, `EXEMPT-VRFCALLBACK`, `EXEMPT-RETRYLOOTBOXRNG`, `VIOLATION`} (no SAFE_BY_DESIGN, no blank)
    - Every §D row classified `VIOLATION` has a matching §E entry with tactic ∈ {(a), (b), (c), (d)} and ≤80-char rationale
    - No 'by construction' / 'covered by single fn' / 'all paths' shortcuts appear in §A or §B (`grep -i "by construction\\|covered by single fn"` returns zero hits)
    - Zero `contracts/` + zero `test/` files modified (`git diff --name-only HEAD` excludes both prefixes)
  </acceptance_criteria>
  <done>§1 section file exists, all 5 CAT sub-headings present, classifications complete, no SAFE_BY_DESIGN tokens, zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; contains 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §1 catalog section file authored + ready for Wave-2 aggregation.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-01-SUMMARY.md` when done.
</output>
