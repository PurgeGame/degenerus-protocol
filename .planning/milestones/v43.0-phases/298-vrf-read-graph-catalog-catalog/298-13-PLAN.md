---
phase: 298-vrf-read-graph-catalog-catalog
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §13 (DecimatorModule._awardDecimatorLootbox cluster at :573 + cross-call re-read at :771) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated (with explicit cross-call decClaimRounds[lvl].rngWord re-read coverage)"
    - "Every participating slot has writer enumeration"
    - "Every tuple classified"
    - "Every VIOLATION has tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md"
      provides: "§13 catalog content for DecimatorModule lootbox award cluster"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from the `DecimatorModule._awardDecimatorLootbox` callsite cluster (`contracts/modules/DegenerusGameDecimatorModule.sol:573` rngWord param consumer + `:771` `decClaimRounds[lvl].rngWord` cross-call re-read) per D-298-CONSUMER-LIST-01 entry §13. Produce the §13 catalog section.

Purpose: CAT-01..06 coverage for the decimator lootbox-award consumer cluster. Cross-call SLOAD pattern at `:771` (`decClaimRounds[lvl].rngWord` re-read) is a distinct participating-SLOAD class per F-41-02/03 precedent — must be explicitly enumerated.

Output: `298-13-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameDecimatorModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 13.1: Sub-agent backward-trace from _awardDecimatorLootbox callsite cluster</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §13; all D-298-* decisions)
    - contracts/modules/DegenerusGameDecimatorModule.sol (consumer cluster at :573 + cross-call re-read at :771)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md (F-41-02/03 precedent: cross-call SLOAD re-read is distinct class)
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at the `DecimatorModule._awardDecimatorLootbox` callsite cluster:
    - `:573` — rngWord param consumer (entry from a caller that passes rngWord as argument)
    - `:771` — `decClaimRounds[lvl].rngWord` cross-call re-read (the SAME function reads the stored rngWord from a different storage slot via SLOAD inside a sibling call)

    Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01.

    Special trace focus per F-41-02/03 cross-call read-freshness precedent:
    - The `:771` cross-call re-read of `decClaimRounds[lvl].rngWord` is itself a participating-SLOAD class — must appear in §B with YES + writer enumeration of `decClaimRounds[lvl].rngWord` SSTORE sites in §C
    - Identify whether the read at `:771` happens in the SAME transaction as the write that established `decClaimRounds[lvl].rngWord`, or across blocks — both cases are interesting for the verdict matrix
    - Per D-298-EXEMPT-REACH-01 per-callsite: classify each callsite reaching the cross-call re-read separately

    Produces §A + §B + §C + §D + §E. Explicit-enumeration + ALL-SLOADs + NO SAFE_BY_DESIGN.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md`. Heading `# §13 — DecimatorModule._awardDecimatorLootbox cluster (file:line 573 + 771 cross-call re-read)` + 5 CAT sub-headings.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-13-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-13-CATALOG-section.md` exists with all 5 CAT sub-headings
    - §B Participating? column populated; NO rows attested
    - §B explicitly covers `decClaimRounds[lvl].rngWord` SLOAD at `:771` with `Participating? = YES`
    - §C covers every YES slot (including `decClaimRounds[lvl].rngWord` writers)
    - Every §D row classified (no SAFE_BY_DESIGN)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§13 section file complete with cross-call SLOAD coverage; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; cross-call re-read SLOAD explicitly enumerated; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §13 catalog section authored (DecimatorModule lootbox-award cluster + cross-call re-read).
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-13-SUMMARY.md` when done.
</output>
