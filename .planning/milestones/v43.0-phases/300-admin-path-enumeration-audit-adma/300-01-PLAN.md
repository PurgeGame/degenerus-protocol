---
phase: 300-admin-path-enumeration-audit-adma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ADMIN-AUDIT.md
autonomous: true
requirements: [ADMA-01, ADMA-02, ADMA-03, ADMA-04]
must_haves:
  truths:
    - "Canonical artifact .planning/ADMIN-AUDIT.md exists at phase close per D-300-ADMA-LAYOUT-01"
    - "§1 enumerates every admin-gated external/public function in contracts/ scope (formal modifiers + hand-rolled msg.sender != ContractAddresses.ADMIN/CREATOR + vault.isVaultOwner(msg.sender) gates), each with file:line + role-gate annotation + admin-class classification, per ADMA-01"
    - "§1.E catalog-erratum subsection records RNGLOCK-CATALOG.md §15 S-06 + §16 V-016/V-017/V-018 + §C.3.2/C.3.3 phantom-row carry forward and the S-06 ADMA verdict (zero admin-class writers)"
    - "§2 cross-reference table marks each admin function with its participating-slot write set keyed against RNGLOCK-CATALOG.md §15 row(s), per ADMA-02"
    - "§3 contains one per-admin-function recommendation entry for every admin function whose §2 row reaches a participating slot at any non-EXEMPT callsite, per ADMA-03 + ADMA-04"
    - "Every §3 recommendation row carries a D-43N-V44-ADMA-NN handoff anchor numbered sequentially from row 1"
    - "§4 consolidated v44.0 FIX-MILESTONE handoff register lists every D-43N-V44-ADMA-NN ID emitted in §3 (deduplicated; ordered numerically) plus the D-43N-V44-ADMA-ERRATUM-01 catalog-erratum carry-forward entry"
    - "§5 grep-completeness gate attestation records hit counts for onlyOwner / onlyAdmin / onlyRole / hand-rolled ADMIN/CREATOR/isVaultOwner patterns and reconciles each hit against an §1 row or a deliberate-exclusion attestation; includes negative-confirmation grep that adminSeedTraitBucket|adminClearTraitBucket returns 0 hits"
    - "§0 executive summary present (admin-function count by role-gate type; participating-slot-writer subset count; recommendation count by admin-class; v44.0 handoff anchor count) and serves as the Phase 303 §3.E ADMA roll-up source"
    - "Zero contracts/ + zero test/ source-tree mutations across the phase (git diff --name-only HEAD against contracts/ + test/ returns empty)"
    - "RNGLOCK-CATALOG.md untouched (Phase 298 closed artifact); KNOWN-ISSUES.md untouched per D-300-KI-01"
    - "Zero SAFE_BY_DESIGN tokens appear anywhere in ADMIN-AUDIT.md"
  artifacts:
    - path: ".planning/ADMIN-AUDIT.md"
      provides: "Canonical Phase 300 ADMA deliverable per D-300-ADMA-LAYOUT-01"
      contains: "## §0"
    - path: ".planning/ADMIN-AUDIT.md"
      provides: "§1 admin enumeration + §2 participating-slot cross-reference + §3 per-admin-function recommendation table + §4 v44.0 consolidated handoff register + §5 grep-completeness gate"
      contains: "## §5"
  key_links:
    - from: ".planning/ADMIN-AUDIT.md §2 cross-reference rows"
      to: ".planning/RNGLOCK-CATALOG.md §15 per-slot writer table"
      via: "writer fn + file:line citation matching CAT-03 enumeration"
      pattern: "S-[0-9]+"
    - from: ".planning/ADMIN-AUDIT.md §3 recommendation rows"
      to: ".planning/RNGLOCK-CATALOG.md §16 verdict-matrix row"
      via: "callsite file:line + slot identity cross-reference (a row in §3 maps to one or more §16 VIOLATION rows when the admin function reaches a participating slot)"
      pattern: "V-[0-9]+"
    - from: ".planning/ADMIN-AUDIT.md §4 consolidated handoff register"
      to: "v44.0 FIX-MILESTONE plan-phase input"
      via: "deduplicated D-43N-V44-ADMA-NN ID list (one per §3 row) + D-43N-V44-ADMA-ERRATUM-01 catalog-erratum entry"
      pattern: "D-43N-V44-ADMA-[0-9A-Z-]+"
---

<objective>
Author the canonical Phase 300 ADMA deliverable `.planning/ADMIN-AUDIT.md` per `D-300-ADMA-LAYOUT-01` — a single AGENT-COMMITTED audit bundle with six sections (§0 + §1 + §2 + §3 + §4 + §5) that enumerates every admin-gated external in `contracts/`, cross-references each against the Phase 298 RNGLOCK-CATALOG §15 writer table, emits a per-admin-function recommendation entry for every admin function reaching a participating slot at a non-EXEMPT callsite, and consolidates the v44.0 FIX-MILESTONE handoff anchors. §1 also records (in §1.E) a catalog-erratum carry-forward for RNGLOCK-CATALOG §15 S-06 + §16 V-016/V-017/V-018 phantom rows (admin trait-bucket writers that do not exist in source).

Wave shape per D-300-WAVE-SHAPE-01: 1 AGENT-COMMITTED ADMA artifact bundle. Per D-300-EXEC-SHAPE-01, this plan uses the main-context end-to-end shape (default), not parallel sub-agents — the admin surface is bounded (~37 functions across ~9 files including formal `onlyOwner` / `onlyVaultOwner` modifiers plus hand-rolled `msg.sender != ContractAddresses.ADMIN` / `CREATOR` and `vault.isVaultOwner(msg.sender)` gates at external entry points), the work is sequentially dependent (§1 enumeration → §2 cross-reference → §3 recommendations → §0/§4/§5 aggregation), and the single deliverable does not benefit materially from parallelization.

AUDIT-ONLY posture per `D-43N-AUDIT-ONLY-01`: zero `contracts/` mutations + zero `test/` mutations. All admin-revert-gating contract changes deferred to v44.0 FIX-MILESTONE via `D-43N-V44-ADMA-NN` handoff anchors emitted in §3 + consolidated in §4. KNOWN-ISSUES.md UNMODIFIED per `D-300-KI-01`. RNGLOCK-CATALOG.md is Phase 298 closed artifact — read-only; §1.E records the catalog erratum without touching RNGLOCK-CATALOG.md itself.

Output: `.planning/ADMIN-AUDIT.md` (single canonical artifact, 6 sections). This artifact is the load-bearing input for (1) Phase 301 FUZZ-02 action set (every admin/owner function); (2) Phase 303 TERMINAL §3.E ADMA roll-up (sourced from §0 executive summary); (3) v44.0 FIX-MILESTONE plan-phase (sourced from §4 consolidated handoff register).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/300-admin-path-enumeration-audit-adma/300-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author §1 — Complete admin function enumeration + §1.E catalog erratum (ADMA-01)</name>
  <files>.planning/ADMIN-AUDIT.md</files>
  <read_first>
    - .planning/phases/300-admin-path-enumeration-audit-adma/300-CONTEXT.md (D-300-ADMA-LAYOUT-01 6-section layout; D-300-ENUM-SCOPE-01 enumeration scope)
    - .planning/RNGLOCK-CATALOG.md §0 (catalog executive summary; informs §0 framing later)
    - .planning/RNGLOCK-CATALOG.md §15 (per-slot writer enumeration — the cross-reference input for Task 2; read now to absorb the writer fn / file:line / callsite shape; pay attention to S-06 rows 154/155/156 which are phantom — see §1.E carry forward below)
    - .planning/RNGLOCK-CATALOG.md §16 (verdict matrix; V-014/V-015 enumerate the actual S-06 writer `_raritySymbolBatch` as EXEMPT-ADVANCEGAME; V-016/V-017/V-018 are phantom — see §1.E carry forward)
    - .planning/RNGLOCK-CATALOG.md §C.3.2 + §C.3.3 (these subsections enumerate the phantom adminSeedTraitBucket / adminClearTraitBucket / helper rows — must be flagged in §1.E)
    - contracts/ContractAddresses.sol (constants used by hand-rolled gates: ADMIN at line 51, CREATOR at line 63, plus integration-trust-boundary constants COIN/COINFLIP/SDGNRS/VAULT/GAME that are NOT admin-class and must be EXCLUDED from §1)
    - contracts/DegenerusGame.sol (top-level admin gates: setLootboxRngThreshold :479 with isVaultOwner gate :480 INCLUDE; adminSwapEthForStEth :1805 with ADMIN gate :1809 INCLUDE; adminStakeEthForStEth :1826 with isVaultOwner :1827 INCLUDE)
    - contracts/DegenerusAdmin.sol (modifier onlyOwner at :436 using isVaultOwner; swapGameEthForStEth :631 INCLUDE; conditional admin-discriminator branches at :507 inside proposeFeedSwap(:479) and :670 inside propose(:647) — these are INTERNAL discriminator branches that route between Admin/Community proposal paths, NOT external admin gates; EXCLUDE with rationale)
    - contracts/DegenerusVault.sol (modifier onlyVault :219 — integration boundary, EXCLUDE; modifier onlyVaultOwner :431 with _isVaultOwner gate :432; the 23 `external onlyVaultOwner` admin entry points at :500 gameAdvance, :513 gamePurchase (multi-line signature; modifier ordering `external payable onlyVaultOwner` at closing :519), :534 gamePurchaseTicketsBurnie, :543 gamePurchaseBurnieLootbox, :551 gameOpenLootBox, :561 gamePurchaseDeityPassFromBoon (`external payable onlyVaultOwner`), :575 gameClaimWinnings, :581 gameClaimWhalePass, :585 gameDegeneretteBet (multi-line signature; modifier ordering `external payable onlyVaultOwner` at closing :601), :620 gameResolveDegeneretteBets, :627 gameSetAutoRebuy, :634 gameSetAutoRebuyTakeProfit, :643 gameSetAfKingMode (multi-line signature; modifier at closing :647), :655 gameSetOperatorApproval, :662 coinDepositCoinflip, :670 coinClaimCoinflips, :677 coinDecimatorBurn, :685 coinSetAutoRebuy, :692 coinSetAutoRebuyTakeProfit, :700 wwxrpMint, :708 jackpotsClaimDecimator, :719 sdgnrsBurn, :725 sdgnrsClaimRedemption; vaultMint :291 and vaultBurn :306 are onlyVault integration callbacks — NOT admin-class and must be EXCLUDED from §1)
    - contracts/StakedDegenerusStonk.sol (modifier onlyGame :276 — EXCLUDE as integration-trust-boundary; check for any owner/admin functions independent of onlyGame)
    - contracts/BurnieCoinflip.sol (modifiers onlyDegenerusGameContract :187 + onlyFlipCreditors :194 + onlyBurnieCoin :205 — ALL integration boundary; EXCLUDE; check for any independent owner/admin setters)
    - contracts/BurnieCoin.sol (modifiers onlyGame :478 + onlyVault :485 — integration boundary; EXCLUDE; check for any owner setters)
    - contracts/GNRUS.sol (modifier onlyGame :256 — integration boundary EXCLUDE; isVaultOwner check at :380 — INCLUDE as admin gate)
    - contracts/DegenerusDeityPass.sol (modifier onlyOwner :80 using isVaultOwner :81; setRenderer :94 INCLUDE; admin setter at :108 INCLUDE)
    - contracts/DegenerusStonk.sol (isVaultOwner gates at :188, :203 — INCLUDE)
    - contracts/DegenerusJackpots.sol (onlyCoin :146 + onlyGame :153 — integration boundary EXCLUDE; check for independent admin setters)
    - contracts/DegenerusQuests.sol (onlyCoin :310 + onlyGame :321 — integration boundary EXCLUDE; check for independent admin setters)
    - contracts/Icons32Data.sol (msg.sender != CREATOR gates at :154, :172, :197 — INCLUDE as CREATOR-class admin)
    - contracts/modules/DegenerusGameAdvanceModule.sol (msg.sender != ADMIN gates at :503 wireVrf, :1682 updateVrfCoordinatorAndSub — INCLUDE; these are governance-class admin)
    - contracts/modules/DegenerusGameMintModule.sol (`_raritySymbolBatch` writes traitBurnTicket (S-06) via inline-asm sstore around :594-:608; the writer is `_raritySymbolBatch` which is INTERNAL-only and reached from advanceGame-chain ADVANCEMODE — RNGLOCK-CATALOG §16 V-014/V-015 correctly enumerates it as EXEMPT-ADVANCEGAME; this is NOT an admin-class writer and contributes ZERO to §1)
    - contracts/modules/DegenerusGameJackpotModule.sol + DegenerusGameDecimatorModule.sol + other modules/*.sol (modules are delegatecall targets so admin gating typically lives in the top-level DegenerusGame.sol entry point that dispatches into the module; verify each module has no independent admin-gated external entry — modules in v42.0 closure are reachable only via DegenerusGame fallback / delegatecall so admin gating is upstream)
    - contracts/DegenerusAffiliate.sol + contracts/WrappedWrappedXRP.sol (low-likelihood admin surface; sweep for any onlyOwner / owner() / isVaultOwner / ADMIN gates)
    - Project memory: feedback_contract_locations.md (only contracts/ directory is authoritative; ignore any stale copies elsewhere)
    - Project memory: feedback_no_history_in_comments.md (authorial discipline carry — describe what IS, not what changed)
    - Project memory: feedback_skeptic_pass_before_catastrophe.md (don't promote any §3 recommendation to CATASTROPHE-tier without skeptic-reviewer filter)
    - Project memory: feedback_verify_call_graph_against_source.md (every cited file:line must be source-verified via fresh grep — DO NOT trust upstream catalog rows that may contain errata; see §1.E)
  </read_first>
  <action>
    Author the canonical `.planning/ADMIN-AUDIT.md` with the document scaffold + §1 admin function enumeration + §1.E catalog-erratum carry forward. Per `D-300-ENUM-SCOPE-01`, scope is all-source admin/owner/role-gated external entry points in `contracts/`. Specifically include:

    **IN SCOPE for §1 enumeration (source-verified at plan-revision time via fresh grep):**
    1. Explicit `onlyOwner` modifier (OZ + custom) callsites at EXTERNAL function declarations — confirmed sites: `DegenerusDeityPass.sol:94` setRenderer, `:108` (modifier defined :80, gated via `vault.isVaultOwner(msg.sender)`); `DegenerusAdmin.sol:631` swapGameEthForStEth (modifier defined :436, gated via `vault.isVaultOwner(msg.sender)`). Total: 3 external usages (2 in DeityPass + 1 in DegenerusAdmin).
    2. `onlyVaultOwner` modifier callsites at EXTERNAL function declarations — every function in `DegenerusVault.sol` carrying `external onlyVaultOwner` (modifier defined :431; gate `_isVaultOwner(msg.sender)` :432). **The grep MUST match all modifier orderings including `external payable onlyVaultOwner` and multi-line signatures where the modifier sits on the closing-paren line — use the bareword-anchored pattern `\bonlyVaultOwner\b` (NOT `external onlyVaultOwner|public onlyVaultOwner`, which misses the `external payable onlyVaultOwner` ordering and multi-line closing-paren placements).** Confirmed source-truth count via fresh grep `grep -nE "\bonlyVaultOwner\b" contracts/DegenerusVault.sol | wc -l`: 24 hits = 1 modifier definition at :431 + 23 external usages. Subtract the modifier definition line to recover the 23-usage count. The 23 external usages cite the function declaration line (pin to the `function NAME(` line, not the closing-paren line, for multi-line signatures): :500 gameAdvance, :513 gamePurchase (closing-paren modifier at :519), :534 gamePurchaseTicketsBurnie, :543 gamePurchaseBurnieLootbox, :551 gameOpenLootBox, :561 gamePurchaseDeityPassFromBoon, :575 gameClaimWinnings, :581 gameClaimWhalePass, :585 gameDegeneretteBet (closing-paren modifier at :601), :620 gameResolveDegeneretteBets, :627 gameSetAutoRebuy, :634 gameSetAutoRebuyTakeProfit, :643 gameSetAfKingMode (closing-paren modifier at :647), :655 gameSetOperatorApproval, :662 coinDepositCoinflip, :670 coinClaimCoinflips, :677 coinDecimatorBurn, :685 coinSetAutoRebuy, :692 coinSetAutoRebuyTakeProfit, :700 wwxrpMint, :708 jackpotsClaimDecimator, :719 sdgnrsBurn, :725 sdgnrsClaimRedemption.
    3. Hand-rolled admin gates of the form `if (msg.sender != ContractAddresses.ADMIN) revert E();` — confirmed sites via fresh grep: `DegenerusGame.sol:1809` (adminSwapEthForStEth declared :1805); `DegenerusGameAdvanceModule.sol:503` (wireVrf); `DegenerusGameAdvanceModule.sol:1682` (updateVrfCoordinatorAndSub). Total: 3.
    4. Hand-rolled CREATOR gates `if (msg.sender != ContractAddresses.CREATOR) revert OnlyCreator();` — confirmed sites: `Icons32Data.sol:154, :172, :197`. Total: 3.
    5. Hand-rolled vault-owner gates `if (!vault.isVaultOwner(msg.sender)) revert ...;` at external function bodies — confirmed sites via fresh grep: `DegenerusGame.sol:480` (setLootboxRngThreshold declared :479), `:1827` (adminStakeEthForStEth declared :1826); `DegenerusStonk.sol:188, :203`; `GNRUS.sol:380`. Total: 5.
    6. Admin-only initializers + admin upgrade functions (delegatecall wiring, VRF rotation) per D-300-ENUM-SCOPE-01 — captured by category (3) above.
    7. Any other `setX(...)` / governance / parameter-update external entry that is gated by an access-control modifier OR hand-rolled access check, located via fresh grep at authoring time.

    **Source-truth per-file tally (REPRODUCE via fresh grep at authoring time; the planner pinned these at plan-revision time, but executor MUST re-verify before relying on them):**
    - DegenerusVault.sol — 23 external `onlyVaultOwner` (grep pattern: `\bonlyVaultOwner\b`, then subtract 1 for the modifier definition at :431; the broken pattern `external onlyVaultOwner|public onlyVaultOwner` misses `external payable onlyVaultOwner` and multi-line closing-paren placements)
    - DegenerusDeityPass.sol — 2 external `onlyOwner` usages (:94 setRenderer + :108)
    - DegenerusAdmin.sol — 1 external `onlyOwner` usage (:631 swapGameEthForStEth)
    - Icons32Data.sol — 3 CREATOR-gate externals (:154, :172, :197)
    - DegenerusGame.sol — 3 hand-rolled gate externals (setLootboxRngThreshold :479/:480, adminSwapEthForStEth :1805/:1809, adminStakeEthForStEth :1826/:1827)
    - DegenerusGameAdvanceModule.sol — 2 ADMIN-gate externals (wireVrf :503, updateVrfCoordinatorAndSub :1682)
    - DegenerusStonk.sol — 2 isVaultOwner externals (:188, :203)
    - GNRUS.sol — 1 isVaultOwner external (:380)
    - **Expected §1 row floor: ~37 rows** (23 Vault + 2 DeityPass + 1 DegenerusAdmin + 3 Icons32Data + 3 DegenerusGame + 2 AdvanceModule + 2 DegenerusStonk + 1 GNRUS = 37). If executor's enumeration produces materially fewer rows, something was missed — re-grep and reconcile.

    **OUT OF SCOPE for §1 enumeration** (per D-300-ENUM-SCOPE-01 + integration-trust-boundary discipline). Document each carve-out at the top of §1 with explicit rationale:
    - `onlyGame` / `onlyCoin` / `onlyCoinflip` / `onlyVault` (capital-V Vault) / `onlyBurnieCoin` / `onlyFlipCreditors` / `onlyDegenerusGameContract` modifiers — integration trust boundaries between protocol contracts, not administrative entry points. Same for hand-rolled `msg.sender != ContractAddresses.{COIN|COINFLIP|GAME|SDGNRS|VAULT}` gates.
    - Pure-view admin functions (no SSTORE) per D-300-ENUM-SCOPE-01. Note: DegenerusGame.sol `sampleTraitTickets(:2380-area)`, `sampleTraitTicketsAtLevel(:2415-area)`, and `getTickets(:2503-area)` are external **view** functions that READ `traitBurnTicket` — they are NOT admin writers and are explicitly excluded; see §1.E for catalog-erratum carry forward.
    - Internal-only admin helpers (no external/public entry point) per D-300-ENUM-SCOPE-01. Note: MintModule `_raritySymbolBatch` (around :594-:608, inline-asm sstore into traitBurnTicket) is INTERNAL-only; reachable only from advanceGame-chain ADVANCEMODE; correctly enumerated by RNGLOCK-CATALOG §16 V-014/V-015 as EXEMPT-ADVANCEGAME; explicitly excluded from §1.
    - **EOA self-config setters** (no admin-class gate; player-callable for own state) — confirmed source-truth via fresh grep at plan-revision time:
      - `DegenerusGame.setOperatorApproval :435` — no access control modifier; writes `operatorApprovals[msg.sender][operator]`.
      - `DegenerusGame.setAutoRebuy :1495` — uses `_resolvePlayer(player)`; only check is `if (rngLockedFlag) revert RngLocked();` already-implemented inside `_setAutoRebuy`. No admin gate.
      - `DegenerusGame.setAutoRebuyTakeProfit :1504` — same shape as setAutoRebuy.
      - `DegenerusGame.setAfKingMode :1559` — same shape as setAutoRebuy.
      - **Rationale:** EOA-callable self-config; no admin-class gate; out of D-300-ENUM-SCOPE-01 scope. (These functions ARE captured by RNGLOCK-CATALOG §16 V-009..V-013 as VIOLATIONs in their own right — that catalog row stays valid as a player-callable RNG-window VIOLATION; ADMA simply does not classify them as admin-class. Phase 303 FINDINGS will route them via the catalog's V-NNN handoff, not via §4 ADMA register.)
    - **DegenerusAdmin internal discriminator branches** (NOT external admin entries) — confirmed source-truth via fresh grep + enclosing-function lookup at plan-revision time:
      - `DegenerusAdmin.sol:507` (`if (vault.isVaultOwner(msg.sender)) { path = ProposalPath.Admin; }`) is INSIDE `proposeFeedSwap` (declared :479). The function itself has NO admin gate at entry — both admins and community members can call it; the branch routes the proposal between Admin/Community paths.
      - `DegenerusAdmin.sol:670` (same shape) is INSIDE `propose` (declared :647). Same disposition.
      - **Rationale:** Internal admin-discriminator branches that route control flow based on caller role; the external function itself is community-callable. Not admin-class externals; out of D-300-ENUM-SCOPE-01 scope. (Phase 298 catalog should be cross-checked for whether these branches write participating slots through their Admin-path — if so, those writes will already be captured in §15/§16 under the enclosing function name `proposeFeedSwap`/`propose`, not under a fictional admin function.)

    **Admin-class classification (5 explicit classes + general catch-all per D-300-CONTEXT.md §specifics):**
    - `governance` — vault-owner / admin-key / creator multisig functions (wireVrf, updateVrfCoordinatorAndSub, swapGameEthForStEth, adminSwapEthForStEth, adminStakeEthForStEth, setRenderer, Icons32Data CREATOR setters, etc.)
    - `parameter-update` — economic / threshold setters (setLootboxRngThreshold, etc.)
    - `charity-allowlist` — charity allowlist mutators (Phase 254-258 v33.0 surface — look for setCharity / allowCharity / etc.)
    - `decimator-config` — decimator-window / bucket-config setters
    - `presale-config` — presale-cap / presale-active setters (look in MintModule / DegenerusGame for presale-related setters)
    - `general` — catch-all for admin functions that don't cleanly map to the above 5

    **§1 row format:**
    Markdown table with columns: `# | Contract | Function (file:line) | Role-gate annotation | Admin-class | Notes`.

    Row format example: `| A-01 | DegenerusGame | adminSwapEthForStEth (DegenerusGame.sol:1805) | hand-rolled: if (msg.sender != ContractAddresses.ADMIN) revert E(); (:1809) | governance | Value-neutral ETH↔stETH swap; admin cannot extract funds per docstring |`

    Number rows sequentially A-01..A-NN.

    **Row-format convention — cite the function declaration line, NOT the closing-paren line, for multi-line signatures.** For multi-line signatures in `DegenerusVault.sol` (gamePurchase :513 closing-paren at :519; gameDegeneretteBet :585 closing-paren at :601; gameSetAfKingMode :643 closing-paren at :647), the §1 row MUST cite the `function NAME(` line (e.g. `:513`, `:585`, `:643`), not the closing-paren modifier line. This keeps the cite anchored to the function identifier and interoperates with downstream slot-cross-reference greps. The per-row verify regex in this task's <verify> block accepts both forms as defense-in-depth, but the canonical form is the declaration line.

    **§1.E — Catalog erratum carry forward (RNGLOCK-CATALOG.md S-06):**

    Author this subsection at the END of §1 (after the row table but before the §2 header). Verbatim text:

    ```
    ### §1.E — Catalog erratum carry forward (RNGLOCK-CATALOG.md S-06)

    RNGLOCK-CATALOG.md §15 rows 154/155/156 and §16 rows V-016/V-017/V-018 enumerate
    phantom admin-class writers (`adminSeedTraitBucket`, `adminClearTraitBucket`,
    `:2510 helper`) at DegenerusGame.sol:2398/:2427/:2510. Direct source verification
    at plan-time shows those line numbers resolve to view-function reads inside
    `sampleTraitTickets` / `sampleTraitTicketsAtLevel` / `getTickets` — these admin
    functions DO NOT EXIST in `contracts/`. Verification grep:
    `grep -n "adminSeedTraitBucket\|adminClearTraitBucket" contracts/` returns 0 hits.
    The actual S-06 `traitBurnTicket` writer is `_raritySymbolBatch` in
    `DegenerusGameMintModule.sol:594/:602` area (inline-asm sstore), which the
    catalog correctly enumerates at §15 row 153 as the writer and §16 rows V-014/V-015
    as EXEMPT-ADVANCEGAME. `_raritySymbolBatch` is INTERNAL-only and is correctly
    out of D-300-ENUM-SCOPE-01 scope.

    ADMA carry forward: S-06 has ZERO admin-class writers; consequently ZERO §1 rows,
    ZERO §2 VIOLATION rows, and ZERO §3 entries for S-06. RNGLOCK-CATALOG.md is
    untouched (Phase 298 closed artifact per D-300-KI-01-adjacent posture); the
    catalog erratum is recorded here and carried into the v44.0 FIX-MILESTONE handoff
    register §4 as `D-43N-V44-ADMA-ERRATUM-01` so v44 plan-phase does NOT spend a
    sub-phase on non-existent functions and so v44 plan-phase OR a future catalog-
    revision phase can correct the upstream §15/§16/§C.3.2/§C.3.3 rows. §17 rows
    1421/1422 and §18 rows 1438/1439 (which reference the same phantom writers in
    the executive-summary / grep-gate sections) inherit the same erratum.
    ```

    **Document scaffold (author the header + section stubs in this task; later tasks fill §2..§5 + §0):**

    ```
    # ADMIN-AUDIT — Phase 300 Admin Path Enumeration Audit (v43.0)

    **Generated:** 2026-05-18
    **Milestone:** v43.0 Total rngLock Determinism Audit (AUDIT-ONLY per `D-43N-AUDIT-ONLY-01`)
    **Audit baseline:** v42.0 closure HEAD `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2`
    **Posture:** Single AGENT-COMMITTED canonical ADMA deliverable for ADMA-01..04; zero `contracts/` and zero `test/` mutations across the phase; per-admin-function FIX recommendations are AGENT-COMMITTED documentation handing forward to v44.0 FIX-MILESTONE via `D-43N-V44-ADMA-NN` anchors.

    This artifact enumerates every admin-gated external / public function in `contracts/`, cross-references each against the Phase 298 RNGLOCK-CATALOG §15 per-slot writer table, and emits per-admin-function recommendation entries for admin functions that reach a participating slot at any non-EXEMPT callsite. Per `D-43N-AUDIT-ONLY-01`, no SAFE_BY_DESIGN classifications appear — every admin-function-reaches-participating-slot edge gets a tactic recommendation + v44.0 handoff anchor.

    ---

    ## §0 — Executive Summary

    *Authored AFTER §1..§5 are complete. Phase 303 §3.E ADMA roll-up sources its prose from this section.*

    ---

    ## §1 — Complete Admin Function Enumeration (ADMA-01)

    **Scope (D-300-ENUM-SCOPE-01):** ...

    **Carve-out (integration-trust-boundary modifiers excluded; EOA self-config excluded; DegenerusAdmin internal discriminator branches excluded; pure-view functions excluded; internal helpers excluded):** ...

    [§1 table A-01..A-NN ~37 rows]

    [§1.E catalog erratum carry forward block]

    ---

    ## §2 — Participating-Slot Cross-Reference Table (ADMA-02)

    *Authored in Task 2.*

    ---

    ## §3 — Per-Admin-Function Recommendation Table (ADMA-03 + ADMA-04)

    *Authored in Task 3.*

    ---

    ## §4 — v44.0 FIX-MILESTONE Consolidated Handoff Register

    *Authored in Task 4.*

    ---

    ## §5 — Grep Completeness Gate Attestation

    *Authored in Task 4.*

    ---

    *Audit metadata footer: generation date, dependencies (Phase 298 RNGLOCK-CATALOG.md), posture, attestations.*
    ```

    **Enumeration procedure for §1:**

    1. Begin with a fresh-sweep grep over `contracts/` (excluding `contracts/test/`, `contracts/mocks/`, `contracts/interfaces/`) for ALL of:
       - `onlyOwner` (formal modifier definitions + callsite usages)
       - `onlyAdmin` (in case any module uses this name)
       - `onlyRole` (in case any module uses OZ AccessControl)
       - `onlyVaultOwner` (D-300 specific)
       - `msg\.sender\s*!=\s*ContractAddresses\.(ADMIN|CREATOR)` (hand-rolled gates)
       - `!vault\.isVaultOwner\(msg\.sender\)` / `!_isVaultOwner\(msg\.sender\)` (hand-rolled vault-owner gates)
    2. Record each grep hit count in a working scratchpad (you will reproduce these numbers verbatim in §5 grep-completeness gate at Task 4).
    3. For each grep hit at a function-definition site, append one row to the §1 table with row ID A-NN, contract name, function name + file:line, role-gate annotation (which modifier or which hand-rolled gate; copy the gate body verbatim with file:line), admin-class classification, and a Notes column.
    4. For each grep hit that is a modifier *definition* (not a usage), do NOT add a row — instead, record the modifier name + file:line in the §1 preamble as a discovered-modifier index that later rows reference.
    5. For modifier *usages*, every external/public function carrying that modifier gets its own §1 row.
    6. For each grep hit at an INTERNAL conditional branch (not at function entry), evaluate whether the enclosing function itself has an admin gate at its entry; if not (e.g., DegenerusAdmin :507 / :670), record in the §1 carve-out list with rationale.
    7. Walk DegenerusVault.sol top-to-bottom and add a row for every function carrying the `onlyVaultOwner` modifier (use the bareword-anchored grep `\bonlyVaultOwner\b` to catch all modifier orderings including `external payable onlyVaultOwner` and multi-line signatures where the modifier sits on the closing-paren line; do NOT use the naive `external onlyVaultOwner|public onlyVaultOwner` pattern — it misses :513/:561/:585). This is the largest single-contract concentration — 23 rows confirmed at plan-revision time (24 grep hits minus 1 modifier-definition at :431).
    8. After enumeration, verify the total row count against the source-truth per-file tally pinned in this task's read_first (expected floor: ~37 rows = 23 Vault + 2 DeityPass + 1 DegenerusAdmin + 3 Icons32Data + 3 DegenerusGame + 2 AdvanceModule + 2 DegenerusStonk + 1 GNRUS). If executor's count diverges by more than ±2, re-run all five Pattern greps and reconcile.

    Do NOT modify `contracts/` or `test/`. Do NOT modify RNGLOCK-CATALOG.md. Do NOT modify KNOWN-ISSUES.md. Do NOT use SAFE_BY_DESIGN classification anywhere. Do NOT inline fenced code blocks of contract source — cite by file:line and name signatures/identifiers only per planner discipline.
  </action>
  <verify>
    <automated>
test -f .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §0 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §1 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §2 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §3 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §4 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §5 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^### §1\.E " .planning/ADMIN-AUDIT.md \
  && [ "$(grep -c "^| A-[0-9]\+ " .planning/ADMIN-AUDIT.md)" -ge 35 ] \
  && grep -q "D-300-ENUM-SCOPE-01" .planning/ADMIN-AUDIT.md \
  && grep -q "integration-trust-boundary" .planning/ADMIN-AUDIT.md \
  && grep -q "EOA self-config" .planning/ADMIN-AUDIT.md \
  && grep -q "discriminator branch\|internal.*branch" .planning/ADMIN-AUDIT.md \
  && grep -q "adminSeedTraitBucket\|catalog erratum" .planning/ADMIN-AUDIT.md \
  && ! grep -q "SAFE_BY_DESIGN" .planning/ADMIN-AUDIT.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && [ -z "$(git status --porcelain .planning/RNGLOCK-CATALOG.md .planning/KNOWN-ISSUES.md 2>/dev/null)" ] \
  && bash -c '
    # Per-row source-existence check: for each A-NN row, extract the file:line citation
    # from the "Function (file:line)" column and verify (a) the file:line exists, (b) the
    # line is within ±5 of an external/public function declaration, (c) the cited role-gate
    # appears in the source near that line.
    rc=0
    while IFS="" read -r row; do
      # Extract file:line from the 3rd column (Function (file:line)). Pattern matches
      # "<word>.sol:<digits>" — pick the first such occurrence on the row.
      cite=$(printf "%s" "$row" | grep -oE "[A-Za-z0-9_/]+\.sol:[0-9]+" | head -1)
      if [ -z "$cite" ]; then
        echo "ROW MISSING file:line citation: $row"
        rc=1
        continue
      fi
      file="contracts/$(printf "%s" "$cite" | cut -d: -f1)"
      ln=$(printf "%s" "$cite" | cut -d: -f2)
      # Resolve module-path variants
      if [ ! -f "$file" ]; then
        alt=$(find contracts -name "$(basename "$file")" -print -quit 2>/dev/null)
        if [ -n "$alt" ]; then file="$alt"; fi
      fi
      if [ ! -f "$file" ]; then
        echo "ROW FILE NOT FOUND: $file (row: $row)"
        rc=1
        continue
      fi
      # Verify cited line is within ±5 lines of an external/public function declaration.
      lo=$((ln - 5)); if [ "$lo" -lt 1 ]; then lo=1; fi
      hi=$((ln + 5))
      if ! sed -n "${lo},${hi}p" "$file" | grep -qE "(function [a-zA-Z_].*\(|\) (external|public).*\{)"; then
        echo "ROW LINE NOT NEAR external/public function: $cite (row: $row)"
        rc=1
      fi
    done < <(awk "/^## §1 /,/^## §2 /" .planning/ADMIN-AUDIT.md | grep -E "^\| A-[0-9]+ ")
    exit $rc
  ' \
  && echo "OK"
    </automated>
  </verify>
  <done>
    .planning/ADMIN-AUDIT.md exists with full 6-section scaffold (§0..§5 headings present); §1 contains ≥35 admin-function rows (A-NN format; floor ~37); §1.E catalog-erratum carry-forward subsection present with adminSeedTraitBucket / catalog erratum verbiage; D-300-ENUM-SCOPE-01 scope + integration-trust-boundary carve-out + EOA-self-config carve-out + DegenerusAdmin-internal-branch carve-out documented in §1 preamble; every §1 row's cited file:line passes the per-row source-existence check (file exists, line is within ±5 of an external/public function declaration); zero SAFE_BY_DESIGN tokens; contracts/ + test/ + RNGLOCK-CATALOG.md + KNOWN-ISSUES.md unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author §2 — Participating-slot cross-reference table (ADMA-02)</name>
  <files>.planning/ADMIN-AUDIT.md</files>
  <read_first>
    - .planning/ADMIN-AUDIT.md (the in-progress artifact from Task 1; specifically §1 rows A-01..A-NN + §1.E catalog erratum)
    - .planning/RNGLOCK-CATALOG.md §14 (unique-slot index S-01..S-67; participating-slot identity)
    - .planning/RNGLOCK-CATALOG.md §15 (per-slot writer enumeration — the cross-reference source-of-truth; load-bearing for this task; IGNORE phantom rows 154/155/156 per §1.E)
    - .planning/RNGLOCK-CATALOG.md §16 (verdict matrix — distinguishes EXEMPT-ADVANCEGAME / EXEMPT-VRFCALLBACK / EXEMPT-RETRYLOOTBOXRNG / VIOLATION classifications per (slot × writer × callsite) tuple; IGNORE phantom rows V-016/V-017/V-018 per §1.E)
    - .planning/phases/300-admin-path-enumeration-audit-adma/300-CONTEXT.md (D-300-ADMA-LAYOUT-01 §2 specification)
    - Project memory: feedback_verify_call_graph_against_source.md (no "by construction" / "single fn reaches all paths" claims — each §2 row grep-verified against source per the Phase 294 BURNIE gap precedent)
    - Project memory: feedback_rng_window_storage_read_freshness.md (ALL writes inside an admin function's reachable callgraph count, not just direct SSTOREs — enumerate the full write set)
  </read_first>
  <action>
    Author `## §2 — Participating-Slot Cross-Reference Table (ADMA-02)` inside `.planning/ADMIN-AUDIT.md`, replacing the placeholder text from Task 1.

    **§2 row format:**
    Markdown table with columns: `Admin fn (§1 row) | Contract.fn (file:line) | Participating slot(s) written | Writer in CAT-03 (RNGLOCK-CATALOG §15 row(s)) | Callsite verdict (EXEMPT-* / VIOLATION) | Notes`.

    **Cross-reference procedure:**

    For each §1 row A-NN:
    1. Identify the admin function's body — extract every SSTORE / mapping-write / array-push / `delete` directly inside the function AND every writer fn it calls (transitive write set per `feedback_verify_call_graph_against_source.md` — no "by construction" claims; grep-verify each callee).
    2. For each write target, check if the slot identity appears in RNGLOCK-CATALOG §14 unique-slot index (S-01..S-67). If yes, this admin function writes a participating slot.
    3. For each (admin-fn, participating-slot) pair, locate the matching row in RNGLOCK-CATALOG §15 (the writer fn + file:line is in §15) and the verdict in §16. Record the §15 row reference and the §16 verdict.
    4. The verdict will almost always be `VIOLATION` because admin functions are not reachable from `advanceGame()`, not the VRF coordinator callback, and not `retryLootboxRng()` — admin functions are user-callable governance/parameter-update entry points. The three EXEMPT classes do not apply unless an admin function happens to be reached EXCLUSIVELY from one of the three exempt entry points (rare for admin functions — flag explicitly if found).
    5. If an admin function writes NO participating slot, record one row with `Participating slot(s) written = (none)` and `Callsite verdict = N/A (pure-admin-state-only)` — these rows do NOT produce a §3 recommendation entry but document the comprehensive cross-reference per ADMA-02.

    **S-06 carry forward from §1.E:** S-06 `traitBurnTicket[lvl][trait]` has ZERO admin-class writers in source. The phantom rows in RNGLOCK-CATALOG §15 (154/155/156) and §16 (V-016/V-017/V-018) and §C.3.2/§C.3.3 are not corrected here (RNGLOCK-CATALOG.md is read-only Phase 298 artifact) but the §1.E carry-forward block documents the erratum and the v44.0 handoff register §4 carries `D-43N-V44-ADMA-ERRATUM-01`. Do NOT emit any §2 row tying any A-NN admin function to S-06 via the phantom catalog rows.

    Author the §2 preamble with an explicit note: "Per §1.E, S-06 has zero admin-class writers; no §2 rows reference S-06."

    **Known cross-references (load-bearing seeds from RNGLOCK-CATALOG.md §15; verify each against source as you author):**
    - `AdvanceModule.updateVrfCoordinatorAndSub` (file:line 1682) → S-47 vrfCoordinator + S-48 vrfSubscriptionId + S-49 vrfKeyHash + S-38 rngRequestTime + S-46 lootboxRngPacked LR_MID_DAY — multi-slot writer; grep-verify each SSTORE in the function body and pin each to a §15 row.
    - `AdvanceModule.wireVrf` (file:line 503) → S-47 + S-48 + S-49 (constructor-one-shot — verify whether this is reachable post-deploy or only at construction time; if construction-only, mark verdict N/A with "construction-time-only" annotation and the §3 recommendation in Task 3 reflects the same).
    - `DegenerusGame.adminSwapEthForStEth` (file:line 1805) → S-20 address(this).balance (ETH balance changes via stETH transfer in/out) — grep-verify the write set.
    - `DegenerusGame.adminStakeEthForStEth` (file:line 1826) → S-20 + S-21 stETH balance — grep-verify.
    - `DegenerusGame.setLootboxRngThreshold` (file:line 479) — search §15 for the threshold slot; may not be a participating slot (parameter-update only) — verify; if NOT participating, mark verdict N/A.
    - `DegenerusAdmin.swapGameEthForStEth` (file:line 631) — similar to adminSwapEthForStEth; grep-verify writes.
    - DegenerusVault 20 onlyVaultOwner externals — each is a dispatcher into a module-level write path; grep-verify the write set for each and pin to §15 rows. Note: many of these (gameAdvance, gameOpenLootBox, gamePurchaseTicketsBurnie, etc.) will reach participating slots via the module write paths and will appear as VIOLATIONs in §2 with corresponding §16 V-NNN rows.
    - DegenerusDeityPass setRenderer :94 + setter at :108 — likely write pass-through; verify if any write hits a participating slot.
    - Icons32Data 3 CREATOR-gate externals at :154/:172/:197 — verify whether trait-data writes hit any participating slot.
    - DegenerusStonk :188, :203 + GNRUS :380 — verify each write set.

    Per `D-300-ADMA-LAYOUT-01`: "marks which functions write participating slots at which callsite(s)". Per-callsite granularity matters — if an admin function dispatches through multiple internal writers reaching the same slot at different file:line callsites, emit a row per callsite.

    **Verdict-classification rule:** For an admin function writing a participating slot, the verdict is `VIOLATION` UNLESS the admin function is reachable only from one of the three exempt entry points (advanceGame / VRF callback / retryLootboxRng). Document the verdict + 1-line rationale per row.

    Do NOT modify `contracts/` or `test/`. Do NOT modify RNGLOCK-CATALOG.md. Do NOT use SAFE_BY_DESIGN. Cite each §15 cross-reference by `RNGLOCK-CATALOG.md §15 ({slot ID}: {writer fn})` for traceability.
  </action>
  <verify>
    <automated>
test -f .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §2 " .planning/ADMIN-AUDIT.md \
  && [ "$(awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -cE "^\| A-[0-9]+ ")" -ge 35 ] \
  && awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -qE "S-(0[1-9]|[1-6][0-9])" \
  && awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -qE "RNGLOCK-CATALOG\.md.*§15" \
  && awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -qE "VIOLATION|EXEMPT|N/A" \
  && awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -qE "S-06 has zero admin|§1\.E|no §2 rows reference S-06" \
  && ! awk '/^## §2 /,/^## §3 /' .planning/ADMIN-AUDIT.md | grep -qE "adminSeedTraitBucket|adminClearTraitBucket" \
  && ! grep -q "SAFE_BY_DESIGN" .planning/ADMIN-AUDIT.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && [ -z "$(git status --porcelain .planning/RNGLOCK-CATALOG.md .planning/KNOWN-ISSUES.md 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    §2 cross-reference table populated; ≥35 admin-fn rows present (one per §1 row; expected floor ~37); each row that reaches a participating slot cites a RNGLOCK-CATALOG §15 row reference + a S-NN slot ID + a verdict (VIOLATION / EXEMPT-* / N/A); §2 preamble references §1.E and asserts zero admin-class writers for S-06; no §2 row mentions the phantom functions `adminSeedTraitBucket` / `adminClearTraitBucket`; zero SAFE_BY_DESIGN; source-tree + catalog + KI unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author §3 — Per-admin-function recommendation table (ADMA-03 + ADMA-04)</name>
  <files>.planning/ADMIN-AUDIT.md</files>
  <read_first>
    - .planning/ADMIN-AUDIT.md (the in-progress artifact from Tasks 1 + 2; specifically §1 + §2 rows + §1.E catalog erratum)
    - .planning/RNGLOCK-CATALOG.md §16 (verdict matrix — pull the v44.0 handoff anchor convention per `D-298-RECOMMEND-DEPTH-01`)
    - .planning/phases/300-admin-path-enumeration-audit-adma/300-CONTEXT.md (D-300-GATING-MECHANISM-01 RngLocked preferred; D-300-ADMA-LAYOUT-01 §3 specification; D-300-CONTEXT.md §specifics admin-class taxonomy + D-43N-V44-ADMA-NN anchor convention)
    - contracts/modules/DegenerusGameMintModule.sol around line 1221 (`if (cachedJpFlag && rngLockedFlag) {revert RngLocked();}` — canonical RngLocked pattern, cite as anchor implementation)
    - contracts/BurnieCoinflip.sol around lines 730 + 780 (`if (degenerusGame.rngLocked()) revert RngLocked();` — cross-contract RngLocked pattern)
    - contracts/StakedDegenerusStonk.sol around lines 492 + 513 (`if (game.rngLocked()) revert BurnsBlockedDuringRng();` — cross-contract RngLocked-named-variant pattern)
    - Project memory: feedback_design_intent_before_deletion.md (per-admin-function rationale should trace what the admin function exists for and what would break if naively gated — DO NOT just slap a revert on without considering whether admin needs to operate during rngLock window for legitimate purposes like emergency VRF rotation)
    - Project memory: feedback_frozen_contracts_no_future_proofing.md (don't keep redundant gates on future-extensibility grounds — recommend the minimal gate)
    - Project memory: feedback_skeptic_pass_before_catastrophe.md (skeptic-reviewer filter on tier classification)
  </read_first>
  <action>
    Author `## §3 — Per-Admin-Function Recommendation Table (ADMA-03 + ADMA-04)` inside `.planning/ADMIN-AUDIT.md`, replacing the placeholder text from Task 1.

    **§3 row format:**
    For each §2 row whose `Callsite verdict` is `VIOLATION` (i.e., admin function writes a participating slot at a non-EXEMPT callsite), emit one §3 entry. Per `D-300-ADMA-LAYOUT-01`, the entry contains:

    - **Row ID:** `R-NN` numbered sequentially from R-01
    - **Admin fn:** Contract.fn + file:line (from §1)
    - **Participating slot(s) reached:** S-NN identities (from §2)
    - **Admin-class disposition:** governance / parameter-update / charity-allowlist / decimator-config / presale-config / general (from §1 classification)
    - **Recommended gating mechanism:** Per `D-300-GATING-MECHANISM-01`, `RngLocked` custom error revert preferred. Cite the canonical implementation pattern with file:line (MintModule:1221 / BurnieCoinflip:730 / sStonk:492). The recommendation is a 1-2-sentence prescription naming the exact line where the gate would land + the revert form. If a specific admin function has a non-revert tactic that is better (e.g., admin config slot becomes immutable post-launch; OR admin function should be reordered to fire only pre-lock), document the exception with rationale per D-300-GATING-MECHANISM-01.
    - **Per-admin-function rationale:** 2-4 sentences answering: (a) what does this admin function exist for? (b) what would break if naively `revert RngLocked()`-gated during the entire rngLocked window? (c) is there a legitimate reason for admin to operate inside the window (e.g., emergency VRF rotation per `retryLootboxRng` precedent — `updateVrfCoordinatorAndSub` may need to fire during stall scenarios)? (d) what is the residual EV / risk if the admin acts inside the window? Per `feedback_design_intent_before_deletion.md` discipline.
    - **v44.0 handoff anchor:** `D-43N-V44-ADMA-NN` where NN matches the §3 row number (R-01 → ADMA-01, R-02 → ADMA-02, etc. — per `D-300-CONTEXT.md` §specifics). Format: `**Anchor:** D-43N-V44-ADMA-NN — {1-line v44.0 plan-phase consumable summary} | **Admin fn:** {Contract.fn at file:line} | **Slot(s):** {S-NN list} | **Tactic:** {gate-mechanism summary}`.
    - **Cross-reference:** Cite the matching RNGLOCK-CATALOG §16 V-NNN VIOLATION row(s) for the (slot × writer × callsite) tuple(s) this admin function manifests (the catalog already enumerated the slot-level violations; ADMA's contribution is the admin-class lens + the per-admin-function recommendation).

    **Format the section as one logical entry per row, with a header `### §3.NN — R-NN: {Contract.fn}` and the 6 fields below, OR as a single wide markdown table if all 6 fields fit cleanly (planner discretion). Logical-entry shape preferred for readability when rationale is multi-sentence.

    **Important — DO NOT collapse rows:** Each admin function reaching a participating slot gets its own §3 entry, even if the recommendation is identical to a sibling entry. v44.0 plan-phase consumes per-admin-function anchors to plan sub-phases — collapsing breaks the handoff register.

    **Edge cases to address explicitly:**
    1. `wireVrf` (constructor-one-shot) — if reachable only at construction, it's not a runtime-window violation. Verify; if true, emit §3 entry with tactic = "immutable (already structurally one-shot)" + rationale + anchor for v44.0 to confirm via grep proof.
    2. `updateVrfCoordinatorAndSub` — emergency VRF rotation is the canonical exception per `retryLootboxRng` precedent. Recommended tactic may be (c) "operationally restrict to pre-lock or post-unlock — admin discipline rather than contract gate" OR (a) "rngLocked revert with operator-emergency-bypass branch" — document tradeoff.
    3. `adminSwapEthForStEth` (S-20 address(this).balance via stETH transfer out) — ETH balance is a participating slot per RNGLOCK-CATALOG §14 S-20. Recommendation is (a) revert per the existing convention; rationale = even though docstring says "value-neutral", swap mutates address(this).balance which is read by §5 consumers.
    4. `adminStakeEthForStEth` (S-20 + S-21 stETH balance) — similar to (3); recommend (a) revert.
    5. **S-06 trait-bucket writers — NONE.** Per §1.E, S-06 `traitBurnTicket` has zero admin-class writers in source. No R-NN entries for the phantom `adminSeedTraitBucket` / `adminClearTraitBucket` / helper. The catalog erratum is carried in §4 as `D-43N-V44-ADMA-ERRATUM-01`, NOT as an R-NN row.

    **Skeptic-reviewer filter** per `feedback_skeptic_pass_before_catastrophe.md`: before emitting any §3 entry that escalates the recommended-tactic urgency above (a), apply the 3-condition EV lens (admin attack vector requires admin-key compromise, which is a structural protection layer — recommendations should stay at "standard" tactic-(a) revert depth unless a specific path bypasses admin-key trust assumption).

    **CRITICAL — Skeptic filter does NOT relax the per-admin-function rationale requirement.** ADMA-03 requires the 4-question (a/b/c/d) design-intent + break-on-naive-gate + residual-EV walk for EVERY §3 entry, including tactic-(a) revert recommendations. The skeptic filter governs TIER classification (don't promote to CATASTROPHE without structural-bypass evidence); it does NOT govern whether the per-row rationale exists. Every R-NN row, regardless of tactic, MUST carry the 2-4 sentence design-intent walk.

    Do NOT modify `contracts/` or `test/`. Do NOT modify RNGLOCK-CATALOG.md. Do NOT use SAFE_BY_DESIGN. Cite cross-references by anchor ID for traceability.
  </action>
  <verify>
    <automated>
test -f .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §3 " .planning/ADMIN-AUDIT.md \
  && bash -c '
    # §3 row count must equal or exceed §2 VIOLATION row count.
    violation_count=$(awk "/^## §2 /,/^## §3 /" .planning/ADMIN-AUDIT.md | grep -cE "VIOLATION")
    adma_count=$(awk "/^## §3 /,/^## §4 /" .planning/ADMIN-AUDIT.md | grep -cE "^### §3\.|^\| R-[0-9]+ ")
    if [ "$adma_count" -lt "$violation_count" ]; then
      echo "§3 row count ($adma_count) < §2 VIOLATION count ($violation_count)"
      exit 1
    fi
    # §3 row count floor: ≥ 5 (sanity floor for any non-trivial admin surface).
    if [ "$adma_count" -lt 5 ]; then
      echo "§3 row count ($adma_count) below sanity floor of 5"
      exit 1
    fi
    # §3 anchor count must match row count.
    anchor_count=$(grep -cE "D-43N-V44-ADMA-[0-9]+" .planning/ADMIN-AUDIT.md)
    if [ "$anchor_count" -lt "$adma_count" ]; then
      echo "§3 anchor count ($anchor_count) < row count ($adma_count)"
      exit 1
    fi
    exit 0
  ' \
  && awk '/^## §3 /,/^## §4 /' .planning/ADMIN-AUDIT.md | grep -qE "RngLocked|MintModule:1221|BurnieCoinflip:730|sStonk:492" \
  && awk '/^## §3 /,/^## §4 /' .planning/ADMIN-AUDIT.md | grep -qE "governance|parameter-update|charity-allowlist|decimator-config|presale-config|general" \
  && ! awk '/^## §3 /,/^## §4 /' .planning/ADMIN-AUDIT.md | grep -qE "adminSeedTraitBucket|adminClearTraitBucket" \
  && ! grep -q "SAFE_BY_DESIGN" .planning/ADMIN-AUDIT.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && [ -z "$(git status --porcelain .planning/RNGLOCK-CATALOG.md .planning/KNOWN-ISSUES.md 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    §3 recommendation table populated with one entry per §2 VIOLATION row (anchored at §2 VIOLATION count, ≥5 entries sanity floor); each entry emits a D-43N-V44-ADMA-NN anchor matching its row number; each entry classifies admin-class (governance / parameter-update / charity-allowlist / decimator-config / presale-config / general); recommended-tactic cites RngLocked convention with file:line anchor (MintModule:1221 / BurnieCoinflip:730 / sStonk:492); per-admin-function rationale present (2-4 sentences answering design-intent + break-on-naive-gate + residual-EV) for EVERY R-NN entry including tactic-(a) entries; no row mentions phantom `adminSeedTraitBucket` / `adminClearTraitBucket`; zero SAFE_BY_DESIGN; source-tree + catalog + KI unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author §4 consolidated handoff register + §5 grep-completeness gate + §0 executive summary (ADMA-04 + completeness attestation + Phase 303 §3.E roll-up source)</name>
  <files>.planning/ADMIN-AUDIT.md</files>
  <read_first>
    - .planning/ADMIN-AUDIT.md (the in-progress artifact from Tasks 1 + 2 + 3; specifically §1 + §1.E + §2 + §3 rows)
    - .planning/RNGLOCK-CATALOG.md §17 (Phase 298 CAT-06 grep-gate completeness attestation — reference format for §5)
    - .planning/RNGLOCK-CATALOG.md §0 (Phase 298 executive summary — reference shape for §0)
    - .planning/phases/300-admin-path-enumeration-audit-adma/300-CONTEXT.md (D-300-ADMA-LAYOUT-01 §0 + §4 + §5 specifications; D-300-KI-01 KI-untouched)
    - Project memory: feedback_verify_call_graph_against_source.md (grep-gate must reproduce hit counts; no "by construction" claims)
  </read_first>
  <action>
    Author the remaining three sections (§4 + §5 + §0) inside `.planning/ADMIN-AUDIT.md`, replacing the placeholder text from Task 1.

    **§4 — v44.0 FIX-MILESTONE Consolidated Handoff Register:**

    Per `D-300-ADMA-LAYOUT-01`: deduplicated `D-43N-V44-ADMA-NN` ID list with one summary line per ID, ordered numerically. Format:

    ```
    | Anchor ID | Admin fn | Slot(s) reached | Admin-class | Tactic | v44.0 sub-phase scope summary |
    |-----------|----------|-----------------|-------------|--------|------------------------------|
    | D-43N-V44-ADMA-01 | {Contract.fn @ file:line} | {S-NN list} | {class} | {tactic} | {1-line scope} |
    | ... | ... | ... | ... | ... | ... |
    | D-43N-V44-ADMA-ERRATUM-01 | (catalog erratum, no admin fn) | S-06 (phantom) | n/a | catalog-correction | RNGLOCK-CATALOG.md §15 rows 154/155/156 + §16 V-016/V-017/V-018 + §C.3.2/C.3.3 enumerate phantom admin trait-bucket writers; source verification (grep adminSeedTraitBucket / adminClearTraitBucket in contracts/) returns 0 hits; the actual S-06 writer `_raritySymbolBatch` is INTERNAL-only EXEMPT-ADVANCEGAME. v44 plan-phase MUST NOT spend a sub-phase on these phantom functions; OPTIONAL future catalog-revision phase may correct §15/§16/§C.3.2/§C.3.3. |
    ```

    Plus an "Admin-class grouping recap" at the end of §4 listing how many anchors fall in each class (governance: N; parameter-update: N; charity-allowlist: N; decimator-config: N; presale-config: N; general: N). The ERRATUM-01 entry sits in its own row outside the per-class grouping (it is not an admin function). v44.0 plan-phase may group sub-phases by class per `D-300-CONTEXT.md` <deferred> note — cross-admin-class FIX wave grouping is explicitly deferred to v44.0 plan-phase discretion.

    Verify with grep that every D-43N-V44-ADMA-NN ID emitted in §3 also appears in §4 (and vice versa, modulo ERRATUM-01 which appears only in §4 + §1.E) — emit attestation line at end of §4: `**§4 ↔ §3 anchor parity:** PASS — every numbered D-43N-V44-ADMA-NN ID emitted in §3 appears in §4; the catalog-erratum entry D-43N-V44-ADMA-ERRATUM-01 is unique to §4 + §1.E by design.`

    **§5 — Grep Completeness Gate Attestation:**

    Per `D-300-ADMA-LAYOUT-01` + Phase 298 §17 CAT-06 precedent. Author as an independent fresh-sweep that reproduces the grep hit counts from Task 1 enumeration and reconciles each hit against an §1 row OR a deliberate-exclusion attestation.

    **Six grep patterns to execute + record:**

    1. **Pattern 1 — formal `onlyOwner`:** `grep -rn "onlyOwner" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/`. Record total hits + per-file breakdown. Each hit: either modifier-definition or modifier-usage. Modifier-definitions go to the §1 discovered-modifier index. Each modifier-usage hit at a function definition must cross-reference an A-NN row in §1.
    2. **Pattern 2 — formal `onlyAdmin` / `onlyRole` / `onlyVaultOwner`:** `grep -rnE "onlyAdmin|onlyRole|onlyVaultOwner" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/`. Same accounting as Pattern 1.
    3. **Pattern 3 — hand-rolled ADMIN/CREATOR gates:** `grep -rnE "msg\.sender\s*!=\s*ContractAddresses\.(ADMIN|CREATOR)" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/ | grep -v ContractAddresses\.sol`. Each hit must cross-reference an A-NN row.
    4. **Pattern 4 — hand-rolled vault-owner gates:** `grep -rnE "!vault\.isVaultOwner\(msg\.sender\)|!_isVaultOwner\(msg\.sender\)" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/`. Each hit must cross-reference an A-NN row OR an explicit-exclusion attestation (e.g., DegenerusAdmin :507 / :670 are internal discriminator branches per §1 carve-out).
    5. **Pattern 5 — integration-trust-boundary modifiers (deliberate-exclusion attestation):** `grep -rnE "onlyGame|onlyCoin|onlyCoinflip|onlyVault\b|onlyBurnieCoin|onlyFlipCreditors|onlyDegenerusGameContract" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/`. Each hit is DELIBERATELY EXCLUDED from §1 per D-300-ENUM-SCOPE-01 carve-out (integration trust boundaries are not admin functions). Record the count + emit attestation: `Pattern 5 hits: N — all deliberately excluded per integration-trust-boundary carve-out (D-300-ENUM-SCOPE-01); zero hits map to admin-class functions.`
    6. **Pattern 6 — negative confirmation of phantom admin functions (catalog-erratum gate):** `grep -rnE "adminSeedTraitBucket|adminClearTraitBucket" contracts/ --include="*.sol" | grep -v contracts/test/ | grep -v contracts/mocks/ | grep -v contracts/interfaces/ | wc -l`. Expected: **0**. This pattern attests that the phantom admin functions referenced by RNGLOCK-CATALOG §15/§16/§C.3.2/§C.3.3 do NOT exist in source. Record the count (MUST be 0) + emit attestation: `Pattern 6 (negative confirmation): 0 hits — phantom admin trait-bucket writers absent from source per §1.E carry forward; D-43N-V44-ADMA-ERRATUM-01 covers the catalog erratum.` If Pattern 6 returns > 0, FAIL — re-verify §1.E and revisit S-06 enumeration.

    **§5 verdict:** Emit `PASS` if every Pattern 1-4 hit is accounted for by either an A-NN row OR an explicit-exclusion attestation (modifier-definition → preamble; pure-view function → exclusion line; internal helper not externally reachable → exclusion line; EOA self-config setter → exclusion line; internal discriminator branch → exclusion line) AND Pattern 6 returns 0 hits. Emit `FAIL` if any Pattern 1-4 hit is unaccounted-for OR Pattern 6 returns > 0 (do NOT proceed to commit if FAIL — fix §1 enumeration first).

    Per `feedback_verify_call_graph_against_source.md` discipline: §5 is the explicit-enumeration gate; no "covered by single fn" or "by construction" claims permitted.

    **§0 — Executive Summary** (authored LAST, after §1..§5 are complete):

    Per `D-300-ADMA-LAYOUT-01`: admin function count by role-gate type + participating-slot-writer subset count + recommendation count by admin-class + v44.0 handoff anchor count. Format as an inline metrics paragraph + a metrics table.

    Required metrics:
    - Total admin functions enumerated (§1 row count A-NN; expected floor ~37)
    - Breakdown by role-gate type (onlyOwner formal: N; onlyVaultOwner formal: N; hand-rolled ADMIN: N; hand-rolled CREATOR: N; hand-rolled isVaultOwner: N)
    - Participating-slot-writer subset count (§2 rows with verdict = VIOLATION)
    - §3 recommendation entry count (R-NN total)
    - Breakdown of §3 entries by admin-class (governance: N; parameter-update: N; charity-allowlist: N; decimator-config: N; presale-config: N; general: N)
    - Total D-43N-V44-ADMA-NN anchors emitted in §4 (numbered anchors + the special ERRATUM-01 entry)
    - §5 grep-completeness verdict (PASS / FAIL) including Pattern 6 negative-confirmation result
    - §1.E catalog-erratum carry-forward attestation (S-06 has zero admin-class writers; phantom functions absent from source)

    Plus a Headline Findings paragraph (top 2-3 by structural risk — admin functions that write the most participating slots, e.g., `updateVrfCoordinatorAndSub` multi-slot VRF rotation; adminSwap/Stake ETH-balance writers feeding S-20). Each headline cites a §3 row R-NN + a D-43N-V44-ADMA-NN anchor. Note explicitly that the prior catalog-suggested S-06 admin trait-bucket cluster (V-016/V-017/V-018) is a catalog erratum, not a headline finding — see §1.E + D-43N-V44-ADMA-ERRATUM-01.

    Plus a Downstream consumers paragraph: Phase 301 FUZZ-02 reads §1 admin function enumeration as the FUZZ action-set input; Phase 303 TERMINAL §3.E ADMA roll-up sources its prose from this §0; v44.0 FIX-MILESTONE plan-phase reads §4 consolidated handoff register as load-bearing input (including the ERRATUM-01 entry so v44 plan-phase skips phantom-function sub-phases).

    **Document tail:** Audit metadata footer — generation date, dependencies (RNGLOCK-CATALOG.md), posture restated, KI policy (UNMODIFIED per D-300-KI-01), zero `contracts/` + zero `test/` source-tree mutation attestation.

    After all sections are written, run the comprehensive verification grep gate (below) and emit a 1-paragraph completion summary citing: total §1 admin functions enumerated, §2 cross-reference rows, §3 recommendation entries, §4 consolidated anchor count, §5 verdict (including Pattern 6).

    Do NOT modify `contracts/` or `test/`. Do NOT modify RNGLOCK-CATALOG.md. Do NOT modify KNOWN-ISSUES.md. Do NOT use SAFE_BY_DESIGN.
  </action>
  <verify>
    <automated>
test -f .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §0 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §4 " .planning/ADMIN-AUDIT.md \
  && grep -qE "^## §5 " .planning/ADMIN-AUDIT.md \
  && awk '/^## §0 /,/^## §1 /' .planning/ADMIN-AUDIT.md | grep -qE "Total admin functions|admin function count" \
  && awk '/^## §0 /,/^## §1 /' .planning/ADMIN-AUDIT.md | grep -qE "catalog erratum|§1\.E|ERRATUM-01" \
  && awk '/^## §4 /,/^## §5 /' .planning/ADMIN-AUDIT.md | grep -qE "D-43N-V44-ADMA-[0-9]+" \
  && awk '/^## §4 /,/^## §5 /' .planning/ADMIN-AUDIT.md | grep -q "D-43N-V44-ADMA-ERRATUM-01" \
  && awk '/^## §4 /,/^## §5 /' .planning/ADMIN-AUDIT.md | grep -qE "anchor parity|PASS|Admin-class grouping" \
  && awk '/^## §5 /,/Audit metadata|^---/' .planning/ADMIN-AUDIT.md | grep -qE "Pattern 1|Pattern 2|Pattern 3|Pattern 4|Pattern 5|Pattern 6" \
  && awk '/^## §5 /,/Audit metadata|^---/' .planning/ADMIN-AUDIT.md | grep -qE "PASS|FAIL" \
  && awk '/^## §5 /,/Audit metadata|^---/' .planning/ADMIN-AUDIT.md | grep -qE "integration-trust-boundary|D-300-ENUM-SCOPE-01" \
  && awk '/^## §5 /,/Audit metadata|^---/' .planning/ADMIN-AUDIT.md | grep -qE "adminSeedTraitBucket|negative confirmation|Pattern 6" \
  && bash -c '
    # Pattern 6 verification: confirm Pattern 6 attests 0 hits.
    p6=$(awk "/^## §5 /,/Audit metadata|^---/" .planning/ADMIN-AUDIT.md | grep -A 3 "Pattern 6")
    if ! printf "%s" "$p6" | grep -qE "0 hits|: 0\b|returns 0"; then
      echo "Pattern 6 must attest 0 hits"
      exit 1
    fi
    # Anchor parity: every numbered D-43N-V44-ADMA-NN in §3 appears in §4. ERRATUM-01 lives only in §4 + §1.E.
    ANCHORS_NUM_3=$(awk "/^## §3 /,/^## §4 /" .planning/ADMIN-AUDIT.md | grep -oE "D-43N-V44-ADMA-[0-9]+" | sort -u)
    ANCHORS_NUM_4=$(awk "/^## §4 /,/^## §5 /" .planning/ADMIN-AUDIT.md | grep -oE "D-43N-V44-ADMA-[0-9]+" | sort -u)
    if [ "$ANCHORS_NUM_3" != "$ANCHORS_NUM_4" ]; then
      echo "Numbered anchor sets differ between §3 and §4"
      diff <(printf "%s\n" "$ANCHORS_NUM_3") <(printf "%s\n" "$ANCHORS_NUM_4")
      exit 1
    fi
    # ERRATUM-01 must be present in §4 and §1.E (and absent from §3 by design).
    if ! awk "/^## §4 /,/^## §5 /" .planning/ADMIN-AUDIT.md | grep -q "D-43N-V44-ADMA-ERRATUM-01"; then
      echo "ERRATUM-01 missing from §4"
      exit 1
    fi
    if ! awk "/^### §1\.E /,/^## §2 /" .planning/ADMIN-AUDIT.md | grep -q "D-43N-V44-ADMA-ERRATUM-01"; then
      echo "ERRATUM-01 missing from §1.E"
      exit 1
    fi
    if awk "/^## §3 /,/^## §4 /" .planning/ADMIN-AUDIT.md | grep -q "D-43N-V44-ADMA-ERRATUM-01"; then
      echo "ERRATUM-01 must NOT appear in §3"
      exit 1
    fi
    exit 0
  ' \
  && ! grep -q "SAFE_BY_DESIGN" .planning/ADMIN-AUDIT.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && [ -z "$(git status --porcelain .planning/RNGLOCK-CATALOG.md .planning/KNOWN-ISSUES.md 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    .planning/ADMIN-AUDIT.md is complete — §0 executive summary + §1 enumeration + §1.E catalog erratum + §2 cross-reference + §3 recommendation + §4 consolidated handoff register (including D-43N-V44-ADMA-ERRATUM-01) + §5 grep-completeness gate (6 patterns including Pattern 6 negative confirmation); §0 includes total counts + admin-class breakdown + headline findings + downstream-consumer paragraph + catalog-erratum attestation; §4 has anchor-parity attestation PASS with numbered-anchor set equality between §3 and §4 and ERRATUM-01 unique to §4 + §1.E; §5 has 6 grep patterns executed with PASS verdict and Pattern 6 attests 0 phantom hits; zero SAFE_BY_DESIGN tokens; contracts/ + test/ + RNGLOCK-CATALOG.md + KNOWN-ISSUES.md unmodified.
  </done>
</task>

</tasks>

<verification>
- `.planning/ADMIN-AUDIT.md` exists at phase close
- §0 executive summary present (admin-function count + participating-slot-writer subset count + recommendation count by admin-class + v44.0 handoff anchor count + headline findings + downstream-consumer paragraph + catalog-erratum attestation)
- §1 complete admin function enumeration (ADMA-01) — ≥35 A-NN rows (expected floor ~37) with file:line + role-gate annotation + admin-class classification; every row passes per-row source-existence check (file:line resolves to an external/public function declaration within ±5 lines)
- §1.E catalog-erratum carry-forward subsection present (RNGLOCK-CATALOG.md S-06 phantom-row record + ZERO-admin-writer attestation)
- §2 participating-slot cross-reference (ADMA-02) — every §1 row appears with participating-slot column + RNGLOCK-CATALOG §15 cross-reference + verdict (VIOLATION / EXEMPT-* / N/A); no row references the phantom S-06 admin writers
- §3 per-admin-function recommendation (ADMA-03) — one entry per §2 VIOLATION row (row count anchored to §2 VIOLATION count, ≥5 sanity floor); each entry has admin-class disposition + recommended gating mechanism (RngLocked preferred per D-300-GATING-MECHANISM-01) + per-admin-function rationale (4-question walk for EVERY entry including tactic-(a)) + D-43N-V44-ADMA-NN anchor (ADMA-04)
- §4 consolidated v44.0 handoff register — deduplicated D-43N-V44-ADMA-NN ID list + D-43N-V44-ADMA-ERRATUM-01 catalog-erratum entry + admin-class grouping recap + §3 ↔ §4 anchor-parity attestation
- §5 grep-completeness gate — 6 grep patterns executed with hit counts + per-hit reconciliation + PASS/FAIL verdict; Pattern 6 negative confirmation attests `adminSeedTraitBucket|adminClearTraitBucket` returns 0 hits
- Zero `SAFE_BY_DESIGN` tokens
- Zero `contracts/` + `test/` source-tree mutations (`git status --porcelain contracts/ test/` returns empty)
- RNGLOCK-CATALOG.md + KNOWN-ISSUES.md unmodified (Phase 298 closed artifact + D-300-KI-01)
- ADMA-01 ✓ (§1 + §1.E) + ADMA-02 ✓ (§2) + ADMA-03 ✓ (§3) + ADMA-04 ✓ (§3 + §4 + ERRATUM-01)
</verification>

<success_criteria>
Single AGENT-COMMITTED canonical Phase 300 ADMA deliverable `.planning/ADMIN-AUDIT.md` complete per `D-300-ADMA-LAYOUT-01`. All ADMA-01..04 requirements satisfied. §1.E catalog-erratum carry forward records RNGLOCK-CATALOG.md S-06 phantom-row anomaly with source-verification grep; §4 D-43N-V44-ADMA-ERRATUM-01 carries the erratum to v44.0 plan-phase. Phase 303 TERMINAL §3.E ADMA roll-up sources prose from §0. v44.0 FIX-MILESTONE plan-phase has load-bearing handoff register input via §4. Phase 301 FUZZ-02 action set sources admin function list from §1. Audit-only posture maintained: zero `contracts/` + `test/` mutations; KNOWN-ISSUES.md UNMODIFIED.
</success_criteria>

<output>
Create `.planning/phases/300-admin-path-enumeration-audit-adma/300-01-SUMMARY.md` when done.
</output>
