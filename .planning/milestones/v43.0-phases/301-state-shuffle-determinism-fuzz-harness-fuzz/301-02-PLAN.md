---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-03
  - FUZZ-04

must_haves:
  truths:
    - "Jackpot-cluster contribution authors 2 of the remaining 11 per-consumer fuzz functions (PayDailyJackpotCoinAndTickets + RunTerminalDecimatorJackpot)."
    - "Each function uses the LOCKED 6-phase template from plan 01 verbatim — only the per-consumer setUp preconditions + VRF-derived output capture + assertion-target lines differ."
    - "Each function's setUp + VRF-output capture cites the catalog §N entry that defines its consumer surface (§2 for PayDailyJackpotCoinAndTickets; §4 for RunTerminalDecimatorJackpot) per `feedback_verify_call_graph_against_source.md` grep-discipline."
  artifacts:
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol"
      provides: "Jackpot-family per-consumer fuzz functions (PayDailyJackpotCoinAndTickets + RunTerminalDecimatorJackpot)"
      contains: "testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets"
  key_links:
    - from: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol"
      to: "test/fuzz/RngLockDeterminism.t.sol"
      via: "Wave 2 aggregator (301-06-PLAN.md) concatenation"
      pattern: "testFuzz_RngLockDeterminism_(PayDailyJackpotCoinAndTickets|RunTerminalDecimatorJackpot)"
---

<objective>
Author the jackpot-family cluster contribution for the Phase 301 Foundry harness. Covers 2 of the 11 remaining per-consumer fuzz functions after plan 01's scaffold:

- `testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets` — catalog §2 — `JackpotModule.payDailyJackpotCoinAndTickets` at file:line 596 — VRF-derived outputs: coin-and-tickets recipient set, ticket allocation across queue, hero-byte override
- `testFuzz_RngLockDeterminism_RunTerminalDecimatorJackpot` — catalog §4 — `DecimatorModule.runTerminalDecimatorJackpot` at file:line 755 — VRF-derived outputs: decimator-tier recipient set, per-bucket payout split

Plan 01 already authored the 2 reference functions for the jackpot family (`PayDailyJackpot` §1 + `RunTerminalJackpot` §3); this cluster completes the family.

Purpose: Parallel-author per-consumer fuzz functions WITHOUT file-overlap conflicts with sibling cluster plans 03/04/05. Each cluster writes its own contribution file in the phase directory; Wave 2 aggregator merges. Mirrors Phase 299 cluster-then-aggregate shape.

Output: Single contribution file `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol` containing 2 fuzz functions. Zero `contracts/` mutations. Zero writes to `test/` tree at this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
@.planning/RNGLOCK-FIXREC.md
@test/fuzz/LootboxRngLifecycle.t.sol
@test/fuzz/helpers/DeployProtocol.sol
@test/fuzz/helpers/VRFHandler.sol

<interfaces>
Catalog §2 consumer surface (`PayDailyJackpotCoinAndTickets`): `JackpotModule.payDailyJackpotCoinAndTickets` at `contracts/modules/DegenerusGameJackpotModule.sol:596`. Triggered from `advanceGame` after `dailyJackpotCoinTicketsPending == true` is set by Phase-1 `payDailyJackpot(true, …)`. SLOAD table at .planning/RNGLOCK-CATALOG.md lines 1080..1107 enumerates the 12 participating slots. VRF-derived outputs: coin-and-tickets winner address, ticket queue allocation snapshot, hero-byte.

Catalog §4 consumer surface (`RunTerminalDecimatorJackpot`): `DecimatorModule.runTerminalDecimatorJackpot` at `contracts/modules/DegenerusGameDecimatorModule.sol:755`. Triggered when `gameOver == true` AND decimator-tier outflow conditions met. VRF-derived outputs: decimator recipient set per tier, per-bucket payout split.

Scaffold contribution (plan 01 output, paste source for aggregator): `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol` defines `_perturb(uint256 seed)` + `_advanceToVrfRequestBoundary()` + `_deliverMockVrf(uint256, uint256)` + `_snapshotPreLock()` + `_revertToPreLock(uint256)` + `_assertVrfOutputByteIdentity(bytes32, bytes32, string)` + `_completeDay(uint256)` + storage-slot readers. This cluster's functions invoke these helpers by name; the helpers physically exist in the aggregated `test/fuzz/RngLockDeterminism.t.sol` at Wave 2 paste time.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PayDailyJackpotCoinAndTickets fuzz function (catalog §2)</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md (full)
    - .planning/RNGLOCK-CATALOG.md §2 lines 1027..1276 (PayDailyJackpotCoinAndTickets — CAT-01 traced functions + CAT-02 SLOAD table + CAT-04 verdict matrix)
    - test/fuzz/LootboxRngLifecycle.t.sol lines 1..230 (pattern reference)
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-PLAN.md (locked 6-phase template; helper signatures)
  </read_first>
  <action>
    Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol`. The file is a PASTE-SOURCE for the Wave 2 aggregator — NOT compiled in place; no contract header, no closing `}` — only the per-consumer function bodies.

    Open the file with header:
    - `// ANCHOR: CLUSTER_JACKPOT_OPEN`
    - NatSpec block citing `D-301-COVERAGE-01` 13-consumer enumeration; this cluster contributes 2 of 11 remaining functions.

    Author `// ANCHOR: FUNC_PayDailyJackpotCoinAndTickets` followed by `function testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets(uint256 vrfWord, uint256 perturbSeed) public` per the 6-phase template (D-301-HARNESS-ARCH-01):

    - Phase 1 (Setup): `vm.assume(vrfWord != 0); uint256 preLockSnap = _snapshotPreLock();` Per catalog §2, the consumer fires only when Phase-1 (`payDailyJackpot(true, …)`) has stored `dailyJackpotCoinTicketsPending = true` and queued `dailyTicketBudgetsPacked`. The setUp arranges this by: (a) completing day 1 to seed `dailyIdx`, (b) warping +1 day, (c) bootstrapping a coin-funded purchase via `game.purchase{value: ...}` with `MintPaymentKind.WithCoin` (or whatever the actual `MintPaymentKind` value is — executor: grep `contracts/interfaces/IDegenerusGame.sol` for the `MintPaymentKind` enum members and pick the coin-bearing variant) so the coin-and-tickets pool is non-zero, (d) calling `game.advanceGame()` to fire Phase-1 daily jackpot + queue the Phase-2 pending flag, (e) fulfilling the Phase-1 VRF with a deterministic seed `0xDEAD0001` (using `_deliverMockVrf`), (f) verifying Phase-2 is now armed via `dailyJackpotCoinTicketsPending` flag check (read via storage slot or public getter; consult catalog §2 SLOAD table for the slot constant). The next `advanceGame()` call will request VRF for Phase-2.
    - Phase 2 (Lock): `game.advanceGame(); uint256 reqId = mockVRF.lastRequestId(); assertTrue(game.rngLocked(), "Phase-2 rngLock must engage"); assertTrue(reqId != 0, "VRF request must be pending"); uint256 lockSnap = vm.snapshot();`
    - Phase 3 (Perturbation): `_perturb(perturbSeed); assertTrue(game.rngLocked(), "lock must not lift under perturbation");`
    - Phase 4 (Resolution under perturbation): `_deliverMockVrf(reqId, vrfWord);` Capture VRF-derived outputs per §2 — coin-and-tickets winner address (via public getter; grep contracts/ for `lastCoinAndTicketsWinner` or similar; if no public getter exists, read storage slot per catalog §2 SLOAD table), ticket-allocation snapshot (read `ticketWriteSlot` from storage slot 325 per §2 line 1208 — or whatever the catalog §2 entry specifies), hero-byte. Pack: `bytes32 perturbedOutputs = keccak256(abi.encode(winner, ticketWriteSlotValue, heroByte));`
    - Phase 5 (Baseline): `_revertToPreLock(preLockSnap);` re-execute Phase 1 + Phase 2 without `_perturb`. Capture `bytes32 baselineOutputs`.
    - Phase 6 (Assert): `_assertVrfOutputByteIdentity(perturbedOutputs, baselineOutputs, "PayDailyJackpotCoinAndTickets VRF outputs must be byte-identical under perturbation");`

    Per-line NatSpec comments cite `RNGLOCK-CATALOG.md §2` + relevant SLOAD-table entries per `feedback_verify_call_graph_against_source.md`.

    If a public getter for the winner address does not exist, fall back to reading the storage slot directly (mirror LootboxRngLifecycle.t.sol `vm.load` pattern). Use `forge inspect DegenerusGame storage-layout` (via `Bash`) to confirm slot indices if needed. Cite the slot derivation in NatSpec.

    Trailing anchor: `// ANCHOR: FUNC_PayDailyJackpotCoinAndTickets_END`.

    Zero `contracts/` mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "// ANCHOR: FUNC_PayDailyJackpotCoinAndTickets" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §2" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>PayDailyJackpotCoinAndTickets function authored using locked 6-phase template; catalog §2 cited in NatSpec; zero contracts/ + test/ mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Author RunTerminalDecimatorJackpot fuzz function (catalog §4) + SUMMARY</name>
  <files>
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol (the file produced in Task 1 — append, do not replace)
    - .planning/RNGLOCK-CATALOG.md §4 (RunTerminalDecimatorJackpot consumer surface — grep `^## §4 ` to find the line range; read CAT-01 + CAT-02 SLOAD table + CAT-04 verdict matrix)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    APPEND to `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol` (do not overwrite Task 1's content).

    Add `// ANCHOR: FUNC_RunTerminalDecimatorJackpot` followed by `function testFuzz_RngLockDeterminism_RunTerminalDecimatorJackpot(uint256 vrfWord, uint256 perturbSeed) public` per the 6-phase template. Per catalog §4, the terminal-decimator-jackpot consumer fires only when `gameOver == true` AND decimator-tier outflow preconditions are met. Setup phase arranges this — if the preconditions cannot be arranged in a fuzz iteration, use `vm.assume(false)` to filter, OR use a more aggressive setUp pattern (advance the game to gameOver state via repeated `_completeDay` loops with decimator-burn actions interspersed; consult catalog §4 SLOAD table + DecimatorModule source for the precondition checks).

    VRF-derived outputs per §4: decimator-tier recipient set (likely an array; pack via `keccak256(abi.encode(recipients))`), per-bucket payout split (read from storage or public getter). Pack: `bytes32 perturbedOutputs = keccak256(abi.encode(recipientsHash, payoutSplitHash));`

    Trailing anchor `// ANCHOR: FUNC_RunTerminalDecimatorJackpot_END` and overall cluster-close `// ANCHOR: CLUSTER_JACKPOT_END`.

    Per-line NatSpec cites `RNGLOCK-CATALOG.md §4`.

    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-SUMMARY.md` per GSD summary template — document the 2 functions authored, their consumer surfaces, locked 6-phase template adherence, anchor list, and zero contracts/+test/ mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_RunTerminalDecimatorJackpot" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "// ANCHOR: CLUSTER_JACKPOT_END" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §4" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol && \
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-SUMMARY.md && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>Both per-consumer functions authored; cluster file closes with CLUSTER_JACKPOT_END anchor; SUMMARY emitted; zero contracts/+test/ mutations.</done>
</task>

</tasks>

<verification>
- 301-02-JACKPOT-CLUSTER-contribution.sol contains 2 testFuzz functions (PayDailyJackpotCoinAndTickets + RunTerminalDecimatorJackpot)
- Each function uses 6-phase template + cites catalog §N
- git status --porcelain contracts/ returns empty
- git status --porcelain test/ returns empty
- 301-02-SUMMARY.md exists
</verification>

<success_criteria>
Jackpot-family cluster contribution exists in the phase directory ready for Wave 2 aggregation. Both functions follow the locked 6-phase template from plan 01.
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-SUMMARY.md` when done.
</output>
