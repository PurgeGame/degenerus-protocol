---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-02
  - FUZZ-05

must_haves:
  truths:
    - "Edge-case cluster contribution authors exactly 5 edge-case fuzz functions per D-301-EDGE-CASES-01: testFuzz_EdgeCase_AdminDuringLock + testFuzz_EdgeCase_NearEndOfWindow + testFuzz_EdgeCase_MultiTxBatch + testFuzz_EdgeCase_MultiBlock + testFuzz_EdgeCase_RetryLootboxRngDuringLock."
    - "testFuzz_EdgeCase_AdminDuringLock exercises every admin function from ADMIN-AUDIT.md §3 R-01..R-22 enumeration (FUZZ-02 admin action set + FUZZ-05 admin-during-lock edge case)."
    - "Each function uses an extended 6-phase template that incorporates the edge-specific perturbation pattern (admin-only / near-end-of-window / multi-tx-batch / multi-block / retry-during-lock)."
  artifacts:
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol"
      provides: "5 edge-case fuzz functions per D-301-EDGE-CASES-01"
      contains: "testFuzz_EdgeCase_AdminDuringLock"
  key_links:
    - from: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol"
      to: "test/fuzz/RngLockDeterminism.t.sol"
      via: "Wave 2 aggregator concatenation"
      pattern: "testFuzz_EdgeCase_(AdminDuringLock|NearEndOfWindow|MultiTxBatch|MultiBlock|RetryLootboxRngDuringLock)"
---

<objective>
Author the edge-case cluster contribution. Covers the 5 edge-case fuzz functions enumerated in `D-301-EDGE-CASES-01`:

1. `testFuzz_EdgeCase_AdminDuringLock` — perturbations exclusively via admin/owner functions from `ADMIN-AUDIT.md §3` R-01..R-22 enumeration; asserts byte-identical VRF outputs across a representative consumer surface (jackpot or lootbox)
2. `testFuzz_EdgeCase_NearEndOfWindow` — perturbations in the last block before unlock; asserts byte-identity
3. `testFuzz_EdgeCase_MultiTxBatch` — multiple perturbations within a single block (multiple `vm.prank` + action calls without `vm.roll`); asserts byte-identity
4. `testFuzz_EdgeCase_MultiBlock` — perturbations across multiple blocks within the rngLock window (using `vm.roll` to advance block number); asserts byte-identity
5. `testFuzz_EdgeCase_RetryLootboxRngDuringLock` — `retryLootboxRng()` failsafe invocation during the rngLock window; this is the FAILSAFE-PATH-DURING-LOCK interaction edge case, distinct from plan 04's consumer-surface RetryLootboxRng function (which tests the canonical fresh-VRF substitution semantics; this edge case tests retry's interaction with a concurrently-perturbed lock state)

Output: `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol`. Zero `contracts/` + `test/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/ADMIN-AUDIT.md
@.planning/RNGLOCK-CATALOG.md
@test/fuzz/helpers/DeployProtocol.sol

<interfaces>
ADMIN-AUDIT.md §3 R-01..R-22 admin function enumeration (22 admin functions):
- R-01: DegenerusGameAdvanceModule.wireVrf
- R-02: DegenerusGameAdvanceModule.updateVrfCoordinatorAndSub
- R-03: DegenerusGame.adminSwapEthForStEth
- R-04: DegenerusGame.adminStakeEthForStEth
- R-05: DegenerusAdmin.swapGameEthForStEth
- R-06: GNRUS.setCharity
- R-07: DegenerusVault.gamePurchase
- R-08: DegenerusVault.gamePurchaseTicketsBurnie
- R-09: DegenerusVault.gamePurchaseBurnieLootbox
- R-10: DegenerusVault.gameOpenLootBox
- R-11: DegenerusVault.gamePurchaseDeityPassFromBoon
- R-12: DegenerusVault.gameDegeneretteBet
- R-13: DegenerusVault.gameSetAutoRebuy
- R-14: DegenerusVault.gameSetAutoRebuyTakeProfit
- R-15: DegenerusVault.gameSetAfKingMode
- R-16: DegenerusVault.coinDepositCoinflip
- R-17: DegenerusVault.coinDecimatorBurn
- R-18: DegenerusVault.gameClaimWinnings
- R-19: DegenerusVault.gameClaimWhalePass
- R-20: DegenerusVault.jackpotsClaimDecimator
- R-21: DegenerusVault.sdgnrsBurn
- R-22: DegenerusVault.sdgnrsClaimRedemption

Each entry at `.planning/ADMIN-AUDIT.md §3.NN` documents the gating role, the participating slots touched, and the recommended `RngLocked` revert mechanism (deferred to v44.0).

Scaffold helpers from plan 01 are available via Wave 2 concatenation: `_perturb`, `_advanceToVrfRequestBoundary`, `_deliverMockVrf`, `_snapshotPreLock`, `_revertToPreLock`, `_assertVrfOutputByteIdentity`, `_completeDay`.

For the AdminDuringLock function, an admin-action-only perturbation helper is required — this plan authors `_perturbAdminOnly(uint256 seed)` as a NEW helper specifically for edge-case use (separate from plan 01's general `_perturb`).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author AdminDuringLock + NearEndOfWindow + MultiTxBatch functions</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md (D-301-EDGE-CASES-01 5-function enumeration)
    - .planning/ADMIN-AUDIT.md §1 lines 68..148 (ADMA-01 admin function enumeration)
    - .planning/ADMIN-AUDIT.md §3 lines 242..470 (ADMA-03 per-admin-function recommendation; details slot writes per admin function)
    - .planning/RNGLOCK-CATALOG.md §1 (PayDailyJackpot — the representative consumer surface for the AdminDuringLock edge case)
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-PLAN.md (locked 6-phase template; helper signatures)
  </read_first>
  <action>
    Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol`. Paste-source; no contract header; no closing `}`.

    Open: `// ANCHOR: CLUSTER_EDGECASE_OPEN` + NatSpec citing D-301-EDGE-CASES-01.

    Author `// ANCHOR: HELPER_perturbAdminOnly` followed by `function _perturbAdminOnly(uint256 seed) internal` — an admin-action-only perturbation helper that draws from the 22 ADMA-01 admin entries (R-01..R-22) via `seed % 22`. Each case `vm.prank(address(admin))` (or the appropriate role-holder) then invokes the admin function with bounded fuzz inputs. For admin functions that require specific preconditions, use `try ... catch { return; }` so unsuitable iterations no-op without failing. Document each case with a NatSpec comment citing `.planning/ADMIN-AUDIT.md §3.NN`. Trailing `// ANCHOR: HELPER_perturbAdminOnly_END`.

    Author `// ANCHOR: FUNC_EdgeCase_AdminDuringLock` followed by `function testFuzz_EdgeCase_AdminDuringLock(uint256 vrfWord, uint256 adminSeed) public` per the 6-phase template. Phase 3 perturbation calls `_perturbAdminOnly(adminSeed)` (NOT the general `_perturb`). Representative consumer surface: PayDailyJackpot (catalog §1) for assertion-target capture. Pack outputs identically to plan 01's reference function. NatSpec cites ADMIN-AUDIT.md §3 + RNGLOCK-CATALOG.md §1.

    Trailing `// ANCHOR: FUNC_EdgeCase_AdminDuringLock_END`.

    Author `// ANCHOR: FUNC_EdgeCase_NearEndOfWindow` followed by `function testFuzz_EdgeCase_NearEndOfWindow(uint256 vrfWord, uint256 perturbSeed) public` per the 6-phase template, modified for near-end-of-window timing:
    - Setup: arrange to VRF-request boundary as usual.
    - Lock: `assertTrue(game.rngLocked());`
    - Pre-perturb timing manipulation: advance `block.timestamp` to the last block before the rngLock window would expire (per `rngRequestTime` + lock window duration; consult `contracts/storage/DegenerusGameStorage.sol` for the lock-timeout constant — the LootboxRngLifecycle.t.sol `warpPastVrfTimeout()` at VRFHandler:41 warps 18h + 1; the lock-end-window is the (18h - epsilon) boundary). Use `vm.warp(block.timestamp + 18 hours - 1);` (or whatever the actual near-end timing is — executor: grep `contracts/` for `VRF_TIMEOUT` / `RNG_LOCK_TIMEOUT` constant and pick the (timeout - 1 block) boundary).
    - Perturbation: `_perturb(perturbSeed);` (general perturbation at near-end timing).
    - Resolution + Baseline + Assert per template.

    NatSpec cites D-301-EDGE-CASES-01 near-end-of-window.

    Trailing `// ANCHOR: FUNC_EdgeCase_NearEndOfWindow_END`.

    Author `// ANCHOR: FUNC_EdgeCase_MultiTxBatch` followed by `function testFuzz_EdgeCase_MultiTxBatch(uint256 vrfWord, uint256 seedA, uint256 seedB, uint256 seedC) public` per 6-phase template. Phase 3 invokes `_perturb` three times sequentially with three different seeds (`_perturb(seedA); _perturb(seedB); _perturb(seedC);`) WITHOUT calling `vm.roll` between them — the perturbations all land in the same block (multi-tx-batch semantics). NatSpec cites D-301-EDGE-CASES-01 multi-tx-batch.

    Trailing `// ANCHOR: FUNC_EdgeCase_MultiTxBatch_END`.

    Zero `contracts/` mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "_perturbAdminOnly" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "testFuzz_EdgeCase_AdminDuringLock" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "testFuzz_EdgeCase_NearEndOfWindow" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "testFuzz_EdgeCase_MultiTxBatch" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "ADMIN-AUDIT.md §3" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>3 of 5 edge-case functions authored + admin-only perturbation helper; zero contracts/+test/ mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Author MultiBlock + RetryLootboxRngDuringLock functions + SUMMARY</name>
  <files>
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol (Task 1 output — append)
    - .planning/RNGLOCK-CATALOG.md §9 (retryLootboxRng — locate via `grep -n "^## §9 — "`)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    APPEND to `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol`.

    Add `// ANCHOR: FUNC_EdgeCase_MultiBlock` followed by `function testFuzz_EdgeCase_MultiBlock(uint256 vrfWord, uint256 seedA, uint256 seedB, uint8 blockDelta) public` per 6-phase template. Phase 3 invokes `_perturb(seedA); vm.roll(block.number + bound(blockDelta, 1, 100)); _perturb(seedB);` — perturbations span multiple blocks within the rngLock window. Optionally `vm.warp(block.timestamp + 13 * (block.number - <starting block>))` to keep block timestamps coherent (12-13s per Ethereum block). NatSpec cites D-301-EDGE-CASES-01 multi-block.

    Trailing `// ANCHOR: FUNC_EdgeCase_MultiBlock_END`.

    Add `// ANCHOR: FUNC_EdgeCase_RetryLootboxRngDuringLock` followed by `function testFuzz_EdgeCase_RetryLootboxRngDuringLock(uint256 vrfWord, uint256 perturbSeed) public` per a HYBRID template:
    - Setup: arrange to lootbox-RNG VRF-request boundary (stall path; do not deliver original VRF for ≥6h to clear retry cooldown).
    - Lock: `uint256 reqId1 = mockVRF.lastRequestId(); assertTrue(game.rngLocked());`
    - Pre-retry perturbation: `_perturb(perturbSeed);` (general perturbation during the stalled lock window).
    - Stall window: `vm.warp(block.timestamp + 6 hours + 1);`
    - Failsafe invocation: `game.retryLootboxRng();` — fresh VRF request issued.
    - Resolution under perturbation+retry: `uint256 reqId2 = mockVRF.lastRequestId(); _deliverMockVrf(reqId2, vrfWord);` Capture `bytes32 perturbedRetryOutputs`.
    - Baseline (NO perturbation, retry only): `_revertToPreLock(preLockSnap);` re-execute setup + lock + stall + retry + deliver `vrfWord` WITHOUT calling `_perturb`. Capture `bytes32 baselineRetryOutputs`.
    - Assert: `_assertVrfOutputByteIdentity(perturbedRetryOutputs, baselineRetryOutputs, "Retry-during-lock + perturbation must produce byte-identical VRF outputs vs retry-only baseline");`

    This edge case verifies that the failsafe's structural invariant (it does not manipulate any pre-lock state per `D-42N-RETRY-RNG-DOMAIN-SEP-01` Option A) holds even when concurrent perturbations occur during the stall window.

    NatSpec cites RNGLOCK-CATALOG.md §9 + D-42N-RETRY-RNG-DOMAIN-SEP-01.

    Trailing `// ANCHOR: FUNC_EdgeCase_RetryLootboxRngDuringLock_END` + cluster-close `// ANCHOR: CLUSTER_EDGECASE_END`.

    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-SUMMARY.md` per GSD summary template. Document: 5 edge-case functions authored matching D-301-EDGE-CASES-01 enumeration; `_perturbAdminOnly` helper added; anchor list; zero contracts/+test/ mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "testFuzz_EdgeCase_MultiBlock" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "testFuzz_EdgeCase_RetryLootboxRngDuringLock" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      grep -q "// ANCHOR: CLUSTER_EDGECASE_END" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol && \
      [ $(grep -c "^    function testFuzz_EdgeCase_\|^function testFuzz_EdgeCase_" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol) -eq 5 ] && \
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-SUMMARY.md && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>All 5 edge-case functions authored matching D-301-EDGE-CASES-01 enumeration; SUMMARY emitted; zero contracts/+test/ mutations.</done>
</task>

</tasks>

<verification>
- 301-05-EDGECASE-CLUSTER-contribution.sol contains exactly 5 testFuzz_EdgeCase_* functions
- _perturbAdminOnly helper present
- 301-05-SUMMARY.md exists
- git status --porcelain contracts/ + test/ returns empty
</verification>

<success_criteria>
All 5 edge-case fuzz functions authored per D-301-EDGE-CASES-01 (FUZZ-02 + FUZZ-05 satisfied). Edge-case cluster ready for Wave 2 aggregation.
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-SUMMARY.md` when done.
</output>
