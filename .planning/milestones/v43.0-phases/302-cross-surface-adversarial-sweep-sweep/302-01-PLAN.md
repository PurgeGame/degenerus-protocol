---
phase: 302-cross-surface-adversarial-sweep-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md
  - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md
  - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md
  - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md
  - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md
autonomous: false  # Task 5 Tier-1 AskUserQuestion + Task 6 FUZZ-harness AskUserQuestion + Task 7 commit needs user diff review per feedback_manual_review_before_push.md
requirements: [SWP-01, SWP-02, SWP-03, SWP-04, SWP-05]

must_haves:
  truths:
    - "302-ADVERSARIAL-CHARGE.md exists, enumerates SWP-01..05 verbatim + carry-forward augments (i)..(iv) with per-hypothesis evidence anchors per D-302-CHARGE-01"
    - "302-ADVERSARIAL-CONTRACT-AUDITOR.md captures /contract-auditor SEQUENTIAL_MAIN_CONTEXT pass against the CHARGE (SWP-01 satisfied)"
    - "302-ADVERSARIAL-ZERO-DAY-HUNTER.md captures /zero-day-hunter PARALLEL_SUBAGENT pass against the CHARGE (SWP-02 satisfied)"
    - "302-ADVERSARIAL-ECONOMIC-ANALYST.md captures /economic-analyst PARALLEL_SUBAGENT pass against the CHARGE (SWP-03 satisfied)"
    - "302-01-ADVERSARIAL-LOG.md integrates 3 H2 sections (one per skill) + Disposition section applying D-302-CONSENSUS-01 two-tier consensus rule + skeptic-reviewer filter per feedback_skeptic_pass_before_catastrophe.md (SWP-04 satisfied)"
    - "/degen-skeptic remains OUT OF SCOPE per D-271-ADVERSARIAL-02 carry; /economic-analyst IN SCOPE per D-271-ADVERSARIAL-03; pre-authorization via D-43N-SWEEP-PREAUTH-01 documented (SWP-05 satisfied)"
    - "AGENT-COMMIT bundles the 5 artifacts in 1 commit with zero contracts/ + zero test/ mutations"
  artifacts:
    - path: ".planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md"
      provides: "Charge prompt enumerating SWP-01..05 + (i)..(iv) augments + 6-disposition rubric + boilerplate"
      contains: "Hypothesis (i)"
    - path: ".planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md"
      provides: "Per-skill /contract-auditor report"
      contains: "Disposition"
    - path: ".planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md"
      provides: "Per-skill /zero-day-hunter report"
      contains: "Disposition"
    - path: ".planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md"
      provides: "Per-skill /economic-analyst report"
      contains: "Disposition"
    - path: ".planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md"
      provides: "Integrated 3-H2-section log + Disposition applying D-302-CONSENSUS-01"
      contains: "Disposition"
  key_links:
    - from: "302-ADVERSARIAL-CHARGE.md"
      to: "RNGLOCK-FIXREC.md / ADMIN-AUDIT.md / RngLockDeterminism.t.sol / RNGLOCK-CATALOG.md §14"
      via: "per-hypothesis evidence anchors for augments (i)..(iv)"
      pattern: "RNGLOCK-FIXREC\\.md|ADMIN-AUDIT\\.md|RngLockDeterminism|§14"
    - from: "302-01-ADVERSARIAL-LOG.md"
      to: "302-ADVERSARIAL-{CONTRACT-AUDITOR,ZERO-DAY-HUNTER,ECONOMIC-ANALYST}.md"
      via: "per-skill H2 section + Report cross-link"
      pattern: "Report.*302-ADVERSARIAL"
    - from: "302-01-ADVERSARIAL-LOG.md Disposition"
      to: "D-302-CONSENSUS-01 two-tier rule + D-302-AUDIT-ONLY-ROUTING-01 elevation routing"
      via: "consensus-rule attestation line + Tier-1/Tier-2 outcome"
      pattern: "D-302-CONSENSUS-01|two-tier"
---

<objective>
3-skill HYBRID adversarial pass against the v43.0 audit subject (rngLock freeze invariant + Phases 298-301 artifacts) per Phase 296 D-296-INVOKE-01 precedent carried forward verbatim.

Purpose: Red-team every storage path that could violate the freeze invariant — composition attacks, cross-module read/write races, ERC-callback-induced state mutations, multi-block window exploits, game-theoretic write-induced effects, multi-tx batched perturbations — plus 4 v43-specific carry-forward augments back-citing the Phase 299 FIXREC + Phase 300 ADMA + Phase 301 FUZZ harness + Phase 298 §14 unique-slot index.

Output: 5 planner-private artifacts (CHARGE + 3 per-skill reports + integrated LOG) bundled into 1 AGENT-COMMITTED commit. Zero `contracts/` + zero `test/` mutations per audit-only posture (`D-43N-AUDIT-ONLY-01`).

Invocation pre-authorized per `D-43N-SWEEP-PREAUTH-01` (user-authorization 2026-05-18) — the 3-skill HYBRID fires without re-pinging the user. Tier-1 any-single-skill FINDING_CANDIDATE still triggers an AskUserQuestion checkpoint at Task 5 integration per `D-302-CONSENSUS-01`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md

# Phase 298-301 load-bearing artifacts (drive hypothesis augments (i)..(iv))
@.planning/RNGLOCK-CATALOG.md
@.planning/RNGLOCK-FIXREC.md
@.planning/ADMIN-AUDIT.md
@test/fuzz/RngLockDeterminism.t.sol

# v42 P296 precedent — verbatim shape inheritance
@.planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-CONTEXT.md
@.planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-ADVERSARIAL-CHARGE.md
@.planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-01-ADVERSARIAL-LOG.md

# v41 P284 precedent — RE-PASS shape (used only if Tier-2 elevation triggers in Task 6)
@.planning/milestones/v41.0-phases/284-delta-audit-findings-consolidation-terminal/284-ADVERSARIAL-RE-PASS-CONTRACT-AUDITOR.md

# Skill source definitions
@~/.claude/skills/contract-auditor/SKILL.md
@~/.claude/skills/zero-day-hunter/SKILL.md
@~/.claude/skills/economic-analyst/SKILL.md

# Methodology feedback (load-bearing for Task 5 skeptic discipline + RNG audit augments)
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_skeptic_pass_before_catastrophe.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_window_storage_read_freshness.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_verify_call_graph_against_source.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
@~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 302-ADVERSARIAL-CHARGE.md (SWP-01..05 verbatim + augments (i)..(iv))</name>
  <files>.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-CHARGE-01 + D-302-INVOKE-01 + D-302-ARTIFACT-SET-01 + D-302-CONSENSUS-01 verbatim; locked-decision list)
    - .planning/REQUIREMENTS.md (SWP-01..05 verbatim)
    - .planning/ROADMAP.md (Phase 302 entry — 3-skill HYBRID with pre-authorization)
    - .planning/RNGLOCK-FIXREC.md (read §0 Executive Summary + section index 1..37; drives augment (i) tactic-adequacy hypothesis — identify the top-3-by-EV FIXREC entries to anchor)
    - .planning/ADMIN-AUDIT.md (read §0 + §1 + §3 R-01..R-22 admin-function index; drives augment (ii) admin-class cross-interaction hypothesis)
    - test/fuzz/RngLockDeterminism.t.sol (locate the 17 vm.skip blocks via `grep -n "vm.skip\\|SKIP: RNGLOCK-FIXREC" test/fuzz/RngLockDeterminism.t.sol`; drives augment (iii) vm.skip coverage-gap hypothesis)
    - .planning/RNGLOCK-CATALOG.md §14 — Unique-Slot Index (drives augment (iv) cross-consumer entropy bleed hypothesis)
    - .planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-ADVERSARIAL-CHARGE.md (verbatim shape template — Context / Charge / Required output per hypothesis / Disposition Rubric / Boilerplate closing)
  </read_first>
  <action>
    Author `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md` mirroring the v42 P296 CHARGE shape verbatim, substituting v43 audit-subject anchors. Per D-302-CHARGE-01 the charge surface is SWP-01..05 verbatim + 4 carry-forward augments (i)..(iv):

    **Sections (in order):**

    1. **Header + Out-of-scope notice.** Title: "Phase 302 Adversarial-Pass Charge — v43.0 rngLock Freeze Invariant Sweep". For: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` (3-skill SWEEP per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry). Out-of-scope: `/degen-skeptic` per D-271-ADVERSARIAL-02. Invocation pattern: HYBRID per D-302-INVOKE-01 (Task 2 `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT; Tasks 3+4 `/zero-day-hunter` + `/economic-analyst` PARALLEL_SUBAGENT via single-message multi-Task dispatch). Pre-authorized per D-43N-SWEEP-PREAUTH-01.

    2. **Context section.** Describe the v43.0 audit subject — the rngLock freeze invariant ("at `rngLockedFlag = true`, every storage slot that participates in deriving any VRF-influenced output is frozen until `rngLockedFlag = false`"; 3 exempt entry points: `advanceGame()`, the VRF coordinator callback, `retryLootboxRng()` failsafe). State the AUDIT-ONLY posture (`D-43N-AUDIT-ONLY-01`): zero `contracts/` + `test/` mutations; FINDING_CANDIDATE elevation routes to FIXREC-augment per D-302-AUDIT-ONLY-ROUTING-01, NOT a Phase 303a/etc FIX wave. Cross-cite the load-bearing prior artifacts: RNGLOCK-CATALOG.md (Phase 298), RNGLOCK-FIXREC.md (Phase 299), ADMIN-AUDIT.md (Phase 300), test/fuzz/RngLockDeterminism.t.sol (Phase 301). State the milestone goal precludes SAFE_BY_DESIGN dispositions for participating slots (D-43N-AUDIT-ONLY-01 explicit exclusion — "could possibly affect = theoretical reachability; eliminate even if economic likelihood is LOW; game-theoretic analysis is not a substitute for structural elimination"). Document that hypothesis-level SAFE_BY_DESIGN may still apply for non-participating-slot vectors or for hypotheses whose evidence chain demonstrates that the proposed vector does not touch a participating slot.

    3. **Charge.** "Walk the 9 hypothesis surfaces below (5 SWP-NN verbatim + 4 carry-forward augments). Per hypothesis return one of the 6 dispositions: SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN / FINDING_CANDIDATE." Mirror the v42 P296 Required output format block per hypothesis (Disposition + Evidence + Notes; FINDING_CANDIDATE adds Description + Severity estimate per D-08 5-Bucket Severity Rubric + Suggested remediation). Hard requirements (carry verbatim from v42 P296): NO contract code in output; NO future-milestone forward-cite tokens (v44.0 handoff is via locked-decision IDs D-43N-V44-HANDOFF-NN + D-43N-V44-ADMA-NN only); beyond-charge entries permitted with same 6-disposition rubric.

    4. **Hypothesis surfaces.** Author the 9 hypotheses with file:line evidence anchors. Group by SWP requirement, then augments:

       - **SWP-01 (`/contract-auditor` charge — freeze-invariant storage paths):** "Walk every storage path that mutates a participating slot during the rngLock window. For each, attest whether the path is one of the 3 exempt entry points (advanceGame / VRF callback / retryLootboxRng failsafe) or whether it constitutes a VIOLATION per CAT-04. Cross-reference RNGLOCK-CATALOG.md §15 + §16 verdict matrix; surface any participating slot whose freeze gate is missing, incorrectly scoped, or bypassed via composition." Evidence anchors: RNGLOCK-CATALOG.md §14 (unique-slot index — 92+ participating slots); §15 per-slot writer enumeration; §16 verdict matrix; RNGLOCK-FIXREC.md per-VIOLATION recommendation entries §1..§37.
       - **SWP-02 (`/zero-day-hunter` charge — novel attack surfaces):** "Walk composition attacks, cross-module read/write races, ERC-callback-induced state mutations, multi-block window exploits. Specifically check: (a) ERC777-style receive hooks during transferFromPool flows; (b) re-entrancy windows where a participating-slot mutation lands between VRF request and fulfillment; (c) cross-module call graphs where Module A's external function reaches Module B's participating-slot writer during rngLock; (d) multi-block perturbations where the attacker spans the rngLock window across N blocks." Evidence anchors: RNGLOCK-CATALOG.md §6 LootboxModule.resolveRedemptionLootbox + §7 _resolveLootboxCommon + §11 BurnieCoinflip + §12 sStonk + §13 DecimatorModule._awardDecimatorLootbox (high-composition-density consumers).
       - **SWP-03 (`/economic-analyst` charge — game-theoretic write-induced effects):** "Walk incentive-compatible adversarial actions during the rngLock window. For each player class (degenerette bettor, lootbox holder, mint queuer, sStonk staker, decimator claimant, deity pass holder, charity admin, governance admin), identify any action available during the window that (a) is profitable in expectation conditional on VRF outcome and (b) writes a participating slot. Apply the 3-condition catastrophe lens per feedback_skeptic_pass_before_catastrophe.md: a finding is catastrophic only when the slot feeds a VRF-derived output AND is mutable mid-rngLock by a non-EXEMPT actor AND the mutation changes a VRF-derived output the mutator profits from after opportunity cost." Evidence anchors: RNGLOCK-FIXREC.md per-VIOLATION FIXREC-03 actor game-theory walks across §1..§37.
       - **SWP-04 (FINDING_CANDIDATE elevation routing):** "If you surface a FINDING_CANDIDATE: do NOT propose contract changes; describe the vector + suggested remediation tactic (one of: rngLockedFlag-gated revert / snapshot-anchor / re-order to pre-lock / immutable per FIX-01 menu). Elevation routes to FIXREC-augment per D-302-AUDIT-ONLY-ROUTING-01, NOT a contract change." Note that any SAFE_BY_DESIGN candidate for a PARTICIPATING SLOT is REJECTED per the milestone goal — if your evidence chain concludes SAFE_BY_DESIGN on a participating-slot writer, re-classify as SAFE_BY_STRUCTURAL_CLOSURE (if the closure is structural) or FINDING_CANDIDATE (if the closure is design-only and not structural). Non-participating-slot SAFE_BY_DESIGN dispositions remain valid.
       - **SWP-05 (skill set + pre-authorization):** Document that `/degen-skeptic` is OUT OF SCOPE per D-271-ADVERSARIAL-02. `/economic-analyst` is IN SCOPE per D-271-ADVERSARIAL-03. Invocation is pre-authorized per D-43N-SWEEP-PREAUTH-01 (no user-ping for invocation; Tier-1 user-review checkpoint discipline at integration is preserved per D-296-CONSENSUS-01 carry).

       Then the 4 carry-forward augments per D-302-CHARGE-01:

       - **(i) FIXREC-recommended tactic adequacy.** "For a representative subset of Phase 299 FIXREC entries (top-3 by EV magnitude — surface from RNGLOCK-FIXREC.md §0 Executive Summary + the per-VIOLATION FIXREC-03 EV estimates in §1..§37; explicitly include V-184 sStonk cross-day re-roll CATASTROPHE per the skeptic-survived finding, plus the 2 next-highest by EV), does the recommended tactic (rngLockedFlag-gated revert / snapshot-anchor / re-order / immutable) actually close the VIOLATION class structurally, or are there secondary attack paths the recommendation misses? Walk each chosen FIXREC entry's recommended tactic + run the actor game-theory against the post-FIX state." Evidence anchors: RNGLOCK-FIXREC.md §1..§37; §0 Executive Summary EV ranking; explicit V-184 anchor at RNGLOCK-FIXREC.md §sec103 with HANDOFF anchor `D-43N-V44-HANDOFF-111`.
       - **(ii) Admin-class cross-interaction.** "For Phase 300 ADMA recommendations R-01..R-22, are there admin-class combinations (e.g., governance + parameter-update + charity-allowlist + vault-routed sub-call invoked in sequence within rngLock window) that bypass any individual admin gate? Walk the admin call graph cross-product; identify any composition where two admin entry points called in sequence reach a participating slot via a path that neither alone touches." Evidence anchors: ADMIN-AUDIT.md §3.01..§3.22 (R-01..R-22 individual recommendations); §2 cross-reference table; §1 admin function enumeration.
       - **(iii) Phase 301 FUZZ harness vm.skip coverage gaps.** "The harness contains 17 vm.skip blocks per D-301-VMSKIP-MECHANISM-01 Option C, each cross-referencing a RNGLOCK-FIXREC section + HANDOFF-NN anchor. Does the harness exercise enough perturbation classes per CAT-01 consumer to surface all VIOLATION instances, or are there perturbation classes the harness misses? Specifically check: (a) are there participating slots in CAT-01 whose writer set is not exercised by any `_perturb(seed)` or `_perturbAdminOnly(seed)` action? (b) does any vm.skip mask a VIOLATION class that the harness's action coverage genuinely fails to reach (vs the documented skip-for-future-fix-flip pattern)? (c) are there edge cases (multi-tx batched perturbations spanning the window, near-end-of-window perturbations, retryLootboxRng-during-lock perturbations) that should be added?" Evidence anchors: test/fuzz/RngLockDeterminism.t.sol line numbers of the 17 vm.skip blocks (enumerate exact line:label triples in the CHARGE — the executor extracts these via grep before writing); RNGLOCK-CATALOG.md §1..§13 13-consumer surface.
       - **(iv) Cross-consumer entropy bleed.** "For shared participating slots SLOAD'd by multiple consumers (per Phase 298 §14 unique-slot index), do cross-consumer perturbations create entropy correlation that breaks one consumer's determinism via another consumer's resolution path? Apply feedback_rng_window_storage_read_freshness.md discipline: enumerate ALL SLOADs inside the rngLock window, not just VRF-derived seeds; non-VRF reads consumed alongside RNG are a distinct bug class (F-41-02/03 precedent). For each shared slot, identify the set of consumers that read it, and ask: can a write by Consumer A's path during its rngLock window perturb the value Consumer B reads at its consumer site even though the writer is not in B's exempt-entry-point set?" Evidence anchors: RNGLOCK-CATALOG.md §14 Unique-Slot Index (identify slots whose writer-set spans 2+ consumers); §15 per-slot writer enumeration; §16 verdict matrix; cross-cite the F-41-02/03 precedent + feedback_rng_window_storage_read_freshness.md.

       Per-hypothesis section MUST include: (a) hypothesis statement; (b) evidence anchors (file:line citations to load-bearing artifacts); (c) expected disposition class (e.g., "expected SAFE_BY_STRUCTURAL_CLOSURE pending skill confirmation" or "expected FINDING_CANDIDATE pending re-attestation against augmented FIXREC tactic"); (d) cross-cite to relevant feedback memory or carry-forward decision anchor.

    5. **Disposition Rubric section.** Verbatim 6-disposition list (SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN / FINDING_CANDIDATE). Each skill returns disposition per hypothesis + 1-2 sentence evidence note + beyond-charge entries with the same rubric. Hard requirement: NO contract code in output (descriptive remediations only per feedback_never_preapprove_contracts.md). Hard requirement: NO post-v43 milestone forward-cite tokens — use locked-decision IDs (D-43N-V44-HANDOFF-NN + D-43N-V44-ADMA-NN) + descriptive labels only. Beyond-charge invitation: "Skills are free to surface beyond-charge hypotheses where they identify a novel attack surface; document each beyond-charge hypothesis with the same disposition rubric."

    6. **Reference files section.** List absolute paths the skills can read for context: RNGLOCK-CATALOG.md, RNGLOCK-FIXREC.md, ADMIN-AUDIT.md, test/fuzz/RngLockDeterminism.t.sol, the v42 P296 CHARGE + LOG for shape reference, and the contract source files in `contracts/modules/` + `contracts/storage/`.

    7. **Footer.** Phase: 302-cross-surface-adversarial-sweep-sweep; Adversarial-charge: 9 hypothesis surfaces (5 SWP verbatim + 4 augments per D-302-CHARGE-01); 3-skill HYBRID dispatch per D-302-INVOKE-01 (Task 2 SEQUENTIAL_MAIN_CONTEXT for `/contract-auditor`; Tasks 3+4 PARALLEL_SUBAGENT for `/zero-day-hunter` + `/economic-analyst`); D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry; `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02; two-tier consensus rule applies at integration per D-302-CONSENSUS-01; RE-PASS scope candidate-fix-only per D-302-REPASS-SCOPE-01; elevation routes to FIXREC-augment per D-302-AUDIT-ONLY-ROUTING-01 (no contract change at v43 per audit-only posture); beyond-charge entries permitted with same 6-disposition rubric.

    Do NOT pre-fill skill dispositions or invent disposition outcomes. The CHARGE document is the PROMPT — the per-skill MD files (Tasks 2/3/4) contain the dispositions.
  </action>
  <verify>
    <automated>test -f .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md &amp;&amp; grep -c "Hypothesis (i)\\|Hypothesis (ii)\\|Hypothesis (iii)\\|Hypothesis (iv)\\|SWP-01\\|SWP-02\\|SWP-03\\|SWP-04\\|SWP-05\\|D-302-CHARGE-01\\|D-302-CONSENSUS-01\\|D-302-INVOKE-01\\|D-302-AUDIT-ONLY-ROUTING-01\\|D-43N-SWEEP-PREAUTH-01\\|FINDING_CANDIDATE\\|SAFE_BY_STRUCTURAL_CLOSURE" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md | awk '$1 &gt;= 10 {exit 0} {exit 1}'</automated>
  </verify>
  <done>302-ADVERSARIAL-CHARGE.md exists, enumerates SWP-01..05 + augments (i)..(iv), references all required locked-decision IDs, applies 6-disposition rubric, contains explicit evidence anchors for each hypothesis pointing to RNGLOCK-FIXREC.md / ADMIN-AUDIT.md / RngLockDeterminism.t.sol / RNGLOCK-CATALOG.md §14.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch /contract-auditor SEQUENTIAL_MAIN_CONTEXT (SWP-01)</name>
  <files>.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md (Task 1 output — passed verbatim to the skill)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-INVOKE-01 invocation discipline)
    - ~/.claude/skills/contract-auditor/SKILL.md (skill definition)
  </read_first>
  <action>
    Invoke `/contract-auditor` in SEQUENTIAL_MAIN_CONTEXT mode per D-302-INVOKE-01. Pass the body of `302-ADVERSARIAL-CHARGE.md` verbatim as the skill prompt (use the `paper-test` skill-orchestration pattern — do NOT pre-summarize the charge for the skill; the skill reads the full charge fresh and applies its persona). Capture the skill's full output to `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md`.

    The skill MUST return a single message containing 9 H2 sections (`## Hypothesis (i)` SWP-01 through `## Hypothesis (iv)` cross-consumer-entropy-bleed augment — using whichever numbering scheme the CHARGE document established). Each section MUST include Disposition + Evidence + Notes blocks per the format in the CHARGE. If any FINDING_CANDIDATE → Description + Severity estimate (per D-08 5-Bucket Severity Rubric: CRITICAL / HIGH / MEDIUM / LOW / INFO) + Suggested remediation blocks (descriptive only — NO contract code).

    Beyond-charge entries are permitted per the CHARGE document's boilerplate; document each with the same 6-disposition rubric.

    Hard discipline reminders to enforce in the captured output:
    - NO contract code in skill output (per `feedback_never_preapprove_contracts.md` + `feedback_no_contract_commits.md`).
    - NO post-v43 milestone forward-cite tokens (use locked-decision IDs D-43N-V44-HANDOFF-NN + D-43N-V44-ADMA-NN + descriptive labels only).
    - Apply `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` + `feedback_rng_window_storage_read_freshness.md` discipline for any RNG-window analysis.
    - Apply `feedback_verify_call_graph_against_source.md` for any "by construction" / "single fn covers all paths" claim (grep-verify against source pre-attestation).

    Write the captured output with a YAML frontmatter header containing: skill, phase, plan, generated_at, charge_hypothesis_count (9 charged + N beyond-charge if any).

    This task runs SEQUENTIALLY before Tasks 3+4 — the `/contract-auditor` output is captured to file BEFORE the parallel sub-agent dispatch in the next single-message block.
  </action>
  <verify>
    <automated>test -f .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md &amp;&amp; grep -c "## Hypothesis\\|Disposition\\|Evidence\\|Notes" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md | awk '$1 &gt;= 20 {exit 0} {exit 1}'</automated>
  </verify>
  <done>302-ADVERSARIAL-CONTRACT-AUDITOR.md exists with 9 H2 hypothesis sections (+ any beyond-charge), each with Disposition + Evidence + Notes; no contract code; no post-v43 forward-cite tokens; SWP-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch /zero-day-hunter PARALLEL_SUBAGENT (SWP-02)</name>
  <files>.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md (passed verbatim to the skill)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-INVOKE-01 invocation discipline)
    - ~/.claude/skills/zero-day-hunter/SKILL.md (skill definition)
  </read_first>
  <action>
    **PARALLEL DISPATCH BLOCK** — Tasks 3 and 4 are dispatched together in a single message containing TWO Task tool calls per D-302-INVOKE-01 PARALLEL_SUBAGENT pattern. Both sub-agents read `302-ADVERSARIAL-CHARGE.md` verbatim as their prompt and write their captured output to their respective per-skill MD files.

    Invoke `/zero-day-hunter` via the Task tool with `subagent_type=general-purpose` (or the skill-specific subagent type if registered). Pass the body of `302-ADVERSARIAL-CHARGE.md` verbatim as the skill prompt — do NOT pre-summarize. The sub-agent reads the full charge fresh and applies the zero-day-hunter persona (novel attack surface hunter for Degenerus Protocol; thinks like a C4A warden hunting one weird edge case; focuses on creative, unconventional, composition-based attack surfaces).

    Sub-agent task brief: "You are `/zero-day-hunter` running the v43.0 Phase 302 adversarial sweep. Read the CHARGE document at `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md` AND read every referenced load-bearing artifact (RNGLOCK-CATALOG.md, RNGLOCK-FIXREC.md, ADMIN-AUDIT.md, test/fuzz/RngLockDeterminism.t.sol, contracts/modules/*.sol). Return your full disposition per the CHARGE's required output format: 9 H2 hypothesis sections + any beyond-charge entries, each with Disposition + Evidence + Notes (+ Description + Severity + Suggested remediation if FINDING_CANDIDATE). Hard constraints: NO contract code in output; NO post-v43 milestone forward-cite tokens; apply `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` + `feedback_rng_window_storage_read_freshness.md` + `feedback_verify_call_graph_against_source.md` discipline. Write your output to `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md` with YAML frontmatter (skill: zero-day-hunter, phase: 302-cross-surface-adversarial-sweep-sweep, plan: 01, generated_at: <date>, charge_hypothesis_count: 9 charged + N beyond-charge)."

    SWP-02 charge focus per the CHARGE: composition attacks, cross-module read/write races, ERC-callback-induced state mutations, multi-block window exploits.
  </action>
  <verify>
    <automated>test -f .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md &amp;&amp; grep -c "## Hypothesis\\|Disposition\\|Evidence\\|Notes" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md | awk '$1 &gt;= 20 {exit 0} {exit 1}'</automated>
  </verify>
  <done>302-ADVERSARIAL-ZERO-DAY-HUNTER.md exists with 9 H2 hypothesis sections (+ any beyond-charge); SWP-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Dispatch /economic-analyst PARALLEL_SUBAGENT (SWP-03)</name>
  <files>.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md (passed verbatim to the skill)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-INVOKE-01 invocation discipline)
    - ~/.claude/skills/economic-analyst/SKILL.md (skill definition)
  </read_first>
  <action>
    **PARALLEL DISPATCH (paired with Task 3)** — Dispatch this Task tool call in the SAME single message as Task 3 per D-302-INVOKE-01 PARALLEL_SUBAGENT pattern. Both sub-agents fire concurrently.

    Invoke `/economic-analyst` via the Task tool with `subagent_type=general-purpose` (or the skill-specific subagent type if registered). Pass the body of `302-ADVERSARIAL-CHARGE.md` verbatim as the skill prompt — do NOT pre-summarize. The sub-agent reads the full charge fresh and applies the economic-analyst persona (game theory and mechanism design specialist; analyzes economic incentives; identifies misaligned actor incentives; models rational behavior; hunts points where actors might work against the system).

    Sub-agent task brief: "You are `/economic-analyst` running the v43.0 Phase 302 adversarial sweep. Read the CHARGE document at `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md` AND read every referenced load-bearing artifact (RNGLOCK-CATALOG.md, RNGLOCK-FIXREC.md, ADMIN-AUDIT.md, test/fuzz/RngLockDeterminism.t.sol, contracts/modules/*.sol). Return your full disposition per the CHARGE's required output format: 9 H2 hypothesis sections + any beyond-charge entries, each with Disposition + Evidence + Notes (+ Description + Severity + Suggested remediation if FINDING_CANDIDATE). Hard constraints: NO contract code in output; NO post-v43 milestone forward-cite tokens; apply the 3-condition catastrophe lens per `feedback_skeptic_pass_before_catastrophe.md` (slot feeds VRF-derived output AND is mutable mid-rngLock by non-EXEMPT actor AND mutation profits mutator after opportunity cost). For each player class (degenerette bettor, lootbox holder, mint queuer, sStonk staker, decimator claimant, deity pass holder, charity admin, governance admin), walk incentive-compatible adversarial actions available during the rngLock window. Write your output to `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md` with YAML frontmatter (skill: economic-analyst, phase: 302-cross-surface-adversarial-sweep-sweep, plan: 01, generated_at: <date>, charge_hypothesis_count: 9 charged + N beyond-charge)."

    SWP-03 charge focus per the CHARGE: game-theoretic write-induced effects (incentive-compatible adversarial actions during the rngLock window).
  </action>
  <verify>
    <automated>test -f .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md &amp;&amp; grep -c "## Hypothesis\\|Disposition\\|Evidence\\|Notes" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md | awk '$1 &gt;= 20 {exit 0} {exit 1}'</automated>
  </verify>
  <done>302-ADVERSARIAL-ECONOMIC-ANALYST.md exists with 9 H2 hypothesis sections (+ any beyond-charge); SWP-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 5: Integrate dispositions + apply two-tier consensus rule + skeptic-reviewer filter + write 302-01-ADVERSARIAL-LOG.md (SWP-04)</name>
  <files>.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md (charge document — anchors hypothesis numbering)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md (Task 2 output)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md (Task 3 output)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md (Task 4 output)
    - .planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-01-ADVERSARIAL-LOG.md (verbatim shape template)
    - ~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_skeptic_pass_before_catastrophe.md (skeptic-reviewer filter — load-bearing per the carry directive)
  </read_first>
  <action>
    Author `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md` mirroring the v42 P296 LOG shape verbatim, substituting v43 artifact anchors.

    **Sections (in order):**

    1. **YAML frontmatter.** `artifact: ADVERSARIAL-LOG`, `phase: 302-cross-surface-adversarial-sweep-sweep`, `plan: 01`, `milestone: v43.0`, `adversarial_pass_skills: [contract-auditor, zero-day-hunter, economic-analyst]`, `adversarial_pass_pattern: HYBRID — Task 2 SEQUENTIAL_MAIN_CONTEXT (/contract-auditor); Tasks 3+4 PARALLEL_SUBAGENT (/zero-day-hunter + /economic-analyst) per D-302-INVOKE-01 + D-43N-SWEEP-PREAUTH-01`, `out_of_scope_skills: [degen-skeptic]`, `audit_subject: rngLock freeze invariant + Phase 298-301 audit artifacts`, `audit_subject_surfaces: [RNGLOCK_FIXREC, ADMIN_AUDIT, FUZZ_HARNESS, CROSS_CONSUMER_ENTROPY]`, `charge_hypothesis_count: 9 charged + N beyond-charge`, `result: <ZERO_FINDING | TIER_1_RESOLVED | TIER_2_ELEVATION>`, `generated_at: <date>`.

    2. **Header prose.** 1-2 paragraph framing of the 3-skill adversarial pass against the v43.0 audit subject (rngLock freeze invariant). Cite the locked decisions: D-302-CHARGE-01 + D-302-CONSENSUS-01 + D-302-REPASS-SCOPE-01 + D-302-INVOKE-01 + D-302-AUDIT-ONLY-ROUTING-01 + D-271-ADVERSARIAL-02 (degen-skeptic OUT OF SCOPE) + D-43N-SWEEP-PREAUTH-01 (pre-authorization). State the invocation pattern: Task 2 `/contract-auditor` ran SEQUENTIAL in main orchestrator context; Tasks 3+4 (`/zero-day-hunter` + `/economic-analyst`) ran PARALLEL via subagent dispatch (two Task tool calls in one message) per D-302-INVOKE-01.

    3. **Three skill H2 sections (one per skill).** Format mirroring v42 P296 LOG verbatim:
       - `## /contract-auditor` — Report link to 302-ADVERSARIAL-CONTRACT-AUDITOR.md + Persona description + Disposition table (9 charged hypotheses + beyond-charge if any: columns Hyp / Disposition / Severity-if-FINDING_CANDIDATE) + Cross-cutting note (1-paragraph summary of the skill's key observation, quoted from its report).
       - `## /zero-day-hunter` — Same shape; report link to 302-ADVERSARIAL-ZERO-DAY-HUNTER.md.
       - `## /economic-analyst` — Same shape; report link to 302-ADVERSARIAL-ECONOMIC-ANALYST.md.

       For any FINDING_CANDIDATE rows, include the report's evidence excerpt (1-3 sentences) + Description + Severity + Suggested remediation cite (linking back to the per-skill MD).

    4. **Disposition section.** This is the load-bearing integration step.

       **Step (a) — Per-hypothesis aggregation table for the 9 CHARGED hypotheses.** Columns: Hyp / /contract-auditor disposition / /zero-day-hunter disposition / /economic-analyst disposition / count_findings (count of FINDING_CANDIDATE across the 3 cells) / Tier (CLEAR / TIER_1 / TIER_2). Apply the consensus rule per D-302-CONSENSUS-01:
       ```
       for each of 9 charged hypothesis rows:
         row = [ca_disp, zdh_disp, ea_disp]
         count_findings = sum(d == "FINDING_CANDIDATE" for d in row)
         if count_findings == 3:   Tier = TIER_2 (definitive elevation; queue RE-PASS automatically per D-302-REPASS-SCOPE-01)
         elif count_findings >= 1: Tier = TIER_1 (user-review checkpoint — invoke AskUserQuestion per Step c below; DO NOT auto-elevate)
         else:                     Tier = CLEAR
       ```

       **Step (b) — Per-hypothesis aggregation table for beyond-charge entries.** One row per beyond-charge hypothesis (the surfacing skill plus any other skill that addressed it; absent skills do NOT count toward consensus — they are noted as `not-targeted`). Apply same rule (Tier 2 only if 3-of-3; otherwise Tier 1 or CLEAR).

       **Step (c) — SKEPTIC FILTER PRE-USER-PRESENTATION (load-bearing per `feedback_skeptic_pass_before_catastrophe.md` carry).** Before presenting any FINDING_CANDIDATE to the user or auto-elevating, apply the skeptic-reviewer pass:
       - **Structural-protection sanity checks:** for each FINDING_CANDIDATE row, verify (1) prizePoolFrozen routing does NOT already block the writer; (2) `_queueTickets`/`_queueTicketRange` downstream-revert in DegenerusGameStorage.sol:572 does NOT already block; (3) existing in-source `rngLockedFlag` reverts (MintModule:877/:906/:1215, BurnieCoinflip:730, sStonk:492, WhaleModule:543, DegenerusGame:1513/:1528/:1575) do NOT already cover; (4) self-attesting state-machine (writer IS the consumer atomically) is NOT the actual structure.
       - **3-condition catastrophe lens:** for any CRITICAL/HIGH/MEDIUM-tagged FINDING_CANDIDATE, verify all three hold: (1) slot value feeds a VRF-derived output computation (not incidental accounting like `claimablePool` /`claimableWinnings`); (2) slot is mutable mid-rngLock by a non-EXEMPT actor; (3) mutation changes a VRF-derived output the mutator profits from after opportunity cost (forfeited stake, Sybil bypass cost, etc.).
       - **Per-finding skeptic verdict:** {REAL_EXPLOIT, FALSE_POSITIVE, ACCEPTABLE_DESIGN, STALE_CATALOG, NEEDS_VERIFY}. Only REAL_EXPLOIT findings get presented to the user as actionable Tier 1/2 escalations. FALSE_POSITIVE/ACCEPTABLE_DESIGN/STALE_CATALOG go in a `### Skeptic-Filtered Appendix` below the Disposition section with reasoning. NEEDS_VERIFY findings get a re-attestation step (re-read the contract source + re-attest before classification).

       Record the skeptic-filter results in a sub-section `### Skeptic-Reviewer Filter Results` with table columns: Hyp / Original FINDING_CANDIDATE source skill / Structural-protection check / 3-condition lens result / Skeptic verdict / Pre-presentation disposition.

       **Step (d) — Tier-1 user-review checkpoint (PAUSE FOR USER INPUT).** For every Tier-1 row that survives the skeptic filter as REAL_EXPLOIT or NEEDS_VERIFY, invoke `AskUserQuestion` BEFORE finalizing the LOG. The prompt MUST surface:
       - the hypothesis ID + statement
       - the flagging skill's disposition + evidence chain + severity
       - the other 2 skills' dispositions on the same hypothesis
       - the skeptic-filter verdict
       - 3 user-disposition options: (a) `ELEVATE_TO_FIXREC_AUGMENT` (route per D-302-AUDIT-ONLY-ROUTING-01 — Task 6 fires); (b) `ACCEPT_AS_DOCUMENTED` (record the user disposition in the LOG; no Task 6 fire); (c) `KEEP_AS_FINDING_CANDIDATE_PENDING_REPASS` (queue for re-attestation; no Task 6 fire this cycle).
       - The orchestrator MUST PAUSE for user input. Do NOT auto-elevate Tier-1 rows.

       For Tier-2 rows (3-of-3 consensus that survive skeptic filter as REAL_EXPLOIT), elevation is automatic per D-302-CONSENSUS-01 — no AskUserQuestion required at integration; Task 6 fires per D-302-AUDIT-ONLY-ROUTING-01. The user-review checkpoint for Tier-2 is implicit at the FIXREC-augment AGENT-COMMIT diff presentation (Task 6/7).

       **Step (e) — Net Assessment.** 1-2 paragraphs summarizing: total hypotheses charged (9 + N beyond-charge); count CLEAR; count TIER_1 (and resolution: ELEVATED / ACCEPT_AS_DOCUMENTED / PENDING_REPASS); count TIER_2 elevated; consensus-rule attestation line ("This pass applied the two-tier consensus rule per D-302-CONSENSUS-01: any-skill flag = user-review checkpoint; 3-of-3 consensus = definitive elevation + automatic RE-PASS. Skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md` applied pre-user-presentation to gate inflated CATASTROPHE/HIGH claims."); v43 closure status; SWP-03 + AUDIT-06 forward-handoff to Phase 303 §4 (adversarial-pass disposition table).

    5. **Footer.** Phase / plan / result / decision anchors (D-302-CHARGE-01 + D-302-CONSENSUS-01 + D-302-REPASS-SCOPE-01 + D-302-INVOKE-01 + D-302-AUDIT-ONLY-ROUTING-01 + D-271-ADVERSARIAL-02 + D-43N-SWEEP-PREAUTH-01 + `feedback_skeptic_pass_before_catastrophe.md`).

    **Crucial discipline reminder:** The skeptic-reviewer filter is NOT optional. Per the carry directive in `<deep_work_rules>` + per `feedback_skeptic_pass_before_catastrophe.md`, Task 5 MUST apply the structural-protection check + 3-condition EV lens BEFORE classifying anything as CATASTROPHE/HIGH and BEFORE presenting any Tier-1 candidate to the user. Two prior v43.0 phases (298 CATALOG + 299 FIXREC) shipped with 7+ inflated CATASTROPHE claims that the user had to debunk one-by-one — Phase 302 must not repeat this.
  </action>
  <verify>
    <automated>test -f .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md &amp;&amp; grep -c "## /contract-auditor\\|## /zero-day-hunter\\|## /economic-analyst\\|## Disposition\\|D-302-CONSENSUS-01\\|Skeptic-Reviewer\\|D-302-AUDIT-ONLY-ROUTING-01\\|count_findings\\|two-tier" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md | awk '$1 &gt;= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <done>302-01-ADVERSARIAL-LOG.md exists with 3 H2 skill sections + Disposition section applying D-302-CONSENSUS-01 two-tier rule + Skeptic-Reviewer Filter Results sub-section + Net Assessment; any Tier-1 row routed via AskUserQuestion pause-for-user discipline (NOT auto-elevated); any Tier-2 row queued for Task 6 elevation; SWP-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 6 (CONDITIONAL): Elevation routing to FIXREC-augment per D-302-AUDIT-ONLY-ROUTING-01</name>
  <files>.planning/RNGLOCK-FIXREC.md (append augment §N+1 entries) OR .planning/RNGLOCK-FIXREC-AUGMENT.md (new file)</files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md (Task 5 Disposition section — identifies Tier-2 rows and any Tier-1 rows where user chose ELEVATE_TO_FIXREC_AUGMENT)
    - .planning/RNGLOCK-FIXREC.md (existing structure — augment must match existing per-VIOLATION section shape §1..§37)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-AUDIT-ONLY-ROUTING-01 routing rule)
  </read_first>
  <action>
    **TASK 6 IS CONDITIONAL — only fires if Task 5's Disposition section contains EITHER:**
    - At least one Tier-2 row (3-of-3 consensus FINDING_CANDIDATE that survived the skeptic filter as REAL_EXPLOIT — automatic elevation per D-302-CONSENSUS-01), OR
    - At least one Tier-1 row where the user explicitly chose `ELEVATE_TO_FIXREC_AUGMENT` at the Task 5 AskUserQuestion checkpoint.

    **If neither condition holds, SKIP THIS TASK ENTIRELY — proceed directly to Task 7 commit.** Document in the LOG's Net Assessment that "Task 6 elevation routing did not fire (no Tier-2 elevation; no Tier-1 user-approved elevation)".

    **If the conditional fires:**

    Per D-302-AUDIT-ONLY-ROUTING-01: route each elevated FINDING_CANDIDATE to an appended FIXREC entry, NOT a Phase 303a/etc FIX wave. For each elevated finding:

    1. **Author the FIXREC entry.** Choose insertion mode based on count: if 1 elevation, append a new section `## §38 — V-<new-id> (<slot> via <writer> at <file:line>)` to `.planning/RNGLOCK-FIXREC.md` mirroring the existing per-VIOLATION section shape (§1..§37). If multiple elevations, create `.planning/RNGLOCK-FIXREC-AUGMENT.md` with §1..§N augment entries. Decision rule: 1 elevation → append-in-place to RNGLOCK-FIXREC.md (matches existing shape; preserves single-file FIXREC convention). 2+ elevations → separate AUGMENT.md (avoids interleaving Phase 302-discovered VIOLATIONs into Phase 299 ordering).

    2. **Per-entry content.** Each FIXREC entry MUST include all 5 standard FIXREC sub-sections per FIXREC-01..05: (FIXREC-01) recommended tactic from menu (a) rngLockedFlag-gated revert / (b) snapshot-anchor / (c) re-order to pre-lock / (d) immutable; (FIXREC-02) design-intent backward-trace per `feedback_design_intent_before_deletion.md` — cite the phase that introduced the slot/writer; (FIXREC-03) actor game-theory walk — who exploits this, how, EV magnitude (LOW/MEDIUM/HIGH/CATASTROPHE-tier per the post-skeptic-filter classification), economic-likelihood disposition; (FIXREC-04) impact estimate for the recommended tactic — bytecode delta direction, storage layout impact (byte-identical or new slot), public ABI impact, BREAKING-vs-NON-BREAKING per D-40N-EVT-BREAK-01 + D-42N-EVT-BREAK-01 precedent; (FIXREC-05) v44.0 FIX-MILESTONE handoff anchor — assign locked-decision ID `D-43N-V44-HANDOFF-<NN+1>` (sequential after the highest existing HANDOFF anchor; query existing range via `grep -oE "D-43N-V44-HANDOFF-[0-9]+" .planning/RNGLOCK-FIXREC.md | sort -u | tail -5`) + file:line cite into RNGLOCK-FIXREC.md + cross-reference to RNGLOCK-CATALOG.md verdict-matrix row.

    3. **FUZZ harness handoff (USER-PING ONLY — DO NOT auto-modify the test).** If the elevated VIOLATION class is not covered by any existing fuzz function in `test/fuzz/RngLockDeterminism.t.sol`, surface this via AskUserQuestion with the prompt: "Elevated FINDING_CANDIDATE V-<new-id> at <slot/writer/callsite> is not covered by any existing fuzz function. Per D-302-AUDIT-ONLY-ROUTING-01, FUZZ harness extension requires user approval (Phase 301 test was USER-APPROVED per `feedback_no_contract_commits.md`). Options: (a) draft a new fuzz function + new `vm.skip` block for user review at commit time; (b) defer fuzz coverage to v44.0 FIX-MILESTONE; (c) note as 'no fuzz coverage required' if structurally redundant with existing function coverage." The user picks; the orchestrator either drafts the fuzz extension (if option a) OR leaves it as a v44.0 handoff in the FIXREC-05 anchor section (option b/c). DO NOT proactively modify `test/fuzz/RngLockDeterminism.t.sol` without explicit user approval.

    4. **Update the LOG.** Append a `## RE-PASS QUEUED` section to `302-01-ADVERSARIAL-LOG.md` per D-302-REPASS-SCOPE-01 — "Tier-2 elevation on hypothesis (X) triggers RE-PASS against the FIXREC-augment §<N+1> diff + the affected hypothesis subset only. Other hypotheses stay at original-pass disposition." Document the affected hypothesis subset explicitly. RE-PASS execution is OUT OF SCOPE FOR THIS PLAN — it would be a follow-on plan if a FIXREC-augment-driven content change materializes that requires re-attestation. Per D-302-AUDIT-ONLY-ROUTING-01, the FIXREC-augment IS the "fix" for re-pass purposes (audit-only posture; no contract change); if the augment text alone is the diff, RE-PASS can either be skipped (if all 3 skills already addressed the augment-introduced surface in their original-pass beyond-charge entries) or queued as a separate plan. Decision recorded in this Net Assessment update.

    5. **Update SWP-04 traceability.** The LOG's Net Assessment must explicitly state the elevation routing applied per D-302-AUDIT-ONLY-ROUTING-01: "FINDING_CANDIDATE elevation routed to FIXREC-augment (RNGLOCK-FIXREC.md §<N+1> / RNGLOCK-FIXREC-AUGMENT.md); v44.0 handoff anchor `D-43N-V44-HANDOFF-<NN+1>` assigned. No contract change at v43 per audit-only posture (`D-43N-AUDIT-ONLY-01`). Phase 303 §4 forward-handoff includes the elevated finding F-43-NN block."
  </action>
  <verify>
    <automated>if grep -q "TIER_2\\|ELEVATE_TO_FIXREC_AUGMENT" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md 2&gt;/dev/null; then grep -q "D-43N-V44-HANDOFF-" .planning/RNGLOCK-FIXREC.md .planning/RNGLOCK-FIXREC-AUGMENT.md 2&gt;/dev/null &amp;&amp; grep -q "RE-PASS QUEUED\\|elevation routing did not fire" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md; else grep -q "elevation routing did not fire" .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md; fi</automated>
  </verify>
  <done>EITHER Task 6 fired and produced a FIXREC-augment entry with HANDOFF anchor + LOG RE-PASS-QUEUED section + (if needed) FUZZ-harness AskUserQuestion captured, OR Task 6 was correctly skipped with explicit Net Assessment statement "elevation routing did not fire (no Tier-2 elevation; no Tier-1 user-approved elevation)".</done>
</task>

<task type="auto">
  <name>Task 7: AGENT-COMMIT artifact bundle + STATE.md update (SWP-05 traceability + AUDIT-only attestation)</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md (Task 5 output — Net Assessment determines commit message contents)
    - .planning/STATE.md (current state shape)
    - .planning/ROADMAP.md (Phase 302 entry — needs Plans: list update)
    - .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-CONTEXT.md (D-302-ARTIFACT-SET-01 bundle contents)
    - ~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md (AGENT-COMMITTED bundle discipline)
    - ~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (zero contracts/ + test/ mutations enforced)
  </read_first>
  <action>
    **Step 1 — Update ROADMAP.md Phase 302 entry.** Append a `**Plans:**` line + `Plans:` list mirroring the format used by Phases 298-301 (see ROADMAP.md lines 73 / 97 / 118 / 129):

    ```
    **Plans:** 1 plan (single Wave-1 main-context end-to-end AGENT-COMMITTED 3-skill HYBRID adversarial-pass artifact bundle per D-302-ARTIFACT-SET-01 + D-302-TASK-SPLIT-01 default; Task 6 elevation routing conditional on Tier-2 or user-approved Tier-1 elevation)

    Plans:
    - [x] 302-01-PLAN.md — Wave 1: Author CHARGE document (SWP-01..05 verbatim + augments (i)..(iv)) + dispatch /contract-auditor SEQUENTIAL_MAIN_CONTEXT + dispatch /zero-day-hunter + /economic-analyst PARALLEL_SUBAGENT + integrate dispositions via two-tier consensus rule + skeptic-reviewer filter pre-user-presentation + (conditional) elevation routing to FIXREC-augment per D-302-AUDIT-ONLY-ROUTING-01 + AGENT-COMMITTED artifact bundle
    ```

    **Step 2 — Update STATE.md.** Update the YAML frontmatter (`last_updated`, `last_activity`, `progress.completed_phases` 4→5, `progress.percent` 67→83). Update the "Current Position" + "Current focus" sections to reflect Phase 302 completion + ready-to-plan-Phase-303. Add a Phase 302 completion bullet to whatever phase-tracking section STATE.md uses (mirror Phase 301 completion bullet shape).

    **Step 3 — Verify zero contracts/ + test/ mutations.** Run `git status --porcelain contracts/ test/` and confirm zero modifications. Per `D-43N-AUDIT-ONLY-01` + `feedback_no_contract_commits.md` + the audit-only posture, the entire Phase 302 footprint MUST be planner-private artifacts + ROADMAP/STATE updates + (conditionally) FIXREC-augment entries.

    **Step 4 — AGENT-COMMIT the artifact bundle.** Per `feedback_batch_contract_approval.md` artifact-bundling discipline + D-302-ARTIFACT-SET-01, bundle into ONE commit:
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-PLAN.md` (this plan)
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CHARGE.md`
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-CONTRACT-AUDITOR.md`
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ZERO-DAY-HUNTER.md`
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-ADVERSARIAL-ECONOMIC-ANALYST.md`
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md`
    - `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-SUMMARY.md` (author per workflow/templates/summary.md before commit)
    - `.planning/STATE.md` (Step 2 updates)
    - `.planning/ROADMAP.md` (Step 1 updates)
    - (conditional) `.planning/RNGLOCK-FIXREC.md` or `.planning/RNGLOCK-FIXREC-AUGMENT.md` if Task 6 fired

    Commit message:
    - **Default (ZERO_FINDING or Tier-1 ACCEPT_AS_DOCUMENTED):** `docs(302): cross-surface adversarial sweep [SWP-01..05]`
    - **Tier-2 elevation OR Tier-1 ELEVATE_TO_FIXREC_AUGMENT:** `docs(302): cross-surface adversarial sweep + FIXREC-augment elevation [SWP-01..05, D-43N-V44-HANDOFF-<NN+1>]`

    Include in the commit message body:
    - Result summary (count CLEAR / TIER_1_RESOLVED / TIER_2_ELEVATED)
    - Skeptic-reviewer filter applied per `feedback_skeptic_pass_before_catastrophe.md`
    - Locked-decision anchors invoked
    - Zero contracts/ + zero test/ mutations attestation (`git diff <commit-sha>^ <commit-sha> -- contracts/ test/` returns no output)

    DO NOT push to remote. Per `feedback_manual_review_before_push.md`, the user reviews the commit + decides push timing.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain contracts/ test/)" &amp;&amp; git log -1 --format=%s | grep -qE "docs\\(302\\): cross-surface adversarial sweep" &amp;&amp; [ -z "$(git diff HEAD~1 HEAD --stat -- contracts/ test/)" ] &amp;&amp; [ -z "$(git diff HEAD~1 HEAD --name-only -- .planning/KNOWN-ISSUES.md)" ]</automated>
  </verify>
  <done>STATE.md + ROADMAP.md updated; SUMMARY.md authored; 5-artifact bundle (+ optional FIXREC-augment) committed in ONE AGENT-COMMITTED commit; zero contracts/ + zero test/ mutations verified via git diff; KNOWN-ISSUES.md UNMODIFIED per D-302-KI-01; commit message anchors SWP-01..05 + (conditional) HANDOFF anchor; ready for Phase 303 plan-phase.</done>
</task>

</tasks>

<verification>

**Phase-level verification (orchestrator runs at phase close):**

1. **All 5 mandatory artifacts exist:**
   ```bash
   for f in 302-ADVERSARIAL-CHARGE.md 302-ADVERSARIAL-CONTRACT-AUDITOR.md \
            302-ADVERSARIAL-ZERO-DAY-HUNTER.md 302-ADVERSARIAL-ECONOMIC-ANALYST.md \
            302-01-ADVERSARIAL-LOG.md; do
     test -f ".planning/phases/302-cross-surface-adversarial-sweep-sweep/$f" || \
       echo "MISSING: $f"
   done
   ```

2. **Zero contracts/ + zero test/ mutations:**
   ```bash
   git diff <baseline-sha> HEAD --stat -- contracts/ test/
   # Expected: empty output (no files changed)
   ```

3. **D-302-INVOKE-01 HYBRID pattern attested in LOG:**
   ```bash
   grep -q "HYBRID.*SEQUENTIAL_MAIN_CONTEXT.*PARALLEL_SUBAGENT" \
     .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md
   ```

4. **D-302-CONSENSUS-01 two-tier rule applied:**
   ```bash
   grep -q "two-tier consensus rule per D-302-CONSENSUS-01" \
     .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md
   ```

5. **Skeptic-reviewer filter applied per `feedback_skeptic_pass_before_catastrophe.md`:**
   ```bash
   grep -q "Skeptic-Reviewer Filter\\|skeptic-reviewer filter\\|feedback_skeptic_pass_before_catastrophe" \
     .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md
   ```

6. **Conditional FIXREC-augment shape verified if Task 6 fired:**
   ```bash
   # If Task 6 fired, FIXREC-augment must have D-43N-V44-HANDOFF-NN anchor
   if grep -q "TIER_2\\|ELEVATE_TO_FIXREC_AUGMENT" \
        .planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-ADVERSARIAL-LOG.md; then
     grep -qE "D-43N-V44-HANDOFF-[0-9]+" .planning/RNGLOCK-FIXREC.md \
       .planning/RNGLOCK-FIXREC-AUGMENT.md 2>/dev/null
   fi
   ```

7. **AGENT-COMMITTED bundle in 1 commit:**
   ```bash
   git log -1 --format=%s | grep -qE "^docs\\(302\\): cross-surface adversarial sweep"
   git diff HEAD~1 HEAD --name-only | grep -c "302-ADVERSARIAL\\|302-01-ADVERSARIAL-LOG\\|302-01-PLAN\\|302-01-SUMMARY" \
     | awk '$1 >= 6 {exit 0} {exit 1}'  # All 5 artifacts + PLAN + SUMMARY in 1 commit
   ```

8. **STATE.md + ROADMAP.md updated:**
   ```bash
   grep -q "302-01-PLAN.md" .planning/ROADMAP.md
   grep -q "Phase 302\\|completed_phases: 5" .planning/STATE.md
   ```

</verification>

<success_criteria>

- [ ] **SWP-01 satisfied:** `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT pass complete; 302-ADVERSARIAL-CONTRACT-AUDITOR.md captures full disposition table + per-hypothesis evidence + (any) beyond-charge entries.
- [ ] **SWP-02 satisfied:** `/zero-day-hunter` PARALLEL_SUBAGENT pass complete; 302-ADVERSARIAL-ZERO-DAY-HUNTER.md captures full disposition + beyond-charge.
- [ ] **SWP-03 satisfied:** `/economic-analyst` PARALLEL_SUBAGENT pass complete; 302-ADVERSARIAL-ECONOMIC-ANALYST.md captures full disposition + beyond-charge.
- [ ] **SWP-04 satisfied:** Integrated 302-01-ADVERSARIAL-LOG.md applies D-302-CONSENSUS-01 two-tier rule + skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md`; any Tier-1 row routed via AskUserQuestion (PAUSE for user input) per `feedback_never_preapprove_contracts.md`; any Tier-2 row queued for D-302-AUDIT-ONLY-ROUTING-01 FIXREC-augment elevation; any FINDING_CANDIDATE for a participating slot REJECTED-as-SAFE_BY_DESIGN per milestone-goal exclusion (re-classified as SAFE_BY_STRUCTURAL_CLOSURE or FINDING_CANDIDATE).
- [ ] **SWP-05 satisfied:** `/degen-skeptic` OUT OF SCOPE attestation in LOG header + footer (D-271-ADVERSARIAL-02 carry); `/economic-analyst` IN SCOPE (D-271-ADVERSARIAL-03); pre-authorization documented (D-43N-SWEEP-PREAUTH-01 — no user-ping for invocation; Tier-1 ping discipline preserved).
- [ ] **Skeptic-reviewer filter applied pre-user-presentation:** `### Skeptic-Reviewer Filter Results` sub-section in LOG Disposition; structural-protection check + 3-condition catastrophe lens applied to every FINDING_CANDIDATE; FALSE_POSITIVE / ACCEPTABLE_DESIGN / STALE_CATALOG / NEEDS_VERIFY findings routed to appendix or re-attestation, NOT presented to user as actionable.
- [ ] **Zero contracts/ + zero test/ mutations:** `git diff <baseline-sha> HEAD --stat -- contracts/ test/` returns empty per `D-43N-AUDIT-ONLY-01` + `feedback_no_contract_commits.md`.
- [ ] **AGENT-COMMITTED single-bundle commit** per `feedback_batch_contract_approval.md` + D-302-ARTIFACT-SET-01: CHARGE + 3 per-skill MDs + integrated LOG + PLAN + SUMMARY + STATE/ROADMAP updates + (conditional) FIXREC-augment all in 1 commit.
- [ ] **Conditional FIXREC-augment** (Task 6) authored ONLY if Tier-2 consensus OR user-approved Tier-1 elevation per D-302-AUDIT-ONLY-ROUTING-01; routes to RNGLOCK-FIXREC.md (single augment) or RNGLOCK-FIXREC-AUGMENT.md (multi); HANDOFF anchor `D-43N-V44-HANDOFF-NN+1` assigned; FUZZ-harness extension request surfaced via AskUserQuestion if needed (DO NOT auto-modify test/fuzz/).
- [ ] **STATE.md + ROADMAP.md updated:** Phase 302 marked complete; ready-to-plan Phase 303.
- [ ] **KNOWN-ISSUES.md UNMODIFIED** per D-302-KI-01.

</success_criteria>

<output>
Create `.planning/phases/302-cross-surface-adversarial-sweep-sweep/302-01-SUMMARY.md` capturing:
- Result classification (ZERO_FINDING / TIER_1_RESOLVED / TIER_2_ELEVATION)
- Per-skill disposition counts (9 charged hypotheses × 3 skills + N beyond-charge)
- Skeptic-reviewer filter results (count REAL_EXPLOIT / FALSE_POSITIVE / ACCEPTABLE_DESIGN / STALE_CATALOG / NEEDS_VERIFY)
- Any user-disposition captured at Tier-1 AskUserQuestion checkpoints (verbatim user response + timestamp)
- (Conditional) FIXREC-augment HANDOFF anchor assigned + affected slot/writer
- Forward-handoff to Phase 303 §4 (adversarial-pass disposition table) + §5 (sweep methodology attestation)
- Commit SHA + diff statistics (planner artifacts: N files / M lines; contracts/: 0 / 0; test/: 0 / 0)
</output>
