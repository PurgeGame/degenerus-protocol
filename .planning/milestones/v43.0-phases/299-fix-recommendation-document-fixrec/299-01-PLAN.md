---
phase: 299-fix-recommendation-document-fixrec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-A FIXREC contribution file exists at .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md"
    - "Every V-NNN VIOLATION row in scope (V-003, V-004, V-005, V-009, V-010, V-011, V-012, V-013) has a §N analytical entry"
    - "Every §N entry contains all 4 sub-sections (§N.A design-intent + §N.B actor walk + §N.C tactic+rationale+impact + §N.D handoff anchor)"
    - "Every §N.D handoff anchor cites D-43N-V44-HANDOFF-NN matching the catalog row's anchor (H-01..H-08)"
    - "Zero SAFE_BY_DESIGN tokens appear anywhere in the output"
    - "Zero contracts/ mutations and zero test/ mutations occurred during plan execution"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster A (dailyHeroWagers + autoRebuyState slot family)"
      contains: "§N.A"
  key_links:
    - from: "299-01-FIXREC-cluster.md §N.D handoff anchor"
      to: ".planning/RNGLOCK-CATALOG.md §16 verdict-matrix row"
      via: "D-43N-V44-HANDOFF-NN ID cross-reference"
      pattern: "D-43N-V44-HANDOFF-(0[1-8])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster A** — `dailyHeroWagers[day][q]` (S-02) + `autoRebuyState[beneficiary]` (S-05) slot family. 8 logical VIOLATIONs in scope: V-003, V-004, V-005 (dailyHeroWagers EOA writers via `_placeDegeneretteBetCore`) + V-009, V-010, V-011, V-012, V-013 (autoRebuyState EOA + coin/coinflip callback writers).

Purpose: Per `D-299-FIXREC-LAYOUT-01` + `D-299-EXEC-SHAPE-01`, this plan is one of 10 parallel cluster contributions that feed the Wave-2 aggregator (299-11-PLAN.md). Per-VIOLATION depth (4 sub-sections per V-NNN entry) is the load-bearing differentiator vs Phase 298's 1-line rationale. Cluster theme: cross-day hero-override day-index manipulation + auto-rebuy/AfKing state mutation during rngLock window.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md` (one file, multiple §N entries). Zero `contracts/` and zero `test/` mutations per `D-43N-AUDIT-ONLY-01` posture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-A FIXREC contribution (dailyHeroWagers + autoRebuyState)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §0 (executive summary; metrics)
    - .planning/RNGLOCK-CATALOG.md §14 (rows S-02 dailyHeroWagers, S-05 autoRebuyState)
    - .planning/RNGLOCK-CATALOG.md §15 (writer rows for S-02 + S-05; file:line citations)
    - .planning/RNGLOCK-CATALOG.md §16 rows V-003, V-004, V-005, V-009, V-010, V-011, V-012, V-013
    - .planning/RNGLOCK-CATALOG.md §3 (JackpotModule.runTerminalJackpot consumer; dailyHeroWagers/dailyIdx consumer context)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md (all D-299-* locked decisions)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (around line 367, 499 — _placeDegeneretteBetCore writes)
    - contracts/DegenerusGame.sol (lines 714 parent dispatch; 1495, 1504, 1512, 1524, 1559, 1569, 1641, 1654, 1670 — autoRebuyState writers; setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode/deactivateAfKingFromCoin/syncAfKingLazyPassFromCoin)
    - contracts/DegenerusVault.sol around line 607 (vault-routed bet path)
    - .planning/milestones/v41.0-phases/288-*/288-01-DESIGN-INTENT-TRACE.md or 288-CONTEXT.md (Phase 288 dailyIdx structural-snapshot precedent — load-bearing for §N.A and §N.C tactic-(b) recommendations)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_backward_trace.md
    - Project memory: feedback_rng_commitment_window.md
  </read_first>
  <action>
    Per `D-299-SUB-AGENT-PROMPT-01`, author the Cluster-A FIXREC contribution as a single markdown file. For each V-NNN row below, emit one `## §N — V-NNN ({slot} via {writer fn} at {callsite})` section containing exactly four sub-sections:

    **§N.A — Design-intent backward-trace** (FIXREC-02): For each slot family, cite the original phase that introduced the slot/writer with file:line. dailyHeroWagers + Phase 288 dailyIdx structural-snapshot precedent is load-bearing — grep `.planning/milestones/v41.0-phases/288-*/` for `288-01-DESIGN-INTENT-TRACE.md` (or equivalent) and quote the Phase 288 dailyIdx rationale verbatim where it applies. For autoRebuyState writers, trace the existing per-writer rngLockedFlag gates (DegenerusGame:1513, :1528, :1575) plus the missing-gate gap at :1641 (`deactivateAfKingFromCoin`) and :1654 (`syncAfKingLazyPassFromCoin`). Document why each slot exists (player-side auto-rebuy state vs hero-override day accumulator) and what behavior would break if naively gated (e.g. a blanket revert during the entire rngLocked window would break user-side toggle on the rebuy state — articulate the actor-friction boundary).

    **§N.B — Actor game-theory walk** (FIXREC-03): Per `feedback_design_intent_before_deletion.md` discipline + `feedback_rng_commitment_window.md` discipline. Enumerate exploit actor class (player / MEV bot / admin / coin-flip callback initiator), specific action sequence during rngLock window (e.g. "player observes pending VRF, fires placeDegeneretteBet at DegeneretteModule:367, mutates dailyHeroWagers[nextDay][q] before the next day's consumer reads it"), EV magnitude estimate (LOW / MEDIUM / HIGH / CATASTROPHE-tier with rationale — autoRebuyState is HIGH because AfKing mode is the per-jackpot-day finalist redirect input), and economic-likelihood disposition. V-012/V-013 are CRITICAL: coin and coinflip callbacks are EOA-triggerable via cheap BurnieCoin transfer / coinflip arming, so the callback path is effectively EOA-controllable.

    **§N.C — Recommended tactic + rationale + impact estimate** (FIXREC-01 + FIXREC-04): Catalog assigns tactic (b) snapshot/anchor to V-003..V-005 and tactic (a) gated-revert to V-009..V-013. Reproduce the catalog's tactic and write a full rationale (~3-5 sentences) plus bytecode/storage-layout/public-ABI impact estimate per `D-298-RECOMMEND-DEPTH-01` extended to per-VIOLATION depth. For tactic (b) on dailyHeroWagers: explicitly invoke Phase 288 dailyIdx snapshot pattern — propose a per-VRF-window dailyHeroWagers snapshot or a write-side dailyIdx-keyed indirection so that the consumer reads the snapshot taken at lock time. For tactic (a) on autoRebuyState: name which exact line gets the `if (rngLockedFlag) revert RngLocked();` guard (catalog already cites :1641 and :1654 as MISSING; V-009..V-011 are gated at runtime but lack coverage attestation). Cite the `RngLocked` custom error convention (MintModule:1221 / BurnieCoinflip:730 / sStonk:492). Bytecode impact: per-gate adds ~30-40 bytes for tactic (a); snapshot/anchor for tactic (b) adds ~100-200 bytes depending on snapshot scope. Storage-layout impact: tactic (a) = byte-identical; tactic (b) = new packed slot or new mapping field. Public ABI impact: tactic (a) = no event topic-hash change, only new `RngLocked` revert path emerges; tactic (b) = potentially a new internal indirection but no public-ABI break per `D-40N-EVT-BREAK-01` + `D-42N-EVT-BREAK-01` BREAKING/NON-BREAKING discipline.

    **§N.D — v44.0 handoff anchor** (FIXREC-05): Emit `D-43N-V44-HANDOFF-NN` exactly as catalog §16 specifies (V-003→H-01, V-004→H-02, V-005→H-03, V-009→H-04, V-010→H-05, V-011→H-06, V-012→H-07, V-013→H-08). Cite the catalog §16 row line: line, the writer fn file:line, and a 1-line summary line that v44.0 plan-phase consumes verbatim. Format: `**Anchor:** D-43N-V44-HANDOFF-NN — {1-line plan-phase consumable summary}. **Catalog row:** RNGLOCK-CATALOG.md:{line} (V-NNN). **Writer:** {file}:{line}.`

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN classification. Do NOT add history/changelog prose per `feedback_no_history_in_comments.md` (authorial discipline carry — describe what IS, not what changed). Do NOT invent design-intent citations — if a Phase NNN-CONTEXT.md citation cannot be located, write `[design-intent: pre-v25 baseline; no dedicated trace artifact]` rather than fabricate. Cite the project memory files verbatim where they apply (e.g. `per feedback_rng_commitment_window.md, every RNG audit must check what player-controllable state can change between VRF request and fulfillment`).

    File ordering: §1 (V-003) → §2 (V-004) → §3 (V-005) → §4 (V-009) → §5 (V-010) → §6 (V-011) → §7 (V-012) → §8 (V-013). Section numbering is local to this cluster file; the Wave-2 aggregator renumbers globally.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md \
  && for V in V-003 V-004 V-005 V-009 V-010 V-011 V-012 V-013; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-01 HANDOFF-02 HANDOFF-03 HANDOFF-04 HANDOFF-05 HANDOFF-06 HANDOFF-07 HANDOFF-08; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md)" -ge 8 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md)" -ge 8 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md)" -ge 8 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md)" -ge 8 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    .planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md exists; contains §N entries for all 8 in-scope VIOLATIONs (V-003, V-004, V-005, V-009, V-010, V-011, V-012, V-013); each entry has all 4 sub-sections (§N.A, §N.B, §N.C, §N.D); each §N.D cites the matching D-43N-V44-HANDOFF-NN anchor (H-01..H-08); zero SAFE_BY_DESIGN tokens; zero contracts/ + test/ source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- Cluster-A FIXREC contribution file exists at `.planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md`
- All 8 V-NNN entries present
- All 8 H-NN handoff anchors present
- 4 sub-sections per entry (§N.A, §N.B, §N.C, §N.D) — total ≥32 sub-section headers
- Zero `SAFE_BY_DESIGN` tokens
- `git status --porcelain contracts/ test/` returns empty (no source-tree mutations)
- Design-intent §N.A cites Phase 288 dailyIdx precedent for dailyHeroWagers VIOLATIONs
- Actor-walk §N.B classifies EV magnitude per VIOLATION
- Tactic §N.C matches catalog (b for V-003..V-005; a for V-009..V-013)
- Handoff §N.D anchor IDs match catalog §16 rows
</verification>

<success_criteria>
Single AGENT-COMMITTED file at `.planning/phases/299-fix-recommendation-document-fixrec/299-01-FIXREC-cluster.md` covering Cluster-A's 8 logical VIOLATIONs with 4 sub-sections each, zero SAFE_BY_DESIGN, zero source-tree mutations, all handoff anchors emitted matching catalog §16 H-01..H-08.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-01-SUMMARY.md` when done.
</output>
