---
phase: 299-fix-recommendation-document-fixrec
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-E FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-054, V-055, V-057, V-058, V-063, V-064, V-065) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-27..H-33)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster E (claimablePool game-over family)"
      contains: "§N.A"
  key_links:
    - from: "299-05-FIXREC-cluster.md §N.D"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN"
      pattern: "D-43N-V44-HANDOFF-(2[7-9]|3[0-3])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster E** — `claimablePool` (S-16) writer race during game-over drain window. 7 logical VIOLATIONs in scope: V-054 (DecimatorModule._awardDecimatorLootbox -= EOA claimDecimatorJackpot), V-055 (MintModule._resolveMintShortfall -= via mintBatch; _livenessTriggered runtime gate covers), V-057 (DegeneretteModule._creditCheckedFromClaimable -= EOA playDegenerette), V-058 (DegeneretteModule._resolveLootboxDirect += EOA branch), V-063 (DegenerusGame.claimWinnings -= EOA), V-064 (DegenerusGame.useClaimableForMint -=; _livenessTriggered gate covers), V-065 (DegenerusGame.sweepSdgnrsClaim -= via sDGNRS callback).

Purpose: Per §0 headline #4, claimablePool writer races during the game-over drain window are a structural-hardening cluster. Drain math is fragile to in-flight available/totalFunds mutations.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-E FIXREC contribution (claimablePool game-over family)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 row S-16
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-16 (10 distinct writers cataloged)
    - .planning/RNGLOCK-CATALOG.md §16 rows V-053..V-065 (focus on VIOLATION rows: V-054, V-055, V-057, V-058, V-063, V-064, V-065)
    - .planning/RNGLOCK-CATALOG.md §5 (GameOverModule rngWordByDay substitution consumer; handleGameOverDrain preRefundAvailable gate consumer)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/modules/DegenerusGameDecimatorModule.sol around :388 (_awardDecimatorLootbox -= write)
    - contracts/modules/DegenerusGameMintModule.sol around :949 (_resolveMintShortfall -=)
    - contracts/modules/DegenerusGameDegeneretteModule.sol around :547 (_creditCheckedFromClaimable -=); :1131 (_resolveLootboxDirect += EOA branch)
    - contracts/DegenerusGame.sol around :946 (useClaimableForMint -=); :1408 (claimWinnings -=); :1739 (sweepSdgnrsClaim -=)
    - contracts/modules/DegenerusGameGameOverModule.sol around :134, :171, :207 (handleGameOverDrain deity refund + decSpend + handleFinalSweep)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_commitment_window.md
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-054 → §2=V-055 → §3=V-057 → §4=V-058 → §5=V-063 → §6=V-064 → §7=V-065.

    **§N.A — Design-intent backward-trace**: For claimablePool, trace its dual role as (a) post-jackpot player-claim accumulator (EOA -= writes via claimWinnings/useClaimableForMint/sweepSdgnrsClaim) and (b) terminal-drain magnitude input (consumed by handleGameOverDrain's preRefundAvailable gate). Document the existing `_livenessTriggered()` runtime gate convention (V-055 + V-064 cite this gate as already covering the path) and why coverage attestation is still needed (FUZZ-301 branch coverage gap). For V-054, V-057, V-058, V-063, V-065, document the missing _livenessTriggered() gate at each writer site. The game-over window opens when `gameOver` flag transitions true; the rngLock window opens at every advanceGame VRF request; both windows are conditions that must prevent claimablePool mutation while the terminal magnitude is computed.

    **§N.B — Actor game-theory walk**: Drain-window writer races. For each writer, exploit-actor = EOA player triggering the entry function (claimDecimatorJackpot, mintBatch, playDegenerette, claimWinnings, useClaimableForMint, sweepSdgnrsClaim) at the precise block where handleGameOverDrain reads claimablePool for preRefundAvailable calculation. Action sequence: observe pending VRF (rngLock open) or pending game-over signal; fire claim/credit before drain computes; the drain math now under- or over-states the available pool. EV magnitude per writer: V-054 (decimator claim) = MEDIUM-HIGH (large per-claim magnitudes); V-055 (mint shortfall) = LOW (existing _livenessTriggered() gate but coverage gap); V-057 (degenerette credit) = MEDIUM (small per-bet, but degenerette is cheap-fire); V-058 (lootbox-direct + EOA branch) = HIGH (lootbox direct payouts can be large); V-063 (claimWinnings) = CATASTROPHE-tier per single claim during terminal jackpot drain (entire user balance withdrawn mid-drain inflates preRefundAvailable inflation); V-064 (useClaimableForMint) = LOW with existing gate; V-065 (sweepSdgnrsClaim via sDGNRS callback) = HIGH (sweep magnitudes follow sStonk redemption magnitudes; mirrors V-063 vector via callback path).

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics (all (a) gated-revert):
    - V-054: Gate `_awardDecimatorLootbox` callsite on `!_livenessTriggered()` to close window
    - V-055: Existing `_livenessTriggered()` revert covers — coverage verification only via FUZZ-301
    - V-057: Gate the EOA-reached `_creditCheckedFromClaimable` callsite on `!_livenessTriggered()`
    - V-058: Gate the EOA-reached `_resolveLootboxDirect` callsite on `!_livenessTriggered()` (the auto-resolve branch remains EXEMPT-VRFCALLBACK)
    - V-063: Gate `claimWinnings` on `!_livenessTriggered() || gameOver` so drain math is stable; single revert closes both claimablePool and balance writers per V-073 co-location note
    - V-064: Existing `_livenessTriggered()` gate covers — coverage verification only
    - V-065: Gate `sweepSdgnrsClaim` on `!_livenessTriggered() || gameOver` to mirror V-063

    Bytecode impact: ~30-50 bytes per gate site (V-055 + V-064 = 0 bytes; only verification). Storage-layout: byte-identical. Public ABI: NON-BREAKING. Cite existing `_livenessTriggered()` convention (specific helper introduced in pre-v25 baseline; document its semantic as `gameOver || (currentDay >= gameOverPossibleDay)` or similar — verify exact body during sub-agent execution).

    **§N.D — v44.0 handoff anchor**: V-054→H-27, V-055→H-28, V-057→H-29, V-058→H-30, V-063→H-31, V-064→H-32, V-065→H-33.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Cite project memory verbatim.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md \
  && for V in V-054 V-055 V-057 V-058 V-063 V-064 V-065; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-27 HANDOFF-28 HANDOFF-29 HANDOFF-30 HANDOFF-31 HANDOFF-32 HANDOFF-33; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md)" -ge 7 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-05-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-E file exists; all 7 V-NNN entries; all 7 H-NN anchors (H-27..H-33); 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 7 V-NNN entries (V-054, V-055, V-057, V-058, V-063, V-064, V-065)
- 7 handoff anchors (H-27..H-33)
- ≥28 sub-section markers
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-E file covering 7 claimablePool game-over family VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-05-SUMMARY.md` when done.
</output>
