---
phase: 299-fix-recommendation-document-fixrec
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-H FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-105, V-109, V-110, V-111, V-112, V-113, V-114, V-117, V-120, V-121, V-122, V-123, V-124, V-125, V-127) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-63..H-77)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster H (presale + mintPacked_ + boonPacked + lastPurchaseDay cross-resolution accumulator family)"
      contains: "§N.A"
  key_links:
    - from: "299-08-FIXREC-cluster.md §N.D"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN"
      pattern: "D-43N-V44-HANDOFF-(6[3-9]|7[0-7])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster H** — `presaleStatePacked` (S-30) + `mintPacked_[player]` (S-32) cross-EOA accumulator + `boonPacked[player]` (S-34) multi-callsite + `lastPurchaseDay` (S-35). 15 logical VIOLATIONs in scope: V-105 (presaleStatePacked _presaleCapCheck buyTickets), V-109 (mintPacked_ via _mintStreakWrite), V-110 (mintPacked_ via _allocateMintPacked 3 callsites), V-111 (mintPacked_ BoonModule.consumeActivityBoon — reorder tactic c), V-112 (mintPacked_ BoonModule._applyBoon whale-pass), V-113 (mintPacked_ WhaleModule multi-callsite), V-114 (mintPacked_ WhaleModule._buyDeityPass), V-117 (mintPacked_ _applyWhalePassStats — reorder tactic c), V-120 (boonPacked LootboxModule._applyBoon multi + issueDeityBoon), V-121 (boonPacked WhaleModule multi), V-122 (boonPacked MintModule._processMint), V-123 (boonPacked checkAndClearExpiredBoon — snapshot tactic b), V-124 (boonPacked consumeActivityBoon slot1 — reorder tactic c), V-125 (boonPacked BoonModule externals), V-127 (lastPurchaseDay MintModule purchase entry).

Purpose: §0 headline #2 sub-cluster — cross-EOA mintPacked_ touched by 6 EOA-reachable writers (Mint / Whale / Affiliate-cache / Boon / Streak / Deity-pass). Phase 281 owed-salt + Phase 288 dailyIdx snapshot precedents apply for tactic (b); reorder tactic (c) for boon-consumption side effects.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-H FIXREC contribution (presale + mintPacked_ + boonPacked + lastPurchaseDay)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-30, S-32, S-34, S-35
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-30 + S-32 + S-34 + S-35
    - .planning/RNGLOCK-CATALOG.md §16 rows V-105, V-109..V-114, V-117, V-120..V-125, V-127
    - .planning/RNGLOCK-CATALOG.md §7 (LootboxModule._resolveLootboxCommon manual-path consumer); §8 (DegeneretteModule._resolveLootboxDirect consumer)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/modules/DegenerusGameMintModule.sol around :240, :275, :369 (_allocateMintPacked), :1026 (_presaleCapCheck), :1433 (_processMint boon write)
    - contracts/modules/DegenerusGameMintStreakUtils.sol around :47 (_mintStreakWrite / _recordMintStreakForLevel)
    - contracts/modules/DegenerusGameBoonModule.sol around :265, :266 (checkAndClearExpiredBoon), :283, :291, :297, :301 (consumeActivityBoon slot1), :303 (_applyBoon whale-pass), :320 (consumeActivityBoon), :41, :67, :93, :122 (BoonModule other externals)
    - contracts/modules/DegenerusGameWhaleModule.sol around :202, :210, :303, :388, :419, :516, :548, :556, :589, :669, :898, :944 (_buyWhaleBundle* writers; _buyDeityPass)
    - contracts/modules/DegenerusGameLootboxModule.sol around :1432, :1452, :1479, :1503, :1526, :1547, :1568, :1603 (_applyBoon multi-callsite); :799 (issueDeityBoon); :1162 (_rollLootboxBoons)
    - contracts/DegenerusGameStorage.sol around :1204 (_applyWhalePassStats)
    - .planning/milestones/v41.0-phases/281-mint-batch-determinism-fix-fix/281-01-DESIGN-INTENT-TRACE.md (Phase 281 owed-salt snapshot precedent)
    - .planning/milestones/v42.0-phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md (Phase 290 MINTCLN gate convention)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_window_storage_read_freshness.md
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-105 → §2=V-109 → §3=V-110 → §4=V-111 → §5=V-112 → §6=V-113 → §7=V-114 → §8=V-117 → §9=V-120 → §10=V-121 → §11=V-122 → §12=V-123 → §13=V-124 → §14=V-125 → §15=V-127.

    **§N.A — Design-intent backward-trace**: For S-30 presaleStatePacked, document presale-phase architecture (pre-v25 baseline; constructor-initialized; phase-transition write at AdvanceModule:433). For S-32 mintPacked_, document its dual role as activity-score-input + streak-input + affiliate/deity sentinel container (multi-field packed slot). Trace the 6 EOA-reachable writer surfaces (Mint allocate + Whale buy-bundle multi + Affiliate cache + Boon consume + Streak write + Deity pass). For S-34 boonPacked, document the boon system architecture (per-player slot0/slot1 packed boon state) and the lootbox-roll consumption path (LootboxModule._applyBoon writes at multiple bit-offsets per boon type). For S-35 lastPurchaseDay, trace the purchase-day flag (advanceGame :176, :397 sets true; phase-transition :439 sets false; consumer reads inside _resolveLootboxCommon §7).

    **§N.B — Actor game-theory walk**: For each VIOLATION, exploit-actor classification:
    - V-105 presale: player buying tickets during rngLock to mutate presaleStatePacked before lootbox/lootbox-direct reads it; EV magnitude: MEDIUM (presale flag only conditional impact)
    - V-109 mintPacked_ via _mintStreakWrite: player mint-flow during rngLock manipulates streak input to inflate lootbox activity score; EV HIGH per `feedback_rng_window_storage_read_freshness.md`
    - V-110 mintPacked_ _allocateMintPacked (3 callsites): same vector as V-109; HIGH
    - V-111 mintPacked_ BoonModule.consumeActivityBoon: side-effect during roll consumption; pre-side-effect read on mintPacked_ is exploit vector; tactic (c) reorder eliminates by moving consume AFTER all RNG sub-rolls return
    - V-112 mintPacked_ BoonModule._applyBoon whale-pass: cross-EOA (issueDeityBoon callable to grant boon to another address); HIGH (whale-bundle / frozen-until state input)
    - V-113 mintPacked_ WhaleModule._buyWhaleBundle*: whale purchases during rngLock; HIGH
    - V-114 mintPacked_ WhaleModule._buyDeityPass: deity-pass purchase during rngLock; HIGH
    - V-117 mintPacked_ _applyWhalePassStats: whale-pass side-effect during lootbox boon roll; tactic (c) reorder eliminates
    - V-120 boonPacked LootboxModule._applyBoon multi-callsite incl. issueDeityBoon: cross-EOA issueDeityBoon enables admin to inject boon mid-rngLock affecting another player's lootbox-roll consumer; HIGH (privileged surface)
    - V-121 boonPacked WhaleModule: whale-bundle boon writes during rngLock; HIGH
    - V-122 boonPacked MintModule._processMint: same mint-time boon write; HIGH
    - V-123 boonPacked checkAndClearExpiredBoon: self-stack pre-roll-consumption with block.timestamp influence; tactic (b) snapshot expiry decision at allocation
    - V-124 boonPacked consumeActivityBoon slot1: tactic (c) reorder
    - V-125 boonPacked BoonModule other externals (:41, :67, :93, :122, :283): per-callsite verification deferred to v44.0; tactic (a) gate
    - V-127 lastPurchaseDay MintModule purchase: purchase entry sets flag affecting lootbox-resolution gate; MEDIUM (gate-flip vector)

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics:
    - V-105: (b) Snapshot presale flag per-index at allocation; Phase 288 dailyIdx precedent
    - V-109: (b) Snapshot streak into the lootbox-index at allocation, not LIVE at open; Phase 281 owed-salt
    - V-110: (b) Snapshot full activity-score-input set at bet/lootbox placement
    - V-111: (c) Reorder consumeActivityBoon to AFTER all RNG-driven sub-rolls return
    - V-112: (b) Snapshot whale-bundle / frozen-until state at lootbox allocation
    - V-113: (b) Same snapshot
    - V-114: (a) Gate buyDeityPass on `rngLockedFlag || lootboxRngWordByIndex[currentIdx] != 0`
    - V-117: (c) Reorder whale-pass side-effect to AFTER roll consumption returns
    - V-120: (a) Gate issueDeityBoon on the recipient having no open lootbox index ready
    - V-121: (a) MINTCLN-style gate on WhaleModule boon writes
    - V-122: (a) MINTCLN-style gate on MintModule boon writes
    - V-123: (b) Snapshot expiry decision based on day at allocation, not at open
    - V-124: (c) Reorder activity-boon consumption to AFTER all RNG-driven sub-rolls return
    - V-125: (a) Gate each EOA-reachable BoonModule external on no-fresh-lootbox-rng-in-window
    - V-127: (a) Gate purchase entry's lastPurchaseDay set on `!rngLockedFlag`

    Bytecode impact: tactic (a) gates ~30-50 bytes each; tactic (b) snapshots ~50-100 bytes per slot; tactic (c) reorders ~0 bytes (pure code-movement). Storage-layout: tactic (a) + (c) = byte-identical; tactic (b) for V-105/V-109/V-110/V-112/V-113/V-123 = new packed snapshot field (per-index extension). Public ABI: NON-BREAKING. Cite Phase 281 owed-salt + Phase 290 MINTCLN precedent.

    **§N.D — v44.0 handoff anchor**: V-105→H-63, V-109→H-64, V-110→H-65, V-111→H-66, V-112→H-67, V-113→H-68, V-114→H-69, V-117→H-70, V-120→H-71, V-121→H-72, V-122→H-73, V-123→H-74, V-124→H-75, V-125→H-76, V-127→H-77.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md \
  && for V in V-105 V-109 V-110 V-111 V-112 V-113 V-114 V-117 V-120 V-121 V-122 V-123 V-124 V-125 V-127; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for N in 63 64 65 66 67 68 69 70 71 72 73 74 75 76 77; do grep -q "D-43N-V44-HANDOFF-$N" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-HANDOFF-$N"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md)" -ge 15 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md)" -ge 15 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md)" -ge 15 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md)" -ge 15 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-08-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-H file exists; all 15 V-NNN entries; all 15 H-NN anchors (H-63..H-77); 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 15 V-NNN entries
- 15 handoff anchors (H-63..H-77)
- ≥60 sub-section markers
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-H file covering 15 presale + mintPacked_ + boonPacked + lastPurchaseDay VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-08-SUMMARY.md` when done.
</output>
