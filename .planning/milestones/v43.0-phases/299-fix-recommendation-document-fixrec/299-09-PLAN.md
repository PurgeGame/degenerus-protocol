---
phase: 299-fix-recommendation-document-fixrec
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-I FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-137, V-140, V-141, V-142, V-147, V-149, V-153, V-155, V-156, V-157, V-158, V-159, V-160, V-161) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-78..H-91)"
    - "§9 scope-expansion candidate for _requestLootboxRng (V-153) is surfaced as scope-expansion analysis per §0 headline #6"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster I (rngRequestTime governance + cross-contract affiliate/quest + Degenerette nonce + presale-pending + lootboxRng commitment-side + VRF config governance)"
      contains: "§N.A"
  key_links:
    - from: "299-09-FIXREC-cluster.md §N.D"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN"
      pattern: "D-43N-V44-HANDOFF-(7[8-9]|8[0-9]|9[0-1])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster I** — governance + cross-contract + commitment-side + VRF-config writers. 14 logical VIOLATIONs in scope: V-137 (rngRequestTime updateVrfCoordinatorAndSub clear governance), V-140 (affiliate cross-contract DegenerusAffiliate.recordAffiliateEarnings), V-141 (questView DegenerusQuests fulfillment), V-142 (degeneretteBets[player][nonce] _placeDegeneretteBetCore; :452 gate covers post-RNG), V-147 (prizePoolPendingPacked _collectBetFunds frozen-branch), V-149 (prizePoolPendingPacked MintModule frozen-branch purchase writers), V-153 (lootboxRngPacked.LR_MID_DAY _requestLootboxRng — §0 headline #6 scope-expansion candidate), V-155 (lootboxRngPacked.LR_MID_DAY updateVrfCoordinatorAndSub clear governance), V-156..V-161 (VRF config: vrfCoordinator/vrfSubscriptionId/vrfKeyHash via wireVrf constructor-only + updateVrfCoordinatorAndSub governance; tactics (d) immutable + (c) reorder).

Purpose: Theme is governance-window mutations + cross-contract dependency writers + the §0 headline #6 scope-expansion candidate. Per `D-299-FIXREC-LAYOUT-01`, this cluster explicitly addresses the catalog's downstream-decision flag: should the EXEMPT class be expanded to cover `_requestLootboxRng` and emergency governance VRF rotation as a milestone-prose amendment with zero contract change?

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-I FIXREC contribution (governance + cross-contract + commitment-side + VRF config)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-38, S-41, S-42, S-43, S-45, S-46, S-47, S-48, S-49
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-38 + S-41 + S-42 + S-43 + S-45 + S-46 + S-47 + S-48 + S-49
    - .planning/RNGLOCK-CATALOG.md §16 rows V-137, V-140, V-141, V-142, V-147, V-149, V-153, V-155, V-156, V-157, V-158, V-159, V-160, V-161
    - .planning/RNGLOCK-CATALOG.md §0 (headline #6 — Phase 299 scope expansion candidate from §9)
    - .planning/RNGLOCK-CATALOG.md §9 (AdvanceModule.retryLootboxRng consumer + _requestLootboxRng sibling)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/modules/DegenerusGameAdvanceModule.sol around :225 (rngGate clear path), :1096 (_requestLootboxRng — §9 scope-expansion candidate origin), :1122 (_tryRequestRng), :1154 (retryLootboxRng cooldown-reset), :1685, :1686, :1687, :1692, :1698 (updateVrfCoordinatorAndSub governance), :506, :507, :508 (wireVrf Admin constructor one-shot)
    - contracts/modules/DegenerusGameMintModule.sol around frozen-branch pending writers (:572 far-future RngLocked gate already)
    - contracts/modules/DegenerusGameDegeneretteModule.sol around :367, :405, :452, :479, :553 (placeDegeneretteBet nonce write + :452 lootboxRngWordByIndex gate)
    - contracts/modules/DegenerusGameJackpotModule.sol for jackpot-phase pending writes
    - contracts/DegenerusAffiliate.sol around recordAffiliateEarnings
    - contracts/DegenerusQuests.sol around external fulfillment writers
    - .planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-CONTEXT.md (D-42N-RETRY-RNG-DOMAIN-SEP-01 — retryLootboxRng domain separation Option A precedent; load-bearing for §0 headline #6 reasoning)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_frozen_contracts_no_future_proofing.md (contracts frozen at deploy — informs tactic (d) immutable recommendation)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-137 → §2=V-140 → §3=V-141 → §4=V-142 → §5=V-147 → §6=V-149 → §7=V-153 → §8=V-155 → §9=V-156 → §10=V-157 → §11=V-158 → §12=V-159 → §13=V-160 → §14=V-161.

    **§N.A — Design-intent backward-trace**: For V-137 + V-155 + V-157 + V-159 + V-161 (updateVrfCoordinatorAndSub governance), trace the governance VRF rotation pathway (introduced for VRF v2.5 migration / coordinator-rotation contingency). For V-140 (DegenerusAffiliate.recordAffiliateEarnings cross-contract), trace affiliate-system architecture (pre-v25 baseline affiliate registration). For V-141 (DegenerusQuests external fulfillment), trace quest-system architecture (pre-v25 baseline quest hooks). For V-142 (degeneretteBets[player][nonce] _placeDegeneretteBetCore write at DegeneretteModule:479), document the per-bet-nonce slot lifecycle (write at place / delete at resolve) and the existing :452 `lootboxRngWordByIndex[index] != 0` gate. For V-147 + V-149 (prizePoolPendingPacked frozen-branch writers during jackpot phase), document Phase 287 JPSURF + Phase 288 freeze-window design intent — the frozen flag at S-44 is set true only during the snap-and-distribute jackpot phase. For V-153 (lootboxRngPacked.LR_MID_DAY _requestLootboxRng), per §0 headline #6, this is the catalog's explicit Phase 299 scope-expansion-candidate flag — substantive risk nil (commitment-side sibling of EXEMPT-RETRYLOOTBOXRNG envelope; the retryLootboxRng caller benefits from both writes existing). For V-156, V-158, V-160 (wireVrf constructor-only Admin one-shot), trace the deploy-time VRF binding pattern (Admin one-shot, never re-callable post-construction in practice if constructor is the sole call site).

    **§N.B — Actor game-theory walk**: For V-137 + V-155 + V-157 + V-159 + V-161 (governance updateVrfCoordinatorAndSub), exploit-actor = adversarial admin (trust-minimization audit posture); action sequence = rotate VRF coordinator mid-stall to swap the in-flight callback source; EV magnitude: CATASTROPHE-tier (entire game RNG state) but LOW likelihood (governance discipline) — disposition: MITIGATE structurally regardless. For V-140 (affiliate), exploit-actor = affiliate registrant or affiliate-fee payer; action sequence = mid-rngLock affiliate registration mutates affiliate-cache fields consumed by §7 lootbox boon/cap calculations; EV magnitude: MEDIUM. For V-141 (quest), exploit-actor = player claiming quest mid-rngLock to mutate quest-streak field; EV magnitude: MEDIUM. For V-142 (degeneretteBets), existing :452 gate covers the post-RNG case — exploit-actor = player firing placeDegeneretteBet during the small window between request and lock; gate covers but edge cases need FUZZ verification. For V-147 + V-149 (prizePoolPendingPacked frozen-branch), exploit-actor = player placing degenerette bet / mint purchase during the snap-and-distribute jackpot phase; existing far-future RngLocked gate at MintModule:572 is partial — extend to pending writes. For V-153 (_requestLootboxRng), exploit-actor = legit player calling requestLootboxRng (this is the catalog's "substantive risk is nil" case); however per `D-43N-AUDIT-ONLY-01` "no SAFE_BY_DESIGN", catalog still classifies as VIOLATION. For V-156, V-158, V-160 (wireVrf constructor-only), exploit-actor = none post-construction; tactic (d) immutable formalizes the de-facto invariant.

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics:
    - V-137: (c) Pre-lock reorder — queue mid-stall rotations until after callback or 12h timeout
    - V-140: (b) Snapshot affiliate points into the lootbox-index at allocation
    - V-141: (b) Snapshot questStreak into the lootbox-index at allocation
    - V-142: (a) Existing :452 `lootboxRngWordByIndex[index] != 0` gate; verify across index-rollover edges via FUZZ-301
    - V-147: (a) Gate place-bet on `rngLockedFlag` so window closes once VRF requested
    - V-149: (a) Existing far-future `RngLocked` gate (:572) covers; extend to pending writes
    - V-153: (c) Pre-lock reorder: classify requestLootboxRng stack as **4th EXEMPT class** — milestone-prose amendment per §0 headline #6 (NO contract change required if classification expands; document the scope-expansion analysis in detail with rationale: substantive risk nil, retryLootboxRng caller benefits from both writes existing, no exploit vector identified)
    - V-155: (c) Pre-lock reorder: queue rotations until callback delivers or 12h timeout
    - V-156: (d) Immutable: bind VRF config at deploy and remove wireVrf or seal post-init
    - V-157: (c) Pre-lock reorder: governance rotation queued past in-flight VRF
    - V-158: (d) Immutable
    - V-159: (c) Pre-lock reorder
    - V-160: (d) Immutable
    - V-161: (c) Pre-lock reorder

    **§9 scope-expansion analysis** for V-153 (load-bearing): Explicit subsection within §7 of the FIXREC contribution. Reproduce §0 headline #6 prose + recommend the milestone-prose amendment shape (where in the milestone-goal prose to add the 4th EXEMPT class; what the amendment text should say; how it preserves the no-SAFE_BY_DESIGN rule by being a structural-classification change, not a SAFE_BY_DESIGN exception). Per `feedback_frozen_contracts_no_future_proofing.md`, contracts are frozen at deploy — the immutable tactic (d) for V-156/V-158/V-160 is structurally cleanest and the de-facto invariant already holds (wireVrf is constructor-only); v44.0 sub-phase materializes by deleting wireVrf or adding a one-shot lock flag.

    Bytecode impact: tactic (a) ~30-50 bytes per gate; tactic (b) snapshots ~50-100 bytes per slot; tactic (c) reorder ~0-50 bytes (may add a small pending-queue slot); tactic (d) immutable saves bytes by removing setter (~-50 to -100 bytes per setter). Storage-layout: (a)+(c) byte-identical; (b) new packed slot per snapshot; (d) converts storage slot to immutable bytecode constant (slot freed, layout shifts if not last slot — verify and document). Public ABI: NON-BREAKING for (a)+(b)+(c); BREAKING for (d) if external `updateVrfCoordinatorAndSub` is removed entirely vs sealed (catalog suggests "remove or seal post-init" — both options viable; document tradeoff).

    **§N.D — v44.0 handoff anchor**: V-137→H-78, V-140→H-79, V-141→H-80, V-142→H-81, V-147→H-82, V-149→H-83, V-153→H-84, V-155→H-85, V-156→H-86, V-157→H-87, V-158→H-88, V-159→H-89, V-160→H-90, V-161→H-91.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN (even for V-153 — use "EXEMPT class expansion" framing per §0 headline #6, not SAFE_BY_DESIGN).
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md \
  && for V in V-137 V-140 V-141 V-142 V-147 V-149 V-153 V-155 V-156 V-157 V-158 V-159 V-160 V-161; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for N in 78 79 80 81 82 83 84 85 86 87 88 89 90 91; do grep -q "D-43N-V44-HANDOFF-$N" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-HANDOFF-$N"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md)" -ge 14 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md)" -ge 14 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md)" -ge 14 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md)" -ge 14 ] \
  && grep -qE "scope.expansion|EXEMPT class expansion|4th EXEMPT" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-09-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-I file exists; all 14 V-NNN entries; all 14 H-NN anchors (H-78..H-91); 4 sub-sections per entry; §9 scope-expansion analysis for V-153 included; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 14 V-NNN entries
- 14 handoff anchors (H-78..H-91)
- ≥56 sub-section markers
- §9 scope-expansion analysis for V-153 surfaces per §0 headline #6
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-I file covering 14 governance + cross-contract + commitment-side + VRF-config VIOLATIONs, including the §0 headline #6 scope-expansion candidate analysis.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-09-SUMMARY.md` when done.
</output>
