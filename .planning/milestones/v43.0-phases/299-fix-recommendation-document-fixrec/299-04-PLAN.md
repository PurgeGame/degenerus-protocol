---
phase: 299-fix-recommendation-document-fixrec
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-D FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-043, V-045, V-046, V-047, V-048, V-050, V-051) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-20..H-26)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster D (sDGNRS poolBalances Reward + Lootbox cross-contract)"
      contains: "§N.A"
  key_links:
    - from: "299-04-FIXREC-cluster.md §N.D handoff anchor"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN cross-reference"
      pattern: "D-43N-V44-HANDOFF-(2[0-6])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster D** — sDGNRS cross-contract pool balances. 7 logical VIOLATIONs in scope: V-043 (poolBalances[Reward] non-advanceGame writer claim/settlement), V-045 (poolBalances[Reward] sDGNRS-internal admin / initial distribution), V-046 (poolBalances[Reward] OZ-inherited ERC20 writers — `_mint`/`_burn`/`transfer`/`transferFrom` per `D-298-OZ-CARVEOUT-01`), V-047 (poolBalances[Lootbox] transferFromPool from openLootBox), V-048 (poolBalances[Lootbox] from openBurnieLootBox), V-050 (poolBalances[Lootbox] from resolveRedemptionLootbox via sStonk claimRedemption), V-051 (poolBalances[Lootbox] transferBetweenPools mixed-callsite).

Purpose: Theme is cross-contract sDGNRS pool balances reached by EOA-callable surfaces during rngLock window. Per `D-298-EXEMPT-CROSSCONTRACT-01`, cross-contract slots inherit per-callsite classification. Phase 281 owed-salt snapshot precedent and activityScore snapshot pattern are load-bearing for tactic (b).

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-D FIXREC contribution (sDGNRS cross-contract pool balances)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-14, S-15
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-14 + S-15
    - .planning/RNGLOCK-CATALOG.md §16 rows V-043, V-045, V-046, V-047, V-048, V-050, V-051
    - .planning/RNGLOCK-CATALOG.md §1 (JackpotModule.payDailyJackpot _handleSoloBucketWinner consumer); §6 (LootboxModule.resolveRedemptionLootbox consumer); §7 (LootboxModule._resolveLootboxCommon manual-path consumer); §11 (BurnieCoinflip._resolveFlip consumer)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/StakedDegenerusStonk.sol around :312 (constructor pool init), :412 (transferFromPool writer), :422 (write site), :453, :455 (transferBetweenPools), :469 (burnAtGameOver), :657 (claimRedemption -= write)
    - contracts/modules/DegenerusGameLootboxModule.sol around :1786 (_creditDgnrsReward — reached from openLootBox/openBurnieLootBox/resolveLootboxDirect/resolveRedemptionLootbox)
    - contracts/DegenerusGame.sol around :420 (payCoinflipBountyDgnrs), :1735, :1739 (claim/settlement writes)
    - contracts/modules/DegenerusGameJackpotModule.sol around :1498 (_handleSoloBucketWinner final-day)
    - .planning/milestones/v41.0-phases/281-mint-batch-determinism-fix-fix/281-01-DESIGN-INTENT-TRACE.md (owed-salt 4th-keccak-input snapshot precedent — load-bearing for tactic (b))
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_backward_trace.md (every audit traces backward from consumer to verify input unknown at commitment)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-043 → §2=V-045 → §3=V-046 → §4=V-047 → §5=V-048 → §6=V-050 → §7=V-051.

    **§N.A — Design-intent backward-trace**: For sDGNRS pool balances, trace the cross-contract pool architecture introduction phase (likely pre-v25 baseline; sDGNRS = staked DegenerusStonk with Reward + Lootbox pool partitioning per `D-298-EXEMPT-CROSSCONTRACT-01` policy). Document why pools are mutable cross-call (legitimate sDGNRS internal accounting + ERC20 standard surface) and what would break if pools were frozen during rngLock (sDGNRS user-side `burn`/`burnWrapped` redemption flow would block — but the catalog tactic (b) snapshot pattern avoids freezing by reading a snapshot taken at allocation/commit time instead). For V-046, document the OZ-inherited writer carve-out per `D-298-OZ-CARVEOUT-01` — `_mint`/`_burn`/`transfer`/`transferFrom`/`approve`/`permit` live in `node_modules/@openzeppelin/contracts/token/ERC20/ERC20.sol`, never under `contracts/`. Pre-launch sDGNRS supply distribution is the originator phase.

    **§N.B — Actor game-theory walk**: For V-043 (non-advanceGame Reward-pool writers from `DegenerusGame.sol:1735, :1739` claim/settlement paths), exploit-actor = player triggering sDGNRS claimRedemption / settlement callbacks; action sequence = burn 1 wei stake mid-rngLock to force a `transferFromPool` call adjusting Reward pool balance before JackpotModule._handleSoloBucketWinner reads it for the final-day solo distribution. EV magnitude: MEDIUM-HIGH (terminal solo-bucket distribution can be CATASTROPHE-tier in absolute USD). For V-045 (sDGNRS-internal admin / initial distribution), exploit-actor = admin or pre-launch initial-distribution surface — MEDIUM (governance/admin trust assumption). For V-046 (OZ ERC20 surface), exploit-actor = any sDGNRS-holding EOA executing `transfer`/`transferFrom` during rngLock — LOW per-transfer EV but accumulates with concurrent claims (Reward pool is consumer-read in `_handleSoloBucketWinner`). For V-047/V-048 (Lootbox pool drained from manual-path openLootBox/openBurnieLootBox), exploit-actor = player opening lootbox during rngLock observing pending VRF — HIGH-tier (manual-path lootbox open is a deep VIOLATION cluster per §0 headline #2; Lootbox pool size is direct dgnrs-reward magnitude input). For V-050 (sStonk claimRedemption reach), exploit-actor = player claiming pending sStonk redemption mid-window — MEDIUM. For V-051 (transferBetweenPools mixed-callsite), per-callsite split: admin paths = MEDIUM; advance-stack = EXEMPT.

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics (all (b) snapshot/anchor except V-051 (b) per-callsite Phase 299 split):
    - V-043: (b) Snapshot `dgnrsPool` value at `_swapAndFreeze` time; consumer `_handleSoloBucketWinner` reads snapshot instead of live SLOAD. New packed slot for Reward-pool snapshot.
    - V-045: (b) Same snapshot-at-freeze pattern eliminates cross-contract write race.
    - V-046: (b) OZ-inherited writer; same snapshot-at-freeze covers ERC20 transfer race. Document OZ carve-out attestation per `D-298-OZ-CARVEOUT-01`.
    - V-047: (b) Snapshot pool balance at burn submission; pass as param into resolveRedemptionLootbox (mirror Phase 281 owed-salt snapshot pattern).
    - V-048: (b) Same snapshot tactic as V-047.
    - V-050: (b) Snapshot pool balance at burn submission alongside activityScore snapshot.
    - V-051: (b) Per-callsite Phase 299 split — admin paths recommend tactic (a) (gated revert on admin); advance-stack callsites are EXEMPT.

    Bytecode impact per (b) snapshot: ~100-200 bytes (new per-state packed snapshot field + 1 SLOAD/SSTORE delta per writer). Storage-layout: new packed slot (extends per-period or per-index struct) — NOT byte-identical. Public ABI: NON-BREAKING (no event topic-hash change; new internal field). Cite Phase 281 owed-salt + Phase 288 dailyIdx snapshot precedent verbatim.

    **§N.D — v44.0 handoff anchor**: V-043→H-20, V-045→H-21, V-046→H-22, V-047→H-23, V-048→H-24, V-050→H-25, V-051→H-26.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Cite project memory verbatim.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md \
  && for V in V-043 V-045 V-046 V-047 V-048 V-050 V-051; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-20 HANDOFF-21 HANDOFF-22 HANDOFF-23 HANDOFF-24 HANDOFF-25 HANDOFF-26; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md)" -ge 7 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-04-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-D file exists; all 7 V-NNN entries; all 7 H-NN anchors; 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 7 V-NNN entries (V-043, V-045..V-048, V-050, V-051)
- 7 handoff anchors (H-20..H-26)
- ≥28 sub-section markers
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-D file covering 7 sDGNRS pool-balance VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-04-SUMMARY.md` when done.
</output>
