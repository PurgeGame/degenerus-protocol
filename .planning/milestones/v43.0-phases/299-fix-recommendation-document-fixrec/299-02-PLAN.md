---
phase: 299-fix-recommendation-document-fixrec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-B FIXREC contribution file exists at .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md"
    - "Every V-NNN row in scope (V-016, V-017, V-018, V-019) has a §N analytical entry"
    - "Every §N entry contains all 4 sub-sections (§N.A + §N.B + §N.C + §N.D)"
    - "Every §N.D anchor cites D-43N-V44-HANDOFF-NN matching the catalog (H-09..H-12)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster B (traitBurnTicket admin + deityBySymbol whale family)"
      contains: "§N.A"
  key_links:
    - from: "299-02-FIXREC-cluster.md §N.D handoff anchor"
      to: ".planning/RNGLOCK-CATALOG.md §16 verdict-matrix row"
      via: "D-43N-V44-HANDOFF-NN ID cross-reference"
      pattern: "D-43N-V44-HANDOFF-(09|1[0-2])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster B** — `traitBurnTicket[lvl][trait]` (S-06) admin/helper writers + `deityBySymbol[fullSymId]` (S-07) whale writer. 4 logical VIOLATIONs in scope: V-016, V-017, V-018 (traitBurnTicket admin writers — `adminSeedTraitBucket`, `adminClearTraitBucket`, helper at :2510) + V-019 (deityBySymbol via `_purchaseDeityPass` EOA writer, runtime gate at WhaleModule:543).

Purpose: Per `D-299-FIXREC-LAYOUT-01` cluster-batch shape. Theme: admin-mutable trait-roll inputs + EOA-mutable deity-symbol roll inputs.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-B FIXREC contribution (traitBurnTicket admin + deityBySymbol whale)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-06, S-07
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-06 + S-07
    - .planning/RNGLOCK-CATALOG.md §16 rows V-016, V-017, V-018, V-019
    - .planning/RNGLOCK-CATALOG.md §1 / §2 / §3 (consumer context for traitBurnTicket roll; JackpotModule.payDailyJackpot + JackpotModule.payDailyJackpotCoinAndTickets + JackpotModule.runTerminalJackpot)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/DegenerusGame.sol around lines 2398, 2427, 2510 (adminSeedTraitBucket, adminClearTraitBucket, helper writer)
    - contracts/modules/DegenerusGameWhaleModule.sol around lines 538, 543, 598 (_purchaseDeityPass writer + existing rngLockedFlag gate at :543)
    - Project memory: feedback_design_intent_before_deletion.md (design-intent trace mandatory)
    - Project memory: feedback_never_preapprove_contracts.md (audit-only posture — no contract change)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-016 → §2=V-017 → §3=V-018 → §4=V-019.

    **§N.A — Design-intent backward-trace** (FIXREC-02): For traitBurnTicket (V-016..V-018), grep `.planning/milestones/` for `adminSeedTraitBucket` / `adminClearTraitBucket` introduction phase — likely pre-v25 baseline admin-bootstrap path; if no dedicated trace artifact, write `[design-intent: pre-v25 baseline admin bootstrap; no dedicated trace artifact]` per the no-fabrication rule. Document why these admin writers exist (initial trait-bucket seeding before launch + clearing for replay) and what behavior would break if a blanket `if (rngLockedFlag) revert` were added (e.g. pre-launch seeding before first rngLockedFlag set would still work; post-launch admin maintenance during live rngLock would be blocked — articulate the actor-friction boundary; the catalog tactic (a) suggests `!rngLockedFlag && !gameOver` which already preserves pre-launch + game-over teardown). For deityBySymbol (V-019), trace WhaleModule's deity-pass purchase design (introduced in pre-v25; deity-pass is a special whale-bundle variant binding fullSymId at purchase time); existing runtime `rngLockedFlag` gate at WhaleModule:543 is partial coverage — catalog flags V-019 as needing additional `!gameOver` gate.

    **§N.B — Actor game-theory walk** (FIXREC-03): For V-016..V-018, exploit-actor = admin (privileged); EV magnitude depends on whether admin is trusted — under trust-minimization audit posture, an adversarial admin during the rngLock window could call `adminSeedTraitBucket` to insert a known-rare-trait holder mid-resolution, shifting trait-roll outcomes. Even non-adversarial admin = HIGH-tier governance-mistake hazard during live resolution. For V-019, exploit-actor = whale player; action sequence = purchase deity pass during rngLock window observing pending VRF; binds fullSymId → buyer mapping that the JackpotModule consumers read. Existing :543 gate already blocks `rngLockedFlag`, but does not block `gameOver` window — terminal jackpot consumes deityBySymbol and an attacker could time deity purchase to capture terminal payout via a strategic symbol bind. EV magnitude: MEDIUM-HIGH (terminal jackpot payouts can be CATASTROPHE-tier in absolute USD).

    **§N.C — Recommended tactic + rationale + impact** (FIXREC-01 + FIXREC-04): Catalog assigns tactic (a) to all 4. Reproduce per-VIOLATION:
    - V-016: gate `adminSeedTraitBucket` on `!rngLockedFlag && !gameOver`
    - V-017: gate `adminClearTraitBucket` on `!rngLockedFlag && !gameOver`
    - V-018: gate helper writer at :2510 on `!gameOver` (catalog notes terminal jackpot bucket must be frozen at drain)
    - V-019: gate `_purchaseDeityPass` on `!gameOver` (rngLockedFlag gate at :543 already in place — extend with gameOver gate)

    Bytecode impact: each `revert RngLocked()` or `revert GameOver()` adds ~30-50 bytes (one SLOAD + one revert). Storage-layout: byte-identical (no new slots). Public ABI: NON-BREAKING per `D-40N-EVT-BREAK-01` — adds new revert path, no event topic-hash change. Cite the `RngLocked` custom error convention used at MintModule:1221 / BurnieCoinflip:730 / sStonk:492.

    **§N.D — v44.0 handoff anchor** (FIXREC-05): Emit D-43N-V44-HANDOFF-09 (V-016), HANDOFF-10 (V-017), HANDOFF-11 (V-018), HANDOFF-12 (V-019). Format: `**Anchor:** D-43N-V44-HANDOFF-NN — {summary}. **Catalog row:** RNGLOCK-CATALOG.md:{line}. **Writer:** {file}:{line}.`

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Do NOT fabricate design-intent citations. Cite feedback memory verbatim where applicable.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md \
  && for V in V-016 V-017 V-018 V-019; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-09 HANDOFF-10 HANDOFF-11 HANDOFF-12; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md)" -ge 4 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md)" -ge 4 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md)" -ge 4 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md)" -ge 4 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-B file exists; all 4 V-NNN entries present; all 4 handoff anchors (H-09..H-12) present; 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- Cluster-B FIXREC contribution exists
- All 4 V-NNN entries (V-016..V-019)
- All 4 handoff anchors (H-09..H-12)
- ≥16 sub-section markers (4 sections × 4 entries)
- Zero SAFE_BY_DESIGN tokens
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED file at `.planning/phases/299-fix-recommendation-document-fixrec/299-02-FIXREC-cluster.md` covering Cluster B's 4 VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-02-SUMMARY.md` when done.
</output>
