---
phase: 359-impl-the-one-carefully-sequenced-batched-contract-diff-handl
plan: 02
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameMintStreakUtils.sol
autonomous: false
requirements: [BURNIE-01, BURNIE-02, SALVAGE-01]
must_haves:
  truths:
    - "A BURNIE coin buy QUEUES tickets (no longer a pure token sink) — _purchaseCoinFor queues on the captured adjusted quantity"
    - "The MINT_BURNIE quest credit is restored on the coin path as a BURN REBATE: full cost gated upfront, burn deferred and netted (never a separate creditFlip)"
    - "The far-future-salvage cash leg pays a combo ETH+BURNIE, BURNIE transferred from sDGNRS-owned BURNIE (never creditFlip-minted), ETH fallback when sDGNRS has none"
    - "Only the ETH part is relabeled out of claimableWinnings[SDGNRS] (≤ cashWei) → solvency-positive; the ETH/claimablePool DEBIT code stays byte-unchanged"
  artifacts:
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "BURNIE-01 queue-on-return + BURNIE-02 deferred net-burn rebate + SALVAGE-01 combo ETH/BURNIE cash-leg split"
      contains: "_queueTicketsScaled"
    - path: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      provides: "the SALVAGE quote extended with the new BURNIE-split seed slice + source-availability/fallback so the offer stays previewable"
      contains: "_quoteFarFutureSwap"
  key_links:
    - from: "_purchaseCoinFor (coin path)"
      to: "_queueTicketsScaled(buyer, targetLevel, adjustedQty, false)"
      via: "capture _callTicketPurchase returns, queue when non-zero (mirrors the ETH path :1251)"
      pattern: "_queueTicketsScaled\\(buyer, targetLevel"
    - from: "_purchaseCoinFor (coin path)"
      to: "quests.handlePurchase MINT_BURNIE leg → returned reward → deferred net burn"
      via: "full-cost gate upfront, handlePurchase (ethMintSpendWei=0, lootBoxAmount=0), burn net = coinCost − reward floored at 0"
      pattern: "handlePurchase"
    - from: "sellFarFutureTickets cash leg"
      to: "claimableWinnings[SDGNRS] -= ethOut (only the ETH part) + sDGNRS-owned BURNIE transfer"
      via: "actualBurnie = min(target, sDGNRS-available); remainder/fallback as ETH; ethOut ≤ ethCap"
      pattern: "claimableWinnings\\[ContractAddresses.SDGNRS\\]"
---

<objective>
Author the two functional-solvency consumers, BOTH in `DegenerusGameMintModule.sol` (+ the shared quote in `DegenerusGameMintStreakUtils.sol`) — opened ONCE for BURNIE + SALVAGE together (SPEC D-02 by-severity/by-file order). Highest-severity first:

1. **BURNIE-01 (Critical loss-of-funds fix):** `_purchaseCoinFor` today calls `_callTicketPurchase(... payInCoin=true ...)` as a bare statement and discards all 4 returns → the coin is burned but ZERO tickets are queued (a pure token sink on a LIVE entrypoint). FIX: capture the returns and `if (adjustedQty != 0) _queueTicketsScaled(buyer, targetLevel, adjustedQty, false);` — mirroring the ETH path at :1251.
2. **BURNIE-02 (MINT_BURNIE quest credit restored as a BURN REBATE):** route `burnieMintUnits` through `quests.handlePurchase`'s MINT_BURNIE leg only (`ethMintSpendWei=0`, `lootBoxAmount=0`); the reward (the value plan 01's producer now RETURNS) is applied as a BURN REBATE — the tx REQUIRES the FULL ticket cost upfront, the burn is DEFERRED until after `handlePurchase` returns, then burns net = full `coinCost` − reward (floored at 0). Never a separate `creditFlip`.
3. **SALVAGE-01 (combo ETH/BURNIE pawn-shop payout):** split the far-future-salvage cash leg into an ETH part + a BURNIE part; randomize a TARGET BURNIE portion `[0..cashWei]` from a NEW slice of the existing prior-day `seed` (no new VRF); pay `actualBurnie = min(target, sDGNRS-available)` by TRANSFERRING sDGNRS-OWNED BURNIE (token balance + claimable coinflip stake), remainder + zero-available case as ETH; only the ETH part leaves `claimableWinnings[SDGNRS]`.

Purpose: close a Critical loss-of-funds bug + restore the intended pre-160 BURNIE→ticket behavior + add the solvency-positive combo salvage payout.
Output: the BURNIE + SALVAGE hunks authored against the frozen subject, NOT committed — held for the plan-04 batched USER hand-review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Execution mode (HARD constraints)
- **Sequential-on-main, worktrees OFF.** Author directly on the working tree. This plan depends on 359-01 (the BATCH-01 producer must already RETURN `burnieMintReward` — BURNIE-02 consumes that return) AND shares `DegenerusGameMintModule.sol` with the BATCH caller fold, so it runs strictly AFTER plan 01.
- **CONTRACT-COMMIT BOUNDARY = HARD STOP.** `autonomous: false`. Author the hunks; DO NOT commit `contracts/*.sol`. Held for the single batched USER hand-review at the plan-04 gate.
- **`ContractAddresses.sol` is freely modifiable** (regenerated by build) — but this plan does not need to touch it.
- **Code-comment style (USER rule):** comments explain HOW THE CODE WORKS + real invariants in plain English. CUT all procedural meta (plan IDs, req tags, spec-line citations, milestone history, "USER-accepted/LOCKED"). The D-NN/anchor citations here are for YOU, not the Solidity. Fix the FALSE/misleading comments you touch; never change logic when trimming.
- **Frozen-subject guard** clean at start; re-read each anchor before editing.
- Do NOT run a per-feature `forge build` (SPEC D-03 = two checkpoints: post-features at end of plan 03, post-UDVT in plan 04).
</execution_context>

<context>
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-CONTEXT.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md

<interfaces>
<!-- The buggy fn, the return signature being discarded, the ETH-path queue analog, the quote, and the sDGNRS-owned-BURNIE source primitives. Extracted @ 1e7a646d. -->

DegenerusGameMintModule.sol:887-907 — the buggy fn (discards 4 returns; coin burned, ZERO tickets queued):
  function _purchaseCoinFor(address buyer, uint256 ticketQuantity) private {
      if (_livenessTriggered()) revert E();                 // :891  freeze gate (no rngWord read)
      if (ticketQuantity != 0) {
          if (gameOverPossible) revert GameOverPossible();   // :895  solvency gate
          _callTicketPurchase(buyer, ticketQuantity, MintPaymentKind.DirectEth,
              true /*payInCoin*/, bytes32(0), 0, level, jackpotPhaseFlag);   // :896  ← 4 returns DISCARDED
      }
  }

DegenerusGameMintModule.sol:1484-1501 — the return signature being discarded:
  function _callTicketPurchase(address buyer, uint256 quantity, MintPaymentKind payKind,
      bool payInCoin, bytes32 affiliateCode, uint256 value, uint24 cachedLevel, bool cachedJpFlag)
      private returns (uint256 bonusCredit, uint32 adjustedQty32, uint24 targetLevel, uint32 burnieMintUnits)

DegenerusGameMintModule.sol:1545-1555 — the payInCoin branch (burns NOW via _coinReceive, never queues):
  if (payInCoin) {
      uint256 coinCost = (quantity * (PRICE_COIN_UNIT / 4)) / TICKET_SCALE;
      _coinReceive(buyer, coinCost);                         // :1548  burns NOW (_coinReceive:1652 → coin.burnCoin:1653)
      uint32 questQty = uint32(quantity / (4 * TICKET_SCALE));
      if (questQty != 0) burnieMintUnits += questQty;        // deferred to handlePurchase
  }                                                          // ← no _queueTicketsScaled

DegenerusGameMintModule.sol:1249-1252 — the ETH queue ANALOG the coin fix mirrors:
  if (adjustedQty != 0) { _queueTicketsScaled(buyer, targetLevel, adjustedQty, false); }   // :1251

DegenerusGameMintModule.sol:1210-1217 — the handlePurchase call (MINT_BURNIE leg: pass ethMintSpendWei=0, lootBoxAmount=0)
DegenerusGameMintModule.sol:1657-1663 — _ethToBurnieValue(amountWei, mintPrice()) = amountWei * PRICE_COIN_UNIT / priceWei
DegenerusGameStorage.sol:612-621 — _queueTicketsScaled(address buyer, uint24 targetLevel, uint32 quantityScaled, bool rngBypass) — internal; reverts on _livenessTriggered(); rngBypass=false

DegenerusGameMintModule.sol:929-986 — sellFarFutureTickets impl; gates :935-937 (rngLockedFlag / gameOver / _livenessTriggered); SDGNRS >=1 ETH floor :958; relabel :976-977; ticket leg :983.
DegenerusGameMintStreakUtils.sol:159-189 — the quote/jitter (SETTLED prior-day word, two seed slices):
  uint256 seed = uint256(keccak256(abi.encodePacked(player, rngWordByDay[_simulatedDayIndex() - 1])));  // :160-163
  uint256 jitterMult     = 7000 + (seed % 4001);          // :165  slice 1 [70%,110%]
  uint256 ticketShareBps = 4000 + ((seed >> 128) % 4001); // :166  slice 2 [40%,80%] → cash [20%,60%]
  ticketWei = (totalBudget * ticketShareBps) / 10_000; ... cashWei = totalBudget - ticketWei;  // :186-189

sDGNRS-owned-BURNIE source primitives (TRANSFER / consume, never creditFlip-mint):
  BurnieCoin.transfer:315 / transferFrom:329 (GAME bypass, no allowance) — sDGNRS token balance
  BurnieCoinflip.previewClaimCoinflips(address):927 + coinflipAmount(address):934 (availability read) + consumeCoinflipsForBurn(address,uint256):366 (onlyBurnieCoin, returns actual consumed) — sDGNRS claimable coinflip stake
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BURNIE-01 + BURNIE-02 — queue-on-return + MINT_BURNIE deferred net-burn rebate (Critical fix)</name>
  <files>contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol:880-907 (the buggy `_purchaseCoinFor`, gates :891/:895), :1484-1501 (`_callTicketPurchase` returns), :1545-1555 (the payInCoin burn branch — burns inside, never queues), :1249-1252 (the ETH queue analog at :1251), :1205-1224 (the `handlePurchase` call + the ETH caller fold), :1652-1663 (`_coinReceive`/`burnCoin` + `_ethToBurnieValue`).
    - contracts/storage/DegenerusGameStorage.sol:612-621 (`_queueTicketsScaled` signature).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — BURNIE-03 §D-21 (the verified bug + the DECISIVE 2-caller grep), §D-22 (queue-on-return), §D-23 (the burn-rebate: full-cost gate upfront / deferred / netted; co-design with the plan-01 producer), §D-24 (freeze/solvency posture-widening flagged) + IMPL Handoff Invariants 3 + 4.
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §2 (the full BURNIE pattern with all anchors + the reorder requirement).
    - The 359-01-SUMMARY.md (confirm plan 01's producer is in place: `handlePurchase` RETURNS `burnieMintReward`; note any caller-fold discrepancy it flagged).
  </read_first>
  <action>
    In `contracts/modules/DegenerusGameMintModule.sol`:
    (BURNIE-01, anchor `_purchaseCoinFor:887-907`, governing SPEC D-22 / Handoff Invariant 3) — CAPTURE the returns of `_callTicketPurchase` (`(uint256 bonusCredit, uint32 adjustedQty32, uint24 targetLevel, uint32 burnieMintUnits)`) instead of discarding them, then `if (adjustedQty32 != 0) _queueTicketsScaled(buyer, targetLevel, adjustedQty32, false);` — the EXACT `_queueTicketsScaled(..., false)` shape the ETH path uses at :1251. The `_livenessTriggered()` (:891) + `gameOverPossible` (:895) gates and the ETH/`claimablePool` DEBIT code stay BYTE-UNCHANGED (Handoff Invariant 3).
    (BURNIE-02, governing SPEC D-23 / Handoff Invariant 4) — restore the MINT_BURNIE quest credit on the coin path as a BURN REBATE, producer-before-consumer: (a) REQUIRE the player to afford the FULL ticket `coinCost` upfront (the eligibility/cost gate is the full amount, never the net — the rebate can never enable a buy the player couldn't otherwise complete); (b) DEFER the actual burn — today the burn happens INSIDE `_callTicketPurchase:1548` before `handlePurchase` runs, so REORDER so the burn occurs AFTER the reward is known; (c) call `quests.handlePurchase` MINT_BURNIE leg only — pass `ethMintSpendWei=0`, `lootBoxAmount=0` (deliberately skipping activity-score/`recordMint`, affiliate, non-mint quests — correct for BURNIE), routing `burnieMintUnits`; (d) take the RETURNED reward (the value plan 01 now returns) and burn net = full `coinCost` − reward, FLOORED at 0. The rebate is NEVER a separate `creditFlip` (it is a reduced burn ≤ full cost). Use `_coinReceive`/`burnCoin` (:1652-1653) as the deferred-burn primitive; value conversions via `_ethToBurnieValue:1657` if needed. Fix the misleading "ETH mint reward" wording in any comment you touch (lean-comment rule). The freeze posture is unaffected (`purchaseCoin` reads no `rngWord`); the SOLVENCY ETH/pool DEBIT is byte-unchanged but ticket claims are RESTORED — the posture-widening is intentional and verified at TST 361.
  </action>
  <verify>
    <automated>grep -c "_queueTicketsScaled(buyer, targetLevel" contracts/modules/DegenerusGameMintModule.sol; grep -c "handlePurchase" contracts/modules/DegenerusGameMintModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `_purchaseCoinFor` no longer calls `_callTicketPurchase(...)` as a bare statement — it CAPTURES the returns and calls `_queueTicketsScaled(buyer, targetLevel, adjustedQty32, false)` when `adjustedQty32 != 0` (mirroring the ETH path :1251).
    - The MINT_BURNIE leg is routed via `quests.handlePurchase` with `ethMintSpendWei=0` and `lootBoxAmount=0`.
    - The full `coinCost` affordability gate is UPFRONT; the burn is DEFERRED to after `handlePurchase` returns; the burned amount is net = `coinCost − reward` floored at 0 (a reduced burn ≤ full cost, never a separate `creditFlip`).
    - The `_livenessTriggered():891` + `gameOverPossible:895` gates and all ETH/`claimablePool` DEBIT code are byte-unchanged.
    - No comment claims a reward is paid in ETH; no procedural-meta comments added.
  </acceptance_criteria>
  <done>A BURNIE coin buy queues tickets and applies the MINT_BURNIE quest credit as a deferred net-burn rebate (full-cost gated upfront); the loss-of-funds sink is closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SALVAGE-01 — far-future-salvage cash-leg combo ETH/BURNIE split (execute path)</name>
  <files>contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol:929-986 (the executing swap; gates :935-937; SDGNRS >=1 ETH floor :958; the relabel :976-977; ticket leg :983), :1657-1663 (`_ethToBurnieValue`).
    - contracts/BurnieCoin.sol:315 (`transfer`) + :329 (`transferFrom`) — sDGNRS token-balance move (GAME bypass, no allowance).
    - contracts/BurnieCoinflip.sol:927 (`previewClaimCoinflips`) + :934 (`coinflipAmount`) + :366 (`consumeCoinflipsForBurn`, onlyBurnieCoin, returns actual consumed) + :904-912 (the sDGNRS stake-consume pattern) — sDGNRS claimable coinflip stake.
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — SALVAGE-02 §D-25 (current structure), §D-26 (the split design + funding fallback + the LOCKED sDGNRS-owned-BURNIE primitives), §D-27 (the pawn-shop total-payout-cap + eth-%-cap safety model), §D-29 (solvency-positive accounting) + IMPL Handoff Invariants 5 + 6.
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §3 (the SALVAGE pattern; the flagged file-PATH drift — use `contracts/modules/DegenerusGameMintStreakUtils.sol`, NOT `contracts/utils/`).
  </read_first>
  <action>
    In `contracts/modules/DegenerusGameMintModule.sol` `sellFarFutureTickets` (anchor :929-986, governing SPEC D-26 / Handoff Invariants 5 + 6): split the cash leg `cashWei` into an ETH part + a BURNIE part. Pay `actualBurnie = min(targetBurnie, sDGNRS-available)` by TRANSFERRING sDGNRS-OWNED BURNIE — NEVER a `creditFlip` mint: sources = sDGNRS's BURNIE token balance (`BurnieCoin.transfer`/`transferFrom`) + sDGNRS's claimable coinflip stake (`coinflipAmount(SDGNRS)`/`previewClaimCoinflips` availability read; `consumeCoinflipsForBurn`-style consume), valued via `_ethToBurnieValue(part, mintPrice())` at the current eth-equivalent. The REMAINDER (`cashWei − actualBurnie`) is paid as ETH via the EXISTING relabel — but ONLY the ETH part leaves SDGNRS now: change `claimableWinnings[SDGNRS] -= totalBudget` / `+= totalBudget` (:976-977) so the SDGNRS debit + the player credit are `ticketWei + ethOut` (i.e. `totalBudget − actualBurnie`), NOT the full `totalBudget`. FUNDING FALLBACK: if sDGNRS has ZERO BURNIE + ZERO claimable coinflip → no BURNIE leg, pay the whole cash leg in ETH (current behavior). Always cap-compliant: `ethOut = cashWei − actualBurnie ≤ cashWei ≤ ethCap`. The `targetBurnie` comes from a NEW derived bit-slice of the SAME `seed` produced by the quote (Task 3) — no new VRF. The ticket leg (:983) is UNCHANGED. The gates (:935-937) and the SDGNRS >=1 ETH floor (:958) are UNCHANGED. Update the `FarFutureSwap` event semantics consistently with the split (the cash component now subdivides into ETH + BURNIE). Keep comments lean + functional.
  </action>
  <verify>
    <automated>grep -c "consumeCoinflipsForBurn\|BurnieCoin\|burnie.transfer\|burnie.transferFrom" contracts/modules/DegenerusGameMintModule.sol; grep -n "claimableWinnings\[ContractAddresses.SDGNRS\]" contracts/modules/DegenerusGameMintModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - The cash leg pays `actualBurnie = min(targetBurnie, sDGNRS-available)` from sDGNRS-OWNED BURNIE via `transfer`/`transferFrom` and/or the claimable-coinflip-stake consume — NEVER a `creditFlip` mint.
    - Only the ETH part (`cashWei − actualBurnie`) is relabeled out of `claimableWinnings[SDGNRS]` (the `-= totalBudget`/`+= totalBudget` pair at :976-977 is reduced to `totalBudget − actualBurnie`).
    - Zero sDGNRS BURNIE + zero claimable coinflip → whole cash leg in ETH (the "like now" fallback).
    - `ethOut = cashWei − actualBurnie ≤ cashWei ≤ ethCap` on every path; the gates (:935-937) and the SDGNRS >=1 ETH floor (:958) are byte-unchanged; the ticket leg (:983) is unchanged.
    - No new VRF read; the BURNIE-split target derives from the existing prior-day `seed` slice (from Task 3).
  </acceptance_criteria>
  <done>The cash leg pays a variable ETH+BURNIE combo, BURNIE transferred from sDGNRS-owned BURNIE (never minted), ETH fallback when sDGNRS empty; only the ETH part leaves SDGNRS (solvency-positive).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SALVAGE-01 — extend the quote with the BURNIE-split slice + source-availability/fallback (preview parity)</name>
  <files>contracts/modules/DegenerusGameMintStreakUtils.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintStreakUtils.sol:127-130 (`_farFutureFractionBps`), :145-190 (`_quoteFarFutureSwap`: seed :160-163, jitter slice 1 :165, ticketShareBps slice 2 :166, cashWei :189, the four named returns :152-157).
    - contracts/BurnieCoinflip.sol:927 (`previewClaimCoinflips`) + :934 (`coinflipAmount`) — the sDGNRS availability reads the preview must reflect.
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — SALVAGE-02 §D-26 (the new slice + the preview-MUST-reflect-source-availability+fallback requirement), §D-27 (the within-day-variability latitude — Claude's discretion, but every offer stays previewable + ≤ no-arb ceiling + eth-% cap), §D-28 (no new VRF; same settled prior-day word) + IMPL Handoff Invariant 6.
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §3 ("SALVAGE-01 new slice (D-26)" + "Settled-prior-day-word seed (no new VRF)" shared pattern) + the flagged PATH drift (this file is `contracts/modules/...`).
  </read_first>
  <action>
    In `contracts/modules/DegenerusGameMintStreakUtils.sol` `_quoteFarFutureSwap` (anchor :145-190, governing SPEC D-26/D-28 / Handoff Invariant 6): add a THIRD derived bit-slice of the SAME `seed` (e.g. a distinct window like `(seed >> 64) % ...`, alongside the existing `seed % 4001` and `(seed >> 128) % 4001`) to pick `targetBurnie ∈ [0, cashWei]` — the randomness SOURCE stays the settled prior-day word `rngWordByDay[_simulatedDayIndex() − 1]`, NO new VRF. Extend the quote so the offer stays truly knowable-in-advance: the preview MUST reflect sDGNRS source-availability (read `coinflipAmount(SDGNRS)` / `previewClaimCoinflips` + the sDGNRS BURNIE token balance) and the ETH fallback — so the previewed ETH/BURNIE split equals what the execute path (Task 2) will actually pay (`actualBurnie = min(target, available)`, remainder ETH; zero-available ⇒ all-ETH). Return the additional split info the execute path consumes (extend the named returns or add a sibling quote helper as needed — keep the existing four named values intact for backward compatibility where other callers read them). Every reachable offer (across seed × bundle × split × timing) MUST stay ≤ the existing no-arb ceiling + the eth-% cap — the split conserves the cash-leg VALUE (it changes the form/source only). Keep comments lean + functional (the seed slices + the cap invariant in plain English).
  </action>
  <verify>
    <automated>grep -c "seed" contracts/modules/DegenerusGameMintStreakUtils.sol; grep -c "coinflipAmount\|previewClaimCoinflips\|balanceOf" contracts/modules/DegenerusGameMintStreakUtils.sol</automated>
  </verify>
  <acceptance_criteria>
    - `_quoteFarFutureSwap` adds a THIRD derived slice of the SAME `seed` to pick `targetBurnie ∈ [0, cashWei]`; the only randomness source remains `rngWordByDay[_simulatedDayIndex() − 1]` (no new VRF dependency introduced).
    - The quote reflects sDGNRS source-availability + the ETH fallback so the previewed split matches the execute path's `actualBurnie = min(target, available)` with ETH remainder/fallback.
    - The existing four named return values stay readable by current callers (backward-compatible — extend, don't break).
    - The total cash-leg VALUE is conserved (split changes form/source only) → every reachable offer ≤ the existing no-arb ceiling + eth-% cap.
  </acceptance_criteria>
  <done>The quote produces a previewable ETH/BURNIE split from a new same-seed slice that reflects source-availability + fallback, conserving the cash-leg value (no new VRF).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DegenerusVault.gamePurchaseTicketsBurnie → DegenerusGame.purchaseCoin → _purchaseCoinFor | a live BURNIE-coin purchase entrypoint — the loss-of-funds surface being fixed |
| _purchaseCoinFor → coin.burnCoin (deferred net burn) | BURNIE burn primitive; the rebate must never enable an unaffordable buy |
| sellFarFutureTickets → claimableWinnings[SDGNRS] (ETH relabel) | the ETH solvency path — DEBIT code must stay byte-unchanged in shape |
| sellFarFutureTickets → BurnieCoin.transfer / consumeCoinflipsForBurn (sDGNRS-owned BURNIE) | TRANSFER of sDGNRS's own BURNIE — never a creditFlip mint; source-availability prevents over-draw |

## STRIDE Threat Register (mapped to the SPEC's discharged register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-09 | Elevation / loss-of-funds | _purchaseCoinFor (coin burned, zero tickets) | mitigate | BURNIE-01 queue-on-return restores the pre-160 BURNIE→ticket behavior; BURNIE-02 burn-rebate requires the FULL cost upfront (the rebate ≤ full cost, can never enable an unaffordable buy). Closed at IMPL; HYG-03 positive test @ TST 361. |
| T-358-09-FREEZE | Tampering (RNG/freeze) | purchaseCoin / _purchaseCoinFor | accept | `purchaseCoin` reads no `rngWord`; gated by `_livenessTriggered():891` + `gameOverPossible:895`. SPEC RNG-Freeze §(6). |
| T-358-09-SOLV | Loss-of-funds (solvency) | ETH prize pools | mitigate | ETH/`claimablePool` DEBIT byte-unchanged; restored ticket claims stay pro-rata from the AVAILABLE pool (no unbacked obligation) → `claimablePool <= balance`. SPEC SOLVENCY §(6); HYG-03 buy-then-claim path @ 361. POSTURE-WIDENING FLAGGED. |
| T-358-12 | Loss-of-funds (solvency) / over-draw | SALVAGE sDGNRS-source over-draw + no-arb cap | mitigate | `actualBurnie = min(target, sDGNRS-available)` + ETH fallback prevents over-draw; only the ETH part (`≤ cashWei`) leaves SDGNRS (solvency-POSITIVE); every reachable offer ≤ no-arb ceiling + eth-% cap (the pawn-shop cap). SPEC D-27/D-29; EXTEND-`SWAP08` no-arb re-proof @ SALVAGE-03 (361). |
| T-358-SALV-FREEZE | Tampering (RNG/freeze) | SALVAGE split randomness | accept | Reads ONLY the settled prior-day word under the `rngLockedFlag` gate (:935); the new split is one MORE slice of the same seed — no new VRF. Non-exploitability rests on the cap, not unpredictability. SPEC RNG-Freeze §(7). |
| T-359-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (Solidity edits only). |

Defer EMPIRICAL proof to TST 361 (HYG-03 BURNIE buy-then-claim + `claimablePool <= balance`; SALVAGE-03 EXTEND-`SWAP08` no-arb across the full split range + the zero-available fallback) and the delta-audit at TERMINAL 362. Do not invent new mitigations — cite the SPEC's.
</threat_model>

<verification>
- `_purchaseCoinFor` captures returns + `_queueTicketsScaled(buyer, targetLevel, adjustedQty32, false)` when non-zero (BURNIE-01).
- The MINT_BURNIE leg is routed via `handlePurchase(ethMintSpendWei=0, lootBoxAmount=0)`; burn is deferred + netted (`coinCost − reward` floored 0); full-cost gate upfront (BURNIE-02).
- The salvage cash leg pays sDGNRS-owned BURNIE (transfer/consume, never `creditFlip`); only the ETH part leaves SDGNRS; ETH fallback present (SALVAGE-01).
- The quote adds a same-seed BURNIE-split slice + reflects source-availability/fallback; no new VRF (SALVAGE-01 preview parity).
- The ETH/`claimablePool` DEBIT code shape is byte-unchanged; gates (:935-937, :891, :895) and the SDGNRS floor (:958) unchanged.
- NOTE: the `forge build` green gate is in plan 03 — do NOT run a per-feature compile here.
</verification>

<success_criteria>
- BURNIE-01: a BURNIE coin buy QUEUES tickets (queue-on-return mirroring the ETH path).
- BURNIE-02: the MINT_BURNIE quest credit is restored as a deferred net-burn rebate (full cost gated upfront; never a separate `creditFlip`).
- SALVAGE-01: the cash leg pays a variable ETH+BURNIE combo (BURNIE transferred from sDGNRS-owned BURNIE, ETH fallback), previewable, only the ETH part leaving SDGNRS (solvency-positive).
- No new VRF; RNG-freeze posture preserved; the ETH/`claimablePool` DEBIT code byte-unchanged.
- No commit made (held for the plan-04 USER hand-review gate).
</success_criteria>

<output>
Create `.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-02-SUMMARY.md` when done. Record: the exact functions/lines changed in `DegenerusGameMintModule.sol` + `DegenerusGameMintStreakUtils.sol`, the deferred-burn reorder shape, the sDGNRS-owned-BURNIE source primitives used, the relabel reduction (only ETH part leaves SDGNRS), the new quote slice + how preview parity is achieved, and an explicit note that NO commit was made (held for the plan-04 batched contract-commit hand-review gate).
</output>
