---
phase: 359-impl-the-one-carefully-sequenced-batched-contract-diff-handl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusQuests.sol
  - contracts/modules/DegenerusGameMintModule.sol
autonomous: false
requirements: [BATCH-01, BATCH-02]
must_haves:
  truths:
    - "DegenerusQuests.handlePurchase no longer credits burnieMintReward inline; it folds it into the returned reward"
    - "The sole ETH caller in DegenerusGameMintModule still credits the same recipient the same total via lootboxFlipCredit"
    - "The misleading ETH-mint-reward comments are corrected to quest-TYPE semantics (no payout is ETH)"
  artifacts:
    - path: "contracts/DegenerusQuests.sol"
      provides: "handlePurchase return-don't-credit-inline reward routing (BATCH-01) + corrected comments (BATCH-02)"
      contains: "function handlePurchase"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "the unchanged caller fold that absorbs the now-larger return (read-attested, no behavior change)"
      contains: "lootboxFlipCredit"
  key_links:
    - from: "contracts/DegenerusQuests.sol handlePurchase"
      to: "contracts/modules/DegenerusGameMintModule.sol caller fold (:1220 → single credit :1355)"
      via: "totalReturned now includes burnieMintReward; caller adds questReward into lootboxFlipCredit"
      pattern: "lootboxFlipCredit \\+= questReward"
---

<objective>
Author the BATCH-01/02 producer hunk — the FIRST edit of the v57.0 batched diff, against the frozen audited subject `1e7a646d`. `DegenerusQuests.handlePurchase` stops crediting `burnieMintReward` inline and instead folds it into the returned `totalReturned`, following the existing "return-don't-credit-inline" lootbox-reward convention; the sole ETH caller in `DegenerusGameMintModule` already folds the full return into `lootboxFlipCredit` (single credit), so the change is behavior-equivalent (same recipient, same amount, additive accumulator) and saves one cross-contract `creditFlip` per MINT_BURNIE-quest-completing buy. The misleading `ethMintReward`/"ETH mint reward" comments are corrected to quest-TYPE semantics.

This is the PRODUCER. The CONSUMER (the BURNIE coin caller in plan 02) nets this same returned reward against a deferred burn — so this plan MUST land first (producer-before-consumer, SPEC D-02 / D-23 / Handoff Invariant 4).

Purpose: behavior-equivalent gas win + naming correctness; establish the returned-reward contract the BURNIE coin caller (plan 02) consumes.
Output: a self-verified BATCH-01/02 hunk in two contract files, locally compiling at the post-features `forge build` (the build gate itself lands at the end of plan 03). NOT committed — held for the single batched USER hand-review at the end of plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Execution mode (HARD constraints — read before starting)
- **Sequential-on-main, worktrees OFF** (`use_worktrees: false`, `no_worktree_paths: [contracts]` — submodule + node_modules make worktrees unsafe). Author directly on the working tree.
- **CONTRACT-COMMIT BOUNDARY = the HARD STOP.** This plan is `autonomous: false`. Author the contract hunk but DO NOT commit `contracts/*.sol`. The single batched diff (all of plans 01–04) is committed only after explicit USER hand-review at the final gate in plan 04. (Project rule: only contract commits need approval; everything else runs hands-off.)
- **Code-comment style (USER rule):** comments explain HOW THE CODE WORKS + real invariants in plain English. CUT all procedural meta — NO plan IDs (359-xx / BATCH-xx), NO requirement tags, NO spec-line citations (358-SPEC.md:NNN), NO milestone history, NO "USER-accepted/LOCKED/by-design". The D-NN / anchor citations in this PLAN are for YOU, never for the authored Solidity. Never change logic when trimming a comment.
- **Frozen-subject guard:** `git diff --quiet 1e7a646d HEAD -- contracts/` is CLEAN at phase start — every cited `file:line` is read-equivalent to the audited baseline. Re-read each anchor before editing.
- Do NOT run `forge build` per-feature (SPEC D-03 = two checkpoints only: post-features at end of plan 03, post-UDVT in plan 04). This plan's `<verify>` is source-assertion + targeted grep, not a full compile.
</execution_context>

<context>
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-CONTEXT.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md

<interfaces>
<!-- The exact contract the producer changes + the consumer fold it must remain compatible with. Extracted from the frozen subject 1e7a646d. -->

contracts/DegenerusQuests.sol — handlePurchase signature (the 4-tuple stays unchanged; only what flows into `reward` changes):
  function handlePurchase(
      address player, uint256 ethMintSpendWei, uint32 burnieMintQty,
      uint256 lootBoxAmount, uint256 mintPrice, uint256 levelQuestPrice
  ) external onlyCoin
      returns (uint256 reward, uint8 questType, uint32 streak, bool completed)

contracts/DegenerusQuests.sol:942-955 — the routing block to change (verbatim @ 1e7a646d):
  // Reward routing:
  // - BURNIE mint rewards: credited here.
  // - Lootbox rewards: returned to the caller only ... NOT credited here.
  // - ETH mint rewards: returned to the caller.
  if (burnieMintReward != 0) { coinflip.creditFlip(player, burnieMintReward); }   // :947-949  ← REMOVE this inline credit
  uint256 totalReturned = ethMintReward + lootboxReward;                          // :951      ← FOLD burnieMintReward in here
  if (anyCompleted) { return (totalReturned, outQuestType, outStreak, true); }     // :953

contracts/modules/DegenerusGameMintModule.sol — the ETH caller fold (read-attest; this absorbs the now-larger return UNCHANGED):
  (uint256 questReward, uint8 questType, uint32 streak, bool questCompleted)
      = quests.handlePurchase(buyer, ethMintSpendWei, burnieMintUnits, lootBoxAmount, priceWei, ...);  // :1210-1217
  if (questCompleted) { lootboxFlipCredit += questReward; ... }                                         // :1220  fold
  ... if (lootboxFlipCredit != 0) coinflip.creditFlip(buyer, lootboxFlipCredit);                        // :1355  single credit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BATCH-01 — fold burnieMintReward into the return (remove the inline credit)</name>
  <files>contracts/DegenerusQuests.sol</files>
  <read_first>
    - contracts/DegenerusQuests.sol:815-826 (the handlePurchase signature + 4-tuple returns — unchanged) and :839-844 (the reward vars: ethMintReward / lootboxReward / burnieMintReward) and :942-955 (the routing block to change) — read the live code, the inline credit is at :947-949.
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — Cross-Cutting RNG-Freeze §(1) + Cross-Cutting SOLVENCY §(1) (BATCH is BURNIE-accounting only, ETH/pool debit byte-unchanged) + the BURNIE-03 D-23 co-design note (the BURNIE coin caller consumes this return).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §1 (BATCH-01/02 pattern: the existing lootbox-reward convention is "return, don't credit inline"; the producer-before-consumer note).
  </read_first>
  <action>
    In `contracts/DegenerusQuests.sol` at the reward-routing block (anchor :942-955, governing SPEC BATCH-01 / D-23 co-design): make `burnieMintReward` follow the EXISTING lootbox-reward convention (return, don't credit inline). DELETE the inline `if (burnieMintReward != 0) { coinflip.creditFlip(player, burnieMintReward); }` (:947-949). ADD `burnieMintReward` into the `totalReturned` sum at :951 so the returned `reward` carries `ethMintReward + lootboxReward + burnieMintReward`. The 4-tuple return signature (:825) and the `anyCompleted` return path (:953) and the no-completion fallback return are UNCHANGED. This is BURNIE-accounting only — touch NO ETH/`claimablePool` debit code. The recipient and amount are unchanged downstream because the sole ETH caller folds the full return into `lootboxFlipCredit` (one credit) — verify that read-equivalence in Task 3.
  </action>
  <verify>
    <automated>grep -c "coinflip.creditFlip(player, burnieMintReward)" contracts/DegenerusQuests.sol | grep -qx 0 && echo "PASS: inline burnie credit removed" || echo "FAIL: inline credit still present"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/DegenerusQuests.sol` no longer contains `coinflip.creditFlip(player, burnieMintReward)` (the :947-949 inline credit is gone).
    - The `totalReturned` expression now includes `burnieMintReward` (grep `totalReturned = ` shows the three-term sum including `burnieMintReward`).
    - The `handlePurchase` 4-tuple return signature `(uint256 reward, uint8 questType, uint32 streak, bool completed)` is byte-unchanged.
    - No ETH/`claimablePool` debit code is touched anywhere in the file (the change is confined to the reward-routing block).
  </acceptance_criteria>
  <done>burnieMintReward is RETURNED (folded into totalReturned), not credited inline; the inline creditFlip is deleted; the return signature is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BATCH-02 — correct the misleading ETH-mint-reward comments to quest-TYPE semantics</name>
  <files>contracts/DegenerusQuests.sol</files>
  <read_first>
    - contracts/DegenerusQuests.sol:942-946 + :950 (the misleading comments to rewrite — "ETH mint rewards: returned to the caller", "Return ETH mint reward + lootbox reward").
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §1 + §"Lean comments (project rule)" — strip plan IDs/tags; keep tight functional explanation.
    - The memory rule: MINT_ETH / LOOTBOX / MINT_BURNIE are quest TYPE constants; ALL quest rewards pay BURNIE flip stake — no payout is ETH.
  </read_first>
  <action>
    In `contracts/DegenerusQuests.sol`, rewrite the reward-routing comments (anchor :942-946 + :950, governing SPEC BATCH-02) so they reflect quest-TYPE semantics, NOT a literal ETH payout: clarify that `MINT_ETH`/`LOOTBOX`/`MINT_BURNIE` are quest-TYPE constants and EVERY quest reward is paid as a BURNIE flip stake — the prefix names the quest type, not the payout currency; all rewards (including the former "ETH mint reward") are now RETURNED to the caller and credited once via the caller's `lootboxFlipCredit` batch. Keep the comment tight and functional (HOW the routing works + the real invariant "all rewards returned, credited once by the caller"). Apply the lean-comment rule — no plan IDs, no req tags, no spec-line citations, no milestone history. Change NO logic.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' contracts/DegenerusQuests.sol | grep -c "ethMintReward" >/dev/null; echo "manual: read the routing comment block — confirm it no longer claims any reward is paid in ETH"</automated>
  </verify>
  <acceptance_criteria>
    - The reward-routing comment block no longer asserts that any reward is paid in ETH (no "ETH mint rewards: ... " currency claim).
    - The comment explains that all quest rewards are BURNIE flip stake, returned to the caller and credited once.
    - No procedural meta (plan IDs, req tags, spec-line citations, milestone history) in the comment.
    - Zero logic change — only comment text edited in this task.
  </acceptance_criteria>
  <done>The reward-routing comments reflect quest-TYPE semantics (all rewards are BURNIE flip stake, returned + credited once); no comment claims an ETH payout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Read-attest the ETH caller fold is behavior-equivalent under the larger return</name>
  <files>contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol:1205-1224 (the handlePurchase call + the `lootboxFlipCredit += questReward` fold at :1220) and :1354-1356 (the single credit `if (lootboxFlipCredit != 0) coinflip.creditFlip(buyer, lootboxFlipCredit)`).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §1 ("Caller fold" + "Producer-before-consumer note").
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md Full Call-Graph Grep-Attestation Block D (BATCH-01 caller fold :1220 / single credit :1355 confirmed).
  </read_first>
  <action>
    Read-attest (do NOT modify in this task) the ETH caller fold in `contracts/modules/DegenerusGameMintModule.sol` (anchor :1220 / :1355): confirm `questReward` (the full `handlePurchase` return) is folded into `lootboxFlipCredit` at :1220 and credited exactly once at :1355. Because the return now ALSO carries `burnieMintReward` (from Task 1), the caller folds the larger value with NO code change — same recipient (`buyer`), same total (additive accumulator), one credit. Confirm there is no SEPARATE inline credit of `burnieMintReward` on this ETH path (it never existed here — the inline credit lived only in `DegenerusQuests`). If — and ONLY if — the caller fold does not absorb the larger return cleanly (e.g. a stale separate handling of the burnie portion exists), record the exact discrepancy for the BURNIE coin-caller plan (02) and do not silently patch it here; otherwise leave `MintModule` byte-unchanged in this plan. This file is OWNED by plan 02 (BURNIE/SALVAGE) — do not author behavior here; this is a read-attestation only.
  </action>
  <verify>
    <automated>grep -c "lootboxFlipCredit += questReward" contracts/modules/DegenerusGameMintModule.sol; grep -c "coinflip.creditFlip(buyer, lootboxFlipCredit)" contracts/modules/DegenerusGameMintModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/modules/DegenerusGameMintModule.sol` contains the fold `lootboxFlipCredit += questReward` (:1220) and the single credit `coinflip.creditFlip(buyer, lootboxFlipCredit)` (:1355) — both present, unchanged.
    - No separate inline `creditFlip` of the burnie reward portion exists on the ETH caller path.
    - `MintModule.sol` is left byte-unchanged by THIS plan (any behavior edits belong to plan 02), OR a precise discrepancy note is recorded in the SUMMARY for plan 02.
  </acceptance_criteria>
  <done>The ETH caller fold is confirmed to absorb the now-larger return with no code change (same recipient/total/single-credit); MintModule untouched by this plan or the discrepancy is documented for plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onlyCoin → DegenerusQuests.handlePurchase | only the BurnieCoin contract may call; reward routing is internal accounting |
| DegenerusQuests → BurnieCoinflip.creditFlip | BURNIE flip-credit emission (off the ETH/claimablePool path) |

## STRIDE Threat Register (mapped to the SPEC's discharged register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-SEC01-BATCH | Tampering (RNG/freeze) | handlePurchase reward routing | accept | BATCH is BURNIE-accounting only; reads/derives NO rngWord; determinism byte-unchanged (same recipient/amount/additive accumulator). SPEC RNG-Freeze §(1). Empirical byte-diff owned at TST 361 SEC-01. |
| T-358-SEC02-BATCH | Information disclosure / loss-of-funds (solvency) | ETH/claimablePool debit | mitigate | The ETH/`claimablePool` DEBIT code is BYTE-UNCHANGED; the reroute (inline→folded) keeps the SAME amount/recipient → `claimablePool <= balance` unaffected. SPEC SOLVENCY §(1). Empirical at TST 361 SEC-02. |
| T-359-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (Solidity edits only); slopcheck N/A. |

Defer EMPIRICAL proof to TST 361 (SEC-01 byte-diff of the handlePurchase/caller fold; SEC-02 ETH/pool debit byte-identical) and the delta-audit at TERMINAL 362. Do not invent new mitigations — cite the SPEC's.
</threat_model>

<verification>
- `grep -c "coinflip.creditFlip(player, burnieMintReward)" contracts/DegenerusQuests.sol` == 0 (inline credit removed).
- `totalReturned` includes `burnieMintReward` (three-term sum).
- The `handlePurchase` 4-tuple return signature is byte-unchanged.
- The reward-routing comments no longer claim an ETH payout.
- The ETH caller fold (`MintModule:1220`/`:1355`) is read-attested unchanged (or a precise discrepancy is documented for plan 02).
- NOTE: the actual `forge build` green gate is in plan 03 (post-features) — do NOT run a per-feature compile here.
</verification>

<success_criteria>
- BATCH-01: `handlePurchase` returns `burnieMintReward` in `totalReturned` instead of crediting it inline; the inline `creditFlip(player, burnieMintReward)` is deleted.
- BATCH-02: the misleading ETH-mint-reward comments are corrected to quest-TYPE semantics (no payout is ETH).
- Behavior-equivalence: same recipient + same amount + additive accumulator on the ETH caller path (one `creditFlip` saved per MINT_BURNIE-quest-completing buy).
- The returned-reward contract is established for the BURNIE coin caller (plan 02) to consume (producer-before-consumer).
- No ETH/`claimablePool` debit code touched; no commit made (held for the plan-04 USER hand-review gate).
</success_criteria>

<output>
Create `.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-01-SUMMARY.md` when done. Record: the exact lines changed in `DegenerusQuests.sol`, the read-attestation result for the `MintModule` caller fold (and any discrepancy handed to plan 02), and an explicit note that NO commit was made (held for the plan-04 batched contract-commit hand-review gate).
</output>
