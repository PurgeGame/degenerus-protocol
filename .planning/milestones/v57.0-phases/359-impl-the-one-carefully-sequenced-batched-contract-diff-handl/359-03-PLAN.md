---
phase: 359-impl-the-one-carefully-sequenced-batched-contract-diff-handl
plan: 03
type: execute
wave: 3
depends_on: ["359-01", "359-02"]
files_modified:
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/DegenerusGame.sol
  - contracts/storage/DegenerusGameStorage.sol
autonomous: false
requirements: [WWXRP-01, TDEC-01, CANCEL-01]
must_haves:
  truths:
    - "A WWXRP-jackpot (s==9, currency 3, ≥MIN_BET_WWXRP) awards whalePassClaims[player] += 1, rationed one per level/10 bracket, credited to the bettor (never msg.sender)"
    - "A new boostTerminalDecimator() scales weightedBurn by the effective quest streak and PROMOTES the bucket (re-derive subBucket, re-key aggregate conserved), gated by require(!_livenessTriggered()), one-time per level via a boosted bit"
    - "Manual sub-cancel auto-claims self pendingBurnie + drains affiliateBase to the 75/20/5 tree BEFORE clear; auto-evict paths explicitly delete _subOf (pure forfeit)"
    - "The full features-first diff compiles: forge build is green (CHECKPOINT 1)"
  artifacts:
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "WWXRP whale-halfpass hook after the s>=7 ETH sDGNRS block"
      contains: "wwxrpJackpotWhalePassBracketAwarded"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "boostTerminalDecimator() weight+bucket+aggregate re-key under the !liveness gate"
      contains: "boostTerminalDecimator"
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "manual-cancel auto-claim (self + tree) + auto-evict explicit-delete forfeit"
      contains: "drainAffiliateBase"
    - path: "contracts/DegenerusGame.sol"
      provides: "the boostTerminalDecimator router/delegatecall stub"
      contains: "boostTerminalDecimator"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "wwxrpJackpotWhalePassBracketAwarded mapping + the boosted bit in TerminalDecEntry"
      contains: "wwxrpJackpotWhalePassBracketAwarded"
  key_links:
    - from: "_resolveFullTicketBet (s==9 WWXRP hook)"
      to: "whalePassClaims[player] += 1 + wwxrpJackpotWhalePassBracketAwarded[level/10] = true"
      via: "s==9 short-circuit first; gate on currency/min-bet/bracket-flag; credit the bettor player"
      pattern: "whalePassClaims\\[player\\]"
    - from: "boostTerminalDecimator()"
      to: "terminalDecBucketBurnTotal re-key (remove-old/add-new, conserved)"
      via: "recompute bucket from live activity score, re-derive subBucket, scale weightedBurn, saturate uint88"
      pattern: "terminalDecBucketBurnTotal"
    - from: "manual cancel (dailyQuantity==0)"
      to: "creditFlip(self pendingBurnie) + drainAffiliateBase → 75/20/5 tree → _finalizeAfking + clear"
      via: "pay self + drain tree BEFORE finalize/clear (CEI zero-first)"
      pattern: "drainAffiliateBase"
---

<objective>
Author the three CLEAN RNG-insensitive / BURNIE-emission items (any order among themselves; SPEC D-02 step 4), then land the FIRST of the two `forge build` BLOCKING checkpoints (post-features green build, SPEC D-03):

1. **WWXRP-01:** hook a small whale-halfpass award into the per-spin loop of `_resolveFullTicketBet`, immediately AFTER the ETH-only `s>=7` sDGNRS block; gate `s == 9 && currency == CURRENCY_WWXRP(3) && amountPerTicket >= MIN_BET_WWXRP && !wwxrpJackpotWhalePassBracketAwarded[level/10]` (the `s == 9` short-circuit FIRST), award `whalePassClaims[player] += 1` to the bettor + set the per-bracket flag; one new mapping appended to `DegenerusGameStorage`.
2. **TDEC-01:** a NEW `boostTerminalDecimator()` entrypoint on `DegenerusGameDecimatorModule` (+ a router stub on `DegenerusGame`) that, under `require(!_livenessTriggered())`, reads the effective quest streak via `getPlayerQuestView` (view only), recomputes the bucket from the live activity score (PROMOTE if strictly lower), re-derives the subBucket, scales `weightedBurn` by the boost factor (saturate uint88), and RE-KEYS the aggregate (remove-from-old / add-to-new, conserved); one-time per terminal level via a `boosted` bit added to the packed `TerminalDecEntry`.
3. **CANCEL-01:** manual sub-cancel auto-claims self `pendingBurnie` (CEI zero-first) + drains `affiliateBase` to the affiliate tree A/U1/U2 = 75/20/5 BEFORE `_finalizeAfking`+clear (closes the loss-on-reclaim race); the auto-evict paths (pass-expiry, funding-out) are made explicit pure FORFEITS via `delete _subOf[player]`.

Then: CHECKPOINT 1 — `forge build` is green with ALL 7 features authored (plans 01–03 hunks present, pre-UDVT).

Purpose: deliver the three clean features + prove the features-first block compiles before the wide UDVT sweep.
Output: the WWXRP/TDEC/CANCEL hunks + a green features-first `forge build`. NOT committed — held for the plan-04 batched USER hand-review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Execution mode (HARD constraints)
- **Sequential-on-main, worktrees OFF.** Author directly on the working tree, strictly AFTER plans 01–02 (this plan touches `DegenerusGameStorage.sol` and `DegenerusGame.sol`, both also touched by later plans — and the post-features build gate needs plans 01+02's hunks already present).
- **CONTRACT-COMMIT BOUNDARY = HARD STOP.** `autonomous: false`. Author the hunks; DO NOT commit `contracts/*.sol`. Held for the single batched USER hand-review at the plan-04 gate.
- **Code-comment style (USER rule):** comments explain HOW THE CODE WORKS + real invariants in plain English. CUT all procedural meta (plan IDs, req tags, spec-line citations, milestone history, "USER-accepted/LOCKED"). Fix the FALSE "claim whenever" cancel comment you touch. Never change logic when trimming.
- **Frozen-subject guard** clean at start; re-read each anchor before editing.
- **CHECKPOINT 1 = the ONLY `forge build` in this plan** (SPEC D-03 two-checkpoint cadence). Foundry compiles `test/*.t.sol` in the same build — at this PRE-UDVT checkpoint the test files still call the raw-int day signatures (no day-type change yet), so the build should be green WITHOUT any test-file churn. (The forge `.t.sol` day-signature churn lands in plan 04, with the UDVT sweep.)
</execution_context>

<context>
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-CONTEXT.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md

<interfaces>
<!-- The WWXRP award analog + storage append, the TDEC writer analog + gate + view + packed struct + router stub, and the CANCEL analogs. Extracted @ 1e7a646d. -->

DegenerusGameDegeneretteModule.sol — the per-spin chokepoint + the award analog:
  _resolveFullTicketBet:614 (the shared resolve chokepoint; player:615 = the bet OWNER from _resolvePlayer:142-150)
  s = _score(...):674 (S ∈ {0..9}); amountPerTicket decoded :624; loop over ticketCount :649
  ETH-only sDGNRS award (the hook goes IMMEDIATELY AFTER): if (currency == CURRENCY_ETH && s >= 7) { _awardDegeneretteDgnrs(player, amountPerTicket, s); }  // :713-715
  CURRENCY_WWXRP = 3 :216 ; MIN_BET_WWXRP = 1 ether :225 ; resolveBets:407 reverts on _livenessTriggered() :413
  currentLevel cached: uint24 currentLevel = level; :675   (bracket key = level / 10)

DegenerusGameStorage.sol:970-977 — the grant primitive + the append point:
  mapping(address => uint256) internal whalePassClaims;                     // :973  grant: whalePassClaims[player] += 1
  mapping(uint48 => mapping(address => uint256)) internal lootboxEthBase;   // :977
  // ← APPEND: mapping(uint256 => bool) internal wwxrpJackpotWhalePassBracketAwarded;  (keyed by level/10)

DegenerusGameDecimatorModule.sol — THE writer analog (recordTerminalDecBurn:693-767):
  uint8 bucket = _terminalDecBucket(bonusBps);            // :709  bucket from activity score (12→2, lower=better)
  if (e.bucket == 0) { e.bucket = bucket; e.subBucket = _decSubbucketFor(player, lvl, bucket); }  // :726-728
  if (newWeighted > type(uint88).max) newWeighted = type(uint88).max;  // :750-752  uint88 saturate
  bytes32 bucketKey = keccak256(abi.encode(lvl, e.bucket, e.subBucket)); // :755  AGGREGATE KEY (abi.encode)
  terminalDecBucketBurnTotal[bucketKey] += weightedAmount;               // :756
  helpers: _terminalDecBucket:925-936 (promote only if STRICTLY lower) · _decSubbucketFor:559-570 (re-derive on promotion) · _terminalDecDaysRemaining:939-950 · _terminalDecMultiplierBps:916-922

DegenerusGameStorage.sol:1585-1592 — the packed struct (232/256 bits; 24 spare → add a `boosted` bit):
  struct TerminalDecEntry { uint80 totalBurn; uint88 weightedBurn; uint8 bucket; uint8 subBucket; uint48 burnLevel; }
DegenerusGameStorage.sol:1231-1240 — _livenessTriggered() (DAY-CONSTANT predicate; the boost gate)
DegenerusQuests.sol:1088-1108 — getPlayerQuestView(player).baseStreak (the effective gap/shield-decayed streak; a VIEW)
DegenerusGame.sol:1275-1293 — the delegatecall router-stub pattern (mirror for boostTerminalDecimator)

GameAfkingModule.sol — CANCEL analogs:
  manual cancel branch :345-362 (FALSE comment :348-351; _finalizeAfking call :353; tombstone c.dailyQuantity=0 :354); subscribe rngLock gate :300
  claimAfkingBurnie:1560-1589 (CEI zero-first :1574: s.pendingBurnie = 0; then creditFlip(player, owed * 1 ether); presaleBoxCredit parity :1575-1582)
  drainAffiliateBase:1605-1610 (read-and-zero: base = s.affiliateBase; s.affiliateBase = 0)
  tombstone-reclaim (the GOOD forfeit): delete _subOf[player]:1148 + _removeFromSet:1149
  pass-expiry evict :1175-1187 (finalize + dailyQuantity=0 + _removeFromSet:1187, NO delete) — ADD delete _subOf
  funding-out evict :1240-1252 (finalize + dailyQuantity=0 + _removeFromSet:1246, NO delete) — ADD delete _subOf
DegenerusAffiliate.claim:629-682 — A/U1/U2 75/20/5 (A = referrer-upline via _referrerAddress:809; u1Share = (sumB-skipU1)*20/100 :679; u2Share = (sumB-skipU2)*5/100 :680; remainder→A :681; no-referrer 50/50 VAULT/DGNRS :669-676)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WWXRP-01 — jackpot whale-halfpass hook + storage append</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol:614-619 (the chokepoint signature), :673-715 (score `s:674` + the ETH-only `s>=7` award block at :713-715), :142-150 (`_resolvePlayer` — returns the bet OWNER), :216 + :225 (CURRENCY_WWXRP=3, MIN_BET_WWXRP=1 ether), :1135-1158 (`_awardDegeneretteDgnrs` — the credits-`player` proof-by-example), :407/:413 (resolveBets liveness revert), :675 (`uint24 currentLevel = level`).
    - contracts/storage/DegenerusGameStorage.sol:965-977 (the append region after `whalePassClaims:973` / `lootboxEthBase:977`).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — WWXRP-02 §D-14 (per-bracket rationing key `level/10`), §D-15 (multi-bracket allow), §D-16 (recipient = bettor `player`), §D-17 (hook + gate, `s==9` first), §D-18 (freeze-safe/pre-liveness/SOLVENCY-neutral) + IMPL Handoff Invariants 1 + 2.
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §4 (the full WWXRP pattern + the flagged `:713-715` drift confirmation) + the "Always-credit-the-OWNER" shared pattern.
  </read_first>
  <action>
    In `contracts/storage/DegenerusGameStorage.sol` (anchor :970-977): APPEND a new mapping after `lootboxEthBase:977` — `mapping(uint256 => bool) internal wwxrpJackpotWhalePassBracketAwarded;` keyed by `level/10` (a clean append — no existing WWXRP state; do NOT reorder existing slots). Governing SPEC D-14.
    In `contracts/modules/DegenerusGameDegeneretteModule.sol` `_resolveFullTicketBet` (anchor: immediately AFTER the ETH-only `s>=7` sDGNRS block at :713-715; governing SPEC D-17 / Handoff Invariants 1 + 2): add the whale-halfpass hook. Gate on `s == 9 && currency == CURRENCY_WWXRP && amountPerTicket >= MIN_BET_WWXRP && !wwxrpJackpotWhalePassBracketAwarded[currentLevel / 10]` — the `s == 9` jackpot check MUST short-circuit FIRST so non-jackpot spins read no new state (zero added cost on the common path). On a hit: `whalePassClaims[player] += 1;` (credit the BETTOR `player` — the owner from `_resolvePlayer`, NEVER `msg.sender`), `wwxrpJackpotWhalePassBracketAwarded[currentLevel / 10] = true;`, and emit a small event. Reuse the `claimWhalePass` future-ticket deferral — touch NO ETH/`claimablePool`. Use the cached `currentLevel` (:675) for the bracket key. Keep comments lean + functional. Do NOT change any router stub (`resolveDegeneretteBets:902`/`degeneretteResolve:1742`/`_degeneretteResolveBet:1900` all funnel here) and do NOT change the liveness revert (:413).
  </action>
  <verify>
    <automated>grep -c "wwxrpJackpotWhalePassBracketAwarded" contracts/storage/DegenerusGameStorage.sol; grep -c "wwxrpJackpotWhalePassBracketAwarded" contracts/modules/DegenerusGameDegeneretteModule.sol; grep -c "whalePassClaims\[player\] += 1" contracts/modules/DegenerusGameDegeneretteModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `DegenerusGameStorage.sol` contains the appended `mapping(uint256 => bool) internal wwxrpJackpotWhalePassBracketAwarded;` AFTER `lootboxEthBase:977` (no existing slots reordered).
    - The hook sits immediately after the ETH-only `s>=7` block; the gate's FIRST conjunct is `s == 9` (short-circuit); the full gate also requires `currency == CURRENCY_WWXRP`, `amountPerTicket >= MIN_BET_WWXRP`, and `!wwxrpJackpotWhalePassBracketAwarded[currentLevel/10]`.
    - On a hit it does `whalePassClaims[player] += 1` (credits `player`, NOT `msg.sender`) and `wwxrpJackpotWhalePassBracketAwarded[currentLevel/10] = true`.
    - No ETH/`claimablePool` touch; no router stub or liveness-revert change.
  </acceptance_criteria>
  <done>A WWXRP jackpot (s==9, currency 3, ≥MIN_BET_WWXRP) awards one whale halfpass to the bettor, rationed one per level/10 bracket; zero added cost on non-jackpot spins.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TDEC-01 — boostTerminalDecimator() entrypoint + router stub + boosted bit</name>
  <files>contracts/modules/DegenerusGameDecimatorModule.sol, contracts/DegenerusGame.sol, contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDecimatorModule.sol:693-767 (THE analog `recordTerminalDecBurn` — bucket from activity score :709, first-burn freeze :726-728, uint88 saturate :750-752, aggregate key :755 / add :756), :559-570 (`_decSubbucketFor`), :912-950 (`_terminalDecMultiplierBps:916` / `_terminalDecBucket:925-936` / `_terminalDecDaysRemaining:939-950`).
    - contracts/storage/DegenerusGameStorage.sol:1585-1592 (the packed `TerminalDecEntry`, 232/256 bits, 24 spare) + :1231-1240 (`_livenessTriggered` — the gate).
    - contracts/DegenerusQuests.sol:1088-1108 (`getPlayerQuestView(player).baseStreak` — the effective streak VIEW, no mutation).
    - contracts/DegenerusGame.sol:1275-1293 (the delegatecall router-stub pattern to mirror).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — TDEC-02 §D-04 (last-day window) / §D-05 (bucket promotion if strictly lower) / §D-06 (forced subBucket re-derive on promotion) / §D-07 (aggregate re-key, remove-old/add-new, conserved) / §D-08 (weight scaling curve — anchors 100→20×, 10→4×, 1× floor; constants Claude's discretion) / §D-09 (effective-streak source) / §D-10 (keep both levers) / §D-11 (saturate uint88) / §D-12 (shields read-only) / §D-13 (boosted bit + existing-entry prerequisite); TDEC-03 the freeze-safety proof + Step 8 IMPL Handoff Invariants 1–5 (the gate is EXACTLY `require(!_livenessTriggered())`, NOT `!gameOver`).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §5 (the full TDEC pattern, all helper anchors, the entrypoint shape) + the "Delegatecall router stub" + "Day-constant freeze gate" + "uint88 saturation" shared patterns.
  </read_first>
  <action>
    In `contracts/storage/DegenerusGameStorage.sol` (anchor :1585-1592, governing SPEC D-13): add a `boosted` bit (a `bool` / `uint8`) to the packed `TerminalDecEntry` using the 24 spare bits — same slot, no new cold slot.
    In `contracts/modules/DegenerusGameDecimatorModule.sol`: author a NEW `boostTerminalDecimator()` entrypoint (governing SPEC TDEC-02 D-04..D-13 + TDEC-03 Step 8 Handoff Invariants 1–5; mirror the writer analog `recordTerminalDecBurn:693-767`). Preconditions: gate EXACTLY `require(!_livenessTriggered())` (NOT `!gameOver`); REQUIRE an existing `TerminalDecEntry` for the current terminal level (you scale committed weight, never buy an entry; consistent with the lazy-reset on stale `burnLevel:716-723`); require `boosted` is unset (one-time per level — set it at the end); validate a live effective streak. Within the last-day window (`_terminalDecDaysRemaining:939-950`; the exact `==0` vs `<=1` threshold is YOUR discretion within the locked window). Read the effective streak via `getPlayerQuestView(player).baseStreak` (a VIEW — no mutation, no shield consume; D-09/D-12). Recompute the bucket from the LIVE activity score via `_terminalDecBucket:925-936`; PROMOTE only if STRICTLY lower than the frozen bucket (D-05). On promotion: re-derive `subBucket` via `_decSubbucketFor:559-570` (D-06) AND RE-KEY the aggregate — REMOVE the pre-boost weighted contribution from `terminalDecBucketBurnTotal[keccak256(abi.encode(lvl, oldBucket, oldSub))]` and ADD the post-boost contribution to the new key (D-07, net conservation). Scale `weightedBurn` by `boostFactor(effectiveStreak)` (anchors streak 100→20×, 10→4×, 1× floor; the candidate two-line bps curve in SPEC D-08 is the TARGET — exact constants are YOUR discretion). SATURATE at `type(uint88).max` (D-11, mirror :750-752). The promotion + subBucket re-derive + re-key + weight scale are a SINGLE ATOMIC in-tx mutation under the gate (Handoff Invariant 2). Set `boosted = true` last. The ETH/BURNIE payout path is UNTOUCHED (weight-only).
    In `contracts/DegenerusGame.sol` (anchor :1275-1293, governing the router-stub pattern): add a `boostTerminalDecimator` delegatecall router stub mirroring `recordTerminalDecBurn:1275` (`delegatecall` GAME_DECIMATOR_MODULE + `abi.encodeWithSelector(IDegenerusGameDecimatorModule.boostTerminalDecimator.selector, ...)` + `_revertDelegate`); add the selector to the module interface as needed. Keep comments lean + functional (the gate rationale + the conservation invariant in plain English — NO spec citations).
  </action>
  <verify>
    <automated>grep -c "boostTerminalDecimator" contracts/modules/DegenerusGameDecimatorModule.sol; grep -c "boostTerminalDecimator" contracts/DegenerusGame.sol; grep -c "require(!_livenessTriggered())\|!_livenessTriggered" contracts/modules/DegenerusGameDecimatorModule.sol; grep -c "boosted" contracts/storage/DegenerusGameStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - `DegenerusGameDecimatorModule.sol` defines `boostTerminalDecimator()` gated EXACTLY by `require(!_livenessTriggered())` (NOT `!gameOver`), requiring an existing entry for the current level + the `boosted` bit unset + a live effective streak from `getPlayerQuestView`.
    - On a strictly-lower live bucket it PROMOTES: re-derives `subBucket` via `_decSubbucketFor` AND re-keys `terminalDecBucketBurnTotal` (remove-from-old + add-to-new, conserved).
    - `weightedBurn` is scaled by the boost factor and saturated at `type(uint88).max`; `boosted` is set true (one-time per level); the effective-streak read is a VIEW (no mutation/shield consume).
    - `DegenerusGame.sol` has a `boostTerminalDecimator` delegatecall router stub mirroring the `recordTerminalDecBurn:1275` pattern; `TerminalDecEntry` gains a `boosted` bit in the existing 24 spare bits (no new cold slot).
    - The ETH/BURNIE payout path is byte-untouched (weight-only change).
  </acceptance_criteria>
  <done>boostTerminalDecimator() scales weightedBurn + promotes the bucket (re-derive subBucket, conserved re-key) under !liveness, one-time per level; the router stub + boosted bit are in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CANCEL-01 — manual-cancel auto-claim (self + tree) + auto-evict explicit-delete forfeit</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/modules/GameAfkingModule.sol:340-363 (the manual-cancel branch; FALSE comment :348-351; `_finalizeAfking` call :353; tombstone :354), :300 (subscribe rngLock gate), :1026 (`_finalizeAfking`), :1144-1156 (the tombstone-reclaim — the GOOD forfeit, `delete _subOf:1148`), :1175-1191 (pass-expiry evict — finalize + dailyQuantity=0 + `_removeFromSet:1187`, NO delete), :1240-1252 (funding-out evict — same shape, `_removeFromSet:1246`, NO delete), :1560-1589 (`claimAfkingBurnie` CEI self-pay, zero-first :1574, presale-box parity :1575-1582), :1605-1610 (`drainAffiliateBase` read-and-zero).
    - contracts/DegenerusAffiliate.sol:629-682 (the A/U1/U2 75/20/5 settle; A = referrer-upline; `_referrerAddress:809`; no-referrer 50/50 VAULT/DGNRS :669-676).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — CANCEL-02 §D-30 (the latent loss bug + the FALSE comment), §D-31 (the manual-cancel auto-claim order: pay self + drain tree BEFORE finalize+clear), §D-32 (auto-evict = pure forfeit, explicit `delete _subOf`), §D-33 (the clean freeze/solvency posture) + IMPL Handoff Invariants 7 + 8.
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md §6 (the full CANCEL pattern + the flagged evict-span drift confirmations) + the "CEI zero-first BURNIE credit" shared pattern.
  </read_first>
  <action>
    In `contracts/modules/GameAfkingModule.sol`:
    (CANCEL-01 manual cancel, anchor :345-362, governing SPEC D-31 / Handoff Invariant 7) — in the `if (dailyQuantity == 0)` manual-cancel branch, BEFORE the existing `_finalizeAfking` (:353) + tombstone (:354): (a) pay the canceller's `pendingBurnie` to THEMSELVES via `coinflip.creditFlip` mirroring `claimAfkingBurnie:1560` — CEI zero-first (`s.pendingBurnie = 0;` THEN `creditFlip(player, owed * 1 ether)`), with the `presaleBoxCredit` grant while `!presaleOver` (the presale-box parity at :1575-1582); (b) drain `affiliateBase` and settle the affiliate tree A/U1/U2 = 75/20/5 — mirror `drainAffiliateBase:1605` (read-and-zero `base = s.affiliateBase; s.affiliateBase = 0`) feeding the `DegenerusAffiliate.claim:629` split, where A = the canceller's REFERRER-upline (resolved via `_referrerAddress:809` — the base is owed UPLINE, NOT to the canceller), `u1Share=(sumB-skipU1)*20/100`, `u2Share=(sumB-skipU2)*5/100`, remainder→A; no-referrer ⇒ 50/50 VAULT/DGNRS; deterministic, no extra roll; (c) THEN `_finalizeAfking` + clear. Order matters: claim self + drain-to-tree, THEN finalize+clear. Replace the FALSE "claim whenever" comment (:348-351) with a correct, lean comment (the auto-claim closes the reclaim race).
    (CANCEL-01 auto-evict forfeit, anchors :1185-1188 pass-expiry + :1244-1247 funding-out, governing SPEC D-32 / Handoff Invariant 8) — make the forfeit EXPLICIT: in BOTH evict paths that currently do `_finalizeAfking` + `sub.dailyQuantity = 0` + `_removeFromSet` but leave `_subOf` claimable out-of-set, ADD `delete _subOf[player];` (mirroring the tombstone-reclaim path at :1148) so both accumulators (`pendingBurnie` + `affiliateBase`) are wiped → truly unclaimable post-evict, no payout to self OR uplines. Keep comments lean + functional. Reads no `rngWord`; touches no ETH/`claimablePool` (the clean posture).
  </action>
  <verify>
    <automated>grep -c "drainAffiliateBase\|affiliateBase = 0" contracts/modules/GameAfkingModule.sol; grep -c "delete _subOf\[player\]" contracts/modules/GameAfkingModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - The manual-cancel branch pays self `pendingBurnie` (CEI zero-first, with presale-box parity) AND drains `affiliateBase` to the 75/20/5 tree (A = referrer-upline) BEFORE `_finalizeAfking` + clear.
    - The FALSE "claim whenever" comment (:348-351) is replaced with a correct lean comment.
    - Both auto-evict paths (pass-expiry + funding-out) now `delete _subOf[player]` (grep shows `delete _subOf[player]` count increased from 1 to 3 — the original tombstone-reclaim plus the two evict paths).
    - The auto-evict pays nothing to self or uplines (pure forfeit).
    - Reads no `rngWord`; no ETH/`claimablePool` touch.
  </acceptance_criteria>
  <done>Manual cancel auto-claims self + tree before clear (loss-race closed); both auto-evict paths explicitly delete _subOf (pure forfeit).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT 1 — post-features forge build green [BLOCKING]</name>
  <action>Run the post-features `forge build` (SPEC D-03 checkpoint 1) and confirm it exits 0 with all 7 features authored (plans 01-03), pre-UDVT, WITHOUT any test-file day-signature churn; confirm the contracts/ delta vs `1e7a646d` is only the 7 feature files and that no contract commit was made. This is a verification gate, not a code-authoring task — see how-to-verify for the steps.</action>
  <what-built>
    All 7 v57.0 behavior features are now authored across plans 01–03 (BATCH-01/02, BURNIE-01/02, SALVAGE-01, WWXRP-01, TDEC-01, CANCEL-01) against the frozen audited subject `1e7a646d` — pre-UDVT. This is the FIRST of the two SPEC D-03 `forge build` checkpoints.
  </what-built>
  <how-to-verify>
    1. Run the post-features compile: `forge build`. It MUST exit 0 (green). Foundry compiles `test/*.t.sol` in the same build — at THIS pre-UDVT checkpoint the test files still use the raw-int day signatures (no day-type change yet), so the build should pass WITHOUT any test-file churn. If a `.t.sol` fails to compile here, it is a FEATURE signature break (e.g. a new entrypoint selector or event the tests reference) — fix the contract hunk, NOT by editing tests (the test day-signature churn is plan 04's job).
    2. Confirm the frozen-subject delta is ONLY the 7 features so far: `git diff --stat 1e7a646d -- contracts/` should show exactly the plans-01–03 files (DegenerusQuests, DegenerusGameMintModule, DegenerusGameMintStreakUtils, DegenerusGameDegeneretteModule, DegenerusGameDecimatorModule, GameAfkingModule, DegenerusGame, DegenerusGameStorage) and NO day-type/UDVT churn yet.
    3. Confirm NO commit was made — `git status` shows the contract files modified but unstaged/uncommitted (the contract-commit boundary HARD STOP — the single batched diff is reviewed + committed only at the plan-04 final gate).
  </how-to-verify>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5; echo "EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (green) with all 7 features authored, pre-UDVT, WITHOUT any test-file day-signature churn.
    - `git diff --stat 1e7a646d -- contracts/` shows only the plans-01–03 feature files (no UDVT/day-type churn yet).
    - No `contracts/*.sol` commit has been made.
  </acceptance_criteria>
  <resume-signal>Type "approved" once `forge build` is green post-features, or describe the compile error so the feature hunk (not the tests) can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| permissionless resolveBets → _resolveFullTicketBet (WWXRP award) | anyone can trigger resolve; the award always credits the bet OWNER (player), never the operator/msg.sender |
| player → boostTerminalDecimator() (router → delegatecall) | a new player-initiated entrypoint; weight+bucket mutation must precede the resolution word |
| subscribe(dailyQuantity=0) / advance-driven evict → GameAfkingModule (BURNIE-emission) | cancel/evict accumulator settlement; off the ETH/claimablePool path |

## STRIDE Threat Register (mapped to the SPEC's discharged register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-07 | Spoofing / operator-redirect | WWXRP award recipient | mitigate | The award credits `whalePassClaims[player]` (the OWNER from `_resolvePlayer:142-150`); `msg.sender`/operator is out of scope at the award site (closed by construction). SPEC WWXRP D-16. |
| T-358-WWXRP-FREEZE | Tampering (RNG/freeze) | WWXRP award | accept | Writes only an RNG-INSENSITIVE counter/flag gated by the already-committed `s==9` (deterministic from the committed daily rngWord); pre-liveness only (:413). SPEC RNG-Freeze §(2). |
| T-358-01 | Tampering (RNG/freeze) | TDEC boost / bucket-promotion timing | mitigate | Gate EXACTLY `require(!_livenessTriggered())` (day-constant); the future-day-word lemma — all weight+bucket+subBucket mutation precedes the resolution word (disjoint windows). SPEC TDEC-03 Steps 1–3. Adversarial probe @ TERMINAL 362. |
| T-358-03 | Tampering (solvency-of-shares) | TDEC aggregate re-key | mitigate | Re-key REMOVES the pre-boost contribution from the old key + ADDS the identical amount to the new key → `terminalDecBucketBurnTotal` total conserved → pro-rata shares still sum to the pool. SPEC TDEC-02 D-07 / TDEC-03 Step 6. @ TST 361 SEC-02. |
| T-358-13 | Loss-of-funds (race) | CANCEL loss-on-reclaim | mitigate | Manual cancel auto-claims self + drains the tree BEFORE clear → the reclaim race is closed; CEI zero-first. SPEC CANCEL D-30/D-31. @ TST 361 CANCEL-03. |
| T-358-14 | Information disclosure (out-of-set residue) | CANCEL auto-evict residue | mitigate | Both evict paths `delete _subOf[player]` → no claimable out-of-set residue (pure forfeit). SPEC CANCEL D-32. @ TST 361 CANCEL-03. |
| T-358-CANCEL-FREEZE | Tampering (RNG/freeze) | cancel/evict paths | accept | Read no `rngWord`; cancel is `rngLock`-gated at the subscribe entry (:300); BURNIE-emission only (off the ETH path). SPEC RNG-Freeze §(8) / SOLVENCY §(8). |
| T-359-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (Solidity edits + a forge build only). |

Defer EMPIRICAL proof to TST 361 (SEC-01 determinism / WWXRP-grant / TDEC bucket-promotion determinism; SEC-02 aggregate-weight conservation + no-ETH-touch; CANCEL-03 loss-race + forfeit) and the adversarial bucket-promotion probe at TERMINAL 362. Do not invent new mitigations — cite the SPEC's.
</threat_model>

<verification>
- WWXRP: the mapping is appended in storage; the hook is after the `s>=7` block with `s==9` short-circuit first; `whalePassClaims[player] += 1` + the bracket flag set; no ETH touch.
- TDEC: `boostTerminalDecimator()` gated by `require(!_livenessTriggered())`; promotes (re-derive subBucket + conserved re-key); saturates uint88; `boosted` bit one-time; router stub present; view-only streak read.
- CANCEL: manual cancel pays self + drains tree BEFORE clear; both evict paths `delete _subOf[player]`; FALSE comment fixed.
- CHECKPOINT 1: `forge build` exits 0 post-features (pre-UDVT, no test churn); only the 7 feature files in the contracts/ delta; no commit made.
</verification>

<success_criteria>
- WWXRP-01: per-bracket rationed whale-halfpass award to the bettor; RNG-insensitive; zero added cost on non-jackpot spins.
- TDEC-01: a freeze-safe (`!_livenessTriggered()` gate), conservation-preserving boost+promotion entrypoint, one-time per level.
- CANCEL-01: the loss-on-reclaim race closed (auto-claim self + tree) + the auto-evict forfeit made explicit.
- CHECKPOINT 1: features-first `forge build` is green (SPEC D-03 first checkpoint).
- No commit made (held for the plan-04 USER hand-review gate).
</success_criteria>

<output>
Create `.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-03-SUMMARY.md` when done. Record: the exact storage append + hook site for WWXRP; the boostTerminalDecimator() shape (gate, promotion/re-key, boost-factor constants chosen, the daysRemaining threshold chosen) + router stub + boosted bit; the CANCEL auto-claim order + the two evict explicit-deletes; the post-features `forge build` result (CHECKPOINT 1 green); and an explicit note that NO commit was made (held for the plan-04 batched contract-commit hand-review gate).
</output>
