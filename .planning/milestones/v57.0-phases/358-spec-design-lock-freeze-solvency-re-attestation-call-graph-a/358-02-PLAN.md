---
phase: 358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a
plan: 02
type: execute
wave: 2
depends_on: ["358-01"]
files_modified:
  - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
autonomous: true
requirements: [WWXRP-02, BURNIE-03, SALVAGE-02, CANCEL-02]
must_haves:
  truths:
    - "358-SPEC.md locks WWXRP-02 (D-14..D-18): GLOBAL-per-bracket rationing keyed level/10 via new mapping wwxrpJackpotWhalePassBracketAwarded (NOT a 0->5 lifetime cap, D-14); multi-bracket allow (D-15); recipient = bettor `player` via _resolvePlayer (D-16); hook after the s>=7 ETH sDGNRS block with gate s==9 && currency==CURRENCY_WWXRP(3) && amountPerTicket>=MIN_BET_WWXRP(1 ether) && !awarded[level/10], award whalePassClaims[player]+=1 (D-17); freeze-safe RNG-insensitive counter, pre-liveness, SOLVENCY-neutral (D-18)"
    - "358-SPEC.md locks BURNIE-03 (D-21..D-24): the verified bug (_purchaseCoinFor :887-907 discards all four returns -> burns coin but queues ZERO tickets; root cause phase-160 24f0898b orphaned the coin path, D-21); the queue-on-return fix (capture returns, if adjustedQty!=0 _queueTicketsScaled(buyer,targetLevel,adjustedQty,false), D-22); the MINT_BURNIE burn-rebate (full-cost affordability gate upfront, deferred net burn = full coinCost - reward floored at 0, MINT_BURNIE-leg-only handlePurchase ethMintSpendWei=0/lootBoxAmount=0 skip score/affiliate/non-mint, co-designed with BATCH-01 return-instead-of-credit, D-23); freeze/solvency framing (purchaseCoin reads no rngWord; ETH/pool DEBIT byte-unchanged but RESTORES ticket claims = posture widening flagged in SEC-02, D-24)"
    - "358-SPEC.md locks SALVAGE-02 (D-25..D-29): current structure (sellFarFutureTickets :2074->MintModule:929, quote _quoteFarFutureSwap :145-190 prior-day seed/jitter 70-110%/ticketShareBps 40-80%, ETH relabel :975-977, _ethToBurnieValue :1657, D-25); the cash-leg split into ETH+BURNIE TARGET full-range [0..cashWei] from a new seed slice no-VRF, ethCap=existing cash<=60% (D-26); the BURNIE leg paid from sDGNRS-OWNED BURNIE (balance + claimable coinflip stake) TRANSFERRED not creditFlip-minted, actualBurnie=min(target,available), remainder+zero-case=ETH fallback (D-26); the pawn-shop safety model = total-payout-cap + eth-%-cap, NOT value-neutral, every reachable offer <= cap (D-27); freeze framing settled-prior-day-word no-new-VRF (D-28); solvency-positive no-new-emission + the no-arb re-proof obligation extend SWAP08 (D-29)"
    - "358-SPEC.md locks CANCEL-02 (D-30..D-33): the latent loss bug (manual cancel :345-362 tombstones + leaves pendingBurnie/affiliateBase; next processSubscriberStage reclaim delete _subOf :1144 wipes both; the :348-351 'claim whenever' comment is FALSE, D-30); manual-cancel auto-claim BEFORE clearing (pay pendingBurnie via creditFlip+presale-box parity mirror claimAfkingBurnie:1560 CEI-zero-first; drain affiliateBase + settle tree A/U1/U2 75/20/5 mirror DegenerusAffiliate.claim:629 via drainAffiliateBase:1605 A=referrer-upline; then _finalizeAfking+clear, D-31); auto-evict = pure FORFEIT delete both accumulators explicit-wipe _subOf no payout (D-32); freeze/solvency BURNIE-emission only no rngWord no ETH touch rngLock-gated CEI (D-33)"
    - "358-SPEC.md WWXRP/BURNIE/SALVAGE/CANCEL sections cite every anchor at the verified 1e7a646d line with drift corrected; the new WWXRP storage shape + the BURNIE producer-before-consumer sequencing + the SALVAGE sDGNRS-owned-BURNIE source primitive + the CANCEL explicit-delete forfeit are each fixed"
  artifacts:
    - path: ".planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md"
      provides: "Appends the four small-feature design-locks (WWXRP-02, BURNIE-03, SALVAGE-02, CANCEL-02) to the TDEC core authored by plan 01"
      contains: "BURNIE-03 — Coin-Buy Ticket-Queue Critical Fix"
      min_lines: 280
  key_links:
    - from: "358-SPEC.md BURNIE-03 section"
      to: "DegenerusGameMintModule.sol:887-907 (_purchaseCoinFor discards returns) + :1545-1554 (payInCoin burns no queue) + _queueTicketsScaled callers"
      via: "queue-on-return fix + MINT_BURNIE burn-rebate co-designed with BATCH-01"
      pattern: "_purchaseCoinFor|_queueTicketsScaled|burn.?rebate"
    - from: "358-SPEC.md SALVAGE-02 section"
      to: "DegenerusGameMintModule.sol:975-977 (SDGNRS ETH relabel) + BurnieCoinflip.sol:908/:366 (sDGNRS-stake-consume) + BurnieCoin.sol transfer"
      via: "split cash leg, BURNIE from sDGNRS-owned transfer not mint, actualBurnie=min(target,available), ETH fallback, no-arb cap"
      pattern: "sDGNRS-owned|actualBurnie|no-?arb|ethCap"
    - from: "358-SPEC.md CANCEL-02 section"
      to: "GameAfkingModule.sol:345-362 (manual cancel) + :1144 (reclaim delete _subOf) + drainAffiliateBase:1605 + claimAfkingBurnie:1560"
      via: "auto-claim self + tree 75/20/5 before clear; auto-evict explicit-wipe forfeit"
      pattern: "75/20/5|drainAffiliateBase|forfeit|pendingBurnie"
---

<objective>
Append the four small-feature design-locks to `358-SPEC.md`: the WWXRP whale-halfpass (WWXRP-02 / D-14..D-18), the BURNIE coin-buy ticket-queue Critical fix (BURNIE-03 / D-21..D-24, the highest-severity item), the sDGNRS salvage-swap combo ETH/BURNIE pawn-shop payout (SALVAGE-02 / D-25..D-29, the ETH/solvency + RNG-reading item), and the manual-cancel auto-claim + auto-evict pure-forfeit (CANCEL-02 / D-30..D-33, the latent-loss-bug fix). Two of these (BURNIE, SALVAGE) are the functional solvency-posture exceptions and must flag the posture widening; the other two (WWXRP, CANCEL) are RNG-insensitive / BURNIE-emission-only and stay in the clean freeze/solvency posture.

Purpose: lock all four designs IMPL-ready so Phase 359 authors the reconciled diff with zero "by construction" assumptions — the BURNIE producer-before-consumer sequencing (reward known before the net burn), the SALVAGE sDGNRS-owned-BURNIE source primitive + the no-arb re-proof obligation, the WWXRP new per-bracket storage shape, and the CANCEL explicit-delete forfeit are each fixed here.

Output: `358-SPEC.md` extended with `## WWXRP-02`, `## BURNIE-03`, `## SALVAGE-02`, `## CANCEL-02`. PAPER-ONLY — ZERO `contracts/*.sol` mutation. Depends on plan 01 (the TDEC core + header must exist first; this plan appends to the same file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md

<frozen_subject>
Baseline = v56.0 closure HEAD, frozen contract subject `1e7a646d`. Working-tree `contracts/` is byte-identical to `1e7a646d` — confirm `git diff --quiet 1e7a646d HEAD -- contracts/` (clean). Every cited `file:line` MUST be grep-re-attested before being written into the SPEC. Anchors readable from the working tree or via `git show 1e7a646d:<path>`.
</frozen_subject>

<key_anchors>
The CONTEXT.md "Source anchors" lists are canonical. Load-bearing anchors per feature (verified present at `1e7a646d` during planning; verify the rest at execute):

WWXRP (D-14..D-18):
- `contracts/modules/DegenerusGameDegeneretteModule.sol` — `_resolveFullTicketBet:614`, score `s` `:674`, the ETH-only sDGNRS `s>=7` block (verified at `:710-715`, gated `currency == CURRENCY_ETH && s >= 7`), `CURRENCY_WWXRP=3` `:216`, `MIN_BET_WWXRP=1 ether` `:225`, `resolveBets:407` (reverts on `_livenessTriggered()` `:414`), `_resolvePlayer:142-150` (validates `operatorApprovals`, returns owner).
- `contracts/DegenerusGame.sol` — resolve router stubs `resolveDegeneretteBets:902`, `degeneretteResolve:1742`, `_degeneretteResolveBet:1900`.
- `contracts/storage/DegenerusGameStorage.sol` — `whalePassClaims:973`, `lootboxEthBase:977` (the WWXRP-state insertion neighborhood ~:977), no existing WWXRP state. Direct grant `whalePassClaims[player] += 1` (conversion `DegenerusGamePayoutUtils.sol:44-61`; materialize via `claimWhalePass`).

BURNIE (D-21..D-24):
- `contracts/modules/DegenerusGameMintModule.sol` — `purchaseCoin:880`, `_purchaseCoinFor:887-907` (VERIFIED at `1e7a646d`: calls `_callTicketPurchase(..., true [payInCoin], ...)` and DISCARDS the returns), the `payInCoin` branch `:1545-1554` (`_coinReceive`→`coin.burnCoin`, no queue), the ETH-path queue caller `_purchaseForWith:1251`, `_ethToBurnieValue:1657`, `quests.handlePurchase` call `:1210-1217`.
- `contracts/DegenerusGame.sol` — `purchaseCoin` router stub `:660`; `contracts/DegenerusVault.sol` — `gamePurchaseTicketsBurnie:571-574` (live consumer); `contracts/storage/DegenerusGameStorage.sol` — `_queueTicketsScaled` (other callers `GameAfkingModule:800`, `DegenerusGame:226`); `contracts/BurnieCoinflip.sol` — `creditFlip:859`. Root-cause commit `24f0898b` (phase 160).
- BATCH-01 co-design anchor (verified at `1e7a646d`): the inline `coinflip.creditFlip(player, burnieMintReward)` `contracts/DegenerusQuests.sol:947-949`; caller fold `contracts/DegenerusGameMintModule.sol:1220` (return-fold) + `:1355` (single `lootboxFlipCredit`).

SALVAGE (D-25..D-29):
- `contracts/DegenerusGame.sol` — `sellFarFutureTickets:2074`, `mintPrice:2539`. `contracts/modules/DegenerusGameMintModule.sol` — `sellFarFutureTickets:929`, the SDGNRS relabel (verified `:975-977`: `claimableWinnings[SDGNRS] -= totalBudget; claimableWinnings[player] += totalBudget`), ticket leg `_purchaseFor(...Claimable):983`, the >=1 ETH SDGNRS floor `:958`, `_ethToBurnieValue:1657`. `contracts/modules/DegenerusGameMintStreakUtils.sol` — `_quoteFarFutureSwap:145-190` (seed = keccak(player, rngWordByDay[today-1]), jitterMult 70-110%, ticketShareBps 40-80%), `_farFutureFractionBps:127-130`. `test/fuzz/FarFutureSalvageSwap.t.sol` — `test_SWAP08_NoArbAtCeiling_SweepAllDistances` (the no-arb proof to EXTEND at SALVAGE-03).
- sDGNRS-owned-BURNIE source primitives: `contracts/BurnieCoinflip.sol` — `previewClaimCoinflips:927`, `coinflipAmount:934`, the sDGNRS-stake-consume pattern `:908` + `consumeCoinflipsForBurn:366` (onlyBurnieCoin), `creditFlip:859`; `contracts/BurnieCoin.sol` — `transfer:315`, `transferFrom:329`; `burnie.balanceOf(SDGNRS)`.

CANCEL (D-30..D-33):
- `contracts/modules/GameAfkingModule.sol` — manual cancel `subscribe(…dailyQuantity=0)` `:345-362` (VERIFIED at `1e7a646d`: `_finalizeAfking` + `c.dailyQuantity = 0`; the "claim whenever" FALSE comment `:348-351`), `subscribe` `rngLock` gate `:300`, auto-evict paths: pass-expiry `:1175-1194`, funding-out `:1226-1252`, tombstone-reclaim+delete `:1144-1156`, `_finalizeAfking:1026`, `claimAfkingBurnie:1560-1589`, `drainAffiliateBase:1605-1610`, `pendingBurnie`/`affiliateBase` accrue `:856-874` + ticket bonus `:803-814`.
- `contracts/storage/DegenerusGameStorage.sol` — `Sub.affiliateBase:1946-1952`, `Sub.pendingBurnie:1953-1960`.
- `contracts/DegenerusAffiliate.sol` — `claim(address[]):629-696` (A/U1/U2 75/20/5), `_referrerAddress:809-815`, `playerReferralCode:228`. `contracts/DegenerusGame.sol` — `claimAfkingBurnie` dispatcher `:413`. `contracts/BurnieCoinflip.sol` — `creditFlip:859`.
</key_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock WWXRP-02 (D-14..D-18) + BURNIE-03 (D-21..D-24)</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md (the TDEC core + header from plan 01 — append to it)
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (D-14..D-18 WWXRP; D-21..D-24 BURNIE; the canonical_refs WWXRP + BURNIE anchor lists; the deferred note that the old PLAN-WWXRP global 0->5 cap is SUPERSEDED + matches==8 is the relabeled s==9)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (:614 _resolveFullTicketBet; :674 score s; :710-715 the currency==CURRENCY_ETH && s>=7 sDGNRS block — the hook goes immediately after; :216 CURRENCY_WWXRP=3; :225 MIN_BET_WWXRP; :407 resolveBets / :414 liveness revert; :142-150 _resolvePlayer)
    - contracts/storage/DegenerusGameStorage.sol (:973 whalePassClaims; :977 lootboxEthBase — the WWXRP-state insertion neighborhood)
    - contracts/modules/DegenerusGameMintModule.sol (:880 purchaseCoin; :887-907 _purchaseCoinFor — confirm it discards the four _callTicketPurchase returns; :1545-1554 the payInCoin burn-no-queue branch; :1251 _purchaseForWith ETH queue caller; :1657 _ethToBurnieValue; :1210-1217 quests.handlePurchase; :1220 return-fold; :1355 lootboxFlipCredit)
    - contracts/DegenerusQuests.sol (:947-949 the inline creditFlip(player, burnieMintReward) — the BATCH-01 co-design site)
    - contracts/DegenerusVault.sol (:571-574 gamePurchaseTicketsBurnie — the live consumer that today receives zero tickets)
  </read_first>
  <action>
    Append two sections to `358-SPEC.md`.

    `## WWXRP-02 — Degenerette Jackpot Whale-Halfpass (LOCKED — design half; WWXRP-01 built at IMPL 359)` — transcribe D-14..D-18, one labelled sub-point each:
    - D-14 rationing = GLOBAL PER BRACKET: ONE pass per `level/10` bracket total, to whoever lands the FIRST WWXRP jackpot in that bracket; later jackpots in the same bracket award nothing. New state: `mapping(uint256 => bool) wwxrpJackpotWhalePassBracketAwarded` keyed by `level/10`, appended to `DegenerusGameStorage` (no existing WWXRP state; slot region after `whalePassClaims:973` / `lootboxEthBase:977`). EXPLICITLY note this SUPERSEDES the old `PLAN-WWXRP-JACKPOT-WHALEPASS.md` global 0->5 lifetime cap, and that the plan's `matches==8` = the relabeled `s==9`.
    - D-15 multi-bracket allow: a single player CAN collect passes across different brackets (naturally rationed by the per-bracket flag).
    - D-16 recipient = the bettor `player`: the pass always accrues to the bet owner. `_resolveFullTicketBet`'s `player` is always the owner (`_resolvePlayer:142-150` validates `operatorApprovals[player][msg.sender]` then returns the owner; the operator address is not in scope at the award site). Permissionless resolve is fine — `resolveBets:407` (no access modifier) + the router stubs `resolveDegeneretteBets`/`degeneretteResolve` funnel through the shared `_resolveFullTicketBet`.
    - D-17 hook + gate: hook at `_resolveFullTicketBet` IMMEDIATELY AFTER the ETH-only `s>=7` sDGNRS block (`:710-715`). Gate: `s == 9 && currency == CURRENCY_WWXRP (3) && amountPerTicket >= MIN_BET_WWXRP (1 ether) && !wwxrpJackpotWhalePassBracketAwarded[level/10]`. The `s == 9` jackpot check short-circuits first (1-in-10M) → zero added cost on non-jackpot spins. Award `whalePassClaims[player] += 1` directly (the cheap freeze-safe grant — skips the ETH→halfpass conversion in `_queueWhalePassClaimCore`); set the bracket flag; emit an event.
    - D-18 freeze-safe + pre-liveness: the award writes only an RNG-INSENSITIVE counter/flag, gated by the already-committed `s == 9` (deterministic from the committed daily `rngWord`); reuses the `claimWhalePass` future-ticket deferral → no ETH/`claimablePool` touch (SOLVENCY-neutral). It only fires pre-liveness anyway (`resolveBets` reverts on `_livenessTriggered()` `:414`). Cross-reference the cross-cutting freeze/solvency section (plan 03).
    Flag "WWXRP-01 owned at IMPL 359 — its design is fixed here".

    `## BURNIE-03 — Coin-Buy Ticket-Queue Critical Fix (LOCKED — design half; BURNIE-01/02 built at IMPL 359; highest severity in the bundle)` — transcribe D-21..D-24, one labelled sub-point each:
    - D-21 the verified bug: `_purchaseCoinFor` (`MintModule:887-907`) calls `_callTicketPurchase(… payInCoin=true …)` and DISCARDS all four returns → the BURNIE buy burns the coin (`_coinReceive`→`coin.burnCoin`, the `payInCoin` branch `:1545-1554`) but queues ZERO tickets (a pure token sink — direct loss of funds on a live entrypoint, incl. `DegenerusVault.gamePurchaseTicketsBurnie:571-574`). Root cause: phase-160 `24f0898b` moved `_queueTicketsScaled` out of `_callTicketPurchase`'s shared tail into the ETH-only `_purchaseForWith:1251`. Record the DECISIVE grep: the queue callers are `_purchaseForWith:1251` (ETH), `GameAfkingModule:800`, `DegenerusGame:226` (vault init) — NONE reachable from the coin path.
    - D-22 BURNIE-01 fix = QUEUE ON RETURN: in `_purchaseCoinFor`, capture `_callTicketPurchase`'s returns and `if (adjustedQty != 0) _queueTicketsScaled(buyer, targetLevel, adjustedQty, false);` — restoring the pre-160 BURNIE→ticket behavior.
    - D-23 BURNIE-02 fix = MINT_BURNIE quest credit as a BURN REBATE: restore the MINT_BURNIE quest credit on the coin path via `quests.handlePurchase`'s MINT_BURNIE leg ONLY (`ethMintSpendWei=0`, `lootBoxAmount=0`); deliberately SKIP activity-score/`recordMint`, affiliate, and non-mint quests (correct for BURNIE). The reward is a BURN REBATE, NOT a separate `creditFlip`: REQUIRE the player to afford the FULL ticket cost upfront, DEFER the burn until after `handlePurchase` returns, then burn net = full coinCost − MINT_BURNIE reward (floored at 0) — the rebate can never enable a buy the player couldn't otherwise complete. CO-DESIGN with BATCH-01 (which makes `handlePurchase` RETURN `burnieMintReward` instead of crediting inline at `DegenerusQuests.sol:947-949`): the coin caller takes the RETURNED reward and nets it against the burn (vs the ETH caller `MintModule:1220/:1355`, which folds it into `lootboxFlipCredit`). State the PRODUCER-BEFORE-CONSUMER obligation: the reward must be known BEFORE the net burn, so the burn is deferred until after `handlePurchase` returns (the full-cost affordability check stays upfront).
    - D-24 freeze/solvency framing: RNG-freeze UNAFFECTED — `purchaseCoin` reads no `rngWord` (gated by `_livenessTriggered`/`gameOverPossible`). SOLVENCY — the ETH/`claimablePool` DEBIT code stays BYTE-UNCHANGED, BUT this RESTORES ticket claims on the ETH prize pools (the intended pre-160 design) — a GENUINE FUNCTIONAL FIX, not a no-op; no unbacked obligation (ticket wins stay pro-rata from available pool; BURNIE pays no ETH into pools, so the restoration must keep `claimablePool <= balance`). The posture-widening is FLAGGED (forward-ref the cross-cutting SEC-02 framing in plan 03). Note HYG-03 (361) adds the positive test + fixes the 3-arg `purchaseCoin` test drift + the unenforced "blocked when RNG locked" docstrings.
    Flag "BURNIE-01/02 owned at IMPL 359 — their design is fixed here".

    No contract code authored; cite anchors + behavior only; no fenced code.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "WWXRP-02 — Degenerette Jackpot Whale-Halfpass" "$f" && grep -q "BURNIE-03 — Coin-Buy Ticket-Queue Critical Fix" "$f" && for d in D-14 D-15 D-16 D-17 D-18 D-21 D-22 D-23 D-24; do grep -q "$d" "$f" || { echo "MISSING $d"; exit 1; }; done && grep -Eq "wwxrpJackpotWhalePassBracketAwarded|level/10" "$f" && grep -Eq "s == 9|s==9" "$f" && grep -Eqi "_purchaseCoinFor" "$f" && grep -Eqi "burn.?rebate|burn rebate" "$f" && grep -Eq "_queueTicketsScaled" "$f" && grep -Eq "24f0898b" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## WWXRP-02` section transcribes D-14..D-18, each labelled + anchored: per-bracket key `wwxrpJackpotWhalePassBracketAwarded[level/10]` (D-14, supersedes 0->5 cap, matches==8≡s==9), multi-bracket allow (D-15), recipient = `player` via `_resolvePlayer:142-150` (D-16), hook after the `:710-715` s>=7 block with the `s==9 && currency==CURRENCY_WWXRP(3) && amountPerTicket>=MIN_BET_WWXRP && !awarded[level/10]` gate + `whalePassClaims[player]+=1` (D-17), freeze-safe RNG-insensitive + pre-liveness + SOLVENCY-neutral (D-18 cites `:414`).
    - `## BURNIE-03` section transcribes D-21..D-24, each labelled + anchored: the verified bug (D-21 cites `_purchaseCoinFor:887-907` + `:1545-1554` + `24f0898b` + the decisive queue-caller grep), queue-on-return (D-22), MINT_BURNIE burn-rebate with full-cost upfront / deferred net burn / co-design with BATCH-01 `:947-949` + producer-before-consumer (D-23), freeze/solvency posture-widening flagged (D-24).
    - Both sections flagged "owned at IMPL 359 — design fixed here".
    - ZERO `contracts/*.sol` files modified; no fenced implementation code.
  </acceptance_criteria>
  <done>WWXRP-02 (D-14..D-18) and BURNIE-03 (D-21..D-24) are design-locked in `358-SPEC.md` with all anchors cited.</done>
</task>

<task type="auto">
  <name>Task 2: Lock SALVAGE-02 (D-25..D-29) + CANCEL-02 (D-30..D-33)</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md (the draft from Task 1 — append to it)
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (D-25..D-29 SALVAGE incl. the refined funding D-26/D-29 sDGNRS-owned-BURNIE-transferred-not-minted + the D-27 pawn-shop NOT-value-neutral model; D-30..D-33 CANCEL; the Claude's-Discretion within-day variability D-27; the canonical_refs SALVAGE + CANCEL anchor lists)
    - contracts/DegenerusGame.sol (:2074 sellFarFutureTickets entry; :2539 mintPrice; :413 claimAfkingBurnie dispatcher)
    - contracts/modules/DegenerusGameMintModule.sol (:929 sellFarFutureTickets; :958 the >=1 ETH SDGNRS floor; :975-977 the SDGNRS->player relabel; :983 the ticket leg; :1657 _ethToBurnieValue)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (:127-130 _farFutureFractionBps; :145-190 _quoteFarFutureSwap — seed/jitter/ticketShareBps cash<=60%)
    - contracts/BurnieCoinflip.sol (:366 consumeCoinflipsForBurn onlyBurnieCoin; :859 creditFlip; :908 the sDGNRS-stake-consume pattern; :927 previewClaimCoinflips; :934 coinflipAmount)
    - contracts/BurnieCoin.sol (:315 transfer; :329 transferFrom; the balanceOf(SDGNRS) source)
    - contracts/modules/GameAfkingModule.sol (:345-362 manual cancel + the :348-351 'claim whenever' FALSE comment; :300 the rngLock gate; :1026 _finalizeAfking; :1144-1156 tombstone-reclaim delete _subOf; :1175-1194 pass-expiry evict; :1226-1252 funding-out evict; :1560-1589 claimAfkingBurnie; :1605-1610 drainAffiliateBase; :856-874 + :803-814 the pendingBurnie/affiliateBase accrue)
    - contracts/DegenerusAffiliate.sol (:629-696 claim A/U1/U2 75/20/5; :809-815 _referrerAddress)
    - contracts/storage/DegenerusGameStorage.sol (:1946-1952 Sub.affiliateBase; :1953-1960 Sub.pendingBurnie)
    - test/fuzz/FarFutureSalvageSwap.t.sol (test_SWAP08_NoArbAtCeiling_SweepAllDistances — the no-arb proof obligation to extend, read-only here)
  </read_first>
  <action>
    Append two sections to `358-SPEC.md`.

    `## SALVAGE-02 — sDGNRS Salvage-Swap Combo ETH/BURNIE Pawn-Shop Payout (LOCKED — design half; SALVAGE-01 built at IMPL 359, SALVAGE-03 proven at TST 361)` — transcribe D-25..D-29, one labelled sub-point each:
    - D-25 current structure: `sellFarFutureTickets(player, levels[], quantities[], queueIndices[])` `DegenerusGame:2074` → delegate → `MintModule:929`. Quote/jitter `_quoteFarFutureSwap` `MintStreakUtils:145-190`: `seed = keccak(player, rngWordByDay[_simulatedDayIndex()-1])` (the SETTLED prior-day word), `jitterMult ∈ [70%,110%]`, `ticketShareBps ∈ [40%,80%]` (→ cash ≤60% = the eth/cash cap). Payout = ticket leg (`ticketWei`→current-level tickets) + cash leg (`cashWei = totalBudget − ticketWei` → ETH in `claimableWinnings[player]`). ETH source: the SDGNRS relabel `MintModule:975-977` (`claimableWinnings[SDGNRS] -= totalBudget; player += totalBudget`, pool-neutral). Gated by `if (rngLockedFlag) revert` + `gameOver` + `_livenessTriggered`. Helpers `_ethToBurnieValue(wei, mintPrice())` `:1657` (= wei·1000e18/price), `coinflip.creditFlip`. No existing BURNIE leg.
    - D-26 SALVAGE-01 design: split the cash leg `cashWei` into an ETH part + a BURNIE part. Randomize a TARGET BURNIE portion full-range `[0 .. cashWei]` from a NEW slice of the existing prior-day `seed` (no new VRF); `ethCap` = the existing cash ≤60%-of-`totalBudget` limit (so ETH out ≤ `cashWei` ≤ cap always; BURNIE NOT counted against it). The BURNIE leg is paid from sDGNRS-OWNED BURNIE — NOT a `creditFlip` mint: sources = sDGNRS's BURNIE token balance (`burnie.balanceOf(SDGNRS)`, via `transfer`/`transferFrom` `BurnieCoin:315/:329`) + sDGNRS's claimable coinflip credit (`coinflipAmount(SDGNRS)`/`previewClaimCoinflips:927/:934`, consumed like the existing sDGNRS-stake-consume `BurnieCoinflip:908`/`consumeCoinflipsForBurn:366`), valued via `_ethToBurnieValue(part, mintPrice())` `:1657` @ current eth-equivalent. FUNDING FALLBACK: `actualBurnie = min(targetBurnie, sDGNRS-available)`; pay `actualBurnie` from sDGNRS-owned BURNIE and the REMAINDER (`cashWei − actualBurnie`) as ETH. If sDGNRS has ZERO BURNIE + ZERO claimable coinflip → no BURNIE leg, pay the whole cash leg in ETH ("do it more like now"). Always cap-compliant (`ethOut = cashWei − actualBurnie ≤ cashWei ≤ ethCap`). The ticket leg is UNCHANGED. Update BOTH the execute path (`MintModule:929/975`) AND the preview/quote (`_quoteFarFutureSwap`) — the preview must reflect source-availability + fallback so the offer stays truly knowable-in-advance. LOCK the exact sDGNRS-owned-BURNIE transfer/consume primitive (`transfer`/`transferFrom` for the token balance; the `consumeCoinflipsForBurn`-pattern for the claimable stake) + the availability read + the ETH fallback.
    - D-27 the "pawn-shop" safety model (load-bearing): the salvage makes a VARIABLE, KNOWABLE-IN-ADVANCE offer — total value AND the ETH/BURNIE split may differ (even intra-day across bundles/timing). It is NOT value-neutral (the earlier value-neutral framing is DROPPED). The SOLE non-exploitability property is the TOTAL PAYOUT CAP (the existing no-arb ceiling) + the eth-% cap: every reachable offer (across all seed × bundle × split × timing) ≤ the cap → no extraction above it regardless of how the player optimizes, even though the offer is fully predictable. Suboptimal players forfeit EV to the protocol/pools (INTENDED game design, not a leak). Note the Claude's-Discretion within-day variability latitude (an optional block/nonce slice MAY mix into the seed AS LONG AS every offer stays previewable + ≤ the no-arb ceiling + eth-% cap; the randomness SOURCE stays the settled prior-day word — no new VRF).
    - D-28 freeze framing: freeze-safe by the EXISTING accepted pattern — the offer is a transparent function of the SETTLED prior-day word under the `rngLockedFlag` gate (the v48 jitter already works this way); the new ETH/BURNIE split is one more derived slice of the same seed → NO new VRF, NO new manipulable freeze surface. Non-exploitability rests on the cap, not on unpredictability. SEC-01 (361) covers it empirically.
    - D-29 solvency framing + no-arb re-proof: SOLVENCY-positive/neutral + NO new BURNIE emission — only the ETH part (`cashWei − actualBurnie`) is relabeled out of `claimableWinnings[SDGNRS]` (≤ the full `cashWei`, so the ETH liability DROPS vs today; `claimablePool ≤ balance` holds); the BURNIE part is a TRANSFER of sDGNRS-OWNED BURNIE (token balance and/or its claimable coinflip stake) to the player — no `creditFlip` mint, no inflation. The source-availability check + ETH fallback guarantee the swap never over-draws sDGNRS's BURNIE and stays cap-compliant. LOCK the load-bearing obligation owned at SALVAGE-02 (verified at SALVAGE-03): EXTEND `test_SWAP08_NoArbAtCeiling_SweepAllDistances` so the max reachable offer still sits below the far-ticket acquisition floor across the FULL split range + the BURNIE-source valuation + the fallback path; since the split conserves the cash-leg VALUE (changes form/source only) the total-value ceiling is unchanged, but the proof must cover the full split range + the BURNIE-source valuation + the fallback.
    Flag "SALVAGE-01 owned at IMPL 359, SALVAGE-03 at TST 361 — design fixed here".

    `## CANCEL-02 — Manual Sub-Cancel Auto-Claim + Auto-Evict Pure-Forfeit (LOCKED — design half; CANCEL-01 built at IMPL 359, CANCEL-03 proven at TST 361)` — transcribe D-30..D-33, one labelled sub-point each:
    - D-30 the latent loss bug it fixes: manual cancel (`GameAfkingModule:345-362`, `subscribe(…, dailyQuantity=0)`) calls `_finalizeAfking` + tombstones (`dailyQuantity=0`) and leaves `pendingBurnie` + `affiliateBase` in the slot — a comment (`:348-351`) promises "claim whenever" — BUT the next `processSubscriberStage` reclaim `:1144` `delete _subOf[player]` wipes both → the accruals are LOST if any advance runs before the player (+ an affiliate caller) claim. State the comment is FALSE; the auto-claim closes the race.
    - D-31 CANCEL-01 manual-cancel auto-claim: on manual cancel, BEFORE clearing: (a) pay the canceller's `pendingBurnie` to themselves via `coinflip.creditFlip` + the presale-box-credit parity (mirror `claimAfkingBurnie:1560`; zero the field FIRST — CEI); (b) drain `affiliateBase` + settle the affiliate tree A/U1/U2 = 75/20/5 (mirror `DegenerusAffiliate.claim:629` via `drainAffiliateBase:1605`; A = the canceller's REFERRER — the base is owed UPLINE, NOT to the canceller; deterministic, no roll; cite `_referrerAddress:809-815`); (c) `_finalizeAfking` + clear.
    - D-32 CANCEL-01 auto-evict = pure FORFEIT: auto-evict (pass-expiry `:1175` + funding-out `:1226` + tombstone-reclaim `:1144`) stays "just delete the data" — delete the Sub record incl. BOTH accumulators, NO payout to self OR uplines. Make the forfeit EXPLICIT: today the pass/funding evict paths `_removeFromSet` but leave `_subOf` claimable out-of-set (a latent inconsistency); the forfeit intent means they MUST wipe the accruals (delete `_subOf`) so they're truly unclaimable post-evict. (USER accepted that auto-evict denies innocent uplines their share — the distinction is chose-to-leave vs got-kicked.)
    - D-33 CANCEL freeze/solvency: BURNIE-emission only (`creditFlip`/`drainAffiliateBase`); reads no `rngWord`; no ETH/`claimablePool` touch (SOLVENCY-01 untouched); cancel is `rngLock`-gated at the `subscribe` entry (`:300`); CEI (zero before credit). No new freeze surface. This is the CLEAN original-v57 posture (unlike BURNIE/SALVAGE).
    Flag "CANCEL-01 owned at IMPL 359, CANCEL-03 at TST 361 — design fixed here".

    No contract code authored; cite anchors + behavior only; no fenced code.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "SALVAGE-02 — sDGNRS Salvage-Swap Combo" "$f" && grep -q "CANCEL-02 — Manual Sub-Cancel Auto-Claim" "$f" && for d in D-25 D-26 D-27 D-28 D-29 D-30 D-31 D-32 D-33; do grep -q "$d" "$f" || { echo "MISSING $d"; exit 1; }; done && grep -Eqi "sDGNRS-owned" "$f" && grep -Eq "actualBurnie" "$f" && grep -Eqi "no-?arb" "$f" && grep -Eq "SWAP08" "$f" && grep -Eq "75/20/5" "$f" && grep -Eqi "forfeit" "$f" && grep -Eq "drainAffiliateBase" "$f" && grep -Eqi "claim whenever|false comment|FALSE" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## SALVAGE-02` section transcribes D-25..D-29, each labelled + anchored: current structure (D-25 cites `:2074`/`:929`/`_quoteFarFutureSwap:145-190`/`:975-977`/`_ethToBurnieValue:1657`), the cash-leg split + sDGNRS-owned-BURNIE-transferred-not-minted source + `actualBurnie=min(target,available)` + ETH fallback (D-26 cites the BurnieCoinflip/BurnieCoin source primitives), the pawn-shop NOT-value-neutral total-payout-cap + eth-%-cap model (D-27), the settled-prior-day-word no-new-VRF freeze framing (D-28), the solvency-positive no-new-emission + the EXTEND-SWAP08 no-arb re-proof obligation (D-29).
    - `## CANCEL-02` section transcribes D-30..D-33, each labelled + anchored: the latent loss bug + FALSE "claim whenever" comment (D-30 cites `:345-362`/`:348-351`/`:1144`), manual-cancel auto-claim self + tree 75/20/5 before clear (D-31 cites `claimAfkingBurnie:1560`/`drainAffiliateBase:1605`/`DegenerusAffiliate.claim:629`/`_referrerAddress:809-815`), auto-evict explicit-delete pure-forfeit (D-32 cites the three evict paths), BURNIE-emission-only freeze/solvency clean posture (D-33 cites the `:300` rngLock gate).
    - Both sections flagged "owned at IMPL 359 / TST 361 — design fixed here".
    - ZERO `contracts/*.sol` files modified; no fenced implementation code.
  </acceptance_criteria>
  <done>SALVAGE-02 (D-25..D-29) and CANCEL-02 (D-30..D-33) are design-locked in `358-SPEC.md` with all anchors cited and the sDGNRS-owned-BURNIE source + no-arb re-proof obligation + explicit-delete forfeit fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (DESIGN-LEVEL — paper-only SPEC phase; zero new code-attack-surface since zero contracts change)

| Boundary | Description |
|----------|-------------|
| Degenerette bettor/operator → `_resolveFullTicketBet` WWXRP award | Permissionless resolve; the pass must accrue to the bet OWNER (`player`), never the operator; the award must be RNG-insensitive (gated by the already-committed `s==9`). |
| BURNIE coin buyer → `_purchaseCoinFor` | The buyer pays BURNIE and must receive the queued tickets (the bug burns with zero queue); the MINT_BURNIE rebate must never enable a buy the player couldn't otherwise afford (full-cost upfront gate). |
| salvage seller → `sellFarFutureTickets` (ETH/solvency + RNG-reading) | The seller is the untrusted EV-seeking actor probing every seed × bundle × split × timing; the SOLE safety property is the total-payout cap + eth-% cap; the BURNIE leg must never over-draw sDGNRS-owned BURNIE. |
| canceller / evicted sub → `subscribe(dailyQuantity=0)` + evict paths | Manual cancel must auto-claim self + uplines before the reclaim race wipes the slot; auto-evict must explicitly forfeit (no out-of-set claimable residue). |
| ETH/`claimablePool` solvency path | WWXRP + CANCEL stay OFF it (RNG-insensitive / BURNIE-emission only); BURNIE keeps the DEBIT byte-unchanged (restores claims); SALVAGE draws LESS ETH (solvency-positive). |

## STRIDE Threat Register (design assertions the SPEC LOCKS; PROVEN at TST 361 / adversarially at TERMINAL 362)

| Threat ID | Category | Component | Disposition | Mitigation Plan (design assertion) |
|-----------|----------|-----------|-------------|------------------------------------|
| T-358-06 | Tampering (RNG) | WWXRP award timed/forced to grind a jackpot pass | mitigate | Award writes only an RNG-insensitive counter/bracket-flag, gated by the already-committed `s==9` (deterministic from the committed `rngWord`); pre-liveness only (`resolveBets` reverts on `_livenessTriggered()` `:414`). SPEC WWXRP-02 D-18; TST 361 SEC-01. |
| T-358-07 | Spoofing (recipient) | operator-placed bet redirects the pass to the operator | mitigate | Recipient is always the bet owner `player` via `_resolvePlayer:142-150`; the operator address is not in scope at the award site. SPEC WWXRP-02 D-16. |
| T-358-08 | Repudiation / loss-of-funds | BURNIE coin buy burns coin but queues zero tickets | mitigate | Queue-on-return (D-22): capture `_callTicketPurchase` returns + `_queueTicketsScaled(buyer,targetLevel,adjustedQty,false)`; restores the live consumer `gamePurchaseTicketsBurnie:571-574`. SPEC BURNIE-03; TST 361 HYG-03 positive test. |
| T-358-09 | Elevation (free rebate) | MINT_BURNIE rebate enables an otherwise-unaffordable buy | mitigate | Full-cost affordability gate is the FULL ticket cost upfront; the burn is deferred + netted (full − reward floored at 0) but eligibility is the full amount; rebate ≤ full cost. SPEC BURNIE-03 D-23. |
| T-358-10 | Tampering (solvency) | BURNIE fix restores claims → unbacked ETH obligation | mitigate | ETH/pool DEBIT byte-unchanged; ticket wins stay pro-rata from the available pool; BURNIE pays no ETH into pools → `claimablePool <= balance` holds. Posture-widening FLAGGED. SPEC BURNIE-03 D-24; cross-cutting SEC-02 (plan 03); TST 361. |
| T-358-11 | Tampering (EV/arb) | salvage seller optimizes seed×bundle×split×timing to extract above face | mitigate | The total-payout cap (no-arb ceiling) + eth-% cap bound EVERY reachable offer regardless of predictability; the split conserves cash-leg VALUE (form/source only). SPEC SALVAGE-02 D-27; EXTEND-SWAP08 no-arb re-proof D-29; TST 361 SALVAGE-03. |
| T-358-12 | Tampering (over-draw) | salvage BURNIE leg over-draws sDGNRS-owned BURNIE | mitigate | `actualBurnie = min(target, sDGNRS-available)` + ETH fallback for the remainder/zero-available case; no `creditFlip` mint → no inflation. SPEC SALVAGE-02 D-26/D-29; TST 361 SALVAGE-03. |
| T-358-13 | Repudiation / loss | manual-cancel accruals lost to the reclaim race | mitigate | Auto-claim self (`pendingBurnie`→`creditFlip`, CEI zero-first) + drain `affiliateBase` to the tree 75/20/5 BEFORE `_finalizeAfking`+clear. SPEC CANCEL-02 D-31; TST 361 CANCEL-03. |
| T-358-14 | Tampering (residue) | auto-evict leaves out-of-set claimable residue | mitigate | Auto-evict explicitly deletes `_subOf` incl. both accumulators (pure forfeit) so post-evict residue is unclaimable. SPEC CANCEL-02 D-32; TST 361 CANCEL-03. |

**Block-on-HIGH note:** Paper-only design phase — no package installs, no contract mutation. The block-on-HIGH gate is the DESIGN gate: any freeze/solvency/loss hole the SPEC cannot close on paper is a blocking threat. The two functional solvency exceptions (BURNIE restoration T-358-10, SALVAGE T-358-11/12) are both flagged + both solvency-positive-or-neutral with the no-arb re-proof obligation handed to TST 361. T-358-06..14 are all mitigated by design assertions with named anchors; no HIGH design hole remains open at lock for these four surfaces.
</threat_model>

<verification>
- `git diff --quiet 1e7a646d HEAD -- contracts/` is clean throughout (ZERO contract mutation — paper-only).
- `358-SPEC.md` extended with `## WWXRP-02`, `## BURNIE-03`, `## SALVAGE-02`, `## CANCEL-02`, each transcribing its full decision range (D-14..D-18 / D-21..D-24 / D-25..D-29 / D-30..D-33) with anchors cited at the verified line.
- The BURNIE producer-before-consumer sequencing, the SALVAGE sDGNRS-owned-BURNIE transfer primitive + `actualBurnie` fallback + the EXTEND-SWAP08 no-arb obligation, the WWXRP new per-bracket storage, and the CANCEL explicit-delete forfeit are each explicitly fixed.
- WWXRP-02, BURNIE-03, SALVAGE-02, CANCEL-02 are the ONLY requirement IDs claimed by this plan.
</verification>

<success_criteria>
- `358-SPEC.md` carries IMPL-ready design-locks for all four small features (WWXRP-02, BURNIE-03, SALVAGE-02, CANCEL-02) with the two functional solvency-posture exceptions flagged.
- Every cited `file:line` is reconciled vs `1e7a646d`; no "by construction" survives un-cited.
- ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-02-SUMMARY.md` when done.
</output>
