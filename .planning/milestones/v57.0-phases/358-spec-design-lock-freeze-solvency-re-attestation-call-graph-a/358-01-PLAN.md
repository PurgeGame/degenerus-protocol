---
phase: 358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
autonomous: true
requirements: [TDEC-02, TDEC-03]
must_haves:
  truths:
    - "358-SPEC.md exists with the standard SPEC header (phase, baseline = frozen subject 1e7a646d, status DRAFT, paper-only note) and the frozen-subject guard (git diff --quiet 1e7a646d HEAD -- contracts/ clean)"
    - "358-SPEC.md locks TDEC-02 terminal-decimator mechanics (D-04..D-13): last-day-only window (D-04), bucket PROMOTION re-derived from live playerActivityScore via _terminalDecBucket with strictly-lower-denom = better (D-05), forced subBucket re-derive on promotion (D-06), aggregate re-key remove-from-old/add-to-new conserving total weight (D-07), weight scaling ×boostFactor(effectiveStreak) anchors 100→20x/10→4x (D-08), effective-streak source getPlayerQuestView(player).baseStreak (D-09), keep-both-levers double-count intentional (D-10), saturate uint88 (D-11), shields read-only no-consume (D-12), one-time boosted bit in TerminalDecEntry 24 spare bits + requires-existing-burn (D-13)"
    - "358-SPEC.md contains a '## TDEC-03 Freeze-Safety Proof' section that RIGOROUSLY discharges the future-day-word lemma (D-01/D-02/D-03): the gate is require(!_livenessTriggered()) ALONE (D-01); the resolution reads rngWordByDay[gameOverDay] which physically cannot exist before the boost gate closes because any !liveness-admitted boost occurred on an EARLIER day (D-02); the same-day rngWord-reuse refinement is RETRACTED as internally inconsistent with a belt-and-suspenders fallback recorded (D-03)"
    - "358-SPEC.md TDEC sections cite every anchor at the verified 1e7a646d lines (TerminalDecEntry :1585-1591 = 232/256 bits 24 spare; _livenessTriggered :1231-1240; getPlayerQuestView :1088; GameOverModule rngWordByDay[day] :106; the bucket/subbucket/aggregate machinery DecimatorModule :559-570/:755/:925-936) with any drift corrected"
  artifacts:
    - path: ".planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md"
      provides: "The v57.0 design-lock SPEC draft header + the terminal-decimator design-lock (TDEC-02) + the load-bearing freeze-safety proof (TDEC-03); WWXRP/BURNIE/SALVAGE/CANCEL + cross-cutting sections are appended by plans 02 and 03"
      contains: "TDEC-03 Freeze-Safety Proof"
      min_lines: 150
  key_links:
    - from: "358-SPEC.md TDEC-03 Freeze-Safety Proof section"
      to: "DegenerusGameStorage.sol:1231-1240 (_livenessTriggered) + DegenerusGameGameOverModule.sol:106 (rngWordByDay[day] read)"
      via: "the future-day-word lemma: !liveness gate closes before the resolution word can be written"
      pattern: "_livenessTriggered|future.?day.?word|gameOverDay"
    - from: "358-SPEC.md TDEC-02 section (bucket promotion + aggregate re-key)"
      to: "DegenerusGameDecimatorModule.sol:559-570 (_decSubbucketFor) + :755 (terminalDecBucketBurnTotal key) + :925-936 (_terminalDecBucket)"
      via: "re-derive bucket+subBucket from live activity score, remove-from-old/add-to-new aggregate key, conserve total weight"
      pattern: "re-?key|terminalDecBucketBurnTotal|subBucket"
---

<objective>
Author the v57.0 design-lock SPEC core — `358-SPEC.md` — establishing the standard header + frozen-subject guard, then settling the terminal-decimator design (TDEC-02, owned here) and discharging the load-bearing freeze-safety proof (TDEC-03, owned here). These are the spine of the milestone: the terminal-decimator boost now PROMOTES the bucket (a NEW weight-determining write before the draw), so the original `subBucket`-fixed simplification no longer holds and the freeze-safety must be re-proven rigorously, not asserted.

Purpose: produce a fully-reconciled terminal-decimator design-lock + freeze proof so Phase 359 IMPL authors `boostTerminalDecimator()` with zero "by construction" assumptions, and the freeze spine is designed BEFORE any code. This is the producer for plans 02 (WWXRP/BURNIE/SALVAGE/CANCEL locks) and 03 (cross-cutting re-attestation + grep-attest + SPEC lock), which append to the same `358-SPEC.md`.

Output: `358-SPEC.md` (the design-lock DRAFT — header + frozen-subject guard + `## TDEC-02` + `## TDEC-03 Freeze-Safety Proof`). PAPER-ONLY — ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md

<frozen_subject>
Baseline = v56.0 closure HEAD, frozen contract subject `1e7a646d` (`MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`). Working-tree `contracts/` is byte-identical to `1e7a646d` — confirm with `git diff --quiet 1e7a646d HEAD -- contracts/` (clean) and record PASS in the SPEC. Every cited `file:line` MUST be grep-re-attested before being written into the SPEC (no "by construction" survives un-checked). Anchors may be read either from the working tree or via `git show 1e7a646d:<path>` — both equal the frozen subject under the guard.
</frozen_subject>

<key_anchors>
The CONTEXT.md "Source anchors" lists are canonical. The load-bearing TDEC anchors this plan must cite (verified present at `1e7a646d` during planning):
- Freeze gate: `_livenessTriggered` `contracts/storage/DegenerusGameStorage.sol:1231-1240` (day-constant liveness predicate; VRF-grace stall branch in the same fn).
- Resolution word read: `handleGameOverDrain` `contracts/modules/DegenerusGameGameOverModule.sol:86`, the `rngWord = rngWordByDay[day]` read `:106` (NOTE the verified DRIFT — at `1e7a646d` this read is INSIDE an `if (preRefundAvailable != 0)` guard, NOT unconditional; the SPEC must reconcile this in the proof: the decimator path `runTerminalDecimatorJackpot(decPool,lvl,rngWord)` `:174-183` needs a real word, so the proof covers both the guarded-read case and the decimator pool branch), `gameOver = true` `:145`.
- RNG word write: `_applyDailyRng` `rngWordByDay[day] = finalWord` `contracts/modules/DegenerusGameAdvanceModule.sol:1879`; the game-over routing gate `_handleGameOverPath` `:591`, `handleGameOverDrain` call `:665-670`.
- Decimator machinery: `recordTerminalDecBurn` `contracts/modules/DegenerusGameDecimatorModule.sol:693` (bucket freeze `:726`, uint88 saturation `:750-752`, burn gate daysRemaining>7 `:701`), `_decSubbucketFor` `:559-570` (subBucket = keccak(player,lvl,bucket) % bucket), `_terminalDecBucket` `:925-936`, `_terminalDecMultiplierBps` `:916`, `terminalDecBucketBurnTotal` key `keccak(lvl,bucket,subBucket)` `:755`, `runTerminalDecimatorJackpot` `:780`, deadline `_terminalDecDaysRemaining` `:939-950`.
- Effective streak: `getPlayerQuestView` `contracts/DegenerusQuests.sol:1088` (a `view`; `.baseStreak` = gap-reset + shields applied); raw spoofable `playerQuestStates.streak` is the REJECTED source.
- Quest streak cap 100: `contracts/utils/MintStreakUtils.sol:251` (a.k.a. `DegenerusGameMintStreakUtils.sol`) — also the `questStreak*100` bps fold into `playerActivityScore` (the double-count lever D-10).
- Packing: `TerminalDecEntry` `contracts/storage/DegenerusGameStorage.sol:1585-1591` (verified: `uint80 totalBurn / uint88 weightedBurn / uint8 bucket / uint8 subBucket / uint48 burnLevel` = 232/256, 24 spare → hosts the `boosted` bit).
</key_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SPEC header + frozen-subject guard, and lock TDEC-02 terminal-decimator mechanics (D-04..D-13)</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (the TDEC decisions D-04..D-13 + the canonical_refs DecimatorModule/Storage anchor lists + the code_context "Reusable Assets" + "Established Patterns")
    - contracts/modules/DegenerusGameDecimatorModule.sol (:559-570 _decSubbucketFor; :693-755 recordTerminalDecBurn incl. the bucket freeze :726 / uint88 saturate :750-752 / burn gate :701 / the terminalDecBucketBurnTotal key :755; :916 _terminalDecMultiplierBps; :925-936 _terminalDecBucket; :939-950 _terminalDecDaysRemaining; :780 runTerminalDecimatorJackpot)
    - contracts/storage/DegenerusGameStorage.sol (:1585-1591 TerminalDecEntry — count the bits: 232/256, 24 spare)
    - contracts/DegenerusQuests.sol (:1088 getPlayerQuestView — confirm it is a `view` returning baseStreak)
    - contracts/utils/MintStreakUtils.sol (:251 the quest-streak→activity-score fold + the streak cap 100)
  </read_first>
  <action>
    Create `358-SPEC.md` with the standard SPEC header: title (`# v57.0 Design-Lock SPEC (Phase 358)`), baseline = frozen subject `1e7a646d` with the closure signal, status DRAFT, and an explicit PAPER-ONLY note (ZERO `contracts/*.sol` mutation — this phase locks design and grep-attests anchors only). Add a `### Frozen-Subject Guard` subsection asserting `git diff --quiet 1e7a646d HEAD -- contracts/` is clean and that every anchor cited in this SPEC is therefore read-equivalent to the frozen subject. Add a one-line table of contents listing the sections plans 01/02/03 will fill (TDEC-02, TDEC-03, WWXRP-02, BURNIE-03, SALVAGE-02, CANCEL-02, UDVT design feed, Cross-Cutting Freeze/Solvency Re-Attestation, Full Call-Graph Grep-Attestation, SPEC Lock) so downstream plans append in order.

    Then author the section `## TDEC-02 — Terminal-Decimator Boost Mechanics (LOCKED — owned here; TDEC-01 built at IMPL 359)` transcribing decisions D-04..D-13 into IMPL-ready prose, one labelled sub-point per decision, each citing its anchor:
    - D-04 window = LAST DAY ONLY: the boost is admissible only in the final window — a game-design lever (separate from freeze) forcing "streak alive to the END" (burn early via `recordTerminalDecBurn` requires daysRemaining>7 at `DecimatorModule:701`, boost only at the deadline day `purchaseStartDay + DEATH_CLOCK_DAYS` [level!=0 ⇒ +120; level 0 ⇒ +365], still `!liveness`). State the exact daysRemaining threshold (==0 / <=1) is Claude's-discretion at IMPL within this locked shape; cite the deadline helper `_terminalDecDaysRemaining:939-950`.
    - D-05 bucket PROMOTION IN: the boost re-derives the bucket from the player's LIVE `playerActivityScore(player)` (which now includes the kept-alive quest streak) via `_terminalDecBucket(...)` (`:925-936`); if the resulting denom is STRICTLY LOWER (= better odds) than the frozen bucket, PROMOTE; else keep the frozen bucket. State that bucket is a function of activity score, NOT of weight.
    - D-06 forced subBucket re-derive on promotion: `subBucket = keccak256(player,lvl,bucket) % bucket` (`_decSubbucketFor:559-570`) is bucket-dependent, so a promotion MUST re-derive subBucket (a kept old subBucket could exceed the new denom and never win) — "keep vs re-derive" is SETTLED = re-derive; safe because committed before the RNG request (forward-ref TDEC-03).
    - D-07 aggregate re-key (solvency conserved): on promotion, move the weighted contribution from `terminalDecBucketBurnTotal[keccak(lvl,oldBucket,oldSub)]` to `[keccak(lvl,newBucket,newSub)]` — remove-from-old, add-to-new — so total weight is conserved and `runTerminalDecimatorJackpot`'s shares still sum to the pool (SOLVENCY-neutral; weight-only, BURNIE/ETH path untouched). Cite the key `:755` + `runTerminalDecimatorJackpot:780`.
    - D-08 weight scaling: multiply `weightedBurn` by `boostFactor(effectiveStreak)`, anchors streak 100→20x, 10→4x, 1x floor at streak 0; record the candidate two-line curve (factorBps = 10000 + 3000·s for s<=10; 40000 + (s-10)·1778 for 10<s<=100; cap 20x) as the calibration target with the exact constants Claude's-discretion at IMPL/GAS. Note quest streak caps at 100 (`MintStreakUtils:251`).
    - D-09 effective-streak source = `getPlayerQuestView(player).baseStreak` (`DegenerusQuests.sol:1088`), the EFFECTIVE streak with daily gap-reset + shields applied — NOT the raw spoofable `playerQuestStates.streak`. State it is a `view` (no mutation).
    - D-10 double-count = KEEP BOTH LEVERS: the quest streak ALREADY feeds burn-time weight (`MintStreakUtils:251` folds `questStreak*100` bps into `playerActivityScore`, driving both the frozen bucket and the `multBps` activity multiplier at `DecimatorModule:709-712`); the final-day boost multiplies a base that already contains streak — this stacking is INTENTIONAL; `playerActivityScore` is left UNTOUCHED (large blast radius; stripping streak rejected).
    - D-11 overflow = SATURATE uint88: the boost clamps `weightedBurn` to `type(uint88).max`, matching the existing `recordTerminalDecBurn` behavior (`:750-752`); the aggregate add stays consistent.
    - D-12 shields = READ-ONLY, no consume: the boost reads the effective streak via the `view` (shields already factored into whether the streak survived gaps); consumes nothing; shields are still consumed naturally by normal quest actions / `_questSyncState`.
    - D-13 idempotence + prerequisite: one-time per terminal level via a `boosted` bit in the packed `TerminalDecEntry` (24 spare bits confirmed: `uint80/uint88/uint8/uint8/uint48` = 232/256, `Storage:1585-1591`); requires an existing terminal-dec burn (scale committed weight, not buy an entry).

    Do NOT author any contract code. Do NOT inline implementation bodies or fenced code — cite identifiers + line ranges + behavior only. Flag the section "TDEC-01 owned at IMPL 359 — its design is fixed here".
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "Frozen-Subject Guard" "$f" && grep -q "TDEC-02 — Terminal-Decimator Boost Mechanics" "$f" && for d in D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-13; do grep -q "$d" "$f" || { echo "MISSING $d"; exit 1; }; done && grep -Eq "getPlayerQuestView" "$f" && grep -Eq "232/256|232 of 256|24 spare" "$f" && grep -Eqi "saturate|uint88" "$f" && grep -Eqi "promot" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `358-SPEC.md` exists with the standard header (baseline `1e7a646d`, status DRAFT, PAPER-ONLY note) + a `### Frozen-Subject Guard` subsection asserting `git diff --quiet 1e7a646d HEAD -- contracts/` is clean + a section table-of-contents listing all downstream sections.
    - A `## TDEC-02 — Terminal-Decimator Boost Mechanics (LOCKED ...)` section transcribes ALL of D-04..D-13, one labelled sub-point each, each citing its anchor at the verified line (window/last-day D-04 cites `:701`+`:939-950`; promotion D-05 cites `_terminalDecBucket:925-936`; subBucket re-derive D-06 cites `_decSubbucketFor:559-570`; aggregate re-key D-07 cites `terminalDecBucketBurnTotal:755`+`runTerminalDecimatorJackpot:780`; weight scaling D-08 with the 100→20x/10→4x anchors + the candidate curve; streak source D-09 cites `getPlayerQuestView:1088`; keep-both-levers D-10 cites `MintStreakUtils:251`; saturate uint88 D-11 cites `:750-752`; shields read-only D-12; boosted-bit idempotence D-13 cites `TerminalDecEntry:1585-1591` 232/256 24-spare).
    - The exact daysRemaining threshold (D-04), boostFactor constants (D-08) are flagged Claude's-discretion-at-IMPL within the locked shapes.
    - ZERO `contracts/*.sol` files modified (paper-only); no fenced implementation code in the authored prose.
  </acceptance_criteria>
  <done>The SPEC header + frozen-subject guard + the full TDEC-02 mechanics design-lock (D-04..D-13) are written into `358-SPEC.md` with all anchors cited.</done>
</task>

<task type="auto">
  <name>Task 2: Discharge the TDEC-03 freeze-safety proof — the future-day-word lemma (D-01/D-02/D-03)</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md (the draft from Task 1 — append to it)
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (D-01/D-02/D-03 + the <specifics> USER framing "stopped when liveness is dead, before the rng request" + the code_context "Established Patterns" freeze invariant)
    - contracts/storage/DegenerusGameStorage.sol (:1231-1240 _livenessTriggered — note it is day-constant: lvl==0 ⇒ +DEPLOY_IDLE_TIMEOUT, lvl!=0 ⇒ +120, plus the VRF-grace stall branch block.timestamp - rngRequestTime >= _VRF_GRACE_PERIOD)
    - contracts/modules/DegenerusGameGameOverModule.sol (:86 handleGameOverDrain; :106 the rngWord = rngWordByDay[day] read — VERIFY it is inside `if (preRefundAvailable != 0)`; :145 gameOver=true; :174-183 runTerminalDecimatorJackpot(decPool,lvl,rngWord))
    - contracts/modules/DegenerusGameAdvanceModule.sol (:591 _handleGameOverPath liveness gate; :665-670 the handleGameOverDrain call; :1879 _applyDailyRng rngWordByDay[day]=finalWord — the ONLY daily-word write)
  </read_first>
  <action>
    Append the section `## TDEC-03 — Freeze-Safety Proof (LOCKED — the load-bearing re-proof under the bucket-promotion allowance)`. RIGOROUSLY discharge the future-day-word lemma; do NOT assert it. Structure the proof as labelled lemmas/steps, each citing anchors:

    - Statement of the obligation: since TDEC-01 now allows a bucket PROMOTION (D-05) — a NEW weight-AND-bucket-determining write — the validator's original `subBucket`-fixed simplification no longer holds, so the freeze-safety argument must be the GENERAL rule "all weight + bucket mutation precedes the draw", NOT "the bucket is immutable" (cite the USER framing from <specifics>: "if the number or bucket was manipulable between rng request and decimator resolution that would violate the rules").

    - Gate (D-01): `boostTerminalDecimator()` gates on `require(!_livenessTriggered())` ALONE (+ the idempotent `boosted` bit, an existing entry for the current terminal level, a live effective streak). Argue this is SUFFICIENT and that the original `!gameOver` framing was the WRONG gate — the gap that matters is between RNG REQUEST and resolution, NOT between rngWord-reveal and the gameOver flip. Cite `_livenessTriggered:1231-1240`.

    - The lemma (D-02): the decimator resolution reads `rngWordByDay[gameOverDay]` (`GameOverModule:106`, fed into `runTerminalDecimatorJackpot:174-183`), where `gameOverDay` is the day liveness fires (e.g. `purchaseStartDay + 121` for the death-clock path). That key is keyed by the CURRENT day and physically cannot be written on any earlier day. On `gameOverDay` itself the day-index liveness predicate (`Storage:1231-1240`) is already `true` from the day's start (it is day-constant: `currentDay - psd > 120` / `> DEPLOY_IDLE_TIMEOUT`), so the first `advanceGame` routes straight into the game-over path (`_handleGameOverPath:591` → `handleGameOverDrain:665-670`) and THAT is what writes/consumes the word (`_applyDailyRng:1879` is the only daily-word write). Conclude: ANY boost that passed `!liveness` necessarily occurred on an EARLIER day, before the resolution word could exist → the resolution word is always a FUTURE-DAY word relative to any admitted boost. The boost + promotion are therefore deterministic from the player's fixed effective-streak factor × their burn, committed BEFORE any randomness is revealed → the player cannot use draw knowledge to manipulate placement (bucket OR weight).

    - The VRF-grace-stall branch (D-02): handle it explicitly — the stalled day's word stays 0; the game is RNG-locked so no new daily words are written during the stall; the decimator path requires a REAL VRF word (`handleGameOverDrain` reverts on `rngWordByDay[day]==0`) so there is no predictable-fallback-seed hole. RECONCILE the verified drift: at `1e7a646d` the `rngWord = rngWordByDay[day]` read at `:106` is INSIDE an `if (preRefundAvailable != 0)` guard — state that the decimator pool branch (`runTerminalDecimatorJackpot(decPool,lvl,rngWord)`) still depends on a real word, and document whether the guard weakens the lemma (it does not: the future-day-word property holds for the key regardless of the read-guard; if no funds are distributable the decimator draw is moot). Record this reconciliation explicitly.

    - The retracted refinement (D-03): record that the earlier "same-day rngWord reuse" concern (gate on `rngWordByDay[currentDay]==0`) was investigated and RETRACTED as internally inconsistent (for liveness to be true in the evening it was true that morning, so any "normal advance" that morning would already have routed to game-over; there is no separate pre-set word). Record the BELT-AND-SUSPENDERS fallback: IF this formal pass surfaces a backlog/catch-up edge where game-over processes a day whose word was pre-set, AND-in a cheap `rngWordByDay[resolutionDay]==0` gate; default = `!liveness` alone (USER). State explicitly whether the formal pass surfaced such an edge (it does not — record the conclusion).

    - Invariant re-attestation: the `require(!gameOver)` / `!liveness` gate + the pool-finalized-in-resolution + the shares-sum-to-pool (D-07 conservation) invariants are re-attested; cross-reference the TDEC-02 aggregate re-key. Note SEC-01 (RNG) is OWNED at TST 361; this proof is the SPEC's design gate, and 362 adversarial-probes the bucket-promotion freeze.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "TDEC-03 — Freeze-Safety Proof" "$f" && grep -Eqi "future.?day.?word" "$f" && grep -q "_livenessTriggered" "$f" && grep -Eq "gameOverDay" "$f" && grep -Eq "preRefundAvailable" "$f" && grep -Eqi "VRF.?grace|grace.?period|stall" "$f" && grep -Eqi "retract" "$f" && grep -Eq "D-01" "$f" && grep -Eq "D-02" "$f" && grep -Eq "D-03" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `358-SPEC.md` contains a `## TDEC-03 — Freeze-Safety Proof` section that DISCHARGES (not asserts) the future-day-word lemma via labelled steps.
    - The proof states the obligation (general "all weight+bucket mutation precedes the draw" rule replacing the obsolete `subBucket`-fixed simplification), locks the `require(!_livenessTriggered())`-alone gate (D-01, cited `:1231-1240`), discharges the future-day-word lemma (D-02, cites `GameOverModule:106`+`:174-183` and `_applyDailyRng:1879`), handles the VRF-grace-stall branch, and RECONCILES the verified `:106` `preRefundAvailable != 0` guard drift explicitly.
    - The retracted same-day-reuse refinement (D-03) is recorded with the belt-and-suspenders fallback and the conclusion (no backlog edge surfaced; default = `!liveness` alone).
    - The shares-sum-to-pool / pool-finalized-in-resolution invariants are re-attested; SEC-01 noted as OWNED at TST 361.
    - ZERO `contracts/*.sol` files modified.
  </acceptance_criteria>
  <done>The TDEC-03 freeze-safety proof rigorously discharges the future-day-word lemma (D-01/D-02/D-03) including the `:106` guard-drift reconciliation, locked in `358-SPEC.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (DESIGN-LEVEL — paper-only SPEC phase; zero new code-attack-surface since zero contracts change)

| Boundary | Description |
|----------|-------------|
| terminal-decimator player → `boostTerminalDecimator()` (NEW entrypoint, built at IMPL) | The player is the untrusted EV-seeking actor attempting to time the boost (or its bucket promotion) to use draw knowledge; the SPEC must design the gate so all weight+bucket mutation provably precedes the RNG word that determines the draw. |
| RNG REQUEST → resolution gap (the freeze spine) | The canonical freeze invariant: no player-controlled mutation of VRF-derived placement between the request and resolution; the bucket promotion is a NEW weight-AND-bucket write that must land strictly before the request. |
| `terminalDecBucketBurnTotal` aggregate (solvency-of-shares) | Re-keying on promotion must conserve total weight so `runTerminalDecimatorJackpot` shares still sum to the pool (no over- or under-allocation). |

## STRIDE Threat Register (design assertions the SPEC LOCKS; PROVEN at TST 361 / adversarially at TERMINAL 362)

| Threat ID | Category | Component | Disposition | Mitigation Plan (design assertion) |
|-----------|----------|-----------|-------------|------------------------------------|
| T-358-01 | Tampering (RNG/freeze) | boost or bucket-promotion timed to exploit draw knowledge | mitigate | The future-day-word lemma (TDEC-03): the resolution word `rngWordByDay[gameOverDay]` cannot exist before the `!_livenessTriggered()` gate closes; any admitted boost occurred on an earlier day → all weight+bucket mutation precedes the draw. SPEC TDEC-03; TST 361 SEC-01; adversarial 362. |
| T-358-02 | Tampering (RNG) | predictable-fallback-seed hole on the VRF-grace stall | mitigate | The decimator path requires a REAL VRF word (`handleGameOverDrain` reverts on `rngWordByDay[day]==0`); no daily words written during the RNG-locked stall. SPEC TDEC-03 stall branch; the `:106` guard-drift reconciled. |
| T-358-03 | Tampering (solvency-of-shares) | aggregate re-key on promotion over/under-allocates the pool | mitigate | Remove-from-old-key / add-to-new-key conserves total weight (D-07) → `runTerminalDecimatorJackpot` shares sum to the pool; weight-only, BURNIE/ETH path untouched. SPEC TDEC-02 D-07; re-attested at the cross-cutting solvency section (plan 03). |
| T-358-04 | Spoofing | spoofed/stale streak inflates the boost factor | mitigate | Effective-streak source is `getPlayerQuestView(player).baseStreak` (gap-reset + shields applied, a `view`), NOT raw `playerQuestStates.streak` (D-09); cap 100. SPEC TDEC-02 D-09. |
| T-358-05 | Elevation (re-boost) | repeat boost to stack weight beyond intent | mitigate | One-time per terminal level via the `boosted` bit in `TerminalDecEntry` (24 spare bits); requires an existing burn (D-13). uint88 saturation (D-11) bounds the weighted value. SPEC TDEC-02 D-11/D-13. |

**Block-on-HIGH note:** Paper-only design phase — no package installs, no contract mutation. The block-on-HIGH gate is the DESIGN gate: any freeze/solvency hole the SPEC cannot close on paper is a blocking threat. T-358-01..05 are all mitigated by design assertions with named anchors; T-358-01 (the bucket-promotion freeze) is the load-bearing one and is discharged rigorously in TDEC-03, not asserted. No HIGH design hole remains open at lock for the terminal-decimator surface.
</threat_model>

<verification>
- `git diff --quiet 1e7a646d HEAD -- contracts/` is clean throughout (ZERO contract mutation — paper-only).
- `358-SPEC.md` exists and contains the header + `### Frozen-Subject Guard` + `## TDEC-02 — Terminal-Decimator Boost Mechanics` (D-04..D-13) + `## TDEC-03 — Freeze-Safety Proof` (D-01/D-02/D-03).
- TDEC-02 cites every anchor at the verified 1e7a646d line; TDEC-03 discharges the future-day-word lemma and reconciles the `:106` `preRefundAvailable != 0` guard drift.
- TDEC-02 and TDEC-03 are the ONLY requirement IDs claimed by this plan.
</verification>

<success_criteria>
- `358-SPEC.md` is a design-lock DRAFT whose header, frozen-subject guard, terminal-decimator mechanics (TDEC-02 / D-04..D-13), and freeze-safety proof (TDEC-03 / D-01..D-03) are complete and IMPL-ready, leaving the remaining sections for plans 02 and 03.
- Every cited `file:line` is reconciled vs `1e7a646d` (the `:106` guard drift recorded); no "by construction" survives un-cited.
- ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-01-SUMMARY.md` when done.
</output>
