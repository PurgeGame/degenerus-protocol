---
phase: 70-coinflip-commitment-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.8-commitment-window-inventory.md
autonomous: true
requirements: [COIN-01, COIN-02]

must_haves:
  truths:
    - "Every state transition in the coinflip lifecycle (deposit -> resolution -> claim) is documented with contract, function, line number, and storage writes"
    - "All 4 coinflip resolution code paths (normal daily, gap-day backfill, game-over entropy, game-over fallback) are traced with their RNG word source"
    - "All 10 BurnieCoinflip external entry points have a SAFE/VULNERABLE verdict with guard analysis for both daily and mid-day commitment windows"
    - "Cross-contract interactions (boon consumption, BAF recording, quest handling) during coinflip operations are assessed for commitment window impact"
  artifacts:
    - path: "audit/v3.8-commitment-window-inventory.md"
      provides: "Coinflip lifecycle trace (COIN-01) and commitment window analysis (COIN-02) appended after Phase 69 content"
      contains: "## Phase 70: Coinflip Commitment Window"
  key_links:
    - from: "audit/v3.8-commitment-window-inventory.md (Phase 70 section)"
      to: "contracts/BurnieCoinflip.sol"
      via: "Line number citations for every state transition and guard"
      pattern: "BurnieCoinflip:\\d+"
    - from: "audit/v3.8-commitment-window-inventory.md (Phase 70 section)"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "Line number citations for rngGate, _applyDailyRng, processCoinflipPayouts call"
      pattern: "AdvanceModule:\\d+"
---

<objective>
Trace the complete coinflip RNG lifecycle and analyze all player-controllable state during the commitment window.

Purpose: COIN-01 requires a full lifecycle trace (bet -> RNG -> resolution -> payout) with every state transition. COIN-02 requires per-function commitment window analysis for all BurnieCoinflip entry points. Together these form the evidence base for the multi-tx attack modeling in Plan 02.

Output: Two new sections appended to audit/v3.8-commitment-window-inventory.md -- "Coinflip Lifecycle Trace" and "Coinflip Commitment Window Analysis"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-coinflip-commitment-window/70-RESEARCH.md
@.planning/phases/69-mutation-verdicts/69-01-SUMMARY.md

Key prior findings consumed by this plan:
- Phase 69: All 51 VRF-touched variables SAFE (including 6 BurnieCoinflip variables)
- Phase 69: 87 permissionless paths enumerated with 7 protection mechanisms
- Phase 69: depositCoinflip intentionally unguarded (day+1 keying)
- Phase 69: bountyOwedTo dual protection (rngLocked + outcome-independence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write coinflip lifecycle trace (COIN-01)</name>
  <files>audit/v3.8-commitment-window-inventory.md</files>
  <read_first>
    - audit/v3.8-commitment-window-inventory.md (current end-of-file to see where to append -- read last 30 lines)
    - contracts/BurnieCoinflip.sol (full file -- 1129 lines, the primary audit target)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 770-810 (rngGate calling processCoinflipPayouts)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1440-1460 (rawFulfillRandomWords)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1510-1540 (_applyDailyRng -- nudge application)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 845-880 (_backfillGapDays -- gap-day coinflip resolution)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 855-870 (_gameOverEntropy -- game-over coinflip path)
    - contracts/DegenerusGame.sol lines 500-510 (currentDayView)
    - contracts/storage/DegenerusGameStorage.sol lines 1260-1270 (_simulatedDayIndex)
  </read_first>
  <action>
Append a new section to audit/v3.8-commitment-window-inventory.md after the Phase 69 content. The section header is:

```
## Phase 70: Coinflip Commitment Window

### Section 1: Coinflip Lifecycle Trace (COIN-01)
```

Write the lifecycle trace with these exact sub-sections:

**1.1 State Transition Table**
Create a structured table with columns: From State | To State | Trigger | Guard | Storage Writes | Line Reference

Trace these transitions (derive exact line numbers from contract source):
- Idle -> Bet Placed: depositCoinflip() -> _depositCoinflip() -> burnForCoinflip() -> handleFlip() -> _addDailyFlip()
  - Storage writes: coinflipBalance[_targetFlipDay()][player], playerState (cursor update), bountyOwedTo (if record), coinflipTopByDay
  - Guard: _coinflipLockedDuringTransition (not rngLocked -- intentional per Phase 69)
- Bet Placed -> VRF Requested: advanceGame() triggers VRF request (separate tx)
- VRF Requested -> VRF Fulfilled: rawFulfillRandomWords stores rngWordCurrent (AdvanceModule:1451)
- VRF Fulfilled -> Resolved: advanceGame() -> rngGate() -> processCoinflipPayouts(bonusFlip, currentWord, epoch)
  - Storage writes: coinflipDayResult[epoch], flipsClaimableDay, currentBounty, bountyOwedTo, playerState (sDGNRS cursor)
  - Key computation: win = (rngWord & 1) == 1 (BurnieCoinflip:810), rewardPercent from seedWord % 20 (BurnieCoinflip:789-799)
- Resolved -> Claimed: claimCoinflips() -> _claimCoinflipsInternal()
  - Storage writes: playerState (cursor), coinflipBalance[epoch][player] = 0
  - Guard: none (claiming is always permitted)

**1.2 Four Resolution Code Paths**
Document each with its RNG word source:
1. Normal daily: advanceGame -> rngGate -> processCoinflipPayouts with VRF-derived currentWord
2. Gap-day backfill: _backfillGapDays -> processCoinflipPayouts with keccak256(vrfWord, gapDay) derived word
3. Game-over entropy: _gameOverEntropy -> processCoinflipPayouts with VRF word
4. Game-over fallback: _gameOverEntropy with 3-day timeout -> fallback from rngWordByDay[1]

For each path, cite the exact AdvanceModule line numbers and trace the word from VRF callback through to processCoinflipPayouts.

**1.3 Backward Trace from Outcome**
Per memory-mandated RNG audit discipline, trace BACKWARD from the win/loss determination:
- win = (rngWord & 1) == 1 at BurnieCoinflip:810
- rngWord parameter comes from processCoinflipPayouts(bonusFlip, currentWord, epoch)
- currentWord comes from rngGate local variable assigned at AdvanceModule:798
- That value is the return of _applyDailyRng which computes rawWord + totalFlipReversals
- rawWord is rngWordCurrent read at AdvanceModule:778
- rngWordCurrent was set by rawFulfillRandomWords from Chainlink VRF

- rewardPercent = range_lookup(seedWord % 20) at BurnieCoinflip:789-799
- seedWord = keccak256(abi.encodePacked(rngWord, epoch)) at BurnieCoinflip:784
- epoch is the day index (time-derived via _simulatedDayIndex, not writable storage)

Conclude: both outcome values (win/loss and rewardPercent) are pure functions of VRF word + epoch. No player-writable storage feeds into the computation.

**1.4 Temporal Separation Property**
Document the _targetFlipDay() = currentDayView() + 1 property with exact line references:
- _targetFlipDay() at BurnieCoinflip:1060-1062
- currentDayView() at DegenerusGame:506-507
- _simulatedDayIndex() at DegenerusGameStorage:1262-1264
- Proof: deposits target day N+1, resolution processes day N, no key overlap possible because currentDayView() is time-derived (not a writable storage variable)

**1.5 Nudge (reverseFlip) Impact**
Document the totalFlipReversals mechanism:
- reverseFlip increments totalFlipReversals (find exact line in AdvanceModule)
- _applyDailyRng adds totalFlipReversals to rawWord (AdvanceModule:1524-1528)
- Effect: each nudge flips bit 0 of the final word, flipping win/loss for ALL coinflip participants
- Guard: reverseFlip is guarded by rngLockedFlag (find exact line)
- Assessment: collective outcome influence by design (the feature IS "reverse flip"); protected from manipulation during commitment window by rngLockedFlag

Every line reference MUST be verified against the actual contract source read in read_first. Do NOT copy line numbers from the research document without verification -- re-derive them from the source.
  </action>
  <verify>
    <automated>grep -c "BurnieCoinflip:" audit/v3.8-commitment-window-inventory.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.8-commitment-window-inventory.md contains "## Phase 70: Coinflip Commitment Window"
    - audit/v3.8-commitment-window-inventory.md contains "### Section 1: Coinflip Lifecycle Trace (COIN-01)"
    - audit/v3.8-commitment-window-inventory.md contains a state transition table with columns "From State" and "To State" and "Storage Writes"
    - audit/v3.8-commitment-window-inventory.md contains "Four Resolution Code Paths" subsection
    - audit/v3.8-commitment-window-inventory.md contains "Backward Trace from Outcome" subsection with "win = (rngWord" text
    - audit/v3.8-commitment-window-inventory.md contains "Temporal Separation Property" subsection with "_targetFlipDay"
    - audit/v3.8-commitment-window-inventory.md contains "Nudge" or "reverseFlip" subsection with "totalFlipReversals"
    - Every line reference (BurnieCoinflip:NNN, AdvanceModule:NNN) is verified against contract source
  </acceptance_criteria>
  <done>Complete coinflip lifecycle trace with state transitions, 4 resolution paths, backward trace, temporal separation proof, and nudge impact -- all with verified line references from contract source</done>
</task>

<task type="auto">
  <name>Task 2: Write coinflip commitment window analysis (COIN-02)</name>
  <files>audit/v3.8-commitment-window-inventory.md</files>
  <read_first>
    - audit/v3.8-commitment-window-inventory.md (read the Phase 70 Section 1 just written by Task 1 -- last ~150 lines)
    - contracts/BurnieCoinflip.sol (full file -- 1129 lines; re-read for all external entry points and their guards)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 770-810 (rngGate flow for cross-contract context)
    - contracts/DegenerusGame.sol (grep for consumeCoinflipBoon to find boon-consumption cross-contract path)
  </read_first>
  <action>
Append a new subsection to the Phase 70 section in audit/v3.8-commitment-window-inventory.md:

```
### Section 2: Coinflip Commitment Window Analysis (COIN-02)
```

Write the commitment window analysis with these exact sub-sections:

**2.1 Per-Function Entry Point Table**
Create a table with columns: Function | Access | rngLocked Guard | Writes To | Daily Window Impact | Mid-Day Window Impact | Verdict

Assess ALL 10 BurnieCoinflip external entry points (from research):
1. depositCoinflip() -- permissionless, NO rngLocked guard, writes coinflipBalance[day+1], playerState, bountyOwedTo (if record with rngLocked check at line 645), coinflipTopByDay
2. claimCoinflips() -- permissionless, NO rngLocked guard, writes playerState (cursor), coinflipBalance (cleared)
3. claimCoinflipsFromBurnie() -- onlyBurnieCoin, NO rngLocked guard, same writes as claimCoinflips
4. claimCoinflipsForRedemption() -- onlySDGNRS, NO rngLocked guard, same writes
5. consumeCoinflipsForBurn() -- onlyBurnieCoin, NO rngLocked guard, same writes
6. setCoinflipAutoRebuy() -- permissionless, YES rngLocked guard (line 706), writes playerState (auto-rebuy config, carry)
7. setCoinflipAutoRebuyTakeProfit() -- permissionless, YES rngLocked guard (line 756), writes playerState (takeProfit)
8. settleFlipModeChange() -- onlyDegenerusGame, NO rngLocked guard, writes playerState (claimableStored)
9. creditFlip() -- onlyFlipCreditors, NO rngLocked guard, writes coinflipBalance[day+1]
10. creditFlipBatch() -- onlyFlipCreditors, NO rngLocked guard, writes coinflipBalance[day+1]

For each function, derive the exact line numbers of the rngLocked guard (or its absence) from the contract source. Verify access control modifiers from the function signature.

For each SAFE verdict, state the specific protection:
- day+1 keying (deposits target tomorrow, resolution processes today)
- rngLockedFlag (blocks during daily window)
- Access control (onlyBurnieCoin, onlySDGNRS, onlyFlipCreditors, onlyDegenerusGame)
- Outcome irrelevance (writes don't feed into win/loss computation)

**2.2 Cross-Contract Interactions**
Analyze these cross-contract paths during coinflip operations:

1. Boon consumption: depositCoinflip -> handleFlip -> _addDailyFlip reads/consumes coinflip boon
   - Find the exact code path where consumeCoinflipBoon is called
   - Assess: boon affects stake size for TOMORROW's flip (day+1), not the day being resolved
   - Verdict: SAFE (temporal separation)

2. BAF recording: _claimCoinflipsInternal -> jackpots.recordBafFlip()
   - Find the exact line where recordBafFlip is called
   - Verify the sDGNRS exclusion: player != ContractAddresses.SDGNRS check (find line)
   - Assess: during processCoinflipPayouts (daily resolution), sDGNRS auto-claim calls _claimCoinflipsInternal with SDGNRS address, which skips BAF recording
   - For player-initiated claims during rngLocked: _coinflipLockedDuringTransition blocks deposits at BAF-relevant levels
   - Verdict with specific guard citations

3. Quest handling: check if _claimCoinflipsInternal or depositCoinflip interact with quest system
   - Find any quest-related calls in BurnieCoinflip.sol (grep for "quest" or "Quest")
   - Assess commitment window impact

**2.3 Open Question Resolution: Game-Over Deposit Blocking**
Resolve the open question from research:
- Trace depositCoinflip -> _depositCoinflip -> _addDailyFlip to check for game-over guards
- Check if advanceGame/gameover processing blocks deposits via some other mechanism
- If deposits are NOT blocked during game-over: assess whether this matters (can a player deposit after game-over but before fallback resolution with the predictable rngWordByDay[1]?)
- Provide a verdict with C4A severity if relevant

**2.4 Open Question Resolution: sDGNRS Auto-Claim During processCoinflipPayouts**
Resolve the open question from research:
- Verify that _claimCoinflipsInternal(ContractAddresses.SDGNRS, false) at line 861 cannot revert during processCoinflipPayouts
- Check: does the BAF exclusion at line 556 prevent any rngLocked-dependent code from executing?
- Provide a verdict

**2.5 Commitment Window Verdict Summary**
Summary table: Function | Verdict | Primary Protection | Notes
All 10 functions listed with final verdicts.

Every line reference MUST be verified against the actual contract source.
  </action>
  <verify>
    <automated>grep -c "Verdict" audit/v3.8-commitment-window-inventory.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.8-commitment-window-inventory.md contains "### Section 2: Coinflip Commitment Window Analysis (COIN-02)"
    - audit/v3.8-commitment-window-inventory.md contains a table with all 10 BurnieCoinflip entry points (depositCoinflip, claimCoinflips, claimCoinflipsFromBurnie, claimCoinflipsForRedemption, consumeCoinflipsForBurn, setCoinflipAutoRebuy, setCoinflipAutoRebuyTakeProfit, settleFlipModeChange, creditFlip, creditFlipBatch)
    - audit/v3.8-commitment-window-inventory.md contains "Cross-Contract Interactions" subsection
    - audit/v3.8-commitment-window-inventory.md contains "Game-Over Deposit Blocking" subsection with a verdict
    - audit/v3.8-commitment-window-inventory.md contains "Commitment Window Verdict Summary" subsection
    - Each function verdict includes specific protection mechanism (day+1 keying, rngLockedFlag, access control, or outcome irrelevance)
  </acceptance_criteria>
  <done>All 10 BurnieCoinflip entry points assessed with SAFE/VULNERABLE verdict, cross-contract interactions analyzed, open questions resolved with verdicts, commitment window summary table complete</done>
</task>

</tasks>

<verification>
- All line references (BurnieCoinflip:NNN, AdvanceModule:NNN) verified against actual contract source
- State transition table covers deposit, resolution (4 paths), and claim flows
- All 10 external entry points have per-function verdicts
- Backward trace from win/loss outcome confirms pure-function property
- Cross-contract boon/BAF/quest paths assessed
- Both open questions from research resolved with evidence
</verification>

<success_criteria>
- COIN-01: Complete lifecycle trace exists with state transitions, 4 resolution paths, backward trace, temporal separation proof, and nudge impact assessment
- COIN-02: All 10 BurnieCoinflip entry points have SAFE/VULNERABLE verdicts with guard analysis for both commitment windows, cross-contract interactions assessed, open questions resolved
- All line references verified against contract source (not copied from research)
</success_criteria>

<output>
After completion, create `.planning/phases/70-coinflip-commitment-window/70-01-SUMMARY.md`
</output>
