---
phase: 73-boon-storage-packing
plan: 03
type: execute
wave: 3
depends_on: [73-01, 73-02]
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameBoonModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
autonomous: false
requirements: [BOON-06]

must_haves:
  truths:
    - "All existing Hardhat tests pass with zero failures"
    - "All existing Foundry tests pass with zero failures"
    - "forge inspect confirms boonPacked mapping exists at a slot after all existing variables"
    - "No old boon mapping slot numbers have shifted"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Final verified storage layout with BoonPacked"
    - path: "contracts/modules/DegenerusGameBoonModule.sol"
      provides: "Verified BoonModule with passing tests"
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Verified LootboxModule with passing tests"
    - path: "contracts/modules/DegenerusGameWhaleModule.sol"
      provides: "Verified WhaleModule with passing tests"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "Verified MintModule with passing tests"
  key_links:
    - from: "test/unit/DegenerusGame.test.js"
      to: "contracts/modules/DegenerusGameBoonModule.sol"
      via: "Hardhat test exercising consumeCoinflipBoon and consumeDecimatorBoon"
      pattern: "consumeCoinflipBoon|consumeDecimatorBoon"
    - from: "test/edge/WhaleBundle.test.js"
      to: "contracts/modules/DegenerusGameWhaleModule.sol"
      via: "Hardhat test exercising whale boon pricing, lazy pass pricing, lootbox boost"
      pattern: "whale.*boon|lootbox.*boost|lazy.*pass"
---

<objective>
Run the full test suite (Hardhat + Foundry), verify storage layout integrity via forge inspect, and fix any test failures caused by the packed storage migration.

Purpose: BOON-06 requires all existing tests pass with equivalent behavior after the storage layout change. This is the final validation gate.

Output: Green test suite across both frameworks. Storage layout verified. Any test fixes committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-boon-storage-packing/73-RESEARCH.md
@.planning/phases/73-boon-storage-packing/73-01-SUMMARY.md
@.planning/phases/73-boon-storage-packing/73-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite, verify storage layout, fix failures</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameBoonModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (verify BoonPacked struct and mapping placement)
    - contracts/modules/DegenerusGameBoonModule.sol (verify packed implementation)
    - .planning/phases/73-boon-storage-packing/73-01-SUMMARY.md (what was done in plan 01)
    - .planning/phases/73-boon-storage-packing/73-02-SUMMARY.md (what was done in plan 02)
    - .planning/phases/73-boon-storage-packing/73-RESEARCH.md (validation architecture section, test file list)
  </read_first>
  <action>
Execute verification in this order:

**Step 1: Storage layout verification via forge inspect**

Run: `forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout --pretty 2>&1 | grep -E 'boonPacked|coinflipBoonDay|lootboxBoon5Active|deityPassBoonTier|lazyPassBoonDay'`

Verify:
- `boonPacked` mapping exists at a slot number HIGHER than all existing variables
- `coinflipBoonDay` still at slot 25
- `lootboxBoon5Active` still at slot 26
- `deityPassBoonTier` still at slot 85
- `lazyPassBoonDay` still at slot 93
- NO slot number has changed from the original layout documented in RESEARCH.md

If any slot has shifted, STOP. The old mapping declarations were likely modified or removed. Check DegenerusGameStorage.sol and fix before proceeding.

**Step 2: Run Hardhat tests (primary behavioral verification)**

Run: `npx hardhat test 2>&1`

This runs all Hardhat test files including:
- `test/unit/DegenerusGame.test.js` -- consumeCoinflipBoon, consumeDecimatorBoon access control
- `test/edge/WhaleBundle.test.js` -- whale boon pricing, lazy pass pricing, lootbox boost consumption, deity boon issuance and expiry
- `test/access/AccessControl.test.js` -- boon function access control
- `test/validation/PaperParity.test.js` -- boon reward parity

If any test fails, analyze the failure:

Common failure categories and fixes:
1. **Tier encoding mismatch**: A test expects exact BPS value but tier decode returns different value. Check the tier-to-BPS mapping tables in RESEARCH.md. Fix the decode helper if wrong.
2. **Lootbox boost upgrade semantics**: The old code uses 3 independent bools; the new code uses max(old, new) tier comparison. If a test sets 5% then upgrades to 15%, verify tier 2 > tier 1 comparison works.
3. **Deity day expiry**: If a test checks deity boon expiry on day change, verify the packed deityDay field is being compared correctly (uint24 vs uint48 truncation).
4. **Decimator no-day-expiry**: If a test expects decimator boon to persist across days (no time expiry), verify consumeDecimatorBoost only checks deity day, not stamp day.
5. **checkAndClearExpiredBoon return value**: If a test checks the hasAnyBoon return, verify the packed version checks the same set of fields as the original.
6. **Stale deity day clearing for inactive boons**: The original checkAndClearExpiredBoon clears stale deity days even for inactive lootbox boons (e.g., lootboxBoon25Active is false but deityLootboxBoon25Day is stale). The packed version must handle this for the single lootbox tier.

For each failure: read the test, understand what behavior it expects, trace through the packed code to find the discrepancy, fix the packed code (NOT the test, unless the test was testing an implementation detail like individual mapping reads).

**Step 3: Run Foundry tests**

Run: `forge test 2>&1`

This runs all Foundry fuzz tests including:
- `test/fuzz/PrecisionBoundary.t.sol` -- boon budget precision
- `test/fuzz/DeployCanary.t.sol` -- boon module deployment address

If Foundry tests fail, analyze similarly. Foundry fuzz tests may expose edge cases that Hardhat unit tests miss (e.g., max uint24 day values, zero-address handling).

**Step 4: If all tests pass, verify dead code**

Run: `grep -rn 'coinflipBoonBps\[' contracts/modules/ contracts/DegenerusGame.sol 2>/dev/null | grep -v '//' | grep -v '@deprecated'`

This should return ZERO matches -- no live code path should read or write the old individual boon mappings. If any matches found, they are bugs (code paths that were missed in Plans 01-02).

Similarly check: `grep -rn 'lootboxBoon5Active\[' contracts/modules/ 2>/dev/null | grep -v '//'`

**Step 5: Report results**

Document: total tests run, pass/fail counts, any fixes applied, storage layout verification result.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test 2>&1 | tail -3 && forge test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` output ends with `X passing` and `0 failing`
    - `forge test` output ends with all tests passing (no FAIL lines)
    - `forge inspect` shows boonPacked at a slot after terminalDecBucketBurnTotal
    - `forge inspect` shows coinflipBoonDay still at slot 25, lootboxBoon5Active still at slot 26
    - `grep -rn 'coinflipBoonBps\[' contracts/modules/` returns no uncommented matches
    - `grep -rn 'lootboxBoon5Active\[' contracts/modules/` returns no uncommented matches
  </acceptance_criteria>
  <done>Full Hardhat + Foundry test suite green. Storage layout verified via forge inspect (boonPacked at correct slot, all old slots unchanged). No dead code references to old boon mappings in any module.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of boon storage packing migration</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameBoonModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameMintModule.sol</files>
  <action>Present the migration results for human review. All automated tests passed in Task 1.</action>
  <what-built>Complete boon storage packing migration: 29 per-player boon mappings replaced with 2-slot packed struct across 5 contracts (DegenerusGameStorage, BoonModule, LootboxModule, WhaleModule, MintModule). All existing tests pass.</what-built>
  <how-to-verify>
    1. Review the BoonPacked struct layout in contracts/storage/DegenerusGameStorage.sol -- verify 2-slot design matches RESEARCH.md specification
    2. Spot-check one consume function (e.g., consumeCoinflipBoon in DegenerusGameBoonModule.sol) -- verify read-modify-write pattern with single SLOAD/SSTORE
    3. Spot-check _applyBoon lootbox boost branch in DegenerusGameLootboxModule.sol -- verify single tier field replaces 3 bool flags (BOON-05)
    4. Verify test results: `npx hardhat test && forge test` both green
    5. Verify storage: `forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout --pretty | grep boonPacked` shows the mapping at the expected slot
  </how-to-verify>
  <verify>Human confirms migration correctness by reviewing struct layout and test results</verify>
  <done>Human approved the boon storage packing migration</done>
  <resume-signal>Type "approved" to confirm the storage packing migration is correct, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npx hardhat test` -- all tests pass
2. `forge test` -- all tests pass
3. `forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout --pretty | grep boonPacked` -- mapping exists
4. `forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout --pretty | grep coinflipBoonDay` -- slot 25 (unchanged)
5. No old boon mapping references in live code paths
</verification>

<success_criteria>
- All Hardhat tests pass (0 failures)
- All Foundry tests pass (0 failures)
- Storage layout integrity confirmed via forge inspect
- No old boon mapping slot numbers shifted
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/73-boon-storage-packing/73-03-SUMMARY.md`
</output>
