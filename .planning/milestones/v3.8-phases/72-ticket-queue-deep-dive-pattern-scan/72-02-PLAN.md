---
phase: 72-ticket-queue-deep-dive-pattern-scan
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.8-commitment-window-inventory.md
autonomous: true
requirements:
  - TQ-03

must_haves:
  truths:
    - "Every VRF-dependent outcome computation category (10 categories from Phase 68) has been scanned for state reads from permissionless-writable storage during commitment window"
    - "Each scanned state read has a verdict: SAFE (with protection mechanism) or VULNERABLE (with exploitation path)"
    - "The _tqWriteKey pattern has been grep-searched across ALL contracts and modules to find any other instances of write-buffer reads during outcome computation"
    - "A summary table exists listing all findings with per-finding verdicts"
  artifacts:
    - path: "audit/v3.8-commitment-window-inventory.md"
      provides: "Phase 72 Section 3 appended (TQ-03 pattern scan)"
      contains: "Cross-Contract Pattern Scan"
  key_links:
    - from: "audit/v3.8-commitment-window-inventory.md"
      to: "contracts/modules/*.sol"
      via: "Exhaustive grep of all VRF-dependent state reads cross-referenced with permissionless writers"
      pattern: "_tqWriteKey\\|_tqReadKey"
---

<objective>
Systematically scan all contracts for commitment window violations similar to the ticket queue _tqWriteKey bug (TQ-03).

Purpose: The _tqWriteKey bug was missed by Phase 69's 87-path exhaustive proof because the proof verified protection mechanisms exist without verifying the correct buffer key was used. A pattern scan ensures no similar seam bugs exist where VRF-dependent outcome computation reads mutable state that is not actually protected during the commitment window.

Output: Section 3 appended to audit/v3.8-commitment-window-inventory.md with per-category scan results and a summary findings table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ticket-queue-deep-dive-pattern-scan/72-RESEARCH.md

<interfaces>
<!-- The 10 VRF-dependent outcome computation categories from Phase 68/Research -->

Category 1: ETH jackpot winners (payDailyJackpot) -- JackpotModule
Category 2: BURNIE jackpot winners (payDailyJackpotCoinAndTickets, payDailyCoinJackpot) -- JackpotModule
Category 3: Lootbox index assignment -- AdvanceModule/LootboxModule
Category 4: Coinflip resolution -- BurnieCoinflip
Category 5: Redemption roll -- StakedDegenerusStonk
Category 6: Degenerette resolution -- DegeneretteModule
Category 7: Gap day backfill -- AdvanceModule
Category 8: Ticket processing trait generation -- JackpotModule
Category 9: Reward jackpots -- JackpotModule
Category 10: Final day DGNRS reward -- JackpotModule

<!-- Key contracts to scan (from research) -->
Core: DegenerusGame.sol, DegenerusAdmin.sol
Storage: DegenerusGameStorage.sol
Modules (12): AdvanceModule, BoonModule, DecimatorModule, DegeneretteModule, EndgameModule, GameOverModule, JackpotModule, LootboxModule, MintModule, MintStreakUtils, PayoutUtils, WhaleModule
Token: BurnieCoinflip.sol, BurnieCoin.sol, DegenerusStonk.sol, StakedDegenerusStonk.sol
Other: DegenerusAffiliate.sol, DegenerusDeityPass.sol, DegenerusJackpots.sol, DegenerusQuests.sol, DegenerusTraitUtils.sol, DegenerusVault.sol, DeityBoonViewer.sol

<!-- High-priority targets from research -->
1. traitBurnTicket -- populated by processTicketBatch, read during jackpot. LIKELY SAFE.
2. autoRebuyState -- read during jackpot credit flow. SAFE (setAutoRebuy has rngLockedFlag guard at DegenerusGame:1494).
3. deityBySymbol -- virtual deity entries in jackpot. SAFE (purchaseDeityPass has rngLockedFlag guard at WhaleModule:475).
4. currentPrizePool -- jackpot budget calculation. SAFE (prizePoolFrozen during jackpot phase).
5. Far-future queue via _tqWriteKey in sampleFarFutureTickets -- VIEW ONLY (DegenerusGame:2683), no outcome effect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-Contract Commitment Window Pattern Scan (TQ-03)</name>
  <read_first>
    - audit/v3.8-commitment-window-inventory.md (lines 1-100 for Phase 68 variable catalog; lines 1200-1500 for Phase 69 verdicts; end of file for append point)
    - contracts/modules/DegenerusGameJackpotModule.sol (all _tqWriteKey and _tqReadKey usages; payDailyJackpot, payDailyJackpotCoinAndTickets, payDailyCoinJackpot, _awardFarFutureCoinJackpot functions)
    - contracts/modules/DegenerusGameAdvanceModule.sol (advanceGame flow, _swapAndFreeze, rngGate, lootbox RNG path)
    - contracts/modules/DegenerusGameLootboxModule.sol (lootbox resolution functions)
    - contracts/BurnieCoinflip.sol (processCoinflipPayouts, coinflip resolution)
    - contracts/StakedDegenerusStonk.sol (redemption roll)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (degenerette resolution)
    - .planning/phases/72-ticket-queue-deep-dive-pattern-scan/72-RESEARCH.md (scan methodology)
  </read_first>
  <files>audit/v3.8-commitment-window-inventory.md</files>
  <action>
Append Section 3 to the Phase 72 section of the audit artifact (after the TQ-01/TQ-02 content written by Plan 01, OR after Phase 71 if Plan 01 hasn't run yet -- check the end of the file).

**Section 3: Cross-Contract Commitment Window Pattern Scan (TQ-03)**

Apply the systematic scan methodology from the research. For EACH of the 10 VRF-dependent outcome computation categories:

3.1 **Scan Methodology** -- Define precisely:
- Step 1: For each category, list every storage variable READ during outcome computation (identify by reading the function code, not from Phase 69 alone -- Phase 69 missed the _tqWriteKey distinction)
- Step 2: For each variable, identify all permissionless external functions that can WRITE it
- Step 3: For each writer, check: is it blocked by rngLockedFlag, prizePoolFrozen, double-buffer (correctly using read key), day+1 keying, or other guard?
- Step 4: If no guard AND write can influence outcome -> VULNERABLE

3.2 **Category-by-Category Scan** -- For each of the 10 categories, create a subsection:

Category 1 (ETH Jackpot -- payDailyJackpot):
- List state reads during ETH distribution
- Cross-reference with permissionless writers
- Verdict for each variable

Category 2 (BURNIE Jackpot -- payDailyJackpotCoinAndTickets):
- This is where the _tqWriteKey bug lives
- List ALL state reads including ticketQueue, traitBurnTicket, deityBySymbol, currentPrizePool
- For each: verify the protection mechanism ACTUALLY applies (don't just state "double-buffer" -- verify the correct key is used)
- Document: ticketQueue via _tqWriteKey = VULNERABLE (TQ-01); all others SAFE with specific evidence

Category 3 (Lootbox Index Assignment):
- List state reads during lootbox index assignment
- Verify lootboxRngWordByIndex is written by VRF callback only
- Check if any permissionless action can affect which lootbox index is assigned

Category 4 (Coinflip Resolution):
- Already analyzed in Phase 70 (COIN-01/02/03). Reference those findings.
- Quick re-verify: does processCoinflipPayouts read any state writable by permissionless callers during commitment window?

Category 5 (Redemption Roll):
- List state reads during redemption roll computation in StakedDegenerusStonk
- Check if any permissionless action can modify redemption-affecting state during commitment window

Category 6 (Degenerette Resolution):
- List state reads during degenerette outcome computation
- Verify all inputs are committed before VRF request

Category 7 (Gap Day Backfill):
- List state reads during _backfillGapDays
- Verify derivation uses only VRF word + gap day number (no mutable state)

Category 8 (Ticket Processing Trait Generation):
- List state reads during processTicketBatch trait generation
- Verify processTicketBatch uses _tqReadKey (already confirmed at line 1891)

Category 9 (Reward Jackpots):
- List state reads during reward jackpot distribution
- Cross-reference with permissionless writers

Category 10 (Final Day DGNRS Reward -- awardFinalDayDgnrsReward):
- List state reads during final day reward at JackpotModule:773
- Cross-reference with permissionless writers

3.3 **_tqWriteKey Pattern Grep** -- Run `grep -rn "_tqWriteKey" contracts/` and document EVERY usage. For each usage, classify:
- Used in a write context (correct -- adding to write buffer) = OK
- Used in a read context during VRF-dependent outcome computation = VULNERABLE
- Used in a view function = INFORMATIONAL (no outcome effect)
Document the grep output and per-usage classification.

3.4 **Additional Pattern: rngLockedFlag Guard Coverage** -- Run `grep -rn "rngLockedFlag" contracts/` and list all functions that DO check rngLockedFlag. Then identify permissionless functions that DON'T check it. For each unguarded permissionless function, verify whether its writes can reach VRF-dependent outcome computation. This catches the class of bug where rngLockedFlag was supposed to guard a function but was forgotten.

3.5 **Summary Findings Table** -- Create a markdown table:
| Category | Variables Scanned | Vulnerable | SAFE | Protection Mechanism |
For the one VULNERABLE finding (ticketQueue via _tqWriteKey in Category 2), reference TQ-01.

3.6 **Overall TQ-03 Verdict** -- State: "One commitment window violation found: _awardFarFutureCoinJackpot reads ticketQueue via _tqWriteKey (JackpotModule:2544). All other VRF-dependent outcome computations across all 10 categories are SAFE. Fix: change _tqWriteKey to _tqReadKey at JackpotModule:2544."
  </action>
  <verify>
    <automated>grep -c "Cross-Contract.*Pattern Scan\|Category [0-9]\|_tqWriteKey Pattern Grep\|Overall TQ-03 Verdict" audit/v3.8-commitment-window-inventory.md | grep -q -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Cross-Contract.*Pattern Scan" audit/v3.8-commitment-window-inventory.md returns at least 1 match
    - grep "Category 1\|Category 2\|Category 3\|Category 4\|Category 5" audit/v3.8-commitment-window-inventory.md returns matches in Phase 72 section (all 10 categories present)
    - grep "Category 6\|Category 7\|Category 8\|Category 9\|Category 10" audit/v3.8-commitment-window-inventory.md returns matches in Phase 72 section
    - grep "_tqWriteKey Pattern Grep\|_tqWriteKey.*grep" audit/v3.8-commitment-window-inventory.md returns at least 1 match
    - grep "rngLockedFlag Guard Coverage\|rngLockedFlag.*guard" audit/v3.8-commitment-window-inventory.md returns at least 1 match in Phase 72 section
    - grep "Overall TQ-03 Verdict" audit/v3.8-commitment-window-inventory.md returns exactly 1 match
    - grep "VULNERABLE\|SAFE" audit/v3.8-commitment-window-inventory.md returns matches in the Phase 72 Section 3 area (verdicts present)
    - grep "Summary.*Findings\|Findings.*Table" audit/v3.8-commitment-window-inventory.md returns at least 1 match in Phase 72 section
    - The scan covers all 10 VRF-dependent outcome computation categories, not just the known-vulnerable one
  </acceptance_criteria>
  <done>
    Section 3 appended to Phase 72 with: systematic scan methodology, per-category analysis for all 10 categories, _tqWriteKey grep with per-usage classification, rngLockedFlag coverage analysis, summary findings table, and overall TQ-03 verdict confirming exactly one vulnerability (JackpotModule:2544) with all other categories SAFE.
  </done>
</task>

</tasks>

<verification>
- Pattern scan section exists: `grep "Cross-Contract.*Pattern Scan" audit/v3.8-commitment-window-inventory.md`
- All 10 categories covered: `grep -c "Category [0-9]" audit/v3.8-commitment-window-inventory.md` returns 10+
- Overall verdict exists: `grep "Overall TQ-03 Verdict" audit/v3.8-commitment-window-inventory.md`
- _tqWriteKey grep documented: `grep "_tqWriteKey Pattern" audit/v3.8-commitment-window-inventory.md`
</verification>

<success_criteria>
TQ-03 is fully addressed: all 10 VRF-dependent outcome computation categories scanned with per-variable verdicts, _tqWriteKey pattern grep with per-usage classification, rngLockedFlag coverage analysis, summary findings table, and overall verdict confirming one vulnerability with fix reference.
</success_criteria>

<output>
After completion, create `.planning/phases/72-ticket-queue-deep-dive-pattern-scan/72-02-SUMMARY.md`
</output>
