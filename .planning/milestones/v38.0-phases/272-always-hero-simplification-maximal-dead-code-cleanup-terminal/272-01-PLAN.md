---
phase: 272-always-hero-simplification-maximal-dead-code-cleanup-terminal
phase_number: 272
plan: 272-01
plan_id: 272-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - test/stat/DegenerettePerNEvExactness.test.js
  - test/stat/DegeneretteBonusEv.test.js
  - test/stat/SurfaceRegression.test.js
  - test/gas/LootboxOpenGas.test.js
  - test/stat/PerPullEmptyBucketSkip.test.js
  - test/gas/Phase261GasRegression.test.js
  - test/gas/Phase264GasRegression.test.js
  - package.json
  - audit/FINDINGS-v38.0.md
  - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md
  - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
autonomous: false
milestone: v38.0
milestone_name: Always-Hero Simplification + Maximal Dead-Code Cleanup
deliverable: audit/FINDINGS-v38.0.md
phase_status: terminal
write_policy: "Multi-wave phase per CONTEXT.md D-272-COMMIT-SHAPE-01 + D-272-APPROVAL-01. Wave 1: single batched USER-APPROVED contract commit (HERO-01..05 + CLEAN-01..05). Wave 2: single batched USER-APPROVED test commit (STAT-01..02 + SURF-01..03 + LBX-02 + GASPIN-02..03 + STAT-03-v35-carry). Wave 3+: AGENT-COMMITTED atomic-per-task for audit/FINDINGS-v38.0.md, 272-01-ADVERSARIAL-LOG.md, 272-01-SUMMARY.md, REQUIREMENTS/ROADMAP/STATE/MILESTONES/PROJECT flips. Per feedback_never_preapprove_contracts.md, agent does NOT pre-approve any Wave 1 or Wave 2 commit — wait for explicit `approved` string. Final user-review gate before READ-only flip on audit/FINDINGS-v38.0.md per feedback_manual_review_before_push.md. Agent does NOT push."

requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
  - STAT-01
  - STAT-02
  - SURF-01
  - SURF-02
  - SURF-03
  - LBX-02
  - GASPIN-02
  - GASPIN-03
  - STAT-03-v35-carry
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - REG-01
  - REG-02
  - REG-03
  - REG-04

# Audit anchors (per CONTEXT.md <domain> + canonical_refs)
baseline: 2654fcc2
baseline_signal: MILESTONE_V37_AT_HEAD_2654fcc2
v36_baseline: 1c0f09132d7439af9881c56fe197f81757f8164a
v36_baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
v34_baseline: 6b63f6d4daf346a53a1d463790f637308ea8d555
v34_baseline_signal: MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555
phase_269_close: 8fd5c2e1

# Closure signal (resolved by Task 4.2 atomic-update per D-272-CLOSURE-01)
milestone_closure_signal: "MILESTONE_V38_AT_HEAD_<sha>"
milestone_closure_signal_resolution: "<sha> = `git rev-parse HEAD` AFTER Wave 3 Task 3.6 §9 attestation commit lands (the mutation-inclusive closure commit). Verbatim emission across 5 FINDINGS locations + 3 cross-document propagation targets (ROADMAP / STATE / MILESTONES) per D-272-CLOSURE-01."

# Deferred-to-planner picks (per CONTEXT.md <deferred_to_planner>)
deferred_picks:
  GASPIN-02:
    chosen: "(a-alt) dedicated `npm run test:gas` script splitting Phase261GasRegression + Phase264GasRegression off `test:stat`"
    rationale: "Option (a) refined `hardhat_reset` was already tried at Phase 269 D-269-STAB-01 with `loadFixture` and FAILED structurally (side-effect regressions). Option (c) widened-tolerance is last-resort and would document a regression in the tolerance envelope. Option (a-alt) isolates the gas-pin assertions in a fresh hardhat process via a separate npm script, sidestepping the cumulative-state drift under `test:stat` ordering. Preserves v36.0 `128k is fine approved` acceptance UNCHANGED for the gas tests themselves; only the runner separation changes. Inline doc in package.json + GASPIN-02 task description."
  LBX-02:
    chosen: "Formal re-defer to v39+ with path-of-investigation prose per Phase 266 GAS-01 + `feedback_gas_worst_case.md` precedent"
    rationale: "Phase 269 already attempted the empirical pin and was blocked by a structural fixture-coverage gap (cannot construct an openable-lootbox fixture that hits the 55%-tickets-path deterministically at the same gas envelope pre/post LBX-01 without a Phase-266-style synthetic harness). The closure-of-record at Phase 269 was an analytical worst-case derivation. Repeating the attempt at v38 without new fixture-coverage tooling will reproduce the same gap. Path-of-investigation prose (what fixture-coverage work would unlock empirical pinning) recorded inline at LBX-02 task + cross-cited from audit/FINDINGS-v38.0.md §9.NN.iv. v37.0 §9.NN.iv carry-forward register closed by FORMAL RE-DEFER (not closed by empirical pin)."
  STAT-03-v35-carry:
    chosen: "ACCEPTED-DESIGN ledger entry referencing v35.0 Phase 265 D-265-STAT03-01 fixture-calibration-error reframe"
    rationale: "Default expectation per v35.0 reframe carry. 88.24% empty-bucket skip rate is a fixture-density artifact (sparse deity-backed holder map) NOT protocol behavior. Populating a dense fixture would be a new mechanical workstream outside v38 phase scope and outside CLEAN-SCOPE narrowing. ACCEPTED-DESIGN ledger entry placed inline in `test/stat/PerPullEmptyBucketSkip.test.js` header documenting the fixture-density attribution + cross-cite to v35.0 Phase 265 reframe + v36.0/v37.0 carry-forward chain."

# Goal-backward must-haves (per ROADMAP success criteria 1-11)
must_haves:
  truths:
    - "v38.0 phase goal: Drop Degenerette hero opt-out semantics in `contracts/modules/DegenerusGameDegeneretteModule.sol`. `_packFullTicketBet` (L823-847) normalizes `heroQuadrant >= 4` to 0 unconditionally; `_resolveFullTicketBet` (L578-594) extracts `heroQuadrant` without `heroEnabled` bit check (heroBits + heroEnabled locals REMOVED); `_fullTicketPayout` (L944-994) drops `bool heroEnabled` parameter from signature + simplifies guard `if (matches >= 2 && matches < 8)`; callsite at L640-650 drops the `heroEnabled` argument; `_applyHeroMultiplier` body UNCHANGED. (Ref: HERO-01..03 + D-272-CLEAN-SCOPE-01)"
    - "Public ABI `placeDegeneretteBet(address, uint8, uint128, uint8, uint32, uint8 heroQuadrant)` signature byte-identical at v38 phase-close HEAD vs v37.0 baseline `2654fcc2`. Inputs `0xFF` and any `>= 4` accepted and normalized to 0 internally per HERO-01 (no revert). (Ref: HERO-05)"
    - "Storage layout byte-identical at v38 phase-close HEAD vs `2654fcc2`. FT_HERO_SHIFT stays 3 bits; enabled bit at FT_HERO_SHIFT + 0 becomes vestigial (always = 1 post-pack). Zero new storage slots; zero new public/external mutation entry points; zero new external pure entry points; zero new admin functions; zero new modifiers. (Ref: HERO-05 + AUDIT-04)"
    - "NatSpec rewrites describe what IS at v38 close per `feedback_no_history_in_comments.md`. L321 FT_HERO_SHIFT comment, `_packFullTicketBet` NatSpec, `_fullTicketPayout` NatSpec, L367 `@param heroQuadrant` line, and L948-953 `@param heroEnabled` line all updated or removed with ZERO comparative/historical language (no 'previously', 'v37 -> v38', 'removed', 'reduced from'). (Ref: HERO-04 + D-272-NATSPEC-DISCIPLINE-01)"
    - "Cleanup-sweep scope NARROWED to `contracts/modules/DegenerusGameDegeneretteModule.sol` ONLY per D-272-CLEAN-SCOPE-01. NO other contracts modified at v38. Discovery via planner manual grep-walk per D-272-CLEAN-DISCOVERY-01; NO `/gas-audit` orchestrator spawn. Each removal carries inline design-intent trace per `feedback_design_intent_before_deletion.md`. Likely candidates (planner verifies via grep): MASK_3 constant L347 (sole callsite at L592 deleted by HERO-02); L592-594 heroBits/heroEnabled extraction block; L948-953 `bool heroEnabled` parameter + `@param heroEnabled` NatSpec; L984 `heroEnabled &&` guard arm; L367 `@param heroQuadrant` rewrite; L321 FT_HERO_SHIFT comment. (Ref: CLEAN-01..05)"
    - "Single USER-APPROVED batched contract commit lands per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` covering HERO-01..05 + CLEAN-01..05 per D-272-COMMIT-SHAPE-01. Commit body includes full per-requirement attribution + per-CLEAN-NN file:line + 1-line design-intent trace per removal + bytecode delta + gas-per-spin delta + storage-layout grep proof. (Ref: CLEAN-06 + D-272-APPROVAL-01)"
    - "Hero EV-neutrality preserved across all (M, N) under always-on hero schedule. `test/stat/DegenerettePerNEvExactness.test.js` STAT-01 re-pins basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N (JS mirrors at L255-293 + L430 drop `heroEnabled` parameter). `test/stat/DegeneretteBonusEv.test.js` STAT-02 re-pins hero EV-neutrality within +/-1% at >=100K hero-active draws/N (JS mirrors at L207-302 + L460+ drop `heroEnabled`; hero-off baseline run DROPPED). (Ref: STAT-01..02)"
    - "v38.0 SURF-01..03 describe extension added to `test/stat/SurfaceRegression.test.js`. SURF-01 asserts byte-identity vs v37.0 baseline `2654fcc2` of `contracts/libraries/EntropyLib.sol` + `contracts/DegenerusTraitUtils.sol` + `contracts/modules/DegenerusGameJackpotModule.sol` + `contracts/modules/DegenerusGameMintModule.sol`. SURF-02 asserts byte-identity vs `2654fcc2` of `contracts/modules/DegenerusGameLootboxModule.sol`. SURF-03 rebases `V36_BASELINE = '1c0f0913...'` -> `PHASE_269_CLOSE_BASELINE = '8fd5c2e1...'` (post-LBX-01 HEAD) per v37+ carry. (Ref: SURF-01..03)"
    - "v37+ carry-forward picks recorded inline: GASPIN-02 (a-alt) dedicated `npm run test:gas` script split; LBX-02 formal re-defer with path-of-investigation prose per Phase 266 GAS-01 precedent; STAT-03-v35-carry ACCEPTED-DESIGN ledger entry. Each pick rationale documented in plan frontmatter `deferred_picks` + inline at task. Clean `npm run test:stat` start-to-finish at v38 HEAD with zero flaky failures (GASPIN-03 verification). (Ref: LBX-02 + GASPIN-02..03 + STAT-03-v35-carry)"
    - "`audit/FINDINGS-v38.0.md` exists at phase close, FINAL READ-only (chmod 444 + `status: FINAL — READ-ONLY` + `read_only: true` in YAML), with all 9 sections populated. §3.A AUDIT-01 delta-surface table enumerates HERO-01..05 + each CLEAN-NN removal with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification. §3.A row for HERO explicitly notes D-272-CLEAN-SCOPE-01 scope narrowing; §3.A row for CLEAN cites manual grep-walk method per D-272-CLEAN-DISCOVERY-01 (NOT /gas-audit orchestrator). (Ref: AUDIT-01)"
    - "§4 5-7 surface adversarial sweep verdicts every Phase 272 surface SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE per CONTEXT.md <specifics>: (a) hero always-on EV-neutrality preserved across (M, N); (b) hero quadrant 0 default does NOT create payout-bias; (c) each cleanup removal preserves invariant it guarded (inline design-intent trace); (d) storage layout byte-identical; (e) public ABI byte-identical; optional (f) variance impact bound on risk-averse subset; optional (g) clean `npm run test:stat` at v38 close. Plus 1-line RNG commitment-window degenerate-PASS attestation per `feedback_rng_commitment_window.md` (zero RNG-path mutation at v38). Default zero F-38-NN finding blocks. (Ref: AUDIT-02 + AUDIT-03 + AUDIT-04)"
    - "§5 regression appendix: REG-01 v37.0 closure signal `MILESTONE_V37_AT_HEAD_2654fcc2` NON-WIDENING (EntropyLib + JackpotModule + MintModule + TraitUtils byte-identical at v38 HEAD; Lootbox unchanged except any incidentally-found cleanup which is OUT OF SCOPE per D-272-CLEAN-SCOPE-01 so byte-identical); REG-02 v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4` NON-WIDENING (TraitUtils existing functions + `_pickSoloQuadrant` + JackpotBucketLib byte-identical); REG-03 EXC-04 RE_VERIFIED NARROWS-retained (EntropyLib byte-identical at v38 HEAD preserves BAF-jackpot-only scope) via backward-trace per `feedback_rng_backward_trace.md`; EXC-01..03 RE_VERIFIED NEGATIVE-scope at v38; REG-04 prior-finding spot-check across audit/FINDINGS-v25.0..v37.0 for v38-touched function set (`_fullTicketPayout`, `_packFullTicketBet`, `_applyHeroMultiplier`, `_resolveFullTicketBet`, MASK_3). (Ref: REG-01..04)"
    - "§6 KI gating walk: §6a Non-Promotion Ledger zero rows (default); §6b 4-row KI envelope re-verifications (EXC-01..03 NEGATIVE-scope at v38; EXC-04 RE_VERIFIED NARROWS retained); §6c verdict summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per D-272-KI-01. KNOWN-ISSUES.md UNMODIFIED at v38 phase-close HEAD unless adversarial pass surfaces FINDING_CANDIDATE requiring explicit user disposition. (Ref: AUDIT-04 + AUDIT-05)"
    - "§8 Forward-Cite Closure emits zero forward-cites per D-272-FCITE-01 terminal-phase invariant. Grep recipe: `! grep -rE 'Phase (273|274|275)|v39\\.0|v40\\.0' audit/FINDINGS-v38.0.md .planning/phases/272-*/` returns no matches. (Ref: D-272-FCITE-01)"
    - "§9 closure-attestation TWO-subsection format per D-272-CLOSURE-02: §9.NN.i USER-APPROVED contracts (Wave 1 batched commit) + §9.NN.ii USER-APPROVED tests (Wave 2 batched commit) + §9.NN.iii AGENT-COMMITTED audit artifacts (Wave 3+ atomic-commit chain); NO §9.NN.iv awaiting-approval subsection. §9c emits closure signal `MILESTONE_V38_AT_HEAD_<sha>` verbatim in 5 FINDINGS locations (frontmatter `closure_signal:` + §2 Closure Verdict Summary + §9c trailing line + §9.NN attestation block + Executive Summary anchor sentence) + 3 cross-document targets (ROADMAP / STATE / MILESTONES). (Ref: AUDIT-05 + D-272-CLOSURE-01..02)"
    - "3-skill PARALLEL adversarial pass per D-272-ADVERSARIAL-01 spawned via SINGLE message AFTER §4 draft is finished: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst`. NOT `/degen-skeptic`. Logged in `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md` with 3 H2 sections (one per skill) + Disposition note. If any skill surfaces FINDING_CANDIDATE -> escalate to user per `feedback_wait_for_approval.md` + D-272-KI-01 deviation procedure. (Ref: AUDIT-06)"
    - "USER-APPROVED batched gates honored: Wave 1 contract diff presented at Task 1.4 — explicit `approved` string awaited before commit; Wave 2 test diff presented at Task 2.7 — explicit `approved` string awaited before commit; Final user-review gate at Task 4.7 before READ-only flip on `audit/FINDINGS-v38.0.md`. Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. (Ref: CLEAN-06 + AUDIT-05 + D-272-APPROVAL-01)"

  artifacts:
    - path: contracts/modules/DegenerusGameDegeneretteModule.sol
      provides: "MODIFIED in single Wave 1 USER-APPROVED batched commit. HERO-01: `_packFullTicketBet` (L823-847) normalizes `heroQuadrant >= 4` to 0 + unconditional pack. HERO-02: `_resolveFullTicketBet` (L592-594) drops heroBits + heroEnabled locals, extracts quadrant directly. HERO-03: `_fullTicketPayout` (L944-953) drops `bool heroEnabled` param + L984 guard simplified + L640-650 callsite drops `heroEnabled` arg. HERO-04: NatSpec rewrites at L321 + L367 + `_packFullTicketBet` block + `_fullTicketPayout` block + drop L948-953 `@param heroEnabled` line. CLEAN-01..05: MASK_3 constant (L347), heroBits extraction block (L592-594), L984 `heroEnabled &&` guard arm, `bool heroEnabled` parameter, `@param heroEnabled` NatSpec line — removed per design-intent trace inline."
      contains: "uint8 effectiveQuadrant"
      contains_alt: "matches >= 2 && matches < 8"
      forbidden: "heroEnabled"
      forbidden_alt: "MASK_3"
    - path: test/stat/DegenerettePerNEvExactness.test.js
      provides: "MODIFIED. STAT-01 re-pin under always-on hero. JS mirror `jsFullTicketPayout` (L255-293) drops `heroEnabled` parameter from signature; L430 call drops `/* heroEnabled */ false` argument. basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N. Assertion: `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js` exits 0."
      forbidden: "heroEnabled"
    - path: test/stat/DegeneretteBonusEv.test.js
      provides: "MODIFIED. STAT-02 re-pin under always-on hero (drops hero-off baseline run). JS mirror `jsApplyHeroMultiplier` + `jsFullTicketPayout` (L207-302 + L460+) drop `heroEnabled`. EV-neutrality formula `P(hero|M,N) × boost + (1-P) × HERO_PENALTY = HERO_SCALE` verified at each (M, N) within +/-1% at >=100K hero-active draws/N. Assertion: `npx hardhat test test/stat/DegeneretteBonusEv.test.js` exits 0."
      forbidden: "heroEnabled"
    - path: test/stat/SurfaceRegression.test.js
      provides: "EXTENDED. v38.0 SURF-01..03 describe block appended after existing v37.0 describe (L609). PHASE_269_CLOSE_BASELINE = '8fd5c2e1...' constant added; v38 anchor constant `V37_BASELINE = '2654fcc2'`. SURF-01 byte-identity walk vs `2654fcc2` for EntropyLib + TraitUtils + JackpotModule + MintModule. SURF-02 byte-identity walk vs `2654fcc2` for LootboxModule. SURF-03 rebase: existing v37.0 SURF-03 it block at L759 changes baseline from `V36_BASELINE` -> `PHASE_269_CLOSE_BASELINE`."
      contains: "PHASE_269_CLOSE_BASELINE"
      contains_alt: "8fd5c2e1"
    - path: test/gas/LootboxOpenGas.test.js
      provides: "MODIFIED (formal re-defer prose only, no new it() block per deferred-pick rationale). Inline JSDoc header comment block appended documenting LBX-02 v37+ carry FORMAL RE-DEFER to v39+ with path-of-investigation prose: (1) what fixture-coverage gap blocks empirical pinning of 55%-tickets-path; (2) what synthetic harness work would unlock it (Phase-266-GAS-01-style deterministic fixture pre/post LBX-01); (3) analytical worst-case load-bearing per `feedback_gas_worst_case.md` carries forward unchanged. Cross-cited from audit/FINDINGS-v38.0.md §9.NN.iv."
      contains: "LBX-02"
      contains_alt: "v39+"
    - path: test/stat/PerPullEmptyBucketSkip.test.js
      provides: "MODIFIED (header documentation only, no test-body changes per deferred-pick rationale). Inline header comment block appended documenting STAT-03-v35-carry ACCEPTED-DESIGN ledger entry: 88.24% empty-bucket skip rate is fixture-density artifact (sparse deity-backed holder map) NOT protocol behavior; cross-cite to v35.0 Phase 265 D-265-STAT03-01 reframe + v36.0/v37.0 carry-forward chain. Test threshold UNCHANGED (still > 10% triggers FAIL per existing semantics); ledger entry is documentation-only attribution."
      contains: "ACCEPTED-DESIGN"
      contains_alt: "fixture-density"
    - path: package.json
      provides: "MODIFIED. GASPIN-02 (a-alt) pick: new `test:gas` script splits `test/gas/Phase261GasRegression.test.js` + `test/gas/Phase264GasRegression.test.js` + `test/gas/Phase268GasRegression.test.js` + `test/gas/LootboxOpenGas.test.js` + `test/gas/AdvanceGameGas.test.js` off the existing `test:stat` script. `test:stat` retains the stat-only suite (TraitDistribution + GoldSoloCoverage + SoloEvUplift + PackFeel + SurfaceRegression + PerPullLevelDistribution + PerPullEmptyBucketSkip + LootboxEntropyDistribution + DegenerettePerNEvExactness + DegenerettePerNEvExactness + DegeneretteProducerChi2 + DegeneretteBonusEv). GASPIN-03 verification: clean run start-to-finish for both scripts."
      contains: "test:gas"
    - path: test/gas/Phase261GasRegression.test.js
      provides: "UNMODIFIED at body. GASPIN-02 (a-alt) pick isolates this file via the new `test:gas` script (package.json change). NO test-body modifications. NO tolerance widening per `feedback_no_history_in_comments.md` discipline (which would require an inline ACCEPTED-TOLERANCE ledger entry — not needed under (a-alt) script-split path). File listed in `files_modified` only for traceability; the actual mutation is in `package.json` script-split."
    - path: test/gas/Phase264GasRegression.test.js
      provides: "UNMODIFIED at body. Same rationale as Phase261GasRegression.test.js — GASPIN-02 (a-alt) script-split moves runner; file body untouched. Listed in `files_modified` for traceability only."
    - path: audit/FINDINGS-v38.0.md
      provides: "Single canonical v38.0 milestone-closure deliverable per D-272-FILES-01. 9-section shape mirroring audit/FINDINGS-v37.0.md skeleton. §1 Frontmatter (YAML with `closure_signal: MILESTONE_V38_AT_HEAD_<sha>` + `status: FINAL — READ-ONLY` + `read_only: true` + `supersedes: none`). §2 Executive Summary (Closure Verdict + Severity Counts D-272-SEV-01 + D-08 + D-09 + Forward-Cite + Attestation Anchor). §3 Per-Phase Sections (§3a Phase 272 single-phase + §3.A AUDIT-01 delta-surface table HERO-01..05 + CLEAN-01..05 rows + §3.B AUDIT-04 zero-new-state attestation + §3.C AUDIT-03 conservation re-proof). §4 F-38-NN Finding Blocks (default zero; 5-7 surface row table covering (a)..(g) per AUDIT-02). §5 Regression Appendix (§5a REG-01 v37.0 + §5b REG-02 v34.0 + §5c REG-04 prior-finding sweep + §5d Distribution Summary). §6 KI Gating Walk (§6a Non-Promotion Ledger zero-row + §6b 4-row KI Envelope Re-Verifications + §6c Verdict Summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`). §7 Prior-Artifact Cross-Cites. §8 Forward-Cite Closure (terminal-phase zero-emission verification). §9 Milestone Closure Attestation (§9a Verdict Distribution + §9b Attestation Block + §9c Closure Signal + §9.NN TWO-subsection commit-readiness register + §9.NN.iv LBX-02 carry-forward formal re-defer). FINAL READ-only at Task 4.7."
      min_lines: 500
      contains: "MILESTONE_V38_AT_HEAD_"
      contains_alt: "## 4. F-38-NN Finding Blocks"
    - path: .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md
      provides: "Adversarial validation log capturing `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` outputs from Task 3.5 single-message PARALLEL spawn. THREE H2 sections (`## /contract-auditor`, `## /zero-day-hunter`, `## /economic-analyst`) populated with each skill's red-team output against the finished §4 inline draft. Task 3.5 Disposition note appended below skill outputs documenting any disagreement + user disposition + KI promotion candidates (default zero). Mirrors v37.0 `271-01-ADVERSARIAL-LOG.md` 3-skill format."
      min_lines: 50
    - path: .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md
      provides: "Phase-closure summary mirroring `271-01-SUMMARY.md` format (per-task atomic-commit log + per-REQ tally for 30 v38.0 requirements + cross-phase cross-cite density + project-feedback-rules-honored table + closure signal `MILESTONE_V38_AT_HEAD_<sha>`)."
      min_lines: 80
    - path: .planning/REQUIREMENTS.md
      provides: "MODIFIED. 30 v38.0 Phase 272 requirements flipped to Complete (or formally re-deferred per planner picks: LBX-02 RE-DEFERRED-V39+ with rationale; GASPIN-02 COMPLETE via (a-alt) script-split; STAT-03-v35-carry COMPLETE via ACCEPTED-DESIGN ledger entry). Traceability table updated; carry-forward register closed."
      contains: "Complete"
    - path: .planning/ROADMAP.md
      provides: "MODIFIED. v38.0 milestone row flipped to ✅ shipped with closure signal `MILESTONE_V38_AT_HEAD_<sha>`. Phase 272 entry checkbox ticked. v37.0 collapsed-summary block appended (or pre-existing carried forward). v38.0 backlog seed table updated."
      contains: "MILESTONE_V38_AT_HEAD_"
    - path: .planning/STATE.md
      provides: "MODIFIED. Milestone v38.0 marked closed; Last Shipped Milestone updated to v38.0 with full closure-signal block; v37.0 demoted to Prior Shipped Milestone. Active Milestone reverted to between-milestones state. Roadmap Overview table updated. Operator Next Steps updated."
      contains: "MILESTONE_V38_AT_HEAD_"
    - path: .planning/MILESTONES.md
      provides: "MODIFIED. v38.0 entry prepended to top with closure-signal recorded; v37.0 demoted one position. Includes phase shape (single-phase), commit shape (2 USER-APPROVED batched + N AGENT-COMMITTED), §4 surface count, REG row count, KI verdict, closure signal."
      contains: "MILESTONE_V38_AT_HEAD_"
    - path: .planning/PROJECT.md
      provides: "MODIFIED. v38.0 accumulated-context entry appended (decisions log + completed-milestone entry). Mirrors v37.0 / v36.0 entry shape."

  key_links:
    - from: contracts/modules/DegenerusGameDegeneretteModule.sol _packFullTicketBet (L823-847)
      to: _resolveFullTicketBet (L578-594) + _fullTicketPayout (L944-994) + _applyHeroMultiplier (L1007+)
      via: "shared FT_HERO_SHIFT bit allocation + heroQuadrant flow. Pack normalizes >= 4 -> 0 unconditionally; Resolve extracts quadrant via `(packed >> (FT_HERO_SHIFT + 1)) & MASK_2`; Payout always applies for M ∈ {2..7}; Multiplier body unchanged."
      pattern: "FT_HERO_SHIFT|heroQuadrant|MASK_2"
    - from: audit/FINDINGS-v38.0.md §3.A AUDIT-01 delta-surface table
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol (HEAD <v38-close-sha>) + git diff 2654fcc2..HEAD"
      via: "every changed declaration classified {MODIFIED_LOGIC: 4 hero functions; DELETED: MASK_3 + heroBits block + heroEnabled param + heroEnabled @param NatSpec; REFACTOR_ONLY: NatSpec rewrites at L321 + L367} with hunk-level evidence + design-intent trace per CLEAN row"
      pattern: "DegenerusGameDegeneretteModule\\.sol|_packFullTicketBet|_resolveFullTicketBet|_fullTicketPayout|MASK_3|MODIFIED_LOGIC|DELETED|REFACTOR_ONLY"
    - from: audit/FINDINGS-v38.0.md §4 5-7 surface row table
      to: "DegenerusGameDegeneretteModule.sol mutated bodies + test/stat/DegenerettePerNEvExactness.test.js (STAT-01) + test/stat/DegeneretteBonusEv.test.js (STAT-02) + test/stat/SurfaceRegression.test.js (SURF-01..03)"
      via: "5-7 rows (a..g) with verdict {SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE} + grep recipe + line cite + prose justification + STAT-01/02 empirical cross-cite + RNG commitment-window degenerate-PASS per `feedback_rng_commitment_window.md`"
      pattern: "Surface \\([a-g]\\)|SAFE_BY_DESIGN|SAFE_BY_STRUCTURAL_CLOSURE|EV-neutrality"
    - from: audit/FINDINGS-v38.0.md §6b KI envelope re-verifications row 4 (EXC-04 NARROWS-retained)
      to: KNOWN-ISSUES.md (UNMODIFIED at v38) + contracts/libraries/EntropyLib.sol (byte-identical at v38 HEAD)
      via: "backward-trace cite per `feedback_rng_backward_trace.md` + EntropyLib byte-identity preserves BAF-jackpot-only NARROWS scope from v36.0; zero RNG-path mutation at v38 means commitment-window check is degenerate-PASS per `feedback_rng_commitment_window.md`"
      pattern: "EXC-04|NARROWS|BAF jackpot|RE_VERIFIED|UNMODIFIED"
    - from: audit/FINDINGS-v38.0.md §9c
      to: "/gsd-complete-milestone for v38.0"
      via: "closure signal `MILESTONE_V38_AT_HEAD_<sha>` triggers milestone-completion handler per D-272-CLOSURE-01"
      pattern: "MILESTONE_V38_AT_HEAD_"
    - from: audit/FINDINGS-v38.0.md §9.NN commit-readiness register
      to: "1 USER-APPROVED Wave 1 contract commit + 1 USER-APPROVED Wave 2 test commit + Wave 3+ AGENT-COMMITTED audit/closure commits"
      via: "§9.NN.i cites Wave 1 contract commit; §9.NN.ii cites Wave 2 test commit; §9.NN.iii cites Wave 3+ atomic-commit chain; NO §9.NN.iv awaiting-approval subsection per D-272-CLOSURE-02"
      pattern: "feat\\(272\\)|test\\(272\\)|docs\\(272\\)|audit\\(272\\)"

tags:
  - audit
  - findings-consolidation
  - milestone-closure
  - terminal-phase
  - v38-deliverable
  - always-hero
  - dead-code-cleanup
  - degenerette
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
  - STAT-01
  - STAT-02
  - SURF-01
  - SURF-02
  - SURF-03
  - LBX-02
  - GASPIN-02
  - GASPIN-03
  - STAT-03-v35-carry
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - REG-01
  - REG-02
  - REG-03
  - REG-04
  - adversarial-validation
  - contract-auditor-spawn
  - zero-day-hunter-spawn
  - economic-analyst-spawn
---

<objective>
Single-phase v38.0 milestone-closure patch mirroring v36.0 Phase 266 precedent. Strip Degenerette hero opt-out semantics in `contracts/modules/DegenerusGameDegeneretteModule.sol` (HERO-01..05); apply Degenerette-module-only dead-code cleanup sweep made dead by the HERO edit (CLEAN-01..05; D-272-CLEAN-SCOPE-01 narrowing); land the v37+ carry-forward bundle (LBX-02 + GASPIN-02 + GASPIN-03 + STAT-03-v35-carry + SURF-03 re-baseline); produce single-canonical `audit/FINDINGS-v38.0.md` 9-section terminal deliverable with closure signal `MILESTONE_V38_AT_HEAD_<sha>` emitted in §9c (5 FINDINGS locations + 3 cross-document targets); 3-skill PARALLEL adversarial pass logged in `272-01-ADVERSARIAL-LOG.md`; flip REQUIREMENTS / ROADMAP / STATE / MILESTONES / PROJECT.

Purpose: Close v38.0 milestone with a behaviorally accepted single-batched-commit pair (1 contract + 1 test, both USER-APPROVED), preserve hero EV-neutrality empirically (STAT-01/02), preserve cross-surface byte-identity (SURF-01..03), and deliver the audit-of-record (FINDINGS-v38.0.md) that records every delta + closes the prior milestone's §9.NN.iv carry-forward register.

Output: 2 USER-APPROVED batched commits (Wave 1 contracts + Wave 2 tests) + N AGENT-COMMITTED atomic-per-task commits (Wave 3+ audit deliverable + adversarial log + summary + REQUIREMENTS/ROADMAP/STATE/MILESTONES/PROJECT flips). Closure signal `MILESTONE_V38_AT_HEAD_<sha>` emitted verbatim across 8 locations (5 FINDINGS + ROADMAP + STATE + MILESTONES).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md
@.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-DISCUSSION-LOG.md

# Precedent templates (READ for shape; executor mirrors)
@.planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-PLAN.md
@.planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-CONTEXT.md
@audit/FINDINGS-v37.0.md
@audit/FINDINGS-v36.0.md

# Live contract state (Phase 272 mutation subject — HEAD `2654fcc2` / working tree `20c7c544`)
@contracts/modules/DegenerusGameDegeneretteModule.sol

# Memory feedback (NON-NEGOTIABLE rules governing this phase)
<!-- All paths below are global memory paths; executor reads them as governing rules -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md -->

<canonical_grep_recipes>
<!-- Bake these into task <action> blocks. Executors copy verbatim — do NOT re-derive. -->

# G1 — HERO-01 _packFullTicketBet pack-site verification (post-edit)
grep -n "effectiveQuadrant\|heroQuadrant < 4 ? heroQuadrant : 0\|(uint256(1) | (uint256(effectiveQuadrant) << 1)) << FT_HERO_SHIFT" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 3 matches in _packFullTicketBet block (L823-847 region post-edit)

# G2 — HERO-02 _resolveFullTicketBet extraction (post-edit)
grep -n "uint8 heroQuadrant = uint8((packed >> (FT_HERO_SHIFT + 1)) & MASK_2)" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 1 match in _resolveFullTicketBet (replaces L592-594 heroBits/heroEnabled block)

# G3 — HERO-02/CLEAN-02 heroBits and heroEnabled removed (post-edit)
grep -nE "heroBits|heroEnabled" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 0 matches (heroBits + heroEnabled fully removed from contract source)

# G4 — CLEAN-02 MASK_3 removed (post-edit)
grep -n "MASK_3" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 0 matches (MASK_3 only callsite was L592 heroBits, removed by HERO-02; constant declaration at L347 removed)

# G5 — CLEAN-02 MASK_3 NOT used by any other contract (verify cross-module scope before deletion)
grep -rnE "\bMASK_3\b" contracts/
# Expected: 0 matches (BitPackingLib has MASK_32 — distinct identifier; MASK_3 was Degenerette-private)

# G6 — HERO-03 _fullTicketPayout signature has NO heroEnabled (post-edit)
grep -nB2 -A8 "function _fullTicketPayout" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: signature contains `uint8 currency, uint128 betAmount, uint256 roiBps, uint256 wwxrpHighRoi, uint8 heroQuadrant` — NO `bool heroEnabled` line

# G7 — HERO-03 guard simplified (post-edit)
grep -n "if (matches >= 2 && matches < 8)" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 1 match (L984 region post-edit); `heroEnabled &&` arm removed

# G8 — HERO-03 callsite drops heroEnabled arg (post-edit)
grep -nB2 -A12 "uint256 payout = _fullTicketPayout(" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: call argument list contains `heroQuadrant` but NO `heroEnabled,` line

# G9 — HERO-04 NatSpec describes what IS (post-edit)
grep -nE "previously|v37|v38|removed|reduced from|opt-out|opt out" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: 0 matches (no comparative/historical language in NatSpec per `feedback_no_history_in_comments.md`)

# G10 — HERO-05 public ABI byte-identical
grep -n "function placeDegeneretteBet" contracts/modules/DegenerusGameDegeneretteModule.sol
# Expected: signature line identical to v37.0 baseline; verify via:
cmp <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | sed -n '/function placeDegeneretteBet/,/^    {/p') <(sed -n '/function placeDegeneretteBet/,/^    {/p' contracts/modules/DegenerusGameDegeneretteModule.sol)
# Expected: exit 0

# G11 — HERO-05 storage-layout byte-identical
grep -cE "^\s*(uint|int|bool|address|bytes|string|mapping)\s" contracts/modules/DegenerusGameDegeneretteModule.sol
# Compare pre-vs-post-edit storage-keyword count; expected identical (FT_HERO_SHIFT 3-bit allocation preserved)

# G12 — SURF-01 byte-identity (other contracts UNTOUCHED at v38)
for f in contracts/libraries/EntropyLib.sol contracts/DegenerusTraitUtils.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameMintModule.sol; do
  if ! cmp <(git show 2654fcc2:$f) $f >/dev/null 2>&1; then echo "DRIFT $f"; else echo "OK $f"; fi
done
# Expected: 4 lines all "OK ..."

# G13 — SURF-02 byte-identity LootboxModule
cmp <(git show 2654fcc2:contracts/modules/DegenerusGameLootboxModule.sol) contracts/modules/DegenerusGameLootboxModule.sol
# Expected: exit 0

# G14 — Test-side heroEnabled removed
grep -nE "heroEnabled" test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js
# Expected: 0 matches

# G15 — SURF-03 baseline rebased
grep -n "PHASE_269_CLOSE_BASELINE\|8fd5c2e1" test/stat/SurfaceRegression.test.js
# Expected: >=1 match each

# G16 — D-272-FCITE-01 zero forward-cite emission
! grep -rE "Phase (273|274|275|276)|v39\\.0|v40\\.0" audit/FINDINGS-v38.0.md .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/
# Expected: exit 0 (no matches)

# G17 — Closure-signal verbatim presence in 5 FINDINGS locations
grep -cE "MILESTONE_V38_AT_HEAD_[0-9a-f]{8}" audit/FINDINGS-v38.0.md
# Expected: >=5

# G18 — Closure-signal cross-document propagation (3 targets)
grep -lE "MILESTONE_V38_AT_HEAD_[0-9a-f]{8}" .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md
# Expected: 3 file paths

# G19 — REG-03 EXC-04 EntropyLib byte-identical at v38 HEAD (BAF-only NARROWS preserved)
cmp <(git show 2654fcc2:contracts/libraries/EntropyLib.sol) contracts/libraries/EntropyLib.sol
# Expected: exit 0 — same EntropyLib means BAF-jackpot-only NARROWS retained from v36

# G20 — Bytecode delta measurement (pre/post)
# Pre-edit (snapshot before Wave 1 contract changes):
npx hardhat compile 2>&1 | tail -5; npx hardhat size-contracts 2>/dev/null | grep -i "DegeneretteModule\|Degenerette" || true
# Post-edit:
npx hardhat compile 2>&1 | tail -5; npx hardhat size-contracts 2>/dev/null | grep -i "DegeneretteModule\|Degenerette" || true
# Expected: deployedBytecode shrinks ~30 bytes; commit message records delta
</canonical_grep_recipes>

</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF-fulfillment → resolve | rngWord stored at request time; player cannot mutate post-commit (commitment-window check is degenerate-PASS at v38 per `feedback_rng_commitment_window.md` — zero RNG-path mutation). |
| Player-side input → packFullTicketBet | `heroQuadrant` parameter accepts any uint8; HERO-01 normalizes `>= 4` to 0. No revert path; no DoS surface. |
| _fullTicketPayout → _applyHeroMultiplier | Always-applied for M ∈ {2..7} post-HERO-03 (was conditional on heroEnabled). Per-N HERO_BOOST tables UNCHANGED; EV-neutrality preserved by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-272-01 | Tampering | `_packFullTicketBet` hero pack | mitigate | HERO-01 normalizes `heroQuadrant >= 4 -> 0`; no revert path; G1+G10 grep gates confirm. |
| T-272-02 | Tampering | `_resolveFullTicketBet` quadrant extract | mitigate | HERO-02 direct extract via `(packed >> (FT_HERO_SHIFT + 1)) & MASK_2`; G2+G3 confirm extraction free of heroBits/heroEnabled intermediate. |
| T-272-03 | Information Disclosure | hero EV-neutrality across (M, N) | accept | EV-neutrality preserved by per-N HERO_BOOST table calibration (UNCHANGED). STAT-01/02 empirical re-pin within tolerance is the load-bearing evidence. |
| T-272-04 | Denial of Service | `placeDegeneretteBet` heroQuadrant input | mitigate | HERO-01 normalizes any input including `0xFF` to a valid quadrant — no revert/DoS surface on hero parameter. |
| T-272-05 | Elevation of Privilege | new admin / new entry points | accept | AUDIT-04 zero-new-state attestation confirms zero new public/external mutation entry points, zero new modifiers, zero new admin functions. |
| T-272-06 | Tampering | CLEAN sweep removing safety-load-bearing code | mitigate | Each CLEAN removal carries inline design-intent trace per `feedback_design_intent_before_deletion.md` + `feedback_no_dead_guards.md`. MASK_3 sole callsite already deleted by HERO-02 (G4+G5 confirm). |
| T-272-07 | Repudiation | unauthorized contract commit without user approval | mitigate | Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`: Wave 1 + Wave 2 both block on explicit `approved` string before `git commit`. Final push gated by `feedback_manual_review_before_push.md`. |

</threat_model>

<tasks>

<!-- ================================================================ -->
<!-- WAVE 1 — CONTRACT MUTATIONS (BATCHED, USER-APPROVED at end)       -->
<!-- HERO-01..05 + CLEAN-01..05; D-272-CLEAN-SCOPE-01 Degenerette-only -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 1.1: Apply HERO-01..05 always-on hero edits to Degenerette module</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (full file; current state at HEAD `20c7c544`)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (locked decisions D-272-CLEAN-SCOPE-01 + D-272-COMMIT-SHAPE-01 + D-272-DESIGN-INTENT-01 + D-272-NATSPEC-DISCIPLINE-01; specifics block with NatSpec rewrite examples)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (governs HERO-04 NatSpec rewrites)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md (governs HERO opt-out removal — design-intent trace at D-272-DESIGN-INTENT-01 in CONTEXT.md is the load-bearing record)
  </read_first>
  <action>
HERO-01 — `_packFullTicketBet` at L823-847: insert `uint8 effectiveQuadrant = heroQuadrant < 4 ? heroQuadrant : 0;` immediately before the pack assignment. Replace the existing `if (heroQuadrant < 4) { packed |= (uint256(1) | (uint256(heroQuadrant) << 1)) << FT_HERO_SHIFT; }` block with the unconditional `packed |= (uint256(1) | (uint256(effectiveQuadrant) << 1)) << FT_HERO_SHIFT;` line. The reserved bit at FT_HERO_SHIFT + 0 is always set post-pack; quadrant 0 default applies for `heroQuadrant >= 4` (including `0xFF`). No revert path; no parameter signature change.

HERO-02 — `_resolveFullTicketBet` at L578-594: replace the 3-line `uint256 heroBits = (packed >> FT_HERO_SHIFT) & MASK_3; bool heroEnabled = (heroBits & 1) != 0; uint8 heroQuadrant = uint8(heroBits >> 1);` block with the single line `uint8 heroQuadrant = uint8((packed >> (FT_HERO_SHIFT + 1)) & MASK_2);`. Locals `heroBits` and `heroEnabled` are REMOVED. The constant `MASK_2` is already declared at L346 — reuse, do not re-declare.

HERO-03 — `_fullTicketPayout` at L944-953: drop the `bool heroEnabled,` line from the parameter list. Resulting signature: `function _fullTicketPayout(uint32 playerTicket, uint32 resultTicket, uint8 matches, uint8 currency, uint128 betAmount, uint256 roiBps, uint256 wwxrpHighRoi, uint8 heroQuadrant) private pure returns (uint256 payout)`. At L984 the guard `if (heroEnabled && matches >= 2 && matches < 8)` becomes `if (matches >= 2 && matches < 8)` — `heroEnabled &&` arm removed. At the L640-650 callsite inside `_resolveFullTicketBet`, drop the `heroEnabled,` argument line; remaining arguments unchanged.

HERO-04 — NatSpec rewrites per D-272-NATSPEC-DISCIPLINE-01 + `feedback_no_history_in_comments.md` (describe what IS at v38; ZERO comparative/historical language):
  - L321 `FT_HERO_SHIFT` comment: change from `// 3 bits: [0]=enabled, [1..2]=quadrant` to `// 3 bits: [0]=reserved, [1..2]=quadrant (always-on hero)`.
  - L367 `@param heroQuadrant`: change from `Hero quadrant (0-3) for payout boost, or 0xFF for no hero.` to `Hero quadrant (0-3) for payout boost. Inputs >= 4 (including 0xFF) normalize to quadrant 0.`
  - `_packFullTicketBet` block (immediately above L823): rewrite the inline `// Hero quadrant: 3 bits at FT_HERO_SHIFT — [0]=enabled, [1..2]=quadrant` comment to describe always-on semantics with quadrant-0 default (do NOT reference what bit 0 used to mean).
  - `_fullTicketPayout` NatSpec (L935-943 region): drop the `@param heroEnabled` line (L948-953 region); rewrite the `@dev` lead to describe hero applying for M ∈ {2..7} under always-on hero schedule. Phrase EV-neutrality without history.
  - `_applyHeroMultiplier` NatSpec (L996-1006): UNCHANGED at body and prose level (math identical; no history phrase exists today).

HERO-05 — verify public ABI byte-identical post-edit. `placeDegeneretteBet` signature (L368-381 region) UNCHANGED. Storage layout byte-identical (FT_HERO_SHIFT 3-bit allocation preserved; enabled bit vestigial post-pack always = 1).

After applying edits, run `npx hardhat compile` and capture exit code.
  </action>
  <verify>
    <automated>
# G1 — HERO-01 pack-site
grep -nE "effectiveQuadrant|heroQuadrant < 4 \\? heroQuadrant : 0" contracts/modules/DegenerusGameDegeneretteModule.sol | wc -l   # expect >=2
# G2 — HERO-02 direct extract
grep -n "uint8 heroQuadrant = uint8((packed >> (FT_HERO_SHIFT + 1)) & MASK_2)" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 1 match
# G3 — heroBits + heroEnabled fully removed
grep -cE "heroBits|heroEnabled" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 0
# G6 — _fullTicketPayout signature has NO heroEnabled
grep -A8 "function _fullTicketPayout" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -c "bool heroEnabled"   # expect 0
# G7 — guard simplified
grep -nE "^\s*if \(matches >= 2 && matches < 8\) \{" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 1
# G9 — NatSpec has no history language
grep -cEi "previously|opt-out|opt out|removed the|reduced from|used to" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 0
# G10 — public ABI byte-identical
cmp <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | awk '/function placeDegeneretteBet\(/,/\) external/') <(awk '/function placeDegeneretteBet\(/,/\) external/' contracts/modules/DegenerusGameDegeneretteModule.sol); echo "abi_cmp_exit=$?"   # expect abi_cmp_exit=0
# Compile gate
npx hardhat compile 2>&1 | tail -3   # expect "Compiled" or "Nothing to compile"
    </automated>
  </verify>
  <done>
G1..G3 + G6 + G7 + G9 + G10 grep gates all return expected values; `npx hardhat compile` exits 0. `heroBits` and `heroEnabled` identifiers no longer exist in the Degenerette module. `_fullTicketPayout` signature has 8 parameters (NO `bool heroEnabled`). `placeDegeneretteBet` public ABI byte-identical vs `2654fcc2`. NatSpec contains zero comparative/historical language. Task creates no commit (batched at Task 1.4).
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Apply CLEAN-01..05 dead-code cleanup (manual grep-walk, Degenerette-module-only)</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (post-Task-1.1 state)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (D-272-CLEAN-SCOPE-01 narrowing + D-272-CLEAN-DISCOVERY-01 manual grep method + <specifics> likely candidate list)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md (PRIMARY governing rule — every removal carries inline 1-line design-intent trace)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md (governs CLEAN-05 redundant safety guard removals)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (governs CLEAN-04 stale-comment rewrites — describe what IS, not history)
  </read_first>
  <action>
Apply cleanup-sweep candidates ORPHANED by the HERO-01..05 edits. Scope is `contracts/modules/DegenerusGameDegeneretteModule.sol` ONLY per D-272-CLEAN-SCOPE-01. Discovery method is manual grep-walk per D-272-CLEAN-DISCOVERY-01 (NO /gas-audit orchestrator). For EACH candidate, before deletion run the design-intent trace inline and capture a 1-line rationale to be propagated into the Wave 1 commit message body.

Verified candidates from CONTEXT.md <specifics> (planner pre-confirmed cross-module scope):

CLEAN-02 — `MASK_3` constant at L347 (`uint256 private constant MASK_3 = 0x7;`). Sole callsite was L592 heroBits extraction, removed by HERO-02 (Task 1.1). Cross-module grep confirms no other contract uses the bare identifier `MASK_3` (BitPackingLib defines `MASK_32` which is a distinct identifier). Design-intent trace: MASK_3 was the 3-bit hero-bits mask for the `[enabled, quadrant_lo, quadrant_hi]` layout; the always-on hero layout reads only the 2-bit quadrant via MASK_2 (declared at L346). Remove the constant declaration line at L347.

CLEAN-02 — `heroBits` local variable in `_resolveFullTicketBet` (L592 pre-edit, removed by HERO-02). Already removed at Task 1.1; this is verification only.

CLEAN-03 — `heroEnabled` local at L593 pre-edit + `heroEnabled` parameter at L952 pre-edit + `heroEnabled &&` guard arm at L984 pre-edit. Already removed by HERO-02 + HERO-03 (Task 1.1). Verification only. Per `feedback_no_dead_guards.md`: the `heroEnabled` guard was an opt-out branch; under always-on hero schedule the predicate is statically true at this site (since pack always sets reserved bit = 1), so guard was dead-on-arrival post-HERO-01. The downstream `matches >= 2 && matches < 8` predicate remains as the load-bearing guard and is preserved.

CLEAN-04 — stale NatSpec lines:
  - L948-953 region `@param heroEnabled` line — REMOVE (parameter no longer exists).
  - L367 `@param heroQuadrant Hero quadrant (0-3) for payout boost, or 0xFF for no hero.` — rewritten by HERO-04 at Task 1.1 (verification only).
  - L321 FT_HERO_SHIFT inline comment — rewritten by HERO-04 at Task 1.1 (verification only).
  - L841 inline comment `// Hero quadrant: 3 bits at FT_HERO_SHIFT — [0]=enabled, [1..2]=quadrant` inside `_packFullTicketBet` — rewritten by HERO-04 at Task 1.1; verify final comment describes always-on semantics with quadrant-0 default (no history).
  - Scan for any remaining tokens via `grep -nEi "enabled|opt-out|opt out|hero off|hero-off" contracts/modules/DegenerusGameDegeneretteModule.sol`. Each match: either rewrite to describe what IS at v38 OR delete (non-load-bearing). If a match is load-bearing prose describing current behavior (e.g., a comment that happens to contain the word "enabled" in non-hero context), leave UNCHANGED.

CLEAN-05 — redundant safety guards: scan `_packFullTicketBet`, `_resolveFullTicketBet`, `_fullTicketPayout`, `_applyHeroMultiplier` for `require`/`revert`/`if` predicates that are statically provable from caller-clamp or upstream invariant. Under always-on hero with quadrant-0 normalization, no new redundant guards are introduced (HERO-01 normalize is the load-bearing clamp; downstream `(packed >> (FT_HERO_SHIFT + 1)) & MASK_2` is bounded to 0..3 by mask construction; `_applyHeroMultiplier` body relies on `heroQuadrant ∈ {0..3}` which is now structurally guaranteed). If no new redundant guards are found, document `CLEAN-05: no additional redundant-guard removals beyond HERO-03 guard simplification` in the commit message body.

CLEAN-01 — populate the candidate-removal ledger: for each candidate above, record (a) file path, (b) line range pre-edit, (c) type {unused constant, dead local, stale NatSpec, redundant guard}, (d) 1-line design-intent trace, (e) removal verdict. Ledger lives inline in the Wave 1 commit message body (Task 1.4).

CLEAN-06 — Wave 1 single batched USER-APPROVED contract commit at Task 1.4 (deferred).

If the manual grep-walk surfaces NON-Degenerette candidates incidentally (cross-module), record them in `.planning/notes/2026-05-11-v39-cleanup-candidates.md` per D-272-CLEAN-SCOPE-01 forward-looking. Do NOT apply at v38. If zero non-Degenerette candidates surface, do NOT create that notes file (skip silently per CONTEXT.md `<deferred>` block).
  </action>
  <verify>
    <automated>
# G4 — MASK_3 declaration removed
grep -c "MASK_3" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 0
# G5 — MASK_3 not used elsewhere (sanity)
grep -rcE "\\bMASK_3\\b" contracts/   # expect 0 total across all files
# Stale @param heroEnabled NatSpec line removed
grep -ic "@param heroEnabled" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 0
# History-language scan
grep -cEi "previously|opt-out|opt out|removed the|used to|hero off|hero-off" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 0
# Compile
npx hardhat compile 2>&1 | tail -3   # expect Compiled / Nothing to compile
    </automated>
  </verify>
  <done>
G4 + G5 grep gates return 0. `@param heroEnabled` line absent. History-language grep returns 0. `npx hardhat compile` exits 0. CLEAN-01 candidate-removal ledger captured in plan-execution notes (to be promoted into Task 1.4 commit message body). If non-Degenerette candidates surfaced, `.planning/notes/2026-05-11-v39-cleanup-candidates.md` exists; else file is absent. Task creates no commit (batched at Task 1.4).
  </done>
</task>

<task type="auto">
  <name>Task 1.3: CLEAN-06 storage-layout grep proof + bytecode + gas-per-spin delta measurement</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (post-Tasks-1.1+1.2 state)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (HERO-02 bit allocation lock — FT_HERO_SHIFT stays 3 bits)
  </read_first>
  <action>
Measure and record deltas; outputs feed Task 1.4 commit message body.

(1) Storage-layout grep proof. Compare the storage-declaration footprint at v38 HEAD vs `2654fcc2`:
  - `cmp <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s") <(grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s" contracts/modules/DegenerusGameDegeneretteModule.sol)` — expected exit 0 (storage declarations byte-identical; only `MASK_3 private constant` constant declaration removed which is a constant not a storage slot; verify the grep restricts to storage-scoped declarations by confirming the deleted line was a `private constant` not a `private` storage variable).
  - Capture the constant-declaration diff separately: `diff <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep "private constant") <(grep "private constant" contracts/modules/DegenerusGameDegeneretteModule.sol)` — expected exactly one `-` line (the MASK_3 deletion); zero `+` lines (no new constants).
  - Verify FT_HERO_SHIFT value unchanged: `grep -n "FT_HERO_SHIFT = 237" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 1 match (preserves 3-bit allocation).

(2) Bytecode delta measurement. Run `npx hardhat compile` and capture deployed-bytecode size of the Degenerette module:
  - Pre-measure baseline: `git stash -- contracts/modules/DegenerusGameDegeneretteModule.sol && npx hardhat compile && (npx hardhat size-contracts 2>/dev/null | grep -i Degenerette || cat artifacts/contracts/modules/DegenerusGameDegeneretteModule.sol/DegenerusGameDegeneretteModule.json | jq -r '.deployedBytecode | length / 2 - 1') > /tmp/272-bytecode-pre.txt`
  - Restore + remeasure post-edit: `git stash pop && npx hardhat compile && (npx hardhat size-contracts 2>/dev/null | grep -i Degenerette || cat artifacts/contracts/modules/DegenerusGameDegeneretteModule.sol/DegenerusGameDegeneretteModule.json | jq -r '.deployedBytecode | length / 2 - 1') > /tmp/272-bytecode-post.txt`
  - Compute delta: `paste /tmp/272-bytecode-pre.txt /tmp/272-bytecode-post.txt | awk '{print "pre="$1" post="$2" delta="$2-$1}'` — expected `delta` in range -100..-10 bytes (target ~-30 bytes shrink per CONTEXT.md goal).

(3) Gas-per-spin delta (best-effort; load-bearing test pin lands in Wave 2 STAT-01). If `npm run test:gas` runs an existing degenerette-spin gas regression, capture gas-per-spin pre/post. If no such test exists at v38 baseline, document `gas-per-spin delta: analytical ~30 gas saved per spin (one less SLOAD-shift-mask for MASK_3 + heroBits + heroEnabled), empirical pin deferred to Wave 2 STAT-01 if surfaced` in the commit body.

(4) Public ABI byte-identity: `cmp <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep -E "^\s*function .* external") <(grep -E "^\s*function .* external" contracts/modules/DegenerusGameDegeneretteModule.sol)` — expected exit 0.

Capture all four outputs (storage grep proof, bytecode delta, gas-per-spin annotation, ABI byte-identity proof) into a scratch file `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/.task-1.3-deltas.txt` (uncommitted scratch; promoted into commit message at Task 1.4).
  </action>
  <verify>
    <automated>
# Storage-keyword count parity
diff <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep -cE "^\\s*(uint|int|bool|address|bytes|mapping|string)\\s+(?!constant|private constant)") <(grep -cE "^\\s*(uint|int|bool|address|bytes|mapping|string)\\s+(?!constant|private constant)" contracts/modules/DegenerusGameDegeneretteModule.sol) ; echo "storage_parity_exit=$?"   # expect storage_parity_exit=0
# Constant-decl diff: exactly one deletion (MASK_3), zero additions
diff <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep "private constant") <(grep "private constant" contracts/modules/DegenerusGameDegeneretteModule.sol) | grep -cE "^< .*MASK_3"   # expect 1
diff <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep "private constant") <(grep "private constant" contracts/modules/DegenerusGameDegeneretteModule.sol) | grep -cE "^> "   # expect 0
# FT_HERO_SHIFT preserved
grep -c "FT_HERO_SHIFT = 237" contracts/modules/DegenerusGameDegeneretteModule.sol   # expect 1
# Public ABI byte-identity
cmp <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep -E "^\\s*function .* external") <(grep -E "^\\s*function .* external" contracts/modules/DegenerusGameDegeneretteModule.sol); echo "abi_parity_exit=$?"   # expect abi_parity_exit=0
# Scratch deltas file exists
test -f .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/.task-1.3-deltas.txt; echo "deltas_file_exit=$?"   # expect 0
    </automated>
  </verify>
  <done>
Storage-keyword parity = 0 exit. Exactly 1 `-` line (MASK_3) + 0 `+` lines in private-constant diff. FT_HERO_SHIFT = 237 preserved (3-bit allocation locked). Public ABI byte-identical. Bytecode shrink delta recorded (target ~-30 bytes; acceptance: any negative delta in -100..-10 range or documented analytical estimate). Scratch deltas file exists at `.task-1.3-deltas.txt` for promotion into Task 1.4 commit message. No commit yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.4: WAVE 1 USER-APPROVED batched contract commit gate</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (NEVER commit contracts/ without explicit user approval — ALL agents)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md (batch all phase contract edits, present one diff, get one approval at the end)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md (orchestrator must NEVER tell agents contract changes are pre-approved)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md (present fix and wait for explicit approval before editing code — extended to commits)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/.task-1.3-deltas.txt (deltas to promote into commit message body)
  </read_first>
  <action>See <what-built> + <how-to-verify> below — this is a checkpoint:human-verify task; the action is to present the diff/state to the user and await an explicit `approved` string before committing per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Detailed steps in <how-to-verify>.</action>
  <verify>
    <automated>Manual verification only — checkpoint:human-verify gate. User runs the steps in &lt;how-to-verify&gt; and types `approved` to resume. No automated pass criterion; resume-signal IS the verify.</automated>
  </verify>
  <what-built>
Wave 1 mutates `contracts/modules/DegenerusGameDegeneretteModule.sol` ONLY (Degenerette-module-only per D-272-CLEAN-SCOPE-01). Hero opt-out semantics removed; `heroQuadrant >= 4` normalizes to 0; `_fullTicketPayout` drops `bool heroEnabled` parameter; MASK_3 + heroBits + heroEnabled removed; NatSpec rewritten per `feedback_no_history_in_comments.md`; storage layout byte-identical; public ABI byte-identical; bytecode shrink delta recorded.
  </what-built>
  <how-to-verify>
1. Run `git diff contracts/modules/DegenerusGameDegeneretteModule.sol` and present the FULL diff to the user.
2. Concatenate `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/.task-1.3-deltas.txt` to the diff presentation: bytecode delta + storage parity proof + ABI parity proof + analytical gas-per-spin estimate.
3. Present a per-requirement attribution summary listing HERO-01..05 + each CLEAN candidate applied with file:line + 1-line design-intent trace per CLEAN row.
4. Confirm grep gates G1..G11 all pass (re-run inline at presentation time).
5. **WAIT** for the user to type the literal string `approved` (case-sensitive) in their reply. Per `feedback_never_preapprove_contracts.md`: do NOT pre-approve. Per `feedback_wait_for_approval.md`: do NOT commit before explicit approval.
6. Upon `approved`, run the batched commit:
```
git add contracts/modules/DegenerusGameDegeneretteModule.sol && git commit -m "$(cat <<'EOF'
feat(272): always-on hero default 0 + degenerette dead-code cleanup [HERO-01..05, CLEAN-01..05]

HERO-01..05:
- _packFullTicketBet normalizes heroQuadrant >= 4 to 0 (effectiveQuadrant local + unconditional pack).
- _resolveFullTicketBet extracts heroQuadrant via (packed >> (FT_HERO_SHIFT + 1)) & MASK_2 (heroBits + heroEnabled locals removed).
- _fullTicketPayout drops bool heroEnabled parameter; guard simplified to (matches >= 2 && matches < 8).
- _applyHeroMultiplier body UNCHANGED.
- NatSpec rewrites at L321 + L367 + _packFullTicketBet block + _fullTicketPayout block (zero comparative/historical language per feedback_no_history_in_comments.md).
- Public ABI placeDegeneretteBet(...) byte-identical vs 2654fcc2.
- Storage layout byte-identical (FT_HERO_SHIFT = 237 preserved; enabled bit vestigial).

CLEAN-01..05 (Degenerette-module-only per D-272-CLEAN-SCOPE-01; manual grep-walk per D-272-CLEAN-DISCOVERY-01):
- MASK_3 (L347) — REMOVED. Sole callsite was L592 heroBits extraction, removed by HERO-02. Cross-module grep confirms no other contract uses bare `MASK_3` identifier. Design-intent: 3-bit hero-bits mask for [enabled, quadrant_lo, quadrant_hi]; always-on hero reads quadrant via MASK_2.
- heroBits + heroEnabled locals (L592-594) — REMOVED via HERO-02. Design-intent: opt-out intermediate; under always-on schedule, direct quadrant extract is the load-bearing form.
- bool heroEnabled parameter (L952) — REMOVED via HERO-03. Design-intent: opt-out toggle; under always-on schedule, guard predicate is statically true at this site so the parameter carried no information.
- @param heroEnabled NatSpec line (L948-953) — REMOVED via HERO-04.
- Stale comments referencing "enabled" / "opt-out" — REWRITTEN to describe always-on semantics with quadrant-0 default (no history).

CLEAN-05: no additional redundant-guard removals beyond HERO-03 guard simplification.

Bytecode delta: <PRE> bytes -> <POST> bytes (delta <DELTA>). Target ~-30 bytes shrink.
Storage layout: byte-identical vs 2654fcc2 (storage-keyword diff exit 0; constant-decl diff: exactly 1 deletion MASK_3, 0 additions).
Public ABI: byte-identical vs 2654fcc2 (cmp on `function .* external` lines exit 0).
Gas-per-spin: analytical ~30 gas saved (one less SLOAD-shift-mask for MASK_3 + heroBits + heroEnabled).

Hero EV-neutrality preserved by per-N HERO_BOOST tables UNCHANGED (empirical re-pin in Wave 2 STAT-01 + STAT-02).

Per D-272-DESIGN-INTENT-01: original heroEnabled opt-out allowed variance-averse players to skip hero boost/penalty and lock payout at basePayout (EV-neutral; variance reduction only). EV-rational players: zero effect. Variance-averse players: lose variance-reduction tool; worst-case downside per spin = 0.95 × basePayout (HERO_PENALTY). Adversarial: cannot dodge hero-penalty post-v38 but EV invariant by construction. Storage layout byte-identical preserves one-line revert path. Pre-launch UI simplification + bytecode shrink + gas savings + bounded acceptable variance impact per user disposition (degen-game context).

Per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md: this is the single batched Wave 1 USER-APPROVED contract commit covering HERO-01..05 + CLEAN-01..05.
EOF
)"
```
7. Substitute `<PRE>`, `<POST>`, `<DELTA>` with the actual numeric values from `.task-1.3-deltas.txt` before running the commit.
8. After commit, delete `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/.task-1.3-deltas.txt` (scratch file, no need to keep).
  </how-to-verify>
  <resume-signal>User types `approved` -> agent runs commit. User types anything else -> agent does NOT commit; presents follow-up question or revision plan.</resume-signal>
  <done>
Wave 1 batched commit lands. `git log --oneline -1` shows `feat(272): always-on hero default 0 + degenerette dead-code cleanup [HERO-01..05, CLEAN-01..05]`. `.task-1.3-deltas.txt` removed. Commit message body contains all four delta blocks (bytecode + storage + ABI + gas-per-spin) with numeric values + per-CLEAN-NN design-intent trace.
  </done>
</task>


<!-- ================================================================ -->
<!-- WAVE 2 — TEST MUTATIONS (BATCHED, USER-APPROVED at end)           -->
<!-- STAT-01..02 + SURF-01..03 + LBX-02 + GASPIN-02..03 + STAT-03-v35  -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 2.1: STAT-01 — re-pin DegenerettePerNEvExactness under always-on hero</name>
  <files>test/stat/DegenerettePerNEvExactness.test.js</files>
  <read_first>
    - test/stat/DegenerettePerNEvExactness.test.js (full file; current state at HEAD `20c7c544`)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (post-Wave-1 state; verify _fullTicketPayout signature has no bool heroEnabled)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md STAT-01 success criterion: basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N)
  </read_first>
  <action>
Mirror the contract-side HERO-03 signature change in the JS mirror functions. Specifically:

(1) `jsFullTicketPayout` mirror at L255-293: drop the `heroEnabled` parameter from the function signature (currently `function jsFullTicketPayout({ ..., heroEnabled, heroQuadrant, ... })` per CONTEXT.md cite). The mirror now matches the Solidity post-Wave-1 signature: `(playerTicket, resultTicket, matches, currency, betAmount, roiBps, wwxrpHighRoi, heroQuadrant)`. Inside the function body, change the guard `if (heroEnabled && matches >= 2 && matches < 8)` to `if (matches >= 2 && matches < 8)`.

(2) At the L430 callsite (per CONTEXT.md `<canonical_refs>`): the call passes `/* heroEnabled */ false, /* heroQuadrant */ 0` — drop the `heroEnabled` argument. New call: `jsFullTicketPayout({ ..., heroQuadrant: 0, ... })` (or positional equivalent depending on existing call shape).

(3) basePayoutEV invariant: the test asserts `basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N`. Hero composition is multiplicative on top of base; under always-on hero with EV-neutral per-N HERO_BOOST tables (UNCHANGED), the basePayoutEV definition is unchanged. Keep existing 100.00 ± 0.50 centi-x assertion + per-N expected-value pin.

(4) If the test file has a hero-off baseline `describe` block (covering the pre-v38 `heroEnabled = false` path), DELETE it. Under always-on hero, hero-off is no longer a reachable state.

Do NOT modify the chi² infrastructure (`makeRng`, `wilsonHilfertyZ`, etc.). Do NOT modify the per-N HERO_BOOST table constants (UNCHANGED in Wave 1 contract edit).
  </action>
  <verify>
    <automated>
# heroEnabled removed
grep -c "heroEnabled" test/stat/DegenerettePerNEvExactness.test.js   # expect 0
# Hero-off baseline describe block removed (if present)
grep -cEi "hero(\s|_|-)off|heroEnabled.*false|/\\* heroEnabled \\*/ false" test/stat/DegenerettePerNEvExactness.test.js   # expect 0
# Test exits 0
npx hardhat test test/stat/DegenerettePerNEvExactness.test.js 2>&1 | tail -10
echo "exit=$?"   # expect exit=0
    </automated>
  </verify>
  <done>
`heroEnabled` identifier absent from `test/stat/DegenerettePerNEvExactness.test.js`. Test exits 0. basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N asserted (existing pin holds under always-on hero). No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="auto">
  <name>Task 2.2: STAT-02 — re-pin DegeneretteBonusEv under always-on hero</name>
  <files>test/stat/DegeneretteBonusEv.test.js</files>
  <read_first>
    - test/stat/DegeneretteBonusEv.test.js (full file; CONTEXT.md cites hero references at L207-302 + L460+)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (post-Wave-1 state)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md STAT-02: EV-neutrality within +/-1% at >=100K hero-active draws/N)
  </read_first>
  <action>
Mirror the contract-side HERO-03 signature change in `jsApplyHeroMultiplier` + `jsFullTicketPayout` JS mirrors (L207-302 + L460+ regions per CONTEXT.md):

(1) In the `jsFullTicketPayout` mirror block near L222 (per existing grep `wwxrpHighRoi, heroEnabled, heroQuadrant,`): drop the `heroEnabled` parameter.

(2) In the guard near L241 (`if (heroEnabled && matches >= 2 && matches < 8)`): change to `if (matches >= 2 && matches < 8)`.

(3) Drop the hero-off baseline run from the STAT-03 EV-neutrality re-pin describe block. The EV-neutrality formula `P(hero|M, N) × boost + (1 − P) × HERO_PENALTY = HERO_SCALE` is verified at each (M, N) via existing per-N HERO_BOOST tables (UNCHANGED).

(4) STAT-02 acceptance: EV-neutrality within +/-1% at >=100K hero-active draws/N. Preserve existing per-(M, N) assertion shape; tighten loop to skip hero-off baseline iteration.

Do NOT modify per-N HERO_BOOST constants. Do NOT modify chi² infra. Preserve `makeRng` + `wilsonHilfertyZ` re-declarations if present (STAT-03 re-use-existing-tooling discipline).
  </action>
  <verify>
    <automated>
# heroEnabled removed
grep -c "heroEnabled" test/stat/DegeneretteBonusEv.test.js   # expect 0
# Hero-off baseline path removed
grep -cEi "hero(\\s|_|-)off|heroEnabled.*false" test/stat/DegeneretteBonusEv.test.js   # expect 0
# Test exits 0
npx hardhat test test/stat/DegeneretteBonusEv.test.js 2>&1 | tail -10
echo "exit=$?"   # expect exit=0
    </automated>
  </verify>
  <done>
`heroEnabled` identifier absent from `test/stat/DegeneretteBonusEv.test.js`. Test exits 0. EV-neutrality within +/-1% at >=100K hero-active draws/N asserted. No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="auto">
  <name>Task 2.3: SURF-01..03 — v38.0 describe block extension + SURF-03 rebase</name>
  <files>test/stat/SurfaceRegression.test.js</files>
  <read_first>
    - test/stat/SurfaceRegression.test.js (full file; existing v37.0 describe at L609 onwards; SURF-03 it block at L759 region)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md SURF-01..03)
    - .planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-PLAN.md (precedent for SURF describe-block extension; see v36.0 SURF-01..04 describe block in `test/stat/SurfaceRegression.test.js` L428-)
  </read_first>
  <action>
Extend `test/stat/SurfaceRegression.test.js` with a v38.0 SURF-01..03 describe block AFTER the existing v37.0 describe block (L609). Mirror the structural shape of the v37.0 describe (existing precedent at L609-770 region).

(1) Add module-scope baseline constants near L599 (alongside `V36_BASELINE`):
```
const V37_BASELINE = "2654fcc2";  // v37.0 closure HEAD
const PHASE_269_CLOSE_BASELINE = "8fd5c2e1";  // post-LBX-01 HEAD; used by SURF-03 rebase below
```

(2) New describe block `describe("v38.0 SURF-01..02 — protected surfaces vs v37.0 baseline 2654fcc2", function () { ... })`:
  - Soft-skip if HEAD == V37_BASELINE (mirror v37.0 describe pattern at L649).
  - SURF-01 it block: byte-identity walk vs `2654fcc2` for 4 contracts UNTOUCHED at v38: `contracts/libraries/EntropyLib.sol`, `contracts/DegenerusTraitUtils.sol`, `contracts/modules/DegenerusGameJackpotModule.sol`, `contracts/modules/DegenerusGameMintModule.sol`. Use `expectFileLevelZeroDiffV37(V37_BASELINE, <path>)` helper if compatible; if not, write a `expectFileLevelZeroDiffV38` helper mirroring the v37 shape.
  - SURF-02 it block: byte-identity walk vs `2654fcc2` for `contracts/modules/DegenerusGameLootboxModule.sol`. Same helper pattern.
  - SURF describe self-test it block (structural pin): documents that v38.0 SURF preservation describe carries the same anchor-discipline as v37.0 — fail-loud-on-empty-diff guard + soft-skip-on-unreachable-baseline (D-IMPL-11 carry).

(3) SURF-03 rebase: locate the existing v37.0 SURF-03 it block at L759 region. Change its baseline reference from `V36_BASELINE` (= `1c0f0913...`) to `PHASE_269_CLOSE_BASELINE` (= `8fd5c2e1...`). One-line edit. Closes v37.0 §9.NN.iv SURF-03 carry-forward item per REQUIREMENTS.md.

(4) Verify the existing v33/v34/v35/v36/v37 describe blocks remain byte-identical (REG-01 carry-forward discipline) — do NOT modify any prior-milestone describe block beyond the SURF-03 rebase.
  </action>
  <verify>
    <automated>
# v38 baseline constants present
grep -nE "V37_BASELINE.*2654fcc2|PHASE_269_CLOSE_BASELINE.*8fd5c2e1" test/stat/SurfaceRegression.test.js   # expect 2 matches
# v38.0 describe block exists
grep -c 'describe("v38.0 SURF' test/stat/SurfaceRegression.test.js   # expect >=1
# SURF-03 rebase: V36_BASELINE no longer cited by SURF-03 it block (still cited elsewhere by v36/v37 blocks)
grep -nE "SURF-03|PHASE_269_CLOSE_BASELINE" test/stat/SurfaceRegression.test.js | head -20
# Test exits 0
npx hardhat test test/stat/SurfaceRegression.test.js 2>&1 | tail -10
echo "exit=$?"   # expect exit=0
    </automated>
  </verify>
  <done>
`V37_BASELINE = "2654fcc2"` and `PHASE_269_CLOSE_BASELINE = "8fd5c2e1"` constants added. New `describe("v38.0 SURF-01..02 — protected surfaces vs v37.0 baseline 2654fcc2", ...)` block exists. SURF-03 it block now references `PHASE_269_CLOSE_BASELINE`. Test exits 0. Prior-milestone describe blocks (v33-v37) byte-identical. No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="auto">
  <name>Task 2.4: LBX-02 — formal re-defer with path-of-investigation prose (deferred-pick rationale inline)</name>
  <files>test/gas/LootboxOpenGas.test.js</files>
  <read_first>
    - test/gas/LootboxOpenGas.test.js (existing fixture; reachOpenableLootbox helper at L122; v36.0 GAS-01 describe at L180)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md LBX-02 + <deferred_to_planner> option set; planner pick = formal re-defer)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md (analytical worst-case load-bearing precedent; Phase 266 GAS-01)
    - .planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-PLAN.md (Phase 266 GAS-01 fixture-coverage-gap precedent prose for LBX-02 reference)
  </read_first>
  <action>
Planner pick: FORMAL RE-DEFER to v39+ (per `deferred_picks.LBX-02` rationale in this plan's frontmatter). Phase 269 already attempted the empirical pin and was blocked by a structural fixture-coverage gap. Repeating the attempt at v38 without new fixture-coverage tooling reproduces the same gap.

Apply a documentation-only edit:

(1) At the top of `test/gas/LootboxOpenGas.test.js` (above the existing header comment block), prepend a `// LBX-02 — v38 FORMAL RE-DEFER` block (multi-line `//` comments; not a JSDoc to avoid mocha parser surprises). Content:

```
// LBX-02 — v38 FORMAL RE-DEFER (carry-forward from v37.0 §9.NN.iv).
//
// Goal: empirical pin for "v37.0 LBX-01 saves 20-50 gas on 55%-tickets-path".
//
// Phase 269 attempted this and was blocked by a structural fixture-coverage
// gap: the existing reachOpenableLootbox harness cannot deterministically
// reach the 55%-tickets-path branch with the same gas envelope pre- vs
// post-LBX-01 without a Phase-266-GAS-01-style synthetic fixture. The
// closure-of-record at Phase 269 was the analytical worst-case derivation
// (per feedback_gas_worst_case.md); v37.0 audit FINDINGS-v37.0.md §9.NN.iv
// recorded the carry-forward.
//
// Path-of-investigation for v39+ pickup:
//   (1) Build a deterministic lootbox-state fixture that lands on the
//       55%-tickets-path branch. The existing reachOpenableLootbox helper
//       walks the production path which is non-deterministic on branch
//       selection without VRF rigging.
//   (2) Capture gasUsed pre- vs post-LBX-01 against the same fixture seed.
//   (3) Pin PER_OPEN_GAS_DELTA_BOUND for the 55%-tickets-path at the
//       observed delta (analytical estimate: 20-50 gas saved per LBX-01
//       Phase 269 commit `8fd5c2e1` -14/+1 LOC plus signature cascade).
//
// Status at v38 close: FORMAL_RE_DEFER_TO_V39_PLUS. Closure recorded in
// audit/FINDINGS-v38.0.md §9.NN.iv. Analytical worst-case load-bearing
// per feedback_gas_worst_case.md remains the v38 acceptance.
```

(2) Do NOT add any new `it()` or `describe()` blocks. Do NOT modify any existing assertion. The test file's runtime behavior is unchanged.

This task closes LBX-02 via formal re-defer (the carry-forward register entry resolves at the audit deliverable §9.NN.iv block — Task 3.6).
  </action>
  <verify>
    <automated>
# LBX-02 prose block present
grep -c "LBX-02 — v38 FORMAL RE-DEFER" test/gas/LootboxOpenGas.test.js   # expect 1
# v39+ forward reference present (load-bearing for §9.NN.iv pickup)
grep -c "v39+" test/gas/LootboxOpenGas.test.js   # expect >=1
# No new it() or describe() added (count parity vs baseline 2654fcc2)
diff <(git show 2654fcc2:test/gas/LootboxOpenGas.test.js | grep -cE "^\\s*(it|describe)\\(") <(grep -cE "^\\s*(it|describe)\\(" test/gas/LootboxOpenGas.test.js); echo "block_count_parity_exit=$?"   # expect block_count_parity_exit=0
# Test still exits 0
npx hardhat test test/gas/LootboxOpenGas.test.js 2>&1 | tail -10
echo "exit=$?"   # expect exit=0
    </automated>
  </verify>
  <done>
`LBX-02 — v38 FORMAL RE-DEFER` prose block prepended. Path-of-investigation prose recorded. No new it/describe blocks added (count parity vs baseline). Test exits 0. Closes v37.0 §9.NN.iv LBX-02 carry-forward via formal re-defer; the corresponding §9.NN.iv entry in `audit/FINDINGS-v38.0.md` is authored at Task 3.6. No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="auto">
  <name>Task 2.5: GASPIN-02 (a-alt) script-split + GASPIN-03 clean-run verification</name>
  <files>package.json, test/gas/Phase261GasRegression.test.js, test/gas/Phase264GasRegression.test.js</files>
  <read_first>
    - package.json (existing `test:stat` script at L9; CONTEXT.md cites the inline script)
    - test/gas/Phase261GasRegression.test.js (verify body UNCHANGED — script-split path leaves test bodies alone)
    - test/gas/Phase264GasRegression.test.js (same)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md GASPIN-02 + <deferred_to_planner> options; planner pick = (a-alt) script-split)
  </read_first>
  <action>
Planner pick: GASPIN-02 (a-alt) dedicated `test:gas` script split (per `deferred_picks.GASPIN-02` rationale in this plan's frontmatter).

(1) In `package.json`, modify the `scripts` block:
  - Existing `test:stat` script (L9): REMOVE `test/gas/Phase261GasRegression.test.js`, `test/gas/Phase264GasRegression.test.js`, and `test/gas/Phase268GasRegression.test.js` from its argument list. Retain stat-only files: `test/stat/TraitDistribution.test.js`, `test/stat/GoldSoloCoverage.test.js`, `test/stat/SoloEvUplift.test.js`, `test/stat/PackFeel.test.js`, `test/stat/SurfaceRegression.test.js`, `test/stat/PerPullLevelDistribution.test.js`, `test/stat/PerPullEmptyBucketSkip.test.js`, `test/stat/LootboxEntropyDistribution.test.js`, `test/stat/DegenerettePerNEvExactness.test.js`, `test/stat/DegeneretteProducerChi2.test.js`, `test/stat/DegeneretteBonusEv.test.js`.
  - Add new `test:gas` script: `"test:gas": "hardhat test test/gas/Phase261GasRegression.test.js test/gas/Phase264GasRegression.test.js test/gas/Phase268GasRegression.test.js test/gas/LootboxOpenGas.test.js test/gas/AdvanceGameGas.test.js"`. Insert directly after the `test:stat` line.

(2) Do NOT modify the bodies of `test/gas/Phase261GasRegression.test.js` or `test/gas/Phase264GasRegression.test.js`. NO tolerance widening (option (c) was NOT chosen; widening would require an inline ACCEPTED-TOLERANCE ledger entry per `feedback_no_history_in_comments.md` which is NOT in scope under (a-alt)).

(3) GASPIN-03 verification: after the script-split, run `npm run test:stat` start-to-finish + `npm run test:gas` start-to-finish at v38 phase-close HEAD. Both must exit 0 with zero flaky failures. Capture exit codes.

(4) Inline rationale comment: add a 3-line comment immediately above the new `test:gas` line in `package.json` (use a JSON-compatible approach — since strict JSON disallows comments, instead place the rationale as a comment field via a `"_comment_test:gas"` key, OR more cleanly, document the script-split rationale in the existing `test:stat` script's adjacent area is not feasible in strict JSON). If `package.json` is strict JSON (no comments), skip the inline comment and rely on the commit message body (Task 2.7) + the plan's `deferred_picks.GASPIN-02` rationale for traceability.
  </action>
  <verify>
    <automated>
# test:gas script exists
grep -c '"test:gas":' package.json   # expect 1
# Phase261/264/268 GasRegression NOT in test:stat
grep '"test:stat":' package.json | grep -cE "Phase26[148]GasRegression"   # expect 0
# Phase261/264/268 GasRegression IN test:gas
grep '"test:gas":' package.json | grep -cE "Phase26[148]GasRegression"   # expect 3
# Test body parity vs baseline
diff <(git show 2654fcc2:test/gas/Phase261GasRegression.test.js) test/gas/Phase261GasRegression.test.js; echo "p261_diff_exit=$?"   # expect p261_diff_exit=0
diff <(git show 2654fcc2:test/gas/Phase264GasRegression.test.js) test/gas/Phase264GasRegression.test.js; echo "p264_diff_exit=$?"   # expect p264_diff_exit=0
# GASPIN-03 clean run verification
npm run test:stat 2>&1 | tail -5; echo "test_stat_exit=$?"   # expect test_stat_exit=0
npm run test:gas 2>&1 | tail -5; echo "test_gas_exit=$?"   # expect test_gas_exit=0
    </automated>
  </verify>
  <done>
`package.json` has `"test:gas"` script with Phase261/264/268 GasRegression + LootboxOpenGas + AdvanceGameGas. `"test:stat"` excludes Phase261/264/268 GasRegression. Test bodies UNCHANGED (diff exit 0 vs baseline). `npm run test:stat` + `npm run test:gas` both exit 0. GASPIN-03 acceptance: clean start-to-finish run with zero flaky failures. No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="auto">
  <name>Task 2.6: STAT-03-v35-carry — ACCEPTED-DESIGN ledger entry</name>
  <files>test/stat/PerPullEmptyBucketSkip.test.js</files>
  <read_first>
    - test/stat/PerPullEmptyBucketSkip.test.js (current state; header comment block at L1-50; STAT-03 describe at L206)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (REQUIREMENTS.md STAT-03-v35-carry + <deferred_to_planner> options; planner pick = ACCEPTED-DESIGN)
    - .planning/milestones/v35.0-phases/265-*/265-CONTEXT.md (if accessible — v35.0 Phase 265 D-265-STAT03-01 reframe origin; otherwise rely on STATE.md citation)
  </read_first>
  <action>
Planner pick: ACCEPTED-DESIGN ledger entry (per `deferred_picks.STAT-03-v35-carry` rationale in this plan's frontmatter). 88.24% empty-bucket skip rate is a fixture-density artifact (sparse deity-backed holder map) NOT protocol behavior.

Apply a documentation-only edit:

(1) Prepend a `// STAT-03-v35-carry — v38 ACCEPTED-DESIGN ledger entry` block at the top of `test/stat/PerPullEmptyBucketSkip.test.js` (above the existing header comment block at L1-50). Content:

```
// STAT-03-v35-carry — v38 ACCEPTED-DESIGN ledger entry
// (carry-forward chain v35.0 -> v36.0 -> v37.0 -> v38.0).
//
// Observed: empty-bucket skip rate ~88.24% under the current sparse
// deity-backed holder fixture.
//
// Attribution: fixture-density artifact. The fixture map populates a
// small subset of (day, level, quadrant) cells; the per-pull skip
// branch fires when the targeted bucket is empty. With sparse fixture
// density, most pulls land in empty buckets and skip — this is the
// fixture configuration, NOT protocol behavior.
//
// v35.0 Phase 265 D-265-STAT03-01 reframe established this as a
// fixture-calibration-error attribution (see audit/FINDINGS-v35.0.md
// §3c and v35.0 milestone close prose in .planning/STATE.md "Just-
// Shipped Milestone Reference" -> v35.0 -> "STAT-03 reframe row
// SAFE_BY_STRUCTURAL_CLOSURE per D-265-STAT03-01").
//
// v36.0 + v37.0 carried the attribution forward without re-litigation.
//
// v38.0 status: ACCEPTED-DESIGN. The 10% skip-rate threshold in the
// describe block below intentionally triggers a test FAIL under the
// sparse fixture — this is the documented gate that flags the
// fixture-density attribution rather than a protocol issue.
// Populating a dense deity-backed fixture is out of v38 scope (would
// require new mechanical workstream outside the v38 always-hero +
// dead-code cleanup payload).
//
// Cross-cited from audit/FINDINGS-v38.0.md §3.A (delta-surface table
// STAT-03-v35-carry row) + §6 (KI gating walk — Non-Promotion Ledger
// retains zero rows; STAT-03 is fixture-density, not a finding).
```

(2) Do NOT modify the existing describe blocks, it blocks, helper functions, or threshold constants. The test threshold (skip rate > 10% triggers FAIL per existing semantics) is UNCHANGED. Per the ledger entry, the FAIL itself is the documented gate that surfaces the fixture-density attribution.

(3) This task does NOT add the test to any npm script (`test:stat` already includes it per package.json L9). The expected runtime behavior under `npm run test:stat` is that the existing STAT-03 it block continues to FAIL with skip rate > 10% — this is the ACCEPTED-DESIGN gate, not a regression. Confirm via Task 2.5's `npm run test:stat` exit code: if `test:stat` previously included `PerPullEmptyBucketSkip.test.js` and was passing OR failing — preserve whichever exit semantics package.json + the existing test produced at v37.0 close.

**CRITICAL CONSISTENCY GATE:** if `PerPullEmptyBucketSkip.test.js` FAILS under `npm run test:stat` post-Task-2.5, then GASPIN-03 acceptance criterion `clean test:stat start-to-finish exit 0` (Task 2.5) is INCONSISTENT with this task's ACCEPTED-DESIGN posture. Resolution: at v37.0 close (per CONTEXT.md `<deferred_to_planner>` STAT-03-v35-carry mentions "88.24% empty-bucket skip in current sparse fixture per v35.0 Phase 265 D-265-STAT03-01 reframe"), if the test was already failing pre-v38 and GASPIN-03 verification was de-facto "no NEW flaky failures introduced by v38 changes" rather than strict 0-exit, document this nuance in the Wave 2 commit message + audit FINDINGS §9.NN.iii. If the test currently passes at v37.0 close (skip rate happens to land below 10% in the existing fixture seed), then no consistency issue — GASPIN-03 clean-run holds. **Executor confirms by running `npx hardhat test test/stat/PerPullEmptyBucketSkip.test.js` standalone BEFORE Task 2.5, captures pre-edit exit code, and proceeds accordingly.**
  </action>
  <verify>
    <automated>
# Ledger entry present
grep -c "STAT-03-v35-carry — v38 ACCEPTED-DESIGN" test/stat/PerPullEmptyBucketSkip.test.js   # expect 1
# fixture-density attribution prose present
grep -c "fixture-density artifact" test/stat/PerPullEmptyBucketSkip.test.js   # expect 1
# Cross-cite to audit/FINDINGS-v38.0.md present
grep -c "FINDINGS-v38.0.md" test/stat/PerPullEmptyBucketSkip.test.js   # expect >=1
# Test body diff: only prepended comment block; no body changes
diff <(git show 2654fcc2:test/stat/PerPullEmptyBucketSkip.test.js | grep -cE "^\\s*(it|describe)\\(") <(grep -cE "^\\s*(it|describe)\\(" test/stat/PerPullEmptyBucketSkip.test.js); echo "block_count_parity_exit=$?"   # expect block_count_parity_exit=0
# Standalone test exit-code captured (executor records pre vs post; same exit code expected)
npx hardhat test test/stat/PerPullEmptyBucketSkip.test.js 2>&1 | tail -10
echo "post_exit=$?"   # record value; compare against pre-edit exit code
    </automated>
  </verify>
  <done>
ACCEPTED-DESIGN ledger entry prepended at file head with cross-cite to audit/FINDINGS-v38.0.md §3.A + §6. Test bodies UNCHANGED. it/describe count parity vs baseline. Standalone test exit code matches pre-edit (no NEW behavior introduced by the ledger-entry edit). Resolves STAT-03-v35-carry via ACCEPTED-DESIGN per CONTEXT.md default expectation. No commit yet (batched at Task 2.7).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.7: WAVE 2 USER-APPROVED batched test commit gate</name>
  <files>test/stat/DegenerettePerNEvExactness.test.js, test/stat/DegeneretteBonusEv.test.js, test/stat/SurfaceRegression.test.js, test/gas/LootboxOpenGas.test.js, test/stat/PerPullEmptyBucketSkip.test.js, package.json</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (test/ commits also require explicit user approval)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
  </read_first>
  <action>See <what-built> + <how-to-verify> below — this is a checkpoint:human-verify task; the action is to present the diff/state to the user and await an explicit `approved` string before committing per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Detailed steps in <how-to-verify>.</action>
  <verify>
    <automated>Manual verification only — checkpoint:human-verify gate. User runs the steps in &lt;how-to-verify&gt; and types `approved` to resume. No automated pass criterion; resume-signal IS the verify.</automated>
  </verify>
  <what-built>
Wave 2 mutates 5 test files + `package.json`:
- `test/stat/DegenerettePerNEvExactness.test.js` — STAT-01 re-pin under always-on hero (drop `heroEnabled` from JS mirror + L430 callsite).
- `test/stat/DegeneretteBonusEv.test.js` — STAT-02 re-pin under always-on hero (drop `heroEnabled`; drop hero-off baseline iteration).
- `test/stat/SurfaceRegression.test.js` — v38.0 SURF-01..02 describe block extension + SURF-03 rebase from `V36_BASELINE` to `PHASE_269_CLOSE_BASELINE = "8fd5c2e1"`.
- `test/gas/LootboxOpenGas.test.js` — LBX-02 v38 FORMAL RE-DEFER prose block prepended (documentation-only; no test-body changes).
- `test/stat/PerPullEmptyBucketSkip.test.js` — STAT-03-v35-carry v38 ACCEPTED-DESIGN ledger entry prepended (documentation-only; no test-body changes).
- `package.json` — GASPIN-02 (a-alt) script-split: new `test:gas` script + `test:stat` excludes Phase261/264/268 GasRegression.
  </what-built>
  <how-to-verify>
1. Run `git diff test/ package.json` and present the FULL diff to the user.
2. Confirm grep gates G14 (test-side `heroEnabled` removed), G15 (SURF-03 rebased to PHASE_269_CLOSE_BASELINE) pass.
3. Confirm Wave 2 acceptance: `npm run test:stat` + `npm run test:gas` exit codes captured + `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js test/stat/SurfaceRegression.test.js` exits 0.
4. Confirm deferred-pick consistency: STAT-03-v35-carry pre-vs-post-edit standalone exit code matches (no NEW behavior); LBX-02 prose block present; GASPIN-02 (a-alt) script-split applied without test-body changes.
5. **WAIT** for the user to type the literal string `approved` (case-sensitive). Per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`: do NOT commit before explicit approval.
6. Upon `approved`, run the batched commit:
```
git add test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js test/stat/SurfaceRegression.test.js test/gas/LootboxOpenGas.test.js test/stat/PerPullEmptyBucketSkip.test.js package.json && git commit -m "$(cat <<'EOF'
test(272): hero-always-on + dead-code cleanup + v37+ carry bundle [STAT-01..02, SURF-01..03, LBX-02, GASPIN-02..03, STAT-03-v35-carry]

STAT-01 + STAT-02 (re-pin under always-on hero):
- test/stat/DegenerettePerNEvExactness.test.js: jsFullTicketPayout mirror drops `heroEnabled` parameter; L430 callsite drops `heroEnabled` arg; basePayoutEV per N within +/-0.50 centi-x at >=1M draws/N preserved.
- test/stat/DegeneretteBonusEv.test.js: jsApplyHeroMultiplier + jsFullTicketPayout mirrors drop `heroEnabled`; hero-off baseline run dropped; EV-neutrality within +/-1% at >=100K hero-active draws/N preserved.

SURF-01..03:
- test/stat/SurfaceRegression.test.js: new v38.0 SURF-01..02 describe block asserts byte-identity vs v37.0 baseline 2654fcc2 for EntropyLib + TraitUtils + JackpotModule + MintModule (SURF-01) + LootboxModule (SURF-02). SURF-03 rebase: V36_BASELINE -> PHASE_269_CLOSE_BASELINE (8fd5c2e1, post-LBX-01 HEAD). Closes v37.0 §9.NN.iv SURF-03 carry-forward.

LBX-02 — v38 FORMAL RE-DEFER:
- test/gas/LootboxOpenGas.test.js: prose-only block prepended documenting Phase 269 fixture-coverage gap + path-of-investigation for v39+ pickup. Analytical worst-case load-bearing per feedback_gas_worst_case.md remains v38 acceptance.

GASPIN-02 (a-alt) + GASPIN-03:
- package.json: new `test:gas` script (Phase261/264/268 GasRegression + LootboxOpenGas + AdvanceGameGas); `test:stat` excludes those files. Test bodies UNCHANGED (diff parity vs 2654fcc2 exit 0). Goal: clean start-to-finish runs under both scripts at v38 HEAD with zero flaky failures (option (a-alt) script-isolation sidesteps cumulative-state drift that affected v36-v37 `test:stat` ordering).

STAT-03-v35-carry — v38 ACCEPTED-DESIGN:
- test/stat/PerPullEmptyBucketSkip.test.js: ledger-entry prose block prepended documenting 88.24% empty-bucket skip rate as fixture-density artifact (sparse deity-backed holder map), NOT protocol behavior. Carry-forward from v35.0 Phase 265 D-265-STAT03-01 reframe. Test bodies UNCHANGED.

Per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md: this is the single batched Wave 2 USER-APPROVED test commit covering 30-v38.0-req test-side scope.
EOF
)"
```
  </how-to-verify>
  <resume-signal>User types `approved` -> agent runs commit. Anything else -> no commit; agent answers question or revises.</resume-signal>
  <done>
Wave 2 batched commit lands. `git log --oneline -1` shows `test(272): hero-always-on + dead-code cleanup + v37+ carry bundle [...]`. `git log --oneline -2` shows Wave 1 + Wave 2 as the two USER-APPROVED commits in the phase. Wave 3+ AGENT-COMMITTED chain can begin.
  </done>
</task>


<!-- ================================================================ -->
<!-- WAVE 3 — AUDIT DELIVERABLE + ADVERSARIAL PASS                     -->
<!-- AGENT-COMMITTED atomic-per-task                                   -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 3.1: AUDIT-01 — Create audit/FINDINGS-v38.0.md §1-§3 skeleton + §3.A delta-surface table</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - audit/FINDINGS-v37.0.md (PRIMARY template — copy §1-§9 structural skeleton; substitute v37 -> v38 identifiers)
    - audit/FINDINGS-v36.0.md (secondary precedent)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (D-272-FILES-01 single-deliverable + D-272-CLEAN-SCOPE-01 + D-272-CLEAN-DISCOVERY-01 — §3.A row attribution rules)
    - .planning/REQUIREMENTS.md (AUDIT-01..06 + REG-01..04 success criteria)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (post-Wave-1 state at v38 HEAD)
  </read_first>
  <action>
Create `audit/FINDINGS-v38.0.md` by copying the §1-§9 structural skeleton from `audit/FINDINGS-v37.0.md`. Substitute `v37` -> `v38`, `MILESTONE_V37_AT_HEAD_2654fcc2` -> `MILESTONE_V38_AT_HEAD_<sha>` (leave `<sha>` placeholder; resolved at Task 4.6), `Phase 267..271` (5 phases) -> `Phase 272` (single phase), `8-surface` -> `5-7-surface`, `F-37-NN` -> `F-38-NN`.

Populate the following sections at this task (sections §4 + §5 + §6 + §7 + §8 + §9 are populated in subsequent tasks):

§1 Frontmatter (YAML):
```
---
milestone: v38.0
milestone_name: Always-Hero Simplification + Maximal Dead-Code Cleanup
audit_baseline: 1c0f09132d7439af9881c56fe197f81757f8164a  # v36.0 closure HEAD (last fully-audited baseline at v38; v37.0 closure 2654fcc2 was a sub-milestone)
audit_baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
v37_intermediate_baseline: 2654fcc2  # v38.0 source-tree start point per CONTEXT.md
v37_intermediate_signal: MILESTONE_V37_AT_HEAD_2654fcc2
audit_subject_head: <sha>  # resolved at Task 4.6 atomic-update
closure_signal: MILESTONE_V38_AT_HEAD_<sha>
status: DRAFT  # flipped to FINAL — READ-ONLY at Task 4.7
read_only: false  # flipped to true at Task 4.7
supersedes: none
phase_count: 1
phase_ids: [272]
requirements_total: 30
adversarial_pass_skills: [contract-auditor, zero-day-hunter, economic-analyst]
adversarial_pass_pattern: PARALLEL_SINGLE_MESSAGE
out_of_scope_skills: [degen-skeptic]
---
```

§2 Executive Summary — Closure Verdict + Severity Counts (D-272-SEV-01) + D-08 5-bucket rubric + D-09 KI rubric + Forward-Cite (D-272-FCITE-01) + Attestation Anchor. Author prose declaring 30/30 v38.0 requirements satisfied; closure signal `MILESTONE_V38_AT_HEAD_<sha>`; LBX-02 RE-DEFERRED-V39+ per planner pick; GASPIN-02 (a-alt) script-split applied; STAT-03-v35-carry ACCEPTED-DESIGN. Default 0 F-38-NN finding blocks.

§3 Per-Phase Sections:
  - §3a Phase 272 — Always-Hero Simplification + Maximal Dead-Code Cleanup (Terminal). Summary of HERO-01..05 + CLEAN-01..05 + Wave 2 test bundle + carry-forward picks.
  - §3.A AUDIT-01 Delta-Surface Table. Row Group 1 — Phase 272 Wave 1 contract changes (commit `<wave-1-sha>`): one row per HERO-NN with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification + design-intent trace + verdict. One row per CLEAN-NN with same shape. **The HERO row group MUST explicitly note D-272-CLEAN-SCOPE-01 scope narrowing (cleanup applies ONLY to `contracts/modules/DegenerusGameDegeneretteModule.sol`).** **The CLEAN row group MUST cite manual grep-walk method per D-272-CLEAN-DISCOVERY-01 (NOT `/gas-audit` orchestrator).** Row Group 2 — Phase 272 Wave 2 test bundle (commit `<wave-2-sha>`): STAT-01/02 + SURF-01..03 + LBX-02 RE-DEFER + GASPIN-02 (a-alt) + STAT-03-v35-carry ACCEPTED-DESIGN rows.
  - §3.B AUDIT-04 Zero-New-State Attestation. Zero new storage slots; zero new public/external mutation entry points; zero new external pure entry points; zero new admin functions; zero new modifiers. Grep recipes per v37.0 §3.B template.
  - §3.C AUDIT-03 Conservation Re-Proof. Total payout invariant `ethShare + lootboxShare = payout` preserved (PAY-SPLIT UNCHANGED at v38). Per-N basePayoutEV = 100 centi-x exact preserved (per-N tables UNCHANGED). Hero EV-neutrality preserved across all (M, N) under always-on hero schedule — per-N HERO_BOOST tables UNCHANGED; STAT-01/02 empirical re-pin cross-cite.

Commit when §1-§3 are populated:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): create FINDINGS-v38.0.md §1-§3 skeleton + AUDIT-01 delta-surface table [AUDIT-01, AUDIT-03, AUDIT-04]"
```
  </action>
  <verify>
    <automated>
test -f audit/FINDINGS-v38.0.md; echo "file_exit=$?"   # expect 0
grep -c "## 3.A " audit/FINDINGS-v38.0.md   # expect 1
grep -c "D-272-CLEAN-SCOPE-01" audit/FINDINGS-v38.0.md   # expect >=1
grep -c "D-272-CLEAN-DISCOVERY-01" audit/FINDINGS-v38.0.md   # expect >=1
grep -c "MILESTONE_V38_AT_HEAD_" audit/FINDINGS-v38.0.md   # expect >=2 (frontmatter + §2; final count goes to >=5 at Task 4.6)
git log --oneline -1 | grep -c "audit(272): create FINDINGS-v38.0.md"   # expect 1
    </automated>
  </verify>
  <done>
`audit/FINDINGS-v38.0.md` exists with §1 frontmatter + §2 Executive Summary + §3a Phase 272 prose + §3.A delta-surface table (HERO-01..05 + CLEAN-01..05 + Wave 2 carry-bundle rows) + §3.B zero-new-state attestation + §3.C conservation re-proof. D-272-CLEAN-SCOPE-01 and D-272-CLEAN-DISCOVERY-01 cited verbatim. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3.2: AUDIT-02 — §4 inline 5-7-surface adversarial draft</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (post-Task-3.1 state)
    - audit/FINDINGS-v37.0.md §4 (8-surface row table — template)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (<specifics> 5-7 surface candidate list (a)..(g))
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md (degenerate-PASS attestation at v38 — zero RNG-path mutation)
  </read_first>
  <action>
Append §4 to `audit/FINDINGS-v38.0.md`. Mirror the v37.0 §4 8-surface row format. Author inline draft for 5-7 surfaces:

(a) **Hero always-on EV-neutrality preserved across (M, N).** Verdict: SAFE_BY_DESIGN. Evidence: per-N HERO_BOOST tables (HERO_BOOST_N0..N4_PACKED at L337-341) UNCHANGED at v38; calibrated so `P(hero|M, N) × boost(M, N) + (1 − P) × HERO_PENALTY = HERO_SCALE`. Empirical cross-cite: STAT-01 (`test/stat/DegenerettePerNEvExactness.test.js`) + STAT-02 (`test/stat/DegeneretteBonusEv.test.js`) re-pinned under always-on hero. Grep recipe: `git diff 2654fcc2..HEAD -- contracts/modules/DegenerusGameDegeneretteModule.sol | grep -E "HERO_BOOST_N[0-4]_PACKED|HERO_PENALTY|HERO_SCALE"` — expected zero matches (constants UNCHANGED).

(b) **Hero quadrant 0 default does NOT create payout-bias for players who omit heroQuadrant.** Verdict: SAFE_BY_DESIGN. Evidence: `packedTraitsDegenerette` producer (v34 trait producer — UNCHANGED at v38) generates uniform symbol distribution across all 4 quadrants. Quadrant choice does not change EV under EV-neutral hero design (per (a) above). Player passing `0xFF` lands in quadrant 0 (top-left); landing there carries no informational advantage or disadvantage vs landing in quadrants 1/2/3 because hero EV-neutrality is per-quadrant identical by table construction.

(c) **Each cleanup removal preserves the invariant it claimed to guard.** Verdict: SAFE_BY_DESIGN. Evidence: per-CLEAN-NN inline design-intent trace recorded in Wave 1 commit message + §3.A row group. MASK_3 sole callsite was the heroBits extraction removed by HERO-02 (design-intent trace: 3-bit mask for [enabled, quadrant_lo, quadrant_hi]; under always-on hero, MASK_2 quadrant-only extract is load-bearing form). heroEnabled local + parameter (design-intent trace: opt-out toggle; under always-on schedule guard is statically true so parameter carries no information). Per `feedback_design_intent_before_deletion.md`: design intent + actor walk-through documented before deletion shape applied.

(d) **Storage layout byte-identical at v38 vs `2654fcc2`.** Verdict: SAFE_BY_STRUCTURAL_CLOSURE. Evidence: FT_HERO_SHIFT = 237 (3-bit allocation) preserved per HERO-02 bit allocation lock; enabled bit at FT_HERO_SHIFT + 0 vestigial (always = 1 post-pack); freed bit reserved for future feature. Grep recipe: `diff <(git show 2654fcc2:contracts/modules/DegenerusGameDegeneretteModule.sol | grep -E "FT_.*_SHIFT") <(grep -E "FT_.*_SHIFT" contracts/modules/DegenerusGameDegeneretteModule.sol)` — expected exit 0.

(e) **Public ABI byte-identical.** Verdict: SAFE_BY_STRUCTURAL_CLOSURE. Evidence: `placeDegeneretteBet(address player, uint8 currency, uint128 amountPerTicket, uint8 ticketCount, uint32 customTicket, uint8 heroQuadrant)` signature UNCHANGED. `0xFF` and any `>= 4` input still accepted (normalized to 0 internally per HERO-01). Grep recipe G10 documented in §3.A.

(f) **Variance impact bound on risk-averse subset.** Verdict: SAFE_BY_DESIGN (accepted variance impact per user disposition). Evidence: under pre-v38 opt-out, variance-averse players could lock payout at basePayout (zero variance from hero multiplier). Under v38 always-on schedule, hero multiplier applies for M ∈ {2..7}: worst-case downside per spin = HERO_PENALTY × basePayout = 0.95 × basePayout; best-case upside = boost-magnitude × basePayout (per-N table, ~1.18×–2.50× at M=2..7). EV-neutral by construction (per (a)). Bounded variance increase; zero EV change. Documented in D-272-DESIGN-INTENT-01 actor walk-through.

(g) **`npm run test:stat` + `npm run test:gas` clean run at v38 close.** Verdict: SAFE_BY_STRUCTURAL_CLOSURE. Evidence: GASPIN-02 (a-alt) script-split (package.json) isolates gas-pin tests from `test:stat` ordering effects. GASPIN-03 verification at Task 2.5 confirmed clean exit codes for both scripts (with STAT-03-v35-carry ACCEPTED-DESIGN gate behaving per pre-edit exit semantics).

RNG commitment-window degenerate-PASS attestation (1-line per `feedback_rng_commitment_window.md`): Phase 272 has zero RNG-path mutation; commitment-window check is structurally trivial at v38 (no VRF request, fulfillment, or word-derived input flow was modified). EntropyLib byte-identical at v38 HEAD (REG-03 cross-cite).

§4.2 Verdict Roll-Up: 7 of 7 surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE per inline draft. Adversarial-pass validation via 3-skill PARALLEL spawn (Task 3.5) per D-272-ADVERSARIAL-01. Default zero F-38-NN finding blocks emit per D-272-FIND-01 carry.

Commit:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): §4 adversarial-sweep inline 7-surface draft [AUDIT-02]"
```
  </action>
  <verify>
    <automated>
grep -c "^## 4\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "Surface (a)\\|Surface (b)\\|Surface (c)\\|Surface (d)\\|Surface (e)\\|Surface (f)\\|Surface (g)" audit/FINDINGS-v38.0.md   # expect >=7
grep -c "SAFE_BY_DESIGN\\|SAFE_BY_STRUCTURAL_CLOSURE" audit/FINDINGS-v38.0.md   # expect >=7
grep -c "commitment-window" audit/FINDINGS-v38.0.md   # expect >=1
git log --oneline -1 | grep -c "audit(272): §4"   # expect 1
    </automated>
  </verify>
  <done>
§4 populated with 7 inline-draft surface rows (a)..(g) all SAFE_*. RNG commitment-window degenerate-PASS attestation present. §4.2 verdict roll-up declares zero F-38-NN finding blocks default. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3.3: AUDIT-03 §5 regression appendix (REG-01..04)</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (post-Task-3.2 state)
    - audit/FINDINGS-v37.0.md §5 (REG-01..04 row format template)
    - audit/FINDINGS-v36.0.md §5 (secondary template)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md (REG-03 EXC-04 backward-trace methodology)
    - audit/FINDINGS-v25.0.md, audit/FINDINGS-v27.0.md, ..., audit/FINDINGS-v37.0.md (REG-04 prior-finding sweep; spot-check rows)
  </read_first>
  <action>
Append §5 to `audit/FINDINGS-v38.0.md`:

§5a REG-01 — v37.0 closure-signal non-widening. Single PASS row. `delta_sha`: `2654fcc2..<v38-close-sha>` (1 Wave 1 contract commit + 1 Wave 2 test commit). `subject_at_head`: EntropyLib + JackpotModule + MintModule + TraitUtils byte-identical at v38 HEAD per SURF-01 grep proof (`cmp` exit 0 for each). Lootbox unchanged (D-272-CLEAN-SCOPE-01 narrowing). `re_verify_evidence`: v37 §4 8-surface verdicts (a)..(h) carry forward unchanged at v38 HEAD. Verdict: PASS.

§5b REG-02 — v34.0 closure-signal non-widening. Single PASS row. `delta_sha`: `6b63f6d4..<v38-close-sha>`. `subject_at_head`: TraitUtils existing functions + `_pickSoloQuadrant` + JackpotBucketLib + EntropyLib byte-identical at v38 HEAD. `re_verify_evidence`: v34 §4 6-surface trait/solo verdicts orthogonal to v38 hero-only payload; carry forward. Verdict: PASS.

§5c REG-03 — KI envelope re-verifications. 4 rows:
  - EXC-01 (affiliate roll) — NEGATIVE-scope at v38 (Phase 272 has zero affiliate-roll interaction; MintModule byte-identical).
  - EXC-02 (gameover prevrandao fallback) — NEGATIVE-scope at v38 (AdvanceModule byte-identical).
  - EXC-03 (gameover RNG substitution for mid-cycle write-buffer tickets) — NEGATIVE-scope at v38 (AdvanceModule + swap path byte-identical).
  - EXC-04 (EntropyLib XOR-shift PRNG — BAF-jackpot-only scope per v36 NARROWS) — RE_VERIFIED with NARROWS retained. Backward-trace per `feedback_rng_backward_trace.md`: EntropyLib.sol byte-identical at v38 HEAD vs `2654fcc2` (and vs v36.0 baseline `1c0f0913` per REG-01 chain). Phase 272 Degenerette payload does not consume xorshift; reads VRF-derived rngWord-indexed lootboxRngWordByIndex via `_resolveFullTicketBet`. BAF jackpot consumer remains; NARROWS scope preserved.

§5d REG-04 — Prior-finding spot-check sweep across `audit/FINDINGS-v25.0.md` ... `audit/FINDINGS-v37.0.md` for findings referencing v38-touched function/surface set: `_fullTicketPayout`, `_packFullTicketBet`, `_resolveFullTicketBet`, `_applyHeroMultiplier`, `MASK_3`, `FT_HERO_SHIFT`, hero quadrant, hero opt-out. Default expected: ALL PASS (none of those functions existed pre-v37 outside Phase 267's Degenerette producer + 5-table payout rewrite which is itself an audit subject of FINDINGS-v37.0.md). Row format per v37.0 §5c precedent. Minimum 5 rows; SUPERSEDED allowed with explicit successor cite.

§5e Distribution Summary: N PASS, 0 SUPERSEDED, 0 REGRESSED per default path. Cite REG-01 + REG-02 + REG-03 (4 EXC rows) + REG-04 sweep count.

Commit:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): §5 regression appendix REG-01..04 [REG-01, REG-02, REG-03, REG-04]"
```
  </action>
  <verify>
    <automated>
grep -c "^## 5\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "REG-01\\|REG-02\\|REG-03\\|REG-04" audit/FINDINGS-v38.0.md   # expect >=4
grep -c "EXC-01\\|EXC-02\\|EXC-03\\|EXC-04" audit/FINDINGS-v38.0.md   # expect >=4
grep -c "MILESTONE_V37_AT_HEAD_2654fcc2\\|MILESTONE_V34_AT_HEAD_6b63f6d4" audit/FINDINGS-v38.0.md   # expect >=2
git log --oneline -1 | grep -c "audit(272): §5"   # expect 1
    </automated>
  </verify>
  <done>
§5 populated with §5a REG-01 + §5b REG-02 + §5c REG-03 (4 EXC rows) + §5d REG-04 prior-finding sweep + §5e distribution summary. EXC-04 backward-trace cite to `feedback_rng_backward_trace.md` + EntropyLib byte-identical at v38 HEAD. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3.4: AUDIT-04 §6 KI gating walk + §7 cross-cites + §8 forward-cite closure</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (post-Task-3.3 state)
    - audit/FINDINGS-v37.0.md §6 + §7 + §8 (templates)
    - KNOWN-ISSUES.md (current state; verify UNMODIFIED at v38 phase-close per D-272-KI-01)
  </read_first>
  <action>
Append §6, §7, §8 to `audit/FINDINGS-v38.0.md`:

§6 KI Gating Walk:
  - §6a Non-Promotion Ledger — default zero rows per D-272-KI-01.
  - §6b 4-row KI envelope re-verifications. Mirror §5c content (EXC-01..03 NEGATIVE-scope at v38; EXC-04 RE_VERIFIED with NARROWS-retained — BAF-jackpot-only scope from v36 carried forward at v38 because EntropyLib byte-identical at v38 HEAD).
  - §6c Verdict Summary: `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per D-272-KI-01 default zero-promotion path.

§7 Prior-Artifact Cross-Cites — table of v25.0..v37.0 FINDINGS deliverables with one-line topical anchor per row; v38 inherits the chain.

§8 Forward-Cite Closure — terminal-phase invariant per D-272-FCITE-01. Grep recipe block:
```
! grep -rE "Phase (273|274|275|276)|v39\\.0|v40\\.0" audit/FINDINGS-v38.0.md .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/
```
Expected: exit 0 (zero matches). Document the recipe with expected output inline so future audits can re-run.

**Exception clause:** if Task 2.4 LBX-02 FORMAL RE-DEFER prose block in `test/gas/LootboxOpenGas.test.js` contains `v39+`, the grep recipe must EXCLUDE the test file path (terminal-phase invariant applies to audit/ + .planning/phases/272-*/ artifacts only; test-file forward references are operational pickup pointers not audit forward-cites). Update the grep recipe accordingly:
```
! grep -rE "Phase (273|274|275|276)|v39\\.0|v40\\.0" audit/FINDINGS-v38.0.md .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/ --exclude='*LootboxOpenGas*'
```
Or simpler: scope to `audit/FINDINGS-v38.0.md` + `.planning/phases/272-*/272-CONTEXT.md` + `.planning/phases/272-*/272-DISCUSSION-LOG.md` + `.planning/phases/272-*/272-01-PLAN.md` + `.planning/phases/272-*/272-01-SUMMARY.md` + `.planning/phases/272-*/272-01-ADVERSARIAL-LOG.md` only. Inline `v39+` mentions in the FINDINGS file itself (e.g., in §9.NN.iv LBX-02 RE-DEFER cite) are LOAD-BEARING and acceptable per terminal-phase pickup-pointer carve-out — document the carve-out inline in §8 prose: "Pickup-pointer carve-out: §9.NN.iv RE-DEFER entries may cite `v39+` as the explicit deferral target. This is a forward-pointer for future-phase pickup, not a forward-cite to a planned future phase. Terminal-phase invariant remains satisfied."

Commit:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): §6 KI gating + §7 cross-cites + §8 forward-cite closure [AUDIT-04, D-272-FCITE-01, D-272-KI-01]"
```
  </action>
  <verify>
    <automated>
grep -c "^## 6\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "^## 7\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "^## 8\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "KI_ELIGIBLE_PROMOTED" audit/FINDINGS-v38.0.md   # expect >=1
grep -c "KNOWN_ISSUES_UNMODIFIED" audit/FINDINGS-v38.0.md   # expect >=1
grep -c "Pickup-pointer carve-out" audit/FINDINGS-v38.0.md   # expect >=1
git log --oneline -1 | grep -c "audit(272): §6"   # expect 1
    </automated>
  </verify>
  <done>
§6 + §7 + §8 populated. KI verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Forward-cite closure recipe documented with carve-out for §9.NN.iv pickup-pointer entries. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3.5: AUDIT-06 — 3-skill PARALLEL adversarial pass + log to 272-01-ADVERSARIAL-LOG.md</name>
  <files>.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (FINISHED §4 7-surface draft; this is the input to the adversarial pass)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-CONTEXT.md (D-272-ADVERSARIAL-01 — 3-skill PARALLEL via single message; /degen-skeptic OUT OF SCOPE)
    - audit/FINDINGS-v37.0.md (precedent for 3-skill output format)
    - .planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md (2-skill v36 precedent for log structure)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md (FINDING_CANDIDATE escalation rule)
  </read_first>
  <action>
Spawn 3 skills IN PARALLEL via a SINGLE message (single Claude turn with 3 tool calls in one block) per D-272-ADVERSARIAL-01:

(1) `/contract-auditor` — Solidity-focused red-team review of the finished §4 7-surface inline draft + the Wave 1 contract diff + the Wave 2 test diff. Look for: missed surfaces; weak verdict justifications; storage-layout edge cases; ABI-byte-identity gaps; HERO-01 normalization edge cases (e.g., what if `heroQuadrant` is passed as a signed value or via assembly); cleanup-removal blast radius beyond Degenerette module.

(2) `/zero-day-hunter` — Vulnerability-class red-team review. Look for: novel attack surfaces opened by removing the opt-out; variance-channel exploitation; payout-bias under quadrant-0 default; RNG commitment-window assumptions; cross-module byte-identity assumptions; carry-forward picks (LBX-02 RE-DEFER prose vs actual gas envelope; STAT-03-v35-carry ACCEPTED-DESIGN gate semantics).

(3) `/economic-analyst` — Mechanism-design red-team review. Look for: variance impact on risk-averse subset (CONTEXT.md flagged this as bounded acceptable); skill-expression channel preservation; player-side strategy shifts post-opt-out-removal; whale vs casual differential; UI-flow simplification consequence on player decision space.

NOT `/degen-skeptic` (explicitly OUT OF SCOPE per D-271-ADVERSARIAL-02 carry → D-272-ADVERSARIAL-01).

Create `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md` with three H2 sections:

```
# Phase 272 Plan 01 — Adversarial Validation Log

Generated: <date>
Adversarial-pass pattern: 3-skill PARALLEL via single message (D-272-ADVERSARIAL-01).
Input: audit/FINDINGS-v38.0.md §4 7-surface inline draft (post-Task-3.4 state).

## /contract-auditor

<verbatim output from skill spawn>

## /zero-day-hunter

<verbatim output from skill spawn>

## /economic-analyst

<verbatim output from skill spawn>

## Disposition

<one-paragraph synthesis>:
- Disagreements: <list each, with surface ID + skill that raised>; or `None` if all 3 skills concurred.
- 8th-surface NEW_VECTOR candidates: <list>; or `None`.
- KI promotion candidates: <list>; or `None`.
- FINDING_CANDIDATE escalation per `feedback_wait_for_approval.md`: <list with user-disposition required>; or `None`.

If FINDING_CANDIDATE list is non-empty: HALT here and escalate to user. Per D-272-KI-01 deviation procedure, user disposition required before §4 verdict roll-up stands. Per `feedback_wait_for_approval.md`, do NOT proceed to Task 3.6 without explicit user approval to either (a) accept the candidate into FINDINGS as an F-38-NN block, (b) accept it as a KI Design Decisions entry (KNOWN-ISSUES.md edit required — REVERTS D-272-KI-01 default), or (c) dispose it as out-of-scope with cited rationale.

If FINDING_CANDIDATE list is empty (default expected path): proceed to Task 3.6.
```

Commit (after skill outputs captured + Disposition authored):
```
git add .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md && git commit -m "audit(272): 3-skill PARALLEL adversarial-pass log [AUDIT-06, D-272-ADVERSARIAL-01]"
```
  </action>
  <verify>
    <automated>
test -f .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md; echo "log_file_exit=$?"   # expect 0
grep -c "^## /contract-auditor$" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect 1
grep -c "^## /zero-day-hunter$" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect 1
grep -c "^## /economic-analyst$" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect 1
grep -c "^## Disposition$" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect 1
grep -c "/degen-skeptic" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect 0 (OUT OF SCOPE)
wc -l .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md   # expect >=50
git log --oneline -1 | grep -c "audit(272): 3-skill PARALLEL"   # expect 1
    </automated>
  </verify>
  <done>
3-skill PARALLEL spawn executed via SINGLE message. `272-01-ADVERSARIAL-LOG.md` has H2 sections for `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` + Disposition. `/degen-skeptic` absent. Disposition documents: FINDING_CANDIDATE list (default `None` -> proceed; if non-`None` -> HALT for user). Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3.6: AUDIT-05 — §9 closure-attestation TWO-subsection format + §9.NN.iv RE-DEFER entries</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (post-Task-3.4 state; §9 not yet populated)
    - audit/FINDINGS-v37.0.md §9 (TWO-subsection format precedent per D-271-CLOSURE-02)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md (Disposition outcome; load-bearing for §9 verdict roll-up)
  </read_first>
  <action>
Append §9 Milestone Closure Attestation to `audit/FINDINGS-v38.0.md`. Mirror v37.0 §9 TWO-subsection format per D-272-CLOSURE-02:

§9a Verdict Distribution: 7 surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; 0 FINDING_CANDIDATE (per Task 3.5 Disposition; default path); 0 KI promotions; KNOWN_ISSUES_UNMODIFIED.

§9b Attestation Block: D-253-15 step 9 attestation prose. Cite 3-skill PARALLEL adversarial-pass concurrence (or non-concurrence + user disposition if Task 3.5 escalated).

§9c Closure Signal Emission: emit `MILESTONE_V38_AT_HEAD_<sha>` as a load-bearing string. Leave `<sha>` as a placeholder; resolved at Task 4.6 atomic-update.

§9.NN Commit-Readiness Register (TWO-subsection format per D-272-CLOSURE-02, NOT three-subsection):
  - §9.NN.i USER-APPROVED contracts — cite Wave 1 batched commit `feat(272): always-on hero default 0 + degenerette dead-code cleanup [HERO-01..05, CLEAN-01..05]` at `<wave-1-sha>`.
  - §9.NN.ii USER-APPROVED tests — cite Wave 2 batched commit `test(272): hero-always-on + dead-code cleanup + v37+ carry bundle [...]` at `<wave-2-sha>`.
  - §9.NN.iii AGENT-COMMITTED audit artifacts — cite Tasks 3.1-3.6 + 4.1-4.7 atomic-commit chain.
  - **NO §9.NN.iv awaiting-approval subsection** per D-272-CLOSURE-02.

§9.NN.iv Carry-Forward RE-DEFER Register (separate subsection from the TWO-subsection commit-readiness register; pickup-pointer carve-out per §8):
  - LBX-02 — FORMAL RE-DEFER to v39+. Cite path-of-investigation prose in `test/gas/LootboxOpenGas.test.js` header. Cross-cite §3.A row.
  - STAT-03-v35-carry — ACCEPTED-DESIGN. Cite ledger entry in `test/stat/PerPullEmptyBucketSkip.test.js` header. Cross-cite §3.A row.
  - GASPIN-02 (a-alt) — script-split applied at `package.json`. CLOSED at v38 (not re-deferred). Verdict: COMPLETE.
  - GASPIN-03 — clean `test:stat` + `test:gas` run verified at Task 2.5. CLOSED at v38. Verdict: COMPLETE.

Commit:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): §9 closure-attestation TWO-subsection + §9.NN.iv RE-DEFER register [AUDIT-05, D-272-CLOSURE-01, D-272-CLOSURE-02]"
```
  </action>
  <verify>
    <automated>
grep -c "^## 9\\. " audit/FINDINGS-v38.0.md   # expect 1
grep -c "### 9.NN.i" audit/FINDINGS-v38.0.md   # expect 1
grep -c "### 9.NN.ii" audit/FINDINGS-v38.0.md   # expect 1
grep -c "### 9.NN.iii" audit/FINDINGS-v38.0.md   # expect 1
grep -c "### 9.NN.iv" audit/FINDINGS-v38.0.md   # expect 1 (RE-DEFER register, separate from TWO-subsection)
grep -c "awaiting-approval" audit/FINDINGS-v38.0.md   # expect 0 per D-272-CLOSURE-02
grep -c "MILESTONE_V38_AT_HEAD_" audit/FINDINGS-v38.0.md   # expect >=3 (frontmatter + §2 + §9c + §9.NN, final >=5 after Task 4.6 expansion)
git log --oneline -1 | grep -c "audit(272): §9"   # expect 1
    </automated>
  </verify>
  <done>
§9 populated with TWO-subsection §9.NN commit-readiness register (i + ii + iii; no `awaiting-approval` subsection) per D-272-CLOSURE-02. §9.NN.iv RE-DEFER register documents LBX-02 + STAT-03-v35-carry + GASPIN-02 (a-alt) + GASPIN-03 outcomes. Closure signal `MILESTONE_V38_AT_HEAD_<sha>` placeholder present in >=3 locations. Atomic commit landed.
  </done>
</task>

<!-- ================================================================ -->
<!-- WAVE 4 — CLOSURE FLIPS (AGENT-COMMITTED atomic-per-task)          -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 4.1: REQUIREMENTS.md flip — 30 v38.0 Phase 272 requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-PLAN.md frontmatter `deferred_picks` (LBX-02 RE-DEFERRED-V39+, GASPIN-02 COMPLETE via (a-alt), STAT-03-v35-carry COMPLETE via ACCEPTED-DESIGN)
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` Traceability table (L78-110 region):
- HERO-01..05 → Complete
- CLEAN-01..06 → Complete
- STAT-01..02 → Complete
- SURF-01..03 → Complete
- LBX-02 → RE-DEFERRED-V39+ (per planner pick: formal re-defer with path-of-investigation prose; cross-cite audit/FINDINGS-v38.0.md §9.NN.iv)
- GASPIN-02 → Complete (via (a-alt) script-split per planner pick; cross-cite package.json change)
- GASPIN-03 → Complete (clean test:stat + test:gas run verified)
- STAT-03-v35-carry → Complete (via ACCEPTED-DESIGN ledger entry per planner pick; cross-cite test/stat/PerPullEmptyBucketSkip.test.js header)
- AUDIT-01..06 → Complete
- REG-01..04 → Complete

Update coverage tally: 30/30 mapped → 29/30 Complete + 1/30 RE-DEFERRED-V39+ (LBX-02). Update the prose: "v37+ carry-forward pickup count: 5 items folded in. Resolution at v38 close: 4 COMPLETE (SURF-03 + GASPIN-02 + GASPIN-03 + STAT-03-v35-carry) + 1 RE-DEFERRED-V39+ (LBX-02 with path-of-investigation prose)."

Commit:
```
git add .planning/REQUIREMENTS.md && git commit -m "docs(272): mark 30 v38.0 Phase 272 requirements complete (29) + LBX-02 RE-DEFERRED-V39+"
```
  </action>
  <verify>
    <automated>
grep -c "HERO-01.*Complete" .planning/REQUIREMENTS.md   # expect 1
grep -c "LBX-02.*RE-DEFERRED" .planning/REQUIREMENTS.md   # expect 1
grep -c "GASPIN-02.*Complete\\|GASPIN-03.*Complete" .planning/REQUIREMENTS.md   # expect 2
grep -c "STAT-03-v35-carry.*Complete" .planning/REQUIREMENTS.md   # expect 1
git log --oneline -1 | grep -c "docs(272): mark 30 v38.0"   # expect 1
    </automated>
  </verify>
  <done>
REQUIREMENTS.md flipped: 29 Complete + 1 RE-DEFERRED-V39+ across 30 v38.0 requirement IDs. Coverage prose updated. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4.2: ROADMAP.md flip — v38.0 milestone shipped + closure signal recorded</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state; L21 v38.0 milestone row marked 🚧)
  </read_first>
  <action>
Update `.planning/ROADMAP.md`:

(1) L21 milestone row: change 🚧 → ✅. Append closure signal: `✅ **v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup** — Phase 272 (shipped 2026-05-11; closure signal `MILESTONE_V38_AT_HEAD_<sha>`)`.

(2) Phase 272 entry (L26 + L30-51): tick the plan-list checkbox: `- [x] 272-01-PLAN.md — ...`. Mark Phase 272 status complete in any explicit status field.

(3) Collapse the v38.0 phase details block into a `<details><summary>` block mirroring the v37.0 collapsed block at L54-65, AFTER the closure signal is resolved at Task 4.6 (this task leaves the details block expanded; Task 4.6 may collapse it OR a follow-up commit handles).

(4) Resolve `<sha>` placeholder: at this task, leave it as `<sha>`. Task 4.6 atomic-updates to the real value across all documents simultaneously.

Commit:
```
git add .planning/ROADMAP.md && git commit -m "docs(272): mark v38.0 milestone shipped + Phase 272 complete in ROADMAP"
```
  </action>
  <verify>
    <automated>
grep -c "✅ \\*\\*v38.0" .planning/ROADMAP.md   # expect 1
grep -c "\\[x\\] 272-01-PLAN.md" .planning/ROADMAP.md   # expect 1
grep -c "MILESTONE_V38_AT_HEAD_" .planning/ROADMAP.md   # expect >=1 (placeholder or final)
git log --oneline -1 | grep -c "docs(272): mark v38.0 milestone shipped"   # expect 1
    </automated>
  </verify>
  <done>
ROADMAP.md milestone row flipped to ✅. Phase 272 plan-list checkbox ticked. Closure signal placeholder `MILESTONE_V38_AT_HEAD_<sha>` recorded. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4.3: STATE.md flip — v38.0 closed, Last Shipped Milestone updated</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state; v37.0 in Just-Shipped Milestone Reference at L87+)
  </read_first>
  <action>
Update `.planning/STATE.md`:

(1) Frontmatter (L1-13): update `milestone` + `milestone_name` + `status` + `progress` (1/1 phase complete; 1/1 plan complete; 100%). `last_updated` → current ISO 8601 timestamp. `last_activity` → "2026-05-11 — Milestone v38.0 SHIPPED at MILESTONE_V38_AT_HEAD_<sha>".

(2) L22 `**Current focus:**` → "between-milestones — v38.0 SHIPPED 2026-05-11 at MILESTONE_V38_AT_HEAD_<sha>; next milestone scope TBD".

(3) L25-30 Current Position → reset to between-milestones state.

(4) L32-46 Last Shipped Milestone block: REPLACE the v36.0 entry with v38.0 entry (mirror v36.0 block shape). Move the existing v36.0 block to "Prior Shipped Milestone". The existing v35.0 + v34.0 prior blocks may rotate or stay; preserve the existing 3-deep chain at minimum.

(5) L83-104 Active Milestone — clear to between-milestones state. Add new "Just-Shipped Milestone Reference" for v38.0 mirroring the v37.0 block structure (Goal, Audit baseline, Phases planned, Requirements, READ-only posture, etc.). Move existing v37.0 just-shipped block down into the rotating history.

(6) L106-116 Roadmap Overview table → replace v37.0 rows with v38.0 Phase 272 row.

(7) Operator Next Steps → update to reflect v38.0 shipped + next-milestone scoping recommendations.

Commit:
```
git add .planning/STATE.md && git commit -m "docs(272): mark v38.0 milestone shipped in STATE.md + rotate Last Shipped chain"
```
  </action>
  <verify>
    <automated>
grep -c "MILESTONE_V38_AT_HEAD_" .planning/STATE.md   # expect >=2
grep -c "milestone: v38.0" .planning/STATE.md   # expect 1
grep -c "Just-Shipped Milestone Reference" .planning/STATE.md   # expect 1
grep -c "v37.0 Degenerette Recalibration\\|Prior Shipped Milestone" .planning/STATE.md   # expect >=1 (v37 demoted)
git log --oneline -1 | grep -c "docs(272): mark v38.0 milestone shipped in STATE"   # expect 1
    </automated>
  </verify>
  <done>
STATE.md frontmatter + status fields reflect v38.0 SHIPPED. Last Shipped Milestone → v38.0; v37.0 demoted. Active Milestone → between-milestones. Operator Next Steps updated. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4.4: MILESTONES.md flip + PROJECT.md append</name>
  <files>.planning/MILESTONES.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/MILESTONES.md (current state; v37.0 at top)
    - .planning/PROJECT.md (current state; tail for append point)
  </read_first>
  <action>
(1) `.planning/MILESTONES.md`: prepend v38.0 entry at the top with closure signal + phase shape (single-phase) + commit shape (2 USER-APPROVED batched + N AGENT-COMMITTED) + §4 surface count (7) + REG row count + KI verdict + closure signal `MILESTONE_V38_AT_HEAD_<sha>`. Demote v37.0 by one position.

(2) `.planning/PROJECT.md`: append a v38.0 entry to the accumulated-context decisions log + a completed-milestone entry. Mirror the v37.0 entry structure (one paragraph milestone summary + decision-anchor IDs).

Commit:
```
git add .planning/MILESTONES.md .planning/PROJECT.md && git commit -m "docs(272): prepend v38.0 entry to MILESTONES + append PROJECT accumulated-context"
```
  </action>
  <verify>
    <automated>
grep -c "MILESTONE_V38_AT_HEAD_" .planning/MILESTONES.md   # expect >=1
grep -c "MILESTONE_V38_AT_HEAD_\\|v38.0 Always-Hero Simplification" .planning/PROJECT.md   # expect >=1
git log --oneline -1 | grep -c "docs(272): prepend v38.0 entry to MILESTONES"   # expect 1
    </automated>
  </verify>
  <done>
MILESTONES.md has v38.0 entry at top; v37.0 demoted. PROJECT.md has v38.0 accumulated-context entry. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4.5: Write 272-01-SUMMARY.md phase-closure summary</name>
  <files>.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md</files>
  <read_first>
    - .planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md (format precedent; planner has not seen but file path documented in CONTEXT.md)
    - .planning/REQUIREMENTS.md (final tally — 29 Complete + 1 RE-DEFERRED post-Task-4.1)
    - audit/FINDINGS-v38.0.md (closure signal + verdict roll-up)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-ADVERSARIAL-LOG.md (Disposition)
  </read_first>
  <action>
Create `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md` mirroring the v37.0 / v36.0 / v35.0 phase-closure summary format. Content blocks:

- Frontmatter: `phase`, `phase_number`, `plan`, `requirements-completed:` (29 IDs), `requirements-redeferred:` (LBX-02), `milestone`, `closure_signal: MILESTONE_V38_AT_HEAD_<sha>` (placeholder; resolved at Task 4.6).
- Per-task atomic-commit log: Tasks 1.1 (no commit, batched), 1.2 (no commit, batched), 1.3 (no commit, batched), 1.4 (Wave 1 batched USER-APPROVED), 2.1-2.6 (no commit, batched), 2.7 (Wave 2 batched USER-APPROVED), 3.1-3.6 (atomic), 4.1-4.5 (atomic), 4.6 (atomic SHA-resolution), 4.7 (final user-review gate + READ-only flip).
- Per-REQ tally for 30 v38.0 requirements: 29 Complete + 1 RE-DEFERRED-V39+ (LBX-02).
- Cross-phase cross-cite density: count of cross-cites to v37.0 (REG-01 chain) + v36.0 (EXC-04 NARROWS chain) + v34.0 (REG-02 chain) + v35.0 (STAT-03-v35-carry chain) + v25.0..v37.0 (REG-04 sweep).
- Project-feedback-rules-honored table: 11 feedback files cited in CONTEXT.md `<canonical_refs>` → mark each as honored at which task with a 1-line cite.
- Closure signal: `MILESTONE_V38_AT_HEAD_<sha>` (placeholder; resolved at Task 4.6).
- Phase shape: single-phase multi-wave; 2 USER-APPROVED batched commits + N AGENT-COMMITTED atomic commits.
- Adversarial-pass outcome: 3-skill PARALLEL; Disposition default (or escalated state if Task 3.5 surfaced FINDING_CANDIDATE).

Commit:
```
git add .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md && git commit -m "docs(272): write 272-01-SUMMARY.md phase-closure summary"
```
  </action>
  <verify>
    <automated>
test -f .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md; echo "summary_file_exit=$?"   # expect 0
grep -c "requirements-completed:" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md   # expect 1
grep -c "MILESTONE_V38_AT_HEAD_" .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md   # expect >=1
wc -l .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md   # expect >=80
git log --oneline -1 | grep -c "docs(272): write 272-01-SUMMARY.md"   # expect 1
    </automated>
  </verify>
  <done>
272-01-SUMMARY.md exists with per-task atomic-commit log + per-REQ tally + cross-cite density + feedback-rules table + closure signal placeholder. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4.6: Closure-signal SHA resolution — atomic-update across 5 FINDINGS + 3 cross-doc targets</name>
  <files>audit/FINDINGS-v38.0.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md (current state with `<sha>` placeholders)
    - .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/phases/.../272-01-SUMMARY.md (each with `<sha>` placeholders)
  </read_first>
  <action>
Resolve `<sha>` to a concrete value. Strategy:

(1) Capture current HEAD: `SHA=$(git rev-parse HEAD)` (this is the commit from Task 4.5 — the most recent placeholder-bearing commit before SHA resolution).

(2) The closure-signal SHA should reference the COMMIT WHERE THE ATTESTATION LANDS. There are two valid options per v36.0 / v37.0 precedent:
  - **Option A (mutation-inclusive HEAD at attestation commit):** SHA = the SHA of THIS task's commit (which substitutes `<sha>` placeholders). The closure-signal references its own commit. Acceptable per D-272-CLOSURE-01 "mutation-inclusive HEAD since Phase 272 introduces 2 USER-APPROVED batched commits".
  - **Option B (pre-resolution HEAD):** SHA = current HEAD BEFORE this commit (i.e., Task 4.5's commit SHA). Closure signal then becomes a backward-reference. Phase 271 precedent at v37.0 used this pattern (closure signal = `2654fcc2` = the Task 12 §9 attestation commit's parent).

Use **Option A** (mutation-inclusive HEAD). Capture `SHA_PRE` = current HEAD; the substitution is done across all files THEN the commit is made; the commit's own SHA becomes `SHA`. Since we can't know the future SHA of a not-yet-made commit, the workaround: substitute `<sha>` with a deterministic placeholder `MILESTONE_V38_AT_HEAD_<TBD>`, make the commit, capture the commit SHA as `SHA_FINAL`, then immediately make a follow-on amend-equivalent commit substituting `<TBD>` with the short form of `SHA_FINAL`. Per `feedback_no_contract_commits.md` we do NOT amend (`feedback_no_contract_commits.md` applies to contracts/test; for .planning/audit AGENT-COMMITTED amendment is acceptable but creates a clean two-commit pair). Acceptable simpler path: use **Option B** (pre-resolution HEAD = Task 4.5 commit SHA; closure signal points backward to the commit chain's most-recent pre-attestation HEAD). This matches Phase 271 precedent.

Pick **Option B** per Phase 271 precedent. Pseudo-script:
```
SHA=$(git rev-parse HEAD)              # this is the Task 4.5 SUMMARY commit's SHA
SHA8=${SHA:0:8}                        # short form
sed -i "s/MILESTONE_V38_AT_HEAD_<sha>/MILESTONE_V38_AT_HEAD_$SHA8/g" audit/FINDINGS-v38.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md
sed -i "s/audit_subject_head: <sha>/audit_subject_head: $SHA/g" audit/FINDINGS-v38.0.md
```

(3) Verify closure signal verbatim presence in 5 FINDINGS locations + 3 cross-doc targets:
  - 5 FINDINGS locations: frontmatter `closure_signal:` field + §2 Executive Summary + §9c trailing line + §9.NN attestation block + (1 additional location per v37.0 precedent — typically `audit_subject_head` field OR a § Anchor Sentence in §2). Verify with `grep -c MILESTONE_V38_AT_HEAD_$SHA8 audit/FINDINGS-v38.0.md` ≥ 5.
  - 3 cross-doc targets: `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/MILESTONES.md`. Verify each contains the signal at least once.

Commit:
```
git add audit/FINDINGS-v38.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md && git commit -m "docs(272): resolve closure-signal SHA MILESTONE_V38_AT_HEAD_${SHA8} across 5 FINDINGS + 3 cross-doc targets [D-272-CLOSURE-01]"
```
  </action>
  <verify>
    <automated>
# Compute the resolved SHA from the commit history (Task 4.5 commit, which is HEAD~1 relative to Task 4.6 commit; but we ran the sed BEFORE this commit so SHA in files == HEAD~1 of post-this-commit)
SHA_USED=$(grep -oE "MILESTONE_V38_AT_HEAD_[0-9a-f]{8}" audit/FINDINGS-v38.0.md | head -1)
echo "SHA_USED=$SHA_USED"
# 5 FINDINGS locations
grep -c "$SHA_USED" audit/FINDINGS-v38.0.md   # expect >=5
# 3 cross-doc targets
grep -lE "$SHA_USED" .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md | wc -l   # expect 3
# Placeholder fully resolved
grep -rE "MILESTONE_V38_AT_HEAD_<sha>|MILESTONE_V38_AT_HEAD_<TBD>" audit/FINDINGS-v38.0.md .planning/ .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/   # expect 0 matches
git log --oneline -1 | grep -c "docs(272): resolve closure-signal SHA"   # expect 1
    </automated>
  </verify>
  <done>
`MILESTONE_V38_AT_HEAD_<SHA8>` resolved across 5 FINDINGS locations + 3 cross-document targets (ROADMAP / STATE / MILESTONES) per D-272-CLOSURE-01. Zero unresolved `<sha>` or `<TBD>` placeholders remain. Atomic commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.7: Final user-review gate + READ-only flip on audit/FINDINGS-v38.0.md</name>
  <files>audit/FINDINGS-v38.0.md</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md (final user-review gate; agent does NOT push)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
    - audit/FINDINGS-v38.0.md (full file at v38 close state)
  </read_first>
  <action>See <what-built> + <how-to-verify> below — this is a checkpoint:human-verify task; the action is to present the diff/state to the user and await an explicit `approved` string before committing per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Detailed steps in <how-to-verify>.</action>
  <verify>
    <automated>Manual verification only — checkpoint:human-verify gate. User runs the steps in &lt;how-to-verify&gt; and types `approved` to resume. No automated pass criterion; resume-signal IS the verify.</automated>
  </verify>
  <what-built>
audit/FINDINGS-v38.0.md is the v38.0 milestone-closure deliverable. 9 sections populated (§1 frontmatter + §2 Executive Summary + §3 Per-Phase + §3.A delta-surface + §3.B zero-new-state + §3.C conservation + §4 7-surface adversarial + §5 regression + §6 KI gating + §7 cross-cites + §8 forward-cite closure + §9 closure attestation). Closure signal `MILESTONE_V38_AT_HEAD_<SHA8>` emitted verbatim in 5 FINDINGS locations + 3 cross-document targets. 3-skill PARALLEL adversarial pass logged in 272-01-ADVERSARIAL-LOG.md.
  </what-built>
  <how-to-verify>
1. Present `cat audit/FINDINGS-v38.0.md` (or `wc -l + section-header summary`) to the user.
2. Present `git log --oneline 2654fcc2..HEAD` to show full v38.0 commit chain (2 USER-APPROVED Wave 1+2 + N AGENT-COMMITTED Wave 3+4).
3. Confirm grep gates:
   - G16: zero v39+ forward-cites in scoped artifacts (FINDINGS + 272-CONTEXT + 272-DISCUSSION-LOG + 272-01-PLAN + 272-01-SUMMARY + 272-01-ADVERSARIAL-LOG); pickup-pointer carve-out in test files is acceptable per §8.
   - G17: closure-signal verbatim count >=5 in `audit/FINDINGS-v38.0.md`.
   - G18: closure-signal verbatim presence in 3 cross-doc targets.
   - 3-skill PARALLEL adversarial-pass Disposition is `None` (default) OR user-dispositioned (escalated path).
4. **WAIT** for the user to type the literal string `approved` (case-sensitive).
5. Upon `approved`, flip `audit/FINDINGS-v38.0.md` to FINAL READ-only:
   - In frontmatter, change `status: DRAFT` → `status: FINAL — READ-ONLY`; `read_only: false` → `read_only: true`.
   - On filesystem: `chmod 444 audit/FINDINGS-v38.0.md`.
6. Commit the READ-only flip:
```
git add audit/FINDINGS-v38.0.md && git commit -m "audit(272): flip audit/FINDINGS-v38.0.md to FINAL READ-only [AUDIT-05]"
```
7. Per `feedback_manual_review_before_push.md`: agent does NOT push. User reviews the full commit chain (`git log --oneline 2654fcc2..HEAD` should show ~13 commits across Waves 1-4 plus the final READ-only flip) and runs `git push` themselves when ready.
  </how-to-verify>
  <resume-signal>User types `approved` -> agent runs chmod 444 + frontmatter status flip + commit. Anything else -> no flip; agent answers question or revises.</resume-signal>
  <done>
`audit/FINDINGS-v38.0.md` is chmod 444 + frontmatter `status: FINAL — READ-ONLY` + `read_only: true`. Final atomic commit landed. v38.0 milestone closure complete. Agent does NOT push; user pushes manually per `feedback_manual_review_before_push.md`.
  </done>
</task>

</tasks>



<verification>
Phase-level verification (see per-task `<verify>` for fine-grain). Plan-close gates:

1. **Wave 1 close:** Wave 1 USER-APPROVED batched contract commit lands. G1..G11 grep gates all return expected outputs. `npx hardhat compile` exits 0. Bytecode delta + gas-per-spin delta recorded in commit message.
2. **Wave 2 close:** Wave 2 USER-APPROVED batched test commit lands. G14, G15, G19 grep gates pass. `npm run test:stat` exits 0 (GASPIN-03 verification). `npm run test:gas` exits 0 (GASPIN-02 (a-alt) script-split verification). `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js test/stat/SurfaceRegression.test.js` exits 0.
3. **Wave 3 close:** `audit/FINDINGS-v38.0.md` exists with 9 sections + closure signal placeholder in §9c. G16 forward-cite grep gate passes (zero v39+ emission). 3-skill PARALLEL adversarial pass log exists at `272-01-ADVERSARIAL-LOG.md` with 3 H2 sections + Disposition note.
4. **Wave 4 close:** G17 closure-signal verbatim count >= 5 in FINDINGS. G18 closure-signal cross-document propagation = 3 files. REQUIREMENTS + ROADMAP + STATE + MILESTONES + PROJECT all reflect v38.0 shipped. `272-01-SUMMARY.md` exists.
5. **Final user-review gate:** Full diff presented before READ-only flip (chmod 444) on `audit/FINDINGS-v38.0.md`; explicit `approved` string awaited per `feedback_manual_review_before_push.md`. Agent does NOT push.

</verification>

<success_criteria>

Phase 272 ships when all 30 v38.0 requirements are satisfied per the must_haves register + the closure signal `MILESTONE_V38_AT_HEAD_<sha>` is emitted verbatim in 5 FINDINGS locations + 3 cross-document propagation targets (ROADMAP / STATE / MILESTONES) + REQUIREMENTS/ROADMAP/STATE/MILESTONES/PROJECT all reflect shipped state + `audit/FINDINGS-v38.0.md` is FINAL READ-only (chmod 444 + `status: FINAL — READ-ONLY` + `read_only: true` in frontmatter) + LBX-02 / GASPIN-02 / STAT-03-v35-carry deferred-picks resolved per `deferred_picks` block + 3-skill PARALLEL adversarial pass returned zero FINDING_CANDIDATE (default) OR any FINDING_CANDIDATE was dispositioned by user per `feedback_wait_for_approval.md`.

</success_criteria>

<output>
After Wave 4 close, create `.planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md` per executor convention (mirror `271-01-SUMMARY.md` format: per-task atomic-commit log + per-REQ tally for 30 v38.0 requirements + cross-phase cross-cite density + project-feedback-rules-honored table + closure signal `MILESTONE_V38_AT_HEAD_<sha>` verbatim).
</output>

