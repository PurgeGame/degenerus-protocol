# v66.0 RNG-Surface & Cross-Contract-Call Manipulability Re-Audit (Cross-Model Council) — ARCHIVED

✅ **AUDIT COMPLETE + PUSHED 2026-06-16** · Phases 410–415 · subject byte-frozen `contracts/` tree `0dd445a6` (commit `42c8e9c6` = origin/main `bb0912a6` + the USER-approved `CurseChanged` indexer-parity emit) · closure signal `MILESTONE_V66_AT_HEAD_e4df55c68f8c00e1df9ff2bd1e7840764aa68e7e` · `origin/main` `aa8e8f9e`.

## VERDICT: 0 CATASTROPHE / 0 HIGH / 0 MEDIUM — 0 real findings
Dispositions: **1 LOW** (by-design reward-pool live-read) · **2 BY-DESIGN** (USER-ruled: salvage address-select, deity-boon) · **1 INFO** (gameover prevrandao fallback emergency 1-bit bias). Canonical record: `audit/FINDINGS-v66.0.md` (chmod 444).

## The thesis (why this milestone)
RNG had been audited 10+ times "clean." The risk was **confirmation convergence** — every prior pass re-derived its VRF-consumer net from the same trusted catalog, which was a **stale, pre-rename snapshot**. A six-lens blind-spot panel (`.planning/v66-BLIND-SPOT-PANEL.md`) confirmed the thesis, so v66 re-derived the net FROM HEAD.

## Headline outcome
- **VRF-consumer net: 13 (catalog) → 72 (re-derived).** The catalog under-counted ~5×.
- **The cross-model council earned its keep:** codex independently found **5 consumers the Claude net + Gemini both missed** (decimator jackpot ×2, daily/level quest rolls, deity-boon viewer).
- **9 stale doc-anchors superseded** (the §12 "headline exploit" already remediated; the v30 clear-site C-02 removed; `currentDayView()` cross-calls gone; line anchors off-by-N). `.planning/v66-RNGNET-CONSUMER-NET.md` supersedes the stale `RNGLOCK-CATALOG.md`.
- **+10 hardening tests** (Phase 414) closed the suite's blind spots (the only real gaps were in the test net, not on-chain). New forge baseline ~899/0/109.

## Method
Cross-model council (Gemini + Codex) as PRIMARY finder + Claude multi-agent workflows (4-cluster net re-derivation, per-seam adversarial freeze-proofs, 3-lens refutation). Every candidate adversarially verified before recording — none survived as a finding. Codex's terminal 412-proof challenge was rate-capped (non-blocking; the proofs + in-phase council coverage stand).

## Phases
| # | Phase | Outcome |
|---|-------|---------|
| 410 | FOUNDATION — Subject Freeze & Green Baseline | frozen `42c8e9c6`/tree `0dd445a6`; baseline forge 889/0/110 + hardhat 1232/136-carried/14 |
| 411 | RNGNET — Re-Derive the Net From HEAD | 13→72 net; council found 5 missed; 9 stale anchors; `v66-RNGNET-CONSUMER-NET.md` |
| 412 | RNGSEAM — Cross-Contract Freeze Seams | 8 seams freeze-hold; 1 LOW by-design; council divergences resolved |
| 413 | RNGSEL + RNGFALL — Input-Selection + Fallback | Degenerette index-key + first-mover hold; salvage + deity-boon by-design; fallback INFO |
| 414 | RNGNET-MECH — Test-Net Closure (test-only) | +10 tests (day+1 mutant catcher, un-skipped binding test, Coinflip behavioral net, b≥50 floor) |
| 415 | TERMINAL — Council Synthesis + FINDINGS + Closure | `audit/FINDINGS-v66.0.md` (444); closure signal recorded |

## One pre-freeze contract change
The USER-approved additive `CurseChanged(address indexed player, uint8 newCurseCount)` emit (`_applyCurseStack`/`_clearCurse` in `MintStreakUtils`, `42c8e9c6`) — makes the cashout/smite curse counter fully event-derivable for the indexer. Proven inert (forge 889/0/110 identical, packed-bytes byte-identical, EIP-170 OK); folded in before the freeze so the audit covers shipping bytecode.

## Carried (non-blocking)
- Full gambit mutation campaign on the Coinflip + RNG spine — CI-resumable (per v63/v64).
- Codex 412-proof challenge — re-runnable when its quota resets.
- Phase dirs `410–415` kept in `.planning/phases/` (archive via `/gsd-cleanup` later).
