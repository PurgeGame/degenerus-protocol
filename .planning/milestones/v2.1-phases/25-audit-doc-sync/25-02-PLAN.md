---
phase: 25-audit-doc-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/state-changing-function-audits.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "All 8 new DegenerusAdmin governance functions have audit entries following the existing format"
    - "unwrapTo function entry exists in DegenerusStonk section"
    - "emergencyRecover entry is marked as REMOVED in v2.1 with annotation"
    - "updateVrfCoordinatorAndSub entry updated to reflect _threeDayRngGap removal and governance caller"
    - "rngGate entry updated from 18h to 12h VRF retry timeout"
    - "_handleGameOverPath entry updated to reflect anyProposalActive() death clock pause"
  artifacts:
    - path: "audit/state-changing-function-audits.md"
      provides: "Complete state-changing function audit with governance entries"
      contains: "propose"
  key_links:
    - from: "audit/state-changing-function-audits.md"
      to: "contracts/DegenerusAdmin.sol"
      via: "Function signatures match contract source"
      pattern: "propose|vote|_executeSwap|_voidAllActive"
    - from: "audit/state-changing-function-audits.md"
      to: "audit/v2.1-governance-verdicts.md"
      via: "Verdicts sourced from Phase 24 audit"
      pattern: "Phase 24|governance audit"
---

<objective>
Update state-changing-function-audits.md (Tier 2 audit document, 13,598 lines) to add ~9 new governance function entries, update ~4 existing entries with v2.1 changes, and mark emergencyRecover as removed.

Purpose: This document is the function-level reference for external auditors. Every governance function must have a complete audit entry, and all stale references must be corrected.
Output: Fully updated state-changing-function-audits.md with governance coverage.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-audit-doc-sync/25-RESEARCH.md
@audit/v2.1-governance-verdicts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new governance function entries and update existing entries in state-changing-function-audits.md</name>
  <files>audit/state-changing-function-audits.md</files>
  <read_first>
    - audit/state-changing-function-audits.md (current state -- 13,598 lines, read the DegenerusAdmin section starting around line 12700 and the AdvanceModule rngGate/updateVrfCoordinatorAndSub sections around lines 2300-2400, and the _handleGameOverPath section)
    - audit/v2.1-governance-verdicts.md (source of truth for all governance function behavior, verdicts)
    - contracts/DegenerusAdmin.sol (governance function signatures, parameters, access control)
    - contracts/modules/DegenerusGameAdvanceModule.sol (rngGate changes, _handleGameOverPath changes, updateVrfCoordinatorAndSub changes)
    - contracts/DegenerusStonk.sol (unwrapTo function signature and implementation)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (stale reference inventory with exact line numbers, function list)
  </read_first>
  <action>
Make the following specific changes to audit/state-changing-function-audits.md:

**PART A: Add 8 new DegenerusAdmin function entries**

Add these entries in the DegenerusAdmin.sol section, after the `shutdownVrf()` entry and before any closing section. Each entry must follow the EXACT format used by existing entries (see `advanceGame()` or `emergencyRecover()` for the template structure with table, State Reads, State Writes, Callers, Callees, ETH Flow, Invariants, NatSpec Accuracy, Gas Flags, Verdict).

Read the actual contract source for each function to fill in accurate signatures, parameters, state reads/writes, callers, callees, and invariants. Cross-reference with v2.1-governance-verdicts.md for verdicts.

**Entry 1: `propose(address newCoordinator, bytes32 newKeyHash)` [external]**
- Visibility: external, state-changing
- Parameters: newCoordinator (address), newKeyHash (bytes32)
- Returns: uint256 proposalId
- State Reads: vrfCoordinator (stall check), block.timestamp, lastVrfProcessedTimestamp (via GameStorage), activeProposalCount, msg.sender, vault.isVaultOwner(msg.sender) (admin path), sdgnrs.balanceOf(msg.sender) and circulatingSupply() (community path)
- State Writes: proposals[proposalId] (new Proposal struct), activeProposalCount++, circulatingSnapshot
- Callers: Admin path (>50.1% DGVE holder after 20h VRF stall), Community path (>= 0.5% sDGNRS after 7d VRF stall)
- Invariants: VRF must be stalled beyond threshold, creates new Active proposal, records circulatingSnapshot
- Verdict: CORRECT (verified in Phase 24, GOV-02)

**Entry 2: `vote(uint256 proposalId, bool approve)` [external]**
- Visibility: external, state-changing
- Parameters: proposalId (uint256), approve (bool)
- State Reads: proposals[proposalId], sdgnrs.balanceOf(msg.sender), block.timestamp
- State Writes: proposals[proposalId].votes[msg.sender], proposals[proposalId].approveWeight/rejectWeight (subtract old + add new), proposals[proposalId].state (if execute/kill triggered)
- Callers: Any sDGNRS holder
- Invariants: Changeable votes via subtract-before-add. Checks execute condition (approveWeight > rejectWeight AND approveWeight * BPS >= threshold * circulatingSnapshot) and kill condition (symmetric). Lazy expiry check at entry.
- Verdict: CORRECT (verified in Phase 24, GOV-03)

**Entry 3: `_executeSwap(uint256 proposalId)` [internal]**
- Visibility: internal, state-changing
- State Reads: proposals[proposalId] (coordinator, keyHash)
- State Writes: proposals[proposalId].state = Executed, calls _voidAllActive
- Callees: gameAdmin.updateVrfCoordinatorAndSub(coordinator, newSubId, keyHash), _voidAllActive(proposalId)
- Invariants: CEI violation noted (GOV-07) -- external call before _voidAllActive. Requires pre-existing governance control to exploit.
- Verdict: CORRECT with KNOWN-ISSUE (GOV-07, Low)

**Entry 4: `_voidAllActive(uint256 exceptId)` [internal]**
- Visibility: internal, state-changing
- State Reads: proposalCount, proposals[i].state for i in 1..proposalCount
- State Writes: proposals[i].state = Killed for all Active proposals except exceptId, activeProposalCount = 0
- Invariants: 1-indexed loop with <= condition. Hard-sets activeProposalCount to 0 (robust, idempotent).
- Verdict: CORRECT (verified in Phase 24, GOV-08)

**Entry 5: `anyProposalActive()` [external view]**
- Visibility: external, view (NOT state-changing, but include for completeness since it's critical for cross-contract interaction)
- Returns: bool (activeProposalCount > 0)
- State Reads: activeProposalCount
- Callers: DegenerusGameAdvanceModule._handleGameOverPath() (death clock pause)
- Verdict: CORRECT (verified in Phase 24, XCON-02)

**Entry 6: `circulatingSupply()` [public view]**
- Visibility: public, view
- Returns: uint256
- State Reads: sdgnrs.totalSupply(), sdgnrs.balanceOf(address(sdgnrs)), dgnrs.balanceOf(address(sdgnrs)) -- wait, verify from contract source. Actually reads sdgnrs.totalSupply() and excludes specific pool addresses.
- Callers: propose() (for circulatingSnapshot), threshold comparison in vote()
- Invariants: Correctly excludes undistributed pools (sDGNRS held by SDGNRS contract) and DGNRS wrapper balance. Underflow impossible.
- Verdict: CORRECT (verified in Phase 24, GOV-10)

**Entry 7: `threshold(uint256 proposalId)` [public view]**
- Visibility: public, view
- Returns: uint256 (BPS value)
- State Reads: proposals[proposalId].createdAt, block.timestamp
- Invariants: 8-step discrete decay: 6000/5000/4000/3000/2000/1000/500/0 BPS at 24h intervals
- Verdict: CORRECT (verified in Phase 24, GOV-04)

**Entry 8: `canExecute(uint256 proposalId)` [external view]**
- Visibility: external, view
- Returns: bool
- State Reads: proposals[proposalId] (state, approveWeight, rejectWeight, circulatingSnapshot), threshold(proposalId)
- Invariants: approveWeight > rejectWeight AND approveWeight * BPS >= threshold * circulatingSnapshot
- Verdict: CORRECT (verified in Phase 24, GOV-05)

**IMPORTANT:** For each entry, read the actual contract source (`contracts/DegenerusAdmin.sol`) to get precise function signatures, parameter types, state reads, state writes, and callee functions. Do NOT guess -- use the source. The v2.1-governance-verdicts.md provides the audit verdict and evidence but the function signature details must come from the contract source.

**PART B: Add 1 new DegenerusStonk function entry**

Add in the DegenerusStonk.sol section (or create section if none exists):

**Entry: `unwrapTo(address to, uint256 amount)` [external]**
- Read contracts/DegenerusStonk.sol for exact implementation
- Visibility: external, state-changing
- Parameters: to (address), amount (uint256)
- State Reads: msg.sender == creator check, block.timestamp, lastVrfProcessedTimestamp (via game storage)
- State Writes: burns DGNRS from sender, transfers underlying sDGNRS to `to`
- Callers: Creator only (CREATOR address)
- Invariants: VRF stall guard -- reverts if block.timestamp - lastVrfProcessedTimestamp > 20 hours (ADMIN_STALL_THRESHOLD). This prevents creator from unwrapping during active governance.
- Verdict: CORRECT (verified in Phase 24, XCON-03)

**PART C: Mark emergencyRecover as REMOVED**

At line 12998, add a v2.1 annotation header BEFORE the existing entry:

```markdown
> **v2.1 REMOVED:** `emergencyRecover` was removed in v2.1 and replaced by governance
> (propose/vote/execute). The entry below is preserved for historical audit traceability.
> See the `propose()`, `vote()`, `_executeSwap()` entries above for current behavior.
```

Do NOT delete the existing entry content -- it provides historical evidence.

**PART D: Update updateVrfCoordinatorAndSub entry (around line 2333)**

Update the following fields:
1. State Reads (line ~2346): Change `_threeDayRngGap` references to note: `_threeDayRngGap` guard was removed in v2.1. Function is now called by governance `_executeSwap`, not by `emergencyRecover`. The `lastVrfProcessedTimestamp` check in DegenerusAdmin.propose() replaces the old 3-day gap check.
2. Callees (line ~2363): Add note that `_threeDayRngGap` is removed from this function in v2.1
3. NatSpec Accuracy (line ~2377): Update the "SECURITY: Requires 3-day gap to prevent abuse" note to reflect v2.1 changes -- `_threeDayRngGap` removed, governance stall thresholds replace it
4. Gas Flags (line ~2382): Update `_threeDayRngGap` reference
5. Invariants (line ~2369-2372): Update "Requires 3-day RNG gap" and "emergency recovery mechanism" references to reflect governance caller

Add a v2.1 annotation at the top of this entry:
```markdown
> **v2.1 Update:** `_threeDayRngGap` guard removed. This function is now called by DegenerusAdmin._executeSwap()
> during governance-approved VRF coordinator rotation (was called by emergencyRecover in v1.0-v2.0).
> Access control unchanged (ContractAddresses.ADMIN only). State resets unchanged.
```

**PART E: Update rngGate entry (around line 2310-2330)**

Update line ~2317: Change `If VRF pending and 18+ hours elapsed: retries request` to `If VRF pending and 12+ hours elapsed: retries request`
Update line ~2318: Change `If VRF pending and < 18 hours: reverts` to `If VRF pending and < 12 hours: reverts`

Add a v2.1 annotation:
```markdown
> **v2.1 Update:** VRF retry timeout changed from 18 hours to 12 hours (XCON-05). Two retries possible before 20h admin governance threshold.
```

**PART F: Update _handleGameOverPath entry**

Find the `_handleGameOverPath` entry in AdvanceModule section. Add:
1. A new State Read: `admin.anyProposalActive()` (via try/catch)
2. A new Callee: `admin.anyProposalActive()` -- pauses death clock if any governance proposal is active
3. A v2.1 annotation:
```markdown
> **v2.1 Update:** Now calls `admin.anyProposalActive()` to pause death clock during active governance proposals (XCON-02).
> Uses try/catch -- if Admin contract reverts, death clock proceeds normally (defensive behavior).
```

**PART G: Update wireVrf NatSpec note**

Find the wireVrf entry. If there's a reference to `emergencyRecover reuses this path`, update it to: `wireVrf is deployment-only; governance coordinator rotation uses updateVrfCoordinatorAndSub (not wireVrf)`
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'propose\|vote\|_executeSwap\|_voidAllActive\|anyProposalActive\|circulatingSupply\|canExecute' audit/state-changing-function-audits.md | xargs test 7 -le && echo "PASS: new function entries present" && grep -c 'v2.1 REMOVED' audit/state-changing-function-audits.md | xargs test 0 -lt && echo "PASS: emergencyRecover marked removed" && grep -c 'unwrapTo' audit/state-changing-function-audits.md | xargs test 0 -lt && echo "PASS: unwrapTo entry present" && grep -c '12.*hours\|12h' audit/state-changing-function-audits.md | xargs test 0 -lt && echo "PASS: 12h timeout present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### .propose' audit/state-changing-function-audits.md` returns >= 1 (propose entry exists)
    - `grep -c '### .vote' audit/state-changing-function-audits.md` returns >= 1 (vote entry exists)
    - `grep -c '_executeSwap' audit/state-changing-function-audits.md` returns >= 2 (entry + references)
    - `grep -c '_voidAllActive' audit/state-changing-function-audits.md` returns >= 2 (entry + references)
    - `grep -c 'anyProposalActive' audit/state-changing-function-audits.md` returns >= 2 (entry + _handleGameOverPath reference)
    - `grep -c 'canExecute' audit/state-changing-function-audits.md` returns >= 1
    - `grep -c 'unwrapTo' audit/state-changing-function-audits.md` returns >= 1
    - `grep 'v2.1 REMOVED' audit/state-changing-function-audits.md` matches at emergencyRecover entry
    - `grep 'v2.1 Update' audit/state-changing-function-audits.md` matches at updateVrfCoordinatorAndSub and rngGate and _handleGameOverPath
    - `grep '12.*hours elapsed\|12+ hours\|12 hours' audit/state-changing-function-audits.md` replaces old 18h in rngGate invariants
    - `grep -c '18.*hours elapsed' audit/state-changing-function-audits.md` returns 0 (no stale 18h VRF retry reference)
  </acceptance_criteria>
  <done>state-changing-function-audits.md has 8 new DegenerusAdmin governance function entries (propose, vote, _executeSwap, _voidAllActive, anyProposalActive, circulatingSupply, threshold, canExecute), 1 new DegenerusStonk entry (unwrapTo), emergencyRecover marked as REMOVED with historical preservation, updateVrfCoordinatorAndSub updated for _threeDayRngGap removal and governance caller, rngGate updated from 18h to 12h, _handleGameOverPath updated with anyProposalActive death clock pause, wireVrf NatSpec reference updated.</done>
</task>

</tasks>

<verification>
- `grep -c 'propose\|_executeSwap\|_voidAllActive\|canExecute' audit/state-changing-function-audits.md` shows multiple new entries
- `grep 'v2.1 REMOVED' audit/state-changing-function-audits.md` confirms emergencyRecover annotated
- `grep -c '18.*hours elapsed' audit/state-changing-function-audits.md` returns 0
- `grep 'unwrapTo' audit/state-changing-function-audits.md` confirms new entry
</verification>

<success_criteria>
state-changing-function-audits.md has complete coverage of all v2.1 governance functions with entries matching the existing format, all stale references updated, and emergencyRecover preserved as historical evidence with REMOVED annotation.
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-doc-sync/25-02-SUMMARY.md`
</output>
