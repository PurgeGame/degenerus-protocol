---
phase: 25-audit-doc-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/FINAL-FINDINGS-REPORT.md
  - audit/KNOWN-ISSUES.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "M-02 finding reflects governance mitigation with severity downgraded from Medium to Low"
    - "Five new governance known issues (GOV-07, VOTE-03, WAR-01, WAR-02, WAR-06) are documented in both FINAL-FINDINGS-REPORT.md and KNOWN-ISSUES.md"
    - "I-09 rationale updated to reflect governance replaces emergencyRecover"
    - "I-22 marked as RESOLVED (duplication no longer exists)"
    - "Severity distribution shows Medium:2, Low:4, Informational:8+"
    - "Plan/phase counts reflect v2.1 totals (15 phases, 87 plans)"
    - "Zero stale emergencyRecover/EmergencyRecovered references remain in either file"
  artifacts:
    - path: "audit/FINAL-FINDINGS-REPORT.md"
      provides: "Updated findings report with governance changes"
      contains: "Downgraded to Low"
    - path: "audit/KNOWN-ISSUES.md"
      provides: "Updated known issues with governance findings"
      contains: "propose/vote/execute"
  key_links:
    - from: "audit/FINAL-FINDINGS-REPORT.md"
      to: "audit/v2.1-governance-verdicts.md"
      via: "M-02 severity rationale references Phase 24 verdicts"
      pattern: "Phase 24|governance"
    - from: "audit/KNOWN-ISSUES.md"
      to: "audit/v2.1-governance-verdicts.md"
      via: "Known issues sourced from Phase 24 verdicts"
      pattern: "GOV-07|VOTE-03|WAR-01|WAR-02|WAR-06"
---

<objective>
Update FINAL-FINDINGS-REPORT.md and KNOWN-ISSUES.md (Tier 1 audit documents) to reflect v2.1 governance changes from Phase 24: rewrite M-02 for governance mitigation with severity downgrade, add 5 new governance known issues, update severity distribution, update plan/phase counts, update I-09 and I-22, and remove all stale emergencyRecover references.

Purpose: C4A wardens read these two documents first. They must accurately reflect the current codebase state -- governance replaces emergencyRecover, and all Phase 24 findings are documented.
Output: Two fully updated Tier 1 audit documents with zero stale references.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-audit-doc-sync/25-RESEARCH.md
@audit/v2.1-governance-verdicts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FINAL-FINDINGS-REPORT.md -- M-02 rewrite, new findings, counts, severity distribution</name>
  <files>audit/FINAL-FINDINGS-REPORT.md</files>
  <read_first>
    - audit/FINAL-FINDINGS-REPORT.md (current state -- 451 lines)
    - audit/v2.1-governance-verdicts.md (source of truth for Phase 24 findings)
    - .planning/STATE.md (all Phase 24 decisions with verdicts)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (stale reference inventory, severity distribution, pitfalls)
  </read_first>
  <action>
Make the following specific changes to audit/FINAL-FINDINGS-REPORT.md:

**1. Update methodology line (line 7):**
Change `13-phase manual code review` to `15-phase manual code review` (add Phases 24-25).

**2. Update executive summary paragraph (line 13):**
- Change `72 plans examining approximately 16,500 lines of Solidity code` to `87 plans examining approximately 16,500 lines of Solidity code`
- After the Phase 23 description, append: `Phase 24 conducted a comprehensive governance security audit of the new VRF coordinator rotation mechanism (propose/vote/execute) across 8 plans, covering storage layout, access control, vote arithmetic, reentrancy, cross-contract interactions, and adversarial war-game scenarios. Phase 25 synchronized all audit documentation with governance changes across 4 plans.`

**3. Update severity distribution (lines 17-22):**
Replace the entire severity distribution block with:
```
- **Critical:** 0
- **High:** 0
- **Medium:** 2 -- WAR-01 (compromised admin key + community absence), WAR-02 (colluding voter cartel at low threshold)
- **Low:** 4 -- M-02 (downgraded from Medium, governance mitigation), GOV-07 (_executeSwap CEI violation), VOTE-03 (uint8 activeProposalCount overflow), WAR-06 (admin spam-propose gas griefing)
- **Informational:** 12+ -- 6 from v1.0-v1.2, 2 from v2.0 delta audit, 4+ from v2.1 governance audit (XCON-03 boundary window, WAR-03 VRF oscillation, WAR-04 unwrapTo timing, WAR-05 post-execute loop)
```

**4. Update "Areas Requiring Attention" section (line 34):**
Replace the current text with:
```
- VRF coordinator rotation is now governed by community propose/vote/execute (replacing the original single-admin `emergencyRecover`). Two residual medium-severity scenarios remain: a compromised admin key combined with 7-day community absence (WAR-01), and a 5% sDGNRS cartel at day-6 threshold decay (WAR-02). Both require specific preconditions and have structural defenses (soulbound sDGNRS, single reject voter blocks). See M-02 (downgraded), WAR-01, and WAR-02 for full analysis.
```

**5. Rewrite M-02 finding (lines 66-97):**
Replace the entire M-02 section with:

```markdown
### M-02: VRF Coordinator Swap Security (Downgraded to Low)

**Original Severity:** MEDIUM (v1.0-v2.0)
**Revised Severity:** LOW (v2.1, governance mitigation)
**Affected Contract:** DegenerusGame / DegenerusAdmin
**Requirement:** FSM-02 (stuck state recovery)

**Description:**
When Chainlink VRF stalls, the admin (any address holding >50.1% DGVE) can propose a VRF coordinator rotation via the governance mechanism (propose/vote/execute). This replaces the original `emergencyRecover` single-admin call, adding community oversight via sDGNRS-weighted voting.

**v2.1 Governance Flow:**
1. After 20 hours of VRF stall, admin can call `propose(newCoordinator, newKeyHash)`
2. After 7 days of VRF stall, any address holding >= 0.5% of circulating sDGNRS can propose
3. sDGNRS holders vote with time-decaying threshold (60% at 0h -> 5% at 144h -> expired at 168h)
4. Execution requires approveWeight > rejectWeight AND approveWeight * BPS >= threshold * circulatingSnapshot
5. A single reject voter can block any proposal that does not meet the threshold

**Original scenario (v1.0-v2.0):** Admin could unilaterally call `emergencyRecover` after 3 days of VRF stall, with no community input required.

**Why downgraded to Low:**
1. Three prerequisites required (was two): VRF stall + admin key compromised + community absent for 7 days
2. 7-day defense window for community response (was immediate after 3-day stall)
3. Soulbound sDGNRS prevents vote weight acquisition via market purchase
4. A single reject voter with sufficient sDGNRS blocks the malicious proposal

**Residual risk:** See WAR-01 (compromised admin + community absence, Medium) and WAR-02 (colluding cartel, Medium) for governance-specific attack scenarios that replace M-02's original threat model.

**Status:** Mitigated by v2.1 governance. Downgraded from Medium to Low. The original single-admin attack vector is eliminated; residual risk is governance-level (WAR-01, WAR-02).

**GAMEOVER RNG fallback (dead VRF, no admin):** When GAMEOVER triggers with a dead VRF coordinator and no admin to propose a rotation, the game needs entropy to finalize the drain. After a 3-day wait, `_getHistoricalRngFallback` combines up to 5 early historical VRF words (committed on-chain, non-manipulable) with `currentDay` and `block.prevrandao`. This provides unpredictability to non-validators at the cost of 1-bit validator influence (propose or skip their slot). This is the strongest fallback available without an external oracle.
```

**6. Add new findings sections after the M-02 section:**

Add a new `## Medium Findings (v2.1 Governance)` section with WAR-01 and WAR-02:

```markdown
### WAR-01: Compromised Admin Key + Community Absence

**Severity:** MEDIUM
**Affected Contract:** DegenerusAdmin
**Source:** Phase 24-07 (war-game scenario analysis)

**Description:**
A compromised admin key holder can propose a malicious VRF coordinator. If the sDGNRS community does not vote to reject within 7 days, the proposal executes via threshold decay (5% at day 6, expired at day 7). The DGVE/sDGNRS separation is the primary defense -- the admin holds DGVE (governance token for admin actions) but cannot acquire sDGNRS voting weight via market purchase (soulbound).

**Preconditions:** VRF stalled 20+ hours + admin key compromised + community absent for full proposal lifetime (up to 168 hours).

**Mitigation:** Soulbound sDGNRS, threshold decay requires waiting, single reject voter blocks.

**Status:** Known issue -- documented as residual governance risk.

### WAR-02: Colluding Voter Cartel at Low Threshold

**Severity:** MEDIUM
**Affected Contract:** DegenerusAdmin
**Source:** Phase 24-07 (war-game scenario analysis)

**Description:**
At day 6 of a proposal's lifetime, the threshold decays to 5% (500 BPS). A cartel holding >= 5% of circulating sDGNRS could approve a malicious coordinator swap if no opposing voter appears. Feasibility depends on sDGNRS concentration -- with few holders, collusion is easier.

**Preconditions:** VRF stalled 20+ hours + proposal alive for 144+ hours + cartel controls >= 5% circulating sDGNRS + no reject voter.

**Mitigation:** Single reject voter with sufficient weight blocks. Soulbound sDGNRS prevents rapid accumulation.

**Status:** Known issue -- documented as residual governance risk.
```

Add a `## Low Findings (v2.1 Governance)` section with GOV-07, VOTE-03, WAR-06:

```markdown
### GOV-07: _executeSwap CEI Violation

**Severity:** LOW
**Affected Contract:** DegenerusAdmin
**Source:** Phase 24-04

**Description:**
`_executeSwap` has a theoretical CEI violation -- the external call to `gameAdmin.updateVrfCoordinatorAndSub()` occurs before `_voidAllActive()` completes state cleanup. A malicious VRF coordinator could theoretically trigger reentrancy to interact with sibling proposals still in Active state. However, exploiting this requires pre-existing governance control (the attacker already controls the coordinator being swapped to).

**Recommended fix:** Move `_voidAllActive` before external calls.

**Status:** Known issue -- requires pre-existing governance control to exploit.

### VOTE-03: uint8 activeProposalCount Overflow

**Severity:** LOW
**Affected Contract:** DegenerusAdmin
**Source:** Phase 24-05

**Description:**
`activeProposalCount` is uint8, incremented with `unchecked`. At 256 proposals, it wraps to 0, causing `anyProposalActive()` to return false. This unpauses the death clock in `_handleGameOverPath()`. Cost to exploit is ~$3,000 (256 proposals at ~$12 gas each).

**Recommended fix:** `require(activeProposalCount < 255)` before increment.

**Status:** Known issue -- low likelihood due to cost and VRF stall requirement.

### WAR-06: Admin Spam-Propose Gas Griefing

**Severity:** LOW
**Affected Contract:** DegenerusAdmin
**Source:** Phase 24-07

**Description:**
No per-proposer cooldown exists. An admin can create many proposals, bloating the `_voidAllActive` loop gas cost when any proposal executes. The gas cost scales linearly with active proposal count.

**Recommended fix:** Per-proposer cooldown or max active proposals.

**Status:** Known issue -- admin self-griefs by increasing own execution cost.
```

**7. Update I-09 in informational findings (line 115):**
Change: `I-09 | DegenerusAdmin | wireVrf() lacks explicit re-initialization guard -- intentional, emergencyRecover reuses this path`
To: `I-09 | DegenerusAdmin | wireVrf() lacks explicit re-initialization guard -- deployment-only function; governance coordinator rotation uses updateVrfCoordinatorAndSub (not wireVrf)`

**8. Update I-22 in informational findings (line 124):**
Change: `I-22 | _threeDayRngGap() duplicated in DegenerusGame + AdvanceModule -- identical logic, immutable post-deploy`
To: `I-22 | ~~_threeDayRngGap() duplicated in DegenerusGame + AdvanceModule~~ **RESOLVED in v2.1** -- _threeDayRngGap removed from AdvanceModule; only exists in DegenerusGame.sol for rngStalledForThreeDays() monitoring view`

**9. Update RNG-06 (line 157):**
Change `VRF retry (18h timeout)` to `VRF retry (12h timeout)`.

**10. Update the 13-Phase Audit Structure table (lines 382-400):**
Add two new rows:
```
| 24 | Core Governance Security Audit | 8 | GOV-01 to GOV-10, XCON-01 to XCON-05, VOTE-01 to VOTE-03, WAR-01 to WAR-06, M02-01, M02-02 |
| 25 | Audit Doc Sync | 4 | DOCS-01 to DOCS-07 |
```
Update the Total row: `| **Total** | | **87 plans** | **90 requirements** |`

Update the `13-Phase Audit Structure` heading to `15-Phase Audit Structure`.

Also update the methodology header (line 7) reference from "13 phases" to "15 phases".

**11. Update requirement counts:**
- Line 144: Change `All 56 v1 requirements` to `All 56 v1.0 requirements` (clarify version)
- Line 211: Change `56/56 PASS` to `56/56 PASS`  (no change needed here)
- Add a new v2.1 requirements section after the v2.0 section:

```markdown
### v2.1 Governance Requirements

| Requirement | Description | Verdict | Notes |
|-------------|-------------|---------|-------|
| **GOV-01** | Storage layout verified -- lastVrfProcessedTimestamp at slot 114, no collisions | **PASS** | Compiler JSON verified |
| **GOV-02** | propose() access control verified -- admin (DGVE >50.1%, 20h) and community (0.5% sDGNRS, 7d) | **PASS** | Both paths correctly gated |
| **GOV-03** | vote() arithmetic verified -- changeable votes subtract-before-add, no double-counting | **PASS** | Depends on VOTE-01 (sDGNRS soulbound) |
| **GOV-04** | Threshold decay verified -- 8-step schedule matches spec | **PASS** | Boundary analysis clean |
| **GOV-05** | Execute condition verified -- overflow-safe, circulatingSnapshot==0 not exploitable | **PASS** | Max 1e31 vs uint256 1.15e77 |
| **GOV-06** | Kill condition verified -- symmetric with execute, mutual exclusion proven | **PASS** | Strict inequality contradiction |
| **GOV-07** | _executeSwap CEI -- theoretical reentrancy via malicious coordinator | **KNOWN-ISSUE (Low)** | Requires pre-existing governance control |
| **GOV-08** | _voidAllActive boundaries correct, idempotent | **PASS** | 1-indexed, <= condition, hard-set to 0 |
| **GOV-09** | Proposal expiry -- lazy expiry reverts roll back, protective behavior | **PASS (INFO)** | activeProposalCount stays inflated |
| **GOV-10** | circulatingSupply correctly excludes pools and wrapper | **PASS** | Underflow impossible |
| **XCON-01** | lastVrfProcessedTimestamp write paths exhaustive | **PASS** | Only _applyDailyRng and wireVrf |
| **XCON-02** | Death clock pause via anyProposalActive() correct | **PASS** | try/catch defensive |
| **XCON-03** | unwrapTo stall guard boundary at exactly 20h | **PASS (INFO)** | 1-second window, not exploitable |
| **XCON-04** | _threeDayRngGap removal from governance paths verified | **PASS (INFO)** | Retained in Game monitoring |
| **XCON-05** | VRF retry timeout 18h->12h, no downstream breakage | **PASS** | Two retries before 20h governance |
| **VOTE-01** | sDGNRS supply frozen during VRF stall proven | **PASS** | All mutation paths blocked |
| **VOTE-02** | circulatingSnapshot immutable post-creation | **PASS** | Single write in propose() |
| **VOTE-03** | uint8 activeProposalCount overflow at 256 | **KNOWN-ISSUE (Low)** | ~$3,000 cost |
| **WAR-01** | Compromised admin key scenario | **KNOWN-ISSUE (Medium)** | DGVE/sDGNRS separation defends |
| **WAR-02** | Colluding voter cartel at low threshold | **KNOWN-ISSUE (Medium)** | Single reject voter blocks |
| **WAR-03** | VRF oscillation attack | **PASS (Low)** | Auto-invalidation + death clock pause |
| **WAR-04** | Creator unwrapTo timing attack | **PASS (INFO)** | 1-second boundary not exploitable |
| **WAR-05** | Post-execute governance loop | **PASS (INFO)** | Intentional design |
| **WAR-06** | Admin spam-propose gas griefing | **KNOWN-ISSUE (Low)** | Per-proposer cooldown recommended |
| **M02-01** | emergencyRecover removed, governance replaces it | **PASS** | Fully verified |
| **M02-02** | M-02 severity downgraded Medium to Low | **PASS** | 3 prerequisites, 7-day defense |

**v2.1 Coverage Summary: 26/26 assessed** (3 KNOWN-ISSUE at stated severities)
```

**12. Update the final report footer (line 450):**
Change `72 individual audit plans across 13 phases` to `87 individual audit plans across 15 phases`.

**13. Update FSM-02 in requirement coverage (line 163):**
Change the Notes column from `Recovery paths exist; M-02 documents dual-failure scenario` to `Recovery paths exist; M-02 downgraded to Low in v2.1 (governance mitigation)`
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'emergencyRecover\|EmergencyRecovered' audit/FINAL-FINDINGS-REPORT.md | xargs test 0 -eq && echo "PASS: zero stale refs" && grep -c 'Downgraded to Low' audit/FINAL-FINDINGS-REPORT.md | xargs test 0 -lt && echo "PASS: M-02 downgraded" && grep -c 'WAR-01\|WAR-02\|GOV-07\|VOTE-03\|WAR-06' audit/FINAL-FINDINGS-REPORT.md | xargs test 5 -le && echo "PASS: new findings present" && grep -c '87 plans' audit/FINAL-FINDINGS-REPORT.md | xargs test 0 -lt && echo "PASS: plan count updated" && grep -c '15-Phase\|15 phases' audit/FINAL-FINDINGS-REPORT.md | xargs test 0 -lt && echo "PASS: phase count updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'emergencyRecover\|EmergencyRecovered' audit/FINAL-FINDINGS-REPORT.md` returns 0
    - `grep 'Downgraded to Low' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'WAR-01' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'WAR-02' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'GOV-07' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'VOTE-03' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'WAR-06' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep '87 plans' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep '15-Phase\|15 phases\|15-phase' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'Medium.*2\|Medium: 2' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'Low.*4\|Low: 4' audit/FINAL-FINDINGS-REPORT.md` matches at least 1 line
    - `grep 'RESOLVED' audit/FINAL-FINDINGS-REPORT.md` matches I-22
    - `grep '12h timeout\|12h' audit/FINAL-FINDINGS-REPORT.md` replaces old 18h reference for RNG-06
    - `grep 'I-09.*wireVrf.*deployment-only\|I-09.*updateVrfCoordinatorAndSub' audit/FINAL-FINDINGS-REPORT.md` matches updated rationale
  </acceptance_criteria>
  <done>FINAL-FINDINGS-REPORT.md has M-02 rewritten as governance-mitigated Low, 5 new governance findings added (WAR-01, WAR-02, GOV-07, VOTE-03, WAR-06), severity distribution updated (Medium:2, Low:4), I-09 and I-22 updated, RNG-06 corrected to 12h, plan/phase counts updated to 87/15, v2.1 requirements table added, zero stale emergencyRecover/EmergencyRecovered references.</done>
</task>

<task type="auto">
  <name>Task 2: Update KNOWN-ISSUES.md -- replace M-02, add governance known issues</name>
  <files>audit/KNOWN-ISSUES.md</files>
  <read_first>
    - audit/KNOWN-ISSUES.md (current state -- 51 lines)
    - audit/v2.1-governance-verdicts.md (source of truth for Phase 24 verdicts)
    - .planning/STATE.md (Phase 24 decisions)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (known issues table in "New Known Issues from Phase 24" section)
  </read_first>
  <action>
Make the following specific changes to audit/KNOWN-ISSUES.md:

**1. Rewrite the M-02 section (lines 9-25):**

Replace the entire `### M-02: Admin + VRF Failure (Medium, Acknowledged)` section with:

```markdown
### M-02: VRF Coordinator Swap Security (Downgraded to Low)

**Original Severity:** MEDIUM (v1.0-v2.0)
**Revised Severity:** LOW (v2.1)
**Contract:** `DegenerusAdmin`

In v2.1, the original `emergencyRecover` single-admin VRF coordinator swap was replaced by a community governance mechanism (propose/vote/execute). The admin (>50.1% DGVE holder) can propose a coordinator swap after 20 hours of VRF stall, but execution requires sDGNRS-weighted community approval with time-decaying threshold. A single reject voter can block any proposal.

**Why downgraded:**
1. Three prerequisites (was two): VRF stall + admin key compromised + community absent for 7 days
2. 7-day defense window for community response (was immediate after 3-day stall)
3. Soulbound sDGNRS prevents vote weight acquisition via market purchase
4. Single reject voter blocks malicious proposals

**Residual governance risks (see WAR-01 and WAR-02 below).**
```

**2. Add 5 new governance known issues after M-02 but before the "Intentional Design" section:**

```markdown
### WAR-01: Compromised Admin Key + Community Absence (Medium)

**Severity:** MEDIUM
**Contract:** `DegenerusAdmin`
**Source:** Phase 24-07

A compromised admin can propose a malicious VRF coordinator. If the sDGNRS community does not vote to reject within 7 days (full proposal lifetime), the proposal executes via threshold decay (5% at day 6). The DGVE/sDGNRS separation is the primary defense -- admin holds DGVE but cannot acquire sDGNRS voting weight (soulbound).

**Preconditions:** VRF stalled 20+ hours + admin key compromised + community absent for 168 hours.

### WAR-02: Colluding Voter Cartel at Low Threshold (Medium)

**Severity:** MEDIUM
**Contract:** `DegenerusAdmin`
**Source:** Phase 24-07

At day 6 of a proposal's lifetime, threshold decays to 5% (500 BPS). A cartel holding >= 5% of circulating sDGNRS can approve a malicious coordinator swap if no reject voter appears. A single reject voter with sufficient weight blocks.

**Preconditions:** VRF stalled 20+ hours + proposal alive 144+ hours + cartel >= 5% circulating sDGNRS + no reject voter.

### GOV-07: _executeSwap CEI Violation (Low)

**Severity:** LOW
**Contract:** `DegenerusAdmin`
**Source:** Phase 24-04

Theoretical reentrancy via malicious VRF coordinator during `_executeSwap` -- external call to `gameAdmin.updateVrfCoordinatorAndSub()` occurs before `_voidAllActive()` completes. Requires pre-existing governance control to exploit (attacker already controls the coordinator being swapped to). Recommended fix: move `_voidAllActive` before external calls.

### VOTE-03: uint8 activeProposalCount Overflow (Low)

**Severity:** LOW
**Contract:** `DegenerusAdmin`
**Source:** Phase 24-05

`activeProposalCount` (uint8, unchecked increment) wraps to 0 at 256 proposals, causing `anyProposalActive()` to return false and unpausing the death clock. Cost ~$3,000 (256 proposals). Recommended fix: `require(activeProposalCount < 255)`.

### WAR-06: Admin Spam-Propose Gas Griefing (Low)

**Severity:** LOW
**Contract:** `DegenerusAdmin`
**Source:** Phase 24-07

No per-proposer cooldown. Admin can create many proposals, bloating `_voidAllActive` loop gas cost. Recommended fix: per-proposer cooldown or max active proposals.
```

**3. Update external dependencies section (line 49):**
Change `3-day emergency fallback (M-02)` to `governance-based coordinator rotation (M-02, downgraded to Low)`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'emergencyRecover\|EmergencyRecovered' audit/KNOWN-ISSUES.md | xargs test 0 -eq && echo "PASS: zero stale refs" && grep -c 'propose/vote/execute' audit/KNOWN-ISSUES.md | xargs test 0 -lt && echo "PASS: governance mechanism referenced" && grep -c 'WAR-01\|WAR-02\|GOV-07\|VOTE-03\|WAR-06' audit/KNOWN-ISSUES.md | xargs test 5 -le && echo "PASS: 5 new known issues present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'emergencyRecover\|EmergencyRecovered' audit/KNOWN-ISSUES.md` returns 0
    - `grep 'propose/vote/execute' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'Downgraded to Low' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'WAR-01' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'WAR-02' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'GOV-07' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'VOTE-03' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'WAR-06' audit/KNOWN-ISSUES.md` matches at least 1 line
    - `grep 'governance-based coordinator rotation' audit/KNOWN-ISSUES.md` matches external deps section
  </acceptance_criteria>
  <done>KNOWN-ISSUES.md has M-02 rewritten for governance with Low severity, 5 new governance known issues added (WAR-01, WAR-02, GOV-07, VOTE-03, WAR-06), zero stale emergencyRecover/EmergencyRecovered references, external dependencies updated.</done>
</task>

</tasks>

<verification>
- `grep -c 'emergencyRecover\|EmergencyRecovered' audit/FINAL-FINDINGS-REPORT.md audit/KNOWN-ISSUES.md` returns 0 for both files
- `grep -c 'WAR-01' audit/FINAL-FINDINGS-REPORT.md audit/KNOWN-ISSUES.md` returns >0 for both files
- `grep '87 plans' audit/FINAL-FINDINGS-REPORT.md` matches
- `grep '15-Phase\|15 phases' audit/FINAL-FINDINGS-REPORT.md` matches
- `grep 'Downgraded to Low' audit/FINAL-FINDINGS-REPORT.md audit/KNOWN-ISSUES.md` matches in both files
</verification>

<success_criteria>
Both Tier 1 audit documents accurately reflect v2.1 governance changes: M-02 downgraded with governance rationale, 5 new findings added, severity distribution correct (Medium:2, Low:4), I-09 and I-22 updated, plan/phase counts correct, zero stale references to emergencyRecover or EmergencyRecovered.
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-doc-sync/25-01-SUMMARY.md`
</output>
