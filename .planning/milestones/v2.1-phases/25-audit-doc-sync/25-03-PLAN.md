---
phase: 25-audit-doc-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-parameter-reference.md
  - audit/v1.2-rng-data-flow.md
  - audit/v1.2-rng-functions.md
  - audit/EXTERNAL-AUDIT-PROMPT.md
  - audit/regression-check-v2.0.md
  - audit/warden-01-contract-auditor.md
  - audit/warden-cross-reference-v2.0.md
autonomous: true
requirements:
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "parameter-reference.md contains all 8 governance constants with values, human-readable descriptions, and file:line references"
    - "parameter-reference.md contains the threshold decay schedule table"
    - "v1.2-rng-data-flow.md has v2.1 annotations on all 3 _threeDayRngGap references"
    - "v1.2-rng-functions.md has _threeDayRngGap marked as REMOVED from AdvanceModule and 18h changed to 12h"
    - "EXTERNAL-AUDIT-PROMPT.md time constants updated to include 12h, 20h, 7d governance values"
    - "Tier 3 historical docs have v2.1 annotations (not rewrites) on all stale references"
  artifacts:
    - path: "audit/v1.1-parameter-reference.md"
      provides: "Governance constants reference section"
      contains: "ADMIN_STALL_THRESHOLD"
    - path: "audit/v1.2-rng-data-flow.md"
      provides: "Annotated RNG data flow with v2.1 notes"
      contains: "v2.1"
    - path: "audit/v1.2-rng-functions.md"
      provides: "Annotated RNG functions with v2.1 notes"
      contains: "v2.1"
    - path: "audit/EXTERNAL-AUDIT-PROMPT.md"
      provides: "Updated external audit prompt with v2.1 time constants"
      contains: "12 hours"
    - path: "audit/regression-check-v2.0.md"
      provides: "Historical regression check with v2.1 annotations"
      contains: "v2.1 Note"
    - path: "audit/warden-01-contract-auditor.md"
      provides: "Historical warden report with v2.1 annotation"
      contains: "v2.1 Note"
    - path: "audit/warden-cross-reference-v2.0.md"
      provides: "Historical warden cross-reference with v2.1 annotations"
      contains: "v2.1 Note"
  key_links:
    - from: "audit/v1.1-parameter-reference.md"
      to: "contracts/DegenerusAdmin.sol"
      via: "Constant values and file:line references"
      pattern: "DegenerusAdmin.sol"
---

<objective>
Update Tier 2 reference docs (parameter-reference.md, RNG docs, external audit prompt) and Tier 3 historical docs (regression check, warden reports) to reflect v2.1 governance changes. Tier 2 gets active replacements/additions; Tier 3 gets inline annotations preserving historical content.

Purpose: Supporting reference docs must be accurate for external auditors (Tier 2) and historical docs must clearly indicate what changed (Tier 3) without destroying audit traceability.
Output: 7 updated audit documents with governance constants, corrected time references, and historical annotations.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-audit-doc-sync/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add governance constants section to parameter-reference.md (DOCS-04)</name>
  <files>audit/v1.1-parameter-reference.md</files>
  <read_first>
    - audit/v1.1-parameter-reference.md (current state -- 737 lines, read first 50 lines for structure and last 50 lines for where to append)
    - contracts/DegenerusAdmin.sol (verify constant values and line numbers)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (governance constants table and threshold decay schedule)
  </read_first>
  <action>
Add a new section to audit/v1.1-parameter-reference.md. Place it before the alphabetical cross-reference index (Section 7) if one exists, or at the end of the constant sections.

**New section header:**

```markdown
## Section 6: Governance Constants (v2.1)

> Added in v2.1 -- VRF coordinator rotation governance parameters in DegenerusAdmin.sol

### VRF Governance Thresholds

| Constant | Value | Human | Purpose | File:Line | Audit Ref |
|----------|-------|-------|---------|-----------|-----------|
| ADMIN_STALL_THRESHOLD | 72000 | 20h | VRF stall duration before admin can propose coordinator rotation | DegenerusAdmin.sol:297 | v2.1-governance-verdicts.md (GOV-02) |
| COMMUNITY_STALL_THRESHOLD | 604800 | 7d | VRF stall duration before community can propose coordinator rotation | DegenerusAdmin.sol:300 | v2.1-governance-verdicts.md (GOV-02) |
| COMMUNITY_PROPOSE_BPS | 50 | 0.5% | Minimum sDGNRS share of circulatingSupply to propose (community path) | DegenerusAdmin.sol:303 | v2.1-governance-verdicts.md (GOV-02) |
| PROPOSAL_LIFETIME | 604800 | 7d (168h) | Maximum proposal lifetime before expiry | DegenerusAdmin.sol:306 | v2.1-governance-verdicts.md (GOV-09) |
| BPS | 10000 | 100% | BPS denominator for vote threshold calculations | DegenerusAdmin.sol:309 | v2.1-governance-verdicts.md (GOV-05) |
| PRICE_COIN_UNIT | 1000 ether | 1000 BURNIE | BURNIE conversion constant for pricing | DegenerusAdmin.sol:284 | v2.1-governance-verdicts.md |
| LINK_ETH_FEED_DECIMALS | 18 | 18 decimals | Expected Chainlink LINK/ETH oracle decimals | DegenerusAdmin.sol:287 | v2.1-governance-verdicts.md |
| LINK_ETH_MAX_STALE | 86400 | 24h (1 day) | Maximum oracle staleness before rejecting price | DegenerusAdmin.sol:290 | v2.1-governance-verdicts.md |

### Threshold Decay Schedule

Vote threshold decays in discrete 24h steps from proposal creation:

| Elapsed Time | Threshold (BPS) | Human | Meaning |
|-------------|----------------|-------|---------|
| 0-24h | 6000 | 60% | Supermajority required |
| 24-48h | 5000 | 50% | Simple majority |
| 48-72h | 4000 | 40% | Strong minority sufficient |
| 72-96h | 3000 | 30% | Moderate minority |
| 96-120h | 2000 | 20% | Low threshold |
| 120-144h | 1000 | 10% | Very low threshold |
| 144-168h | 500 | 5% | Minimal threshold |
| 168h+ | 0 | 0% | Expired (unreachable -- proposal expires at 168h) |

Source: DegenerusAdmin.threshold() function, verified in Phase 24 (GOV-04).

### v2.1 VRF Retry Timeout Change

| Constant | Old Value | New Value | File | Audit Ref |
|----------|-----------|-----------|------|-----------|
| VRF retry timeout in rngGate | 18 hours | 12 hours | DegenerusGameAdvanceModule.sol | v2.1-governance-verdicts.md (XCON-05) |
```

**IMPORTANT:** Verify the exact line numbers (297, 300, 303, 306, 309, 284, 287, 290) by reading contracts/DegenerusAdmin.sol. The RESEARCH.md values are from research time and may have shifted. Use the actual current line numbers.

Also update the document title or header if it says "v1.1" to note it now covers v2.1 additions, e.g., add a note: `> Updated for v2.1 governance constants (2026-03-17)`.

Also add governance constants to the alphabetical cross-reference index (Section 7) if it exists:
- ADMIN_STALL_THRESHOLD -> Section 6
- BPS (governance) -> Section 6
- COMMUNITY_PROPOSE_BPS -> Section 6
- COMMUNITY_STALL_THRESHOLD -> Section 6
- LINK_ETH_FEED_DECIMALS -> Section 6
- LINK_ETH_MAX_STALE -> Section 6
- PRICE_COIN_UNIT -> Section 6
- PROPOSAL_LIFETIME -> Section 6
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'ADMIN_STALL_THRESHOLD\|COMMUNITY_STALL_THRESHOLD\|COMMUNITY_PROPOSE_BPS\|PROPOSAL_LIFETIME' audit/v1.1-parameter-reference.md | xargs test 4 -le && echo "PASS: governance constants present" && grep -c 'Threshold Decay Schedule' audit/v1.1-parameter-reference.md | xargs test 0 -lt && echo "PASS: decay schedule present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'ADMIN_STALL_THRESHOLD' audit/v1.1-parameter-reference.md` matches at least 1 line
    - `grep 'COMMUNITY_STALL_THRESHOLD' audit/v1.1-parameter-reference.md` matches at least 1 line
    - `grep 'COMMUNITY_PROPOSE_BPS' audit/v1.1-parameter-reference.md` matches at least 1 line
    - `grep 'PROPOSAL_LIFETIME' audit/v1.1-parameter-reference.md` matches at least 1 line
    - `grep 'BPS.*10000' audit/v1.1-parameter-reference.md` matches governance BPS entry
    - `grep 'Threshold Decay Schedule' audit/v1.1-parameter-reference.md` matches heading
    - `grep '6000.*60%' audit/v1.1-parameter-reference.md` matches first decay step
    - `grep '500.*5%' audit/v1.1-parameter-reference.md` matches second-to-last decay step
    - `grep '12 hours' audit/v1.1-parameter-reference.md` matches VRF retry timeout change
    - `grep 'DegenerusAdmin.sol' audit/v1.1-parameter-reference.md` matches file references
  </acceptance_criteria>
  <done>parameter-reference.md has complete governance constants section with 8 constants (ADMIN_STALL_THRESHOLD, COMMUNITY_STALL_THRESHOLD, COMMUNITY_PROPOSE_BPS, PROPOSAL_LIFETIME, BPS, PRICE_COIN_UNIT, LINK_ETH_FEED_DECIMALS, LINK_ETH_MAX_STALE), threshold decay schedule, VRF retry timeout change note, and alphabetical index updates.</done>
</task>

<task type="auto">
  <name>Task 2: Update Tier 2b RNG docs and external audit prompt (DOCS-05)</name>
  <files>audit/v1.2-rng-data-flow.md, audit/v1.2-rng-functions.md, audit/EXTERNAL-AUDIT-PROMPT.md</files>
  <read_first>
    - audit/v1.2-rng-data-flow.md (read lines 655-730 for _threeDayRngGap references)
    - audit/v1.2-rng-functions.md (read lines 25-40 for function table, lines 120-135 for DegenerusGame table, lines 285-295 for rngGate timeout)
    - audit/EXTERNAL-AUDIT-PROMPT.md (read lines 240-258 for time constants section)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (exact stale reference locations)
  </read_first>
  <action>
**File 1: audit/v1.2-rng-data-flow.md (3 hits)**

At line ~665 (`_threeDayRngGap` in updateVrfCoordinatorAndSub flow), add annotation:
```markdown
> **v2.1 Note:** `_threeDayRngGap` guard was removed from `updateVrfCoordinatorAndSub` in v2.1.
> Governance now uses `lastVrfProcessedTimestamp` with 20h/7d thresholds via DegenerusAdmin.propose().
> See v2.1-governance-verdicts.md (XCON-04) for verification.
```

At line ~675 (`_threeDayRngGap` in Guards section), add annotation:
```markdown
> **v2.1 Note:** This guard was removed from AdvanceModule in v2.1. `_threeDayRngGap` remains only in
> DegenerusGame.sol for the `rngStalledForThreeDays()` monitoring view function.
```

At line ~718 (`_threeDayRngGap` in Entry Point Matrix), add annotation:
```markdown
> **v2.1 Note:** `_threeDayRngGap` removed from AdvanceModule entry point matrix.
> Governance stall detection now uses `lastVrfProcessedTimestamp` in DegenerusAdmin.
```

**File 2: audit/v1.2-rng-functions.md (2+1 hits)**

At line ~33 (`_threeDayRngGap` in AdvanceModule function table), mark the entry:
```markdown
> **v2.1 REMOVED from AdvanceModule:** `_threeDayRngGap` was completely removed from
> DegenerusGameAdvanceModule in v2.1 (XCON-04). Zero grep matches in current source.
```

At line ~128 (`_threeDayRngGap` in DegenerusGame function table): Do NOT annotate -- this reference is STILL VALID. The function exists in DegenerusGame.sol for `rngStalledForThreeDays()` monitoring.

At line ~291 (`18h` rngGate timeout), change `revert if <18h or retry if >=18h` to `revert if <12h or retry if >=12h`. Add annotation:
```markdown
> **v2.1 Update:** VRF retry timeout changed from 18 hours to 12 hours (XCON-05).
> Two retries are now possible before the 20h admin governance threshold.
```

**File 3: audit/EXTERNAL-AUDIT-PROMPT.md (1 hit)**

At line ~246, update the time constants list. The current text likely reads something like `Time constants: 912 days, 365 days, 18 hours, 3 days, 30 days`. Update to:
```
Time constants: 912 days, 365 days, 12 hours (VRF retry, was 18h pre-v2.1), 3 days, 30 days, 20 hours (admin governance threshold), 7 days (community governance threshold), 168 hours (proposal lifetime)
```

Read the actual line first to ensure the edit is contextually correct.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'v2.1 Note\|v2.1 Update\|v2.1 REMOVED' audit/v1.2-rng-data-flow.md | xargs test 3 -le && echo "PASS: rng-data-flow annotations" && grep -c 'v2.1.*REMOVED\|v2.1 Update' audit/v1.2-rng-functions.md | xargs test 1 -le && echo "PASS: rng-functions annotations" && grep -c '12 hours\|12h' audit/EXTERNAL-AUDIT-PROMPT.md | xargs test 0 -lt && echo "PASS: external prompt updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v2.1 Note' audit/v1.2-rng-data-flow.md` returns >= 3 (three annotations)
    - `grep 'v2.1 REMOVED' audit/v1.2-rng-functions.md` matches at AdvanceModule entry
    - `grep '12h\|12 hours' audit/v1.2-rng-functions.md` matches updated rngGate timeout
    - `grep -v '18h.*retry\|revert.*18h' audit/v1.2-rng-functions.md` confirms no stale 18h VRF retry reference (line ~291 updated)
    - `grep '12 hours' audit/EXTERNAL-AUDIT-PROMPT.md` matches updated time constant
    - `grep '20 hours\|governance threshold' audit/EXTERNAL-AUDIT-PROMPT.md` matches new governance constants
  </acceptance_criteria>
  <done>v1.2-rng-data-flow.md has 3 v2.1 annotations on _threeDayRngGap references. v1.2-rng-functions.md has _threeDayRngGap REMOVED from AdvanceModule table (Game table preserved) and 18h->12h update for rngGate. EXTERNAL-AUDIT-PROMPT.md time constants updated with 12h VRF retry and governance thresholds.</done>
</task>

<task type="auto">
  <name>Task 3: Add v2.1 annotations to Tier 3 historical docs (DOCS-06)</name>
  <files>audit/regression-check-v2.0.md, audit/warden-01-contract-auditor.md, audit/warden-cross-reference-v2.0.md</files>
  <read_first>
    - audit/regression-check-v2.0.md (read full file -- 836 lines, focus on emergencyRecover/_threeDayRngGap references)
    - audit/warden-01-contract-auditor.md (read around line 192 for QA-03 finding)
    - audit/warden-cross-reference-v2.0.md (read full file -- 192 lines, find emergencyRecover references)
    - .planning/phases/25-audit-doc-sync/25-RESEARCH.md (stale reference inventory for Tier 3 docs)
  </read_first>
  <action>
**CRITICAL RULE for all Tier 3 docs:** These are HISTORICAL audit artifacts. Do NOT rewrite content. Do NOT delete stale references. ONLY add inline blockquote annotations using this format:

```markdown
> **v2.1 Note:** `emergencyRecover` was removed in v2.1 and replaced by governance
> (propose/vote/execute). This historical reference is preserved for audit traceability.
> See v2.1-governance-verdicts.md for current behavior.
```

**File 1: audit/regression-check-v2.0.md (13 hits)**

Read the file and identify all 13 references to `emergencyRecover`, `EmergencyRecovered`, `_threeDayRngGap`, and `18 hours`. For each cluster of nearby references (e.g., multiple references in the same section), add ONE annotation covering the cluster rather than 13 separate annotations.

For `emergencyRecover` references: Add annotation noting it was removed in v2.1, replaced by governance propose/vote/execute.

For `_threeDayRngGap` references: Add annotation noting it was removed from AdvanceModule in v2.1. Still exists in DegenerusGame.sol for `rngStalledForThreeDays()` monitoring view.

For any `18 hours` references related to VRF retry: Add annotation noting timeout changed to 12 hours in v2.1.

Group annotations by section -- one annotation per section with stale refs, not one per line.

**File 2: audit/warden-01-contract-auditor.md (1 hit)**

At line ~192 (QA-03 finding about emergencyRecover try/catch), add annotation:
```markdown
> **v2.1 Note:** `emergencyRecover` was removed in v2.1. The try/catch pattern noted in QA-03
> is no longer present. VRF coordinator rotation now uses governance (propose/vote/execute).
> See v2.1-governance-verdicts.md for current behavior.
```

**File 3: audit/warden-cross-reference-v2.0.md (3 hits)**

Read the file and identify the 3 stale references. Add annotations following the same pattern -- one per section cluster.

For each annotation, use the blockquote format with `> **v2.1 Note:**` prefix.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c 'v2.1 Note\|v2.1 note' audit/regression-check-v2.0.md | xargs test 0 -lt && echo "PASS: regression-check annotations" && grep -c 'v2.1 Note\|v2.1 note' audit/warden-01-contract-auditor.md | xargs test 0 -lt && echo "PASS: warden-01 annotation" && grep -c 'v2.1 Note\|v2.1 note' audit/warden-cross-reference-v2.0.md | xargs test 0 -lt && echo "PASS: warden-cross-ref annotations"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v2.1 Note' audit/regression-check-v2.0.md` returns >= 1 (annotations present)
    - `grep -c 'v2.1 Note' audit/warden-01-contract-auditor.md` returns >= 1 (QA-03 annotated)
    - `grep -c 'v2.1 Note' audit/warden-cross-reference-v2.0.md` returns >= 1 (annotations present)
    - All original content is preserved (no deletions of historical references)
    - `grep 'governance' audit/regression-check-v2.0.md` matches at least 1 annotation
    - `grep 'governance' audit/warden-01-contract-auditor.md` matches at least 1 annotation
  </acceptance_criteria>
  <done>All 3 Tier 3 historical audit documents have v2.1 annotations on stale references: regression-check-v2.0.md (13 refs annotated in grouped sections), warden-01-contract-auditor.md (QA-03 annotated), warden-cross-reference-v2.0.md (3 refs annotated). All original content preserved for audit traceability.</done>
</task>

</tasks>

<verification>
- `grep -c 'ADMIN_STALL_THRESHOLD' audit/v1.1-parameter-reference.md` returns >= 1
- `grep -c 'v2.1 Note\|v2.1 Update\|v2.1 REMOVED' audit/v1.2-rng-data-flow.md audit/v1.2-rng-functions.md` returns > 0 for both
- `grep -c 'v2.1 Note' audit/regression-check-v2.0.md audit/warden-01-contract-auditor.md audit/warden-cross-reference-v2.0.md` returns > 0 for all three
- `grep '12 hours' audit/EXTERNAL-AUDIT-PROMPT.md` matches
</verification>

<success_criteria>
All 7 Tier 2/2b/3 audit documents updated: parameter-reference.md has governance constants and decay schedule, RNG docs have v2.1 annotations, external audit prompt has corrected time constants, and all 3 Tier 3 historical docs have annotations preserving audit traceability while noting what changed in v2.1.
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-doc-sync/25-03-SUMMARY.md`
</output>
