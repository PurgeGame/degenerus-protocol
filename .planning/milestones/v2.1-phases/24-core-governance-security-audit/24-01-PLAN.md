---
phase: 24-core-governance-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v2.1-governance-verdicts.md
autonomous: true
requirements: [GOV-01]

must_haves:
  truths:
    - "lastVrfProcessedTimestamp slot number is computed from compiler output and documented"
    - "No slot collision exists between lastVrfProcessedTimestamp and any DegenerusGame-declared storage"
    - "All governance-touched storage variables are mapped to their slots"
  artifacts:
    - path: "audit/v2.1-governance-verdicts.md"
      provides: "GOV-01 storage layout verdict with slot numbers"
      contains: "GOV-01"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "audit/v2.1-governance-verdicts.md"
      via: "compiler storageLayout JSON output"
      pattern: "lastVrfProcessedTimestamp"
---

<objective>
Verify storage layout safety for `lastVrfProcessedTimestamp` and all governance-touched storage variables via compiler output.

Purpose: GOV-01 is the blocking requirement -- a slot collision would invalidate every downstream analysis. This must be verified first before any function-level audit proceeds.
Output: `audit/v2.1-governance-verdicts.md` with GOV-01 verdict section including exact slot numbers from compiler JSON.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-core-governance-security-audit/24-RESEARCH.md

<interfaces>
<!-- Key storage variable declarations from DegenerusGameStorage.sol -->

From contracts/storage/DegenerusGameStorage.sol (line 1616):
```solidity
/// @dev Timestamp of the last successfully processed VRF word.
///      Used by governance to detect VRF stalls (time-based vs day-gap-based).
///      Initialized in wireVrf(), updated in _applyDailyRng().
uint48 internal lastVrfProcessedTimestamp;
```
This is the LAST variable declared in DegenerusGameStorage (1617 lines total).

From contracts/DegenerusGame.sol:
DegenerusGame inherits DegenerusGameStorage. Verify DegenerusGame declares NO additional storage variables (only immutables and constants).

From contracts/modules/DegenerusGameAdvanceModule.sol:
Module inherits DegenerusGameStorage. Executes via delegatecall in DegenerusGame's storage context.

From contracts/DegenerusAdmin.sol:
Admin has its OWN storage (not delegatecall). Governance state variables:
```solidity
uint256 public proposalCount;
uint8 public activeProposalCount;
mapping(uint256 => Proposal) public proposals;
mapping(uint256 => mapping(address => Vote)) public votes;
mapping(uint256 => mapping(address => uint256)) public voteWeight;
```
These are in DegenerusAdmin's own storage context, NOT in DegenerusGameStorage. No collision risk between Admin and GameStorage -- but this must be explicitly stated in the verdict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and extract storage layout for DegenerusGameStorage and DegenerusGame</name>
  <files>audit/v2.1-governance-verdicts.md</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol
    - contracts/DegenerusGame.sol
    - contracts/modules/DegenerusGameAdvanceModule.sol
    - contracts/DegenerusAdmin.sol
    - hardhat.config.js
  </read_first>
  <action>
1. Run `npx hardhat compile` to generate fresh build artifacts with storageLayout output.

2. Extract storage layout JSON from the build-info files:
   - Find the build-info JSON: `ls artifacts/build-info/*.json`
   - Parse the storageLayout for `DegenerusGameStorage` contract
   - Find `lastVrfProcessedTimestamp` -- record its exact slot number, offset, and type
   - Record ALL storage variables in the final 5 slots (neighbors of lastVrfProcessedTimestamp)

3. Extract storage layout for `DegenerusGame` contract:
   - Verify DegenerusGame adds NO new storage variables beyond what DegenerusGameStorage declares
   - If it does, record them and check for slot collision with lastVrfProcessedTimestamp

4. Verify DegenerusAdmin storage is SEPARATE:
   - DegenerusAdmin is a standalone contract (not inherited by Game, not accessed via delegatecall)
   - Its governance storage (proposalCount, activeProposalCount, proposals mapping, votes mapping, voteWeight mapping) exists in DegenerusAdmin's own storage context
   - No collision is possible between Admin storage and GameStorage -- but state this explicitly

5. Map all governance-touched storage variables across all 5 contracts:
   - DegenerusGameStorage: `lastVrfProcessedTimestamp` (slot from compiler)
   - DegenerusGameAdvanceModule: `rngLockedFlag`, `vrfRequestId`, `rngRequestTime`, `rngWordCurrent`, `vrfCoordinator`, `vrfSubscriptionId`, `vrfKeyHash` (all in GameStorage layout, written by AdvanceModule via delegatecall)
   - DegenerusAdmin: `proposalCount`, `activeProposalCount`, `proposals`, `votes`, `voteWeight`, `coordinator`, `subscriptionId`, `vrfKeyHash` (Admin's own storage)
   - DegenerusStonk: reads `lastVrfProcessed()` via external call (no storage writes)

6. Create `audit/v2.1-governance-verdicts.md` with the following structure:

```markdown
# VRF Governance Security Audit — Verdicts

**Phase:** 24 — Core Governance Security Audit
**Auditor:** Adversarial automated audit (CP-01 protocol applied)
**Date:** 2026-03-17
**Contracts audited:** DegenerusAdmin, DegenerusGameAdvanceModule, DegenerusGameStorage, DegenerusGame, DegenerusStonk

---

## GOV-01: Storage Layout Verification

**Verdict:** [PASS/FAIL/KNOWN-ISSUE]
**Severity:** [N/A or severity if issue found]

### Evidence

**Compiler output:** Hardhat storageLayout JSON
**Slot map for lastVrfProcessedTimestamp:**
- Variable: `lastVrfProcessedTimestamp`
- Contract: `DegenerusGameStorage`
- Slot: [exact number from JSON]
- Offset: [offset in bytes]
- Type: `t_uint48`

**Neighboring slots:**
| Slot | Variable | Type | Contract |
|------|----------|------|----------|
| ... | ... | ... | ... |

**Governance-touched variable map:**
| Variable | Storage Context | Slot | Written By |
|----------|----------------|------|------------|
| lastVrfProcessedTimestamp | DegenerusGame (via delegatecall) | [N] | _applyDailyRng(), wireVrf() |
| rngLockedFlag | DegenerusGame (via delegatecall) | [N] | updateVrfCoordinatorAndSub() |
| ... | ... | ... | ... |

### Analysis

[Detailed analysis of collision risk]

### Adversarial Check

**Attack attempted:** [What was tried to find a collision]
**Result:** [Why it fails/succeeds]
```

7. Apply adversarial persona (CP-01):
   - Assume the code is wrong -- actively look for a slot collision
   - Check if DegenerusGame has ANY storage variable declarations (not just what it inherits)
   - Check if any module declares storage that could shadow GameStorage variables
   - Check if the AdvanceModule's delegatecall writes could hit a wrong slot if layout changed
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -5 && test -f audit/v2.1-governance-verdicts.md && grep -c "GOV-01" audit/v2.1-governance-verdicts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v2.1-governance-verdicts.md exists and contains "## GOV-01: Storage Layout Verification"
    - File contains "**Verdict:**" with PASS, FAIL, or KNOWN-ISSUE
    - File contains "**Slot:**" with a numeric slot number for lastVrfProcessedTimestamp
    - File contains "Governance-touched variable map" table with at least 6 rows
    - File contains "### Adversarial Check" section with attack description
    - `npx hardhat compile` succeeds without errors
  </acceptance_criteria>
  <done>GOV-01 verdict written with exact slot numbers from compiler output, collision analysis complete, adversarial check documented</done>
</task>

</tasks>

<verification>
- `npx hardhat compile` succeeds
- `audit/v2.1-governance-verdicts.md` exists with GOV-01 section
- Slot number for `lastVrfProcessedTimestamp` is documented from compiler JSON (not guessed)
- No slot collision identified (or collision documented as finding if found)
</verification>

<success_criteria>
GOV-01 verdict is written with compiler-verified slot numbers. All governance-touched storage variables are mapped. Adversarial collision analysis is documented. The verdict file structure is established for all subsequent plans to append to.
</success_criteria>

<output>
After completion, create `.planning/phases/24-core-governance-security-audit/24-01-SUMMARY.md`
</output>
