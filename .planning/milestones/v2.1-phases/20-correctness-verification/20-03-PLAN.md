---
phase: 20-correctness-verification
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - test/unit/DGNRSLiquid.test.js
  - test/unit/DegenerusStonk.test.js
autonomous: true
requirements: [CORR-03, CORR-04]

must_haves:
  truths:
    - "Test coverage gaps for sDGNRS/DGNRS edge cases are filled with new tests"
    - "All existing tests still pass (no regression)"
    - "Fuzz tests compile cleanly with forge build"
  artifacts:
    - path: "test/unit/DGNRSLiquid.test.js"
      provides: "Additional edge case tests for DGNRS wrapper functions"
      contains: "transferFromPool.*zero address"
    - path: "test/unit/DegenerusStonk.test.js"
      provides: "Additional edge case tests for sDGNRS functions"
      contains: "wrapperTransferTo"
  key_links:
    - from: "test/unit/DGNRSLiquid.test.js"
      to: "contracts/DegenerusStonk.sol"
      via: "test coverage of DGNRS wrapper functions"
      pattern: "DegenerusStonk|DGNRS"
    - from: "test/unit/DegenerusStonk.test.js"
      to: "contracts/StakedDegenerusStonk.sol"
      via: "test coverage of sDGNRS core functions"
      pattern: "StakedDegenerusStonk|sDGNRS"
---

<objective>
Fill test coverage gaps identified by Phase 20 research for sDGNRS/DGNRS edge cases, verify all tests pass, and confirm fuzz test compilation.

Purpose: CORR-03 requires test coverage for new/changed functions. CORR-04 requires fuzz test compilation and correctness. The research identified 6 specific coverage gaps (zero-address recipients, zero-amount deposits, multi-asset burn paths). This plan fills those gaps without modifying the deploy fixture.

Output: Extended test files with additional edge case coverage, confirmed green test suite, confirmed fuzz compilation.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-correctness-verification/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge case tests for sDGNRS and DGNRS coverage gaps</name>
  <files>test/unit/DGNRSLiquid.test.js, test/unit/DegenerusStonk.test.js</files>
  <read_first>
    - test/unit/DGNRSLiquid.test.js (full file -- understand existing test structure, fixture setup, helper patterns)
    - test/unit/DegenerusStonk.test.js (full file -- understand existing test structure, fixture setup, helper patterns)
    - contracts/StakedDegenerusStonk.sol (lines 234-260 for wrapperTransferTo, lines 286-294 for depositSteth, lines 300-332 for transferFromPool)
    - contracts/DegenerusStonk.sol (lines 85-98 for transfer/transferFrom edge cases)
  </read_first>
  <action>
Analyze the existing test files to understand the fixture setup (how contracts are deployed, what accounts are available, how game/wrapper addresses are configured). Then add targeted tests for the 6 identified coverage gaps.

**IMPORTANT:** Do NOT modify the deploy fixture. All new tests must work within the existing test infrastructure. If a gap cannot be tested without fixture changes (e.g., BURNIE burn path requires actual BURNIE deposits which the fixture may not support), document the gap as "untestable without fixture changes" in a comment and skip.

**Coverage gaps to address (from 20-RESEARCH.md):**

1. **transferFromPool with zero address recipient** -- In DegenerusStonk.test.js, add a test in the `transferFromPool` describe block:
   - Call `transferFromPool(Pool.Whale, ethers.ZeroAddress, someAmount)` from the game address
   - Verify it reverts with `ZeroAddress` error (check StakedDegenerusStonk.sol for the actual revert condition -- if there's no zero-address check, the test should verify the actual behavior: tokens transferred to address(0) would effectively be burned from pools, which may be unintended)
   - If sDGNRS does NOT revert on zero address (check source code), document this as a coverage finding

2. **wrapperTransferTo with zero address** -- In DGNRSLiquid.test.js, add a test:
   - Call (via the DGNRS wrapper) a path that triggers wrapperTransferTo with zero address
   - This is triggered by `unwrapTo(address(0), amount)` from the creator -- should revert with `ZeroAddress` at the DGNRS level before reaching sDGNRS
   - Verify the revert

3. **wrapperTransferTo with amount > wrapper balance** -- In DGNRSLiquid.test.js:
   - Call `unwrapTo(someAddress, amount > wrapper sDGNRS balance)` from creator
   - Should revert with `Insufficient` (DGNRS._burn checks balance first)

4. **depositSteth with zero amount** -- In DegenerusStonk.test.js:
   - Call `depositSteth(0)` from the game address
   - Verify behavior (may succeed as a no-op if stETH.transferFrom(game, sDGNRS, 0) succeeds)
   - Document actual behavior

5. **DGNRS transfer to self** -- In DGNRSLiquid.test.js (relates to DELTA-L-01):
   - Have creator transfer DGNRS to themselves
   - Verify balance is unchanged after the transfer
   - Verify Transfer event is emitted
   - This validates the DELTA-L-01 acknowledged behavior

6. **burn with stETH + BURNIE backing** -- In DegenerusStonk.test.js or DGNRSLiquid.test.js:
   - Check if the test fixture deposits stETH or BURNIE into the sDGNRS contract
   - If yes, add a burn test that verifies proportional stETH/BURNIE output
   - If no (fixture only has ETH backing), add a comment: "// stETH/BURNIE burn path not testable without fixture modification -- fixture only provides ETH backing"

For each new test, follow the existing test file patterns exactly:
- Use the same `describe`/`it` nesting structure
- Use the same fixture loading pattern (`loadFixture`)
- Use the same assertion style (expect/revertedWithCustomError/etc.)
- Place new tests in the most relevant existing `describe` block

After adding tests, run the focused test suite to verify all pass:
```
npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js
```
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/DGNRSLiquid.test.js contains a test for self-transfer (DELTA-L-01 behavior)
    - test/unit/DGNRSLiquid.test.js contains a test for unwrapTo with zero address revert
    - test/unit/DGNRSLiquid.test.js contains a test for unwrapTo exceeding balance revert
    - test/unit/DegenerusStonk.test.js contains a test for transferFromPool with zero address
    - test/unit/DegenerusStonk.test.js contains a test for depositSteth with zero amount
    - `npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js` exits 0
    - Total test count is >= 80 (was 38 + 37 = 75 baseline, adding 5-6 new tests)
  </acceptance_criteria>
  <done>At least 5 new edge case tests added across both test files. All focused tests pass. Coverage gaps documented.</done>
</task>

<task type="auto">
  <name>Task 2: Verify fuzz test compilation and full test suite regression</name>
  <files></files>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (verify sDGNRS/DGNRS imports and deploy order)
    - test/fuzz/DeployCanary.t.sol (verify address assertions for SDGNRS and DGNRS)
    - test/fuzz/AffiliateDgnrsClaim.t.sol (verify StakedDegenerusStonk import)
  </read_first>
  <action>
**Step 1: Verify Foundry fuzz test compilation.**

Run:
```
forge build --force 2>&1
```

Verify:
- Exit code 0
- Only warnings (typecast lint), no errors
- All fuzz test contracts compile (DeployCanary.t.sol, AffiliateDgnrsClaim.t.sol, ShareMathInvariants.t.sol)

**Step 2: Verify contract references in fuzz tests.**

Grep the fuzz test files to confirm correct contract name references:
```
grep -rn 'StakedDegenerusStonk\|DegenerusStonk' test/fuzz/
```

Confirm:
- `test/fuzz/helpers/DeployProtocol.sol` imports and deploys both StakedDegenerusStonk and DegenerusStonk
- `test/fuzz/DeployCanary.t.sol` asserts SDGNRS and DGNRS addresses match ContractAddresses
- `test/fuzz/AffiliateDgnrsClaim.t.sol` imports StakedDegenerusStonk and uses Pool.Affiliate enum
- No references to old `IDegenerusStonk` interface (pre-split)
- No references to `burnForGame` (removed function)

**Step 3: Run full Hardhat test suite for regression.**

Run:
```
npm test 2>&1 | tail -20
```

Verify:
- Focused sDGNRS/DGNRS tests pass (should be 80+ now with new tests from Task 1)
- Full suite results match baseline: 1065+ passing, 26 pre-existing failures (all in affiliate/RNG/economic suites)
- No NEW failures introduced

**Step 4: Produce coverage summary.**

Catalog the final test counts:
- DGNRSLiquid.test.js: count tests (was 38 + new)
- DegenerusStonk.test.js: count tests (was 37 + new)
- Fuzz tests: compile status
- Full suite: passing/failing counts

Document any coverage gaps that could not be addressed without fixture changes.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build --force 2>&1 | tail -5 && npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js 2>&1 | grep -E 'passing|failing' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `forge build --force` exits 0 (no compilation errors)
    - `grep -rn 'IDegenerusStonk' test/fuzz/` returns zero matches (no stale interface references)
    - `grep -rn 'burnForGame' test/fuzz/` returns zero matches (no stale function references)
    - `npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js` shows 0 failing
    - Total passing test count across both files is >= 80
    - No NEW test failures in the full suite compared to the 26 pre-existing baseline
  </acceptance_criteria>
  <done>Fuzz tests compile cleanly. All contract references verified correct (no stale IDegenerusStonk or burnForGame). Full test suite matches baseline (1065+ passing, 26 pre-existing failures). CORR-03 and CORR-04 requirements satisfied.</done>
</task>

</tasks>

<verification>
- `npx hardhat test test/unit/DGNRSLiquid.test.js test/unit/DegenerusStonk.test.js` shows 80+ passing, 0 failing
- `forge build --force` exits 0
- `npm test` shows no new failures beyond the 26 pre-existing baseline
- Coverage gaps documented (any untestable paths noted in test comments)
</verification>

<success_criteria>
- At least 5 new edge case tests added and passing
- Zero new test failures introduced
- Fuzz tests compile cleanly with correct contract references
- Full suite regression matches Phase 19 baseline (1065+ passing, 26 pre-existing failures)
- Any coverage gaps that require fixture changes are documented as comments in test files
</success_criteria>

<output>
After completion, create `.planning/phases/20-correctness-verification/20-03-SUMMARY.md`
</output>
