---
phase: 23-gas-optimization-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/DegenerusGameEndgameModule.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/modules/DegenerusGameBoonModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/DegenerusGame.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/libraries/BitPackingLib.sol
  - contracts/libraries/JackpotBucketLib.sol
  - contracts/BurnieCoin.sol
  - contracts/BurnieCoinflip.sol
  - contracts/DegenerusVault.sol
  - contracts/DegenerusAffiliate.sol
  - contracts/DegenerusJackpots.sol
  - contracts/DegenerusQuests.sol
  - contracts/DegenerusDeityPass.sol
  - contracts/StakedDegenerusStonk.sol
  - contracts/DegenerusStonk.sol
  - contracts/WrappedWrappedXRP.sol
autonomous: true
requirements: [GAS-01, GAS-02, GAS-03, GAS-04]

must_haves:
  truths:
    - "All APPROVED removals from the gas audit report have been applied to the source contracts"
    - "All existing tests still pass after changes (zero regressions)"
    - "All contracts compile cleanly with no new warnings"
    - "No storage variable was removed from DegenerusGameStorage.sol (only _deprecated_* renames)"
    - "JackpotModule bytecode size has decreased from baseline"
  artifacts:
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "JackpotModule with approved dead code removed"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Storage layout with _deprecated_* renames (slot alignment preserved)"
  key_links:
    - from: "audit/gas-optimization-report.md"
      to: "contracts/**/*.sol"
      via: "Implementation Order section in report"
      pattern: "APPROVED"
    - from: "contracts/**/*.sol"
      to: "npx hardhat test"
      via: "Full test suite regression gate"
      pattern: "passing"
---

<objective>
Apply all APPROVED behavior-preserving removals from the gas audit report to the source contracts, then verify compilation and tests.

Purpose: Implement the gas optimizations identified and validated in Plan 23-01. Every change must be behavior-preserving -- the full test suite is the regression gate. Focus on JackpotModule first (highest priority due to 95.9% size limit).

Output: Modified contract source files with dead code removed, compilation verified, test suite green.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-gas-optimization-dead-code-removal/23-RESEARCH.md
@.planning/phases/23-gas-optimization-dead-code-removal/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply APPROVED removals from gas audit report</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol,
    contracts/modules/DegenerusGameAdvanceModule.sol,
    contracts/modules/DegenerusGameLootboxModule.sol,
    contracts/modules/DegenerusGameMintModule.sol,
    contracts/modules/DegenerusGameDecimatorModule.sol,
    contracts/modules/DegenerusGameEndgameModule.sol,
    contracts/modules/DegenerusGameGameOverModule.sol,
    contracts/modules/DegenerusGameBoonModule.sol,
    contracts/modules/DegenerusGameWhaleModule.sol,
    contracts/modules/DegenerusGameDegeneretteModule.sol,
    contracts/DegenerusGame.sol,
    contracts/storage/DegenerusGameStorage.sol,
    contracts/libraries/BitPackingLib.sol,
    contracts/libraries/JackpotBucketLib.sol,
    contracts/BurnieCoin.sol,
    contracts/BurnieCoinflip.sol,
    contracts/DegenerusVault.sol,
    contracts/DegenerusAffiliate.sol,
    contracts/DegenerusJackpots.sol,
    contracts/DegenerusQuests.sol,
    contracts/DegenerusDeityPass.sol,
    contracts/StakedDegenerusStonk.sol,
    contracts/DegenerusStonk.sol,
    contracts/WrappedWrappedXRP.sol
  </files>
  <read_first>
    - audit/gas-optimization-report.md (the complete Scavenger/Skeptic report -- read the "Implementation Order" section first, then "Approved Removals" for exact code changes)
    - .planning/phases/23-gas-optimization-dead-code-removal/23-01-SUMMARY.md (what was analyzed, any notes)
    - (plus each contract file before modifying it -- read the file, find the exact lines, make the change)
  </read_first>
  <action>
Apply all APPROVED changes from the gas audit report's "Implementation Order" section, in the specified dependency order.

**Implementation rules:**
1. **Read each target contract file BEFORE modifying it** -- verify the exact lines match what the Scavenger identified
2. **JackpotModule FIRST** -- this is the highest priority target (95.9% of EVM size limit). Record its line count before and after.
3. **For GAS-01 (unreachable checks):** Remove the entire `require()` or `if` block. If the check was the only statement in a function, remove the function only if it has zero callers (confirmed by Skeptic).
4. **For GAS-02 (dead storage):** In DegenerusGameStorage.sol, rename variables to `_deprecated_*` prefix. DO NOT remove them. DO NOT reorder any storage variables. For non-storage dead variables (locals, memory), simply remove them.
5. **For GAS-03 (dead code paths):** Remove the unreachable branch. If it's an `else` clause, keep the `if` but remove the `else`. If it's a standalone `if`, remove the entire block. Remove unused internal/private functions.
6. **For GAS-04 (redundant SLOADs/calls):** Cache the storage read in a local variable at the top of the function. Replace subsequent reads with the local. For redundant external calls, cache the return value.

**CRITICAL safety constraints:**
- NEVER remove or reorder storage variables in DegenerusGameStorage.sol (only rename to `_deprecated_*`)
- NEVER remove public/external functions that appear in IDegenerusGame.sol or IDegenerusGameModules.sol
- NEVER remove event emissions
- NEVER change any function's external behavior -- only internal dead code
- If ANY doubt about a specific change, SKIP it and note why in the summary
- Apply ONLY changes with verdict "APPROVED" -- skip REJECTED, PARTIAL (unless the partial part is clearly specified), and NEEDS_HUMAN_REVIEW

**After all changes, run compilation:**
```bash
npx hardhat compile 2>&1
```
Fix any compilation errors before proceeding. Compilation errors mean a change was incorrect -- revert that specific change.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All APPROVED changes from the gas audit report applied to source contracts
    - `npx hardhat compile` succeeds with no errors
    - No storage variable removed from DegenerusGameStorage.sol (only _deprecated_* renames)
    - JackpotModule modified (if it had APPROVED findings)
    - Each changed file has a git-diff showing only the intended removal
  </acceptance_criteria>
  <done>
    - All APPROVED gas optimizations applied to source contracts
    - Compilation passes cleanly
    - No behavioral changes made (only dead code removal)
    - DegenerusGameStorage.sol slot layout is preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify zero regressions</name>
  <files>audit/gas-optimization-report.md</files>
  <read_first>
    - audit/gas-optimization-report.md (to know what was changed and update with test results)
  </read_first>
  <action>
Run the full Hardhat test suite to verify all changes are behavior-preserving:

```bash
cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test 2>&1
```

**Expected:** ~1,065 passing tests, 26 pre-existing failures (affiliate/RNG/economic -- unrelated to gas optimization scope). Zero NEW failures.

**If any NEW test failure appears:**
1. Identify which SCAV-NNN change caused it
2. Revert ONLY that specific change (not all changes)
3. Re-run the failing test to confirm reversion fixes it
4. Update the gas audit report: change that recommendation's verdict from APPROVED to "REVERTED -- test regression: {test name}"
5. Re-run full suite to confirm green

**After Hardhat passes, run Foundry fuzz tests:**
```bash
cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1
```

Note: `forge build` compilation is the primary Foundry gate. If fuzz tests have separate run commands (`npm run fuzz`), run those too.

**Update the gas audit report** with a "Test Verification" section at the bottom:
- Hardhat: X passing, Y failing (pre-existing), 0 new failures
- Foundry: compilation status
- Any reverted changes and why
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test 2>&1 | grep -E "passing|failing" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` output shows same pass count as baseline (~1,065 passing)
    - Zero new test failures (26 pre-existing failures unchanged)
    - `forge build` compiles successfully
    - audit/gas-optimization-report.md updated with "Test Verification" section
    - If any change was reverted, report documents which SCAV-NNN and why
  </acceptance_criteria>
  <done>
    - Full Hardhat test suite passes with zero new regressions
    - Foundry compiles cleanly
    - Gas audit report updated with test verification results
    - All applied changes confirmed behavior-preserving
  </done>
</task>

</tasks>

<verification>
- `npx hardhat compile` passes
- `npx hardhat test` shows zero new failures vs baseline
- `forge build` succeeds
- git diff shows only dead code removals (no behavioral changes)
- DegenerusGameStorage.sol has no removed/reordered storage variables
- audit/gas-optimization-report.md has "Test Verification" section
</verification>

<success_criteria>
- All APPROVED gas optimizations applied to source contracts
- Full test suite green (zero regressions)
- Compilation clean on both Hardhat and Foundry
- JackpotModule bytecode reduced (if applicable)
- Report updated with implementation results
</success_criteria>

<output>
After completion, create `.planning/phases/23-gas-optimization-dead-code-removal/23-02-SUMMARY.md`
</output>
