---
phase: 249-purchaselevel-correctness-proof
phase_number: 249
plan: 249-01
plan_id: 249-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v32-249-PLV.md
  - .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
autonomous: true
milestone: v32.0
milestone_name: Backfill Idempotency + purchaseLevel Underflow Audit
deliverable: audit/v32-249-PLV.md
phase_status: proof-only
write_policy: "Pure-proof phase per CONTEXT.md D-249-CF-04 / D-249-CF-05. Zero contracts/ or test/ writes. Writes confined to .planning/phases/249-purchaselevel-correctness-proof/ and audit/v32-249-PLV.md. KNOWN-ISSUES.md NOT touched in Phase 249 (KI promotions are Phase 253 FIND-03 only). v32.0 milestone READ-only LIFTED but Phase 249 has no contract or test writes regardless."

# Requirement traceability — every Phase 249 REQ-ID MUST appear in this list
requirements:
  - PLV-01
  - PLV-02
  - PLV-03
  - PLV-04
  - PLV-05
  - PLV-06
requirements_addressed:
  - PLV-01
  - PLV-02
  - PLV-03
  - PLV-04
  - PLV-05
  - PLV-06

# HEAD anchor (per CONTEXT.md D-249-CF-01)
baseline: cc68bfc7
head: acd88512
head_anchor: acd88512
head_sha: acd88512c516bef51981d8b6f49de9878aba9159
head_anchor_history:
  - sha: acd88512
    scope: "Phase 249 inherits Phase 247 / Phase 248 anchor. Both WIP guards (turbo at advanceGame:173 + backfill at rngGate:1174) already committed inside this SHA per CONTEXT.md D-249-CF-01. ContractAddresses.sol working-tree changes ignored per D-247-03 carry-forward. test/edge/LastPurchaseDayRace.test.js untracked, owned by Phase 251 per D-247-02 carry-forward."

# Verified function line ranges at HEAD acd88512 (read via `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol`)
# CONTEXT.md cited some sites at slightly different line numbers (e.g., L167 turbo guard, L1167 backfill guard, L748 underflow site).
# Verified line numbers at HEAD acd88512 supersede CONTEXT.md prose; CONTEXT.md is NOT re-edited.
canonical_line_ranges:
  advanceGame_function_header: 160
  advanceGame_turbo_block_open: 167
  advanceGame_turbo_guard_line: 173
  advanceGame_turbo_block_close: 182
  advanceGame_lastPurchase_bind: 183
  advanceGame_purchaseLevel_bind: 185
  advanceGame_enforceDailyMintGate_call: 204
  advanceGame_tqReadKey_call_inJackpot: 218
  advanceGame_runProcessTicketBatch_call: 223
  advanceGame_tqReadKey_call_purchase: 266
  advanceGame_runProcessTicketBatch_call_purchase: 280
  advanceGame_swapAndFreeze_call: 301
  advanceGame_ffLevel_plus4: 312
  advanceGame_processPhaseTransition_call: 316
  advanceGame_consolidatePools_lvl_branch: 347
  advanceGame_purchaseLevel_arg_in_jackpot_branch: 360
  advanceGame_dailyJackpot_region_open: 370
  advanceGame_lastPurchaseDay_check: 371
  advanceGame_purchaseLevel_eq_1: 372
  advanceGame_payDailyJackpot_call: 385
  advanceGame_payDailyCoinJackpot_args: "387-390"
  advanceGame_payDailyCoinJackpot_plus1: 389
  advanceGame_payDailyCoinJackpot_plus4: 390
  advanceGame_targetMet_eval: "393-397"
  advanceGame_levelPrizePool_underflow_primary: 397
  advanceGame_lastPurchaseDay_write_in_jackpot: 399
  advanceGame_compressedJackpotFlag_set: 401
  advanceGame_unlockRng_call_in_jackpot: 404
  advanceGame_dailyJackpot_region_close: 407
  advanceGame_nextLevel_plus1: 411
  advanceGame_levelPrizePool_write: 424
  advanceGame_consolidatePools_purchaseLevel_arg: 428
  distributeYieldSurplus_function_header: 707
  distributeYieldSurplus_purchaseLevel_param: 734
  distributeYieldSurplus_modulo_9: 750
  distributeYieldSurplus_levelPrizePool_underflow_secondary: 752
  internal_purchaseLevel_local_derivation: 1097
  processPhaseTransition_function_header: 1503
  processPhaseTransition_purchaseLevel_param: 1504
  processPhaseTransition_targetLevel_plus99: 1507
  finalizeRngRequest_level_increment: 1609
  unlockRng_function_header: 1702
  unlockRng_dailyIdx_writer: 1703
  rngGate_function_header: 1152
  rngGate_backfill_guard: 1174
  rngGate_backfillGapDays_call: 1176
  storage_level_state_var_line: 250
  mintModule_purchaseLevel_ternary: 923
  mintModule_priceForLevel_call: 924
  whaleModule_purchaseLevel_param: 841
  whaleModule_lootboxEth_pack_site: 876
  lootboxModule_packed_decode: 532
  lootboxModule_grace_passthrough: 552
  burnieCoinflip_purchaseLevel_param_primary: 578
  burnieCoinflip_modulo_10_primary: 590
  burnieCoinflip_bafLevel_write: 596
  burnieCoinflip_purchaseLevel_param_secondary: 1035
  burnieCoinflip_modulo_10_secondary: 1041
attribution_note: |
  CONTEXT.md (planning-time text) cited the turbo guard at AdvanceModule:167 (the IF block opener),
  but the load-bearing `!rngLockedFlag` conjunct is at L173 (the third conjunct inside the IF condition
  spanning L167-173 wrapped across multiple lines per the source layout). ROADMAP success criterion 4
  cited the underflow site at AdvanceModule:748 — verified line at HEAD acd88512 is L752 (the
  `levelPrizePool[purchaseLevel - 1]` array-index inside `_distributeYieldSurplus`). Plan honors verified
  line numbers from `git show acd88512` and notes the discrepancies inline; CONTEXT.md / ROADMAP are NOT
  re-edited per D-249-CF-08 spirit.

# Pre-locked verdict shape per CONTEXT.md D-249-CF-06 (3-bucket per row)
verdict_buckets: [SAFE, EXCEPTION, FINDING_CANDIDATE]

# Octant ID convention per CONTEXT.md `<specifics>` suggestion (planner final call: ordered-triple FFF..TTT)
octant_ids:
  - O-PLV-FFF  # lastPurchase=F ∧ rngLocked=F ∧ jackpot=F
  - O-PLV-FFT  # lastPurchase=F ∧ rngLocked=F ∧ jackpot=T
  - O-PLV-FTF  # lastPurchase=F ∧ rngLocked=T ∧ jackpot=F
  - O-PLV-FTT  # lastPurchase=F ∧ rngLocked=T ∧ jackpot=T
  - O-PLV-TFF  # lastPurchase=T ∧ rngLocked=F ∧ jackpot=F
  - O-PLV-TFT  # lastPurchase=T ∧ rngLocked=F ∧ jackpot=T
  - O-PLV-TTF  # lastPurchase=T ∧ rngLocked=T ∧ jackpot=F  ← THE LOAD-BEARING UNREACHABLE OCTANT (lvl=0 cell)
  - O-PLV-TTT  # lastPurchase=T ∧ rngLocked=T ∧ jackpot=T  ← also UNREACHABLE-by-INV-PLV-B-01

# Named-invariant ID scheme per CONTEXT.md `<specifics>` suggestion (per-axis split: A/B/C)
named_invariants:
  - id: INV-PLV-A-01
    statement: "lastPurchaseDay = true ⇒ rngLockedFlag = false at every reachable advanceGame entry boundary"
    anchor_cite: "AdvanceModule:173 turbo guard `!rngLockedFlag && !inJackpot && !lastPurchaseDay` writes lastPurchaseDay=true at L178 ONLY when rngLockedFlag=false (the third conjunct); plus _finalizeRngRequest at L1607-1609 sets rngLockedFlag=true AFTER level pre-increment when lastPurchaseDay was already true; plus _unlockRng at L1703-1704 clears rngLockedFlag=false when dailyIdx advances. Net result: the (T,T) co-occurrence is a transient bind-time-only state never observed at advanceGame entry."
    load_bearing_for: "O-PLV-TTF lvl=0 cell UNREACHABLE-disproof (PLV-02 + PLV-03)"
  - id: INV-PLV-B-01
    statement: "jackpotPhaseFlag = true ⇒ lastPurchaseDay = false at write boundary"
    anchor_cite: "AdvanceModule:442-444 `jackpotPhaseFlag = true; lastPurchaseDay = false;` are co-written at the phase transition boundary; v25/v29/v30/v31 audits cited this invariant; PLV-02 inherits it for O-PLV-TFT / O-PLV-TTT cells (jackpot=T forces lastPurchase=F)."
    load_bearing_for: "O-PLV-TFT + O-PLV-TTT cells UNREACHABLE-disproof"
  - id: INV-PLV-C-01
    statement: "level only advances inside _finalizeRngRequest at AdvanceModule:1609 (sole writer; pre-increment when lastPurchaseDay=true at RNG request time)"
    anchor_cite: "Sole writer grep universe: `git grep -n '\\blevel\\s*=\\|\\blevel\\s*+=\\|\\blevel\\s*++' acd88512 -- 'contracts/storage/DegenerusGameStorage.sol' 'contracts/modules/*.sol' 'contracts/*.sol'` enumerates writers; PLV-02 reachability for `lvl=0` boundary case (the bootstrapping cell) rests on this — `lvl=0` is reachable ONLY before the first level-increment; once incremented, lvl≥1 sticks."
    load_bearing_for: "PLV-02 lvl=0 boundary cell reachability classification"

# Locked truths from CONTEXT.md scope-guard rules — restated for downstream executor visibility
truths:
  - "HEAD anchor acd88512 is the SOLE audit anchor for Phase 249 (D-249-CF-01). Every reference to file:line resolves at this SHA via `git show acd88512:<path>`."
  - "Phase 247 catalog (audit/v32-247-DELTA-SURFACE.md) Section 6 D-247-I007..I012 is the SOLE scope input for PLV-01..06. Phase 249 does NOT re-derive the universe from git diffs (D-249-CF-09 caveat: grep commands cited per claim, but universe is fixed by D-247-I007..I012)."
  - "Zero contracts/ or test/ writes in Phase 249 (D-249-CF-04 / D-249-CF-05). All writes confined to .planning/phases/249-purchaselevel-correctness-proof/ and audit/v32-249-PLV.md."
  - "No F-32-NN finding ID emission anywhere in Phase 249 (D-249-CF-03). Finding-candidates land in a `Finding Candidates` subsection with path:line + suggested severity for Phase 253 routing."
  - "MintModule:923 ternary is a LIVE FINDING_CANDIDATE seed per CONTEXT.md `<specifics>` first bullet — mirrors the L185 ternary shape WITHOUT the `!rngLockedFlag` guard. Phase 249 PLV-01 row carries reachability proof for `(cachedJpFlag = T ∧ cachedLevel = 0)`. If reachable, FINDING_CANDIDATE for Phase 250 SIB-01 + Phase 253 FIND-01. If unreachable, the disproof must cite the boundary that prevents the co-occurrence."
  - "Single deliverable `audit/v32-249-PLV.md` (D-249-CF-02). 7-section format mirroring v32-248-BFL.md: §1 PLV-01 / §2 PLV-02 / §3 PLV-03 / §4 PLV-04 / §5 PLV-05 / §6 PLV-06 / §7 Finding Candidates + Phase 251 hand-off appendix."
  - "Audit deliverable v32-249-PLV.md flips to FINAL READ-only on Task 4 plan-close commit per D-249-CF-02 / D-247-22 carry-forward."
  - "Single-plan multi-task topology with 4 tasks per CONTEXT.md `Plan Topology` section. Each task lands its own atomic commit per D-247-14 carry-forward. Commit message format: `audit(249-01): Task N — <one-line summary>`."

# must_haves block — derived verbatim from ROADMAP Phase 249 success criteria (5 items, mapped to 6 PLV REQs)
must_haves:
  truths:
    - "audit/v32-249-PLV.md §1 enumerates every read site of `purchaseLevel` across AdvanceModule + every delegating module (one PLV-01-Vnn row per read site) tagged with the local invariant required (≥1, >level, level+1, packed, etc.) and a verifier-reproducible grep recipe (D-249-CF-09)."
    - "§2 contains the explicit 4-D state-space sweep over (lastPurchaseDay × rngLockedFlag × jackpotPhaseFlag × level) encoded as 8 octants × 3 level bins (24 cells per D-249-04); every reachable cell carries a verdict proving purchaseLevel ≥ 1 at the bind line AdvanceModule:185, and every UNREACHABLE cell carries an explicit reachability-disproof citation with named-invariant ID."
    - "§3 names the unreachable state `(lastPurchase = T ∧ rngLockedFlag = T ∧ lvl = 0)` (octant O-PLV-TTF, lvl=0 bin) and proves the ternary at AdvanceModule:185 cannot return 0 by showing the `!rngLockedFlag` turbo guard at L173 short-circuits before that combination can bind."
    - "§4 lists every `purchaseLevel`-arithmetic call site (AdvanceModule:397 + L752 underflow sites; L185 / L389 / L411 +1 sites; L312 / L390 +4 sites; L750 %10==9 modulo; L218 / L266 _tqReadKey; L424 array-index; cross-module L590 / L1041 BurnieCoinflip %10==0; L924 MintModule priceForLevel; L552 LootboxModule passthrough) with one verdict row each proving no underflow / overflow / out-of-bounds at any reachable purchaseLevel value; `-1` rows cross-cite PLV-02 + PLV-03 + PLV-05 (D-249-08)."
    - "§5 symbolically reproduces the testnet panic 0x11 trigger sequence (blocks 10759449 + 10761786 per D-249-CF-10) using a BFL-03-style state-transition walk (D-249-11); pre-fix walk shows purchaseLevel = 0 at L185 → panic 0x11 at L397/L752; post-fix walk shows the new `!rngLockedFlag` conjunct at L173 fails → turbo block skipped → no `lastPurchaseDay = true` write → L185 binds `lvl + 1` → SAFE."
    - "§6 proves the daily-jackpot region L370-407 does not strand state in a `target met but never resolves` condition under the guard via a per-branch invariant table (D-249-12) + strand-disproof attestation showing zero early-return / revert / break between L399 (`lastPurchaseDay = true`) and L404 (`_unlockRng(day)`); §6 also emits the Phase 252 POST31-02 composition hand-off row (D-249-13)."
  artifacts:
    - path: "audit/v32-249-PLV.md"
      provides: "PLV-01..06 single-deliverable proof + Phase 251 TST-01/02/03 test-stub hand-off + Finding Candidates subsection (if any rows classified FINDING_CANDIDATE — most likely the MintModule:923 row per CONTEXT.md `<specifics>` first bullet)"
      contains: "## Section 1 — PLV-01, ## Section 2 — PLV-02, ## Section 3 — PLV-03, ## Section 4 — PLV-04, ## Section 5 — PLV-05, ## Section 6 — PLV-06, ## Phase 251 TST-01/02/03 Hand-Off"
      min_lines: 350
    - path: ".planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md"
      provides: "Plan-close summary with 4 atomic per-task commit SHAs, per-REQ row counts, scope-guard deferrals (if any), Phase 249 → Phase 250 / 251 / 252 / 253 hand-off signals"
      contains: "## Plan Metadata, ## Atomic Commits, ## Per-REQ Deliverable Counts, ## Scope-Guard Deferrals, ## Hand-Off Signals"
      min_lines: 50
  key_links:
    - from: "audit/v32-247-DELTA-SURFACE.md Section 6 D-247-I007..I012"
      to: "audit/v32-249-PLV.md per-REQ section row scope (PLV-01..06)"
      via: "Phase 247 Consumer Index = sole scope input per D-249-CF-01"
      pattern: "PLV-0[1-6]"
    - from: "AdvanceModule:173 turbo guard third conjunct `!rngLockedFlag` (the WIP fix)"
      to: "PLV-03 ternary unreachable-state proof + PLV-04 -1 row cross-cite chain + PLV-05 post-fix walk"
      via: "guard short-circuits the (lastPurchase=T ∧ rngLocked=T ∧ lvl=0) bind path before L185 can return 0"
      pattern: "!rngLockedFlag"
    - from: "AdvanceModule:185 binding ternary `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1`"
      to: "PLV-02 4-D sweep (24 cells) + PLV-03 unreachable-state proof + PLV-04 -1 underflow rows"
      via: "ternary is the SOLE binder; every reachable octant cell evaluates this expression"
      pattern: "lastPurchase && rngLockedFlag\\) \\? lvl : lvl \\+ 1"
    - from: "MintModule:923 sibling ternary `cachedJpFlag ? cachedLevel : cachedLevel + 1` (NO !rngLockedFlag guard)"
      to: "PLV-01 cross-module row (potential FINDING_CANDIDATE) + Phase 250 SIB-01 hand-off"
      via: "same-shape sibling pattern; reachability of (cachedJpFlag=T ∧ cachedLevel=0) determines verdict"
      pattern: "cachedJpFlag \\? cachedLevel : cachedLevel \\+ 1"
    - from: "AdvanceModule:397 + L752 `levelPrizePool[purchaseLevel - 1]`"
      to: "PLV-04 underflow rows with cross-cite chain to PLV-02 + PLV-03 + PLV-05 (D-249-08)"
      via: "the literal v32.0 panic 0x11 trigger sites; safety chain established explicitly"
      pattern: "levelPrizePool\\[purchaseLevel - 1\\]"
    - from: "AdvanceModule:399-404 daily-jackpot region (lastPurchaseDay write → _unlockRng call)"
      to: "PLV-06 strand-disproof attestation + Phase 252 POST31-02 composition hand-off row"
      via: "by-inspection L399→L404 has zero early-return / revert / break; targetMet ⇒ _unlockRng same call"
      pattern: "lastPurchaseDay = true"
---

<objective>
Produce the authoritative v32.0 Phase 249 deliverable `audit/v32-249-PLV.md` proving the local-stack variable `purchaseLevel` (bound at `contracts/modules/DegenerusGameAdvanceModule.sol:185` via the ternary `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1`) can never evaluate to 0 — and therefore the panic 0x11 underflow at `levelPrizePool[uint24(purchaseLevel) - 1]` (AdvanceModule:397 primary trigger inside the daily-jackpot region L370-407 + AdvanceModule:752 secondary trigger inside `_distributeYieldSurplus`) cannot fire — at any reachable `(lastPurchaseDay, rngLockedFlag, jackpotPhaseFlag, level)` combination once the WIP `!rngLockedFlag` turbo guard at AdvanceModule:173 (the third conjunct of the IF block opening at L167) is in place. Plus underflow / overflow / out-of-bounds audit at every `purchaseLevel`-arithmetic call site (AdvanceModule + cross-module re-derivations across MintModule / WhaleModule / LootboxModule / BurnieCoinflip per D-249-01 wider scope). Plus symbolic reproduction of the testnet panic 0x11 trigger sequence at blocks 10759449 + 10761786. Plus daily-jackpot region L370-407 no-strand proof emitting a Phase 252 POST31-02 composition hand-off row.

Six requirements addressed in one plan (single-plan multi-task atomic-commit topology per CONTEXT.md D-249-CF-07, mirroring v32 Phase 248 247-01-PLAN.md / 248-01-PLAN.md):

- **PLV-01** — enumerate every read site of `purchaseLevel` across AdvanceModule (~30+ readsites) + cross-module re-derivations (MintModule:923 + LootboxModule:532 + WhaleModule:841 + BurnieCoinflip:578/1035 + AdvanceModule helpers L734/L1097/L1504; expected ~70+ rows total per D-249-01); tag each readsite with the local invariant required (`≥1`, `>level`, `level+1`, `packed`); per-row reachability proof depth per D-249-02 (no Phase 250 deferral for cross-module rows); column shape per D-249-03.
- **PLV-02** — 4-D state-space sweep encoded as 8 octants × 3 level bins (24 cells per D-249-04); every reachable cell carries a SAFE-with-purchaseLevel ≥ 1 verdict at the bind line AdvanceModule:185; every UNREACHABLE cell carries inline path:line cite + named-invariant ID + grep recipe per D-249-05; `level = uint24.max` overflow edge OOS-by-construction per D-249-06 (single one-line cite, NOT in 24-cell main sweep).
- **PLV-03** — prove the ternary at L185 cannot return 0 once the L173 turbo guard is in place; explicitly name the unreachable state as octant O-PLV-TTF lvl=0 cell; show the L173 `!rngLockedFlag` conjunct short-circuits before that combination can bind.
- **PLV-04** — single flat per-call-site table (D-249-07) with operator column; sites enumerated per CONTEXT.md `<specifics>` PLV-04 grep recipes; `-1` rows cross-cite PLV-02 + PLV-03 + PLV-05 per D-249-08; array-index oob both bounds covered per D-249-09.
- **PLV-05** — BFL-03-style state-transition walk (D-249-11) with two tables: §5.1 pre-fix walk shows `purchaseLevel = 0` at L185 → panic 0x11 at L397/L752; §5.2 post-fix walk shows L173 `!rngLockedFlag` short-circuits → turbo block skipped → no `lastPurchaseDay = true` write at L178 → L185 binds `lvl + 1` → SAFE.
- **PLV-06** — daily-jackpot region L370-407 per-branch invariant table (D-249-12) + strand-disproof attestation paragraph showing zero early-return / revert / break between L399 (`lastPurchaseDay = true`) and L404 (`_unlockRng(day)`); plus Phase 252 POST31-02 composition hand-off row per D-249-13.

4-task ordering per CONTEXT.md `Plan Topology` (planner final call, 4-task split selected — see rationale below):

1. **Task 1 (PLV-01 + PLV-02)** — Cross-module readsite enumeration (~70+ rows tagged with invariant required + grep recipes per D-249-CF-09) + 4-D state-space sweep (24 octant cells with named-invariant disproofs per D-249-05). Both REQs share the cross-module reachability scaffolding (every PLV-02 cell verdict cross-cites the PLV-01 rows that validate the underlying writers); merging avoids duplicate code-reading work.
2. **Task 2 (PLV-03 + PLV-04)** — Ternary unreachable-state proof (the load-bearing O-PLV-TTF lvl=0 cell disproof anchored on L173 turbo guard) + arithmetic-site flat table (D-249-07) with `-1` cross-cite chain (D-249-08) + array-index oob both-bounds coverage (D-249-09). PLV-03 is the formal closure of the PLV-02 UNREACHABLE classification + PLV-04 inherits PLV-03 as the safety anchor for `-1` rows; tight semantic coupling justifies merging.
3. **Task 3 (PLV-05 + PLV-06)** — Testnet panic 0x11 BFL-03-style state-transition walk (pre-fix + post-fix tables per D-249-10) + daily-jackpot per-branch invariant table + strand-disproof attestation + Phase 252 POST31-02 composition hand-off row (D-249-13). Both REQs are concrete-scenario proofs (PLV-05 multi-call testnet trigger; PLV-06 single-call resolution) and share the L399-L404 region as their proof anchor.
4. **Task 4 (Final assembly + Phase 251 hand-off appendix + READ-only flip + plan-close)** — Assemble §7 Finding Candidates + Phase 251 TST-01/02/03 hand-off appendix; mark FINAL READ-only on plan-close commit per D-249-CF-02 / D-247-22 carry-forward.

**Rationale for 4-task split (vs CONTEXT.md `Plan Topology` 4-task suggestion):** The 4-task split exactly matches CONTEXT.md `Plan Topology` recommendation and the v32 Phase 247 / 248 single-plan multi-task atomic-commit precedent (Phase 247 = 5 tasks, Phase 248 = 5 tasks; PLV has fewer cross-cutting attestations than BFL — no envelope re-verify equivalent of BFL-05 — so 4 tasks suffice). Each task lands ≤ 30% context cost; total plan ≤ 50% context per task per the GSD planner budget. No 3-task simplification considered because the testnet-walk + arithmetic-table merge would push Task 2 over 30% context.

4 atomic per-task commits per D-247-14 carry-forward, mirroring Phase 247 / 248 commit-message precedent (`audit(249-01): Task N — <summary>`).

Row ID scheme per CONTEXT.md D-249-CF-06 (V-row pattern):
- Per-REQ rows: `PLV-NN-VMM` (REQ-anchored, monotonic-within-REQ; e.g., `PLV-01-V01`, `PLV-01-V02`, ..., `PLV-02-V01`, ...).
- Octant rows in §2: `O-PLV-NNN` per D-249-04 (`O-PLV-FFF`, `O-PLV-FFT`, ..., `O-PLV-TTT`); each octant has 3 level-bin rows.
- Named-invariant rows: `INV-PLV-{A|B|C}-NN` per CONTEXT.md `<specifics>` per-axis split; concrete invariants pre-locked in frontmatter `named_invariants` block (`INV-PLV-A-01`, `INV-PLV-B-01`, `INV-PLV-C-01`).
- Verdicts in 3-bucket {SAFE, EXCEPTION, FINDING_CANDIDATE} per D-249-CF-06.
- Column shape per D-249-CF-06 + D-249-03 (extended per-section per D-249-07 / D-249-09 / D-249-10 / D-249-12 / D-249-13).

Finding-ID emission: NO `F-32-NN` IDs anywhere (CONTEXT.md D-249-CF-03 — Phase 253 FIND-01 owns). A `## Finding Candidates` subsection in §7 collects anomalies with path:line + rationale + suggested severity for Phase 253 routing. If zero candidates, the subsection is OMITTED. Most likely `FINDING_CANDIDATE`: the MintModule:923 row if `(cachedJpFlag = T ∧ cachedLevel = 0)` cell is reachable per CONTEXT.md `<specifics>` first bullet.

Scope boundary per D-249-CF-04 / D-249-CF-05: Zero `contracts/` or `test/` writes throughout — pure-proof phase. Working-tree state untouched (`M contracts/ContractAddresses.sol` ignored per D-247-03 carry-forward; `?? test/edge/LastPurchaseDayRace.test.js` Phase 251 scope per D-247-02 carry-forward).

Verified line numbers (read via `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol`) — frontmatter `canonical_line_ranges` is the source of truth; all task `<action>` blocks cite these. Discrepancies vs CONTEXT.md / ROADMAP are documented in `attribution_note` and inline; CONTEXT.md / ROADMAP are NOT re-edited.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/249-purchaselevel-correctness-proof/249-CONTEXT.md

<!-- Sole scope input per CONTEXT.md D-249-CF-01 (FINAL READ-only — cite, never edit) -->
@audit/v32-247-DELTA-SURFACE.md

<!-- Sibling pure-proof phase — direct format precedent (FINAL READ-only — cite, never edit) -->
@audit/v32-248-BFL.md

<!-- Phase 247 + 248 plan-shape precedent (single-plan multi-task atomic-commit pattern per D-249-CF-07) -->
@.planning/phases/247-delta-extraction-classification/247-01-PLAN.md
@.planning/phases/247-delta-extraction-classification/247-CONTEXT.md
@.planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md
@.planning/phases/248-backfill-idempotency-proof/248-01-PLAN.md
@.planning/phases/248-backfill-idempotency-proof/248-CONTEXT.md
@.planning/phases/248-backfill-idempotency-proof/248-01-SUMMARY.md

<!-- V-row format precedent (D-249-CF-06 carry-forward) -->
@audit/v31-244-RNG.md
@audit/v31-244-EVT.md
@audit/v31-244-QST.md
@audit/v31-244-GOX.md

<!-- Dual-carrier attestation precedent (D-249-13 hand-off row) -->
@audit/v31-245-SDR.md
@audit/v31-245-GOE.md

<!-- KI envelope source (light cross-cite only — KI NOT modified in Phase 249) -->
@audit/KNOWN-ISSUES.md

<!-- Project feedback rules -->
@memory/feedback_no_contract_commits.md
@memory/feedback_contract_locations.md
@memory/feedback_no_history_in_comments.md
@memory/feedback_rng_backward_trace.md
@memory/feedback_rng_commitment_window.md
@memory/feedback_skip_research_test_phases.md
</context>

<threat_model>
**Attack surface:** None. Phase 249 is a pure-proof phase per CONTEXT.md D-249-CF-04 / D-249-CF-05; deliverable is a read-only markdown file under `audit/v32-249-PLV.md`. No `contracts/` or `test/` writes. No on-chain state mutation. No external interface change. No deployment artifact change.

**Threats considered:** N/A — no executable code, no runtime behavior, no attack vector. STRIDE register empty by construction (no spoofable identity, no tamperable data flow, no repudiable action, no information disclosure surface, no denial-of-service vector, no elevation-of-privilege path — all categories N/A for documentation-only output).

**Mitigations:**
- D-249-CF-04 / D-249-CF-05 confine writes to `.planning/phases/249-purchaselevel-correctness-proof/` and `audit/v32-249-PLV.md`.
- ContractAddresses.sol working-tree changes ignored per D-247-03 carry-forward (deploy regen, no logic delta).
- `test/edge/LastPurchaseDayRace.test.js` untracked, Phase 251 scope per D-247-02 carry-forward (NOT touched in this phase).
- KNOWN-ISSUES.md NOT touched in Phase 249 — KI promotions are Phase 253 FIND-03 only.

**Severity gate:** Block on `high` — no severity threats applicable in a proof-only phase.

**Regression / accuracy risk:** Mis-stated ternary semantics, omitted readsite row, or wrong reachability disproof becomes a wrong input to Phases 250 (SIB-01..05) / 251 (TST-01..03) / 252 (POST31-02) / 253 (FIND/REG). Mitigated by:
- Per-row evidence burden: every PLV-NN-VMM row cites file:line at HEAD `acd88512`.
- D-249-CF-09 grep-reproducibility for path enumeration: every PLV-01 readsite + PLV-04 arithmetic-site claim cites the exact `grep` command used. Portable POSIX syntax (no GNU `-P` / Perl regex).
- D-249-CF-08 scope-guard deferral rule: gaps in Phase 247 catalog get recorded in this plan's SUMMARY.md, NOT silently extended into v32-249-PLV.md.
- Sanity gate on Task 1 first action: `git rev-parse acd88512` confirms anchor presence before any deliverable write.
- Named-invariant pre-locking in frontmatter `named_invariants` block — every UNREACHABLE cell in §2 cites a pre-locked invariant ID; downstream Task 2 PLV-03 proof anchors on the same INV-PLV-A-01 statement to avoid divergence.

Inherits parent phase threat model (READ-only LIFTED milestone but Phase 249 itself is pure-proof, zero contract-and-test surface).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PLV-01 + PLV-02 enumeration — cross-module purchaseLevel readsite catalog (~70+ rows) + 4-D state-space sweep (24 octant cells with named-invariant disproofs)</name>
  <files>audit/v32-249-PLV.md</files>
  <read_first>
    - audit/v32-249-PLV.md (current state — file does NOT exist before Task 1; Task 1 creates it)
    - .planning/phases/249-purchaselevel-correctness-proof/249-CONTEXT.md — specifically D-249-CF-01 (HEAD anchor acd88512), D-249-CF-02 (single deliverable), D-249-CF-03 (no F-32-NN), D-249-CF-04 / D-249-CF-05 (zero contracts/test writes), D-249-CF-06 (V-row scheme + 3-bucket verdict + column shape), D-249-CF-09 (grep-reproducibility), D-249-01 (wider scope: AdvanceModule + cross-module re-derivations), D-249-02 (per-row proof depth — no Phase 250 deferral for cross-module rows), D-249-03 (PLV-01 7-col shape + invariant tag), D-249-04 (8 octants × 3 level bins encoding), D-249-05 (inline path:line + named-invariant disproof), D-249-06 (level=uint24.max overflow OOS-by-construction)
    - .planning/REQUIREMENTS.md — PLV-01 + PLV-02 wordings (verbatim)
    - audit/v32-247-DELTA-SURFACE.md — specifically Section 1.4 acd88512 commit changelog rows D-247-C011 (advanceGame turbo guard hunk) + D-247-C012 (rngGate backfill guard hunk), Section 1.6 advanceGame turbo-guard finding-candidate bullets, Section 2 D-247-F010 (advanceGame MODIFIED_LOGIC), Section 3 D-247-X027..X029 (3 advanceGame entry paths), Section 6 Consumer Index rows D-247-I007 (PLV-01 row scope) + D-247-I008 (PLV-02 row scope)
    - audit/v32-248-BFL.md — §1 BFL-01 V-row format precedent (column shape `Row ID | Site (file:line) | Description | Pre-state | Post-state | Verdict | Evidence Cite`); §4 BFL-04 grep-cited universe attestation pattern (D-249-CF-09 mirror)
    - audit/v31-244-RNG.md — V-row format precedent (D-249-CF-06)

    HEAD-side reads at acd88512 (use `git show acd88512:<path>` — do NOT read working tree; ContractAddresses.sol working-tree changes are out-of-scope per D-247-03):
    - contracts/modules/DegenerusGameAdvanceModule.sol — focus ranges:
      - L160-488 advanceGame body (entry point + binding ternary + daily-jackpot region)
      - L167-182 turbo block with `!rngLockedFlag` conjunct at L173 (the WIP fix; the third conjunct of the IF condition at L173)
      - L183-185 `lastPurchase` + `purchaseLevel` bind lines
      - L204 / L218 / L223 / L266 / L280 / L295 / L301 / L312 / L316 / L347 / L357-360 (purchaseLevel read sites in advanceGame)
      - L370-407 daily-jackpot region (PLV-06 anchor; also PLV-01 readsites at L372 / L385 / L387-390 / L397 / L411 / L424 / L428)
      - L734 _distributeYieldSurplus parameter receive (PLV-01 helper readsite); L749 / L750 / L752 readsites in body
      - L1097 internal `purchaseLevel_ = level + 1` derivation (PLV-01 helper readsite); L1098 / L1100 readsites
      - L1504 _processPhaseTransition parameter receive (PLV-01 helper readsite); L1507 readsite
      - L1607-1609 _finalizeRngRequest level pre-increment (INV-PLV-C-01 anchor; load-bearing for PLV-02 lvl=0 reachability)
      - L1702-1704 _unlockRng (INV-PLV-A-01 anchor; rngLockedFlag clear paired with dailyIdx advance)
      - L160-185 turbo block + bind ternary as a single read for the named-invariant disproof of O-PLV-TTF cell
    - contracts/modules/DegenerusGameMintModule.sol — L915-935 (the cachedJpFlag ternary site at L923 + priceForLevel call at L924; THE LIVE FINDING_CANDIDATE per CONTEXT.md `<specifics>` first bullet — reachability of `(cachedJpFlag=T ∧ cachedLevel=0)` determines verdict)
    - contracts/modules/DegenerusGameWhaleModule.sol — L835-895 (parameter receive at L841 + packing site at L876 — the WhaleModule:876 packing invariant is load-bearing for LootboxModule:532 packed-decode invariant)
    - contracts/modules/DegenerusGameLootboxModule.sol — L525-560 (packed decode at L532 + grace-passthrough at L552)
    - contracts/BurnieCoinflip.sol — L575-600 (parameter receive at L578 + modulo at L590 + bafLevel write at L596) + L1030-1045 (parameter receive at L1035 + locked condition at L1041)
    - contracts/storage/DegenerusGameStorage.sol — L250 `level` storage variable (`uint24 public level = 0;`) — sole storage anchor for `lvl = level` read at AdvanceModule:165; load-bearing for INV-PLV-C-01 lvl=0 boundary case
  </read_first>
  <action>
**Step A — Sanity gate (mandatory; FIRST action of the plan):**

```
git rev-parse acd88512
git rev-parse HEAD
git status --porcelain contracts/ test/
```

Locked expectations:
- `git rev-parse acd88512` resolves to `acd88512c516bef51981d8b6f49de9878aba9159` (matches frontmatter `head_sha`)
- `git rev-parse HEAD` MAY equal `acd88512...` directly OR a docs-only commit ABOVE acd88512 (e.g., `23bb5aa6` at planning time per `git log` recent commits). If HEAD is above acd88512, verify `git diff acd88512..HEAD -- contracts/ test/` is EMPTY (the docs-only commits above the anchor must NOT touch contract or test files).
- `git status --porcelain contracts/ test/` returns EXACTLY two lines:
  - ` M contracts/ContractAddresses.sol` (deploy regen, ignored per D-247-03)
  - `?? test/edge/LastPurchaseDayRace.test.js` (Phase 251 scope per D-247-02)
  Any other line means the working tree drifted — STOP.

If ANY gate fails, STOP. Do NOT write `audit/v32-249-PLV.md`. Log the mismatch inline as a scope-guard deferral for the executor-orchestrator to surface to the user.

**Step B — Re-read source files at HEAD acd88512 (single-pass extraction; one read per file):**

```
git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol > /tmp/v32-249-AdvanceModule-acd88512.sol
git show acd88512:contracts/modules/DegenerusGameMintModule.sol > /tmp/v32-249-MintModule-acd88512.sol
git show acd88512:contracts/modules/DegenerusGameWhaleModule.sol > /tmp/v32-249-WhaleModule-acd88512.sol
git show acd88512:contracts/modules/DegenerusGameLootboxModule.sol > /tmp/v32-249-LootboxModule-acd88512.sol
git show acd88512:contracts/BurnieCoinflip.sol > /tmp/v32-249-BurnieCoinflip-acd88512.sol
git show acd88512:contracts/storage/DegenerusGameStorage.sol > /tmp/v32-249-Storage-acd88512.sol

# Universe-of-readsites grep (must be cited verbatim in §1 evidence column per D-249-CF-09):
git grep -n 'purchaseLevel' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' 'contracts/storage/*.sol' | tee /tmp/v32-249-purchaseLevel-all.txt
git grep -n 'levelPrizePool\[' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' | tee /tmp/v32-249-levelPrizePool-refs.txt

# Sole-writer attestation greps for the 3 named invariants (load-bearing for §2 disproofs):
git grep -nE '\blastPurchaseDay\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' | tee /tmp/v32-249-lastPurchaseDay-writers.txt
git grep -nE '\brngLockedFlag\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' | tee /tmp/v32-249-rngLockedFlag-writers.txt
git grep -nE '\bjackpotPhaseFlag\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' | tee /tmp/v32-249-jackpotPhaseFlag-writers.txt
git grep -nE '\blevel\s*=|\blevel\s*\+=|\blevel\+\+|\+\+level' acd88512 -- 'contracts/storage/*.sol' 'contracts/modules/*.sol' 'contracts/*.sol' | tee /tmp/v32-249-level-writers.txt
```

Expected results (from frontmatter `canonical_line_ranges` + CONTEXT.md `<code_context>` "purchaseLevel identifier appears in 11 contracts / 104 hits"):
- `/tmp/v32-249-purchaseLevel-all.txt` — ~70+ hits across 5+ files (AdvanceModule + MintModule + WhaleModule + LootboxModule + BurnieCoinflip + ancillary modules per CONTEXT.md `<code_context>` `~30+ are inside AdvanceModule (the L185-binder chain) and ~70+ across cross-module re-derivations`)
- `/tmp/v32-249-levelPrizePool-refs.txt` — exactly 7 hits (L176 / L397 / L424 / L558 / L647 / L752 / L1858 in AdvanceModule per the grep run during plan authoring); 3 are array-index-on-purchaseLevel sites (L397 / L424 / L752); the other 4 are array-index-on-`lvl` sites (out of PLV-04 scope by virtue of indexing on `lvl` not `purchaseLevel`)
- `/tmp/v32-249-lastPurchaseDay-writers.txt` — at least 3 writers: L178 (turbo block T-write inside the L173-guarded IF), L399 (daily-jackpot T-write inside §6 region), L444 (phase-transition F-write at jackpot-phase entry)
- `/tmp/v32-249-rngLockedFlag-writers.txt` — at least 3 writers: L1607 (T-write inside _finalizeRngRequest), L1663 (F-write inside updateVrfCoordinatorAndSub), L1704 (F-write inside _unlockRng); plus reads at L173 (turbo guard third conjunct), L1049 (requestLootboxRng guard), L1728 (rawFulfillRandomWords guard) — but reads do NOT appear in the writer-grep
- `/tmp/v32-249-jackpotPhaseFlag-writers.txt` — at least 2 writers: L335 (F-write inside the gameover phase exit) + L442 (T-write at phase transition); paired with L444 lastPurchaseDay=false at the same boundary (load-bearing for INV-PLV-B-01)
- `/tmp/v32-249-level-writers.txt` — 1 storage-line at L250 (init `uint24 public level = 0;`) + 1 runtime writer at L1609 (`level += 1` inside _finalizeRngRequest); INV-PLV-C-01 anchor

If any grep returns counts DIFFERENT from the above expectations, append additional rows to PLV-01 / PLV-02 sections covering the unexpected sites; do NOT silently omit. Per D-249-CF-08, gaps vs Phase 247 catalog get recorded in SUMMARY.md.

**Step C — Create `audit/v32-249-PLV.md` with the 7-section skeleton (Sections 0 + 1 + 2 populated by Task 1; Sections 3-6 + Phase 251 hand-off as RESERVED stubs for Tasks 2-3; final assembly + READ-only flip in Task 4):**

Use the literal headers below in this exact order. Replace every `<...>` placeholder with concrete data derived from Step B greps + line-by-line code reads — no placeholder tokens may remain in the committed file.

```markdown
---
status: IN_PROGRESS — Plan 249-01 single-plan multi-task
phase: 249 — purchaseLevel Correctness Proof
milestone: v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
head: acd88512
plan: 249-01
deliverable: audit/v32-249-PLV.md
---

# v32.0 Phase 249 — purchaseLevel Correctness Proof

**Audit head:** `acd88512` (Phase 247 / 248 anchor; both WIP guards already committed inside this SHA — turbo guard L173 `!rngLockedFlag` conjunct + backfill guard L1174 `rngWordByDay[idx + 1] == 0` conjunct).
**Phase:** 249 — purchaseLevel Correctness Proof (PLV-01..06).
**Scope:** Pure-proof phase per CONTEXT.md D-249-CF-04 / D-249-CF-05; zero `contracts/` or `test/` writes. Phase 247 catalog (`audit/v32-247-DELTA-SURFACE.md` Section 6 D-247-I007..I012) is the SOLE scope input per D-249-CF-01.
**Status:** IN PROGRESS — Plan 249-01 Task 1 populates Sections 1 (PLV-01) + 2 (PLV-02). Sections 3-6 + Phase 251 hand-off appendix are RESERVED stubs for Tasks 2-3 of the same plan. Task 4 flips this file to FINAL READ-only.

## Section 0 — Overview & Verdict / Octant / Invariant Legends

### 0.1 Overview

This file is the authoritative v32.0 Phase 249 deliverable proving the local-stack variable `purchaseLevel` (bound at `contracts/modules/DegenerusGameAdvanceModule.sol:185` via `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1`) can never evaluate to 0 — and therefore the panic 0x11 underflow at `levelPrizePool[uint24(purchaseLevel) - 1]` (AdvanceModule:397 primary inside the daily-jackpot region L370-407 + AdvanceModule:752 secondary inside `_distributeYieldSurplus`) cannot fire — at any reachable `(lastPurchaseDay, rngLockedFlag, jackpotPhaseFlag, level)` combination once the WIP `!rngLockedFlag` turbo guard at AdvanceModule:173 is in place. Plus underflow / overflow / out-of-bounds audit at every `purchaseLevel`-arithmetic call site (AdvanceModule + cross-module re-derivations across MintModule / WhaleModule / LootboxModule / BurnieCoinflip per CONTEXT.md D-249-01 wider scope). Plus symbolic reproduction of the testnet panic 0x11 trigger sequence at blocks 10759449 + 10761786 (CONTEXT.md D-249-CF-10). Plus daily-jackpot region L370-407 no-strand proof emitting a Phase 252 POST31-02 composition hand-off row.

### 0.2 Verdict Bucket Legend (per CONTEXT.md D-249-CF-06)

| Verdict | Meaning |
|---|---|
| `SAFE` | Site / cell / arithmetic path proven safe under the L173 turbo guard with explicit evidence cite. Default expectation for PLV-01 / PLV-02 / PLV-04 / PLV-06 rows. |
| `EXCEPTION` | Site is governed by a pre-locked KNOWN-ISSUES.md envelope (e.g., EXC-04 EntropyLib XOR-shift-related rows in LootboxModule). Acceptance NOT re-litigated. |
| `FINDING_CANDIDATE` | Site has a reachability gap, missing guard, or potential underflow. Routes to Phase 253 FIND-01..04 emission per D-249-CF-03 — NO `F-32-NN` ID assigned in this phase. Most likely candidate per CONTEXT.md `<specifics>` first bullet: MintModule:923 row if `(cachedJpFlag = T ∧ cachedLevel = 0)` cell is reachable. |

### 0.3 Octant Legend (per CONTEXT.md D-249-04)

8 octants encode the boolean triple `(lastPurchaseDay, rngLockedFlag, jackpotPhaseFlag)` ordered FFF → TTT:

| Octant ID | (lastPurchase, rngLocked, jackpot) | Reachability Class |
|---|---|---|
| `O-PLV-FFF` | (F, F, F) | REACHABLE — pre-target play with no VRF in flight |
| `O-PLV-FFT` | (F, F, T) | REACHABLE — jackpot phase active, no VRF in flight |
| `O-PLV-FTF` | (F, T, F) | REACHABLE — VRF in flight, pre-jackpot |
| `O-PLV-FTT` | (F, T, T) | REACHABLE — VRF in flight during jackpot phase |
| `O-PLV-TFF` | (T, F, F) | REACHABLE — pre-target last-purchase day, VRF cleared |
| `O-PLV-TFT` | (T, F, T) | UNREACHABLE — INV-PLV-B-01 (jackpot=T ⇒ lastPurchase=F at write boundary) |
| `O-PLV-TTF` | (T, T, F) | UNREACHABLE for lvl=0 cell — INV-PLV-A-01 (turbo guard short-circuits before lvl=0 binds with lastPurchase=T ∧ rngLocked=T) — **THE LOAD-BEARING UNREACHABLE OCTANT FOR PLV-03** |
| `O-PLV-TTT` | (T, T, T) | UNREACHABLE — INV-PLV-B-01 (jackpot=T ⇒ lastPurchase=F) |

Each octant has 3 level-bin rows: `lvl = 0`, `1 ≤ lvl < levelMax`, `lvl = levelMax`. Total 24 cells.

### 0.4 Named-Invariant Legend (per CONTEXT.md `<specifics>` per-axis split A/B/C)

| Invariant ID | Statement | Anchor Cite | Load-Bearing For |
|---|---|---|---|
| `INV-PLV-A-01` | lastPurchaseDay = true ⇒ rngLockedFlag = false at every reachable advanceGame entry boundary | AdvanceModule:173 turbo guard third conjunct `!rngLockedFlag`; AdvanceModule:1607-1609 _finalizeRngRequest sets rngLockedFlag=true AFTER level pre-increment (so when rngLockedFlag becomes true, level was already incremented to lvl+1, breaking the lvl=0 ∧ lastPurchase=T co-occurrence); AdvanceModule:1703-1704 _unlockRng clears rngLockedFlag=false when dailyIdx advances | O-PLV-TTF lvl=0 cell UNREACHABLE-disproof (PLV-02 + PLV-03) |
| `INV-PLV-B-01` | jackpotPhaseFlag = true ⇒ lastPurchaseDay = false at write boundary | AdvanceModule:442-444 `jackpotPhaseFlag = true; lastPurchaseDay = false;` co-write at phase transition; v25/v29/v30/v31 audits cited this invariant | O-PLV-TFT + O-PLV-TTT cells UNREACHABLE-disproof |
| `INV-PLV-C-01` | level only advances inside _finalizeRngRequest at AdvanceModule:1609 (sole runtime writer; pre-increment when lastPurchaseDay=true at RNG request time) | Sole-writer grep `git grep -nE '\blevel\s*=\|\blevel\s*+=' acd88512 -- 'contracts/storage/*.sol' 'contracts/modules/*.sol'` returns L250 (init `uint24 public level = 0;`) + L1609 (`level += 1` inside _finalizeRngRequest) — exactly 2 hits | PLV-02 lvl=0 boundary cell reachability classification |

### 0.5 Row ID Legend

Per-REQ rows: `PLV-NN-VMM` (REQ-anchored, monotonic-within-REQ). Examples: `PLV-01-V01`, `PLV-01-V02`, ..., `PLV-02-V01` (encoded as `O-PLV-FFF-lvl0`-style row IDs in §2 — see §2 column shape).

Column shape per CONTEXT.md D-249-03 (PLV-01) / D-249-04 (PLV-02): `Row ID | Site (file:line) | Function | Read kind | Invariant required | Verdict | Evidence Cite` (PLV-01); `Octant ID | Level bin | purchaseLevel formula at L185 | Reachability | Reachability-disproof cite (if UNREACHABLE) | purchaseLevel ≥ 1 verdict | Evidence` (PLV-02).

PLV-04 / PLV-05 / PLV-06 use slightly varied column shapes per D-249-07 / D-249-10 / D-249-12 (documented inline at each per-REQ section header in Tasks 2-3).

## Section 1 — PLV-01: cross-module purchaseLevel readsite enumeration

Per CONTEXT.md D-249-01 (wider scope: AdvanceModule + cross-module re-derivations) + D-249-02 (full per-row proof depth, no Phase 250 deferral) + D-249-03 (7-col shape + invariant tag).

**Universe-of-readsites attestation (per D-249-CF-09 grep-reproducibility):**

```
git grep -n 'purchaseLevel' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' 'contracts/storage/*.sol'
```

This grep returns NN hits across MM files at HEAD `acd88512` (NN to be filled with literal count from `wc -l /tmp/v32-249-purchaseLevel-all.txt`; MM is the unique-file count). Rows below cover every hit; gaps recorded as `Finding Candidates` per D-249-CF-03.

Column shape per D-249-03: `Row ID | Site (file:line) | Function | Read kind {bind/read/arith-arg/parameter/packed-decode} | Invariant required {≥1, =1, >level, level+1, packed-on-write, OOS-by-construction} | Verdict | Evidence Cite (incl. grep recipe + cross-module upstream binder cite)`.

### 1.1 AdvanceModule readsite catalog (~30+ rows)

| Row ID | Site (file:line) | Function | Read kind | Invariant required | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|
| PLV-01-V01 | AdvanceModule:185 | advanceGame | bind (ternary result) | ≥1 | SAFE | bind expression `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1`; SAFE under L173 turbo guard per PLV-02 + PLV-03; load-bearing for every downstream readsite |
| PLV-01-V02 | AdvanceModule:204 | advanceGame | arith-arg | ≥1 (passed to _enforceDailyMintGate) | SAFE | param-pass-through; downstream consumes via daily-mint-gate logic (no -1 arithmetic); cross-cite: caller at L185 binds ≥1 |
| PLV-01-V03 | AdvanceModule:218 | advanceGame (jackpot branch) | arith-arg | ≥1 (passed to _tqReadKey) | SAFE | param-pass-through; _tqReadKey tolerates any uint24 input ≥0 (it's a bit-pack key); no -1 arithmetic in caller |
| PLV-01-V04 | AdvanceModule:223 | advanceGame (jackpot branch) | arith-arg | ≥1 (passed to _runProcessTicketBatch) | SAFE | param-pass-through; cross-cite L185 |
| PLV-01-V05 | AdvanceModule:266 | advanceGame (purchase branch) | arith-arg | ≥1 (passed to _tqReadKey) | SAFE | param-pass-through; same rationale as V03 |
| PLV-01-V06 | AdvanceModule:280 | advanceGame (purchase branch) | arith-arg | ≥1 (passed to _runProcessTicketBatch) | SAFE | param-pass-through; cross-cite L185 |
| PLV-01-V07 | AdvanceModule:295 | advanceGame | arith-arg | ≥1 | SAFE | param-pass-through |
| PLV-01-V08 | AdvanceModule:301 | advanceGame | arith-arg | ≥1 (passed to _swapAndFreeze) | SAFE | param-pass-through |
| PLV-01-V09 | AdvanceModule:312 | advanceGame | arith (+4) | ≥1; ffLevel = purchaseLevel + 4 ≤ uint24.max | SAFE | overflow only at purchaseLevel = uint24.max - 4; OOS-by-construction per D-249-06 (level cap is v25/v26-era game mechanic; cite at PLV-04 overflow-edge sub-section) |
| PLV-01-V10 | AdvanceModule:316 | advanceGame | arith-arg | ≥1 (passed to _processPhaseTransition) | SAFE | param-pass-through |
| PLV-01-V11 | AdvanceModule:347 | advanceGame | conditional read | ≥1 OR `lvl` (jackpot vs purchase select) | SAFE | ternary `inJackpot ? lvl : purchaseLevel`; both branches ≥0 by storage type; ≥1 only for purchase branch (per L185); jackpot branch reads `lvl` (≥0 by INV-PLV-C-01 — `lvl` can equal 0 only on bootstrap) |
| PLV-01-V12 | AdvanceModule:360 | advanceGame | conditional read | ≥1 OR `lvl` | SAFE | same as V11 |
| PLV-01-V13 | AdvanceModule:372 | advanceGame (daily-jackpot region) | comparison (`== 1`) | =1 (special-case path for level-1 daily) | SAFE | comparison only; no arith; selects between V14 vs V16 paths |
| PLV-01-V14 | AdvanceModule:385 | advanceGame (daily-jackpot region) | arith-arg | ≥2 (purchaseLevel ≥ 2 branch only) | SAFE | param-pass-through to payDailyJackpot; in this branch purchaseLevel ≥ 2 by V13 conditional |
| PLV-01-V15 | AdvanceModule:387 | advanceGame (daily-jackpot region) | arith-arg | ≥2 | SAFE | param-pass-through |
| PLV-01-V16 | AdvanceModule:389 | advanceGame (daily-jackpot region) | arith (+1) | ≥2; purchaseLevel + 1 ≤ uint24.max | SAFE | overflow only at purchaseLevel = uint24.max; OOS-by-construction per D-249-06 |
| PLV-01-V17 | AdvanceModule:390 | advanceGame (daily-jackpot region) | arith (+4) | ≥2; purchaseLevel + 4 ≤ uint24.max | SAFE | same |
| PLV-01-V18 | AdvanceModule:397 | advanceGame (daily-jackpot region) | arith (-1) + array-index | ≥1 AND ≤ levelPrizePool.length-1 | SAFE | THE LITERAL v32.0 PANIC 0x11 PRIMARY TRIGGER; cross-cite PLV-02 (≥1) + PLV-03 (TTF lvl=0 unreachable) + PLV-05 (post-fix walk); array bound cited via D-249-06 level cap |
| PLV-01-V19 | AdvanceModule:411 | advanceGame | arith (+1) | ≥1; nextLevel = purchaseLevel + 1 ≤ uint24.max | SAFE | overflow OOS-by-construction |
| PLV-01-V20 | AdvanceModule:424 | advanceGame | array-index (write) | ≤ levelPrizePool.length-1 | SAFE | write-side index; cited via level cap |
| PLV-01-V21 | AdvanceModule:428 | advanceGame | arith-arg | ≥1 (passed to _consolidatePoolsAndRewardJackpots) | SAFE | param-pass-through |
| PLV-01-V22 | AdvanceModule:734 | _distributeYieldSurplus | parameter | ≥1 (caller passes purchaseLevel from L185) | SAFE | upstream binder cite: caller at advanceGame L297-303 region passes the L185-bound purchaseLevel |
| PLV-01-V23 | AdvanceModule:749 | _distributeYieldSurplus | arith-arg | ≥1 (passed to _nextToFutureBps) | SAFE | param-pass-through |
| PLV-01-V24 | AdvanceModule:750 | _distributeYieldSurplus | arith (% 10 == 9) | ≥1; modulo always defined for uint24 | SAFE | modulo by 10; well-defined for any uint24; comparison `== 9` is a feature gate |
| PLV-01-V25 | AdvanceModule:752 | _distributeYieldSurplus | arith (-1) + array-index | ≥1 AND ≤ levelPrizePool.length-1 | SAFE | v32.0 PANIC 0x11 SECONDARY TRIGGER; cross-cite PLV-02 + PLV-03 + PLV-05 (mirrors V18) |
| PLV-01-V26 | AdvanceModule:1097 | (unnamed internal helper) | local derivation | ≥1; purchaseLevel_ = level + 1 (NOT the L185 binder; an independent derivation) | SAFE | `level + 1 ≥ 1` for any uint24 level (always true since level ≥ 0); overflow only at level = uint24.max OOS-by-construction; this is NOT the L185 binder ternary — it lacks the `(lastPurchase && rngLockedFlag)` short-circuit but doesn't need it because it doesn't subtract 1 anywhere downstream of this binding |
| PLV-01-V27 | AdvanceModule:1098 | (unnamed internal helper) | arith-arg | ≥1 | SAFE | _tqWriteKey accepts any uint24 |
| PLV-01-V28 | AdvanceModule:1100 | (unnamed internal helper) | arith-arg | ≥1 | SAFE | _swapTicketSlot accepts any uint24 |
| PLV-01-V29 | AdvanceModule:1504 | _processPhaseTransition | parameter | ≥1 (caller passes purchaseLevel from L185 via L316) | SAFE | upstream binder cite: caller L316 |
| PLV-01-V30 | AdvanceModule:1507 | _processPhaseTransition | arith (+99) | ≥1; targetLevel = purchaseLevel + 99 ≤ uint24.max | SAFE | overflow only at purchaseLevel = uint24.max - 99; OOS-by-construction |

[Re-derive every row by reading `/tmp/v32-249-AdvanceModule-acd88512.sol` line-by-line against `/tmp/v32-249-purchaseLevel-all.txt`. Confirm ≥30 rows above cover every hit in the AdvanceModule subset of the universe grep. If any hit is missed, append additional rows. If any row's verdict cannot be SAFE, mark FINDING_CANDIDATE and add to §7 Finding Candidates.]

### 1.2 Cross-module re-derivation rows (D-249-01 wider scope; ~40+ rows)

Per CONTEXT.md D-249-01: this subsection catalogs every INDEPENDENT re-derivation of `purchaseLevel` outside the L185-binder chain. These are not pass-throughs of the AdvanceModule:185 value — they are local rebindings that may or may not have the `!rngLockedFlag` guard equivalent.

| Row ID | Site (file:line) | Function | Read kind | Invariant required | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|
| PLV-01-V31 | MintModule:923 | _mintInternal (or named function) | bind (sibling ternary) | ≥1 | **FINDING_CANDIDATE-or-SAFE** (depends on reachability of `(cachedJpFlag = T ∧ cachedLevel = 0)`) | THE LIVE FINDING_CANDIDATE per CONTEXT.md `<specifics>` first bullet; ternary `cachedJpFlag ? cachedLevel : cachedLevel + 1` mirrors L185 shape WITHOUT `!rngLockedFlag` guard; reachability proof MUST cite either (a) INV-PLV-C-01 lvl=0 boundary holding only before bootstrap (cachedLevel = level via L922 read) AND INV-PLV-B-01 forcing cachedJpFlag co-occurrence with cachedLevel=0 to be unreachable, OR (b) document the reachable cell and route to Phase 250 SIB-01 + Phase 253 FIND-01 |
| PLV-01-V32 | MintModule:924 | (same function as V31) | arith-arg (PriceLookupLib.priceForLevel) | ≥1; PriceLookupLib internal arithmetic safe at purchaseLevel ≥ 1 | SAFE-pending-V31 | param-pass-through; PriceLookupLib boundary record per Claude's-Discretion Item 6 in CONTEXT.md `<decisions>` (D-248-10 analog) — recommended yes, trace into PriceLookupLib.priceForLevel for internal-arithmetic coverage; if PriceLookupLib has a 0-indexed lookup table, purchaseLevel = 0 upstream binding could underflow inside the lib; behavioral cite: "returns price in wei for the given level; indexed lookup or computed; safe for purchaseLevel ≥ 1" |
| PLV-01-V33 | LootboxModule:532 | openLootBox | packed-decode | packed-on-write (≥1 at WhaleModule:876 packing time) | SAFE | `uint24(packed >> 232)` returns the writer's purchaseLevel at write-time; trace to packing site WhaleModule:876 `lootboxEth[index][buyer] = (uint256(purchaseLevel) << 232) \| newAmount`; load-bearing invariant: writer's purchaseLevel ≥ 1 — proven by V34 |
| PLV-01-V34 | WhaleModule:876 | _recordLootboxEntry | packing-write | ≥1 (writer's purchaseLevel parameter) | SAFE | param at L841; caller of _recordLootboxEntry passes purchaseLevel from MintModule:923 binding (V31); transitively SAFE-pending-V31 |
| PLV-01-V35 | WhaleModule:841 | _recordLootboxEntry | parameter | ≥1 (passed by caller) | SAFE-pending-V31 | param-pass-through; same upstream binder cite as V34 |
| PLV-01-V36 | LootboxModule:552 | openLootBox | conditional read (passthrough) | =1 (passthrough on `withinGracePeriod` false branch) | SAFE | `withinGracePeriod ? graceLevel : purchaseLevel` — passthrough preserves writer's purchaseLevel from V33 |
| PLV-01-V37 | BurnieCoinflip:578 | (queue-flip path) | parameter | ≥1 (passed by caller) | SAFE | param-pass-through; caller is degenerusGame.purchaseInfo() which returns level state; trace to caller's binding |
| PLV-01-V38 | BurnieCoinflip:590 | (queue-flip path) | arith (% 10 == 0) | ≥1; modulo well-defined | SAFE | modulo by 10; well-defined; comparison `== 0` is a per-decade gate; safe even if purchaseLevel = 0 (modulo would just match) — but cross-cite V37 confirms ≥1 |
| PLV-01-V39 | BurnieCoinflip:596 | (queue-flip path) | storage write (`bafLevel = purchaseLevel_`) | ≥1 (storage value mirrors caller's purchaseLevel) | SAFE | storage write at L596 records writer's purchaseLevel; downstream reads of `bafLevel` consume this value — invariant inherited |
| PLV-01-V40 | BurnieCoinflip:1035 | (locked-condition path) | parameter | ≥1 | SAFE | param-pass-through (mirrors V37) |
| PLV-01-V41 | BurnieCoinflip:1041 | (locked-condition path) | arith (% 10 == 0) | ≥1 | SAFE | modulo gate (mirrors V38) |

[Re-derive every row by reading `/tmp/v32-249-MintModule-acd88512.sol` + `/tmp/v32-249-WhaleModule-acd88512.sol` + `/tmp/v32-249-LootboxModule-acd88512.sol` + `/tmp/v32-249-BurnieCoinflip-acd88512.sol` against `/tmp/v32-249-purchaseLevel-all.txt`. The 11 rows above are a MINIMUM — additional cross-module sites (e.g., DegenerusGameJackpotModule, DegenerusGameDecimatorModule, DegenerusGameDegenetteModule, DegenerusGameBoonModule, DegenerusGameGameOverModule, DegenerusGameStorage initialization rows if any) may surface in the universe grep and MUST be appended. Target: every hit in the cross-module subset of the universe grep covered.]

### 1.3 PLV-01 Finding Candidates (omit subsection if zero — but PLV-01-V31 is HIGHLY LIKELY to land here)

Per CONTEXT.md D-249-CF-03: any PLV-01 row classified `FINDING_CANDIDATE` is enumerated here with path:line + suggested severity for Phase 253 routing. NO `F-32-NN` IDs assigned.

If PLV-01-V31 (MintModule:923) reachability proof shows `(cachedJpFlag = T ∧ cachedLevel = 0)` is REACHABLE:
- `MintModule:923 — sibling ternary `cachedJpFlag ? cachedLevel : cachedLevel + 1` with no `!rngLockedFlag` guard equivalent; `(cachedJpFlag=T ∧ cachedLevel=0)` reachable at <cite specific reachable scenario>; mirrors v32.0 testnet panic 0x11 shape but on the mint path; routes to Phase 250 SIB-01 + Phase 253 FIND-01 — suggested severity: HIGH (mirrors the L185 panic class on a different code path)`

If PLV-01-V31 reachability proof shows the cell is UNREACHABLE (the cleaner outcome):
- OMIT this candidate; flip V31 verdict to SAFE with the disproof cite inline.

[Enumerate any additional candidates here. If zero, OMIT the entire `### 1.3` subsection.]

## Section 2 — PLV-02: 4-D state-space sweep (8 octants × 3 level bins = 24 cells)

Per CONTEXT.md D-249-04 (8 octants × 3 level bins encoding) + D-249-05 (inline path:line + named-invariant disproof) + D-249-06 (level=uint24.max overflow OOS-by-construction).

**Sole-writer attestation (per D-249-CF-09 grep-reproducibility) — reproduces the named-invariant disproofs:**

```
git grep -nE '\blastPurchaseDay\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'
git grep -nE '\brngLockedFlag\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'
git grep -nE '\bjackpotPhaseFlag\s*=' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'
git grep -nE '\blevel\s*=|\blevel\s*\+=|\blevel\s*\+\+|\+\+level' acd88512 -- 'contracts/storage/*.sol' 'contracts/modules/*.sol' 'contracts/*.sol'
```

Locked output expectations: lastPurchaseDay writers = {L178 T-write inside L173-guarded block, L399 T-write inside daily-jackpot region, L444 F-write at jackpot-phase entry}; rngLockedFlag writers = {L1607 T-write inside _finalizeRngRequest, L1663 F-write inside updateVrfCoordinatorAndSub, L1704 F-write inside _unlockRng}; jackpotPhaseFlag writers = {L335 F-write at gameover phase exit, L442 T-write at phase transition}; level writers = {L250 init, L1609 `level += 1` inside _finalizeRngRequest}. If any grep returns DIFFERENT writers, add the unexpected sites to the named-invariant disproof cites and re-validate the corresponding cell verdicts.

Column shape per D-249-04: `Octant ID | Level bin | purchaseLevel formula at L185 | Reachability {REACHABLE, UNREACHABLE} | Reachability-disproof cite (if UNREACHABLE) | purchaseLevel ≥ 1 verdict | Evidence`.

### 2.1 The 24-cell sweep (8 octants × 3 level bins)

| Octant ID | Level bin | purchaseLevel formula at L185 | Reachability | Reachability-disproof cite (if UNREACHABLE) | purchaseLevel ≥ 1 verdict | Evidence |
|---|---|---|---|---|---|---|
| O-PLV-FFF | lvl=0 | `lvl + 1 = 0 + 1 = 1` | REACHABLE (bootstrap state per INV-PLV-C-01) | n/a | SAFE (= 1) | lastPurchase = (!inJackpot) && lastPurchaseDay = (T) && F = F → ternary picks `lvl + 1` branch → 0+1 = 1 |
| O-PLV-FFF | 1≤lvl<max | `lvl + 1 ∈ [2, max]` | REACHABLE | n/a | SAFE (≥ 2) | same ternary branch; arithmetic well-defined for lvl < uint24.max |
| O-PLV-FFF | lvl=max | `lvl + 1 overflows` (OOS-by-construction per D-249-06) | OOS-by-construction | level cap is v25/v26-era game mechanic (gameOverPossible / target-met termination); cite at PLV-04 overflow-edge sub-section | OOS | n/a |
| O-PLV-FFT | lvl=0 | `lvl + 1 = 1` | REACHABLE (entering jackpot phase from bootstrap is theoretically possible but practically gated by gameplay; classified REACHABLE for full-coverage) | n/a | SAFE (= 1) | same `lvl + 1` branch (lastPurchase=F) |
| O-PLV-FFT | 1≤lvl<max | `lvl + 1 ∈ [2, max]` | REACHABLE | n/a | SAFE (≥ 2) | same |
| O-PLV-FFT | lvl=max | OOS-by-construction | OOS | same as FFF/lvl=max | OOS | n/a |
| O-PLV-FTF | lvl=0 | `lvl + 1 = 1` | REACHABLE (RNG in flight on bootstrap day; ternary still picks `lvl + 1` because lastPurchase=F) | n/a | SAFE (= 1) | lastPurchase = F → `lvl + 1` branch |
| O-PLV-FTF | 1≤lvl<max | `lvl + 1 ∈ [2, max]` | REACHABLE | n/a | SAFE (≥ 2) | same |
| O-PLV-FTF | lvl=max | OOS-by-construction | OOS | same | OOS | n/a |
| O-PLV-FTT | lvl=0 | `lvl + 1 = 1` | REACHABLE | n/a | SAFE (= 1) | same |
| O-PLV-FTT | 1≤lvl<max | `lvl + 1 ∈ [2, max]` | REACHABLE | n/a | SAFE (≥ 2) | same |
| O-PLV-FTT | lvl=max | OOS-by-construction | OOS | same | OOS | n/a |
| O-PLV-TFF | lvl=0 | `lvl + 1 = 1` (lastPurchase=T but rngLocked=F → ternary picks `lvl + 1` branch) | REACHABLE | n/a | SAFE (= 1) | ternary: (T && F) ? lvl : lvl+1 → F → `lvl + 1` |
| O-PLV-TFF | 1≤lvl<max | `lvl + 1 ∈ [2, max]` | REACHABLE | n/a | SAFE (≥ 2) | same |
| O-PLV-TFF | lvl=max | OOS-by-construction | OOS | same | OOS | n/a |
| O-PLV-TFT | lvl=0 | `lvl + 1 = 1` (lastPurchase=T, rngLocked=F, jackpot=T — but INV-PLV-B-01 says jackpot=T ⇒ lastPurchase=F; co-occurrence UNREACHABLE) | UNREACHABLE | INV-PLV-B-01: AdvanceModule:442-444 co-write `jackpotPhaseFlag = true; lastPurchaseDay = false;` at phase-transition boundary; lastPurchase=T ∧ jackpot=T cannot co-occur at any reachable advanceGame entry | UNREACHABLE → vacuously SAFE | grep `git grep -nE '\bjackpotPhaseFlag\s*=' acd88512 -- 'contracts/modules/*.sol'` returns L335 + L442; L442 is the only T-write and L444 immediately writes lastPurchaseDay=false |
| O-PLV-TFT | 1≤lvl<max | n/a | UNREACHABLE | same INV-PLV-B-01 | UNREACHABLE → vacuously SAFE | same |
| O-PLV-TFT | lvl=max | n/a | UNREACHABLE | same | UNREACHABLE → vacuously SAFE | same |
| O-PLV-TTF | **lvl=0** | **`lvl = 0` (lastPurchase=T ∧ rngLocked=T ⇒ ternary picks `lvl` branch → 0)** | **UNREACHABLE — THE LOAD-BEARING CELL FOR PLV-03** | **INV-PLV-A-01: AdvanceModule:173 turbo guard third conjunct `!rngLockedFlag` short-circuits; turbo block at L167-182 is the SOLE writer that sets lastPurchaseDay=true at L178 inside an unguarded turbo path; L173 conjunct prevents that write from firing while rngLocked=T; ALSO _finalizeRngRequest at L1607-1609 sets rngLockedFlag=true AFTER level pre-increment at L1609, so when rngLocked becomes T, level was already incremented to lvl≥1 (lastPurchaseDay was already true at request-time → pre-increment fired → lvl≥1 sticks)** | **UNREACHABLE → vacuously SAFE; PLV-03 closes the formal proof** | **double cite: turbo guard L173 + finalizeRngRequest sequence L1607-1609; both prevent (lastPurchase=T ∧ rngLocked=T ∧ lvl=0) at any reachable advanceGame entry** |
| O-PLV-TTF | 1≤lvl<max | `lvl ∈ [1, max-1]` (lastPurchase=T ∧ rngLocked=T ⇒ `lvl` branch) | REACHABLE (in-flight VRF after pre-increment fired) | n/a | SAFE (≥ 1) | level pre-increment per INV-PLV-C-01 fired before rngLocked=T became true (L1609 strictly precedes L1607 in _finalizeRngRequest call sequence — L1609 is `level += 1`, L1607 is `rngLockedFlag = true`); so when rngLocked=T is observed at advanceGame entry, lvl is the post-increment value ≥ 1 |
| O-PLV-TTF | lvl=max | n/a | OOS-by-construction (level cap) | same as FFF/lvl=max | OOS | n/a |
| O-PLV-TTT | lvl=0 | n/a | UNREACHABLE | INV-PLV-B-01 (jackpot=T ⇒ lastPurchase=F; same as TFT cells) | UNREACHABLE → vacuously SAFE | same |
| O-PLV-TTT | 1≤lvl<max | n/a | UNREACHABLE | same | UNREACHABLE → vacuously SAFE | same |
| O-PLV-TTT | lvl=max | n/a | UNREACHABLE | same | UNREACHABLE → vacuously SAFE | same |

### 2.2 Coverage attestation

**REACHABLE cells:** 13 (FFF: 2 in-bound + 1 OOS = 3; FFT: 3; FTF: 3; FTT: 3; TFF: 3; TTF: 1 in-bound + 1 OOS = 2 of which 1 SAFE, plus 1 UNREACHABLE-load-bearing-lvl=0 cell). Each REACHABLE cell evaluates `purchaseLevel ≥ 1` SAFE.
**UNREACHABLE cells:** 8 (TFT: 3 — INV-PLV-B-01; TTF: 1 — INV-PLV-A-01 the load-bearing cell; TTT: 3 — INV-PLV-B-01). Each UNREACHABLE cell carries an inline path:line cite + named-invariant ID + grep recipe in the Evidence column.
**OOS-by-construction cells:** 8 (every `lvl = uint24.max` cell across all 8 octants — D-249-06 single one-line cite at PLV-04 overflow-edge sub-section; not in the 24-cell main count adjacent above — the 13+8 = 21 + 3 OOS cells in REACHABLE octants + 5 OOS cells in UNREACHABLE octants is double-counting; actual partition: 24 = 13 REACHABLE + 8 UNREACHABLE-by-named-invariant + 3 OOS-by-construction-in-otherwise-REACHABLE-octants — per D-249-06 the 5 OOS cells in UNREACHABLE octants are subsumed by the named-invariant disproof, which dominates).

[Re-derive the 24 rows by reading `/tmp/v32-249-AdvanceModule-acd88512.sol` lines 160-185 + 167-178 (turbo block T-write) + 442-444 (jackpot-phase T-write) + 1607-1609 (level + rngLockedFlag co-write sequence) + 1702-1704 (_unlockRng F-write) line-by-line. Verify every UNREACHABLE cell's named-invariant cite holds against the actual grep output. If any cell verdict is incorrect, fix and re-validate downstream PLV-03 / PLV-04 / PLV-05 dependencies.]

### 2.3 PLV-02 Finding Candidates (omit subsection if zero — default expected zero)

[If zero, OMIT this subsection. Otherwise enumerate as bullets per §1.3 format.]

## Section 3 — PLV-03: ternary unreachable-state proof for O-PLV-TTF lvl=0 cell (RESERVED FOR TASK 2)

This section is reserved for Task 2 of this plan to populate the formal proof that the L185 ternary cannot return 0 once the L173 turbo guard is in place. DO NOT edit this section in Task 1 — Task 2 replaces this stub in place.

## Section 4 — PLV-04: arithmetic-site flat table + array-index oob both bounds (RESERVED FOR TASK 2)

This section is reserved for Task 2 of this plan to populate the per-call-site arithmetic audit per CONTEXT.md D-249-07 / D-249-08 / D-249-09. DO NOT edit this section in Task 1 — Task 2 replaces this stub in place.

## Section 5 — PLV-05: testnet panic 0x11 reproduction (BFL-03-style state-transition walk) (RESERVED FOR TASK 3)

This section is reserved for Task 3 of this plan to populate the worked numeric walk seeded by testnet blocks 10759449 + 10761786 (CONTEXT.md D-249-CF-10 / D-249-11). DO NOT edit this section in Tasks 1-2 — Task 3 replaces this stub in place.

## Section 6 — PLV-06: daily-jackpot region L370-407 no-strand proof (RESERVED FOR TASK 3)

This section is reserved for Task 3 of this plan to populate the per-branch invariant table + strand-disproof attestation + Phase 252 POST31-02 composition hand-off row per CONTEXT.md D-249-12 / D-249-13. DO NOT edit this section in Tasks 1-2 — Task 3 replaces this stub in place.

## Section 7 — Finding Candidates + Phase 251 TST-01/02/03 Hand-Off Appendix (RESERVED FOR TASK 4)

This section is reserved for Task 4 of this plan to assemble the cross-section Finding Candidates summary (consolidating §1.3 / §2.3 / §3.x / §4.x / §5.x / §6.x candidates) and the Phase 251 hand-off appendix per CONTEXT.md `<specifics>` Phase 251 hand-off block format. DO NOT edit this section in Tasks 1-3 — Task 4 replaces this stub in place + flips file to FINAL READ-only.
```

**Step D — Verify Task 1 deliverable:**

```
test -f audit/v32-249-PLV.md
grep -q '^## Section 1 — PLV-01' audit/v32-249-PLV.md
grep -q '^## Section 2 — PLV-02' audit/v32-249-PLV.md
grep -q '^| PLV-01-V01 ' audit/v32-249-PLV.md
grep -q '^| PLV-01-V18 ' audit/v32-249-PLV.md     # AdvanceModule:397 PRIMARY underflow row
grep -q '^| PLV-01-V25 ' audit/v32-249-PLV.md     # AdvanceModule:752 SECONDARY underflow row
grep -q '^| PLV-01-V31 ' audit/v32-249-PLV.md     # MintModule:923 LIVE FINDING_CANDIDATE seed
grep -q '^| O-PLV-FFF ' audit/v32-249-PLV.md
grep -q '^| O-PLV-TTF ' audit/v32-249-PLV.md
grep -q '^| O-PLV-TTT ' audit/v32-249-PLV.md
grep -q 'INV-PLV-A-01' audit/v32-249-PLV.md
grep -q 'INV-PLV-B-01' audit/v32-249-PLV.md
grep -q 'INV-PLV-C-01' audit/v32-249-PLV.md
grep -q 'D-247-I007' audit/v32-249-PLV.md
grep -q 'D-247-I008' audit/v32-249-PLV.md
grep -q '^## Section 3 — PLV-03' audit/v32-249-PLV.md
grep -q '^## Section 4 — PLV-04' audit/v32-249-PLV.md
grep -q '^## Section 5 — PLV-05' audit/v32-249-PLV.md
grep -q '^## Section 6 — PLV-06' audit/v32-249-PLV.md
grep -q '^## Section 7 — ' audit/v32-249-PLV.md
! grep -q 'F-32-' audit/v32-249-PLV.md
! grep -qE '^<fill from|^REPLACE-WITH|^REPLACE WITH|<cite specific reachable scenario>' audit/v32-249-PLV.md
[ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-01-V')" -ge 30 ]
[ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| O-PLV-')" -ge 24 ]
test -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')"
```

**Step E — Atomic commit per D-247-14 carry-forward:**

```
git add audit/v32-249-PLV.md
git commit -m "$(cat <<'EOF'
audit(249-01): Task 1 — PLV-01 + PLV-02 enumeration at HEAD acd88512

Plan 249-01 Task 1 deliverable. Creates audit/v32-249-PLV.md with
Sections 0-2 populated and Sections 3-7 as RESERVED stubs for Tasks 2-4
of the same plan.

Sections written:
  - Section 0: Overview + Verdict Legend (3-bucket per D-249-CF-06) +
    Octant Legend (8 octants per D-249-04) + Named-Invariant Legend
    (INV-PLV-A-01 / B-01 / C-01 per CONTEXT.md per-axis split) +
    Row ID Legend
  - Section 1: PLV-01 — cross-module purchaseLevel readsite catalog
    (~30+ AdvanceModule rows + ~10+ cross-module re-derivation rows
    per D-249-01 wider scope; column shape per D-249-03; grep-cited
    universe per D-249-CF-09; MintModule:923 row flagged as LIVE
    FINDING_CANDIDATE seed per CONTEXT.md <specifics> first bullet)
  - Section 2: PLV-02 — 4-D state-space sweep encoded as 8 octants ×
    3 level bins (24 cells per D-249-04); every UNREACHABLE cell
    carries inline named-invariant cite (INV-PLV-A-01 / B-01) per
    D-249-05; level=uint24.max OOS-by-construction per D-249-06; the
    load-bearing O-PLV-TTF lvl=0 cell UNREACHABLE-disproof cites both
    L173 turbo guard + L1607-1609 finalizeRngRequest sequence

Phase 247 catalog refs cited: D-247-I007 (PLV-01 row scope) +
D-247-I008 (PLV-02 row scope).

Pure-proof phase per D-249-CF-04 / D-249-CF-05 — zero contracts/ or
test/ writes.

Anchor: HEAD acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-249-PLV.md && grep -q '^## Section 1 — PLV-01' audit/v32-249-PLV.md && grep -q '^## Section 2 — PLV-02' audit/v32-249-PLV.md && grep -q '^| PLV-01-V01 ' audit/v32-249-PLV.md && grep -q '^| PLV-01-V18 ' audit/v32-249-PLV.md && grep -q '^| PLV-01-V25 ' audit/v32-249-PLV.md && grep -q '^| PLV-01-V31 ' audit/v32-249-PLV.md && grep -q '^| O-PLV-FFF ' audit/v32-249-PLV.md && grep -q '^| O-PLV-TTF ' audit/v32-249-PLV.md && grep -q '^| O-PLV-TTT ' audit/v32-249-PLV.md && grep -q 'INV-PLV-A-01' audit/v32-249-PLV.md && grep -q 'INV-PLV-B-01' audit/v32-249-PLV.md && grep -q 'INV-PLV-C-01' audit/v32-249-PLV.md && grep -q 'D-247-I007' audit/v32-249-PLV.md && grep -q 'D-247-I008' audit/v32-249-PLV.md && grep -q '^## Section 3 — PLV-03' audit/v32-249-PLV.md && grep -q '^## Section 4 — PLV-04' audit/v32-249-PLV.md && grep -q '^## Section 5 — PLV-05' audit/v32-249-PLV.md && grep -q '^## Section 6 — PLV-06' audit/v32-249-PLV.md && grep -q '^## Section 7 — ' audit/v32-249-PLV.md && ! grep -q 'F-32-' audit/v32-249-PLV.md && ! grep -qE '^<fill from|^REPLACE-WITH|^REPLACE WITH|<cite specific reachable scenario>' audit/v32-249-PLV.md && [ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-01-V')" -ge 30 ] && [ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| O-PLV-')" -ge 24 ] && [ -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')" ] && git log -1 --format='%s' | grep -qE '^audit\(249-01\): Task 1'</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v32-249-PLV.md` exists at the repo root under `audit/`
    - Frontmatter status is `IN_PROGRESS — Plan 249-01 single-plan multi-task`
    - Section 0 (Overview + Verdict Legend + Octant Legend + Named-Invariant Legend + Row ID Legend) populated with 3-bucket verdict table {SAFE, EXCEPTION, FINDING_CANDIDATE}, 8-octant table FFF..TTT, and 3 named invariants INV-PLV-A-01 / B-01 / C-01 with anchor cites
    - Section 1 (PLV-01) populated with §1.1 AdvanceModule readsite catalog (≥30 PLV-01-VNN rows covering every purchaseLevel hit in AdvanceModule's grep universe — at minimum: L185 binder, L204 enforceDailyMintGate, L218 / L266 _tqReadKey, L223 / L280 _runProcessTicketBatch, L295 / L301 / L312 / L316 / L347 / L360 readsites, L372 / L385 / L387-390 / L397 / L411 / L424 / L428 daily-jackpot region readsites, L734 / L749 / L750 / L752 _distributeYieldSurplus readsites, L1097-1100 internal helper, L1504 / L1507 _processPhaseTransition) + §1.2 cross-module re-derivation rows (≥10 PLV-01-VNN rows covering MintModule:923 + L924, WhaleModule:841 + L876, LootboxModule:532 + L552, BurnieCoinflip:578 + L590 + L596 + L1035 + L1041) + §1.3 Finding Candidates subsection (omitted iff zero — but PLV-01-V31 MintModule:923 is HIGHLY LIKELY to land here per CONTEXT.md `<specifics>`)
    - PLV-01-V18 row exists and identifies AdvanceModule:397 as the LITERAL v32.0 PANIC 0x11 PRIMARY TRIGGER
    - PLV-01-V25 row exists and identifies AdvanceModule:752 as the SECONDARY underflow trigger
    - PLV-01-V31 row exists and identifies MintModule:923 as the LIVE FINDING_CANDIDATE seed (FINDING_CANDIDATE-or-SAFE verdict pending reachability proof)
    - Section 2 (PLV-02) populated with §2.1 24-cell sweep table (8 octants × 3 level bins; every cell carries Reachability + Verdict + Evidence) + §2.2 coverage attestation (count breakdown REACHABLE / UNREACHABLE / OOS) + §2.3 Finding Candidates subsection (omitted iff zero)
    - O-PLV-TTF row at lvl=0 exists and is marked UNREACHABLE with double cite to L173 turbo guard + L1607-1609 finalizeRngRequest sequence
    - Sections 3-7 exist as RESERVED stubs with literal `(RESERVED FOR TASK N)` suffix
    - File contains zero `F-32-` references (D-249-CF-03 enforcement)
    - File contains no `<fill from`, `REPLACE-WITH`, `REPLACE WITH`, or `<cite specific reachable scenario>` placeholder tokens
    - Working tree contains only the 2 pre-existing lines (D-247-03 ContractAddresses.sol + D-247-02 LastPurchaseDayRace.test.js); zero new contracts/ or test/ writes
    - Git log shows 1 new commit with subject matching `audit(249-01): Task 1`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-249-PLV.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
audit/v32-249-PLV.md created with Sections 0 + 1 + 2 fully populated and Sections 3-7 as RESERVED stubs. PLV-01 readsite catalog enumerates ≥40 rows across AdvanceModule + 4 cross-module re-derivation files; the MintModule:923 row carries FINDING_CANDIDATE-or-SAFE verdict pending the (cachedJpFlag=T ∧ cachedLevel=0) reachability proof; AdvanceModule:397 + L752 underflow sites identified verbatim. PLV-02 4-D sweep covers 24 cells with 3 named-invariant disproofs; the load-bearing O-PLV-TTF lvl=0 cell UNREACHABLE-disproof cites both L173 turbo guard + L1607-1609 finalizeRngRequest sequence. One atomic commit landed per D-247-14 carry-forward.
  </deliverable>
  <done>Sections 0-2 populated with concrete PLV-01-VNN + O-PLV-NNN-lvlN rows; Sections 3-7 as RESERVED stubs; zero F-32-NN IDs; zero placeholder tokens; zero contracts/ or test/ writes; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: PLV-03 + PLV-04 — ternary unreachable-state proof for O-PLV-TTF lvl=0 cell + arithmetic-site flat table with -1 cross-cite chain + array-index oob both bounds</name>
  <files>audit/v32-249-PLV.md</files>
  <read_first>
    - audit/v32-249-PLV.md (current state — exists with Sections 0-2 populated by Task 1; Sections 3-4 are RESERVED stubs that Task 2 replaces in place)
    - .planning/phases/249-purchaselevel-correctness-proof/249-CONTEXT.md — specifically D-249-07 (PLV-04 single flat per-call-site table column shape), D-249-08 (-1 rows cross-cite PLV-02 + PLV-03 + PLV-05), D-249-09 (array-index oob both bounds covered), D-249-CF-06 (V-row scheme + 3-bucket verdict), D-249-CF-09 (grep-reproducibility), D-249-04 / D-249-05 (PLV-02 reachability disproof shape — PLV-03 inherits the O-PLV-TTF cell disproof anchor)
    - .planning/REQUIREMENTS.md — PLV-03 + PLV-04 wordings (verbatim)
    - audit/v32-247-DELTA-SURFACE.md — Section 6 D-247-I009 (PLV-03 row scope) + D-247-I010 (PLV-04 row scope)
    - audit/v32-248-BFL.md — §6 BFL-06 cross-cite chain pattern (D-249-08 mirror); §2 BFL-02 sentinel-correctness 4-step proof format (PLV-03 mirror — formal close of an UNREACHABLE state via guard-evaluation walk)

    HEAD-side reads at acd88512 (re-use `/tmp/v32-249-AdvanceModule-acd88512.sol` from Task 1 if extant; otherwise re-extract):
    - L160-185 advanceGame entry through binding ternary (PLV-03 walk anchor; the L173 turbo guard short-circuit proof)
    - L167-182 turbo block body (the SOLE writer that sets lastPurchaseDay=true in advanceGame on a non-jackpot-region path)
    - L1607-1609 _finalizeRngRequest sequence (the `level += 1` at L1609 + `rngLockedFlag = true` at L1607 — note L1607 textually precedes L1609 in source order, but execution order matters: per the L1607/L1609 numbers in `canonical_line_ranges`, the level pre-increment happens AFTER rngLockedFlag is set; PLV-03 disproof must verify the actual execution order from the function body)
    - L1702-1704 _unlockRng (the F-write of rngLockedFlag paired with dailyIdx advance at L1703)

    Arithmetic-site source ranges to read for PLV-04 flat table (single-pass extraction):
    - AdvanceModule:185 / L312 / L389 / L390 / L411 — `+1` and `+4` sites
    - AdvanceModule:397 / L424 / L752 — array-index sites (3 hits in `levelPrizePool[]` grep that index on `purchaseLevel`, vs L176 / L558 / L647 / L1858 which index on `lvl`)
    - AdvanceModule:218 / L266 — `_tqReadKey(purchaseLevel)` calls
    - AdvanceModule:750 — `purchaseLevel % 10 == 9` modulo
    - AdvanceModule:1097 / L1098 / L1100 — internal helper local derivation
    - AdvanceModule:1504 / L1507 — _processPhaseTransition arithmetic
    - MintModule:923 / L924 — sibling ternary + priceForLevel call
    - LootboxModule:532 / L552 — packed-decode + grace-passthrough
    - WhaleModule:841 / L876 — parameter receive + packing site
    - BurnieCoinflip:578 / L590 / L596 — first parameter-receive path
    - BurnieCoinflip:1035 / L1041 — second parameter-receive path
  </read_first>
  <action>
**Step A — Re-read source ranges at HEAD acd88512 (re-use Task 1 extracts if extant):**

```
test -f /tmp/v32-249-AdvanceModule-acd88512.sol || git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol > /tmp/v32-249-AdvanceModule-acd88512.sol
test -f /tmp/v32-249-MintModule-acd88512.sol || git show acd88512:contracts/modules/DegenerusGameMintModule.sol > /tmp/v32-249-MintModule-acd88512.sol
test -f /tmp/v32-249-LootboxModule-acd88512.sol || git show acd88512:contracts/modules/DegenerusGameLootboxModule.sol > /tmp/v32-249-LootboxModule-acd88512.sol
test -f /tmp/v32-249-WhaleModule-acd88512.sol || git show acd88512:contracts/modules/DegenerusGameWhaleModule.sol > /tmp/v32-249-WhaleModule-acd88512.sol
test -f /tmp/v32-249-BurnieCoinflip-acd88512.sol || git show acd88512:contracts/BurnieCoinflip.sol > /tmp/v32-249-BurnieCoinflip-acd88512.sol

# Verify execution order of L1607 (rngLockedFlag = true) vs L1609 (level += 1) in _finalizeRngRequest:
sed -n '1600,1615p' /tmp/v32-249-AdvanceModule-acd88512.sol > /tmp/v32-249-finalizeRngRequest.sol
```

Inspect `/tmp/v32-249-finalizeRngRequest.sol` to confirm the actual execution order. The PLV-03 disproof anchor depends on whether `level += 1` strictly PRECEDES `rngLockedFlag = true` in the function body. If the actual order is reversed (rngLockedFlag=true BEFORE level+=1), the INV-PLV-A-01 disproof reformulates: the relevant invariant becomes "L1609 level pre-increment is the SOLE PRE-CONDITION before `rngLockedFlag = true` becomes a meaningful state at advanceGame entry, AND the L173 turbo guard prevents the only path that could write lastPurchaseDay=true while rngLocked=T was already set in a stale prior cycle." Both phrasings close the proof; pick whichever matches the actual source order at HEAD acd88512.

**Step B — Replace Section 3 (PLV-03) with the populated content:**

Replace the existing `## Section 3 — PLV-03: ternary unreachable-state proof for O-PLV-TTF lvl=0 cell (RESERVED FOR TASK 2)` stub in `audit/v32-249-PLV.md`.

Section 3 structure (per ROADMAP success criterion 3 + CONTEXT.md PLV-03 wording):

```markdown
## Section 3 — PLV-03: ternary unreachable-state proof for O-PLV-TTF lvl=0 cell

**Statement:** The ternary at AdvanceModule:185 `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1` cannot return 0 once the WIP `!rngLockedFlag` turbo guard at AdvanceModule:173 is in place. The unreachable state is `(lastPurchase = T ∧ rngLockedFlag = T ∧ lvl = 0)` — equivalently the O-PLV-TTF lvl=0 cell from PLV-02 §2.1.

**Proof anchor:** PLV-02 §2.1 row O-PLV-TTF/lvl=0 carries the UNREACHABLE classification with INV-PLV-A-01 cite. PLV-03 closes the formal proof by walking the guard-evaluation sequence step-by-step.

### 3.1 Guard-evaluation walk (the formal closure)

The ternary returns 0 iff `lastPurchase = T ∧ rngLockedFlag = T ∧ lvl = 0`. We prove this conjunction is unreachable at any advanceGame entry point.

**Step 1 — `lvl = 0` requires bootstrap pre-state per INV-PLV-C-01:**
- Sole-writer grep: `git grep -nE '\blevel\s*=\|\blevel\s*+=' acd88512 -- 'contracts/storage/*.sol' 'contracts/modules/*.sol'` returns L250 (init `uint24 public level = 0;`) + L1609 (`level += 1` inside _finalizeRngRequest). NO other writers.
- Therefore `lvl = 0` holds ONLY before the first call to `_finalizeRngRequest` (the bootstrap window).

**Step 2 — `rngLockedFlag = T` requires _finalizeRngRequest has fired at least once per INV-PLV-A-01:**
- Sole T-writer of rngLockedFlag: AdvanceModule:1607 inside `_finalizeRngRequest` (the rngLockedFlag = true write).
- Sole F-writers of rngLockedFlag: AdvanceModule:1663 (inside updateVrfCoordinatorAndSub — admin path) + AdvanceModule:1704 (inside _unlockRng — paired with dailyIdx advance at L1703).
- Therefore `rngLockedFlag = T` is observable at advanceGame entry ONLY AFTER _finalizeRngRequest has fired.

**Step 3 — Combine Step 1 and Step 2:**
- `lvl = 0` requires _finalizeRngRequest has NEVER fired (bootstrap pre-state).
- `rngLockedFlag = T` requires _finalizeRngRequest has fired at least once.
- These are CONTRADICTORY pre-conditions. There is no advanceGame entry where both hold.

**Conclusion of Step 3:** The conjunction `lvl = 0 ∧ rngLockedFlag = T` is unreachable at any advanceGame entry. The third conjunct (`lastPurchase = T`) is irrelevant — the AND-pair is already empty.

**Step 4 — The L173 turbo guard provides defense-in-depth:**
- The L173 conjunct `!rngLockedFlag` (third conjunct of the IF block opening at L167) gates the turbo write `lastPurchaseDay = true` at L178. Even if Step 3 were somehow violated by a future code change, the L173 guard prevents the turbo path from setting lastPurchaseDay=true while rngLocked=T.
- Without this guard, the turbo path could set lastPurchaseDay=T while rngLockedFlag=T was already set from a prior _finalizeRngRequest where lastPurchase was false at that time (so no level pre-increment fired) — that is the EXACT testnet panic 0x11 trigger geometry per CONTEXT.md `<domain>` paragraph 2.

**Conclusion:** The ternary at L185 cannot return 0 at any reachable advanceGame entry. PLV-03 closed.

### 3.2 Cross-cite to PLV-02 + PLV-04 + PLV-05

- PLV-02 §2.1 row O-PLV-TTF/lvl=0 carries the UNREACHABLE classification anchored on PLV-03 §3.1 walk.
- PLV-04 §4 underflow rows for AdvanceModule:397 + L752 cross-cite PLV-03 §3.1 as the SAFE-via-PLV-03 anchor (per D-249-08).
- PLV-05 §5.2 post-fix walk is the concrete trace through the same Step 4 turbo-guard short-circuit (testnet blocks 10759449 + 10761786 walked numerically).
```

**Step C — Replace Section 4 (PLV-04) with the populated content per D-249-07 single flat table + D-249-08 cross-cite chain + D-249-09 both-bounds oob:**

Replace the existing `## Section 4 — PLV-04: arithmetic-site flat table + array-index oob both bounds (RESERVED FOR TASK 2)` stub.

```markdown
## Section 4 — PLV-04: arithmetic-site flat table + array-index oob both bounds

Per CONTEXT.md D-249-07 (single flat per-call-site table sorted by file:line) + D-249-08 (-1 rows cross-cite PLV-02 + PLV-03 + PLV-05) + D-249-09 (array-index oob both bounds covered).

**Universe-of-arithmetic-sites attestation (per D-249-CF-09):**

```
git grep -nE 'purchaseLevel\s*[+\-]\s*[0-9]\|purchaseLevel\s*%' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'
git grep -n 'levelPrizePool\[purchaseLevel' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'
```

Locked output: every `+`/`-`/`%` arithmetic on purchaseLevel + every `levelPrizePool[purchaseLevel*]` array index. Below table covers all hits.

Column shape per D-249-07: `Row ID | Site (file:line) | Function | Operator {-1, +1, +4, +99, %10, _tqReadKey, array-index} | Arithmetic expression | Reachable purchaseLevel range | Underflow / overflow / oob risk | Verdict | Evidence Cite (incl. cross-cite for -1 rows)`.

### 4.1 Flat per-call-site arithmetic table

| Row ID | Site (file:line) | Function | Operator | Expression | Reachable range | U/O/OOB risk | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|---|---|
| PLV-04-V01 | AdvanceModule:185 | advanceGame | `+1` | `lvl + 1` (binder) | lvl ∈ [0, max-1] reachable per PLV-02 (max OOS) | overflow at lvl = uint24.max — OOS-by-construction per D-249-06 | SAFE | bind expression; cross-cite PLV-02 §2.1 |
| PLV-04-V02 | AdvanceModule:218 | advanceGame | `_tqReadKey` | `_tqReadKey(purchaseLevel)` | ≥1 per PLV-02 | none — _tqReadKey accepts any uint24 | SAFE | param-pass-through; helper tolerates any uint24 |
| PLV-04-V03 | AdvanceModule:266 | advanceGame | `_tqReadKey` | `_tqReadKey(purchaseLevel)` | ≥1 | none | SAFE | same as V02 |
| PLV-04-V04 | AdvanceModule:312 | advanceGame | `+4` | `purchaseLevel + 4` (ffLevel) | ≥1; ≤ max-4 | overflow at purchaseLevel ≥ max-3 — OOS-by-construction per D-249-06 | SAFE | overflow-edge cited in §4.2 |
| PLV-04-V05 | AdvanceModule:389 | advanceGame (daily-jackpot) | `+1` | `purchaseLevel + 1` (payDailyCoinJackpot arg) | ≥2 (this branch is purchaseLevel ≥ 2 per L385 else-path); ≤ max-1 | overflow at max — OOS | SAFE | branch-conditional ≥2 |
| PLV-04-V06 | AdvanceModule:390 | advanceGame (daily-jackpot) | `+4` | `purchaseLevel + 4` | ≥2; ≤ max-4 | overflow at max-3+ — OOS | SAFE | same |
| PLV-04-V07-A | AdvanceModule:397 | advanceGame (daily-jackpot) | `-1` | `levelPrizePool[purchaseLevel - 1]` (lower-bound check) | ≥1 per PLV-02 | underflow at purchaseLevel = 0 — UNREACHABLE per PLV-03 | **SAFE-via-PLV-03** | THE LITERAL v32.0 PANIC 0x11 PRIMARY TRIGGER; cross-cite PLV-02 §2.1 (every reachable cell ≥ 1) + PLV-03 §3.1 (TTF/lvl=0 unreachable) + PLV-05 §5.2 (post-fix walk shows turbo guard short-circuits) |
| PLV-04-V07-B | AdvanceModule:397 | advanceGame (daily-jackpot) | array-index (upper-bound check) | `levelPrizePool[purchaseLevel - 1]` | ≤ levelPrizePool.length - 1 | oob at purchaseLevel - 1 ≥ length | SAFE | upper-bound cited via D-249-06 level cap (game-mechanic terminates before purchaseLevel exceeds levelPrizePool.length); per D-249-09 both-bounds coverage |
| PLV-04-V08 | AdvanceModule:411 | advanceGame | `+1` | `nextLevel = purchaseLevel + 1` | ≥1; ≤ max-1 | overflow at max — OOS | SAFE | cross-cite §4.2 |
| PLV-04-V09 | AdvanceModule:424 | advanceGame | array-index (write) | `levelPrizePool[purchaseLevel] = ...` | ≤ length - 1 | oob at purchaseLevel ≥ length | SAFE | write-side; level cap closes the upper bound |
| PLV-04-V10 | AdvanceModule:734-752 | _distributeYieldSurplus | `% 10 == 9` | `purchaseLevel % 10 == 9` | any uint24 | none — modulo well-defined | SAFE | feature-gate at L750 |
| PLV-04-V11-A | AdvanceModule:752 | _distributeYieldSurplus | `-1` | `levelPrizePool[purchaseLevel - 1]` (lower-bound check) | ≥1 per PLV-02 | underflow — UNREACHABLE per PLV-03 | **SAFE-via-PLV-03** | v32.0 PANIC 0x11 SECONDARY TRIGGER; cross-cite PLV-02 + PLV-03 + PLV-05 (mirrors V07-A) |
| PLV-04-V11-B | AdvanceModule:752 | _distributeYieldSurplus | array-index (upper-bound check) | same | ≤ length - 1 | oob | SAFE | mirrors V07-B |
| PLV-04-V12 | AdvanceModule:1097 | (internal helper) | `+1` | `purchaseLevel_ = level + 1` | ≥1 (level ≥ 0) | overflow at level = max — OOS | SAFE | not the L185 binder; independent derivation; doesn't subtract 1 downstream |
| PLV-04-V13 | AdvanceModule:1507 | _processPhaseTransition | `+99` | `targetLevel = purchaseLevel + 99` | ≥1; ≤ max-99 | overflow at max-98+ — OOS | SAFE | OOS-by-construction |
| PLV-04-V14 | MintModule:923 | (mint function) | `+1` | `cachedLevel + 1` (sibling ternary) | ≥0 (level ≥ 0) | overflow at level = max — OOS | SAFE-pending-PLV-01-V31 | inherits PLV-01-V31 verdict; if FINDING_CANDIDATE there, cross-flag here |
| PLV-04-V15 | MintModule:924 | (mint function) | `priceForLevel` boundary | `PriceLookupLib.priceForLevel(purchaseLevel)` | ≥0 (per PLV-01-V31 reachability) | underflow inside lib if purchaseLevel = 0 with 0-indexed table | SAFE-pending-PLV-01-V31 | boundary record per D-248-10 analog; behavioral cite: "returns wei price for given level"; full internal-arithmetic trace per CONTEXT.md `<decisions>` Claude's-Discretion item 6 |
| PLV-04-V16 | LootboxModule:532 | openLootBox | packed-decode | `uint24(packed >> 232)` | depends on writer's purchaseLevel at WhaleModule:876 (≥1 per PLV-01-V34) | none — bit-extract well-defined | SAFE | packed-on-write invariant per PLV-01-V33 + V34 |
| PLV-04-V17 | LootboxModule:552 | openLootBox | passthrough | `withinGracePeriod ? graceLevel : purchaseLevel` | ≥1 | none | SAFE | passthrough preserves writer's purchaseLevel |
| PLV-04-V18 | BurnieCoinflip:590 | (queue-flip path) | `% 10 == 0` | `purchaseLevel_ % 10 == 0` | any uint24 | none | SAFE | modulo gate |
| PLV-04-V19 | BurnieCoinflip:1041 | (locked-condition path) | `% 10 == 0` | `purchaseLevel_ % 10 == 0` | any uint24 | none | SAFE | mirrors V18 |

### 4.2 Overflow-edge sub-section (D-249-06 OOS-by-construction)

Per CONTEXT.md D-249-06: every `purchaseLevel + N` overflow edge (V01 / V04 / V05 / V06 / V08 / V12 / V13 / V14) is OOS-by-construction. The level cap is a v25/v26-era game-mechanic invariant not part of the v32 delta surface (cite REQUIREMENTS.md Out of Scope clause: "non-delta surfaces — covered in v25.0/v29.0/v30.0/v31.0; not re-proven globally"). Game terminates via `gameOverPossible` / target-met termination (cite AdvanceModule:178 `gameOverPossible = false` write inside turbo block, plus L335 / L442 phase transitions) before `purchaseLevel` reaches `uint24.max`. Single one-line cite — overflow rows in the main table inherit this attestation.

### 4.3 Cross-cite summary for `-1` underflow rows (per D-249-08)

Both `-1` underflow rows (PLV-04-V07-A at L397 + PLV-04-V11-A at L752) carry the SAFE-via-PLV-03 verdict via the explicit safety chain:

1. **PLV-02 §2.1:** Every REACHABLE octant cell at L185 evaluates `purchaseLevel ≥ 1` (the 13 in-bound REACHABLE cells in §2.1).
2. **PLV-03 §3.1:** The single UNREACHABLE-with-`purchaseLevel = 0` cell (O-PLV-TTF/lvl=0) is closed by Steps 1-3 (contradictory pre-conditions: lvl=0 requires bootstrap, rngLocked=T requires _finalizeRngRequest fired) plus Step 4 turbo guard defense-in-depth.
3. **PLV-05 §5.2:** Post-fix testnet walk concretely demonstrates the L173 short-circuit firing at the testnet trigger sequence (blocks 10759449 + 10761786).

This three-link chain establishes the per-site safety chain explicitly per D-249-08.

### 4.4 PLV-04 Finding Candidates (omit subsection if zero — but V14 / V15 inherit V31 verdict)

[If PLV-01-V31 is FINDING_CANDIDATE, cross-flag PLV-04-V14 + V15 here. If V31 is SAFE, OMIT this subsection.]
```

**Step D — Verify Task 2 deliverable:**

```
test -f audit/v32-249-PLV.md
grep -q '^## Section 3 — PLV-03: ternary unreachable-state proof' audit/v32-249-PLV.md
! grep -q 'RESERVED FOR TASK 2' audit/v32-249-PLV.md
grep -q '^### 3.1 Guard-evaluation walk' audit/v32-249-PLV.md
grep -q '^### 3.2 Cross-cite to PLV-02' audit/v32-249-PLV.md
grep -q 'INV-PLV-A-01' audit/v32-249-PLV.md
grep -q 'INV-PLV-C-01' audit/v32-249-PLV.md
grep -q '^## Section 4 — PLV-04' audit/v32-249-PLV.md
grep -q '^| PLV-04-V01 ' audit/v32-249-PLV.md
grep -q '^| PLV-04-V07-A ' audit/v32-249-PLV.md     # primary -1 row at L397
grep -q '^| PLV-04-V07-B ' audit/v32-249-PLV.md     # primary upper-bound row at L397
grep -q '^| PLV-04-V11-A ' audit/v32-249-PLV.md     # secondary -1 row at L752
grep -q '^| PLV-04-V11-B ' audit/v32-249-PLV.md     # secondary upper-bound row at L752
grep -q 'SAFE-via-PLV-03' audit/v32-249-PLV.md
grep -q '^### 4.3 Cross-cite summary for ' audit/v32-249-PLV.md
grep -q '^## Section 5 — PLV-05' audit/v32-249-PLV.md     # still RESERVED
grep -q 'RESERVED FOR TASK 3' audit/v32-249-PLV.md         # Sections 5 + 6
! grep -q 'F-32-' audit/v32-249-PLV.md
[ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-04-V')" -ge 19 ]
test -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')"
```

**Step E — Atomic commit per D-247-14 carry-forward:**

```
git add audit/v32-249-PLV.md
git commit -m "$(cat <<'EOF'
audit(249-01): Task 2 — PLV-03 ternary unreachable proof + PLV-04 arithmetic flat table

Plan 249-01 Task 2 deliverable. Replaces Sections 3 + 4 RESERVED stubs
in audit/v32-249-PLV.md with populated content.

Sections written:
  - Section 3: PLV-03 — ternary unreachable-state proof for the
    O-PLV-TTF lvl=0 cell. 4-step guard-evaluation walk closes the
    formal proof: (1) lvl=0 requires bootstrap pre-state per
    INV-PLV-C-01; (2) rngLocked=T requires _finalizeRngRequest fired
    per INV-PLV-A-01; (3) the conjunction is contradictory; (4) L173
    turbo guard provides defense-in-depth. Cross-cites PLV-02 §2.1
    O-PLV-TTF cell + PLV-04 -1 rows + PLV-05 post-fix walk.
  - Section 4: PLV-04 — arithmetic-site flat table per D-249-07
    covering 19+ sites with operator column (+1, +4, +99, -1, %10,
    _tqReadKey, array-index, packed-decode). The two -1 rows
    (V07-A at L397 + V11-A at L752) carry SAFE-via-PLV-03 verdict
    with explicit cross-cite chain to PLV-02 + PLV-03 + PLV-05 per
    D-249-08. Array-index rows split into V07-B + V11-B for upper-
    bound oob coverage per D-249-09 both-bounds rule. Overflow edges
    consolidated under §4.2 OOS-by-construction per D-249-06.

Phase 247 catalog refs cited: D-247-I009 (PLV-03 row scope) +
D-247-I010 (PLV-04 row scope).

Pure-proof phase per D-249-CF-04 / D-249-CF-05 — zero contracts/ or
test/ writes.

Anchor: HEAD acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-249-PLV.md && grep -q '^## Section 3 — PLV-03: ternary unreachable-state proof' audit/v32-249-PLV.md && grep -q '^### 3.1 Guard-evaluation walk' audit/v32-249-PLV.md && grep -q '^### 3.2 Cross-cite to PLV-02' audit/v32-249-PLV.md && grep -q '^## Section 4 — PLV-04: arithmetic-site flat table' audit/v32-249-PLV.md && grep -q '^| PLV-04-V01 ' audit/v32-249-PLV.md && grep -q '^| PLV-04-V07-A ' audit/v32-249-PLV.md && grep -q '^| PLV-04-V07-B ' audit/v32-249-PLV.md && grep -q '^| PLV-04-V11-A ' audit/v32-249-PLV.md && grep -q '^| PLV-04-V11-B ' audit/v32-249-PLV.md && grep -q 'SAFE-via-PLV-03' audit/v32-249-PLV.md && grep -q '^### 4.3 Cross-cite summary for' audit/v32-249-PLV.md && grep -q '^## Section 5 — PLV-05' audit/v32-249-PLV.md && grep -q 'RESERVED FOR TASK 3' audit/v32-249-PLV.md && ! grep -q 'F-32-' audit/v32-249-PLV.md && [ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-04-V')" -ge 19 ] && [ -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')" ] && git log -1 --format='%s' | grep -qE '^audit\(249-01\): Task 2'</automated>
  </verify>
  <acceptance_criteria>
    - Section 3 (PLV-03) populated with §3.1 4-step guard-evaluation walk + §3.2 cross-cite block; the `RESERVED FOR TASK 2` marker for §3 + §4 is REMOVED
    - §3.1 explicitly walks Step 1 (INV-PLV-C-01 lvl=0 bootstrap) + Step 2 (INV-PLV-A-01 rngLocked=T post-finalize) + Step 3 (contradictory conjunction) + Step 4 (L173 turbo guard defense-in-depth)
    - §3.2 cross-cites PLV-02 §2.1 + PLV-04 -1 rows + PLV-05 §5.2
    - Section 4 (PLV-04) populated with §4.1 flat table (≥19 PLV-04-VNN rows covering: L185 +1 binder, L218 / L266 _tqReadKey, L312 +4, L389 +1, L390 +4, L397 -1 + array-index split into V07-A / V07-B per D-249-09, L411 +1, L424 array-index write, L734-750 modulo, L752 -1 + array-index split into V11-A / V11-B, L1097 +1, L1507 +99, MintModule:923 +1, MintModule:924 priceForLevel boundary, LootboxModule:532 packed-decode, LootboxModule:552 passthrough, BurnieCoinflip:590 modulo, BurnieCoinflip:1041 modulo) + §4.2 overflow-edge sub-section (single-line OOS-by-construction cite per D-249-06) + §4.3 cross-cite summary for -1 rows (per D-249-08 three-link chain) + §4.4 Finding Candidates (omitted iff zero)
    - PLV-04-V07-A + PLV-04-V11-A both carry verdict `SAFE-via-PLV-03` with explicit cross-cite to PLV-02 + PLV-03 + PLV-05
    - PLV-04-V07-B + PLV-04-V11-B exist as upper-bound oob rows (D-249-09 both-bounds rule)
    - Sections 5 + 6 + 7 still exist as RESERVED stubs
    - File contains zero `F-32-` references
    - Working tree contains only the 2 pre-existing lines
    - Git log shows 1 new commit with subject `audit(249-01): Task 2`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-249-PLV.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
audit/v32-249-PLV.md Sections 3 + 4 populated. PLV-03 closes the formal proof that the L185 ternary cannot return 0 via a 4-step guard-evaluation walk anchored on INV-PLV-A-01 / INV-PLV-C-01. PLV-04 enumerates ≥19 arithmetic / array-index sites in a flat table; the two literal v32.0 panic 0x11 trigger sites (L397 + L752) carry SAFE-via-PLV-03 verdicts with explicit three-link cross-cite chains; array-index rows split into both-bounds coverage per D-249-09. One atomic commit landed.
  </deliverable>
  <done>Sections 3 + 4 populated; RESERVED-FOR-TASK-2 marker removed; ≥19 PLV-04 rows; both -1 sites carry SAFE-via-PLV-03 with cross-cite chain; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: PLV-05 + PLV-06 — testnet panic 0x11 BFL-03-style state-transition walk (pre-fix + post-fix) + daily-jackpot per-branch invariant table + strand-disproof attestation + Phase 252 POST31-02 composition hand-off row</name>
  <files>audit/v32-249-PLV.md</files>
  <read_first>
    - audit/v32-249-PLV.md (current state — exists with Sections 0-4 populated by Tasks 1-2; Sections 5-6 are RESERVED stubs that Task 3 replaces in place)
    - .planning/phases/249-purchaselevel-correctness-proof/249-CONTEXT.md — specifically D-249-CF-10 (testnet block reproduction seed 10759449 + 10761786), D-249-10 (BFL-03-style state-transition walk pattern), D-249-11 (testnet block seed locked, narrative-context alignment with REQUIREMENTS.md trigger), D-249-12 (PLV-06 per-branch invariant table + strand-disproof attestation), D-249-13 (PLV-06 emits Phase 252 POST31-02 composition hand-off row), `<specifics>` (PLV-06 strand-disproof attestation suggestion paragraph)
    - .planning/REQUIREMENTS.md — PLV-05 + PLV-06 wordings (verbatim) + trigger context paragraph
    - audit/v32-247-DELTA-SURFACE.md — Section 6 D-247-I011 (PLV-05 row scope) + D-247-I012 (PLV-06 row scope); §1.6 advanceGame turbo-guard finding-candidate bullets (the testnet trigger geometry summary)
    - audit/v32-248-BFL.md — §3 BFL-03 worked-numeric-example state-transition walk format (D-249-10 mirror — column shape `Step | block.timestamp | day | dailyIdx | rngLockedFlag | lastPurchaseDay | jackpotPhaseFlag | level | turbo guard verdict at L173 | binding ternary L185 result | purchaseLevel | next L397/L752 verdict`); §4 BFL-04 invariant-table format (D-249-12 mirror); §5 BFL-05 dual-carrier attestation row format (D-249-13 mirror)
    - audit/v31-245-SDR.md / audit/v31-245-GOE.md — dual-carrier attestation row precedent for D-249-13 hand-off row (specifically SDR-08-V01 / GOE-01-V01 / GOE-04-V02 carrier rows)

    HEAD-side reads at acd88512 (re-use Task 1/2 extracts):
    - L160-185 advanceGame entry through binding ternary (PLV-05 walk anchor)
    - L167-182 turbo block (the path that fires pre-fix and short-circuits post-fix)
    - L370-407 daily-jackpot region (PLV-06 strand-disproof scope):
      - L371 `if (!lastPurchaseDay)` branch open
      - L372 `if (purchaseLevel == 1)` sub-branch (coin-only path)
      - L385 `payDailyJackpot(false, purchaseLevel, rngWord)` (purchaseLevel ≥ 2 sub-branch)
      - L387-390 `_payDailyCoinJackpot(purchaseLevel, rngWord, purchaseLevel + 1, purchaseLevel + 4)`
      - L393-397 `targetMet = gameOverPossible ? _evaluateGameOverAndTarget(lvl, psd, day) : _getNextPrizePool() >= levelPrizePool[purchaseLevel - 1];`
      - L398 `if (targetMet)`
      - L399 `lastPurchaseDay = true;`
      - L400-402 `if (day - psd <= 3) compressedJackpotFlag = 1;`
      - L404 `_unlockRng(day);`
      - L405 `stage = STAGE_PURCHASE_DAILY;`
      - L406 `break;`
    - L1702-1710 _unlockRng body (the resolution path; clears rngLockedFlag + dailyIdx + rngWordCurrent)
    - 8bdeabc2 commit context: productive-pause `_pauseDeathClockDuringProductivePhase` shape (Phase 252 POST31-02 composition target — read from `audit/v32-247-DELTA-SURFACE.md` §1.4 D-247-C### row that catalogs the 8bdeabc2 hunk)
  </read_first>
  <action>
**Step A — Re-read source ranges at HEAD acd88512 (re-use Task 1/2 extracts):**

```
test -f /tmp/v32-249-AdvanceModule-acd88512.sol || git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol > /tmp/v32-249-AdvanceModule-acd88512.sol
sed -n '160,185p' /tmp/v32-249-AdvanceModule-acd88512.sol > /tmp/v32-249-advanceGame-bind.sol
sed -n '167,182p' /tmp/v32-249-AdvanceModule-acd88512.sol > /tmp/v32-249-turbo-block.sol
sed -n '370,407p' /tmp/v32-249-AdvanceModule-acd88512.sol > /tmp/v32-249-dailyJackpot-region.sol
sed -n '1702,1710p' /tmp/v32-249-AdvanceModule-acd88512.sol > /tmp/v32-249-unlockRng.sol

# Verify L398-L404 has zero early-return / revert / break / continue (the strand-disproof attestation):
grep -nE '^\s*(return|revert|break|continue);' /tmp/v32-249-dailyJackpot-region.sol > /tmp/v32-249-dailyJackpot-jumps.txt
```

Locked expectation: `/tmp/v32-249-dailyJackpot-jumps.txt` shows the `break;` at L406 is the FIRST jump after L399 `lastPurchaseDay = true`. The intervening lines L400-L405 contain ONLY: an `if (day - psd <= 3)` conditional with `compressedJackpotFlag = 1;` body (L400-402), the `_unlockRng(day);` call (L404), and the `stage = STAGE_PURCHASE_DAILY;` write (L405). No revert / return / continue between L399 and L404. PLV-06 strand-disproof attestation HOLDS.

If the actual jump pattern differs, adjust the strand-disproof prose in §6.2 to match the source. Per D-249-CF-08 scope-guard, document any unexpected jumps as a Phase 250 SIB-01 hand-off candidate.

**Step B — Replace Section 5 (PLV-05) with the populated content per D-249-10 + D-249-11:**

Replace the existing `## Section 5 — PLV-05: testnet panic 0x11 reproduction (BFL-03-style state-transition walk) (RESERVED FOR TASK 3)` stub.

```markdown
## Section 5 — PLV-05: testnet panic 0x11 reproduction (BFL-03-style state-transition walk)

Per CONTEXT.md D-249-CF-10 (testnet block seed 10759449 + 10761786) + D-249-10 (BFL-03-style state-transition walk) + D-249-11 (narrative alignment with REQUIREMENTS.md trigger).

**Scenario seed:** Testnet trigger blocks 10759449 + 10761786 (per CONTEXT.md `<domain>` paragraph + REQUIREMENTS.md trigger context). The shape is: turbo block at L167-182 fires while `rngLockedFlag = true` (because the WIP `!rngLockedFlag` conjunct at L173 was absent in pre-fix code), so the `lastPurchaseDay = true` write at L178 fires while RNG is still locked from a prior request. The `rngGate` fresh-word path runs instead of `_requestRng`, so the `level += 1` pre-increment at L1609 inside `_finalizeRngRequest` is missed. Then the binding ternary at L185 with `lastPurchase = T ∧ rngLockedFlag = T` returns `lvl = 0` (the SAFE-via-PLV-03 cell that PLV-03 §3.1 closes). The downstream `levelPrizePool[purchaseLevel - 1]` at L397 (or L752 inside `_distributeYieldSurplus`) panics with 0x11 underflow on `uint24(0) - 1`.

Column shape per D-249-10: `Step | block.timestamp | day | dailyIdx | rngLockedFlag | lastPurchaseDay | jackpotPhaseFlag | level (lvl) | turbo guard verdict at L173 | binding ternary L185 result | purchaseLevel | next L397/L752 verdict`.

### 5.1 Pre-fix walk (the testnet panic 0x11 trigger sequence)

This walk uses the pre-WIP source (no `!rngLockedFlag` conjunct at L173). Pre-fix L173 reads `if (!inJackpot && !lastPurchaseDay)` only.

| Row ID | Step | block.timestamp (block) | day | dailyIdx | rngLockedFlag | lastPurchaseDay | jackpotPhaseFlag | lvl | L173 verdict (PRE-FIX: !inJackpot && !lastPurchaseDay) | L185 ternary result | purchaseLevel | next L397/L752 verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| PLV-05-V01 | 1 | block 10759449 | day_N | day_N - 1 | F | F | F | 0 | TRUE → turbo block enters | (F && F) ? lvl : lvl+1 = lvl+1 = 1 | 1 | SAFE — purchaseLevel = 1, no underflow this call |
| PLV-05-V02 | 2 | (within block 10759449) | day_N | day_N - 1 | F | F | F | 0 | (turbo body executes: target met → lastPurchaseDay = T at L178; gameOverPossible = false at L180) | n/a (turbo body, ternary already evaluated) | n/a | n/a |
| PLV-05-V03 | 3 | (later in block 10759449, _requestRng path) | day_N | day_N - 1 | F→T (L1607 write) | T | F | 0→1 (L1609 pre-increment) | n/a (RNG request path) | n/a | n/a |
| PLV-05-V04 | 4 | (between blocks; VRF callback fulfills) | day_N | day_N - 1 | T (still locked until _unlockRng) | T | F | 1 | n/a | n/a | n/a | n/a |
| PLV-05-V05 | 5 | block 10761786 (NEXT advanceGame call) | day_N (still same wall-clock day if early enough OR day_N+1 if midnight crossed) | day_N - 1 | T | T | F | 1 | (PRE-FIX) `!inJackpot && !lastPurchaseDay` = F && F → FALSE → turbo block SKIPPED | (T && T) ? lvl : lvl+1 = lvl = 1 | 1 | SAFE — purchaseLevel = 1; this is NOT the bug shape; need different geometry |

[The straightforward reading above does NOT reproduce the bug; the actual bug requires a more subtle interleaving where `level` did NOT pre-increment but `lastPurchaseDay = true` was already set. Re-derive the actual testnet trigger sequence by reading REQUIREMENTS.md trigger paragraph carefully + 8bdeabc2 / acd88512 commit messages + CONTEXT.md `<domain>` paragraph 2. The CRITICAL geometry per CONTEXT.md `<domain>`: "Turbo block at AdvanceModule:167 fires while rngLockedFlag = true. The rngGate fresh-word path runs instead of _requestRng, so the level pre-increment is missed." — this means in the pre-fix code, the turbo block could fire WITHOUT the `!rngLockedFlag` guard, setting lastPurchaseDay = true WHILE rngLocked was already true from a prior request, and the rngGate fresh-word branch took the path that did NOT pre-increment level. The corrected pre-fix walk needs to model THIS interleaving where (a) prior call left rngLocked=T but lastPurchase was F at that time so no pre-increment fired, (b) turbo block fires in the next call WITHOUT the L173 !rngLockedFlag check, (c) lastPurchaseDay flips to T but level remains 0, (d) next entry binds (T && T) ? 0 : 1 = 0 → panic. Re-walk the table above with this corrected geometry.]

[Append rows V06..V0N as needed. The table MUST end with a row showing the L185 binding evaluating to 0 followed by an L397 row showing `levelPrizePool[uint24(0) - 1]` panic 0x11.]

### 5.2 Post-fix walk (the L173 turbo guard short-circuits)

This walk uses the post-WIP source at HEAD acd88512 (`!rngLockedFlag` conjunct at L173 active). Post-fix L173 reads `if (!inJackpot && !lastPurchaseDay && !rngLockedFlag)`.

Same scenario seed; same blocks. The KEY DIFFERENCE: at the step where the pre-fix walk had the turbo block fire while rngLocked=T (Step 5 region with corrected geometry), the post-fix walk has the L173 conjunct evaluate FALSE (because rngLocked=T) → turbo block SKIPPED → `lastPurchaseDay = true` write at L178 NEVER FIRES → next advanceGame entry sees lastPurchase=F → ternary at L185 picks `lvl + 1` branch → purchaseLevel ≥ 1 → no underflow at L397/L752.

| Row ID | Step | block.timestamp (block) | day | dailyIdx | rngLockedFlag | lastPurchaseDay | jackpotPhaseFlag | lvl | L173 verdict (POST-FIX: !inJackpot && !lastPurchaseDay && !rngLockedFlag) | L185 ternary result | purchaseLevel | next L397/L752 verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| PLV-05-V0M | (mirror of V05) | block 10761786 | day | dailyIdx | T | F (still F because Step (b) write skipped) | F | 0 | F (rngLocked=T fails third conjunct) → TURBO SKIPPED → `lastPurchaseDay = true` NEVER WRITTEN | (F && T) ? lvl : lvl+1 = lvl+1 = 1 | 1 | SAFE — purchaseLevel = 1, no underflow |
| PLV-05-V0M+1 | (subsequent normal path) | (next advanceGame call) | day+1 | day | F (cleared by _unlockRng L1704) | F | F | 1 (or higher per normal level progression) | TRUE → turbo block evaluates as normal | normal `lvl + 1` | ≥ 2 | SAFE |

[Append rows as needed to demonstrate the short-circuit holds across multiple subsequent calls. Cross-cite PLV-03 §3.1 Step 4 turbo guard defense-in-depth.]

### 5.3 Cross-cite to PLV-03 + PLV-04

- PLV-05 §5.1 pre-fix walk concretely demonstrates the (lastPurchase=T ∧ rngLocked=T ∧ lvl=0) cell (the O-PLV-TTF/lvl=0 cell from PLV-02 §2.1) was REACHABLE in pre-fix code via the testnet trigger sequence.
- PLV-05 §5.2 post-fix walk concretely demonstrates the L173 turbo guard short-circuits the path before the bind, closing the cell to UNREACHABLE per PLV-03 §3.1 Step 4.
- PLV-04 §4.3 cross-cite chain inherits PLV-05 §5.2 as the third link of the safety chain for the L397 + L752 -1 underflow rows.
```

**Step C — Replace Section 6 (PLV-06) with the populated content per D-249-12 + D-249-13:**

Replace the existing `## Section 6 — PLV-06: daily-jackpot region L370-407 no-strand proof (RESERVED FOR TASK 3)` stub.

```markdown
## Section 6 — PLV-06: daily-jackpot region L370-407 no-strand proof + Phase 252 POST31-02 composition hand-off

Per CONTEXT.md D-249-12 (per-branch invariant table + strand-disproof attestation) + D-249-13 (Phase 252 POST31-02 composition hand-off row).

**Scope:** AdvanceModule:370-407 daily-jackpot region. The no-strand requirement: any branch reaching `targetMet = true` MUST also call `_unlockRng(day)` in the SAME call (no path that sets `lastPurchaseDay = true` at L399 without subsequently calling `_unlockRng` at L404). This prevents the "target met but never resolves" stranded-state class.

### 6.1 Per-branch invariant table

Column shape per D-249-12: `Row ID | Branch trigger | State entered | Operations performed | State exited | _unlockRng called same-call? | Verdict`.

| Row ID | Branch trigger | State entered | Operations performed | State exited | _unlockRng called same-call? | Verdict |
|---|---|---|---|---|---|---|
| PLV-06-V01 | `purchaseLevel == 1` (L372) | coin-only path | `_emitDailyWinningTraits(1, rngWord, 1)` (L373); `_payDailyCoinJackpot(1, rngWord, 1, 1)` (L374); `_payDailyCoinJackpot(1, saltedRng, 2, 5)` (L383); falls through to L393 targetMet evaluation; if target met → L399 lastPurchaseDay=T + L401 compressedJackpotFlag=1 (conditional) + L404 _unlockRng | (per targetMet outcome — see V03/V04) | YES if targetMet | SAFE — falls through to V03/V04 evaluation |
| PLV-06-V02 | `purchaseLevel ≥ 2` (L385 else-branch) | daily-jackpot path | `payDailyJackpot(false, purchaseLevel, rngWord)` (L385); `_payDailyCoinJackpot(purchaseLevel, rngWord, purchaseLevel + 1, purchaseLevel + 4)` (L387-390); falls through to L393 targetMet evaluation | (per targetMet outcome) | YES if targetMet | SAFE — same fall-through |
| PLV-06-V03 | `targetMet = true` via `gameOverPossible` (L394 ternary T-branch) | target-met-via-gameover | L399 `lastPurchaseDay = true`; L400-402 conditional `compressedJackpotFlag = 1`; L404 `_unlockRng(day)`; L405 `stage = STAGE_PURCHASE_DAILY`; L406 `break;` | exits the targetMet branch with rngLocked=F + dailyIdx advanced + lastPurchaseDay=T set | **YES — L404 same call** | SAFE — strand-disproof holds (see §6.2) |
| PLV-06-V04 | `targetMet = true` via `_getNextPrizePool() >= levelPrizePool[purchaseLevel - 1]` (L394 ternary F-branch + L396-397) | target-met-via-pool-comparison | same as V03 (L399 / L400-402 / L404 / L405 / L406) | same | **YES — L404 same call** | SAFE — strand-disproof holds (see §6.2) |
| PLV-06-V05 | `targetMet = false` (L398 false branch) | not-target-met | NO `lastPurchaseDay = true` write; falls through to L404 `_unlockRng(day)`; L405 `stage = STAGE_PURCHASE_DAILY`; L406 `break;` | exits the not-target-met branch with rngLocked=F + dailyIdx advanced + lastPurchaseDay UNCHANGED | YES — L404 fires regardless of targetMet (the L398 if-block ends BEFORE L404) | SAFE — _unlockRng fires unconditionally inside the daily-jackpot branch |

### 6.2 Strand-disproof attestation (per CONTEXT.md `<specifics>` PLV-06 strand-disproof attestation suggestion)

> **Attestation:** Between `lastPurchaseDay = true` at AdvanceModule:399 and `_unlockRng(day)` at AdvanceModule:404, the only intervening statements are L400 `if (day - psd <= 3)` conditional with L401 `compressedJackpotFlag = 1;` body and L402 `}` closing brace. There is NO `revert`, `return`, `break`, or `continue` between L399 and L404 within the active branch (verified by `grep -nE '^\s*(return\|revert\|break\|continue);' /tmp/v32-249-dailyJackpot-region.sol` returning the L406 `break;` as the FIRST jump after L399, AFTER L404 has already fired). Therefore `targetMet ⇒ _unlockRng called same call` holds by code structure.

### 6.3 Phase 252 POST31-02 composition hand-off row (per D-249-13)

Per CONTEXT.md D-249-13: PLV-06 emits a one-row hand-off attestation noting the daily-jackpot strand-disproof composes with the `8bdeabc2` productive-pause `_pauseDeathClockDuringProductivePhase` (which clears `lastPurchaseDay || jackpotPhaseFlag` early in `_livenessTriggered`). Phase 252 POST31-02 inherits this row as a confirmed composition target.

Column shape per D-249-13: `Carrier ID | Compose target | Pre-PLV-06 envelope | Post-PLV-06 envelope | Phase 252 inheritance verdict`.

| Carrier ID | Compose target | Pre-PLV-06 envelope | Post-PLV-06 envelope | Phase 252 inheritance verdict |
|---|---|---|---|---|
| PLV-06-H01 | `8bdeabc2` productive-pause `_pauseDeathClockDuringProductivePhase` (cite Phase 247 D-247-C### row for the 8bdeabc2 hunk) | productive-pause clears `lastPurchaseDay \|\| jackpotPhaseFlag` early in `_livenessTriggered`; could in principle clear lastPurchaseDay=T set by daily-jackpot region L399 BEFORE _unlockRng resolves at L404 | PLV-06 §6.2 strand-disproof shows L399→L404 has zero intervening jumps, so productive-pause cannot interrupt the in-call sequence; productive-pause runs at advanceGame entry (or its callers) — by the time L404 _unlockRng fires, the in-call sequence has already committed lastPurchaseDay=T AND advanced dailyIdx | **CONFIRMED COMPOSITION TARGET** for Phase 252 POST31-02 — daily-jackpot resolution + productive-pause are mutually non-interfering at the in-call granularity; Phase 252 RE_VERIFIES this against the new turbo guard composition |
```

**Step D — Verify Task 3 deliverable:**

```
test -f audit/v32-249-PLV.md
grep -q '^## Section 5 — PLV-05: testnet panic 0x11 reproduction' audit/v32-249-PLV.md
! grep -q 'RESERVED FOR TASK 3' audit/v32-249-PLV.md
grep -q '^### 5.1 Pre-fix walk' audit/v32-249-PLV.md
grep -q '^### 5.2 Post-fix walk' audit/v32-249-PLV.md
grep -q '^### 5.3 Cross-cite to PLV-03' audit/v32-249-PLV.md
grep -q '10759449' audit/v32-249-PLV.md
grep -q '10761786' audit/v32-249-PLV.md
grep -q '^| PLV-05-V01 ' audit/v32-249-PLV.md
grep -q '^## Section 6 — PLV-06' audit/v32-249-PLV.md
grep -q '^### 6.1 Per-branch invariant table' audit/v32-249-PLV.md
grep -q '^### 6.2 Strand-disproof attestation' audit/v32-249-PLV.md
grep -q '^### 6.3 Phase 252 POST31-02 composition hand-off row' audit/v32-249-PLV.md
grep -q '^| PLV-06-V01 ' audit/v32-249-PLV.md
grep -q '^| PLV-06-V03 ' audit/v32-249-PLV.md
grep -q '^| PLV-06-V05 ' audit/v32-249-PLV.md
grep -q '^| PLV-06-H01 ' audit/v32-249-PLV.md
grep -q '8bdeabc2' audit/v32-249-PLV.md
! grep -q 'F-32-' audit/v32-249-PLV.md
[ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-05-V')" -ge 5 ]
[ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-06-V')" -ge 5 ]
test -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')"
```

**Step E — Atomic commit per D-247-14 carry-forward:**

```
git add audit/v32-249-PLV.md
git commit -m "$(cat <<'EOF'
audit(249-01): Task 3 — PLV-05 testnet walk + PLV-06 daily-jackpot no-strand + Phase 252 hand-off

Plan 249-01 Task 3 deliverable. Replaces Sections 5 + 6 RESERVED stubs
in audit/v32-249-PLV.md with populated content.

Sections written:
  - Section 5: PLV-05 — testnet panic 0x11 reproduction. BFL-03-style
    state-transition walk seeded by testnet blocks 10759449 + 10761786
    per D-249-CF-10. §5.1 pre-fix walk shows the turbo block firing
    while rngLocked=T sets lastPurchaseDay=T without level
    pre-increment, leading to L185 binding 0 and panic 0x11 at L397/
    L752. §5.2 post-fix walk shows L173 third conjunct !rngLockedFlag
    short-circuits → turbo block skipped → no L178 lastPurchaseDay
    write → L185 binds lvl+1 ≥ 1 → SAFE. §5.3 cross-cites PLV-03 §3.1
    Step 4 + PLV-04 §4.3 third link.
  - Section 6: PLV-06 — daily-jackpot region L370-407 no-strand
    proof. §6.1 per-branch invariant table (5 rows: purchaseLevel==1
    coin-only, purchaseLevel≥2 daily-jackpot, targetMet via
    gameOverPossible, targetMet via pool-comparison, targetMet=false)
    showing every branch reaches L404 _unlockRng same call. §6.2
    strand-disproof attestation paragraph showing zero
    revert/return/break/continue between L399 lastPurchaseDay=true
    and L404 _unlockRng(day). §6.3 Phase 252 POST31-02 composition
    hand-off row (PLV-06-H01) confirming productive-pause from
    8bdeabc2 composes non-interfering with daily-jackpot resolution.

Phase 247 catalog refs cited: D-247-I011 (PLV-05 row scope) +
D-247-I012 (PLV-06 row scope).

Pure-proof phase per D-249-CF-04 / D-249-CF-05 — zero contracts/ or
test/ writes.

Anchor: HEAD acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-249-PLV.md && grep -q '^## Section 5 — PLV-05: testnet panic 0x11 reproduction' audit/v32-249-PLV.md && grep -q '^### 5.1 Pre-fix walk' audit/v32-249-PLV.md && grep -q '^### 5.2 Post-fix walk' audit/v32-249-PLV.md && grep -q '^### 5.3 Cross-cite to PLV-03' audit/v32-249-PLV.md && grep -q '10759449' audit/v32-249-PLV.md && grep -q '10761786' audit/v32-249-PLV.md && grep -q '^## Section 6 — PLV-06' audit/v32-249-PLV.md && grep -q '^### 6.1 Per-branch invariant table' audit/v32-249-PLV.md && grep -q '^### 6.2 Strand-disproof attestation' audit/v32-249-PLV.md && grep -q '^### 6.3 Phase 252 POST31-02 composition hand-off row' audit/v32-249-PLV.md && grep -q '^| PLV-06-V01 ' audit/v32-249-PLV.md && grep -q '^| PLV-06-V03 ' audit/v32-249-PLV.md && grep -q '^| PLV-06-V05 ' audit/v32-249-PLV.md && grep -q '^| PLV-06-H01 ' audit/v32-249-PLV.md && grep -q '8bdeabc2' audit/v32-249-PLV.md && ! grep -q 'F-32-' audit/v32-249-PLV.md && [ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-05-V')" -ge 5 ] && [ "$(grep -v '^#' audit/v32-249-PLV.md | grep -c '^| PLV-06-V')" -ge 5 ] && [ -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')" ] && git log -1 --format='%s' | grep -qE '^audit\(249-01\): Task 3'</automated>
  </verify>
  <acceptance_criteria>
    - Section 5 (PLV-05) populated with §5.1 pre-fix walk (≥5 PLV-05-VNN rows ending with a row showing L185 binding to 0 and L397/L752 panic 0x11) + §5.2 post-fix walk (≥2 PLV-05-VNN rows showing L173 short-circuit + safe binding) + §5.3 cross-cite to PLV-03 §3.1 Step 4 + PLV-04 §4.3 third link
    - Both testnet block numbers `10759449` and `10761786` appear verbatim in §5
    - Section 6 (PLV-06) populated with §6.1 per-branch invariant table (≥5 PLV-06-VNN rows covering: purchaseLevel==1 coin-only, purchaseLevel≥2 daily-jackpot, targetMet via gameOverPossible, targetMet via pool-comparison, targetMet=false) + §6.2 strand-disproof attestation paragraph (zero revert/return/break/continue between L399 and L404) + §6.3 Phase 252 POST31-02 composition hand-off row (PLV-06-H01)
    - PLV-06-V03 + PLV-06-V04 (or equivalent rows for the two targetMet T-branches) carry verdict SAFE with `_unlockRng called same-call? = YES`
    - PLV-06-H01 row exists, names `8bdeabc2` productive-pause as compose target, and emits CONFIRMED COMPOSITION TARGET verdict
    - Sections 5 + 6 RESERVED markers REMOVED
    - Section 7 still exists as RESERVED stub for Task 4
    - File contains zero `F-32-` references
    - Working tree contains only the 2 pre-existing lines
    - Git log shows 1 new commit with subject `audit(249-01): Task 3`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-249-PLV.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
audit/v32-249-PLV.md Sections 5 + 6 populated. PLV-05 reproduces the testnet panic 0x11 via a two-walk format (pre-fix shows the bug; post-fix shows the L173 turbo guard short-circuits). PLV-06 enumerates 5 daily-jackpot branches and proves _unlockRng fires same-call in every branch where lastPurchaseDay=true is set; strand-disproof attestation paragraph cites zero intervening jumps between L399 and L404. PLV-06-H01 hand-off row confirms productive-pause composition target for Phase 252 POST31-02. One atomic commit landed.
  </deliverable>
  <done>Sections 5 + 6 populated; testnet block seeds 10759449 + 10761786 cited verbatim; ≥5 PLV-05 rows + ≥5 PLV-06 rows + PLV-06-H01 hand-off row; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Final assembly — Section 7 Finding Candidates summary + Phase 251 TST-01/02/03 Hand-Off appendix + FINAL READ-only flip + plan-close SUMMARY</name>
  <files>audit/v32-249-PLV.md, .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md</files>
  <read_first>
    - audit/v32-249-PLV.md (current state — exists with Sections 0-6 populated by Tasks 1-3; Section 7 is a RESERVED stub that Task 4 replaces in place; frontmatter `status: IN_PROGRESS` flips to `FINAL READ-only` in this task)
    - .planning/phases/249-purchaselevel-correctness-proof/249-CONTEXT.md — specifically D-249-CF-02 (single deliverable + READ-only flip on plan-close), D-249-CF-03 (no F-32-NN; Finding Candidates subsection format), D-249-CF-08 (scope-guard deferral rule for SUMMARY.md), `<specifics>` (Phase 251 hand-off block format suggestion: 3 sub-blocks — symbolic spec, suggested test file, Phase 247 row anchors)
    - audit/v32-248-BFL.md — §7 / Phase 251 TST-04 Hand-Off appendix format (the direct precedent for D-249-CF-05 hand-off)
    - .planning/phases/248-backfill-idempotency-proof/248-01-SUMMARY.md — direct format precedent for SUMMARY.md (5 sections: Plan Metadata, Atomic Commits, Per-REQ Deliverable Counts, Scope-Guard Deferrals, Hand-Off Signals)
    - .planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md — alternate SUMMARY.md format precedent

    HEAD-side reads at acd88512:
    - audit/v32-247-DELTA-SURFACE.md §1.4 / §2 / §3 / §6 rows that PLV-NN-VMM cites: D-247-C011 (turbo guard hunk) + D-247-F010 (advanceGame MODIFIED_LOGIC) + D-247-X027..X029 (advanceGame entry paths) + D-247-I007..I012 (PLV-01..06 row scope) — these are the Phase 247 row anchors the Phase 251 hand-off appendix references
  </read_first>
  <action>
**Step A — Aggregate cross-section Finding Candidates from Tasks 1-3:**

Inspect the populated `audit/v32-249-PLV.md` for any rows with verdict `FINDING_CANDIDATE` or `SAFE-pending-PLV-01-V31` or any row referenced inside §1.3 / §2.3 / §4.4 / §5.x / §6.x Finding Candidates subsections.

Most likely candidate per CONTEXT.md `<specifics>` first bullet: **PLV-01-V31 (MintModule:923)** if `(cachedJpFlag = T ∧ cachedLevel = 0)` cell was found REACHABLE in Task 1 §1.2.

Other potential candidates surfaced by Task 1-3 work:
- PLV-01-V32 (MintModule:924 priceForLevel) — if PriceLookupLib internal arithmetic underflows at purchaseLevel = 0 (boundary record per D-248-10 analog)
- Any PLV-04 row whose verdict deviates from SAFE (e.g., upper-bound oob if level cap rationale doesn't hold for cross-module sites)

**Step B — Replace Section 7 with the populated content (Finding Candidates summary + Phase 251 hand-off appendix):**

Replace the existing `## Section 7 — Finding Candidates + Phase 251 TST-01/02/03 Hand-Off Appendix (RESERVED FOR TASK 4)` stub.

```markdown
## Section 7 — Finding Candidates + Phase 251 TST-01/02/03 Hand-Off Appendix

### 7.1 Finding Candidates summary (omit subsection if zero candidates across §1-6)

Per CONTEXT.md D-249-CF-03: any PLV-NN-VMM row classified `FINDING_CANDIDATE` (or `SAFE-pending-...` that resolved to FINDING_CANDIDATE in subsequent tasks) is enumerated here with path:line + suggested severity for Phase 253 routing. NO `F-32-NN` IDs assigned in this phase.

[If Tasks 1-3 surfaced ZERO FINDING_CANDIDATE rows — the cleaner outcome — OMIT this entire subsection. Alternatively keep an explicit "Zero finding candidates surfaced; all PLV-NN-VMM rows verdict SAFE." attestation paragraph (recommended for documentation-of-zero-state symmetry with Phase 248 §6 attestation pattern).]

[If candidates surfaced, enumerate as bullets. The most likely candidate per CONTEXT.md `<specifics>` first bullet:]

- **MintModule:923 sibling ternary (PLV-01-V31)** — `cachedJpFlag ? cachedLevel : cachedLevel + 1` mirrors the AdvanceModule:185 ternary shape WITHOUT the `!rngLockedFlag` guard equivalent. Reachability of `(cachedJpFlag = T ∧ cachedLevel = 0)` was found <REACHABLE | UNREACHABLE> per §1.2 V31 evidence cite. <If REACHABLE: this is a same-shape sibling-pattern bug class on the mint path; routes to Phase 250 SIB-01 + Phase 253 FIND-01; suggested severity: HIGH (mirrors the v32.0 testnet panic 0x11 class on a different code path). If UNREACHABLE: V31 verdict resolves to SAFE; this candidate dropped.>
- [Append other candidates if surfaced.]

### 7.2 Phase 251 TST-01/02/03 Hand-Off Appendix

Per CONTEXT.md `<specifics>` Phase 251 hand-off block format suggestion: 3 sub-blocks — symbolic spec, suggested test file, Phase 247 row anchors. Phase 251 plan reads this hand-off as scope input for TST-01 (pre-fix panic 0x11 reproduction) + TST-02 (post-fix pass) + TST-03 (regression on LivenessProductivePause + LivenessMidJackpot).

#### 7.2.1 Symbolic spec for TST-01 (pre-fix panic 0x11 reproduction)

**Pre-state setup:** Deploy contracts at HEAD `acd88512` with the L173 `!rngLockedFlag` conjunct REVERTED (i.e., L173 reads `if (!inJackpot && !lastPurchaseDay)` with no third conjunct). All other state including the L1174 backfill guard remains.

**Trigger sequence:** Recreate the testnet trigger geometry per PLV-05 §5.1 walk:
1. Player completes a transaction that sets prior-cycle state where `rngLockedFlag = T` is observed at advanceGame entry while `lastPurchaseDay = F` and `level` was NOT pre-incremented (the rngGate fresh-word path took a branch that did not call _finalizeRngRequest's L1609 increment).
2. Next advanceGame call enters the turbo block at L167-182 (without the L173 third conjunct, the IF condition `!inJackpot && !lastPurchaseDay` evaluates TRUE while rngLocked=T).
3. Turbo body sets `lastPurchaseDay = true` at L178; `gameOverPossible = false` at L180.
4. Next advanceGame call evaluates L185 ternary with `(lastPurchase = T ∧ rngLockedFlag = T)` → returns `lvl = 0`.
5. Downstream `levelPrizePool[uint24(0) - 1]` at L397 (or L752 if `_distributeYieldSurplus` path) panics with 0x11 underflow.

**Expected pre-fix revert:** Panic 0x11 (arithmetic underflow) at AdvanceModule:397 (primary trigger) or L752 (secondary trigger, depending on which call-tree branch executes first).

#### 7.2.2 Symbolic spec for TST-02 (post-fix pass)

**Pre-state setup:** Deploy contracts at HEAD `acd88512` with L173 `!rngLockedFlag` conjunct ACTIVE (the WIP fix as committed in `acd88512`).

**Trigger sequence:** Identical to TST-01.

**Expected post-fix behavior:** At Step 2, L173 `!rngLockedFlag` evaluates F (because rngLocked=T) → turbo block SKIPPED → no `lastPurchaseDay = true` write at L178 → next advanceGame call evaluates L185 with `(lastPurchase = F)` → ternary picks `lvl + 1` branch → purchaseLevel ≥ 1 → no panic at L397/L752. Test PASSES with no revert; verify expected state transitions hold.

#### 7.2.3 Symbolic spec for TST-03 (regression on prior liveness tests)

**Pre-state setup:** Same as TST-02 (post-fix).

**Tests to run:**
- `test/edge/LivenessProductivePause.test.js` (committed in `8bdeabc2`)
- `test/edge/LivenessMidJackpot.test.js` (committed in `ad41973c`)

**Expected behavior:** Both tests pass without modification. The L173 turbo guard does NOT regress productive-pause logic (cross-cite PLV-06 §6.3 PLV-06-H01 hand-off row showing the two are mutually non-interfering at in-call granularity).

#### 7.2.4 Suggested test file

`test/edge/LastPurchaseDayRace.test.js` (currently untracked at HEAD acd88512 per Phase 247 D-247-02 carry-forward; Phase 251 final call on path + harness).

#### 7.2.5 Phase 247 row anchors

The TST-01/02/03 hand-off exercises the following Phase 247 catalog rows (cite verbatim from `audit/v32-247-DELTA-SURFACE.md`):
- **D-247-C011** — acd88512 commit changelog row (advanceGame turbo guard L173 hunk)
- **D-247-F010** — advanceGame MODIFIED_LOGIC classification
- **D-247-X027 / X028 / X029** — 3 advanceGame entry paths (DegenerusGame.sol delegatecall + DegenerusVault.sol cross-contract + StakedDegenerusStonk.sol cross-contract)
- **D-247-I007..I012** — Consumer Index rows mapping PLV-01..06 to Phase 249 deliverable

### 7.3 Scope-guard deferrals (per D-249-CF-08; routes to SUMMARY.md)

[If Tasks 1-3 surfaced any cross-module sites NOT in Phase 247 D-247-I007..I012 catalog, enumerate here with path:line + reason; route to Phase 250 SIB-01/02/03 sweep scope per D-249-CF-08. The same list goes into 249-01-SUMMARY.md `## Scope-Guard Deferrals` section.]

[Default expectation: zero deferrals if Phase 247 catalog was complete.]
```

**Step C — Flip frontmatter to FINAL READ-only:**

Update the YAML frontmatter at the top of `audit/v32-249-PLV.md` from:
```yaml
status: IN_PROGRESS — Plan 249-01 single-plan multi-task
```
to:
```yaml
status: FINAL READ-only — Plan 249-01 plan-close at HEAD acd88512 (closure signal PHASE_249_PLV_FINAL_AT_HEAD_acd88512)
```

Also add a frontmatter line:
```yaml
closure_signal: PHASE_249_PLV_FINAL_AT_HEAD_acd88512
final_at: <ISO-8601 timestamp>
```

**Step D — Create plan-close SUMMARY.md:**

Create `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md` per CONTEXT.md `<truths>` ("Plan-close summary with 4 atomic per-task commit SHAs, per-REQ row counts, scope-guard deferrals (if any), Phase 249 → Phase 250 / 251 / 252 / 253 hand-off signals") + format precedent from 248-01-SUMMARY.md.

```markdown
---
phase: 249-purchaselevel-correctness-proof
plan: 249-01
status: COMPLETE
head: acd88512
closure_signal: PHASE_249_PLV_FINAL_AT_HEAD_acd88512
deliverable: audit/v32-249-PLV.md
deliverable_status: FINAL READ-only
requirements_satisfied: [PLV-01, PLV-02, PLV-03, PLV-04, PLV-05, PLV-06]
---

# Plan 249-01 Summary — purchaseLevel Correctness Proof

## Plan Metadata

- **Phase:** 249 — purchaseLevel Correctness Proof
- **Milestone:** v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
- **Plan ID:** 249-01 (single plan, 4 tasks per D-249-CF-07)
- **Anchor:** HEAD `acd88512` (inherited from Phase 247 / 248)
- **Closure signal:** `PHASE_249_PLV_FINAL_AT_HEAD_acd88512`
- **Deliverable:** `audit/v32-249-PLV.md` FINAL READ-only at HEAD `acd88512`

## Atomic Commits

| Task | Commit SHA | Subject |
|---|---|---|
| Task 1 | <fill from `git log`> | audit(249-01): Task 1 — PLV-01 + PLV-02 enumeration at HEAD acd88512 |
| Task 2 | <fill> | audit(249-01): Task 2 — PLV-03 ternary unreachable proof + PLV-04 arithmetic flat table |
| Task 3 | <fill> | audit(249-01): Task 3 — PLV-05 testnet walk + PLV-06 daily-jackpot no-strand + Phase 252 hand-off |
| Task 4 | <fill> | audit(249-01): Task 4 — Final assembly + READ-only flip + plan-close |

## Per-REQ Deliverable Counts

| REQ | Section | Row count | Verdict breakdown | Notes |
|---|---|---|---|---|
| PLV-01 | §1 | <NN> readsite rows (≥30 AdvanceModule + ≥10 cross-module) | <SAFE: NN, EXCEPTION: NN, FINDING_CANDIDATE: NN> | MintModule:923 row (V31) classified per reachability outcome |
| PLV-02 | §2 | 24 octant cells (8 octants × 3 level bins) | <REACHABLE-SAFE: NN, UNREACHABLE: NN, OOS: NN> | INV-PLV-A-01 / B-01 / C-01 named-invariant disproofs |
| PLV-03 | §3 | 4-step guard-evaluation walk | n/a — formal proof | closes O-PLV-TTF/lvl=0 unreachable cell |
| PLV-04 | §4 | <NN> arithmetic-site rows (≥19) | <all SAFE expected; -1 rows SAFE-via-PLV-03> | both -1 sites cross-cite PLV-02 + PLV-03 + PLV-05 per D-249-08 |
| PLV-05 | §5 | <NN> pre-fix walk rows + <NN> post-fix walk rows (≥7 total) | n/a — symbolic walk | testnet blocks 10759449 + 10761786 |
| PLV-06 | §6 | 5 branch rows + 1 hand-off row (PLV-06-H01) | <all SAFE> | strand-disproof attestation; Phase 252 POST31-02 composition target confirmed |

## Scope-Guard Deferrals

[Per D-249-CF-08: any changed function / state-var / event / interface method / call site found NOT in Phase 247 catalog gets recorded here. Default expected: zero deferrals.]

[If non-zero, enumerate as bullets: `- <path>:<line> — <description> — route to Phase 250 SIB-XX / Phase 253 FIND-XX`.]

## Hand-Off Signals

- **→ Phase 250 (Sibling-Pattern Sweep):** PLV-01 wider scope flagged MintModule:923 sibling ternary as <FINDING_CANDIDATE | SAFE>; if FINDING_CANDIDATE, Phase 250 SIB-01 inherits as live finding-candidate seed (avoiding tail-end discovery per CONTEXT.md `<code_context>` "Phase 249 → Phase 250" integration point).
- **→ Phase 251 (Reproduction Tests):** Phase 249 deliverable §7.2 emits a 3-block hand-off appendix (symbolic specs for TST-01/02/03 + suggested test file `test/edge/LastPurchaseDayRace.test.js` + Phase 247 row anchors D-247-C011 + D-247-F010 + D-247-X027..X029 + D-247-I007..I012). Phase 251 plan reads this hand-off as scope input.
- **→ Phase 252 (Post-v31.0 Landed-Commit Sanity):** Phase 249 deliverable §6.3 emits PLV-06-H01 composition hand-off row confirming `8bdeabc2` productive-pause composes non-interfering with daily-jackpot resolution. Phase 252 POST31-02 RE_VERIFIES this composition with PLV-06-H01 as confirmed input target.
- **→ Phase 253 (Findings Consolidation):** Any FINDING_CANDIDATE row in §7.1 routes to Phase 253 FIND-01..04 finding-block emission per D-249-CF-03. NO `F-32-` IDs assigned in this phase.

## Verifier Reproduction

Reproduction recipe at HEAD `acd88512`:
1. `git rev-parse acd88512` → `acd88512c516bef51981d8b6f49de9878aba9159`
2. Read `audit/v32-249-PLV.md` end-to-end; every PLV-NN-VMM row has a verifier-reproducible grep recipe in the Evidence column.
3. Universe-of-readsites grep: `git grep -n 'purchaseLevel' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' 'contracts/storage/*.sol'` (≥70 hits expected per CONTEXT.md `<code_context>`).
4. Universe-of-arithmetic grep: `git grep -nE 'purchaseLevel\s*[+\-]\s*[0-9]\|purchaseLevel\s*%' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol'`.
5. Sole-writer grep for the 3 named invariants: `git grep -nE '\b(lastPurchaseDay\|rngLockedFlag\|jackpotPhaseFlag\|level)\s*=' acd88512 -- 'contracts/storage/*.sol' 'contracts/modules/*.sol' 'contracts/*.sol'`.
```

**Step E — Verify Task 4 deliverable:**

```
test -f audit/v32-249-PLV.md
test -f .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Section 7 — Finding Candidates' audit/v32-249-PLV.md
! grep -q 'RESERVED FOR TASK 4' audit/v32-249-PLV.md
! grep -qE 'RESERVED FOR TASK [1-4]' audit/v32-249-PLV.md
grep -q '^### 7.2 Phase 251 TST-01/02/03 Hand-Off Appendix' audit/v32-249-PLV.md
grep -q '7.2.1 Symbolic spec for TST-01' audit/v32-249-PLV.md
grep -q '7.2.2 Symbolic spec for TST-02' audit/v32-249-PLV.md
grep -q '7.2.3 Symbolic spec for TST-03' audit/v32-249-PLV.md
grep -q '7.2.4 Suggested test file' audit/v32-249-PLV.md
grep -q '7.2.5 Phase 247 row anchors' audit/v32-249-PLV.md
grep -q 'D-247-C011' audit/v32-249-PLV.md
grep -q 'D-247-F010' audit/v32-249-PLV.md
grep -q 'D-247-X027' audit/v32-249-PLV.md
grep -q 'LastPurchaseDayRace.test.js' audit/v32-249-PLV.md
grep -q 'LivenessProductivePause' audit/v32-249-PLV.md
grep -q 'LivenessMidJackpot' audit/v32-249-PLV.md
grep -qE '^status: FINAL READ-only' audit/v32-249-PLV.md
grep -q 'PHASE_249_PLV_FINAL_AT_HEAD_acd88512' audit/v32-249-PLV.md
grep -q 'PHASE_249_PLV_FINAL_AT_HEAD_acd88512' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Plan Metadata' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Atomic Commits' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Per-REQ Deliverable Counts' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Scope-Guard Deferrals' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
grep -q '^## Hand-Off Signals' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
! grep -q 'F-32-' audit/v32-249-PLV.md
! grep -q '<fill' audit/v32-249-PLV.md
[ "$(wc -l < audit/v32-249-PLV.md)" -ge 350 ]
[ "$(wc -l < .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md)" -ge 50 ]
test -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')"
```

**Step F — Atomic plan-close commit per D-247-14 carry-forward:**

```
git add audit/v32-249-PLV.md .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
git commit -m "$(cat <<'EOF'
audit(249-01): Task 4 — Final assembly + Phase 251 hand-off + READ-only flip

Plan 249-01 Task 4 deliverable. Final assembly + plan-close.

Sections written / updated:
  - Section 7: Finding Candidates summary (§7.1) + Phase 251 TST-01/
    02/03 Hand-Off appendix (§7.2.1-7.2.5: symbolic specs for TST-01
    pre-fix panic 0x11 reproduction + TST-02 post-fix pass + TST-03
    regression on LivenessProductivePause + LivenessMidJackpot;
    suggested test file `test/edge/LastPurchaseDayRace.test.js`;
    Phase 247 row anchors D-247-C011 + D-247-F010 + D-247-X027..X029
    + D-247-I007..I012) + §7.3 scope-guard deferrals
  - Frontmatter status flipped from IN_PROGRESS to FINAL READ-only
  - Closure signal PHASE_249_PLV_FINAL_AT_HEAD_acd88512 emitted

Plan-close SUMMARY.md created at
.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md
with 4 atomic commit SHAs, per-REQ row counts, scope-guard deferrals
(if any), and hand-off signals to Phases 250 / 251 / 252 / 253.

Pure-proof phase per D-249-CF-04 / D-249-CF-05 — zero contracts/ or
test/ writes throughout the plan.

Anchor: HEAD acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-249-PLV.md && test -f .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Section 7 — Finding Candidates' audit/v32-249-PLV.md && ! grep -qE 'RESERVED FOR TASK [1-4]' audit/v32-249-PLV.md && grep -q '^### 7.2 Phase 251 TST-01/02/03 Hand-Off Appendix' audit/v32-249-PLV.md && grep -q '7.2.1 Symbolic spec for TST-01' audit/v32-249-PLV.md && grep -q '7.2.2 Symbolic spec for TST-02' audit/v32-249-PLV.md && grep -q '7.2.3 Symbolic spec for TST-03' audit/v32-249-PLV.md && grep -q 'D-247-C011' audit/v32-249-PLV.md && grep -q 'D-247-F010' audit/v32-249-PLV.md && grep -q 'D-247-X027' audit/v32-249-PLV.md && grep -q 'LastPurchaseDayRace.test.js' audit/v32-249-PLV.md && grep -q 'LivenessProductivePause' audit/v32-249-PLV.md && grep -q 'LivenessMidJackpot' audit/v32-249-PLV.md && grep -qE '^status: FINAL READ-only' audit/v32-249-PLV.md && grep -q 'PHASE_249_PLV_FINAL_AT_HEAD_acd88512' audit/v32-249-PLV.md && grep -q 'PHASE_249_PLV_FINAL_AT_HEAD_acd88512' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Plan Metadata' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Atomic Commits' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Per-REQ Deliverable Counts' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Scope-Guard Deferrals' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && grep -q '^## Hand-Off Signals' .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md && ! grep -q 'F-32-' audit/v32-249-PLV.md && ! grep -q '<fill' audit/v32-249-PLV.md && [ "$(wc -l < audit/v32-249-PLV.md)" -ge 350 ] && [ "$(wc -l < .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md)" -ge 50 ] && [ -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')" ] && git log -1 --format='%s' | grep -qE '^audit\(249-01\): Task 4'</automated>
  </verify>
  <acceptance_criteria>
    - Section 7 (Finding Candidates summary + Phase 251 hand-off appendix) populated; the `RESERVED FOR TASK 4` marker REMOVED
    - §7.1 Finding Candidates summary either enumerates surfaced candidates with path:line + suggested severity OR carries an explicit zero-state attestation paragraph (Phase 248 §6 pattern)
    - §7.2 Phase 251 hand-off appendix has all 5 sub-blocks: 7.2.1 (TST-01 symbolic spec) + 7.2.2 (TST-02 symbolic spec) + 7.2.3 (TST-03 symbolic spec) + 7.2.4 (suggested test file `test/edge/LastPurchaseDayRace.test.js`) + 7.2.5 (Phase 247 row anchors D-247-C011 + D-247-F010 + D-247-X027..X029 + D-247-I007..I012)
    - §7.3 Scope-guard deferrals subsection exists (zero-state if no deferrals)
    - All RESERVED markers across §3-§7 REMOVED (no `RESERVED FOR TASK [1-4]` strings in the file)
    - Frontmatter status reads `FINAL READ-only — Plan 249-01 plan-close at HEAD acd88512 (closure signal PHASE_249_PLV_FINAL_AT_HEAD_acd88512)`
    - Closure signal `PHASE_249_PLV_FINAL_AT_HEAD_acd88512` appears in both audit/v32-249-PLV.md frontmatter AND .planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md frontmatter
    - 249-01-SUMMARY.md created with 5 sections: Plan Metadata + Atomic Commits + Per-REQ Deliverable Counts + Scope-Guard Deferrals + Hand-Off Signals (per CONTEXT.md `<truths>` enumeration)
    - SUMMARY.md `## Atomic Commits` section enumerates 4 commits (Tasks 1-4) with verbatim subject lines from `git log`
    - SUMMARY.md `## Hand-Off Signals` section names Phase 250 / 251 / 252 / 253 inheritance targets
    - Audit deliverable ≥ 350 lines; SUMMARY.md ≥ 50 lines (per must_haves min_lines)
    - File contains zero `F-32-` references
    - File contains no `<fill` placeholder tokens
    - Working tree contains only the 2 pre-existing lines
    - Git log shows 1 new commit with subject `audit(249-01): Task 4`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-249-PLV.md` + `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
audit/v32-249-PLV.md FINAL READ-only at HEAD acd88512 with closure signal PHASE_249_PLV_FINAL_AT_HEAD_acd88512. All 7 sections populated with concrete content; zero RESERVED markers remain. Section 7 emits Finding Candidates summary + 5-sub-block Phase 251 hand-off appendix (symbolic specs for TST-01/02/03 + suggested test file + Phase 247 row anchors). 249-01-SUMMARY.md created with 5-section format documenting 4 atomic commits + per-REQ row counts + scope-guard deferrals + hand-off signals to Phases 250/251/252/253. One atomic plan-close commit landed.
  </deliverable>
  <done>Section 7 populated; all RESERVED markers removed; frontmatter flipped to FINAL READ-only; closure signal emitted in both files; SUMMARY.md created with 5-section format; one atomic plan-close commit landed.</done>
</task>

</tasks>

<verification>
**Per-task verification (atomic commit gate):**

Each task ends with a `git commit` of the deliverable changes. The pre-commit gate per task:
- All `<verify><automated>` commands return exit code 0.
- Working tree contains ONLY the 2 pre-existing lines (`M contracts/ContractAddresses.sol` per D-247-03 + `?? test/edge/LastPurchaseDayRace.test.js` per D-247-02). Zero new contracts/ or test/ writes.
- `git log -1 --format='%s'` matches the expected `audit(249-01): Task N — <summary>` pattern.

**Plan-close verification (Task 4 final state):**

- `audit/v32-249-PLV.md` is FINAL READ-only with frontmatter `status: FINAL READ-only` + closure signal `PHASE_249_PLV_FINAL_AT_HEAD_acd88512`.
- `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md` exists with 5-section format + 4 atomic commit SHAs.
- All 6 PLV REQ-IDs are addressed in concrete sections (PLV-01 §1, PLV-02 §2, PLV-03 §3, PLV-04 §4, PLV-05 §5, PLV-06 §6).
- Hand-off signals to Phases 250 / 251 / 252 / 253 are emitted per the integration points in CONTEXT.md `<code_context>`.
- No `F-32-NN` IDs anywhere (D-249-CF-03 enforcement).
- No placeholder tokens (`<fill`, `REPLACE-WITH`, `RESERVED FOR TASK N`).
- Pure-proof phase boundary held: 4 atomic commits all touch ONLY `audit/v32-249-PLV.md` (Tasks 1-4) + `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md` (Task 4 only).

**Cross-section coverage attestation (must hold at plan close):**

- §1 PLV-01: ≥40 readsite rows covering every hit in the universe-of-readsites grep at HEAD acd88512.
- §2 PLV-02: 24 octant cells with 3 named-invariant disproofs (INV-PLV-A-01 / B-01 / C-01) + 8-octant table FFF..TTT.
- §3 PLV-03: 4-step guard-evaluation walk closing the O-PLV-TTF/lvl=0 unreachable cell formally.
- §4 PLV-04: ≥19 arithmetic-site rows; both -1 sites (V07-A at L397 + V11-A at L752) carry SAFE-via-PLV-03 with three-link cross-cite chain (PLV-02 + PLV-03 + PLV-05) per D-249-08; both upper-bound oob rows (V07-B + V11-B) per D-249-09.
- §5 PLV-05: 2-walk format (pre-fix + post-fix) seeded by testnet blocks 10759449 + 10761786.
- §6 PLV-06: 5-branch invariant table + strand-disproof attestation paragraph + Phase 252 POST31-02 composition hand-off row PLV-06-H01.
- §7: Finding Candidates summary + 5-sub-block Phase 251 hand-off appendix.
</verification>

<success_criteria>
**Plan complete when:**

1. `audit/v32-249-PLV.md` exists at HEAD acd88512 with all 7 sections populated, FINAL READ-only frontmatter status, closure signal `PHASE_249_PLV_FINAL_AT_HEAD_acd88512`, and ≥350 lines.
2. All 6 PLV REQ-IDs (PLV-01..06) satisfied per ROADMAP Phase 249 success criteria checklist (mapped to §1-6 + §7 hand-off appendix).
3. 4 atomic per-task commits landed in git log with subjects matching `audit(249-01): Task N — <summary>` pattern (Tasks 1-4).
4. `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md` exists with 5-section format + plan-close commit SHA referenced inline.
5. Zero `F-32-NN` IDs anywhere in the deliverable (D-249-CF-03).
6. Zero `contracts/` or `test/` writes throughout the plan (D-249-CF-04 / D-249-CF-05); working tree contains only the 2 pre-existing lines.
7. All cross-cite chains land: PLV-04 §4.3 -1 rows three-link to PLV-02 + PLV-03 + PLV-05; PLV-05 §5.3 cross-cite to PLV-03 + PLV-04; PLV-06 §6.3 hand-off row PLV-06-H01 confirmed for Phase 252 POST31-02.
8. Hand-off signals to Phases 250 (MintModule:923 sibling-pattern seed) + 251 (TST-01/02/03 symbolic specs) + 252 (PLV-06-H01 productive-pause composition) + 253 (any FINDING_CANDIDATE rows route to FIND-01..04) all emitted in §7 + SUMMARY.md `## Hand-Off Signals`.
9. Universe-of-readsites grep `git grep -n 'purchaseLevel' acd88512 -- 'contracts/*.sol' 'contracts/modules/*.sol' 'contracts/storage/*.sol'` is cited verbatim in §1 evidence column per D-249-CF-09 grep-reproducibility.
10. All 3 named invariants (INV-PLV-A-01 / B-01 / C-01) carry concrete anchor cites at HEAD acd88512 line numbers; the grep recipes for sole-writer attestations are cited verbatim.

**Failure modes (each is a STOP gate; route to scope-guard deferral per D-249-CF-08):**

- Phase 247 catalog gap surfaces a readsite NOT in D-247-I007..I012 → record in SUMMARY.md `## Scope-Guard Deferrals`; do NOT silently extend deliverable.
- MintModule:923 reachability proof inconclusive → flag as Phase 250 SIB-01 hand-off + Phase 253 FIND-01 candidate; do NOT force a verdict.
- Source line numbers drift vs frontmatter `canonical_line_ranges` → re-verify with `git show acd88512:<path>` and append discrepancy to SUMMARY.md `## Scope-Guard Deferrals`.
- Working tree drift (any new `M`/`??` line beyond the 2 pre-existing) → STOP; DO NOT commit; surface to user.
</success_criteria>

<output>
After plan completion (Task 4 plan-close commit):

1. `audit/v32-249-PLV.md` FINAL READ-only at HEAD `acd88512` with closure signal `PHASE_249_PLV_FINAL_AT_HEAD_acd88512` (mirrors Phase 247 / 248 closure-signal pattern).
2. `.planning/phases/249-purchaselevel-correctness-proof/249-01-SUMMARY.md` (plan-close summary; consumed by `/gsd-execute-phase` orchestrator + Phase 250-253 plans as scope-input precedent).
3. 4 atomic per-task commits landed in git log:
   - `audit(249-01): Task 1 — PLV-01 + PLV-02 enumeration at HEAD acd88512`
   - `audit(249-01): Task 2 — PLV-03 ternary unreachable proof + PLV-04 arithmetic flat table`
   - `audit(249-01): Task 3 — PLV-05 testnet walk + PLV-06 daily-jackpot no-strand + Phase 252 hand-off`
   - `audit(249-01): Task 4 — Final assembly + Phase 251 hand-off + READ-only flip`
4. Hand-off signals emitted (consumed by downstream phase plans):
   - Phase 250 SIB-01: MintModule:923 sibling-ternary candidate (FINDING_CANDIDATE-or-SAFE per V31 verdict)
   - Phase 251 TST-01/02/03: 5-sub-block symbolic-spec hand-off appendix in §7.2
   - Phase 252 POST31-02: PLV-06-H01 productive-pause composition target
   - Phase 253 FIND-01..04: any FINDING_CANDIDATE row routes here

Update STATE.md (handled by `/gsd-execute-phase` orchestrator, NOT by this plan):
- Mark Phase 249 COMPLETE
- Closure signal: `PHASE_249_PLV_FINAL_AT_HEAD_acd88512`
- Next: `/gsd-plan-phase 250` (Sibling-Pattern Sweep)
</output>