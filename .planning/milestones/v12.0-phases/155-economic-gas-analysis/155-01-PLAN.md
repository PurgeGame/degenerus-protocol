---
phase: 155-economic-gas-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md
autonomous: true
requirements:
  - ECON-01
  - ECON-02
  - GAS-01
  - GAS-02

must_haves:
  truths:
    - "Worst-case BURNIE inflation from level quests is bounded with concrete numbers"
    - "Expected-case BURNIE inflation is modeled with realistic assumptions"
    - "creditFlip is proven to NOT affect futurePool or drip projection"
    - "Eligibility check gas overhead is quantified with SLOAD counts"
    - "Level quest roll gas overhead is quantified and compared to advanceGame ceiling"
  artifacts:
    - path: ".planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md"
      provides: "Complete economic + gas analysis document"
      contains: "ECON-01 SATISFIED"
    - path: ".planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md"
      provides: "gameOverPossible interaction analysis"
      contains: "ECON-02 SATISFIED"
    - path: ".planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md"
      provides: "Eligibility gas analysis"
      contains: "GAS-01 SATISFIED"
    - path: ".planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md"
      provides: "Quest roll gas analysis"
      contains: "GAS-02 SATISFIED"
  key_links:
    - from: "Phase 153 spec Section 7 SLOAD/SSTORE budgets"
      to: "GAS-01 and GAS-02 estimates"
      via: "SLOAD count extraction and gas cost calculation"
      pattern: "SLOAD.*2,100|SSTORE.*22,100"
    - from: "Phase 152 gas baseline (6,996,000 worst-case)"
      to: "GAS-02 ceiling comparison"
      via: "Delta addition to worst-case"
      pattern: "safety margin"
---

<objective>
Produce a single analysis document quantifying BURNIE inflation impact and gas overhead of level quests, confirming the feature is economically and computationally viable.

Purpose: Phase 155 closes the v12.0 milestone by proving level quests are safe to implement -- no runaway inflation, no endgame flag interference, and no gas ceiling breach.
Output: `.planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/153-core-design/153-01-LEVEL-QUEST-SPEC.md
@.planning/phases/152-delta-audit/152-02-GAS-ANALYSIS.md

<interfaces>
<!-- Key contracts and functions the analysis must reference -->

From contracts/BurnieCoinflip.sol (creditFlip mechanism):
```solidity
// creditFlip writes BURNIE-denominated stake to coinflipBalance[targetDay][player].
// It does NOT transfer ETH, does NOT touch futurePool, nextPool, or any prize pool state.
function creditFlip(address player, uint256 amount) external onlyFlipCreditors {
    if (player == address(0) || amount == 0) return;
    _addDailyFlip(player, amount, 0, false, false);
}
// _addDailyFlip writes to: coinflipBalance[targetDay][player] (BURNIE internal ledger only)
```

From contracts/modules/DegenerusGameAdvanceModule.sol (gameOverPossible):
```solidity
// _evaluateGameOverPossible reads ETH prize pools only:
//   _getNextPrizePool() — ETH in next pool (Packed Slot 1)
//   _getFuturePrizePool() — ETH in future pool (packed slot)
//   levelPrizePool[purchaseLevel-1] — ETH target
//   _projectedDrip(futurePool, daysRemaining) — pure ETH arithmetic
// gameOverPossible = _projectedDrip(...) < deficit
// NO BURNIE state is read. NO coinflip state is read.
uint256 private constant DECAY_RATE = 0.9925 ether;
```

From contracts/storage/DegenerusGameStorage.sol:
```solidity
// futurePool: ETH pool that drips into nextPool over time
// gameOverPossible: bool flag based on drip projection vs nextPool deficit
bool internal gameOverPossible;
```

Phase 153 Spec Section 7 — SLOAD/SSTORE budgets:
```
Eligibility check:       1-2 SLOADs (mintPacked_ + optional deityPassCount)
Quest roll (level tx):   0 extra SLOADs + 1 SSTORE (levelQuestType[purchaseLevel])
Progress handler:        2 SLOADs (levelQuestType + levelQuestPlayerState) + 1 SSTORE
Completion check:        0 extra (already loaded from progress check)
Target derivation:       0-1 extra SLOADs (ETH types need mintPrice, likely warm)
```

Phase 152 Gas Baseline:
```
Block gas limit:            14,000,000
Worst-case subsequent batch: 6,996,000 gas
Safety margin:              2.00x
WRITES_BUDGET_SAFE:         550 write-units
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write economic and gas analysis document</name>
  <files>.planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md</files>
  <read_first>
    - .planning/phases/153-core-design/153-01-LEVEL-QUEST-SPEC.md (Section 1 eligibility gas, Section 2 quest roll, Section 7 SLOAD/SSTORE budgets)
    - .planning/phases/152-delta-audit/152-02-GAS-ANALYSIS.md (advanceGame gas ceiling baseline)
    - contracts/BurnieCoinflip.sol lines 892-921 (creditFlip — confirm BURNIE-only, no ETH flow)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1639-1659 (_evaluateGameOverPossible — confirm ETH-only reads)
    - contracts/storage/DegenerusGameStorage.sol (gameOverPossible declaration, futurePool)
  </read_first>
  <action>
Create `.planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md` with the following sections:

**Section 1: BURNIE Inflation Model (ECON-01, per D-01, D-02)**

Worst-case analysis:
- Payout: 800 BURNIE per level per eligible player (via creditFlip, Section 6 of spec)
- Worst-case assumption: ALL eligible players complete the quest EVERY level
- Player count bounds: use reasonable protocol bounds (e.g., 100, 500, 1000 active eligible players)
- Level frequency: levels advance when nextPool fills — estimate levels per month from mint price + pool mechanics. Use conservative assumption of ~1 level per day at steady state (levels can be faster or slower).
- Calculate: worst-case monthly BURNIE inflation = (eligible players) * (levels/month) * 800 BURNIE
- Table: 100/500/1000 players x 30 levels/month worst-case

Expected case:
- Realistic eligible fraction: levelStreak >= 5 OR pass holder, estimated ~30-50% of active players
- Realistic completion rate: 10x targets are hard. Estimate ~20-40% completion rate for eligible players.
- Calculate: expected monthly inflation = (active players) * (eligible fraction) * (completion rate) * (levels/month) * 800

Comparison to existing BURNIE flows (per D-02):
- Document existing mint sources: ticket mints (1000 BURNIE each), quest rewards, coinflip wins
- Document existing burn sinks: coinflip losses (burned by BurnieCoin.burnForCoinflip), decimator burns, lootbox burns
- Contextual comparison: 800 BURNIE per completion is less than 1 ticket purchase (1000 BURNIE). Level quest rewards are a small fraction of daily BURNIE throughput.
- Note: creditFlip credits are not new BURNIE minted — they are stake credits that participate in the coinflip system. Winnings from flips mint new BURNIE; losses burn the stake. Net inflation depends on flip win rate (~47-50%).

Write verdict: "ECON-01 SATISFIED" with summary.

**Section 2: gameOverPossible Interaction (ECON-02, per D-03)**

Trace the data flow:
1. Level quest completion calls `coinflip.creditFlip(player, 800 ether)` (Section 6 of spec)
2. `creditFlip` calls `_addDailyFlip(player, amount, 0, false, false)` (BurnieCoinflip.sol line 900)
3. `_addDailyFlip` writes to `coinflipBalance[targetDay][player]` — a BURNIE-denominated internal ledger in BurnieCoinflip contract
4. NO ETH is transferred. NO state in DegenerusGameStorage is modified. NO prize pool variable is touched.

Trace gameOverPossible's inputs:
1. `_evaluateGameOverPossible` reads: `_getNextPrizePool()` (ETH), `levelPrizePool[purchaseLevel-1]` (ETH), `_getFuturePrizePool()` (ETH), `levelStartTime`
2. `_projectedDrip` is pure arithmetic on ETH values (futurePool * geometric decay)
3. `gameOverPossible = _projectedDrip(...) < deficit` — entirely ETH-denominated

Conclusion: creditFlip and gameOverPossible operate in completely disjoint state domains (BURNIE ledger vs ETH prize pools). Level quest payouts have ZERO effect on the endgame drip projection. No adjustment to the drip projection formula is needed.

Write verdict: "ECON-02 SATISFIED" with summary.

**Section 3: Eligibility Check Gas (GAS-01, per D-04)**

Extract from Phase 153 spec Section 1:
- Best case: 1 SLOAD (mintPacked_ short-circuits loyalty + activity gate). Hot: ~100 gas. Cold: ~2,100 gas.
- Worst case: 2 SLOADs (mintPacked_ + deityPassCount fallback). Hot: ~200 gas. Cold: ~4,200 gas.
- Context: in the quest handler hot path, mintPacked_ is always warm from the mint operation that triggered the handler. So realistic cost is 100-200 gas.
- Arithmetic overhead: bit shifts, comparisons, boolean logic — ~50-80 gas total.
- Total eligibility check: ~150-280 gas (hot path).

This runs on EVERY handleX() call for players. Compare to existing handler cost (thousands of gas for daily quest progress tracking) — eligibility is a negligible addition.

Write verdict: "GAS-01 SATISFIED" with summary.

**Section 4: Level Quest Roll Gas in advanceGame (GAS-02, per D-05, D-06)**

Extract from Phase 153 spec Section 2 and Section 7:
- Quest roll occurs once per level transition in phaseTransitionActive block
- SLOADs: 0 extra (rngWordByDay[day] already warm from rngGate)
- SSTOREs: 1 new — `levelQuestType[purchaseLevel]` = 22,100 gas (cold, new non-zero value)
- Computation: keccak256 hash (30 gas), modulo + weight scan (~100 gas), decimator eligibility check (~200 gas warm)
- Total quest roll overhead: ~22,430 gas

Compare to Phase 152 baseline:
- Phase 152 worst-case: 6,996,000 gas
- Add quest roll: 6,996,000 + 22,430 = 7,018,430 gas
- Updated safety margin: 14,000,000 / 7,018,430 = 1.99x (effectively unchanged from 2.00x)
- Delta: +0.32%

Also estimate progress handler overhead (runs per handleX call, not in advanceGame):
- 2 SLOADs: levelQuestType[level] (cold 2,100, hot 100) + levelQuestPlayerState[player] (cold 2,100, hot 100)
- 1 SSTORE: levelQuestPlayerState[player] update — 5,000 gas (dirty existing) or 22,100 gas (new non-zero first write)
- Target derivation: 0 extra for BURNIE types, 0-1 extra SLOAD for ETH types (mintPrice from Slot 1, likely warm)
- Total handler overhead: ~4,400-26,400 gas depending on cold/hot and first-write

This is a per-transaction cost borne by the player, NOT part of the advanceGame ceiling. It does not affect the 14M block limit analysis.

Write verdict: "GAS-02 SATISFIED" with summary.

**Section 5: Combined Verdict**

All four requirements satisfied. Level quests are economically viable (bounded inflation, no endgame interference) and computationally viable (negligible eligibility cost, quest roll fits within advanceGame ceiling with margin preserved).
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md | grep -q "4" && echo "PASS: all 4 requirements marked satisfied" || echo "FAIL: missing requirement verdicts"</automated>
  </verify>
  <acceptance_criteria>
    - Document contains Section 1 with worst-case and expected-case BURNIE inflation tables, ending with "ECON-01 SATISFIED"
    - Document contains Section 2 with creditFlip data flow trace proving no futurePool interaction, ending with "ECON-02 SATISFIED"
    - Document contains Section 3 with eligibility check SLOAD counts and gas estimates, ending with "GAS-01 SATISFIED"
    - Document contains Section 4 with quest roll SSTORE cost + advanceGame ceiling comparison showing margin preserved, ending with "GAS-02 SATISFIED"
    - All gas figures trace to Phase 153 spec Section 7 and Phase 152 baseline
    - No speculative claims — every number has a source (spec section, contract line, EIP gas schedule)
  </acceptance_criteria>
  <done>Analysis document exists with all 4 requirement verdicts satisfied, BURNIE inflation bounded, gameOverPossible interaction disproven, and gas overhead within ceiling</done>
</task>

</tasks>

<verification>
- grep for "ECON-01 SATISFIED", "ECON-02 SATISFIED", "GAS-01 SATISFIED", "GAS-02 SATISFIED" in the output file
- Verify worst-case inflation table has at least 3 player-count scenarios
- Verify advanceGame gas comparison references Phase 152 baseline (6,996,000)
- Verify safety margin calculation shows >= 1.99x
</verification>

<success_criteria>
- Single analysis document covers all 4 requirements with concrete numbers
- BURNIE inflation is bounded with worst-case and expected-case models
- gameOverPossible interaction is proven absent via data flow trace
- Gas overhead fits within existing 14M block ceiling with >= 1.99x margin
- All figures cite their source (Phase 153 spec section, Phase 152 baseline, EIP gas costs)
</success_criteria>

<output>
After completion, create `.planning/phases/155-economic-gas-analysis/155-01-SUMMARY.md`
</output>
