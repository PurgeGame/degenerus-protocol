---
phase: 391-rng-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md
  - .planning/phases/391-rng-spine/council/rng.gemini.txt
  - .planning/phases/391-rng-spine/council/rng.codex.txt
  - .planning/phases/391-rng-spine/council/rng.council.json
  - .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md
autonomous: true
requirements: [RNG-01, RNG-02, RNG-03, RNG-04, RNG-05, RNG-06]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full RNG-FREEZE slice (backward-trace of every new/changed consumer / decimator uint32 entropy floor + distribution-bias / box-spin replay-safety + domain-separation / resolveLootboxDirect dropped-amount seed collision / redemption day+1 gate + day-boundary divergence / in-window SLOAD enumeration over the repacked slots)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the by-design rulings (lootbox-resolution timing, Degenerette RTP, the documented EV changes) excluded so the council does not waste passes on settled intent, and with the exact frozen RNG line-cites supplied so the council reads the right code"
    - "The decimator uint32 distribution-bias target (RNG-02 / FC-391-04, the §6 prime, the MISSING-property the oracle-holes audit flagged) is charged HARD as a dedicated named break-target demanding a real distribution/bias argument — not hand-waving"
    - "The backward-trace doctrine is encoded: every consumer must be confirmed to consume a word UNKNOWN at the player's input-commitment time; every SLOAD inside an rng-window must be enumerated (not just VRF-derived seeds)"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md"
      provides: "Neutral council prompt for the RNG-01..06 slice + the FC-391-01..05 leads + the inherited cross-refs (FC-389-05 decimator-uint32 gas-half, FC-392-11 stochastic sDGNRS auto-rebuy + claimCoinflipCarry RNG-lock coverage)"
      min_lines: 45
    - path: ".planning/phases/391-rng-spine/391-01-COUNCIL-NET.md"
      provides: "Council-net capture record: raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line"
      min_lines: 20
  key_links:
    - from: "391-01-COUNCIL-PROMPT-RNG.md"
      to: "council.sh --label rng"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label rng"
    - from: "391-01-COUNCIL-NET.md"
      to: "council/rng.council.json"
      via: "manifest available/skipped capture"
      pattern: "rng\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the RNG-FREEZE spine — the VRF-word freshness / freeze-window
correctness of every new and changed RNG consumer in the post-v62 change set (the box-spins, the
survival flip, the redemption day+1 leg, the decimator uint32 claim-seed, the resolveLootboxDirect seed
change, the in-window SLOADs over the repacked slots) — against the byte-frozen subject `a8b702a7`.
Author ONE neutral council prompt for the RNG-01..06 slice (plus the FC-391-01..05 owned leads and the
inherited cross-refs FC-389-05 and FC-392-11), fan it to the gemini+codex CLIs via `council.sh`, capture
the raw output under the phase `council/` dir, and record the council-net capture (available/skipped,
byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
RNG/freeze is the DOMINANT threat class (§4 threat weighting) — a confirmed RNG-manipulability or freeze
break is the highest-severity class — so this slice is planned with the most rigor, and the council — the
PRIMARY finder ([[cross-model-led-audits-over-claude-only]], the council caught the V62-01/RNGRETRY/RNGREUSE
classes Claude-only missed) — goes on record FIRST so the Wave-2 Claude-net + adjudication plan (391-02)
can fold the council leads in before any verdict.

Output: the COUNCIL-PROMPT-RNG doc, the raw gemini/codex output, the council.json manifest, and
391-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/rng-freeze.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "label", "schema", "models":[...], "skipped":[...], "outputs":{model:path} } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed CLI is recorded in skipped[] and -->
<!-- is NEVER fatal. gemini + codex CLIs confirmed at ~/.local/bin. -->

<!-- Council-prompt shape reference (the just-approved 389/390 slices): -->
<!--   .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md -->
<!--   .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format (FINDING: PROPERTY · reachable CALL SEQUENCE · STATE -->
<!--   VAR + file:line at a8b702a7 · SEVERITY · why protections don't stop it | or VERIFIED SOUND + why). -->

<!-- Authoritative frozen RNG line-cites (verified at a8b702a7 — supply these so the council reads the -->
<!-- right code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   EntropyLib.sol: hash2(a,b) @23 (keccak256(abi.encode(a,b)) full-width); hash1(a) @38 (single-word). -->
<!--   DegenerusGameLootboxModule.sol: manual open seed keccak256(abi.encode(rngWord,player,amount)) @560; -->
<!--     resolveLootboxDirect @874 (payable; seed = EntropyLib.hash2(rngWord, uint160(player)) @883 -->
<!--     — amount DROPPED vs baseline); resolveAfkingBox live-level twin seed -->
<!--     keccak256(abi.encode(rngWord,player,day,amount)) @1084; _resolveLootboxRoll @1965 (dispatches -->
<!--     the 3 box-spin rolls; allowEthSpin gate @1245/1973); recirc entry allowEthSpin=false @889; -->
<!--     redemption ETH-spin allowEthSpin=true pool-pre-flush @976. -->
<!--   DegenerusGameDegeneretteModule.sol: BURNIE survival flip hash2(rngWord,betId)&1 @773 -->
<!--     (acc.burnieMint += @774 / -= @777); the spin seed/event hash2(rngWord,betId) @791; bet -->
<!--     delete degeneretteBets[player][betId] @655; acc.burnieMint struct field @413, mintForGame @447; -->
<!--     resolveWwxrpSpinFromBox @1292 (guard address(this)!=GAME @1298; S==9 bracket=level/10 @1324 live -->
<!--     OUTPUT gate); resolveBurnieSpinsFromBox @1347 (guard @1353; survival hash2(seed,BOX_SURVIVAL_TAG -->
<!--     0x537572766976616c)&1 @1385); resolveEthSpinFromBox @1402 (guard @1408); regular-bet WWXRP -->
<!--     bracket level/10 @750 (the unchanged twin). -->
<!--   DegenerusGameDecimatorModule.sol: winner select decBucketOffsetPacked[lvl] write @269; the claim -->
<!--     seed narrowing round.rngWord = uint32(rngWord) @277; claimDecimatorJackpot permissionless @293 -->
<!--     (reads decBucketOffsetPacked[lvl] @312); claimDecimatorJackpotMany @325 (caches packedOffsets -->
<!--     @338, over=gameOver @341 ONCE); _claimDecimatorJackpotFor @385 (over branch @401, passes -->
<!--     round.rngWord @410); _awardDecimatorLootbox @645 (delegatecalls resolveLootboxDirect.selector -->
<!--     @673 with round.rngWord). -->
<!--   StakedDegenerusStonk.sol: rngWordForDay(day) iface @43; BurnsBlockedBeforeDailyRng error @120; -->
<!--     _claimRedemptionFor @821 (lootbox leg reads game.rngWordForDay(day+1) @878, entropy = -->
<!--     hash2(rngWord, uint160(player)) @879); _submitGamblingClaimFrom @976 (currentPeriod = -->
<!--     GameTimeLib.currentDayIndex() @983; gate if rngWordForDay(currentPeriod)==0 revert @991; -->
<!--     stamp != currentPeriod -> PriorDayUnresolved @997; _pendingResolveDay = currentPeriod @998). -->
<!--   BurnieCoinflip.sol: win/loss (rngWord & 1)==1 path; processCoinflipPayouts onlyDegenerusGameContract -->
<!--     @204 modifier; autoRebuyCarry uint128 @154; sdgnrsAutoRebuyArmed @174; settle carry roll -->
<!--     application @412-417; claimCoinflipCarry @754 (rngLocked guard @759, carry @770). -->
<!--   AdvanceModule.sol: hash2(rngWord, FUTURE_KEEP_TAG) keep-roll @~969; gameover historical combine -->
<!--     hash2(combined, w) @~1400; redemption roll (currentWord>>8)%151+25 @~1259; processCoinflipPayouts -->
<!--     calls @1245/1309/1344/1844; requestLootboxRng index-advance + pending-clear (mid-day gate). -->
<!--   RngWindowFreezeHandler.sol authoritative in-window slots @a8b702a7: rngWordByDay slot 10; -->
<!--     lootboxRngPacked slot 34 (low 48 = index cursor); lootboxRngWordByIndex slot 35; dailyIdx slot 0 -->
<!--     byte 3 (uint24). These are the freeze-spine slots to enumerate for RNG-06. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the RNG-FREEZE slice</name>
  <files>.planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md</files>
  <read_first>
    - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md AND .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md (the just-approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, per-finding output format)
    - .planning/v63-surface-map/rng-freeze.md (the full backward-trace thesis §1-§10 + the summary table of all RNG consumers + the "Top leads for the adversarial sweep" — the "what we believe is freeze-safe" thesis to break)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-391-01..05 verbatim + the inherited cross-refs FC-389-05 [decimator uint32 gas-narrowing half] and FC-392-11 [stochastic sDGNRS auto-rebuy backing + claimCoinflipCarry RNG-lock coverage, cross-ref 391])
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md (the "Decimator uint32 claim-seed — note on the entropy-distribution oracle" section: test #8 proves SLOT-ISOLATION + claim-path-reached but NO oracle asserts the per-bucket reward DISTRIBUTION is unbiased across many winners of one level — the MISSING property routed to 391 as RNG-02 / FC-391-04)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (RNG/freeze = DOMINANT) + §5 design-intent anchor (RTP/EV by-design; verify freeze-safety + distribution-unbiasedness, not desirability)
    - the v45 north-star + the RNG-audit doctrine: trace BACKWARD from each consumer to confirm the VRF word was unknown at input-commitment; enumerate ALL SLOADs inside the rng-window (not just VRF-derived seeds); check what player-controllable state can change between VRF request and fulfillment
    - the frozen source for the prime sites via git show a8b702a7:contracts/modules/DegenerusGameDecimatorModule.sol (the uint32 narrowing @277, the claim path @293-410, _awardDecimatorLootbox @645-673), contracts/modules/DegenerusGameLootboxModule.sol (resolveLootboxDirect @874-883, manual open @560, _resolveLootboxRoll @1965), contracts/modules/DegenerusGameDegeneretteModule.sol (box-spin resolvers @1292/1347/1402, survival flip @773-791, bet delete @655), contracts/StakedDegenerusStonk.sol (the redemption gate @976-998, the day+1 read @878), contracts/libraries/EntropyLib.sol (hash1 @38 / hash2 @23)
  </read_first>
  <action>
    Write 391-01-COUNCIL-PROMPT-RNG.md, charged NEUTRALLY ("here is what we believe is freeze-safe about
    every new/changed RNG consumer in the post-v62 change set — find where the freeze property or the
    entropy adequacy breaks") and instructing the council to read the EXACT frozen source at `a8b702a7`
    via `git show a8b702a7:contracts/<File>.sol` (ignore the working tree). Use neutral
    defensive-engineering vocabulary throughout (word, seed, freeze window, commitment, fulfillment,
    domain-separation, entropy, distribution, one-shot, replay) per [[neutral-defensive-audit-vocabulary]].

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 stated so the council knows RNG/FREEZE IS THE
    DOMINANT class for this slice (a confirmed RNG-manipulability or freeze break is the highest severity
    — weigh accordingly; HIGH gas-DoS only in the advanceGame chain; SPINE = solvency; LOW
    access-control/reentrancy/MEV);
    (b) the trust-boundary / freeze-doctrine framing so the council does not waste passes: state the v45
    north-star (every variable that interacts with a VRF word must be frozen between rng-request and
    unlock relative to players; the word must have been UNKNOWN when the player committed their input; no
    player-controllable non-VRF SLOAD inside the rng-window may change between request and fulfillment),
    and instruct the council to trace BACKWARD from each consumer to the commitment point and to
    enumerate EVERY SLOAD inside an rng-window (not just the VRF-derived seed);
    (c) a KNOWN-BY-DESIGN exclusion list so the council does not re-litigate settled intent
    ([[lootbox-resolution-timing-by-design]] lootbox claim/open TIMING is not a player edge — the seed is
    frozen at request, so do NOT flag day/level/wait-to-open steering as a freshness bug;
    [[degenerette-wwxrp-rtp-by-design]] Degenerette RTP>100% and the near-worthless WWXRP token are
    by-design — the council's job is freeze-safety + distribution-unbiasedness, NOT EV desirability;
    [[intended-game-mechanics-not-findings]]; the documented EV changes [EV-multiplier lift floor 90% /
    ceiling 145% / score-to-ceiling 40,000; recycle-bonus relaxation; EV-neutral redistributions] are
    economics audited in the 392 slice — here we only care whether the RNG that drives them is fresh and
    adequately-entropic; the settled-prior-day far-future mint salvage quote being publicly known is
    by-design [it is a pricing quote, not a payout RNG]);
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE
    [multi-tx / observe-then-retrigger where the ordering matters] · STATE VAR + file:line at a8b702a7 ·
    SEVERITY per the threat priority · WHY the existing freeze/one-shot/domain-separation protections do
    not stop it | or VERIFIED SOUND/IDENTICAL: the property and the specific reason it holds — cite the
    commitment point, the tag, the one-shot guard, or the bound — so the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to RNG-01..06, as the claims to BREAK:
    "(RNG-01) every new/changed RNG consumer descends from a word committed BEFORE the player's input
    (the per-index lootbox anchor fixed at index advance after deposit; the per-bet nonce fixed at
    placement before the word lands; the day's VRF word inside the rngLocked window) — find any consumer
    whose seed admits a live, post-reveal, player-controllable input; (RNG-02) the decimator uint32
    claim-seed retains an adequate entropy floor, is non-grindable, AND yields an UNBIASED aggregate
    per-bucket reward distribution across many winners of one level; (RNG-03) the box-spin resolvers
    (WWXRP/BURNIE/ETH) are ONE-SHOT and replay-safe — a player cannot observe a spin outcome and
    re-trigger a fresh spin with a now-known word; (RNG-04) resolveLootboxDirect + every caller's spin
    seed are domain-separated so no two same-word/same-player resolutions collide (the dropped `amount`
    term no longer disambiguates); (RNG-05) the redemption day+1 pre-draw gate holds on the burn side —
    no zero-seed grind, and the local day calc cannot diverge from the game's day index at a boundary;
    (RNG-06) every SLOAD inside an rng-window over the repacked slots is freeze-invariant — no
    player-controllable non-VRF read is consumed alongside the word."

    Name the concrete break-targets. Charge the §6-prime decimator-distribution target HARD as the lead
    dedicated numbered item:

    1. RNG-02 / FC-391-04 (MED, §6 prime — the DEDICATED distribution-bias target, the MISSING property
       the oracle-holes audit flagged): `DecClaimRound.rngWord` was narrowed uint256→uint32
       (`round.rngWord = uint32(rngWord)`, DecimatorModule:277). This is the claim-time LOOTBOX seed ONLY
       — winning subbuckets were already selected from the FULL word at snapshot (`decBucketOffsetPacked`,
       written @269). At claim, the uint32 word flows _claimDecimatorJackpotFor (@385, passes
       round.rngWord @410) → _awardDecimatorLootbox (@645) → resolveLootboxDirect (delegatecall
       @673) → seed = hash2(uint32-word, uint160(player)) @LootboxModule:883. The narrowing is freeze-safe
       (the 32-bit word is fixed at decimator resolution, before any claim; the player can't choose it; the
       address-mix makes the per-player draw effectively full-entropy). DO NOT re-litigate freeze-safety.
       INSTEAD demand a REAL distribution argument: across the WHOLE winning-bucket population of ONE level
       (potentially many winners sharing the SAME 32-bit word, differing only by their 160-bit address in
       hash2), is the JOINT distribution of per-claim lootbox tier outcomes biased, correlated, or
       grindable? Charge the council to find (i) any aggregate per-bucket reward-distribution bias the
       32-bit floor introduces (e.g. a correlation across winners sharing the word that a multi-account
       actor could exploit, or a tier-outcome skew vs the full-word baseline), (ii) any grinding/retry-timing
       edge given the permissionless deterministic claim, or to state VERIFIED SOUND with the concrete
       reason (the per-player hash2(word, address) decorrelates winners; the 32-bit floor over the bucket
       population is adequate because <reason>). A hand-wave ("address-mixed so fine") is NOT acceptable —
       require the distribution reasoning.

    Then name the remaining owned leads + inherited cross-refs as numbered break-targets at their map
    severity:

    2. RNG-04 / FC-391-01 (LOW-MED): resolveLootboxDirect (LootboxModule:874) dropped the `amount` term —
       seed is now `hash2(rngWord, uint160(player))` (@883) while the manual open (@560) and resolveAfkingBox
       (@1084) STILL include amount. Caller-side domain separation is now load-bearing: Degenerette passes
       hash2(rngWord,betId), decimator passes the narrowed round.rngWord, redemption passes
       hash2(rngWordForDay(day+1),player), ETH-spin recirc passes hash2(seed,BOX_RECIRC_TAG). Charge the
       council to find ANY caller path that feeds TWO same-word/same-player resolutions through
       resolveLootboxDirect that would now collide on the seed (the amount term used to disambiguate them).
       Prove no-collision OR surface a finding.

    3. RNG-03 / FC-391-02 (INFO-LOW): the box-spins (WWXRP 15% / BURNIE 10% / ETH 5%) are a NEW consumer
       off the permissionless lootbox-open / bet-resolution surface (resolvers @DegeneretteModule:1292/1347/
       1402, each guarded `address(this) != GAME` @1298/1353/1408). Charge the council to find ANY path
       where a player can OBSERVE a spin outcome and then re-trigger a fresh spin with a now-known word —
       i.e. confirm the lootbox record is zeroed / the bet is `delete`d (@655) BEFORE resolution so the
       spin can only resolve once, and that the module guard + record-clear together fully prevent replay.

    4. RNG-03/RNG-01 / FC-391-03 (LOW): the BURNIE survival flip (DegeneretteModule:773) double-or-nothings
       every BURNIE bet payout on `hash2(rngWord, betId) & 1` (acc.burnieMint += @774 / -= @777, summed then
       mintForGame @447). Both rngWord (the committed per-index anchor) and betId (the placement nonce) are
       committed before fulfillment. Charge the council to (i) confirm the flip is single-shot per bet and
       freeze-safe, and (ii) confirm `acc.burnieMint -=` cannot TRANSIENTLY underflow the unsigned running
       total across a multi-bet batch (acc is shared across bets; the per-bet additions net to zero for the
       same bet, but confirm cross-bet ordering can't drive the running total negative before a later +=).

    5. RNG-05 / FC-391-05 (LOW): the redemption day+1 pre-draw gate. Burn admitted only once
       `currentPeriod`'s word is recorded (`if game.rngWordForDay(currentPeriod)==0 revert
       BurnsBlockedBeforeDailyRng`, StakedDegenerusStonk:991; currentPeriod = GameTimeLib.currentDayIndex()
       @983). The lootbox leg reads `game.rngWordForDay(day + 1)` (@878) — day+1 is NOT yet drawn at burn
       time. Charge the council to find ANY day-boundary where GameTimeLib.currentDayIndex() can diverge
       from the game's dailyIdx so a burn stamps a day whose `day+1` word is ALREADY on-chain (a zero-seed
       / known-draw grind) — spell out the wall-day-vs-advance-lag interleavings to try (a wall day rolled
       past dailyIdx but the advance hasn't run; a same-block burn racing the advance). Prove the gate pins
       a drawn day by construction OR surface a finding.

    6. Inherited cross-ref FC-389-05 (INFO/LOW, the gas-identity narrowing half of the decimator uint32):
       the narrowing-equivalence half — confirm `round.rngWord = uint32(rngWord)` is a deterministic
       fixed-at-resolution value with predictability-WITHOUT-control (player + word frozen, permissionless
       deterministic claim), and that the uint32 packing into the DecClaimRound slot does not corrupt a
       co-resident field (this couples to 389's STORAGE check but the RNG-consumption half is owned here —
       ask the council to confirm the narrowed word's grind/retry-timing posture).

    7. Inherited cross-ref FC-392-11 (MED, RNG-lock-coverage half — owned by 392 for the backing dynamics,
       cross-ref here for the RNG-lock): `claimCoinflipCarry` (BurnieCoinflip:754) reverts on
       `degenerusGame.rngLocked()` (@759) and reads the carry (@770); processCoinflipPayouts is
       onlyDegenerusGameContract (@204) and applies the win/loss roll in the advance walk. Charge the
       council to VERIFY the rngLocked guard FULLY covers the roll application — i.e. that there is no
       window where the pending day's stake word is already on-chain (resolvable) yet a carry claim / settle
       can read or act on the roll outcome before the lock blocks it (the v62 RNG-lock-bypass class). The
       backing/EV half is 392's; here confirm the RNG-lock window over the carry roll is airtight.

    Also charge a sweep of the RNG-06 in-window SLOAD enumeration: over the repacked freeze-spine slots
    (rngWordByDay slot 10, lootboxRngPacked slot 34 [low 48 = index cursor], lootboxRngWordByIndex slot 35,
    dailyIdx slot 0 byte 3 — the authoritative `forge inspect` slots at a8b702a7), ask the council to
    enumerate EVERY SLOAD consumed alongside a VRF word in the daily-resolution / lootbox-resolution
    windows and confirm none is a player-controllable NON-VRF read that can change between request and
    fulfillment (the EntropyLib hash1/hash2 migrations are claimed byte-identical preimages; the
    activityScore threaded into spin EV is claimed a FROZEN snapshot unpacked from the record, not a live
    read — ask the council to break those two claims specifically).

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 391-02. No
    fenced solidity blocks beyond the minimal identifier/selector/line citations the council needs to
    locate the code.
  </action>
  <verify>
    <automated>test -s .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md && grep -q "a8b702a7" .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md && for id in RNG-01 RNG-02 RNG-03 RNG-04 RNG-05 RNG-06 FC-391-01 FC-391-02 FC-391-03 FC-391-04 FC-391-05 FC-389-05 FC-392-11; do grep -q "$id" .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "backward|committed before|unknown when" .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md && grep -Eqi "distribution|uint32|entropy" .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md && grep -Eqi "SLOAD|in-window|rng-window" .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md</automated>
  </verify>
  <acceptance_criteria>
    391-01-COUNCIL-PROMPT-RNG.md exists and is non-empty; instructs reading frozen source at `a8b702a7`
    via git show; states the v45 north-star freeze doctrine + the backward-trace instruction + the
    in-window SLOAD enumeration instruction as the thesis to break; names ALL of RNG-01..06 +
    FC-391-01..05 + the inherited cross-refs FC-389-05 and FC-392-11; charges the §6-prime decimator
    uint32 distribution-bias target (RNG-02 / FC-391-04) as a DEDICATED numbered break-target demanding a
    real distribution argument (not a hand-wave); carries the KNOWN-BY-DESIGN exclusion list (lootbox
    timing, Degenerette/WWXRP RTP, the documented EV changes, the public salvage quote); uses neutral
    vocabulary; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral RNG-FREEZE council prompt is authored against the frozen subject, covering all 6 RNG reqs
    + the 5 FC-391 owned leads + the 2 inherited cross-refs, with the decimator-distribution prime target
    charged hard and the backward-trace + in-window-SLOAD doctrine encoded, ready to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the council fan-out and record the council-net capture</name>
  <files>.planning/phases/391-rng-spine/council/rng.gemini.txt, .planning/phases/391-rng-spine/council/rng.codex.txt, .planning/phases/391-rng-spine/council/rng.council.json, .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md (Task 1 output)
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md (the just-approved COUNCIL-NET capture record shape to match)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/391-rng-spine/council --label rng
    .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md`. council.sh fans gemini+codex in parallel
    internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations is needed
    beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped CLI is
    recorded in the council.json skipped[] and is NOT fatal; do not treat a skip as a failure.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify). Do NOT
    run `hardhat compile --force` (regenerates ContractAddresses source).

    Write 391-01-COUNCIL-NET.md recording: the rng.council.json manifest's available[]/skipped[], the raw
    output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 391-02), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for RNG-FREEZE" line so
    391-02 can confirm both-nets-on-record before any verdict. If a CLI is in skipped[], record WHICH and
    note the slice still has the other model on record (a single available model satisfies "council on
    record" with the skip documented; both-unavailable would require a re-run and must be surfaced, not
    silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/391-rng-spine/council/rng.council.json && test -s .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `rng.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable
    CLI is in the council.json skipped[]; 391-01-COUNCIL-NET.md records the available/skipped + raw output
    paths + the byte-freeze attestation + an explicit "NET 1 ON RECORD" line; `git diff a8b702a7 --
    contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the RNG-FREEZE slice with raw output captured under
    the phase council/ dir, the subject proven byte-frozen, and the capture documented for the Wave-2
    adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable CLI silently dropping the only net (handled by skipped[]) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-391-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir |
| T-391-02 | Information disclosure / false-green | the RNG slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 391-01-COUNCIL-NET.md; both-unavailable is surfaced for re-run, never passed silently |
| T-391-03 | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers RNG-01..06 + FC-391-01..05 +
  the inherited cross-refs FC-389-05 and FC-392-11, with the decimator uint32 distribution-bias prime
  target charged hard and the backward-trace + in-window-SLOAD doctrine encoded.
- `council.sh` ran for `--label rng`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[].
- 391-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full RNG-FREEZE spine, with raw gemini/codex output
captured under the phase council/ dir and the capture documented, so the Wave-2 Claude-net + adjudication
plan can fold the council leads in before issuing any per-item verdict. The subject is proven byte-frozen
throughout.
</success_criteria>

<output>
Create `.planning/phases/391-rng-spine/391-01-SUMMARY.md` when done
</output>
</content>
