---
phase: 391-rng-spine
plan: 02
type: execute
wave: 2
depends_on: ["391-01"]
files_modified:
  - .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md
  - .planning/phases/391-rng-spine/391-FINDINGS.md
autonomous: true
requirements: [RNG-01, RNG-02, RNG-03, RNG-04, RNG-05, RNG-06]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full RNG-FREEZE surface, independent of the council"
    - "Every RNG-01..06 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the commitment-point / bound / source-cite that settles it"
    - "Every FC-391-01..05 owned lead AND every inherited cross-ref (FC-389-05, FC-392-11) has an explicit ADJUDICATED verdict folded with the matching council lead"
    - "The backward-trace doctrine is applied to EVERY new/changed consumer: the word is proven UNKNOWN at the player's input-commitment time, with the commitment point cited (RNG-01)"
    - "RNG-02 / FC-391-04 (decimator uint32 distribution-bias) is settled by a dedicated REAL distribution argument over the winning-bucket population of one level — proven unbiased/non-grindable with the concrete reasoning, OR surfaced as a finding; NOT hand-waved (this is the MISSING property the oracle-holes audit flagged)"
    - "RNG-06 in-window SLOAD enumeration is performed over the repacked freeze-spine slots (rngWordByDay@10, lootboxRngPacked@34, lootboxRngWordByIndex@35, dailyIdx@slot0byte3) — every read consumed alongside a VRF word in the resolution windows is enumerated and proven freeze-invariant or surfaced"
    - "Both nets are on record before the verdict is issued; the skeptic gate is run before any CATASTROPHE/HIGH (RNG/freeze is the DOMINANT class — a confirmed break is highest-severity)"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/391-rng-spine/391-02-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the RNG-FREEZE surface, independent of the council, with per-req / per-lead backward-traces + the dedicated decimator distribution analysis + the in-window SLOAD enumeration"
      min_lines: 90
    - path: ".planning/phases/391-rng-spine/391-FINDINGS.md"
      provides: "The per-item adjudication table: every RNG req + every FC-391 lead + every inherited cross-ref with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding"
      min_lines: 90
  key_links:
    - from: "391-FINDINGS.md"
      to: "391-01 council outputs (council/*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "391-FINDINGS.md"
      to: "RNG-01..06 + FC-391-01..05 + inherited cross-refs"
      via: "per-item adjudicated verdict rows"
      pattern: "RNG-02|FC-391-04"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME RNG-freeze surface the council swept, then
ADJUDICATE every RNG-01..06 requirement, every FC-391-01..05 owned lead, and every inherited cross-ref
(FC-389-05, FC-392-11) against the byte-frozen subject `a8b702a7`, folding in the NET-1 council leads from
391-01. Apply the v45 backward-trace doctrine to every consumer, give the decimator uint32
distribution-bias target (RNG-02 / FC-391-04) a dedicated rigorous real-distribution treatment, enumerate
every in-window SLOAD over the repacked freeze-spine slots (RNG-06), run the skeptic gate before
presenting anything as CATASTROPHE/HIGH, and write `391-FINDINGS.md` with a per-item verdict table
(CONFIRMED / REFUTED / BY-DESIGN / MONITOR) each carrying the commitment-point or source-cite that settles
it.

Purpose: this is the verdict step for the DOMINANT threat class. A no-finding verdict for any sweep slice
requires BOTH nets on record (AUDIT-V63-PLAN §2) — this plan puts NET 2 on record and synthesizes both
nets. RNG/freeze is the highest-priority dimension (§4); a confirmed RNG-manipulability or freeze break is
the highest-severity class, so the decimator distribution prime target and the backward-trace +
in-window-SLOAD doctrine get dedicated rigorous treatment. Any CONFIRMED finding is DOCUMENTED + ROUTED to
a gated USER-hand-review fix, never fixed here (the subject is byte-frozen and re-freezes only after a
separate gated boundary).

Output: 391-02-CLAUDE-NET.md (the independent adversarial analysis) and 391-FINDINGS.md (the adjudication
of the full phase-391 surface). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md
@.planning/v63-surface-map/rng-freeze.md
@.planning/phases/391-rng-spine/391-01-COUNCIL-NET.md
@.planning/phases/391-rng-spine/council/rng.gemini.txt
@.planning/phases/391-rng-spine/council/rng.codex.txt
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 6 reqs to adjudicate (REQUIREMENTS.md RNG): -->
<!--   RNG-01 every new/changed consumer backward-traced — VRF word unknown at input-commitment; -->
<!--   RNG-02 decimator uint32 claim-seed: entropy floor + non-grindable + UNBIASED per-bucket dist; -->
<!--   RNG-03 box-spin resolvers (WWXRP/BURNIE/ETH) one-shot + replay-safe; -->
<!--   RNG-04 resolveLootboxDirect + spin seeds domain-separated (no cross-consumer collision); -->
<!--   RNG-05 redemption day+1 pre-draw gate (BurnsBlockedBeforeDailyRng) holds; no zero-seed grind; -->
<!--   RNG-06 every SLOAD inside an rng-window over the repacked slots is freeze-invariant. -->
<!-- The 5 owned leads (388-02-FINDING-CANDIDATES row D): FC-391-01..05. -->
<!-- The 2 inherited cross-refs: FC-389-05 (decimator uint32 gas-narrowing-equivalence half), -->
<!--   FC-392-11 (stochastic sDGNRS auto-rebuy backing — RNG-lock-coverage half owned here; the -->
<!--   backing/EV dynamics half owned by 392). -->
<!-- Authoritative frozen RNG line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   EntropyLib.sol: hash2 @23 (keccak256(abi.encode) full-width); hash1 @38. -->
<!--   DegenerusGameLootboxModule.sol: manual open keccak256(abi.encode(rngWord,player,amount)) @560; -->
<!--     resolveLootboxDirect @874 payable, seed hash2(rngWord, uint160(player)) @883 (amount DROPPED); -->
<!--     resolveAfkingBox seed keccak256(abi.encode(rngWord,player,day,amount)) @1084; _resolveLootboxRoll -->
<!--     @1965 (allowEthSpin gate @1245/1973); recirc entry allowEthSpin=false @889; redemption ETH-spin -->
<!--     allowEthSpin=true pool-pre-flush @976. -->
<!--   DegenerusGameDegeneretteModule.sol: survival flip hash2(rngWord,betId)&1 @773 (acc.burnieMint += -->
<!--     @774 / -= @777; field @413; mintForGame @447); spin seed/event hash2(rngWord,betId) @791; bet -->
<!--     delete @655; resolveWwxrpSpinFromBox @1292 (guard !=GAME @1298; S==9 bracket=level/10 @1324); -->
<!--     resolveBurnieSpinsFromBox @1347 (guard @1353; survival hash2(seed,BOX_SURVIVAL_TAG)&1 @1385); -->
<!--     resolveEthSpinFromBox @1402 (guard @1408); regular-bet WWXRP bracket level/10 @750. -->
<!--   DegenerusGameDecimatorModule.sol: winner select decBucketOffsetPacked[lvl] @269; uint32 narrowing -->
<!--     round.rngWord = uint32(rngWord) @277; claimDecimatorJackpot @293 (reads offset @312); -->
<!--     claimDecimatorJackpotMany @325 (cache packedOffsets @338, over=gameOver @341 once); -->
<!--     _claimDecimatorJackpotFor @385 (over @401, passes round.rngWord @410); _awardDecimatorLootbox -->
<!--     @645 (delegatecall resolveLootboxDirect.selector @673). -->
<!--   StakedDegenerusStonk.sol: rngWordForDay iface @43; gate _claimRedemptionFor @821 (lootbox leg -->
<!--     rngWordForDay(day+1) @878, entropy hash2(rngWord, uint160(player)) @879); _submitGamblingClaimFrom -->
<!--     @976 (currentPeriod = GameTimeLib.currentDayIndex() @983; if rngWordForDay(currentPeriod)==0 revert -->
<!--     BurnsBlockedBeforeDailyRng @991; PriorDayUnresolved @997; _pendingResolveDay = currentPeriod @998). -->
<!--   BurnieCoinflip.sol: processCoinflipPayouts onlyDegenerusGameContract @204; autoRebuyCarry u128 @154; -->
<!--     sdgnrsAutoRebuyArmed @174; settle carry roll @412-417; claimCoinflipCarry @754 (rngLocked @759, -->
<!--     carry @770). -->
<!--   AdvanceModule.sol: keep-roll hash2(rngWord, FUTURE_KEEP_TAG) @~969; gameover combine hash2(combined,w) -->
<!--     @~1400; redemption roll (currentWord>>8)%151+25 @~1259; processCoinflipPayouts @1245/1309/1344/1844. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110. RNG-window freeze authority = -->
<!--     RngWindowFreeze.inv.t.sol EXERCISED + non-vacuous (afterInvariant gates ghost_windowsOpened>0 AND -->
<!--     ghost_inWindowActions>0) + FALSIFIABLE (test_invariantCatchesSeededInWindowMutation PASS: seeded -->
<!--     in-window mutation of rngWordByDay[snapDay] -> detector fires). Handler in-window slots 10/34/35 + -->
<!--     dailyIdx MATCH the subject forge inspect. Plus the 7/7 GREEN VRFPath suite. DecimatorOffsetIsolation -->
<!--     .t.sol EXERCISED (terminal [lvl+1] isolation + claim-path reached) — but NO oracle asserts the -->
<!--     uint32 per-bucket DISTRIBUTION is unbiased (the MISSING property routed here as RNG-02 / FC-391-04). -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial backward-trace + decimator distribution analysis + in-window SLOAD enumeration + per-req / per-lead adjudication vs frozen source</name>
  <files>.planning/phases/391-rng-spine/391-02-CLAUDE-NET.md</files>
  <read_first>
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-391-01..05 verbatim + the inherited cross-refs FC-389-05, FC-392-11 — the leads to adjudicate)
    - .planning/v63-surface-map/rng-freeze.md (the full §1-§10 backward-trace + the consumer summary table + the top-5 sweep leads to attack)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md (test #1 RngWindowFreeze EXERCISED+falsifiable = the RNG-06 in-window-freeze authority; test #8 DecimatorOffsetIsolation EXERCISED but the per-bucket DISTRIBUTION is a MISSING property routed here as RNG-02 — build the distribution argument; the entropy-distribution-oracle note section)
    - the v45 north-star + the RNG-audit doctrine: trace BACKWARD from each consumer to the commitment point; enumerate ALL SLOADs inside the rng-window (not just VRF-derived seeds); check what player-controllable state can change between VRF request and fulfillment
    - the frozen source via git show a8b702a7:contracts/modules/DegenerusGameDecimatorModule.sol (the uint32 narrowing @277, winner-select @269, the claim path @293-410, _awardDecimatorLootbox @645-673), contracts/modules/DegenerusGameLootboxModule.sol (resolveLootboxDirect @874-883, manual open @560, resolveAfkingBox @1084, _resolveLootboxRoll @1965, the allowEthSpin gating @1245/1973, recirc @889/976), contracts/modules/DegenerusGameDegeneretteModule.sol (box-spin resolvers @1292/1347/1402, survival flip @773-791, bet delete @655, acc.burnieMint @413/447/774/777), contracts/StakedDegenerusStonk.sol (the redemption gate @976-998, the day+1 read @878-879), contracts/BurnieCoinflip.sol (claimCoinflipCarry @754-774, the rngLocked guard @759, processCoinflipPayouts onlyDegenerusGameContract @204, the settle carry roll @412-417), contracts/libraries/EntropyLib.sol (hash1 @38 / hash2 @23), and the in-window slot enumeration over the AdvanceModule daily/lootbox resolution windows
    - .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md + council/*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the freeze/entropy claims first, then fold the council leads in at the end of
    this task). For each of the 6 reqs, 5 owned leads, and 2 inherited cross-refs, attempt to BREAK the
    freeze/entropy/replay/domain-separation claim with a concrete reachable call sequence (multi-tx /
    observe-then-retrigger where the ordering matters), then record the result.

    RNG adjudication (RNG-01..06 + FC-391-01..05 + the inherited cross-refs):

    - RNG-01 (backward-trace every new/changed consumer): for EACH consumer in the changed set
      (resolveLootboxDirect @LootboxModule:883, the box-spins @DegeneretteModule:1292/1347/1402, the
      survival flip @773, the redemption lootbox leg @StakedStonk:878, the decimator claim seed
      @DecimatorModule:277/410, the coinflip win/loss + the carry roll @BurnieCoinflip, the EntropyLib
      migration sites, the reverseFlip nudge, the far-future salvage quote) trace BACKWARD to the player's
      input-commitment point and confirm the VRF word was UNKNOWN at that point. Cite the commitment point
      for each (the per-index anchor fixed at index advance AFTER deposit; the per-bet nonce fixed at
      placement BEFORE the word lands; the day's VRF word inside the rngLocked window; the deploy-time
      seed-stake commit for days 1-20; the settled prior-day word for the public salvage quote, which is a
      QUOTE not a payout). Find ANY consumer whose seed admits a live, post-reveal, player-controllable
      input. State the commitment-point cite that settles each.

    - RNG-02 / FC-391-04 (the §6 prime — DEDICATED REAL distribution treatment, the MISSING property): the
      decimator uint32 claim-seed (`round.rngWord = uint32(rngWord)` @DecimatorModule:277) flows to
      resolveLootboxDirect → seed = hash2(uint32-word, uint160(player)). Freeze-safety is already settled
      (fixed at resolution, address-mixed, permissionless deterministic claim) — DO NOT merely re-assert
      it. PROVE the DISTRIBUTION property OR surface a finding: across the WHOLE winning-bucket population
      of ONE level (many winners potentially sharing the SAME 32-bit word, differing only by their 160-bit
      address inside hash2), reason about (i) whether the JOINT distribution of per-claim lootbox tier
      outcomes is biased, correlated, or grindable; (ii) whether a multi-account actor (controlling N
      winning addresses for one level) can exploit any correlation introduced by the shared 32-bit word
      vs the full-word baseline; (iii) whether the 32-bit floor is adequate for the realistic
      winning-bucket population size (estimate the population bound and reason about birthday/collision and
      tier-skew effects). Give the CONCRETE reasoning (the per-player keccak hash2(word, address) is a
      different uniform draw per address, so two winners sharing the word still get independent uniform
      tier draws because keccak diffuses the address bits across the full 256-bit output before the tier
      modulo — OR find where this breaks). Where the green baseline's DecimatorOffsetIsolation.t.sol proves
      slot-isolation + claim-path-reached but NOT the distribution, RECORD that a distribution/grinding
      oracle is the routed test-hardening item (a statistical/property test over the uint32 seed across a
      winner population — NOT a contract change). Settle either way — no hand-waving.

    - RNG-03 / FC-391-02 (box-spin one-shot/replay): trace the box-spin resolvers
      (@DegeneretteModule:1292/1347/1402, each guarded `address(this) != GAME` @1298/1353/1408). Confirm
      the lootbox record is zeroed / the bet is `delete`d (@655) BEFORE the spin resolves, so a player
      cannot OBSERVE a spin outcome and re-trigger a fresh spin with a now-known word. Attack: any path that
      reaches a resolver twice for the same record/bet, or where the record-clear lands AFTER the spin so a
      revert-and-retry could re-roll. Confirm the module guard + the one-shot record-clear together fully
      prevent replay. FC-391-03 folds in: the BURNIE survival flip (@773, hash2(rngWord,betId)&1) — confirm
      single-shot per bet AND that `acc.burnieMint -=` (@777) cannot TRANSIENTLY underflow the unsigned
      running total across a multi-bet batch (the per-bet additions @774/907 net to zero for the same bet,
      but acc is shared across the batch — trace the cross-bet ordering to confirm the running total never
      goes negative before a later +=, or surface a finding).

    - RNG-04 / FC-391-01 (domain-separation): resolveLootboxDirect dropped the `amount` term (seed =
      hash2(rngWord, uint160(player)) @883) while manual open (@560) and resolveAfkingBox (@1084) keep
      amount. Enumerate EVERY caller of resolveLootboxDirect (decimator @673, the redemption leg, the
      ETH-spin recirc, any direct-open auto-resolve) and confirm each passes a DOMAIN-SEPARATED word
      (Degenerette hash2(rngWord,betId); decimator the narrowed round.rngWord; redemption
      hash2(rngWordForDay(day+1),player); ETH-spin hash2(seed,BOX_RECIRC_TAG)). Attack: any caller path
      that feeds TWO same-word/same-player resolutions through resolveLootboxDirect that would now COLLIDE
      on the seed (the amount term used to disambiguate). Prove no-collision with the per-caller
      domain-separation cite OR surface a finding.

    - RNG-05 / FC-391-05 (redemption day+1 gate + day-boundary): the burn gate
      (`if game.rngWordForDay(currentPeriod)==0 revert BurnsBlockedBeforeDailyRng` @StakedStonk:991;
      currentPeriod = GameTimeLib.currentDayIndex() @983) admits a burn only once currentPeriod's word is
      recorded; the lootbox leg reads `rngWordForDay(day + 1)` @878 (day+1 undrawn at burn time). Attack the
      day-boundary: enumerate whether GameTimeLib.currentDayIndex() (a pure fn of block.timestamp) can
      DIVERGE from the game's dailyIdx so a burn stamps a day whose day+1 word is ALREADY on-chain — spell
      out the interleavings (a wall day rolled past dailyIdx but the advance hasn't run → rngWordForDay
      (currentPeriod) is 0 for the new day → burn reverts, so the stamp can only be the drawn day; a
      same-block burn racing the advance). Confirm the gate pins a DRAWN day by construction
      (currentPeriod <= dailyIdx) so day+1 is never on-chain at burn time, OR surface a finding. Note the
      independent rngLocked guard in burn()/burnWrapped() as a second wall.

    - RNG-06 (in-window SLOAD enumeration — DEDICATED enumeration over the repacked slots): over the
      freeze-spine slots at a8b702a7 (rngWordByDay slot 10; lootboxRngPacked slot 34, low 48 = index
      cursor; lootboxRngWordByIndex slot 35; dailyIdx slot 0 byte 3), enumerate EVERY SLOAD consumed
      ALONGSIDE a VRF word in the daily-resolution window (the JackpotModule traits/coin draw, the redemption
      roll, the coinflip resolution) and the lootbox-resolution window (the box draws + spins). For each,
      confirm it is either VRF-derived OR a FROZEN snapshot (written before the word landed), NOT a
      player-controllable non-VRF read that can change between request and fulfillment. Specifically break
      the two load-bearing claims: (i) the EntropyLib hash1/hash2 migrations produce byte-identical keccak
      preimages (hash2(a,b)==keccak256(abi.encode(a,b)) for full-width 32-byte operands; the address-as-
      uint160 cast left-pads to the same 32-byte word) — confirm every migrated operand is 32-byte; (ii) the
      activityScore threaded into spin EV is the FROZEN snapshot unpacked from the lootbox record (written at
      deposit), NOT a live read — trace the score's provenance through the resolver. Anchor on the EXERCISED
      + falsifiable RngWindowFreeze.inv.t.sol (its handler enumerates slots 10/34/35 + dailyIdx and its
      seeded-mutation test fires) — but ALSO attack independently for any in-window SLOAD the handler's
      action set does not cover. Settle either way.

    - FC-389-05 (inherited, the gas-identity narrowing-equivalence half of the decimator uint32): confirm
      `round.rngWord = uint32(rngWord)` is a deterministic value fixed at resolution with
      predictability-WITHOUT-control (player + word frozen; permissionless deterministic claim cannot
      grind), and that the uint32 packing into the DecClaimRound slot does not corrupt a co-resident field
      (couple to 389's STORAGE attestation; the RNG-consumption + grind/retry-timing half is owned here).
      REFUTE or surface.

    - FC-392-11 (inherited, the RNG-lock-coverage half — the backing/EV dynamics half is 392's):
      `claimCoinflipCarry` (@BurnieCoinflip:754) reverts on `degenerusGame.rngLocked()` (@759) and reads
      the carry (@770); processCoinflipPayouts is onlyDegenerusGameContract (@204) applying the win/loss
      roll in the advance walk (@412-417). VERIFY the rngLocked guard FULLY covers the roll application —
      trace whether there is any window where the pending day's stake word is already on-chain (resolvable)
      yet a carry claim or a settle can read/act on the roll outcome BEFORE the lock blocks it (the v62
      RNG-lock-bypass class). Confirm the lock window over the carry roll is airtight, OR surface a finding.
      Note the backing-solvency dynamics (loss zeroes pending; carry excluded from redemption backing) are
      adjudicated at 392 — record the cross-ref so 392 owns the backing half.

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried (backward-trace to the
    commitment point; multi-tx / observe-then-retrigger where it matters) · STATE VAR + file:line at
    a8b702a7 · the commitment-point/bound/distribution-reasoning/cite that settles it · a provisional
    verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where the green baseline's EXERCISED
    freeze authority (RngWindowFreeze falsifiable invariant, the 7/7 VRFPath suite,
    DecimatorOffsetIsolation slot-isolation) already proves a property — and where the decimator uint32
    DISTRIBUTION property is un-netted by any oracle (record whether a sweep wants a distribution/grinding
    statistical test added, as a routed test-hardening note, NOT a contract change). At the END of the
    task, read the council outputs (391-01 council/*.txt) and note for each item whether the council raised
    a convergent/divergent lead — fold any council-only lead in as an additional row to adjudicate. Write
    all of this to 391-02-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use
    neutral defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md && for id in RNG-01 RNG-02 RNG-03 RNG-04 RNG-05 RNG-06 FC-391-01 FC-391-02 FC-391-03 FC-391-04 FC-391-05 FC-389-05 FC-392-11; do grep -q "$id" .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "backward|commitment|unknown when" .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md && grep -Eqi "distribution|uint32" .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md && grep -Eqi "SLOAD|in-window|rngWordByDay" .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    391-02-CLAUDE-NET.md contains an independent adversarial analysis with a backward-trace + an attack
    attempt + a provisional verdict for EACH of RNG-01..06, FC-391-01..05, and the inherited cross-refs
    FC-389-05, FC-392-11; RNG-01 cites the commitment point for each consumer; RNG-02/FC-391-04 carries a
    dedicated REAL distribution argument over the winning-bucket population (proof or finding, not a
    hand-wave); RNG-06 carries the in-window SLOAD enumeration over slots 10/34/35 + dailyIdx with the
    hash-preimage + activityScore-snapshot claims attacked; RNG-05 carries the day-boundary divergence
    analysis; the decimator-distribution test-hardening gap is noted as a routed item; the council leads
    are folded in at the end; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record, independent of the council, with a per-item
    backward-trace + attack attempt + provisional verdict over the full phase-391 RNG surface, the
    decimator distribution prime target given dedicated rigorous treatment, the in-window SLOADs
    enumerated, the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 391-FINDINGS.md with the per-item verdict table</name>
  <files>.planning/phases/391-rng-spine/391-FINDINGS.md</files>
  <read_first>
    - .planning/phases/391-rng-spine/391-02-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md + council/*.txt (NET 1 leads)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md (the just-approved FINDINGS structure to match — header, both-nets table, per-item verdict table, skeptic gate, routing block)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting: RNG/freeze = DOMINANT) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact RNG-01..06 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into 391-FINDINGS.md — the phase-391 adjudication
    deliverable. Structure:

    (1) Header: subject `a8b702a7` byte-frozen; baseline = REGRESSION-BASELINE-v63 (854/0/110; the
    RNG-window freeze authority = RngWindowFreeze.inv.t.sol EXERCISED + non-vacuous + FALSIFIABLE, the 7/7
    GREEN VRFPath suite, DecimatorOffsetIsolation EXERCISED for slot-isolation — but the decimator uint32
    per-bucket DISTRIBUTION is a MISSING property, routed test-hardening, not relied on as already-netted);
    method = COUNCIL + CLAUDE both; posture = AUDIT-ONLY (a CONFIRMED finding routes to a gated
    USER-hand-review fix, never fixed here); RNG/freeze = the DOMINANT class (§4 — a confirmed
    RNG-manipulability or freeze break is the highest-severity dimension).

    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    391-01-COUNCIL-NET.md) and NET 2 (Claude — 391-02-CLAUDE-NET) are both on record for the RNG slice. A
    no-finding (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 6 reqs (RNG-01..06), ALL 5 owned
    leads (FC-391-01..05), AND the 2 inherited cross-refs (FC-389-05, FC-392-11). Columns: ITEM · what it
    claims · NET-1 result · NET-2 result · ADJUDICATED VERDICT (CONFIRMED / REFUTED / BY-DESIGN / MONITOR)
    · the settling commitment-point/bound/source-cite (file:line at a8b702a7 or the green-baseline anchor).
    RNG-01 MUST cite the commitment point per consumer; RNG-02 / FC-391-04 MUST cite the distribution
    argument (or carry a CONFIRMED finding) AND record the routed distribution-oracle test-hardening note;
    RNG-05 / FC-391-05 MUST cite the day-boundary divergence bound; RNG-06 MUST cite the in-window SLOAD
    enumeration + the RngWindowFreeze falsifiable anchor. Weight each finding per AUDIT-V63-PLAN §4 (RNG is
    the dominant class — a real freshness/freeze break or a grindable distribution is HIGH-or-above; a
    benign INFO timing/comment item is not). Apply the §5 design-intent anchor (lootbox open/resolve TIMING
    is by-design — verify the seed is frozen at request, do not flag timing as a freshness bug; Degenerette
    RTP/WWXRP worthlessness is by-design — verify freeze + distribution, not desirability; the documented EV
    changes are 392's; the public salvage quote is by-design).

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter
    (structural-protection check + the 3-condition EV lens) and record the gate result for each elevated
    item. If nothing reaches HIGH (the surface-maps found 0 HIGH on inspection — the change set preserves
    the RNG-freeze spine), state that explicitly with the gate applied to the MED-attention items
    (FC-391-04 decimator distribution, FC-391-01 dropped-amount collision, FC-392-11 RNG-lock coverage). If
    the dedicated treatment of a prime target surfaces a real break (e.g. a grindable distribution or a
    day-boundary divergence), run the full skeptic gate and weight it as the dominant class.

    (5) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding, its
    weight, the proposed fix shape, and the note that it goes to a SEPARATE gated USER-hand-review boundary
    — batched, never pre-approved, never auto-committed; the subject re-freezes after). If there are zero
    CONFIRMED findings, state "0 CONFIRMED — document-only; RNG-01..06 attested at a8b702a7" and note any
    INFO/MONITOR items carried forward (e.g. the FC-389-05 narrowing-equivalence confirmation, the
    FC-392-11 backing-half cross-ref → 392, and the decimator uint32 DISTRIBUTION-ORACLE test-hardening
    item routed as a possible statistical/property net at a later test phase). Any test-only
    (oracle-integrity / missing-property) gap is routed, NOT a contract finding.

    (6) Re-attestation line: each of RNG-01..06 marked attested-or-finding.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No
    contract source is modified — this is a documentation deliverable.
  </action>
  <verify>
    <automated>test -s .planning/phases/391-rng-spine/391-FINDINGS.md && for id in RNG-01 RNG-02 RNG-03 RNG-04 RNG-05 RNG-06 FC-391-01 FC-391-02 FC-391-03 FC-391-04 FC-391-05 FC-389-05 FC-392-11; do grep -q "$id" .planning/phases/391-rng-spine/391-FINDINGS.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/391-rng-spine/391-FINDINGS.md && grep -Eqi "skeptic" .planning/phases/391-rng-spine/391-FINDINGS.md && grep -Eqi "distribution" .planning/phases/391-rng-spine/391-FINDINGS.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    391-FINDINGS.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH of
    RNG-01..06, FC-391-01..05, and the 2 inherited cross-refs with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR
    verdict + settling cite; RNG-01 cites the per-consumer commitment point; RNG-02/FC-391-04 cites the
    distribution argument (or carries a finding) AND records the routed distribution-oracle item;
    RNG-05/FC-391-05 cites the day-boundary bound; RNG-06 cites the in-window SLOAD enumeration; the
    skeptic gate is run and recorded; any CONFIRMED finding is documented + ROUTED (not fixed);
    `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The phase-391 RNG-freeze surface is adjudicated with both nets on record, the skeptic gate applied,
    every req + owned lead + inherited cross-ref carrying an explicit verdict, the decimator distribution
    prime target settled by a dedicated real-distribution argument (or finding), the in-window SLOADs
    enumerated, and any CONFIRMED finding documented + routed to a gated fix — the subject byte-frozen
    throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED RNG-manipulability / freeze break or a FALSE-GREEN adjudication (a real freshness defect or grindable distribution waved through), or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings, incl. the V62-01/RNGRETRY/RNGREUSE RNG classes) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-391-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-391-05 | Repudiation / false-green | a REFUTED/BY-DESIGN RNG verdict issued without both nets, or the decimator distribution waved as "address-mixed so fine" without a real argument | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; RNG-02/FC-391-04 requires a concrete distribution argument; the missing-distribution-oracle is recorded as a routed test-hardening item, not relied on as netted |
| T-391-06 | Elevation / under-weighting | a CONFIRMED RNG break under-weighted, or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; RNG/freeze weighted as the DOMINANT class per AUDIT-V63-PLAN §4 (a real freshness/freeze break or grindable distribution is HIGH-or-above) |
| T-391-07 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item backward-trace + verdict over all 6 reqs + 5 owned
  leads + 2 inherited cross-refs.
- Both nets on record before the verdict; the both-nets table present.
- RNG-01 cites the commitment point per consumer; RNG-02 / FC-391-04 settled by a real distribution
  argument (proof or finding) + the routed distribution-oracle note; RNG-05 / FC-391-05 settled
  (day-boundary divergence bound); RNG-06 settled (in-window SLOAD enumeration + RngWindowFreeze anchor).
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded; RNG/freeze weighted as the dominant class.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The full phase-391 RNG-freeze surface (RNG-01..06 + the 5 FC-391 leads + the 2 inherited cross-refs) is
adjudicated against the byte-frozen subject `a8b702a7` with BOTH nets on record, the skeptic gate applied,
and every item carrying an explicit CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a
commitment-point, bound, or source-cite. The decimator uint32 distribution-bias prime target is settled by
a dedicated real-distribution argument; the backward-trace is applied to every consumer; the in-window
SLOADs are enumerated. Any CONFIRMED finding is documented + routed to a gated fix; the subject stays
byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/391-rng-spine/391-02-SUMMARY.md` when done
</output>
</content>
