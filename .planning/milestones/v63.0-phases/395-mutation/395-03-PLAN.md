---
phase: 395-mutation
plan: 03
type: execute
wave: 3
depends_on: [395-02]
files_modified:
  - test/mutation/MutationKills.t.sol
  - audit/mutation/MUTATION-FINDINGS-v63.md
  - audit/mutation/CAMPAIGN-REPORT-v63.md
autonomous: true
requirements: [MUT-03]
must_haves:
  truths:
    - "Each GENUINE survivor is either killed by a new regression test or routed to a finding (never fixed in-phase)."
    - "Each new kill-test FAILS with the mutation re-applied and PASSES on the clean subject (it genuinely kills the mutant)."
    - "A GENUINE survivor that reveals a real contract defect is documented + routed to a gated fix (not fixed here)."
    - "The campaign closes with contracts/ byte-identical to a8b702a7 (git diff empty + tree-hash) — a left-behind mutant or dirty tree is a FAIL."
  artifacts:
    - path: "test/mutation/MutationKills.t.sol"
      provides: "New regression tests, one per killed GENUINE survivor (test-only, committable)"
      contains: "function test"
    - path: "audit/mutation/MUTATION-FINDINGS-v63.md"
      provides: "Per-genuine-survivor disposition: KILLED-BY-TEST or ROUTED-TO-FINDING (with the routed gated-fix note)"
      contains: "KILLED"
  key_links:
    - from: "test/mutation/MutationKills.t.sol"
      to: "the GENUINE survivor set"
      via: "each test asserts the behavior the survivor mutated"
      pattern: "function test"
    - from: "audit/mutation/MUTATION-FINDINGS-v63.md"
      to: "396 TERMINAL"
      via: "any ROUTED-TO-FINDING entry carried to the terminal close"
      pattern: "ROUTED|gated"
---

<objective>
Close the campaign: for each GENUINE survivor from 395-02, either KILL it with a new regression test
(preferred — test-only, allowed to commit) or ROUTE it to a finding (if it reveals a real contract
defect → documented + routed to a gated USER-hand-review fix, never fixed in-phase). Then VERIFY the
subject is byte-identical to `a8b702a7` and commit only the test/ + audit/ deliverables.

Purpose: the mutation campaign's value is realized only when each genuine oracle gap is closed (a new
killing test) or surfaced (a routed finding); a campaign that leaves contracts/ dirty or a genuine
gap unhandled is a FAIL.
Output: the new kill-tests (test/mutation/MutationKills.t.sol), the per-survivor disposition ledger,
and the final byte-freeze attestation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@test/REGRESSION-BASELINE-v63.md
@audit/mutation/SURVIVOR-TRIAGE-v63.md
@audit/mutation/CAMPAIGN-REPORT-v63.md

<read_first>
- audit/mutation/SURVIVOR-TRIAGE-v63.md (the GENUINE survivor set — the kill/route worklist)
- audit/mutation/TARGETS-v63.md (which oracle test exercises each target — the home for the kill-test)
- a representative existing kill-test pattern, e.g. test/fuzz/RedemptionStethFallback.t.sol or
  test/fuzz/StorageFoundation.t.sol (the deterministic-branch-proof style the new tests mirror —
  each asserts the branch it intends WAS taken)
- .planning/v63-surface-map/{solvency,coinflip-burnie,storage-packing}.md (the invariant a genuine
  survivor breaks → the kill-test's assertion, or the routed finding's behavior claim)
</read_first>

<interfaces>
<!-- A kill-test is GENUINE-survivor-killing iff: it PASSES on the clean subject, and FAILS when the
     survivor's mutation is re-applied in place. The campaign's "killed" guarantee is exactly this
     fail-with-mutation / pass-without property (mirrors the 388-02 falsifiability discipline:
     RngWindowFreeze's test_invariantCatchesSeededInWindowMutation, PoolConservation's
     test_invariantIsFalsifiable_unbackedCreditMint). -->
New tests live in test/mutation/MutationKills.t.sol (a new directory test/mutation/ — test-only,
  committable). Each test targets one GENUINE survivor and asserts the observable behavior the
  mutation changed, on a path the surface-map proves reachable.
Routing target for a real defect: audit/mutation/MUTATION-FINDINGS-v63.md → carried to 396 TERMINAL
  (the gated USER-hand-review boundary; never fixed in this phase per the audit-only posture).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kill-test → the survivor | a test that passes both with and without the mutation does NOT kill it |
| genuine defect → in-phase fix | a contract defect must be ROUTED to a gated fix, not fixed here (audit-only posture) |
| campaign end → byte-freeze | a left-behind mutant / dirty contracts/ tree is a campaign FAIL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-395-07 | Repudiation (false-green test) | a kill-test that doesn't actually kill the mutant | mitigate | each kill-test is validated fail-with-mutation / pass-without (the survivor re-applied in place, then restored) before it is accepted |
| T-395-08 | Tampering | a stranded mutant / dirty contracts/ at campaign end | mitigate | final `git diff a8b702a7 -- contracts/` empty + tree-hash `2934d3d8…` is a blocking acceptance criterion; the commit-guard hook backstops a dirty-contracts commit |
| T-395-09 | Elevation (in-phase fix) | a contract defect fixed inline instead of routed | mitigate | the posture is audit-only — a GENUINE survivor revealing a real defect is ROUTED to a gated boundary (396), documented, never edited here |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kill each GENUINE survivor with a new regression test</name>
  <files>test/mutation/MutationKills.t.sol</files>
  <behavior>
    - For each GENUINE survivor that is an oracle gap (not a contract defect): a new test PASSES on the
      clean subject AND FAILS when that survivor's mutation is re-applied in place (it genuinely closes
      the net hole).
    - Each test asserts the observable behavior the mutation changed (a solvency identity, a packing
      round-trip, a redemption-leg conservation, a BURNIE conservation), on a surface-map-reachable path.
    - SPINE survivors (solvency/RNG) get a dedicated assertion mirroring the 388-02 falsifiability style
      (assert the targeted branch was taken, then assert the invariant).
  </behavior>
  <read_first>
    - audit/mutation/SURVIVOR-TRIAGE-v63.md (the GENUINE set; skip any classified as a contract defect → Task 2)
    - test/fuzz/RedemptionStethFallback.t.sol + test/fuzz/StorageFoundation.t.sol (the deterministic-branch-proof kill-test pattern)
  </read_first>
  <action>
    Create `test/mutation/MutationKills.t.sol` (new directory `test/mutation/`). For each GENUINE
    survivor whose disposition is "oracle gap" (the regression net simply lacked an assertion — NOT a
    real contract defect), add one `test_kills_<contract>_<line>_<mutator>()` that exercises the
    target on a reachable path and asserts the exact behavior the mutation broke (cite the survivor's
    file:line in a comment). Mirror the existing deterministic-branch-proof style: where the target is a
    SPINE function, assert the targeted branch WAS taken before asserting the invariant (so the test is
    not vacuously green). Validate each test KILLS its survivor: run it green on the clean subject, then
    re-apply the survivor's mutation IN PLACE (edit the contract line to the mutant), run the test,
    confirm it now FAILS, then `git checkout -- contracts/` and confirm green again. Record the
    fail-with-mutation / pass-without evidence per test in the test file's header comment or in
    MUTATION-FINDINGS-v63.md (Task 2). If a GENUINE survivor is a real contract DEFECT (the behavior the
    oracle missed is itself wrong, not just untested), do NOT write a kill-test that locks in the wrong
    behavior — hand it to Task 2 for routing.

    Use a TARGETED run for validation (`forge test --match-path test/mutation/MutationKills.t.sol`), not
    the full suite. NEVER commit while a validation mutant is in place. The test file is test-only and
    committable; no contracts/*.sol is persistently edited.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git checkout -- contracts/ 2>/dev/null; test -z "$(git diff a8b702a7 -- contracts/)" && forge test --match-path test/mutation/MutationKills.t.sol 2>&1 | grep -qE 'Suite result: ok|0 failed' && echo "KILL-TESTS green on clean subject; contracts clean"</automated>
  </verify>
  <done>
    test/mutation/MutationKills.t.sol exists with one passing test per killable GENUINE survivor, each
    validated fail-with-mutation / pass-without; the suite is green on the clean subject; any GENUINE
    survivor that is a real defect (not just untested) is deferred to Task 2; `git diff a8b702a7 --
    contracts/` is EMPTY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route real defects + finalize the disposition ledger + final byte-freeze verification</name>
  <files>audit/mutation/MUTATION-FINDINGS-v63.md, audit/mutation/CAMPAIGN-REPORT-v63.md</files>
  <read_first>
    - audit/mutation/SURVIVOR-TRIAGE-v63.md (the full GENUINE set)
    - test/mutation/MutationKills.t.sol (the killed survivors, from Task 1)
    - audit/mutation/CAMPAIGN-REPORT-v63.md (append the final disposition + score summary)
    - .planning/STATE.md (the 396-carry list — append any routed finding to it via the SUMMARY, not edited here)
  </read_first>
  <action>
    Write `audit/mutation/MUTATION-FINDINGS-v63.md` — the per-GENUINE-survivor disposition ledger. For
    each GENUINE survivor: KILLED-BY-TEST (cite the `test_kills_*` in MutationKills.t.sol + the
    fail-with-mutation / pass-without evidence) OR ROUTED-TO-FINDING. For each ROUTED entry: state the
    observable wrong behavior the mutation exposed, the surface-map invariant it touches, the threat
    class (SPINE solvency / RNG / packing), the severity hint, and that it is routed to a gated
    USER-hand-review boundary at 396 TERMINAL — NOT fixed in this phase (audit-only posture; only a
    USER-approved batched contract change may fix it). If the campaign produced ZERO genuine survivors
    (the net is tight), record that explicitly as the result — a high mutation score with all survivors
    FALSE is a valid, strong outcome (it confirms the regression net catches injected defects in the
    fix-site/spine functions). Append to CAMPAIGN-REPORT-v63.md a final section: the aggregate mutation
    score, the survivor disposition tally (FALSE / KILLED-BY-TEST / ROUTED), and the final byte-freeze
    attestation.

    Then perform the FINAL byte-freeze verification: assert `git diff a8b702a7 -- contracts/` is EMPTY
    and `git rev-parse HEAD:contracts` == `2934d3d8987a09c5f073549a0cb499f6c5f28620`. Run a targeted
    green check that the new kill-tests pass on the clean subject (`forge test --match-path
    test/mutation/MutationKills.t.sol`). A non-empty diff or a wrong tree-hash is a campaign FAIL —
    restore (`git checkout -- contracts/`) and re-verify before completing.

    This task persists only audit/ + the score-append; any routed finding is surfaced in the SUMMARY for
    396, NOT by editing STATE.md or any contract here.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git checkout -- contracts/ 2>/dev/null; test "$(git rev-parse HEAD:contracts)" = "2934d3d8987a09c5f073549a0cb499f6c5f28620" && test -z "$(git diff a8b702a7 -- contracts/)" && grep -qiE 'KILLED|ROUTED|zero genuine|no genuine' audit/mutation/MUTATION-FINDINGS-v63.md && grep -qiE 'byte-identical|tree-hash|2934d3d8' audit/mutation/CAMPAIGN-REPORT-v63.md && forge test --match-path test/mutation/MutationKills.t.sol 2>&1 | grep -qE 'Suite result: ok|0 failed|No tests to run' && echo "FINAL byte-freeze verified, dispositions recorded"</automated>
  </verify>
  <done>
    Every GENUINE survivor has a disposition (KILLED-BY-TEST with evidence, or ROUTED-TO-FINDING with
    the gated-fix note carried to 396) — or the ledger records zero genuine survivors; CAMPAIGN-REPORT-v63.md
    carries the final score + disposition tally + byte-freeze attestation; the FINAL verification passes
    (`git diff a8b702a7 -- contracts/` empty + tree-hash `2934d3d8…` + kill-tests green); no contract
    was edited persistently.
  </done>
</task>

</tasks>

<verification>
- Each GENUINE survivor is KILLED by a new test (validated fail-with-mutation / pass-without) or ROUTED
  to a gated finding — never fixed in-phase (MUT-03).
- The new kill-tests are test-only and committable; they pass green on the clean subject.
- The FINAL byte-freeze holds: `git diff a8b702a7 -- contracts/` EMPTY + tree-hash
  `2934d3d8987a09c5f073549a0cb499f6c5f28620` — a left-behind mutant / dirty tree is a FAIL.
- Any routed finding is carried to 396 TERMINAL (documented, not fixed).
</verification>

<success_criteria>
Every genuine oracle gap is closed by a new regression test or routed to a gated finding; the campaign
closes with the subject byte-identical to a8b702a7; only test/ + audit/ deliverables are written and
committed.
</success_criteria>

<output>
Create `.planning/phases/395-mutation/395-03-SUMMARY.md` when done. Surface any ROUTED-TO-FINDING entry
for the 396 TERMINAL carry list in the SUMMARY.
</output>
