---
phase: 390-solvency-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md
  - .planning/phases/390-solvency-spine/council/solv.gemini.txt
  - .planning/phases/390-solvency-spine/council/solv.codex.txt
  - .planning/phases/390-solvency-spine/council/solv.council.json
  - .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md
autonomous: true
requirements: [SOLV-01, SOLV-02, SOLV-03, SOLV-04, SOLV-05, SOLV-06, SOLV-07]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full SOLVENCY slice (claimablePool identity / sDGNRS backing / submit-claim conservation / dust-forfeit self-credit / redemption-claim liveness-window ordering / CEI-yield-surplus / JackpotModule delta-fold)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the by-design rulings excluded so the council does not waste passes on settled intent, and with the redemption rework's exact frozen line-cites supplied so the council reads the right code"
    - "The §6-prime targets (the redemption CLAIM liveness gate, the dust-forfeit self-credit backing, the V62-03-class CEI / yield-surplus reentrancy) are each charged hard as named break-targets"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md"
      provides: "Neutral council prompt for the SOLV-01..07 slice + FC-390-01..07 leads + the inherited cross-refs (FC-389-02/-08, FC-392-08, FC-393-02/-03)"
      min_lines: 40
    - path: ".planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md"
      provides: "Council-net capture record: raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line"
      min_lines: 20
  key_links:
    - from: "390-01-COUNCIL-PROMPT-SOLV.md"
      to: "council.sh --label solv"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label solv"
    - from: "390-01-COUNCIL-NET.md"
      to: "council/solv.council.json"
      via: "manifest available/skipped capture"
      pattern: "solv\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the SOLVENCY-SPINE surface — the claimablePool / ETH-stETH /
sDGNRS-backing accounting under the redemption-rework + dust-forfeit + CEI + JackpotModule delta-fold —
against the byte-frozen subject `a8b702a7`. Author ONE neutral council prompt for the SOLV-01..07 slice
(plus the FC-390-01..07 leads and the inherited cross-refs FC-389-02/-08, FC-392-08, FC-393-02/-03),
fan it to the gemini+codex CLIs via `council.sh`, capture the raw output under the phase `council/` dir,
and record the council-net capture (available/skipped, byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
Solvency is the SPINE (§4 threat weighting), so the council — the PRIMARY finder
([[cross-model-led-audits-over-claude-only]], the council caught the V62-03 CEI class Claude-only
missed) — goes on record FIRST so the Wave-2 Claude-net + adjudication plan (390-02) can fold the
council leads in before any verdict.

Output: the COUNCIL-PROMPT-SOLV doc, the raw gemini/codex output, the council.json manifest, and
390-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/solvency.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension; with --schema => .json). -->
<!-- council.json shape: { "label", "schema", "models":[...], "skipped":[...], "outputs":{model:path} } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed CLI is recorded in skipped[] and -->
<!-- is NEVER fatal. gemini + codex CLIs confirmed at ~/.local/bin. -->

<!-- Council-prompt shape reference (the just-approved 389 slice): -->
<!--   .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md -->
<!--   header role + "read EXACT frozen source at <sha> via git show <sha>:contracts/<File>.sol" -->
<!--   + threat-priority line + KNOWN-BY-DESIGN exclusion list + numbered break-targets -->
<!--   + per-finding output format (PROPERTY · reachable CALL SEQUENCE · STATE VAR + file:line · -->
<!--   SEVERITY · why protections don't stop it · or "verified SOUND and why"). -->

<!-- Authoritative frozen line-cites (verified at a8b702a7 — supply these so the council reads -->
<!-- the right code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   StakedDegenerusStonk.sol: claimRedemption @771; claimRedemptionMany @787 (both read -->
<!--     isGameOver=game.gameOver() ONCE then call _claimRedemptionFor); _claimRedemptionFor @821; -->
<!--     dust-forfeit branch @845-848 (lootboxEth<MIN_REDEMPTION_LOOTBOX_ETH=0.01 ETH @337) + @897-900 -->
<!--     (creditRedemptionDirect{value: ethForForfeit}(address(this), forfeitEth) @900; -->
<!--     ethForForfeit=min(bal,forfeitEth) @899); the direct leg creditRedemptionDirect{value: -->
<!--     ethForDirect}(player, ethDirect) @890; the segregation release _pendingRedemptionEthValue -= -->
<!--     totalRolledEth @854; the submit-side MAX(175%) reservation _pendingRedemptionEthValue += -->
<!--     maxIncrement @1066; BurnsBlockedBeforeDailyRng @991 via GameTimeLib.currentDayIndex() @983; -->
<!--     _pendingRedemptionEthValue uint96 @223; the GAME pre-approve for both legs @431-432. -->
<!--   DegenerusGameGameOverModule.sol: handleGameOverDrain @73; totalFunds snapshot @78; -->
<!--     reserved=uint256(claimablePool) @85; gameOver=true latch @135; final sweep claimablePool=0 @206. -->
<!--   DegenerusGameLootboxModule.sol: creditRedemptionDirect body @1004-1015 (auth msg.sender==SDGNRS; -->
<!--     msg.value<=amount; pulls amount-msg.value as stETH via transferFrom; _creditClaimable + -->
<!--     claimablePool += amount); resolveRedemptionLootbox @926-957 (credits futurePrizePool, no -->
<!--     claimableWinnings[SDGNRS] debit). -->
<!--   DegenerusGame.sol: claimWinnings CEI @1209 (_debitClaimableAndAfking @1247; claimablePool -= -->
<!--     payout @1248 BEFORE the external call); _payoutWithStethFallback @1888 (the 53cd25cf CEI -->
<!--     reorder: stETH leg out FIRST, untrusted ETH .call LAST); pullRedemptionReserve @1572 -->
<!--     (_debitClaimable(SDGNRS) + claimablePool -= amount @1583; pure-stETH fallback no game move); -->
<!--     creditRedemptionDirect stub @1552. -->
<!--   DegenerusGameJackpotModule.sol: distributeYieldSurplus @682 (obligations sum @686-690 includes -->
<!--     claimablePool; the cached-then-fold write @713); _processSoloBucketWinner @1247 (whalePassCost -->
<!--     -> futurePrizePool, ETH portion -> claimableDelta); daily-jackpot fold claimablePool += -->
<!--     liabilityDelta @1119; _addClaimableEth REMOVED (was an inline _creditClaimable). -->
<!--   DegenerusGameDecimatorModule.sol: claimDecimatorJackpot permissionless @283-322; batch @339-380. -->
<!--   DegenerusVault.sol: burnEth claim-trigger @751-770; deposit() removed; gameDegeneretteBet -->
<!--     overpay guard relocated game-side @577-589 (vs _collectBetFunds:596). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the SOLVENCY slice</name>
  <files>.planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md</files>
  <read_first>
    - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md (the just-approved prompt shape to match — header role, threat-priority line, KNOWN-BY-DESIGN list, per-finding output format)
    - .planning/v63-surface-map/solvency.md (the master invariant restatement + the change inventory A1..G5 + the "where the identity holds" reconciliation + CF-1..CF-7 — the "what we believe is safe" thesis to break)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-390-01..07 verbatim + the inherited cross-refs FC-389-02, FC-389-08, FC-392-08, FC-393-02, FC-393-03)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (solvency = SPINE) + §5 design-intent anchor
    - the frozen source for the prime sites via git show a8b702a7:contracts/StakedDegenerusStonk.sol (the _claimRedemptionFor + dust-forfeit + claimRedemption gameOver-snapshot region ~771-901), contracts/modules/DegenerusGameGameOverModule.sol (handleGameOverDrain ~73-160), contracts/modules/DegenerusGameJackpotModule.sol (distributeYieldSurplus ~682-716), contracts/DegenerusGame.sol (claimWinnings CEI + _payoutWithStethFallback ~1209-1255, 1888-1909)
  </read_first>
  <action>
    Write 390-01-COUNCIL-PROMPT-SOLV.md, charged NEUTRALLY ("here is what we believe is safe about the
    redemption-rework + dust-forfeit + CEI + JackpotModule-fold solvency accounting — find where it
    breaks") and instructing the council to read the EXACT frozen source at `a8b702a7` via
    `git show a8b702a7:contracts/<File>.sol` (ignore the working tree). Use neutral
    defensive-engineering vocabulary throughout (identity, conservation, segregated reserve, backing,
    reconcile, release, in-flight) per [[neutral-defensive-audit-vocabulary]].

    The prompt MUST include: (a) the threat-priority line from AUDIT-V63-PLAN §4 stated so the council
    knows SOLVENCY IS THE SPINE for this slice (DOMINANT RNG/freeze; HIGH gas-DoS in advanceGame; SPINE
    = solvency; LOW access-control/reentrancy/MEV — but note SOLV-06 is the V62-03 reentrancy/CEI class
    so treat that ONE seriously, not as low); (b) a KNOWN-BY-DESIGN exclusion list so the council does
    not re-litigate settled intent ([[intended-game-mechanics-not-findings]], the redemption dust-drop
    forfeit-to-claimable being an intended anti-dust-farm mechanism [[redemption-dust-lootbox-drop-bydesign]]
    — the council's job is to check the forfeit credit is BACKED, NOT to flag the forfeit policy itself;
    [[lootbox-resolution-timing-by-design]] redemption claim/open timing is not a player edge;
    BURNIE is off the ETH/stETH solvency spine [[degenerette-wwxrp-rtp-by-design]] — the keeper
    creditFlip bounty is a BURNIE-dilution lead routed to ACCESS-02/393, NOT an ETH-spine break here);
    (c) the per-finding output format (PROPERTY · reachable CALL SEQUENCE [multi-tx where the ordering
    matters] · STATE VAR + file:line at a8b702a7 · SEVERITY · why the master-invariant protections don't
    stop it · or "verified SOUND and why").

    State the master invariant the slice rests on (from solvency.md): the GAME identity
    `address(this).balance + steth.balanceOf(this) >= claimablePool` and
    `claimablePool == Σ (claimable + afking halves of balancesPacked[*])`; and the sDGNRS backing
    identity `backing = (ETH + stETH + claimableWinnings[SDGNRS] − pendingRedemptionEthValue)`, with
    `pendingRedemptionEthValue` the physically-segregated reserve for unresolved gambling-burn
    redemptions. Then state the safety thesis to break, mapped to SOLV-01..07:
    "(SOLV-01) every changed credit/debit pairs an equal claimablePool move (the _debitClaimableAndAfking
    per-half guards); (SOLV-02) the sDGNRS backing identity holds under the rework — balance+stETH >=
    obligations; (SOLV-03) redemption submit↔claim conserves exactly: ethDirect + lootboxEth + forfeitEth
    == totalRolledEth in EVERY branch and _pendingRedemptionEthValue -= totalRolledEth releases exactly
    that, no double-count or leak across the boundary; (SOLV-04) the dust-forfeit self-credit is always
    backed by value actually leaving sDGNRS (msg.value ETH + the stETH-remainder pull), never a phantom
    claimable bump beyond the pending release; (SOLV-05) the redemption CLAIM path's liveness-window
    ordering cannot strand or double-credit across the handleGameOverDrain totalFunds snapshot /
    livenessTriggered→gameOver latch; (SOLV-06) the CEI / yield-surplus reentrancy is closed on the
    ETH/stETH payout legs (stETH-before-ETH ordering — the V62-03 class); (SOLV-07) the JackpotModule
    delta-fold credits/deletes no pool twice across the _addClaimableEth inline rework."

    Name the concrete break-targets — charge the three §6-prime targets HARD as dedicated numbered items:

    1. SOLV-05 / FC-390-01 (MED, §6 prime — the dedicated multi-tx ordering target): claimRedemption /
       claimRedemptionMany read `isGameOver = game.gameOver()` ONCE (StakedDegenerusStonk.sol:771/787)
       then _claimRedemptionFor routes the live-game path (credits claimable + funds lootbox) when
       !isGameOver. Ask the council to find ANY multi-tx ordering across the
       `livenessTriggered()→gameOver()` latch and `handleGameOverDrain`'s `totalFunds` snapshot
       (GameOverModule.sol:78) + `reserved=claimablePool` (line 85) + the `gameOver=true` latch (line
       135) + the final `claimablePool=0` sweep (line 206) where a redemption settled via the live path
       either (a) strands value (released from pendingRedemptionEthValue but not credited / swept before
       the credit lands) or (b) double-credits (credited live AND swept/refunded by the drain). Spell out
       the exact block/tx interleavings to try (e.g. a live claim landing in the same block as / right
       after the drain's totalFunds snapshot; a claim that races the gameOver latch between the snapshot
       and the sweep). Prove strand/double-credit-free OR surface a finding.

    2. SOLV-04 / FC-390-02 (MED, §6 prime — dust-forfeit backing): the forfeit leg
       `creditRedemptionDirect{value: ethForForfeit}(address(this), forfeitEth)`
       (StakedDegenerusStonk.sol:900) credits sDGNRS's OWN game claimable. In the stETH-leg submit case
       NO claimable was debited at submit. Ask whether `forfeitEth`'s funding can EVER exceed the value
       actually leaving sDGNRS — i.e. find a path where `ethForForfeit = min(bal, forfeitEth)` (line 899)
       under-sends ETH but the GAME-side creditRedemptionDirect (LootboxModule.sol:1004-1015) cannot pull
       the stETH remainder (`amount − msg.value` via transferFrom), so `claimablePool += amount` credits
       MORE than sDGNRS released — a phantom claimable bump. Tie it to the MAX(175%) reservation
       (`_pendingRedemptionEthValue += maxIncrement` @1066) claim that ETH+stETH always >= rolled: ask
       the council to break that guarantee (a partial-balance / many-claim sequence that drains
       address(this).balance below forfeitEth AND leaves insufficient stETH approval/balance for the
       remainder pull).

    3. SOLV-06 (MED for this slice — the V62-03 CEI / yield-surplus class the council caught before):
       `_payoutWithStethFallback` (DegenerusGame.sol:1888) carries the 53cd25cf reorder (stETH leg out
       FIRST, untrusted ETH `.call` LAST). Ask the council to find ANY payout leg (claimWinnings @1209,
       the redemption credit legs, pullRedemptionReserve @1572, or any other ETH `.call`/stETH transfer
       on the spine) where a reentrant `distributeYieldSurplus` (JackpotModule.sol:682) — whose
       obligations sum reads `claimablePool` + the pending pools — can observe in-flight stETH as
       unreserved backing and over-distribute, OR where the CEI ordering is NOT applied (state debit
       AFTER the external call). Charge this as if the V62-03 vector still exists somewhere on the
       changed surface.

    Then name the remaining leads as numbered break-targets at their map severity:
    - FC-390-03 (MED, cross-ref 393): permissionless decimator/redemption claim + post-gameOver/freeze
      edge — the batch caches `over`/`frozen` ONCE; find any path where it flips mid-batch and splits a
      batch across the boundary inconsistently, or resolves a claim into claimable that is then forfeited
      (DecimatorModule.sol:283-380).
    - FC-390-04 (LOW): burnEth claim-trigger simplification (`claimValue > combined`, dropped
      `&& claimable != 0`) — the edge `amount == supplyBefore` (last DGVE burn) letting claimValue
      slightly exceed combined with claimable==0 → a wasted claimWinnings(vault) (DegenerusVault.sol:751-770).
    - FC-390-05 (INFO, verified): re-confirm the gameDegeneretteBet overpay guard relocated game-side
      (_collectBetFunds:596 `if (ethPaid > totalBet) revert`) still matches the removed vault formula.
    - FC-390-06 (LOW/INFO, cross-ref 393/ACCESS-02): keeper-bounty creditFlip issuance bounded (one
      box/wallet/day redemption; one decimator entry per real burn) so a keeper cannot mint unbounded
      BURNIE claim-value that downstream dilutes ETH backing — note BURNIE is off the direct spine but
      ask the council to confirm the issuance bound.
    - FC-390-07 (INFO): vault deposit() removal completeness — no remaining game-side `vault.deposit(...)`
      call; vaultMintAllowance() over-mint reverts.
    - Inherited cross-refs to fold into the slice: FC-389-02 / FC-389-08 (the solvency-conservation half
      of the uint96 `_pendingRedemptionEthValue` / uint128 `poolBalances` narrowing casts — ask whether
      ANY redemption path inflates segregated ETH / a pool beyond width so a silent truncation understates
      segregated ETH → solvency-accounting drift); FC-392-08 (the redemption ETH-spin reaches a live
      ETH-pool RMW + recirc box INSIDE the claim path via `allowEthSpin=true` — ask whether the ETH-spin
      pool writes + dust-forfeit + `pendingRedemptionEthValue` release reconcile EXACTLY and whether the
      recirc box's `_applyEvMultiplierWithCap` cap RMW can be raced across redemption chunks);
      FC-393-02 (forfeit-to-self timing — a keeper biasing WHICH redemptions settle to steer when
      forfeits land vs full lootboxes resolve; confirm no per-victim value extraction, only benign
      backing-accrual timing); FC-393-03 (partial-balance redemption-leg solvency — under an adversarial
      sequence of many partial-balance live claims in ONE block, each leg sends
      `min(address(this).balance, legAmount)` ETH and pulls the rest as stETH; confirm segregation
      accounting never strands ETH or under-pulls stETH).

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 390-02. No
    fenced solidity blocks beyond the minimal identifier/selector/line citations the council needs to
    locate the code.
  </action>
  <verify>
    <automated>test -s .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -q "a8b702a7" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -q "FC-390-01" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -q "FC-390-02" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -q "FC-392-08" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -q "FC-393-03" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -Eq "SOLV-05|liveness" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md && grep -Eq "SOLV-06|CEI" .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md</automated>
  </verify>
  <acceptance_criteria>
    390-01-COUNCIL-PROMPT-SOLV.md exists and is non-empty; instructs reading frozen source at
    `a8b702a7` via git show; states the GAME claimablePool identity + the sDGNRS backing identity as the
    thesis to break; names ALL of SOLV-01..07 + FC-390-01..07 + the inherited cross-refs FC-389-02/-08,
    FC-392-08, FC-393-02/-03; charges the three §6-prime targets (liveness-gate SOLV-05/FC-390-01,
    dust-forfeit SOLV-04/FC-390-02, CEI SOLV-06) as dedicated numbered break-targets with the multi-tx
    ordering interleavings spelled out; uses neutral vocabulary; carries the KNOWN-BY-DESIGN exclusion
    list; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral SOLVENCY council prompt is authored against the frozen subject, covering all 7 SOLV reqs
    + the 7 FC-390 leads + the 5 inherited cross-refs, with the three prime targets charged hard, ready
    to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the council fan-out and record the council-net capture</name>
  <files>.planning/phases/390-solvency-spine/council/solv.gemini.txt, .planning/phases/390-solvency-spine/council/solv.codex.txt, .planning/phases/390-solvency-spine/council/solv.council.json, .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md (Task 1 output)
    - .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md (the just-approved COUNCIL-NET capture record shape to match)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/390-solvency-spine/council --label solv
    .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md`. council.sh fans gemini+codex in
    parallel internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations
    is needed beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped
    CLI is recorded in the council.json skipped[] and is NOT fatal; do not treat a skip as a failure.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be
    empty ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify).
    Do NOT run `hardhat compile --force` (regenerates ContractAddresses source).

    Write 390-01-COUNCIL-NET.md recording: the solv.council.json manifest's available[]/skipped[], the
    raw output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 390-02), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for SOLVENCY" line so
    390-02 can confirm both-nets-on-record before any verdict. If a CLI is in skipped[], record WHICH
    and note the slice still has the other model on record (a single available model satisfies "council
    on record" with the skip documented; both-unavailable would require a re-run and must be surfaced,
    not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/390-solvency-spine/council/solv.council.json && test -s .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `solv.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable
    CLI is in the council.json skipped[]; 390-01-COUNCIL-NET.md records the available/skipped + raw
    output paths + the byte-freeze attestation + an explicit "NET 1 ON RECORD" line; `git diff a8b702a7
    -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the SOLVENCY slice with raw output captured under
    the phase council/ dir, the subject proven byte-frozen, and the capture documented for the Wave-2
    adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable CLI silently dropping the only net (handled by skipped[]) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-390-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir |
| T-390-02 | Information disclosure / false-green | the SOLVENCY slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 390-01-COUNCIL-NET.md; both-unavailable is surfaced for re-run, never passed silently |
| T-390-03 | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers SOLV-01..07 + FC-390-01..07
  + the inherited cross-refs, with the three prime targets (liveness, dust-forfeit, CEI) charged hard.
- `council.sh` ran for `--label solv`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[].
- 390-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full SOLVENCY-SPINE surface, with raw
gemini/codex output captured under the phase council/ dir and the capture documented, so the Wave-2
Claude-net + adjudication plan can fold the council leads in before issuing any per-item verdict. The
subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/390-solvency-spine/390-01-SUMMARY.md` when done
</output>
</content>
</invoke>
