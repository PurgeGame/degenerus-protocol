Read the frozen `a8b702a7` sources via `git show`. I found **no reachable solvency-spine finding** in this slice.

One important caveat: the literal `claimablePool == Σ balancesPacked` equality has a documented conservative exception for decimator rounds: `claimablePool` can pre-reserve an unclaimed decimator pool before individual winners are credited (`a8b702a7:contracts/storage/DegenerusGameStorage.sol:356-366`). That over-reserves backing; it did not produce an underbacked payout path.

**SOLV-01..07**
- **SOLV-01 VERIFIED SOUND:** claimable credits/debits pair at call sites. Direct redemption pulls value then `_creditClaimable` + `claimablePool += amount` (`DegenerusGameLootboxModule.sol:1004-1014`); `claimWinnings` debits packed balance and pool before payout (`DegenerusGame.sol:1247-1248`); reserve pull debits SDGNRS and pool before ETH transfer (`DegenerusGame.sol:1582-1584`). Jackpot folds use one pool bump after accumulated credits (`DegenerusGameJackpotModule.sol:708-713`, `1118-1120`).
- **SOLV-02 VERIFIED SOUND:** sDGNRS backing excludes `_pendingRedemptionEthValue` in previews/burn math (`StakedDegenerusStonk.sol:671-675`, `921-925`). Submit reserves MAX before incrementing pending (`1062-1066`), resolve lowers MAX to rolled (`742-745`), and claim releases the rolled amount (`854`).
- **SOLV-03 VERIFIED SOUND:** per-claim split is exact: gameOver sends `totalRolledEth`; live path splits direct + lootbox, and dust sets `forfeitEth = lootboxEth; lootboxEth = 0` (`StakedDegenerusStonk.sol:830-848`). Every leg’s value movement sums to `totalRolledEth`, and failed external legs revert the earlier pending decrement.
- **SOLV-04 VERIFIED SOUND:** dust-forfeit self-credit is value-in-before-credit. sDGNRS sends `min(balance, forfeitEth)` and the Game pulls the exact stETH remainder before crediting (`StakedDegenerusStonk.sol:897-900`, `DegenerusGameLootboxModule.sol:1006-1014`). If balance/approval is insufficient, the leg reverts before `claimablePool` moves.
- **SOLV-05 VERIFIED SOUND:** no liveness-window strand/double-credit. If `claimRedemption` lands before the game-over drain tx, its direct credit is already in `claimablePool` before the drain snapshots `totalFunds`/`reserved` (`DegenerusGameGameOverModule.sol:78-85`). If the drain lands first, `gameOver = true` is latched (`135`) and later claims use the post-game direct path. A claim cannot execute “after line 78 but before line 206” of another tx; EVM txs are atomic.
- **SOLV-06 VERIFIED SOUND:** the V62-03 class is closed on the spine payout legs. `claimWinnings` debits before payout and sends stETH before any untrusted ETH call (`DegenerusGame.sol:1247-1254`, `1888-1915`); sDGNRS `_payEth` also transfers stETH before ETH (`StakedDegenerusStonk.sol:1108-1115`); `pullRedemptionReserve` debits before calling SDGNRS (`DegenerusGame.sol:1582-1585`). `distributeYieldSurplus` counts `claimablePool`, pools, yield accumulator, and pending frozen pools (`DegenerusGameJackpotModule.sol:682-700`).
- **SOLV-07 VERIFIED SOUND:** JackpotModule delta-fold is single-counted. Yield surplus caches then assigns one folded pool value (`708-713`); daily jackpot accumulates `liabilityDelta` then bumps once (`1100-1120`); solo whale-pass cost routes to `futurePrizePool` while only ETH goes to `claimableDelta` (`1265-1275`).

**Break Targets**
- **FC-390-01 VERIFIED SOUND:** same as SOLV-05. `claimRedemptionMany` caches `isGameOver`, but its live loop has no untrusted player ETH hook capable of flipping `gameOver` mid-batch (`StakedDegenerusStonk.sol:787-814`).
- **FC-390-02 VERIFIED SOUND:** same as SOLV-04. Many partial-balance claims are covered because each leg recomputes current ETH balance and pulls the remainder as stETH (`880-890`, `897-900`).
- **FC-390-03 VERIFIED SOUND:** decimator claims cache `over`/`frozen` consistently; claim effects cannot flip `gameOver` mid-loop (`DegenerusGameDecimatorModule.sol:329-353`). Live claims move lootbox half out of the pre-reserved pool (`459-463`); post-game claims credit from the already reserved pool (`401-404`).
- **FC-390-04 VERIFIED SOUND:** `claimValue > combined` implies `claimable != 0` because `claimValue <= combined + claimable` for any accepted burn amount; last-share burn gives equality when `claimable == 0`, not `>` (`DegenerusVault.sol:753-763`, share burn guard at `309-315`).
- **FC-390-05 VERIFIED SOUND:** vault overpay guard matches exactly: vault forwards combined value (`DegenerusVault.sol:578-584`), game computes `totalBet = amountPerTicket * ticketCount` and reverts if `ethPaid > totalBet` (`DegenerusGameDegeneretteModule.sol:535`, `593-597`).
- **FC-390-06 VERIFIED SOUND:** keeper BURNIE issuance is bounded. Redemption pays only per settled `(player, day)` slot (`StakedDegenerusStonk.sol:803-814`) and each slot requires a real burn with per-wallet/day cap (`314-315`, `1078-1083`). Decimator pays per real claimed entry (`DegenerusGameDecimatorModule.sol:343-369`). At 5-50 gwei, the 0.000024/0.000015 ETH reference bounties are below real gas cost and remain illiquid flip credit.
- **FC-390-07 VERIFIED SOUND:** no frozen-tree `vault.deposit(...)` callers remain. `vaultMintTo` checks live allowance before minting (`BurnieCoin.sol:542-549`).

**Cross-Refs**
- Narrowing casts are not reachable as solvency drift: `_pendingRedemptionEthValue` is real-ETH bounded (`StakedDegenerusStonk.sol:221-223`), submit/resolve/claim use checked arithmetic before uint96 casts (`745`, `854`, `1066`), and pool transfers clamp to available balances (`548-559`, `579-591`).
- Redemption ETH-spin path reconciles: `resolveRedemptionLootbox` credits the pool before chunk resolution (`DegenerusGameLootboxModule.sol:938-947`), ETH-spin flushes claimable/pool writes before recirc (`DegenerusGameDegeneretteModule.sol:1439-1463`), and recirc disables ETH-spin cascade.
- Forfeit-to-self timing is non-extractive: the split is deterministic from fixed `roll` and owed value (`StakedDegenerusStonk.sol:826-848`); keeper choice changes timing of SDGNRS backing accrual, not victim value.
