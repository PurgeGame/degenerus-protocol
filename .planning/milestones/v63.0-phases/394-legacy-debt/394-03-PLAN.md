---
phase: 394-legacy-debt
plan: 03
type: execute
wave: 2
depends_on: ["394-01"]
files_modified:
  - .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md
  - .planning/phases/394-legacy-debt/394-FINDINGS-V50.md
  - audit/FINDINGS-v50.0.md
autonomous: true
requirements: [LEGACY-01, LEGACY-02, LEGACY-05]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full v50 surface (whale-pass O(1) + box-open record; AFSUB pass-gating + OPEN-E; MINTDIV index alignment), independent of the council"
    - "Every LEGACY-01 + LEGACY-02 sub-item has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the value-equivalence arithmetic / freeze snapshot / consent-gate / index-alignment invariant + source-cite (file:line at a8b702a7) that settles it"
    - "The whale-pass O(1) deferred-claim path is settled by a dedicated value-equivalence + freeze-safety analysis (the O(1) claim materializes the SAME pass horizon / box value as the retired inline mint; the WHALE-04 freeze re-verified in code, not trusted from the prior paper proof; no double-claim)"
    - "The AFSUB pass-gating is settled by confirming the inclusive-boundary refresh/evict is CODED as documented (keep while currentLevel <= validThroughLevel, evict at +1 — NOT flagging the intended leniency) and the OPEN-E operator-approval consent gate is checked at the right SUBSCRIBE point"
    - "The MINTDIV index alignment is settled by a lockstep proof — every path advances processed by exactly take (not a writes-derived count); no cross-path drift / double-write / skip across a write-budget boundary"
    - "Both nets are on record before the verdict; the skeptic gate is run before any CATASTROPHE/HIGH (a value-non-equivalence / freeze break is SPINE/DOMINANT)"
    - "audit/FINDINGS-v50.0.md is authored, matching the audit/FINDINGS-v62.0.md format (frozen subject SHA + baseline + date + method header; executive summary with a disposition table; per-finding sections; refuted/by-design section; the v50 surface coverage stated)"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the v50 surface, independent of the council — the whale-pass O(1) value-equivalence + freeze re-verification, the AFSUB boundary/consent analysis, the MINTDIV lockstep proof"
      min_lines: 80
    - path: ".planning/phases/394-legacy-debt/394-FINDINGS-V50.md"
      provides: "The v50 per-item adjudication table: every LEGACY-01/02 sub-item with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing"
      min_lines: 60
    - path: "audit/FINDINGS-v50.0.md"
      provides: "The deferred v50 FINDINGS deliverable (LEGACY-05), matching the FINDINGS-v62.0 format, authored from the dual-net adjudication of the v50 surface"
      min_lines: 60
  key_links:
    - from: "394-FINDINGS-V50.md"
      to: "394-01 council outputs (council/v50.*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "audit/FINDINGS-v50.0.md"
      to: "LEGACY-01 + LEGACY-02 surface (whale-pass O(1) / AFSUB / MINTDIV)"
      via: "the authored deliverable covering the v50 surface"
      pattern: "whale|AFSUB|MINTDIV|claimWhalePass"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME v50 surface the council swept (394-01), then
ADJUDICATE every LEGACY-01 + LEGACY-02 sub-item (the whale-pass O(1) deferred-claim path + box-open record;
the AFSUB pass-gating validThroughLevel eviction/refresh + the OPEN-E operator-approval re-attest; the
MINTDIV index alignment) against the byte-frozen subject `a8b702a7`, folding in the NET-1 council leads.
Give the whale-pass O(1) path a dedicated value-equivalence + freeze-safety re-verification (the O(1) claim
materializes the same value as the retired inline mint; the WHALE-04 freeze re-verified in code), confirm
the AFSUB inclusive-boundary refresh/evict is coded as documented + the OPEN-E consent gate is checked at
the right point, prove the MINTDIV index advances in lockstep with no drift/double-write/skip, run the
skeptic gate before presenting anything as CATASTROPHE/HIGH, write `394-FINDINGS-V50.md` (the slice
adjudication), and AUTHOR `audit/FINDINGS-v50.0.md` (LEGACY-05 — the deferred v50 deliverable, matching the
FINDINGS-v62.0 format).

Purpose: a no-finding verdict for any sweep slice requires BOTH nets on record (AUDIT-V63-PLAN §2). This
plan puts NET 2 on record, synthesizes both nets for the v50 slice, and discharges the deferred v50 FINDINGS
debt. A value-non-equivalence (the O(1) path over-/under-delivers) is SPINE-level; a freeze break on the
deferred claim is DOMINANT; a MINTDIV drift that double-mints or drops paid traits is SPINE-level — so the
whale-pass value-equivalence + freeze and the MINTDIV lockstep get dedicated rigorous treatment. The v50
sweep VERIFIES freeze / value-equivalence / index-alignment / consent-correctness; it does NOT re-litigate
the by-design AFSUB inclusive boundary or the OPEN-E operator-approval trust boundary (§5 + the standing by-
design rulings). Any CONFIRMED finding is DOCUMENTED + ROUTED to a gated USER-hand-review fix, never fixed
here.

Output: 394-03-CLAUDE-NET.md (the independent adversarial analysis), 394-FINDINGS-V50.md (the slice
adjudication), and audit/FINDINGS-v50.0.md (the deferred deliverable). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md
@.planning/phases/394-legacy-debt/council/v50.gemini.txt
@.planning/phases/394-legacy-debt/council/v50.codex.txt
@audit/FINDINGS-v62.0.md
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 2 v50 reqs + LEGACY-05 to adjudicate/discharge (REQUIREMENTS.md): -->
<!--   LEGACY-01 whale-pass O(1) deferred-claim path + box-open record; -->
<!--   LEGACY-02 AFSUB pass-gating (validThroughLevel evict/refresh + OPEN-E re-attest) + MINTDIV alignment; -->
<!--   LEGACY-05 author audit/FINDINGS-v50.0.md (the deferred v50 deliverable). -->
<!-- Authoritative frozen v50 line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   DegenerusGame.sol: claimWhalePass @1503 → _claimWhalePassFor @1508 → delegatecall selector @1513. -->
<!--   DegenerusGameWhaleModule.sol: claimWhalePass(address) @991 (O(1) deferred mint); _recordLootboxEntry -->
<!--     @371/514; LootBoxBuy event @52 (index = lootbox RNG index @49; presale false for pass box @51). -->
<!--   GameAfkingModule.sol: SUB-02 consent OR operatorApprovals[subscriber][msg.sender] @314-317; OPENE-04 -->
<!--     non-self fundingSource operatorApprovals[fundingSource][subscriber] @322-328 (subscribe-only -->
<!--     @299/337); refresh validThroughLevel = _passHorizonOf @419; eviction guard @427; inclusive-boundary -->
<!--     walk currentLevel > sub.validThroughLevel @1246 → validThroughLevel = h @1250 (BY-DESIGN inclusive). -->
<!--   DegenerusGameMintModule.sol: trait write loop @581-681 (processed @581, take = min(owed,maxT) @634, -->
<!--     writesThis @649-651, processed += take @668, per-player reset processed = 0 @608/617/676); the -->
<!--     _processOneTicketEntry caller @881-894. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110 (v50 coverage carried from Phase 336: -->
<!--     whale-pass equivalence + uniform-O(1) + freeze fuzz; AFSUB sweep/evict/refresh + no-pass-SLOAD -->
<!--     oracle; MINTDIV cross-path equality). -->
<!--   FINDINGS-v62.0 format to MATCH (audit/FINDINGS-v62.0.md): header (frozen subject SHA + baseline + -->
<!--     date + method + pipeline) → executive summary + disposition table → actionable findings (per-finding: -->
<!--     origin / defect / reproduced / impact / candidate-fix-gated) → lower-severity → refuted/by-design → -->
<!--     reproduction artifacts → remediation order. For a 0-CONFIRMED outcome, mirror the structure with a -->
<!--     "0 actionable" executive summary + the per-surface attestation + the refuted/by-design + the by- -->
<!--     design-rulings-respected list. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial v50 analysis — whale-pass O(1) value-equivalence + freeze re-verification + AFSUB boundary/consent + MINTDIV lockstep proof vs frozen source</name>
  <files>.planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md</files>
  <read_first>
    - .planning/STATE.md (the v50 charge spec section — the three v50 items + what the v50 close already mitigated [WHALE-04 freeze at SPEC 334, TST-01/03 at 336])
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (freeze DOMINANT; value-non-equivalence/solvency SPINE; access/reentrancy confirmatory) + §5 design-intent anchor (the AFSUB inclusive boundary + the OPEN-E trust boundary are by-design — verify, do not re-litigate)
    - .planning/REQUIREMENTS.md (the exact LEGACY-01 + LEGACY-02 + LEGACY-05 text)
    - the frozen source via git show a8b702a7:contracts/DegenerusGame.sol (claimWhalePass @1503-1513), contracts/modules/DegenerusGameWhaleModule.sol (claimWhalePass @991, _recordLootboxEntry @371/514, LootBoxBuy event @52), contracts/modules/GameAfkingModule.sol (the consent + OPENE-04 gate @314-337, refresh @419, eviction guard @427, the inclusive-boundary walk @1242-1250), contracts/modules/DegenerusGameMintModule.sol (the trait write loop @581-681, the _processOneTicketEntry caller @881-894)
    - the standing by-design rulings: [[afking-pass-eviction-inclusive-boundary-intended]], [[open-e-operator-approval-trust-boundary]], [[degenerette-wwxrp-rtp-by-design]]
    - .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md + council/v50.*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the value-equivalence / freeze / boundary / lockstep claims first, then fold the
    council leads in at the end of this task). For each v50 item, attempt to BREAK the claim with a concrete
    reachable call sequence (the multi-tx claim/subscribe/advance interleaving where ordering matters), then
    record the result.

    v50 adjudication (LEGACY-01 + LEGACY-02 sub-items):

    - LEGACY-01a (whale-pass O(1) value-equivalence — DEDICATED analysis): the O(1) deferred claimWhalePass
      (WhaleModule:991, dispatched via Game:1503-1513) replaced the retired inline ~100-loop pass mint.
      Confirm the deferred claim materializes the SAME value the inline mint would have — the same pass
      horizon (the _passHorizonOf / validThroughLevel set), the same stat application, the same box value
      (_recordLootboxEntry @371/514 deposits the same lootbox ETH share). Attack: any input/state where the
      deferred path over-delivers (a horizon/box larger than the paid value) or under-delivers (a level/box
      dropped), and any DOUBLE-CLAIM where claimWhalePass can be re-driven to materialize the pass or the box
      twice (the claim flag/record not cleared before the external effect). Settle with the value-equivalence
      reasoning + the cite.

    - LEGACY-01b (whale-pass deferred-claim FREEZE-safety — re-verify WHALE-04 in code): the box-open record
      carries the lootbox RNG index (LootBoxBuy @49/52). Trace BACKWARD from the deferred resolution to the
      player's commitment point and confirm the VRF word / index was UNKNOWN at that point — the index is
      anchored at deposit BEFORE the word lands, and the deferred claim cannot steer which word/index it
      reads. Do NOT trust the SPEC-334 paper proof — re-verify at the frozen source. State the commitment
      point that settles it, or surface a finding.

    - LEGACY-02a (AFSUB pass-gating boundary + OPEN-E consent): confirm the inclusive-boundary refresh/evict
      is CODED as documented — the per-walk compare currentLevel > sub.validThroughLevel (@1246) evicts at
      +1, refresh sets validThroughLevel = h (@1250), the eviction guard (@427) handles no-pass/expired-pass;
      this is the INTENDED keep-while-<= / evict-at-+1 boundary ([[afking-pass-eviction-inclusive-boundary-
      intended]]) — verify the compare matches the documented boundary, do NOT flag the leniency. Attack:
      any EARLY eviction (a funded sub dropped before its horizon) or LATE retention (a stale validThroughLevel
      kept past its horizon). For OPEN-E: confirm the consent gate (SUB-02 self/operator @314-317; OPENE-04
      non-self fundingSource @322-328) is checked at the SUBSCRIBE point and cannot be bypassed at subscribe;
      the "later revoke does not stop an active sub" + "re-point = re-subscribe re-checks" are documented
      intent ([[open-e-operator-approval-trust-boundary]] — do not model a tricked-into-approving actor).
      Settle or surface.

    - LEGACY-02b (MINTDIV index alignment — DEDICATED lockstep proof): confirm every path advances the
      within-player trait index by `processed += take` (MintModule:668), where take = min(owed,maxT) (@634)
      is the ACTUAL tickets taken, with the writes-budget (writesThis @649-651) kept SEPARATE from the index
      advance, and the per-player reset processed = 0 (@608/617/676) at the player boundary. Attack: any
      cross-path divergence where the trait write index and the ticket walk index (_processOneTicketEntry
      @881-894) fall OUT of lockstep — a ticket processed twice (double-write/extra mint) or skipped (paid
      traits lost) under a write-budget boundary that splits a player's mint across advance chunks; any
      off-by-one at the per-player reset when a player completes mid-budget. Prove every path advances
      processed by exactly take and the budget split never drops/duplicates a ticket, or surface a finding.

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried · STATE VAR + file:line at
    a8b702a7 · the value-equivalence/freeze/consent/lockstep reasoning + cite that settles it · a provisional
    verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where the green baseline already proves a
    property (the Phase-336 whale-pass equivalence + freeze fuzz, the AFSUB sweep/evict/refresh + no-pass-
    SLOAD oracle, the MINTDIV cross-path equality in REGRESSION-BASELINE-v63). At the END of the task, read
    the council outputs (394-01 council/v50.*.txt) and note for each item whether the council raised a
    convergent/divergent lead — fold any council-only lead in as an additional row to adjudicate. Write all
    of this to 394-03-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use neutral
    defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md && for id in LEGACY-01 LEGACY-02; do grep -q "$id" .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "claimWhalePass|value.?equivalen|O\\(1\\)" .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md && grep -Eqi "freeze|commitment|backward" .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md && grep -Eqi "validThroughLevel|OPEN-?E|consent" .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md && grep -Eqi "MINTDIV|processed|lockstep|take" .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    394-03-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a provisional
    verdict for EACH v50 sub-item (whale-pass O(1) value-equivalence; whale-pass deferred-claim freeze; AFSUB
    boundary + OPEN-E consent; MINTDIV lockstep); the whale-pass O(1) carries the value-equivalence reasoning;
    the freeze re-verification cites the commitment point (not the prior paper proof); the AFSUB confirms the
    inclusive boundary as-coded + the consent point; the MINTDIV carries the lockstep proof (processed += take,
    no drift/double-write/skip); the council leads are folded in at the end; `git diff a8b702a7 -- contracts/`
    is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record for the v50 slice, independent of the council, with a
    per-item attack + provisional verdict, the whale-pass O(1) value-equivalence + freeze re-verified, the
    AFSUB boundary/consent confirmed, the MINTDIV lockstep proven, and the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 394-FINDINGS-V50.md + author audit/FINDINGS-v50.0.md</name>
  <files>.planning/phases/394-legacy-debt/394-FINDINGS-V50.md, audit/FINDINGS-v50.0.md</files>
  <read_first>
    - .planning/phases/394-legacy-debt/394-03-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md + council/v50.*.txt (NET 1 leads)
    - audit/FINDINGS-v62.0.md (the canonical FINDINGS deliverable FORMAT to match — header / executive summary + disposition table / per-finding sections / refuted+by-design / remediation; mirror it for a 0-CONFIRMED outcome)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md (the approved per-item slice-adjudication FINDINGS structure — both-nets table, per-item verdict table, skeptic gate, routing block)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record) + §4 (threat weighting) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact LEGACY-01 + LEGACY-02 + LEGACY-05 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into TWO deliverables: the slice adjudication
    394-FINDINGS-V50.md AND the deferred v50 deliverable audit/FINDINGS-v50.0.md (LEGACY-05).

    (A) 394-FINDINGS-V50.md — the slice adjudication. Structure:
    (1) Header: subject `a8b702a7` byte-frozen (contracts tree 2934d3d8…); baseline = REGRESSION-BASELINE-v63
    (854/0/110); method = COUNCIL + CLAUDE both; posture = AUDIT-ONLY (a CONFIRMED finding routes to a gated
    USER-hand-review fix, never fixed here); the threat weighting (freeze DOMINANT / value-non-equivalence
    SPINE / access confirmatory); the design-intent anchor (the AFSUB inclusive boundary + the OPEN-E trust
    boundary + the whale-pass economics are by-design — verify, do not re-litigate).
    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    394-01-COUNCIL-NET.md, codex skip + 396 re-run flag if applicable) and NET 2 (Claude — 394-03-CLAUDE-NET)
    are both on record for the v50 slice. A no-finding verdict for any item MUST cite both nets.
    (3) The per-item adjudication table — ONE row per v50 sub-item (whale-pass O(1) value-equivalence;
    whale-pass deferred-claim freeze; AFSUB boundary + OPEN-E consent; MINTDIV lockstep), covering LEGACY-01
    + LEGACY-02. Columns: ITEM · what it claims · NET-1 result · NET-2 result · ADJUDICATED VERDICT
    (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) · the settling value-equivalence/freeze/consent/lockstep cite
    (file:line at a8b702a7 or the green-baseline anchor). Weight each finding per §4. Apply the §5 design-
    intent anchor.
    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter (structural-
    protection check + the 3-condition EV lens) and record the gate result. If nothing reaches HIGH, state
    that explicitly with the gate applied to the value-bearing items (the O(1) value-equivalence, the MINTDIV
    lockstep). If a real break surfaces (value-non-equivalence, a freeze break, a MINTDIV drift), run the full
    skeptic gate and weight it.
    (5) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding, its
    weight, the proposed fix shape, the note that it goes to a SEPARATE gated USER-hand-review boundary —
    batched, never auto-committed; the subject re-freezes after). If zero CONFIRMED, state "0 CONFIRMED —
    document-only; LEGACY-01..02 attested at a8b702a7" and note any INFO/MONITOR items.
    (6) Re-attestation line: each of LEGACY-01 + LEGACY-02 marked attested-or-finding.

    (B) audit/FINDINGS-v50.0.md — the deferred v50 deliverable (LEGACY-05). AUTHOR it MATCHING the
    FINDINGS-v62.0 format:
    - Header: title "FINDINGS — v50.0 (Whale-Pass O(1) + AFSUB Pass-Gated Subs + MintModule Index Alignment
      — deferred deliverable, swept under v63.0 Phase 394 LEGACY-DEBT, CROSS-MODEL-LED)"; the frozen audit
      subject SHA `a8b702a7` (the v50 surface as it exists at the v63 subject, the form the cumulative sweep
      covered — NOTE the v50 surface was NOT separately re-frozen; it is audited as it stands in the byte-
      frozen v63 subject); the v50 close history (closed 2026-05-28 MINIMAL CLOSE, the internal sweep
      deferred → folded into v63 Phase 394); date; method = COUNCIL + CLAUDE both (the dual-net); the council
      pipeline (council.sh → gemini + codex, codex skip documented if applicable).
    - Executive summary + a disposition table (HIGH / MED / LOW / refuted+by-design counts) reflecting the
      ACTUAL v50 adjudication outcome (mirror the v62 structure; for a 0-actionable outcome state it plainly
      with the per-surface attestation).
    - The v50 surface coverage stated: the whale-pass O(1) deferred-claim path + box-open record; the AFSUB
      pass-gating (validThroughLevel evict/refresh + OPEN-E re-attest); the MINTDIV index alignment — each
      with its adjudicated verdict + the settling cite.
    - Actionable findings (if any) in the v62 per-finding shape (origin / defect / impact / candidate-fix-
      gated); lower-severity; refuted/by-design (the AFSUB inclusive boundary, the OPEN-E operator-approval,
      the whale-pass economics — recorded so they are not re-flagged); the prior mitigations on record
      (WHALE-04 freeze SPEC 334, TST-01/03 336, the green-baseline coverage); any routed item.
    - Keep it factual and neutral; do NOT chmod 444 (the terminal v63 close handles read-only sealing; this
      is an in-milestone authored deliverable).

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No contract
    source is modified.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-FINDINGS-V50.md && test -s audit/FINDINGS-v50.0.md && for id in LEGACY-01 LEGACY-02; do grep -q "$id" .planning/phases/394-legacy-debt/394-FINDINGS-V50.md || { echo "MISSING $id in slice"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/394-legacy-debt/394-FINDINGS-V50.md && grep -Eqi "skeptic" .planning/phases/394-legacy-debt/394-FINDINGS-V50.md && grep -q "a8b702a7" audit/FINDINGS-v50.0.md && grep -Eqi "whale.?pass|claimWhalePass" audit/FINDINGS-v50.0.md && grep -Eqi "AFSUB|validThroughLevel|OPEN-?E" audit/FINDINGS-v50.0.md && grep -Eqi "MINTDIV|index align" audit/FINDINGS-v50.0.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    394-FINDINGS-V50.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH v50
    sub-item (covering LEGACY-01 + LEGACY-02) with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict + settling
    cite; the skeptic gate run + recorded; any CONFIRMED finding documented + ROUTED (not fixed). AND
    audit/FINDINGS-v50.0.md is authored matching the FINDINGS-v62.0 format: frozen subject SHA `a8b702a7` +
    the v50 close history + date + method header; executive summary + disposition table; the v50 surface
    coverage (whale-pass O(1) / AFSUB / MINTDIV) each with a verdict; the refuted/by-design section (the
    AFSUB inclusive boundary + OPEN-E + whale-pass economics recorded). `git diff a8b702a7 -- contracts/` is
    empty.
  </acceptance_criteria>
  <done>
    The v50 slice is adjudicated with both nets on record, the skeptic gate applied, every LEGACY-01/02
    sub-item carrying an explicit verdict, the whale-pass O(1) value-equivalence + freeze re-verified, the
    AFSUB boundary/consent confirmed, the MINTDIV lockstep proven, and the deferred audit/FINDINGS-v50.0.md
    authored (LEGACY-05) matching the FINDINGS-v62.0 format — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED value-non-equivalence / freeze break / MINTDIV drift, or a FALSE-GREEN adjudication waved through; or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-07 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-394-08 | Repudiation / false-green | a REFUTED/BY-DESIGN v50 verdict issued without both nets, or the whale-pass value-equivalence attested without the arithmetic, or the MINTDIV lockstep waved without a proof | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; the O(1) value-equivalence + the MINTDIV lockstep each require a concrete settling cite |
| T-394-09 | Elevation / under-weighting | a CONFIRMED value-non-equivalence / freeze break / MINTDIV drift under-weighted, or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; value-non-equivalence/MINTDIV weighted SPINE, freeze DOMINANT per §4 |
| T-394-10 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over the v50 sub-items.
- Both nets on record before the verdict; the both-nets table present in 394-FINDINGS-V50.md.
- The whale-pass O(1) cites value-equivalence; the freeze re-verification cites the commitment point; the
  AFSUB cites the inclusive boundary as-coded + the consent point; the MINTDIV cites the lockstep proof.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded.
- audit/FINDINGS-v50.0.md authored matching the FINDINGS-v62.0 format, covering the v50 surface.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The v50 legacy-debt slice (LEGACY-01 + LEGACY-02) is adjudicated against the byte-frozen subject `a8b702a7`
with BOTH nets on record, the skeptic gate applied, and every sub-item carrying an explicit
CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a value-equivalence arithmetic, freeze snapshot,
consent-gate, index-alignment invariant, or source-cite. The whale-pass O(1) value-equivalence + freeze is
re-verified in code; the AFSUB boundary/consent is confirmed; the MINTDIV lockstep is proven. The deferred
audit/FINDINGS-v50.0.md is authored (LEGACY-05) matching the FINDINGS-v62.0 format. Any CONFIRMED finding is
documented + routed to a gated fix; the subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/394-legacy-debt/394-03-SUMMARY.md` when done
</output>
