---
phase: 389-packing-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md
  - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md
  - .planning/phases/389-packing-identity/council/storage.gemini.txt
  - .planning/phases/389-packing-identity/council/storage.codex.txt
  - .planning/phases/389-packing-identity/council/storage.council.json
  - .planning/phases/389-packing-identity/council/gasid.gemini.txt
  - .planning/phases/389-packing-identity/council/gasid.codex.txt
  - .planning/phases/389-packing-identity/council/gasid.council.json
  - .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md
autonomous: true
requirements: [STORAGE-01, STORAGE-02, STORAGE-03, STORAGE-04, STORAGE-05, STORAGE-06, STORAGE-07, GASID-01, GASID-02, GASID-03, GASID-04, GASID-05]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the STORAGE slice (narrowing/RMW/slot-convention/two-window-eviction/capBucketCounts)"
    - "The cross-model council (NET 1) is on record for the GASID slice (selector/preimage/nibble-table/trait-roll behavior-identity)"
    - "Each council prompt is charged neutrally against the frozen subject a8b702a7 with the by-design rulings excluded so the council does not waste passes on settled intent"
    - "Either both gemini+codex outputs exist per slice, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md"
      provides: "Neutral council prompt for the STORAGE-01..07 slice + FC-389-01..04 leads"
      min_lines: 30
    - path: ".planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md"
      provides: "Neutral council prompt for the GASID-01..05 slice + FC-389-05..09 leads"
      min_lines: 30
    - path: ".planning/phases/389-packing-identity/389-01-COUNCIL-NET.md"
      provides: "Council-net capture record: raw output paths, available/skipped per slice, byte-freeze attestation"
      min_lines: 20
  key_links:
    - from: "389-01-COUNCIL-PROMPT-STORAGE.md"
      to: "council.sh --label storage"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label storage"
    - from: "389-01-COUNCIL-PROMPT-GASID.md"
      to: "council.sh --label gasid"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label gasid"
---

<objective>
Run NET 1 (the cross-model council) over the packing-identity surface — the STORAGE-01..07
correctness slice and the GASID-01..05 gas/refactor behavior-identity slice — against the
byte-frozen subject `a8b702a7`. Author two neutral council prompts (one per slice), fan each to
the gemini+codex CLIs via `council.sh`, capture the raw output under the phase `council/` dir, and
record the council-net capture (available/skipped, byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on
record. This plan puts NET 1 on record so the Wave-2 Claude-net + adjudication plan (389-02) can
fold the council leads in before any verdict. The council is the PRIMARY finder
([[cross-model-led-audits-over-claude-only]]).

Output: two COUNCIL-PROMPT docs, the raw gemini/codex output per slice, the council.json manifests,
and 389-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only
over the subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-01-LAYOUT-KEY.md
@.planning/v63-surface-map/storage-packing.md
@.planning/v63-surface-map/gas-identity.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension; with --schema => .json). -->
<!-- council.json shape: { "label", "schema", "models":[...], "skipped":[...], "outputs":{model:path} } -->
<!-- A missing/unauthed CLI is recorded in skipped[] and is NEVER fatal. -->
<!-- gemini + codex CLIs confirmed at ~/.local/bin. -->

<!-- v62 council-prompt shape reference (.planning/milestones/v62.0-phases/383-asymmetry-sweep-council-led/383-COUNCIL-PROMPT.md): -->
<!--   header role + "read EXACT frozen source at <sha> via git show <sha>:contracts/<File>.sol" -->
<!--   + threat-priority line + ALREADY-FOUND + KNOWN-BY-DESIGN exclusion list + numbered focus -->
<!--   + per-finding output format (PROPERTY · reachable CALL SEQUENCE · STATE VAR + file:line · SEVERITY · why protections don't stop it). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two neutral council prompts (STORAGE slice + GASID slice)</name>
  <files>.planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md, .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md</files>
  <read_first>
    - .planning/milestones/v62.0-phases/383-asymmetry-sweep-council-led/383-COUNCIL-PROMPT.md (the prompt shape to match)
    - .planning/v63-surface-map/storage-packing.md (FA-1..FA-4 + the 17 SOUND verdicts — the "what we believe is safe" thesis to break)
    - .planning/v63-surface-map/gas-identity.md (§1-§9 VERIFIED-IDENTICAL claims + F-1..F-5 leads)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-389-01..09 verbatim)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-01-LAYOUT-KEY.md (authoritative slots for citation)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting + §5 design-intent anchor
  </read_first>
  <action>
    Write 389-01-COUNCIL-PROMPT-STORAGE.md and 389-01-COUNCIL-PROMPT-GASID.md, each charged
    NEUTRALLY ("here is what we believe is safe — find where it breaks") and instructing the council
    to read the EXACT frozen source at `a8b702a7` via `git show a8b702a7:contracts/<File>.sol` (ignore
    the working tree). Use neutral defensive-engineering vocabulary throughout (identity, bound,
    co-resident, narrowing, masked RMW) per [[neutral-defensive-audit-vocabulary]].

    Each prompt MUST include: (a) the threat-priority line from AUDIT-V63-PLAN §4 (DOMINANT RNG/freeze;
    HIGH gas-DoS in advanceGame; SPINE solvency; LOW access-control/reentrancy/MEV), noting most
    packing-identity issues are correctness/solvency-adjacent not RNG; (b) a KNOWN-BY-DESIGN exclusion
    list so the council does not re-litigate settled intent (lootbox-resolution-timing, Degenerette
    RTP>100%/worthless WWXRP, the documented EV-multiplier lift/recycle relaxation are NOT in scope of
    this slice); (c) the per-finding output format (PROPERTY · reachable CALL SEQUENCE · STATE VAR +
    file:line at a8b702a7 · SEVERITY · why protections don't stop it · or "verified IDENTICAL/SOUND and
    why").

    STORAGE prompt — thesis to break, mapped to STORAGE-01..07: "every narrowed packed field's width
    >= its real-world maximum (no silent truncating cast); every masked RMW helper preserves co-resident
    fields; cross-module readers/writers of the delegatecall-shared packed slots use identical
    shift/mask conventions; the two-window `lootboxEvCapPacked` never evicts a live key under the real
    resolve-cursor-lag bound; all privatized/packed fields keep their external ABI getters; no harness
    hardcodes a moved slot; capBucketCounts is cap-exact-or-defended." Name the concrete leads to break:
    FC-389-01 (two-window EV-cap eviction under resolve-cursor lag >1 level — the §6 prime target; ask
    the council to find ANY path that records EV-cap usage at a level NOT in {currentLevel,
    currentLevel+1} at write time, e.g. a deferred/queued resolve after two transitions or a far-future
    ticket path), FC-389-02 (`_pendingRedemptionEthValue`=uint96 / `poolBalances`=uint128 narrowing
    casts truncate silently — find any path that inflates segregated ETH / a pool beyond width),
    FC-389-03 (`DecClaimRound.totalBurn` comment-vs-accumulator mismatch — confirm the bound holds and
    flag the comment), FC-389-04 (any `vm.store`/`vm.load` harness still hardcoding a pre-shift slot).
    Add STORAGE-07: ask the council whether `capBucketCounts` can exceed `maxTotal+4` and whether the
    documented downstream clamps fully defend the imprecision. Point them at the authoritative slots
    from 388-01-LAYOUT-KEY.md (Game tail @ slots 26/36/40/43/53; sDGNRS slot-0 pack; the DecClaimRound
    struct offsets 0/12/28) and the helper sites (`_lootboxEvUsedFor`/`_setLootboxEvUsedFor`,
    `_addLevelDgnrsClaimed`, `_setLevelDgnrsAllocation`, `_debitClaimableAndAfking`).

    GASID prompt — thesis to break, mapped to GASID-01..05: "the raw `delegatecall(msg.data)` dispatch
    resolves the same selector and ABI-decodes identically to the prior typed dispatch for all 30 routed
    fns; the hash1/hash2 RNG-byte migrations produce byte-identical keccak preimages; the PriceLookup
    nibble-table is output-identical over the full input domain; the trait-roll consolidation +
    `_farFutureSeed` extraction are equivalent across all inputs/boundaries/revert paths; no gas/refactor
    edit changed an externally-observable output/revert/event." Name the leads to break: FC-389-05
    (`DecClaimRound.rngWord` uint32 narrowing-equivalence half — the entropy-reduction is real; ask
    whether the narrowing changes any claim-time observable beyond the seed width), FC-389-06
    (`lootboxEvCapPacked` level-0 stamp collision — find any caller path that reaches level 0 / a uint24
    `level+1` wrap), FC-389-07 (`_addLevelDgnrsClaimed` unclamped high-half `newClaimed<<128` — find any
    claim path that does NOT enforce `claimed+add<=allocation<=2^128`), FC-389-08 (StakedStonk uint96/
    uint128 narrowings — confirm no path exceeds 2^96 wei segregated; cross-ref 390), FC-389-09 (the
    three dynamic-array `msg.data` wrappers `previewSellFarFutureTickets`/`claimAfkingBurnie`/
    `rawFulfillRandomWords` — ask whether malformed/oversized/non-canonical-offset calldata can make the
    module decoder diverge from the wrapper decoder). Cite the gas-identity map's load-bearing identities
    (the 30 selector table, the hash1/hash2 operand-width rule, the nibble-table `0x4333222111` expr,
    `_rollWinningTraitsPair`/`_applyHeroResult`/`_rollHeroSymbol`).

    Do NOT pre-state any verdict in the prompts — the council finds, Claude adjudicates at 389-02.
    No fenced solidity blocks beyond the minimal identifier/selector citations the council needs to
    locate the code.
  </action>
  <verify>
    <automated>test -s .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md && test -s .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md && grep -q "a8b702a7" .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md && grep -q "a8b702a7" .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md && grep -q "FC-389-01" .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md && grep -q "FC-389-09" .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md</automated>
  </verify>
  <acceptance_criteria>
    Both prompt files exist and are non-empty; each instructs reading frozen source at `a8b702a7`; the
    STORAGE prompt names FC-389-01..04 + STORAGE-07 (capBucketCounts) and the FA-1 two-window-eviction
    prime target; the GASID prompt names FC-389-05..09 and the GASID-01..05 identity theses; both use
    neutral vocabulary and carry the KNOWN-BY-DESIGN exclusion list; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The two neutral council prompts are authored against the frozen subject, each covering its slice's
    reqs + leads, ready to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the council fan-out (both slices) and record the council-net capture</name>
  <files>.planning/phases/389-packing-identity/council/storage.gemini.txt, .planning/phases/389-packing-identity/council/storage.codex.txt, .planning/phases/389-packing-identity/council/storage.council.json, .planning/phases/389-packing-identity/council/gasid.gemini.txt, .planning/phases/389-packing-identity/council/gasid.codex.txt, .planning/phases/389-packing-identity/council/gasid.council.json, .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md (Task 1 output)
    - .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-GASID.md (Task 1 output)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/389-packing-identity/council --label storage
    .planning/phases/389-packing-identity/389-01-COUNCIL-PROMPT-STORAGE.md`, then the same for
    `--label gasid` with the GASID prompt. Pace the two fan-outs serially (one slice, then the next)
    per [[pace-runs-to-survive-5h-cap]] — do not parallelize the two council invocations. A skipped
    CLI is recorded in the council.json skipped[] and is NOT fatal; do not treat a skip as a failure.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be
    empty ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify).
    Do NOT run `hardhat compile --force` (regenerates ContractAddresses source).

    Write 389-01-COUNCIL-NET.md recording: the two council.json manifests' available[]/skipped[] per
    slice, the raw output file paths, a one-line characterization of each model's output per slice
    (raw, NOT adjudicated — adjudication is 389-02), the byte-freeze attestation (`git diff a8b702a7 --
    contracts/` empty; `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for
    STORAGE + GASID" line so 389-02 can confirm both-nets-on-record before any verdict. If a CLI is in
    skipped[], record WHICH and note that the slice still has the other model on record (a single
    available model satisfies "council on record" with the skip documented; both-unavailable would
    require a re-run and must be surfaced, not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/389-packing-identity/council/storage.council.json && test -f .planning/phases/389-packing-identity/council/gasid.council.json && test -s .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    Both `storage.council.json` and `gasid.council.json` exist; for each slice either both
    gemini+codex `.txt` outputs are non-empty OR the unavailable CLI is in the council.json skipped[];
    389-01-COUNCIL-NET.md records the available/skipped per slice + raw output paths + the byte-freeze
    attestation + an explicit "NET 1 ON RECORD" line; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for both the STORAGE and GASID slices with raw output
    captured under the phase council/ dir, the subject proven byte-frozen, and the capture documented
    for the Wave-2 adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable CLI silently dropping a net (handled by skipped[]) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-389-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir |
| T-389-02 | Information disclosure / false-green | a council slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 389-01-COUNCIL-NET.md; both-unavailable on a slice is surfaced for re-run, never passed silently |
| T-389-03 | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- Both council prompts exist, are neutral, charged against `a8b702a7`, cover their slice's reqs + leads.
- `council.sh` ran for both `--label storage` and `--label gasid`; both council.json manifests exist.
- For each slice: both model outputs non-empty OR the missing CLI in skipped[].
- 389-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full STORAGE + GASID packing-identity surface,
with raw gemini/codex output captured under the phase council/ dir and the capture documented, so the
Wave-2 Claude-net + adjudication plan can fold the council leads in before issuing any per-item
verdict. The subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/389-packing-identity/389-01-SUMMARY.md` when done
</output>
