---
phase: 396-terminal
plan: 03
type: execute
wave: 3
depends_on: ["396-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - .planning/phases/396-terminal/396-CLOSURE.md
autonomous: true
requirements: [TERM-03]
must_haves:
  truths:
    - "All 58 requirements in REQUIREMENTS.md are re-attested (each checkbox re-verified against its phase deliverable + the FINDINGS doc) and checked"
    - "The subject is re-confirmed byte-frozen: git diff a8b702a7 -- contracts/ is empty AND the contracts tree-hash equals 2934d3d8987a09c5f073549a0cb499f6c5f28620"
    - "The closure signal MILESTONE_V63_AT_HEAD_<sha> is emitted (per the v62 MILESTONES pattern)"
    - "The milestone is flipped to SHIPPED in STATE.md, ROADMAP.md, and MILESTONES.md"
    - "No contract files are modified; the plan + doc edits are committed (force-add .planning), with no contract-dir token in the commit message"
  artifacts:
    - path: ".planning/phases/396-terminal/396-CLOSURE.md"
      provides: "The closure record — 58-req re-attestation result, byte-freeze re-confirmation, the emitted signal, and the milestone flip"
      min_lines: 25
      contains: "MILESTONE_V63_AT_HEAD"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: "audit/FINDINGS-v63.0.md"
      via: "the v63.0 milestone entry cites the canonical deliverable + the closure signal"
      pattern: "MILESTONE_V63_AT_HEAD"
    - from: ".planning/REQUIREMENTS.md"
      to: "the 58 requirement checkboxes"
      via: "every requirement re-attested and checked"
      pattern: "TERM-03"
---

<objective>
Close the v63.0 milestone: re-attest all 58 requirements against their phase deliverables + the FINDINGS document, re-confirm the subject is byte-frozen at a8b702a7, emit the closure signal MILESTONE_V63_AT_HEAD_<sha> (per the v62 MILESTONES pattern), and flip the milestone status to SHIPPED across STATE.md / ROADMAP.md / MILESTONES.md.

Purpose: TERM-03 is the milestone flip. It seals v63.0 as a shipped audit milestone with the canonical FINDINGS deliverable + the immutable closure signal, and re-verifies the audit-only invariant (zero contract mutation) one final time.

Output: 58 re-attested requirements, the byte-freeze re-confirmation, the closure signal, the flipped milestone status, and 396-CLOSURE.md recording it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md

<read_first>
The deliverable from Plan 02 (the re-attestation evidence base):
@audit/FINDINGS-v63.0.md

The 58 requirements to re-attest:
@.planning/REQUIREMENTS.md

The v62 closure pattern to mirror (the MILESTONES entry format + the closure-signal shape):
@.planning/MILESTONES.md

Subject facts:
- Frozen subject SHA: a8b702a7. Contracts tree-hash: 2934d3d8987a09c5f073549a0cb499f6c5f28620.
- The v62 closure signal shape: MILESTONE_V62_AT_HEAD_77580320b88e77438c893602a068548961824893 (the marker token + the full subject SHA). v63's = MILESTONE_V63_AT_HEAD_<full-sha-of-the-frozen-subject-a8b702a7>.
- The canonical deliverable: audit/FINDINGS-v63.0.md (chmod 444) + AUDIT-V63-REPORT.html.
- The sole open gated item: BURNIE-04 (a separate post-audit USER-reviewed contract change, NOT part of this audit milestone).

Process constraints:
- AUDIT-ONLY — no contract edits. files_modified = .planning/ only.
- The commit-guard hook blocks commits while contracts/*.sol is dirty, and trips on the literal contract-dir path token in the commit MESSAGE — keep contracts clean and avoid that token in the message.
- .planning/ is gitignored → force-add the planning docs.
- The gsd-sdk state.* / phase.complete handlers mis-mutate this repo's custom STATE.md — do the milestone flip BY HAND (edit the frontmatter + status lines directly), do not rely on those handlers.
- Neutral defensive-engineering vocabulary throughout.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-attest all 58 requirements + re-confirm the byte-freeze</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/396-terminal/396-CLOSURE.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (all 58 requirement lines, grouped FND/STORAGE/GASID/SOLV/RNG/ECON/BURNIE/ACCESS/LEGACY/MUT/TERM); audit/FINDINGS-v63.0.md (the per-area verdicts that attest the sweep requirements); the per-phase SUMMARY/FINDINGS for any requirement needing a deliverable cross-check.
  </read_first>
  <action>
    Walk all 58 requirements in REQUIREMENTS.md. For each, re-verify it against its phase deliverable (the per-phase FINDINGS/SUMMARY) and the consolidated FINDINGS-v63.0.md, then check its box ([ ] → [x]). The three TERM requirements are attested by this terminal phase itself: TERM-01 (consolidation + council-on-refuted + skeptic gate — Plan 01 outputs), TERM-02 (FINDINGS-v63.0.md chmod 444 + AUDIT-V63-REPORT.html — Plan 02 outputs), TERM-03 (this closure). Re-confirm the subject is byte-frozen: run `git diff a8b702a7 -- contracts/` (expect EMPTY) and compute the contracts tree-hash, verifying it equals 2934d3d8987a09c5f073549a0cb499f6c5f28620. Start authoring 396-CLOSURE.md with the re-attestation result (58/58 checked, by category) and the byte-freeze re-confirmation (the empty-diff + tree-hash match). If any requirement cannot be attested, STOP and record the gap prominently rather than checking it. Use neutral defensive-engineering vocabulary.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^\- \[x\]' .planning/REQUIREMENTS.md)" -ge 58 ] && [ -z "$(git diff a8b702a7 -- contracts/)" ] && [ "$(git rev-parse a8b702a7:contracts 2>/dev/null)" = "2934d3d8987a09c5f073549a0cb499f6c5f28620" ] && test -f .planning/phases/396-terminal/396-CLOSURE.md && echo ATTEST_AND_FREEZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 58 requirement checkboxes in REQUIREMENTS.md are [x] (re-attested against deliverables, not blindly checked).
    - git diff a8b702a7 -- contracts/ is empty AND `git rev-parse a8b702a7:contracts` equals 2934d3d8987a09c5f073549a0cb499f6c5f28620.
    - 396-CLOSURE.md records the 58/58 re-attestation (by category) + the byte-freeze re-confirmation.
    - Any un-attestable requirement is recorded as a gap (NOT checked) rather than silently passed.
  </acceptance_criteria>
  <done>All 58 requirements are re-attested and checked; the subject is re-confirmed byte-frozen by empty diff + tree-hash match; 396-CLOSURE.md holds the evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the closure signal + flip the milestone to SHIPPED</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/MILESTONES.md, .planning/phases/396-terminal/396-CLOSURE.md</files>
  <read_first>
    .planning/MILESTONES.md (the v62.0 entry — mirror its closure-entry format: closure signal, closure verdict, canonical deliverable line; and the v63.0 ACTIVE entry to flip); .planning/STATE.md (the v63 milestone status + the per-phase dispositions to flip to SHIPPED); .planning/ROADMAP.md (the v63.0 phase list + the Phase 396 entry); 396-CLOSURE.md (Task 1 — append the signal + flip to it).
  </read_first>
  <action>
    Emit the closure signal and flip the milestone, by hand (do NOT use the gsd-sdk state.*/phase.complete handlers — they mis-mutate this repo's custom STATE.md). (1) Resolve the full SHA of the frozen subject (git rev-parse a8b702a7) and form the signal MILESTONE_V63_AT_HEAD_<full-sha>. (2) MILESTONES.md: flip the v63.0 entry from ACTIVE to SHIPPED (date 2026-06-15), add the closure signal, a one-line closure verdict (dual-net audit, 0 HIGH/CATASTROPHE, 1 CONFIRMED MED BURNIE-04 routed to a gated fix, contracts byte-frozen at a8b702a7, FINDINGS-v63.0.md chmod 444 + AUDIT-V63-REPORT.html), and the canonical-deliverable line — mirroring the v62.0 entry shape. (3) ROADMAP.md: mark Phase 396 + the v63.0 milestone header as SHIPPED/complete (check the phase boxes). (4) STATE.md: flip the v63 milestone status to SHIPPED and update the phase positions to closed — edit the frontmatter + status lines directly, then re-read to confirm the frontmatter is well-formed (this repo's STATE.md is custom; fix by hand if any handler-style drift appears). (5) Append the signal + the flip record to 396-CLOSURE.md. Use neutral defensive-engineering vocabulary. Do NOT push; do NOT modify any contract file.
  </action>
  <verify>
    <automated>grep -q "MILESTONE_V63_AT_HEAD" .planning/MILESTONES.md && grep -q "MILESTONE_V63_AT_HEAD" .planning/phases/396-terminal/396-CLOSURE.md && grep -qiE "SHIPPED" .planning/MILESTONES.md && grep -qi "v63" .planning/STATE.md && [ -z "$(git status --porcelain contracts/ 2>/dev/null)" ] && echo FLIP_OK</automated>
  </verify>
  <acceptance_criteria>
    - The closure signal MILESTONE_V63_AT_HEAD_<full-sha-of-a8b702a7> is recorded in MILESTONES.md + 396-CLOSURE.md.
    - MILESTONES.md v63.0 entry flipped ACTIVE → SHIPPED with closure verdict + canonical-deliverable line, mirroring the v62.0 shape.
    - ROADMAP.md Phase 396 + the v63.0 header marked SHIPPED/complete; STATE.md milestone status flipped to SHIPPED with well-formed frontmatter (hand-edited, not via the mis-mutating handlers).
    - No contract file is modified (git status --porcelain contracts/ empty).
  </acceptance_criteria>
  <done>The MILESTONE_V63_AT_HEAD signal is emitted; the milestone is flipped to SHIPPED across MILESTONES.md / ROADMAP.md / STATE.md; contracts untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the planning + audit deliverables (no contract token in the message)</name>
  <files>.planning/phases/396-terminal/396-CLOSURE.md</files>
  <read_first>
    396-CLOSURE.md (Task 1/2); the commit-guard constraint in the context read_first block (the hook blocks commits while contracts/*.sol is dirty AND trips on the literal contract-dir path token in the commit message; .planning/ is gitignored → force-add).
  </read_first>
  <action>
    Commit the v63.0 terminal deliverables + the milestone flip. First verify no contract source is dirty (git status --porcelain on the contract source must be empty) so the commit-guard hook does not block. Force-add the gitignored planning docs (.planning/phases/396-terminal/*, REQUIREMENTS.md, STATE.md, ROADMAP.md, MILESTONES.md) and stage the audit deliverables (audit/FINDINGS-v63.0.md, AUDIT-V63-REPORT.html, and any .planning/audit-v52/cross-model/396-terminal/ council outputs). Commit with a message that closes the v63.0 audit milestone and references the closure signal MILESTONE_V63_AT_HEAD_<sha> — but contains NO literal contract-dir path token (the commit-guard hook trips on it). Do NOT push (push is a separate USER step). Record the commit SHA in 396-CLOSURE.md.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qiE "v63|MILESTONE" && [ -z "$(git status --porcelain contracts/ 2>/dev/null)" ] && git ls-files --error-unmatch audit/FINDINGS-v63.0.md >/dev/null 2>&1 && echo COMMIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The commit is created with the v63.0 closure deliverables (FINDINGS-v63.0.md, AUDIT-V63-REPORT.html, the flipped MILESTONES/ROADMAP/STATE/REQUIREMENTS, the council outputs, 396-CLOSURE.md).
    - The commit message references the closure signal and contains NO literal contract-dir path token.
    - No contract file is in the commit (audit-only milestone); not pushed.
    - The commit SHA is recorded in 396-CLOSURE.md.
  </acceptance_criteria>
  <done>The v63.0 terminal deliverables + milestone flip are committed (force-added planning, no contract token in the message, not pushed); the commit SHA is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closure → subject | the milestone could be flipped while the subject is no longer byte-frozen |
| commit-guard hook | a contract-dir token in the message or a dirty contract file would block/foul the commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-396-07 | Tampering | milestone flipped over a mutated subject | mitigate | re-confirm git diff a8b702a7 -- contracts/ empty + tree-hash 2934d3d8987a09c5f073549a0cb499f6c5f28620 BEFORE the flip (Task 1) |
| T-396-08 | Repudiation | a requirement checked without re-attestation | mitigate | each of 58 reqs re-verified vs its deliverable + FINDINGS-v63.0.md; un-attestable reqs recorded as gaps, not checked |
| T-396-09 | Denial of service | commit-guard hook trips (dirty contract / token in message) | mitigate | verify contracts clean before commit; force-add gitignored planning; no contract-dir token in the message |
</threat_model>

<verification>
- All 58 requirements re-attested and checked in REQUIREMENTS.md.
- git diff a8b702a7 -- contracts/ empty + tree-hash 2934d3d8987a09c5f073549a0cb499f6c5f28620.
- MILESTONE_V63_AT_HEAD_<sha> emitted; milestone flipped to SHIPPED in MILESTONES/ROADMAP/STATE.
- The deliverables committed (no contract token in the message, not pushed); no contract file modified.
</verification>

<success_criteria>
All 58 requirements are re-attested; the subject is re-confirmed byte-frozen; the MILESTONE_V63_AT_HEAD signal is emitted; the milestone is flipped to SHIPPED; the deliverables are committed (no contract edits, no push).
</success_criteria>

<output>
Create `.planning/phases/396-terminal/396-03-SUMMARY.md` when done.
</output>
