---
phase: 392-entropy-and-econ
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md
  - .planning/phases/392-entropy-and-econ/council/econ.gemini.txt
  - .planning/phases/392-entropy-and-econ/council/econ.codex.txt
  - .planning/phases/392-entropy-and-econ/council/econ.council.json
  - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md
autonomous: true
requirements: [ECON-01, ECON-02, ECON-03, ECON-04, ECON-05, ECON-06]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full ECON slice — reward game-theory: bounded accrual below every hard ceiling (ECON-01), EV-neutrality of each redistribution re-verified IN CODE against the documented PAPER-brief claims (ECON-02), the two genuine EV changes match documented intent (ECON-03), the closed positive-EV money-pump search across recycle/spin/recirc/carry/affiliate compositions (ECON-04), the scarce-asset box WWXRP-spin whale-half-pass channel (ECON-05), and the now-uncapped/halved quest-streak rate-bound + decay-gate (ECON-06)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the design-intent anchor explicit: most rebalances are EV-NEUTRAL REDISTRIBUTIONS and the documented EV changes (EV-multiplier lift floor 90%/ceiling 145%/score-to-ceiling 40,000; recycle-bonus ≥3-whole-ticket gate) are BY-DESIGN — the council VERIFIES the stated claims hold in code, it does NOT re-litigate whether the changes are desirable; the by-design RTP/EV/WWXRP-worthless rulings are excluded so the council does not waste passes on settled intent"
    - "The money-pump search (ECON-04) is charged HARD as a dedicated named break-target: find ANY closed positive-EV loop across the composed surfaces (recycle kicker + presale box-credit + spin recirc + carry + affiliate flip-credit) where realized value-out exceeds value-in across a repeatable cycle"
    - "The two prime ECON leads are charged as dedicated numbered break-targets: FC-392-07 (box WWXRP-spin whale-half-pass acquisition-cost quantification, ECON-05) and FC-392-08 (redemption ETH-spin live ETH-pool RMW + recirc inside the claim path, cross-ref 390/393)"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md"
      provides: "Neutral council prompt for the ECON-01..06 slice + the FC-392-01..15 reward-economics leads (with FC-392-07 whale-pass channel + FC-392-08 redemption ETH-spin CEI as the dedicated prime targets and the money-pump search as a dedicated ECON-04 target)"
      min_lines: 50
    - path: ".planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md"
      provides: "Council-net capture record (ECON slice): raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line"
      min_lines: 20
  key_links:
    - from: "392-01-COUNCIL-PROMPT-ECON.md"
      to: "council.sh --label econ"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label econ"
    - from: "392-01-COUNCIL-NET.md"
      to: "council/econ.council.json"
      via: "manifest available/skipped capture"
      pattern: "econ\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the ECON slice — the reward game-theory of the post-v62 reward
rebalances: bounded accrual below every hard ceiling, EV-neutrality of each documented redistribution
re-verified IN CODE against the PAPER brief, the two genuine EV changes matching documented intent, the
closed positive-EV money-pump search, the box WWXRP-spin whale-half-pass scarce-asset channel, and the
now-uncapped/halved quest-streak rate-bound + decay-gate — against the byte-frozen subject `a8b702a7`.
Author ONE neutral council prompt for the ECON-01..06 slice (plus the FC-392-01..15 reward-economics
leads), fan it to the gemini+codex CLIs via `council.sh`, capture the raw output under the phase
`council/` dir, and record the council-net capture (available/skipped, byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
ECON is the reward game-theory dimension — a closed positive-EV money pump (ECON-04) would be HIGH, and
the scarce-asset whale-pass channel (ECON-05) and bounded-accrual (ECON-01) touch value-bearing
invariants. The council — the PRIMARY finder ([[cross-model-led-audits-over-claude-only]]) — goes on
record FIRST so the Wave-2 Claude-net + adjudication plan (392-03) can fold the council leads in before
any verdict. The ECON sweep's job is to VERIFY the documented EV-neutrality / two-EV-changes claims hold
in code, NOT to re-litigate the documented intent (§5 design-intent anchor).

Output: the COUNCIL-PROMPT-ECON doc, the raw gemini/codex output, the council.json manifest, and
392-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/PAPER-REWARD-CHANGES-BRIEF.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/reward-economics.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "models":[...], "answers":{model:answer}, "available":[...], "skipped":[...] } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed CLI is recorded in skipped[] and -->
<!-- is NEVER fatal. gemini + codex CLIs confirmed at ~/.local/bin. -->

<!-- Council-prompt shape reference (the just-approved 389/390/391 slices to MATCH): -->
<!--   .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md -->
<!--   .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format (FINDING: PROPERTY · reachable CALL SEQUENCE · STATE -->
<!--   VAR + file:line at a8b702a7 · SEVERITY · why protections don't stop it | or VERIFIED SOUND + why). -->

<!-- The 6 ECON reqs (REQUIREMENTS.md ECON): -->
<!--   ECON-01 reward accrual saturates below every hard ceiling (activity-score cap, EV/ROI band, -->
<!--     decimator clamps) — no unbounded grind; -->
<!--   ECON-02 EV-neutrality re-verified IN CODE for each redistribution vs the documented claims -->
<!--     (spins, ticket-roll budget ×11/9, far-future 1.5×/0.875×, variance ranges centered on old EV); -->
<!--   ECON-03 the two genuine EV changes match documented intent (floor 90% / ceiling 145% / -->
<!--     score-to-ceiling 40,000; recycle bonus ≥3-whole-ticket gate); -->
<!--   ECON-04 no closed positive-EV money pump across recycle / spin / recirc / carry / affiliate; -->
<!--   ECON-05 scarce-asset: box WWXRP-spin whale-half-pass (15% of opens, S=9) near-unfarmable -->
<!--     (quantify P(S=9) × boxes-per-pass); -->
<!--   ECON-06 quest-streak (uncapped, halved) rate-bounded + decay-gated; activity-score ceiling -->
<!--     reachable only by intended sustained effort. -->

<!-- Authoritative frozen ECON line-cites (verified at a8b702a7 — supply so the council reads the right -->
<!-- code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   PAPER-REWARD-CHANGES-BRIEF documented claims (the design-intent anchor to VERIFY in code): -->
<!--     §1 EV-multiplier band floor 80→90% / ceiling 135→145% / score-to-ceiling 25,500→40,000; -->
<!--     §2/§2b reward split tickets 40% / DGNRS 15% / WWXRP-spin 15% / BURNIE-flat 15% / BURNIE-spins 10% -->
<!--       / ETH-spin 5%; §3 ticket-roll budget ×11/9 (16,100→19,678 bps); §4 far-future 10→20%; -->
<!--     §5 far/near budget 1.5×/0.875× (= 0.2×1.5 + 0.8×0.875 = 1.0 EV-neutral); §6 variance tiers as -->
<!--     symmetric ranges centered on the old value (overall variance EV 0.786× preserved); §7 recycle -->
<!--     bonus relaxed to ≥3-whole-ticket gate (drain-detection removed). -->
<!--   DegenerusGameStorage.sol: LOOTBOX_EV_ACTIVITY_MAX_BPS = 40_000 + the EV band 9000-14500 @~1537-1548 -->
<!--     (_lootboxEvMultiplierFromScore); ACTIVITY_SCORE_HARD_CAP_BPS = 65_534 @~140 (uint16-1 sentinel); -->
<!--     ACTIVITY_SCORE_MAX_BPS = 30_500 (ROI/WWXRP saturation); _effectiveQuestStreak @2284. -->
<!--   DegenerusGameMintStreakUtils.sol: bonusBps += questStreak * 50 @313-315 (was *100, 100-cap removed); -->
<!--     hard-cap clamp @349-351. -->
<!--   DegenerusGameLootboxModule.sol: _applyEvMultiplierWithCap @474 (per-(player,level) LOOTBOX_EV_ -->
<!--     BENEFIT_CAP = 10 ether usedBenefit cap); _resolveLootboxRoll @~1950 (the roll % 20 split + the -->
<!--     allowEthSpin gate); stale EV-band comment @472-473 (says 8000-13500, band moved to 9000-14500). -->
<!--   DegenerusGameDegeneretteModule.sol: resolveWwxrpSpinFromBox @1292 (S==9 whale-half-pass award -->
<!--     @1323-1330; the per-bracket global flag wwxrpJackpotWhalePassBracketAwarded[bracket]); -->
<!--     resolveBurnieSpinsFromBox @1347; resolveEthSpinFromBox @~1400 (3-tier _distributePayout + recirc -->
<!--     box opened allowEthSpin=false → recursion depth 1); _roiBpsFromScore (ROI saturation). -->
<!--   DegenerusMintModule.sol: recycle kicker @1740-1744 (now only totalClaimableUsed >= priceWei*3, the -->
<!--     spentAllClaimable drain block deleted); presale 25% box-credit @1726. -->
<!--   DegenerusQuests.sol: _questCompleteWithPair @~1700-1760 (per-completion streak credit); -->
<!--     _handleLevelQuestProgress @2063-2068 (level-quest +1 NOT gated by daily primary off-run); -->
<!--     _secondaryLocked @1529; the decay anchor lastActiveDay/lastCompletedDay updated only on slot 0. -->
<!--   GameAfkingModule.sol: recordAfkingSecondary @1714 (subStreakLatch +1 sat 255); _afkingStreak @2257. -->
<!--   DegenerusAffiliate.sol: payAffiliate access GAME-only @414-419; affiliateBonusPointsBest -->
<!--     25-ether early-break @725-726; VAULT-terminating chains + 75/20/5 winner-takes-all via creditFlip. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the ECON (reward game-theory) slice</name>
  <files>.planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md</files>
  <read_first>
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md AND .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md (the just-approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, dedicated numbered prime targets, per-finding output format)
    - .planning/PAPER-REWARD-CHANGES-BRIEF.md (FULL — the design-intent anchor: the documented EV-neutral redistributions and the TWO genuine EV changes; the §1-§7 claims to VERIFY in code; the BURNIE:tickets 0.59:1 ratio reference; the framing rule that only the EV-multiplier lift + recycle relaxation change EV)
    - .planning/v63-surface-map/reward-economics.md (FULL §1-§6 + the bounded-accrual table + the candidate focus areas FA-1..FA-15 + the "highest-attention leads" FA-7/FA-8/FA-11 — FA-11 backing-dynamics half is owned by the BURNIE slice 392-02/392-04, cross-ref only)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (section E rows FC-392-01..15 verbatim — the reward-economics owned leads; note the VERIFY-claim tags on FC-392-03/-05/-06/-09 [documented EV changes → verify the stated invariant, do not re-litigate intent])
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (a money-pump = HIGH; solvency = SPINE; RNG = DOMINANT) + §5 design-intent anchor (the reward rebalances are documented; VERIFY the claims, do not re-litigate the documented changes; respect the by-design RTP/WWXRP-worthless/lootbox-timing rulings)
    - the frozen source for the prime sites via git show a8b702a7:contracts/modules/DegenerusGameLootboxModule.sol (_applyEvMultiplierWithCap @474, _resolveLootboxRoll @~1950, the EV band + stale comment @472-473), contracts/DegenerusGameStorage.sol (the EV band 9000-14500 + LOOTBOX_EV_ACTIVITY_MAX_BPS 40000 @~1537-1548, ACTIVITY_SCORE_HARD_CAP_BPS 65534 @~140, _effectiveQuestStreak @2284), contracts/modules/DegenerusGameMintStreakUtils.sol (questStreak*50 @313-315, hard-cap clamp @349-351), contracts/modules/DegenerusGameDegeneretteModule.sol (resolveWwxrpSpinFromBox @1292/1323-1330, resolveEthSpinFromBox @~1400), contracts/modules/DegenerusMintModule.sol (recycle kicker @1740-1744, presale box-credit @1726), contracts/DegenerusQuests.sol (_handleLevelQuestProgress @2063-2068, _questCompleteWithPair @~1700-1760)
  </read_first>
  <action>
    Write 392-01-COUNCIL-PROMPT-ECON.md, charged NEUTRALLY ("here is what we believe is true about the
    reward economics after the post-v62 rebalances — bounded accrual, EV-neutral redistributions, two
    intended EV changes, no money pump, near-unfarmable whale pass — find where one of these breaks") and
    instructing the council to read the EXACT frozen source at `a8b702a7` via `git show
    a8b702a7:contracts/<File>.sol` (ignore the working tree). Use neutral defensive-engineering vocabulary
    throughout per [[neutral-defensive-audit-vocabulary]] (EV, expected value, accrual ceiling, saturation,
    redistribution, composition, value-out/value-in, scarce-asset supply, rate-bound, decay-gate).

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 so the council knows a CLOSED POSITIVE-EV MONEY
    PUMP is HIGH (value extractable in a repeatable cycle), a SCARCE-ASSET supply break (an extra whale
    half-pass minted beyond the per-bracket cap) is value-bearing, an UNBOUNDED accrual grind is
    value-bearing, and a pure reward-magnitude / desirability complaint about a DOCUMENTED change is NOT a
    finding;
    (b) the DESIGN-INTENT ANCHOR framing so the council does not waste passes re-litigating intent: state
    explicitly that per PAPER-REWARD-CHANGES-BRIEF most rebalances are EV-NEUTRAL REDISTRIBUTIONS (the
    spins stake the value they replace; the ticket budget ×11/9 preserves aggregate ticket value; the
    far/near 1.5×/0.875× weighting nets to 1.0; the variance tiers are symmetric ranges centered on the old
    value with EV preserved) and that ONLY two changes alter EV — the EV-multiplier lift (floor 80→90%,
    ceiling 135→145%, score-to-ceiling 25,500→40,000) and the recycle-bonus relaxation (≥3-whole-ticket
    gate, drain-detection removed) — both BY-DESIGN; the council's job is to VERIFY these documented claims
    hold IN CODE (the arithmetic matches, the bound binds), NOT to argue whether the changes are desirable;
    (c) a KNOWN-BY-DESIGN exclusion list so the council does not re-flag settled intent
    ([[degenerette-wwxrp-rtp-by-design]] Degenerette RTP>100% and the near-worthless WWXRP token are
    by-design — verify the scarce whale-pass SUPPLY and the bounded ACCRUAL, not the RTP desirability;
    [[lootbox-resolution-timing-by-design]] lootbox open/resolve TIMING is not a player edge;
    [[intended-game-mechanics-not-findings]] EV>100% RTP / positive-EV lootbox+coinflip / refund floors /
    charity governance are NOT findings; the documented EV-multiplier lift and recycle relaxation are
    economics to VERIFY, not re-litigate);
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE [the
    repeatable cycle for a money pump; the acquisition cost for the whale pass; the grind for unbounded
    accrual] · STATE VAR + file:line at a8b702a7 · SEVERITY per the threat priority · WHY the existing cap /
    saturation / illiquidity / per-bracket flag does not stop it | or VERIFIED SOUND: the property and the
    specific reason it holds — cite the binding cap, the saturation ceiling, the EV arithmetic, or the
    supply flag — so the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to ECON-01..06, as the claims to BREAK:
    "(ECON-01) every reward consumer saturates BELOW its hard ceiling — the activity-score 65,534 hard cap,
    the EV-multiplier 145% + 10-ETH-per-(player,level) benefit cap, the ROI/WWXRP 30,500-bps saturation,
    the terminal-decimator streak re-clamp to 100 — so no input (the now-uncapped quest streak included)
    pushes any reward past its prior saturation ceiling; find any unbounded grind; (ECON-02) each
    EV-neutral redistribution's arithmetic holds IN CODE against the documented claim (the reward split
    40/15/15/15/10/5, the ticket budget ×11/9 = 19,678 bps, the far/near 0.2×1.5+0.8×0.875=1.0, the
    variance ranges centered on the old per-tier value with EV 0.786× preserved) — find any redistribution
    where the coded value diverges from the claimed EV-neutral target; (ECON-03) the two genuine EV changes
    match documented intent — the EV band IS 9000-14500 bps with score-to-ceiling 40,000, and the recycle
    gate IS ≥3-whole-ticket with the drain-detection removed — find any divergence from the documented
    numbers; (ECON-04) NO closed positive-EV money pump exists across the COMPOSED surfaces; (ECON-05) the
    box WWXRP-spin whale-half-pass channel stays near-unfarmable and the per-bracket supply is one half-pass
    regardless of acquisition route; (ECON-06) the now-uncapped/halved quest streak is rate-bounded (≤3/day)
    and decay-gated (off-run anchor = the daily primary; miss a day → streak reads 0) so the activity-score
    ceiling is reachable only by intended sustained effort."

    Name the concrete break-targets. Charge the money-pump search and the two prime leads HARD as dedicated
    numbered items:

    1. ECON-04 (HIGH-if-real — the DEDICATED money-pump search): hunt for ANY closed positive-EV loop where
       realized value-out exceeds value-in across a REPEATABLE cycle, composing the surfaces that the
       individual maps each rated EV-neutral in isolation: the recycle kicker (10% illiquid BURNIE
       flip-credit on ≥3-ticket recycled spend, MintModule:1740-1744) STACKED with the presale 25%
       box-credit on the SAME recycled spend (MintModule:1726); the spin recirc (ETH-spin → 1 recirc box,
       DegeneretteModule:~1400) feeding back into a fresh box open; the auto-rebuy carry; the affiliate
       flip-credit (75/20/5 winner-takes-all via creditFlip). Charge the council to either CONSTRUCT a
       concrete repeatable cycle whose value-out > value-in (accounting for BURNIE flip-credit illiquidity +
       flip-survival, the sub-100% direct lootbox EV, the 10-ETH benefit cap, and the requirement that
       claimable must first be WON), or to state VERIFIED SOUND with the concrete reason no composition
       closes a positive loop (each leg's output is illiquid/sub-100%/capped; the cycle's value-in is real
       won claimable that exceeds the illiquid value-out).

    2. ECON-05 / FC-392-07 (MED, §6 prime — the whale-half-pass acquisition-cost target): the box
       WWXRP-spin (15% of opens) can hit S==9 and award the bracket's one whale half-pass
       (resolveWwxrpSpinFromBox @1323-1330) — a NEW acquisition channel vs only deliberate WWXRP bets. The
       per-bracket flag wwxrpJackpotWhalePassBracketAwarded[bracket] is GLOBAL and SHARED so total SUPPLY
       stays one half-pass per 10-level bracket. Charge the council to QUANTIFY the new acquisition cost:
       P(S==9) for a 1-WWXRP-staked box spin × the expected boxes-per-pass, and confirm (i) the cost still
       exceeds the by-design "near-unfarmable" bar, AND (ii) no path mints a half-pass BEYOND the
       per-bracket cap (e.g. a race between the box route and a deliberate-bet route writing the flag, or a
       recirc box re-rolling the WWXRP-spin within one open). A real SUPPLY break (>1 half-pass per bracket)
       is value-bearing; a mere cost-curve change with supply intact is the documented channel change —
       confirm which.

    3. ECON-04/SOLV cross-ref / FC-392-08 (MED, cross-ref 390/393 — the redemption ETH-spin CEI surface):
       the redemption claim path (allowEthSpin=true) reaches the ETH-spin whose _distributePayout does a
       live ETH-pool read-modify-write + a recirc box INSIDE the redemption claim (the V62-03/council CEI
       region). Charge the council to trace whether (i) the ETH-spin's pool writes + the dust-forfeit leg +
       the pendingRedemptionEthValue release reconcile EXACTLY (no double-count, no leak), and (ii) the
       recirc box's _applyEvMultiplierWithCap benefit-cap RMW can be RACED across redemption chunks to
       re-earn the cap. NOTE this is the ECON-economics half — the solvency-CEI / reentrancy half is owned
       by 390 (SOLV) and the permissionless-composition half by 393 (ACCESS); ask the council to surface any
       ECON-level value-extraction (cap re-earn / EV uplift beyond the documented bound) and cross-ref the
       solvency half to 390.

    Then name the remaining owned reward-economics leads as numbered break-targets at their map severity:

    4. FC-392-01 / FC-392-02 (LOW, ECON-06 streak gaming): the level-quest +1 streak is NOT gated by the
       daily primary off-run (_handleLevelQuestProgress @2063-2068 — credits qs.streak+=1 with no
       primary-completed check; the decay anchor is NOT updated by it); and the afking-secondary
       double-channel (while afking both recordAfkingSecondary and the funded delivered day count toward
       streak). Charge the council to find (i) any path where the off-day level-quest +1 is NOT zeroed by
       the next-day decay when the primary is skipped (a persistent streak bump bypassing the primary gate),
       and (ii) any toggling of afking on/off across a day boundary that harvests BOTH the funded-day streak
       AND a manual +1 for the same primary. Confirm the decay + the rate-bound (≤3/day, level-quest once
       per level) keep the ceiling reachable only by intended sustained effort, OR surface a finding.

    5. FC-392-05 / FC-392-06 / FC-392-09 (INFO-LOW, VERIFY-claim — the EV-cap bound under the composed
       paths): the per-(player,level) 10-ETH usedBenefit cap (_applyEvMultiplierWithCap @474) is now reached
       on a smaller staked notional with the 145% ceiling. Charge the council to confirm the usedBenefit cap
       CANNOT be reset within a level to re-earn the uplift across the paths that all funnel into
       _applyEvMultiplierWithCap (redemption + direct-open + Degenerette-recirc); confirm the recycle kicker
       stays EV-neutral-or-negative (illiquid flip-credit + flip-survival) and does NOT form a positive loop
       when stacked with the presale box-credit (this is the local view of the ECON-04 composition); confirm
       the ETH-spin "EV-equal to the tickets it replaces" claim holds given it runs through the >100%-RTP
       Degenerette payout + recirc — i.e. the aggregate box EV uplift is bounded by the 10-ETH cap. These
       are VERIFY-claim targets (the documented EV changes) — verify the bound binds, do not re-litigate the
       documented lift.

    6. FC-392-03 / FC-392-04 / FC-392-10 (INFO, VERIFY-claim + comment-staleness): the faster
       decimator-max ramp (20× boost in ~33 active days vs ~100 — VERIFY the documented intent matches the
       coded ramp); the stale EV-band comment (LootboxModule:472-473 says 8000-13500 after the band moved to
       9000-14500 — a comment-only economic-spec staleness to note, not a logic finding); the BoxSpin betId
       sentinel collision (BOX_BETID_SENTINEL=1<<63 — confirm a real bet nonce can never reach bit 63 over
       the game lifetime so the event-decode stays correct). Charge the council to confirm or surface each.

    7. FC-392-14 / FC-392-15 (LOW-INFO, affiliate composition): the VAULT-terminating referral chains +
       75/20/5 winner-takes-all — confirm a self-referral / circular-code attempt cannot route the
       upline1/upline2 (20/5%) slices back to the sender, and the no-referrer 50/50 VAULT/DGNRS path cannot
       be steered by choosing a code that flips noReferrer to capture the affiliate 75% slice for an
       attacker-controlled address; re-examine the carried v62 affiliate-score asymmetry against the
       now-GAME-only payAffiliate access (@414-419) + the affiliateBonusPointsBest 25-ether early-break
       (@725-726) to confirm the asymmetry did not change. These feed the ECON-04 composition (the affiliate
       flip-credit leg) — confirm no positive loop and no value-redirect.

    Also charge an explicit ECON-01 BOUNDED-ACCRUAL sweep over the reward-economics map's bounded-accrual
    table: for EACH reward surface (activity score, lootbox EV multiplier, Degenerette/WWXRP ROI,
    terminal-decimator boost, BURNIE survival flip, lootbox spins, recycle bonus, whale half-pass via box
    spin, BURNIE seed emission) ask the council to confirm the named hard bound BINDS (the consumer
    saturates below the cap) and to find ANY surface where the now-uncapped quest-streak input (uint16,
    rate-bounded ≤3/day) can push a downstream reward past its prior saturation ceiling — i.e. confirm every
    consumer saturates BELOW the 65,534 hard cap (EV at 40,000; ROI/WWXRP at 30,500; decimator re-clamps
    streak to 100), so the uncapped streak cannot widen any reward ceiling.

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 392-03. No fenced
    solidity blocks beyond the minimal identifier/selector/line citations the council needs to locate the
    code.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md && grep -q "a8b702a7" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md && for id in ECON-01 ECON-02 ECON-03 ECON-04 ECON-05 ECON-06 FC-392-01 FC-392-02 FC-392-03 FC-392-04 FC-392-05 FC-392-06 FC-392-07 FC-392-08 FC-392-09 FC-392-10 FC-392-14 FC-392-15; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "money.?pump|positive-EV|value-out" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md && grep -Eqi "EV-neutral|redistribution|whale.?half.?pass|whale halfpass" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md && grep -Eqi "saturat|bounded|hard cap" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md</automated>
  </verify>
  <acceptance_criteria>
    392-01-COUNCIL-PROMPT-ECON.md exists and is non-empty; instructs reading frozen source at `a8b702a7`
    via git show; states the design-intent anchor (EV-neutral redistributions + the two documented EV
    changes are BY-DESIGN — VERIFY in code, do not re-litigate); names ALL of ECON-01..06 + FC-392-01..10 +
    FC-392-14/-15; charges the ECON-04 money-pump search as a DEDICATED numbered break-target (closed
    positive-EV cycle across composed surfaces); charges FC-392-07 (whale-pass acquisition cost / supply
    cap) and FC-392-08 (redemption ETH-spin CEI/cap-RMW, cross-ref 390/393) as dedicated prime targets;
    carries the ECON-01 bounded-accrual sweep (every consumer saturates below the hard cap); carries the
    KNOWN-BY-DESIGN exclusion list; uses neutral vocabulary; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral ECON council prompt is authored against the frozen subject, covering all 6 ECON reqs + the
    15 reward-economics owned leads (FC-392-01..15, with FC-392-11/-12/-13 backing-dynamics leads cross-ref
    only to the BURNIE slice), with the money-pump search + the two prime leads charged hard and the
    design-intent anchor + bounded-accrual doctrine encoded, ready to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the ECON council fan-out and record the council-net capture</name>
  <files>.planning/phases/392-entropy-and-econ/council/econ.gemini.txt, .planning/phases/392-entropy-and-econ/council/econ.codex.txt, .planning/phases/392-entropy-and-econ/council/econ.council.json, .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md (Task 1 output)
    - .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md (the just-approved COUNCIL-NET capture record shape to match)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/392-entropy-and-econ/council --label econ
    .planning/phases/392-entropy-and-econ/392-01-COUNCIL-PROMPT-ECON.md`. council.sh fans gemini+codex in
    parallel internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations is
    needed beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped CLI is
    recorded in the council.json skipped[] and is NOT fatal; do not treat a skip as a failure.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify). Do NOT
    run `hardhat compile --force` (regenerates ContractAddresses source).

    Write 392-01-COUNCIL-NET.md recording: the econ.council.json manifest's available[]/skipped[], the raw
    output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 392-03), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for ECON" line so 392-03
    can confirm both-nets-on-record before any verdict. If a CLI is in skipped[], record WHICH and note the
    slice still has the other model on record (a single available model satisfies "council on record" with
    the skip documented; both-unavailable would require a re-run and must be surfaced, not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/392-entropy-and-econ/council/econ.council.json && test -s .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `econ.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable CLI
    is in the council.json skipped[]; 392-01-COUNCIL-NET.md records the available/skipped + raw output paths
    + the byte-freeze attestation + an explicit "NET 1 ON RECORD" line; `git diff a8b702a7 -- contracts/` is
    empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the ECON slice with raw output captured under the phase
    council/ dir, the subject proven byte-frozen, and the capture documented for the Wave-2 adjudication to
    fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable CLI silently dropping the only net (handled by skipped[]) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir |
| T-392-02 | Information disclosure / false-green | the ECON slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 392-01-COUNCIL-NET.md; both-unavailable is surfaced for re-run, never passed silently |
| T-392-03 | Repudiation / scope-drift | the council re-litigating documented intent (the EV changes) instead of verifying the claims, wasting the pass | mitigate | the prompt carries the design-intent anchor + KNOWN-BY-DESIGN list so the council verifies claims-in-code, not desirability |
| T-392-SC | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers ECON-01..06 + FC-392-01..10 +
  FC-392-14/-15, with the money-pump search + FC-392-07 whale-pass + FC-392-08 redemption ETH-spin charged
  hard and the design-intent anchor + bounded-accrual doctrine encoded.
- `council.sh` ran for `--label econ`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[].
- 392-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full ECON (reward game-theory) slice, with raw
gemini/codex output captured under the phase council/ dir and the capture documented, so the Wave-2
Claude-net + adjudication plan can fold the council leads in before issuing any per-item verdict. The
subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/392-entropy-and-econ/392-01-SUMMARY.md` when done
</output>
</content>
