---
phase: 392-entropy-and-econ
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md
  - .planning/phases/392-entropy-and-econ/council/burnie.gemini.txt
  - .planning/phases/392-entropy-and-econ/council/burnie.codex.txt
  - .planning/phases/392-entropy-and-econ/council/burnie.council.json
  - .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md
autonomous: true
requirements: [BURNIE-01, BURNIE-02, BURNIE-03, BURNIE-04, BURNIE-05, BURNIE-06]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full BURNIE/coinflip-rework slice — survive-before-mint across every BURNIE source (BURNIE-01), 200k/day×20d emission conservation vs the removed 2M+2M lumps (BURNIE-02), the sdgnrsAutoRebuyArmed latch monotonicity / no double-claim (BURNIE-03), the auto-rebuy carry reflected in the sDGNRS redemption backing (BURNIE-04 / FC-392-16, the TOP MED lead), the VAULT seed-stake 30-day window-aging forfeiture (BURNIE-05 / FC-392-17, the TOP MED lead, 'most likely to need a contract change'), and the packed-lane + 8-bit 3-state round-trip losslessness with BURNIE off the ETH/claimablePool path (BURNIE-06)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the two prime backing leads (FC-392-16 carry-excluded-from-redemption-backing, FC-392-17 VAULT seed window-aging) charged HARD as dedicated named break-targets demanding a CONFIRM/REFUTE/BY-DESIGN with the backing accounting traced exhaustively — not a hand-wave"
    - "The backing-solvency emphasis is encoded: BURNIE is off the ETH/claimablePool spine by design (BURNIE-06), so the concern is the sDGNRS REDEMPTION-BACKING completeness/conservation (does the carry/seed accounting under-credit or strand vs the old fixed 2M reserve), NOT an ETH solvency break — but a confirmed backing under-credit or a lost-emission window is still a value-bearing finding"
    - "The FC-392-11 backing/EV-dynamics half (cross-referenced from 391, where the RNG-lock half was attested) is folded into this slice: the stochastic auto-rebuy backing dynamics (a loss zeroes the pending stake) modeled vs outstanding redemption obligations + the claimCoinflipCarry settle/withdraw double-count check (FC-392-13)"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md"
      provides: "Neutral council prompt for the BURNIE-01..06 slice + the FC-392-16..20 coinflip-burnie leads + the FC-392-11/-12/-13 backing-dynamics leads (cross-ref from 391) — with FC-392-16 carry-backing + FC-392-17 VAULT window as the dedicated prime targets"
      min_lines: 50
    - path: ".planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md"
      provides: "Council-net capture record (BURNIE slice): raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line"
      min_lines: 20
  key_links:
    - from: "392-02-COUNCIL-PROMPT-BURNIE.md"
      to: "council.sh --label burnie"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label burnie"
    - from: "392-02-COUNCIL-NET.md"
      to: "council/burnie.council.json"
      via: "manifest available/skipped capture"
      pattern: "burnie\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the BURNIE/coinflip-rework slice — the survive-before-mint
invariant across every BURNIE source, the 200k/day×20d seed-emission conservation vs the removed 2M+2M
lumps, the perpetual auto-rebuy latch monotonicity, the auto-rebuy CARRY reflected (or not) in the sDGNRS
redemption backing (the TOP MED lead FC-392-16), the VAULT seed-stake 30-day window-aging forfeiture (the
TOP MED lead FC-392-17, the one the surface map flagged "most likely to need a contract change"), and the
packed-lane / 8-bit 3-state round-trip losslessness with BURNIE off the ETH/claimablePool spine — against
the byte-frozen subject `a8b702a7`. Author ONE neutral council prompt for the BURNIE-01..06 slice (plus
the FC-392-16..20 coinflip-burnie leads and the FC-392-11/-12/-13 backing-dynamics leads cross-referenced
from 391), fan it to the gemini+codex CLIs via `council.sh`, capture the raw output under the phase
`council/` dir, and record the council-net capture (available/skipped, byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record. The
two prime BURNIE leads are solvency-ADJACENT backing concerns (the spine concern of this phase per the
threat weighting): a carry/seed accounting gap that progressively under-credits redeemers or silently
forfeits ~half the seeded initial emission. The council — the PRIMARY finder
([[cross-model-led-audits-over-claude-only]]) — goes on record FIRST so the Wave-2 Claude-net +
adjudication plan (392-04) can fold the council leads in before any verdict. If either FC-392-16 or
FC-392-17 (or any item) CONFIRMS as MED+ after the skeptic gate, it is DOCUMENTED + ROUTED to a gated fix.

Output: the COUNCIL-PROMPT-BURNIE doc, the raw gemini/codex output, the council.json manifest, and
392-02-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/PAPER-REWARD-CHANGES-BRIEF.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/coinflip-burnie.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "models":[...], "answers":{model:answer}, "available":[...], "skipped":[...] } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed CLI is recorded in skipped[] and -->
<!-- is NEVER fatal. gemini + codex CLIs confirmed at ~/.local/bin. -->

<!-- Council-prompt shape reference (the just-approved 390/391 slices to MATCH): -->
<!--   .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md -->
<!--   .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format. -->

<!-- The 6 BURNIE reqs (REQUIREMENTS.md BURNIE): -->
<!--   BURNIE-01 survive-a-coinflip-before-mint across every BURNIE source (seed stakes, spin -->
<!--     double-or-nothing, normal mint); -->
<!--   BURNIE-02 total emission conserved vs the removed 2M+2M lumps (200k/day×20d sums correctly; -->
<!--     no over/under-emission); -->
<!--   BURNIE-03 the auto-rebuy latch sdgnrsAutoRebuyArmed is monotonic — cannot be entered/exited to -->
<!--     double-claim or strand funds; -->
<!--   BURNIE-04 claimCoinflipCarry accounting correct + the redemption BURNIE backing complete — the -->
<!--     auto-rebuy carry reflected in the sDGNRS redemption backing (the FA-1 lead / FC-392-16); -->
<!--   BURNIE-05 the VAULT seed-stakes (days 1-20) window-aging forfeiture is intended or fixed (the FA-2 -->
<!--     lead / FC-392-17; ~half the seeded initial emission at risk); -->
<!--   BURNIE-06 the packed stake lanes + the 8-bit 3-state day-result round-trip losslessly; BURNIE stays -->
<!--     off the ETH/claimablePool solvency path. -->

<!-- Authoritative frozen BURNIE line-cites (verified at a8b702a7 — supply so the council reads the right -->
<!-- code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   BurnieCoin.sol: _supply starts fully zero (no constructor mint) @174; _transfer inline shortfall -->
<!--     top-up (skipped while rngLocked); onlyBurnieCoin narrowed to COIN only. -->
<!--   BurnieCoinflip.sol: constructor seed stakes SEED_FLIP_DAILY=200_000 ether days 1-20 to VAULT+SDGNRS -->
<!--     @140-143/189-197 (direct _setFlipStake writes, off leaderboard/bounty); SEED_FLIP_DAYS=20; -->
<!--     sdgnrsAutoRebuyArmed latch @174 (slot 4 w/ bountyOwedTo@0 + flipsClaimableDay@20 + latch@23); -->
<!--     processCoinflipPayouts @870-893 (not-armed: _claimCoinflipsAmount(SDGNRS,max,true) mints to wallet; -->
<!--     epoch>=20: arm + autoRebuyEnabled + autoRebuyStartDay=lastClaim; armed: _claimCoinflipsInternal -->
<!--     (SDGNRS,false) → carry, 0 take-profit, mints nothing); autoRebuyCarry uint128 @154; -->
<!--     claimCoinflipCarry @754-778 (settle resolved days first, RNG-lock @759, mint up to amount from -->
<!--     settled carry @770); COIN_CLAIM_DAYS=365 / COIN_CLAIM_FIRST_DAYS=30 / AUTO_REBUY_OFF_CLAIM_DAYS_MAX -->
<!--     =1460 @136-138; claim loop start=minClaimableDay=latest-window silently skips below-window days -->
<!--     @420-457 (uint16 shallow / uint32 deep); coinflipDayResultPacked 8-bit 3-state @1092-1106 -->
<!--     (0=unresolved,1=loss,50..156=win); coinflipStakePacked 2 days/slot 128-bit wei lanes @1072-1085; -->
<!--     _claimCoinflipsInternal min-window skip @431-440; redeemBurnieShare consume waterfall @940-975 -->
<!--     (covers base from held balance + claimableStored, never reads autoRebuyCarry); -->
<!--     previewClaimCoinflips @971-975 = _viewClaimableCoin + claimableStored; setCoinflipAutoRebuy -->
<!--     fromGame branch @662-668 (skips approval for non-zero player, grep-clean unreachable). -->
<!--   StakedDegenerusStonk.sol: burnieOwed = (burnieBal + previewClaimCoinflips(sDGNRS))*amount/supply -->
<!--     @1029-1031; sDGNRS does NOT call claimCoinflipCarry (no carry liquidation path); auto-rebuy NOT -->
<!--     enabled in sDGNRS constructor (arms once final seeded day settles). -->
<!--   DegenerusVault.sol: VAULT seed claim path coinClaimCoinflips → claimCoinflips(VAULT,…) @630-647; -->
<!--     VAULT not auto-rebuy-armed, not auto-claimed. -->
<!--   DegenerusGameDegeneretteModule.sol: survival flip hash2(rngWord,betId)&1 @763-780 (acc.burnieMint += -->
<!--     totalPayout / -= totalPayout; payout added in _distributePayout @906; flushed mintForGame @447); -->
<!--     resolveBurnieSpinsFromBox @1347-1394 (3 spins under ONE survival flip hash2(seed,BOX_SURVIVAL_TAG), -->
<!--     mint-only, no pool/ETH/recirc; packed BoxSpin 3×72-bit reels + count@216 + survived@224). -->
<!--   GameAfkingModule.sol: _settlePendingBurnie @1085-1096 (zero-then-creditFlip, CEI; BURNIE flip credit -->
<!--     not ETH). -->
<!--   Removed: claimCoinflipsForRedemption (sDGNRS-only RNG-skip claim) DELETED; CoinflipDayResult struct. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110; BurnieEmissionSeeds invariant -->
<!--     passes 5/5 (the seed-emission conservation anchor); CoinflipCarryClaim.t.sol covers -->
<!--     partial/cap/loss-zeroing/compounding. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the BURNIE/coinflip-rework slice</name>
  <files>.planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md</files>
  <read_first>
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md AND .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md (the just-approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, dedicated numbered prime targets, per-finding output format)
    - .planning/v63-surface-map/coinflip-burnie.md (FULL — §1 what changed [emission re-home, auto-rebuy latch, claimCoinflipCarry, claim-window widening, the two packing lanes, survival flip, afking settlement, shortfall relocation, removed surface]; §2 the correctness review of the focus items; §3 the candidate focus areas FA-1..FA-5 — the prime leads FA-1 carry-backing-strand + FA-2 VAULT-window-aging; §4 files reviewed)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (section F rows FC-392-16..20 verbatim + FC-392-11 [stochastic sDGNRS auto-rebuy backing — the backing/EV-dynamics half owned here, the RNG-lock half attested at 391], FC-392-12 [seed-stake leaderboard/bounty exclusion], FC-392-13 [claimCoinflipCarry settle-ordering double-count])
    - .planning/PAPER-REWARD-CHANGES-BRIEF.md (§2b the BURNIE spins ×3 under one survival flip; §5 the survival-flip EV-neutrality; the BURNIE:tickets 0.59:1 protocol-peg-vs-illiquid framing — context for "BURNIE rated worthless except the whale pass" that bounds the real impact of a backing strand)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (solvency-adjacent backing = the spine concern here) + §5 design-intent anchor (survive-before-mint is the defining intent; the stochastic backing trade is intended variance — verify conservation/completeness, not the variance trade desirability)
    - the frozen source for the prime sites via git show a8b702a7:contracts/BurnieCoinflip.sol (the constructor seed stakes @140-143/189-197, processCoinflipPayouts @870-893, claimCoinflipCarry @754-778, the claim-window min-skip @420-457/431-440, redeemBurnieShare waterfall @940-975, previewClaimCoinflips @971-975, the packing lanes @1072-1106, sdgnrsAutoRebuyArmed @174), contracts/StakedDegenerusStonk.sol (burnieOwed @1029-1031, the no-carry-liquidation path), contracts/DegenerusVault.sol (coinClaimCoinflips @630-647), contracts/modules/DegenerusGameDegeneretteModule.sol (survival flip @763-780, resolveBurnieSpinsFromBox @1347-1394), contracts/BurnieCoin.sol (zero-start emission @174), contracts/modules/GameAfkingModule.sol (_settlePendingBurnie @1085-1096)
  </read_first>
  <action>
    Write 392-02-COUNCIL-PROMPT-BURNIE.md, charged NEUTRALLY ("here is what we believe is true about the
    BURNIE coinflip-seeded emission rework — every BURNIE survives a coinflip before minting, the
    200k/day×20d seed conserves the removed 2M+2M, the auto-rebuy latch is monotonic, the redemption
    backing is complete, the VAULT seed is recoverable, and the packed lanes round-trip losslessly with
    BURNIE off the ETH spine — find where one of these breaks") and instructing the council to read the
    EXACT frozen source at `a8b702a7` via `git show a8b702a7:contracts/<File>.sol` (ignore the working
    tree). Use neutral defensive-engineering vocabulary throughout per
    [[neutral-defensive-audit-vocabulary]] (emission, seed stake, survive-before-mint, conservation,
    latch, monotonic, backing, under-credit, strand, window-aging, forfeiture, lane, round-trip, lossless).

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 so the council knows BURNIE is OFF the ETH/
    claimablePool spine by design (BURNIE-06) — so the concern is NOT an ETH solvency break but the sDGNRS
    REDEMPTION-BACKING completeness/conservation (a carry/seed accounting gap that progressively
    under-credits redeemers or silently forfeits seeded emission is the value-bearing concern here, the
    spine concern of this phase); an emission over-mint (BURNIE-02) or a latch double-claim (BURNIE-03)
    would be value-bearing; a survive-before-mint violation (BURNIE-01) breaks the defining invariant;
    (b) the DESIGN-INTENT ANCHOR framing so the council does not waste passes: state that "all BURNIE
    survives a coinflip before minting" is the defining intent of the rework, the stochastic backing trade
    (a bad-luck seed window leaves sDGNRS with less BURNIE backing than the old fixed 2M; the perpetual
    auto-rebuy compounds the position) is an INTENDED VARIANCE TRADE — the council VERIFIES conservation +
    completeness + monotonicity + round-trip-losslessness, NOT whether the variance trade is desirable; and
    that BURNIE is rated "worthless except the near-unfarmable whale pass" ([[degenerette-wwxrp-rtp-by-
    design]]), which BOUNDS the real impact of a backing under-credit (it is an under-credit/strand class,
    not an ETH insolvency) — but a confirmed under-credit or a lost-emission window is still a finding to
    surface;
    (c) a KNOWN-BY-DESIGN exclusion list ([[intended-game-mechanics-not-findings]] the EV>100% RTP /
    positive-EV coinflip / refund floors are NOT findings; [[lootbox-resolution-timing-by-design]];
    [[open-e-operator-approval-trust-boundary]] operator-approval IS the trust boundary — do not model a
    "tricked into approving" actor; [[genesis-admin-self-break-nonfinding]] an admin/protocol-address
    breaking its OWN position at genesis with no engaged community is a non-finding — but the VAULT/SDGNRS
    seed window-aging is about a PROTOCOL-ADDRESS losing seeded EMISSION that backs real redeemers, which is
    distinct from a genesis self-break, so DO charge it);
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE [the
    multi-day settle/claim sequence; the loss-sequence backing model; the window-aging skip] · STATE VAR +
    file:line at a8b702a7 · SEVERITY per the threat priority · WHY the existing conservation / latch / CEI /
    waterfall does not stop it | or VERIFIED SOUND / INTENDED: the property and the specific reason it holds
    — cite the conservation identity, the monotone latch, the round-trip bound, or the design intent — so
    the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to BURNIE-01..06, as the claims to BREAK:
    "(BURNIE-01) every BURNIE source mints only AFTER surviving a coinflip — the seed stakes (only mint if
    that day's flip survives), the per-bet survival double-or-nothing, the box BURNIE spins ×3 under one
    survival flip, and the normal mint — find any BURNIE that mints without a survived flip; (BURNIE-02)
    total seeded emission conserves the removed 2M direct + 2M virtual lumps — 200k×20×2 = 8M of STAKE,
    ~half expected to win through flip survival, replacing the prior flat grant — find any over- or
    under-emission vs the conservation identity (totalSupply + vaultAllowance = supplyIncUncirculated);
    (BURNIE-03) the sdgnrsAutoRebuyArmed latch is monotone (set once at epoch>=20, never cleared) and
    cannot be entered/exited to double-claim (seed-window mints via _claimCoinflipsAmount(...,true), armed
    never mints) or strand funds; (BURNIE-04) the auto-rebuy CARRY is reflected in the sDGNRS redemption
    backing — claimCoinflipCarry accounting is correct and previewClaimCoinflips/redeemBurnieShare account
    for carry-resident BURNIE; (BURNIE-05) the VAULT seed-stakes (days 1-20) are recoverable within the
    claim window — they do not silently age out and forfeit ~half the seeded initial emission; (BURNIE-06)
    the packed stake lanes (2 days/slot, 128-bit wei) + the 8-bit 3-state day-result lane (0/1/50..156)
    round-trip losslessly and BURNIE stays off the ETH/claimablePool solvency path."

    Name the concrete break-targets. Charge the two prime backing leads HARD as the lead dedicated numbered
    items:

    1. BURNIE-04 / FC-392-16 (MED, the §6 lead #1 — the TOP carry-backing target): does the auto-rebuy
       CARRY get reflected in the sDGNRS redemption backing? burnieOwed = (burnieBal +
       previewClaimCoinflips(sDGNRS)) * amount / supply (StakedStonk:1029-1031); previewClaimCoinflips
       (BurnieCoinflip:971-975) = _viewClaimableCoin + claimableStored; redeemBurnieShare's consume
       waterfall (940-975) covers base from sDGNRS's held balance + claimableStored. After day 20 sDGNRS is
       on perpetual auto-rebuy, so its entire ongoing BURNIE accrual rolls into autoRebuyCarry — which
       NEITHER previewClaimCoinflips NOR the consume waterfall ever reads, AND there is no liquidation path
       (nothing calls claimCoinflipCarry(sDGNRS,…); sDGNRS holds no such code). Charge the council to TRACE
       EXHAUSTIVELY and CONFIRM or REFUTE: (i) is the carry-resident BURNIE invisible to the only consumer
       of sDGNRS's BURNIE backing, so redeemers are progressively under-credited? (ii) is this a CONSERVATIVE
       under-credit/strand (base <= burnieBal + claimableBurnie, waterfall never reverts → no over-credit,
       no insolvency) or can it ever over-credit / strand value irrecoverably? (iii) in steady state does
       _viewClaimableCoin(sDGNRS) return 0 between resolutions (cursor==latest after each auto-claim) so
       burnieOwed reflects only the HELD balance? Settle whether this matches design intent (an intended
       loss of the BURNIE-share economics the old 2M reserve provided, BURNIE being "worthless except the
       whale pass") or is an accidental backing gap. A hand-wave is NOT acceptable — require the backing
       accounting traced to the carry.

    2. BURNIE-05 / FC-392-17 (MED, the §6 lead #2 — the TOP VAULT-window target, "most likely to need a
       contract change"): the constructor seeds the VAULT identically (days 1-20), but ONLY sDGNRS is
       auto-claimed by processCoinflipPayouts. The VAULT must claim via DegenerusVault.coinClaimCoinflips →
       claimCoinflips(VAULT,…). For a player with lastClaim==0 the window is COIN_CLAIM_FIRST_DAYS=30
       (BurnieCoinflip:423), and _claimCoinflipsInternal sets start=minClaimableDay=latest-30 when
       start<minClaimableDay, SILENTLY SKIPPING all days below it (431-440). If the VAULT does not claim
       until flipsClaimableDay>=51, then minClaimableDay=21>20 and EVERY VAULT seed day (1-20) is skipped —
       the VAULT's entire ~half-of-4M expected seed emission never mints. Charge the council to DETERMINE:
       (i) is the VAULT expected/guaranteed to claim within 30 days, or to be on auto-rebuy at deploy? (ii)
       is there any auto-claim or auto-rebuy safety net for the VAULT seed (there is for sDGNRS, none for
       the VAULT)? (iii) under the realistic deploy/operations timeline, is the VAULT seed at risk of
       silent forfeiture? Settle: INTENDED forfeiture (BY-DESIGN, e.g. the VAULT is operationally claimed or
       armed) or a real DEFECT (CONFIRMED → routed fix). This is the one the surface map flagged as the
       most likely real contract change — give it rigorous dedicated treatment.

    Then name the remaining owned coinflip-burnie leads + the cross-ref backing-dynamics leads as numbered
    break-targets at their map severity:

    3. BURNIE-02 (emission conservation): charge the council to confirm the constructor seeds days 1-20
       inclusive to BOTH addresses (200k×20×2 = 8M stake), that processCoinflipPayouts resolves exactly one
       epoch per advanceGame call with sDGNRS auto-claimed every resolved day (cursor lastClaim tracks
       flipsClaimableDay 1:1 through the seed window), and that the seed→arm handoff at epoch>=20 has no
       off-by-one (arming fires AFTER day-20's win mints; autoRebuyStartDay=lastClaim=20; days 21+ roll to
       carry). Anchor on the green BurnieEmissionSeeds invariant (5/5). Find any over/under-emission vs the
       conservation identity, or confirm SOUND.

    4. BURNIE-03 (latch monotonicity): charge the council to confirm sdgnrsAutoRebuyArmed is set exactly
       once (epoch>=20), never cleared, gated behind the else of if(sdgnrsAutoRebuyArmed), with no disarm /
       re-run path; the seed-window branch mints via _claimCoinflipsAmount(...,true) and the armed branch
       never mints (no double-mint); setCoinflipAutoRebuy is NEVER called for sDGNRS post-genesis so the
       enabled flag can't be toggled off to extract carry. FC-392-18 folds in: the setCoinflipAutoRebuy
       fromGame branch (662-668) skips operator-approval for a non-zero player but no in-protocol GAME
       module calls it (grep clean) → presently unreachable + matches baseline; confirm it stays
       unreachable (latent permissive branch, not a regression). Find any latch re-entry / double-claim /
       strand, or confirm SOUND.

    5. BURNIE-01 (survive-before-mint completeness): charge the council to enumerate EVERY BURNIE mint
       source and confirm each gates on a survived coinflip — the seed stakes (mint only if the day's flip
       survives), the per-bet survival double-or-nothing (DegeneretteModule:763-780, acc.burnieMint +=/-=
       nets to zero per losing bet, flushed once @447), the box BURNIE spins ×3 under one survival flip
       (resolveBurnieSpinsFromBox:1347-1394, mint-only), the normal mint, and the afking BURNIE settlement
       (_settlePendingBurnie:1085-1096, a flip credit not a direct mint). FC-392-19 folds in: the survival
       flip seed reuses the box seed hash hash2(rngWord,betId) on the bet path — for BURNIE bets
       betLootboxShare==0 so no box opens (no observable correlation); confirm no path mixes a BURNIE
       survival outcome with a box draw a player could bias via their own sequential betIds. Find any BURNIE
       that mints without a survived flip, or confirm SOUND.

    6. BURNIE-06 (packed-lane round-trip + off-spine): charge the council to confirm the stake lane (wei in
       128-bit halves, bounded by uint128 supply, masked read/write preserves the sibling day) and the
       8-bit 3-state day-result lane (0=unresolved, 1=loss, 50..156=win; b>=50 separates wins; NO win lands
       in [2,49]) round-trip losslessly; and that BURNIE never credits claimableWinnings or the prize pools
       (mintForGame/burnForCoinflip/burnCoin only; the sDGNRS redemption BURNIE leg burn+consume nets new
       BURNIE = 0). Find any lossy lane / sibling-corruption / BURNIE→ETH path, or confirm SOUND.

    7. FC-392-11 (MED, the backing/EV-dynamics half cross-referenced from 391 where the RNG-lock half was
       attested): the stochastic sDGNRS auto-rebuy backing rolls win-after-win until a loss zeroes the
       pending stake. Charge the council to MODEL whether a sequence of losses (or a single large-carry
       loss) can drop sDGNRS BURNIE backing below outstanding redemption obligations, and whether
       claimCoinflipCarry (player-pull from the carry) interacts with the sDGNRS settle to let a redeemer
       extract during a window where backing is mid-roll. NOTE the RNG-lock window over the carry roll was
       attested airtight at 391 (cross-ref); here the concern is the BACKING DYNAMICS (the path-dependent
       backing vs obligations). FC-392-13 folds in: claimCoinflipCarry settles resolved days (wins→carry,
       pending loss→zero) BEFORE withdrawing — confirm the take-profit chunks the settle banks into
       claimableStored + the carry withdrawal cannot double-count a single win across the two channels.
       Model the loss-sequence backing, or confirm SOUND.

    8. FC-392-12 / FC-392-20 (LOW-INFO): the seed-stake leaderboard/bounty exclusion — the seeds are direct
       coinflipBalance/_setFlipStake writes specifically to stay off the top-bettor leaderboard, the bounty,
       and biggest-flip records; confirm no path reads coinflipBalance/stake[d][VAULT|SDGNRS] for days 1-20
       into a leaderboard/bounty/BAF/biggestFlipEver computation that mis-credits the protocol addresses.
       And the claim-window widening to 365 (+1460 auto-rebuy-off) raises the per-claim resolution-walk
       iteration ceiling (deep uint32 safe, shallow uint16 safe for 365; per-call + caller-paid, NOT in the
       advanceGame chain) — confirm no realistic actor can force a many-hundred-day cold-SLOAD walk into a
       gas-sensitive caller (this echoes the permissionless-access gas lead FC-393-04, cross-ref 393).

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 392-04. No fenced
    solidity blocks beyond the minimal identifier/selector/line citations the council needs to locate the
    code.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md && grep -q "a8b702a7" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md && for id in BURNIE-01 BURNIE-02 BURNIE-03 BURNIE-04 BURNIE-05 BURNIE-06 FC-392-11 FC-392-12 FC-392-13 FC-392-16 FC-392-17 FC-392-18 FC-392-19 FC-392-20; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "autoRebuyCarry|carry.*backing|under-credit" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md && grep -Eqi "VAULT.*window|window-aging|aged? out|30-day" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md && grep -Eqi "survive|conservation|round-trip|lossless" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md</automated>
  </verify>
  <acceptance_criteria>
    392-02-COUNCIL-PROMPT-BURNIE.md exists and is non-empty; instructs reading frozen source at `a8b702a7`
    via git show; states the BURNIE-off-the-ETH-spine framing + the survive-before-mint defining intent +
    the intended-variance-trade anchor; names ALL of BURNIE-01..06 + FC-392-16..20 + the cross-ref
    backing-dynamics leads FC-392-11/-12/-13; charges FC-392-16 (carry excluded from redemption backing) and
    FC-392-17 (VAULT seed 30-day window-aging) as DEDICATED prime targets demanding a CONFIRM/REFUTE/BY-
    DESIGN with the backing accounting traced (not a hand-wave); carries the KNOWN-BY-DESIGN exclusion list;
    uses neutral vocabulary; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral BURNIE/coinflip-rework council prompt is authored against the frozen subject, covering all 6
    BURNIE reqs + the 5 coinflip-burnie owned leads (FC-392-16..20) + the 3 cross-ref backing-dynamics leads
    (FC-392-11/-12/-13), with the two prime backing leads charged hard and the survive-before-mint +
    conservation + backing-completeness doctrine encoded, ready to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the BURNIE council fan-out and record the council-net capture</name>
  <files>.planning/phases/392-entropy-and-econ/council/burnie.gemini.txt, .planning/phases/392-entropy-and-econ/council/burnie.codex.txt, .planning/phases/392-entropy-and-econ/council/burnie.council.json, .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md (Task 1 output)
    - .planning/phases/391-rng-spine/391-01-COUNCIL-NET.md (the just-approved COUNCIL-NET capture record shape to match)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/392-entropy-and-econ/council --label burnie
    .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md`. council.sh fans gemini+codex in
    parallel internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations is
    needed beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped CLI is
    recorded in the council.json skipped[] and is NOT fatal; do not treat a skip as a failure. (This plan is
    in the same wave as 392-01 ECON; if both are run by the same executor, let each single fan-out complete
    before the next — but they share no files so they may also run in parallel agents.)

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify). Do NOT
    run `hardhat compile --force` (regenerates ContractAddresses source).

    Write 392-02-COUNCIL-NET.md recording: the burnie.council.json manifest's available[]/skipped[], the raw
    output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 392-04), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for BURNIE" line so 392-04
    can confirm both-nets-on-record before any verdict. If a CLI is in skipped[], record WHICH and note the
    slice still has the other model on record (a single available model satisfies "council on record" with
    the skip documented; both-unavailable would require a re-run and must be surfaced, not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/392-entropy-and-econ/council/burnie.council.json && test -s .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `burnie.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable
    CLI is in the council.json skipped[]; 392-02-COUNCIL-NET.md records the available/skipped + raw output
    paths + the byte-freeze attestation + an explicit "NET 1 ON RECORD" line; `git diff a8b702a7 --
    contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the BURNIE/coinflip-rework slice with raw output
    captured under the phase council/ dir, the subject proven byte-frozen, and the capture documented for
    the Wave-2 adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable CLI silently dropping the only net (handled by skipped[]) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir |
| T-392-05 | Information disclosure / false-green | the BURNIE slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 392-02-COUNCIL-NET.md; both-unavailable is surfaced for re-run, never passed silently |
| T-392-06 | Repudiation / under-charge | the two prime backing leads (FC-392-16/-17) waved as "BURNIE is worthless so it doesn't matter" without tracing the backing accounting | mitigate | the prompt charges both prime leads HARD as dedicated targets demanding a CONFIRM/REFUTE/BY-DESIGN with the backing accounting traced to the carry / the window-aging skip |
| T-392-SC2 | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers BURNIE-01..06 + FC-392-16..20 +
  the cross-ref backing-dynamics leads FC-392-11/-12/-13, with FC-392-16 carry-backing + FC-392-17
  VAULT-window charged hard and the survive-before-mint + conservation + backing-completeness doctrine
  encoded.
- `council.sh` ran for `--label burnie`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[].
- 392-02-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full BURNIE/coinflip-rework slice, with raw
gemini/codex output captured under the phase council/ dir and the capture documented, so the Wave-2
Claude-net + adjudication plan can fold the council leads in before issuing any per-item verdict — the two
prime backing leads (carry-excluded-from-backing, VAULT seed window-aging) charged for a CONFIRM/REFUTE/
BY-DESIGN. The subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/392-entropy-and-econ/392-02-SUMMARY.md` when done
</output>
</content>
