---
phase: 393-permissionless-composition
plan: 02
type: execute
wave: 2
depends_on: ["393-01"]
files_modified:
  - .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md
  - .planning/phases/393-permissionless-composition/393-FINDINGS.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full PERMISSIONLESS-COMPOSITION surface, independent of the council"
    - "Every ACCESS-01..05 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the beneficiary-credit / gate / CEI ordering / reservation bound / un-manufacturability cite that settles it"
    - "Every FC-393-01..04 owned lead AND every inherited cross-ref (FC-390-03, FC-390-06, FC-392-08, FC-392-20) has an explicit ADJUDICATED verdict, consistent with the already-settled solvency/ECON halves at 390/392 (the ACCESS half is adjudicated here)"
    - "ACCESS-02 (keeper box-bounty) is settled by a dedicated REAL economic argument — per-box settle cost at REAL prevailing gas (5/20/50+ gwei) vs the 0.000015-ETH bounty, ×0.5 survive-flip × peg-discount flip-credit illiquidity, AND an un-manufacturability proof (each settle-able box exists only because a real burn created it; issuance bounded per FC-390-06) — proven net-negative + un-manufacturable, OR surfaced as a finding; NOT hand-waved (the substantive faucet item)"
    - "ACCESS-04 / FC-393-03 (partial-balance burst solvency) is settled by a dedicated same-block multi-claim leg-accounting argument — Σ legs == Σ rolled == Σ released _pendingRedemptionEthValue, MAX(175%) reservation covers each leg, ETH-drain shifts to stETH leg fail-closed, no strand / under-pull — proven, OR surfaced; consistent with the 390 FC-392-08/FC-393-03 solvency-half"
    - "ACCESS-03 / FC-393-01 (forced claim-timing) is settled by the adjacent-level reward-MAGNITUDE divergence analysis (timing is by-design — the verdict turns on whether the magnitude diverges enough to be materially harmful), NOT a 'timing is by-design so fine' dismissal"
    - "ACCESS-05 (gate + reentrancy intactness) enumerates the freeze/rngLocked/liveness/gameOver gate + the ETH/stETH CEI ordering on EVERY new/widened entrypoint and proves each intact or surfaces a hole"
    - "Both nets are on record before the verdict is issued; the skeptic gate is run before any CATASTROPHE/HIGH (access/reentrancy/MEV is LOW/confirmatory — but a real grief/faucet/steer or a burst-solvency strand weighs higher)"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the PERMISSIONLESS-COMPOSITION surface, independent of the council, with per-req / per-lead attack passes + the dedicated keeper-bounty real-gas economics + the partial-balance burst-solvency leg accounting + the forced-timing magnitude analysis + the gate/reentrancy enumeration"
      min_lines: 90
    - path: ".planning/phases/393-permissionless-composition/393-FINDINGS.md"
      provides: "The per-item adjudication table: every ACCESS req + every FC-393 lead + every inherited cross-ref with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding"
      min_lines: 90
  key_links:
    - from: "393-FINDINGS.md"
      to: "393-01 council outputs (council/*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "393-FINDINGS.md"
      to: "ACCESS-01..05 + FC-393-01..04 + inherited cross-refs"
      via: "per-item adjudicated verdict rows"
      pattern: "ACCESS-02|FC-393-03"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME PERMISSIONLESS-COMPOSITION surface the council
swept, then ADJUDICATE every ACCESS-01..05 requirement, every FC-393-01..04 owned lead, and every inherited
cross-ref (FC-390-03, FC-390-06, FC-392-08, FC-392-20) against the byte-frozen subject `a8b702a7`, folding
in the NET-1 council leads from 393-01. Give the keeper box-bounty (ACCESS-02) a dedicated REAL economic
treatment against REAL prevailing gas + flip-credit illiquidity + un-manufacturability, give the
partial-balance burst solvency (ACCESS-04 / FC-393-03) a dedicated same-block multi-claim leg-accounting
treatment, settle the forced-claim-timing (ACCESS-03 / FC-393-01) on the adjacent-level reward-MAGNITUDE
question (not the timing model), enumerate the gate + reentrancy intactness on every new/widened entrypoint
(ACCESS-05), run the skeptic gate before presenting anything as CATASTROPHE/HIGH, and write
`393-FINDINGS.md` with a per-item verdict table (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) each carrying
the beneficiary-credit / gate / CEI / reservation / un-manufacturability cite that settles it.

Purpose: this is the verdict step for the permissionless-composition surface. A no-finding verdict for any
sweep slice requires BOTH nets on record (AUDIT-V63-PLAN §2) — this plan puts NET 2 on record and
synthesizes both nets. Access-control / reentrancy / MEV is the LOW/confirmatory threat class (§4), but the
SUBSTANTIVE items are ACCESS-02 (keeper-bounty economics vs real gas) and ACCESS-04 (partial-balance burst
solvency), so those get dedicated rigorous treatment and a real grief/faucet/steer or a burst-solvency
strand weighs higher. This phase CONSUMES the boundary results already adjudicated in 388-392 — the
inherited cross-refs' solvency/ECON halves are settled at 390/392 (FC-390-03 REFUTED, FC-390-06 REFUTED,
FC-392-08 solvency/CEI REFUTED + ECON cap-RMW BY-DESIGN, FC-392-20 INFO) and the ACCESS halves are
adjudicated HERE for cross-ref consistency, not re-derived. Any CONFIRMED finding is DOCUMENTED + ROUTED to
a gated USER-hand-review fix, never fixed here (the subject is byte-frozen).

Output: 393-02-CLAUDE-NET.md (the independent adversarial analysis) and 393-FINDINGS.md (the adjudication of
the full phase-393 surface). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/permissionless-access.md
@.planning/phases/390-solvency-spine/390-FINDINGS.md
@.planning/phases/392-entropy-and-econ/392-FINDINGS.md
@.planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md
@.planning/phases/393-permissionless-composition/council/access.gemini.txt
@.planning/phases/393-permissionless-composition/council/access.codex.txt
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 5 reqs to adjudicate (REQUIREMENTS.md ACCESS): -->
<!--   ACCESS-01 every permissionless claim credits only the beneficiary (no third-party ETH push/forced credit); -->
<!--   ACCESS-02 keeper box-bounty net-negative vs REAL gas (5-50+ gwei, not 0.5-gwei peg) + flip-credit -->
<!--     illiquidity + un-manufacturable; -->
<!--   ACCESS-03 forced claim-timing cannot materially reduce a winner's reward or steer an outcome; -->
<!--   ACCESS-04 partial-balance redemption-leg solvency holds under same-block bursts; -->
<!--   ACCESS-05 freeze/rngLocked/liveness/gameOver gates intact on all new/widened entrypoints; reentrancy -->
<!--     closed across ETH/stETH legs. -->
<!-- The 4 owned leads (388-02-FINDING-CANDIDATES): FC-393-01 (forced-timing on the winner — verify -->
<!--   adjacent-level magnitude), FC-393-02 (forfeit-to-self timing benign?), FC-393-03 (partial-balance -->
<!--   same-block burst solvency, MED prime), FC-393-04 (widened claim-loop gas worst-case). -->
<!-- The 4 inherited cross-refs (solvency/ECON half already adjudicated at 390/392 — adjudicate the ACCESS -->
<!--   half here, consistent): FC-390-03 (mid-batch gameOver/frozen boundary — REFUTED at 390), FC-390-06 -->
<!--   (keeper-bounty creditFlip issuance bound — REFUTED at 390; ACCESS-02 downstream-dilution half here), -->
<!--   FC-392-08 (redemption ETH-spin pool RMW + recirc — solvency/CEI REFUTED at 390, ECON cap-RMW BY-DESIGN -->
<!--   at 392; permissionless cross-chunk-race half here), FC-392-20 (widened claim-loop gas — INFO at 392; -->
<!--   fresh worst-case half here as FC-393-04). -->
<!-- Authoritative frozen ACCESS line-cites (verified via permissionless-access.md against a8b702a7 — re-read -->
<!--   via git show, do not trust blind): -->
<!--   DegenerusGameDecimatorModule.sol: permissionless claimDecimatorJackpot(address,uint24) @293 (credits -->
<!--     player never caller); claimDecimatorJackpotMany @325 (batch skips claimed/non-winner no-revert, caches -->
<!--     over/frozen/offsets/totalBurn once @338-341); shared core _claimDecimatorJackpotFor @385; both gate -->
<!--     prizePoolFrozen @298; bounty BOX_BOUNTY_ETH_TARGET=15_000_000_000_000 wei per settled box paid -->
<!--     !gameOver && settled!=0 as (settled*TARGET*PRICE_COIN_UNIT)/_mintPriceInContext() BURNIE creditFlip -->
<!--     to msg.sender @364-370; _mintPriceInContext() @376; forced-timing _rollTargetLevel(level+1,frozenSeed) -->
<!--     + whale-pass _queueTicketRange(winner,level+1,…) read LIVE level @283-322; frozen seed round.rngWord -->
<!--     snapshot @277. -->
<!--   StakedDegenerusStonk.sol: claimRedemption(address,uint24) @771 (permissionless LIVE; self-only -->
<!--     post-gameOver isGameOver && player!=msg.sender reverts Unauthorized); claimRedemptionMany @787 (batch, -->
<!--     bounty mirror @803-814); dust-lootbox drop <0.01 ETH forfeit to sDGNRS claimable @839-848/:897-901; -->
<!--     partial-balance legs min(address(this).balance,legAmount) ETH + GAME pulls remainder stETH @880/:888/ -->
<!--     :898 (reverts if short); _pendingRedemptionEthValue release; lootbox seed hash2(rngWord,player) -->
<!--     rngWord=rngWordForDay(day+1) @878; submit gate rngWordForDay(currentPeriod)!=0 revert -->
<!--     BurnsBlockedBeforeDailyRng @991; post-gameover claimRedemption delete pendingRedemptions + lower -->
<!--     _pendingRedemptionEthValue BEFORE _payEth(.call) (CEI), _payEth stETH-first/ETH-call-last @1098; Game -->
<!--     callees resolveRedemptionLootbox @926 + creditRedemptionDirect @1004 gate msg.sender!=SDGNRS. -->
<!--   BurnieCoinflip.sol: claimCoinflipCarry(address,uint256) @366 (player=_resolvePlayer self/operator, mints -->
<!--     to player; rngLocked gate @371; settles resolved days first then pays settled carry); COIN_CLAIM_DAYS -->
<!--     90→365; AUTO_REBUY_OFF_CLAIM_DAYS_MAX 1095→1460; counters uint8→uint16 @136-138; deep uint32 (safe -->
<!--     1460), shallow uint16 (safe 365); processCoinflipPayouts onlyDegenerusGameContract auto-settle -->
<!--     deep=false (bounded windowDays 365, ~1 day/advance); onlyBurnieCoin tightened to COIN only. -->
<!--   DegenerusGame.sol: dispatcher stubs claimDecimatorJackpot(address,uint24) @808, claimDecimatorJackpotMany -->
<!--     @832; _payoutWithStethFallback @1888 reordered (53cd25cf) stETH-out-first / ETH-call-last; -->
<!--     distributeYieldSurplus internal-only (AdvanceModule:529/783), no external stub, credits-only when -->
<!--     totalBal>obligations. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110 (the green baseline). Pinned regressions: -->
<!--     DecimatorBountyRegression.t.sol (5 bounty rules: per-box / scales / no-pay-post-gameover / ETH-value -->
<!--     holds across price curve / faucet << burn cost); the 1460-deep + 365-window perma-brick gas regression -->
<!--     (0a2209d4). -->
<!--   Already-adjudicated cross-ref halves (390/392): FC-390-03 REFUTED (390 §2b: batch caches over/frozen -->
<!--     once, resolution-into-claimable only, all value to winner); FC-390-06 REFUTED (390 §2b: per box -->
<!--     actually settled, (wallet,day) 160-ETH base cap + 50%/day supply cap, decimator per real claimed entry; -->
<!--     downstream BURNIE-dilution → ACCESS-02/393); FC-392-08 REFUTED solvency/CEI (390 §2c: flush-before- -->
<!--     recirc, stETH-remainder-in-before-credit, chunks sequential one frame) + ECON cap-RMW BY-DESIGN (392: -->
<!--     recirc depth 1, monotonic cap; permissionless-race → 393); FC-393-02 BY-DESIGN/REFUTED solvency-half + -->
<!--     FC-393-03 REFUTED solvency-half (390 §2c: each leg recomputes fresh bal, GAME pulls remainder fail- -->
<!--     closed, Σ legs == Σ rolled == Σ released, MAX reservation covers — ACCESS half → 393); FC-392-20 INFO -->
<!--     (392: bounded, caller-paid, off advanceGame chain; fresh worst-case → 393 as FC-393-04). -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial attack passes + keeper-bounty real-gas economics + partial-balance burst-solvency + forced-timing magnitude + gate/reentrancy enumeration + per-req / per-lead adjudication vs frozen source</name>
  <files>.planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md</files>
  <read_first>
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-393-01..04 verbatim + the inherited cross-refs FC-390-03, FC-390-06, FC-392-08, FC-392-20 — the leads to adjudicate, with their owner/disposition columns)
    - .planning/v63-surface-map/permissionless-access.md (the full §1-§5 + the net assessment — the "what we believe is safe" thesis to attack: §1 decimator + bounty + forced-timing, §2 redemption + bounty + reentrancy + (a)/(b) residual leads, §3 carry, §4 widened windows + gas, §5 incidental non-widening)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md (§2b/§2c: the already-adjudicated FC-390-03 / FC-390-06 / FC-392-08 solvency-half / FC-393-02 / FC-393-03 solvency-half verdicts + the skeptic-gate 3-condition EV lens applied there — adjudicate the ACCESS half consistently)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md + 392-FINDINGS-BURNIE.md (FC-392-08 ECON cap-RMW BY-DESIGN + permissionless-race → 393; FC-392-20 INFO + gas worst-case → 393 — the ECON/gas halves to fold consistently)
    - the keeper-bounty doctrine [[feedback_bounty_exploit_uses_real_gas_not_peg_ref]] (compare reward to REAL prevailing gas 5-50+ gwei + flip-credit illiquidity, NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg) + the design-intent anchors [[lootbox-resolution-timing-by-design]] + [[open-e-operator-approval-trust-boundary]] + [[redemption-dust-lootbox-drop-bydesign]] + [[genesis-admin-self-break-nonfinding]]
    - the frozen source via git show a8b702a7:contracts/modules/DegenerusGameDecimatorModule.sol (the permissionless claim @293, the batch @325-341, the shared core @385, the bounty @364-376, the forced-timing _rollTargetLevel/_queueTicketRange @283-322, the frozen seed @277), contracts/StakedDegenerusStonk.sol (claimRedemption @771, claimRedemptionMany + bounty @787-814, the dust-forfeit @839-848/:897-901, the partial-balance legs @880/:888/:898, the CEI _payEth @1098, the day+1 read @878, the submit gate @991), contracts/BurnieCoinflip.sol (claimCoinflipCarry @366-371, the widened caps + counters @136-138, the auto-settle deep=false), contracts/modules/DegenerusGame.sol (the dispatcher stubs @808/:832, _payoutWithStethFallback @1888), and test/REGRESSION-BASELINE-v63.md + DecimatorBountyRegression.t.sol (the 5 pinned bounty rules + the perma-brick gas regression)
    - .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md + council/*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the authorization / bounty / solvency / gate / reentrancy claims first, then fold
    the council leads in at the end of this task). For each of the 5 reqs, 4 owned leads, and 4 inherited
    cross-refs, attempt to BREAK the property with a concrete reachable call sequence (multi-tx / same-block
    burst / observe-then-retrigger where the ordering matters), then record the result.

    ACCESS adjudication (ACCESS-01..05 + FC-393-01..04 + the inherited cross-refs):

    - ACCESS-01 (beneficiary-only credit): for EACH permissionless / widened entrypoint
      (claimDecimatorJackpot(player,…) @DecimatorModule:293, claimDecimatorJackpotMany @325, claimRedemption
      (player,…) @StakedStonk:771, claimRedemptionMany @787, claimCoinflipCarry(player,…)
      @BurnieCoinflip:366) trace WHO receives the value. Confirm each forwards value to `player` (or into the
      player's game-claimable via creditRedemptionDirect / lootbox via resolveRedemptionLootbox / minted
      BURNIE to player), never to msg.sender, so a third-party trigger pushes NO ETH at the player and holds
      no exclusive timing. Attack: any path that pushes a forced credit or ETH at a victim, routes value to
      the caller, or lets a third party force a credit that a later sweep forfeits (note: post-gameover
      redemption is self-claim-only — isGameOver && player!=msg.sender reverts Unauthorized @ the gate —
      precisely to avoid a third party forcing a game-claimable credit the post-gameover sweep would
      forfeit). Cite the beneficiary-credit line that settles each. Confirm the thin-dispatcher does not
      mis-route a permissionless stub into a gated callee or vice versa (delegatecall preserves msg.sender +
      address(this); module gates msg.sender != COIN/SDGNRS + onlyFlipCreditors/onlyGame on callees stay
      correct).

    - ACCESS-02 (the keeper box-bounty — DEDICATED REAL economic treatment, the substantive faucet item):
      both bounties are BOX_BOUNTY_ETH_TARGET = 15_000_000_000_000 wei (0.000015 ETH) of value per settled
      box, paid `!gameOver && settled != 0` as `(settled*TARGET*PRICE_COIN_UNIT)/_mintPriceInContext()`
      BURNIE flip-credit to msg.sender (DecimatorModule:364-370; StakedStonk:803-814). DO NOT use the
      0.5-gwei peg. PROVE the bounty is net-NEGATIVE + un-manufacturable OR surface a finding: (i) cost the
      per-box settle (the ~30k-gas settle + the lootbox-resolve delegatecall for a LIVE winner) at REAL
      prevailing gas (5/20/50+ gwei) and show the 0.000015-ETH reward is net-negative BEFORE the 50% flip
      risk and the flip-credit illiquidity (creditFlip → _addDailyFlip next-day stake, must survive a 50/50
      flip ×0.5, then the BURNIE→ETH peg discount ~0.59 ⇒ realized liquid value ≈ a small fraction of
      0.000015 ETH while a single settle tx at 20 gwei is ~0.0006+ ETH — ~40x under-water); (ii) prove
      UN-MANUFACTURABLE — each settle-able box exists only because a real player BURNED (decimator BURNIE /
      sDGNRS gambling-burn), so a Sybil cannot fabricate bounty-eligible work; couple to FC-390-06 (issuance
      bounded: one box per real claim, (wallet,day) 160-ETH base cap + 50%/day supply cap on the redemption
      side, decimator per real claimed entry — no UNBOUNDED dilutive BURNIE mint that downstream dilutes the
      sDGNRS/vault ETH backing); (iii) confirm `_mintPriceInContext()` (@376) reproduces mintPrice() exactly
      so the caller can't SKEW the bounty + the ETH-value is price-independent; (iv) cross-check the 5 pinned
      DecimatorBountyRegression rules (per-box / scales / no-pay-post-gameover / ETH-value-holds-across-price
      / faucet << burn cost) as the green-baseline anchor (record whether any of the FOUR specific real-gas
      claims is UN-NETTED by the existing regression — if the regression pins faucet << burn cost but NOT the
      explicit real-gas-net-negative-after-illiquidity number, note that as a routed test-hardening item, NOT
      a contract change). Settle either way — no hand-wave ("illiquid so fine").

    - ACCESS-04 / FC-393-03 (partial-balance burst solvency — DEDICATED same-block multi-claim leg
      accounting, the MED prime): each redemption leg sends `min(address(this).balance, legAmount)` ETH and
      pulls the rest as stETH (StakedStonk @880/:888/:898); the comment asserts the MAX(175%) reservation
      guarantees ETH+stETH ≥ rolled. PROVE the burst-solvency property OR surface a finding: under an
      ADVERSARIAL sequence of MANY partial-balance LIVE claims in ONE block (permissionless
      claimRedemptionMany / interleaved claimRedemption draining address(this).balance progressively), reason
      whether Σ(legs over the burst) == Σ(rolled) == Σ(released `_pendingRedemptionEthValue`), and whether
      the MAX(175%) reservation (ETH+stETH ≥ Σ MAX ≥ Σ rolled) covers each leg so that an ETH-balance drain
      merely SHIFTS the deficit to the stETH leg of the SAME held reservation (GAME pulls the remainder
      fail-closed, reverting if short), never stranding ETH (left unreleased) or under-pulling stETH. Spell
      out the interleavings: a claim draining balance to ~0 so the next claim's ETH leg clamps to ~0 and the
      whole roll must come from stETH; a claim whose stETH transferFrom would exceed the held reservation.
      This is CONSISTENT with the 390 FC-392-08 / FC-393-03 solvency-half (REFUTED at 390: each leg
      recomputes fresh `bal`, GAME pulls remainder fail-closed, Σ legs == Σ rolled == Σ released, MAX
      reservation covers) — attack the ACCESS half (the cross-chunk / same-block race + the FC-392-08
      cap-RMW-raced-across-chunks half routed here) and either confirm the burst can't strand/under-pull OR
      surface a finding. Record whether a same-block-burst burst-solvency oracle exists or is a routed
      test-hardening item.

    - ACCESS-03 / FC-393-01 (forced claim-timing — the adjacent-level MAGNITUDE question, NOT the timing
      model): a third party can FORCE a winner's live-game decimator claim to resolve at a LIVE `level` the
      winner did not choose — `_rollTargetLevel(level+1, frozenSeed)` + the whale-pass `_queueTicketRange
      (winner, level+1,…)` read the LIVE level @claim (DecimatorModule:283-322). The frozen seed prevents
      win/loss steering (DO NOT re-litigate — timing is by-design per [[lootbox-resolution-timing-by-design]]).
      Settle the MAGNITUDE: quantify whether any reward MAGNITUDE / target-level distribution diverges enough
      between ADJACENT levels to make a forced resolution MATERIALLY harmful to the winner (e.g. a griefer
      settling just before an advance that shifts the target-level distribution or the whale-pass start
      level). Reason about the adjacent-level reward-magnitude / target-level-distribution delta and rule it
      material (→ finding/MONITOR) or inert (→ BY-DESIGN/REFUTED) with the concrete reasoning, not a "timing
      is by-design so fine" dismissal.

    - ACCESS-01 / FC-393-02 (forfeit-to-self timing — benign?): the dust lootbox (<0.01 ETH half) forfeits to
      sDGNRS's OWN claimable (StakedStonk:839-848/:897-901), raising backing for ALL holders. A keeper
      choosing WHICH redemptions to settle could bias WHEN forfeits land vs full lootboxes resolve. Confirm
      the per-claim split is DETERMINISTIC from the fixed roll + the player's own owed value ⇒ NO per-victim
      value extraction, only benign timing of uniform backing accrual (the dust-drop itself is by-design per
      [[redemption-dust-lootbox-drop-bydesign]]; the question is strictly per-victim extractability). This is
      consistent with the 390 FC-393-02 solvency-half (BY-DESIGN/REFUTED). REFUTE/BY-DESIGN or surface.

    - ACCESS-05 (gate + reentrancy intactness enumeration over the new/widened entrypoints): for EACH of
      claimDecimatorJackpot/Many, claimRedemption/Many, claimCoinflipCarry enumerate (i) the
      freeze/rngLocked/liveness/gameOver gate (decimator + redemption claims revert on prizePoolFrozen @298;
      post-gameover redemption self-claim-only — isGameOver && player!=msg.sender reverts Unauthorized;
      claimCoinflipCarry gates rngLocked @371); (ii) the ETH/stETH reentrancy CEI (post-gameover
      claimRedemption deletes pendingRedemptions + lowers _pendingRedemptionEthValue BEFORE the untrusted
      _payEth .call; _payEth stETH-first / ETH-call-last @1098; the Game's _payoutWithStethFallback @1888
      reordered 53cd25cf stETH-out-first / ETH-call-last closing V62-03); (iii) the Game-side callees
      resolveRedemptionLootbox @926 + creditRedemptionDirect @1004 gate msg.sender != SDGNRS; and
      distributeYieldSurplus is reachable only internally from advanceGame (no external stub, credits-only
      when totalBal>obligations — confirm not an independently-exploitable permissionless lever). Attack: any
      missing/widened gate or open reentrancy path across the ETH/stETH legs. Fold in FC-390-03 (LOW — the
      mid-batch gameOver/frozen boundary, solvency REFUTED at 390: the batch caches over/frozen/offsets/
      totalBurn ONCE @338-341 so claim effects can't flip the boundary mid-batch — confirm the ACCESS half:
      no permissionless batch can split inconsistently across the boundary). Settle each gate/reentrancy
      property intact or surface.

    - ACCESS-05 / FC-393-04 (inherited FC-392-20, the widened claim-loop gas worst-case): the coinflip claim
      windows widened (COIN_CLAIM_DAYS 90→365, AUTO_REBUY_OFF_CLAIM_DAYS_MAX 1095→1460, counters
      uint8→uint16 @136-138) over the NEW packed storage (each day a masked sub-word read/write). Claims are
      user-paid + OUT of the advanceGame chain; the advanceGame-chain sDGNRS auto-settle walks deep=false
      (bounded windowDays 365, ~1 day/advance). FC-392-20 ruled this INFO at 392 (bounded, caller-paid). The
      ACCESS half: confirm no realistic actor can force a many-hundred-day cold-SLOAD walk into a
      GAS-SENSITIVE caller (the advanceGame chain specifically) under the new packed masked sub-word layout —
      i.e. the worst-case 365/1460 loop is reachable only by a caller paying their OWN gas, never bricking
      advanceGame. Anchor on the 1460-deep + 365-window perma-brick gas regression (0a2209d4). Settle INFO/
      REFUTED (off the gas-sensitive chain) or surface; record whether a fresh packed-layout worst-case gas
      measurement is a routed test-hardening item.

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried (multi-tx / same-block
    burst / observe-then-retrigger where it matters; WHO gets the value; the gate / CEI / reservation /
    un-manufacturability) · STATE VAR + file:line at a8b702a7 · the beneficiary-credit / gate / CEI /
    reservation / real-gas-number / magnitude-reasoning that settles it · a provisional verdict (CONFIRMED /
    REFUTED / BY-DESIGN / MONITOR). Note where the green baseline's pinned regressions (the 5
    DecimatorBountyRegression rules, the perma-brick gas regression) already prove a property — and where a
    real-gas-net-negative-after-illiquidity number, a same-block-burst burst-solvency oracle, or a fresh
    packed-layout gas measurement is UN-NETTED (record as a routed test-hardening note, NOT a contract
    change). At the END of the task, read the council outputs (393-01 council/*.txt) and note for each item
    whether the council raised a convergent/divergent lead — fold any council-only lead in as an additional
    row to adjudicate. Write all of this to 393-02-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use neutral
    defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && for id in ACCESS-01 ACCESS-02 ACCESS-03 ACCESS-04 ACCESS-05 FC-393-01 FC-393-02 FC-393-03 FC-393-04 FC-390-03 FC-390-06 FC-392-08 FC-392-20; do grep -q "$id" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "gwei|prevailing gas|real gas" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && grep -Eqi "illiquid|un-manufacturab|manufactur" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && grep -Eqi "partial-balance|same-block|burst|reservation" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && grep -Eqi "beneficiary|credits only|msg.sender" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && grep -Eqi "magnitude|adjacent level|adjacent-level" .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    393-02-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a provisional
    verdict for EACH of ACCESS-01..05, FC-393-01..04, and the inherited cross-refs FC-390-03, FC-390-06,
    FC-392-08, FC-392-20; ACCESS-01 cites the beneficiary-credit per entrypoint; ACCESS-02 carries a
    dedicated REAL economic argument (real-gas cost vs the 0.000015-ETH bounty + flip-credit illiquidity +
    un-manufacturability + issuance bound; proof or finding, not a hand-wave); ACCESS-04/FC-393-03 carries
    the same-block multi-claim leg-accounting argument; ACCESS-03/FC-393-01 carries the adjacent-level
    magnitude analysis (not a timing-by-design dismissal); ACCESS-05 enumerates the gate + reentrancy CEI per
    entrypoint; any un-netted test-hardening gap (real-gas number / burst oracle / fresh packed gas) is noted
    as a routed item; the council leads are folded in at the end; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record, independent of the council, with a per-item attack
    attempt + provisional verdict over the full phase-393 permissionless-composition surface, the
    keeper-bounty real-gas economics and the partial-balance burst-solvency given dedicated rigorous
    treatment, the forced-timing settled on the magnitude question, the gate/reentrancy enumerated, the
    council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 393-FINDINGS.md with the per-item verdict table</name>
  <files>.planning/phases/393-permissionless-composition/393-FINDINGS.md</files>
  <read_first>
    - .planning/phases/393-permissionless-composition/393-02-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md + council/*.txt (NET 1 leads)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md (the just-approved FINDINGS structure to match — header, both-nets table, per-item verdict table, the 3-condition skeptic-gate lens, the cross-ref-consistency block, routing block)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS.md (the cross-ref-consistency-notes block shape for FC-392-08/FC-392-20 — match the half-ownership statement)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting: access/reentrancy/MEV = LOW/confirmatory; keeper-bounty uses REAL gas) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact ACCESS-01..05 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH) + [[feedback_bounty_exploit_uses_real_gas_not_peg_ref]]
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into 393-FINDINGS.md — the phase-393 adjudication
    deliverable. Structure:

    (1) Header: subject `a8b702a7` byte-frozen; baseline = REGRESSION-BASELINE-v63 (854/0/110; the pinned
    bounty regression = DecimatorBountyRegression.t.sol 5 rules; the 1460-deep + 365-window perma-brick gas
    regression 0a2209d4 — but record whether the explicit real-gas-net-negative-after-illiquidity number / a
    same-block-burst burst-solvency oracle / a fresh packed-layout worst-case gas measurement are un-netted
    routed test-hardening items, not relied on as already-netted); method = COUNCIL + CLAUDE both; posture =
    AUDIT-ONLY (a CONFIRMED finding routes to a gated USER-hand-review fix, never fixed here); access/
    reentrancy/MEV = LOW/confirmatory (§4) — but the substantive items are ACCESS-02 (keeper-bounty
    economics) + ACCESS-04 (partial-balance burst solvency), so a real grief/faucet/steer or a burst-solvency
    strand weighs higher. State this phase CONSUMES the 388-392 boundary results (the cross-ref solvency/ECON
    halves settled at 390/392; the ACCESS halves adjudicated here).

    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    393-01-COUNCIL-NET.md, with the codex usage-cap skip + the post-reset re-run flag to 396 if codex
    skipped) and NET 2 (Claude — 393-02-CLAUDE-NET) are both on record for the ACCESS slice. A no-finding
    (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 5 reqs (ACCESS-01..05), ALL 4 owned
    leads (FC-393-01..04), AND the 4 inherited cross-refs (FC-390-03, FC-390-06, FC-392-08, FC-392-20).
    Columns: ITEM · what it claims · NET-1 result · NET-2 result · ADJUDICATED VERDICT (CONFIRMED / REFUTED /
    BY-DESIGN / MONITOR) · the settling beneficiary-credit / gate / CEI / reservation / un-manufacturability /
    magnitude cite (file:line at a8b702a7 or the green-baseline anchor). ACCESS-01 MUST cite the beneficiary
    credit per entrypoint; ACCESS-02 MUST cite the real-gas economics + flip-credit illiquidity +
    un-manufacturability + the issuance-bound (FC-390-06) (or carry a CONFIRMED finding) AND record any
    un-netted real-gas test-hardening note; ACCESS-03 / FC-393-01 MUST cite the adjacent-level magnitude
    reasoning; ACCESS-04 / FC-393-03 MUST cite the same-block multi-claim leg-accounting bound (Σ legs == Σ
    rolled == Σ released; MAX reservation covers); ACCESS-05 MUST cite the per-entrypoint gate + the CEI
    ordering. Weight each finding per AUDIT-V63-PLAN §4 (access/reentrancy/MEV LOW/confirmatory — but a real
    faucet/grief/steer or a burst-solvency strand is elevated). Apply the §5 design-intent anchor
    (permissionless/lootbox claim TIMING is by-design — ACCESS-03 turns on MAGNITUDE not timing;
    operator-approval IS the trust boundary; the dust-lootbox-drop is by-design — FC-393-02 turns on
    per-victim extractability; RTP/EV by-design).

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter (structural-
    protection check + the 3-condition EV lens) and record the gate result for each elevated item. If nothing
    reaches HIGH (the surface-map found 0 HIGH on inspection — every permissionless entrypoint credits the
    beneficiary, gates the windows, uses frozen RNG that mixes player not msg.sender, both bounties illiquid
    + below real gas + un-manufacturable, reentrancy follows CEI), state that explicitly with the gate
    applied to the substantive-attention items (ACCESS-02 keeper-bounty, ACCESS-04 partial-balance burst,
    ACCESS-03 forced-timing). If the dedicated treatment surfaces a real break (a net-positive bounty at real
    gas, a manufacturable faucet, a burst-solvency strand, a materially-harmful adjacent-level magnitude
    divergence, or an open reentrancy/gate hole), run the full skeptic gate and weight it accordingly.

    (5) Cross-ref consistency block: a short block stating, for each inherited cross-ref, the
    already-adjudicated solvency/ECON half (FC-390-03 REFUTED at 390; FC-390-06 REFUTED at 390 — ACCESS-02
    downstream-dilution half adjudicated here; FC-392-08 solvency/CEI REFUTED at 390 + ECON cap-RMW BY-DESIGN
    at 392 — permissionless cross-chunk-race half adjudicated here; FC-392-20 INFO at 392 — fresh worst-case
    half adjudicated here as FC-393-04; FC-393-02/FC-393-03 solvency-half BY-DESIGN/REFUTED at 390 — ACCESS
    half adjudicated here) and the ACCESS-half verdict reached here, confirming CONSISTENCY (no half
    contradicts its counterpart).

    (6) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding, its
    weight, the proposed fix shape, and the note that it goes to a SEPARATE gated USER-hand-review boundary —
    batched, never pre-approved, never auto-committed; the subject re-freezes after). If there are zero
    CONFIRMED findings, state "0 CONFIRMED — document-only; ACCESS-01..05 attested at a8b702a7" and note any
    INFO/MONITOR items carried forward (e.g. FC-393-01 forced-timing if it lands MONITOR, FC-393-02
    forfeit-to-self BY-DESIGN, FC-393-04 gas INFO) AND the routed test-hardening items (a
    real-gas-net-negative-after-illiquidity bounty oracle / a same-block-burst burst-solvency oracle / a
    fresh packed-layout worst-case gas measurement, as possible nets at a later test phase). Any test-only
    (oracle-integrity / missing-property) gap is routed, NOT a contract finding. If codex skipped, carry the
    post-reset codex second-source of the substantive ACCESS-02/ACCESS-04 items to 396.

    (7) Re-attestation line: each of ACCESS-01..05 marked attested-or-finding.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No contract
    source is modified — this is a documentation deliverable.
  </action>
  <verify>
    <automated>test -s .planning/phases/393-permissionless-composition/393-FINDINGS.md && for id in ACCESS-01 ACCESS-02 ACCESS-03 ACCESS-04 ACCESS-05 FC-393-01 FC-393-02 FC-393-03 FC-393-04 FC-390-03 FC-390-06 FC-392-08 FC-392-20; do grep -q "$id" .planning/phases/393-permissionless-composition/393-FINDINGS.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/393-permissionless-composition/393-FINDINGS.md && grep -Eqi "skeptic" .planning/phases/393-permissionless-composition/393-FINDINGS.md && grep -Eqi "gwei|real gas|illiquid" .planning/phases/393-permissionless-composition/393-FINDINGS.md && grep -Eqi "partial-balance|burst|reservation" .planning/phases/393-permissionless-composition/393-FINDINGS.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    393-FINDINGS.md contains: a both-nets-on-record attestation (with the codex-cap skip + 396 re-run flag if
    applicable); a per-item verdict row for EACH of ACCESS-01..05, FC-393-01..04, and the 4 inherited
    cross-refs with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict + settling cite; ACCESS-01 cites the
    per-entrypoint beneficiary credit; ACCESS-02 cites the real-gas economics + illiquidity +
    un-manufacturability + issuance bound (or carries a finding) AND records any un-netted real-gas
    test-hardening item; ACCESS-03/FC-393-01 cites the adjacent-level magnitude reasoning; ACCESS-04/FC-393-03
    cites the same-block leg-accounting bound; ACCESS-05 cites the per-entrypoint gate + CEI; a cross-ref
    consistency block confirms each ACCESS half is consistent with its 390/392 solvency/ECON counterpart; the
    skeptic gate is run and recorded; any CONFIRMED finding is documented + ROUTED (not fixed);
    `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The phase-393 permissionless-composition surface is adjudicated with both nets on record, the skeptic
    gate applied, every req + owned lead + inherited cross-ref carrying an explicit verdict, the keeper-bounty
    settled by a dedicated real-gas economic argument, the partial-balance burst-solvency settled by a
    same-block leg-accounting argument, the forced-timing settled on the magnitude question, the
    gate/reentrancy enumerated, the cross-ref ACCESS halves consistent with their 390/392 counterparts, and
    any CONFIRMED finding documented + routed to a gated fix — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED grief/faucet/steer / keeper-bounty-positive-EV-against-real-gas / burst-solvency strand / gate-or-reentrancy hole, or a FALSE-GREEN adjudication (a real defect waved through), or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings, incl. V62-01/02/03) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-393-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-393-05 | Repudiation / false-green | a REFUTED/BY-DESIGN ACCESS verdict issued without both nets, or the keeper-bounty waved as "illiquid so fine" / the burst-solvency waved as "MAX reservation so fine" without a real argument | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; ACCESS-02 requires the real-gas + illiquidity + un-manufacturability accounting (not the 0.5-gwei peg); ACCESS-04 requires the same-block leg-accounting bound; un-netted real-gas/burst/gas oracles recorded as routed test-hardening items, not relied on as netted |
| T-393-06 | Elevation / under-weighting | a real faucet/grief/steer or burst-solvency strand under-weighted as "LOW access-control", or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; ACCESS-02 + ACCESS-04 weighted as the substantive items (a net-positive bounty / manufacturable faucet / burst strand is elevated above the LOW baseline); keeper-bounty uses REAL gas per [[feedback_bounty_exploit_uses_real_gas_not_peg_ref]] |
| T-393-07 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
| T-393-08 | Repudiation / inconsistency | the ACCESS-half verdict contradicting the already-settled solvency/ECON half at 390/392 | mitigate | the cross-ref consistency block states each already-adjudicated half + the ACCESS-half verdict reached here and confirms no contradiction |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over all 5 reqs + 4 owned leads + 4
  inherited cross-refs.
- Both nets on record before the verdict; the both-nets table present (codex-cap skip + 396 re-run flag if
  applicable).
- ACCESS-01 cites the per-entrypoint beneficiary credit; ACCESS-02 settled by a real-gas + illiquidity +
  un-manufacturability argument (proof or finding) + any un-netted real-gas test-hardening note; ACCESS-03 /
  FC-393-01 settled by the adjacent-level magnitude analysis; ACCESS-04 / FC-393-03 settled by the same-block
  leg-accounting bound; ACCESS-05 settled by the per-entrypoint gate + CEI enumeration.
- The cross-ref consistency block confirms each ACCESS half is consistent with its 390/392 counterpart.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded; ACCESS-02 + ACCESS-04 weighted as the
  substantive items.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The full phase-393 permissionless-composition surface (ACCESS-01..05 + the 4 FC-393 leads + the 4 inherited
cross-refs) is adjudicated against the byte-frozen subject `a8b702a7` with BOTH nets on record, the skeptic
gate applied, and every item carrying an explicit CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a
beneficiary-credit, gate, CEI ordering, reservation bound, un-manufacturability, or magnitude cite. The
keeper box-bounty is settled by a dedicated real-gas economic argument (net-negative + un-manufacturable);
the partial-balance burst-solvency is settled by a same-block leg-accounting argument; the forced-timing is
settled on the adjacent-level magnitude question; the gate + reentrancy is enumerated on every new/widened
entrypoint; the ACCESS halves are consistent with their 390/392 solvency/ECON counterparts. Any CONFIRMED
finding is documented + routed to a gated fix; the subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/393-permissionless-composition/393-02-SUMMARY.md` when done
</output>
