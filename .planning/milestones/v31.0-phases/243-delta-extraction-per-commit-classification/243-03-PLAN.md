---
phase: 243-delta-extraction-per-commit-classification
phase_number: 243
plan: 243-03
plan_number: 03
type: execute
wave: 2
depends_on: [243-01]
files_modified:
  - audit/v31-243-DELTA-SURFACE.md
autonomous: true
requirements: [DELTA-03]
requirements_addressed: [DELTA-03]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: cc68bfc7
head_sha: cc68bfc7
head_anchor: cc68bfc7
head_anchor_history:
  - sha: cc68bfc7
    scope: "amended anchor after cc68bfc7 BAF-flip-gate commit landed mid-Phase-243; 243-03 runs only at cc68bfc7 (did not run at 771893d1)"
    addendum_to: 771893d1
must_haves:
  truths:
    - "`audit/v31-243-DELTA-SURFACE.md` Section 3 is populated in place: the literal header `## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)` is replaced with `## Section 3 — Downstream Call-Site Catalog`, and the section body contains the grep-reproducible call-site inventory for every changed `func`/`modifier` AND every changed `interface-method` row from Section 1."
    - "Scope per CONTEXT.md D-14: call-site enumeration covers `contracts/` tree ONLY. `contracts/mocks/`, `contracts/test/`, `test/`, `scripts/`, `deploy/` are OUT of scope. `contracts/ContractAddresses.sol.bak` is OUT of scope (stale per `feedback_contract_locations.md`)."
    - "Per CONTEXT.md D-15 interface drift scope: the changed interface methods in `IDegenerusGame`, `IDegenerusQuests`, `IStakedDegenerusStonk` (from Section 4.3 of 243-01) ALL have their call sites enumerated, including self-calls via `IDegenerusGame(address(this))` pattern. Delegatecall selectors in `IDegenerusGameModules` interface whose target selectors correspond to changed functions are also enumerated."
    - "Per CONTEXT.md D-18 grep-reproducibility mandate: every call-site row carries the exact `grep` command that found it in the `Grep Command Used` column. Portable POSIX syntax (`grep -rn`, explicit path, no GNU-only `-P` or `-E` unless the `-E` is necessary and noted). The aggregate reproduction recipe appendix §7.3 concatenates all grep invocations for replay."
    - "Per CONTEXT.md D-08 columns for Section 3: `Changed Function/Interface Method | Caller File:Line | Caller Function | Call Type (direct/delegatecall/self-call/library) | Grep Command Used`. Row ID prefix `D-243-X###` — zero-padded 3-digit monotonic. Every column populated; Call Type strictly one of {direct, delegatecall, self-call, library}."
    - "Per CONTEXT.md D-07 item 6 + D-10: Section 6 (Consumer Index) is populated — replaces `## Section 6 — Consumer Index (RESERVED FOR 243-03)` with `## Section 6 — Consumer Index`, and maps every v31.0 requirement ID (DELTA-01..03; EVT-01..04; RNG-01..03; QST-01..05; GOX-01..07; SDR-01..08; GOE-01..06; FIND-01..03; REG-01..02) to the subset of 243 Row IDs (C/F/S/X) scoped under it. Row ID prefix `D-243-I###`."
    - "Section 7.3 Reproduction Recipe Appendix — 243-03 commands is populated per CONTEXT.md D-18: replaces the literal header `### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03` with populated content containing every `grep` invocation used for the call-site catalog and a short replay script that concatenates §7.1 + §7.2 + §7.3 for full-phase reproduction."
    - "Per CONTEXT.md D-12 + D-21: Section 3 population + Section 6 population + §7.3 population completes the final consolidation — `audit/v31-243-DELTA-SURFACE.md` is marked READ-only via a `## File Status` header line at the very top of the file reading exactly `**Status:** FINAL — READ-only per CONTEXT.md D-21. Any Phase 244/245 delta/gap beyond this catalog is recorded as a scope-guard deferral in the discovering plan's own SUMMARY.md — this file is NOT re-edited.`"
    - "Zero `F-31-NN` IDs per CONTEXT.md D-20 (Phase 246 FIND-01..03 owns). If 243-03 surfaces a call-site anomaly (e.g., a changed function with zero callers but accessible externally — a potential dead code finding), append to Section 1.6 Finding Candidates as `- contracts/<path>:<line> — <symbol> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH> — surfaced in 243-03`."
    - "Input integrity: verify BOTH 243-01 and 243-02 commits are present before this plan starts. `git log --oneline -- audit/v31-243-DELTA-SURFACE.md | head -5` shows at least one `243-01` commit AND one `243-02` commit. If only 243-01 is present (243-02 still pending), this plan MUST NOT run — wait for 243-02's commit."
    - "Scope gate: zero writes to `contracts/` or `test/`. `git status --porcelain contracts/ test/` empty before and after every task. Zero writes to any file other than `audit/v31-243-DELTA-SURFACE.md`."
    - "Zero overwrite of upstream section content: 243-03 does NOT touch Section 0, Section 1 row data, Section 1.6 candidate text written by 243-01/243-02 (append-only per D-21), Section 2, Section 4, Section 5, Section 7.1, Section 7.2. It ONLY writes Section 3 body, Section 6 body, §7.3 body, the top-of-file `## File Status` header line, and optionally appends new Finding Candidates to Section 1.6."
    - "Row count floor: Section 3 call-site catalog has at least as many rows as the number of changed `func`+`interface-method` symbols (each changed symbol has at least ONE call site OR is explicitly annotated `NO CALLERS — candidate dead code` with a Section 1.6 finding-candidate cross-ref). Internal-only functions with zero external callers that also have zero internal callers are surfaced as finding candidates."
  artifacts:
    - path: "audit/v31-243-DELTA-SURFACE.md"
      provides: "DELTA-03 downstream call-site catalog (Section 3) + Consumer Index (Section 6) + §7.3 reproduction recipe + top-of-file `## File Status` READ-only marker — final consolidation"
      contains: "## Section 3 — Downstream Call-Site Catalog"
  key_links:
    - from: "Section 1 func/interface-method rows + Section 2 classification rows"
      to: "Section 3 per-symbol call-site catalog entries (D-243-X###)"
      via: "CONTEXT.md D-14 grep across contracts/ tree only + D-15 interface drift scope"
      pattern: "D-243-X[0-9]{3}"
    - from: "v31.0 REQUIREMENTS.md (44 requirement IDs)"
      to: "Section 6 Consumer Index rows (D-243-I###)"
      via: "CONTEXT.md D-07 item 6 + D-10 — each requirement maps to its 243 Row-ID subset"
      pattern: "D-243-I[0-9]{3}"
    - from: "grep -rn patterns used per symbol"
      to: "Section 3 `Grep Command Used` column + §7.3 replay recipe"
      via: "CONTEXT.md D-18 grep-reproducibility mandate"
      pattern: "grep -rn"
---

<objective>
DELTA-03 call-site catalog + Consumer Index + final consolidation (wave 2 parallel with 243-02). For every changed function from Section 1 and every changed interface method from Section 4.3 of the committed 243-01 deliverable, enumerate every downstream call site across the `contracts/` tree via grep-reproducible commands. Append Section 3 (Call-Site Catalog) + Section 6 (Consumer Index) + §7.3 (reproduction recipe). Mark the file READ-only.

Output: three in-place appends to `audit/v31-243-DELTA-SURFACE.md`:
1. Section 3 body populated with per-symbol call-site rows (`D-243-X###`), each row carrying the exact `grep` command that found it per D-18.
2. Section 6 body populated with the v31.0 requirement → 243 Row-ID mapping (`D-243-I###`) per D-07 item 6 + D-10.
3. §7.3 body populated with the aggregate grep replay recipe per D-18.
4. A top-of-file `## File Status` header added stating the file is READ-only per D-12 + D-21.

Requirement: DELTA-03 — enumerate every downstream call site of each changed function and interface across `contracts/` via reproducible grep.

Input contract: `audit/v31-243-DELTA-SURFACE.md` (committed from 243-01) provides Section 1 + 4 row list. If 243-02 has already committed, Section 2 classification rows are also present. The wave 2 ordering is: both 243-02 and 243-03 may proceed after 243-01 commits. If 243-02 is still pending when 243-03 runs, 243-03 reads Section 1 / 4 only (Section 2 not yet written) — which is fine, because 243-03's Section 3 catalog is keyed off Section 1 / 4 Row IDs, not Section 2 classification verdicts.

Parallelism: wave 2 alongside 243-02. No cross-dependency between 243-03 and 243-02 — each writes non-overlapping sections. Ownership: 243-03 owns Section 3 body + Section 6 body + §7.3 body + the top-of-file `## File Status` line. 243-02 owns Section 2 body + Section 1 change-count-card lines + §7.2 body. Section 1 row-data / 4 / 5 / 7.1 are READ-ONLY for 243-03 per CONTEXT.md D-21.

CRITICAL — concurrent edit safety: if 243-02 runs first, its commit is visible to 243-03 before Task 1 starts (243-03's Task 1 reads the latest HEAD of `audit/v31-243-DELTA-SURFACE.md`). If 243-03 runs first, its commit leaves 243-02's reserved markers (Section 2 / §7.2) intact for 243-02. Task 1 asserts the expected marker state.

Finalization: Task 3 of this plan adds the top-of-file `## File Status` READ-only note and commits. After this commit, `audit/v31-243-DELTA-SURFACE.md` is READ-only — Phase 244/245 scope-guard deferrals go in their own SUMMARY files, never re-editing this one (per D-21).

Finding-ID emission: NO `F-31-NN` IDs per D-20. Appends new finding candidates to Section 1.6 if any surface (e.g., a changed function with zero callers — candidate dead code).

Scope: READ-only per D-22. Zero `contracts/` or `test/` writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md
@.planning/milestones/v30.0-phases/237-vrf-consumer-inventory-call-graph/237-03-PLAN.md
@.planning/milestones/v29.0-phases/230-delta-extraction-scope-map/230-01-PLAN.md
@audit/v31-243-DELTA-SURFACE.md
</context>

<threat_model>
**Scope:** READ-only audit catalog production + final consolidation / READ-only lock. Zero runtime code modified, zero test modified.

**Writes confined to:** `audit/v31-243-DELTA-SURFACE.md` only (in-place append to Section 3 + Section 6 + §7.3 + top-of-file `## File Status` line + optional Section 1.6 candidate appends). Zero companion files (given 12-file delta surface and expected call-site grep output under 50 rows per symbol per CONTEXT.md D-18 companion-threshold — inline default).

**Threat surface introduced:** NONE (no contract code, no runtime behavior change).

**Regression / accuracy risk:**
- A missed call site propagates into Phase 244/245 blind spots (e.g., a caller of a MODIFIED_LOGIC function not re-examined against new semantics). Mitigated by CONTEXT.md D-18 grep-reproducibility (every row's grep command replayable to catch gaps) + D-21 scope-guard deferral rule (Phase 244/245 surface any missed call site in their own SUMMARY).
- A grep false-positive (e.g., matching a string literal that contains the function name) could inflate the catalog. Mitigated by the Call Type column requiring the executor to classify each match as direct/delegatecall/self-call/library — a comment-only match would be filtered out.
- Concurrent file edit with 243-02 could race. Mitigated by orchestrator serialization + Task 1 marker-state assertion.

Inherits parent phase threat model (READ-only milestone).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify 243-01 committed + read Section 1 / 4 universe + build symbol list for call-site grep sweep</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (committed from 243-01; may or may not have 243-02's Section 2 populated yet — both acceptable)
    - .planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md (D-07 item 6 Section 6 scope; D-08 Section 3 columns; D-10 3-plan wave topology; D-11 wave-2 parallelism; D-12 243-03 consolidation commit pattern; D-14 contracts/ only scope; D-15 interface drift scope; D-18 grep-reproducibility mandate; D-20 no F-31-NN; D-21 READ-only lock after 243-03 commits; D-22 no contracts/ or test/ writes)
    - .planning/REQUIREMENTS.md — full list of v31.0 REQ IDs for Section 6 Consumer Index scope (DELTA / EVT / RNG / QST / GOX / SDR / GOE / FIND / REG series — 44 IDs total)
    - .planning/milestones/v29.0-phases/230-delta-extraction-scope-map/230-01-PLAN.md — Phase 230 single-file catalog precedent (Consumer Index shape precedent)
    - .planning/milestones/v30.0-phases/237-vrf-consumer-inventory-call-graph/237-03-PLAN.md — Phase 237-03 call-graph + Consumer Index precedent (especially the Consumer Index table structure and the ALL / subset Row ID enumeration convention)
    - contracts/ tree at HEAD 771893d1 (no checkout — current working-tree IS head per baseline sanity gate). Specifically the call-site grep sweep reads ALL `.sol` files under `contracts/` EXCEPT `contracts/mocks/`, `contracts/test/`, `contracts/ContractAddresses.sol.bak` (per D-14).
  </read_first>
  <action>
Verify input integrity and build the symbol universe for the call-site grep sweep.

**Step A — Verify input prerequisites:**

```
test -f audit/v31-243-DELTA-SURFACE.md
git log --oneline -- audit/v31-243-DELTA-SURFACE.md | head -5   # show plan commits to date
git rev-parse 7ab515fe
git rev-parse 771893d1
git rev-parse HEAD   # MUST equal 771893d1 per D-03
git status --porcelain contracts/ test/   # MUST be empty
```

Confirm at least one `243-01` commit is present for `audit/v31-243-DELTA-SURFACE.md`. If 243-02 has also committed, that's fine — 243-03 tolerates both sequential (243-02 first) and cross-order (243-03 first) wave 2 topologies.

**Step B — Verify reserved-marker state:**

Check the file's current state of reserved markers:
```
# Section 2 marker state (243-02's territory):
grep -c '^## Section 2 — Aggregate Function Classification (RESERVED FOR 243-02)$' audit/v31-243-DELTA-SURFACE.md   # 1 if 243-02 not yet committed, 0 if 243-02 committed
grep -c '^## Section 2 — Aggregate Function Classification$' audit/v31-243-DELTA-SURFACE.md                         # 0 if 243-02 not committed, 1 if committed

# Section 3 / 6 / 7.3 marker state (243-03's own territory — MUST still be reserved):
grep -c '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md   # MUST be 1
grep -c '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md                 # MUST be 1
grep -c '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md   # MUST be 1
```

Record whether 243-02 has committed (affects whether Section 2 is consulted for Consumer Index classification-bucket subsets). If any of Section 3 / 6 / 7.3's `RESERVED FOR 243-03` markers is missing, STOP — the file is in an unexpected state.

**Step C — Extract the symbol universe for the call-site grep sweep:**

From Section 1 (between `## Section 1 — Per-Commit Changelog` and `## Section 2` headers), extract every row whose `Symbol Kind` is `func` or `modifier`. For each row, record:

```
{
  source_row_id: "D-243-C0NN",           // Section 1 row ID
  commit_sha: "<sha>",
  file: "contracts/<path>",
  symbol_name: "<fn-name>",
  head_line_range: "<start>-<end>",
  change_type: "<ADDED/REMOVED/MODIFIED/...>"
}
```

From Section 4.3 (interface methods), extract every interface-method row. Interface methods are also in scope per D-15 (they have call sites). For each row, record the same shape with `symbol_kind: "interface-method"` and file paths under `contracts/interfaces/`.

Note: skip state-vars / events / errors from the call-site sweep scope — DELTA-03 covers "every changed function and interface" per REQUIREMENTS.md text. State-var reads/writes are tracked in Section 4.1 + Section 5; event emits are enumerated via commit diffs in Section 1; custom errors don't have "call sites" in the grep sense.

Expected universe size: the Section 1 `func`/`modifier` row count + Section 4.3 interface-method row count. At least 11 from D-05 + the 8-path gate swap + other 771893d1 function changes. Realistic estimate: ~15-25 symbols.

**Step D — Pre-compute the grep invocation template per symbol:**

For each symbol, the grep command template is:
```
grep -rn --include='*.sol' '<symbol-name>\b' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/' | grep -v 'ContractAddresses.sol.bak'
```

Rationale for the command shape:
- `grep -rn`: recursive with line numbers (portable POSIX)
- `--include='*.sol'`: restrict to Solidity files (the `--include` flag IS POSIX-compatible in modern `grep`; if the agent's grep is strictly POSIX and lacks `--include`, fallback is `find contracts/ -name '*.sol' -exec grep -Hn '<symbol>\b' {} \;`)
- `'<symbol-name>\b'`: word boundary anchor to avoid matching longer identifiers that happen to contain the symbol as a substring
- Pipe-filter exclusions: explicit `grep -v` removes the D-14 out-of-scope paths (`contracts/mocks/`, `contracts/test/`, stale `.bak`)

For symbols with very short names that would explode grep output (e.g., `burn` — matches any mention of "burn" across the codebase), tighten the pattern to `<symbol>(` to match Solidity call-site syntax:
```
grep -rn --include='*.sol' '\.<symbol>(\|<symbol>(' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
```

For interface methods that are called via `IInterfaceName(target).methodName(...)`, also include self-call variants `IDegenerusGame(address(this)).methodName(...)`:
```
# e.g., for an IDegenerusGame method:
grep -rn --include='*.sol' 'IDegenerusGame(.*)\.<methodName>(' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
```

For delegatecall selectors (per D-15 `IDegenerusGameModules` interface scope), grep for the selector pattern:
```
grep -rn --include='*.sol' '\.<selectorName>\.selector' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
# also:
grep -rn --include='*.sol' 'abi\.encodeWithSelector([^,]*<methodName>' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
```

Each grep command is preserved verbatim in the `Grep Command Used` column for that symbol's call-site rows.

This Task 1 is READ-only preparation — no file writes. Output is an in-memory list of (symbol, grep-command, expected-match-path-constraints) tuples that Task 2 consumes to run the sweep and populate Section 3.
  </action>
  <verify>
    <automated>test -f audit/v31-243-DELTA-SURFACE.md && [ "$(grep -c '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^### 7.3 Plan 243-03 commands' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^| D-243-C' audit/v31-243-DELTA-SURFACE.md)" -ge 15 ] && git log --oneline -- audit/v31-243-DELTA-SURFACE.md | head -5 | grep -qE '243-01' && git rev-parse HEAD | grep -q '^771893d1' && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - `audit/v31-243-DELTA-SURFACE.md` exists with a 243-01 commit in its git log
    - Section 3 `RESERVED FOR 243-03` marker present (1x)
    - Section 6 `RESERVED FOR 243-03` marker present (1x)
    - §7.3 `RESERVED FOR 243-03` marker present (1x)
    - At least 15 `D-243-C` rows in Section 1
    - Current `git rev-parse HEAD` starts with `771893d1` (baseline anchor integrity per D-03)
    - `git status --porcelain contracts/ test/` empty
  </acceptance_criteria>
  <deliverable>
Input validation complete: 243-01 committed, baseline anchors valid, symbol universe extracted from Section 1 / 4.3 for the grep sweep, grep-invocation templates pre-computed per D-18 portable-POSIX convention.
  </deliverable>
  <done>Input integrity verified; symbol universe extracted; grep templates pre-computed; reserved markers for 243-03 territory intact; zero file writes.</done>
</task>

<task type="auto">
  <name>Task 2: Run call-site grep sweep + write Section 3 + write Section 6 Consumer Index + write §7.3 reproduction recipe + optional 1.6 candidate appends</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (entire file — Task 1's symbol list references Section 1 / 4 row IDs; Section 6 references v31.0 REQ IDs and any 243-02 classification-bucket subsets if 243-02 has already committed)
    - .planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md (D-07 item 6 Section 6 scope; D-08 Section 3 + Section 6 columns; D-14 contracts/ scope; D-15 interface drift + delegatecall scope; D-18 grep-reproducibility mandate; D-20 no F-31-NN; D-22 no contracts/ or test/ writes; Specifics note on companion-file threshold ~50 rows per symbol — expect inline default)
    - .planning/REQUIREMENTS.md — full REQ-ID list for Section 6 Consumer Index
    - contracts/**/*.sol at HEAD 771893d1 (all .sol files under contracts/ EXCEPT contracts/mocks/, contracts/test/, ContractAddresses.sol.bak per D-14) — the grep sweep target
  </read_first>
  <action>
Execute the DELTA-03 call-site grep sweep and write Sections 3 + 6 + §7.3 in place.

**Step A — Run the call-site grep sweep per symbol:**

For each symbol in the Task 1 universe (Section 1 `func`/`modifier` + Section 4.3 `interface-method`), run the pre-computed grep command and parse the output into call-site rows.

For each grep output line (format: `<file>:<line>:<code-line-content>`):
1. Parse: `{caller_file: <file>, caller_line: <line>, code_context: <content>}`.
2. Determine `Caller Function` by reading the surrounding context in `<file>` and finding the nearest enclosing `function <name>` header upstream of `<line>` (within the same contract scope).
3. Determine `Call Type`:
   - `direct` — the match is `<symbol>(...)` or `this.<symbol>(...)` where the caller is in the same file as the symbol's definition OR is a top-level function call on the same contract
   - `self-call` — the match is `IDegenerusGame(address(this)).<symbol>(...)` or equivalent self-interface-self-call pattern (used to cross module-delegatecall boundaries back to the top-level contract)
   - `delegatecall` — the match is inside an `abi.encodeWithSelector(...)` / `.<symbolName>.selector` pattern (module-to-module or top-level-to-module delegatecall)
   - `library` — the match is `<LibName>.<symbol>(...)` where `<LibName>` is a library import
   - `comment-only` — the match is inside a `//` / `/* */` comment OR inside a string literal (filter these out; they are not call sites)
4. If `Call Type = comment-only`, drop the row (do not emit).
5. If the symbol has zero non-comment matches, emit a SINGLE annotation row `NO CALLERS — candidate dead code` with `Caller File:Line = (none)`, `Caller Function = (none)`, `Call Type = (none)`, `Grep Command Used = <the command>`, and append a finding candidate to Section 1.6 with rationale "Changed function `<symbol>` has zero call sites in contracts/ at HEAD 771893d1 — candidate dead code for Phase 246 review."

Expected call-site volume: small. The delta touches 12 files with 140/57 lines of churn; most changed functions have 1-5 call sites (internal callers within the same module or self-calls from DegenerusGame.sol). Interface methods typically have 1-3 callers. Total Section 3 row count estimate: 30-80 rows.

**Step B — Replace Section 3 header + populate body:**

Locate the line `## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)` and replace the entire section body (down to the next `## Section 4` header, exclusive) with:

```markdown
## Section 3 — Downstream Call-Site Catalog

Per CONTEXT.md D-14: scope is `contracts/` tree ONLY. `contracts/mocks/`, `contracts/test/`, `test/`, `scripts/`, `deploy/`, and `contracts/ContractAddresses.sol.bak` (stale per `feedback_contract_locations.md`) are OUT of scope.

Per CONTEXT.md D-15: changed interface methods in `IDegenerusGame`, `IDegenerusQuests`, `IStakedDegenerusStonk` have call sites enumerated including self-calls via `IDegenerusGame(address(this))` pattern and delegatecall selectors in `IDegenerusGameModules`.

Per CONTEXT.md D-18 grep-reproducibility mandate: every row carries the exact `grep` command that found it. Portable POSIX syntax. Aggregate replay recipe in Section 7.3.

Row ID prefix: `D-243-X###` zero-padded monotonic.

Call Type vocabulary:
- `direct` — `<symbol>(...)` call inside the same contract or on `this`
- `self-call` — `IDegenerusGame(address(this)).<symbol>(...)` pattern crossing module-delegatecall boundaries
- `delegatecall` — `abi.encodeWithSelector(...)` / `.<symbol>.selector` pattern (module-to-module or top-to-module dispatch)
- `library` — `<LibName>.<symbol>(...)` library call

### 3.1 Per-Symbol Call-Site Catalog

For each changed symbol (Section 1 `func`/`modifier` rows + Section 4.3 `interface-method` rows), every call site enumerated below. Rows are grouped by symbol (one subsection per symbol). Within each symbol subsection, rows are sorted by `Caller File:Line` alphabetically then ascending line number.

#### 3.1.1 Symbol: <first-symbol-name> (from Section 1 row D-243-C0NN)

**Grep command (primary):** `grep -rn --include='*.sol' '<symbol-name>\b' contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'`

| Row ID | Changed Function | Caller File:Line | Caller Function | Call Type | Grep Command Used |
|---|---|---|---|---|---|
| D-243-X001 | `<symbol-name>` | contracts/<caller-path>:<line> | `<caller-fn>` | <direct/self-call/delegatecall/library> | `<the grep command>` |
| D-243-X002 | `<symbol-name>` | contracts/<caller-path>:<line> | `<caller-fn>` | <type> | `<grep>` |

[Additional rows per call site for this symbol.]

#### 3.1.2 Symbol: <second-symbol-name> (from Section 1 row D-243-C0NN)

[Same format, continuing Row ID numbering.]

[...repeat for every symbol from the universe...]

#### 3.1.N Symbol: <interface-method-name> (from Section 4.3 row D-243-C0NN)

**Grep commands (interface method — multiple patterns per D-15):**
- Direct: `grep -rn --include='*.sol' '\.<method>(' contracts/ ...`
- Self-call: `grep -rn --include='*.sol' 'IDegenerusGame(address(this))\.<method>(' contracts/ ...`
- Delegatecall selector: `grep -rn --include='*.sol' '\.<method>\.selector' contracts/ ...`

| Row ID | Changed Function | Caller File:Line | Caller Function | Call Type | Grep Command Used |
|---|---|---|---|---|---|
| D-243-X0NN | `<method-name>` | contracts/<caller-path>:<line> | `<caller-fn>` | self-call | `grep -rn --include='*.sol' 'IDegenerusGame(address(this))\.<method>(' contracts/ \| grep -v '^contracts/mocks/' \| grep -v '^contracts/test/'` |
| ... | ... | ... | ... | ... | ... |

[...repeat for every changed interface method...]

### 3.2 Symbols With Zero Callers (Candidate Dead Code)

| Row ID | Changed Function | Grep Command Used | Finding-Candidate Section 1.6 Ref |
|---|---|---|---|
| D-243-X0NN | `<symbol-name>` | `<grep command>` | Section 1.6 bullet N |

If no symbols are in this bucket, write literally "None. Every changed function has at least one caller in contracts/ at HEAD 771893d1."

### 3.3 Call-Site Catalog Summary

| Metric | Value |
|---|---|
| Total changed symbols in scope (Section 1 func/modifier + Section 4.3 interface-method) | N |
| Total call sites found across `contracts/` | N |
| Unique caller files | N |
| Symbols with zero callers (dead code candidates) | N |
| `direct` call-type rows | N |
| `self-call` call-type rows | N |
| `delegatecall` call-type rows | N |
| `library` call-type rows | N |

Call-Type row counts must sum to the Total call sites value (excluding the zero-caller annotation rows).
```

Replace every `<…>` placeholder with concrete data. No placeholders remain.

If any symbol's call-site list exceeds 50 rows (very unlikely given the 12-file delta surface), move that symbol's rows to a companion file `audit/v31-243-CALLSITES-<symbol-slug>.md` per CONTEXT.md Specifics soft threshold, and reference the companion file from Section 3.1's subsection for that symbol. Default is inline.

**Step C — Replace Section 6 header + populate body:**

Locate the line `## Section 6 — Consumer Index (RESERVED FOR 243-03)` and replace the entire section body (down to the next `## Section 7` header, exclusive) with:

```markdown
## Section 6 — Consumer Index

Per CONTEXT.md D-07 item 6 + D-10: maps every v31.0 requirement ID (44 total across DELTA / EVT / RNG / QST / GOX / SDR / GOE / FIND / REG series) to the subset of Phase 243 Row IDs it will cite. Saves Phase 244-246 planners lookup work.

Row IDs use prefix `D-243-I###` zero-padded monotonic. One row per v31.0 requirement ID.

Scope subset vocabulary per row (explicit — no hand-waves):
- `ALL-SECTION-1` — every `D-243-C###` Section 1 changelog row (used by DELTA-01-closing REQ)
- `ALL-SECTION-2` — every `D-243-F###` Section 2 classification row (used by DELTA-02-closing REQ)
- `ALL-SECTION-3` — every `D-243-X###` Section 3 call-site row (used by DELTA-03-closing REQ)
- `<explicit comma-separated Row ID list>` — specific subset (used by per-commit REQs that only scope one commit's delta)
- `NONE` — requirement does not map to any 243 row (e.g., REG-02 is regression appendix; its scope is prior-milestone findings, not 243 rows — but F-29-04 RE_VERIFIED_AT_HEAD cross-ref does reference the C### rows touching the F-29-04 surface)

### 6.1 v31.0 Requirement → 243 Row-ID Mapping

| Row ID | REQ-ID | Phase | Title Fragment | 243 Row-ID Subset | Rationale |
|---|---|---|---|---|---|
| D-243-I001 | DELTA-01 | 243 | Per-commit function/state/event inventory | `ALL-SECTION-1` | This REQ closes on the Section 1 + Section 4 universe itself. |
| D-243-I002 | DELTA-02 | 243 | 5-bucket function classification | `ALL-SECTION-2` | This REQ closes on the Section 2 classification table itself. |
| D-243-I003 | DELTA-03 | 243 | Downstream call-site catalog | `ALL-SECTION-3` | This REQ closes on the Section 3 catalog itself. |
| D-243-I004 | EVT-01 | 244 | Every JackpotTicketWin emit non-zero scaled | <C### rows citing ced654df JackpotTicketWin emit sites + X### rows citing ticketCount-consuming call sites> | EVT-01 scopes ced654df's emit-path coverage |
| D-243-I005 | EVT-02 | 244 | New JackpotWhalePassWin emit | <C### + X### rows for JackpotWhalePassWin emit + its consumers> | EVT-02 scopes the new event |
| D-243-I006 | EVT-03 | 244 | TICKET_SCALE uniform scaling | <C### rows for ced654df scaling changes> | EVT-03 cross-references BAF + trait-matched paths |
| D-243-I007 | EVT-04 | 244 | Event NatSpec accuracy | <C### rows for ced654df NatSpec hunks> | EVT-04 scopes NatSpec content |
| D-243-I008 | RNG-01 | 244 | _unlockRng(day) removal safety | <C### row for 16597cac _unlockRng removal + F### verdict + X### call sites of the affected function> | RNG-01 scopes the 16597cac behavioral change |
| D-243-I009 | RNG-02 | 244 | rngLockedFlag AIRTIGHT invariant re-verify | <C### rows for 16597cac + 771893d1 rngRequestTime clearing + F### verdicts> + cross-ref to audit/v30-CONSUMER-INVENTORY.md INV-237 rows on rngLockedFlag | RNG-02 re-verifies the v30 invariant |
| D-243-I010 | RNG-03 | 244 | 16597cac reformat behavioral equivalence | <C### row for 16597cac reformat hunk + F### REFACTOR_ONLY verdict> | RNG-03 scopes the reformat |
| D-243-I011 | QST-01 | 244 | MINT_ETH gross spend | <C### rows for 6b3f4f3c DegenerusQuests.sol + MintModule ethMintSpendWei path + F### verdicts + X### call sites> | QST-01 scopes the quest credit path |
| D-243-I012 | QST-02 | 244 | Earlybird DGNRS gross spend | <same as QST-01 subset> | QST-02 shares the MINT_ETH surface |
| D-243-I013 | QST-03 | 244 | Affiliate fresh/recycled 20-25/5 split preserved | <C### rows for 6b3f4f3c affiliate-related hunks if any — likely zero direct; investigate in Phase 244> | QST-03 tests invariant preservation |
| D-243-I014 | QST-04 | 244 | _callTicketPurchase freshEth drop + ethFreshWei rename | <C### rows for 6b3f4f3c _callTicketPurchase + handlePurchase + F### verdicts + X### call sites> | QST-04 scopes the signature change |
| D-243-I015 | QST-05 | 244 | Gas savings claim (-142k/-153k/-76k WC) | NONE (Phase 244 gas-analysis scope, not 243 row scope) | QST-05 is repro-evidence work; 243 has no gas rows |
| D-243-I016 | GOX-01 | 244 | 8 purchase/claim paths gameOver → _livenessTriggered | <C### rows for 771893d1 MintModule + WhaleModule gate-swap paths (8 rows per D-05.7) + F### MODIFIED_LOGIC verdicts + X### call sites of each path> | GOX-01 scopes the 8-path union |
| D-243-I017 | GOX-02 | 244 | sDGNRS.burn/burnWrapped State-1 block | <C### rows for 771893d1 StakedDegenerusStonk.sol burn + burnWrapped + F### verdicts + X### call sites> | GOX-02 scopes the new revert path |
| D-243-I018 | GOX-03 | 244 | handleGameOverDrain pendingRedemptionEthValue subtraction | <C### for 771893d1 handleGameOverDrain + F### verdict + X### call sites> | GOX-03 scopes the drain subtraction |
| D-243-I019 | GOX-04 | 244 | _livenessTriggered VRF-dead 14-day grace | <C### for 771893d1 _livenessTriggered + F### verdict + X### call sites> | GOX-04 scopes the new branch |
| D-243-I020 | GOX-05 | 244 | _livenessTriggered day-math-first ordering | <same as GOX-04 subset — same function, different behavioral aspect> | GOX-05 is the ordering aspect |
| D-243-I021 | GOX-06 | 244 | _gameOverEntropy rngRequestTime clearing + _handleGameOverPath ordering | <C### for 771893d1 _gameOverEntropy + _handleGameOverPath + F### verdicts + X### call sites> | GOX-06 scopes two behavioral changes in one REQ |
| D-243-I022 | GOX-07 | 244 | DegenerusGameStorage.sol slot layout | `ALL-SECTION-5` — every `D-243-S###` row | GOX-07's sole scope input is Section 5 per D-16 |
| D-243-I023 | SDR-01 | 245 | sDGNRS redemption × gameover timing matrix | <C### rows for 771893d1 StakedDegenerusStonk.sol + DegenerusGame.sol + GameOverModule.sol drain-related + F### verdicts + X### call sites of all above> | SDR-01 is cross-cutting |
| D-243-I024 | SDR-02 | 245 | pendingRedemptionEthValue accounting exactness | <C### for 771893d1 pendingRedemptionEthValue-touching fns (handleGameOverDrain + sDGNRS redemption path) + F### verdicts> | SDR-02 scopes the accounting |
| D-243-I025 | SDR-03 | 245 | handleGameOverDrain subtracts full pendingRedemptionEthValue before split | <same as GOX-03 subset — same function, different invariant> | SDR-03 is the BEFORE-split aspect |
| D-243-I026 | SDR-04 | 245 | claimRedemption post-gameOver DOS-free | <C### rows for 771893d1 claimRedemption-touching fns if any + F### verdicts + X### call sites> | SDR-04 scopes the claim path |
| D-243-I027 | SDR-05 | 245 | Per-wei conservation across gameover timings | <cross-cutting — same as SDR-01 + SDR-03 union> | SDR-05 is the closure proof |
| D-243-I028 | SDR-06 | 245 | State-1 orphan-redemption window closed | <same as GOX-02 subset — sDGNRS.burn/burnWrapped> | SDR-06 is the orphan-window closure |
| D-243-I029 | SDR-07 | 245 | sDGNRS supply conservation | <C### for 771893d1 sDGNRS supply-touching fns + F### + X###> | SDR-07 scopes supply invariant |
| D-243-I030 | SDR-08 | 245 | _gameOverEntropy fallback substitution for VRF-pending redemptions (F-29-04 class) | <same as GOX-06 subset — _gameOverEntropy + _handleGameOverPath + cross-ref to audit/v30-CONSUMER-INVENTORY.md F-29-04 INV-237 rows> | SDR-08 scopes F-29-04 interaction |
| D-243-I031 | GOE-01 | 245 | F-29-04 RNG-consumer determinism RE_VERIFIED_AT_HEAD | cross-ref to audit/v30-CONSUMER-INVENTORY.md F-29-04 INV-237 rows + any 243 C### rows touching the same functions | GOE-01 is regression re-verification |
| D-243-I032 | GOE-02 | 245 | claimablePool 33/33/34 split + 30-day sweep | <C### for 771893d1 handleGameOverDrain + any claimablePool-related hunks + F### verdicts> | GOE-02 intersects with SDR-03 |
| D-243-I033 | GOE-03 | 245 | Purchase-blocking entry-point coverage | <same as GOX-01 subset — 8 paths + additional entry points per Phase 245 scope> | GOE-03 updates v24.0 "10 entry points" |
| D-243-I034 | GOE-04 | 245 | VRF-available vs prevrandao fallback gameover-jackpot branches | <C### for 771893d1 _livenessTriggered 14-day grace + _gameOverEntropy + F### verdicts> + cross-ref to KI "Gameover prevrandao fallback" | GOE-04 re-verifies given the new grace |
| D-243-I035 | GOE-05 | 245 | gameOverPossible BURNIE endgame gate | <C### for any 771893d1 hunks that touch gameOverPossible if any — likely zero direct> + cross-ref to v11.0 finding | GOE-05 verifies no regression |
| D-243-I036 | GOE-06 | 245 | NEW cross-feature emergent behavior (liveness × sDGNRS × drain) | <cross-cutting — SDR-01 + GOX-01..06 union> | GOE-06 catches emergent edge cases |
| D-243-I037 | FIND-01 | 246 | Consolidated FINDINGS-v31.0.md | <cross-ref Section 1.6 Finding Candidates> | FIND-01 owns F-31-NN assignment from 243's candidate pool |
| D-243-I038 | FIND-02 | 246 | 5-bucket severity classification | <same as FIND-01 + Phase 244/245 candidates> | FIND-02 applies severity to FIND-01's set |
| D-243-I039 | FIND-03 | 246 | KNOWN-ISSUES.md 3-predicate gated updates | <same as FIND-01 filtered to accepted-design + non-exploitable + sticky> | FIND-03 is KI promotion filter |
| D-243-I040 | REG-01 | 246 | v30.0 F-30-NNN regression spot-check | cross-ref to Section 1.7 light reconciliation + audit/FINDINGS-v30.0.md | REG-01 uses 1.7 as its scope input |
| D-243-I041 | REG-02 | 246 | Prior finding supersession | cross-ref to Section 1.7 + audit/FINDINGS-v30.0.md / v29.0.md | REG-02 checks supersession |

[The total Consumer Index row count depends on the v31.0 REQ-ID list — 44 REQs per REQUIREMENTS.md, but some REQs genuinely NONE-map (QST-05 gas is a good example of NONE) and some pairs share a scope (GOX-03 + SDR-03 for the pendingRedemptionEthValue-before-split aspect). The executor fills in every REQ — zero gaps. Every row's `243 Row-ID Subset` column is either `ALL-SECTION-N`, a concrete comma-separated list, `NONE`, or an explicit `cross-ref to <path>` annotation. No `TBD` markers.]

### 6.2 Consumer Index Integrity Check

| Metric | Value |
|---|---|
| Total v31.0 REQ IDs | 44 |
| REQ IDs mapped | N |
| REQ IDs with `ALL-SECTION-N` subset | N |
| REQ IDs with explicit Row-ID list | N |
| REQ IDs with `NONE` subset | N |
| REQ IDs with `cross-ref to <external>` subset | N |
| REQ IDs not yet mapped (must be 0 to close DELTA-03) | 0 |

If `REQ IDs not yet mapped > 0`, this section is incomplete. Executor MUST resolve every REQ before committing.
```

Replace every `<…>` placeholder with concrete Row ID lists or `NONE` / `cross-ref to <path>`. For `ALL-SECTION-N` subsets, write the literal phrase (no expansion needed since the subset is definitionally all rows of that prefix).

The REQ ID list above is pre-populated from REQUIREMENTS.md — executor re-verifies against the actual REQUIREMENTS.md content before writing, in case REQ-ID list is modified between plan-write time and execute time.

**Step D — Replace §7.3 header + populate body:**

Locate the line `### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03` and replace the section body (down to the next `## ` or file EOF) with:

```markdown
### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog)

**Per-symbol grep sweep (primary pattern — POSIX portable):**

```bash
# Template (run once per changed symbol):
SYMBOL='<symbol-name>'
grep -rn --include='*.sol' "\b${SYMBOL}\b" contracts/ \
  | grep -v '^contracts/mocks/' \
  | grep -v '^contracts/test/' \
  | grep -v 'ContractAddresses\.sol\.bak'

# For symbols with short names prone to substring hits (e.g., "burn", "mint"):
grep -rn --include='*.sol' "\\.${SYMBOL}(\|${SYMBOL}(" contracts/ \
  | grep -v '^contracts/mocks/' \
  | grep -v '^contracts/test/'
```

**Interface-method call-site sweep (D-15 scope — three patterns per method):**

```bash
METHOD='<method-name>'
IFACE='IDegenerusGame'    # or IDegenerusQuests / IStakedDegenerusStonk

# Pattern 1: direct interface-type call
grep -rn --include='*.sol' "\\.${METHOD}(" contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'

# Pattern 2: self-call via IDegenerusGame(address(this)).method(...)
grep -rn --include='*.sol' "${IFACE}(address(this))\\.${METHOD}(" contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'

# Pattern 3: delegatecall selector via abi.encodeWithSelector / .selector
grep -rn --include='*.sol' "\\.${METHOD}\\.selector" contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
grep -rn --include='*.sol' "abi\\.encodeWithSelector([^,)]*${METHOD}" contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/'
```

**Comment/string-literal filtering (post-grep heuristic — not a shell filter):**

Executor reviews each grep output line's content and drops the row if the match is inside a `//` comment (line begins with leading whitespace + `//` before the pattern) or a `/* */` block comment (track nesting across lines) or a string literal (pattern between `"..."`). These are NOT call sites and do not emit Section 3 rows.

**Full-phase replay recipe (concatenates §7.1 + §7.2 + §7.3):**

```bash
# To replay the entire Phase 243 DELTA-01 + DELTA-02 + DELTA-03 enumeration:
#   1. Run §7.1 commands to reproduce Sections 0 + 1 + 4 + 5.
#   2. Run §7.2 commands to reproduce Section 2 classification verdicts.
#   3. Run §7.3 commands (below) to reproduce Section 3 call-site catalog.

# Confirm baseline integrity:
git diff --stat 7ab515fe..771893d1 -- contracts/    # expect: 12 files / 140 insertions / 57 deletions

# Per-symbol sweep (one iteration per D-243-C### func/modifier row + D-243-C### interface-method row):
for SYMBOL in \
  <list every symbol-name from Section 1 func/modifier + Section 4.3 interface-method, space-separated or newline-separated>
do
  echo "=== $SYMBOL ==="
  grep -rn --include='*.sol' "\b${SYMBOL}\b" contracts/ | grep -v '^contracts/mocks/' | grep -v '^contracts/test/' | grep -v 'ContractAddresses\.sol\.bak'
done
```

Commands use portable POSIX (grep / sed). GNU-only flags avoided. `--include='*.sol'` is widely supported in modern grep; if agent's grep lacks it, fallback pattern: `find contracts/ -name '*.sol' ! -path 'contracts/mocks/*' ! -path 'contracts/test/*' -exec grep -Hn "\b<symbol>\b" {} \;`.
```

Replace `<list every symbol-name ...>` with the actual concrete list of symbol names extracted from Section 1 / 4.3.

**Step E — Add the top-of-file `## File Status` READ-only marker:**

Find the file's front matter end (after the closing `---`) and the first `# v31.0 Phase 243 — Delta Surface Catalog` header. Insert a new subsection `## File Status` immediately below the top-of-file H1, BEFORE the existing `## Section 0 — Overview` header:

```markdown
# v31.0 Phase 243 — Delta Surface Catalog

**Status:** FINAL — READ-only per CONTEXT.md D-21. Any Phase 244/245 delta/gap beyond this catalog is recorded as a scope-guard deferral in the discovering plan's own SUMMARY.md — this file is NOT re-edited.

**Audit baseline:** `7ab515fe` (v30.0 milestone HEAD; tag `v30.0`).
**Audit head:** `771893d1` (v31.0 milestone start HEAD).
**Phase:** 243 — Delta Extraction & Per-Commit Classification (DELTA-01 / DELTA-02 / DELTA-03).
**Scope:** READ-only per CONTEXT.md D-22. Zero `contracts/` or `test/` writes.
**Status:** FINAL — 243-01 + 243-02 + 243-03 all consolidated in this file.

## Section 0 — Overview & Row-ID Legend
```

Specifically: the 243-01 commit wrote a `**Status:** IN PROGRESS — Plan 243-01 populates Sections 0 / 1 / 4 / 5 / 7. Sections 2 / 3 / 6 are placeholder stubs reserved for Plans 243-02 (classification) and 243-03 (call-site catalog + Consumer Index + final READ-only lock).` line. This plan REPLACES that line with the `FINAL — READ-only ...` line above. Do NOT add a duplicate status — replace.

**Step F — Append any new Finding Candidates to Section 1.6:**

If the call-site sweep surfaced new candidates (zero-caller symbols = dead code candidates; unexpected call sites in out-of-scope files indicating 243-01 missed a file; delegatecall selectors with no matching impl in `IDegenerusGameModules` indicating interface drift), append to Section 1.6. Format same as 243-02:
`- contracts/<path>:<line> — <symbol> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH> — surfaced in 243-03`.

If 243-01 and 243-02 both wrote "None surfaced" literals, and 243-03 has candidates, replace those literals with an actual bullet list. If 243-03 has no candidates and prior plans had none, leave Section 1.6 untouched.

After writing, verify:
```
grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR 243-03' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index$' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^\*\*Status:\*\* FINAL — READ-only per CONTEXT.md D-21' audit/v31-243-DELTA-SURFACE.md
grep -qE '^\| D-243-X[0-9]{3} \|' audit/v31-243-DELTA-SURFACE.md
grep -qE '^\| D-243-I[0-9]{3} \|' audit/v31-243-DELTA-SURFACE.md
grep -q 'grep -rn' audit/v31-243-DELTA-SURFACE.md
# IN-PROGRESS status line removed:
! grep -q 'Status:\*\* IN PROGRESS' audit/v31-243-DELTA-SURFACE.md
git status --porcelain contracts/ test/   # MUST be empty
```

Do NOT commit in this task — Task 3 commits.
  </action>
  <verify>
    <automated>grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v31-243-DELTA-SURFACE.md && grep -q '^## Section 6 — Consumer Index$' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog)$' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'RESERVED FOR 243-03' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 3.1 Per-Symbol Call-Site Catalog' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 3.2 Symbols With Zero Callers' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 3.3 Call-Site Catalog Summary' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 6.1 v31.0 Requirement' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 6.2 Consumer Index Integrity Check' audit/v31-243-DELTA-SURFACE.md && grep -qE '^\| D-243-X[0-9]{3} \|' audit/v31-243-DELTA-SURFACE.md && grep -qE '^\| D-243-I[0-9]{3} \|' audit/v31-243-DELTA-SURFACE.md && grep -q 'DELTA-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'DELTA-02' audit/v31-243-DELTA-SURFACE.md && grep -q 'DELTA-03' audit/v31-243-DELTA-SURFACE.md && grep -q 'EVT-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'RNG-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'QST-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'GOX-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'GOX-07' audit/v31-243-DELTA-SURFACE.md && grep -q 'SDR-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'GOE-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'FIND-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'REG-01' audit/v31-243-DELTA-SURFACE.md && grep -q 'grep -rn' audit/v31-243-DELTA-SURFACE.md && grep -q '^\*\*Status:\*\* FINAL — READ-only per CONTEXT.md D-21' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'Status:\*\* IN PROGRESS' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md && ! grep -qE '<(symbol|caller-path|caller-fn|fn|path|line|type|grep|method|method-name|second-symbol-name|first-symbol-name|interface-method-name)[>-]' audit/v31-243-DELTA-SURFACE.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - `audit/v31-243-DELTA-SURFACE.md` contains literal header `## Section 3 — Downstream Call-Site Catalog` (WITHOUT `(RESERVED FOR 243-03)` suffix)
    - Section 3 subheaders present: `### 3.1 Per-Symbol Call-Site Catalog`, `### 3.2 Symbols With Zero Callers (Candidate Dead Code)`, `### 3.3 Call-Site Catalog Summary`
    - §3.1 contains at least one `#### 3.1.N Symbol: <name>` subsection per changed `func`/`modifier` row and per changed interface-method row from Section 4.3
    - Section 3 contains at least one `D-243-X` row (zero-caller dead-code case would still emit a §3.2 `D-243-X` annotation row)
    - Every Section 3 row has Call Type ∈ `{direct, self-call, delegatecall, library}` (or `(none)` for zero-caller annotation rows)
    - Every Section 3 row has a non-empty `Grep Command Used` column containing literal `grep -rn`
    - `## Section 6 — Consumer Index` header present (WITHOUT `(RESERVED FOR 243-03)` suffix)
    - Section 6 subheaders: `### 6.1 v31.0 Requirement → 243 Row-ID Mapping`, `### 6.2 Consumer Index Integrity Check`
    - Section 6.1 contains at least one `D-243-I` row per v31.0 REQ-ID series (all 9 series covered: DELTA, EVT, RNG, QST, GOX, SDR, GOE, FIND, REG) — verified by grep assertions on DELTA-01, EVT-01, RNG-01, QST-01, GOX-01, GOX-07, SDR-01, GOE-01, FIND-01, REG-01
    - Section 6.2 `REQ IDs not yet mapped` reports 0
    - `### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog)` header present (WITHOUT `— RESERVED FOR 243-03` suffix)
    - §7.3 body contains grep template `grep -rn --include='*.sol'` + filter pipe `grep -v '^contracts/mocks/'` + full-phase replay recipe
    - Top-of-file status line `**Status:** FINAL — READ-only per CONTEXT.md D-21` present
    - The prior `**Status:** IN PROGRESS` line from 243-01 is removed (replaced)
    - Zero `F-31-NN` references
    - Zero placeholder tokens `<symbol-name>`, `<caller-path>`, `<caller-fn>`, `<fn>`, `<path>`, `<line>`, `<type>`, `<grep>`, `<method>`, `<method-name>`, `<first-symbol-name>`, `<second-symbol-name>`, `<interface-method-name>`, `<list every symbol-name ...>` remain
    - `git status --porcelain contracts/ test/` empty
  </acceptance_criteria>
  <deliverable>
Section 3 populated (call-site catalog with grep-reproducible rows) + Section 6 populated (Consumer Index mapping all 44 v31.0 REQ IDs to 243 Row-ID subsets) + §7.3 populated (grep replay recipe + full-phase replay) + top-of-file `## File Status` FINAL/READ-only marker added. File ready for commit as the final 243 deliverable.
  </deliverable>
  <done>Section 3 populated with per-symbol call-site rows + §3.2 dead-code annotations + §3.3 summary; Section 6 populated with D-243-I rows covering all 9 v31.0 REQ series; §7.3 populated with grep replay; top-of-file status flipped from IN PROGRESS to FINAL READ-only; zero F-31-NN IDs; zero placeholders; zero contracts/ or test/ writes.</done>
</task>

<task type="auto">
  <name>Task 3: Commit 243-03 updates + lock audit/v31-243-DELTA-SURFACE.md as READ-only</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (Task 2 populated Section 3 + Section 6 + §7.3 + top-of-file status — must exist and not yet be committed for THIS plan)
  </read_first>
  <action>
Final sanity gate before committing Plan 243-03 deliverable (the terminal commit for Phase 243).

**Final sanity gate:**
```
test -f audit/v31-243-DELTA-SURFACE.md
! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md
# Zero RESERVED markers of any kind (all three plans' markers replaced):
! grep -q 'RESERVED FOR 243-01' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR 243-02' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR 243-03' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK' audit/v31-243-DELTA-SURFACE.md
# All 8 sections present and populated (no placeholder stubs):
grep -q '^## Section 0 — Overview & Row-ID Legend' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 1 — Per-Commit Changelog' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 2 — Aggregate Function Classification$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 4 — State Variable / Event / Interface Inventory' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 5 — Storage Slot Layout Diff (DegenerusGameStorage.sol)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 7 — Reproduction Recipe Appendix$' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.1 Plan 243-01 commands' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.2 Plan 243-02 commands' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.3 Plan 243-03 commands' audit/v31-243-DELTA-SURFACE.md
# Row ID coverage per prefix:
[ "$(grep -c '^| D-243-C' audit/v31-243-DELTA-SURFACE.md)" -ge 15 ]
[ "$(grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md)" -ge 11 ]
[ "$(grep -c '^| D-243-S' audit/v31-243-DELTA-SURFACE.md)" -ge 1 ]
[ "$(grep -c '^| D-243-X' audit/v31-243-DELTA-SURFACE.md)" -ge 1 ]
[ "$(grep -c '^| D-243-I' audit/v31-243-DELTA-SURFACE.md)" -ge 30 ]
# Top-of-file FINAL status:
grep -q '^\*\*Status:\*\* FINAL — READ-only per CONTEXT.md D-21' audit/v31-243-DELTA-SURFACE.md
# No stale IN-PROGRESS status:
! grep -q 'Status:\*\* IN PROGRESS' audit/v31-243-DELTA-SURFACE.md
# Baseline anchor integrity:
test -z "$(git status --porcelain contracts/ test/)"
[ "$(git diff --name-only 7ab515fe..HEAD -- contracts/ | wc -l)" = "12" ]
git rev-parse HEAD | grep -q '^771893d1'
```

If ANY check fails, STOP and diagnose before committing.

**Commit:**
```
git add audit/v31-243-DELTA-SURFACE.md
git commit -m "docs(243-03): DELTA-03 call-site catalog + Consumer Index + FINAL READ-only lock at HEAD 771893d1

Plan 243-03 Wave 2 deliverable + Phase 243 terminal commit. Completes the
consolidation of audit/v31-243-DELTA-SURFACE.md per CONTEXT.md
D-07/D-08/D-10/D-12/D-14/D-15/D-18/D-21.

Section 3 populated:
  - 3.1 Per-Symbol Call-Site Catalog — every changed func/modifier from
    Section 1 and every changed interface-method from Section 4.3 has a
    subsection enumerating callers across contracts/ tree (D-14 scope;
    mocks/test/deploy/scripts excluded). Every row carries its exact grep
    command per D-18.
  - 3.2 Symbols With Zero Callers — dead-code candidates flagged +
    cross-ref to Section 1.6 Finding Candidates.
  - 3.3 Call-Site Catalog Summary — per-Call-Type row-count breakdown.

Section 6 populated:
  - 6.1 v31.0 Requirement → 243 Row-ID Mapping — every REQ-ID (DELTA /
    EVT / RNG / QST / GOX / SDR / GOE / FIND / REG — 44 total) mapped to
    its 243 Row-ID subset (ALL-SECTION-N / explicit list / NONE / cross-ref).
  - 6.2 Consumer Index Integrity — REQ IDs not yet mapped MUST = 0.

Section 7.3 populated with grep template + interface-method tri-pattern
sweep + comment/string filtering heuristic + full-phase replay recipe
concatenating 7.1 + 7.2 + 7.3 commands.

Top-of-file status flipped: IN PROGRESS → FINAL — READ-only per D-21.
Any Phase 244/245 scope-guard deferrals go in their own plan SUMMARYs,
NOT re-edits of this file.

Prefixes finalized: D-243-C### (Section 1+4), D-243-F### (Section 2),
D-243-S### (Section 5), D-243-X### (Section 3), D-243-I### (Section 6).

Phase 243 COMPLETE: DELTA-01 (243-01) + DELTA-02 (243-02) + DELTA-03
(this plan) satisfied. Single authoritative file for Phases 244-246
downstream.

NO F-31-NN IDs (D-20 — Phase 246 FIND-01..03 owns).
READ-only per D-22 — zero contracts/ or test/ writes.

Baseline anchor: 7ab515fe (D-03). Head anchor: 771893d1 (D-03)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE '243-03.*(DELTA-03|call-site|FINAL).*771893d1' && test -z "$(git status --porcelain contracts/ test/)" && ! grep -q 'RESERVED FOR' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md && grep -q '^\*\*Status:\*\* FINAL — READ-only per CONTEXT.md D-21' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'Status:\*\* IN PROGRESS' audit/v31-243-DELTA-SURFACE.md && [ "$(grep -c '^| D-243-C' audit/v31-243-DELTA-SURFACE.md)" -ge 15 ] && [ "$(grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md)" -ge 11 ] && [ "$(grep -c '^| D-243-S' audit/v31-243-DELTA-SURFACE.md)" -ge 1 ] && [ "$(grep -c '^| D-243-X' audit/v31-243-DELTA-SURFACE.md)" -ge 1 ] && [ "$(grep -c '^| D-243-I' audit/v31-243-DELTA-SURFACE.md)" -ge 30 ] && [ "$(git diff --name-only 7ab515fe..HEAD -- contracts/ | wc -l)" = "12" ]</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `243-03` + (`DELTA-03` OR `call-site` OR `FINAL`) + `771893d1`
    - Commit touches only `audit/v31-243-DELTA-SURFACE.md`
    - Commit body references CONTEXT.md decisions D-07, D-08, D-10, D-12, D-14, D-15, D-18, D-21, D-22
    - `git status --porcelain contracts/ test/` empty
    - Zero `RESERVED FOR` markers (of any plan / task) remain in the file — final consolidation complete
    - Top-of-file status line matches `**Status:** FINAL — READ-only per CONTEXT.md D-21`
    - No stale `**Status:** IN PROGRESS` line remains
    - All 5 Row-ID prefixes represented: `D-243-C` (≥ 15 rows), `D-243-F` (≥ 11 rows), `D-243-S` (≥ 1 row), `D-243-X` (≥ 1 row), `D-243-I` (≥ 30 rows — at least ~70% of the 44 REQ IDs mapped, with the remaining explicitly `NONE` per REQ nature)
    - `git diff --name-only 7ab515fe..HEAD -- contracts/` returns exactly 12 files (baseline integrity preserved across all 3 plan commits)
    - `git rev-parse HEAD` starts with `771893d1`
    - Zero `F-31-NN` references
    - Not pushed
  </acceptance_criteria>
  <deliverable>
`audit/v31-243-DELTA-SURFACE.md` FINALIZED and committed. Phase 243 terminal commit — DELTA-01 + DELTA-02 + DELTA-03 all closed. Single authoritative v31.0 delta-surface catalog ready as sole scope input for Phase 244 (per-commit adversarial audit), Phase 245 (sDGNRS + gameover safety), Phase 246 (findings consolidation). File is READ-only per D-21; downstream phases record scope-guard deferrals in their own SUMMARYs.
  </deliverable>
  <done>Commit exists; contracts/ test/ unmodified; file status flipped to FINAL READ-only; zero RESERVED markers; all 8 sections populated with their respective Row-ID prefixes present; Consumer Index covers all 44 v31.0 REQ IDs; zero F-31-NN IDs; baseline integrity preserved (12 M-status files); not pushed.</done>
</task>

</tasks>

<verification>
- `audit/v31-243-DELTA-SURFACE.md` Section 3 populated in place (literal header without RESERVED suffix)
- §3.1 has one `#### 3.1.N Symbol:` subsection per changed func/modifier + interface-method from Section 1/4.3
- Every Section 3 row has Call Type ∈ {direct, self-call, delegatecall, library} or `(none)` for zero-caller annotation rows
- Every Section 3 row has non-empty `Grep Command Used` column (contains `grep -rn`)
- §3.2 Symbols With Zero Callers present (may be "None" or actual dead-code candidate rows)
- §3.3 Call-Site Catalog Summary present with per-Call-Type row counts
- Section 6 populated in place (literal header without RESERVED suffix)
- §6.1 covers all 9 v31.0 REQ-ID series (DELTA / EVT / RNG / QST / GOX / SDR / GOE / FIND / REG) via grep assertions
- §6.1 every row has `243 Row-ID Subset` column populated (ALL-SECTION-N / explicit list / NONE / cross-ref — zero TBDs)
- §6.2 Consumer Index Integrity Check reports REQ IDs not yet mapped = 0
- §7.3 populated with grep templates, interface-method tri-pattern sweep, comment filtering heuristic, full-phase replay recipe
- Top-of-file status: `**Status:** FINAL — READ-only per CONTEXT.md D-21` present
- Zero stale `**Status:** IN PROGRESS` lines remain
- Zero `RESERVED FOR` markers of any kind (all three plans consolidated)
- Zero `F-31-NN` references (D-20)
- Zero placeholder tokens remaining
- All 5 Row-ID prefixes present in the file (C / F / S / X / I)
- `git status --porcelain contracts/ test/` empty (D-22)
- `git diff --name-only 7ab515fe..HEAD -- contracts/` returns 12 files (baseline integrity across all 3 plan commits)
- Committed locally; not pushed
</verification>

<success_criteria>
DELTA-03 satisfied: every changed function (Section 1 func/modifier rows) and every changed interface method (Section 4.3 interface-method rows) has its downstream call sites across `contracts/` enumerated via grep-reproducible rows; every row carries the exact grep command per D-18; zero callers reachable are unaccounted for (out-of-scope paths per D-14 are explicitly excluded via grep-v filter chain, not silently omitted).

Section 6 Consumer Index satisfied per D-07 item 6 + D-10: all 44 v31.0 REQ-IDs have a 243 Row-ID subset (ALL-SECTION / explicit list / NONE / cross-ref) — Phase 244 (EVT/RNG/QST/GOX), Phase 245 (SDR/GOE), and Phase 246 (FIND/REG) planners inherit their scope-anchor subsets without additional discovery.

Final consolidation per D-12 + D-21: `audit/v31-243-DELTA-SURFACE.md` is FINAL and READ-only. The file combines 243-01's DELTA-01 universe + 243-02's DELTA-02 classification + 243-03's DELTA-03 call-site catalog into a single authoritative catalog. Downstream scope-guard deferrals (per D-21) go in the discovering plan's own SUMMARY.md — this file is not re-edited.

Phase 243 COMPLETE: all three requirements (DELTA-01 / DELTA-02 / DELTA-03) closed at HEAD `771893d1` against baseline `7ab515fe`. Phase 244's sole scope input locked. READ-only discipline preserved across all three plan commits (`git diff --name-only 7ab515fe..HEAD -- contracts/` returns the same 12 M-status files the baseline diff showed).
</success_criteria>

<output>
After completion, create `.planning/phases/243-delta-extraction-per-commit-classification/243-03-SUMMARY.md`
</output>
</content>
