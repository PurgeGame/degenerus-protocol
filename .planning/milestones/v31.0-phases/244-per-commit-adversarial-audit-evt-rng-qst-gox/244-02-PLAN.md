---
phase: 244-per-commit-adversarial-audit-evt-rng-qst-gox
phase_number: 244
plan: 244-02
plan_number: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v31-244-RNG.md
autonomous: true
requirements: [RNG-01, RNG-02, RNG-03]
requirements_addressed: [RNG-01, RNG-02, RNG-03]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: cc68bfc7
head_sha: cc68bfc7
head_anchor: cc68bfc7
in_scope_commits:
  - sha: 16597cac
    summary: "rngunlock fix — _unlockRng(day) removed from two-call-split ETH continuation in advanceGame; +6/-6 net (removal + two multi-line SLOAD/destructuring reformats)"
    drives_reqs: [RNG-01, RNG-02, RNG-03]
in_scope_243_rows:
  changelog:    [D-243-C007, D-243-C016]
  classification: [D-243-F006, D-243-F014]
  call_sites:   [D-243-X011, D-243-X012, D-243-X013, D-243-X014, D-243-X027]
  consumer_index: [D-243-I008, D-243-I009, D-243-I010]
  reconciliation: ["INV-237-021", "INV-237-022", "INV-237-023", "INV-237-024", "INV-237-025", "INV-237-026", "INV-237-027", "INV-237-028", "INV-237-029", "INV-237-030", "INV-237-031", "INV-237-032", "INV-237-033", "INV-237-034", "INV-237-035 (HUNK-ADJACENT)", "INV-237-036", "INV-237-037", "INV-237-059 (HUNK-ADJACENT — _gameOverEntropy reentry adjacency)"]
  finding_candidates_137: ["bullet 3 (_gameOverEntropy rngRequestTime clearing reentry)", "bullet 8 (cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall reentrancy parity — cross-cited with 244-04 GOX-06)"]
ki_envelope_re_verify:
  - exception: "EXC-02 (Gameover prevrandao fallback `_getHistoricalRngFallback`)"
    re_verify_in_req: RNG-02
    rationale: "_gameOverEntropy rngRequestTime clearing must not widen the EXC-02 envelope (prevrandao consumption must remain limited to gameOver-fallback only, no leak into normal-path)"
  - exception: "EXC-03 (Gameover RNG substitution for mid-cycle write-buffer tickets — F-29-04)"
    re_verify_in_req: RNG-01
    rationale: "_unlockRng(day) removal from advanceGame must not regress the F-29-04 mid-cycle substitution invariant (ticket-buffer swap timing must remain unaffected)"
must_haves:
  truths:
    - "audit/v31-244-RNG.md exists with frontmatter `Audit baseline: 7ab515fe`, `Audit head: cc68bfc7`, `Owning commits: 16597cac (+ KI envelope re-verify against EXC-02/EXC-03)`."
    - "Section §RNG-01 verdict table enumerates EVERY reaching path that lands at the post-`payDailyJackpot(true, lvl, rngWord)` continuation in advanceGame after `_unlockRng(day)` removal; for each path the next `rngLocked = false` SSTORE is identified and proven to be reached on the SAME tick (no cross-tick reachability)."
    - "Section §RNG-01 includes 4-actor adversarial closure (player / admin / validator / VRF oracle per Phase 238 D-07 carry) — verdict per actor class verifies no actor reaches a path where rngLocked stays set past the tick."
    - "Section §RNG-01 cross-cites §1.8 INV-237-035 HUNK-ADJACENT row (line 450 — exactly where `_unlockRng(day)` was removed) and §1.7 bullet 3 (`_gameOverEntropy` rngRequestTime clearing reentry adjacency)."
    - "Section §RNG-02 records the v30.0 rngLockedFlag AIRTIGHT invariant statement verbatim and proves RE_VERIFIED_AT_HEAD `cc68bfc7` (no double-set, no set-without-clear, no clear-without-matching-set) — annotation per Phase 238 D-10 pattern."
    - "Section §RNG-02 cites Phase 239 RNG-01..03 result + INV-237-021..037 cross-citation per CONTEXT.md D-11 RNG-02 vector (b)+(c); confirms `_gameOverEntropy` rngRequestTime clearing per §1.7 bullet 3 does NOT break AIRTIGHT invariant (vector RNG-02d)."
    - "Section §RNG-02 includes commitment-window check per project skill `feedback_rng_commitment_window.md` — every audit row checks what player-controllable state can change between VRF request and fulfillment; for the `_unlockRng(day)` removal + the new rngRequestTime clearing, verdict states whether commitment window widened, narrowed, or held."
    - "Section §RNG-01 includes backward-trace methodology per project skill `feedback_rng_backward_trace.md` — the post-removal continuation point is treated as a CONSUMER, traced backward to verify rngWord was unknown at every input commitment time (no path lets a player commit input AFTER rngWord is observable)."
    - "Section §RNG-03 records side-by-side prose diff (NOT bytecode-diff per CONTEXT.md D-17) for the `16597cac` multi-line SLOAD + tuple destructuring reformats; names specific elements proven byte-equivalent (no SSTORE order change, no branch added, no return-path evaluation drift) per Phase 243 D-04 REFACTOR_ONLY definition + D-19 evidence burden."
    - "KI envelope re-verify sub-section: EXC-02 (prevrandao fallback) re-verify under the new `_gameOverEntropy` rngRequestTime clearing — verdict states whether the clearing leaks prevrandao consumption past the gameOver-fallback boundary (expected: no leak, RE_VERIFIED_AT_HEAD); EXC-03 (F-29-04 mid-cycle substitution) re-verify under the `_unlockRng(day)` removal — verdict states whether the removal regresses F-29-04 mid-cycle substitution (expected: no regression, RE_VERIFIED_AT_HEAD)."
    - "Every verdict row carries 8 columns per CONTEXT.md D-06."
    - "Verdict bucket per row drawn from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08 (D-22 KI exceptions get RE_VERIFIED_AT_HEAD annotation, NOT a verdict bucket — they are envelope-non-widening checks)."
    - "Verdict Row IDs follow the `RNG-NN-V##` per-REQ-monotonic scheme."
    - "REQ-summary table aggregates per-REQ closure: every REQ in {RNG-01, RNG-02, RNG-03} has at least one Verdict Row ID prefix; per-REQ severity floor stated."
    - "All D-243 row subsets from D-243-I008..D-243-I010 are cited at least once in verdict-table cells."
    - "Phase 243 §1.7 bullet 3 (`_gameOverEntropy` rngRequestTime clearing reentry) closes here per CONTEXT.md D-09 mapping; bullet 8 (cc68bfc7 jackpots direct-handle reentrancy parity) cross-cited with 244-04 GOX-06 — verdict EITHER closes here OR rolls forward to 244-04 (planner's discretion documented in plan SUMMARY)."
    - "Reproduction recipe sub-section lists POSIX-portable `git show 16597cac -- contracts/modules/DegenerusGameAdvanceModule.sol` + `grep -rn` commands used."
    - "Zero F-31-NN finding-IDs (CONTEXT.md D-21); zero `contracts/` or `test/` writes; zero edits to `audit/v31-243-DELTA-SURFACE.md`."
  artifacts:
    - path: "audit/v31-244-RNG.md"
      provides: "RNG bucket (REQ RNG-01..RNG-03) per-REQ verdict tables + KI envelope re-verify sub-section + finding-candidate prose blocks. Working file consumed by 244-04 consolidation."
      contains: "## §RNG-01"
  key_links:
    - from: "audit/v31-243-DELTA-SURFACE.md §6 D-243-I008..D-243-I010"
      to: "audit/v31-244-RNG.md §RNG-01..§RNG-03 verdict-table cells"
      via: "Source 243 Row(s) column citation per CONTEXT.md D-06"
      pattern: "D-243-(C|F|X)[0-9]{3}"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol:160-480 (advanceGame body) at HEAD cc68bfc7"
      to: "audit/v31-244-RNG.md §RNG-01 reaching-path enumeration"
      via: "trace every path landing at post-payDailyJackpot continuation per CONTEXT.md D-11 RNG-01 vector (a)+(b)"
      pattern: "rngLocked\\s*=\\s*false|_unlockRng"
    - from: "audit/v30-CONSUMER-INVENTORY.md INV-237-021..037 (rngLockedFlag rows) + INV-237-052..059 (_gameOverEntropy rows)"
      to: "audit/v31-244-RNG.md §RNG-02 AIRTIGHT invariant RE_VERIFIED_AT_HEAD annotation + KI envelope re-verify sub-section"
      via: "Phase 239 carry + Phase 238 D-10 pattern"
      pattern: "INV-237-[0-9]{3}|RE_VERIFIED_AT_HEAD"
---

<objective>
Adversarially audit the `16597cac` commit (rngunlock fix). Close all 3 RNG requirements (RNG-01 / RNG-02 / RNG-03) with per-REQ verdict tables + KI envelope re-verify sub-section + finding-candidate prose blocks in working file `audit/v31-244-RNG.md`.

Scope source is `audit/v31-243-DELTA-SURFACE.md` §6 Consumer Index rows D-243-I008..D-243-I010. Scope is READ-only per CONTEXT.md D-18.

Per CONTEXT.md D-09: Phase 243 §1.7 bullet 3 (`_gameOverEntropy` rngRequestTime clearing reentry) is OWNED by 244-02 (RNG-02 sub-vector) and CROSS-CITED with 244-04 (GOX-06). §1.7 bullet 8 (cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall reentrancy parity) is CROSS-CITED across 244-02 (RNG-01 reentrancy adjacency) + 244-04 (GOX-06).

Per CONTEXT.md D-22: KI exceptions EXC-02 (prevrandao fallback) + EXC-03 (F-29-04 mid-cycle substitution) get RE_VERIFIED_AT_HEAD annotation only — no re-litigation. The new `_gameOverEntropy` rngRequestTime clearing must not widen EXC-02; the `_unlockRng(day)` removal must not regress EXC-03.

Per project memory:
- `feedback_rng_backward_trace.md` — RNG-01 audit MUST trace BACKWARD from each consumer to verify rngWord was unknown at input commitment time
- `feedback_rng_commitment_window.md` — RNG-01 + RNG-02 audit MUST check what player-controllable state can change between VRF request and fulfillment

Per CONTEXT.md D-19: HEAD anchor `cc68bfc7` locked. STOP if `git diff cc68bfc7..HEAD -- contracts/` is non-empty. (Verified at planning time: only docs commits since `cc68bfc7`.)

Output: `audit/v31-244-RNG.md` is the working file. Per CONTEXT.md D-05, 244-04 consolidates this into `audit/v31-244-PER-COMMIT-AUDIT.md` at SUMMARY-commit time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md
@.planning/phases/243-delta-extraction-per-commit-classification/243-03-SUMMARY.md
@audit/v31-243-DELTA-SURFACE.md
@audit/v30-CONSUMER-INVENTORY.md
@audit/KNOWN-ISSUES.md
@.planning/milestones/v30.0-phases/238-backward-forward-freeze-proofs/238-CONTEXT.md
@.planning/milestones/v30.0-phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md
</context>

<threat_model>
Per CONTEXT.md D-11 (RNG adversarial vectors — fixed). Actor taxonomy carries from Phase 238 D-07 (player / admin / validator / VRF oracle). Project skills `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` apply. KI envelope re-verify per CONTEXT.md D-22.

**Trust boundaries (within RNG scope):**

| Boundary | Description |
|---|---|
| `VRF callback → advanceGame two-call-split continuation` | rngWord becomes observable at callback time; the post-`_unlockRng(day)` removal point must NOT allow a player to commit input AFTER rngWord is observable |
| `_gameOverEntropy → rngRequestTime SSTORE` | the new `rngRequestTime = 0` clearing on prevrandao-fallback commit must not leak a reentry surface (per §1.7 bullet 3) |
| `cc68bfc7 jackpots direct-handle (parallel to runBafJackpot delegatecall)` | new external-call path could interact with the existing self-call delegatecall reentrancy surface (per §1.7 bullet 8 — cross-cited with 244-04) |

**STRIDE-mapped per-REQ vectors (transcribed from CONTEXT.md D-11 + project skills):**

| Threat ID | REQ | Category | Component | Disposition | Mitigation Plan / Adversarial Vector |
|---|---|---|---|---|---|
| T-244-RNG-01a | RNG-01 | Tampering / Information-Disclosure | every reaching path landing at post-`payDailyJackpot(true, lvl, rngWord)` continuation in advanceGame after `_unlockRng(day)` removal | mitigate-by-audit | Trace every path; for each path identify the next `rngLocked = false` SSTORE and verify it is reached on the SAME tick (no cross-tick reachability where rngLocked stays set into a later block). Cross-cite §1.8 INV-237-035 HUNK-ADJACENT row. |
| T-244-RNG-01b | RNG-01 | Spoofing / Tampering | 4-actor closure (player / admin / validator / VRF oracle per Phase 238 D-07 carry) | mitigate-by-audit | For each actor class, ask: does this actor reach a path where rngLocked stays set past the tick? Player: can a player call any function that reaches the continuation but skips the unlock? Admin: can admin trigger the continuation without the unlock side-effect? Validator: can validator reorder so unlock SSTORE is delayed past tick boundary? VRF oracle: can VRF callback delay re-trigger continuation in a state that bypasses unlock? Verdict per actor. |
| T-244-RNG-01c | RNG-01 | Information-Disclosure (backward-trace) | post-removal continuation point treated as a CONSUMER per `feedback_rng_backward_trace.md` | mitigate-by-audit | Trace BACKWARD from the continuation point: at every input-commitment site reachable from this consumer, verify rngWord was UNKNOWN at commit time. NO path may let a player commit input AFTER rngWord is observable from the VRF callback. |
| T-244-RNG-01d | RNG-01 | Tampering (commitment-window) | player-controllable state between VRF request and fulfillment per `feedback_rng_commitment_window.md` | mitigate-by-audit | For the `_unlockRng(day)` removal, enumerate what player-controllable state can change between VRF request and fulfillment in the new (post-removal) shape. Verdict states whether commitment window widened, narrowed, or held. |
| T-244-RNG-01e | RNG-01 | KI envelope (EXC-03) re-verify per D-22 | F-29-04 mid-cycle write-buffer ticket substitution | accept (KI) — RE_VERIFY only | Confirm `_unlockRng(day)` removal does NOT regress F-29-04 mid-cycle substitution (ticket-buffer swap timing unaffected; `_swapAndFreeze` / `_swapTicketSlot` / `_gameOverEntropy` interaction unchanged). Annotation: RE_VERIFIED_AT_HEAD `cc68bfc7`. |
| T-244-RNG-02a | RNG-02 | Tampering | rngLockedFlag AIRTIGHT invariant at HEAD `cc68bfc7` | mitigate-by-audit | Re-verify v30.0 AIRTIGHT invariant: no double-set, no set-without-clear, no clear-without-matching-set. Annotation: RE_VERIFIED_AT_HEAD `cc68bfc7` per Phase 238 D-10 pattern. Cross-cite Phase 239 RNG-01..03 result + INV-237-021..037. |
| T-244-RNG-02b | RNG-02 | Tampering / commitment-window | `_gameOverEntropy` rngRequestTime clearing per §1.7 bullet 3 | mitigate-by-audit | Confirm the new `rngRequestTime = 0` SSTORE at L1275-1279 in `_gameOverEntropy` does NOT break AIRTIGHT invariant. Specifically: the SSTORE happens AFTER `_finalizeLootboxRng(fallbackWord)` per §1.7 bullet 3 — verify no reentry surface (no external call between the finalize and the clear that could re-enter `_gameOverEntropy` while rngRequestTime is still set). |
| T-244-RNG-02c | RNG-02 | KI envelope (EXC-02) re-verify per D-22 | prevrandao fallback `_getHistoricalRngFallback` | accept (KI) — RE_VERIFY only | Confirm the new `rngRequestTime = 0` clearing does NOT widen the EXC-02 envelope — prevrandao consumption must remain limited to gameOver-fallback only, no leak into normal-path. Annotation: RE_VERIFIED_AT_HEAD `cc68bfc7`. |
| T-244-RNG-02d | RNG-02 | Tampering (commitment-window) per `feedback_rng_commitment_window.md` | rngLocked clear timing relative to VRF request lifecycle | mitigate-by-audit | For each clear-rngLocked SSTORE site at HEAD `cc68bfc7`, verify the clear happens AFTER the VRF word has been consumed (no clear-before-consume where a re-request could overlap a stale unlock). |
| T-244-RNG-03a | RNG-03 | REFACTOR_ONLY equivalence (per D-17) | 16597cac multi-line SLOAD + tuple destructuring reformats | mitigate-by-audit | Side-by-side prose diff (NOT bytecode-diff per D-17) — name specific source elements proven byte-equivalent (no SSTORE order change, no branch added, no return-path evaluation drift). Per Phase 243 D-04 REFACTOR_ONLY definition + D-19 evidence burden. Where REFACTOR_ONLY claim has any doubt, escalate to MODIFIED_LOGIC + a separate verdict per D-17 burden-of-proof rule. |
| T-244-RNG-cc1 | RNG-01 (cross-cite) | reentrancy parity per §1.7 bullet 8 | cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall | mitigate-by-audit OR defer to 244-04 | Cross-cited with 244-04 GOX-06. The two call paths hit the same JACKPOTS contract but via different code paths (self-call delegatecall vs direct external). Confirm no reentrancy or nonce-ordering interaction between the two. Verdict EITHER closes here OR rolls forward to 244-04 (planner's discretion in plan SUMMARY). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit 16597cac _unlockRng(day) removal safety + KI EXC-03 envelope re-verify — close RNG-01 + RNG-03</name>
  <files>audit/v31-244-RNG.md</files>
  <read_first>
    - .planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md — D-04 / D-05 / D-06 / D-07 / D-08 / D-11 (RNG adversarial vectors) / D-17 (REFACTOR_ONLY equivalence prose-diff methodology) / D-18 / D-19 / D-20 / D-21 / D-22 (KI envelope re-verify only)
    - audit/v31-243-DELTA-SURFACE.md §1.2 commit 16597cac changelog row D-243-C007 (advanceGame MODIFIED at L156-480; `_unlockRng(day)` removed from two-call-split ETH continuation; two multi-line SLOAD/destructuring reformats)
    - audit/v31-243-DELTA-SURFACE.md §2.3 classification row D-243-F006 (advanceGame MODIFIED_LOGIC — D-05.1 + D-05.2 collapsed; removal drives verdict, reformat is subordinate)
    - audit/v31-243-DELTA-SURFACE.md §3.1.6 call-site catalog rows D-243-X011 (advanceGame dispatcher at DegenerusGame.sol:284), D-243-X012 (delegatecall selector at DegenerusGame.sol:289), D-243-X013 (DegenerusVault.sol:515 vault-owner wrapper), D-243-X014 (StakedDegenerusStonk.sol:355 sDGNRS wrapper)
    - audit/v31-243-DELTA-SURFACE.md §1.8 reconciliation table — every INV-237-021 through INV-237-037 row (rngLockedFlag overlap context); ESPECIALLY INV-237-035 HUNK-ADJACENT (line 450 — exactly where `_unlockRng(day)` was removed; payDailyJackpot(true, lvl, rngWord) call itself unchanged but post-call unlock dropped)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I008 (RNG-01 → C007 + F006 + X013/X014 + INV-237-035 + §1.7 bullet 3), D-243-I010 (RNG-03 → C007 + F006)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7 — specifically:
      - L156-480 — `advanceGame` body (D-243-C007 changed function); the post-`payDailyJackpot(true, lvl, rngWord)` continuation point at ~L449/450 (per §1.8 INV-237-035) where `_unlockRng(day)` was removed
      - The two multi-line SLOAD reformats at L257-260 (per §1.8 INV-237-022/023 REFORMAT-TOUCHED) and the second tuple destructuring reformat
    - contracts/modules/DegenerusGameAdvanceModule.sol at baseline 7ab515fe (via `git show 7ab515fe:contracts/modules/DegenerusGameAdvanceModule.sol`) — to compare side-by-side for the reformat-only verdict (RNG-03)
    - audit/v30-CONSUMER-INVENTORY.md — INV-237-021..037 rngLockedFlag rows + INV-237-052..059 _gameOverEntropy rows for cross-cite
    - audit/KNOWN-ISSUES.md — EXC-03 entry (Gameover RNG substitution for mid-cycle write-buffer tickets — F-29-04) for RE_VERIFIED_AT_HEAD annotation
    - audit/FINDINGS-v29.0.md — F-29-04 finding for cross-cite
  </read_first>
  <action>
Execute the RNG-01 + RNG-03 audit pass for commit `16597cac`. KI EXC-03 envelope re-verify is part of RNG-01 per CONTEXT.md D-22.

**Step A — Sanity gate (mandatory before any write):**

Run exactly:
```
git rev-parse 7ab515fe
git rev-parse cc68bfc7
git rev-parse HEAD
git diff --stat cc68bfc7..HEAD -- contracts/
git status --porcelain contracts/ test/
```

Locked expectations (same as 244-01 Task 1):
- `git rev-parse cc68bfc7` → `cc68bfc70e76fb75ac6effbc2135aae978f96ff3`
- `git diff --stat cc68bfc7..HEAD -- contracts/` reports zero file changes
- `git status --porcelain contracts/ test/` returns empty

If `git diff cc68bfc7..HEAD -- contracts/` is non-empty, STOP and record scope-guard deferral in plan SUMMARY.

**Step B — Create file shell:**

Create `audit/v31-244-RNG.md` with sections (planner discretion on within-section table-vs-prose ordering):

```
# v31.0 Phase 244 — RNG Bucket Audit (commit 16597cac + KI envelope re-verify)

Audit baseline: 7ab515fe
Audit head:     cc68bfc7
Owning commits: 16597cac (rngunlock fix) + KI envelope re-verify against EXC-02 + EXC-03
Scope per:      audit/v31-243-DELTA-SURFACE.md §6 D-243-I008..D-243-I010
Plan:           244-02-PLAN.md
Phase:          244-per-commit-adversarial-audit-evt-rng-qst-gox

## §0 — Per-Bucket Verdict Count Card

| REQ-ID | Verdict Rows | Finding Candidates | KI Envelope Status | Floor Severity |
| --- | --- | --- | --- | --- |
| RNG-01 | (filled) | (filled) | EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 | (filled) |
| RNG-02 | (filled) | (filled) | EXC-02 RE_VERIFIED_AT_HEAD cc68bfc7 | (filled) |
| RNG-03 | (filled) | (filled) | n/a | (filled) |

## §RNG-01 — _unlockRng(day) removal safety
## §RNG-03 — 16597cac reformat-only behavioral equivalence
## §KI Envelope Re-Verify — EXC-03 (F-29-04 mid-cycle substitution)
## §Reproduction Recipe — RNG bucket
```

(§RNG-02 + §KI Envelope Re-Verify EXC-02 appended by Task 2.)

**Step C — RNG-01 reaching-path enumeration (vector RNG-01a from CONTEXT.md D-11):**

Read `contracts/modules/DegenerusGameAdvanceModule.sol` at HEAD cc68bfc7 around L156-480. Identify the post-`payDailyJackpot(true, lvl, rngWord)` continuation point (per §1.8 INV-237-035 at line 450). Trace EVERY reaching path that lands at the continuation:

1. Identify the call site of `payDailyJackpot(true, lvl, rngWord)` in advanceGame body
2. Walk advanceGame from L160 entry to that call site, enumerating the reachable branches that REACH that call (not the alternate branches that bypass it)
3. For each reaching branch, identify the next `rngLocked = false` SSTORE on the same tick — could be:
   - inline at the continuation (now NOT present since `_unlockRng(day)` removed)
   - inside `payDailyJackpot` itself (verify by reading payDailyJackpot body)
   - inside a downstream call from the continuation (verify by walking downstream)
   - inside a separate dispatcher path that runs BEFORE advanceGame returns (cross-tick boundary check)
4. Verify the unlock is reached on the SAME tick (same transaction) — no path that exits advanceGame with rngLocked still set

For each reaching path, write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-01-V01 | RNG-01 | D-243-C007, D-243-F006, INV-237-035 | contracts/modules/DegenerusGameAdvanceModule.sol:<continuation-line> | RNG-01a (path P1: <branch description>) | <bucket> | <next rngLocked = false SSTORE site> reached via <call chain> on SAME tick | 16597cac |

Multiple paths → multiple V## rows. Aim for completeness; do not collapse paths into "and similarly for X". Per CONTEXT.md D-07 each path-vector pair is its own row.

**Step D — RNG-01 4-actor adversarial closure (vector RNG-01b from CONTEXT.md D-11 + Phase 238 D-07 carry):**

For each actor class (player / admin / validator / VRF oracle), enumerate what calls the actor can make that REACH advanceGame:

- Player: per call sites D-243-X013 (DegenerusVault.gameAdvance L514) + D-243-X014 (StakedDegenerusStonk.gameAdvance L354) — players reach advanceGame via vault wrapper or sDGNRS wrapper. Verdict: does any player-reachable path leave rngLocked set past the tick?
- Admin: per call site D-243-X011 (DegenerusGame.advanceGame L284 dispatcher) — admin (or anyone with the dispatcher entry-point access) reaches the dispatcher directly. Verdict: any admin-only path that bypasses unlock?
- Validator: validator can reorder transactions within a block. Verdict: can validator delay an unlock SSTORE to the next block (cross-tick)?
- VRF oracle: VRF callback can fire at variable timing. Verdict: can VRF callback re-trigger continuation in a state that bypasses unlock?

For each actor, write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-01-V<n> | RNG-01 | D-243-X011/X013/X014 | <entry-point file:line> | RNG-01b actor=<player/admin/validator/VRF> | <bucket> | <reasoning per actor> | 16597cac |

**Step E — RNG-01 backward-trace + commitment-window checks (vectors RNG-01c + RNG-01d per project skills):**

Backward-trace per `feedback_rng_backward_trace.md`:
1. Treat the post-removal continuation point as a CONSUMER of rngWord
2. Trace BACKWARD from the continuation through every input-commitment site reachable from this consumer
3. For each input-commitment site, verify rngWord was UNKNOWN at commit time (commit must precede the VRF callback that produced rngWord)

Commitment-window per `feedback_rng_commitment_window.md`:
1. For the `_unlockRng(day)` removal, enumerate what player-controllable state can change between VRF request and fulfillment in the new (post-removal) shape
2. Compare to the pre-removal shape (baseline 7ab515fe)
3. Verdict: did the commitment window widen, narrow, or hold? If widened, that is a finding-candidate.

Verdict rows:

| RNG-01-V<n> | RNG-01 | D-243-C007, INV-237-035 | <relevant file:line> | RNG-01c backward-trace | <bucket> | <commit-site list + rngWord-unknown check per site> | 16597cac |
| RNG-01-V<n+1> | RNG-01 | D-243-C007 | <relevant file:line> | RNG-01d commitment-window | <bucket> | <pre-removal vs post-removal player-controllable state delta> | 16597cac |

**Step F — RNG-03 reformat-only behavioral equivalence (vector RNG-03a + per CONTEXT.md D-17 methodology):**

Per CONTEXT.md D-17: side-by-side PROSE diff (NOT bytecode-diff). Read advanceGame body at baseline 7ab515fe (via `git show 7ab515fe:contracts/modules/DegenerusGameAdvanceModule.sol`) and at HEAD cc68bfc7. For each of the two reformat hunks:

1. Multi-line SLOAD reformat — per §1.8 INV-237-022/023 REFORMAT-TOUCHED at line 261-262 (HEAD-side; preIdx multi-line decomposition):
   - Baseline form: <verbatim baseline text>
   - HEAD form: <verbatim HEAD text>
   - Element-by-element equivalence: variable name unchanged, SLOAD source slot unchanged, assigned local var name unchanged, no SSTORE introduced, no branch added, no return-path evaluation drift
   - Verdict: REFACTOR_ONLY confirmed (or if any doubt, escalate to MODIFIED_LOGIC per D-17)

2. Tuple destructuring reformat — second reformat in the 16597cac hunk (multi-line tuple destructuring of an existing return value):
   - Baseline form: <verbatim baseline text>
   - HEAD form: <verbatim HEAD text>
   - Element-by-element equivalence: tuple element order unchanged, local var names unchanged (or if renamed, name change is a docs/style change with no semantic effect), no SSTORE introduced, no branch added, no return-path evaluation drift
   - Verdict: REFACTOR_ONLY confirmed (or escalate)

Verdict rows:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-03-V01 | RNG-03 | D-243-C007, D-243-F006 | contracts/modules/DegenerusGameAdvanceModule.sol:257-260 (HEAD) vs corresponding baseline range | RNG-03a multi-line SLOAD reformat | <SAFE expected> | side-by-side prose diff naming the variable + SLOAD slot proven byte-equivalent | 16597cac |
| RNG-03-V02 | RNG-03 | D-243-C007, D-243-F006 | contracts/modules/DegenerusGameAdvanceModule.sol:<tuple-destructure-line> vs baseline | RNG-03a tuple destructuring reformat | <SAFE expected> | side-by-side prose diff naming the tuple elements proven byte-equivalent | 16597cac |

**Step G — KI EXC-03 envelope re-verify sub-section (vector RNG-01e per CONTEXT.md D-22):**

Append `## §KI Envelope Re-Verify — EXC-03 (F-29-04 mid-cycle substitution)` to `audit/v31-244-RNG.md`.

Read audit/KNOWN-ISSUES.md EXC-03 entry verbatim. Read audit/FINDINGS-v29.0.md F-29-04 cross-reference. Then for each `_unlockRng(day)` removal effect on F-29-04 mid-cycle substitution:
1. Identify the F-29-04 mid-cycle substitution path (`_swapAndFreeze` / `_swapTicketSlot` / `_gameOverEntropy` interaction)
2. Verify the `_unlockRng(day)` removal does NOT affect the timing of these calls (the removal is in the post-payDailyJackpot continuation, AFTER the mid-cycle ticket-buffer swap window)
3. Annotate: `RE_VERIFIED_AT_HEAD cc68bfc7 — EXC-03 envelope unchanged. Removal is downstream of the mid-cycle substitution window per F-29-04 invariant (cf. F-29-04 ticket-buffer swap timing).`

Verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-01-V<n> | RNG-01 | D-243-C007, EXC-03, F-29-04 | contracts/modules/DegenerusGameAdvanceModule.sol:<continuation-line> | RNG-01e KI EXC-03 envelope re-verify | RE_VERIFIED_AT_HEAD cc68bfc7 | F-29-04 mid-cycle substitution timing unaffected by removal — `_swapAndFreeze` runs BEFORE the continuation point per <reasoning> | 16597cac |

**Step H — Update §0 Verdict Count Card with RNG-01 + RNG-03 row counts. Append §Reproduction Recipe with the grep + git-show + git-blame commands actually used.**

Constraints:
- Zero edits to `audit/v31-243-DELTA-SURFACE.md`
- Zero F-31-NN finding-IDs
- Zero `contracts/` or `test/` writes
- KI EXC-03 RE_VERIFIED_AT_HEAD annotation only — NO re-litigation of acceptance (CONTEXT.md D-22)
  </action>
  <verify>
    <automated>test -f audit/v31-244-RNG.md && grep -q '^## §RNG-01' audit/v31-244-RNG.md && grep -q '^## §RNG-03' audit/v31-244-RNG.md && grep -q 'RE_VERIFIED_AT_HEAD' audit/v31-244-RNG.md && grep -q 'EXC-03' audit/v31-244-RNG.md && grep -q 'INV-237-035' audit/v31-244-RNG.md && grep -q 'F-29-04' audit/v31-244-RNG.md && grep -qE '^\| RNG-01-V[0-9]+ ' audit/v31-244-RNG.md && grep -qE '^\| RNG-03-V[0-9]+ ' audit/v31-244-RNG.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-244-RNG.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md)" = "" ]</automated>
  </verify>
  <done>
    - `audit/v31-244-RNG.md` exists with §RNG-01, §RNG-03, §KI Envelope Re-Verify EXC-03 sections
    - At least one verdict row per REQ in {RNG-01, RNG-03} with all 8 columns populated
    - Every reaching path landing at post-`payDailyJackpot(true, lvl, rngWord)` continuation enumerated as a separate RNG-01-V## row
    - 4-actor adversarial closure (player / admin / validator / VRF oracle) recorded as 4 RNG-01-V## rows
    - Backward-trace + commitment-window checks recorded as RNG-01-V## rows per project skills
    - 16597cac multi-line SLOAD + tuple destructuring reformats each have a side-by-side prose diff RNG-03-V## row
    - EXC-03 envelope re-verify carries `RE_VERIFIED_AT_HEAD cc68bfc7` annotation
    - Zero F-31-NN finding-IDs
    - Zero `contracts/`, `test/`, or `audit/v31-243-DELTA-SURFACE.md` writes
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit rngLockedFlag AIRTIGHT invariant RE_VERIFIED_AT_HEAD + KI EXC-02 envelope re-verify + close §1.7 bullets 3 + 8 — close RNG-02</name>
  <files>audit/v31-244-RNG.md</files>
  <read_first>
    - .planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md — D-09 (§1.7 bullet 3 closes here; bullet 8 cross-cite with 244-04), D-11 RNG-02 vectors, D-22 KI envelope re-verify only
    - audit/v31-243-DELTA-SURFACE.md §1.4 commit-771893d1 changelog row D-243-C016 (`_gameOverEntropy` MODIFIED at L1216-1294 — rngRequestTime cleared when prevrandao fallback word commits)
    - audit/v31-243-DELTA-SURFACE.md §2.3 classification row D-243-F014 (`_gameOverEntropy` MODIFIED_LOGIC)
    - audit/v31-243-DELTA-SURFACE.md §3.1.14 D-243-X027 (call site of `_gameOverEntropy` at AdvanceModule L560 inside `_handleGameOverPath`)
    - audit/v31-243-DELTA-SURFACE.md §1.7 bullet 3 (`_gameOverEntropy` rngRequestTime clearing at L1275 happens AFTER `_finalizeLootboxRng(fallbackWord)` — confirm no reentry surface)
    - audit/v31-243-DELTA-SURFACE.md §1.7 bullet 8 (cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall reentrancy parity — cross-cite with 244-04 GOX-06)
    - audit/v31-243-DELTA-SURFACE.md §1.8 reconciliation rows INV-237-052..059 (_gameOverEntropy rows, esp. INV-237-059 HUNK-ADJACENT — line 1274 sits just before the new `rngRequestTime = 0` SSTORE at L1275-1279)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index row D-243-I009 (RNG-02 → C007 + C016 + F006 + F014 + X027 + INV-237-021..037 cross-ref + §1.8 reconciliation)
    - .planning/milestones/v30.0-phases/239-rnglocked-invariant-permissionless-sweep/ — Phase 239 RNG-01..03 SUMMARY for AIRTIGHT invariant statement verbatim
    - audit/v30-CONSUMER-INVENTORY.md — INV-237-021..037 (rngLockedFlag rows) + INV-237-052..059 (_gameOverEntropy rows)
    - audit/KNOWN-ISSUES.md — EXC-02 entry (Gameover prevrandao fallback `_getHistoricalRngFallback`) for RE_VERIFIED_AT_HEAD annotation
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7 — L1216-1294 `_gameOverEntropy` body; specifically the new `rngRequestTime = 0` SSTORE at L1275-1279 + the `_finalizeLootboxRng(fallbackWord)` call at L1274
    - contracts/storage/DegenerusGameStorage.sol at HEAD cc68bfc7 — rngLockedFlag declaration + every clear-rngLocked-flag SSTORE site (grep `rngLocked` in storage + modules)
  </read_first>
  <action>
Append §RNG-02 + §KI Envelope Re-Verify EXC-02 + §1.7 bullet 3 closure + §1.7 bullet 8 cross-cite-or-defer to `audit/v31-244-RNG.md` (Task 1 created the file shell + §RNG-01 / §RNG-03 / EXC-03 sections).

**Step A — Sanity gate per Task 1 Step A. Re-confirm `cc68bfc7` is HEAD anchor.**

**Step B — Append `## §RNG-02 — rngLockedFlag AIRTIGHT invariant RE_VERIFIED_AT_HEAD cc68bfc7`:**

Open with the AIRTIGHT invariant statement verbatim (read from Phase 239 RNG-01..03 SUMMARY or from `.planning/milestones/v30.0-phases/239-rnglocked-invariant-permissionless-sweep/`):

```
## §RNG-02 — rngLockedFlag AIRTIGHT invariant RE_VERIFIED_AT_HEAD cc68bfc7

### Invariant statement (from Phase 239 RNG-01..03)
"<verbatim AIRTIGHT invariant text — no double-set, no set-without-clear, no clear-without-matching-set>"

### Re-verification methodology (per CONTEXT.md D-22 + Phase 238 D-10 pattern)
This re-verification confirms the v30.0 invariant holds against the post-v30.0 deltas (`16597cac` `_unlockRng(day)` removal + `771893d1` `_gameOverEntropy` `rngRequestTime` clearing). It does NOT re-litigate the invariant itself — only the envelope.
```

**Step C — RNG-02 verdict table (vector RNG-02a from CONTEXT.md D-11):**

For each set/clear SSTORE of rngLockedFlag at HEAD cc68bfc7, write a verdict row:

1. Grep every set-rngLocked SSTORE site:
   ```
   grep -rn --include='*.sol' 'rngLocked\s*=\s*true' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
   ```
2. Grep every clear-rngLocked SSTORE site:
   ```
   grep -rn --include='*.sol' 'rngLocked\s*=\s*false' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
   grep -rn --include='*.sol' '_unlockRng' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
   ```
3. For each pair (set, clear), verify:
   - No double-set on the same tick without an intervening clear
   - No set-without-eventual-clear (every set has a reachable clear on the same tick or via the unlock path)
   - No clear-without-matching-set (every clear corresponds to a prior set in the same VRF request lifecycle)

For each invariant predicate (no double-set / no set-without-clear / no clear-without-matching-set), write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-02-V01 | RNG-02 | D-243-C007, INV-237-021..037 | contracts/modules/DegenerusGameAdvanceModule.sol:<set-line> + <clear-line> | RNG-02a no-double-set predicate | RE_VERIFIED_AT_HEAD cc68bfc7 | <set-clear pairing list at HEAD> | 16597cac (delta), Phase 239 (origin) |
| RNG-02-V02 | RNG-02 | D-243-C007, INV-237-021..037 | … | RNG-02a no-set-without-clear predicate | RE_VERIFIED_AT_HEAD cc68bfc7 | … | … |
| RNG-02-V03 | RNG-02 | D-243-C007, INV-237-021..037 | … | RNG-02a no-clear-without-matching-set predicate | RE_VERIFIED_AT_HEAD cc68bfc7 | … | … |

**Step D — RNG-02 vector (b)+(c)+(e) cross-cite Phase 239 + INV-237 reconciliation:**

Add a verdict row that explicitly cross-cites Phase 239 RNG-01..03 result and the INV-237-021..037 reconciliation rows from §1.8:

| RNG-02-V04 | RNG-02 | D-243-C007, INV-237-021..037, Phase-239 RNG-01..03 | n/a (cross-cite) | RNG-02b/c Phase 239 carry + §1.8 reconciliation | RE_VERIFIED_AT_HEAD cc68bfc7 | 17 INV-237 rows on rngLockedFlag — every one is "function-level-overlap only" or REFORMAT-TOUCHED per §1.8 (zero RNG surface widening); Phase 239 result still holds | n/a |

**Step E — RNG-02 vector (d) — `_gameOverEntropy` rngRequestTime clearing per §1.7 bullet 3:**

Read `_gameOverEntropy` body at HEAD cc68bfc7 around L1216-1294. Identify:
- L1274: `_finalizeLootboxRng(fallbackWord)` call
- L1275-1279: new `rngRequestTime = 0` SSTORE

Vector check: confirm there is no external call between L1274 and L1275 that could re-enter `_gameOverEntropy` while rngRequestTime is still set. Read `_finalizeLootboxRng` body to confirm no external call escapes inside it (or if it does, confirm reentry into `_gameOverEntropy` is impossible by other guards).

Verdict row:

| RNG-02-V05 | RNG-02 | D-243-C016, D-243-F014, D-243-X027, §1.7 bullet 3 | contracts/modules/DegenerusGameAdvanceModule.sol:1274-1279 | RNG-02d _gameOverEntropy rngRequestTime clearing reentry surface | <SAFE expected, INFO if reentry surface present but not exploitable> | `_finalizeLootboxRng(fallbackWord)` call site analysis + reentry surface verdict | 771893d1 |

If reentry surface exists and is exploitable, escalate to LOW/MEDIUM/HIGH per CONTEXT.md D-08 + emit a finding-candidate.

**Step F — RNG-02 commitment-window check per `feedback_rng_commitment_window.md`:**

For the new `rngRequestTime = 0` clearing, enumerate what player-controllable state can change between VRF request and fulfillment in this new (post-clearing) shape. Compare to pre-clearing baseline. Verdict states whether the commitment window widened, narrowed, or held.

Verdict row:

| RNG-02-V06 | RNG-02 | D-243-C016 | contracts/modules/DegenerusGameAdvanceModule.sol:1275-1279 | RNG-02 commitment-window per project skill | <bucket> | <pre-clearing vs post-clearing player-controllable state delta> | 771893d1 |

**Step G — Append `## §KI Envelope Re-Verify — EXC-02 (prevrandao fallback _getHistoricalRngFallback)`:**

Read audit/KNOWN-ISSUES.md EXC-02 entry verbatim. Then for each `_gameOverEntropy` rngRequestTime clearing effect on EXC-02:
1. Identify the EXC-02 envelope: prevrandao consumption MUST remain limited to gameOver-fallback only (no leak into normal-path)
2. Verify the new `rngRequestTime = 0` SSTORE does NOT enable a path where prevrandao is consumed outside gameOver-fallback context
3. Annotate: `RE_VERIFIED_AT_HEAD cc68bfc7 — EXC-02 envelope unchanged. The clearing happens INSIDE _gameOverEntropy (gameOver-fallback context); no leak.`

Verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| RNG-02-V<n> | RNG-02 | D-243-C016, EXC-02 | contracts/modules/DegenerusGameAdvanceModule.sol:1275-1279 | RNG-02c KI EXC-02 envelope re-verify | RE_VERIFIED_AT_HEAD cc68bfc7 | clearing scoped to gameOver-fallback context per `_gameOverEntropy` body — no leak into normal-path prevrandao consumption | 771893d1 |

**Step H — Close §1.7 bullet 3 (per CONTEXT.md D-09 mapping — owned by 244-02 RNG-02):**

The §RNG-02 vector (d) row from Step E IS the §1.7 bullet 3 closure. Add an explicit cross-reference note in §RNG-02 (or in a dedicated `## §1.7-Bullet-3 closure` block) stating:

```
### §1.7 bullet 3 closure — `_gameOverEntropy` rngRequestTime clearing reentry adjacency
Closed by Verdict Row ID RNG-02-V05 (Step E above). Verdict: <bucket from Step E>.
```

**Step I — §1.7 bullet 8 cross-cite with 244-04 GOX-06:**

Per CONTEXT.md D-09 mapping, §1.7 bullet 8 (cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall reentrancy parity) is CROSS-CITED with 244-04 GOX-06. Planner's discretion: this plan EITHER closes bullet 8 here OR explicitly defers to 244-04 GOX-06 with a hand-off note.

If closing here, add a verdict row:

| RNG-01-V<n> | RNG-01 | D-243-C038, D-243-C039, D-243-X053, §1.7 bullet 8 | contracts/modules/DegenerusGameAdvanceModule.sol:105-106 (jackpots constant) + L831-839 (runBafJackpot self-call vs jackpots.markBafSkipped direct-handle) | RNG-cc1 reentrancy parity per §1.7 bullet 8 | <bucket> | <reentrancy / nonce-ordering interaction analysis between the two paths> | cc68bfc7 |

If deferring, add to plan SUMMARY: `§1.7 bullet 8 deferred to 244-04 GOX-06 — reason: reentrancy analysis benefits from full GOX context (cc68bfc7 BAF gate + GOX-06 _handleGameOverPath ordering interact via the same `_consolidatePoolsAndRewardJackpots` body)`.

**Step J — Update §0 Verdict Count Card with RNG-02 + EXC-02 row counts and KI envelope status. Append §Reproduction Recipe with the new grep + git-show + git-blame commands actually used.**

Constraints:
- Zero edits to `audit/v31-243-DELTA-SURFACE.md`
- Zero F-31-NN finding-IDs
- Zero `contracts/` or `test/` writes
- KI EXC-02 RE_VERIFIED_AT_HEAD only — NO re-litigation
  </action>
  <verify>
    <automated>test -f audit/v31-244-RNG.md && grep -q '^## §RNG-02' audit/v31-244-RNG.md && grep -q 'AIRTIGHT' audit/v31-244-RNG.md && grep -q 'EXC-02' audit/v31-244-RNG.md && grep -q 'INV-237-021' audit/v31-244-RNG.md && grep -q '§1.7 bullet 3' audit/v31-244-RNG.md && grep -q '§1.7 bullet 8' audit/v31-244-RNG.md && grep -qE '^\| RNG-02-V[0-9]+ ' audit/v31-244-RNG.md && grep -q 'RE_VERIFIED_AT_HEAD' audit/v31-244-RNG.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-244-RNG.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md)" = "" ]</automated>
  </verify>
  <done>
    - `audit/v31-244-RNG.md` contains §RNG-02 + §KI Envelope Re-Verify EXC-02 sections
    - AIRTIGHT invariant statement embedded verbatim with RE_VERIFIED_AT_HEAD cc68bfc7 annotation
    - At least 3 RNG-02-V## rows (one per AIRTIGHT predicate: no-double-set / no-set-without-clear / no-clear-without-matching-set)
    - RNG-02-V## row for `_gameOverEntropy` rngRequestTime clearing reentry surface (vector RNG-02d / §1.7 bullet 3 closure)
    - RNG-02-V## row for commitment-window check per project skill `feedback_rng_commitment_window.md`
    - EXC-02 envelope re-verify carries `RE_VERIFIED_AT_HEAD cc68bfc7` annotation
    - §1.7 bullet 3 explicitly closed (cross-ref to RNG-02-V## row)
    - §1.7 bullet 8 EITHER closed (RNG-01-V## row added with vector RNG-cc1) OR deferred to 244-04 GOX-06 with hand-off note in plan SUMMARY
    - Zero F-31-NN finding-IDs
    - Zero `contracts/`, `test/`, or `audit/v31-243-DELTA-SURFACE.md` writes
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification gate (manual, before commit):**

1. **Coverage gate:** Every REQ in {RNG-01, RNG-02, RNG-03} appears at least once as Verdict Row ID prefix.
2. **Source 243 row citation gate:** Every D-243 row in D-243-I008..D-243-I010 appears at least once in verdict-row Source 243 Row(s) cells.
3. **§1.7 candidate closure gate:** Bullet 3 closed in §RNG-02. Bullet 8 closed in §RNG-01 OR deferred to 244-04 with explicit hand-off note in plan SUMMARY.
4. **KI envelope gate:** EXC-02 RE_VERIFIED_AT_HEAD `cc68bfc7`; EXC-03 RE_VERIFIED_AT_HEAD `cc68bfc7`; both annotations present.
5. **Severity gate:** Every verdict row has severity from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} OR `RE_VERIFIED_AT_HEAD cc68bfc7` (KI exception envelope rows). Zero TBD/blank.
6. **Project-skill gate:** RNG-01 includes backward-trace (`feedback_rng_backward_trace.md`) AND commitment-window check (`feedback_rng_commitment_window.md`); RNG-02 includes commitment-window check.
7. **READ-only gate:** `git status --porcelain contracts/ test/` empty; `git status --porcelain audit/v31-243-DELTA-SURFACE.md` empty.
8. **F-31-NN absence gate:** zero F-31-NN finding-IDs.
9. **Reproduction recipe completeness:** §Reproduction Recipe lists every grep / git-show / git-blame command used.
</verification>

<success_criteria>
- `audit/v31-244-RNG.md` exists with valid frontmatter
- §RNG-01, §RNG-02, §RNG-03, §KI Envelope Re-Verify EXC-02, §KI Envelope Re-Verify EXC-03 sections each populated with verdict rows + prose blocks
- §1.7 bullet 3 closed in §RNG-02 with cross-ref
- §1.7 bullet 8 closed in §RNG-01 OR deferred with hand-off note in SUMMARY
- §0 Verdict Count Card filled per REQ + KI envelope status
- §Reproduction Recipe sub-section appended
- Plan SUMMARY records: per-REQ verdict-row count + floor severity + KI envelope status + working-file path `audit/v31-244-RNG.md`
</success_criteria>

<output>
After completion, create `.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-02-SUMMARY.md` per standard template + per-bucket-plan additions:
- Per-REQ verdict-row count + floor severity
- KI envelope status (EXC-02 RE_VERIFIED_AT_HEAD cc68bfc7; EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7)
- Phase 243 §1.7 bullet-closure status (bullet 3 closed in §RNG-02; bullet 8 closed-here or deferred-to-244-04 — state which)
- Path to working file: `audit/v31-244-RNG.md`
- Any scope-guard deferrals per CONTEXT.md D-20
- HEAD anchor confirmation (`cc68bfc7`)
</output>
