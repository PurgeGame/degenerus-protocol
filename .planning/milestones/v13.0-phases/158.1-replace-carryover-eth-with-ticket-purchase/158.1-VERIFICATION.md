---
phase: 158.1-replace-carryover-eth-with-ticket-purchase
verified: 2026-04-01T19:30:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 158.1: Replace Carryover ETH with Ticket Purchase — Verification Report

**Phase Goal:** Replace carryover ETH distribution with ticket purchases (0.5% budget, single random source level, no Phase 1 state machine), remove remaining BurnieCoin quest passthroughs (rollDailyQuest hop, recordMintQuestStreak COIN access), and simplify the daily jackpot carryover flow.
**Verified:** 2026-04-01T19:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Carryover days 2-4 distribute tickets (not ETH) to trait winners at a single random source level, with 0.5% futurePrizePool budget going to nextPrizePool | ✓ VERIFIED | `reserveSlice = _getFuturePrizePool() / 200` at line 394; `_setNextPrizePool(_getNextPrizePool() + carryoverLootboxBudget)` at line 406-409; single source: `_selectCarryoverSourceOffset` returns one `uint8` offset → one `carryoverSourceLevel` |
| 2  | Phase 1 carryover state machine removed (dailyCarryoverEthPool, dailyCarryoverWinnerCap, dailyEthPhase storage vars deleted) | ✓ VERIFIED | All three vars converted to layout-preserving `__gap_` private vars in DegenerusGameStorage.sol (lines 282, 484, 487); zero non-gap references across all contracts |
| 3  | `BurnieCoin.rollDailyQuest` removed; JackpotModule calls `quests.rollDailyQuest()` directly | ✓ VERIFIED | `grep "rollDailyQuest" BurnieCoin.sol` = 0 matches; JackpotModule calls `quests.rollDailyQuest()` at lines 533 and 632 |
| 4  | `DegenerusGame.recordMintQuestStreak` access control tightened to onlyGame (COIN no longer needs to call it) | ✓ VERIFIED | Line 433: `if (msg.sender != ContractAddresses.GAME) revert E();` — single-condition, no COIN check |
| 5  | All 62 Solidity files compile cleanly | ✓ VERIFIED | `npx hardhat compile --force` → "Compiled 62 Solidity files successfully (evm target: paris)" |
| 6  | `isResuming` logic removed from payDailyJackpot — no more resume path, always fresh-start | ✓ VERIFIED | Zero occurrences of `isResuming` in JackpotModule; `payDailyJackpot` unconditionally calls `_rollWinningTraits` and `_syncDailyWinningTraits` on entry |
| 7  | `dailyEthPoolBudget` storage var replaced with local variable (saves ~7,100 gas) | ✓ VERIFIED | `__gap_dailyEthPoolBudget` at Storage.sol line 383; zero live references to `dailyEthPoolBudget` across all contracts |
| 8  | `_clearDailyEthState()` function removed, `dailyJackpotCoinTicketsPending = true` inlined | ✓ VERIFIED | Zero occurrences of `_clearDailyEthState` in any contract; `dailyJackpotCoinTicketsPending = true` appears inline at JackpotModule line 471 |
| 9  | `STAGE_JACKPOT_ETH_RESUME` constant removed from AdvanceModule | ✓ VERIFIED | Zero occurrences of `STAGE_JACKPOT_ETH_RESUME` across all contracts |
| 10 | AdvanceModule resume block (`dailyEthPoolBudget != 0`) removed | ✓ VERIFIED | No `dailyEthPoolBudget` references in AdvanceModule; `payDailyJackpot(true, lvl, rngWord)` called unconditionally after coin+ticket block |
| 11 | `_prepareFutureTickets` guard simplified (dead `dailyEthPoolBudget == 0` condition removed) | ✓ VERIFIED | `_prepareFutureTickets` body begins with level-range logic only — no `dailyEthPoolBudget` guard present |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `contracts/modules/DegenerusGameJackpotModule.sol` | Single-pass ticket carryover in Phase 0, no Phase 1 | ✓ VERIFIED | `carryoverTicketUnits` computed and packed; no Phase 1 block; ETH path still runs for daily ETH; `dailyJackpotCoinTicketsPending = true` inlined |
| `contracts/storage/DegenerusGameStorage.sol` | Storage layout without dailyCarryoverEthPool, dailyCarryoverWinnerCap, dailyEthPhase | ✓ VERIFIED | Three `__gap_` vars at lines 282, 383, 484, 487; slot layout preserved |
| `contracts/modules/DegenerusGameAdvanceModule.sol` | AdvanceModule with no dailyEthPhase references | ✓ VERIFIED | Zero `dailyEthPhase` matches; future-ticket guard is `!dailyJackpotCoinTicketsPending` only |
| `contracts/BurnieCoin.sol` | Without rollDailyQuest, DailyQuestRolled, onlyDegenerusGameContract | ✓ VERIFIED | All three removed; `OnlyGame` error and `questModule` retained (still have callers) |
| `contracts/DegenerusGame.sol` | recordMintQuestStreak with GAME-only access | ✓ VERIFIED | Line 433: GAME-only check; NatSpec updated to "GAME contract only (via MintModule delegatecall)" |
| `contracts/interfaces/IDegenerusGame.sol` | Updated NatSpec for recordMintQuestStreak | ✓ VERIFIED | Line 217: "Called by GAME contract (via MintModule delegatecall)" |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `DegenerusGameJackpotModule.sol` | `DegenerusGameStorage.sol` | storage var deletion | ✓ WIRED | `dailyCarryoverEthPool`, `dailyCarryoverWinnerCap`, `dailyEthPhase` absent as live vars; only `__gap_` private vars remain |
| `DegenerusGameAdvanceModule.sol` | `DegenerusGameJackpotModule.sol` | payDailyJackpot call | ✓ WIRED | `payDailyJackpot(true, lvl, rngWord)` at AdvanceModule line 410; no preceding resume block |
| `DegenerusGameJackpotModule.sol` | `DegenerusQuests.sol` | quests.rollDailyQuest direct call | ✓ WIRED | Lines 533 and 632 in JackpotModule; zero occurrences in BurnieCoin.sol |
| `DegenerusGameMintModule.sol` | `DegenerusGame.sol` | recordMintQuestStreak GAME-only access | ✓ WIRED | Access check `msg.sender != ContractAddresses.GAME` — COIN path fully removed |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CARRY-01 | Plan 01 | Carryover budget 0.5% futurePrizePool, all to tickets | ✓ SATISFIED | `/ 200` at JackpotModule line 394; 100% routes to `nextPrizePool` |
| CARRY-02 | Plan 01 | Single-source carryover level (no multi-loop) | ✓ SATISFIED | One `_selectCarryoverSourceOffset` call yields one `carryoverSourceLevel` |
| CARRY-03 | Plan 01 | Phase 1 ETH state machine removed | ✓ SATISFIED | No `dailyEthPhase`, no Phase 1 block, no `DailyCarryoverStarted` event |
| CARRY-04 | Plan 01 | Storage vars deleted with layout-preserving gaps | ✓ SATISFIED | `__gap_slot0_byte30`, `__gap_dailyCarryoverEthPool`, `__gap_dailyCarryoverWinnerCap`, `__gap_dailyEthPoolBudget` present |
| CLEANUP-02 | Plan 02 | BurnieCoin.rollDailyQuest passthrough removed | ✓ SATISFIED | Zero occurrences in BurnieCoin.sol; interface clean |
| CLEANUP-03 | Plan 02 | recordMintQuestStreak COIN access removed | ✓ SATISFIED | GAME-only check at DegenerusGame.sol line 433 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `contracts/DegenerusQuests.sol` | 32 | Stale NatSpec: "Coin calls `rollDailyQuest()`" — now JackpotModule calls directly | ℹ️ Info | Comment only; no functional impact; not in scope for this phase |

No blocker or warning anti-patterns found in files modified by this phase.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 62 Solidity files compile | `npx hardhat compile --force` | "Compiled 62 Solidity files successfully" | ✓ PASS |
| No legacy carryover ETH vars in live scope | `grep -rn "dailyCarryoverEthPool\|dailyCarryoverWinnerCap\|dailyEthPhase" contracts/` (excluding `__gap_` lines) | 0 matches | ✓ PASS |
| No removed constants remain | `grep -c "DailyCarryoverStarted\|DAILY_CARRYOVER_MIN_WINNERS\|DAILY_REWARD_JACKPOT_LOOTBOX_BPS" JackpotModule.sol` | 0 | ✓ PASS |
| Budget is 0.5% (/ 200) | `grep "/ 200" JackpotModule.sol` | Line 394 found | ✓ PASS |
| rollDailyQuest removed from BurnieCoin | `grep -c "rollDailyQuest" BurnieCoin.sol` | 0 | ✓ PASS |
| recordMintQuestStreak GAME-only | `grep -A2 "recordMintQuestStreak" DegenerusGame.sol` | `msg.sender != ContractAddresses.GAME` only | ✓ PASS |

### Human Verification Required

None. All success criteria are verifiable programmatically via grep and compilation.

### Gaps Summary

No gaps. All 11 must-haves pass at all verification levels (exists, substantive, wired). The phase goal is fully achieved:

- The Phase 1 carryover ETH state machine is gone. Three storage vars have been replaced with layout-preserving gaps. The AdvanceModule no longer references any of the removed state.
- The carryover flow is now a single-pass ticket purchase at 0.5% of `futurePrizePool`, with one randomly-selected source level per jackpot day.
- `BurnieCoin.rollDailyQuest` is dead code that has been removed along with its event and modifier. JackpotModule calls `quests.rollDailyQuest()` directly.
- `recordMintQuestStreak` now enforces GAME-only access; the COIN path is gone from both the implementation and the interface NatSpec.
- 62 Solidity files compile cleanly with no errors.

One informational finding noted: `DegenerusQuests.sol` line 32 contains a stale comment referencing "Coin calls rollDailyQuest()" — the caller is now JackpotModule. This is cosmetic and outside this phase's scope.

---

_Verified: 2026-04-01T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
