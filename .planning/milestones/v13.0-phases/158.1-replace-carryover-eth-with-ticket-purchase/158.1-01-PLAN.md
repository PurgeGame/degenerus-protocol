---
phase: 158.1-replace-carryover-eth-with-ticket-purchase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: true
requirements: [CARRY-01, CARRY-02, CARRY-03, CARRY-04]

must_haves:
  truths:
    - "Carryover days 2-4 distribute tickets (not ETH) to trait winners at a single random source level"
    - "0.5% of futurePrizePool budget goes to nextPrizePool for carryover tickets"
    - "Phase 1 carryover state machine is completely removed -- no multi-call resumption"
    - "dailyCarryoverEthPool, dailyCarryoverWinnerCap, dailyEthPhase storage vars deleted"
    - "DailyCarryoverStarted event removed"
    - "AdvanceModule no longer references dailyEthPhase"
  artifacts:
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "Single-pass ticket carryover in Phase 0, no Phase 1"
      contains: "carryoverTicketUnits"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Storage layout without dailyCarryoverEthPool, dailyCarryoverWinnerCap, dailyEthPhase"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "AdvanceModule with no dailyEthPhase references"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "storage var deletion"
      pattern: "dailyCarryoverEthPool|dailyCarryoverWinnerCap|dailyEthPhase should NOT exist"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "payDailyJackpot call"
      pattern: "payDailyJackpot\\(true"
---

<objective>
Replace the Phase 1 carryover ETH distribution state machine with single-pass ticket distribution in Phase 0. Delete the 3 storage vars that supported the multi-call ETH loop, remove the DailyCarryoverStarted event, change the carryover budget to 0.5% of futurePrizePool (from 1%), and update AdvanceModule to remove dailyEthPhase references.

Purpose: Simplify the daily jackpot carryover from a resumable ETH distribution state machine into a single-pass ticket purchase. The Phase 1 ETH loop existed because ETH distribution could exceed gas limits; ticket distribution is cheaper and fits in one pass.

Output: Modified JackpotModule, Storage, and AdvanceModule with all Phase 1 carryover machinery removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL: Contract changes must NOT be committed by executor agents. Leave as unstaged working-tree modifications for user review. Only commit documentation files (SUMMARY.md, STATE.md, ROADMAP.md updates).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Phase 1 carryover state machine from JackpotModule and delete storage vars</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol,
    contracts/storage/DegenerusGameStorage.sol
  </files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol,
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
**DegenerusGameStorage.sol changes:**

1. Delete the `dailyEthPhase` declaration (currently at byte 30 of Slot 0). This is a `uint8` in the packed Slot 0. Replace it with a padding byte to preserve Slot 0 layout (compressedJackpotFlag must stay at byte 31). Change the variable to an unnamed gap:
   - Replace `uint8 internal dailyEthPhase;` with `uint8 private __gap_slot0_byte30;`
   - Update the Slot 0 layout comment table: change `| [30:31] dailyEthPhase ... |` to `| [30:31] __gap_slot0_byte30  uint8    UNUSED (layout preserved)          |`
   - Update the NatSpec below: remove the `dailyEthPhase` comment, replace with gap comment

2. Delete `dailyCarryoverEthPool` (Slot ~15, standalone uint256). Replace with a gap to preserve slot ordering:
   - Replace `uint256 internal dailyCarryoverEthPool;` with `uint256 private __gap_dailyCarryoverEthPool;`
   - Update the section header comment: change "Daily Jackpot Carryover State" to "Reserved Gaps (former carryover state)"
   - Update the NatSpec: remove the old description, add `/// @dev Layout gap (slot preserved for upgrade safety).`

3. Delete `dailyCarryoverWinnerCap` (uint16, follows dailyCarryoverEthPool). Replace with gap:
   - Replace `uint16 internal dailyCarryoverWinnerCap;` with `uint16 private __gap_dailyCarryoverWinnerCap;`
   - Update NatSpec similarly

NOTE: Comments must describe what IS, not what changed. Use "Layout gap" or "UNUSED", never "formerly X" or "replaced Y".

**DegenerusGameJackpotModule.sol changes:**

1. Remove the `DailyCarryoverStarted` event declaration (lines 64-69).

2. Remove the `DAILY_CARRYOVER_MIN_WINNERS` constant (line ~194). It is only used by the Phase 1 winner cap logic being deleted.

3. In `payDailyJackpotEth` (the daily path, `if (isDaily)`), simplify the resume check and restructure the Phase 0/Phase 1 flow:

   **Simplify the resume check at the top of the daily block.** Currently lines ~329-335 check `dailyEthPoolBudget != 0 || dailyEthPhase != 0` for resuming. Simplify to `bool isResuming = dailyEthPoolBudget != 0;` -- do NOT remove isResuming entirely. The resume guard is still needed because if Phase 0 ETH distribution reverts or runs out of gas mid-execution, `dailyEthPoolBudget` will be non-zero on the next call and the init block must be skipped. Only remove the `dailyEthPhase != 0` part since that variable no longer exists.

   **In the "Fresh start" initialization block** (the `if (!isResuming)` block -- this block and its guard STAY):
   - The winning traits roll (`_rollWinningTraits`, `_syncDailyWinningTraits`) stays conditional on `!isResuming`
   - Remove the `dailyCarryoverEthPool = futureEthPool;` assignment (line ~461)
   - Remove the `dailyEthPhase = 0;` assignment (line ~463)
   - The carryover budget calculation already computes `carryoverTicketUnits` and packs it into `dailyTicketBudgetsPacked` -- this stays

   **Carryover budget simplification (per D-02):**
   - Change `reserveSlice = _getFuturePrizePool() / 100` to `reserveSlice = _getFuturePrizePool() / 200` (0.5% of futurePrizePool)
   - Remove the `DAILY_REWARD_JACKPOT_LOOTBOX_BPS` usage for carryover. Instead, use the full `reserveSlice` as `carryoverLootboxBudget` directly (no BPS split needed since 100% goes to tickets now)
   - Remove the `futureEthPool` variable in the carryover section. The old code split: `futureEthPool = reserveSlice`, then deducted `carryoverLootboxBudget` from it, leaving remainder for ETH distribution. With no ETH distribution, this split is unnecessary
   - Simplify to: `carryoverLootboxBudget = _validateTicketBudget(reserveSlice, carryoverSourceLevel, winningTraitsPacked)`. All of `reserveSlice` goes to tickets. `_setNextPrizePool(_getNextPrizePool() + carryoverLootboxBudget)`.

   **Remove the `DAILY_REWARD_JACKPOT_LOOTBOX_BPS` constant** (line ~173) entirely if it has no other callers. Check: it is at line 173 and used at line 428. Line 428 is in the carryover init block being simplified. If that is the only usage, delete the constant.

   **Remove the `dailyCarryoverWinnerCap` computation** (the block at lines ~513-526 that computes remaining winner cap after daily ETH distribution). This entire block becomes dead code. Instead, after the Phase 0 daily ETH distribution finishes, go directly to `_clearDailyEthState()`.

   **Remove the Phase 1 block entirely** (lines ~540-583, the `if (dailyEthPhase == 1)` block). This is the carryover ETH distribution loop that sends ETH to trait winners. Delete it all.

   **Simplify the Phase 0 exit logic.** Currently after Phase 0 ETH distribution, there is a check `if (dailyCarryoverEthPool == 0 || dailyCarryoverWinnerCap == 0) { _clearDailyEthState(); return; }` followed by `dailyEthPhase = 1; return;`. Replace this entire exit with just `_clearDailyEthState(); return;` -- Phase 0 always completes in one call now.

4. Simplify `_clearDailyEthState`:
   - Remove `dailyEthPhase = 0;` (var no longer exists)
   - Remove `dailyCarryoverEthPool = 0;` (var no longer exists)
   - Remove `dailyCarryoverWinnerCap = 0;` (var no longer exists)
   - Keep: `dailyEthPoolBudget = 0;` and `dailyJackpotCoinTicketsPending = true;`

5. Remove `_highestCarryoverSourceOffset` function (lines ~2623-2636) -- only used by `_selectCarryoverSourceOffset`.

6. Remove `_selectCarryoverSourceOffset` function (lines ~2641-2684) -- replaced by the existing `_pickCarryoverSourceOffset` for single source level selection, OR inline a simpler selection. Actually, check the current code: `_selectCarryoverSourceOffset` IS the function called in the initialization block (line ~404). The `_pickCarryoverSourceOffset` mentioned in CONTEXT.md does not exist (it was a CONTEXT.md approximation). Keep the `_selectCarryoverSourceOffset` function since it is the actual source level picker used by the remaining ticket carryover flow. It stays.

   CORRECTION after re-reading code: The initialization block at line ~404 calls `_selectCarryoverSourceOffset(lvl, winningTraitsPacked, randWord, counter)` to pick a carryover source offset. This function and `_highestCarryoverSourceOffset` are still needed for the ticket carryover (selecting which future level to distribute tickets to). KEEP both functions.

7. Remove `_hasActualTraitTickets` function ONLY IF no other callers exist. Check first.

NOTE on carryover ticket flow: The carryover ticket distribution already happens in `payDailyJackpotCoinAndTickets` (Phase 2) at lines 721-730. The init block already computes `carryoverTicketUnits` and packs them via `_packDailyTicketBudgets`. With the reserve slice change from /100 to /200, the carryover ticket budget becomes 0.5% of futurePrizePool. The existing code at line ~439 (`_setNextPrizePool(_getNextPrizePool() + carryoverLootboxBudget)`) correctly routes the budget to nextPrizePool per D-02.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat compile --force 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "dailyCarryoverEthPool" contracts/storage/DegenerusGameStorage.sol returns 0 for the old var name (gap var uses different name)
    - grep -c "dailyCarryoverWinnerCap" contracts/storage/DegenerusGameStorage.sol returns 0 for the old var name
    - grep -c "dailyEthPhase" contracts/storage/DegenerusGameStorage.sol returns 0 for the old var name (gap uses __gap)
    - grep -c "__gap_slot0_byte30" contracts/storage/DegenerusGameStorage.sol returns at least 1
    - grep -c "__gap_dailyCarryoverEthPool" contracts/storage/DegenerusGameStorage.sol returns at least 1
    - grep -c "dailyEthPhase" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep -c "dailyCarryoverEthPool" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep -c "dailyCarryoverWinnerCap" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep -c "DailyCarryoverStarted" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep -c "DAILY_CARRYOVER_MIN_WINNERS" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep -c "DAILY_REWARD_JACKPOT_LOOTBOX_BPS" contracts/modules/DegenerusGameJackpotModule.sol returns 0
    - grep "getFuturePrizePool.*200" contracts/modules/DegenerusGameJackpotModule.sol returns the 0.5% line
    - grep "isResuming = dailyEthPoolBudget != 0" contracts/modules/DegenerusGameJackpotModule.sol matches the simplified resume check
    - npx hardhat compile --force exits 0 with no errors
  </acceptance_criteria>
  <done>
    Phase 1 carryover ETH state machine fully removed from JackpotModule. Storage vars replaced with layout-preserving gaps. Carryover budget changed to 0.5% of futurePrizePool, 100% converted to tickets. Resume check simplified to dailyEthPoolBudget-only. All 62 Solidity files compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dailyEthPhase references from AdvanceModule</name>
  <files>
    contracts/modules/DegenerusGameAdvanceModule.sol
  </files>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol
  </read_first>
  <action>
Two references to `dailyEthPhase` exist in AdvanceModule:

1. **Line ~305** -- Guard condition for future ticket processing:
   ```solidity
   if (
       !dailyJackpotCoinTicketsPending &&
       dailyEthPoolBudget == 0 &&
       dailyEthPhase == 0
   ) {
   ```
   Remove `dailyEthPhase == 0` from this condition. The remaining two conditions (`!dailyJackpotCoinTicketsPending && dailyEthPoolBudget == 0`) are sufficient to gate future ticket processing. `dailyEthPhase` was only needed because Phase 1 could be in progress between advanceGame calls; with no Phase 1, it is always 0.

2. **Line ~400** -- Jackpot phase resume check:
   ```solidity
   if (dailyEthPhase != 0 || dailyEthPoolBudget != 0) {
       payDailyJackpot(true, lastDailyJackpotLevel, rngWord);
       stage = STAGE_JACKPOT_ETH_RESUME;
       break;
   }
   ```
   Remove `dailyEthPhase != 0 ||` from the condition. Keep only `dailyEthPoolBudget != 0` as the resume trigger. The ETH budget still exists (it is the current-level daily ETH distribution budget set in Phase 0 of payDailyJackpot). The resume path exists because Phase 0 ETH distribution could revert or run out of gas mid-execution, leaving `dailyEthPoolBudget` non-zero. On the next advanceGame call, this resume condition re-enters payDailyJackpot which skips init (via `isResuming = dailyEthPoolBudget != 0` from Task 1) and resumes Phase 0 distribution.

Update the NatSpec comment at line ~397-399: change "Resume Phase 1 carryover ETH distribution" to "Resume daily ETH distribution." Remove the line about matching payDailyJackpot's isResuming condition since the condition is now simpler.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "dailyEthPhase" contracts/modules/DegenerusGameAdvanceModule.sol && npx hardhat compile --force 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "dailyEthPhase" contracts/modules/DegenerusGameAdvanceModule.sol returns 0
    - grep "dailyEthPoolBudget != 0" contracts/modules/DegenerusGameAdvanceModule.sol returns the simplified resume condition
    - grep "dailyJackpotCoinTicketsPending" contracts/modules/DegenerusGameAdvanceModule.sol still present (guard for future tickets)
    - npx hardhat compile --force exits 0 with no errors
  </acceptance_criteria>
  <done>
    AdvanceModule has zero references to dailyEthPhase. Resume condition simplified to dailyEthPoolBudget-only. Future ticket guard simplified. All files compile cleanly.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "dailyEthPhase\|dailyCarryoverEthPool\|dailyCarryoverWinnerCap\|DailyCarryoverStarted\|DAILY_CARRYOVER_MIN_WINNERS" contracts/` returns only the __gap vars in Storage
- `grep -c "DAILY_REWARD_JACKPOT_LOOTBOX_BPS" contracts/modules/DegenerusGameJackpotModule.sol` returns 0
- `npx hardhat compile --force` succeeds with 0 errors
- The carryover ticket flow still works: carryoverTicketUnits packed into dailyTicketBudgetsPacked, distributed in payDailyJackpotCoinAndTickets Phase 2
</verification>

<success_criteria>
- Phase 1 ETH carryover state machine completely removed from JackpotModule
- Storage vars replaced with layout-preserving gaps (no slot shift)
- Carryover budget is 0.5% of futurePrizePool, fully converted to tickets
- DAILY_REWARD_JACKPOT_LOOTBOX_BPS constant removed (no longer needed)
- isResuming simplified to dailyEthPoolBudget-only check (not removed entirely)
- AdvanceModule has zero dailyEthPhase references
- All 62 Solidity files compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/158.1-replace-carryover-eth-with-ticket-purchase/158.1-01-SUMMARY.md`
</output>
