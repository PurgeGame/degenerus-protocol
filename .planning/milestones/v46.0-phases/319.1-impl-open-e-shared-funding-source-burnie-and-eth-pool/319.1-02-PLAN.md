---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
plan: 02
type: execute
wave: 2
depends_on: ["319.1-01"]
files_modified: [test/fuzz/AfKingFundingWaterfall.t.sol, test/fuzz/AfKingConcurrency.t.sol, test/fuzz/AfKingSubscription.t.sol, test/gas/SweepPerPlayerWorstCaseGas.t.sol]
autonomous: true
requirements: [OPENE-01, OPENE-02, OPENE-03, OPENE-04]
user_setup: []

must_haves:
  truths:
    - "Every test-side `subscribe(...)` call site is updated to pass the NEW 6th `address fundingSource` argument (default `address(0)` for existing self-subs) so the suite compiles against the rippled subscribe signature (OPENE-01 signature facet)"
    - "The Sub packed-offset constants in every AfKing test file (OFF_DAILY/OFF_LASTSWEPT/OFF_PAIDTHROUGH/OFF_FLAGS) are re-derived from the post-repack `forge inspect AfKing storage-layout` and match the live layout — no silent vm.store mis-targeting (OPENE-01 slot facet)"
    - "The full AfKing Foundry suite recompiles and PASSES with no slot-index drift after the OPEN-E repack"
    - "Behavioral coverage proves OPENE-02/03: a funded source S covers M's ETH draw + BURNIE charge INCLUDING the window-1 (first subscribe) burn; default-self (fundingSource==0) is byte-equivalent to pre-OPEN-E"
    - "Behavioral coverage proves OPENE-04: an unapproved non-zero source is REFUSED at subscribe (revert NotApproved); a post-subscribe revoke does NOT stop an active sub (the day-31 renewal still draws/burns S — subscribe-only auth); the sub is stopped only by S defunding or M cancelling"
    - "Behavioral coverage proves LANDMINE A: a NORMAL sub setting fundingSource=VAULT does NOT inherit the never-cancel exemption (it is still cancelled on a funding skip)"
  artifacts:
    - path: "test/fuzz/AfKingFundingWaterfall.t.sol"
      provides: "re-derived Sub offsets + updated subscribe() call site + OPEN-E funding-source waterfall (default-self, cross-account ETH+BURNIE incl. window-1) + exemption-spoof regression"
      contains: "fundingSource"
    - path: "test/fuzz/AfKingSubscription.t.sol"
      provides: "re-derived Sub offsets + updated subscribe() call site + cross-account subscribe-only auth coverage + unapproved-source-refused + revoke-does-not-stop-active-sub"
      contains: "fundingSource"
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "re-derived Sub offsets (OFF_LASTSWEPT/OFF_PAIDTHROUGH/OFF_FLAGS) matching the post-repack layout + updated subscribe() call site"
      contains: "OFF_FLAGS"
    - path: "test/gas/SweepPerPlayerWorstCaseGas.t.sol"
      provides: "updated subscribe() call site (passes address(0)) so the gas harness compiles against the rippled signature"
      contains: "address(0)"
  key_links:
    - from: "test Sub offset constants"
      to: "post-repack forge inspect AfKing storage-layout"
      via: "re-derived OFF_* byte offsets"
      pattern: "OFF_FLAGS"
    - from: "every test subscribe(...) call site"
      to: "the rippled AfKing.subscribe(..., address fundingSource) signature"
      via: "appended 6th address(0) argument"
      pattern: "subscribe\\("
    - from: "OPEN-E behavioral tests"
      to: "AfKing.subscribe(fundingSource) / sweep routing / subscribe auth"
      via: "operator-approval-gated cross-account draw + window-1 burn + exemption-spoof + revoke-does-not-stop"
      pattern: "fundingSource"
---

<objective>
Update every AfKing test-side `subscribe(...)` call site to pass the NEW 6th `address fundingSource` argument (the Wave-1 signature ripple), re-derive the `Sub` packed-byte offset constants the tests hardcode for `vm.store` (they DRIFT when the bools collapse into `flags` and `fundingSource` is inserted), and add behavioral coverage proving the OPEN-E routing + the security landmines. This runs AFTER the Wave-1 3-contract diff is approved + committed.

Purpose: The subscribe signature gained a param, so all four test files with a `subscribe(...)` call no longer compile until they pass `address(0)`. The offset constants `OFF_LASTSWEPT=3`/`OFF_PAIDTHROUGH=7`/`OFF_FLAGS=12` were computed from the OLD layout; left stale, the `vm.store`-based tests read/write the wrong bytes and silently mis-test (RESEARCH Pitfall 4). The new behavioral tests lock the FINAL OPEN-E spec: default-self equivalence, cross-account funding INCLUDING the window-1 burn, the Vault/sDGNRS exemption-spoof refusal, the subscribe-only auth, and revoke-does-not-stop-active-sub.

Output: Four updated test files (AGENT-COMMITTED — test/ commits autonomously per feedback_no_contract_commits) with the subscribe-signature sync + re-derived offsets + OPEN-E coverage; the full AfKing suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md

<interfaces>
<!-- Grep-verified this session. The FINAL Wave-1 model is: fundingSource is a subscribe() PARAM, -->
<!-- there is NO setFundingSource setter and NO FundingSourceUpdated event. Tests set a source by -->
<!-- passing it as the new 6th subscribe arg; to RE-point, re-subscribe. Where RESEARCH.md describes -->
<!-- a setter/event, it is SUPERSEDED — do NOT write setFundingSource tests. -->

The FOUR test files carrying a `subscribe(...)` call site (must pass the new 6th arg) — VERIFIED HEAD:
  test/fuzz/AfKingFundingWaterfall.t.sol:306   afKing.subscribe(address(0), drainFirst, true, 1, 0);   → append `, address(0)`
  test/fuzz/AfKingConcurrency.t.sol:411        afKing.subscribe(address(0), false, true, 1, 0);        → append `, address(0)`
  test/fuzz/AfKingSubscription.t.sol:277       afKing.subscribe(address(0), false, true, q, 0);        → append `, address(0)`
  test/gas/SweepPerPlayerWorstCaseGas.t.sol:346 afKing.subscribe(address(0), false, true, 1, reinvestPct); → append `, address(0)`
  (DeployProtocol.sol mentions subscribe in COMMENTS only — no call to edit; the Vault/sDGNRS
   constructors call subscribe internally and were updated in Wave 1.)

The three AfKing test files carrying drifting Sub-offset constants — VERIFIED HEAD:
  test/fuzz/AfKingFundingWaterfall.t.sol:42-48   SUBOF_SLOT=1, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12, FLAG_WINDOW_PAID=1
    Usage: ~:354 builds a uint32 mask at OFF_LASTSWEPT/OFF_PAIDTHROUGH*8; ~:356 writes paidThroughDay;
           ~:186-187 read subscriptionOf(...).flags & FLAG_WINDOW_PAID
  test/fuzz/AfKingConcurrency.t.sol:41-49        SUBOF_SLOT=1, OFF_DAILY=0, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12, FLAG_WINDOW_PAID=1
    Usage: ~:441 reads lastSweptDay >> OFF_LASTSWEPT*8; ~:455-458 mask+write paidThroughDay+flags;
           ~:368-391 read subscriptionOf(...).dailyQuantity/.flags/.paidThroughDay
  test/fuzz/AfKingSubscription.t.sol:36-41       SUBOF_SLOT=1, OFF_DAILY=0, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12

AfKing external surface the tests drive (Wave-1 FINAL model, from 319.1-01):
  subscribe(address player, bool drain, bool tickets, uint8 qty, uint8 reinvestPct, address fundingSource) payable
    — the 6th param is the SOLE place a source is set; a non-zero non-self unapproved source reverts NotApproved
  subscriptionOf(address) returns (Sub)   — Sub now carries `address fundingSource`; the two bools are flag bits
  sweep(uint256)                          — ETH draw + both burnForKeeper sites route to the resolved source (self when 0)
  NO setFundingSource, NO FundingSourceUpdated — do not reference them.

Game / coin surfaces for cross-account setup (read-only, present):
  DegenerusGame.setOperatorApproval(operator, approved) — S (owner) grants M; isOperatorApproved(S, M) is the gate
  ContractAddresses.VAULT / SDGNRS — the exemption-keyed pinned identities

Bool-accessor note: a current grep of test/ shows ZERO reads of `.drainGameCreditFirst`/`.useTickets` typed
accessors, so no conversion is needed today; if any new test needs those bits, read them via
`(subscriptionOf(...).flags & FLAG_x) != 0` since the typed struct members are gone after the repack.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the Sub offsets from the post-repack layout + update every subscribe() call site (signature sync)</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingConcurrency.t.sol; test/fuzz/AfKingSubscription.t.sol; test/gas/SweepPerPlayerWorstCaseGas.t.sol; .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md (AFTER layout table)</read_first>
  <action>Run `forge inspect AfKing storage-layout` against the committed Wave-1 tree to read the AUTHORITATIVE post-repack byte offsets for `dailyQuantity`, `lastSweptDay`, `paidThroughDay`, `reinvestPct`, `flags`, and `fundingSource` within the Sub slot (do NOT trust RESEARCH's illustrative offsets — re-derive). Update the `OFF_DAILY`/`OFF_LASTSWEPT`/`OFF_PAIDTHROUGH`/`OFF_FLAGS` constants in the three fuzz files to the inspected values, and update the trailing byte-range comments to match. Add `OFF_FUNDING_SOURCE` (and `FLAG_DRAIN_FIRST`/`FLAG_USE_TICKETS` mirror constants if a test needs to set those bits via vm.store) where a test will exercise the new field. Audit each file's `vm.store` mask/shift expressions (e.g. AfKingConcurrency ~:455-458, AfKingFundingWaterfall ~:354-356) to confirm they reference the updated constants — fix any that hardcode a literal offset. THEN update all FOUR `subscribe(...)` call sites to pass the new 6th `address fundingSource` argument — append `, address(0)` to the existing self-subs at AfKingFundingWaterfall :306, AfKingConcurrency :411, AfKingSubscription :277, and SweepPerPlayerWorstCaseGas :346 (re-grep the exact line in case of drift). (No `.drainGameCreditFirst`/`.useTickets` typed-accessor reads exist in test/ today; if any are introduced, use the flag-bit form.)</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` (incl. tests) exits 0 — all four files compile against the repacked Sub ABI and the rippled subscribe signature.
    - Every `subscribe(...)` call site in the four files passes a 6th `address` argument (`grep -rn 'afKing.subscribe(' test/fuzz/AfKing*.t.sol test/gas/SweepPerPlayerWorstCaseGas.t.sol` shows each call carrying the new arg, e.g. `, address(0))`).
    - The `OFF_*` constants in the three fuzz files equal the `forge inspect AfKing storage-layout` byte offsets (record the inspected table in the SUMMARY and assert equality field-by-field).
    - No test references the now-removed `.drainGameCreditFirst`/`.useTickets` typed struct accessors (`grep -rn '\.drainGameCreditFirst\|\.useTickets' test/` shows only flag-bit forms or zero hits).
  </acceptance_criteria>
  <done>All four test files compile against the live post-repack layout and the rippled subscribe signature; offsets match `forge inspect` field-by-field; no stale typed-accessor reads remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add OPEN-E behavioral coverage — default-self equivalence, cross-account ETH+BURNIE (incl. window-1), exemption-spoof refusal, subscribe-only auth + revoke-does-not-stop</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingSubscription.t.sol; contracts/AfKing.sol (subscribe + sweep routing, from the Wave-1 commit); .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md §OPENE-02/03/04</read_first>
  <action>Extend the existing AfKing test contracts (do not create a new fixture — reuse the 318-01-repaired DeployProtocol) with OPEN-E cases that exercise the FINAL model (source set via the subscribe param, no setter). In AfKingFundingWaterfall.t.sol: (1) default-self — a sub with `fundingSource==address(0)` draws/burns exactly as today (regression that pre-OPEN-E paths are byte-equivalent); (2) cross-account ETH — S deposits to `_poolOf[S]`, S calls `setOperatorApproval(M, true)`, M subscribes with `fundingSource=S`, the sweep draws/debits `_poolOf[S]` not `_poolOf[M]`; (3) cross-account BURNIE INCLUDING window-1 — S approved, M subscribes with `fundingSource=S` while lacking an active pass, and the FIRST-window SUB-01 burn (:396) draws from S's BURNIE (proving S funds window 1); also the day-31 auto-extract burns from S; all-or-nothing shortfall on S falls through (subscribe revert / day-31 auto-pause); (4) LANDMINE A exemption-spoof — a NORMAL sub subscribes with `fundingSource=VAULT`, hits a funding skip, and is STILL cancelled via swap-pop (the exemption did NOT transfer); the real VAULT/SDGNRS self-subs stay exempt. In AfKingSubscription.t.sol: (5) unapproved-source-refused — subscribing with an UNAPPROVED non-zero non-self source reverts NotApproved; after S approves M it is honored; (6) revoke-does-not-stop-active-sub — after S approves M and M subscribes with `fundingSource=S`, S revokes (`setOperatorApproval(M, false)`); the day-31 renewal STILL draws/burns from S (no re-check — the active sub is trusted, subscribe-only auth); assert the sub is only stopped when S defunds (empty `_poolOf[S]` → InsufficientPool skip / insufficient BURNIE → day-31 auto-pause) or M cancels. Keep assertions specific (exact balances, exact set membership, exact revert selectors). Do NOT write any setFundingSource test — the setter does not exist.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract 'AfKingFundingWaterfall|AfKingSubscription' 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -E 'Suite result|FAIL' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract AfKingFundingWaterfall` reports `Suite result: ok` (0 failures), including default-self, cross-account ETH, cross-account BURNIE-incl-window-1, and the LANDMINE-A exemption-spoof cases.
    - `forge test --match-contract AfKingSubscription` reports `Suite result: ok` (0 failures), including unapproved-source-refused (revert NotApproved) and revoke-does-not-stop-active-sub.
    - The window-1 BURNIE test asserts the FIRST subscribe's SUB-01 burn debits S's BURNIE (not M's) when `fundingSource=S` and approved.
    - The exemption-spoof test asserts a NORMAL sub with `fundingSource=VAULT` IS cancelled (`subscriptionOf(victim).dailyQuantity == 0` + removed from the set) on a funding skip — proving LANDMINE A.
    - The revoke test asserts the day-31 renewal STILL draws/burns S after `setOperatorApproval(M,false)`, and that the sub stops only on S defunding or M cancelling.
    - No test references `setFundingSource` or `FundingSourceUpdated` (`grep -rn 'setFundingSource\|FundingSourceUpdated' test/` returns zero).
  </acceptance_criteria>
  <done>OPEN-E behavioral coverage passes — default-self equivalence, cross-account ETH+BURNIE funding incl. window-1, the exemption-spoof refusal (LANDMINE A), and the subscribe-only auth (unapproved-refused + revoke-does-not-stop) are all proven.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full AfKing-touching suite, confirm no slot drift, and AGENT-COMMIT the test sync</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingConcurrency.t.sol; test/fuzz/AfKingSubscription.t.sol; test/gas/SweepPerPlayerWorstCaseGas.t.sol</read_first>
  <action>Run the full AfKing-related suite (`AfKingConcurrency`, `AfKingFundingWaterfall`, `AfKingSubscription`, plus the crank/RNG-freeze suites that touch Sub via subscriptionOf: `CrankNonBrick`, `RngFreezeAndRemovalProofs`) to confirm the repack introduced NO slot-index drift regression — the pre-OPEN-E pass counts for the unrelated families must hold (compare against the 318 baseline recorded in STATE.md: the suite carried pre-existing baseline failures unrelated to AfKing; the OPEN-E change must not ADD any new failure). Then AGENT-COMMIT the four modified test files (no contract files — those were committed in Wave 1). The contract commit guard hook permits test/.planning commits while contracts/*.sol is clean; if it is dirty, the Wave-1 commit did not complete — stop and surface that. Commit message: `test(319.1-02): OPEN-E test-mirror offset sync + subscribe-param ripple + funding-source coverage [OPENE-01..04]`.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract 'AfKingConcurrency|AfKingFundingWaterfall|AfKingSubscription|CrankNonBrick|RngFreezeAndRemovalProofs' 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -E 'Suite result|FAIL' | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Every AfKing suite (`AfKingConcurrency`, `AfKingFundingWaterfall`, `AfKingSubscription`) reports `Suite result: ok` with 0 failures.
    - `CrankNonBrick` + `RngFreezeAndRemovalProofs` show no NEW failures vs the 318 baseline (the repack added no slot-drift regression to the Sub-reading suites).
    - The four test files are committed in one AGENT-COMMITTED commit with the OPENE-01..04 message; no `contracts/*.sol` is in the commit; nothing pushed.
  </acceptance_criteria>
  <done>The full AfKing-touching suite is green with no new slot-drift failures, and the test-mirror sync + subscribe ripple + OPEN-E coverage is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → AfKing storage | the tests use `vm.store` with hardcoded byte offsets; a stale offset writes the WRONG byte and silently mis-tests |
| test subscribe call → rippled signature | the gained `fundingSource` param breaks compilation until every call site passes the 6th arg |
| OPEN-E behavioral surface | the tests are the verification that the Wave-1 contract correctly gates cross-account funding at subscribe-only, funds window 1, and preserves both landmines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319.1-T1 | Tampering (test fidelity) | stale `vm.store` byte offsets after the repack | mitigate | offsets re-derived from `forge inspect AfKing storage-layout`; field-by-field equality asserted in Task 1; RESEARCH Pitfall 4. |
| T-319.1-T2 | Spoofing (regression coverage) | the Vault/sDGNRS exemption-spoof vector | mitigate | a dedicated test subscribes a NORMAL sub with `fundingSource=VAULT` and asserts it is STILL cancelled (LANDMINE A behavioral proof, Task 2). |
| T-319.1-T3 | Elevation of Privilege (regression coverage) | unapproved cross-account funding + post-revoke drain | mitigate | tests assert an unapproved non-zero source reverts NotApproved at subscribe; and that a post-subscribe revoke does NOT stop the active sub (renewal keeps drawing S) — the by-design trust-the-sub posture; stopping requires S defunding or M cancelling. |
| T-319.1-T4 | Tampering (no contract mutation) | test plan must not edit contracts/*.sol | mitigate | files_modified is test/ only; Task 3 asserts no `contracts/*.sol` in the commit and that the Wave-1 contracts are clean. |

No package-manager installs in this phase → no supply-chain (SC) row (no npm/pip/cargo; RESEARCH §Package Legitimacy Audit = N/A).
</threat_model>

<verification>
- `forge inspect AfKing storage-layout` re-run on the committed tree; offsets match the test constants field-by-field.
- All four test files compile against the rippled subscribe signature (each call passes the 6th `address` arg).
- All three AfKing suites green; `CrankNonBrick`/`RngFreezeAndRemovalProofs` show no new slot-drift failures vs the 318 baseline.
- LANDMINE A proven behaviorally: a `fundingSource=VAULT` NORMAL sub is still cancelled on a funding skip.
- OPEN-E cross-account ETH + BURNIE funding proven INCLUDING the window-1 burn; default-self regression holds.
- Auth proven: unapproved source refused at subscribe; a post-subscribe revoke does NOT stop the active sub (renewal keeps drawing S — trust-the-sub).
- No setFundingSource / FundingSourceUpdated references anywhere in test/.
- Test commit is AGENT-COMMITTED, test-only, not pushed.
</verification>

<success_criteria>
- OPENE-01: every test subscribe call passes the new 6th arg; test-mirror offsets re-derived from the live layout; suite recompiles green; no slot drift.
- OPENE-02/03: cross-account ETH draw + both BURNIE charge sites (incl. window-1) proven against the source; default-self byte-equivalence proven.
- OPENE-04 + LANDMINE A: subscribe-only operator-approval gating (unapproved-refused + revoke-does-NOT-stop-an-active-sub) and the Vault/sDGNRS exemption-spoof refusal proven.
- The full AfKing-touching suite passes with zero NEW failures; tests AGENT-COMMITTED.
</success_criteria>

<output>
Create `.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-02-SUMMARY.md` when done — include the re-derived offset table (vs the Wave-1 AFTER layout), the updated subscribe call-site inventory, the new-test inventory mapped to OPENE-02/03/04 + the two landmines, the suite pass counts vs the 318 baseline, and the test commit SHA.
</output>
