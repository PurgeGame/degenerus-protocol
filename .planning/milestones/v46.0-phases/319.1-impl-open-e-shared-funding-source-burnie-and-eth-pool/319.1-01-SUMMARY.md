# 319.1-01 SUMMARY — OPEN-E shared funding source (3-contract batched diff)

**Plan:** 319.1-01 · **Wave:** 1 · **Status:** complete
**Approved commit:** `42140ceb8a446efced00331cfe11e733050c508f` (USER-approved, not pushed)
**Files:** contracts/AfKing.sol (+85/−31 across all 3), contracts/DegenerusVault.sol, contracts/StakedDegenerusStonk.sol
**Requirements:** OPENE-01, OPENE-02, OPENE-03, OPENE-04

## Self-Check: PASSED

`forge build --skip test` → **CONTRACTS_BUILD_OK** (the three contracts compile cleanly).
Full `forge build` (incl. tests) is intentionally RED — the subscribe-signature change breaks the test call sites; Wave 2 (319.1-02) re-syncs them.

## Sub storage layout — BEFORE → AFTER (authoritative for Wave-2 test-mirror re-derivation)

`forge inspect AfKing storage-layout` shows TOP-LEVEL slots only (Sub is inside the
`mapping(address => Sub) _subOf` at slot 1, so its members are not expanded). The
within-slot byte offsets below are derived from the struct declaration order under
Solidity's byte-tight storage packing (no alignment padding; all fields fit in one slot).

**Top-level slots — UNCHANGED (slots 0–4):**
`_poolOf`(0) · `_subOf`(1) · `_subscribers`(2) · `_subscriberIndex`(3) · `_sweepDay`+`_sweepCursor`(4).

**Sub within-slot byte offsets (slot reached via `_subOf`):**

| Field | BEFORE offset | AFTER offset | Notes |
|-------|---------------|--------------|-------|
| dailyQuantity (uint8) | 0 | **0** | unchanged |
| drainGameCreditFirst (bool) | 1 | — | REMOVED → `flags` bit 1 |
| useTickets (bool) | 2 | — | REMOVED → `flags` bit 2 |
| lastSweptDay (uint32) | 3 | **1** | drifted 3→1 |
| paidThroughDay (uint32) | 7 | **5** | drifted 7→5 |
| reinvestPct (uint8) | 11 | **9** | drifted 11→9 |
| flags (uint8) | 12 | **10** | drifted 12→10; bit0=windowPaid, bit1=drainFirst, bit2=useTickets |
| fundingSource (address) | — | **11–30** | NEW (20 bytes) |
| free padding | 13–31 | 31 | |

**Wave-2 constants to set (`vm.store` offsets):**
`OFF_DAILY=0`, `OFF_LASTSWEPT=1`, `OFF_PAIDTHROUGH=5`, `OFF_REINVEST=9`, `OFF_FLAGS=10`, `OFF_FUNDING_SOURCE=11`.
Flag bits: `FLAG_WINDOW_PAID=1`, `FLAG_DRAIN_FIRST=2`, `FLAG_USE_TICKETS=4`.

## Per-requirement edit summary

- **OPENE-01** — `Sub` repacked: the two standalone bools collapse into `flags` (new `FLAG_DRAIN_FIRST=2`, `FLAG_USE_TICKETS=4` next to `FLAG_WINDOW_PAID=1`); `address fundingSource` appended in the SAME 32-byte slot (slot count 0–4 unchanged). Source set ONLY via the new `subscribe()` param — **no `setFundingSource` setter, no `FundingSourceUpdated` event**. `SubscriptionUpdated` event ABI byte-stable (still two bool params, now sourced from `(s.flags & FLAG_x) != 0`). Signature rippled to Vault + sDGNRS (pass `address(0)` = self).
- **OPENE-02** — ETH draw resolves a once-per-iteration `src = sub.fundingSource == 0 ? player : sub.fundingSource` (AfKing.sol:693); skip-gate read `_poolOf[src]` (724) and CEI debit `_poolOf[src] -= msgValue` (746) use the same `src`. Gas-neutral (single-key SLOAD). The VAULT/SDGNRS exemption (725) stays keyed on the un-spoofable `player`.
- **OPENE-03** — both `burnForKeeper` sites route to the resolved source: subscribe first-window burn (435) and day-31 renewal burn (631). `s.fundingSource = fundingSource` is written (422) BEFORE the first-window burn (435) → **S funds window 1**. All-or-nothing shortfall paths preserved (subscribe revert / day-31 auto-pause).
- **OPENE-04** — exactly ONE new cross-account gate `isOperatorApproved(fundingSource, subscriber)` in `subscribe()` (396), guarded by `fundingSource != 0 && != subscriber`; reverts `NotApproved`. `address(0)` (self) short-circuits the read. Auth checked at subscribe ONLY — never at renewal, never per-draw. **Revoke-does-not-stop-active-sub** by design (NatSpec documented). **BURNIE blast-radius caveat** + `allowBurnieFunding[S][M]` deferred alternative documented in NatSpec.

## Landmines preserved

- **LANDMINE A** — VAULT/SDGNRS exemption (AfKing.sol:725) compares `player`, never `src`; a NORMAL sub cannot inherit the never-cancel exemption via `fundingSource`.
- **LANDMINE B** — per-draw `isOperatorApproved(player, address(this))` (656) byte-unchanged; no second per-draw approval read added.

## Three-contract scope confirmation

- `contracts/AfKing.sol` — struct repack + FLAG constants + subscribe param/auth/write + 3 routing points + NatSpec caveats.
- `contracts/DegenerusVault.sol` — `IAfKingSubscribe.subscribe` decl + call site (:471) pass `address(0)`.
- `contracts/StakedDegenerusStonk.sol` — `IAfKingSubscribe.subscribe` decl + call site (:380) pass `address(0)`.

## Deviations / handoff to Wave 2

- **DEVIATION (must fix in Wave 2):** `test/fuzz/CrankNonBrick.t.sol` has TWO stale 5-arg `subscribe(...)` calls (:464, :490) that break compilation but are NOT in Wave 2's declared `files_modified`. Wave 2 MUST update these (append `, address(0)`) in addition to its four listed files, or the suite won't compile.
- Full list of stale test call sites at Wave-1 HEAD: AfKingConcurrency.t.sol:411, AfKingFundingWaterfall.t.sol:306, AfKingSubscription.t.sol:277, SweepPerPlayerWorstCaseGas.t.sol:346, **CrankNonBrick.t.sol:464 + :490**.
