---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 05
type: execute
wave: 3
depends_on: ["316-02"]
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: true
requirements: [JGAS-01]
must_haves:
  truths:
    - "316-SPEC.md contains a ## JGAS-01 Decision Gate section that traces the two-call split's DESIGN INTENT BEFORE locking deletion (per feedback_design_intent_before_deletion): pure block-gas-ceiling workaround at the 305-winner max-scale jackpot; resumeEthPool carries ONLY the uint128 pool-remainder between call1 (159+1=160 winners) and call2 (95+50=145 winners); the rng lock is held across both calls and the SAME randWord is re-consumed in call2; NO correctness/fairness/EV/determinism property is carried by the split."
    - "316-SPEC.md derives the THEORETICAL worst-case single-call 305-winner gas FIRST (per feedback_gas_worst_case): per-cold-winner credit + 2 events structure, ~9-12M gas estimate, ~2.5-3.3× margin vs ~30M block limit, citing the ~1.3M RM-02 frees (the unconditional cold autoRebuyState SLOAD × 305) and the observational-equivalence argument (single-call ≈ call1+call2 work minus the RM-02 frees)."
    - "316-SPEC.md LOCKS the JGAS-01 decision string verbatim: 'REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented' — the 305 ceiling is PRESERVED, no winner-count/bucket-scaling/payout-EV change; finality gated on the Phase-319 JGAS-04 empirical 305-winner single-call measurement (< block limit with margin)."
    - "316-SPEC.md enumerates + grep-verifies the JGAS deletion footprint across BOTH modules: JackpotModule SPLIT_NONE/CALL1/CALL2 (:197/199/201), JACKPOT_MAX_WINNERS=160 (:219, dead on removal), resumeEthPool (storage :994 slot 33; jackpot reads/writes :349/1201/1252/1253/1348), _resumeDailyEth (:1186), splitMode param (:1248) + call1Bucket mask (:1270-1278/1287-1288), the split-threshold branch (:476-483/480); AdvanceModule STAGE_JACKPOT_ETH_RESUME=8 (:70) + its assignment (:455) + the resume-check (:453-456) — recording the two cosmetic +1 drifts (jackpot :348→349, advance :452-455→453-456) and that DAILY_ETH_MAX_WINNERS=305 (:227) / DAILY_JACKPOT_SCALE_MAX_BPS=63_600 (:248) / the 159/95/50/1 buckets are NOT in the deletion set."
    - "316-SPEC.md states the J5 VRF/freeze-SAFE verdict: the ETH-resume branch never calls _unlockRng (the rng lock is held across the split + the same randWord re-consumed in call2); single-call collapses two same-word consumptions into one, does NOT move _unlockRng, adds NO new in-window player-mutable input, and removes a cross-tx resumeEthPool carry → a VRF-rotation-robustness IMPROVEMENT; stage numbers are NOT load-bearing (function-local stage, assign+emit only, Advance event not consumed on-chain); the only residual risk is gas-fits (liveness), not freeze/RNG; AUDIT-320 re-attests freeze under single-call + under VRF rotation."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "JGAS-01 design-lock section (## JGAS-01 Decision Gate — design-intent + worst-case-first gas + locked decision + deletion footprint + VRF verdict)"
      contains: "JGAS-01 Decision Gate"
  key_links:
    - from: "316-SPEC.md JGAS-01 decision gate"
      to: "316-02 ## Storage Slot-Shift Plan (resumeEthPool slot 33, combined −2)"
      via: "the JGAS footprint here, the slot-derivation consequence in 316-02 — no duplicate slot re-derivation"
      pattern: "resumeEthPool"
    - from: "316-SPEC.md JGAS-01 VRF verdict"
      to: "DegenerusGameAdvanceModule.sol:453-456 resume-check + _unlockRng placement"
      via: "freeze-invariant attestation (lock held across split, no _unlockRng in resume branch)"
      pattern: "_unlockRng"
---

<objective>
Author the `## JGAS-01 Decision Gate` section of `316-SPEC.md`: the daily-ETH two-call-split removal design lock. This is the new SPEC-primary-owned design lock (JGAS-01), folded into the milestone after the original 4 plans were written. It must trace the split's design intent BEFORE locking deletion, derive the theoretical worst-case single-call gas FIRST, lock the decision string, enumerate + grep-verify the deletion footprint across both modules, and state the VRF/freeze-SAFE verdict.

Purpose: JGAS-01 is the SPEC decision gate that authorizes (or withholds) the JGAS-02 IMPL deletion at Phase 317. A wrong lock here is high-cost two ways: (a) locking REMOVE without proving the gas-fits margin → a 305-winner daily-ETH jackpot that cannot complete in one call (a liveness brick on the busiest jackpot day); (b) a stage-machine edit in the VRF-rotation-sensitive `DegenerusGameAdvanceModule` that perturbs the freeze invariant. The research says SAFE on both — but the SPEC must STATE the verdict (with the source trace), not assume it.

Output: The `## JGAS-01 Decision Gate` section of `316-SPEC.md`. Zero `contracts/` and zero `test/` mutations — this plan only reads source and writes markdown.

Scope split with Plan 316-02: this plan owns the JGAS deletion-footprint enumeration + the design-intent/worst-case/decision/VRF-verdict prose. The storage-slot re-derivation CONSEQUENCE of deleting `resumeEthPool` (the combined RM-02+JGAS −2 shift) is owned by 316-02's `## Storage Slot-Shift Plan` — this plan cross-references it and does NOT re-derive slots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md

<interfaces>
<!-- The JGAS deletion footprint with RESEARCH §J1 RE-VERIFIED line numbers (all grep-confirmed against live HEAD this planning pass). Where the requirement text cites a comment line, the live `if` guard is +1 — record the cosmetic drift. Re-grep each before writing (SC#5). -->

modules/DegenerusGameJackpotModule.sol (RESEARCH §J1.1 — all ✓ MATCH, two cosmetic +1 drifts):
- SPLIT_NONE=0 :197, SPLIT_CALL1=1 :199, SPLIT_CALL2=2 :201 (uint8 private constant) — DELETE all three.
- JACKPOT_MAX_WINNERS=160 :219 — the split-threshold constant; its SOLE functional use is the threshold at :480; DEAD on removal → DELETE. (NOT a winner-count cap.)
- DAILY_ETH_MAX_WINNERS=305 :227, DAILY_JACKPOT_SCALE_MAX_BPS=63_600 :248 — PRESERVED (the ceiling + max-scale, NOT in the deletion set).
- bucket sizes 159/95/50/1 (sum 305; call1=159+1=160, call2=95+50=145) — doc-comment :226/:246-247 — PRESERVED (bucket derivation `bucketCountsForPoolCap`/`capBucketCounts` untouched).
- resumeEthPool resume-check (jackpot): `if (resumeEthPool != 0)` at :349 (req cites :348 = the leading comment line → +1 DRIFT, cosmetic). `_resumeDailyEth(lvl, randWord)` called at :350.
- _resumeDailyEth function decl :1186 — DELETE.
- splitMode param (in `_processDailyEth`) :1248; local derived :476/:480/:501; reads :1251 (`if (splitMode == SPLIT_CALL2)`) — DELETE the param + routing.
- call1Bucket mask :1270 (decl), build :1271-1278, skip-routing :1287-1288 — DELETE.
- split-threshold branch: `splitMode = (totalWinners <= JACKPOT_MAX_WINNERS) ? SPLIT_NONE : SPLIT_CALL1;` :476-483; `_processDailyEth(... splitMode ...)` call :493-503 — collapse to the unconditional single-call path.
- resumeEthPool writes/reads: write `resumeEthPool = uint128(ethPool)` ~:1347-1348 (call1); read+zero `ethPool = uint256(resumeEthPool); resumeEthPool = 0;` :1252-1253 (call2); :1201 (`_resumeDailyEth` reads `uint256(resumeEthPool)`).

modules/DegenerusGameAdvanceModule.sol (RESEARCH §J1.2 — ✓ MATCH, one cosmetic +1 drift):
- STAGE_JACKPOT_ETH_RESUME=8 :70 (NatSpec :68-69; req cites :68-70 = doc+decl) — DELETE the constant.
- resume-check + stage handler: `if (resumeEthPool != 0) { payDailyJackpot(true, lvl, rngWord); stage = STAGE_JACKPOT_ETH_RESUME; break; }` :453-456 (req cites :452-455 = leading comment line → +1 DRIFT, cosmetic). Assignment of the stage constant at :455. — DELETE the whole resume-check block.

storage/DegenerusGameStorage.sol (RESEARCH §J1.1 / §J3):
- `uint128 internal resumeEthPool;` :994 — forge slot 33 (its OWN slot; vrfCoordinator starts fresh at slot 34). DELETE. (The slot-shift CONSEQUENCE is owned by 316-02; this plan enumerates the decl as a footprint item only.)

Stage-machine load-bearing analysis (RESEARCH §J2 — verified three ways):
- `stage` is a function-local `uint8` (declared `:260` inside `advanceGame`), never written to storage. No storage-side stage field exists.
- `STAGE_JACKPOT_ETH_RESUME` is ONLY assigned (:455) + emitted (`Advance` event); ZERO `==` comparisons. The only `STAGE_* ==` anywhere is `goStage == STAGE_TICKETS_WORKING` (:188) against a returned local, not a stored stage.
- The `Advance` event (decl :52; emitted :187/:227/:477) is NOT consumed on-chain (the two `gameAdvance()` wrappers DegenerusVault.sol:500 / StakedDegenerusStonk.sol:398 just call advance, do not read the stage). → renumbering 9/10/11 is OPTIONAL/cosmetic; deleting constant 8 + its single assignment is sufficient. No load-bearing renumber hazard.

VRF / freeze-invariant trace (RESEARCH §J5 — HEADLINE):
- Daily ETH jackpot consumes the VRF-derived `randWord`. BOTH call1 (`payDailyJackpot(true, lvl, rngWord)` advance :473) and call2 (`payDailyJackpot(true, lvl, rngWord)` → `_resumeDailyEth(lvl, randWord)` advance :454 / jackpot :350) consume the SAME `randWord` (call2 re-rolls `_rollWinningTraits(randWord)` jackpot :1188 from the held word).
- The ETH-resume branch (:453-456) does NOT call `_unlockRng` — the rng lock (`rngLockedFlag`) stays SET, `rngWordCurrent` stays non-zero, the pool stays frozen across the split. `_unlockRng` is called only at the coin-tickets stage (:467) + the phase-ended / non-jackpot transition paths (:331/:402/:629).
- Single-call collapses two same-word consumptions into one; `_unlockRng` placement UNCHANGED (still the coin-tickets stage :467); no new in-window player-mutable SLOAD (RM-02 removes the `autoRebuyState` read — opposite direction); resume driven by `resumeEthPool != 0` storage read, NOT a stored stage number. Removing a cross-tx `resumeEthPool` carry is a VRF-rotation-robustness IMPROVEMENT (single-call cannot orphan a mid-jackpot index across a tx boundary where an emergency rotation could intervene — consistent with the Phase 312 `a303ae18` detect-preserve-re-issue work).

Worst-case + headroom (RESEARCH §J4):
- J4.2 theoretical worst-case single-call: per cold-new winner ≈ 25-30k gas (cold SSTORE to `claimableWinnings` + `PlayerCredited` + `JackpotEthWin` events) × 305 ≈ 7.6-9.2M, + fixed overhead (4× `_randTraitTicket`, bucket-share math, `bucketCountsForPoolCap`, solo-bucket whale-pass path, prize-pool SSTOREs) ≈ 1-3M → ≈ 9-12M total vs ~30M block limit (~2.5-3.3× margin). Observational equivalence: single-call ≈ call1(160 winners) + call2(145 winners) work − (305 × RM-02-freed SLOAD).
- J4.3 RM-02 frees: per daily-ETH winner an unconditional cold `autoRebuyState[beneficiary]` SLOAD (~4.2k) + the conditional `_processAutoRebuy` branch, removed from `_addClaimableEth` (:800-806/801-804) → ~1.3M off the worst-case single-call total.
- J4.4 disposition to LOCK: "REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented." Makeable at SPEC (REMOVE) with the Phase-319 JGAS-04 empirical gate; if JGAS-04 measures over/near the block limit, IMPL reverts to keeping the split (judged unlikely given the margin + observational equivalence).

Downstream owners (do NOT run now; note for the SPEC): JGAS-02 = Phase 317 IMPL (delete in the SAME batched USER-APPROVED diff); JGAS-03 = Phase 318 TST (305-winner single-call correctness + split grep-clean); JGAS-04 = Phase 319 GAS (empirical 305-winner single-call measurement, the finality gate); Phase 320 TERMINAL delta-audits the split removal + re-attests freeze under single-call + under VRF rotation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the JGAS-01 Decision Gate section (design-intent + worst-case-first gas + locked decision + deletion footprint + VRF verdict)</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §J1 (deletion-footprint verification table — USE these line numbers, note the two +1 resume-check drifts), §J2 (stage-machine NOT load-bearing — three-way proof), §J3 (the combined RM-02+JGAS −2 slot consequence — for the cross-reference to 316-02 ONLY; do not re-derive slots here), §J4 (design-intent J4.1, theoretical worst-case J4.2, RM-02-freed headroom J4.3, the J4.4 locked disposition), §J5 (VRF/freeze-SAFE verdict — `_unlockRng` placement, lock-held-across-split, cross-tx-carry-removal as rotation improvement), §J6 (planning recommendation).
    - `REQUIREMENTS.md` JGAS-01 (the decision-gate text + the BOTH-modules footprint), the JGAS preamble (RM-02 enabling), and the Out-of-Scope row confirming no winner-count/EV change.
    - `ROADMAP.md` the Phase 316 goal's JGAS-01 paragraph + SC#4 (the JGAS-01 decision is locked there).
    - `316-SPEC.md` 316-02's `## Storage Slot-Shift Plan` (confirm it already records the combined −2 / `resumeEthPool` slot 33 consequence, so this section cross-references rather than re-derives).
    - Re-grep each cited file:line in the `<interfaces>` block against HEAD before writing (SC#5 obligation) — confirm `SPLIT_*` :197/199/201, `JACKPOT_MAX_WINNERS=160` :219, `DAILY_ETH_MAX_WINNERS=305` :227, `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` :248, jackpot resume-check :349, `_resumeDailyEth` :1186, `splitMode` :1248, `call1Bucket` :1270, advance `STAGE_JACKPOT_ETH_RESUME=8` :70 + resume-check :453-456, storage `resumeEthPool` :994.
  </read_first>
  <action>
    Append a `## JGAS-01 Decision Gate` section to `316-SPEC.md`. Lock, with exact prose (NO fenced solidity bodies — name symbols/identifiers/line numbers, not implementations), in this order (design-intent → worst-case → decision → footprint → VRF verdict):
    (1) DESIGN INTENT trace FIRST (per `feedback_design_intent_before_deletion` — trace intent before locking the deletion shape): the two-call split is a PURE block-gas-ceiling workaround, NOT a correctness/fairness mechanism. State: the ceiling that forced it (max scale `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` → `DAILY_ETH_MAX_WINNERS=305` across 4 buckets 159/95/50/1); the split threshold `JACKPOT_MAX_WINNERS=160` (:480) — `totalWinners ≤ 160 → SPLIT_NONE` one call, else `SPLIT_CALL1` two calls; how it partitions (call1 = largest+solo = 159+1 = 160 winners, call2 = the two mid buckets = 95+50 = 145); that `resumeEthPool` carries ONLY the uint128 pool-remainder between calls (winner set / trait IDs / shares / entropy are re-derived deterministically in call2 from the SAME held `randWord` via `_resumeDailyEth`/`_rollWinningTraits`); how advanceGame pauses/resumes (call1 in the fresh-daily path, the NEXT advanceGame sees `resumeEthPool != 0` and runs call2 — two bountied advance txs complete one daily ETH jackpot); the actor game-theory (escalating `bountyMultiplier`; merging the chunks merges two bounties into one). LOCK the clean precondition: NO correctness/fairness/EV/determinism property is carried by the split — single-call and two-call produce IDENTICAL payouts (same winners, same per-winner amounts, same solo-bucket whale-pass treatment); it is observationally equivalent modulo gas.
    (2) THEORETICAL worst-case single-call gas SECOND (per `feedback_gas_worst_case` — derive the worst case FIRST, before any reliance): state the per-cold-new-winner cost structure (cold SSTORE to `claimableWinnings` ~22k + `PlayerCredited` event + one `JackpotEthWin` event per winner) ≈ 25-30k × 305 ≈ 7.6-9.2M for the winner-credit loop + fixed overhead (4× `_randTraitTicket`, bucket-share math, `bucketCountsForPoolCap`, solo-bucket whale-pass path, prize-pool SSTOREs) ≈ 1-3M → THEORETICAL worst-case ≈ 9-12M gas vs ~30M block limit (~2.5-3.3× margin). State what RM-02 frees (the unconditional cold `autoRebuyState` SLOAD ~4.2k × 305 ≈ ~1.3M off the path) as the enabling headroom. State the observational-equivalence strengthener: single-call total ≈ call1(160-winner) + call2(145-winner) work − (305 × RM-02-freed SLOAD); today call1 demonstrably pays 160 winners in one tx under the limit, so single-call simply sums the SAME total work into one tx. Mark the absolute 9-12M figure as a structural estimate (±30%), not a measurement — that is exactly why finality is gated on JGAS-04.
    (3) The DECISION GATE resolved + LOCKED THIRD: lock the decision string VERBATIM — "REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented." State: the 305-winner ceiling is PRESERVED; NO winner-count / bucket-scaling / payout-EV change (`DAILY_ETH_MAX_WINNERS=305`, `DAILY_JACKPOT_SCALE_MAX_BPS=63_600`, the 159/95/50/1 derivation are NOT in the deletion set — only the split MECHANISM that chunks the same 305 winners); the JGAS-02 IMPL deletes the split in the same batched diff at Phase 317; finality of the lock is gated on JGAS-04's Phase-319 empirical 305-winner single-call measurement confirming < block limit with margin; RETAIN-fallback documented = if JGAS-04 measures over/uncomfortably-near the limit, the IMPL keeps the split (judged unlikely given the margin + observational equivalence). Note this is a decision MAKEABLE at SPEC (REMOVE) with an empirical confirmation gate — NOT blocked-until-measurement.
    (4) The enumerated + grep-verified DELETION FOOTPRINT FOURTH, across BOTH modules, using the §J1 re-verified line numbers (record the two cosmetic +1 drifts): JackpotModule — `SPLIT_NONE/CALL1/CALL2` (:197/199/201), `JACKPOT_MAX_WINNERS=160` (:219, sole use the threshold :480, DEAD on removal), `resumeEthPool` (storage decl :994 slot 33; jackpot reads/writes :349/:1201/:1252-1253/~:1347-1348), `_resumeDailyEth` (:1186), `splitMode` param (:1248, routing :1251/:476/:480/:501), `call1Bucket` mask (:1270 decl / :1271-1278 build / :1287-1288 skip-routing), the split-threshold branch (:476-483, call :493-503) collapsed to the unconditional single-call; AdvanceModule — `STAGE_JACKPOT_ETH_RESUME=8` (:70) + its single assignment (:455) + the whole resume-check block `if (resumeEthPool != 0) {...}` (:453-456). Record the two +1 drifts: jackpot resume-check req `:348`→live `:349` (comment vs `if`); advance resume-check req `:452-455`→live `:453-456`. State that the stage numbers are NOT load-bearing (§J2: `stage` is function-local, `STAGE_JACKPOT_ETH_RESUME` assign+emit only / zero comparisons, the `Advance` event not consumed on-chain) → renumbering 9/10/11→8/9/10 is OPTIONAL/cosmetic; delete constant 8 + its assignment + the resume-check; document the benign off-chain `Advance`-event note (stage 8 never emitted post-removal). Cross-reference (do NOT re-derive): the `resumeEthPool` slot-33 deletion compounds the RM-06 storage shift to −2 for the slot-≥34 region — that re-derivation is owned by 316-02's `## Storage Slot-Shift Plan` (one combined `forge inspect` pass on the post-(RM-02+JGAS) contract). Confirm the scope-fence: no footprint item forces a winner-count/EV touch.
    (5) The J5 VRF / freeze-invariant SAFE VERDICT FIFTH (HEADLINE — STATE it, do not assume it, per `feedback_security_over_gas` + the v45 VRF-freeze north-star): the daily-ETH stage consumes the VRF `randWord`; both call1 and call2 consume the SAME word; the ETH-resume branch (:453-456) does NOT call `_unlockRng` — the rng lock is HELD across the entire split (call1 → next advanceGame → call2) and the pool stays frozen. Lock the four sub-points: (a) single-call collapses two same-word consumptions into one (fewer consumption points = strictly simpler freeze surface, never weaker; still consumed inside the locked window); (b) `_unlockRng` placement is UNCHANGED (still the coin-tickets stage :467 — the unlock timing relative to VRF consumption does not move); (c) no new player-mutable in-window input (single-call reads the same `traitBurnTicket[lvl]`/held `randWord`; RM-02 removes the `autoRebuyState` read — opposite direction; `dailyHeroWagers[dailyIdx]` keyed on the frozen `dailyIdx`, unchanged); (d) no coupling between stage NUMBER and VRF unlock (resume driven by `resumeEthPool != 0`, NOT the emitted stage; deleting stage-constant 8 cannot perturb VRF sequencing). LOCK the verdict: JGAS is freeze-invariant-SAFE — it removes a VRF-word RE-consumption point + a cross-tx `resumeEthPool` carry (a VRF-rotation-robustness IMPROVEMENT, consistent with the Phase 312 `a303ae18` work), the only residual risk is the gas-fits (liveness) question gated on JGAS-04 — NOT a freeze/RNG-manipulability concern. State the AUDIT-320 re-attestation charge: freeze holds under the post-removal single-call AND under emergency VRF rotation (single-call cannot orphan a mid-jackpot index — strictly less rotation-exposed than the two-call split). Route the "does the stage-machine edit perturb the freeze invariant under rotation" final check to the `zero-day-hunter` skill at AUDIT (name the skill, do NOT run it here).
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## JGAS-01 Decision Gate' "$F" && grep -q 'STAGE_JACKPOT_ETH_RESUME' "$F" && grep -q 'resumeEthPool' "$F" && grep -q '305' "$F" && grep -q 'slot 33' "$F" && grep -q 'REMOVE pending JGAS-04 empirical confirmation' "$F" && grep -q '_unlockRng' "$F" && grep -q '_resumeDailyEth' "$F" && grep -qiE 'design intent|design-intent' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## JGAS-01 Decision Gate` section.
    - The section traces the two-call split's DESIGN INTENT FIRST (pure gas-ceiling workaround; `resumeEthPool` carries only the uint128 pool-remainder; rng lock held across both calls + same `randWord` re-consumed in call2; call1=160 / call2=145; NO correctness/fairness/EV/determinism property carried — observationally equivalent modulo gas), per `feedback_design_intent_before_deletion`.
    - The section derives the THEORETICAL worst-case single-call 305-winner gas SECOND (≈9-12M, ~2.5-3.3× margin vs ~30M, citing the ~1.3M RM-02 frees + the observational-equivalence argument; the absolute figure flagged as a ±30% estimate not a measurement), per `feedback_gas_worst_case`.
    - The section LOCKS the decision string VERBATIM "REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented", states the 305 ceiling is PRESERVED with NO winner-count/bucket-scaling/EV change, and gates finality on the Phase-319 JGAS-04 empirical measurement.
    - The section enumerates + records as grep-verified the JGAS deletion footprint across BOTH modules (JackpotModule `SPLIT_*` :197/199/201, `JACKPOT_MAX_WINNERS=160` :219 dead-on-removal, `resumeEthPool` storage :994 slot 33 + reads :349/1201/1252-1253/~1347-1348, `_resumeDailyEth` :1186, `splitMode` :1248, `call1Bucket` :1270-1278/1287-1288, threshold branch :476-483; AdvanceModule `STAGE_JACKPOT_ETH_RESUME=8` :70 + assignment :455 + resume-check :453-456), records the two cosmetic +1 drifts (jackpot :348→349, advance :452-455→453-456), states `DAILY_ETH_MAX_WINNERS=305`/`DAILY_JACKPOT_SCALE_MAX_BPS=63_600`/the 159/95/50/1 buckets are NOT in the deletion set, and states stage numbers are NOT load-bearing (renumber optional).
    - The section cross-references 316-02's `## Storage Slot-Shift Plan` for the `resumeEthPool` slot-33 −2 compounding consequence and does NOT re-derive slots itself.
    - The section states the J5 VRF/freeze-SAFE verdict: the ETH-resume branch never calls `_unlockRng` (lock held across the split, same `randWord` re-consumed in call2); single-call collapses two consumptions to one, `_unlockRng` placement unchanged, no new in-window player-mutable input, removes a cross-tx `resumeEthPool` carry (rotation-robustness improvement); only residual risk = gas-fits/liveness; AUDIT-320 re-attests freeze under single-call + under rotation; routes the final check to `zero-day-hunter` at AUDIT.
    - NO fenced solidity implementation bodies in the section (symbols/line numbers/decision string named as prose only).
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## JGAS-01 Decision Gate` section locking the design-intent trace, the worst-case-first gas derivation, the verbatim "REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented" decision, the two-module deletion footprint (with the two +1 drifts + the 305-ceiling-preserved scope fence), and the VRF/freeze-SAFE verdict — cross-referencing 316-02 for the slot-shift consequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (cost of a wrong JGAS design lock)

| Boundary | Description |
|----------|-------------|
| JGAS-01 SPEC lock → Phase 317 JGAS-02 deletion | The SPEC decision gate authorizes the IMPL deletion; locking REMOVE without the gas-fits margin proven greenlights a deletion that can brick the busiest jackpot day. |
| stage-machine edit → VRF freeze invariant (AdvanceModule) | A change in the VRF-rotation-sensitive `DegenerusGameAdvanceModule` is exactly the class that can perturb the freeze invariant; the SPEC must STATE the SAFE verdict with the `_unlockRng`-placement trace, not assume it. |
| `resumeEthPool` slot deletion → v45 VRF slot work | Deleting `resumeEthPool` (slot 33) compounds the storage shift to −2 for the slot-≥34 `vrf*`/`lootboxRng*` family; a missed compounding breaks the v45 VRF slot work (re-derivation owned by 316-02, cross-referenced here). |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-17 | Denial of Service | A SPEC that locks REMOVE without the 305-winner single-call gas-fits margin proven (liveness brick on the busiest jackpot day) | mitigate | Task 1 derives the theoretical worst-case FIRST (~9-12M vs ~30M, ~2.5-3.3× margin + observational equivalence + RM-02-freed ~1.3M) and locks finality on the JGAS-04 Phase-319 empirical gate with a RETAIN-fallback. |
| T-316-18 | Tampering | A stage-machine edit in the VRF-rotation-sensitive AdvanceModule that perturbs the freeze invariant | mitigate | Task 1 STATES the J5 SAFE verdict with the `_unlockRng`-placement trace (lock held across the split, unlock unmoved, stage numbers non-load-bearing) + routes the final under-rotation check to `zero-day-hunter` at AUDIT-320. |
| T-316-19 | Elevation | A SPEC that silently changes winner-count / bucket-scaling / payout-EV under cover of the split removal | mitigate | Task 1 locks the scope fence: `DAILY_ETH_MAX_WINNERS=305` / `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` / the 159/95/50/1 derivation are PRESERVED (not in the deletion set); only the split MECHANISM is removed. |
| T-316-20 | Denial of Service | A SPEC that locks the `resumeEthPool` deletion without flagging the −2 slot compounding (mis-derived v45 VRF slots) | mitigate | Task 1 cross-references 316-02's `## Storage Slot-Shift Plan` (combined RM-02+JGAS −2; `lootboxRngWordByIndex` 38→36) so the slot re-derivation is one combined forge-inspect pass, not a naive −1. |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed by this SPEC phase (read-only doc authoring); no install tasks exist. |
</threat_model>

<verification>
- `316-SPEC.md` contains the `## JGAS-01 Decision Gate` section header.
- The design-intent trace precedes the worst-case derivation, which precedes the locked decision (intent-before-deletion + worst-case-first ordering both honored).
- Every cited JGAS file:line is grep-confirmed against HEAD (the two +1 resume-check drifts recorded); no unverified call-graph claim.
- The slot-shift consequence is cross-referenced to 316-02, not re-derived (no duplicate slot derivation).
- No `.sol`/`.t.sol` file appears in this plan's git diff.
</verification>

<success_criteria>
- ROADMAP SC#4's JGAS-01 clause (theoretical worst-case single-call 305-winner gas derived FIRST, the gate resolved, the deletion footprint enumerated + grep-verified across BOTH modules, 305 ceiling preserved) is satisfied by this section.
- JGAS-01 design (REMOVE pending JGAS-04, RETAIN-fallback documented) is locked with the design-intent trace + the freeze-SAFE verdict.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-05-SUMMARY.md` when done.
</output>
</output>
