---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "316-SPEC.md locks the ADD-half do-work entry signatures + work-type encoding (caller-list bets with BatchAlreadyTaken short-circuit, parameterless box cursor per OPEN-D, sub cursor)."
    - "316-SPEC.md locks the reward formula (gasUnits(workType)·0.5gwei via _ethToBurnieValue, reserved per-work-type gas-peg constants, price-unavailable→reward-0-never-revert per OPEN-B) and the one-creditFlip-per-cranker-per-tx batching."
    - "316-SPEC.md locks the batchPurchase(players[],amounts[],modes[]) try/catch/slice-refund shape, batch-level rngLocked/game-over pre-check, and the OPEN-C reentrancy disposition (CEI-proof with guard-fallback note)."
    - "316-SPEC.md locks the cursor sweep design (sweep(maxCount) + daily sweepCursor self-partition + tombstone-on-cancel + in-sweep swap-pop reclaim + windowPaid bit + stall-escalating bounty) and the authorization model (subscribe-time self-or-operator consent, never at sweep) + pass-OR-pay gate via hasAnyLazyPass."
    - "316-SPEC.md states the 5 PROTO additions with exact signatures, all gating on the pinned AF_KING constant; PROTO-01 = rename _hasAnyLazyPass private→external view, no body change."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "ADD-design-lock sections (## ADD Design — Do-Work Crank, ## ADD Design — Subscription Sweep, ## PROTO Additions)"
      contains: "batchPurchase"
  key_links:
    - from: "316-SPEC.md PROTO additions"
      to: "ContractAddresses.sol AF_KING pinned constant"
      via: "PROTO-05 keeper-address gating"
      pattern: "AF_KING"
    - from: "316-SPEC.md reward formula"
      to: "DegenerusGameMintModule.sol:1412 _ethToBurnieValue"
      via: "OPEN-B-safe guarded conversion idiom"
      pattern: "_ethToBurnieValue"
---

<objective>
Author the ADD-half design-lock sections of `316-SPEC.md`: the do-work crank entry signatures + work-type encoding, the reward/charge model, the `batchPurchase` shape, the subscription cursor sweep design, authorization + pass-gate, and the 5 PROTO additions with exact signatures.

Purpose: This is the load-bearing design substrate Phase 317 IMPL builds from. Every signature, encoding decision, and disposition locked here becomes a non-negotiable instruction for the batched contract diff. A wrong or under-specified lock here (e.g. a settable exemption flag, a per-item creditFlip, a box cursor that ignores the VRF-rotation orphan-index coupling) propagates to a broken or unsafe IMPL.

Output: The ADD-design sections of `.planning/phases/316-*/316-SPEC.md`. Zero `contracts/` and zero `test/` mutations — this plan only reads source and writes markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md

<interfaces>
<!-- Source anchors the ADD design must cite. ALL grep-verified in 316-RESEARCH.md §1.10/§1.11/§1.12 against HEAD. The executor uses these directly — re-grep each before writing to satisfy SC#5; do NOT invent line numbers. -->

Reward conversion idiom (OPEN-B-safe by construction):
- `_ethToBurnieValue(amountWei, priceWei)` at `contracts/modules/DegenerusGameMintModule.sol:1412` — private pure; guards `if (amountWei == 0 || priceWei == 0) return 0;` then `(amountWei * PRICE_COIN_UNIT) / priceWei`.
- advanceGame bounty idiom: `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / PriceLookupLib.priceForLevel(lvl)` via `coinflip.creditFlip` at `DegenerusGameAdvanceModule.sol` 190-194 / 228-230 / 478-480 / 886-888; escalating `bountyMultiplier` (2/4/6×) at 244/252/254/256; `ADVANCE_BOUNTY_ETH = 0.005 ether` at :150. `priceForLevel(uint24)` (`PriceLookupLib:21`) is pure and never returns 0.

Resolve / placement gates the crank reuses:
- `resolveDegeneretteBets` at `DegenerusGame.sol:743`; `_requireApproved` at `DegenerusGame.sol:452` / `DegeneretteModule.sol:131`; `_resolvePlayer` at `DegenerusGame.sol:458` / `DegeneretteModule.sol:141`; owner self-resolve `resolveBets(player, betIds)` at `DegeneretteModule.sol:389`.
- bet `delete degeneretteBets[player][betId]` at `DegeneretteModule.sol:580`; bet `RngNotReady` guard `if (rngWord == 0)` at `DegeneretteModule.sol:578` (error decl :49); placement twin `lootboxRngWordByIndex[index] != 0` → `RngNotReady` at `DegeneretteModule.sol:452`.
- `openLootBox` (already permissionless, no caller gate) at `LootboxModule.sol:477`; box `RngNotReady` resolve guard at `LootboxModule.sol:485, 567`; box-zeroing `lootboxEth[index][player] = 0` at `LootboxModule.sol:530` (and `lootboxEthBase` at :531).
- `_distributePayout` decl `DegeneretteModule.sol:705`, frozen-pool solvency check `~738` (PLAN-V47's "742" is inside the body — cite 705/~738).
- OPEN-D box-cursor enqueue anchor: `lootboxEthBase==0` first-deposit signal written `MintModule:1004-1008`, zeroed `LootboxModule:531`.

Pass gate (PROTO-01):
- `_hasAnyLazyPass` decl `DegenerusGame.sol:1610` (private view returns bool); body reads Deity bit 184 (`BitPackingLib.HAS_DEITY_PASS_SHIFT=184`) + `FROZEN_UNTIL_LEVEL` bits 128-151 (`FROZEN_UNTIL_LEVEL_SHIFT=128`, `MASK_24`). Returns "Deity OR frozenUntilLevel > level" = any-of-3. 1 SLOAD common, 2 worst, zero external calls.

Pinned-address identity (PROTO-05 / SUB-06):
- `ContractAddresses.sol`: `VAULT` (:37), `SDGNRS` (:47) already pinned; NO `AF_KING`/`STREAK_KEEPER` constant exists yet — PROTO-05 must ADD it.

PROTO-side interface facts:
- `IBurnieCoinflip.creditFlip` ALREADY declared at :115 (+ `creditFlipBatch` :122) — PROTO-03 interface decl already exists; PROTO-03 only ADDs the keeper to `onlyFlipCreditors` (BurnieCoinflip :194).
- `BurnieCoin.burnForKeeper` does NOT exist yet — PROTO-02 adds it.
- `DegenerusGame.batchPurchase` does NOT exist yet — PROTO-04 adds it.
- Game has NO `nonReentrant`/ReentrancyGuard; protection is CEI throughout (e.g. `claimablePool -= uint128(payout)` at `DegenerusGame.sol:1408` "CEI: update state before external call"; `.call{value}` at 2005/2022/2043).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the do-work crank ADD-design section + reward/charge model + batchPurchase shape</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §1.10 (reward idiom anchors), §1.11 (resolve/placement gates), §5 OPEN-B (price-unavailable → 0), §5 OPEN-C (reentrancy CEI-vs-guard), §8 Pitfall 3 (box-cursor VRF-rotation coupling), §10 "Plan 316-01" scope.
    - `PLAN-CRANK-DO-WORK-INCENTIVE.md` §2 (reward & charge), §5 non-brick batchPurchase, §6 gas levers, §7 three faucet locks, §8 PROTO additions + crank entry, §11 CRANK-01..04 + REW-01..04, §12 OPEN-B/OPEN-C/OPEN-D.
    - `REQUIREMENTS.md` CRANK-01..04, REW-01..04, PROTO-04.
    - Re-grep each cited file:line in the `<interfaces>` block against HEAD before writing (SC#5 obligation).
  </read_first>
  <action>
    Create `316-SPEC.md` (write the file header + a `## ADD Design — Do-Work Crank` section). Lock, with exact prose (NO fenced solidity bodies — name signatures/identifiers/encoding, not implementations):
    (1) The do-work entry signatures + work-type encoding: bets = caller-supplied off-chain-discovered `(player, ids)` lists with the `BatchAlreadyTaken` collision short-circuit (item-0-already-resolved → revert, reusing the SLOAD item 0 needs; per-item try/catch wraps items 1..N) per CRANK-02; boxes = parameterless cursor per OPEN-D (lock the box cursor as the resolution, cite the `lootboxEthBase==0` enqueue anchor); WWXRP `currency==3` earns zero reward per CRANK-04.
    (2) The reward formula: `gasUnits(workType) · 0.5 gwei → BURNIE` via the guarded `_ethToBurnieValue` idiom (MintModule:1412), with RESERVED per-work-type gas-peg constants (state the constants are named/reserved here, numeric values calibrated at Phase 319 per OPEN-A); lock OPEN-B = reward→0-never-revert by reusing the `_ethToBurnieValue` zero-guard (or pegging to non-zero `priceForLevel` — state the chosen disposition + cite the non-zero invariant). Lock REW-02 = coinflip-credit deferred-mint, batch-accumulated to ONE `creditFlip` per cranker per tx (never per-item); REW-03 fixed `gasUnits` constants never `gasleft()`/`tx.gasprice`; REW-04 no caller restriction.
    (3) The `batchPurchase(players[], amounts[], modes[])` shape: keeper-gated (PROTO-04), per-player in-context purchase wrapped in try/catch + slice-refund, ONE batch value transfer, batch-level `rngLocked`/game-over pre-checked once at entry. Lock OPEN-C = CEI-proof (cite the game's existing CEI convention at DegenerusGame.sol:1408 + no ReentrancyGuard) WITH an explicit guard-fallback note (IMPL must trace the mint→lootbox→prize-pool→EV-cap→quest chain for any external call re-entering before the day-stamp; add a guard only if a re-entrant path is found; route the CEI-vs-guard proof to the `contract-auditor` skill at IMPL/TST — name the skill, do NOT run it here).
    (4) Lock the per-item `onlySelf` external sub-call + try/catch isolation as the only Solidity way to isolate an in-context per-item revert (skip stale/reverting/not-ready, reward only successes); note the `_distributePayout` solvency revert (DegeneretteModule:705/~738) as an example skip-and-continue case.
    State the OPEN-D box-cursor VRF-rotation-orphan-index coupling explicitly: the box cursor enqueue/dequeue MUST follow the v45 `a303ae18` detect-preserve-re-issue path; the AUDIT phase re-verifies the freeze invariant under rotation WITH the cursor (this is the milestone's single biggest design landmine — Pitfall 3).
  </action>
  <verify>
    <automated>grep -q '## ADD Design — Do-Work Crank' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'BatchAlreadyTaken' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '_ethToBurnieValue' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'batchPurchase' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -qi 'a303ae18' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## ADD Design — Do-Work Crank` section.
    - The section locks bets = caller-list with `BatchAlreadyTaken` short-circuit (CRANK-02), boxes = parameterless cursor per OPEN-D, WWXRP `currency==3` zero reward (CRANK-04).
    - The reward formula is locked as `gasUnits(workType) · 0.5 gwei → BURNIE` via `_ethToBurnieValue` (cites `DegenerusGameMintModule.sol:1412`), with reserved per-work-type gas-peg constants (values deferred to Phase 319 / OPEN-A) and OPEN-B = reward-0-never-revert.
    - The section locks one `creditFlip` per cranker per tx (REW-02), fixed `gasUnits` constants never measured gas (REW-03), no caller restriction (REW-04).
    - The `batchPurchase(players[], amounts[], modes[])` shape is locked with try/catch + slice-refund, one batch value transfer, batch-level `rngLocked`/game-over pre-check, and OPEN-C = CEI-proof with a guard-fallback note routing the proof to the `contract-auditor` skill at IMPL.
    - The section states the OPEN-D box-cursor follows the v45 `a303ae18` VRF-rotation orphan-index re-issue path (Pitfall 3 landmine).
    - NO fenced solidity implementation bodies in the section (signatures/identifiers/encoding named as prose only).
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` exists with the `## ADD Design — Do-Work Crank` section locking the crank entry encoding, reward/charge model, batchPurchase shape, OPEN-B/OPEN-C dispositions, and the OPEN-D box-cursor VRF-rotation coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Author the subscription cursor-sweep + authorization + pass-gate section and the 5 PROTO signatures</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §1.12 (keeper transitional-state caveat — sweep is OLD `sweep(startIdx,count)`, no cursor/reinvestPct/windowPaid; lock the INTENDED end-state), §2 (`_hasAnyLazyPass` reader-set / PROTO-01), §3 (keeper dependency clean), §6 Component Responsibility Map, §7 Don't-Hand-Roll (existing `lastSweptDay`/`_removeFromSet`), §8 Pitfall 1 (do not treat PLAN-CRANK §9 "done this session" as ground truth).
    - `PLAN-CRANK-DO-WORK-INCENTIVE.md` §3 (architecture / separate-contract rationale), §4 (authorization + pass gate), §5 (sweep design + lifecycle), §8 (PROTO-01..05 signatures), §9 (keeper rework remaining), §11 SUB-01..05.
    - `REQUIREMENTS.md` PROTO-01..05, SUB-01..03, SUB-07, SUB-08.
    - Re-grep the `<interfaces>` PROTO-side + pass-gate anchors against HEAD before writing.
  </read_first>
  <action>
    Append a `## ADD Design — Subscription Sweep & Authorization` section and a `## PROTO Additions` section to `316-SPEC.md`. Lock:
    (1) The cursor sweep design: `sweep(uint256 maxCount)` + internal daily-reset `sweepCursor` (mirror advanceGame's progress-cursor); concurrent same-block callers self-partition via the advancing cursor with per-entry `lastSweptDay` as the idempotency backstop; stall-escalating bounty (mirror advanceGame's 2/4/6× multiplier) drives daily completeness; caller-bounded `maxCount` (no contract-bounded loop) as the anti-gas-DoS property (SUB-03).
    (2) The lapsed/cancelled lifecycle (SUB-07): tombstone-on-cancel (`dailyQuantity=0`, moves nothing); in-sweep swap-pop reclaim (no `++i`); `_subOf` delete-unless-unexpired-PAID-window via a 1-bit `windowPaid` flag (set on `burnForKeeper`, cleared on free pass-extend); transient-skip retry (not-approved-funds / insufficient-pool / lootbox-floor stay in the set); withdrawable stranded `_poolOf` ETH on a cancelled sub.
    (3) Authorization (SUB-02): `subscribe(player, …)` self-consent (`player == msg.sender`/0) OR operator-approved (`isOperatorApproved`), checked ONCE at subscribe (third-party path only), NEVER at sweep — mirror `_resolvePlayer`/`_requireApproved`. The keeper-purchase entry is gated to the pinned keeper and does NO per-player approval check.
    (4) Pass-OR-pay gate (SUB-01): pass = any of Deity/Whale/Lazy via `hasAnyLazyPass` (PROTO-01), checked at the monthly renewal branch only (`paidThroughDay <= today`), never per sweep; no pass → `burnForKeeper` charges (or skip-with-emit if uncoverable). Charge = `burnForKeeper` all-or-nothing burn; bounty = `creditFlip` gas-pegged (SUB-08).
    (5) The 5 PROTO signatures, each gating on the pinned `AF_KING` constant (PROTO-05): PROTO-01 `hasAnyLazyPass(address) external view` = rename `_hasAnyLazyPass` private→external, NO body change (cite §2 reader-set: 3 grep matches total, decl :1610 + readers :1580/:1660); PROTO-02 `burnForKeeper(address user, uint256 amount) returns (uint256 burned)` all-or-nothing `onlyAfKing` (source balanceOf + pending coinflip; `< amount` → burn nothing return 0); PROTO-03 authorize the keeper in `BurnieCoinflip.onlyFlipCreditors` (note the `creditFlip` interface decl ALREADY exists at IBurnieCoinflip:115); PROTO-04 `batchPurchase` (point to the Task-1 lock); PROTO-05 pin `AF_KING` in `BurnieCoin`/`BurnieCoinflip`/`ContractAddresses.sol`.
    State the keeper transitional-state caveat explicitly (Pitfall 1): the keeper's CURRENT source is a MIXED state (19× `pullForKeeper`, no `sweepCursor`/`reinvestPct`/`windowPaid`, OLD `sweep(startIdx,count)`); PLAN-CRANK §9 "done this session" is FALSE vs live source; the SPEC locks against the INTENDED end-state for Phase 317, NOT the current source — cite the §1.12 drift so the plan-checker does not treat §9 as ground truth.
  </action>
  <verify>
    <automated>grep -q '## ADD Design — Subscription Sweep' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '## PROTO Additions' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'sweepCursor' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'windowPaid' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'hasAnyLazyPass' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'burnForKeeper' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'AF_KING' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains `## ADD Design — Subscription Sweep & Authorization` and `## PROTO Additions` sections.
    - The sweep section locks `sweep(maxCount)` + daily `sweepCursor` self-partition + stall-escalating bounty + `lastSweptDay` backstop (SUB-03), and the tombstone-on-cancel + swap-pop + `windowPaid` + transient-skip + withdrawable-stranded-ETH lifecycle (SUB-07).
    - The authorization model locks subscribe-time self-or-operator consent checked once, never at sweep (SUB-02); pass-OR-pay gate via `hasAnyLazyPass` at monthly renewal only (SUB-01); charge = `burnForKeeper` all-or-nothing, bounty = `creditFlip` gas-pegged (SUB-08).
    - The `## PROTO Additions` section states all 5 PROTO signatures gating on the pinned `AF_KING` constant; PROTO-01 = rename `_hasAnyLazyPass` private→external view with NO body change and cites the 3-match reader-set (decl :1610, readers :1580/:1660).
    - The section notes PROTO-03's `creditFlip` interface decl already exists (`IBurnieCoinflip:115`) and PROTO-03 only adds the keeper to `onlyFlipCreditors`.
    - The section records the keeper transitional-state caveat (Pitfall 1): locks against the INTENDED end-state, not live source; explicitly flags PLAN-CRANK §9 "done this session" as false vs §1.12 drift.
    - NO fenced solidity implementation bodies (signatures named as prose only).
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the subscription-sweep/authorization/pass-gate section + the `## PROTO Additions` section locking all 5 PROTO signatures on the pinned `AF_KING` constant, with the keeper transitional-state caveat recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (for the DESIGN being locked — cost of a wrong lock)

| Boundary | Description |
|----------|-------------|
| SPEC lock → Phase 317 IMPL | A wrong/under-specified ADD lock becomes a wrong contract diff that the user approves once and ships. |
| keeper (AfKing) → game `batchPurchase`/`creditFlip`/`burnForKeeper` | The keeper holds BURNIE charge/emission + purchase-on-behalf authority; the gate design locked here is the only thing standing between it and a faucet. |
| cranker → reward emission | Permissionless reward; the faucet locks (purchase-gate + gas-peg + coinflip-credit illiquidity) are design properties locked here. |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-01 | Elevation | A SPEC that locks the box cursor WITHOUT the VRF-rotation orphan-index re-issue coupling | mitigate | Task 1 explicitly states the box cursor follows the v45 `a303ae18` detect-preserve-re-issue path; AUDIT re-verifies freeze under rotation with the cursor (Pitfall 3). |
| T-316-02 | Spoofing | A SPEC reward design that omits a faucet lock (e.g. per-item creditFlip, or no gas-peg) | mitigate | Task 1 locks one creditFlip/cranker/tx, fixed gasUnits constants never measured gas, WWXRP zero reward, coinflip-credit illiquidity. |
| T-316-03 | Tampering | A SPEC that locks an unverified "by construction" call-graph claim | mitigate | Every cited file:line re-grepped against HEAD before writing; the dedicated attestation table is Plan 316-04. |
| T-316-04 | Repudiation | A SPEC that locks PROTO-04 reentrancy as bare CEI with no fallback | accept-with-condition | Task 1 locks CEI-proof WITH an explicit guard-fallback note + routes the trace to `contract-auditor` at IMPL; a guard is added only if a re-entrant path is found. |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed by this SPEC phase (read-only doc authoring); no install tasks exist. |
</threat_model>

<verification>
- `316-SPEC.md` exists and contains the three ADD-design section headers (`## ADD Design — Do-Work Crank`, `## ADD Design — Subscription Sweep & Authorization`, `## PROTO Additions`).
- Every cited file:line in the ADD sections is grep-confirmed against HEAD (no unverified "by construction" claim).
- No `.sol` or `.t.sol` file appears in this plan's git diff (`git diff --name-only -- contracts/ test/` returns empty for this plan's work).
</verification>

<success_criteria>
- ROADMAP SC#1 (ADD design fully locked) and SC#2 (quantity+funding partial — sweep/authorization portion) are satisfied by the sections this plan authors; the quantity+funding-model specifics are completed in Plan 316-03.
- PROTO-01 design (KEEP+EXPOSE `_hasAnyLazyPass` as external view) is locked with the verified reader-set.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-01-SUMMARY.md` when done.
</output>
