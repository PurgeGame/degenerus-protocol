---
phase: 318-tst-subscription-crank-correctness-removal-proofs-tst
plan: 04
type: execute
wave: 2
depends_on: ["318-01"]
files_modified:
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/AfKingFundingWaterfall.t.sol
autonomous: true
requirements: [SAFE-03]

must_haves:
  truths:
    - "Two same-block sweep(maxCount) callers self-partition via the advancing _sweepCursor + per-entry lastSweptDay: every active subscriber is processed exactly once across the two sweeps, none double-bought, none missed"
    - "Tombstone-on-cancel leaves no dead-slot buildup: a cancelled (swap-pop removed) subscriber is reclaimed in-sweep without advancing the cursor, the swap-pop occupant is still processed this sweep, and the every-entry-every-day completeness holds"
    - "Two-tier skip-kill: a NORMAL sub is cancelled (swap-pop) on a funding skip (claimable+pool < cost); Vault + sDGNRS are EXEMPT via the un-spoofable pinned ContractAddresses identity (transient skip, persist) — there is NO settable exemption flag"
    - "Funding waterfall: claimable-first -> pool top-up (Combined) -> InsufficientPool skip; claimable-only (empty pool) degrades to claimable-or-skip with no new flag"
  artifacts:
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "SAFE-03 same-block cursor self-partition + tombstone-on-cancel no-miss coverage"
      contains: "sweep"
    - path: "test/fuzz/AfKingFundingWaterfall.t.sol"
      provides: "Funding waterfall + two-tier pinned-identity skip-kill coverage"
      contains: "VAULT"
  key_links:
    - from: "two concurrent sweep callers"
      to: "_sweepCursor + lastSweptDay"
      via: "advancing cursor self-partition"
      pattern: "sweep"
    - from: "funding-skip kill"
      to: "ContractAddresses.VAULT / SDGNRS exemption"
      via: "pinned-identity branch, no settable flag"
      pattern: "VAULT|SDGNRS"
---

<objective>
Prove SAFE-03: concurrent same-block sweeps self-partition via the advancing cursor + per-entry lastSweptDay (no double-buy, no missed entry), tombstone-on-cancel leaves no dead-slot buildup, and the every-entry-every-day completeness holds. Plus the funding waterfall (claimable -> pool -> InsufficientPool skip) and the two-tier pinned-identity skip-kill (NORMAL cancel via swap-pop; Vault/sDGNRS EXEMPT, no settable flag).

Purpose: The sweep is permissionless and concurrent — multiple keepers/craners can call sweep in the same block. SAFE-03 is the concurrency-correctness floor: each subscriber is bought exactly once per day regardless of how the sweeps interleave, and a cancelled sub never strands the daily completeness.

Output: `test/fuzz/AfKingConcurrency.t.sol` (cursor self-partition + tombstone no-miss) + `test/fuzz/AfKingFundingWaterfall.t.sol` (waterfall + two-tier skip-kill).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The keeper sweep/cursor/skip-kill surface (live file:line)
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-04-SUMMARY.md

<interfaces>
<!-- The sweep / cursor / skip-kill surface under test (live AfKing.sol). Use directly. -->

AfKing.sol:
  function sweep(uint256 maxCount) external returns (uint256 bountyEarned);  // :522
    uint32 today = _currentDay();  // :526  (block.timestamp-82620)/1 days
    // _sweepDay (uint32) + _sweepCursor (uint224) packed in slot 4:
    //   first sweep of a new keeper-local day resets cursor to 0; within a day the cursor advances
    //   monotonically so concurrent same-block callers self-partition.
    // per-entry: lastSweptDay >= today -> skip (reason 2, idempotency backstop)
  function sweepProgress() external view returns (uint32 day, uint256 cursor);  // :482
  function subscriberCount() external view returns (uint256);   // :469
  function subscriberAt(uint256 idx) external view returns (address);  // :474
  function subscribe(address player, bool drainGameCreditFirst, bool useTickets, uint8 dailyQuantity, uint8 reinvestPct) external payable; // :348
  function setDailyQuantity(uint8 q) external;  // :410  (q==0 cancel/tombstone)

  // Two-tier skip-kill (SUB-06) — inside sweep:
  //   NORMAL sub on funding skip (claimable+pool < cost): _removeFromSet swap-pop (dailyQuantity=0,
  //     clear windowPaid, emit SubscriptionExpired(player,1)), continue WITHOUT advancing cursor
  //     (the swap-pop occupant is processed THIS sweep).
  //   if (player == ContractAddresses.VAULT || player == ContractAddresses.SDGNRS) -> EXEMPT  // :673
  //     no-op-and-retry (PlayerSkipped(player,3)), stays in set. NO settable exemption flag exists.

  // Funding waterfall (SUB-05) — inside sweep per player:
  //   !drainGameCreditFirst -> DirectEth msgValue=cost
  //   else: cred>cost -> Claimable msgValue=0; cred>1 -> Combined msgValue=cost-(cred-1); else DirectEth
  //   _poolOf[player] < msgValue -> InsufficientPool funding skip
  //   LootboxFloor transient skip (reason 4): !useTickets && cost < LOOTBOX_MIN -> stays in set, retries
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Same-block cursor self-partition — exactly-once, no double-buy, no miss</name>
  <files>test/fuzz/AfKingConcurrency.t.sol</files>
  <behavior>
    - With N subscribers active for today, two sweep(maxCount) calls in the SAME block (with maxCount < N each so neither covers all in one shot) together process every subscriber exactly once: the cursor advances across the first sweep and the second sweep resumes from the advanced position; no subscriber is bought twice (lastSweptDay >= today blocks a repeat), and none is left unprocessed once the combined maxCount >= N.
    - sweepProgress() reflects the advancing cursor + the keeper-local day; a sweep in a NEW day resets the cursor to 0 (per-entry lastSweptDay also gates the new-day buy).
    - The per-entry lastSweptDay idempotency backstop alone (independent of cursor position) prevents a second buy for a subscriber already bought today, even if a sweep revisits its index.
  </behavior>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (repaired fixture from 318-01; afKing handle)
    - .planning/phases/317-impl-.../317-04-SUMMARY.md (the _sweepDay/_sweepCursor packed slot 4 + daily-reset + monotonic-advance self-partition)
    - contracts/AfKing.sol:522-560 (the cursor/day reset + advance + lastSweptDay backstop) + :482 (sweepProgress)
    - test/fuzz/StallResilience.t.sol or test/fuzz/AdvanceGameRewrite.t.sol (an existing suite's pattern for driving a cursor/progress model + vm.warp day advance)
  </read_first>
  <action>
    Create `test/fuzz/AfKingConcurrency.t.sol` on the repaired fixture. Subscribe N players (fund them so the buy can land). Within one block (no vm.warp between calls), call sweep(maxCount) twice with maxCount each < N. Use subscriberCount / a per-player lastSweptDay or claimable/pool delta to assert every subscriber was processed exactly once across the two sweeps (sum of buys == N, max buys-per-player == 1). Advance a day (vm.warp by 1 days) and assert the cursor resets and each subscriber is eligible for one fresh buy. Add a fuzz property over (N, maxCount split) asserting exactly-once across arbitrary same-block splits. Then assert the lastSweptDay backstop directly: force a sweep to revisit an already-bought index and confirm no second buy. Test-only; do not modify contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract AfKingConcurrency 2>&1 | grep -qE "Suite result: ok" && echo SAFE03_CONCURRENCY_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>Two same-block sweeps process every subscriber exactly once (no double-buy, no miss); cursor resets per day; lastSweptDay backstop prevents repeat buys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tombstone-on-cancel no-miss + no dead-slot buildup</name>
  <files>test/fuzz/AfKingConcurrency.t.sol</files>
  <behavior>
    - A cancelled subscriber (setDailyQuantity(0)) or a funding-skip-killed NORMAL sub is removed via in-sweep swap-pop WITHOUT advancing the cursor — the occupant swapped into the freed slot is still processed THIS sweep, so no active subscriber is skipped by the removal.
    - After a series of cancels across days, subscriberCount equals the count of still-active subs (no dead/tombstoned slots accumulate in the iteration set); withdraw() on a cancelled sub still returns the stranded _poolOf ETH.
    - windowPaid-gated reclaim: a cancel does NOT delete a paid, unexpired window (windowPaid set AND paidThroughDay > today is preserved); an unpaid/expired window is reclaimed.
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-04-SUMMARY.md (SUB-07 lifecycle: tombstone-on-cancel, in-sweep swap-pop reclaim WITHOUT cursor-advance, windowPaid-gated _subOf reclaim)
    - contracts/AfKing.sol:410 (setDailyQuantity(0)) + the _removeFromSet swap-pop (referenced :594 in 317-VERIFICATION) + :307 (withdraw)
    - test/fuzz/AfKingConcurrency.t.sol (Task-1 helpers to extend)
  </read_first>
  <action>
    Extend AfKingConcurrency.t.sol. Cancel a subscriber mid-set and assert the swap-pop occupant is still processed in the same sweep (no missed active sub). Run cancels across several days and assert subscriberCount tracks only active subs (no tombstone buildup in the iteration set). Assert a cancelled sub's stranded pool ETH is withdrawable. Assert the windowPaid reclaim rule (paid+unexpired window preserved on cancel; unpaid/expired reclaimed). Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract AfKingConcurrency 2>&1 | grep -qE "Suite result: ok" && echo SAFE03_TOMBSTONE_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>Swap-pop-on-cancel processes the occupant without skipping; no dead-slot buildup; stranded pool ETH withdrawable; windowPaid reclaim rule holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Funding waterfall + two-tier pinned-identity skip-kill</name>
  <files>test/fuzz/AfKingFundingWaterfall.t.sol</files>
  <behavior>
    - Funding waterfall (SUB-05): drainGameCreditFirst=true with claimable > cost pays from claimable only (msgValue 0); claimable in (1, cost] tops up from pool (Combined, msgValue = cost-(cred-1)); claimable+pool < cost -> InsufficientPool funding skip. drainGameCreditFirst=false pays DirectEth from pool. Claimable-only (empty _poolOf) degrades to claimable-or-skip with NO new flag.
    - Two-tier skip-kill (SUB-06): a NORMAL sub hitting a funding skip is CANCELLED via swap-pop (dailyQuantity=0, SubscriptionExpired(player,1)); the Vault and sDGNRS subs hitting the SAME funding skip are EXEMPT — they persist (no-op-and-retry, PlayerSkipped(player,3)) keyed on the pinned ContractAddresses.VAULT / SDGNRS identity. A renewal LAPSE (not a transient funding skip) still cancels Vault/sDGNRS.
    - No settable exemption flag exists: there is no setter or storage path that lets a NORMAL sub acquire the exemption — the exemption is purely the pinned-address equality branch (grep-clean assertion that no isExempt/exemptFlag/skipKillExempt/_exempt path exists, complementing the runtime test).
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-04-SUMMARY.md (the funding waterfall byte-faithful preservation + the SUB-06 two-tier skip-kill by pinned identity, no settable flag)
    - contracts/AfKing.sol:560-700 (the waterfall mode selection + the NORMAL-cancel swap-pop + the VAULT/SDGNRS exemption branch :673)
    - test/fuzz/helpers/DeployProtocol.sol (Vault + sDGNRS already self-subscribe at deploy via SUB-09; they are live subscribers in the set)
  </read_first>
  <action>
    Create `test/fuzz/AfKingFundingWaterfall.t.sol` on the repaired fixture. Drive each waterfall branch by setting a player's claimable (via the established claimable slot/helper) and pool relative to cost, then sweep and assert the chosen payment mode + the InsufficientPool skip. For the two-tier skip-kill: starve a NORMAL sub of funding and assert it is swap-pop cancelled (subscriberCount drops, SubscriptionExpired emitted); starve the Vault/sDGNRS subs (already in the set via SUB-09) and assert they persist (still in set, PlayerSkipped(.,3), no cancel) — keyed on the pinned identity. Add a grep-clean assertion (vm.readFile on contracts/AfKing.sol or a forge ffi grep) that no settable-exemption-flag symbol exists. Assert a renewal lapse still cancels even Vault/sDGNRS. Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract AfKingFundingWaterfall 2>&1 | grep -qE "Suite result: ok" && echo WATERFALL_SKIPKILL_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>Every waterfall branch + InsufficientPool skip asserted; NORMAL subs cancelled on funding skip while Vault/sDGNRS persist via pinned identity; no settable exemption flag; renewal lapse cancels all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent sweep callers → shared cursor | Same-block sweeps must self-partition without double-buy or miss |
| NORMAL sub vs Vault/sDGNRS → skip-kill | A NORMAL sub must not spoof the exemption; the exemption is pinned-address-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-04-01 | Tampering | double-buy under concurrent sweeps | mitigate | Test asserts cursor + lastSweptDay yield exactly-once across same-block sweeps |
| T-318-04-02 | Spoofing | NORMAL sub claiming Vault/sDGNRS skip-kill exemption | mitigate | Test asserts exemption is pinned-identity-only; grep-clean of any settable flag |
| T-318-04-03 | Denial of Service | a funding-margin griefer force-cancelling a fragile claimable-only sub | mitigate | Test asserts the waterfall skip vs cancel boundary + Vault/sDGNRS persistence (deeper griefing routed to 320 AUDIT) |
| T-318-04-04 | Denial of Service | dead-slot buildup stalling the daily sweep | mitigate | Test asserts swap-pop reclaim leaves no tombstone buildup |
| T-318-04-SC | Tampering | no package installs | mitigate | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- AfKingConcurrency + AfKingFundingWaterfall suites green under the default profile.
- Same-block exactly-once, tombstone no-miss, no dead-slot buildup, full waterfall, two-tier pinned-identity skip-kill, no settable exemption flag all asserted.
- `git diff --name-only -- contracts/` empty (no production-contract mutation).
</verification>

<success_criteria>
- SAFE-03 concurrency proven: same-block sweeps self-partition, no double-buy, no missed entry, no dead-slot buildup.
- Funding waterfall + two-tier pinned-identity skip-kill proven.
- AGENT-COMMITTED test commit(s).
</success_criteria>

<output>
Create `.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-04-SUMMARY.md` when done.
</output>
