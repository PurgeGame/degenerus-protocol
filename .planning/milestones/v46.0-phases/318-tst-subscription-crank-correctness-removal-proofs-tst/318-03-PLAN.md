---
phase: 318-tst-subscription-crank-correctness-removal-proofs-tst
plan: 03
type: execute
wave: 2
depends_on: ["318-01"]
files_modified:
  - test/fuzz/CrankNonBrick.t.sol
  - test/fuzz/AfKingSubscription.t.sol
autonomous: true
requirements: [SAFE-02]

must_haves:
  truths:
    - "A batch (crankBets, crankBoxes, AND batchPurchase) with one reverting / stale / not-ready item completes for the rest — the bad item is isolated via onlySelf + try/catch and skipped, and only the successful items are rewarded / purchased"
    - "batchPurchase refunds the slice of a failed per-player unit (one batch value transfer in, unspent value refunded to the keeper) and never bricks on a single failing player"
    - "A subscription cancel (setDailyQuantity(0) tombstone) is un-brickable — it cannot be blocked by a reverting downstream call and leaves the pool ETH withdrawable"
    - "A re-entrant sweep / cancel cannot double-buy: the in-context per-player sub-call rolls back on revert, leaving no replayable batch-debit"
    - "Subscription correctness: pass-OR-pay gate (hasAnyLazyPass free-extend vs burnForKeeper charge), burnForKeeper all-or-nothing (shortfall burns nothing, returns 0, auto-pauses the sub), and the gas-pegged coinflip bounty (one creditFlip per sweep tx)"
  artifacts:
    - path: "test/fuzz/CrankNonBrick.t.sol"
      provides: "SAFE-02 non-brick coverage for crankBets/crankBoxes/batchPurchase (skip-and-continue, slice-refund, reentrancy rollback)"
      contains: "batchPurchase"
    - path: "test/fuzz/AfKingSubscription.t.sol"
      provides: "SUB/PROTO acceptance: pass-OR-pay, burnForKeeper all-or-nothing, coinflip bounty, cancel un-brickable"
      contains: "burnForKeeper"
  key_links:
    - from: "batchPurchase"
      to: "_batchPurchaseUnit (onlySelf, per-player try/catch + slice refund)"
      via: "try this._batchPurchaseUnit{value: slice}(...) catch {}"
      pattern: "_batchPurchaseUnit"
    - from: "AfKing.sweep renewal branch"
      to: "BurnieCoin.burnForKeeper (all-or-nothing)"
      via: "burned != cost -> auto-pause"
      pattern: "burnForKeeper"
---

<objective>
Prove SAFE-02: the crank entries and batchPurchase are non-brick — one reverting / stale / not-ready item is isolated via onlySelf + try/catch and skipped, the batch completes rewarding/buying only successes, cancel is un-brickable, and a re-entrant sweep/cancel cannot double-buy. Plus the testable acceptance of subscription correctness (SUB-01 pass-OR-pay, SUB-08/PROTO-02 burnForKeeper all-or-nothing, the gas-pegged coinflip bounty).

Purpose: A permissionless mass-do-work entry that a single bad item can brick is a denial-of-service vector. SAFE-02 is the liveness floor: any caller can always make progress past a poisoned item.

Output: `test/fuzz/CrankNonBrick.t.sol` (the crank/batchPurchase non-brick proofs) + `test/fuzz/AfKingSubscription.t.sol` (the pass-OR-pay / all-or-nothing-charge / bounty acceptance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The batchPurchase + crank surface (live file:line) and the keeper surface
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-03-SUMMARY.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-04-SUMMARY.md

<interfaces>
<!-- Non-brick / subscription surface under test (live). Use directly. -->

DegenerusGame.sol:
  function batchPurchase(address[] players, uint256[] amounts, uint8[] modes) external payable; // :1687
    // keeper-gated on ContractAddresses.AF_KING (:1692); once-at-entry rngLocked/gameOver pre-check (:1693)
    // per-player: try this._batchPurchaseUnit{value: slice}(...) catch {}  (slice-refund on failure)
    // ONE batch value transfer; single post-loop refund of unspent value to keeper
  function _batchPurchaseUnit(...) external;  // :1729 onlySelf — skips approval gate, forwards slice msg.value
  function crankBets(address[] players, uint64[] betIds) external;  // :1543 (items 1..N try/catch isolated)
  function crankBoxes(uint256 maxCount) external;                   // :1592 (cursor walk, per-item try/catch)

AfKing.sol keeper:
  function sweep(uint256 maxCount) external returns (uint256 bountyEarned);  // :522
    // per-player: funding waterfall -> batchPurchase -> burnForKeeper charge -> ONE creditFlip bounty at end
    // SUB-01 pass-OR-pay at the day-31 renewal branch only: hasAnyLazyPass(player) -> free extend (:578)
    //   else burnForKeeper(player, cost); burned != cost -> auto-pause (setDailyQuantity 0, swap-pop, SubscriptionExpired)
  function subscribe(address player, bool drainGameCreditFirst, bool useTickets, uint8 dailyQuantity, uint8 reinvestPct) external payable; // :348
  function setDailyQuantity(uint8 q) external;  // :410  (q==0 = cancel/tombstone)
  function withdraw(uint256 amount) external;   // :307  (pool ETH always withdrawable; CEI, no guard)

BurnieCoin.sol:
  function burnForKeeper(address user, uint256 amount) external returns (uint256 burned);  // :456 onlyAfKing (:533)
    // ALL-OR-NOTHING: available (balance + pending coinflip) < amount -> burn nothing, return 0   (:463-466)
BurnieCoinflip.sol:
  function creditFlip(address player, uint256 amount) external;  // :849 onlyFlipCreditors (AF_KING authorized at :198)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-brick — crankBets / crankBoxes / batchPurchase skip-and-continue + slice-refund</name>
  <files>test/fuzz/CrankNonBrick.t.sol</files>
  <behavior>
    - crankBets over a list where one middle item is stale/already-resolved/reverting: items 1..N are each wrapped in try this._crankResolveBet catch {}; the reverting item is skipped, the rest resolve, reward == sum over successes only. (list[0] still gates BatchAlreadyTaken — use a fresh list[0] so the batch proceeds.)
    - crankBoxes where one cursor entry is not-ready (lootboxRngWordByIndex==0) or reverts: the walk skips it and continues to the next, completing maxCount without bricking; reward == successes only.
    - batchPurchase where one player's _batchPurchaseUnit reverts (e.g. insufficient slice / a forced-revert player): the per-player try/catch isolates it, the other players' purchases land, the failed player's value slice is refunded (one batch value transfer in, unspent refunded to the keeper after the loop), and the call does not revert.
    - batchPurchase pre-checks rngLocked/gameOver ONCE at entry: when rngLocked is true the whole batch is rejected at entry (not per-player), proving the batch-level pre-check.
  </behavior>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (repaired fixture from 318-01; afKing + game + coin + coinflip handles)
    - contracts/DegenerusGame.sol:1543-1640 + :1687-1760 (crank loops + batchPurchase + _batchPurchaseUnit slice-refund)
    - test/fuzz/DegeneretteFreezeResolution.t.sol (how an existing suite forces a bet into a resolvable vs not-ready state via slot writes)
    - test/fuzz/LootboxRngLifecycle.t.sol (box-state slot helpers: lootboxRngWordByIndex=36, lootboxEthBase=19)
  </read_first>
  <action>
    Create `test/fuzz/CrankNonBrick.t.sol` on the repaired fixture. Construct mixed batches (one poisoned item among healthy ones) for crankBets, crankBoxes, and batchPurchase. For batchPurchase, call it as the AF_KING keeper (vm.prank(ContractAddresses.AF_KING)) with a players[]/amounts[]/modes[] array where one entry forces the per-player unit to revert; assert the other players minted, the failed slice was refunded to the keeper (balance delta), and the call returned without reverting. Assert the batch-level rngLocked pre-check rejects the whole batch once at entry (set rngLocked via the established slot/helper, expect a single revert, not a partial run). Use fuzz over batch composition (which index is poisoned, batch length) where natural. Test-only; do not modify contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract CrankNonBrick 2>&1 | grep -qE "Suite result: ok" && echo SAFE02_NONBRICK_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>One poisoned item is skipped in all three entries; the batch completes rewarding/buying only successes; the failed batchPurchase slice is refunded; the batch-level rngLocked/gameOver pre-check fires once at entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reentrancy rollback + cancel un-brickable</name>
  <files>test/fuzz/CrankNonBrick.t.sol</files>
  <behavior>
    - A re-entrant attempt during a sweep/cancel (e.g. a malicious player whose in-context sub-call tries to re-enter sweep or cancel) cannot double-buy: the per-player sub-call frame rolls back on revert, leaving no stored batch-debit a re-entry could replay; the player's pool / claimable is not double-spent.
    - Cancel (setDailyQuantity(0)) is un-brickable: a player can always tombstone their sub regardless of a reverting downstream interaction, and afterward their pool ETH is fully withdrawable via withdraw().
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-03-SUMMARY.md (OPEN-C reentrancy trace: CEI-proof, no guard — the per-player slice moves into the sub-call frame each try; a failed slice stays refundable, no stored batch-debit to replay)
    - contracts/AfKing.sol:307 (withdraw — CEI, re-entrant second call reverts InsufficientBalance) + :410 (setDailyQuantity(0) tombstone)
    - contracts/DegenerusGame.sol:1729 (_batchPurchaseUnit onlySelf; slice forwarded as msg.value per call)
  </read_first>
  <action>
    Extend CrankNonBrick.t.sol with a reentrancy harness: a mock player contract that, on receiving control during a sweep-driven batchPurchase or on withdraw, attempts to re-enter sweep / cancel / withdraw. Assert no double-buy occurs (the player's claimable/pool is debited at most once for the batch; the re-entrant frame reverts and unwinds). Assert cancel-then-withdraw always succeeds: setDailyQuantity(0) tombstones the sub, and the player's full _poolOf balance is withdrawable afterward (the CEI withdraw can't be bricked). Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract CrankNonBrick 2>&1 | grep -qE "Suite result: ok" && echo SAFE02_REENTRANCY_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>No double-buy under reentrancy; cancel is un-brickable; pool ETH withdrawable after cancel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Subscription correctness — pass-OR-pay, burnForKeeper all-or-nothing, coinflip bounty</name>
  <files>test/fuzz/AfKingSubscription.t.sol</files>
  <behavior>
    - Pass-OR-pay (SUB-01): a subscriber WITH an active lazy pass (hasAnyLazyPass true — e.g. the permanent-deity Vault/sDGNRS, or a player with a Whale/Lazy pass) takes the FREE 30-day extend at the day-31 renewal branch with zero burnForKeeper charge; a subscriber WITHOUT a pass is charged via burnForKeeper.
    - burnForKeeper all-or-nothing (PROTO-02 / SUB-08): when the player's spendable total (balance + pending coinflip stake) >= cost, exactly `cost` is burned and the sub stays active with windowPaid set; when spendable < cost, NOTHING is burned, burnForKeeper returns 0, and the sweep auto-pauses that sub (setDailyQuantity 0, SubscriptionExpired) without reverting the whole sweep.
    - Bounty (REW/SUB-08): a sweep that successfully processes >=1 subscriber emits exactly ONE creditFlip to msg.sender (the sweeper) for the batch, gas-pegged (BOUNTY_ETH_TARGET-derived, never per-item); a sweep that processes zero reverts NoSubscribersSwept.
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-04-SUMMARY.md (the sweep renewal branch, all-or-nothing burn auto-pause, single creditFlip bounty model)
    - contracts/AfKing.sol:522-760 (sweep loop: funding waterfall, renewal pass-OR-pay :578-595, burnForKeeper charge + auto-pause, the batched creditFlip bounty :745-746)
    - contracts/BurnieCoin.sol:456-466 (burnForKeeper all-or-nothing: available < amount -> return 0)
  </read_first>
  <action>
    Create `test/fuzz/AfKingSubscription.t.sol` on the repaired fixture. Subscribe a no-pass player and a pass-holding player (use the permanent-deity Vault/sDGNRS as the pass case, or grant a player a lazy pass via the established mintPacked/HAS_DEITY_PASS slot helper). Advance to the renewal branch and assert the pass-holder is NOT charged (free extend) while the no-pass player IS charged via burnForKeeper. Fund a player below cost and assert burnForKeeper burns nothing / returns 0 / auto-pauses the sub without reverting the sweep; fund a player at-or-above cost and assert exactly `cost` burned + windowPaid set. Count creditFlip emissions from a multi-subscriber sweep == 1 (REW-02) and assert a zero-subscriber sweep reverts NoSubscribersSwept. Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract AfKingSubscription 2>&1 | grep -qE "Suite result: ok" && echo SUB_ACCEPTANCE_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>Pass-holders free-extend; no-pass players are charged; burnForKeeper is all-or-nothing with auto-pause on shortfall; exactly one gas-pegged creditFlip bounty per non-empty sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crank/sweep caller → per-item sub-call | A poisoned item must not brick the batch; isolation is onlySelf + try/catch |
| keeper → BurnieCoin.burnForKeeper | All-or-nothing charge; a partial burn would strand a sub in a half-paid state |
| player → reentrancy during sweep/batchPurchase | A re-entrant frame must not replay a batch-debit (double-buy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-03-01 | Denial of Service | one poisoned item bricks crank/batchPurchase | mitigate | Test asserts skip-and-continue via onlySelf + try/catch across all three entries |
| T-318-03-02 | Tampering | double-buy via reentrant sweep/cancel | mitigate | Test asserts the in-context sub-call rolls back, no replayable batch-debit |
| T-318-03-03 | Repudiation | partial burnForKeeper leaving a half-paid sub | mitigate | Test asserts all-or-nothing: shortfall burns nothing, returns 0, auto-pauses |
| T-318-03-04 | Denial of Service | cancel blocked by a reverting downstream call | mitigate | Test asserts setDailyQuantity(0) + withdraw always succeed |
| T-318-03-SC | Tampering | no package installs | mitigate | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- CrankNonBrick + AfKingSubscription suites green under the default profile.
- Skip-and-continue, slice-refund, batch-level pre-check, reentrancy rollback, cancel un-brickable, pass-OR-pay, all-or-nothing burn, one bounty per sweep all asserted.
- `git diff --name-only -- contracts/` empty (no production-contract mutation).
</verification>

<success_criteria>
- SAFE-02 non-brick proven for crankBets, crankBoxes, AND batchPurchase; reentrancy cannot double-buy; cancel un-brickable.
- Subscription correctness (pass-OR-pay, burnForKeeper all-or-nothing, gas-pegged coinflip bounty) proven.
- AGENT-COMMITTED test commit(s).
</success_criteria>

<output>
Create `.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-03-SUMMARY.md` when done.
</output>
