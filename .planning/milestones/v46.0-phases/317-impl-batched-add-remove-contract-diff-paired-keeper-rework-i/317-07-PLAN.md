---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 07
type: execute
wave: 5
depends_on: ["317-02", "317-03", "317-04", "317-05", "317-06"]
files_modified:
  - .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-DIFF-REVIEW.md
autonomous: false
requirements: [PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05, CRANK-01, CRANK-02, CRANK-03, CRANK-04, REW-01, REW-02, REW-03, REW-04, SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, SUB-08, SUB-09, RM-01, RM-02, RM-03, RM-04, RM-05, RM-06, JGAS-02]
must_haves:
  truths:
    - "The full degenerus-audit/contracts/ diff (now including AfKing.sol) is presented for ONE explicit USER approval in the D-04 format: (1) a requirement-mapped summary (PROTO-01..05 / CRANK/REW / SUB / RM-01..06 / JGAS-02 → file:hunk), (2) the full git diff, (3) the forge inspect storage-layout before/after so the re-derived −2 slot constants are verifiable — all in one review moment; never pre-approved, never pushed before review."
    - "The paired ../degenerus-utilities AfKing keeper diff is ALSO presented for explicit USER review in the SAME review moment (D-02) — the commit-guard hook does NOT watch that repo, so its gate is the executor pausing for approval."
    - "AFTER approval, the single batched contracts/ commit is made exactly once (CONTRACTS_COMMIT_APPROVED=1 to satisfy the commit-guard hook); ContractAddresses.sol ships in the same batch; the keeper diff is committed in its repo only after USER review."
    - "Only AFTER the approved batched contract commit are the deferred planning/docs/test commits made (the commit-guard hook blocks ALL commits while contracts/*.sol is dirty); .planning/ is force-added (it is gitignored)."
  artifacts:
    - path: ".planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-DIFF-REVIEW.md"
      provides: "The D-04 requirement-mapped summary + storage before/after, the review artifact presented at the checkpoint"
      contains: "Requirement-Mapped Summary"
  key_links:
    - from: "USER approval"
      to: "single batched contracts/ commit"
      via: "CONTRACTS_COMMIT_APPROVED=1 bypass of the commit-guard hook"
      pattern: "CONTRACTS_COMMIT_APPROVED"
    - from: "keeper diff (D-02)"
      to: "../degenerus-utilities commit"
      via: "executor pause-for-approval (hook does not watch the repo)"
      pattern: "degenerus-utilities"
---

<objective>
Gate the entire phase on ONE explicit USER approval of the batched diff (per `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push`), then make the single batched `contracts/` commit and the paired keeper commit, and finally the deferred planning/docs/test commits. This is the milestone's single contract-mutation approval moment. The batched commit gates EVERY Phase-317 requirement (the full PROTO/CRANK/REW/SUB/RM/JGAS set + the SUB-09/PROTO-01/RM-04 IMPL wiring), hence the complete `requirements` list.

Purpose: every prior wave left `contracts/` dirty and uncommitted (the commit-guard hook blocks ALL commits while `contracts/*.sol` is dirty). This plan assembles the D-04 review package, pauses for the human, and only after approval performs the one batched commit. The keeper diff (D-02) is reviewed in the same moment because it is deployed contract code even though the hook does not watch that repo. Confirm direction at this sensitive-contract boundary per `feedback_pause_at_contract_phase_boundaries` even with auto_advance on.
Output: the approved batched `contracts/` commit + the keeper commit + the deferred docs commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-CONTEXT.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Assemble the D-04 batched-diff review package + run the final SC grep/build gates</name>
  <read_first>
    - 317-CONTEXT.md D-04 (review format) + D-02 (keeper review) + the contract-commit discipline
    - 317-06 SUMMARY (the forge inspect before/after storage layout artifact + the cross-repo batchPurchase signature reconciliation result)
    - 316-SPEC.md `## Requirement Design Coverage` (the 26 Phase-317-owned requirement IDs → the surfaces that satisfy each)
    - the git diff of contracts/ (all Wave-2/3 + AfKing.sol) and the ../degenerus-utilities keeper diff
  </read_first>
  <action>
    Assemble 317-DIFF-REVIEW.md in the D-04 format: (1) a `## Requirement-Mapped Summary` table mapping every Phase-317 requirement (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, JGAS-02) to its file:hunk in the diff; (2) a pointer to the full `git diff -- contracts/` (capture it inline or as a referenced block); (3) the `forge inspect` storage-layout BEFORE/AFTER (from Plan 06) so the re-derived −2 slot constants are verifiable. Run and record the final acceptance gates: forge build PASS on the patched tree; the SC#4 RM grep set (afKing/AFKING_/setAutoRebuy/autoRebuyState/AutoRebuyState/_processAutoRebuy/_calcAutoRebuy/settleFlipModeChange/_afKingRecyclingBonus/_afKingDeityBonusHalfBpsWithLevel/deactivateAfKingFromCoin/syncAfKingLazyPassFromCoin) returns ZERO matches outside contracts/test+mocks; the JGAS-02 grep set (resumeEthPool/SPLIT_CALL1/SPLIT_CALL2/_resumeDailyEth/STAGE_JACKPOT_ETH_RESUME/call1Bucket) returns ZERO matches outside contracts/test+mocks; the byte-unmodified check on the BURNIE win/loss RNG path (processCoinflipPayouts, rngWord & 1) + KNOWN_ISSUES vs baseline. Do NOT commit anything in this task — the package is for the human review checkpoint that follows. Do NOT label any contract edit "pre-approved" anywhere.
  </action>
  <verify>
    <automated>test -f .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-DIFF-REVIEW.md && grep -q "Requirement-Mapped Summary" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-DIFF-REVIEW.md && forge build >/dev/null 2>&1 && echo BUILD_PASS && [ "$(grep -rvE '^[[:space:]]*//' --include=*.sol contracts/ 2>/dev/null | grep -vE 'contracts/test/|contracts/mocks/' | grep -cE 'afKing|AFKING_|autoRebuyState|AutoRebuyState|_processAutoRebuy|_calcAutoRebuy|settleFlipModeChange|resumeEthPool|SPLIT_CALL1|SPLIT_CALL2|STAGE_JACKPOT_ETH_RESUME|call1Bucket')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    317-DIFF-REVIEW.md exists with the `## Requirement-Mapped Summary` (every Phase-317 requirement → file:hunk), the git-diff pointer, and the forge inspect before/after; forge build PASSES; the SC#4 RM grep set + the JGAS grep set both return ZERO matches outside contracts/test+mocks (note: the kept `hasAnyLazyPass` is the sole afKing-named survivor and is excluded by being the renamed external view, not an `afKing`/`AFKING_` token — confirm the grep tokens do not match it); the BURNIE win/loss RNG path + KNOWN_ISSUES are byte-unmodified vs baseline. No commit made; no "pre-approved" label anywhere.
  </acceptance_criteria>
  <done>The D-04 review package is assembled and all final SC gates pass; nothing committed yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: USER-APPROVAL checkpoint — review the batched contracts/ diff + the paired keeper diff (D-02/D-04)</name>
  <action>
    PAUSE for the human. Present the assembled 317-DIFF-REVIEW.md package (the D-04 requirement-mapped summary + the full git diff -- contracts/ + the forge inspect storage-layout before/after) AND the paired ../degenerus-utilities AfKing keeper diff in ONE review moment. Make NO commit until the user responds. Per feedback_never_preapprove_contracts, do NOT describe any edit as "pre-approved". Per feedback_pause_at_contract_phase_boundaries, hold here even with auto_advance on. On "approved" → proceed to Task 3; on issues → return them to the revising waves and do not commit.
  </action>
  <what-built>
    The complete batched ADD+REMOVE contract diff across degenerus-audit/contracts/ (PROTO-01..05, the permissionless do-work crank CRANK-01..04 + REW-01..04, the SUB-01..09 subscription/protocol-sub surface, RM-01..06 legacy removals, JGAS-02 daily-ETH split removal, the new in-tree contracts/AfKing.sol keeper, and the test-side slot re-derivation) PLUS the paired ../degenerus-utilities AfKing keeper rework. forge build PASSES on both trees; the RM + JGAS grep sets are clean outside contracts/test+mocks; the BURNIE win/loss RNG path + KNOWN_ISSUES are byte-unmodified.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/317-.../317-DIFF-REVIEW.md` and read the `## Requirement-Mapped Summary` — confirm every Phase-317 requirement (PROTO-01..05 / CRANK-01..04 / REW-01..04 / SUB-01..09 / RM-01..06 / JGAS-02) maps to a file:hunk you accept.
    2. Review the full `git diff -- contracts/` (all 13 protocol files + the new AfKing.sol) — confirm: the afKing-mode surface is gone keeping only the exposed hasAnyLazyPass; batchPurchase is keeper-gated with per-player try/catch + slice-refund; the crank box cursor follows the v45 a303ae18 re-issue coupling (the crank box-cursor path references lootboxRngWordByIndex + the lootboxEthBase==0 first-deposit signal); the crank reward path has the WWXRP currency==3 zero-reward branch and NO gasleft()/tx.gasprice; the keeper's two-tier skip-kill exemption is the pinned VAULT/SDGNRS identity with NO settable exemption flag; RM-03 collapsed to flat 75bps with the win/loss RNG path untouched; the JGAS split is gone at the preserved 305 ceiling; the ctor :222/:223 Deity grant is byte-unmodified.
    3. Review the `forge inspect` storage-layout BEFORE/AFTER — confirm the slot-≥34 vrf/lootboxRng family shifted −2 (NOT −1) and the re-derived test SLOT_* constants match.
    4. Review the paired `../degenerus-utilities` AfKing keeper diff (D-02) — confirm the batchPurchase call site matches the PROTO-04 signature exactly (element order + types, per the Plan-06 reconciliation), the cursor sweep + two-tier skip-kill are present, and the AF_KING pinned address aligns with contracts/ContractAddresses.sol.
    5. If you approve, the executor will make ONE batched contracts/ commit (CONTRACTS_COMMIT_APPROVED=1) and the keeper commit, then the deferred docs commits. If you find issues, describe them and the executor revises before any commit.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched contracts/ commit + the keeper commit, or describe the issues to revise.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: After approval — the single batched contracts/ commit + keeper commit + deferred docs commits</name>
  <read_first>
    - 317-CONTEXT.md (the contract-commit discipline: one batched commit, ContractAddresses freely-modifiable but batched, keeper reviewed-then-committed)
    - the contract-commit-guard hook behavior (blocks ALL commits while contracts/*.sol is dirty; bypass CONTRACTS_COMMIT_APPROVED=1)
  </read_first>
  <action>
    ONLY after the checkpoint returns "approved": make exactly ONE batched degenerus-audit commit of all contracts/ changes (the 13 protocol files + contracts/AfKing.sol; ContractAddresses.sol ships in the same batch) using CONTRACTS_COMMIT_APPROVED=1 to satisfy the commit-guard hook, with a requirement-mapped commit message (PROTO/CRANK/REW/SUB-01..09/RM-01..06/JGAS-02). Commit the paired ../degenerus-utilities AfKing keeper diff in its own repo (D-02 — only after the USER reviewed it at the checkpoint). THEN — and only after contracts/*.sol is clean (committed) so the guard hook no longer blocks — make the deferred AGENT-COMMITTED commits: the test/ slot re-derivation + compile-fixes (one commit), and the planning/docs (the 7 PLAN.md + the SUMMARYs + 317-LEDGER.md + 317-DIFF-REVIEW.md + ROADMAP/STATE updates), force-adding .planning/ (it is gitignored). Never push before the user has reviewed the diff. If the checkpoint returned issues instead of "approved", do NOT commit — return the issues to the revising waves.
  </action>
  <verify>
    <automated>git -C /home/zak/Dev/PurgeGame/degenerus-audit log --oneline -1 -- contracts/ | grep -qiE "317|PROTO|crank|RM-0|JGAS" && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit status --porcelain -- contracts/)" ] && echo CONTRACTS_COMMITTED_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    Exactly ONE batched contracts/ commit exists (after approval) covering all 13 protocol files + AfKing.sol + ContractAddresses.sol with a requirement-mapped message; contracts/ working tree is clean post-commit; the ../degenerus-utilities keeper diff is committed in its repo; the deferred test/ + planning/docs commits land AFTER the contract commit (the guard hook no longer blocks once contracts/*.sol is clean); .planning/ force-added; nothing pushed before user review.
  </acceptance_criteria>
  <done>The single batched contracts/ commit + the keeper commit + the deferred docs/test commits are made post-approval, in order, with the contract tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → contracts/ commit | the commit-guard hook blocks ALL commits while contracts/*.sol is dirty; the single batched commit is the only authorized one, post-approval. |
| USER review → contract mutation | the human is the gate; no contract edit is ever pre-approved or pushed before review. |
| ../degenerus-utilities keeper diff | the hook does not watch that repo, so its commit gate is the executor pausing for explicit approval (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-07-01 | Repudiation | a contract edit committed without explicit user approval, or pushed before review | mitigate | blocking-human checkpoint gates the single commit; CONTRACTS_COMMIT_APPROVED=1 used ONLY after "approved"; never push before review (feedback_manual_review_before_push) |
| T-317-07-02 | Tampering | a partial / multiple contract commit instead of the one batched diff | mitigate | the commit-guard hook batches by blocking until contracts/ is clean in one commit; deferred docs/test commits land only AFTER the contract commit |
| T-317-07-03 | Spoofing | the keeper diff committed without USER review (hook blind to the repo) | mitigate | D-02 enforced manually — the checkpoint presents the keeper diff in the same review moment; the keeper commit follows approval |
| T-317-07-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (review + git commits only) |

> Deep adversarial review of the highest-scrutiny ADD surfaces (burnForKeeper authority, the permissionless crank, the box-cursor VRF coupling, the two-tier skip-kill identity) is NOT attempted in this IMPL phase — it is routed to Phase 318 TST (contract-auditor) and Phase 320 AUDIT (zero-day-hunter / economic-analyst). Any HIGH-severity unmitigated finding there blocks and triggers a RE-PASS.
</threat_model>

<verification>
- The blocking-human checkpoint is reached with the D-04 review package assembled (summary + git diff + forge inspect before/after + keeper diff).
- After "approved": exactly one batched contracts/ commit, contracts/ tree clean, keeper committed, deferred docs/test commits after.
- forge build PASS + the RM/JGAS grep sets clean outside contracts/test+mocks recorded in the review package.
</verification>

<success_criteria>
The phase gates on ONE explicit USER approval of the batched ADD+REMOVE diff (incl. AfKing.sol) + the paired keeper diff (D-02/D-04), then commits exactly once with the contract tree clean, deferring all docs/test commits to after the approved contract commit; nothing pre-approved, nothing pushed before review.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-07-SUMMARY.md` when done.
</output>
