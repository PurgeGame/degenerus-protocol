---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 02
type: execute
wave: 2
depends_on: ["317-01"]
files_modified:
  - contracts/ContractAddresses.sol
  - contracts/BurnieCoin.sol
  - contracts/BurnieCoinflip.sol
  - contracts/interfaces/IBurnieCoinflip.sol
autonomous: true
requirements: [PROTO-02, PROTO-03, PROTO-05, RM-03]
must_haves:
  truths:
    - "BurnieCoin.burnForKeeper(user, amount) returns (uint256 burned) is all-or-nothing (source = balanceOf + pending coinflip; if total < amount burn nothing and return 0), gated onlyAfKing on the pinned AF_KING constant (PROTO-02)."
    - "The AfKing keeper is authorized in BurnieCoinflip.onlyFlipCreditors so its gas-pegged creditFlip bounty works; no new interface decl is needed (creditFlip/creditFlipBatch already declared) (PROTO-03)."
    - "AF_KING is pinned as a frozen address constant in ContractAddresses.sol and referenced from BurnieCoin/BurnieCoinflip; burnForKeeper/creditFlip gate on exactly it (PROTO-05)."
    - "BURNIE flip auto-rebuy is KEPT and collapsed to the flat RECYCLE_BONUS_BPS=75 — the afKing/deity tier (settleFlipModeChange, _afKingRecyclingBonus, _afKingDeityBonusHalfBpsWithLevel, the 5 deity/afKing consts) is removed; the BURNIE win/loss RNG path (processCoinflipPayouts, rngWord & 1) stays byte-unmodified (RM-03/RM-06)."
  artifacts:
    - path: "contracts/BurnieCoin.sol"
      provides: "burnForKeeper + onlyAfKing"
      contains: "burnForKeeper"
    - path: "contracts/BurnieCoinflip.sol"
      provides: "AF_KING in onlyFlipCreditors + flat-75bps recycle collapse"
      contains: "AF_KING"
    - path: "contracts/ContractAddresses.sol"
      provides: "AF_KING pinned constant"
      contains: "AF_KING"
    - path: "contracts/interfaces/IBurnieCoinflip.sol"
      provides: "settleFlipModeChange decl removed; creditFlip/creditFlipBatch kept"
  key_links:
    - from: "contracts/BurnieCoin.sol burnForKeeper"
      to: "ContractAddresses.AF_KING"
      via: "onlyAfKing pinned-constant gate"
      pattern: "AF_KING"
    - from: "contracts/BurnieCoinflip.sol onlyFlipCreditors"
      to: "ContractAddresses.AF_KING"
      via: "added creditor clause"
      pattern: "AF_KING"
---

<objective>
Apply the BURNIE-side PROTO additions (PROTO-02 `burnForKeeper`, PROTO-03 keeper authorization in `onlyFlipCreditors`, PROTO-05 the pinned `AF_KING` constant) and the RM-03 BURNIE-flip-recycle collapse to flat 75bps, leaving `contracts/` dirty for the single batched approval. NO commit in this plan (the commit-guard hook blocks any commit while `contracts/*.sol` is dirty).

Purpose: these three contracts are the keeper's authority surface — the all-or-nothing burn it charges through, the creditFlip bounty it earns through, and the pinned address every gate resolves to. RM-03 collapses the flip recycle to the kept flat rate in the same surgery. File-ownership is exclusive (BurnieCoin/BurnieCoinflip/ContractAddresses/IBurnieCoinflip — no overlap with Plan 03's DegenerusGame or Plan 05's modules), so this runs parallel to Plans 03 and 04.
Output: the four files patched; storage/ABI deltas captured for the batched-diff summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md

<interfaces>
<!-- Pinned-keeper gate model (BurnieCoin.sol onlyVault :483-488), burn primitive (_burn :390), gated-privileged-fn idiom (vaultMintTo :518). -->
<!-- onlyFlipCreditors modifier being extended at BurnieCoinflip.sol:192-203; creditFlip impl :898; RECYCLE_BONUS_BPS=75 :129 KEEP vs AFKING_RECYCLE_BONUS_BPS=100 :130 DELETE. -->
<!-- ContractAddresses two-line const form: address internal constant VAULT = address(0x...); :37/:47. -->
<!-- Use the LIVE line numbers from 317-LEDGER.md — they are authoritative over the SPEC/PATTERNS snapshot. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PROTO-05 pin AF_KING + PROTO-03 authorize keeper in onlyFlipCreditors</name>
  <read_first>
    - 317-LEDGER.md (live `ContractAddresses` const block line; live `onlyFlipCreditors` modifier line; live `creditFlip`/`creditFlipBatch` interface decl lines)
    - 316-SPEC.md `## PROTO Additions` (PROTO-03, PROTO-05)
    - 317-PATTERNS.md `contracts/ContractAddresses.sol` + `contracts/BurnieCoinflip.sol` sections
    - contracts/ContractAddresses.sol, contracts/BurnieCoinflip.sol, contracts/interfaces/IBurnieCoinflip.sol
  </read_first>
  <action>
    In `ContractAddresses.sol` ADD `address internal constant AF_KING = address(0x...);` in the two-line `internal constant` form matching the `VAULT`/`SDGNRS`/`COINFLIP` block, with the deploy-predicted keeper address placeholder consistent with the D-01b cross-repo reconciliation (the `degenerus-utilities` deploy must produce/consume this same address). In `BurnieCoinflip.sol` extend the `onlyFlipCreditors` modifier by ONE clause (`sender != ContractAddresses.AF_KING`) and update the `@dev` allowed-callers list to describe what IS (add the keeper to the GAME/QUESTS/AFFILIATE/ADMIN set). Do NOT add any new interface decl — `creditFlip` and `creditFlipBatch` already exist in `IBurnieCoinflip.sol` (confirmed in the ledger); the keeper's bounty flows through the existing `creditFlip :898` zero-guarded impl. Comments describe what IS — no history prose.
  </action>
  <verify>
    <automated>grep -q "AF_KING" contracts/ContractAddresses.sol && grep -q "ContractAddresses.AF_KING" contracts/BurnieCoinflip.sol</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "AF_KING" contracts/ContractAddresses.sol` ≥ 1 in the two-line `internal constant` form; `onlyFlipCreditors` in `BurnieCoinflip.sol` has the added `AF_KING` clause and an updated allowed-callers `@dev`; no new decl added to `IBurnieCoinflip.sol` for `creditFlip`. No "previously"/"changed"/"removed" prose in any touched comment.
  </acceptance_criteria>
  <done>AF_KING is pinned and the keeper is an authorized flip creditor; no redundant interface decl added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PROTO-02 burnForKeeper + onlyAfKing (all-or-nothing burn)</name>
  <read_first>
    - 317-LEDGER.md (live `onlyVault`/`_burn`/`vaultMintTo` lines + the `spendable = balanceOf[player]` read site + the pending-coinflip source)
    - 316-SPEC.md `## PROTO Additions` (PROTO-02 all-or-nothing) + `## ADD Design — Subscription Sweep & Authorization` (pass-OR-pay charge)
    - 317-PATTERNS.md `contracts/BurnieCoin.sol` section (the highest-scrutiny ADD-surface note)
    - contracts/BurnieCoin.sol
  </read_first>
  <action>
    Author an `onlyAfKing` modifier matching the `onlyVault` house style (`if (msg.sender != ContractAddresses.AF_KING) revert OnlyAfKing();`, custom error, single-line guard) and add the custom error `OnlyAfKing`. Add `function burnForKeeper(address user, uint256 amount) external onlyAfKing returns (uint256 burned)`: sum the user's `balanceOf` plus their pending coinflip stake (the same source the spendable read uses), and apply the ALL-OR-NOTHING rule — if the available total `< amount`, burn nothing and `return 0`; otherwise burn exactly `amount` via the existing `_burn` primitive (which already underflow-reverts on `balanceOf[from] -= amount`) drawing from balance first then pending coinflip as the source model dictates, and `return amount`. Use `_toUint128` width-guard and `unchecked` math only after the capacity check, mirroring the `vaultMintTo` gated-privileged-fn idiom; emit the house event style at the end if the analog does. You cannot refund a burn — the all-or-nothing decision MUST precede any state change. Comments describe what IS.
  </action>
  <verify>
    <automated>grep -q "function burnForKeeper" contracts/BurnieCoin.sol && grep -q "onlyAfKing" contracts/BurnieCoin.sol && grep -q "OnlyAfKing" contracts/BurnieCoin.sol</automated>
  </verify>
  <acceptance_criteria>
    `burnForKeeper(address,uint256) returns (uint256)` exists, gated `onlyAfKing`; the function returns 0 and mutates no state when `balanceOf[user] + pendingCoinflip < amount`, and burns exactly `amount` returning `amount` otherwise; the all-or-nothing capacity check precedes any `_burn`/state change; `OnlyAfKing` custom error declared. No partial-burn path exists.
  </acceptance_criteria>
  <done>burnForKeeper is present, all-or-nothing, and gated on the pinned AF_KING constant.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RM-03 collapse BURNIE flip recycle to flat 75bps (keep core, drop afKing/deity tier)</name>
  <read_first>
    - 317-LEDGER.md (live RM-03 lines: `settleFlipModeChange`, the rebet-bonus afKing branch, the `_claimCoinflipsInternal` sync+deity blocks, `_setCoinflipAutoRebuy`/`_setCoinflipAutoRebuyTakeProfit` floor checks, the helpers `_afKingRecyclingBonus`/`_afKingDeityBonusHalfBpsWithLevel`, the 5 deity/afKing consts, and the KEEP set `RECYCLE_BONUS_BPS=75`/`_recyclingBonus`/`processCoinflipPayouts`/`(rngWord & 1)`)
    - 316-SPEC.md `## REMOVE Footprint` RM-03 + `## RM-05 / RM-06 verify-before-IMPL` byte-unmodified hygiene
    - 317-PATTERNS.md `contracts/BurnieCoinflip.sol` RM-03 section
    - contracts/BurnieCoinflip.sol, contracts/interfaces/IBurnieCoinflip.sol
  </read_first>
  <action>
    In-place surgery, NOT a whole-fn rewrite. DELETE `settleFlipModeChange` (and its decl in `IBurnieCoinflip.sol`). Collapse the rebet-bonus afKing branch to call `_recyclingBonus` only (drop the `afKingModeFor`/`hasDeityPass`/`_afKingDeityBonus`/`_afKingRecyclingBonus` selection). In `_claimCoinflipsInternal`: drop the `syncAfKingLazyPassFromCoin` sync call, drop the `afKingActive`/`hasDeityPass`/`deityBonusHalfBps` block, and collapse the recycle branch to `_recyclingBonus`. In `_setCoinflipAutoRebuy` / `_setCoinflipAutoRebuyTakeProfit`: remove the `deactivateAfKingFromCoin` cross-calls and the `AFKING_KEEP_MIN_COIN` floor checks. DELETE helpers `_afKingRecyclingBonus`, `_afKingDeityBonusHalfBpsWithLevel`. DELETE the 5 consts (`AFKING_RECYCLE_BONUS_BPS=100`, `AFKING_DEITY_BONUS_PER_LEVEL_HALF_BPS`, `AFKING_DEITY_BONUS_MAX_HALF_BPS`, `DEITY_RECYCLE_CAP`, `AFKING_KEEP_MIN_COIN`). KEEP byte-unmodified: `RECYCLE_BONUS_BPS=75`, `_recyclingBonus`, and the BURNIE win/loss RNG path `processCoinflipPayouts` with `bool win = (rngWord & 1) == 1;`. Do NOT delete `RECYCLE_BONUS_BPS=75` by confusing it with the deleted `AFKING_RECYCLE_BONUS_BPS=100` (the ledger records the value distinction). After the edit, run the SC#4 grep set for the afKing/recycle symbols and confirm zero matches in this file outside any comment. Enable/disable/take-profit/carry/claim must still work end-to-end at the flat rate. Comments describe what IS.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' contracts/BurnieCoinflip.sol | grep -cE "settleFlipModeChange|_afKingRecyclingBonus|_afKingDeityBonusHalfBpsWithLevel|AFKING_RECYCLE_BONUS_BPS|AFKING_KEEP_MIN_COIN|deactivateAfKingFromCoin" | grep -qx 0 && grep -q "RECYCLE_BONUS_BPS" contracts/BurnieCoinflip.sol && grep -q "rngWord & 1" contracts/BurnieCoinflip.sol</automated>
  </verify>
  <acceptance_criteria>
    Outside comments, zero matches in `BurnieCoinflip.sol` for `settleFlipModeChange`/`_afKingRecyclingBonus`/`_afKingDeityBonusHalfBpsWithLevel`/`AFKING_RECYCLE_BONUS_BPS`/`AFKING_KEEP_MIN_COIN`/`deactivateAfKingFromCoin`; `RECYCLE_BONUS_BPS` (=75), `_recyclingBonus`, `processCoinflipPayouts`, and `(rngWord & 1)` remain present byte-unmodified; `settleFlipModeChange` decl removed from `IBurnieCoinflip.sol` while `creditFlip`/`creditFlipBatch` are kept.
  </acceptance_criteria>
  <done>The flip recycle is collapsed to flat 75bps with the afKing/deity tier gone and the win/loss RNG path byte-unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → BurnieCoin.burnForKeeper | the keeper is the only authorized caller of the all-or-nothing burn; the gate is the pinned AF_KING constant. |
| keeper → BurnieCoinflip.creditFlip | the keeper earns its bounty as deferred-mint coinflip credit through the extended onlyFlipCreditors gate. |
| external caller → ContractAddresses.AF_KING | the pinned address is the un-spoofable identity every gate resolves to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-02-01 | Spoofing | unauthorized caller of burnForKeeper/creditFlip | mitigate | gate on the pinned `ContractAddresses.AF_KING` constant (`onlyAfKing` / extended `onlyFlipCreditors`), never a settable flag (PROTO-05) |
| T-317-02-02 | Tampering | partial burn leaving an un-refundable shortfall | mitigate | all-or-nothing: the capacity check precedes any `_burn`; on shortfall burn nothing and return 0 (PROTO-02) |
| T-317-02-03 | Elevation | RM-03 collapse silently altering the BURNIE win/loss RNG payout | mitigate | `processCoinflipPayouts` / `(rngWord & 1)` KEPT byte-unmodified (RM-06); grep gate asserts the kept set survives; deep adversarial review of the burnForKeeper authority routed to 318 TST / 320 AUDIT (contract-auditor) |
| T-317-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (Solidity edits only) |
</threat_model>

<verification>
- `git diff --stat -- contracts/` shows exactly the four files touched; the commit-guard hook keeps `contracts/` dirty (no commit here).
- The SC#4 RM-03 grep set returns zero matches outside comments in `BurnieCoinflip.sol`.
- `burnForKeeper` exists, all-or-nothing, gated `onlyAfKing`; `AF_KING` pinned.
</verification>

<success_criteria>
PROTO-02/PROTO-03/PROTO-05 land on the BURNIE side and RM-03 collapses the recycle to flat 75bps with the win/loss RNG path byte-unmodified; `contracts/` is left dirty for the single batched approval.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-02-SUMMARY.md` when done.
</output>
