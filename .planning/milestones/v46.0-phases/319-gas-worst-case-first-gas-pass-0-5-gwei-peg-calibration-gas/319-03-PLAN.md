---
phase: 319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas
plan: 03
type: execute
wave: 2
depends_on: ["319-01"]
files_modified:
  - test/gas/SweepPerPlayerWorstCaseGas.t.sol
autonomous: true
requirements: [GAS-01, GAS-06]
must_haves:
  truths:
    - "The sweep-per-player worst case (a reinvest sub whose effective buy triggers multiple lootbox materializations) is constructed and measured"
    - "The per-successful-player sweep marginal is isolated — the calibration target for BOUNTY_ETH_TARGET (a deploy-script param, NOT a frozen Game constant)"
    - "The whole sweep of N healthy subs fits the REAL 30M mainnet block gas limit"
  artifacts:
    - path: "test/gas/SweepPerPlayerWorstCaseGas.t.sol"
      provides: "GAS-01 sweep-per-player worst-case measurement + per-player marginal for the BOUNTY_ETH_TARGET deploy-param calibration"
      contains: "sweep"
  key_links:
    - from: "test/gas/SweepPerPlayerWorstCaseGas.t.sol"
      to: "the measured per-successful-player marginal that calibrates BOUNTY_ETH_TARGET (DeployProtocol.sol:126 arg 2)"
      via: "emit log_named_uint of the per-player marginal; Plan 05 reads it"
      pattern: "emit log_named_uint"
---

<objective>
Measure the sweep-per-player worst case derived in Plan 01 and isolate the per-successful-player marginal that calibrates the AfKing `BOUNTY_ETH_TARGET` deploy-script parameter (NOT a frozen Game constant).

Purpose: GAS-01 requires measuring the THEORETICAL sweep worst case. The per-player marginal is the calibration input for `BOUNTY_ETH_TARGET` — which RESEARCH §GAS-06 confirms is an AfKing constructor immutable set from a deploy-script arg (`DeployProtocol.sol:126`, arg 2 = `885_000_000`), so its calibrated value is AGENT-editable as a deploy-param, NOT behind the USER-APPROVED frozen-contract gate. This plan MEASURES; Plan 05 decides the deploy-param tune.

Output: one new `test/gas/SweepPerPlayerWorstCaseGas.t.sol` harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md

<interfaces>
<!-- Sweep fixture + idioms, source-verified. Use directly. -->

Sweep fixture (clone AfKingConcurrency.t.sol):
  contract X is DeployProtocol { ... }            AfKingConcurrency.t.sol:37
  SUBOF_SLOT = 1            // _subOf root (Sub = one slot)            :41
  SUBSCRIBER_INDEX_SLOT = 3 // _subscriberIndex root (1-indexed)        :42
  setUp: _deployProtocol(); warp +1 day (clean keeper-local day index)  :63
  healthy subs are ticket mode, operator-approved, pool-funded, not renewal-due (each lands a clean buy)  :32-34
  deploy-time subs (VAULT + SDGNRS) already in the set — isolate test subs from them when summing per-player

Subscriber seeding (public API):
  subscribe(player, drainGameCreditFirst, useTickets, dailyQuantity, reinvestPct)
  afKing.subscribe{value: poolWei}(player, true, true, 1, 0);

Sweep entry + progress:
  afKing.sweep(maxCount)            AfKing.sol:522
    if game.rngLocked() revert SweepAborted   :523
    mp = game.mintPrice() ONCE                :527
    cursor = _sweepDay == today ? _sweepCursor : 0   :532  (self-partition)
    per player: hasAnyLazyPass + isOperatorApproved(:610) + claimableWinningsOf(x2 if reinvest)
      + the batchPurchase._batchPurchaseUnit -> _purchaseFor slice (full mint->lootbox->prize-pool->EV-cap->quest)
    ONE game.batchPurchase{value: totalValue}(...) + ONE creditFlip bounty (stall-scaled)
  afKing.sweepProgress() -> (day, cursor)     (read cursor advance to confirm subs processed)

BOUNTY_ETH_TARGET: AfKing.sol:252 (public immutable), set :268 from constructor arg _bountyEthTarget.
Deploy arg: DeployProtocol.sol:126 -> new AfKing(5_000_000_000, 885_000_000, 10_000_000_000)  // (subCost, bounty, lootboxMin)

Gas idiom (RedemptionGas.t.sol:197-205): gasBefore=gasleft(); call; g=gasBefore-gasleft(); emit log_named_uint; assertLt(g, LIMIT).
Real-30M: uint256 internal constant MAINNET_BLOCK_GAS_LIMIT = 30_000_000;  (JackpotSingleCallCorrectness.t.sol:80-82)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SweepPerPlayerWorstCaseGas — per-player sweep marginal + whole-sweep fit</name>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol (the sweep fixture: DeployProtocol base :37, slot constants :41-44, setUp :60-67, the healthy-subscriber seeding via public subscribe(), the afKing.sweep + sweepProgress cursor-advance assertions :82-96)
    - test/fuzz/RedemptionGas.t.sol:197-205 (gasleft-delta + named-log idiom)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md (the sweep-per-player derivation: dominant cost = cross-contract calls + the _batchPurchaseUnit -> _purchaseFor slice; worst case = a reinvest sub whose buy triggers multiple lootbox materializations)
    - contracts/AfKing.sol:240-268,395,522-651,745 (BOUNTY_ETH_TARGET immutable, the sweep per-player loop, the bounty formula at :745)
  </read_first>
  <behavior>
    - Test A (per-player marginal): seed N healthy subscribers (isolated from the deploy-time VAULT+SDGNRS subs), measure `afKing.sweep(N)` gas, divide by N → the per-successful-player marginal that calibrates BOUNTY_ETH_TARGET; emit it via log_named_uint; assert the whole sweep < MAINNET_BLOCK_GAS_LIMIT.
    - Test B (worst-case-leaning marginal): seed at least one reinvest sub (reinvestPct > 0) whose effective buy is large enough to trigger multiple lootbox materializations (the worst per-player path per the derivation), measure its marginal, and emit it. Assert the worst-case per-player marginal >= the typical per-player marginal (confirms the construction is the heavier path).
    - Test C (non-vacuity): assert the sweep actually bought (cursor advanced by N via sweepProgress / claimable or mint state changed), so the measurement is not of a sweep that skipped every sub (funding-skip / not-approved / renewal-due → zero work).
  </behavior>
  <action>
    Create `test/gas/SweepPerPlayerWorstCaseGas.t.sol` extending `DeployProtocol`. Clone the `AfKingConcurrency` setUp (`_deployProtocol()`, warp +1 day for a clean keeper-local day index) and the healthy-subscriber seeding via the public `subscribe{value: poolWei}(player, drainGameCreditFirst, useTickets, dailyQuantity, reinvestPct)` API (AfKing signature). Make subs healthy: ticket mode, operator-approved, pool-funded, not renewal-due, so each lands a clean buy. ISOLATE the test subs from the two deploy-time subs (VAULT + SDGNRS already in the set) when computing the per-player marginal — subtract their contribution or seed test subs on a disjoint range and divide only by the test-sub count. Define `MAINNET_BLOCK_GAS_LIMIT = 30_000_000`. Bracket `afKing.sweep(N)` with the `gasleft()` delta (RedemptionGas:197-205). Implement Tests A/B/C per `<behavior>`. For Test B, seed at least one reinvest sub (`reinvestPct > 0`) with claimable large enough that its effective buy triggers multiple lootbox materializations (the worst per-player path). Emit the per-successful-player marginal via `emit log_named_uint` (Plan 05 reads it to tune the `BOUNTY_ETH_TARGET` deploy-param). Note in a NatSpec block that `BOUNTY_ETH_TARGET` is a deploy-script param (DeployProtocol.sol:126 arg 2), NOT a frozen Game constant — its tune is AGENT-editable, unlike the two `*_GAS_UNITS` constants. Do NOT touch the deploy-param value here (Plan 05 decides the tune). Zero `contracts/*.sol` mutation; restore ContractAddresses.sol via `git checkout` if the build patched it.
  </action>
  <verify>
    <automated>forge test --match-contract SweepPerPlayerWorstCaseGas -vv 2>&1 | grep -qE "sweep_per_player|[0-9]+ passed" && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract SweepPerPlayerWorstCaseGas -vv` passes; the per-successful-player marginal is emitted via `emit log_named_uint` and the whole sweep is asserted `< 30_000_000`.
    - Test B seeds a reinvest sub on the worst per-player path and asserts its marginal >= the typical marginal.
    - Test C (non-vacuity) proves the sweep actually bought (cursor advanced / state changed), not a skip-everything no-op.
    - The test subs are isolated from the deploy-time VAULT+SDGNRS subs when computing the marginal.
    - `git diff --name-only -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The sweep-per-player worst case is measured, the per-successful-player marginal is logged for the BOUNTY_ETH_TARGET deploy-param calibration, and the whole sweep is proven to fit 30M; contracts/ clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test subs ↔ deploy-time protocol subs | The VAULT+SDGNRS subs are already in the set; not isolating them contaminates the per-player marginal |
| sweep funding/approval state → measured work | A sub that is not approved / renewal-due / underfunded is SKIPPED — measuring a skip-everything sweep yields a vacuous marginal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-08 | Repudiation (vacuous sweep) | sweep fixture | mitigate | Test C asserts the sweep actually bought (cursor advanced via sweepProgress / state changed); healthy-sub seeding (approved, funded, not renewal-due) per the AfKingConcurrency precedent ensures each lands a clean buy |
| T-319-09 | Tampering (contaminated marginal) | per-player marginal | mitigate | Isolate test subs from the deploy-time VAULT+SDGNRS subs; divide only by the test-sub count so the marginal calibrates BOUNTY_ETH_TARGET cleanly |
| T-319-10 | EoP (sweep griefing not modeled) | sweep loop | accept | Sweep griefing / swap-pop integrity is GAS-05's audit charge (Plan 04, guards G10/G11); this plan only measures gas — the guards are not touched |
| T-319-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `forge test --match-contract SweepPerPlayerWorstCaseGas -vv` green.
- The harness asserts the whole sweep fits the REAL 30M, isolates test subs from deploy-time subs, and includes a non-vacuity guard.
- The per-successful-player marginal is emitted via `log_named_uint` for Plan 05's BOUNTY_ETH_TARGET deploy-param decision.
- `git diff --name-only -- contracts/` empty; no NEW failures vs the 44-failure 318 baseline.
</verification>

<success_criteria>
- GAS-01 sweep-per-player worst case measured; the per-player marginal logged as the BOUNTY_ETH_TARGET calibration input.
- Zero `contracts/*.sol` mutation; suite recompiles green.
</success_criteria>

<output>
Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-03-SUMMARY.md` when done.
</output>
