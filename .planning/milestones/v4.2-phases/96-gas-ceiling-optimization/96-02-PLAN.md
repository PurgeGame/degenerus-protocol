---
phase: 96-gas-ceiling-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md
autonomous: true
requirements: [GOPT-01, GOPT-02]

must_haves:
  truths:
    - "Every SLOAD in the daily jackpot hot path is cataloged with location, cold/warm status, per-call count, and whether it is necessary or redundant"
    - "Every loop body in the daily jackpot code path is analyzed for hoistable computation"
    - "Each identified optimization opportunity has a verdict: implement, skip (with justification), or already-optimized"
  artifacts:
    - path: ".planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md"
      provides: "SLOAD audit and loop hoisting audit for daily jackpot hot path"
      contains: "SLOAD Audit"
  key_links:
    - from: "96-OPTIMIZATION-AUDIT.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "Line-by-line storage read analysis"
      pattern: "SLOAD|SSTORE|cold|warm"
---

<objective>
Audit the daily jackpot code path for unnecessary SLOADs and loop-hoistable computation, producing a detailed optimization audit document.

Purpose: The daily jackpot hot path processes up to 321 winners. At ~25,000 gas per auto-rebuy winner, the per-winner cost is dominated by cold SLOADs. Identifying even one unnecessary SLOAD saves 2,100 gas x 321 = 674,100 gas total. This audit catalogs every storage read and loop computation to find optimization opportunities.

Output: `96-OPTIMIZATION-AUDIT.md` -- per-operation audit with verdicts for each SLOAD and loop computation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md

<interfaces>
<!-- Functions to audit (all in DegenerusGameJackpotModule.sol): -->
<!-- _processDailyEthChunk: lines 1329-1424 (outer bucket loop + inner winner loop) -->
<!-- _addClaimableEth: lines 928-949 (per-winner dispatch: normal vs auto-rebuy) -->
<!-- _processAutoRebuy: lines 959-999 (auto-rebuy path: _calcAutoRebuy + _queueTickets + pool update) -->
<!-- _creditClaimable: (normal winner path: claimableWinnings SLOAD + SSTORE) -->
<!-- payDailyJackpot init block: lines 313-638 (init SLOADs before chunk processing) -->
<!-- _clearDailyEthState: lines 2699-2705 (cleanup SSTOREs) -->

<!-- Known SLOAD patterns from RESEARCH.md: -->
<!-- gameOver: warm SLOAD (100 gas) per winner, Slot 0 read in preamble. Potentially hoistable as param. -->
<!-- autoRebuyState[beneficiary]: cold SLOAD (2100 gas) per winner. UNAVOIDABLE (unique per address). -->
<!-- claimableWinnings[beneficiary]: cold SLOAD (2100 gas) per winner. UNAVOIDABLE (unique per address). -->
<!-- traitBurnTicket[lvl]: cold SLOAD per bucket (4x). Mapping root. -->
<!-- level: warm SLOAD (100 gas) per auto-rebuy winner. Slot 0 already read. -->
<!-- prizePoolsPacked: warm SLOAD (100 gas) per auto-rebuy winner after first. -->
<!-- ticketsOwedPacked[player][level]: cold SLOAD+SSTORE (~7200 gas) per auto-rebuy winner. UNAVOIDABLE. -->

<!-- Key insight from RESEARCH.md: -->
<!-- "The hot path's computations that precede the loops are already hoisted above the bucket iteration. -->
<!--  The remaining per-iteration costs are dominated by unavoidable per-address cold SLOADs, -->
<!--  not by redundant computation." -->

<!-- Storage layout: -->
<!-- Slot 0 (packed): level, jackpotPhaseFlag, jackpotCounter, poolConsolidationDone, lastPurchaseDay, decWindowOpen, rngLockedFlag, phaseTransitionActive, gameOver, dailyJackpotCoinTicketsPending -->
<!-- Slot 1 (packed): dailyEthPhase, compressedJackpotFlag, purchaseStartDay, price, ticketWriteSlot, ticketsFullyProcessed, prizePoolFrozen -->
<!-- Slot 2: currentPrizePool (uint256) -->
<!-- Slot 3: prizePoolsPacked (uint256) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SLOAD audit of daily jackpot hot path</name>
  <files>.planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 928-999 for _addClaimableEth and _processAutoRebuy, lines 1329-1424 for _processDailyEthChunk, lines 313-638 for payDailyJackpot)
    - contracts/storage/DegenerusGameStorage.sol (storage layout: slot assignments, packed variables)
    - .planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md (SLOAD patterns table, storage layout section)
  </read_first>
  <action>
Create `.planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md` with a comprehensive SLOAD audit.

**Document structure:**

1. **Header:** SLOAD + Loop Hoisting Audit, date, scope (daily jackpot code path)

2. **Scope:** List every function in the audit scope:
   - `payDailyJackpot` init block (lines 313-638)
   - `_processDailyEthChunk` (lines 1329-1424)
   - `_addClaimableEth` (lines 928-949)
   - `_processAutoRebuy` (lines 959-999)
   - `_creditClaimable` (find exact lines)
   - `_clearDailyEthState` (lines 2699-2705)

3. **SLOAD Inventory Table** (GOPT-01): For EVERY storage read in the audit scope, create a row:

| # | Variable | Solidity Line | EVM Slot | Cold/Warm | Per-Call Count | Total Gas (321 winners) | Verdict |
|---|----------|---------------|----------|-----------|----------------|------------------------|---------|

Verdicts: NECESSARY (can't avoid), ALREADY-OPTIMIZED (warm/packed), OPTIMIZE (can be eliminated or hoisted), MARGINAL (savings < 10,000 gas total, not worth complexity).

For each SLOAD, trace the EVM slot:
- Is the variable packed with other variables in the same slot?
- If packed, is the slot already read earlier in the call? (warm = 100 gas instead of 2100)
- Is the read inside a loop? If so, how many iterations?
- Could the value be passed as a function parameter instead of re-reading storage?

4. **Per-Function SLOAD Breakdown:**

For each function in scope, list:
- Total cold SLOADs (first access to a slot)
- Total warm SLOADs (slot already accessed)
- Total gas from SLOADs
- Which SLOADs are per-winner (multiplied by 321) vs per-call (fixed cost)

5. **SLOAD Optimization Candidates:**

For each SLOAD with verdict OPTIMIZE or MARGINAL:
- What the optimization would be (e.g., "pass gameOver as parameter instead of re-reading Slot 0")
- Gas saved: exact calculation (e.g., "100 gas/winner x 321 = 32,100 gas")
- Complexity cost: what code changes are needed
- Risk: could this change behavior?
- Recommendation: implement or skip

6. **Cumulative SLOAD Summary:**
- Total SLOADs in worst case (321 auto-rebuy winners)
- Total gas from SLOADs
- How much is unavoidable (unique per-address mappings)
- How much is potentially optimizable
- Percentage of total gas budget consumed by SLOADs

**Key analysis technique:** For each SLOAD, answer these questions:
1. Is this the first access to this EVM slot in this transaction? (cold = 2100, warm = 100)
2. Is this access inside the per-winner loop? (multiplied by 321 vs fixed cost)
3. Could this value be computed or passed instead of read from storage?
4. Is this a mapping access with a unique key per iteration? (unavoidable cold)
  </action>
  <verify>
    <automated>grep -c "SLOAD" .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 96-OPTIMIZATION-AUDIT.md exists
    - Contains "SLOAD Inventory" or "SLOAD Audit" table header
    - Contains at least 8 SLOAD entries (gameOver, autoRebuyState, claimableWinnings, traitBurnTicket, level, prizePoolsPacked, ticketsOwedPacked, currentPrizePool)
    - Each entry has Cold/Warm classification
    - Each entry has a Verdict (NECESSARY, ALREADY-OPTIMIZED, OPTIMIZE, or MARGINAL)
    - Contains "Per-Function SLOAD Breakdown" section
    - Contains "SLOAD Optimization Candidates" section (even if empty, documenting "none found")
    - Contains "gameOver" with verdict (MARGINAL or OPTIMIZE -- it's a warm Slot 0 read per winner)
    - Contains "autoRebuyState" with verdict NECESSARY (cold, unique per winner, unavoidable)
    - Contains "claimableWinnings" with verdict NECESSARY (cold, unique per winner, unavoidable)
    - GOPT-01 fully addressed: every SLOAD in the daily jackpot path is cataloged
  </acceptance_criteria>
  <done>Every SLOAD in the daily jackpot hot path is inventoried with source line, EVM slot, cold/warm status, per-call multiplicity, total gas impact, and optimization verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Loop hoisting audit of daily jackpot code path</name>
  <files>.planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</files>
  <read_first>
    - .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md (SLOAD audit from Task 1)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1329-1424 for _processDailyEthChunk loops, lines 928-999 for _addClaimableEth/_processAutoRebuy called per iteration)
    - .planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md (loop-hoistable computation candidates table)
  </read_first>
  <action>
Append a "Loop Hoisting Audit" section to `96-OPTIMIZATION-AUDIT.md`.

**Section structure:**

1. **Loop Inventory:** Catalog every loop in the audit scope:

| Loop | Location (file:line) | Bound | Iterations (worst case) | Body Contains |
|------|----------------------|-------|------------------------|---------------|

Loops to catalog:
- `_processDailyEthChunk` outer bucket loop (j < 4): 4 iterations, iterates over trait buckets
- `_processDailyEthChunk` inner winner loop (i < len): up to 250 per bucket, 321 total
- `_addClaimableEth` (called per winner, not itself a loop but a per-iteration function call)
- `_processAutoRebuy` (called per auto-rebuy winner)
- `payDailyJackpot` earlybird loop (Day 1 only, 100 iterations)

2. **Per-Loop Hoisting Analysis (GOPT-02):**

For each loop, examine every expression in the body:
- Is it loop-invariant (same value every iteration)?
- Is it a function call with constant arguments?
- Is it a storage read that could be cached before the loop?

**Already-hoisted computations to confirm:**
- `PriceLookupLib.priceForLevel(lvl+1)` -- already before outer loop at line ~1341. Confirm.
- `JackpotBucketLib.soloBucketIndex(entropy)` -- already before outer loop at line ~1342. Confirm.
- `JackpotBucketLib.bucketShares(...)` -- already before outer loop at line ~1343. Confirm.
- `JackpotBucketLib.bucketOrderLargestFirst(...)` -- already before outer loop at line ~1351. Confirm.
- `claimablePool` accumulation: uses memory variable `liabilityDelta`, single SSTORE at end. Confirm.

**Potentially hoistable (analyze each):**
- `gameOver` check in `_addClaimableEth`: read per winner (warm SLOAD = 100 gas). Could be passed as parameter. Savings: 100 x 321 = 32,100 gas. Worth the added parameter complexity?
- `level` read in `_processAutoRebuy`: read per auto-rebuy winner (warm SLOAD = 100 gas, same Slot 0). Could be passed as parameter. Savings: 100 x 321 = 32,100 gas.
- Any arithmetic in the inner loop that depends only on outer loop variables (per-bucket constants like `perWinner`, `share`)?

3. **Hoisting Verdicts Table:**

| Computation | Loop | Invariant? | Current Gas | If Hoisted | Savings | Verdict |
|-------------|------|------------|-------------|------------|---------|---------|

Verdicts: ALREADY-HOISTED, HOIST (recommend), SKIP (marginal savings or excessive complexity), NOT-INVARIANT (depends on iteration variable).

4. **Optimization Summary:**

Combine SLOAD audit and loop hoisting findings:
- Total potential gas savings from all OPTIMIZE/HOIST verdicts
- Ranked list of optimizations by gas impact
- Recommendation: which optimizations (if any) are worth implementing in GOPT-03
- If no optimizations are worth implementing, explicitly state "No actionable optimizations found" with justification (e.g., "remaining costs are dominated by unavoidable per-address cold SLOADs")
  </action>
  <verify>
    <automated>grep -c "Loop\|Hoisting\|hoistable\|invariant" .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 96-OPTIMIZATION-AUDIT.md contains "Loop Hoisting Audit" or "Loop Hoisting" section header
    - Contains "Loop Inventory" table with at least 3 loops cataloged
    - Contains "Hoisting Verdicts" or similar table
    - Contains "PriceLookupLib.priceForLevel" with ALREADY-HOISTED verdict
    - Contains "bucketShares" or "bucketOrderLargestFirst" with ALREADY-HOISTED verdict
    - Contains "gameOver" hoisting analysis with a verdict and gas calculation
    - Contains "Optimization Summary" section with a recommendation for GOPT-03
    - GOPT-02 fully addressed: every loop body analyzed for hoistable computation
  </acceptance_criteria>
  <done>Every loop in the daily jackpot code path is analyzed for hoistable computation. Each computation has a verdict (ALREADY-HOISTED, HOIST, SKIP, NOT-INVARIANT). Combined optimization summary recommends which (if any) optimizations to implement in GOPT-03.</done>
</task>

</tasks>

<verification>
- `96-OPTIMIZATION-AUDIT.md` exists with both SLOAD audit and loop hoisting sections
- Every SLOAD has a verdict
- Every loop body computation has a verdict
- Combined optimization summary provides actionable recommendation for Plan 03 (GOPT-03)
</verification>

<success_criteria>
- GOPT-01: Every SLOAD in the daily jackpot hot path is cataloged with cold/warm, per-call count, gas impact, and necessity verdict
- GOPT-02: Every loop body is analyzed for hoistable computation with clear verdicts
- Optimization summary ranks findings by gas impact and recommends implementation path
</success_criteria>

<output>
After completion, create `.planning/phases/96-gas-ceiling-optimization/96-02-SUMMARY.md`
</output>
