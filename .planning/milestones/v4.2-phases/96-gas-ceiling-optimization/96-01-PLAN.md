---
phase: 96-gas-ceiling-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md
autonomous: true
requirements: [CEIL-01, CEIL-02, CEIL-03]

must_haves:
  truths:
    - "Worst-case gas for _processDailyEthChunk (321 winners, all auto-rebuy, 4 populated buckets) is documented with a specific number"
    - "Worst-case gas for payDailyJackpot Phase 0 and Phase 1 are documented separately with specific numbers"
    - "Every profiled stage has a risk classification (SAFE/TIGHT/AT_RISK/BREACH) and headroom percentage"
    - "All profiled paths are confirmed SAFE under 14M or the breach conditions are documented"
  artifacts:
    - path: ".planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md"
      provides: "Post-chunk-removal gas ceiling analysis for daily jackpot stages"
      contains: "Worst-Case Total"
  key_links:
    - from: "96-GAS-ANALYSIS.md"
      to: "57-01-advancegame-gas-analysis.md"
      via: "Updated estimates referencing Phase 57 baseline"
      pattern: "Phase 57.*AT_RISK|TIGHT|SAFE"
---

<objective>
Profile worst-case gas consumption for the daily jackpot code path after Phase 95's chunk removal, and determine whether all paths remain under the 14M gas ceiling.

Purpose: Phase 57 classified Stages 11 and 8 as AT_RISK and Stage 6 as TIGHT. The chunk removal in Phase 95 eliminated dead code (unitsBudget, cursors, _winnerUnits) which changes the gas profile. This plan re-profiles all three stages to produce updated risk classifications.

Output: `96-GAS-ANALYSIS.md` -- comprehensive gas analysis document with per-stage worst-case totals, headroom calculations, and updated risk levels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md
@.planning/milestones/v3.5-phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md

<interfaces>
<!-- Phase 57 risk classifications (baseline to update): -->
<!-- Stage 11 (JACKPOT_DAILY_STARTED): ~14,162,000 gas, AT_RISK (Day 1 breach by 162K) -->
<!-- Stage 8 (JACKPOT_ETH_RESUME): ~13,122,400 gas, AT_RISK (<1M headroom) -->
<!-- Stage 6 (PURCHASE_DAILY): ~12,952,000 gas, TIGHT (1-3M headroom) -->

<!-- Post-chunk-removal changes (from 96-RESEARCH.md): -->
<!-- _winnerUnits removed: saves 1 cold SLOAD (2100 gas) per winner if compiler didn't CSE it -->
<!-- unitsBudget removed: max winners now 321 (DAILY_ETH_MAX_WINNERS) not 333 (1000/3) -->
<!-- Cursor save/restore removed: 2 SSTOREs eliminated (but was dead path) -->
<!-- _skipEntropyToBucket removed: function call overhead eliminated -->

<!-- Key constants post-removal: -->
<!-- DAILY_ETH_MAX_WINNERS = 321 -->
<!-- MAX_BUCKET_WINNERS = 250 -->
<!-- DAILY_CARRYOVER_MIN_WINNERS = 20 -->
<!-- JACKPOT_MAX_WINNERS = 300 (Stage 6 only) -->
<!-- DAILY_COIN_MAX_WINNERS = 50 -->
<!-- LOOTBOX_MAX_WINNERS = 100 -->

<!-- Gas ceiling target: 14,000,000 gas -->
<!-- Compiler: Solidity 0.8.34, via_ir=true, optimizer_runs=2 -->

<!-- Per-winner cost breakdown (from RESEARCH.md): -->
<!-- Normal winner: ~9,300 gas (gameOver warm SLOAD + autoRebuyState cold SLOAD + claimableWinnings cold SLOAD/SSTORE) -->
<!-- Auto-rebuy winner: ~23,000-25,000 gas (above + _calcAutoRebuy + _queueTickets + pool SSTORE + event) -->
<!-- Event per winner: ~3,500 gas (JackpotTicketWinner: 4 indexed + 1 data) -->

<!-- Common overhead (from Phase 57): -->
<!-- 21,000 tx base + ~42,000 cold preamble + ~2,600 delegatecall + ~1,500 Advance event + ~5,000 bounty = ~75,000 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static gas analysis for post-chunk-removal daily jackpot stages</name>
  <files>.planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1329-1424 for _processDailyEthChunk, lines 313-638 for payDailyJackpot, lines 928-949 for _addClaimableEth, lines 959-999 for _processAutoRebuy)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 340-397 for stage dispatch)
    - .planning/milestones/v3.5-phases/57-gas-ceiling-analysis/57-01-advancegame-gas-analysis.md (full document for Phase 57 baseline)
    - .planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md (per-winner cost breakdown, SLOAD patterns, removed code savings)
    - .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md (chunk removal proof, confirms 321 x 3 = 963 < 1000)
  </read_first>
  <action>
Create `.planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md` with the following structure. This is a static analysis document (no code changes).

**Document structure:**

1. **Header:** Phase 96 Gas Ceiling Analysis, date, compiler settings (Solidity 0.8.34, via_ir=true, optimizer_runs=2), gas ceiling target (14,000,000)

2. **Methodology:** Same as Phase 57 (per-operation gas accounting) but updated for post-chunk-removal code. Reference Phase 57 as baseline.

3. **What Changed (Phase 95 Chunk Removal):**
   - Table of removed elements with per-element gas impact:
     - `_winnerUnits(w)` removed: saved 1 cold SLOAD (2100 gas) per winner IF compiler didn't CSE. With 321 max winners, potential savings = 674,100 gas.
     - `unitsUsed + cost > unitsBudget` check removed: saved ~100 gas per winner x 321 = ~32,100 gas
     - Cursor save (dailyEthBucketCursor, dailyEthWinnerCursor) removed: 2 SSTOREs eliminated but these were on the dead path (never executed), so 0 gas saved in practice
     - `_skipEntropyToBucket` removed: function call overhead ~500 gas (but was zero-iteration loop)
     - Winner cap changed: 333 (unitsBudget=1000/3) -> 321 (DAILY_ETH_MAX_WINNERS). 12 fewer worst-case winners.

4. **Stage 11 (JACKPOT_DAILY_STARTED) -- Updated Analysis:**
   - Reproduce Phase 57's component table but with updated values:
     - Common overhead: 75,000 (unchanged)
     - Delegatecall: ~2,600 (unchanged)
     - rngGate: ~5,000 (unchanged)
     - Daily init: ~60,000 (unchanged)
     - _runEarlyBirdLootboxJackpot (Day 1 only): ~1,000,000 (unchanged)
     - _processDailyEthChunk: Recompute for 321 winners (not 333). Per auto-rebuy winner cost:
       - gameOver check: 100 gas (warm SLOAD, Slot 0 already read)
       - autoRebuyState[w]: 2,100 gas (cold SLOAD, unique per winner) -- NOTE: _winnerUnits no longer reads this first, so this is now the ONLY read
       - _calcAutoRebuy arithmetic + PriceLookupLib: ~5,000 gas
       - _queueTickets SLOAD+SSTORE: ~7,200 gas (cold per winner+level)
       - prizePoolsPacked SSTORE: ~200 gas (warm after first)
       - _creditClaimable (if reserved): ~7,200 gas
       - Total per auto-rebuy winner: ~21,800-25,000 gas
     - Winner storage: 321 cold autoRebuyState SLOADs (674,100) + 321 claimableWinnings/ticketsOwedPacked cold SSTOREs
     - Events: 321 x JackpotTicketWinner (~3,500 each) + 321 x AutoRebuyExecuted/PlayerCredited (~3,500 each) = ~2,247,000
   - Worst-case Day 1 total (with earlybird + all auto-rebuy)
   - Worst-case non-Day-1 total (no earlybird, all auto-rebuy)
   - Headroom for both cases
   - Risk classification

5. **Stage 8 (JACKPOT_ETH_RESUME) -- Updated Analysis:**
   - Post-chunk-removal: no more cursor resume (cursors removed). Stage 8 now triggers when `dailyEthPhase != 0 || dailyEthPoolBudget != 0` (check the exact resume conditions in AdvanceModule.sol lines 348-357).
   - Phase 1 carryover path: `_processDailyEthChunk` with `dailyCarryoverWinnerCap` winners (max 321, but typically = max(DAILY_ETH_MAX_WINNERS - Phase0Winners, DAILY_CARRYOVER_MIN_WINNERS=20))
   - Worst case: 321 carryover winners (entire allocation to Phase 1, none in Phase 0) -- is this achievable? Check logic.
   - Realistic worst case: Phase 0 used some winners, Phase 1 gets remainder (likely much smaller)
   - Recompute: same per-winner cost as Stage 11 but no init, no earlybird
   - Resume state restore: ~10,000 gas (read stored traits, level, phase)
   - Headroom and risk classification

6. **Stage 6 (PURCHASE_DAILY) -- Updated Analysis:**
   - Stage 6 uses `_executeJackpot` -> `_distributeJackpotEth`, NOT `_processDailyEthChunk`. Chunk removal does NOT affect this path.
   - Verify: is Stage 6 unchanged? Check _distributeJackpotEth in JackpotModule for any changes from Phase 95.
   - If unchanged: carry forward Phase 57's estimate (~12,952,000) and risk level (TIGHT).
   - Also include _payDailyCoinJackpot overhead (50 coin winners + 10 far-future).

7. **Summary Table:** All three stages with old estimate, new estimate, headroom, risk level, and what changed.

8. **14M Ceiling Verdict (CEIL-03):** For each stage, state whether it is SAFE/TIGHT/AT_RISK/BREACH with:
   - Headroom in gas and percentage
   - The specific conditions required to reach worst case
   - Whether those conditions are economically realistic
   - A one-line verdict

9. **Comparison with Phase 57:** Table showing before/after for each stage, highlighting which improved and by how much.

**Key analysis points to verify during code reading:**
- Confirm `_winnerUnits` is truly gone from `_processDailyEthChunk` (it was removed in Phase 95)
- Confirm the resume condition in AdvanceModule for Stage 8 no longer checks `dailyEthBucketCursor` or `dailyEthWinnerCursor` (removed variables)
- Confirm Stage 6 (`_executeJackpot` path) is truly unaffected by chunk removal
- Count the exact per-winner operations in the current `_addClaimableEth` and `_processAutoRebuy` code
  </action>
  <verify>
    <automated>grep -c "Worst-Case Total" .planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - 96-GAS-ANALYSIS.md exists and contains "Stage 11" AND "Stage 8" AND "Stage 6" sections
    - Document contains "Worst-Case Total" at least 3 times (one per stage)
    - Document contains "Headroom" at least 3 times (one per stage)
    - Document contains "Risk Level" at least 3 times (one per stage classification)
    - Document contains "14M" or "14,000,000" (ceiling reference)
    - Document contains "321" (post-removal winner cap)
    - Document contains a "Summary Table" section with all three stages
    - Document contains a "Comparison with Phase 57" section
    - CEIL-01 addressed: _processDailyEthChunk worst case has a specific gas number
    - CEIL-02 addressed: payDailyJackpot Phase 0 and Phase 1 each have specific gas numbers
    - CEIL-03 addressed: 14M ceiling verdict section exists with per-stage SAFE/TIGHT/AT_RISK/BREACH classification
  </acceptance_criteria>
  <done>All three daily jackpot stages (11, 8, 6) have updated worst-case gas estimates with specific numbers, headroom calculations, and risk classifications. CEIL-01 (processDailyEthChunk profiled), CEIL-02 (payDailyJackpot both phases profiled), and CEIL-03 (14M ceiling verdict) are all documented.</done>
</task>

<task type="auto">
  <name>Task 2: Empirical gas validation via Hardhat gas test</name>
  <files>.planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md</files>
  <read_first>
    - .planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md (the static analysis just created in Task 1)
    - test/gas/AdvanceGameGas.test.js (existing gas test pattern, full file)
  </read_first>
  <action>
Run the existing Hardhat gas test to get empirical gas measurements for the daily jackpot stages. This validates or corrects the static analysis from Task 1.

**Steps:**

1. First, check for ContractAddresses.sol unstaged changes (STATE.md blocker):
   ```
   git stash (if ContractAddresses.sol has changes)
   ```

2. Run the Hardhat gas test:
   ```
   npx hardhat test test/gas/AdvanceGameGas.test.js
   ```

3. Capture the gas benchmark summary table output.

4. Find the empirical gas numbers for:
   - Stage 11 (JACKPOT_DAILY_STARTED) related tests
   - Stage 8 (JACKPOT_ETH_RESUME) related tests
   - Stage 6 (PURCHASE_DAILY) related tests

5. If `git stash` was used, run `git stash pop` to restore.

6. Append an "Empirical Validation" section to `96-GAS-ANALYSIS.md` with:
   - The raw Hardhat gas numbers for each relevant test
   - Comparison table: Static Estimate vs Empirical Measurement for each stage
   - Delta analysis: where static analysis was accurate and where it diverged
   - Note: The Hardhat test may not achieve the TRUE worst case (321 all-auto-rebuy winners) since it drives state organically. Document what scenario the test actually achieves.
   - Updated confidence level for each estimate based on empirical backing

**Important:** The Hardhat gas test drives the game through organic state progression. It may not construct the exact worst-case scenario (321 auto-rebuy winners). The empirical data supplements the static analysis -- it does not replace it. If the empirical number is lower than the static estimate, that's expected (test scenario less extreme than theoretical worst case). If the empirical number is HIGHER, that indicates the static analysis missed something.
  </action>
  <verify>
    <automated>grep -c "Empirical" .planning/phases/96-gas-ceiling-optimization/96-GAS-ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - 96-GAS-ANALYSIS.md contains "Empirical Validation" section
    - Section contains at least one gas number from Hardhat test output (a number with commas or in the millions range)
    - Section contains "Static.*Empirical" or a comparison table header
    - Hardhat test ran successfully (test output captured)
  </acceptance_criteria>
  <done>Empirical gas measurements from Hardhat test are appended to the gas analysis document. Static estimates are compared against empirical data with delta analysis.</done>
</task>

</tasks>

<verification>
- `96-GAS-ANALYSIS.md` exists with complete gas analysis for Stages 11, 8, and 6
- Each stage has a specific worst-case gas number (not a range)
- Each stage has a risk classification (SAFE/TIGHT/AT_RISK/BREACH)
- 14M ceiling verdict documents headroom for all paths
- Empirical validation section compares static analysis against Hardhat measurements
- Document references Phase 57 baseline for comparison
</verification>

<success_criteria>
- CEIL-01: _processDailyEthChunk worst-case gas is documented with a specific number and the scenario (321 winners, all auto-rebuy, 4 populated buckets)
- CEIL-02: payDailyJackpot Phase 0 and Phase 1 worst-case gas are documented separately
- CEIL-03: All profiled paths have a 14M ceiling verdict with headroom percentage
- Static analysis validated against at least one empirical Hardhat measurement
</success_criteria>

<output>
After completion, create `.planning/phases/96-gas-ceiling-optimization/96-01-SUMMARY.md`
</output>
