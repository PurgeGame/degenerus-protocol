---
phase: 95-delta-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/AffiliateDgnrsClaim.t.sol
  - test/fuzz/TicketLifecycle.t.sol
  - test/fuzz/StorageFoundation.t.sol
  - test/fuzz/FuturepoolSkim.t.sol
autonomous: true
requirements: [DELTA-04]

must_haves:
  truths:
    - "All 14 NEW Foundry test failures are fixed by updating stale slot offset constants"
    - "The 15 pre-existing Foundry failures remain unchanged (no new regressions, no accidental fixes)"
    - "Every vm.store and vm.load call in the 4 affected test files uses offsets matching forge inspect output"
  artifacts:
    - path: "test/fuzz/AffiliateDgnrsClaim.t.sol"
      provides: "Fixed mapping slot constants (32/33 instead of 51/52)"
      contains: "SLOT_LEVEL_DGNRS_ALLOCATION = 32"
    - path: "test/fuzz/TicketLifecycle.t.sol"
      provides: "Fixed bit shift constants for slot 0/1 fields"
      contains: "WRITE_SLOT_SHIFT = 176"
    - path: "test/fuzz/StorageFoundation.t.sol"
      provides: "Fixed field offset assertions for slot 1 fields"
      contains: "ticketWriteSlot not at offset 22"
    - path: "test/fuzz/FuturepoolSkim.t.sol"
      provides: "Fixed SkimHarness initialization if needed"
  key_links:
    - from: "test/fuzz/TicketLifecycle.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "vm.store/vm.load with bit shift constants"
      pattern: "WRITE_SLOT_SHIFT|COMPRESSED_FLAG_SHIFT"
    - from: "test/fuzz/AffiliateDgnrsClaim.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "vm.store with mapping slot numbers"
      pattern: "SLOT_LEVEL_DGNRS_ALLOCATION"
---

<objective>
Fix all 14 NEW Foundry test failures caused by stale hardcoded storage layout constants in 4 test files.

Purpose: The chunk removal shifted storage packing in slots 0 and 1. Tests using vm.store/vm.load with hardcoded slot numbers and bit offsets now write to wrong positions, corrupting state and causing E() reverts or assertion failures. All 14 are test infrastructure bugs, not contract regressions.
Output: All 4 test files updated with authoritative slot offsets from `forge inspect DegenerusGame storage`. 14 new failures eliminated. 15 pre-existing failures remain unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-delta-verification/95-RESEARCH.md

<interfaces>
<!-- Authoritative storage layout from `forge inspect DegenerusGame storage` (2026-03-24) -->

SLOT 0 (32 bytes, fully packed, 0 padding):
| Field                          | Offset | Bytes | Bit Shift |
|--------------------------------|--------|-------|-----------|
| levelStartTime                 | 0      | 6     | 0         |
| dailyIdx                       | 6      | 6     | 48        |
| rngRequestTime                 | 12     | 6     | 96        |
| level                          | 18     | 3     | 144       |
| jackpotPhaseFlag               | 21     | 1     | 168       |
| jackpotCounter                 | 22     | 1     | 176       |
| poolConsolidationDone          | 23     | 1     | 184       |
| lastPurchaseDay                | 24     | 1     | 192       |
| decWindowOpen                  | 25     | 1     | 200       |
| rngLockedFlag                  | 26     | 1     | 208       |
| phaseTransitionActive          | 27     | 1     | 216       |
| gameOver                       | 28     | 1     | 224       |
| dailyJackpotCoinTicketsPending | 29     | 1     | 232       |
| dailyEthPhase                  | 30     | 1     | 240       |
| compressedJackpotFlag          | 31     | 1     | 248       |

SLOT 1 (25 bytes, 7 padding):
| Field                   | Offset | Bytes | Bit Shift |
|-------------------------|--------|-------|-----------|
| purchaseStartDay        | 0      | 6     | 0         |
| price                   | 6      | 16    | 48        |
| ticketWriteSlot         | 22     | 1     | 176       |
| ticketsFullyProcessed   | 23     | 1     | 184       |
| prizePoolFrozen         | 24     | 1     | 192       |

KEY MAPPING SLOTS:
| Field                  | Slot |
|------------------------|------|
| levelDgnrsAllocation   | 32   |
| levelDgnrsClaimed      | 33   |
| lootboxRngWordByIndex  | 49   |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AffiliateDgnrsClaim.t.sol mapping slot constants</name>
  <files>test/fuzz/AffiliateDgnrsClaim.t.sol</files>
  <read_first>
    - test/fuzz/AffiliateDgnrsClaim.t.sol
    - contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Fix the stale mapping slot constants and stale NatSpec in AffiliateDgnrsClaim.t.sol.

**Constants to change:**

1. Line 26: `SLOT_LEVEL_DGNRS_ALLOCATION = 51` --> `SLOT_LEVEL_DGNRS_ALLOCATION = 32`
   - Authoritative value from `forge inspect`: levelDgnrsAllocation is at slot 32.

2. Line 27: `SLOT_LEVEL_DGNRS_CLAIMED = 52` --> `SLOT_LEVEL_DGNRS_CLAIMED = 33`
   - Authoritative value from `forge inspect`: levelDgnrsClaimed is at slot 33.

3. Update the comment on line 24 from `// Storage slots (from forge inspect DegenerusGame storage-layout)` to indicate the authoritative values were verified on 2026-03-24 post chunk removal.

4. Update the file-level NatSpec on line 12: change `(slot 51)` to `(slot 32)` to match the actual slot.

**No other changes needed.** The `_setLevel` helper manipulates slot 0 at bit offset 144 for `level` -- this offset has NOT changed (level is still at slot 0 offset 18). The slot 0 read-modify-write in `_setLevel` is safe because it correctly masks only the level bits.

After the fix, run: `forge test --match-contract AffiliateDgnrsClaim -vv`
All 8 previously-failing tests should now pass. Verify the test names that now pass.
  </action>
  <verify>
    <automated>forge test --match-contract AffiliateDgnrsClaim --summary 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AffiliateDgnrsClaim.t.sol contains `SLOT_LEVEL_DGNRS_ALLOCATION = 32`
    - AffiliateDgnrsClaim.t.sol contains `SLOT_LEVEL_DGNRS_CLAIMED = 33`
    - AffiliateDgnrsClaim.t.sol contains `(slot 32)` in the file-level NatSpec (not `slot 51`)
    - `forge test --match-contract AffiliateDgnrsClaim` shows 0 failures for this contract
    - No changes to any assertion logic, only constant values and comments
  </acceptance_criteria>
  <done>All 8 AffiliateDgnrsClaim tests pass with corrected mapping slot constants (32/33 instead of 51/52).</done>
</task>

<task type="auto">
  <name>Task 2: Fix TicketLifecycle.t.sol bit shift constants and slot 0/1 manipulation</name>
  <files>test/fuzz/TicketLifecycle.t.sol</files>
  <read_first>
    - test/fuzz/TicketLifecycle.t.sol
    - contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Fix stale bit shift constants and slot manipulation in TicketLifecycle.t.sol. The chunk removal moved `dailyEthPhase` and `compressedJackpotFlag` from slot 1 to slot 0, and shifted all slot 1 fields down.

**Constants to change:**

1. Line 93: `WRITE_SLOT_SHIFT = 184` --> `WRITE_SLOT_SHIFT = 176`
   - ticketWriteSlot moved from slot 1 offset 23 (bit 184) to offset 22 (bit 176).

2. Line 96: `COMPRESSED_FLAG_SHIFT = 8` --> this constant needs a complete rethink.
   - compressedJackpotFlag MOVED from slot 1 offset 1 (bit 8) to slot 0 offset 31 (bit 248).
   - The constant should be renamed or its usage updated. Since it is now in slot 0 (not slot 1), every place that reads/writes it via `SLOT_1` must now use `SLOT_0`.
   - Change to: `uint256 private constant COMPRESSED_FLAG_SHIFT = 248;`

3. Update the NatSpec comments for the constants:
   - Line 92: Change `slot 1 offset 23 bytes = bits 184-191` to `slot 1 offset 22 bytes = bits 176-183`
   - Line 95: Change `slot 1 offset 1 byte = bits 8-15` to `slot 0 offset 31 bytes = bits 248-255`

4. Update the slot 1 NatSpec docstring (lines 31-34):
   - Remove `[0:1]dailyEthPhase` and `[1:2]compressedJackpotFlag` from the slot 1 description
   - Update: `[0:6]purchaseStartDay [6:22]price [22:23]ticketWriteSlot`

**Code changes for compressedJackpotFlag manipulation:**

In `testLastDayTicketsRouteToNextLevel()` (around line 592-594):
- OLD: Reads from `SLOT_1`, clears bits at `COMPRESSED_FLAG_SHIFT` (8), writes to `SLOT_1`
- NEW: Must read from `SLOT_0`, clear bits at position 248, write to `SLOT_0`
- Change lines 592-594 from:
  ```
  uint256 slot1val = uint256(vm.load(address(game), bytes32(uint256(SLOT_1))));
  slot1val = slot1val & ~(uint256(0xFF) << COMPRESSED_FLAG_SHIFT);
  vm.store(address(game), bytes32(uint256(SLOT_1)), bytes32(slot1val));
  ```
  To incorporate the compressedJackpotFlag clear into the slot 0 write that already happens on line 589. After the existing `slot0 = slot0 | (uint256(1) << RNG_LOCKED_SHIFT);` line, add:
  ```
  // Clear compressedJackpotFlag (now at slot 0 bit 248) for normal mode (step=1)
  slot0 = slot0 & ~(uint256(0xFF) << COMPRESSED_FLAG_SHIFT);
  ```
  Then REMOVE the separate slot 1 read/clear/write (lines 592-594), since compressedJackpotFlag is no longer in slot 1.

Also update the `_getWriteSlot()` helper (around line 2200-2204):
- The comment says `slot 1 offset 23 bytes (bits 184-191)` -- change to `slot 1 offset 22 bytes (bits 176-183)`
- The shift value comes from `WRITE_SLOT_SHIFT` constant which will be updated to 176, so no code change needed in the helper body itself.

After the fix, run: `forge test --match-contract TicketLifecycleTest -vv --summary`
The 5 NEW failures in this contract should now pass. The 3 pre-existing failures should remain.
  </action>
  <verify>
    <automated>forge test --match-contract TicketLifecycleTest --summary 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TicketLifecycle.t.sol contains `WRITE_SLOT_SHIFT = 176`
    - TicketLifecycle.t.sol contains `COMPRESSED_FLAG_SHIFT = 248`
    - The `testLastDayTicketsRouteToNextLevel` function clears compressedJackpotFlag via SLOT_0 (not SLOT_1)
    - No remaining `vm.load(address(game), bytes32(uint256(SLOT_1)))` for compressedJackpotFlag manipulation
    - `forge test --match-contract TicketLifecycleTest` shows exactly 3 failures (the pre-existing ones), not 8
    - NatSpec comments reference correct offsets (22 not 23 for writeSlot, 248 not 8 for compressedFlag)
  </acceptance_criteria>
  <done>5 new TicketLifecycle failures fixed. WRITE_SLOT_SHIFT corrected to 176, COMPRESSED_FLAG_SHIFT corrected to 248 and moved to slot 0 manipulation. 3 pre-existing failures remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Fix StorageFoundation.t.sol and FuturepoolSkim.t.sol offset assertions</name>
  <files>test/fuzz/StorageFoundation.t.sol, test/fuzz/FuturepoolSkim.t.sol</files>
  <read_first>
    - test/fuzz/StorageFoundation.t.sol
    - test/fuzz/FuturepoolSkim.t.sol
    - contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Fix stale field offset assertions in StorageFoundation.t.sol and investigate FuturepoolSkim.t.sol.

**StorageFoundation.t.sol -- `testSlot1FieldOffsets()` (lines 93-114):**

The test asserts slot 1 field offsets that are now wrong because fields shifted:

1. Line 93 NatSpec: Change `offset 24` to `offset 22` for ticketWriteSlot, `offset 25` to `offset 23` for ticketsFullyProcessed, `offset 26` to `offset 24` for prizePoolFrozen.

2. Line 100-101: Change assertion from `>> 192` to `>> 176`:
   - OLD: `assertEq(uint8(uint256(slot1) >> 192), 1, "ticketWriteSlot not at offset 24");`
   - NEW: `assertEq(uint8(uint256(slot1) >> 176), 1, "ticketWriteSlot not at offset 22");`

3. Line 107: Change assertion from `>> 200` to `>> 184`:
   - OLD: `assertEq(uint8(uint256(slot1) >> 200), 1, "ticketsFullyProcessed not at offset 25");`
   - NEW: `assertEq(uint8(uint256(slot1) >> 184), 1, "ticketsFullyProcessed not at offset 23");`

4. Line 113: Change assertion from `>> 208` to `>> 192`:
   - OLD: `assertEq(uint8(uint256(slot1) >> 208), 1, "prizePoolFrozen not at offset 26");`
   - NEW: `assertEq(uint8(uint256(slot1) >> 192), 1, "prizePoolFrozen not at offset 24");`

5. Update all assertion message strings to reference the new correct offsets (22, 23, 24 instead of 24, 25, 26).

6. Update the comments on lines 98-100:
   - OLD: `offset 24 means byte 24 ... ticketWriteSlot at offset 24 = bits [192, 200)`
   - NEW: `offset 22 means byte 22 ... ticketWriteSlot at offset 22 = bits [176, 184)`

**FuturepoolSkim.t.sol:**

The `test_pipeline_varianceBeforeCap` failure shows a numeric mismatch (79932... != 80000...). This test uses a `SkimHarness` that inherits `DegenerusGameAdvanceModule` which inherits `DegenerusGameStorage`. The harness calls `exposed_setPrizePools` and `exposed_applyTimeBasedFutureTake` which write to `prizePoolsPacked` at slot 3 -- a full-slot uint256 that has NOT shifted.

The issue is likely that the SkimHarness, being a standalone contract (not deployed via DeployProtocol), has its slot 0 initialized differently. The `_applyTimeBasedFutureTake` function reads `levelStartTime` from slot 0 offset 0 -- this hasn't changed. Run the test with `-vvvv` first to determine the exact failure cause:
`forge test --match-test test_pipeline_varianceBeforeCap -vvvv`

If the failure is a rounding/precision issue NOT caused by the storage layout shift (the values 79932... vs 80000... suggest a BPS calculation variance, not a slot corruption), then document it as pre-existing exposed by recompilation and do not fix. If it IS caused by a stale offset, fix accordingly.

After fixes, run: `forge test --match-contract StorageFoundationTest --summary` and `forge test --match-contract FuturepoolSkimTest --summary`
  </action>
  <verify>
    <automated>forge test --match-contract StorageFoundationTest --summary 2>&1 | tail -10 && forge test --match-contract FuturepoolSkimTest --summary 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - StorageFoundation.t.sol `testSlot1FieldOffsets` asserts `>> 176` for ticketWriteSlot (not `>> 192`)
    - StorageFoundation.t.sol `testSlot1FieldOffsets` asserts `>> 184` for ticketsFullyProcessed (not `>> 200`)
    - StorageFoundation.t.sol `testSlot1FieldOffsets` asserts `>> 192` for prizePoolFrozen (not `>> 208`)
    - Assertion message strings reference offsets 22, 23, 24 (not 24, 25, 26)
    - `forge test --match-contract StorageFoundationTest` shows pre-existing failure count (2), not more
    - FuturepoolSkim failure is either fixed or documented as pre-existing with justification
  </acceptance_criteria>
  <done>StorageFoundation slot 1 offset assertions corrected (22/23/24 instead of 24/25/26). FuturepoolSkim investigated and either fixed or documented. Total new Foundry failures eliminated = 14, pre-existing count = 15 unchanged.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the full Foundry suite:
`forge test --summary`

Expected result: 15 failures (all pre-existing), 0 new failures.

Pre-existing 15:
- LootboxRngLifecycle (4)
- StorageFoundation (2)
- TicketLifecycle (3)
- VRFCore (2)
- VRFLifecycle (1)
- VRFStallEdgeCases (3)

If the count is not exactly 15 (or very close, accounting for the FuturepoolSkim investigation), investigate any discrepancy.
</verification>

<success_criteria>
- DELTA-04: `forge test --summary` shows exactly 15 failures (all pre-existing), down from 29 (15 pre-existing + 14 new)
- All 4 test files have constants matching authoritative `forge inspect DegenerusGame storage` output
- Every `vm.store`/`vm.load` call uses correct slot numbers and bit offsets
- No assertion logic changes -- only constant values and comments updated
</success_criteria>

<output>
After completion, create `.planning/phases/95-delta-verification/95-02-SUMMARY.md`
</output>
