---
phase: 95-delta-verification
plan: 03
type: execute
wave: 2
depends_on: [95-01, 95-02]
files_modified:
  - .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md
autonomous: true
requirements: [DELTA-03]

must_haves:
  truths:
    - "Side-by-side trace proves _processDailyEthChunk produces identical entropy chain, winner selection, payout amounts, and liability tracking with and without chunk infrastructure"
    - "The dead-code argument is proven with exact arithmetic: 321 * 3 = 963 < 1000"
    - "Every behavioral dimension (entropy, winners, payouts, cursors, return value) is explicitly compared"
  artifacts:
    - path: ".planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md"
      provides: "Formal behavioral equivalence proof document"
      contains: "963 < 1000"
  key_links:
    - from: ".planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "References _processDailyEthChunk implementation"
      pattern: "_processDailyEthChunk"
---

<objective>
Write a formal behavioral equivalence trace proving the chunk removal is side-effect-free.

Purpose: DELTA-03 requires a human-readable proof document that demonstrates identical behavior between the old (chunked) and new (single-pass) _processDailyEthChunk. This is the capstone evidence that the removal is safe, backed by the test suite results from Plans 01 and 02.
Output: 95-BEHAVIORAL-TRACE.md -- a formal trace document with 6 behavioral dimensions, concrete arithmetic, and a worked example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-delta-verification/95-RESEARCH.md
@.planning/phases/95-delta-verification/95-01-SUMMARY.md
@.planning/phases/95-delta-verification/95-02-SUMMARY.md

@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write behavioral equivalence trace document</name>
  <files>.planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (read _processDailyEthChunk function in full)
    - contracts/modules/DegenerusGameAdvanceModule.sol (read payDailyJackpot / advanceGame calling convention)
    - .planning/phases/95-delta-verification/95-RESEARCH.md (sections: Behavioral Equivalence Argument, Why the Chunking Was Dead Code)
  </read_first>
  <action>
Create `.planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md` with the following structure and content.

The document must prove behavioral equivalence across 6 dimensions by comparing old (chunked) vs new (single-pass) `_processDailyEthChunk`. Use the actual code from the contracts as the source of truth.

**Document structure:**

```
# Behavioral Equivalence Trace: _processDailyEthChunk

## Executive Summary
One-paragraph conclusion: chunk removal is behaviorally equivalent because [summary].

## 1. Dead Code Proof
Show the exact arithmetic proving the chunking path was unreachable:
- DAILY_JACKPOT_UNITS_SAFE = 1000 (the gas budget in abstract units)
- DAILY_JACKPOT_UNITS_AUTOREBUY = 3 (cost per auto-rebuy winner)
- DAILY_ETH_MAX_WINNERS = 321 (maximum daily ETH jackpot winners)
- Worst case: 321 winners * 3 units = 963 units < 1000 budget
- Therefore: unitsUsed never exceeds unitsBudget
- Therefore: `complete=false` return path was UNREACHABLE
- Therefore: cursors were ALWAYS (0, 0) on entry

## 2. Entropy Chain Equivalence
Walk through the entropy computation step by step:
- Old: _skipEntropyToBucket(entropy, 0) with startOrderIdx=0 returns entropy unchanged
- Old: For each non-empty bucket, entropy = EntropyLib.entropyStep(entropy ^ (traitIdx << 64) ^ share)
- New: Same entropy = EntropyLib.entropyStep(entropy ^ (traitIdx << 64) ^ share) for each bucket
- Conclusion: identical entropy state at every step

## 3. Winner Selection Equivalence
For each bucket iteration:
- Old: _randTraitTicketWithIndices(entropyState, traitIds[traitIdx], totalCount, 200+traitIdx)
- New: _randTraitTicketWithIndices(entropyState, traitIds[traitIdx], totalCount, 200+traitIdx)
- Same function, same arguments (since entropy is identical per section 2)
- Conclusion: identical winner arrays

## 4. Payout Amount Equivalence
For each bucket:
- Old: perWinner = share / totalCount; iterates winners[0..len-1] (startWinnerIdx=0 always)
- New: perWinner = share / totalCount; iterates winners[0..len-1]
- Same share, same totalCount, same iteration bounds
- Conclusion: identical payout per winner

## 5. Liability Tracking Equivalence
- Old: accumulates liabilityDelta across all buckets, writes claimablePool once
- New: accumulates liabilityDelta across all buckets, writes claimablePool once
- Same accumulation order, same write point
- Conclusion: identical liability tracking

## 6. Return Value and Caller Integration
- Old: returns (paidEth, true) where true = always complete
- New: returns paidEth (no complete flag)
- Caller (payDailyJackpot) updated to not check complete flag
- Net behavior: caller always proceeds to next phase, identical in both versions

## 7. Worked Example
Concrete example with 4 populated buckets:
- Bucket 0: 10 winners, share = 1 ETH
- Bucket 1: 0 winners (skipped)
- Bucket 2: 20 winners, share = 2 ETH
- Bucket 3: 5 winners, share = 0.5 ETH
Walk through entropy, selection, payout at each step for both old and new.
Show they produce identical results.

## 8. Removed Symbols Inventory
Table of all 6 removed symbols with their former purpose and why removal is safe:
| Symbol | Former Purpose | Why Safe to Remove |
|--------|---------------|-------------------|
| dailyEthBucketCursor | Resume bucket iteration | Always 0 (dead code) |
| dailyEthWinnerCursor | Resume winner iteration | Always 0 (dead code) |
| _skipEntropyToBucket | Replay entropy on resume | Never called with non-zero start |
| _winnerUnits | Compute unit cost per winner | Only used in budget tracking |
| DAILY_JACKPOT_UNITS_SAFE | Gas budget constant | Budget never exceeded |
| DAILY_JACKPOT_UNITS_AUTOREBUY | Auto-rebuy cost multiplier | Only used in budget tracking |

## 9. Test Evidence
Reference results from Plans 01 and 02:
- Hardhat: [pass/fail counts from 95-01-SUMMARY.md]
- Foundry: [pass/fail counts from 95-02-SUMMARY.md]
- Both confirm no behavioral regressions.

## Conclusion
The chunk removal is proven behaviorally equivalent. The removed infrastructure was dead code that could never activate under the protocol's constant constraints.
```

Use the ACTUAL function code from the contracts to fill in the specifics. Do not invent placeholder values -- read the real constants, the real loop structures, the real entropy calls. The trace must be verifiable by a reader holding both versions of the code.
  </action>
  <verify>
    <automated>test -f .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md && grep -c "963 < 1000" .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md && grep -c "_processDailyEthChunk" .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md && grep -c "Equivalence" .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/95-delta-verification/95-BEHAVIORAL-TRACE.md
    - Document contains the string "963 < 1000" (the dead-code arithmetic proof)
    - Document contains at least 6 sections covering entropy, winners, payouts, liability, return value, and worked example
    - Document references _processDailyEthChunk by name
    - Document contains the 6-row removed symbols inventory table
    - Document references the Hardhat and Foundry test results from Plans 01 and 02
    - Every behavioral claim is backed by a specific code reference (function name, line range, or constant name)
  </acceptance_criteria>
  <done>95-BEHAVIORAL-TRACE.md exists with a complete formal proof covering all 6 behavioral dimensions, the dead-code arithmetic, a worked example, and cross-references to test evidence from Plans 01 and 02.</done>
</task>

</tasks>

<verification>
- 95-BEHAVIORAL-TRACE.md exists and is non-trivial (> 100 lines)
- All 6 behavioral dimensions are covered with specific code references
- Dead-code proof arithmetic is explicit: 321 * 3 = 963 < 1000
- Worked example walks through a concrete 4-bucket scenario
- Test evidence section references actual pass/fail counts
</verification>

<success_criteria>
- DELTA-03: A human reviewer can read 95-BEHAVIORAL-TRACE.md and verify that _processDailyEthChunk produces identical entropy chain, winner selection, payout amounts, and liability tracking with and without chunk infrastructure
- The dead-code proof is self-contained (no need to read the code to verify the arithmetic)
- The document serves as audit evidence for C4A wardens reviewing the chunk removal
</success_criteria>

<output>
After completion, create `.planning/phases/95-delta-verification/95-03-SUMMARY.md`
</output>
