---
phase: 97-comment-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
autonomous: true
requirements:
  - CMT-01
user_setup: []

must_haves:
  truths:
    - "No inline comments reference removed concepts (chunks, cursors, bucket iteration resumption)"
    - "Storage layout header comments match forge inspect output exactly for Slot 0 and Slot 1"
    - "Function _processDailyEthChunk is renamed to _processDailyEth at definition and both call sites"
    - "NatSpec on _processDailyEth has accurate @dev, @param, and @return annotations"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Corrected storage layout comments for Slot 0 and Slot 1"
      contains: "32 bytes used (0 bytes padding)"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "Renamed function and corrected inline comments"
      contains: "_processDailyEth"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol line 1329"
      to: "contracts/modules/DegenerusGameJackpotModule.sol lines 495, 565"
      via: "function name must match at definition and call sites"
      pattern: "_processDailyEth\\("
    - from: "contracts/storage/DegenerusGameStorage.sol header comments"
      to: "forge inspect DegenerusGame storage output"
      via: "byte offsets in comments must match compiler storage layout"
      pattern: "dailyEthPhase.*30"
---

<objective>
Fix all stale NatSpec and inline comments in the 2 contract files modified during chunk removal, and rename the function that still carries the removed "Chunk" concept in its name.

Purpose: CMT-01 requires accurate comments for all modified code before the v4.2 milestone ships. The research phase identified 8 actionable issues across 2 files (~37 lines of edits).

Output: Two clean contract files with comments matching the post-chunk-removal reality, verified by `forge build` and `forge inspect`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-comment-cleanup/97-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix storage layout comments in DegenerusGameStorage.sol</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (lines 30-80 for header, lines 270-290 for tail/section comments)
    - .planning/phases/97-comment-cleanup/97-RESEARCH.md (Issues 1, 2, 3, 4, 10 for exact corrections)
  </read_first>
  <action>
Fix 5 storage layout comment issues. Use the forge inspect output from 97-RESEARCH.md as the single source of truth for all byte offsets.

**Issue 10 -- Line 35: Slot 0 title.**
Change:
```
 * | EVM SLOT 0 (32 bytes) -- Timing, FSM, Cursors, Counters, Flags              |
```
To:
```
 * | EVM SLOT 0 (32 bytes) -- Timing, FSM, Counters, Flags, ETH Phase           |
```

**Issue 1 -- Lines 49-52: Slot 0 padding and total.**
Replace lines 49-52 (currently showing `<padding> 2 bytes unused` and `Total: 30 bytes used (2 bytes padding)`) with:
```
 * | [30:31] dailyEthPhase            uint8    0=current level, 1=carryover       |
 * | [31:32] compressedJackpotFlag    uint8    0=normal, 1=compressed, 2=turbo    |
 * +-----------------------------------------------------------------------------+
 *   Total: 32 bytes used (0 bytes padding)
```

**Issue 2 -- Lines 54-66: Slot 1 header.**
Replace the entire Slot 1 block. Remove `dailyEthPhase` and `compressedJackpotFlag` entries. Shift all offsets down. The corrected Slot 1 block:
```
 * +-----------------------------------------------------------------------------+
 * | EVM SLOT 1 (32 bytes) -- Price and Double-Buffer Fields                     |
 * +-----------------------------------------------------------------------------+
 * | [0:6]   purchaseStartDay         uint48   Day index when purchase phase began|
 * | [6:22]  price                    uint128  Current mint price in wei          |
 * | [22:23] ticketWriteSlot          uint8    Double-buffer write index (0 or 1) |
 * | [23:24] ticketsFullyProcessed    bool     Read slot fully drained flag       |
 * | [24:25] prizePoolFrozen          bool     Prize pool freeze active flag      |
 * | [25:32] <padding>                         7 bytes unused                     |
 * +-----------------------------------------------------------------------------+
 *   Total: 25 bytes used (7 bytes padding)
```

**Issue 4 -- Lines 279-280: Slot 0 tail comment.**
Replace:
```
    // Note: dailyJackpotCoinTicketsPending (1 byte) is the tail of Slot 0 (byte 30),
    // followed by 2 bytes of padding.
```
With:
```
    // Note: dailyJackpotCoinTicketsPending ends at Slot 0 byte 29.
    // dailyEthPhase (byte 30) and compressedJackpotFlag (byte 31) fill the remaining Slot 0 space. No padding.
```

**Issue 3 -- Lines 283-285: Slot 1 section header.**
Change line 283 from:
```
    // EVM SLOT 1: ETH Phase, Price, and Double-Buffer Fields
```
To:
```
    // EVM SLOT 1: Price and Double-Buffer Fields
```
Change line 285 from:
```
    // Packs into EVM Slot 1: dailyEthPhase through prizePoolFrozen (27 bytes used, 5 bytes padding).
```
To:
```
    // Packs into EVM Slot 1: purchaseStartDay through prizePoolFrozen (25 bytes used, 7 bytes padding).
```

After all edits, run `forge build` to verify no syntax errors were introduced.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - contracts/storage/DegenerusGameStorage.sol contains "32 bytes used (0 bytes padding)" in the Slot 0 total line
    - contracts/storage/DegenerusGameStorage.sol contains "25 bytes used (7 bytes padding)" in the Slot 1 total line
    - contracts/storage/DegenerusGameStorage.sol contains "dailyEthPhase" in the Slot 0 layout block (between "EVM SLOT 0" and the Slot 0 Total line)
    - contracts/storage/DegenerusGameStorage.sol does NOT contain "Cursors" in the Slot 0 title line
    - contracts/storage/DegenerusGameStorage.sol does NOT contain "ETH Phase" in the Slot 1 title line
    - contracts/storage/DegenerusGameStorage.sol does NOT contain "followed by 2 bytes of padding"
    - `forge build` exits 0
  </acceptance_criteria>
  <done>All 5 storage layout comment issues corrected. Slot 0 shows 32/32 bytes with dailyEthPhase and compressedJackpotFlag. Slot 1 shows 25/32 bytes starting at purchaseStartDay. forge build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename _processDailyEthChunk and fix JackpotModule comments</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 320-325 for stale chunk comment, lines 493-497 for call site 1, lines 563-567 for call site 2, lines 1324-1340 for function definition and NatSpec)
    - .planning/phases/97-comment-cleanup/97-RESEARCH.md (Issues 5, 6, 7 for exact corrections)
  </read_first>
  <action>
Fix 3 comment/naming issues in the JackpotModule.

**Issue 5 -- Line 322: Stale "prior chunk" comment.**
Change:
```
            // Check if resuming from a prior chunk
```
To:
```
            // Check if resuming an interrupted daily jackpot (Phase 1 carryover)
```

**Issue 6 -- Lines 495, 565, 1329: Function rename.**
Rename `_processDailyEthChunk` to `_processDailyEth` at all 3 locations:

Line 495 (Phase 0 call site):
```
                    uint256 paidDailyEth = _processDailyEth(
```

Line 565 (Phase 1 call site):
```
                    _processDailyEth(
```

Line 1329 (function definition):
```
    function _processDailyEth(
```

**Issue 7 -- Line 1328: NatSpec on renamed function.**
Replace the current single-line NatSpec:
```
    /// @dev Processes daily jackpot ETH winners across all 4 trait buckets.
```
With full NatSpec:
```
    /// @dev Processes daily jackpot ETH distribution across all 4 trait buckets.
    ///      Iterates each bucket, selects winners proportional to shareBps,
    ///      pays out ETH via _addClaimableEth, and processes auto-rebuy.
    /// @param lvl The level whose winners are being paid.
    /// @param ethPool Total ETH to distribute across all buckets.
    /// @param entropy VRF-derived random word for winner selection.
    /// @param traitIds The 4 winning trait IDs for this daily jackpot.
    /// @param shareBps Basis-point share for each of the 4 buckets.
    /// @param bucketCounts Number of holders in each trait bucket.
    /// @return paidEth Total ETH actually paid out (may be less than ethPool if buckets empty).
```

After all edits, run `forge build` to verify the rename is consistent and no syntax errors exist.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5 && echo "---CHUNK_CHECK---" && grep -rn "Chunk\|chunk" contracts/modules/DegenerusGameJackpotModule.sol; echo "grep_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - contracts/modules/DegenerusGameJackpotModule.sol contains "function _processDailyEth(" (NOT "function _processDailyEthChunk(")
    - contracts/modules/DegenerusGameJackpotModule.sol does NOT contain the string "_processDailyEthChunk" anywhere
    - contracts/modules/DegenerusGameJackpotModule.sol does NOT contain "resuming from a prior chunk"
    - contracts/modules/DegenerusGameJackpotModule.sol contains "resuming an interrupted daily jackpot"
    - contracts/modules/DegenerusGameJackpotModule.sol contains "@param lvl The level whose winners are being paid"
    - contracts/modules/DegenerusGameJackpotModule.sol contains "@return paidEth Total ETH actually paid out"
    - `forge build` exits 0
    - `grep -rn "Chunk\|chunk" contracts/modules/DegenerusGameJackpotModule.sol` returns zero matches (exit code 1)
  </acceptance_criteria>
  <done>Function renamed from _processDailyEthChunk to _processDailyEth at all 3 locations. Stale "prior chunk" comment replaced. Full NatSpec with @param and @return added. forge build passes. Zero occurrences of "chunk" or "Chunk" remain in the file.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase requirement:

1. **Compilation:** `forge build` exits 0 (no syntax errors from edits or rename)
2. **No stale chunk references:** `grep -rn "Chunk\|chunk\|dailyEthBucketCursor" contracts/storage/DegenerusGameStorage.sol contracts/modules/DegenerusGameJackpotModule.sol` returns zero matches
3. **Storage layout accuracy:** Compare Slot 0/1 comment offsets in DegenerusGameStorage.sol against `forge inspect DegenerusGame storage` output -- every field, offset, and byte count must match
4. **Rename consistency:** `grep -rn "_processDailyEthChunk" contracts/` returns zero matches; `grep -rn "_processDailyEth(" contracts/` returns exactly 3 matches (definition + 2 call sites)
</verification>

<success_criteria>
CMT-01 satisfied: All NatSpec and inline comments in code modified during chunk removal (Phase 95) and gas optimization (Phase 96) accurately reflect current behavior. No references to removed concepts. Storage layout comments match forge inspect output. Function naming reflects post-chunk reality.
</success_criteria>

<output>
After completion, create `.planning/phases/97-comment-cleanup/97-01-SUMMARY.md`
</output>
