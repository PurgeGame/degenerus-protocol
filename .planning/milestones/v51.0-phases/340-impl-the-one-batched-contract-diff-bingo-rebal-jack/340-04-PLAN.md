---
phase: 340-impl-the-one-batched-contract-diff-bingo-rebal-jack
plan: 04
type: execute
wave: 3
depends_on: ["340-01", "340-02", "340-03"]
files_modified: []
autonomous: false
worktree: false
requirements: [BATCH-02]
tags: [contracts, batch, forge-build, contract-boundary, hard-stop, checkpoint]

must_haves:
  truths:
    - "forge build compiles the full v51.0 batched diff (BINGO module + storage + ContractAddresses + DegenerusGame entrypoint + interfaces + REBAL + JACK) cleanly — exit 0, no errors, no new warnings (no unused-parameter warning from the JACK deletion)."
    - "The whole batched contracts/*.sol diff is presented to the user for explicit hand-review and is NOT committed without approval (BATCH-02 HARD STOP)."
    - "The diff is exactly the four edit-order parts and nothing else: 3 storage mappings, the new BingoModule, GAME_BINGO_MODULE, the DegenerusGame.claimBingo entrypoint + 2 interface sigs, the REBAL 2-constant swap, the JACK branch/constant/event deletion."
    - "The diff adds NO write to traitBurnTicket and does not touch the MintModule writer (freeze-safety, BINGO-06)."
  artifacts:
    - path: ".planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-04-SUMMARY.md"
      provides: "The forge build result + the batched-diff inventory + the hand-review hand-off record."
  key_links:
    - from: "the applied contracts/*.sol working-tree diff"
      to: "forge build"
      via: "compile-only IMPL bar (D-340-03)"
      pattern: "forge build"
---

<objective>
Verify the **IMPL bar (D-340-03): `forge build` clean ONLY** for the full v51.0 batched diff (the producers from 340-01, the consumer wiring from 340-02, and the REBAL+JACK from 340-03), then **HALT at the contract-commit boundary (BATCH-02 HARD STOP)** and present the single batched `contracts/*.sol` diff to the user for explicit hand-review.

NO test runs (D-340-03 scopes 340 to compile-only; all behavior — per-tier rewards, dedup, precedence-suppression, empty-pool no-op, jackpot-final-day regression, and the NON-WIDENING full-suite regression — is owned by Phase 341 TST-01..06). Do NOT widen this plan into a test phase.

Purpose: confirm the four-part diff compiles as one coherent unit and gate the contract commit on user approval.
Output: `forge build` result + the batched-diff inventory + the hand-review hand-off.

**CONTRACT BOUNDARY (BATCH-02 / `autonomous: false`):** the `contracts/*.sol` diff is applied + compiling but is **NEVER committed without explicit user hand-review** (`feedback_pause_at_contract_phase_boundaries` + `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_no_contract_commits` + `feedback_manual_review_before_push`). Planning/docs/summary artifacts are agent-committable; the `contracts/` diff is not.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-CONTEXT.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: forge build the full batched diff (the IMPL bar)</name>
  <files></files>
  <read_first>
    - `340-CONTEXT.md` D-340-03 (the compile-only IMPL bar; tests are Phase 341).
    - The applied working-tree diff from 340-01/02/03 (`git diff --stat -- contracts/`).
  </read_first>
  <action>
    Run `forge build` at the repo root over the full applied diff. It MUST compile cleanly: exit 0, no compiler errors, and no NEW warnings introduced by the diff — in particular NO unused-parameter warning from the JACK `isFinalDay` orphan (Plan 340-03 Task 2 must have elided or dropped that param). If the build fails or emits a new warning attributable to the diff, report the exact compiler output and the offending file:line so the relevant plan (340-01 module/storage, 340-02 wiring/selector, or 340-03 REBAL/JACK) can be corrected — do NOT proceed to the hand-review gate until `forge build` is clean. Do NOT run `forge test` or any suite (D-340-03; that is Phase 341).
  </action>
  <verify>
    <automated>forge build 2>&1 | tee /tmp/340-forge-build.log; grep -qi 'Compiler run successful' /tmp/340-forge-build.log && grep -qivE 'unused parameter|unused function parameter' /tmp/340-forge-build.log && echo BUILD_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 ("Compiler run successful").
    - No `Error` / `Compilation failed` lines in the output.
    - No `unused parameter` / `unused function parameter` warning attributable to the JACK deletion (the `_handleSoloBucketWinner` `isFinalDay` orphan is handled).
    - All five claimBingo signature sites agree (entrypoint, IDegenerusGame, IDegenerusGameBingoModule, the module body, the delegatecall selector encode) — a mismatch would fail to compile.
  </acceptance_criteria>
  <done>forge build compiles the full v51.0 batched diff cleanly with no new warnings; the compile-only IMPL bar (D-340-03) is met.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory the batched diff vs the SPEC edit-order map</name>
  <files></files>
  <read_first>
    - `339-GREP-ATTESTATION-EDIT-ORDER.md` Part 2 (the binding 4-step edit-order — the exact set of intended changes).
    - The applied diff (`git diff -- contracts/`).
  </read_first>
  <action>
    Produce a written inventory in the SUMMARY confirming the applied `contracts/*.sol` diff contains EXACTLY the four edit-order parts and nothing beyond them (no scope creep, no incidental edits): (1) the 3 storage mappings appended to `DegenerusGameStorage.sol` + the new `DegenerusGameBingoModule.sol` + `GAME_BINGO_MODULE` in `ContractAddresses.sol`; (2) the `DegenerusGame.claimBingo` entrypoint + `IDegenerusGameBingoModule` + the `IDegenerusGame.claimBingo` sig; (3) the REBAL 2-constant swap in `StakedDegenerusStonk.sol`; (4) the JACK branch/constant/event deletion in `DegenerusGameJackpotModule.sol`. Run `git diff --stat -- contracts/` and confirm the changed-file set is exactly those eight files (one new) and NO other `contracts/` file. Confirm the diff adds NO write to `traitBurnTicket` (freeze-safety, BINGO-06) and does NOT touch the MintModule writer `:603-643`. Record any deviation for the hand-review note.
  </action>
  <verify>
    <automated>git diff --name-only -- contracts/ | sort | tr '\n' ' '; echo " <-- expect exactly the 8 SPEC files"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only -- contracts/` lists exactly: `ContractAddresses.sol`, `DegenerusGame.sol`, `StakedDegenerusStonk.sol`, `interfaces/IDegenerusGame.sol`, `interfaces/IDegenerusGameModules.sol`, `modules/DegenerusGameBingoModule.sol`, `modules/DegenerusGameJackpotModule.sol`, `storage/DegenerusGameStorage.sol` — NO other `contracts/` file.
    - No added line in the diff writes/pushes `traitBurnTicket` (only `==` ownership comparisons appear); the MintModule writer `:603-643` is unchanged.
    - The inventory maps each changed file to its edit-order step (1/2/3/4) with no orphan changes.
  </acceptance_criteria>
  <done>The applied diff is exactly the four SPEC edit-order parts across the eight expected files, adds no traitBurnTicket write, and is documented for hand-review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: BATCH-02 contract-commit HARD STOP — user hand-review of the single batched diff</name>
  <action>
    PRESENT the full applied `contracts/*.sol` diff (`git diff -- contracts/`) to the user and HALT for explicit hand-review per BATCH-02. Do NOT commit any `contracts/` file. Summarize the four edit-order parts (BINGO producers, consumer wiring, REBAL, JACK) and surface any Task-2 deviation. Wait for the user's "approved" signal (or change requests) before any `contracts/` commit is recorded. This is a blocking, never-auto-approvable gate (`feedback_no_contract_commits` + `feedback_never_preapprove_contracts` + `feedback_pause_at_contract_phase_boundaries`).
  </action>
  <what-built>
    The single batched v51.0 `contracts/*.sol` diff is applied to the working tree and `forge build` is clean (Task 1), and the diff is inventoried as exactly the four SPEC edit-order parts across eight files (Task 2): the new `DegenerusGameBingoModule.sol` 3-tier color-completion `claimBingo` + the 3 storage bitfields + `GAME_BINGO_MODULE` + the `DegenerusGame.claimBingo` delegatecall entrypoint + the two interface signatures + the `StakedDegenerusStonk` REBAL (AFFILIATE 3500→3000 / REWARD 500→1000, Pool.Reward 50B→100B) + the `JackpotModule` final-day `Pool.Reward` deletion (branch + `FINAL_DAY_DGNRS_BPS` + `JackpotDgnrsWin`).
  </what-built>
  <how-to-verify>
    1. Review the full `contracts/` diff: `git diff -- contracts/`.
    2. Confirm the BINGO module matches the 339 acceptance contract (the quadrant-first-before-symbol-first cascade, BOTH-bits suppression, CEI ordering, the six verbatim constants, the clamped-return `dgnrsPaid`, the empty-pool no-op, the player-indexed events, the custom-error bad-slot guard, NO `traitBurnTicket` write).
    3. Confirm the REBAL is exactly the two constant swaps and the BPS set still sums to 10000.
    4. Confirm the JACK deletion removed only the final-day `Pool.Reward` branch + its orphans, preserving the `lvl+1` gate and the whale-pass branch, with no orphan-param warning.
    5. Confirm `forge build` is clean on your end if desired (`forge build`).
    6. Approve to commit the `contracts/` diff, or describe required changes.
  </how-to-verify>
  <verify>
    <automated>git diff --stat -- contracts/ | tail -1</automated>
  </verify>
  <resume-signal>Type "approved" to authorize the contracts/ commit, or list the changes needed. The contracts/ diff is NOT committed until you approve (per feedback_no_contract_commits + feedback_never_preapprove_contracts).</resume-signal>
  <done>The user has reviewed the full batched contracts/ diff and either approved the commit or returned change requests; no contracts/ file was committed prior to approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent working tree → git history (contracts/) | the contract commit crosses an explicit human-approval gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-15 | Tampering (unreviewed contract change) | the batched contracts/ diff | mitigate | BATCH-02 HARD STOP — `autonomous: false`; the `contracts/` diff is never committed without the Task-3 blocking human hand-review (USER-locked across v44–v50; never auto-approvable regardless of `workflow.auto_advance`) |
| T-340-16 | Tampering (scope creep) | diff beyond the 4 parts | mitigate | Task 2 inventory confirms the changed-file set is exactly the 8 SPEC files + no incidental `contracts/` edit + no `traitBurnTicket` write |
| T-340-SC | Tampering | npm/pip/cargo installs | accept (N/A) | no package-manager installs in this phase; Foundry/forge is the established project toolchain — no new dependency surface, no Package Legitimacy Gate trigger |

This phase introduces NO new package-manager dependencies; the `T-340-SC` row is recorded N/A. Internal 3-skill adversarial sweep DEFERRED → v52 (minimal close).
</threat_model>

<verification>
- IMPL bar (D-340-03): `forge build` clean — Task 1.
- Diff scope: exactly the 4 SPEC edit-order parts across 8 files, no `traitBurnTicket` write — Task 2.
- BATCH-02 HARD STOP: blocking human hand-review before any `contracts/` commit — Task 3.
- NO suite runs (Phase 341 TST owns all behavior + the NON-WIDENING regression).
</verification>

<success_criteria>
- BATCH-02: the single batched `contracts/*.sol` diff is applied + `forge build`-clean + authored in producer-before-consumer order + inventoried as the exact 4 SPEC parts, and is HELD at the contract-commit boundary for explicit user hand-review (never auto-committed).
</success_criteria>

<output>
Create `.planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-04-SUMMARY.md` when done (forge build result + the batched-diff inventory + the hand-review hand-off; do NOT record the contracts/ commit until the user approves).
</output>
