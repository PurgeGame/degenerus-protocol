---
phase: 340-impl-the-one-batched-contract-diff-bingo-rebal-jack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/StakedDegenerusStonk.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
autonomous: false
worktree: false
requirements: [REBAL-01, JACK-01, JACK-02]
tags: [contracts, rebal, jack, deletion, contract-boundary]

must_haves:
  truths:
    - "StakedDegenerusStonk.sol constructor constant AFFILIATE_POOL_BPS is 3000 (was 3500) at :295 and REWARD_POOL_BPS is 1000 (was 500) at :297; the complete pool-BPS set {CREATOR 2000, WHALE 1000, AFFILIATE 3000, LOOTBOX 2000, REWARD 1000, PRESALE_BOX 1000} sums to 10000; no other pool/constant is perturbed; INITIAL_SUPPLY and BPS_DENOM are unchanged."
    - "The isFinalDay Pool.Reward branch (:1339-1352) inside _handleSoloBucketWinner in DegenerusGameJackpotModule.sol is deleted in full."
    - "The FINAL_DAY_DGNRS_BPS = 100 constant (:191) is deleted (its sole use was :1343, inside the deleted branch)."
    - "The JackpotDgnrsWin event declaration (:112) is deleted (its sole emit was :1350, inside the deleted branch)."
    - "After deletion, _handleSoloBucketWinner's isFinalDay parameter no longer triggers an unused-parameter compiler warning (param name elided, or param dropped with the :1190 call site + the _processBucket :1135/:1161 chain updated consistently)."
    - "The rest of the isFinalDay plumbing is PRESERVED untouched: the lvl+1 ticket-index gate (:617), the compute (:614), and the whale-pass-on-final-day branch (:1335-1338); the callers/docs at :1085/1095 remain coherent."
  artifacts:
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "The rebalanced Pool.Reward (50B→100B) funding the claimBingo distribution surface; BPS-sum invariant intact."
      contains: "REWARD_POOL_BPS = 1000"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "The jackpot final-day Pool.Reward one-shot removed; Pool.Reward now flows through claimBingo only; no dangling refs."
      contains: "_handleSoloBucketWinner"
  key_links:
    - from: "contracts/StakedDegenerusStonk.sol::constructor"
      to: "Pool.Reward allocation"
      via: "(INITIAL_SUPPLY * REWARD_POOL_BPS) / BPS_DENOM doubles 50B→100B at the rewardAmount derivation"
      pattern: "REWARD_POOL_BPS = 1000"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol::_handleSoloBucketWinner"
      to: "(removed) Pool.Reward final-day draw"
      via: "the :1339-1352 isFinalDay branch is deleted; whale-pass branch :1335-1338 preserved"
      pattern: "JackpotDgnrsWin"
---

<objective>
Land **Steps 3 + 4 of the BATCH-02 producer-before-consumer edit-order** (the two ISOLATED co-requisites): the `StakedDegenerusStonk` constructor REBAL (doubling `Pool.Reward` 50B→100B) and the `DegenerusGameJackpotModule` JACK deletion (removing the final-day `Pool.Reward` one-shot). These touch different files and share NO symbol with the BINGO producers/consumers → order-independent relative to BINGO (Wave 1, no `files_modified` overlap with Plan 340-01).

Faithful transcription of the LOCKED 339 REBAL-JACK attestation — the BPS-sum invariant + the clean-orphan disposition are PROVEN there. Re-verify each cited line against live source (`feedback_verify_call_graph_against_source`); do not change the design.

Purpose: REBAL funds the continuous `claimBingo` distribution; JACK removes the one-shot it replaces.
Output: 2 constant edits in `StakedDegenerusStonk.sol`; the branch + constant + event deletion in `DegenerusGameJackpotModule.sol`.

**CONTRACT BOUNDARY (BATCH-02 / `autonomous: false`):** edits `contracts/*.sol`; part of the single batched diff HELD for user hand-review at Plan 340-04. Never auto-committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-CONTEXT.md
@.planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-PATTERNS.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-REBAL-JACK-ATTESTATION.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REBAL — swap AFFILIATE_POOL_BPS 3500→3000 and REWARD_POOL_BPS 500→1000</name>
  <files>contracts/StakedDegenerusStonk.sol</files>
  <read_first>
    - `contracts/StakedDegenerusStonk.sol:285-298` (the live constant block: `INITIAL_SUPPLY :285`, `BPS_DENOM :288`, `CREATOR_BPS=2000 :291`, `WHALE_POOL_BPS=1000 :294`, `AFFILIATE_POOL_BPS=3500 :295`, `LOOTBOX_POOL_BPS=2000 :296`, `REWARD_POOL_BPS=500 :297`, `PRESALE_BOX_POOL_BPS=1000 :298`) and `:354-359` (the `(INITIAL_SUPPLY * X_POOL_BPS)/BPS_DENOM` derivations grounding the supply-unchanged claim).
    - `339-REBAL-JACK-ATTESTATION.md` Part 1 (the COMPLETE set sums to 10000 before AND after; net-zero; only :295/:297 change; CREATOR_BPS=2000@:291 is the completeness term).
    - `340-PATTERNS.md` §5 (the exact two single-token edits; the explicit "DO NOT touch CREATOR_BPS@:291" warning).
  </read_first>
  <action>
    Make EXACTLY two single-token edits in the constructor constant block: `AFFILIATE_POOL_BPS` `3500` → `3000` (at `:295`) and `REWARD_POOL_BPS` `500` → `1000` (at `:297`). Do NOT touch `CREATOR_BPS=2000` (`:291` — it is the completeness term in the BPS-sum, one line above the visible block; the doc/CONTEXT mis-framed `:294-298` as the full set), nor `WHALE_POOL_BPS`, `LOOTBOX_POOL_BPS`, `PRESALE_BOX_POOL_BPS`, `INITIAL_SUPPLY`, or `BPS_DENOM`. The swap is net-zero (+500 reward / −500 affiliate); the complete set still sums to 10000; total sDGNRS supply unchanged; `Pool.Reward` doubles 50B→100B; affiliate pool takes a ~14% haircut (350B→300B). Constructor-only constant change → viable only pre-deploy (redeploy-fresh, no migration).
  </action>
  <verify>
    <automated>grep -q 'AFFILIATE_POOL_BPS = 3000' contracts/StakedDegenerusStonk.sol && grep -q 'REWARD_POOL_BPS = 1000' contracts/StakedDegenerusStonk.sol && ! grep -q 'AFFILIATE_POOL_BPS = 3500' contracts/StakedDegenerusStonk.sol && ! grep -q 'REWARD_POOL_BPS = 500;' contracts/StakedDegenerusStonk.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AFFILIATE_POOL_BPS = 3000' contracts/StakedDegenerusStonk.sol` == 1 AND `grep -c 'AFFILIATE_POOL_BPS = 3500' contracts/StakedDegenerusStonk.sol` == 0.
    - `grep -c 'REWARD_POOL_BPS = 1000' contracts/StakedDegenerusStonk.sol` == 1 AND `grep -c 'REWARD_POOL_BPS = 500;' contracts/StakedDegenerusStonk.sol` == 0.
    - `CREATOR_BPS = 2000`, `WHALE_POOL_BPS = 1000`, `LOOTBOX_POOL_BPS = 2000`, `PRESALE_BOX_POOL_BPS = 1000`, `INITIAL_SUPPLY`, and `BPS_DENOM = 10_000` are all byte-unchanged.
    - BPS-sum check: 2000 + 1000 + 3000 + 2000 + 1000 + 1000 == 10000.
  </acceptance_criteria>
  <done>The two REBAL constants are swapped (3500→3000, 500→1000); the complete BPS set sums to 10000; no other constant is perturbed.</done>
</task>

<task type="auto">
  <name>Task 2: JACK — delete the final-day Pool.Reward branch + the orphaned constant + event, preserving the rest of isFinalDay</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol</files>
  <read_first>
    - `contracts/modules/DegenerusGameJackpotModule.sol:1305-1353` (the live `_handleSoloBucketWinner` — the `:1339-1352` `isFinalDay` Pool.Reward branch to delete; note the body otherwise uses `w`/`lvl`/`traitId`/`ticketIndex`/`perWinner`/`entropy`, and the whale-pass branch `:1335-1338` to PRESERVE; the `isFinalDay` param at `:1312` is consumed ONLY by the deleted branch → orphan-param after deletion).
    - `contracts/modules/DegenerusGameJackpotModule.sol:112` (the `JackpotDgnrsWin` event decl, sole emit `:1350`) and `:191` (`FINAL_DAY_DGNRS_BPS = 100`, sole use `:1343`).
    - `contracts/modules/DegenerusGameJackpotModule.sol:614/617/1085/1095/1135/1161/1190` (the PRESERVED plumbing — compute, the `lvl + 1` ticket-index gate, the doc, the `_processDailyEth`/`_processBucket` params + the `_handleSoloBucketWinner` call site).
    - `339-REBAL-JACK-ATTESTATION.md` Part 2 (the clean-orphan grep proofs + the preserved-plumbing enumeration + the `_handleSoloBucketWinner` name correction).
    - `340-PATTERNS.md` §6 (the exact branch/constant/event to delete + the orphan-param compiler-warning warning + the PRESERVE list).
  </read_first>
  <action>
    Delete the whole `if (isFinalDay) { ... }` branch at `:1339-1352` inside `_handleSoloBucketWinner` (the `poolBalance(Pool.Reward)` read → `(dgnrsPool * FINAL_DAY_DGNRS_BPS)/10_000` → the `reward != 0` guard → `transferFromPool(Pool.Reward, w, reward)` → `emit JackpotDgnrsWin(w, reward)`). Delete the now-orphaned `FINAL_DAY_DGNRS_BPS = 100` constant (`:191`) and the `JackpotDgnrsWin` event declaration (`:112`) — both are cleanly orphaned (sole use/emit was inside the deleted branch, PROVEN at 339).

    Handle the orphan parameter (PATTERNS drift #3 / critical constraint #3): after deleting the branch, `_handleSoloBucketWinner`'s `isFinalDay` param (`:1312`) is no longer read inside the function → would raise an unused-parameter compiler warning. To keep `forge build` clean, ELIDE the parameter NAME (keep the `bool` positional type unnamed) so callers `:1190` are unaffected and the `_processBucket`/`_processDailyEth` upstream chain that still uses `isFinalDay` for the `lvl + 1` gate stays intact — OR, if cleaner, drop the param entirely and update the `:1190` call site plus the `_processBucket` (`:1161` param / `:1135` pass) signatures consistently. Author's choice; the bar is no unused-param warning AND no broken upstream `isFinalDay` use.

    PRESERVE (do NOT touch): the `isFinalDay` compute (`:614`), the `lvl + 1` ticket-index gate (`:617`), the `_processDailyEth`/`_processBucket` doc + params + pass-through (`:1085/1095/1135/1161`), and the whale-pass-on-final-day branch (`:1335-1338`, `emit JackpotWhalePassWin`). Do NOT touch the read-side `traitBurnTicket[lvl]` consumer (`:654`). Only the `Pool.Reward` draw is removed.
  </action>
  <verify>
    <automated>grep -c 'JackpotDgnrsWin' contracts/modules/DegenerusGameJackpotModule.sol | grep -qx 0 && grep -c 'FINAL_DAY_DGNRS_BPS' contracts/modules/DegenerusGameJackpotModule.sol | grep -qx 0 && grep -q 'isFinalDay ? lvl + 1 : lvl' contracts/modules/DegenerusGameJackpotModule.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'JackpotDgnrsWin' contracts/modules/DegenerusGameJackpotModule.sol` == 0 (both decl :112 and emit :1350 gone).
    - `grep -c 'FINAL_DAY_DGNRS_BPS' contracts/modules/DegenerusGameJackpotModule.sol` == 0 (both decl :191 and use :1343 gone).
    - No `transferFromPool(... Pool.Reward ...)` call remains inside `_handleSoloBucketWinner` (the deleted branch's draw is gone).
    - PRESERVED: `grep -c 'isFinalDay ? lvl + 1 : lvl' contracts/modules/DegenerusGameJackpotModule.sol` == 1 (the ticket-index gate :617) AND `grep -c 'JackpotWhalePassWin' contracts/modules/DegenerusGameJackpotModule.sol` >= 1 (the whale-pass branch survives).
    - `_handleSoloBucketWinner` still exists (`grep -c '_handleSoloBucketWinner' contracts/modules/DegenerusGameJackpotModule.sol` >= 2 — def + call site).
    - No unused-parameter warning is introduced (verified at the Plan 340-04 `forge build` gate — Solidity treats unused named params as a warning; the param name is elided or the param dropped).
  </acceptance_criteria>
  <done>The final-day Pool.Reward branch + FINAL_DAY_DGNRS_BPS + JackpotDgnrsWin are deleted with zero dangling refs and no orphan-param warning; the lvl+1 gate, the whale-pass branch, and all other isFinalDay plumbing are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constructor → pool allocation (REBAL) | one-time mint split; no runtime input |
| jackpot resolution → solo-bucket winner payout (JACK) | internal call chain; removing one payout branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-11 | Tampering (supply drift) | REBAL BPS sum | mitigate | the COMPLETE set {CREATOR 2000, WHALE 1000, AFFILIATE 3000, LOOTBOX 2000, REWARD 1000, PRESALE_BOX 1000} sums to 10000 before AND after; only :295/:297 change; INITIAL_SUPPLY + BPS_DENOM untouched → total sDGNRS supply unchanged (PROVEN at 339-REBAL-JACK §1) |
| T-340-12 | Denial (lost reward path) | JACK deletion | accept (by design) | the removed final-day one-shot is intentionally replaced by the continuous `claimBingo` Pool.Reward distribution; Pool.Reward (now 100B) is drawn by claimBingo + the existing Degenerette path, never by the deleted jackpot one-shot |
| T-340-13 | Tampering (dangling ref / broken plumbing) | JACK orphans + isFinalDay chain | mitigate | FINAL_DAY_DGNRS_BPS + JackpotDgnrsWin are cleanly orphaned (sole use/emit inside the deleted branch, grep-PROVEN at 339); the lvl+1 gate :617 + whale-pass branch :1335-1338 + caller chain are preserved; orphan-param warning silenced; `forge build` (Plan 340-04) confirms no dangling reference |
| T-340-14 | Repudiation (affiliate haircut) | REBAL affiliate −14% | accept (by design) | affiliate pool 350B→300B is the intended co-requisite of doubling Pool.Reward; pre-launch (no live affiliate balances); USER-locked economics |

Internal 3-skill adversarial sweep over the REBAL/JACK side-effects DEFERRED → v52 (minimal close); this register is the in-phase security record.
</threat_model>

<verification>
- `forge build` clean is verified at Plan 340-04 (the JACK deletion's no-dangling-ref + no-orphan-param-warning is a compile-time check; the REBAL is a pure constant swap). No test runs (D-340-03; the jackpot-final-day regression is Phase 341 TST-05).
</verification>

<success_criteria>
- REBAL-01: `AFFILIATE_POOL_BPS=3000` + `REWARD_POOL_BPS=1000`; complete BPS set sums to 10000; supply unchanged; Pool.Reward 50B→100B.
- JACK-01: the `:1339-1352` final-day `Pool.Reward` branch + `FINAL_DAY_DGNRS_BPS` (:191) + `JackpotDgnrsWin` (:112) are deleted.
- JACK-02: the lvl+1 gate (:617) + the whale-pass branch + the caller chain are preserved; no non-Pool.Reward final-day behavior broken; no orphan-param warning.
</success_criteria>

<output>
Create `.planning/phases/340-impl-the-one-batched-contract-diff-bingo-rebal-jack/340-03-SUMMARY.md` when done.
</output>
