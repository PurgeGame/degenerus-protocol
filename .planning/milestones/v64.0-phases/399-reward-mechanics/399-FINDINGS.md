# Phase 399 — REWARD-MECHANICS: dual-net findings (v64.0)

**Status:** ✅ COMPLETE — both nets on record, adjudicated vs frozen source.
**Requirements:** RWD-01..06 · **Subject frozen** `402855e1` (contracts tree at HEAD `7d395e49`; working tree == frozen, verified clean).
**Design anchor:** `.planning/PAPER-REWARD-CHANGES-BRIEF.md` (verified, not re-litigated) + `.planning/LOOTBOX-DEGENERETTE-SPINS-PLAN.md`.
**Nets:**
- **NET-1 cross-model council** — `gemini` (CLEAN, 0 divergence) + `codex` (2 EV-accounting divergences). Both on record (`council/rwd.{gemini,codex}.txt`; gemini retried once after a first empty-output skip).
- **NET-2 Claude Workflow** — 6 dimension finders + per-lead adversarial-refute + completeness critic (run `w0ra36e4q`, 12 agents): CLEAN + 1 INFO doc-drift.
- **NET-2b Claude Workflow** (corroborating, `w9ssdpm9q`, 8/9 agents — the bonus cross-composition critic did not flush): all 6 dimensions on record — RWD-01/03/05/06 = 0 leads; RWD-04 money-pump REFUTED; **RWD-02 independently flagged the same "EV-neutral vs sub-100% Degenerette house edge" → INFO/by-design (convergent with codex)**. Like gemini and NET-2, it did **not** independently re-derive the seed-emission EV — RWD-A stayed a codex-unique catch.

## Outcome: 0 CATASTROPHE / 0 HIGH / 0 MED / 0 LOW in the reward-mechanics class. 1 INFO (RWD-A, USER-confirmed by-design). 2 BY-DESIGN. 1 INFO doc-drift cluster.

| # | Site | Disposition | Detail |
|---|------|-------------|--------|
| RWD-A | BURNIE seed-emission EV magnitude · `BurnieCoinflip.sol:154-157,544,864-886` | ✅ **INFO — USER-CONFIRMED BY-DESIGN 2026-06-15 (emission magnitude intended; no contract change)** | **codex-only (the valuable cross-model catch — gemini, NET-2, and NET-2b all attested "conservation" / accepted the brief's "~4M EV" without recomputing the win-payout EV).** Seed = `SEED_FLIP_DAILY 200_000 ether × SEED_FLIP_DAYS 20` to **both** VAULT and sDGNRS = **8M staked**. A winning daily flip pays `stake + stake·rewardPercent/100` (`:544`) with rewardPercent averaging **96.85%** (5%×50 + 5%×150 + 90%×~96.5, `:864-883`) → win pays **~1.9685× stake**, and win prob is exactly 50/50 (`(rngWord&1)==1`, `:886`). So **EV ≈ 0.5×1.9685 ≈ 0.984× per staked BURNIE → ~7.87M expected emission, NOT ~4M.** The "~4M EV" in the v63 prior + the 399 council prompt is **wrong arithmetic** (it conflated the 50/50 win *probability* with a 0.5× EV *factor*, ignoring the ~2× win payout). **NOT a contract defect:** the `:879-880` comment states the coinflip is deliberately near-fair ("~99.9% / ~101.9% RTP after recycle compounds"), and the 8M seed sizing is explicit — a near-fair flip on 8M stake ⇒ ~8M emission is the *intended* consequence. Emission is bounded (≤ stake × 2.56× max on a 156%-bonus win), survive-before-mint holds (`BurnieCoin.sol` no premint; mint gated on win), and it accrues to protocol-owned backing entities (VAULT, sDGNRS) — **no ETH-solvency, no freeze, no player harm, no unbacked mint.** **USER-CONFIRMED 2026-06-15: the ~7.87M expected emission magnitude (8M near-fair-coinflip stake, ~2× the old 4M lumps) is intended — closed INFO, no contract change.** Corrects the v63 "emission conservation ≈ 4M" attestation, which was attested on the same slipped arithmetic. |
| RWD-B | Lootbox spins not strictly *realized*-EV-neutral · `DegeneretteModule.sol:1144-1193,1071-1102` | **BY-DESIGN (doc-precision note)** | **codex + NET-2b convergent.** A box value-roll converted to a Degenerette spin realizes the standard Degenerette ROI, not 100%: `_roiBpsFromScore` scales **90%→99.9%** with activity score (`:1144`, always < 100%) over an EV-fair base table ("basePayoutEV = exactly 100 centi-x", `:1061`) → realized spin EV = **90–99.9% of the staked value** (WWXRP high-value ROI tops at **109.9%** at max score, `:1181`; BURNIE spins ride the 90–99.9% curve). So the conversion is score-dependent and marginally sub-100% for typical players — the standard Degenerette house edge. **By-design:** the brief §2b explicitly defines these as "a real Degenerette spin **scored by the player's activity score**, just sourced from the box." "EV-neutral redistribution" denotes the **conserved stake/split** (the 15%/10%/5% shares are staked, not added/removed), not a 100% realized payout. The edge is reduced emission / retained backing, not a leak. The "EV-neutral" headline in the brief is imprecise re: realized value; flagged for paper-precision, **no contract change.** |
| RWD-C | Stale EV-range comments · `DegenerusGameLootboxModule.sol:472,563` + `DegenerusGame.sol:2210-2212` | **INFO doc-drift (deferrable)** | **NET-2 (convergent re-observed by codex).** `@param`/inline comments quote the OLD range "8000-13500" / "80% to 135%" / "100% (quest)"; live constants are 9000–14500 (90%–145%, `DegenerusGameStorage.sol:1545-1549`) and the quest component is uncapped-by-design (brief §1, ceiling threshold raised 25,500→40,000 to compensate). **Inert text** — `_applyEvMultiplierWithCap` reads `evMultiplierBps` only via the `≤ LOOTBOX_EV_NEUTRAL_BPS` branch-guard and the `·/10_000` scaling, never the documented literals; the function `@return` docstring (`Storage:1620`) and the constants are already correct. No EV/freeze/emission effect. Deferrable to a non-frozen comment-cleanup pass. |
| RWD-D | Redemption auto-rebuy-carry parity comment · `StakedDegenerusStonk.sol:875-876` | **CORRECT-BY-DESIGN (NET-2)** | Comment claims byte-identical parity with "every holder's" flip payout; literally true for the natural comparison class (a non-rebuying holder earns exactly principal+reward%). Only an auto-rebuy **carry** slice additionally earns `_recyclingBonus` (≤0.75% / ≤1000 BURNIE) the escrow path omits → a redeemer is paid **≤**, never more (conservative under-emission). No leak, no positive-EV loop, freeze-safe (ABSOLUTE day+1 read). |
| RWD-E | BURNIE-spin `totalStake/3` integer-split dust · `DegeneretteModule.sol:~1355` | **INFO (negligible)** | **codex.** The 3-way BURNIE-spin split drops ≤2 wei of BURNIE per spin to integer flooring. Sub-wei dust, no economic effect. |

## Verification map — every load-bearing claim CONFIRMED by ≥2 nets

- **EV multiplier:** floor 90% @ score 0 (`LOOTBOX_EV_MIN_BPS=9_000`), neutral 100% @ 6,000, ceiling 145% @ ≥40,000 (`=14_500`), two-segment linear (`DegenerusGameStorage.sol:1545-1549,1621-1642`). ✅ all 3 nets.
- **Reward split:** `40/15/15/15/10/5` over `roll%20` = `8/3/3/3/2/1` (sums to 20/20 = 100%, no gap/overlap) (`DegenerusGameLootboxModule.sol:1938-2050`). ✅ all 3 nets.
- **Ticket budget ×11/9:** `LOOTBOX_TICKET_ROLL_BPS=19_678` (`=round(16_100·11/9)`, +0.00113% rounding only); `0.55·1.61 = 0.45·1.9678 ≈ 0.8855` preserves aggregate ticket value. ✅
- **Far/near:** 20%/80% (`uint16(seed)%100 < 20`); far 1.5× (`15_000`) / near 0.875× (`8_750`); `0.2·1.5 + 0.8·0.875 = 1.000` EV-neutral. ✅
- **Variance tiers:** 1/4/20/45/30% (`100/400/2000/4500/3000` bps), symmetric ranges centred on the old fixed values, Σ(chance·midpoint) = 0.78590–0.78595× (integer-floor drift < 0.01%). ✅
- **Degenerette spins one-shot + freeze-safe:** seeds derive purely from the frozen `rngWord` (hash1/hash2-tagged, no live state); resolvers gated `address(this) != GAME ⇒ revert` (delegatecall-only, one-shot); ETH-spin recirc depth-1 via the sole `allowEthSpin=false` (`Lootbox:889/906`), recirc awards tickets not ETH. ✅ all 3 nets.
- **Recycle bonus:** 10% BURNIE flip-credit, new gate = `totalClaimableUsed ≥ priceWei·3` (drain-detection removed, `MintModule:1823`); bonus paid only as illiquid coinflip stake. ✅
- **Money pump:** no closed positive-EV ETH loop — recycle/affiliate pay illiquid BURNIE (survives a 50/50 flip ⇒ ~0.5× realizable, peg-not-market); claimable spent first; recirc bounded; the 10-ETH/(player,level) EV cap holds; recycled ETH feeds the pool the buyer competes for. ✅ all 3 nets.
- **BURNIE conservation/safety:** survive-before-mint (mint only after win gate); 2M+2M lumps removed; no unbacked/double mint; day-20 sDGNRS rebuy latch monotonic (`sdgnrsAutoRebuyArmed` only assigned `=true`, VAULT never armed); BURNIE-04 carry-escrow no double-count. ✅ (magnitude caveat = RWD-A).
- **Quest streak:** halved 0.5%/completion + uncapped component (compensated by the 40,000 ceiling); afking/manual primary mutually exclusive (no same-day double primary accrual, `DegenerusQuests.sol:1760`); ≤3 increments/day; activity score hard-capped 65,534 bps; every consumer saturates the score (Lootbox 14,500 @ ≥40k; Degenerette `ACTIVITY_SCORE_MAX_BPS=30,500`; Decimator 23,500) ⇒ uncapping only *raises* the score to reach the EV ceiling. ✅ all 3 nets.

## Verdict

The reward overhaul **matches its documented design and conserves value**, with the codex-caught caveat that the BURNIE seed emission is a near-fair coinflip (~7.87M expected emission), not the ~4M the audit priors believed — a **by-design** magnitude (USER-confirmed intended 2026-06-15), bounded and survive-before-mint — closed INFO (RWD-A). The lootbox spins carry the standard activity-score-weighted Degenerette house edge (90–99.9%), which is the documented "real Degenerette spin" behavior, not a leak (RWD-B). No money-pump, no RNG-freeze violation, no unbacked/double mint, no quest double-channel. Residual = three stale EV-range comments + sub-wei dust (INFO, deferrable). **Both nets on record; RWD-01..06 attested.** The cross-model council earned its keep here: codex independently re-derived the seed-emission EV that gemini, NET-2, and the prior priors all glossed.
