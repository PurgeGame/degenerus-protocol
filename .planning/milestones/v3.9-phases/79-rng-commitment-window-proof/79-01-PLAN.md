---
phase: 79-rng-commitment-window-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.9-rng-commitment-window-proof.md
autonomous: true
requirements: [RNG-01]

must_haves:
  truths:
    - "All permissionless mutation paths that can modify the FF key population between VRF request and fulfillment are enumerated and each receives a SAFE verdict"
    - "The combined pool length (readLen + ffLen) used by _awardFarFutureCoinJackpot cannot change between VRF request and winner index selection"
    - "The proof follows the v3.8 backward-trace methodology (outcome -> inputs -> mutation paths -> verdicts)"
  artifacts:
    - path: "audit/v3.9-rng-commitment-window-proof.md"
      provides: "RNG commitment window safety proof for far-future coin jackpot"
      contains: "## Backward Trace"
      min_lines: 150
  key_links:
    - from: "audit/v3.9-rng-commitment-window-proof.md"
      to: "audit/v3.8-commitment-window-inventory.md"
      via: "Cross-reference: extends CW-03 Category 3 (Jackpot BURNIE Winner) with FF key data source"
      pattern: "v3\\.8.*commitment.*window"
    - from: "audit/v3.9-rng-commitment-window-proof.md"
      to: "DegenerusGameJackpotModule.sol"
      via: "Source line evidence for every mutation path verdict"
      pattern: "JM:\\d+"
    - from: "audit/v3.9-rng-commitment-window-proof.md"
      to: "DegenerusGameStorage.sol"
      via: "Source line evidence for rngLockedFlag guard"
      pattern: "GS:\\d+"
---

<objective>
Produce a formal proof document demonstrating that no permissionless action during the VRF commitment window can influence which player wins a far-future coin jackpot draw. The proof applies the v3.8 backward-trace methodology to the new `_awardFarFutureCoinJackpot` combined pool logic introduced in Phases 74-77.

Purpose: RNG-01 requires exhaustive enumeration of all mutation paths and SAFE verdicts before the v3.9 milestone can ship. This is the security proof a C4A warden would need to verify that the far-future ticket fix does not introduce a commitment window vulnerability.

Output: `audit/v3.9-rng-commitment-window-proof.md` -- a standalone proof document with backward trace, input inventory, mutation surface, per-path verdicts, and combined pool length invariant proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-rng-commitment-window-proof/79-RESEARCH.md
@.planning/phases/77-jackpot-combined-pool-tq-01-fix/77-01-SUMMARY.md
@.planning/phases/78-edge-case-handling/78-01-SUMMARY.md

<interfaces>
<!-- The executor MUST read these source files to extract exact line numbers for the proof.
     The RESEARCH.md provides the analysis framework; the source files provide ground truth. -->

From DegenerusGameJackpotModule.sol (lines 2522-2614):
  - _awardFarFutureCoinJackpot: combined pool winner selection
  - Two callers: payDailyJackpotCoinAndTickets (JM:707), payDailyCoinJackpot (JM:2370)

From DegenerusGameStorage.sol (lines 537-672):
  - _queueTickets: rngLockedFlag guard at line 545
  - _queueTicketsScaled: rngLockedFlag guard at line 580 (approx)
  - _queueTicketRange: rngLockedFlag guard at line 642 (approx)
  - _tqWriteKey (line 716), _tqReadKey (line 721), _tqFarFutureKey (line 729)

From DegenerusGameAdvanceModule.sol:
  - _finalizeRngRequest (line 1328): sets rngLockedFlag
  - _unlockRng (line 1414): clears rngLockedFlag
  - requestLootboxRng (line 678): blocked by rngLockedFlag
  - _processPhaseTransition (lines 1222-1236): vault/sDGNRS perpetual tickets

From audit/v3.8-commitment-window-inventory.md:
  - CW-01 Forward Trace, CW-02 Backward Trace, CW-03 Mutation Surface
  - Category 3: Jackpot BURNIE Winner (the category this proof extends)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RNG commitment window proof document</name>
  <files>audit/v3.9-rng-commitment-window-proof.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 2522-2614 for _awardFarFutureCoinJackpot, lines 700-710 for payDailyJackpotCoinAndTickets caller, lines 2360-2410 for payDailyCoinJackpot caller)
    - contracts/modules/DegenerusGameStorage.sol (lines 537-672 for _queueTickets/_queueTicketsScaled/_queueTicketRange with rngLockedFlag guards, lines 714-731 for key encoding functions)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 1308-1340 for _finalizeRngRequest setting rngLockedFlag, lines 1410-1420 for _unlockRng clearing it, lines 670-744 for requestLootboxRng blocked by rngLockedFlag, lines 1222-1236 for _processPhaseTransition)
    - audit/v3.8-commitment-window-inventory.md (full document -- the template and prior art this proof extends)
    - .planning/phases/79-rng-commitment-window-proof/79-RESEARCH.md (backward trace, mutation paths, protection layers, pitfalls)
  </read_first>
  <action>
Create `audit/v3.9-rng-commitment-window-proof.md` with the following structure and content. Every claim MUST include exact source line references verified from the read_first files. Do NOT copy line numbers from RESEARCH.md without verifying them against the actual source -- line numbers may have shifted during Phases 74-77 code changes.

**Document structure (7 mandatory sections):**

**1. Header + Scope Statement**
- Title: "v3.9 RNG Commitment Window Proof: Far-Future Coin Jackpot"
- Date, Milestone (v3.9), Phase (79), Mode (Audit-only, no code changes)
- Scope: Prove RNG-01 -- no permissionless action during VRF commitment window can influence far-future coin jackpot winner selection
- Explicitly state this extends v3.8 Category 3 (Jackpot BURNIE Winner) with the new FF key data source introduced in Phases 74-77
- State that `_awardFarFutureCoinJackpot` is ONLY reachable via the daily advanceGame path where rngLockedFlag IS true (cite the two callers: payDailyJackpotCoinAndTickets at JM:707 and payDailyCoinJackpot at JM:2370)
- Explicitly address Pitfall 1 (mid-day lootbox path does NOT call this function)

**2. Backward Trace**
Trace backward from winner selection to ALL inputs. Reproduce the dependency chain from RESEARCH.md but with VERIFIED line numbers from the actual source files:
```
Winner address
  <- readQueue[idx] OR ffQueue[idx - readLen]
    <- idx = (entropy >> 32) % combinedLen
      <- entropy = entropyStep(prevEntropy ^ s)
        <- rngWord ^ (lvl << 192) ^ FAR_FUTURE_COIN_TAG
      <- combinedLen = readLen + ffLen
        <- readLen = ticketQueue[_tqReadKey(candidate)].length
        <- ffLen = ticketQueue[_tqFarFutureKey(candidate)].length
    <- candidate = lvl + 5 + (entropy % 95)
```
Every node must have its exact JM: or GS: or AM: line number verified.

**3. Input Inventory**
Table listing every variable that influences the outcome:

| Variable | Slot | How It Influences Winner | Must Be Immutable During Window? | Commitment Point |
|----------|------|------------------------|----------------------------------|------------------|

Include all 7 variables from RESEARCH.md: rngWord, lvl, ticketQueue[_tqReadKey(candidate)], ticketQueue[_tqFarFutureKey(candidate)], ticketWriteSlot, FAR_FUTURE_COIN_TAG, FAR_FUTURE_COIN_SAMPLES. Add the commitment point for each (when it becomes fixed relative to VRF request).

**4. Mutation Surface**
For each permissionless external function that can write to ticketQueue, create a verdict block following this exact format:

```
### Path N: {function}() -> ... -> _queueTickets
**External function:** {function}() in {Module}
**Call chain:** {function} -> {intermediate} -> _queueTickets
**Target variable:** ticketQueue[_tqFarFutureKey(candidate)] or ticketQueue[_tqWriteKey(candidate)]
**Protection:**
  1. If targetLevel <= level + 6 -> routes to _tqWriteKey (not FF key) -> IRRELEVANT to FF population
  2. If targetLevel > level + 6 -> checks rngLockedFlag ({source file}:{line})
     - rngLockedFlag = true during commitment window -> reverts RngLocked()
     - phaseTransitionActive = false for external callers -> no exemption
**Verdict: SAFE** -- {specific reason}
**Evidence:** {source file}:{line numbers}
```

Enumerate ALL 8 paths from RESEARCH.md mutation table:
1. purchase() -> _purchaseFor -> _queueTickets
2. purchaseCoin() -> _purchaseCoinFor -> _queueTicketsScaled
3. purchaseLazyPass() -> _purchaseLazyPass -> _queueTickets
4. purchaseDeityPass() -> _purchaseDeityPass -> _queueTickets
5. claimWhalePass() -> _queueTickets
6. openLootBox() -> _resolveLootboxCommon -> _queueTicketsScaled
7. openBurnieLootBox() -> _resolveLootboxCommon -> _queueTicketsScaled
8. advanceGame() (auto-rebuy + phase transition) -- SAME TRANSACTION exemption

For path 8 (advanceGame), address BOTH sub-paths:
- _processPhaseTransition: deterministic vault/sDGNRS writes, phaseTransitionActive = true
- _processAutoRebuy: runs atomically in same tx as jackpot draw

Explicitly address Pitfall 4 (auto-rebuy) and Pitfall 5 (processTicketBatch drains current level, not candidate levels [lvl+5, lvl+99]).

Also address the requestLootboxRng/mid-day path: confirm it is blocked by rngLockedFlag at AM:678 so no mid-day buffer swap can occur (Pitfall 3).

**5. Combined Pool Length Invariant**
Prove that `combinedLen = readLen + ffLen` at the jackpot draw cannot differ from what it was at VRF request time:

- readLen stability: _tqReadKey depends on ticketWriteSlot (swapped at request, not modified until _unlockRng). processTicketBatch drains current level only, not candidate levels [lvl+5, lvl+99]. No external function writes to read key.
- ffLen stability: rngLockedFlag blocks all permissionless FF writes. phaseTransitionActive writes are atomic with the jackpot draw.
- Conclusion: combinedLen is stable between VRF request and consumption for all external actors.

Address Open Question 1 from RESEARCH.md: trace the advanceGame call order to confirm processFutureTicketBatch runs BEFORE jackpot functions, and verify it only processes levels already swapped into read-side (not candidate levels).

Address Open Question 2 from RESEARCH.md: auto-rebuy targets level + 1 (confirm from source). Even if in candidate range, atomic with jackpot draw.

**6. Cross-Reference to v3.8**
- State this proof extends v3.8 CW-03 Category 3 (Jackpot BURNIE Winner)
- The v3.8 audit only covered traitBurnTicket-based jackpot selection. Phase 79 adds the ticketQueue-based far-future coin jackpot selection via combined pool
- Note that all v3.8 CW-03 mutations to ticketQueue are covered by the mutation paths above

**7. Summary Verdict**
- RNG-01: **SAFE**
- Three independent protection layers: (1) double-buffer swap freezes read buffer, (2) rngLockedFlag guard blocks FF writes, (3) phaseTransitionActive exemption is atomic
- All permissionless mutation paths receive SAFE verdict
- Combined pool length is stable between VRF request and consumption
- No code changes required

**Format conventions:**
- Use JM: for JackpotModule line numbers, GS: for GameStorage, AM: for AdvanceModule
- Every verdict block must have an Evidence line with specific file:line references
- Do NOT use line numbers from RESEARCH.md without verifying against source -- verify every reference
  </action>
  <verify>
    <automated>grep -c "^### Path" audit/v3.9-rng-commitment-window-proof.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.9-rng-commitment-window-proof.md exists with >= 150 lines
    - Document contains "## Backward Trace" section
    - Document contains "## Input Inventory" section
    - Document contains "## Mutation Surface" section with >= 8 "### Path" subsections (one per mutation path)
    - Document contains "## Combined Pool Length Invariant" section
    - Document contains "## Cross-Reference" section referencing v3.8
    - Document contains "## Summary Verdict" with "RNG-01" and "SAFE"
    - Every "### Path" block contains "Verdict: SAFE" and "Evidence:" with file:line references
    - Document references both callers: payDailyJackpotCoinAndTickets and payDailyCoinJackpot
    - Document addresses mid-day lootbox path (requestLootboxRng blocked by rngLockedFlag)
    - Document addresses processTicketBatch drain timing (current level only, not candidate levels)
    - Document addresses auto-rebuy atomicity
    - All line references use JM:/GS:/AM: notation and are verified against source
  </acceptance_criteria>
  <done>A standalone proof document exists at audit/v3.9-rng-commitment-window-proof.md with backward trace, input inventory, 8+ mutation path verdicts (all SAFE), combined pool length invariant proof, v3.8 cross-reference, and summary verdict -- all with verified source line references</done>
</task>

<task type="auto">
  <name>Task 2: Verify proof completeness and run regression check</name>
  <files>audit/v3.9-rng-commitment-window-proof.md</files>
  <read_first>
    - audit/v3.9-rng-commitment-window-proof.md (the proof just created -- verify its content)
    - audit/v3.8-commitment-window-inventory.md (cross-reference: ensure all CW-03 ticketQueue mutation paths are covered)
    - .planning/phases/79-rng-commitment-window-proof/79-RESEARCH.md (check all 5 pitfalls addressed, both open questions resolved)
  </read_first>
  <action>
Perform three verification passes on the proof document:

**Pass 1: Mutation path completeness**
Cross-reference the proof's mutation surface against v3.8 CW-03's complete list of ticketQueue writers. The v3.8 inventory (section 1.7, 1.8, 1.13) lists ticketQueue[key] writes from:
- advanceGame -> payDailyJackpot -> _addClaimableEth -> _processAutoRebuy -> _queueTickets (slot 15)
- advanceGame -> processTicketBatch (slot 15, R/W -- drains queue)

Plus all external callers (purchase, purchaseCoin, openLootBox, etc.) from CW-03.

Verify every CW-03 entry that writes ticketQueue has a corresponding verdict in the proof. If any path is missing, add it to the proof document.

**Pass 2: Pitfall coverage**
Verify all 5 pitfalls from 79-RESEARCH.md are explicitly addressed:
1. Mid-day lootbox RNG path -- proof states _awardFarFutureCoinJackpot only reachable via daily path
2. Near-future vs FF writes -- proof distinguishes the three data flows
3. ticketWriteSlot stability -- proof notes requestLootboxRng blocked by rngLockedFlag
4. Auto-rebuy path -- proof includes auto-rebuy with atomicity argument
5. processTicketBatch drain timing -- proof shows it drains current level, not candidate levels

If any pitfall is not addressed, add the missing analysis to the proof document.

**Pass 3: Open question resolution**
Verify both open questions from 79-RESEARCH.md are resolved in the proof:
1. processFutureTicketBatch timing vs jackpot candidate range
2. Auto-rebuy target level range and atomicity

If either is unresolved, add the resolution.

**Pass 4: Regression check**
Run `forge test` to confirm no accidental edits broke existing tests. This is a document-only phase so tests should all pass unchanged.

If any issues found in passes 1-3, edit the proof document to add the missing content. Do NOT rewrite the entire document -- make surgical additions only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract "TicketRouting|JackpotCombinedPool|TicketEdgeCases|TicketProcessingFF" -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 8+ mutation paths from v3.8 CW-03 ticketQueue writers are present in the proof
    - All 5 pitfalls from 79-RESEARCH.md are addressed in the proof (grep for "mid-day", "near-future", "ticketWriteSlot" or "buffer swap", "auto-rebuy" or "autoRebuy", "processTicketBatch" or "drain")
    - Both open questions from 79-RESEARCH.md are resolved (grep for "processFutureTicketBatch" and "auto-rebuy target")
    - forge test for TicketRouting, JackpotCombinedPool, TicketEdgeCases, TicketProcessingFF all pass
    - No new test failures introduced
  </acceptance_criteria>
  <done>Proof document verified complete against v3.8 CW-03 inventory, all 5 research pitfalls addressed, both open questions resolved, and all existing Foundry tests pass with no regression</done>
</task>

</tasks>

<verification>
1. `audit/v3.9-rng-commitment-window-proof.md` exists with all 7 sections
2. Every mutation path has a SAFE verdict with source line evidence
3. Combined pool length invariant is proven
4. All existing Foundry tests pass (forge test)
5. Proof cross-references v3.8 commitment window inventory
</verification>

<success_criteria>
- RNG-01 requirement is satisfied: formal proof that no permissionless action during VRF commitment window can influence far-future coin jackpot winner selection
- The proof is a standalone document a C4A warden could review independently
- All mutation paths enumerated with SAFE verdicts and source line evidence
- Combined pool length proven stable between VRF request and consumption
- Zero code changes, zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/79-rng-commitment-window-proof/79-01-SUMMARY.md`
</output>
