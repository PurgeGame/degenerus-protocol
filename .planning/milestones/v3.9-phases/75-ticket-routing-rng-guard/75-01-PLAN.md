---
phase: 75-ticket-routing-rng-guard
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/fuzz/TicketRouting.t.sol
  - contracts/storage/DegenerusGameStorage.sol
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03, RNG-02]

must_haves:
  truths:
    - "Far-future tickets (targetLevel > level+6) route to _tqFarFutureKey in all three queue functions"
    - "Near-future tickets (targetLevel <= level+6) route to _tqWriteKey unchanged"
    - "_queueTickets reverts with RngLocked when writing FF key while rngLockedFlag && !phaseTransitionActive"
    - "_queueTicketsScaled reverts with RngLocked when writing FF key while rngLockedFlag && !phaseTransitionActive"
    - "_queueTicketRange reverts per-level when FF key write attempted while rngLockedFlag && !phaseTransitionActive"
    - "All three functions allow FF key writes when phaseTransitionActive is true (advanceGame exemption)"
    - "Near-future ticket writes are unaffected by rngLockedFlag"
  artifacts:
    - path: "test/fuzz/TicketRouting.t.sol"
      provides: "Foundry test harness + routing and RNG guard tests"
      min_lines: 120
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Modified _queueTickets, _queueTicketsScaled, _queueTicketRange with routing + guard"
      contains: "_tqFarFutureKey"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol:_queueTickets"
      to: "contracts/storage/DegenerusGameStorage.sol:_tqFarFutureKey"
      via: "conditional key selection: targetLevel > level + 6"
      pattern: "isFarFuture.*_tqFarFutureKey"
    - from: "contracts/storage/DegenerusGameStorage.sol:_queueTicketsScaled"
      to: "contracts/storage/DegenerusGameStorage.sol:_tqFarFutureKey"
      via: "conditional key selection: targetLevel > level + 6"
      pattern: "isFarFuture.*_tqFarFutureKey"
    - from: "contracts/storage/DegenerusGameStorage.sol:_queueTicketRange"
      to: "contracts/storage/DegenerusGameStorage.sol:_tqFarFutureKey"
      via: "per-level conditional key selection in loop"
      pattern: "isFarFuture.*_tqFarFutureKey"
    - from: "contracts/storage/DegenerusGameStorage.sol:_queueTickets"
      to: "contracts/storage/DegenerusGameStorage.sol:rngLockedFlag"
      via: "revert guard on FF key + rngLocked + !phaseTransitionActive"
      pattern: "isFarFuture && rngLockedFlag && !phaseTransitionActive"
---

<objective>
Add far-future ticket routing and RNG commitment window guard to the three ticket queue write functions in DegenerusGameStorage.sol.

Purpose: Fix the far-future ticket stranding bug by routing tickets targeting >6 levels ahead to the persistent FF key space (Phase 74), and block permissionless FF writes during VRF commitment windows to prevent jackpot manipulation.

Output: Modified DegenerusGameStorage.sol with routing + guard logic, Foundry test harness proving all routing and guard behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-ticket-routing-rng-guard/75-RESEARCH.md
@.planning/phases/74-storage-foundation/74-01-SUMMARY.md
@contracts/storage/DegenerusGameStorage.sol
@test/fuzz/TqFarFutureKey.t.sol

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From contracts/storage/DegenerusGameStorage.sol (lines 706-721):
```solidity
function _tqWriteKey(uint24 lvl) internal view returns (uint24) {
    return ticketWriteSlot != 0 ? lvl | TICKET_SLOT_BIT : lvl;
}
function _tqReadKey(uint24 lvl) internal view returns (uint24) {
    return ticketWriteSlot == 0 ? lvl | TICKET_SLOT_BIT : lvl;
}
function _tqFarFutureKey(uint24 lvl) internal pure returns (uint24) {
    return lvl | TICKET_FAR_FUTURE_BIT;
}
```

From contracts/storage/DegenerusGameStorage.sol (line 224):
```solidity
uint24 public level = 0;
```

From contracts/storage/DegenerusGameStorage.sol (lines 264, 267):
```solidity
bool internal rngLockedFlag;
bool internal phaseTransitionActive;
```

From contracts/storage/DegenerusGameStorage.sol (line 189):
```solidity
error E();
```

Error pattern used by modules (DegenerusGame.sol:106, WhaleModule:27, AdvanceModule:38):
```solidity
error RngLocked();
```

From test/fuzz/TqFarFutureKey.t.sol (lines 8-32) -- existing harness pattern:
```solidity
contract TqFarFutureKeyHarness is DegenerusGameStorage {
    function tqWriteKey(uint24 lvl) external view returns (uint24) { ... }
    function setTicketWriteSlot(uint8 val) external { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Create Foundry test harness and failing tests for routing + RNG guard</name>
  <files>test/fuzz/TicketRouting.t.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (lines 534-674: the three queue functions being tested)
    - contracts/storage/DegenerusGameStorage.sol (lines 700-721: key encoding helpers)
    - contracts/storage/DegenerusGameStorage.sol (lines 260-267: rngLockedFlag, phaseTransitionActive)
    - contracts/storage/DegenerusGameStorage.sol (line 224: level storage variable)
    - contracts/storage/DegenerusGameStorage.sol (line 189: error E())
    - test/fuzz/TqFarFutureKey.t.sol (existing harness pattern to follow)
  </read_first>
  <behavior>
    - testFarFutureRoutesToFFKey: Set level=10, call _queueTickets(buyer, 17, 1) (17 > 10+6). Verify ticketQueue[_tqFarFutureKey(17)] contains buyer, ticketQueue[_tqWriteKey(17)] is empty.
    - testNearFutureRoutesToWriteKey: Set level=10, call _queueTickets(buyer, 16, 1) (16 <= 10+6). Verify ticketQueue[_tqWriteKey(16)] contains buyer, ticketQueue[_tqFarFutureKey(16)] is empty.
    - testBoundaryLevel6RoutesToWriteKey: Set level=10, call _queueTickets(buyer, 16, 1) (exactly level+6). Must route to write key.
    - testBoundaryLevel7RoutesToFFKey: Set level=10, call _queueTickets(buyer, 17, 1) (exactly level+7). Must route to FF key.
    - testScaledFarFutureRoutesToFFKey: Set level=10, call _queueTicketsScaled(buyer, 17, 100). Verify ticketQueue[_tqFarFutureKey(17)] contains buyer.
    - testScaledNearFutureRoutesToWriteKey: Set level=10, call _queueTicketsScaled(buyer, 16, 100). Verify ticketQueue[_tqWriteKey(16)] contains buyer.
    - testRangeRoutingSplitsCorrectly: Set level=10, call _queueTicketRange(buyer, 14, 6, 1) covering levels 14-19. Levels 14-16 (<=10+6) to write key, levels 17-19 (>10+6) to FF key.
    - testRngGuardRevertsOnFFKey: Set level=10, rngLockedFlag=true, phaseTransitionActive=false. Call _queueTickets(buyer, 17, 1). Must revert with RngLocked().
    - testRngGuardAllowsWithPhaseTransition: Set level=10, rngLockedFlag=true, phaseTransitionActive=true. Call _queueTickets(buyer, 17, 1). Must succeed (advanceGame exemption).
    - testRngGuardIgnoresNearFuture: Set level=10, rngLockedFlag=true, phaseTransitionActive=false. Call _queueTickets(buyer, 16, 1). Must succeed (near-future unaffected).
    - testRngGuardScaledRevertsOnFFKey: Set level=10, rngLockedFlag=true, phaseTransitionActive=false. Call _queueTicketsScaled(buyer, 17, 100). Must revert with RngLocked().
    - testRngGuardRangeRevertsOnFirstFFLevel: Set level=10, rngLockedFlag=true, phaseTransitionActive=false. Call _queueTicketRange(buyer, 14, 6, 1). Must revert when it hits level 17 (first FF level).
  </behavior>
  <action>
    Create test/fuzz/TicketRouting.t.sol with:

    1. A harness contract `TicketRoutingHarness is DegenerusGameStorage` exposing:
       - `queueTickets(address, uint24, uint32)` -> calls `_queueTickets`
       - `queueTicketsScaled(address, uint24, uint32)` -> calls `_queueTicketsScaled`
       - `queueTicketRange(address, uint24, uint24, uint32)` -> calls `_queueTicketRange`
       - `setLevel(uint24 lvl)` -> sets `level = lvl`
       - `setRngLockedFlag(bool v)` -> sets `rngLockedFlag = v`
       - `setPhaseTransitionActive(bool v)` -> sets `phaseTransitionActive = v`
       - `getQueueLength(uint24 wk)` -> returns `ticketQueue[wk].length`
       - `getQueueEntry(uint24 wk, uint256 idx)` -> returns `ticketQueue[wk][idx]`
       - `tqWriteKey(uint24 lvl)` -> returns `_tqWriteKey(lvl)`
       - `tqFarFutureKey(uint24 lvl)` -> returns `_tqFarFutureKey(lvl)`
       - Declare `error RngLocked();` at the top of the harness (needed to match the error the storage contract will emit -- even though the storage contract doesn't have it YET, the test is written RED-first)

    2. A test contract `TicketRoutingTest is Test` with all 12 tests from the behavior block above.

    3. The harness must NOT add `error RngLocked()` to DegenerusGameStorage.sol yet -- that happens in Task 2. The harness just declares it locally for `vm.expectRevert`.

    4. All tests should FAIL at this point because:
       - Routing tests fail: _queueTickets still uses _tqWriteKey unconditionally
       - RNG guard tests fail: no guard exists yet, so no revert happens

    Run: `forge test --match-contract TicketRoutingTest -vv` -- expect ALL tests to fail (RED phase).

    Commit with message: `test(75-01): add failing tests for ticket routing and RNG guard`
  </action>
  <verify>
    <automated>forge test --match-contract TicketRoutingTest -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/TicketRouting.t.sol exists and contains `contract TicketRoutingHarness is DegenerusGameStorage`
    - test/fuzz/TicketRouting.t.sol contains `contract TicketRoutingTest is Test`
    - test/fuzz/TicketRouting.t.sol contains `function testFarFutureRoutesToFFKey`
    - test/fuzz/TicketRouting.t.sol contains `function testNearFutureRoutesToWriteKey`
    - test/fuzz/TicketRouting.t.sol contains `function testBoundaryLevel6RoutesToWriteKey`
    - test/fuzz/TicketRouting.t.sol contains `function testBoundaryLevel7RoutesToFFKey`
    - test/fuzz/TicketRouting.t.sol contains `function testScaledFarFutureRoutesToFFKey`
    - test/fuzz/TicketRouting.t.sol contains `function testRangeRoutingSplitsCorrectly`
    - test/fuzz/TicketRouting.t.sol contains `function testRngGuardRevertsOnFFKey`
    - test/fuzz/TicketRouting.t.sol contains `function testRngGuardAllowsWithPhaseTransition`
    - test/fuzz/TicketRouting.t.sol contains `function testRngGuardIgnoresNearFuture`
    - test/fuzz/TicketRouting.t.sol contains `function testRngGuardScaledRevertsOnFFKey`
    - test/fuzz/TicketRouting.t.sol contains `function testRngGuardRangeRevertsOnFirstFFLevel`
    - `forge test --match-contract TicketRoutingTest` compiles successfully (no Solidity errors)
    - All 12 tests FAIL (RED phase -- routing and guard not implemented yet)
  </acceptance_criteria>
  <done>12 Foundry tests exist covering all 4 requirements (ROUTE-01, ROUTE-02, ROUTE-03, RNG-02). All tests compile but fail because the routing and guard logic does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement routing + RNG guard in DegenerusGameStorage.sol</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (lines 185-189: error declarations -- add RngLocked here)
    - contracts/storage/DegenerusGameStorage.sol (lines 534-560: _queueTickets current code)
    - contracts/storage/DegenerusGameStorage.sol (lines 567-616: _queueTicketsScaled current code)
    - contracts/storage/DegenerusGameStorage.sol (lines 624-658: _queueTicketRange current code)
    - contracts/storage/DegenerusGameStorage.sol (lines 260-267: rngLockedFlag, phaseTransitionActive declarations)
    - contracts/storage/DegenerusGameStorage.sol (line 224: level variable)
    - test/fuzz/TicketRouting.t.sol (the RED tests from Task 1 -- understand what must pass)
  </read_first>
  <action>
    Make exactly these changes to contracts/storage/DegenerusGameStorage.sol:

    **Change 1: Add RngLocked error (line ~190, after `error E();`)**
    ```solidity
    /// @dev Reverts when a permissionless far-future ticket write is attempted during VRF commitment window.
    error RngLocked();
    ```

    **Change 2: Modify _queueTickets (line 541)**
    Replace:
    ```solidity
        uint24 wk = _tqWriteKey(targetLevel);
    ```
    With:
    ```solidity
        bool isFarFuture = targetLevel > level + 6;
        if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();
        uint24 wk = isFarFuture ? _tqFarFutureKey(targetLevel) : _tqWriteKey(targetLevel);
    ```
    Leave all code after `wk` assignment completely unchanged.

    **Change 3: Modify _queueTicketsScaled (line 574)**
    Replace:
    ```solidity
        uint24 wk = _tqWriteKey(targetLevel);
    ```
    With:
    ```solidity
        bool isFarFuture = targetLevel > level + 6;
        if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();
        uint24 wk = isFarFuture ? _tqFarFutureKey(targetLevel) : _tqWriteKey(targetLevel);
    ```
    Leave all code after `wk` assignment completely unchanged.

    **Change 4: Modify _queueTicketRange (starting at line 631)**
    Replace:
    ```solidity
        uint24 lvl = startLevel;
        for (uint24 i = 0; i < numLevels; ) {
            uint24 wk = _tqWriteKey(lvl);
    ```
    With:
    ```solidity
        uint24 currentLevel = level; // cache outside loop to avoid repeated SLOAD
        uint24 lvl = startLevel;
        for (uint24 i = 0; i < numLevels; ) {
            bool isFarFuture = lvl > currentLevel + 6;
            if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();
            uint24 wk = isFarFuture ? _tqFarFutureKey(lvl) : _tqWriteKey(lvl);
    ```
    Leave all code after `wk` assignment in the loop completely unchanged.

    **No other changes.** Do NOT modify _queueLootboxTickets (it delegates to _queueTicketsScaled). Do NOT modify function signatures. Do NOT add parameters.

    After making changes:
    1. Run `npx hardhat compile` to confirm clean compilation across all 11 inheriting contracts
    2. Run `forge test --match-contract TicketRoutingTest -vv` to confirm all 12 tests pass (GREEN)
    3. Run `forge test` to confirm no regressions in existing TqFarFutureKey tests

    Commit with message: `feat(75-01): add far-future routing and rngLocked guard to ticket queue functions`
  </action>
  <verify>
    <automated>npx hardhat compile && forge test --match-contract TicketRoutingTest -vv && forge test --match-contract TqFarFutureKeyTest -vv</automated>
  </verify>
  <acceptance_criteria>
    - contracts/storage/DegenerusGameStorage.sol contains `error RngLocked();`
    - contracts/storage/DegenerusGameStorage.sol _queueTickets contains `bool isFarFuture = targetLevel > level + 6;`
    - contracts/storage/DegenerusGameStorage.sol _queueTickets contains `if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();`
    - contracts/storage/DegenerusGameStorage.sol _queueTickets contains `isFarFuture ? _tqFarFutureKey(targetLevel) : _tqWriteKey(targetLevel)`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketsScaled contains `bool isFarFuture = targetLevel > level + 6;`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketsScaled contains `if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketsScaled contains `isFarFuture ? _tqFarFutureKey(targetLevel) : _tqWriteKey(targetLevel)`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketRange contains `uint24 currentLevel = level;` BEFORE the for loop
    - contracts/storage/DegenerusGameStorage.sol _queueTicketRange loop contains `bool isFarFuture = lvl > currentLevel + 6;`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketRange loop contains `if (isFarFuture && rngLockedFlag && !phaseTransitionActive) revert RngLocked();`
    - contracts/storage/DegenerusGameStorage.sol _queueTicketRange loop contains `isFarFuture ? _tqFarFutureKey(lvl) : _tqWriteKey(lvl)`
    - `npx hardhat compile` exits 0 (all contracts compile)
    - `forge test --match-contract TicketRoutingTest` exits 0 (all 12 tests pass)
    - `forge test --match-contract TqFarFutureKeyTest` exits 0 (no regression in Phase 74 tests)
  </acceptance_criteria>
  <done>All 12 routing and RNG guard tests pass. Compilation is clean across the full contract suite. Phase 74 collision tests still pass. ROUTE-01, ROUTE-02, ROUTE-03, and RNG-02 requirements are implemented.</done>
</task>

</tasks>

<verification>
1. `npx hardhat compile` -- all contracts compile cleanly (no regressions from storage contract changes)
2. `forge test --match-contract TicketRoutingTest -vv` -- all 12 routing + guard tests pass
3. `forge test --match-contract TqFarFutureKeyTest -vv` -- Phase 74 collision tests still pass
4. `forge test` -- full Foundry suite green
5. Manual: Inspect _queueTickets, _queueTicketsScaled, _queueTicketRange -- each has the conditional key selection and RNG guard pattern
6. Manual: Confirm _queueLootboxTickets is NOT modified (delegates to _queueTicketsScaled which has the fix)
7. Manual: Confirm no function signatures changed (central fix property preserved)
</verification>

<success_criteria>
- Far-future tickets (targetLevel > level+6) route to _tqFarFutureKey in _queueTickets, _queueTicketsScaled, and _queueTicketRange
- Near-future tickets (targetLevel <= level+6) route to _tqWriteKey unchanged (ROUTE-02)
- RNG guard reverts with RngLocked() for FF key writes when rngLockedFlag && !phaseTransitionActive (ROUTE-03, RNG-02)
- PhaseTransitionActive exemption allows advanceGame-origin FF writes through the guard
- _queueTicketRange caches level outside the loop (gas optimization, avoids repeated SLOAD)
- All 12 tests pass, full compilation clean, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/75-ticket-routing-rng-guard/75-01-SUMMARY.md`
</output>
