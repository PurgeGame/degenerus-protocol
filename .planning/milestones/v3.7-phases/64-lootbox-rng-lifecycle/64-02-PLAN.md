---
phase: 64-lootbox-rng-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - audit/v3.7-lootbox-rng-findings.md
  - audit/KNOWN-ISSUES.md
autonomous: true
requirements: [LBOX-01, LBOX-02, LBOX-03, LBOX-04, LBOX-05]

must_haves:
  truths:
    - "Every LBOX requirement has a VERIFIED or finding entry in the findings document with test evidence"
    - "_getHistoricalRngFallback missing zero guard is documented as INFO finding with C4A severity"
    - "KNOWN-ISSUES.md has Phase 64 results section with summary and any new findings"
    - "All mutation sites, write sites, and zero-guard inventory from research are traced in findings document"
  artifacts:
    - path: "audit/v3.7-lootbox-rng-findings.md"
      provides: "Phase 64 lootbox RNG lifecycle findings document"
      min_lines: 150
      contains: "LBOX-01"
    - path: "audit/KNOWN-ISSUES.md"
      provides: "Updated known issues with Phase 64 results"
      contains: "Phase 64"
  key_links:
    - from: "audit/v3.7-lootbox-rng-findings.md"
      to: "test/fuzz/LootboxRngLifecycle.t.sol"
      via: "test evidence references"
      pattern: "test_"
    - from: "audit/KNOWN-ISSUES.md"
      to: "audit/v3.7-lootbox-rng-findings.md"
      via: "See reference"
      pattern: "v3.7-lootbox-rng-findings"
---

<objective>
Create Phase 64 findings document cataloging all lootbox RNG lifecycle audit results with C4A severity classifications, and update KNOWN-ISSUES.md with Phase 64 results.

Purpose: Document all findings from the lootbox RNG lifecycle audit (LBOX-01 through LBOX-05) in a format ready for C4A wardens. The findings document traces every mutation site, write site, zero-guard location, entropy derivation path, and lifecycle step with test evidence from Plan 01.

Output: audit/v3.7-lootbox-rng-findings.md (findings document) + updated audit/KNOWN-ISSUES.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-lootbox-rng-lifecycle/64-RESEARCH.md
@.planning/phases/64-lootbox-rng-lifecycle/64-01-SUMMARY.md (from Plan 01 -- test results and any deviations)
@audit/v3.7-vrf-core-findings.md (template and format to follow)
@audit/KNOWN-ISSUES.md (current state, will be appended to)

<interfaces>
<!-- Key formats and patterns from v3.7-vrf-core-findings.md to replicate. -->

Findings document format:
```markdown
# v3.7 Lootbox RNG Findings: [Phase Title]
**Date:** YYYY-MM-DD
**Milestone:** v3.7
**Scope:** Phase 64 -- Lootbox RNG Lifecycle
**Mode:** Audit-only (no code changes)

## Executive Summary
| Metric | Count |
|--------|-------|
| Total findings | N |
| By severity: HIGH/MEDIUM/LOW/INFO | ... |
| Requirements verified | 5/5 |
| Foundry tests created | N |
| Test failures | 0 |

## ID Assignment
V37-XXX namespace. Phase 63 used V37-001 and V37-002. Phase 64 starts at V37-003.

## Master Findings Table
### [SEVERITY] (N)
| ID | Severity | Type | Contract | Lines | Summary | Recommendation |

## Per-Requirement Verification Summary
### LBOX-01: ... -- VERIFIED/FINDING
**Requirement:** ...
**Evidence:** list of test names with brief description

## Cross-Cutting Observations
```

KNOWN-ISSUES.md format (from existing Phase 63 entry):
```markdown
### v3.7 Phase 64: Lootbox RNG Lifecycle (YYYY-MM-DD)

N HIGH, N MEDIUM, N LOW, N INFO. [Summary sentence]. [Test evidence sentence].

- **V37-XXX (SEVERITY):** [description]

See `audit/v3.7-lootbox-rng-findings.md` for full findings document.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v3.7 lootbox RNG findings document</name>
  <files>audit/v3.7-lootbox-rng-findings.md</files>
  <read_first>
    - audit/v3.7-vrf-core-findings.md (FULL FILE: format template -- replicate section structure exactly)
    - .planning/phases/64-lootbox-rng-lifecycle/64-01-SUMMARY.md (test results, any deviations or findings discovered during testing)
    - .planning/phases/64-lootbox-rng-lifecycle/64-RESEARCH.md (mutation sites table, write sites table, zero-guard inventory, entropy analysis, lifecycle trace -- these become the findings document content)
    - test/fuzz/LootboxRngLifecycle.t.sol (test names for evidence references)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 726-743, 845-851, 962, 1267-1280, 1405-1422, 1456-1472 -- exact line numbers for findings)
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 549-600 -- openLootBox entropy derivation)
    - contracts/modules/DegenerusGameMintModule.sol (lines 686-716 -- purchase recording)
  </read_first>
  <action>
    Create audit/v3.7-lootbox-rng-findings.md following the EXACT format of audit/v3.7-vrf-core-findings.md.

    Document structure (match v3.7-vrf-core-findings.md section by section):

    1. **Header block:**
       - Title: "v3.7 Lootbox RNG Findings: Lootbox RNG Lifecycle"
       - Date: 2026-03-22
       - Milestone: v3.7
       - Scope: Phase 64 -- Lootbox RNG Lifecycle (2 plans: fuzz test suite + findings document)
       - Mode: Audit-only (no code changes)
       - Source phase: Phase 64: Lootbox RNG Lifecycle

    2. **Executive Summary table:**
       - Total findings count (expected: at least 1 INFO for _getHistoricalRngFallback zero guard gap)
       - By severity: HIGH/MEDIUM/LOW/INFO
       - Requirements verified: 5/5 (LBOX-01 through LBOX-05)
       - Foundry tests created: N (from Plan 01 summary)
       - Fuzz runs per test: 1,000
       - Test failures: 0
       - Outstanding prior milestones: update count from v3.7-vrf-core-findings.md (82 + any new)
       - Grand total

    3. **ID Assignment:**
       - Phase 63 used V37-001, V37-002
       - Phase 64 findings start at V37-003
       - Table mapping local IDs to consolidated IDs

    4. **Master Findings Table:**
       Based on research analysis, expected findings:
       - V37-003 (INFO): `_getHistoricalRngFallback` missing zero guard -- returns keccak256 output without `if (word == 0) word = 1`. If result were 0 (probability 2^-256) and nudges are 0, `_finalizeLootboxRng` would store 0, making openLootBox permanently revert RngNotReady for that index. Location: DegenerusGameAdvanceModule.sol line ~962. Recommendation: Add `if (fallbackWord == 0) fallbackWord = 1` after keccak256 for defense-in-depth.
       - V37-004 (INFO): Mid-day `lastLootboxRngWord` update path -- `rawFulfillRandomWords` mid-day branch does NOT update `lastLootboxRngWord`. It is only updated via `_finalizeLootboxRng` (daily path) or the mid-day drain path in advanceGame (lines 167-170). This is correct by design (lastLootboxRngWord is only needed for ticket processing entropy, which runs via advanceGame). Document as INFO for warden context.
       - CHECK Plan 01 summary for any additional findings discovered during test development (deviations, auto-fixed issues). Include those as findings if they reveal contract behavior.

    5. **Lootbox RNG Index Mutation Audit (LBOX-01 content):**
       Reproduce the mutation site table from research:
       | Site | Function | Line | When | Increments? |
       Include analysis for each site with test evidence.

    6. **Word-to-Index Write Site Audit (LBOX-02 content):**
       Reproduce the write site table from research:
       All 5 write sites with index computation verification and test evidence.

    7. **Zero-State Guard Inventory (LBOX-03 content):**
       Reproduce the guard inventory table from research:
       | Site | Location | Zero Guard | Status |
       Include the _getHistoricalRngFallback UNGUARDED finding.

    8. **Entropy Derivation Analysis (LBOX-04 content):**
       keccak256 preimage uniqueness proof for (player, day, amount) tuples.
       ETH lootbox and BURNIE lootbox entropy formulas.
       Degenerette and redemption lootbox cross-references.

    9. **Full Lifecycle Trace (LBOX-05 content):**
       6-step lifecycle from research: Purchase -> VRF Request -> VRF Fulfillment -> Daily Processing -> RngNotReady Guard -> Prize Determination. Each step with contract reference and test evidence.

    10. **Per-Requirement Verification Summary:**
        For each LBOX-01 through LBOX-05:
        - Requirement statement
        - Status: VERIFIED
        - Evidence: list of test function names from LootboxRngLifecycle.t.sol with brief descriptions

    11. **Cross-Cutting Observations:**
        - Idempotency of _finalizeLootboxRng (safe against double-call)
        - DegeneretteModule piggybacks on lootbox RNG (same index-to-word guarantees apply)
        - Coordinator swap + backfill path is the most complex but correctly handles orphaned indices
        - Amount accumulation in lootboxEth prevents same-player same-day entropy collision

    Verify line numbers by reading the actual contract files (in read_first). Research may have approximate line numbers -- use the actual lines from the contracts.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -f audit/v3.7-lootbox-rng-findings.md && grep -c "LBOX-0" audit/v3.7-lootbox-rng-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.7-lootbox-rng-findings.md exists
    - File contains "v3.7 Lootbox RNG Findings" in title
    - File contains "LBOX-01" through "LBOX-05" (all 5 requirements referenced)
    - File contains "V37-003" (at least one new finding ID)
    - File contains "VERIFIED" for each LBOX requirement
    - File contains "Executive Summary" table with findings count
    - File contains "Per-Requirement Verification Summary" section
    - File contains at least 5 references to test function names (test_*) from LootboxRngLifecycle.t.sol
    - File contains "_getHistoricalRngFallback" (the known zero-guard gap finding)
    - File is at least 150 lines long
  </acceptance_criteria>
  <done>Findings document created with all 5 LBOX requirements VERIFIED, at least 1 INFO finding (V37-003: _getHistoricalRngFallback zero guard), mutation site audit, write site audit, zero-guard inventory, entropy analysis, and full lifecycle trace with test evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update KNOWN-ISSUES.md with Phase 64 results</name>
  <files>audit/KNOWN-ISSUES.md</files>
  <read_first>
    - audit/KNOWN-ISSUES.md (FULL FILE: current state, find exact insertion point after Phase 63 entry)
    - audit/v3.7-lootbox-rng-findings.md (the document just created in Task 1 -- extract summary counts and finding IDs)
  </read_first>
  <action>
    Update audit/KNOWN-ISSUES.md by adding a Phase 64 entry in the "Audit History" section, directly AFTER the existing "v3.7 Phase 63" entry.

    Insert the following block after the Phase 63 entry (after the line "See `audit/v3.7-vrf-core-findings.md` for full findings document."):

    ```markdown
    ### v3.7 Phase 64: Lootbox RNG Lifecycle (2026-03-22)

    N HIGH, N MEDIUM, N LOW, N INFO. All 5 lootbox RNG lifecycle requirements (LBOX-01 through LBOX-05) VERIFIED with N Foundry fuzz tests (1000 runs each, 0 failures). Index mutation audit: N mutation sites verified (increment on fresh daily + mid-day, no increment on retry + swap). Word write audit: N write sites verified (daily, mid-day, stale, backfill, gameover). Zero-state guards: N/N sites guarded. Entropy derivation: unique per (player, day, amount) tuple via keccak256 preimage. Full lifecycle: purchase -> VRF -> open traced end-to-end.

    - **V37-003 (INFO):** `_getHistoricalRngFallback` (AdvanceModule ~line 962) returns keccak256 output without explicit `if (word == 0) word = 1` guard. All other VRF word injection points have this guard. Probability of keccak256 returning 0 is 2^-256 (negligible). If triggered with zero nudges, would cause permanent RngNotReady for that lootbox index.
    [Include any additional findings from v3.7-lootbox-rng-findings.md]

    See `audit/v3.7-lootbox-rng-findings.md` for full findings document.
    ```

    Replace the N placeholders with actual counts from the findings document created in Task 1. Use the exact finding IDs (V37-003, V37-004, etc.) and severities from the findings document.

    Do NOT modify any existing content in KNOWN-ISSUES.md. Only ADD the new Phase 64 section.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "Phase 64" audit/KNOWN-ISSUES.md && grep "v3.7-lootbox-rng-findings" audit/KNOWN-ISSUES.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/KNOWN-ISSUES.md contains "v3.7 Phase 64: Lootbox RNG Lifecycle"
    - File contains "LBOX-01 through LBOX-05"
    - File contains "v3.7-lootbox-rng-findings.md" reference
    - File contains "V37-003" (the new finding ID)
    - The existing Phase 63 entry is unchanged (grep "v3.7 Phase 63" still matches)
    - The existing "Intentional Design" and "Design Mechanics" sections are unchanged
    - File contains at least 2 entries in the Audit History section (Phase 63 + Phase 64)
  </acceptance_criteria>
  <done>KNOWN-ISSUES.md updated with Phase 64 results section. Phase 63 entry preserved. New finding IDs listed. Full findings document referenced.</done>
</task>

</tasks>

<verification>
Findings document exists and has correct structure:
```bash
test -f audit/v3.7-lootbox-rng-findings.md && head -30 audit/v3.7-lootbox-rng-findings.md
```

KNOWN-ISSUES.md has both Phase 63 and Phase 64 entries:
```bash
grep "v3.7 Phase 6" audit/KNOWN-ISSUES.md
```

All requirement IDs referenced:
```bash
grep -c "LBOX-0" audit/v3.7-lootbox-rng-findings.md
```

All tests still pass:
```bash
forge test --match-path test/fuzz/LootboxRngLifecycle.t.sol --fuzz-runs 1000 -vvv
```
</verification>

<success_criteria>
- audit/v3.7-lootbox-rng-findings.md exists with all 5 LBOX requirements documented as VERIFIED
- At least 1 finding (V37-003: _getHistoricalRngFallback zero guard) documented with C4A severity
- Findings document follows exact format of audit/v3.7-vrf-core-findings.md
- audit/KNOWN-ISSUES.md has Phase 64 results entry with finding summary
- All existing KNOWN-ISSUES.md content preserved unchanged
- Document references test evidence from LootboxRngLifecycle.t.sol
</success_criteria>

<output>
After completion, create `.planning/phases/64-lootbox-rng-lifecycle/64-02-SUMMARY.md`
</output>
