---
phase: 65-vrf-stall-edge-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/VRFStallEdgeCases.t.sol
autonomous: true
requirements: [STALL-01, STALL-02, STALL-03, STALL-04, STALL-05, STALL-06, STALL-07]

must_haves:
  truths:
    - "Gap backfill entropy produces unique nonzero per-day words via keccak256(vrfWord, gapDay)"
    - "Manipulation window between VRF callback and advanceGame consumption is proven identical to standard daily VRF window"
    - "Gap backfill gas for 120-day gap (death clock maximum) fits within 30M block gas limit"
    - "Coordinator swap resets all 8 expected state variables and preserves all intentionally-kept variables"
    - "Zero-seed edge case (lastLootboxRngWord==0 at swap) cannot produce degenerate ticket entropy in any reachable code path"
    - "Gameover _tryRequestRng guard branches return false without revert when VRF not configured (V37-001 deferred coverage)"
    - "dailyIdx-aligned flipDay in resolveRedemptionPeriod is consistent across normal, gameover, and fallback paths"
  artifacts:
    - path: "test/fuzz/VRFStallEdgeCases.t.sol"
      provides: "Fuzz/unit tests covering STALL-01 through STALL-07"
      min_lines: 300
  key_links:
    - from: "test/fuzz/VRFStallEdgeCases.t.sol"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "game.advanceGame(), game.rngWordForDay(), game.lootboxRngWord()"
      pattern: "game\\.(advanceGame|rngWordForDay|lootboxRngWord)"
    - from: "test/fuzz/VRFStallEdgeCases.t.sol"
      to: "test/fuzz/StallResilience.t.sol"
      via: "Reuses helper patterns: _completeDay, _doCoordinatorSwap, _stallAndSwap, _resumeAfterSwap"
      pattern: "_completeDay|_doCoordinatorSwap|_stallAndSwap|_resumeAfterSwap"
---

<objective>
Create a Foundry fuzz/unit test suite proving all 7 VRF stall edge case requirements (STALL-01 through STALL-07) with automated verification.

Purpose: Provide executable test evidence for the Phase 65 findings document. Tests prove gap backfill entropy uniqueness, gas ceiling safety, coordinator swap state completeness, zero-seed unreachability, gameover _tryRequestRng guard branches (V37-001 deferred from Phase 63), and dailyIdx timing consistency.

Output: test/fuzz/VRFStallEdgeCases.t.sol (~350-500 lines) with 15-25 fuzz/unit tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-vrf-stall-edge-cases/65-RESEARCH.md

<interfaces>
<!-- Reusable helper patterns from existing test files. Executor should replicate these in VRFStallEdgeCases.t.sol (do NOT import from StallResilience.t.sol -- Foundry test contracts are standalone). -->

From test/fuzz/StallResilience.t.sol (helper patterns to replicate):
```solidity
import {DeployProtocol} from "./helpers/DeployProtocol.sol";
import {MockVRFCoordinator} from "../../contracts/mocks/MockVRFCoordinator.sol";
import {MintPaymentKind} from "../../contracts/interfaces/IDegenerusGame.sol";

// _completeDay: advanceGame -> VRF fulfill -> loop until !rngLocked
// _doCoordinatorSwap: new MockVRF -> createSubscription -> addConsumer -> admin prank -> updateVrfCoordinatorAndSub
// _stallAndSwap: vm.warp(gapDays) -> _doCoordinatorSwap
// _resumeAfterSwap: advanceGame -> newVRF.fulfillRandomWords -> loop until !rngLocked
```

From test/fuzz/VRFCore.t.sol (storage slot constants):
```solidity
// Slot 0 packed fields: rngLockedFlag, rngRequestTime (bits 96-143)
// Slot 4: rngWordCurrent
// Slot 5: vrfRequestId
```

From game contract public view functions:
```solidity
game.rngWordForDay(uint48 day) returns (uint256)  // rngWordByDay[day]
game.lootboxRngWord(uint48 index) returns (uint256)  // lootboxRngWordByIndex[index]
game.lootboxRngIndexView() returns (uint48)  // current lootboxRngIndex
game.currentDayView() returns (uint48)  // _simulatedDayIndex() wall-clock day
game.rngLocked() returns (bool)  // rngLockedFlag
```

Key contract code (from research, for test design):
```solidity
// _backfillGapDays: keccak256(abi.encodePacked(vrfWord, gapDay)) with zero guard
// _backfillOrphanedLootboxIndices: keccak256(abi.encodePacked(vrfWord, i)) with zero guard
// updateVrfCoordinatorAndSub: resets vrfCoordinator, vrfSubscriptionId, vrfKeyHash, rngLockedFlag, vrfRequestId, rngRequestTime, rngWordCurrent, midDayTicketRngPending
// _tryRequestRng: try/catch around _finalizeRngRequest, returns false if coordinator==address(0) or keyHash==bytes32(0) or subscriptionId==0
// _getHistoricalRngFallback: collects up to 5 early rngWordByDay entries, hashes with currentDay and block.prevrandao
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STALL-01/02/03 gap backfill tests (entropy, manipulation window, gas ceiling)</name>
  <files>test/fuzz/VRFStallEdgeCases.t.sol</files>
  <read_first>
    - test/fuzz/StallResilience.t.sol (helper patterns to replicate: _completeDay, _doCoordinatorSwap, _stallAndSwap, _resumeAfterSwap)
    - test/fuzz/VRFCore.t.sol (storage slot constants, test naming conventions)
    - test/fuzz/LootboxRngLifecycle.t.sol (lootbox purchase helper pattern, absolute timestamp pattern)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1436-1450 (_backfillGapDays implementation)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 794-807 (rngGate gap detection entry point)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1338-1367 (updateVrfCoordinatorAndSub)
  </read_first>
  <action>
Create test/fuzz/VRFStallEdgeCases.t.sol inheriting DeployProtocol. Replicate helpers from StallResilience.t.sol (_completeDay, _doCoordinatorSwap, _stallAndSwap, _resumeAfterSwap) as internal functions (do NOT import -- Foundry test contracts are standalone).

Use absolute timestamps (N * 86400) for all day boundary warps per Phase 63 decision.

**STALL-01 tests (gap backfill entropy uniqueness):**

1. `test_gapBackfillEntropyUnique_fuzz(uint256 vrfWord)` -- Fuzz: complete day 1, stall for 5 gap days, swap, resume with fuzzed VRF word. Verify each gap day word equals keccak256(abi.encodePacked(resumeWord, uint48(gapDay))). Verify all gap day words are distinct from each other. vm.assume(vrfWord != 0).

2. `test_gapBackfillZeroGuard()` -- Unit: find a vrfWord+gapDay combination where keccak256 produces 0 (skip if not feasible -- use a known zero guard assertion). Instead, verify that for a range of gap days (1-10), all derived words are nonzero (statistical verification). The zero guard in _backfillGapDays replaces 0 with 1.

3. `test_gapBackfillSingleDayGap()` -- Unit: complete day 1, stall exactly 1 day (gap count = 1), swap, resume. Verify exactly 1 gap day backfilled with correct keccak256 derivation.

**STALL-02 tests (manipulation window):**

4. `test_manipulationWindowIdenticalToDaily()` -- Unit: demonstrate that the VRF word is stored via rawFulfillRandomWords and consumed on next advanceGame call. Both daily path and gap backfill path use the same rngWordCurrent storage. After VRF callback, read rngWordCurrent (nonzero). After advanceGame processes it, rngWordCurrent is cleared. This is the standard VRF window -- no additional attack surface from gap backfill.

5. `test_gapDayPositionsPreCommitted()` -- Unit: place coinflip bets before stall, verify that after backfill, gap day coinflip results are populated. Players cannot add new bets for past gap days (no function accepts a past day as target). Use coinflip.getCoinflipDayResult(gapDay) to verify nonzero rewardPercent.

**STALL-03 tests (gas ceiling):**

6. `test_gapBackfillGas30Days()` -- Gas profile: complete day 1, stall 30 days, swap, resume. Measure gasleft() before and after _resumeAfterSwap. Assert gasUsed < 10_000_000 (30 days * ~125k = ~3.75M + overhead, well under 30M).

7. `test_gapBackfillGas120Days()` -- Gas profile: complete day 1, stall 120 days (death clock maximum), swap, resume. Measure gas. Assert gasUsed < 25_000_000 (120 days * ~125k = ~15M + overhead). This is the maximum practical gap before game-over transitions.

Write setUp() calling _deployProtocol(). All tests compile and pass with `forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol -vvv`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol --match-test "test_gapBackfill|test_manipulationWindow|test_gapDayPositions" -vvv</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/VRFStallEdgeCases.t.sol exists and contains `contract VRFStallEdgeCases is DeployProtocol`
    - File contains `function test_gapBackfillEntropyUnique_fuzz(uint256 vrfWord)` with vm.assume and keccak256 verification loop
    - File contains `function test_gapBackfillZeroGuard()` verifying all derived words are nonzero
    - File contains `function test_gapBackfillSingleDayGap()` with single gap day assertion
    - File contains `function test_manipulationWindowIdenticalToDaily()` reading rngWordCurrent before and after advanceGame
    - File contains `function test_gapDayPositionsPreCommitted()` with getCoinflipDayResult assertions
    - File contains `function test_gapBackfillGas30Days()` with assertTrue(gasUsed < 10_000_000)
    - File contains `function test_gapBackfillGas120Days()` with assertTrue(gasUsed < 25_000_000)
    - All 7 tests pass: `forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol --match-test "test_gapBackfill|test_manipulationWindow|test_gapDayPositions" -vvv` exits 0
  </acceptance_criteria>
  <done>7 tests covering STALL-01 (entropy uniqueness + zero guard + single day), STALL-02 (window identity + pre-committed positions), STALL-03 (30-day and 120-day gas profiles) all pass</done>
</task>

<task type="auto">
  <name>Task 2: STALL-04/05/06/07 coordinator swap, zero-seed, gameover, and timing tests</name>
  <files>test/fuzz/VRFStallEdgeCases.t.sol</files>
  <read_first>
    - test/fuzz/VRFStallEdgeCases.t.sol (Task 1 output -- read current state before appending)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1338-1367 (updateVrfCoordinatorAndSub -- 8 variables reset)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 858-931 (_gameOverEntropy three paths)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 944-963 (_getHistoricalRngFallback)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1234-1268 (_tryRequestRng guard branches)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 810-820 (rngGate flipDay = day + 1, resolveRedemptionPeriod call)
    - contracts/StakedDegenerusStonk.sol lines 530-563 (resolveRedemptionPeriod -- takes roll and flipDay as parameters, no internal block.timestamp)
    - contracts/storage/DegenerusGameStorage.sol (state variable declarations for slot verification)
  </read_first>
  <action>
Append to test/fuzz/VRFStallEdgeCases.t.sol (add new test functions after the Task 1 tests).

**STALL-04 tests (coordinator swap state cleanup):**

8. `test_coordinatorSwapResetsAllVrfState()` -- Unit: complete day 1, trigger day 2 VRF request (rngLocked=true, vrfRequestId!=0, rngRequestTime!=0, rngWordCurrent==0). Then do coordinator swap. Verify via direct storage reads or public getters:
   - rngLocked() == false
   - vrfRequestId cleared (read slot 5 via vm.load, == 0)
   - rngRequestTime cleared (read slot 0 packed bits 96-143 via vm.load, == 0)
   - rngWordCurrent cleared (read slot 4 via vm.load, == 0)
   Verify preserved variables:
   - lootboxRngIndexView() unchanged from pre-swap value
   - rngWordForDay(1) unchanged (historical words preserved)

9. `test_coordinatorSwapPreservesTotalFlipReversals_fuzz(uint8 nudges)` -- Fuzz: purchase nudges via game.purchase with nudge amount, complete day 1 so nudges are recorded, read totalFlipReversals. Do coordinator swap. Verify totalFlipReversals unchanged. (If reading totalFlipReversals is not possible via public getter, verify indirectly: the next day's RNG word should include nudge application.)

10. `test_coordinatorSwapClearsMidDayPending()` -- Unit: complete day 1, trigger mid-day lootbox RNG request (if reachable), verify midDayTicketRngPending is set, then coordinator swap. Verify midDayTicketRngPending cleared (if readable via storage slot) or verify that after swap, a new daily advanceGame cycle works without "NotTimeYet" revert.

**STALL-05 tests (zero-seed edge case):**

11. `test_zeroSeedUnreachableAfterSwap()` -- Unit: game starts fresh (lastLootboxRngWord == 0 by default). Complete day 1 (sets lastLootboxRngWord to nonzero via _finalizeLootboxRng). Do coordinator swap. Verify lastLootboxRngWord is still the pre-swap nonzero value. Resume with new VRF. After resume, verify lastLootboxRngWord updated to new value via _finalizeLootboxRng.

12. `test_zeroSeedAtGameStart()` -- Unit: verify that at game start (before any day completion), lootboxRngWord(0) == 0 (no word at index 0). After coordinator swap at game start (edge case), the resume cycle (advanceGame -> VRF -> advanceGame) sets lastLootboxRngWord to a nonzero value before any ticket processing can run. Verify by checking that after resume, lootboxRngWord at the fulfilled index is nonzero.

**STALL-06 tests (gameover fallback + V37-001 deferred _tryRequestRng):**

13. `test_tryRequestRngGuardBranches()` -- Unit: deploy a fresh game. Before any VRF configuration or after coordinator swap to address(0) (if possible), verify that _tryRequestRng-equivalent call paths return false / do not revert. Since _tryRequestRng is private, test indirectly: after coordinator swap, call advanceGame in a scenario where _gameOverEntropy would invoke _tryRequestRng. The guard checks are: coordinator == address(0), keyHash == bytes32(0), subscriptionId == 0. Verify that the game does not revert and falls through to the fallback timer path.

    NOTE: Testing this directly may require reaching gameover state which is complex. If unreachable, test the coordinator-is-valid post-swap path instead: after a proper swap to a new valid coordinator, _tryRequestRng should succeed (VRF request fires). Verify by checking rngLocked() == true after the advanceGame call.

14. `test_historicalRngFallbackNonzero()` -- Unit: complete 5 days (storing 5 VRF words), then verify that _getHistoricalRngFallback would return a nonzero value. Since _getHistoricalRngFallback is private, verify indirectly by checking that rngWordByDay for days 1-5 are all nonzero (the inputs to the fallback hash).

**STALL-07 tests (dailyIdx timing consistency):**

15. `test_flipDayAlignedWithDailyIdx()` -- Unit: complete day 1 normally. After day 1 completes, verify rngWordForDay(1) != 0. The coinflip result for day 2 (flipDay = 1 + 1 = 2) should be set by the day-1 processing. Check getCoinflipDayResult(2) has nonzero rewardPercent. This proves flipDay = day + 1 alignment.

16. `test_gapDaysSkipResolveRedemptionPeriod()` -- Unit: complete day 1, stall 3 days, swap, resume. Verify gap day coinflip results are populated (from _backfillGapDays calling processCoinflipPayouts) BUT verify that any pending redemption period resolution only fires for the current (post-gap) day, not for each gap day. This is tested by confirming the backfill succeeds (gap day words exist) and the game advances past the gap.

17. `test_wallClockDayAdvancesDuringStall()` -- Unit: complete day 1, record currentDayView(). Warp +3 days without advancing. Verify currentDayView() has advanced (wall-clock). Verify that dailyIdx has NOT advanced (still at 1 from last completed day -- read indirectly by checking rngWordForDay(2) == 0, meaning day 2 was never processed).

All new tests compile and pass with `forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol -vvv`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol -vvv</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/VRFStallEdgeCases.t.sol contains `function test_coordinatorSwapResetsAllVrfState()` with rngLocked/vrfRequestId/rngRequestTime/rngWordCurrent assertions
    - File contains `function test_coordinatorSwapPreservesTotalFlipReversals` (fuzz or unit)
    - File contains `function test_coordinatorSwapClearsMidDayPending()`
    - File contains `function test_zeroSeedUnreachableAfterSwap()` verifying lastLootboxRngWord nonzero preservation
    - File contains `function test_zeroSeedAtGameStart()` testing the fresh-game edge case
    - File contains `function test_tryRequestRngGuardBranches()` or equivalent gameover/VRF-request test covering V37-001
    - File contains `function test_historicalRngFallbackNonzero()` verifying 5 historical words exist
    - File contains `function test_flipDayAlignedWithDailyIdx()` checking getCoinflipDayResult for flipDay = day + 1
    - File contains `function test_gapDaysSkipResolveRedemptionPeriod()` verifying gap day processing without redemption resolution
    - File contains `function test_wallClockDayAdvancesDuringStall()` showing currentDayView advances while rngWordForDay stays 0
    - Full test suite passes: `forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol -vvv` exits 0
    - Total test count is 15-17 functions matching `function test_`
  </acceptance_criteria>
  <done>10 additional tests covering STALL-04 (state reset + preservation), STALL-05 (zero-seed unreachability), STALL-06 (V37-001 guard branches + historical fallback), STALL-07 (flipDay alignment + gap day skip + wall clock divergence) all pass. Combined with Task 1, total is 15-17 tests.</done>
</task>

</tasks>

<verification>
Full test suite green:
```
forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol -vvv
```

Fuzz runs validation (1000 runs):
```
forge test --match-path test/fuzz/VRFStallEdgeCases.t.sol --fuzz-runs 1000 -vvv
```

Gas report for STALL-03:
```
forge test --match-test "test_gapBackfillGas" --gas-report -vvv
```

No regressions in existing tests:
```
forge test --match-path test/fuzz/VRFCore.t.sol -vvv
forge test --match-path test/fuzz/LootboxRngLifecycle.t.sol -vvv
forge test --match-path test/fuzz/StallResilience.t.sol -vvv
```
</verification>

<success_criteria>
- test/fuzz/VRFStallEdgeCases.t.sol exists with 15-17 tests covering all 7 STALL requirements
- All tests pass with default fuzz runs AND with --fuzz-runs 1000
- Gas ceiling tests prove 120-day gap fits within 25M gas (well under 30M block limit)
- Existing VRFCore.t.sol, LootboxRngLifecycle.t.sol, and StallResilience.t.sol suites still green
- V37-001 deferred _tryRequestRng guard branches tested (resolving Phase 63 deferred item)
</success_criteria>

<output>
After completion, create `.planning/phases/65-vrf-stall-edge-cases/65-01-SUMMARY.md`
</output>
