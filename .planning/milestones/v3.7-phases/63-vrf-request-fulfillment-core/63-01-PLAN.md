---
phase: 63-vrf-request-fulfillment-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/VRFCore.t.sol
autonomous: true
requirements:
  - VRFC-01
  - VRFC-02
  - VRFC-03
  - VRFC-04

must_haves:
  truths:
    - "rawFulfillRandomWords never reverts on any code path except unauthorized msg.sender"
    - "Callback gas usage is proven under 300k for daily, mid-day, and stale/silent-return paths"
    - "vrfRequestId is set exactly once per request, matched on fulfillment, cleared after processing"
    - "Retry detection in _finalizeRngRequest correctly distinguishes retries (isRetry=true, no lootboxRngIndex increment) from fresh requests (isRetry=false, lootboxRngIndex++)"
    - "rngLockedFlag prevents requestLootboxRng from executing while daily RNG is in-flight"
    - "12h timeout retry detects stale requests and re-requests without double-incrementing lootboxRngIndex"
  artifacts:
    - path: "test/fuzz/VRFCore.t.sol"
      provides: "Fuzz/unit test suite covering VRFC-01 through VRFC-04"
      min_lines: 200
      contains: "contract VRFCore is DeployProtocol"
  key_links:
    - from: "test/fuzz/VRFCore.t.sol"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "delegatecall through DegenerusGame.sol"
      pattern: "game\\.advanceGame|mockVRF\\.fulfillRandomWords"
    - from: "test/fuzz/VRFCore.t.sol"
      to: "contracts/mocks/MockVRFCoordinator.sol"
      via: "fulfillRandomWords and fulfillRandomWordsRaw calls"
      pattern: "mockVRF\\.(fulfillRandomWords|fulfillRandomWordsRaw)"
---

<objective>
Write Foundry fuzz/unit tests proving VRF request/fulfillment correctness across all four VRFC requirements: callback revert-safety and gas budget (VRFC-01), requestId lifecycle (VRFC-02), rngLockedFlag mutual exclusion (VRFC-03), and 12h timeout retry (VRFC-04).

Purpose: These tests are the primary audit deliverable for Phase 63. Every success criterion must be proven by an automated test.
Output: test/fuzz/VRFCore.t.sol with all tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-vrf-request-fulfillment-core/63-RESEARCH.md

@test/fuzz/VRFLifecycle.t.sol
@test/fuzz/StallResilience.t.sol
@test/fuzz/helpers/DeployProtocol.sol
@test/fuzz/helpers/VRFHandler.sol
@contracts/mocks/MockVRFCoordinator.sol

<interfaces>
<!-- Key contracts and patterns the executor needs -->

From test/fuzz/helpers/DeployProtocol.sol:
- Inherit `DeployProtocol`, call `_deployProtocol()` in setUp()
- Provides: `mockVRF` (MockVRFCoordinator), `game` (DegenerusGame), `admin` (DegenerusAdmin)
- Deploy timestamp: 86400 (vm.warp(86400) in _deployProtocol)

From test/fuzz/helpers/VRFHandler.sol:
- `fulfillVrf(uint256 randomWord)` -- wraps mockVRF.fulfillRandomWords for fuzzing
- `ghost_vrfFulfillments` -- tracks successful fulfillments
- `warpPastVrfTimeout()` -- vm.warp(block.timestamp + 18 hours + 1)

From contracts/mocks/MockVRFCoordinator.sol:
- `fulfillRandomWords(uint256 requestId, uint256 randomWord)` -- normal fulfillment (reverts if callback reverts)
- `fulfillRandomWordsRaw(uint256 requestId, address consumer, uint256 randomWord)` -- raw fulfillment bypassing request tracking
- `lastRequestId()` -- returns the most recent request ID
- `fundSubscription(uint256 subId, uint96 amount)` -- fund LINK balance for lootbox RNG
- `pendingRequests(uint256 id)` -- returns (subId, consumer, fulfilled)

From contracts/modules/DegenerusGameAdvanceModule.sol:
- rawFulfillRandomWords(uint256 requestId, uint256[] calldata randomWords) -- lines 1402-1423
  - Only revert: `if (msg.sender != address(vrfCoordinator)) revert E()`
  - Silent return: `if (requestId != vrfRequestId || rngWordCurrent != 0) return`
  - Daily branch (rngLockedFlag==true): `rngWordCurrent = word`
  - Mid-day branch (rngLockedFlag==false): stores to lootboxRngWordByIndex[index], clears vrfRequestId and rngRequestTime
- _finalizeRngRequest(bool, uint24, uint256) -- lines 1267-1299
  - isRetry = vrfRequestId != 0 && rngRequestTime != 0 && rngWordCurrent == 0
  - Fresh: lootboxRngIndex++, lootboxRngPendingEth=0, lootboxRngPendingBurnie=0
  - Both: vrfRequestId = requestId, rngWordCurrent = 0, rngRequestTime = uint48(block.timestamp), rngLockedFlag = true
- requestLootboxRng() -- lines 679-744
  - Guards: rngLockedFlag, 15-min window, rngWordByDay[currentDay]!=0, rngRequestTime==0, LINK>=40, pending threshold
  - Sets: lootboxRngIndex++, vrfRequestId, rngRequestTime
- _unlockRng(uint48 day) -- lines 1372-1378
  - Sets: dailyIdx=day, rngLockedFlag=false, rngWordCurrent=0, vrfRequestId=0, rngRequestTime=0
- rngGate timeout at lines 831-837:
  - if elapsed >= 12 hours: _requestRng(isTicketJackpotDay, lvl); return 1;
  - else: revert RngNotReady()

From test/fuzz/StallResilience.t.sol (helper patterns to reuse):
- `_completeDay(uint256 vrfWord)` -- advanceGame + fulfill + loop until unlocked
- `_doCoordinatorSwap()` -- deploy new mock VRF, call updateVrfCoordinatorAndSub via admin prank

From DegenerusGame.sol (view functions available):
- `game.rngLocked()` -- returns rngLockedFlag
- `game.level()` -- returns current level
- `game.advanceGame()` -- main entry point
- `game.requestLootboxRng()` -- mid-day lootbox RNG request
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Callback revert-safety, gas budget, and requestId lifecycle tests (VRFC-01 + VRFC-02)</name>
  <files>test/fuzz/VRFCore.t.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 1267-1299 for _finalizeRngRequest, lines 1402-1423 for rawFulfillRandomWords)
    - contracts/DegenerusGame.sol (lines 2001-2015 for delegatecall proxy)
    - contracts/mocks/MockVRFCoordinator.sol (full file -- understand fulfillRandomWords vs fulfillRandomWordsRaw)
    - test/fuzz/VRFLifecycle.t.sol (existing test patterns)
    - test/fuzz/StallResilience.t.sol (helper patterns: _completeDay, _doCoordinatorSwap)
    - test/fuzz/helpers/DeployProtocol.sol (setUp pattern)
    - test/fuzz/helpers/VRFHandler.sol (VRF handler interface)
  </read_first>
  <behavior>
    VRFC-01 tests:
    - test_callbackNeverReverts_daily(uint256 randomWord): Trigger daily VRF request via advanceGame, fulfill with fuzzed randomWord -- must not revert. Verify rngWordCurrent is set to max(word, 1).
    - test_callbackNeverReverts_staleId(uint256 staleId, uint256 randomWord): Fulfill with a requestId that does not match vrfRequestId -- must silently return (no revert, no state change). Use fulfillRandomWordsRaw with staleId != mockVRF.lastRequestId().
    - test_callbackNeverReverts_duplicateFulfillment(uint256 randomWord): Fulfill the same requestId twice -- second call must silently return (rngWordCurrent already set, so condition `rngWordCurrent != 0` triggers return).
    - test_callbackReverts_unauthorizedSender(): Call game.rawFulfillRandomWords directly from a non-coordinator address -- must revert.
    - test_callbackGasBudget_daily(): Trigger daily callback path, measure gas via gasleft() before/after fulfillment, assert < 300_000.
    - test_callbackGasBudget_midday(): Trigger mid-day callback path (requestLootboxRng flow), measure gas, assert < 300_000.
    - test_callbackZeroGuard(uint256 randomWord): When randomWord == 0, rngWordCurrent must be set to 1 (not 0).

    VRFC-02 tests:
    - test_vrfRequestIdLifecycle_dailyFreshRequest(): After advanceGame triggers VRF, vrfRequestId == mockVRF.lastRequestId(). After fulfillment + processing + _unlockRng, vrfRequestId == 0.
    - test_vrfRequestIdLifecycle_middayRequest(): After requestLootboxRng, vrfRequestId == mockVRF.lastRequestId(). After mid-day fulfillment, vrfRequestId == 0.
    - test_retryDetection_fresh(): On fresh daily request (no prior pending), isRetry is false, so lootboxRngIndex increments by 1.
    - test_retryDetection_timeout(): After 12h timeout + retry via advanceGame, lootboxRngIndex does NOT increment again (stays at value from original request).
    - test_retryDetection_fuzz(uint256 word1, uint256 word2): Fuzz the retry scenario -- request, wait 13h, retry, fulfill -- assert lootboxRngIndex unchanged between first request and post-retry.
  </behavior>
  <action>
    Create test/fuzz/VRFCore.t.sol with contract VRFCore extending DeployProtocol.

    setUp():
    - Call _deployProtocol()
    - Create vrfHandler = new VRFHandler(mockVRF, game)

    Helper _completeDay(uint256 vrfWord):
    - game.advanceGame() -> mockVRF.lastRequestId() -> mockVRF.fulfillRandomWords(reqId, vrfWord) -> loop advanceGame until !game.rngLocked()
    - Copy pattern from StallResilience.t.sol

    Helper _setupForMidDayRng():
    - Must satisfy requestLootboxRng guards:
      1. rngLockedFlag == false (complete a day first)
      2. rngWordByDay[currentDay] != 0 (day must be advanced)
      3. rngRequestTime == 0 (no pending request)
      4. LINK balance >= 40e18 (fund subscription via mockVRF.fundSubscription)
      5. lootboxRngPendingEth or lootboxRngPendingBurnie > 0 (need purchases)
    - Approach: _completeDay with day 1, then make a purchase on the new day to create pending lootbox ETH, then fund the VRF subscription with 100e18 LINK
    - Return the current timestamp for boundary checks

    For gas measurement tests (VRFC-01):
    - Use a pattern like: `uint256 gasBefore = gasleft(); mockVRF.fulfillRandomWords(reqId, word); uint256 gasUsed = gasBefore - gasleft();`
    - NOTE: This measures total gas including MockVRFCoordinator overhead. The important thing is the overall callback path is well under 300k. The research already confirmed ~33k (daily) and ~47k (mid-day) at the opcode level, but a forge --gas-report run will be the definitive proof.
    - Alternative: just run `forge test --match-test test_callbackGas --gas-report -vvv` and inspect the output.

    For unauthorized sender test (VRFC-01):
    - Use vm.prank(address(0xdead)) to set msg.sender to non-coordinator
    - Call game.rawFulfillRandomWords(requestId, words) directly
    - vm.expectRevert() before the call

    For requestId lifecycle tests (VRFC-02):
    - Read vrfRequestId via the storage slot directly: `uint256 storedReqId = uint256(vm.load(address(game), bytes32(uint256(6))));` (Slot 6 per research)
    - Read rngRequestTime from Slot 0 bytes 12-17: extract via vm.load + shifting
    - Alternatively, if game exposes view functions, use those. Check DegenerusGame.sol for public getters.

    For retry detection tests (VRFC-02 + VRFC-04):
    - Day 1: _completeDay(0xDEAD0001) to establish baseline
    - Day 2: vm.warp + 1 days, game.advanceGame() triggers VRF request, record lootboxRngIndex
    - Do NOT fulfill VRF
    - vm.warp + 13 hours (past 12h timeout)
    - game.advanceGame() triggers retry via rngGate timeout path
    - Assert lootboxRngIndex unchanged (retry detection worked)
    - Then fulfill the new VRF request and complete the day

    IMPORTANT: The game uses a simulated day index based on timestamp. Deploy timestamp is 86400. Day boundaries are at multiples of 86400 from the game's epoch. Use vm.warp carefully to ensure day transitions happen correctly.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract VRFCore --match-test "test_callback|test_vrfRequestId|test_retryDetection" -vvv</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/VRFCore.t.sol exists and contains `contract VRFCore is DeployProtocol`
    - File contains at least 7 test functions matching `test_callback*` for VRFC-01
    - File contains at least 5 test functions matching `test_vrfRequestId*` or `test_retryDetection*` for VRFC-02
    - All tests pass: `forge test --match-contract VRFCore -vvv` exits 0
    - test_callbackReverts_unauthorizedSender includes `vm.expectRevert()` and calls `game.rawFulfillRandomWords` with a non-coordinator prank
    - test_callbackZeroGuard verifies word==0 produces stored value of 1
    - test_retryDetection tests verify lootboxRngIndex does NOT increment on retry
    - Gas budget tests run without failure (gas measurement present in test or verified via --gas-report)
  </acceptance_criteria>
  <done>
    All VRFC-01 tests prove: callback never reverts except unauthorized sender, zero-guard works, gas is under budget.
    All VRFC-02 tests prove: requestId set on request, cleared after processing, retry detection prevents double-increment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mutual exclusion and timeout retry tests (VRFC-03 + VRFC-04)</name>
  <files>test/fuzz/VRFCore.t.sol</files>
  <read_first>
    - test/fuzz/VRFCore.t.sol (Task 1 output -- current state of the file)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 679-744 for requestLootboxRng, lines 831-837 for rngGate timeout, lines 1340-1367 for updateVrfCoordinatorAndSub)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 770-828 for rngGate normal flow)
  </read_first>
  <behavior>
    VRFC-03 tests:
    - test_rngLocked_blocksMidDayRequest(): During daily RNG (rngLockedFlag==true), requestLootboxRng must revert with RngLocked.
    - test_midDayRequest_doesNotBlockDaily(): After requestLootboxRng fires (rngLockedFlag stays false), advanceGame on next day can still request daily RNG.
    - test_midDayFulfillment_clearsState(): After mid-day VRF fulfills, vrfRequestId and rngRequestTime are both 0, allowing daily flow to proceed cleanly.
    - test_preResetWindow_blocksMidDay(): requestLootboxRng within 15 minutes of day boundary must revert.
    - test_coordinatorSwap_clearsRngLocked(): After updateVrfCoordinatorAndSub, rngLockedFlag is false, vrfRequestId is 0, rngRequestTime is 0.

    VRFC-04 tests:
    - test_timeoutRetry_12h(): After exactly 12 hours, advanceGame triggers _requestRng (retry), not RngNotReady revert.
    - test_noRetry_before12h(): Before 12 hours, advanceGame reverts with RngNotReady.
    - test_timeoutRetry_staleWordDiscarded(uint256 word1, uint256 word2): After retry overwrites vrfRequestId, old VRF fulfillment (with old requestId) is silently discarded. New fulfillment (with new requestId) succeeds.
    - test_timeoutRetry_lootboxIndexPreserved_fuzz(uint256 word): Fuzz -- trigger request, timeout, retry, fulfill -- lootboxRngIndex never double-increments across any word value.
    - test_crossDayStaleWord(): VRF word arrives from previous day's request. rngGate detects requestDay < day, redirects word to lootbox via _finalizeLootboxRng, requests fresh daily RNG.
  </behavior>
  <action>
    Add tests to the existing VRFCore contract in test/fuzz/VRFCore.t.sol (appending after Task 1 tests).

    For VRFC-03 mutual exclusion tests:
    - test_rngLocked_blocksMidDayRequest: advanceGame to trigger daily VRF (rngLocked=true), then call requestLootboxRng -- must revert. Use vm.expectRevert().
    - test_midDayRequest_doesNotBlockDaily: Complete day 1, set up mid-day RNG on day 2 (using _setupForMidDayRng helper from Task 1), do NOT fulfill mid-day VRF. Warp to day 3. advanceGame should either: (a) wait for VRF (revert RngNotReady since rngRequestTime != 0 and < 12h), OR (b) after 12h timeout, request new daily RNG. Test the 12h path by warping 13h past the mid-day request time, then asserting advanceGame does not revert (it triggers _requestRng which sets rngLockedFlag=true).
    - test_midDayFulfillment_clearsState: After mid-day VRF fulfills, read vrfRequestId (slot 6) and verify == 0. Read rngRequestTime from slot 0 and verify == 0.
    - test_preResetWindow_blocksMidDay: Complete day 1, warp to 15 minutes before day boundary, ensure all other requestLootboxRng guards are satisfied, call requestLootboxRng -- must revert.
    - test_coordinatorSwap_clearsRngLocked: Trigger daily VRF (rngLocked=true), then call updateVrfCoordinatorAndSub via admin prank, assert rngLocked == false. Pattern from StallResilience._doCoordinatorSwap.

    For VRFC-04 timeout retry tests:
    - test_timeoutRetry_12h: Day 2 advanceGame fires VRF request. Do NOT fulfill. Warp to exactly rngRequestTime + 12 hours. advanceGame should call _requestRng (not revert). Assert rngLocked == true (new request in flight). Record lootboxRngIndex before and after -- must be equal (retry, not fresh).
    - test_noRetry_before12h: Same setup but warp only 11h59m. advanceGame should revert with RngNotReady.
    - test_timeoutRetry_staleWordDiscarded: Request on day 2, wait 13h, retry fires (new vrfRequestId). Then fulfill with OLD requestId via fulfillRandomWordsRaw -- should silently discard (requestId mismatch). Then fulfill with NEW requestId -- should succeed and store word.
    - test_timeoutRetry_lootboxIndexPreserved_fuzz: Fuzz over random words. Complete day 1. Day 2: advanceGame (records index). Wait 13h. advanceGame (retry). Assert index unchanged. Fulfill new request. Complete processing. Assert index unchanged from post-first-request value.
    - test_crossDayStaleWord: Complete day 1. Day 2: advanceGame fires VRF request. Fulfill with a VRF word. But warp PAST day boundary before calling advanceGame to process. rngGate should detect requestDay < day and redirect word to lootbox, then request fresh daily RNG.

    IMPORTANT NOTES:
    - Reading storage slots: Use `vm.load(address(game), bytes32(uint256(SLOT)))` for direct state inspection.
    - vrfRequestId is at slot 6.
    - rngRequestTime is packed in slot 0 at bytes 12-17 (uint48). Extract: `uint48(uint256(vm.load(address(game), bytes32(uint256(0)))) >> 96)` -- verify the bit offset by checking DegenerusGameStorage.sol field ordering.
    - For the 15-minute window test: the game's day boundary is at multiples of 86400 from epoch. At deploy, timestamp=86400 (day 1 start). Day 2 starts at 172800. So 15 min before day 2 boundary = 172800 - 900 = 171900.
    - Use `vm.prank(address(admin))` for updateVrfCoordinatorAndSub calls.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract VRFCore --match-test "test_rngLocked|test_midDay|test_preReset|test_coordinatorSwap|test_timeoutRetry|test_noRetry|test_crossDay" -vvv</automated>
  </verify>
  <acceptance_criteria>
    - test/fuzz/VRFCore.t.sol contains at least 5 test functions matching `test_rngLocked*|test_midDay*|test_preReset*|test_coordinatorSwap*` for VRFC-03
    - test/fuzz/VRFCore.t.sol contains at least 5 test functions matching `test_timeoutRetry*|test_noRetry*|test_crossDay*` for VRFC-04
    - All new tests pass: `forge test --match-contract VRFCore -vvv` exits 0
    - test_rngLocked_blocksMidDayRequest includes `vm.expectRevert()` before `game.requestLootboxRng()`
    - test_timeoutRetry tests assert lootboxRngIndex is unchanged between pre-retry and post-retry
    - test_noRetry_before12h includes `vm.expectRevert()` proving RngNotReady is thrown before 12h
    - test_coordinatorSwap_clearsRngLocked asserts `game.rngLocked()` returns false after swap
    - Full suite: `forge test --match-contract VRFCore --fuzz-runs 1000 -vvv` exits 0
  </acceptance_criteria>
  <done>
    All VRFC-03 tests prove: rngLockedFlag prevents daily/mid-day collision, no bypass path exists, coordinator swap clears lock.
    All VRFC-04 tests prove: 12h timeout retry fires correctly, stale fulfillments discarded, lootboxRngIndex never double-increments, cross-day stale words redirected properly.
    Full VRFCore test suite (VRFC-01 through VRFC-04) passes with 1000 fuzz runs.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `forge test --match-contract VRFCore -vvv` -- all tests green
2. `forge test --match-contract VRFCore --fuzz-runs 1000 -vvv` -- all fuzz tests green at 1000 runs
3. `forge test --match-contract VRFCore --gas-report -vvv` -- gas report shows callback functions under 300k
4. Verify test count: at least 12 distinct test functions covering all 4 requirements
</verification>

<success_criteria>
- test/fuzz/VRFCore.t.sol exists with at least 12 test functions
- All 4 requirements (VRFC-01 through VRFC-04) have dedicated test coverage
- `forge test --match-contract VRFCore --fuzz-runs 1000 -vvv` exits 0
- No test relies on hardcoded magic numbers without documented rationale
- Gas budget proven: callback paths confirmed under 300k gas
</success_criteria>

<output>
After completion, create `.planning/phases/63-vrf-request-fulfillment-core/63-01-SUMMARY.md`
</output>
