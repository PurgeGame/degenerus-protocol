---
phase: 344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["344-02", "344-03"]
files_modified:
  - contracts/AfKing.sol
autonomous: false
requirements: [DECUSTODY-01, DECUSTODY-02, DECUSTODY-03, AUTOBUY-05]
must_haves:
  truths:
    - "AfKing holds NO ETH — _poolOf / receive / deposit / depositFor / withdraw / poolOf are deleted"
    - "subscribe stays payable and FORWARDS msg.value → game.depositKeeperFunding{value}(subscriber) (A2)"
    - "the OPEN-E consent gate (:403-408) and src resolution (:686) are UNCHANGED"
    - "each batch slice sets funder: src (D-01); the funding-skip reads keeperFunding[src] via the extended snapshot + keeperFundingOf(src) fallback (D-MR-01)"
    - "the batch call becomes the non-value GAME.batchPurchase(buys); the local CEI _poolOf debit is dropped"
    - "the orphaned Deposited event is removed (D-344-08)"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "the de-custodied AfKing — no ETH custody, funder-wired batch, non-value call"
      contains: "game.depositKeeperFunding"
  key_links:
    - from: "AfKing.subscribe msg.value credit"
      to: "game.depositKeeperFunding{value: msg.value}(subscriber)"
      via: "Decision A2 forward (AfKing never retains ETH)"
      pattern: "depositKeeperFunding\\{value:"
    - from: "AfKing BatchBuy build site"
      to: "funder: src"
      via: "D-01 funder = the resolved src"
      pattern: "funder: src"
    - from: "AfKing batched call"
      to: "GAME.batchPurchase(buys)"
      via: "non-value call (no {value:})"
      pattern: "GAME\\.batchPurchase\\(buys\\)"
---

<objective>
Step 4 of the producer-before-consumer edit-order map (343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 4):
de-custody AfKing — it consumes the new Game ABI declared in 344-03. Delete every ETH-custody symbol; forward
`subscribe`'s `msg.value` to the Game's `depositKeeperFunding`; set `funder: src` per batch slice (D-01); rewire
the funding-skip to read `keeperFunding[src]` (extended snapshot + `keeperFundingOf(src)` fallback for the
OPEN-E `src != player` slice — D-MR-01); drop the local CEI `_poolOf` debit; flip the batch call to non-value;
remove the orphaned `Deposited` event (D-344-08).

⚠ D-06 ORDERING NOTE: `AfKing.withdraw` (`:328`) and `AfKing.poolOf` (`:492`) are orphaned ONLY after their two
callers — the v48 recovery legs in `DegenerusVault` + `StakedDegenerusStonk` — are removed (344-05). In this
SINGLE batched diff the ordering is satisfied by AUTHORING ORDER: 344-05's caller removal is part of the same
diff and is authored before/with these deletions. No intermediate file state ever deletes the views while a
recovery leg still calls them. The build is only verified after ALL plans (incl. 344-05) land (344-05 owns the
`forge build` gate).

Purpose: AfKing becomes a pure subscription/keeper-routing contract with NO ETH custody. The de-custody is a
PURE DELETE — no NatSpec "AfKing holds no ETH" line, no defensive `balance == 0` assert (D-344-02); rely on the
structural no-receive guarantee (after this plan AfKing has no payable path other than `subscribe`, which
forwards). Stale `_poolOf`-referencing comments are REMOVED, not replaced.

Output: Edits applied to `contracts/AfKing.sol`. Part of the single batched 344 diff. NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor NEVER runs `git commit` / `git add
contracts/` (hook-enforced). Orchestrator commits post-approval (344-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md
@.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-CONTEXT.md

<interfaces>
<!-- Re-pinned anchors inherited from 344-01 Task 1. SPEC-attested starting refs. -->

Current AfKing.sol custody + wiring anchors (attested @ 83a84431; re-pin before editing):
- `_poolOf` mapping (slot 0) `:214` — `mapping(address => uint256) private _poolOf; // slot 0`
- `receive()` `:298`, `deposit()` `:305`, `depositFor()` `:314`, `withdraw()` `:328` (CEI debit `:334`)
- `subscribe` def `:381`; OPEN-E consent gate `if(` at `:403`, revert `NotApproved()` at `:408` (UNCHANGED)
- `subscribe` msg.value credit `:413` — `_poolOf[subscriber] += msg.value;` (inside `if (msg.value > 0) {` `:412`) → FORWARD to game
- `poolOf()` view `:492` (return `:493`) — DELETED ENTIRELY (D-05; no forwarding view)
- `src` resolution `:686` — `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` (UNCHANGED)
- funding-skip gate `:695` — `if (_poolOf[src] < ethValue) {` → reads keeperFunding[src]
- VAULT/SDGNRS exemption `:696` — keyed on `player` (UNCHANGED — D-01: exemption stays on player)
- CEI debit `:719` — `_poolOf[src] -= ethValue;` → DROP (debit moved to the Game's batchPurchase)
- BatchBuy build site `:726` — `buys[batchLen] = BatchBuy({ … });` → set `funder: src`
- batched call `:768` — `GAME.batchPurchase{value: totalValue}(buys);` → `GAME.batchPurchase(buys);`
- per-player snapshot fallback `:809` — `(, , uint256[] memory cl) = GAME.keeperSnapshot(snap);` (the D-MR-01 mirror; now also reads keeperFundings)
- `Deposited` event decl `:175`; emit sites `:301, :308, :318, :414` (all in kill-set fns + the rewired credit) → REMOVE (D-344-08)
- stale `_poolOf` comments `:84, :117, :143, :193, :370, :447`; `sum(_poolOf) <= address(this).balance` invariant doc `:117` → REMOVE (not replace)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: subscribe forwards msg.value to the Game + funder=src wiring + funding-skip reads keeperFunding[src] (D-MR-01) + non-value call + drop local CEI debit</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (re-pinned: subscribe credit `:413` inside `if (msg.value > 0)` `:412`; OPEN-E gate `:403-408`; `src` resolution `:686`; funding-skip `:695`; VAULT/SDGNRS exemption `:696`; CEI debit `:719`; BatchBuy build `:726`; batched call `:768`; snapshot fallback `:809`)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 4 (4a subscribe forward A2, 4b funder: src, 4c funding-skip + D-MR-01, 4d drop local debit, 4e non-value call) + Section 3.2 (D-MR-01 src carve-out)
    - 343-SOLVENCY-PROOF.md Section D (OPEN-E 4-protection carry-over verbatim; the D-01 funder identity)
    - 344-CONTEXT.md D-01 (funder = src), DECUSTODY-02 (A2 forward), AUTOBUY-05 (extended snapshot, non-value call)
  </read_first>
  <action>
    Rewire AfKing's keeper path to consume the Game ledger (the OPEN-E consent gate `:403-408` and `src`
    resolution `:686` stay UNCHANGED):
    (a) `subscribe` msg.value credit (re-pinned `:413`, inside `if (msg.value > 0)` `:412`): replace
        `_poolOf[subscriber] += msg.value;` with `game.depositKeeperFunding{value: msg.value}(subscriber);` (A2 —
        AfKing forwards, never retains ETH). `subscribe` stays `payable`.
    (b) BatchBuy build site (re-pinned `:726`): set `funder: src` per slice (D-01), where `src` is the existing
        `:686` resolution (`src = sub.fundingSource == address(0) ? player : sub.fundingSource`). Keep `player:
        player` (the beneficiary) as today. Both fields populated.
    (c) Funding-skip gate (re-pinned `:695`): read `keeperFunding[src]` instead of `_poolOf[src]`. The COMMON path
        (`src == player`) reads it via the extended `keeperSnapshot` per-player `keeperFundings` return (ONE
        staticcall per player — GASOPT-03 / AUTOBUY-05 preserved). The OPEN-E `src != player` slice needs ONE
        EXTRA `game.keeperFundingOf(src)` fallback staticcall (D-MR-01), mirroring the existing per-player
        snapshot fallback at `:809`. The two-tier branching is byte-identical (VAULT/SDGNRS exempt-skip; NORMAL
        sub auto-pause). ⚠ The VAULT/SDGNRS exemption (`:696`) STAYS keyed on the un-spoofable `player` — do NOT
        move it to `src`/`funder`.
    (d) Drop the local CEI debit (re-pinned `:719`): DELETE `_poolOf[src] -= ethValue;` — the debit moves to the
        Game's `batchPurchase` (`keeperFunding[b.funder] -= ev`, 344-02).
    (e) Batched call (re-pinned `:768`): `GAME.batchPurchase{value: totalValue}(buys);` →
        `GAME.batchPurchase(buys);` (non-value call; the `totalValue` accumulation may be removed if it now has no
        consumer — confirm via grep before removing).
    (f) The per-player snapshot fallback (re-pinned `:809`) now also destructures the new `keeperFundings` return
        element from `GAME.keeperSnapshot(snap)` so the funding-skip can read it.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "game.depositKeeperFunding{value: msg.value}(subscriber)" contracts/AfKing.sol | grep -qx 1 && grep -c "funder: src" contracts/AfKing.sol | grep -qx 1 && grep -c "GAME.batchPurchase(buys)" contracts/AfKing.sol | grep -qx 1 && grep -c "GAME.batchPurchase{value:" contracts/AfKing.sol | grep -qx 0 && grep -c "_poolOf\[src\] -= ethValue" contracts/AfKing.sol | grep -qx 0 && grep -c "keeperFundingOf(src)" contracts/AfKing.sol | grep -q "[1-9]" && echo "REWIRED A2 FUNDER-SRC NON-VALUE MR-01"</automated>
  </verify>
  <acceptance_criteria>
    - `subscribe` forwards `msg.value` via `game.depositKeeperFunding{value: msg.value}(subscriber)` and stays `payable`
    - The OPEN-E consent gate (`:403-408`) and `src` resolution (`:686`) are byte-unchanged
    - The BatchBuy build sets `funder: src`; the VAULT/SDGNRS exemption stays keyed on `player`
    - The funding-skip reads `keeperFunding[src]` (extended snapshot for `src == player` + `keeperFundingOf(src)` fallback for `src != player`, D-MR-01); two-tier branching byte-identical
    - The local CEI debit `_poolOf[src] -= ethValue;` is removed
    - The batch call is the non-value `GAME.batchPurchase(buys)` (the `{value:}` form appears 0 times)
  </acceptance_criteria>
  <done>subscribe forwards msg.value via depositKeeperFunding (stays payable); funder: src is set per slice with the exemption still on player; the funding-skip reads keeperFunding[src] (snapshot + keeperFundingOf(src) fallback); the local CEI debit is dropped; the batch call is non-value; the OPEN-E gate + src resolution are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PURE-DELETE the custody surface (_poolOf, receive/deposit/depositFor/withdraw, poolOf) + remove the Deposited event + stale comments</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (re-pinned: `_poolOf` `:214`; `receive()` `:298`; `deposit()` `:305`; `depositFor()` `:314`; `withdraw()` `:328`; `poolOf()` `:492`; `Deposited` decl `:175` + emits `:301/:308/:318/:414`; stale comments `:84/:117/:143/:193/:370/:447`)
    - 343-CLEANUP-INVENTORY.md Section 2 (the 14-item kill-set: #1-#7, #12-#14) + Section 4 (D-05: poolOf DELETED ENTIRELY) + Section 3 (D-06 gate — withdraw/poolOf orphaned only after 344-05's caller removal)
    - 344-CONTEXT.md D-344-02 (PURE DELETE, no added comment / no balance==0 assert; stale comments removed not replaced) + D-344-08 (Deposited removed; SubscriptionUpdated UNCHANGED)
  </read_first>
  <action>
    PURE-DELETE the AfKing ETH-custody surface (D-344-02 — delete cleanly; do NOT add a "AfKing holds no ETH"
    NatSpec line and do NOT add a defensive `balance == 0` assert):
    - DELETE `_poolOf` mapping (re-pinned `:214`, slot 0).
    - DELETE `receive()` (`:298`), `deposit()` (`:305`), `depositFor()` (`:314`), `withdraw()` (`:328`).
    - DELETE `poolOf()` view (`:492`) ENTIRELY — D-05; NO forwarding view (canonical balance source is now
      `game.keeperFundingOf`).
    - REMOVE the `Deposited` event decl (`:175`) and ALL its emit sites (`:301/:308/:318` are inside the deleted
      fns; `:414` was the subscribe credit, now rewired to the Game's `KeeperFunded`). ⚠ The re-grep (344-01) and
      Task 1 must confirm `Deposited` has NO non-orphaned emit; if the author finds a live, non-de-custody
      `Deposited` emit, SURFACE it rather than deleting blind (D-344-08). The existing `SubscriptionUpdated` event
      (with indexed `fundingSource`) STAYS UNCHANGED.
    - REMOVE (not replace) the stale `_poolOf`-referencing comments (`:84/:117/:143/:193/:370/:447`) and the
      `sum(_poolOf) <= address(this).balance` invariant doc (`:117`).
    After this plan, AfKing has no `_poolOf`, no payable path except the forwarding `subscribe`, and no balance
    view. ⚠ D-06: `withdraw`/`poolOf` are deleted here, but their two callers (the v48 recovery legs) are removed
    in 344-05 within the SAME batched diff — do NOT run `forge build` in this plan (it would fail on the dangling
    references until 344-05 lands; the build gate is 344-05's, after all edits).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "_poolOf" contracts/AfKing.sol | grep -qx 0 && grep -c "function poolOf(" contracts/AfKing.sol | grep -qx 0 && grep -c "function withdraw(" contracts/AfKing.sol | grep -qx 0 && grep -c "function depositFor(" contracts/AfKing.sol | grep -qx 0 && grep -c "event Deposited" contracts/AfKing.sol | grep -qx 0 && grep -c "sum(_poolOf)" contracts/AfKing.sol | grep -qx 0 && grep -c "event SubscriptionUpdated" contracts/AfKing.sol | grep -q "[1-9]" && echo "CUSTODY-DELETED DEPOSITED-GONE SUBUPDATE-KEPT"</automated>
  </verify>
  <acceptance_criteria>
    - `_poolOf` appears 0 times in AfKing.sol (mapping + all references gone)
    - `receive()`, `deposit()`, `depositFor()`, `withdraw()`, `poolOf()` are all deleted (no forwarding `poolOf`)
    - The `Deposited` event decl + all its emit sites are removed; `SubscriptionUpdated` is unchanged
    - The `sum(_poolOf) <= address(this).balance` invariant doc and all stale `_poolOf` comments are removed (not replaced)
    - NO "AfKing holds no ETH" NatSpec line and NO `balance == 0` assert were added (D-344-02 pure delete)
    - No `forge build` run in this plan (the D-06 dangling references resolve only after 344-05)
  </acceptance_criteria>
  <done>AfKing holds no ETH — _poolOf + all custody fns (receive/deposit/depositFor/withdraw) + poolOf are deleted, the Deposited event + stale _poolOf comments removed (not replaced), SubscriptionUpdated unchanged, no defensive comment/assert added, and no forge build run (D-06 resolves in 344-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscriber/operator → AfKing.subscribe (payable) | The only remaining payable path; it must forward, never retain. |
| AfKing → Game (depositKeeperFunding / batchPurchase / keeperSnapshot / keeperFundingOf) | AfKing now holds no ETH; all custody is in the Game. |
| OPEN-E operator funding | `src != player` slice — the consent gate + funder-keyed accounting must carry over verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-04-CUST | Elevation/Custody | AfKing ETH custody removal | mitigate | PURE DELETE of `_poolOf`/receive/deposit/withdraw — AfKing has no payable path except the forwarding `subscribe`; plain ETH sends now revert structurally (no `receive()`). Task 2 verify asserts `_poolOf` count 0. (D-344-02) |
| T-344-04-OE | Spoofing/Trust-boundary | OPEN-E operator funding | mitigate | The consent gate (`:403-408`) + `src` resolution (`:686`) are byte-unchanged; `funder: src` keys the Game debit to the operator's bucket; the VAULT/SDGNRS exemption stays on un-spoofable `player`. The 4-protection disposition carries over verbatim. (343-SOLVENCY-PROOF Section D / open-e-operator-approval-trust-boundary) |
| T-344-04-MR | Information Disclosure / Accounting | D-MR-01 src read | mitigate | The `src != player` slice reads `keeperFunding[src]` via the extra `keeperFundingOf(src)` staticcall (mirror `:809`); the common path stays single-staticcall. The funding-skip reads the correct bucket. |
| T-344-04-D6 | DoS (broken build / dangling ref) | withdraw/poolOf deletion | mitigate | D-06 authoring order: the v48 recovery-leg callers are removed in 344-05 within the same batched diff; no intermediate state holds a dangling reference. The build is verified only after 344-05. (343-CLEANUP-INVENTORY Section 3 / T-343-07) |
| T-344-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits only). |
</threat_model>

<verification>
- AfKing holds no ETH (`_poolOf` count 0; all custody fns + the bare `receive()` deleted)
- `subscribe` forwards `msg.value` to `game.depositKeeperFunding` and stays payable
- `funder: src` set per slice; the funding-skip reads `keeperFunding[src]` (snapshot + D-MR-01 fallback); exemption on `player`
- The batch call is non-value; the local CEI debit is dropped
- The `Deposited` event is removed; `SubscriptionUpdated` unchanged
- The OPEN-E consent gate + `src` resolution are byte-unchanged
- No `forge build` in this plan (D-06 references resolve in 344-05); NO executor commit
</verification>

<success_criteria>
- AfKing is fully de-custodied + funder-wired + non-value-called
- The OPEN-E 4-protection carries over verbatim
- Held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-04-SUMMARY.md` when done.
</output>
