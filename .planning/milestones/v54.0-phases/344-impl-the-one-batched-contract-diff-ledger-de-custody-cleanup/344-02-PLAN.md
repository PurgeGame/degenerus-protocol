---
phase: 344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["344-01"]
files_modified:
  - contracts/DegenerusGame.sol
autonomous: false
requirements: [LEDGER-03, LEDGER-04, LEDGER-05, AUTOBUY-01, AUTOBUY-02, AUTOBUY-03, AUTOBUY-04, GAMEOVER-01, GAMEOVER-02]
must_haves:
  truths:
    - "depositKeeperFunding(address) payable credits keeperFunding[player] AND claimablePool in tandem"
    - "withdrawKeeperFunding(uint256) is un-brickable CEI with the GO_SWEPT guard as LINE 1"
    - "batchPurchase is NON-payable and debits keeperFunding[b.funder] (NOT b.player) + claimablePool per slice"
    - "_claimWinningsInternal post-gameOver ALSO pays keeperFunding[caller] (Decision B), zeroing both buckets"
    - "keeperSnapshot returns the per-player keeperFunding alongside the existing tuple (one staticcall/player)"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "the 6 Game-side ledger edits + extended snapshot + invariant comment"
      contains: "function depositKeeperFunding"
  key_links:
    - from: "batchPurchase per-slice debit"
      to: "keeperFunding[b.funder]"
      via: "the D-01 funder-keyed debit (NOT b.player)"
      pattern: "keeperFunding\\[b\\.funder\\]"
    - from: "withdrawKeeperFunding"
      to: "GO_SWEPT latch"
      via: "guard as the FIRST statement, before any debit"
      pattern: "_goRead\\(GO_SWEPT_SHIFT, GO_SWEPT_MASK\\)"
    - from: "depositKeeperFunding / withdrawKeeperFunding / batchPurchase / _claimWinningsInternal"
      to: "claimablePool"
      via: "tandem mutation (D-CF-03: no new aggregate)"
      pattern: "claimablePool [+-]="
---

<objective>
Step 2 of the producer-before-consumer edit-order map (343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 2):
land ALL Game-side ledger functions on `DegenerusGame.sol`. These consume the storage producers from 344-01
(the `keeperFunding` mapping + the `funder` field) and PRODUCE the ABI the interfaces (344-03) and AfKing
(344-04) consume. Six edits:
- `depositKeeperFunding(address) payable` (NEW)
- un-brickable CEI `withdrawKeeperFunding(uint256)` with the GO_SWEPT guard as LINE 1 (NEW)
- `keeperFundingOf(address) view` (NEW)
- `batchPurchase` payable → NON-payable, per-slice `keeperFunding[b.funder]` + `claimablePool` debit (D-01)
- `_claimWinningsInternal` post-gameOver keeper-merge (Decision B)
- `keeperSnapshot` extended to also return `keeperFunding[player]`
- the master-invariant comment at `:18` (and ONLY `:18` — `:5` is `@title`)

Purpose: This is the accounting spine of the ledger. Every `keeperFunding` mutation moves `claimablePool` in
tandem (D-CF-03 — no new aggregate); the keeper bucket inherits the already-correct solvency wiring
(SOLVENCY-01, proven). The two LIVE TRAPS this plan must defuse: (1) the D-01 `b.funder` debit key (NOT
`b.player`), (2) the GO_SWEPT guard as the FIRST line of `withdrawKeeperFunding`.

Output: Edits applied to `contracts/DegenerusGame.sol` (part of the single batched 344 diff). NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor NEVER runs `git commit` / `git add
contracts/` (hook-enforced). Orchestrator commits post-approval (344-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md
@.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-CONTEXT.md

<interfaces>
<!-- Re-pinned anchors are inherited from 344-01 Task 1. These are the SPEC-attested starting refs. -->

Current DegenerusGame.sol anchors (attested @ 83a84431; re-pin per 344-01 before editing):
- `batchPurchase` def `:1824` — currently `function batchPurchase(BatchBuy[] calldata buys) external payable {`
  (was doc-cited `:1809`; DRIFT +15 — use the re-pinned line)
- `purchaseWith` selector call block `:1838` — `IDegenerusGameMintModule.purchaseWith.selector` (the dispatch on b.player)
- `keeperSnapshot` def `:2645` — `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables) {`
- `_claimWinningsInternal` def `:1462`; its GO_SWEPT guard ALREADY present at `:1463`:
  `if (_goRead(GO_SWEPT_SHIFT, GO_SWEPT_MASK) != 0) revert E();` (the precedent withdrawKeeperFunding mirrors)
- master-invariant comment `:18` ONLY — `*      - address(this).balance + steth.balanceOf(this) >= claimablePool`
  (the `:5` "second copy" does NOT exist — `:5` is `* @title DegenerusGame`)
- `claimablePool` is `uint128 internal claimablePool;` (DegenerusGameStorage.sol:355)
- `pullRedemptionReserve` `:1981` — a 4th claimablePool-tandem-debit site; keep keeperFunding DISJOINT (awareness only)
- bare `receive()` `:2915` — routes msg.value to the prize pool; keeper deposits CANNOT use it

Template to mirror (the removed AfKing fns — re-read for the CEI shape):
- AfKing.depositFor `:314` (the deposit mirror)
- AfKing.withdraw `:328-341` (un-brickable CEI: read bal → debit before .call → send; debit at `:334`)
- AfKing per-player snapshot fallback `:809` (the keeperSnapshot consumption pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add depositKeeperFunding + withdrawKeeperFunding (GO_SWEPT line 1) + keeperFundingOf + events</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (the `_claimWinningsInternal` GO_SWEPT guard `:1463` to mirror verbatim; the bare `receive()` `:2915` to confirm keeper deposits cannot use it; existing event decl style)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 1.3 (depositKeeperFunding), Section 1.4 (withdrawKeeperFunding + GO_SWEPT line-1 LOCK), Section 1.5 (keeperFundingOf)
    - 343-SOLVENCY-PROOF.md Section B (the GO_SWEPT withdraw-guard LOCK — B.1 the gap, B.2 the precedent `:1463`, B.3 the LOCK: guard FIRST, checked-math debit, no unchecked on claimablePool)
    - 344-CONTEXT.md D-344-08 (the events: KeeperFunded / KeeperWithdrew; the AfKing Deposited event is removed in 344-04, not here)
  </read_first>
  <action>
    ADD three new external functions to DegenerusGame.sol plus their two events:
    (a) `depositKeeperFunding(address player) external payable` — revert if `player == address(0)`; zero-value
        no-op (`if (msg.value == 0) return;`); `keeperFunding[player] += msg.value;` AND `claimablePool +=
        uint128(msg.value);` (tandem — D-CF-03); `emit KeeperFunded(player, msg.value);`. Mirror the removed
        `AfKing.depositFor:314` shape. The Game's bare `receive()` (`:2915`) routes to the prize pool, so this
        dedicated entrypoint is REQUIRED — keeper deposits cannot use `receive()`.
    (b) `withdrawKeeperFunding(uint256 amount) external` — un-brickable strict CEI. THE GO_SWEPT GUARD IS LINE 1,
        BEFORE any debit: `if (_goRead(GO_SWEPT_SHIFT, GO_SWEPT_MASK) != 0) revert E();` (mirror
        `_claimWinningsInternal:1463` verbatim). Then: zero-value no-op; read `bal = keeperFunding[msg.sender]`;
        `if (amount > bal) revert E();`; debit `keeperFunding[msg.sender] = bal - amount;` (may use `unchecked`
        on THIS subtraction since it is bounds-checked above) AND `claimablePool -= uint128(amount);` —
        ⚠ the `claimablePool -= amount` debit MUST stay CHECKED math (NO `unchecked` — red-team carry-forward #1);
        BOTH debits BEFORE the `.call`; `(bool ok, ) = msg.sender.call{value: amount}("");` then `if (!ok) revert
        E();`; `emit KeeperWithdrew(msg.sender, amount);`. Re-entrant second call re-reads the debited balance and
        reverts — un-brickable.
    (c) `keeperFundingOf(address player) external view returns (uint256) { return keeperFunding[player]; }` — the
        canonical per-player balance view (replaces the DELETED AfKing.poolOf; also the OPEN-E `src != player`
        extra-read source for D-MR-01).
    (d) ADD the events: `event KeeperFunded(address indexed player, uint256 amount);` and
        `event KeeperWithdrew(address indexed player, uint256 amount);`.
    Do NOT touch the unchanged accounting spine (distributeYieldSurplus, the gameOver drain, handleFinalSweep,
    adminStakeEthForStEth, the sDGNRS valuation) — it reserves the keeper total automatically via `claimablePool`
    (SOLVENCY-01, proven). Keep the keeper bucket disjoint from `pullRedemptionReserve:1981` (it already is).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "function depositKeeperFunding(address player) external payable" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "function withdrawKeeperFunding(uint256 amount) external" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "function keeperFundingOf(address" contracts/DegenerusGame.sol | grep -qx 1 && awk '/function withdrawKeeperFunding/{f=1} f&&/_goRead\(GO_SWEPT_SHIFT, GO_SWEPT_MASK\)/{print "GUARD-LINE-1"; exit}' contracts/DegenerusGame.sol | grep -qx "GUARD-LINE-1" && echo "THREE-FNS GO_SWEPT-PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `depositKeeperFunding(address player) external payable` exists: reverts on `player == address(0)`, zero-value no-op, credits `keeperFunding[player]` AND `claimablePool` in tandem, emits `KeeperFunded`
    - `withdrawKeeperFunding(uint256 amount) external` exists with the GO_SWEPT guard as the FIRST statement (before any debit), strict CEI (both debits before the `.call`), `claimablePool -= amount` in CHECKED math (no `unchecked` on that line), reverts on `amount > balance`, zero-value no-op, emits `KeeperWithdrew`
    - `keeperFundingOf(address) external view returns (uint256)` returns `keeperFunding[player]`
    - Both events `KeeperFunded` / `KeeperWithdrew` declared (indexed player)
    - The unchanged accounting spine functions are NOT modified
  </acceptance_criteria>
  <done>depositKeeperFunding / withdrawKeeperFunding (GO_SWEPT line-1, checked-math claimablePool debit, strict CEI) / keeperFundingOf exist with the locked signatures; the two events are declared; the accounting spine is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: batchPurchase → NON-payable with the D-01 funder-keyed debit + per-slice claimablePool release</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (re-pinned `batchPurchase` def, SPEC `:1824` was `external payable`; the loop body; the `purchaseWith` selector dispatch `:1838` on `b.player`; the existing `spent == msg.value` guard being REMOVED)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 1.1 (the FINAL non-payable batchPurchase with the funder debit) + Section 3.1 (D-01: the funder trap, the OPEN-E operator-funded case, the WHY)
    - 343-SOLVENCY-PROOF.md Section D (the D-01 funder/beneficiary identity split; the VAULT/SDGNRS exemption stays on `player`)
    - 344-CONTEXT.md D-01 (LIVE TRAP — three upstream docs literally say `b.player`; the correct debit is `b.funder`)
  </read_first>
  <action>
    ⚠ LIVE TRAP — D-01. Rewrite `batchPurchase` (re-pinned anchor, SPEC `:1824`):
    - Remove the `payable` modifier: `function batchPurchase(BatchBuy[] calldata buys) external {` (was `external payable`).
    - Remove the `spent == msg.value` exact-funding guard ENTIRELY (no value arrives — there is nothing to reconcile against).
    - Keep UNCHANGED: the AF_KING gate (`if (msg.sender != ContractAddresses.AF_KING) revert E();`), the
      `!gameOver` pre-check, the `len != 0` check.
    - Per slice, debit the FUNDER's bucket — NOT the beneficiary's: read `bal = keeperFunding[b.funder];`,
      `if (bal < ev) revert E();` (atomic safety guard — AfKing pre-validates), `keeperFunding[b.funder] = bal -
      ev;` (may use `unchecked` on this bounds-checked subtraction), then `claimablePool -= uint128(ev);` —
      ⚠ this MUST be CHECKED math (release the keeper reservation in tandem with the per-player debit).
      ⛔ DO NOT key the debit on `keeperFunding[b.player]` — that is the trap that breaks the OPEN-E
      operator-funded case (`src != player` would debit the empty subscriber bucket).
    - Dispatch `purchaseWith` on `b.player` (the beneficiary) via the existing selector-call block (`:1838`),
      passing `ev` as the eth value — `_purchaseForWith`/`purchaseWith`/`_settleClaimableShortfall` UNCHANGED. The
      released ETH becomes prize-pool/vault-share ETH exactly as a fresh `msg.value` buy → earns the FRESH
      affiliate rate (20-25%, `isFreshEth=true`) via `payAffiliate` (DegenerusAffiliate.sol:388) — AUTOBUY-03; NO
      affiliate-bonus rework. (Cite `payAffiliate`, NOT `handleAffiliate` — `handleAffiliate` is an unrelated
      quest fn.)
    - Atomic non-brick preserved: a reverting slice rolls back the WHOLE batch (no per-slice try/catch) — benign
      because `advanceGame()` is an independent permissionless entrypoint (AUTOBUY-04).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && awk '/function batchPurchase\(BatchBuy\[\] calldata buys\) external/{print} ' contracts/DegenerusGame.sol | grep -v "payable" | grep -c "external {" | grep -qx 1 && grep -c "keeperFunding\[b\.funder\]" contracts/DegenerusGame.sol | grep -q "[1-9]" && grep -c "keeperFunding\[b\.player\]" contracts/DegenerusGame.sol | grep -qx 0 && echo "NON-PAYABLE FUNDER-DEBIT NO-PLAYER-DEBIT"</automated>
  </verify>
  <acceptance_criteria>
    - `batchPurchase(BatchBuy[] calldata buys) external` is NON-payable (no `payable` modifier on the def line)
    - The `spent == msg.value` guard is GONE
    - The per-slice debit keys on `keeperFunding[b.funder]` (appears ≥1) — `keeperFunding[b.player]` appears EXACTLY 0 times (the D-01 trap is defused)
    - `claimablePool -= uint128(ev)` per slice is CHECKED math (no `unchecked` wrapping that subtraction)
    - The AF_KING gate, `!gameOver`, and `len != 0` checks are UNCHANGED; `purchaseWith` is dispatched on `b.player`
  </acceptance_criteria>
  <done>batchPurchase is non-payable, the spent==msg.value guard is gone, the per-slice debit keys on `keeperFunding[b.funder]` (b.player debit appears 0 times), the claimablePool release is checked math, and the AF_KING/!gameOver/len gates + the b.player dispatch are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: _claimWinningsInternal keeper-merge (Decision B) + extended keeperSnapshot + invariant comment (:18 only)</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (re-pinned `_claimWinningsInternal` def `:1462` + its existing GO_SWEPT guard `:1463`; the existing `claimableWinnings[player]` zero + `claimablePool -=` pattern; `keeperSnapshot` def `:2645` with its current 3-tuple return; the master-invariant comment `:18`)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 1.7 (the Decision-B merge) + Section 1.6 (extended keeperSnapshot) + Section 2.2 / 3.4 (the invariant comment at `:18` ONLY; `:5` is `@title` — NOT edited)
    - 343-SOLVENCY-PROOF.md Section B.2 (the merge is naturally blocked post-sweep by the existing `:1463` guard — no double-spend with withdrawKeeperFunding; both zero the same bucket)
  </read_first>
  <action>
    Three edits:
    (a) `_claimWinningsInternal` (re-pinned `:1462`) — ADD the Decision-B keeper-merge: alongside the existing
        `won = claimableWinnings[player]`, read `keeper = keeperFunding[player]`; pay `payout = won + keeper`;
        zero BOTH buckets (`claimableWinnings[player] = 0` as today, PLUS `keeperFunding[player] = 0`); debit
        `claimablePool` by the COMBINED sum (`claimablePool -= uint128(payout)`). The existing GO_SWEPT guard at
        `:1463` is ALREADY present — do NOT touch it; it naturally blocks the merge post-sweep too. No unbounded
        loop — this is a lazy per-player merge keyed on the caller. No double-spend vs `withdrawKeeperFunding`:
        both zero the SAME per-player bucket → whichever runs second reads zero (GAMEOVER-01).
    (b) `keeperSnapshot` (re-pinned `:2645`) — EXTEND the return tuple to ALSO return a per-player
        `keeperFunding[player]` array alongside the current `(mintPriceWei, rngLocked_, claimables)`: add
        `uint256[] memory keeperFundings` and populate `keeperFundings[i] = keeperFunding[players[i]]` in the
        existing per-player loop (ONE extra per-player READ, NO extra staticcall — GASOPT-03 / AUTOBUY-05
        preserved on the common path). (The OPEN-E `src != player` extra read is wired AfKing-side in 344-04 via
        `keeperFundingOf(src)`, D-MR-01 — not here.)
    (c) The master-invariant comment at `:18` ONLY — refresh the surrounding comment to note `claimablePool` now
        also covers `Σ keeperFunding` (the master invariant FORM `balance + steth >= claimablePool` is
        UNCHANGED). ⛔ Do NOT edit `:5` — it is `* @title DegenerusGame`, NOT a copy of the invariant (the RESEARCH
        "second copy at :5" claim is OVERTURNED; the invariant appears exactly once, at `:18`). Comment describes
        current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && awk '/function _claimWinningsInternal/{f=1} f&&/keeperFunding\[player\] = 0/{print "MERGE-ZEROS-KEEPER"; exit}' contracts/DegenerusGame.sol | grep -qx "MERGE-ZEROS-KEEPER" && grep -c "uint256\[\] memory keeperFundings" contracts/DegenerusGame.sol | grep -q "[1-9]" && sed -n '5p' contracts/DegenerusGame.sol | grep -q "@title" && echo "MERGE SNAPSHOT-EXTENDED TITLE-INTACT"</automated>
  </verify>
  <acceptance_criteria>
    - `_claimWinningsInternal` reads `keeperFunding[player]`, pays `claimableWinnings + keeperFunding`, zeroes BOTH buckets, debits `claimablePool` by the combined sum
    - The existing GO_SWEPT guard at `:1463` is UNCHANGED (still the first statement of `_claimWinningsInternal`)
    - `keeperSnapshot` returns an additional per-player `keeperFunding` array; no extra staticcall on the common path
    - The invariant comment is edited at `:18` only; line `:5` remains `* @title DegenerusGame` (unedited)
  </acceptance_criteria>
  <done>_claimWinningsInternal pays + zeroes the keeper bucket (Decision B) with the `:1463` guard unchanged; keeperSnapshot returns the extended per-player keeperFunding tuple; the invariant comment is edited at `:18` only (`:5` @title intact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscriber/operator → Game (deposit/withdraw) | Untrusted callers fund and withdraw keeper ETH; the ledger must be un-brickable and solvency-preserving. |
| AfKing → Game (batchPurchase) | AF_KING-gated; the per-slice debit must key on the funder (un-spoofable accounting identity = src). |
| post-sweep lifecycle (GO_SWEPT) | After the 30-day final sweep `claimablePool == 0`; a naive withdraw underflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-02-RE | Reentrancy/DoS | `withdrawKeeperFunding` | mitigate | Strict CEI — debit `keeperFunding[msg.sender]` + `claimablePool` BEFORE the `.call`; a re-entrant second call re-reads the debited balance and reverts (un-brickable). Task 1 verify asserts the structure. (343-SOLVENCY-PROOF Section B / probe 8 NEGATIVE-VERIFIED) |
| T-344-02-GS | DoS (underflow) | `withdrawKeeperFunding` post-sweep | mitigate | GO_SWEPT guard as LINE 1 (before any debit) mirrors `_claimWinningsInternal:1463`; `claimablePool -= amount` stays CHECKED — a post-sweep withdraw reverts cleanly instead of underflowing. (343-SOLVENCY-PROOF Section B.3 LOCK / T-343-04) |
| T-344-02-D1 | Tampering/Spoofing | `batchPurchase` debit key | mitigate | Debit keys on `keeperFunding[b.funder]` (= src), NOT `b.player`; VAULT/SDGNRS exemption stays on the un-spoofable `player`. Task 2 verify asserts `b.player` debit appears 0 times. (343-SOLVENCY-PROOF Section D / probe 9 NEGATIVE-VERIFIED) |
| T-344-02-SOL | Solvency invariant break | every keeperFunding mutation | mitigate | No new aggregate (D-CF-03); every mutation moves `claimablePool` in tandem so `claimablePool == Σ claimableWinnings + Σ keeperFunding`; the master invariant `balance + steth >= claimablePool` is preserved and every reservation site already reserves the keeper total. (SOLVENCY-01/03 proven; probe 10 NEGATIVE-VERIFIED) |
| T-344-02-DS | Double-spend | claim-merge vs withdraw | mitigate | Both `_claimWinningsInternal` merge and `withdrawKeeperFunding` zero the SAME per-player `keeperFunding` bucket → whichever runs second reads zero and pays nothing. (343-SOLVENCY-PROOF Section B / probe 10 NEGATIVE-VERIFIED) |
| T-344-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (Solidity edits only). |
</threat_model>

<verification>
- `depositKeeperFunding` / `withdrawKeeperFunding` / `keeperFundingOf` exist with the locked signatures
- The GO_SWEPT guard is LINE 1 of `withdrawKeeperFunding`; `claimablePool -= amount` is checked math
- `batchPurchase` is non-payable and debits `keeperFunding[b.funder]` (D-01); `keeperFunding[b.player]` appears 0 times
- `_claimWinningsInternal` pays + zeroes the keeper bucket (Decision B); the `:1463` guard is unchanged
- `keeperSnapshot` returns the extended tuple; the invariant comment is edited at `:18` only (`:5` is `@title`)
- The unchanged accounting spine is NOT modified
- NO `git commit` / `git add contracts/` was run by the executor
</verification>

<success_criteria>
- All six Game-side ledger edits land on `contracts/DegenerusGame.sol`
- The two live traps (D-01 `b.funder`, GO_SWEPT line-1) are defused and asserted
- Held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-02-SUMMARY.md` when done.
</output>
