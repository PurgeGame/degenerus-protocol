---
phase: 38-rng-delta-security
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - audit/v3.2-rng-delta-findings.md
autonomous: true
requirements:
  - RNG-03
  - RNG-04

must_haves:
  truths:
    - "Decimator per-level claim persistence is verified safe: no double-claim possible, ETH accounting correct across rounds"
    - "Terminal decimator claimed-flag mechanism (weightedBurn=0) is verified as reliable double-claim prevention"
    - "Complete rngLocked consumer dependency matrix covers all 6 contracts with safety verdicts per function"
    - "Combined-change interaction analysis shows no emergent attack vectors from rngLocked removal + decimator persistence + interface deletions"
    - "Every rngLocked consumer has an explicit verdict: still safe without claim-blocking assumption, or not applicable"
  artifacts:
    - path: "audit/v3.2-rng-delta-findings.md"
      provides: "Complete findings document with all 4 RNG requirement sections plus executive summary"
      contains: "## RNG-04"
  key_links:
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: "audit/v3.2-rng-delta-findings.md"
      via: "decimator correctness trace"
      pattern: "decClaimRounds.*claimed"
    - from: "contracts/DegenerusGame.sol"
      to: "audit/v3.2-rng-delta-findings.md"
      via: "rngLocked consumer dependency matrix"
      pattern: "rngLocked.*consumer"
---

<objective>
Audit decimator claim persistence for correctness (RNG-03) and build the cross-contract rngLocked dependency matrix (RNG-04), then finalize the findings document with executive summary.

Purpose: Verify the decimator storage migration creates no exploitable state, and that ALL rngLocked consumers across all contracts remain safe with the combined changes. Produce the final consolidated findings document.

Output: Completed `audit/v3.2-rng-delta-findings.md` with RNG-03 and RNG-04 sections plus executive summary with severity classifications.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-rng-delta-security/38-CONTEXT.md
@.planning/phases/38-rng-delta-security/38-RESEARCH.md
@.planning/phases/38-rng-delta-security/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decimator claim persistence correctness (RNG-03) and cross-contract matrix (RNG-04)</name>
  <files>audit/v3.2-rng-delta-findings.md</files>
  <read_first>
    contracts/modules/DegenerusGameDecimatorModule.sol
    contracts/DegenerusGame.sol
    contracts/BurnieCoin.sol
    contracts/modules/DegenerusGameWhaleModule.sol
    contracts/storage/DegenerusGameStorage.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/BurnieCoinflip.sol
    contracts/interfaces/IBurnieCoinflip.sol
    contracts/interfaces/IDegenerusGame.sol
    audit/v3.2-rng-delta-findings.md
    .planning/phases/38-rng-delta-security/38-RESEARCH.md
    .planning/phases/38-rng-delta-security/38-CONTEXT.md
  </read_first>
  <action>
Append RNG-03 and RNG-04 sections to `audit/v3.2-rng-delta-findings.md`, then add executive summary.

**RNG-03 section must contain these subsections:**

1. **Storage Migration Summary** -- Document the change from `DecClaimRound internal lastDecClaimRound` (single slot) to `mapping(uint24 => DecClaimRound) internal decClaimRounds` (per-level). Verify old field `lastDecClaimRound` is no longer referenced ANYWHERE in the codebase (search all .sol files). Document where the new mapping is declared in DegenerusGameStorage.sol.

2. **Double-Claim Prevention Trace** -- Trace the `_consumeDecClaim` function in DegenerusGameDecimatorModule.sol:
   - Find the exact line where `e.claimed != 0` is checked (research says line 371, verify)
   - Find the exact line where `e.claimed = 1` is set (research says line 385, verify)
   - Verify the check happens BEFORE any state changes or ETH transfers
   - Document: can an attacker claim the same level twice? (No -- `e.claimed` is checked first)

3. **Terminal Decimator Claimed Flag** -- Document the terminal decimator path:
   - `weightedBurn = 0` used as implicit claimed flag (research says line 993, verify)
   - Verify no path can set `weightedBurn` back to non-zero after it's been zeroed for claiming
   - Document: `TerminalDecAlreadyClaimed` error was removed -- verify the weightedBurn=0 check is equivalent protection

4. **ETH Accounting Across Rounds** -- Trace the ETH flow for decimator claims:
   - Where is the decimator ETH pool funded? (From decimation events)
   - How is the per-player share calculated in `_consumeDecClaim`?
   - Does persistence across rounds change the total pool or per-player shares? (Answer: No -- each round's pool is independent, persistence just allows claiming from older rounds)
   - Verify no ETH can be double-counted between rounds

5. **RNG-03 Verdict** -- SAFE or UNSAFE. Expected: SAFE -- this is a storage layout change with clear double-claim prevention, not a new RNG attack surface. Per user decision: "same security posture as before, just different storage layout."

**RNG-04 section must contain these subsections:**

1. **rngLocked Consumer Inventory** -- Complete table of ALL rngLocked consumers across all contracts. Use this format:

   ```
   | Contract | Function | Line | Guard Type | Assumed Claims Blocked? | Still Safe Without? | Verdict |
   ```

   **rngLockedFlag SET:**
   - AdvanceModule._finalizeRngRequest() (research says line 1207, verify)

   **rngLockedFlag CLEARED:**
   - AdvanceModule._unlockRng() (research says line 1283, verify)
   - AdvanceModule.updateVrfCoordinatorAndSub() (research says line 1271, verify)

   **rngLocked() VIEW consumers (via DegenerusGame.rngLocked()):**
   - DegenerusGame.setDecimatorAutoRebuy() (line 1542)
   - DegenerusGame._setAutoRebuy() (line 1563)
   - DegenerusGame._setAutoRebuyTakeProfit() (line 1578)
   - DegenerusGame._setAfKingMode() (line 1643)
   - BurnieCoin.balanceOfWithClaimable() (line 273)
   - BurnieCoinflip._setCoinflipAutoRebuy() (line 691)
   - BurnieCoinflip._setCoinflipAutoRebuyTakeProfit() (line 741)

   **rngLockedFlag DIRECT consumers (via DegenerusGameStorage inheritance):**
   - AdvanceModule.requestLootboxRng() (line 673)
   - AdvanceModule.reverseFlip() (line 1295)
   - AdvanceModule.advanceGame() (line 138)
   - WhaleModule._purchaseDeityPass() (line 470)
   - BurnieCoinflip._claimCoinflipsInternal() (lines 551-562, BAF guard)
   - BurnieCoinflip._coinflipLockedDuringTransition() (line 997)
   - BurnieCoinflip._addDailyFlip() (line 630)

   **REMOVED (the changes under audit):**
   - BurnieCoinflip.claimCoinflips() -- was rngLocked check, removed
   - BurnieCoinflip.claimCoinflipsFromBurnie() -- was rngLocked check, removed
   - BurnieCoinflip.consumeCoinflipsForBurn() -- was rngLocked check, removed
   - BurnieCoinflip.claimCoinflipsTakeProfit() -- entire function deleted

   For EVERY consumer, verify the exact line number against current code and fill in the "Assumed Claims Blocked?" and "Still Safe Without?" columns. The research document provides the expected inventory -- verify each entry.

2. **Dependency Matrix Analysis** -- For each remaining rngLocked consumer, answer:
   - Did this function's safety depend on the assumption that coinflip claims were blocked during RNG lock?
   - If yes, is it still safe now that claims proceed? Why?
   - Key insight: most consumers guard configuration changes (auto-rebuy toggle, take-profit changes), not claims. These guards prevent state changes during RNG processing, which is orthogonal to whether claims proceed.

3. **Combined-Change Interaction Analysis** -- Evaluate whether the three changes COMBINED create emergent vectors:
   - rngLocked removal from claims + decimator persistence: Does allowing claims during RNG lock while also allowing old decimator claims create any interaction? (Expected: No -- different asset systems, different contracts)
   - rngLocked removal + interface deletions (claimCoinflipsTakeProfit, futurePrizePoolTotalView): Do the deleted functions remove any safety check that was relied upon? (Expected: No -- take-profit is now handled within claimCoinflips, view function was informational only)
   - All three combined: Any scenario where a 1000 ETH attacker could exploit the combination?

4. **Interface NatSpec Stale Findings** -- Check IBurnieCoinflip.sol for stale `@custom:reverts RngLocked` tags on:
   - claimCoinflips (research says line 34, verify)
   - claimCoinflipsFromBurnie (research says line 42, verify)
   - consumeCoinflipsForBurn (research says line 52, verify)
   These are now incorrect since the functions no longer revert with RngLocked. Classify as LOW severity comment findings. Also check IDegenerusGame.sol for any stale references to futurePrizePoolTotalView.

5. **RNG-04 Verdict** -- SAFE or UNSAFE. Expected: SAFE -- remaining rngLocked consumers guard configuration changes (not claims), combined changes create no emergent vectors.

**Executive Summary (prepend to top of document, after header):**

Add an executive summary section AFTER the existing document header but BEFORE the RNG-01 section. Contains:
- Overall verdict: SAFE/UNSAFE for the complete set of RNG-adjacent changes
- Summary table: one row per requirement (RNG-01 through RNG-04) with verdict and one-line reasoning
- Findings summary: count of findings by severity (expected: 0 HIGH, 0 MEDIUM, some LOW/INFO for comment and UX issues)
- Key invariant restated: "Carry ETH is never reachable from any claim path while auto-rebuy is enabled"
  </action>
  <verify>
    <automated>grep -c "## RNG-03" audit/v3.2-rng-delta-findings.md && grep -c "## RNG-04" audit/v3.2-rng-delta-findings.md && grep -c "Executive Summary\|executive summary" audit/v3.2-rng-delta-findings.md && grep -c "Dependency Matrix\|dependency matrix\|Consumer Inventory\|consumer inventory" audit/v3.2-rng-delta-findings.md && grep -c "decClaimRounds\|_consumeDecClaim" audit/v3.2-rng-delta-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-rng-delta-findings.md contains "## RNG-03" section
    - audit/v3.2-rng-delta-findings.md contains "## RNG-04" section
    - Document contains "Executive Summary" section
    - Document contains dependency matrix table with columns for Contract, Function, Line, Guard Type, Assumed Claims Blocked, Still Safe Without, Verdict
    - Document contains "decClaimRounds" with line reference from DegenerusGameDecimatorModule.sol
    - Document contains "_consumeDecClaim" with line references for claimed check and set
    - Document contains "weightedBurn" for terminal decimator claimed flag
    - Document contains RNG-03 Verdict with SAFE or UNSAFE
    - Document contains RNG-04 Verdict with SAFE or UNSAFE
    - Document contains at least 15 rngLocked consumer entries in the inventory table (7 VIEW + 7 DIRECT + 4 REMOVED = 18 total per research)
    - Document contains "Combined-Change" or "combined" interaction analysis section
    - Document contains "@custom:reverts RngLocked" stale NatSpec finding with LOW severity
    - Executive summary contains per-requirement verdict table (4 rows: RNG-01 through RNG-04)
    - Executive summary contains findings count by severity
  </acceptance_criteria>
  <done>Complete audit/v3.2-rng-delta-findings.md with all four requirement sections (RNG-01 through RNG-04), executive summary with per-requirement verdicts and severity counts, dependency matrix covering all rngLocked consumers with safety verdicts, decimator correctness trace with double-claim prevention proof, combined-change interaction analysis, and all findings classified by severity.</done>
</task>

</tasks>

<verification>
- RNG-03 covers double-claim prevention via e.claimed and terminal weightedBurn=0
- RNG-03 traces ETH accounting across rounds
- RNG-04 dependency matrix covers all 18+ rngLocked consumers with line numbers
- RNG-04 combined-change analysis evaluates all three changes together
- RNG-04 catches stale NatSpec in IBurnieCoinflip.sol
- Executive summary has per-requirement verdict table
- All 4 requirement IDs have explicit SAFE/UNSAFE verdicts
- Findings classified by severity (HIGH/MEDIUM/LOW/INFO)
</verification>

<success_criteria>
audit/v3.2-rng-delta-findings.md is the complete deliverable with executive summary, all four RNG requirement sections, dependency matrix, and severity-classified findings. Every rngLocked consumer has an explicit safety verdict. No unaddressed attack vectors remain.
</success_criteria>

<output>
After completion, create `.planning/phases/38-rng-delta-security/38-02-SUMMARY.md`
</output>
