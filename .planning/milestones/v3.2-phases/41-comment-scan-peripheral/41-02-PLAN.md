---
phase: 41-comment-scan-peripheral
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md
autonomous: true
requirements:
  - CMT-04
  - CMT-05
must_haves:
  truths:
    - "Every NatSpec tag in DegenerusVault.sol matches actual function signatures and behavior"
    - "Every NatSpec tag in DegenerusAffiliate.sol matches actual function signatures and behavior"
    - "Every NatSpec tag in IBurnieCoinflip.sol matches the BurnieCoinflip.sol implementation (no stale revert annotations)"
    - "Every NatSpec tag in IDegenerusGame.sol matches the DegenerusGame.sol implementation"
    - "All inline and block comments in these 4 contracts accurately describe code behavior"
    - "v3.1 findings CMT-070/CMT-071 (Affiliate), CMT-077/CMT-078 (Vault) verified fixed"
  artifacts:
    - path: ".planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md"
      provides: "Comment audit findings for DegenerusVault, DegenerusAffiliate, IBurnieCoinflip, IDegenerusGame"
      contains: "DegenerusVault"
    - path: ".planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md"
      provides: "Comment audit findings"
      contains: "IBurnieCoinflip"
  key_links:
    - from: "IBurnieCoinflip.sol @custom:reverts RngLocked"
      to: "BurnieCoinflip.sol claim functions"
      via: "Interface NatSpec says RngLocked revert but implementation no longer checks rngLocked()"
      pattern: "@custom:reverts RngLocked"
    - from: "DegenerusVault.sol _transfer @dev"
      to: "DegenerusVault.sol _transfer implementation"
      via: "@dev says zero-address checks (plural) but only `to` is checked"
      pattern: "zero-address checks"
    - from: "IDegenerusGame.sol"
      to: "DegenerusGame.sol"
      via: "futurePrizePoolTotalView removed from interface -- verify no stale references"
      pattern: "futurePrizePoolTotalView"
---

<objective>
Audit all comments in 4 light-change peripheral + interface contracts: DegenerusVault.sol (1,050 lines, 17 changed), DegenerusAffiliate.sol (848 lines, 5 changed), IBurnieCoinflip.sol (173 lines, 14 removed), IDegenerusGame.sol (443 lines, 4 removed).

Purpose: This group includes the critical IBurnieCoinflip interface which has 3 pre-identified stale `@custom:reverts RngLocked` annotations (HIGH-probability findings from research). Also includes the partially-fixed CMT-078 in DegenerusVault.

Output: `41-02-SUMMARY.md` with per-contract findings sections for all 4 contracts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-comment-scan-peripheral/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DegenerusVault.sol and DegenerusAffiliate.sol comments (1,898 lines combined)</name>
  <files>.planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md</files>
  <read_first>
    - contracts/DegenerusVault.sol (full 1,050 lines)
    - contracts/DegenerusAffiliate.sol (full 848 lines)
    - audit/v3.1-findings-35-peripheral-contracts.md (v3.1 findings CMT-070/CMT-071 for Affiliate, CMT-077/CMT-078 for Vault)
    - .planning/phases/41-comment-scan-peripheral/41-RESEARCH.md (pre-identified findings and v3.1 status table)
  </read_first>
  <action>
Create 41-02-SUMMARY.md with audit sections for DegenerusVault.sol and DegenerusAffiliate.sol.

**DegenerusVault.sol audit (1,050 lines, 17 changed):**

Step 1: Verify v3.1 fixes:
- CMT-077 (line 662): Now says "afKing mode" instead of "AFK king mode" -- verify accuracy. Research says FIXED.
- CMT-078 (line 236): `transferFrom` @custom:reverts fixed -- BUT research says PARTIALLY FIXED. The `_transfer` @dev at approximately line 286 still says "zero-address checks" (plural) when only `to` is checked. This is a PRE-IDENTIFIED FINDING. Read `_transfer` function carefully: check if the function validates `from` address (zero check) and `to` address (zero check). If only `to` is checked, the comment saying "checks" (plural) is inaccurate and must be documented as a finding.

Step 2: Audit new code changes for drift:
a) AFK to afKing rename: search all comments for any remaining "AFK" references that should say "afKing"
b) takeProfit removal: `coinClaimCoinflipsTakeProfit` function was removed from DegenerusVault. The internal `ICoinflipPlayerActions` interface lost the function. Search all comments for remaining "takeProfit" or "TakeProfit" references.
c) Check that the `ICoinflipPlayerActions` interface defined within DegenerusVault.sol matches the actual IBurnieCoinflip.sol interface (no stale function declarations).

Step 3: Full NatSpec and inline comment pass on all functions:
- Does @notice accurately describe what each function does?
- Do @param tags match actual parameter names and types?
- Do @return tags match actual return values?
- Do @custom:reverts tags list all actual revert conditions?
- Do inline comments accurately describe the code they annotate?

**DegenerusAffiliate.sol audit (848 lines, 5 changed):**

Step 1: Verify v3.1 fixes:
- CMT-070 (line 383): @param now says "10000+ triggers linear taper to 25% floor" -- verify the taper math in the actual function matches this description (does the floor go to 25%? does 10000+ trigger the taper?)
- CMT-071 (line 546): Now says "Collect recipients for weighted random winner selection" -- verify this describes the actual behavior

Step 2: Audit new code changes:
- New PRNG design note added at approximately line 544: "PRNG is known -- accepted design tradeoff (EV-neutral, manipulation only redistributive between affiliates)". Verify: (a) Is the PRNG actually known/deterministic? (b) Is it actually EV-neutral? (c) Is manipulation truly only redistributive between affiliates, or could it affect non-affiliate outcomes?

Step 3: Full NatSpec and inline comment pass on all functions.

**Write findings using format:**
```
# Phase 41 Plan 02: Light-Change Peripheral + Interfaces — Comment Audit

## DegenerusVault.sol

**Scope:** 1,050 lines | ~X NatSpec tags | ~Y comment lines | Z external/public functions
**Changes since v3.1:** 17 lines changed (AFK->afKing, takeProfit removal, transferFrom @custom:reverts fix)
**v3.1 findings status:** CMT-077 FIXED, CMT-078 PARTIALLY FIXED

### Findings

[findings]

## DegenerusAffiliate.sol

**Scope:** 848 lines | ~X NatSpec tags | ~Y comment lines | Z external/public functions
**Changes since v3.1:** 5 lines changed (taper values fix, batch comment fix)
**v3.1 findings status:** CMT-070 FIXED, CMT-071 FIXED

### Findings

[findings or "No new findings identified."]
```

Start CMT numbering at CMT-201 (Plan 02 series, avoids collision with Plan 01 numbering).
  </action>
  <verify>
    <automated>grep -c "DegenerusVault\|DegenerusAffiliate" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md exists
    - File contains "## DegenerusVault.sol" section header
    - File contains "## DegenerusAffiliate.sol" section header
    - File contains "v3.1 findings status:" line in both contract sections
    - DegenerusVault section addresses the _transfer @dev "zero-address checks" (plural) issue (CMT-078 partial fix from research -- line ~286)
    - DegenerusAffiliate section evaluates the new PRNG design note accuracy
    - Every finding uses the CMT-NNN format with What/Where/Why/Suggestion/Category/Severity fields
    - No contract source files (.sol) were modified
  </acceptance_criteria>
  <done>DegenerusVault.sol and DegenerusAffiliate.sol sections complete in SUMMARY.md. CMT-078 partial fix documented. All NatSpec, inline, and block comments verified. Findings in CMT-NNN format.</done>
</task>

<task type="auto">
  <name>Task 2: Audit IBurnieCoinflip.sol and IDegenerusGame.sol interface comments (616 lines combined)</name>
  <files>.planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md</files>
  <read_first>
    - contracts/interfaces/IBurnieCoinflip.sol (full 173 lines)
    - contracts/interfaces/IDegenerusGame.sol (full 443 lines)
    - contracts/BurnieCoinflip.sol (implementation -- cross-reference NatSpec against interface. Focus on: claimCoinflips, claimCoinflipsFromBurnie, consumeCoinflipsForBurn function signatures and revert conditions)
    - contracts/DegenerusGame.sol (implementation -- cross-reference for futurePrizePoolTotalView removal and remaining interface NatSpec)
    - .planning/phases/41-comment-scan-peripheral/41-RESEARCH.md (pre-identified findings: IBurnieCoinflip lines 33, 42, 51 stale RngLocked)
    - .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md (append to existing file from Task 1)
  </read_first>
  <action>
Append IBurnieCoinflip.sol and IDegenerusGame.sol audit sections to the existing 41-02-SUMMARY.md file.

**IBurnieCoinflip.sol audit (173 lines, 14 removed):**

THIS IS THE HIGHEST-PRIORITY AUDIT SURFACE. Research identified 3 HIGH-probability findings:

Step 1: Check lines 33, 42, and 51 specifically:
- Line 33: `claimCoinflips` -- does the NatSpec contain `@custom:reverts RngLocked If VRF randomness is currently being resolved`? If yes, this is STALE because the BurnieCoinflip.sol implementation no longer calls `rngLocked()` on this function. Document as a finding.
- Line 42: `claimCoinflipsFromBurnie` -- same check. If `@custom:reverts RngLocked` is present, it is stale.
- Line 51: `consumeCoinflipsForBurn` -- same check. If `@custom:reverts RngLocked` is present, it is stale.

For each stale annotation found, create a finding:
```
### CMT-2XX: Stale @custom:reverts RngLocked on [function name]

- **What:** Interface declares `@custom:reverts RngLocked If VRF randomness is currently being resolved` but the implementation (BurnieCoinflip.sol) no longer checks `rngLocked()` on this function — the check was removed as part of the rngLocked removal refactor
- **Where:** `IBurnieCoinflip.sol:LINE`
- **Why:** A warden reading the interface would expect `RngLocked` to be a valid revert condition and might file a false finding based on it, or miss that the actual claim protection is now BAF epoch-based only
- **Suggestion:** Remove `@custom:reverts RngLocked If VRF randomness is currently being resolved` from the NatSpec
- **Category:** CMT
- **Severity:** LOW
```

Step 2: Verify claimCoinflipsTakeProfit was cleanly removed from the interface. Research says it was cleanly removed -- confirm no stale references remain.

Step 3: Full interface NatSpec audit:
- For EVERY function declaration in IBurnieCoinflip.sol, cross-reference against BurnieCoinflip.sol implementation:
  - @param names and types match actual function parameters
  - @return descriptions match actual return values
  - @custom:reverts lists ONLY revert conditions that actually exist in the implementation
  - @notice accurately describes the function's purpose
  - @dev accurately describes implementation notes

**IDegenerusGame.sol audit (443 lines, 4 removed):**

Step 1: Verify futurePrizePoolTotalView was cleanly removed. Grep all comments for any remaining "futurePrizePoolTotalView" or "future prize pool" references.

Step 2: Full interface NatSpec audit:
- For EVERY function declaration in IDegenerusGame.sol, cross-reference against contracts/DegenerusGame.sol:
  - @param names and types match
  - @return descriptions match
  - @custom:reverts lists only actual revert conditions
  - No stale references to removed features

Step 3: Check for any struct, event, or error NatSpec that may be stale.

**Write findings:**
```
## IBurnieCoinflip.sol

**Scope:** 173 lines | ~X NatSpec tags | Y function declarations
**Changes since v3.1:** 14 lines removed (claimCoinflipsTakeProfit removal)
**Known issue:** 3 stale @custom:reverts RngLocked annotations (pre-identified in research)

### Findings

[findings -- expect at least 3 from the RngLocked annotations]

## IDegenerusGame.sol

**Scope:** 443 lines | ~X NatSpec tags | Y function declarations
**Changes since v3.1:** 4 lines removed (futurePrizePoolTotalView removal)

### Findings

[findings or "No new findings identified."]
```

At the bottom of the file, add a summary table:
```
## Plan 02 Summary

| Contract | Lines | v3.1 Findings Verified | New Findings | Total |
|----------|-------|----------------------|--------------|-------|
| DegenerusVault.sol | 1,050 | X fixed, Y partial | Z | N |
| DegenerusAffiliate.sol | 848 | X fixed, Y partial | Z | N |
| IBurnieCoinflip.sol | 173 | N/A (no v3.1 findings) | Z | N |
| IDegenerusGame.sol | 443 | N/A (no v3.1 findings) | Z | N |
| **Total** | **2,514** | | | **N** |
```
  </action>
  <verify>
    <automated>grep -c "IBurnieCoinflip\|IDegenerusGame" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## IBurnieCoinflip.sol" section header
    - File contains "## IDegenerusGame.sol" section header
    - IBurnieCoinflip section contains findings for stale @custom:reverts RngLocked on at least the 3 pre-identified functions (lines 33, 42, 51) -- OR explicitly documents that these annotations have already been removed
    - IBurnieCoinflip section confirms claimCoinflipsTakeProfit was cleanly removed
    - IDegenerusGame section confirms futurePrizePoolTotalView was cleanly removed
    - File contains "## Plan 02 Summary" with the totals table
    - Every finding uses the CMT-NNN format with What/Where/Why/Suggestion/Category/Severity fields
    - No contract source files (.sol) were modified
  </acceptance_criteria>
  <done>IBurnieCoinflip.sol and IDegenerusGame.sol sections appended to SUMMARY.md. Stale RngLocked annotations documented. Interface-implementation NatSpec cross-checked for all function declarations. Summary table present with per-contract finding counts.</done>
</task>

</tasks>

<verification>
- `grep -c "CMT-" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` returns finding count
- `grep "## DegenerusVault" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` confirms section exists
- `grep "## DegenerusAffiliate" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` confirms section exists
- `grep "## IBurnieCoinflip" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` confirms section exists
- `grep "## IDegenerusGame" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` confirms section exists
- `grep "## Plan 02 Summary" .planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md` confirms summary table
- No .sol files in `git diff --name-only` after execution
</verification>

<success_criteria>
All 4 light-change peripheral + interface contracts have complete comment audit sections in 41-02-SUMMARY.md. IBurnieCoinflip stale RngLocked annotations documented. DegenerusVault CMT-078 partial fix documented. Interface-implementation NatSpec parity verified for both interfaces. Summary table with per-contract counts present.
</success_criteria>

<output>
After completion, create `.planning/phases/41-comment-scan-peripheral/41-02-SUMMARY.md`
</output>
