---
phase: 41-comment-scan-peripheral
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md
autonomous: true
requirements:
  - CMT-04
must_haves:
  truths:
    - "Every NatSpec tag in BurnieCoinflip.sol matches actual function signatures and behavior"
    - "Every NatSpec tag in DegenerusQuests.sol matches actual function signatures and behavior"
    - "Every NatSpec tag in DegenerusJackpots.sol matches actual function signatures and behavior"
    - "All inline and block comments in these 3 contracts accurately describe code behavior"
    - "v3.1 findings CMT-072 through CMT-076 (BurnieCoinflip), CMT-059 through CMT-064/DRIFT-004 (Quests), CMT-065 through CMT-069 (Jackpots) verified fixed"
  artifacts:
    - path: ".planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md"
      provides: "Comment audit findings for BurnieCoinflip, DegenerusQuests, DegenerusJackpots"
      contains: "BurnieCoinflip"
    - path: ".planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md"
      provides: "Comment audit findings"
      contains: "DegenerusQuests"
    - path: ".planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md"
      provides: "Comment audit findings"
      contains: "DegenerusJackpots"
  key_links:
    - from: "BurnieCoinflip.sol claim functions"
      to: "rngLocked removal"
      via: "NatSpec @dev/@custom:reverts on claimCoinflips, claimCoinflipsFromBurnie, consumeCoinflipsForBurn"
      pattern: "rngLocked|RngLocked"
    - from: "DegenerusQuests.sol quest types"
      to: "QUEST_TYPE_RESERVED removal"
      via: "Quest type ID renumbering (DECIMATOR 5->4, LOOTBOX 6->5, etc.)"
      pattern: "QUEST_TYPE_"
    - from: "DegenerusJackpots.sol OnlyCoin error"
      to: "onlyCoin modifier"
      via: "Error message says singular but modifier accepts both COIN and COINFLIP"
      pattern: "OnlyCoin|onlyCoin"
---

<objective>
Audit all comments in 3 heavy-change peripheral contracts: BurnieCoinflip.sol (1,114 lines, 68 changed), DegenerusQuests.sol (1,588 lines, 28 changed), DegenerusJackpots.sol (689 lines, 14 changed).

Purpose: These contracts have the densest changes since v3.1 (rngLocked removal, takeProfit removal, quest type renumbering). High risk of new comment drift. Produces findings in the standard CMT-NNN format.

Output: `41-01-SUMMARY.md` with per-contract findings sections for all 3 contracts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-comment-scan-peripheral/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit BurnieCoinflip.sol comments (1,114 lines)</name>
  <files>.planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md</files>
  <read_first>
    - contracts/BurnieCoinflip.sol (the full contract -- read ALL 1,114 lines)
    - contracts/interfaces/IBurnieCoinflip.sol (cross-reference for interface NatSpec parity -- will be formally audited in Plan 02 but needed here for implementation-side context)
    - audit/v3.1-findings-35-peripheral-contracts.md (v3.1 findings CMT-072 through CMT-076 to verify they are fixed)
    - .planning/phases/41-comment-scan-peripheral/41-RESEARCH.md (pre-identified findings and v3.1 status table)
  </read_first>
  <action>
Read BurnieCoinflip.sol in full. Perform a line-by-line comment audit:

**Step 1: Verify v3.1 fixes are complete**
Confirm these v3.1 findings are fully resolved (research says all 5 are FIXED):
- CMT-072 (line 128): `JACKPOT_RESET_TIME` constant removed -- verify no remaining references in comments
- CMT-073 (line 224): NatSpec added for operator pattern -- verify @dev and @param are accurate
- CMT-074 (line 970): "staking removed" vestigial phrase removed -- verify no remaining stale staking references
- CMT-075 (line 165): Section comment now says "Last resolved day -- claims can process up to this day" -- verify accuracy against code
- CMT-076 (line 1142): Now reverts with `NotApproved()` -- verify NatSpec matches

**Step 2: Audit new code changes for comment drift**
Focus on areas with code changes since v3.1:

a) rngLocked removal from claim paths:
- `claimCoinflips` -- check that @dev no longer mentions rngLocked check, check @custom:reverts no longer lists RngLocked
- `claimCoinflipsFromBurnie` -- same checks
- `consumeCoinflipsForBurn` -- same checks
- NOTE: The implementation NatSpec was reportedly updated. Verify these updates are accurate and complete. Any remaining `rngLocked` or `RngLocked` references in comments are findings.

b) claimCoinflipsTakeProfit removal:
- Verify the function and its helper `_claimCoinflipsTakeProfit` are fully removed
- Grep all comments in the file for any remaining references to `takeProfit` or `TakeProfit` -- any found are findings

c) New NatSpec additions:
- Review all newly added @dev, @param, @notice, @return, @custom:reverts tags for accuracy
- Verify parameter names in @param match actual function parameter names
- Verify @return descriptions match actual return types and values

**Step 3: Full pass on all remaining comments**
Read every function in the contract. For each:
- Does @notice accurately describe what the function does?
- Does @dev accurately describe implementation details?
- Do @param tags match actual parameter names and types?
- Do @return tags match actual return values?
- Do @custom:reverts tags list all actual revert conditions (and only actual ones)?
- Do inline comments accurately describe the code they annotate?
- Do section headers and block comments reflect current structure?

**Step 4: Write findings**
For each finding, use this exact format:
```
### CMT-NNN: [Short description]

- **What:** [Precise description of the inaccuracy]
- **Where:** `BurnieCoinflip.sol:LINE`
- **Why:** [Why a warden would be misled]
- **Suggestion:** [Specific fix text]
- **Category:** CMT | DRIFT
- **Severity:** INFO | LOW
```

Start CMT numbering at CMT-101 (to avoid collision with v3.1 numbering which ended around CMT-084).

Begin the SUMMARY.md file with a header section:
```
# Phase 41 Plan 01: Heavy-Change Peripheral Contracts — Comment Audit

## BurnieCoinflip.sol

**Scope:** 1,114 lines | ~X NatSpec tags | ~Y comment lines | Z external/public functions
**Changes since v3.1:** 68 lines changed (rngLocked removal, takeProfit removal, NatSpec additions)
**v3.1 findings status:** CMT-072 through CMT-076 — all verified [FIXED|PARTIAL]

### Findings

[findings or "No new findings identified."]
```
  </action>
  <verify>
    <automated>grep -c "BurnieCoinflip" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md exists
    - File contains "## BurnieCoinflip.sol" section header
    - File contains "v3.1 findings status:" line in BurnieCoinflip section
    - File contains "Changes since v3.1:" line in BurnieCoinflip section
    - If any rngLocked/RngLocked references remain in BurnieCoinflip.sol comments, they are documented as findings
    - If any takeProfit/TakeProfit references remain in BurnieCoinflip.sol comments, they are documented as findings
    - Every finding uses the CMT-NNN format with What/Where/Why/Suggestion/Category/Severity fields
  </acceptance_criteria>
  <done>BurnieCoinflip.sol section complete in SUMMARY.md with all NatSpec, inline, and block comments verified. v3.1 fixes confirmed. New code changes audited for drift. All findings documented in CMT-NNN format.</done>
</task>

<task type="auto">
  <name>Task 2: Audit DegenerusQuests.sol and DegenerusJackpots.sol comments (2,277 lines combined)</name>
  <files>.planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md</files>
  <read_first>
    - contracts/DegenerusQuests.sol (full 1,588 lines)
    - contracts/DegenerusJackpots.sol (full 689 lines)
    - audit/v3.1-findings-35-peripheral-contracts.md (v3.1 findings CMT-059 through CMT-069, DRIFT-004)
    - .planning/phases/41-comment-scan-peripheral/41-RESEARCH.md (pre-identified findings and v3.1 status table)
    - .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md (append to existing file from Task 1)
  </read_first>
  <action>
Append DegenerusQuests.sol and DegenerusJackpots.sol audit sections to the existing 41-01-SUMMARY.md file.

**DegenerusQuests.sol audit (1,588 lines, 28 changed):**

Step 1: Verify v3.1 fixes are complete. Research says all 7 are FIXED:
- CMT-059 (line 16): Header now says "COIN and COINFLIP contracts" -- verify accuracy
- CMT-060 (line 24): `onlyCoinOrGame` reference removed, Security Model rewritten -- verify new text is accurate
- CMT-061 (line 23): Now says "COIN/COINFLIP-gated via `onlyCoin` modifier" -- verify modifier name matches
- CMT-062 (line 303): Now says "Slot 0 is fixed to MINT_ETH (no entropy used)" -- verify against actual code at line 303
- CMT-063 (lines 284,55): Error @notice says "COIN or COINFLIP"; modifier @dev says "COIN or COINFLIP" -- verify both
- CMT-064 (line 1370): Now says "credits streak on slot 0 completion" -- verify against actual behavior
- DRIFT-004 (line 153): `QUEST_TYPE_RESERVED` constant and skip guard removed -- verify no remaining references

Step 2: Audit quest type ID renumbering for drift:
- QUEST_TYPE_RESERVED was removed, causing IDs to shift: DECIMATOR went from 5 to 4, LOOTBOX from 6 to 5, etc.
- QUEST_TYPE_COUNT changed from 9 to 8
- Search ALL comments and NatSpec for hardcoded quest type ID numbers that may be stale
- Search for any remaining references to "QUEST_TYPE_RESERVED" or "reserved" quest type in comments
- Search for "9 quest types" or "nine quest" references that should now say "8"

Step 3: Full NatSpec and inline comment pass on all functions (same methodology as Task 1 Step 3).

**DegenerusJackpots.sol audit (689 lines, 14 changed):**

Step 1: Verify v3.1 fixes. Research says all 5 are FIXED but one may have swung to opposite inaccuracy:
- CMT-065 (line 35): Now says "BurnieCoinflip forwards flips" -- verify accuracy
- CMT-066 (line 164): Section header now says "COINFLIP CONTRACT HOOKS / Called by BurnieCoinflip" -- verify
- CMT-067 (lines 169,173): @dev and @custom:access now say "coinflip contract" -- verify
- CMT-068 (line 47): OnlyCoin error now says "restricted to the coinflip contract" -- BUT check if `onlyCoin` modifier at line ~150 accepts BOTH COIN and COINFLIP addresses. If it does, the error message saying "restricted to the coinflip contract" (singular) is still inaccurate -- it should say "coin or coinflip contract". THIS IS A PRE-IDENTIFIED FINDING from research.
- CMT-069 (line 19): Interface @notice now says "coinflip contract" -- verify

Step 2: Check for any remaining "BurnieCoin" references that should say "BurnieCoinflip" in comments.

Step 3: Full NatSpec and inline comment pass on all functions.

**Write findings:**
Continue CMT numbering from where Task 1 left off. Use the same format:
```
## DegenerusQuests.sol

**Scope:** 1,588 lines | ~X NatSpec tags | ~Y comment lines | Z external/public functions
**Changes since v3.1:** 28 lines changed (QUEST_TYPE_RESERVED removal, ID renumbering, NatSpec fixes)
**v3.1 findings status:** CMT-059 through CMT-064, DRIFT-004 — all verified [FIXED|PARTIAL]

### Findings

[findings]

## DegenerusJackpots.sol

**Scope:** 689 lines | ~X NatSpec tags | ~Y comment lines | Z external/public functions
**Changes since v3.1:** 14 lines changed (BurnieCoin->BurnieCoinflip fixes)
**v3.1 findings status:** CMT-065 through CMT-069 — all verified [FIXED|PARTIAL]

### Findings

[findings]
```

At the bottom of the file, add a summary table:
```
## Plan 01 Summary

| Contract | Lines | v3.1 Findings Verified | New Findings | Total |
|----------|-------|----------------------|--------------|-------|
| BurnieCoinflip.sol | 1,114 | X fixed, Y partial | Z | N |
| DegenerusQuests.sol | 1,588 | X fixed, Y partial | Z | N |
| DegenerusJackpots.sol | 689 | X fixed, Y partial | Z | N |
| **Total** | **3,391** | | | **N** |
```
  </action>
  <verify>
    <automated>grep -c "DegenerusQuests\|DegenerusJackpots" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## DegenerusQuests.sol" section header
    - File contains "## DegenerusJackpots.sol" section header
    - File contains "v3.1 findings status:" line in both contract sections
    - File contains "## Plan 01 Summary" with the totals table
    - DegenerusJackpots section addresses the OnlyCoin error / onlyCoin modifier mismatch (pre-identified finding from research: line 47 says "restricted to the coinflip contract" singular but modifier accepts both COIN and COINFLIP)
    - DegenerusQuests section addresses quest type ID renumbering audit results
    - Every finding uses the CMT-NNN format with What/Where/Why/Suggestion/Category/Severity fields
    - No contract source files (.sol) were modified
  </acceptance_criteria>
  <done>DegenerusQuests.sol and DegenerusJackpots.sol sections appended to SUMMARY.md. v3.1 fixes confirmed. Quest type renumbering audited. OnlyCoin/onlyCoin mismatch evaluated. All findings in CMT-NNN format. Plan 01 summary table present.</done>
</task>

</tasks>

<verification>
- `grep -c "CMT-" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md` returns finding count
- `grep "## BurnieCoinflip" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md` confirms section exists
- `grep "## DegenerusQuests" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md` confirms section exists
- `grep "## DegenerusJackpots" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md` confirms section exists
- `grep "## Plan 01 Summary" .planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md` confirms summary table exists
- No .sol files in `git diff --name-only` after execution (flag-only audit)
</verification>

<success_criteria>
All 3 heavy-change peripheral contracts (BurnieCoinflip, DegenerusQuests, DegenerusJackpots) have complete comment audit sections in 41-01-SUMMARY.md. Every NatSpec tag, inline comment, and block comment has been reviewed against current code. All v3.1 findings verified fixed or documented as partial. All new comment drift from code changes documented as findings. Summary table with per-contract counts present.
</success_criteria>

<output>
After completion, create `.planning/phases/41-comment-scan-peripheral/41-01-SUMMARY.md`
</output>
