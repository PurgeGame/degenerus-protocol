---
phase: 40-comment-scan-core-token
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.2-findings-40-token-contracts.md
autonomous: true
requirements:
  - CMT-03

must_haves:
  truths:
    - "Every v3.1 finding (CMT-041 through CMT-058) in BurnieCoin, DegenerusStonk, StakedDegenerusStonk, and WrappedWrappedXRP has a verified fix status"
    - "BurnieCoin rngLocked removal has NatSpec verified -- no remaining comments reference old rngLocked pattern in shortfall context"
    - "WrappedWrappedXRP partial fix CMT-057 is verified -- line 279 'Wrapping is disabled' is flagged if still present"
    - "WrappedWrappedXRP unfixed CMT-058 (VaultAllowanceSpent NatSpec) is re-flagged if still not fixed"
    - "Every external/public function across all 4 token contracts (70 total) has NatSpec verified against actual behavior"
    - "All section headers and block comments in BurnieCoin reflect post-coinflip-split architecture"
  artifacts:
    - path: "audit/v3.2-findings-40-token-contracts.md"
      provides: "Complete findings document for token contracts"
      contains: "## BurnieCoin.sol"
    - path: "audit/v3.2-findings-40-token-contracts.md"
      provides: "Small token contracts section"
      contains: "## DegenerusStonk.sol"
    - path: "audit/v3.2-findings-40-token-contracts.md"
      provides: "WrappedWrappedXRP section with partial fix verification"
      contains: "## WrappedWrappedXRP.sol"
  key_links:
    - from: "audit/v3.1-findings-34-token-contracts.md"
      to: "audit/v3.2-findings-40-token-contracts.md"
      via: "Fix verification cross-reference"
      pattern: "FIXED|PARTIAL|NOT FIXED"
---

<objective>
Fresh comment scan of the 4 token contracts (BurnieCoin.sol, DegenerusStonk.sol, StakedDegenerusStonk.sol, WrappedWrappedXRP.sol) totaling 2,153 lines. Two-pass methodology: (1) verify all 18 v3.1 findings were correctly fixed, (2) independent fresh scan. Special attention to BurnieCoin rngLocked removal and WrappedWrappedXRP known partial/unfixed items.

Purpose: Satisfy CMT-03 -- all comments in token contracts verified accurate, including post-sDGNRS/DGNRS-split changes and rngLocked removal.
Output: audit/v3.2-findings-40-token-contracts.md with per-contract sections, fix verification results, and any new findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-comment-scan-core-token/40-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan BurnieCoin.sol</name>
  <files>audit/v3.2-findings-40-token-contracts.md</files>
  <read_first>
    - contracts/BurnieCoin.sol (1,024 lines -- the contract being audited)
    - audit/v3.1-findings-34-token-contracts.md (v3.1 fix checklist -- contains CMT-041 through CMT-053 for BurnieCoin)
  </read_first>
  <action>
**Pass 1: Fix Verification (13 findings)**

For each v3.1 finding below, check the CURRENT file contents:

1. **CMT-041** (BurnieCoin.sol:213-227): v3.1 said orphaned DATA TYPES section documents structs that live in BurnieCoinflip. Verify the entire DATA TYPES section (lines 213-227) is removed. If any remnant remains, flag as PARTIAL.

2. **CMT-042** (BurnieCoin.sol:333-361): v3.1 said orphaned BOUNTY STATE section documents variables in BurnieCoinflip. Verify the entire section (~30 lines) is removed.

3. **CMT-043** (BurnieCoin.sol:663): v3.1 said onlyAdmin modifier uses OnlyGame() error without acknowledgment comment. Verify an inline comment like "// Reusing error for simplicity" or "// reuses OnlyGame()" is now present.

4. **CMT-044** (BurnieCoin.sol:854): v3.1 said burnCoin @dev has redundant "DegenerusGame, game, or affiliate". Verify it now says "game or affiliate (onlyTrustedContracts)" or equivalent non-redundant phrasing.

5. **CMT-045** (BurnieCoin.sol): v3.1 said Events header referenced coinflip/bounty. Verify the events section header no longer mentions coinflip or bounty.

6. **CMT-046** (BurnieCoin.sol): v3.1 said processCoinflipPayouts listed in modifier "Used for". Verify processCoinflipPayouts is removed from modifier description.

7. **CMT-047** (BurnieCoin.sol): v3.1 said creditCoin missing from onlyFlipCreditors "Used for" list. Verify creditCoin is now listed.

8. **CMT-048** (BurnieCoin.sol): v3.1 said onlyTrustedContracts missing from security header. Verify it is now listed in the security/access control section.

9. **CMT-049** (BurnieCoin.sol): v3.1 said section name should be "DECIMATOR & QUEST INTEGRATION HELPERS". Verify the section is renamed accordingly.

10. **CMT-050** (BurnieCoin.sol): v3.1 said vaultEscrow @dev should say "game contract or vault". Verify the NatSpec now mentions vault.

11. **CMT-051** (BurnieCoin.sol): v3.1 said _burn @dev references depositCoinflip but should reference burnCoin. Verify updated.

12. **CMT-052** (BurnieCoin.sol): v3.1 said NatSpec missing from _claimCoinflipShortfall and _consumeCoinflipShortfall. Verify NatSpec now exists on both functions.

13. **CMT-053** (BurnieCoin.sol): v3.1 said vault path undocumented in _mint and _burn @dev. Verify vault is now documented in both.

**rngLocked Removal Verification (Code Change):**
- The `if (degenerusGame.rngLocked()) return;` check was removed from both `_claimCoinflipShortfall` and `_consumeCoinflipShortfall`.
- New NatSpec was added referencing "BurnieCoinflip blocks auto-rebuy players during RNG lock."
- Verify this new NatSpec accurately describes the current architecture: BurnieCoinflip now handles RNG lock checks internally, BurnieCoin no longer checks rngLocked.
- Search for ANY remaining references to "rngLocked" or "rngLock" or "RNG lock" in BurnieCoin.sol comments. If any comment still says BurnieCoin checks rngLocked, flag as a new finding.

**Pass 2: Fresh Independent Scan**

BurnieCoin.sol (1,024 lines, 29 ext/pub functions):
- Read all NatSpec blocks systematically. For each of the 29 external/public functions, verify: @dev matches behavior, @param matches signature, @return matches returns, @custom:reverts lists actual reverts.
- Verify section headers (ERC20 STATE, MODIFIERS, MINTING, BURNING, COINFLIP INTEGRATION, etc.) match current code organization.
- Verify all modifier NatSpec (onlyGame, onlyAdmin, onlyFlipCreditors, onlyTrustedContracts) accurately describes access control.
- Verify no remaining references to pre-coinflip-split patterns (bounty, leaderboard, processCoinflipPayouts as a BurnieCoin function).
- Verify _mint and _burn internal function NatSpec accurately lists all callers and paths.

**Output format:** Create audit/v3.2-findings-40-token-contracts.md with:
- File header: Phase 40 Findings: Token Contracts, Date, Scope (4 contracts), Pass (v3.2 re-scan), Mode (flag-only)
- Summary table (Contract | CMT new | Fix verified | Fix partial | Fix not applied | Total) -- fill BurnieCoin row, leave other rows as placeholders
- BurnieCoin.sol section with: review scope, fix verification results table (one row per v3.1 finding), rngLocked removal verification, then any new findings in What/Where/Why/Suggestion/Category/Severity format
- Placeholder sections: "## DegenerusStonk.sol\n\n*Pending Task 2*", same for StakedDegenerusStonk.sol and WrappedWrappedXRP.sol
  </action>
  <verify>
    <automated>grep -c '## BurnieCoin.sol' audit/v3.2-findings-40-token-contracts.md && grep -c 'CMT-041' audit/v3.2-findings-40-token-contracts.md && grep -c 'CMT-053' audit/v3.2-findings-40-token-contracts.md && grep -c 'rngLock' audit/v3.2-findings-40-token-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-40-token-contracts.md exists and contains "## BurnieCoin.sol"
    - All 13 v3.1 finding IDs (CMT-041 through CMT-053) appear in the fix verification table
    - Each fix verification entry has a status of FIXED, PARTIAL, NOT FIXED, or NEW ISSUE
    - rngLocked removal has an explicit verification section with verdict
    - BurnieCoin fresh scan covers all 29 external/public functions
    - Any new findings use the What/Where/Why/Suggestion/Category/Severity format
    - File contains placeholder sections for the other 3 token contracts
  </acceptance_criteria>
  <done>BurnieCoin.sol has complete fix verification (13 findings + rngLocked change) and fresh independent scan. All findings documented in the standard format.</done>
</task>

<task type="auto">
  <name>Task 2: Scan DegenerusStonk, StakedDegenerusStonk, WrappedWrappedXRP and finalize</name>
  <files>audit/v3.2-findings-40-token-contracts.md</files>
  <read_first>
    - contracts/DegenerusStonk.sol (226 lines -- the contract being audited)
    - contracts/StakedDegenerusStonk.sol (514 lines -- the contract being audited)
    - contracts/WrappedWrappedXRP.sol (389 lines -- the contract being audited)
    - audit/v3.2-findings-40-token-contracts.md (current state from Task 1 -- must read before modifying)
    - audit/v3.1-findings-34-token-contracts.md (v3.1 fix checklist -- contains CMT-054 through CMT-058)
  </read_first>
  <action>
**Pass 1: Fix Verification (5 findings)**

1. **CMT-054** (DegenerusStonk.sol): v3.1 said _transfer had no NatSpec and self-transfer block was undocumented. Verify NatSpec was added to _transfer and an inline comment explains the self-transfer revert.

2. **CMT-055** (DegenerusStonk.sol): v3.1 said transfer/transferFrom missing @custom:reverts Unauthorized. Verify @custom:reverts Unauthorized is now present on both functions.

3. **CMT-056** (StakedDegenerusStonk.sol): v3.1 said 3 NatSpec lines still used "DGNRS" where they should say "sDGNRS". Verify all 3 are corrected. Search for any remaining instances where NatSpec says "DGNRS" when referring to the staked token (should be "sDGNRS").

4. **CMT-057** (WrappedWrappedXRP.sol): v3.1 said contract-level header and section header both reference nonexistent wrap function. Research indicates PARTIAL fix: line 19 was fixed ("No wrap function") but line 279 still says "Wrapping is disabled." Verify: (a) line 19 no longer says wrapping is disabled and instead accurately describes the contract, (b) find line ~279 and check if it still says "Wrapping is disabled" -- if so, re-flag as a still-open finding.

5. **CMT-058** (WrappedWrappedXRP.sol): v3.1 said VaultAllowanceSpent event @param says "Amount that was spent" but the event emits `vault.remaining` (the remaining allowance, not the spent amount). Research indicates NOT FIXED in the diff. Verify: locate the VaultAllowanceSpent event definition, check if the @param NatSpec now says "remaining allowance" or similar. If still says "spent", re-flag.

**Pass 2: Fresh Independent Scan**

DegenerusStonk.sol (226 lines, 12 ext/pub functions):
- Read all NatSpec blocks. For each function verify @dev, @param, @return, @custom:reverts.
- Verify contract-level @dev accurately describes the token (non-transferable governance token with admin-only minting).
- Verify modifier NatSpec (onlyAdmin) matches actual access control.
- Check that the sDGNRS/DGNRS split is correctly documented -- DegenerusStonk is DGNRS (the base token), not sDGNRS.

StakedDegenerusStonk.sol (514 lines, 18 ext/pub functions):
- Read all NatSpec blocks. For each of the 18 functions verify @dev, @param, @return, @custom:reverts.
- Verify staking/unstaking NatSpec accurately describes the share math.
- Verify all references correctly distinguish between DGNRS (the deposit token) and sDGNRS (the share/receipt token).
- Verify pool NatSpec describes actual pool mechanics (deposit, withdrawal, reward distribution).
- Check for any remaining "DGNRS" usage where "sDGNRS" is meant (the CMT-056 fix addresses 3 instances but there may be more).

WrappedWrappedXRP.sol (389 lines, 11 ext/pub functions):
- Read all NatSpec blocks. For each of the 11 functions verify @dev, @param, @return, @custom:reverts.
- Verify contract-level description accurately describes what WrappedWrappedXRP is (wrapped version of WXRP for protocol use).
- Verify all event NatSpec matches actual emitted values (not just event names).
- Verify vault interaction NatSpec accurately describes the allowance/spend pattern.
- Search for any remaining references to "wrapping" or "wrap function" that imply users can wrap tokens when they cannot.

**Finalize findings document:**
- Replace all 3 placeholder sections with full contract sections (review scope, fix verification table, new findings).
- Update the summary table at the top with final counts across all 4 contracts.
- Add a "## Phase 40 Plan 02 Summary" section at the bottom with: total lines scanned, total functions verified, total v3.1 findings verified (18), total new findings, overall verdict for CMT-03.
  </action>
  <verify>
    <automated>grep -c 'Pending Task 2' audit/v3.2-findings-40-token-contracts.md | grep '^0$' && grep -c '## DegenerusStonk.sol' audit/v3.2-findings-40-token-contracts.md && grep -c '## StakedDegenerusStonk.sol' audit/v3.2-findings-40-token-contracts.md && grep -c '## WrappedWrappedXRP.sol' audit/v3.2-findings-40-token-contracts.md && grep -c 'CMT-057' audit/v3.2-findings-40-token-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-40-token-contracts.md no longer contains "Pending Task 2"
    - DegenerusStonk.sol section contains fix verification for CMT-054, CMT-055
    - StakedDegenerusStonk.sol section contains fix verification for CMT-056
    - WrappedWrappedXRP.sol section contains fix verification for CMT-057, CMT-058
    - CMT-057 has explicit status noting whether line ~279 "Wrapping is disabled" is still present
    - CMT-058 has explicit status noting whether VaultAllowanceSpent event @param is still incorrect
    - Fresh scan covers all 41 ext/pub functions across the 3 contracts
    - Summary table has accurate counts for all 4 token contracts
    - File contains "## Phase 40 Plan 02 Summary" with total lines scanned, functions verified, and CMT-03 verdict
    - All 18 v3.1 findings (CMT-041 through CMT-058) have explicit fix status across the full document
  </acceptance_criteria>
  <done>All 4 token contracts have complete fix verification and fresh independent scan. CMT-03 has an explicit overall verdict with evidence. Findings document is complete and self-contained, ready for Phase 43 consolidation.</done>
</task>

</tasks>

<verification>
- audit/v3.2-findings-40-token-contracts.md exists with 4 contract sections
- All 18 v3.1 findings have fix status verdicts
- rngLocked removal in BurnieCoin has explicit verification
- Known partial fix (CMT-057) and unfixed item (CMT-058) explicitly addressed
- Summary table has per-contract counts
- CMT-03 has explicit overall verdict
- No "Pending" placeholders remain
</verification>

<success_criteria>
CMT-03 is fully satisfied: BurnieCoin, DegenerusStonk, StakedDegenerusStonk, and WrappedWrappedXRP comments have all been verified against current code, with every v3.1 finding accounted for (including known partial/unfixed items) and a fresh independent scan completed. The findings document is ready for Phase 43 consolidation.
</success_criteria>

<output>
After completion, create `.planning/phases/40-comment-scan-core-token/40-02-SUMMARY.md`
</output>
