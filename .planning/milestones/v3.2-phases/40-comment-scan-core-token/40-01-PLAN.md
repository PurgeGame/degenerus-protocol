---
phase: 40-comment-scan-core-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.2-findings-40-core-game-contracts.md
autonomous: true
requirements:
  - CMT-02

must_haves:
  truths:
    - "Every v3.1 finding (CMT-001 through CMT-010, DRIFT-001, DRIFT-002) in DegenerusAdmin, GameStorage, and DegenerusGame has a verified fix status (FIXED / PARTIAL / NOT FIXED / NEW ISSUE)"
    - "Every external/public function in DegenerusAdmin (22 functions) has its NatSpec verified against actual behavior"
    - "Every external/public function in DegenerusGame (51 functions) has its NatSpec verified against actual behavior"
    - "All storage layout comments in DegenerusGameStorage are verified against actual storage variables"
    - "The deleted jackpot payout block in DegenerusGame is evaluated for whether replacement documentation is needed"
    - "CMT-003 (misplaced SLOT 1 header in GameStorage) is re-verified and re-flagged if still present"
  artifacts:
    - path: "audit/v3.2-findings-40-core-game-contracts.md"
      provides: "Complete findings document for core game contracts"
      contains: "## DegenerusAdmin.sol"
    - path: "audit/v3.2-findings-40-core-game-contracts.md"
      provides: "GameStorage section with storage layout verification"
      contains: "## DegenerusGameStorage.sol"
    - path: "audit/v3.2-findings-40-core-game-contracts.md"
      provides: "DegenerusGame section with all 51 functions verified"
      contains: "## DegenerusGame.sol"
  key_links:
    - from: "audit/v3.1-findings-31-core-game-contracts.md"
      to: "audit/v3.2-findings-40-core-game-contracts.md"
      via: "Fix verification cross-reference"
      pattern: "FIXED|PARTIAL|NOT FIXED"
---

<objective>
Fresh comment scan of the 3 core game contracts (DegenerusAdmin.sol, DegenerusGameStorage.sol, DegenerusGame.sol) totaling 5,262 lines. Two-pass methodology: (1) verify all 12 v3.1 findings were correctly fixed in the working tree, (2) independent fresh scan catching anything v3.1 missed or that fixes introduced.

Purpose: Satisfy CMT-02 -- all comments in core game contracts verified accurate against current code behavior, including post-v3.1 fix changes and post-governance changes.
Output: audit/v3.2-findings-40-core-game-contracts.md with per-contract sections, fix verification results, and any new findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-comment-scan-core-token/40-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan DegenerusAdmin.sol and DegenerusGameStorage.sol</name>
  <files>audit/v3.2-findings-40-core-game-contracts.md</files>
  <read_first>
    - contracts/DegenerusAdmin.sol (800 lines -- the contract being audited)
    - contracts/storage/DegenerusGameStorage.sol (1,625 lines -- the contract being audited)
    - audit/v3.1-findings-31-core-game-contracts.md (v3.1 fix checklist -- contains CMT-001 through CMT-004, DRIFT-001, DRIFT-002)
  </read_first>
  <action>
**Pass 1: Fix Verification (6 findings)**

For each v3.1 finding below, check the CURRENT file contents (not the diff -- the files now contain the applied fixes):

1. **CMT-001** (DegenerusAdmin.sol:38): v3.1 said header says "60%" but threshold starts at 50%. Verify line 38 now says "50% -> 5%". If it says "50%" the fix is correct. Grep for any other "60%" references in the file.

2. **CMT-002** (DegenerusAdmin.sol:41): v3.1 said "Death clock pauses while any proposal is active" is stale. Verify line 41 is deleted or no longer contains "death clock" or "pauses while any proposal is active".

3. **DRIFT-001** (DegenerusAdmin.sol:70): v3.1 said jackpotPhase() is vestigial in IDegenerusGameAdmin interface. Verify no `jackpotPhase` declaration remains in the interface. Also verify the interface @dev comment (around line 67) no longer mentions "liveness" -- if it still says "liveness" flag as a new finding since no remaining member implements liveness.

4. **DRIFT-002** (DegenerusAdmin.sol:392-398): v3.1 said propose() NatSpec omits 1-per-address limit. Verify the @dev block now includes text about "one active proposal" or "AlreadyHasActiveProposal".

5. **CMT-003** (DegenerusGameStorage.sol): v3.1 said misplaced SLOT 1 header. Research indicates this was NOT in the diff (likely deferred). Re-scan the storage layout section. If the SLOT 1 header is still misplaced (i.e., it appears before a group of variables that are NOT in slot 1), re-flag it with the same description.

6. **CMT-004** (DegenerusGameStorage.sol): v3.1 said free-floating NatSpec (4 orphaned lines). Verify those 4 lines are deleted.

**Pass 2: Fresh Independent Scan**

DegenerusAdmin.sol (800 lines, 22 ext/pub functions):
- Read all NatSpec blocks systematically. For each external/public function, verify: @dev description matches actual behavior, @param names and descriptions match signature, @return matches actual return value(s), @custom:reverts lists actual revert conditions.
- Verify block comment headers (governance architecture, threshold decay, proposal lifecycle) match current code.
- Verify inline comments at decision points (threshold calculation, vote counting, timelock checks) are accurate.

DegenerusGameStorage.sol (1,625 lines, 0 ext/pub functions -- storage-only):
- Verify all SLOT N headers match actual storage layout order.
- Verify NatSpec on each state variable describes its actual purpose and usage.
- Verify section headers (GAME STATE, PLAYER STATE, ECONOMY STATE, etc.) group variables correctly.
- Verify any packed storage layout comments accurately describe bit widths and field positions.
- Cross-reference storage variables against their usage in DegenerusGame.sol to confirm descriptions are accurate.

**Output format:** Create audit/v3.2-findings-40-core-game-contracts.md with:
- File header: Phase 40 Findings: Core Game Contracts, Date, Scope (3 contracts), Pass (v3.2 re-scan), Mode (flag-only)
- Summary table (Contract | CMT new | Fix verified | Fix partial | Fix not applied | Total)
- Per-contract section with: review scope (line count, NatSpec count, function count), fix verification results table (one row per v3.1 finding: ID | Status | Notes), then any new findings in the same format as v3.1 (What/Where/Why/Suggestion/Category/Severity)
- For DegenerusAdmin.sol section and DegenerusGameStorage.sol section only in this task. Leave a placeholder line "## DegenerusGame.sol" followed by "*Pending Task 2*" for the next task.
  </action>
  <verify>
    <automated>grep -c '## DegenerusAdmin.sol' audit/v3.2-findings-40-core-game-contracts.md && grep -c '## DegenerusGameStorage.sol' audit/v3.2-findings-40-core-game-contracts.md && grep -c 'CMT-001' audit/v3.2-findings-40-core-game-contracts.md && grep -c 'DRIFT-001' audit/v3.2-findings-40-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-40-core-game-contracts.md exists and contains "## DegenerusAdmin.sol"
    - audit/v3.2-findings-40-core-game-contracts.md contains "## DegenerusGameStorage.sol"
    - Every v3.1 finding ID (CMT-001, CMT-002, CMT-003, CMT-004, DRIFT-001, DRIFT-002) appears in the fix verification table
    - Each fix verification entry has a status of FIXED, PARTIAL, NOT FIXED, or NEW ISSUE
    - DegenerusAdmin fresh scan covers all 22 external/public functions (each has a verdict line or is part of a "MATCH" batch)
    - DegenerusGameStorage fresh scan covers storage layout headers and all variable NatSpec
    - Any new findings use the What/Where/Why/Suggestion/Category/Severity format
    - File contains placeholder "## DegenerusGame.sol" for Task 2
  </acceptance_criteria>
  <done>DegenerusAdmin.sol and DegenerusGameStorage.sol both have complete fix verification AND fresh independent scan results in the findings document. All 6 v3.1 findings have explicit fix status verdicts.</done>
</task>

<task type="auto">
  <name>Task 2: Scan DegenerusGame.sol and finalize findings document</name>
  <files>audit/v3.2-findings-40-core-game-contracts.md</files>
  <read_first>
    - contracts/DegenerusGame.sol (2,837 lines -- the contract being audited)
    - audit/v3.2-findings-40-core-game-contracts.md (current state from Task 1 -- must read before modifying)
    - audit/v3.1-findings-31-core-game-contracts.md (v3.1 fix checklist -- contains CMT-005 through CMT-010)
  </read_first>
  <action>
**Pass 1: Fix Verification (6 findings)**

For each v3.1 finding below, check the CURRENT file contents:

1. **CMT-005** (DegenerusGame.sol): v3.1 said block comment references "18h" but code uses 12h. Verify the comment now says "12h" (or the line was removed). Grep for any remaining "18h" references.

2. **CMT-006** (DegenerusGame.sol): v3.1 said jackpot payout section header had inaccurate prize pool info. This was fixed by DELETING the entire 12-line block comment. Verify the block is gone. Then evaluate: does the code region that followed the deleted block comment (admin reward vault section) need any replacement documentation? If it is self-documenting, note "deletion adequate." If a gap exists, flag as a new finding suggesting a corrected block comment.

3. **CMT-007** (DegenerusGame.sol): Same block as CMT-006 -- verify deletion covers this finding too.

4. **CMT-008** (DegenerusGame.sol): Same block as CMT-006 -- verify deletion covers this finding too.

5. **CMT-009** (DegenerusGame.sol): v3.1 said futurePrizePoolTotalView function was stale. Verify the function is entirely removed (no `futurePrizePoolTotalView` string in the file). Grep to confirm.

6. **CMT-010** (DegenerusGame.sol): v3.1 said orphaned NatSpec line existed. Verify the orphaned line is deleted.

**Pass 2: Fresh Independent Scan**

DegenerusGame.sol (2,837 lines, 51 ext/pub functions):
- Read all NatSpec blocks systematically. For each of the 51 external/public functions, verify: @dev matches behavior, @param matches signature, @return matches returns, @custom:reverts lists actual reverts.
- Verify the module delegatecall section: are the listed modules in the header comment accurate against the actual module addresses used in delegatecall?
- Verify inline comments in the core game loop (advance, roll, payout) match actual logic.
- Verify block comment headers (GAME LIFECYCLE, PLAYER ACTIONS, ECONOMY, etc.) match current section contents.
- Verify any comments referencing other contracts (BurnieCoin, BurnieCoinflip, DegenerusVault) describe the current interaction pattern (not pre-split patterns).
- Pay special attention to the region where the jackpot block was deleted -- verify surrounding code context is still coherent.

**Finalize findings document:**
- Replace the "## DegenerusGame.sol" placeholder with the full DegenerusGame.sol section (review scope, fix verification table, new findings).
- Update the summary table at the top of the file with final counts across all 3 contracts.
- Add a "## Phase 40 Plan 01 Summary" section at the bottom with: total lines scanned, total functions verified, total v3.1 findings verified, total new findings, overall verdict for CMT-02.
  </action>
  <verify>
    <automated>grep -c 'Pending Task 2' audit/v3.2-findings-40-core-game-contracts.md | grep '^0$' && grep -c '## DegenerusGame.sol' audit/v3.2-findings-40-core-game-contracts.md && grep -c 'CMT-005' audit/v3.2-findings-40-core-game-contracts.md && grep -c 'CMT-010' audit/v3.2-findings-40-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-40-core-game-contracts.md no longer contains "Pending Task 2"
    - DegenerusGame.sol section contains fix verification for CMT-005, CMT-006, CMT-007, CMT-008, CMT-009, CMT-010
    - Each fix verification entry has a status of FIXED, PARTIAL, NOT FIXED, or NEW ISSUE
    - Fresh scan covers all 51 external/public functions (each has a verdict or is part of a batch summary)
    - Summary table at the top has accurate counts for all 3 contracts
    - File contains "## Phase 40 Plan 01 Summary" section with total lines scanned, functions verified, and CMT-02 verdict
    - All 12 v3.1 findings (CMT-001 through CMT-010, DRIFT-001, DRIFT-002) have explicit fix status across the document
  </acceptance_criteria>
  <done>All 3 core game contracts have complete fix verification and fresh independent scan. CMT-02 has an explicit overall verdict with evidence. Findings document is complete and self-contained.</done>
</task>

</tasks>

<verification>
- audit/v3.2-findings-40-core-game-contracts.md exists with 3 contract sections
- All 12 v3.1 findings have fix status verdicts
- Summary table has per-contract counts
- CMT-02 has explicit overall verdict
- No "Pending" placeholders remain
</verification>

<success_criteria>
CMT-02 is fully satisfied: DegenerusGame, DegenerusGameStorage, and DegenerusAdmin comments have all been verified against current code, with every v3.1 finding accounted for and a fresh independent scan completed. The findings document is ready for Phase 43 consolidation.
</success_criteria>

<output>
After completion, create `.planning/phases/40-comment-scan-core-token/40-01-SUMMARY.md`
</output>
