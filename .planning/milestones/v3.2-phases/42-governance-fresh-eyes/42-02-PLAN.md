---
phase: 42-governance-fresh-eyes
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - audit/v3.2-governance-fresh-eyes.md
autonomous: true
requirements:
  - GOV-03

must_haves:
  truths:
    - "All 7 state variables reset by updateVrfCoordinatorAndSub are traced from DegenerusAdmin._executeSwap through DegenerusGame delegatecall to AdvanceModule storage writes"
    - "lastVrfProcessedTimestamp non-reset is verified as intentional with explicit reasoning"
    - "unwrapTo 5h guard is verified against DegenerusStonk.sol source with exact line reference and lastVrfProcessed() call chain"
    - "circulatingSupply accounting is verified: totalSupply - self-held - DGNRS-held, with each component traced to StakedDegenerusStonk.sol"
    - "sDGNRS soulbound invariant is verified: no transfer(), no transferFrom(), no approve() public functions"
    - "No stale state persists after a governance swap that could create exploitable conditions"
  artifacts:
    - path: "audit/v3.2-governance-fresh-eyes.md"
      provides: "GOV-03 cross-contract state consistency verification and executive summary"
      contains: "## GOV-03"
  key_links:
    - from: "contracts/DegenerusAdmin.sol"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "updateVrfCoordinatorAndSub delegatecall through DegenerusGame"
      pattern: "updateVrfCoordinatorAndSub"
    - from: "contracts/DegenerusStonk.sol"
      to: "contracts/DegenerusGame.sol"
      via: "lastVrfProcessed() view call for unwrapTo 5h guard"
      pattern: "lastVrfProcessed.*5 hours"
    - from: "contracts/StakedDegenerusStonk.sol"
      to: "contracts/DegenerusAdmin.sol"
      via: "balanceOf for vote weight and circulatingSupply calculation"
      pattern: "circulatingSupply"
---

<objective>
Verify cross-contract governance state consistency across the 5 governance-relevant contracts (GOV-03). Trace the full state reset path from DegenerusAdmin._executeSwap through DegenerusGame delegatecall to AdvanceModule, verify the unwrapTo vote-stacking guard, verify circulatingSupply accounting, and confirm the sDGNRS soulbound invariant. Write the executive summary for the complete findings document.

Purpose: Ensure that governance actions produce consistent state across all contracts involved, with no stale state that could create exploitable conditions after a VRF coordinator swap.

Output: GOV-03 section and executive summary appended to `audit/v3.2-governance-fresh-eyes.md`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-governance-fresh-eyes/42-RESEARCH.md
@.planning/phases/42-governance-fresh-eyes/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-contract state consistency verification and executive summary (GOV-03)</name>
  <files>audit/v3.2-governance-fresh-eyes.md</files>
  <read_first>
    contracts/DegenerusAdmin.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/storage/DegenerusGameStorage.sol
    contracts/DegenerusStonk.sol
    contracts/StakedDegenerusStonk.sol
    contracts/DegenerusGame.sol
    audit/v3.2-governance-fresh-eyes.md
    .planning/phases/42-governance-fresh-eyes/42-RESEARCH.md
  </read_first>
  <action>
Append the GOV-03 section and executive summary to `audit/v3.2-governance-fresh-eyes.md`.

**GOV-03 section: Cross-Contract Governance State Consistency**

Structure with these mandatory subsections:

1. **State Reset Trace: _executeSwap -> updateVrfCoordinatorAndSub** -- Trace the full call path:

   Step 1: DegenerusAdmin._executeSwap (verify exact line ~605-610) calls:
   ```solidity
   IDegenerusGameAdmin(game).updateVrfCoordinatorAndSub(
       p.newCoordinator, newSubId, p.newKeyHash
   );
   ```
   - Verify the exact line number and arguments passed
   - Verify this is called AFTER `_voidAllActive` (CEI ordering confirmed in GOV-01)

   Step 2: DegenerusGame receives the call. Find the `updateVrfCoordinatorAndSub` dispatch in DegenerusGame.sol:
   - It should be a `delegatecall` to AdvanceModule
   - Document the exact dispatch mechanism (fallback? explicit function? selector routing?)
   - Document the line reference in DegenerusGame.sol

   Step 3: DegenerusGameAdvanceModule.updateVrfCoordinatorAndSub executes (lines 1258-1276 per research):
   - Access control: `if (msg.sender != ContractAddresses.ADMIN) revert E()` -- verify this check exists and uses the correct ADMIN address
   - State changes (document each with its exact line number and the storage variable it writes to in DegenerusGameStorage):

   | # | Variable | Written Value | AdvanceModule Line | Storage Declaration Line |
   |---|----------|---------------|-------------------|-------------------------|
   | 1 | vrfCoordinator | newCoordinator (cast to IVRFCoordinator) | ~1263 | Find in GameStorage |
   | 2 | vrfSubscriptionId | newSubId | ~1264 | Find in GameStorage |
   | 3 | vrfKeyHash | newKeyHash | ~1265 | Find in GameStorage |
   | 4 | rngLockedFlag | false | ~1268 | Find in GameStorage |
   | 5 | vrfRequestId | 0 | ~1269 | Find in GameStorage |
   | 6 | rngRequestTime | 0 | ~1270 | Find in GameStorage |
   | 7 | rngWordCurrent | 0 | ~1271 | Find in GameStorage |

   For each variable, verify:
   - The storage declaration exists in DegenerusGameStorage.sol (find exact line)
   - The reset value is appropriate (false/0 means "clean state ready for new coordinator")
   - No other variable should be reset but is not (check for any VRF-related state in GameStorage that is NOT in this list)

   Step 4: Verify `lastVrfProcessedTimestamp` is NOT reset:
   - Find `lastVrfProcessedTimestamp` in DegenerusGameStorage.sol -- document its declaration line
   - Confirm it does NOT appear in `updateVrfCoordinatorAndSub`
   - Document the implication: stall timer continues from original stall, meaning governance can trigger again immediately if new coordinator doesn't respond
   - Evaluate: This is intentional -- if lastVrfProcessedTimestamp WERE reset, a malicious coordinator would get a free 20h window before governance could replace it. Not resetting is the safer choice.
   - Classification: BY DESIGN (not a finding)

2. **lastVrfProcessedTimestamp Lifecycle** -- Trace all write paths:
   - Set in `_applyDailyRng` (find exact line in AdvanceModule, should be around line 1360): set to `block.timestamp` when VRF word is successfully applied
   - Set in `wireVrf` (find exact line in AdvanceModule, should be around line 402): set to `block.timestamp` at deploy/wire time
   - Read in `lastVrfProcessed()` view function (find in DegenerusGame.sol): returns `lastVrfProcessedTimestamp` storage value
   - Consumed by: DegenerusAdmin (stall check in propose/vote), DegenerusStonk (unwrapTo 5h guard)
   - Verify: No other write path exists. Search AdvanceModule and GameStorage for `lastVrfProcessedTimestamp` assignments.

3. **unwrapTo Vote-Stacking Guard Verification** -- Full trace of the DegenerusStonk unwrapTo guard:
   - Find `unwrapTo` in DegenerusStonk.sol (lines 149-158 per research)
   - Document the guard: `if (block.timestamp - IDegenerusGame(ContractAddresses.GAME).lastVrfProcessed() > 5 hours) revert Unauthorized()`
   - Verify the call chain: DegenerusStonk calls `lastVrfProcessed()` on DegenerusGame, which returns `lastVrfProcessedTimestamp` from storage
   - Verify the 5h threshold is hardcoded (not configurable via governance or admin)
   - Verify the timing relationship: 5h < 20h (admin governance) < 168h (community governance). unwrapTo is blocked for at least 15h before governance becomes possible.
   - Document what unwrapTo does: burns sDGNRS from msg.sender, transfers DGNRS to recipient via `stonk.wrapperTransferTo`. This is the DGNRS->sDGNRS conversion path (unwrap = sDGNRS back to DGNRS).
   - Wait -- verify direction: `unwrapTo` should UNWRAP, meaning convert sDGNRS back to DGNRS. But the comment says "prevents creator vote-stacking". The vote-stacking concern is acquiring MORE sDGNRS during a stall. So verify: is there a `wrapTo` or equivalent that converts DGNRS->sDGNRS? Or does `unwrapTo` go the other direction? Read the function carefully:
     - `_burn(msg.sender, amount)` -- burns sDGNRS from the DegenerusStonk contract (which is the DGNRS wrapper)
     - `stonk.wrapperTransferTo(recipient, amount)` -- calls the sDGNRS contract to transfer sDGNRS
     - This appears to be: burn DGNRS, give sDGNRS. So `unwrapTo` converts DGNRS to sDGNRS for a recipient. Blocking this during stall prevents DGNRS holders from gaining sDGNRS vote weight.
   - CRITICAL: Independently verify the direction by reading the function. The research says "DGNRS->sDGNRS conversion path" and blocking prevents "vote-stacking". Document your finding.

4. **circulatingSupply Accounting Verification** -- Trace the calculation:
   - Find `circulatingSupply()` in DegenerusAdmin.sol (should call sDGNRS interface)
   - Verify the formula: `sDGNRS.totalSupply() - sDGNRS.balanceOf(SDGNRS) - sDGNRS.balanceOf(DGNRS)`
   - Trace each component:
     - `totalSupply()`: all minted sDGNRS (find in StakedDegenerusStonk.sol)
     - `balanceOf(SDGNRS)`: undistributed pools held by the sDGNRS contract itself (game reward pools not yet distributed)
     - `balanceOf(DGNRS)`: sDGNRS allocated to the DGNRS wrapper contract (backing for wrapped tokens)
   - Verify: Does this correctly represent "sDGNRS in the hands of voters"?
   - Edge cases:
     - If all sDGNRS is in pools (totalSupply == self-held + DGNRS-held): circulatingSupply = 0. Verify propose() handles this (should revert).
     - If sDGNRS is burned: totalSupply decreases, circulating decreases. circulatingSnapshot (taken at propose time) is higher than current circulating. Threshold becomes harder to meet (as analyzed in GOV-02 OQ-3).

5. **sDGNRS Soulbound Invariant** -- Verify sDGNRS cannot be transferred:
   - Read StakedDegenerusStonk.sol public/external function list
   - Verify: NO `transfer(address, uint256)` function exists
   - Verify: NO `transferFrom(address, address, uint256)` function exists
   - Verify: NO `approve(address, uint256)` function exists
   - Document all functions that CAN change sDGNRS balances:
     - `transferFromPool(address, address, uint256)`: restricted to `onlyGame` modifier -- only DegenerusGame can call
     - `burnRemainingPools(uint256)`: restricted to `onlyGame` -- burns pool tokens at game over
     - Any burn function: check access control
     - `wrapperTransferTo(address, uint256)`: check access control -- should be restricted to DGNRS contract
   - Conclusion: sDGNRS balances can only change via game operations (which require VRF, blocked during stall) or burns (which reduce supply and hurt attackers). Flash loans impossible. Direct transfers impossible.

6. **Cross-Contract Consistency Matrix** -- Create a summary matrix showing all governance-relevant state and which contracts read/write it:

   | State Variable | Written By | Read By | Reset on Swap? |
   |---------------|------------|---------|----------------|
   | vrfCoordinator | AdvanceModule | AdvanceModule (VRF calls) | YES |
   | vrfSubscriptionId | AdvanceModule | AdvanceModule (VRF calls) | YES |
   | vrfKeyHash | AdvanceModule | AdvanceModule (VRF calls) | YES |
   | rngLockedFlag | AdvanceModule | Multiple modules, DegenerusAdmin | YES |
   | vrfRequestId | AdvanceModule | AdvanceModule | YES |
   | rngRequestTime | AdvanceModule | AdvanceModule | YES |
   | rngWordCurrent | AdvanceModule | AdvanceModule | YES |
   | lastVrfProcessedTimestamp | AdvanceModule | DegenerusAdmin, DegenerusStonk | NO (by design) |
   | proposal state | DegenerusAdmin | DegenerusAdmin | N/A (managed internally) |
   | circulatingSnapshot | DegenerusAdmin | DegenerusAdmin | N/A (per proposal) |

   Verify each row against actual code. Add any governance-relevant state variables not listed.

7. **GOV-03 Verdict** -- Overall verdict for cross-contract state consistency. Expected: SAFE -- all 7 state variables correctly reset, lastVrfProcessedTimestamp non-reset is intentional, unwrapTo guard correctly prevents vote-stacking, circulatingSupply accounting is sound, sDGNRS soulbound invariant holds completely.

**Executive Summary** -- Fill in the executive summary placeholder at the top of the document:

```
## Executive Summary

### Overall Verdict: [SAFE / CONCERN]

The VRF swap governance system has been independently re-verified from a fresh perspective.
[1-2 sentences summarizing the overall finding]

### Per-Requirement Verdicts

| Requirement | Verdict | One-Line Reasoning |
|-------------|---------|-------------------|
| GOV-01 | [verdict] | [one line] |
| GOV-02 | [verdict] | [one line] |
| GOV-03 | [verdict] | [one line] |

### Known Issue Re-Verification

| Finding | Original Severity | Current Status | Notes |
|---------|-------------------|----------------|-------|
| WAR-01 | MEDIUM | [Confirmed / Changed] | [brief] |
| WAR-02 | MEDIUM | [Confirmed / Changed] | [brief] |
| WAR-06 | LOW | [Confirmed / Changed] | [brief] |
| GOV-07 | LOW (Fixed) | [Confirmed Fixed] | [brief] |
| VOTE-03 | LOW (Fixed) | [Confirmed Fixed] | [brief] |

### Findings Summary

| Severity | Count | Details |
|----------|-------|---------|
| HIGH | [N] | [details or --] |
| MEDIUM | [N] | [details or --] |
| LOW | [N] | [details or --] |
| INFO | [N] | [details or --] |
```

Fill in based on the GOV-01, GOV-02, and GOV-03 sections already in the document. Ensure the executive summary accurately reflects all verdicts and findings from the full document.
  </action>
  <verify>
    <automated>grep -c "## GOV-03" audit/v3.2-governance-fresh-eyes.md && grep -c "updateVrfCoordinatorAndSub" audit/v3.2-governance-fresh-eyes.md && grep -c "lastVrfProcessedTimestamp" audit/v3.2-governance-fresh-eyes.md && grep -c "circulatingSupply" audit/v3.2-governance-fresh-eyes.md && grep -c "soulbound" audit/v3.2-governance-fresh-eyes.md && grep -c "Executive Summary" audit/v3.2-governance-fresh-eyes.md && grep -c "Overall Verdict" audit/v3.2-governance-fresh-eyes.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-governance-fresh-eyes.md contains "## GOV-03"
    - Document contains "updateVrfCoordinatorAndSub" at least 5 times (call path trace, state reset table, each contract reference)
    - Document contains a state reset table with all 7 variables (vrfCoordinator, vrfSubscriptionId, vrfKeyHash, rngLockedFlag, vrfRequestId, rngRequestTime, rngWordCurrent) with line numbers from both AdvanceModule and GameStorage
    - Document contains "lastVrfProcessedTimestamp" with explicit "NOT reset" and "BY DESIGN" classification
    - Document contains "lastVrfProcessed()" lifecycle trace with _applyDailyRng and wireVrf write paths
    - Document contains "unwrapTo" with 5h guard verification and direction analysis
    - Document contains "circulatingSupply" with formula verification (totalSupply - self-held - DGNRS-held)
    - Document contains "soulbound" with function-by-function verification (no transfer, no transferFrom, no approve)
    - Document contains "transferFromPool" and "onlyGame" in sDGNRS balance-change enumeration
    - Document contains cross-contract consistency matrix with "Written By" and "Read By" columns
    - Document contains "Executive Summary" with "Overall Verdict"
    - Executive summary contains per-requirement verdict table (GOV-01, GOV-02, GOV-03)
    - Executive summary contains known issue re-verification table (WAR-01, WAR-02, WAR-06, GOV-07, VOTE-03)
    - Executive summary contains findings summary table with severity counts
    - Document contains "GOV-03 Verdict" with SAFE or CONCERN
  </acceptance_criteria>
  <done>GOV-03 cross-contract state consistency verification complete with full state reset trace (7 variables with line numbers), lastVrfProcessedTimestamp lifecycle and non-reset analysis, unwrapTo guard verification with direction analysis, circulatingSupply formula verification, sDGNRS soulbound invariant proof, cross-contract consistency matrix, and executive summary with all per-requirement verdicts and known issue re-verification.</done>
</task>

</tasks>

<verification>
- GOV-03 state reset trace covers all 7 variables with line numbers from both AdvanceModule and GameStorage
- GOV-03 lastVrfProcessedTimestamp analyzed as intentional non-reset with security reasoning
- GOV-03 unwrapTo guard verified with correct direction and 5h threshold
- GOV-03 circulatingSupply formula verified with edge cases
- GOV-03 sDGNRS soulbound invariant proven with function enumeration
- Cross-contract consistency matrix covers all governance-relevant state with read/write/reset columns
- Executive summary accurately reflects all GOV-01/02/03 verdicts and WAR/GOV/VOTE re-verifications
</verification>

<success_criteria>
audit/v3.2-governance-fresh-eyes.md is complete with GOV-01, GOV-02, and GOV-03 sections plus executive summary. GOV-03 contains the full cross-contract state reset trace, lastVrfProcessedTimestamp lifecycle, unwrapTo guard verification, circulatingSupply accounting, sDGNRS soulbound proof, and consistency matrix. Executive summary has per-requirement verdicts, known issue re-verification table, and findings summary with severity counts. Document is ready as a standalone deliverable for the protocol team.
</success_criteria>

<output>
After completion, create `.planning/phases/42-governance-fresh-eyes/42-02-SUMMARY.md`
</output>
