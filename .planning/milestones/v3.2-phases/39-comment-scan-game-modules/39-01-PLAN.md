---
phase: 39-comment-scan-game-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.2-findings-39-jackpot-module.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every NatSpec tag in JackpotModule matches actual function signatures and behavior"
    - "Every inline comment in JackpotModule accurately describes the code it annotates"
    - "All 6 v3.1 fixes (CMT-025 through CMT-030) in JackpotModule are verified correct"
    - "Any new comment inaccuracies introduced by code changes are identified"
  artifacts:
    - path: "audit/v3.2-findings-39-jackpot-module.md"
      provides: "JackpotModule comment audit findings"
      contains: "## DegenerusGameJackpotModule.sol"
  key_links:
    - from: "audit/v3.2-findings-39-jackpot-module.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "line-level findings referencing source"
      pattern: "DegenerusGameJackpotModule.sol:\\d+"
---

<objective>
Audit every comment (NatSpec, inline, block) in DegenerusGameJackpotModule.sol against the current working tree code. Verify v3.1 fixes are correct. Produce findings for any remaining or new inaccuracies.

Purpose: JackpotModule is the largest module (2,792 lines, 57 functions) and had 6 v3.1 findings. Its complexity in prize pool routing, auto-rebuy logic, and terminal jackpot flows makes comment accuracy critical for C4A wardens.
Output: `audit/v3.2-findings-39-jackpot-module.md` -- intermediate findings file for later consolidation.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-comment-scan-game-modules/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DegenerusGameJackpotModule.sol comments</name>
  <files>audit/v3.2-findings-39-jackpot-module.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/storage/DegenerusGameStorage.sol
    audit/PAYOUT-SPECIFICATION.html
  </read_first>
  <action>
Read the ENTIRE DegenerusGameJackpotModule.sol from the working tree (all 2,792 lines). This file has uncommitted changes containing v3.1 comment fixes.

First, retrieve v3.1 findings for this file for cross-reference:
```bash
git show HEAD:audit/v3.1-findings-33-game-modules-batch-b.md
```
The v3.1 findings for JackpotModule are CMT-025 through CMT-030:
- CMT-025: Numbering gap in JACKPOT FLOW OVERVIEW (1,2,4 -> 1,2,3)
- CMT-026: payDailyJackpot NatSpec physically attached to runTerminalJackpot (moved above correct function)
- CMT-027: _processAutoRebuy NatSpec "50/50" changed to "25% each, level+1 through level+4"
- CMT-028: "loot box conversion" changed to "auto-rebuy to tickets"
- CMT-029: "loot box awards (added to nextPrizePool)" changed to "auto-rebuy tickets (added to next/futurePool)"
- CMT-030: Orphaned NatSpec from removed loot box distribution function removed

For EACH v3.1 fix: read the fix location in the working tree, verify the new text is accurate against the actual code behavior. Record pass/fail in the findings file.

Then perform a FRESH full audit of all comments:

1. **NatSpec audit** (~258 NatSpec tags across 57 functions): For every @notice, @dev, @param, @return, @custom tag, verify it matches the actual function signature, parameter names, return values, and code behavior. Pay special attention to:
   - Prize pool routing comments (nextPrizePool vs futurePrizePool vs currentJackpot)
   - Auto-rebuy flow comments (level offset calculations, fund routing)
   - Terminal jackpot comments (payout percentages, winner selection)
   - consolidatePrizePools comments (30-65% keep range, no future dump)
   - Ticket processing batch comments (gas budget, write limits)

2. **Inline comment audit** (~424 inline comments): For every `//` comment, verify it accurately describes the adjacent code. Watch for:
   - Stale references to removed features (future dump, loot box distribution)
   - Percentage/ratio comments that don't match actual constants
   - Fund routing comments that describe wrong destination pools

3. **Block comment audit** (~57 block/section comments): Verify structural accuracy of section headers and architecture overview blocks.

4. **Cross-reference check**: For comments referencing other contracts (DegenerusGame, GameStorage, DegenerusJackpots), verify the referenced behavior exists in those contracts.

Write findings to `audit/v3.2-findings-39-jackpot-module.md` using this format:

```markdown
# v3.2 Findings: JackpotModule Comment Audit

**Date:** 2026-03-19
**Scope:** DegenerusGameJackpotModule.sol (2,792 lines, 57 functions)
**Pass:** v3.2 fresh scan (v3.1 fixes applied in working tree)

## v3.1 Fix Verification

| v3.1 ID | Description | Status | Notes |
|---------|-------------|--------|-------|
| CMT-025 | Numbering gap fixed | PASS/FAIL | [verification details] |
| CMT-026 | payDailyJackpot NatSpec repositioned | PASS/FAIL | ... |
| CMT-027 | _processAutoRebuy 50/50 -> 25% each | PASS/FAIL | ... |
| CMT-028 | loot box conversion -> auto-rebuy | PASS/FAIL | ... |
| CMT-029 | loot box awards -> auto-rebuy tickets | PASS/FAIL | ... |
| CMT-030 | Orphaned NatSpec removed | PASS/FAIL | ... |

## New Findings

### CMT-V32-NNN: [Brief title]

- **What:** [Description of the inaccuracy]
- **Where:** DegenerusGameJackpotModule.sol:[line(s)]
- **Why:** [How this would mislead a C4A warden]
- **Suggestion:** [Specific fix text]
- **Category:** comment-inaccuracy | intent-drift
- **Severity:** LOW | INFO

## Summary

| Category | Count |
|----------|-------|
| v3.1 fixes verified | N/6 |
| New CMT findings | N |
| New DRIFT findings | N |
| **Total new** | **N** |
```

Use fresh numbering starting at CMT-V32-001. If no new findings exist for a file, explicitly state "No new findings -- all comments verified accurate."
  </action>
  <verify>
    <automated>test -f audit/v3.2-findings-39-jackpot-module.md && grep -c "CMT-025\|CMT-026\|CMT-027\|CMT-028\|CMT-029\|CMT-030" audit/v3.2-findings-39-jackpot-module.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-39-jackpot-module.md exists
    - File contains "## v3.1 Fix Verification" section
    - File contains all 6 v3.1 IDs (CMT-025 through CMT-030) with PASS or FAIL status
    - File contains "## New Findings" section (even if empty with "No new findings" note)
    - File contains "## Summary" table with counts
    - Every new finding uses CMT-V32-NNN numbering format
    - Every new finding has all 6 required fields: What, Where, Why, Suggestion, Category, Severity
    - Every "Where" field includes a specific line number (not just the filename)
  </acceptance_criteria>
  <done>JackpotModule (2,792 lines) fully audited. All 6 v3.1 fixes verified. All new findings documented with line-level precision. Intermediate findings file written.</done>
</task>

</tasks>

<verification>
- `audit/v3.2-findings-39-jackpot-module.md` exists and follows established format
- All 6 v3.1 fixes for JackpotModule have PASS/FAIL verification
- Any new findings use CMT-V32-NNN numbering with complete fields
</verification>

<success_criteria>
- Every NatSpec tag in JackpotModule verified against actual function behavior
- Every inline comment verified against adjacent code
- v3.1 fixes independently verified (not assumed correct)
- Zero false positives (each finding confirmed by code reference)
</success_criteria>

<output>
After completion, create `.planning/phases/39-comment-scan-game-modules/39-01-SUMMARY.md`
</output>
