---
phase: 39-comment-scan-game-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.2-findings-39-decimator-degenerette-mint.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every NatSpec tag in DecimatorModule, DegeneretteModule, and MintModule matches actual function signatures and behavior"
    - "Every inline comment in all 3 modules accurately describes the code it annotates"
    - "All v3.1 fixes in these 3 files are verified correct (CMT-031/033/034/035 for Decimator, CMT-020 for Degenerette, CMT-011 through CMT-015 for Mint)"
    - "Decimator claim expiry removal is fully reflected in comments (no stale expiry references)"
  artifacts:
    - path: "audit/v3.2-findings-39-decimator-degenerette-mint.md"
      provides: "DecimatorModule + DegeneretteModule + MintModule comment audit findings"
      contains: "## DegenerusGameDecimatorModule.sol"
  key_links:
    - from: "audit/v3.2-findings-39-decimator-degenerette-mint.md"
      to: "contracts/modules/DegenerusGameDecimatorModule.sol"
      via: "line-level findings referencing source"
      pattern: "DegenerusGameDecimatorModule.sol:\\d+"
    - from: "audit/v3.2-findings-39-decimator-degenerette-mint.md"
      to: "contracts/modules/DegenerusGameMintModule.sol"
      via: "line-level findings referencing source"
      pattern: "DegenerusGameMintModule.sol:\\d+"
---

<objective>
Audit every comment in DecimatorModule (1,031 lines), DegeneretteModule (1,178 lines), and MintModule (1,149 lines) against the current working tree code. Verify v3.1 fixes. Pay special attention to DecimatorModule where the claim expiry removal changed actual code behavior.

Purpose: DecimatorModule had both code changes (expiry removal) and comment fixes. MintModule had the most v3.1 findings (5). All three are mid-complexity modules where comment accuracy is critical for warden comprehension of burn mechanics, degenerette betting, and minting flows.
Output: `audit/v3.2-findings-39-decimator-degenerette-mint.md` -- intermediate findings file for later consolidation.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-comment-scan-game-modules/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DecimatorModule and DegeneretteModule comments</name>
  <files>audit/v3.2-findings-39-decimator-degenerette-mint.md</files>
  <read_first>
    contracts/modules/DegenerusGameDecimatorModule.sol
    contracts/modules/DegenerusGameDegeneretteModule.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Read the ENTIRE DegenerusGameDecimatorModule.sol (1,031 lines) and DegenerusGameDegeneretteModule.sol (1,178 lines) from the working tree.

First, retrieve v3.1 findings for cross-reference:
```bash
git show HEAD:audit/v3.1-findings-33-game-modules-batch-b.md
git show HEAD:audit/v3.1-findings-32-game-modules-batch-a.md
```

**DecimatorModule v3.1 fixes to verify (5 findings):**
- CMT-031: "player burn resets" changed to "carried over to the new bucket"
- CMT-033: Added full NatSpec to TerminalDecBurnRecorded event (7 @param tags)
- CMT-034: Removed unused TerminalDecAlreadyClaimed error
- CMT-035: Constants @dev updated to include "activity cap"
- Inline comment fix: "totalBurn == 0 as claimed flag" changed to "weightedBurn is zeroed after claiming"

**CRITICAL for DecimatorModule:** The decimator claim expiry was removed (commit 19f5bc60). Claims now persist across rounds. Search for ANY remaining comments that reference:
- Claim expiry windows or timeouts
- Round-scoped claims (claims that expire when a new round starts)
- TerminalDecAlreadyClaimed error (should be removed)
- "claim window" or "expiry" in the context of decimator claims
Run: `grep -n "expir\|claim window\|round-scoped\|TerminalDecAlreadyClaimed" contracts/modules/DegenerusGameDecimatorModule.sol`

**DegeneretteModule v3.1 fix to verify (1 finding):**
- CMT-020: Removed orphaned NatSpec line "Places Full Ticket bets using pending affiliate Degenerette credit"

For EACH file, perform the full audit methodology:

1. **NatSpec audit**: DecimatorModule has ~200 NatSpec tags across 32 functions; DegeneretteModule has ~187 NatSpec tags across 31 functions. Verify every @notice, @dev, @param, @return, @custom tag matches actual signatures and behavior. For DecimatorModule, pay special attention to:
   - Burn bucket lifecycle comments (creation, rollover, claiming)
   - Terminal decimator claim flow (now persistent, no expiry)
   - Weighted burn calculation comments (formula accuracy)
   - Activity cap references in constants

2. **Inline comment audit**: DecimatorModule ~281 inline, DegeneretteModule ~325 inline. Verify each `//` comment against adjacent code. For DegeneretteModule, watch for:
   - Bet placement flow comments (credit mechanics, ticket pricing)
   - RNG resolution comments (lootbox integration points)
   - Payout routing comments (house edge, affiliate splits)

3. **Block comment audit**: Verify section headers and architecture overviews.

4. **Cross-reference check**: Comments referencing DegenerusGame, GameStorage, or other modules -- verify referenced behavior exists.

Create the findings file `audit/v3.2-findings-39-decimator-degenerette-mint.md` with the header and first two contract sections. Use the same format as Plan 01's JackpotModule findings. Start numbering after the last CMT-V32-NNN used by Plan 01 (check audit/v3.2-findings-39-jackpot-module.md if it exists; if not, start at CMT-V32-001 and the consolidation plan will renumber).

Structure:
```markdown
# v3.2 Findings: DecimatorModule + DegeneretteModule + MintModule Comment Audit

**Date:** 2026-03-19
**Scope:** DegenerusGameDecimatorModule.sol (1,031 lines), DegenerusGameDegeneretteModule.sol (1,178 lines), DegenerusGameMintModule.sol (1,149 lines)
**Pass:** v3.2 fresh scan (v3.1 fixes applied in working tree)

## DegenerusGameDecimatorModule.sol

### v3.1 Fix Verification
[table with CMT-031, CMT-033, CMT-034, CMT-035, inline fix]

### New Findings
[findings or "No new findings"]

## DegenerusGameDegeneretteModule.sol

### v3.1 Fix Verification
[table with CMT-020]

### New Findings
[findings or "No new findings"]
```

Do NOT write the MintModule section yet -- that is Task 2.
  </action>
  <verify>
    <automated>test -f audit/v3.2-findings-39-decimator-degenerette-mint.md && grep -c "DecimatorModule\|DegeneretteModule" audit/v3.2-findings-39-decimator-degenerette-mint.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-39-decimator-degenerette-mint.md exists
    - File contains "## DegenerusGameDecimatorModule.sol" section
    - File contains "## DegenerusGameDegeneretteModule.sol" section
    - DecimatorModule section has v3.1 Fix Verification table with CMT-031, CMT-033, CMT-034, CMT-035 and the inline fix
    - DegeneretteModule section has v3.1 Fix Verification table with CMT-020
    - No references to "TerminalDecAlreadyClaimed" exist in DecimatorModule source (confirm removal)
    - No references to claim "expiry" or "window" exist in DecimatorModule source (or if they do, they are flagged as findings)
    - Every new finding has all 6 required fields (What, Where, Why, Suggestion, Category, Severity)
  </acceptance_criteria>
  <done>DecimatorModule (1,031 lines) and DegeneretteModule (1,178 lines) fully audited. v3.1 fixes verified. Expiry removal verified with no stale references. Findings written to intermediate file.</done>
</task>

<task type="auto">
  <name>Task 2: Audit MintModule comments and finalize findings file</name>
  <files>audit/v3.2-findings-39-decimator-degenerette-mint.md</files>
  <read_first>
    contracts/modules/DegenerusGameMintModule.sol
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.2-findings-39-decimator-degenerette-mint.md
  </read_first>
  <action>
Read the ENTIRE DegenerusGameMintModule.sol (1,149 lines) from the working tree.

Retrieve v3.1 findings for MintModule:
```bash
git show HEAD:audit/v3.1-findings-32-game-modules-batch-a.md
```

**MintModule v3.1 fixes to verify (5 findings):**
- CMT-011: Orphaned NatSpec + blank lines at end of file removed (lines 1140-1193 deleted)
- CMT-012: Added full NatSpec to processFutureTicketBatch (6 tags: @notice, @dev, @param lvl, @return worked, @return finished, @return writesUsed)
- CMT-013: RNG gating reference removed from purchaseCoin @dev, replaced with accurate cutoff description
- CMT-014: "milestones" removed from recordMintData @dev
- CMT-015: "+10pp" inline comment replaced with accurate "+100 BURNIE per ticket for affiliates"

For EACH v3.1 fix, read the actual location in the working tree and verify the new text is accurate against code behavior. For CMT-012 specifically: verify the new NatSpec on processFutureTicketBatch accurately describes: (a) the function's purpose, (b) the @param lvl meaning, (c) the @return worked/finished/writesUsed semantics, (d) the write budget system.

Perform the full audit:

1. **NatSpec audit** (~56 NatSpec tags across 16 functions): Verify every tag. Pay special attention to:
   - processFutureTicketBatch (the newly-added NatSpec from CMT-012 fix)
   - purchaseCoin (the updated @dev from CMT-013 fix -- verify no RNG references remain)
   - recordMintData (the updated @dev from CMT-014 fix -- verify no "milestones" reference)
   - Affiliate-related comments (CMT-015 fix area)
   - Any references to rngLocked (should not appear in MintModule since rngLocked was removed from coinflip claim paths)

2. **Inline comment audit** (~143 inline comments): Verify each against adjacent code. Watch for:
   - Stale references to removed lootbox resolution function (CMT-011 area)
   - Ticket pricing and BURNIE conversion comments
   - Gas budget / write limit comments in processFutureTicketBatch

3. **Block comment audit**: Verify section headers and architecture overview.

4. **Cross-reference check**: MintModule references to DegenerusGame, GameStorage, BurnieCoin -- verify referenced behavior.

Run: `grep -n "rngLock\|rng.*lock\|RNG.*lock" contracts/modules/DegenerusGameMintModule.sol` to confirm no stale rngLocked references.

Append the MintModule section and overall summary table to the existing findings file:

```markdown
## DegenerusGameMintModule.sol

### v3.1 Fix Verification
[table with CMT-011 through CMT-015]

### New Findings
[findings or "No new findings"]

---

## Overall Summary

| Contract | v3.1 Fixes Verified | New CMT | New DRIFT | Total New |
|----------|---------------------|---------|-----------|-----------|
| DecimatorModule | N/5 | N | N | N |
| DegeneretteModule | N/1 | N | N | N |
| MintModule | N/5 | N | N | N |
| **Total** | **N/11** | **N** | **N** | **N** |
```
  </action>
  <verify>
    <automated>grep -c "MintModule\|Overall Summary" audit/v3.2-findings-39-decimator-degenerette-mint.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## DegenerusGameMintModule.sol" section
    - MintModule section has v3.1 Fix Verification table with CMT-011, CMT-012, CMT-013, CMT-014, CMT-015
    - File contains "## Overall Summary" table with per-contract counts
    - No orphaned NatSpec remains at end of MintModule source (verify CMT-011 fix -- file should end cleanly)
    - No "rngLocked" or "RNG is locked" references in MintModule source (or flagged if present)
    - Every new finding has all 6 required fields
    - Finding numbers continue from Task 1's last number (no gaps, no duplicates)
  </acceptance_criteria>
  <done>MintModule (1,149 lines) fully audited. All 5 v3.1 fixes verified. Findings file complete for all 3 contracts with summary table.</done>
</task>

</tasks>

<verification>
- `audit/v3.2-findings-39-decimator-degenerette-mint.md` exists with all 3 contract sections
- All 11 v3.1 fixes across 3 contracts have PASS/FAIL verification
- DecimatorModule has no stale expiry/claim-window references
- MintModule has no stale rngLocked references
- Summary table tallies match individual section findings
</verification>

<success_criteria>
- All 3 modules (3,358 total lines) have every comment verified against code
- DecimatorModule claim expiry removal fully reflected in comments
- v3.1 fixes independently verified (not assumed correct)
- Zero false positives
</success_criteria>

<output>
After completion, create `.planning/phases/39-comment-scan-game-modules/39-02-SUMMARY.md`
</output>
