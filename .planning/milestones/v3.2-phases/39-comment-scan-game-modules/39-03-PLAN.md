---
phase: 39-comment-scan-game-modules
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.2-findings-39-lootbox-advance.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every NatSpec tag in LootboxModule and AdvanceModule matches actual function signatures and behavior"
    - "Every inline comment in both modules accurately describes the code it annotates"
    - "All v3.1 fixes are verified correct (CMT-021 through CMT-024 for Lootbox, CMT-039 for Advance)"
    - "AdvanceModule delegatecall header correctly lists all 10 module addresses"
  artifacts:
    - path: "audit/v3.2-findings-39-lootbox-advance.md"
      provides: "LootboxModule + AdvanceModule comment audit findings"
      contains: "## DegenerusGameLootboxModule.sol"
  key_links:
    - from: "audit/v3.2-findings-39-lootbox-advance.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "line-level findings referencing source"
      pattern: "DegenerusGameLootboxModule.sol:\\d+"
    - from: "audit/v3.2-findings-39-lootbox-advance.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "line-level findings referencing source"
      pattern: "DegenerusGameAdvanceModule.sol:\\d+"
---

<objective>
Audit every comment in LootboxModule (1,778 lines) and AdvanceModule (1,382 lines) against the current working tree code. Verify v3.1 fixes. AdvanceModule is the central orchestrator (delegatecall hub) so its module list header accuracy is critical.

Purpose: LootboxModule is the second-largest module with complex reward distribution. AdvanceModule orchestrates the entire game flow via delegatecall. Comment accuracy in both is essential for wardens understanding cross-module interactions.
Output: `audit/v3.2-findings-39-lootbox-advance.md` -- intermediate findings file for later consolidation.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-comment-scan-game-modules/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit LootboxModule comments</name>
  <files>audit/v3.2-findings-39-lootbox-advance.md</files>
  <read_first>
    contracts/modules/DegenerusGameLootboxModule.sol
    contracts/storage/DegenerusGameStorage.sol
    contracts/ContractAddresses.sol
  </read_first>
  <action>
Read the ENTIRE DegenerusGameLootboxModule.sol (1,778 lines) from the working tree.

Retrieve v3.1 findings:
```bash
git show HEAD:audit/v3.1-findings-32-game-modules-batch-a.md
```

**LootboxModule v3.1 fixes to verify (4 findings):**
- CMT-021: "260%" changed to "255%" (verify the actual max multiplier in code matches 255%)
- CMT-022: Removed "resolveLootboxRng" from function list in contract @dev (verify this function no longer appears in the function list)
- CMT-023: "decimator claims" changed to "inline using a provided RNG word" (verify the new description matches actual lootbox resolution mechanism)
- CMT-024: Added "11=LazyPassBoon" to LootBoxReward event @param rewardType (verify enum value 11 is indeed LazyPassBoon in the code)

For EACH v3.1 fix, read the fix location in the working tree, verify the new text is accurate against actual code.

Perform the full audit:

1. **NatSpec audit** (~329 NatSpec tags across 27 functions): Verify every tag. Pay special attention to:
   - Lootbox reward distribution comments (11 reward types, probability weights)
   - Lootbox opening flow (@dev describing the resolution pipeline)
   - The LootBoxReward event @param rewardType enum -- verify ALL enum values listed match code
   - MaxMultiplier/probability constants (verify 255% and all probability breakpoints match code)
   - Any references to "resolveLootboxRng" (should be gone per CMT-022 fix)

2. **Inline comment audit** (~395 inline comments): Verify each against adjacent code. Watch for:
   - Reward type dispatch comments (case statements matching reward IDs)
   - Probability calculation comments (verify math matches constants)
   - LazyPassBoon handling comments (added in CMT-024 area)
   - Cross-module references to MintModule, DecimatorModule (verify referenced behavior)

3. **Block comment audit**: Verify section headers and reward type tables.

4. **Cross-reference check**: LootboxModule references to other modules and contracts -- verify each.

Create the findings file with LootboxModule section:

```markdown
# v3.2 Findings: LootboxModule + AdvanceModule Comment Audit

**Date:** 2026-03-19
**Scope:** DegenerusGameLootboxModule.sol (1,778 lines), DegenerusGameAdvanceModule.sol (1,382 lines)
**Pass:** v3.2 fresh scan (v3.1 fixes applied in working tree)

## DegenerusGameLootboxModule.sol

### v3.1 Fix Verification
[table with CMT-021 through CMT-024]

### New Findings
[findings or "No new findings"]
```

Start numbering at CMT-V32-001 (the consolidation plan will assign global numbers).
  </action>
  <verify>
    <automated>test -f audit/v3.2-findings-39-lootbox-advance.md && grep -c "LootboxModule\|CMT-021\|CMT-022\|CMT-023\|CMT-024" audit/v3.2-findings-39-lootbox-advance.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-findings-39-lootbox-advance.md exists
    - File contains "## DegenerusGameLootboxModule.sol" section
    - v3.1 Fix Verification table has CMT-021, CMT-022, CMT-023, CMT-024 with PASS/FAIL
    - No "resolveLootboxRng" appears in the LootboxModule source function list (confirm CMT-022 fix)
    - LootBoxReward event @param lists all reward types including "11=LazyPassBoon" (confirm CMT-024 fix)
    - Every new finding has all 6 required fields
  </acceptance_criteria>
  <done>LootboxModule (1,778 lines) fully audited. All 4 v3.1 fixes verified. Findings written.</done>
</task>

<task type="auto">
  <name>Task 2: Audit AdvanceModule comments and finalize findings file</name>
  <files>audit/v3.2-findings-39-lootbox-advance.md</files>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/ContractAddresses.sol
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.2-findings-39-lootbox-advance.md
  </read_first>
  <action>
Read the ENTIRE DegenerusGameAdvanceModule.sol (1,382 lines) from the working tree.

Retrieve v3.1 findings:
```bash
git show HEAD:audit/v3.1-findings-33-game-modules-batch-b.md
```

**AdvanceModule v3.1 fixes to verify (2 findings, 1 was LOW severity):**
- CMT-039 (LOW): Delegatecall module header corrected. The header block now lists: EndgameModule, JackpotModule, MintModule, GameOverModule. Verify these 4 are correct by checking which modules AdvanceModule actually delegates to. Cross-reference with ContractAddresses.sol to verify the GAME_*_MODULE addresses used in the AdvanceModule delegatecall targets.
- CMT-040: [Check the v3.1 findings for the second AdvanceModule finding and verify its fix]

**CRITICAL for AdvanceModule:** This module is the central game flow orchestrator. Its header/architecture comment describes the delegatecall targets. Verify:
1. Every module address used in delegatecall calls within the code is listed in the header
2. No module listed in the header is NOT actually called via delegatecall
3. The flow description (advanceGame sequence) matches the actual code execution order

For the delegatecall header verification, grep for all delegatecall targets:
```bash
grep -n "delegatecall\|GAME_.*MODULE" contracts/modules/DegenerusGameAdvanceModule.sol
```
Cross-reference with ContractAddresses.sol:
```bash
grep "GAME_.*MODULE" contracts/ContractAddresses.sol
```

Perform the full audit:

1. **NatSpec audit** (~119 NatSpec tags across 38 functions): Verify every tag. Pay special attention to:
   - advanceGame flow comments (the main game progression function)
   - Level transition comments (endgame triggers, whale pass distribution)
   - Delegatecall target comments (which module handles which responsibility)
   - RNG references (verify none reference rngLocked if it was removed from this flow)

2. **Inline comment audit** (~203 inline comments): Verify each. Watch for:
   - Flow step numbering and ordering (must match actual execution sequence)
   - Module dispatch comments (which module handles decimator, jackpot, lootbox, etc.)
   - Gas management comments (batch processing, write budgets)

3. **Block comment audit**: The architecture header is the most critical block comment in the entire module system. Verify it completely.

4. **Cross-reference check**: AdvanceModule references nearly every other module. Verify each cross-reference.

Also check: `grep -n "rngLock\|rng.*lock" contracts/modules/DegenerusGameAdvanceModule.sol` for stale rngLocked references.

Append AdvanceModule section and summary to findings file:

```markdown
## DegenerusGameAdvanceModule.sol

### v3.1 Fix Verification
[table with CMT-039, CMT-040 (if exists)]

### New Findings
[findings or "No new findings"]

---

## Overall Summary

| Contract | v3.1 Fixes Verified | New CMT | New DRIFT | Total New |
|----------|---------------------|---------|-----------|-----------|
| LootboxModule | N/4 | N | N | N |
| AdvanceModule | N/2 | N | N | N |
| **Total** | **N/6** | **N** | **N** | **N** |
```
  </action>
  <verify>
    <automated>grep -c "AdvanceModule\|Overall Summary" audit/v3.2-findings-39-lootbox-advance.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## DegenerusGameAdvanceModule.sol" section
    - v3.1 Fix Verification table has CMT-039 (and CMT-040 if applicable) with PASS/FAIL
    - The delegatecall header module list in the source matches the actual delegatecall targets found by grep
    - No stale "rngLocked" references in AdvanceModule source (or flagged if present)
    - File contains "## Overall Summary" table
    - Every new finding has all 6 required fields
    - Finding numbers continue from Task 1's last number
  </acceptance_criteria>
  <done>AdvanceModule (1,382 lines) fully audited. v3.1 fixes verified. Delegatecall header accuracy confirmed. Findings file complete for both contracts.</done>
</task>

</tasks>

<verification>
- `audit/v3.2-findings-39-lootbox-advance.md` exists with both contract sections
- All v3.1 fixes for both contracts have PASS/FAIL verification
- AdvanceModule delegatecall header lists exactly the modules called in code
- Summary table tallies match individual section findings
</verification>

<success_criteria>
- Both modules (3,160 total lines) have every comment verified against code
- AdvanceModule as central orchestrator has its module list and flow description confirmed accurate
- v3.1 fixes independently verified
- Zero false positives
</success_criteria>

<output>
After completion, create `.planning/phases/39-comment-scan-game-modules/39-03-SUMMARY.md`
</output>
