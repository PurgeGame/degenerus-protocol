---
phase: 336-tst-equivalence-freeze-safety-divergence-repro-non-widening-
plan: 04
type: execute
wave: 4
depends_on: [336-03]
files_modified:
  - test/fuzz/AfKingSubscription.t.sol
autonomous: true
worktree: false
requirements: [TST-02]
must_haves:
  truths:
    - "A new oracle in test/fuzz/AfKingSubscription.t.sol uses vm.expectCall(IGame.lazyPassHorizon.selector, count: 0) to assert ZERO external pass reads on the non-crossing AfKing autoBuy iteration"
    - "The oracle is the FIRST vm.expectCall use in the entire test/ tree (RESEARCH §Summary finding 1) — staging strictly precedes the cheatcode invocation per Pitfall 1"
    - "TST-02 D-TST02-02 satisfied — empirically proves AFSUB-02 (cheap stored-field compare, NO per-iter external pass read on non-crossing) and the GASOPT-05-class no-regression invariant from v49"
    - "contracts/*.sol shows ZERO byte diff vs e756a6f3 after this plan lands"
  artifacts:
    - path: "test/fuzz/AfKingSubscription.t.sol"
      provides: "TST-02 D-TST02-02 hot-path-accurate no-SLOAD oracle"
      contains: "testNonCrossingPathPerformsZeroLazyPassHorizonSloads"
  key_links:
    - from: "new oracle test"
      to: "vm.expectCall(address(game), abi.encodeWithSelector(IGame.lazyPassHorizon.selector), 0)"
      via: "forge-std cheatcode — auto-verifies on test teardown"
      pattern: "vm\\.expectCall.*lazyPassHorizon"
    - from: "AfKing.autoBuy(50) on non-crossing sub"
      to: "the early-skip on currentLevel <= sub.validThroughLevel (line ~627)"
      via: "stored-field branch; lazyPassHorizon at line :628 only inside the crossing block"
      pattern: "validThroughLevel"
---

<objective>
Close the explicit TST-02 D-TST02-02 oracle gap: empirically prove the non-crossing AfKing autoBuy iteration performs ZERO external `IGame.lazyPassHorizon` reads, using `vm.expectCall(..., count: 0)` as the hot-path-accurate Foundry-native primitive.

Purpose: This is the load-bearing TST-02 gap. The 4 already-migrated AfKing test files cover sweep-while-valid / evict-at-crossing / refresh-at-crossing / OPEN-E re-attest / SUB-07 cancel-tombstone / swap-pop invariant (per 335 D-IMPL-02) — none assert the negative no-SLOAD property on the non-crossing hot path.
Output: One new test function added to `test/fuzz/AfKingSubscription.t.sol` (PATTERNS.md §"Why this file is the preferred TST-02 home"). NO contract mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-CONTEXT.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md

@test/fuzz/AfKingSubscription.t.sol
@contracts/AfKing.sol
@contracts/DegenerusGame.sol

<interfaces>
Existing scaffold and helpers to REUSE verbatim from test/fuzz/AfKingSubscription.t.sol (lines ~362-460):

- `_currentLevel() internal view returns (uint24)`
- `_subscribeTicketMode(address who, uint8 q) internal`
- `_approveKeeper(address who) internal`
- `_fundPool(address who, uint256 amount) internal`
- `_grantDeityPass(address who) internal` (horizon = uint24.max — the deity sentinel that pins validThroughLevel non-crossing)

Closest analog: `testNonCrossingPassHolderBuysWithoutRefresh` at lines 182-199 — the non-crossing staging spine the new test mirrors.

`IGame` interface lives at `contracts/AfKing.sol` (lines ~27-48). The selector to target: `IGame.lazyPassHorizon` (returns `uint24`). Import path: `import {IGame} from "../../contracts/AfKing.sol";` if not already in scope.

Subject under test: `contracts/AfKing.sol:627-647`. The non-crossing branch is the implicit "else" of `if (currentLevel > sub.validThroughLevel)`; only the crossing block executes `uint24 h = GAME.lazyPassHorizon(player)` at line :628.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add testNonCrossingPathPerformsZeroLazyPassHorizonSloads — the vm.expectCall no-SLOAD oracle</name>
  <files>test/fuzz/AfKingSubscription.t.sol</files>
  <read_first>
    - test/fuzz/AfKingSubscription.t.sol full file — confirm: imports already present (ContractAddresses, Vm), the testNonCrossingPassHolderBuysWithoutRefresh template at lines 182-199, the helpers at lines 362-460 (_grantDeityPass, _subscribeTicketMode, _approveKeeper, _fundPool, _currentLevel), the `game` and `afKing` fixture members from DeployProtocol
    - contracts/AfKing.sol around lines 27-48 (the IGame interface — confirm the exact selector name `lazyPassHorizon`) AND lines 627-647 (the crossing block where lazyPassHorizon fires; the non-crossing path is the early-skip)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md §"test/fuzz/AfKingSubscription.t.sol" (lines 241-356) — the exact diff shape, the strict staging order, the IGame import direction, Pitfall 1
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md §"Pattern 2: vm.expectCall" (lines 135-159) + §"Pitfall 1" — the consumption-before-stage trap mitigation
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md — confirms the non-crossing path's cheap stored-field compare property (lazyPassHorizon is structurally crossing-only)
  </read_first>
  <behavior>
    - New test function: `function testNonCrossingPathPerformsZeroLazyPassHorizonSloads() public`
    - The function MUST reference `IGame.lazyPassHorizon.selector` — add the `IGame` import if not already in scope
    - Staging order STRICTLY: (1) grantDeity / subscribe / approve / fund — ALL external calls happen HERE; (2) `vm.expectCall(address(game), abi.encodeWithSelector(IGame.lazyPassHorizon.selector), 0)`; (3) `vm.prank(autoBuyer)`; (4) `afKing.autoBuy(50)` — NOTHING between (2) and (4)
    - Cheatcode AUTO-VERIFIES on test teardown — NO explicit assertEq needed; if any IGame.lazyPassHorizon call fires after step (2), the test FAILS at teardown
    - Pass at default profile (no deep gating needed)
  </behavior>
  <action>
    Append a new test function to test/fuzz/AfKingSubscription.t.sol, placed immediately adjacent to testNonCrossingPassHolderBuysWithoutRefresh (insertion point around line 200 — confirm on read). Function name MUST be testNonCrossingPathPerformsZeroLazyPassHorizonSloads.

    First, add the IGame import IF not already in scope. Read the file's existing imports; if `IGame` is not already imported (likely it is NOT — RESEARCH §Summary finding 1 notes vm.expectCall is brand new to this tree), add: `import {IGame} from "../../contracts/AfKing.sol";` adjacent to the existing imports near the top of the file.

    Inline docstring above the function signature: declare TST-02 D-TST02-02 intent, cite AFSUB-02, name the crossing site (AfKing.sol:627-647), and flag this as the FIRST vm.expectCall use in the test tree per RESEARCH §Summary finding 1.

    Implementation steps (mirror PATTERNS.md §"D-TST02-02 addition pattern"):

    STEP 1 — STAGE FIRST. All external calls happen BEFORE the expectCall. Allocate a `pass` address via `makeAddr("nx_no_sload_holder")`. Call `_grantDeityPass(pass)` to land horizon = uint24.max. Call `_subscribeTicketMode(pass, 1)` to bind validThroughLevel = uint24.max (the deity sentinel — guarantees non-crossing for the entire game lifetime). Call `_approveKeeper(pass)`. Call `_fundPool(pass, 1 ether)`. After this block, NO further external call may occur before STEP 3.

    STEP 2 — Invoke the cheatcode. Call `vm.expectCall(address(game), abi.encodeWithSelector(IGame.lazyPassHorizon.selector), 0)`. The third argument is the integer literal `0` — meaning EXACTLY zero calls into the lazyPassHorizon selector at the game address are expected for the duration of the test. The cheatcode begins counting from the NEXT external call.

    STEP 3 — Prank the autoBuyer. Call `vm.prank(makeAddr("autoBuyer_no_sload_check"))`.

    STEP 4 — Invoke the subject. Call `afKing.autoBuy(50)`. This is the autoBuy iteration whose non-crossing path the oracle measures.

    STEP 5 — End of test (no explicit assertEq). The cheatcode auto-verifies on test teardown.

    Pitfall 1 enforcement: NOTHING between STEP 2 and STEP 4 — no logging, no helper call, no comment-via-emit. Per RESEARCH §Pitfall 1, even a benign external call between them consumes the budget and breaks the count==0 assertion.

    Implements TST-02 D-TST02-02 per the locked design + PATTERNS.md analog scaffolding.

    DO NOT touch the existing testNonCrossingPassHolderBuysWithoutRefresh — it asserts complementary (event-count) properties and is byte-locked.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit &amp;&amp; forge test --match-path test/fuzz/AfKingSubscription.t.sol --match-test testNonCrossingPathPerformsZeroLazyPassHorizonSloads -vv 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "testNonCrossingPathPerformsZeroLazyPassHorizonSloads" test/fuzz/AfKingSubscription.t.sol` returns at least 1
    - `grep -c "vm.expectCall" test/fuzz/AfKingSubscription.t.sol` returns exactly 1 (the new oracle — first use in the test tree per RESEARCH §Summary)
    - `grep -c "lazyPassHorizon.selector" test/fuzz/AfKingSubscription.t.sol` returns at least 1
    - `forge test --match-test testNonCrossingPathPerformsZeroLazyPassHorizonSloads -vv` EXITS 0
    - The pre-existing `testNonCrossingPassHolderBuysWithoutRefresh` still PASSES (no regression)
    - `git diff e756a6f3 -- contracts/` returns empty (zero contracts mutation per D-TST04-04)
    - Strict staging-order check: in the new function body, the LAST line before `vm.expectCall(...)` is a staging helper or address-allocation call (none is a `vm.prank` or `afKing.*` invocation); the LINE IMMEDIATELY AFTER `vm.expectCall(...)` is `vm.prank(...)`; the line after that is `afKing.autoBuy(50)`. Verify by reading the function body.
  </acceptance_criteria>
  <done>New oracle test present + GREEN; vm.expectCall used exactly once in the file (first in tree); existing tests untouched + still green; zero contracts diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → AfKing → game contract | Test pranks autoBuyer and invokes afKing.autoBuy(50); the AfKing internal loop reads sub.validThroughLevel (stored field) and may invoke game.lazyPassHorizon ONLY inside the crossing branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-336-04-01 | Tampering | vm.expectCall fires from a staging call (e.g. _grantDeityPass) instead of from the autoBuy loop | mitigate | Pitfall 1 enforcement — strict staging order (STAGE BEFORE vm.expectCall). The acceptance criterion checks the function body's structural ordering. If the test fails with `counted 1 of expected 0` the executor must trace the offending call into the staging helpers and either move it pre-cheatcode or use `vm.expectCall` with the specific call-data the autoBuy loop would emit. |
| T-336-04-02 | Repudiation | Test silently passes because `afKing.autoBuy(50)` never iterates (empty subscriber set, no funded pool, sub already evicted) | mitigate | The _subscribeTicketMode + _fundPool calls in staging ensure the sub IS in the active set with funding; the existing testNonCrossingPassHolderBuysWithoutRefresh (immediately above) uses the SAME staging shape and asserts the sub stays in the set + the buy happens — that test passing alongside the new oracle confirms the iteration actually runs. |
| T-336-04-03 | Repudiation | The IGame import shadows a different IGame interface elsewhere in scope | mitigate | The import is `{IGame} from "../../contracts/AfKing.sol"` — the canonical interface AfKing itself uses. If a different IGame exists in another import already in the file, the executor reads the existing imports first and either reuses the in-scope IGame OR uses a renamed alias `import {IGame as IAfKingGame} from ...`. |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/AfKingSubscription.t.sol -vv` EXITS 0 — new oracle + all existing tests (including testNonCrossingPassHolderBuysWithoutRefresh) green
- `git diff e756a6f3 -- contracts/` empty (zero contracts mutation per D-TST04-04)
- `git diff e756a6f3 -- test/fuzz/AfKingSubscription.t.sol` shows ONLY the new test function (+ the IGame import if it wasn't already in scope) — existing tests + helpers UNCHANGED
- `grep -rn "vm.expectCall" test/` returns exactly the new line (the first-in-tree finding per RESEARCH §Summary)
- Per-plan atomic commit per D-CC-01: a single commit scoped to `336-04` / TST-02 D-TST02-02
</verification>

<success_criteria>
- TST-02 D-TST02-02 no-SLOAD oracle GREEN — empirically proves AFSUB-02 (cheap stored-field compare on non-crossing; lazyPassHorizon NEVER read on the hot path when currentLevel <= validThroughLevel)
- The GASOPT-05-class regression invariant (v49 precedent) re-attested empirically: zero per-iter external pass reads on non-crossing
- First-in-tree vm.expectCall use lands cleanly without breaking adjacent tests (RESEARCH §Summary finding 1 satisfied)
- Zero `contracts/*.sol` mutation per D-TST04-04
- Sequential-on-main, no-worktrees per D-CC-02; per-plan atomic commit per D-CC-01
</success_criteria>

<output>
Create `.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-04-SUMMARY.md` when done.
</output>
