---
phase: 334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "The validThroughLevel field placement in the existing Sub layout (repurposing paidThroughDay offset, :86-93) is settled"
    - "The new level-horizon pass view signature is settled: deity = type(uint24).max sentinel, lazy/whale = covered-through level (vs today's boolean hasAnyLazyPass)"
    - "The refresh-or-evict crossing control flow is settled: per-iter cheap compare currentLevel <= validThroughLevel (NO external pass read off-crossing), crossing re-reads ONCE → refresh-or-evict (not an unconditional kick)"
    - "burnForKeeper full removal from BOTH AfKing AND BurnieCoin is settled, lazy-only (no refreshPass)"
    - "OPEN-E 4-protection + SUB-07 cancel-tombstone + v49 swap-pop preservation criteria are recorded"
  artifacts:
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md"
      provides: "The settled AfKing pass-gated subscription signatures + preservation criteria (SC1, AFSUB slice)"
      contains: "validThroughLevel"
  key_links:
    - from: "334-DESIGN-LOCK-AFKING.md"
      to: "AfKing.sol Sub :86-93 + _autoBuy :630/:631 crossing + DegenerusGame.sol:1520 (hasAnyLazyPass → the new level-horizon view)"
      via: "field-placement + refresh-or-evict control flow"
      pattern: "validThroughLevel|lazyPassHorizon|hasAnyLazyPass"
---

<objective>
Author the AfKing pass-gated subscription design-lock (Phase 334 Success Criterion 1, the AFSUB slice). Even though AFSUB-01..05 land at IMPL 335, the SPEC settles their signatures now so the IMPL re-authors them in the one batched diff with zero "by construction" assumptions.

Purpose: The IMPL phase (335) re-authors the pass-gated AfKing subscription against these settled signatures — the validThroughLevel field placement, the new level-horizon pass view, the refresh-or-evict crossing control flow, the burnForKeeper removal across both contracts — and the preservation criteria (OPEN-E 4-protection, SUB-07 cancel-tombstone, v49 swap-pop) the pass-gated model must satisfy.
Output: One markdown design-lock artifact in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-CONTEXT.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RESEARCH.md

<interfaces>
<!-- AfKing pass-gating subject (anchors confirmed vs b0511ca2): -->
- AfKing.sol Sub struct :86-93 — { uint8 dailyQuantity; uint32 lastAutoBoughtDay; uint32 paidThroughDay (:89, the field to repurpose → validThroughLevel, a uint24 level fits the uint32 slot); uint8 reinvestPct; uint8 flags; address fundingSource; }
- AfKing.sol:630 — _autoBuy per-iter validity check `if (sub.paidThroughDay <= today)` → becomes `if (currentLevel > sub.validThroughLevel)`
- AfKing.sol:631 — the day-31 crossing `hasAnyLazyPass(player)` (fires ONLY at the crossing) → becomes re-read the level horizon ONCE → refresh-or-evict
- AfKing.sol:432 — subscribe-time `hasAnyLazyPass` (pass-OR-pay) → becomes set validThroughLevel from the level-horizon view
- AfKing.sol:374 — subscribe
- AfKing.sol:393-403 — OPENE-04 gate (`isOperatorApproved(fundingSource, subscriber)` consent gate)
- AfKing.sol:458 — setDailyQuantity (def); the in-autoBuy reclaim/tombstone logic is at _autoBuy :605
- AfKing.sol:214 — _autoBuyCursor; :220 — WINDOW_DAYS=30; :57 — burnForKeeper iface; FLAG_WINDOW_PAID + the BurnieAutoExtracted PAID-branch (to remove, D-09)
- BurnieCoin.sol:472 — burnForKeeper impl to delete; :85 KeeperBurn event; :549 onlyAfKing modifier (grep shows only burnForKeeper uses it)
- DegenerusGame.sol:1520 — hasAnyLazyPass impl (boolean today): deity bit (HAS_DEITY_PASS_SHIFT) → permanent; else frozenUntilLevel (FROZEN_UNTIL_LEVEL_SHIFT) > level. The new level-horizon view lives alongside it, exposed via IGame.
- D-08: pass-gating scope = the autoBuy sub window ONLY (cursor-driven); autoOpen is a permissionless router leg, UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle the AfKing field + level-horizon view signatures (SC1 — AFSUB structural slice)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md</files>
  <read_first>
    - contracts/AfKing.sol (the Sub struct :86-93, subscribe :374, the _autoBuy validity check :630 + crossing :631, hasAnyLazyPass subscribe :432, OPENE-04 gate :393-403, setDailyQuantity :458 + reclaim _autoBuy :605, WINDOW_DAYS :220, burnForKeeper iface :57, the FLAG_WINDOW_PAID / BurnieAutoExtracted PAID branch — quote real line numbers)
    - contracts/BurnieCoin.sol (burnForKeeper :472, KeeperBurn :85, onlyAfKing :549 — confirm burnForKeeper is the only onlyAfKing user)
    - contracts/DegenerusGame.sol (hasAnyLazyPass impl :1520 — the boolean today; the deity-bit + frozenUntilLevel source the new level-horizon view reads)
    - .planning/phases/.../334-RESEARCH.md ("Architecture Patterns — Pattern 2 + Pattern 3" the level-horizon view; "Don't Hand-Roll" the eviction/tombstone reuse; "Storage-layout note"; the Anti-Pattern "per-iteration external pass read")
    - .planning/phases/.../334-CONTEXT.md (D-08 scope=autoBuy only; D-09 burnForKeeper removed from BOTH; D-10 lazy-only, no refreshPass; D-11 the level-horizon view [deity = type.max sentinel]; D-12 preserved invariants; D-13 no migration; Claude's Discretion on the view name/signature + validThroughLevel field placement)
  </read_first>
  <action>
    Author 334-DESIGN-LOCK-AFKING.md settling the AfKing pass-gated subscription signatures. Record the validThroughLevel field placement (per D-11/D-12 + Claude's Discretion): repurpose the existing Sub.paidThroughDay slot (offset matching :89, uint32 — a uint24 level fits) → validThroughLevel; this is an in-place reinterpretation, not a layout break (D-13 makes any break fine regardless, pre-launch redeploy-fresh). Record the new level-horizon pass view signature (per D-11): a per-pass-type level coverage horizon (NOT a boolean) readable at subscribe AND at the crossing — deity = type(uint24).max permanent sentinel (never crosses, cheapest case), lazy/whale = the covered-through level (the frozenUntilLevel from mintPacked_); it lives alongside hasAnyLazyPass at DegenerusGame:1520 and is exposed via the IGame interface AfKing reads; record the recommended name lazyPassHorizon(address) returns (uint24) as Claude's-Discretion-constrained, leaving the final name/signature to IMPL. Record the refresh-or-evict crossing control flow (per D-12): subscribe sets `validThroughLevel = IGame(GAME).lazyPassHorizon(subscriber)`; the per-iter non-crossing check is the cheap stored-field compare `currentLevel <= sub.validThroughLevel` (NO per-iteration external pass read — preserves the GASOPT-05 win; the Anti-Pattern to avoid); at the crossing (`currentLevel > validThroughLevel`) re-read the horizon EXACTLY ONCE → if still covered refresh validThroughLevel and the sub continues, else evict via the existing setDailyQuantity(0)-equivalent in-place tombstone + the _autoBuy:605 swap-pop reclaim — this is NOT an unconditional kick, and the crossing is the ONLY external pass read on the hot path. Record D-08 scope: pass-gating gates the autoBuy sub window ONLY (cursor-driven, _autoBuyCursor :214); autoOpen is a permissionless router leg and stays UNCHANGED. Record D-09 burnForKeeper full removal from BOTH contracts: delete the AfKing burnForKeeper call sites + the paidThroughDay/WINDOW_DAYS window accounting + FLAG_WINDOW_PAID + the BurnieAutoExtracted PAID day-31 branch, AND the now-dead BurnieCoin.sol:472 burnForKeeper impl + KeeperBurn :85 + the onlyAfKing :549 modifier (after confirming no other onlyAfKing user — grep shows only burnForKeeper) — so the batched IMPL diff touches BurnieCoin.sol. Record D-10: lazy-only refresh, NO refreshPass() entrypoint (the crossing re-check already catches a post-subscribe upgrade — the upgrader is re-read and refreshed at the crossing, never wrongly evicted; the convenience entrypoint is skipped for smallest surface). Cite every file:line.
  </action>
  <acceptance_criteria>
    - 334-DESIGN-LOCK-AFKING.md exists and contains the string "validThroughLevel"
    - The doc records repurposing the Sub.paidThroughDay slot (AfKing.sol:86-93, field :89) → validThroughLevel as an in-place reinterpretation (D-13 redeploy-fresh)
    - The doc records the new level-horizon pass view: deity = type(uint24).max sentinel, lazy/whale = covered-through level, exposed via IGame, alongside hasAnyLazyPass at DegenerusGame:1520 (vs today's boolean)
    - The doc records the per-iter cheap compare `currentLevel <= validThroughLevel` with NO external pass read off-crossing (GASOPT-05 preserved), and the crossing as the ONLY external pass read → refresh-or-evict (not an unconditional kick)
    - The doc records burnForKeeper full removal from BOTH AfKing AND BurnieCoin (AfKing :57 + the window accounting; BurnieCoin :472 + KeeperBurn :85 + onlyAfKing :549), so the IMPL diff touches BurnieCoin.sol (D-09)
    - The doc records lazy-only / NO refreshPass (D-10) and pass-gating scope = autoBuy window ONLY, autoOpen unchanged (D-08)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -q "validThroughLevel" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qE "type\(uint24\)\.max|type\.max|uint24\).max" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -q "burnForKeeper" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -q "BurnieCoin" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qiE "refresh-or-evict|refresh or evict" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md</automated>
  </verify>
  <done>The AfKing structural signatures are settled: validThroughLevel field placement, the level-horizon view (deity sentinel), the refresh-or-evict crossing flow, burnForKeeper removal across both contracts, lazy-only/no-refreshPass, autoBuy-only scope — citing real anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Record the AFSUB preservation criteria (OPEN-E / SUB-07 tombstone / v49 swap-pop)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md</files>
  <read_first>
    - contracts/AfKing.sol (the OPENE-04 gate :393-403 `isOperatorApproved(fundingSource, subscriber)`, the fundingSource field in Sub :86-93, the setDailyQuantity(0) in-place tombstone :458 + the _autoBuy:605 swap-pop reclaim — quote real anchors)
    - .planning/phases/.../334-RESEARCH.md ("Security Domain — Keeper/sub access control" + the "Known threat patterns" table rows for OPEN-E + the H-CANCEL-SWAP-MISS class; "Don't Hand-Roll" the eviction/tombstone reuse)
    - .planning/phases/.../334-CONTEXT.md (D-12 the preserved invariants in full)
    - Memory context already in scope: open-e-operator-approval-trust-boundary (the 4 structural protections + the operator-approval IS the trust boundary, BURNIE-funding overload accepted-by-design), afking-cancel-tombstone-streak-finding (H-CANCEL-SWAP-MISS — setDailyQuantity(0) swap-pop must not relocate a pending tail behind the sweep cursor → missed day → streak reset)
  </read_first>
  <action>
    Append to 334-DESIGN-LOCK-AFKING.md a preservation-criteria section recording the invariants the pass-gated model MUST satisfy (the acceptance criteria for AFSUB-04/05 at IMPL 335). Record OPEN-E preservation (D-12, AFSUB-04): third-party box funding (the OPEN-E shared fundingSource in Sub :86-93) STAYS — pass-gating does NOT moot OPEN-E (third-party box funding is independent of the autoBuy pass window); the 4 OPEN-E structural protections re-attest to hold under the pass-gated model — (1) consent-gate-at-subscribe (the OPENE-04 `isOperatorApproved(fundingSource, subscriber)` gate at AfKing:393-403), (2) default-self byte-identical, (3) no-escalation, (4) trust-the-sub temporal bound — per open-e-operator-approval-trust-boundary (operator-approval IS the trust boundary; do NOT model a "tricked into approving" actor; BURNIE-funding overload is accepted-by-design). Record SUB-07 + swap-pop preservation (D-12, AFSUB-05): the eviction path reuses the existing setDailyQuantity(0) in-place cancel-tombstone (:458) + the _autoBuy:605 swap-pop reclaim, and the v49 swap-pop membership invariant (membership ⟺ packed != 0) holds — so pass-eviction at the crossing must NOT relocate a pending tail behind the mid-sweep cursor (the H-CANCEL-SWAP-MISS missed-day / mint-streak-reset class per afking-cancel-tombstone-streak-finding). State that these are the structural-preservation acceptance criteria the IMPL pass-gated model is built to satisfy (not bolted on), empirically re-attested by TST-02 at Phase 336 and the SWEEP-01 OPEN-E re-attest at Phase 338. Cite every file:line.
  </action>
  <acceptance_criteria>
    - 334-DESIGN-LOCK-AFKING.md contains a preservation-criteria section
    - The doc records OPEN-E preservation: fundingSource STAYS, pass-gating does NOT moot OPEN-E, and the 4 named structural protections (consent-gate-at-subscribe / default-self byte-identical / no-escalation / trust-the-sub temporal bound) re-attest under the pass-gated model, citing the OPENE-04 gate at AfKing:393-403
    - The doc records SUB-07 cancel-tombstone + the v49 swap-pop membership invariant (membership ⟺ packed != 0) preservation, citing setDailyQuantity :458 + _autoBuy :605
    - The doc names the H-CANCEL-SWAP-MISS missed-day class as the regression eviction must NOT reproduce
    - The doc records that these criteria are empirically re-attested at TST-02 (336) and the SWEEP-01 OPEN-E re-attest (338)
  </acceptance_criteria>
  <verify>
    <automated>grep -qiE "OPEN-E|OPENE" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qiE "swap-pop|swap pop" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qiE "tombstone" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qE "393-403|393–403|isOperatorApproved" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md && grep -qE "H-CANCEL-SWAP-MISS" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md</automated>
  </verify>
  <done>The AFSUB preservation criteria are recorded: OPEN-E 4-protection (fundingSource stays, not mooted), SUB-07 cancel-tombstone + v49 swap-pop membership invariant, the H-CANCEL-SWAP-MISS regression bar — citing real anchors, flagged for TST-02 + SWEEP-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the correctness of the security-bearing AfKing design decisions feeding IMPL 335.

| Boundary | Description |
|----------|-------------|
| SPEC AFSUB design-lock → IMPL 335 | A wrong preservation criterion lets a real eviction-griefing / consent-bypass vulnerability ship at IMPL. |
| keeper sub → third-party fundingSource (OPEN-E) | An operator funds boxes for a subscriber; the consent gate IS the trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-334-06 | Elevation of Privilege | OPEN-E fundingSource consent | mitigate | The design-lock records the 4 structural protections + the OPENE-04 consent-gate-at-subscribe (AfKing:393-403) as preservation criteria the pass-gated model must satisfy; operator-approval is the trust boundary (open-e-operator-approval-trust-boundary). |
| T-334-07 | Denial of Service | sub eviction griefing (H-CANCEL-SWAP-MISS class) | mitigate | The design-lock records the SUB-07 in-place tombstone (:458) + v49 swap-pop membership invariant (:605) reuse as the AFSUB-05 criterion — eviction must not relocate a pending tail behind the sweep cursor (missed-day/streak-reset). |
| T-334-08 | Spoofing | per-iter pass validity (GASOPT-05) | mitigate | The validity check is the cheap stored-field compare `currentLevel <= validThroughLevel`; the external pass re-read happens ONCE at the crossing — the Anti-Pattern "per-iteration external pass read" is explicitly forbidden in the design-lock. |
| T-334-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring (RESEARCH "Package Legitimacy Audit": not applicable). |
</threat_model>

<verification>
- 334-DESIGN-LOCK-AFKING.md exists with both the structural-signature section and the preservation-criteria section.
- validThroughLevel field placement, the level-horizon view (deity sentinel), refresh-or-evict crossing, burnForKeeper removal across both contracts, lazy-only.
- OPEN-E 4-protection, SUB-07 tombstone + swap-pop, H-CANCEL-SWAP-MISS bar.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 334-DESIGN-LOCK-AFKING.md satisfies all Task 1 + Task 2 acceptance criteria (SC1 AFSUB slice).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-03-SUMMARY.md` when done
</output>
