---
phase: 334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "The whale-pass shared signatures are settled: box-open O(1) record onto the existing whalePassClaims, the existing claimWhalePass(address) signature, claim-time anchoring, stats-at-claim"
    - "Q1 is recorded as LOCKED per D-21: box-open whale pass CONVERGES to the existing flat grant shape; the ≤level-10 40/lvl bonus band is DROPPED; single counter; value delta flagged to the 338 economic-analyst"
    - "The MintModule within-player index alignment shape is settled: :716 >>1 → += take matching :502"
    - "The RNGAUDIT R1→R4 structure + cold-start context-pack skeleton + the no-answer-key / package-only / model-agnostic framing are sketched as the Phase-337 authoring target"
    - "Every cited file:line is grep-attested vs b0511ca2 with drift corrected (no 'by construction' survives un-checked)"
  artifacts:
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md"
      provides: "The settled whale-pass + MintModule shared signatures (SC1, partial)"
      contains: "claimWhalePass"
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md"
      provides: "The RNGAUDIT R1→R4 + context-pack skeleton sketch (SC4)"
      contains: "no answer key"
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md"
      provides: "The complete grep-attestation table vs b0511ca2 (SC5)"
      contains: "b0511ca2"
  key_links:
    - from: "334-DESIGN-LOCK-WHALE-MINTDIV.md"
      to: "DegenerusGameWhaleModule.sol:1018 (existing claimWhalePass) + PayoutUtils.sol:45 (the += writer the box-open record mirrors)"
      via: "convergence-onto-existing-machinery"
      pattern: "claimWhalePass|whalePassClaims"
    - from: "334-GREP-ATTESTATION.md"
      to: "the v49.0-closure HEAD b0511ca2 (contracts/ byte-identical at working HEAD)"
      via: "git diff empty + per-anchor file:line confirmation"
      pattern: "b0511ca2"
---

<objective>
Author the cross-cutting BATCH-01 design-lock artifacts that record the established research conclusions: the whale-pass + MintModule shared signatures (Success Criterion 1, the whale/MintModule slice), the RNGAUDIT external-protocol structure sketch (Success Criterion 4), and the complete grep-attestation table vs the v49.0-closure HEAD b0511ca2 (Success Criterion 5).

Purpose: The IMPL phase (335) re-authors the whale-pass O(1) refactor + the MintModule one-liner against these settled signatures with zero "by construction" assumptions. Phase 337 authors the external RNG-audit kit against this structure sketch. The attestation table confirms every cited anchor is real against the frozen baseline.
Output: Three markdown artifacts in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-CONTEXT.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RESEARCH.md

<interfaces>
<!-- Whale-pass convergence (the existing machinery — D-20): -->
- DegenerusGameWhaleModule.sol:1018 — existing `claimWhalePass(address player) external` (permissionless w/ beneficiary arg = D-01; startLevel = level+1 = D-03; whalePassClaims count = D-02; _applyWhalePassStats at claim = D-04; _queueTicketRange(player, level+1, 100, halfPasses, false))
- DegenerusGamePayoutUtils.sol:45 — _queueWhalePassClaimCore (`whalePassClaims[winner] += fullHalfPasses` — the O(1) writer the box-open record mirrors)
- DegenerusGameJackpotModule.sol:1410 — `whalePassClaims[winner] += whalePassCount`
- DegenerusGameLootboxModule.sol:1240 — _activateWhalePass (the inline 100-loop :1250-1260 to delete); bonus constants :205 (TICKETS_PER_LEVEL=2), :207 (BONUS_TICKETS=40), :209 (BONUS_END=10); BOON_WHALE_PASS=28 :378
- DegenerusGame.sol:1561 (OPEN_NORMAL_GAS_UNIT), :1687 (autoOpen), :1728 (weighted += math) — the WHALE-03 autoOpen carve-out to retire (D-07)
<!-- MintModule alignment shape: -->
- DegenerusGameMintModule.sol:716 (`processed += writesUsed >> 1`) → align to :502 (`processed += take`)
<!-- RNGAUDIT structure inputs: -->
- VRF entry: rawFulfillRandomWords (AdvanceModule:1735, DegenerusGame.sol:2226); retryLootboxRng (AdvanceModule:1105, DegenerusGame.sol:2177) — exempt
- VRF gate/consume: rngGate (AdvanceModule:1152) returns rngWord; advanceGame (AdvanceModule:154) consume driver — exempt
- Lock lifecycle: rngLockedFlag = true (AdvanceModule:1640), = false (_unlockRng :1719/:1721); _livenessTriggered (Storage:1213); _VRF_GRACE_PERIOD = 14 days (Storage:198)
- rngLock mechanics: rngLockedFlag (Storage:279, bit-doc :55); write-time gate in _queueTickets/_queueTicketsScaled/_queueTicketRange; BurnieCoin.rngLocked() mirrors
- Module inventory: 11 modules under contracts/modules/ (Advance, Boon, Decimator, Degenerette, GameOver, Jackpot, Lootbox, Mint, MintStreakUtils, PayoutUtils, Whale) + contracts/storage/DegenerusGameStorage.sol + DegenerusGame.sol facade + peripherals (AfKing, BurnieCoin, BurnieCoinflip, DegenerusJackpots, GNRUS)
<!-- Grep-attestation: the full confirmed table is in 334-RESEARCH.md "Grep-Attestation Table" + "Newly-surfaced anchors". -->
<!-- git diff b0511ca2 HEAD -- contracts/ is EMPTY (confirmed) — working tree IS the frozen baseline. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the grep-attestation table vs b0511ca2 (SC5)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/.../334-RESEARCH.md ("Grep-Attestation Table (D-19)" + "Newly-surfaced anchors" — the complete authoritative table)
    - .planning/phases/.../334-CONTEXT.md (D-19 grep-attest every anchor; the Post-Research Reconciliation "Anchor corrections" — _livenessTriggered def Storage:1213 [not :571], Sub :86-93, processFutureTicketBatch :393)
    - contracts/modules/DegenerusGameLootboxModule.sol, contracts/storage/DegenerusGameStorage.sol, contracts/AfKing.sol, contracts/BurnieCoin.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGamePayoutUtils.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/DegenerusGame.sol (re-confirm each cited line with grep/sed; do not trust the seed's ~ approximations)
  </read_first>
  <action>
    First run `git diff b0511ca29130c36cbe9bfb44e282c7379f9778c9 HEAD -- contracts/` and confirm it is empty — record that the working tree IS the frozen v49.0-closure baseline for contracts/, so grep against the working tree IS grep against b0511ca2. Then author 334-GREP-ATTESTATION.md as a table with columns: Anchor | CONTEXT.md/seed said | Confirmed file:line | Notes/drift. Transcribe the complete authoritative table from 334-RESEARCH.md and re-confirm each row with grep/sed against contracts/ (do NOT copy blindly — verify). MUST include: the whale-pass _activateWhalePass:1240 + inline loop :1250-1260 + BOON_WHALE_PASS:378 + bonus constants :205/:207/:209 + jackpot event :1638; _applyWhalePassStats def Storage:1111; the corrected _livenessTriggered def at Storage:1213 (record that the :571 in Canonical References is a gate call-site, not the def — both real); the two _applyWhalePassStats callers WhaleModule:1032 + DecimatorModule:588; AfKing burnForKeeper iface :57, Sub layout :86-93 (paidThroughDay :89), WINDOW_DAYS :220, subscribe :374, OPENE-04 gate :393-403, hasAnyLazyPass :432 + :631, setDailyQuantity :458 (reclaim logic in _autoBuy :605), autoBuy cursor :214; BurnieCoin.burnForKeeper :472 (KeeperBurn :85, onlyAfKing :549); MintModule processFutureTicketBatch :393 (+= take :502), processTicketBatch :671 (writesUsed>>1 :716), _raritySymbolBatch :546, WRITES_BUDGET_SAFE :93. Also include the newly-surfaced anchors NOT in CONTEXT.md: existing claimWhalePass WhaleModule:1018, whalePassClaims counter (PayoutUtils:52, JackpotModule:1410, WhaleModule:1020/1024), _queueWhalePassClaimCore PayoutUtils:45, _queueTicketRange Storage:647 (gate :655/:661), the two processTicketBatch callers AdvanceModule:561/:1496, the autoOpen gas-weight DegenerusGame:1561/:1687/:1728, hasAnyLazyPass impl DegenerusGame:1520. Mark each row confirmed-exact or drift-corrected. End with a one-line attestation that NO "by construction" claim survives un-checked (the feedback_verify_call_graph_against_source floor).
  </action>
  <acceptance_criteria>
    - 334-GREP-ATTESTATION.md exists and records that `git diff b0511ca2 HEAD -- contracts/` is empty (working tree = frozen baseline)
    - The doc contains the string "b0511ca2"
    - The table records the corrected _livenessTriggered def at DegenerusGameStorage.sol:1213 (with the note that :571 is a gate call-site)
    - The table records the AfKing Sub struct at :86-93 and processFutureTicketBatch at :393
    - The table includes the newly-surfaced existing-machinery anchors: claimWhalePass WhaleModule:1018, _queueWhalePassClaimCore PayoutUtils:45, the two processTicketBatch callers AdvanceModule:561 and :1496
    - The table includes the MintModule :716 / :502 / :93 anchors and the AfKing :57 / :472 (BurnieCoin) burnForKeeper anchors
    - The doc ends with the no-"by-construction"-survives-unchecked attestation
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md && grep -q "b0511ca2" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md && grep -q "1213" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md && grep -q "1018" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md && grep -qE "86-93|86–93" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md && grep -q "716" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md</automated>
  </verify>
  <done>The grep-attestation table records the empty-diff baseline identity, every cited anchor confirmed/drift-corrected against b0511ca2 (incl. the corrected :1213 def, Sub :86-93, the newly-surfaced existing-machinery anchors), and the no-"by-construction" attestation.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the whale-pass + MintModule shared signatures (SC1 — whale/MintModule slice)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md</files>
  <read_first>
    - contracts/modules/DegenerusGameWhaleModule.sol (the existing claimWhalePass :1018 signature — quote it verbatim)
    - contracts/modules/DegenerusGamePayoutUtils.sol (the _queueWhalePassClaimCore :45 `whalePassClaims +=` writer the box-open record mirrors)
    - contracts/modules/DegenerusGameLootboxModule.sol (_activateWhalePass :1240, the inline loop :1250-1260, the bonus band constants :205/:207/:209, BOON_WHALE_PASS :378)
    - contracts/modules/DegenerusGameMintModule.sol (the :716 advance vs the :502 reference advance)
    - .planning/phases/.../334-RESEARCH.md ("Architecture Patterns — Pattern 1" + "Don't Hand-Roll" + Pitfall 1 + Pitfall 2 + "Alternatives Considered" — the grant-shape reconciliation)
    - .planning/phases/.../334-CONTEXT.md (D-01 through D-07 whale-pass decisions; D-20 [convergence onto existing whalePassClaims; pendingWhalePasses is a RELABEL — do NOT build a parallel map]; D-21 [Q1 LOCKED — flat grant shape, ≤level-10 40/lvl band DROPPED, single counter, value delta to 338 economic-analyst]; D-23 [gameOver-forfeit]; D-14/D-15 MintDiv; Claude's Discretion on the claimWhalePass entrypoint home + storage slot)
  </read_first>
  <action>
    Author 334-DESIGN-LOCK-WHALE-MINTDIV.md settling the whale-pass + MintModule shared signatures. Record the whale-pass pending-claim storage = the EXISTING `whalePassClaims[beneficiary]` uint counter (per D-02/D-20 — pendingWhalePasses is a RELABEL of this existing map, NOT a new structure; cite PayoutUtils:45 + JackpotModule:1410 as the existing writers). Record the claimWhalePass signature = the EXISTING deployed `claimWhalePass(address player) external` at WhaleModule:1018 (permissionless w/ beneficiary arg per D-01; HARD CONSTRAINT — the claim must NEVER be auto-triggered by box-open or autoOpen, per D-01). Record the box-open change = replace the _activateWhalePass:1240 inline 100-loop with a single O(1) `whalePassClaims[beneficiary] += grant` mirroring PayoutUtils:45; box-open writes NO mintPacked_ (stats move to claim per D-04); claim-time anchoring (D-03) and stats-at-claim (D-04) are realized by the existing claim, and the two other _applyWhalePassStats callers (WhaleModule:1032 bundle, DecimatorModule:588 Decimator) STAY immediate-apply UNTOUCHED. Record Q1 as a SETTLED DECISION (per D-21, NOT an open task): the box-open whale pass CONVERGES to the existing flat per-level grant shape — the early-game ≤level-10 WHALE_PASS_BONUS_TICKETS_PER_LEVEL=40 bonus band is DROPPED, the per-level rate aligns to the existing claim, a single counter (no grant-shape param, no second counter); this is a deliberate economic REDUCTION of the box-open whale-pass reward whose value delta is routed to the 338 SWEEP economic-analyst to sign off (per D-06). Do NOT use "v1/simplified/for-now" language — this is the final settled shape. Record WHALE-03 (per D-07): uniform O(1) opens follow from D-02/D-04, so the 331 whale-pass-weighted autoOpen budget (DegenerusGame:1561/:1687/:1728) is retired and OPEN_BATCH returns to flat per-box sizing at IMPL 335 (re-confirmed under worst-case uniform open). Record the MintModule within-player index alignment shape (per D-15): `:716` `processed += writesUsed >> 1` → `processed += take` matching the correct `processFutureTicketBatch:502`; the two near-duplicate loops STAY separate (full dedup explicitly rejected, D-15). Record the gameOver-forfeit rule (D-23): unclaimed whalePassClaims at gameOver are FORFEIT (no future levels to materialize; consistent with the existing claim's _livenessTriggered revert and "claim whenever is fine") — one sentence. Record the Claude's-Discretion items left to IMPL: the exact claimWhalePass entrypoint home (Game external fn delegating to WhaleModule vs the existing module-direct fn) and the precise pending-counter storage slot. Cite every file:line.
  </action>
  <acceptance_criteria>
    - 334-DESIGN-LOCK-WHALE-MINTDIV.md exists and references the existing claimWhalePass(address player) at WhaleModule:1018 (NOT a new claim function)
    - The doc records that the pending-claim storage is the EXISTING whalePassClaims counter (pendingWhalePasses is a relabel per D-20), citing PayoutUtils:45 and JackpotModule:1410 as existing writers
    - The doc records the box-open change as a single O(1) whalePassClaims += record replacing the _activateWhalePass:1240 inline loop, with NO mintPacked_ write at open
    - The doc records Q1 as LOCKED per D-21: convergence to the flat grant shape, the ≤level-10 40/lvl bonus band DROPPED, single counter, value delta flagged to the 338 economic-analyst — and contains NO "v1"/"simplified"/"for now" scope-reduction language
    - The doc records the MintModule alignment shape: :716 >>1 → += take matching :502; the two loops stay separate
    - The doc records the HARD CONSTRAINT that the claim is never auto-triggered by box-open/autoOpen (D-01)
    - The doc records the gameOver-forfeit rule (D-23) for unclaimed whalePassClaims
    - The doc records WHALE-03 (autoOpen carve-out retirement follows from D-02/D-04, IMPL-335 work) and names the two Claude's-Discretion IMPL items
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && grep -q "claimWhalePass" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && grep -q "whalePassClaims" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && grep -q "D-21" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && grep -q "forfeit" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && grep -qiE "take" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md && ! grep -qiE "\bv1\b|simplified version|for now" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md</automated>
  </verify>
  <done>The whale-pass + MintModule signatures are settled: convergence onto the existing claimWhalePass/whalePassClaims, Q1 LOCKED (flat shape, bonus band dropped, value delta to 338), the box-open O(1) record, the :716→:502 alignment, gameOver-forfeit, and the discretion items — no scope-reduction language.</done>
</task>

<task type="auto">
  <name>Task 3: Sketch the RNGAUDIT external-protocol structure (SC4)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md</files>
  <read_first>
    - .planning/phases/.../334-RESEARCH.md ("RNGAUDIT Structure Inputs" — VRF word entry/consume points, rngLock mechanics, module inventory, exempt entry points, the R1→R4 skeleton)
    - .planning/phases/.../334-CONTEXT.md (D-17 — the locked R1→R4 sequence + cold-start context-pack skeleton + the "no answer key" / package-only / model-agnostic framing)
    - .planning/ROADMAP.md (Phase 337 entry + its 4 Success Criteria — the authoring target this sketch feeds; RNGAUDIT-01..04 in REQUIREMENTS.md)
    - contracts/modules/DegenerusGameAdvanceModule.sol (rawFulfillRandomWords :1735, rngGate :1152, advanceGame :154, retryLootboxRng :1105, rngLockedFlag = true :1640 / _unlockRng :1719/:1721 — confirm the exempt entry points and lock lifecycle anchors)
  </read_first>
  <action>
    Author 334-RNGAUDIT-STRUCTURE-SKETCH.md fixing the Phase-337 authoring target (sketch only — 337 authors against the FROZEN post-v50 tree). Record the freeze invariant as the external auditor's target (RNGAUDIT-01 input): "while rngLockedFlag = true, every storage slot that participates in any VRF-influenced output is frozen until rngLockedFlag = false; only the incoming VRF word + its deterministic derivations may be unknown" — grounded in v45-vrf-freeze-invariant. Record the exempt entry points: advanceGame() + reachable resolution flow, the VRF coordinator callback (rawFulfillRandomWords AdvanceModule:1735 / DegenerusGame:2226), retryLootboxRng() failsafe (AdvanceModule:1105 / DegenerusGame:2177) — these legitimately write VRF-window slots because they ARE the resolution. Record the R1→R4 multi-round sequence as headings: R1 catalog the VRF read-graph (every participating slot with writers + readers across all 11 modules) → R2 independently re-derive each slot's freeze status (frozen / reverts-if-written-during-lock / proven-non-participating) → R3 adversarially challenge the catalog (hunt any writer that escapes the freeze, any cross-module composition that does) → R4 reconcile + report. Record the self-contained cold-start context-pack skeleton (the sections 337 will fill): the module/RNG-window map · the rngLock mechanics (rngLockedFlag Storage:279, the _queueTickets/_queueTicketsScaled/_queueTicketRange far-future + rngLock write-time gate, BurnieCoin.rngLocked() mirror, _livenessTriggered Storage:1213, _VRF_GRACE_PERIOD Storage:198) · where the VRF word enters and is consumed (entry rawFulfillRandomWords; gate rngGate AdvanceModule:1152; consume sites — _processFutureTicketBatch, payDailyJackpot, _distributeYieldSurplus, rollLevelQuest, _emitDailyWinningTraits, _gameOverEntropy, the lootbox lootboxRngWordByIndex path consumed in processTicketBatch:696) · the contract inventory (11 modules under contracts/modules/ + DegenerusGameStorage.sol + DegenerusGame.sol facade + peripherals AfKing/BurnieCoin/BurnieCoinflip/DegenerusJackpots/GNRUS) · the back-and-forth cross-module variable-tracing methodology ("trace every variable across modules — what writes it, what reads it, what is locked during an RNG window"). Record the constraints (D-17): "drive the external model's OWN discovery — NO answer key, no embedded internal findings ('different perspective' is the point)"; PACKAGE-ONLY (running it through Gemini/ChatGPT + triaging is a FUTURE cycle, OUT of v50.0); model-agnostic (usable in both Gemini and ChatGPT, with context-window chunking guidance for feeding the contracts). State explicitly that this is a STRUCTURE SKETCH and full authoring against the FROZEN post-v50 tree is Phase 337. Cite the VRF/lock anchors.
  </action>
  <acceptance_criteria>
    - 334-RNGAUDIT-STRUCTURE-SKETCH.md exists and states the freeze invariant precisely ("while rngLockedFlag = true ... frozen until rngLockedFlag = false")
    - The doc records the four exempt entry points (advanceGame + resolution flow, rawFulfillRandomWords, retryLootboxRng failsafe) with their anchors
    - The doc contains the R1→R4 sequence as four labeled rounds (catalog → independent re-derive → adversarial challenge → reconcile/report)
    - The doc contains the cold-start context-pack skeleton sections: module/RNG-window map, rngLock mechanics, VRF word entry/consume points, contract inventory, variable-tracing methodology
    - The doc contains the literal "no answer key" constraint and the PACKAGE-ONLY + model-agnostic framing
    - The doc states it is a STRUCTURE SKETCH and that full authoring is Phase 337 against the FROZEN post-v50 tree
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md && grep -q "no answer key" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md && grep -qiE "package-only|package only" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md && grep -qE "R1|R2|R3|R4" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md && grep -q "rngLockedFlag" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md && grep -q "337" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md</automated>
  </verify>
  <done>The RNGAUDIT structure is sketched: the freeze-invariant target + exempt entry points, the R1→R4 sequence, the cold-start context-pack skeleton, and the no-answer-key / package-only / model-agnostic framing — flagged as the Phase-337 authoring target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the correctness + completeness of the design-lock decisions and the anchor attestation feeding IMPL 335 / AUDIT-PROTOCOL 337.

| Boundary | Description |
|----------|-------------|
| SPEC design-lock → IMPL 335 | A mis-settled signature or an un-attested anchor lets a broken/wrong-surface diff ship at IMPL. |
| SPEC structure sketch → AUDIT-PROTOCOL 337 | A structure sketch that leaks internal findings would defeat the "drive the external model's OWN discovery" intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-334-03 | Tampering | grep-attestation table | mitigate | Every cited file:line re-confirmed with grep/sed against contracts/ at the empty-diff baseline (b0511ca2); drift corrected (e.g. _livenessTriggered def :1213 vs gate :571); no "by construction" survives un-checked (feedback_verify_call_graph_against_source). |
| T-334-04 | Tampering | whale-pass signature reconciliation | mitigate | The design-lock converges onto the EXISTING claimWhalePass/whalePassClaims (D-20) rather than authoring a parallel map (Pitfall 1) — avoiding a doubled, untested surface; Q1 economic delta is explicitly routed to the 338 economic-analyst (D-06/D-21), not silently absorbed. |
| T-334-05 | Information Disclosure | RNGAUDIT structure sketch | mitigate | The sketch records the explicit "no answer key / no embedded internal findings" constraint so the Phase-337 deliverable forces the external model's own discovery; it ships no internal audit conclusions. |
| T-334-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring (RESEARCH "Package Legitimacy Audit": not applicable). |
</threat_model>

<verification>
- All three artifacts exist in the phase directory.
- Grep-attestation: empty-diff baseline recorded; corrected anchors (:1213, Sub :86-93); newly-surfaced existing-machinery anchors; no-"by-construction" attestation.
- Whale/MintDiv design-lock: existing claimWhalePass/whalePassClaims convergence, Q1 LOCKED, :716→:502 alignment, gameOver-forfeit, no scope-reduction language.
- RNGAUDIT sketch: freeze invariant, exempt entry points, R1→R4, context-pack skeleton, no-answer-key/package-only/model-agnostic.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 334-GREP-ATTESTATION.md satisfies all Task 1 acceptance criteria (SC5).
- 334-DESIGN-LOCK-WHALE-MINTDIV.md satisfies all Task 2 acceptance criteria (SC1 whale/MintDiv slice).
- 334-RNGAUDIT-STRUCTURE-SKETCH.md satisfies all Task 3 acceptance criteria (SC4).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-02-SUMMARY.md` when done
</output>
