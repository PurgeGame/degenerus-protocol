---
phase: 334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md
  - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md
autonomous: true
requirements: [WHALE-04, MINTDIV-01]

must_haves:
  truths:
    - "The WHALE-04 freeze-safety proof is recorded as a §1–§5 slot-by-slot argument with the verdict FREEZE-SAFE"
    - "The proof maps box-open writes = {whalePassClaims} (no frozen slot) and claimWhalePass writes = {whalePassClaims←0, mintPacked_ future-anchored, ticketsOwedPacked future levels}"
    - "The MINTDIV-01 verdict is recorded as PROVEN REACHABLE with the writesUsed>>1 vs +=take arithmetic trace"
    - "The MINTDIV-01 verdict cites both live callers (AdvanceModule:561 gameover-drain, :1496 advance-drain) and the owed=300/maxT=292 concrete scenario"
    - "The MINTDIV-01 verdict decides MINTDIV-02 scope: REACHABLE → the D-15 one-liner ships; the D-16 NEGATIVE branch does NOT apply"
  artifacts:
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md"
      provides: "The WHALE-04 RNG-freeze-safety proof for the deferred-claim split (SC2)"
      contains: "FREEZE-SAFE"
    - path: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md"
      provides: "The MINTDIV-01 PROVEN-REACHABLE verdict with traced evidence (SC3)"
      contains: "PROVEN REACHABLE"
  key_links:
    - from: "334-WHALE04-FREEZE-PROOF.md"
      to: "DegenerusGameStorage.sol:655/:661 (the _queueTicketRange far-future + rngLock gate) + WhaleModule:1019 (_livenessTriggered revert)"
      via: "slot-by-slot citation"
      pattern: "_queueTicketRange|_livenessTriggered"
    - from: "334-MINTDIV01-REACHABILITY-VERDICT.md"
      to: "DegenerusGameMintModule.sol:716 / :502 + AdvanceModule.sol:561 / :1496"
      via: "divergence-arithmetic + two-caller citation"
      pattern: "writesUsed >> 1|processed \\+= take|AdvanceModule"
---

<objective>
Record the two design-gating PROOFS that the research has already established, as standalone verifiable SPEC artifacts: the WHALE-04 RNG-freeze-safety proof of the deferred whale-pass claim split (Phase 334 Success Criterion 2), and the MINTDIV-01 divergence reachability verdict (Success Criterion 3).

Purpose: These two proofs GATE the IMPL phase (335). WHALE-04's FREEZE-SAFE verdict authorizes the box-open → deferred-claim split; MINTDIV-01's REACHABLE verdict decides that MINTDIV-02 ships the D-15 one-liner rather than closing as a documented NEGATIVE. Both must be recorded as verifiable acceptance criteria (not re-derived) so the IMPL phase executes without re-asking.
Output: Two markdown proof documents in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-CONTEXT.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RESEARCH.md

<interfaces>
<!-- Anchors confirmed against b0511ca2 (contracts/ at HEAD is byte-identical; git diff empty). -->
<!-- WHALE-04 proof load-bearing facts: -->
- DegenerusGameStorage.sol:560-573 — _queueTickets: `if (_livenessTriggered()) revert E();` then `bool isFarFuture = targetLevel > level + 5;` then `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();`
- DegenerusGameStorage.sol:647 — _queueTicketRange ("optimized for whale pass claims"); same far-future + rngLock gate at :655/:661
- DegenerusGameStorage.sol:1111 — _applyWhalePassStats def (writes mintPacked_ frozenUntilLevel/levelCount, delta-based, no double-dip)
- DegenerusGameStorage.sol:1213 — _livenessTriggered def (the :571 cited elsewhere is a gate call-site)
- DegenerusGameStorage.sol:279 — rngLockedFlag
- DegenerusGameWhaleModule.sol:1018 — claimWhalePass(address player); :1019 reverts under _livenessTriggered; startLevel = level+1; _queueTicketRange(player, startLevel, 100, halfPasses, false)
- DegenerusGamePayoutUtils.sol:45 — _queueWhalePassClaimCore (the reference O(1) `whalePassClaims[winner] += ...` writer)
- DegenerusGameJackpotModule.sol:1410 — `whalePassClaims[winner] += whalePassCount`
- DegenerusGameLootboxModule.sol:1240 — _activateWhalePass (passLevel = level+1 at :1243; the inline 100-loop :1250-1260 box-open mints to delete)
- DegenerusGameWhaleModule.sol:1032 + DegenerusGameDecimatorModule.sol:588 — the two _applyWhalePassStats callers that STAY immediate-apply (D-04, untouched)

<!-- MINTDIV-01 proof load-bearing facts: -->
- DegenerusGameMintModule.sol:393 — processFutureTicketBatch (CORRECT); `processed += take` at :502
- DegenerusGameMintModule.sol:671 — processTicketBatch (SUSPECT); `processed += writesUsed >> 1` at :716
- DegenerusGameMintModule.sol:546 — _raritySymbolBatch (the startIndex-driven LCG consumer; group/offset/quadrant at :565-597)
- DegenerusGameMintModule.sol:93 — `WRITES_BUDGET_SAFE = 550`
- writesUsed formula: `(take<=256 ? take*2 : take+256) + baseOv + (take==owed?1:0)`, baseOv ∈ {2,4}; maxT ~292 warm / ~99 cold
- DegenerusGameAdvanceModule.sol:561 — processTicketBatch caller (gameover terminal-jackpot drain; comment at :552 anticipates "queue exceeds the block gas limit")
- DegenerusGameAdvanceModule.sol:1496 — processTicketBatch caller (_runProcessTicketBatch, advance-time current-level drain)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the WHALE-04 RNG-freeze-safety proof (SC2)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the _queueTickets gate :560-573, _queueTicketRange :647/:655/:661, _applyWhalePassStats :1111, _livenessTriggered def :1213, rngLockedFlag :279 — quote real line numbers, do not assume)
    - contracts/modules/DegenerusGameWhaleModule.sol (claimWhalePass :1018, the :1019 _livenessTriggered revert, startLevel = level+1, _queueTicketRange call, the bundle _applyWhalePassStats caller :1032 that stays untouched)
    - contracts/modules/DegenerusGameLootboxModule.sol (_activateWhalePass :1240, the inline 100-loop :1250-1260 that box-open replaces)
    - .planning/phases/.../334-RESEARCH.md ("WHALE-04 Freeze-Safety Proof (slot-by-slot)" §1–§5 + Common Pitfall 4 — the far-future level+5 boundary analysis)
    - .planning/phases/.../334-CONTEXT.md (D-01, D-02 [and D-20: pendingWhalePasses is a RELABEL of the existing whalePassClaims], D-03 claim-time anchoring, D-04 stats-at-claim, the v45-vrf-freeze-invariant)
  </read_first>
  <action>
    Author 334-WHALE04-FREEZE-PROOF.md recording the §1–§5 slot-by-slot freeze-safety argument from 334-RESEARCH.md with verdict FREEZE-SAFE. State up front (per D-20) that WHALE is a CONVERGENCE onto the existing deployed claimWhalePass(address)/whalePassClaims machinery — the box-open O(1) record is `whalePassClaims[beneficiary] += grant` mirroring PayoutUtils.sol:45, NOT a new pendingWhalePasses map. Record §1: box-open O(1) record writes ONLY whalePassClaims (a counter, not a VRF-influenced slot); no mintPacked_ and no ticketsOwedPacked write at open (per D-04). §2: claimWhalePass queues currentLevel+1..+100 — currentLevel+6..+100 are far-future (targetLevel > level+5) gated by _queueTicketRange's `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked()` (Storage:661, rngBypass=false passed at WhaleModule:1034); currentLevel+1..+5 are near-future, a disjoint write keyspace (_tqWriteKey(targetLevel) for targetLevel > level) from the current read slot (_tqReadKey(level)); and claimWhalePass:1019 reverts entirely under _livenessTriggered (def Storage:1213) during the terminal window. §3: _applyWhalePassStats at claim writes mintPacked_ future-anchored to ticketStartLevel = currentLevel+1 (targetFrozenLevel = +99), perturbing no current-frozen input — same internal already proven freeze-safe at the WhaleModule:1032 + DecimatorModule:588 immediate-apply callers (which D-04 keeps UNTOUCHED); moving the box-open caller to claim-time changes WHEN, not WHICH slots. §4: liveness gate keeps the pass claimable-eventually — the whalePassClaims counter persists across rngLock (never gated; only the materializing claim reverts), so no grant is marooned. §5: re-attest v45-vrf-freeze-invariant for the split — the current-window VRF-derived outputs read only current-level ticketsOwedPacked + current mintPacked_ freeze state, none of which the deferred record or gated claim mutate within the locked window. Include the far-future level+5 boundary analysis explicitly (Pitfall 4 — do not write "future levels therefore safe" without the level+5 boundary). Cite every file:line.
  </action>
  <acceptance_criteria>
    - 334-WHALE04-FREEZE-PROOF.md exists and contains the literal verdict "FREEZE-SAFE"
    - The doc contains five labeled sections §1 through §5
    - §1 states box-open writes ONLY whalePassClaims and writes NO mintPacked_ / NO ticketsOwedPacked at open
    - §2 cites the _queueTicketRange far-future + rngLock gate at DegenerusGameStorage.sol:661 AND the claimWhalePass _livenessTriggered revert at DegenerusGameWhaleModule.sol:1019, AND distinguishes the currentLevel+6..+100 far-future band (gated) from the currentLevel+1..+5 near-future disjoint keyspace
    - §3 cites _applyWhalePassStats def at DegenerusGameStorage.sol:1111 and names the two callers (WhaleModule:1032 bundle, DecimatorModule:588 Decimator) that STAY immediate-apply per D-04
    - The doc states the box-open write set = {whalePassClaims} and the claimWhalePass write set = {whalePassClaims←0, mintPacked_ future-anchored, ticketsOwedPacked future levels (far-future gated / near-future disjoint)}
    - §5 re-attests v45-vrf-freeze-invariant by name for the split
    - The doc records (per D-20) that this is a re-attestation of EXISTING gates on the existing claimWhalePass machinery, not a novel claim system, and that pendingWhalePasses is a relabel of whalePassClaims
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md && grep -q "FREEZE-SAFE" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md && grep -qE "661|_queueTicketRange" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md && grep -q "1019" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md && grep -q "v45-vrf-freeze-invariant" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md</automated>
  </verify>
  <done>The WHALE-04 freeze proof is recorded with verdict FREEZE-SAFE, the §1–§5 slot map, the far-future level+5 boundary, the two preserved immediate-apply callers, and the v45 re-attestation — citing real anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Record the MINTDIV-01 reachability verdict (SC3)</name>
  <files>.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md</files>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (processFutureTicketBatch :393 with `processed += take` :502; processTicketBatch :671 with `processed += writesUsed >> 1` :716; _raritySymbolBatch :546 and the LCG group/offset/quadrant at :565-597; WRITES_BUDGET_SAFE :93 — read and quote real numbers)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the two processTicketBatch callers at :561 gameover-drain [note the :552 "queue exceeds the block gas limit" comment] and :1496 advance-drain)
    - .planning/phases/.../334-RESEARCH.md ("MINTDIV-01 Reachability Verdict" + "Code Examples — the divergence arithmetic" + Common Pitfall 3 — the missing second caller)
    - .planning/phases/.../334-CONTEXT.md (D-14 proof-not-assertion, D-15 the if-reachable one-liner, D-16 the if-refuted NEGATIVE, and D-22 the locked PROVEN-REACHABLE reconciliation)
  </read_first>
  <action>
    Author 334-MINTDIV01-REACHABILITY-VERDICT.md recording the verdict PROVEN REACHABLE (per the locked D-22). Record Leg (a) — the divergence is arithmetic fact: writesUsed = (take<=256 ? take*2 : take+256) + baseOv + (take==owed?1:0) with baseOv ∈ {2,4}, so `writesUsed >> 1 != take` whenever the not-finished branch (advance==false, take < owed) fires; include the two worked numbers against the real WRITES_BUDGET_SAFE=550: warm owed=1000 → maxT=292, take=292, writesUsed=550, writesUsed>>1=275 vs take=292 → divergence −17; cold first batch (budget 357) owed=1000 → maxT=99, take=99, writesUsed=200, writesUsed>>1=100 vs take=99 → divergence +1. State that the wrong `processed` becomes the wrong startIndex into _raritySymbolBatch (:546), which deterministically drives the per-ticket LCG group (i>>4) / offset (i&15) / quadrant (i&3) at :565-597 → divergent traits. Record Leg (b) — the split branch (take < owed) is LIVE not dead: it requires owed > maxT (~292 warm / ~99 cold), and processTicketBatch has TWO confirmed callers — AdvanceModule:561 (gameover terminal-jackpot drain, the :552 comment anticipates large queues) and AdvanceModule:1496 (_runProcessTicketBatch advance-time current-level drain). Record the concrete reachability scenario: a single player accumulates owed=300 at level L; when L becomes the current read slot and processTicketBatch(L) runs warm (budget 550), maxT=292 so take=292 < owed=300 → not-finished branch → `processed += writesUsed>>1 = 275` (should be 292) → the next batch resumes _raritySymbolBatch at startIndex=275, re-generating ticket-indices 275..291 (overlap) and skipping the correct continuation → the 300 ticket-traits are NOT the contiguous LCG sequence a single pass produces → DIVERGENT TRAITS confirmed. Record the verdict decision (per D-22/D-15): REACHABLE → MINTDIV-02 SHIPS the D-15 one-liner (`:716` `processed += writesUsed >> 1` → `processed += take`, matching the correct `:502`) at IMPL 335; the not-reachable NEGATIVE branch (D-16) does NOT apply; the two near-duplicate loops STAY separate (full dedup explicitly rejected). Flag (per the research Assumptions A2) that the exact minimal owed>maxT scenario is what TST-03 codifies empirically at Phase 336 — but the divergence MECHANISM (Leg a) is certain regardless. Avoid the Pitfall-3 single-caller trap: enumerate BOTH callers. Cite every file:line.
  </action>
  <acceptance_criteria>
    - 334-MINTDIV01-REACHABILITY-VERDICT.md exists and contains the literal verdict "PROVEN REACHABLE"
    - The doc records both worked divergence numbers: warm −17 (owed=1000, maxT=292, writesUsed>>1=275 vs take=292) and cold +1 (owed=1000, maxT=99, writesUsed>>1=100 vs take=99)
    - The doc cites DegenerusGameMintModule.sol:716 (`processed += writesUsed >> 1`) and :502 (`processed += take`) as the diverging vs correct advances
    - The doc enumerates BOTH live callers: AdvanceModule:561 (gameover-drain) and :1496 (advance-drain) — not a single-caller argument
    - The doc records the concrete owed=300 / maxT=292 / processed+=275-instead-of-292 / startIndex=275 scenario yielding divergent traits
    - The doc records the decision: REACHABLE → MINTDIV-02 ships the D-15 `:716`→`:502` one-liner; the D-16 NEGATIVE branch does NOT apply; the two loops stay separate
    - The doc flags that TST-03 (Phase 336) codifies the exact minimal scenario empirically
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -q "PROVEN REACHABLE" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -q "716" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -q "502" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -qE "561" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -qE "1496" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md && grep -q "MINTDIV-02" .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md</automated>
  </verify>
  <done>The MINTDIV-01 verdict is recorded as PROVEN REACHABLE with the −17/+1 arithmetic trace, both live callers (:561 / :1496), the owed=300/maxT=292 scenario, and the decision that MINTDIV-02 ships the one-liner — citing real anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only SPEC phase that ships no code. The threat surface is the CORRECTNESS of the security-bearing design proofs this plan records.

| Boundary | Description |
|----------|-------------|
| SPEC proof → IMPL 335 | A wrong freeze proof or an un-attested reachability claim lets a real vulnerability ship at IMPL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-334-01 | Tampering | WHALE-04 freeze proof | mitigate | The §1–§5 slot-by-slot proof cites real grep-attested anchors (Storage:661 gate, WhaleModule:1019 liveness revert); the far-future level+5 boundary is analyzed explicitly so "future levels therefore safe" hand-waving cannot ship. |
| T-334-02 | Tampering | MINTDIV-01 verdict | mitigate | The verdict enumerates BOTH live callers (AdvanceModule:561/:1496) — the feedback_verify_call_graph_against_source floor — and grounds REACHABLE in arithmetic fact + a concrete owed=300 scenario, not a single-caller "by construction" claim. |
| T-334-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring against the frozen tree (RESEARCH "Package Legitimacy Audit": not applicable). |
</threat_model>

<verification>
- Both proof documents exist in the phase directory.
- WHALE-04 proof: verdict FREEZE-SAFE, §1–§5, the gate + liveness anchors, v45 re-attestation.
- MINTDIV-01 verdict: PROVEN REACHABLE, the −17/+1 trace, both callers, the owed=300 scenario, the MINTDIV-02 decision.
- No contracts/*.sol modified (paper-only): `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 334-WHALE04-FREEZE-PROOF.md satisfies all Task 1 acceptance criteria (SC2).
- 334-MINTDIV01-REACHABILITY-VERDICT.md satisfies all Task 2 acceptance criteria (SC3).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-01-SUMMARY.md` when done
</output>
