---
phase: 337-audit-protocol-author-the-model-agnostic-multi-round-externa
plan: 03
type: execute
wave: 3
depends_on: ["337-01", "337-02"]
files_modified:
  - audit/rng-audit-kit/CHUNK-MANIFEST.md
  - audit/rng-audit-kit/RNG-AUDIT-KIT.md
autonomous: true
requirements: [RNGAUDIT-04]
must_haves:
  truths:
    - "CHUNK-MANIFEST.md inventories the full corpus (18-file core + DegenerusQuests) with per-file line/char counts that match wc at HEAD and group token sums that add up"
    - "The manifest defines the 3 chunk groups (RNG-CORE / CONSUME-B / FACADE+PERIPHERAL-C) with DegenerusGameStorage.sol travelling in EVERY chunked group"
    - "The kit ships a per-model feeding recipe (Gemini one-feed / GPT-5.5 + GPT-4.1 one-feed / ChatGPT-web 3-group chunked) and reflects the post-v50 frozen surface"
    - "The kit states explicitly PACKAGE-ONLY — running it through Gemini/ChatGPT + triaging output is a FUTURE cycle, OUT of v50.0"
  artifacts:
    - path: "audit/rng-audit-kit/CHUNK-MANIFEST.md"
      provides: "Contract-corpus chunking map (file inventory + sizes + 3 groups + Storage-travels rule) — the human-ops manual"
      min_lines: 60
    - path: "audit/rng-audit-kit/RNG-AUDIT-KIT.md"
      provides: "The model-agnostic feeding recipe + the explicit PACKAGE-ONLY / future-cycle statement (RNGAUDIT-04) appended to the kit"
      min_lines: 40
  key_links:
    - from: "audit/rng-audit-kit/CHUNK-MANIFEST.md per-file sizes"
      to: "wc -l / wc -c at HEAD"
      via: "manifest counts match the working tree"
      pattern: "DegenerusGame.sol"
    - from: "RNG-AUDIT-KIT.md feeding recipe"
      to: "CHUNK-MANIFEST.md groups"
      via: "the recipe references the 3 groups for the ChatGPT-web chunked path"
      pattern: "RNG-CORE"
---

<objective>
Author the PACKAGING layer (RNGAUDIT-04): (1) `CHUNK-MANIFEST.md` — the human-ops manual that inventories the contract corpus with sizes and defines the chunk groups so the cross-module read-graph survives a split; and (2) the model-agnostic feeding recipe + the explicit PACKAGE-ONLY statement, appended to `RNG-AUDIT-KIT.md`. This completes Layout B: `RNG-AUDIT-KIT.md` = the paste-into-model artifact (protocol + context pack + recipe), `CHUNK-MANIFEST.md` = the operator's chunking/attachment manual.

Purpose: a human in a FUTURE cycle must be able to feed the frozen post-v50 contracts to either Gemini or ChatGPT and run R1→R4 — with Storage travelling in every chunk so every freeze claim resolves against the slot layout + write-gates.
Output: `audit/rng-audit-kit/CHUNK-MANIFEST.md` + the feeding-recipe / package-only sections of `RNG-AUDIT-KIT.md`.

DEFAULT FLAGGED FOR USER REVIEW: per RESEARCH §5 A3, INCLUDE `DegenerusQuests.sol` in the corpus (it is the named `rollLevelQuest` VRF consume site; omitting it leaves the read-graph incomplete). Total ~280K tokens. The user may scope it out at the gate; the default is to include it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md
@audit/rng-audit-kit/RNG-AUDIT-KIT.md
@audit/C4A-CONTEST-README.md

<interfaces>
<!-- Corpus sizes measured by the planner at HEAD (RESEARCH §5; chars/token ~3.6, rounded up). The executor MUST re-run wc and use the live counts. -->
<!-- 18-file core set + DegenerusQuests = ~280K tokens. -->
RNG-CORE group (~114K tok — the irreducible core, never split): DegenerusGameStorage.sol, DegenerusGameAdvanceModule.sol, DegenerusGameMintModule.sol, DegenerusGameLootboxModule.sol, DegenerusQuests.sol
CONSUME-B group (~73K tok, re-attach Storage): DegenerusGameJackpotModule, DegenerusGameDegeneretteModule, DegenerusGameDecimatorModule, DegenerusGameWhaleModule, DegenerusGameBoonModule, DegenerusGameGameOverModule, DegenerusGameMintStreakUtils, DegenerusGamePayoutUtils
FACADE+PERIPHERAL-C group (~93K tok): DegenerusGame.sol, AfKing.sol, BurnieCoin.sol, BurnieCoinflip.sol, DegenerusJackpots.sol, GNRUS.sol

Context windows (RESEARCH §6, May 2026): Gemini 2.5 Pro ~1,048,576 (whole corpus fits, ~720K headroom); GPT-5.5 API 1,000,000 (fits); GPT-4.1 1,047,576 (fits); GPT-5.4 standard 272,000 (~280K JUST exceeds — chunk or Codex/1M); ChatGPT web UI = far below the API ceiling (the binding reason chunking guidance is required regardless of model).

Storage-travels-with-every-chunk: DegenerusGameStorage.sol (~24K tok) re-attaches to each chunked group because the delegatecall facade puts a slot's writer and reader in different files; the slot layout + the 3 write-gates are the shared anchor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CHUNK-MANIFEST.md (inventory + sizes + 3 groups + Storage-travels rule)</name>
  <files>audit/rng-audit-kit/CHUNK-MANIFEST.md</files>
  <read_first>
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§5 the full size table + the 3-group strategy + "Storage MUST travel with every group"; A3 include Quests)
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md (the context-pack 4d inventory — the manifest must list the same file set)
    - audit/C4A-CONTEST-README.md (the "Key Contracts" inventory-table house style)
    - contracts/ (re-run wc -l / wc -c for each listed file at HEAD to populate live counts — do not copy the RESEARCH numbers blindly)
  </read_first>
  <action>
Create `audit/rng-audit-kit/CHUNK-MANIFEST.md` — the operator's chunking/feeding manual (kept SEPARATE from RNG-AUDIT-KIT.md so the paste-into-model artifact carries no human-ops noise). Author three sections.

`## Corpus Inventory`: a table over the 18-file core set + DegenerusQuests.sol with columns File · Lines · Chars · ~Tokens · Group. Populate Lines/Chars by RE-RUNNING `wc -l`/`wc -c` against HEAD (the executor runs these; record the live numbers). Compute ~Tokens at ~3.6 chars/token rounded up. Add a SUBTOTAL row (18-file core) and a TOTAL-with-Quests row (~280K). State the chars/token basis explicitly so the 337-04 manifest-sum lint can re-derive.

`## Chunk Groups`: define the 3 groups exactly — RNG-CORE (DegenerusGameStorage + Advance + Mint + Lootbox + Quests; ~114K; label it "the irreducible core — never split it"), CONSUME-B (Jackpot + Degenerette + Decimator + Whale + Boon + GameOver + MintStreakUtils + PayoutUtils; ~73K), FACADE+PERIPHERAL-C (DegenerusGame facade + AfKing + BurnieCoin + BurnieCoinflip + DegenerusJackpots + GNRUS; ~93K). For each group list its files + the token sum.

`## The Storage-Travels Rule`: state that DegenerusGameStorage.sol (~24K) MUST re-attach to EVERY chunked group because the delegatecall architecture puts a slot's writer and reader in different files; every freeze claim resolves against the slot layout + the 3 write-gates. Note the effective-token cost of 3-group chunking (~257K + 2×24K ≈ 305K) so the operator understands the repetition.

Keep it a NEUTRAL operations manual: file paths + sizes + grouping rationale. No freeze verdicts, no FINDINGS/CATALOG references.
  </action>
  <verify>
    <automated>test -f audit/rng-audit-kit/CHUNK-MANIFEST.md && grep -q 'RNG-CORE' audit/rng-audit-kit/CHUNK-MANIFEST.md && grep -q 'CONSUME-B' audit/rng-audit-kit/CHUNK-MANIFEST.md && grep -qE 'PERIPHERAL-C' audit/rng-audit-kit/CHUNK-MANIFEST.md && grep -qi 'DegenerusQuests' audit/rng-audit-kit/CHUNK-MANIFEST.md && [ "$(grep -c "$(wc -l < contracts/DegenerusGame.sol | tr -d ' ')" audit/rng-audit-kit/CHUNK-MANIFEST.md)" -ge 1 ] && ! grep -riE 'FINDINGS-v[0-9]|RNGLOCK-CATALOG' audit/rng-audit-kit/CHUNK-MANIFEST.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f audit/rng-audit-kit/CHUNK-MANIFEST.md` exits 0
    - all 3 groups named: `grep -c 'RNG-CORE' ...` >= 1, `grep -c 'CONSUME-B' ...` >= 1, `grep -cE 'PERIPHERAL-C' ...` >= 1
    - Quests included: `grep -ci 'DegenerusQuests' audit/rng-audit-kit/CHUNK-MANIFEST.md` returns >= 1
    - the facade's live line count is present (sizes match HEAD): `grep -c "$(wc -l < contracts/DegenerusGame.sol | tr -d ' ')" audit/rng-audit-kit/CHUNK-MANIFEST.md` returns >= 1
    - the Storage-travels rule is stated: `grep -ci 'travel' audit/rng-audit-kit/CHUNK-MANIFEST.md` returns >= 1
    - self-containment: `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/CHUNK-MANIFEST.md` returns 0
  </acceptance_criteria>
  <done>CHUNK-MANIFEST.md exists with the corpus inventory (live HEAD sizes), the 3 chunk groups, and the Storage-travels rule; neutral, self-contained.</done>
</task>

<task type="auto">
  <name>Task 2: Append the model-agnostic feeding recipe + the PACKAGE-ONLY statement to RNG-AUDIT-KIT.md</name>
  <files>audit/rng-audit-kit/RNG-AUDIT-KIT.md</files>
  <read_first>
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md (the protocol + context pack from 337-01/02 — the recipe sits at the end)
    - audit/rng-audit-kit/CHUNK-MANIFEST.md (the 3 groups the ChatGPT-web path references)
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§6 the per-model recipe + token-math conclusion; §2 the post-v50 surface the kit reflects)
    - .planning/REQUIREMENTS.md (RNGAUDIT-04 verbatim — model-agnostic + chunking guidance + explicitly PACKAGE-ONLY)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md (§5.2 package-only + §5.3 model-agnostic framing)
  </read_first>
  <action>
Append two sections to `audit/rng-audit-kit/RNG-AUDIT-KIT.md` (after the context pack).

`## Feeding the Contracts (Model-Agnostic Recipe)`: a per-model recipe sourced from RESEARCH §6 — (a) Gemini 2.5 Pro (recommended primary one-feed): attach the whole ~280K corpus in one session, run R1→R4 as 4 turns in that session so the model retains its R1 catalog; no chunking. (b) GPT-5.5 / GPT-4.1 (API or Pro file upload): whole corpus fits; prefer file upload over inline paste. (c) ChatGPT web UI (chunked path): feed the 3 groups in order RNG-CORE → CONSUME-B → FACADE+PERIPHERAL-C (per CHUNK-MANIFEST.md), re-attaching DegenerusGameStorage.sol with each group; do R1 only after all 3 groups load, or run R1 per-group and merge catalogs in a reconciliation turn before R2. (d) general: instruct the operator to have the model persist its R1 catalog and paste it back at the top of each later round. State that the kit is model-agnostic — usable in both Gemini and ChatGPT — and that the contracts are referenced BY PATH (the operator attaches the files listed in CHUNK-MANIFEST.md), not inlined. Reference the manifest by name for the file list + sizes. Note the kit reflects the FROZEN post-v50 tree (the O(1) whale-pass claim, the MintModule alignment, the AfKing pass-gating) — without stating any freeze conclusion about them.

`## Scope — PACKAGE-ONLY`: state explicitly and unmissably that this is a PACKAGE-ONLY deliverable — authoring the kit is the deliverable; RUNNING it through Gemini/ChatGPT and TRIAGING the external model's output is a FUTURE cycle, explicitly OUT of v50.0. Use the literal phrase "PACKAGE-ONLY" and the literal phrase "future cycle".

ANSWER-KEY DISCIPLINE: the recipe and scope sections must NOT leak any freeze verdict or reference audit/FINDINGS-*.md or RNGLOCK-CATALOG.md.
  </action>
  <verify>
    <automated>grep -qi 'Gemini' audit/rng-audit-kit/RNG-AUDIT-KIT.md && grep -qi 'ChatGPT' audit/rng-audit-kit/RNG-AUDIT-KIT.md && grep -q 'RNG-CORE' audit/rng-audit-kit/RNG-AUDIT-KIT.md && grep -qi 'PACKAGE-ONLY' audit/rng-audit-kit/RNG-AUDIT-KIT.md && grep -qi 'future cycle' audit/rng-audit-kit/RNG-AUDIT-KIT.md && ! grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/RNG-AUDIT-KIT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - both models named: `grep -ci 'Gemini' ...` >= 1 AND `grep -ci 'ChatGPT' ...` >= 1
    - the chunked path references the manifest groups: `grep -c 'RNG-CORE' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1
    - the literal PACKAGE-ONLY framing present: `grep -ci 'PACKAGE-ONLY' audit/rng-audit-kit/RNG-AUDIT-KIT.md` >= 1 AND `grep -ci 'future cycle' audit/rng-audit-kit/RNG-AUDIT-KIT.md` >= 1
    - no-answer-key + self-containment still clean across the whole file: `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG|safe by construction|no (writer )?escape|the invariant holds' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0
  </acceptance_criteria>
  <done>RNG-AUDIT-KIT.md ends with the model-agnostic feeding recipe (Gemini one-feed / GPT one-feed / ChatGPT-web 3-group chunked, Storage re-attached) referencing CHUNK-MANIFEST.md, and an explicit PACKAGE-ONLY / future-cycle scope statement; lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale corpus sizes → operator misfeed | If manifest line/char counts drift from HEAD, the operator may mis-chunk and break the cross-module read-graph. |
| feeding recipe prose → freeze verdict | The recipe must reflect the post-v50 surface (whale O(1), MintModule alignment, pass-gating) WITHOUT concluding any of them is freeze-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-07 | Tampering (stale sizes) | CHUNK-MANIFEST.md inventory | mitigate | Sizes re-run via wc at HEAD (Task 1); the 337-04 manifest-sum lint re-derives counts against the working tree; the facade live-line-count grep is a Task-1 acceptance check. |
| T-337-08 | Information Disclosure | feeding recipe + scope prose | mitigate | No-answer-key + self-containment lints (Task 2 acceptance, whole file) return 0; the recipe names the post-v50 surface as files to feed, never as proven-safe. |
| T-337-09 | Repudiation (scope creep into running the kit) | PACKAGE-ONLY statement | mitigate | The literal "PACKAGE-ONLY" + "future cycle" phrasing is a Task-2 acceptance grep; RNGAUDIT-04 + REQUIREMENTS Out-of-Scope bound it. |
| T-337-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — documentation-only; no Package Legitimacy Audit applies. |
</threat_model>

<verification>
- `test -f audit/rng-audit-kit/CHUNK-MANIFEST.md`; 3 groups named; Quests included; facade live line count present.
- RNG-AUDIT-KIT.md names Gemini + ChatGPT, references RNG-CORE, and states PACKAGE-ONLY + future cycle.
- Self-containment + no-answer-key lints clean across both files.
</verification>

<success_criteria>
- RNGAUDIT-04: corpus inventory with live HEAD sizes + 3 chunk groups + Storage-travels rule (CHUNK-MANIFEST.md); model-agnostic feeding recipe + explicit PACKAGE-ONLY statement (RNG-AUDIT-KIT.md).
- Layout B complete: paste-into-model artifact + operator manual, both lint-clean.
</success_criteria>

<output>
Create `.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-03-SUMMARY.md` when done.
</output>
