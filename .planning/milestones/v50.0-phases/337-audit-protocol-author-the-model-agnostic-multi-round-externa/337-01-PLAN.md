---
phase: 337-audit-protocol-author-the-model-agnostic-multi-round-externa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md
  - audit/rng-audit-kit/RNG-AUDIT-KIT.md
autonomous: true
requirements: [RNGAUDIT-03]
must_haves:
  truths:
    - "Every contracts/ file:line the kit will cite is grep/sed-resolved against current HEAD (not the pre-v50 b0511ca2 sketch lines) and recorded in a fresh attestation table"
    - "The cold-start context pack ships NEUTRAL LOCATORS only (slot decl, writers, readers, file:line, where the VRF word enters + is consumed) with ZERO freeze verdicts"
    - "The kit's context pack is self-contained — it contains no reference to audit/FINDINGS-*.md or .planning/RNGLOCK-CATALOG.md"
    - "The two-file Layout B scaffold exists under the new audit/rng-audit-kit/ directory, physically separate from audit/FINDINGS-*.md"
  artifacts:
    - path: "audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md"
      provides: "Fresh HEAD-resolved anchor table for every file:line the kit cites (re-attested, not copied from the 334 pre-v50 sketch)"
      min_lines: 40
    - path: "audit/rng-audit-kit/RNG-AUDIT-KIT.md"
      provides: "The cold-start context pack sections 4a-4e (module/RNG-window map, rngLock mechanics, VRF entry/consume, contract inventory, cross-module tracing methodology) — locators only, no verdicts"
      min_lines: 120
  key_links:
    - from: "audit/rng-audit-kit/RNG-AUDIT-KIT.md context pack"
      to: "contracts/ source at HEAD"
      via: "every cited file:line resolves at HEAD"
      pattern: "contracts/.*:[0-9]+"
    - from: "audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md"
      to: "RESEARCH §3 re-attested anchor set"
      via: "drifted anchors cited at NEW lines (MintModule:720 not :716; LootboxModule:1253 not the 1250-1260 loop)"
      pattern: "DegenerusGameMintModule.sol:720"
---

<objective>
Lay the FACTUAL FOUNDATION of the external RNG-audit kit: (1) re-attest every `contracts/` anchor the kit will cite against current HEAD into a fresh attestation table (the 334 sketch + 334-GREP-ATTESTATION cite the *pre-v50* `b0511ca2` lines; several anchors MOVED in the 3 touched code files), and (2) author the self-contained cold-start CONTEXT PACK (RNGAUDIT-03 — the sketch's sections 4a-4e) into `RNG-AUDIT-KIT.md`, shipping NEUTRAL LOCATORS only and WITHHOLDING all freeze verdicts.

This plan is foundational: the freeze-invariant target (337-02), the R1→R4 protocol (337-02), and the chunk manifest / packaging (337-03) all reference the inventory and locators authored here.

Purpose: a model with NO prior knowledge of the codebase must be able to build its OWN R1 read-graph catalog from this context pack alone — without our answer key and without our FINDINGS reports.
Output: `audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` (fresh HEAD anchor table) + the context-pack sections (4a-4e) of `audit/rng-audit-kit/RNG-AUDIT-KIT.md`.

PROPOSED DEFAULTS FLAGGED FOR USER REVIEW AT THE PLAN-CHECKER GATE (no CONTEXT.md exists; the user asked us to propose): (a) two-file Layout B per RESEARCH §7 — `RNG-AUDIT-KIT.md` (protocol + context pack, the paste-into-model artifact) + `CHUNK-MANIFEST.md` (the human-ops chunking/feeding manual, authored in 337-03); (b) repo location `audit/rng-audit-kit/` (deliberately separate from `audit/FINDINGS-*.md` for SC3 self-containment); (c) contracts referenced BY PATH + a chunk manifest, NOT inlined (925KB Solidity is too large to inline). These are defaults to confirm, not locked decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md
@audit/STORAGE-WRITE-MAP.md
@audit/C4A-CONTEST-README.md

<interfaces>
<!-- Re-attested HEAD anchors (grep/sed-confirmed by the planner at HEAD 03697dc4; git diff e756a6f3 HEAD -- contracts/ is EMPTY = subject frozen). -->
<!-- These are NEUTRAL LOCATORS the context pack ships. The "internal freeze status" RESEARCH §3 column is the ANSWER KEY — DO NOT ship it. -->

Lock lifecycle (DegenerusGameStorage.sol + DegenerusGameAdvanceModule.sol — UNCHANGED by v50, byte-identical to b0511ca2):
- rngLockedFlag decl: DegenerusGameStorage.sol:279 ; bit-doc :55
- lock set-true: DegenerusGameAdvanceModule.sol:1640 ; _unlockRng def :1719 ; set-false :1721
- write-time gate `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();`: Storage:573 (_queueTickets) / :605 (_queueTicketsScaled) / :661 (_queueTicketRange)
- _livenessTriggered def Storage:1213 ; gate call-sites :571/:602/:655 ; _VRF_GRACE_PERIOD=14 days :198

VRF entry + gate + consume driver (AdvanceModule — UNCHANGED; the 4 EXEMPT entries):
- advanceGame() (consume driver) :154 ; rawFulfillRandomWords (VRF callback) :1735 ; retryLootboxRng() failsafe :1105 ; rngGate(...) :1152

VRF-word consume sites (where rngWord flows):
- _processFutureTicketBatch Advance:1418 (called :398) ; payDailyJackpot Advance:888 + JackpotModule:320 ; _distributeYieldSurplus Advance:675 (:407) ; quests.rollLevelQuest Advance:426 / DegenerusQuests.sol:1781 ; _emitDailyWinningTraits Advance:955 ; _gameOverEntropy Advance:1241
- lootbox path lootboxRngWordByIndex decl Storage:1401 ; readers LootboxModule:510/587/616, MintModule:696 ; gate MintModule:1414

v50-DRIFTED anchors (cite the NEW line, NOT the 334 sketch line):
- MintModule MINTDIV alignment: processTicketBatch within-player advance now `processed += take;` at MintModule:720 (was `writesUsed>>1` at :716 pre-v50); reference loop processFutureTicketBatch `processed += take;` at :502
- Whale box-open O(1) record: `whalePassClaims[player] += 1;` at LootboxModule:1253 (was the inline 100-iter _queueTickets loop :1250-1260)
- claimWhalePass materializer: WhaleModule:1018 ; `_queueTicketRange(player, startLevel, 100, uint32(halfPasses), false)` :1034 ; reads whalePassClaims :1020
- whalePassClaims decl Storage:955 ; writers LootboxModule:1253 + PayoutUtils:52 + JackpotModule:1410 ; readers WhaleModule:1020/1024, getter Game:2645
- lazyPassHorizon view Game:1540 (NEW in v50, non-VRF) ; autoOpen Game:1695 ; OPEN_NORMAL_GAS_UNIT carve-out DELETED
- _applyWhalePassStats def Storage:1111 ; call sites now 2 (WhaleModule:1032 + DecimatorModule:588 — the LootboxModule box-open caller is GONE post-v50)

11 game modules at contracts/modules/: Advance, Boon, Decimator, Degenerette, GameOver, Jackpot, Lootbox, Mint, MintStreakUtils, PayoutUtils, Whale. + storage/DegenerusGameStorage.sol + facade DegenerusGame.sol + peripherals AfKing/BurnieCoin/BurnieCoinflip/DegenerusJackpots/GNRUS (+ DegenerusQuests as a named consume site).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-attest every kit anchor against HEAD into 337-ANCHOR-ATTESTATION.md</name>
  <files>audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§1 "Post-v50 Anchor Re-Attestation" tables A-H + the Drift summary — the raw re-attested set)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md (§2 exempt set + §4b/4c anchors — the PRE-v50 lines that must be re-confirmed, several drifted)
    - contracts/storage/DegenerusGameStorage.sol (lines 55, 198, 279, 573, 605, 661, 955, 1111, 1213, 1401 — the UNCHANGED lock/gate/slot anchors)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 154, 1105, 1152, 1640, 1719, 1721, 1735 — the UNCHANGED VRF-entry/lifecycle anchors)
    - contracts/modules/DegenerusGameMintModule.sol (lines 502, 671, 696, 720, 1414 — the MINTDIV drift)
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 510, 587, 616, 1253 — the whale O(1) drift)
    - contracts/modules/DegenerusGameWhaleModule.sol (lines 1018, 1020, 1032, 1034 — the claim materializer)
    - contracts/DegenerusGame.sol (lines 1540, 1695, 2645 — the lazyPassHorizon/autoOpen drift)
  </read_first>
  <action>
Create `audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` — a fresh, HEAD-resolved attestation table for EVERY `contracts/` anchor the kit will cite, so no pre-v50 line leaks into the deliverable. State at the top: the frozen-subject SHA fact (`git diff e756a6f3 HEAD -- contracts/` is empty = subject byte-frozen) and the HEAD short SHA at authoring time. Author one table per RESEARCH §3 grouping with these columns: Anchor (symbol/role) · Confirmed `file:line` at HEAD · Status (unchanged / DRIFTED-from-sketch). Required groupings and rows: (A) lock lifecycle + write-gate [Storage:279/55/573/605/661/1213/571/602/655/198, Advance:1640/1719/1721]; (B) VRF entry + gate + consume driver [the 4 EXEMPT: Advance:154/1735/1105/1152]; (C) VRF-word consume sites [Advance:1418/888/675/426/955/1241, JackpotModule:320, DegenerusQuests:1781, lootbox path Storage:1401 → LootboxModule:510/587/616 + MintModule:696, gate MintModule:1414]; (D) MINTDIV drift [MintModule:720 `processed += take` vs reference :502; def :671]; (E) whale O(1) drift [LootboxModule:1253 `whalePassClaims[player] += 1`; claimWhalePass WhaleModule:1018/:1034; whalePassClaims decl Storage:955 + writers/readers; _applyWhalePassStats Storage:1111 + the now-2 call sites]; (F) lazyPassHorizon/autoOpen drift [Game:1540 NEW, Game:1695, OPEN_NORMAL_GAS_UNIT DELETED]. Mark each of the 6 RESEARCH §3 "Drift summary" items explicitly as DRIFTED so the executor of later plans cannot copy a stale line. Re-run a `grep -n`/`sed -n 'Np'` for each cited line and record the confirmed symbol next to it (this table IS the source of truth the 337-04 anchor-resolution lint checks against). Do NOT add any freeze-status column — locators only.
  </action>
  <verify>
    <automated>test -f audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md && grep -q 'DegenerusGameMintModule.sol:720' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md && grep -q 'whalePassClaims\[player\] += 1' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md && ! grep -qE ':716|1250-1260|OPEN_NORMAL_GAS_UNIT = 90_000' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` exits 0
    - `grep -c 'DegenerusGameMintModule.sol:720' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` returns >= 1 (the drifted MINTDIV anchor cited at the NEW line)
    - `grep -c 'whalePassClaims\[player\] += 1' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` returns >= 1 (the O(1) write cited, not the loop)
    - `grep -cE ':716|1250-1260' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` returns 0 (no stale pre-v50 lines)
    - all 4 exempt-entry anchors present: `grep -cE 'advanceGame|rawFulfillRandomWords|retryLootboxRng|rngGate' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` returns >= 4
    - for every `contracts/...:NNN` token in the file, `sed -n 'NNNp' <file>` at HEAD contains the symbol named beside it (spot-checkable; the 337-04 lint enforces the full set)
  </acceptance_criteria>
  <done>337-ANCHOR-ATTESTATION.md exists with HEAD-resolved tables A-F, every drifted anchor cited at its NEW line, zero stale pre-v50 lines, no freeze-status column.</done>
</task>

<task type="auto">
  <name>Task 2: Author the cold-start context pack (sections 4a-4e) into RNG-AUDIT-KIT.md — locators only, no verdicts</name>
  <files>audit/rng-audit-kit/RNG-AUDIT-KIT.md</files>
  <read_first>
    - audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md (the table authored in Task 1 — the locator source of truth)
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§3 first 4 columns = shippable locators; §4 the SHIP/WITHHOLD boundary rule — DECISIVE; §2 the post-v50 read-graph deltas the catalog must reflect; §7 Layout B; the Reconciliation-with-RNGLOCK-CATALOG note = do NOT ship or reference it)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md (§4 the 4a-4e skeleton headings to fill — STRUCTURE IS LOCKED; §5 the no-answer-key / model-agnostic / package-only framing)
    - audit/STORAGE-WRITE-MAP.md (the writer/reader table house style to mirror — neutral, no verdicts)
    - audit/C4A-CONTEST-README.md (§"Key Contracts" — the contract-inventory style for 4d; note its "10 modules" framing is STALE, use the actual 11)
  </read_first>
  <action>
Create `audit/rng-audit-kit/RNG-AUDIT-KIT.md` and author ONLY the cold-start CONTEXT PACK (RNGAUDIT-03). Subsequent plan 337-02 prepends the freeze-invariant target + exempt set + R1→R4 to the SAME file; do NOT author those here, and leave a clearly-marked placeholder comment at the top reserving that space so 337-02 can fill it without races. Author these five sections with these EXACT headings (filling the 334 sketch §4 skeleton against the frozen post-v50 tree, all anchors sourced from the Task-1 table):

`## Context Pack 4a — Module / RNG-Window Map`: a table of the storage slots/variables that participate in (or gate) VRF-derived output, the owning module, and the daily-RNG-window relevance — as NEUTRAL LOCATORS (slot decl file:line, one-line role). Include: rngLockedFlag (Storage:279), rngWordByDay/rngWordCurrent (the per-day VRF word), lootboxRngWordByIndex (Storage:1401), the far-future ticket queue + the 3 write-gates (Storage:573/605/661), whalePassClaims (Storage:955 — the v50 NEW third writer at LootboxModule:1253), level (Storage:245), _livenessTriggered inputs (Storage:1213, _VRF_GRACE_PERIOD:198). NO "frozen / non-participating" column.

`## Context Pack 4b — rngLock Mechanics`: declaration + bit-doc (Storage:279/:55); set-true at lock (Advance:1640); cleared in _unlockRng (Advance:1719/:1721); the write-time gate verbatim `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();` present at Storage:573/:605/:661; the terminal-freeze gate _livenessTriggered (Storage:1213, call-sites :571/:602/:655, grace :198); the BurnieCoin.rngLocked() mirror (BurnieCoin:455/:470) noted as a coinflip-timing gate locator. Describe mechanics as facts; do NOT conclude any slot's freeze status.

`## Context Pack 4c — Where the VRF Word Enters and Is Consumed`: ENTRY = rawFulfillRandomWords (Advance:1735 / facade DegenerusGame:2226); failsafe retryLootboxRng (Advance:1105 / facade :2177). GATE = rngGate (Advance:1152) returns the word; consume driver advanceGame (Advance:154). CONSUME sites as LOCATIONS only: _processFutureTicketBatch, payDailyJackpot, _distributeYieldSurplus, quests.rollLevelQuest, _emitDailyWinningTraits, _gameOverEntropy, and the lootbox path via lootboxRngWordByIndex (consumed at MintModule:696 + LootboxModule:510/587/616). Cite each at its Task-1 line.

`## Context Pack 4d — Contract Inventory`: the 11 game modules under contracts/modules/ (Advance, Boon, Decimator, Degenerette, GameOver, Jackpot, Lootbox, Mint, MintStreakUtils, PayoutUtils, Whale) + storage/DegenerusGameStorage.sol + the DegenerusGame.sol facade + peripherals (AfKing, BurnieCoin, BurnieCoinflip, DegenerusJackpots, GNRUS) + DegenerusQuests (named consume site). One-line role per file, NEUTRAL. Note the delegatecall-facade fact (writers and readers of one slot live in different module files) as structural orientation. State the actual count is 11 modules (the legacy "10 modules" framing in older audit docs is stale).

`## Context Pack 4e — Cross-Module Variable-Tracing Methodology`: the back-and-forth method verbatim-in-spirit from the sketch §4e: "trace every participating variable across every module — what writes it, what reads it, what is locked during an RNG window. A slot is only frozen if EVERY writer either cannot fire during the lock or reverts; a single escaping writer breaks the invariant." Frame this as HOW TO DERIVE (a conditional method), NOT as a verdict about any specific slot.

ANSWER-KEY DISCIPLINE (RESEARCH §4, decisive): ship LOCATORS + METHODOLOGY + the structural exempt-entry framing; WITHHOLD every freeze verdict. Do NOT write "slot X is frozen", "reverts-if-written-during-lock" as a per-slot conclusion, "proven-non-participating", "safe by construction", "no escape", "the invariant holds", "we verified/found/confirmed", the WHALE-04 future-level-so-safe reasoning, or any MINTDIV safety conclusion. Do NOT reference `audit/FINDINGS-*.md`, `.planning/RNGLOCK-CATALOG.md`, or prior-milestone verdicts anywhere. When in doubt, if removing a sentence forces the external model to do more independent work, cut it.
  </action>
  <verify>
    <automated>test -f audit/rng-audit-kit/RNG-AUDIT-KIT.md && [ "$(grep -cE '^## Context Pack 4[a-e]' audit/rng-audit-kit/RNG-AUDIT-KIT.md)" -eq 5 ] && ! grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/RNG-AUDIT-KIT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## Context Pack 4[a-e]' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns exactly 5 (all of 4a/4b/4c/4d/4e present)
    - self-containment: `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0 matches
    - no-answer-key (verdict phrasings absent from the context pack; the methodology conditional "frozen if EVERY writer" is allowed): `grep -riE 'is frozen because|proven[- ]non-participating|no (writer )?escape|safe by construction|the invariant holds|we (found|verified|confirmed)' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0 matches
    - the write-gate verbatim string is present: `grep -cF 'if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1
    - contract inventory uses the actual 11-module count: `grep -c 'MintStreakUtils' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1
    - the drifted post-v50 surface is cataloged, not the pre-v50: `grep -cE ':716|inline 100-iter|1250-1260' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0
  </acceptance_criteria>
  <done>RNG-AUDIT-KIT.md exists with all 5 context-pack sections (4a-4e) as neutral locators against HEAD, the verbatim write-gate string present, self-containment + no-answer-key lints clean, and a reserved placeholder for the 337-02 protocol sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal audit knowledge → shipped kit | The authoring agent KNOWS the freeze verdicts (WHALE-04 proof, MINTDIV safety, "no escape"). This knowledge must NOT cross into the deliverable — the kit ships locators, withholds conclusions. |
| post-v50 frozen tree → cited anchors | Every file:line must resolve at HEAD; a stale pre-v50 line (the 334 sketch baseline `b0511ca2`) misdirects the future external auditor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-01 | Information Disclosure | Context pack 4a-4e (RNG-AUDIT-KIT.md) | mitigate | No-answer-key lint (Task 2 acceptance): grep for freeze-verdict phrasings returns 0; the SHIP/WITHHOLD rule (RESEARCH §4) governs every sentence — locators ship, verdicts withheld. |
| T-337-02 | Information Disclosure | Self-containment (SC3) | mitigate | Self-containment lint (Task 2 acceptance): `grep -riE 'FINDINGS-v[0-9]\|audit/FINDINGS\|RNGLOCK-CATALOG'` returns 0; the kit lives under a NEW `audit/rng-audit-kit/` dir, physically separate from FINDINGS. |
| T-337-03 | Tampering (stale-anchor misdirection) | 337-ANCHOR-ATTESTATION.md + cited anchors | mitigate | Fresh HEAD re-attestation (Task 1); the 6 drifted anchors marked explicitly; `grep` for `:716`/`1250-1260` returns 0; the 337-04 anchor-resolution lint enforces the full set against HEAD. |
| T-337-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — documentation-only deliverable, no Standard Stack, no Package Legitimacy Audit applies (RESEARCH §"Standard Stack" = Not applicable). |
</threat_model>

<verification>
- Both authored files exist under `audit/rng-audit-kit/`.
- `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/` returns 0 matches (self-containment, both files).
- `grep -cE ':716|1250-1260' audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` returns 0 (no stale anchors).
- All 5 context-pack headings present; all 4 exempt-entry anchors present in the attestation table.
</verification>

<success_criteria>
- 337-ANCHOR-ATTESTATION.md: fresh HEAD-resolved table; 6 drift items flagged; zero stale lines; no freeze-status column.
- RNG-AUDIT-KIT.md context pack (4a-4e): neutral locators only; self-contained; no-answer-key lint clean; 11-module inventory; verbatim write-gate string present.
- Layout B scaffold under `audit/rng-audit-kit/` established for 337-02/03 to build on.
</success_criteria>

<output>
Create `.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-01-SUMMARY.md` when done.
</output>
