---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 06
type: execute
wave: 4
depends_on: [335-05]
files_modified:
  - contracts/DegenerusGame.sol
  - test/gas/RouterWorstCaseGas.t.sol
autonomous: true
worktree: false
requirements: [WHALE-03, BATCH-02]
tags: [verification, forge-build, forge-test, gas-measurement, open-batch, d-impl-04]

must_haves:
  truths:
    - "`forge build` exits 0 against the post-Plans-335-01..05 working tree."
    - "`forge test` runs cleanly: any NEW red vs the v49.0 baseline `666/42/17`-by-NAME is either (a) reconciled inside Plan 335-05's migration and now green OR (b) a genuine v50 regression that triggers a STOP-and-re-spec signal."
    - "The `KeeperOpenBoxWorstCaseGas` harness emits a measurable per-box gas figure under worst-case uniform-O(1) opens (whale-pass holder vs. non-whale-pass opener — uniform per D-02/D-04)."
    - "A flat `OPEN_BATCH` value is picked from the measurement: `OPEN_BATCH = floor((16_700_000 - HEADROOM) / measured_per_box_gas)` with `HEADROOM ≥ measured_per_box_gas` (≥ 1 box worth)."
    - "The attestation `OPEN_BATCH × measured_per_box ≤ 16_700_000 - HEADROOM` is satisfied; if measurement reveals a ceiling overshoot under any reasonable `OPEN_BATCH`, STOP-and-re-spec (D-IMPL-04 hard floor)."
    - "`335-LOCAL-VERIFICATION.md` is authored in the phase directory recording: measured per-box gas, picked OPEN_BATCH, the chosen × measured product, the build/test pass-or-baseline ledger."
  artifacts:
    - path: ".planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md"
      provides: "Local verification ledger — measured per-box gas, picked OPEN_BATCH, forge build/test pass-or-baseline reconciliation."
    - path: "contracts/DegenerusGame.sol"
      provides: "Final flat `OPEN_BATCH` constant value (if it lives in this file as a constant — or in `AfKing.sol` if that's where it's consumed; verify before the picker writes)."
    - path: "test/gas/RouterWorstCaseGas.t.sol"
      provides: "Final `OPEN_BATCH` value in the test mirror (replacing the placeholder Plan 335-05 Task 7 installed)."
  key_links:
    - from: "Plan 335-06 measurement"
      to: "contracts/DegenerusGame.sol::OPEN_BATCH (or AfKing.sol — verify which file owns the constant)"
      via: "the picked value derived from `KeeperOpenBoxWorstCaseGas` re-run"
      pattern: "OPEN_BATCH = [0-9]+"
    - from: "Plan 335-06 forge test ledger"
      to: "Plan 335-07 USER HAND-REVIEW gate"
      via: "the green-or-known-baseline suite is the integration oracle the USER reviews"
      pattern: "forge test"
---

<objective>
Local verification of the full Plans 335-01..05 diff:

1. **`forge build`** — MUST be green. Any compile error is a STOP signal (likely caused by a missed symbol migration in Plan 335-05 or a contract-side mistake in Plans 335-01..04).

2. **`forge test` (full suite)** — run, capture the pass/fail ledger, and reconcile against the v49.0 baseline `666 passed / 42 failed / 17 skipped`-by-NAME (per 332 verification). Any NEW red (a test that was passing or skipped in v49 baseline and is now failing) MUST be reconciled INSIDE this plan, NOT deferred — per D-IMPL-03. The fixture-migration in Plan 335-05 IS the reconciliation vehicle: if a new red is a fixture-migration artifact (test asserting a removed surface), it belongs in the migration; if it's a genuine v50 regression, STOP and re-spec.

3. **`KeeperOpenBoxWorstCaseGas` re-run** — measure per-box gas under uniform-O(1) opens (whale-pass holder vs. non-whale-pass opener — assert the figures are within a tight band — per D-02/D-04 the uniformity is the load-bearing WHALE-01 claim). Record the measured per-box gas (expected ~89K, but USE the measured value).

4. **Pick flat `OPEN_BATCH`** — `OPEN_BATCH = floor((16_700_000 - HEADROOM) / measured_per_box_gas)` with `HEADROOM ≥ measured_per_box_gas` (≥ 1 box worth — to absorb worst-case-vs-typical drift). Write the picked value into the contract constant (verify which file owns it post-Plans-335-01..04 — likely a new const in `DegenerusGame.sol` if the autoOpen path uses it, OR `AfKing.sol` if the keeper uses it; the constant did not exist pre-edit because the gas-weighted path used `OPEN_NORMAL_GAS_UNIT`). Also write the picked value into the test mirror at `test/gas/RouterWorstCaseGas.t.sol` (replacing the placeholder Plan 335-05 Task 7 installed).

5. **Author `335-LOCAL-VERIFICATION.md`** in the phase directory recording all of the above plus the binding attestation `chosen × measured ≤ 16.7M − headroom`.

This delivers:
- **WHALE-03 (re-confirmation piece)** — the flat OPEN_BATCH is empirically attested under uniform-O(1) opens at the worst case (D-IMPL-04).
- **BATCH-02 (verification piece)** — the diff is locally compiled + tested green-or-known-baseline at the contract-commit boundary; the next plan (335-07) HOLDS for USER review.

Purpose: this is the local-verification chokepoint of the phase. Wave 4 because it depends on Plans 335-01..05 having landed. The output (a measurable per-box gas + a picked OPEN_BATCH + a forge ledger) IS what the USER reviews at Plan 335-07.

CRITICAL Pitfall: D-IMPL-04 STOP-and-re-spec floor — if measurement reveals that the only OPEN_BATCH satisfying the ceiling is LOWER than 331's old value (~50-100) OR if uniform-O(1) cannot be empirically established (whale vs non-whale opens diverge materially), STOP. Do NOT silently lower OPEN_BATCH below what 331 considered usable — that would be a freeze-floor-class signal that WHALE-01's uniform-O(1) assumption broke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-02-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-04-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-05-PLAN.md

<interfaces>
<!-- The verification chokepoint. The harness measures; the picker derives; the ledger attests. -->

`test/gas/KeeperOpenBoxWorstCaseGas.t.sol` — pre-existing harness (UNTOUCHED in Plan 335-05). Already measures the right thing: per-box gas under worst-case open. After Plans 335-01..05 land, re-running it produces the new uniform-O(1) figure.

The 331-rescope value (for context, NOT for re-use):
- 331 picked `OPEN_BATCH = 100` against the gas-weighted path with whale-pass clusters. The 16.7M ceiling: `OPEN_BATCH=100 × ~89K-typical/~167K-cluster` was the binding limit.
- Under WHALE-03 / D-02 / D-04, all opens are uniform ~89K (no cluster path). The new picker derives from the measured uniform figure.

The 16.7M ceiling is the `BLOCK_GAS_LIMIT` − safety margin used by `AfKing`'s autoOpen flow (33M block / 2 for keeper-safety, per 331-rescope). The headroom rule from D-IMPL-04: HEADROOM ≥ 1 box worth = measured_per_box_gas.

Constant location (where `OPEN_BATCH` lives post-edit):
- Pre-edit: `test/gas/RouterWorstCaseGas.t.sol:117` mirrors a `OPEN_BATCH = 100` constant. The contract-side ORIGINAL was the gas-weighted path's `OPEN_NORMAL_GAS_UNIT = 90_000` at `DegenerusGame.sol:1561` (deleted by Plan 335-01 Task 3).
- Post-edit: Plan 335-01 Task 3 retired the gas-weighting but did NOT introduce a new `OPEN_BATCH` constant on the contract side (the flat opened-count loop guard uses `maxCount` as the caller-supplied bound). The constant may not need to live on the contract side at all if `autoOpen(uint256 maxCount)` is always called with a caller-chosen `maxCount` (e.g. by AfKing).
- VERIFY: read `contracts/AfKing.sol` to see if AfKing's autoOpen-side caller hard-codes a value (e.g. `IGame(GAME).autoOpen(OPEN_BATCH)` with a local OPEN_BATCH constant in AfKing). If yes, the constant lives in AfKing.sol; the picker writes it there. If no (the caller passes a parameter), the only place the value lives is the test mirror.

The picker writes ONLY to the file(s) where `OPEN_BATCH` is referenced — DO NOT add a constant to a file that doesn't reference it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: forge build — must exit 0</name>
  <files>(no file edits; runs forge build)</files>
  <read_first>
    - The output of `git diff --stat` to confirm the expected files were touched (Plans 335-01..05 outputs).
  </read_first>
  <action>
    Run `forge build` against the post-Plans-335-01..05 working tree.

    - Exit code 0 → green → proceed to Task 2.
    - Non-zero exit code → STOP. Capture the compiler error output. The most likely causes:
      1. Plan 335-05 Task 5/6/7 missed a `paidThroughDay` / `burnForKeeper` / `OPEN_NORMAL_GAS_UNIT` reference (re-grep `test/`).
      2. Plan 335-04 Task 1 missed adding `lazyPassHorizon` (or `level()`) to the `IGame` iface decl in AfKing.sol.
      3. Plan 335-01 Task 2's `lazyPassHorizon` view signature doesn't match what Plan 335-04 references on the iface.
      4. Plan 335-04 Task 4's BurnieCoin error deletion left a dangling reference (e.g. `revert OnlyAfKing();` in a not-yet-deleted helper).

    Fix the compile error directly (edit the offending file), re-run `forge build`, repeat until green. Record the fixed file paths in the SUMMARY.

    If `forge build` cannot reach green after 3 fix attempts, STOP and escalate to USER — a deeper structural issue exists in Plans 335-01..05.

    DO NOT proceed to Task 2 until `forge build` is green.
  </action>
  <verify>
    <automated>forge build 2>&amp;1 | tail -20; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - `forge build` output contains no compile errors (warnings are acceptable).
    - If any fix was applied to reach green, the fix file paths + the one-line diff are recorded in `335-LOCAL-VERIFICATION.md` (authored in Task 5).
  </acceptance_criteria>
  <done>`forge build` is green; the working tree post-Plans-335-01..05 + any green-getting fixes compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: forge test — full suite; reconcile against v49.0 baseline 666/42/17 by NAME</name>
  <files>(no file edits unless a fixture-migration gap is found)</files>
  <read_first>
    - .planning/phases/332-tst-equivalence-freeze-safety-divergence-repro-and-non-widening-/ (or wherever the v49.0 baseline ledger lives — the 332 verification recorded 666 passed / 42 failed / 17 skipped by NAME)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md (D-IMPL-03 — reconciliation policy)
  </read_first>
  <action>
    Run `forge test` against the post-Plans-335-01..05 + Task 1 fixes working tree.

    Capture the FULL pass/fail ledger by NAME (test contract::test function). Compare against the v49.0 baseline `666/42/17`-by-NAME.

    **Reconciliation policy (D-IMPL-03)**:
    - **Baseline red, now red, same name** → CARRIED v49 baseline red. OK. (e.g. v49's enumerated-red set carries forward.)
    - **Baseline red, now green** → an INCIDENTAL fix; record but do not treat as a regression. The v50 baseline ledger that 336 TST-04 produces will reflect this.
    - **Baseline green, now red** → NEW RED. Triage:
      - If the test asserts a removed surface (e.g. asserts `burnForKeeper` is called) AND the migration missed the corresponding fixture rewrite → fix the migration NOW (edit the test file in Plan 335-05's scope), re-run `forge test`. This is the D-IMPL-02 full-alignment vehicle.
      - If the test asserts a NEW v50 contract behavior incorrectly (e.g. asserts pass-eviction event has reason code 99 but Plan 335-04 emits reason code 1) → fix the test assertion to match the actual contract behavior. Document the fix in 335-LOCAL-VERIFICATION.md.
      - If the test asserts a v49-era behavior that v50 LEGITIMATELY changed (e.g. asserts the inline 100-loop fires at box-open) AND the assertion no longer applies → delete or refactor the test (this is a Plan 335-05 scope gap; close it here).
      - If NONE of the above apply (test is correct, contract behavior is genuinely regressed) → **STOP and re-spec**. Capture the test name + the failure mode + the suspected contract surface in 335-LOCAL-VERIFICATION.md. Do NOT proceed to Task 3.
    - **Baseline skipped, now red** → ditto NEW RED triage.
    - **Baseline skipped, now green** → an INCIDENTAL un-skip; record but do not treat as a regression.
    - **Baseline green, now skipped** → suspicious — did the migration accidentally `vm.skip` something? Triage as NEW RED.

    Re-run `forge test` after any fix. Repeat until the green-or-known-baseline ledger holds (no NEW reds, OR all NEW reds reconciled to fixture-migration artifacts now fixed).

    Record the FINAL ledger in 335-LOCAL-VERIFICATION.md (Task 5): `<X> passed / <Y> failed / <Z> skipped`, the per-test diff vs `666/42/17`-by-NAME (incidental fixes, incidental un-skips, fixture migrations closed here, etc.).

    If the iteration loop doesn't converge after 5 fix-and-rerun cycles, STOP and escalate to USER.
  </action>
  <verify>
    <automated>forge test 2>&amp;1 | tail -40; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `forge test` runs to completion (no test-runner-internal failure).
    - The final ledger is `<X> passed / <Y> failed / <Z> skipped` with `Y ≤ 42`-baseline (no new reds) OR `Y > 42` with every new red explicitly attributed to a v49 baseline red carrying forward in 335-LOCAL-VERIFICATION.md.
    - NO test failure is attributed to "fixture migration artifact, deferred to 336" — all such artifacts are closed in THIS plan (D-IMPL-03).
    - If a genuine v50 regression surfaced and was fixed in a contract file: the fix file path + diff is recorded in 335-LOCAL-VERIFICATION.md, with the v49 ↔ v50 behavior diff explained.
  </acceptance_criteria>
  <done>`forge test` runs to completion with a green-or-known-baseline ledger; all NEW reds reconciled (fixture-migration closed OR genuine-regression fixed OR re-spec STOP signal raised); per-test diff vs `666/42/17`-by-NAME recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: KeeperOpenBoxWorstCaseGas re-run — measure uniform-O(1) per-box gas</name>
  <files>(no file edits)</files>
  <read_first>
    - test/gas/KeeperOpenBoxWorstCaseGas.t.sol (full file — confirm the harness measures the right thing post-Plans-335-01..05)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§6 — the WHALE-03 retirement justification + the expected ~89K figure)
  </read_first>
  <action>
    Run `forge test --match-path test/gas/KeeperOpenBoxWorstCaseGas.t.sol -vv` (or the equivalent gas-harness invocation that emits the measured per-box gas figure).

    Capture the output. The harness should emit (or allow inference of) two figures:
    1. Per-box gas for a NON-WHALE-PASS opener (an opener whose `_activateWhalePass` does NOT fire).
    2. Per-box gas for a WHALE-PASS opener (an opener whose `_activateWhalePass` DOES fire — the O(1) `whalePassClaims +=` write per Plan 335-02).

    **Verify the uniform-O(1) claim (D-02 / D-04)**: the two figures MUST be within a tight band (e.g. < 10% difference). The WHALE-PASS opener pays exactly ONE extra `SSTORE` cost over the non-whale-pass opener — that's the `whalePassClaims[player] += 1` write. The pre-edit difference (~5.4M gas vs ~89K) collapses to a single SSTORE delta. Record both figures.

    **Record the chosen `measured_per_box_gas`**: use the HIGHER of the two figures (the whale-pass opener — the conservative ceiling). Expected ~89K-90K. The 335-LOCAL-VERIFICATION.md authored in Task 5 captures this.

    **STOP-and-re-spec triggers** (D-IMPL-04 hard floor):
    - The two figures differ by > 25% → uniform-O(1) assumption broke; STOP.
    - The higher figure is > 167K (the 331-era weighted-cluster ceiling) → the gas-weight retirement was premature; STOP.
    - The harness reverts or produces no measurable figure → instrumentation bug; STOP and fix the harness OR escalate.
  </action>
  <verify>
    <automated>forge test --match-path test/gas/KeeperOpenBoxWorstCaseGas.t.sol -vv 2>&amp;1 | tail -50; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The harness runs to completion (no revert, no instrumentation failure).
    - Two figures captured: `gas_non_whale = <measured>` and `gas_whale = <measured>`.
    - `|gas_whale - gas_non_whale| / max(gas_whale, gas_non_whale) < 0.25` (uniform-O(1) within 25% band).
    - `max(gas_whale, gas_non_whale) ≤ 167_000` (the 331-era weighted ceiling is not exceeded).
    - The chosen `measured_per_box_gas = max(gas_whale, gas_non_whale)` is recorded for Task 4.
  </acceptance_criteria>
  <done>The harness re-runs successfully; per-box gas figures captured for both opener types; uniform-O(1) within tolerance; STOP triggers not hit; the conservative measurement is ready for Task 4's picker.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Pick flat OPEN_BATCH from the measurement; write to contract + test mirror</name>
  <files>contracts/DegenerusGame.sol, test/gas/RouterWorstCaseGas.t.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (the autoOpen call site — does it consume an OPEN_BATCH constant or take maxCount as a parameter?)
    - contracts/AfKing.sol (the autoOpen caller — is OPEN_BATCH defined here as a constant used by the keeper?)
    - test/gas/RouterWorstCaseGas.t.sol (the placeholder Plan 335-05 Task 7 installed; the TODO citing Plan 335-06)
    - Task 3's output (the `measured_per_box_gas` figure)
  </read_first>
  <action>
    Pick the flat `OPEN_BATCH` value:

    `OPEN_BATCH = floor((16_700_000 - HEADROOM) / measured_per_box_gas)` where `HEADROOM = measured_per_box_gas` (≥ 1 box worth, per D-IMPL-04).

    Example for the expected ~89K case: `OPEN_BATCH = floor((16_700_000 - 89_000) / 89_000) = floor(186.6) = 186`. Round DOWN to the nearest 10 or 50 for human-readability if desired (e.g. `OPEN_BATCH = 180` or `OPEN_BATCH = 150`), as long as `OPEN_BATCH × measured_per_box_gas ≤ 16_700_000 - HEADROOM` still holds.

    **Locate the constant home (post-Plan-335-01..04 codebase)**:
    - Read `contracts/AfKing.sol` for any `OPEN_BATCH` constant decl (likely candidate: the keeper sweep's autoOpen call passes `OPEN_BATCH` as `maxCount`).
    - Read `contracts/DegenerusGame.sol` for any `OPEN_BATCH` constant decl.
    - If neither file has one: the value lives ONLY in the test mirror (the contract's autoOpen takes `maxCount` as a parameter; the keeper's call site picks the value). In this case, write the value at the keeper's call site (likely `AfKing.sol`'s `_autoBuy` or `doWork` — whichever calls `autoOpen(...)`).

    **Write the picked value**:
    1. **Contract side**: add or update `uint256 internal constant OPEN_BATCH = <picked>;` at the file that owns it. If the value is inlined at the call site (`afterAutoOpenCallSite.autoOpen(<picked>)`), use the literal there. Either is acceptable; the constant form is preferred for traceability.
    2. **Test mirror**: at `test/gas/RouterWorstCaseGas.t.sol`, replace the Plan 335-05 placeholder `OPEN_BATCH = 100;` with the picked value. Update the TODO comment to a recorded value comment (`// Picked from KeeperOpenBoxWorstCaseGas measurement: <measured>K per box, OPEN_BATCH = <picked>, attestation chosen × measured = <product> ≤ 16.7M − HEADROOM`).

    **Re-run the gas-budget assertion** in `test/gas/RouterWorstCaseGas.t.sol`:
    `forge test --match-path test/gas/RouterWorstCaseGas.t.sol -vv` MUST pass. If it fails (likely cause: the picked value × measured > 16.7M - HEADROOM), reduce OPEN_BATCH by 10 and retry. If reduction below ~100 is needed to fit, STOP-and-re-spec (D-IMPL-04 hard floor — do NOT silently undershoot the 331-era ~100).

    Re-run the FULL `forge test` to confirm no other test is destabilized by the picked value (some tests may bound their iteration to OPEN_BATCH; their oracles should pick up the new value automatically if they reference the constant — verify).
  </action>
  <verify>
    <automated>grep -nE "OPEN_BATCH = [0-9]+" contracts/AfKing.sol contracts/DegenerusGame.sol test/gas/RouterWorstCaseGas.t.sol 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "OPEN_BATCH = [0-9]+" test/gas/RouterWorstCaseGas.t.sol` returns ≥ 1 line; the value is NOT `100` (the placeholder); the value is the picked one.
    - If contract-side constant chosen: `grep -nE "OPEN_BATCH = [0-9]+" contracts/AfKing.sol contracts/DegenerusGame.sol` returns 1 line in one of them, matching the test mirror value.
    - `picked × measured_per_box_gas ≤ 16_700_000 - HEADROOM` (HEADROOM ≥ measured) — arithmetic asserted in 335-LOCAL-VERIFICATION.md and Task 5.
    - `forge test --match-path test/gas/RouterWorstCaseGas.t.sol -vv` passes (the assertion bound with the new constant holds).
    - `forge test` (full suite) re-runs cleanly with no new reds caused by the picked value.
    - The Plan 335-05 TODO is replaced with a recorded value comment (no `TODO` text near the OPEN_BATCH line).
  </acceptance_criteria>
  <done>Flat OPEN_BATCH picked from measurement; constant written to contract (if homed there) + test mirror; attestation arithmetic satisfied; tests still pass; Plan 335-05's TODO closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author 335-LOCAL-VERIFICATION.md — the local-verification ledger</name>
  <files>.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md</files>
  <read_first>
    - The outputs of Tasks 1, 2, 3, 4 (all should be captured during execution; do not re-run).
  </read_first>
  <action>
    Author `335-LOCAL-VERIFICATION.md` in the phase directory. The file is the load-bearing handoff to Plan 335-07's USER hand-review.

    Required sections:

    ## 1. forge build
    - Exit code: 0
    - Any green-getting fixes applied during Task 1 (file paths + one-line diff descriptions).

    ## 2. forge test ledger (vs v49.0 baseline 666/42/17 by NAME)
    - Final counts: `<X> passed / <Y> failed / <Z> skipped`.
    - Per-test diff vs the v49 baseline:
      - Carried baseline reds (still red, same name): list.
      - Incidental fixes (baseline red → now green): list.
      - Incidental un-skips (baseline skipped → now green): list.
      - Fixture-migration artifacts closed in Plan 335-06 (NEW red found in Task 2 → fixed mid-task): list with the fix file paths.
      - Genuine v50 contract fixes (NEW red found in Task 2 → contract bug surfaced → fixed in a contract file): list with the v49 ↔ v50 behavior diff.
    - Statement: "No NEW reds remain at hand-review" OR (if STOP triggered) the STOP signal write-up.

    ## 3. KeeperOpenBoxWorstCaseGas measurement
    - `gas_non_whale = <measured>`
    - `gas_whale = <measured>`
    - Uniform-O(1) tolerance check: `|gas_whale - gas_non_whale| / max < 0.25` ✓
    - Ceiling check: `max ≤ 167_000` ✓
    - `measured_per_box_gas = max(gas_whale, gas_non_whale) = <picked>`

    ## 4. OPEN_BATCH picker
    - Formula: `OPEN_BATCH = floor((16_700_000 - HEADROOM) / measured_per_box_gas)`
    - HEADROOM: `<chosen, ≥ measured_per_box_gas>`
    - Picked OPEN_BATCH: `<value>`
    - Attestation: `<picked> × <measured_per_box> = <product> ≤ 16_700_000 - <HEADROOM>` ✓
    - Constant home (file:line where it's written): contract side (if homed) + test mirror.
    - Rounding rationale (if any): "rounded down to nearest 10 for readability — strict math still holds."

    ## 5. Per-anchor `file:line` re-attestation vs `b0511ca2`
    - Spot-check 5-10 anchors from `334-GREP-ATTESTATION.md` to confirm the post-edit file has the new content at the expected lines (e.g. `_activateWhalePass` body is now O(1) at `LootboxModule.sol:~1240-1250`; `validThroughLevel` is in the Sub struct at `AfKing.sol:~89`; `OPEN_NORMAL_GAS_UNIT` is GONE from `DegenerusGame.sol`).
    - This section is the prep for Plan 335-07's USER review — give the USER a quick-look table of "the SPEC said X at line Y; the post-edit file has X' at line Y'."

    ## 6. The unified diff envelope
    - `git diff <baseline> HEAD -- contracts/ test/ | wc -l` — the line count of the diff.
    - `git diff --stat <baseline> HEAD -- contracts/ test/` — the file-by-file breakdown.
    - The baseline for `<baseline>` is the v49.0 closure HEAD `b0511ca2` per 335-CONTEXT.md.

    ## 7. The "OK to commit?" preview
    - A 5-line summary: "Plans 335-01..05 + 335-06 verifications complete. Forge build green. Forge test green-or-known-baseline. OPEN_BATCH = `<picked>` empirically attested. Ready for USER hand-review at Plan 335-07."
    - State the held BATCH-02 HARD STOP — the diff is APPLIED but NOT committed.

    Constraints:
    - The file MUST be authored under `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/`.
    - The file MUST be committable by the planner/executor (it is documentation, not contract — `feedback_wait_for_approval` does not apply to `.md` files).
    - All numeric figures MUST be the actual measured/picked values from Tasks 1-4, not estimates.
  </action>
  <verify>
    <automated>ls -la .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md; grep -cE "^## [1-7]\." .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md`.
    - All 7 sections present (`grep -cE "^## [1-7]\." returns 7).
    - The `measured_per_box_gas`, `picked OPEN_BATCH`, and `chosen × measured ≤ 16.7M − HEADROOM` arithmetic are all numerically present (not placeholders).
    - The forge build/test ledger is concrete with per-test names (not summarized).
  </acceptance_criteria>
  <done>The local-verification ledger is authored; all numeric figures are concrete measurements/picks; the 5-line "OK to commit?" preview is ready for Plan 335-07's USER hand-review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forge build/test → Plans 335-01..05 outputs | Local development environment; no untrusted input. The integration oracle. |
| KeeperOpenBoxWorstCaseGas measurement → OPEN_BATCH picker | Local measurement; deterministic; same harness as 331-era. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-30 | Tampering (silent OPEN_BATCH undershoot) | Task 4 picker if measurement is high | mitigate (D-IMPL-04 STOP) | If the only OPEN_BATCH satisfying `chosen × measured ≤ 16.7M − HEADROOM` is LOWER than ~100 (the 331-era usable value), STOP and re-spec. Task 4 acceptance criteria + the 335-LOCAL-VERIFICATION.md attestation make this a hard floor, not a soft preference. |
| T-335-31 | Tampering (NEW reds silently deferred to 336) | Task 2 if a fixture-migration artifact is mistaken for a "carried baseline red" | mitigate (D-IMPL-03 per-test triage) | Task 2's reconciliation policy enumerates the four diff cells (baseline×current → carried, fixed, new-red, un-skipped) and forces a per-test classification. The 335-LOCAL-VERIFICATION.md ledger captures each. No "TODO: defer to 336" annotation is allowed; all NEW reds are closed in this plan or STOP-and-re-spec triggered. |
| T-335-32 | Repudiation | claim "the forge test ledger doesn't match v49 baseline because v49 baseline was wrong" | mitigate (cite 332 verification) | The v49 baseline 666/42/17-by-NAME was empirically established at 332 verification (per STATE.md MEMORY index). Task 2 cites it directly; the per-test diff is the source-of-truth.  |
| T-335-33 | Information disclosure | the 335-LOCAL-VERIFICATION.md leaks contract behavior to a third party | accept | The file lives in `.planning/`, which is the planning workspace; it is committed alongside the diff but does NOT contain any secret material. The measurements are reproducible by anyone with the post-v50 source tree. |
| T-335-34 | Tampering (Task 4 forgets to update the test mirror) | OPEN_BATCH contract-side ≠ test-mirror value | mitigate (acceptance criteria assert equality) | Task 4 acceptance criteria require: `grep OPEN_BATCH` returns the picked value in BOTH the contract-side (if homed) AND the test mirror; the values match. The 335-LOCAL-VERIFICATION.md records both `file:line`s. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — the forge-test full-suite + the freeze-related test files (`RngFreezeAndRemovalProofs.t.sol` after Plan 335-05 Task 5) are the empirical attest. Deeper freeze fuzz at 336/TST-01.
- **OPEN-E operator-approval trust boundary** — `AfKingFundingWaterfall.t.sol` after Plan 335-05 Task 2 is the empirical attest.
- **AFKING cancel-tombstone + swap-pop invariant** — `AfKingConcurrency.t.sol` after Plan 335-05 Task 3 is the empirical attest.
- **D-IMPL-04 measurement-first OPEN_BATCH** — Task 3 + Task 4 are the load-bearing attestation. The 335-LOCAL-VERIFICATION.md is the artifact.
</threat_model>

<verification>
Plan-level checks (executor MUST confirm after all 5 tasks):

1. `forge build` exits 0 — captured in 335-LOCAL-VERIFICATION.md §1.
2. `forge test` ledger is green-or-known-baseline with per-test diff vs `666/42/17`-by-NAME — captured in §2.
3. `KeeperOpenBoxWorstCaseGas` measurement (whale + non-whale per-box gas; uniform-O(1) within 25%; ≤ 167K) — captured in §3.
4. Flat `OPEN_BATCH` picked; attestation `chosen × measured ≤ 16.7M − HEADROOM` holds — captured in §4.
5. Constant `OPEN_BATCH = <picked>` present in contract-side (if homed) + test mirror — confirmed by grep.
6. `grep -nE "TODO" test/gas/RouterWorstCaseGas.t.sol` returns 0 lines (Plan 335-05's TODO is closed).
7. 335-LOCAL-VERIFICATION.md has all 7 sections with concrete numbers — captured by Task 5 acceptance.
8. No STOP-and-re-spec triggers fired (D-IMPL-03 / D-IMPL-04 floors held).
</verification>

<success_criteria>
- forge build green.
- forge test green-or-known-baseline with explicit per-test ledger vs v49 baseline.
- KeeperOpenBoxWorstCaseGas measurement captured; uniform-O(1) within tolerance.
- Flat OPEN_BATCH picked; attestation arithmetic recorded.
- 335-LOCAL-VERIFICATION.md authored with all 7 sections.
- WHALE-03 empirical re-confirmation done.
- BATCH-02 local-verification leg done; the diff is now ready for Plan 335-07's USER hand-review.
- No STOP signal raised (or if raised, the SUMMARY + 335-LOCAL-VERIFICATION.md document it explicitly and HALT before Plan 335-07).
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-06-SUMMARY.md` when done (in addition to the 335-LOCAL-VERIFICATION.md authored in Task 5).

The SUMMARY records:
- A one-paragraph recap of the verification (build, test, measurement, picker).
- Pointer to 335-LOCAL-VERIFICATION.md for the full ledger.
- Confirmation that no STOP-and-re-spec triggers fired.
- Statement that the diff is APPLIED to the working tree but NOT COMMITTED (BATCH-02 HARD STOP at Plan 335-07).
</output>
