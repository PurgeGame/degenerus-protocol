---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 05
type: execute
wave: 3
depends_on: [335-01, 335-02, 335-03, 335-04]
files_modified:
  - test/fuzz/AfKingSubscription.t.sol
  - test/fuzz/AfKingFundingWaterfall.t.sol
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/KeeperNonBrick.t.sol
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
  - test/gas/KeeperLeversAndPacking.t.sol
  - test/gas/RouterWorstCaseGas.t.sol
autonomous: true
worktree: false
requirements: [BATCH-02]
tags: [tests, fixture-migration, full-alignment, d-impl-02]

must_haves:
  truths:
    - "All 7 affected test files compile under the post-AFSUB / post-WHALE / post-MINTDIV-02 contract surface (no surviving `paidThroughDay`, `burnForKeeper`, `hasAnyLazyPass` writes in test bodies that touch the migrated semantics — except `hasAnyLazyPass` which is RETAINED on the contract per Plan 335-01 Task 2; tests that previously asserted AfKing's specific use of it are the ones migrating)."
    - "`OPEN_NORMAL_GAS_UNIT` and `_activateWhalePass` private-fn references are gone from `test/gas/RouterWorstCaseGas.t.sol`."
    - "The packing oracle in `test/gas/KeeperLeversAndPacking.t.sol` asserts `validThroughLevel` (not `paidThroughDay`); the `burnForKeeper(` byte-presence assertion (G8) is DELETED."
    - "The new pass-eviction tombstone behavior is positively asserted in `AfKingSubscription.t.sol` (eviction branch) AND `AfKingConcurrency.t.sol` (H-CANCEL-SWAP-MISS regression non-reproduction under pass-eviction)."
    - "OPEN-E 4-protection assertions in `AfKingFundingWaterfall.t.sol` SURVIVE (they were never tied to `burnForKeeper`); the new assertion is pass-eviction-preserves-fundingSource state."
    - "`test/gas/KeeperOpenBoxWorstCaseGas.t.sol` is UNTOUCHED in this plan (Plan 335-06 re-runs the harness for the OPEN_BATCH measurement)."
  artifacts:
    - path: "test/fuzz/AfKingSubscription.t.sol"
      provides: "Pass-eviction + refresh-at-crossing semantics tests"
    - path: "test/fuzz/AfKingFundingWaterfall.t.sol"
      provides: "OPEN-E 4-protection re-attest + pass-eviction-preserves-tombstone"
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "Pass-gated sweep + v49 swap-pop membership invariant"
    - path: "test/fuzz/KeeperNonBrick.t.sol"
      provides: "No-brick under pass-eviction; H-CANCEL-SWAP-MISS class non-reproduction"
    - path: "test/fuzz/RngFreezeAndRemovalProofs.t.sol"
      provides: "Trivial assertions: `whalePassClaims +=` is non-frozen-slot; `lazyPassHorizon` is non-RNG-window read"
    - path: "test/gas/KeeperLeversAndPacking.t.sol"
      provides: "Migrated `validThroughLevel` packing oracle; deleted G8 `burnForKeeper(` byte-presence assertion"
    - path: "test/gas/RouterWorstCaseGas.t.sol"
      provides: "Flat `OPEN_BATCH` budget assertions; dropped gas-weighted assertions; `OPEN_NORMAL_GAS_UNIT` mirror deleted"
  key_links:
    - from: "test/fuzz/AfKingSubscription.t.sol"
      to: "contracts/AfKing.sol (post-AFSUB surface)"
      via: "test fixture exercises the new `subscribe`/`_autoBuy` semantics"
      pattern: "validThroughLevel"
    - from: "test/gas/RouterWorstCaseGas.t.sol"
      to: "Plan 335-06 OPEN_BATCH measurement"
      via: "the flat `OPEN_BATCH` value is PICKED from the measurement (per D-IMPL-04 / Pitfall P4); the test mirror uses the same picked value"
      pattern: "OPEN_BATCH"
---

<objective>
Migrate the 7 affected test files in lockstep with the contract diff (Plans 335-01..04), per the D-IMPL-02 FULL ALIGNMENT policy. Without this migration, `forge build` fails (~70 references across these 7 files directly interrogate removed surfaces — `Sub.paidThroughDay` (×40), `burnForKeeper()` (×19), `hasAnyLazyPass()` (×14 in AfKing-test contexts), `OPEN_NORMAL_GAS_UNIT` (×6), `_activateWhalePass` (×5) — counts from 335-CONTEXT.md). The migration is the integration oracle: a green-or-known-baseline suite at HARD STOP is what makes the BATCH-02 USER review crisp (NEW red == regression; baseline red == carried).

This delivers:
- **BATCH-02 (full-alignment piece)** — the diff that ships to USER review covers both `contracts/` AND `test/`, just like the v49 BATCH-02 precedent (Phase 330 IMPL `63bc16ca` shipped 5 contracts + 9 tests in one batch).

Effective by-product:
- **TST-01 (AfKing pass-gated coverage piece)** — the test bodies in `AfKingSubscription.t.sol` empirically prove the new pass-eviction / refresh-at-crossing semantics. The deeper RNG-freeze fuzz of the deferred-claim path is NOT in this plan — that is 336/TST-01 freeze leg.
- **TST-02 (whale-pass equivalence piece)** — the post-edit `RngFreezeAndRemovalProofs.t.sol` carries trivial assertions about the new write paths; the deeper equivalence + uniform-O(1) gas assertions are 336/TST-01.

Purpose: this plan ships the test side of the single batched diff. Wave 3 because it depends on Plans 335-01..04 having landed their contract surfaces — the migrated test bodies reference the new contract symbols (`validThroughLevel`, `lazyPassHorizon`, the absent `burnForKeeper`).

Output: edits to 7 test files. The full `forge build` + `forge test` verification lives at Plan 335-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-02-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-04-PLAN.md

<interfaces>
<!-- All 7 test files are self-analog (PATTERNS §2.7-§2.13 + §1 file classification). The post-AFSUB / post-WHALE / post-MINTDIV-02 contract API is the migration target. -->

Removed surfaces (post-Plans 335-01..04):
- `Sub.paidThroughDay` → renamed to `Sub.validThroughLevel` (offset 5, same width if uint32 kept)
- `burnForKeeper(address,uint256)` → DELETED entirely from contracts/
- `hasAnyLazyPass(address)` → STILL EXISTS on `DegenerusGame.sol` (Plan 335-01 Task 2 preserved it for non-AfKing callers); ONLY AfKing's two reads (`AfKing.sol:432`/`:631`) were removed. Test files that read `IGame(GAME).hasAnyLazyPass(...)` directly may still do so; tests that asserted AFKING'S USE of it (e.g. `_grantDeityPass` → "subscribe-time `hasAnyLazyPass` path fires the free-extend") must migrate to the new `validThroughLevel` semantics.
- `OPEN_NORMAL_GAS_UNIT = 90_000` → DELETED from `DegenerusGame.sol`
- `WINDOW_DAYS = 30` → DELETED from `AfKing.sol`
- `FLAG_WINDOW_PAID = 1` → DELETED from `AfKing.sol`
- `BurnieAutoExtracted(...)` event in `AfKing.sol` → DELETED (the PAID branch's event)
- `KeeperBurn(...)` event in `BurnieCoin.sol` → DELETED

Added surfaces (post-Plans 335-01..04):
- `Sub.validThroughLevel` (uint32 or uint24 — IMPL planner's choice per Plan 335-04 Task 1)
- `IGame(GAME).lazyPassHorizon(address) external view returns (uint24)` — the producer view (Plan 335-01 Task 2)
- `whalePassClaims[player] += 1;` write at box-open (Plan 335-02)
- `MintModule:716` advance: `processed += take;` (Plan 335-03 — does not change any test API surface; full-suite regression covers it)
- `SubscriptionExpired(player, 1)` reason code `1` is now "pass-eviction" (was "burnForKeeper shortfall"; same code, repurposed semantically)
- `SubscriptionExtendedFree(player, today)` event from the refresh branch (or `SubscriptionRefreshed` if Plan 335-04 Task 3 renamed it; verify before referencing)

Per-file migration scope (from PATTERNS §2.7-§2.13 + 335-CONTEXT.md D-IMPL-02 counts):

| File | paidThroughDay refs | burnForKeeper refs | hasAnyLazyPass refs | Notes |
|------|---------------------|--------------------|---------------------|-------|
| test/fuzz/AfKingSubscription.t.sol | ×14 | ×11 | ×3 | Heaviest migration; the pass-OR-pay test bodies become pass-eviction-OR-refresh test bodies. |
| test/fuzz/AfKingFundingWaterfall.t.sol | ×5 | ×6 | ×3 | OPEN-E 4-protection assertions PRESERVED; add pass-eviction-preserves-fundingSource. |
| test/fuzz/AfKingConcurrency.t.sol | ×18 | 0 | 0 | Rebase `paidThroughDay` writes to `validThroughLevel`; H-CANCEL-SWAP-MISS regression under pass-eviction. |
| test/fuzz/KeeperNonBrick.t.sol | ×1 | 0 | ×1 | Small rewrite; no-brick assertion under heavy concurrent pass-expiration. |
| test/fuzz/RngFreezeAndRemovalProofs.t.sol | 0 | 0 | ×7 | Trivial assertion rewrite (the deeper freeze fuzz is 336/TST-01). |
| test/gas/KeeperLeversAndPacking.t.sol | ×2 | ×2 | 0 | Rename oracle string; delete G8 byte-presence assertion. |
| test/gas/RouterWorstCaseGas.t.sol | 0 (touches `OPEN_NORMAL_GAS_UNIT` ×6 + `_activateWhalePass` ×5) | 0 | 0 | Drop gas-weighted assertions; re-target flat `OPEN_BATCH`. |

`test/gas/KeeperOpenBoxWorstCaseGas.t.sol` is UNTOUCHED in this plan — it ALREADY measures the right thing (uniform-O(1) per-box gas). Plan 335-06 re-runs it for the D-IMPL-04 measurement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AfKingSubscription.t.sol — migrate pass-OR-pay → pass-eviction-OR-refresh</name>
  <files>test/fuzz/AfKingSubscription.t.sol</files>
  <read_first>
    - test/fuzz/AfKingSubscription.t.sol (full file — pre-edit; pay particular attention to lines 38-49 offset oracle, lines 79-129 pass-OR-pay test bodies, and the `_forceRenewalDue` / `_fundBurnie` helper definitions)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.7 — "The heaviest migration" — full settled migration pattern for testCrossingPassHolderRefreshedNotEvicted + testCrossingNoPassEvictedViaTombstone)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-04-PLAN.md (Plan 335-04 Tasks 1-3, especially the event name `SubscriptionExtendedFree` / `SubscriptionRefreshed` decision and the `SubscriptionExpired(player, 1)` reason-code repurpose)
  </read_first>
  <action>
    Rewrite `test/fuzz/AfKingSubscription.t.sol` per PATTERNS §2.7. Concrete migration steps:

    (a) **Storage oracle (lines 38-49)** — rename `OFF_PAIDTHROUGH = 5` → `OFF_VALIDTHROUGHLEVEL = 5` (same offset; same width per Plan 335-04 Task 1's uint32 default). If Plan 335-04 picked `uint24`, update the read-helper width too.

    (b) **Helpers** — rename `_forceRenewalDue(addr) → _forceCrossingDue(addr)`. The helper writes the slot at `OFF_VALIDTHROUGHLEVEL` directly via `vm.store` (the existing pattern, just retargeted). Concretely: the helper writes `validThroughLevel < currentLevel` (e.g. `validThroughLevel = 0` always forces crossing). Drop any `_fundBurnie(addr, cost)` helper calls — BURNIE is no longer involved in subscribe/renewal.

    (c) **Test body migration** — replace each pass-OR-pay test body with the analogous pass-eviction-OR-refresh body. Two canonical cases (mirror PATTERNS §2.7 last code block):
        - `testCrossingPassHolderRefreshedNotEvicted` (pass-holder REFRESHES at crossing): grant deity, subscribe, approve keeper, fund pool, `_forceCrossingDue`, `vm.recordLogs()`, prank an autoBuyer + call `afKing.autoBuy(50)`, then assert: 1 `SubscriptionExtendedFree` (or `SubscriptionRefreshed`) event emitted, `subscriptionOf(pass).validThroughLevel > _currentLevel()`. DO NOT assert BURNIE balance unchanged (BURNIE is not involved); DO NOT assert FLAG_WINDOW_PAID bit (bit 0 freed).
        - `testCrossingNoPassEvictedViaTombstone` (no-pass holder EVICTED at crossing): subscribe a no-pass addr (no BURNIE charge needed at subscribe under AFSUB), approve keeper, fund pool, `_forceCrossingDue`, `vm.recordLogs()`, prank an autoBuyer + call `afKing.autoBuy(50)` (MUST NOT revert), assert: ≥ 1 `SubscriptionExpired(player, 1)` event, `subscriptionOf(nopass).dailyQuantity == 0` (tombstoned), `_subscriberIndexOf(nopass) == 0` (removed from iterable set; the v49 swap-pop semantic — Pitfall P6).

    (d) **Migrate all 14 `paidThroughDay` refs** to `validThroughLevel`; **drop all 11 `burnForKeeper` refs** (no replacement — the function is gone); **migrate the 3 `hasAnyLazyPass` refs**:
        - If a test was asserting "the subscribe path called hasAnyLazyPass on the subscriber" → migrate to "the subscribe path read `lazyPassHorizon(subscriber)` exactly once" (assert via `vm.recordLogs` or a state-read oracle if available — or simply assert the resulting `validThroughLevel` matches what `lazyPassHorizon` would return).
        - If a test was asserting "`hasAnyLazyPass` is the gate on the FREE-extend branch" → migrate to "the refresh branch is taken iff `lazyPassHorizon(player) >= currentLevel`."

    (e) **SUB-09 deploy-time self-subscribes (VAULT + SDGNRS)** — per Plan 335-04 Task 2 explicit behavior change, VAULT + SDGNRS subscribe at deploy-time with `validThroughLevel = lazyPassHorizon(VAULT or SDGNRS) = 0` (they hold no passes). On first crossing, they are evicted via tombstone. If any test fixture in this file relies on VAULT/SDGNRS being subscribed for the duration of the test (e.g. testing the two-tier funding-skip), update the fixture or the test to either: (i) re-subscribe VAULT/SDGNRS post-eviction within the test, OR (ii) accept the eviction as expected behavior and assert it. Record the chosen approach in the SUMMARY.

    Constraints:
    - The file MUST compile after this task (the fixture-migration integration oracle).
    - The migration is BEHAVIOR-PRESERVING in spirit: a pass-holder still avoids being terminated at the crossing (refresh ≡ free-extend); a non-pass-holder is still terminated at the crossing (evict ≡ shortfall failure). Only the MECHANISM changes (level vs. day; tombstone-via-dailyQuantity vs. setMembershipExpired).
    - DO NOT migrate `hasAnyLazyPass` test references that pertain to NON-AfKing surfaces — e.g. a test reading `degenerusGame.hasAnyLazyPass(...)` directly is fine to keep (the view itself is preserved per Plan 335-01 Task 2). Only the AfKing-specific assertions migrate.
  </action>
  <verify>
    <automated>grep -cE "validThroughLevel" test/fuzz/AfKingSubscription.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "paidThroughDay" test/fuzz/AfKingSubscription.t.sol` returns 0 lines.
    - `grep -nE "burnForKeeper" test/fuzz/AfKingSubscription.t.sol` returns 0 lines.
    - `grep -cE "validThroughLevel" test/fuzz/AfKingSubscription.t.sol` returns ≥ 14 (the migrated references — at minimum mirroring the old paidThroughDay count).
    - `grep -nE "function testCrossing" test/fuzz/AfKingSubscription.t.sol` returns ≥ 2 lines (the new canonical refresh + evict tests).
    - `grep -nE "SubscriptionExpired" test/fuzz/AfKingSubscription.t.sol` returns ≥ 1 line (the evict event assertion).
    - The SUMMARY records the VAULT/SDGNRS first-crossing-eviction handling decision.
  </acceptance_criteria>
  <done>The pass-OR-pay test bodies are migrated to pass-eviction-OR-refresh; storage oracle renamed; helpers retargeted; all paidThroughDay/burnForKeeper references purged; AfKing-specific hasAnyLazyPass references migrated to validThroughLevel/lazyPassHorizon; VAULT/SDGNRS fixture handling decided + recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AfKingFundingWaterfall.t.sol — preserve OPEN-E 4 protections + add pass-eviction-preserves-fundingSource</name>
  <files>test/fuzz/AfKingFundingWaterfall.t.sol</files>
  <read_first>
    - test/fuzz/AfKingFundingWaterfall.t.sol (full file — pre-edit; pay attention to the OPEN-E 4-protection assertion structure)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.8)
    - memory open-e-operator-approval-trust-boundary.md (the 4 protections that MUST re-attest)
  </read_first>
  <action>
    Migrate `test/fuzz/AfKingFundingWaterfall.t.sol`:

    (a) **PRESERVE all OPEN-E 4-protection assertions**: consent-gate-at-subscribe (SUB-02 + OPENE-04 still fire), default-self byte-identical (subscribe-without-fundingSource → `fundingSource == address(0)` ≡ pay-from-self semantics), no-escalation (operator can't expand the consent surface mid-life), trust-the-sub temporal bound (cancel + re-approve flow). These were never tied to `burnForKeeper` — they assert subscribe-time consent.

    (b) **Drop all `burnForKeeper` refs** (×6) — they were checking that the keeper extract flowed FROM `fundingSource` not from the subscriber, via the resolved-source argument to `burnForKeeper`. Replace with the new analog: "pass-eviction tombstones the SUB, NOT the fundingSource's separate state."

    (c) **Drop all `paidThroughDay` refs** (×5) → migrate to `validThroughLevel` where the assertion remains meaningful (e.g. "after subscribe, validThroughLevel is set from the subscriber's pass — NOT the fundingSource's pass").

    (d) **Drop all `hasAnyLazyPass` refs** (×3) where they pertained to "subscribe-time free-extend taken for a pass-holding subscriber" — migrate to the new horizon-based assertion. If they pertained to the test-level `_grantDeityPass` helper checking eligibility, those callers may keep `hasAnyLazyPass` (the contract function still exists per Plan 335-01 Task 2 — only AfKing's two reads were removed).

    (e) **ADD new positive assertion**: "a `fundingSource`-funded sub whose subscriber's pass expires at a crossing → SUB evicted (tombstoned), fundingSource address still in state (e.g. `sub.fundingSource` is set on the deleted sub's storage AT THE MOMENT OF EVICTION — record-then-delete; if Plan 335-04 Task 3 does `delete _subOf[player]`, then ALL Sub fields including `fundingSource` are zeroed AT eviction; if it does `setDailyQuantity(0)` semantics that preserve fundingSource for re-subscribe, the assertion differs). VERIFY against Plan 335-04 Task 3's final code; the test must assert WHAT THE CODE DOES.

    Constraints:
    - DO NOT assert behavior that is NOT documented in Plan 335-04 (e.g. don't assert "the fundingSource is preserved across eviction" if Plan 335-04 chose `delete _subOf[player]` — read Plan 335-04 first, then assert the actual behavior).
    - The 4 OPEN-E protection assertions PRESERVE their existing structure — those tests are NOT regressions to write; they survive the migration unchanged in spirit.
  </action>
  <verify>
    <automated>grep -nE "burnForKeeper|paidThroughDay" test/fuzz/AfKingFundingWaterfall.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "burnForKeeper" test/fuzz/AfKingFundingWaterfall.t.sol` returns 0 lines.
    - `grep -nE "paidThroughDay" test/fuzz/AfKingFundingWaterfall.t.sol` returns 0 lines.
    - `grep -nE "fundingSource" test/fuzz/AfKingFundingWaterfall.t.sol` returns ≥ 5 lines (OPEN-E 4-protection assertions preserved + new pass-eviction-vs-fundingSource assertion).
    - The SUMMARY records whether Plan 335-04 Task 3's eviction zeros `sub.fundingSource` or preserves it (read Plan 335-04 Task 3's final code BEFORE asserting either).
  </acceptance_criteria>
  <done>OPEN-E 4-protection assertions preserved; burnForKeeper / paidThroughDay references purged; new pass-eviction-vs-fundingSource assertion added per Plan 335-04 Task 3's actual eviction behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AfKingConcurrency.t.sol — pass-gated sweep + v49 swap-pop invariant under pass-eviction</name>
  <files>test/fuzz/AfKingConcurrency.t.sol</files>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol (full file — pre-edit; pay attention to the v49 swap-pop membership ⟺ packed != 0 invariant assertions)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.9 + §3.4)
    - memory afking-cancel-tombstone-streak-finding.md (H-CANCEL-SWAP-MISS class — the scenario the test re-derives)
  </read_first>
  <action>
    Migrate `test/fuzz/AfKingConcurrency.t.sol`:

    (a) **Rebase all 18 `paidThroughDay` writes to `validThroughLevel`** — same slot, same width if uint32 kept; the storage-poke helpers work unchanged with the renamed field.

    (b) **Re-derive the H-CANCEL-SWAP-MISS scenario under pass-eviction**: a player whose pass expires mid-sweep MUST NOT relocate a pending tail behind the cursor. The reclaim path (`_autoBuy:605-617` `continue` WITHOUT advancing cursor — Pitfall P6) is what protects this; the test asserts the swap-pop occupant is processed in the same autoBuy call.

    (c) **PRESERVE the v49 swap-pop assertion**: `membership ⟺ packed != 0`. After eviction (writing `dailyQuantity = 0`), the `Sub.packed` representation has all fields zeroed → membership is `false`. Concretely: after a forced eviction via crossing, `_subscriberIndexOf(evicted) == 0` and `_subOf[evicted].dailyQuantity == 0` both hold.

    Constraints:
    - DO NOT drop any v49 swap-pop assertions — they STAY (they were never tied to BURNIE).
    - The H-CANCEL-SWAP-MISS regression test was originally written against `setDailyQuantity(0)`-triggered swap-pop; the new test re-runs the SAME scenario against pass-eviction-triggered swap-pop. The structural invariant is identical; the trigger is different.
  </action>
  <verify>
    <automated>grep -nE "paidThroughDay" test/fuzz/AfKingConcurrency.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "paidThroughDay" test/fuzz/AfKingConcurrency.t.sol` returns 0 lines.
    - `grep -cE "validThroughLevel" test/fuzz/AfKingConcurrency.t.sol` returns ≥ 18 (the migrated references mirroring the old count).
    - `grep -nE "_subscriberIndex|_subscriberIndexOf" test/fuzz/AfKingConcurrency.t.sol` returns ≥ 1 line (the swap-pop invariant still asserted).
    - At least one test function name contains "swap-pop" / "SwapPop" / "CancelTombstone" / similar (preserved from pre-edit naming).
  </acceptance_criteria>
  <done>paidThroughDay writes rebased to validThroughLevel; v49 swap-pop membership invariant assertions preserved; H-CANCEL-SWAP-MISS regression re-run under pass-eviction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: KeeperNonBrick.t.sol — no-brick under pass-eviction</name>
  <files>test/fuzz/KeeperNonBrick.t.sol</files>
  <read_first>
    - test/fuzz/KeeperNonBrick.t.sol (full file)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.10)
  </read_first>
  <action>
    Small rewrite. Migrate:
    (a) The 1 `paidThroughDay` reference → `validThroughLevel`.
    (b) The 1 `hasAnyLazyPass` reference: if it pertains to AFKING'S USE (the subscribe-time or day-31 read), migrate to the new horizon-based check; if it pertains to a DegenerusGame-direct read for an unrelated assertion, keep.
    (c) ADD an assertion: under heavy concurrent pass-expiration (e.g. many subs whose validThroughLevel is below currentLevel simultaneously), `autoBuy` MUST NOT revert; the H-CANCEL-SWAP-MISS class missed-day / mint-streak-reset does NOT reproduce. The test can force the scenario by directly poking validThroughLevel via `vm.store` for many subs.

    Constraints:
    - The no-brick assertion is the load-bearing one — preserve it across the migration.
  </action>
  <verify>
    <automated>grep -nE "paidThroughDay" test/fuzz/KeeperNonBrick.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "paidThroughDay" test/fuzz/KeeperNonBrick.t.sol` returns 0 lines.
    - At least one test function contains a `vm.expectRevert`-free `autoBuy(...)` call under a forced mass-expiration setup (the no-brick assertion).
  </acceptance_criteria>
  <done>paidThroughDay reference migrated; hasAnyLazyPass usage migrated where AFKING-specific; no-brick assertion preserved and exercises pass-eviction scenarios.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: RngFreezeAndRemovalProofs.t.sol — trivial assertion rewrites (deeper freeze fuzz is 336/TST-01)</name>
  <files>test/fuzz/RngFreezeAndRemovalProofs.t.sol</files>
  <read_first>
    - test/fuzz/RngFreezeAndRemovalProofs.t.sol (full file — pre-edit; pay attention to the existing freeze-write assertion patterns)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.11)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md (§1 slot map; §5 verdict)
  </read_first>
  <action>
    Trivial assertion-only rewrite per PATTERNS §2.11 + 335-CONTEXT.md D-IMPL-02:

    (a) **Drop the 7 `hasAnyLazyPass` references** — they pertained to AFKING'S USE of the view; the AfKing surface they tested is gone. Replace with `lazyPassHorizon`-based reads where the test logic requires a pass check (e.g. asserting "the contract responds to a pass query without writing to a frozen slot").

    (b) **ADD trivial assertion 1**: the box-open `whalePassClaims +=` write occurs at a non-frozen-slot. Cite WHALE04-FREEZE-PROOF §1 (`whalePassClaims` is a pending-claim accumulator, NOT VRF-influenced). The assertion can be expressed as: "after a box-open that triggers `_activateWhalePass`, the `whalePassClaims[player]` value differs from before; this write occurred during a transaction that may or may not be under `rngLock`; assert no revert occurred" — the trivial form is: `whalePassClaims[player]` increased; transaction succeeded.

    (c) **ADD trivial assertion 2**: the AfKing crossing's `lazyPassHorizon` external view read is not an RNG-window read. Cite WHALE04-FREEZE-PROOF §5 (`mintPacked_` freeze-state is the read target; reads do not write — the freeze invariant is about writes, not reads). The assertion: call `lazyPassHorizon(player)` from a test, assert no revert AND no state change to the player's `mintPacked_` slot (via `vm.load`-before/after).

    (d) **DEFER the deeper RNG-freeze fuzz** of the deferred-claim path to 336/TST-01 freeze leg. Document this deferral explicitly in the test file comments (so 338's economic-analyst doesn't expect a deeper proof from this plan).

    Constraints:
    - Keep the file's existing freeze-write assertion patterns for OTHER slots (e.g. `ticketsOwedPacked`, `mintPacked_`); only the 7 `hasAnyLazyPass`-tied assertions migrate.
    - The deeper freeze fuzz IS NOT in this plan's scope (335-CONTEXT.md D-IMPL-02 + Deferred Ideas).
  </action>
  <verify>
    <automated>grep -cE "hasAnyLazyPass" test/fuzz/RngFreezeAndRemovalProofs.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "hasAnyLazyPass" test/fuzz/RngFreezeAndRemovalProofs.t.sol` returns 0 lines (all 7 references migrated or dropped).
    - At least one test function asserts `whalePassClaims[player]` ALSO writes non-frozen-slot (the new positive assertion 1).
    - At least one test function asserts `lazyPassHorizon` read does not touch RNG slots (the new positive assertion 2).
    - A TODO-style comment near the top of the file explicitly defers the deeper deferred-claim freeze fuzz to 336/TST-01.
  </acceptance_criteria>
  <done>7 hasAnyLazyPass references migrated/dropped; 2 trivial positive assertions added for the new write paths; deeper freeze fuzz deferred to 336 (recorded in test file).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: KeeperLeversAndPacking.t.sol — rename packing oracle string; delete G8 byte-presence assertion</name>
  <files>test/gas/KeeperLeversAndPacking.t.sol</files>
  <read_first>
    - test/gas/KeeperLeversAndPacking.t.sol (full file — pay attention to lines 215-235 `_structFieldBytes` packing oracle and lines 296-300 G8 `burnForKeeper(` byte-presence assertion)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.12)
  </read_first>
  <action>
    Two surgical edits:

    (a) **At `:229`** (or wherever the `_structFieldBytes(afking, "uint32 paidThroughDay;", 4)` oracle reference lives): rename the oracle string to `"uint32 validThroughLevel;"` (4 bytes). If Plan 335-04 Task 1 picked `uint24` instead of `uint32`, rename to `"uint24 validThroughLevel;"` (3 bytes) AND update the second argument (the width).
        Also update the surrounding docstring comment block at `:215-229` that enumerates the `Sub` struct field widths — change `paidThroughDay(4)` → `validThroughLevel(4)` (or `(3)` if uint24).

    (b) **DELETE the G8 assertion at `:296-297`**: the `assertGt(_countOccurrences(afking, "burnForKeeper("), 0, "G8: burnForKeeper all-or-nothing charge byte-present");` line and its docstring (`// G8 — burnForKeeper all-or-nothing (AfKing:396 — IBurnie.burnForKeeper).`). After AFSUB, `burnForKeeper(` MUST NOT appear in the deployed bytecode; the assertion ALREADY would fail — delete the assertion (it has no replacement; the BURNIE-sink semantics it tested are gone).

    Constraints:
    - DO NOT touch any other byte-presence assertions in the file.
    - DO NOT add a new "validThroughLevel" byte-presence assertion (no need — the packing oracle covers the struct layout).
  </action>
  <verify>
    <automated>grep -nE "paidThroughDay|burnForKeeper" test/gas/KeeperLeversAndPacking.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "paidThroughDay" test/gas/KeeperLeversAndPacking.t.sol` returns 0 lines.
    - `grep -nE "burnForKeeper" test/gas/KeeperLeversAndPacking.t.sol` returns 0 lines.
    - `grep -nE "validThroughLevel" test/gas/KeeperLeversAndPacking.t.sol` returns ≥ 1 line.
    - The "G8" docstring is also gone (`grep -nE "G8" test/gas/KeeperLeversAndPacking.t.sol` returns 0 lines).
  </acceptance_criteria>
  <done>Packing oracle string renamed (with width matching Plan 335-04 Task 1's choice); G8 byte-presence assertion deleted with its docstring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: RouterWorstCaseGas.t.sol — drop gas-weighted assertions; re-target flat OPEN_BATCH (value placeholder; Plan 335-06 picks)</name>
  <files>test/gas/RouterWorstCaseGas.t.sol</files>
  <read_first>
    - test/gas/RouterWorstCaseGas.t.sol (full file — pay attention to lines 115-128 constants block and the `_activateWhalePass` private-fn references)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.13 + Pitfall P4 — measurement-first)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§6 — WHALE-03 retirement)
  </read_first>
  <action>
    Migrate `test/gas/RouterWorstCaseGas.t.sol`:

    (a) **DELETE the `OPEN_NORMAL_GAS_UNIT = 90_000` mirror at `:121`** (6 occurrences across the file per 335-CONTEXT.md count — `:121` is the constant decl; the other 5 are references in test bodies). All 6 references go.

    (b) **DELETE the `WHALE_CLUSTER_WORD` constant + cluster-search helper at `:128`** (and any helper function that uses it) — under WHALE-01, opens are uniform O(1); the whale-pass-cluster worst-case path that this constant identified no longer exists.

    (c) **DELETE all 5 `_activateWhalePass` references** — the private function is no longer the gas-monster surface; uniform opens make it irrelevant. The tests that referenced it were measuring whale-pass-weighted budget behavior — that whole concept retired with WHALE-03.

    (d) **DROP the gas-weighted budget assertions** — concretely, the test functions named `testGasWeightedBudgetClusterCapsLeg`, `GAS-02 weighted-budget worst case`, or similar (per PATTERNS §2.13 last bullet). These tests are deleted entirely; they have no migrated analog under flat OPEN_BATCH.

    (e) **RE-TARGET the test surface** to flat `OPEN_BATCH × measured_per_box_gas ≤ 16.7M − headroom`:
        - Add a placeholder constant `uint256 internal constant OPEN_BATCH = 100;` (or whatever value Plan 335-06 picks — for THIS plan, use a placeholder of `100` as a documented PLACEHOLDER value with a comment noting Plan 335-06 will overwrite it from the measurement).
        - Replace any "BUY_BATCH = 50" / "OPEN_BATCH = 100" oracle references that survived (test/gas/RouterWorstCaseGas.t.sol:115 region) with the new flat values; document the placeholder status in a comment.
        - Add an assertion (parameterized on `OPEN_BATCH × measured ≤ 16.7M`) — the actual `measured` value plugs in from Plan 335-06's `KeeperOpenBoxWorstCaseGas` re-run. For THIS plan, write the assertion with a placeholder `measured = 89_000` (the expected ~89K) AND a TODO comment "Plan 335-06 finalizes the measured value + the picked OPEN_BATCH".

    Constraints:
    - Pitfall P4 — do NOT pre-derive `OPEN_BATCH = ⌊16.7M / 89_000⌋`. The value comes from the measurement. This task installs a placeholder with TODO; Plan 335-06 finalizes.
    - DO NOT delete the file or any unrelated tests; only the gas-weighted-budget cohort goes.
    - If a test in this file exercised the BUY_BATCH path (buy-side gas worst-case), KEEP it untouched (BUY_BATCH = 50 from v49 GAS-02 stands).
  </action>
  <verify>
    <automated>grep -nE "OPEN_NORMAL_GAS_UNIT|_activateWhalePass" test/gas/RouterWorstCaseGas.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "OPEN_NORMAL_GAS_UNIT" test/gas/RouterWorstCaseGas.t.sol` returns 0 lines.
    - `grep -nE "_activateWhalePass" test/gas/RouterWorstCaseGas.t.sol` returns 0 lines.
    - `grep -nE "WHALE_CLUSTER_WORD|weighted budget" test/gas/RouterWorstCaseGas.t.sol` returns 0 lines.
    - `grep -nE "OPEN_BATCH" test/gas/RouterWorstCaseGas.t.sol` returns ≥ 1 line (the new placeholder constant).
    - A `TODO`-style comment near the OPEN_BATCH constant explicitly cites Plan 335-06 as the finalizer.
    - BUY_BATCH-side assertions UNTOUCHED (`grep -nE "BUY_BATCH" test/gas/RouterWorstCaseGas.t.sol` returns ≥ 1 line, value 50).
  </acceptance_criteria>
  <done>OPEN_NORMAL_GAS_UNIT mirror + 5 _activateWhalePass refs + gas-weighted-budget tests deleted; flat OPEN_BATCH placeholder + TODO recorded for Plan 335-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → contract state | Tests use `vm.store` / `vm.prank` / `forge` cheatcodes — fully controlled environment; no untrusted input. |
| post-edit test compilation → forge build oracle | The migration is itself the integration oracle: if `forge build` fails, the contract edits in Plans 335-01..04 OR the test migration in this plan have left a dangling reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-25 | Tampering (test migration fails to catch a real regression) | the migrated test bodies are too lenient (e.g. all asserts dropped) | mitigate (positive-assertion preservation rule) | The PATTERNS §2.7-§2.13 migrations specify CONCRETE replacement assertions for each removed one. The acceptance criteria in each task assert grep-presence of the NEW assertion patterns (e.g. `testCrossingPassHolderRefreshedNotEvicted` exists; `SubscriptionExpired` is asserted on the EVICT path). Plan 335-06's `forge test` then exercises them. |
| T-335-26 | Repudiation | a NEW red surfaces in `forge test` at Plan 335-06 → unclear whether it's a fixture-migration artifact or a real v50 regression | mitigate (D-IMPL-03 discrimination policy) | The full-alignment policy (D-IMPL-02) ensures tests follow the new code in the SAME diff — so the only legitimate NEW reds at Plan 335-06 are genuine v50 regressions, NOT fixture-migration artifacts. If a NEW red surfaces that's genuinely a regression: STOP and re-spec (per Plan 335-06 D-IMPL-03 / 335-CONTEXT.md). The crisp discrimination IS the point of full alignment. |
| T-335-27 | Information disclosure | test files reveal contract internals (slot offsets, packing, internal helpers) | accept (existing pattern) | Pre-edit tests already reveal slot offsets + packing oracles for adversarial defenseive testing; the migration preserves the pattern, doesn't widen it. The repo is private; tests are not deployed. |
| T-335-28 | Tampering (VAULT/SDGNRS first-crossing eviction breaks the test suite) | Plan 335-04 Task 2's behavior change → SUB-09 deploy-time self-subscribes evict on first crossing | mitigate (SUMMARY records the choice; tests assert what the code does) | Plan 335-04 Task 2 explicitly recorded this behavior change. Plan 335-05 Task 1 records the fixture-handling choice (re-subscribe vs. accept eviction). The SUMMARY captures the decision; Plan 335-06's `forge test` is the empirical re-attest. |
| T-335-29 | Tampering (test imports a deleted symbol) | the migration misses a reference; build fails | mitigate (full-alignment + grep gates) | Each task's acceptance criteria includes a grep gate for the deleted symbol (`paidThroughDay`, `burnForKeeper`, `OPEN_NORMAL_GAS_UNIT`, `_activateWhalePass`). The system-wide gate at Plan 335-06 is `forge build` success. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — Task 5 adds the two trivial positive assertions (whalePassClaims/lazyPassHorizon are non-frozen / non-RNG-window). The deeper freeze fuzz is 336/TST-01.
- **OPEN-E operator-approval trust boundary** — Task 2 preserves the 4-protection assertions and adds the pass-eviction-vs-fundingSource assertion.
- **AFKING cancel-tombstone + swap-pop invariant** — Tasks 1 + 3 preserve the swap-pop invariant assertions; Task 3 re-derives the H-CANCEL-SWAP-MISS scenario under pass-eviction; Task 4 adds the no-brick assertion.
- **GASOPT-05 per-iter invariant** — not directly asserted in tests; Task 6's packing oracle exercises the struct layout (no per-iter SLOAD widening).
</threat_model>

<verification>
Plan-level checks (executor MUST run after all 7 tasks):

1. **System-wide test-side burnForKeeper purge**: `grep -rnE "burnForKeeper" test/` returns 0 lines (the load-bearing integration oracle).
2. **System-wide test-side paidThroughDay purge**: `grep -rnE "paidThroughDay" test/` returns 0 lines.
3. **OPEN_NORMAL_GAS_UNIT purge**: `grep -rnE "OPEN_NORMAL_GAS_UNIT" test/` returns 0 lines.
4. **_activateWhalePass purge in tests**: `grep -rnE "_activateWhalePass" test/` returns 0 lines.
5. **validThroughLevel introduced**: `grep -rnE "validThroughLevel" test/` returns ≥ 30 lines (Task 1: ~14, Task 3: ~18, Tasks 2/4/6: a few more).
6. **AfKingSubscription canonical tests exist**: `grep -nE "function testCrossing(PassHolderRefreshedNotEvicted|NoPassEvictedViaTombstone)" test/fuzz/AfKingSubscription.t.sol` returns ≥ 2 lines.
7. **G8 byte-presence assertion deleted**: `grep -nE "G8" test/gas/KeeperLeversAndPacking.t.sol` returns 0 lines.
8. **OPEN_BATCH placeholder present**: `grep -nE "OPEN_BATCH" test/gas/RouterWorstCaseGas.t.sol` returns ≥ 1 line; the surrounding text contains a TODO citing Plan 335-06.
9. **`test/gas/KeeperOpenBoxWorstCaseGas.t.sol` is UNTOUCHED**: `git diff test/gas/KeeperOpenBoxWorstCaseGas.t.sol` returns empty (no changes in this plan; Plan 335-06 just re-runs it).
10. **`hasAnyLazyPass` PRESERVED on the contract side**: `grep -n "function hasAnyLazyPass" contracts/DegenerusGame.sol` STILL returns the original line (Plan 335-01 Task 2 preserved it; this plan only migrates AfKing-specific test usages).
</verification>

<success_criteria>
- All 7 test files migrated per their per-file rule sets.
- System-wide grep: `burnForKeeper`, `paidThroughDay`, `OPEN_NORMAL_GAS_UNIT`, `_activateWhalePass` all return 0 in `test/`.
- `validThroughLevel`, `lazyPassHorizon`, `SubscriptionExpired(player, 1)` asserted on the new EVICT paths; OPEN-E 4-protection assertions preserved.
- VAULT/SDGNRS first-crossing-eviction handling decided + recorded in SUMMARY.
- `test/gas/KeeperOpenBoxWorstCaseGas.t.sol` UNTOUCHED (Plan 335-06 re-runs it).
- `OPEN_BATCH` placeholder + TODO cite Plan 335-06.
- The integration oracle (`forge build` green) is asserted by Plan 335-06.
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-05-SUMMARY.md` when done. The SUMMARY MUST record:
- Per-file before/after counts of the migrated symbols (paidThroughDay/burnForKeeper/hasAnyLazyPass/OPEN_NORMAL_GAS_UNIT/_activateWhalePass).
- The VAULT/SDGNRS first-crossing-eviction handling choice (re-subscribe in fixture OR accept eviction as expected behavior) — with the test function name(s) that codify the choice.
- The pass-eviction-vs-fundingSource assertion result (Plan 335-04 Task 3's `delete _subOf[player]` vs. `setDailyQuantity(0)`-preserves-fundingSource — assert what the code actually does).
- The OPEN_BATCH placeholder value used in `test/gas/RouterWorstCaseGas.t.sol` and the TODO comment text linking to Plan 335-06.
- A grep-attestation block confirming all 10 plan-level verification gates pass.
- A statement that the deeper RNG-freeze fuzz of the deferred-claim path is deferred to 336/TST-01 freeze leg.
</output>
