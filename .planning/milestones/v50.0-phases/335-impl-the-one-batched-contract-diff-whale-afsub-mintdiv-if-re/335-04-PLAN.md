---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 04
type: execute
wave: 2
depends_on: [335-01]
files_modified:
  - contracts/AfKing.sol
  - contracts/BurnieCoin.sol
autonomous: true
worktree: false
requirements: [AFSUB-01, AFSUB-02, AFSUB-03, AFSUB-04, AFSUB-05]
tags: [contracts, afking, burniecoin, afsub, pass-gated, open-e, tombstone, swap-pop]

must_haves:
  truths:
    - "`burnForKeeper` is gone from `AfKing.sol` (iface decl + 2 call sites) AND from `BurnieCoin.sol` (impl + event + modifier + bound error)."
    - "`paidThroughDay` and `WINDOW_DAYS` are gone from `AfKing.sol`; the `Sub` slot offset 5 now holds `validThroughLevel`."
    - "`FLAG_WINDOW_PAID` (bit 0 of `Sub.flags`) is freed; no future-proofing reservation."
    - "`subscribe` encodes `validThroughLevel` from a SINGLE `IGame(GAME).lazyPassHorizon(subscriber)` call; no BURNIE charge at subscribe."
    - "`_autoBuy` performs ZERO external pass reads per iteration on the non-crossing path (the `currentLevel <= sub.validThroughLevel` check is a pure stored-field compare)."
    - "At the crossing (`currentLevel > sub.validThroughLevel`), `lazyPassHorizon(player)` is read EXACTLY ONCE — refresh (`sub.validThroughLevel = h`) if still covered, else evict via the existing tombstone-then-reclaim pattern (`dailyQuantity = 0` + `_removeFromSet` + `continue` without advancing cursor)."
    - "OPEN-E `fundingSource` (offset 11), the OPENE-04 gate (`:393-403`), and the SUB-02 self-consent gate (`:385-391`) are UNTOUCHED — the 4 structural protections survive by construction."
    - "The v49 swap-pop reclaim at `_autoBuy:605-617` is UNTOUCHED structurally; eviction routes through `setDailyQuantity(0)`-style tombstone semantics (Pitfall P6 — H-CANCEL-SWAP-MISS class does NOT reproduce)."
    - "`AfKing.sol`'s `IGame` iface declaration (:18–43) exposes the new `lazyPassHorizon(address) external view returns (uint24)` member."
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "Pass-gated subscription model — subscribe encodes horizon; per-iter is stored-field compare; crossing reads horizon ONCE → refresh-or-evict; OPEN-E + tombstone preserved."
    - path: "contracts/BurnieCoin.sol"
      provides: "Deletion of dead `burnForKeeper` surface (impl + event + modifier + error)."
  key_links:
    - from: "contracts/AfKing.sol::subscribe"
      to: "contracts/DegenerusGame.sol::lazyPassHorizon"
      via: "IGame(GAME).lazyPassHorizon(subscriber) at subscribe-time (single read)"
      pattern: "lazyPassHorizon\\("
    - from: "contracts/AfKing.sol::_autoBuy crossing branch"
      to: "contracts/DegenerusGame.sol::lazyPassHorizon"
      via: "IGame(GAME).lazyPassHorizon(player) at crossing only (exactly once per crossing — NOT per-iter)"
      pattern: "lazyPassHorizon\\("
    - from: "contracts/AfKing.sol::_autoBuy eviction"
      to: "contracts/AfKing.sol::setDailyQuantity (0) / _autoBuy:605-617 reclaim"
      via: "the existing tombstone-then-reclaim pattern (Pitfall P6 / SUB-07 + v49 swap-pop preservation)"
      pattern: "sub\\.dailyQuantity = 0"
---

<objective>
Execute Step 5 of the SPEC-locked edit-order map: the AFSUB cluster — delete `burnForKeeper`/`paidThroughDay`/`WINDOW_DAYS`/`FLAG_WINDOW_PAID` from `AfKing.sol`; repurpose `Sub` slot offset 5 → `validThroughLevel`; rewrite `subscribe` to encode the horizon from `lazyPassHorizon` (Plan 335-01's producer); rewrite `_autoBuy:630` per-iter check to a stored-field compare; rewrite the crossing to read `lazyPassHorizon` EXACTLY ONCE and refresh-or-evict through the existing tombstone-then-reclaim pattern; PRESERVE OPEN-E (4 protections) + the v49 swap-pop / SUB-07 cancel-tombstone invariants. Concurrently delete the now-dead `burnForKeeper` impl + event + modifier + bound error in `BurnieCoin.sol`.

This delivers ALL FIVE AFSUB requirements:
- **AFSUB-01** — BURNIE window REMOVED (`burnForKeeper` deleted in BOTH files; `paidThroughDay` + `WINDOW_DAYS` deleted).
- **AFSUB-02** — PASS-GATED with stored-field compare (the cheap per-iter `currentLevel <= sub.validThroughLevel` shape; NO per-iter external pass SLOAD on the non-crossing path; GASOPT-05 preserved).
- **AFSUB-03** — Refresh-or-evict at the crossing; `lazyPassHorizon` read EXACTLY ONCE; not an unconditional kick.
- **AFSUB-04** — OPEN-E `fundingSource` + 4 structural protections preserved by construction (UNTOUCHED).
- **AFSUB-05** — SUB-07 cancel-tombstone + v49 swap-pop membership invariant preserved; pass-eviction routes through the existing tombstone path (Pitfall P6 — H-CANCEL-SWAP-MISS class never reproduces).

Purpose: this is the largest blast-radius plan in the diff. Depends on Plan 335-01 because the AfKing edits CONSUME the `lazyPassHorizon` view that Plan 335-01 produces on `DegenerusGame.sol`. Within this plan, the within-cluster ordering (AfKing-first vs BurnieCoin-first) is Claude's Discretion per D-09; this plan prefers AfKing-deletions-first for reviewer clarity (so no intermediate state has AfKing referencing a `burnForKeeper` BurnieCoin doesn't implement, though the atomic-diff property makes either order safe).

Output: edits to `contracts/AfKing.sol` (large blast radius) + `contracts/BurnieCoin.sol` (small deletion). The full-suite regression — including the pass-eviction tombstone test and the OPEN-E re-attest — lives at Plan 335-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-PLAN.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md

<interfaces>
<!-- The AFSUB cluster is in-place repurpose, NOT new infra. PATTERNS §2.5 + §2.6. -->

From contracts/AfKing.sol:18-43 (the IGame iface declaration AfKing reads through — ADD `lazyPassHorizon`):
- Pre-edit: declares whatever `IGame` members AfKing currently consumes (e.g. `hasAnyLazyPass(address)`, `level()` if exposed, `subscribe`-time check helpers).
- POST-EDIT: ADD `function lazyPassHorizon(address player) external view returns (uint24);` member. Verify whether `level()` is already exposed on this iface (it should be — the per-iter check needs it). If `level()` is NOT on the iface, ADD it too (`function level() external view returns (uint24);`) — but only if grep confirms it is missing. The PATTERNS doc says "hoist a single read at autoBuy entry" — the read needs an iface method.

From contracts/AfKing.sol:57 (DELETE):
- `IBurnie internal immutable burnForKeeper;` (or whatever the exact decl is — verify; the iface decl line for the `burnForKeeper(address,uint256) external returns (uint256)` member must also be deleted from any `interface IBurnie { ... }` block in the same file; check the imports / iface decl section above the contract body).

From contracts/AfKing.sol:79-92 (PRE-EDIT `Sub` struct):
- Pre-edit layout (paraphrased from PATTERNS §2.5):
  ```
  struct Sub {
      uint8 dailyQuantity;       // offset 0
      uint32 lastAutoBoughtDay;  // offset 1
      uint32 paidThroughDay;     // offset 5  ← REPURPOSE → validThroughLevel
      uint8 reinvestPct;         // offset 9
      uint8 flags;               // offset 10  (bit 0 = FLAG_WINDOW_PAID — FREED)
      address fundingSource;     // offset 11  (UNTOUCHED — OPEN-E preservation)
  }
  ```
- POST-EDIT: rename `paidThroughDay` → `validThroughLevel`. Width = `uint32` (zero packing churn) or `uint24` (mirrors `level`'s width) — Claude's Discretion per 335-CONTEXT.md. **Recommendation: keep `uint32`** (zero packing churn vs the existing layout, no codegen-shape disruption, no slot churn for the existing tests at `test/gas/KeeperLeversAndPacking.t.sol:229` which the migration in Plan 335-05 retargets to the new field name). If the planner picks `uint24`, the `lazyPassHorizon` view's `uint24` return is a clean assignment; if `uint32`, the assignment cast is `uint32(IGame(GAME).lazyPassHorizon(...))` — both shapes are coherent.

From contracts/AfKing.sol:220 (DELETE):
- `uint32 internal constant WINDOW_DAYS = 30;`

From contracts/AfKing.sol:239 (DELETE):
- `uint8 internal constant FLAG_WINDOW_PAID = 1;` (bit 0 freed; do NOT reserve for future-proofing per `feedback_frozen_contracts_no_future_proofing`).

From contracts/AfKing.sol:374-444 (`subscribe` body — REWRITE the pass-OR-pay block):
- Pre-edit at `:414-416` (the anchor math), `:424` (paidThroughDay write), `:430-443` (the SUB-01 pass-OR-pay gate with `hasAnyLazyPass` at `:432` and `burnForKeeper` at `:437`) — ALL DELETED.
- KEEP `:385-391` (SUB-02 self-consent), `:393-403` (OPENE-04 fundingSource consent gate).
- REPLACE with single horizon encode: `s.validThroughLevel = IGame(GAME).lazyPassHorizon(subscriber);` (or `= uint32(IGame(GAME).lazyPassHorizon(subscriber));` if uint32 kept).

From contracts/AfKing.sol:458-468 (PRESERVED — `setDailyQuantity` tombstone):
- The `if (q == 0) { s.dailyQuantity = 0; ... return; }` early-out STAYS UNCHANGED — this is the in-place tombstone the AFSUB eviction MUST route through (Pitfall P6).

From contracts/AfKing.sol:605-617 (PRESERVED structurally — the in-autoBuy reclaim):
- The `if (sub.dailyQuantity == 0) { ... _removeFromSet(player); continue; }` block stays — this is the swap-pop reclaim. NOTE the `preservePaidWindow` branch references `FLAG_WINDOW_PAID` + `paidThroughDay`; these are GONE post-AFSUB. PATTERNS §2.5 records this as a decide-AT-IMPL choice: either DROP the `preservePaidWindow` branch entirely (every cancel = full delete) OR repurpose against `validThroughLevel` (`currentLevel <= sub.validThroughLevel` means "preserve").
  - **Recommendation: DROP the branch entirely.** Rationale: under AFSUB, "preserve the window" loses its meaning — there is no time-based prepaid window any more. A still-valid pass-holder at the moment of cancel-tombstone trigger has `validThroughLevel >= currentLevel` BY THE TIME the crossing branch fires next; the path is "evict via tombstone → re-subscribe to renew." The `preservePaidWindow` semantics were a refund of unused BURNIE; with BURNIE gone (AFSUB-01), there is nothing to preserve. DROP the branch; `if (sub.dailyQuantity == 0) { delete _subOf[player]; _removeFromSet(player); emit SubscriptionExpired(player, 2); didWork = true; unchecked { ++processed; } continue; }`.

From contracts/AfKing.sol:619-665 (`_autoBuy` per-iter validity + crossing — REWRITE):
- Pre-edit at `:630` (the `if (sub.paidThroughDay <= today)` check) — REPLACE with `if (currentLevel > sub.validThroughLevel)`.
- Pre-edit at `:631` (the inner `hasAnyLazyPass` check) — REPLACE with `lazyPassHorizon` read (the SINGLE external read on the crossing path).
- Pre-edit at `:633` (the free-extend write `sub.paidThroughDay = today + WINDOW_DAYS;`) — REPLACE with `sub.validThroughLevel = h;` (where `h` is the freshly-read horizon).
- Pre-edit at `:637-664` (the entire PAID branch — `burnForKeeper` call, shortfall handling, `BurnieAutoExtracted` emit) — DELETE.
- The EVICT branch replaces the shortfall path: write `sub.dailyQuantity = 0;`, call `_removeFromSet(player)`, emit `SubscriptionExpired(player, 1)`, `++processed`, `continue` (WITHOUT advancing cursor — preserves swap-pop semantics).

From contracts/AfKing.sol (autoBuy entry — HOIST the level read):
- BEFORE the per-iter loop, read `uint24 currentLevel = IGame(GAME).level();` ONCE per autoBuy call. The per-iter check then becomes a pure stored-field compare with no SLOAD on the non-crossing path beyond the `sub` struct already loaded. NOTE: if the iface does not expose `level()`, ADD it to the iface decl at `:18-43` AND verify the `DegenerusGame.sol` contract actually exposes a `level()` external view (the storage layout has `uint24 level;` at the top of `DegenerusGameStorage.sol`; the facade typically has an auto-generated getter — confirm by grep `grep -n "level()" contracts/DegenerusGame.sol`).

From contracts/BurnieCoin.sol:82-85 (DELETE — event):
- `event KeeperBurn(address indexed user, uint256 amountBurned);`

From contracts/BurnieCoin.sol:472-488 (DELETE — impl):
- `function burnForKeeper(address user, uint256 amount) external onlyAfKing returns (uint256 burned) { ... }`

From contracts/BurnieCoin.sol:549-552 (DELETE — modifier):
- `modifier onlyAfKing() { if (msg.sender != ContractAddresses.AF_KING) revert OnlyAfKing(); _; }`

From contracts/BurnieCoin.sol (DELETE — error decl):
- `error OnlyAfKing();` (search the errors block at the top of the contract).

From contracts/BurnieCoin.sol:533-545 (PRESERVED — sibling modifiers):
- `modifier onlyGame()` and `modifier onlyVault()` STAY UNCHANGED. The deletion symmetry: `onlyAfKing` matches `onlyGame`/`onlyVault`'s shape — drop the third member.

**Decision recorded for the SUMMARY (D-09 Claude's Discretion):** within-cluster order is AfKing-first (delete the `IBurnie.burnForKeeper` iface decl + the 2 call sites at `:437`/`:641` BEFORE deleting the BurnieCoin impl). This is the producer-before-consumer narrative preferred for reviewer clarity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AfKing storage + constants — delete dead surfaces (burnForKeeper iface, WINDOW_DAYS, FLAG_WINDOW_PAID) + repurpose Sub.paidThroughDay → validThroughLevel</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (lines 1–100 — imports, iface decls, contract header, `Sub` struct at `:79-92`)
    - contracts/AfKing.sol (lines 200–260 — `WINDOW_DAYS:220`, `FLAG_WINDOW_PAID:239`, surrounding constants block; also `_autoBuyCursor:214` UNTOUCHED)
    - contracts/AfKing.sol (lines 50–60 — `IBurnie burnForKeeper:57` decl + any `interface IBurnie` block above the contract that declares the `burnForKeeper(address,uint256) returns (uint256)` member)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.5 "Constants/iface deletions" + Pitfall P5 + P6)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md (§2 Sub layout repurpose; §5.1 deletions)
  </read_first>
  <action>
    Edit `contracts/AfKing.sol`:

    (a) **Delete the `burnForKeeper` iface decl at `:57`** — the storage variable holding the IBurnie reference for the BURNIE-funding sink. If there is also an inline `interface IBurnie { function burnForKeeper(address,uint256) external returns (uint256 burned); ... }` block in the imports/iface section, REMOVE the `burnForKeeper` member from that interface (KEEP other IBurnie members that the contract still uses — e.g. balance/transfer/mint helpers used elsewhere; do NOT delete the entire IBurnie iface block).

    (b) **Delete `:220`**: `uint32 internal constant WINDOW_DAYS = 30;`.

    (c) **Delete `:239`**: `uint8 internal constant FLAG_WINDOW_PAID = 1;`. Bit 0 of `Sub.flags` is FREED — do NOT reserve it for future use (`feedback_frozen_contracts_no_future_proofing`).

    (d) **Repurpose `Sub` struct at `:79-92`**: rename the field at offset 5 from `paidThroughDay` to `validThroughLevel`. Recommended width: `uint32` (zero packing churn — keeps the existing slot layout byte-identical at the struct level, no shift in subsequent field offsets). If the planner picks `uint24`, document the choice in the SUMMARY and verify the `test/gas/KeeperLeversAndPacking.t.sol:229` packing oracle migration in Plan 335-05 matches.

    (e) **ADD `lazyPassHorizon` to the IGame iface declaration at `:18-43`**: insert `function lazyPassHorizon(address player) external view returns (uint24);` as a new member. Also VERIFY `function level() external view returns (uint24);` is on the iface — if NOT, ADD it (the per-iter check needs to call `IGame(GAME).level()` once per autoBuy call; if `level()` is missing from the iface, the codegen will not produce the call site).

    Constraints:
    - DO NOT delete any other IBurnie member (only the `burnForKeeper` decl is dead post-AFSUB).
    - DO NOT touch the `_subOf` mapping decl, the `_subscriberIndex` mapping decl, the `_autoBuyCursor` decl, or any other Sub-adjacent state. The struct rename is in-place; the surrounding state is untouched.
    - The `Sub.fundingSource` field at offset 11 (OPEN-E preservation per AFSUB-04) is UNTOUCHED.
    - If the file has a `using SafeCast for uint32 paidThroughDay;` or any `paidThroughDay`-named library member, update those references too.
  </action>
  <verify>
    <automated>grep -nE "WINDOW_DAYS|FLAG_WINDOW_PAID|burnForKeeper|paidThroughDay" contracts/AfKing.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "WINDOW_DAYS" contracts/AfKing.sol` returns 0 lines.
    - `grep -nE "FLAG_WINDOW_PAID" contracts/AfKing.sol` returns 0 lines.
    - `grep -nE "burnForKeeper" contracts/AfKing.sol` returns 0 lines (the storage decl at `:57` + the iface member are both gone).
    - `grep -nE "paidThroughDay" contracts/AfKing.sol` returns 0 lines (the struct field is renamed to `validThroughLevel`).
    - `grep -nE "validThroughLevel" contracts/AfKing.sol` returns ≥ 1 line (the new `Sub` field — Tasks 2 + 3 add more references).
    - `grep -nE "function lazyPassHorizon\(address[^)]*\) external view returns \(uint24\);" contracts/AfKing.sol` returns exactly 1 line (the iface member).
    - `grep -nE "function level\(\) external view returns \(uint24\);" contracts/AfKing.sol` returns ≥ 1 line (either pre-existing on iface or newly added).
    - `grep -nE "address fundingSource" contracts/AfKing.sol` STILL returns the existing `Sub` field decl at offset 11 (OPEN-E preservation).
  </acceptance_criteria>
  <done>The dead constants + the `burnForKeeper` iface decl are gone; `Sub.paidThroughDay` is renamed to `validThroughLevel`; `lazyPassHorizon` (and `level()` if needed) is exposed on the IGame iface; OPEN-E `fundingSource` field preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AfKing.subscribe — replace the SUB-01 pass-OR-pay block with a single horizon encode</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (lines 374–445 — `subscribe` body end-to-end; pay particular attention to `:385-391` SUB-02 self-consent (PRESERVE), `:393-403` OPENE-04 (PRESERVE), `:414-416` anchor math (DELETE), `:424` paidThroughDay write (DELETE), `:430-443` SUB-01 pass-OR-pay (DELETE wholesale))
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.5 — "Subscribe-time encode pattern" subsection)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md (§4 — subscribe encoding + §6.1 OPEN-E preservation criteria)
    - memory open-e-operator-approval-trust-boundary.md (referenced in CLAUDE memory — the 4 protections that MUST hold)
  </read_first>
  <action>
    Edit `subscribe` in `contracts/AfKing.sol:374-444`:

    (a) **KEEP UNTOUCHED**:
        - `:385-391` (SUB-02 self-consent — `if (msg.sender != subscriber && !approval[subscriber][msg.sender]) revert`).
        - `:393-403` (OPENE-04 — `fundingSource` consent gate — the 4 structural protections live here).
        - The `Sub storage s = _subOf[subscriber];` (or equivalent) reference.
        - The `dailyQuantity`, `reinvestPct`, `flags` (bits 1+2) assignments — these stay (the only flag bit deleted is bit 0 `FLAG_WINDOW_PAID`).
        - The `_addToSet(subscriber)` / membership-set insertion call (this is the v49 swap-pop infra).
        - The `SubscriptionUpdated` event emit (or whatever event is fired at the end of `subscribe`).

    (b) **DELETE**:
        - `:414-416` — the anchor math computing `paidThroughDay` from `today + WINDOW_DAYS` or similar.
        - `:424` — the `s.paidThroughDay = ...;` write.
        - `:430-443` — the entire SUB-01 pass-OR-pay block (the `if (IGame(GAME).hasAnyLazyPass(subscriber)) { ... s.flags |= FLAG_WINDOW_PAID; } else { ... burnForKeeper call ... }` shape). Note that this block also includes the subscribe-time `hasAnyLazyPass` read at `:432` and the subscribe-time `burnForKeeper` call at `:437` — both subsumed by the deletion of the entire block.

    (c) **REPLACE** the deleted block with a single horizon encode:
        - `s.validThroughLevel = uint32(IGame(GAME).lazyPassHorizon(subscriber));` (or just `IGame(GAME).lazyPassHorizon(subscriber)` if `Sub.validThroughLevel` is `uint24`).
        - NO BURNIE charge at subscribe (AFSUB-01).
        - NO `FLAG_WINDOW_PAID` write (bit 0 freed).

    (d) **VERIFY post-edit position**: the new horizon encode MUST be placed AFTER the OPENE-04 consent gate (`:393-403`) so a still-pending revert preempts the storage write. Position rule: after all consent gates, before the final event emit.

    Constraints:
    - The `subscribe` function signature MUST be unchanged.
    - The set-of-subscribers membership add (`_addToSet`) MUST still fire.
    - The function MUST still revert under the original pre-conditions (already-subscribed, zero daily quantity, consent gates) — these are upstream of the SUB-01 block being deleted.
    - **SUB-09 deploy-time self-subscribes (VAULT + SDGNRS)** — these subscriptions occur at fixture/deploy time (PATTERNS §2.7 pitfall guard). The new horizon encode will set their `validThroughLevel` to whatever `lazyPassHorizon(VAULT)` / `lazyPassHorizon(SDGNRS)` returns (likely `0` since they hold no passes). On their first `_autoBuy` crossing, they will be evicted by the tombstone path. This is the OPENE-04 / two-tier funding-skip invariant the test fixture in Plan 335-05 must re-attest. The SUMMARY MUST record this behavior change explicitly.
  </action>
  <verify>
    <automated>grep -nE "s\.validThroughLevel = " contracts/AfKing.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "s\.validThroughLevel = " contracts/AfKing.sol` returns ≥ 1 line (the new horizon encode).
    - `grep -nE "lazyPassHorizon\(subscriber\)" contracts/AfKing.sol` returns exactly 1 line (the subscribe-time read — Task 3 adds the crossing-time read for the `player` arg).
    - `grep -nE "hasAnyLazyPass" contracts/AfKing.sol` returns 0 lines (subscribe-time `:432` + day-31 `:631` references both gone — the latter via Task 3).
    - `grep -nE "WINDOW_DAYS" contracts/AfKing.sol` returns 0 lines (Task 1 deleted the constant; this task confirms `subscribe` has no surviving reference).
    - `grep -nE "FLAG_WINDOW_PAID" contracts/AfKing.sol` returns 0 lines (no subscribe-time set).
    - `grep -nE "s\.paidThroughDay" contracts/AfKing.sol` returns 0 lines.
    - The SUB-02 self-consent gate (`:385-391` region) STILL contains a `revert` on non-self / non-approved msg.sender — confirmed by `grep -nE "approval\[subscriber\]" contracts/AfKing.sol` returning ≥ 1 line.
    - The OPENE-04 gate (`:393-403` region) STILL contains the `fundingSource` consent logic — confirmed by `grep -nE "fundingSource" contracts/AfKing.sol` returning ≥ 2 lines (the field decl + the gate read; Task 3's eviction may reference it too but does not modify).
  </acceptance_criteria>
  <done>`subscribe` replaces the pass-OR-pay block with a single `validThroughLevel = lazyPassHorizon(subscriber)` write; the two PRESERVED consent gates (SUB-02 + OPENE-04) are intact; the BURNIE charge is removed; bit 0 of `flags` is not set.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AfKing._autoBuy — hoist currentLevel, rewrite per-iter check, rewrite crossing refresh-or-evict</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (lines 600–670 — `_autoBuy` end-to-end: entry preamble, the `:605-617` swap-pop reclaim, the `:619-624` already-bought-today skip, the `:630-665` per-iter validity + day-31 branch)
    - contracts/AfKing.sol (lines 458–470 — `setDailyQuantity` tombstone — UNTOUCHED but referenced by the eviction action shape)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.5 "Settled rewrite" + §3.3 stored-field-compare + §3.4 tombstone-then-reclaim + Pitfalls P5, P6)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md (§4 — refresh-or-evict + §6.2/6.3 SUB-07 + swap-pop preservation)
    - memory afking-cancel-tombstone-streak-finding.md (the H-CANCEL-SWAP-MISS class — pass-eviction MUST NOT reproduce)
  </read_first>
  <action>
    Edit `_autoBuy` in `contracts/AfKing.sol` (around lines 600-665):

    (a) **Hoist the level read** at the autoBuy entry (BEFORE the per-iter loop begins): insert `uint24 currentLevel = IGame(ContractAddresses.GAME).level();` ONCE per autoBuy call. Position: after the function's preamble (parameter validation, cursor read) and BEFORE the `while (processed < maxCount && cursor < ...)` loop. This SINGLE SLOAD-equivalent is the per-autoBuy-call cost; per-iter the check is a pure stored-field compare.

    (b) **PRESERVE STRUCTURALLY** `:605-617` (the in-autoBuy swap-pop reclaim — `if (sub.dailyQuantity == 0) { ... _removeFromSet(player); continue; }` without advancing cursor). Modify per the decision recorded in `<interfaces>` above:
        - **DROP** the `preservePaidWindow` branch entirely (it referenced `FLAG_WINDOW_PAID` + `paidThroughDay`, both gone post-AFSUB).
        - The simplified block:
          ```
          if (sub.dailyQuantity == 0) {
              delete _subOf[player];
              _removeFromSet(player);
              emit SubscriptionExpired(player, 2);   // reason code 2 = cancel-tombstone
              didWork = true;
              unchecked { ++processed; }
              continue;
          }
          ```
        - The `continue` MUST be WITHOUT cursor advance (the swap-pop occupant is processed in this same slot) — Pitfall P6 / SUB-07 invariant.

    (c) **PRESERVE** `:619-624` already-bought-today skip:
        ```
        if (sub.lastAutoBoughtDay >= today) {
            emit PlayerSkipped(player, 2);
            unchecked { ++cursor; ++processed; }
            continue;
        }
        ```

    (d) **REWRITE the per-iter check + crossing** at `:630-665`:
        ```
        if (currentLevel > sub.validThroughLevel) {
            // CROSSING — re-read horizon EXACTLY ONCE
            uint24 h = IGame(ContractAddresses.GAME).lazyPassHorizon(player);
            if (currentLevel <= h) {
                // REFRESH (still covered)
                sub.validThroughLevel = uint32(h);   // or `= h;` if validThroughLevel is uint24
                emit SubscriptionExtendedFree(player, today);
                didWork = true;
            } else {
                // EVICT — via tombstone-then-reclaim shape (Pitfall P6 — H-CANCEL-SWAP-MISS preservation)
                sub.dailyQuantity = 0;
                _removeFromSet(player);
                emit SubscriptionExpired(player, 1);  // reason code 1 = pass-eviction (replaces burnForKeeper shortfall code 1)
                didWork = true;
                unchecked { ++processed; }
                continue;     // WITHOUT advancing cursor — swap-pop occupant gets processed this slot
            }
        }
        // OTHERWISE: currentLevel <= sub.validThroughLevel — proceed with the buy unchanged below
        ```

    (e) **DELETE** the entire `:637-664` PAID branch (the `burnForKeeper` call, the shortfall handling, the `BurnieAutoExtracted` event emit). The new EVICT branch in (d) replaces it.

    (f) **KEEP** the per-iter buy logic AFTER the new crossing block (whatever the `_autoBuy` does to actually purchase a ticket / open a box for the player when validity passes) — UNTOUCHED.

    Constraints:
    - The per-iter check on the NON-crossing path MUST be a pure stored-field compare (`currentLevel > sub.validThroughLevel`) — NO external SLOAD beyond the `sub` struct already loaded. This is GASOPT-05 preservation (Pitfall P5 + §3.3 of PATTERNS).
    - `lazyPassHorizon(player)` is read EXACTLY ONCE per crossing iteration. NEVER per-iter on the non-crossing path.
    - The EVICT branch MUST route through `sub.dailyQuantity = 0` + `_removeFromSet` + `continue` WITHOUT cursor advance (Pitfall P6 — the v49 swap-pop reclaim semantics). A direct `_removeFromSet` mid-sweep WITHOUT the tombstone write re-opens H-CANCEL-SWAP-MISS.
    - DO NOT add a `refreshPass()` external entrypoint (Pitfall P5 / D-10). The crossing IS the refresh path; lazy-only.
    - The `SubscriptionExpired(player, reason)` reason codes: `1` = pass-eviction (was `1` = burnForKeeper shortfall — same code, repurposed); `2` = cancel-tombstone (unchanged from pre-edit). Both code constants are preserved if defined elsewhere.
    - The `SubscriptionExtendedFree(player, today)` event name: PRESERVED from pre-edit (was the free-extend on pass-holder branch). Planner's discretion to rename to `SubscriptionRefreshed` for clarity — if renamed, ALL test files in Plan 335-05 must be updated to the new sig.
  </action>
  <verify>
    <automated>grep -nE "if \(currentLevel > sub\.validThroughLevel\)" contracts/AfKing.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "uint24 currentLevel = IGame\(ContractAddresses\.GAME\)\.level\(\)" contracts/AfKing.sol` returns ≥ 1 line (the hoisted SLOAD).
    - `grep -nE "if \(currentLevel > sub\.validThroughLevel\)" contracts/AfKing.sol` returns exactly 1 line (the per-iter check — the GASOPT-05 stored-field compare).
    - `grep -nE "lazyPassHorizon\(player\)" contracts/AfKing.sol` returns exactly 1 line (the crossing-time read — exactly ONCE per crossing iteration).
    - `grep -nE "lazyPassHorizon\(" contracts/AfKing.sol` returns ≥ 3 lines total: 1 in iface decl (Task 1) + 1 in subscribe (Task 2) + 1 in crossing (this task). NOT 4+ (a 4th would mean a per-iter read snuck in — Pitfall P5/Pitfall break).
    - `grep -nE "burnForKeeper" contracts/AfKing.sol` returns 0 lines (the day-31 PAID branch is gone).
    - `grep -nE "BurnieAutoExtracted" contracts/AfKing.sol` returns 0 lines (the PAID-branch event is gone).
    - `grep -nE "sub\.dailyQuantity = 0" contracts/AfKing.sol` returns ≥ 2 lines (the swap-pop reclaim at `:605` PLUS the new EVICT branch).
    - `grep -nE "_removeFromSet\(player\)" contracts/AfKing.sol` returns ≥ 2 lines (preserved + new EVICT).
    - `grep -nE "SubscriptionExpired\(player, 1\)" contracts/AfKing.sol` returns ≥ 1 line (the EVICT branch).
    - `grep -nE "SubscriptionExpired\(player, 2\)" contracts/AfKing.sol` returns ≥ 1 line (the cancel-tombstone reclaim at `:605`).
    - `grep -nE "SubscriptionExtendedFree" contracts/AfKing.sol` returns ≥ 1 line (the REFRESH branch — or `SubscriptionRefreshed` if renamed).
  </acceptance_criteria>
  <done>`_autoBuy` hoists `currentLevel` once; per-iter check is the stored-field compare; crossing reads `lazyPassHorizon` ONCE → refresh-or-evict; EVICT routes through the tombstone-then-reclaim pattern (Pitfall P6); the PAID branch is gone; GASOPT-05 stored-field-compare invariant preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: BurnieCoin — delete burnForKeeper impl + KeeperBurn event + onlyAfKing modifier + OnlyAfKing error</name>
  <files>contracts/BurnieCoin.sol</files>
  <read_first>
    - contracts/BurnieCoin.sol (lines 1–100 — header, imports, errors block, events block at `:82-85`)
    - contracts/BurnieCoin.sol (lines 460–500 — the `burnForKeeper` impl at `:472-488`)
    - contracts/BurnieCoin.sol (lines 530–560 — the modifier triad `onlyGame:535` / `onlyVault:542` / `onlyAfKing:549`)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.6 Step 5b — deletion symmetry)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md (§5.2 — confirmed `burnForKeeper` is the SOLE user of `onlyAfKing`)
  </read_first>
  <action>
    Delete four surfaces from `contracts/BurnieCoin.sol`:

    (a) **DELETE the impl at `:472-488`** — the entire `function burnForKeeper(address user, uint256 amount) external onlyAfKing returns (uint256 burned) { ... }` body.

    (b) **DELETE the event at `:82-85`** — `event KeeperBurn(address indexed user, uint256 amountBurned);` and any docstring directly attached.

    (c) **DELETE the modifier at `:549-552`** — the entire `modifier onlyAfKing() { if (msg.sender != ContractAddresses.AF_KING) revert OnlyAfKing(); _; }` body.

    (d) **DELETE the bound error decl** — search the errors block at the top of the contract for `error OnlyAfKing();` and remove it. If `OnlyAfKing()` is also defined in an inherited base contract or in a separate `errors.sol`, leave that decl untouched (only delete the one local to `BurnieCoin.sol`); verify with `grep -rn "error OnlyAfKing" contracts/`.

    Constraints:
    - DO NOT touch `onlyGame:535` and `onlyVault:542` modifiers (PRESERVED — deletion symmetry: drop the third member only).
    - DO NOT touch any other event in the events block.
    - DO NOT touch any other error in the errors block.
    - DO NOT touch the `ContractAddresses.AF_KING` constant reference elsewhere in the file (if BurnieCoin references AF_KING for any OTHER purpose — unlikely but verify with grep before assuming).
    - The grep verification AFTER the diff lands MUST return zero hits for `burnForKeeper` across `contracts/` (the 334-DESIGN-LOCK-AFKING §5.2 verification).
  </action>
  <verify>
    <automated>grep -rnE "burnForKeeper" contracts/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "burnForKeeper" contracts/` returns 0 lines (the function is gone from EVERY contract file — the load-bearing system-wide attestation).
    - `grep -nE "function burnForKeeper" contracts/BurnieCoin.sol` returns 0 lines.
    - `grep -nE "event KeeperBurn" contracts/BurnieCoin.sol` returns 0 lines.
    - `grep -nE "modifier onlyAfKing" contracts/BurnieCoin.sol` returns 0 lines.
    - `grep -nE "error OnlyAfKing" contracts/BurnieCoin.sol` returns 0 lines.
    - `grep -nE "modifier onlyGame" contracts/BurnieCoin.sol` returns ≥ 1 line (PRESERVED).
    - `grep -nE "modifier onlyVault" contracts/BurnieCoin.sol` returns ≥ 1 line (PRESERVED).
  </acceptance_criteria>
  <done>The dead `burnForKeeper` surface is deleted from BurnieCoin (impl + event + modifier + local error); the two sibling modifiers (`onlyGame`, `onlyVault`) are PRESERVED; the system-wide `grep burnForKeeper contracts/` returns zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscriber → `subscribe` | UNTOUCHED — SUB-02 self-consent gate (`:385-391`) STILL enforces caller-is-subscriber-or-approved; OPENE-04 (`:393-403`) STILL enforces `fundingSource` consent. The 4 OPEN-E structural protections (consent-at-subscribe / default-self byte-identical / no-escalation / trust-the-sub temporal bound) survive by construction. |
| keeper (any address) → `_autoBuy` | UNTOUCHED — public, permissionless; the per-iter check + crossing run inside the bounded cursor walk. The new EVICT branch can only delete a sub whose `validThroughLevel < currentLevel` AND whose owner does NOT hold a still-valid pass at crossing time. |
| funded subscriber (whose `fundingSource != msg.sender`) → eviction | The OPEN-E `fundingSource` field is UNTOUCHED. Pass-eviction tombstones the SUB (the subscription record), NOT the funding source — the funder's separate state is unaffected. Plan 335-05 Test 2 re-attests this. |
| `BurnieCoin.burnForKeeper` callers | GONE. The only caller was `AfKing.sol` (verified by grep in 334-DESIGN-LOCK-AFKING §5.2). System-wide post-diff: `grep burnForKeeper contracts/` returns 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-16 | Tampering (OPEN-E 4 protections regression) | `subscribe` consent gates under AFSUB rewrite | mitigate (preserved by construction) | SUB-02 (`:385-391`) and OPENE-04 (`:393-403`) consent gates are UNTOUCHED. The new horizon encode is placed AFTER these gates (Task 2 (d)). The `fundingSource` field at offset 11 is UNTOUCHED. The 4 protections (consent-at-subscribe / default-self / no-escalation / trust-the-sub temporal bound) hold by construction. Plan 335-05 Test 2 (`AfKingFundingWaterfall.t.sol` migration) empirically re-attests. |
| T-335-17 | Tampering (H-CANCEL-SWAP-MISS class regression) | `_autoBuy` pass-eviction at the crossing | mitigate (Pitfall P6 — route through tombstone) | The EVICT branch routes through `sub.dailyQuantity = 0` + `_removeFromSet` + `continue` WITHOUT cursor advance — the IDENTICAL pattern as the existing v49 swap-pop reclaim at `:605-617`. A direct mid-sweep `_removeFromSet` WITHOUT the tombstone write would relocate a pending tail behind the cursor → missed day → mint-streak reset (the H-CANCEL-SWAP-MISS class per memory `afking-cancel-tombstone-streak-finding`). The acceptance criteria assert the tombstone shape grep-attested. |
| T-335-18 | Tampering (membership ⟺ packed != 0 invariant) | `_autoBuy` and `setDailyQuantity(0)` swap-pop semantics | mitigate (preserved) | Both the cancel-tombstone reclaim (`:605-617`) and the new pass-eviction (`_autoBuy` crossing EVICT) use the same `dailyQuantity=0 + _removeFromSet + continue-without-cursor-advance` pattern. The v49 swap-pop invariant (membership ⟺ packed != 0) holds: writing `dailyQuantity=0` ≡ packed==0 ≡ membership false. Plan 335-05 Test 3 (`AfKingConcurrency.t.sol` migration) re-derives the H-CANCEL-SWAP-MISS scenario under pass-eviction and asserts non-reproduction. |
| T-335-19 | Tampering (GASOPT-05 per-iter regression) | `_autoBuy` per-iter validity check under AFSUB | mitigate (stored-field compare preserved) | The per-iter shape is `if (currentLevel > sub.validThroughLevel)` — a pure stored-field compare with NO external read on the non-crossing path. The `currentLevel` SLOAD is HOISTED once per autoBuy call (Task 3 (a)). Per-iter the shape is IDENTICAL in cost-class to the pre-edit `paidThroughDay <= today` shape — both are 1 SLOAD (the `sub` struct), 1 compare. PATTERNS §3.3 records this is the GASOPT-05 preservation pattern; the acceptance grep asserts `lazyPassHorizon(player)` appears exactly ONCE in `_autoBuy` (i.e. NOT inside the loop's non-crossing branch). |
| T-335-20 | Tampering (a "refreshPass()" entrypoint slips in) | new external surface that wasn't in the SPEC | mitigate (avoid) | **DO NOT ADD** per Pitfall P5 / D-10. The crossing IS the refresh path; lazy-only. A proactive `refreshPass()` external entrypoint widens the attack surface AND violates D-10. The acceptance criteria do NOT assert its absence by grep (it's a "what should not exist" item) but the SUMMARY MUST explicitly state no such function was added. |
| T-335-21 | Information disclosure | `validThroughLevel` is a public-readable field of `Sub` via the existing `subscriptionOf(...)` getter | accept | Pre-edit `paidThroughDay` was already publicly readable through the same `subscriptionOf` getter (used heavily in the test suite, see Plan 335-05 Tests 1-3). The semantic information disclosed (a per-sub level horizon) is derivable from the public `mintPacked_[player]` + the public `level()` getter anyway via the new `lazyPassHorizon` view; nothing newly private leaks. |
| T-335-22 | Repudiation | claim "AFSUB broke OPEN-E because the funding-source semantics changed" | mitigate (proven by construction + Test 2) | `fundingSource` field at offset 11 is UNTOUCHED (Task 1 explicitly preserved). The OPENE-04 gate at `:393-403` in `subscribe` is UNTOUCHED (Task 2 KEEP). Pass-eviction tombstones the SUB, not the funder's separate state. Plan 335-05 Test 2 migration re-attests by exercising a funded-by-OPEN-E sub whose subscriber's pass expires — assert: SUB tombstoned, fundingSource state intact, the 4 protections still pass their existing assertions. |
| T-335-23 | Denial of service | pass-holder is mass-evicted by a single autoBuy call iterating over a full set | accept (intended) | Pass-eviction is a per-iter check; a single autoBuy iterates `min(maxCount, ...)` subs. If ALL subs in the iterated range have expired passes AND none refresh at the crossing, ALL get evicted in one call. This is the intended semantics: a dead-set sweeps clean in O(N) work. Liveness is preserved (the autoBuy itself returns; the keeper bounty / next-call sweep handles cleanup). |
| T-335-24 | Tampering (BurnieCoin loses an ancillary user beyond AfKing) | `burnForKeeper` deletion is broader than intended | mitigate (system-wide grep) | The 334-DESIGN-LOCK-AFKING §5.2 grep confirmed `burnForKeeper` is the ONLY user of `onlyAfKing`. Task 4 acceptance criteria assert `grep -rn burnForKeeper contracts/` returns 0 system-wide. If a hidden user surfaces (any test file or other contract referring to `IBurnie.burnForKeeper`), the test migration in Plan 335-05 catches it via `forge build` red. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — N/A directly to AfKing/BurnieCoin (no `rngLock`-frozen-slot writes in this plan's surfaces; the AfKing keeper writes `Sub` storage which is not VRF-derived). The `lazyPassHorizon` view reads `mintPacked_` but is a read; per 334-WHALE04-FREEZE-PROOF §5 the read-side is not a freeze concern.
- **OPEN-E operator-approval trust boundary** — the 4 protections (consent-at-subscribe / default-self / no-escalation / trust-the-sub temporal bound) PRESERVED by leaving `fundingSource` field, SUB-02, and OPENE-04 untouched. Plan 335-05 Test 2 owns the empirical re-attest.
- **AFKING cancel-tombstone + swap-pop invariant** (`afking-cancel-tombstone-streak-finding`, `feedback`) — pass-eviction routes through the SAME tombstone-then-reclaim shape as the existing cancel path. Plan 335-05 Test 3 owns the empirical re-attest.
- **D-IMPL-01 gameOver-forfeit structural guard** — N/A (whale-side).
- **`_applyWhalePassStats` 3-caller invariant** — N/A (whale-side).
- **GASOPT-05 per-iter no-external-read invariant** — preserved by the hoisted `currentLevel` read + the pure stored-field compare per-iter (T-335-19).
</threat_model>

<verification>
Plan-level checks (executor MUST run after all 4 tasks):

1. **System-wide burnForKeeper purge**: `grep -rnE "burnForKeeper" contracts/` returns 0 lines.
2. **System-wide paidThroughDay purge in contracts/**: `grep -rnE "paidThroughDay" contracts/` returns 0 lines.
3. **lazyPassHorizon usage count in AfKing.sol**: `grep -cE "lazyPassHorizon\(" contracts/AfKing.sol` returns ≥ 3 (iface decl + subscribe + crossing) and ≤ 4 (allowing for one extra in a comment) — anything higher means a per-iter sneak (Pitfall P5/break).
4. **OPEN-E preservation**: `grep -nE "fundingSource" contracts/AfKing.sol` returns ≥ 3 lines (struct field + OPENE-04 gate + any reclaim references that didn't get touched). `grep -nE "approval\[subscriber\]" contracts/AfKing.sol` returns ≥ 1 line (SUB-02 preserved).
5. **Tombstone preservation**: `grep -nE "sub\.dailyQuantity = 0" contracts/AfKing.sol` returns ≥ 2 lines (existing cancel reclaim + new EVICT branch); `grep -nE "_removeFromSet\(player\)" contracts/AfKing.sol` returns ≥ 2 lines.
6. **validThroughLevel installed**: `grep -nE "validThroughLevel" contracts/AfKing.sol` returns ≥ 5 lines (struct decl + subscribe write + per-iter compare + crossing refresh write + any aux references).
7. **No refreshPass entrypoint**: `grep -nE "function refreshPass" contracts/AfKing.sol` returns 0 lines.
8. **No new external Burnie call from AfKing**: `grep -nE "IBurnie\(" contracts/AfKing.sol | grep -v "(?:KeeperBurn|burnForKeeper)"` — verify no other IBurnie member is newly added (the iface usage in AfKing may still exist for other purposes if any; just confirm `burnForKeeper` is not re-introduced).
9. **BurnieCoin sibling modifier preservation**: `grep -nE "modifier onlyGame|modifier onlyVault" contracts/BurnieCoin.sol` returns ≥ 2 lines (the deletion symmetry — drop ONLY `onlyAfKing`).
10. **AfKing IGame iface complete**: `grep -nE "function lazyPassHorizon\(address[^)]*\) external view returns \(uint24\);" contracts/AfKing.sol` returns 1 line.
</verification>

<success_criteria>
- AFSUB-01 — `burnForKeeper` deleted in BOTH `AfKing.sol` AND `BurnieCoin.sol`; `paidThroughDay` + `WINDOW_DAYS` + `FLAG_WINDOW_PAID` deleted; the BURNIE window is GONE.
- AFSUB-02 — `validThroughLevel` encoded at subscribe from `lazyPassHorizon`; per-iter check is the pure stored-field compare `currentLevel > sub.validThroughLevel`; NO per-iter external pass read (GASOPT-05 preserved).
- AFSUB-03 — Crossing reads `lazyPassHorizon` EXACTLY ONCE per iteration → refresh-or-evict (refresh sets new `validThroughLevel`; evict tombstones); not an unconditional kick.
- AFSUB-04 — OPEN-E `fundingSource` field + SUB-02 + OPENE-04 gate UNTOUCHED; the 4 structural protections preserved by construction.
- AFSUB-05 — EVICT branch routes through `dailyQuantity=0 + _removeFromSet + continue-without-cursor-advance` — the SAME tombstone-then-reclaim shape as the existing cancel path; H-CANCEL-SWAP-MISS class structurally cannot reproduce.
- NO `refreshPass()` entrypoint (Pitfall P5 / D-10).
- NO parallel BURNIE-charge resurrection.
- System-wide `grep burnForKeeper contracts/` returns 0.
- The SUB-09 deploy-time self-subscribes (VAULT + SDGNRS) will be evicted on first crossing — behavior change recorded in SUMMARY for Plan 335-05 test fixture re-attest.
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-04-SUMMARY.md` when done. The SUMMARY MUST record:
- The post-edit `Sub` struct layout (field name + width + offset for each field, confirming the `paidThroughDay → validThroughLevel` rename + the chosen width).
- The post-edit IGame iface declaration (which members are exposed, confirming `lazyPassHorizon` and `level()` are present).
- The post-edit `subscribe` body — the deleted line ranges + the single replacement horizon-encode line.
- The post-edit `_autoBuy` body around the per-iter + crossing — the hoisted `currentLevel` read, the per-iter compare, the refresh-or-evict crossing, the deleted PAID branch.
- The post-edit `:605-617` swap-pop reclaim — confirm the `preservePaidWindow` branch was DROPPED (or, if kept and repurposed against `validThroughLevel`, the rationale).
- The deleted line ranges in `BurnieCoin.sol` (impl + event + modifier + error).
- An explicit statement that NO `refreshPass()` entrypoint was added (Pitfall P5 / D-10).
- An explicit statement that the SUB-09 deploy-time self-subscribes (VAULT + SDGNRS) will tombstone on first crossing — Plan 335-05 test fixture must re-attest this expected behavior.
- A grep-attestation block confirming all 10 plan-level verification gates pass.
- The within-cluster ordering choice recorded for the diff narrative (AfKing-first per the preferred reviewer-clarity order — but the atomic-diff property means BurnieCoin-first would also be safe).
</output>
