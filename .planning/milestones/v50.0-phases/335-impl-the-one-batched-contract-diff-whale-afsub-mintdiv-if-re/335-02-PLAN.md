---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
autonomous: true
worktree: false
requirements: [WHALE-01, WHALE-02]
tags: [contracts, whale, lootbox, o1, claim, freeze-safety]

must_haves:
  truths:
    - "`_activateWhalePass` in DegenerusGameLootboxModule.sol no longer contains the inline 100-iteration `_queueTickets` loop — opens are uniform O(1)."
    - "The replacement body writes exactly one storage slot: `whalePassClaims[player] += grant;` (mirroring `PayoutUtils:52`)."
    - "The call to `_applyWhalePassStats(player, ticketStartLevel)` is REMOVED from `_activateWhalePass` (stats move to claim-time per D-04)."
    - "The bonus-band constants `WHALE_PASS_BONUS_TICKETS_PER_LEVEL` (`:207`) and `WHALE_PASS_BONUS_END_LEVEL` (`:209`) are deleted (≤10 band dropped per D-21)."
    - "The materialization endpoint for WHALE-02 is the existing `claimWhalePass(address)` at `WhaleModule:1018` — NOT a new function. The convergence story (D-20) holds."
    - "Box-open writes NO `mintPacked_` slot at open-time (D-04 / 334-WHALE04-FREEZE-PROOF)."
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Rewritten `_activateWhalePass` body (O(1) `+=` write); bonus-band constants removed."
  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol::_activateWhalePass"
      to: "contracts/storage/DegenerusGameStorage.sol::whalePassClaims (decl)"
      via: "`whalePassClaims[player] += grant` — IDENTICAL slot as the existing writer at PayoutUtils:52 and JackpotModule:1410"
      pattern: "whalePassClaims\\[(player|beneficiary|winner)\\] \\+="
    - from: "contracts/modules/DegenerusGameLootboxModule.sol::_activateWhalePass (caller emits LootboxWhalePassJackpot)"
      to: "contracts/modules/DegenerusGameWhaleModule.sol::claimWhalePass:1018"
      via: "materialization deferred to player-paid claim (D-20 / WHALE-02 convergence)"
      pattern: "claimWhalePass\\("
---

<objective>
Execute Step 3 of the SPEC-locked edit-order map: replace the inline 100-iteration `_queueTickets` whale-pass mint at box-open in `contracts/modules/DegenerusGameLootboxModule.sol:1240-1261` with an O(1) `whalePassClaims[player] += grant;` write — converging onto the existing materialization endpoint at `WhaleModule:1018` (D-20).

This delivers:
- **WHALE-01** — the box-open mint stops looping; opens become uniform O(1) regardless of whale-pass status.
- **WHALE-02** — a player-paid claim materializes the deferred mint; the entrypoint is `WhaleModule.claimWhalePass:1018` (already deployed, already permissionless-w/-beneficiary, already `_livenessTriggered`-gated, already calls `_applyWhalePassStats` then `_queueTicketRange(player, startLevel, 100, halfPasses, false)` — D-20).

Purpose: this is the load-bearing WHALE consumer plan. It writes the slot Plan 335-01 confirmed; it defers materialization to the endpoint Plan 335-01 left in place at the module-direct path. The WHALE-03 carve-out retirement (autoOpen weighting) shipped in Plan 335-01; the empirical re-confirmation of flat OPEN_BATCH lives in Plan 335-06.

Output: a partial diff under `contracts/modules/DegenerusGameLootboxModule.sol`. The file compiles in isolation after this plan (no caller of the inline-loop body remains broken — the function still returns `ticketStartLevel = level + 1` for the caller's `LootboxWhalePassJackpot` event at `:1638`).

**Freeze-safety attestation (re-cited, not re-derived):** `334-WHALE04-FREEZE-PROOF.md` VERDICT = FREEZE-SAFE. The O(1) record at box-open writes only `whalePassClaims[player]`, which the freeze-proof maps as a non-frozen pending-claim accumulator (not a VRF-influenced slot). `_applyWhalePassStats` moves to claim-time (where it is gated by `_livenessTriggered`), preserving the timing/semantics of the stats application (D-04). The two OTHER `_applyWhalePassStats` callers (`WhaleModule:1032` bundle purchase, `DecimatorModule:588` Decimator win) stay IMMEDIATE-apply — UNTOUCHED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md

<interfaces>
<!-- The CONVERGENCE story (D-20): writer-mirror + materialization-endpoint are BOTH in-repo. PATTERNS §0 and §2.3. -->

From contracts/modules/DegenerusGamePayoutUtils.sol:45-61 (THE writer-mirror analog A2 — IDENTICAL storage slot):
```
function _queueWhalePassClaimCore(address winner, uint256 amount) internal {
    if (winner == address(0) || amount == 0) return;
    uint256 fullHalfPasses = amount / HALF_WHALE_PASS_PRICE;
    uint256 remainder = amount - (fullHalfPasses * HALF_WHALE_PASS_PRICE);
    if (fullHalfPasses != 0) {
        whalePassClaims[winner] += fullHalfPasses;     // :52  ← THE LINE TO MIRROR
    }
    if (remainder != 0) { ... }
}
```

From contracts/modules/DegenerusGameJackpotModule.sol:1410 (second `+=` writer, confirms the pattern is established):
- `whalePassClaims[winner] += whalePassCount;`

From contracts/modules/DegenerusGameWhaleModule.sol:1018 (the EXISTING materialization endpoint — D-20):
- `function claimWhalePass(address player) external` — permissionless-w/-beneficiary; `_livenessTriggered`-gated revert at :1019; `level+1`-anchored at the read; zeroes `whalePassClaims[player]` before applying; calls `_applyWhalePassStats(player, startLevel)`; queues `_queueTicketRange(player, startLevel, 100, halfPasses, false)`. UNTOUCHED.

From contracts/modules/DegenerusGameLootboxModule.sol:1240-1261 (the EXISTING body being replaced — DELETE the inline loop, KEEP the `ticketStartLevel = level + 1` assignment for the caller):
- `function _activateWhalePass(address player) private returns (uint24 ticketStartLevel)` — pre-edit:
  - `uint24 passLevel = level + 1;`
  - `ticketStartLevel = passLevel;`
  - `_applyWhalePassStats(player, ticketStartLevel);` (`:1247` — DELETE per D-04)
  - The 100-iter loop at `:1250-1260` writing per-level via `_queueTickets(...)` — DELETE.

From contracts/modules/DegenerusGameLootboxModule.sol:205-209 (the bonus-band constants — DELETE per D-21):
- `:205 KEEP` — `uint32 private constant WHALE_PASS_TICKETS_PER_LEVEL = 2;` (verify before deleting; may be used elsewhere)
- `:207 DELETE` — `uint32 private constant WHALE_PASS_BONUS_TICKETS_PER_LEVEL = 40;`
- `:209 DELETE` — `uint24 private constant WHALE_PASS_BONUS_END_LEVEL = 10;`

From contracts/modules/DegenerusGameLootboxModule.sol:378 (the boon flag — UNTOUCHED):
- `BOON_WHALE_PASS` is the flag the caller dispatches on.

From contracts/modules/DegenerusGameLootboxModule.sol:1635-1639 (the CALLER emitting the event — UNTOUCHED):
- The caller of `_activateWhalePass` still emits `LootboxWhalePassJackpot(player, ticketStartLevel)` (or equivalent) using the returned `ticketStartLevel`. The return contract MUST be preserved.

**Grant shape resolution (D-21 / 334-DESIGN-LOCK-WHALE-MINTDIV §3):** the flat per-level shape converges to "ONE whale-pass boon = ONE half-pass added to the player's claim counter." Concretely: `whalePassClaims[player] += 1;` on each whale-pass boon activation. The existing `claimWhalePass:1018` reads `halfPasses = whalePassClaims[player]; whalePassClaims[player] = 0;` and feeds `halfPasses` into `_queueTicketRange(... , 100, halfPasses, false)`. The "100" is the existing per-claim level count (`claimWhalePass` already implements this fanout); the half-pass count is the per-boon increment.

> Per 334-DESIGN-LOCK-WHALE-MINTDIV.md §3, the grant per box-open boon is ONE half-pass. The "100 levels × tickets-per-level" fanout is the CLAIM-SIDE materialization shape (already in WhaleModule:1018), not something the writer side encodes. The writer side is literally `+= 1` (or the named constant if a `BOX_OPEN_WHALE_PASS_HALF_PASSES = 1` constant is preferred for readability — Claude's Discretion within the locked one-half-pass-per-boon shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace _activateWhalePass body — O(1) record + caller event invariant preserved</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 1235–1265 — `_activateWhalePass` body; lines 1630–1645 — the caller's event emit using `ticketStartLevel`)
    - contracts/modules/DegenerusGamePayoutUtils.sol (lines 40–65 — the analog A2 `_queueWhalePassClaimCore` writer to mirror; line 52 is THE line)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1405–1415 — the second `+=` writer to confirm the established pattern)
    - contracts/modules/DegenerusGameWhaleModule.sol (lines 1015–1045 — the existing `claimWhalePass` to confirm halfPasses-read + zero-then-apply semantics; this is the materialization endpoint D-20 points to)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.3 Step 3 + §3.1 the O(1) `+=` writer pattern + Pitfalls P1, P2, P3)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§1 storage slot, §2 writer-side, §3 grant shape D-21)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md (§1 slot map — `whalePassClaims` is a non-frozen pending-claim accumulator)
  </read_first>
  <action>
    Replace the body of `function _activateWhalePass(address player) private returns (uint24 ticketStartLevel)` at `contracts/modules/DegenerusGameLootboxModule.sol:1240-1261` with the O(1) record shape:

    (a) **KEEP** the `ticketStartLevel = level + 1;` assignment (the caller at `:1638` reads this for the `LootboxWhalePassJackpot` event — the caller-event invariant MUST be preserved). Equivalently: assign `ticketStartLevel` via the existing `uint24 passLevel = level + 1; ticketStartLevel = passLevel;` shape, or collapse to `ticketStartLevel = level + 1;`. The visible behavior is identical.

    (b) **DELETE** the `_applyWhalePassStats(player, ticketStartLevel);` call at `:1247` (D-04 — stats move to claim-time via the existing `WhaleModule:1018` path, where `_applyWhalePassStats` is called immediately after the read-then-zero of `whalePassClaims[player]`).

    (c) **DELETE** the entire 100-iter loop body at `:1250-1260`:
        - The `for (uint24 i = 0; i < 100; )` loop header.
        - The inner `uint24 lvl = ticketStartLevel + i;` and `bool isBonus = (lvl >= passLevel && lvl <= WHALE_PASS_BONUS_END_LEVEL);` lines.
        - The inner `_queueTickets(player, lvl, isBonus ? WHALE_PASS_BONUS_TICKETS_PER_LEVEL : WHALE_PASS_TICKETS_PER_LEVEL, false);` call.
        - The `unchecked { ++i; }` increment + the closing `}`.

    (d) **ADD** the O(1) record AT THE END of the function body (after `ticketStartLevel = level + 1;`):
        `whalePassClaims[player] += 1;`

        Equivalently, if you prefer a named constant for readability, declare `uint256 private constant BOX_OPEN_WHALE_PASS_HALF_PASSES = 1;` near the other LootboxModule constants and use `whalePassClaims[player] += BOX_OPEN_WHALE_PASS_HALF_PASSES;` — both are coherent within D-21's one-half-pass-per-boon lock. The bare `+= 1` is the minimum-surface choice and is what the SUMMARY should default to unless a downstream caller in this module also needs to reference the constant.

    Constraints:
    - The function visibility (`private`) and return signature (`returns (uint24 ticketStartLevel)`) MUST be unchanged.
    - The function name `_activateWhalePass` MUST be unchanged (the caller at `:1635-1639` dispatches to it via `BOON_WHALE_PASS:378`).
    - DO NOT introduce a parallel `pendingWhalePasses` map (Pitfall P1 / D-02 / D-20). The slot IS `whalePassClaims` (Plan 335-01 Task 1 confirmed it at Storage:955).
    - DO NOT modify `WhaleModule:1018` (`claimWhalePass`). DO NOT add a `gameOver` guard there (Pitfall P2 / D-IMPL-01 — `_livenessTriggered` enforces forfeit structurally).
    - DO NOT modify the OTHER two `_applyWhalePassStats` callers (`WhaleModule:1032`, `DecimatorModule:588`) — they stay immediate-apply (Pitfall P3 / D-04).
    - The caller's `LootboxWhalePassJackpot` event emit at `:1638` (or equivalent name) MUST still receive `ticketStartLevel` from the return value — verify by reading the immediate caller block.
  </action>
  <verify>
    <automated>grep -nE "whalePassClaims\[(player|beneficiary)\] \+=" contracts/modules/DegenerusGameLootboxModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "whalePassClaims\[(player|beneficiary)\] \+=" contracts/modules/DegenerusGameLootboxModule.sol` returns exactly 1 line.
    - `grep -cE "for \(uint24 i = 0; i < 100;" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (the 100-iter loop is gone).
    - `grep -cE "_applyWhalePassStats" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (the call to stats is gone from this module — the OTHER two callers in WhaleModule + DecimatorModule survive in their own files).
    - `grep -cE "_queueTickets\(player, lvl" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (the per-level inline queue calls are gone).
    - `grep -nE "function _activateWhalePass\(address player\) private returns \(uint24 ticketStartLevel\)" contracts/modules/DegenerusGameLootboxModule.sol` returns exactly 1 line (signature preserved).
    - `grep -nE "ticketStartLevel = level \+ 1" contracts/modules/DegenerusGameLootboxModule.sol` returns ≥ 1 line (the caller-event invariant preserved).
    - Inspecting the caller block at `:1630-1645`: `_activateWhalePass(player)` call site UNCHANGED; the emitted event still uses the returned `ticketStartLevel`.
    - `grep -nE "function claimWhalePass" contracts/modules/DegenerusGameWhaleModule.sol` STILL returns the original `:1018` line — `WhaleModule:1018` is NOT modified by this plan.
  </acceptance_criteria>
  <done>The O(1) `+=` write replaces the inline 100-loop; `_applyWhalePassStats` removed from this module path (D-04); `ticketStartLevel = level + 1` preserved for the caller's event; function signature unchanged; the two other `_applyWhalePassStats` callers untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the ≤10 bonus-band constants (D-21 — early-game bonus DROPPED)</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 200–215 — the three whale-pass constants and their docstrings; verify `:205`'s `WHALE_PASS_TICKETS_PER_LEVEL` usage before deciding to keep or also delete)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.3 Bonus-band constants to delete — and the warning to verify `:205` usage)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§3 D-21 lock — ≤10 bonus DROPPED; economic delta routed to 338 SWEEP)
  </read_first>
  <action>
    Delete the two bonus-band constants at `contracts/modules/DegenerusGameLootboxModule.sol`:

    (a) **DELETE** `:207`: `uint32 private constant WHALE_PASS_BONUS_TICKETS_PER_LEVEL = 40;` (the 40-tickets-per-level bonus rate — the early-game windfall D-21 drops).

    (b) **DELETE** `:209`: `uint24 private constant WHALE_PASS_BONUS_END_LEVEL = 10;` (the ≤10 bonus-band ceiling — the bonus carve-out's upper bound).

    Also delete the docstring/comment lines (if any) directly attached to these two consts; do NOT leave dangling NatSpec.

    (c) **VERIFY before touching `:205`**: `grep -rn "WHALE_PASS_TICKETS_PER_LEVEL\b" contracts/` — if `:205`'s `WHALE_PASS_TICKETS_PER_LEVEL = 2` has NO remaining caller after Task 1's delete (the inline loop was its primary user; check `_queueTicketRange` callers + any direct refs), DELETE it too. If it IS still referenced (e.g. by a separate code path or a downstream module that imports it via inheritance), KEEP it untouched and note the kept-but-unused-locally status in the SUMMARY.

    **NB:** the `claimWhalePass` materialization at `WhaleModule:1018` uses a HARD-CODED `100` levels × `halfPasses` shape — it does NOT reference `WHALE_PASS_TICKETS_PER_LEVEL`. So `:205`'s constant is likely orphaned by Task 1's delete; verify and act accordingly.

    Constraints:
    - DO NOT introduce a replacement bonus constant. The ≤10 band is DROPPED (D-21). Any "flat extension to make up the lost windfall" is OUT of scope; the value delta is routed to the 338 SWEEP economic-analyst (D-06/D-21).
    - DO NOT touch `BOON_WHALE_PASS:378` (the boon flag — UNTOUCHED per PATTERNS §2.3 last bullet).
  </action>
  <verify>
    <automated>grep -nE "WHALE_PASS_BONUS_TICKETS_PER_LEVEL|WHALE_PASS_BONUS_END_LEVEL" contracts/modules/DegenerusGameLootboxModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "WHALE_PASS_BONUS_TICKETS_PER_LEVEL|WHALE_PASS_BONUS_END_LEVEL" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 lines.
    - `grep -rn "WHALE_PASS_BONUS_TICKETS_PER_LEVEL\|WHALE_PASS_BONUS_END_LEVEL" contracts/` returns 0 lines (entire `contracts/` tree — these constants were only referenced inside `_activateWhalePass`'s deleted loop).
    - If `WHALE_PASS_TICKETS_PER_LEVEL` was deleted: `grep -rn "WHALE_PASS_TICKETS_PER_LEVEL" contracts/` returns 0 lines.
    - If `WHALE_PASS_TICKETS_PER_LEVEL` was kept (still referenced elsewhere): the SUMMARY explicitly records the surviving caller's file:line for 338's adversarial re-attest.
  </acceptance_criteria>
  <done>The two bonus-band constants are deleted; `:205`'s `WHALE_PASS_TICKETS_PER_LEVEL` status (kept-or-deleted) is decided by orphan-check and recorded in the SUMMARY; no replacement bonus constant introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| box-opener (any player) → `_activateWhalePass` (via the BOON_WHALE_PASS dispatch in `_autoOpenBox` / `openLootBox`) | Untrusted opener crosses here. Pre-edit: O(N) inline mint scaled with the bonus-band. Post-edit: O(1) `+=` write. The opener's funds path (the lootbox open itself) is upstream and is not touched by this plan. |
| beneficiary (any address) → `WhaleModule.claimWhalePass(beneficiary)` | UNTOUCHED. Pre-existing trust boundary: the claim is permissionless-w/-beneficiary-arg; `_livenessTriggered` revert at :1019 enforces forfeit post-gameOver (D-IMPL-01 transitivity). |
| post-WHALE-01 vs RNG window | The new `whalePassClaims[player] += 1;` write occurs INSIDE a box-open transaction. Box opens are permissible during `rngLock` iff the lootbox open path's existing freeze gates allow them — 334-WHALE04-FREEZE-PROOF §3-§5 maps the write target to a non-frozen pending-claim accumulator, NOT a VRF-influenced slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-06 | Tampering (RNG-freeze invariant regression) | `_activateWhalePass` new `+=` write during `rngLock` | mitigate (proven safe — re-cite) | `334-WHALE04-FREEZE-PROOF.md` §1 catalogs `whalePassClaims[*]` as a non-frozen pending-claim accumulator; §5 VERDICT FREEZE-SAFE for the deferred-claim split. No new write into any frozen slot is introduced — the bonus-band loop's per-level `_queueTickets` writes (which DID touch `ticketsOwedPacked`, a VRF-adjacent slot) are GONE, replaced by a single accumulator increment. The fuzz-empirical re-attest lives at 336/TST-01 freeze leg (D-IMPL-02). |
| T-335-07 | Tampering (claim-time stats application timing) | `_applyWhalePassStats` no longer fired at box-open; moves to `claimWhalePass:1018` | mitigate (semantic preservation by D-04) | The existing `WhaleModule:1018` already calls `_applyWhalePassStats(player, startLevel)` immediately after read-then-zero of `whalePassClaims[player]` — same per-claim arity, same logical anchor. The two OTHER callers (`WhaleModule:1032` bundle, `DecimatorModule:588` Decimator) stay IMMEDIATE-apply (Pitfall P3). The `_applyWhalePassStats` body itself (`Storage:1111`) is UNTOUCHED. |
| T-335-08 | Denial of service | beneficiary refuses to claim → unclaimed `whalePassClaims[player]` grows | accept (intended deferral; gameOver forfeits per D-IMPL-01) | The claim is player-paid (the gas borne by the beneficiary at claim time, not by the box-opener at open time — WHALE-02). If the beneficiary never claims, the value sits in `whalePassClaims` until gameOver, at which point `_livenessTriggered` (now true forever) reverts the claim — D-23 forfeit by structural transitivity. The SUMMARY MUST cite the D-IMPL-01 transitivity trace for the 338 economic-analyst to re-attest. |
| T-335-09 | Information disclosure | `whalePassClaims[player]` is a public-readable accumulator (existing pattern) | accept | Pre-existing: the slot is already publicly readable via the inherited storage layout, and the existing `PayoutUtils:52` + `JackpotModule:1410` writers + `WhaleModule:1018` reader already establish the public-accumulator pattern. WHALE-01 adds a third writer; no new disclosure. |
| T-335-10 | Elevation of privilege | introducing a `pendingWhalePasses[*]` parallel map | mitigate (avoid) | **DO NOT INTRODUCE** per Pitfall P1 / D-02 / D-20. The slot IS `whalePassClaims`. The convergence-onto-existing-machinery story (D-20) is the entire reason WHALE-01 is a one-line edit and not a new subsystem. |
| T-335-11 | Tampering | moving `_applyWhalePassStats` calls in `WhaleModule:1032` or `DecimatorModule:588` to claim-time too | mitigate (avoid) | **DO NOT TOUCH** per Pitfall P3 / D-04 / 334-GREP-ATTESTATION drift-correction #3. Only the LootboxModule box-open path moves to claim-time; the bundle-purchase and Decimator-win paths apply stats immediately at the originating call. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — `whalePassClaims +=` writes target a non-frozen slot per 334-WHALE04-FREEZE-PROOF §1; the deleted `_queueTickets`-per-level writes (which DID touch `ticketsOwedPacked`) are gone from the box-open path. The deeper freeze fuzz lives at 336/TST-01.
- **OPEN-E operator-approval trust boundary** — N/A to this plan (AfKing-side; Plan 335-04).
- **AFKING cancel-tombstone + swap-pop invariant** — N/A to this plan (AfKing-side; Plan 335-04).
- **D-IMPL-01 gameOver-forfeit structural guard** — preserved by NOT touching `claimWhalePass:1018`. `_livenessTriggered`'s revert at `:1019` enforces forfeit by structural transitivity (the trace recorded in 335-CONTEXT.md and to be re-cited in this plan's SUMMARY).
- **`_applyWhalePassStats` 3-caller invariant** — preserved: the LootboxModule call is REMOVED (D-04), the other two (WhaleModule:1032, DecimatorModule:588) are UNTOUCHED. Post-edit, `_applyWhalePassStats` has 3 callers: WhaleModule:1018 (existing, claim-time), WhaleModule:1032 (existing, bundle), DecimatorModule:588 (existing, win) — same 3, different timing for one of them.
</threat_model>

<verification>
Plan-level checks (executor MUST run after both tasks):

1. `grep -nE "whalePassClaims\[(player|beneficiary)\] \+=" contracts/modules/DegenerusGameLootboxModule.sol` returns exactly 1 line (the new write).
2. `grep -cE "for \(uint24 i = 0; i < 100;" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (inline loop gone).
3. `grep -cE "_applyWhalePassStats" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (this module's call removed; other two callers untouched in their own files).
4. `grep -rn "_applyWhalePassStats" contracts/modules/DegenerusGameWhaleModule.sol contracts/modules/DegenerusGameDecimatorModule.sol` returns ≥ 2 lines (the surviving callers at WhaleModule:1032 + DecimatorModule:588 — Pitfall P3).
5. `grep -nE "WHALE_PASS_BONUS_TICKETS_PER_LEVEL|WHALE_PASS_BONUS_END_LEVEL" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 lines.
6. `grep -nE "function _activateWhalePass\(address player\) private returns \(uint24 ticketStartLevel\)" contracts/modules/DegenerusGameLootboxModule.sol` returns 1 line (signature preserved).
7. `grep -nE "ticketStartLevel = level \+ 1" contracts/modules/DegenerusGameLootboxModule.sol` returns ≥ 1 line (caller-event invariant).
8. `grep -nE "function claimWhalePass" contracts/modules/DegenerusGameWhaleModule.sol` STILL returns the original `:1018` line (UNTOUCHED — Pitfall P2 + D-IMPL-01).
9. NO parallel map introduced: `grep -rni "pendingWhalePasses" contracts/` returns 0 lines (Pitfall P1).
</verification>

<success_criteria>
- WHALE-01 mechanically complete: O(1) box-open record replaces the inline 100-loop; `_applyWhalePassStats` moved to claim-time via the existing endpoint.
- WHALE-02 satisfied by convergence: the materialization endpoint at `WhaleModule:1018` (already deployed) is reachable from any beneficiary's `claimWhalePass(beneficiary)` call; no new entrypoint authored.
- ≤10 bonus band dropped (D-21); two bonus constants deleted; `WHALE_PASS_TICKETS_PER_LEVEL` status decided by orphan-check.
- `WhaleModule:1018` UNTOUCHED (Pitfall P2 / D-IMPL-01).
- `_applyWhalePassStats` body + the two other callers UNTOUCHED (Pitfall P3 / D-04).
- NO parallel `pendingWhalePasses` map (Pitfall P1).
- Freeze-safety: re-cited from 334-WHALE04-FREEZE-PROOF (VERDICT FREEZE-SAFE); deeper fuzz at 336/TST-01.
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-02-SUMMARY.md` when done. The SUMMARY MUST record:
- The post-edit body of `_activateWhalePass` (final version), with the line range it occupies in the post-edit file.
- The deleted line ranges (the inline loop, the `_applyWhalePassStats` call, the two bonus constants, and any docstrings).
- The `WHALE_PASS_TICKETS_PER_LEVEL` orphan-check result (kept-or-deleted with the grep-attested rationale).
- A re-citation of the D-IMPL-01 transitivity trace (gameOver-forfeit by `_livenessTriggered`) — load-bearing for the 338 economic-analyst.
- A re-citation of 334-WHALE04-FREEZE-PROOF VERDICT FREEZE-SAFE — the freeze-safety attestation the deeper fuzz at 336/TST-01 will empirically prove.
- A grep-attestation block confirming all 9 plan-level verification gates pass on the post-edit file.
</output>
