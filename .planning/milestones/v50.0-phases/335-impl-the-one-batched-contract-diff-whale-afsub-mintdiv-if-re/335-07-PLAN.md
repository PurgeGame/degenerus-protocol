---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 07
type: execute
wave: 5
depends_on: [335-06]
files_modified: []
autonomous: false
worktree: false
requirements: [BATCH-02]
tags: [hard-stop, user-hand-review, commit-gate, batch-02]

must_haves:
  truths:
    - "A unified diff (`git diff b0511ca2 HEAD -- contracts/ test/`) is presented to the USER in full, alongside `335-LOCAL-VERIFICATION.md`."
    - "A per-anchor `file:line` re-attestation table vs `b0511ca2` is presented (leverages 334-GREP-ATTESTATION patterns)."
    - "The USER is asked an explicit question: 'OK to commit the v50.0 IMPL batched diff?' — and the executor WAITS for an explicit response."
    - "ONLY upon explicit USER approval ('ok' / 'approved' / 'yes commit' / equivalent) does the single batched commit fire."
    - "The single batched commit covers ALL of: `contracts/storage/DegenerusGameStorage.sol` (confirm-only — no diff), `contracts/DegenerusGame.sol`, `contracts/modules/DegenerusGameLootboxModule.sol`, `contracts/modules/DegenerusGameMintModule.sol`, `contracts/AfKing.sol`, `contracts/BurnieCoin.sol`, plus the 7 migrated test files + any green-getting fixes from Plan 335-06."
    - "The commit message follows the v49 BATCH-02 precedent format: scope tag + concise description + co-authored-by footer."
    - "If the USER rejects or asks for changes, NO commit fires; the executor records the feedback and HALTS for re-spec or follow-up plans."
  artifacts:
    - path: "git history (single new commit on main)"
      provides: "The v50.0 IMPL contract subject — applied to `contracts/` + `test/` under one USER-approved commit, per the v49 Phase 330 IMPL `63bc16ca` precedent."
  key_links:
    - from: "Plan 335-06 outputs"
      to: "USER hand-review"
      via: "335-LOCAL-VERIFICATION.md + the unified diff + the per-anchor re-attestation"
      pattern: "OK to commit?"
---

<objective>
This is the **BATCH-02 HARD STOP** — the contract-commit boundary that closes Phase 335.

Per `feedback_wait_for_approval` / `feedback_no_contract_commits` / `feedback_pause_at_contract_phase_boundaries` / `feedback_manual_review_before_push`:

- Editing `.sol` autonomously is FINE (Plans 335-01..06 already did so).
- COMMITTING `.sol` requires explicit USER hand-review of the unified diff.
- The auto-advance is HELD at this boundary; the executor MUST present the diff + the verification ledger + the per-anchor re-attestation and ASK before committing.

This plan delivers:
- **BATCH-02 (commit-gate piece)** — the single USER-approved batched commit, covering all contract + test edits in one atomic commit (the v49 Phase 330 IMPL `63bc16ca` precedent: "5 contracts + 9 tests in one batch").

The plan has TWO outcomes:
1. **USER approves** → executor fires the single batched commit. Phase 335 closes.
2. **USER rejects / asks for changes** → executor records the feedback; NO commit fires; the diff stays uncommitted in the working tree. The phase HALTS pending re-spec or follow-up plans (likely a Plan 335-04.5 or a new 335-08 reverting/adjusting specific bits).

CRITICAL: this plan is `autonomous: false`. The executor MUST pause at Task 2 (the presentation) and WAIT for the USER's response before proceeding to Task 3 (the commit). No "I'll wait 30 seconds and assume approval" — explicit affirmation required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-02-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-03-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-04-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-05-PLAN.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Assemble the review package — unified diff + ledger + per-anchor re-attestation</name>
  <files>(no file edits; assembles review artifacts)</files>
  <read_first>
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md (Plan 335-06's output)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md (the anchor patterns)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-SUMMARY.md through 335-06-SUMMARY.md (per-plan SUMMARYs)
  </read_first>
  <action>
    Assemble the review package — three artifacts the USER inspects:

    **(a) The unified diff** — capture `git diff b0511ca2 HEAD -- contracts/ test/` and `git diff --stat b0511ca2 HEAD -- contracts/ test/`. The baseline `b0511ca2` is the v49.0 closure HEAD per 335-CONTEXT.md. Confirm:
    - `git diff --stat b0511ca2 HEAD -- contracts/` shows file changes in: `DegenerusGame.sol`, `DegenerusGameLootboxModule.sol`, `DegenerusGameMintModule.sol`, `AfKing.sol`, `BurnieCoin.sol` (5 files). `DegenerusGameStorage.sol` shows 0 changes (confirm-only per Plan 335-01 Task 1).
    - `git diff --stat b0511ca2 HEAD -- test/` shows file changes in: `test/fuzz/AfKingSubscription.t.sol`, `test/fuzz/AfKingFundingWaterfall.t.sol`, `test/fuzz/AfKingConcurrency.t.sol`, `test/fuzz/KeeperNonBrick.t.sol`, `test/fuzz/RngFreezeAndRemovalProofs.t.sol`, `test/gas/KeeperLeversAndPacking.t.sol`, `test/gas/RouterWorstCaseGas.t.sol` (7 files). `test/gas/KeeperOpenBoxWorstCaseGas.t.sol` shows 0 changes (Plan 335-05 left it untouched).

    Any DEVIATION from this expected file list is a red flag — re-verify Plans 335-01..05 didn't touch a file they shouldn't have.

    **(b) The local-verification ledger** — `335-LOCAL-VERIFICATION.md` (already authored by Plan 335-06 Task 5). Just point the USER to it; no re-authoring.

    **(c) The per-anchor `file:line` re-attestation table** — author a fresh table verifying that the post-edit working tree matches what the SPEC promised. Use the 334-GREP-ATTESTATION patterns. Anchors to re-attest (minimum set; expand if the SPEC cites more):

    | Anchor | SPEC promise | Post-edit verification |
    |--------|--------------|------------------------|
    | `DegenerusGame.sol:1520` neighborhood — `lazyPassHorizon` view added | New external view returning `uint24`; deity sentinel = `type(uint24).max` | `grep -nE "function lazyPassHorizon\(address[^)]*\) external view returns \(uint24\)" contracts/DegenerusGame.sol` returns 1 line |
    | `DegenerusGame.sol` — `OPEN_NORMAL_GAS_UNIT` gone | Constant deleted; gas-weighting math removed; flat opened-count guard | `grep -nE "OPEN_NORMAL_GAS_UNIT" contracts/DegenerusGame.sol` returns 0 |
    | `LootboxModule.sol:1240-1260` — inline 100-loop replaced | O(1) `whalePassClaims[player] += 1` (or named constant); `_applyWhalePassStats` call removed | `grep -nE "for \(uint24 i = 0; i < 100;" contracts/modules/DegenerusGameLootboxModule.sol` returns 0; `grep -nE "whalePassClaims\[player\] \+=" contracts/modules/DegenerusGameLootboxModule.sol` returns 1 |
    | `LootboxModule.sol:205-209` — bonus constants gone | `WHALE_PASS_BONUS_TICKETS_PER_LEVEL` + `WHALE_PASS_BONUS_END_LEVEL` deleted | `grep -nE "WHALE_PASS_BONUS_(TICKETS_PER_LEVEL\|END_LEVEL)" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 |
    | `WhaleModule.sol:1018` — UNTOUCHED | `claimWhalePass(address)` still exists at original line | `grep -nE "function claimWhalePass" contracts/modules/DegenerusGameWhaleModule.sol` returns 1 line at or near :1018; `git diff b0511ca2 HEAD -- contracts/modules/DegenerusGameWhaleModule.sol` shows no changes |
    | `MintModule.sol:716` — one-liner aligned | `processed += take;` (NOT `>>1`) | `grep -nE "processed \+= writesUsed >> 1" contracts/modules/DegenerusGameMintModule.sol` returns 0; `grep -nE "^\s*processed \+= take;" contracts/modules/DegenerusGameMintModule.sol` returns ≥ 2 lines |
    | `AfKing.sol` — `burnForKeeper`/`paidThroughDay`/`WINDOW_DAYS` gone; `validThroughLevel` installed | All four refs purged; new field present | `grep -rnE "burnForKeeper\|paidThroughDay\|WINDOW_DAYS" contracts/AfKing.sol` returns 0; `grep -nE "validThroughLevel" contracts/AfKing.sol` returns ≥ 5 lines |
    | `AfKing.sol` — IGame iface exposes `lazyPassHorizon` | Iface decl includes the view | `grep -nE "function lazyPassHorizon\(address[^)]*\) external view returns \(uint24\);" contracts/AfKing.sol` returns 1 line |
    | `AfKing.sol` — OPEN-E `fundingSource` UNTOUCHED | Field at offset 11 preserved; OPENE-04 gate at `:393-403` preserved | `grep -nE "address fundingSource" contracts/AfKing.sol` returns ≥ 1 line; `grep -nE "approval\[subscriber\]" contracts/AfKing.sol` returns ≥ 1 line |
    | `AfKing.sol` — crossing reads `lazyPassHorizon` ONCE | Exactly one `lazyPassHorizon(player)` call in `_autoBuy` | `grep -cE "lazyPassHorizon\(player\)" contracts/AfKing.sol` returns exactly 1 |
    | `BurnieCoin.sol` — `burnForKeeper` impl + event + modifier + error gone | All four surfaces deleted; siblings preserved | `grep -nE "burnForKeeper\|KeeperBurn\|onlyAfKing\|OnlyAfKing" contracts/BurnieCoin.sol` returns 0; `grep -nE "onlyGame\|onlyVault" contracts/BurnieCoin.sol` returns ≥ 2 |
    | System-wide: `burnForKeeper` purged | Zero references in `contracts/` and `test/` | `grep -rnE "burnForKeeper" contracts/ test/` returns 0 lines |
    | System-wide: `paidThroughDay` purged | Zero references | `grep -rnE "paidThroughDay" contracts/ test/` returns 0 lines |
    | `test/gas/KeeperOpenBoxWorstCaseGas.t.sol` UNTOUCHED | Plan 335-05 didn't edit it | `git diff b0511ca2 HEAD -- test/gas/KeeperOpenBoxWorstCaseGas.t.sol` returns empty |
    | `RouterWorstCaseGas.t.sol` — OPEN_BATCH from measurement | Picked value, NOT placeholder 100 | `grep -nE "OPEN_BATCH = " test/gas/RouterWorstCaseGas.t.sol` returns the picked value matching 335-LOCAL-VERIFICATION.md §4 |
    | `WhaleModule:1032` + `DecimatorModule:588` UNTOUCHED — `_applyWhalePassStats` other callers | Stay immediate-apply (Pitfall P3) | `git diff b0511ca2 HEAD -- contracts/modules/DegenerusGameWhaleModule.sol contracts/modules/DegenerusGameDecimatorModule.sol` shows no `_applyWhalePassStats` changes |

    Run each grep / git diff command, record the actual output, and present the table to the USER along with the diff and the ledger.

    Constraints:
    - Use only grep / git tools to verify; do NOT edit any file in this task.
    - If ANY anchor fails its verification, STOP. Do NOT proceed to Task 2 (the USER ask). Report the anchor failure as a Plan 335-01..05 regression — re-run the offending plan or escalate.
    - The 16-anchor minimum set above is the MINIMUM. If the executor has bandwidth, expand to all anchors in 334-GREP-ATTESTATION.md.
  </action>
  <verify>
    <automated>git diff --stat b0511ca2 HEAD -- contracts/ test/ | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat b0511ca2 HEAD -- contracts/ test/` shows the expected 5 contract files + 7 test files changed; nothing else.
    - All 16 re-attestation table rows verify as expected (each grep / git diff returns the asserted shape).
    - The full table is committed to a temporary buffer (in-memory in the executor's context) ready for Task 2's USER presentation.
    - If any anchor fails: HALT — the failure is captured for USER awareness in Task 2, but the commit is NOT fired in Task 3.
  </acceptance_criteria>
  <done>The review package is assembled: unified diff stat, 335-LOCAL-VERIFICATION.md pointer, 16-row re-attestation table. Anchor failures (if any) are captured and reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: USER HAND-REVIEW — present diff + ledger + re-attestation; ask "OK to commit?"</name>
  <files>(no file edits in this task — checkpoint gate only)</files>
  <action>Execute the checkpoint flow defined in `<what-built>` / `<how-to-verify>` / `<resume-signal>` below: present the unified diff, the local-verification ledger, and the per-anchor re-attestation table to the USER, then ask "OK to commit?" and WAIT for explicit response. Do NOT proceed to Task 3 (the commit) without explicit affirmation.</action>
  <verify>WAIT for USER response via the resume-signal; no automated verification (this is the human gate).</verify>
  <done>USER has responded explicitly (approved / rejected / change requested); the response is captured for Task 3's branching logic.</done>
  <what-built>
    The full Plans 335-01..06 output:
    - 5 contracts edited: `DegenerusGame.sol` (view + autoOpen retirement), `DegenerusGameLootboxModule.sol` (WHALE-01 O(1) record), `DegenerusGameMintModule.sol` (MINTDIV-02 one-liner), `AfKing.sol` (AFSUB cluster rewrite), `BurnieCoin.sol` (burnForKeeper surface deletion).
    - 7 test files migrated under D-IMPL-02 full alignment.
    - Local verification (Plan 335-06): forge build green, forge test green-or-known-baseline, measured per-box gas, picked OPEN_BATCH.
    - All anchors re-attested vs `b0511ca2` (Task 1 above).
  </what-built>
  <how-to-verify>
    Present the USER with THREE artifacts:

    1. **The unified diff** — show `git diff --stat b0511ca2 HEAD -- contracts/ test/` (the file-by-file change summary). For deeper review, the USER can request individual hunks via `git diff b0511ca2 HEAD -- <path>`. The TOTAL is ~5 contract files + 7 test files; if any other file is touched, surface it explicitly.

    2. **The local-verification ledger** — `cat .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md` (Plan 335-06 Task 5 output). The USER inspects: forge build status, forge test ledger vs `666/42/17` by NAME, KeeperOpenBoxWorstCaseGas measurement, picked OPEN_BATCH, attestation arithmetic.

    3. **The per-anchor re-attestation table** — the 16-row table assembled in Task 1. The USER inspects each row to confirm the SPEC promises landed.

    Then ask the USER explicitly:

    > "OK to commit the v50.0 IMPL batched diff?
    >
    > This will create a single new commit on `main` covering:
    > - 5 modified contracts: `DegenerusGame.sol`, `DegenerusGameLootboxModule.sol`, `DegenerusGameMintModule.sol`, `AfKing.sol`, `BurnieCoin.sol`.
    > - 7 migrated tests under `test/fuzz/` and `test/gas/`.
    > - Plus any green-getting fixes from Plan 335-06 (if recorded in 335-LOCAL-VERIFICATION.md §1).
    >
    > The commit will NOT be pushed (push gate is separate per `feedback_wait_for_approval`). Following the v49 BATCH-02 precedent (Phase 330 `63bc16ca`: 5 contracts + 9 tests in one batch).
    >
    > Approve? (yes / no / edit specific bit)"

    WAIT for the USER's explicit response. Do NOT proceed to Task 3 (the commit) without explicit affirmation.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" / "ok" / "yes commit" / "go" / equivalent → Task 3 fires the single batched commit.
    - "reject" / "no" / "needs changes" → Task 3 does NOT fire; the executor records the rejection rationale + the requested changes; the phase HALTS pending re-spec or follow-up plans.
    - Specific edit requests ("change OPEN_BATCH to N", "revert the validThroughLevel width to uint24", etc.) → Task 3 does NOT fire; the executor records the requests + the diff stays uncommitted; the USER + executor coordinate the requested changes (likely a Plan 335-08 or re-running parts of 335-04 / 335-06).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fire the single batched commit — IFF Task 2 was approved</name>
  <files>(commits all of contracts/ + test/ — no NEW file edits in this task)</files>
  <read_first>
    - Task 2's USER response (in the executor's context — DO NOT proceed without explicit approval).
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-LOCAL-VERIFICATION.md (for the commit-message content).
  </read_first>
  <action>
    **ONLY IF Task 2 returned an explicit USER approval**, fire the single batched commit.

    Command sequence:

    (a) Verify the working tree state: `git status` should show modified files matching the expected list from Task 1. If anything unexpected is staged or modified, STOP and re-verify before committing.

    (b) Stage the expected files:
        - `git add contracts/storage/DegenerusGameStorage.sol` (confirm-only, no diff — but `git add` is idempotent; safe)
        - `git add contracts/DegenerusGame.sol`
        - `git add contracts/modules/DegenerusGameLootboxModule.sol`
        - `git add contracts/modules/DegenerusGameMintModule.sol`
        - `git add contracts/AfKing.sol`
        - `git add contracts/BurnieCoin.sol`
        - `git add test/fuzz/AfKingSubscription.t.sol`
        - `git add test/fuzz/AfKingFundingWaterfall.t.sol`
        - `git add test/fuzz/AfKingConcurrency.t.sol`
        - `git add test/fuzz/KeeperNonBrick.t.sol`
        - `git add test/fuzz/RngFreezeAndRemovalProofs.t.sol`
        - `git add test/gas/KeeperLeversAndPacking.t.sol`
        - `git add test/gas/RouterWorstCaseGas.t.sol`
        - If Plan 335-06 §1 recorded any green-getting fixes (additional files edited mid-build), `git add` those too.

        DO NOT `git add` any other files. DO NOT `git add` the `.planning/` SUMMARY files in this commit — they go in a separate planning commit handled by the orchestrator's commit_docs step.

    (c) Verify the staged state: `git diff --cached --stat` should show the same file list as `git diff --stat b0511ca2 HEAD -- contracts/ test/` minus any planning files. Any discrepancy → STOP and resolve.

    (d) Fire the commit. Suggested commit-message format (mirroring v49 `63bc16ca` precedent):

        ```
        feat(335): v50.0 IMPL batched diff — WHALE-O(1) + AFSUB pass-gated + MINTDIV-02 alignment

        Subject: v50.0 audit-baseline `b0511ca2` → post-IMPL HEAD.

        - WHALE-01: `LootboxModule._activateWhalePass` replaces inline 100-iter `_queueTickets` mint
          with O(1) `whalePassClaims[player] += 1`. Converges onto existing `WhaleModule.claimWhalePass:1018`
          materialization endpoint (D-20). Stats move to claim-time (D-04). ≤10 bonus band dropped (D-21).
        - WHALE-02: player-paid claim via existing endpoint at WhaleModule:1018 (no new entrypoint).
        - WHALE-03: `DegenerusGame.autoOpen` retires `OPEN_NORMAL_GAS_UNIT`-weighted budget; flat
          opened-count guard; `OPEN_BATCH = <picked>` from `KeeperOpenBoxWorstCaseGas` measurement
          (per-box gas <measured>K, headroom ≥ 1 box; attestation `<picked>×<measured> ≤ 16.7M − headroom`).
        - AFSUB-01: `burnForKeeper` deleted from BOTH `AfKing.sol` + `BurnieCoin.sol`;
          `paidThroughDay`/`WINDOW_DAYS`/`FLAG_WINDOW_PAID` purged.
        - AFSUB-02/03: `Sub.validThroughLevel` (in-place repurpose of offset 5); subscribe encodes
          horizon from new `DegenerusGame.lazyPassHorizon` view (deity = `type(uint24).max`);
          `_autoBuy` per-iter is pure stored-field compare; crossing reads horizon ONCE → refresh-or-evict.
        - AFSUB-04: OPEN-E `fundingSource` field + 4 structural protections UNTOUCHED.
        - AFSUB-05: pass-eviction routes through `dailyQuantity=0` + `_removeFromSet` + `continue`
          without cursor advance — v49 swap-pop / SUB-07 cancel-tombstone preserved;
          H-CANCEL-SWAP-MISS class structurally cannot reproduce.
        - MINTDIV-02: `MintModule.sol:716` one-liner `processed += writesUsed >> 1` → `+= take`
          (PROVEN REACHABLE per 334-MINTDIV01-REACHABILITY-VERDICT — owed=300 / 2 callers / −17 warm trace).
          Loops STAY separate (D-15 full-dedup REJECTED).
        - Test migration (D-IMPL-02): 7 test files migrated under full-alignment policy — pass-OR-pay →
          pass-eviction-OR-refresh; OPEN-E 4-protection re-attest; H-CANCEL-SWAP-MISS regression
          under pass-eviction; flat OPEN_BATCH oracle; G8 `burnForKeeper(` byte-presence assertion deleted.
        - Local verification: `forge build` green; `forge test` <X>/<Y>/<Z> reconciled vs v49 666/42/17 by NAME;
          KeeperOpenBoxWorstCaseGas re-run captured uniform per-box gas <measured>K (whale vs non-whale within 25%).
        - Freeze safety: re-cited from 334-WHALE04-FREEZE-PROOF (VERDICT FREEZE-SAFE); deeper fuzz at 336/TST-01.
        - D-IMPL-01 gameOver-forfeit: enforced structurally by `_livenessTriggered` (no IMPL change to claimWhalePass).
        - 5 contracts + 7 tests = 12 files. The v49 BATCH-02 precedent (Phase 330 `63bc16ca`): 5 contracts + 9 tests in one batch.

        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
        ```

        The executor may compress/expand the message body as appropriate; the scope tag `feat(335):` + the headline + the co-authored-by footer are the load-bearing parts.

    (e) Verify the commit landed: `git log -1 --stat` should show the new commit with the expected file list. Capture the new commit SHA — it becomes the v50.0 IMPL HEAD that Plan 336 builds against.

    (f) DO NOT push. `git push` is a SEPARATE gate per `feedback_wait_for_approval`. Phase 335 closes at the commit; the push happens at v50.0 closure (Phase 338) per the v49 precedent ("USER said 'ok lets push' at v49 close").

    Constraints:
    - The commit is ATOMIC: ONE commit covering all `contracts/` + `test/` files. NEVER split into multiple commits (the v49 precedent + `feedback_batch_contract_approval`).
    - The commit is signed with the co-authored-by footer per the global CLAUDE.md instruction.
    - The commit message MUST cite the requirement IDs touched (WHALE-01/02/03, AFSUB-01..05, MINTDIV-02, BATCH-02).
    - NO push. The push gate is held until Phase 338 closure (or earlier USER explicit "lets push" if requested).
  </action>
  <verify>
    <automated>git log -1 --stat</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE new commit on `main` since `b0511ca2`'s descendant (or whatever the pre-Plan-335 HEAD was).
    - `git log -1 --stat` shows the expected 12 files (5 contracts + 7 tests; potentially +1-2 for green-getting fixes from Plan 335-06).
    - The commit message contains: scope tag `feat(335):`, the requirement IDs touched, the co-authored-by footer.
    - `git diff b0511ca2 HEAD -- contracts/ test/` AFTER the commit is unchanged from BEFORE the commit (the commit just records the existing working-tree state; no new edits).
    - `git status` is clean for `contracts/` + `test/` (no uncommitted contract/test changes; planning files may still be uncommitted — those go in the orchestrator's commit_docs step).
    - The new commit SHA is recorded in the SUMMARY for downstream phases.
  </acceptance_criteria>
  <done>The single batched commit is fired covering all contract + test edits; commit SHA recorded; no push. Phase 335 closes at the commit boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → USER hand-review | The executor presents the diff + ledger + re-attestation; the USER reviews and decides. The decision crosses the trust boundary from executor (autonomous) to USER (authoritative). |
| USER decision → git commit | An explicit affirmation is required before the commit fires. No implicit/timeout-based approval. |
| commit → push | A SEPARATE downstream gate. Phase 335 does not push; that happens at Phase 338 closure or upon explicit USER "lets push." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-35 | Elevation of privilege | executor fires the commit without explicit USER approval | mitigate (autonomous: false + checkpoint:human-verify gate) | This plan is `autonomous: false`. Task 2 is a `checkpoint:human-verify` gate. The executor MUST WAIT for the USER's `<resume-signal>` before proceeding to Task 3. Per `feedback_wait_for_approval`: "no contract COMMIT without hand-review." |
| T-335-36 | Tampering | executor stages files it shouldn't (e.g. `.planning/` SUMMARYs in the contract commit) | mitigate (explicit file list + git diff --cached verification) | Task 3 (b) enumerates the exact files to `git add`; (c) verifies the staged state matches. The `.planning/` SUMMARYs are EXPLICITLY excluded from this commit (they go in the orchestrator's commit_docs step). |
| T-335-37 | Repudiation | claim "the commit was approved" when it wasn't | mitigate (explicit USER message in the resume signal) | The `<resume-signal>` enumerates the acceptable affirmative responses ("approved", "ok", "yes commit", etc.) AND the rejection responses. The executor records the actual USER response verbatim in the SUMMARY. |
| T-335-38 | Denial of service | the executor presents the diff but the USER doesn't respond | accept (HALT is the intended behavior) | If the USER doesn't respond, the phase HALTs at Task 2. There is no timeout-based fallback. The executor returns control; the USER picks up later, or the orchestrator times out the session and the phase resumes when the USER re-engages. |
| T-335-39 | Information disclosure | the commit message reveals too much architectural detail | accept | The repo is private; commits are not deployed. The v49 BATCH-02 precedent (`63bc16ca`) uses similar verbose commit messages. The verbose detail aids 338's adversarial sweep + the eventual external audit. |
| T-335-40 | Tampering | the commit silently includes a file that wasn't in Plans 335-01..05's scope | mitigate (Task 1 git diff --stat anchor verification + Task 3 git diff --cached verification) | Task 1 (a) confirms the EXPECTED file list before presenting to USER; Task 3 (c) confirms the STAGED file list matches; any deviation is STOP-and-investigate. |
| T-335-41 | Elevation of privilege | the executor pushes after committing without USER push approval | mitigate (avoid) | Task 3 (f) explicitly states NO PUSH. The push gate is separate per `feedback_wait_for_approval` and happens at Phase 338 closure or upon explicit USER request. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — re-attested by 334-WHALE04-FREEZE-PROOF (cited in the commit message) AND by the trivial assertions in `RngFreezeAndRemovalProofs.t.sol` (Plan 335-05 Task 5).
- **OPEN-E operator-approval trust boundary** — preserved by Plan 335-04 leaving `fundingSource` + SUB-02 + OPENE-04 untouched; empirically re-attested by `AfKingFundingWaterfall.t.sol` (Plan 335-05 Task 2). Task 1 re-attestation table confirms.
- **AFKING cancel-tombstone + swap-pop invariant** — preserved by Plan 335-04 routing pass-eviction through the same tombstone-then-reclaim pattern; empirically re-attested by `AfKingConcurrency.t.sol` (Plan 335-05 Task 3) + `KeeperNonBrick.t.sol` (Task 4). Task 1 re-attestation table confirms.
- **D-IMPL-01 gameOver-forfeit** — structurally preserved by Plans 335-01/02 NOT touching `claimWhalePass` (`_livenessTriggered` enforces by transitivity). The commit message cites the trace; 338's economic-analyst re-attests.
- **`_applyWhalePassStats` 3-caller invariant** — Plan 335-02 moved ONLY the LootboxModule caller to claim-time; the other two (`WhaleModule:1032`, `DecimatorModule:588`) UNTOUCHED. Task 1 re-attestation row confirms.
- **D-IMPL-04 OPEN_BATCH measurement-first** — Plan 335-06 measured + picked; the commit message records the figures.
- **BATCH-02 HARD STOP** — THIS plan IS the HARD STOP. Task 2 is the explicit gate.
</threat_model>

<verification>
Plan-level checks (executor MUST verify before declaring phase complete):

1. Task 2 received an explicit USER affirmation OR explicit USER rejection. NO implicit / timeout-based approval was accepted.
2. If approved: exactly ONE new commit on main since `b0511ca2`'s descendant, covering exactly the expected file list, with the expected commit-message structure (scope tag + requirements cited + co-authored-by footer).
3. If approved: `git push` was NOT executed (push gate is separate).
4. If rejected: NO new commit; the USER feedback is recorded in the SUMMARY; the phase HALTs pending re-spec or follow-up plans.
5. The Task 1 re-attestation table (16 rows) all passed (or the failures were reported in Task 2 for USER awareness — if Task 2 approved despite the failures, the SUMMARY records both the failures AND the USER's explicit override).
6. The new commit SHA (if any) is recorded in the SUMMARY for Phase 336's `depends_on` reference.
</verification>

<success_criteria>
- BATCH-02 HARD STOP respected — no commit without explicit USER hand-review approval.
- If approved: single atomic commit fired, no push, SHA recorded.
- If rejected: NO commit; feedback captured; phase HALTs.
- Phase 335 closes at the commit boundary (approved path) OR remains open pending re-spec (rejected path).
- The v50.0 audit subject is now FROZEN at the new IMPL HEAD (approved path) — Phase 336 TST builds against it.
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-07-SUMMARY.md` when done. The SUMMARY MUST record:

- The Task 2 USER response (verbatim).
- IF APPROVED:
  - The new commit SHA.
  - The commit-message text (or a pointer to `git log -1 --format=%B`).
  - Confirmation that NO push was executed.
  - Statement that Phase 335 closes at this commit; Phase 336 TST depends on this commit SHA.
- IF REJECTED:
  - The USER's rejection rationale + any specific change requests.
  - The set of follow-up actions (re-run Plans X / Y, author new Plan 335-08, etc.).
  - Statement that the phase HALTs pending USER + executor coordination on the requested changes.
- IF ANCHOR FAILURES surfaced in Task 1:
  - The failed anchor + the actual vs expected output.
  - Statement that the USER was informed in Task 2 (or that the executor HALTed before Task 2 if the failures were structural).
- The Task 1 re-attestation table (full 16 rows with their grep / git diff outputs) — committed alongside the SUMMARY as the load-bearing handoff to 338's adversarial sweep + delta audit.
</output>
