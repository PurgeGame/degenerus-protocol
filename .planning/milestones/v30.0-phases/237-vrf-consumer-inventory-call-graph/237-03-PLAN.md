---
phase: 237-vrf-consumer-inventory-call-graph
plan: 237-03
type: execute
wave: 2
depends_on: [237-01]
files_modified:
  - audit/v30-237-03-CALLGRAPH.md
  - audit/v30-CONSUMER-INVENTORY.md
autonomous: true
requirements: [INV-03]
requirements_addressed: [INV-03]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-237-03-CALLGRAPH.md` contains a ## Per-Consumer Call Graphs section with one call-graph entry per INV-237-NNN Universe List row from 237-01 (row count matches 237-01 exactly — no rows added or dropped)"
    - "Per D-11: every call graph covers request origination → `rawFulfillRandomWords` → intermediate storage touchpoints → consumption site. STOPS at consumption (forward SSTORE effects are Phase 238 FWD scope, NOT 237)."
    - "Per D-11: delegatecalls are traced to the target module (not treated as a black box); library calls are traced to the library function signature + return. The call graph names the target module / library function for every delegatecall / library call on the chain."
    - "Per D-12: call graphs for consumers whose graph body exceeds ~30 lines are hived off to `audit/v30-237-CALLGRAPH-<slug>.md` companion files; the main file's entry for those rows contains a Call Graph Ref link to the companion file. The soft threshold is planner's discretion — executor may inline graphs up to ~30 lines or hive off shorter ones when the companion file improves readability."
    - "Per D-11 depth stop: every call graph terminates at the consumption site (the keccak/hash2/entropyStep/SLOAD line that is the Row's Consumption File:Line). Forward SSTORE of the consumption result (e.g., writing a winner's jackpot credit into claimableWinnings) is NOT traced — that is Phase 238 FWD scope."
    - "A ## Consumer Index is present at the end of the final consolidated `audit/v30-CONSUMER-INVENTORY.md` (emitted by Task 3 of this plan) mapping every v30.0 requirement ID to the specific INV-237-NNN Row IDs it will cite. Requirements covered: INV-01, INV-02, INV-03 (Phase 237 own); BWD-01, BWD-02, BWD-03 (Phase 238); FWD-01, FWD-02, FWD-03 (Phase 238); RNG-01, RNG-02, RNG-03 (Phase 239); GO-01, GO-02, GO-03, GO-04, GO-05 (Phase 240); EXC-01, EXC-02, EXC-03, EXC-04 (Phase 241); REG-01, REG-02 (Phase 242); FIND-01, FIND-02, FIND-03 (Phase 242)."
    - "Per D-08: Task 3 emits `audit/v30-CONSUMER-INVENTORY.md` — the final consolidated deliverable — by MERGING `audit/v30-237-01-UNIVERSE.md` + `audit/v30-237-02-CLASSIFICATION.md` + `audit/v30-237-03-CALLGRAPH.md`. The merged file contains: Universe List with ALL classification columns filled (Path Family / Subcategory / KI Cross-Ref from 237-02; Call Graph Ref from 237-03) + Path-Family Legend + Subcategory Legend + Per-Consumer Call Graphs + Prior-Artifact Reconciliation (from 237-01) + Finding Candidates (merged from all 3 plans) + Scope-Guard Deferrals + Consumer Index (this plan) + Attestation. NO TBD-237-02 / TBD-237-03 placeholders remain in the consolidated file."
    - "Per D-15: NO F-30-NN finding IDs in either deliverable. A ## Finding Candidates section in the consolidated file enumerates anomalies from call-graph construction (e.g., a consumer whose call graph dead-ends before reaching any VRF request origination — suggesting a gap in 237-01's origination identification) for Phase 242 routing."
    - "Per D-18: zero contracts/ or test/ writes. Verified via `git status --porcelain contracts/ test/` empty before and after each task."
    - "HEAD anchor `7ab515fe` locked in frontmatter per D-17; verified via `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty."
    - "Per D-16: `audit/v30-237-01-UNIVERSE.md` and `audit/v30-237-02-CLASSIFICATION.md` are NOT edited by this plan — the consolidated file is a new file assembled from them. Verified via `git status --porcelain audit/v30-237-01-UNIVERSE.md audit/v30-237-02-CLASSIFICATION.md` empty after Task 3."
  artifacts:
    - path: "audit/v30-237-03-CALLGRAPH.md"
      provides: "INV-03 per-consumer call graphs (request origination → fulfillment → consumption, stopping at consumption per D-11)"
      contains: "## Per-Consumer Call Graphs"
    - path: "audit/v30-CONSUMER-INVENTORY.md"
      provides: "Final consolidated INV-01 + INV-02 + INV-03 deliverable per D-08 — single authoritative file for Phases 238-242"
      contains: "## Consumer Index"
  key_links:
    - from: "audit/v30-237-01-UNIVERSE.md Universe List rows"
      to: "audit/v30-237-03-CALLGRAPH.md Per-Consumer Call Graphs entries"
      via: "1:1 call-graph entry per Row ID"
      pattern: "INV-237-[0-9]{3}"
    - from: "audit/v30-237-03-CALLGRAPH.md companion references"
      to: "audit/v30-237-CALLGRAPH-<slug>.md companion files (per D-12)"
      via: "Call Graph Ref links for oversized graphs"
      pattern: "v30-237-CALLGRAPH-"
    - from: "v30.0 REQUIREMENTS.md (26 requirements)"
      to: "audit/v30-CONSUMER-INVENTORY.md ## Consumer Index"
      via: "per D-10 requirement → Row ID mapping"
      pattern: "INV-01|INV-02|INV-03|BWD-0[1-3]|FWD-0[1-3]|RNG-0[1-3]|GO-0[1-5]|EXC-0[1-4]|REG-0[1-2]|FIND-0[1-3]"
---

<objective>
INV-03 per-consumer call-graph pass + final consolidation. For every Universe List row from Plan 237-01, produce a call graph from VRF request origination → `rawFulfillRandomWords` → intermediate storage touchpoints → consumption site (stopping at consumption per D-11). Assemble the final `audit/v30-CONSUMER-INVENTORY.md` from 237-01 + 237-02 + 237-03 outputs per D-08.

**Two sub-deliverables:**
- `audit/v30-237-03-CALLGRAPH.md` — INV-03 call graphs + any oversized companion files (`audit/v30-237-CALLGRAPH-<slug>.md` per D-12)
- `audit/v30-CONSUMER-INVENTORY.md` — FINAL consolidated Phase 237 deliverable (Task 3 assembly output per D-08)

**Input contracts:**
- `audit/v30-237-01-UNIVERSE.md` — Universe List + Reconciliation + Finding Candidates from Plan 237-01
- `audit/v30-237-02-CLASSIFICATION.md` — Classification Table + Path-Family Legend + Subcategory Legend + KI Cross-Ref Summary from Plan 237-02

Both input files are READ-only per D-16 — this plan does NOT edit them; the consolidated file is a new assembly.

**Parallelism:** Runs in Wave 2 alongside Plan 237-02 — 237-02 and 237-03 are independent (each reads 237-01 only). 237-03's Task 3 is the final serialization point for Phase 237 — it runs after both Task 1 of this plan AND Task 2 of plan 237-02 have committed. The executor's wave orchestration MUST ensure 237-02's commit lands before 237-03's Task 3 runs. (237-02 has 2 tasks; 237-03 has 3 tasks. 237-03 Tasks 1+2 run in parallel with 237-02. 237-03 Task 3 blocks on 237-02 commit — the executor checks `git log --oneline -1 audit/v30-237-02-CLASSIFICATION.md` before Task 3 proceeds.)

**Call-graph depth (per D-11):** covers request → fulfillment → consumption. Delegatecalls traced to target module. Library calls traced to library function signature. STOPS at consumption — forward SSTORE of consumption result is Phase 238 FWD scope.

**Call-graph presentation (per D-12):** tabular + prose, no mermaid (per D-09). Oversized graphs (>~30 lines) moved to companion files.

**Consumer Index (per D-10):** maps every v30.0 requirement ID to cited Row IDs. Saves Phases 238-242 planner-lookup work.

**Finding-ID emission:** NO `F-30-NN` IDs per D-15. Finding candidates sectioned for Phase 242 routing.

Scope: READ-only per D-18. HEAD anchor `7ab515fe` locked per D-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/237-vrf-consumer-inventory-call-graph/237-CONTEXT.md
@audit/v30-237-01-UNIVERSE.md
</context>

<threat_model>
READ-only audit milestone: no runtime code modified, no test modified. Attack surface added: NONE. Only writes: markdown under `audit/v30-237-03-CALLGRAPH.md`, optional companion `audit/v30-237-CALLGRAPH-<slug>.md` files, and the consolidated `audit/v30-CONSUMER-INVENTORY.md`. All read-only post-commit per D-16. No STRIDE category applies. Inherits parent phase summary.

Residual risk: an incorrect call graph (missing an intermediate hop, misidentifying the VRF request origination) propagates into wrong scope for Phase 238 BWD-01 per-consumer backward trace. Mitigation: every call graph links request-origin file:line + fulfillment file:line + every intermediate storage touchpoint file:line — each anchor is independently verifiable via grep against `contracts/` at HEAD 7ab515fe; the Consumer Index provides a requirement-level cross-ref sanity check.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build per-consumer call graphs (request → fulfillment → consumption, stop-at-consumption per D-11) → audit/v30-237-03-CALLGRAPH.md + any companion files</name>
  <files>audit/v30-237-03-CALLGRAPH.md</files>
  <read_first>
    - audit/v30-237-01-UNIVERSE.md (Universe List — authoritative Row ID set. MUST be present and committed before this task starts.)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-CONTEXT.md (D-11 call-graph depth rule — stop at consumption; D-12 companion-file threshold; D-09 tabular, no mermaid; D-15 no F-30-NN; D-18 READ-only)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — the central VRF-request + fulfillment file. Specifically:
        * `advanceGame` — daily RNG request trigger
        * `requestRandomWords(...)` call sites — every VRF-request origination point (daily path, lootbox path, gameover path, backfill path)
        * `rawFulfillRandomWords(...)` at line 1690-1720 — THE fulfillment callback; every call graph terminates intermediate touchpoints at this function's SSTORE sites (`rngWordCurrent = word;` / `rngWordByDay[day] = word;` / `lootboxRngWordByIndex[idx] = word;`)
        * `rngGate(...)` — rngLockedFlag gate; intermediate touchpoint
        * `_swapAndFreeze`, `_swapTicketSlot` — buffer swap sites (F-29-04 scope)
        * `_finalizeRngRequest` — post-fulfillment finalization
        * `_processFutureTicketBatch`, `_prepareFutureTickets` — FF-promotion + near-future prep consumer passthrough
        * `_backfillGapDays` — gap-backfill entropy derivation
        * `_gameOverEntropy` at line 1222-1246 — gameover VRF word source
        * `_getHistoricalRngFallback` at line 1301 — prevrandao KI-exception path
    - contracts/modules/DegenerusGameLootboxModule.sol at HEAD 7ab515fe — mid-day-lootbox consumer chain: `requestLootboxRng`, `lootboxRngWordByIndex` SSTORE/SLOAD sites, lootbox opening consumers, `openLootbox` / related flows
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD 7ab515fe — daily + gameover consumer chain: every hash2/keccak consumer (16 sites per 230-02 addendum) + the caller chain from `advanceGame` → `_consolidatePoolsAndRewardJackpots` → the specific jackpot function
    - contracts/modules/DegenerusGameMintModule.sol at HEAD 7ab515fe — entropy-passthrough receiver: `processFutureTicketBatch(lvl, entropy)` → `_prepareFutureTickets` → `_raritySymbolBatch` + `_rollRemainder` consumers
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD 7ab515fe — gameover-specific consumer chain
    - contracts/modules/DegenerusGameBoonModule.sol at HEAD 7ab515fe — daily boon consumer chain
    - contracts/modules/DegenerusGameDecimatorModule.sol + DegenerusGameDegeneretteModule.sol + DegenerusGameWhaleModule.sol + DegenerusGameMintStreakUtils.sol + DegenerusGamePayoutUtils.sol at HEAD 7ab515fe — auxiliary consumer chains (e.g., `_calcAutoRebuy` in PayoutUtils)
    - contracts/DegenerusGame.sol at HEAD 7ab515fe — for any delegatecall boundary on the RNG-consumer chain (IM-10/IM-11/IM-12/IM-13 style delegatecall IDs per 230-01 interaction map)
    - contracts/DegenerusAffiliate.sol at HEAD 7ab515fe — affiliate winner-roll KI-exception call graph (originates in non-VRF seed, terminates at consumption — the graph is short)
    - contracts/BurnieCoinflip.sol at HEAD 7ab515fe — coinflip outcome consumer chain
    - contracts/DegenerusQuests.sol at HEAD 7ab515fe — quest resolution chain tied to daily RNG
    - contracts/libraries/EntropyLib.sol at HEAD 7ab515fe — library-function signatures for `entropyStep(uint256 state) → uint256` + `hash2(uint256, uint256) → uint256` (library call hop per D-11)
    - contracts/libraries/BitPackingLib.sol + JackpotBucketLib.sol + GameTimeLib.sol + PriceLookupLib.sol at HEAD 7ab515fe — any library-call hop appearing on a consumer chain
    - contracts/storage/DegenerusGameStorage.sol at HEAD 7ab515fe — intermediate storage touchpoint anchors: `rngWordCurrent`, `rngWordByDay`, `lootboxRngWordByIndex`, `rngLockedFlag`, `midDayTicketRngPending`, `vrfRequestId`, `totalFlipReversals` (cited in 232.1 fix series), `decimatorPool`, `earlybirdDgnrsPoolStart`
    - contracts/interfaces/IVRFCoordinator.sol at HEAD 7ab515fe — VRF coordinator interface signature (`requestRandomWords` + `rawFulfillRandomWords` external interface)
    - .planning/milestones/v29.0-phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2 Cross-Module Interaction Map (IM-10..IM-16) — baseline call-chain reference for entropy-passthrough + bonus-trait + BAF chains (may be read freely in this plan; D-07 zero-glance applies only to plan 237-01 Task 1)
  </read_first>
  <action>
Execute the INV-03 per-consumer call-graph construction per CONTEXT.md D-11/D-12.

**Verify prerequisites:**
```
test -f audit/v30-237-01-UNIVERSE.md
git log --oneline -1 -- audit/v30-237-01-UNIVERSE.md   # MUST show plan 237-01 Task 3 commit
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty
```

**Step 1 — Read the Universe List from `audit/v30-237-01-UNIVERSE.md`.** Extract every `INV-237-NNN` Row ID + its Consumption File:Line + Consumer Function + VRF Request Origin File:Line + Fulfillment Site File:Line + Notes.

**Step 2 — For each Row ID, construct the call graph (depth per D-11: request → fulfillment → consumption, STOP at consumption):**

Call-graph structure (tabular per D-09 — NO mermaid). Template:

```
### INV-237-NNN — <Consumer Function> (<Path Family from 237-02>)

**Consumption site:** contracts/<path>:<line>
**Path family:** <family from 237-02 — cite 237-02 for classification authority>

| Step | File:Line | Site | Hop Type |
|---|---|---|---|
| 1 | contracts/modules/DegenerusGameAdvanceModule.sol:<request-line> | `requestRandomWords(...)` VRF request | request-origination |
| 2 | contracts/modules/DegenerusGameAdvanceModule.sol:<storage-sstore> | `vrfRequestId = <value>` / `rngLockedFlag = true` | storage-mutation (request commitment) |
| 3 | [VRF coordinator callback — external contract, not traced] | `rawFulfillRandomWords(requestId, randomWords)` | vrf-callback |
| 4 | contracts/modules/DegenerusGameAdvanceModule.sol:1702 | `rngWordCurrent = word;` (or `lootboxRngWordByIndex[idx] = word;`) | storage-sstore (fulfillment commitment) |
| 5 | contracts/<path>:<intermediate-line> | `<intermediate function call>` | delegatecall OR library-call OR direct-call |
| ... | ... | ... | ... |
| N | contracts/<path>:<consumption-line> | `<consumption site — keccak / hash2 / entropyStep caller / SLOAD>` | consumption — STOP HERE per D-11 |

**Intermediate storage touchpoints:** <bullet list of any SLOAD/SSTORE on storage variables that mediate the word's path from fulfillment to consumption — e.g. `lootboxRngWordByIndex[idx]` read/write pair at SSTORE line X / SLOAD line Y>.

**Delegatecall / library hops traced (per D-11):**
- <if any>: contracts/<caller>:<line> — `IDegenerusGameMintModule.processFutureTicketBatch.selector` delegatecall → contracts/modules/DegenerusGameMintModule.sol:<target-line> `processFutureTicketBatch(uint24 lvl, uint256 entropy)` receiver
- <if library>: contracts/<caller>:<line> — `EntropyLib.entropyStep(state)` library call → contracts/libraries/EntropyLib.sol:<target-line> `entropyStep(uint256) returns (uint256)` signature

**KI Cross-Ref (from 237-02):** <e.g., [KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"] — or N/A>

**Graph line count:** <count — if > 30, CONFIRM companion file per D-12>
```

Replace every `<placeholder>` with real values derived from reading contracts/ at HEAD 7ab515fe. NO placeholder tokens may remain in the committed file.

**Step 3 — Apply D-12 companion-file rule:**
- For each call graph whose tabular body exceeds ~30 lines (the soft threshold; planner-discretion), hive off to `audit/v30-237-CALLGRAPH-<slug>.md` where `<slug>` is derived from `<path-family>-<function-name-kebab-case>` (e.g., `v30-237-CALLGRAPH-gameover-swap-and-freeze.md`). The main file's entry for that Row ID becomes a one-paragraph summary + Call Graph Ref link: `→ audit/v30-237-CALLGRAPH-<slug>.md`.
- Use companion files sparingly — only when inlining would genuinely hurt readability. The default is inline.
- Most graphs will fit inline (typical depth: ~5-12 steps). The 16 c2e5e0a9 JackpotModule sites share most of their chain; use a shared-prefix note rather than repeating the `advanceGame → rngGate → _consolidatePoolsAndRewardJackpots → <jackpot fn>` prefix 16 times.

**Step 4 — Write `audit/v30-237-03-CALLGRAPH.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Phase 237 Plan 03 — INV-03 Per-Consumer Call Graphs

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-17).
**Requirement addressed:** INV-03 — per-consumer call graph from VRF request origination through `rawFulfillRandomWords` to consumption site, including all intermediate storage touchpoints.
**Depth rule (per D-11):** request → fulfillment → consumption. STOP at consumption; forward SSTORE of consumption result is Phase 238 FWD scope. Delegatecalls traced to target module; library calls traced to library function signature.
**Presentation (per D-09):** tabular + prose. No mermaid.
**Companion files (per D-12):** oversized graphs (>~30 lines) hived off to `audit/v30-237-CALLGRAPH-<slug>.md` companion files (if any created, linked from the corresponding entry below).

## Call Graph Conventions

| Hop Type | Meaning |
|---|---|
| `request-origination` | VRF `requestRandomWords(...)` call site |
| `storage-mutation (request commitment)` | SSTORE writing request-side state (e.g., `vrfRequestId`, `rngLockedFlag = true`) |
| `vrf-callback` | VRF coordinator calls back into the contract via `rawFulfillRandomWords` — external contract hop, not traced further |
| `storage-sstore (fulfillment commitment)` | SSTORE writing the VRF word into storage (`rngWordCurrent`, `rngWordByDay[day]`, `lootboxRngWordByIndex[idx]`) |
| `direct-call` | Solidity `fn(...)` or `IContract.fn(...)` call on same or external address |
| `delegatecall` | `address.delegatecall(abi.encodeWithSelector(IFACE.fn.selector, ...))` — traced to target module per D-11 |
| `library-call` | library-function invocation (e.g., `EntropyLib.hash2(a, b)`) — traced to library function signature per D-11 |
| `consumption` | the consumption site — D-11 STOP boundary |

## Shared-Prefix Notes

**Shared daily-path prefix (applies to `daily` family rows):**
1. `advanceGame()` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — entry
2. `rngGate(...)` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — rngLockedFlag gate + `rngWordCurrent` SLOAD
3. `rawFulfillRandomWords` callback at contracts/modules/DegenerusGameAdvanceModule.sol:1690-1720 — VRF callback + `rngWordCurrent = word;` SSTORE at :1702
4. `_finalizeRngRequest(...)` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — post-fulfillment finalization
5. `_consolidatePoolsAndRewardJackpots(...)` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — pool consolidation + jackpot dispatch
6. [per-consumer tail: the specific jackpot / trait-roll / coin-branch function]

**Shared mid-day-lootbox prefix:**
1. `requestLootboxRng(...)` at contracts/modules/DegenerusGameLootboxModule.sol:<line> — mid-day VRF request
2. `rawFulfillRandomWords` callback at contracts/modules/DegenerusGameAdvanceModule.sol:1690-1720 — SSTORE to `lootboxRngWordByIndex[idx]` at :<line>
3. `openLootbox(...)` / `<lootbox-consumer>(...)` at contracts/modules/DegenerusGameLootboxModule.sol:<line> — SLOAD `lootboxRngWordByIndex[idx]`
4. [per-consumer tail: the specific rarity / ticket-count / BURNIE-amount / boon roll]

**Shared gameover-entropy prefix:**
1. `advanceGame()` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — entry
2. Gameover trigger condition (120-day liveness stall OR pool deficit) at contracts/modules/DegenerusGameAdvanceModule.sol:<line>
3. `_gameOverEntropy(...)` at contracts/modules/DegenerusGameAdvanceModule.sol:1222-1246 — gameover VRF word derivation (normal VRF-available branch OR `_getHistoricalRngFallback` prevrandao branch after 14-day gate)
4. [per-consumer tail: the specific winner-selection / trait-roll / terminal-drain / burn / coinflip / sweep consumer]

**Shared gap-backfill prefix:**
1. `_backfillGapDays(...)` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — gap-day iteration
2. `keccak256(abi.encode(vrfWord, gapDay))` at contracts/modules/DegenerusGameAdvanceModule.sol:<line> — per-day entropy derivation
3. [per-consumer tail: the specific gap-day consumer]

Shared-prefix placeholders (`<line>`) MUST be populated with real line numbers during the fresh read — no `<line>` tokens may remain.

## Per-Consumer Call Graphs

### INV-237-001 — <Consumer Function> (<Path Family>)

[Full tabular call graph as described in Step 2. Shared-prefix rows may be abbreviated as "[daily shared prefix, steps 1-5 above]" to avoid repetition.]

### INV-237-002 — ...

[Every Row ID from 237-01 gets an entry here. Oversized graphs link to companion file.]

## Finding Candidates

[Per D-15. Any anomaly discovered during call-graph construction: a consumer whose chain dead-ends before reaching any `requestRandomWords` origination (suggesting 237-01 missed the origin); a consumer whose fulfillment site differs from the default `rawFulfillRandomWords`; any dual-trigger case where 237-01 may not have correctly expanded per D-03. Format: bullet list — `- INV-237-NNN — contracts/<path>:<line> — <fn> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH>`. NO F-30-NN IDs. If none, write "None surfaced during call-graph construction."]

## Scope-Guard Deferrals

[Per D-16. If any gap requires editing 237-01 or 237-02 outputs, record here. If none, "None surfaced."]

## Downstream Hand-offs

- **Task 3 of this plan:** merges this file + 237-01 + 237-02 → `audit/v30-CONSUMER-INVENTORY.md` with full Consumer Index.
- **Phase 238 BWD/FWD:** every backward-trace / forward-enumeration proof cites an INV-237-NNN Row ID + this plan's call graph as its scope anchor. BWD-01 specifically uses the "intermediate storage touchpoints" lists here.
- **Phase 239 RNG-02:** permissionless-sweep classification uses call graphs to identify which permissionless functions touch any step on any RNG-consumer chain.
- **Phase 240 GO-01:** gameover-entropy call graphs become GO-01's consumer inventory — specifically the gameover-shared-prefix branching.
- **Phase 241 EXC-02:** prevrandao-fallback call graph (via `_getHistoricalRngFallback`) becomes EXC-02's proof subject.
- **Phase 241 EXC-04:** EntropyLib.entropyStep() caller graphs become EXC-04's proof subject.

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Input file `audit/v30-237-01-UNIVERSE.md` was present and committed before this task started
- Input Row ID integrity: this file contains a Per-Consumer Call Graphs entry for every INV-237-NNN Row ID in 237-01 (row count match verified)
- Companion files created (if any): [list `audit/v30-237-CALLGRAPH-<slug>.md` paths, or "None — all graphs inline."]
- Contract source reads limited to `contracts/**/*.sol` at HEAD `7ab515fe`
- `audit/v30-237-01-UNIVERSE.md` and `audit/v30-237-02-CLASSIFICATION.md` NOT modified by this task (per D-16)
```

Every Per-Consumer Call Graph entry MUST:
- Have Row ID matching `INV-237-[0-9]{3}` AND appearing in `audit/v30-237-01-UNIVERSE.md`
- Have a Consumption site file:line rooted in `contracts/` (never external paths)
- Have at least 2 steps in the tabular graph (request-origination + consumption at minimum) OR be flagged as a KI-exception degenerate case (affiliate non-VRF seed graph has no VRF-request hop — Notes column must make this explicit)
- Terminate at consumption (no forward-SSTORE-of-result step — D-11 boundary)
- Cite at least one Hop Type from the conventions table
- Never contain placeholder tokens `<path>`, `<line>`, `<fn`, `<family>`, `<slug>`
- Never contain F-30-NN

Companion files `audit/v30-237-CALLGRAPH-<slug>.md` (if created) MUST:
- Have the same Call Graph Conventions header
- Cite HEAD anchor `7ab515fe`
- End with a back-link to the main `audit/v30-237-03-CALLGRAPH.md` file

READ-only per D-18. Only writes: `audit/v30-237-03-CALLGRAPH.md` + any companion files. `audit/v30-237-01-UNIVERSE.md` and `audit/v30-237-02-CLASSIFICATION.md` NOT edited. `git status --porcelain contracts/ test/` MUST be empty after the write.
  </action>
  <verify>
    <automated>test -f audit/v30-237-03-CALLGRAPH.md && grep -q "^## Call Graph Conventions" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Shared-Prefix Notes" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Per-Consumer Call Graphs" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Finding Candidates" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Scope-Guard Deferrals" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Downstream Hand-offs" audit/v30-237-03-CALLGRAPH.md && grep -q "^## Attestation" audit/v30-237-03-CALLGRAPH.md && [ "$(grep -cE '^### INV-237-[0-9]{3}' audit/v30-237-03-CALLGRAPH.md)" -ge 40 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-01-UNIVERSE.md | sort -u | wc -l)" = "$(grep -cE '^### INV-237-[0-9]{3}' audit/v30-237-03-CALLGRAPH.md)" ] && ! grep -q "F-30-" audit/v30-237-03-CALLGRAPH.md && ! grep -q "<line>" audit/v30-237-03-CALLGRAPH.md && ! grep -q "<path>" audit/v30-237-03-CALLGRAPH.md && ! grep -q "<slug>" audit/v30-237-03-CALLGRAPH.md && grep -q "7ab515fe" audit/v30-237-03-CALLGRAPH.md && ! grep -qi "```mermaid" audit/v30-237-03-CALLGRAPH.md && test -z "$(git status --porcelain contracts/ test/)" && [ -z "$(git status --porcelain audit/v30-237-01-UNIVERSE.md audit/v30-237-02-CLASSIFICATION.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-237-03-CALLGRAPH.md` exists
    - File contains literal headers in this order: `## Call Graph Conventions`, `## Shared-Prefix Notes`, `## Per-Consumer Call Graphs`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Downstream Hand-offs`, `## Attestation`
    - Per-Consumer Call Graphs section has one `### INV-237-NNN` subsection per Row ID in 237-01; count matches exactly
    - Every call graph entry has a tabular body with at least one row (or an explicit KI-exception degenerate-case note for affiliate-style non-VRF chains)
    - Every call graph terminates at consumption (no forward-SSTORE-of-result hop — D-11 enforced)
    - No `F-30-` references (D-15)
    - No mermaid code fences (D-09)
    - No placeholder tokens `<line>`, `<path>`, `<fn`, `<slug>`, `<family>`
    - File contains literal `7ab515fe`
    - `audit/v30-237-01-UNIVERSE.md` and `audit/v30-237-02-CLASSIFICATION.md` NOT edited (D-16)
    - `git status --porcelain contracts/ test/` returns empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty
    - Any companion `audit/v30-237-CALLGRAPH-<slug>.md` files referenced in the main file exist and have a back-link to the main file
  </acceptance_criteria>
  <deliverable>
`audit/v30-237-03-CALLGRAPH.md` — INV-03 per-consumer call-graph deliverable with call-graph entry per Row ID, shared-prefix notes, D-11 depth compliance (stop-at-consumption), D-12 companion files (if any). Zero F-30-NN IDs. Zero mermaid. Zero contracts/ or test/ writes. 237-01 and 237-02 outputs unmodified.
  </deliverable>
  <done>Call-graph file exists; 7 required sections present; one call-graph entry per Universe List row; depth stops at consumption; delegatecall + library hops traced per D-11; no prior-plan outputs edited.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit/v30-237-03-CALLGRAPH.md (+ any companion files)</name>
  <files>audit/v30-237-03-CALLGRAPH.md</files>
  <read_first>
    - audit/v30-237-03-CALLGRAPH.md (must exist from Task 1)
    - any audit/v30-237-CALLGRAPH-*.md companion files created in Task 1 (discover via `ls audit/v30-237-CALLGRAPH-*.md 2>/dev/null`)
  </read_first>
  <action>
Stage and commit `audit/v30-237-03-CALLGRAPH.md` and any companion files. Use `git add` with explicit paths — do NOT `git add .`.

```
git add audit/v30-237-03-CALLGRAPH.md
# If any companion files exist, add them too:
for f in audit/v30-237-CALLGRAPH-*.md; do [ -f "$f" ] && git add "$f"; done
git commit -m "docs(237-03): INV-03 per-consumer call graphs at HEAD 7ab515fe

Per-Consumer Call Graphs — one entry per INV-237-NNN Universe List row
from Plan 237-01. Every graph covers request origination → rawFulfill
RandomWords → intermediate storage touchpoints → consumption site,
stopping at consumption per D-11 (forward SSTORE is Phase 238 FWD scope).

Delegatecalls traced to target module; library calls traced to library
function signature. Shared-prefix notes deduplicate the common daily /
mid-day-lootbox / gameover-entropy / gap-backfill chains.

Oversized graphs (>30 lines soft threshold) hived off to
audit/v30-237-CALLGRAPH-<slug>.md companion files per D-12.

Tabular format per D-09 (no mermaid).

NO F-30-NN IDs (D-15 — Phase 242 owns).
237-01 and 237-02 outputs NOT modified (D-16 READ-only-after-commit).
READ-only per D-18. HEAD anchor 7ab515fe (D-17)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "237-03.*(INV-03|call graph).*7ab515fe" && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-237-01-UNIVERSE.md audit/v30-237-02-CLASSIFICATION.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `237-03` + (`INV-03` OR `call graph`) + `7ab515fe`
    - Commit touches only `audit/v30-237-03-CALLGRAPH.md` and/or `audit/v30-237-CALLGRAPH-*.md` companion files
    - `git status --porcelain contracts/ test/` empty
    - `git status --porcelain audit/v30-237-01-UNIVERSE.md audit/v30-237-02-CLASSIFICATION.md` empty (237-01 + 237-02 outputs untouched)
    - Not pushed
  </acceptance_criteria>
  <deliverable>Approved `audit/v30-237-03-CALLGRAPH.md` + any companion files committed; local-only.</deliverable>
  <done>Commit exists; contracts/ test/ unmodified; 237-01 + 237-02 outputs unmodified; INV-03 deliverable committed.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble final consolidated audit/v30-CONSUMER-INVENTORY.md from 237-01 + 237-02 + 237-03 outputs (per D-08)</name>
  <files>audit/v30-CONSUMER-INVENTORY.md</files>
  <read_first>
    - audit/v30-237-01-UNIVERSE.md (Universe List + Reconciliation + Finding Candidates — MUST be committed from Plan 237-01)
    - audit/v30-237-02-CLASSIFICATION.md (Classification Table + Path-Family Legend + Subcategory Legend + Classification Summary + KI Cross-Ref Summary + Finding Candidates — MUST be committed from Plan 237-02)
    - audit/v30-237-03-CALLGRAPH.md (Per-Consumer Call Graphs + Shared-Prefix Notes + Call Graph Conventions + Finding Candidates — MUST be committed from Task 2 of THIS plan)
    - any audit/v30-237-CALLGRAPH-*.md companion files (via `ls audit/v30-237-CALLGRAPH-*.md 2>/dev/null`)
    - .planning/REQUIREMENTS.md (26 v30.0 requirement IDs for Consumer Index scope — INV/BWD/FWD/RNG/GO/EXC/REG/FIND series)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-CONTEXT.md (D-08 consolidated-file shape; D-10 Consumer Index pattern; D-15 no F-30-NN; D-16 READ-only-after-commit for 237-01 + 237-02 + 237-03 files; D-18 READ-only)
  </read_first>
  <action>
Assemble the final consolidated Phase 237 deliverable per D-08.

**Verify Wave 2 coordination — all three sub-deliverables must be committed before this task proceeds:**
```
test -f audit/v30-237-01-UNIVERSE.md && git log --oneline -1 -- audit/v30-237-01-UNIVERSE.md
test -f audit/v30-237-02-CLASSIFICATION.md && git log --oneline -1 -- audit/v30-237-02-CLASSIFICATION.md
test -f audit/v30-237-03-CALLGRAPH.md && git log --oneline -1 -- audit/v30-237-03-CALLGRAPH.md
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty
```
If ANY of the three input files is missing OR not committed, STOP — Plan 237-02 has not completed (Wave 2 parallel dependency).

**Step 1 — Read all three input files + any companion callgraph files + REQUIREMENTS.md.**

**Step 2 — Merge the Universe List columns.** For each INV-237-NNN Row ID:
- Use `audit/v30-237-01-UNIVERSE.md` for: Row ID, Consumption File:Line, Consumer Function, VRF Request Origin File:Line, Fulfillment Site File:Line, Notes
- Use `audit/v30-237-02-CLASSIFICATION.md` for: Path Family, Subcategory, KI Cross-Ref
- Use `audit/v30-237-03-CALLGRAPH.md` for: Call Graph Ref (either `§<anchor-in-this-file>` for inline graphs, or `audit/v30-237-CALLGRAPH-<slug>.md` for companion files)
- All TBD placeholders (TBD-237-02, TBD-237-03) MUST be replaced with real values.

**Step 3 — Build the Consumer Index (per D-10).** Map every v30.0 requirement ID to the INV-237-NNN Row IDs it will cite:

| Requirement | Phase | Expected Row-ID subset |
|---|---|---|
| INV-01 | 237 | ALL Row IDs (this is the universe list itself) |
| INV-02 | 237 | ALL Row IDs (classification applies to all) |
| INV-03 | 237 | ALL Row IDs (call graph applies to all) |
| BWD-01 / BWD-02 / BWD-03 | 238 | ALL Row IDs (per-consumer backward trace) |
| FWD-01 / FWD-02 / FWD-03 | 238 | ALL Row IDs (per-consumer forward enumeration) |
| RNG-01 | 239 | rngLocked set/clear state machine — subset: rows touching `rngLockedFlag` SSTORE/SLOAD (Notes column flags these; Call Graph Step 2 `storage-mutation (request commitment)` rows) |
| RNG-02 | 239 | Permissionless sweep — ALL Row IDs (each permissionless function classified against each Row's state set) |
| RNG-03 | 239 | The two documented asymmetries: (a) every `mid-day-lootbox` Row ID (index-advance isolation); (b) any row referencing `phaseTransitionActive` |
| GO-01 | 240 | All `gameover-entropy` Path Family rows + the F-29-04 `exception-mid-cycle-substitution` rows that enter the gameover path |
| GO-02 / GO-03 / GO-04 | 240 | Same subset as GO-01 |
| GO-05 | 240 | Specifically F-29-04 substitution rows (subcategory `exception-mid-cycle-substitution`) |
| EXC-01 | 241 | Affiliate winner-roll row(s) — subcategory `exception-non-VRF-seed` |
| EXC-02 | 241 | Prevrandao-fallback row(s) — subcategory `exception-prevrandao-fallback` |
| EXC-03 | 241 | F-29-04 row(s) — subcategory `exception-mid-cycle-substitution` |
| EXC-04 | 241 | EntropyLib.entropyStep() caller rows — KI Cross-Ref `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]` |
| REG-01 | 242 | Row IDs for any consumer overlapping with F-29-03 / F-29-04 finding subjects |
| REG-02 | 242 | Row IDs for any consumer overlapping with v25.0 / v3.7 / v3.8 RNG findings (via 237-01 reconciliation `confirmed-fresh-matches-prior` rows that cite v25.0 / v3.7 / v3.8 artifacts) |
| FIND-01 / FIND-02 / FIND-03 | 242 | Every row with a `Finding Candidates` entry in any of the three sub-deliverables |

For each requirement, enumerate the specific Row IDs (or "ALL" where that is the correct scope).

**Step 4 — Write `audit/v30-CONSUMER-INVENTORY.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Consumer Inventory — Phase 237 Final Consolidated Deliverable

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-17).
**Requirements satisfied:** INV-01, INV-02, INV-03.
**Status post-commit:** READ-only per D-16. Downstream Phases 238-242 cite Row IDs; they do NOT edit this file. Inventory gaps discovered downstream are recorded as scope-guard deferrals in each phase's own SUMMARY.
**Source sub-deliverables:** assembled from `audit/v30-237-01-UNIVERSE.md` + `audit/v30-237-02-CLASSIFICATION.md` + `audit/v30-237-03-CALLGRAPH.md` (each committed separately per Plan 237-01 / 237-02 / 237-03 Task 2).

## Table of Contents

1. [Path-Family Legend](#path-family-legend)
2. [Subcategory Legend](#subcategory-legend)
3. [Call Graph Conventions](#call-graph-conventions)
4. [Universe List](#universe-list)
5. [Classification Summary](#classification-summary)
6. [KI Cross-Ref Summary](#ki-cross-ref-summary)
7. [Per-Consumer Call Graphs](#per-consumer-call-graphs)
8. [Shared-Prefix Notes](#shared-prefix-notes)
9. [Prior-Artifact Reconciliation](#prior-artifact-reconciliation)
10. [Finding Candidates](#finding-candidates)
11. [Scope-Guard Deferrals](#scope-guard-deferrals)
12. [Consumer Index](#consumer-index)
13. [Attestation](#attestation)

## Path-Family Legend

[Copy verbatim from audit/v30-237-02-CLASSIFICATION.md.]

## Subcategory Legend

[Copy verbatim from audit/v30-237-02-CLASSIFICATION.md.]

## Call Graph Conventions

[Copy verbatim from audit/v30-237-03-CALLGRAPH.md.]

## Universe List

| Row ID | Consumption File:Line | Consumer Function | Path Family | Subcategory | VRF Request Origin File:Line | Fulfillment Site File:Line | Call Graph Ref | KI Cross-Ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| INV-237-001 | contracts/<path>:<line> | <fn> | <family> | <sub> | contracts/<path>:<line> | contracts/<path>:<line> | §INV-237-001 | <KI-ref> | <notes> |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

[Every Row ID from 237-01. Classification columns from 237-02. Call Graph Ref from 237-03 — either inline anchor `§INV-237-NNN` or companion file path. NO TBD-237-02 / TBD-237-03 placeholders. NO `<path>`, `<line>`, `<fn>`, `<family>` token placeholders.]

## Classification Summary

[Copy verbatim from audit/v30-237-02-CLASSIFICATION.md.]

## KI Cross-Ref Summary

[Copy verbatim from audit/v30-237-02-CLASSIFICATION.md.]

## Per-Consumer Call Graphs

[Copy verbatim from audit/v30-237-03-CALLGRAPH.md — including every `### INV-237-NNN` subsection. Each subsection is targeted by the Universe List's Call Graph Ref `§INV-237-NNN` anchor.]

## Shared-Prefix Notes

[Copy verbatim from audit/v30-237-03-CALLGRAPH.md.]

## Prior-Artifact Reconciliation

[Copy the Reconciliation section + Reconciliation Table + Coverage Delta Narrative verbatim from audit/v30-237-01-UNIVERSE.md.]

## Finding Candidates

[Merge the Finding Candidates sections from all three sub-deliverables. Deduplicate identical items. Preserve per-item source attribution (e.g., "— surfaced in 237-01 reconciliation" or "— surfaced in 237-02 classification" or "— surfaced in 237-03 call-graph construction"). NO F-30-NN IDs. If none of the three sub-deliverables surfaced candidates, write "None surfaced across all three Phase 237 sub-plans."]

## Scope-Guard Deferrals

[Merge the Scope-Guard Deferrals sections from all three sub-deliverables. Per-item source attribution. If all three are empty, write "None surfaced."]

## Consumer Index

Per CONTEXT.md D-10: maps every v30.0 requirement ID to the INV-237-NNN Row IDs it will cite. Saves Phases 238-242 planners lookup work.

| Requirement | Phase | Scope | Row IDs |
|---|---|---|---|
| INV-01 | 237 | Universe list (all) | `ALL` (INV-237-001 … INV-237-NNN) |
| INV-02 | 237 | Classification (all) | `ALL` |
| INV-03 | 237 | Call graph (all) | `ALL` |
| BWD-01 | 238 | Per-consumer backward trace | `ALL` |
| BWD-02 | 238 | Per-consumer storage-read enumeration | `ALL` |
| BWD-03 | 238 | Per-consumer adversarial closure (backward) | `ALL` |
| FWD-01 | 238 | Per-consumer consumption-state read universe | `ALL` |
| FWD-02 | 238 | Per-consumer adversarial closure (forward) | `ALL` |
| FWD-03 | 238 | Per-consumer gating verification | `ALL` |
| RNG-01 | 239 | rngLocked state machine | [enumerate Row IDs whose Call Graph Step 2 is `storage-mutation (request commitment)` on rngLockedFlag] |
| RNG-02 | 239 | Permissionless sweep | `ALL` |
| RNG-03 | 239 | Index-advance + phaseTransitionActive asymmetries | [enumerate every `mid-day-lootbox` Row ID + any row citing `phaseTransitionActive`] |
| GO-01 | 240 | Gameover consumer inventory | [enumerate every `gameover-entropy` Row ID + F-29-04 rows that enter the gameover path] |
| GO-02 | 240 | Gameover VRF-available determinism | Same as GO-01 |
| GO-03 | 240 | Gameover state-freeze enumeration | Same as GO-01 |
| GO-04 | 240 | Gameover trigger-timing disproof | Same as GO-01 |
| GO-05 | 240 | F-29-04 scope containment | [enumerate rows with subcategory `exception-mid-cycle-substitution`] |
| EXC-01 | 241 | Affiliate winner-roll exhaustiveness | [enumerate rows with subcategory `exception-non-VRF-seed`] |
| EXC-02 | 241 | Prevrandao-fallback gating | [enumerate rows with subcategory `exception-prevrandao-fallback`] |
| EXC-03 | 241 | F-29-04 scope re-verification | [enumerate rows with subcategory `exception-mid-cycle-substitution`] |
| EXC-04 | 241 | EntropyLib keccak-seed re-verification | [enumerate rows with KI Cross-Ref `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]`] |
| REG-01 | 242 | v29.0 F-29-03 / F-29-04 regression | [enumerate rows whose Notes or Reconciliation rationale mentions F-29-03 / F-29-04] |
| REG-02 | 242 | v25.0 / v3.7 / v3.8 RNG regression | [enumerate rows whose Reconciliation verdict is `confirmed-fresh-matches-prior` against v25.0 / v3.7 / v3.8] |
| FIND-01 | 242 | Consolidated findings | [enumerate every Row ID appearing in `## Finding Candidates`] |
| FIND-02 | 242 | Regression appendix | Same as REG-01 + REG-02 |
| FIND-03 | 242 | KI promotions | [enumerate Row IDs that might qualify for new KI entries] |

Every row's Row IDs column MUST be either `ALL` OR a concrete comma-separated list of `INV-237-NNN` IDs. `TBD` and placeholder tokens are forbidden.

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Source sub-deliverables (all committed before this task started):
  - `audit/v30-237-01-UNIVERSE.md` — Plan 237-01 INV-01 deliverable
  - `audit/v30-237-02-CLASSIFICATION.md` — Plan 237-02 INV-02 deliverable
  - `audit/v30-237-03-CALLGRAPH.md` — Plan 237-03 INV-03 deliverable + any `audit/v30-237-CALLGRAPH-*.md` companion files
- Assembly integrity: Universe List Row ID set in this file is identical to the Row ID set in each of the three sub-deliverables (verified by `diff` of sorted-unique Row ID lists)
- Assembly integrity: no `TBD-237-02` or `TBD-237-03` placeholders remain in this file
- READ-only policy (D-16): source sub-deliverables were NOT modified by this task (verified by `git status --porcelain` empty for their paths)
- READ-only policy (D-18): zero `contracts/` or `test/` writes
```

The consolidated file is large (~1000+ lines depending on row count). Use verbatim copies from source files where possible (Path-Family Legend, Subcategory Legend, Call Graph Conventions, Classification Summary, KI Cross-Ref Summary, Per-Consumer Call Graphs, Shared-Prefix Notes, Prior-Artifact Reconciliation). This reduces transcription error risk.

After writing, commit:
```
git add audit/v30-CONSUMER-INVENTORY.md
git commit -m "docs(237-03): final consolidated v30-CONSUMER-INVENTORY.md per D-08

Assembles the three Phase 237 sub-deliverables into the single authoritative
inventory file for v30.0 downstream Phases 238-242:
  - audit/v30-237-01-UNIVERSE.md (INV-01 universe + reconciliation)
  - audit/v30-237-02-CLASSIFICATION.md (INV-02 path-family + KI cross-ref)
  - audit/v30-237-03-CALLGRAPH.md (INV-03 call graphs)

Universe List has ALL classification columns filled — zero TBD placeholders.

Consumer Index (per D-10) maps every v30.0 requirement (INV / BWD / FWD /
RNG / GO / EXC / REG / FIND — 26 total) to its INV-237-NNN Row-ID subset.
Phases 238-242 can plan without additional discovery.

Source sub-deliverables NOT modified (D-16 READ-only-after-commit).

NO F-30-NN IDs (D-15 — Phase 242 FIND-01..03 owns).
READ-only per D-18. HEAD anchor 7ab515fe (D-17).

Phase 237 (INV-01 + INV-02 + INV-03) complete."
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Path-Family Legend" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Subcategory Legend" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Call Graph Conventions" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Universe List" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Classification Summary" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## KI Cross-Ref Summary" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Per-Consumer Call Graphs" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Shared-Prefix Notes" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Prior-Artifact Reconciliation" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Finding Candidates" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Scope-Guard Deferrals" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Consumer Index" audit/v30-CONSUMER-INVENTORY.md && grep -q "^## Attestation" audit/v30-CONSUMER-INVENTORY.md && [ "$(grep -c '^| INV-237-' audit/v30-CONSUMER-INVENTORY.md)" -ge 40 ] && ! grep -q "TBD-237-02" audit/v30-CONSUMER-INVENTORY.md && ! grep -q "TBD-237-03" audit/v30-CONSUMER-INVENTORY.md && ! grep -q "F-30-" audit/v30-CONSUMER-INVENTORY.md && grep -q "7ab515fe" audit/v30-CONSUMER-INVENTORY.md && grep -q "INV-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "INV-02" audit/v30-CONSUMER-INVENTORY.md && grep -q "INV-03" audit/v30-CONSUMER-INVENTORY.md && grep -q "BWD-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "FWD-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "RNG-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "GO-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "EXC-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "REG-01" audit/v30-CONSUMER-INVENTORY.md && grep -q "FIND-01" audit/v30-CONSUMER-INVENTORY.md && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-237-01-UNIVERSE.md audit/v30-237-02-CLASSIFICATION.md audit/v30-237-03-CALLGRAPH.md 2>/dev/null)" && git log -1 --oneline | grep -qE "237-03.*consolidated.*(v30-CONSUMER-INVENTORY|D-08)"</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-CONSUMER-INVENTORY.md` exists
    - File contains 13 literal section headers in the required order: Path-Family Legend, Subcategory Legend, Call Graph Conventions, Universe List, Classification Summary, KI Cross-Ref Summary, Per-Consumer Call Graphs, Shared-Prefix Notes, Prior-Artifact Reconciliation, Finding Candidates, Scope-Guard Deferrals, Consumer Index, Attestation
    - Universe List has ≥ 40 INV-237-NNN rows with Row ID set IDENTICAL to 237-01 + 237-02 + 237-03 sub-deliverables
    - Zero `TBD-237-02` / `TBD-237-03` placeholders remain — all classification + call-graph-ref columns populated
    - Zero `F-30-` references (D-15)
    - Consumer Index maps all 26 v30.0 requirement IDs (INV-01..03, BWD-01..03, FWD-01..03, RNG-01..03, GO-01..05, EXC-01..04, REG-01..02, FIND-01..03) — verified by 10 grep assertions covering one ID from each series
    - Every Consumer Index row has either `ALL` or a concrete comma-separated list of `INV-237-NNN` IDs in its Row IDs column (no `TBD`, no placeholders)
    - File contains literal `7ab515fe` in the Audit baseline line
    - `audit/v30-237-01-UNIVERSE.md`, `audit/v30-237-02-CLASSIFICATION.md`, `audit/v30-237-03-CALLGRAPH.md` NOT edited (D-16 enforcement)
    - `git status --porcelain contracts/ test/` returns empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty
    - One new commit with subject containing `237-03` + `consolidated` + (`v30-CONSUMER-INVENTORY` OR `D-08`)
    - Commit touches only `audit/v30-CONSUMER-INVENTORY.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
`audit/v30-CONSUMER-INVENTORY.md` — FINAL consolidated Phase 237 deliverable per D-08. Single authoritative file for Phases 238-242. Universe List fully classified + call-graph-referenced; Consumer Index maps all 26 v30.0 requirements to Row IDs; all three sub-deliverables intact and unmodified. Zero F-30-NN IDs. Zero contracts/ or test/ writes. Committed locally; not pushed.
  </deliverable>
  <done>Consolidated inventory file exists; 13 required sections present; Row ID set matches sub-deliverables; no TBD placeholders; Consumer Index covers all 26 v30.0 requirements; sub-deliverables intact (D-16); committed.</done>
</task>

</tasks>

<verification>
- `audit/v30-237-03-CALLGRAPH.md` exists with all 7 required sections (Call Graph Conventions, Shared-Prefix Notes, Per-Consumer Call Graphs, Finding Candidates, Scope-Guard Deferrals, Downstream Hand-offs, Attestation)
- One call-graph entry per INV-237-NNN Row ID from 237-01 — row count matches exactly
- D-11 depth compliance: every graph terminates at consumption; no forward-SSTORE-of-result steps
- D-12 companion-file rule applied: oversized graphs in `audit/v30-237-CALLGRAPH-<slug>.md` files with back-links
- Zero mermaid code fences (D-09 tabular-only)
- `audit/v30-CONSUMER-INVENTORY.md` exists with all 13 required sections
- Universe List in consolidated file: ≥ 40 rows; zero TBD-237-02 / TBD-237-03 placeholders
- Consumer Index (D-10): maps all 26 v30.0 requirements (INV/BWD/FWD/RNG/GO/EXC/REG/FIND) to Row IDs
- Zero F-30-NN references in either deliverable (D-15)
- Zero contracts/ or test/ writes (D-18)
- Zero edits to `audit/v30-237-01-UNIVERSE.md` and `audit/v30-237-02-CLASSIFICATION.md` (D-16)
- HEAD anchor 7ab515fe attested; `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty
- Both deliverables committed; not pushed
</verification>

<success_criteria>
INV-03 satisfied: every Universe List row has a per-consumer call graph from VRF request origination → `rawFulfillRandomWords` → intermediate storage touchpoints → consumption site, stopping at consumption per D-11. Delegatecall + library hops traced per D-11. Oversized graphs in companion files per D-12.

D-08 satisfied: `audit/v30-CONSUMER-INVENTORY.md` is the single authoritative file containing all five expected sections plus the Consumer Index mapping v30.0 requirements to Row IDs. Phases 238-242 planners inherit their scope anchor without additional discovery. The three sub-deliverables remain intact and committed separately — the consolidated file is a new assembly, not a replacement.

Phase 237 complete: INV-01 (237-01) + INV-02 (237-02) + INV-03 (237-03) all satisfied. Downstream phase scope locked.
</success_criteria>

<output>
After completion, create `.planning/phases/237-vrf-consumer-inventory-call-graph/237-03-SUMMARY.md`
</output>
</content>
