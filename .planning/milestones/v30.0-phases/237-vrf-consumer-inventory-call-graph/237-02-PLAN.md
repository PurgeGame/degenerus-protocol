---
phase: 237-vrf-consumer-inventory-call-graph
plan: 237-02
type: execute
wave: 2
depends_on: [237-01]
files_modified:
  - audit/v30-237-02-CLASSIFICATION.md
autonomous: true
requirements: [INV-02]
requirements_addressed: [INV-02]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-237-02-CLASSIFICATION.md` contains a ## Classification Table with one classification row per INV-237-NNN Universe List row from 237-01 (row count matches 237-01 exactly — no rows added or dropped)"
    - "Per D-04: every row's Path Family column is exactly one of the 5 locked values: `daily`, `mid-day-lootbox`, `gap-backfill`, `gameover-entropy`, `other`. No other vocabulary is permitted."
    - "Per D-05 taxonomy rules applied:\n  - `daily` ← coinflip outcome / daily lootbox ticket resolution / boons at daily VRF fulfillment / quest resolution tied to daily RNG\n  - `mid-day-lootbox` ← mid-day lootbox VRF fulfillment + all downstream consumers (trait rolls, rarity, BAF sentinel emission, bonus outcomes)\n  - `gap-backfill` ← `_backfillGapDays` + `keccak256(vrfWord, gapDay)` derivation + orphaned lootbox index fallback\n  - `gameover-entropy` ← `_gameOverEntropy` + every consumer of the gameover VRF word (winner selection, trait rolls, terminal drain, final burn/coinflip resolution, sweep distribution)\n  - `other` ← anything that doesn't fit cleanly — MUST carry a named subcategory"
    - "Per D-04: every `other` row MUST have a non-empty Subcategory AND a one-line justification in the Justification column. Permitted subcategory vocabulary (extensible per D-05 rule): `exception-non-VRF-seed`, `exception-prevrandao-fallback`, `exception-mid-cycle-substitution`, `library-wrapper`, `fulfillment-callback`, `request-origination` — plus any new subcategory the executor adds WITH a named justification."
    - "Per D-06: KI Cross-Ref column populated on every inventory row that maps to a KNOWN-ISSUES.md RNG-exception entry, using the exact quoted KI header format: `[KI: \"Non-VRF entropy for affiliate winner roll\"]`, `[KI: \"Gameover prevrandao fallback\"]`, `[KI: \"Gameover RNG substitution for mid-cycle write-buffer tickets\"]`, `[KI: \"EntropyLib XOR-shift PRNG for lootbox outcome rolls\"]`, `[KI: \"Lootbox RNG uses index advance isolation instead of rngLockedFlag\"]`. Rows with no KI linkage carry `N/A` in KI Cross-Ref."
    - "A ## Path-Family Legend section is present, reproducing the D-04/D-05 definitions in tabular form, and a ## Subcategory Legend section is present enumerating every subcategory used in the Classification Table with its definition."
    - "A ## Classification Summary section is present, giving the row count per Path Family (daily N / mid-day-lootbox N / gap-backfill N / gameover-entropy N / other N, summing to the total Universe List row count)"
    - "A ## KI Cross-Ref Summary section is present, listing for each of the 5 KI RNG entries: KI header (quoted), the INV-237-NNN rows mapped to it, a one-line rationale. Confirms D-06 hand-off to Phase 241 EXC-01..04."
    - "Per D-15: NO F-30-NN finding IDs appear. A ## Finding Candidates section enumerates any classification edge case (e.g., a consumer that genuinely doesn't fit any path family and requires an `other` subcategory with named justification) + any ambiguity worth Phase 242 review. Format: bullet list with file:line + rationale + suggested severity."
    - "Per D-18: zero contracts/ or test/ writes. Verified via `git status --porcelain contracts/ test/` empty before and after each task."
    - "HEAD anchor `7ab515fe` locked in frontmatter per D-17; verified via `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty."
    - "Row-ID integrity: every Classification Table Row ID also appears in `audit/v30-237-01-UNIVERSE.md`; no orphan Row IDs; no dropped Row IDs. Verified via `diff <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-01-UNIVERSE.md | sort -u) <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-02-CLASSIFICATION.md | sort -u)` returns empty."
  artifacts:
    - path: "audit/v30-237-02-CLASSIFICATION.md"
      provides: "INV-02 path-family classification + subcategory + KI cross-ref for every INV-237-NNN Universe List row"
      contains: "## Classification Table"
  key_links:
    - from: "audit/v30-237-01-UNIVERSE.md Universe List rows (INV-237-NNN)"
      to: "audit/v30-237-02-CLASSIFICATION.md Classification Table rows"
      via: "1:1 row-for-row classification — same Row ID set, no adds, no drops"
      pattern: "INV-237-[0-9]{3}"
    - from: "KNOWN-ISSUES.md 5 RNG exception entries (quoted headers)"
      to: "Classification Table KI Cross-Ref column + ## KI Cross-Ref Summary section"
      via: "D-06 inventory-row cross-ref using exact quoted KI header"
      pattern: "\\[KI: \""
    - from: "CONTEXT.md D-04/D-05 (5 path families + subcategory rules)"
      to: "Classification Table Path Family + Subcategory columns + Path-Family Legend + Subcategory Legend"
      via: "D-04/D-05 locked vocabulary"
      pattern: "daily|mid-day-lootbox|gap-backfill|gameover-entropy|other"
---

<objective>
INV-02 classification pass — type every Universe List row from Plan 237-01 into exactly one of the 5 locked path families (`daily` / `mid-day-lootbox` / `gap-backfill` / `gameover-entropy` / `other`) per CONTEXT.md D-04/D-05, with named subcategory for every `other` row per D-04, and KI Cross-Ref column populated for every row mapping to a KNOWN-ISSUES.md RNG exception per D-06.

Output: `audit/v30-237-02-CLASSIFICATION.md` — standalone sub-deliverable. Plan 237-03 will merge 237-01 + 237-02 + 237-03 outputs into the final `audit/v30-CONSUMER-INVENTORY.md`.

**Input contract:** `audit/v30-237-01-UNIVERSE.md` provides the authoritative row list with `TBD-237-02` placeholders in the Path Family, Subcategory, and KI Cross-Ref columns. This plan produces a separate file that REPLACES those placeholders — it does NOT edit `audit/v30-237-01-UNIVERSE.md` in place (per D-16 READ-only-after-commit rule on Phase 237 outputs).

**Parallelism:** Runs in Wave 2 alongside Plan 237-03 (call graph). No cross-dependency between 237-02 and 237-03 — each reads 237-01 independently and writes to its own file. 237-03's final task consolidates all three into `audit/v30-CONSUMER-INVENTORY.md`.

**Finding-ID emission:** NO `F-30-NN` IDs per D-15. Finding candidates (e.g., a consumer genuinely outside the 4 named families that demands an `other` subcategory) go into a `## Finding Candidates` section for Phase 242 routing.

Scope: READ-only per D-18. HEAD anchor `7ab515fe` locked per D-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/237-vrf-consumer-inventory-call-graph/237-CONTEXT.md
@audit/v30-237-01-UNIVERSE.md
@KNOWN-ISSUES.md
</context>

<threat_model>
READ-only audit milestone: no runtime code modified, no test modified. Attack surface added: NONE. Only writes: markdown under `audit/v30-237-02-CLASSIFICATION.md` (read-only post-commit per D-16). No STRIDE category applies. Inherits parent phase summary.

Residual risk: a classification error propagates into downstream scope definitions — e.g., classifying a gameover consumer as `daily` would exclude it from Phase 240 GO-01's scope. Mitigation: Path-Family Legend + Subcategory Legend make the classification rubric auditable ex-post; Classification Summary provides a per-family count sanity check against CONTEXT.md expectations; KI Cross-Ref Summary ensures every KI exception is traceable to Phase 241 proof subjects.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Classify every Universe List row into path family + subcategory + KI Cross-Ref → audit/v30-237-02-CLASSIFICATION.md</name>
  <files>audit/v30-237-02-CLASSIFICATION.md</files>
  <read_first>
    - audit/v30-237-01-UNIVERSE.md (authoritative Row ID list — Universe List section. MUST be present and committed before this task starts.)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-CONTEXT.md (D-04 5-family taxonomy lock; D-05 per-family rules; D-06 KI inventory-row cross-ref; D-15 no F-30-NN; D-16 READ-only-after-commit — do NOT edit 237-01 output; D-18 READ-only)
    - KNOWN-ISSUES.md — specifically the 5 RNG exception entries (for the KI Cross-Ref column):
        1. "Non-VRF entropy for affiliate winner roll"
        2. "Gameover prevrandao fallback"
        3. "Gameover RNG substitution for mid-cycle write-buffer tickets"
        4. "EntropyLib XOR-shift PRNG for lootbox outcome rolls"
        5. "Lootbox RNG uses index advance isolation instead of rngLockedFlag"
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — for confirming path-family assignment of consumers located in this file: `_gameOverEntropy`, `_getHistoricalRngFallback`, `_backfillGapDays`, `_swapAndFreeze`, `_swapTicketSlot`, `rngGate`, `_finalizeRngRequest`, `rawFulfillRandomWords`, `advanceGame`, `_processFutureTicketBatch`, `_prepareFutureTickets`, `_handleGameOverPath`
    - contracts/modules/DegenerusGameLootboxModule.sol at HEAD 7ab515fe — for mid-day-lootbox path-family assignment (rarity rolls, ticket counts, BURNIE amounts, boon rolls, index-advance isolation sites); also for "Lootbox RNG uses index advance isolation" KI cross-ref application
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD 7ab515fe — for daily / gameover / purchase-phase consumer classification (hash2/keccak sites from the 230-02 addendum list)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD 7ab515fe — for entropy-passthrough consumers (`processFutureTicketBatch`, `_raritySymbolBatch`, `_rollRemainder`) — classify by the triggering path (daily context vs gameover context per D-03)
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD 7ab515fe — for gameover-entropy classification
    - contracts/modules/DegenerusGameBoonModule.sol at HEAD 7ab515fe — for daily-VRF-derived boon rolls (daily classification)
    - contracts/modules/DegenerusGameDecimatorModule.sol + DegenerusGameDegeneretteModule.sol + DegenerusGameWhaleModule.sol + DegenerusGameMintStreakUtils.sol + DegenerusGamePayoutUtils.sol at HEAD 7ab515fe — for classifying auxiliary consumers (auto-rebuy, whale claim, degenerette/decimator specific RNG)
    - contracts/DegenerusAffiliate.sol at HEAD 7ab515fe — for KI-exception row classification (`other` + `exception-non-VRF-seed` + KI Cross-Ref to "Non-VRF entropy for affiliate winner roll")
    - contracts/BurnieCoinflip.sol at HEAD 7ab515fe — for daily path-family classification of coinflip outcome consumption
    - contracts/DegenerusQuests.sol at HEAD 7ab515fe — for daily path-family (quest resolution tied to daily RNG)
    - contracts/libraries/EntropyLib.sol at HEAD 7ab515fe — for classifying EntropyLib.entropyStep() callers (classified by caller site per D-02; KI Cross-Ref to "EntropyLib XOR-shift PRNG")
    - contracts/libraries/BitPackingLib.sol + JackpotBucketLib.sol + GameTimeLib.sol + PriceLookupLib.sol at HEAD 7ab515fe — for library-wrapper classification if any of their functions appear as consumer rows
  </read_first>
  <action>
Execute the INV-02 classification pass per CONTEXT.md D-04/D-05/D-06.

**Verify prerequisites:**
```
test -f audit/v30-237-01-UNIVERSE.md
git log --oneline -1 -- audit/v30-237-01-UNIVERSE.md   # MUST show plan 237-01's Task 3 commit
git diff --name-only 7ab515fe..HEAD -- contracts/ test/
```
The third command MUST return empty. If not empty, STOP — baseline drift; record a scope-guard deferral.

**Step 1 — Read the Universe List from `audit/v30-237-01-UNIVERSE.md`.** Extract all `INV-237-NNN` Row IDs + their Consumption File:Line + Consumer Function + Notes. This is the authoritative row set.

**Step 2 — For each row, determine Path Family per D-04/D-05 taxonomy rules:**

Decision procedure (apply in this exact order — first match wins):

1. **If the consumer is reached from `_getHistoricalRngFallback` → Path Family = `other`, Subcategory = `exception-prevrandao-fallback`, KI Cross-Ref = `[KI: "Gameover prevrandao fallback"]`.** Justification: "KI-exception path — prevrandao-derived entropy, not VRF-backed."

2. **If the consumer is the affiliate winner roll (non-VRF deterministic seed) in `contracts/DegenerusAffiliate.sol` → Path Family = `other`, Subcategory = `exception-non-VRF-seed`, KI Cross-Ref = `[KI: "Non-VRF entropy for affiliate winner roll"]`.** Justification: "KI-exception path — deterministic seed, not VRF-backed."

3. **If the consumer is one of the F-29-04 write-buffer substitution sites (`_swapAndFreeze` at AdvanceModule:292, `_swapTicketSlot` at AdvanceModule:1082, OR any consumer that reads a ticket slot whose RNG word was substituted at gameover per the F-29-04 KI entry) → Path Family = `other`, Subcategory = `exception-mid-cycle-substitution`, KI Cross-Ref = `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]`.** Justification: "KI-exception path — terminal-state mid-cycle substitution (F-29-04)." Note: the `_gameOverEntropy` site itself (AdvanceModule:1222-1246) is classified per rule #4 below as `gameover-entropy`; rule #3 applies ONLY to the write-buffer-swap sites + any consumers that specifically read the substituted ticket buffer.

4. **If the consumer is `_gameOverEntropy` itself OR a downstream consumer of the gameover VRF word (gameover jackpot winner selection, gameover trait rolls, terminal ticket drain, final-day burn/coinflip resolution, sweep distribution) → Path Family = `gameover-entropy`, Subcategory = `-` (no subcategory required for non-`other` families; leave blank or `-`), KI Cross-Ref = `N/A`.** Justification: "Gameover-phase VRF-word consumer." Note: per D-03, if the same helper function is called from both `gameover-entropy` AND another family (dual-trigger), 237-01 should have already expanded it into 2+ rows — each row gets its own classification here.

5. **If the consumer is `_backfillGapDays` OR a consumer of `keccak256(vrfWord, gapDay)`-derived entropy OR an orphaned-lootbox-index fallback consumer → Path Family = `gap-backfill`, Subcategory = `-`, KI Cross-Ref = `N/A`.** Justification: "Gap-backfill entropy derivation."

6. **If the consumer is in the mid-day lootbox RNG fulfillment path (`requestLootboxRng`, `lootboxRngWordByIndex[...]` consumers, trait rolls, rarity, BAF sentinel, bonus outcomes downstream of mid-day VRF) → Path Family = `mid-day-lootbox`, Subcategory = `-`, KI Cross-Ref = `[KI: "Lootbox RNG uses index advance isolation instead of rngLockedFlag"]` IF the consumer's isolation mechanism is the index-advance (not rngLockedFlag) — which applies to the whole mid-day-lootbox family per KI. Justification: "Mid-day lootbox VRF consumer; gated by index-advance isolation per KI."**

7. **If the consumer is in the daily VRF path (coinflip outcome, daily lootbox ticket resolution, boons at daily VRF fulfillment, quest resolution tied to daily RNG, daily jackpot entropy consumers, any consumer reached through `rngGate` → `rngWordCurrent` on the daily advance path) → Path Family = `daily`, Subcategory = `-`, KI Cross-Ref = `N/A`.** Justification: "Daily VRF consumer."

8. **If the consumer is a VRF-fulfillment callback (`rawFulfillRandomWords`) OR a VRF-request origination (`requestRandomWords` via the coordinator) OR a library wrapper (e.g., the EntropyLib.hash2 helper itself — although D-02 says callers are rows, not the library internals; this rule applies if 237-01 included any library-scope row) → Path Family = `other`, Subcategory = `fulfillment-callback` / `request-origination` / `library-wrapper` (pick whichever fits), KI Cross-Ref = `N/A`.** Justification: "Infrastructure: VRF callback / request / library-wrapper — not a path-family consumer per D-11 depth rule; recorded for completeness."

9. **If the consumer is an `EntropyLib.entropyStep()` caller → Path Family follows from the CALLING site's family per D-02; KI Cross-Ref = `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]`.** Note: D-02 classification by caller means most entropyStep callers land in `mid-day-lootbox` (lootbox outcome rolls) but a caller in a daily/gameover context gets the corresponding family. The KI cross-ref applies regardless because the PRNG property is the KI-exception subject.

10. **If none of rules 1-9 match → Path Family = `other`, Subcategory = `<new-subcategory-name-derived-from-function-semantics>`, KI Cross-Ref = `N/A`.** Justification: "Does not fit daily / mid-day-lootbox / gap-backfill / gameover-entropy families. Surfaced as finding candidate for Phase 242 routing."

**Step 3 — For dual-trigger rows (D-03) already expanded by 237-01:** honour the per-row Notes column's trigger-context hint when assigning family. The SAME function may appear as one `daily` row and one `gameover-entropy` row if 237-01 expanded it per D-03.

**Step 4 — Cross-check: every KI exception entry has at least one corresponding Classification Table row:**
- "Non-VRF entropy for affiliate winner roll" ← at least 1 row in contracts/DegenerusAffiliate.sol
- "Gameover prevrandao fallback" ← at least 1 row involving `_getHistoricalRngFallback`
- "Gameover RNG substitution for mid-cycle write-buffer tickets" ← at least 1 row for each of AdvanceModule:292 / :1082 and/or consumers
- "EntropyLib XOR-shift PRNG for lootbox outcome rolls" ← at least 1 row per caller of `EntropyLib.entropyStep()`
- "Lootbox RNG uses index advance isolation instead of rngLockedFlag" ← KI Cross-Ref populated on EVERY `mid-day-lootbox` row

If any KI has ZERO matching rows in the Universe List, surface in Finding Candidates with rationale "KI exception has no corresponding universe row — potential 237-01 gap or KI drift."

**Step 5 — Write `audit/v30-237-02-CLASSIFICATION.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Phase 237 Plan 02 — INV-02 Path-Family Classification

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-17).
**Requirement addressed:** INV-02 — path-family classification + subcategory + KI cross-ref for every Universe List row from Plan 237-01.
**Input:** `audit/v30-237-01-UNIVERSE.md` (Universe List, INV-237-NNN rows).
**Output:** this file — replaces `TBD-237-02` placeholders in the Path Family / Subcategory / KI Cross-Ref columns. Does NOT edit `audit/v30-237-01-UNIVERSE.md` (per D-16 READ-only-after-commit). Final consolidated `audit/v30-CONSUMER-INVENTORY.md` is Plan 237-03's assembly output.
**Vocabulary (locked per D-04/D-05):** Path Family ∈ `{daily, mid-day-lootbox, gap-backfill, gameover-entropy, other}`. `other` MUST carry a named Subcategory.

## Path-Family Legend

| Path Family | Definition (per D-05) |
|---|---|
| `daily` | Coinflip outcome / daily lootbox ticket resolution / boons at daily VRF fulfillment / quest resolution tied to daily RNG / consumers reached via `rngGate` → `rngWordCurrent` on the daily advance path |
| `mid-day-lootbox` | Mid-day lootbox VRF fulfillment + all downstream consumers (trait rolls, rarity, BAF sentinel emission, bonus outcomes). All rows in this family carry `[KI: "Lootbox RNG uses index advance isolation instead of rngLockedFlag"]` per D-06. |
| `gap-backfill` | `_backfillGapDays` + `keccak256(vrfWord, gapDay)` derivation + orphaned lootbox index fallback |
| `gameover-entropy` | `_gameOverEntropy` + every consumer of the gameover VRF word (winner selection, trait rolls, terminal drain, final burn/coinflip resolution, sweep distribution) |
| `other` | Anything that doesn't fit cleanly. MUST carry a named Subcategory + one-line justification per D-04. |

## Subcategory Legend

| Subcategory | Applies To | Definition |
|---|---|---|
| `exception-non-VRF-seed` | `other` | KI-exception: deterministic-seed consumer, not VRF-backed (affiliate winner roll only per KI). KI Cross-Ref: `[KI: "Non-VRF entropy for affiliate winner roll"]`. |
| `exception-prevrandao-fallback` | `other` | KI-exception: prevrandao-derived entropy in `_getHistoricalRngFallback` after 14-day VRF outage. KI Cross-Ref: `[KI: "Gameover prevrandao fallback"]`. |
| `exception-mid-cycle-substitution` | `other` | KI-exception: F-29-04 terminal-state write-buffer RNG substitution. KI Cross-Ref: `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]`. |
| `library-wrapper` | `other` | EntropyLib / BitPackingLib / JackpotBucketLib / GameTimeLib / PriceLookupLib helper recorded as consumer for completeness per D-11 |
| `fulfillment-callback` | `other` | `rawFulfillRandomWords` VRF callback site — the terminal fulfillment of every VRF-backed consumer row |
| `request-origination` | `other` | `requestRandomWords` VRF origination site — the start of every VRF-backed consumer row's chain |
| [any other subcategory introduced during classification] | `other` | [definition provided by executor with justification] |

## Classification Table

| Row ID | Consumption File:Line | Consumer Function | Path Family | Subcategory | KI Cross-Ref | Justification |
|---|---|---|---|---|---|---|
| INV-237-001 | contracts/<path>:<line> | <fn> | <family> | <sub-or-dash> | <KI-ref-or-N/A> | <one-line justification per D-04/D-05> |
| ... | ... | ... | ... | ... | ... | ... |

[Every row from 237-01 Universe List appears here exactly once. Row count matches 237-01 exactly.]

## Classification Summary

| Path Family | Row Count | Expected Range (CONTEXT heuristic) |
|---|---|---|
| `daily` | N | ~30-50% of total rows (daily VRF is the dominant path) |
| `mid-day-lootbox` | N | ~20-30% of total rows |
| `gap-backfill` | N | ~5-10% of total rows (narrow scope) |
| `gameover-entropy` | N | ~10-20% of total rows |
| `other` | N | ~5-15% of total rows (KI exceptions + infrastructure) |
| **Total** | **N** | Matches Universe List row count |

[Verify the row counts sum to the Universe List total. If any family's percentage falls wildly outside the heuristic range, surface in Finding Candidates as a sanity-check flag.]

## KI Cross-Ref Summary

| KI Header (quoted) | Matching Row IDs | Rationale |
|---|---|---|
| `"Non-VRF entropy for affiliate winner roll"` | INV-237-NNN, ... | Affiliate winner-roll deterministic seed — Phase 241 EXC-01 proof subject. |
| `"Gameover prevrandao fallback"` | INV-237-NNN, ... | `_getHistoricalRngFallback` prevrandao mix — Phase 241 EXC-02 proof subject. |
| `"Gameover RNG substitution for mid-cycle write-buffer tickets"` | INV-237-NNN, ... | F-29-04 terminal-state substitution — Phase 241 EXC-03 proof subject. |
| `"EntropyLib XOR-shift PRNG for lootbox outcome rolls"` | INV-237-NNN, ... | Every caller of `EntropyLib.entropyStep()` — Phase 241 EXC-04 proof subject. |
| `"Lootbox RNG uses index advance isolation instead of rngLockedFlag"` | INV-237-NNN, ... (every `mid-day-lootbox` row) | Gating asymmetry — Phase 239 RNG-03 re-justification subject. |

**Coverage attestation:** every KI RNG-exception entry has at least one matching Universe List row (confirming D-06 inventory-row cross-ref completeness). If any KI has zero rows, it is flagged in Finding Candidates.

## Finding Candidates

[Per D-15. Any classification edge case: a consumer that fell through rules 1-9 and got `other` with a new subcategory (i.e., a genuinely novel path); a KI exception with zero matching rows (inventory gap); a classification ambiguity where the executor had to make a discretionary call (document the call + alternative interpretation). Format: bullet list — `- INV-237-NNN — contracts/<path>:<line> — <fn> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH>`. NO F-30-NN IDs. If none, write "None surfaced during classification."]

## Scope-Guard Deferrals

[Per D-16. Any gap requiring edit of `audit/v30-237-01-UNIVERSE.md` (e.g., 237-01 produced a row that cannot be classified because its Consumer Function name is wrong) is recorded here — `audit/v30-237-01-UNIVERSE.md` is NOT edited. If none, "None surfaced."]

## Downstream Hand-offs

- **Plan 237-03:** reads this file's Classification Table to assign `gameover-entropy` / `mid-day-lootbox` / `daily` rows to Plan 237-03's Consumer Index subsets. Consolidates this file + 237-01 + its own output into `audit/v30-CONSUMER-INVENTORY.md`.
- **Phase 238 BWD/FWD:** uses the Path Family classification to split its plans by family (Phase 238 plan split is likely `daily` / `mid-day-lootbox` / `gap-backfill` / `gameover-entropy` / `other` per ROADMAP "may split by consumer family").
- **Phase 239 RNG-02:** uses the `mid-day-lootbox` family + the `[KI: "Lootbox RNG uses index advance isolation..."]` cross-ref to bound the permissionless-sweep classification scope.
- **Phase 240 GO-01:** uses the `gameover-entropy` family AS its consumer inventory (GO-01 is a subset of INV-01 filtered to this family).
- **Phase 241 EXC-01..04:** uses the KI Cross-Ref column + KI Cross-Ref Summary to bound each EXC requirement's proof subjects.
- **Phase 242 FIND-01..03:** consumes this file's Finding Candidates.

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Input file `audit/v30-237-01-UNIVERSE.md` was present and committed before this task started
- Input Row ID integrity: `grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-01-UNIVERSE.md | sort -u` produced N Row IDs; this Classification Table contains the same N Row IDs. No orphans, no drops.
- Contract source reads limited to `contracts/**/*.sol` at HEAD `7ab515fe`
- `audit/v30-237-01-UNIVERSE.md` is NOT modified by this task (per D-16)
```

Every Classification Table row MUST:
- Have Row ID matching `INV-237-[0-9]{3}` regex AND appearing in `audit/v30-237-01-UNIVERSE.md`
- Path Family strictly ∈ `{daily, mid-day-lootbox, gap-backfill, gameover-entropy, other}`
- Subcategory non-empty if Path Family = `other`; `-` or blank for other families
- KI Cross-Ref either `N/A` or `[KI: "<exact quoted header>"]` — no free-form cross-refs
- Justification non-empty (≥ 5 words)
- Never contain F-30-NN
- Never contain placeholder tokens `<path>`, `<line>`, `<fn`, `<family>`, `<sub-or-dash>`, `<KI-ref-or-N/A>`

READ-only per D-18. Only writes: `audit/v30-237-02-CLASSIFICATION.md`. `audit/v30-237-01-UNIVERSE.md` is NOT edited. `git status --porcelain contracts/ test/` MUST be empty after the write.
  </action>
  <verify>
    <automated>test -f audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Path-Family Legend" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Subcategory Legend" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Classification Table" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Classification Summary" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## KI Cross-Ref Summary" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Finding Candidates" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Scope-Guard Deferrals" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Downstream Hand-offs" audit/v30-237-02-CLASSIFICATION.md && grep -q "^## Attestation" audit/v30-237-02-CLASSIFICATION.md && [ "$(grep -c '^| INV-237-' audit/v30-237-02-CLASSIFICATION.md)" -ge 40 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-01-UNIVERSE.md | sort -u | wc -l)" = "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-02-CLASSIFICATION.md | sort -u | wc -l)" ] && grep -q "^| INV-237-.* | daily |" audit/v30-237-02-CLASSIFICATION.md && grep -q "^| INV-237-.* | mid-day-lootbox |" audit/v30-237-02-CLASSIFICATION.md && grep -q "^| INV-237-.* | gameover-entropy |" audit/v30-237-02-CLASSIFICATION.md && grep -q "^| INV-237-.* | other |" audit/v30-237-02-CLASSIFICATION.md && grep -q "\\[KI: \"Non-VRF entropy for affiliate winner roll\"\\]" audit/v30-237-02-CLASSIFICATION.md && grep -q "\\[KI: \"Gameover prevrandao fallback\"\\]" audit/v30-237-02-CLASSIFICATION.md && grep -q "\\[KI: \"Gameover RNG substitution for mid-cycle write-buffer tickets\"\\]" audit/v30-237-02-CLASSIFICATION.md && grep -q "\\[KI: \"EntropyLib XOR-shift PRNG for lootbox outcome rolls\"\\]" audit/v30-237-02-CLASSIFICATION.md && grep -q "\\[KI: \"Lootbox RNG uses index advance isolation instead of rngLockedFlag\"\\]" audit/v30-237-02-CLASSIFICATION.md && ! grep -q "F-30-" audit/v30-237-02-CLASSIFICATION.md && ! grep -q "TBD-237-02" audit/v30-237-02-CLASSIFICATION.md && ! grep -q "<line>" audit/v30-237-02-CLASSIFICATION.md && ! grep -q "<path>" audit/v30-237-02-CLASSIFICATION.md && grep -q "7ab515fe" audit/v30-237-02-CLASSIFICATION.md && test -z "$(git status --porcelain contracts/ test/)" && [ -z "$(git status --porcelain audit/v30-237-01-UNIVERSE.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-237-02-CLASSIFICATION.md` exists
    - File contains literal headers in this order: `## Path-Family Legend`, `## Subcategory Legend`, `## Classification Table`, `## Classification Summary`, `## KI Cross-Ref Summary`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Downstream Hand-offs`, `## Attestation`
    - Classification Table contains same Row ID set as Universe List — counts match, `diff <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-01-UNIVERSE.md | sort -u) <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-237-02-CLASSIFICATION.md | sort -u)` returns empty
    - At least one row exists per each of the 5 path families: `daily`, `mid-day-lootbox`, `gap-backfill`, `gameover-entropy`, `other` — verified by 5 grep assertions (see automated verify)
    - Every `other` row has a non-empty Subcategory value from the Subcategory Legend
    - All 5 KI exception quoted headers appear at least once in the KI Cross-Ref column or KI Cross-Ref Summary — verified by 5 grep assertions
    - File does NOT contain `F-30-` (any form)
    - File does NOT contain `TBD-237-02` placeholders (this plan's job is to REPLACE those)
    - File does NOT contain placeholder tokens `<line>`, `<path>`, `<fn`, `<family>`, `<sub-or-dash>`, `<KI-ref-or-N/A>`
    - File contains literal `7ab515fe` in the Audit baseline line
    - `git status --porcelain contracts/ test/` returns empty
    - `git status --porcelain audit/v30-237-01-UNIVERSE.md` returns empty (THIS plan does NOT edit 237-01's output — D-16 compliance)
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty
  </acceptance_criteria>
  <deliverable>
`audit/v30-237-02-CLASSIFICATION.md` — INV-02 classification deliverable: Path-Family Legend + Subcategory Legend + 1:1 classification of every Universe List row + Classification Summary counts + KI Cross-Ref Summary (all 5 KI entries mapped) + Finding Candidates + Scope-Guard Deferrals + Downstream Hand-offs + Attestation. TBD placeholders from 237-01 replaced. Zero F-30-NN IDs. Zero contracts/ or test/ writes. 237-01's output file unmodified.
  </deliverable>
  <done>Classification file exists; 9 required sections present; every Universe Row classified with Path Family + Subcategory (for `other`) + KI Cross-Ref; at least one row per path family; all 5 KI entries represented; 237-01 output untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit/v30-237-02-CLASSIFICATION.md</name>
  <files>audit/v30-237-02-CLASSIFICATION.md</files>
  <read_first>
    - audit/v30-237-02-CLASSIFICATION.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `audit/v30-237-02-CLASSIFICATION.md` only.

```
git add audit/v30-237-02-CLASSIFICATION.md
git commit -m "docs(237-02): INV-02 path-family classification at HEAD 7ab515fe

Classifies every INV-237-NNN Universe List row from Plan 237-01 into one
of the 5 locked path families per D-04/D-05: daily / mid-day-lootbox /
gap-backfill / gameover-entropy / other. Every 'other' row carries a
named Subcategory + one-line justification per D-04.

KI Cross-Ref column populated per D-06 using exact quoted KI headers
for all 5 RNG-exception entries:
  - \"Non-VRF entropy for affiliate winner roll\"
  - \"Gameover prevrandao fallback\"
  - \"Gameover RNG substitution for mid-cycle write-buffer tickets\"
  - \"EntropyLib XOR-shift PRNG for lootbox outcome rolls\"
  - \"Lootbox RNG uses index advance isolation instead of rngLockedFlag\"

Classification Summary confirms per-family row distribution. KI Cross-Ref
Summary confirms every KI entry has matching Universe rows (Phase 241
EXC-01..04 proof-subject handoff).

237-01 output (audit/v30-237-01-UNIVERSE.md) NOT edited (D-16 READ-only-
after-commit). Plan 237-03 will merge 237-01 + 237-02 + 237-03 into the
final audit/v30-CONSUMER-INVENTORY.md.

NO F-30-NN IDs (D-15). READ-only per D-18. HEAD anchor 7ab515fe (D-17)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "237-02.*(INV-02|classification).*7ab515fe" && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-237-01-UNIVERSE.md)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `237-02` + (`INV-02` OR `classification`) + `7ab515fe`
    - Commit touches only `audit/v30-237-02-CLASSIFICATION.md`
    - `git status --porcelain contracts/ test/` empty
    - `git status --porcelain audit/v30-237-01-UNIVERSE.md` empty (237-01's output untouched)
    - Not pushed
  </acceptance_criteria>
  <deliverable>Approved `audit/v30-237-02-CLASSIFICATION.md` committed; local-only.</deliverable>
  <done>Commit exists; contracts/ test/ unmodified; 237-01 output unmodified; INV-02 deliverable committed.</done>
</task>

</tasks>

<verification>
- `audit/v30-237-02-CLASSIFICATION.md` exists with all 9 required sections: Path-Family Legend, Subcategory Legend, Classification Table, Classification Summary, KI Cross-Ref Summary, Finding Candidates, Scope-Guard Deferrals, Downstream Hand-offs, Attestation
- Classification Table row count matches `audit/v30-237-01-UNIVERSE.md` Universe List row count exactly; Row ID sets are identical
- Every row has Path Family ∈ {daily, mid-day-lootbox, gap-backfill, gameover-entropy, other}
- Every `other` row has a non-empty Subcategory from the Subcategory Legend
- At least one row per each of the 5 path families
- All 5 KNOWN-ISSUES RNG-exception quoted headers appear in the KI Cross-Ref Summary
- KI Cross-Ref column uses exact `[KI: "<quoted header>"]` format or `N/A`
- Classification Summary row counts sum to total Universe List count
- Zero F-30-NN references (D-15)
- Zero TBD-237-02 placeholders remaining (this plan's job is to replace them)
- Zero contracts/ or test/ writes (D-18)
- Zero edits to `audit/v30-237-01-UNIVERSE.md` (D-16)
- HEAD anchor 7ab515fe attested and verified via `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty
- Deliverable committed; not pushed
</verification>

<success_criteria>
Every INV-237-NNN row from Plan 237-01 has a locked path-family classification, with `other` rows carrying named subcategories and every KI-exception row cross-referenced to the exact KNOWN-ISSUES.md header. Phase 241 EXC-01..04 inherits its proof-subject set directly from the KI Cross-Ref Summary. Phase 238 BWD/FWD can split plans by path family. Phase 240 GO-01 inherits its consumer inventory from the `gameover-entropy` subset. The classification rubric is auditable ex-post via the Path-Family and Subcategory Legends.
</success_criteria>

<output>
After completion, create `.planning/phases/237-vrf-consumer-inventory-call-graph/237-02-SUMMARY.md`
</output>
</content>
