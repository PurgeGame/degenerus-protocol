---
phase: 239-rnglocked-invariant-permissionless-sweep
plan: 239-02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-PERMISSIONLESS-SWEEP.md
  - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
autonomous: true
requirements: [RNG-02]
requirements_addressed: [RNG-02]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-PERMISSIONLESS-SWEEP.md` exists with exactly these top-level sections in this exact order per D-10/D-11/D-23: `## Executive Summary`, `## Methodology — Two-Pass Sweep (Pass 1 + Pass 2)`, `## Pass 1 — Mechanical Grep Discovery`, `## Permissionless Sweep Table`, `## Classification Distribution Heatmap (Contract × Classification)`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`."
    - "Per D-10 the Permissionless Sweep Table has exactly these 11 columns in this exact order: `Row ID | Contract | Function | File:Line | Visibility | Mutates Storage? | Caller Gates | Touches RNG-Consumer State? | Classification | Evidence (File:Line + RNG-03/INV-237-NNN cite) | Verdict`. Row ID format `PERM-239-NNN` (three-digit zero-padded, starting at `PERM-239-001`). One row per permissionless function discovered via D-11 Pass 1 + Pass 2 methodology at HEAD `7ab515fe`."
    - "Per D-08 the `Classification` cell is exactly one of 3 locked values: `respects-rngLocked` / `respects-equivalent-isolation` / `proven-orthogonal`. Any function that cannot be classified into one of these three produces verdict `CANDIDATE_FINDING` and routes to Finding Candidates appendix per D-23. No 4th class permitted."
    - "Per D-08 the `Verdict` cell is exactly one of 2 locked values: `CLASSIFIED_CLEAN` / `CANDIDATE_FINDING`. No hedged verdicts."
    - "Per D-09 the permissionless scope is enforced: IN = external/public visibility + mutates storage (not view/pure) + no admin-gate (not `onlyAdmin` / `onlyOwner` / `onlyGovernance` / `onlyVaultOwner`) + no game-internal gate (not `onlyGame` / `onlyCoinflip` / self-call / delegatecall-only-invoked). OUT = `contracts/mocks/` + all `view`/`pure` functions + all admin-gated + all game-internal. Row-set attestation shows counts for each exclusion category."
    - "Per D-11 the two-pass methodology is documented: `## Pass 1 — Mechanical Grep Discovery` preserves the exact grep commands used to produce the candidate universe + raw candidate count (grep-reproducible at HEAD 7ab515fe); `## Methodology` section describes how Pass 2 semantic classification assigns each candidate its D-08 classification via touch-check against Phase 237 inventory + RNG-consumer read set."
    - "Per D-12 the RNG-consumer state space input is `audit/v30-CONSUMER-INVENTORY.md` Consumer Index (specifically Universe List + Per-Consumer Call Graphs — READ-only per D-28). Phase 238 BWD/FWD output is NOT an input to this plan (preserves single-wave parallel topology D-02). Inventory is cited by `INV-237-NNN` row IDs in the Evidence column."
    - "Per D-15 `respects-equivalent-isolation` classification rows MUST cite RNG-03(a) by file+section path (`audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A`) as the equivalence warrant in the Evidence column — forward-cite only (Wave 1 parallel; Plan 239-03 may commit before or after this plan; either order is acceptable). If RNG-03(a) lands a structure that differs from this plan's anticipated form, reconciliation happens in the SUMMARY as an erratum row — no re-edit of this plan's audit file (D-16 READ-only-after-commit)."
    - "Per D-08 `proven-orthogonal` classification rows MUST cite in the Evidence column: (a) the function's storage write set (derived from contracts/ at HEAD), (b) the RNG-consumer storage read set (derived from `audit/v30-CONSUMER-INVENTORY.md` Per-Consumer Call Graphs + Phase 238-02 FWD-01 if useful as corroborating cross-cite), (c) the disjointness statement `WRITE_SET ∩ RNG_CONSUMER_READ_SET = ∅`."
    - "Per D-08 `respects-rngLocked` classification rows MUST cite in the Evidence column the specific `rngLockedFlag` / `rngLocked()` read site in the function's code path (either direct `if (rngLockedFlag)` guard or inherited via helper call — cite the helper's File:Line also). The Evidence cell text includes the exact source-line context from contracts/ at HEAD."
    - "Per D-16/D-17 `## Prior-Artifact Cross-Cites` section lists at minimum: v3.8 Phases 68-72 (`audit/v1.2-RNG-PERMISSIONLESS-AUDIT.md` or equivalent — 87-permissionless-path + 51/51 SAFE general proof) as structural baseline for permissionless scope definition; `STORAGE-WRITE-MAP.md` (repo-root audit artifact) as write-set corroboration for `proven-orthogonal` classification; `ACCESS-CONTROL-MATRIX.md` (repo-root audit artifact) as caller-gate corroboration; v25.0 Phase 215 RNG fresh-eyes SOUND verdict as structural baseline. Every cite carries `re-verified at HEAD 7ab515fe` note with one-line structural-equivalence statement."
    - "Per D-22 NO `F-30-NN` finding IDs appear anywhere in `audit/v30-PERMISSIONLESS-SWEEP.md`. Phase 242 FIND-01..03 owns ID assignment. `grep -E 'F-30-[0-9]' audit/v30-PERMISSIONLESS-SWEEP.md` returns zero matches."
    - "Per D-23 the `## Finding Candidates` appendix lists every row whose verdict is `CANDIDATE_FINDING` as a structured Finding Candidate block with `ROW_REF` (PERM-239-NNN) + `FILE:LINE` citation + `SEVERITY: TBD-242` + observation text. If no `CANDIDATE_FINDING` rows exist, the section states `**None surfaced.**` explicitly."
    - "Per D-27 READ-only scope holds throughout: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty at task start and end; `git status --porcelain contracts/ test/` empty before and after each task; `KNOWN-ISSUES.md` not edited."
    - "Per D-26 HEAD anchor `7ab515fe` locked in frontmatter + echoed in `audit/v30-PERMISSIONLESS-SWEEP.md` `Audit baseline` line + in plan SUMMARY frontmatter + in Attestation section."
    - "Per D-28 if any permissionless function discovered that touches RNG-consumer state but the consumer is NOT in `audit/v30-CONSUMER-INVENTORY.md` — inventory NOT edited; scope-guard deferral row in `## Scope-Guard Deferrals` records `file:line + context + proposed inventory delta + SEVERITY: TBD-242` routes to Phase 242 FIND-01."
    - "Row-set integrity: Pass 1 raw candidate count documented in `## Pass 1 — Mechanical Grep Discovery` section; Permissionless Sweep Table row count matches Pass 1 raw candidates MINUS explicitly-excluded-per-D-09 rows MINUS explicitly-excluded-as-out-of-in-scope rows (with every exclusion reason documented either in the Methodology section or an exclusion sub-table). Total counts reconcile."
    - "All tasks commit in sequence with subjects matching `docs(239-02):` prefix regex. No `--no-verify` flag used; no force-push; no push to remote."
  artifacts:
    - path: "audit/v30-PERMISSIONLESS-SWEEP.md"
      provides: "RNG-02 permissionless sweep — two-pass methodology + sweep table with 3-class closed taxonomy (respects-rngLocked / respects-equivalent-isolation / proven-orthogonal) + Classification Distribution Heatmap + Finding Candidates"
      contains: "## Permissionless Sweep Table"
    - path: ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md"
      provides: "Plan 239-02 summary — HEAD 7ab515fe attestation, Pass 1 raw candidate count, classification distribution, grep commands preserved"
      contains: "HEAD `7ab515fe`"
  key_links:
    - from: "contracts/ external/public functions at HEAD 7ab515fe (discovered via D-11 Pass 1 mechanical grep)"
      to: "audit/v30-PERMISSIONLESS-SWEEP.md Permissionless Sweep Table rows"
      via: "D-11 two-pass methodology — Pass 1 grep discovery + Pass 2 semantic classification against Phase 237 inventory"
      pattern: "(external|public)\\b.*\\bfunction"
    - from: "CONTEXT.md D-08 closed 3-class taxonomy {respects-rngLocked / respects-equivalent-isolation / proven-orthogonal}"
      to: "Permissionless Sweep Table Classification column"
      via: "D-08 closed taxonomy + D-23 CANDIDATE_FINDING escape for unclassifiable rows"
      pattern: "respects-rngLocked|respects-equivalent-isolation|proven-orthogonal"
    - from: "audit/v30-CONSUMER-INVENTORY.md Consumer Index + Per-Consumer Call Graphs (Phase 237 output; READ-only per D-28)"
      to: "Permissionless Sweep Table `Touches RNG-Consumer State?` + Evidence columns (INV-237-NNN cross-cites)"
      via: "D-12 input source — Phase 237 inventory, NOT Phase 238 output (preserves D-02 single-wave parallel topology)"
      pattern: "INV-237-[0-9]{3}"
    - from: "Plan 239-03 RNG-03 Asymmetry A (lootbox index-advance equivalent to flag-based isolation)"
      to: "Permissionless Sweep Table Evidence column for `respects-equivalent-isolation` rows"
      via: "D-15 forward cite by file+section path — Wave 1 parallel; either plan may commit first"
      pattern: "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md.*Asymmetry A"
---

<objective>
RNG-02 whole-tree permissionless sweep of `contracts/` at HEAD `7ab515fe`: every external/public state-changing function NOT admin-gated / game-internal / mock, classified into exactly one of three closed verdicts `respects-rngLocked` / `respects-equivalent-isolation` / `proven-orthogonal` (or `CANDIDATE_FINDING` if unclassifiable). Two-pass methodology per D-11: mechanical grep discovery → semantic classification against Phase 237 RNG-consumer inventory.

**Single requirement satisfied (directly):**
- **RNG-02** — Every permissionless function in `contracts/` that could touch RNG-consumer input or consumption-time state is classified into one of three closed verdicts: (a) respects rngLocked via explicit `if (rngLockedFlag)` guard or inherited revert, (b) respects equivalent isolation via lootbox-index-advance (cites RNG-03(a)), or (c) proven orthogonal (write-set ∩ RNG-consumer-read-set = ∅, cites Phase 237 inventory). Any function outside these three classes is `CANDIDATE_FINDING`.

**Deliverable (per D-24):**
- `audit/v30-PERMISSIONLESS-SWEEP.md` — Methodology + Pass 1 mechanical grep discovery + Permissionless Sweep Table (3-class classification) + Classification Distribution Heatmap + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation

**Wave 1 parallel topology (per D-02):** This plan has zero cross-plan dependencies at HEAD. Runs concurrently with 239-01 and 239-03. RNG-03(a) is forward-cited by file+section path for `respects-equivalent-isolation` rows (D-15); either plan may commit first — reconciliation via SUMMARY erratum if structure diverges.

**Fresh-eyes mandate (per D-16/D-17):** Every row derived from fresh grep over `contracts/` at HEAD `7ab515fe` + fresh touch-check against Phase 237 inventory. Prior-milestone artifacts (v3.8 Phases 68-72 87-permissionless-paths general proof; v25.0 Phase 215; repo-root `STORAGE-WRITE-MAP.md` + `ACCESS-CONTROL-MATRIX.md`) CROSS-CITED as corroborating evidence only — NOT relied upon. Every cite carries `re-verified at HEAD 7ab515fe` note.

**Scope containment (per D-18/D-20):**
- IN scope: external/public + mutating + no admin/game-internal gate permissionless functions in `contracts/` (excluding `contracts/mocks/`).
- OUT of scope: admin-gated functions (Phase 238 BWD-03/FWD-02 admin actor class already covers); game-internal functions (inter-module calls); view/pure functions (no state mutation); mocks (non-production); regression / finding-ID assignment (Phase 242); per-consumer freeze verdicts (Phase 238); asymmetry first-principles re-derivation (Plan 239-03 RNG-03).

**No F-30-NN emission (per D-22):** `CANDIDATE_FINDING` rows produce Finding Candidate blocks; Phase 242 FIND-01..03 owns ID assignment.

**READ-only scope (per D-27):** Only writes: `audit/v30-PERMISSIONLESS-SWEEP.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md`. `KNOWN-ISSUES.md` NOT touched. `contracts/` / `test/` NOT touched. Phase 237 inventory + Phase 238 outputs READ-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@audit/v30-FREEZE-PROOF.md
@KNOWN-ISSUES.md
</context>

<threat_model>
N/A — READ-only audit milestone per D-27. No runtime code modified, no test modified, no `contracts/` or `test/` writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-PERMISSIONLESS-SWEEP.md` + `.planning/` SUMMARY. No STRIDE category applies to documentation-only output; threat register empty by construction.

Residual audit-quality risk: an incorrectly-classified `proven-orthogonal` row (a function whose write-set actually intersects RNG-consumer read-set but audit missed it) masks a real determinism violation from Phase 242 FIND-01 intake. Mitigation: (a) `proven-orthogonal` Evidence column MUST cite the exact storage write set from contracts/ at HEAD AND the RNG-consumer read set from `audit/v30-CONSUMER-INVENTORY.md` — grep-verifiable disjointness statement; (b) Pass 1 mechanical grep + Pass 2 semantic classification methodology makes the scope-to-row mapping explicit — any permissionless function in `contracts/` MUST appear in Pass 1 raw candidate count, MUST be either IN-scope-with-row or OUT-scope-with-exclusion-reason; (c) D-08 closed 3-class taxonomy + D-23 `CANDIDATE_FINDING` escape rule forces any audit-ambiguous function to surface in Finding Candidates, not silently classified; (d) `respects-rngLocked` rows cite the exact `if (rngLockedFlag)` or helper-inherited revert site — grep-diff on those sites between this plan and Plan 239-01 Set-Site Table catches coverage gaps; (e) `respects-equivalent-isolation` rows forward-cite RNG-03(a) — if Plan 239-03 lands a different equivalence form, reconciliation happens in SUMMARY (not silent drift).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pass 1 mechanical grep discovery of permissionless candidate universe; preserve grep commands and raw counts; OUT-scope exclusion attestation</name>
  <files>audit/v30-PERMISSIONLESS-SWEEP.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-08 closed 3-class taxonomy; D-09 permissionless scope definition; D-10 sweep table shape — 11 columns in exact order; D-11 two-pass methodology + Claude's Discretion to preserve grep commands; D-12 Phase 237 inventory as input NOT Phase 238; D-22 no F-30-NN; D-25 tabular/no mermaid; D-27 READ-only; D-28 scope-guard deferral)
    - .planning/REQUIREMENTS.md (RNG-02 exact wording — names 3-class taxonomy; "no permissionless function may touch RNG-consumer input state or consumption-time state without falling into one of these three classes")
    - .planning/ROADMAP.md Phase 239 block (Success Criterion 2 — names `audit/v30-PERMISSIONLESS-SWEEP.md` as the RNG-02 deliverable; names the 3-class taxonomy)
    - audit/v30-CONSUMER-INVENTORY.md §"Universe List" (146 INV-237-NNN rows — scope anchor for `Touches RNG-Consumer State?` column) + §"Per-Consumer Call Graphs" (storage read set per consumer — feeds `proven-orthogonal` disjointness evidence) + §"Consumer Index" RNG-02 row (scope = `ALL` 146 per 237-03 Consumer Index)
    - contracts/ tree at HEAD 7ab515fe — 17 top-level contracts + 11 modules + 5 libraries + interfaces per CONTEXT.md `<canonical_refs>` §"In-scope contract tree"; pre-scan density per CONTEXT.md `<code_context>`: `DegenerusGame.sol` 101 decls, `DegenerusVault.sol` 76, `DegenerusAdmin.sol` 50 (mostly admin-gated), `StakedDegenerusStonk.sol` 43, `GNRUS.sol` 38, `DegenerusStonk.sol` 26, `DegenerusDeityPass.sol` 25, `BurnieCoinflip.sol` 25, `BurnieCoin.sol` 28 — RE-GREP AT HEAD, do not rely on CONTEXT.md approximate counts
    - contracts/mocks/ — EXPLICITLY EXCLUDED per D-09 (non-production; carries forward Phase 237 D-18 scope tree)
    - Repo-root `STORAGE-WRITE-MAP.md` (storage-write corroboration for `proven-orthogonal` disjointness evidence) + `ACCESS-CONTROL-MATRIX.md` (caller-gate corroboration for D-09 admin/game-internal exclusion)
    - memory/feedback_contract_locations.md — contracts/ is the only authoritative source; stale copies (degenerus-contracts/, testing/contracts/) MUST NOT be read
    - memory/feedback_rng_backward_trace.md — RNG audit methodology: trace backward from every consumer to verify word unknown at input commitment (informs Pass 2 semantic classification)
    - memory/feedback_rng_commitment_window.md — commitment-window methodology (informs `Touches RNG-Consumer State?` evidence for Pass 2)
  </read_first>
  <action>
    Create `audit/v30-PERMISSIONLESS-SWEEP.md` with exactly these top-level sections in this exact order:

    ```
    # v30.0 Permissionless Sweep — RNG-Consumer State Classification (RNG-02)

    **Audit baseline:** HEAD `7ab515fe`
    **Plan:** 239-02
    **Requirement:** RNG-02

    ## Executive Summary
    ## Methodology — Two-Pass Sweep (Pass 1 + Pass 2)
    ## Pass 1 — Mechanical Grep Discovery
    ## Permissionless Sweep Table
    ## Classification Distribution Heatmap (Contract × Classification)
    ## Prior-Artifact Cross-Cites
    ## Finding Candidates
    ## Scope-Guard Deferrals
    ## Attestation
    ```

    **Step 1 — Populate `## Methodology — Two-Pass Sweep (Pass 1 + Pass 2)` with D-11 methodology description:**

    Describe explicitly:
    - Pass 1: mechanical grep over `contracts/` (excluding `contracts/mocks/`) for `external` / `public` visibility function declarations; filter to non-`view` / non-`pure`; filter out functions with explicit admin-gate modifiers (`onlyAdmin`, `onlyOwner`, `onlyGovernance`, `onlyVaultOwner`) or game-internal modifiers (`onlyGame`, `onlyCoinflip`) or self-call-only / delegatecall-only-invoked. Result: candidate permissionless universe.
    - Pass 2: for each candidate, (i) enumerate storage write set (direct SSTORE or helper-called SSTORE); (ii) touch-check against `audit/v30-CONSUMER-INVENTORY.md` Consumer Index + Per-Consumer Call Graphs (storage read set); (iii) assign one of D-08's 3 classes OR `CANDIDATE_FINDING` per D-23.

    **Step 2 — Execute Pass 1 mechanical grep and populate `## Pass 1 — Mechanical Grep Discovery`:**

    Run the following greps at HEAD 7ab515fe and preserve the exact commands + output counts in the audit file (Claude's Discretion per CONTEXT.md — reviewer sanity-check):

    ```bash
    # Raw external/public function declarations
    grep -rn -E 'function\s+\w+.*\b(external|public)\b' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | wc -l

    # Exclude view/pure (non-mutating)
    grep -rn -E 'function\s+\w+.*\b(external|public)\b' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | grep -vE '\b(view|pure)\b'

    # Exclude admin-gated (common modifier names — enumerate from codebase at HEAD, may include more than shown)
    # onlyAdmin / onlyOwner / onlyGovernance / onlyVaultOwner / onlyGame / onlyCoinflip
    grep -rn -E 'function\s+\w+' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | grep -E '\bonly(Admin|Owner|Governance|VaultOwner|Game|Coinflip)\b' | wc -l
    ```

    Document raw counts for:
    - Total `external`/`public` declarations in `contracts/` (excluding mocks and test)
    - Count after removing `view`/`pure`
    - Count after removing admin-gated
    - Count after removing game-internal-gated
    - Pass 1 candidate count = final filtered result (becomes row count for Permissionless Sweep Table + exclusion attestation sum)

    Include the raw grep OUTPUT as well (fenced code block under the grep commands) so reviewers can re-run at HEAD and diff.

    **Step 3 — Populate exclusion attestation sub-section with reconciliation rules:**

    Per D-09, every function in the raw `external`/`public` set MUST be accounted for: either it appears as a row in the Permissionless Sweep Table, or it is excluded with a documented reason. Exclusion reasons from D-09:
    - `EXCLUDED_VIEW_PURE` (`view` / `pure` visibility — no state mutation)
    - `EXCLUDED_ADMIN_GATED` (`onlyAdmin` / `onlyOwner` / `onlyGovernance` / `onlyVaultOwner` — admin actor class is Phase 238 scope)
    - `EXCLUDED_GAME_INTERNAL` (`onlyGame` / `onlyCoinflip` / self-call / delegatecall-only-invoked — inter-module / protocol-internal calls)
    - `EXCLUDED_MOCKS` (under `contracts/mocks/` — non-production)

    Present the exclusion counts as a small table:
    ```
    | Exclusion Reason | Count |
    |---|---|
    | EXCLUDED_VIEW_PURE | <N1> |
    | EXCLUDED_ADMIN_GATED | <N2> |
    | EXCLUDED_GAME_INTERNAL | <N3> |
    | EXCLUDED_MOCKS | <N4> (or 'not counted — mocks pre-filtered') |
    | **Permissionless Sweep candidates (Pass 1 result)** | **<P>** |
    | **TOTAL raw external/public declarations** | **<P + N1 + N2 + N3>** |
    ```

    Sanity-check the sum: `TOTAL = P + N1 + N2 + N3` must hold exactly; record the equation in the sub-section.

    **Step 4 — Build `## Executive Summary` (4-6 bullets):**
    - Pass 1 raw candidate count: P (to be filled from Step 2)
    - Permissionless Sweep Table row count: P (one row per candidate; or P-minus-overlap if a function has multiple entry points unified)
    - Classification distribution (to be finalized in Task 2): placeholder `TBD_TASK_2`
    - D-12 input source: `audit/v30-CONSUMER-INVENTORY.md` (Phase 237 Consumer Index + Per-Consumer Call Graphs); NOT Phase 238 output
    - Scope exclusions attested per D-09: N admin-gated / N game-internal / N view/pure / N mocks
    - HEAD anchor `7ab515fe`, READ-only, zero F-30-NN

    **Step 5 — Seed `## Permissionless Sweep Table` header row with the exact 11-column D-10 format:**
    ```
    | Row ID | Contract | Function | File:Line | Visibility | Mutates Storage? | Caller Gates | Touches RNG-Consumer State? | Classification | Evidence (File:Line + RNG-03/INV-237-NNN cite) | Verdict |
    |---|---|---|---|---|---|---|---|---|---|---|
    ```

    Leave table body empty for now — Task 2 populates all rows via Pass 2 semantic classification.

    **Step 6 — Frontmatter of audit file:** include `audit_baseline: 7ab515fe` + `plan: 239-02` + `requirement: RNG-02`.

    Do NOT populate Permissionless Sweep Table body, Classification Distribution Heatmap, Prior-Artifact Cross-Cites, Finding Candidates, Scope-Guard Deferrals, or Attestation yet — those are Task 2. Do NOT commit — commit is part of Task 2 so Pass 1 + Pass 2 land together.
  </action>
  <verify>
    <automated>
      # File exists with mandatory sections
      test -f audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^# v30.0 Permissionless Sweep' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^## Methodology — Two-Pass Sweep' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^## Pass 1 — Mechanical Grep Discovery$' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^## Permissionless Sweep Table$' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -qE '^audit_baseline: 7ab515fe' audit/v30-PERMISSIONLESS-SWEEP.md

      # Pass 1 section contains grep commands (fenced code blocks) and raw counts
      grep -qE 'grep.*external|public' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -qE '```(bash|sh)?' audit/v30-PERMISSIONLESS-SWEEP.md

      # Exclusion attestation sub-section contains all 4 exclusion reasons
      grep -q 'EXCLUDED_VIEW_PURE' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q 'EXCLUDED_ADMIN_GATED' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q 'EXCLUDED_GAME_INTERNAL' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q 'EXCLUDED_MOCKS' audit/v30-PERMISSIONLESS-SWEEP.md

      # Permissionless Sweep Table header row present with exact 11 columns
      grep -qE '\| Row ID \| Contract \| Function \| File:Line \| Visibility \| Mutates Storage\? \| Caller Gates \| Touches RNG-Consumer State\? \| Classification \| Evidence' audit/v30-PERMISSIONLESS-SWEEP.md

      # HEAD anchor echoed in file body
      grep -q '7ab515fe' audit/v30-PERMISSIONLESS-SWEEP.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-PERMISSIONLESS-SWEEP.md

      # Zero mermaid fences per D-25
      ! grep -qi '```mermaid' audit/v30-PERMISSIONLESS-SWEEP.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `audit/v30-PERMISSIONLESS-SWEEP.md` exists with all 9 mandatory top-level section headings (Executive Summary / Methodology / Pass 1 / Permissionless Sweep Table / Classification Distribution Heatmap / Prior-Artifact Cross-Cites / Finding Candidates / Scope-Guard Deferrals / Attestation). Some sections may still be empty placeholders for Task 2 to populate.
    - `## Pass 1 — Mechanical Grep Discovery` section contains: fenced code block with exact grep commands used at HEAD 7ab515fe; raw counts for total external/public, view/pure exclusions, admin-gated exclusions, game-internal exclusions; fenced code block with grep output for reviewer re-run; exclusion attestation table with 4 rows + sum equation.
    - `## Permissionless Sweep Table` header row matches the exact 11-column format per D-10: `Row ID | Contract | Function | File:Line | Visibility | Mutates Storage? | Caller Gates | Touches RNG-Consumer State? | Classification | Evidence (File:Line + RNG-03/INV-237-NNN cite) | Verdict`.
    - `audit_baseline: 7ab515fe` appears in file frontmatter and `HEAD 7ab515fe` appears in file body per D-26.
    - Sum equation in exclusion attestation sub-section holds: `TOTAL_RAW = PERMISSIONLESS_CANDIDATES + EXCLUDED_VIEW_PURE + EXCLUDED_ADMIN_GATED + EXCLUDED_GAME_INTERNAL`.
    - `grep -E 'F-30-[0-9]' audit/v30-PERMISSIONLESS-SWEEP.md` returns zero matches per D-22.
    - `grep -i 'mermaid' audit/v30-PERMISSIONLESS-SWEEP.md` returns zero matches per D-25.
    - `git status --porcelain contracts/ test/` returns empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` returns empty per D-28.
    - No commit yet (commit in Task 2 after Pass 2 classification complete).
  </acceptance_criteria>
  <done>
    File skeleton created with D-10 table header + D-11 Pass 1 methodology + Pass 1 grep commands + raw counts + exclusion attestation table with sum equation; HEAD anchor locked; READ-only scope confirmed; zero F-30-NN / mermaid / placeholders; Phase 237/238/KNOWN-ISSUES untouched. Ready for Task 2 (Pass 2 classification + row population + commit).
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass 2 semantic classification; populate Permissionless Sweep Table with 3-class D-08 taxonomy + Evidence cites + Verdict; Classification Distribution Heatmap; Prior-Artifact Cross-Cites; Finding Candidates; Scope-Guard Deferrals; Attestation; commit</name>
  <files>audit/v30-PERMISSIONLESS-SWEEP.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-08 3-class closed taxonomy + Evidence requirements; D-10 11-column table; D-11 Pass 2 semantic classification methodology; D-12 Phase 237 inventory input; D-15 RNG-03 forward-cite; D-16/D-17 cross-cite format; D-22 no F-30-NN; D-23 Finding Candidates; D-25 tabular/no mermaid; D-28 scope-guard deferral)
    - audit/v30-PERMISSIONLESS-SWEEP.md (from Task 1 — Pass 1 raw candidate list is the input to Pass 2 classification)
    - audit/v30-CONSUMER-INVENTORY.md §"Universe List" + §"Per-Consumer Call Graphs" + §"Consumer Index" — RNG-02 scope = `ALL` 146 rows; for each permissionless candidate, check if its storage write set intersects any INV-237-NNN consumer's storage read set (Per-Consumer Call Graph entry)
    - audit/v30-238-02-FWD.md §"Forward Enumeration Table" — per-consumer storage reads at consumption time (useful corroborating cross-cite for `proven-orthogonal` disjointness evidence; NOT the primary input — Phase 237 inventory is per D-12; cite as supplementary)
    - contracts/modules/DegenerusGameAdvanceModule.sol + contracts/DegenerusGame.sol + contracts/DegenerusVault.sol + contracts/StakedDegenerusStonk.sol + contracts/DegenerusStonk.sol + contracts/BurnieCoinflip.sol + contracts/BurnieCoin.sol + contracts/GNRUS.sol + contracts/DegenerusDeityPass.sol + contracts/DegenerusAffiliate.sol + contracts/DegenerusQuests.sol + contracts/DegenerusJackpots.sol + contracts/modules/DegenerusGameBoonModule.sol + contracts/modules/DegenerusGameGameOverModule.sol + contracts/modules/DegenerusGameLootboxModule.sol + contracts/modules/DegenerusGameMintModule.sol + contracts/modules/DegenerusGameJackpotModule.sol + contracts/modules/DegenerusGameWhaleModule.sol — per-candidate source inspection for write-set enumeration (Pass 2 step (i))
    - Repo-root `STORAGE-WRITE-MAP.md` — storage-write corroboration for `proven-orthogonal` disjointness evidence; cross-cite with `re-verified at HEAD 7ab515fe`
    - Repo-root `ACCESS-CONTROL-MATRIX.md` — caller-gate corroboration; cross-cite with `re-verified at HEAD 7ab515fe`
    - .planning/milestones/v3.8-phases/ — v3.8 Phase 72 87-permissionless-path + 51/51 SAFE general proof (structural baseline per D-16)
    - .planning/milestones/v25.0-phases/215-rng-fresh-eyes/ — v25.0 Phase 215 SOUND verdict (structural baseline)
    - audit/v30-ASYMMETRY-RE-JUSTIFICATION.md (IF Plan 239-03 has already committed — check via `ls audit/v30-ASYMMETRY-RE-JUSTIFICATION.md && git log --oneline -- audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`; if committed, cite the § Asymmetry A section directly; if NOT committed, cite by file+section path as forward-reference per D-15 with note "per Plan 239-03 Wave 1 parallel — either plan may commit first; reconciliation in SUMMARY erratum if structure diverges")
    - memory/feedback_rng_backward_trace.md + memory/feedback_rng_commitment_window.md (methodology anchors for Pass 2 semantic touch-check — trace backward from each RNG consumer in the 237 inventory and verify each permissionless function's write-set is disjoint OR gated)
  </read_first>
  <action>
    Populate `audit/v30-PERMISSIONLESS-SWEEP.md` with:

    **Step 1 — Populate `## Permissionless Sweep Table` body rows per Pass 2 semantic classification:**

    For each candidate from Pass 1 (Task 1's preserved grep output), produce ONE row in the exact 11-column D-10 format. For each row:
    - `Row ID` — `PERM-239-NNN` (three-digit zero-padded, starting at `PERM-239-001`; monotonic).
    - `Contract` — the `.sol` file name (e.g., `DegenerusGame.sol`).
    - `Function` — the function signature as declared in the source.
    - `File:Line` — exact file path + line from the function declaration at HEAD 7ab515fe.
    - `Visibility` — `external` or `public` (per the declaration).
    - `Mutates Storage?` — `yes` (else the row shouldn't be here — Pass 1 filtered view/pure).
    - `Caller Gates` — one of `{none-permissionless, onlyAdmin, onlyGame, ...}` — for in-scope rows expected `none-permissionless` (else row should be EXCLUDED, not a row). If no gate but the function can only be reached via internal helper chains (self-call only), note here `self-call-only-reachable` and exclude from Pass 2 (moved to exclusion attestation).
    - `Touches RNG-Consumer State?` — `yes` if the function's storage write set intersects any INV-237-NNN consumer's storage read set (per Phase 237 inventory); `no` otherwise. If `yes`, cite the INV-237-NNN row IDs in the Evidence column.
    - `Classification` — exactly one of `{respects-rngLocked, respects-equivalent-isolation, proven-orthogonal}` per D-08. If unclassifiable, `Classification` cell is empty and `Verdict` = `CANDIDATE_FINDING`.
    - `Evidence (File:Line + RNG-03/INV-237-NNN cite)` — cell contents depend on classification:
      - `respects-rngLocked`: cite specific `if (rngLockedFlag)` / `rngLocked()` read site with file:line (either in the function itself or in an invoked helper). Example: `Guard at DegenerusGameAdvanceModule.sol:1031 via _rngGateCheck() helper call at DegenerusGameMintModule.sol:<line>; touches INV-237-NNN,...`.
      - `respects-equivalent-isolation`: cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` by file+section path + function's lootbox-index-advance interaction (e.g., `lootboxRngIndex` read at `contracts/.../<file>:<line>`). Note: this is a forward-cite per D-15; Plan 239-03 may commit before or after — either order acceptable.
      - `proven-orthogonal`: cite (i) function's storage write set (e.g., `Writes: DegenerusQuests.sol:150 (questProgress[player])`) + (ii) disjointness statement `WRITE_SET ∩ RNG_CONSUMER_READ_SET = ∅` + (iii) cross-cite to `STORAGE-WRITE-MAP.md` + Phase 237 inventory (specifically which INV-237-NNN consumers' read sets were checked against).
      - `CANDIDATE_FINDING`: cite the specific code path where classification breaks down + observation text; surface as Finding Candidate in appendix per D-23.
    - `Verdict` — exactly one of `{CLASSIFIED_CLEAN, CANDIDATE_FINDING}` per D-08. `CLASSIFIED_CLEAN` iff Classification ∈ 3-class taxonomy; `CANDIDATE_FINDING` otherwise.

    Sanity-check: row count matches Pass 1 candidate count (from Task 1 `## Pass 1 — Mechanical Grep Discovery` section). Discrepancies are documented in the Exclusion Attestation (moved-from-Pass-1-to-excluded) or Scope-Guard Deferral (out-of-inventory discoveries).

    **Step 2 — Populate `## Classification Distribution Heatmap (Contract × Classification)`:**

    Matrix table showing row counts per (Contract, Classification) pair. Columns: `respects-rngLocked / respects-equivalent-isolation / proven-orthogonal / CANDIDATE_FINDING / Total`. Rows: one per contract file present in Permissionless Sweep Table.

    Example form:
    ```
    | Contract | respects-rngLocked | respects-equivalent-isolation | proven-orthogonal | CANDIDATE_FINDING | Total |
    |---|---|---|---|---|---|
    | DegenerusGame.sol | <x> | <x> | <x> | <x> | <sum> |
    | ... |
    | **TOTAL** | <X> | <Y> | <Z> | <W> | <P> |
    ```

    Row totals + column totals + grand total must reconcile; grand total = Permissionless Sweep Table row count = Pass 1 candidate count.

    **Step 3 — Populate `## Prior-Artifact Cross-Cites` per D-16/D-17:**

    Each cite structured as:
    ```
    ### <Milestone> <Phase> <artifact>
    **Artifact:** `<path>`
    **Corroboration:** <one-line what this supports>
    **Re-verification note:** re-verified at HEAD 7ab515fe — <structural-equivalence statement>
    ```

    Minimum cites (all with `re-verified at HEAD 7ab515fe` + structural-equivalence statement):
    - v3.8 Phases 68-72 commitment-window audit — 87-permissionless-path + 51/51 SAFE general proof (structural baseline for permissionless scope definition); re-verify: `v30.0 Pass 1 candidate count <P> compared against v3.8 87-path baseline; delta <D> explained by post-v3.8 contract additions`.
    - Repo-root `STORAGE-WRITE-MAP.md` — write-set corroboration for `proven-orthogonal` disjointness evidence; re-verify: `write-set entries used in Evidence column cross-referenced against STORAGE-WRITE-MAP.md at HEAD 7ab515fe`.
    - Repo-root `ACCESS-CONTROL-MATRIX.md` — caller-gate corroboration for D-09 admin/game-internal exclusion; re-verify: `admin-gated + game-internal exclusion counts cross-referenced against ACCESS-CONTROL-MATRIX.md at HEAD 7ab515fe`.
    - v25.0 Phase 215 RNG fresh-eyes SOUND verdict — structural baseline.
    - `audit/v30-CONSUMER-INVENTORY.md` Consumer Index RNG-02 row — scope anchor (`ALL` 146 per 237-03 Consumer Index); `re-verified at HEAD 7ab515fe — Phase 237 inventory READ-only per D-28`.

    **Step 4 — Populate `## Finding Candidates` per D-23:**

    If any row has verdict `CANDIDATE_FINDING`:
    ```
    ### CANDIDATE_FINDING #N (— surfaced in 239-02 RNG-02)
    **ROW_REF:** PERM-239-NNN
    **FILE:LINE:** <contracts/... path + line>
    **OBSERVATION:** <one-paragraph observation of why the function cannot be classified into the 3-class taxonomy>
    **SEVERITY:** TBD-242
    **ROUTING:** Phase 242 FIND-01 intake
    ```

    If zero `CANDIDATE_FINDING` rows: state `**None surfaced.** Every permissionless function in contracts/ at HEAD 7ab515fe classified into one of the 3 closed verdicts per D-08.`

    **Step 5 — Populate `## Scope-Guard Deferrals` per D-28:**

    If any permissionless function touches RNG-consumer state for a consumer NOT in `audit/v30-CONSUMER-INVENTORY.md` Universe List: record as scope-guard deferral block — file:line + context + proposed inventory delta + `SEVERITY: TBD-242` + `ROUTING: Phase 242 FIND-01`. Inventory NOT edited per D-28.

    If zero gaps: state `**None surfaced.** Every permissionless-function-touched RNG-consumer state maps to an existing INV-237-NNN Universe List row.`

    **Step 6 — Populate `## Executive Summary` with final numbers (replace Task 1 TBD_TASK_2 placeholder):**

    Final counts:
    - Pass 1 raw candidates: P
    - Permissionless Sweep Table rows: P
    - Classification distribution: `respects-rngLocked` = X / `respects-equivalent-isolation` = Y / `proven-orthogonal` = Z / `CANDIDATE_FINDING` = W; X+Y+Z+W = P
    - Scope-Guard Deferrals: K entries (or None surfaced)
    - HEAD anchor `7ab515fe`, READ-only, zero F-30-NN

    **Step 7 — Populate `## Attestation`:**
    ```
    **HEAD anchor:** `7ab515fe` (contract tree identical to v29.0 `1646d5af`; all post-v29 commits are docs-only per PROJECT.md).
    **Scope:** RNG-02 permissionless sweep — every external/public state-changing function in contracts/ (excluding mocks) NOT admin-gated / game-internal, classified per D-08 3-class taxonomy.
    **D-09 scope-enforcement:** Pass 1 raw <TOTAL> declarations; <N1> view/pure + <N2> admin-gated + <N3> game-internal + 0 mock = <TOTAL-P> excluded; <P> permissionless candidates classified in Pass 2.
    **D-12 input:** Phase 237 `audit/v30-CONSUMER-INVENTORY.md` Consumer Index + Per-Consumer Call Graphs (NOT Phase 238 output).
    **Fresh-eyes mandate (D-16/D-17):** Every row derived fresh at HEAD 7ab515fe. Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD notes — NOT relied upon.
    **Finding-ID emission (D-22):** Zero F-30-NN. Finding Candidates routed to Phase 242 FIND-01.
    **READ-only scope (D-27):** Zero contracts/ or test/ writes. KNOWN-ISSUES.md untouched.
    **D-15 forward cite handling:** `respects-equivalent-isolation` rows cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` by file+section path; Plan 239-03 Wave 1 parallel — SUMMARY records reconciliation if structural divergence occurs.
    **Row-set integrity:** Pass 1 candidate count = Permissionless Sweep Table row count = <P>; Classification Distribution Heatmap grand total = <P>.
    ```

    **Step 8 — Commit:**

    Stage ONLY `audit/v30-PERMISSIONLESS-SWEEP.md`. Commit with message:
    ```
    docs(239-02): RNG-02 permissionless sweep with 3-class D-08 taxonomy at HEAD 7ab515fe

    Two-pass methodology (D-11): Pass 1 mechanical grep over contracts/ (excluding
    mocks) for external/public mutating functions minus admin/game-internal gates;
    Pass 2 semantic classification per D-08 3-class closed taxonomy
    {respects-rngLocked / respects-equivalent-isolation / proven-orthogonal} +
    CANDIDATE_FINDING escape per D-23.

    Input per D-12: Phase 237 audit/v30-CONSUMER-INVENTORY.md Consumer Index +
    Per-Consumer Call Graphs (NOT Phase 238 output — preserves D-02 single-wave
    parallel topology). respects-equivalent-isolation rows forward-cite RNG-03(a)
    per D-15.

    READ-only: zero contracts/ or test/ writes per D-27. Zero F-30-NN per D-22.
    HEAD anchor 7ab515fe locked per D-26. Phase 237 inventory unmodified per D-28.
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify. Single commit only.
  </action>
  <verify>
    <automated>
      # Permissionless Sweep Table has ≥ 1 row (Pass 1 candidate count > 0 given contracts/ scale)
      grep -cE '\| PERM-239-[0-9]{3} \|' audit/v30-PERMISSIONLESS-SWEEP.md | awk '{ if ($1 < 1) exit 1 }'

      # Every Classification cell is one of the 3 closed values OR empty (empty implies CANDIDATE_FINDING)
      # Check no unexpected values in Classification column — iterate rows and verify
      ! grep -E '\| PERM-239-[0-9]{3} \|' audit/v30-PERMISSIONLESS-SWEEP.md | awk -F'|' '{ gsub(/^ +| +$/, "", $10); print $10 }' | grep -vE '^(respects-rngLocked|respects-equivalent-isolation|proven-orthogonal|)$'

      # Every Verdict cell is one of 2 closed values
      ! grep -E '\| PERM-239-[0-9]{3} \|' audit/v30-PERMISSIONLESS-SWEEP.md | awk -F'|' '{ gsub(/^ +| +$/, "", $12); print $12 }' | grep -vE '^(CLASSIFIED_CLEAN|CANDIDATE_FINDING)$'

      # Classification Distribution Heatmap section populated
      grep -q '^## Classification Distribution Heatmap' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -qE '\| \*\*TOTAL\*\*' audit/v30-PERMISSIONLESS-SWEEP.md

      # Prior-Artifact Cross-Cites with ≥ 3 re-verified-at-HEAD notes
      grep -q '^## Prior-Artifact Cross-Cites$' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -cE 're-verified at HEAD 7ab515fe' audit/v30-PERMISSIONLESS-SWEEP.md | awk '{ if ($1 < 3) exit 1 }'

      # Finding Candidates + Scope-Guard Deferrals + Attestation sections present
      grep -q '^## Finding Candidates$' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^## Scope-Guard Deferrals$' audit/v30-PERMISSIONLESS-SWEEP.md
      grep -q '^## Attestation$' audit/v30-PERMISSIONLESS-SWEEP.md

      # D-15 forward cite to RNG-03(a) — at least one row must cite Asymmetry A if respects-equivalent-isolation class is used
      # Or zero if classification doesn't use that class at HEAD
      # Soft check: if 'respects-equivalent-isolation' appears in any row, 'audit/v30-ASYMMETRY-RE-JUSTIFICATION.md' must appear somewhere
      if grep -qE 'respects-equivalent-isolation' audit/v30-PERMISSIONLESS-SWEEP.md; then
        grep -qE 'audit/v30-ASYMMETRY-RE-JUSTIFICATION\.md' audit/v30-PERMISSIONLESS-SWEEP.md
      fi

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-PERMISSIONLESS-SWEEP.md

      # Zero mermaid per D-25
      ! grep -qi '```mermaid' audit/v30-PERMISSIONLESS-SWEEP.md

      # Zero placeholder tokens
      ! grep -qE '<line>|<path>|<fn|<slug>|TBD-239|TBD_TASK_2' audit/v30-PERMISSIONLESS-SWEEP.md

      # HEAD anchor echoed
      grep -q '7ab515fe' audit/v30-PERMISSIONLESS-SWEEP.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — subject prefix docs(239-02):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-02\):'
      # Commit touched ONLY audit/v30-PERMISSIONLESS-SWEEP.md
      git show --stat HEAD | grep -E '^\s+audit/v30-PERMISSIONLESS-SWEEP.md'
      test "$(git show --stat HEAD --name-only --format='' | grep -v '^$' | wc -l)" -eq 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `## Permissionless Sweep Table` populated with ≥ 1 row; row count equals Pass 1 candidate count from Task 1's `## Pass 1 — Mechanical Grep Discovery` section.
    - Every row's `Classification` cell is one of `{respects-rngLocked, respects-equivalent-isolation, proven-orthogonal}` OR empty-with-CANDIDATE_FINDING verdict per D-08.
    - Every row's `Verdict` cell is one of `{CLASSIFIED_CLEAN, CANDIDATE_FINDING}` per D-08.
    - `respects-rngLocked` rows cite specific `if (rngLockedFlag)` / `rngLocked()` site with file:line in Evidence column.
    - `respects-equivalent-isolation` rows cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` in Evidence column per D-15 (forward cite acceptable in Wave 1 parallel).
    - `proven-orthogonal` rows cite function's storage write set + disjointness statement `WRITE_SET ∩ RNG_CONSUMER_READ_SET = ∅` + cross-cite to `STORAGE-WRITE-MAP.md` + INV-237-NNN row IDs in Evidence column.
    - `## Classification Distribution Heatmap` has rows per contract + `**TOTAL**` row; grand total = Permissionless Sweep Table row count.
    - `## Prior-Artifact Cross-Cites` has ≥ 3 cites with `re-verified at HEAD 7ab515fe` notes per D-17 (v3.8 87-path baseline; STORAGE-WRITE-MAP.md; ACCESS-CONTROL-MATRIX.md minimum).
    - `## Finding Candidates` has either N structured blocks OR `**None surfaced.**` statement.
    - `## Scope-Guard Deferrals` has either N deferral blocks OR `**None surfaced.**` statement per D-28.
    - `## Attestation` lists HEAD anchor, D-09 scope-enforcement counts, D-12 input source, fresh-eyes mandate, D-22 attestation, D-27 READ-only attestation, D-15 forward-cite handling, row-set integrity.
    - `## Executive Summary` has final (non-TBD) classification distribution counts.
    - `grep -E 'F-30-[0-9]' audit/v30-PERMISSIONLESS-SWEEP.md` returns zero matches per D-22.
    - `grep -i 'mermaid' audit/v30-PERMISSIONLESS-SWEEP.md` returns zero matches per D-25.
    - Exactly one new commit on `main` with subject matching regex `^docs\(239-02\):`; the commit touches exactly `audit/v30-PERMISSIONLESS-SWEEP.md`.
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28.
    - No `--no-verify`, no force-push, no push-to-remote.
  </acceptance_criteria>
  <done>
    Permissionless Sweep Table fully populated with 3-class closed taxonomy + 2-value verdict closed taxonomy; Classification Distribution Heatmap reconciles with row count; Prior-Artifact Cross-Cites carry re-verified-at-HEAD notes; Finding Candidates + Scope-Guard Deferrals follow exact format; Attestation locks HEAD + READ-only + D-15 forward-cite handling; one clean commit on main; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Plan 239-02 SUMMARY with grep-reproducibility + RNG-03 reconciliation note + commit</name>
  <files>.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template structure)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-02-SUMMARY.md (Plan 237-02 classification summary precedent — grep preservation + classification distribution table)
    - .planning/phases/238-backward-forward-freeze-proofs/238-03-SUMMARY.md (Phase 238 summary precedent)
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-11 Claude's Discretion for grep-command preservation in SUMMARY; D-15 RNG-03 reconciliation via SUMMARY erratum if structure diverges; D-26 HEAD anchor; D-27 READ-only; D-28 scope-guard deferral format)
    - audit/v30-PERMISSIONLESS-SWEEP.md (produced by Task 1 + Task 2 — source for Pass 1 grep counts, classification distribution, and cross-cite count)
    - audit/v30-ASYMMETRY-RE-JUSTIFICATION.md (IF Plan 239-03 has committed — check via `ls` + `git log`; if present, cross-check `§ Asymmetry A` section structure against Plan 239-02's forward-cite expectations; record reconciliation in SUMMARY)
    - git log --oneline -1 (capture commit SHA for SUMMARY Task Commits section)
  </read_first>
  <action>
    Create `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md` with frontmatter + body per template.

    **Frontmatter:**
    ```yaml
    ---
    phase: 239-rnglocked-invariant-permissionless-sweep
    plan: 239-02
    subsystem: audit
    tags: [v30.0, VRF, RNG-02, permissionless-sweep, 3-class-taxonomy, fresh-eyes, HEAD-7ab515fe]
    head_anchor: 7ab515fe

    # Dependency graph
    requires:
      - phase: 237-vrf-consumer-inventory-call-graph
        provides: "audit/v30-CONSUMER-INVENTORY.md Consumer Index RNG-02 scope (ALL 146 rows per 237-03 Consumer Index)"
    provides:
      - "audit/v30-PERMISSIONLESS-SWEEP.md — RNG-02 deliverable per D-10: two-pass methodology + Pass 1 mechanical grep + Permissionless Sweep Table (<P> rows) with 3-class D-08 closed taxonomy + Classification Distribution Heatmap + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation at HEAD 7ab515fe."
      - "Classification distribution: respects-rngLocked = <X> / respects-equivalent-isolation = <Y> / proven-orthogonal = <Z> / CANDIDATE_FINDING = <W> (X+Y+Z+W = P)."
      - "Pass 1 mechanical-grep candidate count = <P>; exclusion breakdown = <N1> view/pure + <N2> admin-gated + <N3> game-internal + 0 mock (mocks pre-filtered)."
      - "D-15 forward cite to Plan 239-03 RNG-03(a) Asymmetry A — reconciliation note: <fill: if 239-03 committed and structure matches; OR 239-03 not yet committed and forward-cite by file+section path; OR 239-03 structure diverges and erratum recorded>."
    affects: [240-gameover-jackpot-safety, 241-exception-closure, 242-findings-consolidation]

    # Tech tracking
    tech-stack:
      added: []
      patterns:
        - "D-08 closed 3-class taxonomy {respects-rngLocked / respects-equivalent-isolation / proven-orthogonal} — no 4th class; D-23 CANDIDATE_FINDING escape"
        - "D-09 permissionless scope definition (external/public + mutating + no admin-gate + no game-internal-gate; mocks excluded)"
        - "D-10 11-column Permissionless Sweep Table format"
        - "D-11 two-pass methodology (mechanical grep + semantic classification); grep commands preserved in audit file + SUMMARY for reviewer reproducibility"
        - "D-12 Phase 237 inventory as input (NOT Phase 238 output — preserves D-02 single-wave parallel topology)"
        - "D-15 RNG-03 forward-cite by file+section path; Wave 1 parallel independence"
        - "D-22 no F-30-NN finding-ID emission"
        - "D-25 tabular / grep-friendly / no mermaid"
        - "D-26 HEAD anchor 7ab515fe locked in frontmatter + echoed in Attestation"
        - "D-27 READ-only — zero contracts/ or test/ writes; KNOWN-ISSUES untouched"
        - "D-28 scope-guard deferral for out-of-inventory touches"

    key-files:
      created:
        - "audit/v30-PERMISSIONLESS-SWEEP.md (<LINE_COUNT> lines committed at <COMMIT_SHA> — 9 required sections)"
        - ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md"
      modified: []

    requirements-completed: [RNG-02]

    metrics:
      duration: <FILL>
      completed: 2026-04-19
      tasks_executed: 3
      lines_in_audit_file: <FILL>
      pass_1_candidate_count: <P>
      commits:
        - sha: <COMMIT_SHA>
          subject: docs(239-02): RNG-02 permissionless sweep with 3-class D-08 taxonomy at HEAD 7ab515fe
    ---
    ```

    **Body sections per template:**

    ```markdown
    # Phase 239 Plan 02: RNG-02 Permissionless Sweep Summary

    **Whole-tree permissionless sweep of contracts/ at HEAD `7ab515fe`: <P> permissionless functions classified per D-08 3-class closed taxonomy {respects-rngLocked / respects-equivalent-isolation / proven-orthogonal}. Two-pass methodology (mechanical grep + semantic classification); Phase 237 inventory consumed directly (NOT Phase 238 output per D-12).**

    ## Performance
    (fill duration + started/completed + tasks + files)

    ## Accomplishments
    - Pass 1 mechanical grep discovery: <P> permissionless candidates from <TOTAL> raw external/public declarations (<N1> view/pure + <N2> admin-gated + <N3> game-internal excluded per D-09)
    - Pass 2 semantic classification: <X> respects-rngLocked / <Y> respects-equivalent-isolation / <Z> proven-orthogonal / <W> CANDIDATE_FINDING (X+Y+Z+W = P)
    - Classification Distribution Heatmap reconciles: grand total = <P> = Pass 1 candidate count
    - D-12 input: Phase 237 `audit/v30-CONSUMER-INVENTORY.md` Consumer Index + Per-Consumer Call Graphs; Phase 238 output NOT consumed (D-02 single-wave parallel topology preserved)
    - D-15 forward cite to Plan 239-03 RNG-03(a) Asymmetry A: <fill: cite mechanism + reconciliation status>
    - Prior-artifact cross-cites: <count> with re-verified-at-HEAD notes (v3.8 87-path baseline / STORAGE-WRITE-MAP.md / ACCESS-CONTROL-MATRIX.md / v25.0 Phase 215 / Phase 237 inventory)

    ## Grep Commands (reproducibility — Claude's Discretion per CONTEXT.md)

    Pass 1 mechanical grep commands preserved here for reviewer re-run at HEAD 7ab515fe. Same commands appear in `audit/v30-PERMISSIONLESS-SWEEP.md` `## Pass 1 — Mechanical Grep Discovery` section.

    ```bash
    # Raw external/public function declarations
    grep -rn -E 'function\s+\w+.*\b(external|public)\b' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | wc -l

    # Exclude view/pure
    grep -rn -E 'function\s+\w+.*\b(external|public)\b' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | grep -vE '\b(view|pure)\b' | wc -l

    # Count admin-gated / game-internal-gated
    grep -rn -E 'function\s+\w+' contracts/ --include='*.sol' | grep -v 'contracts/mocks/' | grep -v 'contracts/test/' | grep -E '\bonly(Admin|Owner|Governance|VaultOwner|Game|Coinflip)\b' | wc -l
    ```

    Raw counts at HEAD 7ab515fe:
    - TOTAL external/public: <FILL>
    - After view/pure filter: <FILL>
    - Admin/game-internal-gated: <FILL>
    - Pass 1 candidates (final): <P>

    ## Task Commits
    1. **Task 1 + Task 2 (combined commit): Pass 1 mechanical grep + Pass 2 semantic classification + all remaining sections + commit** — `<COMMIT_SHA>` (docs(239-02): RNG-02 permissionless sweep with 3-class D-08 taxonomy at HEAD 7ab515fe). <LINE_COUNT> lines; zero F-30-NN; zero mermaid; zero placeholder tokens; HEAD anchor attested.

    ## Files Created/Modified
    - `audit/v30-PERMISSIONLESS-SWEEP.md` (CREATED) — <fill counts>
    - `audit/v30-CONSUMER-INVENTORY.md` (UNCHANGED per D-28 — inventory READ-only after 237 commit)
    - `audit/v30-238-*.md`, `audit/v30-FREEZE-PROOF.md` (UNCHANGED per D-18 — Phase 238 READ-only; Phase 239-02 consumes Phase 237 inventory directly per D-12, not Phase 238 output)
    - `KNOWN-ISSUES.md` (UNCHANGED per D-27 — Phase 242 FIND-03 owns KI promotions)
    - `contracts/`, `test/` (UNCHANGED per D-27 — READ-only audit phase)

    ## D-15 Forward-Cite Reconciliation (RNG-03(a) Asymmetry A)

    Plan 239-02 `respects-equivalent-isolation` rows forward-cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` (Plan 239-03 deliverable) per D-15. Wave 1 parallel topology: either plan may commit first. Reconciliation status at time of this SUMMARY commit:

    <FILL one of the following based on `ls audit/v30-ASYMMETRY-RE-JUSTIFICATION.md && git log --oneline -- audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`:>

    - **If Plan 239-03 has committed and § Asymmetry A structure matches 239-02's forward-cite expectations:** "Plan 239-03 committed at <SHA>; `§ Asymmetry A` section structure matches 239-02's forward-cite expectations (section heading present, equivalence-to-flag-based-isolation proof present); no reconciliation erratum needed."

    - **If Plan 239-03 has NOT committed at time of this SUMMARY:** "Plan 239-03 not yet committed at time of 239-02 commit `<COMMIT_SHA>`; forward-cite by file+section path held. When 239-03 commits, if structural divergence occurs, erratum recorded in 239-02-SUMMARY update (no re-edit of committed audit/v30-PERMISSIONLESS-SWEEP.md per D-16 READ-only-after-commit)."

    - **If Plan 239-03 committed with divergent § Asymmetry A structure:** "Plan 239-03 committed at <SHA>; structural divergence detected: <detail>. Reconciliation erratum: <detail + routing recommendation to Phase 242 FIND-01>. No re-edit of committed audit/v30-PERMISSIONLESS-SWEEP.md per D-16 READ-only-after-commit."

    ## Decisions Made
    (fill 3-5 decisions Claude made during execution — Claude's Discretion items in CONTEXT.md: grep-command preservation in SUMMARY, severity pre-classification vs TBD-242, etc.)

    ## Deviations from Plan
    (fill with any deviations, or "None — plan executed exactly as written.")

    ## Issues Encountered
    (fill with any issues, or "None.")

    ## User Setup Required
    None — no external service configuration. Deliverable is markdown-only under `audit/`.

    ## Next Phase Readiness
    **Phase 239 Plan 02 complete (RNG-02 closed). Plans 239-01 (RNG-01 state machine) + 239-03 (RNG-03 asymmetries) running in parallel (Wave 1 per D-02) — no cross-dependencies beyond D-15 forward cite.**

    ## Self-Check: PASSED
    (fill checklist items — all must-haves truths satisfied; see Attestation section of audit file)
    ```

    Replace `<COMMIT_SHA>`, `<LINE_COUNT>`, `<P>`, `<X>`, `<Y>`, `<Z>`, `<W>`, `<N1>`, `<N2>`, `<N3>`, `<TOTAL>`, `<FILL>` placeholders with actual values.

    **Commit:**
    Stage `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md`. Commit with message:
    ```
    docs(239-02): SUMMARY — RNG-02 permissionless sweep complete; grep-reproducibility preserved; D-15 RNG-03 forward-cite reconciliation noted
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      grep -qE '^phase: 239-rnglocked-invariant-permissionless-sweep' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      grep -qE '^plan: 239-02' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      grep -qE '^head_anchor: 7ab515fe' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      grep -qE 'requirements-completed:.*RNG-02' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md

      # Grep Commands section present (reproducibility per Claude's Discretion)
      grep -qE 'Grep Commands|## Grep|Pass 1.*grep' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md

      # D-15 RNG-03 reconciliation section present
      grep -qE 'D-15|RNG-03|Asymmetry A|Forward-Cite' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md

      # Placeholders filled in
      ! grep -q '<COMMIT_SHA>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      ! grep -q '<LINE_COUNT>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      ! grep -qE '<[A-Z][A-Z_]*>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md
      ! grep -q '<FILL>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — subject prefix docs(239-02):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-02\):'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md` exists with full frontmatter: `phase`, `plan`, `subsystem`, `tags`, `head_anchor: 7ab515fe`, `requires`, `provides`, `affects`, `tech-stack.patterns` (≥ 10 items), `key-files.created` (≥ 2 items), `requirements-completed: [RNG-02]`, `metrics.pass_1_candidate_count: <P>`.
    - Body includes `## Performance`, `## Accomplishments`, `## Grep Commands (reproducibility — Claude's Discretion per CONTEXT.md)`, `## Task Commits`, `## Files Created/Modified`, `## D-15 Forward-Cite Reconciliation (RNG-03(a) Asymmetry A)`, `## Decisions Made`, `## Deviations from Plan`, `## Issues Encountered`, `## User Setup Required`, `## Next Phase Readiness`, `## Self-Check: PASSED`.
    - Grep Commands section preserves the Pass 1 commands exactly as run at HEAD 7ab515fe (reviewer reproducibility).
    - D-15 Forward-Cite Reconciliation section explicitly addresses the 3 possible states (Plan 239-03 committed-matching / not-committed / committed-diverging) and selects one with concrete detail.
    - No literal placeholder tokens remain (`<COMMIT_SHA>`, `<LINE_COUNT>`, `<FILL>`, `<P>`, `<X>`, `<Y>`, `<Z>`, `<W>`, `<N1>`, `<N2>`, `<N3>`, `<TOTAL>`).
    - `grep -E 'F-30-[0-9]'` returns zero matches per D-22.
    - Commit with subject matching `^docs\(239-02\):` prefix (may be absorbed into Task 2 commit or land as second — both acceptable per precedent).
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28.
  </acceptance_criteria>
  <done>
    SUMMARY committed with all placeholders filled; grep commands preserved for reviewer reproducibility; D-15 forward-cite reconciliation status explicitly addressed for 239-03 coordination; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched; clean commit on main.
  </done>
</task>

</tasks>

<verification>
- `audit/v30-PERMISSIONLESS-SWEEP.md` exists with 9 required sections; Permissionless Sweep Table uses 3-class closed taxonomy per D-08; Classification Distribution Heatmap reconciles with row count; Pass 1 grep commands preserved for reviewer reproducibility per D-11.
- `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md` exists with HEAD anchor, grep-command reproducibility, D-15 RNG-03 reconciliation status, and full frontmatter.
- Zero F-30-NN per D-22. Zero contracts/ or test/ writes per D-27. Zero edits to Phase 237 inventory / Phase 238 outputs / KNOWN-ISSUES per D-28.
- Exactly 1-2 new commits on `main` with subject prefix `docs(239-02):`.
</verification>

<success_criteria>
- RNG-02 requirement satisfied: every permissionless function in `contracts/` at HEAD 7ab515fe classified per D-08 3-class closed taxonomy `{respects-rngLocked / respects-equivalent-isolation / proven-orthogonal}` with `CANDIDATE_FINDING` escape per D-23.
- Two-pass methodology per D-11 documented: Pass 1 mechanical grep preserved for reviewer reproducibility, Pass 2 semantic classification against Phase 237 inventory.
- D-12 input-source preservation: Phase 237 `audit/v30-CONSUMER-INVENTORY.md` Consumer Index + Per-Consumer Call Graphs consumed directly; Phase 238 output NOT consumed (preserves D-02 single-wave parallel topology).
- D-15 forward-cite to Plan 239-03 RNG-03(a) Asymmetry A held; reconciliation via SUMMARY erratum if structure diverges.
- Fresh-eyes mandate upheld; prior-milestone artifacts cross-cited with `re-verified at HEAD 7ab515fe` notes.
- Finding Candidates appendix routes any unclassifiable row to Phase 242 FIND-01 intake; zero F-30-NN IDs emitted.
- READ-only scope preserved across all tasks.
</success_criteria>

<output>
After completion, `audit/v30-PERMISSIONLESS-SWEEP.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md` are committed on `main` at HEAD descendant of `7ab515fe`. Phase 239 Plan 01 (RNG-01 state machine) + Plan 03 (RNG-03 asymmetry re-justification) run in parallel Wave 1 per D-02 — no cross-dependencies beyond D-15 forward cite.
</output>
