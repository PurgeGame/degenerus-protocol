# v65.0 Token Rename (BURNIE→FLIP + Stonk→DGNRS/sDGNRS) — ARCHIVED

✅ **SHIPPED + PUSHED 2026-06-16** · Phases 406–409 · baseline = v64.0 closure `891f7a8f` (contracts tree `402855e1`) · rename commit `44642aba` · `origin/main` `4adcd5f4` · implementation milestone (not an audit) · **full FUNCTIONAL PARITY (names/selectors/metadata strings only, zero logic change)**.

**OUTCOME:** the protocol currency + equity tokens were rebranded in the contracts repo with the creator persona, the `burn` verb, and the `coinflip` action preserved. **forge 889/0/110 — byte-identical to baseline**; hardhat back to baseline parity; gas-regression + 16.7M advanceGame ceiling + EIP-170 all hold at the bumped optimizer setting. Stats: 5 commits, contract diff 31 files **1037+/1037− (balanced = name-only)** + 15 file renames; full range 149 files +3205/−2848.

## Scope (USER-directed 2026-06-16, BROAD)
Execute the BURNIE→FLIP rename **plus** the equity-token rename **within this repo** (contracts + forge/JS tests + deploy scripts). The website/papers repo is the separate, out-of-scope half. BROAD = also rebrand the `coin`-as-the-token identifiers; the equity rename is FULL (interfaces/errors/vars).

**Renames:** `BurnieCoin`→`FLIP`, `BurnieCoinflip`→`Coinflip`, `IBurnieCoinflip`→`ICoinflip` (+Player/LinkReward/Affiliate); `DegenerusStonk`→`DGNRS`, `StakedDegenerusStonk`→`sDGNRS`, `IStakedDegenerusStonk`→`IsDGNRS` (+ collision fix: Admin minimal `IsDGNRS`→`IsDGNRSVotes`), `IDegenerusStonkWrapper`→`IDGNRS`, `IStakedDegenerusStonkBurn`→`IsDGNRSBurn`, `OnlyStakedDegenerusStonk`→`OnlysDGNRS`; `DGVB`→`DGVF`; `flipWindowOpen`→`ticketRedemptionOpen`. Strings: `name "Burnies"`→`"Degenerus Gambling Token"`, `symbol "BURNIE"`→`"FLIP"`, `"Degenerus Stonk"`→`"Degenerus Protocol Equity Token"` (+ staked). **KEEP:** creator persona `Burnie Degenerus` (19 files) + email, the `burn` verb, the `coinflip`/`flip` action, `DGVE`, generic `coin` (`burnCoin`/`mintForGame`/`PRICE_COIN_UNIT`/`IDegenerusCoin`/`IVaultCoin`).

## Method
Deterministic, ordered, exact-match rename engine (`scripts/v65_flip_rename.py`): mask creator → metadata strings → special contract/interface names (longest-first; `DegenerusAdmin.sol`-scoped collision rule for idempotency) → case-preserving global `burnie→flip` (safe: the `burn` verb has no "ie", `coinflip` has no "burnie") → BROAD `coin`-as-token → STONK → DGVB → SEMANTIC. Authoritative surface map: `.planning/v65-FLIP-RENAME-SURFACE-MAP.md` (built from an 8-agent read-only classification). Validated by dry-run (correctness + completeness) and proven idempotent (0 changes on re-run across 60 contracts + 252 test/scripts).

## Phases (all SHIPPED, in commit `44642aba` + scaffolding)
- **406 FOUNDATION** — green baseline captured (forge 889/0/110 ⚠ via warm cache; `forge clean` exposed the latent issue below), canonical rename table + carve-outs + selector/event/artifact manifest locked, deterministic engine built + dry-run validated. KEEP-01/02/03, EXT-01(draft).
- **407 CONTRACT REFACTOR** — engine applied to all contracts + 6 `.sol` file renames; one batched diff → USER hand-review → approval → commit (2-layer commit-guard: `CONTRACTS_COMMIT_APPROVED=1` + pre-commit hook move-aside). CTR-01..04, STR-01/02, IDT-01/02/03, STK-01/02/03, KEEP-01/02/03.
- **408 TESTS & SCRIPTS** — 107 test/script files renamed + 9 test-file renames + `predictAddresses.js`/`deployFixture`/`package.json` artifact-name deps. TST-01/02/03.
- **409 VERIFY & RE-ATTEST** — forge 889/0/110 identical; hardhat baseline-parity (the 2 byte-identical-to-HEAD source guards self-resolved post-commit); gas tests + 16.7M ceiling + EIP-170 verified; renamed subject re-frozen at `44642aba`. VER-01/02/03, EXT-01(final = surface-map §6/§10/§11).

## Milestone summary
- **Key wrinkle (resolved):** the rename tipped `_purchaseForWithCached` (MintModule) one slot over the `viaIR` stack-too-deep limit at `optimizer_runs=200` — the function sat exactly at the edge; both forge & hardhat baselines had masked it via warm cache (a pure name change perturbs solc's brittle stack-layout heuristic). **USER chose Option B: `optimizer_runs` 200/50 → 1000** (foundry + hardhat). Resolves it; *lowers* runtime gas; all deployed code fits EIP-170 (Game margin 4,149; MintModule 23,156 / margin 1,420). No gas recalibration needed (regression tests + ceiling pass).
- **Engine non-idempotency bug (found + fixed):** re-running the full engine on already-processed files re-fired the `IsDGNRS→IsDGNRSVotes` collision rule, clobbering the main `IsDGNRS` in 2 files. Reverted + made the rule `DegenerusAdmin.sol`-scoped → idempotent. Lesson: for follow-on renames, prefer targeted single-rule subs.
- **Tech debt / follow-ups:** (1) the off-chain indexer/ABI must re-vendor (selector/event-topic/artifact changes — manifest in the surface map); (2) the website/papers repo rename (separate repo); (3) the 3 post-v64 contract commits (`eb8e91b2` century streak-shield, `ab1214c3` afking streak floor, `ff09d19b` PrizePoolDailySnapshot) remain UNAUDITED — a future audit-milestone candidate.
