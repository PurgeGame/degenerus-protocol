---
phase: 325-spec-design-lock-call-graph-attestation-shared-surface-recon
plan: 03
type: execute
wave: 2
depends_on: ["325-01", "325-02"]
files_modified:
  - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
autonomous: true
requirements: [BATCH-01, RFALL-04, KEEP-04, KEEP-05, POOL-06]
must_haves:
  truths:
    - "325-SPEC.md settles ONE final signature per multi-item shared construct in DegenerusGame.sol (items 2/3/7 + inline claimableWinnings[SDGNRS] debit), StakedDegenerusStonk.sol (items 2/4), DegenerusVault.sol (items 3/4/7) with an apply-order so none of items 2/3/4/7 can land as a conflicting independent diff"
    - "Every SPEC-time open item is resolved on paper: RFALL-04 (D-06 single pendingRedemptionEthValue), KEEP-04 (VAULT registered code), KEEP-05 (autoOpen scope), POOL-06 (D-04 accept-as-minor no second sweep), BTOMB vaultAllowance checked-add/cap one-shot packing, HERO-04 payout shape (D-01/02/03) + S=8/S=9 (>32-bit) packing"
    - "The SPEC folds the Plan 01 ATTEST blocker roll-ups + the Plan 02 SWAP no-arb margin/STOP verdict, jitter-source pin, and swap-pop enumeration result into a per-item IMPL blueprint + a file/edit-order map for Phase 326"
    - "The matches 0-8 to 0-9 event-range widening is flagged as a frontend/indexer out-of-scope concern (flag, not fix)"
  artifacts:
    - path: ".planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md"
      provides: "The reconciled v48.0 design-lock blueprint: attestation verdict roll-up (section 0), shared-signature reconciliation R1..Rn (section 1), open-item resolutions (section 2), per-item IMPL blueprint + edit-order map (section 3)"
      contains: "Shared Signatures"
  key_links:
    - from: "the 4 ATTEST docs (PFIX-RFALL, KEEP-POOL, BTOMB-HERO, SWAP)"
      to: "325-SPEC.md section 0 verdict roll-up + section 1 shared signatures"
      via: "fold attestation drift + open-item verdicts into the settled signatures"
      pattern: "pullRedemptionReserve|sellFarFutureTickets|recoverAfKingPool|affiliateCode"
    - from: "DegenerusGame.sol items 2+3+7 joint edits"
      to: "one settled signature per shared construct + apply-order"
      via: "section 1 reconciliation R-rows"
      pattern: "apply.order|Shared Signatures"
---

<objective>
Author `325-SPEC.md` — the reconciled v48.0 design-lock blueprint (the analog of v47's 321-SPEC.md) —
consuming the four `325-ATTEST-*.md` docs from Plans 01 and 02. Three jobs:
(1) settle ONE final signature per multi-item shared construct across the three shared files so none
of items 2/3/4/7 can land as a conflicting independent diff, with an explicit apply-order;
(2) resolve every SPEC-time open item on paper — RFALL-04 (D-06), KEEP-04, KEEP-05, POOL-06 (D-04),
the BTOMB `vaultAllowance` checked-add/cap one-shot packing, and the HERO-04 payout SHAPE
(D-01/02/03) + the S=8/S=9 (>32-bit) packing scheme;
(3) produce the section-0 attestation verdict roll-up + the per-item IMPL blueprint + file/edit-order
map that is the load-bearing input to Phase 326.

Purpose: this is the BATCH-01 reconciliation half plus the four SPEC-owned requirements (RFALL-04,
KEEP-04, KEEP-05, POOL-06). After this, Phase 326 applies one fully reconciled batched diff with zero
"by construction" assumptions and zero shared-signature conflicts.

Output: one `325-SPEC.md` under the phase directory. ZERO contracts/*.sol mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-CONTEXT.md

# The precedent SPEC to mirror (section 0 corrections / section 1 reconciliation / section 2 blueprint + edit-order map):
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md

# The 7 plan docs (the binding design — read each for the shared-construct edits + open items):
@.planning/PLAN-V48-PRESALE-BOX-DRAIN-FIX.md
@.planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md
@.planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md
@.planning/PLAN-V48-AFKING-POOL-RECOVERY.md
@.planning/PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md
@.planning/PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md
@.planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md

<wave1_inputs>
# Read all four ATTEST docs produced by Plans 01 + 02 — they are the verdict inputs to the SPEC:
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md
# And the two SUMMARYs (the consumable roll-ups):
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-01-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-02-SUMMARY.md
</wave1_inputs>

<shared_surface_map>
<!-- The multi-item overlap the SPEC MUST settle to one signature each (per ROADMAP SC1 + CONTEXT). -->
DegenerusGame.sol (items 2/3/7):
  - item 2 RFALL: `pullRedemptionReserve` gains an ETH-vs-stETH coverage branch (single pendingRedemptionEthValue, D-06)
  - item 3 KEEP: the in-game mass-resolve/open crank entrypoints RENAMED (autoBuy/autoOpen/autoResolve)
  - item 7 SWAP: NEW `sellFarFutureTickets(address,uint32[],uint256[],uint256[])` entrypoint + the inline `claimableWinnings[SDGNRS]` debit (mirrors pullRedemptionReserve's checked pattern)
StakedDegenerusStonk.sol (items 2/4):
  - item 2 RFALL: `_submitGamblingClaimFrom` maxIncrement pull gains the pure-ETH-OR-pure-stETH segregation
  - item 4 POOL: `receive()` relaxed to accept AF_KING; `burnAtGameOver` folds afKing.withdraw(poolOf(this)) BEFORE the balanceOf(this)==0 early-return; inline IAfKingSubscribe (:57) gains withdraw(uint256)+poolOf(address)
DegenerusVault.sol (items 3/4/7):
  - item 3 KEEP: AfKing affiliate-code pass-through set to VAULT's registered code
  - item 4 POOL: NEW permissionless `recoverAfKingPool()`; inline IAfKing/IAfKingSubscribe (:76) gains withdraw(uint256)+poolOf(address)
  - item 7 SWAP: NEW `gameSellFarFutureTickets(...) onlyVaultOwner` wrapper + the IDegenerusGamePlayerActions interface entry
ISOLATED (no shared-signature reconciliation, but still blueprinted): item 1 PFIX (LootboxModule 1-line),
  item 5 BTOMB (BurnieCoin + GameOverModule), item 6 HERO (DegeneretteModule).
Cross-repo (flag, land before freeze): DroneManager +1 typed pass-through (folds into its pending v47 re-sync).
</shared_surface_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 325-SPEC.md section 0 (attestation roll-up) + section 1 (shared-signature reconciliation R-rows)</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-03-PLAN.md (this plan, the shared_surface_map + wave1_inputs)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md (the section 0 carried-corrections + section 1 R1..R7 reconciliation format to mirror)
    - all four 325-ATTEST-*.md docs (the verdict inputs — every SHIFTED/ABSENT row + the KEEP-04/05/POOL-05 + SWAP verdicts)
    - .planning/phases/325-.../325-01-SUMMARY.md + 325-02-SUMMARY.md (the consumable roll-ups)
    - .planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md (RFALL shared edits) + .planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md (SWAP shared edits + section 10 contract placement) + .planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md + .planning/PLAN-V48-AFKING-POOL-RECOVERY.md (KEEP/POOL shared edits)
  </read_first>
  <action>Create 325-SPEC.md with a header (Milestone v48.0, Baseline HEAD da5c9d50, Inputs = the 7 plan docs + the 4 ATTEST docs, Owns = BATCH-01 + RFALL-04 + KEEP-04 + KEEP-05 + POOL-06).
Section 0 ATTESTATION VERDICT: fold the four ATTEST docs into one roll-up — total IMPL-blocker count across all 7 items, the list of carried corrections (line-drift + any material clarifications, mirror the v47 C1..Cn style), and a re-grep-at-edit-time note (the batched diff shifts lines). State the SWAP no-arb margin verdict from Plan 02 (the re-derived ceiling margin, or surface the STOP if Plan 02 emitted one) and the KEEP-04/KEEP-05/POOL-05 resolutions.
Section 1 SHARED SIGNATURES (BATCH-01 reconciliation) — a "## Shared Signatures" heading with one settled signature + apply-order per shared construct, mirroring v47's R1..R7:
  R1 DegenerusGame.pullRedemptionReserve — the FINAL signature with the ETH-vs-stETH coverage branch (D-06 single pendingRedemptionEthValue, donation-robust, fail-closed revert-if-neither), composing on top of the v47 SDGNRS-gated form.
  R2 DegenerusGame crank-entrypoint RENAME — the final autoBuy/autoOpen/autoResolve names (use the KEEP-05 verdict to scope autoOpen as existing-or-new) + the affiliate-code arg now passing VAULT's registered code (KEEP-03/04).
  R3 DegenerusGame.sellFarFutureTickets — the final entrypoint signature + the inline claimableWinnings[SDGNRS] debit (mirroring pullRedemptionReserve's checked pattern), the swap-pop _removeFarFutureTickets primitive, and the apply-order vs R1/R2 on the same file.
  R4 StakedDegenerusStonk — the joint _submitGamblingClaimFrom maxIncrement segregation (item 2) + receive() AF_KING relaxation + burnAtGameOver pool-recovery placement BEFORE the balanceOf(this)==0 early-return (item 4) + the inline IAfKingSubscribe withdraw/poolOf adds (POOL-05 verbatim-match) — settled together with apply-order.
  R5 DegenerusVault — the affiliateCode pass-through (item 3) + recoverAfKingPool() permissionless (item 4) + gameSellFarFutureTickets onlyVaultOwner wrapper + the IDegenerusGamePlayerActions interface entry (item 7) — settled together.
  R6 the cross-repo DroneManager +1 typed pass-through (flag, lands before freeze, folds into its v47 re-sync) + the LOCKED OPEN-E operator-trust disposition confirmed to cover the first value-destructive operator action.
For each R-row state explicitly which items co-edit the construct and the order to apply them inside the single diff. NO contracts/*.sol edit.</action>
  <verify>
    <automated>grep -q "Shared Signatures" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "pullRedemptionReserve" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "sellFarFutureTickets" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "recoverAfKingPool" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -Eqi "apply.order" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "da5c9d50" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - 325-SPEC.md exists with a header naming baseline HEAD da5c9d50 + Owns = BATCH-01/RFALL-04/KEEP-04/KEEP-05/POOL-06.
    - Section 0 rolls up the four ATTEST docs into a single IMPL-blocker count + carried corrections + the SWAP no-arb margin verdict (or surfaced STOP) + the KEEP-04/KEEP-05/POOL-05 resolutions.
    - A "## Shared Signatures" section settles ONE signature each for: DegenerusGame.pullRedemptionReserve coverage branch, the renamed crank entrypoints, DegenerusGame.sellFarFutureTickets + the inline claimableWinnings[SDGNRS] debit, the StakedDegenerusStonk joint receive()/burnAtGameOver/_submitGamblingClaimFrom + interface adds, and the DegenerusVault affiliateCode/recoverAfKingPool()/gameSellFarFutureTickets wrapper.
    - Every R-row names the co-editing items and an explicit apply-order within the single diff.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-SPEC.md sections 0 + 1 committed: attestation roll-up folded, one settled signature + apply-order per shared construct so items 2/3/4/7 cannot land as conflicting diffs; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Author 325-SPEC.md section 2 (open-item resolutions) + section 3 (per-item IMPL blueprint + edit-order map)</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-03-PLAN.md (this plan)
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md (sections 0+1 just authored by Task 1 — section 2/3 must be consistent with the settled signatures)
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-CONTEXT.md (the locked decisions D-01..D-06 + the Claude's-Discretion attestation list — section 2 resolves exactly these)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md (section 2 per-item IMPL blueprint + the "Files in the diff / Order" edit-order map format to mirror)
    - .planning/PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md (HERO-04 shape: D-01 continuity / D-02 S=2 ~40-60% partial refund / D-03 bonus thresholds at S>=7; the QUICK_PLAY_PAYOUTS 9-bucket→10-bucket + S=8/S=9 >32-bit packing)
    - .planning/PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md (BTOMB vaultAllowance checked-add/cap one-shot)
    - .planning/PLAN-V48-AFKING-POOL-RECOVERY.md (POOL-06 D-04 accept-as-minor) + .planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md (RFALL-04 D-06) + .planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md (KEEP-04/05)
    - the four 325-ATTEST-*.md docs (the BTOMB feasibility row, the HERO anchor verdicts, the SWAP units/swap-pop result feed the blueprint)
  </read_first>
  <action>Append sections 2 and 3 to 325-SPEC.md.
Section 2 OPEN-ITEM RESOLUTIONS — one subsection per open item, each recording the decision + its source (D-NN or grep verdict) + the IMPL instruction it produces:
  - RFALL-04: D-06 single tracked pendingRedemptionEthValue (pure-ETH OR pure-stETH, no separate stETH-denominated reservation value), applied consistently across submit/claim/gameOver; coverage checked against the same asset basis the base is inflated by.
  - KEEP-04: state the VAULT registered-code verdict from the ATTEST (exists with owner==VAULT, or the conditional register-one setup step); pin which code AfKing passes.
  - KEEP-05: state the autoOpen existing-vs-new verdict and scope the rename accordingly.
  - POOL-06: D-04 accept-as-minor — NO second sweep in handleFinalSweep; record the donor-only post-gameOver re-stranding residual as a documented known-minor (VAULT unaffected — anytime recoverAfKingPool).
  - BTOMB packing: the _supply.vaultAllowance checked-add/cap so existing + 1e36 wei can't overflow uint128 (~340x headroom), strictly one-shot from the gameover-drain via the existing GAME-gated increase path.
  - HERO-04 shape: D-01 continuity (S=3..9 track old M=2..8 shift-by-one, forced by S=9 == old M=8) + D-02 S=2 ~40-60% partial-refund consolation + D-03 bonus-currency thresholds at S>=7; the byte-exact constants are SOLVED by derive_5_tables.py at TST (Phase 327) NOT here — record the shape as the design lock + the byte-reproduce-gate handoff.
  - S=8/S=9 packing: settle the re-pack scheme (both >32-bit; likely separate uint256 per N — record the chosen layout for QUICK_PLAY_PAYOUTS_N*).
Section 3 PER-ITEM IMPL BLUEPRINT + EDIT-ORDER MAP — a "Files in the diff" list with the order (storage/enums first → helpers → callers → entrypoints → interfaces, mirroring v47) and one blueprint paragraph per item (1 PFIX isolated 1-line, 2 RFALL via R1/R4, 3 KEEP via R2/R5, 4 POOL via R4/R5, 5 BTOMB isolated, 6 HERO isolated + the byte-reproduce-gate TST handoff, 7 SWAP via R3/R5/R6) referencing the R-rows from section 1. Flag the matches 0-8 → 0-9 event-range widening as a frontend/indexer out-of-scope concern (flag, not fix). End with a section-N success-criteria checklist mapped 1:1 to the ROADMAP Phase 325 SC1..SC5 + a "SOURCE-TREE not mutated at SPEC" line. NO contracts/*.sol edit.</action>
  <verify>
    <automated>grep -q "RFALL-04" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "KEEP-04" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "KEEP-05" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -q "POOL-06" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -Eqi "vaultAllowance|1e36" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -Eqi "S=2|partial.refund|continuity" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -Eqi "edit.order|Files in the diff" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md && grep -Eqi "0-9|indexer|frontend" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 resolves EACH open item with a labeled subsection: RFALL-04 (D-06), KEEP-04, KEEP-05, POOL-06 (D-04), BTOMB packing, HERO-04 shape (D-01/02/03), S=8/S=9 packing — each stating the decision + source + the IMPL instruction.
    - HERO-04 records the SHAPE design-lock only (continuity + S=2 ~40-60% partial refund + thresholds S>=7) and explicitly hands the byte-exact constants to the TST derive_5_tables.py byte-reproduce gate (Phase 327), not this phase.
    - POOL-06 records D-04 accept-as-minor (no second sweep) + the documented donor-only residual.
    - Section 3 has a "Files in the diff" edit-order map and one blueprint paragraph per item (1..7) referencing the section-1 R-rows; the matches 0-8→0-9 widening is flagged frontend/indexer out-of-scope.
    - A success-criteria checklist maps to ROADMAP Phase 325 SC1..SC5 + a SOURCE-TREE-not-mutated line.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-SPEC.md sections 2 + 3 committed: every SPEC-time open item resolved on paper, per-item IMPL blueprint + edit-order map produced as the Phase 326 input, SC1..SC5 checklist present; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only reconciliation. The boundary is the SPEC itself: an under-specified shared signature or an
unresolved open item lets a conflicting independent diff or an economic regression slip into Phase 326's
IMPL diff. This plan consumes the Wave-1 attestation evidence and must not contradict it.

| Boundary | Description |
|----------|-------------|
| ATTEST verdicts → SPEC settled signatures | A signature settled ignoring an item's co-edit on the same construct produces conflicting diffs |
| open-item decision (D-NN) → IMPL instruction | An open item left unresolved (or resolved against the locked decision) ships the wrong behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-R1 | Tampering | A shared signature is settled for item 3 (crank rename) omitting item 7's inline claimableWinnings[SDGNRS] debit / item 2's pullRedemptionReserve coverage branch on the same file → IMPL produces conflicting DegenerusGame.sol diffs | mitigate | Section 1 R-rows each name ALL co-editing items + an explicit apply-order; R1/R2/R3 jointly cover the DegenerusGame items 2/3/7 overlap, R4 the sStonk 2/4, R5 the Vault 3/4/7 |
| T-325-R2 | Information disclosure | The SWAP no-arb margin is recorded in the SPEC without carrying Plan 02's ceiling re-derivation (or its STOP) → a grinder-capturable +EV swap ships | mitigate | Section 0 folds Plan 02's exact re-derived ceiling margin verdict (or surfaces the STOP); the SPEC does not soften D-05's ceiling-not-mean requirement |
| T-325-R3 | Repudiation | An open item (RFALL-04/KEEP-04/KEEP-05/POOL-06/BTOMB/HERO-04/S8-S9) is left ambiguous → IMPL improvises | mitigate | Section 2 resolves each with a labeled subsection citing D-NN or the ATTEST grep verdict + the concrete IMPL instruction |
| T-325-R4 | Tampering | The interface adds (POOL-05) or the renamed entrypoints (KEEP) settle to signatures that don't match AfKing.sol / the live callers → revert at IMPL | mitigate | R2/R4 carry the POOL-05 verbatim-match + KEEP-05 scope verdicts from the ATTEST docs verbatim |
| T-325-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 325-SPEC.md exists with section 0 (attestation roll-up), section 1 (Shared Signatures R-rows w/ apply-order), section 2 (open-item resolutions), section 3 (IMPL blueprint + edit-order map).
- All 5 SPEC-owned requirements (BATCH-01 + RFALL-04 + KEEP-04 + KEEP-05 + POOL-06) are resolved in writing.
- The SWAP no-arb margin verdict (or STOP) from Plan 02 is folded into section 0.
- The matches 0-8→0-9 widening is flagged out-of-scope.
- Zero contracts/*.sol modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- One settled signature + apply-order per multi-item shared construct (SC1).
- Attestation verdicts folded; no un-grepped "by construction" claim survives (SC2, leaning on Wave 1).
- SWAP no-arb floor re-confirmation carried into the SPEC (SC3, from Plan 02).
- Every SPEC-time open item resolved (SC4).
- SWAP-06 swap-pop H-CANCEL-SWAP-MISS-class disposition carried into the SPEC blueprint (SC5, from Plan 02).
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-03-SUMMARY.md` when done, listing 325-SPEC.md's sections, the settled shared signatures, the open-item resolutions, and confirming SC1..SC5 coverage for the Phase 325 verification.
</output>
