---
phase: 325-spec-design-lock-call-graph-attestation-shared-surface-recon
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "The SWAP-08 no-arb inequality is re-derived from current source: max full payout 110% × fractionBps(6) = 16.5% of face @d6 is shown < the cheapest far-future-entry acquisition (~21%, lootbox tier-1) re-confirmed at HEAD da5c9d50, with the numeric margin stated and a STOP-if-violated rule"
    - "BURNIE is confirmed unable to mint a far-future (d>=6) entry at the baseline (purchaseCoin has no level arg; mint targets cachedLevel/+1; v47 removed the BURNIE-lootbox path)"
    - "The SWAP-03 jitter seed is pinned to an already-SETTLED past VRF word (named source) confirmed freeze-safe per v45-vrf-freeze-invariant (no new mutable SLOAD enters the rng window)"
    - "Every ticketQueue / _tqFarFutureKey consumer is enumerated and shown to maintain membership-iff-packed-nonzero under the O(1) caller-verified swap-pop, NOT reproducing the H-CANCEL-SWAP-MISS operation class; far-future jackpot samplers gain no hot-path read"
    - "The priceForLevel/ticket unit basis (owed in entries, 4 entries = 1 ticket, oneTicketWei = priceForLevel(currentLevel)) is grep-confirmed so the no-arb arithmetic uses the true face basis"
  artifacts:
    - path: ".planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md"
      provides: "The load-bearing SWAP-08 no-arb re-derivation + SWAP-03 jitter-source pin + SWAP-06 swap-pop enumeration + SWAP unit/anchor attestation at HEAD da5c9d50"
      contains: "STOP"
  key_links:
    - from: "fractionBps(6) jitter-ceiling payout (16.5% of face)"
      to: "cheapest far-future-entry acquisition (~21%, lootbox tier-1) at HEAD da5c9d50"
      via: "re-derivation from PriceLookupLib.priceForLevel + lootbox tier-1 cost grep"
      pattern: "margin|STOP"
    - from: "SWAP swap-pop on full sell-out"
      to: "ticketQueue[_tqFarFutureKey] far-future jackpot samplers"
      via: "membership-iff-packed-nonzero invariant maintenance, no persistent cursor on the popped key"
      pattern: "sampleFarFutureTickets|_awardFarFutureCoinJackpot|processFutureTicketBatch"
---

<objective>
Re-attest the load-bearing economics + RNG + queue-safety of item 7 (the sDGNRS far-future salvage
swap) against the v47.0-closure HEAD `da5c9d50989707c8964a9411e68c51ca1b1a25f2` — the single most
security-critical SPEC deliverable. Produce `325-ATTEST-SWAP.md` covering:
(1) the SWAP-08 no-arb inequality RE-DERIVED from current source at the jitter band CEILING (not the
mean), with the numeric margin and a hard STOP-if-violated rule;
(2) BURNIE confirmed unable to mint a far-future entry;
(3) the SWAP-03 jitter seed pinned to a named already-SETTLED past VRF word, freeze-safe;
(4) the SWAP-06 swap-pop enumeration of every `ticketQueue`/`_tqFarFutureKey` consumer proving the
`membership-iff-packed-nonzero` invariant is maintained and the `H-CANCEL-SWAP-MISS` operation class
is NOT reproduced;
(5) the `priceForLevel`/ticket unit basis grep-confirmed so the arithmetic uses the true face.

Purpose: D-05 accepts the ~4.5pp ceiling margin as the security floor, but the SPEC MUST re-derive it
from live source — if the margin does NOT hold at the band ceiling, the executor STOPS and surfaces,
never silently proceeds. This is the BATCH-01 attestation half for the load-bearing item.

Output: one `325-ATTEST-SWAP.md` under the phase directory. ZERO contracts/*.sol mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-CONTEXT.md

# The load-bearing plan doc (read section 0 attestation priorities #1-#5, section 3 jitter band, section 5 swap-pop mechanics, section 6 guards in full):
@.planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md

# The negative precedent the swap-pop must NOT reproduce:
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md

<attestation_method>
contracts/ is byte-identical to baseline HEAD da5c9d50 (verified zero-drift). Grep the LIVE tree;
verdicts are baseline-anchored. For belt-and-suspenders: `git show da5c9d50:contracts/<path> | grep -n`.
Read source from contracts/ ONLY. State the byte-identical-to-da5c9d50 fact in the doc header.

The no-arb re-derivation is the load-bearing deliverable. D-05 LOCKS the ~4.5pp margin as ACCEPTABLE,
but the SPEC must SHOW the arithmetic from source: if the re-derived margin is non-positive at the band
CEILING, emit a `## STOP — NO-ARB MARGIN VIOLATED` block and surface to the user; do NOT widen the curve
or band (curve `15%@d6 to 5%@d100`, band `fraction in [70%,110%]`, `cash in [20%,60%]` are NOT changed here).
</attestation_method>

<interfaces>
<!-- Grep-confirmed SWAP anchors at the baseline (DegenerusGame.sol ~2748 lines). -->
DegenerusGame.sol:
  :449 `function _resolvePlayer(` (operator-approval honor; seller resolution path)
  `_tqFarFutureKey(L)` (disjoint far-future key space for level > currentLevel + 5 == the d>=6 range)
  `ticketsOwedPacked[ffk][player]` (uint40 packed [32 owed][8 rem]; TICKET_SCALE=100)
  `ticketQueue[ffk]` (append-only address[]; push-on-first-nonzero; never removed until drain today)
  `_queueTickets(SDGNRS, L, n, false)` (reuse to credit sDGNRS; inner RngLocked + _livenessTriggered guards)
  `sampleFarFutureTickets` (BAF far-future jackpot sampler ~:2592) — must gain NO hot-path read
  `ticketsOwedView` (~:2214, near-future _tqWriteKey — returns 0 for far-future; swap needs a far-aware read)
  `pullRedemptionReserve` (~:1888 — the checked-subtraction pattern the inline claimable[SDGNRS] debit mirrors)
  `dailyHeroWagers`/`_rollHeroSymbol` (~:2693 — unrelated; do not touch)
DegenerusGameJackpotModule.sol:
  `_awardFarFutureCoinJackpot` (25% daily coin far-future sampler ~:1754) — must gain NO hot-path read
MintModule.sol:
  `processFutureTicketBatch` (~:438-450 — the ONLY cursor-iterator; rngLock-gated on the NEAR-future key)
  `purchaseCoin` (~:858-862 — no level arg; BURNIE mint) + mint target cachedLevel/+1 (~:1360)
AdvanceModule.sol:
  far-future queue `length > 0` gates (~:214/:262/:1083); `_runEarlyBirdLootboxJackpot` (~:639 — draws activating-level queue, NOT far membership — confirm)
PriceLookupLib: `priceForLevel(level)` (the only non-manipulable face anchor; mint cost = price × quantity/(4×TICKET_SCALE))
Lootbox tier-1 acquisition cost (~21% of face) — re-confirm the cheapest ETH far-entry path at the baseline.
SWAP-03 jitter seed candidate: `rngWordByDay[currentDay-1]` / `lastDayRng` — pin the EXACT settled-word source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive SWAP-08 no-arb floor + confirm BURNIE-cant-mint-far + pin SWAP-03 jitter source (325-ATTEST-SWAP.md sections A/B/C)</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-02-PLAN.md (this plan, interfaces + attestation_method)
    - .planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md (section 0 #1 no-arb / #2 units / #3 re-grep; section 3 jitter band + freeze note; section 6.3 rngLock gate)
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-CONTEXT.md (D-05 ~4.5pp margin lock; SWAP-03 jitter Discretion item)
    - contracts/libraries/PriceLookupLib.sol (or wherever priceForLevel lives) — the face anchor + mint-cost formula price × quantity/(4×TICKET_SCALE)
    - contracts/modules/DegenerusGameLootboxModule.sol + contracts/modules/DegenerusGameMintModule.sol (lootbox tier-1 acquisition cost ~21% of face; purchaseCoin no-level-arg ~:858-862; mint target cachedLevel/+1 ~:1360; whale-bundle far-grant for the worked cross-check)
    - contracts/DegenerusGame.sol (the settled past VRF word source: rngWordByDay / lastDayRng; rngLocked() gate; confirm no new mutable SLOAD enters the rng window)
  </read_first>
  <action>Create 325-ATTEST-SWAP.md with a Scope + Sources-of-truth + byte-identical-to-da5c9d50 header, then three sections.
Section A NO-ARB RE-DERIVATION (SWAP-08): from source compute (i) fractionBps(6)=1500 (15% of face) per the section-3 two-line curve; (ii) the jitter-ceiling payout 110% x 15% = 16.5% of face at d6; (iii) the cheapest far-future-entry acquisition cost RE-CONFIRMED from the lootbox tier-1 path (the ~21%-of-face number) by reading the actual lootbox cost/grant code at the baseline — show the arithmetic, do not assume 21%; (iv) the numeric margin (acquisition minus ceiling) stated explicitly. Include the section-0 whale-bundle worked cross-check (2.4 ETH cost vs <=16.5% salvage ceiling) as corroboration. State the hard rule: if margin is non-positive at the ceiling, emit a STOP block naming the violated distance and surface — do NOT widen the curve/band. Also assert and grep-show that cash share <= 60% makes the cash leg a subset of the full-fraction bound (max withdrawable approx 9.9% of face at d6) so the cash split does not move the bar.
Section B BURNIE-CANT-MINT-FAR: grep-confirm purchaseCoin takes no level arg, every mint targets cachedLevel/cachedLevel+1, and v47 removed the BURNIE-lootbox path — so the only token cheap enough to threaten 15% cannot acquire a d>=6 entry. Enumerate the remaining d>=6 acquisition paths (whale bundle, deity, lootbox, un-farmable win rewards) and confirm each is ETH-priced >= ~21% or un-farmable.
Section C JITTER SOURCE PIN (SWAP-03): name the EXACT settled past VRF word the jitter seed reads (e.g. rngWordByDay[currentDay-1]), grep-confirm it is already-revealed/unlocked at offer time (NOT the in-flight cycle's word), and that reading it adds NO new mutable SLOAD inside the rng window — freeze-safe per v45-vrf-freeze-invariant. Confirm the swap stays rngLocked()-gated. Note the offer is player-computable (wait-able/grindable) so all safety bounds are proven at the band CEILING. NO contracts/*.sol edit.</action>
  <verify>
    <automated>grep -q "16.5" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -qi "margin" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "STOP" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "purchaseCoin" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -Eqi "rngWordByDay|lastDayRng|settled" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "da5c9d50" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 325-ATTEST-SWAP.md exists; header states contracts/ byte-identical to da5c9d50.
    - Section A shows the full arithmetic: fractionBps(6)=1500 → 15% of face → 110% jitter ceiling = 16.5% of face @d6; the lootbox tier-1 acquisition cost is RE-CONFIRMED from source code (arithmetic shown, not assumed) and the numeric (acquisition − 16.5%) margin is stated. A STOP-if-violated rule (margin non-positive at the ceiling → STOP block + surface, no curve/band widening) is present.
    - Section A confirms cash share <= 60% keeps the cash leg a subset of the full-fraction bound (max withdrawable approx 9.9% of face @d6).
    - Section B grep-confirms purchaseCoin has no level arg, mint targets cachedLevel/+1, v47 removed the BURNIE-lootbox path, and enumerates the remaining d>=6 acquisition paths as ETH-priced >= ~21% or un-farmable.
    - Section C names the EXACT settled past VRF word for the jitter seed, confirms it is revealed-at-offer (not in-flight), adds no new mutable SLOAD in the rng window, and the swap stays rngLocked()-gated.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-ATTEST-SWAP.md sections A/B/C committed: no-arb margin re-derived with STOP rule, BURNIE-can't-mint-far confirmed, jitter source pinned freeze-safe; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate every ticketQueue/_tqFarFutureKey consumer + prove swap-pop safety + pin units (325-ATTEST-SWAP.md sections D/E)</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-02-PLAN.md (this plan, interfaces)
    - .planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md (section 5 ticket-ledger mechanics + swap-pop, "Why swap-pop is safe here (vs H-CANCEL-SWAP-MISS)", section 2 units; section 0 #2 units RESOLVED + #4 swap-pop)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md (the H-CANCEL-SWAP-MISS operation-class anatomy: a PERSISTENT sweep cursor across a swap-pop mutation — the thing to prove ABSENT here)
    - contracts/DegenerusGame.sol (`_tqFarFutureKey`, `ticketsOwedPacked`, `ticketQueue`, `_queueTickets`, `sampleFarFutureTickets` ~:2592, `ticketsOwedView` ~:2214)
    - contracts/modules/DegenerusGameJackpotModule.sol (`_awardFarFutureCoinJackpot` ~:1754)
    - contracts/modules/DegenerusGameMintModule.sol (`processFutureTicketBatch` ~:438-450 — the only cursor-iterator; confirm rngLock-gated on the NEAR-future key)
    - contracts/modules/DegenerusGameAdvanceModule.sol (`length > 0` gates ~:214/:262/:1083; `_runEarlyBirdLootboxJackpot` ~:639 — confirm it draws the activating-level queue, NOT far-future membership)
    - contracts/libraries/PriceLookupLib.sol (priceForLevel; the mint-cost formula for oneTicketWei + faceWei unit basis)
  </read_first>
  <action>Append sections D and E to 325-ATTEST-SWAP.md.
Section D SWAP-06 SWAP-POP ENUMERATION: produce a table that ENUMERATES EVERY consumer of `ticketQueue[_tqFarFutureKey(L)]` / the far-future packed ledger, one row each — `sampleFarFutureTickets` (BAF), `_awardFarFutureCoinJackpot` (daily coin 25%), `processFutureTicketBatch` (the cursor-iterator), the AdvanceModule `length > 0` gates (:214/:262/:1083), `_runEarlyBirdLootboxJackpot` (:639), `ticketsOwedView` (:2214), and any other grep hit — each row stating the consumer's access pattern (random-access sampler / persistent-cursor / length-gate / view) and the verdict that it stays correct under the O(1) caller-verified swap-pop. Prove the invariant `membership-iff-packed-nonzero` (today: push-on-first-nonzero, never removed until drain → holds by construction; post-swap: full sell-out pops the verified index → still holds). Show the two samplers gain NO hot-path read (they keep their existing `winner != address(0)` check). Prove the swap-pop does NOT reproduce the `H-CANCEL-SWAP-MISS` operation class: that bug required a PERSISTENT sweep cursor reading the SAME key being mutated; here the only cursor-iterator (`processFutureTicketBatch`) runs at level-activation under rngLock on the NEAR-future key, never the far-future key being popped, so nothing is relocated behind a live cursor — state this as an explicit no-persistent-cursor-on-the-popped-key attestation. Confirm `_runEarlyBirdLootboxJackpot` draws the activating-level queue, not far membership.
Section E UNITS PIN (SWAP-02 basis): grep-confirm `owed` is in ENTRIES (4 entries = 1 whole ticket; purchase adds owed += 4, _queueTickets(VAULT,16)=4 tickets), `oneTicketWei = priceForLevel(currentLevel)` (NOT /4), and `faceWei = priceForLevel(L) × wholeTickets` so the section-A no-arb arithmetic and the section-5 worked example use the TRUE face basis (flag the plan doc's section-12 /4-per-ticket example as needing recompute, per its own NOTE). NO contracts/*.sol edit.</action>
  <verify>
    <automated>grep -q "sampleFarFutureTickets" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "_awardFarFutureCoinJackpot" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "processFutureTicketBatch" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "H-CANCEL-SWAP-MISS" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -Eqi "membership|packed" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md && grep -q "priceForLevel" .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Section D contains a per-consumer enumeration table covering at minimum sampleFarFutureTickets, _awardFarFutureCoinJackpot, processFutureTicketBatch, the AdvanceModule length>0 gates, _runEarlyBirdLootboxJackpot, and ticketsOwedView — each with an access-pattern + under-swap-pop verdict.
    - The `membership-iff-packed-nonzero` invariant is proven maintained (pre and post swap-pop); the two samplers are shown to gain no hot-path read.
    - An explicit attestation states no persistent cursor reads the far-future key being popped (the only cursor-iterator processFutureTicketBatch runs on the NEAR-future key under rngLock), so the H-CANCEL-SWAP-MISS operation class is NOT reproduced.
    - _runEarlyBirdLootboxJackpot confirmed to draw the activating-level queue, not far membership.
    - Section E pins the unit basis: owed in entries (4/ticket), oneTicketWei = priceForLevel(currentLevel) not /4, faceWei = priceForLevel(L) × wholeTickets; the plan-doc section-12 /4 example flagged for recompute.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-ATTEST-SWAP.md sections D/E committed: every ticketQueue consumer enumerated safe under swap-pop, H-CANCEL-SWAP-MISS class proven absent, units pinned to the true face basis; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only attestation. The boundary is between the plan doc's *claimed* economics/RNG/queue safety
and what the baseline source *actually* implements — an under-derived no-arb margin or a missed
`ticketQueue` consumer lets an economic exploit or a queue-corruption regression reach the IMPL diff.

| Boundary | Description |
|----------|-------------|
| jitter band CEILING payout → cheapest acquisition floor | If proven only at the mean, a waiter/grinder captures the +EV ceiling — the swap becomes a money pump |
| swap-pop mutation → far-future jackpot samplers / cursor-iterators | A persistent cursor on the popped key reproduces H-CANCEL-SWAP-MISS (silent eligibility/streak corruption) |
| settled VRF word → rng-window freeze invariant | A jitter seed read inside the in-flight window violates v45-vrf-freeze-invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-S1 | Elevation of privilege (economic) | SWAP-08 no-arb margin re-derived only at the mean, not the band CEILING | mitigate | Section A re-derives at the 110% ceiling (16.5% of face @d6) with the numeric margin stated and a hard STOP-if-non-positive rule; the curve/band are not widened to manufacture margin |
| T-325-S2 | Tampering | A `ticketQueue`/`_tqFarFutureKey` consumer missed in the enumeration breaks under swap-pop (phantom eligibility or H-CANCEL-SWAP-MISS regression) | mitigate | Section D enumerates EVERY grep-discovered consumer with an under-swap-pop verdict and proves membership-iff-packed-nonzero + no-persistent-cursor-on-the-popped-key |
| T-325-S3 | Tampering | A BURNIE path can mint a far-future entry cheaply, defeating the no-arb floor | mitigate | Section B grep-confirms purchaseCoin has no level arg + mint targets cachedLevel/+1 + v47 removed the BURNIE-lootbox path; remaining d>=6 paths enumerated ETH-priced/un-farmable |
| T-325-S4 | Tampering (RNG) | The jitter seed reads an in-flight VRF word → freeze-invariant violation | mitigate | Section C pins the seed to a named already-SETTLED past word, confirms no new mutable SLOAD in the rng window, swap stays rngLocked()-gated |
| T-325-S5 | Information disclosure (false basis) | The no-arb arithmetic uses the wrong /4-per-ticket unit basis → wrong margin | mitigate | Section E pins owed-in-entries / oneTicketWei = priceForLevel(currentLevel) / faceWei = priceForLevel(L) × wholeTickets and flags the plan-doc example for recompute |
| T-325-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 325-ATTEST-SWAP.md exists with sections A (no-arb), B (BURNIE-can't-mint-far), C (jitter source), D (swap-pop enumeration), E (units).
- The no-arb margin is re-derived from source with explicit numbers + a STOP-if-violated rule.
- Every ticketQueue/_tqFarFutureKey consumer has an enumeration row + under-swap-pop verdict; H-CANCEL-SWAP-MISS class proven absent.
- Zero contracts/*.sol modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- SWAP-08 no-arb floor RE-DERIVED at the band ceiling from live source with a hard STOP-if-violated rule (not assumed from D-05).
- BURNIE-can't-mint-far + jitter-source freeze-safety + units all grep-confirmed.
- SWAP-06 swap-pop proven NOT to reproduce H-CANCEL-SWAP-MISS across the full ticketQueue consumer set.
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-02-SUMMARY.md` when done, stating the re-derived no-arb margin (or the STOP verdict), the pinned jitter-word source, the swap-pop consumer-enumeration result, and the unit basis — for Plan 03 to fold into the SPEC.
</output>
