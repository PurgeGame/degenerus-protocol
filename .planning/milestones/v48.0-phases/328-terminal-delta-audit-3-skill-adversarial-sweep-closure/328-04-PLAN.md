---
phase: 328-terminal-delta-audit-3-skill-adversarial-sweep-closure
plan: 04
type: execute
wave: 3
depends_on: ["328-03"]
files_modified:
  - audit/FINDINGS-v48.0.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [BATCH-03]

must_haves:
  truths:
    - "A single USER checkpoint approves the closure verdict + the MILESTONE_V48_AT_HEAD_<sha> signal BEFORE it is emitted"
    - "MILESTONE_V48_AT_HEAD_<sha> is resolved to the concrete closure-HEAD SHA and propagated verbatim across all targets"
    - "The atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied with all 40 v48.0 requirements re-attested at closure"
    - "audit/FINDINGS-v48.0.md is chmod 444 at the resolved closure HEAD"
    - "git diff 1575f4a9 -- contracts/ is empty (closure is doc-only; subject frozen at 1575f4a9)"
  artifacts:
    - path: "audit/FINDINGS-v48.0.md"
      provides: "The locked (chmod 444) v48.0 findings deliverable with the resolved closure-signal SHA"
      contains: "MILESTONE_V48_AT_HEAD_"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 40 v48.0 REQ rows flipped to a closure attestation"
      contains: "BATCH-03"
  key_links:
    - from: ".planning/STATE.md"
      to: "audit/FINDINGS-v48.0.md"
      via: "verbatim closure-signal propagation"
      pattern: "MILESTONE_V48_AT_HEAD_"
    - from: ".planning/REQUIREMENTS.md"
      to: "the 40 v48.0 REQ-IDs"
      via: "closure re-attestation"
      pattern: "Complete|RESOLVED|NEGATIVE-VERIFIED"
---

<objective>
Author the SC4 CLOSURE FLIP: gate on a single USER approval of the closure verdict + the `MILESTONE_V48_AT_HEAD_<sha>` signal, then resolve the placeholder SHA, apply the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) with all 40 requirements re-attested, and chmod 444 the findings deliverable. Closure is LAST. Doc-only — ZERO `contracts/*.sol` edits (the subject stays frozen at `1575f4a9`).

Purpose: SC4 of the terminal phase — the milestone close. This is `autonomous: false` per the v45.0/v47.0 "USER-APPROVED closure" precedent + the project's review-before-closure discipline (feedback_pause_at_contract_phase_boundaries): the USER must approve the closure verdict + the signal string before it is emitted/propagated.
Output: the resolved `MILESTONE_V48_AT_HEAD_<sha>` signal propagated verbatim; the 5-doc flip applied; audit/FINDINGS-v48.0.md locked chmod 444.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v48.0.md
@audit/FINDINGS-v47.0.md

<interfaces>
<!-- The closure-flip mechanics (from the v46/v47 terminal precedent). Executor follows exactly. -->
- The 5 atomic flip targets: (1) ROADMAP.md — flip Phase 328 to Complete + the v48.0 milestone to shipped + the Progress table row; (2) STATE.md — move v48.0 from the active "Current Milestone" position to "Last Shipped Milestone" with the closure-signal + verdict, update progress (4/4 phases, percent 100), and clear the stale "Next:" warning; (3) MILESTONES.md — prepend the v48.0 archive entry (mirror the v47.0 archive block at the top of the file); (4) PROJECT.md — flip the Current Milestone section to the next state (v48.0 shipped); (5) REQUIREMENTS.md — flip all 40 v48.0 REQ rows in the Traceability table to a closure attestation (Complete / RESOLVED / NEGATIVE-VERIFIED as appropriate; the SPEC rows (5) + TST rows (9) already Complete stay, the 25 IMPL Pending rows + BATCH-03 flip to attested-at-closure).
- The closure signal: `MILESTONE_V48_AT_HEAD_<sha>` where `<sha>` is the v48.0 closure HEAD. Per the v44/v46/v47 2-commit sequential-SHA orchestration, the placeholder `<sha>` resolves to the closure commit's own SHA. The signal propagates VERBATIM to: audit/FINDINGS-v48.0.md frontmatter (closure_signal + audit_subject_head) + §1 + §9b/§9c; ROADMAP.md; STATE.md (Last Shipped Milestone); MILESTONES.md (archive). Replace every `MILESTONE_V48_AT_HEAD_<sha>` placeholder string with the resolved value in one pass.
- The audit subject is FROZEN at `1575f4a9`; the closure is a DOC-ONLY flip — no contracts/*.sol edits. `.planning/` is gitignored, so force-add planning docs at commit time (feedback_contract_commit_guard_hook); audit/FINDINGS-v48.0.md is tracked.
- chmod 444: applied to audit/FINDINGS-v48.0.md at closure (the v44/v46/v47 precedent — FINAL READ-only at the closure HEAD).
- The USER gate (autonomous: false): present the closure verdict (from FINDINGS-v48.0 §9a) + the proposed `MILESTONE_V48_AT_HEAD_<sha>` signal + the new_findings disposition (from 328-02) and WAIT for explicit USER approval BEFORE resolving the SHA / propagating / flipping. This is the single review-before-closure gate (v45.0/v47.0 "USER-APPROVED closure" precedent + feedback_pause_at_contract_phase_boundaries). Do NOT auto-advance even if auto_advance is on.
- new_findings: if 328-02 surfaced a FINDING_CANDIDATE, the USER adjudicates its disposition (DEFER / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED) at THIS gate, exactly as v47 F-47-01/F-47-02 were DEFER-adjudicated; the verdict's `0 NEW_FINDINGS` clause is amended to match. If a candidate is FIX-adjudicated, closure HALTS and a new contract phase is planned (the subject is frozen at 1575f4a9; any fix needs USER approval). If zero candidates, the `0 NEW_FINDINGS` clause holds verbatim.
- Commit discipline: planning docs + audit/FINDINGS-v48.0.md are AGENT-committable (no contracts/*.sol in the diff -> the commit-guard hook does not block; verify `git diff 1575f4a9 HEAD -- contracts/` empty before committing). End the commit message with the Co-Authored-By trailer per the global commit convention.
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is the doc-only milestone closure flip. The audit subject stays frozen at `1575f4a9`. The only "boundary" is the human review gate before the closure signal is emitted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-328-04-PREMATURE | Elevation | emitting closure signal before review | mitigate | The plan is autonomous: false — a single blocking USER checkpoint approves the verdict + signal BEFORE resolution/propagation (v45.0/v47.0 USER-APPROVED-CLOSE precedent). |
| T-328-04-SHADRIFT | Tampering | closure-signal SHA propagation | mitigate | The `MILESTONE_V48_AT_HEAD_<sha>` placeholder is resolved + propagated VERBATIM in one pass across all targets; a final grep confirms zero unresolved placeholders remain. |
| T-328-04-CONTRACTS | Tampering | frozen subject | mitigate | `git diff 1575f4a9 HEAD -- contracts/` verified empty before the closure commit; closure is doc-only. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: USER closure-verdict approval gate (before emitting MILESTONE_V48_AT_HEAD)</name>
  <files>(none — this is a blocking human-verify gate; the doc flips happen in Task 2 after approval)</files>
  <what-built>
    The full v48.0 audit substance is complete and consolidated: 328-01 delta-audit (every contract surface NON-WIDENING vs v47, each delta change mapped to one of the 7 v48 surfaces, F-47-01 + F-47-02 RESOLVED-AT-V48, regression 632/42 net-zero + Hardhat PASS_ALL GREEN), 328-02 adversarial sweep (3-skill disposition + skeptic filter), and audit/FINDINGS-v48.0.md (the 9-section deliverable, F-47-01 + F-47-02 recorded RESOLVED-AT-V48). The closure verdict + the proposed closure signal are ready but NOT yet emitted.
  </what-built>
  <how-to-verify>
    1. Read audit/FINDINGS-v48.0.md §9a (the closure verdict — locked-target + actual) and §4.1 (the adversarial sweep outcome: row counts + any FINDING_CANDIDATE).
    2. Confirm the delta-audit attests all 7 surfaces NON-WIDENING (328-01-DELTA-AUDIT.md), the regression baseline is 632/42 net-zero new regression (Hardhat PASS_ALL GREEN), and F-47-01 + F-47-02 are recorded RESOLVED-AT-V48.
    3. Approve (a) the closure verdict text, (b) the `MILESTONE_V48_AT_HEAD_<sha>` signal format, and (c) the disposition of any FINDING_CANDIDATE surfaced by 328-02 (DEFER / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED — the v47 F-47-01/F-47-02 DEFER precedent). If a candidate is FIX-adjudicated, closure HALTS and a new contract phase is planned (the subject is frozen at 1575f4a9; any fix needs USER approval).
  </how-to-verify>
  <read_first>
    - audit/FINDINGS-v48.0.md (§9a verdict + §4 adversarial disposition + §3 delta-surface)
    - .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md
    - .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-02-ADVERSARIAL-LOG.md
    - .planning/ROADMAP.md (the Phase 328 goal — the locked-target verdict source)
  </read_first>
  <action>
    Present the closure verdict (FINDINGS-v48.0 §9a), the adversarial sweep outcome (§4.1 row counts + any FINDING_CANDIDATE), the delta-audit NON-WIDENING attestation, and the F-47-01 + F-47-02 RESOLVED-AT-V48 dispositions to the USER, then BLOCK for explicit approval of the verdict text + the MILESTONE_V48_AT_HEAD signal format + any FINDING_CANDIDATE disposition. Resolve / propagate / flip nothing until approval is given. This gate must not auto-advance even with auto_advance on (per feedback_pause_at_contract_phase_boundaries — confirm direction at the closure boundary).
  </action>
  <verify>
    <human-check>The USER reviews FINDINGS-v48.0 §9a + §4.1 + the 328-01/328-02 logs and explicitly approves the verdict + the signal + any FINDING_CANDIDATE disposition.</human-check>
  </verify>
  <acceptance_criteria>
    - The closure verdict (FINDINGS §9a), the sweep outcome (§4.1), the F-47-01 + F-47-02 RESOLVED-AT-V48 dispositions, and any FINDING_CANDIDATE disposition are presented to the USER.
    - Execution BLOCKS until the USER explicitly approves the verdict + the signal format + the FINDING_CANDIDATE disposition.
    - No closure-signal SHA is resolved or propagated, and no doc is flipped, before approval.
    - This gate does NOT auto-advance even if auto_advance is on.
  </acceptance_criteria>
  <done>The USER has explicitly approved the closure verdict + the MILESTONE_V48_AT_HEAD signal + any FINDING_CANDIDATE disposition; execution is cleared to proceed to Task 2.</done>
  <resume-signal>Type "approved" to emit MILESTONE_V48_AT_HEAD and apply the 5-doc flip, or describe required changes / the FINDING_CANDIDATE disposition.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve the closure-signal SHA, apply the atomic 5-doc flip, chmod 444, commit</name>
  <files>audit/FINDINGS-v48.0.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - audit/FINDINGS-v47.0.md §9 (the 2-commit sequential-SHA closure orchestration + the verbatim propagation-target list to mirror)
    - audit/FINDINGS-v48.0.md (the placeholder targets to resolve: frontmatter + §1 + §9b/§9c)
    - .planning/ROADMAP.md (Phase 328 + the v48.0 milestone flip points + the Progress table)
    - .planning/STATE.md (the Current Milestone -> Last Shipped Milestone move + the stale "Next:" warning to clear)
    - .planning/MILESTONES.md (the v47.0 archive block at the top — the format to mirror for the v48.0 entry, prepended)
    - .planning/PROJECT.md (the Current Milestone section)
    - .planning/REQUIREMENTS.md (the Traceability table — all 40 v48.0 REQ rows)
  </read_first>
  <action>
    After USER approval (Task 1), execute the closure:
    1. Verify `git diff 1575f4a9 HEAD -- contracts/` is empty (the subject is frozen; closure is doc-only).
    2. Resolve the `MILESTONE_V48_AT_HEAD_<sha>` placeholder to the closure-HEAD SHA per the v44/v46/v47 2-commit sequential-SHA orchestration (the signal resolves to the closure commit's own SHA). Replace EVERY `MILESTONE_V48_AT_HEAD_<sha>` placeholder string in audit/FINDINGS-v48.0.md (frontmatter closure_signal + audit_subject_head, §1, §9b, §9c) with the resolved value.
    3. Apply the atomic 5-doc flip, propagating the resolved signal VERBATIM:
       - ROADMAP.md: Phase 328 -> Complete; the v48.0 milestone -> shipped (closure signal + verdict); the Progress table row 328 -> Complete.
       - STATE.md: move v48.0 to "Last Shipped Milestone" (closure signal + the 4-phase summary + verdict + the new_findings disposition); update progress (4/4 phases, percent 100); CLEAR the stale "Next:" warning about the HERO-04 landing (it is DONE — landed at `1575f4a9`).
       - MILESTONES.md: prepend the v48.0 archive entry mirroring the v47.0 block (subject `1575f4a9`, baseline `da5c9d50`, the 4-phase shape, closure signal, F-47-01 + F-47-02 RESOLVED-AT-V48, regression 632/42 + Hardhat PASS_ALL GREEN, any FINDING_CANDIDATE disposition).
       - PROJECT.md: flip the Current Milestone section (v48.0 shipped).
       - REQUIREMENTS.md: flip all 40 v48.0 REQ rows in the Traceability table to a closure attestation (the 25 Pending IMPL rows + BATCH-03 -> Complete/attested-at-closure; the 5 SPEC rows + 9 TST rows already Complete stay).
    4. chmod 444 audit/FINDINGS-v48.0.md (FINAL READ-only at the closure HEAD, the v44/v46/v47 precedent).
    5. Commit the closure flip (planning docs force-added since .planning/ is gitignored; audit/FINDINGS-v48.0.md tracked; no contracts/*.sol in the diff so the commit-guard hook does not block). End the commit message with the Co-Authored-By trailer.
    Confirm a final grep finds ZERO unresolved `MILESTONE_V48_AT_HEAD_<sha>` placeholders remaining (the literal `<sha>` token must be gone). Doc-only — no contracts/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(git diff 1575f4a9 HEAD -- contracts/)" && test "$(stat -c '%a' audit/FINDINGS-v48.0.md)" = "444" && ! grep -q "MILESTONE_V48_AT_HEAD_<sha>" audit/FINDINGS-v48.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff 1575f4a9 HEAD -- contracts/` is empty (closure is doc-only; subject frozen).
    - The `MILESTONE_V48_AT_HEAD_<sha>` placeholder is resolved to the concrete closure SHA and propagated verbatim to all FINDINGS targets (frontmatter + §1 + §9b/§9c); zero unresolved `<sha>` placeholders remain.
    - The atomic 5-doc flip is applied: ROADMAP (Phase 328 Complete + milestone shipped + Progress row), STATE (Last Shipped Milestone + signal + verdict + stale "Next:" warning cleared), MILESTONES (v48.0 archive entry prepended), PROJECT (Current Milestone flipped), REQUIREMENTS (all 40 v48.0 rows attested at closure).
    - audit/FINDINGS-v48.0.md is chmod 444.
    - The closure flip is committed with the Co-Authored-By trailer; the commit-guard hook did not block (no contracts/*.sol in the diff).
  </acceptance_criteria>
  <done>Closure signal resolved + propagated verbatim; the atomic 5-doc flip applied with all 40 reqs re-attested; FINDINGS-v48.0.md chmod 444; closure committed; contracts/ byte-identical to 1575f4a9.</done>
</task>

</tasks>

<verification>
- `git diff 1575f4a9 HEAD -- contracts/` empty (closure is doc-only; subject frozen throughout).
- audit/FINDINGS-v48.0.md is chmod 444 with the resolved closure SHA (no `<sha>` placeholder remaining).
- The 5 docs (ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS) are flipped atomically; all 40 v48.0 REQ rows attested at closure.
- The USER approved the verdict + signal at the Task 1 blocking gate before any propagation.
</verification>

<success_criteria>
SC4 satisfied: the USER approves the closure verdict + the MILESTONE_V48_AT_HEAD signal at a single blocking gate; the signal is resolved + propagated verbatim; the atomic 5-doc closure flip is applied with all 40 requirements re-attested; FINDINGS-v48.0.md is locked chmod 444. contracts/ byte-identical to 1575f4a9.
</success_criteria>

<output>
Create `.planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-04-SUMMARY.md` when done
</output>
