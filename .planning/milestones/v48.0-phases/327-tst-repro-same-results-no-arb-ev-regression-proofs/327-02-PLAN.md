---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RFALL-05, POOL-04]
files_modified:
  - test/fuzz/RedemptionStethFallback.t.sol
  - test/fuzz/handlers/RedemptionHandler.sol
  - test/invariant/RedemptionAccounting.t.sol
must_haves:
  truths:
    - "The v47 REDEEM-08 invariants STILL hold under the new stETH fallback: two same-period claimants both paid, BURNIE-can't-block-ETH, value conservation, and address(this).balance + steth.balanceOf(this) >= claimablePool (and >= pendingRedemptionEthValue) at every step"
    - "A mid-game ETH depletion (claimable[SDGNRS] cannot cover the 175% MAX) falls back to the pure-stETH leg and the submit does NOT revert when sDGNRS holds enough stETH; reverts fail-closed only when neither pure leg covers"
    - "A stETH donation / selfdestruct force-feed to sDGNRS cannot brick submit (donation-robust); coverage is checked against steth.balanceOf(SDGNRS), the same basis a donation inflates"
    - "The sDGNRS receive() AF_KING relaxation is accounting-safe: reserves are read via address(this).balance (no running counter in receive()), so an AF_KING-sourced credit is never mis-attributed / double-counted / bypassed"
  artifacts:
    - path: "test/fuzz/RedemptionStethFallback.t.sol"
      provides: "Foundry tests for the RFALL stETH-fallback path + POOL receive() AF_KING accounting-safety"
      contains: "pullRedemptionReserve"
  key_links:
    - from: "test/fuzz/RedemptionStethFallback.t.sol"
      to: "contracts/DegenerusGame.sol"
      via: "pullRedemptionReserve ETH-or-stETH coverage branch driven through submit"
      pattern: "pullRedemptionReserve|steth"
    - from: "test/invariant/RedemptionAccounting.t.sol"
      to: "contracts/StakedDegenerusStonk.sol"
      via: "solvency invariant balance+stETH >= claimablePool >= pendingRedemptionEthValue under the fallback"
      pattern: "claimablePool|pendingRedemptionEthValue"
---

<objective>
Prove the F-47-02 redemption stETH-fallback fix (RFALL-01/02/03) preserves the v47 REDEEM-08 invariants under stETH coverage (RFALL-05), AND prove the sDGNRS `receive()` AF_KING relaxation (POOL-02/03) is accounting-safe (POOL-04). Both target the applied Phase-326 diff.

- **RFALL-05** — under the new pure-ETH-OR-pure-stETH `pullRedemptionReserve` branch (game ETH leg / sDGNRS-stETH fallback / fail-closed revert if neither): two same-period claimants are both paid, BURNIE-can't-block-ETH, value is conserved, and the load-bearing solvency invariant `address(this).balance + steth.balanceOf(this) >= claimablePool` (and `>= pendingRedemptionEthValue`) holds at every step — including the mid-game-ETH-depletion case the fix was written for, and the stETH-donation case.
- **POOL-04** — the sDGNRS `receive()` relaxed to accept `AF_KING` does not introduce a running reserve counter; reserves are read live via `address(this).balance`, so an `AF_KING`-sourced ETH credit cannot be mis-attributed / double-counted / used to inflate a claim.

Purpose: the redemption-desk solvency invariant is the most load-bearing accounting property in the protocol; the fix touches the submit-side reservation that backs it. The threat is a test that proves the fallback "works" without ever DRIVING ETH to empty (so the stETH leg is never taken) or without a donation force-feed (so donation-robustness is untested).
Output: `test/fuzz/RedemptionStethFallback.t.sol` + extended `RedemptionHandler.sol`/`RedemptionAccounting.t.sol`. `forge test --match-path test/fuzz/RedemptionStethFallback.t.sol` exits 0; `FOUNDRY_PROFILE=deep forge test --match-path test/invariant/RedemptionAccounting.t.sol` exits 0.

NO `contracts/*.sol` (mainnet) edits (subject FROZEN at the Phase-326 diff). Editing `test/` + `contracts/test/*.sol` helpers IS allowed. If a real defect surfaces, STOP and surface it — do NOT edit contracts, do NOT weaken assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-04-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-05-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md

<interfaces>
<!-- Anchors from the APPLIED Phase-326 diff. Re-grep at execute-time. -->

DegenerusGame.sol pullRedemptionReserve (the RFALL fix, ~:1895):
  function pullRedemptionReserve(uint256 amount) external;  // SDGNRS-gated; amount==0 early-return
  // ETH leg: if claimable[SDGNRS] >= amount && address(this).balance >= amount -> CHECKED debit
  //          claimable[SDGNRS]-=amount; claimablePool-=uint128(amount); CEI call{value:amount}(SDGNRS); return
  // stETH leg (fallback): else if (steth.balanceOf(SDGNRS) >= amount) return;  // NO game move, NO ledger debit
  // neither -> revert E()  (fail-closed)

StakedDegenerusStonk.sol (submit-side reservation, ~:847-887):
  totalMoney = address(this).balance + steth.balanceOf(this) + claimable[SDGNRS] - pendingRedemptionEthValue;  // :847
  ethValueOwed = totalMoney * amount / supplyBefore;                                                            // :848
  maxIncrement = (newBaseWei * MAX_ROLL)/100 - (prevBaseWei * MAX_ROLL)/100;   // MAX_ROLL=175 (175% reservation)
  if (maxIncrement != 0) game.pullRedemptionReserve(maxIncrement);            // :884-886
  pendingRedemptionEthValue += maxIncrement;                                  // :887  (single D-06 tracker)
  // claim payout: _payEth (ETH-first, stETH-fallback) at :918-933; game-over burn stETH path at :622
  function pullRedemptionReserve(uint256) external; // sStonk inline IGame view

StakedDegenerusStonk.sol receive() (POOL relaxation, ~:439):
  receive() external payable { if (msg.sender != GAME && msg.sender != AF_KING) revert Unauthorized(); }  // NO reserve counter SSTORE
  function burnAtGameOver() external onlyGame; // folds afKing.withdraw(afKing.poolOf(this)) BEFORE the bal==0 early-return (:535-539)

Solvency invariant (DegenerusGame NatSpec): address(this).balance + steth.balanceOf(this) >= claimablePool.

Existing REDEEM-08 anchors (v47, to MIRROR — the invariants were proven at Phase 323 plan 03):
  test/invariant/RedemptionAccounting.t.sol  (INV-01..13; MAX_ROLL=175 conservation; balance+stETH >= pending)
  test/fuzz/handlers/RedemptionHandler.sol   (the redemption action handler)
  test/fuzz/StakedStonkRedemption.t.sol, test/fuzz/RedemptionSplit.t.sol, test/fuzz/RedemptionEdgeCases.t.sol
  test/fuzz/invariant/RedemptionInvariants.inv.t.sol
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RFALL-05 — stETH-fallback path + REDEEM-08 invariants (mid-game ETH depletion + donation-robust + fail-closed)</name>
  <files>test/fuzz/RedemptionStethFallback.t.sol</files>
  <read_first>
    contracts/DegenerusGame.sol `pullRedemptionReserve` (the full ETH-leg/stETH-leg/revert body ~:1895-1922),
    the solvency invariant NatSpec (~:18), contracts/StakedDegenerusStonk.sol submit reservation (:844-887),
    `_payEth` ETH→stETH fallback (:918-933), `burnAtGameOver` stETH path (:622); the EXISTING REDEEM-08
    coverage so the new tests MIRROR not duplicate: test/invariant/RedemptionAccounting.t.sol header +
    INV functions, test/fuzz/StakedStonkRedemption.t.sol, test/fuzz/RedemptionSplit.t.sol,
    test/fuzz/handlers/RedemptionHandler.sol, test/fuzz/helpers/DeployProtocol.sol (the deploy harness +
    how stETH is mocked/seeded); 326-04-SUMMARY.md (the no-op stETH-leg accounting interpretation flagged
    for scrutiny), 325-ATTEST-PFIX-RFALL.md §B (the verified anchors + the gap rationale).
  </read_first>
  <action>
    Create `test/fuzz/RedemptionStethFallback.t.sol`. Author DETERMINISTIC scenario tests that DRIVE the
    contract into each `pullRedemptionReserve` branch (use the project's stETH mock + ETH-balance control via
    `vm.deal` / the existing deploy harness; mirror how RedemptionAccounting.t.sol seeds state):

    (a) `test_RFALL05_EthLeg_HappyPath`: claimable[SDGNRS] and game ETH both cover the 175% MAX → ETH leg taken,
        claimable[SDGNRS] and claimablePool both decrement by `maxIncrement`, ETH lands at sDGNRS, submit succeeds.
        Assert the solvency invariant `address(this).balance + steth.balanceOf(this) >= claimablePool` AND
        `... >= pendingRedemptionEthValue` hold after submit.
    (b) `test_RFALL05_StethFallback_MidGameEthDepletion`: arrange game ETH < `maxIncrement` (mid-game depletion)
        but sDGNRS holds >= `maxIncrement` stETH → the stETH fallback leg is taken: submit does NOT revert, NO
        game-side ETH move, NO ledger debit (claimable[SDGNRS]/claimablePool UNCHANGED by the pull), and
        `pendingRedemptionEthValue` still increments by `maxIncrement`. Then CLAIM and assert the payout is
        delivered in stETH (claim-time asset selection matches the reserved asset, RFALL-03) and value is
        conserved. Assert the solvency invariant holds throughout.
    (c) `test_RFALL05_DonationRobust_StethForceFeed`: force-feed sDGNRS extra stETH (a donation that inflates
        `totalMoney`'s stETH term beyond claimable[SDGNRS]) → submit MUST NOT revert/brick (coverage is checked
        against `steth.balanceOf(SDGNRS)`, the same basis the donation inflated). Assert submit succeeds and the
        invariant holds.
    (d) `test_RFALL05_FailClosed_NeitherLegCovers`: arrange neither game ETH nor sDGNRS stETH covers `maxIncrement`
        → submit REVERTS fail-closed (`vm.expectRevert`). Assert no partial state mutation leaked (claimablePool
        and pendingRedemptionEthValue unchanged).
    (e) `test_RFALL05_TwoSamePeriodClaimants_BothPaid`: two players submit in the SAME period (one on the ETH leg,
        one forced onto the stETH leg if possible), both resolve + claim; assert BOTH are paid the rolled amount,
        no underflow/no double-spend of the SDGNRS claimable, and the solvency invariant holds after each claim.
    (f) `test_RFALL05_BurnieCannotBlockEth`: confirm the v47 BURNIE-can't-block-ETH property still holds under the
        fallback (mirror the v47 REDEEM-08 BURNIE assertion against the current claim path).

    Each test must EXPLICITLY assert the branch it intends to take was actually taken (e.g. for (b) assert
    game ETH balance < maxIncrement BEFORE the pull, and claimablePool UNCHANGED AFTER — proving the stETH leg,
    not the ETH leg, ran). Run command:
    `forge test --match-path test/fuzz/RedemptionStethFallback.t.sol -vv`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/RedemptionStethFallback.t.sol --match-test "RFALL05" -vv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    All six RFALL05 tests PASS. (b) PROVES the stETH leg ran (game ETH < maxIncrement before, claimablePool/
    claimable[SDGNRS] unchanged after, pendingRedemptionEthValue incremented) and the claim pays stETH;
    (c) PROVES a stETH donation cannot brick submit; (d) PROVES fail-closed revert when neither leg covers, with
    no leaked state; (e) PROVES two same-period claimants both paid with no underflow; (f) PROVES BURNIE can't
    block ETH. Every test asserts `balance + stETH >= claimablePool` and `>= pendingRedemptionEthValue`. Zero
    `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The stETH-fallback path is proven on all four branches with the REDEEM-08 invariants (two claimants, BURNIE-can't-block, conservation, solvency) holding throughout.</done>
</task>

<task type="auto">
  <name>Task 2: RFALL-05 invariant extension — solvency holds under the fallback in the existing invariant suite</name>
  <files>test/fuzz/handlers/RedemptionHandler.sol, test/invariant/RedemptionAccounting.t.sol</files>
  <read_first>
    test/invariant/RedemptionAccounting.t.sol (the full INV-01..13 functions + MAX_ROLL/SCAN_CAP constants +
    setUp), test/fuzz/handlers/RedemptionHandler.sol (the action surface — submit/resolve/claim handler fns
    + slot constants), contracts/DegenerusGame.sol `pullRedemptionReserve`, the solvency invariant NatSpec.
  </read_first>
  <action>
    Extend the EXISTING redemption invariant suite so the bounded random walk now ALSO drives the stETH-fallback
    branch, and re-asserts solvency under it:
    - In `RedemptionHandler.sol`: add a handler action (or a fuzz lever) that randomly depletes the game's liquid
      ETH below the reservation and/or force-feeds sDGNRS stETH, so the handler sequence reaches the stETH-leg
      and fail-closed branches in addition to the ETH leg. Keep the handler's existing actions intact (no
      regression — the v47 INV-01..13 must still be exercised).
    - In `RedemptionAccounting.t.sol`: add an invariant `invariant_RFALL05_SolvencyUnderFallback` asserting
      `address(this).balance(game) + steth.balanceOf(game) >= claimablePool` AND
      `>= pendingRedemptionEthValue` after EVERY handler action, and that the sum of unresolved-day MAX (175%)
      reservations never exceeds the segregated backing. Do NOT delete or weaken any existing INV function.
    Run command: `FOUNDRY_PROFILE=deep forge test --match-path test/invariant/RedemptionAccounting.t.sol`.
  </action>
  <verify>
    <automated>FOUNDRY_PROFILE=deep forge test --match-path test/invariant/RedemptionAccounting.t.sol 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `FOUNDRY_PROFILE=deep forge test --match-path test/invariant/RedemptionAccounting.t.sol` exits 0 with the
    new `invariant_RFALL05_SolvencyUnderFallback` present and passing, the handler reaching the stETH-fallback
    branch (not only the ETH leg), and all pre-existing INV-01..13 still passing (non-widening; no INV weakened
    or deleted). Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The redemption solvency invariant is proven to hold under the stETH-fallback branch in the bounded-random-walk invariant suite, with the v47 invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 3: POOL-04 — sDGNRS receive() AF_KING relaxation accounting-safety</name>
  <files>test/fuzz/RedemptionStethFallback.t.sol</files>
  <read_first>
    contracts/StakedDegenerusStonk.sol `receive()` (~:439, the GAME-or-AF_KING gate + NatSpec :435-437 "no
    running counter is kept here"), `burnAtGameOver` (~:535-539, the AfKing pool-recover folded before the
    bal==0 early-return), the submit/preview/resolve bases that read `address(this).balance` directly
    (:844/:598/:758); contracts/AfKing.sol `withdraw`/`poolOf`/`depositFor`; 326-05-SUMMARY.md (POOL-04
    accounting-safe note), 325-ATTEST-KEEP-POOL.md (the receive() accounting-safety SPEC check).
  </read_first>
  <action>
    Add POOL-04 accounting-safety tests to `test/fuzz/RedemptionStethFallback.t.sol` (same file — it already
    deploys sDGNRS + the redemption harness):

    (a) `test_POOL04_ReceiveReadsLiveBalance_NoRunningCounter`: send ETH into sDGNRS via the `AF_KING`-gated
        `receive()` (simulate AfKing's withdraw send-back by pranking `AF_KING` and calling `receive`), then
        assert a redemption submit/preview/resolve reads the reserves via `address(this).balance` — i.e. the
        AF_KING credit IS reflected in the submit base (proving it's read live), and there is NO separate
        running-counter field that could disagree with the balance. Compare: a submit's `totalMoney` ETH term
        equals the actual `address(this).balance` (not a stale counter) before and after the AF_KING credit.
    (b) `test_POOL04_AfKingCreditNotDoubleCounted`: prove the AF_KING-sourced ETH is counted EXACTLY ONCE in
        the redemption base (assert the submit base increases by exactly the credited amount, not 2× — no
        double-count via a counter + balance both being read).
    (c) `test_POOL04_NonGameNonAfKingReceiveReverts`: confirm a `receive()` from any sender != GAME && != AF_KING
        reverts `Unauthorized` (the relaxation did not open an arbitrary deposit vector). Use `vm.expectRevert`.
    (d) `test_POOL04_BurnAtGameOverRecoversPool_ZeroPoolTokenSafe`: with a zero pool-TOKEN sDGNRS but a nonzero
        AfKing prepaid pool, call the gameover drain path; assert `burnAtGameOver` recovers the pool ETH (lands
        via receive()) and does NOT brick on the `bal==0` early-return (withdraw(0) no-op when pool empty).
    Run command: `forge test --match-path test/fuzz/RedemptionStethFallback.t.sol`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/RedemptionStethFallback.t.sol --match-test "POOL04" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    All four POOL04 tests PASS: the AF_KING credit is read live via `address(this).balance` (no running
    counter), counted exactly once in the redemption base, a non-GAME/non-AF_KING `receive()` reverts
    `Unauthorized`, and `burnAtGameOver` recovers the AfKing pool before the bal==0 early-return without
    bricking. Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The sDGNRS receive() AF_KING relaxation is proven accounting-safe — live-balance read, no double-count, no arbitrary deposit vector, gameover recovery safe for a zero-pool-token sDGNRS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claimant → redemption desk | Untrusted submit amount + period timing; the 175% MAX reservation backs solvency |
| donor / AF_KING → sDGNRS balance | A stETH donation or an AF_KING ETH send-back crosses into the reserve basis read by submit |

## STRIDE Threat Register (test-authoring false-confidence focus)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-02-FC1 | Information (false confidence) | RFALL test "passes" without ever driving ETH to empty (stETH leg never taken) | mitigate | Task 1(b) ASSERTS game ETH < maxIncrement BEFORE the pull and claimablePool UNCHANGED AFTER — proving the stETH leg ran, not the ETH leg |
| T-327-02-FC2 | Information (false confidence) | donation-robustness untested (no force-feed) | mitigate | Task 1(c) explicitly force-feeds sDGNRS stETH and asserts submit does not brick |
| T-327-02-FC3 | Denial of Service | fail-closed revert not actually proven (test never reaches the neither-leg case) | mitigate | Task 1(d) arranges neither leg covers and `vm.expectRevert`s, asserting no leaked state |
| T-327-02-FC4 | Information (false confidence) | POOL-04 "no double count" claim asserted on a path that never credits via AF_KING | mitigate | Task 3(a/b) credit via the AF_KING-gated receive() and assert the base moves by exactly the credited amount once |
| T-327-02-INV | Tampering | solvency invariant weakened to make the suite green | mitigate | Task 2 ADDS an invariant and preserves all INV-01..13 (non-widening); a real defect STOPs, never weakens |
| T-327-02-SC | Tampering | npm/forge install of a new test dep | accept | No new package installs; existing forge-std + repo helpers + stETH mock only |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/RedemptionStethFallback.t.sol` exits 0 (RFALL05 + POOL04 tests).
- `FOUNDRY_PROFILE=deep forge test --match-path test/invariant/RedemptionAccounting.t.sol` exits 0 with the new fallback invariant + all INV-01..13 preserved.
- The stETH leg, fail-closed revert, donation force-feed, and two-same-period-claimant cases are each explicitly proven to be the branch taken.
- Zero `contracts/*.sol` (mainnet) modifications (`git status --porcelain contracts/ | grep -v '/test/'` empty).
</verification>

<success_criteria>
The F-47-02 stETH-fallback fix preserves every v47 REDEEM-08 invariant (two claimants, BURNIE-can't-block-ETH, conservation, `balance + stETH >= claimablePool/pending`) under stETH coverage — including the mid-game ETH-depletion and stETH-donation cases — and the sDGNRS receive() AF_KING relaxation is proven accounting-safe (live-balance read, no double-count, no arbitrary deposit vector), all against the applied Phase-326 diff.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-02-SUMMARY.md` when done.
Record: which branch each RFALL05 test proved was taken, the donation/force-feed mechanism used, the new invariant name + the preserved INV count, and the POOL-04 double-count assertion result.
</output>
