---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BTOMB-03]
files_modified:
  - test/fuzz/BurnieTombstone.t.sol
must_haves:
  truths:
    - "The 1e36-wei gameover tombstone flood does NOT touch circulating totalSupply() (it is identical before and after the flood)"
    - "The signal lands only in vaultMintAllowance(), supplyIncUncirculated(), and balanceOf(VAULT) — supplyIncUncirculated() increases by exactly 1e36, vaultMintAllowance() increases by exactly 1e36"
    - "The flood is strictly one-shot (a second call cannot re-flood) and the checked add cannot overflow uint128 from the seeded + escrowed existing allowance + 1e36"
    - "The DGVB pro-rata BURNIE-claim math does not overflow / revert when a claimant claims a share of a 1e36-inflated allowance"
  artifacts:
    - path: "test/fuzz/BurnieTombstone.t.sol"
      provides: "Foundry test proving the BURNIE tombstone is non-circulating + one-shot + DGVB-claim-safe"
      contains: "tombstoneAtGameOver"
  key_links:
    - from: "test/fuzz/BurnieTombstone.t.sol"
      to: "contracts/BurnieCoin.sol"
      via: "tombstoneAtGameOver() GAME-gated one-shot flood; totalSupply()/supplyIncUncirculated()/vaultMintAllowance() reads"
      pattern: "tombstoneAtGameOver|supplyIncUncirculated|totalSupply"
---

<objective>
Prove the gameover BURNIE tombstone (BTOMB-01/02) signals only in UNCIRCULATED supply (BTOMB-03), against the applied Phase-326 diff. Four properties:
1. **Non-circulating** — the `tombstoneAtGameOver()` 1e36-wei flood does NOT change `totalSupply()`.
2. **Signal localization** — the 1e36 lands ONLY in `vaultMintAllowance()` (+= exactly 1e36), `supplyIncUncirculated()` (+= exactly 1e36), and the VAULT's mintable headroom (`balanceOf(VAULT)` semantics per the contract).
3. **One-shot + checked add** — a second `tombstoneAtGameOver()` cannot re-flood (`_tombstoneFlooded` latch, early-return), and the CHECKED add (`_toUint128`) cannot overflow `uint128` from the seeded 2_000_000-ether + any escrowed allowance + 1e36 (well below 1e38, ~340× headroom).
4. **DGVB claim-safe** — the DGVB pro-rata BURNIE-claim math does not overflow / revert when a claimant claims a share of a 1e36-inflated allowance.

Purpose: a worthless-token tombstone is only safe if it cannot distort any circulating-supply-keyed math or overflow a downstream claim. The threat is a test that checks `totalSupply()` is unchanged but never claims against the 1e36 allowance (so a downstream overflow goes undetected), or never proves the one-shot latch.
Output: `test/fuzz/BurnieTombstone.t.sol`. `forge test --match-path test/fuzz/BurnieTombstone.t.sol` exits 0.

NO `contracts/*.sol` (mainnet) edits (subject FROZEN at the Phase-326 diff). Editing `test/` + `contracts/test/*.sol` helpers IS allowed. If a real defect surfaces, STOP and surface it — do NOT edit contracts, do NOT weaken assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-02-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md

<interfaces>
<!-- Anchors from the APPLIED Phase-326 diff (contracts/BurnieCoin.sol). Re-grep at execute-time. -->

BurnieCoin.sol:
  uint256 private constant BURNIE_TOMBSTONE_WEI = 1e36;                  // :174 (~340x headroom under uint128 max ~3.4e38)
  struct Supply { uint128 totalSupply; uint128 vaultAllowance; }         // :178-179 (co-packed one slot)
  Supply _supply = Supply({totalSupply: 0, vaultAllowance: 2_000_000 ether}); // :185 ctor seed
  bool private _tombstoneFlooded;                                        // :188 one-shot latch
  function totalSupply() external view returns (uint256);               // :264 returns _supply.totalSupply (EXCLUDES vaultAllowance)
  function supplyIncUncirculated() external view returns (uint256);     // :271 returns totalSupply + vaultAllowance
  function vaultMintAllowance() external view returns (uint256);        // :278 returns _supply.vaultAllowance
  function tombstoneAtGameOver() external;                              // :583 GAME-gated; if(_tombstoneFlooded) return; _tombstoneFlooded=true;
                                                                         //   _supply.vaultAllowance = _toUint128(uint256(vaultAllowance) + 1e36);  // CHECKED add (:587)
  // _toUint128(value): reverts SupplyOverflow if value > type(uint128).max (~3.4e38)

GameOverModule wires `burnie.tombstoneAtGameOver();` adjacent to `dgnrs.burnAtGameOver();` in the gameover-drain (one-shot, GAME-gated).

DGVB (the BURNIE-claim contract) computes a pro-rata claimant share against the VAULT allowance / supply — find the actual claim fn under contracts/ (grep "claim" + "vaultAllowance"/"supplyIncUncirculated" in the DGVB / BURNIE-claim contract; the 326 SUMMARYs reference it as the pro-rata BURNIE-claim math). The 1e36 share must not overflow uint256 intermediate products.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BTOMB-03 — non-circulating + signal localization + one-shot + checked-add</name>
  <files>test/fuzz/BurnieTombstone.t.sol</files>
  <read_first>
    contracts/BurnieCoin.sol (`tombstoneAtGameOver` :583-589, the `Supply` struct + seed :178-185, the
    `totalSupply`/`supplyIncUncirculated`/`vaultMintAllowance` views :264/:271/:278, `_toUint128` cap,
    `BURNIE_TOMBSTONE_WEI` :174, `_tombstoneFlooded` :188), contracts/modules/DegenerusGameGameOverModule.sol
    (the `tombstoneAtGameOver` wiring adjacent to `burnAtGameOver`), the GAME address constant; an existing
    BurnieCoin Foundry test or the deploy harness (test/fuzz/helpers/DeployProtocol.sol) to mirror setup;
    326-02-SUMMARY.md (the exact impl: checked add, early-return latch, reuses VaultEscrowRecorded),
    325-ATTEST-BTOMB-HERO.md §A (verified anchors + the unchecked-vs-checked clarification).
  </read_first>
  <action>
    Create `test/fuzz/BurnieTombstone.t.sol`. Deploy BURNIE + the gameover path; prank as `GAME` (the only
    authorized caller). Tests:

    (a) `test_BTOMB03_TotalSupplyUntouched`: snapshot `totalSupply()` before; call `tombstoneAtGameOver()`
        (as GAME); assert `totalSupply()` AFTER == BEFORE (the 1e36 flood does NOT touch circulating supply).
    (b) `test_BTOMB03_SignalLandsOnlyInUncirculated`: snapshot `vaultMintAllowance()` and `supplyIncUncirculated()`
        before; flood; assert `vaultMintAllowance()` increased by EXACTLY 1e36 and `supplyIncUncirculated()`
        increased by EXACTLY 1e36, while `totalSupply()` is unchanged (so the entire delta is in the uncirculated
        leg).
    (c) `test_BTOMB03_OneShot`: call `tombstoneAtGameOver()` twice as GAME; assert the SECOND call is a no-op
        (early-return) — `vaultMintAllowance()` increased by exactly 1e36 total (not 2e36), and the call did not
        revert (early-return, not revert, per the impl choice so it cannot brick the gameover path).
    (d) `test_BTOMB03_GameGated`: call `tombstoneAtGameOver()` from a non-GAME sender; assert it reverts
        (`OnlyGame`). Use `vm.expectRevert`.
    (e) `test_BTOMB03_CheckedAddNoOverflow`: seed/escrow the existing `vaultAllowance` to a realistic high
        value (seeded 2_000_000 ether + plausible escrows), flood, and assert no `SupplyOverflow` revert and
        the result equals `existing + 1e36` (the checked add holds with ~340× headroom). Optionally drive the
        existing allowance near `type(uint128).max - 1e36` and assert the flood still does NOT overflow at that
        bound (and that pushing past it WOULD revert via `_toUint128`, confirming the cap is live — a negative
        control).
    Run command: `forge test --match-path test/fuzz/BurnieTombstone.t.sol -vv`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/BurnieTombstone.t.sol --match-test "BTOMB03_" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    Tests (a)-(e) PASS: `totalSupply()` unchanged by the flood; `vaultMintAllowance()` and `supplyIncUncirculated()`
    each += EXACTLY 1e36; a second flood is a no-op (total += 1e36, no revert); a non-GAME caller reverts; the
    checked add holds at the seeded+escrowed existing value with a live-cap negative control. Zero `contracts/*.sol`
    (mainnet) modifications.
  </acceptance_criteria>
  <done>The tombstone is proven non-circulating, signal-localized to the uncirculated leg, strictly one-shot, GAME-gated, and overflow-safe via the checked add.</done>
</task>

<task type="auto">
  <name>Task 2: BTOMB-03 — DGVB pro-rata BURNIE-claim math does not overflow on a 1e36 allowance share</name>
  <files>test/fuzz/BurnieTombstone.t.sol</files>
  <read_first>
    The DGVB / BURNIE-claim contract under contracts/ (grep for the pro-rata claim fn that reads
    `vaultMintAllowance()` / `supplyIncUncirculated()` / a VAULT-allowance share — confirm the exact contract
    + fn name + its arithmetic; the 326 SUMMARYs name it "DGVB pro-rata BURNIE-claim math"), contracts/BurnieCoin.sol
    `balanceOf(VAULT)` semantics, the Task-1 harness in test/fuzz/BurnieTombstone.t.sol.
  </read_first>
  <action>
    Add `test_BTOMB03_DgvbClaimNoOverflowOn1e36Share` to `test/fuzz/BurnieTombstone.t.sol`. After flooding the
    VAULT allowance by 1e36, drive a DGVB pro-rata BURNIE-claim for a claimant entitled to a share of the
    1e36-inflated allowance. Assert:
    - The claim does NOT revert (no `uint256` intermediate-product overflow in the pro-rata `share * allowance / total`
      style math, and no `uint128` cast revert on the share).
    - The claimed amount is the correct pro-rata share (sane bound: `<= the 1e36 allowance`, `> 0` for a nonzero
      entitlement), so the test fails if the math silently returns 0 or a wrong magnitude.
    If the DGVB claim path requires a specific game-over state / claim-window, set it up via the deploy harness
    + the gameover drain. If after reading the contract the DGVB claim cannot reach a 1e36-allowance share by
    construction (e.g. the claim caps against circulating supply, not the allowance), RECORD that finding in the
    SUMMARY as the disposition for BTOMB-03's DGVB clause (proven safe by construction) WITH the grep proof —
    do not fabricate a path that does not exist.
    Run command: `forge test --match-path test/fuzz/BurnieTombstone.t.sol`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/BurnieTombstone.t.sol --match-test "DgvbClaim" -vv 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    `test_BTOMB03_DgvbClaimNoOverflowOn1e36Share` PASSES proving the DGVB pro-rata claim does not overflow/revert
    on a 1e36-inflated allowance share and returns a correct-magnitude payout — OR, if the claim cannot reach
    the allowance share by construction, the SUMMARY records the grep-proven safe-by-construction disposition.
    Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The DGVB pro-rata BURNIE-claim is proven overflow-safe against a 1e36-inflated allowance share (or proven unreachable by construction with grep evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GAME → BURNIE allowance | Only GAME (gameover-drain) can flood the allowance; non-GAME must revert |
| claimant → DGVB pro-rata claim | A claimant computes a share against the 1e36-inflated allowance — intermediate products must not overflow |

## STRIDE Threat Register (test-authoring false-confidence focus)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-03-FC1 | Information (false confidence) | test checks totalSupply() unchanged but never claims against the 1e36 allowance (downstream overflow undetected) | mitigate | Task 2 drives an actual DGVB pro-rata claim against the 1e36 share and asserts no overflow + correct magnitude |
| T-327-03-FC2 | Information (false confidence) | one-shot latch never tested (a re-flood would double the signal) | mitigate | Task 1(c) calls twice and asserts total += exactly 1e36 with no revert |
| T-327-03-FC3 | Elevation | checked-add cap not actually exercised at the boundary | mitigate | Task 1(e) drives the existing allowance near uint128 max and proves the flood holds + the cap is a live negative control |
| T-327-03-SC | Tampering | npm/forge install of a new test dep | accept | No new package installs; existing forge-std + repo helpers only |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/BurnieTombstone.t.sol` exits 0.
- totalSupply() unchanged; vaultMintAllowance()/supplyIncUncirculated() each += exactly 1e36; one-shot proven; GAME-gated; checked add overflow-safe with a live-cap negative control.
- DGVB pro-rata claim proven overflow-safe on a 1e36 share (or grep-proven unreachable).
- Zero `contracts/*.sol` (mainnet) modifications (`git status --porcelain contracts/ | grep -v '/test/'` empty).
</verification>

<success_criteria>
The gameover BURNIE tombstone is proven non-circulating (totalSupply() untouched), signal-localized to the uncirculated leg (+= exactly 1e36), strictly one-shot, GAME-gated, overflow-safe via the checked add, and the DGVB pro-rata BURNIE-claim math is proven not to overflow on a 1e36-inflated allowance share — all against the applied Phase-326 diff.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-03-SUMMARY.md` when done.
Record: the exact deltas asserted (totalSupply 0, allowance +1e36), the one-shot result, the checked-add boundary tested, and the DGVB claim disposition (proven-safe-by-test or proven-unreachable-by-construction with grep).
</output>
