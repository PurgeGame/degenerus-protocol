---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 06
type: execute
wave: 2
depends_on: [327-01, 327-02, 327-03, 327-04, 327-05]
autonomous: true
requirements: []
files_modified:
  - test/REGRESSION-BASELINE-v48.md
must_haves:
  truths:
    - "The FULL forge test tree (NOT --match-path) is run after all five wave-1 test files exist, and the NET new regression introduced by the 5 new wave-1 Foundry test files is EXACTLY 0: every red forge test is a member of the named baseline (the 42-failure v47/VRF carry-over set) OR the named KNOWN-TST-DEFERRED HERO byte-reproduce set — no test outside that enumerated union is red"
    - "The exact failure arithmetic against the 326-08 baseline (594 passed / 42 failed of 636) is stated: post-wave-1 forge totals = (594 + NEW_PASSING from the 5 plans) passed / (42 baseline + 0 net-new) failed, with the 5 new wave-1 Foundry test files contributing only PASSING tests (their RED-by-design cases — e.g. the SWAP no-arb STOP or a real defect — would be a genuine finding, not a baseline red)"
    - "The HERO byte-reproduce gate (npm run test:stat -> DegenerettePerNEvExactness + DegeneretteBonusEv) is the documented EXPECTED-RED-until-contract-landing set; this plan records it as a CONDITIONAL closure path, does NOT force it green, and does NOT apply any contracts/*.sol edit"
    - "The post-landing delta is DOCUMENTED, not executed: once the single hand-reviewed CONTRACTS_COMMIT_APPROVED=1 constant-only diff lands derive_5_tables.py's finals into DegenerusGameDegeneretteModule.sol (OUT OF THIS PHASE), re-running this sweep shows the KNOWN-TST-DEFERRED Degenerette tests GREEN and total failures drop by exactly the recorded HERO count"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v48.md"
      provides: "The named expected-red enumeration + the exact 326-08 arithmetic + the conditional post-landing delta (the clean-v48.0-baseline gate ledger)"
      contains: "326-08"
  key_links:
    - from: "test/REGRESSION-BASELINE-v48.md"
      to: ".planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-08-SUMMARY.md"
      via: "the 594/42 baseline the net-zero-new-regression arithmetic is computed against"
      pattern: "594|42|326-08"
---

<objective>
Close the phase-goal Success Criterion that NO single wave-1 plan covers: "restoring a clean v48.0 regression baseline." Every wave-1 plan (327-01..05) runs only `forge test --match-path <its own file>` — none proves the FULL suite has zero NET new regression from the five new test files. This plan runs the WHOLE tree and asserts it.

Purpose: the milestone's regression-baseline gate. Wave-1 proves each fix individually; this plan proves the act of adding five new test files (PresaleBoxDrain, RedemptionStethFallback, BurnieTombstone, DegeneretteHeroScore + the stat-gate edits, FarFutureSalvageSwap) did NOT introduce any net-new red test and did NOT silently mask a real regression as "baseline."

This plan authors NO `contracts/*.sol` edit and NO new `contracts/*.sol`-touching test file — it RUNS the suite and records the named-red ledger in a single markdown artifact (`test/REGRESSION-BASELINE-v48.md`). It has `requirements: []` because the phase-goal "clean regression baseline" Success Criterion has no dedicated REQ ID (the per-fix REQs PFIX-02/03, RFALL-05, POOL-04, BTOMB-03, HERO-04/06, SWAP-08/09 are each owned by a wave-1 plan); this plan is the SC-level gate they compose into a clean baseline.

Two assertions:
1. **Zero NET new regression (forge):** total `forge test` (whole tree) failures introduced by the 5 new wave-1 Foundry test files = 0. The only red `forge test` results are members of the ENUMERATED union: (i) the pre-existing 42-failure baseline (VRF / v47-carried — out of v48 scope; v48 touched no VRF/Advance code per 326-08), PLUS (ii) any KNOWN-TST-DEFERRED HERO red that surfaces in the FOUNDRY tree because the contract still ships the placeholder S-distribution constants. No test NAME outside that enumerated union may be red.
2. **HERO byte-reproduce gate stays an EXPECTED, CONDITIONAL, documented RED — never forced green and never landed by this phase.** The Hardhat byte-reproduce gate (`npm run test:stat` -> `DegenerettePerNEvExactness.test.js` + `DegeneretteBonusEv.test.js`, authored RED-by-design in 327-04 against the placeholder constants) is recorded as the expected-red HERO set. This plan documents the conditional closure delta; it does NOT apply or require the out-of-phase contract-constant landing to pass.

NO `contracts/*.sol` (mainnet) edits. NO new `contracts/*.sol`-touching test. If the suite reveals a genuinely NEW red (a test outside the enumerated union, or a wave-1 test failing on a REAL defect rather than the intended STOP), STOP and surface it in the SUMMARY as a finding — do NOT reclassify it as "baseline," do NOT loosen the bound.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-08-SUMMARY.md
@.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-01-SUMMARY.md
@.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-02-SUMMARY.md
@.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-03-SUMMARY.md
@.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-04-SUMMARY.md
@.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-05-SUMMARY.md

<interfaces>
<!-- The baseline this plan computes against. Re-confirm by reading 326-08-SUMMARY.md at execute-time. -->

326-08 baseline (the v48.0 applied-diff HEAD, held-for-hand-review):
  `forge test` (whole tree) = 594 succeeded / 42 failed (636 total).
  - Pre-existing baseline reds (out of v48 scope, DO NOT chase): VRFPathInvariants (3 —
    gap-day / coordinator-swap / stall-recovery; test/fuzz/invariant/VRFPathInvariants.inv.t.sol) + the
    carried VRF/baseline failures. v48 touched no VRF/Advance code.
  - KNOWN-TST-DEFERRED (HERO): the Degenerette payout-table VALUE assertions fail because the byte-exact
    S in {0..9} constants are intentional placeholders (S8=0; packed=old M-indexed; WWXRP=old). These live in
    the HARDHAT stat tree (test/stat/DegenerettePerNEvExactness.test.js + DegeneretteBonusEv.test.js, run via
    `npm run test:stat` — NOT `forge test`). Any FOUNDRY test that ALSO asserts placeholder-sensitive payout
    magnitudes (re-grep at execute-time) is part of the SAME deferred set.
  - NEW v48 (class-c, must be 0): none indicated at 326-08 (count <= baseline; clean compile).

The 5 new wave-1 Foundry test files (must contribute only PASSING tests — their intended RED states are
genuine findings, not baseline reds):
  test/fuzz/PresaleBoxDrain.t.sol          (327-01 — PFIX)
  test/fuzz/RedemptionStethFallback.t.sol  (327-02 — RFALL + POOL)  [+ invariant extension in
                                            test/invariant/RedemptionAccounting.t.sol + handler]
  test/fuzz/BurnieTombstone.t.sol          (327-03 — BTOMB)
  test/fuzz/DegeneretteHeroScore.t.sol     (327-04 — HERO scoring/DGAS/no-leak, placeholder-independent)
  test/fuzz/FarFutureSalvageSwap.t.sol     (327-05 — SWAP)

The HERO byte-reproduce gate (327-04, EXPECTED-RED-until-contract-landing; Hardhat, not forge):
  npm run test:stat  -> includes test/stat/DegenerettePerNEvExactness.test.js + test/stat/DegeneretteBonusEv.test.js
  (RED by design against the placeholder constants; the script-regenerated finals + the `## STOP — HERO
  BYTE-REPRODUCE NEEDS CONTRACT-CONSTANT LANDING` handoff are recorded in 327-04-SUMMARY.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fingerprint the named expected-red set + run the FULL forge tree + assert net-zero new regression</name>
  <files>test/REGRESSION-BASELINE-v48.md</files>
  <read_first>
    .planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-08-SUMMARY.md (the 594/42 baseline +
    the red classification), the five 327-01..05 SUMMARYs (which Foundry tests each wave-1 plan added and how many
    PASS — to compute the expected NEW_PASSING delta), foundry.toml (profiles), and re-grep the Foundry tree for
    any test that asserts placeholder-sensitive Degenerette payout MAGNITUDES (grep -rln for QUICK_PLAY / 10_756 /
    basePayout under test/fuzz + test/invariant) so the FOUNDRY-side HERO-deferred members are enumerated by NAME,
    not assumed empty.
  </read_first>
  <action>
    (1) ENUMERATE the expected-red set BY NAME (the false-confidence guard — never a bare count):
    - Baseline reds: list the exact failing `forge test` names from the 326-08 baseline (VRFPathInvariants 3 +
      the carried VRF/baseline set). If 326-08 captured the per-suite failing list, transcribe it; otherwise
      re-establish it deterministically by running the full tree (see (2)) and classifying each red as either
      VRF/baseline (out of v48 scope) or HERO-deferred — every red MUST land in exactly one named bucket.
    - HERO-deferred reds (FOUNDRY side, if any): re-grep the Foundry tree for placeholder-sensitive payout-magnitude
      assertions and list each such test NAME. (The Hardhat-side HERO gate is handled in Task 2 — keep the two
      runners separate.)
    Write these two named lists into `test/REGRESSION-BASELINE-v48.md` as the AUTHORITATIVE expected-red union for
    `forge test`. Any forge red NOT in this union is a NEW regression -> STOP.

    (2) Run the FULL forge tree (NOT `--match-path`) after confirming all five wave-1 Foundry test files exist on
    disk (this plan is wave 2, depends_on all five): run `forge test` capturing the log. Parse the final
    "X passed; Y failed" summary AND the per-test failing list (the FAIL lines). Then:
    - Compute the exact arithmetic vs 326-08: expected passed = 594 + NEW_PASSING (the count of newly-passing
      tests the 5 wave-1 files added — read each 327-0x-SUMMARY for its passing test count); expected failed =
      42 + 0 net-new. Record the actual passed/failed and the NEW_PASSING reconciliation in the artifact.
    - ASSERT (and STATE numerically) that the SET of red forge test names is a SUBSET of the enumerated union from
      (1). If ANY red test name is OUTSIDE the union (a new name, or one of the 5 new wave-1 files failing on a
      REAL defect rather than its intended STOP), the plan FAILS: emit a `## STOP — NEW REGRESSION OUTSIDE BASELINE`
      block in the SUMMARY naming the offending test(s); do NOT widen the union, do NOT reclassify as baseline.
    - ASSERT the 5 new wave-1 Foundry test files contributed only PASSING tests (zero of their cases is red, EXCEPT
      a deliberately-RED no-arb STOP from 327-05 or a real defect — either of which is a finding, not a baseline red,
      and triggers the STOP above).

    (3) Compute and RECORD (do not assert as a forge requirement) the count of FOUNDRY HERO-deferred reds so Task 2
    can state the total HERO count and the post-landing delta.

    Author `test/REGRESSION-BASELINE-v48.md` (a plain markdown ledger — NOT a `.sol` file, NOT a runnable test):
    the 326-08 baseline figures, the named expected-red union (forge + the Hardhat HERO gate), the actual full-tree
    run result with the NEW_PASSING reconciliation, the net-new-regression count (must be 0), and a pointer to Task
    2's conditional post-landing delta.
    Run command: `forge test` (whole tree).
  </action>
  <verify>
    <automated>forge test 2>&1 | tee /tmp/327-06-forge.log | tail -5; echo "=== RED forge tests (every one MUST be in the named baseline+HERO-deferred union) ==="; grep -aE 'FAIL' /tmp/327-06-forge.log | head -80; echo "=== artifact present + references the 326-08 baseline ==="; test -f test/REGRESSION-BASELINE-v48.md && grep -c "326-08" test/REGRESSION-BASELINE-v48.md</automated>
  </verify>
  <acceptance_criteria>
    The full `forge test` tree ran (NOT --match-path). The artifact `test/REGRESSION-BASELINE-v48.md` exists and
    enumerates the expected-red union BY NAME (VRF/baseline reds + any FOUNDRY-side HERO-deferred reds). The actual
    red forge-test set is a strict SUBSET of that named union (net-new regression == 0); the exact arithmetic vs the
    326-08 594/42 baseline is stated (actual passed == 594 + NEW_PASSING; actual failed == 42 + 0). The 5 new wave-1
    Foundry files contributed only PASSING tests. If any red is outside the union, the SUMMARY carries
    `## STOP — NEW REGRESSION OUTSIDE BASELINE` and the plan does NOT pass. Zero `contracts/*.sol` (mainnet)
    modifications; no new `contracts/*.sol`-touching test authored.
  </acceptance_criteria>
  <done>The full forge tree is run, every red is accounted for by NAME in the enumerated baseline+HERO-deferred union, the net-new-regression count is proven 0 against the 326-08 594/42 baseline, and the named ledger is written to test/REGRESSION-BASELINE-v48.md.</done>
</task>

<task type="auto">
  <name>Task 2: Document the HERO byte-reproduce gate as the conditional, expected-red closure path + the post-landing delta</name>
  <files>test/REGRESSION-BASELINE-v48.md</files>
  <read_first>
    .planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-04-SUMMARY.md (the recorded
    placeholder->final diff + the `## STOP — HERO BYTE-REPRODUCE NEEDS CONTRACT-CONSTANT LANDING` handoff + the
    count of HERO-deferred reds), package.json (`test:stat` script), the Task-1 ledger artifact.
  </read_first>
  <action>
    Run the Hardhat HERO byte-reproduce gate to capture its CURRENT (expected-RED, pre-landing) state — do NOT
    force it green, do NOT apply the contract-constant landing:
    `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js`
    (or the relevant subset of `npm run test:stat`). Record in `test/REGRESSION-BASELINE-v48.md`:
    - The CURRENT state: which stat assertions are RED against the placeholder constants (the byte-reproduce
      0-diff assertion + per-N EV / ETH-bonus checks that depend on the final tables), and confirm 327-04 emitted
      the `## STOP` handoff with the ready-to-apply finals.
    - The CONDITIONAL closure path (DOCUMENTED, not executed): once the single hand-reviewed,
      `CONTRACTS_COMMIT_APPROVED=1`-gated, constant-ONLY diff lands `derive_5_tables.py`'s finals into
      contracts/modules/DegenerusGameDegeneretteModule.sol (OUT OF THIS no-contract phase — owned by a future
      hand-review step, NOT this plan), re-running this sweep (`forge test` + the stat gate) MUST show: (a) the
      KNOWN-TST-DEFERRED Degenerette tests GREEN, and (b) total failures drop by EXACTLY the recorded HERO count
      (the Foundry HERO-deferred count from Task 1(3) + the Hardhat stat HERO reds), with NO other delta. State
      the expected post-landing totals numerically (forge: 42 - <foundry HERO count> failed; stat gate: PASS_ALL
      0-diff + per-N EV == 100 + ETH bonus == 5.000%).
    - The EXPLICIT non-action: this plan does NOT apply, require, or stage the contract edit; a RED HERO gate here
      is the EXPECTED in-scope state of the no-contract phase and is NOT a plan failure. The verifier must treat the
      documented conditional delta — not a green HERO gate — as the satisfied acceptance.
    Run command: `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js`.
  </action>
  <verify>
    <automated>npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js 2>&1 | tail -20; echo "=== conditional post-landing delta documented ==="; grep -c "CONTRACTS_COMMIT_APPROVED=1\|post-landing\|HERO BYTE-REPRODUCE" test/REGRESSION-BASELINE-v48.md</automated>
  </verify>
  <acceptance_criteria>
    The Hardhat HERO byte-reproduce gate was RUN and its expected-RED (pre-landing) state captured in
    `test/REGRESSION-BASELINE-v48.md` — confirming 327-04's `## STOP` handoff + ready-to-apply finals. The
    CONDITIONAL post-landing delta is DOCUMENTED numerically (after the out-of-phase constant-only landing, forge
    failures drop by exactly the recorded HERO count and the stat gate flips to PASS_ALL 0-diff + EV checks). This
    plan does NOT apply or require any `contracts/*.sol` edit; a RED HERO gate is the expected in-scope outcome, not
    a failure. Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The HERO byte-reproduce gate is recorded as the expected-red, conditional closure path with a numerically-documented post-landing delta — not forced green, no contract edit applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wave-1 test additions -> full suite | Adding 5 new test files must not introduce any net-new red, nor mask a real regression as "baseline" |
| placeholder HERO constants -> regression ledger | The intentional-placeholder HERO reds must be named + isolated, never conflated with a real regression |

## STRIDE Threat Register (regression-gate false-confidence focus)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-06-FC1 | Information (false confidence) | a loose failure-COUNT check (e.g. `failed <= 42`) masks a newly-introduced regression by coincidentally matching the baseline number | mitigate | Task 1 asserts the red test NAME SET is a strict SUBSET of the enumerated baseline+HERO-deferred union, not a bare count; any name outside the union -> `## STOP — NEW REGRESSION OUTSIDE BASELINE` |
| T-327-06-FC2 | Information (false confidence) | the HERO-deferred reds get conflated with a real regression (or vice-versa), so a real break hides behind "it's just the HERO placeholders" | mitigate | Task 1 enumerates the FOUNDRY HERO-deferred reds BY NAME via grep; Task 2 isolates the Hardhat HERO gate; every red lands in exactly one named bucket |
| T-327-06-FC3 | Information (false confidence) | the gate "passes" by forcing the HERO byte-reproduce gate green (e.g. hand-typing constants or applying the contract edit in this no-contract phase) | mitigate | Task 2 RECORDS the expected-RED state + the conditional documented delta; it FORBIDS applying/requiring the contract-constant landing; a RED HERO gate is the expected in-scope outcome |
| T-327-06-FC4 | Information (false confidence) | the full tree is never actually run (only `--match-path` like wave-1), so cross-file regressions go undetected | mitigate | Task 1 runs `forge test` on the WHOLE tree (NOT --match-path) and reconciles the total against 594/42 + NEW_PASSING |
| T-327-06-SC | Tampering | npm/forge install of a new test dep | accept | No new package installs; existing forge + hardhat + repo helpers only; the artifact is plain markdown |
</threat_model>

<verification>
- The FULL `forge test` tree (NOT --match-path) is run; the red test NAME set is a strict subset of the named baseline (VRF/v47-carried) + HERO-deferred (FOUNDRY-side) union; net-new regression == 0.
- The exact arithmetic vs the 326-08 baseline is stated: actual passed == 594 + NEW_PASSING; actual failed == 42 + 0 net-new.
- The Hardhat HERO byte-reproduce gate is run; its expected-RED pre-landing state + the conditional, numerically-documented post-landing delta are recorded in `test/REGRESSION-BASELINE-v48.md`; the gate is NOT forced green and NO contract edit is applied.
- Any red outside the union -> `## STOP — NEW REGRESSION OUTSIDE BASELINE`. Zero `contracts/*.sol` (mainnet) modifications; no new `contracts/*.sol`-touching test authored.
</verification>

<success_criteria>
The clean v48.0 regression baseline is proven: the full `forge test` tree runs with zero NET new regression (every red accounted for by NAME in the enumerated baseline + HERO-deferred union, reconciled against the 326-08 594/42 baseline + the wave-1 NEW_PASSING delta), and the HERO byte-reproduce gate is recorded as the expected-red, conditional closure path with a numerically-documented post-landing delta — without forcing it green and without applying any `contracts/*.sol` edit in this no-contract phase.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-06-SUMMARY.md` when done.
Record: the actual full-tree forge passed/failed totals + the NEW_PASSING reconciliation vs 594/42, the named
expected-red union (forge baseline + FOUNDRY HERO-deferred + Hardhat HERO gate), the net-new-regression count
(must be 0), the recorded HERO count, and the conditional post-landing delta. If any red surfaced outside the
union, the `## STOP — NEW REGRESSION OUTSIDE BASELINE` block.
</output>
</content>
