---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PFIX-02, PFIX-03]
files_modified:
  - test/fuzz/PresaleBoxDrain.t.sol
must_haves:
  truths:
    - "Over a realistic 50-ETH presale (random 50/40/10 outcomes across many boxes), the closing-box sweep transfers only variance dust (<= a small explicit bound), NOT ~60% of the pool, and the pool ends ~empty"
    - "The tier shape is preserved post-fix: a tier-1 buyer still gets exactly 3x the DGNRS-per-ETH of a tier-5 buyer (only the absolute scale moved)"
    - "A run of early DGNRS hits empties the pool before close, so the closing sweep is ~0 and never reverts / never over-draws (transferFromPool clamp holds)"
  artifacts:
    - path: "test/fuzz/PresaleBoxDrain.t.sol"
      provides: "Foundry test proving the F-47-01 closing-box dust bound + tier shape + clamp"
      contains: "transferFromPool"
  key_links:
    - from: "test/fuzz/PresaleBoxDrain.t.sol"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "presale-box open path exercising _presaleBoxDgnrsReward + the closing-box transferFromPool sweep"
      pattern: "PresaleBoxOpened|transferFromPool"
---

<objective>
Prove the F-47-01 presale-box DGNRS drain fix (PFIX-01, divisor `1_000 → 400`, base `poolStart/100 → poolStart/40`) empirically. Three properties, ALL against the applied Phase-326 diff:
1. **PFIX-02 dust bound** — over a realistic ~50-ETH presale run (random 50/40/10 box outcomes across many boxes) the closing-box sweep mops up only **variance dust** (≤ a small explicit wei bound), NOT the ~60% windfall the v47 closer captured; the pool ends ~empty.
2. **PFIX-03 tier shape preserved** — tier-1 buyer still earns exactly 3× the DGNRS-per-ETH of tier-5 (scale-only move; only absolute magnitude changes).
3. **PFIX-03 clamp holds** — a run of early DGNRS hits empties the pool BEFORE the closing box, so the closing sweep is ~0 and `transferFromPool` clamps (no revert / no over-draw).

Purpose: turn the F-47-01 fix from a 1-line divisor change into a proven dust bound a C4A warden cannot reopen. The threat is a test that "passes" without ever running the realistic ~40%-branch-rate distribution that exposed the original windfall.
Output: `test/fuzz/PresaleBoxDrain.t.sol` (Foundry), `forge test --match-path test/fuzz/PresaleBoxDrain.t.sol` exits 0.

NO `contracts/*.sol` (mainnet) edits. The subject is FROZEN at the Phase-326 applied diff (working tree). If a test reveals a real contract defect (a property that SHOULD hold but does not), STOP and surface it in the SUMMARY — do NOT edit `contracts/`, do NOT weaken the assertion. Editing `contracts/test/*.sol` helpers IS allowed (test scaffolding, not the audit subject).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-01-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md

<interfaces>
<!-- Anchors from the APPLIED Phase-326 diff (contracts/modules/DegenerusGameLootboxModule.sol). -->
<!-- Use directly; re-grep at execute-time since later plans do not touch this file. -->

The fixed reward math (DegenerusGameLootboxModule.sol):
  // base = poolStart / 40 DGNRS per ETH; tier multiplier in tenths.
  uint256 tierTenths = _presaleBoxDgnrsTierTenths(soldBefore);          // :718
  uint256 dgnrsAmount = (poolStart * tierTenths * amount) / (400 * 1 ether);  // :721 (was /(1_000 * 1 ether))

The tier ladder (BYTE-UNCHANGED by the fix; tier-1 = 3.0, tier-5 = 1.0 tenths):
  _presaleBoxDgnrsTierTenths(soldBefore) -> PRESALE_BOX_DGNRS_TIER1..5_TENTHS (3.0/2.5/2.0/1.5/1.0)  // :734+

The per-box award + closing sweep (the F-47-01 site):
  dgnrsOut = _presaleBoxDgnrsReward(player, amount, soldBefore);   // :672 (the ~40% DGNRS branch, outcome < 90)
  if (closing) { remaining = dgnrs.poolBalance(Pool.PresaleBox); swept = dgnrs.transferFromPool(Pool.PresaleBox, player, remaining); dgnrsOut += swept; }  // :682-691
  emit PresaleBoxOpened(player, index, amount, burnieOut, dgnrsOut, wwxrpOut, closing);  // :696

The clamp (StakedDegenerusStonk.sol transferFromPool): returns the CLAMPED amount; `if (available == 0) return 0;` then `if (amount > available) amount = available;` -> cannot over-draw.

Outcome branches per box open: BURNIE (outcome < 50, ~50%), DGNRS (50 <= outcome < 90, ~40%), WWXRP (outcome >= 90, ~10%). The DGNRS branch is the only one that draws Pool.PresaleBox via _presaleBoxDgnrsReward; the closing box additionally sweeps the remainder.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PFIX-03 — tier-shape parity + clamp/empty-before-close proofs (deterministic)</name>
  <files>test/fuzz/PresaleBoxDrain.t.sol</files>
  <read_first>
    contracts/modules/DegenerusGameLootboxModule.sol (the `_presaleBoxDgnrsReward` body :706-727, the
    `_presaleBoxDgnrsTierTenths` ladder :734+, the closing-box sweep :682-691, the outcome-branch dispatch
    around :669-696), contracts/StakedDegenerusStonk.sol (`transferFromPool` clamp + `poolBalance`),
    an existing presale-box Foundry test or fixture to mirror the deploy/open harness (grep for
    `PresaleBoxOpened` / `Pool.PresaleBox` under test/), test/edge/PresalePerMintCap.test.js (presale
    setup reference), .planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-01-SUMMARY.md
    (the exact edit map), 325-ATTEST-PFIX-RFALL.md §A (the verified anchors).
  </read_first>
  <action>
    Create `test/fuzz/PresaleBoxDrain.t.sol` with two DETERMINISTIC tests (no fuzzing yet):

    (a) `test_PFIX03_TierShapePreserved`: drive a tier-1 box open and a tier-5 box open (set `soldBefore`
    into the tier-1 band and the tier-5 band respectively, via the same path the presale uses; if direct
    state seeding is needed mirror the slot-seed helper pattern used in the existing redemption/degenerette
    Foundry tests). Force the DGNRS outcome branch (inject a VRF/seed so `outcome` lands in [50,90)). Assert
    `dgnrsRewardTier1 / amount == 3 * (dgnrsRewardTier5 / amount)` EXACTLY (tier-1 = 3× tier-5 DGNRS-per-ETH),
    using equal `amount` and equal `poolStart`. This proves the tier ladder ratio survived the divisor move.

    (b) `test_PFIX03_EarlyDgnrsRunEmptiesPoolBeforeClose_ClampHolds`: seed a SMALL `Pool.PresaleBox` balance
    relative to the per-box DGNRS reward (so a run of early DGNRS-branch opens drains the pool before the
    closing box). Open several DGNRS-branch boxes; assert `poolBalance(Pool.PresaleBox)` reaches ~0 BEFORE the
    closing box. Then open the CLOSING box (`closing == true`); assert: it does NOT revert, the closing
    `swept` value is `== 0` (or `<= 1 wei` dust from the clamp), and the per-box reward never exceeded the live
    pool balance (no over-draw — `transferFromPool` returned the clamped amount). Capture `swept` via the
    `PresaleBoxOpened` event's `dgnrsOut` minus the box's own roll award, or via the pool-balance delta across
    the closing open.

    Use realistic constants read from the contract (do not hardcode magic numbers the contract defines).
    Run command: `forge test --match-path test/fuzz/PresaleBoxDrain.t.sol -vv`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/PresaleBoxDrain.t.sol --match-test "PFIX03" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `test_PFIX03_TierShapePreserved` PASSES asserting tier-1 DGNRS-per-ETH `== 3 *` tier-5 DGNRS-per-ETH
    (exact integer equality on equal `amount`/`poolStart`). `test_PFIX03_EarlyDgnrsRunEmptiesPoolBeforeClose_ClampHolds`
    PASSES asserting (i) the pool reaches ~0 before the closing box, (ii) the closing open does not revert,
    (iii) closing `swept <= 1 wei`, (iv) no per-box draw exceeded the live pool balance. Zero `contracts/*.sol`
    (mainnet) modifications.
  </acceptance_criteria>
  <done>The tier-shape parity (3×) and the empty-before-close clamp (no revert/no over-draw, swept ~0) are proven against the applied diff.</done>
</task>

<task type="auto">
  <name>Task 2: PFIX-02 — realistic 50-ETH presale run, closing sweep is variance dust (NOT ~60%)</name>
  <files>test/fuzz/PresaleBoxDrain.t.sol</files>
  <read_first>
    The Task-1 harness in test/fuzz/PresaleBoxDrain.t.sol, the outcome-branch dispatch in
    DegenerusGameLootboxModule.sol (~:669-696 — the 50/40/10 BURNIE/DGNRS/WWXRP split), the
    PFIX-02 acceptance text in REQUIREMENTS.md ("~40% realized DGNRS branch rate × the 2.5×-larger curve
    drains the full pool in expectation"), and the PFIX SUMMARY note "per-box DGNRS reward rises ~2.5x so the
    ~40%-DGNRS branch draws the pool down through the boxes".
  </read_first>
  <action>
    Add `test_PFIX02_RealisticRun_ClosingSweepIsDust` to `test/fuzz/PresaleBoxDrain.t.sol`. Set `poolStart`
    and a presale budget sized so a realistic run sells ~50 ETH of presale boxes across MANY boxes (e.g. a
    few hundred boxes). Drive each box's outcome from a deterministic seeded keccak-counter PRNG so the
    realized branch mix is ~50% BURNIE / ~40% DGNRS / ~10% WWXRP (assert the realized DGNRS branch count is
    within a tolerance band of 40% so the run is genuinely the distribution that exposed F-47-01 — do NOT let
    a degenerate all-BURNIE run silently pass). Open all non-closing boxes, then open the closing box.

    Assert:
    - The closing-box `swept` (the `transferFromPool(Pool.PresaleBox, ...)` at the closer) is `<= POOL_DUST_BOUND`,
      an EXPLICIT small bound expressed as a fraction of `poolStart` (e.g. `<= poolStart / 100` = 1% — choose the
      bound so the v47 behavior (~60% of pool) FAILS it by a wide margin; document the chosen bound + rationale
      in a comment). The bound must be tight enough that the OLD `/1_000` curve would NOT satisfy it.
    - The residual `poolBalance(Pool.PresaleBox)` after the closing box is ~empty (`<= POOL_DUST_BOUND`).
    - A REGRESSION-DIRECTION guard (the false-confidence guard): assert the cumulative DGNRS distributed
      through the per-box rewards is the dominant share of `poolStart` (e.g. `>= poolStart * 90 / 100`), so the
      pool is genuinely drained THROUGH the boxes (not parked for the closer). If the per-box draw were still
      the small v47 amount, this guard fails — proving the test exercises the fixed curve.
    Run command: `forge test --match-path test/fuzz/PresaleBoxDrain.t.sol`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/PresaleBoxDrain.t.sol --match-test "PFIX02" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `test_PFIX02_RealisticRun_ClosingSweepIsDust` PASSES: over a ~50-ETH run with realized DGNRS branch rate
    asserted ~40% (within tolerance), the closing-box `swept <= poolStart/100` (an explicit dust bound the v47
    ~60% windfall would fail), the residual pool ends `<= poolStart/100`, and the per-box cumulative DGNRS draw
    is `>= 90%` of `poolStart` (the curve-exercised guard). The test fails loudly if the realized branch mix is
    not the ~40%-DGNRS distribution. Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The realistic 50-ETH presale run proves the closing sweep is variance dust (NOT ~60% of pool) and the pool ends empty, with a guard that the fixed curve is genuinely exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → applied contract diff | The test must exercise the REAL `_presaleBoxDgnrsReward` + closing-sweep path, not a JS replica or a degenerate input |

## STRIDE Threat Register (test-authoring false-confidence focus)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-01-FC1 | Information (false confidence) | PFIX-02 run never hits the ~40% DGNRS branch rate | mitigate | Task 2 ASSERTS the realized DGNRS branch count is ~40% within a tolerance band; a degenerate all-BURNIE run fails the test, not passes it |
| T-327-01-FC2 | Information (false confidence) | dust bound chosen so loose the v47 windfall would also pass | mitigate | Task 2 fixes an EXPLICIT `<= poolStart/100` bound + a `>= 90% of poolStart` per-box-draw guard, so the OLD `/1_000` curve would FAIL both directions; bound rationale documented in-test |
| T-327-01-FC3 | Information (false confidence) | clamp test never actually empties the pool before close | mitigate | Task 1(b) seeds a SMALL pool relative to per-box reward and ASSERTS pool ~0 before the closing box, then asserts closing swept ~0 + no over-draw |
| T-327-01-SC | Tampering | npm/forge install of a new test dep | accept | No new package installs; uses existing forge-std + repo test helpers only |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/PresaleBoxDrain.t.sol` exits 0.
- PFIX-02: realistic ~50-ETH run, realized ~40% DGNRS branch asserted, closing `swept <= poolStart/100`, pool ends ~empty, per-box draw `>= 90%` of poolStart.
- PFIX-03: tier-1 == 3× tier-5 DGNRS-per-ETH; early-DGNRS-run empties pool before close → closing swept ~0, no revert, no over-draw (clamp).
- Zero `contracts/*.sol` (mainnet) modifications (`git status --porcelain contracts/ | grep -v '/test/'` empty).
</verification>

<success_criteria>
The F-47-01 fix is proven: the closing-box sweep is variance dust over a realistic 50-ETH run (not the ~60% windfall), the tier shape is preserved exactly (3×), and the `transferFromPool` clamp holds when early DGNRS hits empty the pool before close — all against the applied Phase-326 diff, with explicit guards against a false-confidence pass.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-01-SUMMARY.md` when done.
Record: the chosen dust bound + rationale, the realized branch-mix tolerance, the tier-shape ratio asserted, and the per-test pass result.
</output>
