---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 04
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [HERO-04, HERO-06]
files_modified:
  - .planning/notes/degenerette-recalibration/derive_5_tables.py
  - test/stat/DegenerettePerNEvExactness.test.js
  - test/stat/DegeneretteBonusEv.test.js
  - test/fuzz/DegeneretteHeroScore.t.sol
must_haves:
  truths:
    - "derive_5_tables.py is EXTENDED to the new 10-bucket S in {0..9} design (S=A+2H, max 9, pay floor S>=2) and prints the canonical byte-reproduced QUICK_PLAY_PAYOUTS S=0..7 packed + S=8 + S=9 separate uint256s + WWXRP factors (B=6..9) per N, all at basePayoutEV = 100 centi-x"
    - "The canonical generator (derive_5_tables.py) produces the byte-exact S=0..9 finals; the PASS_ALL gate REGENERATES every per-N constant from the script (NOT hand-typed) and diffs it against the contract source — IF the contract holds the finals the diff is 0; IF it still holds the intentional placeholders (S8=0, packed=old M-indexed, WWXRP=old) the SUMMARY records the exact placeholder->final diff and emits the `## STOP — HERO BYTE-REPRODUCE NEEDS CONTRACT-CONSTANT LANDING` handoff (the contract-constant landing is OUT OF THIS no-contract phase). The generator is authoritative; constants are never hand-typed."
    - "The chosen payout SHAPE + WWXRP-bonus/sDGNRS/high-tier thresholds re-map onto the 10-point scale and the S=8/S=9 (>32-bit) packing is verified decodable"
    - "The recalibration stays write-batch byte-identical to v47 resolveBets (DGAS): per-bet vs batched resolve produce identical payouts/claimable/pool/event"
    - "The dailyHeroWagers / _rollHeroSymbol daily-hero-symbol jackpot is unaffected by the matches-range (0-8 -> 0-9) change (reads wagered symbols, not per-bet scores) — no leakage"
  artifacts:
    - path: ".planning/notes/degenerette-recalibration/derive_5_tables.py"
      provides: "The canonical 10-bucket S=0..9 EV-equal payout generator (the byte-reproduce source of truth)"
      contains: "S9"
    - path: "test/stat/DegenerettePerNEvExactness.test.js"
      provides: "The Phase-267-style PASS_ALL byte-reproduce gate + per-N EV exactness on the 10-bucket scale"
      contains: "derive_5_tables"
    - path: "test/fuzz/DegeneretteHeroScore.t.sol"
      provides: "Foundry proof of S=A+2H scoring, write-batch DGAS equivalence, and HERO-06 no-leak"
      contains: "dailyHeroWagers"
  key_links:
    - from: "test/stat/DegenerettePerNEvExactness.test.js"
      to: ".planning/notes/degenerette-recalibration/derive_5_tables.py"
      via: "regenerate the canonical constants and diff against the contract source (PASS_ALL gate, not hand-typed)"
      pattern: "derive_5_tables|child_process|spawnSync"
    - from: "test/fuzz/DegeneretteHeroScore.t.sol"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      via: "_score(playerTicket, resultTicket, heroQuadrant) + resolveBets write-batch + dailyHeroWagers no-leak"
      pattern: "_score|resolveBets|dailyHeroWagers"
---

<objective>
Prove the Degenerette hero 2-point rescale (S = A + 2*H, max 9, pay floor S>=2) is byte-identical from the canonical generator (HERO-04) and write-batch / daily-hero-jackpot safe (HERO-06), against the applied Phase-326 diff.

Five properties:
1. HERO-04 byte-reproduce gate — EXTEND derive_5_tables.py to the NEW 10-bucket S in {0..9} design (the script currently produces the OLD 9-bucket M=0..8 SHAPE), so it prints the canonical QUICK_PLAY_PAYOUTS_N{N}_PACKED (S=0..7), the separate QUICK_PLAY_PAYOUT_N{N}_S8 + _S9 (both >32-bit), and the WWXRP_FACTORS_N{N}_PACKED (B=6..9) at basePayoutEV = 100 centi-x. Author the Phase-267-style PASS_ALL gate that REGENERATES every constant from the script and DIFFS it against the contract source (NOT hand-typed).
2. HERO-04 shape + thresholds + packing — the chosen payout SHAPE over S in {0..9} + the WWXRP-bonus / sDGNRS / "high tier" thresholds re-map onto the 10-point scale; the S=8/S=9 >32-bit packing is verified decodable.
3. HERO-06 DGAS — the recalibration stays write-batch byte-identical to v47 resolveBets (per-bet vs batched resolve = identical payout/claimable/pool/event).
4. HERO-06 no-leak — the dailyHeroWagers / _rollHeroSymbol daily-hero-symbol jackpot is unaffected by the matches range change (0-8 -> 0-9); it reads wagered symbols, not per-bet scores.

CRITICAL SCOPE NOTE (read before starting — there is a blocking dependency this phase cannot resolve alone):
The byte-reproduce constants must live in contracts/modules/DegenerusGameDegeneretteModule.sol, which currently ships INTENTIONAL PLACEHOLDERS (committed f50cc634): QUICK_PLAY_PAYOUTS_N{N}_PACKED (S=0..7) = OLD M-indexed values; QUICK_PLAY_PAYOUT_N{N}_S8 = 0; WWXRP_FACTORS_N{N}_PACKED = old. ONLY the _S9 constants are final (the M=8 relabel). Replacing the placeholders with the byte-reproduced S-distribution constants is a contracts/*.sol MAINNET EDIT — which is HARD-PROHIBITED in this TST phase (test-authoring only; hand-review gate). Therefore the PASS_ALL gate will REMAIN RED on the placeholder constants until the contract-constant landing happens. This plan AUTHORS the canonical generator + the gate + the diff so the EXACT replacement constants are produced, and SURFACES the contract-constant landing as a STOP/handoff. Do NOT edit contracts/*.sol. Do NOT weaken the gate to make it green against placeholders. Do NOT hand-type constants into the test to fake a pass.

The byte-reproduce gate's PASS/RED state is CONDITIONAL by design and the post-execution verifier must NOT treat a RED gate as a failure of this plan: this plan's deliverable is the authoritative generator + the gate + the produced finals + the STOP handoff. A 0-diff PASS only happens IF the (out-of-phase, hand-reviewed, `CONTRACTS_COMMIT_APPROVED=1`-gated) constant-only landing of the `derive_5_tables.py` finals into the contract has already occurred. Within this no-contract phase the EXPECTED state is RED-with-recorded-diff + STOP. The cross-phase regression closure of this RED gate is owned by 327-06 (which documents the expected post-landing failure-count delta) — NOT by this plan applying any contract edit.

Output: extended derive_5_tables.py, the PASS_ALL gate in DegenerettePerNEvExactness.test.js (+ DegeneretteBonusEv.test.js), and test/fuzz/DegeneretteHeroScore.t.sol. The Foundry scoring/DGAS/no-leak proofs (Tasks 2-3) run GREEN against the applied diff (they do not depend on the final table VALUES). The byte-reproduce gate (Task 1) produces the canonical constants and either (i) passes if the contract already holds them, or (ii) records the precise placeholder->final diff + STOP if the placeholders are still present.

NO contracts/*.sol (mainnet) edits. The subject is FROZEN at the Phase-326 diff. Editing the .planning/notes Python generator + test/ files IS allowed (not contract source). If a real defect surfaces, STOP and surface it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-03-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md

<interfaces>
<!-- Anchors from the APPLIED Phase-326 diff + the canonical generator. Re-grep at execute-time. -->

contracts/modules/DegenerusGameDegeneretteModule.sol (the rescaled scoring/payout):
  _score(uint32 playerTicket, uint32 resultTicket, uint8 heroQuadrant) private pure returns (uint8 s);  // :928 — S=A+2H, max 9
  _getBasePayoutBps(uint8 N, uint8 s) private pure returns (uint256);  // :1038 — if(s>=9) S9; if(s==8) S8; else packed (s*32)
  _wwxrpBonusBucket(uint8 s) private pure;        // floor S>=6 -> buckets 6..9
  _wwxrpFactor(uint8 N, uint8 bucket) private pure;  // dispatch offset (bucket-6)
  _fullTicketPayout(...);  // :997 — dispatches on s; hero multiplier DELETED
  _awardDegeneretteDgnrs(player, amountPerTicket, s);  // gate s>=7; DEGEN_DGNRS_7/8/9_BPS = 400/800/1500
  resolveBets(player, betIds) -> _resolveBet -> _resolveFullTicketBet -> _distributePayout : cross-bet acc flushed ONCE (DGAS write-batch)
  // KEPT: FT_HERO_SHIFT decode, heroQuadrant>=4 revert. Event field still named `matches`; NatSpec range 0-9.
  // dailyHeroWagers[day][heroQuadrant] WRITE :538-546 (reads the player's WAGERED heroSymbol, NOT _score)

  PLACEHOLDER constants (committed f50cc634 — NOT final):
    QUICK_PLAY_PAYOUTS_N{N}_PACKED (S=0..7) = old M-indexed values  (:260-264)
    QUICK_PLAY_PAYOUT_N{N}_S8 = 0                                   (:277-281)
    QUICK_PLAY_PAYOUT_N{N}_S9 = 10_756_411.. (FINAL, M=8 relabel)   (:269-273)
    WWXRP_FACTORS_N{N}_PACKED = old values                         (:299-303)

.planning/notes/degenerette-recalibration/derive_5_tables.py (the canonical generator):
  - CURRENT: SHAPE = [0,0,190,475,1500,4250,19500,100000,10_000_000] (9 buckets M=0..8); per-N convolution P_N(M);
    scales each table to basePayoutEV=100 centi-x; prints QUICK_PLAY_PAYOUTS_N{N}_PACKED (M=0..7) + _M8 + WWXRP (B=5..8) + HERO_BOOST.
  - MUST BE EXTENDED to the 10-bucket S=0..9 design: hero now contributes +2 to score (S=A+2H), the standalone
    hero multiplier is GONE (delete the HERO_BOOST derivation), pay floor S>=2, S=9 is the jackpot (== old M=8
    relabel, value unchanged), the per-S match-count distribution P_N(S) is the convolution over the 7 ordinary
    axes + the doubled hero-symbol axis; WWXRP buckets move to B=6..9; both S=8 and S=9 are separate uint256s.

test/stat/DegenerettePerNEvExactness.test.js — the EXISTING PASS_ALL byte-reproduce harness (pastes the .sol
  constants + a JS-replica of the dispatch chain; asserts per-N basePayoutEV == 100 centi-x within +/-0.5; was
  "verified PASS_ALL_25 against derive_5_tables.py" for the OLD 9-bucket layout). Runs under `npm run test:stat`.
test/stat/DegeneretteBonusEv.test.js — the WWXRP/ETH-bonus EV exactness harness (same per-N factor basis).
test/helpers/rollHeroSymbolRef.mjs — the JS reference for _rollHeroSymbol (reads dailyHeroWagers, NOT scores).
test/fuzz/handlers/DegeneretteHandler.sol, test/fuzz/invariant/DegeneretteBet.inv.t.sol — Foundry bet harness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HERO-04 — extend derive_5_tables.py to the 10-bucket S=0..9 design + the PASS_ALL byte-reproduce gate (regenerate, never hand-type)</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py, test/stat/DegenerettePerNEvExactness.test.js, test/stat/DegeneretteBonusEv.test.js</files>
  <read_first>
    .planning/notes/degenerette-recalibration/derive_5_tables.py (the WHOLE script — P_COMMON/P_GOLD per-quadrant
    distributions, convolution, SHAPE + scale-to-100, the WWXRP factor + hero-boost blocks, the FINAL PASTE-READY
    section), test/stat/DegenerettePerNEvExactness.test.js (the pasted-constant block ~:112-160 + the JS-replica
    dispatch ~:209-306 + the per-N EV assertion ~:448-490), test/stat/DegeneretteBonusEv.test.js (the bonus-EV
    basis), contracts/modules/DegenerusGameDegeneretteModule.sol (`_score` :928, `_getBasePayoutBps` :1038 S8/S9
    dispatch, `_wwxrpBonusBucket`/`_wwxrpFactor` bucket offsets, the placeholder constants :260-303, the FINAL
    _S9 values :269-273), 326-03-SUMMARY.md (the SHAPE DECISION flag: WWXRP floor S>=6 vs S>=7, packing layout,
    threshold shift-by-one), 325-ATTEST-BTOMB-HERO.md section B (the verified deleted/kept constructs + H11 packing).
  </read_first>
  <action>
    (A) EXTEND `.planning/notes/degenerette-recalibration/derive_5_tables.py` to the 10-bucket S in {0..9} design:
    - Replace the per-quadrant match logic with the hero-aware score S = A + 2*H: A = matches among the 7 ordinary
      axes (4 colors + 3 non-hero symbols); H = 1 iff the hero quadrant's SYMBOL matches; pay floor S>=2. Derive
      P_N(S) analytically as the convolution over the 7 ordinary axes (color distribution depends on N gold
      quadrants per the existing P_COMMON/P_GOLD; symbol axes Bernoulli 1/8) PLUS the doubled hero-symbol axis
      (a Bernoulli(1/8) contributing 0 or 2 to S). The hero quadrant's COLOR stays an ordinary axis. Cross-check:
      sum_S P_N(S) == 1 and S=9 is exactly the all-7-ordinary + hero-symbol event (== old M=8 odds).
    - Solve each per-N payout table to basePayoutEV = 100 centi-x (keep the existing scale-then-refine approach;
      choose the SHAPE per 326-03/SPEC — frequent-small vs juicy-top; document the chosen SHAPE inline). Pin S=9
      to the FINAL relabel values already in the contract (10_756_411.. — assert the script reproduces them so the
      relabel is consistent). Emit S=8 and S=9 as SEPARATE uint256s (both >32-bit), S=0..7 packed.
    - Recompute the WWXRP factor tables for buckets B=6..9 (the rescaled floor) holding ETH-bonus EV = 5.000% +
      WWXRP high-roi EV per N (the existing factor-solve logic, re-bucketed). DELETE the standalone HERO_BOOST
      derivation (the multiplier is gone).
    - Print FINAL PASTE-READY `uint256 private constant QUICK_PLAY_PAYOUTS_N{N}_PACKED`, `_S8`, `_S9`,
      `WWXRP_FACTORS_N{N}_PACKED` hex + a per-N basePayoutEV drift line (target 100.0000, |drift| < 0.5 centi-x).

    (B) Update the PASS_ALL byte-reproduce gate in `test/stat/DegenerettePerNEvExactness.test.js`:
    - Add a test that RUNS the python script (child_process spawnSync 'python3' derive_5_tables.py) and PARSES its
      FINAL PASTE-READY hex, then compares each constant to the value READ from the contract source
      (fs.readFileSync DegenerusGameDegeneretteModule.sol + regex the QUICK_PLAY_PAYOUTS_N{N}_PACKED / _S8 / _S9 /
      WWXRP_FACTORS_N{N}_PACKED literals). Assert the diff is 0 for EVERY constant (Phase-267-style PASS_ALL_N).
      The constants compared MUST be the script's regenerated values, never hand-typed in the test. Assert the
      spawnSync python3 invocation succeeded (status === 0) BEFORE parsing — a generator failure must surface as a
      test failure, never a silently-empty parse that masks itself.
    - Update the JS-replica dispatch chain to the 10-bucket S-scale (S8/S9 separate, WWXRP buckets 6..9, score
      S=A+2H, DGNRS gate S>=7) and re-assert per-N basePayoutEV == 100 centi-x within +/-0.5 against the
      REGENERATED tables.
    - Mirror the WWXRP/ETH-bonus EV re-bucketing into `test/stat/DegeneretteBonusEv.test.js` (assert ETH bonus EV
      == 5.000% per N on the new factor tables).
    - If the contract still holds the placeholders (S8==0, packed==old): the diff assertion FAILS — record the
      EXACT script-regenerated final constants + the placeholder->final diff in the SUMMARY and emit a
      `## STOP — HERO BYTE-REPRODUCE NEEDS CONTRACT-CONSTANT LANDING` block (the final constants are ready to be
      applied to the contract under the hand-review gate; this TST phase cannot apply them). This RED-with-diff +
      STOP is the EXPECTED, in-scope outcome of the no-contract phase — it is NOT a plan failure. Do NOT edit the
      contract; do NOT skip/weaken the gate.

    Run commands: `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` (prints the constants),
    then `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js`.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py 2>&1 | grep -E "QUICK_PLAY_PAYOUT_N._S8|QUICK_PLAY_PAYOUTS_N._PACKED|basePayoutEV" | tail -20 && npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    derive_5_tables.py prints the 10-bucket S in {0..9} canonical constants (S=0..7 packed + S=8 + S=9 separate
    uint256s + WWXRP B=6..9) with per-N basePayoutEV drift |<0.5| centi-x and S=9 == the FINAL relabel values.
    The PASS_ALL gate REGENERATES every constant from the script (not hand-typed; spawnSync status checked) and
    asserts a 0 diff vs the contract source. If the contract holds the byte-reproduced finals, the gate PASSES and
    per-N EV == 100 within +/-0.5; if the contract still holds the placeholders, the gate records the precise
    placeholder->final diff + the `## STOP` handoff (the finals are ready for the hand-review contract-constant
    landing) — the EXPECTED, in-scope no-contract-phase outcome, NOT a plan failure. The verify chain is joined
    with `&&` so a non-zero python3 exit propagates (no `echo`-separator masking a generator failure).
    DegeneretteBonusEv asserts ETH bonus EV == 5.000% per N on the new factors. Zero contracts/*.sol (mainnet)
    modifications.
  </acceptance_criteria>
  <done>The canonical 10-bucket generator + the PASS_ALL byte-reproduce gate are authored; either the contract matches byte-for-byte or the exact final constants + the contract-landing STOP are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: HERO-04/06 scoring — S=A+2H {0..9}, packing decodable, S=9==M=8 jackpot relabel, DGNRS thresholds re-mapped</name>
  <files>test/fuzz/DegeneretteHeroScore.t.sol</files>
  <read_first>
    contracts/modules/DegenerusGameDegeneretteModule.sol (`_score` :928, `_resolveFullTicketBet` :676-724,
    `_getBasePayoutBps` :1038 + the S8/S9 separate-uint256 dispatch :1040+, `_wwxrpBonusBucket`/`_wwxrpFactor`
    :959-980, `_awardDegeneretteDgnrs` gate s>=7 + DEGEN_DGNRS_7/8/9_BPS :205-207, the FullTicketResult event :98,
    FT_HERO_SHIFT decode + heroQuadrant>=4 revert), test/fuzz/handlers/DegeneretteHandler.sol +
    test/fuzz/invariant/DegeneretteBet.inv.t.sol (the bet/resolve harness to mirror), the deploy + VRF-injection
    harness (test/fuzz/helpers/DeployProtocol.sol, VRFHandler), 326-03-SUMMARY.md (scoring + threshold details),
    325-ATTEST-BTOMB-HERO.md (the kept/deleted constructs).
  </read_first>
  <action>
    Create `test/fuzz/DegeneretteHeroScore.t.sol` (Foundry). These prove SCORING SHAPE/BEHAVIOR and do NOT depend
    on the final table VALUES (so they pass against the applied diff regardless of the Task-1 placeholder state):

    (a) `test_HERO_ScoreFormula`: construct player/result tickets with controlled ordinary matches A and a hero
        symbol match/miss; via a VRF-injected resolve (or a thin test harness reaching the resolve path), assert
        the score the contract uses is S = A + 2*H, capped at 9; assert hero-symbol-alone (A=0, H=1) yields S=2
        (a guaranteed win, pay floor S>=2 — payout > 0); assert the hero quadrant's COLOR is an ordinary axis
        (a hero-color-only match contributes 1, not 2).
    (b) `test_HERO_S9EqualsOldM8Jackpot`: construct the all-7-ordinary + hero-symbol event (S=9); assert the
        payout equals the FINAL `QUICK_PLAY_PAYOUT_N{N}_S9` value (the relabel — identical odds/physical event as
        old M=8). This anchors the jackpot relabel against the FINAL constant (which is already correct, not a
        placeholder).
    (c) `test_HERO_S8S9PackingDecodable`: assert `_getBasePayoutBps(N, 8)` and `_getBasePayoutBps(N, 9)` dispatch
        to the separate uint256 constants (not the packed slot), and that S=0..7 decode from the packed slot via
        `(packed >> (s*32)) & 0xFFFFFFFF` — i.e. the >32-bit S8/S9 packing scheme is decodable end-to-end.
        (Values may be placeholders; this proves the DISPATCH/packing shape, which is final.)
    (d) `test_HERO_DgnrsThresholdsRemapped`: assert the DGNRS award gate fires only for S>=7 (not S>=6), and the
        BPS tiers map S=7->4% / S=8->8% / S=9->15% (DEGEN_DGNRS_7/8/9_BPS). Drive scores S=6/7/8/9 and assert
        award/no-award + the correct BPS.
    Run command: `forge test --match-path test/fuzz/DegeneretteHeroScore.t.sol -vv`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/DegeneretteHeroScore.t.sol --match-test "HERO_Score|HERO_S9|HERO_S8|HERO_Dgnrs" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    All four tests PASS: S = A + 2*H capped at 9 with hero-alone => S=2 win and hero-color ordinary; S=9 pays the
    FINAL S9 jackpot constant (relabel of old M=8); the S8/S9 separate-uint256 packing + the S0..7 packed slot are
    decodable via the dispatch; the DGNRS award gate fires only S>=7 with the re-mapped BPS tiers. Zero
    contracts/*.sol (mainnet) modifications.
  </acceptance_criteria>
  <done>The S=A+2H scoring, the S=9==M=8 jackpot relabel, the S8/S9 packing dispatch, and the re-mapped DGNRS thresholds are proven against the applied diff.</done>
</task>

<task type="auto">
  <name>Task 3: HERO-06 — write-batch DGAS equivalence + dailyHeroWagers / _rollHeroSymbol no-leak</name>
  <files>test/fuzz/DegeneretteHeroScore.t.sol</files>
  <read_first>
    contracts/modules/DegenerusGameDegeneretteModule.sol (`resolveBets` :415 -> `_resolveBet` -> `_resolveFullTicketBet`
    -> `_distributePayout`, the cross-bet `acc` accumulator flushed ONCE :676-724/:806; the dailyHeroWagers WRITE
    :538-546 reading the player's WAGERED heroSymbol; FullTicketResult event :98), contracts/DegenerusGame.sol
    + contracts/modules/DegenerusGameJackpotModule.sol (`_rollHeroSymbol` reads dailyHeroWagers, NOT scores),
    test/helpers/rollHeroSymbolRef.mjs (the JS reference for the daily-hero jackpot), test/edge/HeroOverrideDayIndex.test.js
    + test/edge/HeroOverrideWeightedRoll.test.js (existing daily-hero-jackpot coverage to mirror, NOT duplicate),
    the Task-2 harness; 325-ATTEST-BTOMB-HERO.md H19 (the no-leak attestation).
  </read_first>
  <action>
    Add HERO-06 tests to `test/fuzz/DegeneretteHeroScore.t.sol`:

    (a) `test_HERO06_WriteBatchByteIdentical_DGAS`: resolve a set of N bets via a SINGLE `resolveBets(player,
        betIds[])` call, and INDEPENDENTLY resolve the SAME N bets one-at-a-time (fresh state, same VRF words,
        same tickets). Assert the cumulative claimable credited, the pool deltas, and the per-bet FullTicketResult
        events are byte-identical between the batched and per-bet paths (the cross-bet `acc` flush is same-results
        — the DGAS constraint). Use deterministic VRF injection so both runs see identical randomness.
    (b) `test_HERO06_DailyHeroJackpotUnaffected_NoLeak`: place ETH bets with chosen hero symbols (driving
        dailyHeroWagers[day][quadrant] via the WAGER write path), resolve them with VARIED per-bet scores S
        (including the new S=8/S=9), and assert dailyHeroWagers[day][quadrant] reflects ONLY the wagered symbols
        (the bet's chosen heroSymbol decoded from the ticket), NOT the per-bet `_score`/matches outcome — i.e.
        changing the resolution score does not change the daily-hero wager ledger. Then drive `_rollHeroSymbol`
        (or its JS reference rollHeroSymbolRef.mjs replay against the on-chain dailyHeroWagers) and assert the
        rolled hero symbol depends only on the wager pool, unaffected by the 0-8->0-9 matches-range change.
        Concretely: two runs with IDENTICAL wagers but DIFFERENT resolution scores must produce the IDENTICAL
        dailyHeroWagers ledger and the IDENTICAL _rollHeroSymbol result.
    Run command: `forge test --match-path test/fuzz/DegeneretteHeroScore.t.sol`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/DegeneretteHeroScore.t.sol --match-test "HERO06" -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    (a) PASSES proving batched `resolveBets` == per-bet resolution byte-identical (claimable/pool/event) — the
    DGAS write-batch same-results constraint; (b) PASSES proving dailyHeroWagers + _rollHeroSymbol depend ONLY on
    wagered symbols (identical wagers + different resolution scores => identical ledger + identical roll), so the
    0-8->0-9 matches-range change cannot leak into the daily-hero jackpot. Zero contracts/*.sol (mainnet)
    modifications.
  </acceptance_criteria>
  <done>The recalibration is proven write-batch byte-identical to v47 resolveBets, and the daily-hero-symbol jackpot is proven unaffected by the matches-range change (no leak).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical generator -> contract constants | The byte-reproduce gate must regenerate from derive_5_tables.py, never trust hand-typed constants |
| per-bet score -> daily-hero jackpot ledger | The matches-range change must not leak into dailyHeroWagers (different state, different code path) |

## STRIDE Threat Register (test-authoring false-confidence focus)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-04-FC1 | Information (false confidence) | byte-reproduce gate compares against HAND-TYPED constants instead of regenerating from the script | mitigate | Task 1(B) spawns python3 derive_5_tables.py at test time (status checked) and diffs the REGENERATED hex vs the contract source; no constant is hand-typed in the test |
| T-327-04-FC2 | Information (false confidence) | gate quietly weakened/skipped to pass against the placeholders | mitigate | Task 1 FORBIDS weakening; if placeholders remain, the gate FAILS and emits the `## STOP — HERO BYTE-REPRODUCE NEEDS CONTRACT-CONSTANT LANDING` handoff with the exact diff (the expected in-scope no-contract-phase outcome) |
| T-327-04-FC3 | Information (false confidence) | DGAS "same-results" asserted without an independent per-bet baseline | mitigate | Task 3(a) resolves the SAME bets both batched and per-bet under identical VRF and asserts byte-identical claimable/pool/event |
| T-327-04-FC4 | Information (false confidence) | no-leak asserted by inspection, not by a differential run | mitigate | Task 3(b) runs identical wagers with DIFFERENT resolution scores and asserts identical dailyHeroWagers + _rollHeroSymbol |
| T-327-04-FC5 | Information (false confidence) | a python3 generator failure is masked by an `echo` separator so the stat gate "passes" on stale/empty output | mitigate | Task 1 joins the python3 generation and the `npx hardhat test` with `&&` (non-zero python3 exit propagates) AND asserts spawnSync status === 0 before parsing |
| T-327-04-SC | Tampering | npm/forge/python install of a new dep | accept | No new installs; uses repo python3 + existing hardhat/forge + repo helpers (child_process is node builtin) |
</threat_model>

<verification>
- `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` prints the 10-bucket S=0..9 canonical constants (drift |<0.5| centi-x; S=9 == FINAL relabel) and EXITS 0; the stat gate is chained with `&&` so a generator failure propagates.
- `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js`: PASS_ALL byte-reproduce (0 diff) + per-N EV==100 + ETH bonus==5.000% IF the contract holds the finals — OR the precise placeholder->final diff + `## STOP` handoff if the contract still holds placeholders (the EXPECTED in-scope no-contract-phase state; not a plan failure).
- `forge test --match-path test/fuzz/DegeneretteHeroScore.t.sol` exits 0: S=A+2H scoring, S=9 jackpot relabel, S8/S9 packing dispatch, DGNRS thresholds, write-batch DGAS equivalence, and dailyHeroWagers/_rollHeroSymbol no-leak.
- Zero contracts/*.sol (mainnet) modifications (`git status --porcelain contracts/ | grep -v '/test/'` empty).
</verification>

<success_criteria>
The Degenerette hero rescale is proven: the canonical 10-bucket S=0..9 constants are regenerated byte-for-byte from derive_5_tables.py under a PASS_ALL gate (the EXACT contract constants, produced from the script — not hand-typed), the S=A+2H scoring + S=9 jackpot relabel + S8/S9 packing + re-mapped DGNRS thresholds hold, the write-batch resolveBets is byte-identical to v47 (DGAS), and the daily-hero-symbol jackpot is unaffected by the matches-range change. The one item this TST phase cannot complete — landing the byte-reproduced constants into the frozen contract — is surfaced as an explicit STOP/handoff with the ready-to-apply finals (see PHASE-LEVEL BLOCKER in the planner return). A RED byte-reproduce gate against the placeholders is the EXPECTED in-scope outcome, NOT a plan failure; its cross-phase closure is documented by 327-06.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-04-SUMMARY.md` when done.
Record: the chosen payout SHAPE, the per-N basePayoutEV drift, the full set of script-regenerated FINAL constants
(S=0..7 packed + S8 + S9 + WWXRP) ready for the contract-constant landing, whether the contract matched or the
placeholder->final diff + the `## STOP` handoff, the DGAS batched-vs-per-bet result, and the no-leak differential result.
</output>
</content>
</invoke>
