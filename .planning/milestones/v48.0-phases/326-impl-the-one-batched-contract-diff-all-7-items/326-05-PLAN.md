---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 05
type: execute
wave: 3
depends_on: ["326-04"]
files_modified:
  - contracts/StakedDegenerusStonk.sol
  - contracts/DegenerusVault.sol
autonomous: true
requirements: [POOL-01, POOL-02, POOL-03, POOL-05]
must_haves:
  truths:
    - "VAULT has a permissionless recoverAfKingPool() that calls afKing.withdraw(afKing.poolOf(address(this))); recovered ETH lands in VAULT reserves via its open receive(); no gameOver gate (anytime recovery)."
    - "sDGNRS receive() is relaxed to accept AF_KING in addition to GAME, so AfKing's withdraw send-back lands; accounting-safe because reserves are read live via address(this).balance (no running counter SSTORE in receive())."
    - "sDGNRS auto-recovers its AfKing pool inside burnAtGameOver() BEFORE the balanceOf(this)==0 early-return, so a zero-pool-TOKEN sDGNRS still recovers its ETH pool; no standalone sDGNRS withdraw (D-04)."
    - "AfKing.sol itself stays UNCHANGED for item 4; the IAfKing/IAfKingSubscribe withdraw(uint256)/poolOf(address) interface adds in sStonk + VAULT match AfKing.sol's signatures verbatim (POOL-05)."
  artifacts:
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "receive() relaxed to accept AF_KING; burnAtGameOver pool-recovery before the balanceOf(this)==0 early-return; IAfKingSubscribe withdraw/poolOf adds"
      contains: "recoverAfKingPool"
    - path: "contracts/DegenerusVault.sol"
      provides: "permissionless recoverAfKingPool(); IAfKing/IAfKingSubscribe withdraw/poolOf adds"
      contains: "recoverAfKingPool"
  key_links:
    - from: "contracts/DegenerusVault.sol recoverAfKingPool()"
      to: "AfKing.withdraw / AfKing.poolOf (UNCHANGED)"
      via: "afKing.withdraw(afKing.poolOf(address(this)))"
      pattern: "afKing.withdraw"
    - from: "contracts/StakedDegenerusStonk.sol burnAtGameOver"
      to: "AfKing.withdraw / AfKing.poolOf (UNCHANGED)"
      via: "afKing.withdraw(afKing.poolOf(address(this))) before the balanceOf(this)==0 early-return"
      pattern: "afKing.withdraw"
---

<objective>
Item 4 (POOL) — make the AfKing prepaid pools recoverable. VAULT/sDGNRS AfKing prepaid-pool ETH is permanently stuck today: both only import `subscribe`, never call `withdraw`; ingress is the permissionless `depositFor`; sweep (the only `_poolOf` ETH consumer) is `rngLocked`-gated and ends with the game, so donated VAULT/SDGNRS pool ETH strands. Worse, sDGNRS's `receive()` is `onlyGame`, so AfKing's `withdraw` send-back reverts. The locked fix: VAULT gets a permissionless `recoverAfKingPool()`; sDGNRS relaxes `receive()` to also accept `AF_KING` and auto-recovers its pool inside `burnAtGameOver()` (before the `balanceOf(this)==0` early-return); `AfKing.sol` itself stays UNCHANGED (the `withdraw`/`poolOf` it already has are sufficient — POOL-05 verbatim).

Purpose: ROADMAP SC4 (POOL-01/02/03/05) — VAULT permissionless `recoverAfKingPool()` (ETH to VAULT reserves via its open `receive()`, no gameOver gate); sDGNRS `receive()` accepts `AF_KING`; sDGNRS auto-recovers inside `burnAtGameOver()` BEFORE the early-return (no standalone sDGNRS withdraw); `AfKing.sol` UNCHANGED with `withdraw(uint256)`/`poolOf(address)` interface adds matching its signatures verbatim. This is the SECOND sStonk edit (after RFALL's item-2 segregation in 326-04 — hence Wave 3, serialized) and opens `DegenerusVault.sol` (shared later with KEEP/SWAP — apply-order R5: interface adds first, then the wrappers).
Output: The sStonk `receive()` relaxation + `burnAtGameOver` pool-recovery + interface adds; the VAULT `recoverAfKingPool()` + interface adds.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 326-08. NOTHING pre-approved. `AfKing.sol` MUST stay byte-UNCHANGED in THIS plan (item 4 leaves it untouched; item 3's `sweep`→`autoBuy` rename is plan 326-06, a separate work item). APPLY-ORDER: on sStonk, the `IAfKingSubscribe` interface adds (:57) come FIRST (the decl must precede its first use), then `receive()` (:433), then `burnAtGameOver` (:525). On VAULT, the interface adds (:76) FIRST, then `recoverAfKingPool()` with the other `gameXxx`/recovery wrappers. Do NOT touch the item-2 RFALL surfaces (`_submitGamblingClaimFrom`, `pullRedemptionReserve`) or item-7 SWAP's VAULT wrapper / `IDegenerusGamePlayerActions` entry (plan 326-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md
@.planning/PLAN-V48-AFKING-POOL-RECOVERY.md

<interfaces>
<!-- AfKing's EXACT signatures the interface adds must match VERBATIM (POOL-05). Extracted from live source. -->

contracts/AfKing.sol (UNCHANGED in this plan — these are the verbatim signatures to mirror):
  :318  function withdraw(uint256 amount) external                 // → interface decl `function withdraw(uint256) external;`
  :503  function poolOf(address player) external view returns (uint256)  // → `function poolOf(address) external view returns (uint256);`
  :304  function depositFor(address player) external payable        // (permissionless ingress — context only)
  :327  (bool ok, ) = msg.sender.call{value: amount}("");           // sends to CALLER (sDGNRS-as-caller → its receive())

contracts/StakedDegenerusStonk.sol:
  :57-67  interface IAfKingSubscribe { function subscribe(address, bool, bool, uint8, uint8, address) external payable; }  // single member today — ADD withdraw/poolOf
  :433    receive() external payable onlyGame { ... }               // relax to also accept AF_KING
  :525    function burnAtGameOver() external onlyGame               // :526 bal = balanceOf[this]; :527 if (bal == 0) return; — pool-recover BEFORE :527

contracts/DegenerusVault.sol:
  :76-86  interface IAfKingSubscribe { function subscribe(...) external payable; }  // single member today — ADD withdraw/poolOf
  :497    receive() external payable { emit Deposit(...); }         // open receive — recovered ETH lands here
  :550    function gameOpenLootBox(...) external onlyVaultOwner     // the gameXxx wrapper pattern to mirror (recoverAfKingPool is PERMISSIONLESS, no onlyVaultOwner)
</interfaces>

<grep_protocol>
Baseline `da5c9d50` (live tree byte-identical). Plan 326-04 (Wave 2) already edited `StakedDegenerusStonk.sol` (the item-2 `_submitGamblingClaimFrom` segregation) — so the sStonk line numbers below (:57/:433/:525) may have SHIFTED by 326-04's hunks. RE-GREP `interface IAfKingSubscribe` / `receive() external payable onlyGame` / `function burnAtGameOver` / `if (bal == 0) return` in sStonk and `interface IAfKingSubscribe` / `receive() external payable` / `onlyVaultOwner` in VAULT before editing — never trust a raw line number. The 325-ATTEST-KEEP-POOL §B table (L1-L15) + POOL-05 resolution + the `receive()` accounting-safety note + carried correction (POOL-06 accept-as-minor) are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VAULT — add the IAfKing/IAfKingSubscribe withdraw/poolOf adds + the permissionless recoverAfKingPool() (R5 item-4)</name>
  <read_first>
    - contracts/DegenerusVault.sol (the inline `IAfKingSubscribe` interface :76-86 — single `subscribe` member; the open `receive()` :497; the `gameXxx onlyVaultOwner` wrapper pattern, e.g. `gameOpenLootBox` :550; how VAULT holds its `afKing` reference / ctor subscribe :471)
    - contracts/AfKing.sol (`withdraw(uint256)` :318, `poolOf(address) returns (uint256)` :503 — the VERBATIM signatures the interface adds must match; UNCHANGED in this plan)
    - 325-ATTEST-KEEP-POOL.md §B (L5/L9/L13/L14) + POOL-05 resolution + 325-SPEC.md §1 R5 (item-4 portion)
    - PLAN-V48-AFKING-POOL-RECOVERY.md (the permissionless `recoverAfKingPool()` design)
  </read_first>
  <action>
    Per POOL-01/02/05 + SPEC §1 R5 (item-4 VAULT portion). (A) Extend VAULT's inline interface to declare AfKing's `withdraw`/`poolOf`: add `function withdraw(uint256) external;` and `function poolOf(address) external view returns (uint256);` to the single-member interface (the decl must precede its first use — interface adds FIRST). Match `AfKing.sol:318/:503` VERBATIM (POOL-05). If the interface is named `IAfKingSubscribe` and the design expects a separate `IAfKing`, follow the SPEC R5 naming (it cites both `IAfKing`/`IAfKingSubscribe`) — re-grep the existing interface name and add the two members to it (or add the named `IAfKing` if the design doc uses that), but the SELECTORS must be verbatim. (B) Add `function recoverAfKingPool() external { afKing.withdraw(afKing.poolOf(address(this))); }` — PERMISSIONLESS (NO `onlyVaultOwner`, NO gameOver gate — anytime recovery). Recovered ETH lands in VAULT reserves via VAULT's open `receive()` (:497). Follow the existing `gameXxx` wrapper shape for placement/style but WITHOUT the access modifier (it is intentionally permissionless — anyone may trigger VAULT's own-pool recovery; harms no one, the ETH only ever lands in VAULT). RE-GREP the interface + the wrapper region before editing.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function recoverAfKingPool" contracts/DegenerusVault.sol && grep -n "function withdraw(uint256) external;\|function poolOf(address) external view returns (uint256);" contracts/DegenerusVault.sol && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - VAULT declares `withdraw(uint256)` + `poolOf(address) returns (uint256)` matching `AfKing.sol:318/:503` VERBATIM (selectors identical).
    - `recoverAfKingPool()` exists, is PERMISSIONLESS (no `onlyVaultOwner`, no gameOver gate), and calls `afKing.withdraw(afKing.poolOf(address(this)))`.
    - VAULT's open `receive()` (:497) is byte-unchanged (recovered ETH lands there).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>VAULT has the verbatim `withdraw`/`poolOf` interface adds + a permissionless `recoverAfKingPool()` that withdraws its own AfKing pool to VAULT reserves; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: sDGNRS — interface adds, receive() AF_KING relaxation, burnAtGameOver pool-recovery before the early-return (R4 item-4)</name>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (the inline `IAfKingSubscribe` interface :57-67 — single member; the `onlyGame` modifier :336; `receive() external payable onlyGame` :433; `burnAtGameOver() external onlyGame` :525, `bal = balanceOf[this]` :526, `if (bal == 0) return;` :527, the burn body :528-533; the `afKing` reference / ctor subscribe :384; how `AF_KING` / `ContractAddresses.AF_KING` is referenced for the `receive()` relaxation — re-grep)
    - contracts/AfKing.sol (`withdraw(uint256)` :318, `poolOf(address) returns (uint256)` :503 — VERBATIM; the caller-send `msg.sender.call{value}` :327 — sDGNRS-as-caller receives via its `receive()`, hence the relaxation; UNCHANGED in this plan)
    - 325-ATTEST-KEEP-POOL.md §B (L8/L10/L11/L12/L15) + POOL-05 resolution + the `receive()` accounting-safety note + 325-SPEC.md §1 R4 (item-4 portion) + §2 POOL-06 (accept-as-minor — NO second sweep)
    - PLAN-V48-AFKING-POOL-RECOVERY.md (sDGNRS auto-recover in `burnAtGameOver` + the `receive()` AF_KING relax)
  </read_first>
  <action>
    Per POOL-02/03/05 + SPEC §1 R4 (item-4 sStonk portion) + §2 POOL-06. APPLY-ORDER on sStonk: (A) interface adds FIRST — add `function withdraw(uint256) external;` + `function poolOf(address) external view returns (uint256);` to sStonk's inline `IAfKingSubscribe` (matching `AfKing.sol:318/:503` VERBATIM, POOL-05). (B) `receive()` relax: change `receive() external payable onlyGame` so it ALSO accepts `AF_KING` (re-grep the exact `AF_KING` / `ContractAddresses.AF_KING` reference style; relax the gate from GAME-only to GAME-or-AF_KING so AfKing's `withdraw` send-back lands). Accounting-safety: do NOT add any running-reserve counter SSTORE in `receive()` — reserves are read LIVE via `address(this).balance` (the submit/preview/resolve bases at :844/:598/:758), so an `AF_KING`-sourced credit is not mis-attributed/double-counted (POOL-04). (C) `burnAtGameOver` pool-recovery: fold `afKing.withdraw(afKing.poolOf(address(this)))` into `burnAtGameOver()` BEFORE the `if (bal == 0) return;` early-return (:527), so a zero-pool-TOKEN sDGNRS still recovers its ETH pool. NO standalone sDGNRS `withdraw` function (D-04 locked). POOL-06: do NOT add a second sweep in `handleFinalSweep` (accept-as-minor — a `depositFor(SDGNRS)` landing AFTER `burnAtGameOver` re-strands, donor-only loss, no protocol loss; record the documented known-minor in the SUMMARY). The `afKing.withdraw(...)` call composes on the item-2 RFALL `burnAtGameOver` payout-asset selection from 326-04 — re-grep `burnAtGameOver` to see 326-04's edits and place the pool-recover BEFORE the `bal == 0` early-return without disturbing the item-2 payout logic. DO NOT touch the item-2 `_submitGamblingClaimFrom` / `pullRedemptionReserve` surfaces. RE-GREP all sStonk anchors before editing (326-04 shifted lines).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "afKing.withdraw" contracts/StakedDegenerusStonk.sol && grep -n "AF_KING" contracts/StakedDegenerusStonk.sol | head && grep -n "function withdraw(uint256) external;\|function poolOf(address) external view returns (uint256);" contracts/StakedDegenerusStonk.sol && ! grep -qE "git_unchanged_AfKing" /dev/null; cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet contracts/AfKing.sol && echo "AFKING_UNCHANGED" || echo "AFKING_DIRTY_FAIL"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - sStonk declares `withdraw(uint256)` + `poolOf(address) returns (uint256)` matching `AfKing.sol:318/:503` VERBATIM.
    - `receive()` accepts BOTH `GAME` and `AF_KING` (grep confirms the relaxed gate references `AF_KING`); no running-reserve counter SSTORE is added in `receive()` (accounting-safe, POOL-04).
    - `burnAtGameOver` calls `afKing.withdraw(afKing.poolOf(address(this)))` BEFORE the `if (bal == 0) return;` early-return (read the function order to confirm).
    - NO standalone sDGNRS `withdraw` function added (D-04); NO second sweep in `handleFinalSweep` (POOL-06 accept-as-minor; recorded in SUMMARY).
    - `git diff --quiet contracts/AfKing.sol` → AfKing.sol is byte-UNCHANGED (item 4 leaves it untouched).
    - The item-2 `_submitGamblingClaimFrom` / `pullRedemptionReserve` edits from 326-04 are preserved (not reverted/disturbed).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>sStonk has the verbatim interface adds, a GAME-or-AF_KING `receive()` (no new counter), and `burnAtGameOver` recovers the AfKing pool before the `bal==0` early-return; AfKing.sol unchanged; no second sweep; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anyone → recoverAfKingPool() | the recovery is permissionless; the threat is whether a caller can redirect or steal the recovered ETH. |
| AfKing → sDGNRS receive() | relaxing the gate widens who may send ETH to sDGNRS; the threat is mis-attribution / a hostile credit. |
| depositFor ingress vs recovery timing | a post-gameOver `depositFor(SDGNRS)` re-strands; the threat is a protocol-level loss vs a donor-only loss. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-05-01 | Elevation of Privilege / Theft | permissionless `recoverAfKingPool()` abuse | mitigate | The function ONLY moves `afKing.poolOf(address(this))` (VAULT's OWN pool) to VAULT's own `receive()` — `afKing.withdraw` sends to the CALLER (AfKing.sol:327 sends to `msg.sender`, which is VAULT, not the external trigger). An external trigger cannot redirect the ETH; it only forces VAULT to recover its own funds. No theft surface. Permissionless is safe by construction. |
| T-326-05-02 | Tampering | `receive()` AF_KING relaxation mis-attributes a hostile credit | mitigate | sStonk reserves are read LIVE via `address(this).balance` (no running-counter SSTORE in `receive()`); an `AF_KING`-sourced credit lands in the balance and flows out through the deterministic burn / `handleFinalSweep`, never double-counted/bypassed (POOL-04, proven at TST). The relaxation only ADDS `AF_KING` to the allow-list — no other sender gains access. |
| T-326-05-03 | Denial of Service | zero-pool-TOKEN sDGNRS strands its ETH pool at gameOver | mitigate | The `afKing.withdraw` is placed BEFORE the `balanceOf(this)==0` early-return in `burnAtGameOver`, so a sDGNRS holding no DGNRS token still recovers its ETH pool. |
| T-326-05-04 | Repudiation | post-gameOver `depositFor(SDGNRS)` re-strands (POOL-06) | accept | ACCEPT-AS-MINOR (D-04): a `depositFor(SDGNRS)` landing AFTER `burnAtGameOver` re-strands (sDGNRS has no later trigger, no standalone withdraw by design) — but it is an adversarial/pointless self-donation harming only the donor; VAULT is unaffected (anytime `recoverAfKingPool()`). No protocol loss. No second sweep added. |
| T-326-05-05 | Tampering | item 4 silently mutates AfKing.sol | mitigate | `git diff --quiet contracts/AfKing.sol` is an explicit acceptance gate — AfKing.sol stays byte-unchanged for item 4 (POOL-05); the only AfKing edit in the milestone is item 3's `sweep`→`autoBuy` rename (plan 326-06). |
| T-326-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over both files.
- VAULT: verbatim `withdraw`/`poolOf` adds + permissionless `recoverAfKingPool()`.
- sStonk: verbatim adds + GAME-or-AF_KING `receive()` (no new counter) + `burnAtGameOver` pool-recover before the `bal==0` early-return.
- `git diff --quiet contracts/AfKing.sol` (AfKing.sol byte-unchanged for item 4).
- No standalone sDGNRS withdraw; no second sweep (POOL-06 accept-as-minor recorded).
- The item-2 RFALL sStonk edits from 326-04 are preserved.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC4 (POOL-01/02/03/05) satisfied: VAULT permissionless `recoverAfKingPool()` → `afKing.withdraw(afKing.poolOf(address(this)))` (ETH to VAULT reserves, no gameOver gate); sDGNRS `receive()` accepts `AF_KING`; sDGNRS auto-recovers inside `burnAtGameOver()` before the `balanceOf(this)==0` early-return (no standalone withdraw); `AfKing.sol` UNCHANGED with the verbatim `withdraw`/`poolOf` interface adds.
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-05-SUMMARY.md` when done. Record the VAULT recovery wrapper, the sStonk `receive()` relaxation + `burnAtGameOver` placement, the verbatim interface adds, the AfKing.sol-unchanged confirmation, and the POOL-06 accept-as-minor documented known-minor. Do NOT git commit.
</output>
