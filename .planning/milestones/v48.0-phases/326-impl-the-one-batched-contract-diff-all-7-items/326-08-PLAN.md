---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 08
type: execute
wave: 6
depends_on: ["326-01", "326-02", "326-03", "326-04", "326-05", "326-06", "326-07"]
files_modified: []
autonomous: false
requirements: [BATCH-02]
must_haves:
  truths:
    - "The full batched diff compiles (forge build succeeds, 0 errors) with the reconciled shared signatures intact: ONE pullRedemptionReserve with the pure-ETH-OR-pure-stETH coverage branch; the renamed crank entrypoints + bytes32(\"DGNRS\") affiliate wiring; ONE sellFarFutureTickets + _removeFarFutureTickets; the sStonk receive()/burnAtGameOver/interface adds; the VAULT recoverAfKingPool()/gameSellFarFutureTickets."
    - "The existing test suite shows NO NEW breakage vs the v47.0-closure baseline (documented pre-existing failures are not regressions; do not chase them)."
    - "The cross-item reconciliation joint-checks pass: claimablePool == sum(claimableWinnings) stays balanced across RFALL/SWAP; no unchecked claimable subtraction survives in the redemption/swap paths; the SWAP no-arb still holds at the jitter-band CEILING (+4.5pp @d6); AfKing.sol is byte-unchanged except the item-3 sweep→autoBuy rename; the swap-pop does NOT reproduce H-CANCEL-SWAP-MISS; crank/sweep/do-work purged; the SWAP-03 jitter reads the SETTLED past VRF word."
    - "The batched contracts/*.sol diff is HELD at the contract-commit boundary and presented for ONE explicit USER hand-review; nothing is committed without that approval."
  artifacts: []
  key_links: []
---

<objective>
Verify the complete batched diff (all 7 items, plans 326-01..07) and HOLD it at the contract-commit boundary for the single USER hand-review. This plan compiles the whole tree, runs the existing test suite to confirm no NEW breakage vs the v47.0-closure baseline, runs the cross-item reconciliation joint-checks (the BATCH-01 surfaces settled in the 325 SPEC), and then presents the ONE batched `contracts/*.sol` diff for explicit user approval. It does NOT commit — the user reviews the diff first.

Purpose: ROADMAP SC7 (BATCH-02) — the diff is reconciled per the SPEC's settled shared signatures, compiles + passes the existing suite with no NEW breakage, and is HELD at the contract-commit boundary for ONE explicit USER hand-review. The single approval gate for the whole milestone's contract surface.
Output: A verified, compiling, non-regressing batched diff + the user-approval gate.

NON-NEGOTIABLE: This is the ONLY place the batched diff is presented for approval, and it is `autonomous: false`. NO task may state or imply the contracts are "pre-approved." The executor APPLIES nothing new here (all edits landed in 326-01..07); it VERIFIES + PRESENTS. Commit only AFTER the user explicitly approves the diff (the commit-guard hook blocks commits while `.sol` is dirty; the approved commit uses the sanctioned `CONTRACTS_COMMIT_APPROVED=1` bypass). Scope is IMPL-only: confirm it compiles + no NEW test breakage — do NOT author new behavioral/gas/repro tests (Phase 327) and do NOT run the formal RNG-freeze sweep or the no-arb empirical proof (Phase 327/328; may NOTE the freeze argument + the no-arb re-derivation hold).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-01-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-02-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-03-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-04-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-05-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-06-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-07-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md

<baseline_note>
The v47.0-closure baseline is HEAD `da5c9d50` (the live `contracts/` tree was byte-identical to it before this phase — `git diff --name-only da5c9d50 HEAD -- 'contracts/*.sol'` was EMPTY). The documented suite baseline carried forward across v45→v47 has ~44 pre-existing failures (v45/v46-documented, unrelated). A NEW failure = a count above that baseline OR a previously-passing test now failing. Do NOT chase the pre-existing baseline failures. Re-establish the EXACT current baseline by reading the most recent prior-milestone TST/closure suite count (the v47.0 closure ran the suite) and comparing — the goal is ZERO NEW failures attributable to the v48 diff. (The HERO byte-exact table values are TST-handoff placeholders per 326-03; some Degenerette payout tests may be expected to differ until Phase 327's `derive_5_tables.py` byte-reproduce — classify those as KNOWN-TST-DEFERRED, not v48 regressions, and record them.)
</baseline_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile the full batched diff + run the cross-item reconciliation joint-checks (BATCH-01 surfaces)</name>
  <read_first>
    - All 7 SUMMARYs (326-01..07) — collect the per-plan dispositions: the PFIX divisor, the BTOMB one-shot latch/checked-add, the HERO TST-handoff placeholders, the RFALL coverage legs, the POOL AfKing-unchanged confirmation + POOL-06 known-minor, the KEEP rename map + purge counts + the bytes32("DGNRS") site, the SWAP entrypoint + swap-pop + the R6 DroneManager/OPEN-E flags
    - 325-SPEC.md §1 R1-R6 + §0 C1-C8 (the reconciliation surfaces to joint-check) + §3 (the per-item IMPL blueprint + edit-order map)
  </read_first>
  <action>
    Compile + reconcile. (1) `forge build` over the whole tree — it MUST succeed (0 errors). Any error means a cross-plan reconciliation gap (a stale identifier, an arity mismatch, a dangling rename, a duplicated signature) — fix it (this plan owns final cross-plan reconciliation for BATCH-01). (2) Run the joint-checks (grep + targeted read), recording each result PASS/FAIL with evidence in the SUMMARY:
      - R1 (RFALL coverage): exactly ONE `pullRedemptionReserve` on `DegenerusGame.sol`, SDGNRS-gated, with the pure-ETH-OR-pure-stETH coverage branch and CHECKED (no `unchecked`) debit; the single `pendingRedemptionEthValue` tracker on sStonk (no second slot).
      - R2 (KEEP): `crankBets`/`crankBoxes`/`_crankResolveBet`/`_crankOpenBox`/`enqueueBoxForCrank` all gone (renamed auto*); `bytes32("DGNRS")` wired at the `_batchPurchaseUnit` self-call; no `bytes32(0), payKind`; `bytes32("VAULT")` NOT wired.
      - R3 (SWAP): exactly ONE `sellFarFutureTickets` + ONE `_removeFarFutureTickets`; the inline `claimableWinnings[SDGNRS]` debit is CHECKED + floored at ≥1 ETH; NO `pendingRedemptionEthValue` term in the swap; NO daily-cap state; the jitter seed reads `rngWordByDay[currentDay-1]` (NOT `rngWordCurrent`); the swap stays `rngLocked()`-gated.
      - R4 (sStonk co-edit): the item-2 `_submitGamblingClaimFrom` segregation AND the item-4 `receive()` AF_KING relax + `burnAtGameOver` pool-recover + interface adds all coexist coherently; the single tracker is intact.
      - R5 (VAULT co-edit): `recoverAfKingPool()` (permissionless) + `gameSellFarFutureTickets onlyVaultOwner` + the `withdraw`/`poolOf` + `sellFarFutureTickets` interface entries all present; no conflicting signature.
      - Balance invariant: every `claimablePool` mutation has a matching `claimableWinnings` mutation of equal magnitude OR a real ETH in/out (spot-check `pullRedemptionReserve`, the SWAP inline debit + the aggregated ticket mint, the BTOMB flood which touches vaultAllowance NOT claimablePool) — `claimablePool == sum(claimableWinnings)` stays balanced across RFALL/SWAP.
      - C-checks: PFIX divisor `400 * 1 ether` (C1); the affiliate wiring is at the game self-call wrapper, NOT AfKing (C2); BTOMB reuses the checked `vaultEscrow`-style path + one-shot latch, NOT the `:370` reclassification (C4); HERO `FT_HERO_SHIFT` decode + `heroQuadrant >= 4` revert KEPT, the multiplier apparatus net-deleted (C5/H1-H5); `dailyHeroWagers`/`_rollHeroSymbol` untouched (C6 no-leak); the SWAP `_runEarlyBirdLootboxJackpot` reference is correct (C7); the unit basis is `priceForLevel(currentLevel)` not /4 (C8).
      - Purge: `grep -ci 'crank\|do-work'` across `AfKing.sol` + `DegenerusGame.sol` = 0; `grep -ic 'sweep'` (allowing only any deliberately-renamed remnant that no longer means the keeper action — but the keeper `sweep`/`sweepProgress`/`SweepAborted` MUST be renamed) = 0 keeper-sweep hits.
      - AfKing scope: `git diff contracts/AfKing.sol` shows ONLY the item-3 `sweep`→`autoBuy` rename hunks (KEEP-01) — NO item-4 change (POOL-05 left AfKing untouched).
      - No-arb (note, not re-prove): confirm no `fractionBps`/fraction-band/cash-band widening landed vs the SPEC curve (the empirical proof is SWAP-08 at TST).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 500 forge build 2>&1 | tail -3 && timeout 500 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " total build errors (MUST be 0)"; echo "--- purge ---"; grep -ci 'crank\|do-work' contracts/AfKing.sol contracts/DegenerusGame.sol; echo "--- affiliate ---"; grep -c 'bytes32("DGNRS")' contracts/DegenerusGame.sol; echo "--- swap entrypoints ---"; grep -c "function sellFarFutureTickets\|function _removeFarFutureTickets" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` succeeds with ZERO errors over the whole tree.
    - All BATCH-01 joint-checks (R1-R5 + the balance invariant + the C-checks + the purge + the AfKing-scope check) PASS with grep/read evidence recorded in the SUMMARY.
    - Exactly ONE `pullRedemptionReserve`, ONE `sellFarFutureTickets`, ONE `_removeFarFutureTickets`; `bytes32("DGNRS")` wired; no `bytes32(0), payKind`; no `crank`/`do-work`/keeper-`sweep` survivor.
    - `git diff contracts/AfKing.sol` is the item-3 rename ONLY (no item-4 change).
    - No `fractionBps`/band widening vs the SPEC no-arb curve.
  </acceptance_criteria>
  <done>The full batched diff compiles; every cross-item reconciliation joint-check passes with recorded evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run the existing test suite — confirm NO NEW breakage vs the v47.0 baseline</name>
  <read_first>
    - <baseline_note> above (the ~44 documented pre-existing failures + the HERO TST-handoff placeholders)
    - The most recent prior-milestone TST/closure suite count (the v47.0 closure ran the suite — re-establish the exact pre-v48 baseline before classifying)
  </read_first>
  <action>
    Run the existing test suite (`forge test`, or the project's standard invocation — check `foundry.toml` for the profile). Capture the pass/fail count. Re-establish the exact pre-v48 baseline (the v47.0-closure suite count) and compare. Classify EVERY failure: (a) a documented pre-existing baseline failure (unrelated to v48 — do NOT chase), (b) a KNOWN-TST-DEFERRED Degenerette payout-table failure caused by the HERO TST-handoff placeholder constants (326-03; these are byte-reproduced by `derive_5_tables.py` at Phase 327 — record them as deferred-to-TST, NOT a v48 contract regression), or (c) a NEW failure attributable to the v48 diff. For any class-(c) NEW failure, root-cause it and fix the CONTRACT (not the test — tests are Phase 327; only fix a contract bug the test legitimately catches). Goal: ZERO class-(c) NEW failures. Note (do NOT execute): the SWAP-03 RNG-freeze formal re-verification + the SWAP-08 no-arb empirical proof + the PFIX/RFALL/POOL/BTOMB/HERO behavioral proofs are Phase 327/328 — you MAY note the freeze argument (settled word + rngLocked gate, no new mutable SLOAD) and the no-arb re-derivation (+4.5pp @d6) hold per the SPEC, but the formal proofs are out of scope here.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 600 forge test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The suite runs to completion; the pass/fail count is captured and compared to the re-established v47.0 baseline.
    - Every failure is classified (a) pre-existing-baseline, (b) KNOWN-TST-DEFERRED (HERO placeholder tables), or (c) NEW-v48; ZERO class-(c) failures (any is contract-root-caused + fixed).
    - The HERO placeholder-table failures are recorded as deferred-to-Phase-327 (`derive_5_tables.py` byte-reproduce), not v48 regressions.
  </acceptance_criteria>
  <done>The suite shows no NEW (class-c) breakage vs the v47.0 baseline; all failures classified; the diff is correctness-clean at the IMPL level (HERO table values pending the TST byte-reproduce).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: USER hand-review of the ONE batched contract diff (the single approval gate)</name>
  <what-built>
    The complete v48.0 batched `contracts/*.sol` diff — all 7 work items reconciled per the 325 SPEC's settled shared signatures (R1-R6):
    1. PFIX — `_presaleBoxDgnrsReward` divisor `1_000 → 400` (`base = poolStart/40`); tier shape preserved; closing-sweep clamp held. (F-47-01 fixed.)
    2. RFALL — `pullRedemptionReserve` pure-ETH-OR-pure-stETH coverage branch + `_submitGamblingClaimFrom` segregation; fail-closed, donation-robust; claim-asset-matched payout; single `pendingRedemptionEthValue`. (F-47-02 fixed.)
    3. KEEP — `AfKing.sweep`→`autoBuy`; `crankBets`→`autoResolve`, `crankBoxes`→`autoOpen` + helper renames; crank/sweep/do-work purged; `bytes32("DGNRS")` affiliate wiring (primary SDGNRS / secondary VAULT); `creditFlip`/`BOUNTY_ETH_TARGET` kept.
    4. POOL — VAULT permissionless `recoverAfKingPool()`; sDGNRS `receive()` AF_KING relax + `burnAtGameOver` pool-recover before the `bal==0` early-return; `IAfKing` interface adds; AfKing.sol unchanged for item 4.
    5. BTOMB — `gameOver()` one-shot 1e36-wei `vaultAllowance` flood (GAME-gated, checked add/cap, one-shot latch); `totalSupply()` untouched.
    6. HERO — `_countMatches`→`_score(...) ∈ {0..9}` (`S = A + 2·H`); standalone multiplier net-deleted; S=8/S=9 separate-`uint256` packing; thresholds re-mapped to `S≥7`; jackpot relabel `S=9 ≡ M=8`. (Byte-exact table VALUES are TST-handoff placeholders for `derive_5_tables.py`.)
    7. SWAP — `sellFarFutureTickets(...)` (operator-honor, rngLocked-gated, d-curve + settled-word jitter, ticket floor, inline fail-closed claimable debit to ≥1 ETH, no cap) + `_removeFarFutureTickets` swap-pop; VAULT `gameSellFarFutureTickets onlyVaultOwner` wrapper. (DroneManager cross-repo re-sync flagged out-of-diff.)
    Verified: `forge build` succeeds; all BATCH-01 reconciliation joint-checks pass; the existing suite shows no NEW breakage vs the v47.0 baseline (HERO placeholder-table failures are deferred to the Phase-327 byte-reproduce).
  </what-built>
  <how-to-verify>
    1. Review the full batched diff: `git diff -- contracts/` (read every changed file; this is the single hand-review for the whole milestone's contract surface).
    2. Confirm the reconciled surfaces match the SPEC: ONE `pullRedemptionReserve` with the pure-ETH-OR-pure-stETH coverage branch, CHECKED, single tracker (R1/RFALL-04); the renamed crank entrypoints + `bytes32("DGNRS")` at the `_batchPurchaseUnit` self-call, NOT `bytes32("VAULT")` (R2/KEEP-04); ONE `sellFarFutureTickets` + `_removeFarFutureTickets`, jitter from `rngWordByDay[currentDay-1]`, no daily cap, no `pendingRedemptionEthValue` term (R3/SWAP); the sStonk `receive()` AF_KING relax + `burnAtGameOver` pool-recover (R4/POOL); VAULT `recoverAfKingPool()` + `gameSellFarFutureTickets` (R5); the PFIX `400 * 1 ether` divisor (C1); the BTOMB one-shot checked flood (C4); the HERO `_score` + net-deleted multiplier + TST-placeholder tables (HERO-04); AfKing.sol = item-3 rename only (POOL-05).
    3. Confirm the build/test evidence in the 326-08 SUMMARY (forge build clean; no NEW failures vs baseline; HERO placeholders classified deferred).
    4. If approved, the diff may be committed (a single sanctioned contract commit using the approved bypass — `CONTRACTS_COMMIT_APPROVED=1`); the `.planning/` docs (already authored) are force-added as needed. If issues are found, describe them for a gap-closure pass.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched contract commit, or describe the issues to fix (gap-closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| applied .sol diff → committed source | the contract-commit boundary; ONLY crossed after explicit user hand-review. |
| cross-item reconciliation | the 7 plans' edits to the shared files (`DegenerusGame.sol`, `StakedDegenerusStonk.sol`, `DegenerusVault.sol`) must compose into one coherent diff (BATCH-01). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-08-01 | Tampering | a cross-item reconciliation gap ships silently | mitigate | `forge build` MUST be zero-error + the R1-R6/C/balance/purge/AfKing-scope joint-checks run with grep+read evidence before the gate. |
| T-326-08-02 | Repudiation | a contract committed without review | mitigate | Task 3 is `autonomous:false` blocking-human; NO pre-approval; commit only after explicit "approved"; the commit-guard hook blocks commits while `.sol` is dirty (the `CONTRACTS_COMMIT_APPROVED=1` bypass is used only post-approval). |
| T-326-08-03 | Tampering | a v48 regression masquerades as a baseline / TST-deferred failure | mitigate | Every test failure is classified (a) pre-existing-baseline, (b) KNOWN-TST-DEFERRED (HERO placeholder tables), or (c) NEW-v48; only (a)+(b) are tolerated; any (c) is contract-root-caused + fixed. The HERO placeholders are explicitly bounded to Degenerette payout tests. |
| T-326-08-04 | Tampering | RNG-freeze / no-arb breakage slips past IMPL | accept | The SWAP-03 freeze argument (settled `rngWordByDay[currentDay-1]` + `rngLocked()` gate, no new mutable SLOAD) and the SWAP-08 no-arb re-derivation (+4.5pp @d6) are NOTED to hold per the SPEC; the FORMAL re-verification + empirical proof are Phase 327/328 (out of IMPL scope). The IMPL acceptance gate (326-07) already asserts the seed + the un-widened bands. |
| T-326-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this milestone (audit-remediation, no new deps). No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build`: zero errors over the whole tree.
- All BATCH-01 joint-checks (R1-R6 + balance invariant + C-checks + purge + AfKing-scope) PASS with recorded evidence.
- `forge test`: no NEW (class-c) failures vs the v47.0 baseline; all failures classified (HERO placeholders = TST-deferred).
- Task 3 reached as a blocking-human gate; nothing committed until explicit user approval.
</verification>

<success_criteria>
- ROADMAP SC7 (BATCH-02) satisfied: the diff is reconciled per the SPEC's settled shared signatures, compiles + passes the existing suite with no NEW breakage, and is HELD at the contract-commit boundary for the single explicit USER hand-review (no pre-approval).
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-08-SUMMARY.md` when done. Record: the forge build result, the full BATCH-01 joint-check table (with grep/read evidence), the test pass/fail count vs the re-established v47.0 baseline + the failure classification (incl. the HERO TST-deferred placeholders), and the user-approval outcome. The single batched contract commit happens ONLY after explicit user approval (this plan does not pre-commit).
</output>
