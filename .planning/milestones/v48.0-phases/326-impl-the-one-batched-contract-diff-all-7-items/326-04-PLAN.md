---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 04
type: execute
wave: 2
depends_on: ["326-01", "326-02", "326-03"]
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/StakedDegenerusStonk.sol
autonomous: true
requirements: [RFALL-01, RFALL-02, RFALL-03]
must_haves:
  truths:
    - "When a redemption submit's 175% reservation cannot be covered by the ETH side (claimableWinnings[SDGNRS] / game ETH), the reservation segregates in PURE stETH instead — no ETH+stETH mix; if neither pure leg covers, the submit reverts fail-closed."
    - "Coverage is checked against the SAME asset basis the submit base was inflated by, so a stETH donation / selfdestruct force-feed that inflates stethBal is coverable by the pure-stETH leg and cannot brick submit (donation-robust)."
    - "Claim-time payout asset matches the reserved asset (stETH-reserved → stETH-paid) via the existing steth.transfer paths (:622/:932); the single pendingRedemptionEthValue tracker is reused (no second stETH-denominated reservation slot, D-06)."
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "pullRedemptionReserve with the pure-ETH-OR-pure-stETH coverage branch (R1, D-06), fail-closed, donation-robust"
      contains: "pullRedemptionReserve"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "_submitGamblingClaimFrom maxIncrement reservation segregated pure-ETH-OR-pure-stETH (R4 item-2), claim-asset-matched payout"
      contains: "_submitGamblingClaimFrom"
  key_links:
    - from: "contracts/StakedDegenerusStonk.sol _submitGamblingClaimFrom maxIncrement"
      to: "contracts/DegenerusGame.sol pullRedemptionReserve coverage branch"
      via: "the submit-side reservation pull selects the same asset the coverage branch reserves"
      pattern: "pullRedemptionReserve"
---

<objective>
Item 2 (RFALL) — fix F-47-02, the mid-game ETH-depletion + stETH-donation redemption-submit brick. The submit-time 175% reservation (`maxIncrement` → `pullRedemptionReserve`) is ETH-only and fail-closed today: if `claimableWinnings[SDGNRS]` (the ETH side) cannot cover the 175% MAX, the burn reverts with NO fallback to sDGNRS's stETH balance — and a stETH donation that inflates the submit base makes the required reservation un-coverable by the ETH leg. The locked fix extends the claim-side `_payEth` ETH→stETH pattern to the SUBMIT-side reservation: segregate the reservation as PURE ETH OR PURE stETH (no mix), fall back to pure stETH when ETH can't cover, and revert fail-closed if neither pure leg covers — checking coverage against the SAME asset basis the submit base is inflated by (donation-robust). The single `pendingRedemptionEthValue` tracker is reused (D-06).

Purpose: ROADMAP SC2 (RFALL-01/02/03) — the 175% reservation segregates pure-ETH OR pure-stETH (no mix), falls back to pure-stETH when ETH can't cover, REVERTS fail-closed if neither pure leg covers; coverage checked against the asset basis the base is inflated by (donation-robust); claim-time payout asset matches the reserved asset. This is the FIRST `DegenerusGame.sol` edit of the batched diff (R1 apply-order: R1 → R2 (KEEP, 326-06) → R3 (SWAP, 326-07)) and the item-2 portion of the sStonk co-edit (R4); it runs in Wave 2, after the three isolated Wave-1 plans, because it opens both shared files.
Output: The `pullRedemptionReserve` coverage branch (R1) + the `_submitGamblingClaimFrom` maxIncrement segregation + claim-asset-matched payout (R4 item-2).

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 326-08. NOTHING pre-approved. APPLY-ORDER DISCIPLINE: this plan opens `DegenerusGame.sol` (R1, the lowest line range ~:1888) and `StakedDegenerusStonk.sol` (the item-2 `_submitGamblingClaimFrom` segregation at ~:880-887). It MUST NOT touch the sStonk `receive()` / `burnAtGameOver` / `IAfKingSubscribe` interface adds (those are item 4 / POOL, plan 326-05, which runs in Wave 3 AFTER this plan precisely because both edit sStonk) and MUST NOT touch the renamed crank entrypoints / affiliate wiring (item 3 / KEEP, plan 326-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md
@.planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md

<interfaces>
<!-- Key constructs the executor needs. Extracted from live source at da5c9d50. Use directly. -->

contracts/DegenerusGame.sol :1888-1899 (the F-47-02 gap — CHECKED claimable-only debit, no stETH leg):
  function pullRedemptionReserve(uint256 amount) external
    if (msg.sender != ContractAddresses.SDGNRS) revert E();   // :1889 SDGNRS-gated
    if (amount == 0) return;                                   // :1890
    claimableWinnings[ContractAddresses.SDGNRS] -= amount;     // :1893 CHECKED debit
    claimablePool -= uint128(amount);                          // :1894
    (bool ok, ) = payable(ContractAddresses.SDGNRS).call{value: amount}("");  // :1897 CEI ETH move
    if (!ok) revert E();                                       // :1898

contracts/StakedDegenerusStonk.sol :844-848 (the 4-term submit base — what the reservation inflates by):
  uint256 ethBal = address(this).balance;                                     // :844
  uint256 stethBal = steth.balanceOf(address(this));                          // :845
  uint256 claimableEth = _claimableWinnings();                                // :846
  uint256 totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue; // :847

contracts/StakedDegenerusStonk.sol :880-887 (the MAX 175% reservation increment + pull):
  uint256 maxIncrement = (newBaseWei * MAX_ROLL) / 100 - (prevBaseWei * MAX_ROLL) / 100;  // :883
  if (maxIncrement != 0) { game.pullRedemptionReserve(maxIncrement); }                    // :884-886
  pendingRedemptionEthValue += maxIncrement;                                              // :887

contracts/StakedDegenerusStonk.sol :622 / :932 (the two EXISTING stETH-transfer claim paths — reuse, no new selector):
  :622  if (!steth.transfer(beneficiary, stethOut)) revert TransferFailed();   // game-over burn payout
  :932  if (!steth.transfer(player, stethOut)) revert TransferFailed();        // _payEth ETH→stETH fallback (claim)

contracts/StakedDegenerusStonk.sol :263  uint256 public pendingRedemptionEthValue;  // the SINGLE tracker (D-06)
</interfaces>

<grep_protocol>
Baseline `da5c9d50` (live tree byte-identical). The R1 site (`pullRedemptionReserve` :1888) was re-grepped at plan time. Because plans 01/02/03 (Wave 1) edit DIFFERENT files, the `DegenerusGame.sol` / `StakedDegenerusStonk.sol` line numbers are still at baseline when this plan runs — but RE-GREP `function pullRedemptionReserve` / `_submitGamblingClaimFrom` / `maxIncrement` before editing anyway (project rule: never trust a raw line number). The 325-ATTEST-PFIX-RFALL §B table (R1-R6) + the exact fix-target bodies + carried correction C3 are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the pure-ETH-OR-pure-stETH coverage branch to DegenerusGame.pullRedemptionReserve (R1, D-06)</name>
  <read_first>
    - contracts/DegenerusGame.sol (`pullRedemptionReserve` :1888-1899 — full body in the <interfaces> block; the SDGNRS-gate :1889; the `amount == 0` early-return :1890; how the game references stETH / sDGNRS's stETH balance — re-grep for `steth` / `IStETH` / the stETH token reference the game uses; how the game tracks its ETH side `claimableWinnings[SDGNRS]` + `claimablePool`)
    - 325-ATTEST-PFIX-RFALL.md §B (R3 — the gap CONFIRMED PRESENT; the exact :1888-1899 body) + 325-SPEC.md §1 R1 (the coverage-branch spec, 3 steps) + §2 RFALL-04
    - PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md (the locked pure-ETH-OR-pure-stETH segregation + REDEEM-04 reference)
  </read_first>
  <action>
    Per RFALL-01/02/03 + SPEC §1 R1 + C3. Compose the coverage branch ON TOP of the v47 form (keep the unchanged selector `function pullRedemptionReserve(uint256 amount) external`, the SDGNRS gate `if (msg.sender != ContractAddresses.SDGNRS) revert E();`, and the `if (amount == 0) return;` early-return). BEFORE the checked claimable debit, SELECT the reservation asset against the SAME basis the submit base was inflated by:
    (1) ETH leg (as today): if the ETH side (`claimableWinnings[SDGNRS]` / the game's ETH) covers `amount`, do the existing CHECKED `claimableWinnings[SDGNRS] -= amount; claimablePool -= uint128(amount);` then the CEI `payable(SDGNRS).call{value: amount}("")` move.
    (2) stETH leg (NEW fallback): if the ETH side cannot cover, segregate the reservation in PURE stETH — sDGNRS already holds stETH; the claim then pays stETH via the existing :622/:932 transfer sites (no new external-call selector). The coverage check is against the stETH basis. The PHYSICAL move for the stETH leg follows the design doc / R1 step 2 (the stETH stays on sDGNRS or transfers to the game side as the reservation; mirror how the ETH leg moves to SDGNRS, but in stETH). Keep the single `pendingRedemptionEthValue` semantics on the sStonk side (D-06) — this game-side function reserves; it does NOT add a second tracker.
    (3) Neither covers → revert fail-closed (`revert E();` — the locked "neither covers" case is not realistic).
    DONATION-ROBUSTNESS: the coverage check for each leg is against the SAME asset basis the submit base (`ethBal + stethBal + claimableEth`) is inflated by — do NOT reintroduce a claimable-ETH-only chokepoint that a stETH force-feed could brick. CHECKED arithmetic throughout (no `unchecked` debit). RE-GREP the live `pullRedemptionReserve` line before editing.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function pullRedemptionReserve" contracts/DegenerusGame.sol && grep -ic "steth" contracts/DegenerusGame.sol | tr -d "\n"; echo " stETH refs (>0 means the stETH leg wired)" && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `pullRedemptionReserve` keeps its selector, the SDGNRS gate, and the `amount == 0` early-return unchanged.
    - There are exactly TWO coverage legs (ETH then stETH) and a fail-closed `revert` if neither covers — no ETH+stETH mix in a single reservation (read the branch structure to confirm a pure-one-or-the-other selection).
    - The debit arithmetic is CHECKED (no `unchecked` block introduced in this function).
    - No second stETH-denominated reservation slot is introduced (the single `pendingRedemptionEthValue` on sStonk is reused — D-06).
    - The coverage check is against the asset basis the submit base is inflated by (donation-robust; the stETH leg is reachable when a stETH force-feed makes the ETH leg insufficient).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`pullRedemptionReserve` selects pure-ETH-OR-pure-stETH coverage, falls back to pure stETH, reverts if neither covers, checks against the inflating basis (donation-robust), keeps the single tracker; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Segregate the _submitGamblingClaimFrom maxIncrement reservation + asset-match the claim payout (R4 item-2)</name>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (the 4-term submit base :844-848; the maxIncrement reservation pull :880-887 — `game.pullRedemptionReserve(maxIncrement)` :885, `pendingRedemptionEthValue += maxIncrement` :887; the existing claim-side stETH-transfer paths :622 (game-over burn) + :932 (`_payEth` ETH→stETH fallback, :918-933); the single tracker :263; `_submitGamblingClaimFrom` opens :810)
    - 325-ATTEST-PFIX-RFALL.md §B (R1/R2/R4/R6 + the exact :847 / :883-887 bodies + the Key RFALL finding) + 325-SPEC.md §1 R4 (item-2 portion) + §2 RFALL-04
    - PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md (the submit-side reservation + claim-asset-matched payout)
  </read_first>
  <action>
    Per RFALL-01/02/03 + SPEC §1 R4 (item-2 portion ONLY — NOT the item-4 `receive()`/`burnAtGameOver`/interface adds, which are plan 326-05). The 175% `maxIncrement` reservation pull (:880-887) gains the pure-ETH-OR-pure-stETH segregation mirroring R1's coverage branch: when the game-side ETH cannot cover the `maxIncrement` reservation, the submit-side records the reservation in the pure-stETH leg instead of reverting (try ETH; fall back to pure stETH; revert if neither). KEEP the 4-term submit base (:847 `ethBal + stethBal + claimableEth - pendingRedemptionEthValue`) and the single `pendingRedemptionEthValue` (:263) UNCHANGED (D-06). CLAIM-TIME ASSET MATCH: the payout asset selection at claim must match the reserved asset — a stETH-reserved redemption pays stETH via the existing :622/:932 `steth.transfer` sites (no new external-call selector); an ETH-reserved one pays ETH. Re-grep the claim-side payout dispatch and ensure the reserved-asset → paid-asset correspondence holds (the existing `_payEth` already does ETH-insufficient→stETH at claim; this plan ensures the submit-side reservation and the claim-side payout agree on the asset). DO NOT touch the sStonk `receive()` (:433), `burnAtGameOver` (:525), or the `IAfKingSubscribe` interface (:57) — those are item 4 (plan 326-05). RE-GREP `_submitGamblingClaimFrom` / `maxIncrement` / `pullRedemptionReserve` before editing.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "_submitGamblingClaimFrom\|maxIncrement\|pullRedemptionReserve" contracts/StakedDegenerusStonk.sol | head && grep -c "pendingRedemptionEthValue" contracts/StakedDegenerusStonk.sol | tr -d "\n"; echo " pendingRedemptionEthValue refs (single tracker kept)" && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The `maxIncrement` reservation pull selects pure-ETH-OR-pure-stETH (try ETH; fall back to pure stETH; revert if neither) — mirroring R1.
    - The 4-term submit base (:847) and the single `pendingRedemptionEthValue` (:263) are UNCHANGED (`git diff` shows no edit to the base expression or the tracker decl; D-06 — no second stETH-denominated slot).
    - Claim-time payout asset matches the reserved asset (stETH-reserved → stETH-paid via :622/:932; ETH-reserved → ETH-paid).
    - The sStonk `receive()` (:433), `burnAtGameOver` (:525), and `IAfKingSubscribe` interface (:57) are byte-UNCHANGED (those belong to plan 326-05; `git diff` shows no hunk there).
    - `forge build` reports 0 errors over both files.
  </acceptance_criteria>
  <done>The submit-side reservation segregates pure-ETH-OR-pure-stETH; the 4-term base + single tracker are unchanged; claim payout asset matches the reserved asset; the item-4 sStonk surfaces are untouched; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| attacker → sDGNRS stETH balance | anyone can force-feed stETH via transfer / a self-destruct ETH donation; this inflates the submit base and must NOT brick a legitimate redemption submit. |
| redemption desk solvency | the 175% reservation segregates real backing per submit — the fallback must reserve REAL stETH, not a phantom credit, or the desk over-promises. |
| submit asset ↔ claim asset | a reservation in one asset must be paid in that asset; a mismatch is a value-conservation break. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-04-01 | Denial of Service | stETH-donation / ETH-depletion bricks redemption submit (F-47-02) | mitigate | The reservation falls back to a PURE stETH leg when the ETH side can't cover, checked against the SAME basis the submit base is inflated by — a stETH force-feed inflates `stethBal` AND is coverable by the stETH leg, so submit cannot brick. Donation-robust by construction (coverage basis == inflation basis). |
| T-326-04-02 | Information Disclosure / Integrity | ETH+stETH mix double-counts backing / loses value | mitigate | The reservation is PURE one asset or the other (no mix); the single `pendingRedemptionEthValue` records the segregated amount, the physically-moved asset records which leg. Claim pays the reserved asset (asset-match). Value conservation is the RFALL-05 regression proof at TST. |
| T-326-04-03 | Tampering | a malicious "neither covers" path drains via an unchecked debit | mitigate | All arithmetic is CHECKED (0.8 checked subtraction); the "neither pure leg covers" case reverts fail-closed. No `unchecked` debit introduced. |
| T-326-04-04 | Repudiation | redemption-desk solvency under the fallback | mitigate | The stETH leg reserves REAL stETH that sDGNRS already holds; the desk's `balance >= pending` invariant (REDEEM-08) is re-proven under the stETH fallback at TST (RFALL-05). The 175% MAX defends against the concurrent SUB-09 self-sub claimable drain (R5). |
| T-326-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over both files.
- `pullRedemptionReserve` has the pure-ETH-OR-pure-stETH coverage branch (R1), fail-closed, donation-robust, checked.
- `_submitGamblingClaimFrom` maxIncrement reservation is segregated; the 4-term base + single tracker unchanged; claim asset matches reserved asset.
- The sStonk `receive()` / `burnAtGameOver` / `IAfKingSubscribe` are byte-unchanged (item-4 surfaces deferred to 326-05).
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC2 (RFALL-01/02/03) satisfied: the 175% redemption reservation segregates pure-ETH OR pure-stETH (no mix), falls back to pure-stETH when ETH can't cover, REVERTS fail-closed if neither pure leg covers (`StakedDegenerusStonk._submitGamblingClaimFrom` maxIncrement pull + `DegenerusGame.pullRedemptionReserve` coverage branch); coverage checked against the asset basis the base is inflated by (donation-robust); claim-time payout asset matches the reserved asset — extending the v47 game-over ETH→stETH fallback (REDEEM-04) to the mid-game depletion case.
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-04-SUMMARY.md` when done. Record the two coverage-leg structures, the donation-robustness argument (coverage basis == inflation basis), the asset-match at claim, and confirmation the single tracker + the item-4 sStonk surfaces are untouched. Do NOT git commit.
</output>
