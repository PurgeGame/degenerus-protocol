---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
autonomous: true
requirements: [PFIX-01]
must_haves:
  truths:
    - "_presaleBoxDgnrsReward divides by 400 (not 1_000), i.e. base = poolStart/40 — the absolute presale-box DGNRS scale moves up ~2.5x per ETH, so the ~40%-DGNRS branch drains the pool through the boxes instead of leaving ~60% to the closing-box sweep (fixes F-47-01)."
    - "The tier curve [3.0,2.5,2.0,1.5,1.0] (tier-1 still 3x tier-5 DGNRS-per-ETH) is byte-unchanged — only the absolute scale moved."
    - "The two inline derivation comments describe poolStart/40 and (40 * 10 * 1 ether), matching the new divisor; no historical 'changed from' note."
    - "The closing-box transferFromPool sweep still clamps to live pool balance — a run of early DGNRS hits cannot over-draw and the closing sweep returns ~0 without revert."
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "_presaleBoxDgnrsReward with the /400 divisor (base = poolStart/40) and updated derivation comments"
      contains: "400 * 1 ether"
  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol _presaleBoxDgnrsReward"
      to: "_presaleBoxDgnrsTierTenths (tier curve, untouched)"
      via: "tierTenths multiplier preserved; only the divisor changed"
      pattern: "_presaleBoxDgnrsTierTenths"
---

<objective>
Item 1 (PFIX) — fix F-47-01, the presale closing-box DGNRS over-distribution. ISOLATED, single-divisor edit in `DegenerusGameLootboxModule.sol`. The `_presaleBoxDgnrsReward` per-box DGNRS reward divides `poolStart * tierTenths * amount` by `1_000 * 1 ether` today (`base = poolStart/100`), which leaves ~60% of the presale DGNRS pool to be swept to the closing-box buyer as a windfall. The locked fix moves the divisor to `400 * 1 ether` (`base = poolStart/40`), raising the per-box reward ~2.5x so the pool is drawn down through the boxes and the closing sweep mops up only variance dust.

Purpose: ROADMAP SC1 — divisor `1_000 → 400`, `base = poolStart/100 → poolStart/40`, tier shape preserved, `transferFromPool` clamp held, touching no other item's surface. This file is fully disjoint from the items-2/3/4/7 shared surface AND from items 5/6, so it runs in Wave 1 alongside BTOMB (326-02) and HERO (326-03), which touch disjoint files.
Output: The corrected `_presaleBoxDgnrsReward` divisor + the two updated derivation comments.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. The whole milestone is ONE batched diff committed only after the final USER-APPROVAL gate (plan 326-08). The plan docs are already committed by the planner; do NOT git commit during execution (the commit-guard hook blocks commits while `.sol` is dirty anyway). NOTHING in this plan is pre-approved — the user reviews the diff at 326-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md
@.planning/PLAN-V48-PRESALE-BOX-DRAIN-FIX.md

<grep_protocol>
Every cited `file:line` is from the v47.0-closure baseline `da5c9d50` (live tree byte-identical — confirmed). Re-grepped at plan time: the divisor `(1_000 * 1 ether)` is at `:720`, the function `_presaleBoxDgnrsReward` opens at `:705`, the derivation comments at `:716` + `:718-719`, the tier function `_presaleBoxDgnrsTierTenths` at `:733-747`. This is the first edit to this file and nothing else in the batched diff touches it, so lines barely drift — but re-grep `_presaleBoxDgnrsReward` / `1_000 \* 1 ether` to find the CURRENT line before editing. The 325-ATTEST-PFIX-RFALL §A table (P1-P7) is the source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the _presaleBoxDgnrsReward divisor 1_000 → 400 + update the two derivation comments</name>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (the function `_presaleBoxDgnrsReward` at :705-727; divisor line `:720`; derivation comments `:716` and `:718-719`; the tier function `_presaleBoxDgnrsTierTenths` :733-747; the 40%-DGNRS branch call site :669-671; the closing-box `transferFromPool` sweep :681-691)
    - contracts/StakedDegenerusStonk.sol (the `transferFromPool` clamp at :475-483 — read to confirm clamp-to-live-balance; NOT edited by this plan)
    - 325-ATTEST-PFIX-RFALL.md §A (P1-P7 anchor table + the exact `:716-720` fix-target body)
    - PLAN-V48-PRESALE-BOX-DRAIN-FIX.md §"Fix" (the locked 1_000→400 / poolStart/100→poolStart/40 instruction)
  </read_first>
  <action>
    Per PFIX-01 + SPEC §3 item-1 + carried correction C1. In `_presaleBoxDgnrsReward` change the divisor in the `dgnrsAmount` computation from `(1_000 * 1 ether)` to `(400 * 1 ether)` — the only arithmetic change. Then rewrite the two inline derivation comments so they describe what IS after the fix (no "changed from" / historical note per the project rule): the `base = poolStart / 100 DGNRS per ETH` comment becomes `base = poolStart / 40 DGNRS per ETH`, and the `= poolStart * tierTenths * amount / (100 * 10 * 1 ether)` derivation comment becomes `= poolStart * tierTenths * amount / (40 * 10 * 1 ether)`. Do NOT touch `_presaleBoxDgnrsTierTenths` (the `[3.0,2.5,2.0,1.5,1.0]` tier ladder via `PRESALE_BOX_DGNRS_TIER1..5_TENTHS`) — the tier shape is scale-only and preserved (tier-1 stays 3x tier-5 DGNRS-per-ETH because both legs share the new divisor). Do NOT touch the closing-box `transferFromPool` sweep (:681-691) — it already clamps to the live pool balance via `StakedDegenerusStonk.transferFromPool` (:481-483), so a run of early DGNRS hits cannot over-draw; the fix works by drawing the pool DOWN through the boxes so the closing sweep finds ~dust.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "400 \* 1 ether" contracts/modules/DegenerusGameLootboxModule.sol && ! grep -q "1_000 \* 1 ether" contracts/modules/DegenerusGameLootboxModule.sol && ! grep -qi "poolStart / 100" contracts/modules/DegenerusGameLootboxModule.sol && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "400 \* 1 ether" contracts/modules/DegenerusGameLootboxModule.sol` returns ≥1 (the new divisor is present).
    - `grep -q "1_000 \* 1 ether"` returns nothing (the old `1_000` divisor is gone).
    - `grep -i "poolStart / 100"` returns nothing AND `grep "poolStart / 40"` returns the updated comment.
    - `grep "(40 \* 10 \* 1 ether)"` returns the updated derivation comment (no surviving `(100 * 10 * 1 ether)`).
    - `git diff contracts/modules/DegenerusGameLootboxModule.sol` shows NO hunk inside `_presaleBoxDgnrsTierTenths` and NO hunk inside the closing-box `transferFromPool` sweep block.
    - `forge build` reports 0 errors for the touched file.
  </acceptance_criteria>
  <done>The `_presaleBoxDgnrsReward` divisor is `400 * 1 ether`; both derivation comments describe `poolStart/40` and `(40 * 10 * 1 ether)`; the tier function and the closing sweep are byte-untouched; the file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| presale-box buyer → DGNRS pool | a buyer's box draw + the closing-box sweep both pull from the bounded `Pool.PresaleBox` DGNRS pool; the threat is over-distribution to a single (closing) buyer. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-01-01 | Information Disclosure / Integrity | `_presaleBoxDgnrsReward` over-distribution (F-47-01) | mitigate | Divisor `1_000→400` raises per-box reward ~2.5x so the pool is drawn down through the boxes; the closing sweep finds ~dust instead of a ~60% windfall. The dust bound is empirically proven at TST (PFIX-02/03). |
| T-326-01-02 | Denial of Service | over-draw on a run of early DGNRS hits | accept | The per-box draw and the closing sweep both route through `StakedDegenerusStonk.transferFromPool`, which clamps to the live pool balance (:481-483) and returns the clamped amount — cannot over-draw or revert on an empty pool (returns 0). Grep-verified, not assumed; no edit needed. |
| T-326-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` compiles the touched file with 0 errors.
- `grep "400 \* 1 ether"` present; `grep "1_000 \* 1 ether"` absent; `grep "poolStart / 40"` present; `grep "poolStart / 100"` absent.
- The tier function `_presaleBoxDgnrsTierTenths` and the closing-box sweep are byte-unchanged.
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC1 satisfied: `_presaleBoxDgnrsReward` divisor moved `1_000 → 400` (`base = poolStart/40`), the inline derivation comment updated, the tier shape preserved (tier-1 still 3x tier-5 DGNRS-per-ETH), and the `transferFromPool` clamp held — an ISOLATED edit touching no other item's surface.
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-01-SUMMARY.md` when done. Record the before/after divisor, the comment edits, and confirmation the tier function + sweep are byte-untouched. Do NOT git commit.
</output>
