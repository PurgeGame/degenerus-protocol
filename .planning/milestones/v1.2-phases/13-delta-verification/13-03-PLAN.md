---
phase: 13-delta-verification
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified: [audit/v1.2-delta-new-attack-surfaces.md]
autonomous: true
requirements: [DELTA-03]

must_haves:
  truths:
    - "lastLootboxRngWord attack surfaces are explicitly identified and analyzed"
    - "midDayTicketRngPending attack surfaces are explicitly identified and analyzed"
    - "Coinflip lock changes attack surfaces are explicitly identified and analyzed"
    - "Each new attack surface has a SAFE/EXPLOITABLE verdict with evidence"
  artifacts:
    - path: "audit/v1.2-delta-new-attack-surfaces.md"
      provides: "Analysis of new attack surfaces from added state variables and lock changes"
      contains: "lastLootboxRngWord"
  key_links:
    - from: "audit/v1.2-delta-new-attack-surfaces.md"
      to: "audit/v1.2-delta-attack-reverification.md"
      via: "Builds on re-verified attack scenarios"
      pattern: "Attack"
    - from: "audit/v1.2-delta-new-attack-surfaces.md"
      to: "audit/v1.2-delta-rng-impact-assessment.md"
      via: "Uses NEW SURFACE and MODIFIED SURFACE findings"
      pattern: "NEW SURFACE|MODIFIED SURFACE"
    - from: "audit/v1.2-delta-new-attack-surfaces.md"
      to: "audit/v1.2-rng-storage-variables.md"
      via: "Variable lifecycle traces for new variables"
      pattern: "lastLootboxRngWord|midDayTicketRngPending"
---

<objective>
Analyze new attack surfaces introduced by `lastLootboxRngWord`, `midDayTicketRngPending`, and coinflip lock changes, building on the re-verification (Plan 01) and impact assessment (Plan 02) findings.

Purpose: The v1.0 code changes introduced new state variables and modified lock behavior. These create potential attack surfaces not covered by the original 8 scenarios. This plan performs focused adversarial analysis on each new surface.

Output: `audit/v1.2-delta-new-attack-surfaces.md` with attack surface analysis and SAFE/EXPLOITABLE verdicts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@audit/v1.2-delta-attack-reverification.md
@audit/v1.2-delta-rng-impact-assessment.md
@audit/v1.2-rng-storage-variables.md
@audit/v1.2-rng-functions.md
@audit/v1.2-rng-data-flow.md
@audit/v1.0-rng-and-changes-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze lastLootboxRngWord and midDayTicketRngPending attack surfaces</name>
  <files>audit/v1.2-delta-new-attack-surfaces.md</files>
  <action>
Create `audit/v1.2-delta-new-attack-surfaces.md` with the following structure:

**Header:** Title, scope (new attack surfaces from v1.0 code changes), methodology (adversarial analysis of each new variable/mechanism).

**Reference the Phase 12 lifecycle traces** from `audit/v1.2-rng-storage-variables.md` for `lastLootboxRngWord` and `midDayTicketRngPending`. Reference the NEW SURFACE and MODIFIED SURFACE findings from `audit/v1.2-delta-rng-impact-assessment.md`.

**Section 1: lastLootboxRngWord Attack Surface**

Analyze this variable adversarially:
- **What it stores:** The most recent lootbox RNG word, copied from `lootboxRngWordByIndex` during finalization
- **Who writes it:** Identify exact write sites in current code (grep for assignment)
- **Who reads it:** Identify all consumers (grep for reads)
- **Visibility:** Is this value observable on-chain? Can an attacker read it from a prior block?
- **Attack vector 1 -- Known-word exploitation:** If an attacker knows `lastLootboxRngWord` from a prior block, can they influence any outcome? Trace every consumer function to determine if knowledge of this value provides advantage. Consider: is it used as entropy for future outcomes? Is it mixed with other entropy sources?
- **Attack vector 2 -- Stale-word recycling:** The Phase 12 data flow shows stale daily VRF words get routed to lootbox. Does `lastLootboxRngWord` create a secondary recycling path?
- **Verdict:** SAFE / EXPLOITABLE with detailed reasoning

**Section 2: midDayTicketRngPending Attack Surface**

Analyze this flag adversarially:
- **What it stores:** Boolean flag indicating a mid-day lootbox RNG request is pending
- **Who writes it:** Identify set/clear sites
- **Who reads it:** Identify all check sites
- **Attack vector 1 -- State desync:** Can an attacker manipulate timing to create a state where `midDayTicketRngPending` is true but no VRF request is actually in-flight? What happens if VRF callback arrives with unexpected flag state?
- **Attack vector 2 -- Gate bypass:** Does this flag gate any operation that could be bypassed by manipulating when it gets set/cleared?
- **Attack vector 3 -- Interaction with rngLockedFlag:** Can the two flags be in contradictory states? What happens if `rngLockedFlag=false` but `midDayTicketRngPending=true`?
- **Verdict:** SAFE / EXPLOITABLE with detailed reasoning

Use this format for each attack vector:
```
#### Attack Vector: [Name]

**Hypothesis:** [What the attacker tries to achieve]
**Mechanism:** [How they would attempt it]
**Analysis:**
1. [Step-by-step trace through code]
2. [What blocks or enables this]
**Verdict:** BLOCKED / SAFE BY DESIGN / EXPLOITABLE
**Evidence:** [File:line references]
```
  </action>
  <verify>
    <automated>grep -c "lastLootboxRngWord\|midDayTicketRngPending" audit/v1.2-delta-new-attack-surfaces.md | xargs test 5 -le</automated>
  </verify>
  <done>Both lastLootboxRngWord and midDayTicketRngPending have full adversarial analysis with attack vectors, code traces, and SAFE/EXPLOITABLE verdicts</done>
</task>

<task type="auto">
  <name>Task 2: Analyze coinflip lock changes and produce consolidated summary</name>
  <files>audit/v1.2-delta-new-attack-surfaces.md</files>
  <action>
Append to the document created in Task 1:

**Section 3: Coinflip Lock Changes Attack Surface**

The v1.0 changes removed `rngLockedFlag` guards from several purchase paths (LOCK-01 through LOCK-06, with LOCK-06 re-added). Analyze the coinflip-specific implications:

- **BurnieCoinflip external contract:** How does BurnieCoinflip.sol read `rngLockedFlag`? (It uses a view function call to the game contract, per Phase 12 decision). What coinflip operations are gated by this?
- **`_coinflipLockedDuringTransition` flag:** Is this a separate mechanism from `rngLockedFlag`? When is it set/cleared? What does it gate?
- **Attack vector 1 -- Coinflip during daily RNG:** With lock removals, can a player execute a coinflip while daily RNG is in-flight? If so, what entropy source does the coinflip use? Is it independent of the daily VRF word?
- **Attack vector 2 -- Coinflip timing around lootbox RNG:** Can a player time coinflip execution around lootbox VRF callback to gain advantage?
- **Attack vector 3 -- Lock gap between jackpot days:** During the 5-day jackpot phase, `rngLockedFlag` clears between daily `advanceGame()` calls but `prizePoolFrozen` stays true. Can coinflip operations during this gap affect jackpot outcomes?
- **Verdict:** SAFE / EXPLOITABLE for each vector

**Section 4: Cross-Variable Interaction Analysis**

Brief analysis of interactions between the three new surfaces:
- Can `lastLootboxRngWord` + `midDayTicketRngPending` create a combined attack not possible with either alone?
- Do coinflip lock changes interact with either new variable?

**Section 5: Consolidated Summary**

Summary table of all new attack surfaces:

| Surface | Variable/Mechanism | Vectors Analyzed | Overall Verdict | Key Evidence |
|---------|-------------------|------------------|-----------------|--------------|
| 1 | lastLootboxRngWord | [N] | SAFE/EXPLOITABLE | [brief] |
| 2 | midDayTicketRngPending | [N] | SAFE/EXPLOITABLE | [brief] |
| 3 | Coinflip lock changes | [N] | SAFE/EXPLOITABLE | [brief] |
| 4 | Cross-variable interactions | [N] | SAFE/EXPLOITABLE | [brief] |

**Handoff to Phase 14:** List any findings marked EXPLOITABLE or any manipulation windows identified that need deeper analysis in the Manipulation Window Analysis phase.
  </action>
  <verify>
    <automated>grep -c "coinflip\|Coinflip\|COINFLIP" audit/v1.2-delta-new-attack-surfaces.md | xargs test 3 -le</automated>
  </verify>
  <done>Coinflip lock changes fully analyzed, cross-variable interactions assessed, consolidated summary table with verdicts present, and Phase 14 handoff notes included</done>
</task>

</tasks>

<verification>
- All three new attack surfaces explicitly analyzed: lastLootboxRngWord, midDayTicketRngPending, coinflip lock changes
- Each has multiple adversarial attack vectors with SAFE/EXPLOITABLE verdicts
- Cross-variable interactions assessed
- Consolidated summary table present
- Phase 14 handoff notes identify anything needing deeper window analysis
</verification>

<success_criteria>
- lastLootboxRngWord has at least 2 attack vectors analyzed with verdicts
- midDayTicketRngPending has at least 3 attack vectors analyzed with verdicts
- Coinflip lock changes have at least 3 attack vectors analyzed with verdicts
- Every verdict includes code file:line evidence
- Summary table covers all surfaces with overall verdicts
</success_criteria>

<output>
After completion, create `.planning/phases/13-delta-verification/13-03-SUMMARY.md`
</output>
