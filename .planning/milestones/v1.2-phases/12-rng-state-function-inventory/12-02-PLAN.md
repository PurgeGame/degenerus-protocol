---
phase: 12-rng-state-function-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.2-rng-functions.md
autonomous: true
requirements:
  - RFN-01
  - RFN-02
  - RFN-04

must_haves:
  truths:
    - "Every function that reads or writes RNG state is catalogued with access pattern"
    - "Every external/public entry point that can modify RNG-dependent state is identified"
    - "Guard conditions (rngLockedFlag, prizePoolFrozen) are catalogued with which functions check them"
  artifacts:
    - path: "audit/v1.2-rng-functions.md"
      provides: "Complete catalogue of RNG-touching functions and entry points"
      contains: "rawFulfillRandomWords"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "audit/v1.2-rng-functions.md"
      via: "Function extraction with access patterns"
      pattern: "advanceGame|requestLootboxRng|rawFulfillRandomWords"
---

<objective>
Catalogue every function that touches RNG state, every external entry point that can modify RNG-dependent state, and every guard condition protecting RNG operations.

Purpose: Function-side inventory for the RNG security audit -- complements the storage variable inventory (Plan 01). Downstream phases need to know every code path that can influence or consume RNG.
Output: `audit/v1.2-rng-functions.md` with three sections: RNG function catalogue, external entry points, guard analysis.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@audit/v1.0-rng-and-changes-audit.md
@contracts/DegenerusGame.sol
@contracts/BurnieCoinflip.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/modules/DegenerusGameMintModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogue all RNG-touching functions and external entry points (RFN-01, RFN-02)</name>
  <files>audit/v1.2-rng-functions.md</files>
  <action>
Search every `.sol` file in `contracts/` and `contracts/modules/` for functions that read or write any RNG storage variable (use the variable names from storage: rngWordCurrent, rngWordByDay, vrfRequestId, rngLockedFlag, lootboxRngWordByIndex, lootboxRngIndex, lootboxRngRequestIndexById, lastLootboxRngWord, midDayTicketRngPending, prizePoolFrozen, rngRequestTime, ticketsFullyProcessed, totalFlipReversals, and any others found).

For each function, record:
1. **Function name** (exact Solidity identifier)
2. **Contract/Module** and line number
3. **Visibility** (external, public, internal, private)
4. **Access pattern**: READ (reads RNG state), WRITE (modifies RNG state), GATE (checks RNG state as guard condition), or combinations
5. **RNG variables touched** (which specific variables)
6. **Brief description** of what the function does with RNG state

Create `audit/v1.2-rng-functions.md` with:

**Section 1: Complete RNG Function Catalogue (RFN-01)**

Table format:
```
| Function | Contract/Module:Line | Visibility | Access | Variables Touched | Description |
```

Organize by module for readability. Include ALL functions -- internal helpers, private utilities, everything. Do not skip functions just because they are internal.

**Section 2: External/Public Entry Points (RFN-02)**

Separate table listing ONLY external/public functions that can (directly or through internal calls) modify RNG-dependent state. For each:
1. Function signature (name + parameters)
2. Contract:line
3. Which RNG state it can ultimately modify (trace through internal calls)
4. Access control (onlyOwner, anyone, specific role)
5. Whether it is callable during RNG-locked or frozen states

Key entry points to find (non-exhaustive):
- `advanceGame()` -- daily RNG consumption
- `rawFulfillRandomWords()` -- VRF callback
- `requestLootboxRng()` -- mid-day RNG request
- `reverseFlip()` -- nudge mechanism
- `_purchaseDeityPass()` path from external purchase functions
- Ticket purchase paths that modify queue state
- `openLootBox()` / `openBurnieLootBox()` -- lootbox resolution
- `claimDecimatorJackpot()` -- pool mutation during freeze
- BurnieCoinflip entry points
- Admin functions (updateVrfCoordinatorAndSub, etc.)
  </action>
  <verify>
    <automated>grep -c "rawFulfillRandomWords\|advanceGame\|requestLootboxRng\|reverseFlip\|openLootBox\|claimDecimatorJackpot\|purchaseDeityPass" audit/v1.2-rng-functions.md | awk '{if ($1 >= 7) print "PASS: key functions catalogued"; else print "FAIL: missing key functions, found " $1}'</automated>
  </verify>
  <done>Every function touching RNG state is listed with module, line, visibility, access pattern, and variables touched. Every external/public entry point that can modify RNG-dependent state is identified with access control and freeze/lock callability.</done>
</task>

<task type="auto">
  <name>Task 2: Guard condition analysis (RFN-04)</name>
  <files>audit/v1.2-rng-functions.md</files>
  <action>
Add Section 3 to `audit/v1.2-rng-functions.md`:

**Section 3: Guard Condition Analysis (RFN-04)**

For each RNG-gating condition, create a dedicated subsection:

**3a. `rngLockedFlag` Guard**
- Grep all occurrences of `rngLockedFlag` across all contracts
- For each check: function name, module:line, what it guards (revert? conditional branch?), the exact Solidity expression
- Cross-reference against v1.0 audit (which listed 3 checkers + 6 removed) -- confirm current state matches or document changes

**3b. `prizePoolFrozen` Guard**
- Grep all occurrences of `prizePoolFrozen` across all contracts
- For each: function, module:line, what it guards, the effect (revert, route to pending pool, skip operation)
- Include the freeze/unfreeze lifecycle (who sets true, who sets false)

**3c. Other RNG Guards**
Search for any other conditions that gate RNG operations:
- `rngRequestTime` checks (staleness detection)
- `ticketsFullyProcessed` checks (drain gate)
- `midDayTicketRngPending` checks (mid-day sequencing)
- `lootboxRngMinLinkBalance` checks (LINK balance gate)
- `lootboxRngThreshold` checks (ETH threshold gate)
- Any `require` or `if...revert` near RNG variable reads/writes

For each guard, document:
1. The condition expression
2. What state it protects
3. What happens when the guard triggers (revert, skip, alternative path)
4. Whether the guard is sufficient for its purpose (brief assessment referencing v1.0 audit findings where applicable)

Create a summary table:
```
| Guard | Functions Protected | Module:Lines | Trigger Effect |
```
  </action>
  <verify>
    <automated>grep -c "rngLockedFlag.*Guard\|prizePoolFrozen.*Guard\|rngRequestTime\|ticketsFullyProcessed" audit/v1.2-rng-functions.md | awk '{if ($1 >= 4) print "PASS: guard sections present"; else print "FAIL: missing guard sections, found " $1}'</automated>
  </verify>
  <done>Every RNG guard condition is documented with exact code references, protected functions, trigger effects, and sufficiency assessment. Summary table provides quick reference. rngLockedFlag state matches or updates v1.0 audit findings.</done>
</task>

</tasks>

<verification>
- Document contains three sections: Function Catalogue, External Entry Points, Guard Analysis
- Every function has module:line reference, visibility, and access pattern
- External entry points include access control and lock/freeze callability
- Guard analysis covers rngLockedFlag, prizePoolFrozen, and at least 3 other guard conditions
- Cross-references v1.0 audit findings where applicable
</verification>

<success_criteria>
- All RNG-touching functions catalogued across all 12 modules + main contract + coinflip (minimum 20 functions)
- All external entry points identified (minimum 8)
- Guard analysis covers rngLockedFlag (minimum 3 check sites), prizePoolFrozen (minimum 10 check sites), and other guards
- Every entry in function tables has exact line number references
</success_criteria>

<output>
After completion, create `.planning/phases/12-rng-state-function-inventory/12-02-SUMMARY.md`
</output>
