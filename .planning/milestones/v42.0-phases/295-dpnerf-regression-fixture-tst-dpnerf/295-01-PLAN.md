---
phase: 295-dpnerf-regression-fixture-tst-dpnerf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/randTraitTicketRef.mjs
  - test/edge/DeityPassGoldNerfRegression.test.js
autonomous: false
requirements:
  - TST-DPNERF-01
  - TST-DPNERF-02
  - TST-DPNERF-03
  - TST-DPNERF-04
  - TST-DPNERF-05
requirements_addressed:
  - TST-DPNERF-01
  - TST-DPNERF-02
  - TST-DPNERF-03
  - TST-DPNERF-04
  - TST-DPNERF-05
user_setup: []

must_haves:
  truths:
    # ROADMAP success criteria 1-5 (verbatim from .planning/ROADMAP.md L219-L223)
    - "Gold-tile virtual-count assertion PASSES — deity-pass holder + gold-tier trait win (`((trait >> 3) & 7) == 7`) via ETH jackpot path (`payDailyJackpot` -> `_processDailyEth` -> `_randTraitTicket` callsite 3 at L1296) yields `virtualCount == 1` (not `max(len/50, 2)`); confirms TST-DPNERF-01"
    - "Common-tier virtual-count preserved PASSES — deity-pass holder + common-color trait win (color in [0..6]) yields `virtualCount == max(len/50, 2)` per the unchanged v41 logic; confirms TST-DPNERF-02"
    - "BURNIE coin jackpot path coverage PASSES per D-42N-PATH-COVERAGE-01 + D-294-BURNIE-INLINE-01 — fixture replicates the gold-tile scenario via natural-production-flow `payDailyCoinJackpot(lvl, randWord, minLevel, maxLevel)` -> `_awardDailyCoinToTraitWinners` inline-duplicate gold-tier block at L1867-L1874; asserts identical `virtualCount == 1` semantics via `JackpotBurnieWin(winner==deity, lvlPrime, trait, amount, ticketIdx==type(uint256).max)` event sentinel; confirms TST-DPNERF-03"
    - "Gold-tile EV regression measured — N=1,000 gold-tier trait wins across both ETH (25-winner draw) + BURNIE (1-winner-per-pull) paths via JS-replay oracle in `test/helpers/randTraitTicketRef.mjs`; empirical deity virtual-entry total equals N × 1 = N (vs v41 baseline N × max(len/50, 2)); 16-32 production cross-attestation iterations confirm JS<->EVM bit-identity; confirms TST-DPNERF-04"
    - "Non-deity holders unaffected PASSES — gold-tier trait win with `deityBySymbol[fullSymId] == address(0)` yields `virtualCount == 0` (entire `if (deity != address(0))` block skipped); ZERO deity-sentinel entries in 25-winner draw; all 25 `ticketIndexes[i] < len`; confirms TST-DPNERF-05"
    # Locked CONTEXT.md decisions (every D-295-* cited)
    - "D-295-EV-METHODOLOGY-01 honored — hybrid 1,000 JS-replay + small-N (16-32) production cross-attestation; planner-chosen split 750 ETH + 250 BURNIE proportional to production call frequency (ETH 4 callsites + 25-winner draw vs BURNIE 1-winner-per-pull); cross-attestation N=16 within the 16-32 band (smallest N satisfying per-iteration deity-win-count chi^2 goodness-of-fit at p > 0.05 against the analytical 1/(len+1) per-draw expectation; df=1 critical value 3.841 at alpha=0.05 per the chi^2 table imported by sister `HeroOverrideWeightedRoll.test.js` L141-L149)"
    - "D-295-BURNIE-PATH-01 honored — TST-DPNERF-03 invokes `payDailyCoinJackpot` directly (NOT JS-replay-only); JS-replay BURNIE arithmetic is implemented in `randTraitTicketRef.mjs` as a component of TST-DPNERF-04 only; the JSDoc explicitly cites the inline-duplicate site L1867-L1874 vs `_randTraitTicket` L1731-L1738 per `feedback_verify_call_graph_against_source.md` Phase 294 BURNIE gap precedent"
    - "D-295-GAS-01 honored — ZERO gas measurement: no `console.log` of gas, no soft/hard assertion, no helper, no informational trace; Phase 294 §5 attestation (+86 byte runtime bytecode delta; per-call gas negligible at ~20-50) is the load-bearing reference cited at Phase 297 §3.A"
    - "D-295-CALLSITE-SCOPE-01 honored — TST-DPNERF JSDoc includes 5-row callsite-coverage table enumerating callsite 1 (L698 `_runEarlyBirdLootboxJackpot` -> deferred-to-296-SWEEP), callsite 2 (L988 `_distributeTicketsToBucket` -> deferred-to-296-SWEEP), callsite 3 (L1296 `_processDailyEth` -> covered-by-TST-DPNERF-01/02), callsite 4 (L1399 `_resolveTraitWinners` -> covered-by-TST-DPNERF-04), BURNIE L1867 inline-duplicate (covered-by-TST-DPNERF-03 + TST-DPNERF-04); documentation-only, no test logic"
    - "D-295-INVOKE-01 honored — JS-replay oracle + small-N production cross-attestation as PRIMARY methodology; the `_randTraitTicket private view` + `address[][256] storage` signature constraint makes inheritance-harness inapplicable; the visibility-flip escalation path is NOT invoked at this plan and NOT pre-approved (any contracts/ touch requires explicit USER APPROVAL per `feedback_never_preapprove_contracts.md`)"
    # Mandatory audit-feedback constraints
    - "ZERO contracts/ mutations — `git diff HEAD -- contracts/` is EMPTY after this plan completes per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md`"
    - "ONE batched USER-APPROVED commit at phase close (test file + JS helper in a single diff) per `feedback_batch_contract_approval.md`; executor presents diff for review per `feedback_manual_review_before_push.md` BEFORE staging or committing; no `git push`"
    - "Test JSDoc + JS helper describe what IS (v42 DPNERF mechanic) — ZERO history language (no `previously` / `v41 form` / `used to be` / `was max(len/50, 2)`) per `feedback_no_history_in_comments.md`"
    - "Grep-verification of BURNIE inline-duplicate site executed in Task 3 — `grep -n 'if (((trait_i >> 3) & 7) == 7)' contracts/modules/DegenerusGameJackpotModule.sol` returns L1868 (BURNIE inline-duplicate) AND `grep -n 'if (((trait >> 3) & 7) == 7)' ...` returns L1732 (ETH `_randTraitTicket`); both sites carry the same locked branch shape per D-294-BURNIE-INLINE-01"

  artifacts:
    - path: "test/helpers/randTraitTicketRef.mjs"
      provides: "Pure-function JS bit-mirror of (a) `_randTraitTicket` L1707-L1763 ETH-25-winner draw + (b) `_awardDailyCoinToTraitWinners` per-pull inline gold-tier block L1860-L1894 BURNIE 1-winner draw; drives TST-DPNERF-04 1,000-iter EV regression"
      exports:
        - "randTraitTicketRef({ traitBurnTicketBucket, randomWord, trait, numWinners, salt, deityPresent }) -> { winners, ticketIndexes, deitySentinelMask }"
        - "awardDailyCoinPullRef({ traitBurnTicketBucket, randomWord, trait_i, lvlPrime, i, deityPresent }) -> { idx, isDeitySentinel }"
        - "goldTierVirtualCount(trait, len, deityPresent) -> bigint  // shared primitive"
      min_lines: 130

    - path: "test/edge/DeityPassGoldNerfRegression.test.js"
      provides: "TST-DPNERF-01..05 regression fixture; 5 describe-block / 5+ it-block assertions; JSDoc path-of-investigation header with 5-row callsite-coverage table + BURNIE inline-duplicate grep-verification note"
      contains:
        - "describe(\"TST-DPNERF-01"
        - "describe(\"TST-DPNERF-02"
        - "describe(\"TST-DPNERF-03"
        - "describe(\"TST-DPNERF-04"
        - "describe(\"TST-DPNERF-05"
        - "JackpotBurnieWin"
        - "payDailyCoinJackpot"
        - "deityBySymbol"
        - "callsite-coverage"
        - "L1867"
        - "L1731"
      min_lines: 500

  key_links:
    - from: "test/edge/DeityPassGoldNerfRegression.test.js"
      to: "test/helpers/randTraitTicketRef.mjs"
      via: "ES module import: import { randTraitTicketRef, awardDailyCoinPullRef, goldTierVirtualCount } from '../helpers/randTraitTicketRef.mjs'"
      pattern: "from \"../helpers/randTraitTicketRef\\.mjs\""

    - from: "test/edge/DeityPassGoldNerfRegression.test.js"
      to: "test/helpers/deployFixture.js"
      via: "loadFixture(deployFullProtocol) — reused verbatim per sister test pattern"
      pattern: "loadFixture\\(deployFullProtocol\\)"

    - from: "test/edge/DeityPassGoldNerfRegression.test.js (TST-DPNERF-03)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:1773 payDailyCoinJackpot"
      via: "Natural production-flow invocation; emits JackpotBurnieWin events parsed for deity sentinel"
      pattern: "payDailyCoinJackpot\\("

    - from: "test/edge/DeityPassGoldNerfRegression.test.js (TST-DPNERF-01/02/05)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:1296 _processDailyEth -> :1707 _randTraitTicket"
      via: "Natural production-flow `payDailyJackpot` invocation; 25-winner draw return parsed for deity sentinel (winners[i]==deity paired with ticketIndexes[i]==type(uint256).max per L1755-L1757 invariant)"
      pattern: "payDailyJackpot"

    - from: "test/helpers/randTraitTicketRef.mjs"
      to: "ethers.utils.keccak256 + AbiCoder.defaultAbiCoder().encode"
      via: "Bit-mirror of `keccak256(abi.encode(randomWord, trait, salt, i))` at L1750 (ETH) + `keccak256(abi.encode(randomWord, trait_i, lvlPrime, i))` at L1883 (BURNIE) — uses `abi.encode` NOT `abi.encodePacked`"
      pattern: "abiCoder\\.encode|defaultAbiCoder"
---

<objective>
Ship `test/edge/DeityPassGoldNerfRegression.test.js` + `test/helpers/randTraitTicketRef.mjs` covering TST-DPNERF-01..05 against the Phase 294 audit-subject commit `47936e0c` (locked DPNERF-01 gold-tier nerf at `_randTraitTicket` L1731-L1738) and the post-Phase-294 BURNIE gap-closure amendment commit `38319463` (parallel gold-tier branch at `_awardDailyCoinToTraitWinners` L1867-L1874 inline-duplicate site).

Purpose:
- TST-DPNERF-01 + TST-DPNERF-02 + TST-DPNERF-05 assert single-iteration `virtualCount` semantics on ETH path via natural-production `payDailyJackpot` invocation
- TST-DPNERF-03 asserts BURNIE path semantics via natural-production `payDailyCoinJackpot` invocation per D-295-BURNIE-PATH-01
- TST-DPNERF-04 asserts 1,000-iteration deity-EV regression via JS-replay oracle (750 ETH + 250 BURNIE split) + 16-iteration production cross-attestation per D-295-EV-METHODOLOGY-01

Output:
- New file `test/helpers/randTraitTicketRef.mjs` (~150 lines; pure ES module; mirrors `rollHeroSymbolRef.mjs` shape per D-295-INVOKE-01 default disposition)
- New file `test/edge/DeityPassGoldNerfRegression.test.js` (~550 lines; mirrors `HeroOverrideWeightedRoll.test.js` shape; 5 describe blocks + setup-and-sanity block + cross-attestation block)
- ONE batched USER-APPROVED commit at phase close per `feedback_batch_contract_approval.md`
- ZERO contracts/ mutations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/295-dpnerf-regression-fixture-tst-dpnerf/295-CONTEXT.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-02-SUMMARY.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
@contracts/modules/DegenerusGameJackpotModule.sol
@test/edge/HeroOverrideWeightedRoll.test.js
@test/edge/MintCleanupRegression.test.js
@test/edge/MintBatchDeterminism.test.js
@test/helpers/rollHeroSymbolRef.mjs
@test/helpers/raritySymbolBatchRef.mjs
@test/helpers/deployFixture.js
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_verify_call_graph_against_source.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_skip_research_test_phases.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md

<interfaces>
<!-- Key contract identifiers and signatures the executor needs. Extracted from source pre-plan. -->
<!-- Executor uses these directly — no codebase exploration needed beyond confirming line ranges. -->

From contracts/modules/DegenerusGameJackpotModule.sol L1707-L1763 (`_randTraitTicket` ETH path body):
  Signature: `function _randTraitTicket(address[][256] storage traitBurnTicket_, uint256 randomWord, uint8 trait, uint8 numWinners, uint8 salt) private view returns (address[] memory winners, uint256[] memory ticketIndexes)`
  Body key lines:
    L1718: `address[] storage holders = traitBurnTicket_[trait];`
    L1719: `uint256 len = holders.length;`
    L1726: `uint8 fullSymId = (trait >> 6) * 8 + (trait & 0x07);`
    L1729: `if (fullSymId < 32) {`
    L1730: `    deity = deityBySymbol[fullSymId];`
    L1731: `    if (deity != address(0)) {`
    L1732: `        if (((trait >> 3) & 7) == 7) {`           // GOLD-TIER BRANCH — DPNERF-01 audit-subject
    L1733: `            virtualCount = 1;`
    L1734: `        } else {`
    L1735: `            virtualCount = len / 50;`
    L1736: `            if (virtualCount < 2) virtualCount = 2;`
    L1737: `        }`
    L1741: `uint256 effectiveLen = len + virtualCount;`
    L1748-L1762 sampling loop:
      L1750: `uint256 idx = uint256(keccak256(abi.encode(randomWord, trait, salt, i))) % effectiveLen;`
      L1752-L1754: if (idx < len) winners[i]=holders[idx]; ticketIndexes[i]=idx;
      L1755-L1757: else winners[i]=deity; ticketIndexes[i]=type(uint256).max;     // DEITY SENTINEL pair invariant

From contracts/modules/DegenerusGameJackpotModule.sol L1773 (BURNIE entry point):
  `function payDailyCoinJackpot(uint24 lvl, uint256 randWord, uint24 minLevel, uint24 maxLevel) external`
  Selector: 0xdbedb1c1 (UNCHANGED vs v41 per Phase 294 §4)

From contracts/modules/DegenerusGameJackpotModule.sol L1822-L1913 (`_awardDailyCoinToTraitWinners` BURNIE path):
  Signature: `function _awardDailyCoinToTraitWinners(uint24 minLevel, uint24 maxLevel, uint32 winningTraitsPacked, uint256 coinBudget, uint256 randomWord) private`
  Body key lines:
    L1830: `uint16 cap = DAILY_COIN_MAX_WINNERS;`
    L1852: `for (uint256 i; i < cap; ) {`
    L1853: `    uint8 traitIdx = uint8(i % 4);`
    L1854: `    uint8 trait_i = traitIds[traitIdx];`
    L1856-L1858: `lvlPrime = minLevel + uint24(uint256(keccak256(abi.encode(randomWord, COIN_LEVEL_TAG, i))) % range);`
    L1860: `address[] storage holders = traitBurnTicket[lvlPrime][trait_i];`
    L1861: `uint256 len = holders.length;`
    L1862: `address deity = deityCache[traitIdx];`
    L1867: `if (deity != address(0)) {`
    L1868: `    if (((trait_i >> 3) & 7) == 7) {`            // GOLD-TIER INLINE-DUPLICATE — D-294-BURNIE-INLINE-01
    L1869: `        virtualCount = 1;`
    L1870: `    } else {`
    L1871: `        virtualCount = len / 50;`
    L1872: `        if (virtualCount < 2) virtualCount = 2;`
    L1873: `    }`
    L1874: `}`
    L1883: `uint256 idx = uint256(keccak256(abi.encode(randomWord, trait_i, lvlPrime, i))) % effectiveLen;`
    L1888-L1893: deity sentinel pair (`winner = deity; ticketIdx = type(uint256).max;` when idx >= len)
    L1899-L1905: `emit JackpotBurnieWin(winner, lvlPrime, trait_i, amount, ticketIdx);`

From contracts/storage/DegenerusGameStorage.sol:
  L415: `mapping(uint24 => address[][256]) internal traitBurnTicket;`     // [lvl][trait] -> address[] holders
  L965: `mapping(uint8 => address) internal deityBySymbol;`              // [fullSymId] -> deity address (0..31 only)

From contracts/modules/DegenerusGameWhaleModule.sol:
  L598: `deityBySymbol[symbolId] = buyer;`   // ONLY writer (natural-production path for seeding)

Trait byte layout (per L1724-L1725 + L1726):
  `traitId = (quadrant << 6) | (color << 3) | symIdx`
  `fullSymId = (trait >> 6) * 8 + (trait & 0x07)` — maps to [0..31]; only fullSymId<32 has a deity slot
  `color = (trait >> 3) & 7` — gold = 7
  Example gold trait byte: `(0 << 6) | (7 << 3) | 0 = 0b00111000 = 56` (quadrant 0, color gold, symIdx 0; fullSymId=0)
  Example common trait byte: `(0 << 6) | (0 << 3) | 0 = 0` (quadrant 0, color 0, symIdx 0; fullSymId=0)

Sister-test helpers already in scope (DO NOT extend in-place per D-295 deferred register):
  test/helpers/rollHeroSymbolRef.mjs  -> shape template for the new file
  test/helpers/raritySymbolBatchRef.mjs -> ES-module export convention reference
  test/helpers/deployFixture.js (deployFullProtocol, restoreAddresses) -> reuse verbatim

Chi^2 critical-value table at alpha=0.05 (copy verbatim from HeroOverrideWeightedRoll.test.js L141-L149):
  df=1: 3.841   df=2: 5.991   df=3: 7.815   df=4: 9.488   df=5: 11.070   df=6: 12.592   df=7: 14.067
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JS-reference helper test/helpers/randTraitTicketRef.mjs</name>
  <files>test/helpers/randTraitTicketRef.mjs</files>
  <read_first>
    - test/helpers/rollHeroSymbolRef.mjs (closest sister shape; mirror the JSDoc header form, the AbiCoder + keccak256 import pattern, the U64/U32/U256 mask constants pattern, the named-export ES-module shape, the body-trace JSDoc style commenting each Solidity line vs the JS mirror)
    - test/helpers/raritySymbolBatchRef.mjs (pattern reference for `computeBaseKey` + multi-export shape — confirms the file-separation convention; do NOT import this helper here)
    - contracts/modules/DegenerusGameJackpotModule.sol L1707-L1763 (`_randTraitTicket` body; this is the ETH-path bit-mirror source)
    - contracts/modules/DegenerusGameJackpotModule.sol L1822-L1913 (`_awardDailyCoinToTraitWinners` body; this is the BURNIE per-pull inline-duplicate gold-tier bit-mirror source)
    - .planning/phases/295-dpnerf-regression-fixture-tst-dpnerf/295-CONTEXT.md `<decisions>` D-295-EV-METHODOLOGY-01 + Claude's Discretion "JS-reference impl shape" subsection (locks the exported function signatures + the `goldTierVirtualCount(trait, len, deityPresent)` shared primitive option)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (helper JSDoc describes the v42 mechanic only; no "previously" / "v41 form" language)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md (no extensibility hooks; no optional parameters with default branching; mirror EVM verbatim)
  </read_first>
  <action>
    Create `test/helpers/randTraitTicketRef.mjs` as a pure ES module mirroring the `test/helpers/rollHeroSymbolRef.mjs` shape verbatim (SPDX-License-Identifier header + file-level JSDoc + `import { AbiCoder, keccak256 } from "ethers"` + mask constants + named-export `function ...Ref({ ... })` + per-line Solidity-to-JS body-trace JSDoc + frozen constants export at end-of-file). Imports use the same `AbiCoder.defaultAbiCoder()` instance pattern at module top, NOT per-call instantiation.

    Export three named functions:
    1. `goldTierVirtualCount(trait, len, deityPresent)` — the shared primitive bit-mirror of the L1726-L1738 gold-tier-vs-common-tier branch logic. Inputs: `trait` (uint8 as Number; the trait byte), `len` (BigInt; bucket holder count), `deityPresent` (boolean; whether the caller passed a non-zero deity for the symbol). Returns BigInt `virtualCount`. Body computes `fullSymId = ((trait >> 6) * 8) + (trait & 0x07)`; if `fullSymId >= 32` or `!deityPresent` return `0n`; if `((trait >> 3) & 7) === 7` return `1n`; else compute `vc = len / 50n` and return `vc < 2n ? 2n : vc` (BigInt division floors per Solidity semantics).

    2. `randTraitTicketRef({ holders, randomWord, trait, numWinners, salt, deity })` — bit-mirror of `_randTraitTicket` L1707-L1763 (ETH 25-winner draw). Inputs: `holders` (string[] of addresses; the bucket — substitutes for the storage reference parameter), `randomWord` (BigInt uint256), `trait` (Number uint8), `numWinners` (Number uint8), `salt` (Number uint8), `deity` (string address or zero address). Returns `{ winners: string[], ticketIndexes: BigInt[], deitySentinelMask: boolean[], virtualCount: BigInt }`. Body: compute `len = BigInt(holders.length)`; compute `virtualCount = goldTierVirtualCount(trait, len, deity !== "0x0000000000000000000000000000000000000000" && deity !== ZeroAddress)` (accept both lower/upper-case zero forms); compute `effectiveLen = len + virtualCount`; if `effectiveLen === 0n || numWinners === 0` return all-empty arrays; loop `i in [0, numWinners)` computing `idx = BigInt(keccak256(abiCoder.encode(["uint256","uint8","uint8","uint8"], [randomWord, trait, salt, i]))) % effectiveLen`; if `idx < len` -> `winners[i] = holders[Number(idx)]; ticketIndexes[i] = idx; deitySentinelMask[i] = false`; else -> `winners[i] = deity; ticketIndexes[i] = (1n << 256n) - 1n; deitySentinelMask[i] = true`. Body-trace JSDoc cross-references every Solidity line at L1718, L1719, L1726, L1729-L1738, L1741-L1743, L1746-L1762 verbatim (mirrors `rollHeroSymbolRef.mjs` L50-L70 trace style).

    3. `awardDailyCoinPullRef({ holders, randomWord, trait_i, lvlPrime, pullIdx, deity })` — bit-mirror of the per-pull body of `_awardDailyCoinToTraitWinners` L1860-L1894 (single-winner-per-pull BURNIE draw). Inputs: `holders` (string[] for `traitBurnTicket[lvlPrime][trait_i]`), `randomWord` (BigInt uint256), `trait_i` (Number uint8), `lvlPrime` (Number uint24), `pullIdx` (BigInt; the loop index `i`), `deity` (string address; per-trait cached). Returns `{ winner: string, ticketIdx: BigInt, isDeitySentinel: boolean, virtualCount: BigInt, idx: BigInt }`. Body: `len = BigInt(holders.length)`; `virtualCount = goldTierVirtualCount(trait_i, len, deity !== zeroAddress)`; `effectiveLen = len + virtualCount`; if `effectiveLen === 0n` return `{ winner: zeroAddress, ticketIdx: 0n, isDeitySentinel: false, virtualCount, idx: 0n }` (mirrors the L1876-L1881 empty-bucket skip); `idx = BigInt(keccak256(abiCoder.encode(["uint256","uint8","uint24","uint256"], [randomWord, trait_i, lvlPrime, pullIdx]))) % effectiveLen`; if `idx < len` -> sentinel false, winner from holders; else sentinel true, winner = deity, ticketIdx = type(uint256).max. Body-trace JSDoc cross-references L1853-L1893 verbatim.

    4. Export `RAND_TRAIT_TICKET_CONSTANTS = Object.freeze({ U256_MASK, U64_MASK, U32_MASK, DEITY_SENTINEL_TICKET_IDX: (1n << 256n) - 1n })` at end-of-file mirroring `rollHeroSymbolRef.mjs` L185-L189.

    File-level JSDoc header (~30 lines) must include:
    - SPDX-License-Identifier line
    - File purpose: "JS bit-mirror of (a) `_randTraitTicket` L1707-L1763 ETH 25-winner draw and (b) `_awardDailyCoinToTraitWinners` per-pull inline gold-tier block L1860-L1894 BURNIE 1-winner draw, at the v42.0 DPNERF audit-subject commit `47936e0c` + BURNIE gap-closure commit `38319463`. Drives the 1,000-iteration TST-DPNERF-04 EV regression per D-295-EV-METHODOLOGY-01."
    - Algorithm-lock anchor cites: D-42N-GOLD-FLOOR-01 (gold flat-1), D-42N-DEITY-EV-01 (commons unchanged), D-294-BURNIE-INLINE-01 (BURNIE inline-duplicate site at L1867-L1874 is NOT a caller of `_randTraitTicket`; the gold-tier branch is duplicated inline because `_awardDailyCoinToTraitWinners` is a multi-bucket/1-winner-per-iteration sampler architecturally incompatible with `_randTraitTicket`'s single-bucket/N-winner signature).
    - Symmetric file-separation note: "Sister helpers `rollHeroSymbolRef.mjs` + `raritySymbolBatchRef.mjs` cover the v42 HRROLL + v41/v42 MINTCLN audit subjects; this file is a new sibling for the DPNERF audit subject per the Phase 282/291/293 file-separation pattern. Do NOT merge."
    - Numeric-integrity note: `len` and `virtualCount` are uint256 in Solidity; mirror via BigInt; mask intermediate ops with U256_MASK only where Solidity would (the `% effectiveLen` operation produces values < effectiveLen which is < 2^256 by construction, so no mask needed on `idx`).
    - ZERO history language. The phrase "v41 form was `virtualCount = max(len/50, 2)` uniformly across all 8 colors" is FORBIDDEN — the helper describes only the v42 mechanic.
  </action>
  <verify>
    <automated>node -e "import('./test/helpers/randTraitTicketRef.mjs').then(m => { console.log(Object.keys(m).join(',')); if (!m.randTraitTicketRef || !m.awardDailyCoinPullRef || !m.goldTierVirtualCount || !m.RAND_TRAIT_TICKET_CONSTANTS) process.exit(1); const goldTrait = (0 << 6) | (7 << 3) | 0; const commonTrait = 0; const goldVC = m.goldTierVirtualCount(goldTrait, 50n, true); const commonVC = m.goldTierVirtualCount(commonTrait, 50n, true); const noDeityVC = m.goldTierVirtualCount(goldTrait, 50n, false); console.log('goldVC='+goldVC+' commonVC='+commonVC+' noDeityVC='+noDeityVC); if (goldVC !== 1n) process.exit(2); if (commonVC !== 2n) process.exit(3); if (noDeityVC !== 0n) process.exit(4); });"</automated>
    <automated>test -f test/helpers/randTraitTicketRef.mjs &amp;&amp; wc -l test/helpers/randTraitTicketRef.mjs | awk '{ if ($1 &lt; 130) { print "FAIL: file too short"; exit 1 } else print "PASS: " $1 " lines" }'</automated>
    <automated>! grep -E "previously|v41 form|used to be|was max\(len/50, 2\)|was 2% of bucket" test/helpers/randTraitTicketRef.mjs</automated>
    <automated>grep -c "abi.encode\|abiCoder\.encode\|defaultAbiCoder" test/helpers/randTraitTicketRef.mjs | awk '{ if ($1 &lt; 2) { print "FAIL: missing abi.encode mirror"; exit 1 } else print "PASS: " $1 " matches" }'</automated>
  </verify>
  <done>
    `test/helpers/randTraitTicketRef.mjs` exists; 3 named function exports + 1 frozen constants export; `goldTierVirtualCount(goldTrait, 50n, true) === 1n` AND `goldTierVirtualCount(commonTrait, 50n, true) === 2n` AND `goldTierVirtualCount(goldTrait, 50n, false) === 0n`; ZERO history language (no `previously` / `v41 form` / `used to be`); at least 2 occurrences of `abi.encode` mirror via `abiCoder.encode` or `defaultAbiCoder`; file ≥ 130 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test/edge/DeityPassGoldNerfRegression.test.js with TST-DPNERF-01..05 + JSDoc header</name>
  <files>test/edge/DeityPassGoldNerfRegression.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (closest sister shape — JSDoc 5-bullet path-of-investigation header at L1-L110; module-level constants + helpers pattern at L130-L420; top-level describe + setup-and-sanity sub-describe pattern at L425-L470; per-TST-NN describe block pattern at L472+; chi^2 helper + Wilson-Hilferty wiring at L211-L240; storage-slot derivation via `forge inspect` at L242-L320; production-path replay pattern via `advanceGame()` + `payDailyJackpot`-equivalent at L1000+; `restoreAddresses()` after-hook at L428)
    - test/edge/MintCleanupRegression.test.js (sister fixture; reference for `loadFixture(deployFullProtocol)` deployment + 5-test describe-block ordering + breaking-change JSDoc disposition note pattern)
    - test/edge/MintBatchDeterminism.test.js (JS-replay oracle pattern reference; ~800-line fixture shape)
    - test/helpers/deployFixture.js (`deployFullProtocol` + `restoreAddresses` — reuse verbatim; deity-pass exposed as `contracts.deityPass` per the mapping at L122)
    - test/helpers/randTraitTicketRef.mjs (created in Task 1; import `randTraitTicketRef`, `awardDailyCoinPullRef`, `goldTierVirtualCount`, `RAND_TRAIT_TICKET_CONSTANTS`)
    - contracts/modules/DegenerusGameJackpotModule.sol L1707-L1763 (ETH `_randTraitTicket` audit-subject; locked branch at L1731-L1738)
    - contracts/modules/DegenerusGameJackpotModule.sol L1773-L1796 (`payDailyCoinJackpot` BURNIE entry point — selector 0xdbedb1c1)
    - contracts/modules/DegenerusGameJackpotModule.sol L1822-L1913 (BURNIE path body; inline-duplicate gold-tier branch at L1867-L1874; `JackpotBurnieWin` emission at L1899-L1905)
    - contracts/storage/DegenerusGameStorage.sol L415 (`traitBurnTicket[lvl][trait]` storage layout) + L965 (`deityBySymbol[fullSymId]`)
    - contracts/modules/DegenerusGameWhaleModule.sol L598 (`deityBySymbol[symbolId] = buyer` — ONLY writer; informs the natural-production seeding path OR the hardhat_setStorageAt seeding alternative)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md §(iv) Path-Coverage Trade-Offs + Decision-Anchor table (5-surface enumeration + D-294-BURNIE-INLINE-01 — load-bearing context for the JSDoc inline-duplicate citation)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md §1 + §3 (callsite enumeration — feeds the JSDoc 5-row callsite-coverage table)
    - .planning/phases/295-dpnerf-regression-fixture-tst-dpnerf/295-CONTEXT.md `<specifics>` (locks the holder-bucket-size ~50 default + the 750 ETH + 250 BURNIE split for TST-DPNERF-04 + the cross-attestation N=16 default within the 16-32 band)
  </read_first>
  <action>
    Create `test/edge/DeityPassGoldNerfRegression.test.js` mirroring `test/edge/HeroOverrideWeightedRoll.test.js` structure end-to-end. File layout (~550 lines):

    [JSDoc header — ~110 lines]:
    Opening block describes the audit subject: "Phase 294 audit-subject commit `47936e0c` (locked DPNERF-01 gold-tier nerf at `_randTraitTicket` L1731-L1738 inside the existing `if (deity != address(0))` block) + Phase 294 BURNIE gap-closure amendment commit `38319463` (parallel gold-tier branch at `_awardDailyCoinToTraitWinners` L1867-L1874 inline-duplicate site; the BURNIE path is NOT a caller of `_randTraitTicket`, it inlines its own `virtualCount = len/50; if (virtualCount < 2) virtualCount = 2;` because the BURNIE flow is a multi-bucket/1-winner-per-iteration sampler architecturally incompatible with `_randTraitTicket`'s single-bucket/N-winner signature)." Cite both line ranges verbatim per `feedback_verify_call_graph_against_source.md`.

    Path-of-investigation 5-bullet block (mirror HeroOverrideWeightedRoll.test.js L11-L110 style):
    (i)   Mechanic covered: v42 DPNERF gold-tier flat-1 `virtualCount` on `((trait >> 3) & 7) == 7`; common-tier `max(len/50, 2)` UNCHANGED; both ETH (`_randTraitTicket` L1707-L1763) AND BURNIE (`_awardDailyCoinToTraitWinners` L1822-L1913 inline-duplicate) surfaces.
    (ii)  JS-replay oracle + cross-attestation strategy per D-295-INVOKE-01 + D-295-EV-METHODOLOGY-01: hybrid 1,000 JS-replay iterations (750 ETH + 250 BURNIE split proportional to production call frequency) + 16 production cross-attestation iterations (smallest N satisfying per-iteration deity-win-count chi^2 goodness-of-fit at p > 0.05 against analytical 1/(len+1) per-draw expectation; df=1 critical value 3.841 at alpha=0.05). The `_randTraitTicket` function's `private view` visibility + `address[][256] storage` storage-mapping reference parameter makes inheritance-harness invocation infeasible; the JS-replay oracle is the primary methodology per Phase 282/291/293 lineage.
    (iii) D-295-GAS-01 SKIP-GAS posture: no gas measurement at this phase; Phase 294 §5 attestation (+86 byte runtime bytecode delta; per-call gas negligible at ~20-50) is load-bearing for Phase 297 §3.A. Mirrors D-291-GAS-01.
    (iv)  D-295-BURNIE-PATH-01 + D-294-BURNIE-INLINE-01 callsite reach: TST-DPNERF-03 invokes `payDailyCoinJackpot` (selector 0xdbedb1c1) directly to drive the BURNIE inline-duplicate gold-tier branch at L1867-L1874; the `JackpotBurnieWin(winner, lvlPrime, trait, amount, ticketIdx)` event emission at L1899-L1905 carries the deity sentinel pair (`winner == deity` paired with `ticketIdx == type(uint256).max` per L1888-L1893).
    (v)   D-295-CALLSITE-SCOPE-01 — callsite-coverage matrix shipped as a JSDoc table below; callsites 1 (L698 `_runEarlyBirdLootboxJackpot`) + 2 (L988 `_distributeTicketsToBucket`) DEFERRED to Phase 296 SWEEP per D-294-CALLER-UNIFORM-01 SWEEP-scope expansion.

    Callsite-coverage table (5 rows, JSDoc table form):
    | Surface | Line | Function | Coverage |
    | ETH callsite 1 | L698 | `_runEarlyBirdLootboxJackpot` | DEFERRED to Phase 296 SWEEP (D-294-CALLER-UNIFORM-01 SWEEP-scope) |
    | ETH callsite 2 | L988 | `_distributeTicketsToBucket` | DEFERRED to Phase 296 SWEEP |
    | ETH callsite 3 | L1296 | `_processDailyEth` | covered by TST-DPNERF-01 + TST-DPNERF-02 (production path via `payDailyJackpot`) |
    | ETH callsite 4 | L1399 | `_resolveTraitWinners` | covered by TST-DPNERF-04 cross-attestation (production path via `payDailyJackpot` reaches this sub-flow) |
    | BURNIE inline-duplicate | L1867-L1874 | `_awardDailyCoinToTraitWinners` | covered by TST-DPNERF-03 + TST-DPNERF-04 BURNIE half (production path via `payDailyCoinJackpot`) |

    Per-test mapping JSDoc block (one line per TST-DPNERF-NN -> describe block):
    TST-DPNERF-01 — deity-pass + gold-tier ETH trait win -> virtualCount == 1; via production-path `payDailyJackpot`; bucket size ~50; deity-sentinel count in 25-winner draw at gold-tier ≈ 25/(50+1) ≈ 0.49 expected.
    TST-DPNERF-02 — deity-pass + common-tier ETH trait win -> virtualCount == max(len/50, 2) preserved; bucket size ~50; expected deity-sentinel count in 25-winner draw ≈ 25 * 2/(50+2) ≈ 0.96.
    TST-DPNERF-03 — deity-pass + gold-tier BURNIE trait win -> virtualCount == 1 via inline-duplicate site; production-path `payDailyCoinJackpot`; `JackpotBurnieWin` event sentinel observed.
    TST-DPNERF-04 — 1,000-iter EV regression: 750 ETH JS-replay + 250 BURNIE JS-replay; empirical deity virtual-entry total = N × 1 = N = 1,000; 16-iter production cross-attestation confirms JS<->EVM bit-identity (per-iteration deity-win-count chi^2 goodness-of-fit at p > 0.05 vs analytical 1/(len+1)).
    TST-DPNERF-05 — gold-tier trait win without deity (deityBySymbol[fullSymId] == address(0)) -> virtualCount == 0; ZERO deity-sentinel entries in 25-winner draw; all 25 ticketIndexes[i] < len.

    [Imports]:
    `import { expect } from "chai";` + `import hre from "hardhat";` + `import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers.js";` + `import { execSync } from "node:child_process";` + `import { deployFullProtocol, restoreAddresses } from "../helpers/deployFixture.js";` + import helpers from `../helpers/testUtils.js` matching the HRROLL test's import list (`advanceToNextDay`, `getLastVRFRequestId`, `fulfillVRF`, `eth`, `ZERO_ADDRESS`) + `import { randTraitTicketRef, awardDailyCoinPullRef, goldTierVirtualCount, RAND_TRAIT_TICKET_CONSTANTS } from "../helpers/randTraitTicketRef.mjs";`.

    [Module-level constants]:
    `N_EV = 1000` (TST-DPNERF-04 total iteration count) | `N_EV_ETH = 750` | `N_EV_BURNIE = 250` | `N_CROSS = 16` (cross-attestation iteration count per D-295-EV-METHODOLOGY-01) | `BUCKET_SIZE = 50` (holder-bucket size per CONTEXT.md `<specifics>` — yields v42 virtualCount=1 distinct from v41 baseline `max(50/50,2)=2`) | `NUM_WINNERS_ETH = 25` (the standard ETH trait-winner draw cap per `_processDailyEth`) | `GOLD_TRAIT = (0 << 6) | (7 << 3) | 0` (= 56; quadrant 0, color gold, symIdx 0; fullSymId=0) | `COMMON_TRAIT = 0` (quadrant 0, color 0, symIdx 0; fullSymId=0) | `DAILY_ENTROPY = 0x2f02_3456_...` (reuse the HRROLL fixture's pinned entropy at L154-L155 verbatim for cross-phase trace stability) | `CHI2_CRIT_05 = Object.freeze({ 1: 3.841, ... })` (copy verbatim from HeroOverrideWeightedRoll.test.js L141-L149) | `DEITY_SENTINEL_TICKET_IDX = (1n << 256n) - 1n` (mirrors L1757 sentinel).

    [Module-level helpers]:
    - `deriveDeityBySymbolBaseSlot()` — mirror `deriveDailyHeroWagersBaseSlot()` at HRROLL test L245-L288; runs `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout`; parses the `deityBySymbol` row; returns the slot index as BigInt; expected slot index per L965 storage declaration order. Re-validates the Phase 294 §2 EMPTY-diff storage attestation.
    - `deriveTraitBurnTicketBaseSlot()` — same shape; parses the `traitBurnTicket` row at L415.
    - `computeDeityBySymbolSlot(fullSymId, baseSlot)` — mapping(uint8 => address) at base slot: `slot = keccak256(abi.encode(uint256(fullSymId), uint256(baseSlot)))`.
    - `seedDeityBySymbol(gameAddr, fullSymId, deityAddr, baseSlot)` — direct storage write via `hardhat_setStorageAt` to the computed slot with `deityAddr` left-padded to 32 bytes. This is the deity-seeding path; the natural-production alternative (via `DegenerusGameWhaleModule.sol:598`) is more expensive and the test-only direct-write mirrors HRROLL fixture's `seedDailyHeroWagersDirect` at L407-L419.
    - `seedTraitBucket(gameAddr, lvl, trait, holders, baseSlot)` — write a synthetic `traitBurnTicket[lvl][trait]` bucket. Nested mapping slot derivation: `outerSlot = keccak256(abi.encode(uint256(lvl), uint256(baseSlot)))`; the inner `address[][256]` fixed-array element at index `trait` lives at `outerSlot + trait`; the inner `address[]` dynamic-array starts at `keccak256(outerSlot + trait)` for elements and the length lives at `outerSlot + trait`. Set length to `holders.length` then write each holder at `keccak256(outerSlot+trait) + i`.
    - `computeChi2Multinomial(observed, expected)` — copy verbatim from HRROLL test L222-L240.

    [Top-level describe block]:
    `describe("DeityPassGoldNerfRegression — Phase 295 v42.0 DPNERF regression fixture", function() { this.timeout(900_000); after(() => restoreAddresses()); ... })` (mirror HRROLL test L425-L429).

    [Setup-and-sanity sub-describe — wires the imports + storage-slot derivations + JS-replay oracle sanity check]:
    - `it("derives deityBySymbol base slot from forge inspect storageLayout ...", ...)` — call `deriveDeityBySymbolBaseSlot()`, assert BigInt + non-negative.
    - `it("derives traitBurnTicket base slot from forge inspect storageLayout ...", ...)` — same for traitBurnTicket.
    - `it("the JS-replay oracle goldTierVirtualCount returns 1n on gold + deity + bucket=50, 2n on common + deity + bucket=50, 0n on gold + no-deity", ...)` — assert all three branches directly against the helper.
    - `it("the JS-replay oracle randTraitTicketRef returns 25 winners with deity-sentinel count following Bernoulli(1/(50+1)) on gold + deity (single draw smoke check)", ...)` — call the helper with `holders=Array(50).fill(...)`, `deity=<nonzero>`, `trait=GOLD_TRAIT`, `numWinners=25`; assert `winners.length===25` AND `virtualCount===1n` AND deitySentinelMask is consistent (count not asserted at single iteration; chi^2 lives in TST-DPNERF-04).

    [TST-DPNERF-01 describe block]:
    `describe("TST-DPNERF-01 — gold-tier ETH trait win virtualCount == 1", ...)` — load fixture (`loadFixture(deployFullProtocol)`); seed `deityBySymbol[0] = <deity-address>` via `seedDeityBySymbol`; seed `traitBurnTicket[lvl=1][trait=GOLD_TRAIT]` with `BUCKET_SIZE=50` distinct non-deity holders via `seedTraitBucket`; pin daily entropy via `pinDailyEntropy(game, deployer, mockVRF, DAILY_ENTROPY)` (reuse HRROLL helper pattern); invoke `payDailyJackpot` to drive `_processDailyEth -> _randTraitTicket(traitBurnTicket[lvl], DAILY_ENTROPY, GOLD_TRAIT, 25, salt)`; capture the 25-winner draw output. Assert via JS-replay cross-check: call `randTraitTicketRef({ holders, randomWord: DAILY_ENTROPY, trait: GOLD_TRAIT, numWinners: 25, salt: <observed salt>, deity })`; assert `virtualCount === 1n`; assert `deitySentinelMask` is byte-equal between JS oracle output AND EVM-observed winners[i]==deity & ticketIndexes[i]==DEITY_SENTINEL_TICKET_IDX pairs. Single-iteration is sufficient because TST-DPNERF-01 is existence-style (does the gold branch fire); TST-DPNERF-04 covers EV-quantitative.

    [TST-DPNERF-02 describe block]:
    `describe("TST-DPNERF-02 — common-tier ETH trait win virtualCount == max(len/50, 2) preserved", ...)` — same scaffold as TST-DPNERF-01 but with `trait=COMMON_TRAIT` (color 0). Assert `virtualCount === 2n` (since BUCKET_SIZE=50, `len/50 = 1n`, floor pushes to `2n`); JS-replay cross-check on `deitySentinelMask` byte-equality.

    [TST-DPNERF-03 describe block]:
    `describe("TST-DPNERF-03 — BURNIE coin jackpot path gold-tier virtualCount == 1 via inline-duplicate L1867-L1874", ...)` — load fixture; seed `deityBySymbol[0]`; seed `traitBurnTicket[lvlPrime=...][trait=GOLD_TRAIT]` across multiple `lvlPrime` levels covering the `[minLevel, maxLevel]` range that `payDailyCoinJackpot` samples from; pin daily entropy; invoke `payDailyCoinJackpot(lvl, DAILY_ENTROPY, minLevel, maxLevel)` directly per D-295-BURNIE-PATH-01 (natural production-flow, NOT JS-replay-only). Parse the receipt for `JackpotBurnieWin(winner, lvlPrime, trait, amount, ticketIdx)` events; for each event matching trait=GOLD_TRAIT, assert (winner==deity && ticketIdx==DEITY_SENTINEL_TICKET_IDX) iff the corresponding JS-replay `awardDailyCoinPullRef` returned `isDeitySentinel=true`. JS-replay cross-check: invoke `awardDailyCoinPullRef` for each pull `i in [0, cap)` with the observed `lvlPrime` for that pull, and assert byte-equality on `(isDeitySentinel, idx, ticketIdx)` against the EVM event output. JSDoc inline-comment at the top of this describe block MUST cite L1867-L1874 verbatim (the BURNIE inline-duplicate site) and contrast with L1731-L1738 (the ETH `_randTraitTicket` site) — describing what IS, not history.

    [TST-DPNERF-04 describe block]:
    `describe("TST-DPNERF-04 — gold-tile EV regression at N=1000 across both paths", ...)` — implement the hybrid methodology per D-295-EV-METHODOLOGY-01:

    Sub-it 1 — JS-replay 750 ETH iterations:
      For i in [0, 750): seed `holders = Array(50).fill().map((_,j) => addr(i, j))`; `randomWord = uint256(keccak256(abi.encode(DAILY_ENTROPY, i)))`; `salt = i % 256`; call `randTraitTicketRef({ holders, randomWord, trait: GOLD_TRAIT, numWinners: 25, salt, deity })`; accumulate `totalDeityVirtualEntries += virtualCount` (= 1n per iter). Assert `totalDeityVirtualEntries === 750n` (= 750 × 1 = 750 per D-42N-DEITY-EV-01 intentional reduction); separately accumulate `totalDeitySentinelDraws += deitySentinelMask.filter(x => x).length` and assert `totalDeitySentinelDraws / (750 * 25)` is within chi^2 confidence interval of analytical 1/(50+1) per-draw expectation (df=1; critical value 3.841 at alpha=0.05).

    Sub-it 2 — JS-replay 250 BURNIE iterations:
      For i in [0, 250): seed `holders = Array(50)`; `randomWord = uint256(keccak256(...))`; `lvlPrime = 1 + (i % 10)`; call `awardDailyCoinPullRef({ holders, randomWord, trait_i: GOLD_TRAIT, lvlPrime, pullIdx: BigInt(i), deity })`; accumulate `totalBurnieDeityVirtualEntries += virtualCount` (= 1n per iter). Assert total === 250n. Accumulate sentinel count and run chi^2 against 1/(50+1).

    Sub-it 3 — Combined total assertion:
      Assert `totalDeityVirtualEntries + totalBurnieDeityVirtualEntries === BigInt(N_EV)` (= 1000n) per the locked D-42N-DEITY-EV-01 deity-EV-reduction claim. Print informational log: `[TST-DPNERF-04] PASS — JS-replay 1000-iter deity virtual-entry total = 1000; v41 baseline would have been 1000 × max(50/50,2) = 2000`. (Information-only — does NOT assert the v41 baseline as a hard target.)

    Sub-it 4 — 16-iter production cross-attestation per D-295-EV-METHODOLOGY-01:
      For i in [0, 16): load fresh fixture OR roll the same fixture forward (planner discretion in choosing the deterministic scaffold); seed deity + bucket; pin a per-iteration entropy via `keccak256(abi.encode(DAILY_ENTROPY, "CROSS", i))`; invoke `payDailyJackpot` (ETH path); capture the 25-winner output. Run the JS-replay oracle with the same inputs. Assert byte-equal `deitySentinelMask` between JS and EVM for all 16 iterations (= 16 × 25 = 400 paired draws). Run per-iteration chi^2 goodness-of-fit of the EVM deity-win-count vs analytical 1/(50+1) at df=1 critical 3.841; assert chi^2 < 3.841 (p > 0.05) per the locked N=16 disposition.

    [TST-DPNERF-05 describe block]:
    `describe("TST-DPNERF-05 — non-deity holders unaffected on gold-tier trait win", ...)` — load fixture; DO NOT seed `deityBySymbol[0]` (leave at default zero); seed `traitBurnTicket[lvl=1][trait=GOLD_TRAIT]` with 50 holders; pin entropy; invoke `payDailyJackpot`. Assert the 25-winner draw output contains ZERO deity-sentinel entries (`ticketIndexes[i] < BigInt(holders.length)` for ALL i in [0,25); `winners[i] !== address(0)` for ALL i). JS-replay cross-check: `randTraitTicketRef({ ..., deity: ZERO_ADDRESS })` returns `virtualCount === 0n` AND `deitySentinelMask.every(x => x === false) === true`. Run across 5 entropy variations to confirm determinism (5 independent passes; ZERO sentinel entries across all 5).

    [end of file]:
    `restoreAddresses()` already in the top-level `after()` hook. NO `git push` anywhere. NO `console.log` of gas anywhere per D-295-GAS-01.

    Inline comments throughout describe what IS (the v42 mechanic) — ZERO history language (no "previously" / "v41 form" / "used to be" / "was max(len/50, 2)") per `feedback_no_history_in_comments.md`.
  </action>
  <verify>
    <automated>test -f test/edge/DeityPassGoldNerfRegression.test.js &amp;&amp; wc -l test/edge/DeityPassGoldNerfRegression.test.js | awk '{ if ($1 &lt; 500) { print "FAIL: file too short ("$1" lines)"; exit 1 } else print "PASS: " $1 " lines" }'</automated>
    <automated>grep -c 'describe("TST-DPNERF-0[1-5]' test/edge/DeityPassGoldNerfRegression.test.js | awk '{ if ($1 != 5) { print "FAIL: expected 5 TST-DPNERF describe blocks, found "$1; exit 1 } else print "PASS: 5 describe blocks" }'</automated>
    <automated>grep -E "L1731|L1732|L1738|L1867|L1868|L1874" test/edge/DeityPassGoldNerfRegression.test.js | head -5 | wc -l | awk '{ if ($1 &lt; 3) { print "FAIL: missing line-range citations to ETH L1731-L1738 + BURNIE L1867-L1874"; exit 1 } else print "PASS: "$1" line-range citations present" }'</automated>
    <automated>grep -c "callsite-coverage\|L698\|L988\|L1296\|L1399" test/edge/DeityPassGoldNerfRegression.test.js | awk '{ if ($1 &lt; 4) { print "FAIL: callsite-coverage matrix incomplete"; exit 1 } else print "PASS: "$1" callsite-matrix tokens" }'</automated>
    <automated>grep -c "payDailyCoinJackpot\|JackpotBurnieWin" test/edge/DeityPassGoldNerfRegression.test.js | awk '{ if ($1 &lt; 2) { print "FAIL: BURNIE production-path invocation missing"; exit 1 } else print "PASS: "$1" BURNIE-path references" }'</automated>
    <automated>grep -c "randTraitTicketRef\|awardDailyCoinPullRef\|goldTierVirtualCount" test/edge/DeityPassGoldNerfRegression.test.js | awk '{ if ($1 &lt; 3) { print "FAIL: JS-replay oracle imports/usage missing"; exit 1 } else print "PASS: "$1" JS-replay refs" }'</automated>
    <automated>grep -v '^\s*//' test/edge/DeityPassGoldNerfRegression.test.js | grep -v '^\s*\*' | grep -Ec "previously|v41 form|used to be|was max\(len/50, 2\)|was 2% of bucket" | awk '{ if ($1 != 0) { print "FAIL: history language found in code ("$1" matches)"; exit 1 } else print "PASS: zero history language in code body" }'</automated>
    <automated>! grep -E "console\.log.*[Gg]as\b" test/edge/DeityPassGoldNerfRegression.test.js &amp;&amp; ! grep -E "expect.*gasUsed" test/edge/DeityPassGoldNerfRegression.test.js</automated>
    <automated>git diff --name-only HEAD -- contracts/ | wc -l | awk '{ if ($1 != 0) { print "FAIL: contracts/ touched - "$1" file(s) modified"; exit 1 } else print "PASS: zero contracts/ modifications" }'</automated>
    <automated>npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js 2>&amp;1 | tee /tmp/295-test-run.log | tail -40; grep -E "passing|failing" /tmp/295-test-run.log | tail -3</automated>
  </verify>
  <done>
    `test/edge/DeityPassGoldNerfRegression.test.js` exists; ≥500 lines; 5 `describe("TST-DPNERF-0N` blocks; JSDoc header includes callsite-coverage table with L698/L988/L1296/L1399 + BURNIE L1867 citations; ETH `_randTraitTicket` L1731-L1738 + BURNIE `_awardDailyCoinToTraitWinners` L1867-L1874 line ranges cited; `payDailyCoinJackpot` + `JackpotBurnieWin` references present for TST-DPNERF-03; all 3 JS-replay-helper exports imported; ZERO history language in code body; ZERO gas measurement (no `console.log` of gas; no `gasUsed` assertions) per D-295-GAS-01; ZERO contracts/ files modified; `npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js` exits 0 with all tests passing (TST-DPNERF-01..05 + setup-and-sanity sub-describe + cross-attestation; expected ≥ 10 it-block passes total).
  </done>
</task>

<task type="auto">
  <name>Task 3: Grep-verify BURNIE inline-duplicate site against contract source per feedback_verify_call_graph_against_source.md</name>
  <files></files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol L1707-L1763 (ETH `_randTraitTicket` body; gold-tier branch at L1732)
    - contracts/modules/DegenerusGameJackpotModule.sol L1822-L1913 (BURNIE `_awardDailyCoinToTraitWinners` body; gold-tier inline-duplicate at L1868)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md Decision-Anchor table row D-294-BURNIE-INLINE-01 + §(iv) Path-Coverage Trade-Offs subsection (c)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Per `feedback_verify_call_graph_against_source.md`, verify the test JSDoc callsite-coverage table is consistent with the live contract source. Run the following grep commands and capture the output:

    1. `grep -n "if (((trait >> 3) & 7) == 7)" contracts/modules/DegenerusGameJackpotModule.sol` — MUST return exactly L1732 (the ETH `_randTraitTicket` gold-tier branch).

    2. `grep -n "if (((trait_i >> 3) & 7) == 7)" contracts/modules/DegenerusGameJackpotModule.sol` — MUST return exactly L1868 (the BURNIE `_awardDailyCoinToTraitWinners` gold-tier inline-duplicate).

    3. `grep -c "_randTraitTicket(" contracts/modules/DegenerusGameJackpotModule.sol` — MUST be at least 5 (1 declaration at L1707 + 4 callsites at L698, L988, L1296, L1399). NOTE: this count is informational; the BURNIE path is NOT a caller — `payDailyCoinJackpot -> _awardDailyCoinToTraitWinners` is structurally independent and inlines the duplicate at L1868.

    4. `grep -c "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol` — MUST be exactly 2 (ETH L1733 + BURNIE L1869; both surfaces carry the locked gold-tier flat-1 disposition per D-42N-GOLD-FLOOR-01).

    5. `grep -c "virtualCount = len / 50;" contracts/modules/DegenerusGameJackpotModule.sol` — MUST be exactly 2 (ETH L1735 + BURNIE L1871; both surfaces preserve the v41 common-tier formula UNCHANGED per D-42N-DEITY-EV-01).

    If ANY of the 5 greps returns an unexpected count or unexpected line number, FAIL the task and report the discrepancy. The JSDoc callsite-coverage table in `test/edge/DeityPassGoldNerfRegression.test.js` is the audit-deliverable form of this verification per `feedback_verify_call_graph_against_source.md`; this task is the explicit re-validation step that the JSDoc claims match the live source pre-commit.

    No files modified by this task. Output is captured to the executor's stdout for the diff-review record at Task 4.
  </action>
  <verify>
    <automated>grep -n "if (((trait >> 3) &amp; 7) == 7)" contracts/modules/DegenerusGameJackpotModule.sol | grep -c "1732:" | awk '{ if ($1 != 1) { print "FAIL: ETH gold-tier branch not at L1732"; exit 1 } else print "PASS: ETH gold-tier at L1732" }'</automated>
    <automated>grep -n "if (((trait_i >> 3) &amp; 7) == 7)" contracts/modules/DegenerusGameJackpotModule.sol | grep -c "1868:" | awk '{ if ($1 != 1) { print "FAIL: BURNIE inline-duplicate not at L1868"; exit 1 } else print "PASS: BURNIE inline-duplicate at L1868" }'</automated>
    <automated>grep -c "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol | awk '{ if ($1 != 2) { print "FAIL: expected 2 virtualCount=1 sites (ETH+BURNIE), found "$1; exit 1 } else print "PASS: 2 virtualCount=1 sites" }'</automated>
    <automated>grep -c "virtualCount = len / 50;" contracts/modules/DegenerusGameJackpotModule.sol | awk '{ if ($1 != 2) { print "FAIL: expected 2 common-tier formulas (ETH+BURNIE), found "$1; exit 1 } else print "PASS: 2 common-tier formulas preserved" }'</automated>
    <automated>grep -c "_randTraitTicket(" contracts/modules/DegenerusGameJackpotModule.sol | awk '{ if ($1 &lt; 5) { print "FAIL: expected ≥5 _randTraitTicket sites (1 decl + 4 callsites), found "$1; exit 1 } else print "PASS: "$1" _randTraitTicket sites" }'</automated>
  </verify>
  <done>
    All 5 grep verifications pass: ETH gold-tier branch confirmed at L1732; BURNIE inline-duplicate confirmed at L1868; exactly 2 `virtualCount = 1;` sites (gold-tier-flat at both surfaces); exactly 2 `virtualCount = len / 50;` sites (common-tier preserved at both surfaces); ≥5 `_randTraitTicket(` occurrences (1 declaration + ≥4 ETH callsites per D-294-CALLER-UNIFORM-01); BURNIE path is NOT among the callers (the inline-duplicate is verified at L1868 separately, NOT via a call to `_randTraitTicket`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: [BLOCKING] User reviews diff before commit per feedback_manual_review_before_push.md</name>
  <what-built>
    Two new files staged for review:
    1. `test/helpers/randTraitTicketRef.mjs` — pure-function JS bit-mirror of `_randTraitTicket` L1707-L1763 (ETH 25-winner draw) + `_awardDailyCoinToTraitWinners` per-pull inline gold-tier block L1860-L1894 (BURNIE 1-winner draw). 3 named exports (`goldTierVirtualCount`, `randTraitTicketRef`, `awardDailyCoinPullRef`) + frozen constants export.
    2. `test/edge/DeityPassGoldNerfRegression.test.js` — TST-DPNERF-01..05 regression fixture with JSDoc path-of-investigation header + 5-row callsite-coverage table citing both ETH L1731-L1738 + BURNIE L1867-L1874 audit-subject sites per `feedback_verify_call_graph_against_source.md`. Uses production-path `payDailyJackpot` for TST-DPNERF-01/02/05 + `payDailyCoinJackpot` for TST-DPNERF-03 + hybrid 1,000-iter JS-replay (750 ETH + 250 BURNIE) + 16-iter production cross-attestation for TST-DPNERF-04 per D-295-EV-METHODOLOGY-01. NO gas measurement per D-295-GAS-01. ZERO contracts/ mutations.

    Task 3 grep-verification PASSED: ETH gold-tier branch at L1732, BURNIE inline-duplicate at L1868, both surfaces carry the locked flat-1 disposition; `_randTraitTicket` has 1 declaration + 4 ETH callsites; BURNIE path is structurally independent (inline-duplicate, NOT a caller).

    `npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js` PASSED (per Task 2 verification log captured at /tmp/295-test-run.log).
  </what-built>
  <how-to-verify>
    Per `feedback_manual_review_before_push.md` + `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`:

    1. Run `git status` — confirm exactly 2 new untracked files staged (`test/helpers/randTraitTicketRef.mjs` + `test/edge/DeityPassGoldNerfRegression.test.js`); ZERO modifications to existing files; ZERO contracts/ touches.

    2. Run `git diff --stat HEAD -- contracts/` — MUST be EMPTY (zero contracts/ modifications per `feedback_no_contract_commits.md` + D-295-INVOKE-01 escalation NOT invoked).

    3. Review `test/helpers/randTraitTicketRef.mjs` (~150 lines):
       - JSDoc header cites Phase 294 commit `47936e0c` + BURNIE gap-closure commit `38319463`
       - 3 named exports: `goldTierVirtualCount`, `randTraitTicketRef`, `awardDailyCoinPullRef`
       - ZERO history language ("previously" / "v41 form" / "used to be")
       - Bit-mirror cross-references every Solidity line at L1718-L1762 (ETH) + L1853-L1893 (BURNIE)

    4. Review `test/edge/DeityPassGoldNerfRegression.test.js` (~550 lines):
       - JSDoc header has the 5-bullet path-of-investigation block AND the 5-row callsite-coverage table
       - Callsite-coverage table cites L698 + L988 + L1296 + L1399 + L1867 by line number
       - 5 `describe("TST-DPNERF-0[1-5]"...)` blocks present
       - TST-DPNERF-03 invokes `payDailyCoinJackpot` natural production-flow (NOT JS-replay-only)
       - TST-DPNERF-04 sub-its: 750 ETH JS-replay + 250 BURNIE JS-replay + combined assertion (total=1000) + 16-iter production cross-attestation
       - TST-DPNERF-05 asserts ZERO deity-sentinel entries with `deityBySymbol[0]==address(0)`
       - ZERO `console.log` of gas; ZERO `gasUsed` assertions (D-295-GAS-01 SKIP)
       - ZERO history language in code body

    5. Run `npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js` one more time interactively — confirm exit code 0 + all tests passing.

    6. Type `approved` to authorize the batched commit in Task 5, OR describe issues to surface back to the planner.

    USER-APPROVAL CONDITIONS:
    - If the diff is acceptable as-is → reply with `approved` and Task 5 proceeds with the single batched commit.
    - If you spot any issue (history-language slip, gas measurement leakage, scope creep, missing callsite citation, etc.) → describe the issue; the executor revises and re-presents.
    - If you want to invoke the D-295-INVOKE-01 escalation path (visibility-flip Phase 294 amendment) → state that explicitly; the executor returns to planning with the escalation requirement so a Phase 294 contract amendment can be re-planned under the batched-approval discipline.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the batched commit; or describe issues for the executor to revise</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Single batched USER-APPROVED commit at phase close per feedback_batch_contract_approval.md</name>
  <files>
    test/helpers/randTraitTicketRef.mjs
    test/edge/DeityPassGoldNerfRegression.test.js
  </files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
    - Recent commit messages: `git log --oneline -10` (mirror the Phase 294 commit subject style; precedent at commit `38319463 feat(294): extend DPNERF gold nerf to BURNIE coin path [DPNERF-02,03] [USER-APPROVED]` + commit `6d117c80 docs(phase-294): re-verify after BURNIE gap closure (status: passed)`)
  </read_first>
  <action>
    AFTER user approval at Task 4, stage and commit the two new test files in a SINGLE batched commit per `feedback_batch_contract_approval.md`. Commit subject mirrors the Phase 291/293/294 lineage:

    `test(295): DPNERF regression fixture — TST-DPNERF-01..05 [USER-APPROVED]`

    Commit body (HEREDOC):
      - Lists the 2 files added with one-line description each
      - Cites the audit-subject commits: Phase 294 `47936e0c` (locked DPNERF-01 gold-tier nerf at `_randTraitTicket` L1731-L1738) + Phase 294 BURNIE gap-closure `38319463` (parallel branch at `_awardDailyCoinToTraitWinners` L1867-L1874 inline-duplicate)
      - Lists the 5 TST-DPNERF requirements addressed
      - Cites the 5 D-295-* decision anchors (D-295-EV-METHODOLOGY-01, D-295-BURNIE-PATH-01, D-295-GAS-01, D-295-CALLSITE-SCOPE-01, D-295-INVOKE-01)
      - Notes the audit-feedback discipline followed: zero contracts/ mutations (`feedback_no_contract_commits.md`), single batched commit (`feedback_batch_contract_approval.md`), user-approved before commit (`feedback_manual_review_before_push.md` + `feedback_never_preapprove_contracts.md`), no history language (`feedback_no_history_in_comments.md`), grep-verified BURNIE inline-duplicate site (`feedback_verify_call_graph_against_source.md`)
      - `[USER-APPROVED]` trailer

    Commands (execute in order):
      1. `git status` — confirm only the 2 new files untracked, NO modifications to existing files (especially zero contracts/ + zero pre-existing test/* mutations).
      2. `git add test/helpers/randTraitTicketRef.mjs test/edge/DeityPassGoldNerfRegression.test.js` — stage by explicit filename only (do NOT use `git add .` or `git add -A` per `feedback_no_contract_commits.md` general staging discipline).
      3. `git diff --cached --stat` — confirm exactly 2 files staged + zero other changes; print to stdout for the audit record.
      4. `git commit -m "$(cat <<'EOF'
test(295): DPNERF regression fixture — TST-DPNERF-01..05 [USER-APPROVED]

Ships TST-DPNERF-01..05 regression fixture against the Phase 294 audit-subject
commit 47936e0c (locked DPNERF-01 gold-tier nerf at _randTraitTicket L1731-L1738
inside the existing `if (deity != address(0))` block) + the BURNIE gap-closure
amendment commit 38319463 (parallel gold-tier branch at
_awardDailyCoinToTraitWinners L1867-L1874 inline-duplicate site per
D-294-BURNIE-INLINE-01; the BURNIE path is NOT a caller of _randTraitTicket
— architecturally incompatible due to single-bucket/N-winner vs
multi-bucket/1-winner-per-iteration sampler shape).

Files added:
- test/helpers/randTraitTicketRef.mjs — pure-function JS bit-mirror of
  _randTraitTicket (L1707-L1763 ETH 25-winner draw) + per-pull inline gold-tier
  block in _awardDailyCoinToTraitWinners (L1860-L1894 BURNIE 1-winner draw).
  3 named exports: goldTierVirtualCount, randTraitTicketRef,
  awardDailyCoinPullRef.
- test/edge/DeityPassGoldNerfRegression.test.js — TST-DPNERF-01..05 fixture
  with JSDoc path-of-investigation header + 5-row callsite-coverage table.
  Production-path payDailyJackpot (TST-DPNERF-01/02/05) + payDailyCoinJackpot
  (TST-DPNERF-03) + hybrid 1,000-iter JS-replay (750 ETH + 250 BURNIE) +
  16-iter production cross-attestation (TST-DPNERF-04).

Requirements addressed: TST-DPNERF-01, TST-DPNERF-02, TST-DPNERF-03,
                         TST-DPNERF-04, TST-DPNERF-05.

Decision anchors honored:
- D-295-EV-METHODOLOGY-01: hybrid 1000 JS-replay + 16 production cross-attestation
- D-295-BURNIE-PATH-01: natural-production payDailyCoinJackpot for TST-DPNERF-03
- D-295-GAS-01: SKIP empirical gas (Phase 294 §5 attestation load-bearing for §3.A)
- D-295-CALLSITE-SCOPE-01: cover callsites 3+4+BURNIE; defer 1+2 to Phase 296 SWEEP
- D-295-INVOKE-01: JS-replay primary; visibility-flip escalation NOT invoked

Audit-feedback discipline:
- feedback_no_contract_commits.md: ZERO contracts/ mutations
- feedback_batch_contract_approval.md: single batched commit at phase close
- feedback_manual_review_before_push.md + feedback_never_preapprove_contracts.md:
  user reviewed diff + typed `approved` before this commit
- feedback_no_history_in_comments.md: helper + test JSDoc describe v42 mechanic
  only; zero history language ("previously" / "v41 form" / "used to be")
- feedback_verify_call_graph_against_source.md: BURNIE inline-duplicate site
  at L1868 grep-verified vs live source pre-commit; ETH gold-tier branch at
  L1732 verified; both surfaces carry the locked virtualCount=1 disposition
- feedback_gas_worst_case.md: no empirical gas (theoretical-first attestation
  already shipped at Phase 294 §5; +86 byte runtime bytecode delta is the
  load-bearing reference cited at Phase 297 §3.A)
- feedback_frozen_contracts_no_future_proofing.md: helpers frozen-at-write;
  no extensibility hooks; bit-mirror EVM verbatim

Test status: npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js
            passes (all TST-DPNERF-01..05 + setup-and-sanity + cross-attestation).

[USER-APPROVED]
EOF
)"`
      5. `git status` — confirm clean working tree post-commit.
      6. `git log --oneline -1` — print the new commit SHA + subject for the audit record.

    DO NOT run `git push` — per `feedback_manual_review_before_push.md`, push requires a separate explicit user instruction.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -E "test\(295\):.*TST-DPNERF.*USER-APPROVED" | wc -l | awk '{ if ($1 != 1) { print "FAIL: commit subject not matching test(295):...TST-DPNERF...[USER-APPROVED]"; exit 1 } else print "PASS: commit subject matches" }'</automated>
    <automated>git diff HEAD~1 HEAD --name-only | sort | awk 'BEGIN { count=0 } /^test\/edge\/DeityPassGoldNerfRegression\.test\.js$/ { count++ } /^test\/helpers\/randTraitTicketRef\.mjs$/ { count++ } END { if (count != 2) { print "FAIL: expected exactly 2 files in commit, got "count; exit 1 } else print "PASS: 2 files in commit" }'</automated>
    <automated>git diff HEAD~1 HEAD --name-only -- contracts/ | wc -l | awk '{ if ($1 != 0) { print "FAIL: contracts/ touched in commit"; exit 1 } else print "PASS: zero contracts/ in commit" }'</automated>
    <automated>git show HEAD --stat | grep -E "USER-APPROVED" | wc -l | awk '{ if ($1 &lt; 1) { print "FAIL: USER-APPROVED trailer missing"; exit 1 } else print "PASS: USER-APPROVED trailer present" }'</automated>
    <automated>git status --porcelain | wc -l | awk '{ if ($1 != 0) { print "FAIL: working tree not clean post-commit"; exit 1 } else print "PASS: clean working tree" }'</automated>
    <automated>! git log --oneline -2 | grep -i "push"</automated>
  </verify>
  <done>
    ONE new commit on HEAD: subject `test(295): DPNERF regression fixture — TST-DPNERF-01..05 [USER-APPROVED]`; commit contains exactly 2 files (`test/helpers/randTraitTicketRef.mjs` + `test/edge/DeityPassGoldNerfRegression.test.js`); ZERO contracts/ files in the commit; `[USER-APPROVED]` trailer present in commit body; working tree clean; NO `git push` executed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only fixture under `test/edge/` + `test/helpers/`; ZERO production contract changes; ZERO new external surface; ZERO new admin/upgrade entry points; ZERO new modifiers; ZERO new state |

## STRIDE Threat Register

No production threats — test-only fixture. Test-side discipline review against the same `./CLAUDE.md` standards (loaded from `~/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/` audit-feedback files):

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-295-01 | Tampering | test/edge/DeityPassGoldNerfRegression.test.js fixture state seeding | mitigate | Direct storage writes via `hardhat_setStorageAt` to `deityBySymbol` + `traitBurnTicket` slots derived from `forge inspect storageLayout` at test runtime (mirrors HRROLL fixture's `seedDailyHeroWagersDirect` pattern at L407-L419); base-slot indices re-validated at every test run vs the v41 audit baseline. Natural-production-flow alternative via `DegenerusGameWhaleModule.sol:598` (deity-pass purchase) + `_processOneTicketEntry` (ticket bucket population) is more expensive scaffolding but identical attestation; planner-discretion at executor-time |
| T-295-02 | Information Disclosure | test/helpers/randTraitTicketRef.mjs JS-replay oracle bit-identity | mitigate | The 16-iter production cross-attestation in TST-DPNERF-04 asserts byte-equal `deitySentinelMask` between JS oracle and EVM-observed winners across 16 × 25 = 400 paired ETH draws; chi^2 goodness-of-fit at df=1 critical 3.841 (p > 0.05) validates per-iteration deity-win-count vs analytical 1/(50+1); any JS<->EVM bit-divergence at the keccak-input encoding (`abi.encode` vs `abi.encodePacked`) or the `% effectiveLen` BigInt arithmetic surfaces as cross-attestation failure |
| T-295-03 | Repudiation | Test JSDoc claims vs live contract source | mitigate | Task 3 grep-verification step asserts the JSDoc callsite-coverage table claims (ETH L1732 + BURNIE L1868) match the live source pre-commit per `feedback_verify_call_graph_against_source.md` Phase 294 BURNIE gap precedent; the JSDoc is the audit-deliverable form of the verification |
| T-295-04 | Denial of Service | Test runtime explosion on TST-DPNERF-04 1,000-iter EV regression | accept | Per D-295-EV-METHODOLOGY-01, the 1,000-iter loop runs as pure JS-replay (deterministic, ~second-scale wall-clock per the sister Phase 282/291/293 oracle benchmarks); only the 16-iter cross-attestation invokes production-path `payDailyJackpot` (~minute-scale wall-clock total); `this.timeout(900_000)` (15-minute mocha test timeout) at top-level describe provides headroom; if cross-attestation runtime exceeds budget at executor-time, N may be reduced within the 16-32 band per D-295-EV-METHODOLOGY-01 (chi^2 goodness-of-fit still applies at smaller N) |
| T-295-SC | Tampering | npm/pip/cargo installs | n/a | NO new package installs in this plan; reuses `ethers`, `chai`, `hardhat`, `@nomicfoundation/hardhat-toolbox` already in package.json; package legitimacy audit not required (zero new dependencies); `feedback_verify_call_graph_against_source.md` package-legitimacy gate is N/A here |

The fixture treats Phase 294 audit-subject commit `47936e0c` + BURNIE gap-closure `38319463` as frozen — no contract amendments per `feedback_frozen_contracts_no_future_proofing.md`. The D-295-INVOKE-01 visibility-flip escalation path is NOT pre-approved per `feedback_never_preapprove_contracts.md` and surfaces as a Task 4 user-decision option, NOT a silent automation.
</threat_model>

<verification>
## End-of-Phase Verification

After Task 5 completes, the following invariants MUST hold:

1. **Two new files committed:**
   - `test/helpers/randTraitTicketRef.mjs` (~150 lines; pure-function JS bit-mirror)
   - `test/edge/DeityPassGoldNerfRegression.test.js` (~550 lines; TST-DPNERF-01..05 fixture)

2. **ZERO contracts/ files modified** (`git diff HEAD~1 HEAD -- contracts/` is EMPTY).

3. **All 5 TST-DPNERF requirements implemented and PASSING:**
   - TST-DPNERF-01: gold-tier ETH virtualCount=1 (production-path replay)
   - TST-DPNERF-02: common-tier ETH virtualCount=2 preserved (BUCKET_SIZE=50)
   - TST-DPNERF-03: BURNIE path virtualCount=1 via inline-duplicate L1868 (production-path `payDailyCoinJackpot`)
   - TST-DPNERF-04: 1,000-iter JS-replay deity virtual-entry total = 1000 (750 ETH + 250 BURNIE) + 16-iter production cross-attestation chi^2 < 3.841
   - TST-DPNERF-05: non-deity gold-tier virtualCount=0 (ZERO deity sentinels)

4. **All 5 D-295-* decisions honored** (cited in must_haves.truths; visible in test JSDoc + helper JSDoc):
   - D-295-EV-METHODOLOGY-01 (hybrid 1000+16)
   - D-295-BURNIE-PATH-01 (natural-flow `payDailyCoinJackpot`)
   - D-295-GAS-01 (SKIP gas; ZERO `console.log` of gas; ZERO `gasUsed` assertions)
   - D-295-CALLSITE-SCOPE-01 (5-row JSDoc callsite-coverage table)
   - D-295-INVOKE-01 (JS-replay primary; visibility-flip escalation NOT invoked)

5. **All audit-feedback constraints honored:**
   - `feedback_no_contract_commits.md` — ZERO contracts/ mutations
   - `feedback_batch_contract_approval.md` — single batched commit at close
   - `feedback_manual_review_before_push.md` — Task 4 [BLOCKING] human-verify before Task 5 commit
   - `feedback_never_preapprove_contracts.md` — D-295-INVOKE-01 escalation NOT pre-approved
   - `feedback_no_history_in_comments.md` — ZERO history language in test/* code body
   - `feedback_verify_call_graph_against_source.md` — Task 3 grep-verification PASSED (ETH L1732 + BURNIE L1868)
   - `feedback_gas_worst_case.md` — no empirical gas; Phase 294 §5 attestation load-bearing
   - `feedback_frozen_contracts_no_future_proofing.md` — helpers frozen-at-write; no extensibility hooks
   - `feedback_skip_research_test_phases.md` — directly to plan; no research phase invoked

6. **Test suite green:** `npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js` exits 0 with all it-blocks passing.

7. **No git push executed** (push requires separate explicit user instruction).
</verification>

<success_criteria>
Phase 295 is complete when:

- [ ] `test/helpers/randTraitTicketRef.mjs` exists with 3 named function exports (`goldTierVirtualCount`, `randTraitTicketRef`, `awardDailyCoinPullRef`) + frozen constants export
- [ ] `test/edge/DeityPassGoldNerfRegression.test.js` exists with 5 `describe("TST-DPNERF-0N"...)` blocks + setup-and-sanity sub-describe + cross-attestation sub-describe inside TST-DPNERF-04
- [ ] JSDoc header includes 5-bullet path-of-investigation block AND 5-row callsite-coverage table citing L698/L988/L1296/L1399/L1867 by line number
- [ ] Task 3 grep-verification PASSED (ETH L1732 + BURNIE L1868 both confirmed)
- [ ] `npx hardhat test test/edge/DeityPassGoldNerfRegression.test.js` exits 0
- [ ] Task 4 [BLOCKING] human-verify checkpoint resolved with `approved`
- [ ] ONE batched commit on HEAD: subject `test(295): DPNERF regression fixture — TST-DPNERF-01..05 [USER-APPROVED]`
- [ ] Commit contains exactly 2 files; ZERO contracts/ touches
- [ ] `[USER-APPROVED]` trailer present in commit body
- [ ] No `git push` executed
- [ ] Working tree clean post-commit
</success_criteria>

<output>
Create `.planning/phases/295-dpnerf-regression-fixture-tst-dpnerf/295-01-SUMMARY.md` when Task 5 completes. Summary must:
- List the 2 files added with line counts
- Cite the audit-subject commits (Phase 294 `47936e0c` + BURNIE gap-closure `38319463`)
- Tabulate TST-DPNERF-01..05 coverage matrix (per-requirement → describe block → disposition PASS)
- Tabulate D-295-* decision-anchor coverage (each anchor → fixture vehicle → disposition HONORED)
- Cite the commit SHA + subject
- Self-check section confirming all `<verification>` invariants hold
- Carry-forward notes to Phase 296 SWEEP (DPNERF hypothesis surface; the 4 SWEEP-02(iii) expected dispositions from Phase 294 carry forward; callsites 1+2 deferred-coverage handoff per D-295-CALLSITE-SCOPE-01) + Phase 297 §3.A (the test fixture is one of three v42-surface closures — MINTCLN at Phase 291, HRROLL at Phase 293, DPNERF at Phase 295 — that Phase 297 cites as the regression-coverage row alongside the contract-side delta surface row)
</output>
