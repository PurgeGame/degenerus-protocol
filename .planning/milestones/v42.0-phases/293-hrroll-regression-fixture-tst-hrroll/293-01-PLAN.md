---
phase: 293-hrroll-regression-fixture-tst-hrroll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/rollHeroSymbolRef.mjs
autonomous: true
requirements: [TST-HRROLL-01, TST-HRROLL-02, TST-HRROLL-03, TST-HRROLL-04, TST-HRROLL-05]
requirements_addressed: [TST-HRROLL-01, TST-HRROLL-02, TST-HRROLL-03, TST-HRROLL-04, TST-HRROLL-05]

must_haves:
  truths:
    - "test/helpers/rollHeroSymbolRef.mjs (new file) exports a pure-function bit-mirror of contracts/modules/DegenerusGameJackpotModule.sol:1639-1700 _rollHeroSymbol body"
    - "The reference function accepts ({ day: uint32, entropy: uint256-as-BigInt, dailyHeroWagers: BigInt[4] }) and returns { hasWinner: boolean, winQuadrant: 0..3, winSymbol: 0..7 }"
    - "Pass 1 decodes 32 × uint32 amounts via Number((packed >> BigInt(s*32)) & 0xFFFFFFFFn) into a flat weights array of length 32 indexed by (q << 3) | s; tracks total (BigInt uint64-masked), maxAmount (uint32), leaderIdx (first-seen-on-strict-greater per D-42N-DETERMINISM-01)"
    - "Total==0 early-bail returns { hasWinner:false, winQuadrant:0, winSymbol:0 } matching the EVM (false,0,0) tuple — HRROLL-01 zero-wager invariant"
    - "Pass 2 computes pick = (BigInt(ethers.keccak256(AbiCoder.defaultAbiCoder().encode([\"uint256\",\"uint32\"], [entropy, day]))) % effectiveTotal) and walks cumulative cursor flat-idx ascending; leaderBonus = maxAmount / 2 (integer divide) added at idx == leaderIdx; returns (true, idx>>3, idx & 7) on the first cumulative > pick — matches contracts/modules/DegenerusGameJackpotModule.sol:1681-1699 verbatim"
    - "Helper is a pure ES module export (no chai plugin, no extension of expect, no side effects, no test runner integration); mirror of test/helpers/raritySymbolBatchRef.mjs shape per the Phase 282/291 file-separation pattern"
    - "Helper does NOT extend test/helpers/raritySymbolBatchRef.mjs in-place (that file is the v41/v42 mint-batch oracle; different audit subject) — new sibling file only"
    - "Zero contracts/ mutations (helper-only plan)"
    - "Zero git commits in this plan — single USER-APPROVED batched commit lives at Plan 02 close per feedback_batch_contract_approval.md"
  artifacts:
    - path: "test/helpers/rollHeroSymbolRef.mjs"
      provides: "JS-replay oracle for _rollHeroSymbol; consumed by Plan 02 TST-HRROLL-01..05 assertions"
      contains: "export function rollHeroSymbolRef"
  key_links:
    - from: "test/helpers/rollHeroSymbolRef.mjs::rollHeroSymbolRef"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:1639-1700 _rollHeroSymbol body"
      via: "verbatim port of pass-1 amount decode + pass-2 cumulative cursor walk + leaderBonus add at idx==leaderIdx"
      pattern: "AbiCoder.*encode.*uint256.*uint32"
    - from: "test/helpers/rollHeroSymbolRef.mjs keccak input"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:1684 keccak256(abi.encode(entropy, day))"
      via: "ethers AbiCoder.defaultAbiCoder().encode([\"uint256\",\"uint32\"], [entropy, day]) byte-equivalence to Solidity abi.encode (D-42N-DETERMINISM-01)"
      pattern: "keccak256\\(.*abiCoder|defaultAbiCoder\\(\\)\\.encode"
---

<objective>
Ship `test/helpers/rollHeroSymbolRef.mjs` — a pure-function ES-module bit-mirror of `_rollHeroSymbol` at `contracts/modules/DegenerusGameJackpotModule.sol:1639-1700` (audit-subject commit `a0218952`). This helper is the JS-replay oracle that Plan 02 drives at N=10,000 iterations for TST-HRROLL-01 (chi² weighted-distribution) + TST-HRROLL-02 (×1.5 leader-bonus binomial) + the small-N edge case fixtures TST-HRROLL-04 (single-bettor) + TST-HRROLL-05 (zero-wager), and against captured `dailyHeroWagers[D][q]` storage bytes for TST-HRROLL-03 (commitment-window).

Purpose: D-293-INVOKE-01 default disposition — `_rollHeroSymbol` is `private view` so no inheritance harness can reach it; ALGORITHM_VERIFIED via JS-replay oracle (Phase 282 → Phase 291 lineage). The helper is the load-bearing oracle for 5 of the 6 TST-HRROLL assertions; cross-attestation JS↔EVM happens in Plan 02 Task 6 via a small-N production-path replay through `payDailyJackpot` parsing the `DailyWinningTraits` event.

Output: ONE new file `test/helpers/rollHeroSymbolRef.mjs`. Zero `contracts/` mutations. NO `git commit` in this plan — the executor stages the file and defers commit to Plan 02's USER-APPROVED batched-commit checkpoint per `feedback_batch_contract_approval.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-02-SUMMARY.md
@.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-01-PLAN.md
@test/helpers/raritySymbolBatchRef.mjs
@contracts/modules/DegenerusGameJackpotModule.sol

<interfaces>
Live `_rollHeroSymbol` body (contracts/modules/DegenerusGameJackpotModule.sol:1639-1700) — verbatim port target:

Function signature: `function _rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)`

Pass-1 structure (L1647-L1675):
  - Declare `uint32[32] memory weights; uint64 total; uint32 maxAmount; uint8 leaderIdx;`
  - Outer loop `for (uint8 q; q < 4; ) { uint256 packed = dailyHeroWagers[day][q]; ... ++q }`
  - Inner loop `for (uint8 s; s < 8; ) { uint32 amount = uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF); uint8 idx = (q << 3) | s; weights[idx] = amount; total += uint64(amount); if (amount > maxAmount) { maxAmount = amount; leaderIdx = idx; } ++s }`
  - Strict-`>` tie-break = first-seen wins (per D-42N-DETERMINISM-01 leader-bonus tie-break rule)

Early-bail (L1677-L1679): `if (total == 0) return (false, 0, 0);`

Pass-2 structure (L1681-L1699):
  - `uint64 leaderBonus = uint64(maxAmount) / 2;` — integer divide
  - `uint64 effectiveTotal = total + leaderBonus;`
  - `uint64 pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal);` — abi.encode (NOT encodePacked) per D-42N-DETERMINISM-01
  - `uint64 cumulative; for (uint8 idx; idx < 32; ) { cumulative += uint64(weights[idx]); if (idx == leaderIdx) { cumulative += leaderBonus; } if (cumulative > pick) { return (true, uint8(idx >> 3), uint8(idx & 7)); } ++idx }`
  - Implicit fall-through returns named-return defaults (false, 0, 0) — unreachable by invariant `pick < effectiveTotal == sum(weights) + leaderBonus`; preserved by Phase 292 per `feedback_no_dead_guards.md`

`dailyHeroWagers` packed layout (contracts/storage/DegenerusGameStorage.sol:1470-1475):
  - `mapping(uint32 => uint256[4]) internal dailyHeroWagers`
  - Each `uint256` packs 8 × `uint32` amounts (32-bit fields) — symbol s within quadrant q at bit offset `s*32`
  - Amount units: 1e12 wei (max ~4,295 ETH/symbol/day before saturation)

Sister helper shape to mirror (test/helpers/raritySymbolBatchRef.mjs L1-L80):
  - Pure ES module imports from `ethers`: `AbiCoder`, `keccak256`, `getBytes` (only as needed)
  - Module-level constant `U64_MASK = (1n << 64n) - 1n` for uint64 masking
  - Top-of-file JSDoc block citing the live contract line range
  - Single primary exported function + secondary helper exports (no chai plugin, no expect extension)

Existing helpers in scope (DO NOT extend in-place):
  - `test/helpers/raritySymbolBatchRef.mjs` — Phase 282/291 mint-batch oracle; out-of-scope audit subject (per 293-CONTEXT.md Out-of-Scope Source-Tree Surfaces)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/helpers/rollHeroSymbolRef.mjs with the verbatim port of _rollHeroSymbol</name>
  <files>test/helpers/rollHeroSymbolRef.mjs</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol L1630-L1701 (live `_rollHeroSymbol` body — pass 1 weight cache + leader tracking; total==0 early-bail; pass 2 keccak-derived pick + cumulative cursor walk + leaderBonus add at idx==leaderIdx)
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (`dailyHeroWagers` packed layout — 4 × uint256 per day, each packing 8 × uint32 amounts at 32-bit offsets)
    - test/helpers/raritySymbolBatchRef.mjs L1-L80 (sister helper shape — pure ES module, ethers AbiCoder import, JSDoc header citing contract line range, U64_MASK pattern)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md §"D-42N-DETERMINISM-01" anchor block (algorithm lock: abi.encode NOT abi.encodePacked; pass-2 cursor walks flat idx ascending; leader-bonus add at idx==leaderIdx; pass-1 strict-> tie-break first-seen wins)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md §3.b D-42N-CACHE-01 LOCK (flat uint32[32] indexed (q<<3)|s — verbatim cache shape) + §3.e D-42N-COLOR-ENTROPY-01 (color path consumes randomWord bits quadrant*3; symbol path consumes keccak256(abi.encode(entropy, day)) — orthogonal entropy domains; the helper handles symbol-path only)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md <decisions> "JS-reference impl shape" sub-decision + <specifics> "Default JS-reference helper" entry
    - .planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-01-PLAN.md (sister-phase plan; shape reference for ES module + AbiCoder import pattern + JSDoc no-history rule)
    - test/helpers/raritySymbolBatchRef.mjs (verify the existing file is NOT modified by this plan — only a NEW sibling file is created)
  </read_first>
  <action>
    Create a NEW file `test/helpers/rollHeroSymbolRef.mjs`. The file is a pure-function ES-module bit-mirror of the live `_rollHeroSymbol` body at `contracts/modules/DegenerusGameJackpotModule.sol:1639-1700`. Concrete shape:

    1. SPDX header `// SPDX-License-Identifier: AGPL-3.0-only`.

    2. Top-of-file JSDoc block (~12-20 lines) per `feedback_no_history_in_comments.md`. Content describes what IS — the v42 HRROLL weighted-roll oracle for `_rollHeroSymbol` at `contracts/modules/DegenerusGameJackpotModule.sol:1639-1700` (audit-subject commit `a0218952`). MUST cite (i) the live contract line range, (ii) the D-42N-DETERMINISM-01 algorithm lock (`abi.encode` NOT `abi.encodePacked`; pass-2 cursor walks flat idx ascending; leader-bonus added at `idx == leaderIdx`; pass-1 strict-`>` first-seen tie-break), (iii) the D-42N-CACHE-01 flat `uint32[32]` cache shape per `292-01-MEASUREMENT.md` §3.b, (iv) the D-42N-COLOR-ENTROPY-01 note that this helper handles the symbol-roll path ONLY (color path lives in `_applyHeroOverride` and is NOT modeled here). DO NOT write "previously took X" / "v41 form" / "used to be" — describe v42 mechanics only.

    3. Imports: `import { AbiCoder, keccak256 } from "ethers";`

    4. Module-level constants:
       - `const U64_MASK = (1n << 64n) - 1n;` — uint64 mask for `total` and `cumulative` masking
       - `const U32_MASK = (1n << 32n) - 1n;` — uint32 mask for amount extraction
       - `const abiCoder = AbiCoder.defaultAbiCoder();` — module-singleton ethers AbiCoder

    5. Primary exported function:

       Signature: `export function rollHeroSymbolRef({ day, entropy, dailyHeroWagers })`

       Inputs:
       - `day`: number OR BigInt — the uint32 day key (coerced via `Number(day) >>> 0` then BigInt for keccak input)
       - `entropy`: BigInt — the uint256 entropy word (the raw `randWord` from the jackpot VRF; D-42N-BONUS-ENTROPY-01 carry — color path consumes `r` bits, symbol path consumes raw `randWord` keccak-hashed with day)
       - `dailyHeroWagers`: `BigInt[4]` — the four packed uint256 slots for `dailyHeroWagers[day]`, indexed by quadrant 0..3 ascending. Each BigInt packs 8 × uint32 amounts at 32-bit offsets (symbol s at bits `s*32 .. s*32+31`).

       Output: an object `{ hasWinner: boolean, winQuadrant: number, winSymbol: number }` where `winQuadrant ∈ {0,1,2,3}` and `winSymbol ∈ {0..7}`. When `hasWinner === false`, `winQuadrant === 0 && winSymbol === 0` to mirror the Solidity named-return tuple `(false, 0, 0)`.

       Body (mirror `_rollHeroSymbol` lines 1647-1699 verbatim):

       (a) Declare a fresh `Uint32Array(32)` named `weights`. Maintain `let total = 0n;` (BigInt for uint64 width), `let maxAmount = 0;` (Number — uint32 fits in 32-bit safe range), `let leaderIdx = 0;` (Number — uint8 0..31).

       (b) Outer loop `for (let q = 0; q < 4; ++q)`. Read `const packed = BigInt(dailyHeroWagers[q]);`. Inner loop `for (let s = 0; s < 8; ++s)`. Extract `const amount = Number((packed >> BigInt(s * 32)) & U32_MASK);`. Compute `const idx = (q << 3) | s;`. Set `weights[idx] = amount;`. Increment `total = (total + BigInt(amount)) & U64_MASK;`. Apply strict-`>` first-seen tie-break: `if (amount > maxAmount) { maxAmount = amount; leaderIdx = idx; }`.

       (c) Early-bail: `if (total === 0n) return { hasWinner: false, winQuadrant: 0, winSymbol: 0 };`

       (d) Pass 2:
       - `const leaderBonus = BigInt(maxAmount) >> 1n;` — integer divide by 2 matching Solidity `uint64(maxAmount) / 2` (the BigInt right-shift-1 form is equivalent to integer-divide-by-2 for non-negative values and avoids any floating-point coercion).
       - `const effectiveTotal = (total + leaderBonus) & U64_MASK;`
       - Build the keccak input via `abiCoder.encode(["uint256","uint32"], [entropy, BigInt(day)])`. Compute `const hashHex = keccak256(encoded);` and `const pick = BigInt(hashHex) % effectiveTotal;` — `pick` fits in uint64 by construction (`% effectiveTotal` and `effectiveTotal <= total + leaderBonus < 2^64`).
       - Cursor walk: `let cumulative = 0n; for (let idx = 0; idx < 32; ++idx) { cumulative = (cumulative + BigInt(weights[idx])) & U64_MASK; if (idx === leaderIdx) { cumulative = (cumulative + leaderBonus) & U64_MASK; } if (cumulative > pick) { return { hasWinner: true, winQuadrant: idx >> 3, winSymbol: idx & 7 }; } }`
       - Fall-through return `{ hasWinner: false, winQuadrant: 0, winSymbol: 0 }` — unreachable by invariant but matches the Solidity named-return default (per D-42N-DETERMINISM-01 + 292-02-SUMMARY.md key-decisions "Implicit (false,0,0) named-return for the (proven-unreachable) loop-exit path").

    6. Secondary exported helper for ergonomic test-side fixture construction:

       `export function packDailyHeroWagers(rawAmounts)` — takes a 32-length array of uint32 numbers (flat idx layout: `(q << 3) | s`) and returns `BigInt[4]` with `out[q] = sum over s of (BigInt(rawAmounts[(q<<3)|s]) << BigInt(s*32))`. Cap each amount at `0xFFFFFFFF` (uint32 max) per the `placeDegeneretteBet` saturation at `contracts/modules/DegenerusGameDegeneretteModule.sol:495`. The helper is for synthetic-state seeding by Plan 02 fixtures (TST-HRROLL-01 chi² seed `[400, 300, 200, 100]`-at-quadrant-leaders + TST-HRROLL-02 leader seed `[500, 200, 200, 100]` per the LOCKED disposition).

    7. Tertiary exported helper for symmetric debug:

       `export const ROLL_HERO_SYMBOL_CONSTANTS = Object.freeze({ U64_MASK, U32_MASK });` — exposes the masks for cross-validation in test assertions (e.g., `expect(amount).to.be.lte(Number(ROLL_HERO_SYMBOL_CONSTANTS.U32_MASK))`).

    Restrictions:
    - Pure ES module — no `require`, no CommonJS exports. File extension `.mjs` matches sister `raritySymbolBatchRef.mjs`.
    - No side effects on import (no console.log, no top-level await, no fixture loading).
    - No chai plugin, no `expect` extension, no test runner integration.
    - Do NOT import from `hardhat` or `chai` — this is a domain helper, not a test file.
    - Do NOT add a JS-side `revert` for the unreachable fall-through path — return `{ hasWinner: false, winQuadrant: 0, winSymbol: 0 }` (matches the contract's named-return per D-42N-DETERMINISM-01 + 292-02 key-decisions per `feedback_no_dead_guards.md`).
    - Do NOT extend `test/helpers/raritySymbolBatchRef.mjs` in-place — the v42 HRROLL helper is a NEW sibling file (per 293-CONTEXT.md Out-of-Scope surfaces and Phase 282/291 file-separation pattern).
    - Do NOT add gas measurement, no gas constant, no gas log — gas measurement is Plan 02 TST-HRROLL-06 production-path delta scope, NOT this helper's concern.
    - Do NOT commit (`git commit`) in this plan — defer to Plan 02 USER-APPROVED batched-commit per `feedback_batch_contract_approval.md`.
  </action>
  <verify>
    <automated>
test -f test/helpers/rollHeroSymbolRef.mjs
    </automated>
    <automated>
node -e "import('./test/helpers/rollHeroSymbolRef.mjs').then(m => { if (typeof m.rollHeroSymbolRef !== 'function') process.exit(1); if (typeof m.packDailyHeroWagers !== 'function') process.exit(2); if (typeof m.ROLL_HERO_SYMBOL_CONSTANTS !== 'object') process.exit(3); console.log('exports OK'); }).catch(e => { console.error(e); process.exit(99); })"
    </automated>
    <automated>
node -e "import('./test/helpers/rollHeroSymbolRef.mjs').then(m => { const out = m.rollHeroSymbolRef({ day: 7, entropy: 0n, dailyHeroWagers: [0n, 0n, 0n, 0n] }); if (out.hasWinner !== false || out.winQuadrant !== 0 || out.winSymbol !== 0) { console.error('zero-wager early-bail FAILED', out); process.exit(1); } console.log('zero-wager early-bail OK'); }).catch(e => { console.error(e); process.exit(99); })"
    </automated>
    <automated>
node -e "import('./test/helpers/rollHeroSymbolRef.mjs').then(m => { const raw = new Array(32).fill(0); raw[0] = 1000; const packed = m.packDailyHeroWagers(raw); for (let i = 0; i < 50; ++i) { const out = m.rollHeroSymbolRef({ day: 42, entropy: BigInt('0x' + 'ab'.repeat(32 - (i % 4)) + i.toString(16).padStart(2,'0').repeat(i % 4 || 1)).valueOf(), dailyHeroWagers: packed }); if (!out.hasWinner || out.winQuadrant !== 0 || out.winSymbol !== 0) { console.error('single-bettor determinism FAILED at i=' + i, out); process.exit(1); } } console.log('single-bettor determinism OK (50 entropy variations)'); }).catch(e => { console.error(e); process.exit(99); })"
    </automated>
    <automated>
node -e "import('./test/helpers/rollHeroSymbolRef.mjs').then(m => { const raw = new Array(32).fill(0); raw[0]=500; raw[1]=200; raw[2]=200; raw[3]=100; const packed = m.packDailyHeroWagers(raw); let leaderHits = 0; const N = 2000; for (let i = 0; i < N; ++i) { const out = m.rollHeroSymbolRef({ day: 1, entropy: BigInt(i) * 0x100000000000000000000000000n + 1n, dailyHeroWagers: packed }); if (out.hasWinner && out.winQuadrant === 0 && out.winSymbol === 0) leaderHits++; } const rate = leaderHits / N; console.log('smoke leader rate at N=' + N + ': ' + rate.toFixed(4) + ' (expected ~0.60 under D-42N-LEADER-BONUS-01)'); if (rate < 0.50 || rate > 0.70) { console.error('leader pick-rate outside loose [0.50, 0.70] smoke bound'); process.exit(1); } }).catch(e => { console.error(e); process.exit(99); })"
    </automated>
    <automated>
grep -nE "export function rollHeroSymbolRef|export function packDailyHeroWagers|export const ROLL_HERO_SYMBOL_CONSTANTS" test/helpers/rollHeroSymbolRef.mjs
    </automated>
    <automated>
grep -nE "abiCoder\.encode\(\[\"uint256\",\"uint32\"\]|defaultAbiCoder\(\)\.encode\(\[.\"uint256.,..uint32" test/helpers/rollHeroSymbolRef.mjs
    </automated>
    <automated>
grep -cE "\\babi\\.encodePacked\\b|encodePacked" test/helpers/rollHeroSymbolRef.mjs
    </automated>
    <automated>
git diff --name-only contracts/ | grep -v "^$" | head -1 | { read line; if [ -n "$line" ]; then echo "FAIL: contracts/ touched: $line"; exit 1; else echo "contracts/ clean"; fi; }
    </automated>
    <automated>
git diff --name-only test/helpers/raritySymbolBatchRef.mjs | grep -v "^$" | head -1 | { read line; if [ -n "$line" ]; then echo "FAIL: raritySymbolBatchRef.mjs modified (must be NEW sibling only): $line"; exit 1; else echo "sister helper untouched"; fi; }
    </automated>
  </verify>
  <done>
    - `test/helpers/rollHeroSymbolRef.mjs` exists as a new file.
    - Exports `rollHeroSymbolRef` (primary), `packDailyHeroWagers` (fixture helper), `ROLL_HERO_SYMBOL_CONSTANTS` (mask register).
    - The keccak input uses `abiCoder.encode(["uint256","uint32"], [entropy, BigInt(day)])` — matches `abi.encode(entropy, day)` byte layout per D-42N-DETERMINISM-01. Zero `abi.encodePacked` / `encodePacked` occurrences in the file.
    - Smoke test #3 (verify gate): zero-wager fixture returns `(false, 0, 0)` deterministically — HRROLL-01 early-bail.
    - Smoke test #4: single-bettor seed `[1000@idx=0, 0×31]` returns `(true, 0, 0)` for 50 entropy variations — TST-HRROLL-04 prerequisite.
    - Smoke test #5: LOCKED TST-HRROLL-02 seed `[500, 200, 200, 100]` (4 non-zero entries at flat idx 0..3 ⇒ all in quadrant 0; leader at idx 0) produces empirical leader pick-rate within loose `[0.50, 0.70]` bound at N=2000 (the precise 60.0% chi² assertion is Plan 02's responsibility at N=10000; this gate only sanity-checks the algorithm doesn't drift wildly).
    - Zero `contracts/` files modified; `test/helpers/raritySymbolBatchRef.mjs` byte-identical.
    - No `git commit` issued in this plan — staged change only, deferred to Plan 02 USER-APPROVED batched commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS reference impl → EVM contract semantics | The reference function is the test-side oracle; the trust risk is JS-side numeric drift away from the EVM semantics (uint64 overflow, uint32 truncation, keccak input byte layout, integer-divide vs floating-point). |

(No production attack surface introduced — this plan is test-only and adds a new helper file. Per `293-CONTEXT.md` <threat_model> guidance: Phase 293 is test-only by default; threats N/A unless the D-293-INVOKE-01 escalation path is invoked at Plan 02 Task 7, which is OUT OF SCOPE for this plan.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-293-01-01 | Tampering | `rollHeroSymbolRef` keccak input encoding | mitigate | Use `AbiCoder.defaultAbiCoder().encode(["uint256","uint32"], [entropy, BigInt(day)])` — byte-equivalent to Solidity `abi.encode(uint256, uint32)` (each value left-padded to a 32-byte word; verified at v41 helper via Phase 282 W2 invariant pass for the analogous mint-batch keccak). Verify gate #6 greps for the exact AbiCoder signature pattern. |
| T-293-01-02 | Tampering | Pass-1 strict-`>` first-seen tie-break | mitigate | Use literal `if (amount > maxAmount)` JS comparison — non-strict `>=` would change the tie-break to last-seen-wins and break the D-42N-DETERMINISM-01 lock. Verified at runtime by the smoke gate #5 leader-pick rate envelope (a flipped tie-break would put the leader at idx=3, not idx=0, and leader pick-rate would collapse). |
| T-293-01-03 | Information disclosure / numeric overflow | `total += BigInt(amount)` uint64 mask | mitigate | Apply `& U64_MASK` after every BigInt arithmetic op on `total` and `cumulative` to match Solidity uint64 wraparound semantics. Max possible total at runtime: 32 × 0xFFFFFFFF ≈ 1.37e11 — well under 2^64-1 (~1.84e19), so wraparound never triggers organically, but the mask is structural correctness. |
| T-293-01-04 | Tampering | `leaderBonus = uint64(maxAmount) / 2` Solidity integer-divide vs JS BigInt right-shift | mitigate | `BigInt(maxAmount) >> 1n` is exactly equivalent to `BigInt(maxAmount) / 2n` for non-negative BigInts and avoids any floating-point coercion. Verify gate #5 LOCKED TST-HRROLL-02 seed exercises the leaderBonus add at idx=0 and asserts the empirical pick rate is within the smoke envelope. |
| T-293-01-SC | Tampering | npm install / `ethers` package legitimacy | accept | No `npm install` performed in this plan — `ethers` is already a project dependency. Package-legitimacy gate per `feedback_skip_research_test_phases.md` / planner-source-audit: NOT APPLICABLE (no new package adds). |

**Escalation threat (D-293-INVOKE-01 path):** NOT INVOKED at this plan. This plan ships only the JS-replay oracle; the escalation path lives at Plan 02 Task 7 as a `[BLOCKING_ESCALATION]` checkpoint.

**Severity:** Low (test-only file; no on-chain or production surface introduced).
</threat_model>

<verification>
- `test/helpers/rollHeroSymbolRef.mjs` parses as a valid ES module under Node ≥ 18.
- Three exports (`rollHeroSymbolRef`, `packDailyHeroWagers`, `ROLL_HERO_SYMBOL_CONSTANTS`) all resolve.
- Zero-wager smoke returns `(false, 0, 0)` — HRROLL-01 invariant.
- Single-bettor smoke (1000 @ idx 0) returns `(true, 0, 0)` for 50 entropy variations — TST-HRROLL-04 prerequisite.
- LOCKED TST-HRROLL-02 seed `[500, 200, 200, 100]` produces empirical leader pick-rate within `[0.50, 0.70]` smoke envelope at N=2000 (precise 60.0% assertion is Plan 02's N=10000 chi² responsibility).
- Keccak input uses `abi.encode(uint256, uint32)` byte layout (NOT `abi.encodePacked`); verified via grep.
- Zero `contracts/` modifications; sister helper `raritySymbolBatchRef.mjs` byte-identical.
</verification>

<success_criteria>
- The JS-replay oracle is callable from Plan 02 via `import { rollHeroSymbolRef, packDailyHeroWagers } from "../helpers/rollHeroSymbolRef.mjs";`
- The oracle's output for every (day, entropy, dailyHeroWagers) triple is bit-identical to what the EVM `_rollHeroSymbol` would produce (cross-validation is Plan 02 Task 6's production-path replay).
- D-293-INVOKE-01 default disposition honored: ALGORITHM_VERIFIED via JS-replay oracle; the `_rollHeroSymbol` visibility-flip escalation is NOT invoked at Plan 01.
- D-42N-DETERMINISM-01 lock honored: `abi.encode`, flat idx ascending, leader-bonus at `idx == leaderIdx`, strict-`>` first-seen tie-break.
- D-42N-CACHE-01 lock honored: flat `uint32[32]` (via `Uint32Array(32)`) indexed `(q << 3) | s`.
- Zero `contracts/` mutations; zero git commits in this plan.
</success_criteria>

<output>
Create `.planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-01-SUMMARY.md` when done. Stage `test/helpers/rollHeroSymbolRef.mjs` but DO NOT `git commit` — the single USER-APPROVED batched commit lives at Plan 02 close per `feedback_batch_contract_approval.md`.
</output>
</content>
