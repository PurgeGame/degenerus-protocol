---
phase: 294-deity-pass-gold-nerf-dpnerf
plan: 02
type: execute
wave: 2
depends_on: [294-01]
files_modified:
  - contracts/modules/DegenerusGameJackpotModule.sol
  - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
autonomous: false
requirements: [DPNERF-01, DPNERF-02, DPNERF-03, DPNERF-04, DPNERF-05]
tags: [audit, deity-pass, gold-nerf, contract-patch, user-approved, batched-commit, v42.0]
must_haves:
  truths:
    # DPNERF-01 — gold-tier branch shape inserted before existing virtualCount logic
    - "`_randTraitTicket` body at `contracts/modules/DegenerusGameJackpotModule.sol` gains a gold-tier branch BEFORE the existing `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;` logic. The branch shape locked in CONTEXT.md `<specifics>` is inserted verbatim inside the existing `if (deity != address(0))` block (replacing the two pre-patch lines at L1730-L1731): `if (((trait >> 3) & 7) == 7) { virtualCount = 1; } else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }`. Gold-tier executes `virtualCount = 1` (skipping the floor); commons execute the unchanged v41 logic in the else branch (per DPNERF-01 + D-42N-GOLD-FLOOR-01)"
    # DPNERF-01 — color-extraction idiom matches the precedent at L1105
    - "The gold-tier color check uses the inline expression `((trait >> 3) & 7) == 7` matching the established precedent at `contracts/modules/DegenerusGameJackpotModule.sol:1105` (`_pickSoloQuadrant`). NO `GOLD_COLOR = 7` named constant introduced; NO `uint8 color = (trait >> 3) & 7;` local-variable cache introduced — per `feedback_frozen_contracts_no_future_proofing.md` + Claude's-Discretion disposition in CONTEXT.md `<decisions>`"
    # DPNERF-01 — branch placement inside the existing deity-guard
    - "The new branch fires INSIDE the existing `if (deity != address(0))` block at pre-patch L1729-L1732 — the gold-tier nerf only applies when a deity exists for the symbol, matching the v41 conditional shape. The downstream `effectiveLen = len + virtualCount` arithmetic at L1735 is path-uniform across both new-gold-branch + old-else-branch and is NOT modified by this patch. Non-deity branch (when `deity == address(0)`) is UNCHANGED — `virtualCount` stays 0 by Solidity default per DPNERF-01 carry of v41 behavior"
    # DPNERF-02 — all-4-callsite uniformity by construction
    - "Path coverage achieved by construction per `D-294-CALLER-UNIFORM-01`: the single `_randTraitTicket` function-body change reaches ALL 4 callsites uniformly: `_runEarlyBirdLootboxJackpot` at pre-patch L698; `_distributeTicketsToBucket` at pre-patch L988; `_processDailyEth` at pre-patch L1296; `_resolveTraitWinners` at pre-patch L1399 — plus the BURNIE near-future coin jackpot path via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+). No callsite flag; no path-discrimination logic; ZERO callsite-side edits in this plan (per DPNERF-02 + D-42N-PATH-COVERAGE-01 + D-294-CALLER-UNIFORM-01)"
    # DPNERF-03 — common-tier preserved; intentional EV reduction
    - "Common-tier branch (color in [0..6]) is byte-identical to v41 `virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2;` — preserved verbatim in the `else` branch of the new gold-tier check. No common-tier compensation logic added (per DPNERF-03 + D-42N-DEITY-EV-01 intentional-reduction disposition). Net result: deity-pass holders earn less total EV across all 8 colors (gold contribution reduced from `max(len/50, 2)` to 1; commons unchanged)"
    # D-294-NATSPEC-01 — locked 5-line two-tier `what IS` comment block at L1721-1723
    - "The inline comment block at pre-patch L1721-L1723 is REWRITTEN verbatim from CONTEXT.md `<specifics>` per `D-294-NATSPEC-01`: the new 5-line block reads `// Virtual deity entries (if a deity exists for this symbol):` + `//   Gold tier (color == 7): flat 1 virtual entry.` + `//   Common tier (color in [0..6]): floor(2% of bucket), minimum 2.` + `// traitId layout: (quadrant << 6) | (color << 3) | symIdx` + `// fullSymId = quadrant * 8 + symIdx`. ZERO history language (no `previously`, `pre-v41`, `pre-cleanup`, `formerly`, `the bug at`, `was max(len/50, 2)`, `the old form` etc.) per `feedback_no_history_in_comments.md`. ZERO decision-anchor citations in source comments (no `// D-42N-GOLD-FLOOR-01`, no `// DPNERF-01`, no `// Phase 294`) per D-294-NATSPEC-01"
    # NatSpec function-level docstring UNCHANGED
    - "The function-level docstring at pre-patch L1706 (`/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices.`) is UNCHANGED — the DPNERF change is internal-behavior-only; the function's contract (input/output shape, return values, semantic role) is preserved per CONTEXT.md `<decisions>` Claude's-Discretion disposition"
    # DPNERF-04 — storage byte-identity (zero new slots; zero new SSTORE/SLOAD)
    - "Storage byte-identical to v41 closure HEAD `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` on `deityBySymbol[uint8 => address]` mapping (the only DPNERF-touched storage access; UNCHANGED in slot offset, type, label, count); zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in DPNERF scope (`_randTraitTicket` reads the SAME `deityBySymbol[fullSymId]` slot at the SAME line that v41 read; same access pattern); `forge inspect storageLayout` diff for `DegenerusGameJackpotModule.sol` + `DegenerusGameStorage.sol` against v41 close baseline is EMPTY — recorded in `294-01-MEASUREMENT.md` §2 (per DPNERF-04)"
    # DPNERF-05 — public ABI byte-identity
    - "Public ABI byte-identical: `_randTraitTicket` is `private` (signature change WOULD have been a private-function delta only — but in this patch the signature is UNCHANGED; only the function body changes); all 4 direct callers + `payDailyCoinJackpot` (the only `external` entry within 2 hops at L1767) + every other public/external entry point reachable through `_runJackpotEthFlow` + `_distributeTicketJackpot` + `_runEarlyBirdLootboxJackpot` chains have UNCHANGED selectors + parameter types + return shapes; zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks in DPNERF scope; `forge inspect methodIdentifiers` diff for `DegenerusGameJackpotModule.sol` against v41 close baseline is EMPTY — recorded in `294-01-MEASUREMENT.md` §4 (per DPNERF-05)"
    # Theoretical bytecode delta — analytical first; no empirical second-pass
    - "Theoretical bytecode-delta estimate per `feedback_gas_worst_case.md` analytical-first methodology: ~+10-30 bytes runtime bytecode for the new gold-tier branch (one `SHR(3)` + one `AND(7)` + one `EQ(7)` + one `JUMPI` + one constant push for `1` + one `MSTORE` for the gold-tier assignment; the else branch retains the original v41 logic verbatim — no overlapping bytecode removed); recorded in `294-01-MEASUREMENT.md` §5. NO empirical second-pass at Phase 294 — TST-DPNERF-01..05 scope at Phase 295 includes NO gas-regression test (TST-DPNERF-04 is an EV regression at N=1000, not a gas regression); runtime gas cost is negligible (single comparison + conditional store; well below practical measurement noise floor)"
    # Zero-new-state grep-proof — strengthens DPNERF-04 at function-body level
    - "Post-patch zero-new-state grep evidence recorded in `294-01-MEASUREMENT.md` §6: `_randTraitTicket` function body has ZERO new SSTORE-equivalent statements (assignments to mapping / storage-array elements) vs v41 close; ZERO new SLOAD-equivalent statements (mapping / storage-array reads) vs v41 close. The only DPNERF-touched storage access remains the existing `deityBySymbol[fullSymId]` SLOAD inside the unchanged-conditional `if (fullSymId < 32)` block — UNCHANGED in count, slot, type by the patch. Strengthens DPNERF-04 storage byte-identity attestation at the function-body level"
    # NatSpec discipline — no history in source comments
    - "Zero comparative/historical language in any NatSpec edit touched by this plan (no `previously`, `pre-v41`, `pre-cleanup`, `formerly took`, `the bug at v40 was`, `was max(len/50, 2)`, `the old form`, `historically`, `before this nerf`) per `feedback_no_history_in_comments.md`; the v41-vs-v42 DPNERF cleanup narrative lives ONLY in `294-01-DESIGN-INTENT-TRACE.md` + the commit message body + audit/FINDINGS-v42.0.md §3.A at Phase 297"
    # Out-of-scope surfaces UNCHANGED
    - "`_pickSoloQuadrant` (at pre-patch L1080-L1130) is byte-identical post-patch — adjacent gold-tier code path; orthogonal to `_randTraitTicket`'s deity-virtual-count behavior; out of scope per CONTEXT.md `<code_context>` Out-of-Scope Source-Tree Surfaces; verified via `git diff` showing zero hunks outside the `_randTraitTicket` body + the L1721-L1723 comment block"
    - "`contracts/modules/DegenerusGameDegeneretteModule.sol` UNCHANGED (boon distribution + deity-pass tier-shift flow `BP_DEITY_PASS_TIER_SHIFT` excluded per REQUIREMENTS.md line 25)"
    - "`contracts/modules/DegenerusGameWhaleModule.sol` UNCHANGED (deity-pass purchase pricing `DEITY_PASS_BASE` excluded per REQUIREMENTS.md line 26)"
    - "`contracts/DegenerusDeityPass.sol` UNCHANGED (soulbound NFT minting excluded per REQUIREMENTS.md line 27)"
    - "`contracts/modules/DegenerusGameBoonModule.sol` UNCHANGED (boon distribution flow excluded)"
    - "`contracts/storage/DegenerusGameStorage.sol` UNCHANGED (zero storage layout changes per DPNERF-04)"
    - "`contracts/interfaces/IDegenerusGameModules.sol` UNCHANGED (zero public ABI changes per DPNERF-05)"
    - "`test/` UNCHANGED (TST-DPNERF-01..05 fixture ships at Phase 295; zero `test/` mutations at Phase 294)"
    - "`KNOWN-ISSUES.md` UNMODIFIED per CONTEXT.md `<decisions>` Claude's-Discretion disposition (mirrors D-281-KI-01 + D-291-KI-01 + D-293-STALE-VIEW-01 pattern for surface-mutation phases)"
    # Wave-shape lock — ONE batched USER-APPROVED commit; no push
    - "All contract edits + the populated `294-01-MEASUREMENT.md` collapse into ONE USER-APPROVED batched commit at Task 5; the orchestrator + planner have NOT pre-approved the contract diff per `feedback_never_preapprove_contracts.md`; the user reviews the full diff per `feedback_manual_review_before_push.md` before approving; no `git push` after the commit (push requires a separate explicit user instruction)"
    # Commit message format locked
    - "Commit message subject: `feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]` (locked verbatim per CONTEXT.md `<specifics>` + mirrors Phase 290 + Phase 292 contract commit patterns)"
  artifacts:
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "DPNERF-01..05 contract patch — `_randTraitTicket` body at pre-patch L1707-L1757 gains a gold-tier branch inside the existing `if (deity != address(0))` block: `if (((trait >> 3) & 7) == 7) { virtualCount = 1; } else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }` replacing the pre-patch two lines `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;`. Inline comment block at pre-patch L1721-L1723 rewritten to the locked 5-line two-tier `what IS` shape per D-294-NATSPEC-01. Function-level docstring at L1706 UNCHANGED. All adjacent code UNCHANGED."
      contains: "(trait >> 3) & 7"
    - path: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md"
      provides: "DPNERF-04 + DPNERF-05 attestations populated post-patch — §2 storage byte-identity (`forge inspect storageLayout` EMPTY diff expected); §4 public ABI byte-identity (`forge inspect methodIdentifiers` EMPTY diff expected); §5 theoretical bytecode-delta actual byte count post-`forge build`; §6 zero-new-state grep evidence for `_randTraitTicket` body. §1 + §3 were FINAL at Plan 01 time."
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol (`_randTraitTicket` body post-patch at L1707-L17xx)"
      to: "All 4 callsites at pre-patch L698 + L988 + L1296 + L1399 + the BURNIE path via payDailyCoinJackpot L1767 + _awardDailyCoinToTraitWinners L1816+"
      via: "single function-body change reaches every caller by construction per D-294-CALLER-UNIFORM-01 — no callsite-side edits"
      pattern: "_randTraitTicket"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol (`_randTraitTicket` gold-tier branch)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol (`_pickSoloQuadrant` L1105 precedent)"
      via: "shared inline color-extraction idiom `((trait >> 3) & 7) == 7` (no GOLD_COLOR constant; no local-var cache) per feedback_frozen_contracts_no_future_proofing.md"
      pattern: "\\(trait >> 3\\) & 7"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md (populated post-patch)"
      to: "Task 5 batched commit message body"
      via: "copy-forward of §2 storage byte-identity result + §4 public ABI byte-identity result + §5 actual bytecode delta + §6 zero-new-state grep evidence; §1 + §3 unchanged from Plan 01 final state"
      pattern: "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md (Plan 01 output)"
      to: "Task 5 batched commit message body"
      via: "copy-forward of 4-section design-intent trace + the 5 phase-scope decision anchors (D-42N-GOLD-FLOOR-01 + D-42N-DEITY-EV-01 + D-42N-PATH-COVERAGE-01 + D-294-CALLER-UNIFORM-01 + D-294-NATSPEC-01) + SWEEP-02(iii) pre-emptive answers"
      pattern: "D-42N-GOLD-FLOOR-01"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md §3 callsite enumeration"
      to: ".planning/phases/297-terminal-audit-findings-v42/297-PLAN.md §3.A delta-surface table (DPNERF row)"
      via: "all-4-callsite citation per D-294-CALLER-UNIFORM-01 — Phase 297 §3.A table cites every caller path by line number under the DPNERF row + Phase 296 SWEEP attests their behavior + Phase 297 §3.C conservation re-proof for DPNERF: gold-tile virtualCount = 1; common-tile UNCHANGED at max(len/50, 2); all 4 callsites uniform"
      pattern: "L698|L988|L1296|L1399"
---

<objective>
Land the DPNERF-01..05 contract patch on `contracts/modules/DegenerusGameJackpotModule.sol` as ONE USER-APPROVED batched commit covering: (1) DPNERF-01 — insert the locked gold-tier branch `if (((trait >> 3) & 7) == 7) { virtualCount = 1; } else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }` inside the existing `if (deity != address(0))` block at pre-patch L1729-L1732 of `_randTraitTicket` (replacing the two pre-patch lines `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;`); (2) DPNERF-02 — path coverage achieved by construction (single function-body change reaches all 4 callsites + BURNIE path uniformly per D-294-CALLER-UNIFORM-01; ZERO callsite-side edits); (3) DPNERF-03 — common-tier `virtualCount = max(len/50, 2)` byte-identical to v41 in the else branch (intentional EV reduction per D-42N-DEITY-EV-01; no commons compensation); (4) DPNERF-04 — storage byte-identity attested via `forge inspect storageLayout` EMPTY diff (recorded in `294-01-MEASUREMENT.md` §2); (5) DPNERF-05 — public ABI byte-identity attested via `forge inspect methodIdentifiers` EMPTY diff (recorded in `294-01-MEASUREMENT.md` §4); plus the D-294-NATSPEC-01 inline comment-block rewrite at pre-patch L1721-L1723 to the locked 5-line two-tier `what IS` shape; plus the theoretical bytecode-delta + zero-new-state grep-proof fills in `294-01-MEASUREMENT.md` §5 + §6. Present the consolidated diff to the user; on approval, create ONE batched commit per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. No `git push`.

Purpose: Implement the v42.0 DPNERF gold-tier nerf — flat 1 virtual deity entry on gold-tier (color == 7) trait wins; common-tier preserved at `max(len/50, 2)`. Single targeted function-body change in `_randTraitTicket` that reaches all 4 callsites + the BURNIE near-future coin jackpot path by construction. Zero new storage; zero new SSTORE/SLOAD; zero new public/external entry points; private function signature UNCHANGED. Inherit the v41 audit baseline + Phase 292 close byte-identity chain on storage + public ABI. Reuse the established `((trait >> 3) & 7) == 7` inline color-extraction idiom from the `_pickSoloQuadrant` precedent at L1105 per `feedback_frozen_contracts_no_future_proofing.md`. Bound bytecode regression analytically at ~+10-30 bytes per `feedback_gas_worst_case.md` theoretical-first methodology; no empirical second-pass at Phase 294 (TST-DPNERF-01..05 scope at Phase 295 ships no gas-regression test).

Output:
- `contracts/modules/DegenerusGameJackpotModule.sol` patched (2 hunk groups: gold-tier branch insertion at pre-patch L1730-L1731 inside the `if (deity != address(0))` block; comment-block rewrite at pre-patch L1721-L1723)
- `294-01-MEASUREMENT.md` populated post-patch (§2 storage byte-identity EMPTY-diff result for `DegenerusGameJackpotModule.sol` + `DegenerusGameStorage.sol`; §4 public ABI byte-identity EMPTY-diff result for `DegenerusGameJackpotModule.sol`; §5 actual bytecode-delta byte count post-`forge build`; §6 zero-new-state grep evidence for the `_randTraitTicket` body; §1 + §3 unchanged from Plan 01 final state)
- ONE USER-APPROVED batched commit covering the 2 paths above

This plan makes contract edits and triggers the USER-APPROVAL gate at Task 5 per `feedback_no_contract_commits.md`. The orchestrator + planner have NOT pre-approved the diff per `feedback_never_preapprove_contracts.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md

@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
<!-- Load-bearing identifiers / line ranges / current code shapes / target shapes. Extracted from live v42.0 HEAD relative to audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. -->
<!-- Executor uses these directly; no codebase exploration needed beyond the @-references above. -->

Pre-patch `_randTraitTicket` function-level docstring + signature (`contracts/modules/DegenerusGameJackpotModule.sol:1706-1717` — UNCHANGED by this plan; docstring stays verbatim per CONTEXT.md `<decisions>` Claude's-Discretion disposition):
```
/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices.
function _randTraitTicket(
    address[][256] storage traitBurnTicket_,
    uint256 randomWord,
    uint8 trait,
    uint8 numWinners,
    uint8 salt
)
    private
    view
    returns (address[] memory winners, uint256[] memory ticketIndexes)
{
```

Pre-patch inline virtual-entry comment block + computation (`contracts/modules/DegenerusGameJackpotModule.sol:1720-1733` — PATCHED by this plan: the 3-line comment at L1721-L1723 expands to the locked 5-line shape per D-294-NATSPEC-01; the two lines `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;` at L1730-L1731 are replaced by the locked branched form):
```
    address[] storage holders = traitBurnTicket_[trait];
    uint256 len = holders.length;

    // Virtual deity entries: floor(2% of bucket tickets), minimum 2, if a deity exists for this symbol.
    // traitId layout: (quadrant << 6) | (color << 3) | symIdx
    // fullSymId = quadrant * 8 + symIdx
    uint8 fullSymId = (trait >> 6) * 8 + (trait & 0x07);
    address deity;
    uint256 virtualCount;
    if (fullSymId < 32) {
        deity = deityBySymbol[fullSymId];
        if (deity != address(0)) {
            virtualCount = len / 50;
            if (virtualCount < 2) virtualCount = 2;
        }
    }

    uint256 effectiveLen = len + virtualCount;
```

Target post-patch inline virtual-entry comment block + computation (the 5-line comment from D-294-NATSPEC-01 + the gold-tier branch from CONTEXT.md `<specifics>` — copy these into the executor edit verbatim; do NOT improvise wording or branch shape):
```
    address[] storage holders = traitBurnTicket_[trait];
    uint256 len = holders.length;

    // Virtual deity entries (if a deity exists for this symbol):
    //   Gold tier (color == 7): flat 1 virtual entry.
    //   Common tier (color in [0..6]): floor(2% of bucket), minimum 2.
    // traitId layout: (quadrant << 6) | (color << 3) | symIdx
    // fullSymId = quadrant * 8 + symIdx
    uint8 fullSymId = (trait >> 6) * 8 + (trait & 0x07);
    address deity;
    uint256 virtualCount;
    if (fullSymId < 32) {
        deity = deityBySymbol[fullSymId];
        if (deity != address(0)) {
            if (((trait >> 3) & 7) == 7) {
                virtualCount = 1;
            } else {
                virtualCount = len / 50;
                if (virtualCount < 2) virtualCount = 2;
            }
        }
    }

    uint256 effectiveLen = len + virtualCount;
```

Existing gold-tier extraction idiom precedent (`contracts/modules/DegenerusGameJackpotModule.sol:1098-1115` `_pickSoloQuadrant` — UNCHANGED by this plan; out of scope per CONTEXT.md `<code_context>`; cited as the idiom-precedent source the DPNERF branch reuses):
```
function _pickSoloQuadrant(uint8[4] memory traits, uint256 entropy) internal pure returns (uint8) {
    uint256 goldQuads;
    uint8 goldCount;
    for (uint8 i; i < 4; ++i) {
        if (((traits[i] >> 3) & 7) == 7) {     // <-- the same idiom DPNERF reuses
            goldQuads |= uint256(i) << (goldCount * 8);
            unchecked { ++goldCount; }
        }
    }
    // ...
}
```

The four `_randTraitTicket` callsites covered uniformly by this plan's function-body change per `D-294-CALLER-UNIFORM-01` — ZERO callsite-side edits (the function-body change reaches them all by construction):
- `contracts/modules/DegenerusGameJackpotModule.sol:698` — `_runEarlyBirdLootboxJackpot`: `) = _randTraitTicket(bucket, rngWord, traitId, 25, t);`
- `contracts/modules/DegenerusGameJackpotModule.sol:988` — `_distributeTicketsToBucket`: `) = _randTraitTicket(` (helper called by `_distributeTicketJackpot` from L637 / L652 / L883)
- `contracts/modules/DegenerusGameJackpotModule.sol:1296` — `_processDailyEth`: `) = _randTraitTicket(` (the roadmap-named `_runJackpotEthFlow` path resolves through here)
- `contracts/modules/DegenerusGameJackpotModule.sol:1399` — `_resolveTraitWinners`: `) = _randTraitTicket(`

BURNIE near-future coin jackpot path (called out in the roadmap; reaches `_randTraitTicket` via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+) → trait-bucket sampling).

Audit baseline reference: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD; source of truth for "byte-identical to v41 close" assertions in `294-01-MEASUREMENT.md` §2 + §4). Phase 292 close cited as the most-recent intermediate anchor.

Commit message subject (locked verbatim per CONTEXT.md `<specifics>` + `<phase_specific_guidance>`):
```
feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the DPNERF gold-tier branch + comment-block rewrite to `_randTraitTicket` (DPNERF-01..03 + D-294-NATSPEC-01)</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol
  </files>
  <read_first>
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md (Plan 01 output — verify it exists and contains all 5 phase-scope anchors; this is the pre-patch gate per `feedback_design_intent_before_deletion.md`)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md (Plan 01 output — verify §1 + §3 are FINAL and §2 + §4 + §5 + §6 carry `<FILL-IN-Plan-02>` placeholders)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md (load-bearing; `<specifics>` for the exact branch shape + comment shape; `<decisions>` for D-294-NATSPEC-01 + D-294-CALLER-UNIFORM-01 + Claude's-Discretion dispositions; `<code_context>` for out-of-scope surfaces)
    - .planning/REQUIREMENTS.md lines 80-87 (DPNERF-01..06 verbatim) + lines 22-27 (DPNERF Out-of-Scope register)
    - contracts/modules/DegenerusGameJackpotModule.sol L1700-L1760 (the `_randTraitTicket` function body — pre-patch state; the patch target)
    - contracts/modules/DegenerusGameJackpotModule.sol L1080-L1130 (the `_pickSoloQuadrant` precedent — read for the existing `((traits[i] >> 3) & 7) == 7` idiom; UNCHANGED by this patch)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (D-294-NATSPEC-01 enforcement)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md (no GOLD_COLOR constant; no color local-var cache)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md (no unreachable safety branches)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_contract_locations.md (read contracts only from `contracts/` directory; ignore any stale copies elsewhere)
  </read_first>
  <action>
    Pre-patch gate (run first; halt if either fails):
    1. Confirm `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md` exists and contains all 5 phase-scope anchors via `grep -cE "D-42N-GOLD-FLOOR-01|D-42N-DEITY-EV-01|D-42N-PATH-COVERAGE-01|D-294-CALLER-UNIFORM-01|D-294-NATSPEC-01"` (expect ≥ 5).
    2. Confirm `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` exists and §3 contains all 4 callsite line numbers (L698 + L988 + L1296 + L1399).
    If either gate fails → halt and instruct the orchestrator to complete Plan 01 first.

    Apply TWO edits to `contracts/modules/DegenerusGameJackpotModule.sol`:

    Edit A — Comment-block rewrite at pre-patch L1721-L1723 (3 lines → 5 lines per D-294-NATSPEC-01):

    Locate the 3 current comment lines (immediately above the `uint8 fullSymId = ...` line):
    ```
    // Virtual deity entries: floor(2% of bucket tickets), minimum 2, if a deity exists for this symbol.
    // traitId layout: (quadrant << 6) | (color << 3) | symIdx
    // fullSymId = quadrant * 8 + symIdx
    ```
    REPLACE with the locked 5-line two-tier `what IS` shape from CONTEXT.md `<specifics>`:
    ```
    // Virtual deity entries (if a deity exists for this symbol):
    //   Gold tier (color == 7): flat 1 virtual entry.
    //   Common tier (color in [0..6]): floor(2% of bucket), minimum 2.
    // traitId layout: (quadrant << 6) | (color << 3) | symIdx
    // fullSymId = quadrant * 8 + symIdx
    ```
    Preserve the leading indentation matching the surrounding code (4 spaces inside the function body — the comment lives at the same indentation as `uint8 fullSymId` below it). DO NOT add `// D-42N-GOLD-FLOOR-01` / `// DPNERF-01` / `// Phase 294` / `// previously` / `// was max(len/50, 2)` markers — per D-294-NATSPEC-01 + `feedback_no_history_in_comments.md`. (DPNERF-01 + D-294-NATSPEC-01 + DPNERF-06 deferred enforcement)

    Edit B — Gold-tier branch insertion inside the existing `if (deity != address(0))` block (pre-patch L1729-L1732):

    Locate the current 2 lines inside the `if (deity != address(0))` block (at pre-patch L1730-L1731):
    ```
                virtualCount = len / 50;
                if (virtualCount < 2) virtualCount = 2;
    ```
    REPLACE with the locked branched form from CONTEXT.md `<specifics>`:
    ```
                if (((trait >> 3) & 7) == 7) {
                    virtualCount = 1;
                } else {
                    virtualCount = len / 50;
                    if (virtualCount < 2) virtualCount = 2;
                }
    ```
    Preserve indentation: the `if (((trait >> 3) & 7) == 7) {` line sits at the same indentation as the pre-patch `virtualCount = len / 50;` line (one more level inside the `if (deity != address(0))` block — 16 spaces / 4 levels of indentation in the surrounding code style). The inner statements `virtualCount = 1;` and `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;` sit one further indentation level deeper (20 spaces / 5 levels). Match the surrounding code style exactly. (DPNERF-01 + DPNERF-03 + D-42N-GOLD-FLOOR-01 + D-42N-DEITY-EV-01)

    Constraints — verify each before declaring the edits complete:
    - Function-level docstring at pre-patch L1706 (`/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices.`) UNCHANGED per CONTEXT.md `<decisions>` Claude's-Discretion disposition.
    - Function signature at pre-patch L1707-L1717 UNCHANGED — `_randTraitTicket` stays `private view` with the same 5 parameters + same return tuple.
    - The downstream `effectiveLen = len + virtualCount;` line at pre-patch L1735 UNCHANGED — path-uniform across the new gold-tier branch + the old else branch.
    - The winner-sampling loop at pre-patch L1740-L1756 UNCHANGED — keccak input + modulo + idx-vs-len branching all preserved verbatim.
    - The non-deity branch (when `deity == address(0)`) UNCHANGED — `virtualCount` stays at Solidity default `0`.
    - All 4 callsites at L698 + L988 + L1296 + L1399 UNCHANGED — zero callsite-side edits; path coverage by construction per D-294-CALLER-UNIFORM-01.
    - `_pickSoloQuadrant` (L1080-L1130) UNCHANGED — adjacent gold-tier function; orthogonal to DPNERF scope per CONTEXT.md `<code_context>`.
    - Everything else in `contracts/modules/DegenerusGameJackpotModule.sol` outside the Edit A + Edit B hunks is UNCHANGED.
    - ZERO edits to `contracts/storage/DegenerusGameStorage.sol`. (DPNERF-04)
    - ZERO edits to `contracts/modules/DegenerusGameDegeneretteModule.sol`. (per CONTEXT.md `<code_context>` Out-of-Scope)
    - ZERO edits to `contracts/modules/DegenerusGameWhaleModule.sol`. (REQUIREMENTS.md line 26)
    - ZERO edits to `contracts/DegenerusDeityPass.sol`. (REQUIREMENTS.md line 27)
    - ZERO edits to `contracts/modules/DegenerusGameBoonModule.sol`. (CONTEXT.md `<code_context>`)
    - ZERO edits to `contracts/interfaces/IDegenerusGameModules.sol`. (DPNERF-05)
    - ZERO edits to `test/`. (REQUIREMENTS.md DPNERF scope; Phase 295 ships TST-DPNERF)
    - ZERO edits to `KNOWN-ISSUES.md`. (per CONTEXT.md `<decisions>` Claude's-Discretion)
    - ZERO edits to `audit/FINDINGS-v42.0.md`. (closure-flip happens at Phase 297)
    - NO `GOLD_COLOR = 7` named constant introduced anywhere in the patch. (per `feedback_frozen_contracts_no_future_proofing.md` + Claude's-Discretion)
    - NO `uint8 color = (trait >> 3) & 7;` local-variable cache introduced. (per Claude's-Discretion)
    - NO comparative/historical language in any comment touched (no `previously`, `pre-v41`, `pre-cleanup`, `formerly`, `the bug at`, `was max(len/50, 2)`, `the old form`, `before this nerf`, `historically`) per `feedback_no_history_in_comments.md`.
    - NO decision-anchor citation (`D-42N-GOLD-FLOOR-01` / `DPNERF-01` / `Phase 294`) in any source comment. (per D-294-NATSPEC-01)
    - NO `// @dev removed` or other dead-guard markers — there is nothing being removed (the else branch IS the v41 logic verbatim) so no removal-history marker applies. (per `feedback_no_dead_guards.md`)
    - RNG audit-methodology disposition: DPNERF doesn't introduce a new RNG consumer (the `randomWord` consumption at the existing pre-patch L1743-L1745 winner-sampling loop is UNCHANGED in form); the gold-tier branch is deterministic on `trait` which is already committed at VRF-request time by the upstream `_rollWinningTraits` consumer. `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` are NOT applicable to Phase 294 per CONTEXT.md `<canonical_refs>`.

    Do NOT compile, build, or run forge in this task — Task 2 + Task 3 + Task 4 handle measurement. The edits land as a clean working-tree diff ready for measurement.

    DO NOT commit yet. The user reviews the diff at Task 5 before the single batched commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "D-42N-GOLD-FLOOR-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "D-294-CALLER-UNIFORM-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "if \(\(\(trait >> 3\) & 7\) == 7\)" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "Gold tier \(color == 7\): flat 1 virtual entry\." contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "Common tier \(color in \[0\.\.6\]\): floor\(2% of bucket\), minimum 2\." contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "Virtual deity entries \(if a deity exists for this symbol\):" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices\." contracts/modules/DegenerusGameJackpotModule.sol && [ $(grep -cE "function _randTraitTicket" contracts/modules/DegenerusGameJackpotModule.sol) -eq 1 ] && [ $(grep -cE "_randTraitTicket\(" contracts/modules/DegenerusGameJackpotModule.sol) -ge 5 ] && ! grep -qE "GOLD_COLOR" contracts/modules/DegenerusGameJackpotModule.sol && ! grep -qE "uint8 color = " contracts/modules/DegenerusGameJackpotModule.sol && ! grep -qE "// D-42N-GOLD-FLOOR-01|// DPNERF-01|// Phase 294" contracts/modules/DegenerusGameJackpotModule.sol && [ $(grep -E "previously|pre-v41|pre-cleanup|formerly|was max\(len/50|the old form|before this nerf|historically" contracts/modules/DegenerusGameJackpotModule.sol | grep -vE "^[[:space:]]*//|^[[:space:]]*\*" | wc -l) -eq 0 ] && [ -z "$(git diff --name-only HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Pre-patch gate: Both `294-01-DESIGN-INTENT-TRACE.md` + `294-01-MEASUREMENT.md` exist; trace contains all 5 phase-scope anchors; measurement scaffold §3 contains all 4 callsite line numbers.
    - Comment-block rewrite present: `grep -qE "Virtual deity entries \(if a deity exists for this symbol\):"` matches; `grep -qE "Gold tier \(color == 7\): flat 1 virtual entry\."` matches; `grep -qE "Common tier \(color in \[0\.\.6\]\): floor\(2% of bucket\), minimum 2\."` matches.
    - Gold-tier branch present: `grep -qE "if \(\(\(trait >> 3\) & 7\) == 7\)"` matches; `grep -qE "virtualCount = 1;"` matches in the gold-tier assignment.
    - Else-branch v41 logic preserved verbatim: `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;` remain in the else branch (verified by reading the diff).
    - Function-level docstring at pre-patch L1706 UNCHANGED (`grep -qE "/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices\."` matches).
    - `_randTraitTicket` function exists exactly once (`grep -cE "function _randTraitTicket"` returns `1`).
    - All 4 callsites still present: `grep -cE "_randTraitTicket\("` returns at least 5 (1 declaration + 4 callsites; the actual count may be higher if internal recursion / additional documentation references exist — minimum bound is 5).
    - NO `GOLD_COLOR` named constant introduced (`grep -qE "GOLD_COLOR"` returns false).
    - NO `color` local-variable cache introduced (`grep -qE "uint8 color = "` returns false in the patched function body).
    - NO decision-anchor citation in source comments (`grep -qE "// D-42N-GOLD-FLOOR-01|// DPNERF-01|// Phase 294"` returns false).
    - NO history language in any comment (filtered grep for `previously|pre-v41|pre-cleanup|formerly|was max(len/50|the old form|before this nerf|historically` after stripping comment-leader lines returns ZERO).
    - `contracts/storage/DegenerusGameStorage.sol` UNCHANGED (`git diff` shows no changes).
    - `contracts/modules/DegenerusGameDegeneretteModule.sol` UNCHANGED.
    - `contracts/modules/DegenerusGameWhaleModule.sol` UNCHANGED.
    - `contracts/DegenerusDeityPass.sol` UNCHANGED.
    - `contracts/modules/DegenerusGameBoonModule.sol` UNCHANGED.
    - `contracts/interfaces/IDegenerusGameModules.sol` UNCHANGED.
    - `test/` UNCHANGED.
    - `KNOWN-ISSUES.md` UNCHANGED.
    - No `forge build` or `cast keccak` run in this task — Tasks 2-4 handle that.
    - No `git commit` issued yet.
  </acceptance_criteria>
  <done>
    DPNERF-01..03 + D-294-NATSPEC-01 contract patch is fully applied to `DegenerusGameJackpotModule.sol` as a clean working-tree diff. The gold-tier branch is in inside the existing `if (deity != address(0))` block; the inline comment block at pre-patch L1721-L1723 is rewritten to the locked 5-line two-tier `what IS` shape; the function-level docstring is UNCHANGED; the function signature is UNCHANGED; the non-deity + else (commons) paths are UNCHANGED. Storage + ABI + sibling modules + tests + KNOWN-ISSUES UNCHANGED. Diff is ready for Task 2 (storage-layout grep) + Task 3 (public ABI grep) + Task 4 (bytecode delta + zero-new-state grep) + Task 5 (USER-APPROVAL gate).
  </done>
</task>

<task type="auto">
  <name>Task 2: Storage byte-identity attestation — populate `294-01-MEASUREMENT.md` §2 (DPNERF-04)</name>
  <files>
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state)
    - contracts/storage/DegenerusGameStorage.sol (UNCHANGED by this plan)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md (scaffold from Plan 01; §1 + §3 final; §2 + §4 + §5 + §6 carry `<FILL-IN-Plan-02>` placeholders)
    - foundry.toml OR hardhat.config.{js,ts} (root of repo — confirm which toolchain to use; mirror Phase 290/292 toolchain discovery pattern)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §2 (sister precedent — `forge inspect storageLayout` against v41 worktree pattern)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md §2 (sister precedent — storage byte-identity attestation pattern)
  </read_first>
  <action>
    Measure storage-layout byte-identity vs `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` baseline.

    Toolchain discovery: check repo root for `foundry.toml` (forge toolchain) OR `hardhat.config.{js,ts}` (hardhat toolchain). Prefer forge if both present (matches Phase 290 + Phase 292 precedent).

    Baseline checkout strategy: use `git worktree add /tmp/v41-baseline 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` to materialize the v41 close HEAD in a sibling directory without disturbing the working tree. If the worktree already exists from a previous Phase 290 or Phase 292 run, reuse it. If creation fails, use `git stash --include-untracked` + `git checkout 315978a0...` + measure + `git checkout -` + `git stash pop` as a fallback. After measurement, leave the worktree in place OR clean up via `git worktree remove /tmp/v41-baseline --force` — executor's discretion.

    §2 — Storage Byte-Identity Attestation:
    1. Run `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule storageLayout` against the post-patch tree → write to `/tmp/v42-jackpot-storage-294.json`.
    2. Run the same command against the v41 baseline worktree → write to `/tmp/v41-jackpot-storage-294.json`.
    3. Compute `diff /tmp/v41-jackpot-storage-294.json /tmp/v42-jackpot-storage-294.json`. Expected: EMPTY.
    4. Run `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout` against both trees similarly → diff. Expected: EMPTY.

    If hardhat is the toolchain instead: use `npx hardhat storage-layout` (requires `hardhat-storage-layout` plugin); if the plugin is not installed, fall back to forge if available; if NEITHER is available, record the failure mode + escalate to user before Task 5.

    Populate §2 of `294-01-MEASUREMENT.md` with:
    - `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule storageLayout diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = [EMPTY | <non-empty-diff-text>]`
    - `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = [EMPTY | <non-empty-diff-text>]`

    If either diff is non-empty → record the exact diff text + flag `🚨 STORAGE LAYOUT REGRESSION — STOP — escalate to user before Task 5`.

    Populate the §2 "Locks under DPNERF-04" block: confirm `deityBySymbol[uint8 => address]` remains at the same slot offset / type / label; confirm zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in DPNERF scope (`_randTraitTicket` reads the SAME `deityBySymbol[fullSymId]` slot at the SAME line that v41 read; same access pattern).

    Replace the §2 `<FILL-IN-Plan-02>` placeholder with the populated diff result + the Locks block.

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "storageLayout diff" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ -z "$(awk '/^## §2|^## \(2\)/,/^## §3|^## \(3\)/' .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && grep -qE "deityBySymbol" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ -z "$(git diff --name-only HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §2 populated with the `forge inspect storageLayout` diff result for BOTH `DegenerusGameJackpotModule.sol` AND `DegenerusGameStorage.sol`; the `<FILL-IN-Plan-02>` placeholder in §2 is GONE.
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited in §2.
    - §2 "Locks under DPNERF-04" block records: `deityBySymbol` at same slot / type / label; zero new storage slots / mappings / SSTORE / SLOAD in DPNERF scope.
    - If §2 records non-empty storage diff → measurement doc carries the `🚨 STORAGE LAYOUT REGRESSION` flag + Task 5 user-approval gate halts on the flag.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `294-01-MEASUREMENT.md` §2 carries the storage-layout grep result vs v41 baseline. DPNERF-04 storage byte-identity attestation is recorded (EMPTY diff expected). If a regression appears, the doc carries the flag for Task 5 to halt on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Public ABI byte-identity attestation — populate `294-01-MEASUREMENT.md` §4 (DPNERF-05)</name>
  <files>
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md (post-Task-2 partial state)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §4 (sister precedent — selector attestation table; `cast sig` / `forge inspect methodIdentifiers` methodology)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md §4 (sister precedent — public ABI byte-identity attestation pattern)
  </read_first>
  <action>
    Measure public ABI byte-identity vs `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` baseline.

    Toolchain: same as Task 2 (prefer forge `forge inspect methodIdentifiers`; fall back to `cast sig` per-function if methodIdentifiers unavailable).

    §4 — Public ABI Byte-Identity Attestation:
    1. Run `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule methodIdentifiers` against the post-patch tree → write to `/tmp/v42-jackpot-methods-294.json`.
    2. Run the same command against the v41 baseline worktree → write to `/tmp/v41-jackpot-methods-294.json`.
    3. Compute `diff /tmp/v41-jackpot-methods-294.json /tmp/v42-jackpot-methods-294.json`. Expected: EMPTY.

    Populate §4 with:
    - `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule methodIdentifiers diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = [EMPTY | <non-empty-diff-text>]`

    Additionally, record the key selector values for cross-citation in the commit body:
    - `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` selector — compute via `cast sig "payDailyCoinJackpot(uint24,uint256,uint24,uint24)"` (the only `external` entry within 2 hops of `_randTraitTicket`). Record the 4-byte selector. MUST match v41 close.
    - Any other public/external mutation entry points in `DegenerusGameJackpotModule.sol` reachable through `_runJackpotEthFlow` + `_distributeTicketJackpot` + `_runEarlyBirdLootboxJackpot` chains — enumerate via `grep -nE "^[[:space:]]*function.*external|^[[:space:]]*function.*public" contracts/modules/DegenerusGameJackpotModule.sol` and record each with its canonical signature + selector. ALL MUST match v41 close.

    Record private-function deltas for completeness (do NOT count against the public-ABI DPNERF-05 invariant):
    - `_randTraitTicket(address[][256] storage,uint256,uint8,uint8,uint8)` — private; canonical signature UNCHANGED at v42 (only function body changed); selector not in public ABI but recorded for audit-story completeness.

    Record in §4 as a table mirroring the Phase 290 + Phase 292 §4 shape:

    | Function | Canonical signature | 4-byte selector | Disposition |
    |---|---|---|---|
    | `payDailyCoinJackpot` | `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` | `0x........` | UNCHANGED vs v41 close |
    | <each other public/external entry point in DegenerusGameJackpotModule.sol> | `<full-sig>` | `0x........` | UNCHANGED vs v41 close |
    | `_randTraitTicket` (private) | `_randTraitTicket(address[][256] storage,uint256,uint8,uint8,uint8)` | n/a (private) | Body changed; signature UNCHANGED |

    Method line: `FOUNDRY_DISABLE_NIGHTLY_WARNING=1 cast sig "<canonical-signature>"` (foundry 1.6.0-nightly OR equivalent).

    Lock under DPNERF-05: all PUBLIC selectors byte-identical to v41 close. PASSED (expected).

    Replace the §4 `<FILL-IN-Plan-02>` placeholder with the populated diff result + table + method line + lock statement.

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "methodIdentifiers diff" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "payDailyCoinJackpot" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "_randTraitTicket\(address\[\]\[256\] storage,uint256,uint8,uint8,uint8\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "0x[0-9a-f]{8}" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ -z "$(awk '/^## §4|^## \(4\)/,/^## §5|^## \(5\)/' .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && [ -z "$(git diff --name-only HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §4 records the `forge inspect methodIdentifiers` diff result for `DegenerusGameJackpotModule.sol` (EMPTY or recorded non-empty text).
    - §4 records the 4-byte selector for `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` with the actual `0x........` value. MUST match v41 close.
    - §4 enumerates any other public/external mutation entry points in `DegenerusGameJackpotModule.sol` reachable from the `_randTraitTicket` callsite chain (via grep `^function.*external|^function.*public`) with their selectors.
    - §4 records the canonical Solidity signature for `_randTraitTicket` as `_randTraitTicket(address[][256] storage,uint256,uint8,uint8,uint8)` — recorded for audit-story completeness, not counted against the public-ABI DPNERF-05 invariant.
    - `<FILL-IN-Plan-02>` placeholder in §4 is GONE.
    - If any PUBLIC selector mismatch appears, the doc carries the `🚨 ATTESTATION REGRESSION` flag and Task 5 halts.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `294-01-MEASUREMENT.md` §4 carries the public ABI byte-identity attestation. DPNERF-05 is attested (PUBLIC selectors match v41 close). Private-function signature delta for `_randTraitTicket` (body changed; signature UNCHANGED) is documented for audit-story symmetry.
  </done>
</task>

<task type="auto">
  <name>Task 4: Bytecode-delta + zero-new-state grep-proof — populate `294-01-MEASUREMENT.md` §5 + §6 (theoretical-first per feedback_gas_worst_case.md)</name>
  <files>
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state — read the `_randTraitTicket` body in full; the post-patch line range may differ from pre-patch L1707-L1757 due to the +5-line comment expansion + the +3-line branch addition)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md (post-Task-3 partial state; §5 + §6 still carry `<FILL-IN-Plan-02>` placeholders)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md (theoretical-first methodology; no empirical second-pass at Phase 294)
  </read_first>
  <action>
    §5 — Bytecode-Delta Estimate:

    1. Theoretical derivation (already framework-locked at Plan 01; reaffirm the analytical estimate here): ~+10-30 bytes runtime bytecode for the new gold-tier branch. The else branch retains the original v41 logic verbatim — no overlapping bytecode removed.

    2. Empirical measurement: run `forge build` against the post-patch tree → record the deployed-runtime bytecode size of `DegenerusGameJackpotModule.sol`. Run `forge build` against the v41 baseline worktree (`/tmp/v41-baseline` from Task 2) → record the deployed-runtime bytecode size. Compute delta = `<v42-size> - <v41-size>` (in bytes; signed integer).

    Note: the v41 baseline already represents the state BEFORE Phase 290 + Phase 292 landed; the delta against v41 close therefore INCLUDES the Phase 290 MINTCLN delta + the Phase 292 HRROLL delta + the Phase 294 DPNERF delta. To isolate the DPNERF contribution, ALSO measure against the Phase 292 close (the most-recent intermediate anchor — cited at §1). Locate the Phase 292 close commit SHA (search via `git log --oneline contracts/modules/DegenerusGameJackpotModule.sol | grep -i hrroll` for the commit that lands the HRROLL patch; record the SHA in §5).

    Populate §5 with:
    - Theoretical estimate: `~+10-30 bytes (analytical: one EQ + JUMPI + small constant pool + gold-tier MSTORE; else branch byte-identical to v41)`
    - Empirical delta vs `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` = `<+NNN bytes>` (compound: Phase 290 + Phase 292 + Phase 294 contributions; recorded for the v41-close-to-v42-DPNERF-close delta-surface table at Phase 297)
    - Empirical delta vs Phase 292 close (commit SHA `<recorded>`) = `<+NN bytes>` (isolated: Phase 294 DPNERF contribution only)
    - Disposition: theoretical-only attestation accepted at Phase 294 per `feedback_gas_worst_case.md`; no empirical gas-regression test in Phase 295 TST-DPNERF-01..05 scope; runtime gas cost is negligible (single comparison + conditional store; well below practical measurement noise).

    If the actual byte delta vs Phase 292 close exceeds +50 bytes → flag `🚨 BYTECODE-DELTA EXCEEDS ANALYTICAL ESTIMATE — investigate before Task 5`.

    §6 — Zero-New-State Grep-Proof:

    Extract the post-patch `_randTraitTicket` function body (from the start of the function declaration at the matching pre-patch L1707 — adjusted by the +8 lines added in Task 1 — through the closing brace) into `/tmp/v42-randTraitTicket-294.sol`. Extract the v41 `_randTraitTicket` function body from the baseline worktree similarly into `/tmp/v41-randTraitTicket-294.sol`.

    Grep both for SSTORE-equivalent statements (assignments to mapping / storage-array elements; the patterns to look for include `\w+\[.*\] = `, `mapping(.*).*\.push\(`, `storage \w+ = `). Confirm both have IDENTICAL counts (the DPNERF patch should NOT introduce any new such assignments — the function is `view` so it cannot SSTORE at all; this is a structural belt-and-braces check).

    Grep both for SLOAD-equivalent statements (mapping / storage-array reads on storage variables). The only DPNERF-touched storage access is `deityBySymbol[fullSymId]` inside the existing `if (fullSymId < 32)` block; this read is UNCHANGED in count, slot, type by the patch.

    Populate §6 with:
    - Post-patch `_randTraitTicket` body line range: `<L1707-L17NN>` (record the actual line numbers post-patch).
    - SSTORE-equivalent statement count: v41 = `<N>`; v42 = `<N>` (must match; `_randTraitTicket` is `view` so both should be 0).
    - SLOAD-equivalent statement count: v41 = `<N>`; v42 = `<N>` (must match; the one storage read is `deityBySymbol[fullSymId]`).
    - Grep evidence: the actual matched lines from both versions (showing they are identical except for the gold-tier branch addition + comment-block rewrite, which add no storage accesses).
    - Lock under DPNERF-04 strengthening: zero new SSTORE / SLOAD callsites at the function-body level. PASSED (expected).

    Replace the §5 + §6 `<FILL-IN-Plan-02>` placeholders with the populated content.

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "Theoretical estimate" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Empirical delta" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "SSTORE-equivalent" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "SLOAD-equivalent" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "deityBySymbol" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ -z "$(awk '/^## §5|^## \(5\)/,/^## §6|^## \(6\)/' .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && [ -z "$(awk '/^## §6|^## \(6\)/,/^## Source-Doc/' .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && ! grep -q "FILL-IN-Plan-02" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ -z "$(git diff --name-only HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §5 records the theoretical estimate (~+10-30 bytes analytical) + the empirical delta vs `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (compound) + the empirical delta vs Phase 292 close (isolated DPNERF contribution) + the disposition statement (theoretical-only attestation; no Phase 295 gas-regression test).
    - If empirical DPNERF-isolated delta vs Phase 292 close exceeds +50 bytes, §5 carries the `🚨 BYTECODE-DELTA EXCEEDS ANALYTICAL ESTIMATE` flag.
    - §6 records the post-patch `_randTraitTicket` body line range + the SSTORE-equivalent statement count (v41 vs v42; must match) + the SLOAD-equivalent statement count (v41 vs v42; must match) + the grep evidence (matched lines from both versions).
    - §6 confirms `deityBySymbol[fullSymId]` is the only DPNERF-touched storage access and is UNCHANGED.
    - §6 records the "Lock under DPNERF-04 strengthening: zero new SSTORE / SLOAD callsites at function-body level" statement.
    - ALL `<FILL-IN-Plan-02>` placeholders in `294-01-MEASUREMENT.md` are now GONE.
    - If §5 or §6 shows any unexpected mismatch, the doc carries the corresponding `🚨` flag and Task 5 halts.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `294-01-MEASUREMENT.md` §5 carries the theoretical + empirical bytecode-delta attestation. §6 carries the zero-new-state grep-proof for the `_randTraitTicket` body. ALL 6 sections of `294-01-MEASUREMENT.md` are now populated (§1 + §3 from Plan 01; §2 + §4 + §5 + §6 from Plan 02 Tasks 2-4) and ready for copy-forward into the Task 5 commit message body.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Present batched contract + measurement diff for explicit USER review + USER-APPROVED commit (NO push)</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol,
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
  </files>
  <read_first>
    - All 2 files in `<files>` (full post-Task-1..4 state)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md (Plan 01 output — referenced in the commit message body)
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md (commit message subject locked in `<specifics>`)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
  </read_first>
  <action>
    Present the working-tree diff to the user for explicit review per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` + `feedback_batch_contract_approval.md` + `feedback_wait_for_approval.md`.

    Pre-flight checks the agent runs FIRST (do not proceed if any fail):

    1. Confirm `294-01-MEASUREMENT.md` carries no `<FILL-IN-Plan-02>` placeholders and no `🚨 STORAGE LAYOUT REGRESSION` / `🚨 ATTESTATION REGRESSION` / `🚨 BYTECODE-DELTA EXCEEDS ANALYTICAL ESTIMATE` flags.
    2. Confirm `git status --porcelain` shows EXACTLY the 2 paths in `<files>` (`contracts/modules/DegenerusGameJackpotModule.sol` + `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md`) as modified — no other paths.
    3. Confirm `git diff contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` is EMPTY.
    4. Confirm `grep -qE "if \(\(\(trait >> 3\) & 7\) == 7\)" contracts/modules/DegenerusGameJackpotModule.sol` matches (gold-tier branch present).
    5. Confirm `grep -qE "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol` matches (gold-tier assignment present).
    6. Confirm `grep -qE "Gold tier \(color == 7\): flat 1 virtual entry\." contracts/modules/DegenerusGameJackpotModule.sol` matches (locked comment block present).
    7. Confirm `grep -qE "Common tier \(color in \[0\.\.6\]\): floor\(2% of bucket\), minimum 2\." contracts/modules/DegenerusGameJackpotModule.sol` matches.
    8. Confirm `grep -qE "/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices\." contracts/modules/DegenerusGameJackpotModule.sol` matches (function-level docstring UNCHANGED).
    9. Confirm `grep -cE "function _randTraitTicket" contracts/modules/DegenerusGameJackpotModule.sol` returns `1` (function exists exactly once).
    10. Confirm `! grep -qE "GOLD_COLOR" contracts/modules/DegenerusGameJackpotModule.sol` (no named constant introduced).
    11. Confirm `! grep -qE "uint8 color = " contracts/modules/DegenerusGameJackpotModule.sol` (no local-var cache introduced).
    12. Confirm `! grep -qE "// D-42N-GOLD-FLOOR-01|// DPNERF-01|// Phase 294" contracts/modules/DegenerusGameJackpotModule.sol` (no decision-anchor citations in source comments).
    13. Confirm filtered grep for history language (`previously|pre-v41|pre-cleanup|formerly|was max\(len/50|the old form|before this nerf|historically`) after stripping comment-leader lines returns ZERO matches in the patched file.
    14. Confirm `grep -qE "_pickSoloQuadrant" contracts/modules/DegenerusGameJackpotModule.sol` STILL matches AND the function body is byte-identical pre/post-patch (verified by `git diff` showing zero hunks outside L1707-L17NN range).
    15. Confirm `forge build` (or equivalent) succeeds on the post-patch tree (no compile errors).

    If any check fails → escalate to user with the failure mode; do NOT proceed to the diff presentation.

    Present the consolidated diff: run `git diff contracts/modules/DegenerusGameJackpotModule.sol .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` and ask the user to review per `<how-to-verify>` below. Wait for the user to type "approved" (or to describe required fixes) per `feedback_wait_for_approval.md`. On "approved": stage exactly the 2 paths listed in `<files>` and create ONE batched commit using the commit-template in `<how-to-verify>`. Substitute the placeholders (`<0x...>`, `[EMPTY | <recorded value>]`, `<actual byte delta>`, `<post-patch line range>`) with the actual values from `294-01-MEASUREMENT.md`. Do NOT `git push` after the commit — pushing requires a separate explicit user instruction per `feedback_manual_review_before_push.md`. See `<what-built>` + `<how-to-verify>` + `<resume-signal>` for the full checkpoint specification.
  </action>
  <verify>
    <human-check>User reads the full diff via `git diff contracts/modules/DegenerusGameJackpotModule.sol`, reads `294-01-DESIGN-INTENT-TRACE.md` (Plan 01 output) + `294-01-MEASUREMENT.md` (populated through Task 4), and types "approved" before any commit. Post-commit verification: `git log -1 --pretty=format:"%h %s"` shows the new commit subject `feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]`; `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` is EMPTY.</human-check>
    <automated>git log -1 --pretty=format:"%s" | grep -qE "DPNERF" && [ -z "$(git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md 2>/dev/null)" ] && grep -qE "if \(\(\(trait >> 3\) & 7\) == 7\)" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "Gold tier \(color == 7\): flat 1 virtual entry\." contracts/modules/DegenerusGameJackpotModule.sol && ! grep -qE "GOLD_COLOR" contracts/modules/DegenerusGameJackpotModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight checks 1-15 all PASS before the user-diff presentation.
    - User reads the full diff + the trace doc + the measurement doc and types "approved".
    - Exactly ONE batched commit lands covering the 2 paths in `<files>`.
    - Commit subject matches the locked template: `feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]`.
    - `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` is EMPTY (storage / sibling modules / interfaces / tests / KI UNCHANGED).
    - `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` shows ONLY the Task 1 Edit A (comment-block rewrite at pre-patch L1721-L1723) + Edit B (gold-tier branch insertion at pre-patch L1730-L1731) hunks within the `_randTraitTicket` body — plus any prior Phase 290 + Phase 292 hunks already present at HEAD (which are NOT part of this Phase 294 commit; they are at earlier commits).
    - `_pickSoloQuadrant` body (pre-patch L1080-L1130) byte-identical at HEAD vs `315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` baseline (out-of-scope surface UNCHANGED).
    - No `git push` issued by the executor.
  </acceptance_criteria>
  <done>
    ONE USER-APPROVED batched commit lands at HEAD covering: the contract patch (Task 1 Edit A comment-block rewrite + Edit B gold-tier branch insertion) + the populated `294-01-MEASUREMENT.md`. KNOWN-ISSUES UNCHANGED. Sibling modules + storage + interfaces + tests UNCHANGED. No `git push`. Phase 294 audit-subject HEAD ready for Phase 295 TST-DPNERF fixture to assert against + for Phase 296 SWEEP 3-skill PARALLEL adversarial pass to red-team + for Phase 297 audit deliverable §3.A delta-surface table to cite.
  </done>
  <what-built>
    Two working-tree changes ready for ONE batched commit covering DPNERF-01..05:

    1. `contracts/modules/DegenerusGameJackpotModule.sol` — 2 hunk groups inside the `_randTraitTicket` function body:
       (a) Comment-block rewrite at pre-patch L1721-L1723: the 3-line block becomes the locked 5-line two-tier `what IS` shape per D-294-NATSPEC-01 (Gold tier flat 1; Common tier floor(2%) min 2 + traitId layout + fullSymId formula). Zero history language; zero decision-anchor citations.
       (b) Gold-tier branch insertion at pre-patch L1730-L1731 (inside the existing `if (deity != address(0))` block): the two pre-patch lines `virtualCount = len / 50;` + `if (virtualCount < 2) virtualCount = 2;` become the locked branched form `if (((trait >> 3) & 7) == 7) { virtualCount = 1; } else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }`. Gold-tier executes `virtualCount = 1`; commons execute the unchanged v41 logic in the else branch.

       The function-level docstring at pre-patch L1706 is UNCHANGED. The function signature is UNCHANGED. The non-deity branch + the downstream `effectiveLen = len + virtualCount;` arithmetic + the winner-sampling loop are UNCHANGED. All adjacent code (including `_pickSoloQuadrant` at pre-patch L1080-L1130) is UNCHANGED.

    2. `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` — §2 + §4 + §5 + §6 populated post-patch:
       - §2 storage byte-identity: `forge inspect storageLayout` diff EMPTY for `DegenerusGameJackpotModule.sol` + `DegenerusGameStorage.sol` vs `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` baseline; `deityBySymbol` at unchanged slot/type/label.
       - §4 public ABI byte-identity: `forge inspect methodIdentifiers` diff EMPTY for `DegenerusGameJackpotModule.sol`; `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` selector UNCHANGED; private `_randTraitTicket` signature UNCHANGED (only body changed).
       - §5 bytecode-delta: theoretical ~+10-30 bytes (analytical: one EQ + JUMPI + small constant pool + gold-tier MSTORE); empirical actual delta filled in; no Phase 295 gas-regression test (per `feedback_gas_worst_case.md` + TST-DPNERF scope).
       - §6 zero-new-state grep-proof: zero new SSTORE / SLOAD callsites at the `_randTraitTicket` function-body level; the only DPNERF-touched storage access remains the existing `deityBySymbol[fullSymId]` SLOAD inside the unchanged-conditional `if (fullSymId < 32)` block.
       - §1 + §3 unchanged from Plan 01 final state.

    The contract diff is small (~10-15 hunk lines: the 5-line comment block + the 6-line branch + the 2-line removal of the pre-patch shape). Storage layout UNCHANGED. Sibling modules UNCHANGED. Public ABI UNCHANGED.

    Per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md`: the orchestrator + planner have NOT pre-approved this diff. Per `feedback_manual_review_before_push.md`: the user reviews the diff IN FULL before commit; no `git push` without a second explicit user instruction afterward. Per `feedback_batch_contract_approval.md`: ONE batched commit covers all DPNERF-01..05 edits + attestations (no piecemeal commits).
  </what-built>
  <how-to-verify>
    1. Run `git status` — confirm only the 2 expected paths are modified/created (no rogue contract / test / KI edits; no Degenerette / Whale / DeityPass / Boon / Storage / Interfaces module edits).
    2. Run `git diff contracts/modules/DegenerusGameJackpotModule.sol` — read the contract diff in full. Confirm:
       - Function-level docstring at the `_randTraitTicket` header UNCHANGED (`/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices.`).
       - Function signature UNCHANGED — 5 parameters, `private view`, returns `(address[] memory winners, uint256[] memory ticketIndexes)`.
       - Comment block (pre-patch L1721-L1723) rewritten to the locked 5-line shape:
         ```
         // Virtual deity entries (if a deity exists for this symbol):
         //   Gold tier (color == 7): flat 1 virtual entry.
         //   Common tier (color in [0..6]): floor(2% of bucket), minimum 2.
         // traitId layout: (quadrant << 6) | (color << 3) | symIdx
         // fullSymId = quadrant * 8 + symIdx
         ```
       - Gold-tier branch inserted inside `if (deity != address(0))` block:
         ```
         if (((trait >> 3) & 7) == 7) {
             virtualCount = 1;
         } else {
             virtualCount = len / 50;
             if (virtualCount < 2) virtualCount = 2;
         }
         ```
       - Else branch contains the v41 logic verbatim (`virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2;`).
       - The downstream `effectiveLen = len + virtualCount;` line UNCHANGED.
       - The winner-sampling loop (keccak input + modulo + idx-vs-len) UNCHANGED.
       - The non-deity branch UNCHANGED.
       - Zero comparative/historical language in any comment touched (no `previously`, `pre-v41`, `pre-cleanup`, `formerly`, `was max(len/50, 2)`, `the old form`, `before this nerf`, `historically`).
       - Zero decision-anchor citations in source comments (no `// D-42N-GOLD-FLOOR-01`, no `// DPNERF-01`, no `// Phase 294`).
       - Zero `GOLD_COLOR = 7` named constant.
       - Zero `uint8 color = (trait >> 3) & 7;` local-var cache.
       - `_pickSoloQuadrant` (pre-patch L1080-L1130) UNCHANGED — verify by scrolling through the diff and confirming no hunks outside the `_randTraitTicket` body.
    3. Run `git diff contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` — confirm EMPTY (no storage / sibling-module / interface / test / KI changes).
    4. Read `294-01-DESIGN-INTENT-TRACE.md` (Plan 01 output) — confirm 4 DPNERF-06 trace sections + 5 phase-scope decision anchors + out-of-scope register + SWEEP-02(iii) pre-emptive answers + Plan-02 pre-patch-gate.
    5. Read `294-01-MEASUREMENT.md` (populated through Task 4) — confirm:
       - §1 audit baseline anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` + Phase 292 close intermediate anchor (FINAL at Plan 01 time).
       - §2 storage-layout grep diff EMPTY for `DegenerusGameJackpotModule.sol` + `DegenerusGameStorage.sol` (or escalated flag if non-empty).
       - §3 callsite enumeration table — all 4 callsites (L698 + L988 + L1296 + L1399) + BURNIE path (FINAL at Plan 01 time).
       - §4 `methodIdentifiers` diff EMPTY for `DegenerusGameJackpotModule.sol`; `payDailyCoinJackpot` selector UNCHANGED.
       - §5 theoretical estimate (~+10-30 bytes) + actual empirical delta vs v41 baseline + Phase 292 close + disposition (no Phase 295 gas-regression test).
       - §6 zero-new-state grep-proof — SSTORE-equivalent count matches v41 vs v42; SLOAD-equivalent count matches; `deityBySymbol[fullSymId]` is the only DPNERF-touched storage access.
       - No `<FILL-IN-Plan-02>` placeholders remain.
       - No `🚨 STORAGE LAYOUT REGRESSION` / `🚨 ATTESTATION REGRESSION` / `🚨 BYTECODE-DELTA EXCEEDS ANALYTICAL ESTIMATE` flags.
    6. If everything is correct, instruct the agent to commit via the template below. If anything is wrong, instruct the agent to fix it BEFORE commit (do not commit-then-revert).

    Commit-template the executor uses post-approval (do NOT execute until user types "approved"):
    ```
    git add contracts/modules/DegenerusGameJackpotModule.sol \
            .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
    git commit -m "$(cat <<'EOF'
    feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]

    D-42N-GOLD-FLOOR-01 — flat 1 virtual entry on gold-tier (color == 7) (user-locked 2026-05-17)
    D-42N-DEITY-EV-01 — intentional EV reduction; no common-tier compensation (user-locked 2026-05-17)
    D-42N-PATH-COVERAGE-01 — both ETH + BURNIE coin jackpot paths covered (user-locked 2026-05-17)
    D-294-CALLER-UNIFORM-01 — all 4 _randTraitTicket callsites covered uniformly by the single function-body change (planner-locked 2026-05-17)
    D-294-NATSPEC-01 — locked 5-line two-tier `what IS` comment block; no history language; no anchor citations in source (planner-locked 2026-05-17)

    Design-intent trace: see 294-01-DESIGN-INTENT-TRACE.md (DPNERF-06; per feedback_design_intent_before_deletion.md) — 4-section trace covers (i) original `virtualCount = len/50; if < 2: = 2` rationale (v41 small-bucket baseline-payout guarantee for deity), (ii) gold-tile concentration issue (small-bucket × 2% floor disproportionately rewards deity on smallest tier), (iii) compensation trade-offs locked at intentional reduction (no commons-bump, no rebias-toward-commons; deity earns less total EV across all 8 colors), (iv) path-coverage trade-offs locked at all-4-callsite uniform (no callsite flag; no path-discrimination logic). SWEEP-02(iii) DPNERF adversarial-hypothesis pre-emptive answers recorded for Phase 296 (all-4-callsite uniformity / carryover-ticket-distribution / secondary-strategy / ETH-vs-BURNIE differential — all SAFE_BY_* dispositions). Out-of-scope register enumerates 17 items.

    Contract patch:
    - Inline comment block at pre-patch L1721-L1723 rewritten to the locked 5-line two-tier `what IS` shape per D-294-NATSPEC-01 (Gold tier flat 1 / Common tier floor(2%) min 2 / traitId layout / fullSymId formula). Zero history language; zero decision-anchor citations in source per feedback_no_history_in_comments.md.
    - Gold-tier branch inserted at pre-patch L1730-L1731 inside the existing `if (deity != address(0))` block: `if (((trait >> 3) & 7) == 7) { virtualCount = 1; } else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }`. Reuses the established inline color-extraction idiom from `_pickSoloQuadrant` precedent at L1105 per feedback_frozen_contracts_no_future_proofing.md (no GOLD_COLOR constant; no color local-var cache).
    - Function-level docstring at L1706 UNCHANGED. Function signature UNCHANGED. Non-deity branch + else (commons) branch + downstream effectiveLen arithmetic + winner-sampling loop UNCHANGED.

    Path coverage by construction (DPNERF-02 + D-294-CALLER-UNIFORM-01): the single function-body change reaches all 4 _randTraitTicket callsites uniformly: L698 _runEarlyBirdLootboxJackpot + L988 _distributeTicketsToBucket (helper called by _distributeTicketJackpot from L637/L652/L883) + L1296 _processDailyEth (the roadmap-named _runJackpotEthFlow path) + L1399 _resolveTraitWinners — plus the BURNIE near-future coin jackpot path via payDailyCoinJackpot (L1767) → _awardDailyCoinToTraitWinners (L1816+). Zero callsite-side edits.

    Storage byte-identity (DPNERF-04): see 294-01-MEASUREMENT.md §2 — forge inspect storageLayout diff for contracts/modules/DegenerusGameJackpotModule.sol + contracts/storage/DegenerusGameStorage.sol vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 = [EMPTY | <recorded value>]. deityBySymbol[uint8 => address] at unchanged slot/type/label. Zero new storage slots; zero new mappings; zero new SSTORE/SLOAD callsites in DPNERF scope (_randTraitTicket reads the SAME deityBySymbol[fullSymId] slot at the SAME conditional block that v41 read). §6 zero-new-state grep-proof at the function-body level confirms identical SSTORE/SLOAD counts pre/post-patch.

    Public ABI byte-identity (DPNERF-05): see 294-01-MEASUREMENT.md §4 — forge inspect methodIdentifiers diff for contracts/modules/DegenerusGameJackpotModule.sol = [EMPTY | <recorded value>]. payDailyCoinJackpot(uint24,uint256,uint24,uint24) selector <0x........> UNCHANGED vs v41 close. All other public/external entry points UNCHANGED. _randTraitTicket is private (canonical signature _randTraitTicket(address[][256] storage,uint256,uint8,uint8,uint8) UNCHANGED — only function body changed; private-function delta does NOT count against public-ABI invariant). Zero new public/external entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks.

    Theoretical bytecode delta (per feedback_gas_worst_case.md): see 294-01-MEASUREMENT.md §5 — analytical estimate ~+10-30 bytes (one EQ + JUMPI + small constant pool + gold-tier MSTORE; else branch byte-identical to v41). Empirical actual delta vs Phase 292 close: <+NN bytes>. NO empirical gas-regression test in Phase 295 TST-DPNERF-01..05 scope (TST-DPNERF-04 is an EV regression at N=1000, not gas). Runtime gas cost negligible — single comparison + conditional store.

    RNG audit-methodology disposition: feedback_rng_backward_trace.md + feedback_rng_commitment_window.md NOT applicable to Phase 294 — DPNERF doesn't introduce a new RNG consumer (_randTraitTicket's randomWord consumption pattern UNCHANGED at the existing winner-sampling loop); gold-tier branch is deterministic on `trait` which is already committed at VRF-request time by the upstream _rollWinningTraits consumer. No commitment-window backward-trace burden added.

    Out-of-scope surfaces UNCHANGED (per REQUIREMENTS.md lines 22-27 + CONTEXT.md out-of-scope register): _pickSoloQuadrant at L1080-L1130 byte-identical; DegenerusGameDegeneretteModule.sol UNCHANGED (boon distribution + BP_DEITY_PASS_TIER_SHIFT); DegenerusGameWhaleModule.sol UNCHANGED (DEITY_PASS_BASE); DegenerusDeityPass.sol UNCHANGED (soulbound NFT); DegenerusGameBoonModule.sol UNCHANGED; DegenerusGameStorage.sol UNCHANGED; IDegenerusGameModules.sol UNCHANGED; test/ UNCHANGED (TST-DPNERF ships at Phase 295); KNOWN-ISSUES.md UNMODIFIED (per D-42N-KI-01 default disposition at Phase 297).

    Carry-forward locks preserved:
    - D-42N-MILESTONE-OPEN-01: v42.0 milestone open at v41 close HEAD 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 — Phase 294 audit-subject ships against this baseline.
    - D-281-FIX-SHAPE-01: v41 mint-batch determinism path UNCHANGED (distinct subsystem).
    - D-288-FIX-SHAPE-01: v41 dailyIdx structural anchor UNCHANGED (distinct subsystem).
    - D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-02 + D-271-ADVERSARIAL-03: adversarial pass deferred to Phase 296 SWEEP (3-skill PARALLEL: /contract-auditor + /zero-day-hunter + /economic-analyst; /degen-skeptic OUT). Per D-294-CALLER-UNIFORM-01: SWEEP DPNERF hypothesis surface MUST cover all 4 callsites (uniformity attestation + incentive-shift across early-bird lootbox + carryover-ticket-distribution paths).

    Phase 294 — third contract-surface phase of v42.0 milestone (after Phase 290 MINTCLN + Phase 292 HRROLL).
    Audit baseline: MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4
    Intermediate anchor: Phase 292 close.
    Wave shape: 1 USER-APPROVED batched contract commit (per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md + feedback_manual_review_before_push.md + feedback_wait_for_approval.md)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    DO NOT push after commit. Per `feedback_manual_review_before_push.md`: pushing requires a second explicit user instruction.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the batched commit, or describe required fixes (the agent will apply fixes and re-present for review before any commit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01 trace + scaffold → Plan 02 executor | The 5 phase-scope anchors (D-42N-GOLD-FLOOR-01 + D-42N-DEITY-EV-01 + D-42N-PATH-COVERAGE-01 + D-294-CALLER-UNIFORM-01 + D-294-NATSPEC-01) + the §3 callsite enumeration + the §5 theoretical bytecode-delta framework are the load-bearing inputs Plan 02 cites; corruption of any anchor or framework would let Plan 02 ship a deviation from the user's locked decisions. Mitigated by the Task 1 pre-patch gate + the post-Task acceptance criteria grep gates on the locked branch shape + locked comment shape. |
| `_randTraitTicket` function body → all 4 callsites + BURNIE path | Single function-body change reaches every caller uniformly by construction per D-294-CALLER-UNIFORM-01 — no callsite-side edits possible without diverging from the locked patch shape. Mitigated by Task 1 acceptance criterion enforcing zero `git diff` in any file other than the two paths in `<files>`. |
| Player → trait-bucket distribution via the 4 jackpot paths | The deity-virtual-count allocation determines deity's win probability on each trait bucket; the gold-tier nerf reduces deity's expected share on gold-color wins from `max(len/50, 2)` to 1 virtual entry. Player cannot influence which trait wins (`randomWord` from VRF; committed at jackpot-resolution time strictly after wager + ticket-purchase + lootbox-purchase actions). |
| Chainlink VRF → jackpot resolution → `_randTraitTicket` winner-sampling | The `randomWord` consumption pattern in the winner-sampling loop at L1740-L1756 is UNCHANGED by Phase 294 — `randomWord` flows through the same `keccak256(abi.encode(randomWord, trait, salt, i)) % effectiveLen` cursor. The gold-tier branch affects `effectiveLen` via `virtualCount` but does NOT alter the RNG consumption pattern. No commitment-window burden added per CONTEXT.md `<canonical_refs>` audit-methodology disposition. |
| `deityBySymbol[fullSymId]` storage → `_randTraitTicket` gold-tier check | The deity-existence check at L1728 reads the SAME storage slot pre- and post-patch; the gold-tier branch fires only when `deity != address(0)` (matching v41 behavior). The mapping is set at deity-pass mint time by a separate module (out of DPNERF scope); Phase 294 doesn't touch the writer side. |
| Plan 02 measurement scaffold → Plan 02 batched commit message body | The §2 storage byte-identity result + §4 public ABI byte-identity result + §5 bytecode-delta + §6 zero-new-state grep-proof are the copy-forward source for the commit body. Corruption of any §-fill would force the commit body to invent attestations ad-hoc. Mitigated by Task 5 pre-flight check #1 (no `<FILL-IN-Plan-02>` placeholders + no `🚨` flags). |

## STRIDE Threat Register — Phase 294 Plan 02 (DPNERF Contract Patch)

(Mirrors the SWEEP-02(iii) DPNERF adversarial hypotheses pre-emptively answered in `294-01-DESIGN-INTENT-TRACE.md` per the planner contract; Phase 296 PARALLEL adversarial pass audits against this same register.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-294-02-01 | Tampering | Gold-tier branch shape divergence from CONTEXT.md `<specifics>` lock | mitigate | Task 1 acceptance criterion enforces grep matches on the EXACT branch shape `if (((trait >> 3) & 7) == 7) { virtualCount = 1; }` + `else { virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2; }`. Task 5 pre-flight checks 4-7 reaffirm at user-approval time. The locked shape comes verbatim from CONTEXT.md `<specifics>` — no executor latitude. |
| T-294-02-02 | Tampering | Comment-block divergence from D-294-NATSPEC-01 lock | mitigate | Task 1 acceptance criterion enforces grep matches on the 3 exact comment lines (`Virtual deity entries (if a deity exists for this symbol):` + `Gold tier (color == 7): flat 1 virtual entry.` + `Common tier (color in [0..6]): floor(2% of bucket), minimum 2.`). Task 5 pre-flight checks 6-7 reaffirm. History-language grep + decision-anchor-citation grep at Task 1 acceptance criterion enforce the `feedback_no_history_in_comments.md` + D-294-NATSPEC-01 disciplines. |
| T-294-02-03 | Spoofing / Tampering | All-4-callsite uniformity (D-294-CALLER-UNIFORM-01) | mitigate | Function-body change is the SOLE mechanism of nerf delivery; by construction all 4 callsites + BURNIE path inherit the nerf uniformly. Task 1 acceptance criterion enforces `_randTraitTicket(` callsite count ≥ 5 (1 declaration + 4 callsites; min bound). `294-01-MEASUREMENT.md` §3 callsite enumeration table (FINAL at Plan 01 time) records all 4 callsites for Phase 296 SWEEP + Phase 297 §3.A delta-surface table citation. SWEEP-02(iii) Q1 + Q2 expected disposition: SAFE_BY_DESIGN + SAFE_BY_STRUCTURAL_UNIFORMITY. |
| T-294-02-04 | Information Disclosure | Intentional EV reduction (D-42N-DEITY-EV-01) — secondary-strategy destabilization | accept | Phase 294 explicitly accepts that deity-pass holder economics shift toward common-color EV emphasis; commons floor UNCHANGED so commons-tier dynamics are not directly perturbed. Any secondary-market deity-pass price shift is an INTENDED outcome of the nerf, not an exploit. SWEEP-02(iii) Q3 expected disposition: SAFE_BY_INTENT. Documented in `294-01-DESIGN-INTENT-TRACE.md` § Section (iii). |
| T-294-02-05 | Tampering | ETH vs BURNIE differential-behavior | mitigate | NO differential by construction — single function-body change in `_randTraitTicket` reaches both paths via the same code path; no callsite flag; no path-discrimination logic. Task 1 acceptance criterion + Task 5 pre-flight reaffirm zero callsite-side edits. SWEEP-02(iii) Q4 expected disposition: SAFE_BY_CONSTRUCTION. |
| T-294-02-06 | Tampering | Storage byte-identity (DPNERF-04) | mitigate | Task 2 §2 records `forge inspect storageLayout` diff for `DegenerusGameJackpotModule.sol` + `DegenerusGameStorage.sol` vs v41 close baseline (expected EMPTY). Plan 02 makes ZERO edits to `contracts/storage/DegenerusGameStorage.sol`. `_randTraitTicket` reads the SAME `deityBySymbol[fullSymId]` slot that v41 read — same SLOAD count, same access pattern. Task 6 §6 grep-proof strengthens at the function-body level. Task 5 pre-flight halts if `git diff contracts/storage/` shows any change. |
| T-294-02-07 | Tampering | Public ABI byte-identity (DPNERF-05) | mitigate | Task 3 §4 records `forge inspect methodIdentifiers` diff for `DegenerusGameJackpotModule.sol` (expected EMPTY) + the `payDailyCoinJackpot` selector + all other public/external entry points. `_randTraitTicket` is private (signature UNCHANGED — only body changed). Zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks. Task 5 pre-flight check #15 confirms `forge build` succeeds. |
| T-294-02-08 | Tampering | RNG consumption pattern UNCHANGED | mitigate | The winner-sampling loop at pre-patch L1740-L1756 (`keccak256(abi.encode(randomWord, trait, salt, i)) % effectiveLen` cursor) is UNCHANGED by the gold-tier branch. The branch only affects `virtualCount` (and therefore `effectiveLen = len + virtualCount`), not the RNG input set. `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` NOT applicable per CONTEXT.md `<canonical_refs>` audit-methodology disposition. |
| T-294-02-09 | Tampering | Frozen-contract discipline drift — `GOLD_COLOR` constant / `color` local-var cache | mitigate | Task 1 acceptance criterion enforces grep `! grep -qE "GOLD_COLOR"` AND `! grep -qE "uint8 color = "` in the patched file. Per `feedback_frozen_contracts_no_future_proofing.md`: deploy-frozen contracts MUST NOT carry extensibility hooks; the magic-`7` is well-established across the codebase (10+ source citations). Task 5 pre-flight checks 10-11 reaffirm at user-approval time. |
| T-294-02-10 | Tampering | Dead-guard / unreachable-branch insertion | mitigate | The patch adds ONE new conditional branch (`if (((trait >> 3) & 7) == 7) {`) — both arms are reachable by construction (some `trait` values have color 7; others don't). No defensive `return` / `revert` added; no `// @dev removed` marker; no preserved-alternative-mechanic flag-gated branch. Per `feedback_no_dead_guards.md`. The Task 1 acceptance criterion's structural diff implicitly enforces (the diff is tightly bounded to the locked branch shape + the locked comment shape). |
| T-294-02-11 | Tampering | NatSpec history-language drift / decision-anchor citation drift | mitigate | Task 1 acceptance criterion enforces zero history language (filtered grep) AND zero anchor citations in source comments (`grep -qE "// D-42N-GOLD-FLOOR-01|// DPNERF-01|// Phase 294"` returns false). Per `feedback_no_history_in_comments.md` + D-294-NATSPEC-01. Task 5 pre-flight checks 12-13 reaffirm. The audit-decision narrative lives in `294-01-DESIGN-INTENT-TRACE.md` + the commit message body + `audit/FINDINGS-v42.0.md` at Phase 297 — NEVER in source. |
| T-294-02-12 | Tampering | Out-of-scope source-tree surface drift | mitigate | Task 1 acceptance criterion enforces `git diff --name-only` showing zero changes in `contracts/storage/`, `contracts/modules/DegenerusGameDegeneretteModule.sol`, `contracts/modules/DegenerusGameWhaleModule.sol`, `contracts/DegenerusDeityPass.sol`, `contracts/modules/DegenerusGameBoonModule.sol`, `contracts/interfaces/`, `test/`, `KNOWN-ISSUES.md`. Per CONTEXT.md `<code_context>` Out-of-Scope Source-Tree Surfaces + REQUIREMENTS.md lines 22-27. Task 5 pre-flight check #3 reaffirms at user-approval time. `_pickSoloQuadrant` byte-identical post-patch (check 14). |
| T-294-02-13 | Tampering | Wave-shape lock — single batched commit per `feedback_batch_contract_approval.md` | mitigate | Task 5 is the only commit gate; Tasks 1-4 produce a working-tree diff with NO intermediate commits. Per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. User reviews the consolidated diff once; ONE batched commit lands; NO `git push` per `feedback_manual_review_before_push.md`. |
| T-294-02-SC | Tampering | Package install (npm/pip/cargo) | accept (rationale: not applicable) | Phase 294 makes ZERO package installs. No npm/pip/cargo `install` operations scheduled. Adversarial-pass spawn at Phase 296 covers the smart-contract adversarial surface. Package-legitimacy gate does not fire. |

(Severity ceiling for this plan: `medium`. The gold-tier nerf is a player-experience rebalance with bounded bytecode regression + byte-identical storage + byte-identical public ABI + zero new RNG consumer + zero new attack surface. The threat surface reduces to (i) branch-shape correctness — locked by CONTEXT.md `<specifics>` + enforced by Task 1 acceptance criteria; (ii) byte-identity attestation correctness — enforced by Task 2-4 measurement framework + Task 5 pre-flight checks; (iii) user-approval gate correctness — enforced by `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. No `high` severity threat is in scope.)
</threat_model>

<verification>
After Task 5's USER-APPROVED commit:

1. `git log -1 --pretty=format:"%h %s"` shows the new commit with subject containing `DPNERF` (and matching the locked subject `feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]`).
2. `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` shows the Phase 294 hunks within the `_randTraitTicket` body (plus any prior Phase 292 HRROLL hunks in the same file at earlier commits — those are NOT part of this commit but DO appear in the v41-to-HEAD comparison).
3. `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` is EMPTY (zero out-of-scope modifications across the full v42.0 surface chain).
4. `forge build` (or hardhat compile) succeeds for the post-patch tree (no compile errors from the gold-tier branch addition).
5. `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule storageLayout` diffed against v41 close baseline is EMPTY (per Task 2 §2 record).
6. `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule methodIdentifiers` diffed against v41 close baseline is EMPTY (per Task 3 §4 record).
7. `grep -qE "if \(\(\(trait >> 3\) & 7\) == 7\)" contracts/modules/DegenerusGameJackpotModule.sol` matches (gold-tier branch landed).
8. `grep -qE "virtualCount = 1;" contracts/modules/DegenerusGameJackpotModule.sol` matches.
9. `grep -qE "Gold tier \(color == 7\): flat 1 virtual entry\." contracts/modules/DegenerusGameJackpotModule.sol` matches.
10. `grep -qE "Common tier \(color in \[0\.\.6\]\): floor\(2% of bucket\), minimum 2\." contracts/modules/DegenerusGameJackpotModule.sol` matches.
11. `! grep -qE "GOLD_COLOR" contracts/modules/DegenerusGameJackpotModule.sol` (no named constant introduced).
12. `! grep -qE "uint8 color = " contracts/modules/DegenerusGameJackpotModule.sol` (no local-var cache introduced).
13. `! grep -qE "// D-42N-GOLD-FLOOR-01|// DPNERF-01|// Phase 294" contracts/modules/DegenerusGameJackpotModule.sol` (no decision-anchor citations in source).
14. Filtered grep for history language returns ZERO matches after stripping comment-leader lines.
15. Both plan-02 artifacts have been committed: `contracts/modules/DegenerusGameJackpotModule.sol` (patched) + `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` (populated).
16. KNOWN-ISSUES.md UNMODIFIED at this plan's HEAD.
17. NO `git push` issued by the executor.
</verification>

<success_criteria>
- DPNERF-01 contract patch lands: gold-tier branch inserted at pre-patch L1730-L1731 inside the existing `if (deity != address(0))` block with the locked shape from CONTEXT.md `<specifics>`.
- DPNERF-02 path coverage achieved by construction: single function-body change reaches all 4 callsites + BURNIE path uniformly; zero callsite-side edits.
- DPNERF-03 common-tier preserved: else branch retains v41 `virtualCount = max(len/50, 2)` verbatim; intentional EV reduction per D-42N-DEITY-EV-01.
- DPNERF-04 storage byte-identity attested via `forge inspect storageLayout` EMPTY diff in `294-01-MEASUREMENT.md` §2; strengthened by §6 zero-new-state grep-proof at function-body level.
- DPNERF-05 public ABI byte-identity attested via `forge inspect methodIdentifiers` EMPTY diff in `294-01-MEASUREMENT.md` §4.
- D-294-NATSPEC-01 comment-block rewrite landed verbatim from CONTEXT.md `<specifics>` (5-line two-tier `what IS` shape); zero history language; zero decision-anchor citations in source comments.
- Theoretical bytecode-delta attestation in `294-01-MEASUREMENT.md` §5 per `feedback_gas_worst_case.md` (no empirical second-pass at Phase 294).
- ONE USER-APPROVED batched commit lands with the locked commit subject `feat(294): deity-pass gold nerf via flat-1 virtualCount on color==7 [DPNERF-01..06]`.
- All out-of-scope surfaces UNCHANGED (storage / sibling modules / interfaces / tests / KNOWN-ISSUES / `_pickSoloQuadrant`).
- No `git push` issued.
- Phase 294 audit-subject HEAD ready for Phase 295 TST-DPNERF + Phase 296 SWEEP + Phase 297 terminal closure-flip.
</success_criteria>

<output>
Create `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-02-SUMMARY.md` when this plan completes. Record: (a) the actual post-patch line range of the `_randTraitTicket` function body; (b) the commit SHA of the USER-APPROVED batched commit; (c) the storage-layout diff result (EMPTY expected); (d) the public-ABI methodIdentifiers diff result (EMPTY expected); (e) the actual bytecode-delta byte count vs Phase 292 close (isolated DPNERF contribution); (f) explicit confirmation that `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusDeityPass.sol contracts/modules/DegenerusGameBoonModule.sol contracts/interfaces/ test/ KNOWN-ISSUES.md` is EMPTY; (g) explicit hand-off statement: "Phase 294 audit-subject HEAD ready for Phase 295 TST-DPNERF-01..05 regression fixture + Phase 296 SWEEP 3-skill PARALLEL adversarial pass + Phase 297 §3.A delta-surface table citation."
</output>
</content>
</invoke>