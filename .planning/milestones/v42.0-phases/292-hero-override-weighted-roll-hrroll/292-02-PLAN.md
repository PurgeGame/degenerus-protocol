---
phase: 292-hero-override-weighted-roll-hrroll
plan: 02
type: execute
wave: 2
depends_on: [292-01]
files_modified:
  - contracts/modules/DegenerusGameJackpotModule.sol
  - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
autonomous: false
requirements: [HRROLL-01, HRROLL-02, HRROLL-03, HRROLL-04, HRROLL-06, HRROLL-07, HRROLL-08]
tags: [audit, hero-override, weighted-roll, contract-patch, user-approved, v42.0]
must_haves:
  truths:
    # HRROLL-01 — _rollHeroSymbol replaces _topHeroSymbol with two-pass weighted roll
    - "`_topHeroSymbol(uint32 day)` (`contracts/modules/DegenerusGameJackpotModule.sol:1625-1653`) DELETED entirely (function body + its NatSpec block at L1623-1624 removed); no deprecated stub, no `// @dev removed` marker per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md` (per HRROLL-01)"
    - "`_rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)` ADDED at the slot vacated by `_topHeroSymbol`. Two-pass implementation: pass 1 SLOADs the 4 `dailyHeroWagers[day][q]` packed slots once, extracts 32 × `uint32` amounts via the v41 bit-extract idiom (`uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF)`), accumulates `total` as `uint64`, tracks `maxAmount` + `leaderIdx` via strict `>` (first-seen on ties matches v41 scan order); early-bail `(false, 0, 0)` when `total == 0`; pass 2 walks the flat `uint32[32]` memory cache (per D-42N-CACHE-01 LOCKED in `292-01-MEASUREMENT.md` §3.b) with cumulative cursor against `pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)`; cursor direction is flat idx ascending = q ascending → s ascending (matches v41 scan order) (per HRROLL-01 + D-42N-DETERMINISM-01)"
    # HRROLL-02 — ×1.5 leader-weight bonus per D-42N-LEADER-BONUS-01
    - "Leader bonus computed as `leaderBonus = maxAmount / 2` (Solidity integer division — `uint64`-typed); `effectiveTotal = total + leaderBonus` (uint64; max ≈ 2.06e11 << 2^64; no overflow, no modulo bias); pass 2 cumulative add at the leader's idx applies the bonus: `cumulative += weight; if (idx == leaderIdx) cumulative += leaderBonus; if (cumulative > pick) return (true, uint8(idx >> 3), uint8(idx & 7));`. Tie-break on leader: scan-order-first via the strict-`>` pass-1 update — preserves v41 `_topHeroSymbol` first-seen tie-break behavior (per HRROLL-02 + D-42N-LEADER-BONUS-01)"
    # HRROLL-03 — no min-wager floor
    - "Every `(q, s)` slot with `amount > 0` participates in the weighted roll; no eligibility floor; smallest organic bettor wins with probability `amount / effectiveTotal` (or `(amount + leaderBonus) / effectiveTotal` if leader). Single-bettor edge case: when only one `(q, s)` slot has any wagers, the roll trivially returns that slot's `(q, s)` (same outcome as v41 `_topHeroSymbol` deterministic pick) — confirmed at HRROLL-03 (per D-42N-FLOOR-01 — no floor predicate added to the source)"
    # HRROLL-04 — _applyHeroOverride signature gains second entropy parameter; callsite at L1941 updated
    - "`_applyHeroOverride` (`contracts/modules/DegenerusGameJackpotModule.sol:1594-1621`) signature GAINS a third parameter `uint256 heroEntropy` after the existing `uint256 randomWord` parameter; body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)` instead of `_topHeroSymbol(dailyIdx)`; the existing color-path logic at L1605-L1614 reading bits of `randomWord` (i.e. `r` in `_rollWinningTraits` scope) UNCHANGED — color path stays on the existing entropy domain (per HRROLL-04 + D-42N-BONUS-ENTROPY-01)"
    - "`_applyHeroOverride` NatSpec at L1584-L1593 REWRITTEN to describe what IS post-patch: weighted random roll across the 32 `(quadrant, symbol)` slots of `dailyHeroWagers[dailyIdx]` with a ×1.5 leader-weight bonus and no min-wager floor; raw VRF entropy `heroEntropy` feeds `_rollHeroSymbol(dailyIdx, heroEntropy)` and lands the same `(q, s)` per jackpot day across both regular + bonus trait rolls per the D-42N-BONUS-ENTROPY-01 cross-bonus invariance; the existing color-path bit-source description for `randomWord` preserved; `dailyIdx` continues as the frozen day-key carrier per v41 Phase 288 D-288-FIX-SHAPE-01 (still set only at `_unlockRng`). ZERO comparative/historical language: no `previously`, no `pre-v41`, no `pre-cleanup`, no `v41 form`, no `(CALL 1 / CALL 2) determinism` wording — that historical narrative lives ONLY in `292-01-DESIGN-INTENT-TRACE.md` + the commit message body per `feedback_no_history_in_comments.md`"
    - "`_rollWinningTraits` (`contracts/modules/DegenerusGameJackpotModule.sol:1928-1943`) callsite at L1941 updated to read `_applyHeroOverride(traits, r, randWord);` (3 args; the third arg is the RAW `randWord` per D-42N-BONUS-ENTROPY-01 — NOT the post-bonus-tag `r`). Note: `_rollWinningTraits` has 12 upstream callers (L285, 354, 520, 531, 538, 609, 610, 689, 1180, 1734, 1754, 1756) — all UNCHANGED because `randWord` is already the first parameter of `_rollWinningTraits` at every caller per the context-gathering scout verification"
    # HRROLL-05 — RNG commitment-window invariant preserved (verified at Plan 01 trace §(iv); structurally preserved here)
    - "RNG commitment-window invariant preserved structurally: `dailyHeroWagers[dailyIdx][q]` writes happen at `placeDegeneretteBet` (`contracts/modules/DegenerusGameDegeneretteModule.sol:484-501`) — UNCHANGED by this plan; `dailyIdx` writers UNCHANGED by this plan (only `_unlockRng` mutates `dailyIdx` per v41 Phase 288 D-288-FIX-SHAPE-01 carry); `randWord` continues to arrive via VRF callback at jackpot resolution — strictly after wager-time commitment; wager amounts for day D LOCKED before day D+1's VRF request fires (per HRROLL-05 + `292-01-DESIGN-INTENT-TRACE.md` §(iv) backward-trace)"
    # HRROLL-06 — storage byte-identity (zero new slots; zero new SSTORE/SLOAD)
    - "Storage byte-identical to v41 closure HEAD `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` on `dailyHeroWagers[uint32 => uint256[4]]` mapping (at `contracts/storage/DegenerusGameStorage.sol:1475`) + `dailyIdx`; zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in HRROLL scope (`_rollHeroSymbol` reads the SAME 4 `dailyHeroWagers[day][q]` slots that v41 `_topHeroSymbol` read; same access pattern); `forge inspect storageLayout` diff for `DegenerusGameStorage.sol` against v41 close baseline is EMPTY — recorded in `292-01-MEASUREMENT.md` §2 (per HRROLL-06)"
    # HRROLL-07 — public ABI byte-identity
    - "Public ABI byte-identical: `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + all view-function selectors UNCHANGED (none of the public/external mutation entry points + view function signatures touched by this plan); `_applyHeroOverride` is private (signature change is a private-function delta; does NOT count against the public ABI invariant); `_rollHeroSymbol` is new private; `_topHeroSymbol` removed private; zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks in HRROLL scope (per HRROLL-07)"
    # HRROLL-08 — gas regression bounded; theoretical attestation at Plan 01; empirical at Phase 293
    - "Worst-case gas regression bounded by the D-42N-CACHE-01 chosen-shape theoretical figure recorded in `292-01-MEASUREMENT.md` §3.b (~+475 gas vs v41 `_topHeroSymbol` baseline; well under the +10K ESCALATION-CHECKPOINT threshold; the D-42N-GAS-01 acceptance threshold +500 soft / +750 hard is recorded in §3.c); empirical confirmation deferred to Phase 293 TST-HRROLL-06 per the Phase 291 D-291-GAS-01 mirror pattern (theoretical-at-contract-phase + empirical-at-test-phase) (per HRROLL-08)"
    # NatSpec discipline — no history in source comments
    - "Zero comparative/historical language in any NatSpec edit touched by this plan (no `previously`, `pre-v41 form`, `formerly took`, `the bug at v40 was`, `pre-cleanup`, `(CALL 1 / CALL 2) determinism`) per `feedback_no_history_in_comments.md`; the v41-vs-v42 cleanup narrative lives ONLY in `292-01-DESIGN-INTENT-TRACE.md` + the commit message body + audit/FINDINGS-v42.0.md §3.A at Phase 297"
    # _topHeroSymbol deletion is unconditional
    - "`_topHeroSymbol` fully deleted; no stub, no `// @dev removed` marker, no preserved-alternative-mechanic flag-gated branch; `grep -n \"function _topHeroSymbol\" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches post-patch; `grep -n \"_topHeroSymbol\" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches post-patch (call sites also gone) (per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md`)"
    # D-42N-BONUS-ENTROPY-01 — raw randWord plumbed; NOT post-bonus-tag r
    - "The third argument passed to `_applyHeroOverride` at the L1941 callsite is the RAW `randWord` (the function's first parameter), NOT the post-bonus-tag local `r`; on jackpot days that trigger BOTH regular + bonus trait rolls, both invocations of `_applyHeroOverride` receive the same `randWord` value for `heroEntropy` and therefore the symbol-roll lands the SAME hero `(q, s)` per day; only colors differ (via the existing `r`-derived bits at L1605-L1614 which still depend on `isBonus` per L1937-L1939) (per D-42N-BONUS-ENTROPY-01)"
    # D-42N-COLOR-ENTROPY-01 — non-collision by construction
    - "Color path consumes bits `quadrant*3` of the existing `randomWord` (`r` in `_rollWinningTraits` scope: `r = isBonus ? keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)) : randWord`); symbol-roll path consumes `keccak256(abi.encode(heroEntropy, day))` over the raw `randWord` plus day — orthogonal entropy domains by construction (keccak output is independent of any specific bit-slice of its input); attestation recorded in `292-01-MEASUREMENT.md` §3.e (per D-42N-COLOR-ENTROPY-01)"
    # Single-site B2-degeneration
    - "HRROLL `_applyHeroOverride` has EXACTLY ONE caller in the codebase (`_rollWinningTraits` at L1941); the v40 B2-symmetric callsite pattern degenerates to a single-site verification for this phase — recorded in `292-01-MEASUREMENT.md` §6; post-patch `grep -nE \"_applyHeroOverride\" contracts/modules/DegenerusGameJackpotModule.sol` returns exactly 2 occurrences (1 declaration + 1 callsite)"
    # Wave-shape lock — ONE batched USER-APPROVED commit; no push
    - "All contract edits + the populated `292-01-MEASUREMENT.md` collapse into ONE USER-APPROVED batched commit at Task 5; the orchestrator + planner have NOT pre-approved the contract diff per `feedback_never_preapprove_contracts.md`; the user reviews the full diff per `feedback_manual_review_before_push.md` before approving; no `git push` after the commit (push requires a separate explicit user instruction)"
    # No KI promotion
    - "KNOWN-ISSUES.md is NOT modified by this plan; HRROLL is a player-experience rebalance under pre-launch posture (per D-42N-KI-01 default disposition at Phase 297 — KI promotion not applicable here; v42 HRROLL does not produce new findings — only mechanic rebalance)"
    # No tests touched
    - "Zero `test/` mutations in this plan; TST-HRROLL-01..06 fixture lives at Phase 293 per the roadmap split"
  artifacts:
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "HRROLL-01..04 contract patch — `_topHeroSymbol` deleted (function body L1625-1653 + NatSpec L1623-1624); `_rollHeroSymbol(uint32 day, uint256 entropy)` two-pass weighted roll with flat uint32[32] cache + ×1.5 leader-weight bonus + no floor added; `_applyHeroOverride` signature gains 3rd parameter `uint256 heroEntropy` and body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)`; `_applyHeroOverride` NatSpec rewritten (no history language); `_rollWinningTraits` L1941 callsite updated to pass raw `randWord` as 3rd arg"
      contains: "function _rollHeroSymbol"
    - path: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md"
      provides: "HRROLL-06 + HRROLL-07 attestations populated post-patch — §2 storage-slot grep result (EMPTY expected) for `DegenerusGameStorage.sol`; §4 selector attestations confirming public-ABI byte-identity for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions; §6 single-site callsite verification confirming `_applyHeroOverride` L1941 reads `(traits, r, randWord)` post-patch. §1 + §3 + §5 were FINAL at Plan 01 time."
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol (`_rollWinningTraits` L1941 callsite)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol (`_applyHeroOverride` L1594-1621 + new `_rollHeroSymbol` L1625-)"
      via: "raw `randWord` plumbed as 3rd arg per D-42N-BONUS-ENTROPY-01 — preserves per-jackpot-day hero-symbol lock-in across regular + bonus rolls"
      pattern: "_applyHeroOverride\\(traits, r, randWord\\)"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol (`_rollHeroSymbol` pass 2 cursor walk)"
      to: "keccak input set"
      via: "exact algorithm per D-42N-DETERMINISM-01: `pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)` — `abi.encode` (NOT `abi.encodePacked`)"
      pattern: "keccak256\\(abi\\.encode\\(entropy, day\\)\\)"
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (populated post-patch)"
      to: "Task 5 batched commit message body"
      via: "copy-forward of storage-slot grep result + selector attestations + single-site callsite verification + the §3 D-42N-CACHE-01 + D-42N-GAS-01 + D-42N-COLOR-ENTROPY-01 figures locked at Plan 01"
      pattern: "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4"
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md (Plan 01 output)"
      to: "Task 5 batched commit message body"
      via: "copy-forward of design-intent trace + the 7 Phase 292-scope decision anchors (D-42N-LEADER-BONUS-01 + D-42N-FLOOR-01 + D-42N-COLOR-ENTROPY-01 + D-42N-DETERMINISM-01 + D-42N-GAS-01 + D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01)"
      pattern: "D-42N-BONUS-ENTROPY-01"
---

<objective>
Land the HRROLL-01..04 contract patch on `contracts/modules/DegenerusGameJackpotModule.sol` as ONE USER-APPROVED batched commit covering: (1) HRROLL-01 — DELETE `_topHeroSymbol(uint32 day)` (L1625-1653) + its NatSpec (L1623-1624); ADD `_rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool, uint8, uint8)` with the two-pass weighted-roll algorithm + flat `uint32[32]` memory cache per D-42N-CACHE-01 LOCKED + leader-bonus arithmetic + cursor walk per D-42N-DETERMINISM-01; (2) HRROLL-02 — ×1.5 leader-weight bonus via `leaderBonus = maxAmount / 2` applied at the leader's idx in pass 2; first-seen tie-break preserved via strict `>` in pass 1; (3) HRROLL-03 — zero eligibility floor; every slot with `amount > 0` participates proportionally; (4) HRROLL-04 — `_applyHeroOverride` signature gains 3rd parameter `uint256 heroEntropy`; body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)` instead of `_topHeroSymbol(dailyIdx)`; color-path UNCHANGED; NatSpec rewritten per `feedback_no_history_in_comments.md` (no `(CALL 1 / CALL 2)` wording); `_rollWinningTraits` L1941 callsite updated to pass raw `randWord` as 3rd arg per D-42N-BONUS-ENTROPY-01; (5) HRROLL-06 — storage byte-identity attested via `forge inspect storageLayout` empty-diff (recorded in `292-01-MEASUREMENT.md` §2); (6) HRROLL-07 — public ABI byte-identity attested via selector recalc + zero-new-entry-points grep (recorded in `292-01-MEASUREMENT.md` §4); (7) single-site callsite verification (recorded in `292-01-MEASUREMENT.md` §6). Populate `292-01-MEASUREMENT.md` §2 + §4 + §6 post-patch (§1 + §3 + §5 were FINAL at Plan 01 time). Present the consolidated diff to the user; on approval, create ONE batched commit per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md`. No `git push`.

Purpose: Replace the deterministic single-leader winner-takes-all `_topHeroSymbol` selector with the weighted random roll specified by the locked anchors. Preserve all v41 storage + public ABI byte-identity invariants (HRROLL-06 + HRROLL-07). Inherit the v41 Phase 288 `dailyIdx` single-writer-day-key invariant unchanged (HRROLL-05). Implement the D-42N-DETERMINISM-01 exact algorithm + the D-42N-CACHE-01 chosen flat `uint32[32]` cache shape verbatim. Preserve per-jackpot-day hero-symbol lock-in across regular + bonus trait rolls via D-42N-BONUS-ENTROPY-01 (raw `randWord` plumbed; NOT post-bonus-tag `r`). Bound gas regression to the D-42N-GAS-01 acceptance threshold (~+475 gas; well under the +10K ESCALATION-CHECKPOINT threshold; soft +500 / hard +750). Empirical regression assertion lives at Phase 293 TST-HRROLL-06 per the D-291-GAS-01 mirror pattern.

Output:
- `contracts/modules/DegenerusGameJackpotModule.sol` patched (4 hunk groups: `_applyHeroOverride` NatSpec + signature + body update; `_topHeroSymbol` deletion; `_rollHeroSymbol` addition; `_rollWinningTraits` L1941 callsite update)
- `292-01-MEASUREMENT.md` populated post-patch (§2 storage-slot grep EMPTY + §4 selectors UNCHANGED + §6 single-site callsite verification; §1 + §3 + §5 unchanged from Plan 01 final state)
- ONE USER-APPROVED batched commit covering the 2 paths above

This plan makes contract edits and triggers the USER-APPROVAL gate at Task 5 per `feedback_no_contract_commits.md`. The orchestrator + planner have NOT pre-approved the diff per `feedback_never_preapprove_contracts.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-CONTEXT.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md

@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
<!-- Load-bearing identifiers / signatures / NatSpec / line ranges. Extracted from live v42.0 HEAD = audit baseline v41.0 closure HEAD `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. -->
<!-- Executor uses these directly; no codebase exploration needed beyond the @-references above. -->

Current `_applyHeroOverride` NatSpec + signature + body (`contracts/modules/DegenerusGameJackpotModule.sol:1584-1621` — REWRITTEN by this plan; signature gains `uint256 heroEntropy` parameter; NatSpec rewritten to describe what IS post-patch with NO `(CALL 1 / CALL 2) determinism` wording per `feedback_no_history_in_comments.md`):
```
/// @dev If a top hero symbol exists for the prior day's settled wager pool,
///      that symbol auto-wins its own quadrant (with random color), replacing
///      only that quadrant. Applied to all jackpot paths (purchase phase +
///      jackpot phase). Reads `dailyHeroWagers[dailyIdx]`: `dailyIdx` is
///      written only at `_unlockRng` (AdvanceModule), so during jackpot
///      processing it is frozen at the previous day's index. Both CALL 1 and
///      CALL 2 of the daily ETH split therefore read the same slot regardless
///      of any physical-day boundary crossed between calls. Bets placed on
///      day D write to `dailyHeroWagers[D]`; day D+1's jackpot reads slot[D]
///      via `dailyIdx == D` (set by day D's `_unlockRng`).
function _applyHeroOverride(
    uint8[4] memory w,
    uint256 randomWord
) private view {
    (
        bool hasHeroWinner,
        uint8 heroQuadrant,
        uint8 heroSymbol
    ) = _topHeroSymbol(dailyIdx);
    if (!hasHeroWinner) return;

    uint8 heroColor;
    if (heroQuadrant == 0) {
        heroColor = uint8(randomWord & 7);
    } else if (heroQuadrant == 1) {
        heroColor = uint8((randomWord >> 3) & 7);
    } else if (heroQuadrant == 2) {
        heroColor = uint8((randomWord >> 6) & 7);
    } else {
        heroColor = uint8((randomWord >> 9) & 7);
    }

    w[heroQuadrant] = uint8(
        (uint256(heroQuadrant) << 6) |
            (uint256(heroColor) << 3) |
            uint256(heroSymbol)
    );
}
```

Target post-patch `_applyHeroOverride` NatSpec + signature + body (3-param signature; consumer-call updated to `_rollHeroSymbol(dailyIdx, heroEntropy)`; NatSpec describes what IS — weighted roll across 32 slots; no `(CALL 1 / CALL 2)` wording; the `dailyIdx`-frozen-across-rng-lock invariant is restated as a current-state fact, not a history note):
```
/// @dev Replaces the winning quadrant's trait with a hero-symbol override sampled by
///      `_rollHeroSymbol` from the prior day's settled wager pool. Applied to all jackpot
///      paths (purchase phase + jackpot phase). Reads `dailyHeroWagers[dailyIdx]`:
///      `dailyIdx` is written only at `_unlockRng` (AdvanceModule), so during jackpot
///      processing it is frozen at the previous day's index — every consumer in a single
///      jackpot resolution therefore reads the same wager pool. Bets placed on day D
///      write to `dailyHeroWagers[D]`; day D+1's jackpot reads slot[D] via
///      `dailyIdx == D` (set by day D's `_unlockRng`).
///
///      `heroEntropy` is the raw VRF entropy word for the day; the same value flows into
///      every `_applyHeroOverride` invocation within a jackpot resolution, so on days
///      that produce both regular and bonus trait rolls both rolls land on the same hero
///      `(quadrant, symbol)` pair (only the per-quadrant colors differ, sampled from
///      `randomWord`). Symbol entropy and color entropy live in orthogonal domains:
///      colors read bit-slices of `randomWord`; the symbol roll consumes
///      `keccak256(abi.encode(heroEntropy, day))`.
function _applyHeroOverride(
    uint8[4] memory w,
    uint256 randomWord,
    uint256 heroEntropy
) private view {
    (
        bool hasHeroWinner,
        uint8 heroQuadrant,
        uint8 heroSymbol
    ) = _rollHeroSymbol(dailyIdx, heroEntropy);
    if (!hasHeroWinner) return;

    uint8 heroColor;
    if (heroQuadrant == 0) {
        heroColor = uint8(randomWord & 7);
    } else if (heroQuadrant == 1) {
        heroColor = uint8((randomWord >> 3) & 7);
    } else if (heroQuadrant == 2) {
        heroColor = uint8((randomWord >> 6) & 7);
    } else {
        heroColor = uint8((randomWord >> 9) & 7);
    }

    w[heroQuadrant] = uint8(
        (uint256(heroQuadrant) << 6) |
            (uint256(heroColor) << 3) |
            uint256(heroSymbol)
    );
}
```

Current `_topHeroSymbol` (`contracts/modules/DegenerusGameJackpotModule.sol:1623-1653` — DELETED ENTIRELY by this plan; both the function body L1625-1653 and the NatSpec block L1623-1624 go away):
```
/// @dev Returns the top hero symbol for a day across all quadrants.
///      Tie-breaker is deterministic first-seen order (q asc, symbol asc).
function _topHeroSymbol(
    uint32 day
)
    private
    view
    returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)
{
    uint32 topAmount;
    for (uint8 q; q < 4; ) {
        uint256 packed = dailyHeroWagers[day][q];
        for (uint8 s; s < 8; ) {
            uint32 amount = uint32(
                (packed >> (uint256(s) * 32)) & 0xFFFFFFFF
            );
            if (amount > topAmount) {
                topAmount = amount;
                winQuadrant = q;
                winSymbol = s;
            }
            unchecked {
                ++s;
            }
        }
        unchecked {
            ++q;
        }
    }
    hasWinner = topAmount != 0;
}
```

Target post-patch `_rollHeroSymbol` (ADDED at the slot vacated by `_topHeroSymbol`; flat `uint32[32]` cache shape per D-42N-CACHE-01 LOCKED; exact algorithm per D-42N-DETERMINISM-01):
```
/// @dev Samples the day's hero `(quadrant, symbol)` via a weighted random roll across
///      the 32 packed slots of `dailyHeroWagers[day]`. Pass 1 SLOADs the 4 packed
///      quadrants once, decodes 32 uint32 amounts, accumulates the total, and tracks
///      the largest-amount slot (first-seen on ties to match the legacy scan order).
///      Pass 2 walks the cached weights with a cumulative cursor against
///      `pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)`
///      and applies a `leaderBonus = maxAmount / 2` add at the largest-amount slot —
///      effective ×1.5 weight on the leader, no min-wager floor on any other slot.
///      Returns `(false, 0, 0)` when no slot has any wagers.
function _rollHeroSymbol(
    uint32 day,
    uint256 entropy
)
    private
    view
    returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)
{
    uint32[32] memory weights;
    uint64 total;
    uint32 maxAmount;
    uint8 leaderIdx;

    for (uint8 q; q < 4; ) {
        uint256 packed = dailyHeroWagers[day][q];
        for (uint8 s; s < 8; ) {
            uint32 amount = uint32(
                (packed >> (uint256(s) * 32)) & 0xFFFFFFFF
            );
            uint8 idx;
            unchecked {
                idx = (q << 3) | s;
            }
            weights[idx] = amount;
            total += uint64(amount);
            if (amount > maxAmount) {
                maxAmount = amount;
                leaderIdx = idx;
            }
            unchecked {
                ++s;
            }
        }
        unchecked {
            ++q;
        }
    }

    if (total == 0) {
        return (false, 0, 0);
    }

    uint64 leaderBonus = uint64(maxAmount) / 2;
    uint64 effectiveTotal = total + leaderBonus;
    uint64 pick = uint64(
        uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal
    );

    uint64 cumulative;
    for (uint8 idx; idx < 32; ) {
        cumulative += uint64(weights[idx]);
        if (idx == leaderIdx) {
            cumulative += leaderBonus;
        }
        if (cumulative > pick) {
            return (true, uint8(idx >> 3), uint8(idx & 7));
        }
        unchecked {
            ++idx;
        }
    }

    // Unreachable: effectiveTotal == sum(weights) + leaderBonus, so the cumulative
    // sweep must overtake `pick` (which is strictly less than effectiveTotal) before
    // the loop exits. Returning (false, 0, 0) here would be defensive dead code per
    // `feedback_no_dead_guards.md`; instead, fall through to a revert that signals an
    // unreachable invariant break if it ever fires.
    revert("HRROLL: cursor underflow");
}
```

Current `_rollWinningTraits` (`contracts/modules/DegenerusGameJackpotModule.sol:1928-1943` — L1941 callsite UPDATED to pass raw `randWord` as 3rd arg per D-42N-BONUS-ENTROPY-01; signature + body otherwise UNCHANGED; 12 upstream callers of `_rollWinningTraits` UNCHANGED because `randWord` is already in scope at every caller per the CONTEXT.md scout verification):
```
/// @dev Roll winning traits with hero symbol override.
///      All paths use fully random traits (6 bits per quadrant).
///      Hero override replaces the winning quadrant's trait if a top hero symbol exists.
/// @param randWord VRF entropy.
/// @param isBonus When true, applies keccak256 domain separation for independent bonus traits.
function _rollWinningTraits(
    uint256 randWord,
    bool isBonus
) private view returns (uint32 packed) {
    uint256 r = isBonus
        ? uint256(keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)))
        : randWord;
    uint8[4] memory traits = JackpotBucketLib.getRandomTraits(r);
    _applyHeroOverride(traits, r);
    packed = JackpotBucketLib.packWinningTraits(traits);
}
```

Target post-patch `_rollWinningTraits` (only L1941 line changes — gains 3rd arg `randWord`):
```
function _rollWinningTraits(
    uint256 randWord,
    bool isBonus
) private view returns (uint32 packed) {
    uint256 r = isBonus
        ? uint256(keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)))
        : randWord;
    uint8[4] memory traits = JackpotBucketLib.getRandomTraits(r);
    _applyHeroOverride(traits, r, randWord);
    packed = JackpotBucketLib.packWinningTraits(traits);
}
```

Note on the trailing comment in `_rollWinningTraits`: the NatSpec line "Hero override replaces the winning quadrant's trait if a top hero symbol exists." stays acceptable as-is (it describes what IS — the hero override still replaces the winning quadrant's trait if any hero symbol is selected; the word "top" is the existing phrasing and is not strictly historical, though it could be tightened). If the executor decides the phrasing reads stale post-patch, an acceptable tightening is "Hero override replaces the winning quadrant's trait when the daily wager pool yields a hero symbol." NO history language in either form. The NatSpec @param block needs no update because the parameters are unchanged here.

Audit baseline reference: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD).

Single callsite of `_applyHeroOverride` in the codebase (verified at Plan 01 authorship via `grep -nE "_applyHeroOverride" contracts/modules/DegenerusGameJackpotModule.sol`):
- L1602 — inside `_applyHeroOverride` body (the function calls the now-removed `_topHeroSymbol`)
- L1594 — function declaration
- L1941 — call site inside `_rollWinningTraits`

Post-patch the grep should return EXACTLY 2 occurrences: 1 declaration (L1594 area) + 1 callsite (L1941 area; line number may shift due to function-body length changes between v41 `_topHeroSymbol` and v42 `_rollHeroSymbol`). The L1602 occurrence (`_topHeroSymbol(dailyIdx)` call) is REPLACED by `_rollHeroSymbol(dailyIdx, heroEntropy)`, eliminating the `_topHeroSymbol` reference entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply HRROLL-01..04 contract patch to `DegenerusGameJackpotModule.sol` (no commit yet)</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol
  </files>
  <read_first>
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md (Plan 01 output — MUST exist; this read is the pre-patch-gate verification per `feedback_design_intent_before_deletion.md`; confirms the 7 decision anchors + HRROLL-05 backward-trace are recorded)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (Plan 01 output — MUST exist with the 6 attestation sections; §3 ESCALATION-CHECKPOINT MUST be NOT TRIGGERED before this task starts; §3.b LOCKED cache shape is the implementation reference for the `_rollHeroSymbol` body)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-CONTEXT.md (the locked D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01 + pass-2-cursor-walk + weight-arithmetic-widening notes)
    - contracts/modules/DegenerusGameJackpotModule.sol (full file; at minimum L1580-L1700 covering `_applyHeroOverride` + `_topHeroSymbol` + the surrounding helper-band; also L1925-L1945 covering `_rollWinningTraits` for the callsite update; ALSO grep for any OTHER caller of `_applyHeroOverride` to confirm L1941 is the only callsite — grep result MUST show exactly 2 occurrences pre-patch: 1 declaration + 1 callsite; ALSO grep for any caller of `_topHeroSymbol` outside `_applyHeroOverride` body — grep result MUST show ZERO external callers; `_topHeroSymbol` is only called from inside `_applyHeroOverride` at L1602)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L482-L501 (`placeDegeneretteBet` — UNCHANGED by this plan but read for the HRROLL-05 commitment-window mental model)
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (`dailyHeroWagers` declaration — UNCHANGED by this plan)
    - .planning/REQUIREMENTS.md lines 58-69 (HRROLL-01..10 verbatim spec; locked requirement set, do NOT expand scope) + lines 15-18 (HRROLL Out-of-Scope register)
    - .planning/ROADMAP.md (Phase 292 success criteria 1-5)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
  </read_first>
  <action>
    Pre-patch gate check FIRST: confirm `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md` AND `292-01-MEASUREMENT.md` both exist and contain the anchors `D-42N-LEADER-BONUS-01` + `D-42N-FLOOR-01` + `D-42N-COLOR-ENTROPY-01` + `D-42N-DETERMINISM-01` + `D-42N-GAS-01` + `D-42N-BONUS-ENTROPY-01` + `D-42N-CACHE-01` + `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. ADDITIONALLY confirm `292-01-MEASUREMENT.md` §3.d ESCALATION-CHECKPOINT is NOT TRIGGERED (grep for the literal "NOT TRIGGERED" string near the §3.d header, OR confirm that no `🚨 ESCALATION-CHECKPOINT — STOP` flag is present in §3). If any file is missing, any anchor is missing, OR the ESCALATION-CHECKPOINT is TRIGGERED, STOP — the gate is not satisfied; escalate to user (the orchestrator should have run Plan 01 first OR the user should have resolved the escalation).

    Apply the following edits inside `contracts/modules/DegenerusGameJackpotModule.sol`:

    Edit A — `_applyHeroOverride` NatSpec rewrite (L1584-L1593): replace with the post-patch target NatSpec shown in `<interfaces>` above. Drop the `(CALL 1 / CALL 2) of the daily ETH split` wording entirely; replace with a current-state description of the `dailyIdx`-frozen-across-rng-lock invariant ("during jackpot processing it is frozen at the previous day's index — every consumer in a single jackpot resolution therefore reads the same wager pool"). Add a paragraph explaining the `heroEntropy` parameter — raw VRF flows into every `_applyHeroOverride` invocation within a single resolution; bonus + regular rolls land same `(q, s)`; only colors differ; symbol-entropy and color-entropy live in orthogonal domains (per D-42N-COLOR-ENTROPY-01). NO comparative/historical language ("previously", "v41 form", "pre-cleanup", "(CALL 1 / CALL 2) determinism"). Per `feedback_no_history_in_comments.md`. (HRROLL-04 + D-42N-BONUS-ENTROPY-01 + D-42N-COLOR-ENTROPY-01)

    Edit B — `_applyHeroOverride` signature gains 3rd parameter (L1594-L1597): insert `uint256 heroEntropy` as the third positional parameter after the existing `uint256 randomWord`. Post-edit signature: `function _applyHeroOverride(uint8[4] memory w, uint256 randomWord, uint256 heroEntropy) private view`. (HRROLL-04 + D-42N-BONUS-ENTROPY-01)

    Edit C — `_applyHeroOverride` body update (L1602): change `_topHeroSymbol(dailyIdx)` to `_rollHeroSymbol(dailyIdx, heroEntropy)`. The rest of the function body (color sampling at L1605-L1614 + trait packing at L1616-L1620) UNCHANGED — the color path continues to read bit-slices of the EXISTING `randomWord` parameter. (HRROLL-04 + D-42N-BONUS-ENTROPY-01)

    Edit D — `_topHeroSymbol` deletion (L1623-L1653): DELETE entirely — both the function body (L1625-1653) AND the NatSpec block at L1623-1624. No stub. No `// @dev removed` marker. No deprecated alternative. Per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md`. Post-edit `grep -n "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches. (HRROLL-01 — deletion arm)

    Edit E — `_rollHeroSymbol` addition (at the slot vacated by `_topHeroSymbol`): INSERT the function as shown verbatim in `<interfaces>` "Target post-patch `_rollHeroSymbol`" above. Reuse the v41 `_topHeroSymbol` bit-extract idiom (`uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF)`) inside pass 1; reuse the `unchecked { ++s; ++q; }` counter-increment idiom. Cache shape is flat `uint32[32]` per D-42N-CACHE-01 LOCKED in `292-01-MEASUREMENT.md` §3.b. Compute `idx = (q << 3) | s` inside an `unchecked` block (q is uint8 ≤ 3, s is uint8 ≤ 7, so `(q << 3) | s` fits in uint8 ≤ 31 with no overflow concern). `total` widened to `uint64` to prevent overflow at the 32-slot accumulation worst case. `leaderBonus = uint64(maxAmount) / 2` widened to `uint64` to safely carry up to `0.5 × uint32.max ≈ 2.15e9` (well within uint64). `pick` derived via `uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)` — `abi.encode` (NOT `abi.encodePacked`) per D-42N-DETERMINISM-01 to avoid type-coercion ambiguity. Pass-2 cursor walk: `cumulative += uint64(weights[idx]); if (idx == leaderIdx) cumulative += leaderBonus; if (cumulative > pick) return (true, uint8(idx >> 3), uint8(idx & 7));`. Unreachable-cursor revert at the end: `revert("HRROLL: cursor underflow");` — per the inline comment in the target body, this signals an invariant break (effectiveTotal is the sum of all weights + leaderBonus, so the cumulative sweep MUST overtake `pick` before the loop exits; reaching this line indicates a corrupted-storage condition); a defensive `return (false, 0, 0)` would be dead code per `feedback_no_dead_guards.md`. (HRROLL-01 + HRROLL-02 + HRROLL-03 + D-42N-DETERMINISM-01 + D-42N-CACHE-01 + D-42N-LEADER-BONUS-01 + D-42N-FLOOR-01)

    Edit F — `_rollWinningTraits` L1941 callsite update: change `_applyHeroOverride(traits, r);` to `_applyHeroOverride(traits, r, randWord);` (3 args; the third arg is the RAW `randWord` — the function's first parameter — NOT the post-bonus-tag `r`). The `r` variable continues to be used as the color-entropy argument (existing behavior). Per D-42N-BONUS-ENTROPY-01: passing the raw `randWord` (which is identical across bonus and regular calls within the same jackpot resolution) ensures the symbol-roll lands the SAME `(q, s)` per day. (HRROLL-04 + D-42N-BONUS-ENTROPY-01)

    Optional Edit G — `_rollWinningTraits` NatSpec tightening (L1928-L1932): the existing line "Hero override replaces the winning quadrant's trait if a top hero symbol exists." is acceptable as-is post-patch (the word "top" is approximate but not strictly historical). If the executor judges the phrasing reads stale, tighten to "Hero override replaces the winning quadrant's trait when the daily wager pool yields a hero symbol." NO history language. The NatSpec @param block needs no update because the function's parameters are unchanged. This edit is OPTIONAL — the executor decides at edit time.

    Constraints — verify each before declaring the edit complete:
    - Storage layout unchanged on `dailyHeroWagers`, `dailyIdx`, all other storage vars in `DegenerusGameStorage.sol`. Zero new storage slots; zero new mappings. ZERO edits to `contracts/storage/DegenerusGameStorage.sol`. (HRROLL-06)
    - Public ABI on `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + all view functions byte-identical (selectors preserved; no entry-point signatures touched by this plan). Zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks. (HRROLL-07)
    - `_topHeroSymbol` fully deleted (`grep -n "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO). Post-edit grep for `function _topHeroSymbol` AND any callsite — both ZERO. (HRROLL-01)
    - `_rollHeroSymbol` added exactly once (`grep -nE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns exactly 1 match; `grep -nE "_rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns exactly 2 matches: 1 declaration + 1 callsite inside `_applyHeroOverride`).
    - `_applyHeroOverride` signature has 3 positional parameters (`uint8[4] memory w, uint256 randomWord, uint256 heroEntropy`); grep for the function-declaration line returns the new 3-param form.
    - `_applyHeroOverride` callsite count UNCHANGED: `grep -cE "_applyHeroOverride\(" contracts/modules/DegenerusGameJackpotModule.sol` returns EXACTLY 2 (1 declaration + 1 callsite at the L1941 area).
    - L1941 callsite reads `_applyHeroOverride(traits, r, randWord)` (3 args; raw `randWord` is the 3rd) — grep for `_applyHeroOverride\(traits, r, randWord\)` returns 1 match.
    - Zero comparative/historical language in any NatSpec edit (no `previously`, `pre-v41`, `pre-cleanup`, `formerly`, `(CALL 1 / CALL 2) determinism`). The `feedback_no_history_in_comments.md` grep filter strips `//` + `*` comment leaders; remainder MUST be ZERO of those tokens.
    - Cache shape inside `_rollHeroSymbol` body is the LOCKED flat `uint32[32]` per `292-01-MEASUREMENT.md` §3.b. NOT `uint64[32]`. NOT packed `uint256[4]`. NOT re-SLOAD without cache. (D-42N-CACHE-01)
    - Pass-2 cursor walk direction is flat idx ascending (q ascending → s ascending); leader-bonus added at `idx == leaderIdx`; first-seen tie-break via strict `>` in pass-1. (D-42N-DETERMINISM-01)
    - Keccak input form is `abi.encode(entropy, day)` (NOT `abi.encodePacked`). Per D-42N-DETERMINISM-01.
    - `contracts/modules/DegenerusGameDegeneretteModule.sol` UNCHANGED (no edits — `placeDegeneretteBet` wager-write site is the HRROLL-05 backward-trace destination and stays load-bearing). KNOWN-ISSUES.md UNCHANGED. test/ UNCHANGED.
    - No dead guards / no future-proofing added beyond the unreachable-cursor `revert` (which is intentional and documented inline as an invariant signal per `feedback_no_dead_guards.md` — a defensive `return (false, 0, 0)` at that point would be dead-code-disguised-as-safety).
    - RNG backward-trace check (`feedback_rng_backward_trace.md`): `_rollHeroSymbol` consumes `entropy` (the new `heroEntropy` parameter, raw `randWord`) + `dailyHeroWagers[day][q]` (4 SLOADs). Player CANNOT influence `randWord` (Chainlink VRF output). Player CANNOT influence wager amounts post-commit (HRROLL-05 commitment-window backward-trace verified in `292-01-DESIGN-INTENT-TRACE.md` §(iv); wager amounts locked at `placeDegeneretteBet` time, strictly before VRF request).
    - RNG commitment-window check (`feedback_rng_commitment_window.md`): wager amounts for day D are LOCKED before day D+1's VRF request fires; symbol-roll entropy is unknowable at wager time. SAFE.

    Do NOT compile, build, or run forge in this task — Task 2 + Task 3 + Task 4 handle measurement. The edits land as a clean working-tree diff ready for measurement.

    DO NOT commit yet. The user reviews the diff at Task 5 before the single batched commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "NOT TRIGGERED" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && ! grep -nE "function _topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol && ! grep -nE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol && grep -cE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol | grep -qE "^1$" && grep -cE "_rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol | grep -qE "^2$" && grep -qE "function _applyHeroOverride\(" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "uint256 heroEntropy" contracts/modules/DegenerusGameJackpotModule.sol && grep -cE "_applyHeroOverride\(" contracts/modules/DegenerusGameJackpotModule.sol | grep -qE "^2$" && grep -qE "_applyHeroOverride\(traits, r, randWord\)" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "keccak256\(abi\.encode\(entropy, day\)\)" contracts/modules/DegenerusGameJackpotModule.sol && grep -qE "uint32\[32\] memory weights" contracts/modules/DegenerusGameJackpotModule.sol && [ -z "$(git diff --name-only HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol test/ KNOWN-ISSUES.md 2>/dev/null)" ] && [ $(grep -E "previously|pre-v41|pre-cleanup|formerly|the bug at" contracts/modules/DegenerusGameJackpotModule.sol | grep -vE "^[[:space:]]*//|^[[:space:]]*\*" | wc -l) -eq 0 ] && ! grep -qE "CALL 1 and.*CALL 2" contracts/modules/DegenerusGameJackpotModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - Pre-patch gate: Both `292-01-DESIGN-INTENT-TRACE.md` + `292-01-MEASUREMENT.md` exist with all 7 anchors recorded; `292-01-MEASUREMENT.md` §3.d ESCALATION-CHECKPOINT is `NOT TRIGGERED`.
    - `_topHeroSymbol` fully deleted: `grep -n "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches (function declaration + every callsite gone).
    - `_rollHeroSymbol` added exactly once: `grep -cE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns `1`; full grep `grep -cE "_rollHeroSymbol"` returns `2` (1 declaration + 1 callsite inside `_applyHeroOverride`).
    - `_applyHeroOverride` signature has 3 positional parameters; grep for `uint256 heroEntropy` matches at least once (the signature) — the executor SHOULD verify it matches in the declaration only (not in some other accidentally-introduced site).
    - `_applyHeroOverride` callsite count is EXACTLY 2 (`grep -cE "_applyHeroOverride\("` returns `2` — 1 declaration + 1 callsite at the `_rollWinningTraits` body).
    - L1941 callsite (post-patch line number may shift) reads `_applyHeroOverride(traits, r, randWord)` literally.
    - `_rollHeroSymbol` body uses the LOCKED flat `uint32[32]` cache shape (grep for `uint32[32] memory weights` matches; NO `uint64[32]` declaration; NO `uint256[4]` cache pattern).
    - Keccak input form is `abi.encode(entropy, day)` (NOT `abi.encodePacked(entropy, day)`); grep for `keccak256\(abi\.encode\(entropy, day\)\)` matches.
    - Zero comparative/historical language in any NatSpec edit (grep filter strips comment leaders; remainder MUST be ZERO for `previously`, `pre-v41`, `pre-cleanup`, `formerly`, `the bug at`).
    - `(CALL 1 and ... CALL 2)` wording is GONE from the `_applyHeroOverride` NatSpec (grep for `CALL 1 and.*CALL 2` returns ZERO matches).
    - `contracts/storage/DegenerusGameStorage.sol` UNCHANGED (`git diff` shows no changes).
    - `contracts/modules/DegenerusGameDegeneretteModule.sol` UNCHANGED (`git diff` shows no changes — `placeDegeneretteBet` wager-write site is the HRROLL-05 backward-trace destination and stays load-bearing).
    - `KNOWN-ISSUES.md` UNCHANGED.
    - `test/` UNCHANGED.
    - No `forge build` or `cast keccak` run in this task — Tasks 2-4 handle that.
    - No `git commit` issued yet.
  </acceptance_criteria>
  <done>
    HRROLL-01..04 contract patch is fully applied to `DegenerusGameJackpotModule.sol` as a clean working-tree diff. `_topHeroSymbol` is gone; `_rollHeroSymbol` is in; `_applyHeroOverride` gains the 3rd parameter; `_rollWinningTraits` L1941 callsite passes raw `randWord`. Storage + ABI + Degenerette module + tests + KNOWN-ISSUES UNCHANGED. Diff is ready for Task 2 (storage-layout grep) + Task 3 (selector recalc) + Task 4 (single-site callsite verification) + Task 5 (USER-APPROVAL gate).
  </done>
</task>

<task type="auto">
  <name>Task 2: Storage-slot grep proof — populate `292-01-MEASUREMENT.md` §2 (HRROLL-06)</name>
  <files>
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state)
    - contracts/storage/DegenerusGameStorage.sol (UNCHANGED by this plan)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (scaffold from Plan 01; §1 + §3 + §5 already final; §2 + §4 + §6 carry `<FILL-IN-Plan-02>` placeholders)
    - foundry.toml OR hardhat.config.{js,ts} (root of repo — confirm which toolchain to use; mirror Phase 290 Task 2 toolchain discovery pattern)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §2 (Phase 290's storage-grep proof technique — `forge inspect storageLayout` + cross-tree diff against the v41 worktree)
  </read_first>
  <action>
    Measure storage-layout byte-identity against `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` baseline.

    Toolchain discovery: check repo root for `foundry.toml` (forge toolchain) OR `hardhat.config.{js,ts}` (hardhat toolchain). Pick forge if both present (faster + clearer storage layout output) — matches Phase 290 Task 2 precedent.

    Baseline checkout strategy: use `git worktree add /tmp/v41-baseline 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` to materialize the v41 close HEAD in a sibling directory without disturbing the working tree. If the worktree already exists from a previous Plan 02 run (e.g. Phase 290's), reuse it. If creation fails for another reason, use `git stash --include-untracked` + `git checkout 315978a0...` + measure + `git checkout -` + `git stash pop` as a fallback. After measurement, leave the worktree in place (the next sibling-phase task may reuse it) OR clean up via `git worktree remove /tmp/v41-baseline --force` — executor's discretion.

    §2 — Storage-Slot Grep Proof: Run `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout` against the post-patch tree → write to `/tmp/v42-storage-292.json`. Run same command against the v41 baseline worktree → write to `/tmp/v41-storage-292.json`. Compute `diff /tmp/v41-storage-292.json /tmp/v42-storage-292.json`. Expected output: EMPTY (HRROLL touches ZERO storage — `dailyHeroWagers` mapping at `DegenerusGameStorage.sol:1475` UNCHANGED + `dailyIdx` UNCHANGED + every other storage var UNCHANGED). ALSO run the same grep against `contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule` if applicable (note: storage is typically declared in the storage module only; the module may not have its own layout output, in which case skip).

    Note: this plan does NOT modify `contracts/modules/DegenerusGameMintModule.sol` (Phase 290's audit-subject) — so a storage-layout diff against that file would either be EMPTY (no Phase 292 contribution) or would surface Phase 290's diff if measured against pre-Phase-290 baseline. To stay clean, measure ONLY against `contracts/storage/DegenerusGameStorage.sol` for this phase — the Mint module storage was attested at Phase 290 and remains UNCHANGED at Phase 292.

    If hardhat is the toolchain instead: use `npx hardhat storage-layout` (requires `hardhat-storage-layout` plugin); if the plugin is not installed, fall back to `forge inspect` if forge is also available; if NEITHER is available, record the failure mode + escalate to user before Task 5.

    Populate §2 of `292-01-MEASUREMENT.md` with: `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = [EMPTY | <non-empty-diff-text>]`. If non-empty → record the exact diff text + flag `🚨 STORAGE LAYOUT REGRESSION — STOP — escalate to user before Task 5`. ALSO populate the §2 "Locks under HRROLL-06" block: confirm `dailyHeroWagers[uint32 => uint256[4]]` remains at the same slot offset / type / label; confirm `dailyIdx` remains at the same slot offset / type / label; confirm zero new storage slots, zero new mappings, zero new SSTORE callsites, zero new SLOAD callsites in HRROLL scope (`_rollHeroSymbol` reads the SAME 4 `dailyHeroWagers[day][q]` slots that v41 `_topHeroSymbol` read — same access pattern, same slot count).

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "storageLayout diff" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ -z "$(awk '/^## \(2\) Storage-Slot Grep Proof/,/^## \(3\)/' .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && grep -qE "dailyHeroWagers" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ -z "$(git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §2 populated with the `forge inspect` diff result (EMPTY or recorded non-empty text) for `contracts/storage/DegenerusGameStorage.sol`; the `<FILL-IN-Plan-02>` placeholder in §2 is GONE.
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited in §2.
    - §2 "Locks under HRROLL-06" block records: `dailyHeroWagers` at same slot / type / label; `dailyIdx` at same slot / type / label; zero new storage slots / mappings / SSTORE / SLOAD in HRROLL scope.
    - If §2 records non-empty storage diff → measurement doc carries the `🚨 STORAGE LAYOUT REGRESSION` flag + Task 5 user-approval gate halts on the flag.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `292-01-MEASUREMENT.md` §2 carries the storage-layout grep result vs v41 baseline. HRROLL-06 storage byte-identity attestation is recorded (EMPTY diff expected). If a regression appears the doc carries the flag for Task 5 to halt on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Selector attestations — populate `292-01-MEASUREMENT.md` §4 (HRROLL-07)</name>
  <files>
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state — confirm `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view function signatures are UNCHANGED)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (post-Task-2 partial state)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §4 (Phase 290's selector-attestation pattern — `cast sig "<canonical-signature>"` + record in tabular form)
    - Grep `contracts/modules/DegenerusGameJackpotModule.sol` for `external` + `public` function declarations to enumerate the public-ABI surface; specifically locate `payDailyJackpot` + `payDailyJackpotCoinAndTickets` declarations and capture their full canonical Solidity signatures (parameter types).
  </read_first>
  <action>
    Compute selectors for the public-ABI mutation entry points + record the private-function signature changes for completeness. Toolchain: `cast` from foundry if available (`cast sig`); fall back to python `keccak` if cast unavailable: `python3 -c 'from eth_utils import keccak; print("0x" + keccak(b"<canonical-signature>").hex()[:8])'`.

    §4 — Selector Attestations:
    - `payDailyJackpot(<full-canonical-sig>)` selector — locate the function in `contracts/modules/DegenerusGameJackpotModule.sol`, extract the canonical Solidity signature with parameter types (e.g., `payDailyJackpot()` if no-arg, or `payDailyJackpot(uint256)` etc.), compute via `cast sig "<canonical-signature>"` (4-byte 0x........). MUST match v41 close (function signature unchanged by HRROLL). Record.
    - `payDailyJackpotCoinAndTickets(<full-canonical-sig>)` selector — same procedure. MUST match v41 close. Record.
    - View functions touching `dailyHeroWagers` or `dailyIdx` — grep `contracts/modules/DegenerusGameJackpotModule.sol` for `view` returning anything related to hero wagers or daily index; record each function's selector + canonical signature. MUST match v41 close.
    - Private function deltas (recorded for completeness — do NOT count against the public-ABI HRROLL-07 invariant):
      - `_applyHeroOverride` v41 canonical: `_applyHeroOverride(uint8[4],uint256)` → v42 canonical: `_applyHeroOverride(uint8[4],uint256,uint256)`. The v42 form has one additional parameter `uint256 heroEntropy`. Private; selector not in public ABI.
      - `_rollHeroSymbol` v42 canonical: `_rollHeroSymbol(uint32,uint256)` — NEW at v42; no v41 equivalent. Private; selector not in public ABI.
      - `_topHeroSymbol` v41 canonical: `_topHeroSymbol(uint32)` — REMOVED at v42. Private; recorded for audit-story symmetry. Selector not in public ABI.

    Record in §4 as a table mirroring the Phase 290 §4 shape:

    | Function | Canonical signature | 4-byte selector | Disposition |
    |---|---|---|---|
    | `payDailyJackpot` | `<full-sig>` | `0x........` | UNCHANGED vs v41 close |
    | `payDailyJackpotCoinAndTickets` | `<full-sig>` | `0x........` | UNCHANGED vs v41 close |
    | <each view function touching dailyHeroWagers> | `<full-sig>` | `0x........` | UNCHANGED vs v41 close |
    | `_applyHeroOverride` (private; v42 form) | `_applyHeroOverride(uint8[4],uint256,uint256)` | n/a (private) | Signature changed (gained 3rd param); private — not in public ABI |
    | `_rollHeroSymbol` (private; new at v42) | `_rollHeroSymbol(uint32,uint256)` | n/a (private) | NEW at v42; private — not in public ABI |
    | `_topHeroSymbol` (private; removed at v42) | `_topHeroSymbol(uint32)` | n/a (private) | REMOVED at v42; private — not in public ABI |

    Method line: `FOUNDRY_DISABLE_NIGHTLY_WARNING=1 cast sig "<canonical-signature>"` (foundry 1.6.0-nightly OR equivalent).

    Lock under HRROLL-07: all PUBLIC selectors byte-identical to v41 close. PASSED.

    Replace the §4 `<FILL-IN-Plan-02>` placeholder with the populated table + method line + lock statement.

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "^0x[0-9a-f]{8}.*payDailyJackpot|payDailyJackpot.*0x[0-9a-f]{8}" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "payDailyJackpotCoinAndTickets.*0x[0-9a-f]{8}|0x[0-9a-f]{8}.*payDailyJackpotCoinAndTickets" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "_rollHeroSymbol\(uint32,uint256\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "_applyHeroOverride\(uint8\[4\],uint256,uint256\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "_topHeroSymbol\(uint32\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ -z "$(awk '/^## \(4\) Selector Attestations/,/^## \(5\)/' .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && [ -z "$(git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §4 records the 4-byte selectors for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` with the actual 0x........ values. Both MUST match v41 close.
    - §4 enumerates any view functions touching `dailyHeroWagers` / `dailyIdx` with their selectors (UNCHANGED vs v41 close).
    - §4 records the canonical Solidity signatures for the private functions (`_applyHeroOverride(uint8[4],uint256,uint256)` post-patch; `_rollHeroSymbol(uint32,uint256)` NEW; `_topHeroSymbol(uint32)` REMOVED) — recorded for audit-story completeness, not counted against the public-ABI HRROLL-07 invariant.
    - `<FILL-IN-Plan-02>` placeholder in §4 is GONE.
    - If any PUBLIC selector mismatch appears, the doc carries the `🚨 ATTESTATION REGRESSION` flag and Task 5 halts.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `292-01-MEASUREMENT.md` §4 carries the selector + canonical-signature attestations. HRROLL-07 public ABI byte-identity is attested (PUBLIC selectors match v41 close). Private-function signature deltas are documented for audit-story symmetry.
  </done>
</task>

<task type="auto">
  <name>Task 4: Single-site callsite verification — populate `292-01-MEASUREMENT.md` §6</name>
  <files>
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
  </files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Task-1 patched state)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (post-Task-3 partial state)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §6 (Phase 290's B2-symmetric callsite-diff pattern; the HRROLL form degenerates to single-site verification because `_applyHeroOverride` has only one caller)
  </read_first>
  <action>
    Verify the single-site `_applyHeroOverride` callsite update structurally.

    §6 — Single-Site Callsite Verification:
    - Run `grep -nE "_applyHeroOverride" contracts/modules/DegenerusGameJackpotModule.sol` — expected output: EXACTLY 2 matches (1 function declaration line + 1 callsite line inside `_rollWinningTraits`). Record both line numbers in the doc (post-patch line numbers may differ from the pre-patch L1594 + L1941 due to the function-body length changes between v41 `_topHeroSymbol` and v42 `_rollHeroSymbol`).
    - Run `grep -nE "_applyHeroOverride\(traits, r, randWord\)" contracts/modules/DegenerusGameJackpotModule.sol` — expected output: EXACTLY 1 match (the L1941-area callsite reads the post-patch 3-arg form with raw `randWord` as the 3rd arg per D-42N-BONUS-ENTROPY-01). Record the line number.
    - Run `grep -nE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` — expected output: ZERO matches (function fully deleted; no residual callsite). Confirm.
    - Run `grep -nE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` — expected output: EXACTLY 1 match (the function declaration). Record the line number.
    - Run `grep -nE "_rollHeroSymbol\(dailyIdx, heroEntropy\)" contracts/modules/DegenerusGameJackpotModule.sol` — expected output: EXACTLY 1 match (the call inside `_applyHeroOverride` body). Record the line number.

    Record in §6:

    | Verification | Expected count | Actual count | Disposition |
    |---|---|---|---|
    | `grep -cE "_applyHeroOverride"` matches | 2 | <fill-in> | PASS / FAIL |
    | `grep -cE "_applyHeroOverride\(traits, r, randWord\)"` matches | 1 | <fill-in> | PASS / FAIL |
    | `grep -cE "_topHeroSymbol"` matches | 0 | <fill-in> | PASS / FAIL |
    | `grep -cE "function _rollHeroSymbol"` matches | 1 | <fill-in> | PASS / FAIL |
    | `grep -cE "_rollHeroSymbol\(dailyIdx, heroEntropy\)"` matches | 1 | <fill-in> | PASS / FAIL |

    Note: the Phase 290 B2-symmetric pattern (which paired the `processFutureTicketBatch` callsite with the `_processOneTicketEntry` callsite and ran a structural diff between the two) degenerates to single-site verification at HRROLL because `_applyHeroOverride` has only one caller in the codebase. Record this structural distinction explicitly: "The B2-symmetric Phase 290 pattern does not apply at HRROLL because `_applyHeroOverride` has exactly one caller (`_rollWinningTraits` L1941 area). Single-site verification is the appropriate analog — confirming the lone callsite reads the post-patch 3-arg form. If any grep above returns an unexpected count, escalate to user with the unexpected output before Task 5 commit."

    Replace the §6 `<FILL-IN-Plan-02>` placeholder with the populated grep-count table + the structural-distinction note.

    Do NOT make any contract edits in this task.
  </action>
  <verify>
    <automated>grep -qE "_applyHeroOverride\(traits, r, randWord\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "function _rollHeroSymbol" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "_topHeroSymbol" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "Single-Site|single-site|single site" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ -z "$(awk '/^## \(6\) Callsite Diff/,/^## Source-Doc/' .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md | grep -E 'FILL-IN-Plan-02')" ] && ! grep -q "FILL-IN-Plan-02" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ -z "$(git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - §6 records the 5 grep-count verification rows with expected vs actual counts + per-row PASS/FAIL disposition.
    - Each grep matches the expected count: 2 occurrences of `_applyHeroOverride`; 1 occurrence of the 3-arg post-patch callsite `_applyHeroOverride(traits, r, randWord)`; 0 occurrences of `_topHeroSymbol`; 1 occurrence of `function _rollHeroSymbol`; 1 occurrence of `_rollHeroSymbol(dailyIdx, heroEntropy)`.
    - §6 explicitly notes the B2-symmetric Phase 290 pattern degenerates to single-site verification at HRROLL.
    - ALL `<FILL-IN-Plan-02>` placeholders in `292-01-MEASUREMENT.md` are now GONE.
    - If any grep returns an unexpected count, the doc carries the `🚨 ATTESTATION REGRESSION` flag and Task 5 halts.
    - Zero contract-source edits in this task.
  </acceptance_criteria>
  <done>
    `292-01-MEASUREMENT.md` §6 carries the single-site callsite verification with all 5 grep-count rows. The B2-degeneration distinction is documented. All 6 sections of `292-01-MEASUREMENT.md` are now populated (§1 + §3 + §5 from Plan 01; §2 + §4 + §6 from Plan 02 Tasks 2-4) and ready for copy-forward into the Task 5 commit message body.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Present batched contract + measurement diff for explicit USER review + USER-APPROVED commit (NO push)</name>
  <files>
    contracts/modules/DegenerusGameJackpotModule.sol,
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
  </files>
  <read_first>
    - All 2 files in `<files>` (full post-Task-1..4 state)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md (Plan 01 output — referenced in the commit message body)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
  </read_first>
  <action>
    Present the working-tree diff to the user for explicit review per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` + `feedback_batch_contract_approval.md`. Pre-flight checks the agent runs FIRST (do not proceed if any fail):

    1. Confirm `292-01-MEASUREMENT.md` carries no `<FILL-IN-Plan-02>` placeholders and no `🚨 STORAGE LAYOUT REGRESSION` / `🚨 ATTESTATION REGRESSION` / `🚨 ESCALATION-CHECKPOINT` flags.
    2. Confirm `git diff --name-only` shows EXACTLY the 2 paths in `<files>` (`contracts/modules/DegenerusGameJackpotModule.sol` + `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md`).
    3. Confirm `git diff contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` is EMPTY.
    4. Confirm `grep -nE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches.
    5. Confirm `grep -cE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns `1`.
    6. Confirm `grep -cE "_applyHeroOverride\(" contracts/modules/DegenerusGameJackpotModule.sol` returns `2`.
    7. Confirm `grep -qE "_applyHeroOverride\(traits, r, randWord\)" contracts/modules/DegenerusGameJackpotModule.sol`.
    8. Confirm `grep -qE "keccak256\(abi\.encode\(entropy, day\)\)" contracts/modules/DegenerusGameJackpotModule.sol`.
    9. Confirm `grep -qE "uint32\[32\] memory weights" contracts/modules/DegenerusGameJackpotModule.sol` (D-42N-CACHE-01 chosen flat-uint32 cache shape implemented).
    10. Confirm `! grep -qE "CALL 1 and.*CALL 2" contracts/modules/DegenerusGameJackpotModule.sol` (no `(CALL 1 / CALL 2)` wording in any NatSpec).

    If any check fails → escalate to user with the failure mode; do NOT proceed to the diff presentation.

    Present the consolidated diff: run `git diff contracts/modules/DegenerusGameJackpotModule.sol .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` and ask the user to review per `<how-to-verify>` below. Wait for the user to type "approved" (or to describe required fixes). On "approved": stage exactly the 2 paths listed in `<files>` and create ONE batched commit using the commit-template in `<how-to-verify>`. Substitute the placeholders (`<0x...>`, `[EMPTY | <recorded value>]`, `<gas savings>`, `<line-numbers>`) with the actual values from `292-01-MEASUREMENT.md`. Do NOT `git push` after the commit — pushing requires a separate explicit user instruction per `feedback_manual_review_before_push.md`. See `<what-built>` + `<how-to-verify>` + `<resume-signal>` for the full checkpoint specification.
  </action>
  <verify>
    <human-check>User reads the full diff via `git diff contracts/modules/DegenerusGameJackpotModule.sol`, reads `292-01-DESIGN-INTENT-TRACE.md` (Plan 01 output) + `292-01-MEASUREMENT.md` (populated through Task 4), and types "approved" before any commit. Post-commit verification: `git log -1 --pretty=format:"%h %s"` shows the new commit subject `feat(292): HRROLL — weighted-roll hero-override with ×1.5 leader bonus + no floor + cross-bonus invariance [HRROLL-01..04,06,07,08]`; `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` is EMPTY.</human-check>
    <automated>git log -1 --pretty=format:"%s" | grep -qE "HRROLL" && git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD --name-only | sort -u | grep -E "contracts/modules/DegenerusGameJackpotModule\.sol|292-01-MEASUREMENT\.md" | wc -l | grep -qE "^[2-9]$|^[0-9]{2,}$" && [ -z "$(git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/ 2>/dev/null)" ] && ! grep -qE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight checks all PASS before the user-diff presentation.
    - User reads the full diff + the trace doc + the measurement doc and types "approved".
    - Exactly ONE batched commit lands covering the 2 paths in `<files>`.
    - Commit subject matches the template (subject contains `HRROLL` token).
    - `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` is EMPTY (storage / Degenerette module / KI / tests UNCHANGED).
    - `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` shows ONLY the Task 1 Edits A..F hunks (`_applyHeroOverride` NatSpec/signature/body + `_topHeroSymbol` deletion + `_rollHeroSymbol` addition + `_rollWinningTraits` L1941 callsite update).
    - `grep -nE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches at HEAD.
    - No `git push` issued by the executor.
  </acceptance_criteria>
  <done>
    ONE USER-APPROVED batched commit lands at HEAD covering: the contract patch (Task 1 Edits A..F) + the populated `292-01-MEASUREMENT.md`. KNOWN-ISSUES UNCHANGED. Degenerette module + storage + tests UNCHANGED. No `git push`. Phase 292 audit-subject HEAD ready for Phase 293 TST-HRROLL fixture to assert against + for Phase 297 audit deliverable §3.A to cite.
  </done>
  <what-built>
    Two working-tree changes ready for ONE batched commit covering HRROLL-01..04 + HRROLL-06 + HRROLL-07 + HRROLL-08:

    1. `contracts/modules/DegenerusGameJackpotModule.sol` — 4 hunk groups (mapped to Task 1 Edits A..F):
       (a) `_applyHeroOverride` NatSpec rewritten (no `(CALL 1 / CALL 2) determinism` wording; describes weighted-roll mechanic + raw VRF entropy + per-day lock-in across bonus/regular rolls per D-42N-BONUS-ENTROPY-01 + orthogonal entropy domains per D-42N-COLOR-ENTROPY-01) + signature gains 3rd parameter `uint256 heroEntropy` + body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)` — covers HRROLL-04 + Edits A, B, C.
       (b) `_topHeroSymbol(uint32 day)` (L1623-1653 in v41 form) DELETED entirely (NatSpec block + function body); no stub, no marker per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md` — covers HRROLL-01 deletion arm + Edit D.
       (c) `_rollHeroSymbol(uint32 day, uint256 entropy)` ADDED at the slot vacated by `_topHeroSymbol` with the two-pass weighted-roll algorithm + flat `uint32[32]` memory cache (D-42N-CACHE-01 LOCKED) + ×1.5 leader bonus (`leaderBonus = uint64(maxAmount) / 2`) + no eligibility floor + early-bail when `total == 0` + cursor walk with `pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)` + flat idx ascending direction + leader-bonus add at `idx == leaderIdx` + first-seen tie-break via strict `>` in pass 1 + unreachable-cursor `revert` for invariant signal — covers HRROLL-01 addition arm + HRROLL-02 + HRROLL-03 + D-42N-DETERMINISM-01 + Edit E.
       (d) `_rollWinningTraits` L1941 callsite updated to read `_applyHeroOverride(traits, r, randWord);` — raw `randWord` plumbed as 3rd arg per D-42N-BONUS-ENTROPY-01; optionally tightens the `_rollWinningTraits` NatSpec to remove the word "top" — covers HRROLL-04 + Edit F + (optional) Edit G.

    2. `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` — §2 + §4 + §6 populated post-patch (storage-slot grep EMPTY for `DegenerusGameStorage.sol`; public selectors UNCHANGED for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions; single-site callsite grep verification matrix PASS) — covers HRROLL-06 + HRROLL-07 attestations. §1 + §3 + §5 were FINAL at Plan 01 time (audit baseline + theoretical gas + D-42N-CACHE-01 three-shape comparison + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision attestation + no-events distinction).

    The contract diff is medium-sized (~50-70 hunk lines including the `_rollHeroSymbol` body addition + `_topHeroSymbol` deletion + `_applyHeroOverride` NatSpec rewrite + signature change + body line change + `_rollWinningTraits` callsite line change). Storage layout UNCHANGED. Degenerette module UNCHANGED. Public ABI UNCHANGED.

    Per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md`: the orchestrator + planner have NOT pre-approved this diff. Per `feedback_manual_review_before_push.md`: the user reviews the diff IN FULL before commit; no `git push` without a second explicit user instruction afterward. Per `feedback_batch_contract_approval.md`: ONE batched commit covers all HRROLL-01..04 + HRROLL-06 + HRROLL-07 + HRROLL-08 edits + attestations (no piecemeal commits).
  </what-built>
  <how-to-verify>
    1. Run `git status` — confirm only the 2 expected paths are modified/created (no rogue contract / test / KI edits; no Degenerette module edit; no storage module edit).
    2. Run `git diff contracts/modules/DegenerusGameJackpotModule.sol` — read the contract diff in full. Confirm:
       - `_applyHeroOverride` signature has 3 positional parameters (`uint8[4] memory w, uint256 randomWord, uint256 heroEntropy`).
       - `_applyHeroOverride` body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)` (NOT `_topHeroSymbol(dailyIdx)`).
       - `_applyHeroOverride` color-path bit-reads of `randomWord` at the existing 4-quadrant if/else block UNCHANGED.
       - `_applyHeroOverride` NatSpec describes the weighted-roll mechanic with NO history language and NO `(CALL 1 / CALL 2) determinism` wording.
       - `_topHeroSymbol` function declaration + body + NatSpec block ENTIRELY GONE (no stub, no marker).
       - `_rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)` declaration ADDED.
       - `_rollHeroSymbol` body uses flat `uint32[32] memory weights` cache (D-42N-CACHE-01 chosen shape).
       - `_rollHeroSymbol` pass-1 uses the v41 bit-extract idiom (`uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF)`).
       - `_rollHeroSymbol` total widened to `uint64`; leaderBonus = `uint64(maxAmount) / 2`; effectiveTotal = `total + leaderBonus` typed `uint64`.
       - `_rollHeroSymbol` pick computed via `uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal)` — `abi.encode` (NOT `abi.encodePacked`).
       - `_rollHeroSymbol` pass-2 reads `cumulative += uint64(weights[idx]); if (idx == leaderIdx) cumulative += leaderBonus; if (cumulative > pick) return (true, uint8(idx >> 3), uint8(idx & 7));`.
       - `_rollHeroSymbol` early-bail at `total == 0` returns `(false, 0, 0)`.
       - `_rollHeroSymbol` ends with `revert("HRROLL: cursor underflow");` (or equivalent unreachable-invariant signal) — not a defensive `return (false, 0, 0)`.
       - `_rollWinningTraits` L1941 callsite reads `_applyHeroOverride(traits, r, randWord);` (3 args; raw `randWord` is the 3rd).
       - Zero comparative/historical language anywhere in NatSpec touched ("previously", "pre-v41", "formerly", "the bug at v40 was", "pre-cleanup", "(CALL 1 / CALL 2) determinism").
    3. Run `git diff contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` — confirm EMPTY (no storage / Degenerette / KI / test changes).
    4. Read `292-01-DESIGN-INTENT-TRACE.md` (Plan 01 output) — confirm 5 HRROLL-10 trace sections + 7 decision anchors + HRROLL-05 backward-trace + SWEEP-02(ii) pre-emptive answers + Plan-02 pre-patch-gate.
    5. Read `292-01-MEASUREMENT.md` (populated through Task 4) — confirm:
       - §1 audit baseline anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`.
       - §2 storage-layout grep diff EMPTY for `DegenerusGameStorage.sol` (or escalated flag if non-empty).
       - §3 D-42N-CACHE-01 three-shape comparison table + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision attestation + ESCALATION-CHECKPOINT marker `NOT TRIGGERED`.
       - §4 selectors recorded for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions (all UNCHANGED vs v41 close) + private-function canonical signatures recorded for completeness.
       - §5 explicit "Events — NONE Touched" disposition.
       - §6 single-site callsite verification matrix (5 grep-count rows) all PASS.
       - No `<FILL-IN-Plan-02>` placeholders remain.
    6. If everything is correct, instruct the agent to commit via the template below. If anything is wrong, instruct the agent to fix it BEFORE commit (do not commit-then-revert).

    Commit-template the executor uses post-approval (do NOT execute until user types "approved"):
    ```
    git add contracts/modules/DegenerusGameJackpotModule.sol \
            .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
    git commit -m "$(cat <<'EOF'
    feat(292): HRROLL — weighted-roll hero-override with ×1.5 leader bonus + no floor + cross-bonus invariance [HRROLL-01..04,06,07,08]

    D-42N-LEADER-BONUS-01 — ×1.5 leader-weight bonus (leaderBonus = maxAmount / 2) (user-locked 2026-05-17)
    D-42N-FLOOR-01 — no min-wager floor; smallest organic bettor wins proportionally (user-locked 2026-05-17)
    D-42N-BONUS-ENTROPY-01 — raw randWord plumbed to _rollHeroSymbol; cross-bonus invariance preserved (bonus + regular rolls same (q,s) per day) (user-locked 2026-05-17)
    D-42N-COLOR-ENTROPY-01 — color bits from `r`; symbol bits from keccak256(abi.encode(randWord, day)) — orthogonal entropy domains by construction
    D-42N-DETERMINISM-01 — exact algorithm locked: abi.encode (NOT abi.encodePacked); flat idx ascending; leader-bonus add at idx == leaderIdx; strict > tie-break
    D-42N-CACHE-01 — flat uint32[32] memory cache (lowest worst-case among 3 candidate shapes per 292-01-MEASUREMENT.md §3.b)
    D-42N-GAS-01 — acceptance threshold +500 soft / +750 hard vs v41 _topHeroSymbol baseline (theoretical +475; empirical at Phase 293 TST-HRROLL-06)

    Design-intent trace: see 292-01-DESIGN-INTENT-TRACE.md (HRROLL-10; per feedback_design_intent_before_deletion.md) — 5-section trace covers (i) original deterministic _topHeroSymbol single-leader rationale, (ii) leader-bonus magnitude trade-offs (×2 vs ×1.5 vs none), (iii) sybil exposure / no-floor trade-offs, (iv) HRROLL-05 RNG commitment-window backward-trace from _rollHeroSymbol consumer to placeDegeneretteBet wager-write at contracts/modules/DegenerusGameDegeneretteModule.sol:484-501, (v) gas headroom + D-42N-CACHE-01 cache-shape + D-42N-DETERMINISM-01 algorithm lock. SWEEP-02(ii) HRROLL adversarial-hypothesis pre-emptive answers recorded for Phase 296. Out-of-scope register enumerated.

    Contract patch:
    - _applyHeroOverride NatSpec rewritten (no `(CALL 1 / CALL 2) determinism` wording per feedback_no_history_in_comments.md); signature gains 3rd parameter `uint256 heroEntropy`; body invokes _rollHeroSymbol(dailyIdx, heroEntropy) instead of _topHeroSymbol(dailyIdx); color-path UNCHANGED (HRROLL-04).
    - _topHeroSymbol(uint32 day) at v41 L1623-1653 DELETED entirely (no stub, no marker per feedback_no_dead_guards.md + feedback_frozen_contracts_no_future_proofing.md) (HRROLL-01 deletion arm).
    - _rollHeroSymbol(uint32 day, uint256 entropy) ADDED with two-pass weighted-roll: pass 1 SLOADs 4 packed quadrants once + decodes 32 amounts + tracks (total, maxAmount, leaderIdx) via strict > (first-seen tie-break); early-bail (false, 0, 0) when total == 0; pass 2 cursor walk against pick = uint64(uint256(keccak256(abi.encode(entropy, day))) % effectiveTotal) over flat uint32[32] cache with leader-bonus add at idx == leaderIdx (HRROLL-01 addition arm + HRROLL-02 + HRROLL-03 + D-42N-DETERMINISM-01 + D-42N-CACHE-01).
    - _rollWinningTraits L1941 callsite updated to pass raw `randWord` as 3rd arg: `_applyHeroOverride(traits, r, randWord)` (HRROLL-04 + D-42N-BONUS-ENTROPY-01).

    Storage byte-identity (HRROLL-06): see 292-01-MEASUREMENT.md §2 — forge inspect storageLayout diff for contracts/storage/DegenerusGameStorage.sol vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 = [EMPTY | <recorded value>]. dailyHeroWagers[uint32 => uint256[4]] at unchanged slot; dailyIdx at unchanged slot. Zero new storage slots; zero new mappings; zero new SSTORE/SLOAD callsites in HRROLL scope (_rollHeroSymbol reads the SAME 4 dailyHeroWagers[day][q] slots that v41 _topHeroSymbol read).

    Public ABI byte-identity (HRROLL-07): see 292-01-MEASUREMENT.md §4 — payDailyJackpot selector <0x........> UNCHANGED; payDailyJackpotCoinAndTickets selector <0x........> UNCHANGED; all view-function selectors UNCHANGED. _applyHeroOverride is private (signature change is private-function delta; does NOT count against public-ABI invariant). _rollHeroSymbol is new private. _topHeroSymbol removed private. Zero new public/external entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks.

    Worst-case gas (HRROLL-08): see 292-01-MEASUREMENT.md §3 — theoretical derivation per feedback_gas_worst_case.md: v41 _topHeroSymbol baseline ~9494 gas; D-42N-CACHE-01 three-shape comparison locked flat uint32[32] at ~9969 gas (~+475 gas regression — lowest among 3 candidate shapes; uint64[32] pre-bonus-apply +489 / packed uint256[4] +1051; re-SLOAD-without-cache REJECTED per feedback_no_dead_guards.md). D-42N-GAS-01 acceptance threshold: +500 soft / +750 hard (well under +10K ESCALATION-CHECKPOINT threshold). Empirical confirmation DEFERRED to Phase 293 TST-HRROLL-06 per Phase 291 D-291-GAS-01 mirror pattern.

    HRROLL-05 RNG commitment-window invariant preserved structurally: dailyHeroWagers[dailyIdx][q] writes at placeDegeneretteBet (DegenerusGameDegeneretteModule.sol:484-501) UNCHANGED; dailyIdx writers UNCHANGED (only _unlockRng mutates per v41 Phase 288 D-288-FIX-SHAPE-01 carry); randWord arrives via VRF callback strictly after wager-time commitment.

    D-42N-COLOR-ENTROPY-01 non-collision attestation: color path consumes bits quadrant*3 of `r` (existing); symbol-roll consumes keccak256(abi.encode(randWord, day)) — structurally orthogonal entropy domains (keccak output independent of input bit-slices by hash design); attestation recorded in 292-01-MEASUREMENT.md §3.e.

    Cross-bonus invariance (D-42N-BONUS-ENTROPY-01): raw randWord is identical across bonus + regular _rollWinningTraits invocations within a single jackpot resolution → both _applyHeroOverride invocations receive the same `heroEntropy` → symbol-roll lands the SAME (q,s) per day; only colors differ (via the existing `r`-vs-`randWord` distinction at L1937-L1939). Per-jackpot-day hero-symbol lock-in preserved.

    Single-site callsite verification (degeneration of v40 B2-symmetric pattern): _applyHeroOverride has exactly 1 caller (_rollWinningTraits L1941 area); recorded in 292-01-MEASUREMENT.md §6; grep-count matrix all PASS.

    Carry-forward locks preserved:
    - D-288-FIX-SHAPE-01: dailyIdx single-writer-day-key invariant inherited (only _unlockRng mutates dailyIdx; UNCHANGED at v42).
    - D-40N-MINTBOOST-OUT-01: mint-boost path UNCHANGED (HRROLL doesn't touch mint).
    - D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-02 + D-271-ADVERSARIAL-03: adversarial pass deferred to Phase 296 SWEEP (3-skill PARALLEL: /contract-auditor + /zero-day-hunter + /economic-analyst; /degen-skeptic OUT).
    - contracts/storage/DegenerusGameStorage.sol UNCHANGED at this plan.
    - contracts/modules/DegenerusGameDegeneretteModule.sol UNCHANGED (placeDegeneretteBet wager-write site is the HRROLL-05 backward-trace destination).
    - test/ UNCHANGED (TST-HRROLL-01..06 fixture ships at Phase 293).
    - KNOWN-ISSUES.md UNMODIFIED (per D-42N-KI-01 default at Phase 297).

    Phase 292 — second contract-surface phase of v42.0 milestone (after Phase 290 MINTCLN).
    Audit baseline: MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4
    Wave shape: 1 USER-APPROVED batched contract commit (per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md + feedback_manual_review_before_push.md)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    DO NOT push after commit. Per `feedback_manual_review_before_push.md`: pushing requires a second explicit user instruction.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the batched commit, or describe required fixes (the agent will apply fixes and re-present for review before any commit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player → `placeDegeneretteBet` (wager-write site at `DegenerusGameDegeneretteModule.sol:484-501`) | Untrusted caller writes to `dailyHeroWagers[dailyIdx][q]` at bet time; the wager amount becomes the symbol-roll weight at day D+1's jackpot. HRROLL-05 backward-trace verified the commitment-window invariant: wager amounts locked before VRF request fires; symbol-roll entropy unknowable at wager time. UNCHANGED by this plan. |
| Chainlink VRF → jackpot resolution → `_rollWinningTraits` → `_applyHeroOverride` → `_rollHeroSymbol` | `randWord` flows from VRF callback into `_rollWinningTraits`, then unchanged into `_applyHeroOverride` (3rd arg), then into `_rollHeroSymbol(dailyIdx, heroEntropy)`. The raw `randWord` (NOT post-bonus-tag `r`) is the symbol-roll entropy per D-42N-BONUS-ENTROPY-01; cross-bonus invariance preserved. |
| `dailyHeroWagers[dailyIdx]` storage → `_rollHeroSymbol` 4-SLOAD reads | The 4 packed uint256 slots are READ-ONLY in HRROLL scope; SLOAD count UNCHANGED vs v41 `_topHeroSymbol`; access pattern UNCHANGED; storage byte-identical per HRROLL-06. |
| `dailyIdx` storage → `_applyHeroOverride` consumer | `dailyIdx` writers UNCHANGED by this plan (only `_unlockRng` mutates per v41 Phase 288 D-288-FIX-SHAPE-01 carry); frozen across rng-lock window; single-writer-day-key invariant preserved. |
| `_rollHeroSymbol` keccak output → pick modulo → cursor → returned `(q, s)` | Symbol-roll consumes `keccak256(abi.encode(heroEntropy, day))` mod `effectiveTotal`; `effectiveTotal = total + leaderBonus` is uint64 (well under 2^64); modulo bias vanishing (effectiveTotal << 2^256). Returned `(q, s)` flows into `_applyHeroOverride` color sampling + trait packing — UNCHANGED downstream of the override. |
| `_applyHeroOverride` → `_rollWinningTraits` → trait-pack output → jackpot winners | Trait-pack output flows into jackpot trait-winner selection; unchanged downstream of the override; correctness of `_rollHeroSymbol` directly determines which `(q, s)` slot wins the hero override. |

## STRIDE Threat Register — Phase 292 Plan 02 (HRROLL Contract Patch)

(Mirrors the SWEEP-02(ii) HRROLL adversarial hypotheses pre-emptively answered in `292-01-DESIGN-INTENT-TRACE.md` per the planner contract; Phase 296 PARALLEL adversarial pass audits against this same register.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-292-02-01 | Tampering / Spoofing | ×1.5 leader-bonus whale-coordination / wash-trading MEV | mitigate | Leader bonus computed ON CHAIN from on-chain wager state at `_rollHeroSymbol` invocation time. Attacker cannot wash-trade between bet-placement and roll-execution because symbol-roll entropy unknowable at bet time per HRROLL-05. Coordinating whales can push a (q, s) to leader status — that is the mechanic working as designed (×1.5 rewards capital commitment per D-42N-LEADER-BONUS-01), not an MEV exploit. SWEEP-02(ii) Q1 expected finding: SAFE_BY_DESIGN. |
| T-292-02-02 | Spoofing / Denial of Service | No-floor sybil-dilution attack | mitigate | Sybil dilution structurally bounded: 32 × 1 wei of wager weight requires 32 × 1e12 wei = 0.032 ETH of actual stake (per `wagerUnit = totalBet / 1e12` scaling at DegenerusGameDegeneretteModule.sol:489); at realistic daily wager volumes the dilution is negligible (≤ 32 / effectiveTotal). SWEEP-02(ii) Q2 expected finding: SAFE_BY_STRUCTURAL_CLOSURE. |
| T-292-02-03 | Information Disclosure / Tampering | New RNG-consumer (symbol roll consuming VRF bits via keccak) collision with existing consumers | mitigate | Symbol-roll consumes `keccak256(abi.encode(heroEntropy, day))` output — structurally orthogonal to raw `randWord` bit-slices used by jackpot-path-select (bits[0..12]) / lootbox-Bernoulli (bits[152..167]) / jackpot-Bernoulli (bits[200..215]) / color-sample (bits `quadrant*3`). Keccak output independent of input bit-slices by hash design. Non-collision is by construction, not probabilistic. Attestation recorded in `292-01-MEASUREMENT.md` §3.e per D-42N-COLOR-ENTROPY-01. SWEEP-02(ii) Q3 expected finding: SAFE_BY_DESIGN. |
| T-292-02-04 | Denial of Service | Gas regression DOS surface | mitigate | D-42N-GAS-01 acceptance threshold derived from D-42N-CACHE-01 chosen-shape worst case (~+475 gas vs v41 baseline; soft +500 / hard +750; well under +10K ESCALATION-CHECKPOINT threshold). Computation is bounded: 4 SLOAD + 2 × 32-iteration loops + 1 keccak + 1 modulo + 1 division — no unbounded loops, no caller-controlled iteration count. Phase 293 TST-HRROLL-06 empirically asserts against the threshold per D-291-GAS-01 mirror pattern. SWEEP-02(ii) Q4 expected finding: SAFE_BY_BOUNDED_COMPUTATION. |
| T-292-02-05 | Tampering | RNG commitment-window invariant preservation (HRROLL-05) | mitigate | Backward-trace verified at Plan 01 `292-01-DESIGN-INTENT-TRACE.md` §(iv): `dailyHeroWagers[dailyIdx][q]` writes at `placeDegeneretteBet` UNCHANGED; `dailyIdx` writers UNCHANGED (only `_unlockRng` mutates); wager amounts for day D locked before day D+1 VRF request; symbol-roll entropy unknowable at wager time. v41 Phase 288 D-288-FIX-SHAPE-01 single-writer-day-key invariant inherited verbatim. Player cannot adjust wagers to bias symbol-roll. |
| T-292-02-06 | Tampering | Storage byte-identity (HRROLL-06) | mitigate | Task 2 §2 records `forge inspect storageLayout` diff for `DegenerusGameStorage.sol` vs v41 close baseline (expected EMPTY). Plan 02 makes ZERO edits to `contracts/storage/DegenerusGameStorage.sol`. `_rollHeroSymbol` reads the SAME 4 `dailyHeroWagers[day][q]` slots that v41 `_topHeroSymbol` read — same SLOAD count, same access pattern. Task 5 pre-flight halts if `git diff contracts/storage/` shows any change. |
| T-292-02-07 | Tampering | Public ABI byte-identity (HRROLL-07) | mitigate | Task 3 §4 records selectors for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions (expected UNCHANGED vs v41 close). Private-function signature deltas (`_applyHeroOverride` gains parameter; `_rollHeroSymbol` added; `_topHeroSymbol` removed) are documented but do NOT count against the public-ABI invariant. Zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks. |
| T-292-02-08 | Tampering | D-42N-BONUS-ENTROPY-01 cross-bonus invariance | mitigate | Raw `randWord` (NOT post-bonus-tag `r`) plumbed to `_rollHeroSymbol` via the 3rd `_applyHeroOverride` parameter; verified at Task 4 §6 single-site callsite grep (`_applyHeroOverride(traits, r, randWord)` exact match). On jackpot days that trigger both bonus + regular `_rollWinningTraits` invocations, both pass the same `randWord` value to `_applyHeroOverride` (same caller scope) → both `_applyHeroOverride` invocations consume the same `heroEntropy` → symbol-roll lands the SAME `(q, s)` per day. Per-jackpot-day hero-symbol lock-in preserved. |
| T-292-02-09 | Tampering | D-42N-DETERMINISM-01 exact algorithm preservation | mitigate | Task 1 Edit E acceptance criteria enforce: `abi.encode(entropy, day)` (NOT `abi.encodePacked`); flat idx ascending direction (q ascending → s ascending); leader-bonus add at `idx == leaderIdx`; first-seen tie-break via strict `>` in pass 1. Plan 02 implements verbatim from the spec recorded in `292-01-DESIGN-INTENT-TRACE.md` §(v). |
| T-292-02-10 | Tampering | D-42N-CACHE-01 chosen-shape implementation | mitigate | Task 1 Edit E acceptance criterion enforces flat `uint32[32]` cache (grep `uint32[32] memory weights` matches; `uint64[32]` + `uint256[4]` patterns absent). The chosen shape was locked in `292-01-MEASUREMENT.md` §3.b based on the three-shape gas comparison; Plan 02 implements without deviation. |
| T-292-02-11 | Tampering | `_topHeroSymbol` complete deletion (`feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md`) | mitigate | Task 1 Edit D acceptance criterion enforces ZERO matches for `_topHeroSymbol` in the post-patch file (function declaration + every callsite gone). No stub. No `// @dev removed` marker. No deprecated alternative preserved for "future flag-gating" — frozen-contract discipline prohibits future-proofing hooks. Unreachable-cursor `revert` in `_rollHeroSymbol` end is intentional invariant signaling, not a dead guard (a defensive `return (false, 0, 0)` would be dead code per `feedback_no_dead_guards.md`). |
| T-292-02-12 | Tampering | NatSpec history-language drift (`feedback_no_history_in_comments.md`) | mitigate | Task 1 Edit A + Edit E acceptance criteria enforce zero comparative/historical language in any NatSpec touched (`previously`, `pre-v41`, `formerly`, `(CALL 1 / CALL 2) determinism`, `pre-cleanup`); historical narrative lives ONLY in `292-01-DESIGN-INTENT-TRACE.md` + Task 5 commit message body + Phase 297 audit deliverable. Task 5 pre-flight halts if any of the prohibited tokens reappear. |
| T-292-02-SC | Tampering | Package install (npm/pip/cargo) | accept (rationale: not applicable) | Phase 292 makes ZERO package installs. No npm/pip/cargo `install` operations scheduled. Adversarial-pass spawn at Phase 296 covers the smart-contract adversarial surface instead. Package-legitimacy gate does not fire. |

(Severity ceiling for this plan: `medium`. The `_topHeroSymbol` deletion + new RNG consumer introduction are the highest-impact changes but all dispositions are `mitigate` via the locked decision anchors + the AGENT-COMMITTED measurement framework. No `high` severity threat is in scope — the cleanup is a player-experience rebalance with bounded gas regression + byte-identical storage + byte-identical public ABI.)
</threat_model>

<verification>
After Task 5's USER-APPROVED commit:

1. `git log -1 --pretty=format:"%h %s"` shows the new commit with subject containing `HRROLL`.
2. `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` shows exactly the Task 1 Edits A..F hunks and NOTHING ELSE in any contract file under `contracts/`.
3. `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` is EMPTY.
4. `forge build` (or hardhat compile) succeeds for the post-patch tree (no compile errors from the `_topHeroSymbol` removal / `_rollHeroSymbol` addition / `_applyHeroOverride` signature change / `_rollWinningTraits` callsite update).
5. `forge inspect contracts/storage/DegenerusGameStorage.sol storageLayout` diffed against v41 close baseline is EMPTY (per Task 2 §2 record).
6. `cast sig "payDailyJackpot(<canonical-sig>)"` matches the v41 close selector value recorded in `292-01-MEASUREMENT.md` §4.
7. `cast sig "payDailyJackpotCoinAndTickets(<canonical-sig>)"` matches the v41 close selector value recorded in `292-01-MEASUREMENT.md` §4.
8. `grep -nE "_topHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO matches.
9. `grep -cE "function _rollHeroSymbol" contracts/modules/DegenerusGameJackpotModule.sol` returns `1`.
10. `grep -cE "_applyHeroOverride\(" contracts/modules/DegenerusGameJackpotModule.sol` returns `2` (1 declaration + 1 callsite).
11. `grep -qE "_applyHeroOverride\(traits, r, randWord\)" contracts/modules/DegenerusGameJackpotModule.sol` matches.
12. `grep -qE "keccak256\(abi\.encode\(entropy, day\)\)" contracts/modules/DegenerusGameJackpotModule.sol` matches.
13. `grep -qE "uint32\[32\] memory weights" contracts/modules/DegenerusGameJackpotModule.sol` matches.
14. `! grep -qE "CALL 1 and.*CALL 2" contracts/modules/DegenerusGameJackpotModule.sol` (no `(CALL 1 / CALL 2)` wording in any NatSpec).
15. Both plan-02 artifacts in the working tree have been committed: `contracts/modules/DegenerusGameJackpotModule.sol` (patched) + `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` (populated).
16. KNOWN-ISSUES.md UNMODIFIED at this plan's HEAD.
17. NO `git push` issued by the executor.
18. Single-site callsite verification (recorded in `292-01-MEASUREMENT.md` §6) shows all 5 grep-count rows PASS.
</verification>

<success_criteria>
- ONE USER-APPROVED batched commit lands containing: the HRROLL-01..04 contract patch (4 hunk groups in `DegenerusGameJackpotModule.sol`) + the populated `292-01-MEASUREMENT.md` (§2 + §4 + §6).
- HRROLL-01 implemented: `_topHeroSymbol` fully deleted; `_rollHeroSymbol(uint32 day, uint256 entropy)` added with two-pass weighted-roll algorithm + flat `uint32[32]` cache + early-bail when `total == 0`.
- HRROLL-02 implemented: `leaderBonus = uint64(maxAmount) / 2` applied at the leader's idx in pass 2; effective ×1.5 weight on leader; first-seen tie-break preserved via strict `>` in pass 1.
- HRROLL-03 implemented: no eligibility floor; every slot with `amount > 0` participates proportionally.
- HRROLL-04 implemented: `_applyHeroOverride` signature gains 3rd parameter `uint256 heroEntropy`; body invokes `_rollHeroSymbol(dailyIdx, heroEntropy)`; color-path UNCHANGED; NatSpec rewritten (no `(CALL 1 / CALL 2)` wording); `_rollWinningTraits` L1941 callsite updated to pass raw `randWord` as 3rd arg per D-42N-BONUS-ENTROPY-01.
- HRROLL-05 invariant preserved structurally: `dailyHeroWagers` writers + `dailyIdx` writers UNCHANGED; backward-trace verified at Plan 01 trace §(iv).
- HRROLL-06 attested: storage byte-identical to v41 close HEAD (forge inspect storageLayout diff EMPTY for `DegenerusGameStorage.sol`); recorded in `292-01-MEASUREMENT.md` §2.
- HRROLL-07 attested: public ABI selectors UNCHANGED on `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions; recorded in `292-01-MEASUREMENT.md` §4.
- HRROLL-08 attested: worst-case gas bounded by D-42N-GAS-01 acceptance threshold (~+475 theoretical; soft +500 / hard +750; recorded in `292-01-MEASUREMENT.md` §3.b + §3.c); empirical assertion deferred to Phase 293 TST-HRROLL-06.
- Single-site callsite verification PASS (recorded in `292-01-MEASUREMENT.md` §6).
- D-42N-DETERMINISM-01 exact algorithm implemented verbatim: `abi.encode(entropy, day)` (NOT `abi.encodePacked`); flat idx ascending; leader-bonus add at `idx == leaderIdx`; first-seen tie-break via strict `>`.
- D-42N-CACHE-01 LOCKED flat `uint32[32]` cache shape implemented verbatim.
- D-42N-BONUS-ENTROPY-01 cross-bonus invariance preserved: raw `randWord` plumbed; bonus + regular rolls land same `(q, s)` per day.
- D-42N-COLOR-ENTROPY-01 non-collision attestation recorded in `292-01-MEASUREMENT.md` §3.e (color from `r`; symbol from `keccak(randWord, day)` — orthogonal entropy domains by construction).
- `_topHeroSymbol` fully deleted per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md` — no stub, no marker, no deprecated alternative.
- Zero comparative/historical language in any NatSpec edit per `feedback_no_history_in_comments.md` — historical narrative lives ONLY in `292-01-DESIGN-INTENT-TRACE.md` + the commit message body + Phase 297 audit deliverable.
- Storage + `DegenerusGameDegeneretteModule.sol` + `test/` + `KNOWN-ISSUES.md` UNCHANGED.
- No `git push` issued by the executor.
- Threat-model table T-292-02-01..12 + T-292-02-SC mirrors the SWEEP-02(ii) HRROLL adversarial hypotheses + ready for Phase 296 PARALLEL adversarial-pass audit.
</success_criteria>

<output>
Create `.planning/phases/292-hero-override-weighted-roll-hrroll/292-02-SUMMARY.md` when this plan completes. Record: (a) all 7 must-haves verified PASS (HRROLL-01..04 + HRROLL-06 + HRROLL-07 + HRROLL-08); (b) commit hash + subject for the USER-APPROVED batched commit; (c) `292-01-MEASUREMENT.md` populated values (storage-layout diff result; selector values; single-site callsite verification matrix; D-42N-CACHE-01 chosen shape + D-42N-GAS-01 threshold from Plan 01 + D-42N-COLOR-ENTROPY-01 attestation cite); (d) confirmation that `git diff 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4..HEAD -- contracts/storage/ contracts/modules/DegenerusGameDegeneretteModule.sol KNOWN-ISSUES.md test/` is EMPTY; (e) confirmation no `git push` was issued; (f) hand-off statement: "Phase 292 audit-subject HEAD ready for Phase 293 TST-HRROLL-01..06 regression fixture + for Phase 297 audit deliverable §3.A delta-surface citation."
</output>
