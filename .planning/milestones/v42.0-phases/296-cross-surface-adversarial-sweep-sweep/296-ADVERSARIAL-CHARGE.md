# Phase 296 Adversarial-Pass Charge — v42.0 Cross-Surface Sweep (MINTCLN + HRROLL + DPNERF)

**For:** `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` (3-skill SWEEP per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry).

**Out of scope:** `/degen-skeptic` per D-271-ADVERSARIAL-02 carry.

**Invocation pattern:** SEQUENTIAL_MAIN_CONTEXT — orchestrator (main context) dispatches the 3 skills one after another, each receiving this same prompt body verbatim. Per D-296-INVOKE-01 (carry of D-284-ADVERSARIAL-SCOPE-01 + v35 Phase 265 documented experience: skills do not run cleanly in subagent contexts; main orchestrator context is the required invocation site). The "PARALLEL via single message" phrasing of the v41 P284 charge is preserved in name only; practical execution sequences the 3 skill invocations.

---

## Context

You are reviewing the v42.0 milestone audit subject for the Degenerus Protocol. The audit subject is three independent surface changes plus one gap-closure amendment, all landed against the v41 closure baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`.

**Working tree note (UPDATED mid-sweep):** uncommitted `retryLootboxRng()` addition in `contracts/modules/DegenerusGameAdvanceModule.sol` is now IN-SCOPE as Hypothesis (xiv) below per user request mid-sweep. Skills must produce a disposition for it under the same 6-rubric format. Other 13 hypotheses (i)..(xiii) remain unchanged.

The three audit-subject surfaces and the gap-closure amendment:

1. **MINTCLN — Phase 290 commit `<MINTCLN-commit>` against `contracts/modules/DegenerusGameMintModule.sol` + `contracts/storage/DegenerusGameStorage.sol`.** Mint-batch event-signature cleanup. `_raritySymbolBatch` collapses from 6-positional (4-input keccak) to 5-positional (3-input keccak); per-group seed becomes `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` where `baseKey = (uint256(lvl) << 224) | (queueIdx << 192) | (uint256(uint160(player)) << 32) | uint256(owed)`. The 4th-arg `ownedSalt` is collapsed into the low 32 bits of `baseKey`. `TraitsGenerated` event transitions from 6-field shape `(address indexed player, uint24 indexed level, uint32 queueIdx, uint32 startIndex, uint32 count, uint256 entropy)` to 3-field shape `(address indexed player, uint256 baseKey, uint32 take)` — BREAKING topic-hash from `0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9` to `0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3` per D-42N-EVT-BREAK-01 (inherits v40 D-40N-EVT-BREAK-01 disposition; pre-launch posture; no live indexer impact).
   - Bytecode delta: **-81 bytes** runtime (`contracts/modules/DegenerusGameMintModule.sol`: 16223 vs 16304 baseline). Storage layout `forge inspect storageLayout` diff vs v41 close = **EMPTY** (both module + storage targets byte-identical; 169 lines module / 171 lines storage). Public ABI byte-identity preserved: `processFutureTicketBatch` selector `0x9103766f` UNCHANGED; `processTicketBatch` selector `0x2ff3118b` UNCHANGED; `_processOneTicketEntry` private signature UNCHANGED (body restructured; `rollSalt` collapsed into `baseKey`). Cross-call seed separation across multi-call drains on the same `(rk, player)` pair preserved via the `baseKey`-carrying-owed shape: when the outer slot is re-entered with smaller `owed`, `baseKey` low 32 bits change correspondingly. References: `.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md` + `290-01-DESIGN-INTENT-TRACE.md`.

2. **HRROLL — Phase 292 commit `a0218952` against `contracts/modules/DegenerusGameJackpotModule.sol`.** Hero-override weighted-roll restructure. v41 `_topHeroSymbol(uint32 day)` (deterministic max-amount scan; first-seen-wins on ties) is REMOVED. v42 `_rollHeroSymbol(uint32 day, uint256 entropy)` (private view; weighted roll with `×1.5` leader bonus on the maximum-amount slot) is INTRODUCED. `_applyHeroOverride` signature extends from `(uint8[4], uint256)` to `(uint8[4], uint256, uint256)` — gains a 3rd `uint256 heroEntropy` parameter; sole callsite is `_rollWinningTraits` at L1988 post-patch, which passes raw `randWord` (NOT post-bonus-tag `r`) as the 3rd argument per D-42N-BONUS-ENTROPY-01.
   - Algorithm shape per D-42N-DETERMINISM-01: pass 1 scans `idx = 0..31` (`q = idx >> 3`, `s = idx & 7`); SLOADs 4 packed slots once; extracts 32 × uint32 amounts; accumulates `total` and tracks `(maxAmount, leaderIdx)` via strict-`>` (first-seen wins on ties); builds the flat `uint32[32]` cache per D-42N-CACHE-01. Early-bail if `total == 0`. Then `leaderBonus = maxAmount / 2`; `effectiveTotal = total + leaderBonus`; `pick = uint64(uint256(keccak256(abi.encode(heroEntropy, day))) % effectiveTotal)` (NOT `abi.encodePacked`). Pass 2 walks the cache ascending; cumulative += weight; if `idx == leaderIdx` cumulative += leaderBonus; first cumulative `>` pick wins.
   - Storage `forge inspect storageLayout` diff vs v41 close = **EMPTY** (`dailyHeroWagers[uint32 => uint256[4]]` + `dailyIdx` UNCHANGED). Public ABI `forge inspect methodIdentifiers` diff vs v41 close = **EMPTY** (10/10 public selectors UNCHANGED; private-signature changes on `_applyHeroOverride` + new private `_rollHeroSymbol` + removed `_topHeroSymbol` do not count). Theoretical worst-case gas delta vs v41 baseline: **~+431 gas** (under the D-42N-GAS-01 soft acceptance threshold of +500; well below the +10K ESCALATION-CHECKPOINT bound). D-42N-COLOR-ENTROPY-01 cross-RNG-consumer non-collision: HRROLL symbol-roll keccak output lives in a separate keccak-hash domain — NOT in the raw `randWord` bit register — so collision with existing bit-slice consumers (jackpot-path-select bits[0..12]; lootbox-Bernoulli bits[152..167]; jackpot-Bernoulli bits[200..215]; color-sample bits `quadrant*3` of `r`) is **structurally impossible**, NOT probabilistic. References: `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` + `292-01-DESIGN-INTENT-TRACE.md`.

3. **DPNERF — Phase 294 commit `47936e0c` against `contracts/modules/DegenerusGameJackpotModule.sol`, plus BURNIE gap-closure amendment commit `38319463`.** Deity-pass gold-tier nerf. Inside `_randTraitTicket` (L1707-L1763) at L1731-L1737, the existing `if (deity != address(0))` block branches on `((trait >> 3) & 7) == 7`: gold (`color == 7`) → `virtualCount = 1` (flat); commons (`color ∈ [0..6]`) → `virtualCount = max(len/50, 2)` (UNCHANGED from v41). Same gold-tier branch is also inline-duplicated at `_awardDailyCoinToTraitWinners` L1867-L1874 — the BURNIE near-future coin jackpot path is NOT a caller of `_randTraitTicket` (architecturally incompatible signature: multi-bucket / 1-winner-per-iteration sampler vs single-bucket / N-winner aggregated return); commit `38319463` added the parallel +4/-2 source delta at L1867-L1874 mirroring the L1731-L1737 patch.
   - Storage `forge inspect storageLayout` diff vs v41 close = **EMPTY** at both module + storage targets (171-line byte-identical). Public ABI `forge inspect methodIdentifiers` diff vs v41 close = **EMPTY** (10/10 public selectors UNCHANGED; `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` selector `0xdbedb1c1` UNCHANGED). Runtime bytecode delta: **+86 bytes** vs Phase 292 close (24,503 vs 24,417); USER-ACCEPTED per Phase 294 Task 5 disposition (probable cause: via_ir Yul-IR optimizer reshuffle around the introduced branch); deployment within EIP-170 with ~73 bytes headroom. Per-call runtime gas ~20-50 gas. Function-body-level zero-new-state grep-proof: SSTORE-equivalent + SLOAD-equivalent counts identical pre/post-patch. References: `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` + `294-01-DESIGN-INTENT-TRACE.md` + `294-02-SUMMARY.md`.

`_randTraitTicket` is reached from 4 callsites (ETH paths): (1) L698 `_runEarlyBirdLootboxJackpot` (early-bird lootbox jackpot trait winners; 3% of `futurePrizePool` at `lvl+1`; 100 winners / 4 traits); (2) L988 `_distributeTicketsToBucket` (helper invoked by `_distributeTicketJackpot` from L637 daily-tickets / L652 carryover-tickets / L883 early-bird-post-purchase-tickets); (3) L1296 `_processDailyEth` (daily ETH jackpot trait winners; routed through `_runJackpotEthFlow` L1142); (4) L1399 `_resolveTraitWinners` (ETH trait-winner resolution sub-flow). The BURNIE path resolves via `payDailyCoinJackpot` L1773 → `_awardDailyCoinToTraitWinners` L1822 → inline-duplicate at L1867-L1874. Per D-295-CALLSITE-SCOPE-01, callsites 1 + 2 are NOT covered by TST-DPNERF-01..05 regression fixtures — Phase 296 SWEEP attests their behavior adversarially per D-294-CALLER-UNIFORM-01 SWEEP-scope expansion.

---

## Charge — red-team the audit subject

Walk the **13 hypothesis surfaces** (i)..(xiii) below. Per hypothesis, return one of these dispositions:

- **SAFE** — verified safe via the cited evidence; no adversarial vector.
- **SAFE_BY_DESIGN** — intentional/documented design that closes the surface.
- **SAFE_BY_STRUCTURAL_CLOSURE** — surface closed by structural property (state-machine gate, atomic state, type-system invariant).
- **NEGATIVE_RESULT_ONLY** — searched the vector, found nothing exploitable; document the negative result.
- **ACCEPTED_DESIGN** — design tradeoff identified; not a finding (e.g., intentional EV reduction as documented; user-accepted bytecode delta).
- **FINDING_CANDIDATE** — vector exists; describe the finding + suggested remediation (DO NOT propose contract changes; just describe for user review).

**Required output format per hypothesis:**

```markdown
## Hypothesis (X) — [short title]

**Disposition:** SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN / FINDING_CANDIDATE

**Evidence:**
- [line citations, control-flow trace, storage-write inventory, etc.]

**Notes:**
- [reasoning, edge cases considered, alternative attack vectors explored]
```

If FINDING_CANDIDATE → include a **Description** block (the bug at v42 close), a **Severity estimate** (per D-08 5-Bucket Severity Rubric: CRITICAL / HIGH / MEDIUM / LOW / INFO), and a **Suggested remediation** block (descriptive only — no contract code).

---

## Hypothesis Surfaces

### Group I — MINTCLN core (Phase 290 audit-subject; SWEEP-02(i))

#### Hypothesis (i) — Does the 3-input hash re-introduce a different determinism break?

Multi-call drains on the same `(rk, player)` pair must produce pairwise-distinct keccak inputs across batches so the `_raritySymbolBatch` LCG seeds remain pairwise-distinct (the v41 F-41-01 finding closed under v41 Phase 281 owed-salt; v42 MINTCLN collapses that 4-input shape to 3-input with `owed` packed into `baseKey` low 32 bits). **Question:** under any adversarial trace, do two `_raritySymbolBatch` invocations within a single drain or across successive drains land on identical `(baseKey, entropyWord, groupIdx)` triples? Consider: (a) a player who triggers `_resolveZeroOwedRemainder` (`mint:731-757`) inside `_processOneTicketEntry` such that `baseKey` is constructed at `mint:800-802` with stale `owed == 0` while the helper subsequently rolls `owed` to 1 at the local scope — does the next invocation in the same outer-loop iteration carry stale low-32 bits? (b) a player who manipulates `ticketsOwedPacked[rk][player]` between calls via a queue-time bump (mint-boost path or autorebuy) that lands MID-DRAIN — does the bumped `owed` collide with a previous batch's `baseKey` low-32 bits? (c) any path where `owed_at_call_entry == 0` makes the low 32 bits of `baseKey` non-distinct from a prior batch?

- **Evidence anchors:** `contracts/modules/DegenerusGameMintModule.sol:426-429` (Path A `baseKey` construction); `:469-471` (Path A `_raritySymbolBatch` + `TraitsGenerated` emit); `:763-766` (Path B `baseKey`); `:793-794` (Path B); `:534-643` (`_raritySymbolBatch` body; seed at `:572` post-patch); `:731-757` (`_resolveZeroOwedRemainder` zero-owed → rolled-to-1 path). Storage SSTOREs touching `ticketsOwedPacked[rk][player]`: 5 callsites in `processFutureTicketBatch` + `_processOneTicketEntry` + `_resolveZeroOwedRemainder` (byte-identical SSTORE inventory vs v41 close).
- **Expected disposition class:** SAFE_BY_STRUCTURAL_CLOSURE pending skill confirmation (the changing low-32 bits of `baseKey` mirror the v41 owed-salt mechanism by construction; section (i) of `290-01-DESIGN-INTENT-TRACE.md` records the pre-emptive answer).
- **Cross-cite:** `290-01-DESIGN-INTENT-TRACE.md` §(i) original 4-input hash rationale + SWEEP-02(i) Hypothesis 1 pre-emptive answer; carry-forward decision anchor D-281-FIX-SHAPE-01 (v41 Phase 281 owed-salt reference pattern).

#### Hypothesis (ii) — Does `owed` packed into `baseKey` open any new griefing on shape collision?

`baseKey` layout post-MINTCLN-02 is `(lvl << 224) | (queueIdx << 192) | (player << 32) | owed`. The four components occupy non-overlapping bit ranges (lvl bits 224-255; queueIdx bits 192-223; player bits 32-191; owed bits 0-31). **Question:** for any pair of distinct `(lvl, queueIdx, player, owed)` tuples, can two collide on `baseKey`? Consider: (a) `queueIdx` is typed `uint256` at the construction sites (`mint:424` + `mint:801`) but reserved 32 bits in the layout — what if `queueIdx >= 2^32` is reachable in practice? (b) player addresses occupy 160 bits at offset 32; can a craftable address collide into the `queueIdx` or `lvl` ranges? (c) the zero-owed branch at `_resolveZeroOwedRemainder` constructs `baseKey` with `owed == 0` in low 32 bits — does another invocation in the same `(lvl, queueIdx, player)` triple with `owed > 0` deterministically produce a distinct `baseKey` that the same player cannot grief?

- **Evidence anchors:** `contracts/modules/DegenerusGameMintModule.sol:424` + `:801` (`baseKey` construction with `uint256(queueIdx) << 192` and `uint256(owed)` in low bits); `:646-658` (`_rollRemainder` consumer of `baseKey` via `EntropyLib.hash2(entropy, baseKey, rem)`); `:443` + `:489` + `:746` + `:824` (4 `_rollRemainder` callsites); `:443` Path A pre-take, `:489` Path A post-take; `:746` `_resolveZeroOwedRemainder`; `:824` Path B post-take.
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (bit-range layout is non-overlapping by construction; `ticketCursor` is `uint32` so `queueIdx < 2^32` structurally; player addresses fit in 160 bits and cannot overflow into queueIdx).
- **Cross-cite:** `290-01-DESIGN-INTENT-TRACE.md` §(i) + SWEEP-02(i) Hypothesis 2 pre-emptive answer.

#### Hypothesis (iii) — Does the breaking topic-hash on `TraitsGenerated` create a parsing-ambiguity vector for any caller decoding the event?

v41 topic-0 hash `0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9` (`TraitsGenerated(address,uint24,uint32,uint32,uint32,uint256)`) is retired at v42 close. v42 topic-0 hash `0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3` (`TraitsGenerated(address,uint256,uint32)`) is new. **Question:** is there any indexer / decoder / caller / fallback handler reading `TraitsGenerated` emissions that could mis-parse the new shape as the old shape (or vice versa) given the topic-hash drift? Consider: (a) decoders matching by partial topic or by event name string rather than topic-hash — does any production tool do so? (b) decoders maintaining a "deprecated topics" map for pre-launch historical reads — is the indexer rebuild story confirmed (no live state to preserve)? (c) any other consumer reading the emit-time payload size (4 indexed/non-indexed fields × 32 bytes was 128 B LOGDATA; v42 is 2 fields × 32 B = 64 B LOGDATA + 2 topics vs 3 topics) — could the size shift confuse a fixed-buffer parser? (d) any in-contract or cross-module consumer of `TraitsGenerated` (grep `contracts/` for any internal `emit TraitsGenerated` reader)?

- **Evidence anchors:** `contracts/storage/DegenerusGameStorage.sol:484-491` (v41 6-field event decl) → post-patch (3-field decl); `contracts/modules/DegenerusGameMintModule.sol:471` (Path A emit) + `:794` (Path B emit); LOG opcode delta: `LOG3 → LOG2` (`-1 topic × 375 = -375 gas`; `-64 B data × 8 = -512 gas`; total ~-893 gas per emit). Topic hashes verifiable via `cast keccak "TraitsGenerated(address,uint24,uint32,uint32,uint32,uint256)"` and `cast keccak "TraitsGenerated(address,uint256,uint32)"`.
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (pre-launch posture per D-42N-EVT-BREAK-01; no live indexer; mutually-exclusive topic hashes by keccak avalanche; v40 D-40N-EVT-BREAK-01 operational template).
- **Cross-cite:** `290-01-DESIGN-INTENT-TRACE.md` §(iii) breaking-topic-hash justification + indexer-migration handoff + SWEEP-02(i) Hypothesis 3 pre-emptive answer.

---

### Group II — HRROLL core (Phase 292 audit-subject; SWEEP-02(ii))

#### Hypothesis (iv) — Does ×1.5 leader bonus open whale-coordination or wash-trading MEV?

`leaderBonus = maxAmount / 2`; `effectiveTotal = total + leaderBonus`. The leader's effective weight in the cumulative-sum roll is `1.5 × maxAmount`. **Question:** is there any timing window, mempool-observable bet pattern, or cross-account coordination strategy that lets one or more whales bias the symbol-roll outcome in their favor beyond what the on-chain wager state expresses? Consider: (a) a whale who observes the runner-up wager near the day-rollover boundary and places a final-block bet to vault into leader status — does the symbol-roll entropy delivery atomicity prevent this from being asymmetric-information exploitation? (b) two coordinating whales who alternate leader status on adjacent days to game `dailyIdx` rollover — does any cross-day state read affect the current day's pick? (c) wash-trading via cross-account hero-symbol bet rotation — does the on-chain wager state always reflect committed capital with no rebate path? (d) any MEV-bot reordering of bet-placement transactions within the same block that could change leader identity after `_unlockRng` snapshots wager state?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol` post-patch `_rollHeroSymbol` at L1639+ (signature `(uint32 day, uint256 entropy)`); `_applyHeroOverride` at L1600 + callsite at L1988 in `_rollWinningTraits`; leader-bonus computation `leaderBonus = maxAmount / 2` + `effectiveTotal = total + leaderBonus`. `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501` (wager-time write to `dailyHeroWagers[day][heroQuadrant]`). VRF callback delivers `randWord` atomically at jackpot-resolution time; symbol-roll entropy `keccak256(abi.encode(heroEntropy, day))` unknowable at wager-time.
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (mechanic rewards capital commitment by design; RNG commitment-window argument blocks asymmetric-information attacks per `feedback_rng_commitment_window.md`).
- **Cross-cite:** `292-01-DESIGN-INTENT-TRACE.md` §(ii) leader-bonus magnitude trade-offs + §(iv) RNG commitment-window backward-trace + SWEEP-02(ii) Hypothesis 1 pre-emptive answer; carry-forward decision anchor D-42N-LEADER-BONUS-01.

#### Hypothesis (v) — Does no-floor design open sybil dilution attack (1-wei spam across 32 slots)?

No minimum-wager floor for eligibility per D-42N-FLOOR-01. Every wei of `dailyHeroWagers[day][q]` participates proportionally in the weighted roll via `amount / effectiveTotal`. **Question:** can an attacker dilute organic winners by spamming small bets across all 32 slots? Consider: (a) capital cost analysis — `wagerUnit = totalBet / 1e12` scaling at `placeDegeneretteBet` (`DegenerusGameDegeneretteModule.sol:489`); to deposit 32 × 1 wager-unit (1 unit per slot) costs `32 × 1e12 wei = 3.2e13 wei = 0.032 ETH` of at-risk capital. (b) at realistic daily wager volumes (a single 0.001 ETH bet creates 1000 wager-units), the attacker's 32 wager-unit dilution payload represents <<1% of `effectiveTotal`. (c) does the attacker recover the at-risk capital via any rebate path or via winning their own diluted slot? (d) is there any path-dependent state (e.g., `traitBurnTicket` bucket population, ticket-distribution accumulators) that a sybil-spammer could perturb to amplify the dilution payoff beyond the proportional payout?

- **Evidence anchors:** `contracts/modules/DegenerusGameDegeneretteModule.sol:489` (`wagerUnit = totalBet / 1e12` scaling); `:484-501` (wager-time write merging `updated` into `dailyHeroWagers[day][heroQuadrant]` via shifted-OR; `updated` saturates at `0xFFFFFFFF` on uint32 overflow); `contracts/modules/DegenerusGameJackpotModule.sol` `_rollHeroSymbol` pass-1 total accumulation + pass-2 cumulative walk; no rebate path in the daily-jackpot resolution flow.
- **Expected disposition class:** SAFE_BY_STRUCTURAL_CLOSURE pending skill confirmation (capital-cost argument bounds the dilution payoff structurally; attacker pays full bet price for negligible effect).
- **Cross-cite:** `292-01-DESIGN-INTENT-TRACE.md` §(iii) sybil-exposure trade-offs + SWEEP-02(ii) Hypothesis 2 pre-emptive answer; carry-forward decision anchor D-42N-FLOOR-01.

#### Hypothesis (vi) — Symbol-roll VRF bit-collision with existing entropy consumers?

HRROLL introduces a new RNG consumer at `_rollHeroSymbol`: `pick = uint64(uint256(keccak256(abi.encode(heroEntropy, day))) % effectiveTotal)`, where `heroEntropy == randWord` (raw VRF callback payload, pre-bonus-tag, per D-42N-BONUS-ENTROPY-01). Existing consumers in the v42 entropy register read RAW `randWord` bit-slices: jackpot-path-select bits[0..12]; lootbox-Bernoulli (manual+auto-resolve) bits[152..167]; jackpot-Bernoulli (BAF) bits[200..215]; color-sample bits `quadrant*3` of `r` (where `r = randWord` for regular trait roll; `r = keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` for bonus). **Question:** does the new HRROLL symbol-roll consumer collide (statistically or structurally) with any existing consumer? Consider: (a) keccak avalanche property — does the keccak output domain remain structurally orthogonal to ANY bit-slice of its input `(heroEntropy, day)`? Backward-trace per `feedback_rng_backward_trace.md`: confirm `heroEntropy` source is the raw VRF payload (`_rollWinningTraits` first parameter; UNKNOWABLE pre-VRF). (b) for the bonus path, `r = keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` — does HRROLL's `keccak256(abi.encode(heroEntropy, day))` produce a hash that could correlate with any bit-slice of `r` (note: `abi.encode` vs `abi.encodePacked` domain separation is the v41 P281 idiom); (c) `dailyIdx` is the single-writer day-key (D-288-FIX-SHAPE-01 carry); does the keccak's `day` input read a value that could be ambiguous at consumer time vs writer time?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol` `_rollHeroSymbol` keccak at L1684 (`keccak256(abi.encode(entropy, day))`); `_applyHeroOverride` L1600-L1621 (`heroEntropy` 3rd parameter passed from `_rollWinningTraits` L1988 carrying raw `randWord`); `_rollWinningTraits` L1937-L1939 (`r = isBonus ? keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)) : randWord`); `BONUS_TRAITS_TAG` constant L183 reused at L1755 + L1938; `contracts/modules/DegenerusGameAdvanceModule.sol` `_unlockRng` (sole writer of `dailyIdx` per D-288-FIX-SHAPE-01).
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (keccak output orthogonal to input bit-slices by hash-function design; non-collision is structural NOT probabilistic per D-42N-COLOR-ENTROPY-01).
- **Cross-cite:** `292-01-MEASUREMENT.md` §(3.e) D-42N-COLOR-ENTROPY-01 non-collision attestation + `292-01-DESIGN-INTENT-TRACE.md` §(v) D-42N-DETERMINISM-01 exact algorithm + SWEEP-02(ii) Hypothesis 3 pre-emptive answer; carry-forward decision anchor D-42N-COLOR-ENTROPY-01 + D-288-FIX-SHAPE-01.

#### Hypothesis (vii) — Does the gas regression open a DOS surface?

Theoretical worst-case gas delta vs v41 `_topHeroSymbol` baseline: ~+431 gas per `_rollHeroSymbol` call (under D-42N-GAS-01 soft acceptance of +500 gas; well below +10K ESCALATION-CHECKPOINT). Components: 4 × SLOAD (~8400; cold, jackpot-resolution context) + 2 × 32-iteration loops (~574 + 298) + 1 × keccak (~87) + 1 × MOD + 1 × DIV — no unbounded computation; loop bounds fixed at `idx ∈ [0, 32)`. **Question:** under any adversarial state setup, can `_rollHeroSymbol` (or the jackpot-resolution caller chain that invokes it via `_rollWinningTraits` L1988) be made to consume gas beyond the theoretical bound such that the daily jackpot resolution gets DoS'd? Consider: (a) memory-expansion edge cases — does the flat `uint32[32]` cache shape (D-42N-CACHE-01) produce non-uniform memory growth on any adversarial pre-state? (b) is `_rollWinningTraits` invoked multiple times per day (regular + bonus paths)? Does the per-call overhead compound dangerously? (c) any caller in the jackpot caller chain (`_runJackpotEthFlow`, `_processDailyEth`, `payDailyJackpot`, `payDailyJackpotCoinAndTickets`, `_distributeTicketJackpot`) where `_rollWinningTraits → _applyHeroOverride → _rollHeroSymbol` lands in a writes-budget-bounded inner loop subject to writes-budget overrun? (d) is there a state pre-condition (`dailyHeroWagers[day][q]` patterns; `dailyIdx` values; bet-volume edge cases) that exercises the worst-case branch under adversary control?

- **Evidence anchors:** `292-01-MEASUREMENT.md` §(3.a)..§(3.c) theoretical derivation; v41 baseline ~9494 gas; v42 chosen-shape ~9925 gas (~+431); D-42N-GAS-01 soft threshold +500 / hard upper-bound +750. Phase 293 TST-HRROLL-06 empirical regression target (within ±100 of soft target). Caller chain: `_rollWinningTraits` L1933 → `_applyHeroOverride` L1988 → `_rollHeroSymbol` L1639+. Bonus-path: `_rollWinningTraits` invoked twice on bonus-active days (regular + bonus); `_rollHeroSymbol` invoked per `_applyHeroOverride` call.
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (loop bounds fixed; no unbounded computation; theoretical worst-case bounded). Apply `feedback_gas_worst_case.md` discipline.
- **Cross-cite:** `292-01-MEASUREMENT.md` §(3) + `292-01-DESIGN-INTENT-TRACE.md` §(v) gas-headroom analysis + SWEEP-02(ii) Hypothesis 4 pre-emptive answer; carry-forward decision anchor D-42N-GAS-01.

---

### Group III — DPNERF core (Phase 294 audit-subject; SWEEP-02(iii))

#### Hypothesis (viii) — Intentional EV reduction secondary attacks (deity owners pivot to non-gold strategies destabilizing commons)?

D-42N-DEITY-EV-01 locks the gold-tier deity virtual entries to flat 1 (no scaling, no floor); commons (`color ∈ [0..6]`) retain `virtualCount = max(len/50, 2)` UNCHANGED. Deity-pass holders earn strictly less total EV across all 8 colors than at v41 close; no commons-tier compensation. **Question:** does the intentional EV reduction shift deity-pass holder behavior in ways that secondarily destabilize the commons-tier player-economy? Consider: (a) deity-pass holders pivoting to common-color betting strategies — does the increased deity-pass presence in commons-tier buckets perturb the `virtualCount = max(len/50, 2)` per-bucket EV expectation for organic non-deity holders? (b) secondary-market deity-pass price equilibrium adjustment — does the reduced EV expectation produce a price collapse cascade that affects other game mechanics relying on deity-pass ownership signals (`DegenerusGameBoonModule.sol` boon distribution; `DegenerusGameWhaleModule.sol` pricing; `BP_DEITY_PASS_TIER_SHIFT` flow)? (c) deity-pass holders timing their pass acquisition / disposal across daily-jackpot cycles to capture commons-tier EV while ducking gold-tier nerf — is there a temporal arbitrage window? (d) does the gold-tier nerf interact adversely with the BURNIE near-future coin jackpot path's multi-bucket sampler shape (different per-pull probability arithmetic vs `_randTraitTicket` 25-winner draw)?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol:1731-1737` (ETH path gold-tier branch); `:1867-1874` (BURNIE path inline-duplicate gold-tier branch per commit `38319463`); `_randTraitTicket` L1707-L1763 (ETH 25-winner draw shape); `_awardDailyCoinToTraitWinners` L1822-L1913 (BURNIE 1-winner-per-pull shape); `contracts/modules/DegenerusGameDegeneretteModule.sol:854-859` (gold-tier extraction idiom `color == 7`); deity-EV math examples in `294-01-DESIGN-INTENT-TRACE.md` §(ii): `len=30` gold v42 `1/31 ≈ 3.23%` vs v41 `2/32 ≈ 6.25%`.
- **Expected disposition class:** SAFE_BY_INTENT pending skill confirmation (intentional EV reduction is the user-locked outcome; secondary-market price equilibrium adjustment is accepted as a feature of the nerf, not a bug). Apply economic-analyst persona's actor-walk methodology.
- **Cross-cite:** `294-01-DESIGN-INTENT-TRACE.md` §(ii) + §(iii) + SWEEP-02(iii) Hypothesis 3 pre-emptive answer; carry-forward decision anchor D-42N-DEITY-EV-01.

#### Hypothesis (ix) — ETH↔BURNIE both-paths differential-behavior exploitation?

The same gold-tier branch shape is applied at two surfaces: `_randTraitTicket` L1731-L1737 (ETH path, single-bucket / N-winner aggregated return) and `_awardDailyCoinToTraitWinners` L1867-L1874 (BURNIE path, multi-bucket / 1-winner-per-pull sampler). The arithmetic shapes differ (`virtualCount = 1` is the same in both; surrounding sampler logic differs). **Question:** does any attacker-observable behavioral asymmetry exist between the ETH and BURNIE paths that could be gamed? Consider: (a) deity-sentinel emission shape differs: ETH sets `winners[i] = deity; ticketIndexes[i] = type(uint256).max` at `_randTraitTicket` L1755-L1757; BURNIE sets `winner = deity; ticketIdx = type(uint256).max` and emits `JackpotBurnieWin(winner, lvlPrime, trait, amount, ticketIdx)` at `_awardDailyCoinToTraitWinners` L1888-L1893 + event emission L1899-L1905 — does the event-emission asymmetry leak any timing or ordering information? (b) per-pull randomness derivation differs across paths — ETH per-winner-i `idx = uint256(keccak256(abi.encode(randomWord, trait, salt, i))) % effectiveLen`; BURNIE per-pull does similar arithmetic but iterates over `lvlPrime` ∈ [minLevel, maxLevel] then samples — is the per-bucket entropy commitment-window identical? (c) BURNIE `coinBudget` flows differ from ETH `ethPool` flows — does any deity-pass holder behavior asymmetry leverage the different prize-currency economics?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol:1707-1763` (`_randTraitTicket` ETH path); `:1755-1757` (ETH deity-sentinel pair); `:1822-1913` (`_awardDailyCoinToTraitWinners` BURNIE path); `:1864-1867` + `:1867-1874` (BURNIE inline-duplicate gold-tier branch from commit `38319463`); `:1888-1893` (BURNIE deity-sentinel pair); `:1899-1905` (`JackpotBurnieWin` event emit). `payDailyCoinJackpot(uint24,uint256,uint24,uint24)` at L1773-L1796 (BURNIE external entry).
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (gold-tier branch produces identical `virtualCount` semantics on both surfaces; sentinel-pair invariants are path-symmetric; sampler-shape asymmetry is architectural and preserves the deity-EV invariant).
- **Cross-cite:** `294-01-DESIGN-INTENT-TRACE.md` §(iv) Path-Coverage Trade-Offs + SWEEP-02(iii) Hypothesis 4 pre-emptive answer; carry-forward decision anchor D-42N-PATH-COVERAGE-01 + D-294-CALLER-UNIFORM-01 + D-294-BURNIE-INLINE-01.

---

### Group IV — Carry-forward augments (cross-surface; recorded post-original-roadmap-authoring)

#### Hypothesis (x) — BURNIE inline-duplicate vs ETH differential at L1867-L1874 (commit `38319463`) vs L1731-L1737 — does it break D-294-CALLER-UNIFORM-01 path-uniformity in any subtle way?

The BURNIE gap-closure amendment (commit `38319463`) added the gold-tier branch at `_awardDailyCoinToTraitWinners` L1867-L1874 as a parallel +4/-2 source delta mirroring the `_randTraitTicket` L1731-L1737 patch. The two sites are textually parallel but live in architecturally distinct sampler shapes. **Question:** apply grep-verified call-graph attestation per `feedback_verify_call_graph_against_source.md` (Phase 294 BURNIE gap precedent is the load-bearing prior). Does any subtle differential exist between the two sites that breaks the by-construction uniformity Phase 294 claimed? Consider: (a) sentinel-pair invariants — ETH writes `winners[i] = deity; ticketIndexes[i] = type(uint256).max`; BURNIE writes `winner = deity; ticketIdx = type(uint256).max` then emits via event; are the invariants algebraically equivalent? (b) `virtualCount` arithmetic — both sites set `virtualCount = 1` on gold and `virtualCount = max(len/50, 2)` on commons; is the surrounding `effectiveLen = len + virtualCount` computation byte-identical at both sites? (c) deity-selection probability — does the BURNIE multi-bucket sampler produce identical per-pull deity-selection probability (`1/(len+1)` on gold; `max(len/50, 2)/(len + max(len/50, 2))` on commons) as the ETH N-winner draw at the bucket level? (d) any state-dependent perturbation in BURNIE that the ETH path lacks (e.g., `coinBudget` underflow handling; `lvlPrime` boundary effects)?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol:1731-1737` (ETH gold-tier branch); `:1867-1874` (BURNIE gold-tier branch from commit `38319463`); `:1755-1757` vs `:1888-1893` (sentinel-pair sites); `_randTraitTicket` L1707-L1763 single-bucket / N-winner shape vs `_awardDailyCoinToTraitWinners` L1822-L1913 multi-bucket / per-pull shape. Phase 294 `294-02-SUMMARY.md` (BURNIE gap-closure amendment context — commits `38319463` BURNIE extend + `6d117c80` re-verify + `a5493122` ROADMAP+STATE complete-mark).
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation (commit `38319463` was authored specifically to preserve path-uniformity after Phase 294 Plan 02 verification proved the planner's "by construction" claim wrong; D-294-BURNIE-INLINE-01 records the corrected disposition).
- **Cross-cite:** `294-02-SUMMARY.md` carry-forward notes (hypothesis (x) explicit hand-forward) + `294-01-DESIGN-INTENT-TRACE.md` §(iv) corrected 5-surface enumeration + `feedback_verify_call_graph_against_source.md`; carry-forward decision anchor D-294-CALLER-UNIFORM-01 + D-294-BURNIE-INLINE-01 + D-42N-PATH-COVERAGE-01.

#### Hypothesis (xi) — DPNERF callsites 1 (L698) + 2 (L988) production-path coverage gap per D-295-CALLSITE-SCOPE-01 — do they exhibit any structural behavior that breaks the D-294-CALLER-UNIFORM-01 by-construction uniformity argument?

TST-DPNERF-01..05 explicitly cover `_randTraitTicket` callsites 3 (L1296 `_processDailyEth`) + 4 (L1399 `_resolveTraitWinners`) + the BURNIE path (L1867 inline-duplicate). Callsites 1 (L698 `_runEarlyBirdLootboxJackpot`) + 2 (L988 `_distributeTicketsToBucket`) are NOT explicitly covered per D-295-CALLSITE-SCOPE-01 — Phase 296 SWEEP is the attestation site. **Question:** apply grep-verified call-graph attestation per `feedback_verify_call_graph_against_source.md`. Does either callsite exhibit any state-dependent behavior (caller's setup of `traitBurnTicket[lvl][trait]` buckets; `numWinners` parameter values; `salt` parameter values; `randomWord` source provenance; bucket-population invariants) that breaks the uniformity claim? Consider: (a) Callsite 1 L698 `_runEarlyBirdLootboxJackpot` runs at `lvl+1` distributing 3% of `futurePrizePool` to 100 winners across 4 traits (25/trait) — is the per-trait bucket size in the early-bird window structurally different from the daily-ETH bucket size such that the gold-tier nerf produces different effective EV per call? (b) Callsite 2 L988 `_distributeTicketsToBucket` is invoked by `_distributeTicketJackpot` from THREE upstream paths (L637 daily-tickets / L652 carryover-tickets / L883 early-bird-post-purchase-tickets) — does any of the 3 upstream paths set up `traitBurnTicket` buckets in a way that differs from the L1296 / L1399 callsites that ARE covered by TST-DPNERF? (c) Does the carryover-ticket path's level-crossing behavior (tickets from level N enter the level-N+1 bucket distribution) leak un-nerfed gold EV? (d) Does the early-bird-post-purchase path's bucket-distribution timing relative to `_unlockRng` produce any commitment-window asymmetry vs the daily-ETH path?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol:698` (`_runEarlyBirdLootboxJackpot` `_randTraitTicket` callsite 1); `:988` (`_distributeTicketsToBucket` `_randTraitTicket` callsite 2); `:1296` (`_processDailyEth` `_randTraitTicket` callsite 3, TST-DPNERF covered); `:1399` (`_resolveTraitWinners` `_randTraitTicket` callsite 4, TST-DPNERF covered); `:637` + `:652` + `:883` (3 upstream callers of `_distributeTicketJackpot`); `:1707-1763` `_randTraitTicket` body (function-body change reaches all 4 callsites uniformly by construction per D-294-CALLER-UNIFORM-01).
- **Expected disposition class:** SAFE_BY_STRUCTURAL_UNIFORMITY pending skill confirmation (single function-body change is the SOLE delivery mechanism; all 4 callers inherit by construction; coverage gap is documentation / regression-fixture-coverage gap, NOT a behavioral-divergence gap).
- **Cross-cite:** `295-CONTEXT.md` D-295-CALLSITE-SCOPE-01 + `294-02-SUMMARY.md` carry-forward notes (callsites 1+2 explicit hand-forward to Phase 296) + `294-01-DESIGN-INTENT-TRACE.md` §(iv) + SWEEP-02(iii) Hypothesis 1 + Hypothesis 2 pre-emptive answers; carry-forward decision anchor D-294-CALLER-UNIFORM-01 + D-42N-PATH-COVERAGE-01 + D-295-CALLSITE-SCOPE-01.

#### Hypothesis (xii) — MINTCLN owed-in-baseKey collapse vs v41 Phase 281 D-281-FIX-SHAPE-01 owed-salt reference pattern

v41 Phase 281's owed-salt approach passed `uint32 ownedSalt` as a 6th positional argument to `_raritySymbolBatch`, mixing into the per-group keccak input as `keccak256(abi.encode(baseKey, entropyWord, groupIdx, ownedSalt))`. v42 MINTCLN collapses the shape to 5-positional / 3-input `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` with `owed` packed into `baseKey` low 32 bits. The structural invariant carries forward via `baseKey` low-32-bit variation across multi-call drains. **Question:** apply backward-trace per `feedback_rng_backward_trace.md`. Does the new packing structurally re-introduce the v40 collision class that v41 Phase 281 closed, or produce a different equivalence-class break? Consider: (a) the v40 baseline (pre-Phase-281) collision class was within-day determinism break across multi-call drains on the same `(rk, player)` pair when `owed > writesBudget / 2` — does the new 3-input shape preserve pairwise-distinct keccak inputs across batches for the SAME equivalence class? (b) is there an adversarial trace where the v41 owed-salt invariant held but the v42 owed-in-baseKey invariant fails (e.g., a path where `_processOneTicketEntry` `_resolveZeroOwedRemainder` zero-owed → rolled-to-1 branch leaves `baseKey` with stale `owed == 0` in low 32 bits at the `_raritySymbolBatch` call — per `290-01-DESIGN-INTENT-TRACE.md` §(ii) note 2)? (c) does any OTHER consumer of `baseKey` (the `_rollRemainder` callers at `mint:443` + `:489` + `:746` + `:824` hashing through `EntropyLib.hash2(entropy, baseKey, rem)`) inherit a different equivalence-class break from the `owed`-carrying-baseKey shape? (d) re-test the v41 owed-salt invariant verbatim at v42 — does any path that v41 attestation classified as SAFE break under the v42 packing?

- **Evidence anchors:** `contracts/modules/DegenerusGameMintModule.sol:426-429` + `:763-766` (v42 `baseKey` construction sites packing `owed` into low 32 bits); `:572` post-patch seed `keccak256(abi.encode(baseKey, entropyWord, groupIdx))`; `:534-643` `_raritySymbolBatch` body; `:646-658` `_rollRemainder` (consumer of `baseKey` via `EntropyLib.hash2`); `:443` + `:489` + `:746` + `:824` 4 `_rollRemainder` callsites; `:731-757` `_resolveZeroOwedRemainder` zero-owed → rolled-to-1 path with stale `baseKey` disposition documented in `290-01-DESIGN-INTENT-TRACE.md` §(ii) (routed to Phase 296 SWEEP for adversarial re-pass). v41 Phase 281 reference shape: 4-input keccak `(baseKey, entropyWord, groupIdx, ownedSalt)` at v41 close (carry-forward decision anchor D-281-FIX-SHAPE-01).
- **Expected disposition class:** SAFE_BY_STRUCTURAL_CLOSURE pending skill confirmation (v41 owed-salt invariant is preserved via the `baseKey` low-32-bit mechanism; section (i) of `290-01-DESIGN-INTENT-TRACE.md` records the pre-emptive answer; section (ii) of the same trace explicitly hands the zero-owed branch SAFE_BY_STRUCTURAL_CLOSURE expectation forward to Phase 296 SWEEP-02(i)).
- **Cross-cite:** `290-01-DESIGN-INTENT-TRACE.md` §(i) + §(ii) (zero-owed branch routed to SWEEP) + SWEEP-02(i) Hypothesis 1 pre-emptive answer; carry-forward decision anchor D-281-FIX-SHAPE-01 (v41 Phase 281 owed-salt reference pattern).

#### Hypothesis (xiii) — HRROLL leader-bonus + rngLocked window interaction

The new symbol-roll consumer at `_rollHeroSymbol(uint32 day, uint256 entropy)` uses `keccak256(abi.encode(entropy, day))`. The `entropy` parameter carries raw `randWord` per D-42N-BONUS-ENTROPY-01; `day` carries `dailyIdx` (single-writer single-reader invariant from D-288-FIX-SHAPE-01 v41 Phase 288 carry). **Question:** apply commitment-window check per `feedback_rng_commitment_window.md`. Does the leader-bonus computation (`leaderBonus = maxAmount / 2`) or the pass-1 leader tracking (strict-`>` first-seen-wins; produces `leaderIdx`) read any player-controllable state between VRF request and fulfillment that could open a leader-manipulation vector during the rngLocked window? Consider: (a) `dailyHeroWagers[day][q]` write site at `placeDegeneretteBet` (`DegenerusGameDegeneretteModule.sol:484-501`) — is the write site gated to BEFORE VRF request (rngLocked window opens AT VRF request? AT VRF callback?) or can a wager-write land BETWEEN VRF request and callback? (b) `dailyIdx` mutation at `_unlockRng` (`DegenerusGameAdvanceModule.sol`) — does any state transition between VRF request and fulfillment cause `dailyIdx` to drift or wagerers to write to a DIFFERENT day's hero-wager slot? (c) the rngLocked window state machine — what player-callable functions remain accessible during the window? Do any of them write `dailyHeroWagers[day][q]` for the day whose pool will be consumed by the in-flight VRF? (d) cross-day boundary races at JACKPOT_RESET_TIME — does a wager placed near the day rollover land in the OLD day's wager pool or the NEW day's, and does the rngLocked window correctly gate this?

- **Evidence anchors:** `contracts/modules/DegenerusGameJackpotModule.sol` `_rollHeroSymbol` body L1639+ (pass-1 SLOAD of `dailyHeroWagers[day][q]` × 4; pass-1 leader tracking; pass-2 cursor walk with `leaderBonus` conditional add); `_applyHeroOverride` L1600 + callsite at L1988 (carries `heroEntropy = randWord` raw VRF payload + `day = dailyIdx`); `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501` (wager-time write site); `contracts/modules/DegenerusGameAdvanceModule.sol` `_unlockRng` (sole writer of `dailyIdx`; clears `rngWordCurrent`); `_simulatedDayIndex()` reader sites; rngLocked window state machine. v41 Phase 288 D-288-FIX-SHAPE-01 carry: `dailyIdx` single-writer invariant.
- **Expected disposition class:** SAFE_BY_STRUCTURAL_CLOSURE pending skill confirmation (commitment-window backward-trace in `292-01-DESIGN-INTENT-TRACE.md` §(iv) records: wager-writes for day D commit BEFORE day D+1 VRF callback consumes them; randomness UNKNOWABLE at wager-time; D-288-FIX-SHAPE-01 single-writer invariant preserved verbatim at v42 close).
- **Cross-cite:** `292-01-DESIGN-INTENT-TRACE.md` §(iv) HRROLL-05 RNG commitment-window backward-trace + SWEEP-02(ii) Hypothesis 1 pre-emptive answer + `feedback_rng_commitment_window.md` + `feedback_rng_backward_trace.md`; carry-forward decision anchor D-288-FIX-SHAPE-01 + D-42N-BONUS-ENTROPY-01.

---

### Group V — User-added beyond-charge surface (added mid-sweep)

#### Hypothesis (xiv) — Does `retryLootboxRng()` do what its docstring claims and is it free of exploits?

A new permissionless `retryLootboxRng()` function at `contracts/modules/DegenerusGameAdvanceModule.sol:1132-1155` re-fires a stalled mid-day VRF request after a 6-hour timeout. Comments claim: (a) only reachable when LR_MID_DAY=1 (mid-day buffer swap committed) and VRF callback has not delivered; (b) re-fires VRF with same parameters; (c) stalled requestId auto-rejected via requestId match in `rawFulfillRandomWords`; (d) buffer state and pre-advanced `lootboxRngIndex` preserved so the new word lands in the same bucket. **Question:** verify the function behaves as the docstring claims AND has no exploitable surface. Consider: (a) state preservation correctness — does `LR_INDEX` stay un-advanced, does the buffer-swap state stay intact? (b) stalled-callback auto-rejection mechanism — does the `vrfRequestId` overwrite reliably drop the stalled callback? (c) re-entrancy via the VRF coordinator external call at `:1142` (state updates at `:1153-1154` happen AFTER the external call); (d) interaction with the daily-RNG-flow's `isRetry` shortcut at `_finalizeRngRequest:1615-1617` — what happens if `retryLootboxRng` fires while `rngLockedFlag=true` (no explicit check); (e) griefing surface via permissionless permission + LINK consumption rate; (f) potential stuck-state if daily-flow takes over the in-flight VRF and `_unlockRng` zeroes `rngRequestTime` while LR_MID_DAY remains 1 (retry guard set requires `rngRequestTime != 0`, locking the retry function out).

- **Evidence anchors:** `contracts/modules/DegenerusGameAdvanceModule.sol:1132-1155` (`retryLootboxRng` body); `:1043-1123` (`requestLootboxRng` reference for state-shape comparison); `:1745-1766` (`rawFulfillRandomWords` mid-day vs daily branch); `:1610-1669` (`_finalizeRngRequest` isRetry shortcut at `:1615-1617`); `:1729-1736` (`_unlockRng` clears `rngRequestTime`); `:205-238` (advanceGame mid-day path consuming the buffer); `:1253-1258` (`_finalizeLootboxRng` writes to `lootboxRngWordByIndex[LR_INDEX − 1]`).
- **Expected disposition class:** SAFE_BY_DESIGN pending skill confirmation — function appears to implement its documented purpose correctly with bounded griefing surface; observations on docstring/scope boundary and daily-flow interaction may surface as INFO or MEDIUM.
- **Cross-cite:** Working-tree note at top of this CHARGE. No prior decision anchors — this is a fresh surface added mid-sweep.

---

## Required output per skill

Single message containing 13 H2 sections (`## Hypothesis (i)` through `## Hypothesis (xiii)`), each with Disposition + Evidence + Notes blocks per the format above. If FINDING_CANDIDATE → include Description + Severity estimate + Suggested remediation blocks.

**Hard requirement: NO contract code in your output.** Suggested remediations are descriptive only (per `feedback_never_preapprove_contracts.md` + `feedback_no_contract_commits.md` — only the user can approve contract changes, and the user reviews remediation descriptions before any patch is drafted).

**Hard requirement: NO future-milestone forward-cite tokens.** Phase 297 (the v42.0 terminal phase) owns the forward-cite envelope per the v42 closure invariant. Do not name any post-v42 milestone, version, or release identifier. Use locked-decision IDs + descriptive labels only.

**Beyond-charge entries.** Skills are free to surface beyond-charge hypotheses where they identify a novel attack surface; document each beyond-charge hypothesis with the same disposition rubric (SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN / FINDING_CANDIDATE).

---

## Reference files

You can read these for context (all paths absolute):

- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/modules/DegenerusGameMintModule.sol` (MINTCLN audit surface)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/modules/DegenerusGameJackpotModule.sol` (HRROLL + DPNERF audit surface)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/modules/DegenerusGameAdvanceModule.sol` (rngLocked window + `_unlockRng` semantics)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/modules/DegenerusGameDegeneretteModule.sol` (`placeDegeneretteBet` wager-write site; gold-tier idiom precedent)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/storage/DegenerusGameStorage.sol` (storage byte-identity reference)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/DegenerusTraitUtils.sol` (gold-color semantic confirmation)
- `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/DegenerusDeityPass.sol` (deity-pass NFT)
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-02-SUMMARY.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/295-dpnerf-regression-fixture-tst-dpnerf/295-CONTEXT.md`
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/milestones/v41.0-phases/284-delta-audit-findings-consolidation-terminal/284-ADVERSARIAL-CHARGE.md` (v41 P284 shape precedent)
- `/home/zak/Dev/PurgeGame/degenerus-audit/.planning/milestones/v41.0-phases/284-delta-audit-findings-consolidation-terminal/284-01-ADVERSARIAL-LOG.md` (v41 P284 integrated LOG shape)

---

*Phase: 296-cross-surface-adversarial-sweep-sweep*
*Adversarial-charge: 13 hypothesis surfaces (i)..(xiii) per D-296-CHARGE-01 = 9 SWEEP-02 verbatim + 4 carry-forward augments*
*3-skill SEQUENTIAL_MAIN_CONTEXT dispatch per D-296-INVOKE-01 (carry of D-284-ADVERSARIAL-SCOPE-01); D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry; degen-skeptic OUT OF SCOPE per D-271-ADVERSARIAL-02*
*Two-tier consensus rule applies at integration per D-296-CONSENSUS-01; RE-PASS scope candidate-fix-only per D-296-REPASS-SCOPE-01*
*Beyond-charge entries permitted; each MUST use the same 6-disposition rubric*
