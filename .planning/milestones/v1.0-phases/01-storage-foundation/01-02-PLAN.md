---
phase: 01-storage-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/DegenerusGameEndgameModule.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/interfaces/IDegenerusGame.sol
  - test/fuzz/StorageFoundation.t.sol
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "forge clean && forge build succeeds with zero errors after all 96 references migrated to shim calls"
    - "No direct references to nextPrizePool or futurePrizePool remain in any contract file (except NatSpec comments in shims)"
    - "_getPrizePools/_setPrizePools round-trip uint128 boundary values correctly"
    - "_getPendingPools/_setPendingPools round-trip uint128 boundary values correctly"
    - "_tqWriteKey != _tqReadKey for same level input, for both ticketWriteSlot values"
    - "_swapTicketSlot reverts when read slot has entries, succeeds when drained"
    - "_swapAndFreeze activates freeze and zeros pending accumulators on first freeze; preserves accumulators on re-freeze"
    - "_unfreezePool merges pending into live pools, clears freeze flag; no-op when not frozen"
  artifacts:
    - path: "test/fuzz/StorageFoundation.t.sol"
      provides: "Test harness + unit tests for STOR-01 through STOR-04 plus swap/freeze/unfreeze"
      min_lines: 150
  key_links:
    - from: "all module files"
      to: "_legacyGet/SetNextPrizePool, _legacyGet/SetFuturePrizePool"
      via: "shim function calls replacing direct variable access"
      pattern: "_legacy(Get|Set)(Next|Future)PrizePool"
    - from: "test/fuzz/StorageFoundation.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "StorageHarness inherits DegenerusGameStorage, exposes internals"
      pattern: "StorageHarness is DegenerusGameStorage"
---

<objective>
Migrate all 96 nextPrizePool/futurePrizePool references across 10 consumer files to use the compatibility shims from Plan 01, then create unit tests verifying all Phase 1 storage primitives.

Purpose: Plan 01 replaced the variable declarations but left 96 references broken across 10 files. This plan restores compilation and then proves correctness of every STOR requirement via dedicated unit tests.

Output: Full project compiles (`forge build` succeeds). `test/fuzz/StorageFoundation.t.sol` passes all tests.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-storage-foundation/01-CONTEXT.md
@.planning/phases/01-storage-foundation/01-RESEARCH.md
@.planning/phases/01-storage-foundation/01-01-SUMMARY.md
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
<!-- From Plan 01, the following shims exist in DegenerusGameStorage.sol: -->

Compatibility shims (all internal, DEPRECATED):
  function _legacyGetNextPrizePool() internal view returns (uint256);
  function _legacySetNextPrizePool(uint256 val) internal;
  function _legacyGetFuturePrizePool() internal view returns (uint256);
  function _legacySetFuturePrizePool(uint256 val) internal;

New helper functions (all internal):
  function _setPrizePools(uint128 next, uint128 future) internal;
  function _getPrizePools() internal view returns (uint128 next, uint128 future);
  function _setPendingPools(uint128 next, uint128 future) internal;
  function _getPendingPools() internal view returns (uint128 next, uint128 future);
  function _tqWriteKey(uint24 level) internal view returns (uint24);
  function _tqReadKey(uint24 level) internal view returns (uint24);
  function _swapTicketSlot(uint24 purchaseLevel) internal;
  function _swapAndFreeze(uint24 purchaseLevel) internal;
  function _unfreezePool() internal;

Constants:
  uint24 internal constant TICKET_SLOT_BIT = 1 << 23;
  error E();

New Slot 1 fields:
  uint8 internal ticketWriteSlot;       // Slot 1, offset 24
  bool internal ticketsFullyProcessed;  // Slot 1, offset 25
  bool internal prizePoolFrozen;        // Slot 1, offset 26

Packed pool variables:
  uint256 internal prizePoolsPacked;       // Slot 3 (was nextPrizePool)
  uint256 internal prizePoolPendingPacked; // Slot 16 (was futurePrizePool)

Existing mappings (used by swap helper):
  mapping(uint24 => address[]) internal ticketQueue;

<!-- Reference counts per file (96 total across 10 files, excluding 5 in Storage itself): -->
<!-- DegenerusGame.sol: 22 refs -->
<!-- DegenerusGameJackpotModule.sol: 35 refs -->
<!-- DegenerusGameAdvanceModule.sol: 11 refs -->
<!-- DegenerusGameWhaleModule.sol: 6 refs -->
<!-- DegenerusGameDecimatorModule.sol: 6 refs -->
<!-- DegenerusGameEndgameModule.sol: 5 refs -->
<!-- DegenerusGameGameOverModule.sol: 4 refs -->
<!-- DegenerusGameDegeneretteModule.sol: 3 refs -->
<!-- DegenerusGameMintModule.sol: 2 refs -->
<!-- IDegenerusGame.sol: 2 refs (interface function sigs - leave as-is, they don't reference the variable) -->

<!-- Test infrastructure: -->
<!-- foundry.toml: test = "test/fuzz" (tests must be in test/fuzz/ to be discovered by default forge test) -->
<!-- Existing test pattern: test/fuzz/DeployCanary.t.sol imports from test/fuzz/helpers/DeployProtocol.sol -->
<!-- ContractAddresses requires constructor args - harness needs dummy addresses -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 96 nextPrizePool/futurePrizePool references to shim calls</name>
  <files>contracts/DegenerusGame.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameEndgameModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameMintModule.sol</files>
  <action>
  Migrate all direct `nextPrizePool` and `futurePrizePool` variable references to use the compatibility shims added in Plan 01. There are 96 references across 10 files (excluding the 5 in DegenerusGameStorage.sol which Plan 01 already handled).

  **Migration rules:**

  READ patterns:
  - `nextPrizePool` (in expressions, comparisons, returns) -> `_legacyGetNextPrizePool()`
  - `futurePrizePool` (in expressions, comparisons, returns) -> `_legacyGetFuturePrizePool()`

  WRITE patterns:
  - `nextPrizePool = value` -> `_legacySetNextPrizePool(value)`
  - `futurePrizePool = value` -> `_legacySetFuturePrizePool(value)`

  COMPOUND ASSIGNMENT patterns (most dangerous -- must decompose):
  - `nextPrizePool += share` -> `_legacySetNextPrizePool(_legacyGetNextPrizePool() + share)`
  - `nextPrizePool -= share` -> `_legacySetNextPrizePool(_legacyGetNextPrizePool() - share)`
  - `futurePrizePool += share` -> `_legacySetFuturePrizePool(_legacyGetFuturePrizePool() + share)`
  - `futurePrizePool -= share` -> `_legacySetFuturePrizePool(_legacyGetFuturePrizePool() - share)`

  SPECIAL: For lines that read BOTH nextPrizePool and futurePrizePool in the same expression (e.g., `nextPrizePool + futurePrizePool`), each reference becomes its own shim call.

  **LEAVE ALONE:**
  - `contracts/interfaces/IDegenerusGame.sol` -- the 2 references are interface function signatures (`futurePrizePoolView`, `futurePrizePoolTotalView`). These are function NAMES, not variable references. Do not change them.
  - Any reference inside NatSpec comments (`///`) -- leave as-is for documentation clarity.

  **Process:**
  Work through each file systematically. Start with the highest-reference-count files (JackpotModule: 35, DegenerusGame: 22) since errors there are most impactful.

  After migrating each file, check that it has zero remaining `nextPrizePool` or `futurePrizePool` identifiers (excluding comments and the interface file).

  After ALL files migrated, run `forge clean && forge build`. It MUST succeed with zero errors.

  **Verification grep** (should return zero results excluding comments and interface):
  ```bash
  grep -rn 'nextPrizePool\|futurePrizePool' contracts/ --include='*.sol' | grep -v '///' | grep -v 'IDegenerusGame' | grep -v '_legacy' | grep -v 'DegenerusGameStorage'
  ```
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge clean && forge build 2>&1 | tail -5 && echo "---GREP CHECK---" && grep -rn 'nextPrizePool\|futurePrizePool' contracts/ --include='*.sol' | grep -v '///' | grep -v 'IDegenerusGame' | grep -v '_legacy' | grep -v 'DegenerusGameStorage' | grep -v 'prizePoolsPacked' | head -5 || echo "CLEAN: no stale references"</automated>
  </verify>
  <done>All 96 references migrated to shim calls. forge clean && forge build succeeds. Grep for direct nextPrizePool/futurePrizePool access returns zero non-comment, non-shim results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test harness and unit tests for field placement and packed pool helpers</name>
  <files>test/fuzz/StorageFoundation.t.sol</files>
  <behavior>
    - testSlot1FieldOffsets: ticketWriteSlot at Slot 1 offset 24, ticketsFullyProcessed at offset 25, prizePoolFrozen at offset 26
    - testPackedPoolSlotsUnshifted: prizePoolsPacked at Slot 3, prizePoolPendingPacked at Slot 16, ticketQueue at Slot 17
    - testPrizePoolPackingRoundTrip: (0,0), (max,0), (0,max), (max,max), (12345,67890) all round-trip
    - testPendingPoolPackingRoundTrip: same pattern for pending pools
    - testTicketSlotKeysDiffer: writeKey != readKey for same level, for both ticketWriteSlot values
    - testTicketSlotKeyBit23: correct bit-23 behavior for both ticketWriteSlot=0 and ticketWriteSlot=1
  </behavior>
  <action>
  Create `test/fuzz/StorageFoundation.t.sol` (in test/fuzz/ to be discovered by default `forge test`).

  1. Define a `StorageHarness` contract that inherits `DegenerusGameStorage`. Since DegenerusGameStorage likely has no constructor but inherits ContractAddresses, check what ContractAddresses requires. If it has hardcoded constants (not constructor params), the harness can be simple. If it needs constructor args, provide dummy addresses.

  The harness must expose:
  - `exposed_setPrizePools(uint128, uint128)` calling `_setPrizePools`
  - `exposed_getPrizePools()` calling `_getPrizePools`
  - `exposed_setPendingPools(uint128, uint128)` calling `_setPendingPools`
  - `exposed_getPendingPools()` calling `_getPendingPools`
  - `exposed_tqWriteKey(uint24)` calling `_tqWriteKey`
  - `exposed_tqReadKey(uint24)` calling `_tqReadKey`
  - `exposed_swapTicketSlot(uint24)` calling `_swapTicketSlot`
  - `exposed_swapAndFreeze(uint24)` calling `_swapAndFreeze`
  - `exposed_unfreezePool()` calling `_unfreezePool`
  - Direct public getters/setters for `ticketWriteSlot`, `ticketsFullyProcessed`, `prizePoolFrozen`
  - `pushToTicketQueue(uint24 key, address addr)` to push entries for swap revert tests

  2. Define `StorageFoundationTest is Test` that deploys `StorageHarness` in setUp().

  **STOR-01 tests (field placement via assembly sload):**
  - `testSlot1FieldOffsets`: Use `vm.store` or harness setters to set each field individually, then load Slot 1 via `vm.load(address(harness), bytes32(uint256(1)))` and check the correct byte offset. For ticketWriteSlot=1 at offset 24: the value 1 should appear at byte position 24 in the 32-byte word.
  - `testPackedPoolSlotsUnshifted`: Store a known value at Slot 3 via `vm.store`, read `prizePoolsPacked` via harness, confirm match. Same for Slot 16 / prizePoolPendingPacked. Confirm ticketQueue mapping root is Slot 17 by checking `vm.load` at slot 17 is consistent.

  **STOR-02 tests (prize pool packing):**
  - `testPrizePoolPackingZero`: Set (0, 0), get back (0, 0)
  - `testPrizePoolPackingMaxNext`: Set (type(uint128).max, 0), get back correctly
  - `testPrizePoolPackingMaxFuture`: Set (0, type(uint128).max), get back correctly
  - `testPrizePoolPackingMaxBoth`: Set (max, max), get back correctly
  - `testPrizePoolPackingArbitrary`: Set (12345, 67890), get back correctly

  **STOR-03 tests (pending pool packing):**
  - `testPendingPoolPackingRoundTrip`: Same 5 cases as STOR-02 but for pending pools

  **STOR-04 tests (key encoding):**
  - `testTicketSlotKeysDifferSlot0`: With ticketWriteSlot=0, assert writeKey(5) != readKey(5)
  - `testTicketSlotKeysDifferSlot1`: With ticketWriteSlot=1, assert writeKey(5) != readKey(5)
  - `testTicketSlotKeyBit23Slot0`: With ticketWriteSlot=0, writeKey(5)==5, readKey(5)==5|TICKET_SLOT_BIT
  - `testTicketSlotKeyBit23Slot1`: With ticketWriteSlot=1, writeKey(5)==5|TICKET_SLOT_BIT, readKey(5)==5
  - `testTicketSlotKeyMultipleLevels`: Test levels 0, 1, 100, 8388607 for both slot values
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/fuzz/StorageFoundation.t.sol --match-test "testSlot1|testPacked|testPrizePool|testPending|testTicketSlot" -vvv 2>&1 | tail -30</automated>
  </verify>
  <done>All STOR-01 through STOR-04 tests pass. Field placement verified via sload. Packed pool helpers round-trip boundary values. Key encoding produces divergent keys for same input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add swap, freeze, and unfreeze behavior tests</name>
  <files>test/fuzz/StorageFoundation.t.sol</files>
  <behavior>
    - testSwapTicketSlotSuccess: empty read queue -> swap succeeds, ticketWriteSlot toggles, ticketsFullyProcessed resets
    - testSwapTicketSlotRevertsNonEmpty: read queue has entries -> revert E()
    - testSwapTicketSlotDoubleToggle: two swaps return ticketWriteSlot to 0
    - testSwapAndFreezeActivates: sets prizePoolFrozen=true, zeros pending accumulators
    - testSwapAndFreezeAlreadyFrozen: swap succeeds, pending accumulators preserved (not zeroed)
    - testUnfreezePoolMerges: pending applied to live, pending zeroed, freeze cleared
    - testUnfreezePoolNoop: no-op when not frozen
  </behavior>
  <action>
  Add to the existing `StorageFoundationTest` contract in `test/fuzz/StorageFoundation.t.sol`:

  **Swap tests:**
  - `testSwapTicketSlotSuccess`: Ensure ticketWriteSlot starts at 0 (default). Call `exposed_swapTicketSlot(0)` with empty read queue. Assert ticketWriteSlot is now 1. Set ticketsFullyProcessed=true before swap, assert false after.
  - `testSwapTicketSlotRevertsNonEmpty`: Push an address into `ticketQueue[readKey(0)]` via the harness `pushToTicketQueue`. Call `exposed_swapTicketSlot(0)` and expect revert with `vm.expectRevert(StorageHarness.E.selector)`.
  - `testSwapTicketSlotDoubleToggle`: Swap twice (both with empty queues). ticketWriteSlot should be back to 0.

  **Freeze tests:**
  - `testSwapAndFreezeActivates`: Set some pending pool values (100, 200) first via exposed setter. Call `exposed_swapAndFreeze(0)`. Assert prizePoolFrozen == true. Assert pending pools are (0, 0) because freeze zeroed them.
  - `testSwapAndFreezeAlreadyFrozen`: Set prizePoolFrozen=true directly and set pending pools to (100, 200). Call `exposed_swapAndFreeze(0)`. Assert prizePoolFrozen still true. Assert pending pools are still (100, 200) -- NOT zeroed because already frozen.

  **Unfreeze tests:**
  - `testUnfreezePoolMerges`: Set live pools to (1000, 2000) and pending to (100, 200). Set prizePoolFrozen=true. Call `exposed_unfreezePool()`. Assert live pools are (1100, 2200). Assert pending pools are (0, 0). Assert prizePoolFrozen == false.
  - `testUnfreezePoolNoop`: Set live pools to (1000, 2000). Ensure prizePoolFrozen=false. Call `exposed_unfreezePool()`. Assert live pools unchanged at (1000, 2000).

  Run full test file: `forge test --match-path test/fuzz/StorageFoundation.t.sol -vvv`

  Then run the full test suite to confirm no regressions: `forge test`
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/fuzz/StorageFoundation.t.sol -vvv 2>&1 | tail -30</automated>
  </verify>
  <done>All swap/freeze/unfreeze tests pass. Revert on non-empty read slot confirmed. Freeze activation zeros accumulators, re-freeze preserves them. Unfreeze merges pending into live and clears flag. Full forge test suite passes with zero regressions.</done>
</task>

</tasks>

<verification>
Full phase verification after all tasks complete:
1. `forge clean && forge build` -- zero errors across entire codebase
2. `forge test --match-path test/fuzz/StorageFoundation.t.sol -vvv` -- all unit tests pass
3. `forge test` -- full test suite passes (no regressions)
4. `forge inspect DegenerusGameStorage storage-layout --json` -- final layout matches expectations
5. `grep -rn 'nextPrizePool\|futurePrizePool' contracts/ --include='*.sol' | grep -v '///' | grep -v 'IDegenerusGame' | grep -v '_legacy' | grep -v 'DegenerusGameStorage' | grep -v 'prizePoolsPacked'` -- returns zero results
</verification>

<success_criteria>
- forge build succeeds with zero errors
- All unit tests in StorageFoundation.t.sol pass
- Tests cover: field offsets (STOR-01), packed pool round-trips (STOR-02, STOR-03), key encoding invariants (STOR-04), swap revert/success, freeze activation/persistence, unfreeze merge
- No direct nextPrizePool/futurePrizePool references remain outside shims and comments
- Full forge test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-foundation/01-02-SUMMARY.md`
</output>
