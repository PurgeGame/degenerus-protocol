---
phase: 87-other-jackpots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-other-jackpots-earlybird-finaldgnrs.md
autonomous: true
requirements:
  - OJCK-01
  - OJCK-05
  - OJCK-06

must_haves:
  truths:
    - "The early-bird lootbox jackpot trigger condition (jackpot Day 1) is documented with file:line from AdvanceModule and JackpotModule"
    - "The 3% futurePrizePool allocation, deduction, and nextPrizePool recycling are traced with file:line"
    - "The 100-winner loop, EntropyLib.entropyStep RNG derivation, traitId selection, and level offset roll are documented with file:line"
    - "The _randTraitTicket call with traitId = uint8(entropy) (values 0-255 for 32 traits) is analyzed for modular bias"
    - "The final-day DGNRS trigger condition (jackpotCounter >= 5) is documented with file:line from AdvanceModule"
    - "The 1% Reward pool source, solo bucket derivation, and _randTraitTicket winner selection are traced with file:line"
    - "Every claim about early-bird or final-day DGNRS in prior audit docs is cross-referenced and confirmed or flagged as [DISCREPANCY]"
  artifacts:
    - path: "audit/v4.0-other-jackpots-earlybird-finaldgnrs.md"
      provides: "Early-bird lootbox and final-day DGNRS jackpot audit with file:line citations"
      contains: "## 1. Early-Bird Lootbox Jackpot"
  key_links:
    - from: "audit/v4.0-other-jackpots-earlybird-finaldgnrs.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "file:line citations for _runEarlyBirdLootboxJackpot (JM:801-864) and awardFinalDayDgnrsReward (JM:773-798)"
      pattern: "JM:\\d+"
    - from: "audit/v4.0-other-jackpots-earlybird-finaldgnrs.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "file:line citations for early-bird trigger (AM:381 area) and final-day DGNRS trigger (AM:365)"
      pattern: "AM:\\d+"
---

<objective>
Audit the early-bird lootbox jackpot and final-day DGNRS distribution mechanics with exhaustive file:line citations. Both mechanisms live in JackpotModule.sol and are triggered during level transitions via AdvanceModule.

Purpose: These two jackpot types are the simplest of the "other jackpots" but share the JackpotModule contract and _randTraitTicket winner selection. Auditing them together provides a baseline for the trait-based winner selection pattern reused across other jackpot types.

Output: `audit/v4.0-other-jackpots-earlybird-finaldgnrs.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-other-jackpots/87-RESEARCH.md

Key contract files to trace:
- contracts/modules/DegenerusGameJackpotModule.sol (early-bird: JM:801-864, final-day DGNRS: JM:773-798, _randTraitTicket, constants)
- contracts/modules/DegenerusGameAdvanceModule.sol (trigger orchestration: early-bird at ~AM:381, final-day DGNRS at AM:365)
- contracts/storage/DegenerusGameStorage.sol (lastDailyJackpotWinningTraits, futurePrizePool, nextPrizePool, traitBurnTicket storage)
- contracts/DegenerusGame.sol (delegatecall wrappers if any)

Prior audit docs for cross-reference:
- audit/v4.0-ticket-creation-queue-mechanics.md (ticket queuing paths, _queueTickets behavior)
- audit/v3.9-rng-commitment-window-proof.md (RNG dependency context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit early-bird lootbox jackpot mechanics (OJCK-01)</name>
  <files>audit/v4.0-other-jackpots-earlybird-finaldgnrs.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Create the audit document `audit/v4.0-other-jackpots-earlybird-finaldgnrs.md` with the following structure. Begin with a header and phase/requirement metadata.

**Section 1: Early-Bird Lootbox Jackpot (OJCK-01)**

Trace the complete early-bird lootbox jackpot mechanism:

1.1 **Trigger path**: Starting from advanceGame in AdvanceModule, trace the exact call chain to `_runEarlyBirdLootboxJackpot`. Document:
   - The condition that determines "jackpot Day 1" (isEarlyBirdDay check around JM:381)
   - The call from payDailyJackpot into `_runEarlyBirdLootboxJackpot(lvl, rngWord)` at JM:801
   - Exact file:line for the trigger condition, the function entry point, and the call site

1.2 **Prize pool source**: Trace the 3% allocation from futurePrizePool:
   - `_getFuturePrizePool()` read at JM:803
   - `totalBudget = pool * EARLYBIRD_LOOTBOX_BPS / 10_000` (verify the BPS constant value and location)
   - futurePrizePool deduction at JM:807
   - `perWinnerEth = totalBudget / 100` integer division (document dust: up to 99 wei)
   - nextPrizePool addition at JM:863 (budget recycling)

1.3 **Winner selection loop (100 iterations)**:
   - Loop bounds at JM:829
   - `EntropyLib.entropyStep(entropy)` RNG derivation at JM:830
   - `traitId = uint8(entropy)` at JM:831 -- analyze this: values 0-255 for 32 traits (0-31). Does _randTraitTicket handle traitId >= 32? Check the function body to determine behavior (likely returns 0 / no winner for out-of-range traits, meaning ~87.5% of iterations find no winner)
   - `_randTraitTicket(traitBurnTicket[lvl], entropy, traitId, 1, uint8(i))` at JM:832-838
   - Winner zero check and level offset roll (0-4, 20% each) at JM:843
   - Ticket count derivation from perWinnerEth / level price at JM:846
   - `_queueTickets(winner, baseLevel + levelOffset, ticketCount)` at JM:848

1.4 **Storage reads/writes**: Table of every storage variable read or written, with file:line.

1.5 **RNG dependency**: Document that rngWord comes from VRF via advanceGame. Note that rngLockedFlag is true during this call but _queueTickets routes to _tqWriteKey for near-future levels (not FF), so the rngLocked guard does NOT trigger.

1.6 **Audit findings**: Flag any discrepancies with prior audit documentation. Specifically check:
   - Whether the traitId modular bias is documented anywhere in prior audits
   - Whether the 100-winner loop with ~87.5% miss rate is documented
   - Whether the dust from integer division is documented
  </action>
  <verify>
    <automated>grep -c "JM:" audit/v4.0-other-jackpots-earlybird-finaldgnrs.md | xargs test 15 -le</automated>
  </verify>
  <acceptance_criteria>
    - File audit/v4.0-other-jackpots-earlybird-finaldgnrs.md exists
    - Contains "## 1. Early-Bird Lootbox Jackpot" section header
    - Contains file:line citations matching pattern "JM:\d+" at least 15 times
    - Contains file:line citations matching pattern "AM:\d+" at least 2 times
    - Contains "traitId = uint8(entropy)" analysis with modular bias discussion
    - Contains "_runEarlyBirdLootboxJackpot" function name
    - Contains "futurePrizePool" and "nextPrizePool" in prize pool flow
    - Contains "EntropyLib.entropyStep" RNG derivation trace
    - Contains "_queueTickets" ticket creation trace
    - If any discrepancy found, contains "[DISCREPANCY]" or "[NEW FINDING]" tag with severity
  </acceptance_criteria>
  <done>Early-bird lootbox jackpot fully traced from trigger through prize allocation, 100-winner loop, trait selection, ticket queuing, and budget recycling -- all with file:line citations</done>
</task>

<task type="auto">
  <name>Task 2: Audit final-day DGNRS distribution mechanics (OJCK-05)</name>
  <files>audit/v4.0-other-jackpots-earlybird-finaldgnrs.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Append to the existing audit document created in Task 1.

**Section 2: Final-Day DGNRS Distribution (OJCK-05)**

Trace the complete final-day DGNRS distribution mechanism:

2.1 **Trigger path**: Starting from advanceGame in AdvanceModule, trace the exact call chain:
   - `_awardFinalDayDgnrsReward(lvl, rngWord)` call at AM:365
   - The condition: `jackpotCounter >= JACKPOT_LEVEL_CAP` (verify JACKPOT_LEVEL_CAP = 5 constant and its location)
   - This fires AFTER the last daily jackpot completes at the current level
   - Delegatecall target: `JackpotModule.awardFinalDayDgnrsReward(uint24 lvl, uint256 rngWord)` at JM:773

2.2 **Prize pool source**: Trace the 1% sDGNRS Reward pool allocation:
   - `FINAL_DAY_DGNRS_BPS = 100` constant (verify at JM:173)
   - `reward = dgnrs.poolBalance(IStakedDegenerusStonk.Pool.Reward) * FINAL_DAY_DGNRS_BPS / 10_000` at JM:774-775
   - This is sDGNRS tokens, NOT ETH

2.3 **Winner selection**:
   - Entropy derivation: `rngWord ^ (uint256(lvl) << 192)` at JM:779
   - Solo bucket index: `JackpotBucketLib.soloBucketIndex(entropy)` at JM:780
   - Unpack Day 5 winning traits from `lastDailyJackpotWinningTraits` at JM:781-782
   - Select winner: `_randTraitTicket(traitBurnTicket[lvl], entropy, traitIds[soloIdx], 1, 254)` at JM:784-790
   - Document: what happens if no winner found (winner == address(0))

2.4 **Payout**: If winner found:
   - `dgnrs.transferFromPool(IStakedDegenerusStonk.Pool.Reward, winners[0], reward)` at JM:792-796
   - This is a direct sDGNRS transfer, no lootbox, no auto-rebuy

2.5 **Key dependency**: `lastDailyJackpotWinningTraits` must have been set by Day 5's payDailyJackpot. Trace where it is written (JM:2632 area) and verify it is populated before this function is called in the advanceGame flow.

2.6 **Storage reads/writes**: Table of every storage variable read or written, with file:line.

2.7 **Audit findings**: Flag any discrepancies. Specifically check:
   - Whether the dependency on lastDailyJackpotWinningTraits ordering is documented in prior audits
   - Whether the "no winner" path (traitBurnTicket empty for selected trait) is documented
   - Cross-reference with Phase 81 DSC-02 (sampleFarFutureTickets) -- this function does NOT use sampleFarFutureTickets, but verify

**Section 3: Cross-Cutting Analysis**

3.1 Shared patterns between early-bird and final-day: both use _randTraitTicket, both depend on traitBurnTicket[lvl] population
3.2 Summary table of all findings with severity ratings

Tag all discrepancies with [DISCREPANCY] and new issues with [NEW FINDING] per OJCK-06.
  </action>
  <verify>
    <automated>grep -c "awardFinalDayDgnrsReward\|FINAL_DAY_DGNRS_BPS\|lastDailyJackpotWinningTraits\|soloBucketIndex" audit/v4.0-other-jackpots-earlybird-finaldgnrs.md | xargs test 4 -le</automated>
  </verify>
  <acceptance_criteria>
    - Contains "## 2. Final-Day DGNRS Distribution" section header
    - Contains "awardFinalDayDgnrsReward" function name with file:line citation
    - Contains "FINAL_DAY_DGNRS_BPS" constant reference
    - Contains "lastDailyJackpotWinningTraits" storage dependency analysis
    - Contains "soloBucketIndex" winner selection trace
    - Contains "transferFromPool" payout trace
    - Contains "## 3. Cross-Cutting Analysis" section
    - Contains a findings summary table with severity ratings
    - If any discrepancy found, contains "[DISCREPANCY]" or "[NEW FINDING]" tag
  </acceptance_criteria>
  <done>Final-day DGNRS distribution fully traced from trigger through Reward pool allocation, solo bucket derivation, trait-based winner selection, and sDGNRS transfer -- all with file:line citations. Cross-cutting analysis complete.</done>
</task>

</tasks>

<verification>
- All file:line citations in the audit document are verifiable against the contracts/ directory
- Every storage variable read/write is documented with file:line
- traitId modular bias in early-bird (uint8 for 32 traits) is analyzed
- lastDailyJackpotWinningTraits ordering dependency is verified
- All discrepancies tagged with [DISCREPANCY] or [NEW FINDING] and severity
</verification>

<success_criteria>
- audit/v4.0-other-jackpots-earlybird-finaldgnrs.md exists with 2 main sections + cross-cutting analysis
- At least 20 file:line citations (JM:, AM:, GS: patterns)
- traitId = uint8(entropy) modular bias documented
- Budget recycling (nextPrizePool) documented
- lastDailyJackpotWinningTraits dependency documented
- All findings tagged per OJCK-06
</success_criteria>

<output>
After completion, create `.planning/phases/87-other-jackpots/87-01-SUMMARY.md`
</output>
