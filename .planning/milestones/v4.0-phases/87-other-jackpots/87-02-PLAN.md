---
phase: 87-other-jackpots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-other-jackpots-baf.md
autonomous: true
requirements:
  - OJCK-02
  - OJCK-06

must_haves:
  truths:
    - "The BAF jackpot trigger path from advanceGame through runRewardJackpots to the two-contract system is documented with file:line"
    - "The pool allocation (10% or 20% of futurePrizePool based on level type) is traced with file:line"
    - "All 7 prize distribution slices (10% top BAF, 5% top coinflip, 5% random 3rd/4th, 5%+5% far-future, 45% scatter 1st, 25% scatter 2nd) are documented with file:line from DegenerusJackpots.sol"
    - "The scatter mechanics (50 rounds, level targeting, trait sampling, top-2 selection) are traced with file:line"
    - "The payout processing in EndgameModule (large/small winner threshold, ETH/lootbox split, whale pass) is documented with file:line"
    - "sampleFarFutureTickets using _tqWriteKey (DSC-02 from Phase 81) is confirmed and its impact on BAF fairness is assessed"
    - "The BAF leaderboard state tracking (bafTotals, bafTop, bafEpoch) and cleanup are documented with file:line"
    - "Every claim about BAF mechanics in prior audit docs is cross-referenced and confirmed or flagged as [DISCREPANCY]"
  artifacts:
    - path: "audit/v4.0-other-jackpots-baf.md"
      provides: "BAF jackpot audit covering winner selection, scatter mechanics, and payout processing with file:line citations"
      contains: "## 1. BAF Jackpot Overview"
  key_links:
    - from: "audit/v4.0-other-jackpots-baf.md"
      to: "contracts/DegenerusJackpots.sol"
      via: "file:line citations for runBafJackpot (DJ:229-529), prize slices, scatter mechanics"
      pattern: "DJ:\\d+"
    - from: "audit/v4.0-other-jackpots-baf.md"
      to: "contracts/modules/DegenerusGameEndgameModule.sol"
      via: "file:line citations for _runBafJackpot payout processing (EM:345-423) and runRewardJackpots trigger (EM:168)"
      pattern: "EM:\\d+"
    - from: "audit/v4.0-other-jackpots-baf.md"
      to: "contracts/DegenerusGame.sol"
      via: "file:line citations for sampleFarFutureTickets (DG:2669-2705) and sampleTraitTicketsAtLevel"
      pattern: "DG:\\d+"
---

<objective>
Audit the BAF (Buy and Flip) jackpot mechanics -- the most complex "other jackpot" spanning two contracts: DegenerusJackpots.sol for winner selection and EndgameModule.sol for payout processing. Document the complete prize distribution across 7 slices, scatter mechanics, and payout split logic with exhaustive file:line citations.

Purpose: BAF is the highest-value periodic jackpot (10-20% of futurePrizePool every 10 levels) and the only jackpot that uses an external contract (DegenerusJackpots) for winner selection. Its scatter mechanics involve complex trait sampling across multiple level targets. This audit ensures the distribution logic, leaderboard tracking, and payout processing are all correctly understood.

Output: `audit/v4.0-other-jackpots-baf.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-other-jackpots/87-RESEARCH.md

Key contract files to trace:
- contracts/DegenerusJackpots.sol (runBafJackpot DJ:229-529, recordBafFlip DJ:174, prize distribution slices, scatter mechanics DJ:393-463, _bafTop, _updateBafTop, _clearBafTop)
- contracts/modules/DegenerusGameEndgameModule.sol (_runBafJackpot EM:345-423, runRewardJackpots EM:168-243, large/small winner split)
- contracts/modules/DegenerusGameAdvanceModule.sol (trigger from advanceGame level transition)
- contracts/DegenerusGame.sol (sampleFarFutureTickets DG:2669-2705, sampleTraitTicketsAtLevel)
- contracts/storage/DegenerusGameStorage.sol (bafTotals, bafTop, bafTopLen, bafEpoch, bafPlayerEpoch, lastBafResolvedDay)

Prior audit docs for cross-reference:
- audit/v4.0-ticket-creation-queue-mechanics.md (DSC-02 sampleFarFutureTickets _tqWriteKey issue)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit BAF winner selection in DegenerusJackpots.sol (OJCK-02 part 1)</name>
  <files>audit/v4.0-other-jackpots-baf.md</files>
  <read_first>
    contracts/DegenerusJackpots.sol
    contracts/modules/DegenerusGameEndgameModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/DegenerusGame.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Create the audit document `audit/v4.0-other-jackpots-baf.md` with the following structure. Begin with a header and phase/requirement metadata.

**Section 1: BAF Jackpot Overview**

1.1 **Trigger path**: Trace from advanceGame through level transition:
   - `runRewardJackpots` call in AdvanceModule (find exact line)
   - Condition: `lvl % 10 == 0` in EndgameModule at EM:168 area
   - Pool allocation at EM:180: verify the three tiers (10% standard, 20% level 50, 20% century)
   - External call to `jackpots.runBafJackpot(poolWei, lvl, rngWord)` at EM:363

1.2 **BAF state tracking**: Document the leaderboard system:
   - `recordBafFlip(player, lvl, amount)` at DJ:174 -- called by coin/coinflip contracts
   - `bafTotals[lvl][player]` -- accumulated coinflip stake per player per level
   - `bafTop[lvl]` -- top-4 leaderboard (PlayerScore[4], sorted descending)
   - `bafTopLen[lvl]` -- current leaderboard length
   - `bafEpoch[lvl]` / `bafPlayerEpoch[lvl][player]` -- lazy reset mechanism
   - `_updateBafTop` insertion sort logic -- trace exact file:line

**Section 2: Prize Distribution Slices**

Trace each of the 7 prize slices from `runBafJackpot` (DJ:229-529):

2.1 **Slice A -- Top BAF bettor (10%)**: `_bafTop(lvl, 0)` at DJ:253. Document:
   - How the top bettor is determined (highest bafTotals)
   - What happens if bafTopLen < 1 (no qualifying bettor)
   - The refund path for empty slots

2.2 **Slice A2 -- Top coinflip bettor (5%)**: `coin.coinflipTopLastDay()` at DJ:266. Document:
   - External call to DegenDeathmatch contract
   - How "last day" is determined
   - Refund path if no coinflip activity

2.3 **Slice B -- Random 3rd/4th BAF (5%)**: `_bafTop(lvl, 2 + uint8(entropy & 1))` at DJ:283. Document:
   - RNG selection between slot 2 and 3
   - What happens if bafTopLen < 3 or < 4

2.4 **Slice D -- Far-future ticket holders, 1st draw (5%)**: `degenerusGame.sampleFarFutureTickets(entropy)` at DJ:298. Document:
   - The sampleFarFutureTickets function at DG:2669-2705
   - The _tqWriteKey usage at DG:2681 (confirm DSC-02 from Phase 81)
   - How winners are selected (up to 2 addresses)
   - 3%/2% split between 1st and 2nd sampled address
   - Refund path if no FF tickets exist

2.5 **Slice D2 -- Far-future ticket holders, 2nd draw (5%)**: DJ:340. Document:
   - Same sampling mechanism, separate entropy step
   - Same 3%/2% split

2.6 **Slice E-1st -- Scatter 1st place (45%)**: DJ:382-480. Document the scatter mechanics in detail:
   - 50 rounds (`BAF_SCATTER_ROUNDS` constant at DJ:114, verify value)
   - Level targeting: non-century uses lvl+1 to +4 (weighted), century uses lvl+1/+2/+3 (12 rounds) + random from past 99 levels (38 rounds)
   - `degenerusGame.sampleTraitTicketsAtLevel(targetLvl, entropy)` -- returns up to 4 tickets
   - Top-2 by BAF score selected per round
   - Accumulation into scatter winners array
   - `winnerMask` flags for last 40 scatter winners (`BAF_SCATTER_TICKET_WINNERS = 40`)

2.7 **Slice E-2nd -- Scatter 2nd place (25%)**: DJ:483-498. Document:
   - How 2nd place is derived from same scatter data

2.8 **Return values**: Document the return tuple `(address[] winners, uint256[] amounts, uint256 winnerMask, uint256 returnAmountWei)` and what `returnAmountWei` represents (unawarded refund).

**Section 3: DSC-02 Impact on BAF**

Assess the sampleFarFutureTickets _tqWriteKey issue (DSC-02 from Phase 81) specifically in BAF context:
   - During jackpot phase (post-swap), the write key contains newly-written tickets for next level
   - This means far-future sampling may miss valid ticket holders or sample stale data
   - Assess severity: is this a fairness issue or just a minor distribution skew?

**Section 4: winnerMask Usage Analysis**

Trace whether the winnerMask return value from runBafJackpot is consumed by EndgameModule or is dead code. Check EM:363 and all code after the external call.

Tag all discrepancies with [DISCREPANCY] and new issues with [NEW FINDING] per OJCK-06.
  </action>
  <verify>
    <automated>grep -c "DJ:" audit/v4.0-other-jackpots-baf.md | xargs test 20 -le</automated>
  </verify>
  <acceptance_criteria>
    - File audit/v4.0-other-jackpots-baf.md exists
    - Contains "## 1. BAF Jackpot Overview" section header
    - Contains "## 2. Prize Distribution Slices" section header
    - Contains file:line citations matching pattern "DJ:\d+" at least 20 times
    - Contains file:line citations matching pattern "EM:\d+" at least 8 times
    - Contains all 7 slice labels (Slice A, A2, B, D, D2, E-1st, E-2nd or equivalent)
    - Contains "sampleFarFutureTickets" with _tqWriteKey analysis (DSC-02 assessment)
    - Contains "BAF_SCATTER_ROUNDS" scatter mechanics trace
    - Contains "runBafJackpot" function entry point
    - Contains "winnerMask" usage analysis
    - Contains "recordBafFlip" leaderboard recording trace
  </acceptance_criteria>
  <done>BAF winner selection fully traced through all 7 prize slices, scatter mechanics, leaderboard tracking, and sampleFarFutureTickets DSC-02 impact -- all with file:line citations from DegenerusJackpots.sol</done>
</task>

<task type="auto">
  <name>Task 2: Audit BAF payout processing in EndgameModule and cleanup (OJCK-02 part 2)</name>
  <files>audit/v4.0-other-jackpots-baf.md</files>
  <read_first>
    contracts/modules/DegenerusGameEndgameModule.sol
    contracts/DegenerusJackpots.sol
    contracts/modules/DegenerusGameJackpotModule.sol
  </read_first>
  <action>
Append to the audit document created in Task 1.

**Section 5: BAF Payout Processing (EndgameModule)**

Trace `_runBafJackpot` at EM:345-423:

5.1 **Large/small winner threshold**: `largeWinnerThreshold = poolWei / 20` (5% of pool) at EM:373. Document the threshold calculation.

5.2 **Large winner payout** (amount >= threshold):
   - 50% ETH via `_addClaimableEth(winner, ethPortion, rngWord)` -- trace this function (it includes auto-rebuy logic)
   - 50% lootbox: small amounts via `_awardJackpotTickets`, large amounts via `_queueWhalePassClaimCore`
   - Document the `LOOTBOX_CLAIM_THRESHOLD` that determines small vs large lootbox path
   - Trace the rngWord passthrough for auto-rebuy

5.3 **Small winner payout** (amount < threshold):
   - Even index: 100% ETH via `_addClaimableEth`
   - Odd index: 100% lootbox via `_awardJackpotTickets`
   - Document the alternating pattern

5.4 **Pool accounting**: `lootboxToFuture = lootboxTotal` at EM:419. Document:
   - ETH distributed as claimable: deducted from claimablePool
   - Lootbox ETH: stays in futurePrizePool (backing for lootbox tickets)
   - returnAmountWei: refunded back to futurePrizePool

5.5 **_addClaimableEth auto-rebuy**: Trace the JackpotModule/EndgameModule version of _addClaimableEth (NOT the Degenerette version, which lacks auto-rebuy). Document:
   - Auto-rebuy check
   - claimablePool credit
   - claimableWinnings credit

**Section 6: BAF Cleanup and State Reset**

Trace the cleanup at DJ:526-528:
   - `_clearBafTop(lvl)` -- what this resets
   - Epoch increment at DJ:527 -- how lazy reset works
   - `lastBafResolvedDay` update at DJ:528

**Section 7: Findings Summary**

7.1 Summary table of all findings with columns: ID, Description, Severity, Location
7.2 Assessment of DSC-02 impact on BAF specifically
7.3 winnerMask dead code assessment
  </action>
  <verify>
    <automated>grep -c "EM:" audit/v4.0-other-jackpots-baf.md | xargs test 10 -le</automated>
  </verify>
  <acceptance_criteria>
    - Contains "## 5. BAF Payout Processing" section header
    - Contains "largeWinnerThreshold" with file:line citation
    - Contains "_addClaimableEth" with auto-rebuy trace
    - Contains "_awardJackpotTickets" with file:line
    - Contains "_queueWhalePassClaimCore" with file:line
    - Contains "## 6. BAF Cleanup" section header
    - Contains "_clearBafTop" and "epoch" cleanup trace
    - Contains "## 7. Findings Summary" with severity table
    - Contains file:line citations matching pattern "EM:\d+" at least 10 times total in document
    - If any discrepancy found, contains "[DISCREPANCY]" or "[NEW FINDING]" tag with severity
  </acceptance_criteria>
  <done>BAF payout processing fully traced through large/small winner split, ETH/lootbox routing, pool accounting, auto-rebuy, and cleanup -- all with file:line citations. Complete findings summary with severity ratings.</done>
</task>

</tasks>

<verification>
- All file:line citations in the audit document are verifiable against the contracts/ directory
- All 7 prize distribution slices documented with percentage and recipient
- sampleFarFutureTickets DSC-02 impact assessed in BAF context
- Large/small winner payout split logic traced completely
- winnerMask usage determined (consumed or dead code)
- All discrepancies tagged with [DISCREPANCY] or [NEW FINDING] and severity
</verification>

<success_criteria>
- audit/v4.0-other-jackpots-baf.md exists with 7 sections
- At least 30 file:line citations (DJ:, EM:, DG:, AM: patterns combined)
- All 7 prize slices documented (10%+5%+5%+5%+5%+45%+25% = 100%)
- Scatter mechanics (50 rounds, level targeting, trait sampling) traced
- Payout processing (large/small threshold, ETH/lootbox split) traced
- DSC-02 impact on BAF assessed with severity
- winnerMask usage determined
- All findings tagged per OJCK-06
</success_criteria>

<output>
After completion, create `.planning/phases/87-other-jackpots/87-02-SUMMARY.md`
</output>
