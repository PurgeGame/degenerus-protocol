---
phase: 85-daily-eth-jackpot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-daily-eth-jackpot.md
autonomous: true
requirements:
  - DETH-01
  - DETH-02
  - DETH-05

must_haves:
  truths:
    - "The currentPrizePool BPS allocation table is documented for all 5 logical days (counter 0-4) with exact constant values and file:line citations"
    - "The budget split (ETH vs lootbox, carryover vs main) is documented with the exact BPS values from code"
    - "Phase 0 (current level) and Phase 1 (carryover) behavioral differences are documented in a comparison table with file:line citations for each property"
    - "The early-burn path (purchase phase) is documented as a distinct distribution path with its own BPS and caps"
    - "Compressed mode and turbo mode BPS doubling/stepping are documented"
    - "Every discrepancy found during DETH-01/DETH-02 tracing is tagged with [DISCREPANCY] or [NEW FINDING]"
  artifacts:
    - path: "audit/v4.0-daily-eth-jackpot.md"
      provides: "Daily ETH jackpot audit document with BPS allocation, Phase 0/1 comparison, and early-burn path documentation"
      contains: "## BPS Allocation Table"
  key_links:
    - from: "audit/v4.0-daily-eth-jackpot.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "file:line citations for payDailyJackpot (JM:323-667), constants (JM:104-224), _dailyCurrentPoolBps (JM:2656-2668)"
      pattern: "JM:\\d+"
    - from: "audit/v4.0-daily-eth-jackpot.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "file:line citations for call sites (AM:282, AM:356, AM:363, AM:379)"
      pattern: "AM:\\d+"
---

<objective>
Trace the daily ETH jackpot entry points, BPS allocation table, budget split logic, and Phase 0 vs Phase 1 behavioral differences with exhaustive file:line citations. Document the early-burn path as a distinct distribution mechanism. Flag all discrepancies against prior audit documentation found during the trace.

Purpose: The daily ETH jackpot is the primary value distribution mechanism in the Degenerus Protocol. This plan establishes the document structure and documents the high-level flow, BPS allocations, and phase differences that Plan 02 builds upon for the algorithmic deep-dive.

Output: `audit/v4.0-daily-eth-jackpot.md` -- initial sections covering DETH-01, DETH-02, and partial DETH-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-daily-eth-jackpot/85-RESEARCH.md

Key contract files to trace:
- contracts/modules/DegenerusGameJackpotModule.sol (JM, 2794 lines -- primary)
- contracts/modules/DegenerusGameAdvanceModule.sol (AM -- call sites)
- contracts/storage/DegenerusGameStorage.sol (GS -- storage layout)

Prior audit docs for cross-reference (DETH-05):
- audit/v3.2-findings-39-jackpot-module.md (CMT-V32-001, CMT-V32-002)
- audit/v3.8-commitment-window-inventory.md (Section 1.7)
- audit/PAYOUT-SPECIFICATION.html (PAY-01, PAY-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace payDailyJackpot entry points and BPS allocation table</name>
  <files>audit/v4.0-daily-eth-jackpot.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.2-findings-39-jackpot-module.md
    audit/v3.8-commitment-window-inventory.md
  </read_first>
  <action>
Create `audit/v4.0-daily-eth-jackpot.md` with the following structure, built entirely from reading current Solidity.

**Document header:**
```
# v4.0 Daily ETH Jackpot Audit

**Date:** {date}
**Milestone:** v4.0 (Ticket Lifecycle & RNG-Dependent Variable Re-Audit)
**Phase:** 85 (Daily ETH Jackpot)
**Methodology:** Exhaustive code trace with file:line citations; all prior audit prose treated as unverified
```

**Section 1: Call Sites in advanceGame (DETH-01 partial)**
Document every location in DegenerusGameAdvanceModule.sol that calls into payDailyJackpot or payDailyJackpotCoinAndTickets. The research identifies 4 call sites:
1. Purchase phase early-burn: `payDailyJackpot(false, purchaseLevel, rngWord)` at AM:282
2. Jackpot phase resume: `payDailyJackpot(true, lastDailyJackpotLevel, rngWord)` at AM:356
3. Coin+ticket phase: `payDailyJackpotCoinAndTickets(rngWord)` at AM:363
4. Jackpot phase fresh start: `payDailyJackpot(true, lvl, rngWord)` at AM:379

For each, read the ACTUAL code and document:
- The exact line number (verify against current source, do NOT trust research line numbers blindly)
- The `if` condition that gates the call
- The arguments passed (isDaily, level, rngWord)
- Whether this is a resume or fresh-start invocation

Also document the resume detection condition at AM:350-354 (verify exact lines):
- What storage variables are checked (dailyEthBucketCursor, dailyEthPhase, dailyEthPoolBudget, dailyEthWinnerCursor)
- How the condition determines "is resuming" vs "fresh start"

Create a call site table:
```
| Call Site | Location | Condition | isDaily | Level Arg | Context |
```

**Section 2: Constants and Share Packing (DETH-01)**
Document ALL constants from JM:104-224 that relate to the daily ETH jackpot. Read the actual constant definitions and document:

Timing constants:
- JACKPOT_RESET_TIME -- confirm value and line
- JACKPOT_LEVEL_CAP -- confirm value and line

Share packing constants:
- DAILY_JACKPOT_SHARES_PACKED at JM:114-125 area -- confirm exact line, hex value, unpack to BPS per bucket (expected: 20/20/20/20+remainder)
- FINAL_DAY_SHARES_PACKED -- confirm exact line, hex value, unpack to BPS per bucket (expected: 60/13/13/13+remainder)

Winner caps:
- DAILY_ETH_MAX_WINNERS -- confirm value (expected: 321) and line
- DAILY_CARRYOVER_MIN_WINNERS -- confirm value (expected: 20) and line
- JACKPOT_MAX_WINNERS -- confirm value (expected: 300) and line (used by early-burn, NOT daily)

BPS constants:
- DAILY_REWARD_JACKPOT_LOOTBOX_BPS -- confirm value (expected: 5000) and line
- PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS -- confirm value (expected: 7500) and line
- JACKPOT_SCALE_MAX_BPS -- confirm value (expected: 40000) and line

For each constant, quote the exact Solidity declaration with line number.

**Section 3: BPS Allocation Table (DETH-01)**
Document the complete BPS allocation table by tracing `_dailyCurrentPoolBps` at JM:2656-2668 (verify exact lines):

Read the function and document:
1. How the random BPS is derived: `keccak256(randWord, DAILY_CURRENT_BPS_TAG, counter)` -- confirm the tag constant and modulo range
2. The output range: 600-1400 BPS (6%-14%) for days 1-4
3. Day 5 override: 10000 BPS (100%) when counter >= 4 -- confirm from `payDailyJackpot` at JM:368 area

Also trace compressed mode at JM:372-374 (verify exact lines):
1. When `compressedJackpotFlag == 1`: `counterStep = 2` and dailyBps is doubled
2. When `compressedJackpotFlag == 2` (turbo): `counterStep = 5` and all five logical days happen in one physical day
3. Document the exact code for counterStep packing via `_packDailyTicketBudgets` at JM:2753-2764

Create the allocation table:
```
| Day | Counter | Mode: Normal | Mode: Compressed | Mode: Turbo | Share Packing | Source |
```

**Section 4: Budget Split Logic (DETH-01)**
Trace the fresh-start budget calculation at JM:365-404 (verify exact lines):

1. `dailyBps` calculation: read from `_dailyCurrentPoolBps` or 10000 for final day
2. `budget = currentPrizePool * dailyBps / 10000` -- confirm exact formula and line
3. `lootboxBudget = budget / 5` (20%) -- confirm exact formula and line
4. `ethBudget = budget - lootboxBudget` (80%) -- confirm exact formula and line
5. Lootbox distribution via `_distributeLootboxAndTickets` at JM:1079-1102 -- document what this does with the lootbox budget (ticket conversion, nextPrizePool contribution)
6. Early-bird lootbox on day 1 only: `_runEarlyBirdLootboxJackpot` at JM:801-864 -- document the 3% futurePrizePool source and conditions

For each calculation step, provide the exact Solidity expression and line number.

Create a budget flow diagram:
```
currentPrizePool
  |-- dailyBps% --> budget
       |-- 20% --> lootboxBudget --> _distributeLootboxAndTickets
       |-- 80% --> ethBudget --> _processDailyEthChunk (Phase 0)
  |-- (retained) --> next day's currentPrizePool
```

**Section 5: Cross-Reference for Sections 1-4 (DETH-05 partial)**
After completing Sections 1-4, cross-reference against:
1. `audit/v3.2-findings-39-jackpot-module.md`: Check CMT-V32-001 and CMT-V32-002 -- are these findings still relevant? Quote the prior finding, then state CONFIRMED or [DISCREPANCY] with current code evidence. Research says CMT-V32-002 (inline comment at JM:609) was updated -- verify.
2. `audit/v3.8-commitment-window-inventory.md` Section 1.7: Verify the storage variable R/W claims for payDailyJackpot match current code. Check specifically: currentPrizePool (R/W pattern), jackpotCounter (read-only in payDailyJackpot?), compressedJackpotFlag (read-only?), dailyEthPoolBudget (R/W pattern), dailyEthPhase (R/W pattern), dailyEthBucketCursor (R/W pattern).
3. `audit/PAYOUT-SPECIFICATION.html`: If accessible, verify PAY-01 (purchase-phase daily) and PAY-02 (jackpot-phase daily) BPS values match current code.

Tag all discrepancies found during Sections 1-4 tracing as [DISCREPANCY] or [NEW FINDING] with severity assessment.
  </action>
  <verify>
    <automated>grep -c "JM:\|AM:\|GS:" audit/v4.0-daily-eth-jackpot.md | awk '{if ($1 > 40) print "PASS: " $1 " file:line citations"; else print "FAIL: only " $1 " citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-daily-eth-jackpot.md exists and is non-empty
    - Document contains "## Call Sites in advanceGame" section with a table containing at least 4 call sites (AM:282, AM:356, AM:363, AM:379 or actual verified lines)
    - Document contains "## Constants and Share Packing" section with DAILY_JACKPOT_SHARES_PACKED, FINAL_DAY_SHARES_PACKED, DAILY_ETH_MAX_WINNERS cited with exact lines
    - Document contains "## BPS Allocation Table" section with rows for days 1-5 in normal, compressed, and turbo modes
    - grep for "_dailyCurrentPoolBps" in the document returns at least 2 matches
    - Document contains "## Budget Split Logic" section with the 20/80 lootbox/ETH split documented
    - grep for "lootboxBudget\|ethBudget" in the document returns at least 3 matches
    - Document contains "## Cross-Reference" section with at least one CONFIRMED or [DISCREPANCY] tag
    - grep for "CMT-V32-001\|CMT-V32-002" in the document returns at least 1 match
    - grep for "DETH-01\|DETH-02\|DETH-05" in the document returns at least 3 matches (requirement traceability)
  </acceptance_criteria>
  <done>The payDailyJackpot entry points, BPS allocation table (all 5 days, all 3 modes), budget split logic (20/80, carryover), and early-burn path are documented with exact Solidity citations. All prior audit claims about BPS values and call sites have been cross-referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 0 vs Phase 1 behavior and early-burn path</name>
  <files>audit/v4.0-daily-eth-jackpot.md</files>
  <read_first>
    audit/v4.0-daily-eth-jackpot.md
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Append to `audit/v4.0-daily-eth-jackpot.md` the Phase 0 vs Phase 1 comparison and early-burn path sections.

**Section 6: Phase 0 -- Current Level ETH Distribution (DETH-02)**
Trace the Phase 0 code block at JM:488-556 (verify exact lines). Document:

1. Entry condition: `dailyEthPhase == 0 && dailyEthPoolBudget != 0` -- confirm from code
2. Pool source: `dailyEthPoolBudget` (pre-stored from fresh-start calculation)
3. Target level: current jackpot level (`lvl` parameter)
4. Winner cap: `DAILY_ETH_MAX_WINNERS` (321) -- confirm passed to `_processDailyEthChunk`
5. Entropy derivation: `randWord ^ (uint256(lvl) << 192)` at JM:474 area -- confirm exact formula and line
6. The `_processDailyEthChunk` call at JM:513-526 area -- document all arguments passed
7. On completion: `currentPrizePool -= paidDailyEth` at JM:522 area -- confirm the deduction model
8. Winner cap derivation for carryover at JM:528-543 area: `dailyCarryoverWinnerCap = max(DAILY_ETH_MAX_WINNERS - totalDailyWinners, DAILY_CARRYOVER_MIN_WINNERS)` -- confirm exact formula
9. Transition to Phase 1: `dailyEthPhase = 1` at JM:553 area -- confirm exact line and condition

Quote exact Solidity for each key operation with line numbers.

**Section 7: Phase 1 -- Carryover ETH Distribution (DETH-02)**
Trace the Phase 1 code block at JM:559-606 (verify exact lines). Document:

1. Entry condition: `dailyEthPhase == 1` -- confirm from code
2. Pool source: `dailyCarryoverEthPool` (pre-deducted from futurePrizePool at fresh-start)
3. Target level: `lvl + carryoverSourceOffset` (random future level from `_selectCarryoverSourceOffset`)
4. Winner cap: `dailyCarryoverWinnerCap` (derived at end of Phase 0)
5. Entropy derivation: `randWord ^ (uint256(carryoverSourceLevel) << 192)` -- confirm uses carryover level, not current level
6. Pool deduction model: NO deduction -- pool was pre-deducted from futurePrizePool at JM:430 area. Confirm the return value for paidEth is assigned to `_` (discarded) at JM:590 area.
7. On completion: `_clearDailyEthState()` at JM:604 area -- document what this clears (from JM:2785-2793)
8. Day 1 skip: carryover is skipped on day 1 (counter == 0) -- confirm from code. Instead, day 1 runs `_runEarlyBirdLootboxJackpot`.
9. Skip conditions: `dailyCarryoverEthPool == 0` or `dailyCarryoverWinnerCap == 0` -- confirm from code and document what happens (goes straight to _clearDailyEthState)

Quote exact Solidity for each key operation with line numbers.

**Section 8: Phase 0 vs Phase 1 Comparison Table (DETH-02)**
Create a detailed comparison table covering every property difference. Verify each cell against the actual code traced in Sections 6-7:

```
| Property | Phase 0 (dailyEthPhase=0) | Phase 1 (dailyEthPhase=1) | Source |
|----------|--------------------------|--------------------------|--------|
| Source pool | ... | ... | JM:{line} |
| Target level | ... | ... | JM:{line} |
| Winner cap | ... | ... | JM:{line} |
| Entropy derivation | ... | ... | JM:{line} |
| Share packing | ... | ... | JM:{line} |
| Pool deduction | ... | ... | JM:{line} |
| Triggered when | ... | ... | JM:{line} |
| On completion | ... | ... | JM:{line} |
| Skipped when | ... | ... | JM:{line} |
| Day 1 behavior | ... | ... | JM:{line} |
```

Each row MUST have a JM:{line} citation proving the claim.

**Section 9: Early-Burn Path (DETH-01 + DETH-02)**
Trace the early-burn path (non-daily, purchase phase) at JM:609-667 (verify exact lines). This is called with `isDaily=false` at AM:282. Document:

1. Entry conditions: `!isDaily` AND `jackpotCounter > 0` (not day 1) AND `lvl > 1` -- confirm from code at JM:622-624 area
2. Pool source: `futurePrizePool * 100 / 10000` (1% drip) at JM:629-630 area -- confirm exact formula
3. Lootbox split: `PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS = 7500` (75%) at JM:640 area -- confirm exact value
4. ETH distribution: via `_executeJackpot` at JM:1309-1323 -> `_runJackpotEthFlow` at JM:1326-1351 -> `_distributeJackpotEth` at JM:1512-1551 -- trace this call chain and confirm it is NON-chunked (no cursor resume)
5. Bucket sizing: `JACKPOT_MAX_WINNERS (300)`, `JACKPOT_SCALE_MAX_BPS (40000)` -- confirm these are different from the daily path constants
6. Share packing: `DAILY_JACKPOT_SHARES_PACKED` always (not final day) -- confirm from code

Create a comparison table between early-burn and daily paths:
```
| Property | Early-Burn (Purchase Phase) | Daily (Jackpot Phase) |
|----------|---------------------------|----------------------|
| isDaily arg | false | true |
| Source pool | futurePrizePool 1% | currentPrizePool * dailyBps |
| Lootbox BPS | 7500 (75%) | 2000 (20%) |
| Max winners | 300 | 321 |
| Scale BPS | 40000 | {find from code} |
| Chunked? | No | Yes (cursor resume) |
| Share packing | DAILY_JACKPOT always | FINAL_DAY on day 5 |
```
  </action>
  <verify>
    <automated>grep -c "Phase 0\|Phase 1\|dailyEthPhase\|carryover\|early-burn\|Early-Burn" audit/v4.0-daily-eth-jackpot.md | awk '{if ($1 > 20) print "PASS: " $1 " phase/carryover references"; else print "FAIL: only " $1 " references"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-daily-eth-jackpot.md contains "## Phase 0" section documenting current-level ETH distribution with JM:{line} citations
    - audit/v4.0-daily-eth-jackpot.md contains "## Phase 1" section documenting carryover ETH distribution with JM:{line} citations
    - Document contains Phase 0 vs Phase 1 comparison table with at least 8 rows and a Source column with JM:{line} citations
    - grep for "dailyEthPhase" in the document returns at least 5 matches
    - grep for "currentPrizePool -= paidDailyEth\|paidDailyEth" in the document returns at least 1 match
    - Document contains "## Early-Burn Path" section with PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS cited
    - grep for "JACKPOT_MAX_WINNERS\|DAILY_ETH_MAX_WINNERS" in the document returns at least 2 matches (both caps documented)
    - Early-burn vs daily comparison table has at least 5 rows
    - grep for "_clearDailyEthState" in the document returns at least 1 match
    - grep for "dailyCarryoverWinnerCap\|DAILY_CARRYOVER_MIN_WINNERS" in the document returns at least 2 matches
  </acceptance_criteria>
  <done>Phase 0 and Phase 1 daily ETH distribution paths are fully documented with line-by-line code citations, a comparison table captures every behavioral difference, and the early-burn path is documented as a distinct mechanism with its own BPS and caps.</done>
</task>

</tasks>

<verification>
- `audit/v4.0-daily-eth-jackpot.md` exists with Sections 1-9
- All 4 advanceGame call sites documented with exact conditions and line numbers
- All BPS constants (DAILY_JACKPOT_SHARES_PACKED, FINAL_DAY_SHARES_PACKED, etc.) documented with values
- BPS allocation table covers all 5 days, all 3 modes (normal/compressed/turbo)
- Budget split logic (20/80 lootbox/ETH, carryover 1% drip, early-bird day 1) documented
- Phase 0 and Phase 1 comparison table has 10+ properties with JM:{line} citations in every row
- Early-burn path documented as distinct mechanism with its own parameters
- CMT-V32-001 and CMT-V32-002 cross-referenced against current code
- v3.8 Section 1.7 storage variable R/W patterns verified
</verification>

<success_criteria>
Requirements DETH-01 and DETH-02 are satisfied: the BPS allocation table is complete with all modes and days, the budget split logic is traced from currentPrizePool through lootbox/ETH/carryover paths, Phase 0 vs Phase 1 differences are documented in a verified comparison table, and the early-burn path is documented as a distinct mechanism. DETH-05 is partially satisfied with cross-reference of prior audit claims found during the trace.
</success_criteria>

<output>
After completion, create `.planning/phases/85-daily-eth-jackpot/85-01-SUMMARY.md`
</output>
