---
phase: 91-consolidated-findings-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/KNOWN-ISSUES.md
autonomous: true
requirements:
  - CFND-02
gap_closure: true

must_haves:
  truths:
    - "KNOWN-ISSUES.md v4.0 audit history entry reflects all 8 phases, not just Phase 81"
    - "KNOWN-ISSUES.md correctly states 0 HIGH, 0 MEDIUM, 0 LOW -- all findings are INFO"
    - "DEC-01 and DGN-01 are noted as withdrawn false positives, not listed as active above-INFO findings"
    - "Finding count in the audit history entry is ~47, not 3"
  artifacts:
    - path: "audit/KNOWN-ISSUES.md"
      provides: "Updated v4.0 audit history with full phase coverage"
      contains: "v4.0 Ticket Lifecycle"
  key_links:
    - from: "audit/KNOWN-ISSUES.md"
      to: "audit/v4.0-findings-consolidated.md"
      via: "See reference in audit history entry"
      pattern: "v4\\.0-findings-consolidated\\.md"
---

<objective>
Update KNOWN-ISSUES.md v4.0 audit history entry to reflect the complete milestone findings.

Purpose: The current entry says "3 INFO findings from Phase 81" and lists only DSC-01, DSC-02, DSC-03. The actual v4.0 milestone has ~47 unique INFO findings across 8 phases. Since DEC-01 and DGN-01 are both withdrawn as false positives, there are NO findings above INFO -- so no new entries in the "Intentional Design" or "Design Mechanics" body sections are needed. Only the Audit History entry needs updating.

Output: Updated KNOWN-ISSUES.md with accurate v4.0 audit history entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/91-consolidated-findings-rewrite/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite v4.0 audit history entry in KNOWN-ISSUES.md</name>
  <files>audit/KNOWN-ISSUES.md</files>
  <read_first>
    - audit/KNOWN-ISSUES.md (current state -- the v4.0 entry starts at "### v4.0 Ticket Lifecycle" and lists only 3 findings)
    - audit/v4.0-other-jackpots-decimator.md (lines 790-802 -- confirms DEC-01 is WITHDRAWN/FALSE POSITIVE)
    - audit/v4.0-other-jackpots-degenerette.md (lines 423-434 -- confirms DGN-01 is WITHDRAWN/FALSE POSITIVE)
  </read_first>
  <action>
    Replace the existing v4.0 audit history entry in KNOWN-ISSUES.md. The current entry reads:

    ```
    ### v4.0 Ticket Lifecycle & RNG-Dependent Variable Re-Audit (2026-03-23)

    3 INFO findings from Phase 81. No HIGH, MEDIUM, or LOW.

    - **DSC-01 (INFO):** ...
    - **DSC-02 (INFO):** ...
    - **DSC-03 (INFO):** ...

    See `audit/v4.0-findings-consolidated.md`.
    ```

    Replace with an entry that covers all 8 phases. The new entry should follow this structure:

    ```
    ### v4.0 Ticket Lifecycle & RNG-Dependent Variable Re-Audit (2026-03-23)

    ~47 INFO findings across 8 phases (81-88). No HIGH, MEDIUM, or LOW.

    Two findings initially reported above INFO were withdrawn as false positives during Phase 87 audit:
    - ~~DEC-01 (MEDIUM)~~: `decBucketOffsetPacked` collision between regular and terminal decimator -- withdrawn because regular decimator never resolves at a stalled level and `poolWei == 0` guard prevents access.
    - ~~DGN-01 (LOW)~~: `_collectBetFunds` uses `<=` instead of `<` for claimable balance -- withdrawn because `<=` is intentional due to 1-wei sentinel in `claimableWinnings`.

    Key findings by phase:
    - **Phase 81 (Ticket Creation):** DSC-01 (stale v3.9 proof), DSC-02 (wrong key in sampleFarFutureTickets view), DSC-03 (NatSpec/unchecked mismatch)
    - **Phase 82 (Ticket Processing):** P82-01 through P82-06 (byte offset, setter attribution, missing writer, line drifts)
    - **Phase 83 (Winner Selection):** 0 new findings; DSC-01/DSC-02 independently re-confirmed
    - **Phase 84 (Prize Pool):** DSC-84-01 through DSC-84-06 (slot shifts from boon packing, line drift, NatSpec gaps, freeze redirect caveat)
    - **Phase 85 (Daily ETH Jackpot):** 9 new INFO (v3.8 slot/access discrepancies, line drifts, scope omission)
    - **Phase 86 (Daily Coin+Ticket):** 6 new INFO (stale FF key claim, line drifts, duplicate winners by design, level arithmetic asymmetry)
    - **Phase 87 (Other Jackpots):** 22 INFO + 2 withdrawn (earlybird dust/truncation/PRNG, BAF zero-score/dead code, decimator design patterns, degenerette auto-rebuy/decorative state/frozen guard)
    - **Phase 88 (RNG Re-verification):** 0 new findings; 55/55 v3.8 rows confirmed SAFE, 27 slot shifts documented

    See `audit/v4.0-findings-consolidated.md`.
    ```

    Preserve ALL other sections of KNOWN-ISSUES.md unchanged:
    - "## Intentional Design (Not Bugs)" section -- NO changes
    - "## Design Mechanics" section -- NO changes (no above-INFO findings to add)
    - All other "### v3.X" audit history entries -- NO changes

    Do NOT add DEC-01 or DGN-01 to the "Intentional Design" or "Design Mechanics" sections since they are withdrawn false positives, not known issues.
  </action>
  <verify>
    <automated>
      grep -c "~47 INFO findings" audit/KNOWN-ISSUES.md && \
      grep -c "DEC-01" audit/KNOWN-ISSUES.md && \
      grep -c "DGN-01" audit/KNOWN-ISSUES.md && \
      grep -c "withdrawn" audit/KNOWN-ISSUES.md && \
      grep -c "Phase 84" audit/KNOWN-ISSUES.md && \
      grep -c "Phase 87" audit/KNOWN-ISSUES.md && \
      grep -c "v3.7 VRF Path Audit" audit/KNOWN-ISSUES.md
    </automated>
  </verify>
  <acceptance_criteria>
    - audit/KNOWN-ISSUES.md contains "~47 INFO findings" (or exact count) in the v4.0 entry
    - audit/KNOWN-ISSUES.md contains "DEC-01" with strikethrough or "withdrawn" context
    - audit/KNOWN-ISSUES.md contains "DGN-01" with strikethrough or "withdrawn" context
    - audit/KNOWN-ISSUES.md contains "Phase 84" in the v4.0 key findings list
    - audit/KNOWN-ISSUES.md contains "Phase 87" in the v4.0 key findings list
    - audit/KNOWN-ISSUES.md contains "No HIGH, MEDIUM, or LOW" in the v4.0 entry
    - audit/KNOWN-ISSUES.md still contains "v3.7 VRF Path Audit" (prior entries preserved)
    - audit/KNOWN-ISSUES.md still contains "## Intentional Design (Not Bugs)" (body sections preserved)
    - audit/KNOWN-ISSUES.md still contains "## Design Mechanics" (body sections preserved)
    - audit/KNOWN-ISSUES.md does NOT contain "3 INFO findings from Phase 81" (old incomplete entry replaced)
  </acceptance_criteria>
  <done>
    KNOWN-ISSUES.md v4.0 audit history entry accurately reflects all 8 phases with ~47 INFO findings, DEC-01/DGN-01 documented as withdrawn false positives, and all prior sections preserved unchanged.
  </done>
</task>

</tasks>

<verification>
- grep "~47 INFO" audit/KNOWN-ISSUES.md returns a match (or exact count variant)
- grep "withdrawn" audit/KNOWN-ISSUES.md returns matches for DEC-01 and DGN-01
- The "## Intentional Design" and "## Design Mechanics" sections are unchanged
- All prior milestone audit history entries (v3.2 through v3.7) remain present and unmodified
</verification>

<success_criteria>
CFND-02 is satisfied: KNOWN-ISSUES.md updated with complete v4.0 audit history. Since DEC-01 and DGN-01 are withdrawn false positives and all remaining findings are INFO, no new entries in the document body sections are required -- only the audit history entry needs updating.
</success_criteria>

<output>
After completion, create `.planning/phases/91-consolidated-findings-rewrite/91-02-SUMMARY.md`
</output>
