---
phase: 120-test-suite-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - test/helpers/deployFixture.js
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "npx hardhat test runs to completion with zero failures"
    - "LCOV coverage report exists for Foundry suite with per-contract line coverage"
    - "LCOV coverage report exists for Hardhat suite with per-contract line coverage"
  artifacts:
    - path: "coverage/foundry/lcov.info"
      provides: "Foundry LCOV coverage data"
    - path: "coverage/hardhat/lcov.info"
      provides: "Hardhat LCOV coverage data"
    - path: ".planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md"
      provides: "Per-contract line coverage percentages from both suites"
  key_links:
    - from: ".planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md"
      to: "coverage/foundry/lcov.info"
      via: "extracted coverage percentages"
      pattern: "Line coverage"
    - from: ".planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md"
      to: "coverage/hardhat/lcov.info"
      via: "extracted coverage percentages"
      pattern: "Line coverage"
---

<objective>
Establish Hardhat green baseline and generate LCOV coverage reports for both suites.

Purpose: Hardhat green baseline completes the dual-suite regression safety net. LCOV reports document per-contract coverage for Phase 125 redundancy pruning.
Output: Both test suites passing 100%, coverage baseline document with per-contract line coverage percentages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-test-suite-cleanup/120-CONTEXT.md
@.planning/phases/120-test-suite-cleanup/120-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardhat green baseline</name>
  <read_first>
    - test/helpers/deployFixture.js (Hardhat deploy fixture -- understand the test infrastructure)
    - test/unit/DegenerusGame.test.js (largest unit test file -- check for import/deploy patterns)
    - test/deploy/DeployScript.test.js (deploy script test -- often fails on address mismatches)
  </read_first>
  <files>
    test/helpers/deployFixture.js
  </files>
  <action>
    **Step 1: Run full Hardhat suite and capture output (per D-09)**

    Run `npx hardhat test 2>&1 | tail -40` to see pass/fail summary.

    **Step 2: If any tests fail, diagnose and fix**

    For each failure:
    1. Read the failing test file
    2. Identify root cause (stale deploy addresses, changed function signatures, removed features, timing issues)
    3. Fix the test -- same policy as Foundry: fix if covering unique code path, delete only if feature was intentionally removed (per D-01)
    4. For each deletion, add a comment with justification (per D-02)
    5. For each deletion, verify filename does not reference an audit finding ID (per D-03)

    **Step 3: Document any known pre-existing failures (per D-10)**

    If any test was already failing before this phase and is now fixed or deleted, document it.
    Zero tolerance for silent known-failures -- every failure must be either fixed or documented with justification.

    **Step 4: Verify full green**

    Run `npx hardhat test` again after all fixes. Must show 0 failures.
    Count total passing tests and report.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` output contains "passing" and does NOT contain "failing"
    - Exit code is 0
    - Total passing count is documented
    - Any previously-known failures are documented with justification for fix or deletion
  </acceptance_criteria>
  <done>npx hardhat test achieves 100% pass rate with zero failures (TEST-03). No silent known-failures (per D-10).</done>
</task>

<task type="auto">
  <name>Task 2: Generate LCOV coverage reports and document baseline</name>
  <read_first>
    - foundry.toml (check for existing coverage configuration)
    - hardhat.config.js (check for solidity-coverage plugin configuration)
  </read_first>
  <files>
    .planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md
  </files>
  <action>
    **Step 1: Generate Foundry LCOV report (per D-11)**

    Run `forge coverage --report lcov` to produce lcov.info.
    If the default output location differs, move the file to `coverage/foundry/lcov.info`.
    If `forge coverage` fails, diagnose the issue (common: missing src/test separation, IR compilation issues).

    **Step 2: Generate Hardhat LCOV report (per D-11)**

    Run `npx hardhat coverage` to produce coverage/lcov.info.
    If the default output location is `coverage/lcov.info`, copy to `coverage/hardhat/lcov.info` for clarity.
    If `npx hardhat coverage` fails, check that `solidity-coverage` is installed and configured in hardhat.config.js. If not installed, run `npm install --save-dev solidity-coverage` and add to hardhat config.

    **Step 3: Extract per-contract line coverage percentages (per D-12)**

    Parse both lcov.info files to extract per-contract line coverage. For each contract in `contracts/`:
    - File path
    - Lines found (LF)
    - Lines hit (LH)
    - Line coverage percentage (LH/LF * 100)

    Use a command like:
    ```
    grep -E "^SF:|^LF:|^LH:" coverage/foundry/lcov.info | paste - - - | awk -F'[:|]' '{printf "%-60s %d/%d (%.1f%%)\n", $2, $6, $4, ($6/$4)*100}'
    ```

    **Step 4: Create COVERAGE-BASELINE.md (per D-12)**

    Create `.planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md` with:

    ```markdown
    # Coverage Baseline -- Phase 120

    Generated: [date]

    ## Foundry Suite Coverage

    | Contract | Lines Found | Lines Hit | Coverage |
    |----------|-------------|-----------|----------|
    | [per contract rows] |

    ## Hardhat Suite Coverage

    | Contract | Lines Found | Lines Hit | Coverage |
    |----------|-------------|-----------|----------|
    | [per contract rows] |

    ## Combined Notes

    - Total Foundry tests: [count]
    - Total Hardhat tests: [count]
    - Contracts with zero coverage in both suites: [list if any]
    - This baseline feeds Phase 125 redundancy analysis
    ```

    If either coverage tool fails to produce LCOV output, document the failure reason and provide whatever coverage data is available (e.g., forge coverage summary table, hardhat coverage text output).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -f .planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md && echo "COVERAGE-BASELINE.md exists" && head -5 .planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md` exists
    - COVERAGE-BASELINE.md contains a "Foundry Suite Coverage" section with at least 5 contract rows
    - COVERAGE-BASELINE.md contains a "Hardhat Suite Coverage" section with at least 5 contract rows
    - Each row contains a numeric "Lines Found", "Lines Hit", and "Coverage" percentage
    - At least one LCOV file exists (either `coverage/foundry/lcov.info` or forge coverage output location)
  </acceptance_criteria>
  <done>LCOV coverage reports generated for both suites. Per-contract line coverage percentages documented in COVERAGE-BASELINE.md (TEST-04). Coverage data ready for Phase 125 redundancy analysis.</done>
</task>

</tasks>

<verification>
1. `npx hardhat test` exits with code 0 and reports 0 failures
2. `forge test` still passes (no regression from Hardhat-related changes)
3. COVERAGE-BASELINE.md exists with per-contract coverage from both suites
4. At least one LCOV data file exists on disk
</verification>

<success_criteria>
- TEST-03: `npx hardhat test` achieves 100% pass rate
- TEST-04: LCOV coverage reports generated for both suites with per-contract line coverage documented
- Both suites green simultaneously -- the dual-suite regression baseline is established
</success_criteria>

<output>
After completion, create `.planning/phases/120-test-suite-cleanup/120-02-SUMMARY.md`
</output>
