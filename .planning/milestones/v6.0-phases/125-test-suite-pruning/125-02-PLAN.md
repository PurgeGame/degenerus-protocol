---
phase: 125-test-suite-pruning
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - .planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md
autonomous: true
requirements: [PRUNE-03, PRUNE-04]

must_haves:
  truths:
    - "forge test passes 100% after pruning"
    - "npx hardhat test passes 100% after pruning"
    - "Coverage comparison demonstrates zero unique line coverage lost"
    - "Final test counts documented for both suites"
  artifacts:
    - path: ".planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md"
      provides: "Before/after comparison proving no coverage loss + final pass/fail counts"
      contains: "Coverage Comparison"
  key_links:
    - from: "COVERAGE-COMPARISON.md"
      to: ".planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md"
      via: "before counts from Phase 120 baseline"
      pattern: "Before.*After"
    - from: "COVERAGE-COMPARISON.md"
      to: ".planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md"
      via: "deletion manifest drives expected count reduction"
      pattern: "Deleted"
---

<objective>
Verify both test suites pass 100% after pruning and document the coverage comparison.

Purpose: Prove that pruning removed only redundant tests -- no unique line coverage was lost. Establish the final green baseline for both suites as the milestone deliverable.
Output: Both suites green, COVERAGE-COMPARISON.md with before/after counts and coverage analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-test-suite-pruning/125-CONTEXT.md
@.planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md
@.planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md
@.planning/phases/125-test-suite-pruning/125-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run both suites and fix any breakage from pruning</name>
  <read_first>
    - .planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md (understand what was deleted and what remains)
    - .planning/phases/125-test-suite-pruning/125-01-SUMMARY.md (understand what Plan 01 actually did)
    - hardhat.config.js (verify TEST_DIR_ORDER is consistent with remaining directories)
    - test/helpers/testUtils.js (check if any deleted test was imported by helpers)
  </read_first>
  <files>
    Any test file or helper that has broken imports referencing deleted files
  </files>
  <action>
    **Step 1: Run Foundry suite**
    Run `forge test` and capture output. Foundry tests should be unaffected by Hardhat file deletions since they are independent .t.sol files. If any Foundry test fails, investigate -- it may be due to contract changes from Phases 121-124 that were not caught during those phases (unlikely but possible).

    **Step 2: Run Hardhat suite**
    Run `npx hardhat test 2>&1 | tail -40` and capture pass/fail counts.

    Possible failure modes after pruning:
    1. **Missing import** -- a remaining test file imports a helper or fixture from a deleted file. Fix: trace the import, determine if it should be redirected to another file or if the import is dead code.
    2. **Test ordering dependency** -- a remaining test depends on EVM state left by a deleted test (unlikely with loadFixture pattern). Fix: add missing setup to the affected test.
    3. **Hardhat config crash** -- TEST_DIR_ORDER references a directory that no longer has any files. Fix: remove the empty directory entry from TEST_DIR_ORDER in hardhat.config.js.

    For each failure: read the failing test, identify root cause, fix minimally. Do NOT add new test coverage -- only fix broken references.

    **Step 3: Re-run both suites until green**
    Iterate until both `forge test` and `npx hardhat test` show 0 failures. Record final passing counts.

    **Step 4: Record final counts**
    - Foundry: N tests passing (expected: same as Phase 120 baseline of 369, since Foundry tests were unlikely deletion targets)
    - Hardhat: N tests passing (expected: 1242 minus the number of tests in deleted files)

    Count the number of individual test cases (it/test blocks) in deleted files to compute expected Hardhat delta.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test 2>&1 | tail -3 && echo "---" && npx hardhat test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `forge test` output contains "ok" or "passed" with 0 failures
    - `npx hardhat test` output contains "passing" and does NOT contain "failing"
    - Both commands exit with code 0
    - Final test counts are recorded (Foundry N, Hardhat N)
  </acceptance_criteria>
  <done>Both forge test and npx hardhat test pass 100% after pruning with documented final counts (PRUNE-04 partial).</done>
</task>

<task type="auto">
  <name>Task 2: Create coverage comparison document</name>
  <read_first>
    - .planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md (before counts: Foundry 369, Hardhat 1242)
    - .planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md (what was deleted and why)
  </read_first>
  <files>.planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md</files>
  <action>
    Create COVERAGE-COMPARISON.md with the following sections:

    **Section 1: Test Count Comparison**

    | Suite | Before (Phase 120) | After (Phase 125) | Delta | Files Deleted |
    |-------|-------------------|-------------------|-------|---------------|
    | Foundry | 369 tests / 43 files | [actual] tests / [actual] files | [delta] | [count] |
    | Hardhat | 1242 tests / 47 files | [actual] tests / [actual] files | [delta] | [count] |
    | Total | 1611 tests / 90 files | [actual] tests / [actual] files | [delta] | [count] |

    **Section 2: Coverage Loss Analysis**

    Since LCOV is infeasible (documented in Phase 120 COVERAGE-BASELINE.md), coverage comparison uses function-level tracing:

    For each deleted file, list:
    - What contract functions/behaviors it tested
    - Which remaining test file(s) cover those same functions/behaviors
    - Conclusion: zero unique coverage lost because [specific justification]

    Format:
    ```
    ### [deleted file]
    **Tested:** [contract functions]
    **Covered by:** [remaining file(s)] which tests the same functions with [equal or better] thoroughness
    **Unique coverage lost:** None
    ```

    For ghost tests (poc/), the analysis is simpler: they were never executed, so they provided zero coverage. Deleting them loses nothing.

    **Section 3: Final Green Baseline**

    ```
    ## Final Green Baseline

    Foundry: [N] tests passing, 0 failing ([N] suites)
    Hardhat: [N] tests passing, 0 failing
    Total: [N] tests

    Verified: [date]
    Commands:
      forge test — exit 0
      npx hardhat test — exit 0
    ```

    **Section 4: Deleted Files Manifest**

    Complete list of all deleted files with one-line justification (copied from REDUNDANCY-AUDIT.md Deletion Manifest for traceability).

    IMPORTANT: The key claim is "zero unique line coverage lost" (PRUNE-03). For each deleted file, the document must name which remaining file covers the same contract code. If any deleted file tested a contract function not tested by any remaining file, this is a coverage gap and must be flagged as an error requiring remediation.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -f .planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md && grep -c "Unique coverage lost: None" .planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/125-test-suite-pruning/COVERAGE-COMPARISON.md exists
    - COVERAGE-COMPARISON.md contains "## Test Count Comparison" section with Before/After/Delta columns
    - COVERAGE-COMPARISON.md contains "## Coverage Loss Analysis" section with per-deleted-file analysis
    - COVERAGE-COMPARISON.md contains "## Final Green Baseline" section with exact passing counts for both suites
    - COVERAGE-COMPARISON.md contains "## Deleted Files Manifest" section
    - Every deleted file entry has "Unique coverage lost: None" (or flags coverage gaps for remediation)
    - Before counts match Phase 120 baseline (369 Foundry, 1242 Hardhat)
    - After counts are less than Before counts (pruning removed something)
    - Both "forge test" and "npx hardhat test" commands listed with "exit 0"
  </acceptance_criteria>
  <done>COVERAGE-COMPARISON.md documents zero unique coverage loss with before/after counts. Final green baseline established: both suites pass 100% with documented counts (PRUNE-03, PRUNE-04).</done>
</task>

</tasks>

<verification>
1. `forge test` exits 0 after all pruning
2. `npx hardhat test` exits 0 after all pruning
3. COVERAGE-COMPARISON.md proves zero unique coverage lost
4. Final test counts documented (both suites)
5. Before counts from Phase 120 baseline match COVERAGE-COMPARISON.md
</verification>

<success_criteria>
- PRUNE-03: COVERAGE-COMPARISON.md proves zero lost lines via function-level tracing for each deleted file
- PRUNE-04: Both suites green with documented final pass/fail counts
- v6.0 milestone can close with confidence that test coverage is maintained
</success_criteria>

<output>
After completion, create `.planning/phases/125-test-suite-pruning/125-02-SUMMARY.md`
</output>
