---
phase: 121-storage-and-gas-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/libraries/BitPackingLib.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
autonomous: true
requirements: [FIX-01, FIX-05, FIX-07, FIX-08]

must_haves:
  truths:
    - "lastLootboxRngWord storage declaration is fully deleted from DegenerusGameStorage.sol"
    - "All 3 write sites for lastLootboxRngWord are deleted from AdvanceModule"
    - "The 1 read site in JackpotModule uses lootboxRngWordByIndex[lootboxRngIndex - 1] instead"
    - "No downstream storage slots shifted dangerously (forge inspect proves layout stability)"
    - "advanceBounty is computed at payout time using current price, not eagerly at function entry"
    - "BitPackingLib NatSpec says bits 152-153, not bits 152-154"
    - "Tests referencing lastLootboxRngWord slot 55 are updated or removed"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "lastLootboxRngWord declaration deleted"
      contains: "No line containing 'lastLootboxRngWord'"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "Write deletions + advanceBounty deferred computation"
    - path: "contracts/libraries/BitPackingLib.sol"
      provides: "Corrected NatSpec"
      contains: "bits 152-153"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: "lootboxRngWordByIndex mapping"
      via: "redirected read at L1838"
      pattern: "lootboxRngWordByIndex\\[lootboxRngIndex - 1\\]"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "coin.creditFlip"
      via: "inline bounty computation at each payout site"
      pattern: "ADVANCE_BOUNTY_ETH.*PRICE_COIN_UNIT.*bountyMultiplier.*price"
---

<objective>
Delete the lastLootboxRngWord storage variable (FIX-01), rewrite advanceBounty to compute at payout time (FIX-07), correct BitPackingLib NatSpec (FIX-05), and run the FIX-08 delta audit proving the deletion is safe.

Purpose: Eliminate a redundant storage variable (saves ~20K gas/write across 3 paths), fix stale-price bounty computation, and correct misleading documentation.
Output: Modified contracts, updated tests, delta audit verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-storage-and-gas-fixes/121-CONTEXT.md

@contracts/storage/DegenerusGameStorage.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/libraries/BitPackingLib.sol
@test/fuzz/VRFStallEdgeCases.t.sol

<interfaces>
<!-- Storage layout around lastLootboxRngWord (slot 55) -->
From contracts/storage/DegenerusGameStorage.sol (L1225-1235):
```solidity
uint256 internal lootboxRngPendingBurnie;       // slot 54
uint256 internal lastLootboxRngWord;             // slot 55 — DELETE THIS
bool internal midDayTicketRngPending;            // slot 56
```

<!-- The mapping that replaces lastLootboxRngWord reads -->
From contracts/storage/DegenerusGameStorage.sol (L1200):
```solidity
mapping(uint48 => uint256) internal lootboxRngWordByIndex;
```

<!-- The 3 write sites to delete in AdvanceModule -->
L162: `lastLootboxRngWord = word;`           (mid-day path)
L862: `lastLootboxRngWord = rngWord;`        (_finalizeLootboxRng)
L1526: `lastLootboxRngWord = fallbackWord;`  (game-over fallback loop)

<!-- The 1 read site to redirect in JackpotModule -->
L1838: `uint256 entropy = lastLootboxRngWord;`

<!-- advanceBounty current code -->
L127: `uint256 advanceBounty = (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / price;`
L189-202: escalation block multiplies advanceBounty
L177: `coin.creditFlip(caller, advanceBounty);`  (ticket-working early return)
L214: `coin.creditFlip(caller, advanceBounty);`  (ticket-processing early return)
L396: `coin.creditFlip(caller, advanceBounty);`  (normal completion)

<!-- BitPackingLib NatSpec -->
L59: `/// @notice Bit position for whale bundle type (bits 152-154)`
Should be: `(bits 152-153)` — callers use mask=3 (0b11) = 2-bit field

<!-- Test constants referencing slot 55 -->
test/fuzz/VRFStallEdgeCases.t.sol:
L328: `uint256 constant SLOT_LAST_LOOTBOX_RNG_WORD = 55;`
L470-541: test_zeroSeedUnreachableAfterSwap and test_zeroSeedAtGameStart read from slot 55
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete lastLootboxRngWord — storage, writes, and read redirect (FIX-01)</name>
  <files>
    contracts/storage/DegenerusGameStorage.sol,
    contracts/modules/DegenerusGameAdvanceModule.sol,
    contracts/modules/DegenerusGameJackpotModule.sol,
    test/fuzz/VRFStallEdgeCases.t.sol
  </files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol (L1220-1250 for declaration context),
    contracts/modules/DegenerusGameAdvanceModule.sol (L155-165 for write 1, L855-865 for write 2, L1515-1530 for write 3),
    contracts/modules/DegenerusGameJackpotModule.sol (L1830-1850 for read site),
    test/fuzz/VRFStallEdgeCases.t.sol (L325-330 for slot constant, L465-545 for affected tests)
  </read_first>
  <action>
    **Per D-01:** In DegenerusGameStorage.sol, DELETE the entire declaration block for lastLootboxRngWord (L1228-1231 — the NatSpec comment and the `uint256 internal lastLootboxRngWord;` line). This is a full deletion per user decision, NOT a deprecation comment.

    **Per D-02:** In DegenerusGameAdvanceModule.sol, delete these 3 lines:
    - L162: delete `lastLootboxRngWord = word;`
    - L862: delete `lastLootboxRngWord = rngWord;`
    - L1526: delete `lastLootboxRngWord = fallbackWord;`
    Do NOT delete surrounding code. Only remove these exact assignment statements.

    **Per D-03:** In DegenerusGameJackpotModule.sol L1838, replace:
    ```solidity
    uint256 entropy = lastLootboxRngWord;
    ```
    with:
    ```solidity
    uint256 entropy = lootboxRngWordByIndex[lootboxRngIndex - 1];
    ```

    **Test updates for VRFStallEdgeCases.t.sol:**
    The tests at L470-541 (test_zeroSeedUnreachableAfterSwap, test_zeroSeedAtGameStart) directly read storage slot 55 via `vm.load` to verify lastLootboxRngWord state. Since the slot is being deleted, these tests must be updated:

    Option A (preferred): Convert these tests to read `lootboxRngWordByIndex` via the existing view function `game.lootboxRngWord(index)` instead of raw slot reads. The behavioral invariant is the same — after day completion, the lootbox RNG word for that index is nonzero.

    For test_zeroSeedUnreachableAfterSwap:
    - Replace all `vm.load(address(game), bytes32(uint256(SLOT_LAST_LOOTBOX_RNG_WORD)))` reads with `game.lootboxRngWord(game.lootboxRngIndexView() - 1)` — this reads the same value that processTicketBatch will use.
    - Update assertion messages from "lastLootboxRngWord" to "lootboxRngWord at current index".

    For test_zeroSeedAtGameStart:
    - The test already asserts `game.lootboxRngWord(0) == 0` at start. Replace the slot-55 reads with equivalent `game.lootboxRngWord(index)` reads.
    - After resume, verify `game.lootboxRngWord(resumeIndex) != 0` (this assertion already exists at L537-540).

    Remove the `SLOT_LAST_LOOTBOX_RNG_WORD = 55` constant declaration at L328 if no remaining tests use it. Keep `SLOT_MID_DAY_PENDING = 56` — that slot still exists (but note its slot number will shift to 55 after deletion; update the constant accordingly after running `forge inspect`).

    After all changes, verify the deleted variable is not referenced anywhere:
    `grep -r "lastLootboxRngWord" contracts/ test/` should return zero hits in .sol files.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -r "lastLootboxRngWord" contracts/ test/ --include="*.sol" | grep -v "\.md" | wc -l</automated>
    Expected: 0 hits.
    Then: `forge build` compiles without errors.
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "lastLootboxRngWord" in any .sol file under contracts/ or test/
    - `forge build` succeeds with zero errors
    - DegenerusGameStorage.sol no longer contains the `uint256 internal lastLootboxRngWord;` line
    - DegenerusGameAdvanceModule.sol has no lines assigning to lastLootboxRngWord
    - DegenerusGameJackpotModule.sol L1838 reads `lootboxRngWordByIndex[lootboxRngIndex - 1]`
    - VRFStallEdgeCases.t.sol tests still compile and verify the equivalent behavioral invariant via lootboxRngWord view
  </acceptance_criteria>
  <done>lastLootboxRngWord fully removed from codebase. Read site redirected. Tests updated to use lootboxRngWord view function.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite advanceBounty to payout-time computation (FIX-07) + BitPackingLib NatSpec (FIX-05)</name>
  <files>
    contracts/modules/DegenerusGameAdvanceModule.sol,
    contracts/libraries/BitPackingLib.sol
  </files>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol (L120-140 for function entry and eager computation, L185-215 for escalation block, L390-400 for final payout site),
    contracts/libraries/BitPackingLib.sol (L55-65 for NatSpec)
  </read_first>
  <action>
    **FIX-07 per D-13 through D-16:**

    1. **Delete L127** — remove the line `uint256 advanceBounty = (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / price;`
       This eagerly computes bounty using potentially stale `price`. The fix computes at each payout site.

    2. **Replace L189-202 escalation block** with a `bountyMultiplier` computation. The current block multiplies `advanceBounty` (which will no longer exist). Replace with:
       ```solidity
       uint256 bountyMultiplier = 1;
       {
           uint256 dayStart = (uint256(day - 1) +
               ContractAddresses.DEPLOY_DAY_BOUNDARY) *
               1 days +
               82_620;
           uint256 elapsed = ts - dayStart;
           if (elapsed >= 2 hours) {
               bountyMultiplier = 6;
           } else if (elapsed >= 1 hours) {
               bountyMultiplier = 4;
           } else if (elapsed >= 20 minutes) {
               bountyMultiplier = 2;
           }
       }
       ```
       Declare `bountyMultiplier` BEFORE the escalation block scope so it persists. The variable must be visible at L396.

    3. **Replace all 3 `coin.creditFlip(caller, advanceBounty)` calls:**

       At L177 (ticket-working early return):
       ```solidity
       coin.creditFlip(caller, (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / price);
       ```
       NOTE: L177 is BEFORE the escalation block (L189), so at this point `bountyMultiplier` is not yet computed. L177 is in the `if (!ticketsFullyProcessed)` branch (the "same-day ticket drain" path), which exits before reaching the new-day escalation logic. For this path, the bounty is always 1x base (no escalation applies to same-day paths). So L177 should be:
       ```solidity
       coin.creditFlip(caller, (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / price);
       ```
       (1x multiplier, inline computation using current `price`.)

       At L214 (ticket-processing early return in the new-day path, AFTER escalation block):
       ```solidity
       coin.creditFlip(caller, (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / price);
       ```

       At L396 (normal completion, AFTER escalation block):
       ```solidity
       coin.creditFlip(caller, (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / price);
       ```

    IMPORTANT: Verify that `price` is read from storage at function entry and is still in scope at all 3 payout sites. If `price` is a storage variable read, it reflects current state. The improvement is that by not eagerly computing the division at L127, we save a SLOAD on early-exit paths that never reach a payout.

    **FIX-05 per D-10:**

    In BitPackingLib.sol L59, change:
    ```solidity
    /// @notice Bit position for whale bundle type (bits 152-154)
    ```
    to:
    ```solidity
    /// @notice Bit position for whale bundle type (bits 152-153)
    ```
    This is a NatSpec-only change. Callers use mask=3 (0b11) = 2-bit field, so bits 152-153 is correct.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
    Expected: compilation success, zero errors.
    Additionally: `grep "bits 152-153" contracts/libraries/BitPackingLib.sol` returns a match.
    Additionally: `grep "advanceBounty" contracts/modules/DegenerusGameAdvanceModule.sol` returns zero hits (variable name eliminated).
  </verify>
  <acceptance_criteria>
    - No variable named `advanceBounty` exists in DegenerusGameAdvanceModule.sol
    - L127 eager computation line is deleted
    - L189-202 replaced with bountyMultiplier computation (1x/2x/4x/6x)
    - All 3 creditFlip calls compute bounty inline: `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / price` (or `* 1` for the pre-escalation path at L177)
    - BitPackingLib.sol L59 reads "bits 152-153"
    - `forge build` succeeds
    - `forge inspect DegenerusGameAdvanceModule bytecode` differs from pre-change (confirming code change)
    - `forge inspect BitPackingLib bytecode` is IDENTICAL to pre-change (NatSpec only, zero bytecode change per D-10)
  </acceptance_criteria>
  <done>advanceBounty computed at each payout site using current price. bountyMultiplier replaces eager multiplication. BitPackingLib NatSpec corrected. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: FIX-08 delta audit — prove deletion safety via forge inspect + path enumeration</name>
  <files>
    (no files modified — verification-only task)
  </files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol (verify deletion is applied from Task 1),
    contracts/modules/DegenerusGameAdvanceModule.sol (verify writes are gone from Task 1),
    contracts/modules/DegenerusGameJackpotModule.sol (verify read redirect from Task 1)
  </read_first>
  <action>
    **FIX-08 delta audit per D-17, D-18. This task runs AFTER Tasks 1 and 2 are complete.**

    **Part A: Storage layout verification via forge inspect**

    Run `forge inspect DegenerusGameStorage storage-layout --pretty` and verify:
    1. The slot formerly occupied by `lastLootboxRngWord` (slot 55) is gone or shows as a gap.
    2. `midDayTicketRngPending` (formerly slot 56) — check its new slot number. If it shifted to slot 55, that is EXPECTED and SAFE because:
       - All modules are deployed fresh (not upgraded via proxy)
       - All delegatecall modules inherit from DegenerusGameStorage and are compiled together
       - There is no persistent on-chain storage that could be misinterpreted
    3. Verify no other variable's slot number changed unexpectedly relative to its position in the declaration order.

    If the storage layout shows that `midDayTicketRngPending` moved from slot 56 to slot 55 (or similar compaction): this is safe for fresh deploys. Document it.

    If any mapping or dynamic storage slot changed: FLAG AS BLOCKER.

    **Part B: Path-by-path value equivalence proof**

    Enumerate all 5 RNG paths and prove `lootboxRngWordByIndex[lootboxRngIndex - 1]` returns the identical value that `lastLootboxRngWord` would have held:

    1. **Normal VRF path** (_finalizeLootboxRng at former L858-863):
       - `lootboxRngWordByIndex[index] = rngWord;` was written at L861
       - `lastLootboxRngWord = rngWord;` was written at L862 (now deleted)
       - Proof: both wrote the same `rngWord` value. After deletion, only the mapping write remains. Read site `lootboxRngWordByIndex[lootboxRngIndex - 1]` reads the same index = `lootboxRngIndex - 1`, same value. EQUIVALENT.

    2. **Mid-day path** (advanceGame at former L159-162):
       - `word = lootboxRngWordByIndex[lootboxRngIndex - 1];` was read at L159
       - `lastLootboxRngWord = word;` was written at L162 (now deleted)
       - Proof: L162 wrote the value that was ALREADY in the mapping. The mapping was the source of truth. Deleting L162 changes nothing — the read site already goes to the mapping. EQUIVALENT.

    3. **Stall backfill path** (game-over fallback loop at former L1521-1527):
       - `lootboxRngWordByIndex[i] = fallbackWord;` at L1525
       - `lastLootboxRngWord = fallbackWord;` at L1526 (now deleted)
       - Loop iterates backward, so the LAST iteration writes to the lowest unfilled index. After loop, `lootboxRngWordByIndex[lootboxRngIndex - 1]` reads the value written in the FIRST iteration (highest index). Wait — need to verify: `lootboxRngIndex - 1` is the current index. The loop fills from `lootboxRngIndex - 1` downward. So `lootboxRngWordByIndex[lootboxRngIndex - 1]` gets the value written in the FIRST loop iteration, which is the same value that `lastLootboxRngWord` ended up with after the LAST iteration. Are these the same?

       CRITICAL CHECK: The loop runs `for i = lootboxRngIndex - 1 downto firstUnfilled`. Each iteration writes a DIFFERENT `fallbackWord` (derived from `keccak256(abi.encodePacked(vrfWord, i))`). `lastLootboxRngWord` was overwritten each iteration, so after the loop it held the value from the LAST iteration (lowest i). But `lootboxRngWordByIndex[lootboxRngIndex - 1]` reads from the FIRST iteration (highest i = lootboxRngIndex - 1).

       These are DIFFERENT values. However, this is CORRECT because `processTicketBatch` uses the word for the CURRENT index, not the last-written word. The old `lastLootboxRngWord` was actually WRONG for stall backfill — it held the word for the oldest backfilled index, not the current one. The mapping read is MORE CORRECT. Document this as a latent bug fix.

    4. **Coordinator swap path**: The coordinator swap does NOT write to `lastLootboxRngWord` or `lootboxRngWordByIndex`. It resets VRF request state. After swap + resume, the normal VRF path fires. EQUIVALENT (covered by path 1).

    5. **Game-over single-word fallback**: If only one word is missing (no loop), `lootboxRngWordByIndex[lootboxRngIndex - 1] = fallbackWord` and `lastLootboxRngWord = fallbackWord` wrote the same value. EQUIVALENT.

    **Part C: Underflow check (D-18)**

    Verify `lootboxRngIndex` is never 0 when the read site executes:
    - `lootboxRngIndex` starts at 1 (initialized in constructor or first advanceGame)
    - It increments on each VRF request
    - The read site in processTicketBatch only executes after at least one day has completed (VRF fulfilled)
    - Therefore `lootboxRngIndex >= 2` when processTicketBatch runs, so `lootboxRngIndex - 1 >= 1`, no underflow.
    - Verify by grepping for the initial value of lootboxRngIndex.

    **Part D: Run full test suite**

    Run `forge test` and confirm all tests pass (zero failures). This is the regression gate.

    Write the delta audit results as a comment block at the top of the git commit message or as output in the summary.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge inspect DegenerusGameStorage storage-layout --pretty 2>&1 | grep -E "lastLootbox|midDay|lootboxRng" && echo "---" && forge test 2>&1 | tail -3</automated>
    Expected: `lastLootboxRngWord` does NOT appear in storage layout. `midDayTicketRngPending` appears (possibly at new slot). `forge test` shows all tests passing.
  </verify>
  <acceptance_criteria>
    - `forge inspect DegenerusGameStorage storage-layout` does NOT contain "lastLootboxRngWord"
    - `midDayTicketRngPending` slot position documented (shifted or not)
    - All 5 RNG paths analyzed with value equivalence proven (or latent-bug-fix documented for stall backfill)
    - `lootboxRngIndex` underflow impossibility verified
    - `forge test` passes with zero failures
  </acceptance_criteria>
  <done>FIX-08 delta audit complete. Storage layout verified safe. All 5 RNG paths proven equivalent (or improved). Full test suite green.</done>
</task>

</tasks>

<verification>
1. `grep -r "lastLootboxRngWord" contracts/ test/ --include="*.sol"` returns 0 hits
2. `grep "advanceBounty" contracts/modules/DegenerusGameAdvanceModule.sol` returns 0 hits
3. `grep "bits 152-153" contracts/libraries/BitPackingLib.sol` returns 1 hit
4. `forge build` compiles successfully
5. `forge test` passes with zero failures
6. `forge inspect DegenerusGameStorage storage-layout --pretty` shows no lastLootboxRngWord
</verification>

<success_criteria>
- lastLootboxRngWord fully deleted from storage, all writes removed, read redirected to mapping
- advanceBounty computed at payout time with bountyMultiplier pattern
- BitPackingLib NatSpec corrected to bits 152-153
- Delta audit proves deletion safe across all 5 RNG paths
- Full forge test suite passes green
</success_criteria>

<output>
After completion, create `.planning/phases/121-storage-and-gas-fixes/121-01-SUMMARY.md`
</output>
