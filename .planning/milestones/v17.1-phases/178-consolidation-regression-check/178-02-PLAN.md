---
phase: 178-consolidation-regression-check
plan: 02
type: execute
wave: 2
depends_on:
  - 178-01
files_modified:
  - .planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md
autonomous: true
requirements:
  - CON-02
must_haves:
  truths:
    - "Every finding from v3.1 and v3.5 sweeps that was marked as fixed has been spot-checked against the current contract source"
    - "Any v3.1/v3.5 finding where the fix has regressed is documented as a new finding in the consolidated document"
    - "The consolidated document's Regression Findings section is populated (or explicitly states 'No regressions found')"
    - "The spot-check covers the high-priority LOWs from v3.1 and v3.5: DegenerusAdmin line 38 threshold, DegenerusAdmin line 41 death clock, DegenerusGame 18h VRF timeout, LootboxModule header, DegenerusAdmin _applyVote returns, DegenerusVault transferFrom @custom:reverts, sDGNRS RedemptionClaimed event"
  artifacts:
    - path: ".planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md"
      provides: "Complete consolidated findings including regression check results"
      contains: "## Regression Findings"
  key_links:
    - from: "v17.1-comment-findings-consolidated.md Regression Findings section"
      to: "current contract source"
      via: "spot-check grep commands"
      pattern: "FIXED|REGRESSION|REG-"
---

<objective>
Spot-check key findings from the v3.1 and v3.5 comment sweeps that were documented as fixed. Verify the fixes are still present in the current contract source. Append any regressions to the consolidated document as new findings.

Purpose: The v3.1 and v3.5 sweeps identified comment discrepancies that were subsequently fixed in Phase 133 (comment re-scan, 2026-03-27). Since then, significant contract changes have occurred (v16.0 module consolidation, v17.0 affiliate bonus cache). Some fixes may have been overwritten or new stale comments introduced at the same locations.

Output: Appended "## Regression Findings" section in `v17.1-comment-findings-consolidated.md`. If no regressions found, the section contains a confirmation statement with each check listed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md
@.planning/PROJECT.md
</context>

<interfaces>
<!-- Key v3.1 and v3.5 LOW findings that were fixed in Phase 133, to check for regressions -->
<!-- Source: v3.1 Phase 31 VERIFICATION.md and v3.5 Phase 58 PLAN.md -->

v3.1 LOWs fixed (from Phase 31 VERIFICATION.md citation accuracy spot-checks):
- DegenerusAdmin.sol:38 — was "60% -> 5% over 7 days", fixed to "50% -> 5%"
- DegenerusAdmin.sol:41 — was "Death clock pauses while any proposal is active", removed/fixed
- DegenerusGame.sol:287 — was "18h timeout", fixed to "12h timeout"

v3.5 LOWs fixed (from Phase 58 PLAN.md low-findings list):
- CMT-V35-01-001: StakedDegenerusStonk.sol:137,633 — RedemptionClaimed flipWon/flipResolved mismatch
- CMT-V35-02-003: LootboxModule.sol:32 — Header lists nonexistent resolveLootboxRng
- CMT-V35-03-004: IDegenerusGameModules.sol:51-53 — Orphaned NatSpec for removed finalizeEndgame
- CMT-V35-03-005: IDegenerusGameModules.sol:410-413 — Corrupted NatSpec on resolveBets
- CMT-V35-04-001: DegenerusAdmin.sol:38 — Header says 60% threshold, actual is 50% (same as v3.1)
- CMT-V35-04-002: DegenerusAdmin.sol:41 — Stale "death clock pauses" reference (same as v3.1)
- CMT-V35-04-003: DegenerusVault.sol:235 — transferFrom @custom:reverts says "from or to" but only "to" checked

Phase 133 fixed all of these in commits docs(133-01) through docs(133-05) (2026-03-27).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Spot-check v3.1 and v3.5 fixed findings against current source</name>

  <read_first>
    - contracts/DegenerusAdmin.sol (lines 35-45 for threshold comment, lines 40-45 for death clock comment, lines 789-800 for _applyVote returns)
    - contracts/DegenerusGame.sol (lines 235-245 for VRF timeout comment)
    - contracts/DegenerusVault.sol (lines 228-245 for transferFrom @custom:reverts)
    - contracts/StakedDegenerusStonk.sol (lines 130-145 for RedemptionClaimed event, lines 620-640 for flipWon/flipResolved emit site)
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 25-42 for contract header)
    - contracts/interfaces/IDegenerusGameModules.sol (lines 45-65 for finalizeEndgame area, lines 400-425 for resolveBets NatSpec)
  </read_first>

  <files>.planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md</files>

  <action>
Read the listed contract sections. For each of the 7 priority regression checks below, determine: (a) is the original fix still in place, or (b) has the comment regressed to its broken state, or (c) has new breakage been introduced at the same location.

**Priority regression checks:**

1. **DegenerusAdmin.sol ~line 38** — Threshold comment.
   Was: "60% -> 5% over 7 days". Fixed to: "50% -> 5%".
   Check: Does the current architecture overview mention "50%" (not "60%") for the initial threshold?
   Note: Phase 177 finding ADM-02 already documents an INFO-level nuance (5% reached at day 6, lifetime is 7 days) — that is a separate ongoing finding, not a regression of this fix.

2. **DegenerusAdmin.sol ~line 41** — Death clock pause comment.
   Was: "Death clock pauses while any proposal is active". Fixed: removed or corrected.
   Check: Does any comment near line 41 still reference death clock pausing?

3. **DegenerusVault.sol `transferFrom` @custom:reverts** — "from or to" vs "to" only.
   Was: @custom:reverts says "from or to" but only "to" is checked. Fixed: updated to say "to" only.
   Check: Look at DegenerusVault.sol around line 235 for the transferFrom function. Does the @custom:reverts note still say "from or to" (regression) or has it been corrected?

4. **StakedDegenerusStonk.sol `RedemptionClaimed` event** — flipWon/flipResolved parameter mismatch.
   Was: event declared with `flipWon bool` but emitted with field named `flipResolved`.
   Check: Current RedemptionClaimed event declaration and its emit site — do the parameter names match now?

5. **DegenerusGameLootboxModule.sol contract header ~line 32** — nonexistent `resolveLootboxRng`.
   Was: header listed resolveLootboxRng as a function. Fixed: removed.
   Check: Does the current contract header (lines 25-42) mention resolveLootboxRng?

6. **IDegenerusGameModules.sol** — Orphaned `finalizeEndgame` NatSpec.
   Was: NatSpec for deleted function finalizeEndgame present at lines 51-53. Fixed: removed.
   Check: Does IDegenerusGameModules.sol near lines 45-65 still have any mention of finalizeEndgame?

7. **IDegenerusGameModules.sol** — Corrupted `resolveBets` NatSpec.
   Was: resolveBets NatSpec at lines 410-413 was incorrect/corrupted. Fixed: corrected.
   Note: Phase 175-04 finding D-01 already documents a current issue with resolveBets having two @notice tags — confirm whether this is the same issue or a different one. If D-01 is already logged, do not double-count as a regression.
   Check: Does the resolveBets NatSpec look reasonable now, or does it have a copy-paste corruption beyond what D-01 already documents?

**Skip list (already captured as current Phase 176/177 findings):**
- DGM-03 (18h VRF timeout at line 239): already in consolidated doc as a current finding — do NOT re-log as regression
- ADM-01 (_applyVote return count): already in consolidated doc as a current finding — do NOT re-log as regression

**For each check, record one of:**
- FIXED: [brief confirmation of what the current comment says]
- REGRESSION: [what the comment says now vs what it should say] — create REG-NN finding
- SUPERSEDED: [the location has changed due to refactoring; current state is X]
- ALREADY-FOUND: [finding ID already in the consolidated doc that captures this]

**After completing all checks, replace the stub in the consolidated document:**

Replace the line `*To be appended by Plan 02 after regression check is complete.*` with the full results section.

If no regressions:
```
## Regression Findings

**Sweep:** v3.1 and v3.5 prior fixed findings spot-checked 2026-04-03
**Result:** No regressions found — all spot-checked fixes remain in place

| Check | Prior Finding | Current State | Status |
|-------|--------------|---------------|--------|
| DegenerusAdmin.sol threshold | v3.1 CMT-001 / v3.5 CMT-V35-04-001 | "50% -> 5%" present | FIXED |
| DegenerusAdmin.sol death clock | v3.1 CMT-002 / v3.5 CMT-V35-04-002 | Stale comment absent | FIXED |
| DegenerusVault.sol transferFrom @custom:reverts | v3.5 CMT-V35-04-003 | corrected | FIXED |
| StakedDegenerusStonk RedemptionClaimed | v3.5 CMT-V35-01-001 | parameter names match | FIXED |
| DegenerusGameLootboxModule header | v3.5 CMT-V35-02-003 | resolveLootboxRng absent | FIXED |
| IDegenerusGameModules finalizeEndgame | v3.5 CMT-V35-03-004 | orphaned NatSpec absent | FIXED |
| IDegenerusGameModules resolveBets NatSpec | v3.5 CMT-V35-03-005 | [status] | FIXED or ALREADY-FOUND |
```

If regressions found, add finding entries below the table:
```
### REG-01 — [SEVERITY] — [Contract] line [N]
**Prior finding:** [ID from v3.1/v3.5]
**Comment says:** [current broken text]
**Code does:** [what the code actually does]
```
  </action>

  <verify>
    <automated>python3 -c "
with open('.planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md') as f:
    content = f.read()
assert '## Regression Findings' in content, 'Missing Regression Findings section'
reg_section = content[content.index('## Regression Findings'):]
assert len(reg_section) > 150, f'Regression Findings section is too short: {len(reg_section)} chars'
assert 'To be appended' not in reg_section, 'Stub placeholder not replaced — Plan 02 did not execute'
assert ('No regressions found' in reg_section or 'REG-' in reg_section), 'Must confirm no regressions or list REG-NN findings'
assert '| FIXED' in reg_section or '| REGRESSION' in reg_section or '| ALREADY-FOUND' in reg_section or '| SUPERSEDED' in reg_section, 'Missing status column in spot-check table'
print('All checks passed')
"</automated>
  </verify>

  <done>
    - The "## Regression Findings" stub in the consolidated document is replaced with actual spot-check results
    - All 7 priority checks are documented with FIXED / REGRESSION / SUPERSEDED / ALREADY-FOUND status
    - Any regressions found are added as REG-NN findings in standard finding format
    - The consolidated document is now the complete v17.1 deliverable with no outstanding stubs
  </done>
</task>

</tasks>

<verification>
```bash
python3 -c "
with open('.planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md') as f:
    content = f.read()
if '## Regression Findings' in content:
    reg_section = content[content.index('## Regression Findings'):]
    stub_present = 'To be appended' in reg_section
    print(f'Stub replaced: {not stub_present}')
    print(f'Section length: {len(reg_section)} chars')
    print('PASS' if not stub_present and len(reg_section) > 150 else 'FAIL')
else:
    print('FAIL: no Regression Findings section')
"
wc -l .planning/phases/178-consolidation-regression-check/v17.1-comment-findings-consolidated.md
```
</verification>

<success_criteria>
- Every v3.1/v3.5 priority LOW finding has been spot-checked against current source
- Regression check results are documented in the consolidated file (no regressions, or REG-NN entries for each regression)
- The consolidated document is the complete v17.1 deliverable: Phase 175-177 findings + regression results
- No stub placeholder ("To be appended") remains in the document
</success_criteria>

<output>
After completion, create `.planning/phases/178-consolidation-regression-check/178-02-SUMMARY.md`
</output>
