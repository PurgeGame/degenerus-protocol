---
phase: 175-game-module-comment-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in LootboxModule and MintStreakUtils has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "175-03-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md"
      provides: "Comment audit findings for LootboxModule and MintStreakUtils"
      contains: "CMT-01"
  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "LootboxModule"
    - from: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "MintStreakUtils"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusGameLootboxModule (1778 lines) and
DegenerusGameMintStreakUtils (173 lines) against actual code behavior. Log every discrepancy
as a LOW or INFO finding with contract name, line reference, and plain-English description.

Purpose: LootboxModule is the third-largest module and was last swept at v3.5. MintStreakUtils
is small but received the affiliate bonus cache reader (Phase 173) — a new cache-hit path that
any existing comment on `_playerActivityScore` may now mis-describe. Output: 175-03-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusGameLootboxModule.sol comments</name>
  <read_first>
    contracts/modules/DegenerusGameLootboxModule.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameLootboxModule.sol in full (1778 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify that the comment
accurately describes the code immediately below or around it. Focus areas given changes since v3.5:

1. **Redemption lootbox (v3.4)** — Added in v3.4. Any comments describing the redemption lootbox
   flow, eligibility conditions, or payout structure. Verify they match the actual code paths.

2. **BURNIE endgame gate (v11.0)** — Any comment referencing lootbox availability after game end,
   or burn requirements for lootbox claims. Verify described conditions match actual `if` branches.

3. **Activity score contributions from lootbox** — Any comment on how lootbox events affect
   `_playerActivityScore`. Verify described scoring behavior matches actual calls and weights.

4. **Level quest triggering from lootbox** — Any comment referencing `_processQuestCompletion`
   or quest-related logic in the lootbox path. Verify trigger conditions described are accurate.

5. **Storage repack (v16.0)** — currentPrizePool was downsized to uint128 in v16.0 storage repack.
   Any comment that states it is uint256 or references a 256-bit prize pool variable would be stale.
   Verify any comment about prizePool, currentPrizePool, or prizePoolsPacked matches the current
   storage type and layout.

6. **HAS_DEITY_PASS_SHIFT read** — Around line 1040, there is a mintPacked_ read for deity pass.
   Verify the comment (if any) accurately describes the bit used (bit 184) and the purpose.

7. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify parameter names,
   return descriptions, and @notice summaries are accurate.

8. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// Revert if`, etc. Verify
   these explanatory comments still accurately describe the current code branch behavior.

For each discrepancy, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusGameLootboxModule.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Write findings to `.planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md`
under the section heading `## DegenerusGameLootboxModule`.

If no discrepancy exists for a section, no finding is needed. Do not force findings.
  </action>
  <verify>
    <automated>grep -c "DegenerusGameLootboxModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md 2>/dev/null && echo "section present"</automated>
  </verify>
  <acceptance_criteria>
    - 175-03-FINDINGS.md exists and contains a `## DegenerusGameLootboxModule` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings: section contains "No discrepancies found"
    - The full 1778-line contract has been read (not just a sample)
    - Storage repack (uint128 currentPrizePool) has been explicitly checked
  </acceptance_criteria>
  <done>DegenerusGameLootboxModule fully swept. All comment discrepancies logged as LOW/INFO
findings with line references. Section written to 175-03-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DegenerusGameMintStreakUtils.sol comments and finalize</name>
  <read_first>
    contracts/modules/DegenerusGameMintStreakUtils.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameMintStreakUtils.sol in full (173 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify that the comment
accurately describes the code immediately below or around it. Focus areas given changes since v3.5:

1. **Affiliate bonus cache reader (Phase 173)** — Lines ~136-146 contain the new cache-read path
   for `_playerActivityScore`. The cache reads AFFILIATE_BONUS_LEVEL_SHIFT and
   AFFILIATE_BONUS_POINTS_SHIFT from mintPacked_. Verify:
   - The comment above the cache block (if any) accurately describes the cache-hit vs cache-miss
     logic: hit when `cachedLevel == currLevel`, miss falls through to live `affiliateBonusPointsBest`
   - The comment does NOT incorrectly describe the path as always calling the affiliate contract
   - The comment does NOT omit mention of the fallback live-lookup on miss

2. **Activity score components** — Any comment describing the components of `_playerActivityScore`
   (mintScore, streakScore, burnScore, affiliateScore) and how they are combined. Verify the
   described formula and component weights match the actual code.

3. **_updateMintStreak function** — NatSpec and inline comments. Verify the described update
   conditions (when streak increments vs resets), the bit packing of MINT_STREAK_LAST_COMPLETED_SHIFT,
   and the described day/level calculations match the actual code.

4. **Frozen level handling** — Any comment describing what happens when a player is frozen
   (FROZEN_UNTIL_LEVEL_SHIFT). Verify described behavior matches actual `if` branch.

5. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify parameter names
   match actual signatures and return descriptions match actual returns.

For each discrepancy, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusGameMintStreakUtils.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append findings to `.planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md`
under the section heading `## DegenerusGameMintStreakUtils`.

After writing both sections, prepend a header at the TOP of the file:

```
# Phase 175 Comment Audit — Plan 03 Findings
**Contracts:** DegenerusGameLootboxModule, DegenerusGameMintStreakUtils
**Requirement:** CMT-01
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md && grep "MintStreakUtils" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 175-03-FINDINGS.md contains a `## DegenerusGameMintStreakUtils` section
    - 175-03-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), comment says, code does
    - If zero findings for MintStreakUtils: section contains "No discrepancies found"
    - The affiliate bonus cache reader (lines ~136-146) has been explicitly checked:
      either a finding exists there, or a note that the comment is accurate is present
    - File is self-contained — a reader understands each finding without opening the contract
  </acceptance_criteria>
  <done>DegenerusGameMintStreakUtils fully swept. All comment discrepancies logged. 175-03-FINDINGS.md
complete with header summary and both contract sections.</done>
</task>

</tasks>

<verification>
175-03-FINDINGS.md exists with:
- A header section naming both contracts and providing a finding count summary
- A `## DegenerusGameLootboxModule` section (findings or "No discrepancies found")
- A `## DegenerusGameMintStreakUtils` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- LootboxModule (1778 lines) swept end-to-end
- MintStreakUtils (173 lines) swept end-to-end
- Affiliate bonus cache reader (Phase 173) in MintStreakUtils explicitly verified
- Storage repack (uint128 prizePool) impact in LootboxModule explicitly checked
- 175-03-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/175-game-module-comment-sweep/175-03-SUMMARY.md`
</output>
