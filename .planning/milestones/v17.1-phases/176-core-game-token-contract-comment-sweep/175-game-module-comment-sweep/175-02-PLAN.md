---
phase: 175-game-module-comment-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in JackpotModule has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "175-02-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md"
      provides: "Comment audit findings for JackpotModule"
      contains: "CMT-01"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "JackpotModule"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusGameJackpotModule (2813 lines) against actual
code behavior. Log every discrepancy as a LOW or INFO finding with contract name, line reference,
and plain-English description of what the comment says vs what the code does.

Purpose: JackpotModule is the largest game module and absorbed runRewardJackpots from the deleted
EndgameModule (v16.0), plus received the BURNIE endgame gate logic and activity score changes.
It gets its own plan because 2813 lines at thorough comment-sweep pace fills significant context.
Output: 175-02-FINDINGS.md with all comment discrepancies found in JackpotModule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusGameJackpotModule.sol — first half (lines 1-1400)</name>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameJackpotModule.sol in full. This task covers the first half of
the contract (approximately lines 1-1400). For every comment (single-line `//`, block `/* */`,
NatSpec `///` / `/** */`), verify the comment accurately describes the code around it.

Focus areas given changes since v3.5:

1. **runRewardJackpots absorbed from EndgameModule (v16.0)** — This function (or its callsite)
   was moved from the deleted EndgameModule into JackpotModule. Verify:
   - NatSpec @notice and @dev accurately describe what it does in its current location
   - Any comment referencing "EndgameModule" or "endgame" is either still accurate or is now stale
   - @param and @return tags match the function signature as it exists now in JackpotModule

2. **BURNIE endgame gate (v11.0)** — Any comment describing the conditions under which jackpot
   rewards are distributed at game end. Verify the described burnRequired check, BURNIE burn
   threshold, and game-over flag conditions match the actual `if` branches in the code.

3. **Daily jackpot chunk removal (v4.2)** — Any comment that still references "daily chunk",
   "daily jackpot distribution", or time-chunked payout logic. These were removed in v4.2 and
   would be stale comments if still present.

4. **Jackpot bucket packing (prizePoolsPacked)** — Any comment describing the packed storage layout
   of jackpot prize pools (from v4.3). Verify bit ranges and bucket indexing described in comments
   match what JackpotBucketLib actually implements.

5. **Activity score contributions** — Any comment on how jackpot wins affect `_playerActivityScore`.
   Verify the described scoring behavior matches the actual calls.

6. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag in lines 1-1400.
   Verify parameter names match actual signatures, return descriptions match actual returns,
   and @notice summaries do not omit significant side effects.

For each discrepancy, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusGameJackpotModule.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Write findings to `.planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md`
under the section heading `## DegenerusGameJackpotModule — Lines 1-1400`.

If a focus area has no discrepancy, no finding is needed. Do not force findings.
  </action>
  <verify>
    <automated>grep -c "DegenerusGameJackpotModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md 2>/dev/null && echo "section present"</automated>
  </verify>
  <acceptance_criteria>
    - 175-02-FINDINGS.md exists and contains a section for JackpotModule lines 1-1400
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings in this range: section contains "No discrepancies found in lines 1-1400"
    - runRewardJackpots function and BURNIE endgame gate sections have been explicitly checked
  </acceptance_criteria>
  <done>JackpotModule lines 1-1400 swept. All comment discrepancies logged as LOW/INFO findings
with line references. Written to 175-02-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DegenerusGameJackpotModule.sol — second half (lines 1401-end) and finalize</name>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
  </read_first>
  <action>
Continue reading contracts/modules/DegenerusGameJackpotModule.sol covering the second half
(approximately lines 1401 through end, ~1400 more lines). Apply the same comment-vs-code
verification as Task 1.

Additional focus areas for the second half:

1. **Jackpot carryover logic** — Any comment describing how unclaimed jackpot funds carry over
   to the next round. Verify the described carryover conditions and amounts match the actual
   computation.

2. **Whale jackpot path** — Any comment describing special jackpot treatment for whale bundles
   or whale-level players. Verify conditions described match actual `if` branches.

3. **Payout calculations** — Any comment describing ETH distribution percentages, fee splits,
   or prize pool draws. Verify the described math matches what the code computes. Cross-check
   against any hardcoded basis points or percentage constants cited in comments.

4. **Quest integration in jackpot path** — Any comment referencing quest completion triggered
   from jackpot events. Verify described trigger conditions are accurate.

5. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag in lines 1401 to end.

For each discrepancy, record:
- Severity: LOW or INFO
- Location: `DegenerusGameJackpotModule.sol` line N (or N-M)
- What the comment says / what the code does

Append findings to `.planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md`
under the section heading `## DegenerusGameJackpotModule — Lines 1401-end`.

After writing both sections, prepend a header at the TOP of the file:

```
# Phase 175 Comment Audit — Plan 02 Findings
**Contract:** DegenerusGameJackpotModule
**Requirement:** CMT-01
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md && grep "Lines 1401" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-02-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 175-02-FINDINGS.md contains a section for JackpotModule lines 1401-end
    - 175-02-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), comment says, code does
    - If zero findings in second half: section contains "No discrepancies found in lines 1401-end"
    - File is self-contained — a reader understands each finding without opening the contract
    - Both line-range sections present in the file
  </acceptance_criteria>
  <done>JackpotModule swept end-to-end across both halves. 175-02-FINDINGS.md complete with
header summary and both line-range sections.</done>
</task>

</tasks>

<verification>
175-02-FINDINGS.md exists with:
- A header section naming JackpotModule and providing a finding count summary
- A section for lines 1-1400 (findings or "No discrepancies found")
- A section for lines 1401-end (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- JackpotModule (2813 lines) swept end-to-end
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 175-02-FINDINGS.md is self-contained and reviewable without opening source contracts
- runRewardJackpots (absorbed from EndgameModule) and BURNIE endgame gate comment
  accuracy explicitly verified
</success_criteria>

<output>
After completion, create `.planning/phases/175-game-module-comment-sweep/175-02-SUMMARY.md`
</output>
