---
phase: 176-core-game-token-contract-comment-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md
autonomous: true
requirements:
  - CMT-03
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in DegenerusStonk, StakedDegenerusStonk, and GNRUS has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "The gambling burn system in StakedDegenerusStonk has been explicitly verified for comment accuracy"
    - "176-03-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md"
      provides: "Comment audit findings for DegenerusStonk, StakedDegenerusStonk, and GNRUS"
      contains: "CMT-03"
  key_links:
    - from: "contracts/DegenerusStonk.sol"
      to: ".planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "DegenerusStonk"
    - from: "contracts/StakedDegenerusStonk.sol"
      to: ".planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "StakedDegenerusStonk"
    - from: "contracts/GNRUS.sol"
      to: ".planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "GNRUS"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusStonk (359 lines), StakedDegenerusStonk
(874 lines), and GNRUS (547 lines) against actual code behavior. Log every discrepancy as a
LOW or INFO finding with contract name, line reference, and plain-English description of what
the comment says vs what the code does.

Purpose: StakedDegenerusStonk (sDGNRS) contains the gambling burn system introduced after the
v3.5 sweep — comments describing burn mechanics, staking/unstaking flows, and vault interactions
need verification. DegenerusStonk (DGNRS) is the underlying token; its comments about staking
delegation and transfer restrictions must be checked. GNRUS is a governance token; its NatSpec
about voting power and supply constraints needs verification.
Output: 176-03-FINDINGS.md with all comment discrepancies found in these three contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusStonk.sol and GNRUS.sol comments</name>
  <read_first>
    contracts/DegenerusStonk.sol
    contracts/GNRUS.sol
  </read_first>
  <action>
Read contracts/DegenerusStonk.sol in full (359 lines) and contracts/GNRUS.sol in full (547 lines).
For every comment (single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify that
the comment accurately describes the code immediately below or around it.

**DegenerusStonk.sol (359 lines) focus areas:**

1. **Staking delegation comments** — DegenerusStonk tokens are staked into StakedDegenerusStonk.
   Verify any comment describing how staking is initiated, what transfers are gated, or how the
   token interacts with sDGNRS is accurate.

2. **Transfer restriction comments** — If there are comments describing transfer restrictions
   (e.g., "non-transferable while staked", "requires approval from staking contract"), verify
   these match the actual `_beforeTokenTransfer` or `_transfer` hooks.

3. **Mint/burn access control** — Comments describing who can mint or burn DGNRS. Verify against
   actual modifiers and require checks (game contract, vault, operator, etc.).

4. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify parameter
   names, descriptions, and return values match actual code.

**GNRUS.sol (547 lines) focus areas:**

1. **Governance/voting power comments** — Any comment describing how GNRUS affects voting power,
   governance proposals, or quorum calculations. Verify against actual ERC20Votes or similar
   integration if present.

2. **Supply cap comments** — Any comment describing a max supply or supply constraint. Verify
   the described cap matches the actual enforcement in mint functions.

3. **Mint/burn access control** — Comments describing who can mint or burn GNRUS. Verify against
   actual modifiers and require checks.

4. **Token math** — Any comment describing how GNRUS is earned, distributed, or converted.
   Verify the described formula or rate matches the actual computation.

5. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify accuracy.

For each discrepancy found in either contract, record:
- Severity: LOW (misleads a reader about behavior, access control, or token math) or
  INFO (minor imprecision, typo, stale wording)
- Location: `DegenerusStonk.sol` or `GNRUS.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

If no discrepancy exists for a contract, no finding is needed. Do not force findings.

Write all findings to `.planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md`:
- DegenerusStonk findings under `## DegenerusStonk`
- GNRUS findings under `## GNRUS`
  </action>
  <verify>
    <automated>grep -c "DegenerusStonk\|GNRUS" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md 2>/dev/null && echo "sections present"</automated>
  </verify>
  <acceptance_criteria>
    - 176-03-FINDINGS.md exists and contains a `## DegenerusStonk` section
    - 176-03-FINDINGS.md exists and contains a `## GNRUS` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings for a contract: the section contains "No discrepancies found"
    - Both full contracts have been read (all 359 + 547 lines), not just sampled
  </acceptance_criteria>
  <done>DegenerusStonk and GNRUS fully swept. All comment discrepancies logged as LOW/INFO findings
with line references. Sections written to 176-03-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep StakedDegenerusStonk.sol comments</name>
  <read_first>
    contracts/StakedDegenerusStonk.sol
  </read_first>
  <action>
Read contracts/StakedDegenerusStonk.sol in full (874 lines). For every comment (single-line `//`,
block `/* */`, and NatSpec `///` / `/** */`), verify that the comment accurately describes the
code immediately below or around it. Focus areas:

1. **Gambling burn system** — StakedDegenerusStonk contains a gambling burn mechanic where staked
   tokens can be burned based on game outcomes. Verify:
   - Comments describing when burns are triggered (specific game events, VRF outcomes, thresholds)
     match the actual trigger conditions in the code
   - Comments describing the burn amount formula or percentage match the actual computation
   - Comments describing who can trigger burns (game contract, vault, operator) match actual checks
   - Any comment saying burns are "optional" or "guaranteed" matches the actual logic

2. **Staking and unstaking mechanics** — Comments describing the staking deposit flow (DGNRS in,
   sDGNRS out), the unstaking flow (sDGNRS in, DGNRS out), and any lockup or cooldown period.
   Verify these match the actual logic including any time-lock checks.

3. **Reward distribution comments** — Any comment describing how ETH or token rewards are
   distributed to stakers (per-share accounting, pro-rata, etc.). Verify the described formula
   and accounting matches the actual code.

4. **Vault interaction comments** — StakedDegenerusStonk interacts with DegenerusVault. Any
   comment describing how funds move between sDGNRS and the vault (e.g., "sends ETH to vault
   on burn", "receives ETH from vault on reward") must be verified against actual call sites.
   Note: prior finding W05-01/G05-01 (Phase 175) found GameOverModule sends to SDGNRS not DGNRS
   — check that sDGNRS-side comments about receiving from GameOverModule are accurate.

5. **Share-based accounting comments** — If sDGNRS uses ERC4626 or a similar share-based model,
   verify comments about the exchange rate between shares and underlying assets are accurate.

6. **Access control comments** — Comments describing which addresses can call privileged functions
   (stake, unstake, triggerBurn, distributeRewards, etc.). Verify against actual modifiers.

7. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify parameter
   names, descriptions, and return values match actual code.

8. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// per-share`, etc. Verify
   these are accurate given the current code.

For each discrepancy, record:
- Severity: LOW (misleads about burn mechanics, staking, rewards, or access control) or
  INFO (minor imprecision, stale wording, typo)
- Location: `StakedDegenerusStonk.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append all findings to `.planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md`
under the section heading `## StakedDegenerusStonk`.

After writing all three sections, add a final header to the file:

```
# Phase 176 Comment Audit — Plan 03 Findings
**Contracts:** DegenerusStonk, GNRUS, StakedDegenerusStonk
**Requirement:** CMT-03
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```

Place this header at the TOP of the file (above the per-contract sections).
  </action>
  <verify>
    <automated>grep -c "StakedDegenerusStonk" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md 2>/dev/null && grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 176-03-FINDINGS.md contains a `## StakedDegenerusStonk` section
    - 176-03-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), "comment says", "code does"
    - If zero findings for StakedDegenerusStonk: section contains "No discrepancies found"
    - The gambling burn system comments have been explicitly checked (finding exists or accuracy confirmed)
    - Vault interaction comments have been explicitly checked, cross-referenced against the
      Phase 175 W05-01/G05-01 finding that GameOverModule sends to SDGNRS not DGNRS
    - File is self-contained — a reader can understand each finding without opening the contract
  </acceptance_criteria>
  <done>StakedDegenerusStonk fully swept. All comment discrepancies logged. 176-03-FINDINGS.md
complete with header summary and all three contract sections.</done>
</task>

</tasks>

<verification>
176-03-FINDINGS.md exists with:
- A header section naming all three contracts and providing a finding count summary
- A `## DegenerusStonk` section (findings or "No discrepancies found")
- A `## GNRUS` section (findings or "No discrepancies found")
- A `## StakedDegenerusStonk` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- DegenerusStonk (359 lines) swept end-to-end
- GNRUS (547 lines) swept end-to-end
- StakedDegenerusStonk (874 lines) swept end-to-end with explicit focus on gambling burn
  system comment accuracy and vault interaction cross-reference
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 176-03-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/176-core-game-token-contract-comment-sweep/176-03-SUMMARY.md`
</output>
