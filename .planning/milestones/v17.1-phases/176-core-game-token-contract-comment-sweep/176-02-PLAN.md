---
phase: 176-core-game-token-contract-comment-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md
autonomous: true
requirements:
  - CMT-03
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in BurnieCoin and BurnieCoinflip has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "The creditor expansion (v10.1) and mintForGame merger changes in BurnieCoinflip have been explicitly verified"
    - "176-02-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md"
      provides: "Comment audit findings for BurnieCoin and BurnieCoinflip"
      contains: "CMT-03"
  key_links:
    - from: "contracts/BurnieCoin.sol"
      to: ".planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "BurnieCoin"
    - from: "contracts/BurnieCoinflip.sol"
      to: ".planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "BurnieCoinflip"
---

<objective>
Sweep all inline comments and NatSpec in BurnieCoin (717 lines) and BurnieCoinflip (1159 lines)
against actual code behavior. Log every discrepancy as a LOW or INFO finding with contract name,
line reference, and plain-English description of what the comment says vs what the code does.

Purpose: BurnieCoinflip is the most-changed token contract since the v3.5 sweep. v10.1 expanded
the creditor list (allowing multiple creditors, not just one), and the mintForGame function was
merged with the primary mint path. BurnieCoin is the ERC-20 for the BURNIE token used as the
endgame gate in v11.0 — verify its NatSpec and mint/burn access control comments are accurate.
Output: 176-02-FINDINGS.md with all comment discrepancies found in BurnieCoin + BurnieCoinflip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep BurnieCoin.sol comments</name>
  <read_first>
    contracts/BurnieCoin.sol
  </read_first>
  <action>
Read contracts/BurnieCoin.sol in full (717 lines). For every comment (single-line `//`,
block `/* */`, and NatSpec `///` / `/** */`), verify that the comment accurately describes
the code immediately below or around it. Focus areas:

1. **Mint and burn access control** — v11.0 added BurnieCoin as the BURNIE endgame gate token.
   Verify any comment describing who can call mint or burn (e.g., "only game contract",
   "only minter role") accurately reflects the actual `require` checks, modifiers, or role checks.

2. **Token math and supply comments** — Any comment describing totalSupply, decimals, max supply,
   or how minting/burning affects the supply. Verify numeric values and formulas are accurate.

3. **Event comments** — Any comment describing which events are emitted. Verify against actual
   `emit` statements.

4. **NatSpec on all public/external functions** — Every `@notice`, `@param`, `@return`, `@dev` tag.
   Verify:
   - `@param` names and descriptions match actual parameter names
   - `@return` descriptions match what is actually returned
   - `@notice` summaries do not omit significant side effects (e.g., "only burns from caller"
     when the function actually burns from an arbitrary address)
   - `@dev` notes about access control or integration contracts are accurate

5. **Constructor and initialization comments** — Comments describing what is set at deploy time
   and which addresses have elevated permissions. Verify these match the actual constructor.

6. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// minter`, etc. Verify these
   are accurate.

For each discrepancy found, record:
- Severity: LOW (misleads a reader about behavior, access control, or token math) or
  INFO (minor imprecision, typo, stale wording)
- Location: `BurnieCoin.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

If no discrepancy exists for a section, no finding is needed. Do not force findings.

Write all findings to `.planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md`
under the section heading `## BurnieCoin`.
  </action>
  <verify>
    <automated>grep -c "BurnieCoin" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md 2>/dev/null && echo "section present"</automated>
  </verify>
  <acceptance_criteria>
    - 176-02-FINDINGS.md exists and contains a `## BurnieCoin` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings: the section contains "No discrepancies found" — not just an empty section
    - Mint/burn access control comments have been explicitly checked (finding exists or accuracy confirmed)
    - The full 717-line contract has been read (not just a sample)
  </acceptance_criteria>
  <done>BurnieCoin fully swept. All comment discrepancies logged as LOW/INFO findings with line
references. Section written to 176-02-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep BurnieCoinflip.sol comments</name>
  <read_first>
    contracts/BurnieCoinflip.sol
  </read_first>
  <action>
Read contracts/BurnieCoinflip.sol in full (1159 lines). For every comment (single-line `//`,
block `/* */`, and NatSpec `///` / `/** */`), verify that the comment accurately describes
the code immediately below or around it. Focus areas given changes since v3.5:

1. **Creditor expansion (v10.1)** — The creditor model was expanded to allow multiple creditors
   (not just one). Verify:
   - Any comment describing the creditor as singular ("the creditor", "a single creditor") when
     the code handles an array or mapping of creditors is a discrepancy
   - Any comment describing creditor registration, limit, or iteration that predates the expansion
   - NatSpec on creditor-related functions accurately describes the multi-creditor behavior

2. **mintForGame merger** — The `mintForGame` function was merged into the primary mint path.
   Verify:
   - No comment still describes `mintForGame` as a separate entry point if it was merged
   - Any comment on the mint path accurately describes the unified logic after the merge
   - If `mintForGame` still exists as a function but delegates internally, the comment should
     reflect that it is now a thin wrapper, not a separate code path

3. **VRF / RNG integration comments** — BurnieCoinflip uses VRF for coinflip outcomes. Verify:
   - Any comment describing the VRF request/fulfillment flow is accurate
   - Comments describing what player state is locked during a pending VRF request are correct
   - Comments describing which VRF word is consumed for the coinflip are accurate

4. **Payout math comments** — Any comment describing the coinflip payout formula, odds, or
   token distribution. Verify the described math matches the actual computation.

5. **Access control comments** — Comments describing who can call each function (game contract,
   operator, player, creditors). Verify against actual modifiers and require checks.

6. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify parameter
   names, descriptions, and return values match actual code.

7. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// creditor:`, etc. Verify
   these are accurate given the current code.

For each discrepancy, record:
- Severity: LOW (misleads about behavior, creditors, payout math, or access control) or
  INFO (minor imprecision, stale wording, typo)
- Location: `BurnieCoinflip.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append all findings to `.planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md`
under the section heading `## BurnieCoinflip`.

After writing both sections, add a final header to the file:

```
# Phase 176 Comment Audit — Plan 02 Findings
**Contracts:** BurnieCoin, BurnieCoinflip
**Requirement:** CMT-03
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```

Place this header at the TOP of the file (above the per-contract sections).
  </action>
  <verify>
    <automated>grep -c "BurnieCoinflip" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md 2>/dev/null && grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/176-core-game-token-contract-comment-sweep/176-02-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 176-02-FINDINGS.md contains a `## BurnieCoinflip` section
    - 176-02-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), "comment says", "code does"
    - If zero findings for BurnieCoinflip: section contains "No discrepancies found"
    - The creditor expansion (v10.1) and mintForGame merger have been explicitly checked
      (either findings exist, or notes confirm accuracy)
    - File is self-contained — a reader can understand each finding without opening the contract
  </acceptance_criteria>
  <done>BurnieCoinflip fully swept. All comment discrepancies logged. 176-02-FINDINGS.md complete
with header summary and both contract sections.</done>
</task>

</tasks>

<verification>
176-02-FINDINGS.md exists with:
- A header section naming both contracts and providing a finding count summary
- A `## BurnieCoin` section (findings or "No discrepancies found")
- A `## BurnieCoinflip` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- BurnieCoin (717 lines) swept end-to-end with explicit focus on mint/burn access control accuracy
- BurnieCoinflip (1159 lines) swept end-to-end with explicit focus on creditor expansion (v10.1)
  and mintForGame merger comment accuracy
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 176-02-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/176-core-game-token-contract-comment-sweep/176-02-SUMMARY.md`
</output>
