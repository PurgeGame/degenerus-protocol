<!-- ARCHIVED 2026-06-04 — v58.0 SHIPPED (document-only audit). Closure signal MILESTONE_V58_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51 (audited frozen contract subject 2b26ec91; baseline = v57.0 closure HEAD b7237db4). All 22 v58.0 requirements were RE-ATTESTED at the Phase 369 TERMINAL close per audit/FINDINGS-v58.0.md (chmod 444); the per-requirement traceability TABLE below is COMPLETE (22/22 ✅ Attested). Outcome: the 5 Claude analysis phases (364-368) found 0 findings on their own surfaces; the XMODEL Gemini+Codex council surfaced 7 CONFIRMED findings (1 CRIT · 1 HIGH · 2 MED · 2 LOW · 1 INFO) + 5 REFUTED on adjacent surfaces — all document-only, routed to a LATER remediation milestone (no contracts/*.sol change in v58.0). -->

# Requirements — v58.0 RNG-Safety, ETH-Flow & Gas-Ceiling Documentation Audit

**Defined:** 2026-06-04
**Core Value:** Every finding a C4A warden could submit is identified and either fixed or documented as known before the audit begins.

> **Audit baseline → subject:** the v57.0 closure HEAD. **Contract subject FROZEN at `2b26ec91`** (the last `contracts/*.sol`-touching commit; `b7237db4` closure + `0d5d2afe` archive sit on top but are docs-only → identical contracts). Baseline closure signal `MILESTONE_V57_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`. Phase 363 PREP-01 pins `FROZEN_SHA` precisely. Every cited `file:line` MUST be re-attested against the frozen HEAD before use (the `audit-v52/` context-pack refs predate the AfKing.sol→GameAfkingModule dissolution and are stale).
>
> **Scope (USER-locked 2026-06-04, reshaped from `.planning/PLAN-V58-CONSOLIDATED-AUDIT.md`):** a DOCUMENT-ONLY, autonomous, resumable audit over the **full cumulative v50→v57 surface** producing FIVE artifacts — (1) a **Player-Function RNG-Safety** page (what each player-facing function does + WHY it cannot influence any RNG-dependent variable, + a sanity/comment check that flags behavior↔name/comment mismatches as findings); (2) an **RNG-Variable Freeze** table (every RNG-interacting variable + why it cannot change between RNG request and resolution); (3) a full **ETH-Flow chart** (inflow → pool cycling → outflow); (4) an **advanceGame Gas-Ceiling** table (theoretical max gas per stage vs the 16.7M cap); (5) a side-track **BURNIE ticket-mint forensic**. Packaged as ONE cohesive dark-themed static site (`audit/site/`).
>
> **External corroboration (retained, budget-aware):** ~5 targeted area prompts × 2 models (Gemini + Codex, $20-tier) as a SECOND OPINION only on the load-bearing axes; Claude adjudicates + synthesizes. NOT the per-cell council.
>
> **Posture (USER-locked 2026-06-04):** Document-only — any fix routes to a LATER milestone. **NO contract-commit gate** — this milestone changes NO `contracts/*.sol` (analysis + HTML + markdown only) → no HARD STOP, runs fully hands-off. **One cohesive static site** (`audit/site/`). **v50/v51 FINDINGS backfill DROPPED** from this run (carried forward). Runs AUTONOMOUSLY + RESUMES GRACEFULLY on any usage-limit/error; paced to ~45% of a weekly allotment over 24h WITHOUT slamming a 5h cap mid-task (`concurrency: 1`, small batches, frequent persist, per-phase commits, spine-weighted). The drafted plan's TOP track (the AfKing reward-leak hunt) folds into the RNG-safety + ETH-flow + solvency tracks rather than leading. No research (pure in-repo code analysis). Phase numbering continues from 362 → v58.0 starts at Phase 363.

---

## v58.0 Requirements

### PREP — Scope Freeze, Inventory & Run Scaffold
- [x] **PREP-01**: `FROZEN_SHA` is pinned to the v57.0 contract-frozen HEAD (`2b26ec91`; later commits are docs-only), and the `audit-v52/` context-pack is rebuilt + re-attested against it — `scope.txt` (drop `AfKing.sol`; add `GameAfkingModule.sol` + `DegenerusGameBingoModule.sol`), `targets.md`, `hot-surfaces.md` — with EVERY cited `file:line` verified against the frozen tree (the existing refs predate the AfKing dissolution and are wrong).
- [x] **PREP-02**: The raw inventory for the four web deliverables is enumerated against the frozen HEAD and logged as the source-of-truth list each section is built from: (a) every player-facing (external/public, user-callable, non-admin) function across all contracts; (b) every state variable that feeds/interacts with an RNG-derived output; (c) every ETH inflow + pool + outflow site; (d) the ordered stages of `advanceGame`. Enumeration gaps are flagged explicitly (no silent truncation).
- [x] **PREP-03**: The cohesive dark-themed static site is scaffolded under `audit/site/` — vanilla HTML/CSS/JS, shared nav/tabs across the four sections, a searchable/filterable table component, NO build step / NO framework / NO external CDN (opens from a local file). The run scaffold is locked: the `coordinator.sh`/harness config + the checkpoint-and-persist convention that makes the run resumable.

### RNGSAFE — Player-Function RNG-Safety + Sanity/Comment Check (Deliverable 1)
- [x] **RNGSAFE-01**: Every player-facing function has a site row — contract, signature, plain-English "what it does", and its access/gating (`rngLockedFlag` / `gameOver` / `onlyOwner` / permissionless). Coverage is COMPLETE against the PREP-02(a) enumeration (0 unlisted player-facing functions; any deliberate exclusion, e.g. pure admin, is noted with reason).
- [x] **RNGSAFE-02**: Each row carries an RNG-safety verdict + reasoning — WHY the function cannot influence any RNG-dependent variable between request and resolution: it writes no RNG-participating slot, OR it reverts under `rngLockedFlag`, OR it only reads post-resolution state — citing the gating code (`file:line`). Any function that CAN touch a participating slot during the freeze window is escalated to a finding (AUDIT-01).
- [x] **RNGSAFE-03**: A sanity + comment check on every row flags any function whose actual behavior diverges from its name / NatSpec / inline comment / evident intent (e.g. a "mint-with-BURNIE" that burns BURNIE but queues zero tickets). Each mismatch is recorded as a finding with severity + a fix sketch; behavior↔comment matches are marked verified.

### RNGVAR — RNG-Variable Freeze Invariant Table (Deliverable 2)
- [x] **RNGVAR-01**: Every state variable that participates in an RNG-derived output is enumerated — entropy inputs to the `abi.encode(Packed)(…)` sites, ticket-selection inputs, weights/buckets (terminal-decimator, lootbox, jackpot), day keys, `rngWordByDay`, the lock flags — each with its declaring contract + slot/struct location and the RNG outputs it feeds.
- [x] **RNGVAR-02**: Each variable carries its freeze proof — WHY it cannot be modified between RNG request (`rngLockedFlag = true`) and resolution: the set of writers is enumerated and each is shown to either revert under the lock or be unreachable in the window, citing `file:line`. Any writer that can mutate a participating slot during the window is escalated to a finding (AUDIT-01).
- [x] **RNGVAR-03**: Rendered as the site's RNG-Variable Freeze section — a searchable table (variable → RNG outputs fed → writers → freeze-gate verdict).

### ETHFLOW — ETH Flow Chart (Deliverable 3)
- [x] **ETHFLOW-01**: Every ETH INFLOW is traced — presale, ticket buys, cover-buys, redemptions-in, any `payable`/`receive` entry — with the entry `file:line` and the destination pool/ledger.
- [x] **ETHFLOW-02**: The internal pool cycling + every OUTFLOW is traced — the `claimablePool` / `_poolOf` / `Pool.*` buckets (Affiliate / Reward / Jackpot / etc.), the consolidation path, and every exit (claims, payouts, redemptions-out, withdrawals, the salvage ETH leg) — with the conservation/solvency invariant noted at each hop.
- [x] **ETHFLOW-03**: Rendered as a full ETH-flow diagram (a readable flow chart — nodes = sources/pools/sinks, edges = labeled transfers) in the site, with a companion table backing each edge to a `file:line`.

### GASCEIL — advanceGame Gas-Ceiling (Deliverable 4)
- [x] **GASCEIL-01**: `advanceGame` is decomposed into its ordered stages (the per-stage work units — subscriber STAGE, resolution, jackpot/decimator, pool consolidation, etc.), each with its bounding loop and the protocol cap that bounds its iteration count.
- [x] **GASCEIL-02**: The THEORETICAL WORST-CASE (max) gas per stage is derived from the measured per-item marginal × the proven iteration ceiling, with the bounding assumptions stated explicitly — worst-case branch, NOT typical seed (per the `gas-measure-worstcase-branch-not-typical-seed` rule).
- [x] **GASCEIL-03**: Rendered as a gas-ceiling table (stage → iteration bound → per-item marginal → worst-case total) with a per-stage verdict against the 16,777,216 (EIP-7825) per-tx cap; any stage that could approach/exceed the cap is escalated to a finding (AUDIT-01).

### FORENSIC — BURNIE Ticket-Mint Regression Forensic (Deliverable 5, side track)
- [x] **FORENSIC-01**: The regression is root-caused — the commit SHA (and the era/refactor) where BURNIE ticket buying (`purchaseCoin` → `_purchaseCoinFor`) stopped queuing tickets while still burning BURNIE (the BURNIE-01 coin-burned-zero-tickets sink, fixed in `980865e8`) is identified via git history (candidate: the phase-160 `_queueTicketsScaled` relocation, or the v47 BURNIE-lootbox-removal rework).
- [x] **FORENSIC-02**: HOW it went unnoticed is explained (no test asserted post-buy ticket-queue growth on the coin path) + the regression test that would have caught it is recommended. Delivered as a short root-cause note (the commit SHA + why no test caught it + the test recommendation).

### XMODEL — Targeted External Corroboration
- [x] **XMODEL-01**: ~5 area prompts × 2 models (Gemini + Codex, $20-tier second-opinion budget) run on the load-bearing axes — (1) RNG-freeze invariant, (2) solvency / value-conservation (the ETH-flow), (3) AfKing / value-leak, (4) v57 money paths (BURNIE / SALVAGE / CANCEL), (5) composition (cross-module interactions) — via the existing `cross-model/` area-result pattern (NOT the per-candidate council). Claude smoke-tests the council first (PONG), adjudicates each external claim, and folds the corroboration/divergence into the artifacts + FINDINGS.

### SITE — Cohesive Site Assembly & Ship
- [x] **SITE-01**: The four analysis sections (RNGSAFE, RNGVAR, ETHFLOW, GASCEIL) are assembled into ONE cohesive navigable site under `audit/site/` — shared nav/tabs, consistent dark game aesthetic, searchable tables, the embedded ETH-flow diagram — opening correctly from a local file with no server/build step.

### AUDIT — Findings Consolidation & Closure
- [x] **AUDIT-01**: Every finding surfaced by RNGSAFE-03 / RNGVAR-02 / ETHFLOW-02 / GASCEIL-03 / FORENSIC / XMODEL is consolidated into `audit/FINDINGS-v58.0.md` (severity-classified, each with a fix sketch routed to a LATER milestone — document-only), and the milestone closes via the established atomic closure flip (STATE / MILESTONES / ROADMAP / PROJECT) + a `MILESTONE_V58_AT_HEAD_<sha>` signal. All v58.0 requirements are re-attested against the frozen HEAD.

### OPS — Autonomous, Resumable, Budget-Paced Execution (cross-cutting)
- [x] **OPS-01**: The milestone executes AUTONOMOUSLY + hands-off (no user interaction required mid-run) and RESUMES GRACEFULLY after any usage-limit hit or error — every phase checkpoints its partial artifact and commits per-phase (planning is gitignored-local; artifacts under `audit/` persist on disk), so a restart continues from the last checkpoint rather than redoing completed work.
- [x] **OPS-02**: The run is PACED to ~45% of a weekly usage allotment over 24h, NEVER slamming a 5h cap mid-task — `concurrency: 1`, small batches with frequent persist, spine-weighted depth (RNG-freeze + solvency + the AfKing surface first), pausable between work windows.

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Any `contracts/*.sol` change / fix | DOCUMENT-ONLY milestone — every fix (incl. anything the audit surfaces) routes to a later remediation milestone. No contract-commit gate this run. |
| `FINDINGS-v50.0.md` + `FINDINGS-v51.0.md` backfill | USER-dropped from this run (2026-06-04); carried forward to a future consolidated track. |
| The full per-cell cross-model council (13×9 loop-until-dry) | Budget-constrained — external models are a ~5-prompt targeted second opinion only, NOT full breadth. |
| Non-player / admin-only / governance functions in the RNG-Safety page | The page is PLAYER-facing; admin/governance surfaces are noted as excluded with reason, not row-by-row analyzed (covered by prior governance milestones). |
| Re-running the v57 internal sweep | v57 already closed with its own in-milestone 3-skill sweep + `FINDINGS-v57.0.md`; v58 folds the v57 surface into the cumulative artifacts, not a re-close. |

## Traceability

Which phases cover which requirements. Filled at roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| PREP-01 | Phase 363 | ✅ Attested |
| PREP-02 | Phase 363 | ✅ Attested |
| PREP-03 | Phase 363 | ✅ Attested |
| OPS-01 | Phase 363 | ✅ Attested |
| OPS-02 | Phase 363 | ✅ Attested |
| RNGSAFE-01 | Phase 364 | ✅ Attested |
| RNGSAFE-02 | Phase 364 | ✅ Attested |
| RNGSAFE-03 | Phase 364 | ✅ Attested |
| RNGVAR-01 | Phase 365 | ✅ Attested |
| RNGVAR-02 | Phase 365 | ✅ Attested |
| RNGVAR-03 | Phase 365 | ✅ Attested |
| ETHFLOW-01 | Phase 366 | ✅ Attested |
| ETHFLOW-02 | Phase 366 | ✅ Attested |
| ETHFLOW-03 | Phase 366 | ✅ Attested |
| GASCEIL-01 | Phase 367 | ✅ Attested |
| GASCEIL-02 | Phase 367 | ✅ Attested |
| GASCEIL-03 | Phase 367 | ✅ Attested |
| FORENSIC-01 | Phase 368 | ✅ Attested |
| FORENSIC-02 | Phase 368 | ✅ Attested |
| XMODEL-01 | Phase 369 | ✅ Attested |
| SITE-01 | Phase 369 | ✅ Attested |
| AUDIT-01 | Phase 369 | ✅ Attested |

**Coverage:**
- v58.0 requirements: 22 total
- Mapped to phases: 22
- Unmapped: 0 ✓

---

**✅ CLOSURE RE-ATTESTATION (2026-06-04, Phase 369):** all 22 v58.0 requirements re-attested against the frozen HEAD `2b26ec91`. Closure signal `MILESTONE_V58_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`. Outcome: 5 Claude analysis phases = 0 findings on their own surfaces; XMODEL council (RNGSAFE-03/RNGVAR-02/ETHFLOW-02/GASCEIL-03/FORENSIC/XMODEL) surfaced **7 CONFIRMED findings (1 CRIT · 1 HIGH · 2 MED · 2 LOW · 1 INFO) + 5 REFUTED**, consolidated into `audit/FINDINGS-v58.0.md` (chmod 444) with fix sketches routed to a LATER remediation milestone (document-only — no `contracts/*.sol` change). Coverage gaps logged: 2 incomplete Gemini council legs (rng-freeze empty, solvency Plan-Mode refusal).

*Requirements defined: 2026-06-04*
*Last updated: 2026-06-04 — v58.0 SHIPPED (closure flip + 22 reqs re-attested)*
