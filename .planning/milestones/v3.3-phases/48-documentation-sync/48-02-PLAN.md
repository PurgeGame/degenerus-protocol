---
phase: 48-documentation-sync
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - audit/FINAL-FINDINGS-REPORT.md
  - audit/KNOWN-ISSUES.md
  - audit/EXTERNAL-AUDIT-PROMPT.md
  - audit/v3.2-rng-delta-findings.md
  - audit/PAYOUT-SPECIFICATION.html
  - audit/v3.1-findings-consolidated.md
  - audit/v3.1-findings-34-token-contracts.md
  - audit/v3.1-findings-35-peripheral-contracts.md
  - audit/v3.2-findings-consolidated.md
  - audit/v3.2-findings-40-token-contracts.md
  - audit/v3.2-findings-40-core-game-contracts.md
  - audit/v3.1-findings-31-core-game-contracts.md
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "FINAL-FINDINGS-REPORT.md documents the 3 HIGH and 1 MEDIUM findings from v3.3 and their fixes"
    - "KNOWN-ISSUES.md explains gambling burn design mechanics so wardens don't re-report them"
    - "EXTERNAL-AUDIT-PROMPT.md includes the redemption system in scope and mechanics"
    - "PAYOUT-SPECIFICATION.html documents the gambling burn payout path alongside the deterministic path"
    - "v3.2-rng-delta-findings.md notes the new redemption roll RNG consumer"
  artifacts:
    - path: "audit/FINAL-FINDINGS-REPORT.md"
      provides: "v3.3 findings section with CP-08, CP-06, Seam-1, CP-07 fixed status"
      contains: "v3.3"
    - path: "audit/KNOWN-ISSUES.md"
      provides: "Gambling burn design mechanics section"
      contains: "gambling burn"
    - path: "audit/EXTERNAL-AUDIT-PROMPT.md"
      provides: "Redemption system in scope and mechanics"
      contains: "redemption"
    - path: "audit/PAYOUT-SPECIFICATION.html"
      provides: "Gambling burn payout rules"
      contains: "gambling"
  key_links:
    - from: "audit/FINAL-FINDINGS-REPORT.md"
      to: "contracts/StakedDegenerusStonk.sol"
      via: "Finding references point to correct post-fix line numbers"
      pattern: "StakedDegenerusStonk"
    - from: "audit/EXTERNAL-AUDIT-PROMPT.md"
      to: "contracts/StakedDegenerusStonk.sol"
      via: "Code scope includes sDGNRS gambling burn functions"
      pattern: "gambling burn\\|redemption"
---

<objective>
Update all audit documentation to reflect v3.3 gambling burn findings, fixes, and mechanism description (DOC-04).

Purpose: External auditors (C4A wardens) reading the audit docs see the gambling burn mechanism fully described, the 4 fixed findings documented, and the known design mechanics catalogued so they don't waste time re-discovering known behaviors.

Output: Updated audit docs across 12 files covering tier-1 (critical), tier-2 (important), and tier-3 (version stamp) updates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-documentation-sync/48-RESEARCH.md
@.planning/phases/44-delta-audit-redemption-correctness/44-01-SUMMARY.md
@.planning/phases/44-delta-audit-redemption-correctness/44-02-SUMMARY.md
@.planning/phases/44-delta-audit-redemption-correctness/44-03-SUMMARY.md
@.planning/phases/45-invariant-test-suite/45-01-SUMMARY.md
@.planning/phases/47-gas-optimization/47-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-1 audit doc updates (FINAL-FINDINGS, KNOWN-ISSUES, EXTERNAL-AUDIT-PROMPT)</name>
  <files>
    audit/FINAL-FINDINGS-REPORT.md
    audit/KNOWN-ISSUES.md
    audit/EXTERNAL-AUDIT-PROMPT.md
  </files>
  <read_first>
    audit/FINAL-FINDINGS-REPORT.md
    audit/KNOWN-ISSUES.md
    audit/EXTERNAL-AUDIT-PROMPT.md
    .planning/phases/44-delta-audit-redemption-correctness/44-01-SUMMARY.md
    .planning/phases/45-invariant-test-suite/45-01-SUMMARY.md
  </read_first>
  <action>
**FINAL-FINDINGS-REPORT.md updates:**

1. Update the executive summary (line 12-16). Change:
   - `"Overall Assessment: SOUND. No open findings."` stays (findings are fixed)
   - The paragraph starting "All findings identified..." must be rewritten to:
     ```
     All findings identified during the audit have been resolved. v3.3 identified four findings in the gambling burn redemption system (three High, one Medium) -- all fixed in code. Four Low-severity issues from earlier milestones were also fixed (CEI ordering, proposal count overflow, spam-propose griefing, dead code removal). No open findings remain.
     ```

2. Update the Findings section (line 28-29). Replace `"No open findings. All issues identified during the audit were resolved."` with a findings table and detail:

   ```markdown
   ## Findings

   **No open findings.** All issues identified during the audit were resolved in code.

   ### v3.3 Findings (Gambling Burn Redemption System)

   | ID | Severity | Title | Status |
   |----|----------|-------|--------|
   | CP-08 | HIGH | `_deterministicBurnFrom` missing pending redemption deduction | FIXED |
   | CP-06 | HIGH | `_gameOverEntropy` missing `resolveRedemptionPeriod` call | FIXED |
   | Seam-1 | HIGH | `DGNRS.burn()` orphans gambling claim under contract address | FIXED |
   | CP-07 | MEDIUM | Coinflip dependency blocks ETH claim at game boundary | FIXED |

   **CP-08 (HIGH -- FIXED):** `_deterministicBurnFrom` did not subtract `pendingRedemptionEthValue` and `pendingRedemptionBurnie` from the total reserves before computing proportional share. Post-gameOver burns could double-spend ETH/BURNIE already reserved for pending gambling claims. Fix: deduct pending reserves in both `totalMoney` and `totalBurnie` calculations (StakedDegenerusStonk.sol).

   **CP-06 (HIGH -- FIXED):** `_gameOverEntropy` in DegenerusGameAdvanceModule.sol did not call `resolveRedemptionPeriod()`, permanently stranding any gambling burn claims pending at game-over. Fix: added redemption resolution blocks to both VRF and fallback paths in `_gameOverEntropy`, mirroring `rngGate`.

   **Seam-1 (HIGH -- FIXED):** `DGNRS.burn()` during active game submitted a gambling claim recorded under the DGNRS contract address (not the actual user). The claim could never be claimed, trapping the user's share of backing. Fix: `DegenerusStonk.burn()` reverts with `GameNotOver()` during active game. Users must use `burnWrapped()` which correctly routes through sDGNRS.

   **CP-07 (MEDIUM -- FIXED):** `claimRedemption()` required full coinflip resolution before paying any portion. If the coinflip for a period hadn't resolved yet, both ETH and BURNIE were stuck. Fix: split claim -- ETH is always claimable once the period is resolved; BURNIE payout is conditional on coinflip resolution (paid on win, forfeited on loss, deferred if unresolved).
   ```

3. Update the Risk Assessment table (line 45-53). Add a row:
   ```
   | Gambling Burn | **Very Low** | Four findings found and fixed; invariant test suite provides regression coverage. |
   ```

4. Update Scope section (line 59). After the Core Contracts list, the contract count is now more accurate. Add after the existing scope text:
   ```markdown
   **v3.3 Gambling Burn Scope:** StakedDegenerusStonk.sol (gambling burn functions: burn, burnWrapped, claimRedemption, resolveRedemptionPeriod, hasPendingRedemptions), DegenerusStonk.sol (Seam-1 fix: GameNotOver guard), BurnieCoinflip.sol (claimCoinflipsForRedemption), DegenerusGameAdvanceModule.sol (redemption resolution in rngGate and _gameOverEntropy)
   ```

5. Update Tools line (line 65). Add after "27 Foundry harnesses": `, 7 Foundry invariant tests for redemption system`

**KNOWN-ISSUES.md updates:**

6. Add a new section after "## Design Mechanics" with gambling burn design mechanics:

   ```markdown
   **Gambling burn mechanism.** During active game, sDGNRS/DGNRS burns enter a gambling path instead of deterministic redemption. The burn amount is submitted to a pending redemption queue. When the next advanceGame resolves via VRF, a roll (25-175%) is applied to determine the ETH payout multiplier. BURNIE payout is conditional on a separate coinflip result. This is intentional -- it creates an RNG-gated burn with variable outcomes. Post-gameOver, burns revert to deterministic proportional payouts.

   **Split-claim design (CP-07 fix).** `claimRedemption()` pays ETH immediately once the period is resolved, regardless of coinflip state. BURNIE is paid only if the coinflip resolved and won. If the coinflip hasn't resolved yet, ETH is paid and the claim remains open for BURNIE. This prevents ETH from being stuck due to unresolved coinflips.

   **50% supply cap per period.** Each gambling burn period is capped at 50% of current totalSupply. This prevents bank-run scenarios and ensures the RNG roll applies to at most half the supply in any period.

   **RNG-locked burn rejection.** Burns revert with `BurnsBlockedDuringRng` during VRF resolution lock to prevent front-running the RNG outcome.
   ```

**EXTERNAL-AUDIT-PROMPT.md updates:**

7. In "### Core Mechanics" section (around line 30-46), add after line 44 (`- Pull-pattern ETH/stETH withdrawals (no push payments)`):
   ```
   - Gambling burn: during active game, sDGNRS/DGNRS burns enter RNG-gated redemption (roll 25-175% of proportional share); post-gameOver, deterministic proportional payout
   - Redemption lifecycle: submit (burn sDGNRS) -> resolve (advanceGame applies VRF roll) -> claim (ETH always, BURNIE conditional on coinflip)
   - Split-claim: ETH paid immediately on period resolution; BURNIE conditional on coinflip win/loss
   ```

8. In "### Code Scope" section (around line 56-80), update the StakedDegenerusStonk and DegenerusStonk entries:
   - After `- contracts/StakedDegenerusStonk.sol (sDGNRS -- soulbound, holds all reserves and pools)` add: ` + gambling burn redemption system (burn/claim/resolve lifecycle)`
   - After `- contracts/DegenerusStonk.sol (DGNRS -- transferable ERC20 wrapper)` add: ` + GameNotOver guard on burn()`

9. In "### Required Audit Coverage" section, add item 11:
   ```markdown
   11. Gambling Burn Redemption System
   - Verify segregation accounting (pendingRedemptionEthValue/Burnie) never exceeds contract holdings
   - Check split-claim (ETH always, BURNIE conditional) for double-claim vectors
   - Validate 50% supply cap enforcement per period
   - Verify resolveRedemptionPeriod is called in all RNG resolution paths (rngGate and _gameOverEntropy)
   - Check RNG-locked burn rejection timing
   ```

10. In "### Important Context" section (around line 239-251), add:
    ```
    - Gambling burn: `sDGNRS.burn(amount)` during active game submits claim; `sDGNRS.claimRedemption()` after resolution
    - Redemption roll: `(currentWord >> 8) % 151 + 25` gives range 25-175 (applied as percentage)
    - BURNIE gamble: staked as coinflip via `creditFlip`; payout depends on flip win/loss
    - Split-claim: ETH portion always claimable after period resolution; BURNIE requires coinflip resolution
    - DGNRS.burn() reverts with GameNotOver during active game (Seam-1 fix)
    ```
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "CP-08" audit/FINAL-FINDINGS-REPORT.md && grep -c "gambling burn" audit/KNOWN-ISSUES.md && grep -c "redemption" audit/EXTERNAL-AUDIT-PROMPT.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/FINAL-FINDINGS-REPORT.md contains "CP-08" and "CP-06" and "Seam-1" and "CP-07"
    - audit/FINAL-FINDINGS-REPORT.md contains "FIXED" for all 4 findings
    - audit/FINAL-FINDINGS-REPORT.md risk assessment table contains "Gambling Burn"
    - audit/KNOWN-ISSUES.md contains "Gambling burn mechanism"
    - audit/KNOWN-ISSUES.md contains "Split-claim design"
    - audit/KNOWN-ISSUES.md contains "50% supply cap"
    - audit/EXTERNAL-AUDIT-PROMPT.md contains "Gambling burn" in Core Mechanics
    - audit/EXTERNAL-AUDIT-PROMPT.md contains "Gambling Burn Redemption System" in Required Audit Coverage
    - audit/EXTERNAL-AUDIT-PROMPT.md contains "redemption roll" or "(currentWord >> 8) % 151 + 25" in Important Context
  </acceptance_criteria>
  <done>Tier-1 audit docs updated: FINAL-FINDINGS-REPORT.md has v3.3 findings table with fix details, KNOWN-ISSUES.md has gambling burn design mechanics, EXTERNAL-AUDIT-PROMPT.md includes redemption system in scope/mechanics/coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-2 and tier-3 audit doc updates (RNG findings, payout spec, version stamps)</name>
  <files>
    audit/v3.2-rng-delta-findings.md
    audit/PAYOUT-SPECIFICATION.html
    audit/v3.1-findings-consolidated.md
    audit/v3.1-findings-34-token-contracts.md
    audit/v3.1-findings-35-peripheral-contracts.md
    audit/v3.2-findings-consolidated.md
    audit/v3.2-findings-40-token-contracts.md
    audit/v3.2-findings-40-core-game-contracts.md
    audit/v3.1-findings-31-core-game-contracts.md
  </files>
  <read_first>
    audit/v3.2-rng-delta-findings.md
    audit/PAYOUT-SPECIFICATION.html
    audit/v3.1-findings-consolidated.md
    audit/v3.1-findings-34-token-contracts.md
    audit/v3.1-findings-35-peripheral-contracts.md
    audit/v3.2-findings-consolidated.md
    audit/v3.2-findings-40-token-contracts.md
    audit/v3.2-findings-40-core-game-contracts.md
    audit/v3.1-findings-31-core-game-contracts.md
    contracts/StakedDegenerusStonk.sol
  </read_first>
  <action>
**Tier-2: v3.2-rng-delta-findings.md**

1. In the "Scope" line (line 4), append: `, redemption roll RNG consumer added in v3.3`

2. In the "Contracts audited" line (line 6), add `StakedDegenerusStonk.sol` if not already listed.

3. Add a new section before the final `---` or at the end of the document:
   ```markdown
   ## v3.3 Addendum: New RNG Consumer

   **Added:** Redemption roll consumer in DegenerusGameAdvanceModule.sol (rngGate line 773, _gameOverEntropy lines 835 and 864).

   **Operation:** `(currentWord >> 8) % 151 + 25` extracts bits 8+ from the VRF word.

   **Bit safety:** Bits 8+ do not overlap with bit 0 (coinflip win/loss). All other VRF consumers use full-word modular arithmetic or keccak mixing, so no bit-level collision exists. See BIT ALLOCATION MAP comment in DegenerusGameAdvanceModule.sol above `rngGate()`.

   **Verdict:** SAFE. The new consumer follows the established bit-extraction pattern and does not create any bit-overlap concern with existing consumers.
   ```

**Tier-2: PAYOUT-SPECIFICATION.html**

4. In the Token Burns section (section 5, around line 1750-1830), add a new subsystem card AFTER the PAY-14/PAY-15 card for the gambling burn path. Insert before the closing `</section>` of section 5:

   ```html
   <!-- PAY-16: Gambling Burn Redemption (Active Game) -->
   <div class="system-card" id="pay-16">
     <h3>PAY-16: Gambling Burn Redemption</h3>
     <div class="system-meta">
       <span class="badge badge-eth">ETH</span>
       <span class="badge badge-burnie">BURNIE</span>
     </div>

     <h4 style="margin-top: 1rem; color: #0d47a1;">During Active Game: RNG-Gated Burn</h4>
     <table class="info-table">
       <tr><th>Trigger</th><td>Holder calls <code>burn(amount)</code> or <code>burnWrapped(amount)</code> on sDGNRS/DGNRS during active game</td></tr>
       <tr><th>Submit Phase</th><td>sDGNRS burned, proportional ETH/BURNIE base reserved in segregated accounting</td></tr>
       <tr><th>Resolution</th><td>Next <code>advanceGame()</code> applies VRF roll (25-175%) to base amounts</td></tr>
       <tr><th>ETH Payout</th><td><code>(ethValueOwed * roll) / 100</code> &mdash; always claimable after resolution</td></tr>
       <tr><th>BURNIE Payout</th><td><code>(burnieOwed * roll * (100 + rewardPercent)) / 10000</code> &mdash; conditional on coinflip win</td></tr>
       <tr><th>Claim</th><td>Player calls <code>claimRedemption()</code>; ETH paid immediately, BURNIE deferred if flip unresolved</td></tr>
       <tr><th>Cap</th><td>50% of totalSupply per redemption period</td></tr>
     </table>

     <div class="formula-block">
       <h4>Formula</h4>
       <pre><code>// PAY-16 -- StakedDegenerusStonk.sol
// Submit phase (during burn):
ethBase = (totalMoney * amount) / supplyBefore;    // proportional ETH share
burnieBase = (totalBurnie * amount) / supplyBefore; // proportional BURNIE share
// Bases added to pendingRedemptionEthBase/BurnieBase

// Resolution phase (advanceGame):
roll = (currentWord >> 8) % 151 + 25;  // range 25-175
rolledEth = (ethBase * roll) / 100;
burnieToCredit = (burnieBase * roll) / 100;
// BURNIE credited as coinflip stake via creditFlip

// Claim phase (claimRedemption):
ethPayout = (claim.ethValueOwed * roll) / 100;  // always claimable
// BURNIE: if flip won, (claim.burnieOwed * roll * (100 + rewardPercent)) / 10000
// BURNIE: if flip lost or unresolved, 0 (ETH still paid)</code></pre>
       <p class="file-ref"><strong>Code:</strong> StakedDegenerusStonk.sol (burn, resolveRedemptionPeriod, claimRedemption)</p>
     </div>
   </div>
   ```

5. Update the PAY-14 card's formula section to note the CP-08 fix: in the formula block around line 1784-1801, add a comment line:
   ```
   // v3.3 fix (CP-08): totalMoney and totalBurnie subtract pendingRedemptionEthValue/Burnie
   // to prevent double-spending reserves already allocated to gambling claims
   ```

**Tier-3: Version stamps on findings docs**

6. For each of the following files, add a version stamp note at the TOP of the file (after the title/header line) or in an appropriate location:

   - `audit/v3.1-findings-consolidated.md`: Add after the title: `> **v3.3 Note:** StakedDegenerusStonk.sol, DegenerusStonk.sol, BurnieCoinflip.sol, and DegenerusGameAdvanceModule.sol have been modified for the gambling burn system. Findings CMT-076 (OnlyBurnieCoin reuse) has been fixed: _resolvePlayer now uses NotApproved, claimCoinflipsForRedemption uses OnlyStakedDegenerusStonk.`

   - `audit/v3.1-findings-34-token-contracts.md`: Add after the title: `> **v3.3 Note:** StakedDegenerusStonk.sol and DegenerusStonk.sol have been modified. sDGNRS now includes gambling burn redemption system (7 new state variables, 3 new functions). DGNRS now reverts burn() during active game (GameNotOver guard).`

   - `audit/v3.1-findings-35-peripheral-contracts.md`: Add after the title: `> **v3.3 Note:** BurnieCoinflip.sol has been modified. New function claimCoinflipsForRedemption added. Error OnlyBurnieCoin reuse (CMT-076) has been fixed: _resolvePlayer uses NotApproved, claimCoinflipsForRedemption uses OnlyStakedDegenerusStonk.`

   - `audit/v3.2-findings-consolidated.md`: Add after the title: `> **v3.3 Note:** Four contracts modified for gambling burn system. See FINAL-FINDINGS-REPORT.md for v3.3 findings (3 HIGH, 1 MEDIUM -- all fixed).`

   - `audit/v3.2-findings-40-token-contracts.md`: Add after the title: `> **v3.3 Note:** StakedDegenerusStonk.sol and DegenerusStonk.sol modified for gambling burn system. See FINAL-FINDINGS-REPORT.md for details.`

   - `audit/v3.2-findings-40-core-game-contracts.md`: Add after the title: `> **v3.3 Note:** DegenerusGameAdvanceModule.sol modified to resolve gambling burn redemptions in both rngGate and _gameOverEntropy paths.`

   - `audit/v3.1-findings-31-core-game-contracts.md`: Add after the title: `> **v3.3 Note:** DegenerusGameAdvanceModule.sol modified for gambling burn redemption resolution. See v3.3 findings in FINAL-FINDINGS-REPORT.md.`

The following files need NO changes (confirmed by research): `v3.2-governance-fresh-eyes.md`, `v3.2-findings-39-*.md` (5 files), `v3.1-findings-32-*.md`, `v3.1-findings-33-*.md`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "v3.3" audit/v3.2-rng-delta-findings.md && grep -c "PAY-16" audit/PAYOUT-SPECIFICATION.html && grep -c "v3.3 Note" audit/v3.1-findings-consolidated.md audit/v3.2-findings-consolidated.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.2-rng-delta-findings.md contains "v3.3 Addendum" or "New RNG Consumer"
    - audit/v3.2-rng-delta-findings.md contains "(currentWord >> 8) % 151 + 25"
    - audit/PAYOUT-SPECIFICATION.html contains "PAY-16" and "Gambling Burn Redemption"
    - audit/PAYOUT-SPECIFICATION.html PAY-14 formula section contains "pendingRedemptionEthValue"
    - audit/v3.1-findings-consolidated.md contains "v3.3 Note"
    - audit/v3.1-findings-34-token-contracts.md contains "v3.3 Note"
    - audit/v3.1-findings-35-peripheral-contracts.md contains "v3.3 Note"
    - audit/v3.2-findings-consolidated.md contains "v3.3 Note"
    - audit/v3.2-findings-40-token-contracts.md contains "v3.3 Note"
    - audit/v3.2-findings-40-core-game-contracts.md contains "v3.3 Note"
    - audit/v3.1-findings-31-core-game-contracts.md contains "v3.3 Note"
  </acceptance_criteria>
  <done>All 12 audit docs updated: RNG findings doc has new consumer addendum, payout spec has PAY-16 gambling burn section, 7 findings docs have v3.3 version stamps cross-referencing gambling burn changes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "v3.3" audit/FINAL-FINDINGS-REPORT.md` shows findings section
2. `grep -c "gambling burn" audit/KNOWN-ISSUES.md` returns >= 1
3. `grep -c "Gambling Burn Redemption System" audit/EXTERNAL-AUDIT-PROMPT.md` returns 1
4. `grep -c "PAY-16" audit/PAYOUT-SPECIFICATION.html` returns >= 1
5. `grep -c "v3.3" audit/v3.2-rng-delta-findings.md` returns >= 1
6. Count of files with "v3.3 Note" version stamps: `grep -rl "v3.3 Note" audit/` returns 7 files
</verification>

<success_criteria>
- DOC-04: All 3 tier-1 audit docs updated with gambling burn mechanism, v3.3 findings, and redemption system scope
- DOC-04: Payout specification includes PAY-16 gambling burn payout path
- DOC-04: RNG delta findings document references new redemption roll consumer
- DOC-04: 7 findings docs have v3.3 version stamps noting contract modifications
- No audit docs contain stale information that contradicts the post-fix code
</success_criteria>

<output>
After completion, create `.planning/phases/48-documentation-sync/48-02-SUMMARY.md`
</output>
