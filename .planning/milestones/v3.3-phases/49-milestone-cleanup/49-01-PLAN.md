---
phase: 49-milestone-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - audit/v3.2-rng-delta-findings.md
  - .planning/phases/47-gas-optimization/47-01-gas-analysis.md
  - .planning/phases/48-documentation-sync/48-01-SUMMARY.md
  - .planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md
autonomous: true
requirements: [ANOMALY-1, ANOMALY-2, ANOMALY-3]
gap_closure: true

must_haves:
  truths:
    - "BIT ALLOCATION MAP line numbers match actual source locations for all 5 non-delegatecall consumers"
    - "v3.3 addendum in v3.2-rng-delta-findings.md references correct line numbers"
    - "47-01-gas-analysis.md line references match current StakedDegenerusStonk.sol"
    - "48-01-SUMMARY.md lists DOC-01, DOC-02, DOC-03 in requirements_completed"
    - "46-01-SUMMARY.md does not declare Phase 47 as a dependency"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "Corrected BIT ALLOCATION MAP comment"
      contains: "AdvanceModule.sol:795"
    - path: "audit/v3.2-rng-delta-findings.md"
      provides: "Corrected v3.3 addendum line references"
      contains: "rngGate line 795"
    - path: ".planning/phases/47-gas-optimization/47-01-gas-analysis.md"
      provides: "Corrected variable liveness line references"
      contains: "StakedDegenerusStonk.sol:191"
    - path: ".planning/phases/48-documentation-sync/48-01-SUMMARY.md"
      provides: "Populated requirements_completed"
      contains: "requirements-completed: [DOC-01, DOC-02, DOC-03]"
    - path: ".planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md"
      provides: "Removed spurious Phase 47 dependency"
  key_links: []
---

<objective>
Fix all stale line references in audit documentation and contract comments, and repair metadata gaps in SUMMARY files.

Purpose: C4A wardens will cross-reference line numbers in audit docs against source code. Stale references erode credibility and waste reviewer time.
Output: Corrected BIT ALLOCATION MAP comment, corrected v3.3 addendum, corrected gas analysis line refs, fixed SUMMARY metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.3-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BIT ALLOCATION MAP and v3.3 addendum line references</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, audit/v3.2-rng-delta-findings.md</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 739-757 for BIT ALLOCATION MAP, lines 793-795 for redemption roll, line 826 for lootbox, line 1033 for variance)
    - contracts/BurnieCoinflip.sol (line 809 for coinflip win/loss, lines 783-788 for reward percent)
    - audit/v3.2-rng-delta-findings.md (lines 920-928 for v3.3 addendum)
  </read_first>
  <action>
**In contracts/modules/DegenerusGameAdvanceModule.sol**, update the BIT ALLOCATION MAP comment block (lines 739-756). Change these specific line references:

Line 743: Change `BurnieCoinflip.sol:808` to `BurnieCoinflip.sol:809`
Line 744: Change `AdvanceModule.sol:773` to `AdvanceModule.sol:795`
Line 745: Change `BurnieCoinflip.sol:782-787` to `BurnieCoinflip.sol:783-788`
Line 748: Change `AdvanceModule.sol:804` to `AdvanceModule.sol:826`
Line 749: Change `AdvanceModule.sol:1010` to `AdvanceModule.sol:1033`

Also fix the internal cross-reference comment at line 854 inside _gameOverEntropy:
Change `// Resolve gambling burn period if pending (mirrors rngGate lines 770-780)` to `// Resolve gambling burn period if pending (mirrors rngGate lines 792-802)`

And at line 883:
Change `// Resolve gambling burn period if pending (mirrors rngGate lines 770-780)` to `// Resolve gambling burn period if pending (mirrors rngGate lines 792-802)`

**In audit/v3.2-rng-delta-findings.md**, update the v3.3 addendum (line 922). Change:
`(rngGate line 773, _gameOverEntropy lines 835 and 864)` to `(rngGate line 795, _gameOverEntropy lines 858 and 887)`

These are the actual locations of `uint16 redemptionRoll = uint16((currentWord >> 8) % 151 + 25)` in each function:
- rngGate: line 795
- _gameOverEntropy VRF path: line 858
- _gameOverEntropy fallback path: line 887
  </action>
  <verify>
    <automated>grep -n "AdvanceModule.sol:795" contracts/modules/DegenerusGameAdvanceModule.sol && grep -n "BurnieCoinflip.sol:809" contracts/modules/DegenerusGameAdvanceModule.sol && grep -n "rngGate line 795" audit/v3.2-rng-delta-findings.md && grep -n "lines 858 and 887" audit/v3.2-rng-delta-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `AdvanceModule.sol:795` (redemption roll)
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `BurnieCoinflip.sol:809` (coinflip win/loss)
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `BurnieCoinflip.sol:783-788` (reward percent)
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `AdvanceModule.sol:826` (lootbox RNG)
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `AdvanceModule.sol:1033` (future take variance)
    - contracts/modules/DegenerusGameAdvanceModule.sol contains `mirrors rngGate lines 792-802` (both cross-refs)
    - contracts/modules/DegenerusGameAdvanceModule.sol does NOT contain `AdvanceModule.sol:773` anywhere
    - audit/v3.2-rng-delta-findings.md contains `rngGate line 795`
    - audit/v3.2-rng-delta-findings.md contains `lines 858 and 887`
    - audit/v3.2-rng-delta-findings.md does NOT contain `rngGate line 773` or `rngGate:773`
    - forge build succeeds (comment-only changes, no code impact)
  </acceptance_criteria>
  <done>BIT ALLOCATION MAP references all 5 correct source lines; v3.3 addendum cites correct rngGate:795 and _gameOverEntropy:858/887; forge build passes</done>
</task>

<task type="auto">
  <name>Task 2: Fix gas analysis line refs and SUMMARY metadata</name>
  <files>.planning/phases/47-gas-optimization/47-01-gas-analysis.md, .planning/phases/48-documentation-sync/48-01-SUMMARY.md, .planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/47-gas-optimization/47-01-gas-analysis.md (full file -- contains ~100 line references to update)
    - .planning/phases/48-documentation-sync/48-01-SUMMARY.md (frontmatter, line 44 for requirements-completed)
    - .planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md (frontmatter, lines 8-16 for requires block)
    - contracts/StakedDegenerusStonk.sol (lines 191-198 for declarations, key functions for reference)
  </read_first>
  <action>
**In 47-01-gas-analysis.md**, update ALL stale line references. The variables were shifted by Phase 48's NatSpec additions. Here is the complete correction map (documented line → actual line in current StakedDegenerusStonk.sol):

**Header (line 5):** Change `declared at lines 193-200` to `declared at lines 191-198`

**Variable declarations (-2 shift):**
- `:193` → `:191` (pendingRedemptionEthValue)
- `:194` → `:192` (pendingRedemptionBurnie)
- `:195` → `:193` (pendingRedemptionEthBase)
- `:196` → `:194` (pendingRedemptionBurnieBase)
- `:198` → `:196` (redemptionPeriodSupplySnapshot)
- `:199` → `:197` (redemptionPeriodIndex)
- `:200` → `:198` (redemptionPeriodBurned)

**resolveRedemptionPeriod function (+1 shift in early body):**
- `:548` → `:549` (period read)
- `:549` → `:550` (early exit check)
- `:552` → `:553` (rolledEth computation)
- `:553` → `:554` (ethValue adjustment) -- NOTE: line 553 appears as BOTH read and write site; doc has TWO entries at :553, update both to :554 where it's the write, and :553 stays for read at rolledEth computation (actually both refer to the same expression on the same line; in current code pendingRedemptionEthValue adjustment is at :554)
- `:554` → `:555` (ethBase zeroed)
- `:557` → `:558` (burnieToCredit computation)
- `:560` → `:561` (burnie release subtraction)
- `:561` → `:562` (burnieBase zeroed)

**hasPendingRedemptions (+1 shift):**
- `:537` → `:538` (both ethBase and burnieBase checks)

**claimRedemption (+3 shift):**
- `:599` → `:602` (ethPayout subtraction)

**_deterministicBurnFrom (+1 shift):**
- `:477` → `:478` (totalMoney computation)
- `:482` → `:483` (totalBurnie computation)

**previewBurn (+9 shift):**
- `:633` → `:642` (totalMoney computation)
- `:637` → `:646` (ethAvailable check)
- `:638` → `:647` (ethAvailable subtraction)
- `:651` → `:660` (totalBurnie computation)

**burnieReserve (+9 shift):**
- `:661` → `:670` (return statement)

**_submitGamblingClaimFrom (+10 shift):**
- `:681` → `:691` (period boundary check)
- `:682` → `:692` (snapshot assignment)
- `:683` → `:693` (periodIndex assignment)
- `:684` → `:694` (burned reset)
- `:686` → `:696` (50% cap check)
- `:687` → `:697` (burned accumulation)
- `:695` → `:705` (totalMoney computation)
- `:701` → `:711` (totalBurnie computation)
- `:712` → `:722` (ethValue segregation)
- `:713` → `:723` (ethBase accumulation)
- `:714` → `:724` (burnie reservation)
- `:715` → `:725` (burnieBase accumulation)

**Packing analysis section line refs (same shifts apply):**
- `:681` → `:691`, `:682` → `:692`, `:683` → `:693`, `:684` → `:694`, `:686` → `:696`, `:687` → `:697`
- `:537` → `:538`, `:549` → `:550`, `:552` → `:553` through `:554` → `:555`
- `:557` → `:558`, `:560` → `:561` through `:561` → `:562`
- `:713` → `:723`, `:715` → `:725`
- `:578` → `:580`, `:580` → `:582`, `:590` → `:588`, `:595` → `:597`
- `:602` → `:606` (delete), `:719` → `:729`, `:722` → `:732`, `:723` → `:733`, `:724` → `:734`

Wait -- the PendingRedemption struct references need separate verification. Let me be precise about the struct access lines:

For the PendingRedemption struct section, verify and correct each reference against actual code. The references in the packing section (Opportunity 3) cite struct field accesses:
- `:578` claim.periodIndex NoClaim check → actual `:580`
- `:580` claim.periodIndex load period → actual `:582`
- `:590` claim.ethValueOwed → actual `:588`
- `:595` claim.burnieOwed → actual `:597`
- `:602` delete pendingRedemptions → actual `:606`
- `:719` claim.periodIndex existing check → actual `:729`
- `:722` claim.ethValueOwed += → actual `:732`
- `:723` claim.burnieOwed += → actual `:733`
- `:724` claim.periodIndex = currentPeriod → actual `:734`

Also update the storage layout table: slot assignments remain correct but declaration lines in variable names section should reference the corrected line numbers.

Also in the INITIAL_SUPPLY reference:
- `:207` → verify actual line. Search for `INITIAL_SUPPLY` to confirm.

**In 48-01-SUMMARY.md**, the frontmatter line 44 currently reads:
`requirements-completed: [DOC-01, DOC-02, DOC-03]`

Wait -- re-reading the file, line 44 already says `requirements-completed: [DOC-01, DOC-02, DOC-03]`. The milestone audit says it's empty, but the file shows it populated. Let me re-verify. If it already has the correct value, no change needed. If the milestone audit was correct at audit time and the file was subsequently fixed, skip this change.

ACTUALLY -- re-reading the 48-01-SUMMARY.md output from the initial read: line 44 shows `requirements-completed: [DOC-01, DOC-02, DOC-03]`. This appears to already be correct. The milestone audit may have been run before this was populated. Verify the file before modifying -- if `requirements-completed` already lists `[DOC-01, DOC-02, DOC-03]`, no change is needed.

**In 46-01-SUMMARY.md**, remove the spurious Phase 47 dependency from the requires block. Currently the frontmatter contains:
```yaml
requires:
  - phase: 44-delta-audit-redemption-correctness
    provides: ...
  - phase: 45-invariant-tests
    provides: ...
  - phase: 47-gas-optimization
    provides: ...
```
Remove the entire `- phase: 47-gas-optimization` entry (lines ~14-15 in the requires block). Phase 46 does not depend on Phase 47 -- they are parallel. The warden simulation referenced Phase 44 fixes and Phase 45 invariant tests, never Phase 47 gas results.
  </action>
  <verify>
    <automated>grep "declared at lines 191-198" .planning/phases/47-gas-optimization/47-01-gas-analysis.md && grep "StakedDegenerusStonk.sol:191" .planning/phases/47-gas-optimization/47-01-gas-analysis.md && grep -c "47-gas-optimization" .planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 47-01-gas-analysis.md contains `declared at lines 191-198` (not `193-200`)
    - 47-01-gas-analysis.md contains `StakedDegenerusStonk.sol:191` for pendingRedemptionEthValue declaration
    - 47-01-gas-analysis.md contains `StakedDegenerusStonk.sol:722` for _submitGamblingClaimFrom ethValue (not `:712`)
    - 47-01-gas-analysis.md contains `StakedDegenerusStonk.sol:602` for claimRedemption ethPayout (not `:599`)
    - 47-01-gas-analysis.md contains `StakedDegenerusStonk.sol:696` for 50% cap check (not `:686`)
    - 46-01-SUMMARY.md grep for `47-gas-optimization` returns 0 matches
    - 46-01-SUMMARY.md still contains `44-delta-audit-redemption-correctness` and `45-invariant-tests` dependencies
    - 48-01-SUMMARY.md contains `requirements-completed: [DOC-01, DOC-02, DOC-03]` (verify or fix)
  </acceptance_criteria>
  <done>All ~50 line references in gas analysis match current StakedDegenerusStonk.sol; 46-01-SUMMARY.md has no Phase 47 dependency; 48-01-SUMMARY.md metadata is correct</done>
</task>

</tasks>

<verification>
1. `forge build` passes (comment-only changes in AdvanceModule.sol)
2. `grep -c "AdvanceModule.sol:773" contracts/modules/DegenerusGameAdvanceModule.sol` returns 0
3. `grep -c "rngGate line 773" audit/v3.2-rng-delta-findings.md` returns 0
4. `grep "declared at lines 191-198" .planning/phases/47-gas-optimization/47-01-gas-analysis.md` finds match
5. `grep -c "47-gas-optimization" .planning/phases/46-adversarial-sweep-economic-analysis/46-01-SUMMARY.md` returns 0
</verification>

<success_criteria>
- All line references in BIT ALLOCATION MAP, v3.3 addendum, and gas analysis match current source code
- No stale `:773` or `:193` references remain in any modified file
- 46-01-SUMMARY.md has no Phase 47 dependency
- 48-01-SUMMARY.md requirements_completed is populated
- forge build passes
</success_criteria>

<output>
After completion, create `.planning/phases/49-milestone-cleanup/49-01-SUMMARY.md`
</output>
