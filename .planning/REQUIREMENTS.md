# Requirements: v42.0 Mint-Batch Event/Sig Cleanup + Hero-Override Weighted Roll + Deity-Pass Gold Nerf + Lootbox RNG Retry — SHIPPED 2026-05-18

**Milestone:** v42.0 — SHIPPED 2026-05-18
**Closure signal:** `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2`
**Audit baseline:** `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`
**Phase shape:** Parallel-per-idea 8-phase shape (290 MINTCLN contract + 291 MINTCLN tests + 292 HRROLL contract + 293 HRROLL tests + 294 DPNERF contract + 295 DPNERF tests + 296 SWEEP + 297 TERMINAL)
**Single terminal deliverable:** `audit/FINDINGS-v42.0.md` (per D-NN-FILES-01 carry → D-42N-FILES-01)
**Scope:** Three independent jackpot/mint surface changes — (1) MINTCLN cleans up `TraitsGenerated` event + `_raritySymbolBatch` signature in `DegenerusGameMintModule.sol` by folding `owed` into `baseKey` low 32 bits and dropping `ownedSalt` arg (post-v41-Phase-281 cleanup with breaking topic-hash; inherits v40 D-40N-EVT-BREAK-01 indexer-migration posture); (2) HRROLL replaces deterministic `_topHeroSymbol` hero-override selector with weighted random roll across all 32 `(quadrant, symbol)` slots using VRF entropy with ×1.5 leader-weight bonus and no min-wager floor (`DegenerusGameJackpotModule.sol:1594-1653`); (3) DPNERF nerfs deity-pass virtual entries from `max(len/50, 2)` to flat 1 on gold-tier (`color == 7`) trait wins across both ETH + BURNIE coin jackpot paths via single-function change in `_randTraitTicket` (`DegenerusGameJackpotModule.sol:1671-1710`) with intentional EV reduction (no common-tier compensation). All 3 surfaces share a single 3-skill PARALLEL adversarial pass at Phase 296 + a single `audit/FINDINGS-v42.0.md` terminal deliverable at Phase 297. Pre-launch posture preserved; v42.0 lands before mainnet activation.

## Out of Scope

- **MINTCLN duplicate-logic helper extraction** (`processFutureTicketBatch` inline loop mint:421-509 vs `processTicketBatch` + `_processOneTicketEntry` split mint:670-834 parallel-emit/parallel-hash duplication) — flag-only per D-42N-MINTCLN-SCOPE-01 user decision 2026-05-17; deferred to v43.0+ maintenance bundle.
- **MINTCLN `processed += take` (mint:499) vs `processed += writesUsed >> 1` (mint:714) asymmetry** — flagged but NOT touched; out of scope per user disposition 2026-05-17.
- **MINTCLN storage layout changes** — `ticketsOwedPacked[rk][player]` 40-bit packed form (8 bits `rem` + 24 bits `owed` + 8 bits `processed`-via-Phase-281-owed-salt) UNCHANGED; zero new storage slots.
- **Indexer rebuild tooling** — the breaking `TraitsGenerated` topic-hash requires off-chain indexer rebuild; v42.0 deliverable cites this as an indexer-migration handoff per D-42N-EVT-BREAK-01 (mirrors v40 D-40N-EVT-BREAK-01 posture) but does NOT produce migration tooling. Pre-launch posture; no live indexer impact.
- **HRROLL min-wager floor for eligibility** — natural weighting handles sybil per D-42N-FLOOR-01 user decision 2026-05-17; no eligibility floor implemented. Smallest bettor wins with proportional probability.
- **HRROLL leader-bonus magnitude alternatives** (×2, ×3, flat floor) — ×1.5 locked per D-42N-LEADER-BONUS-01 user decision 2026-05-17.
- **HRROLL storage layout changes** — `dailyHeroWagers[uint32 => uint256[4]]` + `dailyIdx` UNCHANGED per the note's "no storage changes" constraint.
- **HRROLL public ABI changes** — `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view function signatures byte-identical post-fix.
- **Per-bet hero-quadrant multiplier `_applyHeroMultiplier`** in `DegenerusGameDegeneretteModule.sol` — explicitly unrelated mechanic; stays as-is per v37 Phase 267 design.
- **`wagerUnit` granularity** (`totalBet / 1e12`) at Degenerette bet-write — unchanged.
- **4-day BURNIE near-future + ETH split** — unchanged.
- **DPNERF common-tier compensation** — intentional EV reduction per D-42N-DEITY-EV-01 user decision 2026-05-17; common-tier `virtualCount = max(len/50, 2)` UNCHANGED. Net result: deity weaker overall on gold-tile wins (no rebalance toward common-tier).
- **DPNERF storage layout changes** — single-function `_randTraitTicket` body change; zero new storage; zero new SSTORE; zero new SLOAD.
- **DPNERF public ABI changes** — `_randTraitTicket` is private; ETH + BURNIE coin jackpot caller signatures unchanged.
- **DPNERF boon distribution for deity-pass holders** (`DegenerusGameBoonModule.sol` `BP_DEITY_PASS_TIER_SHIFT` flow) — unchanged.
- **DPNERF deity-pass purchase pricing** (`DegenerusGameWhaleModule.sol` `DEITY_PASS_BASE`) — unchanged.
- **DPNERF soulbound NFT minting** (`DegenerusDeityPass.sol`) — unchanged.
- **Mint-boost fractional retirement** (`D-40N-MINTBOOST-OUT-01` v41-carry / `D-274-MINTBOOST-OUT-01`) — `_queueTicketsScaled` + `_rollRemainder` + `rem` byte stay at `DegenerusGameMintModule.sol:1142` (deterministic dust accumulator; not RNG-driven; out of v42.0 scope).
- **Game-over thorough hardening** — deferred to dedicated game-over hardening milestone (carry from v41+).
- **LBX-02 fixture-coverage gap** (`D-40N-LBX02-OUT-01` v41+-carry; RE-DEFERRED-V42+) — analytical worst-case continues to be load-bearing per Phase 266 GAS-01 + `feedback_gas_worst_case.md`.
- **Superseded-baseline SURF `it.skip` cleanup** (v41+-carry per Phase 279 D-279-02-SURF-SUPERSEDED-01; RE-DEFERRED-V42+) — v42+ surfaces a NEW superseded-baseline SURF row at v41→v42 boundary for the MintModule patch; v43+ backlog cleanup task.
- **`runrewardjackpots` module-misplacement note** — stale 2026-04-02 backlog note; not v42.0-tagged; carries forward.
- **`/degen-skeptic` adversarial skill** — OUT OF SCOPE per D-271-ADVERSARIAL-02 carry; Phase 296 SWEEP uses `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03.

## v42.0 Requirements

### MINTCLN — Mint-batch event/signature cleanup (contracts/modules/DegenerusGameMintModule.sol + contracts/modules/DegenerusGameStorage.sol)

- [x] **MINTCLN-01**: `_raritySymbolBatch` signature changes from 4-input hash to 3-input hash — drop `uint32 ownedSalt` parameter (mint:544-551); body hashes `keccak256(baseKey, entropyWord, groupIdx)` at mint:572. Same 256-bit seed space, same uniformity, same cross-call seed separation when same slot is re-entered with smaller owed (the owed value is now carried inside `baseKey` low 32 bits, so the algorithm-level invariant is preserved). Function selector + return type byte-identical.
- [x] **MINTCLN-02**: `baseKey` construction at both callsites (`processFutureTicketBatch` mint:423-425 + `_processOneTicketEntry` mint:800-802) ORs `owed` into low 32 bits: `baseKey = (lvl<<224) | (queueIdx<<192) | (player<<32) | owed`. Both callsites updated identically (B2-symmetric per v41 Phase 281 precedent).
- [x] **MINTCLN-03**: `_raritySymbolBatch` callsite invocations updated to drop the `ownedSalt` arg — `processFutureTicketBatch` (mint:469) + `_processOneTicketEntry` (mint:803). Both call the 3-input signature post-fix.
- [x] **MINTCLN-04**: `TraitsGenerated` event signature changes from `(address indexed player, uint256 baseKey, uint32 startIndex, uint32 take)` to `(address indexed player, uint256 baseKey, uint32 take)` — rename + drop. Event declaration in `DegenerusGameStorage.sol:484-491`. Both emit sites updated (mint:469-477 + mint:804-811) — emit `(player, baseKey, take)`. **BREAKING topic-hash** on `TraitsGenerated` — inherits v40 D-40N-EVT-BREAK-01 posture (pre-launch; indexer rebuild required; no live indexer impact). v42.0 audit deliverable `audit/FINDINGS-v42.0.md` §9 "Deferred to Future Milestones" cites indexer-migration as a forward-handoff per D-42N-EVT-BREAK-01.
- [x] **MINTCLN-05**: `rollSalt` (currently constructed at mint:771-773 — identical to `baseKey` construction at mint:800-802) collapses to reuse `baseKey`. Either drop the separate `rollSalt` local entirely OR rename one for clarity; both `_rollRemainder` callsites (zero-owed branch + post-take branch) continue to hash through `EntropyLib.hash2(entropy, rollSalt)` with the new `baseKey`-carrying-owed value. Distribution stays uniform — no fairness impact.
- [x] **MINTCLN-06**: Side-bug fix — `TraitsGenerated.startIndex` field-name mismatch at mint:474 + mint:807 (currently the field declared as `startIndex` receives the `owed` value, a Phase-281 naming artifact) resolved by the MINTCLN-04 rename (event no longer has a `startIndex` field; `owed` lives in `baseKey` low 32 bits).
- [x] **MINTCLN-07**: `_raritySymbolBatch` docstring rewritten (mint:534-543) to describe the post-MINTCLN shape (3-input hash, owed carried in baseKey). Per `feedback_no_history_in_comments.md`: comment describes what IS, never what changed. No "previously took ownedSalt" language. No reference to v41 Phase 281 or v42 cleanup.
- [x] **MINTCLN-08**: Storage byte-identical to v41 closure HEAD on non-mutated slots — `ticketsOwedPacked[rk][player]` 40-bit packed form (8 bits `rem` + 24 bits `owed` + 8 bits `processed`-via-Phase-281-owed-salt) UNCHANGED; zero new storage slots; zero new SSTORE callsites; zero new SLOAD callsites in MINTCLN scope. Storage-slot grep proof committed to AUDIT artifacts.
- [x] **MINTCLN-09**: Public ABI signature byte-identical — `processFutureTicketBatch(uint24, uint256) → (bool, bool, uint32)` + `processTicketBatch` + `_processOneTicketEntry` external/public selectors + parameter types + return shapes unchanged post-fix. `TicketsCredited` + `TicketsQueued` event topic hashes preserved (only `TraitsGenerated` topic-hash changes per MINTCLN-04). Zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks in MINTCLN scope.
- [x] **MINTCLN-10**: MINTCLN-scope decision anchors recorded BEFORE the contract patch lands per `feedback_design_intent_before_deletion.md`: `D-42N-MINTCLN-SCOPE-01` (narrow scope, no helper extraction — duplicate-logic flagged-only) + `D-42N-EVT-BREAK-01` (breaking `TraitsGenerated` topic-hash accepted; indexer migration inherits v40 D-40N-EVT-BREAK-01 posture). Design-intent trace covers: (i) original 4-input hash rationale + why `ownedSalt` was separate arg post-Phase-281 vs always-in-baseKey; (ii) original `TraitsGenerated` field-set rationale + why `startIndex` was named that way post-Phase-281 vs `baseKey`-as-uint256; (iii) breaking-topic-hash justification (pre-launch posture). Decision rationale committed to plan artifact.

### TST-MINTCLN — Mint-batch cleanup regression fixture (test/edge/ or test/mint/)

- [x] **TST-MINTCLN-01**: RNG correctness re-validation — for any `(rk, player)` pair where `owed > writesBudget / 2` (forcing multi-call drain across N ≥ 2 calls), the trait multiset produced post-MINTCLN equals the trait multiset produced by v41 Phase 281 owed-salt fix at the same `(level, queueIdx, player, dailyEntropy)`. Cross-call seed separation preserved (owed shrinks per emit; 3-input hash still produces pairwise-distinct keccak inputs across multi-call drains). The MINTCLN refactor preserves the v41 Phase 281 algorithmic invariant — only the function signature + event shape change.
- [x] **TST-MINTCLN-02**: `TraitsGenerated` event-shape regression — fixture mints a player into the multi-call drain scenario; advances `processFutureTicketBatch` N times; asserts each emission decodes to `(player, baseKey, take)` 3-tuple post-MINTCLN; asserts `baseKey` low 32 bits decodes to the expected `owed` value at call entry; asserts `baseKey` upper bits decode to `(lvl, queueIdx, player)` per the construction at mint:423-425. Event topic-hash differs from v41 emission (breaking-change attestation).
- [x] **TST-MINTCLN-03**: Both callsite paths covered (B2 symmetric per v41 Phase 281 precedent) — TST-MINTCLN-01 + TST-MINTCLN-02 exercise both Path A (`processFutureTicketBatch` whale-bundle at future levels) + Path B (`_processOneTicketEntry` direct-purchase) emission paths. Both call the new 3-input `_raritySymbolBatch` signature; both emit the new 3-field `TraitsGenerated` shape.
- [x] **TST-MINTCLN-04**: Storage layout regression — `ticketsOwedPacked[rk][player]` 40-bit packed form (`rem` low 8 + `owed` next 24 + `processed`-via-owed-salt high 8) byte-identical to v41 close HEAD on a representative drain run. Storage-slot grep proof reads same slot bytes pre + post-MINTCLN.
- [x] **TST-MINTCLN-05**: Indexer-migration note — test file header documents the breaking `TraitsGenerated` topic-hash change with a path-of-investigation comment per the v40 Phase 277 D-40N-EVT-BREAK-01 precedent (pre-launch; rebuild required; no live indexer impact). The note is a documentation-cleanup item; no on-chain assertion, no test logic.

### HRROLL — Hero-override weighted random roll (contracts/modules/DegenerusGameJackpotModule.sol)

- [x] **HRROLL-01**: `_topHeroSymbol` (L1625-1653) replaced with `_rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)`. Implementation: two-pass over the 32 `(quadrant, symbol)` slots in `dailyHeroWagers[day]` — pass 1 computes total weight + identifies max-amount leader; pass 2 walks the table with cumulative weight cursor against a 64-bit `pick` value derived from `keccak256(abi.encode(entropy, day)) % effectiveTotal`. Return `(false, 0, 0)` when `total == 0`. Cursor-walk direction: q ascending → s ascending (matches current `_topHeroSymbol` scan order for clarity, even though weighted picks shouldn't care about scan order).
- [x] **HRROLL-02**: ×1.5 leader-weight bonus per D-42N-LEADER-BONUS-01 — the max-amount `(q, s)` slot gets `weight = amount + leaderBonus` during pass 2, where `leaderBonus = maxAmount / 2` (effective ×1.5 weight). `effectiveTotal = total + leaderBonus` to keep proportional math correct. Tie-break on leader (multiple slots with same max amount): scan-order-first (q ascending → s ascending) match v41 `_topHeroSymbol` behavior.
- [x] **HRROLL-03**: No min-wager floor for eligibility per D-42N-FLOOR-01 — every slot with `amount > 0` is included in the weighted roll regardless of magnitude. Smallest bettor wins with proportional probability `amount / effectiveTotal` (or `(amount + leaderBonus) / effectiveTotal` if leader). Single-bettor edge case: if only one `(q, s)` slot has any wagers, the roll trivially returns that entry (same outcome as v41 `_topHeroSymbol` deterministic pick).
- [x] **HRROLL-04**: `_applyHeroOverride` (L1594-1621) call-site updated to invoke `_rollHeroSymbol(dailyIdx, randomWord)` instead of `_topHeroSymbol(dailyIdx)`. `randomWord` source comes from the existing jackpot VRF entropy reach (verify at plan-phase that `randomWord` is in-scope at the `_applyHeroOverride` call frame). Color sampling stays via `randomWord` bits `quadrant*3` per existing logic (3 bits per quadrant); symbol roll uses keccak-derived bits from `keccak256(abi.encode(entropy, day))` per HRROLL-01 — non-overlapping bit allocation attested at plan-phase per D-42N-COLOR-ENTROPY-01.
- [x] **HRROLL-05**: RNG commitment-window proof per `feedback_rng_commitment_window.md` — backward-trace from `_rollHeroSymbol` invocation back to wager-time confirms: (i) `dailyHeroWagers[dailyIdx][q]` writes happen at `DegenerusGameDegeneretteModule.sol:484-501` during `placeDegeneretteBet` (bet placement); (ii) `dailyIdx` is set at `_unlockRng` (end of prior-day jackpot resolution); (iii) v41 Phase 288 `dailyIdx` structural fix established `dailyIdx` as a single-writer storage slot frozen across the rng-lock window; (iv) wager amounts for day D are LOCKED before day D+1's VRF request fires; (v) `randomWord` (the symbol-roll entropy) is unknown at wager time. Player-controllable input (wager amounts) committed before randomness available — RNG commitment-window invariant SAFE.
- [x] **HRROLL-06**: Storage byte-identical — `dailyHeroWagers[uint32 => uint256[4]]` layout in `DegenerusGameStorage.sol:1478` UNCHANGED; `dailyIdx` UNCHANGED; zero new storage slots; zero new SSTORE callsites; zero new SLOAD callsites added (only existing `dailyHeroWagers[day][q]` reads). Storage-slot grep proof.
- [x] **HRROLL-07**: Public ABI byte-identical — `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view function signatures unchanged. `_rollHeroSymbol` is private; `_topHeroSymbol` deletion doesn't affect external surface. Zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks in HRROLL scope.
- [x] **HRROLL-08**: Gas regression — worst-case `_rollHeroSymbol` execution measured against v41 `_topHeroSymbol` baseline. Expected delta ~+5-8K gas per jackpot call (two 32-slot passes + a 32-entry memory array). Run on the active gas budget; budget tolerance attested per `feedback_gas_worst_case.md` (theoretical worst case derived first; tested second). Decision-anchor D-42N-GAS-01 locks the acceptance threshold at plan-phase.
- [x] **HRROLL-09**: Determinism / replayability spec — the exact `_rollHeroSymbol` algorithm (keccak input ordering `keccak256(abi.encode(entropy, day))`, modulo source `effectiveTotal`, cursor-walk direction q ascending → s ascending, leader-bonus tie-break scan-order-first) locked at plan-phase per D-42N-DETERMINISM-01. Any test fixture can reproduce the roll outcome deterministically given `(dailyHeroWagers[day], randomWord)` inputs.
- [x] **HRROLL-10**: HRROLL-scope decision anchors recorded BEFORE the contract patch lands per `feedback_design_intent_before_deletion.md`: `D-42N-LEADER-BONUS-01` (×1.5 locked per user 2026-05-17) + `D-42N-FLOOR-01` (no floor locked per user 2026-05-17) + `D-42N-COLOR-ENTROPY-01` (color/symbol bit-allocation non-collision; plan-phase) + `D-42N-DETERMINISM-01` (exact roll algorithm; plan-phase) + `D-42N-GAS-01` (gas acceptance threshold; plan-phase). Design-intent trace covers: (i) original deterministic `_topHeroSymbol` "single-leader" rationale + why winner-takes-all was the initial design; (ii) leader-bonus magnitude trade-offs (×2 monopolization risk vs ×1.5 balanced bias vs no-bonus pure-proportional); (iii) sybil exposure trade-offs (no-floor simplicity vs floor anti-spam); (iv) RNG-window backward-trace verification; (v) gas budget headroom. Decision rationale committed to plan artifact.

### TST-HRROLL — Hero-override weighted roll regression fixture (test/edge/)

- [x] **TST-HRROLL-01**: Weighted-distribution chi² test — fixture seeds `dailyHeroWagers[day]` with a known weight distribution (e.g. 4 slots with weights [400, 300, 200, 100], total 1000); runs `_rollHeroSymbol` N = 10,000 iterations with deterministic-but-distinct `(entropy, day)` inputs; asserts empirical pick-rate per slot matches expected proportional rate within chi² p > 0.05 tolerance (matching v34.0 Phase 262 STAT-04 statistical-validation methodology).
- [x] **TST-HRROLL-02**: ×1.5 leader-bonus sanity — fixture seeds a known leader scenario (e.g. weights [500, 100, 100, 100]; leader at slot 0); asserts empirical leader pick-rate matches `(500 + 250) / 1250 = 60%` within chi² tolerance. Confirms leader-bonus arithmetic at HRROLL-02 produces the expected ×1.5 effective weight.
- [x] **TST-HRROLL-03**: RNG commitment-window proof regression — fixture writes wagers to `dailyHeroWagers[day]` during day D; advances to day D+1; asserts day D's `_rollHeroSymbol` execution at day D+1 jackpot time reads the LOCKED day-D wager amounts (no intra-day mutations affect the roll); asserts `dailyIdx` is the controlling day key (per v41 Phase 288 dailyIdx structural fix). HRROLL-05 invariant regression at the test-fixture level.
- [x] **TST-HRROLL-04**: Single-bettor edge case — fixture seeds `dailyHeroWagers[day]` with exactly one non-zero slot; asserts `_rollHeroSymbol` returns that slot's `(q, s)` deterministically (single-bettor wins with probability 1.0). Confirms HRROLL-03 single-bettor disposition acceptable.
- [x] **TST-HRROLL-05**: Zero-wager edge case — fixture seeds `dailyHeroWagers[day]` with all-zero slots; asserts `_rollHeroSymbol` returns `(false, 0, 0)` per HRROLL-01 early-bail.
- [x] **TST-HRROLL-06**: Gas regression — fixture measures `_rollHeroSymbol` worst-case execution gas (all 32 slots populated; leader-bonus computation; full cursor walk to last slot) and asserts against the D-42N-GAS-01 acceptance threshold per `feedback_gas_worst_case.md`.

### DPNERF — Deity-pass gold nerf (contracts/modules/DegenerusGameJackpotModule.sol)

- [x] **DPNERF-01**: `_randTraitTicket` (L1671-1710) body adds a color-tier check before the existing `virtualCount = len / 50` + `if (virtualCount < 2) virtualCount = 2` logic. When `(trait >> 3) & 7 == 7` (gold color tier per the trait byte layout), set `virtualCount = 1` and skip the existing `max(len/50, 2)` floor. When color tier is NOT gold (commons, `color ∈ [0..6]`), execute the existing v41 logic unchanged.
- [x] **DPNERF-02**: Both ETH + BURNIE coin jackpot paths covered per D-42N-PATH-COVERAGE-01 — `_randTraitTicket` is invoked by both `_runJackpotEthFlow` (ETH jackpot trait winners) and `payDailyCoinJackpot` → `_awardDailyCoinToTraitWinners` (BURNIE near-future coin jackpot winners). Single function change reaches both paths; no callsite flag, no path-discrimination logic.
- [x] **DPNERF-03**: Intentional EV reduction per D-42N-DEITY-EV-01 — no common-tier compensation logic added. Common-tier `virtualCount` unchanged at `max(len/50, 2)`. Deity earns less total EV across all 8 colors (gold contribution reduced from `max(len/50, 2)` to 1; commons unchanged). Decision-intent trace at D-42N-DEITY-EV-01 documents this is a deity-pass holder rebalance, not just an internal redistribution.
- [x] **DPNERF-04**: Storage byte-identical — single-function `_randTraitTicket` body change; zero new storage slots; zero new SSTORE callsites; zero new SLOAD callsites in DPNERF scope. Storage-slot grep proof.
- [x] **DPNERF-05**: Public ABI byte-identical — `_randTraitTicket` is private; `_runJackpotEthFlow` + `payDailyCoinJackpot` + `_awardDailyCoinToTraitWinners` external/public selectors + parameter types + return shapes unchanged. Zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks in DPNERF scope.
- [x] **DPNERF-06**: DPNERF-scope decision anchors recorded BEFORE the contract patch lands per `feedback_design_intent_before_deletion.md`: `D-42N-GOLD-FLOOR-01` (flat 1 locked per user 2026-05-17) + `D-42N-DEITY-EV-01` (intentional reduction; no compensation; locked per user 2026-05-17) + `D-42N-PATH-COVERAGE-01` (both ETH + BURNIE locked per user 2026-05-17). Design-intent trace covers: (i) original `virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2` rationale — why 2% with min-2 floor was the initial design; (ii) gold-tile concentration issue — small bucket × 2% floor disproportionately rewards deity owners; (iii) compensation trade-offs (keep EV constant vs intentional reduction vs intentional rebias); (iv) path-coverage trade-offs (both paths symmetric vs ETH-only). Decision rationale committed to plan artifact.

### TST-DPNERF — Deity-pass gold nerf regression fixture (test/edge/)

- [x] **TST-DPNERF-01**: Gold-tile virtual-count assertion — fixture mints a player with a deity-pass NFT; sets up a gold-tier trait win scenario (winning trait color `(trait >> 3) & 7 == 7`); calls `_randTraitTicket` (via the ETH jackpot path); asserts `virtualCount == 1` (not `max(len/50, 2)`). Confirms DPNERF-01 + DPNERF-03.
- [x] **TST-DPNERF-02**: Common-tier virtual-count preserved — fixture mints a deity-pass holder + common-color trait win scenario (color ∈ [0..6]); asserts `virtualCount == max(len/50, 2)` per the unchanged v41 logic. Confirms DPNERF-01 doesn't disturb common-tier path.
- [x] **TST-DPNERF-03**: BURNIE coin jackpot path coverage per D-42N-PATH-COVERAGE-01 — fixture replicates TST-DPNERF-01 via the BURNIE coin jackpot path (`payDailyCoinJackpot` → `_awardDailyCoinToTraitWinners` → `_randTraitTicket`). Asserts identical `virtualCount == 1` on gold-tile. Confirms DPNERF-02 path-symmetric behavior.
- [x] **TST-DPNERF-04**: Gold-tile EV regression — fixture runs N = 1,000 gold-tier trait wins across both ETH + BURNIE paths; computes empirical deity virtual-entry total (should equal N × 1 = N) vs v41 baseline (which would have been N × max(len/50, 2)). Asserts the EV reduction matches the analytical expectation per D-42N-DEITY-EV-01 intentional-reduction disposition.
- [x] **TST-DPNERF-05**: Non-deity holders unaffected — fixture runs gold-tier trait win without deity-pass ownership; asserts `_randTraitTicket` `virtualCount` logic for non-deity bucket is unchanged (DPNERF-01 only branches when `deity != address(0)` per the existing logic). Confirms DPNERF doesn't accidentally narrow the function's non-deity behavior.

### SWEEP — Cross-surface adversarial sweep (Phase 296; .planning/phases/296-*/)

- [x] **SWEEP-01**: 3-skill HYBRID adversarial pass per D-296-INVOKE-01 (Task 2 `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT; Tasks 3+4 `/zero-day-hunter` + `/economic-analyst` PARALLEL_SUBAGENT under user authorization 2026-05-18). `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. Surface set EXPANDED to 4 audit-subject surfaces (MINTCLN + HRROLL + DPNERF + RETRY_LOOTBOX_RNG) mid-sweep per D-297-RETRY-INTEGRATION-01.
- [x] **SWEEP-02**: All required adversarial hypothesis surfaces covered — 14 charged hypotheses (i)..(xiv) + 8 beyond-charge entries (5 `/zero-day-hunter` B1..B5 + 3 `/economic-analyst` (xv)..(xvii)). Per Phase 296 `296-01-ADVERSARIAL-LOG.md`.
- [x] **SWEEP-03**: Adversarial-pass disposition committed to `.planning/phases/296-cross-surface-adversarial-sweep-sweep/296-01-ADVERSARIAL-LOG.md` (integrated 3-H2 + Disposition section). Result: ZERO_FINDING after Tier-1 (xiv) ACCEPT_AS_DOCUMENTED resolution per user disposition 2026-05-18. Tier-2 (3-of-3 consensus) did NOT trigger. RE-PASS NOT triggered per D-296-REPASS-SCOPE-01.
- [x] **SWEEP-04**: 1 Tier-1 FINDING_CANDIDATE on (xiv) `retryLootboxRng` entropy-correlation under daily-flow-takeover composition — user disposition 2026-05-18 ACCEPT_AS_DOCUMENTED ("intended design"); no F-42-NN block authored; no FIX-SWEEP-NN commit landed. Cited at v42 audit deliverable §4.2 + §3.C 4th conservation invariant + §9.NN `ADVERSARIAL_TIER_1_RESOLVED` register entry.
- [x] **SWEEP-05**: Outcome — 0 additional F-42-NN findings beyond what surface phases (290 + 292 + 294) deliver. Wave shape: 0 USER-APPROVED contract commits + 0 USER-APPROVED test commits from SWEEP-finding-driven remediations + 1 AGENT-COMMITTED ADVERSARIAL-LOG bundle `f2bf0767`. Plus 1 USER-APPROVED mid-sweep feature commit `123f2dac` (retryLootboxRng — NOT a SWEEP-finding remediation; a user-authorized beyond-charge feature added mid-sweep).

### AUDIT — Delta audit + findings consolidation (Phase 297 Terminal; audit/FINDINGS-v42.0.md)

- [x] **AUDIT-01**: `audit/FINDINGS-v42.0.md` published FINAL READ-only at v42.0 closure HEAD (`chmod 444` post-closure-flip) with 9 sections per v40 + v41 precedent.
- [x] **AUDIT-02**: §3.A delta-surface table covers all v41→v42 audit-subject commits — 5 USER-APPROVED contract commits + 4 USER-APPROVED test commits across 4 audit-subject surfaces (MINTCLN + HRROLL + DPNERF + RETRY_LOOTBOX_RNG per `D-297-RETRY-INTEGRATION-01` mid-sweep expansion); 16 rows total (14 audit-subject-commit rows + 1 ANALYTICAL Phase 296 LOG row + 1 Phase 297 SOURCE-TREE-FROZEN attestation row).
- [x] **AUDIT-03**: §3.B 4-surface attestation matrix per `D-297-RETRY-INTEGRATION-01` — MINTCLN + HRROLL + DPNERF: zero new public/external entry points; RETRY_LOOTBOX_RNG: ONE new (`retryLootboxRng()` permissionless with 6h cooldown). Aggregate: only `TraitsGenerated` topic-hash signature change. Storage byte-identical across 4 surfaces.
- [x] **AUDIT-04**: §3.C 4-invariant conservation re-proof — (i) MINTCLN 256-bit seed-space invariant preserved via owed-in-baseKey carry; (ii) HRROLL VRF bit-slice non-collision; (iii) DPNERF deity-payout invariant; (iv) RETRY_LOOTBOX_RNG entropy-correlation under daily-flow-takeover composition (INTENDED design per Phase 296 (xiv) ACCEPT_AS_DOCUMENTED resolution).
- [x] **AUDIT-05**: §4 adversarial surfaces enumerated with citation chain — §4.1 14-charged + 8-beyond-charge hypothesis-disposition table copied verbatim from Phase 296 LOG; §4.2 dedicated Phase 296 disposition subsection per `D-297-VERDICT-01` (ZERO_FINDING result; 1 Tier-1 (xiv) ACCEPT_AS_DOCUMENTED).
- [x] **AUDIT-06**: KI walkthrough complete — EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v42 (with explicit distinction prose for the `retryLootboxRng` rng-retry path vs game-over surfaces per `D-297-KI-01`); EXC-04 STRUCTURALLY ELIMINATED preserved (grep proof: zero `entropyStep` matches in `contracts/`).
- [x] **AUDIT-07**: KNOWN-ISSUES.md UNMODIFIED at v42 close per `D-297-KI-01`. Closure verdict `KNOWN_ISSUES_UNMODIFIED`.
- [x] **AUDIT-08**: §9 closure verdict `0 of 0 F-42-NN RESOLVED_AT_V42; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per `D-297-VERDICT-01`. Closure signal `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets per `D-297-CLOSURE-01`. Atomic 5-doc closure flip across ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS landed at Commit 2.
- [x] **AUDIT-09**: Forward-cite zero-emission at Phase 297 terminal per `D-297-FCITE-01` — zero post-v42.0 milestone-version tokens or post-Phase-297 phase-number tokens emitted across scoped artifacts (`audit/FINDINGS-v42.0.md` + `297-FINDINGS-DRAFT.md`). §9d "Deferred to Future Milestones" 9-entry register uses locked-decision IDs + descriptive labels only per `D-297-DEFER-01`. SOURCE-TREE FROZEN — zero `contracts/` + zero `test/` mutations during Phase 297; audit deliverable + closure-flip docs AGENT-COMMITTED.

### REG — LEAN regression appendix (§5 of audit/FINDINGS-v42.0.md)

- [x] **REG-01**: PASS for v41.0 closure signal `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` non-widening at v42 close HEAD on v41-touched surfaces NOT in v42 scope. MINTCLN preserves Phase 281 cross-call seed separation via owed-in-baseKey carry; HRROLL preserves Phase 288 `dailyIdx` structural fix; deity common-tier baseline `max(len/50, 2)` UNCHANGED post-DPNERF.
- [x] **REG-02**: PASS for v40.0 closure signal `MILESTONE_V40_AT_HEAD_cd549499` non-widening at v42 close HEAD. LootboxModule Bernoulli + WWXRP consolation byte-identical; `JackpotModule._jackpotTicketRoll` Bernoulli + keccak self-mix byte-identical; event topic-hashes byte-identical except `TraitsGenerated` per MINTCLN-04; whole-BURNIE floor at 3 RNG-amount sites byte-identical.
- [x] **REG-03**: PASS for v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening at v42 close HEAD. TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical.
- [x] **REG-04**: PASS — prior-finding spot-check sweep across `audit/FINDINGS-v25..v41.0.md` for v42-touched function/surface set (including new RETRY_LOOTBOX_RNG surface per `D-297-RETRY-INTEGRATION-01`). Each prior finding re-verified RESOLVED or NEGATIVE-scope at v42 close HEAD.

## Future Requirements

_Carried forward into v43.0+ (next-milestone candidates):_

- **MINTCLN duplicate-logic helper extraction** — `processFutureTicketBatch` inline loop (mint:421-509) vs `processTicketBatch` + `_processOneTicketEntry` split (mint:670-834) parallel-emit/parallel-hash duplication; flag-only at v42 per D-42N-MINTCLN-SCOPE-01; cleanup eligible for v43+ maintenance bundle. Trace original design intent per `feedback_design_intent_before_deletion.md` before consolidation.
- **MINTCLN `processed` semantics asymmetry** — `processed += take` (mint:499) vs `processed += writesUsed >> 1` (mint:714); flagged at v42; out of scope for MINTCLN cleanup; cleanup eligible for v43+.
- **Lootbox empirical 55%-tickets-path gas-savings test pin** (LBX-02 RE-DEFERRED-V42+ per `D-40N-LBX02-OUT-01`) — fixture-coverage gap persists; analytical worst-case load-bearing per Phase 266 GAS-01 + `feedback_gas_worst_case.md`; carried unchanged from v38 + v39 + v40 + v41 close.
- **Superseded-baseline SURF `it.skip` cleanup** — pre-existing superseded-baseline SURF failures + the v41→v42 boundary SURF row for the MintModule patch; cleanup eligible for v43+ maintenance bundle.
- **Mint-boost fractional retirement** (`D-40N-MINTBOOST-OUT-01` v41-carry; future-milestone consideration) — `_queueTicketsScaled` + `_rollRemainder` + `rem` byte stay at `DegenerusGameMintModule.sol:1142`.
- **`runrewardjackpots` module-misplacement note** — stale 2026-04-02 backlog note; not v42.0-tagged; carries forward.
- **Game-over thorough hardening** — deferred to dedicated game-over hardening milestone.
- **Indexer-migration tooling** for the MINTCLN breaking `TraitsGenerated` topic-hash change — pre-launch posture accepts the migration as a forward-handoff; tooling lives outside the audit repo. v42.0 deliverable cites the handoff in §9; does NOT produce tooling.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| MINTCLN-01..10 | Phase 290 | Complete (Phase 290) |
| TST-MINTCLN-01..05 | Phase 291 | Complete (Phase 291) |
| HRROLL-01..10 | Phase 292 | Complete (Phase 292) |
| TST-HRROLL-01..06 | Phase 293 | Complete (Phase 293) |
| DPNERF-01..06 | Phase 294 | Complete (Phase 294) |
| TST-DPNERF-01..05 | Phase 295 | Complete (Phase 295) |
| SWEEP-01..05 | Phase 296 | Complete (Phase 296) |
| AUDIT-01..09 | Phase 297 | Complete (Phase 297) |
| REG-01..04 | Phase 297 | Complete (Phase 297) |

**Total: 60 / 60 requirements complete** across 8 phases (MINTCLN-01..10 + TST-MINTCLN-01..05 + HRROLL-01..10 + TST-HRROLL-01..06 + DPNERF-01..06 + TST-DPNERF-01..05 + SWEEP-01..05 + AUDIT-01..09 + REG-01..04). Milestone v42.0 SHIPPED 2026-05-18; closure signal `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2`.
