# Requirements — v57.0 Small-Feature Bundle + Day-Type UDVT Refactor

> **Baseline:** v56.0 HEAD — frozen contract subject `1e7a646d`, closure `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`.
> **Design-lock inputs:** `.planning/PLAN-WWXRP-JACKPOT-WHALEPASS.md` · `.planning/PLAN-TERMINAL-DECIMATOR-STREAK-BOOST.md` · the `type-day-udvt-post-v56-seed` + `handlepurchase-burnie-flip-batching-post-v56-seed` memories. All 5 items were PRE-VALIDATED READ-ONLY against the live `1e7a646d` tree on 2026-06-04 (verdicts + corrections folded in below).
> **Scope (USER-locked 2026-06-04):** bundle ALL of — (1) `handlePurchase` BURNIE-flip batching, (2) the WWXRP jackpot whale-halfpass, (3) the terminal-decimator final-day streak boost, (4) test/comment hygiene, (5) the repo-wide `type Day is uint24` UDVT, (6) **the BURNIE coin-buy ticket-queue Critical fix** (ADDED 2026-06-04 mid-358 — see BURNIE-01/02/03 below), (7) **the sDGNRS salvage-swap combo ETH/BURNIE pawn-shop payout** (ADDED 2026-06-04 mid-358 — see SALVAGE-01/02/03 below), (8) **manual sub-cancel auto-claim (self + affiliate tree) + auto-evict pure-forfeit** (ADDED 2026-06-04 mid-358 — see CANCEL-01/02/03 below; fixes a latent loss bug) — into ONE milestone, ONE carefully-sequenced batched contract diff. The UDVT is the heavy item and dominates IMPL/TST; the BURNIE fix is the highest-SEVERITY item.
> **Posture:** Hard floor on EVERY item = RNG-freeze intact + SOLVENCY-01 byte-untouched (each change is BURNIE-credit / weight-only / pure-annotation, OFF the ETH/`claimablePool` path). Carefully-sequenced batched USER-APPROVED contract diff (HARD STOP at the contract-commit boundary — only contract commits need approval; docs/tests run hands-off); sequential-on-main (worktrees unsafe: submodule + node_modules); pre-launch redeploy-fresh (storage break fine). FULL close (internal 3-skill genuine-parallel sweep IN-MILESTONE at TERMINAL, like v54/v55/v56). The v52 consolidated cross-model audit stays a SEPARATE future track.

---

## v57.0 Requirements

### BATCH — handlePurchase BURNIE-flip batching (manual path)
- [ ] **BATCH-01**: `DegenerusQuests.handlePurchase` returns `burnieMintReward` in `totalReturned` instead of crediting it inline (`coinflip.creditFlip(player, burnieMintReward)` at `DegenerusQuests.sol:947-949` dropped); the sole caller `DegenerusGameMintModule.sol:1220` already folds the full return into `lootboxFlipCredit` (single credit `:1355`) → same recipient + same amount + additive accumulator = behavior-equivalent, saving one cross-contract `creditFlip` (~5–25k gas) per buy that completes a MINT_BURNIE quest. The afking path is unaffected (it never calls `handlePurchase`).
- [ ] **BATCH-02**: The misleading `ethMintReward` / "ETH mint reward" naming + comments are corrected to reflect quest-TYPE semantics — `MINT_ETH`/`LOOTBOX`/`MINT_BURNIE` are quest TYPE constants and ALL quest rewards pay BURNIE flip stake; no payout is ETH.

### BURNIE — coin-buy ticket-queue Critical fix (ADDED 2026-06-04 mid-358; highest SEVERITY in the bundle)
> Discovered via the degenerus-sim afking-scale stress test; VERIFIED against the audit tree `1e7a646d` (4 decisive anchors below). Pre-launch / redeploy-fresh → no live-funds emergency, so it rides the normal v57.0 SPEC→IMPL→GAS→TST→TERMINAL flow rather than a separate hot-fix.
- [ ] **BURNIE-01**: `_purchaseCoinFor` (`DegenerusGameMintModule.sol:887-907`) calls `_callTicketPurchase(… payInCoin=true …)` and DISCARDS all four return values → the BURNIE ticket buy burns the coin (`_coinReceive` → `coin.burnCoin`) but queues ZERO tickets (a pure token sink; direct loss of funds on a live entrypoint, incl. `DegenerusVault.gamePurchaseTicketsBurnie:571-574`). Root cause: phase-160 (`24f0898b`) moved `_queueTicketsScaled` out of the shared tail of `_callTicketPurchase` into the ETH-only `_purchaseForWith:1251`, orphaning the coin path (the only purchase-side queue caller is `:1251`). FIX: capture the returns in `_purchaseCoinFor` and `if (adjustedQty != 0) _queueTicketsScaled(buyer, targetLevel, adjustedQty, false);` — restoring the pre-160 BURNIE→ticket behavior.
- [ ] **BURNIE-02**: Restore the MINT_BURNIE quest credit on the BURNIE buy path — route `burnieMintUnits` through `quests.handlePurchase`'s MINT_BURNIE leg ONLY (`ethMintSpendWei=0`, `lootBoxAmount=0`; deliberately SKIP activity-score/`recordMint`, affiliate, and non-mint quests — correct for BURNIE). **The quest reward is applied as a BURN REBATE** (USER 2026-06-04): it REDUCES the net BURNIE actually burned (net burn = full ticket cost − MINT_BURNIE reward, floored at 0), NOT a separate `creditFlip` — **BUT the tx still REQUIRES the player to afford the FULL ticket cost upfront** (the eligibility/cost gate is the full amount, not the net; the rebate can never enable a buy the player couldn't otherwise complete). Co-designed with BATCH-01 (which makes `handlePurchase` RETURN `burnieMintReward` instead of crediting inline): the BURNIE caller takes the returned reward and nets it against the coin burn (vs the ETH caller, which folds it into `lootboxFlipCredit`). Producer-before-consumer sequencing required — the reward must be known BEFORE the net burn, so the burn is deferred until after `handlePurchase` returns (the full-cost affordability check stays upfront).
- [x] **BURNIE-03** (SPEC, design-lock): lock BURNIE-01/02 + re-attest freeze/solvency for the fix — the exact `_purchaseCoinFor` queue-on-return algorithm; the `handlePurchase` MINT_BURNIE-leg-only routing + the burn-rebate mechanic (full-cost affordability gate, deferred net burn, rebate cap at full cost); the BATCH-01 reconciliation/sequencing; and the SEC framing: RNG-freeze unaffected (`purchaseCoin` reads no `rngWord`; gated by `_livenessTriggered`/`gameOverPossible`), and SOLVENCY — the ETH/`claimablePool` DEBIT code stays byte-unchanged (SEC-02 narrow), but this RESTORES ticket claims on the ETH prize pools (the intended pre-160 design; a genuine functional fix, unlike the other 5 weight-only/annotation items) — the posture widening is flagged explicitly. Grep-attest every BURNIE anchor vs `1e7a646d`.

### SALVAGE — sDGNRS far-future salvage swap: combo ETH/BURNIE pawn-shop payout (ADDED 2026-06-04 mid-358)
> Extends the v48.0 `sellFarFutureTickets` salvage swap. VERIFIED against `1e7a646d`: entry `DegenerusGame.sol:2074` → `DegenerusGameMintModule.sol:929`; quote/jitter `DegenerusGameMintStreakUtils.sol:145-190` (`seed = keccak(player, rngWordByDay[today-1])`, `jitterMult` 70–110%, `ticketShareBps` 40–80% → cash ≤60%); ETH relabel `MintModule:975-977`; helper `_ethToBurnieValue` `MintModule:1657`; `coinflip.creditFlip`; gated by `rngLockedFlag`/`gameOver`/`_livenessTriggered`. ETH/solvency-path + RNG-reading item → widens SEC-01 (RNG) + SEC-02 (solvency).
- [ ] **SALVAGE-01**: Turn the cash (non-ticket) leg into a combo ETH + BURNIE payout. Randomize a TARGET BURNIE portion of `cashWei` full-range `[0 .. cashWei]` from a NEW slice of the existing prior-day `seed` (no new VRF); the ETH out is the remainder and is bounded by the existing eth-% cap (the current cash ≤60%-of-`totalBudget` limit; ETH out ≤ `cashWei` ≤ cap always; BURNIE NOT counted against it). **The BURNIE leg is paid from sDGNRS-OWNED BURNIE — NOT a `creditFlip` mint** (USER 2026-06-04): sources = sDGNRS's BURNIE token balance (`burnie.balanceOf(SDGNRS)` via `transfer`/`transferFrom`) + sDGNRS's claimable coinflip credit (`coinflipAmount(SDGNRS)`/`previewClaimCoinflips`, consumed like the existing sDGNRS-stake-consume at `BurnieCoinflip:908`/`consumeCoinflipsForBurn:366`), valued via `_ethToBurnieValue(part, mintPrice())` @ current eth-equivalent. **Funding fallback:** pay `actualBurnie = min(target, sDGNRS-available)` from sDGNRS-owned BURNIE and the remainder (`cashWei − actualBurnie`) as ETH; if sDGNRS has ZERO BURNIE + ZERO claimable coinflip → no BURNIE leg, pay the whole cash leg in ETH ("do it more like now"). The ticket leg ("ticket-value") is UNCHANGED. Only the ETH part is relabeled out of `claimableWinnings[SDGNRS]` (≤ `cashWei` → solvency-POSITIVE: less ETH out, NO new BURNIE emission — the BURNIE is sDGNRS's own, transferred). BOTH the execute path (`MintModule:929/975`) and the preview/quote (`_quoteFarFutureSwap`) are updated (the preview must reflect source-availability + fallback) so the offer is previewable.
- [x] **SALVAGE-02** (SPEC, design-lock): lock the "pawn-shop" model + re-prove safety. The salvage makes a VARIABLE, KNOWABLE-IN-ADVANCE offer (total value + ETH/BURNIE split may differ, even intra-day) — the SOLE safety property is the **total payout cap (the existing no-arb ceiling) + the eth-% cap**: every reachable offer (across all seed × bundle × split × timing) ≤ the cap → NO exploitability even though predictable; suboptimal players forfeit EV to the protocol/pools (intended game design, NOT a leak). Lock: the ETH/BURNIE split randomization source (a new slice of the settled prior-day `seed`; reuse the `rngLockedFlag` gate → no new VRF); **the BURNIE-source rule** (the BURNIE leg is paid from sDGNRS-OWNED BURNIE — token balance + claimable coinflip stake — TRANSFERRED to the player, NOT `creditFlip`-minted; `actualBurnie = min(target, sDGNRS-available)`, the remainder + the zero-available case paid as ETH = current behavior); the solvency accounting (only the ETH part relabeled out of `claimableWinnings[SDGNRS]` ≤ `cashWei`, NO new BURNIE emission, the source-availability check prevents over-draw, SOLVENCY-01 ETH-debit invariant intact); and the freeze framing (no new manipulable VRF dependency — transparent function of the SETTLED prior-day word; the preview reflects source-availability). The no-arb re-proof obligation (extend `SWAP08_NoArbAtCeiling` to the new variability + the fallback path) is owned here (verified empirically at SALVAGE-03). Grep-attest all anchors vs `1e7a646d` (incl. the sDGNRS-owned-BURNIE source primitives).
- [ ] **SALVAGE-03** (TST): prove the combo payout + the caps + the no-arb ceiling. A test drives `sellFarFutureTickets` and asserts: when sDGNRS holds BURNIE, some swaps pay a BURNIE leg **transferred from sDGNRS-owned BURNIE** (token balance and/or claimable coinflip stake) and some pay ETH (the split varies); when sDGNRS has ZERO BURNIE + ZERO claimable coinflip, the swap pays the whole cash leg in ETH (the "like now" fallback); the BURNIE leg never over-draws sDGNRS (`actualBurnie ≤ sDGNRS-available`); ETH out ≤ the eth-% cap on every path; the total payout value ≤ the no-arb ceiling across the new variability + the fallback (EXTEND `test_SWAP08_NoArbAtCeiling_SweepAllDistances` so the max reachable offer still sits below the far-ticket acquisition floor); and SOLVENCY holds (claimablePool never exceeds balance after a salvage; only the ETH part leaves `claimableWinnings[SDGNRS]`, NO new BURNIE minted).

### CANCEL — manual sub-cancel auto-claim (self + affiliate tree); auto-evict pure-forfeit (ADDED 2026-06-04 mid-358)
> Afking-subscription cleanup. VERIFIED vs `1e7a646d`. **Fixes a LATENT LOSS BUG:** manual cancel (`GameAfkingModule.sol:345-362`) tombstones (`dailyQuantity=0`) and leaves `pendingBurnie`/`affiliateBase` in the slot (a comment promises "claim whenever"), BUT the next `processSubscriberStage` reclaim `delete _subOf[player]` (`:1144`) wipes both → the accruals are lost in the race if any advance runs before the player + an affiliate caller claim. All BURNIE-emission, OFF the ETH path, `rngLock`-gated → SOLVENCY-untouched + RNG-freeze-clean (back in the original v57 posture, unlike BURNIE/SALVAGE).
- [ ] **CANCEL-01**: manual cancel (`subscribe(…, dailyQuantity=0)`) auto-claims BEFORE clearing: (a) pay the canceller's `pendingBurnie` to themselves via `coinflip.creditFlip` + the presale-box-credit parity (mirror `claimAfkingBurnie:1560`; zero the field, CEI), (b) drain `affiliateBase` + settle the affiliate tree **A/U1/U2 = 75/20/5** (mirror `DegenerusAffiliate.claim:629` via `drainAffiliateBase:1605`; A = the canceller's referrer — the base is owed UPLINE, not to the canceller), (c) `_finalizeAfking` + clear. Closes the loss-on-reclaim race. **AUTO-EVICT** (pass-expiry `:1175` + funding-out `:1226` + tombstone-reclaim `:1144`) stays a **pure FORFEIT** (USER: "just delete the data") — delete the Sub record incl. BOTH accumulators, no payout to self OR uplines. (Make the forfeit explicit: today the pass/funding evict paths `_removeFromSet` but leave `_subOf` claimable out-of-set; the forfeit intent means they must wipe the accruals so they're truly unclaimable.)
- [x] **CANCEL-02** (SPEC, design-lock): lock the manual-cancel auto-claim (self + tree, then clear) + the auto-evict pure-forfeit asymmetry; document the latent loss bug it fixes; re-attest freeze/solvency — BURNIE-emission only via `creditFlip` (no `rngWord` read, no ETH/`claimablePool` touch; cancel is `rngLock`-gated at the `subscribe` entry; CEI: zero before credit). Grep-attest all anchors vs `1e7a646d`.
- [ ] **CANCEL-03** (TST): manual cancel pays the canceller's `pendingBurnie` + the tree A/U1/U2 even when an advance/STAGE runs immediately after (no loss); auto-evict forfeits both (self + uplines get nothing; accruals wiped + unclaimable post-evict); idempotent re-cancel / re-claim finds 0; CEI ordering holds.

### WWXRP — Degenerette jackpot whale-halfpass
- [ ] **WWXRP-01**: A player who hits the Degenerette jackpot (`s == 9` — the 8-match jackpot is relabeled S=9 in current code) on a `currency == CURRENCY_WWXRP` (=3) bet with `amountPerTicket >= MIN_BET_WWXRP` is awarded a Whale halfpass via `whalePassClaims[player] += 1`, **rationed to one award per 10-level bracket** (a per-bracket award flag keyed by `level/10` — USER override 2026-06-04, REPLACING the old plan's global "first 5 ever" 0→5 lifetime cap). Hook: `DegeneretteModule._resolveFullTicketBet` (~:713, after the existing s≥7 sDGNRS award; `player`/`currency`/`amountPerTicket` in scope). New `wwxrpJackpotWhalePass*` per-bracket state in `DegenerusGameStorage`.
- [x] **WWXRP-02**: The award is freeze-safe and cheap — it writes only an RNG-insensitive counter/mapping, gated by `s==9` (already deterministically derived from the committed `rngWord`), reuses the existing `claimWhalePass` future-ticket deferral (no ETH/`claimablePool` touch), and short-circuits to zero added cost on non-jackpot spins. The per-bracket rationing key and the operator-placed-bet recipient policy (pass to `player` = bet owner vs operator; whether one player may win multiple brackets) are settled at SPEC.

### TDEC — Terminal-decimator final-day streak boost
- [ ] **TDEC-01**: A new `boostTerminalDecimator()` lets a player on the final day multiply their terminal-decimator `weightedBurn` by their effective quest-streak factor (streak 100→20×, 10→4×), folding the delta into `terminalDecBucketBurnTotal[key]` via the existing `keccak256(abi.encode(lvl, bucket, subBucket))` key. **The boost ALSO updates the player's bucket if the boosted `weightedBurn` qualifies for an IMPROVED (better) bucket** — i.e. the boost can PROMOTE the bucket, not only raise the share within the originally-recorded bucket (USER directive 2026-06-04). The exact "improvement" rule (what makes a bucket better) + whether the `subBucket` is kept or re-derived on a bucket promotion are settled at SPEC. Mechanism reuses the live `DegenerusGameDecimatorModule` machinery (`recordTerminalDecBurn` / `runTerminalDecimatorJackpot` / `_terminalDecMultiplierBps`).
- [x] **TDEC-02**: The streak is validated via `getPlayerQuestView` (the EFFECTIVE streak with daily gap-reset + shields applied) — NOT the raw stored `playerQuestStates.streak`, which is stale/spoofable; the boost is idempotent via a `boosted` bit added to the packed `TerminalDecEntry` (24 spare bits), overflow-safe under `uint88 weightedBurn` (base × time-mult ≤20× × boost ≤20× ≈ 400× base — saturate or prove headroom), and the double-count policy against the existing burn-time `multBps` streak lever is resolved at SPEC (keep both levers vs strip streak from burn-time); shields consume-vs-read is decided at SPEC.
- [x] **TDEC-03**: The boost (and any bucket promotion under TDEC-01) is freeze-safe and solvency-neutral — it is weight-only, gated by `require(!gameOver)`, and lands in the SAME tx BEFORE `gameOver` flips and before subbuckets are drawn from `rngWord`. The boosted weight + any improved bucket are DETERMINISTIC from the player's fixed effective-streak factor × their burn, committed before any randomness is revealed → the player cannot use draw knowledge to manipulate placement (the freeze-safety argument is "all weight/bucket mutation precedes the draw", NOT "the bucket is immutable" — since TDEC-01 now allows promotion, the validator's original `subBucket`-fixed simplification no longer holds and SPEC must re-prove freeze-safety under the promotion allowance). The pool is still finalized in the resolution tx and shares still sum to the pool.

### UDVT — `type Day is uint24` repo-wide
- [ ] **UDVT-01**: A `type Day is uint24` user-defined value type with global operator overloads (`<`, `<=`, `==`, `%`, `+`, `-`) replaces the raw day-counter integers across the day-bearing contract surface (~649 lines / 27 contracts — heaviest DegenerusQuests/GameAfkingModule/DegenerusGameStorage/AdvanceModule/BoonModule), improving type-safety with zero behavior change. solc 0.8.34 supports UDVT + operator overloads (confirmed).
- [ ] **UDVT-02**: The RNG byte-image is preserved bit-for-bit — every `abi.encodePacked(…day…)` RNG-entropy site (`DegenerusGameAdvanceModule.sol:1828` + `:1405`, `DegenerusGame.sol:1011`) explicitly casts the day to `uint32` (the seed's "pure annotation" assumption is FALSE here: an unwrapped uint24-backed `Day` would shorten the keccak preimage and change the derived word); the `rngWordByDay` mapping KEY layout is unchanged (uint24/uint32 zero-pad to the same slot). Enforced by a per-site RNG-freeze byte-diff gate.
- [ ] **UDVT-03**: Storage layout + gas are preserved — packed `Sub`/struct day fields stay uint24-backed (no cold-slot spill → the v56 gas win intact), while standalone day slots + `indexed` day event topics widen to `uint32` explicitly; the forge/Hardhat suite is NON-WIDENING vs the `1e7a646d` baseline (every pre-existing red enumerated BY NAME).

### HYG — Test/comment hygiene (no contract logic change)
- [ ] **HYG-01**: The stale `gameSetAutoRebuy` references are fixed to `coinSetAutoRebuy(true, 0)` (renamed, two args, `onlyVaultOwner`) across the suites that error on the missing ABI member — `test/unit/GovernanceGating.test.js:247`, `test/unit/DegenerusVault.test.js:385/456`, `test/fuzz/CoverageGap222.t.sol:1055/1060/1084/1085` — restoring the intended `onlyVaultOwner`-rejects-non-majority assertions.
- [ ] **HYG-02**: The two stale `_runRewardJackpots` / `EndgameModule` comment references are corrected to `_consolidatePoolsAndRewardJackpots` (`DegenerusGameAdvanceModule.sol:1191`, `DegenerusGameDegeneretteModule.sol:809`); the reward-jackpot resolution is already inlined there (validated — no code change, comment-only).
- [ ] **HYG-03**: The doc/test drift surfaced by the BURNIE finding (ADDED 2026-06-04) — (a) ADD a POSITIVE test asserting `purchaseCoin` queues a near ticket at `targetLevel` AND applies the MINT_BURNIE burn rebate (the missing coverage that let BURNIE-01 hide — existing tests only assert negatives, e.g. `test/fuzz/FarFutureSalvageSwap.t.sol` `test_SWAP08`); (b) fix the stale 3-arg `purchaseCoin(address,uint256,uint256)` call in `test/fuzz/CoverageGap222.t.sol` → the live 2-arg `purchaseCoin(address,uint256)`; (c) correct the "blocked when RNG locked" docstrings on the `purchaseCoin` entrypoints (`DegenerusGame.sol` + `DegenerusGameMintModule.sol`) — the claim is unenforced (`_purchaseCoinFor` reads no `rngLockedFlag`).

### SEC — Security floor (the hard gate)
- [ ] **SEC-01**: RNG-freeze is intact across all eight items — no new player-manipulable read/write of VRF-derived state: the UDVT `abi.encodePacked` sites preserve the exact byte-image (UDVT-02), the WWXRP grant + the terminal-decimator boost touch only RNG-insensitive counters/weights gated by already-committed outcomes, `handlePurchase` batching is BURNIE-accounting only, the BURNIE ticket-queue fix reads no `rngWord` (`purchaseCoin` gated by `_livenessTriggered`/`gameOverPossible`), the SALVAGE combo split reads only the SETTLED prior-day word under the existing `rngLockedFlag` gate (no new VRF; non-exploitability rests on the total-payout cap, not unpredictability), and the CANCEL auto-claim reads no `rngWord` (`creditFlip`/`drainAffiliateBase` only; cancel is `rngLock`-gated at the `subscribe` entry). Proven empirically (byte-diff + determinism) + adversarially at the in-milestone close.
- [ ] **SEC-02**: SOLVENCY is preserved. Six of the eight items are BURNIE flip-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path (incl. CANCEL — pure BURNIE-emission via `creditFlip`, no ETH touch); the ETH/pool DEBIT code is byte-unchanged across ALL — SOLVENCY-01 byte-anchor holds. **Two functional exceptions, both flagged + both solvency-positive-or-neutral:** the BURNIE fix RESTORES queued ticket claims on the ETH prize pools (intended pre-160 design — no unbacked obligation; ticket wins stay pro-rata from available pool); the SALVAGE combo payout draws LESS ETH from `claimableWinnings[SDGNRS]` (only the ETH part, not the full `cashWei`) and pays the rest by TRANSFERRING sDGNRS-OWNED BURNIE (token balance + claimable coinflip stake — NO new emission; falls back to ETH when sDGNRS has none), so the ETH liability drops (solvency-POSITIVE). The solvency review explicitly covers both: claimablePool never exceeds balance after either path. Proven empirically (solvency-invariant) + adversarially.

### AUDIT — Terminal close
- [ ] **AUDIT-01**: The in-milestone TERMINAL close — a delta-audit (every changed surface NON-WIDENING vs the v56 baseline `1e7a646d`, with the RNG-freeze byte-diff + SOLVENCY-01 byte-anchor re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter) + `audit/FINDINGS-v57.0.md` (chmod 444) + the atomic 5-doc closure flip with the `MILESTONE_V57_AT_HEAD_<sha>` signal — re-attests all v57.0 requirements against the frozen closure HEAD.

## Future Requirements (deferred)
- Generalized operator-spend of `claimableWinnings` (carried from v54/v55/v56) — larger blast radius, separate optional feature.

## Out of Scope
- The v52 consolidated cross-model audit (separate track; the v57 surface folds into it as an additional track, not a substitute for v57's own in-milestone close).
- Any ETH/`claimablePool`/solvency-path change (this is a BURNIE-emission / weight / type-annotation change only — SOLVENCY-01 byte-untouched is a hard floor).
- The already-validated NON-ISSUES confirmed already-done at the 2026-06-04 validation (NOT work): O1 quest lootbox double-credit (already credited exactly once by the v56 routing rework), the resolveLootboxRoll dead burnie-conversion branch (refactored away), the runrewardjackpots module misplacement (already inlined — only the 2 stale comments remain → HYG-02), and the 4 older balance notes (degenerette recalibration, burnie near-future per-pull-level, deity-pass gold nerf, hero-override weighted roll — all shipped in commits 267/292/etc).
- Off-chain indexer / webpage (separate frontend track).

## Traceability

_Phase mapping (roadmapper, 2026-06-04; BURNIE + SALVAGE categories + HYG-03 ADDED 2026-06-04 mid-358 — the coin-buy ticket-queue Critical bug + the salvage-swap combo ETH/BURNIE pawn-shop payout, folded in after 358 discuss). Each REQ-ID maps to exactly ONE owning phase. Phases continue from 357 → 358. Shape: 358 SPEC → 359 IMPL → 360 GAS → 361 TST → 362 TERMINAL (the established v54/v55/v56 audit pattern). **25/25 mapped — 0 orphaned, 0 duplicated.** Per-category: BATCH 2 (IMPL) · BURNIE 3 (2 IMPL + 1 SPEC design half) · SALVAGE 3 (1 IMPL + 1 SPEC design half + 1 TST) · CANCEL 3 (1 IMPL + 1 SPEC design half + 1 TST) · WWXRP 2 (1 SPEC design half + 1 IMPL) · TDEC 3 (2 SPEC design half + 1 IMPL) · UDVT 3 (IMPL; the gas-half measured at 360, the regression-half proven at 361) · HYG 3 (TST) · SEC 2 (TST) · AUDIT 1 (TERMINAL). Phase 360 GAS owns NO REQ-ID — it is the gas-neutrality measurement gate (the v56 packed-Sub uint24 gas win must be PROVEN intact; Outcome-A no-diff in the expected gas-neutral-by-construction case)._

| Requirement | Phase | Phase Type | Status |
|-------------|-------|------------|--------|
| BATCH-01 | 359 | IMPL | Not started |
| BATCH-02 | 359 | IMPL | Not started |
| BURNIE-01 | 359 | IMPL | Not started |
| BURNIE-02 | 359 | IMPL | Not started |
| BURNIE-03 | 358 | SPEC | Complete |
| SALVAGE-01 | 359 | IMPL | Not started |
| SALVAGE-02 | 358 | SPEC | Complete |
| SALVAGE-03 | 361 | TST | Not started |
| CANCEL-01 | 359 | IMPL | Not started |
| CANCEL-02 | 358 | SPEC | Complete |
| CANCEL-03 | 361 | TST | Not started |
| WWXRP-01 | 359 | IMPL | Not started |
| WWXRP-02 | 358 | SPEC | Complete |
| TDEC-01 | 359 | IMPL | Not started |
| TDEC-02 | 358 | SPEC | Not started |
| TDEC-03 | 358 | SPEC | Not started |
| UDVT-01 | 359 | IMPL | Not started |
| UDVT-02 | 359 | IMPL | Not started |
| UDVT-03 | 359 | IMPL | Not started |
| HYG-01 | 361 | TST | Not started |
| HYG-02 | 361 | TST | Not started |
| HYG-03 | 361 | TST | Not started |
| SEC-01 | 361 | TST | Not started |
| SEC-02 | 361 | TST | Not started |
| AUDIT-01 | 362 | TERMINAL | Not started |
