# Build artifacts
node_modules/
cache/
artifacts/
forge-out/
foundry.lock
out/
coverage/
lcov.info
.gas-snapshot
.gas-snapshot-*

# Local state
.env
runs/
scripts/*.db
*.sol.bak
/memory
.claude/

# Internal planning artifacts
.planning/

# Internal audit/review docs — keep only the contest readme visible
audit/*
!audit/C4A-CONTEST-README.md

# Machine-local bytecode-identity snapshot (scripts/bytecode-manifest.sh)
.bytecode-manifest

# v74 adversarial agent: runtime state + local-node deployment artifacts + design scratch
agent/.state/
deployments/localhost.json
deployments/localhost-abis/
.agent-design/
