# v4.0 Ticket Processing Mechanics Audit

**Date:** 2026-03-23
**Milestone:** v4.0 (Ticket Lifecycle & RNG-Dependent Variable Re-Audit)
**Phase:** 82 (Ticket Processing Mechanics)
**Methodology:** Exhaustive code trace with file:line citations; all prior audit prose treated as unverified

## Executive Summary

Exhaustive trace of both ticket processing functions -- `processTicketBatch` (JackpotModule) and `processFutureTicketBatch` (MintModule) -- documenting entry points, all callers, trigger conditions from advanceGame, processing loop structure, gas budgeting, and the complete RNG word derivation chain from VRF callback through trait generation. Two distinct entropy sources are identified and traced independently.

### Key Conclusions

1. `processTicketBatch` (JM:1889) uses `lastLootboxRngWord` (JM:1915) as entropy; triggered from 3 advanceGame paths via `_runProcessTicketBatch` (AM:1198)
2. `processFutureTicketBatch` (MM:298) uses `rngWordCurrent` (MM:301) as entropy; triggered from 2 call sites via `_processFutureTicketBatch` (AM:1134)
3. Both share identical `_raritySymbolBatch` LCG PRNG algorithm (duplicated for delegatecall isolation) with the same LCG constant
4. The mid-day path entropy source for `processTicketBatch` can differ from the daily word -- it reads from `lootboxRngWordByIndex[lootboxRngIndex - 1]` (AM:159) rather than the daily VRF word
5. Five total advanceGame trigger points for ticket processing are identified and verified

---

## 1. processTicketBatch Entry Point (TPROC-01)

### 1.1 Function Signature

```
function processTicketBatch(uint24 lvl) external returns (bool finished)
```

**Location:** JM:1889
**Module:** DegenerusGameJackpotModule.sol
**Return:** `bool finished` -- true if all tickets for this level have been fully processed

### 1.2 Delegatecall Entry

The single entry point into `processTicketBatch` is `_runProcessTicketBatch` at AM:1198:

```
function _runProcessTicketBatch(uint24 lvl) private returns (bool worked, bool finished)
```

**Mechanism (AM:1198-1215):**
1. Records pre-call cursor state: `prevCursor = ticketCursor` (AM:1201), `prevLevel = ticketLevel` (AM:1202)
2. Delegatecalls `GAME_JACKPOT_MODULE` with selector `IDegenerusGameJackpotModule.processTicketBatch.selector` (AM:1203-1210)
3. Reverts on failure via `_revertDelegate(data)` (AM:1211)
4. Reverts with `E()` if `data.length == 0` (AM:1212)
5. Decodes `finished` from return data (AM:1213)
6. Determines `worked` by comparing cursors: `worked = (ticketCursor != prevCursor) || (ticketLevel != prevLevel)` (AM:1214)

### 1.3 advanceGame Trigger Paths

**Path 1: Mid-day (same day) -- AM:154-181**

Conditions:
- `day == dailyIdx` (AM:154) -- caller purchases on the same day as the current daily index
- `!ticketsFullyProcessed` (AM:156) -- read slot not yet fully drained

Processing flow:
1. If `midDayTicketRngPending` is true (AM:158), reads `word = lootboxRngWordByIndex[lootboxRngIndex - 1]` (AM:159)
   - Reverts with `NotTimeYet()` if `word == 0` (AM:160) -- VRF not yet arrived
   - Sets `lastLootboxRngWord = word` (AM:162) -- **updates entropy source from mid-day lootbox VRF word**
2. Checks `_tqReadKey(purchaseLevel)` queue has entries (AM:165-166)
3. Calls `_runProcessTicketBatch(purchaseLevel)` (AM:170)
4. If `ticketWorked || !ticketsFinished` (AM:171):
   - If `ticketsFinished`: sets `ticketsFullyProcessed = true` (AM:173), clears `midDayTicketRngPending = false` (AM:174)
   - Emits `Advance(STAGE_TICKETS_WORKING, lvl)` (AM:176)
   - Credits caller bounty (AM:177)
   - Returns (AM:178) -- single batch per call

**Path 2: Daily drain gate -- AM:204-219**

Conditions:
- `day > dailyIdx` (implicit from reaching this code path after AM:184 `revert NotTimeYet()` for same-day)
- `!ticketsFullyProcessed` (AM:205) -- read slot not yet fully drained

Processing flow:
1. Checks `_tqReadKey(purchaseLevel)` queue has entries (AM:206-207)
2. Calls `_runProcessTicketBatch(purchaseLevel)` (AM:211)
3. If `ticketWorked || !ticketsFinished` (AM:212):
   - Emits `Advance(STAGE_TICKETS_WORKING, lvl)` (AM:213)
   - Credits caller bounty (AM:214)
   - Returns (AM:215) -- single batch per call
4. If queue empty or just finished: sets `ticketsFullyProcessed = true` (AM:218) -- **regardless of whether processing ran**

**Path 3: New-day post-RNG current-level -- AM:269-276**

Conditions:
- `day > dailyIdx` (new day)
- `ticketsFullyProcessed` is true (daily drain gate passed at AM:204-219)
- RNG word received (`rngGate` returned non-1 at AM:225-236)
- `!phaseTransitionActive` (AM:239)
- Near-future ticket processing complete (AM:255-266, `_prepareFutureTickets(lvl)` returned true)

Processing flow:
1. Calls `_runProcessTicketBatch(purchaseLevel)` (AM:269-271)
2. If `ticketWorked || !ticketsFinished` (AM:272):
   - Sets `stage = STAGE_TICKETS_WORKING` (AM:273)
   - Breaks to emit advance event and credit bounty
3. On completion: sets `ticketsFullyProcessed = true` at AM:276 (comment: "ADV-03: set before jackpot/phase logic")

### 1.4 Processing Loop Structure

**processTicketBatch (JM:1889-1950):**

1. **Queue selection:** `rk = _tqReadKey(lvl)` (JM:1890), `queue = ticketQueue[rk]` (JM:1891)
2. **Level switch:** If `ticketLevel != lvl`: resets `ticketLevel = lvl`, `ticketCursor = 0` (JM:1895-1897)
3. **Already done check:** If `idx >= total`: deletes queue, resets cursor to 0, resets level to 0, returns `true` (JM:1901-1907)
4. **Gas budget:** `writesBudget = WRITES_BUDGET_SAFE` (550) at JM:1909; if `idx == 0` (first batch): `writesBudget -= (writesBudget * 35) / 100` = 357 (JM:1910-1912) -- 65% scaling for cold storage
5. **Entropy source:** `entropy = lastLootboxRngWord` (JM:1915)
6. **Processing loop (JM:1918-1937):** While `idx < total && used < writesBudget`:
   - Calls `_processOneTicketEntry(queue[idx], lvl, rk, writesBudget - used, processed, entropy, idx)` (JM:1919-1927)
   - Returns `(writesUsed, advance)` (JM:1919)
   - If `writesUsed == 0 && !advance`: breaks (budget exhausted) (JM:1928)
   - If `advance`: increments `idx`, resets `processed = 0` (JM:1931-1933)
   - Else: `processed += writesUsed >> 1` (approximate tickets processed) (JM:1935)
7. **Save cursor:** `ticketCursor = uint32(idx)` (JM:1940)
8. **Completion check:** If `idx >= total`: deletes queue, resets `ticketCursor = 0`, `ticketLevel = 0`, returns `true` (JM:1942-1948)

### 1.5 Helper Chain (processTicketBatch only)

`processTicketBatch` delegates to a chain of four helper functions. `processFutureTicketBatch` has inline processing (no helpers).

**_processOneTicketEntry (JM:1984-2047):**
1. Reads `packed = ticketsOwedPacked[rk][player]` (JM:1993), extracts `owed = uint32(packed >> 8)` (JM:1994)
2. Constructs `rollSalt = (uint256(lvl) << 224) | (queueIdx << 192) | (uint256(uint160(player)) << 32)` (JM:1995-1997)
3. Zero-owed handling: calls `_resolveZeroOwedRemainder` (JM:2002-2009)
4. Budget calculation: `baseOv = (processed == 0 && owed <= 2) ? 4 : 2` (JM:2017), batch size limited by `room - baseOv` (JM:2021-2025)
5. Calls `_generateTicketBatch(player, lvl, processed, take, entropy, queueIdx)` (JM:2030)
6. Calculates actual writes used (JM:2033-2036)
7. Calls `_finalizeTicketEntry(rk, player, packed, owed, take, entropy, rollSalt)` (JM:2037-2045)

**_generateTicketBatch (JM:2050-2070):**
1. Constructs `baseKey = (uint256(lvl) << 224) | (queueIdx << 192) | (uint256(uint160(player)) << 32)` (JM:2058-2060)
2. Calls `_raritySymbolBatch(player, baseKey, processed, take, entropy)` (JM:2061)
3. Emits `TraitsGenerated(player, lvl, uint32(queueIdx), processed, take, entropy)` (JM:2062-2069)

**_finalizeTicketEntry (JM:2073-2098):**
1. Extracts `rem = uint8(packed)` (JM:2082)
2. Calculates `remainingOwed = owed - take` (JM:2085)
3. If `remainingOwed == 0 && rem != 0`: calls `_rollRemainder(entropy, rollSalt, rem)` (JM:2087-2088); if wins, `remainingOwed = 1` (JM:2089)
4. Updates `ticketsOwedPacked[rk][player]` if changed (JM:2093-2095)
5. Returns `done = (remainingOwed == 0)` (JM:2097)

### 1.6 Gas Budget Detail

| Constant | Value | Location |
|----------|-------|----------|
| `WRITES_BUDGET_SAFE` | 550 | JM:157 |
| Cold storage scaling | 35% reduction (first batch) | JM:1910-1911 |
| Effective first-batch budget | 357 | Computed: `550 - (550 * 35) / 100` |
| Per-ticket write cost | ~2 SSTOREs (trait array push + length) | JM:2198-2216 assembly |
| Per-player overhead | 2-4 (baseOv depends on processed == 0 && owed <= 2) | JM:2017 |

---

## 2. processFutureTicketBatch Entry Point (TPROC-02)

### 2.1 Function Signature

```
function processFutureTicketBatch(uint24 lvl) external returns (bool worked, bool finished, uint32 writesUsed)
```

**Location:** MM:298
**Module:** DegenerusGameMintModule.sol
**Return:** Three values: `worked` (any tickets minted), `finished` (queue fully drained), `writesUsed` (budget units consumed)

### 2.2 Delegatecall Entry

The single entry point into `processFutureTicketBatch` is `_processFutureTicketBatch` at AM:1134:

```
function _processFutureTicketBatch(uint24 lvl) private returns (bool worked, bool finished, uint32 writesUsed)
```

**Mechanism (AM:1134-1148):**
1. Delegatecalls `GAME_MINT_MODULE` with selector `IDegenerusGameMintModule.processFutureTicketBatch.selector` (AM:1137-1143)
2. Reverts on failure via `_revertDelegate(data)` (AM:1145)
3. Reverts with `E()` if `data.length == 0` (AM:1146)
4. Decodes `(bool, bool, uint32)` from return data (AM:1147)

### 2.3 Call Sites

**Call Site 1: _prepareFutureTickets -- AM:1156-1184**

Called at AM:262 in the new-day path: `_prepareFutureTickets(lvl)`

**_prepareFutureTickets (AM:1156-1184):**
1. Computes range: `startLevel = lvl + 2`, `endLevel = lvl + 6` (AM:1157-1158)
2. Reads resume state: `resumeLevel = ticketLevel` (AM:1159), `baseResume = resumeLevel & ~uint24(TICKET_FAR_FUTURE_BIT)` (AM:1162) -- strips FF bit for range comparison
3. **Resume in-flight level first (AM:1165-1170):** If `baseResume >= startLevel && baseResume <= endLevel`, calls `_processFutureTicketBatch(baseResume)`. If `worked || !levelFinished`: returns false (more work needed)
4. **Iterate remaining levels (AM:1173-1183):** For `target = startLevel` to `endLevel`, skipping `baseResume`, calls `_processFutureTicketBatch(target)`. If `worked || !levelFinished`: returns false
5. Returns true when all 5 target levels fully processed

**Call Site 2: Direct call at AM:305**

Location: AM:298-310, in the purchase-phase last-purchase-day path

Conditions:
- `!inJackpot` (AM:279) -- still in purchase phase
- `lastPurchaseDay` is true (AM:281-296 -- target prize pool reached)
- After daily jackpots and current-level ticket processing complete

Processing flow:
1. `nextLevel = purchaseLevel + 1` (AM:300)
2. Calls `_processFutureTicketBatch(nextLevel)` (AM:305)
3. If `futureWorked || !futureFinished` (AM:306): sets `stage = STAGE_FUTURE_TICKETS_WORKING` (AM:307), breaks

### 2.4 Dual-Queue Drain Logic

`processFutureTicketBatch` implements a two-phase queue drain (read-side first, then far-future):

**Phase detection (MM:302):**
`inFarFuture = (ticketLevel == (lvl | TICKET_FAR_FUTURE_BIT))`

**Queue selection (MM:303):**
`rk = inFarFuture ? _tqFarFutureKey(lvl) : _tqReadKey(lvl)`

**Empty queue handling (MM:306-318):**
If `total == 0`:
- a. If NOT in FF phase: checks `_tqFarFutureKey(lvl)` for entries (MM:308-309)
  - If FF queue has entries: sets `ticketLevel = lvl | TICKET_FAR_FUTURE_BIT`, resets `ticketCursor = 0` (MM:310-311), returns `(false, false, 0)` -- signals FF transition
  - If FF queue also empty: resets `ticketCursor = 0`, `ticketLevel = 0` (MM:315-316), returns `(false, true, 0)` -- done
- b. If in FF phase (and FF queue is empty): same reset (MM:315-316), returns done

**Level switch (MM:320-323):**
If `!inFarFuture && ticketLevel != lvl`: sets `ticketLevel = lvl`, `ticketCursor = 0`

**Read-side exhausted mid-processing (MM:326-338):**
If `idx >= total` (read-side fully iterated):
1. Deletes `ticketQueue[rk]` (MM:327)
2. If NOT in FF phase: checks FF queue for entries (MM:329-330)
   - If FF exists: transition to FF (MM:331-332), returns `(false, false, 0)`
   - If no FF: resets cursor, returns `(false, true, 0)` (MM:336-338)

**Completion on finish (MM:436-452):**
When inline processing loop completes (`idx >= total`):
1. Deletes queue (MM:437)
2. If NOT in FF: checks for FF transition (MM:438-443)
3. If in FF (or no FF entries): resets `ticketCursor = 0`, `ticketLevel = 0` (MM:445-446 or MM:449-450)

### 2.5 Inline Processing Loop

Unlike `processTicketBatch` which delegates to a helper chain, `processFutureTicketBatch` has the processing logic inline (MM:350-430):

1. **Load player data (MM:351-357):**
   - `player = queue[idx]` (MM:351)
   - `baseKey = (uint256(lvl) << 224) | (idx << 192) | (uint256(uint160(player)) << 32)` (MM:352-354)
   - `packed = ticketsOwedPacked[rk][player]` (MM:355)
   - `owed = uint32(packed >> 8)`, `rem = uint8(packed)` (MM:356-357)

2. **Zero-owed handling (MM:358-382):**
   - If `owed == 0 && rem == 0`: cleanup skip, charges 1 budget unit (MM:359-367)
   - If `owed == 0 && rem != 0`: calls `_rollRemainder(entropy, baseKey, rem)` (MM:369)
     - Roll fails: cleanup, advance (MM:370-373)
     - Roll wins: set `packed = uint40(1) << 8`, `owed = 1`, `rem = 0` (MM:375-381)

3. **Batch size calculation (MM:383-390):**
   - `room = writesBudget - used` (MM:383)
   - `baseOv = (processed == 0 && owed <= 2) ? 4 : 2` (MM:384)
   - `maxT = (room <= 256) ? (room / 2) : (room - 256)` (MM:388)
   - `take = owed > maxT ? maxT : owed` (MM:389)

4. **Trait generation (MM:392):**
   - `_raritySymbolBatch(player, baseKey, processed, take, entropy)` -- same function as JM copy

5. **Finalization (MM:407-429):**
   - Calculates `remainingOwed = owed - take` (MM:409)
   - Remainder roll if `remainingOwed == 0 && rem != 0` (MM:411-415)
   - Updates `ticketsOwedPacked[rk][player]` if changed (MM:418-420)
   - If `remainingOwed == 0`: advances `idx`, resets `processed = 0` (MM:426-428)

### 2.6 Structural Difference from processTicketBatch

| Aspect | processTicketBatch (JM) | processFutureTicketBatch (MM) |
|--------|------------------------|------------------------------|
| Loop body | Delegated to `_processOneTicketEntry` (JM:1984) | Inline (MM:350-430) |
| Helper chain | `_processOneTicketEntry` -> `_generateTicketBatch` -> `_raritySymbolBatch` -> `_finalizeTicketEntry` | Direct `_raritySymbolBatch` call |
| baseKey construction | In `_generateTicketBatch` (JM:2058-2060) | Inline (MM:352-354) |
| Emit pattern | In `_generateTicketBatch` (JM:2062-2069) | Inline (MM:393-400) |
| Queue drain | Single queue: `_tqReadKey` only | Dual queue: `_tqReadKey` then `_tqFarFutureKey` |
| Entropy source | `lastLootboxRngWord` (JM:1915) | `rngWordCurrent` (MM:301) |
| Gas budget | Identical: WRITES_BUDGET_SAFE=550, 65% cold scaling | Identical: WRITES_BUDGET_SAFE=550, 65% cold scaling |

---

## 3. advanceGame Trigger Summary Table

| # | Path | Location | When | Function Called |
|---|------|----------|------|-----------------|
| 1 | Mid-day (same day) | AM:154-181 | `day == dailyIdx`, `!ticketsFullyProcessed`, queue non-empty | `_runProcessTicketBatch(purchaseLevel)` (AM:170) |
| 2 | Daily drain gate | AM:204-219 | `day > dailyIdx`, `!ticketsFullyProcessed`, queue non-empty | `_runProcessTicketBatch(purchaseLevel)` (AM:211) |
| 3 | New-day post-RNG current-level | AM:269-276 | After RNG, after `_prepareFutureTickets`, `!phaseTransitionActive` | `_runProcessTicketBatch(purchaseLevel)` (AM:269) |
| 4 | New-day near-future (lvl+2..lvl+6) | AM:262 via AM:1156 | After RNG, before daily draws, `!dailyJackpotCoinTicketsPending` etc. | `_prepareFutureTickets(lvl)` -> `_processFutureTicketBatch` per level (AM:1166/1175) |
| 5 | Last-purchase-day next-level | AM:305 | After current-level done, `lastPurchaseDay`, before pool consolidation | `_processFutureTicketBatch(nextLevel)` (AM:305) |

**Verification notes:**
- Path 1 condition verified at AM:154 (`day == dailyIdx`) and AM:156 (`!ticketsFullyProcessed`)
- Path 2 condition: `day > dailyIdx` is implicit (code reaches AM:204 only after AM:184 `revert NotTimeYet()` blocks same-day)
- Path 3 verified at AM:269; near-future processing guard at AM:255-266
- Path 4 verified: `_prepareFutureTickets` called at AM:262; iterates levels `lvl+2..lvl+6` (AM:1157-1158)
- Path 5 verified at AM:300-305; `nextLevel = purchaseLevel + 1`

All five trigger points confirmed against current code.

---

## 4. RNG Word Derivation Chain (TPROC-03)

Two distinct entropy chains feed into the two ticket processing functions. Both originate from Chainlink VRF but diverge at different storage points.

### 4.1 Chain A: processTicketBatch Entropy (lastLootboxRngWord)

**Step 1: VRF callback -- rawFulfillRandomWords (AM:1442-1463)**
1. Receives `requestId` and `randomWords[0]` from VRF coordinator (AM:1442-1444)
2. Validates `msg.sender == vrfCoordinator` (AM:1446)
3. Guards: `requestId == vrfRequestId && rngWordCurrent == 0` (AM:1447)
4. Normalizes zero: `if (word == 0) word = 1` (AM:1450)
5. **Branching on `rngLockedFlag`:**
   - If `rngLockedFlag` true (daily RNG): stores `rngWordCurrent = word` (AM:1454) -- raw word awaits `rngGate` processing
   - If `rngLockedFlag` false (mid-day RNG): directly finalizes lootbox -- stores `lootboxRngWordByIndex[index] = word` (AM:1458), emits event (AM:1459), clears `vrfRequestId` and `rngRequestTime` (AM:1460-1461)

**Step 2 (daily path): rngGate -- AM:768-841**
1. Called during `advanceGame` new-day path (AM:225)
2. If `rngWordByDay[day] != 0`: returns cached (AM:776)
3. If `rngWordCurrent != 0 && rngRequestTime != 0` (AM:781): has fresh VRF word
4. Calls `_applyDailyRng(day, currentWord)` (AM:798) -- **applies nudges, stores result**
5. Calls `_finalizeLootboxRng(currentWord)` (AM:824) -- **stores to lastLootboxRngWord**
6. Returns the nudge-adjusted word (AM:825)

**Step 3: _applyDailyRng -- AM:1523-1539**
1. Reads `nudges = totalFlipReversals` (AM:1527)
2. Applies: `finalWord = rawWord + nudges` (AM:1531) if nudges non-zero
3. Clears `totalFlipReversals = 0` (AM:1533)
4. Stores: `rngWordCurrent = finalWord` (AM:1535) -- **overwrites raw with nudged word**
5. Stores: `rngWordByDay[day] = finalWord` (AM:1536)
6. Emits `DailyRngApplied(day, rawWord, nudges, finalWord)` (AM:1538)

**Step 4: _finalizeLootboxRng -- AM:843-848**
1. Reads `index = lootboxRngIndex - 1` (AM:844)
2. Guards: `if (lootboxRngWordByIndex[index] != 0) return` (AM:845) -- already filled (e.g., mid-day)
3. Stores: `lootboxRngWordByIndex[index] = rngWord` (AM:846) -- the nudge-adjusted word from _applyDailyRng
4. Stores: `lastLootboxRngWord = rngWord` (AM:847) -- **this is what processTicketBatch reads**
5. Emits `LootboxRngApplied(index, rngWord, vrfRequestId)` (AM:848)

**Step 5: processTicketBatch reads entropy**
- `entropy = lastLootboxRngWord` at JM:1915
- Passed to `_processOneTicketEntry` (JM:1919-1927) as `entropy` parameter
- Flows to `_generateTicketBatch` (JM:2030) then `_raritySymbolBatch` (JM:2061)

**Daily path summary:** rawFulfillRandomWords (AM:1454) -> rngWordCurrent -> rngGate (AM:798) -> _applyDailyRng (AM:1535, applies nudges) -> _finalizeLootboxRng (AM:847, copies to lastLootboxRngWord) -> processTicketBatch (JM:1915, reads lastLootboxRngWord)

### 4.2 Chain B: processFutureTicketBatch Entropy (rngWordCurrent)

**Steps 1-3: Same as Chain A** -- rawFulfillRandomWords -> rngGate -> _applyDailyRng

The key difference: `processFutureTicketBatch` reads `rngWordCurrent` (MM:301), which is the nudge-adjusted daily VRF word stored by `_applyDailyRng` at AM:1535.

**Step 4: processFutureTicketBatch reads entropy**
- `entropy = rngWordCurrent` at MM:301
- Passed to `_raritySymbolBatch(player, baseKey, processed, take, entropy)` inline at MM:392

**Daily path summary:** rawFulfillRandomWords (AM:1454) -> rngWordCurrent -> rngGate (AM:798) -> _applyDailyRng (AM:1535, applies nudges, stores back to rngWordCurrent) -> processFutureTicketBatch (MM:301, reads rngWordCurrent)

### 4.3 Entropy Source Comparison

| Property | Chain A (processTicketBatch) | Chain B (processFutureTicketBatch) |
|----------|-----------------------------|------------------------------------|
| Variable read | `lastLootboxRngWord` (JM:1915) | `rngWordCurrent` (MM:301) |
| Set by (daily) | `_finalizeLootboxRng` at AM:847 | `_applyDailyRng` at AM:1535 |
| Set by (mid-day) | AM:162 from `lootboxRngWordByIndex` | Not applicable (MM only called in new-day path) |
| Value (daily) | Same as rngWordCurrent (nudge-adjusted) | Nudge-adjusted raw VRF word |
| Value (mid-day) | Mid-day lootbox VRF word (different VRF request) | N/A |
| Storage location | GS:1240 (`lastLootboxRngWord`) | GS:362 (`rngWordCurrent`) |

**Critical finding:** In the daily path, both variables hold the same nudge-adjusted VRF word. But in the mid-day path, `lastLootboxRngWord` can hold a different VRF word sourced from `lootboxRngWordByIndex`. See Section 4.5 for detailed analysis.

### 4.4 Mid-Day Entropy Divergence Analysis

**Question:** Can `processTicketBatch` use a different entropy word than the daily VRF word?

**Answer: Yes.** In the mid-day path (AM:154-181), `lastLootboxRngWord` is explicitly set from a mid-day lootbox VRF word, not the daily word.

**Trace:**
1. Mid-day path entered: `day == dailyIdx` (AM:154)
2. `!ticketsFullyProcessed` (AM:156) and `midDayTicketRngPending` (AM:158)
3. Reads `word = lootboxRngWordByIndex[lootboxRngIndex - 1]` (AM:159)
4. Requires `word != 0` (AM:160) -- VRF must have arrived
5. Sets `lastLootboxRngWord = word` (AM:162) -- **this is a mid-day lootbox VRF word**
6. `processTicketBatch` subsequently reads `entropy = lastLootboxRngWord` (JM:1915) -- gets the mid-day word

**How `lootboxRngWordByIndex` gets the mid-day word:**
- During `rawFulfillRandomWords`, when `rngLockedFlag` is false (mid-day): `lootboxRngWordByIndex[index] = word` (AM:1458)
- This is a separate VRF request from the daily one -- the mid-day lootbox RNG request is triggered by purchase events that need lootbox randomness

**Entropy divergence confirmed:**
- **Daily path (Paths 2 & 3):** `lastLootboxRngWord` = nudge-adjusted daily VRF word (set by `_finalizeLootboxRng` at AM:847)
- **Mid-day path (Path 1):** `lastLootboxRngWord` = mid-day lootbox VRF word (set directly at AM:162)
- These are different VRF request/fulfillment pairs, producing different random words

**Security implication:** This is by design. The mid-day path processes tickets with the entropy that was pending (the lootbox RNG word), while the daily path uses the daily VRF word. Both are VRF-derived and unknown at commitment time. No vulnerability -- just an architectural detail that must be documented correctly.

Note: `processFutureTicketBatch` is NOT affected by mid-day entropy divergence because it only runs in the new-day path (Paths 4 and 5), where `rngWordCurrent` always holds the daily nudge-adjusted word.

---

## 5. Per-Ticket Entropy Derivation (_raritySymbolBatch)

Both processing functions delegate trait generation to `_raritySymbolBatch`, which is duplicated in both modules for delegatecall isolation. The implementations are identical.

### 5.1 _raritySymbolBatch Algorithm

**Locations:** JM:2127-2221 (JackpotModule) and MM:462-555 (MintModule)

**Input parameters:**
- `player`: address receiving trait tickets
- `baseKey`: encoded key `(uint256(lvl) << 224) | (queueIdx << 192) | (uint256(uint160(player)) << 32)`
  - JM: constructed in `_generateTicketBatch` at JM:2058-2060
  - MM: constructed inline at MM:352-354
- `startIndex`: starting position within player's owed tickets
- `count`: number of tickets to process this batch
- `entropyWord`: VRF-derived entropy (lastLootboxRngWord for JM, rngWordCurrent for MM)

**Algorithm (using JM line numbers, MM is identical +335 offset):**

1. **Group iteration (JM:2146-2175):** Processes tickets in groups of 16
   - `groupIdx = i >> 4` (JM:2147) -- group index per 16 symbols
   - Per-group seed: `seed = (baseKey + groupIdx) ^ entropyWord` (JM:2151)

2. **LCG initialization (JM:2153-2157):**
   - `s = uint64(seed) | 1` (JM:2153) -- ensure odd for full LCG period
   - Advance to start position: `s = s * (TICKET_LCG_MULT + uint64(offset)) + uint64(offset)` (JM:2156) where `offset = uint8(i & 15)` (JM:2154)

3. **LCG step (JM:2161):**
   - `s = s * TICKET_LCG_MULT + 1` -- standard LCG with increment 1

4. **Trait generation (JM:2164-2165):**
   - `traitId = DegenerusTraitUtils.traitFromWord(s) + (uint8(i & 3) << 6)`
   - `traitFromWord` produces a 6-bit base trait (0-63)
   - `(uint8(i & 3) << 6)` adds 2-bit quadrant offset: 0x00, 0x40, 0x80, or 0xC0

5. **Tracking (JM:2168-2169):**
   - `counts[traitId]++` -- increment occurrence count per trait
   - `touchedTraits[touchedLen++] = traitId` -- record first occurrence for batch write

6. **Assembly storage write (JM:2187-2221):**
   - `levelSlot = keccak256(lvl . traitBurnTicket.slot)` (JM:2188-2190) -- root slot for this level
   - For each touched trait:
     - `elem = levelSlot + traitId` (JM:2200) -- length slot for trait array
     - `len = sload(elem)` (JM:2201) -- current array length
     - `sstore(elem, len + occurrences)` (JM:2203) -- update length
     - `data = keccak256(elem)` (JM:2207) -- first data element slot
     - For `k = 0` to `occurrences - 1`: `sstore(data + len + k, player)` (JM:2214) -- push player address

### 5.2 LCG Constant Identity Verification

| Module | Location | Representation | Value |
|--------|----------|---------------|-------|
| JackpotModule | JM:170 | Hex | `0x5851F42D4C957F2D` |
| MintModule | MM:83 | Decimal | `6364136223846793005` |

**Verification:** `0x5851F42D4C957F2D` = `6364136223846793005` in decimal. This is the Knuth MMIX LCG multiplier (as noted in JM:169 comment: "Knuth's MMIX constant"). Both modules use the identical constant, ensuring consistent trait distribution.

### 5.3 traitFromWord Internals

**Location:** DegenerusTraitUtils.sol:143-150

```
function traitFromWord(uint64 rnd) internal pure returns (uint8) {
    uint8 category = weightedBucket(uint32(rnd));       // low 32 bits  -> category
    uint8 sub = weightedBucket(uint32(rnd >> 32));      // high 32 bits -> sub-bucket
    return (category << 3) | sub;                       // 6-bit trait ID
}
```

**weightedBucket (DegenerusTraitUtils.sol:113-128):**
- Scales `uint32` input to 0-74 range: `scaled = uint32((uint64(rnd) * 75) >> 32)` (DegenerusTraitUtils.sol:116)
- Maps to 8 buckets with weighted distribution (DegenerusTraitUtils.sol:119-127):
  - Buckets 0-3: width 10 (~13.3% each)
  - Buckets 4-6: width 9 (~12.0% each)
  - Bucket 7: width 8 (~10.7%)

**Final trait ID format: 8-bit `[QQ][CCC][SSS]`**
- Bits 7-6 (`QQ`): quadrant from `i & 3`, added by caller at JM:2165/MM:499
- Bits 5-3 (`CCC`): category from `weightedBucket(low 32 bits of LCG state)`
- Bits 2-0 (`SSS`): sub-bucket from `weightedBucket(high 32 bits of LCG state)`

Total trait space: 4 quadrants x 8 categories x 8 sub-buckets = 256 possible trait IDs (0x00-0xFF).

### 5.4 _rollRemainder for Fractional Tickets

**Locations:** JM:2101-2108 and MM:559-565

```
function _rollRemainder(uint256 entropy, uint256 rollSalt, uint8 rem) private pure returns (bool win) {
    uint256 rollEntropy = EntropyLib.entropyStep(entropy ^ rollSalt);
    return (rollEntropy % TICKET_SCALE) < rem;
}
```

Where `TICKET_SCALE = 100`.

**Derivation:**
1. XOR entropy with rollSalt (rollSalt is the baseKey for the player) (JM:2106/MM:564)
2. Apply `EntropyLib.entropyStep` -- XOR-shift PRNG step (EntropyLib.sol:16-22)
3. Take modulo 100 (JM:2107/MM:565)
4. Win if result < remainder (0-99 range, so rem=50 means 50% chance)

**EntropyLib.entropyStep (EntropyLib.sol:16-23):**
```
function entropyStep(uint256 state) internal pure returns (uint256) {
    unchecked {
        state ^= state << 7;   // EntropyLib.sol:18
        state ^= state >> 9;   // EntropyLib.sol:19
        state ^= state << 8;   // EntropyLib.sol:20
    }
    return state;              // EntropyLib.sol:22
}
```

XOR-shift PRNG with three operations (shift-left 7, shift-right 9, shift-left 8). Seeded from VRF-derived entropy, so ultimately secure despite the simple structure.

**_rollRemainder usage:**
- In JM: called from `_resolveZeroOwedRemainder` (JM:1958) and `_finalizeTicketEntry` (JM:2088) when owed reaches 0 but remainder exists
- In MM: called inline at MM:369 (zero-owed path) and MM:412 (after full batch processed)
- Both implementations are identical in behavior

---

## 6. Cursor Lifecycle (TPROC-04)

Three state variables form a shared cursor that tracks ticket processing progress across both `processTicketBatch` and `processFutureTicketBatch`. They are written by both processing functions and read by `advanceGame` to determine whether processing is complete.

### 6.1 Variable Declarations

| Variable | Location | Type | Slot Position | Purpose |
|----------|----------|------|---------------|---------|
| `ticketCursor` | GS:474 | `uint32` | Slot 17, offset 0 (bytes 0-3) | Index into `ticketQueue[key]` for current level |
| `ticketLevel` | GS:477 | `uint24` | Slot 17, offset 4 (bytes 4-6) | Which level is being processed; bit 22 = FF marker |
| `ticketsFullyProcessed` | GS:332 | `bool` | Slot 1, offset 25 (byte 25) | Read slot fully drained for current purchase level |

Note: `ticketCursor` and `ticketLevel` share EVM slot 17 (packed into the same 32-byte word). `ticketsFullyProcessed` is in slot 1 with other single-byte flags (see GS:56-67 slot map).

v3.8 inventory claims (Section 1.13 line 175-176): "ticketLevel at slot 17 offset 4, ticketCursor at slot 17 offset 0". This is consistent with the declaration order: `uint32 ticketCursor` (4 bytes at offset 0, GS:474) followed by `uint24 ticketLevel` (3 bytes at offset 4, GS:477). CONFIRMED.

### 6.2 State Machine

The cursor variables encode four logical states:

```
State            ticketLevel              ticketCursor    Meaning
-------          ---------------          ------------    -------
IDLE             0                        0               No processing in progress
PROCESSING       lvl (raw level)          idx             Advancing through read-side queue
FF_PROCESSING    lvl | TICKET_FAR_FUTURE_BIT   idx        Advancing through far-future queue
DONE             0                        0               Returns to IDLE after completion
```

**Transitions:**

```
IDLE -> PROCESSING:    processTicketBatch sets ticketLevel = lvl, ticketCursor = 0 (JM:1896-1897)
                       processFutureTicketBatch sets ticketLevel = lvl, ticketCursor = 0 (MM:321-322)

PROCESSING -> DONE:    processTicketBatch resets to 0/0 on completion (JM:1904-1905, JM:1945-1946)
                       processFutureTicketBatch resets to 0/0 (MM:315-316, MM:336-337)

PROCESSING -> FF_PROCESSING:
                       processFutureTicketBatch transitions when read-side exhausted and FF queue exists
                       (MM:310-311, MM:331-332, MM:441-442)

FF_PROCESSING -> DONE: processFutureTicketBatch resets to 0/0 on completion
                       (MM:445-446, MM:449-450)
```

**Critical property:** `processTicketBatch` (JM) never sets the FF bit -- it processes only read-side queues. Only `processFutureTicketBatch` (MM) manages FF transitions. However, both functions share the same `ticketLevel`/`ticketCursor` variables, so `_prepareFutureTickets` (AM:1159-1162) must strip the FF bit to detect an in-flight FF level and resume it correctly.

### 6.3 All Write Sites

#### ticketLevel writes (12 total)

**processTicketBatch (JackpotModule):**

| # | Line | Value Written | Context |
|---|------|---------------|---------|
| 1 | JM:1896 | `lvl` | Level switch: `ticketLevel = lvl` when `ticketLevel != lvl` (JM:1895) |
| 2 | JM:1905 | `0` | Early done: `ticketLevel = 0` when `idx >= total` before processing loop (JM:1901) |
| 3 | JM:1946 | `0` | Loop done: `ticketLevel = 0` when `idx >= total` after processing loop (JM:1942) |

**processFutureTicketBatch (MintModule):**

| # | Line | Value Written | Context |
|---|------|---------------|---------|
| 4 | MM:310 | `lvl \| TICKET_FAR_FUTURE_BIT` | Empty read-side queue, FF queue exists -> transition to FF phase |
| 5 | MM:316 | `0` | Empty read-side queue, no FF queue -> done |
| 6 | MM:321 | `lvl` | Level switch: `ticketLevel = lvl` when `!inFarFuture && ticketLevel != lvl` (MM:320) |
| 7 | MM:331 | `lvl \| TICKET_FAR_FUTURE_BIT` | Read-side exhausted mid-loop (`idx >= total`), FF queue exists -> transition |
| 8 | MM:337 | `0` | Read-side exhausted mid-loop, no FF queue -> done |
| 9 | MM:441 | `lvl \| TICKET_FAR_FUTURE_BIT` | Processing loop finished (`idx >= total`), not in FF, FF queue exists -> transition |
| 10 | MM:446 | `0` | Processing loop finished, not in FF, no FF queue -> done |
| 11 | MM:450 | `0` | Processing loop finished, in FF phase -> done |

**Not a storage write (local variable shadow):**
- MM:1034: `uint24 ticketLevel = targetLevel` -- this is a local variable in `_mintBatchFor`, NOT a write to the storage variable at GS:477. Confirmed by surrounding code: passed to `_queueTicketsScaled` at MM:1038 as a parameter.

#### ticketCursor writes (14 total)

**processTicketBatch (JackpotModule):**

| # | Line | Value Written | Context |
|---|------|---------------|---------|
| 1 | JM:1897 | `0` | Level switch reset: cursor starts at 0 for new level |
| 2 | JM:1904 | `0` | Early done reset: `ticketCursor = 0` before `ticketLevel = 0` |
| 3 | JM:1940 | `uint32(idx)` | Cursor save: saves current processing position after loop |
| 4 | JM:1945 | `0` | Loop done reset: `ticketCursor = 0` after completion |

**processFutureTicketBatch (MintModule):**

| # | Line | Value Written | Context |
|---|------|---------------|---------|
| 5 | MM:311 | `0` | FF transition from empty read-side: reset cursor for FF queue |
| 6 | MM:315 | `0` | Done reset: no queues remain |
| 7 | MM:322 | `0` | Level switch reset: start from index 0 for new level |
| 8 | MM:332 | `0` | FF transition from exhausted read-side: reset cursor |
| 9 | MM:336 | `0` | Done reset: read-side exhausted, no FF queue |
| 10 | MM:434 | `uint32(idx)` | Cursor save: saves position after inline loop |
| 11 | MM:442 | `0` | Finished processing, FF transition: reset cursor |
| 12 | MM:445 | `0` | Finished processing, done (no FF after read-side): reset |
| 13 | MM:449 | `0` | Finished processing, done (in FF phase): reset |

Note: MM:322 is inside a block guarded by `!inFarFuture && ticketLevel != lvl` (MM:320). This handles the case where `processFutureTicketBatch` is called for a level different from the current `ticketLevel`, but only when NOT already in FF processing.

#### ticketsFullyProcessed writes (4 total)

| # | Line | Value Written | Context |
|---|------|---------------|---------|
| 1 | AM:173 | `true` | Mid-day path: `_runProcessTicketBatch` returned `finished=true` (AM:172) |
| 2 | AM:218 | `true` | Daily drain gate: read slot empty or just finished processing |
| 3 | AM:276 | `true` | New-day post-RNG: "ADV-03: set before jackpot/phase logic" |
| 4 | GS:713 | `false` | `_swapTicketSlot`: reset on queue buffer swap |

**Indirect setter (via call chain):**
- `_swapAndFreeze` (GS:719) calls `_swapTicketSlot` (GS:720), which triggers the GS:713 `false` write.
- `requestLootboxRng` calls `_swapTicketSlot` (AM:720), same GS:713 `false` write.

v3.8 inventory Section 2.6 (line 940) claims: "advanceGame -> processTicketBatch -> ticketsFullyProcessed = true (when queue drained)". [DISCREPANCY] -- `ticketsFullyProcessed` is NOT set by `processTicketBatch` itself (JM:1889-1950). It is set by `advanceGame` code in AdvanceModule (AM:173, AM:218, AM:276) AFTER `_runProcessTicketBatch` returns. The v3.8 claim conflates the caller with the callee. Functionally equivalent but technically inaccurate.

### 6.4 All Read Sites

#### ticketLevel reads

| # | File:Line | Function | Purpose |
|---|-----------|----------|---------|
| 1 | JM:1895 | `processTicketBatch` | Level switch guard: `if (ticketLevel != lvl)` |
| 2 | MM:302 | `processFutureTicketBatch` | FF phase detection: `ticketLevel == (lvl \| TICKET_FAR_FUTURE_BIT)` |
| 3 | MM:320 | `processFutureTicketBatch` | Level switch guard: `!inFarFuture && ticketLevel != lvl` |
| 4 | AM:1159 | `_prepareFutureTickets` | Resume level read: `resumeLevel = ticketLevel` |
| 5 | AM:1202 | `_runProcessTicketBatch` | Pre-call snapshot: `prevLevel = ticketLevel` |
| 6 | AM:1214 | `_runProcessTicketBatch` | Work detection: `ticketLevel != prevLevel` |

#### ticketCursor reads

| # | File:Line | Function | Purpose |
|---|-----------|----------|---------|
| 1 | JM:1900 | `processTicketBatch` | Load cursor: `idx = ticketCursor` |
| 2 | MM:325 | `processFutureTicketBatch` | Load cursor: `idx = ticketCursor` |
| 3 | AM:1201 | `_runProcessTicketBatch` | Pre-call snapshot: `prevCursor = ticketCursor` |
| 4 | AM:1214 | `_runProcessTicketBatch` | Work detection: `ticketCursor != prevCursor` |

#### ticketsFullyProcessed reads

| # | File:Line | Function | Purpose |
|---|-----------|----------|---------|
| 1 | AM:156 | advanceGame mid-day path | Guard: `!ticketsFullyProcessed` before ticket processing |
| 2 | AM:205 | advanceGame daily drain | Guard: `!ticketsFullyProcessed` before drain processing |
| 3 | AM:719 | `requestLootboxRng` | Condition: `ticketsFullyProcessed` checked with write-queue presence for mid-day swap |

Note: AM:276 writes `true` unconditionally in the new-day post-RNG path. There is no read of `ticketsFullyProcessed` guarding this write -- it is a forced assertion ("ADV-03: set before jackpot/phase logic").

### 6.5 _prepareFutureTickets Resume Logic

**Location:** AM:1156-1184

The resume mechanism handles the case where `processFutureTicketBatch` was interrupted mid-processing (cursor saved at a non-zero position) and `_prepareFutureTickets` is called again on a subsequent `advanceGame` invocation.

**Resume detection (AM:1159-1162):**
```
uint24 resumeLevel = ticketLevel;                              // AM:1159
uint24 baseResume = resumeLevel & ~uint24(TICKET_FAR_FUTURE_BIT);  // AM:1162
```

The FF bit is stripped via `~TICKET_FAR_FUTURE_BIT` (bitwise NOT of bit 22 mask) to extract the raw level, regardless of whether we are mid-FF-processing or mid-read-side-processing.

**Resume-first behavior (AM:1165-1170):**
If `baseResume >= startLevel && baseResume <= endLevel` (the interrupted level falls within the `[lvl+2, lvl+6]` range), the function resumes that level FIRST by calling `_processFutureTicketBatch(baseResume)`. The callee reads `ticketCursor` to pick up where it left off. If `worked || !levelFinished`: return false (more batches needed).

**Iterate remaining (AM:1173-1183):**
After the resumed level completes (or if no resume was needed), the function iterates `startLevel..endLevel`, SKIPPING `baseResume` (the just-completed or in-flight level). For each level, calls `_processFutureTicketBatch(target)`. Returns false on first batch of work or unfinished level.

**Key correctness property:** The `baseResume` check at AM:1165 uses the stripped value for range comparison, but `_processFutureTicketBatch(baseResume)` receives the RAW base level -- NOT the FF-flagged value. Inside `processFutureTicketBatch` (MM:302), the FF detection re-checks `ticketLevel == (lvl | TICKET_FAR_FUTURE_BIT)` to determine whether to continue with the FF queue. This means the FF state is transparently preserved through the resume cycle.

---

## 7. traitBurnTicket Storage Layout (TPROC-05)

### 7.1 Declaration

```solidity
// GS:417
mapping(uint24 => address[][256]) internal traitBurnTicket;
```

**Structure:**
- Outer key: `uint24 level` -- the purchase level for which tickets were processed
- Inner index: `uint8 traitId` (0-255) -- the generated trait ID from `_raritySymbolBatch`
- Value: `address[]` -- dynamic array of player addresses who hold that trait ticket

Each entry in the array is a player address, and the same player can appear multiple times (more occurrences = higher win probability in jackpot selection).

**Storage slot:** The v3.8 inventory consistently claims `traitBurnTicket` is at slot 11 (mapping root). The GS header comments (GS:104) note: "The traitBurnTicket nested mapping uses keccak256(traitId . keccak256(level . slot)) for data location." The assembly code at JM:2189 and MM:523 references `traitBurnTicket.slot`, which is resolved by the Solidity compiler to the mapping's root slot.

Note: `forge inspect DegenerusGame storage-layout` was not executed (compilation time exceeds execution budget for this audit phase). The v3.8 slot 11 claim is consistent with the variable declaration order in GS: slot 0 (packed Slot 0 fields), slot 1 (packed Slot 1 fields), slot 2 (`currentPrizePool`), slot 3 (`prizePoolsPacked`), slot 4 (`rngWordCurrent`), slot 5 (`vrfRequestId`), slot 6 (`totalFlipReversals`), slot 7 (`dailyTicketBudgetsPacked`), slot 8 (`dailyEthPoolBudget`), slot 9 (`claimableWinnings` mapping), slot 10 (`claimablePool`), slot 11 (`traitBurnTicket` mapping). This ordering is CONFIRMED by sequential declaration from GS:300 through GS:417. Slot 11 is correct.

### 7.2 Assembly Write Pattern

Both `_raritySymbolBatch` implementations use identical inline assembly to write to `traitBurnTicket`. The JackpotModule version is at JM:2187-2221; the MintModule version is at MM:521-555.

**Step-by-step derivation (using JM line numbers; MM offsets +334):**

```
Step 1: Compute level slot (JM:2187-2191 / MM:521-525)
    mstore(0x00, lvl)                     // JM:2188 / MM:522
    mstore(0x20, traitBurnTicket.slot)    // JM:2189 / MM:523
    levelSlot = keccak256(0x00, 0x40)     // JM:2190 / MM:524
    // = keccak256(abi.encode(lvl, traitBurnTicket.slot))

Step 2: Per-trait loop (JM:2194-2221 / MM:528-555)
    For each touched trait:
        elem = levelSlot + traitId        // JM:2200 / MM:534 -- length slot for this trait's array
        len = sload(elem)                 // JM:2201 / MM:535 -- current array length
        newLen = len + occurrences         // JM:2202 / MM:536
        sstore(elem, newLen)              // JM:2203 / MM:537 -- update array length

Step 3: Compute data slot and write entries (JM:2206-2216 / MM:540-550)
        mstore(0x00, elem)
        data = keccak256(0x00, 0x20)      // JM:2207 / MM:541 -- first data element slot
        dst = data + len                  // JM:2208 / MM:542 -- write position (after existing entries)
        for k in [0, occurrences):
            sstore(dst, player)           // JM:2214 / MM:548 -- push player address
            dst += 1                      // JM:2215 / MM:549
```

**Solidity storage layout verification:**

For `mapping(uint24 => address[][256])`:
- The mapping root is at slot `S` (= 11 per v3.8 and declaration order).
- `traitBurnTicket[level]` resolves to: `keccak256(abi.encode(level, S))` -- this is `levelSlot` above.
- `traitBurnTicket[level][traitId]` for a fixed-size array `address[][256]`: the element at index `traitId` occupies slot `levelSlot + traitId`. This stores the dynamic array's length.
- Data elements of `traitBurnTicket[level][traitId]` start at `keccak256(abi.encode(levelSlot + traitId))` -- this is `dataSlot` above.

The assembly pattern matches the standard Solidity storage layout for `mapping(K => T[N])` where `T` is a dynamic array. CONFIRMED correct.

### 7.3 All Write Paths (Complete Enumeration)

| # | Location | Function | Module | Context |
|---|----------|----------|--------|---------|
| 1 | JM:2187-2221 | `_raritySymbolBatch` | JackpotModule | Called by `_generateTicketBatch` (JM:2061), called by `_processOneTicketEntry` (JM:2030), called by `processTicketBatch` loop (JM:1919) |
| 2 | MM:521-555 | `_raritySymbolBatch` | MintModule | Called inline by `processFutureTicketBatch` loop (MM:392) |

**Completeness verification:** Grep for all `traitBurnTicket` references across `contracts/` confirms exactly two `sstore` patterns (JM:2198-2216 and MM:532-550) and two `.slot` references (JM:2189 and MM:523). All other references are reads (`sload`, `.length`, or storage reference bindings). No other function writes to `traitBurnTicket` via assembly or Solidity syntax.

v3.8 inventory (line 764, 766) claims: "Only written by processTicketBatch which reads from the READ slot." and "No permissionless external function directly writes traitBurnTicket outside of processTicketBatch." [DISCREPANCY] -- `processFutureTicketBatch` (added v3.9 Phase 76) also writes to `traitBurnTicket` via `_raritySymbolBatch` in MintModule (MM:521-555). The v3.8 claim is stale. The corrected statement should be: "No permissionless external function directly writes traitBurnTicket outside of processTicketBatch and processFutureTicketBatch."

### 7.4 All Read Paths (Complete Enumeration)

#### Winner Selection Functions (JackpotModule)

| # | Location | Function | Purpose |
|---|----------|----------|---------|
| 1 | JM:785 | `_runDgnrsRewardJackpot` | Passes `traitBurnTicket[lvl]` to `_randTraitTicket` for DGNRS reward winner selection |
| 2 | JM:833 | `_runEarlyBirdLootboxJackpot` | Passes `traitBurnTicket[lvl]` to `_randTraitTicket` for early-bird lootbox winners (per iteration) |
| 3 | JM:1040 | `_hasTraitTickets` | Length check: `traitBurnTicket[lvl][trait].length != 0` for winning trait existence |
| 4 | JM:1191 | `_distributeBucketTickets` | Passes `traitBurnTicket[lvl]` to `_randTraitTicket` for daily ticket jackpot bucket winners |
| 5 | JM:1231 | `_computeBucketCounts` | Length check: `traitBurnTicket[lvl][trait].length != 0` for active bucket detection |
| 6 | JM:1448 | `_runCoinJackpotPhase` (decimator area) | Passes `traitBurnTicket[lvl]` to `_randTraitTicketWithIndices` for coin jackpot winners |
| 7 | JM:1641 | `_runCoinJackpotDistribute` | Passes `traitBurnTicket[lvl]` to `_randTraitTicketWithIndices` for coin jackpot distribution |
| 8 | JM:2244 | `_randTraitTicket` | Reads `traitBurnTicket_[trait]` holders array and length for index-based winner selection |
| 9 | JM:2294 | `_randTraitTicketWithIndices` | Same as `_randTraitTicket` plus returns ticket indices: `traitBurnTicket_[trait]` |
| 10 | JM:2459 | `_awardFarFutureCoinJackpot` | Passes `traitBurnTicket[lvl]` to `_randTraitTicketWithIndices` for far-future coin jackpot |
| 11 | JM:2680 | `_hasActualTraitTickets` | Length check: `traitBurnTicket[lvl][traitIds[i]].length != 0` for actual (non-virtual) ticket existence |

#### View Functions (DegenerusGame)

| # | Location | Function | Purpose |
|---|----------|----------|---------|
| 12 | DG:2618 | `sampleTraitTickets` | View: sample up to 4 trait burn tickets using VRF-derived level and trait selection |
| 13 | DG:2647 | `sampleTraitTicketsAtLevel` | View: sample up to 4 trait burn tickets at a specific level |
| 14 | DG:2730 | `getTickets` | View: paginated query of tickets by trait, level, and player |

**Total read paths: 14** (11 in JackpotModule for winner selection/existence checks, 3 in DegenerusGame for view functions).

### 7.5 Storage Slot Verification

**v3.8 claim:** `traitBurnTicket` at slot 11.

**Verification method:** Declaration order counting in DegenerusGameStorage.sol:

| Slot | Variable | Declaration Line |
|------|----------|-----------------|
| 0 | Packed slot 0 (dailyIdx through dailyEthBucketCursor) | GS:223-310 |
| 1 | Packed slot 1 (dailyEthPhase through prizePoolFrozen) | GS:312-339 |
| 2 | currentPrizePool | GS:348 |
| 3 | prizePoolsPacked | GS:356 |
| 4 | rngWordCurrent | GS:362 |
| 5 | vrfRequestId | GS:368 |
| 6 | totalFlipReversals | GS:372 |
| 7 | dailyTicketBudgetsPacked | GS:379 |
| 8 | dailyEthPoolBudget | GS:383 |
| 9 | claimableWinnings (mapping) | GS:394 |
| 10 | claimablePool | GS:403 |
| 11 | traitBurnTicket (mapping) | GS:417 |

Slot 11 is CONFIRMED by sequential declaration order. The GS header comment at GS:104 also references traitBurnTicket as a nested mapping, consistent with slot 11 being its root.

v3.8 claim for `lastLootboxRngWord` at slot 70 cannot be verified by simple declaration counting (too many intervening variables and mappings which each consume a slot). Flagged for manual verification or `forge inspect` in a future phase.

---

## 8. Cross-Reference with Prior Audit Claims (TPROC-06)

Systematic verification of v3.8 commitment window inventory claims (Section 1.13 and Section 2) against current code. Each claim is assigned a verdict: CONFIRMED, CONFIRMED with drift, [DISCREPANCY], or [NEW FINDING].

### 8.1 v3.8 Section 1.13 Claims

| # | v3.8 Claim | v3.8 Location | Current Code | Verdict |
|---|-----------|---------------|-------------|---------|
| 1 | processTicketBatch at JackpotModule:1890-1951 | Section 1.13 heading (line 171) | JM:1889-1950 (1-line shift) | CONFIRMED with drift |
| 2 | ticketLevel at slot 17, offset 4 | Section 1.13 line 175 | GS:477, declared after `uint32 ticketCursor` (GS:474); slot 17 offset 4 by declaration order counting | CONFIRMED |
| 3 | ticketCursor at slot 17, offset 0 | Section 1.13 line 176 | GS:474, first in slot 17 packing; `uint32` at offset 0 | CONFIRMED |
| 4 | ticketQueue at slot 15 (mapping) | Section 1.13 line 177 | GS:463, `mapping(uint24 => address[])` -- slot 15 by counting (11 traitBurnTicket + 12 mintPacked_ + 13 rngWordByDay + 14 prizePoolPendingPacked + 15 ticketQueue) | CONFIRMED |
| 5 | lastLootboxRngWord at slot 70 | Section 1.13 line 178 | GS:1240, many intervening variables; slot 70 cannot be verified without `forge inspect` | UNVERIFIED (deferred to Phase 88) |
| 6 | ticketsOwedPacked at slot 16 (mapping) | Section 1.13 line 179 | GS:467, `mapping(uint24 => mapping(address => uint40))` -- slot 16 by counting | CONFIRMED |
| 7 | traitBurnTicket at slot 11 (mapping) | Section 1.13 line 180 | GS:417, slot 11 verified by declaration order (see Section 7.5) | CONFIRMED |

### 8.2 v3.8 Section 2.6 (Permissioned Writer Tables)

| # | v3.8 Claim | v3.8 Location | Current Code | Verdict |
|---|-----------|---------------|-------------|---------|
| 8 | ticketsFullyProcessed at slot 1, offset 24 | Section heading (line 936) | GS slot layout header (GS:63): `[25:26] ticketsFullyProcessed` -- byte offset 25, not 24. v3.8 may count from 0 differently, but the GS header clearly shows ticketWriteSlot at `[24:25]` and ticketsFullyProcessed at `[25:26]` | [DISCREPANCY] -- offset is 25, not 24 |
| 9 | "advanceGame -> processTicketBatch -> ticketsFullyProcessed = true (when queue drained)" | Section 2.6 line 940 | `ticketsFullyProcessed` is set to `true` by advanceGame code in AdvanceModule (AM:173, AM:218, AM:276), NOT by processTicketBatch (JM:1889-1950). processTicketBatch returns `bool finished` to the caller; the caller sets the flag. | [DISCREPANCY] -- setter is advanceGame (AM), not processTicketBatch (JM) |
| 10 | "No permissionless external function directly writes traitBurnTicket outside of processTicketBatch" | Section 2.6 line 766 | processFutureTicketBatch (MM:298) also writes traitBurnTicket via _raritySymbolBatch (MM:521-555). Added in v3.9 Phase 76. | [DISCREPANCY] -- must include processFutureTicketBatch |
| 11 | traitBurnTicket writer table lists only processTicketBatch (JackpotModule) | Section 2.6 line 764 | Two writers: processTicketBatch (JM:2187-2221) and processFutureTicketBatch (MM:521-555) | [DISCREPANCY] -- MintModule writer missing |

### 8.3 v3.8 Section 4 (Mutation Surface Summary)

| # | v3.8 Claim | v3.8 Location | Current Code | Verdict |
|---|-----------|---------------|-------------|---------|
| 12 | traitBurnTicket mutated only by "advanceGame() -> processTicketBatch [game-internal only]" | Section 4 line 1183 | Also mutated by advanceGame -> processFutureTicketBatch -> _raritySymbolBatch (MM:521-555) | [DISCREPANCY] -- missing processFutureTicketBatch path |
| 13 | traitBurnTicket verdict SAFE with reasoning "no permissionless external function outside advanceGame writes traitBurnTicket" | Section 4.4 line 1434-1441 | Reasoning and verdict are still correct -- processFutureTicketBatch is also game-internal (called via advanceGame). But the documentation is incomplete. | CONFIRMED (verdict correct, documentation stale) |

### 8.4 v3.8 Coverage of processFutureTicketBatch

**Grep result:** `processFutureTicketBatch` appears ZERO times in `audit/v3.8-commitment-window-inventory.md`.

This is expected: `processFutureTicketBatch` was added in v3.9 Phase 76 as part of the far-future ticket fix. The v3.8 commitment window inventory was written before that change and was never updated to cover it. [DISCREPANCY] -- the v3.8 inventory is incomplete for ticket processing; it documents only `processTicketBatch` (single function) when two functions now exist.

**Impact assessment:** The v3.8 SAFE verdict for traitBurnTicket remains correct in conclusion (processFutureTicketBatch is also game-internal via advanceGame), but the reasoning text and writer tables are stale. Any future auditor relying solely on v3.8 Section 2.6 would miss the MintModule write path.

### 8.5 v3.8 ticketsFullyProcessed Writer Analysis

v3.8 Section 2.6 (line 940-942) lists three writers of `ticketsFullyProcessed`:
1. "advanceGame -> processTicketBatch -> ticketsFullyProcessed = true" (line 940)
2. "advanceGame -> _swapAndFreeze -> _swapTicketSlot -> ticketsFullyProcessed = false" (line 941)
3. "requestLootboxRng -> _swapTicketSlot -> ticketsFullyProcessed = false" (line 942)

**Current code has four `ticketsFullyProcessed = true` setters:**
- AM:173 (mid-day path)
- AM:218 (daily drain gate)
- AM:276 (new-day post-RNG)

The v3.8 table implies a single `true` setter via processTicketBatch. [DISCREPANCY] -- there are three distinct `true` write sites in AdvanceModule, not one in JackpotModule. The `false` setter via _swapTicketSlot (GS:713) is correct.

### 8.6 Phase 81 Findings Cross-Reference

Phase 81 identified three findings relevant to ticket processing:

| ID | Relevance to Phase 82 | Status |
|----|----------------------|--------|
| DSC-01 | v3.9 RNG proof stale (combined pool -> FF-only revert) | Not affected by Phase 82 -- this concerns _awardFarFutureCoinJackpot, not ticket processing |
| DSC-02 | sampleFarFutureTickets uses _tqWriteKey | View function only -- not related to traitBurnTicket writes |
| DSC-03 | NatSpec cap claim vs unchecked add | ticketsOwedPacked overflow claim -- not affected by processing logic |

No Phase 81 findings require revision based on Phase 82 analysis.

---

## 9. Phase 82 Summary and Finding Catalog

### 9.1 Audit Statistics

| Metric | Value |
|--------|-------|
| Total sections in this document | 9 |
| Total file:line citations (Sections 1-9) | 300+ |
| Functions fully traced | 4 (processTicketBatch, processFutureTicketBatch, _raritySymbolBatch x2) |
| Helper functions documented | 7 (_processOneTicketEntry, _generateTicketBatch, _finalizeTicketEntry, _rollRemainder x2, _prepareFutureTickets, _runProcessTicketBatch) |
| Cursor write sites enumerated | 30 (12 ticketLevel + 14 ticketCursor + 4 ticketsFullyProcessed) |
| Cursor read sites enumerated | 13 (6 ticketLevel + 4 ticketCursor + 3 ticketsFullyProcessed) |
| traitBurnTicket write paths | 2 (JM:2187-2221, MM:521-555) |
| traitBurnTicket read paths | 14 (11 JackpotModule + 3 DegenerusGame) |
| v3.8 claims cross-referenced | 13 |
| advanceGame trigger paths | 5 |

### 9.2 Finding Catalog

| Finding ID | Type | Severity | Description | Location |
|-----------|------|----------|-------------|----------|
| P82-01 | [DISCREPANCY] | INFO | v3.8 ticketsFullyProcessed offset claim (24) does not match GS header (25) | v3.8 line 936 vs GS:63 |
| P82-02 | [DISCREPANCY] | INFO | v3.8 claims processTicketBatch sets ticketsFullyProcessed; actually set by advanceGame in AdvanceModule (AM:173, AM:218, AM:276) | v3.8 line 940 vs AM:173/218/276 |
| P82-03 | [DISCREPANCY] | INFO | v3.8 traitBurnTicket writer table incomplete -- missing processFutureTicketBatch (MM:521-555) added in v3.9 Phase 76 | v3.8 lines 764, 766, 1183, 1434 vs MM:521-555 |
| P82-04 | [DISCREPANCY] | INFO | v3.8 ticketsFullyProcessed writer table lists one `true` setter (via processTicketBatch); actually three distinct `true` setters in AdvanceModule | v3.8 line 940 vs AM:173, AM:218, AM:276 |
| P82-05 | CONFIRMED with drift | INFO | processTicketBatch line range drifted by 1 line (v3.8: 1890-1951, current: 1889-1950) | v3.8 line 171 vs JM:1889-1950 |
| P82-06 | UNVERIFIED | INFO | lastLootboxRngWord slot 70 claim cannot be confirmed without forge inspect | v3.8 line 178 vs GS:1240 |

**All findings are INFO severity.** No HIGH, MEDIUM, or LOW vulnerabilities discovered. All discrepancies are documentation staleness or minor inaccuracies in the v3.8 audit text. The security conclusions from v3.8 (traitBurnTicket SAFE, ticket processing game-internal only) remain valid in substance.

### 9.3 Confirmed Claims Summary

The following v3.8 claims are CONFIRMED against current code:

| Claim | Status |
|-------|--------|
| ticketLevel at slot 17, offset 4 | CONFIRMED |
| ticketCursor at slot 17, offset 0 | CONFIRMED |
| ticketQueue at slot 15 | CONFIRMED |
| ticketsOwedPacked at slot 16 | CONFIRMED |
| traitBurnTicket at slot 11 | CONFIRMED |
| traitBurnTicket SAFE verdict | CONFIRMED (reasoning stale but conclusion valid) |
| processTicketBatch line range | CONFIRMED with drift (1-line shift) |

### 9.4 Recommendations

1. **Update v3.8 inventory:** Section 1.13 should be expanded to include processFutureTicketBatch as a second ticket processing function. Section 2.6 traitBurnTicket writer table needs a MintModule row. Defer to a future documentation cleanup phase.

2. **ticketsFullyProcessed offset:** Correct the v3.8 Section 2.6 heading from "offset 24" to "offset 25" to match the GS storage layout header. Low priority (no security impact).

3. **Phase 88 verification:** lastLootboxRngWord slot 70 should be verified via `forge inspect` during the RNG-dependent variable re-verification phase.

4. **v3.9 documentation gap:** processFutureTicketBatch was added in v3.9 Phase 76 but no v3.9 audit document covers its commitment window behavior. The function is game-internal (called only via advanceGame delegatecall chain), so the SAFE verdict holds, but formal documentation is missing.
