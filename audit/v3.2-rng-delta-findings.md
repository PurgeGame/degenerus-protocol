# v3.2 RNG Delta Security Findings

**Audit date:** 2026-03-19
**Scope:** rngLocked removal from coinflip claim paths, BAF guard sufficiency, decimator claim persistence, cross-contract RNG data flow, redemption roll RNG consumer added in v3.3
**Methodology:** Manual code trace, formal invariant derivation, adversarial attack modeling
**Contracts audited:** BurnieCoinflip.sol, DegenerusGameAdvanceModule.sol, DegenerusGameDecimatorModule.sol, DegenerusGame.sol, BurnieCoin.sol, DegenerusGameWhaleModule.sol, DegenerusGameStorage.sol, DegenerusJackpots.sol, StakedDegenerusStonk.sol

---

## Executive Summary

### Overall Verdict: SAFE

The complete set of RNG-adjacent changes (rngLocked removal from coinflip claim paths, BAF guard retention, decimator claim persistence migration, and interface deletions) introduces no exploitable attack vectors. All four requirements pass with explicit SAFE verdicts.

### Per-Requirement Verdicts

| Requirement | Verdict | One-Line Reasoning |
|-------------|---------|-------------------|
| RNG-01 | **SAFE** | Carry isolation holds by construction via `rebuyActive` branching (lines 416-421, 498-508), not the removed rngLocked guards |
| RNG-02 | **SAFE** | BAF epoch-based guard covers the exact resolution window; sDGNRS BAF-ineligible at both caller and callee layers |
| RNG-03 | **SAFE** | Per-level `decClaimRounds` mapping with `e.claimed` flag prevents double-claims; ETH pools are independent per round |
| RNG-04 | **SAFE** | All 18 rngLocked consumers audited; remaining guards protect configuration changes (not claims); no emergent combined-change vectors |

### Findings Summary

| Severity | Count | Details |
|----------|-------|---------|
| HIGH | 0 | -- |
| MEDIUM | 0 | -- |
| LOW | 3 | Stale `@custom:reverts RngLocked` NatSpec on 3 IBurnieCoinflip functions (LOW-01, LOW-02, LOW-03) |
| INFO | 1 | `balanceOfWithClaimable` UX inconsistency during RNG lock (INFO-01) |

### Key Invariant

> **Carry ETH is never reachable from any claim path while auto-rebuy is enabled.**
>
> The `rebuyActive` branch at BurnieCoinflip.sol lines 416-421 and 498-508 is the isolation mechanism. The remaining `rngLocked` guard on `_setCoinflipAutoRebuy()` (line 691) prevents the only carry-extraction path (toggling auto-rebuy off during RNG lock).

---

## RNG-01: Carry Isolation -- rngLocked Removal Safety

### Change Summary

Four `rngLocked` checks were removed from BurnieCoinflip.sol coinflip claim paths:

| # | Function | Change | Previous Location |
|---|----------|--------|-------------------|
| 1 | `claimCoinflips()` (line 328) | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` guard before `_claimCoinflipsAmount` call | Was at function entry, before line 332 |
| 2 | `claimCoinflipsFromBurnie()` (line 339) | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` guard before `_claimCoinflipsAmount` call | Was at function entry, before line 343 |
| 3 | `consumeCoinflipsForBurn()` (line 349) | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` guard before `_claimCoinflipsAmount` call | Was at function entry, before line 353 |
| 4 | `claimCoinflipsTakeProfit()` | **Deleted entirely** -- function and interface entry removed. Callers now use `claimCoinflips()` with an amount parameter. | Was a standalone function in BurnieCoinflip.sol and IBurnieCoinflip.sol |

**What remains:** The `rngLocked` guard is still enforced in:
- `_setCoinflipAutoRebuy()` at line 691 -- blocks toggling auto-rebuy during RNG lock
- `_setCoinflipAutoRebuyTakeProfit()` at line 741 -- blocks changing take-profit during RNG lock
- BAF guard within `_claimCoinflipsInternal()` at lines 555-562 -- targeted BAF resolution protection (analyzed in RNG-02)

### Carry Isolation Trace

The safety argument for removing `rngLocked` from claim paths rests on **carry isolation**: the `autoRebuyCarry` field (rolling bankroll for future flips) and `claimableStored` field (take-profit amounts available for withdrawal) are separate storage fields with separate write paths. Claims only expose `claimableStored + mintable`, and `mintable` never contains carry while auto-rebuy is enabled.

#### Storage Layout (PlayerCoinflipState struct)

```solidity
struct PlayerCoinflipState {
    uint128 claimableStored;     // Take-profit: withdrawable at any time
    uint48 lastClaim;
    uint48 autoRebuyStartDay;
    bool autoRebuyEnabled;
    uint128 autoRebuyStop;
    uint128 autoRebuyCarry;      // Rolling bankroll: bets on future flips
}
```

#### Write Paths: `autoRebuyCarry`

| # | Location | Function | Value Written | Conditions | Can Carry Cross to Claimable? |
|---|----------|----------|---------------|------------|-------------------------------|
| 1 | Line 578 | `_claimCoinflipsInternal()` | `uint128(carry)` | `rebuyActive == true` AND `oldCarry != carry` (carry changed during day processing) | **NO** -- carry is written back to its own storage field. The local `carry` variable holds payout minus take-profit portions. `mintable` only receives `reserved` amounts (line 503). |
| 2 | Line 420 | `_claimCoinflipsInternal()` | `0` (zeroed) | `rebuyActive == false` AND `oldCarry != 0` (leftover carry from previously-enabled auto-rebuy) | **YES, but safely** -- carry is added to `mintable` at line 419, but this path only executes when `rebuyActive == false`, meaning auto-rebuy is already disabled. Disabling auto-rebuy requires `rngLocked == false` (line 691). |
| 3 | Line 722 | `_setCoinflipAutoRebuy()` | `0` (zeroed) | Called when disabling auto-rebuy (`enabled == false`). Carry is added to `mintable` at line 721. | **YES, but guarded** -- this function has its own `rngLocked` guard at line 691. Carry can only enter `mintable` via this path when RNG is unlocked. |

**Key observation:** Path 1 is the hot path during normal auto-rebuy operation. Carry stays in `autoRebuyCarry` storage. Paths 2 and 3 are the only paths where carry enters `mintable`, and both require auto-rebuy to be disabled (which itself requires `rngLocked == false`).

#### Write Paths: `claimableStored`

| # | Location | Function | Value Written | Conditions | Source of Value |
|---|----------|----------|---------------|------------|-----------------|
| 1 | Line 219 | `settleFlipModeChange()` | `state.claimableStored + mintable` (addition) | Called by game contract before afKing mode changes. `mintable` comes from `_claimCoinflipsInternal(player, false)`. | `mintable` = take-profit reserved amounts (when `rebuyActive`) or full payout (when `!rebuyActive`). Never carry while rebuy is active. |
| 2 | Line 260 | `_depositCoinflip()` | `state.claimableStored + mintable` (addition) | Called during new deposits. `mintable` comes from `_claimCoinflipsInternal(caller, false)`. | Same as path 1. `mintable` from `_claimCoinflipsInternal` never contains carry while rebuy is active. |
| 3 | Line 374 | `_claimCoinflipsAmount()` | `uint128(stored - toClaim)` (deduction) | Called from `claimCoinflips`, `claimCoinflipsFromBurnie`, `consumeCoinflipsForBurn`. `stored = state.claimableStored + mintable` (line 366). | Deduction from existing stored balance. `mintable` from `_claimCoinflipsInternal` never contains carry while rebuy is active. |

**Key observation:** All three `claimableStored` write paths source their `mintable` value from `_claimCoinflipsInternal()`. The carry isolation invariant therefore depends entirely on proving that `_claimCoinflipsInternal()` never puts carry into `mintable` while auto-rebuy is enabled.

### Formal Invariant

```
INVARIANT: While state.autoRebuyEnabled == true, no execution path from
claimCoinflips(), claimCoinflipsFromBurnie(), or consumeCoinflipsForBurn()
can cause state.autoRebuyCarry to be added to mintable, claimableStored,
or any value returned to msg.sender.
```

**Proof from code:**

1. All three claim functions call `_claimCoinflipsAmount()` (lines 332, 343, 353).

2. `_claimCoinflipsAmount()` calls `_claimCoinflipsInternal(player, false)` at line 365. The second argument `deepAutoRebuy` is always `false` for claim paths. This does not affect carry isolation (it only controls the claim window size).

3. Inside `_claimCoinflipsInternal()`, the local variable `rebuyActive` is set from `state.autoRebuyEnabled` at line 396:
   ```solidity
   bool rebuyActive = state.autoRebuyEnabled;
   ```

4. **When `rebuyActive == true`:** The carry-to-mintable transfer path (lines 418-421) is gated by `!rebuyActive`:
   ```solidity
   if (rebuyActive) {
       carry = oldCarry;            // line 417: carry stays local
   } else if (oldCarry != 0) {
       mintable += oldCarry;        // line 419: carry -> mintable (ONLY when !rebuyActive)
       state.autoRebuyCarry = 0;    // line 420: zero storage
   }
   ```
   Since `rebuyActive == true`, the `else if` branch is unreachable. Carry stays in the local `carry` variable.

5. **During day processing (win path, lines 498-508):** When `rebuyActive == true`:
   ```solidity
   if (rebuyActive) {
       if (takeProfit != 0) {
           uint256 reserved = (payout / takeProfit) * takeProfit;
           mintable += reserved;     // line 503: ONLY take-profit goes to mintable
           carry = payout - reserved; // line 505: remainder stays in carry
       } else {
           carry = payout;           // line 507: ALL goes to carry
       }
   }
   ```
   Only the take-profit `reserved` portion enters `mintable`. The remainder stays in the local `carry` variable.

6. **After the loop (line 577-578):** Carry is written back to storage, not to mintable:
   ```solidity
   if (rebuyActive && oldCarry != carry) {
       state.autoRebuyCarry = uint128(carry);  // line 578
   }
   ```

7. **Return value:** `mintable` is returned at line 585. It contains only take-profit reserved amounts (never carry) when `rebuyActive == true`.

8. **Back in `_claimCoinflipsAmount()`** (line 366):
   ```solidity
   uint256 stored = state.claimableStored + mintable;
   ```
   Since `mintable` never contains carry (per step 7), `stored` never contains carry. The value returned to the player (minted at line 379) comes from `toClaim <= stored`, which is carry-free.

**QED:** The carry isolation invariant holds by construction. The `rngLocked` guard on claim paths was defense-in-depth but never the mechanism preventing carry leakage. The mechanism is the `rebuyActive` branch at lines 416-421 and 498-508.

### Attacker Model Evaluation

#### Attacker Model 1: MEV-Aware Attacker (1000 ETH budget)

**Scenario:** Attacker observes the VRF random word in the mempool (via `rawFulfillRandomWords` calldata) before it is processed by `advanceGame`. They know the coinflip outcome (win or loss) for the current day before it is recorded.

**Attack vector:** Can the attacker front-run `advanceGame` with a claim transaction to extract carry before a known loss?

**Analysis:**
- The attacker calls `claimCoinflips()` which calls `_claimCoinflipsAmount()` -> `_claimCoinflipsInternal()`.
- `_claimCoinflipsInternal()` processes days up to `flipsClaimableDay` (line 393). The current day's result is NOT yet recorded (that happens in `processCoinflipPayouts` during `advanceGame`).
- Therefore, the claim only processes already-resolved days. The pending day (which the attacker knows will be a loss) is not claimable yet.
- Carry isolation holds regardless: even if claims processed the pending day, `mintable` would not contain carry (per the invariant proof above).

**Verdict:** No advantage. The attacker gains nothing from claiming during RNG lock because (a) the pending day is not yet claimable, and (b) carry isolation holds independently of RNG state.

#### Attacker Model 2: Compromised VRF Operator (1000 ETH budget)

**Scenario:** A compromised Chainlink VRF operator knows the random word before delivering it. They can choose when to deliver the word and can potentially delay delivery.

**Attack vector:** Can the operator manipulate carry by choosing favorable delivery timing combined with claim transactions?

**Analysis:**
- The operator knows the word but cannot change it (VRF proof verification prevents this).
- The operator can delay delivery, but this only extends the RNG lock window. It does not change which days are claimable (still limited to `flipsClaimableDay`).
- Even with advance knowledge, the operator cannot cause carry to enter `mintable` while auto-rebuy is enabled. The isolation is structural (code branching), not temporal (timing-dependent).
- The operator could theoretically toggle auto-rebuy off to extract carry, but `_setCoinflipAutoRebuy()` has its own `rngLocked` guard at line 691, which prevents toggling during RNG lock regardless of VRF operator behavior.

**Verdict:** No advantage. Carry isolation is a structural property of the code, not a timing property. The compromised VRF operator cannot bypass it.

### RNG-01 Verdict

**SAFE** -- Carry isolation holds by construction: the `rebuyActive` branch (lines 416-421, 498-508) prevents carry from entering `mintable` while auto-rebuy is enabled, regardless of RNG lock state. The removed `rngLocked` guards were defense-in-depth, not the mechanism preventing carry leakage. The remaining `rngLocked` guard on `_setCoinflipAutoRebuy()` (line 691) prevents the only carry-extraction path (toggling auto-rebuy off during RNG lock).

### Open Questions / INFO Findings

#### INFO-01: `balanceOfWithClaimable` UX Inconsistency

**Location:** BurnieCoin.sol, line 273

**Finding:** `balanceOfWithClaimable()` still excludes coinflip claimable amounts when `rngLocked()` returns true:

```solidity
function balanceOfWithClaimable(address player) external view returns (uint256 spendable) {
    spendable = balanceOf[player];
    if (player == ContractAddresses.VAULT) {
        spendable += uint256(_supply.vaultAllowance);
    }
    if (degenerusGame.rngLocked()) return spendable;  // <-- line 273: excludes claimable
    unchecked {
        spendable += IBurnieCoinflip(coinflipContract).previewClaimCoinflips(player);
    }
}
```

However, since `rngLocked` guards were removed from the claim functions (`claimCoinflips`, `claimCoinflipsFromBurnie`, `consumeCoinflipsForBurn`), claims now succeed during RNG lock. This creates a UX inconsistency: the view function reports less spendable balance than is actually available.

**Severity:** INFO -- The view function is conservative (reports less than available), which is the safe direction. No funds are at risk. This is a UX issue only -- frontends relying on `balanceOfWithClaimable` will underreport available balance during RNG lock.

**Note:** This may also be caught by Phase 41 comment re-scan (the `@dev` comment on line 264 says "claimable coinflips cannot be pulled" during RNG lock, which is now incorrect).

---

## RNG-02: BAF Epoch-Based Guard Sufficiency

### BAF Guard Location and Conditions

The BAF guard is located in `_claimCoinflipsInternal()` at BurnieCoinflip.sol lines 542-563. It is the sole remaining RNG-sensitive protection on coinflip claim paths after the four `rngLocked` removals.

```solidity
// BurnieCoinflip.sol lines 542-563
if (winningBafCredit != 0 && player != ContractAddresses.SDGNRS) {  // line 542
    if (!levelCached) {                                              // line 543
        cachedLevel = game.level();                                  // line 544
        levelCached = true;                                          // line 545
    }                                                                // line 546
    (                                                                // line 547
        uint24 purchaseLevel_,                                       // line 548
        bool inJackpotPhase,                                         // line 549
        bool lastPurchaseDay_,                                       // line 550
        bool rngLocked_,                                             // line 551
                                                                     // line 552
    ) = game.purchaseInfo();                                         // line 553
    bool over = game.gameOver();                                     // line 554
    if (                                                             // line 555
        !inJackpotPhase &&                                           // line 556
        !over &&                                                     // line 557
        lastPurchaseDay_ &&                                          // line 558
        rngLocked_ &&                                                // line 559
        (purchaseLevel_ % 10 == 0)                                   // line 560
    ) {                                                              // line 561
        revert RngLocked();                                          // line 562
    }                                                                // line 563
    // ... recordBafFlip follows at line 569 ...
}
```

**All 7 conditions that must be true for the guard to revert:**

| # | Condition | Code | Meaning |
|---|-----------|------|---------|
| 1 | `winningBafCredit != 0` | line 542 | Player has winning BAF flips to record for this claim batch |
| 2 | `player != ContractAddresses.SDGNRS` | line 542 | Player is NOT the sDGNRS staking contract |
| 3 | `!inJackpotPhase` | line 556 | Game is in purchase phase (not jackpot phase) |
| 4 | `!over` | line 557 | Game is not over |
| 5 | `lastPurchaseDay_` | line 558 | Purchase target met, level is transitioning |
| 6 | `rngLocked_` | line 559 | VRF request is currently in flight |
| 7 | `purchaseLevel_ % 10 == 0` | line 560 | Current purchase level is a BAF resolution level (multiple of 10) |

If ANY condition is false, the guard does NOT revert and the claim proceeds normally (with BAF credit being recorded via `recordBafFlip` at line 569).

### BAF Guard Bypass Enumeration

For each of the 7 conditions, this section documents: what makes it false (legitimate bypass), whether an attacker can force it false, and whether bypassing creates a security issue.

#### Condition 1: `winningBafCredit != 0`

**What makes it false:** Player has no winning flips to record in this claim batch. This happens when the player had no coinflip stakes during the BAF-eligible days, or all their flips were losses.

**Can attacker force it false?** No. `winningBafCredit` is accumulated from actual coinflip results recorded on-chain (line 496: `winningBafCredit += payout` only on `cursor > bafResolvedDay` for winning days). An attacker cannot retroactively change flip results.

**Security impact of bypass:** None. If there are no winning BAF credits, there is nothing to record. The `recordBafFlip` call that follows the guard would be a no-op anyway (zero amount).

#### Condition 2: `player != ContractAddresses.SDGNRS`

**What makes it false:** Player IS the sDGNRS staking contract. This is a hardcoded constant address comparison.

**Can attacker force it false?** No. The sDGNRS address is a constant (`ContractAddresses.SDGNRS`). An attacker cannot impersonate it.

**Security impact of bypass:** None -- see "sDGNRS BAF Ineligibility Proof" section below. sDGNRS is truly ineligible for BAF at both caller and callee layers.

#### Condition 3: `!inJackpotPhase`

**What makes it false:** Game IS in jackpot phase. During jackpot phase, BAF has already been resolved for this level (BAF jackpot fires at the purchase-to-jackpot transition, not during jackpot phase).

**Can attacker force it false?** No. Phase transitions are controlled by `advanceGame()` which requires VRF words and specific game state conditions. An attacker cannot force a phase transition.

**Security impact of bypass:** None. During jackpot phase, BAF leaderboard data has already been consumed by `runBafJackpot()`. Any credits recorded during jackpot phase will be accumulated for the NEXT BAF resolution (next multiple-of-10 level), which is the correct behavior.

#### Condition 4: `!over`

**What makes it false:** Game IS over (`gameOver == true`). This is a terminal state.

**Can attacker force it false?** No. Game over is set by `_handleGameOverPath` in AdvanceModule and requires liveness guard timeout (120 days inactivity). Irreversible.

**Security impact of bypass:** None. When the game is over, no further BAF resolutions will occur. BAF credits are meaningless.

#### Condition 5: `lastPurchaseDay_`

**What makes it false:** Purchase target is NOT yet met. The game is in normal purchase phase, still accumulating prize pool toward the level target.

**Can attacker force it false?** No. `lastPurchaseDay` is set when `_getNextPrizePool() >= levelPrizePool[lvl]` (AdvanceModule lines 131-134). An attacker cannot reduce the prize pool below the target to unset this flag, and the flag is only set to true by the game contract itself.

**Security impact of bypass:** None. If `lastPurchaseDay` is false, the level is not transitioning, so no BAF resolution is imminent. Recording BAF credit during normal purchase phase is correct behavior -- it builds the leaderboard for the eventual BAF resolution.

#### Condition 6: `rngLocked_`

**What makes it false:** RNG is NOT locked. No VRF request is in flight.

**Can attacker force it false?** No. `rngLockedFlag` is set by `_finalizeRngRequest()` at AdvanceModule line 1207 and cleared by `_unlockRng()` at AdvanceModule line 1283. Both are internal functions callable only within the `advanceGame()` execution flow. An attacker cannot clear the RNG lock.

**Security impact of bypass:** None -- this is the key insight. When RNG is not locked, there is no pending VRF word to front-run. BAF credit recording during RNG-unlocked periods is completely safe because the BAF jackpot resolution (which consumes the leaderboard) only happens during `advanceGame` after `rngGate` processes a VRF word.

#### Condition 7: `purchaseLevel_ % 10 == 0`

**What makes it false:** Current purchase level is NOT a multiple of 10 (e.g., level 13, 27, etc.). BAF jackpots only fire at levels 10, 20, 30, etc.

**Can attacker force it false?** No. The purchase level is derived from `level + 1` (or `level` when lastPurchaseDay and rngLocked). Level increments are controlled by `_finalizeRngRequest()` at AdvanceModule line 1219 and only occur on fresh RNG requests during lastPurchaseDay.

**Security impact of bypass:** None. Non-BAF levels (not multiples of 10) have no BAF resolution. Credits recorded at non-BAF levels accumulate toward the next BAF level bracket (handled by `_bafBracketLevel()` at BurnieCoinflip line 1091).

### sDGNRS BAF Ineligibility Proof

sDGNRS (the staked DegenerusStonk contract) is proven BAF-ineligible at two independent layers:

#### Caller-side: BurnieCoinflip.sol (line 542)

```solidity
if (winningBafCredit != 0 && player != ContractAddresses.SDGNRS) {
    // ... entire BAF section skipped for sDGNRS ...
}
```

When `player == ContractAddresses.SDGNRS`, the condition `player != ContractAddresses.SDGNRS` evaluates to `false`, and the entire BAF block (lines 542-570) is skipped. No `purchaseInfo()` call, no guard check, no `recordBafFlip` call.

#### Callee-side: DegenerusJackpots.sol (line 175)

```solidity
function recordBafFlip(address player, uint24 lvl, uint256 amount) external override onlyCoin {
    if (player == ContractAddresses.VAULT || player == ContractAddresses.SDGNRS) return;
    // ... BAF recording logic ...
}
```

Even if the caller-side skip were somehow bypassed, `recordBafFlip` returns immediately for `ContractAddresses.SDGNRS` at line 175. No BAF totals are accumulated, no leaderboard position is updated.

#### processCoinflipPayouts Path (line 846)

The critical sDGNRS claim path runs during `advanceGame`:

```solidity
// BurnieCoinflip.sol line 846 (in processCoinflipPayouts)
_claimCoinflipsInternal(ContractAddresses.SDGNRS, false);
```

This call occurs during active RNG lock (called from `advanceGame` -> `rngGate` -> `processCoinflipPayouts` at AdvanceModule line 764). The call chain is:

1. `advanceGame()` calls `rngGate()` (AdvanceModule line 228)
2. `rngGate()` calls `coinflip.processCoinflipPayouts()` (AdvanceModule line 764)
3. `processCoinflipPayouts()` calls `_claimCoinflipsInternal(ContractAddresses.SDGNRS, false)` (BurnieCoinflip line 846)
4. Inside `_claimCoinflipsInternal()`, the sDGNRS skip at line 542 bypasses the entire BAF section
5. The BAF guard (lines 555-562) is never reached for sDGNRS

**Without the sDGNRS skip:** If the BAF guard applied to sDGNRS, `processCoinflipPayouts` would always revert during RNG lock because conditions 5 and 6 (`lastPurchaseDay_` and `rngLocked_`) would typically be true during the transition path. This would break the game's daily advance flow.

#### sDGNRS Ineligibility Conclusion

sDGNRS is truly BAF-ineligible, not just "skipping the guard." The exclusion operates at both the caller-side (BurnieCoinflip line 542 skips the entire BAF section) and the callee-side (DegenerusJackpots line 175 returns early). This dual-layer exclusion ensures sDGNRS can never accumulate BAF credit or appear on BAF leaderboards, making BAF guard analysis irrelevant for the sDGNRS path.

### Reentrancy Analysis

**Question:** Can an attacker use reentrancy to bypass the BAF guard?

**Check 1: External calls before BAF guard**

Inside `_claimCoinflipsInternal()`, before the BAF guard at line 542:
- Line 392: `game.syncAfKingLazyPassFromCoin(player)` -- external call to DegenerusGame
- Line 492: `jackpots.getLastBafResolvedDay()` -- external view call to DegenerusJackpots

Both calls occur before the BAF guard check. However:
- `syncAfKingLazyPassFromCoin` is a state-sync function on DegenerusGame. Even if an attacker could reenter during this call, they cannot change `purchaseInfo()` return values (which depend on game-level state variables set by `advanceGame`).
- `getLastBafResolvedDay` is a `view` function -- no state changes possible.

**Check 2: Reentrancy protection**

The claim functions (`claimCoinflips`, `claimCoinflipsFromBurnie`, `consumeCoinflipsForBurn`) do NOT have explicit `nonReentrant` modifiers. However:
- `claimCoinflipsFromBurnie` and `consumeCoinflipsForBurn` are restricted to `onlyBurnieCoin` (BurnieCoin contract only).
- BurnieCoin's `_spendAllowance` / `transferFrom` paths that call `claimCoinflipsFromBurnie` use CEI pattern (checks-effects first, external call last).
- `claimCoinflips` can be called by anyone (via `_resolvePlayer`), but the mint at line 379 (`burnie.mintForCoinflip`) happens after state updates (line 374: `state.claimableStored = ...`), following CEI.

**Check 3: State manipulation via reentrancy**

Even if an attacker achieved reentrancy into `_claimCoinflipsInternal` during the `mintForCoinflip` call:
- `state.lastClaim` has already been updated (line 574), so the reentrant call would process 0 days.
- `state.claimableStored` has already been updated (line 374 in the outer `_claimCoinflipsAmount`).
- The 7 BAF guard conditions read from `game.purchaseInfo()` and `game.gameOver()` -- game-level state that cannot be changed by coinflip reentrancy.

**Reentrancy Verdict:** No reentrancy-based BAF guard bypass is possible. The guard conditions depend on game-level state (not coinflip state), and coinflip state updates follow CEI pattern.

### Timing Attack Analysis

**Question:** Can an attacker exploit the timing window between `processCoinflipPayouts` and `_unlockRng` within `advanceGame`?

#### rngLockedFlag Lifecycle

```
_requestRng() / _finalizeRngRequest()
    --> rngLockedFlag = true (AdvanceModule line 1207)
    --> level incremented if lastPurchaseDay (line 1219)
    |
    v
rawFulfillRandomWords()
    --> rngWordCurrent = word (line 1334, stored but NOT unlocked)
    |
    v
advanceGame() --> rngGate() processes word
    |             --> _applyDailyRng() records word (line 763)
    |             --> coinflip.processCoinflipPayouts() resolves flips (line 764)
    |             --> _finalizeLootboxRng() records lootbox word (line 765)
    |
    v (later in the same advanceGame call)
_unlockRng(day) --> rngLockedFlag = false (AdvanceModule line 1283)
                    rngWordCurrent = 0 (line 1284)
                    vrfRequestId = 0 (line 1285)
                    rngRequestTime = 0 (line 1286)
```

#### Ordering Within advanceGame (purchase phase, daily path)

Looking at the purchase phase daily path (AdvanceModule lines 277-291):

```
1. rngGate() called at line 228
   - _applyDailyRng() at line 763
   - coinflip.processCoinflipPayouts() at line 764
   - _finalizeLootboxRng() at line 765
   - returns rngWord
2. payDailyJackpot() at line 279
3. _payDailyCoinJackpot() at line 280
4. _unlockRng(day) at line 287    <-- RNG unlocked AFTER all processing
5. _unfreezePool() at line 288
```

**Can an attacker submit a claim that executes after _unlockRng but during the same advanceGame call?**

No. `_unlockRng()` is a private function called within the `advanceGame()` transaction. There is no external call between `_unlockRng` and the end of the function that could allow reentrancy. The `emit Advance(stage, lvl)` and `coin.creditFlip(caller, advanceBounty)` at lines 375-376 happen after unlockRng, but:
- `emit` is not an external call
- `coin.creditFlip` calls BurnieCoinflip which calls `_addDailyFlip` (not a claim path)

**Can an attacker submit a separate transaction that gets included after _unlockRng in the same block?**

No. `_unlockRng` executes within the `advanceGame` transaction. Once the transaction completes, RNG is unlocked. Any subsequent claim transaction in the same block would see `rngLocked_ == false`, so BAF guard condition 6 would be false, and the guard would not revert. This is correct behavior -- RNG is no longer locked, so there is no VRF word to front-run.

**Timing Attack Verdict:** No timing-based BAF guard bypass is possible. `_unlockRng` always executes after `processCoinflipPayouts` within the same transaction. Between RNG lock and unlock, all claim transactions from non-sDGNRS players will hit the BAF guard if all 7 conditions are met.

### Deposit Lock Layer

A separate protection layer exists for deposits during BAF resolution windows.

**Location:** BurnieCoinflip.sol, `_coinflipLockedDuringTransition()` at lines 985-998:

```solidity
function _coinflipLockedDuringTransition()
    private
    view
    returns (bool locked)
{
    (
        uint24 purchaseLevel_,
        bool inJackpotPhase,
        bool lastPurchaseDay_,
        bool rngLocked_,

    ) = degenerusGame.purchaseInfo();
    locked = (!inJackpotPhase) && !degenerusGame.gameOver() && lastPurchaseDay_ && rngLocked_ && (purchaseLevel_ % 10 == 0);
}
```

This function is called at `_depositCoinflip()` line 255:
```solidity
if (_coinflipLockedDuringTransition()) revert CoinflipLocked();
```

**Purpose:** Deposits trigger `_claimCoinflipsInternal()` (line 258), which would record BAF credit. By blocking deposits during BAF resolution levels, this prevents:
1. Players from front-running BAF resolution by depositing large amounts that would boost their BAF credit via auto-claim
2. Auto-claim from updating the BAF leaderboard mid-resolution

**Relationship to BAF guard:** This is a complementary protection layer. The BAF guard in `_claimCoinflipsInternal` reverts if claims would record BAF credit during resolution. The deposit lock prevents the deposit -> auto-claim -> BAF credit path entirely. Together, they form a two-layer defense:
- **Layer 1 (deposit lock):** Blocks deposits that would trigger auto-claim during BAF resolution
- **Layer 2 (BAF guard):** Reverts if direct claims would record BAF credit during BAF resolution

Both layers use the same 5 conditions (`!inJackpotPhase`, `!gameOver`, `lastPurchaseDay_`, `rngLocked_`, `purchaseLevel_ % 10 == 0`), ensuring consistent protection.

### Attacker Model Evaluation

#### Attacker Model 1: MEV-Aware (1000 ETH budget)

**Scenario:** Attacker observes the VRF word in the mempool (via `rawFulfillRandomWords` calldata) and knows the BAF resolution outcome before `advanceGame` processes it.

**Attack vector 1: Front-run with claims to manipulate BAF leaderboard.**
The attacker calls `claimCoinflips()` before `advanceGame` processes the VRF word. They have winning BAF credit to record.

**Analysis:** The BAF guard catches this exactly. During the BAF resolution window (lastPurchaseDay, rngLocked, BAF level), all 7 conditions are true, and the claim reverts with `RngLocked()`. The attacker cannot record BAF credit before the resolution.

**Attack vector 2: Back-run advanceGame to record BAF credit for the NEXT level.**
After `advanceGame` processes and unlocks RNG, the attacker claims immediately to record BAF credit for the next BAF bracket.

**Analysis:** This is legitimate behavior. After `_unlockRng()`, the current level has incremented, and BAF resolution for the previous level is complete. Recording BAF credit for the next level bracket is correct -- it builds the leaderboard for the next resolution. No advantage is gained because the attacker's credit is proportional to their actual coinflip stakes.

**MEV-Aware Verdict:** No advantage. The BAF guard prevents credit recording during the exact window where it could affect the current resolution, and post-unlock recording is legitimate.

#### Attacker Model 2: Compromised VRF Operator (1000 ETH budget)

**Scenario:** A compromised Chainlink VRF operator knows the random word before delivering it and can choose delivery timing.

**Attack vector 1: Delay VRF delivery to extend the BAF resolution window.**
The operator delays `rawFulfillRandomWords` to keep `rngLocked_ == true` for longer, preventing other players from recording BAF credit.

**Analysis:** This extends the window where the BAF guard reverts, but it does NOT help the attacker. The attacker's own claims would also be blocked by the same guard. The operator could delay indefinitely, but after 12 hours, `advanceGame` will retry the VRF request (AdvanceModule lines 772-774), generating a new request with a new word. The operator would need to compromise the next word too.

**Attack vector 2: Deliver VRF at a specific time to create a BAF bypass window.**
The operator delivers the word exactly when the attacker's front-run claim transaction is in the mempool.

**Analysis:** The VRF word delivery (`rawFulfillRandomWords`) does NOT unlock RNG. It only stores the word (`rngWordCurrent = word` at line 1334). RNG remains locked until `advanceGame` calls `_unlockRng()`. There is no window between word delivery and unlock where claims succeed but BAF credit recording is still meaningful -- the BAF guard checks `rngLocked_` which remains true until `_unlockRng()`.

**Compromised VRF Verdict:** No advantage. The separation of word delivery (`rawFulfillRandomWords`) from word processing and unlock (`advanceGame` -> `_unlockRng()`) ensures no exploitable window exists. The `rngLockedFlag` lifecycle is: set at request, maintained through delivery, cleared only after all processing completes.

### RNG-02 Verdict

**SAFE** -- The BAF epoch-based guard is sufficient as the sole claim protection during BAF resolution windows. The guard covers the exact window where claims could affect BAF outcomes (all 7 conditions precisely define the BAF resolution window). sDGNRS is truly BAF-ineligible at both the caller layer (BurnieCoinflip line 542) and callee layer (DegenerusJackpots line 175), with no guard bypass needed for the processCoinflipPayouts path. No bypass is possible via reentrancy (guard conditions depend on game-level state), timing attacks (`_unlockRng` executes after all payout processing within a single transaction), or attacker models (MEV-aware and compromised VRF scenarios evaluated). The deposit lock layer (`_coinflipLockedDuringTransition` at line 985-998) provides complementary protection against deposit-triggered auto-claim during BAF resolution.

---

## RNG-03: Decimator Claim Persistence Correctness

### 1. Storage Migration Summary

The decimator claim tracking was migrated from a single-slot struct to a per-level mapping:

| Aspect | Old (pre-v3.2) | New (current) |
|--------|---------------|---------------|
| **Declaration** | `DecClaimRound internal lastDecClaimRound` (single struct) | `mapping(uint24 => DecClaimRound) internal decClaimRounds` |
| **Location** | DegenerusGameStorage.sol | DegenerusGameStorage.sol, line 1511 |
| **Behavior** | Only most recent level claimable; new resolution overwrites | All historical levels claimable indefinitely |
| **Expiry** | Implicit (overwritten by next resolution) | None -- claims persist across rounds |

**Verification: `lastDecClaimRound` removed from codebase.** A search of all `.sol` files in the `contracts/` directory confirms zero references to `lastDecClaimRound`. The field has been completely replaced. The only single-slot claim round remaining is `lastTerminalDecClaimRound` (DegenerusGameStorage.sol, line 1624), which is a separate struct (`TerminalDecClaimRound`) used exclusively for the terminal decimator (GAMEOVER path).

**New mapping declaration (DegenerusGameStorage.sol, lines 1509-1511):**

```solidity
/// @dev Decimator claim round snapshots per level.
///      Claims persist indefinitely -- no expiry on prior rounds.
mapping(uint24 => DecClaimRound) internal decClaimRounds;
```

**DecClaimRound struct (DegenerusGameStorage.sol, lines 1491-1498):**

```solidity
struct DecClaimRound {
    uint256 poolWei;       // ETH prize pool available for claims
    uint256 rngWord;       // VRF random word for lootbox entropy
    uint232 totalBurn;     // Total qualifying burn (pro-rata denominator)
}
```

### 2. Double-Claim Prevention Trace

The `_consumeDecClaim` function (DegenerusGameDecimatorModule.sol, lines 363-386) is the sole path for consuming decimator claims. All external claim functions route through it:

- `claimDecimatorJackpot()` (line 416) calls `_consumeDecClaim(msg.sender, lvl)`
- `consumeDecClaim()` (line 398) calls `_consumeDecClaim(player, lvl)` (game-only access)

**Check-before-state-change trace:**

```solidity
function _consumeDecClaim(
    address player,
    uint24 lvl
) internal returns (uint256 amountWei) {
    DecClaimRound storage round = decClaimRounds[lvl];
    if (round.poolWei == 0) revert DecClaimInactive();      // line 368: no snapshot exists

    DecEntry storage e = decBurn[lvl][player];
    if (e.claimed != 0) revert DecAlreadyClaimed();          // line 371: CHECK -- already claimed?

    // ... calculate pro-rata share (lines 374-382) ...

    e.claimed = 1;                                           // line 385: EFFECT -- mark claimed
}
```

**Line 371:** `if (e.claimed != 0) revert DecAlreadyClaimed()` -- The claimed check reads from `decBurn[lvl][player].claimed`. This is a per-level per-player flag (uint8). A value of 0 means unclaimed; any non-zero value means claimed.

**Line 385:** `e.claimed = 1` -- Sets the claimed flag to 1 AFTER all calculations but BEFORE any ETH transfer occurs. The ETH distribution happens in the calling functions (`claimDecimatorJackpot` calls `_creditDecJackpotClaimCore` or `_addClaimableEth` after `_consumeDecClaim` returns).

**Check-before-effect ordering:** The `e.claimed != 0` check at line 371 occurs BEFORE the `e.claimed = 1` write at line 385. No state changes or external calls intervene between the check and the effect within `_consumeDecClaim`. The ETH crediting happens in the calling function after `_consumeDecClaim` returns, making the pattern check-effect-interact (CEI compliant).

**Can an attacker claim the same level twice?** No. On the first call, `e.claimed == 0` passes the check, and `e.claimed` is set to 1 at line 385. On any subsequent call for the same `(lvl, player)` pair, `e.claimed != 0` is true, and the function reverts with `DecAlreadyClaimed`. There is no path to reset `e.claimed` back to 0 -- the `DecEntry` struct has no function that writes `claimed = 0` after initial setup.

**Reentrancy consideration:** `_consumeDecClaim` is an `internal` function with no external calls. The calling functions credit ETH via `_addClaimableEth` (pull pattern -- credits to `claimableWinnings` mapping, no ETH transfer). No reentrancy vector exists.

### 3. Terminal Decimator Claimed Flag

The terminal decimator uses a different claimed-flag mechanism: `weightedBurn = 0` as an implicit claimed marker.

**Location:** DegenerusGameDecimatorModule.sol, `_consumeTerminalDecClaim()` (lines 970-994)

```solidity
function _consumeTerminalDecClaim(
    address player
) private returns (uint256 amountWei) {
    uint24 lvl = lastTerminalDecClaimRound.lvl;
    if (lvl == 0) revert TerminalDecNotActive();                    // line 974

    TerminalDecEntry storage e = terminalDecEntries[player];
    if (e.burnLevel != uint48(lvl) || e.weightedBurn == 0)          // line 977: CHECK
        revert TerminalDecNotWinner();

    uint88 weight = e.weightedBurn;                                 // line 980: read before zero

    // ... calculate pro-rata share (lines 982-990) ...

    e.weightedBurn = 0;                                             // line 993: EFFECT -- mark claimed
}
```

**Check (line 977):** `e.weightedBurn == 0` rejects already-claimed entries. On first claim, `weightedBurn > 0` (accumulated from burns). After claim, `weightedBurn == 0` (set at line 993).

**Effect (line 993):** `e.weightedBurn = 0` zeroes the weight, serving dual purpose: (1) prevents double-claim on subsequent calls, and (2) removes the player's share from future calculations (view function `terminalDecClaimable` also checks `e.weightedBurn == 0` at line 954).

**Can `weightedBurn` be set back to non-zero after claiming?** Only via `recordTerminalDecBurn()` (line 808), which contains a lazy-reset mechanism at lines 827-833:

```solidity
if (e.burnLevel != uint48(lvl)) {
    e.totalBurn = 0;
    e.weightedBurn = 0;    // reset for new level
    e.bucket = 0;
    e.subBucket = 0;
    e.burnLevel = uint48(lvl);
}
```

This reset only triggers when `e.burnLevel != uint48(lvl)` -- i.e., when the GAME LEVEL has changed. The terminal decimator resolution sets `lastTerminalDecClaimRound.lvl = lvl` (line 921), and claims check `e.burnLevel != uint48(lvl)` (line 977). For the resolved level, `burnLevel == lvl`, so the lazy reset does NOT trigger, and `weightedBurn` remains 0 after claiming. New burns via `recordTerminalDecBurn` would only set `weightedBurn` for a DIFFERENT level (post-GAMEOVER, no more burns are possible anyway since `recordTerminalDecBurn` requires `msg.sender == ContractAddresses.COIN` and the game contract controls burn recording).

**Equivalence to removed `TerminalDecAlreadyClaimed` error:** The old explicit error was replaced by the `weightedBurn == 0` check. The behavior is equivalent: any attempt to claim after the first successful claim reverts with `TerminalDecNotWinner` (because `weightedBurn == 0`). The error message is less specific but the protection is identical.

### 4. ETH Accounting Across Rounds

#### How the decimator ETH pool is funded

Decimator pools are funded during level-end jackpot resolution. The call chain is:

1. `advanceGame()` -> `_runRewardJackpots(lvl, rngWord)` (AdvanceModule line 359)
2. `runRewardJackpots` in EndgameModule calls `runDecimatorJackpot(poolWei, lvl, rngWord)` (DegenerusGameDecimatorModule line 298)
3. `runDecimatorJackpot` snapshots the pool: `decClaimRounds[lvl].poolWei = poolWei` (line 344)

The `poolWei` amount comes from the decimator's share of the level prize pool, determined by the EndgameModule's allocation formula.

#### Per-player share calculation in `_consumeDecClaim`

```solidity
// _consumeDecClaim lines 374-382
uint64 packedOffsets = decBucketOffsetPacked[lvl];
uint256 totalBurn = uint256(round.totalBurn);
amountWei = _decClaimableFromEntry(
    round.poolWei,    // pool for THIS level
    totalBurn,        // total burn for THIS level
    e,                // player's entry for THIS level
    packedOffsets     // winning subbuckets for THIS level
);
```

The pro-rata formula (in `_decClaimableFromEntry`, line 635):
```solidity
amountWei = (poolWei * uint256(entryBurn)) / totalBurn;
```

Every value in this calculation is keyed by `lvl`:
- `poolWei` = `decClaimRounds[lvl].poolWei` -- the specific pool for that level
- `totalBurn` = `decClaimRounds[lvl].totalBurn` -- total qualifying burn for that level
- `entryBurn` = `decBurn[lvl][player].burn` -- player's burn for that level
- `packedOffsets` = `decBucketOffsetPacked[lvl]` -- winning subbuckets for that level

#### Does persistence change total pool or per-player shares?

**No.** Each round's `DecClaimRound` is stored independently in `decClaimRounds[lvl]`. The pool, totalBurn, and rngWord are all level-specific. Claiming from level 5 reads `decClaimRounds[5]`; claiming from level 15 reads `decClaimRounds[15]`. These are separate mapping entries with no cross-contamination.

Previously, `lastDecClaimRound` was a single struct that got overwritten on each new resolution. The migration to `decClaimRounds[lvl]` means old rounds are no longer overwritten -- they persist. But the per-round accounting is identical: same pool, same totalBurn, same pro-rata formula.

#### Can ETH be double-counted between rounds?

**No.** The `claimablePool` accounting is managed at claim time:

1. **Pool reservation:** `runDecimatorJackpot` receives `poolWei` as an argument (already deducted from prize pools by the caller). It either returns `poolWei` (no winners) or returns 0 (all funds held for claims). The pool is accounted for in `claimablePool` at the EndgameModule level.

2. **Per-claim deduction:** When a player claims via `claimDecimatorJackpot`:
   - 50% goes to `_addClaimableEth` (stays in claimablePool)
   - 50% goes to lootbox tickets: `claimablePool -= lootboxPortion` (line 538 in `_creditDecJackpotClaimCore`)

3. **Cross-round isolation:** Level 5's `poolWei` was funded from level 5's resolution. Level 15's `poolWei` was funded from level 15's resolution. These are separate ETH allocations from separate prize pool snapshots. A player claiming from level 5 cannot access level 15's pool, and vice versa.

4. **Double-snapshot prevention:** `runDecimatorJackpot` checks `decClaimRounds[lvl].poolWei != 0` at line 306. If a level is already snapshotted, the full `poolWei` is returned to the caller (not added to the existing snapshot). This prevents double-funding of the same level.

### 5. RNG-03 Verdict

**SAFE** -- The decimator claim persistence migration from `lastDecClaimRound` to `decClaimRounds[lvl]` is a storage layout change with clear correctness properties:

1. **Double-claim prevention** is enforced by `e.claimed != 0` check at line 371 (regular decimator) and `e.weightedBurn == 0` check at line 977 (terminal decimator). Both checks occur before any state changes.
2. **ETH accounting** is per-level: each `DecClaimRound` stores its own `poolWei` and `totalBurn`, with no cross-round contamination. The pro-rata formula reads only from the specific level's data.
3. **Double-snapshot prevention** via `decClaimRounds[lvl].poolWei != 0` check at line 306 ensures a level cannot be funded twice.
4. **No RNG attack surface** -- persistence allows claiming from older rounds but does not introduce new RNG dependencies. The VRF word used for winner selection is snapshotted at resolution time and cannot be manipulated retroactively.

Per user decision: "same security posture as before, just different storage layout." Confirmed -- the security mechanisms (claimed flag, pro-rata calculation, snapshot protection) are unchanged from the single-slot design.

---

## RNG-04: Cross-Contract rngLocked Dependency Matrix

### 1. rngLocked Consumer Inventory

The following table documents every `rngLocked` consumer across all contracts. Each entry is verified against the current codebase with exact line numbers.

#### rngLockedFlag SET

| Contract | Function | Line | Action |
|----------|----------|------|--------|
| AdvanceModule | `_finalizeRngRequest()` | 1207 | `rngLockedFlag = true` -- set on every VRF request (daily or retry) |

#### rngLockedFlag CLEARED

| Contract | Function | Line | Action |
|----------|----------|------|--------|
| AdvanceModule | `_unlockRng()` | 1283 | `rngLockedFlag = false` -- cleared after daily processing completes |
| AdvanceModule | `updateVrfCoordinatorAndSub()` | 1271 | `rngLockedFlag = false` -- governance emergency VRF rotation |

#### rngLocked() VIEW Consumers (via DegenerusGame.rngLocked())

| Contract | Function | Line | Guard Type | Assumed Claims Blocked? | Still Safe Without? | Verdict |
|----------|----------|------|-----------|------------------------|--------------------| --------|
| DegenerusGame | `setDecimatorAutoRebuy()` | 1542 | `if (rngLockedFlag) revert RngLocked()` | No -- guards ETH auto-rebuy config toggle for decimator claims | Yes -- toggling during lock could let attacker disable auto-rebuy before a known loss, but decimator payouts are from jackpot resolution (past event), not pending flips | **SAFE** |
| DegenerusGame | `_setAutoRebuy()` | 1563 | `if (rngLockedFlag) revert RngLocked()` | No -- guards ETH auto-rebuy toggle | Yes -- prevents toggling off during RNG lock to extract carry from ETH jackpot winnings before a known loss | **SAFE** (guard retained) |
| DegenerusGame | `_setAutoRebuyTakeProfit()` | 1578 | `if (rngLockedFlag) revert RngLocked()` | No -- guards ETH take-profit change | Yes -- prevents changing take-profit threshold during lock | **SAFE** (guard retained) |
| DegenerusGame | `_setAfKingMode()` | 1643 | `if (rngLockedFlag) revert RngLocked()` | No -- guards afKing mode toggle | Yes -- afKing enables auto-rebuy for both ETH and coin; toggling during lock could extract carry | **SAFE** (guard retained) |
| BurnieCoin | `balanceOfWithClaimable()` | 273 | `if (degenerusGame.rngLocked()) return spendable` | No -- excludes coinflip claimable from view | N/A -- view function, no state change. Now conservative (underreports) since claims succeed during lock | **INFO** (documented as INFO-01) |
| BurnieCoinflip | `_setCoinflipAutoRebuy()` | 691 | `if (degenerusGame.rngLocked()) revert RngLocked()` | No -- guards coinflip auto-rebuy toggle | Yes -- this is the critical carry-extraction prevention; must remain to block toggling off during lock | **SAFE** (guard retained) |
| BurnieCoinflip | `_setCoinflipAutoRebuyTakeProfit()` | 741 | `if (degenerusGame.rngLocked()) revert RngLocked()` | No -- guards coinflip take-profit change | Yes -- prevents changing take-profit to extract more carry during lock | **SAFE** (guard retained) |

#### rngLockedFlag DIRECT Consumers (via DegenerusGameStorage inheritance)

| Contract | Function | Line | Guard Type | Assumed Claims Blocked? | Still Safe Without? | Verdict |
|----------|----------|------|-----------|------------------------|--------------------| --------|
| AdvanceModule | `requestLootboxRng()` | 673 | `if (rngLockedFlag) revert RngLocked()` | No -- prevents competing VRF requests | N/A -- operational guard, not claims-related | **SAFE** (guard retained) |
| AdvanceModule | `reverseFlip()` | 1295 | `if (rngLockedFlag) revert RngLocked()` | No -- prevents nudges during pending VRF | N/A -- prevents manipulation of pending word | **SAFE** (guard retained) |
| AdvanceModule | `advanceGame()` | 138 | `bool lastPurchase && rngLockedFlag` (level calc) | No -- determines purchaseLevel value | N/A -- computational use, not a guard | **SAFE** (logic dependency) |
| WhaleModule | `_purchaseDeityPass()` | 470 | `if (rngLockedFlag) revert RngLocked()` | No -- prevents deity pass purchase during lock | N/A -- deity pass queues tickets; blocking during lock prevents ticket queue manipulation | **SAFE** (guard retained) |
| BurnieCoinflip | `_claimCoinflipsInternal()` | 555-562 | BAF guard (5-condition revert) | No -- guards BAF credit recording during resolution | Yes -- targeted protection for BAF resolution window only | **SAFE** (analyzed in RNG-02) |
| BurnieCoinflip | `_coinflipLockedDuringTransition()` | 997 | Compound condition including `rngLocked_` | No -- blocks deposits during BAF resolution | Yes -- complementary to BAF guard (deposit path) | **SAFE** (analyzed in RNG-02) |
| BurnieCoinflip | `_addDailyFlip()` | 630 | `!game.rngLocked()` (bounty arm condition) | No -- prevents arming bounty during lock | Yes -- bounty record is non-critical; prevents manipulation of biggest-flip record when VRF outcome is known | **SAFE** (guard retained) |

#### REMOVED (the v3.2 changes under audit)

| Contract | Function | Previous Line | Change | Impact |
|----------|----------|---------------|--------|--------|
| BurnieCoinflip | `claimCoinflips()` | Was at function entry | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` | Claims now proceed during lock; safety via carry isolation (RNG-01) |
| BurnieCoinflip | `claimCoinflipsFromBurnie()` | Was at function entry | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` | Same as above |
| BurnieCoinflip | `consumeCoinflipsForBurn()` | Was at function entry | Removed `if (degenerusGame.rngLocked()) revert RngLocked()` | Same as above |
| BurnieCoinflip | `claimCoinflipsTakeProfit()` | Entire function | **Deleted** -- function and interface entry removed | Callers use `claimCoinflips()` with amount parameter |

**Total inventory: 18 entries** (7 VIEW consumers + 7 DIRECT consumers + 4 REMOVED)

### 2. Dependency Matrix Analysis

For each remaining rngLocked consumer, the critical question is: **Did this function's safety depend on the assumption that coinflip claims were blocked during RNG lock?**

| Consumer | Assumed Claims Blocked? | Still Safe Now? | Why |
|----------|------------------------|----------------|-----|
| `setDecimatorAutoRebuy` | No | Yes | Guards ETH decimator auto-rebuy toggle. Decimator payouts come from resolved jackpots (past events), not pending coinflip results. Whether coinflip claims proceed has no bearing on decimator auto-rebuy safety. |
| `_setAutoRebuy` | No | Yes | Guards ETH auto-rebuy toggle for ticket purchases. ETH carry isolation is independent of coinflip claims -- it operates in the game contract's claimableWinnings system, not the coinflip contract's carry system. |
| `_setAutoRebuyTakeProfit` | No | Yes | Same as `_setAutoRebuy` -- ETH take-profit changes are orthogonal to coinflip claim state. |
| `_setAfKingMode` | No | Yes | Enabling afKing forces auto-rebuy on for both ETH and coin. The guard prevents state changes during lock, not claims. Coinflip claims proceeding does not affect afKing activation safety. |
| `balanceOfWithClaimable` | Indirectly | N/A (view) | Previously, excluding claimable during lock was accurate (claims would revert). Now it underreports. No safety impact -- view function is conservative. |
| `_setCoinflipAutoRebuy` | No (but critical) | Yes | This guard prevents toggling auto-rebuy off during lock, which would extract carry. Its safety does NOT depend on claims being blocked -- it depends on the toggle being blocked. Claims proceeding is irrelevant because claims never expose carry (per RNG-01 invariant). |
| `_setCoinflipAutoRebuyTakeProfit` | No | Yes | Similar to `_setCoinflipAutoRebuy`. The guard prevents take-profit threshold changes that could redirect carry to mintable. Independent of whether claims proceed. |
| `requestLootboxRng` | No | Yes | Prevents competing VRF requests. Completely unrelated to coinflip claims. |
| `reverseFlip` | No | Yes | Prevents nudging pending VRF word. Completely unrelated to coinflip claims. |
| `advanceGame` (line 138) | No | Yes | Computational use for `purchaseLevel` calculation. Not a safety guard. |
| `_purchaseDeityPass` | No | Yes | Blocks deity pass purchases that queue tickets. Independent of coinflip claims. |
| BAF guard (lines 555-562) | No | Yes | Guards BAF credit recording. Independent of whether the outer claim functions have rngLocked checks. The BAF guard runs inside `_claimCoinflipsInternal` regardless of the caller. |
| `_coinflipLockedDuringTransition` | No | Yes | Blocks deposits during BAF resolution. Independent of claim-path guards. |
| `_addDailyFlip` (line 630) | No | Yes | Prevents bounty arming during lock. Independent of claim-path guards. |

**Key insight:** All remaining rngLocked consumers guard **configuration changes** (auto-rebuy toggles, take-profit thresholds, deity pass purchases, nudges) or **operational exclusion** (competing VRF requests, BAF credit recording). None of them assumed that coinflip claims were blocked during RNG lock as part of their safety model. Their guards are orthogonal to the claim-path guards that were removed.

### 3. Combined-Change Interaction Analysis

Three categories of changes are under audit. This section evaluates whether they create emergent attack vectors when combined.

#### Interaction 1: rngLocked Removal + Decimator Persistence

**Question:** Does allowing coinflip claims during RNG lock while also allowing old decimator claims create any interaction?

**Analysis:**
- **Different asset systems:** Coinflip claims produce BURNIE tokens (minted by BurnieCoin). Decimator claims produce ETH (credited to `claimableWinnings` in DegenerusGame). These are separate token flows with separate contracts.
- **Different contracts:** Coinflip claims execute in BurnieCoinflip.sol. Decimator claims execute in DegenerusGameDecimatorModule.sol (via delegatecall to DegenerusGame). No shared mutable state between the two claim paths.
- **No timing dependency:** Decimator claims from level 5 do not depend on the current RNG state. The `decClaimRounds[5]` snapshot was created at level 5's resolution, with its own VRF word. Whether coinflip claims proceed during the current RNG lock is irrelevant.
- **Attacker scenario (1000 ETH):** An attacker with historical decimator wins and current coinflip positions claims both during RNG lock. Decimator claims succeed (no rngLocked guard, never had one). Coinflip claims succeed (rngLocked guard removed). Neither claim path interferes with the other. No value extracted beyond legitimate entitlements.

**Verdict:** No interaction. Different assets, different contracts, different timing dependencies.

#### Interaction 2: rngLocked Removal + Interface Deletions

**Question:** Do the deleted functions (`claimCoinflipsTakeProfit`, `futurePrizePoolTotalView`) remove any safety check that was relied upon?

**`claimCoinflipsTakeProfit` deletion:**
- This function was a convenience wrapper that claimed the take-profit portion of coinflip winnings. Its functionality is now handled by `claimCoinflips()` with an explicit `amount` parameter.
- The old function had the same `rngLocked` guard as the other claim functions. Its removal does not introduce a new unguarded path -- `claimCoinflips` is the replacement, and both the old and new paths route through `_claimCoinflipsAmount()` -> `_claimCoinflipsInternal()`.
- No safety check was lost. The take-profit claim path still exists; it just uses a different entry point.

**`futurePrizePoolTotalView` deletion:**
- This was a read-only view function that returned the combined future prize pool value. It has been removed from IDegenerusGame.sol.
- View functions cannot enforce safety invariants. Their removal cannot open attack vectors.
- The underlying `futurePrizePoolView()` function still exists in the interface (line 150), providing the same data through a different accessor.

**Verdict:** No interaction. The deleted function had the same guards as its replacement. The deleted view function had no security role.

#### Interaction 3: All Three Combined

**Question:** Can a 1000 ETH attacker exploit the combination of rngLocked removal + decimator persistence + interface deletions?

**Scenario construction:** The attacker would need to find a state where:
1. They can claim coinflip winnings during RNG lock (enabled by rngLocked removal), AND
2. They can claim decimator jackpots from historical levels (enabled by persistence), AND
3. The combination produces more value than claiming each independently.

**Analysis:**
- Coinflip claims during RNG lock expose only `claimableStored + mintable` (take-profit). Carry is isolated (RNG-01). The claim amount is bounded by the player's actual coinflip results.
- Decimator claims from historical levels expose the player's pro-rata share of that level's pool. The claim amount is bounded by `(poolWei * playerBurn) / totalBurn`.
- Neither claim path influences the other's calculation. Coinflip `mintable` comes from `_claimCoinflipsInternal` (BURNIE). Decimator `amountWei` comes from `_consumeDecClaim` (ETH). No shared state is read or written.
- The deleted interface functions do not create new entry points or remove guards from existing ones.

**1000 ETH attacker model:**
1. Attacker deposits 1000 ETH into purchases, maximizing ticket count and decimator burn.
2. Attacker stakes large BURNIE amounts into coinflip.
3. During RNG lock, attacker claims coinflip take-profit (succeeds, bounded by actual winnings) and claims historical decimator jackpots (succeeds, bounded by actual burn share).
4. Total extracted = coinflip take-profit + decimator share. Both are legitimate entitlements. No amplification from the combination.

**Verdict:** No emergent attack vector. The three changes operate on independent state domains (BURNIE coinflip carry, ETH decimator pools, interface bindings) with no cross-domain amplification.

### 4. Interface NatSpec Stale Findings

Three functions in IBurnieCoinflip.sol retain stale `@custom:reverts RngLocked` NatSpec tags that are no longer accurate:

#### LOW-01: Stale `@custom:reverts RngLocked` on `claimCoinflips`

**Location:** IBurnieCoinflip.sol, line 33

```solidity
/// @custom:reverts RngLocked If VRF randomness is currently being resolved.
function claimCoinflips(address player, uint256 amount) external returns (uint256 claimed);
```

**Issue:** The `rngLocked` guard was removed from `claimCoinflips()` in BurnieCoinflip.sol (line 328). This function no longer reverts with `RngLocked`. The `@custom:reverts` tag is stale.

**Note:** The function CAN still revert with `RngLocked` via the BAF guard in `_claimCoinflipsInternal()` (line 562), but only under the specific 5-condition BAF resolution window. The unconditional entry-level guard was removed.

**Severity:** LOW -- NatSpec documentation does not match implementation. Could mislead integrators or auditors.

#### LOW-02: Stale `@custom:reverts RngLocked` on `claimCoinflipsFromBurnie`

**Location:** IBurnieCoinflip.sol, line 42

```solidity
/// @custom:reverts RngLocked If VRF randomness is currently being resolved.
function claimCoinflipsFromBurnie(address player, uint256 amount) external returns (uint256 claimed);
```

**Issue:** Same as LOW-01. The `rngLocked` guard was removed from `claimCoinflipsFromBurnie()` at BurnieCoinflip.sol line 339.

**Severity:** LOW -- Stale NatSpec.

#### LOW-03: Stale `@custom:reverts RngLocked` on `consumeCoinflipsForBurn`

**Location:** IBurnieCoinflip.sol, line 52

```solidity
/// @custom:reverts RngLocked If VRF randomness is currently being resolved.
function consumeCoinflipsForBurn(address player, uint256 amount) external returns (uint256 consumed);
```

**Issue:** Same as LOW-01 and LOW-02. The `rngLocked` guard was removed from `consumeCoinflipsForBurn()` at BurnieCoinflip.sol line 349.

**Severity:** LOW -- Stale NatSpec.

#### IDegenerusGame.sol: `futurePrizePoolTotalView`

**Check:** Searched IDegenerusGame.sol for any reference to `futurePrizePoolTotalView`. **No references found.** The function has been cleanly removed from both the interface and implementation. No stale NatSpec or dangling references remain.

### 5. RNG-04 Verdict

**SAFE** -- The cross-contract rngLocked dependency analysis confirms:

1. **All 18 rngLocked consumers** are accounted for (7 VIEW, 7 DIRECT, 4 REMOVED). Every remaining consumer has an explicit safety verdict.
2. **No consumer assumed claims were blocked** as part of its safety model. All remaining guards protect configuration changes (auto-rebuy toggles, take-profit thresholds), operational exclusion (competing VRF requests, BAF credit recording), or computational dependencies (purchaseLevel calculation).
3. **Combined-change analysis** shows no emergent attack vectors across the three change categories (rngLocked removal, decimator persistence, interface deletions). The changes operate on independent state domains with no cross-domain amplification.
4. **Interface NatSpec findings** are classified LOW severity -- three stale `@custom:reverts RngLocked` tags on IBurnieCoinflip functions that no longer unconditionally revert with `RngLocked`.

The remaining rngLocked guards form a coherent and complete protection surface: configuration guards prevent carry extraction during lock, the BAF guard prevents leaderboard manipulation during resolution, and operational guards prevent VRF interference. No gap exists from the removal of claim-path guards.

---

## v3.3 Addendum: New RNG Consumer

**Added:** Redemption roll consumer in DegenerusGameAdvanceModule.sol (rngGate line 773, _gameOverEntropy lines 835 and 864).

**Operation:** `(currentWord >> 8) % 151 + 25` extracts bits 8+ from the VRF word.

**Bit safety:** Bits 8+ do not overlap with bit 0 (coinflip win/loss). All other VRF consumers use full-word modular arithmetic or keccak mixing, so no bit-level collision exists. See BIT ALLOCATION MAP comment in DegenerusGameAdvanceModule.sol above `rngGate()`.

**Verdict:** SAFE. The new consumer follows the established bit-extraction pattern and does not create any bit-overlap concern with existing consumers.
