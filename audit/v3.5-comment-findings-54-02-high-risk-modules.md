# v3.5 Comment Correctness Findings: High-Risk Game Modules

**Date:** 2026-03-22
**Scope:** DegenerusGameAdvanceModule.sol (1,453 lines), DegenerusGameLootboxModule.sol (1,814 lines) -- 3,267 lines total
**Pass:** v3.5 comment correctness
**Mode:** Flag-only (no code changes)

---

## Summary

| Contract | New Findings | Prior Findings Verified | Total |
|----------|-------------|------------------------|-------|
| DegenerusGameAdvanceModule.sol | 2 | 3 (all fixed) | 2 |
| DegenerusGameLootboxModule.sol | 3 | 1 (fixed) | 3 |
| **Total** | **5** | **4** | **5** |

---

## Prior Finding Verification

### CMT-V32-003 (LootboxModule -- PlayerCredited event missing @param recipient)

**Status: FIXED**

The `PlayerCredited` event now has complete NatSpec at lines 50-53:
- `@param player` -- present
- `@param recipient` -- present (was missing in v3.2)
- `@param amount` -- present

### CMT-V32-004 (AdvanceModule -- wireVrf missing @param keyHash_)

**Status: FIXED**

The `wireVrf` function now has complete NatSpec at lines 392-397:
- `@param coordinator_` -- present
- `@param subId` -- present
- `@param keyHash_` -- present (was missing in v3.2)

### F-50-01 (AdvanceModule -- full 256-bit modulo on additive random step)

**Status: FIXED**

Line 1022 now reads: `// Step 2: Additive random 0-10% on bps (full 256-bit modulo; functionally uniform)`. The parenthetical accurately describes that `rngWord % (ADDITIVE_RANDOM_BPS + 1)` operates on the full 256-bit VRF word, not a bit-isolated subset. The modulo operation makes this functionally uniform for the small output range (0-1000).

### F-50-02 (AdvanceModule -- roll1/roll2 bit overlap documented)

**Status: FIXED**

Lines 1029-1030 now read: `// roll1 uses bits [64:255], roll2 uses bits [192:255] -- overlap at [192:255] is // irrelevant because modulo by range (small) makes outputs independent.`

Verified against code at lines 1038-1039:
- `roll1 = (rngWord >> 64) % range` -- shift by 64 gives bits [64:255] (192 bits), correct
- `roll2 = (rngWord >> 192) % range` -- shift by 192 gives bits [192:255] (64 bits), correct
- Overlap at [192:255] is accurately documented and the independence argument via modulo is sound for practical range values (halfWidth << 2^64)

---

## DegenerusGameAdvanceModule.sol

**Review scope:** 1,453 lines, 73 NatSpec tags, 38 functions (1 external, 1 internal, 36 private)

### New Findings

| ID | Severity | Contract | Lines | Summary | Recommendation |
|----|----------|----------|-------|---------|----------------|
| CMT-V35-001 | INFO | AdvanceModule.sol | 749, 753-754 | Bit allocation map has stale line references: redemption roll says "AdvanceModule.sol:795" (actual: 800), lootbox RNG storage says "AdvanceModule.sol:826" (actual: 831 in `_finalizeLootboxRng`), future take variance says "AdvanceModule.sol:1033" (actual: 1037-1039 for range/roll computation) | Update line references in the bit allocation map comment to match current source |
| CMT-V35-002 | INFO | AdvanceModule.sol | 754 | Bit allocation map labels future take variance as "full" word consumer, but actual code uses `rngWord >> 64` and `rngWord >> 192` (shifted subsets, not full word). The Step 4 inline comment (lines 1029-1030, fba43f2c fix) correctly documents these bit ranges, creating an inconsistency between the map and the inline comment. | Update bit allocation map to show actual bit ranges (64+ and 192+) instead of "full" for future take variance, or add a note that "full" means the word is used after shifting |

### Fresh Scan Notes

- **advanceGame() NatSpec** (line 124-125): "Called daily to process jackpots, mints, and phase transitions" -- accurate. "Caller receives ~0.01 ETH worth of BURNIE as flip credit" -- accurate (ADVANCE_BOUNTY_ETH = 0.01 ether, escalated 2-3x after stall).
- **wireVrf NatSpec** (lines 392-397): All 3 @param tags present and accurate after CMT-V32-004 fix. @dev access control note is correct (ContractAddresses.ADMIN only).
- **updateVrfCoordinatorAndSub NatSpec** (lines 1323-1328): All 3 @param tags accurate. @dev governance note is correct.
- **rawFulfillRandomWords NatSpec** (lines 1386-1392): @param tags accurate. @dev correctly distinguishes daily vs mid-day RNG paths.
- **_handleGameOverPath** (lines 417-418): Has @dev and return description but no @param tags for 6 parameters. Consistent with other private helper functions in this contract -- not flagged as the contract follows a pattern of full NatSpec on public/external functions and lighter NatSpec on private helpers.
- **rngGate NatSpec** (lines 763-765): @dev description accurately covers all 4 operations (apply VRF, coinflip payouts, redemption resolution, lootbox RNG). No @param tags on internal function -- consistent with contract style.
- **_gameOverEntropy NatSpec** (lines 836-840): @dev accurately describes fallback behavior and CP-06 fix. @return accurately describes the three possible return values (word, 1, or 0).
- **_getHistoricalRngFallback NatSpec** (lines 916-926): Thorough documentation of entropy sourcing, validator manipulation tradeoff, and level-0 edge case. All accurate.
- **Skim pipeline inline comments** (lines 985-1057): All 5 steps verified against code:
  - Step 1 (ratio adjust): Comment "+-4% based on future/next ratio (target 2:1)" matches code (400 bps limit)
  - Step 2 (additive random): Updated by fba43f2c, verified accurate
  - Step 3 (compute take): Straightforward, accurate
  - Step 4 (triangular variance): Updated by fba43f2c, verified accurate
  - Step 5 (take cap 80%): Matches NEXT_TO_FUTURE_BPS_MAX = 8000
- **reverseFlip NatSpec** (lines 1360-1364): @dev accurately describes cost scaling (+50% compound), reset after fulfillment, and security model.
- **_currentNudgeCost NatSpec** (lines 1435-1440): NOTE comment about O(n) complexity is a useful design note. Accurate.

---

## DegenerusGameLootboxModule.sol

**Review scope:** 1,814 lines, 315 NatSpec tags, 28 functions (5 external, 0 internal, 23 private)

### New Findings

| ID | Severity | Contract | Lines | Summary | Recommendation |
|----|----------|----------|-------|---------|----------------|
| CMT-V35-003 | LOW | LootboxModule.sol | 32 | Contract header lists `resolveLootboxRng` as a function in the "Lootbox opening" group, but this function does not exist in the contract or anywhere in the codebase. The actual function for redemption lootbox resolution is `resolveRedemptionLootbox` (line 724). Stale reference from a rename. | Replace `resolveLootboxRng` with `resolveRedemptionLootbox` in the contract header. A C4A warden reading the header would search for a function that doesn't exist. |
| CMT-V35-004 | INFO | LootboxModule.sol | 149 | `LootBoxReward` event @param rewardType NatSpec lists types 2, 4, 5, 6, 8, 9, 10 but omits `11=LazyPassBoon`. Lazy pass boon emissions use rewardType=11 at line 1548. | Add `11=LazyPassBoon` to the rewardType enumeration in NatSpec |
| CMT-V35-005 | INFO | LootboxModule.sol | 684 | `resolveLootboxDirect` @notice says "for decimator claims" but the function is also called from DegeneretteModule (line 708 in DegeneretteModule) for degenerette lootbox resolution. Description is narrower than actual usage. | Change to "Resolve a lootbox directly without RNG wait" or "for decimator and degenerette claims" |

### Fresh Scan Notes

- **PlayerCredited event NatSpec** (lines 50-53): All 3 @param tags present and accurate. CMT-V32-003 confirmed fixed.
- **LootBoxOpened event NatSpec** (lines 56-63): All 7 @param tags present. `@param index The day index when the lootbox was opened` -- the event parameter is named `index` but receives a day value; the NatSpec accurately describes this as "day index". Minor naming disconnect but NatSpec is correct about what's emitted.
- **BurnieLootOpen event NatSpec** (lines 74-80): All 6 @param tags accurate. `@param index The RNG index of the lootbox` -- correct, the emit at line 675 passes the RNG index parameter directly.
- **LootBoxWhalePassJackpot event NatSpec** (lines 90-97): `@param tickets Tickets per level granted by the whale pass reward` -- slightly misleading since actual tickets/level vary (40 for bonus levels, 2 for normal), but the NatSpec describes the base rate. `@param statsBoost Reserved for future use (always 0)` and `@param frozenUntilLevel Reserved for future use (always 0)` -- correct, both are always emitted as 0 (line 1533).
- **openLootBox NatSpec** (lines 547-552): @param and @custom:reverts tags accurate. Both revert paths (amount==0 -> E, rngWord==0 -> RngNotReady) are correctly documented.
- **openBurnieLootBox NatSpec** (lines 627-632): @param and @custom:reverts accurate. Additional E revert for price==0 is correctly documented in the combined "zero or price is zero" description.
- **resolveRedemptionLootbox NatSpec** (lines 717-723): All 4 @param tags accurate. @dev correctly describes delegatecall path and snapshotted activity score usage.
- **deityBoonSlots NatSpec** (lines 756-762): All @param and @return tags accurate. @dev correctly describes deterministic generation and decimator window gating.
- **issueDeityBoon NatSpec** (lines 779-790): All @param and @custom:reverts tags verified against implementation:
  - deity/recipient zero check (line 792) -- documented
  - self-boon check (line 793) -- documented
  - slot >= 3 check (line 794) -- documented
  - no purchased passes check (line 795) -- documented
  - no RNG available check (line 798) -- documented
  - recipient already received boon (line 803) -- documented
  - slot already used (line 807) -- documented
- **_rollTargetLevel NatSpec** (lines 823-828): Probability distribution "95% chance: 0-5 levels" and "5% chance: 5-50 levels" verified against modulo operations (% 100, % 6, % 46 + 5). All @param and @return tags accurate.
- **_resolveLootboxCommon NatSpec** (lines 849-866): All 13 @param and 3 @return tags verified. Descriptions match parameter usage throughout the function.
- **_resolveLootboxRoll NatSpec** (lines 1552-1565): "55% tickets, 10% DGNRS, 10% WWXRP, 25% BURNIE" -- verified against `roll = nextEntropy % 20` with thresholds 11, 13, 15. All 8 @param and 4 @return tags accurate.
- **_lootboxTicketCount NatSpec** (lines 1654-1661): Variance tier distribution "1% get 4.6x, 4% get 2.3x, 20% get 1.1x, 45% get 0.651x, 30% get 0.45x" -- verified against chance constants and BPS values. 100% coverage confirmed (100+400+2000+4500+3000=10000 bps).
- **_lootboxDgnrsReward NatSpec** (lines 1708-1712): "79.5% small, 15% medium, 5% large, 0.5% mega" -- verified against `tierRoll % 1000` with thresholds 795, 945, 995. All correct.
- **Boon weight totals** (lines 458-461): BOON_WEIGHT_TOTAL=1298 and BOON_WEIGHT_TOTAL_NO_DECIMATOR=1248 verified by manual summation of all individual weights. BOON_WEIGHT_DEITY_PASS_ALL=40 verified (28+10+2=40).
- **Activity score EV multiplier NatSpec** (lines 467-470, 476-479): Linear interpolation thresholds (0%->80%, 60%->100%, 255%+->135%) verified against constants and code. Range 8000-13500 bps is correct.
- **_applyEvMultiplierWithCap NatSpec** (lines 503-510): 10 ETH cap per account per level accurately described. Partial cap application logic verified.
- **Redemption lootbox 50/50 split comments**: Not in this module (handled in StakedDegenerusStonk.sol and DegenerusGame.sol). Verified that `resolveRedemptionLootbox` correctly receives the pre-split lootbox amount and uses snapshotted activity score.
- **Reclassification logic**: Not present in this module (accounting-only reclassification is in StakedDegenerusStonk.sol).
- **Boon application logic** (_applyBoon, lines 1382-1550): All boon type branches verified. Upgrade semantics (lootbox-sourced) vs overwrite semantics (deity-sourced) correctly documented. Day tracking for expiry is accurate.
