# Phase 32 Findings: Game Modules Batch A

**Date:** 2026-03-18
**Scope:** MintModule, DegeneretteModule, WhaleModule, BoonModule, LootboxModule, PayoutUtils, MintStreakUtils
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusGameMintModule.sol | 5 | 0 | 5 |
| DegenerusGameDegeneretteModule.sol | X | Y | Z |
| DegenerusGameWhaleModule.sol | 3 | 0 | 3 |
| DegenerusGameBoonModule.sol | 1 | 0 | 1 |
| DegenerusGameLootboxModule.sol | X | Y | Z |
| DegenerusGamePayoutUtils.sol | 0 | 0 | 0 |
| DegenerusGameMintStreakUtils.sol | 0 | 0 | 0 |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*

---

## DegenerusGameMintModule.sol

**Review scope:** 1,193 lines, 47 NatSpec tags, ~208 comment lines, 16 functions.
**Post-Phase-29 changes:** Commit 93708354 (removed last-purchase-day lootbox block: 2 lines removed). No stale comment left behind from this change -- verified independently by searching for references to the removed guard.

### CMT-011: Orphaned NatSpec from removed lootbox resolution function

- **What:** Lines 1140-1146 contain 6 NatSpec lines (`@notice Resolve a lootbox directly (decimator claims) using provided RNG.`, `@dev Access: ContractAddresses.JACKPOTS contract only...`, and 3 `@param` tags) with no corresponding function body. Lines 1147-1192 are empty whitespace. The function was removed but its NatSpec documentation was left behind. A NatSpec parser would associate these tags with the contract's closing brace `}` at line 1193.
- **Where:** DegenerusGameMintModule.sol:1140-1146
- **Why:** A warden reading the source or using a NatSpec extraction tool would see documentation for a `resolveLootboxDirect(address player, uint256 amount, uint256 rngWord)` function that does not exist. This could lead a warden to investigate why the function is documented but absent -- potentially filing a finding about missing functionality or incomplete implementation. The orphaned NatSpec also inflates the perceived API surface of the module.
- **Suggestion:** Delete lines 1140-1146 and the trailing blank lines (1147-1192).
- **Category:** comment-inaccuracy
- **Severity:** INFO

### CMT-012: Missing NatSpec on external function processFutureTicketBatch

- **What:** `processFutureTicketBatch(uint24 lvl)` at line 294 is a significant external function with zero NatSpec -- no `@notice`, `@dev`, `@param`, or `@return` tags. This function processes the ticket activation queue (rolling fractional remainders, generating trait batches via `_raritySymbolBatch`, managing write budgets for gas control) and is callable externally. It returns three values (`worked`, `finished`, `writesUsed`) with no documentation of their meaning.
- **Where:** DegenerusGameMintModule.sol:294-296
- **Why:** A warden auditing the module's external API would encounter a 127-line function with complex gas budgeting, LCG-based trait generation, and packed storage manipulation -- all without any NatSpec guidance. The return values `worked`, `finished`, and `writesUsed` have non-obvious semantics (e.g., `worked` means "at least one write was performed," `finished` means "queue fully processed," `writesUsed` is the gas budget consumed). Without NatSpec, a warden must reverse-engineer the function's purpose, access control expectations, and return value semantics from code alone.
- **Suggestion:** Add NatSpec documenting: the function's purpose (process ticket activation queue for a given level), access control (called via delegatecall from DegenerusGame during `advanceGame`), parameters (`@param lvl` -- the level whose ticket queue to process), return values (`@return worked` -- true if any queue entries were processed, `@return finished` -- true if the entire queue was drained, `@return writesUsed` -- storage writes consumed from the gas budget), and the write budget system (`@dev` explaining WRITES_BUDGET_SAFE and cold storage scaling).
- **Category:** comment-inaccuracy
- **Severity:** LOW

### CMT-013: purchaseCoin NatSpec claims RNG gating that does not exist

- **What:** The `@dev` NatSpec for `purchaseCoin` at line 564 states `BURNIE ticket and loot box purchases are allowed whenever RNG is unlocked`, implying that RNG lock status gates the function. However, neither the module's `_purchaseCoinFor` implementation nor the `DegenerusGame.purchaseCoin` entry point checks `rngLockedFlag`. The only gating conditions are `gameOver` (line 592) and the time-based `CoinPurchaseCutoff` for tickets (line 598). BURNIE purchases proceed regardless of VRF state.
- **Where:** DegenerusGameMintModule.sol:564
- **Why:** A warden reading this NatSpec would assume BURNIE purchases revert when a VRF request is pending (`rngLockedFlag == true`). This would affect their analysis of the purchase flow's availability during jackpot resolution, potentially leading to incorrect conclusions about player optionality during VRF-locked periods. The warden might also file a finding about inconsistent RNG gating if they observe that ETH `purchase()` similarly lacks an RNG check despite its own "Blocked when RNG is locked" NatSpec (DegenerusGame.sol:537).
- **Suggestion:** Remove the RNG reference. Change to: `@dev BURNIE ticket purchases are gated by a time-based cutoff before the liveness guard fires. Loot box purchases require lootboxRngIndex > 0.`
- **Category:** comment-inaccuracy
- **Severity:** INFO

### CMT-014: recordMintData NatSpec lists "milestones" as tracked field but no milestone logic exists

- **What:** The `recordMintData` function's `@dev` NatSpec at line 169 states `mintPacked_[player] updated with level count, whale bonuses, milestones`. The function updates level count (levelCount field in mintPacked_) and interacts with whale bundle frozen state (clearing frozenUntilLevel and whaleBundleType when the player reaches the frozen level). However, there is no "milestones" concept in this function -- no milestone tracking, no milestone field in mintPacked_, and the word "milestones" appears nowhere else in the contract.
- **Where:** DegenerusGameMintModule.sol:169
- **Why:** A warden reading the NatSpec would look for milestone-related logic in the function and, not finding any, might suspect that milestone tracking was removed without updating the documentation -- potentially filing a finding about missing functionality. The NatSpec inflates the perceived scope of `recordMintData` beyond its actual behavior.
- **Suggestion:** Change line 169 to: `- \`mintPacked_[player]\` updated with level count, level units, and whale bundle frozen state`
- **Category:** comment-inaccuracy
- **Severity:** INFO

### CMT-015: Inline comment claims "+10pp" affiliate bonus but code applies variable multiplier

- **What:** Line 942 states `// Final jackpot day affiliate bonus: +10pp on fresh ETH`. The code at lines 947-949 applies two different multipliers depending on the target level: `(freshBurnie * 7) / 5` for `targetLevel <= 3` (a 40% increase) and `(freshBurnie * 3) / 2` for `targetLevel > 3` (a 50% increase). The "+10pp" (percentage points) characterization is incorrect for both cases -- the actual effect depends on the affiliate rate. For a 20% affiliate rate, the 50% multiplier yields +10pp (to 30%), but the 40% multiplier yields only +8pp (to 28%). For any other affiliate rate, neither multiplier produces a +10pp increase.
- **Where:** DegenerusGameMintModule.sol:942
- **Why:** A warden analyzing the affiliate reward system would use "+10pp" as a fixed quantity when calculating effective affiliate rates during jackpot resolution. The actual bonus is a 40% or 50% multiplier on the BURNIE base (not a fixed percentage point increase), and the two tiers are not distinguished in the comment. A warden would miss both the tier split at targetLevel 3 and the rate-dependent nature of the actual percentage point increase.
- **Suggestion:** Change to: `// Final jackpot day affiliate bonus: 50% boost on fresh ETH BURNIE base (40% for levels 0-3)`
- **Category:** comment-inaccuracy
- **Severity:** INFO

---

**DegenerusGameMintModule.sol review complete.** 47 NatSpec tags verified, ~208 comment lines reviewed, all inline comments cross-referenced against current code behavior. 5 findings total: all comment-inaccuracy (CMT), 0 intent-drift (DRIFT). Post-Phase-29 commit 93708354 (removed last-purchase-day lootbox block) verified clean -- no stale comments left behind. The 2 pre-identified issues (orphaned NatSpec at lines 1140-1146, missing NatSpec on processFutureTicketBatch at line 294) confirmed and flagged. 3 additional findings discovered: false RNG gating claim in purchaseCoin NatSpec, phantom "milestones" in recordMintData NatSpec, and misleading "+10pp" affiliate bonus description. No intent drift detected -- no vestigial guards from removed features, no unnecessary restrictions accumulated.

---

## DegenerusGameWhaleModule.sol

**Review scope:** 840 lines, 85 NatSpec tags, ~179 comment lines, 14 functions.
**Post-Phase-29 changes:** Two commits modified this contract:
- **3542e227:** Blocked lazy pass at x99 levels, updated `purchaseLazyPass` `@dev` and `@custom:reverts` NatSpec (added x99 exclusion), added `currentLevel % 100 == 99` guard.
- **9aff84b2:** Limited boon discount to first bundle only, gated x99 single whale buys, updated pricing inline comment ("boon discount applies to first bundle only"), updated `@dev` for `WHALE_BUNDLE_STANDARD_PRICE` from "x49/x99 levels" to "levels 4+".

### CMT-016: Misleading "Tickets always start at x1" in purchaseWhaleBundle NatSpec

- **What:** The `purchaseWhaleBundle` `@dev` NatSpec at line 166 states `Tickets always start at x1`. The code at line 211 sets `ticketStartLevel = passLevel` where `passLevel = level + 1` (line 192). Tickets start at whatever `level + 1` evaluates to, which could be any value -- e.g., if the game is at level 5, tickets start at level 6, not at a level "ending in 1." The "x1" notation in the game's nomenclature means a level ending in 1 (e.g., 1, 11, 21, 31), but `level + 1` does not necessarily end in 1.
- **Where:** DegenerusGameWhaleModule.sol:166
- **Why:** A warden reading this NatSpec would believe whale bundles always align ticket ranges to x01 boundaries (1, 101, 201...), which would simplify their analysis of ticket distribution and century bonus interactions. The actual behavior is that tickets start at the very next level, creating potentially unaligned 100-level ranges. This matters for understanding how overlapping whale bundles interact, how century bonuses at x00 levels intersect with whale ticket ranges, and whether ticket queuing at non-boundary levels creates edge cases.
- **Suggestion:** Change to: `Tickets start at the next level (level + 1) and cover 100 consecutive levels.`
- **Category:** comment-inaccuracy
- **Severity:** INFO

### CMT-017: purchaseWhaleBundle NatSpec omits "first bundle only" boon discount restriction

- **What:** The `purchaseWhaleBundle` `@dev` NatSpec at line 172 states `10/25/50% off standard with boon` without specifying that the boon discount applies only to the first bundle in a multi-bundle purchase. Commit 9aff84b2 changed the pricing logic so that `totalPrice = discountedPrice + WHALE_BUNDLE_STANDARD_PRICE * (quantity - 1)` (line 235) -- only the first bundle gets the discounted price, subsequent bundles are at the full 4 ETH standard price. The inline comment at line 226 was correctly updated to say `boon discount applies to first bundle only`, but the function-level NatSpec at line 172 was not updated.
- **Where:** DegenerusGameWhaleModule.sol:172
- **Why:** A warden reading the function NatSpec (the primary documentation surface) would calculate whale bundle costs by applying the boon discount to ALL bundles. For example, with a 50% discount and quantity=3, the NatSpec implies a cost of 3 x 2 ETH = 6 ETH, but the actual cost is 2 + 4 + 4 = 10 ETH. This 67% discrepancy in price modeling would affect warden analysis of whale bundle economics, potential arbitrage via multi-bundle purchases with boons, and whale pool fund flow calculations. Additionally, the `@custom:reverts` tags at lines 179-181 do not include the x99 minimum-2-bundles revert condition added by the same commit (line 238: `if (passLevel % 100 == 0 && quantity < 2) revert E()`).
- **Suggestion:** Update line 172 to: `Price: 2.4 ETH at levels 0-3, 4 ETH at levels 4+, 10/25/50% off standard on first bundle with boon (remaining bundles at standard price). At x99 levels without boon, minimum 2 bundles.` Add `@custom:reverts E When at x99 level without boon and quantity < 2.`
- **Category:** comment-inaccuracy
- **Severity:** LOW

### CMT-018: purchaseWhaleBundle @param quantity range inaccurate at x99 levels

- **What:** The `@param` NatSpec for `quantity` at line 178 states `Number of bundles to purchase (1-100)`. After commit 9aff84b2, when `passLevel % 100 == 0` (game at x99 levels) and no boon is active, `quantity < 2` causes a revert (line 238). This means the valid range at x99 levels without boon is 2-100, not 1-100. The `@param` documents the general case but does not account for this level-dependent constraint.
- **Where:** DegenerusGameWhaleModule.sol:178
- **Why:** A warden or integrator reading the function signature and `@param` documentation would attempt single-bundle purchases at x99 levels and encounter an unexplained `E()` revert. The x99 restriction is documented only in the inline comment at line 237 (`// x99 levels: minimum 2 bundles (8 ETH) to deter fresh-account century bonus farming`) but is absent from the NatSpec that defines the `quantity` parameter's valid range.
- **Suggestion:** Change to: `@param quantity Number of bundles to purchase (1-100; minimum 2 at x99 levels without boon).`
- **Category:** comment-inaccuracy
- **Severity:** INFO

---

**DegenerusGameWhaleModule.sol review complete.** 85 NatSpec tags verified, ~179 comment lines reviewed, all inline comments cross-referenced against current code behavior. 3 findings total: all comment-inaccuracy (CMT), 0 intent-drift (DRIFT). Both post-Phase-29 commits (3542e227 and 9aff84b2) specifically reviewed: commit 3542e227 correctly updated the `purchaseLazyPass` NatSpec (x99 exclusion), but commit 9aff84b2 only updated the `WHALE_BUNDLE_STANDARD_PRICE` `@dev` and the pricing inline comment -- the function-level NatSpec for `purchaseWhaleBundle` was left stale regarding boon discount scope and x99 minimum quantity. The pre-identified misleading "x1" NatSpec at line 166 confirmed and flagged. 2 additional findings discovered from the 9aff84b2 NatSpec gap. No intent drift detected -- all post-Phase-29 guards serve their intended purpose (x99 lazy pass block, x99 whale minimum, boon discount limit), and no vestigial restrictions were identified.

---

## DegenerusGamePayoutUtils.sol

**Review scope:** 94 lines, 7 NatSpec tags, ~8 comment lines, 3 functions.
**Post-Phase-29 changes:** None.

**0 findings.** All NatSpec verified accurate. All inline comments verified. No intent drift detected.

- Contract-level `@dev` ("Shared payout helpers for jackpot-related modules") accurately describes the 3 helper functions: `_creditClaimable`, `_calcAutoRebuy`, and `_queueWhalePassClaimCore`.
- `PlayerCredited` event NatSpec (4 tags: `@notice`, `@param player`, `@param recipient`, `@param amount`) accurately describes the event semantics. The separate `player`/`recipient` parameters are architecturally correct even though current usage always passes the same address for both.
- `HALF_WHALE_PASS_PRICE` `@dev` ("Half whale pass price unit (each half-pass = 1 ticket/level for 100 levels)") verified against `_queueWhalePassClaimCore` which divides payouts by 2.25 ETH to compute half-pass units, and against `DegenerusGameEndgameModule.claimWhalePass` which awards N tickets/level for 100 levels where N = accumulated half-passes.
- `_queueWhalePassClaimCore` `@dev` ("Queue deferred whale pass claims for large payouts") accurately describes the function's behavior: divides payout by HALF_WHALE_PASS_PRICE, accumulates `whalePassClaims`, and credits remainder to `claimableWinnings`.
- `_calcAutoRebuy` (37 lines, 7 parameters, struct return) has no NatSpec but is an internal pure function. Missing NatSpec on internal helpers is a style concern, not a warden-readability finding. The function is only called from within the inheritance chain.
- No intent drift: all payout helpers serve their current purpose. No vestigial guards, no stale cross-module references, no unnecessary restrictions.

---

## DegenerusGameMintStreakUtils.sol

**Review scope:** 62 lines, 5 NatSpec tags, ~6 comment lines, 2 functions.
**Post-Phase-29 changes:** None.

**0 findings.** All NatSpec verified accurate. All inline comments verified. No intent drift detected.

- Contract-level `@dev` ("Shared mint streak helpers (credits on completed 1x price ETH quest)") verified against `DegenerusGame.recordMintQuestStreak` which calls `_recordMintStreakForLevel` after a 1x price ETH quest completion. The parenthetical accurately describes the trigger condition.
- `MINT_STREAK_LAST_COMPLETED_SHIFT` `@dev` ("Packed mint data field storing last level credited for mint streak (24 bits)") verified: shift value 160, 24-bit mask applied at lines 20-22 and 42-44. Bit position and width are accurate.
- `MINT_STREAK_FIELDS_MASK` `@dev` ("Mask for clearing last-completed + streak fields in one pass") verified: combines two 24-bit masks at shifts 160 and `LEVEL_STREAK_SHIFT`, used at line 42 via `~MINT_STREAK_FIELDS_MASK` to clear both fields before writing new values.
- `_recordMintStreakForLevel` `@dev` ("Record a mint streak completion for a given level (idempotent per level)") verified: early return at line 23 when `lastCompleted == mintLevel` ensures idempotency. Streak increments on consecutive levels (line 26: `lastCompleted + 1 == mintLevel`), resets to 1 otherwise (line 39). Overflow protection at line 31 (`streak < type(uint24).max`).
- `_mintStreakEffective` `@dev` ("Effective mint streak (resets if a level was missed)") verified: returns 0 when `currentMintLevel > lastCompleted + 1` (line 57), indicating a missed level. Otherwise returns stored streak value.
- No intent drift: streak mechanics are self-contained. No references to removed features, no stale thresholds, no vestigial logic.

---

## DegenerusGameBoonModule.sol

**Review scope:** 359 lines, 18 NatSpec tags, ~35 comment lines, 5 functions.
**Post-Phase-29 changes:** None.

### CMT-019: Contract @notice claims "lootbox view functions" but none exist

- **What:** The contract-level `@notice` at line 12 states `Delegatecall module for boon consumption and lootbox view functions.` The contract contains 5 functions: `consumeCoinflipBoon`, `consumePurchaseBoost`, `consumeDecimatorBoost`, `checkAndClearExpiredBoon`, and `consumeActivityBoon`. None of these are lootbox view functions. The `@dev` at line 15 explains the contract was "Split from DegenerusGameLootboxModule to stay under EIP-170 size limit," suggesting the "lootbox view functions" were part of the original split but have since been moved back to LootboxModule or removed.
- **Where:** DegenerusGameBoonModule.sol:12
- **Why:** A warden reading the contract's title block would expect to find lootbox view functions (e.g., `getLootboxContents`, `getLootboxRewardHistory`) within this module. Not finding any, the warden might investigate whether the module is incomplete, whether views were accidentally omitted during the split, or whether the module serves a different purpose than documented. The stale `@notice` misrepresents the contract's actual API surface: it is purely a boon consumption and maintenance module with no lootbox view capabilities.
- **Suggestion:** Change line 12 to: `@notice Delegatecall module for boon consumption and expiry management.`
- **Category:** comment-inaccuracy
- **Severity:** INFO

---

**DegenerusGameBoonModule.sol review complete.** 18 NatSpec tags verified, ~35 comment lines reviewed, all inline comments cross-referenced against current code behavior. 1 finding (CMT-019: stale "lootbox view functions" in contract @notice). 0 intent-drift findings.

**Consume function pattern-check:** All 3 standard consume functions (`consumeCoinflipBoon`, `consumePurchaseBoost`, `consumeDecimatorBoost`) follow the same structural pattern: null check, deity day check, expiry day check (where applicable), return BPS, clear state. The @return NatSpec BPS values for each were verified against the constants in LootboxModule that set them: coinflip (500/1000/2500), purchase (500/1500/2500), decimator (1000/2500/5000). `consumeDecimatorBoost` intentionally omits the stamp-day expiry check -- confirmed that no `decimatorBoostDay` storage variable exists, so the decimator boost has deity-day-only expiry by design.

**checkAndClearExpiredBoon pattern-check:** All 10 boon-clearing blocks verified for structural consistency. Each block correctly checks its deity day condition and (where applicable) stamp day + expiry constant. Lazy pass boon uses a hardcoded `4` instead of a named constant for its expiry period (line 247) but no NatSpec claims otherwise. Deity pass boon uses `currentDay > deityDay` comparison (allows boon for full deity day) instead of `deityDay != currentDay` (expires at end of day) -- this is intentionally different from the other boon types and matches the deity pass's multi-day expiry model. Return value checks all 10 boon types. Activity boon reuses `COINFLIP_BOON_EXPIRY_DAYS` (2 days) for its expiry -- consistent between `consumeActivityBoon` and the expiry check block.

**consumeActivityBoon:** NatSpec accurately describes the function as consuming a pending activity boon and applying it to player stats. Verified it adds pending level count to `mintPacked_` via BitPackingLib and calls `quests.awardQuestStreakBonus`. The `@dev` correctly states it is called via nested delegatecall from LootboxModule (confirmed via grep: LootboxModule.sol:947).
