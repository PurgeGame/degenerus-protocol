# Phase 40 Findings: Token Contracts

**Date:** 2026-03-19
**Scope:** BurnieCoin.sol, DegenerusStonk.sol, StakedDegenerusStonk.sol, WrappedWrappedXRP.sol (4 contracts, 2,153 lines)
**Pass:** v3.2 re-scan (post-v3.1 fix verification + fresh independent scan)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT new | Fix verified | Fix partial | Fix not applied | Total findings |
|----------|---------|-------------|-------------|-----------------|----------------|
| BurnieCoin.sol | 2 | 13 | 0 | 0 | 15 |
| DegenerusStonk.sol | - | - | - | - | - |
| StakedDegenerusStonk.sol | - | - | - | - | - |
| WrappedWrappedXRP.sol | - | - | - | - | - |
| **Total** | **2** | **13** | **0** | **0** | **15** |

---

## BurnieCoin.sol

**Review scope:** 1,024 lines, 229 NatSpec tags, 29 external/public functions, 6 private/internal functions.
**Post-v3.1 changes:** v3.1 comment fixes applied (uncommitted); rngLocked removal from _claimCoinflipShortfall and _consumeCoinflipShortfall.

### Pass 1: Fix Verification (13 v3.1 findings)

| ID | Description | Status | Evidence |
|----|-------------|--------|----------|
| CMT-041 | Orphaned DATA TYPES section (lines 213-227) | **FIXED** | Entire section removed. Lines 212-222 are now "WIRED CONTRACTS & MODULE STATE" header. No DATA TYPES header or orphaned struct NatSpec remains. |
| CMT-042 | Orphaned BOUNTY STATE section (lines 333-361) | **FIXED** | Entire section removed. No bounty, currentBounty, biggestFlipEver, or bountyOwedTo references remain (`grep` confirmed). |
| CMT-043 | onlyAdmin reverts OnlyGame() without acknowledgment | **FIXED** | Line 622: `revert OnlyGame(); // reuses OnlyGame() to save deploy gas` -- comment now present, consistent with coinflip guard pattern. |
| CMT-044 | burnCoin @dev says "DegenerusGame, game, or affiliate" | **FIXED** | Line 813: `@dev Access: game or affiliate (onlyTrustedContracts).` -- non-redundant, matches 2-caller modifier. |
| CMT-045 | EVENTS header references coinflip/bounty | **FIXED** | Lines 43-44: "Standard ERC20 events plus gameplay signals for off-chain indexers/clients." No coinflip or bounty references. |
| CMT-046 | onlyDegenerusGameContract lists processCoinflipPayouts | **FIXED** | Line 584: `Used for: rollDailyQuest.` -- processCoinflipPayouts removed. Only rollDailyQuest uses this modifier (confirmed). |
| CMT-047 | onlyFlipCreditors "Used for" omits creditCoin | **FIXED** | Lines 601-602: `Used for: creditCoin, creditFlip, creditFlipBatch.` -- all 3 users listed. |
| CMT-048 | Contract @dev SECURITY omits onlyTrustedContracts | **FIXED** | Line 21: `Access control: onlyDegenerusGameContract, onlyFlipCreditors, onlyTrustedContracts, onlyVault, onlyAdmin` -- all 5 modifiers listed. |
| CMT-049 | Section name "QUEST INTEGRATION HELPERS" misleading | **FIXED** | Line 972: Section now named "DECIMATOR & QUEST INTEGRATION HELPERS" -- accurately reflects the 3 functions (_adjustDecimatorBucket, _decimatorBurnMultiplier, _questApplyReward). |
| CMT-050 | vaultEscrow @dev omits VAULT as caller | **FIXED** | Line 635: `Called by game contract or vault to credit virtual BURNIE to the vault.` -- matches inline access check (GAME, VAULT). |
| CMT-051 | _burn @dev references depositCoinflip | **FIXED** | Line 442: `SECURITY: Burns BEFORE any external calls (CEI pattern) in burnCoin/decimatorBurn.` -- depositCoinflip removed. See CMT-059 for incomplete caller list. |
| CMT-052 | _claimCoinflipShortfall and _consumeCoinflipShortfall have no NatSpec | **FIXED** | Lines 536-537: _claimCoinflipShortfall now has @dev describing auto-claim behavior and RNG lock delegation. Lines 550-552: _consumeCoinflipShortfall now has @dev describing auto-consume behavior with consumed return value. |
| CMT-053 | _mint and _burn @dev omit vault path | **FIXED** | Line 421: _mint @dev now says "Vault path: adds to vaultAllowance instead of totalSupply (virtual escrow)." Line 441: _burn @dev now says "Vault path: reduces vaultAllowance instead of totalSupply." Both accurately describe the branching behavior. |

**Fix verification result: 13/13 FIXED.** All v3.1 findings for BurnieCoin have been correctly addressed.

### rngLocked Removal Verification

**Code change:** The `if (degenerusGame.rngLocked()) return;` guard was removed from both `_claimCoinflipShortfall` (previously ~line 582) and `_consumeCoinflipShortfall` (previously ~line 595). BurnieCoin no longer checks rngLocked in these shortfall paths.

**New NatSpec accuracy:**
- Line 537: `_claimCoinflipShortfall` @dev says "BurnieCoinflip blocks auto-rebuy players during RNG lock." -- This accurately describes the new architecture: BurnieCoinflip.sol now internally handles the RNG lock check when claimCoinflipsFromBurnie is called.
- Line 552: `_consumeCoinflipShortfall` @dev says "BurnieCoinflip blocks auto-rebuy players during RNG lock." -- Same pattern, accurate.

**Remaining rngLocked references in BurnieCoin.sol:**
- Line 273: `if (degenerusGame.rngLocked()) return spendable;` -- This is **code**, not a stale comment. The `balanceOfWithClaimable` view function still correctly checks rngLocked to determine whether to include claimable amounts in the spendable total. This is intentional and accurate.
- Line 264: `@dev If RNG is locked, claimable coinflips cannot be pulled, so only returns on-chain balance.` -- Accurately describes the view function behavior at line 273.

**Verdict: rngLocked removal is cleanly documented.** No stale rngLocked references remain in comments. The only rngLocked reference is in the view function where it is still actively used in code.

### Pass 2: Fresh Independent Scan

All 29 external/public functions verified systematically. Section headers verified against code organization. Modifier NatSpec cross-referenced against actual callers.

**Functions verified (no issues):**
- `claimableCoin()` -- @dev/@return match proxy to BurnieCoinflip
- `balanceOfWithClaimable(address)` -- @dev/@param/@return all accurate, vault path documented, rngLocked check documented
- `previewClaimCoinflips(address)` -- @dev/@param/@return match proxy behavior
- `coinflipAutoRebuyInfo(address)` -- @return named values match BurnieCoinflip proxy return
- `totalSupply()` -- simple view, accurate
- `supplyIncUncirculated()` -- @dev accurately describes sum
- `vaultMintAllowance()` -- @dev matches behavior
- `decimals` -- constant, accurate
- `approve(address, uint256)` -- standard ERC20, NatSpec accurate
- `transfer(address, uint256)` -- NatSpec accurate, shortfall auto-claim documented
- `transferFrom(address, address, uint256)` -- game bypass documented, infinite approval documented
- `_transfer(address, address, uint256)` -- vault redirect path documented, zero-address check documented
- `_mint(address, uint256)` -- vault path now documented (CMT-053 fix), non-vault path accurate
- `_burn(address, uint256)` -- vault path now documented (CMT-053 fix), CEI note present (see CMT-059)
- `burnForCoinflip(address, uint256)` -- access check and error reuse documented
- `mintForCoinflip(address, uint256)` -- same pattern as burnForCoinflip, accurate
- `mintForGame(address, uint256)` -- @dev matches GAME-only access
- `creditCoin(address, uint256)` -- @dev matches onlyFlipCreditors, mint behavior accurate
- `creditFlip(address, uint256)` -- @dev matches proxy to BurnieCoinflip
- `creditFlipBatch(address[3], uint256[3])` -- @dev matches batch proxy
- `creditLinkReward(address, uint256)` -- @dev accurately describes ADMIN access, flip stake credit, LINK context
- `vaultEscrow(uint256)` -- @dev now accurate (CMT-050 fix)
- `vaultMintTo(address, uint256)` -- @dev matches onlyVault, allowance reduction, mint behavior
- `affiliateQuestReward(address, uint256)` -- @dev matches AFFILIATE-only access, quest routing
- `rollDailyQuest(uint48, uint256)` -- @dev matches GAME-only access via onlyDegenerusGameContract
- `notifyQuestMint(address, uint32, bool)` -- @dev matches GAME access, quest routing, mint streak callback
- `notifyQuestLootBox(address, uint256)` -- @dev matches GAME access, quest routing
- `notifyQuestDegenerette(address, uint256, bool)` -- @dev matches GAME access, quest routing
- `burnCoin(address, uint256)` -- @dev now accurate (CMT-044 fix)
- `decimatorBurn(address, uint256)` -- @dev/@param/@custom:reverts all verified
- `terminalDecimatorBurn(address, uint256)` -- @dev/@param all verified
- `coinflipAmount(address)` -- @dev matches proxy to BurnieCoinflip

**Private/internal helpers verified:**
- `_toUint128(uint256)` -- no NatSpec but behavior is self-evident (single-line utility)
- `_claimCoinflipShortfall(address, uint256)` -- NatSpec now present (CMT-052 fix)
- `_consumeCoinflipShortfall(address, uint256)` -- NatSpec now present (CMT-052 fix)
- `_adjustDecimatorBucket(uint256, uint8)` -- @dev accurately describes activity-based bucket adjustment
- `_decimatorBurnMultiplier(uint256)` -- @dev matches formula (base + bonus/3)
- `_questApplyReward(address, uint256, uint8, uint32, bool)` -- @dev/@param/@return all accurate

**Section headers verified:**
- EVENTS -- matches actual events (Transfer, Approval, DecimatorBurn, TerminalDecimatorBurn, DailyQuestRolled, QuestCompleted, LinkCreditRecorded, VaultEscrowRecorded, VaultAllowanceSpent)
- ERRORS -- all 11 errors listed with accurate NatSpec
- ERC20 STATE -- storage layout matches (slot 0: _supply, slot 1: balanceOf, slot 2: allowance)
- WIRED CONTRACTS & MODULE STATE -- accurately lists compile-time constant references
- VAULT ESCROW -- describes virtual mint allowance pattern
- VIEW HELPERS -- groups view functions correctly
- ERC20 DECIMALS -- trivial, accurate
- ERC20 FUNCTIONS -- game bypass documented
- COINFLIP CONTRACT INTEGRATION -- groups coinflip permission functions
- MODIFIERS -- hierarchy table matches actual 5 modifiers and their callers
- VAULT ESCROW FUNCTIONS -- groups vaultEscrow/vaultMintTo
- QUEST INTEGRATION -- describes hub routing pattern
- DECIMATOR -- describes burn-to-participate pattern
- TERMINAL DECIMATOR (DEATH BET) -- describes always-open death bet
- COINFLIP VIEW FUNCTIONS -- groups read-only coinflip queries
- DECIMATOR & QUEST INTEGRATION HELPERS -- groups 3 private helpers (CMT-049 fix)

### New Findings

#### CMT-059: _burn @dev CEI note lists only 2 of 4 callers

- **What:** The `_burn` function's `@dev` at line 442 says `SECURITY: Burns BEFORE any external calls (CEI pattern) in burnCoin/decimatorBurn.` The `_burn` function is called from 4 sites: `burnForCoinflip` (line 476), `burnCoin` (line 823), `decimatorBurn` (line 857), and `terminalDecimatorBurn` (line 947). The CEI note lists only 2 of 4 callers. While `burnForCoinflip` is a simple pass-through with no external calls after _burn (so CEI is trivially satisfied), `terminalDecimatorBurn` does call `degenerusGame.recordTerminalDecBurn` after `_burn` -- making it a relevant CEI site that should be documented.
- **Where:** BurnieCoin.sol:442
- **Why:** The v3.1 fix (CMT-051) replaced "depositCoinflip" with "burnCoin" but did not add the other two callers. A warden verifying the CEI claim would trace _burn call sites and find undocumented ones -- potentially leading to a QA finding about incomplete security annotations.
- **Suggestion:** Change to: `SECURITY: Burns BEFORE any external calls (CEI pattern) in burnForCoinflip/burnCoin/decimatorBurn/terminalDecimatorBurn.`
- **Category:** comment-inaccuracy
- **Severity:** INFO

#### CMT-060: VaultAllowanceSpent event @notice inaccurate for vaultMintTo emission path

- **What:** The `VaultAllowanceSpent` event NatSpec at line 105 says `@notice Emitted when the vault spends from its mint allowance without minting tokens.` and `@param spender The vault address.` The event is emitted in two locations: (1) `_burn` at line 454 as `VaultAllowanceSpent(from, amount)` where from=VAULT and no tokens are minted -- NatSpec accurate for this path. (2) `vaultMintTo` at line 664 as `VaultAllowanceSpent(address(this), amount)` -- here tokens ARE minted (line 665 emits Transfer from address(0)), and `address(this)` is BurnieCoin, not the vault. The @notice "without minting tokens" is false for path (2), and @param "The vault address" is false for path (2) since spender is BurnieCoin's own address.
- **Where:** BurnieCoin.sol:105-108
- **Why:** A warden or event indexer consuming VaultAllowanceSpent events would see inconsistent spender values (sometimes VAULT, sometimes BurnieCoin) and the "without minting tokens" claim contradicts the vaultMintTo path where tokens are actively minted. This creates confusion about the event's semantics -- does it mean "allowance reduced" (the common factor) or "allowance spent without minting" (only true for _burn)?
- **Suggestion:** Change @notice to: `Emitted when the vault's mint allowance is reduced.` Change @param spender to: `The address that triggered the allowance reduction (VAULT for burns, BurnieCoin for vaultMintTo).`
- **Category:** comment-inaccuracy
- **Severity:** INFO

---

**BurnieCoin.sol review complete.** 229 NatSpec tags verified, 29 external/public functions scanned, 6 private/internal functions scanned, 16 section headers verified. 13 v3.1 findings all FIXED. rngLocked removal cleanly documented. 2 new findings (CMT-059, CMT-060) identified. No remaining pre-coinflip-split references found.

---

## DegenerusStonk.sol

*Pending Task 2*

## StakedDegenerusStonk.sol

*Pending Task 2*

## WrappedWrappedXRP.sol

*Pending Task 2*
