# v3.5 Comment Correctness Findings: Peripheral Contracts, Remaining Interfaces, and Libraries

**Date:** 2026-03-22
**Milestone:** v3.5
**Scope:** ~21 files, ~5,028 lines (peripheral contracts, remaining interfaces, all libraries)
**Pass:** v3.5 comment correctness (Plan 54-06)
**Mode:** Flag-only -- no code changes

---

## Executive Summary

| Metric | Count |
|--------|-------|
| **New findings** | **3** |
| By severity: LOW | 0 |
| By severity: INFO | 3 |
| **Prior findings re-verified** | **10** |
| Prior findings now FIXED | 10 |
| Prior findings still open | 0 |

All 10 v3.2 "accept as known" findings assigned to this batch have been resolved. The code has been updated with accurate NatSpec across all 10 items.

---

## Summary Table

| Contract | New Findings | Prior Findings Verified | Total |
|----------|-------------|------------------------|-------|
| **Core Peripherals** | | | |
| DegenerusQuests.sol | 1 | 0 | 1 |
| DegenerusJackpots.sol | 0 | 1 (CMT-104) | 0 |
| DegenerusAffiliate.sol | 1 | 0 | 1 |
| DegenerusDeityPass.sol | 0 | 0 | 0 |
| DeityBoonViewer.sol | 1 | 0 | 1 |
| **Small Contracts** | | | |
| WrappedWrappedXRP.sol | 0 | 3 (CMT-057, CMT-058, CMT-061) | 0 |
| Icons32Data.sol | 0 | 0 | 0 |
| DegenerusTraitUtils.sol | 0 | 0 | 0 |
| ContractAddresses.sol | 0 | 1 (CMT-079) | 0 |
| **Interfaces** | | | |
| IDegenerusGame.sol | 0 | 5 (CMT-205, CMT-206, CMT-207, CMT-208, CMT-209) | 0 |
| IDegenerusQuests.sol | 0 | 0 | 0 |
| IDegenerusAffiliate.sol | 0 | 0 | 0 |
| IDegenerusCoin.sol | 0 | 0 | 0 |
| IStETH.sol | 0 | 0 | 0 |
| IDegenerusJackpots.sol | 0 | 0 | 0 |
| IVRFCoordinator.sol | 0 | 0 | 0 |
| DegenerusGameModuleInterfaces.sol | 0 | 0 | 0 |
| IVaultCoin.sol | 0 | 0 | 0 |
| **Libraries** | | | |
| JackpotBucketLib.sol | 0 | 0 | 0 |
| BitPackingLib.sol | 0 | 0 | 0 |
| PriceLookupLib.sol | 0 | 0 | 0 |
| GameTimeLib.sol | 0 | 0 | 0 |
| EntropyLib.sol | 0 | 0 | 0 |
| **Total** | **3** | **10** | **3** |

---

## Prior Finding Verification (10 items)

All 10 v3.2 findings assigned to this batch have been verified as FIXED.

| v3.2 ID | Contract | Issue | Status | Notes |
|---------|----------|-------|--------|-------|
| CMT-208 | IDegenerusGame.sol | 3 terminal dec functions lack NatSpec | **FIXED** | Lines 207-233 now have complete `@notice`, `@param`, `@return` tags for `recordTerminalDecBurn`, `runTerminalDecimatorJackpot`, `terminalDecWindow` |
| CMT-209 | IDegenerusGame.sol | 4 Degenerette view functions lack NatSpec | **FIXED** | Lines 451-458 now have `@notice` tags on `getDailyHeroWager`, `getDailyHeroWinner`, `getPlayerDegeneretteWager`, `getTopDegenerette` |
| CMT-205 | IDegenerusGame.sol | decClaimable "or expired" stale | **FIXED** | Line 259: `@return amountWei Claimable amount (0 if not winner or already claimed).` -- no "expired" reference |
| CMT-206 | IDegenerusGame.sol | Duplicate stale @notice on resolveDegeneretteBets | **FIXED** | Line 339: `@notice Resolve Degenerette bets once RNG is available.` -- distinct and accurate |
| CMT-207 | IDegenerusGame.sol | Phantom useBoon parameter | **FIXED** | Lines 397-400: clean `@notice Purchase a deity pass for a specific symbol (0-31).` with correct `@param buyer` and `@param symbolId`. No "Two modes" text, no phantom `useBoon` |
| CMT-057 | WrappedWrappedXRP.sol | Section header at line 279 "Wrapping is disabled" | **FIXED** | Line 279: `Wrap not implemented; unwrap/donate are enabled.` -- accurate |
| CMT-058 | WrappedWrappedXRP.sol | VaultAllowanceSpent @param says "vault address" | **FIXED** | Line 74: `@param spender The contract spending from allowance (address(this))` -- correctly describes address(this), not vault |
| CMT-061 | WrappedWrappedXRP.sol | EVENTS header references "wrap" | **FIXED** | Line 44: `Standard ERC20 events plus unwrap/donate tracking` -- no wrap reference |
| CMT-079 | ContractAddresses.sol | "zeroed in source" comment still present | **FIXED** | Lines 4-5: now say `// Compile-time constants populated by the deploy script.` and `// The deploy pipeline predicts addresses and patches this file before compilation.` -- accurate |
| CMT-104 | DegenerusJackpots.sol | OnlyCoin says "coinflip contract" | **FIXED** | Line 47: `@notice Thrown when a function restricted to COIN or COINFLIP is called by another address.` -- matches modifier logic at line 150 |

---

## Core Peripherals

### DegenerusQuests.sol (~1,598 lines)

**Review scope:** Full NatSpec and inline comment audit. 1 prior v3.1 finding (none assigned to this batch). No modifications since v3.2.

**Prior fix verification:** No prior findings assigned to this file for this batch.

**Fresh scan:**

| ID | Severity | Contract | Line | Summary | Recommendation |
|----|----------|----------|------|---------|----------------|
| CMT-V35-030 | INFO | DegenerusQuests.sol | 54 | `OnlyCoin` error @notice says "COIN or COINFLIP contract" but the `onlyCoin` modifier (line 284-288) does check both COIN and COINFLIP -- however the error name `OnlyCoin` is slightly misleading since it also gates COINFLIP. The error is functionally accurate for the modifier's behavior but the name does not reflect both contracts. | Cosmetic -- rename to `OnlyCoinOrCoinflip` for precision, or accept as minor naming shorthand. No functional impact. |

**Notes:** DegenerusQuests.sol has thorough NatSpec coverage across all public/external functions, events, errors, structs, and constants. The `rollDailyQuest` function has accurate `@param`, `@return`, and `@custom:reverts` documentation. All handler functions (`handleMint`, `handleFlip`, `handleDecimator`, `handleAffiliate`, `handleLootBox`, `handleDegenerette`) have complete and accurate NatSpec including access control documentation. Internal helpers are well-documented with @dev notes explaining streak logic, progress versioning, and anti-exploit mechanisms.

### DegenerusJackpots.sol (~689 lines)

**Review scope:** Full NatSpec and inline comment audit. 1 prior finding (CMT-104).

**Prior fix verification:** CMT-104 FIXED (see table above).

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Clean contract with thorough NatSpec. The BAF prize distribution comment block (lines 191-215) accurately describes the 10/5/5/5/5/45/25 split. All struct fields, state variables, modifiers, and internal helpers have accurate documentation. The `runBafJackpot` function has complete `@param` and `@return` tags matching the signature. `_creditOrRefund`, `_bafScore`, `_score96`, `_updateBafTop`, `_bafTop`, `_clearBafTop` all have accurate `@dev` documentation.

### DegenerusAffiliate.sol (~840 lines)

**Review scope:** Full NatSpec and inline comment audit. No prior findings assigned to this batch.

**Fresh scan:**

| ID | Severity | Contract | Line | Summary | Recommendation |
|----|----------|----------|------|---------|----------------|
| CMT-V35-031 | INFO | DegenerusAffiliate.sol | 52-59 | Duplicate `@title DegenerusAffiliate` block. The contract has two top-level NatSpec blocks: one at line 8-25 (above imports) with full ARCHITECTURE/SECURITY details, and a second at line 52-59 (above `contract` keyword) with a shorter description and INTEGRATION POINTS. Both use `@title DegenerusAffiliate`. The second block is the one the compiler sees; the first is orphaned. | Merge into a single `@title` block above the `contract` keyword, or convert the first block to a file-level comment (remove @title). No functional impact. |

**Notes:** DegenerusAffiliate.sol has comprehensive NatSpec on all external and view functions. The `payAffiliate` function has a detailed REWARD FLOW diagram, reward rates, and lootbox taper documentation that matches the implementation. The 3-tier referral structure (Affiliate -> Upline1 (20%) -> Upline2 (4%)) is accurately documented. Internal helpers `_referrerAddress`, `_setReferralCode`, `_createAffiliateCode`, `_applyLootboxTaper`, `_rollWeightedAffiliateWinner`, `_updateTopAffiliate`, `_score96` all have accurate documentation. Events have complete `@param` tags.

### DegenerusDeityPass.sol (~392 lines)

**Review scope:** Full NatSpec and inline comment audit. No prior findings assigned to this batch.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Soulbound ERC721 with clean NatSpec. The `tokenURI` function (line 129) has accurate `@notice`/`@dev` noting the internal renderer default with optional external renderer fallback. All ERC721 standard functions are present. The soulbound functions (`approve`, `setApprovalForAll`, `transferFrom`, `safeTransferFrom`) correctly revert with `Soulbound()`. `mint` is properly gated to GAME contract. `setRenderer`, `setRenderColors`, `renderColors` all have accurate NatSpec. Private rendering helpers lack NatSpec (acceptable for private pure functions).

### DeityBoonViewer.sol (~171 lines)

**Review scope:** Full NatSpec and inline comment audit. No prior findings assigned to this batch.

**Fresh scan:**

| ID | Severity | Contract | Line | Summary | Recommendation |
|----|----------|----------|------|---------|----------------|
| CMT-V35-032 | INFO | DeityBoonViewer.sol | 79-84 | `deityBoonSlots` @notice says "Compute deity boon slots for a given deity" but does not document that the `game` parameter allows calling against any IDeityBoonDataSource implementation (not just the official DegenerusGame). The function has no access control -- any address can be passed as `game`. This is by design (view-only contract) but the NatSpec could note that `game` must point to a contract implementing `deityBoonData`. | Add `@dev game must implement IDeityBoonDataSource` to clarify the trust boundary. No security impact (pure view contract). |

**Notes:** Clean view-only contract. Boon weight constants and type ID constants are well-organized. The `_boonFromRoll` function uses a cumulative cursor pattern that matches the weight definitions. The final fallback `return DEITY_BOON_ACTIVITY_50` (line 169) is a safe default. All weight constants are documented by name matching their boon type.

---

## Small Contracts

### WrappedWrappedXRP.sol (~389 lines)

**Review scope:** Full NatSpec and inline comment audit. 3 prior findings (CMT-057, CMT-058, CMT-061).

**Prior fix verification:** All 3 FIXED (see table above).

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Comprehensive NatSpec coverage. The top-level `@dev` block (lines 4-26) accurately describes WARNING (joke token), FEATURES (mintPrize/vaultMintTo/wrap disabled), and SECURITY (CEI pattern). All functions have complete `@notice`, `@param`, `@return`, and `@custom:reverts` tags. The `VaultAllowanceSpent` event correctly documents `@param spender` as `address(this)`. The `_transfer` function correctly checks both `from` and `to` against address(0) (line 242). The EVENTS section header at line 44 now accurately says "unwrap/donate tracking".

### Icons32Data.sol (~228 lines)

**Review scope:** Full NatSpec and inline comment audit. No prior findings.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Thorough documentation. The large block comment (lines 6-82) accurately describes icon index layout, design rationale, security considerations, and trust assumptions. The `symbol` function (line 221) correctly documents that quadrant 3 returns empty string. `setPaths` and `setSymbols` have complete `@custom:reverts` tags. All errors have accurate `@notice` tags.

### DegenerusTraitUtils.sol (~183 lines)

**Review scope:** Full NatSpec and inline comment audit. No prior findings.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Pure library with exceptional documentation. The header block (lines 4-82) provides a complete specification of the trait ID structure, packed traits layout, weighted distribution, random seed usage, and security considerations. All three functions (`weightedBucket`, `traitFromWord`, `packedTraitsFromSeed`) have accurate and detailed `@notice`/`@dev`/`@param`/`@return` documentation. The bucket thresholds in `weightedBucket` (lines 103-110) match the implementation exactly.

### ContractAddresses.sol (~38 lines)

**Review scope:** Full NatSpec and inline comment audit. 1 prior finding (CMT-079).

**Prior fix verification:** CMT-079 FIXED (see table above).

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Lines 4-5 now accurately describe the deploy pipeline's role in populating addresses. The file contains only compile-time constants -- no functions requiring NatSpec.

---

## Interfaces

### IDegenerusGame.sol (~459 lines)

**Review scope:** Full NatSpec and inline comment audit. 5 prior findings (CMT-205, CMT-206, CMT-207, CMT-208, CMT-209).

**Prior fix verification:** All 5 FIXED (see table above).

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** This interface has received the most NatSpec improvements since v3.2. Every function now has complete `@notice`, `@dev` (where applicable), `@param`, and `@return` documentation. Key verifications:
- `MintPaymentKind` enum (lines 5-10): accurate descriptions for DirectEth, Claimable, Combined.
- `purchaseInfo` (lines 50-60): all 5 return values documented with accurate descriptions.
- `creditDecJackpotClaimBatch` and `creditDecJackpotClaim` (lines 165-179): accurate 50/50 split documentation with GAMEOVER 100% ETH note.
- `recordDecBurn` (lines 181-194): `@return bucketUsed` accurately notes "may differ from requested if not an improvement".
- `decClaimable` (lines 256-261): clean -- no stale "expired" reference.
- Terminal decimator functions (lines 207-233): now fully documented.
- Degenerette views (lines 447-458): now have `@notice` tags.
- `purchaseDeityPass` (lines 397-400): clean -- no phantom parameter.

### IDegenerusQuests.sol (~150 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Complete NatSpec on all interface functions and structs. `QuestRequirements`, `QuestInfo`, `PlayerQuestView` structs have `@notice` on all fields. All `handle*` functions have matching `@param` and `@return` documentation. The `rollDailyQuest` function's `@return rolled` accurately documents that it returns false if already rolled.

### IDegenerusAffiliate.sol (~62 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Clean interface. `payAffiliate` has accurate `@dev` noting fresh/recycled ETH reward rates. `affiliateTop`, `affiliateScore`, `totalAffiliateScore`, `affiliateBonusPointsBest`, `getReferrer` all have accurate documentation matching the DegenerusAffiliate implementation.

### IDegenerusCoin.sol (~41 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Extends `IDegenerusCoinModule`. All functions (`creditCoin`, `burnCoin`, `mintForGame`, `notifyQuestMint`, `notifyQuestLootBox`, `notifyQuestDegenerette`) have accurate `@notice` and `@param` documentation.

### IStETH.sol (~36 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Minimal Lido stETH interface. `submit`, `balanceOf`, `transfer`, `approve`, `transferFrom` all have correct `@notice`, `@param`, `@return` tags.

### IDegenerusJackpots.sol (~33 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Clean interface. `runBafJackpot` `@return` tags match the DegenerusJackpots implementation. `recordBafFlip` and `getLastBafResolvedDay` are accurately documented.

### IVRFCoordinator.sol (~46 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** `VRFRandomWordsRequest` struct fields all have `@notice` tags matching Chainlink VRF V2.5 Plus spec. `requestRandomWords` and `getSubscription` have accurate `@param` and `@return` documentation. `getSubscription` return values match the Chainlink coordinator interface.

### DegenerusGameModuleInterfaces.sol (~32 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Single interface `IDegenerusCoinModule` with `creditFlip`, `creditFlipBatch`, `rollDailyQuest`, `vaultEscrow` -- all accurately documented.

### IVaultCoin.sol (~31 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Interface for vault mint allowance pattern. `vaultEscrow`, `vaultMintTo`, `vaultMintAllowance`, `balanceOf`, `transfer` all have accurate NatSpec matching implementations (BurnieCoin, WrappedWrappedXRP).

---

## Libraries

### JackpotBucketLib.sol (~307 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Comprehensive library documentation. `traitBucketCounts` (line 36) accurately describes base counts [25, 15, 8, 1] and entropy rotation. `scaleTraitBucketCountsWithCap` (line 55) correctly documents the 1x/2x/maxScale scaling thresholds. `capBucketCounts` (line 115) correctly handles the solo bucket preservation. `bucketShares` (line 211) accurately describes the remainder bucket pattern. `soloBucketIndex`, `rotatedShareBps`, `shareBpsByBucket` helper functions are well-documented. Trait packing functions (`packWinningTraits`, `unpackWinningTraits`, `getRandomTraits`) have accurate bit-level documentation.

### BitPackingLib.sol (~88 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** The top-level `@dev` block (lines 9-22) provides a complete bit-field layout specification that matches the constant definitions. All bit masks (`MASK_16`, `MASK_24`, `MASK_32`) and shift positions are accurately documented with their bit ranges. The `setPacked` function has correct `@param` and `@return` documentation with the formula noted in the `@dev` tag.

### PriceLookupLib.sol (~47 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Single function `priceForLevel` with detailed NatSpec covering the 100-level cycle, intro pricing overrides, and all 5 price tiers. The documented prices match the implementation code exactly (0.01, 0.02, 0.04, 0.08, 0.12, 0.16, 0.24 ETH).

### GameTimeLib.sol (~35 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Two functions (`currentDayIndex`, `currentDayIndexAt`) with accurate NatSpec. The `@dev` note about Day 1 = deploy day and 22:57 UTC reset time matches `JACKPOT_RESET_TIME = 82620` (82620 seconds = 22 hours 57 minutes).

### EntropyLib.sol (~24 lines)

**Review scope:** Full NatSpec and inline comment audit.

**Fresh scan:** No new findings. All NatSpec verified accurate.

**Notes:** Single function `entropyStep` with accurate `@dev` documenting XOR-shift PRNG with VRF seeding. The shift values (7, 9, 8) match the implementation.

---

## New Findings Table

| ID | Severity | Contract | Line | Summary | Recommendation |
|----|----------|----------|------|---------|----------------|
| CMT-V35-030 | INFO | DegenerusQuests.sol | 54 | `OnlyCoin` error name implies only COIN access, but `onlyCoin` modifier (line 284) gates both COIN and COINFLIP. Error name is a minor misnomer. | Rename to `OnlyCoinOrCoinflip` for precision, or accept as naming shorthand. No functional impact. |
| CMT-V35-031 | INFO | DegenerusAffiliate.sol | 52 | Duplicate `@title DegenerusAffiliate` NatSpec block. First block at line 8 (above imports) has full ARCHITECTURE/SECURITY detail; second block at line 52 (above `contract`) has shorter INTEGRATION POINTS. Compiler uses the second block, orphaning the first. | Merge into single `@title` block above `contract`, or convert first to file-level comment. Cosmetic. |
| CMT-V35-032 | INFO | DeityBoonViewer.sol | 79-84 | `deityBoonSlots` accepts arbitrary `game` address parameter but NatSpec does not document that `game` must implement `IDeityBoonDataSource`. No access control (view-only contract, by design). | Add `@dev game must implement IDeityBoonDataSource` to clarify trust boundary. No security impact. |

---

## Cross-Cutting Observations

1. **Massive NatSpec improvement since v3.2:** All 10 prior "accept as known" findings are now fully resolved. IDegenerusGame.sol in particular went from 5 open findings to zero.

2. **Consistent NatSpec quality across libraries:** All 5 libraries have complete and accurate NatSpec on all public/internal functions. No gaps found.

3. **Interface-implementation consistency verified:**
   - IDegenerusGame.sol vs DegenerusGame: `@param` names match, `@return` semantics match.
   - IDegenerusQuests.sol vs DegenerusQuests.sol: all function signatures and NatSpec align.
   - IDegenerusAffiliate.sol vs DegenerusAffiliate.sol: `payAffiliate` parameters and return semantics match.
   - IDegenerusJackpots.sol vs DegenerusJackpots.sol: `runBafJackpot` parameters and returns match.

4. **No stale references to removed features** found in any of the 21 files. The previous cleanup of "or expired", "wrap" tracking, "zeroed in source", and phantom parameters has been thorough.

---

*Generated: 2026-03-22*
*Milestone: v3.5 Comment Correctness*
*Plan: 54-06 (Peripheral Contracts, Remaining Interfaces, Libraries)*
