# Phase 40 Findings: Core Game Contracts

**Date:** 2026-03-19
**Scope:** DegenerusAdmin.sol (800 lines), DegenerusGameStorage.sol (1,625 lines), DegenerusGame.sol (2,837 lines)
**Pass:** v3.2 re-scan (post-v3.1 fix verification + fresh independent scan)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT new | Fix verified | Fix partial | Fix not applied | Total |
|----------|---------|-------------|-------------|-----------------|-------|
| DegenerusAdmin.sol | 1 | 4 | 0 | 0 | 5 |
| DegenerusGameStorage.sol | 0 | 1 | 0 | 1 | 2 |
| DegenerusGame.sol | 1 | 6 | 0 | 0 | 7 |
| **Total** | **2** | **11** | **0** | **1** | **14** |

---

## DegenerusAdmin.sol

**Review scope:** 800 lines, 60 NatSpec tags, 21 ext/pub functions (12 functions + 9 public state variable auto-getters).
**Post-v3.1 changes:** v3.1 comment fixes applied in working tree diff. All 4 v3.1 findings addressed.

### Fix Verification (4 findings)

| ID | v3.1 Description | Status | Verification Notes |
|----|------------------|--------|-------------------|
| CMT-001 | Header says "60%" but threshold starts at 50% | **FIXED** | Line 38 now reads `50%` in the threshold decay description. No other `60%` references remain in the file (grep confirms zero matches). New text `(50% -> 5% over 7 days)` matches `threshold()` at line 538 which returns `5000` (50%) as initial value. Fix is correct and complete. |
| CMT-002 | "Death clock pauses while any proposal is active" is stale | **FIXED** | Line 41 (the stale "Death clock" reference) has been deleted. No remaining references to "death clock" or "pauses while any proposal" exist in the file (case-insensitive grep confirms zero matches). The governance description at lines 36-40 now fully and accurately describes current governance behavior without the removed feature. |
| DRIFT-001 | `jackpotPhase()` vestigial in IDegenerusGameAdmin interface | **FIXED** | `jackpotPhase()` has been removed from the IDegenerusGameAdmin interface (grep confirms zero matches for `jackpotPhase` in the file). The interface now declares only functions actually called by DegenerusAdmin: `lastVrfProcessed`, `gameOver`, `updateVrfCoordinatorAndSub`, `wireVrf`, `adminSwapEthForStEth`, `adminStakeEthForStEth`, `setLootboxRngThreshold`, `purchaseInfo`. However, the interface `@dev` comment at line 66 still reads `"Game contract admin interface (VRF + liquidity + liveness)"` -- see NEW-001 below. |
| DRIFT-002 | `propose()` NatSpec omits 1-per-address limit | **FIXED** | Line 394 now reads `Each address may have at most one active proposal at a time.` in the `@dev` block. This accurately describes the `AlreadyHasActiveProposal` revert at line 413. Fix is correct and complete. |

### New Findings

#### NEW-001: IDegenerusGameAdmin interface @dev still mentions "liveness"

- **What:** The `IDegenerusGameAdmin` interface `@dev` comment at line 66 reads `"Game contract admin interface (VRF + liquidity + liveness)"`. The "liveness" concept referred to the removed death clock / jackpotPhase interaction. No remaining interface member implements or relates to liveness checking. The only liveness-adjacent function is `lastVrfProcessed()` which supports governance stall detection, but that falls under "VRF" not "liveness" in the original terminology.
- **Where:** DegenerusAdmin.sol:66
- **Why:** A warden reading the interface would expect to find a liveness-related function (such as the removed `jackpotPhase()`). When none is found, they may file a finding about incomplete interface design or missing functionality. The v3.1 DRIFT-001 fix removed `jackpotPhase()` but did not update this comment.
- **Suggestion:** Change line 66 to: `/// @dev Game contract admin interface (VRF + liquidity management).`
- **Category:** comment-inaccuracy
- **Severity:** INFO

### Fresh Scan: NatSpec Verification (21 ext/pub functions)

**Block comments:**
- Lines 6-41 (contract-level header): VERIFIED ACCURATE. Architecture overview, deployment model, ownership model, VRF subscription lifecycle, and governance description all match current code. Threshold now correctly says "50%" (CMT-001 fix). Death clock reference removed (CMT-002 fix). No stale references.

**Interface declarations (lines 48-141):**
- `IVRFCoordinatorV2_5Owner` (line 48): @dev accurate ("Minimal VRF coordinator surface for subscription management"). All 4 functions match VRF V2.5 interface. MATCH.
- `IDegenerusGameAdmin` (line 67): @dev mentions "liveness" -- flagged as NEW-001. Function set matches actual usage in DegenerusAdmin. MATCH (except @dev, see NEW-001).
- `ILinkTokenLike` (line 99): @dev accurate ("LINK token interface (ERC-677 with transferAndCall)"). MATCH.
- `IDegenerusCoinLinkReward` (line 113): @dev accurate. MATCH.
- `IAggregatorV3` (line 118): @dev accurate ("Chainlink price feed interface (AggregatorV3)"). MATCH.
- `IDegenerusVaultOwner` (line 133): @dev accurate ("Vault interface for ownership check"). MATCH.
- `IsDGNRS` (line 138): @dev accurate ("sDGNRS interface for governance voting weight and circulating supply"). MATCH.

**Public state variables (auto-getters):**
1. `coordinator` (248): @notice "Current VRF coordinator address." MATCH.
2. `subscriptionId` (251): @notice "Current VRF subscription ID." MATCH.
3. `vrfKeyHash` (254): @notice "VRF key hash for the current coordinator." MATCH.
4. `proposalCount` (261): @notice "Total proposals ever created (also serves as next ID - 1)." MATCH -- `++proposalCount` at line 432 confirms 1-indexed.
5. `proposals` (264): @notice "Proposal data by ID (1-indexed)." MATCH.
6. `votes` (267): @notice "Vote direction per voter per proposal." MATCH.
7. `voteWeight` (270): @notice "Vote weight recorded at time of vote." MATCH.
8. `activeProposalId` (273): @notice "Tracks each address's current active proposal ID (0 = none)." MATCH.
9. `linkEthPriceFeed` (284): @notice "Chainlink LINK/ETH price feed address." MATCH.

**Functions:**
1. `setLinkEthPriceFeed(address)` (356): @notice/@param match. Reverts: FeedHealthy (if current feed is healthy), InvalidFeedDecimals (if new feed has wrong decimals). No @custom:reverts tag but revert conditions are clear from code. MATCH.
2. `swapGameEthForStEth()` (373): No NatSpec. Function is simple passthrough to `gameAdmin.adminSwapEthForStEth`. Acceptable for trivial delegation. MATCH.
3. `stakeGameEthToStEth(uint256)` (378): No NatSpec. Simple passthrough. MATCH.
4. `setLootboxRngThreshold(uint256)` (382): No NatSpec. Simple passthrough. MATCH.
5. `propose(address, bytes32)` (398): @notice/@dev/@param/@return all accurate. @dev correctly documents Admin path (DGVE >50.1%, 20h stall), Community path (0.5% sDGNRS, 7d stall), and 1-per-address limit (DRIFT-002 fix). MATCH.
6. `vote(uint256, bool)` (452): @notice/@dev/@param accurate. @dev correctly documents changeable votes, stall re-check, and auto-cancellation behavior. MATCH.
7. `circulatingSupply()` (520): @notice accurate -- "excludes undistributed pools and DGNRS wrapper". Code at lines 521-523 confirms subtraction of sDGNRS and DGNRS balances. MATCH.
8. `threshold(uint256)` (530): @notice/@dev/@param/@return all accurate. @dev "Returns 0 if proposal has expired (168h+)" matches line 532. Return type documented as basis points. MATCH.
9. `canExecute(uint256)` (544): @notice/@param/@return accurate. "View-only, no side effects" is correct -- function only reads state. MATCH.
10. `shutdownVrf()` (648): @notice/@dev accurate. "Only callable by the GAME contract (during handleFinalSweep)" matches line 649 check. MATCH.
11. `onTokenTransfer(address, uint256, bytes)` (680): @notice/@param accurate. "ERC-677 callback: handles LINK donations to fund VRF subscription" matches behavior. MATCH.
12. `linkAmountToEth(uint256)` (731): @dev "Convert LINK amount to ETH-equivalent using price feed." Accurate. Note: function is `external` but called via `this.linkAmountToEth()` at line 709 -- this is a deliberate try/catch pattern, not a comment issue. MATCH.

**Internal functions:**
- `_executeSwap(uint256)` (563): @dev accurate. "Execute VRF coordinator swap and void all other active proposals." Steps 1-5 match code. MATCH.
- `_voidAllActive(uint256)` (628): @dev accurate. "Uses voidedUpTo watermark to skip already-voided prefix." MATCH.
- `_linkRewardMultiplier(uint256)` (755): @dev "Calculate reward multiplier based on subscription LINK balance." Accurate. MATCH.
- `_feedHealthy(address)` (774): @dev "Check if a price feed is healthy (fresh and valid)." Accurate. MATCH.

**Constants:**
- `PRICE_COIN_UNIT` (287): @dev accurate. MATCH.
- `LINK_ETH_FEED_DECIMALS` (290): @dev accurate. MATCH.
- `LINK_ETH_MAX_STALE` (293): @dev accurate ("Max staleness window before LINK/ETH feed is considered unhealthy"). MATCH.
- All governance constants (299-312): All @dev descriptions match values and usage. MATCH.

**Section headers:** All section headers (GOVERNANCE TYPES, CUSTOM ERRORS, EVENTS, PRECOMPUTED ADDRESSES, VRF STATE, GOVERNANCE STATE, LINK REWARD STATE, GOVERNANCE CONSTANTS, ACCESS CONTROL, CONSTRUCTOR, PRICE FEED MANAGEMENT, LIQUIDITY MANAGEMENT, VRF COORDINATOR SWAP GOVERNANCE, GOVERNANCE INTERNAL, VRF SHUTDOWN, LINK DONATION HANDLING, INTERNAL HELPERS) accurately group their contents. MATCH.

**DegenerusAdmin.sol fresh scan complete.** 1 new finding (NEW-001: interface @dev still mentions "liveness"). All 21 ext/pub functions verified. All 4 v3.1 findings confirmed FIXED.

---

## DegenerusGameStorage.sol

**Review scope:** 1,625 lines, 475 NatSpec tags, 0 ext/pub functions (abstract storage contract -- contains internal variables and helpers only).
**Post-v3.1 changes:** v3.1 comment fixes applied in working tree diff. CMT-004 fix applied; CMT-003 NOT fixed (deferred).

### Fix Verification (2 findings)

| ID | v3.1 Description | Status | Verification Notes |
|----|------------------|--------|-------------------|
| CMT-003 | Misplaced SLOT 1 header above Slot 0 variables | **NOT FIXED** | The "EVM SLOT 1" section header has moved from the v3.1 location (line 226) to line 292 in the current file. However, it is now misplaced in a different way: three Slot 1 variables (`dailyEthPhase` at line 276, `compressedJackpotFlag` at line 283, `purchaseStartDay` at line 289) appear ABOVE the "EVM SLOT 1" section header. These three variables are definitively in Slot 1 per the authoritative diagram (lines 56-67): Slot 1 starts at `dailyEthPhase` (byte 0) through `prizePoolFrozen` (byte 26). The section header at line 292 should appear at line 274 (before `dailyEthPhase`). The inline comment at line 294 ("Packs into EVM Slot 1: dailyEthPhase through prizePoolFrozen") is textually accurate but its placement after three Slot 1 variables is misleading -- a reader would assume `dailyEthPhase`, `compressedJackpotFlag`, and `purchaseStartDay` are still part of Slot 0. |
| CMT-004 | Free-floating NatSpec (4 orphaned lines 147-149) | **FIXED** | The orphaned `@dev` NatSpec about daily jackpot percentage mechanics has been removed. Lines 147-149 now contain the `TICKET_SLOT_BIT` constant with its own attached NatSpec. No detached NatSpec remains between `EARLYBIRD_TARGET_ETH` (line 144) and `TICKET_SLOT_BIT` (line 150). Fix is correct and complete. |

### Fresh Scan: Storage Layout and NatSpec Verification

**Authoritative storage layout diagram (lines 34-112):**
- Slot 0 diagram (lines 34-53): VERIFIED ACCURATE. 14 fields listed, byte offsets [0:32], types and descriptions all match variable declarations at lines 191-272. Total: 32 bytes (fully packed). MATCH.
- Slot 1 diagram (lines 56-67): VERIFIED ACCURATE. 7 fields listed, byte offsets [0:27], types match declarations at lines 276-327. Total: 27 bytes used, 5 bytes padding. MATCH.
- Slot 2 diagram (lines 70-73): VERIFIED ACCURATE. `currentPrizePool` uint256. MATCH.
- Slots 3+ description (lines 75-79): VERIFIED ACCURATE. Full-width variables, arrays, mappings. MATCH.
- Security considerations (lines 81-106): VERIFIED ACCURATE. All 6 points match current contract behavior. MATCH.
- Upgrade notes (lines 107-112): VERIFIED ACCURATE. MATCH.

**Section headers vs. actual variable grouping:**

1. "CONSTANTS" (line 118-170): Contains `PRICE_COIN_UNIT`, `TICKET_SCALE`, `LOOTBOX_CLAIM_THRESHOLD`, `BOOTSTRAP_PRIZE_POOL`, `EARLYBIRD_END_LEVEL`, `EARLYBIRD_TARGET_ETH`, `TICKET_SLOT_BIT`, `DISTRESS_MODE_HOURS`, `_DEPLOY_IDLE_TIMEOUT_DAYS`, and `_isDistressMode()` function. All NatSpec accurate. MATCH.

2. "Errors" (line 172-177): Contains `error E()`. NatSpec: "Gas-minimal revert signal. Matches codebase convention." MATCH.

3. "SLOT 0: Level Timing, Batching, and Finite State Machine" (line 179-219): Contains `levelStartTime`, `dailyIdx`, `rngRequestTime`, `level`, `jackpotPhaseFlag`. All are Slot 0 variables. All NatSpec verified accurate against usage. MATCH.

4. "EVM SLOT 0 (continued): Counters and Flags" (line 221-272): Contains `jackpotCounter`, `earlyBurnPercent`, `poolConsolidationDone`, `lastPurchaseDay`, `decWindowOpen`, `rngLockedFlag`, `phaseTransitionActive`, `gameOver`, `dailyJackpotCoinTicketsPending`, `dailyEthBucketCursor`. All are Slot 0 variables. All NatSpec verified accurate. MATCH.

5. **MISPLACED**: `dailyEthPhase` (line 276), `compressedJackpotFlag` (line 283), `purchaseStartDay` (line 289) appear without a section header -- they are Slot 1 variables above the "EVM SLOT 1" header. This is CMT-003 (still present).

6. "EVM SLOT 1: ETH Phase, Price, and Double-Buffer Fields" (line 292): Contains `price`, `ticketWriteSlot`, `ticketsFullyProcessed`, `prizePoolFrozen`. These are Slot 1 variables but the section is incomplete (3 Slot 1 variables appear above it). All NatSpec within this section verified accurate. PARTIAL MATCH (section boundary wrong, content accurate).

7. "SLOTS 3+: Full-Width Balances and Pools" (line 329): Header says "SLOTS 3+" but actually starts at Slot 2 (`currentPrizePool` is Slot 2 per the diagram). However, the comment at line 332 says "Each uint256 occupies its own 32-byte slot" which is accurate for these variables. Minor inconsistency but the diagram at lines 70-73 correctly identifies Slot 2. MATCH (acceptable -- section groups all full-width variables together).

**Variable NatSpec verification (all 100+ internal variables):**

All variable NatSpec blocks were systematically verified against their usage context. Key verifications:

- `rngLockedFlag` (line 257): @dev says "True when daily RNG is locked (jackpot resolution in progress). Set when daily VRF is requested, cleared when daily processing completes. Mid-day lootbox RNG does NOT set this flag." Accurate per AdvanceModule and LootboxModule usage. MATCH.
- `prizePoolsPacked` (line 344): @dev correctly describes packed layout `[128:256] futurePrizePool | [0:128] nextPrizePool`. Matches `_getPrizePools`/`_setPrizePools` implementations at lines 671-679. MATCH.
- `traitBurnTicket` (line 405): @dev structure description matches actual mapping type. MATCH.
- `mintPacked_` (line 414): @dev correctly notes "Layout defined by ETH_* constants in DegenerusGame". MATCH.
- `ticketsOwedPacked` (line 455): @dev "Layout: [32 bits owed][8 bits remainder]" matches uint40 type and bit manipulation in `_queueTickets` (line 534-535). MATCH.
- `ticketCursor` (line 462): @dev describes dual-purpose cursor (SETUP/PURCHASE/JACKPOT phases). MATCH.
- `AutoRebuyState` struct (line 907-916): All 4 field NatSpec descriptions verified accurate. MATCH.
- `DecEntry` struct (line 1479-1488): All 4 field NatSpec descriptions verified accurate. Note line 1487: "Claim flag (0 = unclaimed, 1 = claimed)" matches claim logic. MATCH.
- `DecClaimRound` struct (line 1491-1498): All 3 field NatSpec descriptions verified accurate. MATCH.
- `decClaimRounds` (line 1511): @dev "Claims persist indefinitely -- no expiry on prior rounds." Accurate post-decimator-expiry-removal (commit 19f5bc60). MATCH.
- `TerminalDecEntry` struct (line 1603-1609): All 5 field NatSpec descriptions verified accurate. MATCH.
- `TerminalDecClaimRound` struct (line 1619-1623): @dev "No rngWord needed -- claims are 100% ETH post-GAMEOVER (auto-rebuy skipped)." Accurate. MATCH.

**Internal helper functions:**
- `_queueTickets` (line 525): @dev/@param accurate. Overflow cap logic matches code. MATCH.
- `_queueTicketsScaled` (line 558): @dev/@param accurate. Remainder promotion logic matches. MATCH.
- `_queueTicketRange` (line 615): @dev/@param accurate. "Optimized for whale pass claims" matches usage context. MATCH.
- `_queueLootboxTickets` (line 655): @dev/@param accurate. MATCH.
- Prize pool helpers (lines 671-767): All @dev descriptions match implementations. MATCH.
- Ticket queue key encoding (lines 695-704): @dev descriptions match `TICKET_SLOT_BIT` XOR logic. MATCH.
- Queue swap/freeze helpers (lines 710-739): All @dev descriptions match behavior. MATCH.
- `_awardEarlybirdDgnrs` (line 1058): @dev/@param accurate. Quadratic emission curve description matches code. MATCH.
- `_activate10LevelPass` (line 1126): @dev/@param accurate. MATCH.
- `_applyWhalePassStats` (line 1211): @dev/@param accurate. MATCH.
- Day index helpers (lines 1277-1307): All @dev descriptions match. MATCH.

**Events (lines 488-517):**
- All 4 ticket queue events (`TraitsGenerated`, `TicketsQueued`, `TicketsQueuedScaled`, `TicketsQueuedRange`) have accurate NatSpec. MATCH.

**DegenerusGameStorage.sol fresh scan complete.** 0 new findings. CMT-003 confirmed NOT FIXED (misplaced SLOT 1 header -- now affects 3 Slot 1 variables appearing above the section header). CMT-004 confirmed FIXED. All storage layout comments verified accurate against the authoritative diagram. All variable NatSpec verified accurate. All internal helper NatSpec verified accurate.

---

## DegenerusGame.sol

**Review scope:** 2,836 lines, 507 NatSpec tags, 51 ext/pub functions (excluding interface declarations and private/internal helpers).
**Post-v3.1 changes:** v3.1 comment fixes applied in working tree diff. All 6 v3.1 findings addressed (5 direct fixes + 1 group deletion covering 3 findings).

### Fix Verification (6 findings)

| ID | v3.1 Description | Status | Verification Notes |
|----|------------------|--------|-------------------|
| CMT-005 | Block comment says "18h timeout" but code uses 12h | **FIXED** | Line 287 now reads `RNG must be ready (not locked) or recently stale (12h timeout)`. No remaining "18h" references exist in the file (grep confirms zero matches). The contract-level header at line 27 also correctly states "12h VRF timeout". Fix is correct and complete. |
| CMT-006 | Jackpot section header omits compression tiers, oversimplifies "day 5" | **FIXED** | The entire 12-line jackpot payout block comment has been deleted. The region now transitions directly from LOOTBOX CLAIMS (ending line 1786) to ADMIN: REWARD VAULT & LIQUIDITY (starting line 1788). The new section header at lines 1788-1798 accurately describes admin liquidity management. No stale jackpot references remain. The deletion is adequate -- the code in this region (admin swap/stake functions) is self-documenting and does not require a jackpot overview comment. |
| CMT-007 | Jackpot section header misrepresents decimator scope and percentage | **FIXED** | Covered by the same block deletion as CMT-006. No "Decimator - Special 100-level milestone jackpot" text remains in the file. The actual decimator documentation now exists only in the DECIMATOR JACKPOT sections (lines 1059-1353) where function-level NatSpec accurately describes behavior. |
| CMT-008 | Jackpot section header misrepresents BAF scope | **FIXED** | Covered by the same block deletion as CMT-006. No "BAF - Big-ass-flip jackpot (20% of pool at L%100=0)" text remains. BAF is now documented only via inline references (e.g., line 2626 "Used by BAF scatter") which are accurate. |
| CMT-009 | `futurePrizePoolTotalView()` NatSpec says "aggregate" but returns same as `futurePrizePoolView()` | **FIXED** | The `futurePrizePoolTotalView()` function has been entirely removed. Grep confirms zero matches for "futurePrizePoolTotalView" in the file. Only `futurePrizePoolView()` (line 2046) remains, with accurate NatSpec: `@notice Get the unified future pool reserve.` Fix is correct and complete. |
| CMT-010 | Orphaned NatSpec from removed `lastPurchaseDayFlipTotals()` function | **FIXED** | The orphaned `@notice Return last-purchase-day coinflip totals for payout tuning.` line has been deleted. Grep confirms zero matches for "lastPurchaseDayFlipTotals" in the file. The PLAYER MINT STATISTICS section (line 2315) now begins cleanly without orphaned NatSpec. Fix is correct and complete. |

### New Findings

#### NEW-002: DELEGATE MODULE HELPERS header omits 4 of 9 modules

- **What:** The "DELEGATE MODULE HELPERS" block comment at lines 1006-1021 lists 5 modules (DECIMATOR, ENDGAME, MINT, WHALE, JACKPOT) but the contract actually uses 9 modules via delegatecall. The 4 omitted modules are: GAME_ADVANCE_MODULE (advanceGame, wireVrf, VRF callbacks, requestLootboxRng, reverseFlip), GAME_LOOTBOX_MODULE (openLootBox, openBurnieLootBox, issueDeityBoon), GAME_DEGENERETTE_MODULE (placeFullTicketBets, resolveDegeneretteBets), and GAME_BOON_MODULE (consumeCoinflipBoon, consumeDecimatorBoon, consumePurchaseBoost).
- **Where:** DegenerusGame.sol:1012-1017
- **Why:** A warden reading the module inventory would believe the contract only delegates to 5 modules. When they discover 4 additional module addresses in the code, they may question whether these are unauthorized or undocumented code paths, potentially filing a finding about incomplete documentation or hidden functionality.
- **Suggestion:** Update the module list to include all 9 modules:
  ```
  |  • GAME_ADVANCE_MODULE    - State machine advancement, VRF lifecycle, RNG requests           |
  |  • GAME_BOON_MODULE       - Boon consumption (coinflip, decimator, purchase boosts)          |
  |  • GAME_DECIMATOR_MODULE  - Decimator claim credits and lootbox payouts                      |
  |  • GAME_DEGENERETTE_MODULE- Degenerette bet placement and resolution                         |
  |  • GAME_ENDGAME_MODULE    - Endgame settlement (payouts, wipes, jackpots)                    |
  |  • GAME_JACKPOT_MODULE    - Jackpot calculations and payouts                                 |
  |  • GAME_LOOTBOX_MODULE    - Loot box opening and deity boon issuance                         |
  |  • GAME_MINT_MODULE       - Mint data recording, purchase processing                         |
  |  • GAME_WHALE_MODULE      - Whale/lazy/deity pass purchases                                  |
  ```
- **Category:** comment-inaccuracy
- **Severity:** INFO

### Fresh Scan: NatSpec Verification (51 ext/pub functions)

**Contract-level header (lines 4-28):**
- VERIFIED ACCURATE. Architecture description (2-state FSM, delegatecall modules, prize pool flow), critical invariants, and security notes all match current code. Line 27 correctly states "12h VRF timeout" (CMT-005 fix confirmed). No stale references. MATCH.

**Second @title block (lines 85-93):**
- VERIFIED ACCURATE. Inheritance and delegatecall pattern description. MATCH.

**Core state machine header (lines 270-292):**
- VERIFIED ACCURATE. State transitions, gating tiers (deity > anyone after 30min > pass after 15min > DGVE majority), presale mechanics, and VRF timeout all match current code. Line 287 now says "12h timeout" (CMT-005 fix). MATCH.

**External interface declaration (lines 60-79):**
- `IDegenerusQuestView` (line 61): @notice/@param/@return all accurate. MATCH.

**Precomputed addresses (lines 141-172):**
- All 6 constant references (`coin`, `coinflip`, `steth`, `affiliate`, `dgnrs`, `questView`) have accurate @notice/@dev. MATCH.

**Constants (lines 174-235):**
- All constant @dev descriptions match values and usage. MATCH. Key verifications: `AFKING_LOCK_LEVELS = 5` (line 191), `PURCHASE_TO_FUTURE_BPS = 1000` (10%, line 194), `COINFLIP_BOUNTY_DGNRS_BPS = 20` (0.2%, line 197). Mint packed bit layout (lines 217-235) matches BitPackingLib constants. MATCH.

**Constructor (lines 244-264):**
- @notice/@dev accurate. levelStartTime initialization, bootstrap prize pool, vault deity-equivalent, and pre-queued tickets all match code. MATCH.

**Functions (51 ext/pub, grouped by section):**

1. `advanceGame()` (316): @notice/@dev accurate. Flow overview (7 steps) matches actual delegatecall to AdvanceModule. Security notes accurate. MATCH.
2. `wireVrf(address, uint256, bytes32)` (340): @notice/@dev/@param/@custom:reverts accurate. MATCH.
3. `recordMint(address, uint24, uint256, uint32, MintPaymentKind)` (383): @notice/@dev/@param/@return/@custom:reverts all accurate. Payment modes documented correctly. MATCH.
4. `recordMintQuestStreak(address)` (433): @notice/@dev accurate. Access: COIN only. MATCH.
5. `payCoinflipBountyDgnrs(address, uint256, uint256)` (444): @notice/@dev/@param/@custom:reverts accurate. MATCH.
6. `setOperatorApproval(address, bool)` (477): @notice/@param/@custom:reverts accurate. MATCH.
7. `isOperatorApproved(address, address)` (487): @notice/@param/@return accurate. MATCH.
8. `currentDayView()` (513): @notice accurate. MATCH.
9. `setLootboxRngThreshold(uint256)` (521): @notice/@dev/@param/@custom:reverts accurate. MATCH.
10. `purchase(address, uint256, uint256, bytes32, MintPaymentKind)` (543): @notice/@dev/@param accurate. Describes all purchase modes including affiliate support. MATCH.
11. `purchaseCoin(address, uint256, uint256)` (588): @notice/@dev/@param accurate. MATCH.
12. `purchaseBurnieLootbox(address, uint256)` (610): @notice/@param accurate. MATCH.
13. `purchaseWhaleBundle(address, uint256)` (641): @notice/@dev/@param accurate. Price tiers, ticket distribution, and fund split all documented correctly. MATCH.
14. `purchaseLazyPass(address)` (666): @notice/@dev/@param accurate. Availability conditions documented. MATCH.
15. `purchaseDeityPass(address, uint8)` (686): @notice/@param accurate. Symbol range documented. MATCH.
16. `openLootBox(address, uint48)` (707): @notice/@param accurate. MATCH.
17. `openBurnieLootBox(address, uint48)` (715): @notice/@param accurate. MATCH.
18. `placeFullTicketBets(address, uint8, uint128, uint8, uint32, uint8)` (756): @notice/@param accurate. All 6 parameters documented. MATCH.
19. `resolveDegeneretteBets(address, uint64[])` (785): @notice/@param accurate. MATCH.
20. `consumeCoinflipBoon(address)` (806): @notice/@dev/@param/@return/@custom:reverts accurate. MATCH.
21. `consumeDecimatorBoon(address)` (830): @notice/@dev/@param/@return/@custom:reverts accurate. MATCH.
22. `consumePurchaseBoost(address)` (851): @notice/@dev/@param/@return/@custom:reverts accurate. MATCH.
23. `deityBoonData(address)` (874): @notice/@param/@return accurate. All 5 return values documented. MATCH.
24. `issueDeityBoon(address, address, uint8)` (903): @notice/@param/@custom:reverts accurate. MATCH.
25. `creditDecJackpotClaimBatch(address[], uint256[], uint256)` (1074): @notice/@dev/@param accurate. MATCH.
26. `creditDecJackpotClaim(address, uint256, uint256)` (1101): @notice/@dev/@param accurate. MATCH.
27. `recordDecBurn(address, uint24, uint8, uint256, uint256)` (1133): @notice/@dev/@param/@return accurate. MATCH.
28. `runDecimatorJackpot(uint256, uint24, uint256)` (1163): @notice/@dev/@param/@return accurate. MATCH.
29. `recordTerminalDecBurn(address, uint24, uint256)` (1190): @notice/@dev accurate. MATCH.
30. `runTerminalDecimatorJackpot(uint256, uint24, uint256)` (1210): @notice/@dev accurate. MATCH.
31. `terminalDecWindow()` (1234): @notice/@return accurate. MATCH.
32. `runTerminalJackpot(uint256, uint24, uint256)` (1246): @notice/@dev/@param/@return accurate. MATCH.
33. `consumeDecClaim(address, uint24)` (1272): @notice/@dev/@param/@return accurate. MATCH.
34. `claimDecimatorJackpot(uint24)` (1293): @notice/@param accurate. MATCH.
35. `decClaimable(address, uint24)` (1312): @notice/@param/@return accurate. MATCH.
36. `claimWinnings(address)` (1415): @notice/@dev accurate. Pull pattern and sentinel documented. MATCH.
37. `claimWinningsStethFirst()` (1422): @notice/@dev accurate. Vault/DGNRS restriction documented. MATCH.
38. `claimAffiliateDgnrs(address)` (1458): @notice/@dev/@param accurate. Segregated allocation, score-proportional distribution, and deity bonus all documented correctly. MATCH.
39. `setAutoRebuy(address, bool)` (1530): @notice/@dev/@param accurate. Ticket bonus rates (30%/45%) documented. MATCH.
40. `setDecimatorAutoRebuy(address, bool)` (1539): @notice/@dev/@param accurate. MATCH.
41. `setAutoRebuyTakeProfit(address, uint256)` (1554): @notice/@dev/@param accurate. MATCH.
42. `autoRebuyEnabledFor(address)` (1593): @notice/@param/@return accurate. MATCH.
43. `decimatorAutoRebuyEnabledFor(address)` (1602): @notice/@param/@return accurate. MATCH.
44. `autoRebuyTakeProfitFor(address)` (1611): @notice/@param/@return accurate. MATCH.
45. `setAfKingMode(address, bool, uint256, uint256)` (1627): @notice/@dev/@param/@custom:reverts accurate. All 3 revert conditions documented. MATCH.
46. `hasActiveLazyPass(address)` (1691): @notice/@param/@return accurate. MATCH.
47. `afKingModeFor(address)` (1703): @notice/@param/@return accurate. MATCH.
48. `afKingActivatedLevelFor(address)` (1710): @notice/@param/@return accurate. MATCH.
49. `deactivateAfKingFromCoin(address)` (1720): @notice/@dev/@param/@custom:reverts accurate. MATCH.
50. `syncAfKingLazyPassFromCoin(address)` (1733): @notice/@dev/@param/@return/@custom:reverts accurate. MATCH.
51. `claimWhalePass(address)` (1771): @notice/@dev/@param accurate. MATCH.

**Admin functions (1788-1848):**
- `adminSwapEthForStEth(address, uint256)` (1808): @notice/@dev/@param/@custom:reverts accurate. MATCH.
- `adminStakeEthForStEth(uint256)` (1828): @notice/@dev/@param/@custom:reverts accurate. Reserve calculation documented. MATCH.

**VRF integration (1850-1946):**
- Section header (lines 1850-1867): VERIFIED ACCURATE. VRF lifecycle (5 steps) and security notes all match. MATCH.
- `updateVrfCoordinatorAndSub(address, uint256, bytes32)` (1875): @notice/@dev/@param/@custom:reverts accurate. MATCH.
- `requestLootboxRng()` (1898): @notice/@dev accurate. Revert conditions described. MATCH.
- `reverseFlip()` (1915): @notice/@dev/@custom:reverts accurate. Nudge mechanism documented. MATCH.
- `rawFulfillRandomWords(uint256, uint256[])` (1932): @notice/@dev/@param accurate. MATCH.

**View functions -- Game Status (2019-2229):**
All 20 view functions in this section verified: `prizePoolTargetView`, `nextPrizePoolView`, `futurePrizePoolView`, `ticketsOwedView`, `lootboxStatus`, `degeneretteBetInfo`, `lootboxPresaleActiveFlag`, `lootboxRngIndexView`, `lootboxRngWord`, `lootboxRngThresholdView`, `lootboxRngMinLinkBalanceView`, `currentPrizePoolView`, `rewardPoolView`, `claimablePoolView`, `isFinalSwept`, `yieldPoolView`, `yieldAccumulatorView`, `mintPrice`, `rngWordForDay`, `lastRngWord`, `rngLocked`, `isRngFulfilled`, `rngStalledForThreeDays`, `lastVrfProcessed`. All @notice/@dev/@param/@return accurate. MATCH.

**View functions -- Decimator & Purchase (2231-2313):**
All functions verified: `decWindow`, `decWindowOpenFlag`, `jackpotCompressionTier`, `jackpotPhase`, `purchaseInfo`. All NatSpec accurate. `purchaseInfo` @return documents 5 return values matching actual returns. MATCH.

**View functions -- Player Mint Statistics (2315-2379):**
All 4 functions verified: `ethMintLastLevel`, `ethMintLevelCount`, `ethMintStreakCount`, `ethMintStats`. All @notice/@param/@return accurate. Deity pass short-circuit (returning `level`) is consistent across all 4. MATCH.

**View functions -- Activity Score (2381-2509):**
- `playerActivityScore(address)` (2404): @notice/@dev/@param/@return accurate. Component breakdown in @dev matches implementation. Consumer cap documentation accurate. MATCH.
- Section header correctly lists activity components and their caps. MATCH.

**View functions -- Claims & Lootbox Counts (2511-2568):**
All 5 functions verified: `getWinnings`, `claimableWinningsOf`, `whalePassClaimAmount`, `deityPassCountFor`, `deityPassPurchasedCountFor`, `deityPassTotalIssuedCount`. All NatSpec accurate. MATCH.

**Trait Ticket Sampling (2570-2694):**
All 3 functions verified: `sampleTraitTickets`, `sampleTraitTicketsAtLevel`, `sampleFarFutureTickets`. All @notice/@dev/@param/@return accurate. MATCH.

**Trait Ticket Queries (2696-2743):**
All 2 functions verified: `getTickets`, `getPlayerPurchases`. All NatSpec accurate. MATCH.

**Degenerette Tracking Views (2744-2809):**
All 4 functions verified: `getDailyHeroWager`, `getDailyHeroWinner`, `getPlayerDegeneretteWager`, `getTopDegenerette`. All NatSpec accurate. MATCH.

**Receive function (2818-2836):**
- `receive()` (2827): @notice "Accept ETH and add to the future pool reserve." Accurate -- plain ETH transfers route to futurePrizePool (or pending accumulator if frozen). MATCH.

**Section headers:** All section headers verified against their contents: ERRORS, EVENTS, PRECOMPUTED ADDRESSES, CONSTANTS, MINT PACKED BIT LAYOUT, CONSTRUCTOR, MODIFIERS, CORE STATE MACHINE, ADMIN VRF FUNCTIONS, MINT RECORDING, OPERATOR APPROVALS, LOOT BOX CONTROLS, DELEGATE MODULE HELPERS (incomplete module list -- NEW-002), DECIMATOR JACKPOT CREDITS, DECIMATOR JACKPOT LOGIC, CLAIMING WINNINGS, AUTO-REBUY TOGGLE, LOOTBOX CLAIMS, ADMIN: REWARD VAULT & LIQUIDITY, VRF INTEGRATION, PAYMENT HELPERS, VIEW sections. All accurate except DELEGATE MODULE HELPERS (see NEW-002).

**Deleted jackpot block evaluation:** The deleted 12-line jackpot payout block comment (CMT-006/007/008) previously appeared between LOOTBOX CLAIMS and the admin section. The code region following the deletion is the admin reward vault section (`adminSwapEthForStEth`, `adminStakeEthForStEth`) which has its own accurate section header and function-level NatSpec. No documentation gap exists. Deletion adequate.

**DegenerusGame.sol fresh scan complete.** 1 new finding (NEW-002: incomplete module list in DELEGATE MODULE HELPERS header). All 51 ext/pub functions verified. All 6 v3.1 findings confirmed FIXED.

---

## Phase 40 Plan 01 Summary

| Metric | Value |
|--------|-------|
| Total lines scanned | 5,261 (800 + 1,625 + 2,836) |
| Total ext/pub functions verified | 72 (21 + 0 + 51) |
| Total v3.1 findings verified | 12 (CMT-001 through CMT-010, DRIFT-001, DRIFT-002) |
| v3.1 findings FIXED | 11 |
| v3.1 findings NOT FIXED | 1 (CMT-003: misplaced SLOT 1 header in GameStorage) |
| New findings from fresh scan | 2 (NEW-001: interface liveness comment; NEW-002: incomplete module list) |
| Total findings in document | 14 (11 fix verifications + 1 not-fixed + 2 new) |

### CMT-02 Verdict

**SATISFIED with 1 known deferral.** All comments in DegenerusAdmin.sol, DegenerusGameStorage.sol, and DegenerusGame.sol have been verified against current code behavior. The 11 of 12 v3.1 findings are confirmed FIXED. CMT-003 (misplaced SLOT 1 header) remains NOT FIXED but is INFO severity and does not affect code correctness -- it is a warden-readability concern only. Two new INFO-severity findings were discovered during the fresh independent scan (NEW-001 and NEW-002), both concerning incomplete header comments. No functional, security, or high-severity comment issues remain in these 3 contracts.
