# v3.8 Commitment Window Inventory

**Date:** 2026-03-22
**Milestone:** v3.8
**Scope:** Phase 68 -- Commitment Window Inventory
**Mode:** Audit-only (no code changes)

---

## Forward-Trace Catalog (CW-01)

This section catalogs every storage variable read or written during VRF fulfillment and all downstream processing. The trace starts at `rawFulfillRandomWords` (AdvanceModule line 1436) and follows every code path through `advanceGame -> rngGate` and all delegatecall modules.

Slot numbers are from `forge inspect` output (authoritative). All DegenerusGameStorage variables are accessed via delegatecall modules operating on the game contract's storage.

### Branch 1: Daily RNG Path (rngLockedFlag == true)

**Entry:** `rawFulfillRandomWords` (AdvanceModule:1436) stores `rngWordCurrent = word` when `rngLockedFlag` is true.

**Processing:** The stored word is consumed by the next `advanceGame()` call, which enters `rngGate` (AdvanceModule:765) and triggers all downstream consumers.

#### 1.1 rawFulfillRandomWords Reads/Writes (AdvanceModule:1436-1457)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| vrfCoordinator | DegenerusGameStorage | 57 | address (IVRFCoordinator) | VRF callback sender validation | R | rawFulfillRandomWords |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Request ID matching | R | rawFulfillRandomWords |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Revert guard (must be 0) | R | rawFulfillRandomWords |
| rngLockedFlag | DegenerusGameStorage | 0 (offset 26) | bool | Branch selector (daily vs mid-day) | R | rawFulfillRandomWords |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Store VRF word for advanceGame | W | rawFulfillRandomWords |

#### 1.2 rngGate Reads/Writes (AdvanceModule:765-838)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| rngWordByDay[day] | DegenerusGameStorage | 13 (mapping) | uint256 | Check if already processed | R | rngGate |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Load VRF word for processing | R | rngGate |
| rngRequestTime | DegenerusGameStorage | 0 (offset 12) | uint48 | Check request is pending | R | rngGate |
| dailyIdx | DegenerusGameStorage | 0 (offset 6) | uint48 | Detect gap days (day > idx + 1) | R | rngGate |
| levelStartTime | DegenerusGameStorage | 0 (offset 0) | uint48 | Extended by gap count for stall recovery | R/W | rngGate |

#### 1.3 _applyDailyRng Reads/Writes (AdvanceModule:1517-1533)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| totalFlipReversals | DegenerusGameStorage | 6 | uint256 | Nudge count to add to VRF word | R | rngGate -> _applyDailyRng |
| totalFlipReversals | DegenerusGameStorage | 6 | uint256 | Reset to 0 after applying nudges | W | rngGate -> _applyDailyRng |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Store final word (raw + nudges) | W | rngGate -> _applyDailyRng |
| rngWordByDay[day] | DegenerusGameStorage | 13 (mapping) | uint256 | Archive final word by day index | W | rngGate -> _applyDailyRng |
| lastVrfProcessedTimestamp | DegenerusGameStorage | 103 | uint48 | Record processing time | W | rngGate -> _applyDailyRng |

#### 1.4 coinflip.processCoinflipPayouts Reads/Writes (BurnieCoinflip:778-862)

External call from rngGate (AdvanceModule:796). BurnieCoinflip is a separate contract with its own storage.

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| coinflipDayResult[epoch] | BurnieCoinflip | 1 (mapping) | CoinflipDayResult struct | Store win/loss + reward percent for day | W | rngGate -> coinflip.processCoinflipPayouts |
| currentBounty | BurnieCoinflip | 3 (offset 0) | uint128 | Read current bounty pool, then update | R/W | rngGate -> coinflip.processCoinflipPayouts |
| bountyOwedTo | BurnieCoinflip | 4 (offset 0) | address | Read bounty owner, clear after resolution | R/W | rngGate -> coinflip.processCoinflipPayouts |
| flipsClaimableDay | BurnieCoinflip | 4 (offset 20) | uint48 | Advance claimable window to current epoch | W | rngGate -> coinflip.processCoinflipPayouts |
| coinflipBalance[epoch][player] | BurnieCoinflip | 0 (mapping) | uint256 | Read bet amounts for sDGNRS auto-claim | R | rngGate -> coinflip.processCoinflipPayouts -> _claimCoinflipsInternal |
| playerState[player] | BurnieCoinflip | 2 (mapping) | PlayerCoinflipState struct | Update sDGNRS cursor during auto-claim | R/W | rngGate -> coinflip.processCoinflipPayouts -> _claimCoinflipsInternal |

#### 1.5 resolveRedemptionPeriod Reads/Writes (StakedDegenerusStonk:538-563)

External call from rngGate (AdvanceModule:808). Only called when `sdgnrs.hasPendingRedemptions()` returns true.

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| redemptionPeriodIndex | StakedDegenerusStonk | 14 | uint48 | Current period index | R | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionEthBase | StakedDegenerusStonk | 11 | uint256 | Base ETH value to multiply by roll | R | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionBurnieBase | StakedDegenerusStonk | 12 | uint256 | Base BURNIE value to multiply by roll | R | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionEthValue | StakedDegenerusStonk | 9 | uint256 | Adjusted by roll result | R/W | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionEthBase | StakedDegenerusStonk | 11 | uint256 | Cleared after roll | W | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionBurnie | StakedDegenerusStonk | 10 | uint256 | Reduced by base amount | R/W | rngGate -> sdgnrs.resolveRedemptionPeriod |
| pendingRedemptionBurnieBase | StakedDegenerusStonk | 12 | uint256 | Cleared after roll | W | rngGate -> sdgnrs.resolveRedemptionPeriod |
| redemptionPeriods[period] | StakedDegenerusStonk | 8 (mapping) | RedemptionPeriod struct | Store roll + flipDay result | W | rngGate -> sdgnrs.resolveRedemptionPeriod |

#### 1.6 _finalizeLootboxRng Reads/Writes (AdvanceModule:840-846)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| lootboxRngIndex | DegenerusGameStorage | 60 | uint48 | Derive current index (index - 1) | R | rngGate -> _finalizeLootboxRng |
| lootboxRngWordByIndex[index] | DegenerusGameStorage | 64 (mapping) | uint256 | Check if already filled, then store RNG word | R/W | rngGate -> _finalizeLootboxRng |
| lastLootboxRngWord | DegenerusGameStorage | 70 | uint256 | Update latest lootbox entropy | W | rngGate -> _finalizeLootboxRng |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Read for event emission | R | rngGate -> _finalizeLootboxRng |

#### 1.7 payDailyJackpot Reads/Writes (JackpotModule:323-522+, via delegatecall)

Delegatecall from advanceGame (AdvanceModule:279/353/376). All storage accesses are on DegenerusGame's storage.

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| currentPrizePool | DegenerusGameStorage | 2 | uint256 | Snapshot pool, deduct daily budget | R/W | advanceGame -> payDailyJackpot |
| jackpotCounter | DegenerusGameStorage | 0 (offset 22) | uint8 | Determine day index within jackpot phase | R | advanceGame -> payDailyJackpot |
| compressedJackpotFlag | DegenerusGameStorage | 1 (offset 0) | uint8 | Turbo/compressed jackpot mode | R | advanceGame -> payDailyJackpot |
| dailyEthPoolBudget | DegenerusGameStorage | 8 | uint256 | Store/read daily ETH budget | R/W | advanceGame -> payDailyJackpot |
| dailyEthPhase | DegenerusGameStorage | 0 (offset 31) | uint8 | Track Phase 0/Phase 1 distribution state | R/W | advanceGame -> payDailyJackpot |
| dailyEthBucketCursor | DegenerusGameStorage | 0 (offset 30) | uint8 | Track bucket progress within phase | R/W | advanceGame -> payDailyJackpot |
| dailyEthWinnerCursor | DegenerusGameStorage | 17 (offset 7) | uint16 | Track winner progress within bucket | R/W | advanceGame -> payDailyJackpot |
| lastDailyJackpotWinningTraits | DegenerusGameStorage | 42 (offset 0) | uint32 | Store/restore rolled winning traits | R/W | advanceGame -> payDailyJackpot |
| lastDailyJackpotLevel | DegenerusGameStorage | 42 (offset 4) | uint24 | Store/restore jackpot level | R/W | advanceGame -> payDailyJackpot |
| lastDailyJackpotDay | DegenerusGameStorage | 42 (offset 7) | uint48 | Store quest day for trait sync | R/W | advanceGame -> payDailyJackpot |
| dailyTicketBudgetsPacked | DegenerusGameStorage | 7 | uint256 | Store ticket budget packing | R/W | advanceGame -> payDailyJackpot |
| dailyCarryoverEthPool | DegenerusGameStorage | 18 | uint256 | Store carryover ETH pool for Phase 1 | W | advanceGame -> payDailyJackpot |
| dailyCarryoverWinnerCap | DegenerusGameStorage | 19 | uint16 | Store carryover winner cap | W | advanceGame -> payDailyJackpot |
| traitBurnTicket[level] | DegenerusGameStorage | 11 (mapping) | address[][256] | Winner selection from trait ticket arrays | R | advanceGame -> payDailyJackpot |
| claimableWinnings[winner] | DegenerusGameStorage | 9 (mapping) | uint256 | Credit ETH to winners | W | advanceGame -> payDailyJackpot |
| claimablePool | DegenerusGameStorage | 10 | uint256 | Track aggregate liability | R/W | advanceGame -> payDailyJackpot |
| prizePoolsPacked | DegenerusGameStorage | 3 | uint256 | Read next/future pool (via getter helpers) | R/W | advanceGame -> payDailyJackpot |
| dailyJackpotCoinTicketsPending | DegenerusGameStorage | 0 (offset 29) | bool | Set true when Phase 2 (coin+tickets) deferred | W | advanceGame -> payDailyJackpot |
| autoRebuyState[player] | DegenerusGameStorage | 36 (mapping) | AutoRebuyState struct | Check auto-rebuy on winner credit | R | advanceGame -> payDailyJackpot -> _addClaimableEth |
| ticketQueue[key] | DegenerusGameStorage | 15 (mapping) | address[] | Queue tickets for auto-rebuy winners | W | advanceGame -> payDailyJackpot -> _addClaimableEth -> _processAutoRebuy |
| ticketsOwedPacked[key][player] | DegenerusGameStorage | 16 (mapping) | uint40 | Record ticket counts for auto-rebuy | W | advanceGame -> payDailyJackpot -> _addClaimableEth -> _processAutoRebuy |
| deityBySymbol[symId] | DegenerusGameStorage | 54 (mapping) | address | Virtual deity entries in trait selection | R | advanceGame -> payDailyJackpot |
| level | DegenerusGameStorage | 0 (offset 18) | uint24 | Read for auto-rebuy target level | R | advanceGame -> payDailyJackpot -> _addClaimableEth |
| gameOver | DegenerusGameStorage | 0 (offset 28) | bool | Skip auto-rebuy post-game | R | advanceGame -> payDailyJackpot -> _addClaimableEth |
| earlybirdDgnrsPoolStart | DegenerusGameStorage | 55 | uint256 | Early-bird lootbox jackpot seed | R | advanceGame -> payDailyJackpot -> _runEarlyBirdLootboxJackpot |
| earlybirdEthIn | DegenerusGameStorage | 56 | uint256 | Early-bird cumulative ETH | R | advanceGame -> payDailyJackpot -> _runEarlyBirdLootboxJackpot |

#### 1.8 payDailyJackpotCoinAndTickets Reads/Writes (JackpotModule:681-766, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| dailyJackpotCoinTicketsPending | DegenerusGameStorage | 0 (offset 29) | bool | Guard + clear pending flag | R/W | advanceGame -> payDailyJackpotCoinAndTickets |
| dailyTicketBudgetsPacked | DegenerusGameStorage | 7 | uint256 | Unpack ticket budgets + counter step | R | advanceGame -> payDailyJackpotCoinAndTickets |
| lastDailyJackpotLevel | DegenerusGameStorage | 42 (offset 4) | uint24 | Restore level from Phase 1 | R | advanceGame -> payDailyJackpotCoinAndTickets |
| lastDailyJackpotWinningTraits | DegenerusGameStorage | 42 (offset 0) | uint32 | Restore winning traits from Phase 1 | R | advanceGame -> payDailyJackpotCoinAndTickets |
| jackpotCounter | DegenerusGameStorage | 0 (offset 22) | uint8 | Increment by counterStep | R/W | advanceGame -> payDailyJackpotCoinAndTickets |
| traitBurnTicket[level] | DegenerusGameStorage | 11 (mapping) | address[][256] | Coin jackpot winner selection | R | advanceGame -> payDailyJackpotCoinAndTickets |
| ticketQueue[key] | DegenerusGameStorage | 15 (mapping) | address[] | Far-future coin jackpot winners | R | advanceGame -> payDailyJackpotCoinAndTickets -> _awardFarFutureCoinJackpot |
| jackpotPhaseFlag | DegenerusGameStorage | 0 (offset 21) | bool | Determine trait roll mode | R | advanceGame -> payDailyJackpotCoinAndTickets |

#### 1.9 _payDailyCoinJackpot Reads/Writes (JackpotModule:2361-2402, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| traitBurnTicket[level] | DegenerusGameStorage | 11 (mapping) | address[][256] | Coin jackpot winner selection | R | advanceGame -> _payDailyCoinJackpot |
| ticketQueue[key] | DegenerusGameStorage | 15 (mapping) | address[] | Far-future coin jackpot winners | R | advanceGame -> _payDailyCoinJackpot -> _awardFarFutureCoinJackpot |

#### 1.10 _consolidatePrizePools Reads/Writes (JackpotModule:879-908, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| yieldAccumulator | DegenerusGameStorage | 100 | uint256 | x00 yield dump (50% to future pool) | R/W | advanceGame -> _consolidatePrizePools |
| currentPrizePool | DegenerusGameStorage | 2 | uint256 | Merge next into current | R/W | advanceGame -> _consolidatePrizePools |
| prizePoolsPacked | DegenerusGameStorage | 3 | uint256 | Read/write next + future pools | R/W | advanceGame -> _consolidatePrizePools |
| claimableWinnings[addr] | DegenerusGameStorage | 9 (mapping) | uint256 | Yield surplus distribution | W | advanceGame -> _consolidatePrizePools -> _distributeYieldSurplus |
| claimablePool | DegenerusGameStorage | 10 | uint256 | Track yield surplus liability | R/W | advanceGame -> _consolidatePrizePools -> _distributeYieldSurplus |
| autoRebuyState[addr] | DegenerusGameStorage | 36 (mapping) | AutoRebuyState struct | Auto-rebuy on yield surplus credits | R | advanceGame -> _consolidatePrizePools -> _distributeYieldSurplus |

#### 1.11 _applyTimeBasedFutureTake Reads/Writes (AdvanceModule:1026-1101)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| levelStartTime | DegenerusGameStorage | 0 (offset 0) | uint48 | Time-based BPS calculation baseline | R | advanceGame -> _applyTimeBasedFutureTake |
| prizePoolsPacked | DegenerusGameStorage | 3 | uint256 | Read/write next + future pools | R/W | advanceGame -> _applyTimeBasedFutureTake |
| levelPrizePool[lvl-1] | DegenerusGameStorage | 45 (mapping) | uint256 | Previous level target for overshoot calc | R | advanceGame -> _applyTimeBasedFutureTake |
| yieldAccumulator | DegenerusGameStorage | 100 | uint256 | Insurance skim accumulation | R/W | advanceGame -> _applyTimeBasedFutureTake |

#### 1.12 _runRewardJackpots Reads/Writes (EndgameModule:168-243, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| prizePoolsPacked | DegenerusGameStorage | 3 | uint256 | Read/write future pool for BAF/Decimator budgets | R/W | advanceGame -> _runRewardJackpots |
| claimablePool | DegenerusGameStorage | 10 | uint256 | Track claimed amounts | R/W | advanceGame -> _runRewardJackpots |
| traitBurnTicket[level] | DegenerusGameStorage | 11 (mapping) | address[][256] | BAF jackpot winner selection | R | advanceGame -> _runRewardJackpots -> _runBafJackpot |
| claimableWinnings[winner] | DegenerusGameStorage | 9 (mapping) | uint256 | Credit BAF/Decimator winners | W | advanceGame -> _runRewardJackpots |

#### 1.13 processTicketBatch Reads/Writes (JackpotModule:1890-1951, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| ticketLevel | DegenerusGameStorage | 17 (offset 4) | uint24 | Track/set current processing level | R/W | advanceGame -> processTicketBatch |
| ticketCursor | DegenerusGameStorage | 17 (offset 0) | uint32 | Track/set current position in queue | R/W | advanceGame -> processTicketBatch |
| ticketQueue[readKey] | DegenerusGameStorage | 15 (mapping) | address[] | Read slot queue for processing | R/W | advanceGame -> processTicketBatch |
| lastLootboxRngWord | DegenerusGameStorage | 70 | uint256 | Entropy source for trait generation | R | advanceGame -> processTicketBatch |
| ticketsOwedPacked[key][player] | DegenerusGameStorage | 16 (mapping) | uint40 | Read owed count, update after processing | R/W | advanceGame -> processTicketBatch |
| traitBurnTicket[level][traitId] | DegenerusGameStorage | 11 (mapping) | address[] | Push generated trait tickets | W | advanceGame -> processTicketBatch -> _raritySymbolBatch |

#### 1.14 _backfillGapDays Reads/Writes (AdvanceModule:1470-1488)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| rngWordByDay[gapDay] | DegenerusGameStorage | 13 (mapping) | uint256 | Store derived words for gap days | W | rngGate -> _backfillGapDays |

Plus external call to `coinflip.processCoinflipPayouts` for each gap day (same BurnieCoinflip variables as section 1.4).

#### 1.15 _backfillOrphanedLootboxIndices Reads/Writes (AdvanceModule:1494-1514)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| lootboxRngIndex | DegenerusGameStorage | 60 | uint48 | Read current index for scan start | R | rngGate -> _backfillOrphanedLootboxIndices |
| lootboxRngWordByIndex[i] | DegenerusGameStorage | 64 (mapping) | uint256 | Check if empty, then fill with derived word | R/W | rngGate -> _backfillOrphanedLootboxIndices |
| lastLootboxRngWord | DegenerusGameStorage | 70 | uint256 | Update to latest backfilled word | W | rngGate -> _backfillOrphanedLootboxIndices |

#### 1.16 _unlockRng Reads/Writes (AdvanceModule:1406-1412)

Called at end of daily processing to reset VRF state.

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| dailyIdx | DegenerusGameStorage | 0 (offset 6) | uint48 | Update to current day | W | advanceGame -> _unlockRng |
| rngLockedFlag | DegenerusGameStorage | 0 (offset 26) | bool | Clear lock (false) | W | advanceGame -> _unlockRng |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Clear to 0 | W | advanceGame -> _unlockRng |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Clear to 0 | W | advanceGame -> _unlockRng |
| rngRequestTime | DegenerusGameStorage | 0 (offset 12) | uint48 | Clear to 0 | W | advanceGame -> _unlockRng |

#### 1.17 awardFinalDayDgnrsReward Reads/Writes (JackpotModule:773-798, via delegatecall)

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| lastDailyJackpotWinningTraits | DegenerusGameStorage | 42 (offset 0) | uint32 | Re-derive solo bucket | R | advanceGame -> awardFinalDayDgnrsReward |
| traitBurnTicket[level] | DegenerusGameStorage | 11 (mapping) | address[][256] | Winner selection for DGNRS reward | R | advanceGame -> awardFinalDayDgnrsReward |

#### 1.18 advanceGame FSM State Reads/Writes (AdvanceModule:125-382)

Additional variables read/written by the advanceGame FSM that gate or influence VRF-dependent processing:

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| jackpotPhaseFlag | DegenerusGameStorage | 0 (offset 21) | bool | Determines purchase vs jackpot phase | R/W | advanceGame |
| level | DegenerusGameStorage | 0 (offset 18) | uint24 | Current level | R/W | advanceGame, _requestRng -> _finalizeRngRequest |
| lastPurchaseDay | DegenerusGameStorage | 0 (offset 24) | bool | Target reached flag | R/W | advanceGame |
| ticketsFullyProcessed | DegenerusGameStorage | 1 (offset 24) | bool | Queue drain gate | R/W | advanceGame |
| midDayTicketRngPending | DegenerusGameStorage | 71 | bool | Mid-day ticket swap pending | R/W | advanceGame |
| phaseTransitionActive | DegenerusGameStorage | 0 (offset 27) | bool | Level transition state | R/W | advanceGame |
| prizePoolFrozen | DegenerusGameStorage | 1 (offset 25) | bool | Pool freeze during jackpot | R/W | advanceGame |
| poolConsolidationDone | DegenerusGameStorage | 0 (offset 23) | bool | Consolidation state | R/W | advanceGame |
| price | DegenerusGameStorage | 1 (offset 7) | uint128 | Set at level transitions | R/W | advanceGame -> _finalizeRngRequest |
| ticketWriteSlot | DegenerusGameStorage | 1 (offset 23) | uint8 | Double-buffer slot selector | R/W | advanceGame -> _swapAndFreeze |
| decWindowOpen | DegenerusGameStorage | 0 (offset 25) | bool | Decimator window open/close | R/W | advanceGame |
| lootboxRngIndex | DegenerusGameStorage | 60 | uint48 | Incremented at RNG request | R/W | advanceGame -> _finalizeRngRequest |
| lootboxRngPendingEth | DegenerusGameStorage | 61 | uint256 | Cleared at RNG request | W | advanceGame -> _finalizeRngRequest |
| lootboxRngPendingBurnie | DegenerusGameStorage | 69 | uint256 | Cleared at RNG request | W | advanceGame -> _finalizeRngRequest |
| lootboxPresaleActive | DegenerusGameStorage | 21 | bool | Presale end check | R/W | advanceGame |
| lootboxPresaleMintEth | DegenerusGameStorage | 22 | uint256 | Presale ETH cap check | R | advanceGame |
| prizePoolPendingPacked | DegenerusGameStorage | 14 | uint256 | Freeze accumulators during jackpot | R/W | advanceGame -> _unfreezePool |
| levelPrizePool[lvl] | DegenerusGameStorage | 45 (mapping) | uint256 | Target check + set at transition | R/W | advanceGame |
| purchaseStartDay | DegenerusGameStorage | 1 (offset 1) | uint48 | Reset at level transition | W | advanceGame |

### Branch 2: Mid-Day Lootbox RNG Path (rngLockedFlag == false)

**Entry:** `rawFulfillRandomWords` (AdvanceModule:1449-1456) when `rngLockedFlag` is false.

This path directly finalizes lootbox RNG without going through `advanceGame -> rngGate`. It is triggered by `requestLootboxRng()`.

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| vrfCoordinator | DegenerusGameStorage | 57 | address (IVRFCoordinator) | VRF callback sender validation | R | rawFulfillRandomWords |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Request ID matching | R | rawFulfillRandomWords |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Revert guard (must be 0) | R | rawFulfillRandomWords |
| rngLockedFlag | DegenerusGameStorage | 0 (offset 26) | bool | Branch selector | R | rawFulfillRandomWords |
| lootboxRngIndex | DegenerusGameStorage | 60 | uint48 | Derive target index (index - 1) | R | rawFulfillRandomWords |
| lootboxRngWordByIndex[index] | DegenerusGameStorage | 64 (mapping) | uint256 | Store VRF word directly | W | rawFulfillRandomWords |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Clear to 0 | W | rawFulfillRandomWords |
| rngRequestTime | DegenerusGameStorage | 0 (offset 12) | uint48 | Clear to 0 | W | rawFulfillRandomWords |

### Branch 2b: requestLootboxRng Setup (AdvanceModule:674-741)

The mid-day request function sets up VRF state and performs the ticket buffer swap:

| Variable | Contract/Storage | Slot | Type | Purpose | R/W | Function Chain |
|---|---|---|---|---|---|---|
| rngLockedFlag | DegenerusGameStorage | 0 (offset 26) | bool | Guard: must be false | R | requestLootboxRng |
| rngWordByDay[currentDay] | DegenerusGameStorage | 13 (mapping) | uint256 | Guard: daily RNG must be done | R | requestLootboxRng |
| rngRequestTime | DegenerusGameStorage | 0 (offset 12) | uint48 | Guard: no pending request | R | requestLootboxRng |
| vrfSubscriptionId | DegenerusGameStorage | 59 | uint256 | LINK balance check | R | requestLootboxRng |
| lootboxRngPendingEth | DegenerusGameStorage | 61 | uint256 | Threshold check | R | requestLootboxRng |
| lootboxRngPendingBurnie | DegenerusGameStorage | 69 | uint256 | Threshold check | R | requestLootboxRng |
| lootboxRngThreshold | DegenerusGameStorage | 62 | uint256 | Threshold amount | R | requestLootboxRng |
| price | DegenerusGameStorage | 1 (offset 7) | uint128 | BURNIE->ETH conversion | R | requestLootboxRng |
| level | DegenerusGameStorage | 0 (offset 18) | uint24 | Derive purchase level | R | requestLootboxRng |
| ticketQueue[writeKey] | DegenerusGameStorage | 15 (mapping) | address[] | Check if write slot non-empty | R | requestLootboxRng |
| ticketsFullyProcessed | DegenerusGameStorage | 1 (offset 24) | bool | Check if processing done | R | requestLootboxRng |
| ticketWriteSlot | DegenerusGameStorage | 1 (offset 23) | uint8 | Swap write->read if applicable | R/W | requestLootboxRng -> _swapTicketSlot |
| midDayTicketRngPending | DegenerusGameStorage | 71 | bool | Set true on buffer swap | W | requestLootboxRng |
| lootboxRngIndex | DegenerusGameStorage | 60 | uint48 | Increment for next RNG | R/W | requestLootboxRng |
| lootboxRngPendingEth | DegenerusGameStorage | 61 | uint256 | Clear to 0 | W | requestLootboxRng |
| lootboxRngPendingBurnie | DegenerusGameStorage | 69 | uint256 | Clear to 0 | W | requestLootboxRng |
| vrfRequestId | DegenerusGameStorage | 5 | uint256 | Store new request ID | W | requestLootboxRng |
| rngWordCurrent | DegenerusGameStorage | 4 | uint256 | Clear to 0 | W | requestLootboxRng |
| rngRequestTime | DegenerusGameStorage | 0 (offset 12) | uint48 | Store request timestamp | W | requestLootboxRng |

### Cross-Contract Storage Domain Summary

| Domain | Contract | Access Method | Slot Range | Variables Touched by VRF Path |
|---|---|---|---|---|
| DegenerusGameStorage | All delegatecall modules | delegatecall (shared storage) | 0-106 | ~55 unique variables |
| BurnieCoinflip | BurnieCoinflip.sol | External call | 0-4 | 6 variables (coinflipBalance, coinflipDayResult, playerState, currentBounty, bountyOwedTo, flipsClaimableDay) |
| StakedDegenerusStonk | StakedDegenerusStonk.sol | External call | 7-14 | 7 variables (pendingRedemptions, redemptionPeriods, pendingRedemptionEthValue, pendingRedemptionEthBase, pendingRedemptionBurnie, pendingRedemptionBurnieBase, redemptionPeriodIndex) |

### Open Question Resolution

**Q1: StakedDegenerusStonk internal storage** -- RESOLVED. `resolveRedemptionPeriod` reads/writes 7 variables in slots 8-14 as cataloged in section 1.5 above.

**Q2: DegenerusJackpots.sol external calls** -- RESOLVED. `payDailyJackpot` and `payDailyJackpotCoinAndTickets` do NOT make external calls to DegenerusJackpots.sol. The JackpotModule operates entirely via delegatecall on DegenerusGame storage. `runRewardJackpots` (EndgameModule) calls `runDecimatorJackpot` which is routed through `IDegenerusGame(address(this))` -- a self-call that resolves within DegenerusGame's own storage context. No separate DegenerusJackpots storage is touched by VRF-dependent paths.

**Q3: Degenerette (roulette) RNG dependency** -- RESOLVED. The Degenerette module reads `lootboxRngWordByIndex[index]` at resolution time (DegeneretteModule:597). Bets are placed via `placeFullTicketBets` which records `lootboxRngIndex` as the bet's RNG index (DegeneretteModule:473-475). Resolution reads the VRF word for that index. This is a 7th VRF-dependent outcome category, documented in the backward trace.

---

## Backward-Trace Catalog (CW-02)

This section traces BACKWARD from each player-visible outcome to every input variable that influenced the result. The question for each is: "What storage variables were inputs to this outcome, and when were they committed?"

This is independently derived from the outcome computation code, NOT a reshuffling of the forward-trace rows. Per project memory (`feedback_rng_backward_trace.md`): the forward trace follows the VRF word, but the backward trace follows ALL inputs to the outcome, including inputs set before VRF was requested.

### Category 1: Coinflip Win/Loss

**Outcome description:** Whether a player's daily coinflip wager is a win or loss (50/50), plus the bonus reward percent applied on wins. This determines whether deposited BURNIE is doubled or lost.

**Computation location:** `BurnieCoinflip.sol` lines 783-810

**Backward dependency chain:**

```
Win/Loss outcome (bool win)
  <- (rngWord & 1) == 1                           [BurnieCoinflip:810]
    <- rngWord (parameter to processCoinflipPayouts) [BurnieCoinflip:780]
      <- currentWord (after _applyDailyRng)         [AdvanceModule:796]
        <- rawWord + totalFlipReversals              [AdvanceModule:1521-1528]
          <- rngWordCurrent (from rawFulfillRandomWords) [AdvanceModule:1448]
            <- VRF random word (external, unknown)

Reward percent outcome (uint16 rewardPercent)
  <- seedWord % 20                                  [BurnieCoinflip:789]
    <- keccak256(rngWord, epoch)                     [BurnieCoinflip:784]
      <- rngWord (same as above)
      <- epoch (day index, parameter)
  <- bonusFlip && lootboxPresaleActive               [BurnieCoinflip:802-806]
    <- bonusFlip (parameter from advanceGame)
    <- lootboxPresaleActive (game storage)

Payout amount
  <- coinflipBalance[epoch][player]                  [BurnieCoinflip:810+ claim path]
    <- deposited by player via depositCoinflip
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| rngWordCurrent | DegenerusGameStorage | 4 | Source of VRF word; bit 0 = win/loss | At VRF fulfillment (rawFulfillRandomWords callback) |
| totalFlipReversals | DegenerusGameStorage | 6 | Added to VRF word, shifts bit 0 parity | Accumulates via reverseFlip() calls BEFORE rngLockedFlag is set; locked once daily RNG requested |
| coinflipBalance[epoch][player] | BurnieCoinflip | 0 (mapping) | Bet amount -- determines payout size on win | At player's depositCoinflip() call (before day resolution) |
| coinflipDayResult[epoch] | BurnieCoinflip | 1 (mapping) | Stores computed win/loss + rewardPercent | Written during processCoinflipPayouts (output, not input) |
| lootboxPresaleActive | DegenerusGameStorage | 21 | +6pp bonus on rewardPercent during presale | Set at deploy, cleared at level 3 or ETH cap |
| currentBounty | BurnieCoinflip | 3 | Bounty pool paid to bounty owner on win | Accumulates across days (1 BURNIE per resolved day) |
| bountyOwedTo | BurnieCoinflip | 4 (offset 0) | Recipient of bounty payout | Set by biggest flip record holder |
| epoch (day index) | Parameter | N/A | Mixed into keccak for rewardPercent seed | Derived from block.timestamp at advanceGame time |
| jackpotCounter | DegenerusGameStorage | 0 (offset 22) | bonusFlip = true when counter==0 in jackpot phase | Set during jackpot phase progression |
| level | DegenerusGameStorage | 0 (offset 18) | bonusFlip = true at level 0 | Set at game start / level transitions |

### Category 2: Jackpot ETH Winner Selection

**Outcome description:** Which player(s) win the daily ETH jackpot. Winners are selected from `traitBurnTicket` arrays based on VRF-derived trait selection. The pool is split across 4 trait-matched buckets with variable share percentages.

**Computation location:** `DegenerusGameJackpotModule.sol` lines 323-522 (payDailyJackpot) and lines 1310-1380+ (_executeJackpot)

**Backward dependency chain:**

```
Winner address
  <- traitBurnTicket[level][traitId][index]           [JackpotModule:2274]
    <- index = (entropy XOR trait XOR salt) % effectiveLen  [JackpotModule:2269-2273]
      <- entropy = randWord XOR (level << 192)         [JackpotModule:474]
        <- randWord = rngWord from advanceGame           [AdvanceModule:279]
      <- traitId = from _rollWinningTraits(lvl, randWord) [JackpotModule:2611-2626]
        <- randWord (same VRF word)
      <- effectiveLen = traitBurnTicket[lvl][trait].length + virtualDeityCount
        <- traitBurnTicket[lvl][trait] (ticket holders array)
        <- deityBySymbol[symId] (virtual deity entry check) [JackpotModule:2254-2259]

Payout amount per winner
  <- currentPrizePool * dailyBps / 10000               [JackpotModule:376]
    <- currentPrizePool (pool snapshot at jackpot start) [JackpotModule:365]
    <- dailyBps from _dailyCurrentPoolBps(counter, randWord) [JackpotModule:2657-2669]
      <- jackpotCounter (physical day within jackpot phase)
      <- randWord (VRF word for daily BPS randomization)
    <- shareBps[bucket] from JackpotBucketLib           [JackpotModule:504-508]
      <- entropy & 3 (solo bucket selection)
  <- compressedJackpotFlag (affects counterStep: turbo=all 5, compressed=2 days) [JackpotModule:353-361]
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| rngWordCurrent | DegenerusGameStorage | 4 | Source of all entropy for trait rolls, winner index, daily BPS | At VRF fulfillment |
| totalFlipReversals | DegenerusGameStorage | 6 | Modifies VRF word before use as randWord | Locked at daily RNG request (reverseFlip blocked by rngLockedFlag) |
| traitBurnTicket[level][traitId] | DegenerusGameStorage | 11 (mapping) | Winner pool -- determines who can win | Populated by processTicketBatch from ticketQueue entries |
| currentPrizePool | DegenerusGameStorage | 2 | Total pool budget for the level | Accumulated from purchases during the level |
| jackpotCounter | DegenerusGameStorage | 0 (offset 22) | Day within jackpot phase (affects BPS) | Incremented by payDailyJackpotCoinAndTickets each day |
| compressedJackpotFlag | DegenerusGameStorage | 1 (offset 0) | Turbo/compressed mode multiplier | Set when target reached on day 1 or 3 |
| deityBySymbol[symId] | DegenerusGameStorage | 54 (mapping) | Virtual deity ticket entries (2% of bucket) | Set at deity pass purchase time |
| level | DegenerusGameStorage | 0 (offset 18) | Determines which level's trait tickets are eligible | Set at level transition during RNG request |
| prizePoolsPacked | DegenerusGameStorage | 3 | Future pool source for carryover jackpot | Accumulated from prior levels |
| autoRebuyState[player] | DegenerusGameStorage | 36 (mapping) | Converts winnings to tickets instead of ETH | Set by player via setAutoRebuy |

### Category 3: Jackpot BURNIE (Coin) Winner

**Outcome description:** Which player(s) win the daily BURNIE jackpot. Similar trait-based selection as ETH jackpot but awards BURNIE tokens. Split 25% far-future (ticketQueue-based) and 75% near-future (trait-matched).

**Computation location:** `DegenerusGameJackpotModule.sol` lines 2361-2402 (payDailyCoinJackpot) and lines 681-766 (payDailyJackpotCoinAndTickets)

**Backward dependency chain:**

```
Near-future winner
  <- traitBurnTicket[targetLevel][traitId][index]      [JackpotModule:2460]
    <- targetLevel = lvl + (entropy % 5)                [JackpotModule:2411]
      <- entropy = randWord XOR (lvl << 192) XOR COIN_JACKPOT_TAG
        <- randWord from advanceGame
    <- traitId from _rollWinningTraits or stored lastDailyJackpotWinningTraits
    <- index from entropy-based modular selection

Far-future winner
  <- ticketQueue[readKey][index]                        [JackpotModule via _awardFarFutureCoinJackpot]
    <- index = entropy % ticketQueue length
    <- readKey derived from level + write slot

Payout amount
  <- levelPrizePool[lvl-1] * PRICE_COIN_UNIT / (price * 200) [JackpotModule:2651]
    <- levelPrizePool[lvl-1] (previous level target)
    <- price (current BURNIE price in ETH)
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| rngWordCurrent | DegenerusGameStorage | 4 | All entropy for trait rolls and winner selection | At VRF fulfillment |
| totalFlipReversals | DegenerusGameStorage | 6 | Modifies VRF word | Locked at daily RNG request |
| traitBurnTicket[level][traitId] | DegenerusGameStorage | 11 (mapping) | Near-future winner pool | Populated by processTicketBatch |
| ticketQueue[key] | DegenerusGameStorage | 15 (mapping) | Far-future winner pool (25% of BURNIE jackpot) | Populated by purchase/auto-rebuy; read slot swapped at RNG request via _swapAndFreeze |
| levelPrizePool[lvl-1] | DegenerusGameStorage | 45 (mapping) | Determines BURNIE budget | Set at previous level transition |
| price | DegenerusGameStorage | 1 (offset 7) | ETH-to-BURNIE conversion rate | Set at level transitions |
| lastDailyJackpotWinningTraits | DegenerusGameStorage | 42 (offset 0) | Reused traits from Phase 1 (payDailyJackpotCoinAndTickets) | Set during payDailyJackpot |
| lastDailyJackpotLevel | DegenerusGameStorage | 42 (offset 4) | Restored level for Phase 2 | Set during payDailyJackpot |
| jackpotPhaseFlag | DegenerusGameStorage | 0 (offset 21) | Determines trait roll mode (burn-weighted vs random) | Set at jackpot phase entry |

### Category 4: Lootbox Outcome

**Outcome description:** The target level and scaled amount for lootbox rewards when a player opens a lootbox. The lootbox RNG word determines the target level roll and all downstream reward calculations.

**Computation location:** `DegenerusGameLootboxModule.sol` lines 542-614 (openLootBox) and lines 622-641 (openBurnieLootBox)

**Backward dependency chain:**

```
Target level
  <- _rollTargetLevel(baseLevel, entropy)               [LootboxModule:571]
    <- entropy = keccak256(rngWord, player, day, amount) [LootboxModule:570]
      <- rngWord = lootboxRngWordByIndex[index]          [LootboxModule:549]
        <- VRF word stored by rawFulfillRandomWords (mid-day)
           OR _finalizeLootboxRng (daily)
           OR _backfillOrphanedLootboxIndices (stall recovery)
      <- player (msg.sender)
      <- day = lootboxDay[index][player]                 [LootboxModule:553]
      <- amount = lootboxEth[index][player]              [LootboxModule:544]

Scaled reward amount
  <- amount * evMultiplierBps / 10000                    [LootboxModule:583-588]
    <- evMultiplierBps from lootboxEvScorePacked[index][player] [LootboxModule:579-582]
      OR from _lootboxEvMultiplierBps(player) (runtime)
    <- amount from lootboxEth[index][player]

Base level (grace period)
  <- lootboxBaseLevelPacked[index][player]               [LootboxModule:566]
    <- stored at purchase time
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| lootboxRngWordByIndex[index] | DegenerusGameStorage | 64 (mapping) | Primary entropy for target level roll and all downstream | At VRF fulfillment (mid-day or daily path) |
| lootboxEth[index][player] | DegenerusGameStorage | 20 (mapping) | Bet amount -- determines reward scale | At purchase() time |
| lootboxEthBase[index][player] | DegenerusGameStorage | 43 (mapping) | Original ETH amount (pre-boost) | At purchase() time |
| lootboxDay[index][player] | DegenerusGameStorage | 65 (mapping) | Day of purchase, mixed into entropy | At purchase() time |
| lootboxBaseLevelPacked[index][player] | DegenerusGameStorage | 66 (mapping) | Grace period base level | At purchase() time |
| lootboxEvScorePacked[index][player] | DegenerusGameStorage | 67 (mapping) | Activity-based EV multiplier (80%-135%) | At purchase() time |
| lootboxBurnie[index][player] | DegenerusGameStorage | 68 (mapping) | BURNIE lootbox amount | At purchase() time |
| lootboxDistressEth[index][player] | DegenerusGameStorage | 99 (mapping) | Distress ETH component | At purchase() time |
| level | DegenerusGameStorage | 0 (offset 18) | Current level for target floor | Read at open time (not committed at purchase) |
| price | DegenerusGameStorage | 1 (offset 7) | BURNIE-to-ETH conversion for BURNIE lootboxes | Read at open time |
| lootboxPresaleActive | DegenerusGameStorage | 21 | Presale bonus on rewards | Read at open time |

### Category 5: Redemption Roll

**Outcome description:** The percentage applied to pending gambling burn redemptions. Range [25, 175], determines how much of the base ETH/BURNIE value each redeemer receives.

**Computation location:** `DegenerusGameAdvanceModule.sol` lines 804-805

**Backward dependency chain:**

```
Redemption percentage (uint16 redemptionRoll)
  <- ((currentWord >> 8) % 151) + 25                    [AdvanceModule:804-805]
    <- currentWord (after _applyDailyRng)                [AdvanceModule:795]
      <- rawWord + totalFlipReversals                    [AdvanceModule:1521-1528]
        <- rngWordCurrent (from rawFulfillRandomWords)   [AdvanceModule:1448]
          <- VRF random word

Resolved amounts
  <- pendingRedemptionEthBase * roll / 100               [StakedDegenerusStonk:545]
    <- pendingRedemptionEthBase (base ETH from burns)
  <- pendingRedemptionBurnieBase * roll / 100            [StakedDegenerusStonk:550]
    <- pendingRedemptionBurnieBase (base BURNIE from burns)

Resolution also stores flipDay for BURNIE coinflip dependency:
  <- flipDay = day + 1                                   [AdvanceModule:807]
    <- day (current day index from advanceGame)
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| rngWordCurrent | DegenerusGameStorage | 4 | Bits 8+ determine redemption roll | At VRF fulfillment |
| totalFlipReversals | DegenerusGameStorage | 6 | Shifts the word, can change bits 8+ | Locked at daily RNG request |
| pendingRedemptionEthBase | StakedDegenerusStonk | 11 | Base ETH value to scale by roll | Accumulated from burn() calls during the redemption period |
| pendingRedemptionBurnieBase | StakedDegenerusStonk | 12 | Base BURNIE value to scale by roll | Accumulated from burn() calls during the redemption period |
| pendingRedemptionEthValue | StakedDegenerusStonk | 9 | Total segregated ETH, adjusted by roll | Accumulated from burn() calls |
| pendingRedemptionBurnie | StakedDegenerusStonk | 10 | Total pending BURNIE, reduced by base | Accumulated from burn() calls |
| redemptionPeriodIndex | StakedDegenerusStonk | 14 | Period identifier for storage | Incremented at period boundary |
| redemptionPeriodBurned | StakedDegenerusStonk | 15 | Total sDGNRS burned in period | Accumulated from burn() calls |
| redemptionPeriodSupplySnapshot | StakedDegenerusStonk | 13 | Supply at period start | Set at first burn in period |

### Category 6: Prize Pool Consolidation and Future Take

**Outcome description:** How much ETH is moved from the next prize pool to the future prize pool at level transition. This affects the jackpot sizes for subsequent levels. The VRF word introduces randomized variance.

**Computation location:** `DegenerusGameAdvanceModule.sol` lines 1026-1101 (_applyTimeBasedFutureTake) and `DegenerusGameJackpotModule.sol` lines 879-908 (consolidatePrizePools)

**Backward dependency chain:**

```
Future take amount
  <- (nextPool * bps / 10000) with +/-25% variance      [AdvanceModule:1069-1091]
    <- bps = _nextToFutureBps(elapsed, lvl)              [AdvanceModule:1034]
      + ratioAdjust (future:next ratio vs 2:1 target)    [AdvanceModule:1042-1050]
      + overshootSurcharge (if next > 1.25x previous)    [AdvanceModule:1053-1063]
      + rngWord % (ADDITIVE_RANDOM_BPS + 1)              [AdvanceModule:1066]
        <- rngWord from advanceGame

    Variance
    <- roll1 = (rngWord >> 64) % range                   [AdvanceModule:1082]
    <- roll2 = (rngWord >> 192) % range                  [AdvanceModule:1083]
      <- rngWord from advanceGame

consolidatePrizePools (x00 levels):
  <- _futureKeepBps(rngWord)                             [JackpotModule:1282-1297]
    <- keccak256(rngWord, FUTURE_KEEP_TAG)
    <- rngWord from advanceGame

Yield distribution (consolidatePrizePools):
  <- _distributeYieldSurplus(rngWord)                    [JackpotModule:912]
    <- stETH balance vs obligations                      [JackpotModule:913-921]
    <- rngWord (for auto-rebuy entropy)
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| rngWordCurrent | DegenerusGameStorage | 4 | Additive random BPS + variance rolls + future keep BPS | At VRF fulfillment |
| totalFlipReversals | DegenerusGameStorage | 6 | Modifies VRF word | Locked at daily RNG request |
| prizePoolsPacked | DegenerusGameStorage | 3 | Source next + future pool values | Accumulated across level from purchases |
| currentPrizePool | DegenerusGameStorage | 2 | Merged with next pool during consolidation | Accumulated across level |
| levelStartTime | DegenerusGameStorage | 0 (offset 0) | Time-based BPS calculation (faster = lower skim) | Set at level transition |
| levelPrizePool[lvl-1] | DegenerusGameStorage | 45 (mapping) | Overshoot surcharge reference | Set at previous level |
| level | DegenerusGameStorage | 0 (offset 18) | x9 bonus, x00 yield dump | Set at level transition |
| yieldAccumulator | DegenerusGameStorage | 100 | Insurance skim accumulation + x00 dump | Accumulated from insurance skims |
| autoRebuyState[addr] | DegenerusGameStorage | 36 (mapping) | Yield surplus auto-rebuy conversion | Set by players |
| claimableWinnings[addr] | DegenerusGameStorage | 9 (mapping) | Yield surplus credits | Prior state |
| claimablePool | DegenerusGameStorage | 10 | Yield surplus aggregate tracking | Prior state |

### Category 7: Degenerette (Roulette) Outcome

**Outcome description:** The result ticket for Degenerette full-ticket bets. A player commits a custom ticket; the VRF-derived result ticket determines matches (0-8 symbols) and payout.

**Computation location:** `DegenerusGameDegeneretteModule.sol` lines 585-667 (_resolveFullTicketBet)

**Backward dependency chain:**

```
Result ticket
  <- DegenerusTraitUtils.packedTraitsFromSeed(resultSeed)  [DegeneretteModule:619]
    <- resultSeed = keccak256(rngWord, index, QUICK_PLAY_SALT)  [DegeneretteModule:617]
      <- rngWord = lootboxRngWordByIndex[index]             [DegeneretteModule:597]
        <- VRF word stored by rawFulfillRandomWords (mid-day path)
           OR _finalizeLootboxRng (daily path)
      <- index = packed >> FT_INDEX_SHIFT                    [DegeneretteModule:591]
        <- stored from lootboxRngIndex at bet placement time [DegeneretteModule:473]

Match count
  <- _countMatches(playerTicket, resultTicket)            [DegeneretteModule:625]
    <- playerTicket = customTicket from packed bet         [DegeneretteModule:587]

Payout amount
  <- _fullTicketPayout(playerTicket, resultTicket, matches, currency, amount, roiBps, ...) [DegeneretteModule:628-638]
    <- roiBps = _roiBpsFromScore(activityScore)            [DegeneretteModule:602]
      <- activityScore stored in packed bet at placement time [DegeneretteModule:480]
    <- amountPerTicket from packed bet                      [DegeneretteModule:590]
    <- heroQuadrant from packed bet                         [DegeneretteModule:595]
```

**All input variables:**

| Variable | Contract/Storage | Slot | How It Influences Outcome | Committed When? |
|---|---|---|---|---|
| lootboxRngWordByIndex[index] | DegenerusGameStorage | 64 (mapping) | Primary entropy for result ticket derivation | At VRF fulfillment (mid-day or daily) |
| degeneretteBets[player][nonce] | DegenerusGameStorage | 83 (mapping) | Packed bet data: ticket, amount, currency, index, activity score, hero | At placeFullTicketBets() time |
| degeneretteBetNonce[player] | DegenerusGameStorage | 84 (mapping) | Bet identifier | At placeFullTicketBets() time |
| lootboxRngIndex | DegenerusGameStorage | 60 | Determines which RNG word maps to this bet | Read at bet placement time |
| claimableWinnings[player] | DegenerusGameStorage | 9 (mapping) | ETH payout from claimable (if bet uses claimable) | Prior state at bet time |
| claimablePool | DegenerusGameStorage | 10 | Deducted if bet uses claimable ETH | Prior state at bet time |
| currentPrizePool | DegenerusGameStorage | 2 | ETH payout cap (10% of pool per win) | Current state at resolution time |
| prizePoolsPacked | DegenerusGameStorage | 3 | Next pool receives bet ETH | Current state at bet time |
| prizePoolFrozen | DegenerusGameStorage | 1 (offset 25) | Directs ETH to pending pools if frozen | Current state at bet time |
| prizePoolPendingPacked | DegenerusGameStorage | 14 | Accumulates bet ETH while frozen | Current state at bet time |
| dailyHeroWagers[day][quadrant] | DegenerusGameStorage | 96 (mapping) | Hero wager tracking (not outcome-affecting) | At bet time |
| playerDegeneretteEthWagered[player][lvl] | DegenerusGameStorage | 97 (mapping) | Per-player per-level ETH wagered (leaderboard) | At bet time |
| topDegeneretteByLevel[lvl] | DegenerusGameStorage | 98 (mapping) | Top wagerer leaderboard | At bet time |

### Backward Trace: Variables Found by Backward Trace NOT in Forward Trace

The following variables are inputs to VRF-dependent outcomes but were NOT directly touched by the forward trace from `rawFulfillRandomWords`. They were committed by player actions or game state changes at earlier points:

| Variable | Contract/Storage | Slot | Outcome Category | Why Forward Trace Missed It |
|---|---|---|---|---|
| coinflipBalance[epoch][player] | BurnieCoinflip | 0 (mapping) | Cat 1: Coinflip | Committed by depositCoinflip(), not written during VRF processing |
| lootboxEth[index][player] | DegenerusGameStorage | 20 (mapping) | Cat 4: Lootbox | Committed by purchase(), read at open time, not during VRF processing |
| lootboxEthBase[index][player] | DegenerusGameStorage | 43 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| lootboxDay[index][player] | DegenerusGameStorage | 65 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| lootboxBaseLevelPacked[index][player] | DegenerusGameStorage | 66 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| lootboxEvScorePacked[index][player] | DegenerusGameStorage | 67 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| lootboxBurnie[index][player] | DegenerusGameStorage | 68 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| lootboxDistressEth[index][player] | DegenerusGameStorage | 99 (mapping) | Cat 4: Lootbox | Same -- purchase-time commitment |
| degeneretteBets[player][nonce] | DegenerusGameStorage | 83 (mapping) | Cat 7: Degenerette | Committed by placeFullTicketBets(), not written during VRF |
| degeneretteBetNonce[player] | DegenerusGameStorage | 84 (mapping) | Cat 7: Degenerette | Same -- bet-time commitment |
| pendingRedemptionEthBase | StakedDegenerusStonk | 11 | Cat 5: Redemption | Accumulated by burn() calls during period |
| pendingRedemptionBurnieBase | StakedDegenerusStonk | 12 | Cat 5: Redemption | Same |
| redemptionPeriodSupplySnapshot | StakedDegenerusStonk | 13 | Cat 5: Redemption | Set at first burn in period |
| redemptionPeriodBurned | StakedDegenerusStonk | 15 | Cat 5: Redemption | Accumulated by burns |
| dailyHeroWagers[day][quadrant] | DegenerusGameStorage | 96 (mapping) | Cat 7: Degenerette | Tracking only, committed at bet time |
| playerDegeneretteEthWagered[player][lvl] | DegenerusGameStorage | 97 (mapping) | Cat 7: Degenerette | Tracking only, committed at bet time |
| topDegeneretteByLevel[lvl] | DegenerusGameStorage | 98 (mapping) | Cat 7: Degenerette | Tracking only, committed at bet time |

This table demonstrates the value of backward tracing -- 17 variables that influence VRF-dependent outcomes were NOT directly visited by the forward trace from `rawFulfillRandomWords`. These are the variables that, if mutable during the commitment window, could allow outcome manipulation.

---

## Mutation Surface Catalog (CW-03)

This section catalogs every external/public function that can write to each variable from the Forward-Trace (CW-01) and Backward-Trace (CW-02) catalogs. For each mutation path, the call-graph depth and access control are recorded.

**Methodology:**

For each cataloged variable:
1. All contracts are searched for assignments (not just the module that reads it), because all delegatecall modules share DegenerusGameStorage.
2. Each write is traced backward to the nearest external/public function entry point.
3. Call-graph depth is recorded:
   - **D0 (direct):** External function directly writes the variable
   - **D1 (one hop):** External -> internal that writes
   - **D2 (two hops):** External -> internal -> internal that writes
   - **D3+ (deep):** Three or more hops
4. Access control is noted: **permissionless** (any caller), **admin-only** (DegenerusAdmin owner), **game-only** (only DegenerusGame contract), **VRF-only** (only VRF coordinator).

### Ticket Queue Double-Buffer Commitment Boundary

**CRITICAL: The `_swapAndFreeze` / `_swapTicketSlot` commitment boundary.**

The ticket queue uses a double-buffer controlled by `ticketWriteSlot` (slot 1, offset 23). The TICKET_SLOT_BIT (1 << 23) is XORed into the uint24 level key to create separate read and write keys:
- **Write key:** `level ^ (ticketWriteSlot == 1 ? TICKET_SLOT_BIT : 0)` -- where new purchases write
- **Read key:** `level ^ (ticketWriteSlot == 0 ? TICKET_SLOT_BIT : 0)` -- where processTicketBatch reads

**When `_swapAndFreeze` is called** (at daily RNG request time in advanceGame, AdvanceModule:230):
1. `_swapTicketSlot(purchaseLevel)` is called (DegenerusGameStorage:713-718)
2. Guard: the read slot must be empty (`ticketQueue[readKey].length != 0` reverts)
3. `ticketWriteSlot ^= 1` -- flips the write/read buffers
4. `ticketsFullyProcessed = false` -- marks the new read slot as not yet processed
5. If not already frozen: `prizePoolFrozen = true`, `prizePoolPendingPacked = 0`

**When `_swapTicketSlot` is called** (at mid-day lootbox RNG request in requestLootboxRng, AdvanceModule:717):
1. Same buffer swap as above, but WITHOUT prize pool freeze
2. `midDayTicketRngPending = true` is set (AdvanceModule:718)

**What happens if `purchase()` is called between swap and fulfillment:**
- New purchases write to the NEW write slot (post-swap), which is separate from the read slot being processed
- The read slot (old write slot) contains tickets committed before the swap
- **The double-buffer protects `ticketQueue` reads:** processTicketBatch reads from the old buffer, new purchases go to the new buffer
- **BUT:** purchases still write to `currentPrizePool` (or `prizePoolPendingPacked` if frozen) and other shared variables. The prize pool freeze mitigates current pool mutation during jackpot, but `prizePoolPendingPacked` accumulation continues.

### 3.1 DegenerusGameStorage Variables

#### rngWordCurrent (slot 4)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| rawFulfillRandomWords() | AdvanceModule (delegatecall) | rawFulfillRandomWords -> rngWordCurrent = word | D0 | VRF-only (msg.sender == vrfCoordinator) | Daily RNG path: stores VRF word |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> rngWordCurrent = 0 | D0 | permissionless | Clears to 0 before mid-day VRF request |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _applyDailyRng -> rngWordCurrent = finalWord | D2 | permissionless | Applies nudges and stores final word |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _unlockRng -> rngWordCurrent = 0 | D1 | permissionless | Cleared at end of daily processing |
| executeSwap() (governance) | AdvanceModule (delegatecall) | executeSwap -> configureVrf -> clears VRF state | D1 | governance (sDGNRS vote) | VRF coordinator swap resets all VRF state |

#### vrfRequestId (slot 5)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| rawFulfillRandomWords() | AdvanceModule (delegatecall) | rawFulfillRandomWords -> vrfRequestId read (guard) | D0 | VRF-only | Read-only in daily path; cleared in mid-day path |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> vrfRequestId = id | D0 | permissionless | Stores new VRF request ID |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> vrfRequestId = id | D1 | permissionless | Stores daily VRF request ID |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _unlockRng -> vrfRequestId = 0 | D1 | permissionless | Cleared at end of processing |
| configureVrf() | AdvanceModule (delegatecall) | configureVrf -> resets VRF state | D0 | admin-only (msg.sender == ADMIN) | Initial VRF setup |

#### totalFlipReversals (slot 6)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| reverseFlip() | AdvanceModule (delegatecall) | reverseFlip -> totalFlipReversals = newCount | D0 | permissionless | **CRITICAL:** Player-callable. Guard: `if (rngLockedFlag) revert RngLocked()`. Only callable when rngLockedFlag is false (purchase phase). Burns BURNIE as cost. |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _applyDailyRng -> totalFlipReversals = 0 | D2 | permissionless | Reset to 0 after applying nudges to VRF word |

**Commitment window analysis for reverseFlip():** The `rngLockedFlag` guard prevents reverseFlip during the daily RNG commitment window (rngLockedFlag is set true at `_swapAndFreeze` call in advanceGame). However, for the mid-day lootbox RNG path, `rngLockedFlag` remains FALSE. reverseFlip() can be called during a mid-day VRF request. This does not affect lootbox outcomes (lootbox RNG does not use totalFlipReversals), but it does affect the NEXT daily RNG word if daily processing occurs before the accumulated reversals are consumed.

#### rngWordByDay[day] (slot 13, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _applyDailyRng -> rngWordByDay[day] = finalWord | D2 | permissionless | Archive final word per day |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _backfillGapDays -> rngWordByDay[gapDay] = derived | D2 | permissionless | Backfill derived words for stall gaps |

#### ticketQueue[key] (slot 15, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> _queueTickets -> ticketQueue[wk].push(buyer) | D2 | permissionless | Writes to WRITE slot. Protected by double-buffer. |
| purchaseCoin() | MintModule (delegatecall) | purchaseCoin -> _purchaseCoinFor -> _queueTicketsScaled -> ticketQueue[wk].push(buyer) | D2 | permissionless | Writes to WRITE slot. Protected by double-buffer. |
| purchaseLazyPass() | WhaleModule (delegatecall) | purchaseLazyPass -> _purchaseLazyPass -> _queueTickets -> ticketQueue[wk].push(buyer) | D2 | permissionless | Writes to WRITE slot. Protected by double-buffer. |
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _purchaseDeityPass -> _queueTickets -> ticketQueue[wk].push(buyer) | D2 | permissionless | Writes to WRITE slot. Protected by double-buffer. |
| claimWhalePass() | EndgameModule (delegatecall) | claimWhalePass -> _queueTickets -> ticketQueue[wk].push(buyer) | D1 | permissionless | Writes to WRITE slot. Protected by double-buffer. |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> payDailyJackpot -> _addClaimableEth -> _processAutoRebuy -> _queueTickets -> ticketQueue[wk].push | D3+ | permissionless | Auto-rebuy: writes to WRITE slot |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> processTicketBatch -> reads ticketQueue[readKey]; deletes on completion | D1 | permissionless | Reads from READ slot. Clears after processing. |

#### ticketsOwedPacked[key][player] (slot 16, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> _queueTickets -> ticketsOwedPacked[wk][buyer] += quantity | D2 | permissionless | Writes to WRITE slot key |
| purchaseCoin() | MintModule (delegatecall) | purchaseCoin -> _purchaseCoinFor -> _queueTicketsScaled -> ticketsOwedPacked[wk][buyer] update | D2 | permissionless | Writes to WRITE slot key |
| purchaseLazyPass() | WhaleModule (delegatecall) | purchaseLazyPass -> _purchaseLazyPass -> _queueTickets -> ticketsOwedPacked[wk][buyer] += | D2 | permissionless | Writes to WRITE slot key |
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _purchaseDeityPass -> _queueTickets -> ticketsOwedPacked[wk][buyer] += | D2 | permissionless | Writes to WRITE slot key |
| claimWhalePass() | EndgameModule (delegatecall) | claimWhalePass -> _queueTickets -> ticketsOwedPacked[wk][buyer] += | D1 | permissionless | Writes to WRITE slot key |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> processTicketBatch -> ticketsOwedPacked[rk][player] update/clear | D1 | permissionless | Reads/clears from READ slot key |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> payDailyJackpot -> _processAutoRebuy -> _queueTickets -> ticketsOwedPacked[wk] += | D3+ | permissionless | Auto-rebuy: writes to WRITE slot |

#### traitBurnTicket[level][traitId] (slot 11, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | JackpotModule/MintModule (delegatecall) | advanceGame -> processTicketBatch -> _raritySymbolBatch -> assembly sstore (traitBurnTicket[lvl][traitId].push) | D2 | permissionless | Written during ticket processing via assembly. Only written by processTicketBatch which reads from the READ slot. |

**No permissionless external function directly writes traitBurnTicket outside of processTicketBatch.** The trait ticket arrays are only populated during ticket batch processing, which occurs during advanceGame. This is a game-internal write path.

#### currentPrizePool (slot 2)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> adds to currentPrizePool or prizePoolPendingPacked | D1 | permissionless | Direct pool addition (or pending if frozen) |
| purchaseLazyPass() | WhaleModule (delegatecall) | purchaseLazyPass -> _purchaseLazyPass -> adds to currentPrizePool or pending | D1 | permissionless | Direct pool addition (or pending if frozen) |
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _purchaseDeityPass -> adds to currentPrizePool or pending | D1 | permissionless | Direct pool addition (or pending if frozen) |
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> adds bet ETH to currentPrizePool or pending | D1 | permissionless | Degenerette bet ETH flows to prize pool |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> payDailyJackpot -> currentPrizePool -= dailyBudget | D1 | permissionless | Deducted during jackpot payouts |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> _consolidatePrizePools -> currentPrizePool += nextPool | D1 | permissionless | Merge next into current at level transition |
| advanceGame() | GameOverModule (delegatecall) | gameOver -> currentPrizePool = 0 | D1 | permissionless | Zeroed at game over |

**Freeze protection:** During jackpot phase, `prizePoolFrozen = true`. New purchase revenue goes to `prizePoolPendingPacked` instead of `currentPrizePool`. The freeze is activated by `_swapAndFreeze` at daily RNG request time and lifted by `_unfreezePool` after jackpot processing.

#### prizePoolsPacked (slot 3)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> _setNextPrizePool / _setFuturePrizePool via helper | D1 | permissionless | Adds future pool share (10% via PURCHASE_TO_FUTURE_BPS) |
| purchaseLazyPass() | WhaleModule (delegatecall) | purchaseLazyPass -> _purchaseLazyPass -> _setFuturePrizePool | D1 | permissionless | Adds future pool share |
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _purchaseDeityPass -> _setFuturePrizePool | D1 | permissionless | Adds future pool share |
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> adds bet ETH to next pool (or pending) | D1 | permissionless | Bet ETH to next pool |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> _consolidatePrizePools / _applyTimeBasedFutureTake -> prizePoolsPacked update | D1 | permissionless | Level transition consolidation |
| advanceGame() | EndgameModule (delegatecall) | advanceGame -> _runRewardJackpots -> prizePoolsPacked (future pool deductions) | D1 | permissionless | BAF/Decimator budget from future pool |
| claimDecimatorJackpot() | DecimatorModule (delegatecall) | claimDecimatorJackpot -> prizePoolsPacked (future pool for lootbox portion) | D1 | permissionless | Decimator claim lootbox portion from future pool |

#### prizePoolPendingPacked (slot 14)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> _addPendingNext / _addPendingFuture | D1 | permissionless | Accumulates during freeze |
| purchaseLazyPass() | WhaleModule (delegatecall) | purchaseLazyPass -> _addPendingFuture | D1 | permissionless | Accumulates during freeze |
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _addPendingFuture | D1 | permissionless | Accumulates during freeze |
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> _addPendingNext | D1 | permissionless | Bet ETH to pending during freeze |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _swapAndFreeze -> prizePoolPendingPacked = 0 | D1 | permissionless | Zeroed at freeze start (if not already frozen) |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _unfreezePool -> prizePoolPendingPacked = 0 | D1 | permissionless | Zeroed at unfreeze |

#### claimableWinnings[addr] (slot 9, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> payDailyJackpot -> claimableWinnings[winner] += payout | D1 | permissionless | Credits ETH jackpot winners |
| advanceGame() | EndgameModule (delegatecall) | advanceGame -> _runRewardJackpots -> claimableWinnings[winner] += | D1 | permissionless | BAF/Decimator reward credits |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> _consolidatePrizePools -> _distributeYieldSurplus -> claimableWinnings[addr] += | D2 | permissionless | Yield surplus distribution |
| claimWinnings() | DegenerusGame (direct) | claimWinnings -> claimableWinnings[player] = 1 (sentinel) | D0 | permissionless | Player claims ETH, balance zeroed to sentinel |
| purchase() | MintModule (delegatecall) | purchase -> claimableWinnings[player] -= (if MintPaymentKind.Claimable) | D0 | permissionless | Player spends claimable on purchase |
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> claimableWinnings[player] -= fromClaimable | D0 | permissionless | Player spends claimable on Degenerette bet |
| openLootBox() | LootboxModule (delegatecall) | openLootBox -> _creditClaimable -> claimableWinnings[player] += | D1 | permissionless | Lootbox win credits |

#### claimablePool (slot 10)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> payDailyJackpot -> claimablePool += | D1 | permissionless | Aggregate liability tracking |
| advanceGame() | EndgameModule (delegatecall) | advanceGame -> _runRewardJackpots -> claimablePool += | D1 | permissionless | BAF/Decimator liability |
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> _consolidatePrizePools -> _distributeYieldSurplus -> claimablePool += | D2 | permissionless | Yield surplus liability |
| claimWinnings() | DegenerusGame (direct) | claimWinnings -> claimablePool -= payout | D0 | permissionless | Reduced on claim |
| purchase() | MintModule (delegatecall) | purchase -> claimablePool -= claimableUsed | D0 | permissionless | Reduced when claimable spent on purchase |
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> claimablePool -= fromClaimable | D0 | permissionless | Reduced when claimable spent on bet |
| claimDecimatorJackpot() | DecimatorModule (delegatecall) | claimDecimatorJackpot -> claimablePool -= | D0 | permissionless | Decimator claim reduces pool |
| openLootBox() | LootboxModule (delegatecall) | openLootBox -> claimablePool += | D1 | permissionless | Lootbox win adds to pool |

#### lootboxRngWordByIndex[index] (slot 64, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| rawFulfillRandomWords() | AdvanceModule (delegatecall) | rawFulfillRandomWords -> lootboxRngWordByIndex[index] = word | D0 | VRF-only | Mid-day path: direct store |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _finalizeLootboxRng -> lootboxRngWordByIndex[index] = word | D2 | permissionless | Daily path: derived from daily VRF word |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _backfillOrphanedLootboxIndices -> lootboxRngWordByIndex[i] = derived | D2 | permissionless | Stall recovery: fill orphaned indices |

#### lastLootboxRngWord (slot 70)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _finalizeLootboxRng -> lastLootboxRngWord = word | D2 | permissionless | Updated to latest lootbox entropy |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _backfillOrphanedLootboxIndices -> lastLootboxRngWord = derived | D2 | permissionless | Updated during stall recovery |

#### midDayTicketRngPending (slot 71)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> midDayTicketRngPending = true | D0 | permissionless | Set when ticket buffer swapped during mid-day RNG |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> midDayTicketRngPending = false (cleared during processing) | D0 | permissionless | Cleared after processing |

#### lootboxRngIndex (slot 60)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> lootboxRngIndex++ | D0 | permissionless | Incremented for next mid-day RNG |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> lootboxRngIndex++ | D1 | permissionless | Incremented for daily RNG |

#### lastVrfProcessedTimestamp (slot 103)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> rngGate -> _applyDailyRng -> lastVrfProcessedTimestamp = uint48(block.timestamp) | D2 | permissionless | Records VRF processing time |
| configureVrf() | AdvanceModule (delegatecall) | configureVrf -> lastVrfProcessedTimestamp = uint48(block.timestamp) | D0 | admin-only | Initial VRF setup |

#### ticketWriteSlot (slot 1, offset 23)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _swapAndFreeze -> _swapTicketSlot -> ticketWriteSlot ^= 1 | D2 | permissionless | Daily RNG request buffer swap |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> _swapTicketSlot -> ticketWriteSlot ^= 1 | D1 | permissionless | Mid-day RNG request buffer swap |

#### rngLockedFlag (slot 0, offset 26)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> rngLockedFlag = true | D1 | permissionless | Set at daily RNG request |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _unlockRng -> rngLockedFlag = false | D1 | permissionless | Cleared after daily processing |
| executeSwap() (governance) | AdvanceModule (delegatecall) | executeSwap -> configureVrf -> rngLockedFlag = false | D1 | governance | VRF coordinator swap resets lock |

#### jackpotPhaseFlag (slot 0, offset 21)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> jackpotPhaseFlag = true/false | D0 | permissionless | Toggled during phase transitions |

#### level (slot 0, offset 18)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> level++ | D1 | permissionless | Incremented at level transitions |

#### price (slot 1, offset 7)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> price = newPrice | D1 | permissionless | Set at level transitions |

#### yieldAccumulator (slot 100)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> _consolidatePrizePools -> yieldAccumulator += / read+dump | D1 | permissionless | Insurance skim accumulation |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _applyTimeBasedFutureTake -> yieldAccumulator += | D1 | permissionless | Insurance skim from future take |
| advanceGame() | GameOverModule (delegatecall) | gameOver -> yieldAccumulator = 0 | D1 | permissionless | Zeroed at game over |

#### autoRebuyState[player] (slot 36, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| setAutoRebuy() | DegenerusGame (direct) | setAutoRebuy -> _setAutoRebuy -> autoRebuyState[player] update | D1 | permissionless | Player sets auto-rebuy preference |
| setAutoRebuyTakeProfit() | DegenerusGame (direct) | setAutoRebuyTakeProfit -> autoRebuyState[player] update | D1 | permissionless | Player sets take-profit threshold |

#### deityBySymbol[symId] (slot 54, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchaseDeityPass() | WhaleModule (delegatecall) | purchaseDeityPass -> _purchaseDeityPass -> deityBySymbol[symId] = buyer | D1 | permissionless | Set at deity pass purchase time |

#### lootboxRngPendingEth (slot 61)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxRngPendingEth += lootBoxAmount | D1 | permissionless | Accumulates pending lootbox ETH |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> lootboxRngPendingEth = 0 | D0 | permissionless | Cleared at mid-day RNG request |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> lootboxRngPendingEth = 0 | D1 | permissionless | Cleared at daily RNG request |

#### lootboxRngPendingBurnie (slot 69)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchaseBurnieLootbox() | MintModule (delegatecall) | purchaseBurnieLootbox -> _purchaseBurnieLootboxFor -> lootboxRngPendingBurnie += | D1 | permissionless | Accumulates pending BURNIE lootbox |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> lootboxRngPendingBurnie = 0 | D0 | permissionless | Cleared at mid-day RNG request |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _finalizeRngRequest -> lootboxRngPendingBurnie = 0 | D1 | permissionless | Cleared at daily RNG request |

#### ticketsFullyProcessed (slot 1, offset 24)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | JackpotModule (delegatecall) | advanceGame -> processTicketBatch -> ticketsFullyProcessed = true (when queue drained) | D1 | permissionless | Set after read slot fully processed |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _swapAndFreeze -> _swapTicketSlot -> ticketsFullyProcessed = false | D2 | permissionless | Reset on buffer swap |
| requestLootboxRng() | AdvanceModule (delegatecall) | requestLootboxRng -> _swapTicketSlot -> ticketsFullyProcessed = false | D1 | permissionless | Reset on mid-day buffer swap |

#### prizePoolFrozen (slot 1, offset 25)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _swapAndFreeze -> prizePoolFrozen = true | D1 | permissionless | Activated at daily RNG request |
| advanceGame() | AdvanceModule (delegatecall) | advanceGame -> _unfreezePool -> prizePoolFrozen = false | D1 | permissionless | Deactivated after jackpot processing |

#### Backward-Trace-Only Variables (from Plan 01 CW-02)

#### lootboxEth[index][player] (slot 20, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxEth[index][buyer] = packed | D1 | permissionless | Written at purchase time with amount + level |
| openLootBox() | LootboxModule (delegatecall) | openLootBox -> lootboxEth[index][player] = 0 (cleared) | D0 | permissionless | Cleared on open |

#### lootboxEthBase[index][player] (slot 43, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxEthBase[index][buyer] = originalAmount | D1 | permissionless | Written at purchase time |

#### lootboxDay[index][player] (slot 65, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxDay[index][buyer] = day | D1 | permissionless | Written at purchase time |

#### lootboxBaseLevelPacked[index][player] (slot 66, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxBaseLevelPacked[index][buyer] = packed | D1 | permissionless | Written at purchase time |

#### lootboxEvScorePacked[index][player] (slot 67, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxEvScorePacked[index][buyer] = packed | D1 | permissionless | Written at purchase time |

#### lootboxBurnie[index][player] (slot 68, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchaseBurnieLootbox() | MintModule (delegatecall) | purchaseBurnieLootbox -> _purchaseBurnieLootboxFor -> lootboxBurnie[index][buyer] = amount | D1 | permissionless | Written at purchase time |
| openBurnieLootBox() | LootboxModule (delegatecall) | openBurnieLootBox -> lootboxBurnie[index][player] = 0 | D0 | permissionless | Cleared on open |

#### lootboxDistressEth[index][player] (slot 99, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| purchase() | MintModule (delegatecall) | purchase -> _purchaseFor -> lootboxDistressEth[index][buyer] = amount (if distress mode) | D1 | permissionless | Written at purchase time during distress |

#### degeneretteBets[player][nonce] (slot 83, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> degeneretteBets[player][nonce] = packed | D0 | permissionless | Written at bet placement time |
| _resolveFullTicketBet() | DegeneretteModule (delegatecall) | openLootBox (internal trigger) -> _resolveFullTicketBet -> delete degeneretteBets[player][betId] | D1 | permissionless | Cleared on resolution |

#### degeneretteBetNonce[player] (slot 84, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> degeneretteBetNonce[player]++ | D0 | permissionless | Incremented at bet placement |

#### dailyHeroWagers[day][quadrant] (slot 96, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> dailyHeroWagers[day][quadrant] += | D0 | permissionless | Tracking only; not outcome-affecting |

#### playerDegeneretteEthWagered[player][lvl] (slot 97, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> playerDegeneretteEthWagered[player][lvl] += | D0 | permissionless | Leaderboard tracking; not outcome-affecting |

#### topDegeneretteByLevel[lvl] (slot 98, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| placeFullTicketBets() | DegeneretteModule (delegatecall) | placeFullTicketBets -> topDegeneretteByLevel[lvl] update | D0 | permissionless | Leaderboard tracking; not outcome-affecting |

### 3.2 BurnieCoinflip Variables

#### coinflipBalance[epoch][player] (slot 0, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| depositCoinflip() | BurnieCoinflip (direct) | depositCoinflip -> _depositCoinflip -> _addDailyFlip -> coinflipBalance[day][player] += amount | D2 | permissionless | **CRITICAL:** Player-callable. Burns BURNIE and credits flip balance for future day. No rngLockedFlag guard on BurnieCoinflip. |
| creditFlip() | BurnieCoinflip (direct) | creditFlip -> _addDailyFlip -> coinflipBalance[day][player] += | D1 | game-only (onlyFlipCreditors: DegenerusGame or BurnieCoin) | Game credits flips (quest rewards, auto-rebuy) |
| creditFlipBatch() | BurnieCoinflip (direct) | creditFlipBatch -> _addDailyFlip -> coinflipBalance[day][player] += | D1 | game-only (onlyFlipCreditors) | Batch flip credits |

#### coinflipDayResult[epoch] (slot 1, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| processCoinflipPayouts() | BurnieCoinflip (direct) | processCoinflipPayouts -> coinflipDayResult[epoch] = result | D0 | game-only (onlyDegenerusGameContract) | Written during daily RNG processing. Only callable by DegenerusGame. |

#### playerState[player] (slot 2, mapping)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| depositCoinflip() | BurnieCoinflip (direct) | depositCoinflip -> _depositCoinflip -> _addDailyFlip -> playerState[player] update | D2 | permissionless | Updates auto-rebuy cursor and state |
| claimCoinflips() | BurnieCoinflip (direct) | claimCoinflips -> _claimCoinflipsInternal -> playerState[player] update | D1 | permissionless | Updates claim cursor |
| setCoinflipAutoRebuy() | BurnieCoinflip (direct) | setCoinflipAutoRebuy -> playerState[player].autoRebuyEnabled = enabled | D0 | permissionless | Player sets auto-rebuy preference |
| processCoinflipPayouts() | BurnieCoinflip (direct) | processCoinflipPayouts -> _claimCoinflipsInternal -> playerState[player] update | D1 | game-only | sDGNRS auto-claim during processing |

#### currentBounty (slot 3, offset 0)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| processCoinflipPayouts() | BurnieCoinflip (direct) | processCoinflipPayouts -> currentBounty update | D0 | game-only | Accumulates or pays out bounty |

#### bountyOwedTo (slot 4, offset 0)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| processCoinflipPayouts() | BurnieCoinflip (direct) | processCoinflipPayouts -> bountyOwedTo cleared | D0 | game-only | Cleared after bounty payment |
| depositCoinflip() | BurnieCoinflip (direct) | depositCoinflip -> _depositCoinflip -> sets biggestFlipEver/bountyOwedTo if record | D1 | permissionless | Set when player makes largest-ever flip |

#### flipsClaimableDay (slot 4, offset 20)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| processCoinflipPayouts() | BurnieCoinflip (direct) | processCoinflipPayouts -> flipsClaimableDay = epoch | D0 | game-only | Advanced during daily resolution |

### 3.3 StakedDegenerusStonk Variables

#### pendingRedemptionEthBase (slot 11)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> pendingRedemptionEthBase += ethBase | D1 | permissionless | **Guard:** `if (game.rngLocked()) revert BurnsBlockedDuringRng()`. Blocked during daily RNG window. |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> pendingRedemptionEthBase += | D1 | permissionless | Same rngLocked guard |
| resolveRedemptionPeriod() | StakedDegenerusStonk (direct) | resolveRedemptionPeriod -> pendingRedemptionEthBase = 0 | D0 | game-only (msg.sender == GAME) | Cleared during resolution |

#### pendingRedemptionBurnieBase (slot 12)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> pendingRedemptionBurnieBase += burnieBase | D1 | permissionless | Same rngLocked guard as above |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> pendingRedemptionBurnieBase += | D1 | permissionless | Same rngLocked guard |
| resolveRedemptionPeriod() | StakedDegenerusStonk (direct) | resolveRedemptionPeriod -> pendingRedemptionBurnieBase = 0 | D0 | game-only | Cleared during resolution |

#### pendingRedemptionEthValue (slot 9)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> pendingRedemptionEthValue += segregatedEth | D1 | permissionless | Same rngLocked guard |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> pendingRedemptionEthValue += | D1 | permissionless | Same rngLocked guard |
| resolveRedemptionPeriod() | StakedDegenerusStonk (direct) | resolveRedemptionPeriod -> pendingRedemptionEthValue adjusted by roll | D0 | game-only | Adjusted during resolution |
| claimRedemption() | StakedDegenerusStonk (direct) | claimRedemption -> pendingRedemptionEthValue -= totalRolledEth | D0 | permissionless | Reduced on player claim |

#### pendingRedemptionBurnie (slot 10)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> pendingRedemptionBurnie += reserved | D1 | permissionless | Same rngLocked guard |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> pendingRedemptionBurnie += | D1 | permissionless | Same rngLocked guard |
| resolveRedemptionPeriod() | StakedDegenerusStonk (direct) | resolveRedemptionPeriod -> pendingRedemptionBurnie -= base | D0 | game-only | Released during resolution |

#### redemptionPeriodIndex (slot 14)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> redemptionPeriodIndex = currentPeriod (if new period) | D1 | permissionless | Same rngLocked guard |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> redemptionPeriodIndex update | D1 | permissionless | Same rngLocked guard |

#### redemptionPeriodSupplySnapshot (slot 13)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> redemptionPeriodSupplySnapshot = totalSupply (at period start) | D1 | permissionless | Same rngLocked guard |

#### redemptionPeriodBurned (slot 15)

| External Function | Module/Contract | Call Path | Depth | Access Control | Notes |
|---|---|---|---|---|---|
| burn() | StakedDegenerusStonk (direct) | burn -> _submitGamblingClaim -> redemptionPeriodBurned += amount | D1 | permissionless | Same rngLocked guard |
| burnWrapped() | StakedDegenerusStonk (direct) | burnWrapped -> _submitGamblingClaimFrom -> redemptionPeriodBurned += | D1 | permissionless | Same rngLocked guard |

### 3.4 Special Attention Items

#### reverseFlip() During Commitment Window

**Function:** `reverseFlip()` (DegenerusGame:1984 -> AdvanceModule:1419-1427)
**Access control:** Permissionless (any player with BURNIE)
**Guard:** `if (rngLockedFlag) revert RngLocked()` (AdvanceModule:1420)
**Variable written:** `totalFlipReversals` (slot 6) -- incremented by 1

**Analysis:** reverseFlip is SAFE during the daily RNG commitment window because `rngLockedFlag` is set true BEFORE the daily VRF request (at `_swapAndFreeze` time in advanceGame). The guard blocks all calls during the window.

For the mid-day lootbox RNG path: `rngLockedFlag` is NOT set by requestLootboxRng. However, totalFlipReversals is not consumed by the mid-day path -- `rawFulfillRandomWords` directly stores `lootboxRngWordByIndex[index]` without applying nudges. totalFlipReversals is only consumed by `_applyDailyRng` during the next daily RNG gate. So reverseFlip during a mid-day VRF request is harmless because the mid-day RNG result is independent of totalFlipReversals.

#### requestLootboxRng() Side Effects

**Function:** `requestLootboxRng()` (DegenerusGame:1967 -> AdvanceModule:674-741)
**Access control:** Permissionless
**State changes beyond VRF request:**

1. `ticketWriteSlot ^= 1` (if write slot non-empty and read slot empty) -- buffer swap via `_swapTicketSlot`
2. `ticketsFullyProcessed = false` -- marks new read slot as unprocessed
3. `midDayTicketRngPending = true` -- flags that mid-day swap occurred
4. `lootboxRngIndex++` -- increments for next RNG
5. `lootboxRngPendingEth = 0` -- cleared
6. `lootboxRngPendingBurnie = 0` -- cleared
7. `vrfRequestId = id` -- stores new request
8. `rngWordCurrent = 0` -- cleared
9. `rngRequestTime = uint48(block.timestamp)` -- stores request time

**Note:** requestLootboxRng does NOT set `rngLockedFlag = true` or `prizePoolFrozen = true`. These are only set by the daily `_swapAndFreeze` path.

#### purchase() / purchaseCoin() / purchaseLazyPass() During Commitment Window

**Functions:** All purchase functions (MintModule, WhaleModule)
**Access control:** Permissionless
**Variables written during commitment window:**

1. **ticketQueue[writeKey]** -- writes to WRITE slot. Protected by double-buffer: processTicketBatch reads from READ slot, which was frozen at swap time.
2. **ticketsOwedPacked[writeKey][buyer]** -- writes to WRITE slot. Same double-buffer protection.
3. **currentPrizePool** -- ONLY written if `prizePoolFrozen == false`. During daily commitment window, freeze is active so purchases go to `prizePoolPendingPacked`. During mid-day commitment window (requestLootboxRng), freeze is NOT active, so purchases can write directly to currentPrizePool.
4. **prizePoolsPacked** -- Future pool share always written (not frozen). 10% of purchase goes to future pool via PURCHASE_TO_FUTURE_BPS.
5. **prizePoolPendingPacked** -- Written when frozen (daily path). Not written when unfrozen (mid-day path).
6. **lootboxRngPendingEth** -- Accumulated, but already cleared to 0 at RNG request time.
7. **lootboxEth[index][buyer]** et al. -- Per-player lootbox commitment data, keyed by the CURRENT lootboxRngIndex. Since index was incremented at RNG request, new purchases use the NEXT index, not the one currently awaiting VRF fulfillment.
8. **mintPacked_[buyer]** -- Player mint state; not VRF-outcome-affecting.

### Mutation Surface Summary

| Variable | Slot | Mutated By (permissionless) | Mutated By (admin/governance) | Mutated By (game-internal) |
|---|---|---|---|---|
| rngWordCurrent | 4 | advanceGame(), requestLootboxRng() | configureVrf(), executeSwap() | rawFulfillRandomWords() [VRF-only] |
| vrfRequestId | 5 | advanceGame(), requestLootboxRng() | configureVrf() | rawFulfillRandomWords() [VRF-only] |
| totalFlipReversals | 6 | reverseFlip() [rngLockedFlag guard], advanceGame() | -- | -- |
| rngWordByDay[day] | 13 | advanceGame() | -- | -- |
| ticketQueue[key] | 15 | purchase(), purchaseCoin(), purchaseLazyPass(), purchaseDeityPass(), claimWhalePass() [WRITE slot] | -- | advanceGame() [READ slot, processTicketBatch] |
| ticketsOwedPacked[key][player] | 16 | purchase(), purchaseCoin(), purchaseLazyPass(), purchaseDeityPass(), claimWhalePass() [WRITE slot] | -- | advanceGame() [READ slot] |
| traitBurnTicket[level][traitId] | 11 | -- | -- | advanceGame() -> processTicketBatch [game-internal only] |
| currentPrizePool | 2 | purchase(), purchaseLazyPass(), purchaseDeityPass(), placeFullTicketBets() [freeze-gated] | -- | advanceGame() [jackpot deduction, consolidation] |
| prizePoolsPacked | 3 | purchase(), purchaseLazyPass(), purchaseDeityPass(), placeFullTicketBets() | -- | advanceGame() [consolidation, reward jackpots] |
| prizePoolPendingPacked | 14 | purchase(), purchaseLazyPass(), purchaseDeityPass(), placeFullTicketBets() [frozen-only] | -- | advanceGame() [freeze/unfreeze] |
| claimableWinnings[addr] | 9 | claimWinnings(), purchase() [Claimable pay], placeFullTicketBets() [Claimable pay] | -- | advanceGame() [jackpot/yield credits] |
| claimablePool | 10 | claimWinnings(), purchase() [Claimable pay], placeFullTicketBets() [Claimable pay], claimDecimatorJackpot() | -- | advanceGame() [credits] |
| lootboxRngWordByIndex[index] | 64 | -- | -- | rawFulfillRandomWords() [VRF-only], advanceGame() [finalize/backfill] |
| lastLootboxRngWord | 70 | -- | -- | advanceGame() [finalize/backfill] |
| midDayTicketRngPending | 71 | requestLootboxRng() | -- | advanceGame() [cleared] |
| lootboxRngIndex | 60 | requestLootboxRng() | -- | advanceGame() [_finalizeRngRequest] |
| lastVrfProcessedTimestamp | 103 | -- | configureVrf() | advanceGame() [_applyDailyRng] |
| ticketWriteSlot | 1 | requestLootboxRng() | -- | advanceGame() [_swapAndFreeze] |
| rngLockedFlag | 0 | -- | configureVrf(), executeSwap() | advanceGame() [set/clear] |
| jackpotPhaseFlag | 0 | -- | -- | advanceGame() [phase transitions] |
| level | 0 | -- | -- | advanceGame() [level transitions] |
| price | 1 | -- | -- | advanceGame() [level transitions] |
| yieldAccumulator | 100 | -- | -- | advanceGame() [skim/dump] |
| autoRebuyState[player] | 36 | setAutoRebuy(), setAutoRebuyTakeProfit() | -- | -- |
| deityBySymbol[symId] | 54 | purchaseDeityPass() | -- | -- |
| lootboxRngPendingEth | 61 | purchase() | -- | requestLootboxRng(), advanceGame() [clear] |
| lootboxRngPendingBurnie | 69 | purchaseBurnieLootbox() | -- | requestLootboxRng(), advanceGame() [clear] |
| ticketsFullyProcessed | 1 | requestLootboxRng() [reset] | -- | advanceGame() [set/reset] |
| prizePoolFrozen | 1 | -- | -- | advanceGame() [set/clear] |
| lootboxEth[index][player] | 20 | purchase() | -- | openLootBox() [cleared] |
| lootboxEthBase[index][player] | 43 | purchase() | -- | -- |
| lootboxDay[index][player] | 65 | purchase() | -- | -- |
| lootboxBaseLevelPacked[index][player] | 66 | purchase() | -- | -- |
| lootboxEvScorePacked[index][player] | 67 | purchase() | -- | -- |
| lootboxBurnie[index][player] | 68 | purchaseBurnieLootbox() | -- | openBurnieLootBox() [cleared] |
| lootboxDistressEth[index][player] | 99 | purchase() [distress only] | -- | -- |
| degeneretteBets[player][nonce] | 83 | placeFullTicketBets() | -- | -- |
| degeneretteBetNonce[player] | 84 | placeFullTicketBets() | -- | -- |
| dailyHeroWagers[day][quadrant] | 96 | placeFullTicketBets() | -- | -- |
| playerDegeneretteEthWagered[player][lvl] | 97 | placeFullTicketBets() | -- | -- |
| topDegeneretteByLevel[lvl] | 98 | placeFullTicketBets() | -- | -- |
| coinflipBalance[epoch][player] | 0 (CF) | depositCoinflip() | -- | creditFlip(), creditFlipBatch() [game-only] |
| coinflipDayResult[epoch] | 1 (CF) | -- | -- | processCoinflipPayouts() [game-only] |
| playerState[player] | 2 (CF) | depositCoinflip(), claimCoinflips(), setCoinflipAutoRebuy() | -- | processCoinflipPayouts() [game-only] |
| currentBounty | 3 (CF) | -- | -- | processCoinflipPayouts() [game-only] |
| bountyOwedTo | 4 (CF) | depositCoinflip() [if record flip] | -- | processCoinflipPayouts() [game-only] |
| flipsClaimableDay | 4 (CF) | -- | -- | processCoinflipPayouts() [game-only] |
| pendingRedemptionEthBase | 11 (sDGNRS) | burn(), burnWrapped() [rngLocked guard] | -- | resolveRedemptionPeriod() [game-only] |
| pendingRedemptionBurnieBase | 12 (sDGNRS) | burn(), burnWrapped() [rngLocked guard] | -- | resolveRedemptionPeriod() [game-only] |
| pendingRedemptionEthValue | 9 (sDGNRS) | burn(), burnWrapped() [rngLocked guard], claimRedemption() | -- | resolveRedemptionPeriod() [game-only] |
| pendingRedemptionBurnie | 10 (sDGNRS) | burn(), burnWrapped() [rngLocked guard] | -- | resolveRedemptionPeriod() [game-only] |
| redemptionPeriodIndex | 14 (sDGNRS) | burn(), burnWrapped() [rngLocked guard] | -- | -- |
| redemptionPeriodSupplySnapshot | 13 (sDGNRS) | burn() [rngLocked guard] | -- | -- |
| redemptionPeriodBurned | 15 (sDGNRS) | burn(), burnWrapped() [rngLocked guard] | -- | -- |


