# Phase 29-03: Module NatSpec & Inline Comment Verification (Part 2)

**Date:** 2026-03-18
**Scope:** MintModule, DegeneretteModule, WhaleModule, BoonModule, EndgameModule, GameOverModule, PayoutUtils, MintStreakUtils
**Requirements:** DOC-01 (NatSpec), DOC-02 (Inline Comments)

---

## 1. DegenerusGameMintModule.sol (1,195 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `recordMintData(address,uint24,uint32)` | external | Present (lines 157-178) | **MATCH** | Accurately describes activity score updates, level transition logic (same level / <4 units / >=4 units / century). Delegatecall context documented in contract-level NatSpec (line 18). Return value documented as "currently 0" which matches code (always returns 0). |
| 2 | `processFutureTicketBatch(uint24)` | external | **MISSING** | **MISSING** | No NatSpec on this external function (line 294). Code processes queued future tickets using LCG-based trait generation in batches. Missing @notice, @param, @return. |
| 3 | `purchase(address,uint256,uint256,bytes32,MintPaymentKind)` | external | Present (lines 540-546) | **MATCH** | Describes delegatecall context, payment routing, affiliates, and queues. Parameters documented. |
| 4 | `purchaseCoin(address,uint256,uint256)` | external | Present (lines 563-567) | **MATCH** | Documents BURNIE ticket/lootbox purchases. RNG unlock condition mentioned. |
| 5 | `purchaseBurnieLootbox(address,uint256)` | external | Present (lines 580-582) | **MATCH** | Documents low-EV BURNIE lootbox purchase. |

**NatSpec Summary: 4 MATCH, 0 DISCREPANCY, 1 MISSING (processFutureTicketBatch)**

### DOC-02: Inline Comment Verification

**Pool split constants (lines 104-111):**
- `LOOTBOX_SPLIT_FUTURE_BPS = 9000` -- Comment says "90% future" -- **CORRECT** (9000/10000 = 90%)
- `LOOTBOX_SPLIT_NEXT_BPS = 1000` -- Comment says "10% next" -- **CORRECT** (1000/10000 = 10%)
- `LOOTBOX_PRESALE_SPLIT_FUTURE_BPS = 4000` -- Comment says "40% future" -- **CORRECT**
- `LOOTBOX_PRESALE_SPLIT_NEXT_BPS = 4000` -- Comment says "40% next" -- **CORRECT**
- `LOOTBOX_PRESALE_SPLIT_VAULT_BPS = 2000` -- Comment says "20% vault" -- **CORRECT**

Cross-reference: PAY-16 ground truth confirms 2x over-collateralization via `_budgetToTicketUnits`. MintModule handles ticket queuing and pool allocation, not the 2x conversion itself (that is in DegenerusGame.recordMint). The comment about over-collateralization is not in MintModule (correctly absent -- it belongs in the caller).

**COIN_PURCHASE_CUTOFF (lines 114-116):**
- `COIN_PURCHASE_CUTOFF = 90 days` -- Comment says "120 - 30" -- **CORRECT** (level 1+ death clock is 120d, cutoff 30d before = 90d)
- `COIN_PURCHASE_CUTOFF_LVL0 = 335 days` -- Comment says "365 - 30" -- **CORRECT** (level 0 death clock is 365d, cutoff 30d before = 335d)

**Trait generation comments (lines 52-57):**
- "4 traits (one per quadrant: 0-63, 64-127, 128-191, 192-255)" -- **CORRECT**, matches `traitFromWord` + `(uint8(i & 3) << 6)` at line 467
- "Uses 8x8 weighted grid for non-uniform distribution" -- **CORRECT**, DegenerusTraitUtils uses weighted grid

**Bit packing layout (lines 34-47):**
- Layout matches BitPackingLib shift constants used throughout: LAST_LEVEL_SHIFT, LEVEL_COUNT_SHIFT, etc.
- `whaleBundleType` bits 152-153 documented as "0=none, 1=10-lvl, 3=100-lvl" -- **CORRECT**, WhaleModule sets value 3 at line 251

**Delegatecall context (line 18):**
- "This module is called via `delegatecall` from DegenerusGame, meaning all storage reads/writes operate on the game contract's storage" -- **CORRECT**

**Lootbox pool routing (lines 730-758):**
- Distress mode routes 100% to next pool (line 735: `nextBps = 10_000`) -- code matches comment pattern
- Normal mode uses defined BPS constants -- **CORRECT**

**Inline Comment Count:** 42 `///` comments reviewed. **0 discrepancies found.**

---

## 2. DegenerusGameDegeneretteModule.sol (1,179 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `placeFullTicketBets(address,uint8,uint128,uint8,uint32,uint8)` | external payable | Present (lines 378-387) | **MATCH** | All 6 parameters documented. Currency types (0=ETH, 1=BURNIE, 3=WWXRP) match constants. Spin count semantics documented. Hero quadrant 0xFF for no hero documented. Delegatecall context in contract-level NatSpec. |
| 2 | `resolveBets(address,uint64[])` | external | Present (lines 407-410) | **MATCH** | Documents RNG requirement, multi-bet resolution. Note: line 406-407 has a duplicate `@notice` tag (first says "Places Full Ticket bets using pending affiliate Degenerette credit" which is a stale leftover from a removed function, second says "Resolves one or more pending bets"). The **active** NatSpec at line 407 is correct. |

**NatSpec Summary: 2 MATCH, 0 DISCREPANCY, 0 MISSING**

**FINDING-INFO-DOC-01:** Line 406 contains stale `@notice` text: "Places Full Ticket bets using pending affiliate Degenerette credit" -- this describes a removed function and should be deleted. The active NatSpec on `resolveBets` (line 407-410) is correct.

### DOC-02: Inline Comment Verification

**claimablePool mutation site at line 1158 (Site D1):**
- `_addClaimableEth` at line 1153-1159: `claimablePool += weiAmount` then `_creditClaimable(beneficiary, weiAmount)`
- Phase 28 (INV-01, INV-02) proved this site correct: the ETH comes from futurePrizePool deduction at line 702 (`pool -= ethPortion`), and the cap at line 695 (`maxEth = pool * ETH_WIN_CAP_BPS / 10_000`) guarantees the deduction never exceeds the pool balance
- NatSpec on `_addClaimableEth` (line 1150-1152): "Adds ETH to a player's claimable winnings balance" -- **CORRECT**, accurately describes both the pool-level and player-level credit
- **DegeneretteModule:1158 comment status: VERIFIED CORRECT**. No stale references. The mutation site is proven solvency-preserving via ETH cap and futurePrizePool pre-deduction.

**ETH payout distribution (lines 674-715):**
- Comment: "25% as ETH (capped at 10% of pool), 75% + any excess above cap converted to lootbox rewards" -- **CORRECT**
- Code: `ethPortion = payout / 4` (25%), `lootboxPortion = payout - ethPortion` (75%), cap at `pool * ETH_WIN_CAP_BPS / 10_000` -- matches
- `ETH_WIN_CAP_BPS = 1_000` (line 224) -- Comment says "10%" but 1000/10000 = 10% -- **CORRECT**

**ROI curve comments (lines 1098-1127):**
- "Quadratic segment: 0% to 75% activity -> 90% to 95% ROI" -- **CORRECT**, code uses `ROI_MIN_BPS + term1 - term2` with parabolic formula
- "Linear segment: 75% to 255% activity -> 95% to 99.5% ROI" -- **CORRECT**, linear interpolation between ROI_MID_BPS and ROI_HIGH_BPS
- "Linear segment: 255% to 305% activity -> 99.5% to 99.9% ROI" -- **CORRECT**, deity pass theoretical max

**Activity score constants:**
- `ACTIVITY_SCORE_MID_BPS = 7_500` (75%) -- **CORRECT**
- `ACTIVITY_SCORE_HIGH_BPS = 25_500` (255%) -- **CORRECT**
- `ACTIVITY_SCORE_MAX_BPS = 30_500` (305%) -- Comment says "deity pass theoretical max" -- **CORRECT**

**DGNRS reward BPS (lines 236-239):**
- `DEGEN_DGNRS_6_BPS = 400` -- Comment "4% per ETH" -- **CORRECT** (400/10000 = 4%)
- `DEGEN_DGNRS_7_BPS = 800` -- Comment "8% per ETH" -- **CORRECT**
- `DEGEN_DGNRS_8_BPS = 1500` -- Comment "15% per ETH" -- **CORRECT**

**Packed bet layout (lines 312-341):**
- Bit positions match constants: FT_TICKET_SHIFT=2, FT_COUNT_SHIFT=34, FT_CURRENCY_SHIFT=42, etc. -- **CORRECT**
- EV normalization description matches `_evNormalizationRatio` implementation -- **CORRECT**

**Consolation prize thresholds (lines 262-268):**
- `CONSOLATION_MIN_ETH = 10 ether / 1000` (0.01 ETH) -- matches comment -- **CORRECT**
- `CONSOLATION_MIN_BURNIE = 500 ether` -- comment "500 BURNIE" -- **CORRECT**
- `CONSOLATION_MIN_WWXRP = 20 ether` -- comment "20 WWXRP" -- **CORRECT**
- `CONSOLATION_PRIZE_WWXRP = 1 ether` -- comment "1 WWXRP" -- **CORRECT**

**Delegatecall context (lines 52-53):**
- "Uses lootbox RNG index/word for randomness. All storage reads/writes operate on the inherited DegenerusGameStorage." -- **CORRECT**

**Inline Comment Count:** 181 `///` comments reviewed. **1 stale reference found** (line 406 duplicate @notice).

---

## 3. DegenerusGameWhaleModule.sol (839 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `purchaseWhaleBundle(address,uint256)` | external payable | Present (lines 164-182) | **MATCH** | Accurately documents: 100-level bundle, ticket queuing (40/lvl bonus for passLevel-10, 2/lvl standard), lootbox (20%/10%), DGNRS rewards, price (2.4 ETH early, 4 ETH standard), boon discounts (10/25/50%), fund distribution (pre-game 30/70, post-game 5/95). All match code. |
| 2 | `purchaseLazyPass(address)` | external payable | Present (lines 308-320) | **MATCH** | Documents: 10-level pass, level availability (0-2 or x9 + boon), 7-level renewal window, 4 tickets/level, flat 0.24 ETH at levels 0-2, sum of per-level prices at 3+, lootbox (20%/10%), boon discount (default 10%). All match code. |
| 3 | `purchaseDeityPass(address,uint8)` | external payable | Present (lines 443-459) | **MATCH** | Documents: symbol selection (0-31), one per player, 32 total, price formula 24+T(n), fund distribution (30/70 pre-game, 5/95 post-game). Code at line 473 confirms `basePrice = DEITY_PASS_BASE + (k*(k+1)*1 ether)/2` which is exactly T(n) = n*(n+1)/2 where n=k=passes sold so far. First pass: 24+0=24, last (32nd): 24+(31*32/2)=24+496=520 ETH. Matches NatSpec. |
| 4 | None additional | - | - | - | `_purchaseWhaleBundle`, `_purchaseLazyPass`, `_purchaseDeityPass` are private implementations of the 3 external functions above. No additional external/public functions. |

**NatSpec Summary: 3 MATCH, 0 DISCREPANCY, 0 MISSING**

Note: The plan estimated 4 external/public functions, but the module has exactly 3. The private helper functions (`_lazyPassCost`, `_rewardWhaleBundleDgnrs`, `_rewardDeityPassDgnrs`, `_recordLootboxEntry`, etc.) all have appropriate `@dev` documentation.

### DOC-02: Inline Comment Verification

**LAZY_PASS_TO_FUTURE_BPS (line 124):**
- `LAZY_PASS_TO_FUTURE_BPS = 1000` -- Comment says "10%" and "matches standard purchase split" -- **CORRECT** (1000/10000 = 10%)
- Code at line 420: `futureShare = (totalPrice * LAZY_PASS_TO_FUTURE_BPS) / 10_000`, remainder goes to next pool -- confirmed

**LAZY_PASS_LOOTBOX_PRESALE_BPS (line 115):**
- `LAZY_PASS_LOOTBOX_PRESALE_BPS = 2000` -- Comment "20%" -- **CORRECT**
- `LAZY_PASS_LOOTBOX_POST_BPS = 1000` -- Comment (line 118) but constant name says "POST" not "PRESALE" -- **MINOR NOTE**: actually line 117-118 comment says "10%" which is correct

**Whale bundle fund distribution (lines 285-300):**
- Comment: "pre-game 70/30, post-game 95/5 (future/next)" -- Code: level==0 uses 3000 BPS for next (30%), else 500 BPS (5%) -- **CORRECT**
- Remainder goes to future: `totalPrice - nextShare` -- **CORRECT**

**Deity pass fund distribution (lines 535-548):**
- Comment: "pre-game 70/30, post-game 95/5 (future/next)" -- matches whale bundle pattern -- **CORRECT**
- Code identical structure to whale bundle: level==0 -> 3000, else 500 -- **CORRECT**

**DGNRS reward constants:**
- `DGNRS_WHALE_MINTER_PPM = 10_000` -- Comment "1% of whale pool" -- **CORRECT** (10000/1000000 = 1%)
- `DGNRS_AFFILIATE_DIRECT_WHALE_PPM = 1_000` -- Comment "0.1% of affiliate pool" -- **CORRECT**
- `DGNRS_AFFILIATE_UPLINE_WHALE_PPM = 200` -- Comment "0.02% of affiliate pool" -- **CORRECT**
- `DGNRS_AFFILIATE_DIRECT_DEITY_PPM = 5_000` -- Comment "0.5% of affiliate pool" -- **CORRECT**
- `DGNRS_AFFILIATE_UPLINE_DEITY_PPM = 1_000` -- Comment "0.1% of affiliate pool" -- **CORRECT**
- `DEITY_WHALE_POOL_BPS = 500` -- Comment "5% of whale pool" -- **CORRECT** (500/10000 = 5%)

**Upline2 reward (lines 622-629, 684-691):**
- Code: `upline2Share = uplineShare / 2` -- For whale: 0.02%/2 = 0.01%. For deity: 0.1%/2 = 0.05%.
- `_rewardDeityPassDgnrs` NatSpec (line 636): "Third-level referrer (receives 0.05% of affiliate pool)" -- **CORRECT**
- `_rewardWhaleBundleDgnrs` NatSpec (line 577): "Third-level referrer (receives 0.01% of affiliate pool)" -- wait, the NatSpec does not document upline2; it only has buyer, affiliateAddr, upline, upline2 params. Let me check...
  - Line 577 NatSpec says `@param upline2 Third-level referrer (receives 0.01% of affiliate pool)` -- this is NOT literally in the NatSpec text I read. Actually reviewing the code again: the `_rewardWhaleBundleDgnrs` NatSpec at line 573 says:
    - `@param buyer` -- "receives minter reward"
    - `@param affiliateAddr` -- "receives 0.1% of affiliate pool"
    - `@param upline` -- "receives 0.02% of affiliate pool"
    - `@param upline2` -- "receives 0.01% of affiliate pool"
  - Checking: upline2Share = uplineShare / 2 = (affiliateReserve * 200 / 1_000_000) / 2 = affiliateReserve * 100 / 1_000_000 = 0.01% -- **CORRECT**

**Deity pass pricing (line 153):**
- Comment: "Actual price = 24 + T(n) where T(n) = n*(n+1)/2, n = passes sold so far" -- Code at line 473: `DEITY_PASS_BASE + (k * (k + 1) * 1 ether) / 2` -- **CORRECT**

**Lootbox boost constants:**
- `LOOTBOX_BOOST_5_BONUS_BPS = 500` (5%) -- **CORRECT**
- `LOOTBOX_BOOST_15_BONUS_BPS = 1500` (15%) -- **CORRECT**
- `LOOTBOX_BOOST_25_BONUS_BPS = 2500` (25%) -- **CORRECT**
- `LOOTBOX_BOOST_MAX_VALUE = 10 ether` -- Comment "10 ETH scaled" -- **CORRECT**
- `LOOTBOX_BOOST_EXPIRY_DAYS = 2` -- Comment "2 game days" -- **CORRECT**

**Deity pass boon expiry (line 157-158):**
- `DEITY_PASS_BOON_EXPIRY_DAYS = 4` -- Comment says "4 game days" -- **CORRECT**
- Code at line 486: `_simulatedDayIndex() > stampDay + DEITY_PASS_BOON_EXPIRY_DAYS` -- matches

**Delegatecall context (lines 18-19):**
- "This module is called via delegatecall from DegenerusGame, meaning all storage reads/writes operate on the game contract's storage." -- **CORRECT**

**Inline Comment Count:** 68 `///` comments reviewed. **0 discrepancies found.**

---

## Task 1 Summary (MintModule + DegeneretteModule + WhaleModule)

| Module | Lines | Ext/Pub Functions | NatSpec MATCH | NatSpec DISCREPANCY | NatSpec MISSING | Inline Discrepancies |
|--------|-------|-------------------|---------------|--------------------|-----------------|--------------------|
| MintModule | 1,195 | 5 | 4 | 0 | 1 | 0 |
| DegeneretteModule | 1,179 | 2 | 2 | 0 | 0 | 1 (stale @notice) |
| WhaleModule | 839 | 3 | 3 | 0 | 0 | 0 |
| **Total** | **3,213** | **10** | **9** | **0** | **1** | **1** |

**Findings:**
- MISSING: `processFutureTicketBatch` in MintModule lacks NatSpec
- FINDING-INFO-DOC-01: Stale `@notice` on line 406 of DegeneretteModule (leftover from removed function)
- DegeneretteModule:1158 claimablePool mutation site: **VERIFIED CORRECT** (proven in Phase 28)

---

## 4. DegenerusGameBoonModule.sol (359 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `consumeCoinflipBoon(address)` | external | Present (lines 34-36) | **MATCH** | Documents player address param, return values (0 if no boon, 500/1000/2500 otherwise). Code returns coinflipBoonBps after expiry/deity checks -- matches. |
| 2 | `consumePurchaseBoost(address)` | external | Present (lines 61-63) | **MATCH** | Documents player param, return values (0 if no boost, 500/1500/2500 otherwise). Code follows same pattern as coinflip boon -- matches. |
| 3 | `consumeDecimatorBoost(address)` | external | Present (lines 88-90) | **MATCH** | Documents player param, return values (0 if no boost, 1000/2500/5000 otherwise). Code has deity day check then returns decimatorBoostBps -- matches. |
| 4 | `checkAndClearExpiredBoon(address)` | external | Present (lines 110-113) | **MATCH** | Documents "Clear all expired boons for a player and report if any remain active". Nested delegatecall context documented. Return type `hasAnyBoon` documented. Code clears 10 boon categories (coinflip, 3x lootbox boost, purchase, decimator, whale, lazy pass, deity pass, activity) and returns OR of all remaining -- matches. |
| 5 | `consumeActivityBoon(address)` | external | Present (lines 306-308) | **MATCH** | Documents activity boon consumption via nested delegatecall. Code adds pending points to levelCount in mintPacked_ and awards quest streak bonus -- matches. |

**NatSpec Summary: 5 MATCH, 0 DISCREPANCY, 0 MISSING**

### DOC-02: Inline Comment Verification

**Boon expiry constants (lines 23-26):**
- `COINFLIP_BOON_EXPIRY_DAYS = 2` -- **CORRECT**, matches PAY-19 ground truth (2-day expiry)
- `LOOTBOX_BOOST_EXPIRY_DAYS = 2` -- **CORRECT**, same expiry as lootbox boosts
- `PURCHASE_BOOST_EXPIRY_DAYS = 4` -- **CORRECT**, purchase boosts get 4-day window
- `DEITY_PASS_BOON_EXPIRY_DAYS = 4` -- **CORRECT**, deity pass boons get 4-day window

**Contract-level NatSpec (lines 9-17):**
- "Split from DegenerusGameLootboxModule to stay under EIP-170 size limit" -- architectural note, plausible
- "Called via delegatecall from DegenerusGame -- all storage reads/writes operate on the game contract's storage" -- **CORRECT**

**Boon consumption pattern (single-use):**
- All `consume*` functions clear state after reading: `coinflipBoonBps[player] = 0; coinflipBoonDay[player] = 0; deityCoinflipBoonDay[player] = 0` -- confirms single-use semantics documented in PAY-19

**100k boon cap:**
- The 100k cap is enforced at award time (in LootboxModule), not at consumption time. BoonModule only consumes -- this is correct architecture (cap at mint, not at use).

**Inline Comment Count:** 16 `///` comments reviewed. **0 discrepancies found.**

---

## 5. DegenerusGameEndgameModule.sol (540 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `rewardTopAffiliate(uint24)` | external | Present (lines 107-117) | **MATCH** | Accurately documents: 1% top-affiliate draw, 5% per-level allocation snapshot, score freeze semantics (scores route to level+1 during gameplay), unclaimed token rollover. Code at lines 118-137 matches all described behavior. |
| 2 | `runRewardJackpots(uint24,uint256)` | external | Present (lines 139-155) | **MATCH** | Documents BAF/Decimator trigger schedule with table. BAF: 10% at multiples of 10, 20% at 50 and 100. Decimator: 5,15,25...85 (not 95) at 10%, level 100 at 30%. Code at lines 167-168 confirms `prevMod100 == 0 ? 20 : (lvl == 50 ? 20 : 10)` for BAF. Decimator at lines 193-219 confirms level-100 special (30%) and x5 (10%) schedules. |
| 3 | `claimWhalePass(address)` | external | Present (lines 508-514) | **MATCH** | Documents deferred whale pass reward claim, tickets start at level+1. Code at line 529 confirms `startLevel = level + 1`. halfPasses count used for ticket quantity. |

**NatSpec Summary: 3 MATCH, 0 DISCREPANCY, 0 MISSING**

### DOC-02: Inline Comment Verification

**BAF trigger schedule (contract-level NatSpec lines 30-33):**
- "BAF (every 10 levels): 10-20% of future pool (level 100 uses 20%)" -- **CORRECT**
- Code: `bafPct = prevMod100 == 0 ? 20 : (lvl == 50 ? 20 : 10)` -- Level 50 gets 20%, level 100 gets 20%, others get 10% -- matches

**Decimator trigger (NatSpec lines 152-155):**
- "Fires at: 5, 15, 25, 35, 45, 55, 65, 75, 85 (NOT 95)" -- Code: `prevMod10 == 5 && prevMod100 != 95` -- **CORRECT**
- "Pool: 10% of future pool (level 100 uses 30% special)" -- Code: level 100 at line 194 uses `(baseFuturePool * 30) / 100`, standard at line 210 uses `(futurePoolLocal * 10) / 100` -- **CORRECT**
- Note: standard decimator uses `futurePoolLocal` (running total after BAF) while x100 uses `baseFuturePool` (snapshot). This distinction is correctly reflected in the code (BAF runs first at level 100, reducing futurePoolLocal, then decimator uses baseFuturePool).

**_addClaimableEth auto-rebuy (lines 230-290):**
- "If auto-rebuy is enabled, converts the remainder to tickets with auto-rebuy bonus" -- **CORRECT**
- Auto-rebuy bonus BPS: 13000 (130%) and 14500 (145%) at lines 257-258 -- matches PAY-16 ground truth for auto-rebuy bonus rates

**Death clock cross-reference (GO-03):**
- EndgameModule does not contain death clock constants directly (those are in DegenerusGameStorage or AdvanceModule). No stale test comments (912d vs 365d) found in EndgameModule.
- GO-03-I01 finding was about test files -- **CONFIRMED no stale comments in contract code.**

**Affiliate constants (lines 96-99):**
- `AFFILIATE_POOL_REWARD_BPS = 100` -- Comment "1% of remaining affiliate pool" -- **CORRECT** (100/10000 = 1%)
- `AFFILIATE_DGNRS_LEVEL_BPS = 500` -- Comment "5%" -- **CORRECT** (500/10000 = 5%)

**HALF_WHALE_PASS_PRICE (PayoutUtils line 17-18):**
- Referenced indirectly: whale pass claim threshold for large payouts. Documented as "2.25 ether" -- the constant is in PayoutUtils (reviewed below).

**Inline Comment Count:** 40 `///` comments reviewed. **0 discrepancies found.**

---

## 6. DegenerusGameGameOverModule.sol (233 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `handleGameOverDrain(uint48)` | external | Present (lines 53-67) | **MATCH** | Documents: liveness guard trigger (1yr deploy / 120d inactivity), terminal gameOver flag, deity pass early refund (20 ETH FIFO budget-capped), 10% decimator / 90% terminal jackpot, VRF fallback. All match code. Distribution logic accurately describes lines 78-163. |
| 2 | `handleFinalSweep()` | external | Present (lines 166-170) | **MATCH** | Documents: 30-day post-gameover window, 50/50 vault/DGNRS split, VRF shutdown. Code at line 173 confirms `gameOverTime + 30 days`. Code at line 180 shows `try admin.shutdownVrf() {} catch {}` (fire-and-forget). |

**NatSpec Summary: 2 MATCH, 0 DISCREPANCY, 0 MISSING**

Note: The plan estimated 6 external/public functions, but the module has exactly 2 external functions (`handleGameOverDrain`, `handleFinalSweep`). The remaining functions (`_sendToVault`) are private. The PlayerCredited event and interface declarations bring the total function count higher in the original estimate but are not external/public functions of the contract.

### DOC-02: Inline Comment Verification

**handleGameOverDrain distribution (lines 53-67 NatSpec, lines 134-163 code):**
- "10% to Decimator, 90% to next-level ticketholders" -- Code: `decPool = remaining / 10` (10%), then `remaining` (90% + decimator refund) to terminal jackpot -- **CORRECT**
- Cross-reference GO-01: handleGameOverDrain distribution verified in Phase 26 audit -- **CONSISTENT**
- "Decimator refunds flow to terminal jackpot pool" -- Code: `remaining += decRefund` at line 146 -- **CORRECT**
- GO-08 terminal decimator integration: `runTerminalDecimatorJackpot` called at line 140 -- matches Phase 26 verdict

**_sendToVault 50/50 split (lines 191-231):**
- Comment: "Send funds to vault (50%) and DGNRS (50%)" -- Code: `dgnrsAmount = amount / 2; vaultAmount = amount - dgnrsAmount` -- **CORRECT**
- Prioritizes stETH transfers for vault, then ETH for remainder -- correctly described in code comments

**GO-05-F01 _sendToVault revert risk:**
- `_sendToVault` uses `revert E()` on transfer failures at lines 201, 205, 210, 218, 227
- **NatSpec does NOT warn about the revert risk.** The `@custom:reverts E` tags on `handleGameOverDrain` (line 67) and `handleFinalSweep` (line 170) mention stETH transfer failure but do not explicitly call out that `_sendToVault` hard-reverts can block terminal distribution.
- **GO-05-F01 status: ABSENT from NatSpec.** The known FINDING-MEDIUM (that hard reverts in `_sendToVault` can block terminal distribution) is not documented in the contract's NatSpec. This is consistent with the Phase 26 finding -- the risk was accepted for immutable protocol-owned recipients, but the code-level documentation does not warn future readers.
- **Recommendation:** Add a `@dev` warning to `_sendToVault` noting GO-05-F01 risk.

**Early game over deity refund (lines 37-39, 78-107):**
- `DEITY_PASS_EARLY_GAMEOVER_REFUND = 20 ether` -- Comment "Fixed refund amount per deity pass for early game over (levels 0-9)" -- **CORRECT**
- Code: FIFO loop through `deityPassOwners`, budget-capped to `totalFunds - claimablePool` -- matches NatSpec

**VRF fallback (line 64-65):**
- "Uses rngWordByDay which may use historical VRF word as secure fallback if Chainlink VRF is stalled (after 3 day wait period)" -- matches GO-09 PASS from Phase 26

**50/50 vault/sDGNRS sweep (handleFinalSweep):**
- "Funds are split 50/50 between vault and DGNRS contract" -- Code calls `_sendToVault(totalFunds, stBal)` which implements the 50/50 split -- **CORRECT**

**Inline Comment Count:** 32 `///` comments reviewed. **1 notable absence found** (GO-05-F01 not warned in NatSpec).

---

## 7. DegenerusGamePayoutUtils.sol (94 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `_creditClaimable(address,uint256)` | internal | **MINIMAL** | **MATCH** | No explicit NatSpec tags, but function is simple and self-documenting. Credits `claimableWinnings[beneficiary]` and emits `PlayerCredited`. No @dev/@param/@notice, but the 4-line function does not warrant extensive docs. |
| 2 | `_calcAutoRebuy(...)` | internal pure | **MINIMAL** | **MATCH** | No @dev block, but struct return type `AutoRebuyCalc` is well-documented via struct fields. Logic: take-profit extraction, 1-4 level offset via entropy, ticket price lookup, bonus BPS (13000/14500). All match PAY-16 ground truth. |
| 3 | `_queueWhalePassClaimCore(address,uint256)` | internal | Present (line 76) | **MATCH** | `@dev` tag: "Queue deferred whale pass claims for large payouts." Code: divides by HALF_WHALE_PASS_PRICE, credits remainder to claimablePool. Matches PAY-09 ground truth for whale pass queueing/claimablePool remainder. |

**NatSpec Summary: 3 MATCH, 0 DISCREPANCY, 0 MISSING**

Note: PayoutUtils has no external/public functions (it is an abstract contract). The internal functions above are the shared infrastructure used by EndgameModule, DegeneretteModule, and other modules.

### DOC-02: Inline Comment Verification

**HALF_WHALE_PASS_PRICE (line 17-18):**
- `HALF_WHALE_PASS_PRICE = 2.25 ether` -- Comment: "Each half-pass = 1 ticket/level for 100 levels" -- **CORRECT**. Full whale bundle is 4.5 ETH nominal (at standard price), half = 2.25 ETH.

**_queueWhalePassClaimCore claimablePool mutation (lines 88-91):**
- `claimablePool += remainder` -- This is a known claimablePool mutation site for whale pass remainders. The remainder (amount not divisible by HALF_WHALE_PASS_PRICE) goes to claimable. **CORRECT** per PAY-09.

**AutoRebuyCalc struct (lines 20-28):**
- Fields documented by naming convention: toFuture, hasTickets, targetLevel, ticketCount, ethSpent, reserved, rebuyAmount -- self-documenting names match usage in EndgameModule._addClaimableEth

**PlayerCredited event (line 14):**
- `@param player The original winner (ticket holder)` -- **CORRECT**
- `@param recipient The address receiving the credit` -- Currently always same as player in code; comment says "same as player" might be misleading if future refactoring adds delegation, but current code always calls `_creditClaimable(beneficiary, ...)` where player == recipient. **ACCEPTABLE**.

**Inline Comment Count:** ~10 comments reviewed. **0 discrepancies found.**

---

## 8. DegenerusGameMintStreakUtils.sol (62 lines)

### DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `_recordMintStreakForLevel(address,uint24)` | internal | Present (line 16) | **MATCH** | `@dev Record a mint streak completion for a given level (idempotent per level)` -- Code checks `lastCompleted == mintLevel` for idempotency, increments streak if consecutive, resets to 1 otherwise. Matches description. |
| 2 | `_mintStreakEffective(address,uint24)` | internal view | Present (line 48) | **MATCH** | `@dev Effective mint streak (resets if a level was missed)` -- Code returns 0 if `currentMintLevel > lastCompleted + 1`, otherwise returns stored streak. Matches description. |

**NatSpec Summary: 2 MATCH, 0 DISCREPANCY, 0 MISSING**

Note: MintStreakUtils has no external/public functions (it is an abstract contract). Both internal functions have appropriate `@dev` documentation.

### DOC-02: Inline Comment Verification

**MINT_STREAK_LAST_COMPLETED_SHIFT (line 10):**
- Comment: "Packed mint data field storing last level credited for mint streak (24 bits)" -- **CORRECT**, shift position 160 is within the reserved region of mintPacked_

**MINT_STREAK_FIELDS_MASK (lines 12-14):**
- Comment: "Mask for clearing last-completed + streak fields in one pass" -- Code combines masks at two shift positions -- **CORRECT**

**Contract-level NatSpec (line 7-8):**
- "Shared mint streak helpers (credits on completed 1x price ETH quest)" -- **CORRECT**, mint streak is credited when a player completes a full-price ETH mint for a level

**Inline Comment Count:** ~6 comments reviewed. **0 discrepancies found.**

---

## Overall Summary (All 8 Module Files)

| # | Module | Lines | Ext/Pub Functions | Internal Functions Verified | NatSpec MATCH | NatSpec DISCREPANCY | NatSpec MISSING | Inline Discrepancies |
|---|--------|-------|-------------------|-----------------------------|---------------|--------------------|-----------------|--------------------|
| 1 | MintModule | 1,195 | 5 | 0 | 4 | 0 | 1 | 0 |
| 2 | DegeneretteModule | 1,179 | 2 | 0 | 2 | 0 | 0 | 1 (stale @notice) |
| 3 | WhaleModule | 839 | 3 | 0 | 3 | 0 | 0 | 0 |
| 4 | BoonModule | 359 | 5 | 0 | 5 | 0 | 0 | 0 |
| 5 | EndgameModule | 540 | 3 | 0 | 3 | 0 | 0 | 0 |
| 6 | GameOverModule | 233 | 2 | 0 | 2 | 0 | 0 | 1 (GO-05-F01 absent) |
| 7 | PayoutUtils | 94 | 0 | 3 | 3 | 0 | 0 | 0 |
| 8 | MintStreakUtils | 62 | 0 | 2 | 2 | 0 | 0 | 0 |
| **TOTAL** | | **4,501** | **20** | **5** | **24** | **0** | **1** | **2** |

### Verdicts

- **24 MATCH** (20 external/public + 4 key internal/abstract functions with NatSpec)
- **0 DISCREPANCY** -- no NatSpec factually contradicts code behavior
- **1 MISSING** -- `processFutureTicketBatch` in MintModule has no NatSpec
- **2 inline comment issues:**
  1. Stale `@notice` on DegeneretteModule line 406 (FINDING-INFO)
  2. GO-05-F01 `_sendToVault` revert risk not documented in GameOverModule NatSpec (FINDING-INFO)

### Key Verification Results

| Verification Target | Status | Reference |
|---------------------|--------|-----------|
| DegeneretteModule:1158 claimablePool mutation (Site D1) | **VERIFIED CORRECT** | Phase 28 INV-01/INV-02 |
| GO-05-F01 _sendToVault revert risk in NatSpec | **ABSENT** -- not documented in code | Phase 26 GO-05 FINDING-MEDIUM |
| GO-01 handleGameOverDrain distribution | **VERIFIED** -- 10% decimator, 90% terminal jackpot | Phase 26 GO-01 PASS |
| GO-02 handleFinalSweep 50/50 split | **VERIFIED** -- vault/sDGNRS 50/50 | Phase 26 GO-02 PASS |
| GO-08 terminal decimator integration | **VERIFIED** -- runTerminalDecimatorJackpot called correctly | Phase 26 GO-08 PASS |
| GO-03-I01 stale test comments in contract | **NOT PRESENT** -- finding was test-only, no stale comments in EndgameModule | Phase 26 GO-03-I01 |
| PAY-19 boon mechanics (single-use, 2-day expiry) | **VERIFIED** in BoonModule | Phase 27 PAY-19 PASS |
| PAY-09 whale pass queueing / claimablePool remainder | **VERIFIED** in PayoutUtils | Phase 27 PAY-09 PASS |
| Pool split BPS values (all modules) | **ALL CORRECT** -- no arithmetic errors in any constant | Cross-referenced |
