# v1.2 Delta Attack Re-verification

**Scope:** Re-verification of all 8 attack scenarios from the v1.0 RNG audit against the current contract code (post-v1.1 economics changes).
**Source audit:** `audit/v1.0-rng-and-changes-audit.md`
**Methodology:** For each v1.0 finding, grep the current contract source for the specific guard/mechanism and confirm it remains intact with updated line numbers. Any structural changes are documented under "Delta."

---

## Attack Scenario Re-verification

### Attack 1: VRF Callback Race Condition

**v1.0 Verdict:** BLOCKED at multiple layers
**v1.0 Evidence:** `requestLootboxRng()` checks `rngLockedFlag` at AdvanceModule:635; `rngRequestTime != 0` at line 645; `rawFulfillRandomWords()` routes on `rngLockedFlag`

**Current Code Check:**
- `rngLockedFlag` guard in `requestLootboxRng()`: DegenerusGameAdvanceModule.sol:674 -- `if (rngLockedFlag) revert RngLocked();` -- PRESENT
- `rngRequestTime` guard: DegenerusGameAdvanceModule.sol:684 -- `if (rngRequestTime != 0) revert E();` -- PRESENT
- `rawFulfillRandomWords()` routing on `rngLockedFlag`: DegenerusGameAdvanceModule.sol:1326-1346 -- `if (rngLockedFlag) { rngWordCurrent = word; } else { ... lootbox finalization ... }` -- PRESENT
- Single `vrfRequestId` serialization: DegenerusGameAdvanceModule.sol:1331 -- `if (requestId != vrfRequestId || rngWordCurrent != 0) return;` -- PRESENT

**Delta:** UNCHANGED -- All guards present at new line numbers (functions shifted due to code additions elsewhere in file).
**Current Verdict:** PASS

---

### Attack 2: Deity Pass Purchase During Jackpot Resolution

**v1.0 Verdict:** BLOCKED
**v1.0 Evidence:** `_purchaseDeityPass()` checks `rngLockedFlag` at WhaleModule:468

**Current Code Check:**
- `rngLockedFlag` guard in `_purchaseDeityPass()`: DegenerusGameWhaleModule.sol:468 -- `if (rngLockedFlag) revert RngLocked();` -- PRESENT
- Additional guards (gameOver, symbolId bounds, uniqueness): DegenerusGameWhaleModule.sol:469-472 -- PRESENT

**Delta:** UNCHANGED -- Guard at same line number; no structural changes to function.
**Current Verdict:** PASS

---

### Attack 3: Ticket Purchase Manipulation During Lock

**v1.0 Verdict:** SAFE (double-buffer architecture)
**v1.0 Evidence:** Tickets queue into write buffer (`ticketQueue[writeKey]`); advanceGame processes from read buffer (`ticketQueue[readKey]`); `ticketsFullyProcessed` flag gates drain

**Current Code Check:**
- `_tqReadKey` usage for drain processing: DegenerusGameAdvanceModule.sol:169 -- `uint24 rk = _tqReadKey(purchaseLevel);` -- PRESENT
- `_tqWriteKey` usage for new ticket queuing: DegenerusGameAdvanceModule.sol:188 -- `uint24 wk = _tqWriteKey(purchaseLevel);` -- PRESENT
- `ticketsFullyProcessed` flag set after drain: DegenerusGameAdvanceModule.sol:178 -- `ticketsFullyProcessed = true;` -- PRESENT
- Additional drain paths: DegenerusGameAdvanceModule.sol:213-223 and :274 -- all set `ticketsFullyProcessed = true` -- PRESENT
- Write buffer swap guard: DegenerusGameAdvanceModule.sol:712-713 -- `if (ticketQueue[wk].length > 0 && ticketsFullyProcessed)` -- PRESENT

**Delta:** UNCHANGED -- Double-buffer architecture fully intact. Additional mid-day ticket RNG pending logic added (lines 162-167) but this is additive and does not weaken the buffer isolation.
**Current Verdict:** PASS

---

### Attack 4: Lootbox Open Timing Manipulation

**v1.0 Verdict:** SAFE by design (per-player entropy)
**v1.0 Evidence:** `openLootBox` uses `lootboxRngWordByIndex[index]` with per-player entropy `keccak256(rngWord, player, day, amount)`

**Current Code Check:**
- `lootboxRngWordByIndex[index]` lookup: DegenerusGameLootboxModule.sol:559 -- `uint256 rngWord = lootboxRngWordByIndex[index];` -- PRESENT
- Per-player entropy derivation: DegenerusGameLootboxModule.sol:580 -- `uint256 entropy = uint256(keccak256(abi.encode(rngWord, player, day, amount)));` -- PRESENT
- Same pattern in `openBurnieLootBox`: DegenerusGameLootboxModule.sol:637 and :654 -- independent `lootboxRngWordByIndex` + per-player entropy -- PRESENT
- Additional lootbox path: DegenerusGameLootboxModule.sol:693 -- same entropy pattern -- PRESENT

**Delta:** UNCHANGED -- Per-player entropy derivation identical. Additional lootbox paths follow same pattern.
**Current Verdict:** PASS

---

### Attack 5: Nudge Grinding (reverseFlip)

**v1.0 Verdict:** SAFE (economically prohibitive, blocked during lock)
**v1.0 Evidence:** `reverseFlip` checks `rngLockedFlag`; cost compounds at 50% per nudge; 256-bit word space

**Current Code Check:**
- `rngLockedFlag` guard in `reverseFlip()`: DegenerusGameAdvanceModule.sol:1299 -- `if (rngLockedFlag) revert RngLocked();` -- PRESENT
- Cost compounding: DegenerusGameAdvanceModule.sol:1301 -- `uint256 cost = _currentNudgeCost(reversals);` -- PRESENT
- Nudge mechanism in `_applyDailyRng`: DegenerusGameAdvanceModule.sol:1354-1360 -- `finalWord += nudges;` with counter reset -- PRESENT
- Function doc confirms 50% compounding: DegenerusGameAdvanceModule.sol:1294 -- "Cost scales +50% per queued nudge" -- PRESENT

**Delta:** UNCHANGED -- Guard and compounding mechanism intact. Nudge application moved to dedicated `_applyDailyRng` helper (line 1350) but logic is identical.
**Current Verdict:** PASS

---

### Attack 6: Block Builder VRF Front-Running

**v1.0 Verdict:** SAFE (two-phase commit prevents same-block manipulation)
**v1.0 Evidence:** VRF callback stores word in `rngWordCurrent`; consumption deferred to separate `advanceGame()` tx

**Current Code Check:**
- Daily path: `rawFulfillRandomWords()` stores `rngWordCurrent = word` at DegenerusGameAdvanceModule.sol:1338 -- callback only stores, does NOT process -- PRESENT
- Daily processing deferred to `rngGate()` called from `advanceGame()`: DegenerusGameAdvanceModule.sol:229 -- `uint256 rngWord = rngGate(ts, day, purchaseLevel, lastPurchase);` -- PRESENT
- `rngGate` reads stored word: DegenerusGameAdvanceModule.sol:746 -- `uint256 currentWord = rngWordCurrent;` -- PRESENT
- Lootbox path: callback directly finalizes at DegenerusGameAdvanceModule.sol:1341-1345 -- no user action influences this -- PRESENT
- Coordinator address validation: DegenerusGameAdvanceModule.sol:1330 -- `if (msg.sender != address(vrfCoordinator)) revert E();` -- PRESENT

**Delta:** UNCHANGED -- Two-phase commit architecture intact. Daily RNG nudge application now occurs in `_applyDailyRng` (called from `rngGate` at line 763), which is still within the separate `advanceGame` transaction.
**Current Verdict:** PASS

---

### Attack 7: Stale RNG Word Exploitation

**v1.0 Verdict:** SAFE (deterministic stale-word routing)
**v1.0 Evidence:** `rngGate()` at lines 700-709 detects stale words via `requestDay < day`; routes to `_finalizeLootboxRng()` only; fresh request made for current day

**Current Code Check:**
- `rngGate()` function: DegenerusGameAdvanceModule.sol:737 -- `function rngGate(...)` -- PRESENT
- `_simulatedDayIndexAt(rngRequestTime)` staleness check: DegenerusGameAdvanceModule.sol:751 -- `uint48 requestDay = _simulatedDayIndexAt(rngRequestTime);` -- PRESENT
- Stale word routing: DegenerusGameAdvanceModule.sol:754-759 -- `if (requestDay < day) { _finalizeLootboxRng(currentWord); rngWordCurrent = 0; _requestRng(isTicketJackpotDay, lvl); return 1; }` -- PRESENT
- Already-recorded shortcut: DegenerusGameAdvanceModule.sol:744 -- `if (rngWordByDay[day] != 0) return rngWordByDay[day];` -- PRESENT

**Delta:** UNCHANGED -- Stale-word detection and routing logic identical. Function signature gained `isTicketJackpotDay` parameter (was `lastPurchase` in v1.0) but staleness logic unchanged.
**Current Verdict:** PASS

---

### Attack 8: 50% Ticket Conversion Economic Impact

**v1.0 Verdict:** SAFE (economic improvement, not vulnerability)
**v1.0 Evidence:** `ticketConversionBps = 5_000` halves ticket count; ETH backing per ticket doubles; parameter is hardcoded

**Current Code Check:**
- `5_000` ticket conversion BPS: DegenerusGameJackpotModule.sol:684 -- `5_000 // 50% ticket conversion -- improves pool/ticket backing ratio` -- PRESENT
- `ticketConversionBps` parameter in `_distributeJackpotTickets`: DegenerusGameJackpotModule.sol:1117 -- `uint16 ticketConversionBps` -- PRESENT
- Ticket basis calculation: DegenerusGameJackpotModule.sol:1123 -- `uint256 ticketBasis = (lootboxBudget * ticketConversionBps) / 10_000;` -- PRESENT

**Delta:** UNCHANGED -- 50% conversion rate hardcoded at call site. Calculation logic identical.
**Current Verdict:** PASS

---

## FIX-1 Confirmation: claimDecimatorJackpot Freeze Guard

**Status:** CONFIRMED
**Location:** DegenerusGameDecimatorModule.sol:420

**Code:**
```solidity
function claimDecimatorJackpot(uint24 lvl) external {
    // Block claims while prize pools are frozen (jackpot phase days 1-4).
    // This path writes to futurePrizePool (lootbox portion) and
    // next/futurePrizePool (auto-rebuy) — allowing it during freeze would
    // corrupt the snapshot that advanceGame/runRewardJackpots operates on.
    if (prizePoolFrozen) revert E();

    uint256 amountWei = _consumeDecClaim(msg.sender, lvl);
    ...
```

**Verification:**
- Guard position: Before any state mutation (line 420, first executable statement after function signature at line 415) -- CORRECT
- `creditDecJackpotClaimBatch`: No `prizePoolFrozen` guard (correct) -- gated by `if (msg.sender != ContractAddresses.JACKPOTS) revert E();` at line 124, JACKPOTS-only internal path
- `creditDecJackpotClaim`: No `prizePoolFrozen` guard (correct) -- gated by `if (msg.sender != ContractAddresses.JACKPOTS) revert E();` at line 184, JACKPOTS-only internal path

**Pool Mutation Spot-Checks (from v1.0 exhaustive table):**

| v1.0 Entry | Location | v1.0 Protection | Current Code | Status |
|------------|----------|-----------------|--------------|--------|
| DegenerusGame.sol:414 (recordMint) | DegenerusGame.sol:409-414 | Routes to pending pools when frozen | `if (prizePoolFrozen) { _setPendingPools(...) } else { _setPrizePools(...) }` | CONFIRMED |
| DegenerusGame.sol:2824 (receive) | DegenerusGame.sol:2802-2807 | Routes to pending pools when frozen | `if (prizePoolFrozen) { _setPendingPools(...) } else { _setPrizePools(...) }` | CONFIRMED |
| WhaleModule:301 (purchaseWhalePass) | DegenerusGameWhaleModule.sol:296 | Routes to pending pools when frozen | `if (prizePoolFrozen) { ... }` guard present | CONFIRMED |
| WhaleModule:549 (purchaseDeityPass) | DegenerusGameWhaleModule.sol:544 | Routes to pending pools when frozen | `if (prizePoolFrozen) { ... }` guard present | CONFIRMED |

All 4 spot-checked entries confirmed present with identical protection patterns.

---

## Summary

| # | Attack / Finding | v1.0 Verdict | Current Verdict | Delta |
|---|-----------------|-------------|-----------------|-------|
| 1 | VRF Callback Race Condition | BLOCKED | PASS | UNCHANGED |
| 2 | Deity Pass Purchase During Jackpot | BLOCKED | PASS | UNCHANGED |
| 3 | Ticket Purchase Manipulation During Lock | SAFE | PASS | UNCHANGED |
| 4 | Lootbox Open Timing Manipulation | SAFE | PASS | UNCHANGED |
| 5 | Nudge Grinding (reverseFlip) | SAFE | PASS | UNCHANGED |
| 6 | Block Builder VRF Front-Running | SAFE | PASS | UNCHANGED |
| 7 | Stale RNG Word Exploitation | SAFE | PASS | UNCHANGED |
| 8 | 50% Ticket Conversion Economic Impact | SAFE | PASS | UNCHANGED |
| FIX-1 | claimDecimatorJackpot Freeze Guard | APPLIED | CONFIRMED | UNCHANGED |

**Overall assessment:** All 8 attack scenarios and FIX-1 from the v1.0 audit remain fully intact in the current codebase. No regressions detected. Line numbers have shifted in some cases due to code additions, but all guard mechanisms, architectural patterns, and hardcoded parameters are structurally unchanged.
