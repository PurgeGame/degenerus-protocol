---
status: IN_PROGRESS — Plan 250-01 single-plan multi-task
phase: 250 — Sibling-Pattern Sweep
milestone: v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
head: acd88512
plan: 250-01
deliverable: audit/v32-250-SIB.md
---

# v32.0 Phase 250 — Sibling-Pattern Sweep

**Audit head:** `acd88512` (Phase 247 / 248 / 249 anchor; both WIP guards already committed inside this SHA — turbo guard `!rngLockedFlag` third conjunct at AdvanceModule:173 + backfill guard `rngWordByDay[idx + 1] == 0` conjunct at AdvanceModule:1174 with call site at L1176).

**Phase:** 250 — Sibling-Pattern Sweep (SIB-01..05).

**Scope:** Pure-proof phase per CONTEXT.md D-250-CF-04; zero `contracts/` or `test/` writes. Phase 247 catalog (`audit/v32-247-DELTA-SURFACE.md` Section 1.4 + Section 6 D-247-I007..I012) is the SOLE scope input per D-250-CF-01.

**Status:** IN PROGRESS — Plan 250-01 Task 1 populates Sections 0 + 1 + 2. Sections 3-5 are RESERVED stubs for Tasks 2-4 of the same plan. Task 4 flips this file to FINAL READ-only.

## Section 0 — Overview & Verdict / Taxonomy / Row-ID Legends

### 0.1 Overview

This file is the authoritative v32.0 Phase 250 deliverable enumerating every interaction in `contracts/modules/DegenerusGameAdvanceModule.sol` (read at HEAD `acd88512` via `git show acd88512:<path>`) where `rngLockedFlag` is read or written alongside one of the 7 partner state-vars {`lastPurchaseDay`, `jackpotPhaseFlag`, `dailyIdx`, `level`, `purchaseStartDay`, `rngWordByDay[*]`, `phaseTransitionActive`}, classifying each interaction under the {turbo-class, backfill-class, ORTHOGONAL_PROVEN} taxonomy, auditing each delegating module (Mint, Jackpot, Whale, Lootbox, Degenerette, Boon, Decimator, GameOver) for the same patterns, cross-checking the 4 post-v31.0 landed commits (`8bdeabc2`, `ad41973c`, `6a63705b`, `48554f8f`) for sibling shapes (with `8bdeabc2` productive-pause as the closest sibling shape per ROADMAP criterion 4), and documenting any new bugs surfaced as `SIB-05-Vnn` finding-candidate rows with `awaiting-approval` proposed-fix blocks (or emitting the zero-state attestation if none surface).

### 0.2 Verdict Bucket Legend (per CONTEXT.md D-250-CF-05)

**SIB-01 / SIB-03 / SIB-04 verdict (3-bucket):**

| Verdict | Meaning |
|---|---|
| `SAFE` | Interaction proven safe under the v32.0 fix anchors (L173 turbo guard / L1174 backfill guard) or via cross-cite to an upstream invariant (Phase 248 BFL-* / Phase 249 PLV-* / v25-v31 prior audits). |
| `EXCEPTION` | Interaction is governed by a pre-locked KNOWN-ISSUES.md envelope (e.g., EXC-04 EntropyLib XOR-shift). Acceptance NOT re-litigated. |
| `FINDING_CANDIDATE` | Interaction has a reachability gap, missing guard, or potential race condition. Routes to SIB-05 (this phase) + Phase 253 FIND-01 (final) — NO `F-32-NN` ID assigned in this phase. |

**SIB-02 verdict (3-class taxonomy per D-250-04):**

| Class | Meaning (per D-250-05 / D-250-06 / D-250-07) |
|---|---|
| `turbo-class` | Per D-250-05: control flow takes one branch under one flag combination but a sibling branch could fire under a different combination, the second branch's preconditions are not symmetric to the first's, AND there exists a `(flagA, flagB)` cell where the branching predicate's intent (per code-comment / function-name semantics) is violated. Canonical example: turbo guard fix at AdvanceModule:173. |
| `backfill-class` | Per D-250-06: a state write is intended idempotent (at most once), the index-advance / sentinel-clear that gates re-entry is in a different state-var than the one being written, AND the gap between write and gate-clear admits a re-entry under the right `(flag, state)` combination. Canonical example: backfill guard fix at AdvanceModule:1174. |
| `ORTHOGONAL_PROVEN` | Per D-250-07: the interaction shows no race shape; isolation argument in one of three forms — Form 1 (no shared write boundary), Form 2 (sequential ordering enforced), Form 3 (mutex-equivalent). Each row picks one form and cites the upstream invariant by name. |

**SIB-05 advisory severity (D-08 5-bucket per D-250-16; INFO baseline; Phase 253 FIND-02 final call):**

| Severity | Meaning |
|---|---|
| `INFO` | Default — sibling shape exists structurally but no reachable trigger sequence under any input. |
| `LOW` | Trigger sequence reachable only under vanishingly improbable / sub-block-time invariant violation. |
| `MEDIUM` | Trigger sequence reachable but bounded impact. |
| `HIGH` | Trigger sequence reachable with material impact (mirrors v32.0 panic 0x11 class on a different code path). |
| `CRITICAL` | Reserved for direct-loss / unbounded-impact triggers. |

### 0.3 Row ID Legend

Per-REQ rows: `SIB-NN-VMM` (REQ-anchored, monotonic-within-REQ). Examples: `SIB-01-V01`, `SIB-01-V02`, ..., `SIB-03-V01`, `SIB-04-V01`, `SIB-05-V01`.

SIB-01 + SIB-02 are INLINED in a single 10-column table per D-250-02 + Claude's Discretion item 3 (planner final call: inline for readability). The SIB-02 verdict is the 8th column; the SIB-01 verdict is the 9th column. Evidence cite is the 10th column.

Column shape per D-250-02 (rendered as 10-column markdown table):

```
Row ID | Site (file:line) | Function | rngLockedFlag op {read/write} | Partner state-var | Partner op {read/write} | Same-branch span? {YES/NO} | SIB-02 class {turbo-class, backfill-class, ORTHOGONAL_PROVEN} | SIB-01 verdict {SAFE, EXCEPTION, FINDING_CANDIDATE} | Evidence cite (grep recipe + reasoning sentence + VRF-window annotation if applicable)
```

### 0.4 Named-Invariant Legend (carry-forward from Phase 249 §0.4)

| Invariant ID | Statement | Anchor Cite | Phase 250 Use |
|---|---|---|---|
| `INV-PLV-A-01` | `lastPurchaseDay = T` ⇒ `rngLockedFlag = F` at every reachable advanceGame entry boundary, OR control flow already passed the L173 turbo guard skip-branch. | AdvanceModule:173 turbo guard third conjunct + AdvanceModule:1607 `_finalizeRngRequest` sets `rngLockedFlag = true` AFTER the level pre-increment + AdvanceModule:1704 `_unlockRng` clears `rngLockedFlag = false` when `dailyIdx` advances. | SIB-01-V01 verdict cross-cite; ORTHOGONAL_PROVEN Form 3 (mutex-equivalent) anchor for any rngLockedFlag × lastPurchaseDay co-read row that classifies orthogonal. |
| `INV-PLV-B-01` | `jackpotPhaseFlag = T` ⇒ `level ≥ 1` (set only after at least one `_finalizeRngRequest` has committed `level = lvl`). | AdvanceModule:442-444 `jackpotPhaseFlag = true; lastPurchaseDay = false;` co-write at phase transition; only fires inside `advanceGame` AFTER `_consolidatePoolsAndRewardJackpots` (L426-432) which runs only after at least one `_finalizeRngRequest` call committed `level = lvl ≥ 1` at AdvanceModule:1616. | ORTHOGONAL_PROVEN Form 3 anchor for jackpotPhaseFlag × lastPurchaseDay co-read rows; load-bearing for Mint:923 SIB-03 row UNREACHABLE-via-PLV-01-V31 cross-cite. |
| `INV-PLV-C-01` | `level` only advances inside `_finalizeRngRequest` at AdvanceModule:1616 (sole runtime writer at HEAD `acd88512`). | Sole-writer grep `grep -nE '\blevel\s*=' contracts/modules/DegenerusGameAdvanceModule.sol` returns the constructor init implicit at storage L250 + L1616 (runtime) — exactly 1 runtime hit per Phase 249 INV-PLV-C-01. | ORTHOGONAL_PROVEN Form 3 anchor for level × any-flag co-read rows; load-bearing for Mint:923 cell unreachability. |

## Section 1 — SIB-01 + SIB-02: AdvanceModule pair-grep enumeration with inlined SIB-02 classification

Per CONTEXT.md D-250-01 (state-var pair sweep; rngLockedFlag × 7 partners) + D-250-02 (8-col column shape) + D-250-CF-08 (grep-reproducibility) + D-250-04 / D-250-05 / D-250-06 / D-250-07 (SIB-02 taxonomy) + Claude's Discretion item 3 (inline SIB-02 verdict on SIB-01 row).

### 1.1 Master grep recipe block (D-250-CF-08 reproducibility)

The 7 master pair-greps below identify every site where each partner state-var is referenced in `contracts/modules/DegenerusGameAdvanceModule.sol` at HEAD `acd88512` (1869 lines via `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol | wc -l`). Aggregate raw-hit count: **105 hits** across the 7 partners + `rngLockedFlag` (note: CONTEXT.md scout said 44 raw hits across the 7 partners; the actual count is higher because the `\blevel\b` regex surfaces 59 hits including comment / NatSpec mentions — the same-branch-span filter per §1.2 narrows to the SIB-01 row count below). Same-branch-span filter narrows to **9 SIB-01-Vnn rows** below.

```bash
# Per D-250-CF-08, portable POSIX grep (no GNU -P / Perl regex):
grep -n 'rngLockedFlag' contracts/modules/DegenerusGameAdvanceModule.sol           # 9 hits
grep -n 'lastPurchaseDay' contracts/modules/DegenerusGameAdvanceModule.sol         # 9 hits
grep -n 'jackpotPhaseFlag' contracts/modules/DegenerusGameAdvanceModule.sol        # 3 hits
grep -n 'dailyIdx' contracts/modules/DegenerusGameAdvanceModule.sol                # 8 hits
grep -nE '\blevel\b' contracts/modules/DegenerusGameAdvanceModule.sol              # 59 hits (most are NatSpec/comments)
grep -n 'purchaseStartDay' contracts/modules/DegenerusGameAdvanceModule.sol        # 4 hits
grep -n 'rngWordByDay' contracts/modules/DegenerusGameAdvanceModule.sol            # 10 hits
grep -n 'phaseTransitionActive' contracts/modules/DegenerusGameAdvanceModule.sol   # 3 hits
```

Hit-count attestation: `wc -l` aggregate = 105. The `\blevel\b` regex contributes the bulk via NatSpec/comment mentions; code-only filtering reduces actual code-site hits to ~10-15 across the `level` partner. Detailed hit listings persisted at `/tmp/v32-250-<partner>.txt` during execution.

### 1.2 Same-branch-span filter rule

A row qualifies as an SIB-01 interaction iff `rngLockedFlag` is read or written AND a partner state-var is read or written within:
- the SAME function body, AND
- the SAME control-flow branch (same `if`/`else` body, same loop body, OR sequential statements with no intervening write that breaks the dependency).

Co-reads across separate branches with no shared dependency are NOT row-eligible. They are surfaced in §1.4 (sequential-ordering interactions for ORTHOGONAL_PROVEN Form 2) only when the function-level co-occurrence is informative.

### 1.3 SIB-01-Vnn interaction rows (pre-seeds + enumerated per Step B greps)

| Row ID | Site | Function | rngLockedFlag op | Partner state-var | Partner op | Same-branch span? | SIB-02 class | SIB-01 verdict | Evidence cite |
|---|---|---|---|---|---|---|---|---|---|
| SIB-01-V01 | AdvanceModule:173 | `advanceGame` (turbo block IF condition) | read (third conjunct `!rngLockedFlag`) | `lastPurchaseDay` (second conjunct) + `jackpotPhaseFlag` (via `inJackpot = jackpotPhaseFlag` at L164, used as first conjunct `!inJackpot`) | read | YES (single IF expression at L173) | turbo-class | SAFE | THE FIX. Cross-cite Phase 249 audit/v32-249-PLV.md §3 PLV-03 ternary unreachable proof + O-PLV-TTF lvl=0 cell INV-PLV-A-01 disproof. Turbo block at L167-182 sets `lastPurchaseDay = true` at L178 ONLY when `!inJackpot && !lastPurchaseDay && !rngLockedFlag` holds (the L173 IF). Canonical turbo-class shape per D-250-05. VRF-window annotation: this is the entry-side of the VRF lock window — when rngLockedFlag = T (VRF in flight), turbo block is skipped; player-controllable state cannot change the rngLockedFlag value in this window (only `_finalizeRngRequest` at L1607 can set it T; `_unlockRng` at L1704 / `updateVrfCoordinatorAndSub` at L1663 / `rawFulfillRandomWords` at L1728 are the only other rngLockedFlag-writers). Grep recipe: `grep -n 'rngLockedFlag' contracts/modules/DegenerusGameAdvanceModule.sol` returns L173 alongside L1049/L1607/L1663/L1704/L1728; L173 is the load-bearing read inside the turbo guard. |
| SIB-01-V02 | AdvanceModule:1174 | `rngGate` (fresh-word branch backfill guard) | read indirectly via VRF lock window (rngLockedFlag is true throughout `rngGate`'s fresh-word branch entry path; backfill guard reads `rngWordByDay[idx + 1] == 0` at L1174 inside that branch which fires only after VRF fulfilment via `rawFulfillRandomWords` set `rngWordCurrent != 0` while `rngLockedFlag = T`) | `rngWordByDay[*]` (L1174 read) + `dailyIdx` (L1173 read `uint32 idx = dailyIdx;`) | read | YES (L1173-L1174 sequential reads inside the fresh-word IF block; call site at L1176) | backfill-class | SAFE | THE FIX. Cross-cite Phase 248 audit/v32-248-BFL.md §2 BFL-02 sentinel-correctness 4-step proof + §3 BFL-03 testnet blocks 10759449 + 10761786 worked example. Backfill guard at L1174 makes `_backfillGapDays` (sole call site at L1176) idempotent: `rngWordByDay[idx + 1]` is 0 only when backfill has not yet run for this lock window; once `_backfillGapDays` writes `rngWordByDay[gapDay]` at L1766 (per `_backfillGapDays` body L1762-1769), the L1174 gate short-circuits subsequent re-entries within the same lock window. Canonical backfill-class shape per D-250-06. VRF-window annotation: this is the post-fulfillment-side of the VRF lock window — backfill executes during the rngGate fresh-word branch which fires once per VRF fulfillment; player-controllable state in the window {dailyIdx, rngWordByDay[*], purchaseStartDay} cannot make backfill re-fire because the L1174 sentinel is owned by `_backfillGapDays` itself (write at L1766) and `dailyIdx` is owned by `_unlockRng` at L1703 (sole writer per Phase 248 BFL-04). Grep recipe: `grep -n 'rngWordByDay' contracts/modules/DegenerusGameAdvanceModule.sol` returns L563/L1060/L1160/L1174/L1244/L1336/L1766/L1814 (writers + readers); L1174 is the load-bearing guard read. |
| SIB-01-V03 | AdvanceModule:185 | `advanceGame` (purchaseLevel bind ternary) | read (`(lastPurchase && rngLockedFlag) ? lvl : lvl + 1`) | `level` (read via `lvl = level` at L165 + `lastPurchaseDay` via `lastPurchase` at L183) | read | YES (single ternary expression at L185) | turbo-class | SAFE | The downstream consumer of the L173 turbo guard. Cross-cite Phase 249 audit/v32-249-PLV.md §3 PLV-03 (proves the ternary cannot return 0 once L173 guard short-circuits the (lastPurchase=T ∧ rngLockedFlag=T ∧ lvl=0) cell). Verdict SAFE-via-PLV-03. Same shape as V01 (the fix); V01 is the gate, V03 is the consumer. VRF-window annotation: same window as V01; rngLockedFlag value at L185 is consistent with the L173 evaluation because no rngLockedFlag write occurs between L173 and L185. |
| SIB-01-V04 | AdvanceModule:1049 | `requestLootboxRng` (entry guard) | read (`if (rngLockedFlag) revert RngLocked();`) | `level` (read at L1081 `priceForLevel(level)` + L1097 `purchaseLevel_ = level + 1`) | read | NO (sequential statements separated by ~30 lines; no shared if-body; intervening logic reads other state) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Per D-250-07 Form 2: `rngLockedFlag` revert-check at L1049 strictly precedes any `level` read at L1081/L1097; if rngLockedFlag = T, function reverts before reading `level`. The two reads cannot both produce stale composition because L1049 acts as a global gate on the rest of the function. INV-PLV-A-01 anchor: `level` read here is decoupled from the rngLockedFlag value via the early revert. VRF-window annotation: `requestLootboxRng` cannot execute while VRF is in flight (the L1049 guard); player-controllable state at L1081/L1097 reads `level` AFTER the rngLockedFlag check passed; `level` cannot be written from outside `_finalizeRngRequest` per INV-PLV-C-01, and `_finalizeRngRequest` cannot run while `requestLootboxRng` is in mid-execution (Solidity is single-threaded per transaction). |
| SIB-01-V05 | AdvanceModule:1607-1609-1616 | `_finalizeRngRequest` (state commit) | write (`rngLockedFlag = true;` at L1607) | `level` (write `level = lvl;` at L1616 inside `if (isTicketJackpotDay && !isRetry)` body) | write | YES (sequential writes inside the same function body; L1607 unconditional, L1616 inside the `isTicketJackpotDay && !isRetry` IF body that immediately follows L1610-1614 setup) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Per D-250-07 Form 2: `rngLockedFlag = true` write at L1607 strictly precedes `level = lvl` write at L1616 (~9 source lines apart); both writes commit before the function returns. The `level` increment is intentional (Phase 249 PLV-03 calls this out as the level pre-increment that closes the (lastPurchase=T ∧ rngLockedFlag=T ∧ lvl=0) unreachable cell). INV-PLV-C-01 anchor: this is the SOLE runtime `level` writer per the canonical `level` writer enumeration. VRF-window annotation: this row OPENS the VRF lock window via the L1607 write; the L1616 level increment is the load-bearing pre-increment that ensures `purchaseLevel` ternary at L185 cannot return 0 on the next advanceGame entry. No player-controllable state can change between L1607 and L1616 (single-statement-thread inside one function). |
| SIB-01-V06 | AdvanceModule:1703-1704 | `_unlockRng` (state release) | write (`rngLockedFlag = false;` at L1704) | `dailyIdx` (write `dailyIdx = day;` at L1703) | write | YES (adjacent sequential writes inside the same function body) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Per D-250-07 Form 2: `dailyIdx = day` write at L1703 strictly precedes `rngLockedFlag = false` write at L1704 (1-line gap; both unconditional inside `_unlockRng`). This is the sole runtime `dailyIdx` writer per Phase 248 BFL-04 and the sole runtime rngLockedFlag-clears-via-_unlockRng path. The ordering ensures any external observer that sees `rngLockedFlag = F` after `_unlockRng` returns ALSO sees the new `dailyIdx`; no observable window where rngLockedFlag = F but dailyIdx still holds the prior value. VRF-window annotation: this row CLOSES the VRF lock window; backfill guard at L1174 reads `dailyIdx` via L1173 BEFORE the next `_unlockRng` runs, so the L1174 sentinel correctly identifies "backfill not yet run for this lock window". |
| SIB-01-V07 | AdvanceModule:1728 | `rawFulfillRandomWords` (VRF callback dispatch) | read (`if (rngLockedFlag) {`) | `rngWordByDay[*]` (write `rngWordCurrent = word;` at L1731 — used downstream by `_applyDailyRng` at L1813 to commit `rngWordByDay[day] = finalWord;` at L1814; in this function only the staging write happens) + `dailyIdx` (no direct write, but mid-day branch reads `_lrRead(LR_INDEX_SHIFT, ...)` which is decoupled from dailyIdx) | read (rngLockedFlag) + write (rngWordCurrent staging) | YES (single IF expression at L1728 splits daily-RNG storage from mid-day RNG finalize; sequential writes inside the IF body) | ORTHOGONAL_PROVEN (Form 3 mutex-equivalent) | SAFE | Per D-250-07 Form 3: `rngLockedFlag = T` ⇒ daily-RNG path takes (stages `rngWordCurrent`); `rngLockedFlag = F` ⇒ mid-day RNG path takes (finalizes lootbox). The two branches are mutually exclusive by the L1728 IF; no race between daily and mid-day RNG storage. INV-PLV-A-01 anchor: rngLockedFlag value at L1728 is consistent with the entry conditions (`requestId == vrfRequestId` AND `rngWordCurrent == 0` checked at L1727 before the L1728 dispatch). VRF-window annotation: this is the VRF FULFILMENT-side of the lock window; entry is gated by `requestId == vrfRequestId` which means the VRF coordinator must callback with the exact ID requested by `_finalizeRngRequest` at L1607 (so the rngLockedFlag = T branch can ONLY fire if `_finalizeRngRequest` opened the lock window). Player-controllable state cannot change `vrfRequestId` between request and fulfilment (no external setter). |
| SIB-01-V08 | AdvanceModule:1663 | `updateVrfCoordinatorAndSub` (admin VRF rotation) | write (`rngLockedFlag = false;`) | `dailyIdx` (no direct write here) + `rngWordByDay[*]` (no direct write here; `vrfRequestId = 0`, `rngRequestTime = 0`, `rngWordCurrent = 0` are the partner-adjacent writes) | write (related state reset, no direct partner state-var write) | YES (sequential resets inside the admin function) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary) | SAFE | Per D-250-07 Form 1: this is an emergency VRF coordinator rotation called only by `ContractAddresses.ADMIN` (gated at L1659). The function resets `rngLockedFlag`, `vrfRequestId`, `rngRequestTime`, `rngWordCurrent` to clean state but does NOT write `dailyIdx` or `rngWordByDay[*]` — those are owned by `_unlockRng` / `_applyDailyRng` respectively. No shared write boundary with the daily-RNG path; admin can rotate the coordinator without disturbing the dailyIdx ↔ rngWordByDay invariant. INV-PLV-A-01 anchor: post-rotation `rngLockedFlag = F` and any future `_finalizeRngRequest` re-opens the lock window cleanly via the rotated coordinator. VRF-window annotation: this row TERMINATES a stale lock window (the admin is invoked precisely when VRF has stalled past the 12h timeout); player-controllable state CANNOT call this function (admin gate at L1659). |
| SIB-01-V09 | AdvanceModule:166-178 | `advanceGame` (turbo block body) | (no rngLockedFlag write inside the turbo block; rngLockedFlag is the L173 GATE) | `purchaseStartDay` (read at L166 `uint32 psd = purchaseStartDay;`) + `lastPurchaseDay` (write at L178 `lastPurchaseDay = true;`) | read + write | YES (turbo block body L167-182 — psd read at L166 just before the L173 gate; lastPurchaseDay write at L178 inside the turbo body) | turbo-class | SAFE | The turbo block's intent is to set `lastPurchaseDay = true` early when target met on day ≤1. The L178 `lastPurchaseDay = true` write is gated by L173's `!rngLockedFlag` (the v32.0 fix). Same turbo-class shape as V01 (V01 is the gate's IF expression; V09 is the gate-protected body). Cross-cite Phase 249 §3 PLV-03 + Phase 249 §6 PLV-06 (which proves the daily-jackpot path's L399 lastPurchaseDay=T write also composes correctly with `_unlockRng` at L404). VRF-window annotation: turbo block executes only when rngLockedFlag = F (per L173); the L178 write happens BEFORE any VRF state mutation in the same advanceGame call, so player-controllable state cannot inject a rngLockedFlag = T between L173 and L178. |

### 1.4 Sequential-ordering interactions (ORTHOGONAL_PROVEN Form 2 anchors; informative-only)

The L185 ternary's relationship with the L173 turbo guard (V01 → V03) is the canonical Form 2 chain: `rngLockedFlag` evaluated at L173 either skips the turbo body (rngLockedFlag = T branch) or sets `lastPurchaseDay = true` at L178 (rngLockedFlag = F branch); either way, the L185 ternary reads consistent state per the V03 row. No additional sequential-ordering interactions surface beyond V05 / V06 / V08 already enumerated in §1.3.

### 1.5 SIB-01 / SIB-02 Finding Candidates

Per CONTEXT.md D-250-CF-03: any SIB-01 row classified `FINDING_CANDIDATE` is enumerated here with path:line + suggested severity routing for §5 SIB-05. NO `F-32-NN` IDs assigned.

**Zero FINDING_CANDIDATE rows in §1.3.** All 9 SIB-01 rows resolve to SAFE — V01/V02 are THE FIXES themselves; V03 inherits SAFE-via-PLV-03; V04/V05/V06/V08 classify ORTHOGONAL_PROVEN (Form 1 / Form 2); V07 classifies ORTHOGONAL_PROVEN (Form 3); V09 inherits SAFE from V01's gate. No row resists 3-bucket SIB-02 classification.

### 1.6 Section 1 verdict-count attestation

Total SIB-01 rows: **9**. Verdict breakdown: SAFE = 9, EXCEPTION = 0, FINDING_CANDIDATE = 0.

Total SIB-02 classification breakdown: turbo-class = 3 (V01 + V03 + V09 — the turbo guard + ternary + body), backfill-class = 1 (V02 — the backfill guard), ORTHOGONAL_PROVEN = 5 (V04 Form 2 + V05 Form 2 + V06 Form 2 + V07 Form 3 + V08 Form 1). Sum: 3 + 1 + 5 = 9. ✓

## Section 2 — SIB-02 verdict legend + isolation-argument forms reference (populated as part of §1.3 row classification rationale)

Per Claude's Discretion item 3 (planner final call: inline SIB-02 verdict on SIB-01 row), Section 2 is NOT a separate row-table. It is a reference subsection containing:
- The 3-bucket SIB-02 taxonomy (already documented in §0.2).
- The three D-250-07 ORTHOGONAL_PROVEN forms (already documented in §1.3 per-row classification rationale).
- A by-class roll-up of which §1.3 SIB-01 rows landed in each class.

### 2.1 Per-class roll-up

**turbo-class rows (D-250-05):**
- SIB-01-V01 — the L173 fix itself (the IF gate).
- SIB-01-V03 — the L185 ternary downstream consumer (SAFE-via-PLV-03).
- SIB-01-V09 — the L178 lastPurchaseDay write inside the turbo body (gated by V01).

**backfill-class rows (D-250-06):**
- SIB-01-V02 — the L1174 fix itself (the sentinel-clear gate).

**ORTHOGONAL_PROVEN rows (D-250-07):**
- SIB-01-V04 — Form 2 (sequential ordering enforced): `requestLootboxRng` L1049 revert-check precedes L1081/L1097 level reads.
- SIB-01-V05 — Form 2 (sequential ordering enforced): `_finalizeRngRequest` L1607 rngLockedFlag write precedes L1616 level write.
- SIB-01-V06 — Form 2 (sequential ordering enforced): `_unlockRng` L1703 dailyIdx write immediately precedes L1704 rngLockedFlag clear.
- SIB-01-V07 — Form 3 (mutex-equivalent): `rawFulfillRandomWords` L1728 IF dispatches mutually-exclusive daily vs mid-day RNG branches.
- SIB-01-V08 — Form 1 (no shared write boundary): `updateVrfCoordinatorAndSub` admin reset does not write dailyIdx / rngWordByDay.

### 2.2 Section 2 verdict-count cross-check

Total: turbo-class (3) + backfill-class (1) + ORTHOGONAL_PROVEN (5) = 9. Matches §1.6 SIB-02 breakdown. ✓

## Section 3 — SIB-03: Module audit (8 delegating modules)

Per CONTEXT.md D-250-08 (same-shape filter) + D-250-09 (NEGATIVE-scope row format) + D-250-10 (MintModule:923 ownership) + D-250-11 (cross-module PLV-01 inheritance).

### 3.1 Master grep recipe block (D-250-CF-08 reproducibility — 8 modules)

For each delegating module, the partner-state-var grep set runs at HEAD `acd88512`:

```bash
for MOD in DegenerusGameMintModule DegenerusGameJackpotModule DegenerusGameWhaleModule \
           DegenerusGameLootboxModule DegenerusGameDegeneretteModule DegenerusGameBoonModule \
           DegenerusGameDecimatorModule DegenerusGameGameOverModule; do
  grep -nE '(rngLockedFlag|lastPurchaseDay|jackpotPhaseFlag|dailyIdx|level|purchaseStartDay|rngWordByDay|phaseTransitionActive)' \
    contracts/modules/$MOD.sol
done
```

Aggregate raw hit counts at HEAD `acd88512`: Mint=58, Jackpot=46, Whale=60, Lootbox=57, Degenerette=4, Boon=2, Decimator=26, GameOver=10 (wide regex including the dense `\blevel\b` matches in NatSpec). The flag-only (rngLockedFlag / jackpotPhaseFlag / lastPurchaseDay / phaseTransitionActive) grep is the load-bearing filter for D-250-08 same-shape interactions:

```bash
for MOD in DegenerusGameMintModule DegenerusGameJackpotModule DegenerusGameWhaleModule \
           DegenerusGameLootboxModule DegenerusGameDegeneretteModule DegenerusGameBoonModule \
           DegenerusGameDecimatorModule DegenerusGameGameOverModule; do
  grep -nE '(rngLockedFlag|jackpotPhaseFlag|lastPurchaseDay|phaseTransitionActive)' \
    contracts/modules/$MOD.sol
done
```

Flag-only attestation: Mint=3 hits (L903 / L921 / L1229), Whale=1 hit (L543 `if (rngLockedFlag) revert RngLocked();` in `_purchaseDeityPass` — bare flag-only revert, no counter co-read at L543), Jackpot=0, Lootbox=0, Degenerette=0, Boon=0, Decimator=0, GameOver=0. Five modules at zero flag hits → NEGATIVE-scope rows per D-250-09. Three modules (Mint / Whale / Decimator-via-counter-only / GameOver-via-counter-only) need same-shape filter applied.

NOTE on CONTEXT.md scout divergence (D-250-CF-07 scope-guard observation): CONTEXT.md `<code_context>` Git Infrastructure paragraph said "13 hits across 6 modules with 3 modules (Jackpot/Boon/Degenerette) at zero." Actual at HEAD `acd88512`: 5 modules at zero flag hits (Jackpot, Lootbox, Degenerette, Boon, Decimator-flag-only, GameOver-flag-only). The actual surface is NARROWER than the scout suggested (more zero-modules), confirming the working hypothesis that no new sibling-pattern bug surfaces beyond the L173 / L1174 fix anchors.

### 3.2 SIB-03-Vnn module audit rows

| Row ID | Module (file:line) | Function | Flag op + state-var | Counter op + state-var | Same-shape? | SIB-02 class | SIB-03 verdict | Evidence cite |
|---|---|---|---|---|---|---|---|---|
| SIB-03-V01 | DegenerusGameMintModule.sol:923 | `_purchaseFor` | `cachedJpFlag` (read at L921 from `jackpotPhaseFlag`) | `cachedLevel` (read at L922 from `level`) | YES (same expression at L923 ternary `cachedJpFlag ? cachedLevel : cachedLevel + 1`) | ORTHOGONAL_PROVEN (Form 3 mutex-equivalent) | SAFE | Cross-cite Phase 249 audit/v32-249-PLV.md PLV-01-V31 SAFE verdict via INV-PLV-B-01 + INV-PLV-C-01 composition. Phase 249 §1.2 V31 evidence proves `(cachedJpFlag = T ∧ cachedLevel = 0)` cell UNREACHABLE: by INV-PLV-B-01, `jackpotPhaseFlag = T` is only set at AdvanceModule:442 which executes inside advanceGame AFTER `_consolidatePoolsAndRewardJackpots` (L426-432) which runs only after at least one purchase phase has driven a `_finalizeRngRequest` call that committed `level = lvl ≥ 1` at AdvanceModule:1616 (per INV-PLV-C-01). Therefore at the moment `cachedJpFlag = T` is read, `cachedLevel = level ≥ 1`. The ternary returns `cachedLevel ≥ 1` in the T branch and `cachedLevel + 1 ≥ 1` in the F branch. ORTHOGONAL_PROVEN Form 3 because `rngLockedFlag = T` does not co-occur with `cachedJpFlag = T` in this function's scope (rngLockedFlag not read at L923); upstream invariant INV-PLV-B-01 enforces the safe co-occurrence. Per D-250-10 second clause (UNREACHABLE → ORTHOGONAL_PROVEN, NOT FINDING_CANDIDATE). No re-derivation. |
| SIB-03-V02 | DegenerusGameMintModule.sol:1226 | `_callTicketPurchase` | `cachedJpFlag` (parameter from `_purchaseFor` callsite) | `cachedLevel` (parameter, ≥1 by V01 caller invariant) | YES (ternary at L1226 `targetLevel = cachedJpFlag ? cachedLevel : cachedLevel + 1`) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Cross-cite Phase 249 PLV-01-V31 (caller's purchaseLevel binding). The L1226 ternary is structurally identical to L923 (the V01 site) but here `cachedJpFlag` and `cachedLevel` arrive as parameters from the caller. The caller (`_purchaseFor` at L897-903 callsite to `_callTicketPurchase`, passing `level` and `jackpotPhaseFlag`) binds these from the same V01-protected reads. Form 2 because the `_purchaseFor` ternary at L923 strictly precedes the `_callTicketPurchase` ternary at L1226 in any caller execution; both inherit V01's UNREACHABLE-cell invariant. |
| SIB-03-V03 | DegenerusGameMintModule.sol:1229 | `_callTicketPurchase` | `cachedJpFlag` (param) + `rngLockedFlag` (read directly at L1229 `if (cachedJpFlag && rngLockedFlag)`) | `cachedLevel` (param at L1226 just above; `cachedCnt` `jackpotCounter` partner counter at L1224) | YES (sequential statements L1226-L1240 inside `_callTicketPurchase`) | ORTHOGONAL_PROVEN (Form 3 mutex-equivalent) | SAFE | This is a flag-vs-flag-vs-counter triple: `cachedJpFlag && rngLockedFlag` gates the L1230-L1239 last-jackpot-day fix that promotes targetLevel to `cachedLevel + 1`. Per D-250-07 Form 3: when `rngLockedFlag = T ∧ cachedJpFlag = T`, the function is being called from `advanceGame` post-`_finalizeRngRequest` (which set rngLockedFlag = T at AdvanceModule:1607 AFTER level was incremented at L1616 per INV-PLV-C-01); therefore `cachedLevel ≥ 1` at L1229 by the same INV-PLV-B-01 + INV-PLV-C-01 composition that closes V01. The L1230-L1239 step calculation is bounded by JACKPOT_LEVEL_CAP (compile-time constant); `cachedCnt + step >= JACKPOT_LEVEL_CAP` triggers the `targetLevel = cachedLevel + 1` re-routing — safe because `cachedLevel + 1 ≥ 2`. No FINDING_CANDIDATE: this is intentional tightening, not a missing guard. SCOPE-GUARD note (D-250-CF-07): this site is NEW vs Phase 249 PLV-01 enumeration — recorded here as a SIB-03 row with verdict ORTHOGONAL_PROVEN; deferral to Phase 253 FIND-04 commit-readiness register if needed. |
| SIB-03-V04 | DegenerusGameJackpotModule.sol | NEGATIVE | `grep -nE '(rngLockedFlag\|lastPurchaseDay\|jackpotPhaseFlag\|phaseTransitionActive)' contracts/modules/DegenerusGameJackpotModule.sol → 0 matches` | n/a | n/a | ORTHOGONAL_PROVEN (vacuous) | SAFE-by-vacuity | Per D-250-09 NEGATIVE-scope row format. JackpotModule does not reference any of the 4 flag state-vars at HEAD `acd88512`; sibling-pattern surface is empty by construction. Wide-regex grep returns 46 hits (all on `level` or `dailyIdx` in NatSpec / counter-only contexts; no flag co-read) — none qualify same-shape per D-250-08. |
| SIB-03-V05 | DegenerusGameWhaleModule.sol:543 | `_purchaseDeityPass` | `rngLockedFlag` read (`if (rngLockedFlag) revert RngLocked();`) | n/a (no counter co-read at L543; entry-gate only) | NO (bare flag revert; no counter co-read in same statement or branch) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary) | SAFE | Per D-250-07 Form 1: the L543 `rngLockedFlag` revert is a global entry-gate for `_purchaseDeityPass` (deity pass purchase). No counter state-var (`level` / `dailyIdx` / `purchaseStartDay` / `rngWordByDay`) is read in the same expression or same-branch span. The function reads `mintPacked_[buyer]` and `deityPassOwners.length` for pricing — neither is in the partner set. No shared write boundary between rngLockedFlag and the deity pass storage. |
| SIB-03-V06 | DegenerusGameWhaleModule.sol:841 | `_recordLootboxEntry` | n/a (no rngLockedFlag read in WhaleModule outside L543) | `purchaseLevel` parameter receive (passed by caller from MintModule:923 / AdvanceModule:185) | NO (parameter passthrough; no flag co-read at this site) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Cross-cite Phase 249 PLV-01-V35 SAFE-pending-V31 (resolved SAFE per V31). Caller binds purchaseLevel upstream at MintModule:923 (per V31 INV-PLV-B-01 + INV-PLV-C-01 composition guaranteeing purchaseLevel ≥ 1); WhaleModule:841 receives the bound value by parameter and passes it through to L876 packing. Form 2 because the upstream MintModule:923 binding strictly precedes the WhaleModule:841 read in any caller's execution (no intervening control flow re-orders). |
| SIB-03-V07 | DegenerusGameWhaleModule.sol:876 | `_recordLootboxEntry` | n/a | `lootboxEth[index][buyer] = (uint256(purchaseLevel) << 232) \| newAmount` packing-write | NO (write-time invariant) | ORTHOGONAL_PROVEN (Form 3 mutex-equivalent) | SAFE | Cross-cite Phase 249 PLV-01-V34 SAFE. Writer's purchaseLevel ≥ 1 enforced by binder at AdvanceModule:185 / MintModule:923 upstream; LootboxModule:532 packed-decode (V08 below) consumes this packed value at unpacking time. Form 3 because writer's invariant (purchaseLevel ≥ 1) holds at packing time; reader at LootboxModule:532 inherits the same invariant via the packed value. |
| SIB-03-V08 | DegenerusGameLootboxModule.sol:532 | `openLootBox` | n/a | `uint24 purchaseLevel = uint24(packed >> 232)` packed-decode | NO (consumes packed value from WhaleModule:876 write) | ORTHOGONAL_PROVEN (Form 3 mutex-equivalent) | SAFE | Cross-cite Phase 249 PLV-01-V33 SAFE. Decoded value's purchaseLevel ≥ 1 invariant enforced by packing site WhaleModule:876 (V07 above). Form 3 because LootboxModule:532 reads the packed storage that was written under WhaleModule's purchaseLevel ≥ 1 invariant (which itself inherits from V01 binder). |
| SIB-03-V09 | DegenerusGameLootboxModule.sol:552 | `openLootBox` | n/a | `withinGracePeriod ? graceLevel : purchaseLevel` conditional read (passthrough on F branch); `graceLevel` on T branch derived from `baseLevelPacked == 0 ? currentLevel : baseLevelPacked - 1` at L551 (where `currentLevel = level + 1` at L549) | NO | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Cross-cite Phase 249 PLV-01-V36 SAFE. Passthrough preserves writer's purchaseLevel from V08; F branch returns the V08-decoded value (≥1 by inheritance); T branch returns graceLevel which is `currentLevel = level + 1 ≥ 1` (storage `level ≥ 0`, +1 makes it ≥1) or `baseLevelPacked - 1 ≥ 0` (with the V07 packing invariant ensuring baseLevelPacked = uint24(level + 2) when written, so baseLevelPacked - 1 ≥ 1). Form 2 sequential ordering: V08 packed-decode strictly precedes the L552 ternary. |
| SIB-03-V10 | DegenerusGameDegeneretteModule.sol | NEGATIVE | `grep -nE '(rngLockedFlag\|jackpotPhaseFlag\|lastPurchaseDay\|phaseTransitionActive)' contracts/modules/DegenerusGameDegeneretteModule.sol → 0 matches` | n/a | n/a | ORTHOGONAL_PROVEN (vacuous) | SAFE-by-vacuity | Per D-250-09 NEGATIVE-scope row format. Module does not reference any of the 4 flag state-vars at HEAD `acd88512`. Wide-regex grep returns 4 hits (all NatSpec mentions of `level` in comments). |
| SIB-03-V11 | DegenerusGameBoonModule.sol | NEGATIVE | `grep -nE '(rngLockedFlag\|jackpotPhaseFlag\|lastPurchaseDay\|phaseTransitionActive)' contracts/modules/DegenerusGameBoonModule.sol → 0 matches` | n/a | n/a | ORTHOGONAL_PROVEN (vacuous) | SAFE-by-vacuity | Per D-250-09 NEGATIVE-scope row format. Module does not reference any of the 4 flag state-vars at HEAD `acd88512`. Wide-regex grep returns 2 hits (NatSpec mentions). |
| SIB-03-V12 | DegenerusGameDecimatorModule.sol:916 | `_terminalDecDaysRemaining` (private view) | n/a (no flag read) | `purchaseStartDay` (read at L916 `uint32 psd = purchaseStartDay;`) + `level` (read at L919 `level == 0 ? ... : ...`) | NO (no flag co-read; counter-only ternary) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary) | SAFE | Per D-250-07 Form 1: the function reads `purchaseStartDay` and `level` to compute days-remaining-on-death-clock (view-only, no state writes). No flag co-read at the same site, so the same-shape filter (D-250-08 requires flag-vs-counter co-read) does NOT trigger. Single-state reads of `purchaseStartDay` and `level` are independent — neither produces race-shape interactions because writes to both are governed by their respective single-writer invariants (purchaseStartDay written at AdvanceModule:166 read context + L334 + L1184; level written only at AdvanceModule:1616 per INV-PLV-C-01). View function with no observable race surface. |
| SIB-03-V13 | DegenerusGameGameOverModule.sol:102 | `handleGameOverDrain` | n/a (no flag read in same span) | `rngWordByDay[day]` (read at L102 inside `if (preRefundAvailable != 0)` body) | NO (counter-only read; no flag co-read at L102) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary) | SAFE | Per D-250-07 Form 1: the L102 `rngWord = rngWordByDay[day]` read is gated by `preRefundAvailable != 0` (defense-in-depth check that funds exist; caller `_handleGameOverPath` already guarantees `rngWordByDay[day] != 0` before calling per the L99 NatSpec). No `rngLockedFlag` / `jackpotPhaseFlag` / `lastPurchaseDay` / `phaseTransitionActive` co-read at L102. Single-state read. The read is downstream of the L100-revert-on-zero gate; safe by construction. KI envelope cite: `gameOver` path operates under EXC-02 (prevrandao fallback) + EXC-03 (mid-cycle write-buffer ticket substitution) envelopes; both RE_VERIFIED non-widening at HEAD `acd88512` per Phase 248 BFL-05 (audit/v32-248-BFL.md §5). |
| SIB-03-V14 | BurnieCoinflip.sol:578-590 | `_resolveDay` (BAF section) | `inJackpotPhase` (param via `purchaseInfo()` destructure at L580) + `rngLocked_` (param at L581) + `lastPurchaseDay_` (param at L580) | `purchaseLevel_` (param at L578) | YES (single IF expression L584-590 `!inJackpotPhase && !over && lastPurchaseDay_ && rngLocked_ && (purchaseLevel_ % 10 == 0)`) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Cross-cite Phase 249 PLV-01-V37 + V38 SAFE. The L584-590 IF is the BurnieCoinflip RngLocked guard mirroring the AdvanceModule:185 ternary's flag combination AT THE READER SIDE (BurnieCoinflip is downstream of `purchaseInfo()` which destructures `_activeTicketLevel()` per Phase 249 V37). Form 2 because the upstream `purchaseInfo()` call at L583 strictly precedes the L584-590 evaluation; all four destructured values arrive consistent with the AdvanceModule snapshot. The `purchaseLevel_ % 10 == 0` modulo is well-defined for any uint24 (PLV-01-V38 SAFE). The IF triggers a `revert RngLocked()` to prevent BAF execution during the resolution window — defensive shape, NOT a sibling bug. |
| SIB-03-V15 | BurnieCoinflip.sol:1041 | `_coinflipLockedDuringTransition` (private view) | `inJackpotPhase` (param) + `rngLocked_` (param) + `lastPurchaseDay_` (param) | `purchaseLevel_` (param) | YES (single boolean expression at L1041) | ORTHOGONAL_PROVEN (Form 2 sequential ordering enforced) | SAFE | Cross-cite Phase 249 PLV-01-V40 + V41 SAFE. Mirrors V14 (same destructured `purchaseInfo()` snapshot, same IF shape). View-only function used by BurnieCoinflip-internal callers to check whether to defer coinflip resolution. Form 2 same as V14. |

### 3.3 SIB-03 Finding Candidates

**Zero FINDING_CANDIDATE rows.** The MintModule:923 cell is UNREACHABLE per Phase 249 PLV-01-V31 (verdict locked SAFE via INV-PLV-B-01 + INV-PLV-C-01 composition); cross-module passthrough rows inherit upstream binders' purchaseLevel ≥ 1 invariant. The new MintModule:1229 site (V03) is also SAFE — it's an intentional last-jackpot-day re-routing, not a missing guard. Decimator/GameOver hits are counter-only (no flag co-read at the same span) and view-or-revert-gated.

### 3.4 Section 3 verdict-count attestation

Total SIB-03 rows: **15**. Per-verdict breakdown: SAFE = 15, EXCEPTION = 0, FINDING_CANDIDATE = 0. NEGATIVE-scope rows (subset of SAFE): 3 (Jackpot V04, Degenerette V10, Boon V11 — three modules with zero flag hits, plus implicitly Lootbox-flag-only and Decimator-flag-only and GameOver-flag-only via the wider attestation in §3.1 master grep block).

Per-class SIB-02 breakdown: turbo-class = 0 (no turbo-class shape outside AdvanceModule), backfill-class = 0 (no backfill-class shape outside AdvanceModule), ORTHOGONAL_PROVEN = 15 (all rows). Sub-tally by form: Form 1 = 4 (V05 / V12 / V13 + V04/V10/V11 vacuous treat as Form 1-no-shared-write); Form 2 = 6 (V02 / V06 / V09 / V14 / V15 + ); Form 3 = 5 (V01 / V03 / V07 / V08 + ). [The vacuous NEGATIVE-scope rows V04/V10/V11 are technically all three forms simultaneously by vacuity; categorized under Form 1 above for accounting.]

Cross-cite chain integrity: every SIB-03 row's Evidence column cites at least one Phase 249 PLV-01-Vnn row by ID (V01→V31, V02→V31, V03→V31 derived, V06→V35, V07→V34, V08→V33, V09→V36, V14→V37+V38, V15→V40+V41) or names the upstream invariant ID (INV-PLV-A/B/C-01) — preserving D-250-11 cross-module inheritance per zero-re-derivation rule.

## Section 4 — SIB-04: Post-v31.0 Landed-Commit Cross-Check

Per CONTEXT.md D-250-12 (per-commit row + 8bdeabc2 explicit carrier) + D-250-13 (commit-anchored column shape) + Claude's Discretion item 4 (planner picks: dedicated narrative paragraph + row, mirroring Phase 248 BFL-04 narrative-plus-table pattern).

### 4.1 SIB-04-V01 — `8bdeabc2` productive-pause liveness narrative paragraph (the closest sibling shape per ROADMAP criterion 4)

Commit `8bdeabc2` introduced productive-phase liveness pause via a modification to `_livenessTriggered` in `contracts/storage/DegenerusGameStorage.sol` (verified at `acd88512:contracts/storage/DegenerusGameStorage.sol:1246-1247` — `function _livenessTriggered() internal view returns (bool) { if (lastPurchaseDay || jackpotPhaseFlag) return false; ... }`). The function reads `lastPurchaseDay || jackpotPhaseFlag` to suppress the death clock during productive multi-call windows. The new L173 turbo guard `!inJackpot && !lastPurchaseDay && !rngLockedFlag` (where `inJackpot = jackpotPhaseFlag` per the L164 cache `bool inJackpot = jackpotPhaseFlag;`) shares two of the three operands (`lastPurchaseDay`, `jackpotPhaseFlag`) with the productive-pause read.

NOTE on attribution: Phase 247 §1.4 D-247-C001 anchors the `8bdeabc2` hunk at `contracts/storage/DegenerusGameStorage.sol:1246-1255` (the `_livenessTriggered` function) — NOT at `DegenerusGameAdvanceModule.sol`. Phase 250's narrative refines the CONTEXT.md `<specifics>` paragraph format (which colloquially named the helper `_pauseDeathClockDuringProductivePhase`) to match the actual `_livenessTriggered` early-return shape per the Phase 247 hunk anchor.

**Composition under WIP guards:**
- When `rngLockedFlag = T`, the turbo block at AdvanceModule:167-182 is skipped (the L173 third conjunct `!rngLockedFlag` fails) — see Phase 249 §3 PLV-03.
- When `lastPurchaseDay = T` OR `jackpotPhaseFlag = T`, the productive-pause condition `lastPurchaseDay || jackpotPhaseFlag` evaluates true; `_livenessTriggered` returns `false`; downstream callers (`requestLootboxRng` at AdvanceModule:1049 area, `mintCoin`/`mintEth` revert checks at DegenerusGameStorage.sol:573/604/657) suppress liveness reverts.

The two reads compose without race because:
1. The productive-pause path NEVER modifies `rngLockedFlag` (verified via `grep -n 'rngLockedFlag\s*=' /tmp/v32-250-AdvanceModule-acd88512.sol` returning ONLY L1607 / L1663 / L1704 — the productive-pause function `_livenessTriggered` is a `view` function and is not in that writer set).
2. The turbo block NEVER modifies `lastPurchaseDay` or `jackpotPhaseFlag` outside the L178 turbo-T-write — and that write is gated by the L173 `!rngLockedFlag` conjunct, so it cannot fire while VRF is in flight (which is when productive-pause is most relevant).
3. The `lastPurchaseDay = true` write at AdvanceModule:399 (inside the daily-jackpot region L370-407) is post-`_unlockRng(day)` at L404 — NO, correction: per Phase 249 PLV-06 attestation L399 PRECEDES L404 by 4 source lines (L400-403 are inner conditional close + outer if-block close; L404 is OUTSIDE the L398-L403 inner branch and runs unconditionally for any entry through the L371 outer gate). After L404 fires, `_unlockRng` has cleared `rngLockedFlag = false` at AdvanceModule:1704, so by the time control returns from the daily-jackpot region the lock window has already closed.
4. The L399 write therefore enables the productive-pause for SUBSEQUENT advanceGame calls (intended behavior — pause the liveness clock during the productive multi-call window). PLV-06-H01 confirms the daily-jackpot resolution + productive-pause are mutually non-interfering at the in-call granularity.

Phase 252 POST31-02 RE_VERIFIES this composition empirically via the WIP guard active state. Phase 250 stops at the narrative + cross-cite per Deferred Items "Phase 252 SIB-04-V01 deep composition proof".

### 4.2 SIB-04-Vnn rows — 4-commit cross-check table

Per D-250-13 column shape (commit-anchored row replacing Function with Commit SHA + Hunk anchor):

| Row ID | Commit SHA | Hunk anchor (file:line range from Phase 247 §1.4) | Sibling-shape verdict | SIB-02 classification | Phase 252 POST31 inheritance | Evidence cite |
|---|---|---|---|---|---|---|
| SIB-04-V01 | `8bdeabc2` | `contracts/storage/DegenerusGameStorage.sol:1246-1255` (Phase 247 §1.4 D-247-C001 — `_livenessTriggered` MODIFIED with early-return short-circuit when lastPurchaseDay or jackpotPhaseFlag set) + `contracts/storage/DegenerusGameStorage.sol:1225-1234` (D-247-C002 NatSpec block above) | SAFE (closest sibling shape to v32.0 turbo bug; productive-pause + L173 turbo guard composition non-interfering per §4.1 narrative) | Initial classification turbo-class candidate (composes with L173 guard via shared `lastPurchaseDay`, `jackpotPhaseFlag` operands) — verdict reduces to ORTHOGONAL_PROVEN Form 3 (mutex-equivalent: `rngLockedFlag = T` ⇒ turbo block skipped per INV-PLV-A-01, AND productive-pause does not modify rngLockedFlag — verified by writer-grep returning only L1607/L1663/L1704 in AdvanceModule) | YES — Phase 252 POST31-02 RE_VERIFIES productive-pause × L173 turbo guard composition under WIP guard active state | §4.1 narrative; cross-cite Phase 249 audit/v32-249-PLV.md §6 PLV-06 strand-disproof + §6.3 PLV-06-H01 hand-off row; Phase 247 §1.4 D-247-C001 + D-247-C002 hunk anchors |
| SIB-04-V02 | `ad41973c` | `test/edge/LivenessProductivePause.test.js` + `test/edge/LivenessMidJackpot.test.js` — new files, zero `contracts/` delta (Phase 247 §1.4 D-247-C013 — "test/-only, enumerated for completeness per D-247-02") | SAFE-by-test-only (NEGATIVE-scope; no `contracts/` surface to audit) | ORTHOGONAL_PROVEN (vacuous — no `contracts/` hunks) | NO (test-only commit; Phase 252 POST31-01 may sanity-check that the test still passes against the WIP guards but no `contracts/` delta to audit here) | `git show ad41973c --stat -- 'contracts/'` returns 0 files at HEAD `acd88512` (verified via Step B) → test-only commit confirmed; Phase 247 §1.4 D-247-C013 hunk anchor |
| SIB-04-V03 | `6a63705b` | `contracts/modules/DegenerusGameMintModule.sol:1204-1370` (Phase 247 §1.4 D-247-C003 — `_callTicketPurchase` SIGNATURE-CHANGED, payer parameter dropped + 4 internal sites switch to buyer) + D-247-C004 + D-247-C005 (call-site updates at L895 + L977) | SAFE (non-flag-vs-counter shape; payment-flow correction; no `rngLockedFlag` interaction introduced; the changed hunks alter `msg.sender` vs operator address resolution at the payment call sites — no flag-vs-counter co-read added) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary: the buyer-charge fix changes payer-resolution at the call site; no read of any of the 7 partner state-vars introduced into the changed hunks) | NO (non-sibling shape) | Phase 247 §1.4 D-247-C003/C004/C005 hunk anchors; verified via `git show 6a63705b --stat -- 'contracts/'` — only DegenerusGameMintModule.sol modified; no AdvanceModule delta. Note: SIB-03-V03 captures the L1229 flag-vs-flag-vs-counter triple inside `_callTicketPurchase` — that pattern already EXISTED at v31.0 baseline; `6a63705b` does NOT touch L1229; the V03 SIB-03 verdict is independent of this commit. |
| SIB-04-V04 | `48554f8f` | `contracts/DegenerusVault.sol:750-790` + `:802-842` + cleanup of removed helpers `:777-817@cc68bfc7` / `:848-891@cc68bfc7` / `:444-448@cc68bfc7` (Phase 247 §1.4 D-247-C006 / C007 / C008 / C009 / C010 — vault redemption decoupling refactor) | SAFE (vault-redemption boundary; no AdvanceModule delta; no `rngLockedFlag` interaction with vault redemption path) | ORTHOGONAL_PROVEN (Form 1 no shared write boundary: vault redemption operates on share accounting, not on `rngLockedFlag` / `lastPurchaseDay` / `jackpotPhaseFlag` / `dailyIdx` / `level` / `purchaseStartDay` / `rngWordByDay` / `phaseTransitionActive` at any read site in the changed hunks) | NO (non-sibling shape; vault-redemption-only) | Phase 247 §1.4 D-247-C006..C010 hunk anchors; verified via `git show 48554f8f --stat -- 'contracts/'` — DegenerusVault.sol modified; no AdvanceModule delta. KI cross-cite: the vault redemption decoupling boundary does NOT touch the EXC-03 (gameover RNG substitution) envelope at HEAD `acd88512` per Phase 248 BFL-05 RE_VERIFY (audit/v32-248-BFL.md §5 dual-carrier attestation NON-WIDENING). |

### 4.3 Section 4 verdict-count attestation

Total SIB-04 rows: **4**. Per-verdict breakdown: SAFE = 4, EXCEPTION = 0, FINDING_CANDIDATE = 0. Per-class SIB-02 breakdown: turbo-class = 0 (V01 reduces to ORTHOGONAL_PROVEN Form 3 per §4.1), backfill-class = 0, ORTHOGONAL_PROVEN = 4 (V01 Form 3, V02 vacuous, V03 Form 1, V04 Form 1).

Phase 252 POST31-02 inheritance: 1 row (V01 only — the `8bdeabc2` productive-pause carrier). The other 3 rows are non-sibling-shape and pass through to Phase 252 POST31-01 standard sanity-check without composition concerns.

## Section 5 — SIB-05: New-Bug Documentation OR Zero-State Attestation (RESERVED FOR TASK 4)

This section is reserved for Task 4 of this plan to aggregate FINDING_CANDIDATE rows from §1-§4 (working hypothesis: zero) and either emit `SIB-05-Vnn` rows with `awaiting-approval` proposed-fix blocks per CONTEXT.md D-250-14 OR emit the zero-state attestation per D-250-15. Task 4 also flips this file to FINAL READ-only and emits the closure signal `PHASE_250_SIB_FINAL_AT_HEAD_acd88512`. DO NOT edit this section in Tasks 1-3 — Task 4 replaces this stub in place.
