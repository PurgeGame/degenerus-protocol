---
status: IN_PROGRESS — Plan 248-01 single-plan multi-task
phase: 248 — Backfill Idempotency Proof
milestone: v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
head: acd88512
plan: 248-01
deliverable: audit/v32-248-BFL.md
---

# v32.0 Phase 248 — Backfill Idempotency Proof

**Audit head:** `acd88512` (Phase 247 anchor; both WIP guards already committed inside this SHA — turbo at advanceGame:173 + backfill at rngGate:1174).
**Phase:** 248 — Backfill Idempotency Proof (BFL-01..06).
**Scope:** Pure-proof phase per CONTEXT.md D-248-04 / D-248-05; zero `contracts/` or `test/` writes. Phase 247 catalog (`audit/v32-247-DELTA-SURFACE.md` Section 6 D-247-I001..I006) is the SOLE scope input per D-248-01.
**Status:** IN PROGRESS — Plan 248-01 Task 1 populates Sections 1 (BFL-01) + 2 (BFL-02). Sections 3-6 + Phase 251 TST-04 Hand-Off appendix are RESERVED stubs for Tasks 2-4 of the same plan. Task 5 flips this file to FINAL READ-only.

## Section 0 — Overview & Verdict Legend

### 0.1 Overview

This file is the authoritative v32.0 Phase 248 deliverable proving the new `rngWordByDay[idx + 1] == 0` guard at `contracts/modules/DegenerusGameAdvanceModule.sol:1174` (inside `rngGate`'s fresh-word branch L1165-1209) makes `_backfillGapDays` (sole call site at L1176; function body L1752-1773) execute at most once per VRF lock window across every reachable `advanceGame` re-entry path. Conservation closes across the gap range (no doubled `purchaseStartDay`, no doubled coinflip credits, lock-window-scoped supply invariants for sDGNRS / DGNRS / BURNIE per REQUIREMENTS.md OUT OF SCOPE). KI EXC-02 (prevrandao fallback at `_getHistoricalRngFallback` AdvanceModule:1329) and EXC-03 (gameover RNG substitution at `_gameOverEntropy` AdvanceModule:1238) envelopes are RE_VERIFIED non-widening per D-248-13 dual-carrier attestation pattern.

Per CONTEXT.md `attribution_note`: KNOWN-ISSUES.md envelope paragraphs cite `_getHistoricalRngFallback` at `AdvanceModule:1301` and `_gameOverEntropy` at `AdvanceModule:1252` / `1222-1246` (planning-time prose). At HEAD `acd88512` the verified line numbers are `_getHistoricalRngFallback` at L1329 and `_gameOverEntropy` at L1238. This deliverable honors the HEAD-verified line numbers throughout; KNOWN-ISSUES.md is NOT edited in Phase 248 per D-248-04 (KI promotions are Phase 253 FIND-03 only).

### 0.2 Verdict Bucket Legend (per CONTEXT.md D-248-12)

| Verdict | Meaning |
|---|---|
| `SAFE` | Site/path/state-write proven safe under the guard with explicit evidence cite. Default expectation for BFL-01 / BFL-02 / BFL-04 / BFL-06 rows. |
| `EXCEPTION` | Site is governed by a pre-locked KNOWN-ISSUES.md envelope (EXC-02 prevrandao fallback or EXC-03 gameover RNG substitution). Acceptance NOT re-litigated; envelope re-verify only per BFL-05. |
| `FINDING_CANDIDATE` | Site has a guard-evaluation gap, off-by-one risk, conservation violation, or envelope widening. Routes to Phase 253 FIND-01..04 emission per CONTEXT.md D-248-03 — NO v32 finding-ID assigned in this phase. |

### 0.3 Row ID Legend

Per-REQ rows: `BFL-NN-VMM` (REQ-anchored, monotonic-within-REQ). Examples: `BFL-01-V01`, `BFL-01-V02`, ..., `BFL-02-V01`, ..., `BFL-06-VNN`.

Column shape per CONTEXT.md D-248-12: `Row ID | Site (file:line) | Description | Pre-state | Post-state | Verdict | Evidence Cite`. BFL-04 / BFL-05 / BFL-06 use slightly varied column shapes per D-248-07 / D-248-13 / D-248-10 (documented inline at each per-REQ section header).

## Section 1 — BFL-01: rngGate fresh-word branch reachability + 3-path multiplier

### 1.1 3-Path Entry Multiplier Attestation (per CONTEXT.md D-248-08)

`advanceGame` (AdvanceModule:160) is reached via 3 distinct call paths catalogued in Phase 247 Section 3. Inside `advanceGame` the sole `rngGate` call site is at AdvanceModule:292 (Phase 247 D-247-X030). The branch selection inside `rngGate` depends only on `rngWordByDay[day]` / `currentWord` / `rngRequestTime` / `dailyIdx` — none of which the 3 entry paths set differently — so the inner state-transition proof in §1.2 walks the rngGate fresh-word branch exactly ONCE; the multiplier table closes the proof for all 3 entry paths without 3× duplication.

| Carrier ID | Phase 247 Row | Entry Path | Caller (file:line) | Funnels Into | rngGate Surface Identical? |
|---|---|---|---|---|---|
| BFL-01-M01 | D-247-X027 | DegenerusGame.sol delegatecall to advanceGame | contracts/DegenerusGame.sol:289 | AdvanceModule:160 advanceGame entry → rngGate at AdvanceModule:292 | YES — branch selection at rngGate L1163 / L1174 depends only on `rngWordByDay[day]` / `currentWord` / `rngRequestTime` / `dailyIdx`, none of which the entry path sets differently |
| BFL-01-M02 | D-247-X028 | DegenerusVault.sol cross-contract call to advanceGame | contracts/DegenerusVault.sol:503 | AdvanceModule:160 advanceGame entry → rngGate at AdvanceModule:292 | YES — same rationale; cross-contract call to `IDegenerusGamePlayerActions(GAME).advanceGame()` arrives at the same top-level dispatcher |
| BFL-01-M03 | D-247-X029 | StakedDegenerusStonk.sol cross-contract call to advanceGame | contracts/StakedDegenerusStonk.sol:355 | AdvanceModule:160 advanceGame entry → rngGate at AdvanceModule:292 | YES — same rationale; sDGNRS redemption helper wraps `game.advanceGame()` to flush state before/after redemption |

Conclusion: per D-248-08, the 3 entry paths share the rngGate-side reachability surface. The inner state-transition proof (§1.2) walks the rngGate fresh-word branch exactly ONCE; this multiplier table closes the proof for all 3 entry paths.

### 1.2 rngGate Fresh-Word Branch State-Transition Table (the "inner" proof)

Column shape per D-248-12: `Row ID | Site (file:line) | Description | Pre-state | Post-state | Verdict | Evidence Cite`.

The fresh-word branch at rngGate L1163-1209 selects on `(currentWord != 0 && rngRequestTime != 0)` (entry condition at L1165). Within the branch, the backfill guard at L1174 selects on `(day > idx + 1 && rngWordByDay[idx + 1] == 0)`. Each row below walks one branch / re-entry shape.

| Row ID | Site (file:line) | Description | Pre-state | Post-state | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|
| BFL-01-V01 | AdvanceModule:1160 | Outer early-return short-circuit on already-recorded day | `rngWordByDay[day] != 0` | function returns `(rngWordByDay[day], 0)` early; no inner writes; backfill branch unreachable | SAFE | `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol` L1160 — `if (rngWordByDay[day] != 0) return (rngWordByDay[day], 0);` |
| BFL-01-V02 | AdvanceModule:1165-1186 | First-entry into fresh-word branch with backfill needed | `rngWordByDay[day] == 0 ∧ currentWord != 0 ∧ rngRequestTime != 0 ∧ day > dailyIdx + 1 ∧ rngWordByDay[dailyIdx + 1] == 0` | both conjuncts at L1174 TRUE; guarded block L1174-1186 executes once: `_backfillGapDays(currentWord, idx + 1, day, bonusFlip)` runs (writes rngWordByDay[idx+1..day-1] inclusive of sentinel key); `_backfillOrphanedLootboxIndices(currentWord)` runs; `purchaseStartDay += gapCount`; `gapDays = gapCount`. Always-executed L1189-1207 also runs writing `rngWordByDay[day]` | SAFE | L1174 conjunct `rngWordByDay[idx + 1] == 0` is TRUE on first entry (no prior backfill ran for this lock window); first iteration of `_backfillGapDays` loop (L1761) writes `rngWordByDay[startDay] = rngWordByDay[idx + 1]` at L1766 |
| BFL-01-V03 | AdvanceModule:1165-1186 | Re-entry into fresh-word branch on a later wall-clock day BEFORE `_unlockRng` fires | `rngWordByDay[day] == 0 ∧ currentWord != 0 ∧ rngRequestTime != 0 ∧ day > dailyIdx + 1 ∧ rngWordByDay[dailyIdx + 1] != 0` (sentinel set by prior BFL-01-V02 execution) | left conjunct `day > idx + 1` may be TRUE, but right conjunct `rngWordByDay[idx + 1] == 0` is FALSE → guarded block SKIPPED. Always-executed writes L1189-1207 still run on the new current day (`day_new`) where `rngWordByDay[day_new] == 0` (different key from sentinel) | SAFE | L1174 conjunct `rngWordByDay[idx + 1] == 0` is FALSE on re-entry because BFL-01-V02 wrote `rngWordByDay[idx + 1]` at L1766 inside `_backfillGapDays`; `dailyIdx` cannot have advanced because `_unlockRng` (sole writer at L1703) is not called inside `rngGate`'s fresh-word branch — see BFL-04 invariant (Task 2) |
| BFL-01-V04 | AdvanceModule:1160 | Re-entry on the SAME wall-clock day (defensive cross-check) | `rngWordByDay[day] != 0` (BFL-01-V02 just wrote it via `_applyDailyRng` at L1189 inside the same call) | OUTER early-return at L1160 short-circuits before reaching L1174; identical behavior to BFL-01-V01 | SAFE | belt-and-braces — even if L1174 guard were absent, the L1160 outer guard catches same-day re-entry |
| BFL-01-V05 | AdvanceModule:1165-1186 | First-entry into fresh-word branch with NO backfill needed (no gap; normal next-day) | `rngWordByDay[day] == 0 ∧ currentWord != 0 ∧ rngRequestTime != 0 ∧ day == dailyIdx + 1` | left conjunct `day > idx + 1` is FALSE → guarded block SKIPPED. Always-executed writes L1189-1207 run for the current day | SAFE | normal next-day path; no gap to backfill; the L1174 guard correctly does NOT enter the backfill branch in the no-gap shape |
| BFL-01-V06 | AdvanceModule:1212-1218 | VRF timeout retry path (12h elapsed) | `currentWord == 0 ∧ rngRequestTime != 0 ∧ elapsed >= 12 hours` | `_requestRng(isTicketJackpotDay, lvl)` fires; function returns `(1, 0)`; backfill branch NOT reached | SAFE | branch-condition mutual exclusion: this path is the alternate branch at L1212 (`currentWord != 0 && rngRequestTime != 0` was FALSE at L1165), so no backfill state mutation can occur on this path |
| BFL-01-V07 | AdvanceModule:1221-1223 | Need-fresh-RNG path (no in-flight request) | `currentWord == 0 ∧ rngRequestTime == 0` | `_requestRng(isTicketJackpotDay, lvl)` fires; function returns `(1, 0)`; backfill branch NOT reached | SAFE | branch-condition mutual exclusion as in BFL-01-V06 |

**Path coverage attestation:** The 7 rows above partition the rngGate function body into mutually-exclusive reachable paths under the natural reading of the if-cascade at L1160 / L1165 / L1212. The only `_backfillGapDays` call site is at L1176 inside the guarded block at L1174-1186, reachable only via BFL-01-V02 (first-entry-with-gap). Every alternate path (V01 outer-skip, V03 re-entry-skip, V04 same-day-skip, V05 no-gap-skip, V06 timeout-retry, V07 fresh-RNG-request) provably does NOT reach `_backfillGapDays`. Therefore `_backfillGapDays` executes AT MOST ONCE per VRF lock window across every reachable rngGate invocation under the new guard, completing the BFL-01 idempotency proof.

## Section 2 — BFL-02: WHOLE-guarded-block state-write inventory (L1174-1186)

Per CONTEXT.md D-248-09: BFL-02 enumerates every state write the guard at L1174 protects from re-execution — NOT just `_backfillGapDays` body. Specifically the entire `if (day > idx + 1 && rngWordByDay[idx + 1] == 0)` block at L1174-1186 plus the body of every function the block calls into (`_backfillGapDays` at L1752-1773, `_backfillOrphanedLootboxIndices` at L1779-1798).

Column shape: `Row ID | Site (file:line) | Write | State Touched | Pre-Guard Behavior | Post-Guard Behavior | Sentinel Off-By-One Check | Verdict | Evidence Cite`.

### 2.1 Guarded-Block State Writes (in-scope per D-248-09)

| Row ID | Site (file:line) | Write | State Touched | Pre-Guard Behavior | Post-Guard Behavior | Sentinel Off-By-One Check | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|---|---|
| BFL-02-V01 | AdvanceModule:1766 (inside `_backfillGapDays` loop body L1761-1772) | `rngWordByDay[gapDay] = derivedWord` (loop iterates `gapDay = startDay..endDay-1`) | `rngWordByDay[d]` for d ∈ [idx+1, day) | runs on every fresh-word re-entry — overwrites already-set `rngWordByDay[d]` from previous backfill (last-write-wins; final value identical because loop is deterministic per `currentWord`) | runs ONCE per lock window; subsequent re-entries see `rngWordByDay[idx + 1] != 0` and skip the entire guarded block via L1174 | sentinel index `idx + 1` matches the FIRST gap day written by this loop — `startDay = idx + 1` per call at L1176 (`_backfillGapDays(currentWord, idx + 1, day, bonusFlip)`); first iteration at L1761 writes the sentinel key; no off-by-one against `idx` or `day` | SAFE | D-248-09; HEAD acd88512 L1766 — `rngWordByDay[gapDay] = derivedWord;` inside loop seeded at `gapDay = startDay = idx + 1` |
| BFL-02-V02 | AdvanceModule:1767 (inside `_backfillGapDays` loop body) | `coinflip.processCoinflipPayouts(bonusFlip, derivedWord, gapDay)` (external call) | BurnieCoinflip per-day pool credits (boundary record per D-248-10) | runs N times per gap day per lock window across multi-day stall (the literal testnet bug shape — doubled / N-tupled ETH credits per gap day) | runs ONCE per gap day per lock window | indirectly correct — tied to BFL-02-V01 sentinel write; same off-by-one analysis applies | SAFE | D-248-10 boundary record; behavioral cite: "credits ETH coinflip pool / debits per-day reservation per `contracts/BurnieCoinflip.sol::processCoinflipPayouts`" — re-audit OUT OF SCOPE per D-248-10 |
| BFL-02-V03 | AdvanceModule:1768 (inside `_backfillGapDays` loop body) | `emit DailyRngApplied(gapDay, derivedWord, 0, derivedWord)` (event emission) | event log only; no storage | runs N times per gap day per lock window (duplicate event emissions visible to off-chain indexers) | runs ONCE per gap day per lock window | sentinel correctness inherited from BFL-02-V01 | SAFE | event-only; no storage delta |
| BFL-02-V04 | AdvanceModule:1180 (inside guarded block at rngGate) | `_backfillOrphanedLootboxIndices(currentWord)` (internal call; mutates `lootboxRngWordByIndex[i]` + emits `LootboxRngApplied` per body L1779-1798) | `lootboxRngWordByIndex[i]` for orphaned lootbox indices | runs on every fresh-word re-entry — would re-derive lootbox words from same `vrfWord`. Idempotent on `lootboxRngWordByIndex[i]` because the helper short-circuits at L1785 (`if (lootboxRngWordByIndex[i] != 0) break;`) once it hits a filled index. Pre-guard behavior: still wastes gas + emits duplicate `LootboxRngApplied` events for the topmost previously-orphaned index on the FIRST iteration of the helper's reverse scan | runs ONCE per lock window | not directly index-on-sentinel; relies on `lootboxRngWordByIndex[i] != 0` early-break inside the helper (L1785) as defense-in-depth | SAFE | helper body L1779-1798; the outer L1174 guard is the primary defense (skips the entire helper call on re-entry); the inner L1785 break is belt-and-braces |
| BFL-02-V05 | AdvanceModule:1184 (inside guarded block at rngGate) | `purchaseStartDay += gapCount` | `purchaseStartDay` (death-clock anchor) | **THE TESTNET BUG TRIGGER** — runs on every fresh-word re-entry, incrementing `purchaseStartDay` by `gapCount` each time. After N re-entries before `_unlockRng` fires: `purchaseStartDay_after = purchaseStartDay_before + N * gapCount`. Eventually `purchaseStartDay` exceeds the safe range against downstream consumers (the literal underflow shape per CONTEXT.md `<domain>` — see BFL-03 worked example) | runs ONCE per lock window; `purchaseStartDay` advances exactly `gapCount` once per gap range | sentinel `rngWordByDay[idx + 1]` correctly identifies "this lock window's backfill has not yet run" because BFL-02-V01 writes that exact key on the first iteration of `_backfillGapDays`'s loop — see §2.3 sentinel-index correctness verification | SAFE | this is the load-bearing write the L1174 guard protects; tying it to BFL-02-V01 sentinel write closes the proof |
| BFL-02-V06 | AdvanceModule:1185 (inside guarded block at rngGate) | `gapDays = gapCount` (named-return local assignment) | local return value passed back to `advanceGame` caller (read by the gap-aware downstream logic in `advanceGame` to extend bounty / death-clock semantics) | runs on every fresh-word re-entry — caller would see non-zero `gapDays` on every re-entry, potentially re-running `gapDays`-dependent downstream work in `advanceGame` | runs ONCE per lock window | inherited from BFL-02-V05 sentinel | SAFE | local-only at the rngGate frame; affects caller `advanceGame`'s downstream gapDays handling |

**In-scope row count:** 6 BFL-02-VNN rows covering every state mutation reachable via the L1174-1186 block at HEAD `acd88512`.

### 2.2 Always-Executed Writes Inside Fresh-Word Branch (OUT-of-BFL-02-scope per D-248-09)

These writes run on every `rngGate` fresh-word-branch invocation (regardless of the L1174 guard) and are correct by virtue of the always-checked early-return at L1160 (`if (rngWordByDay[day] != 0) return (rngWordByDay[day], 0)`). Enumerated for boundary clarity per CONTEXT.md `<specifics>` "BFL-02 always-executed-vs-guarded write distinction".

| Row ID | Site (file:line) | Write | Why Out-of-Scope |
|---|---|---|---|
| BFL-02-X01 | AdvanceModule:1189 | `currentWord = _applyDailyRng(day, currentWord)` (mutates `rngWordCurrent`, `rngWordByDay[day]`, `lastVrfProcessedTimestamp`, `totalFlipReversals` per body L1801-1817) | always-executed; idempotent via L1160 outer early-return on subsequent calls (the SAME-day re-entry case where `rngWordByDay[day] != 0` short-circuits before reaching L1189) |
| BFL-02-X02 | AdvanceModule:1190 | `coinflip.processCoinflipPayouts(bonusFlip, currentWord, day)` (current-day, NOT gap-day) | always-executed; idempotent via L1160 outer early-return (same-day re-entry caught upstream) |
| BFL-02-X03 | AdvanceModule:1191 | `quests.rollDailyQuest(day, currentWord)` | always-executed; safe via L1160 outer early-return |
| BFL-02-X04 | AdvanceModule:1203 | `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` (gated on `sdgnrs.hasPendingRedemptions()`) | always-executed when pending; safe via L1160 outer early-return |
| BFL-02-X05 | AdvanceModule:1207 | `_finalizeLootboxRng(currentWord)` | always-executed; idempotent via internal `if (lootboxRngWordByIndex[index] != 0) return` at function head L1228 |

These rows are intentionally NOT verdict-tagged (they are out-of-scope-by-construction; not part of BFL-02 proof scope).

### 2.3 Sentinel-Index Correctness Verification

The guard at AdvanceModule:1174 reads `rngWordByDay[idx + 1]` where `idx = dailyIdx`. For the guard to correctly detect "backfill not yet run for this lock window," the sentinel index must satisfy the following four conditions:

1. **The sentinel key is in the gap range:** `idx + 1 ∈ [startDay, endDay)` where `startDay = idx + 1` and `endDay = day`. Confirmed: `startDay == idx + 1` by construction at L1176 (`_backfillGapDays(currentWord, idx + 1, day, bonusFlip)`). The gap range starts AT the sentinel key.

2. **The sentinel key is the FIRST gap day written by `_backfillGapDays`:** The loop at L1761 starts at `gapDay = startDay = idx + 1`, and the first iteration writes `rngWordByDay[idx + 1] = derivedWord` at L1766. The sentinel write happens on the first loop iteration, so the sentinel flips from 0 → non-zero atomically with the first state mutation in the guarded block. No intermediate state is observable where the sentinel reads 0 but `_backfillGapDays` already mutated `rngWordByDay`.

3. **No off-by-one against `idx`:** sentinel reads `idx + 1` (NOT `idx`). At entry, `dailyIdx == idx` already has `rngWordByDay[idx]` set (from the prior wall-clock day's normal `rngGate` cycle that produced that idx via `_unlockRng`). If the guard read `rngWordByDay[idx]` it would always evaluate FALSE (already-set) and skip backfill on the very first lock-window entry — defeating the purpose of the guard. The chosen index `idx + 1` is correct.

4. **No off-by-one against `day`:** sentinel reads `idx + 1` (NOT `day - 1`). `day - 1` is the LAST gap day; reading `rngWordByDay[day - 1]` would incorrectly evaluate as "not yet run" on the second invocation if a hypothetical partial loop got partway through and didn't finish (impossible under current single-call semantics, but a defensive-correctness check). The chosen index `idx + 1` (the FIRST gap day) is the safer choice because it commits atomically with the very first state mutation inside the loop.

**Conclusion:** Sentinel index `idx + 1` is correct. No off-by-one against either `idx` or `day`. BFL-02 sentinel-correctness invariant HOLDS.

## Section 3 — BFL-03: multi-day VRF stall worked numeric example

**Scenario seed (per CONTEXT.md D-248-16):** Testnet blocks 10759449 + 10761786. Block 10759449 = the first `advanceGame` call that started the multi-day VRF stall: a fresh daily VRF request was issued via `_swapAndFreeze(purchaseLevel)` at AdvanceModule:292, but the chain advanced past midnight UTC before the coordinator returned. Block 10761786 = the second `advanceGame` call on the next wall-clock day re-entering rngGate's fresh-word branch BEFORE `_unlockRng` had a chance to fire (because `currentWord` had been populated by `rawFulfillRandomWords` while `rngLockedFlag` was still TRUE, leaving the rngGate fresh-word branch entry condition `currentWord != 0 && rngRequestTime != 0` satisfied — see CONTEXT.md `<domain>` for the full trigger geometry).

Pre-fix: the guarded block at L1174-1186 ran TWICE (once per `advanceGame` re-entry) → `purchaseStartDay += gapCount` ran twice → `purchaseStartDay` doubled past safe range → death-clock anchor produced underflow downstream. Post-fix: the L1174 conjunct `rngWordByDay[idx + 1] == 0` evaluates FALSE on the second entry (because the first entry's `_backfillGapDays` body wrote `rngWordByDay[idx + 1]` at L1766 inside the loop's first iteration) → entire guarded block SKIPPED → `purchaseStartDay` advances exactly once.

Concrete state seeds for the walk below: assume `dailyIdx = N` at the start of the stall, with `rngWordByDay[N] = W_prev` (set on the prior wall-clock day's `_unlockRng` + `_applyDailyRng` cycle). Assume the stall spans 2 wall-clock days such that at the second `advanceGame` call `day = N + 2` (gapCount = 1; the gap range is the single day `[N+1, N+2) = {N+1}`). Block-timestamp anchors: `T1 = block.timestamp` at block 10759449 such that `day = N + 1` (no gap yet at this call); `T2 = block.timestamp` at block 10761786 such that `day = N + 2` (one gap day).

Column shape per D-248-07: `Step | Block | day | dailyIdx | rngLockedFlag | rngRequestTime | currentWord | rngWordByDay[idx+1] | guard verdict | purchaseStartDay (post) | gapDays (post) | _backfillGapDays called?`.

### 3.1 Pre-fix walk (no L1174 sentinel conjunct — historical shape, retained for narrative contrast)

Pre-fix shape: the guard at L1174 was `if (day > idx + 1)` only (single conjunct; no `rngWordByDay[idx + 1] == 0` clause). Rows demonstrate the double-execution path that produced the testnet panic.

| Row ID | Step | Block | day | dailyIdx | rngLockedFlag | rngRequestTime | currentWord | rngWordByDay[idx+1] | guard verdict | purchaseStartDay (post) | gapDays (post) | _backfillGapDays called? |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BFL-03-V01 | 1 — first advanceGame at start of stall | 10759449 | N+1 | N | true (locked by prior `_swapAndFreeze`) | T_req (set when prior `_swapAndFreeze` ran) | 0 (VRF still pending) | 0 (no gap yet) | rngGate enters branch at L1212 (timeout retry / rng-not-ready); guarded block NOT reached this call | unchanged | 0 | NO |
| BFL-03-V02 | 2 — VRF fulfillment lands while rngLockedFlag still TRUE | (off-chain VRF callback) | N+1 | N | true | T_req | W1 (rawFulfillRandomWords stores it at L1730 because rngLockedFlag) | 0 | n/a — callback only sets `rngWordCurrent` and does not touch the guard | unchanged | 0 | NO |
| BFL-03-V03 | 3 — chain crosses midnight UTC (no advanceGame call) | (block-timestamp advance) | N+2 | N | true | T_req | W1 | 0 | n/a — no call | unchanged | 0 | NO |
| BFL-03-V04 | 4 — second advanceGame at block 10761786 (FIRST guarded-block execution) | 10761786 | N+2 | N | true | T_req | W1 | 0 (still — never been written) | rngGate L1160 outer-skip FAILS (`rngWordByDay[N+2] == 0`); fresh-word branch entered (L1165 `W1 != 0 && T_req != 0` TRUE); L1174 (PRE-FIX): `day > idx + 1` is `N+2 > N+1` TRUE → guarded block executes. `_backfillGapDays(W1, N+1, N+2, bonusFlip)` runs (writes `rngWordByDay[N+1] = derivedW`); `purchaseStartDay += 1`; `gapDays = 1` | startBefore + 1 | 1 | YES (1st time) |
| BFL-03-V05 | 5 — third advanceGame in the SAME wall-clock day BEFORE `_unlockRng` fires (e.g., a redemption-triggered cross-contract advanceGame from DegenerusVault.sol:503 or StakedDegenerusStonk.sol:355 per BFL-01-M02 / M03) | 10761786 + n | N+2 | N (still — `_unlockRng` not called inside rngGate; sole writer at L1703) | true | T_req | W1 (still) | derivedW (Step 4 wrote it) | rngGate L1160 outer-skip succeeds (`rngWordByDay[N+2]` was set by Step 4's `_applyDailyRng` at L1814) → function returns early; PRE-FIX guarded block SKIPPED for this re-entry shape | startBefore + 1 | 0 | NO |

**Pre-fix outcome:** Although Step 5 in the V01..V05 sequence is short-circuited by the L1160 outer guard (because Step 4 wrote `rngWordByDay[N+2]`), the testnet bug shape arises when a re-entry occurs on a NEW wall-clock day before `_unlockRng` has fired — the L1160 outer guard is keyed on the CURRENT `day` (which has changed), so the outer-skip does NOT catch the re-entry. The next row demonstrates this:

| Row ID | Step | Block | day | dailyIdx | rngLockedFlag | rngRequestTime | currentWord | rngWordByDay[idx+1] | guard verdict | purchaseStartDay (post) | gapDays (post) | _backfillGapDays called? |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BFL-03-V06 | 6 — chain crosses midnight UTC AGAIN (now 2 days into stall, still no `_unlockRng`) | (block-timestamp advance) | N+3 | N | true | T_req | W1 | derivedW | n/a — no call | startBefore + 1 | 0 | NO |
| BFL-03-V07 | 7 — fourth advanceGame at start of new wall-clock day (PRE-FIX SECOND guarded-block execution) | next-day block | N+3 | N (still) | true | T_req | W1 | derivedW (set by Step 4) | rngGate L1160 outer-skip FAILS (`rngWordByDay[N+3] == 0`); fresh-word branch entered; L1174 (PRE-FIX): `day > idx + 1` is `N+3 > N+1` TRUE → guarded block executes AGAIN. `_backfillGapDays(W1, N+1, N+3, bonusFlip)` overwrites `rngWordByDay[N+1] = derivedW` (loop is deterministic per `currentWord = W1`, so derivedW is the SAME value — no entropy delta) AND writes `rngWordByDay[N+2] = derivedW2` for the new gap day; `purchaseStartDay += 2`; `gapDays = 2` | startBefore + 1 + 2 = startBefore + 3 | 2 | YES (2nd time) |
| BFL-03-V08 | 8 — eventual `_unlockRng` fires; downstream consumes inflated purchaseStartDay → underflow at the literal testnet panic site | (later, when rngLockedFlag flips) | N+3 | N+3 | false | 0 | 0 | (irrelevant — past the lock window) | downstream subtractor reads `purchaseStartDay = startBefore + 3` (vs expected startBefore + 2 for a 2-day gap); arithmetic underflow on a downstream `purchaseStartDay - X` site → testnet panic 0x11 | startBefore + 3 (already inflated) | n/a | n/a |

**Pre-fix conclusion:** Across N successive `advanceGame` re-entries on different wall-clock days (each crossing midnight UTC) before `_unlockRng` fires, `purchaseStartDay` accumulates `gapCount_1 + gapCount_2 + ... + gapCount_N` increments where each `gapCount_i` includes the prior gap days re-counted. For a 3-day stall this can produce `+1 + +2 = +3` instead of the correct `+2`, an over-application of one extra day per re-entry. Larger stalls scale super-linearly in the over-application.

### 3.2 Post-fix walk (L1174 sentinel conjunct `rngWordByDay[idx + 1] == 0` active — current behavior at HEAD acd88512)

Post-fix shape: the guard at L1174 reads `if (day > idx + 1 && rngWordByDay[idx + 1] == 0)`. The walk below seeds the same scenario as §3.1 and shows the short-circuit on re-entry.

| Row ID | Step | Block | day | dailyIdx | rngLockedFlag | rngRequestTime | currentWord | rngWordByDay[idx+1] | guard verdict | purchaseStartDay (post) | gapDays (post) | _backfillGapDays called? |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BFL-03-V09 | 1 — first advanceGame at start of stall | 10759449 | N+1 | N | true | T_req | 0 | 0 | rngGate enters branch at L1212 (timeout retry / rng-not-ready); guarded block NOT reached this call | unchanged | 0 | NO |
| BFL-03-V10 | 2 — VRF fulfillment populates currentWord | (off-chain VRF callback) | N+1 | N | true | T_req | W1 | 0 | n/a — callback path | unchanged | 0 | NO |
| BFL-03-V11 | 3 — second advanceGame at block 10761786 (FIRST + ONLY guarded-block execution) | 10761786 | N+2 | N | true | T_req | W1 | 0 | rngGate L1160 outer-skip FAILS; fresh-word branch entered; L1174 (POST-FIX): `day > idx + 1` TRUE AND `rngWordByDay[N+1] == 0` TRUE → both conjuncts TRUE → guarded block executes. `_backfillGapDays(W1, N+1, N+2, bonusFlip)` runs (writes `rngWordByDay[N+1] = derivedW` — flips the sentinel from 0 to non-zero); `purchaseStartDay += 1`; `gapDays = 1` | startBefore + 1 | 1 | YES (1st and ONLY time) |
| BFL-03-V12 | 4 — third advanceGame on same wall-clock day | 10761786 + n | N+2 | N | true | T_req | W1 | derivedW | rngGate L1160 outer-skip succeeds (`rngWordByDay[N+2]` was set by Step 3's `_applyDailyRng`) → function returns early | startBefore + 1 | 0 | NO |
| BFL-03-V13 | 5 — chain crosses midnight UTC again (still no `_unlockRng`) | (block-timestamp advance) | N+3 | N | true | T_req | W1 | derivedW | n/a — no call | startBefore + 1 | 0 | NO |
| BFL-03-V14 | 6 — fourth advanceGame at start of new wall-clock day (POST-FIX SHORT-CIRCUIT) | next-day block | N+3 | N | true | T_req | W1 | derivedW (set by Step 3) | rngGate L1160 outer-skip FAILS (`rngWordByDay[N+3] == 0`); fresh-word branch entered; L1174 (POST-FIX): `day > idx + 1` is `N+3 > N+1` TRUE BUT `rngWordByDay[N+1] == 0` is FALSE (Step 3 wrote it) → guarded block SKIPPED. Always-executed writes L1189-1207 still run for current day N+3. `_backfillGapDays` NOT called. `purchaseStartDay` UNCHANGED. `gapDays = 0` | startBefore + 1 (still) | 0 | NO (short-circuited) |
| BFL-03-V15 | 7 — eventual `_unlockRng` fires; downstream consumes correct purchaseStartDay; no underflow | (later, when rngLockedFlag flips) | N+3 | N+3 | false | 0 | 0 | (irrelevant) | downstream subtractor reads `purchaseStartDay = startBefore + 1`; the day N+2 gap was correctly accounted; subsequent days N+3 onward are normal-path advances | startBefore + 1 (correct) | n/a | n/a |

**Post-fix conclusion:** The L1174 sentinel conjunct provably blocks all re-entries beyond the first within a single VRF lock window (single `_unlockRng`-bounded interval). `purchaseStartDay` advances exactly `gapCount` once per lock window; downstream subtractors operate on a value that did not receive over-application; the testnet panic 0x11 path is closed. Each row's post-state evidence is observable via `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol` at the cited line.

All BFL-03-V09..V15 rows verdict: **SAFE** (post-fix shape; all reachable paths short-circuit correctly under the L1174 sentinel).

All BFL-03-V01..V08 rows verdict: **SAFE-IF-POST-FIX-APPLIED** (pre-fix narrative-only rows; the bug exists in pre-fix shape, the proof closes in post-fix shape — these rows exist for narrative contrast only and are not part of the post-fix proof obligations).

## Section 4 — BFL-04: dailyIdx ↔ rngWordByDay[idx] ↔ _unlockRng invariant table

Per CONTEXT.md D-248-15, the universe-of-write-sites for `dailyIdx` and `rngWordByDay[*]` at HEAD `acd88512` is established by the following grep commands cited verbatim:

```
git grep -n '\bdailyIdx\s*=' acd88512 -- 'contracts/*.sol' 'contracts/**/*.sol'
# Returns 2 results at HEAD acd88512:
#   contracts/DegenerusGame.sol:219              (constructor init — runs once at deploy)
#   contracts/modules/DegenerusGameAdvanceModule.sol:1703   (inside _unlockRng — runtime sole writer)

git grep -n '\brngWordByDay\[[^]]*\]\s*=' acd88512 -- 'contracts/*.sol' 'contracts/**/*.sol'
# Returns 2 write-site results at HEAD acd88512 (filtered to LHS-of-assignment):
#   contracts/modules/DegenerusGameAdvanceModule.sol:1766   (inside _backfillGapDays loop — gap-day writer)
#   contracts/modules/DegenerusGameAdvanceModule.sol:1814   (inside _applyDailyRng — current-day writer)
```

Note on `dailyIdx` constructor write at `DegenerusGame.sol:219`: this is a CONSTRUCTOR-only initialization (`dailyIdx = currentDay` set to the deploy-day index). It runs exactly once at deploy and is structurally outside any VRF lock window. The runtime sole-writer attestation for `dailyIdx` therefore narrows to AdvanceModule:1703 inside `_unlockRng`, with the constructor row recorded for completeness. This refinement is captured in Phase 248 SUMMARY.md as a scope-guard note (per CONTEXT.md D-248-14): the canonical_line_ranges in Phase 248 PLAN.md frontmatter cited "sole writer" at L1703 — that is the runtime sole writer; the deploy-time constructor init is a separate write that does not invalidate the runtime invariant.

Column shape per CONTEXT.md `<specifics>` BFL-04 row format: `Row ID | Site (file:line) | Function | State-var written | Write expression | Guard preconditions | Holds 'dailyIdx only mutates inside _unlockRng' (runtime)? | Holds 'rngWordByDay[idx+1] is correct sentinel'? | Verdict | Evidence Cite`.

| Row ID | Site (file:line) | Function | State-var written | Write expression | Guard preconditions | Holds 'dailyIdx only mutates inside `_unlockRng`' (runtime)? | Holds 'rngWordByDay[idx+1] is correct sentinel'? | Verdict | Evidence Cite |
|---|---|---|---|---|---|---|---|---|---|
| BFL-04-V01 | DegenerusGame.sol:219 | `constructor` (DegenerusGame) | `dailyIdx` | `dailyIdx = currentDay` (set to deploy-day index) | constructor-only; runs exactly once at deploy; structurally outside any VRF lock window | HOLDS at runtime — this is a deploy-time bootstrap, not a runtime mutation; the runtime sole-writer invariant is preserved because no runtime path can reach this line again after deploy | N/A — this row attests `dailyIdx` initialization, not the sentinel | HOLDS | grep result line 1; `git show acd88512:contracts/DegenerusGame.sol` L218-219 — `uint32 currentDay = GameTimeLib.currentDayIndex(); ... dailyIdx = currentDay;` inside `constructor()` |
| BFL-04-V02 | AdvanceModule:1703 | `_unlockRng` | `dailyIdx` | `dailyIdx = day;` | `_unlockRng` is `private`; called ONLY from within AdvanceModule; reachable via `advanceGame` post-rngGate path when `rngLockedFlag` flips false → daily unlock cadence | HOLDS — this IS the runtime sole writer; no other runtime path mutates `dailyIdx` per the grep enumeration above | N/A — this row attests `dailyIdx`'s sole writer invariant; the sentinel correctness column applies to the rngWordByDay rows below | HOLDS | grep result line 2; `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol` L1702-1709 — `function _unlockRng(uint32 day) private { dailyIdx = day; rngLockedFlag = false; ... }` |
| BFL-04-V03 | AdvanceModule:1814 | `_applyDailyRng` | `rngWordByDay[day]` | `rngWordByDay[day] = finalWord;` (current-day write — `day` is the function-call parameter, NOT `idx + 1`) | always-executed inside rngGate's fresh-word branch at L1189; runs AFTER the L1174 backfill guard; out-of-scope-for-guarded-block per BFL-02 §2.2 boundary | HOLDS — does not write `dailyIdx` | HOLDS as ITSELF — this write does NOT corrupt the sentinel because `day != idx + 1` whenever the guarded block also runs (gap implies `day > idx + 1`, so the always-path's `rngWordByDay[day]` write targets a strictly-different key from `rngWordByDay[idx + 1]`). On the no-gap path (`day == idx + 1`) `_applyDailyRng` writes `rngWordByDay[idx + 1]`, but on that path the L1174 left conjunct `day > idx + 1` is FALSE so the guarded block was already not entered — no sentinel-vs-write race | HOLDS | `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol` L1801-1817 — `function _applyDailyRng(uint32 day, uint256 rawWord) private returns (uint256 finalWord) { ... rngWordByDay[day] = finalWord; ... }` |
| BFL-04-V04 | AdvanceModule:1766 | `_backfillGapDays` (inside loop body L1761-1772) | `rngWordByDay[gapDay]` | `rngWordByDay[gapDay] = derivedWord;` (loop iterates `gapDay = startDay..endDay-1` where `startDay = idx + 1` per L1176 call) | called from rngGate L1176 inside guarded block L1174-1186; only reachable when both L1174 conjuncts TRUE | HOLDS — does not write `dailyIdx` | HOLDS — first iteration writes `rngWordByDay[startDay]` where `startDay == idx + 1` per the call site at L1176, which IS the sentinel key. This is the write that flips the sentinel from 0 → non-zero on first execution, enabling the L1174 short-circuit on subsequent re-entries | HOLDS | `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol` L1761-1772 — loop with `rngWordByDay[gapDay] = derivedWord;` at L1766 |

**Invariant-closure paragraph:**

Per the BFL-04 invariant table, `dailyIdx` mutates at runtime ONLY at AdvanceModule:1703 inside `_unlockRng` (sole runtime writer attested via `git grep -n '\bdailyIdx\s*=' acd88512 -- 'contracts/*.sol' 'contracts/**/*.sol'` returning the constructor write at DegenerusGame.sol:219 and the runtime write at AdvanceModule:1703 — the constructor write is structurally outside any VRF lock window). `rngWordByDay[idx + 1]` is correctly identified as the sentinel for "backfill not yet run for this lock window" because: (i) the FIRST write to that key happens at L1766 inside `_backfillGapDays`'s loop on the FIRST iteration (`gapDay == startDay == idx + 1` per the L1176 call site); (ii) the always-executed `_applyDailyRng` at L1814 writes a DIFFERENT key (`rngWordByDay[day]`, where `day > idx + 1` whenever a gap exists; on the no-gap path the L1174 left conjunct already SKIPS the guarded block, so no sentinel-vs-current-day race is reachable); (iii) `dailyIdx` cannot advance between the L1174 guard read and the L1176 backfill call because the guarded block executes synchronously inside `rngGate` without re-entering `_unlockRng` (the sole runtime writer of `dailyIdx`). All four rows verdict HOLDS; the BFL-04 invariant is satisfied across all 4 enumerated write sites.

## Section 5 — BFL-05: KI EXC-02 + EXC-03 envelope RE_VERIFY

**Methodology (per CONTEXT.md D-248-13):**

BFL-05 RE_VERIFIES the two pre-locked KNOWN-ISSUES.md envelopes against the new backfill guard at AdvanceModule:1174 using a dual-carrier attestation pattern (mirrors v31-245-SDR.md SDR-08-V01 / v31-245-GOE.md GOE-01-V01 + GOE-04-V02 precedents). Each carrier row independently checks one envelope's pre-guard vs post-guard surface. Envelope text is cited verbatim from `KNOWN-ISSUES.md` as evidence; the file itself is NOT edited in Phase 248 per D-248-04 (KI promotions are Phase 253 FIND-03 only).

The pre-locked envelopes RE_VERIFIED here:

- **EXC-02 (Gameover prevrandao fallback)** — `_getHistoricalRngFallback` admixes `block.prevrandao` with up to 5 historical VRF words when VRF is unavailable for ≥14 days at gameover. The 1-bit validator-proposer bias is accepted because the path activates only after both VRF AND coordinator-swap governance fail to land a fresh coordinator within 14 days. Verified line at HEAD `acd88512`: `_getHistoricalRngFallback` at AdvanceModule:L1329 (KNOWN-ISSUES.md cites L1301; both refer to the same function — the renumbering reflects line-shift from acd88512 inserts above the function, not a function-body change).
- **EXC-03 (Gameover RNG substitution for mid-cycle write-buffer tickets)** — at gameover, mid-cycle write-buffer tickets drain under `_gameOverEntropy` rather than the originally-anticipated mid-day VRF word. Accepted because gameover is a terminal state with no further gameplay and no player-reachable exploit. Verified line at HEAD `acd88512`: `_gameOverEntropy` at AdvanceModule:L1238 (KNOWN-ISSUES.md cites L1222-1246 / L1252; same function, body unchanged).

Default expected verdict: **NON-WIDENING** for both carriers. The L1174 backfill guard governs `rngGate`'s fresh-word-branch reachability; both EXC-02 and EXC-03 are reachable only via the `_gameOverEntropy` call site (a SIBLING entropy-substitution surface that is never reached from inside `rngGate`'s fresh-word branch). If any envelope were to widen as a consequence of the new guard, the corresponding row would be classified `FINDING_CANDIDATE` per D-248-12 and routed to a §5.4 Finding Candidates gating-walk subsection per D-248-03 (no v32 finding-ID emitted; Phase 253 D-09 owns).

### 5.1 Dual-Carrier Attestation Table (per D-248-13)

Column shape: `Carrier ID | EXC ID | Trigger condition (path:line) | Pre-guard envelope (one-line) | Post-guard envelope (one-line) | Widening? | Evidence cite`.

| Carrier ID | EXC ID | Trigger condition (path:line) | Pre-guard envelope (one-line) | Post-guard envelope (one-line) | Widening? | Evidence cite |
|---|---|---|---|---|---|---|
| BFL-05-V01 | EXC-02 | `_getHistoricalRngFallback` at AdvanceModule:1329; reached only via `_gameOverEntropy`'s 14-day-elapsed branch at AdvanceModule:1273-1306 (gameover-only path; rngLockedFlag dead by then) | "`_getHistoricalRngFallback` admixes `block.prevrandao` with up to 5 historical VRF words after a 14-day VRF outage at gameover; 1-bit validator-proposer bias is accepted because both VRF AND coordinator-swap governance must have failed within the window" | IDENTICAL to pre-guard. The L1174 backfill guard governs `rngGate`'s fresh-word branch reachability, NOT `_getHistoricalRngFallback`'s reach. EXC-02 is triggered by historical-RNG reconstruction inside `_gameOverEntropy`'s fallback branch (L1277), which calls `_getHistoricalRngFallback(day)` to read `rngWordByDay[searchDay]` for `searchDay ∈ [1, min(currentDay, 30))` (L1334-1347). Whether `_backfillGapDays` ran ONCE (post-fix) or N times (pre-fix), the FINAL value of each `rngWordByDay[gapDay]` is identical (loop is deterministic per `currentWord` — see BFL-04-V04 evidence cite); therefore the historical-RNG reconstruction reads the SAME values regardless of guard regime. The trigger condition (no fresh VRF for 14 days at gameover) is unaffected by the guard. NON-WIDENING. | NON-WIDENING | KNOWN-ISSUES.md §"Gameover prevrandao fallback" (verbatim quoted in §5.2 below); Phase 247 D-247-X030 (rngGate caller catalog confirms rngGate's only caller is `advanceGame` at AdvanceModule:292, disjoint from `_gameOverEntropy`'s reach); HEAD `acd88512` source at AdvanceModule:1329 |
| BFL-05-V02 | EXC-03 | `_gameOverEntropy` at AdvanceModule:1238; reached only via the gameover branch dispatched from `advanceGame`'s gameover-flow logic (mutually exclusive with the normal-day-advancement branch that contains rngGate's fresh-word block) | "At gameover, mid-cycle write-buffer tickets drain under `_gameOverEntropy`'s VRF word (or VRF+prevrandao admixture per EXC-02) rather than the originally-anticipated mid-day VRF word; accepted because gameover is a terminal state with no further gameplay and no player-reachable exploit (gameover triggered by 120-day liveness stall or pool deficit, neither timer-able against a specific mid-cycle write-buffer state)" | IDENTICAL to pre-guard. `_gameOverEntropy` fires on the gameover branch which is mutually exclusive with the normal-day-advancement branch that contains `rngGate`'s fresh-word block. The L1174 guard is structurally unreachable in any path that reaches `_gameOverEntropy`: gameover dispatch is taken in `advanceGame`'s top-level branching BEFORE rngGate is invoked (Phase 247 D-247-F010 advanceGame MODIFIED_LOGIC row notes the turbo-guard at L173 is the only acd88512 advanceGame change; the gameover-branch dispatch is unchanged). The substitution envelope (mid-cycle write-buffer tickets reading gameover entropy) is determined by `_gameOverEntropy`'s body L1238-1316, not by whether `_backfillGapDays` ran 0 or 1 times beforehand. NON-WIDENING. | NON-WIDENING | KNOWN-ISSUES.md §"Gameover RNG substitution for mid-cycle write-buffer tickets" (verbatim quoted in §5.2 below); Phase 247 D-247-X030 + D-247-F010 advanceGame catalog row (gameover-dispatch unchanged in acd88512); HEAD `acd88512` source at AdvanceModule:1238 |

### 5.2 Verbatim envelope quotations (evidence)

> **[EXC-02 envelope, KNOWN-ISSUES.md L29 verbatim:]**
>
> **Gameover prevrandao fallback.** `_getHistoricalRngFallback` (`DegenerusGameAdvanceModule.sol:1301`) hashes `block.prevrandao` together with up to 5 historical VRF words as supplementary entropy when VRF is unavailable at game over. A block proposer can bias prevrandao (1-bit manipulation on binary outcomes). Trigger gating: only reachable inside `_gameOverEntropy` (`AdvanceModule:1252`) and only when an in-flight VRF request has been outstanding for at least `GAMEOVER_RNG_FALLBACK_DELAY = 14 days` (`AdvanceModule:109`). The 14-day window is ~17× the 20-hour VRF coordinator-swap governance threshold (see "VRF swap governance" entry above), so this path activates only after both VRF itself AND the governance recovery mechanism have failed to land a fresh coordinator within 14 days. The 5 committed historical VRF words provide bulk entropy; prevrandao only adds unpredictability.

> **[EXC-03 envelope, KNOWN-ISSUES.md L38 verbatim:]**
>
> **Gameover RNG substitution for mid-cycle write-buffer tickets.** Degenerus enforces an "RNG-consumer determinism" invariant: every RNG consumer's entropy must be fully committed at input time — the VRF word that a consumer will ultimately read must be unknown-but-bound at the moment that consumer's input parameters are committed to storage. One terminal-state case technically violates it: if a mid-cycle ticket-buffer swap has occurred (daily RNG request via `_swapAndFreeze(purchaseLevel)` at `DegenerusGameAdvanceModule.sol:292`, OR mid-day lootbox RNG request via `_swapTicketSlot(purchaseLevel_)` at `DegenerusGameAdvanceModule.sol:1082`) and the new write buffer is populated with tickets queued at the current level awaiting the expected-next VRF fulfillment, a game-over event intervening before that fulfillment causes those tickets to drain under the final gameover entropy (`_gameOverEntropy` at `DegenerusGameAdvanceModule.sol:1222-1246` — the gameover VRF word under normal conditions, or the VRF-plus-`block.prevrandao` admixture described in the "Gameover prevrandao fallback" entry above when an in-flight VRF request stays unfulfilled for 14+ days) rather than the originally-anticipated mid-day VRF word. The substitution applies in both gameover variants — it is NOT contingent on the prevrandao fallback activating. Acceptance rationale: (a) only reachable at gameover — a terminal state with no further gameplay after the 30-day post-gameover window; (b) no player-reachable exploit — gameover is triggered by a 120-day liveness stall or a pool deficit, neither of which an attacker can time against a specific mid-cycle write-buffer state; (c) at gameover the protocol must drain within bounded transactions and cannot wait for a deferred fulfillment that may never arrive if the VRF coordinator itself is the reason for the liveness stall; (d) all substitute entropy is VRF-derived or VRF-plus-prevrandao.

### 5.3 Per-carrier reasoning paragraph

**BFL-05-V01 (EXC-02 NON-WIDENING):** `_getHistoricalRngFallback` is invoked from `_gameOverEntropy`'s 14-day-elapsed branch (AdvanceModule:1277) and reads `rngWordByDay[searchDay]` for `searchDay ∈ [1, min(currentDay, 30))`. The values it consumes are written by `_applyDailyRng` (current-day path; BFL-04-V03) and `_backfillGapDays` (gap-day path; BFL-04-V04). The L1174 guard changes how many times `_backfillGapDays` runs per lock window (post-fix: ONCE; pre-fix: N times) but the final value of each `rngWordByDay[gapDay]` is identical regardless of N because the loop body at L1761-1772 is deterministic in `currentWord` (a single fresh VRF word that has been committed before either guard regime evaluates). Therefore the historical-RNG reconstruction `_getHistoricalRngFallback` reads the SAME value set per day under both regimes. The EXC-02 trigger condition ("no fresh VRF for 14 days at gameover") and the EXC-02 substitution surface (prevrandao admixture into 5 historical VRF words) are both unaffected by the guard. The pre-guard envelope and the post-guard envelope are identical. **NON-WIDENING.**

**BFL-05-V02 (EXC-03 NON-WIDENING):** `_gameOverEntropy` is invoked on the gameover branch dispatched from `advanceGame`'s top-level gameover-flow logic. Per Phase 247 D-247-F010 (advanceGame MODIFIED_LOGIC), the only acd88512 change to `advanceGame` is the turbo-guard at L173 — the gameover-branch dispatch is unchanged and remains mutually exclusive with the normal-day-advancement branch that contains rngGate's fresh-word block. The L1174 guard is therefore structurally unreachable in any path that reaches `_gameOverEntropy`. The EXC-03 substitution envelope (mid-cycle write-buffer tickets reading gameover entropy when intervened upon by gameover before the anticipated mid-day VRF fulfillment) is determined by `_gameOverEntropy`'s body L1238-1316 and the call-site predicates above it; the L1174 guard adds no new reach into that surface and removes none from it. **NON-WIDENING.**

**KNOWN-ISSUES.md status:** UNCHANGED throughout Phase 248 per D-248-04 last sentence. Both envelopes verdict NON-WIDENING; no Phase 253 D-09 gating walk required.

## Section 6 — BFL-06: lock-window-scoped conservation proof

Per REQUIREMENTS.md OUT OF SCOPE clauses, BFL-06 conservation is scoped to the VRF lock window only (the bounded interval from a fresh `_swapAndFreeze` daily VRF request to its corresponding `_unlockRng` call). Global ETH / sDGNRS / DGNRS / BURNIE conservation on non-delta surfaces was proven in v25.0 / v29.0 / v30.0 / v31.0 and is NOT re-proven here.

### 6.1 Conservation Algebra (lock-window-scoped)

Let:

- `startDay = idx + 1` (the first gap day; equals `dailyIdx + 1` at L1174 evaluation time)
- `endDay = day` (the current wall-clock day; the call argument to rngGate)
- `gapCount = endDay - startDay = day - (idx + 1)` (the number of gap days; matches the local `gapCount` at L1175)
- `Σ = Σ_{d=startDay..endDay-1} (per-day state-write effect of coinflip.processCoinflipPayouts(bonusFlip, derivedWord, d))` — boundary cite per D-248-10: per-day state writes are `coinflipDayResult[d]` (BurnieCoinflip.sol:838-841), `flipsClaimableDay` (BurnieCoinflip.sol:862), `currentBounty` (BurnieCoinflip.sol:865), plus the bounty-resolution credit at BurnieCoinflip.sol:855-863 when the bounty is armed. ETH-pool credit is not direct here — `processCoinflipPayouts` records the day's win/loss and reward percent; ETH credits land later via `_claimCoinflipsInternal`. Re-running `processCoinflipPayouts` for the same `epoch` would (i) overwrite `coinflipDayResult[epoch]` (last-write-wins; final value identical because rngWord is the same `derivedWord`), (ii) increment `currentBounty` an extra `PRICE_COIN_UNIT` per re-execution (THE LITERAL CONSERVATION VIOLATION on the BurnieCoinflip side), (iii) re-emit `CoinflipDayResolved` events.
- `N` = number of fresh-word re-entries before `_unlockRng` fires within a single VRF lock window (`N >= 2` for the multi-day VRF stall trigger shape per BFL-03 §3.1)

#### Pre-fix algebra (no L1174 sentinel conjunct)

- `purchaseStartDay_after = purchaseStartDay_before + Σ_{i=1..N} gapCount_i` where `gapCount_i = day_i - (idx + 1)` for the i-th re-entry. Because `idx` does NOT advance between re-entries (BFL-04 invariant — `_unlockRng` is the sole runtime writer), `gapCount_i` strictly increases with each re-entry on a new wall-clock day. For a 3-day stall this produces `+1 + +2 = +3` instead of the correct `+2` (BFL-03 §3.1 V07 evidence) — an over-application of `gapCount_N - gapCount_1` extra days.
- `coinflip per-day state writes = N * Σ` (each `epoch ∈ [startDay, endDay-1]` is reprocessed on each re-entry; `currentBounty` accumulates an extra `PRICE_COIN_UNIT` per re-execution per gap day)
- Worst-case downstream consequence: `purchaseStartDay_after` exceeds the safe range against downstream subtractors (the literal testnet panic 0x11 path — see BFL-03 §3.1 V08).

#### Post-fix algebra (L1174 sentinel conjunct `rngWordByDay[idx + 1] == 0` active — current behavior at HEAD acd88512)

- `purchaseStartDay_after = purchaseStartDay_before + 1 * gapCount = purchaseStartDay_before + gapCount` (single application; subsequent re-entries SKIP the guarded block per BFL-01-V03 + BFL-02-V05 sentinel correctness — the sentinel is set by the FIRST iteration of `_backfillGapDays`'s loop at L1766 inside the same call)
- `coinflip per-day state writes = 1 * Σ` (each `epoch ∈ [startDay, endDay-1]` is processed exactly once)
- No underflow: `purchaseStartDay` advances exactly `gapCount` once per gap range; downstream subtractors operate on a value that did not receive over-application.

#### sDGNRS / DGNRS / BURNIE supply invariance (lock-window scope)

The guarded block L1174-1186 contains NO direct `_mint` and NO direct `_burn` calls. Verified via grep at HEAD `acd88512`:

```
sed -n '1174,1186p' contracts/modules/DegenerusGameAdvanceModule.sol | grep -nE '_mint|_burn'
# Returns: 0 hits

sed -n '1761,1772p' contracts/modules/DegenerusGameAdvanceModule.sol | grep -nE '_mint|_burn'
# Returns: 0 hits (inside _backfillGapDays loop body)

sed -n '1779,1798p' contracts/modules/DegenerusGameAdvanceModule.sol | grep -nE '_mint|_burn'
# Returns: 0 hits (inside _backfillOrphanedLootboxIndices body)
```

The external call `coinflip.processCoinflipPayouts` (L1767) does not mint or burn — its body at BurnieCoinflip.sol:802-887 records the day result, accumulates the bounty pool, and credits the bounty owner via `_addDailyFlip` (a flip-stake credit, not a token mint). Therefore total supply of sDGNRS, DGNRS, and BURNIE is invariant across the lock window, regardless of how many times the guarded block executes (pre-fix or post-fix). REQUIREMENTS.md BFL-06 lock-window-scoped supply-invariance clause is satisfied.

### 6.2 Per-mutation Conservation Closure Table

Column shape: `Row ID | State-var | Pre-fix mutation expression | Post-fix mutation expression | Conservation closure (bool) | Evidence Cite`.

| Row ID | State-var | Pre-fix mutation expression | Post-fix mutation expression | Conservation closure | Evidence Cite |
|---|---|---|---|---|---|
| BFL-06-V01 | `purchaseStartDay` | `purchaseStartDay += gapCount_i` runs once per re-entry across N re-entries → `purchaseStartDay_after = purchaseStartDay_before + Σ gapCount_i` (super-linear over-application because `gapCount_i` grows with each new wall-clock day before `_unlockRng` fires) | runs ONCE per lock window → `purchaseStartDay_after = purchaseStartDay_before + gapCount` | TRUE (post-fix matches single-application semantics required by REQUIREMENTS.md BFL-06) | AdvanceModule:1184 (guarded block); BFL-02-V05 sentinel correctness; BFL-01-V03 short-circuit row; BFL-03 §3.2 V11 + V14 walk |
| BFL-06-V02 | `coinflipDayResult[epoch]` (per gap day) for `epoch ∈ [startDay, endDay-1]` | `coinflipDayResult[epoch] = CoinflipDayResult{rewardPercent, win}` overwritten N times per gap day (last-write-wins; final value identical because `derivedWord = keccak256(currentWord, gapDay)` is deterministic in `currentWord`) | written ONCE per gap day | TRUE (final value identical between pre-fix and post-fix on this row by virtue of loop determinism, but: the side-effect ROWS V03 + V04 below are the load-bearing conservation rows for this site) | AdvanceModule:1767 → BurnieCoinflip.sol:838-841 per D-248-10 boundary cite |
| BFL-06-V03 | `currentBounty` (BurnieCoinflip storage) | incremented by `PRICE_COIN_UNIT` ONCE per `processCoinflipPayouts` call → bumped N times per gap day across N re-entries | bumped ONCE per gap day | TRUE (post-fix matches single-application semantics; pre-fix would over-bump bounty by `(N-1) * PRICE_COIN_UNIT * gapCount` per lock window) | AdvanceModule:1767 → BurnieCoinflip.sol:865 (`currentBounty = currentBounty_ + uint128(PRICE_COIN_UNIT)`) per D-248-10 boundary cite |
| BFL-06-V04 | bounty payout (BurnieCoinflip — bounty-armed branch) | if bounty was armed AND win: bounty owner credited via `_addDailyFlip(to, slice, 0, false, false)` ONCE per `processCoinflipPayouts` call where `slice = currentBounty_ >> 1` → on N re-entries the bounty would be drained N times (or, more precisely, `bountyOwedTo` is cleared on first execution per BurnieCoinflip.sol:863, so subsequent re-entries see `bountyOwner == address(0)` and skip — the bounty path is naturally idempotent on re-entry within the SAME currentBounty) | runs ONCE per gap day | TRUE (post-fix exact; pre-fix idempotent-on-bounty-clear via the inner `bountyOwedTo = address(0)` mechanism, but still over-bumps `currentBounty` per V03) | AdvanceModule:1767 → BurnieCoinflip.sol:847-863 per D-248-10 boundary cite |
| BFL-06-V05 | `rngWordByDay[gapDay]` for `gapDay ∈ [startDay, endDay)` | overwritten N times with `derivedWord` (last-write-wins; final value identical because `derivedWord = keccak256(currentWord, gapDay)` is deterministic in `currentWord`) | written ONCE per gap day | TRUE — final state IDENTICAL between pre-fix and post-fix (loop deterministic) | AdvanceModule:1766 |
| BFL-06-V06 | `lootboxRngWordByIndex[i]` for orphaned indices | written N times by `_backfillOrphanedLootboxIndices`. The helper short-circuits at L1785 (`if (lootboxRngWordByIndex[i] != 0) break;`) once it hits a filled index, so subsequent re-entries break on the first iteration after seeing the prior re-entry's writes — first execution flips the previously-orphaned indices, subsequent re-entries are effectively no-op on the storage but still pay a `LootboxRngApplied` event re-emission for the topmost previously-orphaned index | written ONCE per orphaned index | TRUE — internal idempotency guard at L1785 makes pre-fix N executions effectively equivalent to 1 on the storage; the outer L1174 guard removes the duplicate event emission and the wasted gas | AdvanceModule:1180 (call site); helper body L1779-1798 |
| BFL-06-V07 | `gapDays` (rngGate named-return local) | assigned N times to `gapCount_i` (local variable; not storage) | assigned ONCE | TRUE — local; the caller `advanceGame` reads it at most once per call regardless of pre-fix/post-fix shape, but the VALUE differs (pre-fix: latest re-entry's `gapCount_N`; post-fix: the only re-entry's `gapCount`). Downstream `gapDays`-dependent advanceGame logic operates on a different value across re-entries pre-fix vs the consistent value post-fix | AdvanceModule:1185 |
| BFL-06-V08 | sDGNRS supply | invariant (no direct `_mint`/`_burn` in guarded block; verified via grep — 0 hits in L1174-1186, 0 hits in `_backfillGapDays` loop, 0 hits in `_backfillOrphanedLootboxIndices`) | invariant | TRUE | grep evidence in §6.1; BurnieCoinflip.sol::processCoinflipPayouts contains no token mint/burn |
| BFL-06-V09 | DGNRS supply | invariant (same grep evidence as V08) | invariant | TRUE | same evidence |
| BFL-06-V10 | BURNIE supply | invariant (same grep evidence as V08) | invariant | TRUE | same evidence |

**Closure paragraph:**

Per the BFL-06 per-mutation row table, lock-window-scoped conservation closes algebraically: `purchaseStartDay` advances exactly `gapCount` once per lock window post-fix (vs `Σ gapCount_i` pre-fix where `gapCount_i` grows with each new-day re-entry); `coinflipDayResult[epoch]` final values are identical between regimes (loop deterministic) but `currentBounty` is over-bumped by `(N-1) * PRICE_COIN_UNIT * gapCount` pre-fix and exactly once per gap day post-fix; `rngWordByDay[gapDay]` final values are identical between regimes (loop deterministic); `lootboxRngWordByIndex[i]` is structurally idempotent via the inner L1785 break, but the outer L1174 guard removes wasted gas + duplicate `LootboxRngApplied` events; sDGNRS / DGNRS / BURNIE supplies are invariant under both regimes because the guarded block contains no token minting or burning. The post-fix algebra matches the expected single-application semantics required by REQUIREMENTS.md BFL-06.

## Phase 251 TST-04 Hand-Off

Per CONTEXT.md D-248-06, this appendix provides scope input for Phase 251 TST-04 reproduction-test design. Phase 251 reads this appendix and decides on final test file naming, fixture extension vs new file, and assertion granularity.

### 251.1 Symbolic Spec

> **Pre-state setup:** Lock window crosses ≥2 wall-clock days. Trigger conditions at the second `advanceGame` call:
> - `currentWord != 0` (VRF fulfillment landed while `rngLockedFlag` was still TRUE — `rawFulfillRandomWords` stored it at AdvanceModule:1730 but did not clear `rngLockedFlag`)
> - `rngRequestTime != 0` (the daily VRF request is still in flight from the protocol state-machine perspective)
> - `rngWordByDay[day] == 0` (current wall-clock day's RNG not yet recorded)
> - `day > dailyIdx + 1` (gap exists)
> - First call: `rngWordByDay[dailyIdx + 1] == 0` (no prior backfill); subsequent calls: `rngWordByDay[dailyIdx + 1] != 0` (BUT see below — the test must construct this sentinel-FALSE state observably)
>
> Block-timestamp advancement: forward by ≥1 day between successive `advanceGame` calls so the second call sees `day = dailyIdx + 2` (gapCount = 1 in the simplest case). `_unlockRng` MUST NOT fire between the two calls (i.e., the fresh-word branch re-enters before unlock — see BFL-03 §3.1 walk for the exact sequence). Practical setup tip: the test fixture should maintain `rngLockedFlag = true` between the two calls (the daily VRF request remains in flight from the state-machine perspective even though `currentWord` is populated; this is the testnet trigger state).
>
> **Call sequence to trigger:**
> 1. Fast-forward setup to the start of the stall: ensure `dailyIdx = N` and `rngWordByDay[N] = W_prev` (consume any prior day's VRF cleanly).
> 2. Issue daily VRF request via the normal `advanceGame` path (this calls `_swapAndFreeze` and sets `rngLockedFlag = true`, `rngRequestTime = T1`).
> 3. Mock `rawFulfillRandomWords` to deliver `currentWord = W1` while `rngLockedFlag` remains TRUE (post-Step 2 state is the natural setup; Chainlink VRF coordinator returns the word during step 2's flow but the gate-clear happens later).
> 4. Advance `block.timestamp` by 1 day (forge `vm.warp` or hardhat `evm_increaseTime` + `evm_mine`). Do NOT call `_unlockRng` (which would advance `dailyIdx` and reset the trigger).
> 5. Call `advanceGame()` again. Pre-fix expected: guarded block executes, `purchaseStartDay += 1`. Post-fix expected: same as pre-fix (this is the FIRST guarded-block execution; the sentinel was 0).
> 6. Advance `block.timestamp` by another 1 day. Still no `_unlockRng`.
> 7. Call `advanceGame()` AGAIN (cross-contract or via test-fixture re-entry — the simplest test path is a direct repeated `advanceGame()` call from the test EOA, which still funnels through entry path D-247-X027 from DegenerusGame.sol:289). Pre-fix expected: guarded block executes a SECOND time, `purchaseStartDay += 2` (over-application). Post-fix expected: L1174 sentinel SKIPS the guarded block; `purchaseStartDay` UNCHANGED.
> 8. Read `purchaseStartDay` after Step 7.
>
> **Expected pre-fix assertion failure:** `purchaseStartDay` exceeds the value expected for a 2-day gap (`startBefore + 3` instead of `startBefore + 2`). Downstream subtractor that consumes `purchaseStartDay` (e.g., death-clock check) reverts with panic 0x11 (arithmetic underflow) when the inflated value is subtracted from a smaller bound. The exact revert site at HEAD `acd88512` depends on which downstream consumer Phase 251 chooses to assert against (the cleanest assertion is direct: `purchaseStartDay == startBefore + 2` post-Step 7).
>
> **Expected post-fix pass behavior:** `purchaseStartDay == startBefore + 2` after Step 7 (single application of the gap-day count); no panic; downstream invariants hold. Additional post-fix assertions: `currentBounty` (BurnieCoinflip storage) is bumped exactly twice (once per gap day in the 2-day gap window) — pre-fix would bump it 3+ times.

### 251.2 Suggested Test File

> **Option A (preferred — new isolated edge test):** `test/edge/BackfillIdempotency.test.js` (hardhat). A new test file dedicated to this trigger, mirroring the `test/edge/LastPurchaseDayRace.test.js` shape (the sibling untracked file in working tree per Phase 247 D-247-02 carry-forward). Phase 251 final naming.
>
> **Option B (alternate — extend existing race test):** Add a new `it("blocks doubled purchaseStartDay across multi-day VRF stall", ...)` block inside `test/edge/LastPurchaseDayRace.test.js`. This option is appropriate if Phase 251 decides the two race shapes (last-purchase-day + backfill-idempotency) belong in the same fixture for setup-cost amortization (the VRF-stall scaffolding is the same for both).
>
> Phase 251 picks the final option + final filename. This appendix is a HAND-OFF (scope input), not a prescriptive constraint.

### 251.3 Phase 247 Row Anchors

> Phase 251 inherits the following Phase 247 catalog rows as TST-04 scope input:
>
> - **D-247-C012** — acd88512 commit changelog row for the rngGate L1174 backfill guard hunk (the source-of-truth diff Phase 251 tests against; the `if (day > idx + 1 && rngWordByDay[idx + 1] == 0)` conjunct that landed in this commit).
> - **D-247-F011** — Section 2 classification row for the rngGate L1174 backfill guard hunk (MODIFIED_LOGIC — confirms the change is behavioral, not stylistic; the test must observe the behavior delta).
> - **D-247-X030** — Section 3 caller catalog row (rngGate sole caller is `advanceGame` at AdvanceModule:292; Phase 251 test fixture invokes `rngGate` only via `advanceGame` per the 3-path multiplier in BFL-01 §1.1 — the simplest test invocation is a direct EOA call to `DegenerusGame.advanceGame()` per entry path D-247-X027).
