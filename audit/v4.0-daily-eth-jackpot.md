# v4.0 Daily ETH Jackpot Audit

**Date:** 2026-03-23
**Milestone:** v4.0 (Ticket Lifecycle & RNG-Dependent Variable Re-Audit)
**Phase:** 85 (Daily ETH Jackpot)
**Methodology:** Exhaustive code trace with file:line citations; all prior audit prose treated as unverified
**Requirements:** DETH-01, DETH-02, DETH-05

---

## 1. Call Sites in advanceGame (DETH-01 partial)

All calls into `payDailyJackpot` and `payDailyJackpotCoinAndTickets` originate from `DegenerusGameAdvanceModule.advanceGame`.

### 1.1 Call Site Table

| # | Call Site | Location | Gating Condition | isDaily | Level Arg | Context |
|---|----------|----------|-----------------|---------|-----------|---------|
| 1 | `payDailyJackpot(false, purchaseLevel, rngWord)` | AM:282 | `!inJackpot && !lastPurchaseDay` (purchase phase, not final day) | `false` | `purchaseLevel` | Early-burn path during purchase phase |
| 2 | `payDailyJackpot(true, lastDailyJackpotLevel, rngWord)` | AM:356 | Resume condition (see 1.2 below) | `true` | `lastDailyJackpotLevel` (stored) | Jackpot phase resume from prior chunk |
| 3 | `payDailyJackpotCoinAndTickets(rngWord)` | AM:363 | `dailyJackpotCoinTicketsPending` | N/A | (reads `lastDailyJackpotLevel` internally at JM:693) | Phase 2: coin + ticket distribution |
| 4 | `payDailyJackpot(true, lvl, rngWord)` | AM:379 | No resume condition, no coin/ticket pending -- fresh start fallthrough | `true` | `lvl` (current level) | Jackpot phase fresh daily start |

### 1.2 Resume Detection Condition

The resume detection condition at AM:350-354 matches the `isResuming` condition inside `payDailyJackpot` at JM:333-336:

```solidity
// AM:350-354
if (
    dailyEthBucketCursor != 0 ||
    dailyEthPhase != 0 ||
    dailyEthPoolBudget != 0 ||
    dailyEthWinnerCursor != 0
) {
    payDailyJackpot(true, lastDailyJackpotLevel, rngWord);
    // AM:357
    stage = STAGE_JACKPOT_ETH_RESUME;
    break;
}
```

```solidity
// JM:333-336
bool isResuming = dailyEthPoolBudget != 0 ||
    dailyEthPhase != 0 ||
    dailyEthBucketCursor != 0 ||
    dailyEthWinnerCursor != 0;
```

The conditions check the same four storage variables (order differs but semantics identical). Both detect ANY non-zero state from prior chunk processing.

**Storage variables checked:**
| Variable | Slot | Type | Purpose |
|----------|------|------|---------|
| `dailyEthBucketCursor` | Slot 0, offset 30:31 | `uint8` | Bucket order index (0..3) within current phase | GS:282 |
| `dailyEthPhase` | Slot 1, offset 0:1 | `uint8` | 0 = Phase 0 (current level), 1 = Phase 1 (carryover) | GS:293 |
| `dailyEthPoolBudget` | Slot 8 | `uint256` | Stored ETH budget for current phase | GS:383 |
| `dailyEthWinnerCursor` | Slot 17 | `uint16` | Winner index within current bucket | GS:485 |

### 1.3 Advance Flow Ordering

The jackpot phase branch in `advanceGame` executes in this priority order (AM:345-381):

1. **Resume check** (AM:350-354): If any daily ETH state is non-zero, resume via Call Site #2
2. **Coin+ticket pending** (AM:362): If `dailyJackpotCoinTicketsPending`, execute Phase 2 via Call Site #3
3. **Fresh start** (AM:379): Otherwise, start fresh daily jackpot via Call Site #4

The purchase phase branch always executes Call Site #1 on non-final purchase days (AM:279-282).

Additionally, before any jackpot processing, the advance module checks at AM:255-261 whether fresh daily tickets need processing:
```solidity
// AM:255-261
if (
    !dailyJackpotCoinTicketsPending &&
    dailyEthPoolBudget == 0 &&
    dailyEthPhase == 0 &&
    dailyEthBucketCursor == 0 &&
    dailyEthWinnerCursor == 0
) {
    if (!_prepareFutureTickets(lvl)) { ... }
}
```
This ensures future ticket queues are processed ONLY when no daily ETH distribution is in progress.

---

## 2. Constants and Share Packing (DETH-01)

All constants from JM:104-224 relevant to the daily ETH jackpot:

### 2.1 Timing Constants

```solidity
// JM:105
uint48 private constant JACKPOT_RESET_TIME = 82620;
```
Daily jackpot reset boundary: 82620 seconds from midnight UTC = 22:57 UTC.

```solidity
// JM:108
uint8 private constant JACKPOT_LEVEL_CAP = 5;
```
Maximum number of daily jackpots per level before forcing level transition.

### 2.2 Share Packing Constants

**Final Day Shares (Day 5):**
```solidity
// JM:114-120
/// @dev Day-5 trait bucket shares packed into 64 bits: [6000, 1333, 1333, 1334] = 10000 bps.
///      With rotation, the 60% share is assigned to the solo (1-winner) bucket.
uint64 private constant FINAL_DAY_SHARES_PACKED =
    (uint64(6000)) |
        (uint64(1333) << 16) |
        (uint64(1333) << 32) |
        (uint64(1334) << 48);
```

Unpacked BPS per bucket: `[6000, 1333, 1333, 1334]` = 10000 BPS total. The 60% share goes to the solo (1-winner) bucket via entropy-based rotation.

**Daily Jackpot Shares (Days 1-4):**
```solidity
// JM:124-125
/// @dev Daily jackpot trait bucket shares: 2000 bps each x 4 = 8000 bps.
///      Remaining 20% is assigned to the entropy-selected solo bucket.
uint64 private constant DAILY_JACKPOT_SHARES_PACKED =
    uint64(2000) * 0x0001000100010001;
```

This packs `[2000, 2000, 2000, 2000]` = 8000 BPS. The remaining 2000 BPS (20%) goes to the solo bucket as remainder via `JackpotBucketLib.bucketShares`. Note: the solo bucket receives its base 2000 BPS share PLUS the 2000 BPS remainder = 4000 BPS (40%) of total.

### 2.3 Winner Cap Constants

```solidity
// JM:191
/// @dev Maximum total winners per jackpot payout (including solo bucket).
uint16 private constant JACKPOT_MAX_WINNERS = 300;
```
Used by the early-burn (purchase phase) path via `_executeJackpot` -> `_runJackpotEthFlow` (JM:1339).

```solidity
// JM:193
/// @dev Maximum total ETH winners across daily + carryover jackpots.
uint16 private constant DAILY_ETH_MAX_WINNERS = 321;
```
Used by Phase 0 and Phase 1 daily distribution via `_processDailyEthChunk` (JM:495, JM:577).

```solidity
// JM:197
/// @dev Minimum carryover winners when carryover is active.
uint16 private constant DAILY_CARRYOVER_MIN_WINNERS = 20;
```
Floor for Phase 1 carryover winner cap (JM:537-538).

### 2.4 BPS Constants

```solidity
// JM:176
/// @dev Portion of reward-pool-funded daily jackpot ETH converted to loot boxes (50%).
uint16 private constant DAILY_REWARD_JACKPOT_LOOTBOX_BPS = 5000;
```
Used for carryover lootbox split at JM:434.

```solidity
// JM:179
/// @dev Portion of purchase-phase reward-pool jackpots converted to loot boxes (3/4).
uint16 private constant PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS = 7500;
```
Used for early-burn lootbox split at JM:640.

```solidity
// JM:220
/// @dev Maximum scale for bucket sizing (4x at 200 ETH+).
uint16 private constant JACKPOT_SCALE_MAX_BPS = 40_000;
```
Used for early-burn path bucket scaling via `_runJackpotEthFlow` (JM:1340).

```solidity
// JM:223
/// @dev Daily jackpot max scale (6.6667x) to allow up to DAILY_ETH_MAX_WINNERS.
uint32 private constant DAILY_JACKPOT_SCALE_MAX_BPS = 66_667;
```
Used for daily path Phase 0 (JM:496) and Phase 1 (JM:578) bucket scaling.

### 2.5 BPS Range Constants

```solidity
// JM:146-147
uint16 private constant DAILY_CURRENT_BPS_MIN = 600;
uint16 private constant DAILY_CURRENT_BPS_MAX = 1400;
```
Random daily BPS range for days 1-4: 600-1400 (6%-14%), average 10%.

### 2.6 Entropy Salt Constants

```solidity
// JM:135-136
bytes32 private constant DAILY_CURRENT_BPS_TAG = keccak256("daily-current-bps");
```
Domain separator for `_dailyCurrentPoolBps` (JM:2665).

```solidity
// JM:139-140
bytes32 private constant DAILY_CARRYOVER_SOURCE_TAG = keccak256("daily-carryover-source");
```
Domain separator for `_selectCarryoverSourceOffset` (JM:2726).

```solidity
// JM:143
uint8 private constant DAILY_CARRYOVER_MAX_OFFSET = 5;
```
Maximum forward offset for carryover source selection (levels lvl+1 through lvl+5).

### 2.7 Gas Budgeting Constants

```solidity
// JM:164
uint16 private constant DAILY_JACKPOT_UNITS_SAFE = 1000;
```
Unit budget for chunked daily ETH distribution (JM:477).

```solidity
// JM:167
uint8 private constant DAILY_JACKPOT_UNITS_AUTOREBUY = 3;
```
Cost multiplier per winner with auto-rebuy enabled (JM:1361).

---

## 3. BPS Allocation Table (DETH-01)

### 3.1 _dailyCurrentPoolBps Function

```solidity
// JM:2656-2668
function _dailyCurrentPoolBps(
    uint8 counter,
    uint256 randWord
) private pure returns (uint16 bps) {
    if (counter >= JACKPOT_LEVEL_CAP - 1) return 10_000;

    uint16 range = DAILY_CURRENT_BPS_MAX - DAILY_CURRENT_BPS_MIN + 1;
    uint256 seed = uint256(
        keccak256(
            abi.encodePacked(randWord, DAILY_CURRENT_BPS_TAG, counter)
        )
    );
    return uint16(DAILY_CURRENT_BPS_MIN + (seed % range));
}
```

- **Tag:** `DAILY_CURRENT_BPS_TAG = keccak256("daily-current-bps")` (JM:135-136)
- **Modulo range:** `DAILY_CURRENT_BPS_MAX - DAILY_CURRENT_BPS_MIN + 1 = 1400 - 600 + 1 = 801`
- **Output for counter < 4:** `600 + (seed % 801)` = [600, 1400] BPS (6%-14%)
- **Output for counter >= 4:** `10000` BPS (100%) -- the `JACKPOT_LEVEL_CAP - 1 = 4` check at JM:2660

Note: The day 5 override is also enforced at the caller level in `payDailyJackpot`:
```solidity
// JM:362-368
bool isFinalPhysicalDay = (counter + counterStep >= JACKPOT_LEVEL_CAP);
// ...
if (isFinalPhysicalDay) {
    dailyBps = 10_000; // Final physical day: 100% of remaining pool
} else {
    dailyBps = _dailyCurrentPoolBps(counter, randWord);
```
So `_dailyCurrentPoolBps` is only called for non-final days. The function's own `counter >= 4` guard is a defense-in-depth redundancy.

### 3.2 Compressed Mode (JM:351-374)

```solidity
// JM:353-361
if (compressedJackpotFlag == 2 && counter == 0) {
    counterStep = JACKPOT_LEVEL_CAP;  // = 5
} else if (
    compressedJackpotFlag == 1 &&
    counter > 0 &&
    counter < JACKPOT_LEVEL_CAP - 1
) {
    counterStep = 2;
}
```

- **Normal mode (flag=0):** `counterStep = 1`, five physical days
- **Compressed mode (flag=1):** `counterStep = 2` on days 2-4 (when `counter > 0 && counter < 4`). Day 1 still has counterStep=1. This collapses 5 logical days into 3 physical days.
- **Turbo mode (flag=2):** `counterStep = 5` on day 1 only (when `counter == 0`). All 5 logical days in 1 physical day.

BPS doubling for compressed mode:
```solidity
// JM:372-374
if (counterStep == 2) {
    dailyBps *= 2;
}
```
When `counterStep == 2`, the random BPS is doubled: e.g., if `_dailyCurrentPoolBps` returns 1000 BPS, the actual dailyBps becomes 2000 BPS. This combines two days' payouts into one physical day.

### 3.3 Counter Step Packing

The counter step is packed into `dailyTicketBudgetsPacked` at JM:459-464 via `_packDailyTicketBudgets`:
```solidity
// JM:2753-2763
function _packDailyTicketBudgets(
    uint8 counterStep,
    uint256 dailyTicketUnits,
    uint256 carryoverTicketUnits,
    uint8 carryoverSourceOffset
) private pure returns (uint256) {
    return
        uint256(counterStep) |
        (dailyTicketUnits << 8) |
        (carryoverTicketUnits << 72) |
        (uint256(carryoverSourceOffset) << 136);
}
```

Layout: `[counterStep (8 bits @ 0)] [dailyTicketUnits (64 bits @ 8)] [carryoverTicketUnits (64 bits @ 72)] [carryoverSourceOffset (8 bits @ 136)]`

### 3.4 Allocation Table

| Day | Counter | Normal Mode (flag=0) | Compressed Mode (flag=1) | Turbo Mode (flag=2) | Share Packing | Source |
|-----|---------|---------------------|-------------------------|---------------------|---------------|--------|
| Day 1 | 0 | 6-14% (600-1400 BPS), step=1 | 6-14%, step=1 (counter=0, not > 0) | 100% (10000 BPS), step=5 (isFinalPhysicalDay=true) | DAILY_JACKPOT (turbo: FINAL_DAY) | JM:370, JM:353-354, JM:362-368 |
| Day 2 | 1 | 6-14%, step=1 | 12-28% (doubled), step=2 | N/A (all done in day 1) | DAILY_JACKPOT | JM:370, JM:356-360, JM:372-374 |
| Day 3 | 2 | 6-14%, step=1 | 12-28% (doubled), step=2 | N/A | DAILY_JACKPOT | JM:370, JM:356-360, JM:372-374 |
| Day 4 | 3 | 6-14%, step=1 | 100% (isFinalPhysicalDay), step=2 | N/A | FINAL_DAY | JM:370, JM:362-368 |
| Day 5 | 4 | 100% (10000 BPS), step=1 | N/A (already done) | N/A | FINAL_DAY | JM:367-368 |

**Notes on compressed day 3 (counter=3):** With counter=3 and counterStep=2, `counter + counterStep = 5 >= JACKPOT_LEVEL_CAP`, so `isFinalPhysicalDay = true` and `dailyBps = 10000`. This is the final physical day in compressed mode, using FINAL_DAY_SHARES_PACKED.

**Notes on compressed day 1:** At counter=0, `compressedJackpotFlag == 1 && counter > 0` is false, so counterStep remains 1. Compressed mode only doubles on days 2 and 3.

**Notes on turbo:** At counter=0, `compressedJackpotFlag == 2 && counter == 0` sets counterStep=5. Then `counter + counterStep = 5 >= 5`, so `isFinalPhysicalDay = true` and `dailyBps = 10000`. All five logical days happen in one physical day with 100% of pool.

---

## 4. Budget Split Logic (DETH-01)

### 4.1 Fresh-Start Budget Calculation (JM:365-405)

```solidity
// JM:365-376
uint256 poolSnapshot = currentPrizePool;
uint16 dailyBps;
if (isFinalPhysicalDay) {
    dailyBps = 10_000; // Final physical day: 100% of remaining pool
} else {
    dailyBps = _dailyCurrentPoolBps(counter, randWord);
    // Double BPS on compressed days to combine two days' payouts.
    if (counterStep == 2) {
        dailyBps *= 2;
    }
}
uint256 budget = (poolSnapshot * dailyBps) / 10_000;
```

Step 1: `poolSnapshot = currentPrizePool` (JM:365) -- snapshot before any deductions.

Step 2: `dailyBps` determined by day/mode (JM:367-374).

Step 3: `budget = (poolSnapshot * dailyBps) / 10_000` (JM:376).

### 4.2 Early-Bird Lootbox on Day 1 (JM:378-382)

```solidity
// JM:380-382
if (isEarlyBirdDay) {
    _runEarlyBirdLootboxJackpot(lvl + 1, randWord);
}
```

On day 1 only (`counter == 0`), runs `_runEarlyBirdLootboxJackpot` (JM:801-864):
- Takes 3% from `futurePrizePool`: `reserveContribution = (_getFuturePrizePool() * 300) / 10_000` (JM:803)
- Deducts immediately from `futurePrizePool` (JM:807)
- Distributes to 100 winners as ticket queue entries across levels lvl+0..lvl+4 (JM:829-859)
- Entire budget goes to `nextPrizePool` (JM:863) -- this increases the backing for the tickets distributed
- **This replaces the normal carryover flow on day 1** -- the `isEarlyBirdDay` guard at JM:409 prevents carryover calculation on day 1

### 4.3 Lootbox/ETH Split (JM:384-405)

```solidity
// JM:384-392
uint256 dailyLootboxBudget = _validateTicketBudget(
    budget / 5,                    // 20% = 1/5
    lvl,
    winningTraitsPacked
);
if (dailyLootboxBudget != 0) {
    budget -= dailyLootboxBudget;
}

dailyEthPoolBudget = budget;      // JM:394
```

- `dailyLootboxBudget = budget / 5` = 20% of total budget (JM:386)
- `_validateTicketBudget` (JM:1053-1060) returns 0 if no trait tickets exist at `lvl` for the winning traits, otherwise returns the budget unchanged
- `budget -= dailyLootboxBudget` => `ethBudget = budget * 4/5` = 80% (JM:391)
- Stored as `dailyEthPoolBudget` (JM:394)

```solidity
// JM:397-405
uint256 dailyTicketUnits = _budgetToTicketUnits(dailyLootboxBudget, lvl + 1);
if (dailyTicketUnits != 0) {
    // Deduct from current pool and add to next pool to back tickets
    currentPrizePool -= dailyLootboxBudget;     // JM:403
    _setNextPrizePool(_getNextPrizePool() + dailyLootboxBudget);   // JM:404
}
```

The lootbox budget is moved from `currentPrizePool` to `nextPrizePool` to back the tickets. Tickets are distributed later in Phase 2 via `payDailyJackpotCoinAndTickets`.

### 4.4 Carryover Budget (JM:407-467)

Only runs on days 2-5 (when `!isEarlyBirdDay` at JM:409):

```solidity
// JM:423-431
// 1% = 1/100
uint256 reserveSlice;
if (!isEarlyBirdDay && initCarryoverSourceOffset != 0) {
    reserveSlice = _getFuturePrizePool() / 100;
    // Deduct immediately (upfront model)
    _setFuturePrizePool(_getFuturePrizePool() - reserveSlice);
}
```

- `reserveSlice = futurePrizePool / 100` (1% flat drip) (JM:427)
- Deducted immediately from `futurePrizePool` (upfront model) (JM:430)
- Only calculated when `initCarryoverSourceOffset != 0` (eligible carryover source exists)

Carryover lootbox split:
```solidity
// JM:433-448
uint256 futureEthPool = reserveSlice;
uint16 carryoverLootboxBps = DAILY_REWARD_JACKPOT_LOOTBOX_BPS;  // 5000 = 50%

uint256 carryoverLootboxBudget = _validateTicketBudget(
    (futureEthPool * carryoverLootboxBps) / 10_000,
    carryoverSourceLevel,
    winningTraitsPacked
);
if (carryoverLootboxBudget != 0) {
    futureEthPool -= carryoverLootboxBudget;
    _setNextPrizePool(_getNextPrizePool() + carryoverLootboxBudget);   // JM:446
}
```

- 50% of carryover goes to lootbox tickets (JM:434, JM:438)
- 50% remains as ETH pool for Phase 1 distribution
- Stored in `dailyCarryoverEthPool` (JM:467)

### 4.5 _distributeLootboxAndTickets (JM:1079-1102)

The lootbox budget distribution (for the early-burn path) works as:
```solidity
// JM:1086-1101
_setNextPrizePool(_getNextPrizePool() + lootboxBudget);       // Budget to nextPrizePool

uint256 ticketBasis = (lootboxBudget * ticketConversionBps) / 10_000;
uint256 ticketUnits = _budgetToTicketUnits(ticketBasis, lvl + 1);
if (ticketUnits != 0) {
    _distributeTicketJackpot(...);   // Queue ticket entries to winners
}
```

It adds the full lootbox budget to `nextPrizePool` as backing, then distributes a fraction as actual tickets based on `ticketConversionBps`. For the early-burn path (JM:663), `ticketConversionBps = 5000` (50%).

### 4.6 Budget Flow Diagram

```
currentPrizePool (snapshot at JM:365)
  |
  |-- dailyBps% --> budget (JM:376)
  |     |
  |     |-- 20% (budget/5) --> dailyLootboxBudget (JM:386)
  |     |     |-- currentPrizePool -= lootboxBudget (JM:403)
  |     |     |-- nextPrizePool += lootboxBudget (JM:404)
  |     |     +-- Tickets distributed in Phase 2 (payDailyJackpotCoinAndTickets)
  |     |
  |     |-- 80% (budget - lootboxBudget) --> dailyEthPoolBudget (JM:394)
  |     |     +-- Phase 0: _processDailyEthChunk -> claimableWinnings[winners]
  |     |     +-- currentPrizePool -= paidDailyEth (JM:522)
  |     |
  |     +-- (retained) --> next day's currentPrizePool
  |
  (Day 1 only): futurePrizePool * 3% --> _runEarlyBirdLootboxJackpot (JM:803)
  |                                       --> nextPrizePool += totalBudget (JM:863)
  |                                       --> ticket queue entries to winners (JM:848)
  |
  (Days 2-5): futurePrizePool / 100 --> reserveSlice (JM:427)
        |-- 50% --> carryoverLootboxBudget --> nextPrizePool (JM:446)
        |                                      --> tickets in Phase 2
        |-- 50% --> futureEthPool --> dailyCarryoverEthPool (JM:467)
                                      --> Phase 1: _processDailyEthChunk -> claimableWinnings
```

---

## 5. Cross-Reference for Sections 1-4 (DETH-05 partial)

### 5.1 v3.2 Findings Cross-Reference

**CMT-V32-001: _resolveTraitWinners @return ticketSpent NatSpec**

Prior finding states the `@return ticketSpent` NatSpec at JM:1605 describes "auto-rebuy tickets" when the actual return is whale pass conversion ETH going to `futurePrizePool` only.

Current code at JM:1605:
```solidity
/// @return ticketSpent ETH converted into auto-rebuy tickets (added to next/futurePool).
```

**Status: CONFIRMED** -- the finding remains unresolved. The NatSpec at JM:1605 still says "auto-rebuy tickets" and "next/futurePool" when the actual return tracks whale pass ETH going exclusively to `futurePrizePool`. This is an existing INFO finding (not a new discovery in this phase). The inaccuracy affects understanding of `ticketSpent` in `_processOneBucket` (JM:1580) where it is added to `ctx.totalPaidEth`.

**CMT-V32-002: Inline comment says "BURNIE only, no ETH bonuses"**

Prior finding flagged the inline comment at JM:609 as inaccurate.

Current code at JM:609:
```solidity
// Non-daily (early-burn) path - BURNIE and ETH bonuses on non-day-1 levels
```

**Status: [DISCREPANCY] RESOLVED** -- the v3.2 finding is no longer applicable. The inline comment has been updated to accurately describe both BURNIE and ETH distribution. The research document (85-RESEARCH.md) noted this was updated, and code confirms: the current text "BURNIE and ETH bonuses on non-day-1 levels" correctly reflects the early-burn path behavior (ETH from future pool drip on days after day 1, BURNIE always).

### 5.2 v3.8 Commitment Window Inventory Cross-Reference

Section 1.7 lists storage variables R/W by `payDailyJackpot`. Verification against current code:

| Variable | v3.8 Claim (Slot, R/W) | Current Code Verification | Status |
|----------|------------------------|--------------------------|--------|
| `currentPrizePool` | Slot 2, R/W | Read at JM:365 (poolSnapshot), Write at JM:403 (lootbox deduction), JM:522 (daily payout deduction) | CONFIRMED |
| `jackpotCounter` | Slot 0 offset 22, R | Read at JM:349 (`counter = jackpotCounter`). Written only in `payDailyJackpotCoinAndTickets` at JM:757 | CONFIRMED -- read-only in payDailyJackpot |
| `compressedJackpotFlag` | Slot 1 offset 0, R | Read at JM:353, JM:356. Never written in payDailyJackpot | CONFIRMED -- read-only |
| `dailyEthPoolBudget` | Slot 8, R/W | Written at JM:394, Read at JM:333 (isResuming check), JM:489 | CONFIRMED |
| `dailyEthPhase` | v3.8 says Slot 0 offset 31, R/W | **[DISCREPANCY]**: Actual location is **Slot 1, offset 0:1** per GS:58, GS:293. Slot 0 has 31 bytes used with 1 byte padding at offset 31:32. dailyEthPhase is the first variable in Slot 1. R/W confirmed: read at JM:335/488/560, written at JM:469/553/2786 | DISCREPANCY -- v3.8 slot offset wrong |
| `dailyEthBucketCursor` | Slot 0 offset 30, R/W | Read at JM:336/1413, Written at JM:470/554/1470/2787. GS:50 confirms offset 30:31 in Slot 0 | CONFIRMED |
| `dailyEthWinnerCursor` | Slot 17 offset 7, R/W | v3.8 claims offset 7. GS:485 declares `uint16 internal dailyEthWinnerCursor` -- need to verify exact offset from storage layout. Read at JM:337/1414, Written at JM:471/555/1471/2788 | CONFIRMED R/W pattern (offset needs storage deep-dive in Phase 88) |
| `lastDailyJackpotWinningTraits` | Slot 42 offset 0, R/W | Read at JM:340, Written at JM:2632 via `_syncDailyWinningTraits` | CONFIRMED |
| `lastDailyJackpotLevel` | Slot 42 offset 4, R/W | Read at JM:341, Written at JM:2633 via `_syncDailyWinningTraits` | CONFIRMED |
| `dailyTicketBudgetsPacked` | Slot 7, R/W | Written at JM:459, Read at JM:483 via `_unpackDailyTicketBudgets` | CONFIRMED |
| `dailyCarryoverEthPool` | Slot 18, W | Written at JM:467, Read at JM:547/561, Cleared at JM:2790 | **[DISCREPANCY]**: v3.8 says W-only, but it is also READ at JM:547 (skip check) and JM:561 (Phase 1 entry). Should be R/W |
| `dailyCarryoverWinnerCap` | Slot 19, W | Written at JM:531/538/542, Read at JM:547/562, Cleared at JM:2791 | **[DISCREPANCY]**: v3.8 says W-only, but it is also READ at JM:547 (skip check) and JM:562. Should be R/W |
| `dailyJackpotCoinTicketsPending` | Slot 0 offset 29, W | Written at JM:2792 via `_clearDailyEthState`. Also read in the AdvanceModule at AM:362 | CONFIRMED W in payDailyJackpot scope |
| `prizePoolsPacked` | Slot 3, R/W | Read via `_getFuturePrizePool()` at JM:427/803/807, Written via `_setFuturePrizePool()` at JM:430/807 and `_setNextPrizePool()` at JM:404/446/863/1087 | CONFIRMED |
| `claimableWinnings[winner]` | Slot 9 (mapping), W | Written via `_addClaimableEth` at JM:1479 | CONFIRMED |
| `claimablePool` | Slot 10, R/W | Written at JM:1473/1504-1505 | CONFIRMED |
| `traitBurnTicket[level]` | Slot 11 (mapping), R | Read at JM:1448, JM:833, JM:2680 | CONFIRMED |
| `autoRebuyState[player]` | Slot 36 (mapping), R | Read via `_winnerUnits` at JM:1358 and `_addClaimableEth` | CONFIRMED |

**v3.8 Call site line numbers:** v3.8 header says "AdvanceModule:279/353/376" for the three call sites. Current code:
- AM:282 (purchase phase early-burn) -- v3.8 says 279 -- **[DISCREPANCY]** off by 3 lines (likely due to code additions above)
- AM:356 (resume) -- v3.8 says 353 -- **[DISCREPANCY]** off by 3 lines
- AM:379 (fresh) -- v3.8 says 376 -- **[DISCREPANCY]** off by 3 lines

These line number drifts are expected after code modifications (INFO severity).

### 5.3 PAYOUT-SPECIFICATION.html Cross-Reference

**PAY-01 (Purchase-Phase Daily Jackpot):**
- Source pool: `futurePrizePool` (1% daily drip) -- CONFIRMED at JM:629-630
- BPS: `poolBps = 100` (1%) -- CONFIRMED at JM:629
- Lootbox split: `PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS = 7500` (75%) -- CONFIRMED at JM:640
- Currency: "75% lootbox tickets, 25% ETH" -- CONFIRMED from the 7500 BPS lootbox split
- **[DISCREPANCY]** PAY-01 formula block cites "JackpotModule.sol:635-639" for the poolBps calculation; current code is at JM:628-633 -- line number drift (INFO)

**PAY-02 (Jackpot-Phase 5-Day Draws):**
- Source pool: `currentPrizePool` (6-14% days 1-4, 100% day 5) -- CONFIRMED at JM:367-370
- **[DISCREPANCY]** PAY-02 claims share split is `JACKPOT_SHARES_PACKED = 60/13/13/13` with values "6000/1300/1300/1300/100 BPS". The actual code has TWO share packings:
  - Days 1-4: `DAILY_JACKPOT_SHARES_PACKED = [2000, 2000, 2000, 2000]` (20/20/20/20 with remainder to solo) -- JM:124-125
  - Day 5: `FINAL_DAY_SHARES_PACKED = [6000, 1333, 1333, 1334]` (60/13.33/13.33/13.34) -- JM:114-120

  PAY-02 presents only the day 5 shares as though they apply to all days, which is inaccurate. The BPS values are also wrong: PAY-02 says 1300/1300/1300/100, but actual values are 1333/1333/1334 (no separate 1% remainder entry -- the 1334 bucket absorbs the extra BPS). **Severity: INFO** -- the PAY-02 card simplifies/conflates the two share packings.
- **[DISCREPANCY]** PAY-02 describes "4 winners per draw" which is misleading -- there are 4 trait BUCKETS with up to `DAILY_ETH_MAX_WINNERS (321)` total winners across all buckets, not 4 winners total. **Severity: INFO** -- PAY-02 appears to describe a simplified/outdated model.
- **[DISCREPANCY]** PAY-02 code reference says "JackpotModule.sol:336-613" and "JackpotModule.sol:2723-2736". Current: payDailyJackpot at JM:323-667, _dailyCurrentPoolBps at JM:2656-2668 -- line number drift and range expansion (INFO)

### 5.4 Findings Summary for Sections 1-4

| # | Tag | Severity | Description | Source |
|---|-----|----------|-------------|--------|
| 1 | CONFIRMED | INFO | CMT-V32-001 still unresolved: _resolveTraitWinners @return ticketSpent NatSpec inaccurate | JM:1605 |
| 2 | [DISCREPANCY] RESOLVED | INFO | CMT-V32-002 fixed: inline comment at JM:609 updated to "BURNIE and ETH bonuses" | JM:609 |
| 3 | [DISCREPANCY] | INFO | v3.8 claims dailyEthPhase at Slot 0 offset 31 -- actual is Slot 1 offset 0 | GS:58, GS:293 |
| 4 | [DISCREPANCY] | INFO | v3.8 claims dailyCarryoverEthPool is W-only -- it is R/W (read at JM:547, JM:561) | JM:547, JM:561 |
| 5 | [DISCREPANCY] | INFO | v3.8 claims dailyCarryoverWinnerCap is W-only -- it is R/W (read at JM:547, JM:562) | JM:547, JM:562 |
| 6 | [DISCREPANCY] | INFO | v3.8 call site line numbers drifted by 3 (279->282, 353->356, 376->379) | AM:282, AM:356, AM:379 |
| 7 | [DISCREPANCY] | INFO | PAY-01 formula cites JM:635-639, actual is JM:628-633 -- line drift | JM:628-633 |
| 8 | [DISCREPANCY] | INFO | PAY-02 share split claims 60/13/13/13/1 for all days, actual: 20/20/20/20+remainder (days 1-4) and 60/13.33/13.33/13.34 (day 5) | JM:114-125 |
| 9 | [DISCREPANCY] | INFO | PAY-02 says "4 winners per draw" -- actually up to 321 winners across 4 buckets | JM:193, JM:495 |
| 10 | [DISCREPANCY] | INFO | PAY-02 line references drifted: 336-613 -> 323-667, 2723-2736 -> 2656-2668 | JM:323, JM:2656 |
