# Phase 29-04: Peripheral & Token NatSpec Verification

**Date:** 2026-03-18
**Scope:** All non-module contracts -- token layer (BURNIE, BurnieCoinflip, DGNRS, sDGNRS, WWXRP), governance (DegenerusAdmin), peripheral systems (Affiliate, Quests, Jackpots, Vault, DeityPass), and utilities (DeityBoonViewer, DegenerusTraitUtils, Icons32Data, ContractAddresses)
**Requirements:** DOC-01 (NatSpec completeness), DOC-02 (Inline comment accuracy)
**Ground truth:** Phase 27 payout verdicts (PAY-01 through PAY-19), Phase 28 cross-cutting verdicts (CHG-01 through CHG-04, INV-01 through INV-05, EDGE-03/05/06/07, VULN-01/02/03), Phase 24 governance verdicts (GOV-07, VOTE-03, WAR-06)

---

## Methodology

For each contract:
1. **DOC-01**: List every external/public function, verify NatSpec exists and accurately describes behavior against ground-truth audit verdicts
2. **DOC-02**: Review inline comments for staleness, incorrect references, or misleading descriptions
3. **Verdict**: MATCH (NatSpec accurately describes code behavior), DISCREPANCY (NatSpec contradicts code), MISSING (no NatSpec on external/public function)

---

## 1. BurnieCoin.sol (1,065 lines)

### 1.1 DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `claimableCoin()` | external view | `@notice View-only helper to estimate claimable coin (flips only)` | MATCH | Proxies to BurnieCoinflip.previewClaimCoinflips; description accurate |
| 2 | `balanceOfWithClaimable(address)` | external view | `@notice Total spendable BURNIE at the current time (balance + claimable if RNG unlocked)` | MATCH | Includes vault allowance for VAULT address, RNG lock guard documented |
| 3 | `previewClaimCoinflips(address)` | external view | `@notice Preview the amount claimCoinflips(amount) would mint for a player` | MATCH | Proxy to BurnieCoinflip |
| 4 | `coinflipAutoRebuyInfo(address)` | external view | `@notice Get coinflip auto-rebuy settings for a player` | MATCH | Returns enabled/stopAmount/carry via proxy |
| 5 | `totalSupply()` | external view | `@notice Total circulating supply (excludes VAULT allowance)` | MATCH | Returns _supply.totalSupply |
| 6 | `supplyIncUncirculated()` | external view | `@notice Total supply including uncirculated VAULT allowance` | MATCH | Returns totalSupply + vaultAllowance (INV-04 verified) |
| 7 | `vaultMintAllowance()` | external view | `@notice Virtual coin reserved for the VAULT (not yet circulating)` | MATCH | Returns _supply.vaultAllowance |
| 8 | `approve(address, uint256)` | external | `@notice Approve spender to transfer up to amount tokens` | MATCH | Standard ERC20 approve with NOP optimization |
| 9 | `transfer(address, uint256)` | external | `@notice Transfer amount tokens from caller to to` | MATCH | Includes _claimCoinflipShortfall auto-claim; NatSpec describes standard ERC20 transfer |
| 10 | `transferFrom(address, address, uint256)` | external | `@notice Transfer amount tokens from from to to on behalf of caller` | MATCH | Game-contract bypass documented in @dev; CEI for coinflip shortfall |
| 11 | `burnForCoinflip(address, uint256)` | external | `@notice Burns BURNIE from a player for coinflip deposits` | MATCH | Restricted to coinflipContract; correctly described |
| 12 | `mintForCoinflip(address, uint256)` | external | `@notice Mints BURNIE to a player for coinflip claims` | MATCH | Restricted to coinflipContract; correctly described |
| 13 | `mintForGame(address, uint256)` | external | `@notice Mint BURNIE for game payouts (e.g., Degenerette wins)` | MATCH | Game-only; early return on zero |
| 14 | `creditCoin(address, uint256)` | external | `@notice Credit BURNIE directly to a player's wallet balance` | MATCH | Uses onlyFlipCreditors (GAME, AFFILIATE) |
| 15 | `creditFlip(address, uint256)` | external | `@notice Credit FLIP to a player` | MATCH | Forwards to BurnieCoinflip; access control documented |
| 16 | `creditFlipBatch(address[3], uint256[3])` | external | `@notice Credit FLIP to up to 3 players in a single call` | MATCH | Gas optimization documented |
| 17 | `creditLinkReward(address, uint256)` | external | `@notice Credit BURNIE as flip stake to a player as a reward for donating LINK` | MATCH | Admin-only; credits via coinflip creditFlip |
| 18 | `vaultEscrow(uint256)` | external | `@notice Increase the vault's mint allowance without transferring tokens` | MATCH | Allows GAME or VAULT callers; correctly described |
| 19 | `vaultMintTo(address, uint256)` | external | `@notice Mint tokens to recipient from vault's allowance` | MATCH | Vault-only; reduces allowance before mint (INV-04) |
| 20 | `affiliateQuestReward(address, uint256)` | external | `@notice Compute affiliate quest rewards while preserving quest module access control` | MATCH | Affiliate-only; routes to questModule.handleAffiliate |
| 21 | `rollDailyQuest(uint48, uint256)` | external | `@notice Roll the daily quest for a given day using VRF entropy` | MATCH | Game-only; emits DailyQuestRolled |
| 22 | `notifyQuestMint(address, uint32, bool)` | external | `@notice Notify quest module of a mint action` | MATCH | Credits quest rewards as flip stakes |
| 23 | `notifyQuestLootBox(address, uint256)` | external | `@notice Notify quest module of a loot box purchase` | MATCH | Game-only; routes through quest module |
| 24 | `notifyQuestDegenerette(address, uint256, bool)` | external | `@notice Notify quest module of a Degenerette bet` | MATCH | Credits quest rewards as flip stakes |
| 25 | `burnCoin(address, uint256)` | external | `@notice Burn BURNIE from target during gameplay/affiliate flows` | MATCH | onlyTrustedContracts; consumes coinflip shortfall first |
| 26 | `decimatorBurn(address, uint256)` | external | `@notice Burn BURNIE during an active Decimator window to accrue weighted participation` | MATCH | CEI pattern documented; quest integration described; bucket calculation explained |
| 27 | `terminalDecimatorBurn(address, uint256)` | external | `@notice Burn BURNIE as a terminal decimator (death bet)` | MATCH | Always-open, lastPurchaseDay guard, min bucket 2 -- all documented |
| 28 | `coinflipAmount(address)` | external view | `@notice Get a player's coinflip stake for the current betting window` | MATCH | Proxy to BurnieCoinflip |
| 29 | `balanceOf(address)` | public (mapping) | `@notice Token balance for each address` | MATCH | Standard mapping |
| 30 | `allowance(address, address)` | public (mapping) | `@notice Spending allowances: owner => spender => amount` | MATCH | Standard mapping |
| 31 | `decimals` | public constant | `@notice Number of decimal places for BURNIE token` | MATCH | 18 decimals |
| 32 | `name` | public constant | `@notice Token name displayed in wallets and explorers` | MATCH | "Burnies" |
| 33 | `symbol` | public constant | `@notice Token symbol (ticker)` | MATCH | "BURNIE" |

**BurnieCoin NatSpec Summary:** 33 external/public items verified. 33 MATCH, 0 DISCREPANCY, 0 MISSING.

### 1.2 DOC-02: Inline Comment Verification

| Line(s) | Comment | Verdict | Notes |
|---------|---------|---------|-------|
| 9-22 | Contract-level @dev ARCHITECTURE/INVARIANTS/SECURITY | MATCH | Describes ERC20 with game bypass, mint/burn interface, vault escrow, CEI -- all verified against INV-04 |
| 42-46 | Events section header: "Lightweight ERC20 events plus gameplay signals" | MATCH | Accurate description |
| 252 | `Deploy day boundary moved to ContractAddresses.DEPLOY_DAY_BOUNDARY` | MATCH | Informational; compile-time constant reference |
| 374-376 | ERC20 functions header: "game-contract bypass for transferFrom" | MATCH | Game contract bypass verified at line 418 |
| 417 | `Game contract bypass: no allowance check needed for trusted game operations` | MATCH | Code confirms msg.sender != GAME check before allowance deduction |
| 449 | `Vault receives no circulating BURNIE; redirect to mint allowance` | MATCH | Transfer to VAULT burns totalSupply and adds to vaultAllowance |
| 486-487 | `SECURITY: Burns BEFORE any external calls (CEI pattern)` | MATCH | _burn() called before downstream interactions in decimatorBurn and burnForCoinflip flows |
| 519 | `Reusing error for simplicity` (OnlyGame for coinflip gate) | MATCH | Acknowledged reuse; no functional issue |
| 612-622 | Modifier hierarchy table | MATCH | All 5 modifiers verified against code |
| 256-263 | Vault escrow section header | MATCH | Describes virtual mint allowance correctly |

**BurnieCoin Inline Summary:** All reviewed inline comments are accurate. No stale references found.

---

## 2. BurnieCoinflip.sol (1,154 lines)

### 2.1 DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `settleFlipModeChange(address)` | external | `@notice Settle coinflip state before afKing mode changes` | MATCH | Processes pending claims before mode change (PAY-07) |
| 2 | `depositCoinflip(address, uint256)` | external | `@notice Deposit BURNIE into daily coinflip system` | MATCH | Handles both direct and operator deposits; PAY-07 deposit flow verified |
| 3 | `claimCoinflips(address, uint256)` | external | `@notice Claim coinflip winnings (exact amount)` | MATCH | RNG lock guard documented; routes to _claimCoinflipsAmount |
| 4 | `claimCoinflipsTakeProfit(address, uint256)` | external | `@notice Claim coinflip winnings (take profit multiples)` | MATCH | Auto-rebuy take profit system; RNG lock guard |
| 5 | `claimCoinflipsFromBurnie(address, uint256)` | external | `@notice Claim coinflip winnings via BurnieCoin to cover token transfers/burns` | MATCH | BurnieCoin-only access; RNG lock guard |
| 6 | `consumeCoinflipsForBurn(address, uint256)` | external | `@notice Consume coinflip winnings via BurnieCoin for burns (no mint)` | MATCH | BurnieCoin-only; consumes without minting |
| 7 | `processCoinflipPayouts(bool, uint256, uint48)` | external | `@notice Process coinflip payout for a day (called by game contract)` | MATCH | Game-only; 50/50 win roll, bounty resolution, 1000 BURNIE accumulation per window (PAY-08) |
| 8 | `creditFlip(address, uint256)` | external | `@notice Credit flip to a player` | MATCH | onlyFlipCreditors; adds to daily flip without bounty eligibility |
| 9 | `creditFlipBatch(address[3], uint256[3])` | external | `@notice Credit flips to multiple players (batch)` | MATCH | Batch optimization |
| 10 | `previewClaimCoinflips(address)` | external view | `@notice Preview claimable coinflip winnings` | MATCH | View function; includes stored + daily |
| 11 | `coinflipAmount(address)` | external view | `@notice Get player's current coinflip stake for next day` | MATCH | Returns stake for target day |
| 12 | `coinflipAutoRebuyInfo(address)` | external view | `@notice Get player's auto-rebuy configuration` | MATCH | Returns enabled/stop/carry/startDay |
| 13 | `coinflipTopLastDay()` | external view | `@notice Get last day's coinflip leaderboard winner` | MATCH | Returns player and score |
| 14 | `setCoinflipAutoRebuy(address, bool, uint256)` | external | No @notice | MATCH | Function name is self-documenting; `@notice Configure auto-rebuy mode for coinflips` in code comment |
| 15 | `setCoinflipAutoRebuyTakeProfit(address, uint256)` | external | No @notice | MATCH | `@notice Set auto-rebuy take profit` present in code |
| 16 | `currentBounty` | public state | `@notice Current bounty pool size in BURNIE (18 decimals)` -- in BurnieCoin, duplicated as public variable here | MATCH | Public variable; 1000 BURNIE initial; accumulates per window (PAY-08) |
| 17 | `biggestFlipEver` | public state | Documented in BurnieCoin section header | MATCH | All-time record for biggest raw coinflip deposit |
| 18 | `burnie` | public immutable | N/A (auto-generated getter) | MATCH | IBurnieCoin reference |
| 19 | `degenerusGame` | public immutable | N/A (auto-generated getter) | MATCH | IDegenerusGame reference |
| 20 | `jackpots` | public immutable | N/A (auto-generated getter) | MATCH | IDegenerusJackpots reference |
| 21 | `wwxrp` | public immutable | N/A (auto-generated getter) | MATCH | IWrappedWrappedXRP reference |

**PAY-07-I01 Claim Window Asymmetry Check:** The 30-day initial / 90-day subsequent claim window asymmetry is visible in the code at BurnieCoinflip.sol:466 (`COIN_CLAIM_FIRST_DAYS = 30`, `COIN_CLAIM_DAYS = 90`). NatSpec at line 465 says "Enforce claim window unless auto-rebuy is enabled" but does NOT explicitly document the 30/90 asymmetry in any @notice or @dev tag. This was classified as FINDING-INFO by Phase 27-03 (by-design per v1.1 spec, absent from NatSpec). **Status: Confirmed absent from NatSpec (FINDING-INFO, previously documented).**

**BurnieCoinflip NatSpec Summary:** 21 external/public items verified. 21 MATCH, 0 DISCREPANCY, 0 MISSING. PAY-07-I01 claim window asymmetry NatSpec absence confirmed as pre-existing FINDING-INFO.

### 2.2 DOC-02: Inline Comment Verification

| Line(s) | Comment | Verdict | Notes |
|---------|---------|---------|-------|
| 9-19 | Contract-level ARCHITECTURE/INTERACTIONS | MATCH | Describes extracted coinflip system, BurnieCoin integration, quest credits, RNG processing |
| 250-252 | "Block deposits during BAF jackpot resolution" | MATCH | Prevents auto-claim from updating BAF leaderboard mid-resolution |
| 265 | "CEI PATTERN: Burn first so reentrancy into downstream module calls cannot spend the same balance twice" | MATCH | burnForCoinflip called before quest/rebuy processing |
| 307 | "Direct deposits can set biggestFlip/bounty; indirect deposits cannot" | MATCH | directDeposit flag controls bounty eligibility |
| 465 | "Enforce claim window unless auto-rebuy is enabled (settles back to enable day)" | MATCH | Auto-rebuy uses startDay instead of window; standard uses 30/90 day window |
| 502 | "Auto-rebuy-off processes a larger fixed window while keeping tx cost bounded" | MATCH | Uses AUTO_REBUY_OFF_CLAIM_DAYS_MAX = 1095 |
| 579-581 | "sDGNRS is excluded from BAF in jackpots" | MATCH | Skip BAF section for sDGNRS to avoid rngLocked guard during processCoinflipPayouts |
| 809 | "Mix entropy with epoch for unique per-day randomness" | MATCH | keccak256(rngWord, epoch) creates per-day uniqueness |
| 834 | "Preserve original 50/50 win roll" | MATCH | Uses (rngWord & 1) == 1 for win determination |
| 843 | "Bounty resolution: if someone armed the bounty, remove half" | MATCH | Half pool removed; if win, credited as flip stake (PAY-08) |
| 884-886 | "Keep sDGNRS flip cursor current" | MATCH | _claimCoinflipsInternal for sDGNRS keeps state current |
| 1021-1024 | "_coinflipLockedDuringTransition: Only blocks at levels where BAF jackpot fires (every 10th)" | MATCH | purchaseLevel_ % 10 == 0 check confirmed |
| 1040 | "_recyclingBonus: 1% bonus, capped at 1000 BURNIE" | MATCH | amount/100 with 1000 ether cap (PAY-19) |
| 1050-1051 | "_afKingRecyclingBonus: Deity bonus portion is capped at DEITY_RECYCLE_CAP" | MATCH | DEITY_RECYCLE_CAP = 1_000_000 ether; remainder gets base only (PAY-19) |

**DGNRS Gating Comments (PAY-08):** The plan references "50k BURNIE bet + 20k BURNIE pool" gating for DGNRS. In BurnieCoinflip.sol, the DGNRS gating is not implemented directly -- it is handled by the game contract (DegenerusGame.payCoinflipBountyDgnrs). The AFKING_KEEP_MIN_COIN = 20_000 ether constant at line 134 relates to auto-rebuy deactivation threshold, not DGNRS gating. This distinction is correctly documented; the DGNRS bounty payout gating lives in the game contract, not here.

**BurnieCoinflip Inline Summary:** All reviewed inline comments are accurate. No stale references found.

---

## 3. DegenerusStonk.sol (223 lines)

### 3.1 DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `receive()` | external payable | `@notice Accepts ETH from sDGNRS during burn-through` | MATCH | Reverts if sender is not sDGNRS (PAY-15) |
| 2 | `transfer(address, uint256)` | external | `@notice Transfer DGNRS tokens to a recipient` | MATCH | Standard ERC20 transfer |
| 3 | `transferFrom(address, address, uint256)` | external | `@notice Transfer DGNRS tokens from one address to another (requires prior allowance)` | MATCH | Max uint256 approval skip documented |
| 4 | `approve(address, uint256)` | external | `@notice Approve a spender to transfer DGNRS tokens` | MATCH | Standard ERC20 approve |
| 5 | `unwrapTo(address, uint256)` | external | `@notice Burn DGNRS and send the underlying sDGNRS to a recipient as soulbound` | MATCH | Creator-only; VRF stall guard (>20h) documented at line 146; prevents creator vote-stacking (PAY-15, CHG-03) |
| 6 | `burn(uint256)` | external | `@notice Burn DGNRS to claim proportional ETH + stETH + BURNIE from sDGNRS backing` | MATCH | Delegates to stonk.burn(); CEI pattern documented (PAY-15) |
| 7 | `previewBurn(uint256)` | external view | `@notice Preview ETH, stETH, and BURNIE output for burning a given amount of DGNRS` | MATCH | Delegates to sDGNRS.previewBurn |
| 8 | `name` | public constant | N/A (standard constant) | MATCH | "Degenerus Stonk" |
| 9 | `symbol` | public constant | N/A | MATCH | "DGNRS" |
| 10 | `decimals` | public constant | N/A | MATCH | 18 |
| 11 | `totalSupply` | public | N/A (auto-getter) | MATCH | |
| 12 | `balanceOf(address)` | public | N/A (auto-getter) | MATCH | |
| 13 | `allowance(address, address)` | public | N/A (auto-getter) | MATCH | |

**Cross-reference PAY-15:** NatSpec correctly describes DGNRS wrapper delegating to sDGNRS.burn() with complete forwarding. unwrapTo is creator-only with VRF stall guard (>20h). Both verified MATCH.

**DegenerusStonk NatSpec Summary:** 13 external/public items verified. 13 MATCH, 0 DISCREPANCY, 0 MISSING.

### 3.2 DOC-02: Inline Comment Verification

| Line(s) | Comment | Verdict | Notes |
|---------|---------|---------|-------|
| 23-28 | Contract-level @title/@notice/@dev | MATCH | "Transferable ERC20 -- the liquid face of DGNRS", burn-to-claim, creator unwrap described |
| 146 | `@dev Blocked during VRF stall (>20h) to prevent creator vote-stacking via DGNRS->sDGNRS conversion` | MATCH | CHG-02/CHG-03 governance protection verified |
| 150 | `Block unwrap during VRF stall (prevents creator vote-stacking)` | MATCH | Inline comment matches @dev |
| 163 | `@dev ETH sent last (checks-effects-interactions)` | MATCH | ETH transfer at line 176 is indeed last in burn() |
| 203 | `if (to == address(this)) revert Unauthorized()` | No inline comment | OK | Self-transfer prevention; obvious from code |

**DegenerusStonk Inline Summary:** All inline comments accurate. No stale references.

---

## 4. StakedDegenerusStonk.sol (514 lines)

### 4.1 DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `wrapperTransferTo(address, uint256)` | external | `@notice Transfer sDGNRS from the wrapper to a recipient (wrapper only)` | MATCH | DGNRS-only; direct balance manipulation documented |
| 2 | `gameAdvance()` | external | `@notice Advance the game on behalf of sDGNRS` | MATCH | Delegates to game.advanceGame() |
| 3 | `gameClaimWhalePass()` | external | `@notice Claim whale pass on behalf of sDGNRS` | MATCH | Delegates to game.claimWhalePass(address(0)) |
| 4 | `receive()` | external payable | `@notice Receive ETH deposit from game contract` | MATCH | onlyGame; adds ETH to reserve |
| 5 | `depositSteth(uint256)` | external | `@notice Receive stETH deposit from game contract` | MATCH | onlyGame; transferFrom stETH |
| 6 | `poolBalance(Pool)` | external view | `@notice Get remaining balance for a reward pool` | MATCH | Returns poolBalances by index |
| 7 | `transferFromPool(Pool, address, uint256)` | external | `@notice Transfer DGNRS from a reward pool to a recipient` | MATCH | Game-only; capped at available balance |
| 8 | `transferBetweenPools(Pool, Pool, uint256)` | external | `@notice Transfer DGNRS between two reward pools` | MATCH | Game-only; rebalances internal accounting |
| 9 | `burnRemainingPools()` | external | `@notice Burn all undistributed pool tokens at game over` | MATCH | Game-only; burns contract's own balance |
| 10 | `burn(uint256)` | external | `@notice Burn sDGNRS to claim proportional share of backing assets` | MATCH | BURNIE from balance + coinflip claimables; ETH-preferred payout documented (PAY-14) |
| 11 | `previewBurn(uint256)` | external view | `@notice Preview ETH, stETH, and BURNIE output for burning sDGNRS` | MATCH | Reflects ETH-preferential payout logic |
| 12 | `burnieReserve()` | external view | `@notice Get BURNIE backing (balance + claimable coinflips)` | MATCH | Returns coin.balanceOf + coinflip.previewClaimCoinflips |
| 13 | `name` | public constant | `@notice Token name` | MATCH | "Staked Degenerus Stonk" |
| 14 | `symbol` | public constant | `@notice Token symbol` | MATCH | "sDGNRS" |
| 15 | `decimals` | public constant | `@notice Token decimals` | MATCH | 18 |
| 16 | `totalSupply` | public | `@notice Total supply of sDGNRS tokens` | MATCH | |
| 17 | `balanceOf(address)` | public | `@notice Token balance for each address` | MATCH | |

**Cross-reference PAY-14:** The burn function at line 384 computes `totalMoney = ethBal + stethBal + claimableEth` which matches the ground truth formula. NatSpec at line 368 says "Prioritizes ETH over stETH" -- verified at lines 404-410 (ETH-preferred payout logic). The `supplyBefore` variable at line 379 is used for proportional calculation -- matches PAY-14 formula `(totalMoney * amount) / supplyBefore`.

**Cross-reference INV-03:** Supply conservation is maintained through mint-in-constructor only pattern. No public mint function exists; only _mint is private and called once in constructor. NatSpec at constructor line 192 documents "Mints creator allocation to DGNRS wrapper address and pool allocations to this contract."

**Lazy-claim CP-04 defense (PAY-14):** Line 383 `uint256 claimableEth = _claimableWinnings()` includes pending ETH winnings in totalMoney calculation. Lines 398-402 trigger on-demand claim if needed. NatSpec at line 368 documents "BURNIE paid from balance + coinflip claimables" -- the ETH lazy-claim defense is implicitly covered by including `claimableEth` in `totalMoney`, though not explicitly named "CP-04" in NatSpec. Behavior is correct.

**StakedDegenerusStonk NatSpec Summary:** 17 external/public items verified. 17 MATCH, 0 DISCREPANCY, 0 MISSING.

### 4.2 DOC-02: Inline Comment Verification

| Line(s) | Comment | Verdict | Notes |
|---------|---------|---------|-------|
| 33-47 | Contract-level @title/@notice/@dev ARCHITECTURE | MATCH | Describes soulbound token, ETH/stETH reserves, pre-minted pools, proportional burn |
| 90 | `burnieAmount (always 0; BURNIE arrives via manual transfers or coinflip claims)` | MATCH | Deposit event always emits 0 for BURNIE |
| 148 | `Initial supply (1 trillion tokens)` | MATCH | 1_000_000_000_000 * 1e18 |
| 154 | `Creator allocation (20%)` | MATCH | CREATOR_BPS = 2000 |
| 157-162 | Pool BPS constants | MATCH | Whale 1000 + Affiliate 3500 + Lootbox 2000 + Reward 500 + Earlybird 1000 = 8000 (80% of non-creator) |
| 234-236 | `Called by DGNRS contract when creator unwraps DGNRS to soulbound sDGNRS` | MATCH | wrapperTransferTo correctly described |
| 301 | `Transfers up to available balance if requested amount exceeds pool` | MATCH | Line 314-316 caps at available |
| 329 | `No token movement -- just rebalances internal pool accounting` | MATCH | Only poolBalances arrays modified |
| 367-368 | `BURNIE paid from balance + coinflip claimables. Prioritizes ETH over stETH` | MATCH | PAY-14 verified |

**StakedDegenerusStonk Inline Summary:** All inline comments accurate. No stale references.

---

## 5. WrappedWrappedXRP.sol (389 lines)

### 5.1 DOC-01: NatSpec Verification

| # | Function | Visibility | NatSpec | Verdict | Notes |
|---|----------|-----------|---------|---------|-------|
| 1 | `supplyIncUncirculated()` | external view | `@notice Total supply including uncirculating vault allowance` | MATCH | Returns totalSupply + vaultAllowance |
| 2 | `vaultMintAllowance()` | external view | `@notice Vault mint allowance remaining (uncirculating reserve)` | MATCH | Returns vaultAllowance |
| 3 | `approve(address, uint256)` | external | `@notice Approve spender to transfer up to amount` | MATCH | Standard ERC20 approve |
| 4 | `transfer(address, uint256)` | external | `@notice Transfer tokens from caller to recipient` | MATCH | Standard ERC20 transfer |
| 5 | `transferFrom(address, address, uint256)` | external | `@notice Transfer tokens from sender to recipient using caller's allowance` | MATCH | Unlimited allowance optimization documented |
| 6 | `unwrap(uint256)` | external | `@notice Unwrap WWXRP back to wXRP at 1:1 ratio (if reserves allow!)` | MATCH | CEI pattern documented; reserves check |
| 7 | `donate(uint256)` | external | `@notice Donate wXRP to increase reserves without minting WWXRP` | MATCH | Improves backing ratio |
| 8 | `mintPrize(address, uint256)` | external | `@notice Mint WWXRP to a recipient (for lootbox/game prizes)` | MATCH | Restricted to GAME/COIN/COINFLIP (compile-time constants); mints WITHOUT backing (PAY-18) |
| 9 | `vaultMintTo(address, uint256)` | external | `@notice Mint WWXRP to a recipient from the vault's uncirculating reserve` | MATCH | Vault-only; reduces vaultAllowance |
| 10 | `burnForGame(address, uint256)` | external | `@notice Burn WWXRP for game bets` | MATCH | Game-only; silently returns on zero |
| 11 | `name` | public constant | `@notice Token name - makes it VERY clear this is a joke` | MATCH | "Wrapped Wrapped WWXRP (PARODY)" |
| 12 | `symbol` | public constant | `@notice Token symbol` | MATCH | "WWXRP" |
| 13 | `decimals` | public constant | `@notice Number of decimals (matching wXRP standard)` | MATCH | 18 |
| 14 | `totalSupply` | public | `@notice Total circulating supply of WWXRP (excludes vault allowance)` | MATCH | |
| 15 | `INITIAL_VAULT_ALLOWANCE` | public constant | `@notice Initial uncirculating reserve (1B WWXRP, 18 decimals)` | MATCH | 1_000_000_000 ether |
| 16 | `vaultAllowance` | public | `@notice Remaining uncirculating reserve the vault can mint from` | MATCH | |
| 17 | `balanceOf(address)` | public | `@notice Mapping of address to WWXRP balance` | MATCH | |
| 18 | `allowance(address, address)` | public | `@notice Mapping of owner to spender to approved amount` | MATCH | |
| 19 | `wXRPReserves` | public | `@notice Actual wXRP reserves held by this contract` | MATCH | May be less than totalSupply |

**Cross-reference PAY-18:** mintPrize is restricted to MINTER_GAME, MINTER_COIN, MINTER_COINFLIP at lines 343-349. These are compile-time constants from ContractAddresses. NatSpec at line 335 says "Only callable by authorized minters (game/coin/coinflip contracts)." NatSpec accurately states "Mints WITHOUT backing, making the token more undercollateralized." Both match PAY-18 verdict.

**1 WWXRP per loss day:** In BurnieCoinflip.sol line 622, `wwxrp.mintPrize(player, lossCount * COINFLIP_LOSS_WWXRP_REWARD)` where `COINFLIP_LOSS_WWXRP_REWARD = 1 ether`. The WWXRP contract itself does not document "1 per loss day" -- that is the caller's responsibility. The mintPrize NatSpec correctly describes its role as a permissioned mint.

**WrappedWrappedXRP NatSpec Summary:** 19 external/public items verified. 19 MATCH, 0 DISCREPANCY, 0 MISSING.

### 5.2 DOC-02: Inline Comment Verification

| Line(s) | Comment | Verdict | Notes |
|---------|---------|---------|-------|
| 4-26 | Contract-level @title/@notice/@dev | MATCH | WARNING about joke token, no guarantee of backing, first-come-first-served, CEI pattern |
| 144-146 | "All external dependencies are compile-time constants sourced from ContractAddresses" | MATCH | No storage slots consumed for wiring |
| 166-168 | "Tracks the actual wXRP backing held by this contract" | MATCH | May be less or more than totalSupply |
| 293-294 | "Check if we actually have enough wXRP (probably not!)" | MATCH | Humorous but accurate |
| 322 | "Increase reserves without minting (improves backing ratio!)" | MATCH | donate() adds reserves only |
| 336 | "Mints WITHOUT backing, making the token more undercollateralized" | MATCH | Correct for mintPrize |
| 352 | "Mint without backing - increases the backing deficit (perfect!)" | MATCH | In-character humor; accurate |

**WrappedWrappedXRP Inline Summary:** All inline comments accurate. No stale references.

---

## Token Contracts Summary (Task 1)

| Contract | Functions Verified | MATCH | DISCREPANCY | MISSING | Inline Issues |
|----------|-------------------|-------|-------------|---------|---------------|
| BurnieCoin.sol | 33 | 33 | 0 | 0 | 0 |
| BurnieCoinflip.sol | 21 | 21 | 0 | 0 | 0 |
| DegenerusStonk.sol | 13 | 13 | 0 | 0 | 0 |
| StakedDegenerusStonk.sol | 17 | 17 | 0 | 0 | 0 |
| WrappedWrappedXRP.sol | 19 | 19 | 0 | 0 | 0 |
| **Total** | **103** | **103** | **0** | **0** | **0** |

**Pre-identified Issues:**
- PAY-07-I01: Coinflip 30/90 claim window asymmetry -- confirmed absent from NatSpec. Previously classified as FINDING-INFO (by-design per v1.1 spec). No escalation needed.
