# NatSpec and Inline Comment Verification: Game Modules Part 1

**Phase:** 29-02 (Comment/Documentation Correctness)
**Date:** 2026-03-18
**Auditor:** Claude Opus 4.6 (automated, v3.0 methodology)
**Scope:** DegenerusGameJackpotModule.sol (2,819 lines), DegenerusGameDecimatorModule.sol (1,027 lines)

---

## Methodology

For each module:
1. Enumerate all `external`/`public` functions and verify NatSpec tags (`@notice`, `@dev`, `@param`, `@return`, `@custom:reverts`, `@custom:access`) against audit ground truth (Phase 26-27 verdict reports).
2. Review all `///` inline comments for accuracy against verified code behavior.
3. Flag delegatecall context confusion (NatSpec claiming "this contract" when executing in game storage context).
4. Cross-reference specific findings (PAY-03-I01 winnerMask, lastDecClaimRound expiry, pool split percentages, auto-rebuy bonuses, BPS ranges).

Ground truth documents:
- `audit/v3.0-payout-jackpot-distribution.md` (PAY-01, PAY-02, PAY-16)
- `audit/v3.0-payout-scatter-decimator.md` (PAY-03, PAY-04, PAY-05, PAY-06)
- `audit/v3.0-gameover-core-distribution.md` (GO-08, GO-01)

---

## DOC-01: NatSpec Verification -- JackpotModule.sol

### External/Public Function NatSpec Summary

JackpotModule.sol contains **7 external functions** (the plan estimated 5 based on the primary entry points; the additional 2 are `runTerminalJackpot` and `payDailyCoinJackpot`).

| # | Function | Line | NatSpec Tags Present | Verdict | Notes |
|---|----------|------|---------------------|---------|-------|
| 1 | `runTerminalJackpot` | 288 | @notice, @dev, @param(3), @return | DISCREPANCY | NatSpec block is merged with payDailyJackpot documentation (see D1 below) |
| 2 | `payDailyJackpot` | 329 | @param(3) only | DISCREPANCY | Missing @notice and @dev -- detailed documentation is misplaced above runTerminalJackpot (see D1 below) |
| 3 | `payDailyJackpotCoinAndTickets` | 687 | @notice, @dev, @param | MATCH | Correctly describes Phase 2 of daily jackpot; stored value references accurate; gas optimization rationale correct |
| 4 | `awardFinalDayDgnrsReward` | 779 | @notice, @dev, @param(2) | MATCH | Correctly describes Day 5 DGNRS reward to solo bucket winner; 1% of reward pool matches `FINAL_DAY_DGNRS_BPS = 100` |
| 5 | `consolidatePrizePools` | 886 | @notice, @dev, @param(2) | MATCH | Flow description accurate: merge nextPrizePool, x00 keep-roll, 1-in-1e15 dump, yield surplus distribution. Matches PAY-16 verification |
| 6 | `processTicketBatch` | 1914 | @notice, @dev, @param, @return | MATCH | Gas budgeting description accurate: WRITES_BUDGET_SAFE=550, 35% cold-storage scaling, iterative completion model |
| 7 | `payDailyCoinJackpot` | 2385 | @notice, @dev, @param(2) | MATCH | 75/25 near/far-future split matches `FAR_FUTURE_COIN_BPS = 2500`; correctly describes daily BURNIE jackpot |

**Summary:** 7 functions verified. 5 MATCH, 2 DISCREPANCY (both related to misplaced NatSpec block D1).

### NatSpec Discrepancies

**D1: Misplaced payDailyJackpot NatSpec block (lines 258-287)**

The comprehensive `@notice`/`@dev` documentation for `payDailyJackpot` (describing both daily and early-burn paths) appears in lines 258-278, but is positioned above `runTerminalJackpot` (line 288), not above `payDailyJackpot` (line 329). The Solidity compiler associates the entire `///` comment block (lines 258-287) with `runTerminalJackpot`, leaving `payDailyJackpot` with only `@param` tags (lines 326-328).

Content accuracy of the misplaced block: The descriptions are **factually correct** when verified against PAY-01 and PAY-02:
- Daily path: "6%-14% slice of remaining currentPrizePool" -- matches `DAILY_CURRENT_BPS_MIN=600, DAILY_CURRENT_BPS_MAX=1400`
- "Day 5: 100% of remaining currentPrizePool" -- matches `dailyBps = 10_000` at line 374
- "1% from the unified future pool" -- matches `reserveSlice = _getFuturePrizePool() / 100` at line 433
- Early-burn path: "1% futurePrizePool ETH slice with 75% converted to lootbox tickets" -- matches `PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS = 7500`

**Severity:** INFORMATIONAL -- NatSpec is accurate but misplaced. No security impact. Tools parsing NatSpec (Etherscan, documentation generators) would show the wrong description for each function.

**D2: runTerminalJackpot inherits unrelated documentation**

As a consequence of D1, `runTerminalJackpot`'s compiled NatSpec would include the full payDailyJackpot description (lines 258-278) prepended to its own description (lines 280-287). The actual `@notice`/`@dev`/`@param`/`@return` tags for `runTerminalJackpot` (lines 280-287) are correct:
- "Terminal jackpot for x00 levels: Day-5-style bucket distribution" -- matches code using `FINAL_DAY_SHARES_PACKED` (60/13/13/13)
- "Called via IDegenerusGame(address(this)) from EndgameModule and GameOverModule" -- matches `msg.sender != ContractAddresses.GAME` guard

### Delegatecall Context Assessment

The contract-level NatSpec (lines 16-43) **correctly** documents the delegatecall architecture:
- Line 19: "Delegate-called module that hosts the jackpot distribution logic for DegenerusGame"
- Lines 22-23: "This contract is ONLY meant to be invoked via delegatecall from the main game contract. Storage layout inherits from DegenerusGameStorage"
- Line 26: "DO NOT deploy this contract standalone or call it directly"

No individual function NatSpec incorrectly claims "this contract" owns storage. The contract-level documentation is thorough and accurate. **PASS -- no delegatecall context confusion.**

---

## DOC-01: NatSpec Verification -- DecimatorModule.sol

### External/Public Function NatSpec Summary

DecimatorModule.sol contains **11 external functions** (the plan estimated 2 based on the primary player-facing claim functions; the additional 9 are delegatecall targets, recording functions, resolution functions, and view helpers).

| # | Function | Line | NatSpec Tags Present | Verdict | Notes |
|---|----------|------|---------------------|---------|-------|
| 1 | `creditDecJackpotClaimBatch` | 120 | @notice, @dev, @param(3), @custom:reverts(2) | MATCH | Correctly describes 50/50 split during normal play, 100% ETH during GAMEOVER. Access restriction to JACKPOTS contract accurate |
| 2 | `creditDecJackpotClaim` | 180 | @notice, @dev, @param(3), @custom:reverts | MATCH | Single-account version of batch; NatSpec mirrors batch version accurately |
| 3 | `recordDecBurn` | 222 | @notice, @dev, @param(5), @return, @custom:access | MATCH | Comprehensive: bucket migration, subbucket assignment, uint192 saturation, multiplier cap. All verified against code |
| 4 | `runDecimatorJackpot` | 298 | @notice, @dev, @param(3), @return, @custom:access | MATCH | "Returns poolWei if level already snapshotted or no qualifying burns" -- matches double-snapshot guard at line 306. lastDecClaimRound overwrite correctly undocumented (by-design per PAY-05) |
| 5 | `consumeDecClaim` | 395 | @notice, @dev, @param(2), @return, @custom:access | MATCH | Wrapper for internal `_consumeDecClaim`; access restriction to GAME contract documented |
| 6 | `claimDecimatorJackpot` | 411 | @notice, @dev, @param, @custom:reverts(3) | MATCH | Correctly documents prizePoolFrozen guard, claim expiry behavior, three revert conditions. Per PAY-05 verified |
| 7 | `decClaimable` | 442 | @notice, @dev, @param(2), @return(2) | MATCH | View function; "Only returns non-zero for the last decimator" correctly documents expiry behavior |
| 8 | `recordTerminalDecBurn` | 804 | @notice, @dev, @param(3) | MATCH | Documents coin contract access, bucket/multiplier from activity score, death clock deadline. Matches GO-08 verification |
| 9 | `runTerminalDecimatorJackpot` | 880 | @notice, @dev, @param(3), @return | MATCH | "10% of remaining" in @param matches GAMEOVER allocation. Double-resolution guard documented |
| 10 | `claimTerminalDecimatorJackpot` | 930 | @notice, @dev | MATCH | "Only callable post-GAMEOVER" accurate; level read from resolved claim round. 100% ETH confirmed by code (entropy=0, auto-rebuy skipped) |
| 11 | `terminalDecClaimable` | 943 | @notice, @param, @return(2) | MATCH | View function for terminal decimator claims |

**Summary:** 11 functions verified. **11 MATCH, 0 DISCREPANCY.** DecimatorModule NatSpec is exemplary -- comprehensive tags on every function, accurate descriptions, proper @custom:reverts and @custom:access annotations.

### Delegatecall Context Assessment

The contract-level NatSpec (lines 11-17) correctly documents the delegatecall architecture:
- Line 14: "Delegate-called module handling decimator jackpot tracking, resolution, and claim credits"
- Lines 15-16: "This module is called via delegatecall from DegenerusGame, meaning all storage reads/writes operate on the game contract's storage"

**PASS -- no delegatecall context confusion.**

### PAY-03-I01 winnerMask Status

The PAY-03-I01 finding (unused winnerMask variable) pertains to `DegenerusJackpots.sol` and `DegenerusGameEndgameModule.sol`, **not** DecimatorModule.sol. The winnerMask is returned by `DegenerusJackpots.runBafJackpot()` but unused by the caller in EndgameModule. Within DecimatorModule.sol, there are **zero references** to winnerMask. The NatSpec in `DegenerusJackpots.sol` (lines 105, 110, 214, 219, 226) correctly describes winnerMask's purpose for scatter ticket routing, and the `@return winnerMask` tag at line 226 is accurate to what the function returns. The finding is that the caller (EndgameModule) discards the value -- this is a dead-code observation classified as INFO with no NatSpec inaccuracy.

**Status: PAY-03-I01 NatSpec is accurate in the source contract (DegenerusJackpots.sol). The dead-code issue is in the caller, not in the NatSpec.**

---

## DOC-02: Inline Comment Verification -- JackpotModule.sol

Total `///` comment lines: 259. Reviewed for accuracy against audit verdicts.

### Verified Accurate Comments (selected key items)

| Line(s) | Comment | Verification |
|----------|---------|-------------|
| 104-105 | "Seconds offset from midnight UTC for daily jackpot reset boundary (22:57 UTC)" | `JACKPOT_RESET_TIME = 82620` = 22h57m00s. Correct (82620 / 3600 = 22.95h = 22:57) |
| 107-108 | "Maximum number of daily jackpots per level before forcing level transition" | `JACKPOT_LEVEL_CAP = 5`. Matches PAY-02: 5-day draw sequence |
| 114-115 | "Day-5 trait bucket shares packed: [6000, 1333, 1333, 1334] = 10000 bps. 60% share assigned to solo bucket" | Matches PAY-02: 60/13/13/13 distribution verified in v3.0-payout-jackpot-distribution.md |
| 122-123 | "Daily jackpot trait bucket shares: 2000 bps each x 4 = 8000 bps. Remaining 20% assigned to entropy-selected solo bucket" | Matches PAY-02: equal 20% shares during days 1-4 |
| 145-146 | "Current-pool daily jackpot percentage bounds for days 1-4 (6%-14%)" | `DAILY_CURRENT_BPS_MIN=600, DAILY_CURRENT_BPS_MAX=1400`. Matches PAY-02 verified range |
| 155-156 | "1 in 1 quadrillion chance for a 90% future->current dump" | `FUTURE_DUMP_ODDS = 1_000_000_000_000_000`. Matches consolidatePrizePools behavior |
| 162-163 | "Default SSTORE budget for processTicketBatch to stay safely under 15M gas" | `WRITES_BUDGET_SAFE = 550`. Matches gas budgeting strategy documented in processTicketBatch NatSpec |
| 178-179 | "Portion of DGNRS reward pool paid to the day-5 solo bucket winner (1%)" | `FINAL_DAY_DGNRS_BPS = 100`. Matches awardFinalDayDgnrsReward code |
| 181-182 | "Portion of reward-pool-funded daily jackpot ETH converted to loot boxes (50%)" | `DAILY_REWARD_JACKPOT_LOOTBOX_BPS = 5000`. Matches carryover 50% lootbox split |
| 184-185 | "Portion of purchase-phase reward-pool jackpots converted to loot boxes (3/4)" | `PURCHASE_REWARD_JACKPOT_LOOTBOX_BPS = 7500` = 75%. Matches PAY-01: 75/25 lootbox/ETH split |
| 211-212 | "Share of daily BURNIE budget awarded to far-future ticket holders (25%)" | `FAR_FUTURE_COIN_BPS = 2500`. Matches payDailyCoinJackpot 75/25 near/far split |
| 635 | "1% daily drip from futurePool" | `poolBps = 100 / 10_000 = 1%`. Matches PAY-01 |
| 808-809 | "Take 3% from unified reserve" | `(_getFuturePrizePool() * 300) / 10_000 = 3%`. Early-bird lootbox allocation verified |
| 867-869 | "All budget goes to nextPrizePool (like purchases during purchase phase)" | Pool routing correct per PAY-16 pool transition chain |
| 887-888 | "x00 yield accumulator dump: 50% into futurePool before keep-roll, 50% retained" | Matches PAY-13: `half = acc >> 1`. Rounding favors retention (line 892) |
| 926-927 | "23% DGNRS claimable, 23% vault claimable, 46% yield accumulator (~8% buffer)" | Matches PAY-12: `2300/10000 + 2300/10000 + 4600/10000 = 92%`, ~8% unextracted buffer |
| 91-92 | AUTO_REBUY_BONUS_BPS = 13_000 "130% = 1.3x tickets" | Matches PAY-16: auto-rebuy 130% bonus verified |
| 94-95 | AFKING_AUTO_REBUY_BONUS_BPS = 14_500 "145% = 1.45x tickets" | Matches PAY-16: afKing 145% bonus verified |

### Discrepancies Found

**IC-D1: No inline comment discrepancies found.**

All 259 `///` comment lines reviewed. Every numerical constant, percentage, pool source reference, and behavioral description is consistent with the audit ground truth from Phases 26-27. The compressed/turbo mode descriptions at lines 357-367 are accurate ("Turbo flag=2: all 5 logical days in 1 physical day. Compressed flag=1: 5 logical days in 3 physical days"). The BPS day 1-4 range (6-14%) and day 5 (100%) are correctly documented in both constants and inline comments.

**JackpotModule.sol inline comment summary: 259 comment lines reviewed, 0 discrepancies.**

---

## DOC-02: Inline Comment Verification -- DecimatorModule.sol

Total `///` comment lines: 189. Reviewed for accuracy against audit verdicts.

### Verified Accurate Comments (selected key items)

| Line(s) | Comment | Verification |
|----------|---------|-------------|
| 91-92 | "Auto-rebuy bonus in basis points (130% = 1.3x tickets)" | `AUTO_REBUY_BONUS_BPS = 13_000`. Matches PAY-05/PAY-16 |
| 94-95 | "afKing mode auto-rebuy bonus in basis points (145% = 1.45x tickets)" | `AFKING_AUTO_REBUY_BONUS_BPS = 14_500`. Matches PAY-05/PAY-16 |
| 100-101 | "Multiplier cap for Decimator burns (200 mints worth)" | `DECIMATOR_MULTIPLIER_CAP = 200 * PRICE_COIN_UNIT`. Matches decimator cap behavior per PAY-05 |
| 110-113 | creditDecJackpotClaimBatch: "During GAMEOVER state, credits 100% as ETH. During normal play, splits 50/50" | Matches PAY-05: 50/50 ETH/lootbox split verified. GAMEOVER path verified per GO-08 |
| 165 | "Add all lootbox ETH to futurePrizePool once at the end" | Batched futurePrizePool update. Matches PAY-05 accounting verification |
| 207-213 | recordDecBurn: "First burn sets player's bucket. Subsequent burns accumulate unless strictly better bucket provided" | Matches decimator bucket migration logic verified in PAY-05 |
| 289-292 | "Snapshots winning subbuckets for deferred claim distribution. Returns poolWei if level already snapshotted" | Matches double-snapshot guard at line 306 and PAY-05 |
| 343 | "Snapshot last claim round (overwrites previous - old claims expire)" | Matches PAY-05: lastDecClaimRound overwrite expiry documented as by-design per v1.1 spec Section 8 |
| 368 | "Only allow claims for the last decimator (claims expire when next decimator runs)" | Accurately describes expiry semantics. Matches PAY-05 verdict |
| 412-415 | "Block claims while prize pools are frozen (jackpot phase days 1-4). This path writes to futurePrizePool (lootbox portion)" | Matches prizePoolFrozen guard rationale verified in PAY-05 |
| 534 | "Split 50/50: half ETH, half lootbox tickets" | `ethPortion = amount >> 1; lootboxPortion = amount - ethPortion`. Correct 50/50 split |
| 540-541 | "Lootbox portion is no longer claimable ETH; remove from reserved pool" | `claimablePool -= lootboxPortion`. Matches INV-01 claimablePool invariant verification from Phase 28 |
| 784-785 | "Seconds... 365 days" for terminal dec idle timeout | `TERMINAL_DEC_IDLE_TIMEOUT_DAYS = 365`. Matches GO-08 |
| 786 | "120 days" death clock | `TERMINAL_DEC_DEATH_CLOCK = 120 days`. Matches GO-08 verification |
| 493-494 | "Decimator pool was pre-reserved in claimablePool; deduct ticket conversion" | `claimablePool -= calc.ethSpent`. Correctly documents the pre-reserve/deduct pattern verified in INV-01 |
| 752-753 | "Always-open burn for GAMEOVER. Time multiplier rewards early conviction. 200k cap equalizes bankroll" | Matches GO-08: time-based multiplier (not phase-based), cap prevents whale dominance |
| 858-859 | "Update bucket aggregate (key includes lvl, so old-level entries are naturally stale)" | `bucketKey = keccak256(abi.encode(lvl, e.bucket, e.subBucket))`. Level scoping prevents cross-level contamination |
| 916 | "Snapshot claim round (single slot -- no rngWord needed, GAMEOVER skips auto-rebuy)" | Correct: `_addClaimableEth` with entropy=0, `_processAutoRebuy` returns false when `gameOver==true` |
| 996-999 | "Time multiplier: > 10 days: daysRemaining / 4 (30x at 120 days, 2.75x at 11 days); <= 10 days: linear 2x to 1x" | Matches GO-08: `_terminalDecMultiplierBps(120) = 120*2500 = 300000 = 30x`; `_terminalDecMultiplierBps(11) = 11*2500 = 27500 = 2.75x`; `_terminalDecMultiplierBps(10) = 10000 + (9*10000)/9 = 20000 = 2x`. Correct |

### baseFuturePool vs futurePoolLocal Distinction

The plan specifically asked to verify this distinction in DecimatorModule. Analysis:

- DecimatorModule does NOT use `baseFuturePool` or `futurePoolLocal` directly. These are variables in `EndgameModule.runRewardJackpots()`.
- DecimatorModule operates on `poolWei` (passed as parameter), `futurePrizePool` (via `_getFuturePrizePool()`/`_setFuturePrizePool()`), and `claimablePool`.
- The pool source distinction is correctly documented in the audit ground truth (`audit/v3.0-payout-scatter-decimator.md`): normal decimator gets 10% of `futurePoolLocal`, x00 decimator gets 30% of `baseFuturePool`. These are computed in EndgameModule and passed as `poolWei` to DecimatorModule.
- DecimatorModule's NatSpec does not reference these internal EndgameModule variables, which is correct -- it would be misleading for DecimatorModule to document pool source details that belong to its caller.

**No comment inaccuracy regarding pool sources.**

### Discrepancies Found

**IC-D1: No inline comment discrepancies found.**

All 189 `///` comment lines reviewed. Every constant annotation, behavioral description, and accounting rationale is consistent with the audit ground truth from Phases 26-28.

**DecimatorModule.sol inline comment summary: 189 comment lines reviewed, 0 discrepancies.**

---

## Module 1-2 Summary

| Module | Lines | External Functions | /// Comments | NatSpec Verdicts | NatSpec Discrepancies | Inline Discrepancies |
|--------|-------|-------------------|-------------|-----------------|----------------------|---------------------|
| JackpotModule.sol | 2,819 | 7 | 259 | 5 MATCH, 2 DISCREPANCY | 2 (D1: misplaced block, D2: inherited docs) | 0 |
| DecimatorModule.sol | 1,027 | 11 | 189 | 11 MATCH, 0 DISCREPANCY | 0 | 0 |
| **Total** | **3,846** | **18** | **448** | **16 MATCH, 2 DISCREPANCY** | **2** | **0** |

**Delegatecall context:** Both modules correctly document their delegatecall architecture at the contract level. No individual function NatSpec incorrectly claims independent storage ownership.

**PAY-03-I01 winnerMask:** Not in scope for these two modules. Source NatSpec in DegenerusJackpots.sol is accurate. Dead-code observation pertains to EndgameModule caller.

**lastDecClaimRound expiry:** Correctly documented as "overwrites previous - old claims expire" (line 343) and "claims expire when next decimator runs" (line 368). Matches by-design classification from PAY-05.

---

## DOC-01: NatSpec Verification -- LootboxModule.sol

### External/Public Function NatSpec Summary

LootboxModule.sol contains **5 external functions** (matching plan estimate). Additionally, 1 external view function (`deityBoonSlots`) is included.

| # | Function | Line | NatSpec Tags Present | Verdict | Notes |
|---|----------|------|---------------------|---------|-------|
| 1 | `openLootBox` | 552 | @notice, @dev, @param(2), @custom:reverts(2) | MATCH | Correctly describes ETH lootbox opening with RNG dependency; activity score EV multiplier and 10 ETH cap documented |
| 2 | `openBurnieLootBox` | 632 | @notice, @dev, @param(2), @custom:reverts(2) | MATCH | "Converts BURNIE to ETH-equivalent value at 80% rate" -- matches code `(burnieAmount * priceWei * 80) / (PRICE_COIN_UNIT * 100)` at line 645 |
| 3 | `resolveLootboxDirect` | 688 | @notice, @dev, @param(3) | MATCH | "Resolve a lootbox directly for decimator claims (no RNG wait needed)" accurately describes the sync resolution path; "Jackpot/claim lootboxes do not award boons (allowBoons=false)" matches the `false` parameter in `_resolveLootboxCommon` |
| 4 | `deityBoonSlots` | 727 | @notice, @dev, @param, @return(3) | MATCH | Correctly describes deterministic boon generation from deity address and day; decimator window gating documented; return types accurate |
| 5 | `issueDeityBoon` | 755 | @notice, @dev, @param(3), @custom:reverts(6) | MATCH | Comprehensive revert documentation: zero address, self-boon, slot overflow, no purchased passes, no RNG, recipient already received, slot already used. All verified against code guards at lines 756-771 |

**Summary:** 5 functions verified. **5 MATCH, 0 DISCREPANCY.**

### Delegatecall Context Assessment

The contract-level NatSpec (lines 22-34) correctly documents the delegatecall architecture:
- Line 25: "Delegatecall module for lootbox opening, boon consumption, and deity boon system"
- Lines 27-28: "This module is called via delegatecall from DegenerusGame, meaning all storage reads/writes operate on the game contract's storage"

**PASS -- no delegatecall context confusion.**

### Lootbox Reward Type NatSpec Verification (PAY-09 cross-reference)

The 5 lootbox reward types verified in PAY-09 have corresponding inline documentation:

1. **Whale pass queueing and claimablePool remainder:** Documented at lines 306-311 (HALF_WHALE_PASS_PRICE = 2.25 ether). The `_resolveLootboxCommon` flow correctly routes large awards through whale pass queueing per PAY-09 verification.

2. **Lazy pass discount boons:** Constants at lines 203-206 (10%/25%/50% tiers). NatSpec accurately describes these as discount boons, not direct lazy pass grants. Matches PAY-09: "implemented as discount boons, not direct lazy pass grants."

3. **Deity pass boons:** Constants at lines 207-214 with tier identifiers (1/2/3) and max total threshold (32). Matches PAY-09 deity pass discount description.

4. **5-tier ticket variance:** Constants at lines 266-285 document all 5 tiers with accurate chance percentages (1%/4%/20%/45%/30% default) and multipliers (4.6x/2.3x/1.1x/0.651x/0.45x). All values match the code constants exactly.

5. **BURNIE creditFlip delivery:** Documented through the resolution flow in `_resolveLootboxCommon`. BURNIE rewards are delivered via `coin.creditFlip()`, matching the creditFlip mechanism verified in PAY-09.

---

## DOC-01: NatSpec Verification -- AdvanceModule.sol

### External/Public Function NatSpec Summary

AdvanceModule.sol contains **7 external functions** (the plan estimated 4 based on primary entry points; the additional 3 are `wireVrf`, `updateVrfCoordinatorAndSub`, and `rawFulfillRandomWords`).

| # | Function | Line | NatSpec Tags Present | Verdict | Notes |
|---|----------|------|---------------------|---------|-------|
| 1 | `advanceGame` | 120 | @notice (brief, 2 lines) | MATCH | "Advance game state. Called daily to process jackpots, mints, and phase transitions. Caller receives ~0.01 ETH worth of BURNIE as flip credit" -- matches `ADVANCE_BOUNTY_ETH = 0.01 ether` and `coin.creditFlip(caller, advanceBounty)` |
| 2 | `wireVrf` | 391 | @notice, @dev, @param(3) | MATCH | "Wire VRF config, called once from the ADMIN constructor" -- matches single-use deployment pattern. Access documented as ContractAddresses.ADMIN only |
| 3 | `requestLootboxRng` | 673 | @notice, @dev | MATCH | "Standalone function for mid-day lootbox RNG requests. Cannot be called while daily RNG is locked" -- matches `rngLockedFlag` guard at line 674. VRF callback direct finalization documented |
| 4 | `reverseFlip` | 1295 | @notice, @dev | MATCH | "Pay BURNIE to nudge the next RNG word by +1. Cost scales +50% per queued nudge" -- matches escalating cost mechanism in `_currentNudgeCost`. RNG security note accurate: players cannot predict base word |
| 5 | `updateVrfCoordinatorAndSub` | 1259 | @notice, @dev, @param(3) | MATCH | "Emergency VRF coordinator rotation (governance-gated)" -- matches ADMIN-only access and stall duration governance. Correctly documents state reset behavior |
| 6 | `rawFulfillRandomWords` | 1323 | @notice, @dev, @param(2) | MATCH | "Chainlink VRF callback for random word fulfillment" -- correctly describes dual path (daily RNG vs mid-day RNG). Security validation of requestId and coordinator address documented |
| 7 | `resolveLootboxRng` (internal dispatch) | N/A | N/A | N/A | Not a separate external function; lootbox RNG resolution is handled within `rawFulfillRandomWords` |

**Note:** The plan counted 4 external functions. The actual count is 6 external functions with NatSpec (advanceGame, wireVrf, requestLootboxRng, reverseFlip, updateVrfCoordinatorAndSub, rawFulfillRandomWords). Function #7 in the table is not separate.

**Summary:** 6 functions verified. **6 MATCH, 0 DISCREPANCY.**

### Advance Bounty Escalation Verification (PAY-17 cross-reference)

The plan specifically requires verification that the advance bounty escalation is **time-based** (not phase-based).

**Code (AdvanceModule.sol:197-207):**
```
// Escalate bounty if daily processing is stalled (new-day path only).
// 2x after 1 hour past jackpot reset, 3x after 2 hours, capped at 3x.
{
    uint256 dayStart = ...
    uint256 elapsed = ts - dayStart;
    if (elapsed >= 2 hours) {
        advanceBounty *= 3;
    } else if (elapsed >= 1 hours) {
        advanceBounty *= 2;
    }
}
```

**Verification:** The escalation is explicitly time-based: `elapsed >= 2 hours` gives 3x, `elapsed >= 1 hours` gives 2x, otherwise 1x. This matches PAY-17 verdict: "Advance bounty multipliers are time-based (1h/2h elapsed), not phase-based as research suggested." The inline comment at lines 197-198 accurately describes this as "2x after 1 hour past jackpot reset, 3x after 2 hours, capped at 3x."

**CONFIRMED: Time-based escalation (1x/2x/3x), not phase-based.**

### Mint-Gate Verification

The `_enforceDailyMintGate` function (called at line 153) implements caller eligibility for advanceGame. The inline comments at lines 627-666 describe:
- "Always allow if caller minted today" (lines 627-632)
- "Anyone after 30 min" (line 651)
- "Any pass holder after 15 min" (lines 654-660)
- "DGVE majority bypass -- last resort, external call" (line 663)

These match the mint-gate verification in PAY-17.

### creditFlip Delivery Mechanism

The advance bounty is delivered via `coin.creditFlip(caller, advanceBounty)` at lines 172, 190, 217, and 376. This matches PAY-17: "0.01 ETH in BURNIE via creditFlip." The `advanceBounty = (ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / price` formula at line 122 correctly converts the 0.01 ETH target to BURNIE equivalent. Division-by-zero is impossible because `price` is initialized to 0.01 ETH and only mutated to non-zero tier values (confirmed in PAY-17).

### Queue Processing Logic (EDGE-03 context)

The advanceGame function processes ticket queues via `_runProcessTicketBatch` (lines 214, 266) with batch-based iteration. The inline comments at lines 210-222 accurately describe the "Daily drain gate: ensure read slot is fully processed before RNG" pattern. The EDGE-03 finding (queue inflation delays jackpots but batch mechanism prevents gas exhaustion) is consistent with the documented behavior -- the code handles large queues by splitting across multiple advanceGame calls.

### Delegatecall Context Assessment

The contract-level NatSpec (line 27) correctly documents:
- "Delegate-called module for advanceGame and VRF lifecycle handling"

The module inherits from `DegenerusGameStorage` (line 28), which is the correct storage layout inheritance for delegatecall context.

**PASS -- no delegatecall context confusion.**

---

## DOC-02: Inline Comment Verification -- LootboxModule.sol

Total `///` comment lines: 324. Reviewed for accuracy against audit verdicts.

### Verified Accurate Comments (selected key items)

| Line(s) | Comment | Verification |
|----------|---------|-------------|
| 191-192 | "Portion of lootbox EV reserved for boon/pass draw (10%)" | `LOOTBOX_BOON_BUDGET_BPS = 1000` = 10%. Matches lootbox resolution logic |
| 196-197 | "Assumed utilization of max boon value (50%)" | `LOOTBOX_BOON_UTILIZATION_BPS = 5000`. Used in EV estimation |
| 199-202 | "Whale boon discount tiers (10%, 25%, 50%)" | Constants 1000/2500/5000 BPS. Matches PAY-09 lazy/whale discount boon tiers |
| 266-267 | "Base ticket roll budget in BPS (~127% EV after variance, 55% chance path)" | `LOOTBOX_TICKET_ROLL_BPS = 16_100`. EV estimation comment is informational, matches design intent |
| 268-285 | 5-tier ticket variance: "1% chance tier 1 (4.6x), 4% tier 2 (2.3x), 20% tier 3 (1.1x), 45% tier 4 (0.651x), default tier 5 (0.45x)" | All chance BPS (100/400/2000/4500) and multiplier BPS (46000/23000/11000/6510/4500) match constants. Sum of chances: 1+4+20+45 = 70%, leaving 30% for tier 5. Matches PAY-09 |
| 286-293 | DGNRS pool per-ETH tiers: "0.001% small, 0.039% medium, 0.08% large, 0.8% mega" | PPM values (10/390/800/8000) match these percentages exactly |
| 294-295 | "Fixed WWXRP prize amount (1 token)" | `LOOTBOX_WWXRP_PRIZE = 1 ether`. Matches PAY-18: 1 WWXRP per award |
| 304-305 | "Presale BURNIE bonus in BPS (62% bonus)" | `LOOTBOX_PRESALE_BURNIE_BONUS_BPS = 6_200`. Matches presale bonus logic |
| 306-311 | "Whale pass price (200 tickets over levels 10-109): 4.50 ether. Half: 2.25 ether" | Matches PAY-09 whale pass queueing: `HALF_WHALE_PASS_PRICE = 2.25 ether` |
| 312-314 | "Threshold above which lootbox is split into two rolls (0.5 ETH)" | `LOOTBOX_SPLIT_THRESHOLD = 0.5 ether`. Matches split logic in resolution |
| 316-317 | "Distress-mode ticket bonus in basis points (25%)" | `DISTRESS_TICKET_BONUS_BPS = 2500`. Matches distress mode bonus |
| 546-547 | "Open an ETH lootbox once RNG is available. Applies activity score EV multiplier with a 10 ETH cap per account per level" | Matches code at lines 587-598: EV multiplier application with cap enforcement |
| 626-627 | "Open a BURNIE lootbox once RNG is available. Converts BURNIE to ETH-equivalent value at 80% rate" | Matches code: `(burnieAmount * priceWei * 80) / (PRICE_COIN_UNIT * 100)` at line 645 |
| 683-684 | "Resolve a lootbox directly for decimator claims (no RNG wait needed). Jackpot/claim lootboxes do not award boons" | Matches direct resolution with `allowBoons=false` parameter |
| 787-788 | "Roll a target level: 95% chance: 0-5 levels above base. 5% chance: 5-50 levels above base" | Matches `_rollTargetLevel` code: `rangeRoll < 5` for 5% far-future, `% 46 + 5` for offset 5-50, `% 6` for offset 0-5 |

### Discrepancies Found

**No inline comment discrepancies found.**

All 324 `///` comment lines reviewed. Constant annotations match their values, behavioral descriptions match verified code paths, and EV estimation comments are consistent with the lootbox resolution logic verified in PAY-09.

**LootboxModule.sol inline comment summary: 324 comment lines reviewed, 0 discrepancies.**

---

## DOC-02: Inline Comment Verification -- AdvanceModule.sol

Total `///` comment lines: 119. Reviewed for accuracy against audit verdicts.

### Verified Accurate Comments (selected key items)

| Line(s) | Comment | Verification |
|----------|---------|-------------|
| 90 | "Deploy idle timeout: 365 days" | `DEPLOY_IDLE_TIMEOUT_DAYS = 365`. Matches GAMEOVER timeout at level 0 per GO-01 |
| 91 | "GAMEOVER RNG fallback delay: 3 days" | `GAMEOVER_RNG_FALLBACK_DELAY = 3 days`. Matches VRF fallback verified in GO-06 |
| 92 | "Maximum jackpots per level: 5" | `JACKPOT_LEVEL_CAP = 5`. Matches PAY-02 5-day draw sequence |
| 93-94 | "VRF callback gas limit: 300k. Request confirmations: 10" | Matches VRF configuration |
| 96 | "RNG nudge base cost: 100 BURNIE" | `RNG_NUDGE_BASE_COST = 100 ether`. Matches reverseFlip base cost |
| 97 | "BURNIE RNG trigger: 40,000 BURNIE" | `BURNIE_RNG_TRIGGER = 40_000 ether`. Threshold for lootbox RNG request |
| 99-104 | Next-to-future BPS parameters: "fast=3000, min=1300, week_step=100, x9_bonus=200, variance=1000, variance_min=1000" | All constants match their BPS values exactly |
| 105 | "Insurance skim: 1% of nextPool -> yieldAccumulator" | `INSURANCE_SKIM_BPS = 100` = 1%. Matches PAY-12: "insurance skim 1% of nextPool" |
| 108-109 | "Presale auto-ends after 200 ETH" | `LOOTBOX_PRESALE_ETH_CAP = 200 ether`. Matches presale logic at line 315 |
| 111-112 | "ETH-equivalent target for advanceGame bounty (~0.01 ETH worth of BURNIE)" | `ADVANCE_BOUNTY_ETH = 0.01 ether`. Matches PAY-17: "0.01 ETH base" |
| 118-119 | "Advance game state. Called daily. Caller receives ~0.01 ETH worth of BURNIE as flip credit" | Matches advanceBounty calculation and creditFlip delivery |
| 127-128 | "Turbo: if target already met on day <=1, flag now" | Matches compressed/turbo mode logic: `compressedJackpotFlag = 2` for turbo |
| 197-198 | "Escalate bounty if daily processing is stalled. 2x after 1 hour, 3x after 2 hours, capped at 3x" | Time-based escalation confirmed per PAY-17. NOT phase-based |
| 210 | "Daily drain gate: ensure read slot is fully processed before RNG" | Matches queue processing order verified in EDGE-03 |
| 250-251 | "Process near-future ticket queues (+2..+6) before daily draws to include fresh lootbox-driven tickets" | Matches pre-processing logic for ticket queue freshness |
| 379-384 | "Deploy-only VRF setup called from ContractAddresses.ADMIN constructor" | Matches wireVrf access pattern |
| 669-671 | requestLootboxRng: "Standalone function for mid-day lootbox RNG requests. Cannot be called while daily RNG is locked" | Matches rngLockedFlag guard |
| 708-709 | "Freeze ticket buffer: swap write->read so tickets purchased after VRF delivery can't be resolved by this word" | Matches double-buffer ticket isolation pattern |
| 1290-1293 | reverseFlip: "Cost scales +50% per queued nudge and resets after fulfillment. Only available while RNG is unlocked" | Matches escalating cost and rngLockedFlag guard |
| 1316-1320 | rawFulfillRandomWords: "Daily RNG: stores word for advanceGame processing. Mid-day RNG: directly finalizes lootbox RNG" | Matches dual-path VRF callback at lines 1333-1339 |

### Discrepancies Found

**No inline comment discrepancies found.**

All 119 `///` comment lines reviewed. Every constant annotation, time-based escalation description, and VRF lifecycle comment is accurate. The advance bounty escalation is correctly documented as time-based (1x/2x/3x) throughout.

**AdvanceModule.sol inline comment summary: 119 comment lines reviewed, 0 discrepancies.**

---

## Overall Summary -- All 4 Modules

| Module | Lines | External Functions | /// Comments | NatSpec Verdicts | NatSpec Discrepancies | Inline Discrepancies |
|--------|-------|-------------------|-------------|-----------------|----------------------|---------------------|
| JackpotModule.sol | 2,819 | 7 | 259 | 5 MATCH, 2 DISCREPANCY | 2 (D1: misplaced block, D2: inherited docs) | 0 |
| DecimatorModule.sol | 1,027 | 11 | 189 | 11 MATCH | 0 | 0 |
| LootboxModule.sol | 1,778 | 5 | 324 | 5 MATCH | 0 | 0 |
| AdvanceModule.sol | 1,383 | 6 | 119 | 6 MATCH | 0 | 0 |
| **Total** | **7,007** | **29** | **891** | **27 MATCH, 2 DISCREPANCY** | **2** | **0** |

### Key Findings

1. **NatSpec Discrepancies (2, both INFORMATIONAL):** Both in JackpotModule.sol -- misplaced NatSpec block for `payDailyJackpot` sitting above `runTerminalJackpot`. Content is factually correct but compiler-associated with the wrong function. No security impact.

2. **Delegatecall Context:** All 4 modules correctly document their delegatecall architecture at the contract level. No function-level NatSpec incorrectly claims independent storage ownership.

3. **Advance Bounty Escalation (PAY-17):** Confirmed as **time-based** (1x/2x/3x at 0h/1h/2h elapsed). Both code and inline comments are consistent. Not phase-based as some early research suggested.

4. **PAY-03-I01 winnerMask:** Not present in any of these 4 modules. Source NatSpec in DegenerusJackpots.sol is accurate; dead-code observation pertains to EndgameModule caller.

5. **Inline Comment Quality:** 891 `///` comment lines across all 4 modules, 0 discrepancies found. Every numerical constant, percentage, pool source reference, and behavioral description verified accurate against Phase 26-28 audit verdicts.
