# RNG-Freeze-at-Commitment Proof — v68.0

> **AI/CI re-verification artifact.** Every consumer below is a structured row an AI can re-check against source WITHOUT prose interpretation: `id`, `consumptionSite` (file:line where a VRF value is consumed), `commitmentPoint` (file:line P where the value is sealed), the formal freeze invariant, the frozen-input enumeration, the verification recipe (grep/read predicates), the `freezeClass`, and the independent adversarial re-verification verdict (`freezeHolds` + finding). The companion machine index is `audit/rng-freeze-proof-v68.index.json`.

## Header

- **Subject:** contracts tree `4970ba5b` @ HEAD `414209c4` (v68 logic-frozen). The prompt names `d0af2984` (the v67 tip / an ancestor); the `contracts/` tree object is byte-identical at HEAD, so the source under proof is the named frozen subject. Includes v67 fixes: MIDRNG-02 `73eb242a`, DELEGATE-FIND-01 `095a7ac9`, BRICK gas `2aed5d28`.
- **Method:** per-cluster ledger (consumption-site + commitment-point + freeze invariant + frozen-input enumeration + verification recipe) merged with an independent adversarial re-verification leg (strongest-refutation-attempted per consumer; `freezeHolds` = does the freeze survive the strongest source-grounded attack; `recipeReproducible` = did the recipe greps/reads reproduce verbatim against current source).
- **Freeze semantics:** a consumer is *frozen at commitment* if, for every actor-reachable path between the commitment point P (where the VRF value and its frozen inputs are sealed) and the consumption site C (where an outcome is derived), no actor-controllable input can change WHICH committed word is consumed or HOW the outcome is derived. Pool-magnitude/EV reads that only scale an already-determined payout (never the selection) are recorded as `liveInputs` and do not break the freeze.

### Totals

- **Total VRF consumers covered:** 79
- **freezeHolds = true:** 78 / **flagged (freezeHolds=false):** 1

| freezeClass | count |
|---|---|
| FROZEN-AT-COMMIT | 39 |
| CROSS-CONTRACT-SEAM | 33 |
| NEEDS-PROOF | 5 |
| MUTABLE-INPUT | 2 |
| **TOTAL** | **79** |

**Severity-if-broken distribution:** n/a=58, INFO=10, LOW=7, MED=2, HIGH=2

**Residual / flagged entries:** 12 (1 freezeHolds=false, 1 recipe-not-reproduced, the rest severity>=LOW with freezeHolds=true). Detailed in the [Residual / flagged](#residual--flagged) section.

---

## Cluster: AdvanceModule — advanceGame / rngGate / VRF-callback / backfill / gameover-entropy

_All 17 advance/rngGate/callback/backfill/gameover-entropy consumers exist at HEAD; the redemption-roll formula uint16((word>>8)%151+25) is identical at all 3 sites (rngGate :1262, gameover-normal :1324, gameover-fallback :1359) and Halmos-bounded [25,175]. MIDRNG-02 (v67 73eb242a) reshaped the mid-day lootbox latch path (LR_MID_DAY release added on the new-day path :304-306 in addition to same-day :241); no NEW VRF-derived VALUE consumer was introduced — it is control-flow/latch hygiene around RNGF-ADV-01/02/13. RNGF-ADV-16/17 (rollDailyQuest/rollLevelQuest, codex-found rows 70/71) are formally proven here as advance-dispatched FROZEN-AT-COMMIT consumers. Residuals: ADV-10/12 carry the source-documented 1-bit block.prevrandao validator bias on the gameover-only fallback after 14d VRF death (NEEDS-PROOF/INFO)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-ADV-01` | contracts/modules/DegenerusGameAdvanceModule.sol:226-230 (advanceGame same-day day==dIdx branch: lootboxRngWordByIndex[(LR_INDEX-1)] read; word==0 reverts RngNotReady) | Index reserved at requestLootboxRng (AdvanceModule.sol:1138 LR_MID_DAY=1, :1146 _lrAdvanceIndexClearPending advances LR_INDEX) AND the word landing point (rawFulfillRandomWords mid-day :1850 or new-day assign :288). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-02` | AdvanceModule.sol:280-289 (new-day daily-drain: if lootboxRngWordByIndex[preIdx]==0, cw=rngWordCurrent+totalFlipReversals, write, emit) | rngWordCurrent committed by rawFulfillRandomWords daily branch :1846 (under rngLockedFlag==true); guarded by rngWordCurrent!=0 short-circuit :1839; preIdx=LR_INDEX-1 request-advanced. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-03` | AdvanceModule.sol:1929-1930 (_applyDailyRng: finalWord=rawWord+nudges; rngWordCurrent=finalWord; rngWordByDay[day]=finalWord) — canonical write all downstream read | Same site — this IS commitment point P for the day word. rawWord from VRF callback rawFulfillRandomWords:1846 before _applyDailyRng runs in next advanceGame. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-04` | AdvanceModule.sol:480-485 (saltedRng=keccak256(rngWord,BONUS_TRAITS_TAG); _payDailyCoinJackpot(1,saltedRng,2,5)) + direct rngWord uses :474-479 | rngWord returned by rngGate at :380 (=rngWordByDay[day], committed at RNGF-ADV-03 :1930). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-05` | AdvanceModule.sol:875 (bps += rngWord % 1001) and :889-891 (roll1=(rngWord>>64)%range; roll2=(rngWord>>192)%range; combined=(roll1+roll2)/2) | rngWord = frozen daily word passed from advanceGame :529 (rngGate return :380, committed :1930); runs only on lastPurchase leg with rngLockedFlag held. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-06` | AdvanceModule.sol:928 (if ((rngWord&1)==1) -> runBafJackpot(bafPoolWei,lvl,rngWord) :932; else jackpots.markBafSkipped(lvl) :940) | rngWord frozen daily word (rngGate :380 -> _consolidate :529). Bit 0 same bit Coinflip win uses (bit-allocation map :1183-1184). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-ADV-07` | AdvanceModule.sol:964-979 (seed=EntropyLib.hash2(rngWord,FUTURE_KEEP_TAG); total=sum of five (seed>>{0,16,32,48,64})%4; keepBps=3000+total*3500/15; moveWei) | rngWord frozen daily word (rngGate :380 -> _consolidate :529); guarded by (lvl%100)==0 :963. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-08` | AdvanceModule.sol:1261-1264 (redemptionRoll=uint16((currentWord>>8)%151+25); sdgnrs.resolveRedemptionPeriod(redemptionRoll,toResolve)) | currentWord committed by _applyDailyRng :1239 (RNGF-ADV-03 :1930); resolved day = sdgnrs.pendingResolveDay() :1259 stamped at burn submit (sDGNRS._submitGamblingClaimFrom :1043). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-ADV-09` | AdvanceModule.sol:1323-1326 (_gameOverEntropy: redemptionRoll=uint16((currentWord>>8)%151+25); sdgnrs.resolveRedemptionPeriod(redemptionRoll,toResolve)) | currentWord=_applyDailyRng(day,rngWordCurrent) :1309 (RNGF-ADV-03 :1930), reached only when fresh VRF word exists (rngWordCurrent!=0 && rngRequestTime!=0 :1308). Sentinel toResolve stamped at submit. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-ADV-10` | AdvanceModule.sol:1358-1361 (_gameOverEntropy fallback: redemptionRoll=uint16((fallbackWord>>8)%151+25); sdgnrs.resolveRedemptionPeriod(redemptionRoll,toResolve)) | fallbackWord=_applyDailyRng(day,_getHistoricalRngFallback(day)-totalFlipReversals) :1343-1344; fallback word = keccak of up-to-5 committed historical VRF words + currentDay + block.prevrandao :1414-1416. Reachable only after GAMEOVER_RNG_FALLBACK_DELAY (14d) of VRF deadness :1335. | NEEDS-PROOF | yes | INFO | yes |
| `RNGF-ADV-11` | AdvanceModule.sol:1288-1291 (_finalizeLootboxRng: index=LR_INDEX-1; if lootboxRngWordByIndex[index]==0 then =rngWord; emit). Called from rngGate :1268, _gameOverEntropy :1329 and :1364. | rngWord = the applied daily/gameover/fallback word (RNGF-ADV-03/09/10); index=LR_INDEX-1 advanced at RNG request (_finalizeRngRequest :1694 / requestLootboxRng :1146). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-12` | AdvanceModule.sol:1414-1417 (word=keccak256(combined,currentDay,block.prevrandao); if word==0 word=1). combined=hash2-chain of up to 5 historical rngWordByDay[searchDay] :1400-1408. | Historical words rngWordByDay[1..searchLimit] each committed at their own RNGF-ADV-03 on past days (immutable). Fallback word produced at call time; reachable only on the 14-day-VRF-dead gameover path (_gameOverEntropy :1335). | NEEDS-PROOF | yes | INFO | yes |
| `RNGF-ADV-13` | AdvanceModule.sol:1849-1851 (rawFulfillRandomWords mid-day branch, rngLockedFlag==false: index=LR_INDEX-1; lootboxRngWordByIndex[index]=word; emit) | Mid-day request reserved the index at requestLootboxRng (LR_INDEX advanced :1146, LR_MID_DAY=1 :1138), ticket queue swap-frozen :1137 — all before this callback. word=randomWords[0] (raw VRF :1841). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-14` | AdvanceModule.sol:1876-1883 (derivedWord=keccak256(vrfWord,gapDay); rngWordByDay[gapDay]=derivedWord; coinflip.processCoinflipPayouts(0,derivedWord,gapDay)) | vrfWord = first post-gap rngWordCurrent (VRF callback :1846, single-shot); gap range [dailyIdx+1,day) calendar-derived. Entered once per lock window, gated by rngWordByDay[idx+1]==0 at rngGate :1224. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-15` | AdvanceModule.sol:1903-1907 (fallbackWord=keccak256(vrfWord,i); lootboxRngWordByIndex[i]=fallbackWord) scanning backwards from LR_INDEX-1 until a filled index :1900-1901 | vrfWord = fresh post-gap VRF word (callback-committed); orphaned indices i and their box commitment slots written at purchase, before this fresh word existed. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-16` | contracts/DegenerusQuests.sol:395-402 (bonusEntropy=(entropy>>128)\|(entropy<<128); bonusType=_bonusQuestType(bonusEntropy,...); seed slot 1). Dispatched from advanceGame->rngGate AdvanceModule:1245-1249. | entropy=currentWord (frozen daily word RNGF-ADV-03 :1930) passed to quests.rollDailyQuest at AdvanceModule:1246. day arg pins the roll to one wall-clock day (idempotent: quests[0].day==day early-returns Quests :383). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-ADV-17` | contracts/DegenerusQuests.sol:2016 (levelQuestType=_bonusQuestType(entropy,type(uint8).max,decAllowed); ++levelQuestVersion). Dispatched at level transition from advanceGame AdvanceModule:543. | entropy=rngWord (frozen daily word, rngGate :380) passed to quests.rollLevelQuest at AdvanceModule:543, on the lastPurchase consolidation leg with rngLockedFlag held. | FROZEN-AT-COMMIT | yes | n/a | yes |

#### `RNGF-ADV-01` — midday-lootbox-readiness-gate (advanceGame same-day drain word presence)
- **Invariant:** The word read at :226 is the per-index VRF word for the index reserved at the mid-day request; LR_INDEX advanced at request so (LR_INDEX-1) is request-fixed; queue swap-frozen at :1137; gate only reverts until the word lands, derives no outcome.
- **Frozen inputs:** LR_INDEX advanced by _lrAdvanceIndexClearPending at request (:1146/:1694); LR_MID_DAY latch set only at :1138 under rngLockedFlag==false; tickets frozen by write->read _swapTicketSlot :1137; lootboxRngWordByIndex[index] zero until VRF callback :1850 or daily assign :288
- **Verification recipe:** grep LR_MID_DAY latch gate at :225; lootboxRngWordByIndex[(LR_INDEX-1)] read at :226-229 revert RngNotReady on word==0; _lrAdvanceIndexClearPending only at :1146/:1694; _swapTicketSlot at :1137 before _requestVrfWord :1143; LR_MID_DAY=1 written ONLY at :1138.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: advance LR_INDEX without setting LR_MID_DAY to read a stale index. Defeated: gate at :225 fires only when latch set; latch set only at :1138 and requestLootboxRng reverts at :1089 if already set (no stacking); :1098 reverts if rngRequestTime!=0 so advances serialize 1:1 with fulfillment. Gate only reverts (RngNotReady :229). All recipe greps reproduce verbatim.
- **Backing:** test/fuzz/LootboxRngLifecycle.t.sol; test/fuzz/invariant/RngWindowFreeze.inv.t.sol

#### `RNGF-ADV-02` — midday-lootbox-word-assign on new-day path (rngWordCurrent+totalFlipReversals -> lootboxRngWordByIndex[preIdx])
- **Invariant:** cw = daily VRF word (rngWordCurrent) + accumulated FLIP nudges (totalFlipReversals); both sealed before this read; reverseFlip reverts while rngLockedFlag set (flag set at request :1702 before fulfillment); no actor input between request and assign changes preIdx, word, or nudge total.
- **Frozen inputs:** rngWordCurrent single-shot (rawFulfillRandomWords :1846, re-entry guard :1839); totalFlipReversals incremented only in reverseFlip (DegenerusGame.sol:1826), reverts at :1818 if rngLockedFlag; preIdx (LR_INDEX-1) request-advanced; lootboxRngWordByIndex[preIdx] ==0 guard at :280
- **Verification recipe:** grep totalFlipReversals sole writer reverseFlip DegenerusGame.sol:1826 + rngLockedFlag revert :1818; rngLockedFlag=true in _finalizeRngRequest :1702 (request time); rawFulfillRandomWords :1839 single-shot.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-arm totalFlipReversals between word becoming knowable and the assign. Defeated: reverseFlip reverts at :1818 if rngLockedFlag, set at request :1702 before callback writes rngWordCurrent :1846 — only mutator gated closed across request->assign; no fresh word exists while nudges mutable. Noted out-of-scope: :280-289 reads totalFlipReversals without zeroing while _applyDailyRng :1925-1927 adds-and-zeros — a value-consistency question, not actor-grindability.
- **Backing:** test/fuzz/invariant/RngWindowFreeze.inv.t.sol; test/fuzz/RngLockRotationDeterminism.t.sol

#### `RNGF-ADV-03` — daily-word producer + nudge application (_applyDailyRng — rngWordByDay[day] commit point)
- **Invariant:** rngWordByDay[day] determined once per day from frozen VRF rawWord + totalFlipReversals (frozen by rngLockedFlag gate); rngGate :1209-1210 short-circuits if !=0; day pure function of block.timestamp.
- **Frozen inputs:** rawWord=rngWordCurrent single-shot (:1846, guard :1839); nudges=totalFlipReversals frozen (reverseFlip reverts under rngLockedFlag set at :1702); consumed-and-zeroed :1927; day pure timestamp _simulatedDayIndexAt; RNGREUSE clamp :184-186 only lowers day to dIdx+1; rngWordByDay[day] one-shot (rngGate :1209)
- **Verification recipe:** grep rngWordByDay[day]=finalWord single write :1930; recordedWord!=0 short-circuit rngGate :1210 (idempotent/day); _simulatedDayIndexAt pure DegenerusGameStorage.sol:1476.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-derive/overwrite rngWordByDay[day] or bias day to a known cached word. Defeated: single write :1930; rngGate :1209-1210 early-returns when recordedWord!=0 (idempotent); finalWord=single-shot word + frozen nudges; day pure-timestamp. No live SLOAD in derivation.
- **Backing:** test/fuzz/invariant/RngWindowFreeze.inv.t.sol; test/fuzz/VRFCore.t.sol; test/fuzz/RngLockRotationDeterminism.t.sol

#### `RNGF-ADV-04` — salted FLIP/coin daily jackpot seed (purchaseLevel==1 second draw, BONUS_TRAITS_TAG salt)
- **Invariant:** saltedRng is fixed-salt keccak re-derivation of the frozen daily word; salt + level args are compile-time/level constants; seed fully determined by frozen rngWord. Winner-selection trait-pool freeze is a separate cross-contract consumer.
- **Frozen inputs:** rngWord frozen daily word from rngGate :380; BONUS_TRAITS_TAG bytes32 private constant :139; level/range args (1,2,5) constants
- **Verification recipe:** grep BONUS_TRAITS_TAG const :139 use :482; rngWord at :380 is rngGate return. Predicate: seed=keccak(frozen rngWord, constant salt); no live SLOAD.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: feed a live/player-controllable input into the salted seed. Defeated: saltedRng=keccak256(rngWord,BONUS_TRAITS_TAG), rngWord frozen, tag compile-time constant. Pure keccak; no live SLOAD. Downstream JackpotModule trait-pool freeze correctly out of scope.
- **Backing:** n/a (direct source argument)

#### `RNGF-ADV-05` — future-take additive-random + triangular +-25% variance (_consolidatePoolsAndRewardJackpots)
- **Invariant:** Additive-random and triangular variance are pure bit-slices/modulos of the frozen daily word against memory pool magnitudes; no player-selectable preimage; pools are advance-controlled memory locals under held lock.
- **Frozen inputs:** rngWord frozen daily word (:380->:529); memNext/memFuture/memCurrent/memYieldAcc memory snapshots :835-839 under held lock; elapsed (day-psd-7) calendar-derived :843-844; ADDITIVE_RANDOM_BPS, NEXT_SKIM_VARIANCE_BPS constants
- **Verification recipe:** grep rngWord % (ADDITIVE_RANDOM_BPS :875; rngWord>>64 / >>192 :889-890; caller passes rngGate word :529.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: mutate a pool magnitude between word commit and the bit-slice rolls to bias variance. Defeated: additive (:875) and triangular (:889-891) are pure bit-slices/modulos of the frozen word; pool operands are in-memory locals loaded under held rngLockedFlag within a single synchronous (delegatecall-free) block.
- **Backing:** test/fuzz/V56FreezeSolvency.t.sol; test/fuzz/V55FreezeDeterminism.t.sol

#### `RNGF-ADV-06` — BAF fire gate (bit-0 daily flip) + dispatch to runBafJackpot
- **Invariant:** BAF fire decision is bit 0 of the frozen daily word; no actor input at the gate changes it. Fired BAF forwards SAME frozen word to runBafJackpot — winner selection samples player-mutable pools (separate seam), but fire/skip is sealed at the daily word commit.
- **Frozen inputs:** rngWord bit 0 frozen daily word; bafPct/bafPoolWei from frozen lvl + baseMemFuture snapshot :918
- **Verification recipe:** grep (rngWord&1)==1 :928; bit-0 dual-use :1183-1184; markBafSkipped on losing branch :940.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: flip the BAF fire decision at :928 by injecting actor input. Defeated: fire/skip is (rngWord&1)==1 of frozen word; losing branch markBafSkipped :940; fired path forwards SAME frozen word; winner-pool selection correctly classified CROSS-CONTRACT-SEAM and deferred to DegenerusJackpots.runBafJackpot.
- **Backing:** test/fuzz/RngReuseJackpotStraddle.t.sol; DegenerusJackpots.runBafJackpot cluster

#### `RNGF-ADV-07` — x00 keep-roll (5d4 dice via EntropyLib.hash2, FUTURE_KEEP_TAG)
- **Invariant:** Keep-fraction dice are fixed-tag hash2 of the frozen daily word; salt is a constant; operands are memory pool magnitudes. No player input enters seed or dice.
- **Frozen inputs:** rngWord frozen daily word; FUTURE_KEEP_TAG bytes32 private constant :138; memFuture/memCurrent memory snapshots under held lock
- **Verification recipe:** grep FUTURE_KEEP_TAG const :138 use :964; guard (lvl%100)==0 :963.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias the 5d4 keep-roll seed. Defeated: seed=hash2(rngWord, FUTURE_KEEP_TAG) with rngWord frozen and tag a private constant; five dice pure bit-slices; keepBps=3000+total*3500/15; guard (lvl%100)==0. Operands memory pool magnitudes under held lock.
- **Backing:** n/a (direct source argument)

#### `RNGF-ADV-08` — redemption roll (rngGate normal path) — sDGNRS gambling-burn pool resolution
- **Invariant:** Roll bits [8+] from the frozen daily word for the resolving day; toResolve is a submit-stamped single-pool sentinel (sDGNRS :1042), not selectable at resolution; resolve single-shot per day; magnitude (pendingByDay[day].ethBase) segregated at submit, re-arm blocked by BurnsBlockedBeforeDailyRng (sDGNRS :1036).
- **Frozen inputs:** currentWord frozen daily word; toResolve single sentinel day stamped at submit (sDGNRS :1043), GAME-gated resolve :743; redemption magnitude segregated at submit; re-arm blocked (sDGNRS :1036/:1042)
- **Live inputs (scale-not-select):** cross-contract redemption-pool magnitude read live in sDGNRS.resolveRedemptionPeriod but cannot bias WHICH word or HOW the roll derives — roll is f(frozen word) only; magnitude frozen at submit
- **Verification recipe:** grep (currentWord>>8)%151)+25 :1262; sDGNRS pendingResolveDay def :240 getter :538 stamp :1043 clear :767; BurnsBlockedBeforeDailyRng :1036 + PriorDayUnresolved :1042; resolveRedemptionPeriod GAME-only :743 one-shot delete :764.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-arm magnitude against a now-known resolving word, or select which day resolves. Defeated: roll=uint16((currentWord>>8)%151+25), toResolve submit-stamped sentinel; submit gate requires rngWordForDay(currentPeriod)!=0 while pool resolves on a LATER advance day undrawn at submit; only ethBase writers are the gated += at submit (sDGNRS:1106) and delete at resolve; resolve GAME-only one-shot. Halmos bounds [25,175].
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/StakedStonkRedemption.t.sol; test/fuzz/RedemptionEdgeCases.t.sol

#### `RNGF-ADV-09` — redemption roll (gameover normal-word path) — _gameOverEntropy
- **Invariant:** Identical shape to ADV-08 on the gameover branch: roll bits [8+] from frozen daily word; resolving day = sentinel; resolution single-shot GAME-gated. Gameover latch does not change which word or how the roll derives.
- **Frozen inputs:** currentWord frozen via _applyDailyRng :1309; toResolve sentinel (sDGNRS :1043), GAME-gated :743, one-shot; redemption magnitude frozen at submit, re-arm blocked (sDGNRS :1036/:1042)
- **Live inputs (scale-not-select):** cross-contract redemption magnitude (scales payout, not roll-derivation; frozen at submit)
- **Verification recipe:** grep second (currentWord>>8)%151)+25 hit :1324 inside _gameOverEntropy; branch guard rngWordCurrent!=0 :1308; same sDGNRS anchors as ADV-08.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: identical seam to ADV-08 on gameover normal-word branch. Defeated: roll=uint16((currentWord>>8)%151+25), currentWord=_applyDailyRng(day,rngWordCurrent) reached only when fresh VRF word exists (:1308); same sDGNRS sentinel/gate anchors. Gameover latch changes neither word nor derivation.
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/VRFStallEdgeCases.t.sol; test/fuzz/StallResilience.t.sol

#### `RNGF-ADV-10` — redemption roll (gameover prevrandao-fallback path) — _gameOverEntropy fallback
- **Invariant:** Roll derived from the fallback word sealed when this gameover advance runs. Nudges pre-subtracted at :1343 (fallback path never set rngLockedFlag) neutralizing nudge steering. ONLY non-frozen entropy = block.prevrandao (1-bit validator bias) on a gameover-only path after 14-day VRF death; cannot select WHICH historical words or the resolving day (sentinel).
- **Frozen inputs:** historical words rngWordByDay[1..searchLimit] immutable committed VRF words (RNGF-ADV-12); currentDay pure timestamp; toResolve sentinel stamped at submit (sDGNRS :1043); totalFlipReversals pre-subtracted :1343 so nudges cannot steer
- **Live inputs (scale-not-select):** block.prevrandao: 1-bit validator bias on the gameover-only fallback word; documented acceptable trade-off :1385-1391. Cannot bias WHICH slot/day; only adds 1 bit. NEEDS-PROOF retained because the 1-bit bias is a live validator input.
- **Verification recipe:** grep (fallbackWord>>8)%151)+25 :1359; fallbackWord -= totalFlipReversals :1343; block.prevrandao :1415; 14-day delay GAMEOVER_RNG_FALLBACK_DELAY :118/:1335; rngRequestTime=0 release :1367.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Correctly classified NEEDS-PROOF with a source-documented residual; freeze for the actor-controllable surface HOLDS. (a) nudges pre-subtracted at :1343 cancel the += inside _applyDailyRng, neutralizing nudge steering. (b) 1-bit prevrandao validator bias retained as residual: a proposer can choose between 2 candidate fallback words -> 2 candidate rolls, ONLY post-gameover after 14d VRF death; cannot select WHICH historical word slot or the resolving day. Severity INFO: extreme-tail, gameover-only, 1-bit, magnitude-bounded by the [25,175] cap.
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/VRFStallEdgeCases.t.sol; test/fuzz/VrfRotationLiveness.t.sol

#### `RNGF-ADV-11` — _finalizeLootboxRng (daily/gameover/fallback word -> current lootbox index)
- **Invariant:** Current lootbox index receives the frozen day/gameover/fallback word exactly once (==0 guard :1289). Index request-advanced (not selectable); per-index box commitment slots written at purchase before this word existed.
- **Frozen inputs:** rngWord frozen applied word (RNGF-ADV-03/09/10); index=LR_INDEX-1 request-advanced (:1694/:1146); lootboxRngWordByIndex[index] one-shot (==0 guard :1289)
- **Verification recipe:** grep function _finalizeLootboxRng :1287 body :1288-1292; callers :1268/:1329/:1364; LR_INDEX advanced only at request paths.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: select or re-point the finalized lootbox index, or land a second word on the same index. Defeated: _finalizeLootboxRng writes lootboxRngWordByIndex[LR_INDEX-1]=rngWord guarded ==0 (one-shot); index request-advanced with isRetry suppression :1684-1695 so a retry never double-advances; per-index box slots written at purchase. Callers pass the frozen applied word.
- **Backing:** test/fuzz/LootboxRngLifecycle.t.sol; test/fuzz/invariant/RngWindowFreeze.inv.t.sol

#### `RNGF-ADV-12` — historical-RNG prevrandao fallback word producer (_getHistoricalRngFallback)
- **Invariant:** Fallback word = keccak of immutable historical VRF words + pure currentDay + block.prevrandao. Historical words and day fully frozen; only live input block.prevrandao (1-bit propose-or-skip bias). Gameover-only after 14-day VRF death — one-time terminal entropy, not a recurring grindable draw.
- **Frozen inputs:** rngWordByDay[1..min(currentDay,30)] (up to 5 found) immutable committed VRF words; currentDay pure timestamp; searchLimit/found deterministic loop bounds
- **Live inputs (scale-not-select):** block.prevrandao 1-bit validator manipulation, accepted by design for gameover-only fallback after VRF confirmed dead (:1385-1391). At level 0 no VRF history so prevrandao-only entropy irrelevant (:1389-1391).
- **Verification recipe:** grep function _getHistoricalRngFallback :1394 body :1397-1418; sole caller _gameOverEntropy :1337 under elapsed>=GAMEOVER_RNG_FALLBACK_DELAY :1335; view, reads only rngWordByDay + block.prevrandao.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Correctly classified NEEDS-PROOF; frozen surface HOLDS, prevrandao residual retained (this is the producer for ADV-10). Word/day selection defeated: combined is a deterministic hash2-chain over the first up-to-5 nonzero immutable committed VRF words; currentDay pure timestamp. ONLY live input is block.prevrandao (1-bit), view fn, sole caller _gameOverEntropy under 14d guard. Cannot select which slot/day. Severity INFO.
- **Backing:** test/fuzz/VRFStallEdgeCases.t.sol; test/fuzz/StallResilience.t.sol; test/fuzz/VrfRotationLiveness.t.sol

#### `RNGF-ADV-13` — rawFulfillRandomWords mid-day direct lootbox finalize (VRF callback)
- **Invariant:** Mid-day callback writes raw VRF word into the request-reserved index; access VRF-coordinator-only :1838 and single-shot (requestId==vrfRequestId && rngWordCurrent==0 :1839). Branch selected by rngLockedFlag :1844 — mid-day request leaves flag false; index request-advanced; tickets swap-frozen.
- **Frozen inputs:** index=LR_INDEX-1 advanced at requestLootboxRng :1146; LR_MID_DAY latch set only at :1138 selects this branch; word=randomWords[0] raw VRF (coordinator :1838); single-shot requestId match + rngWordCurrent==0 :1839; vrfRequestId/rngRequestTime cleared :1852-1853; ticketQueue swap-frozen :1137
- **Verification recipe:** grep function rawFulfillRandomWords :1834 mid-day branch :1847-1854; coordinator-only :1838; requestId/rngWordCurrent guard :1839; rngLockedFlag selects branch :1844; LR_INDEX advance/swap :1137-1146.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: steer the mid-day callback into the daily branch, re-point the index, or land a second word. Defeated: rawFulfillRandomWords coordinator-only :1838 and single-shot :1839; daily-vs-mid-day selected by rngLockedFlag :1844 — mid-day request leaves flag false; writes lootboxRngWordByIndex[LR_INDEX-1]=randomWords[0] into the request-reserved index and clears vrfRequestId/rngRequestTime.
- **Backing:** test/fuzz/LootboxRngLifecycle.t.sol; test/fuzz/VrfRotationOrphanIndex.t.sol; test/fuzz/RngLockRotationDeterminism.t.sol

#### `RNGF-ADV-14` — _backfillGapDays derived per-day words (VRF-stall gap recovery)
- **Invariant:** Each gap day's word = keccak(first-post-gap VRF word, gapDay) — deterministic per-day derivation of a freshly committed VRF word that did NOT exist while players placed coinflip bets for the stalled days. gapDay purely calendar; idempotent via rngWordByDay[idx+1]==0 guard.
- **Frozen inputs:** vrfWord first post-gap VRF word single-shot callback; gapDay calendar index in [startDay,endDay) pure; coinflip bets for gap days committed in real time during the stall, before this word; 120-day cap :1874 bounds the loop
- **Verification recipe:** grep function _backfillGapDays :1867 body :1875-1888; rngWordByDay[idx+1]==0 gate :1224; STAGE_GAP_BACKFILLED decouple :402-405.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: front-run a gap-day bet against a knowable backfill word, or double-run the backfill. Defeated: derivedWord=keccak256(vrfWord,gapDay), vrfWord first post-gap single-shot word that did not exist while players bet; gapDay pure calendar; rngGate :1224 gates on rngWordByDay[idx+1]==0 (at most once per lock window). 120-day cap + STAGE segregation keep each tx under the gas ceiling.
- **Backing:** test/edge/BackfillIdempotency.test.js; test/fuzz/VRFStallEdgeCases.t.sol; test/fuzz/StallResilience.t.sol

#### `RNGF-ADV-15` — _backfillOrphanedLootboxIndices derived words (stall-orphaned lootbox indices)
- **Invariant:** Each orphaned index gets keccak(fresh VRF word, i) exactly once (scan stops at first filled index :1901). Per-index box deposit data committed at buy time, so the derived word cannot be front-run; index i is the monotonic cursor walk, not actor-selectable.
- **Frozen inputs:** vrfWord fresh post-gap VRF word (callback-committed); index i backward scan from LR_INDEX-1 :1900 deterministic; stops at first filled :1901; per-index box commitment slots written at purchase before this word
- **Verification recipe:** grep function _backfillOrphanedLootboxIndices :1895 body :1896-1913; break on filled index :1901; keccak(vrfWord,i) :1903; sole caller rngGate :1230.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: front-run an orphaned-index box deposit against a knowable derived word, or select index i. Defeated: each orphaned index gets keccak256(vrfWord,i) where vrfWord is the fresh post-gap word; backward scan from LR_INDEX-1 stops at the first filled index :1901 (deterministic monotonic walk, each index written at most once); box slots written at purchase. Sole caller rngGate :1230.
- **Backing:** test/fuzz/LootboxRngLifecycle.t.sol; test/fuzz/VrfRotationOrphanIndex.t.sol

#### `RNGF-ADV-16` — daily quest type roll (rollDailyQuest slot-1 weighted type select)
- **Invariant:** Slot-1 quest TYPE is a fixed bit-rotation + weighted select of the frozen daily word; onlyGame-gated (Quests :381), one-shot per day (:383). Global daily quest (not per-player) so no per-player edge; forceMintFlip is a frozen-protocol-state flag computed in advanceGame.
- **Frozen inputs:** entropy=currentWord frozen daily word; day pure timestamp; one-shot per day (Quests :383); forceMintFlip frozen protocol flag computed in advanceGame; onlyGame gate Quests :381
- **Verification recipe:** grep function rollDailyQuest Quests :381 (onlyGame) idempotency :383; quests.rollDailyQuest AdvanceModule :1245 with currentWord; bonusEntropy rotation :395 + _bonusQuestType weighted select.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: steer the daily quest TYPE for per-player gain, or re-roll. Defeated: rollDailyQuest onlyGame (:381) and one-shot per day (:383); bonusEntropy=(entropy>>128)|(entropy<<128) is a fixed bit-rotation of the frozen daily word fed to _bonusQuestType weighted select. Global quest (not per-player) so even a known type yields no edge; forceMintFlip frozen. No live SLOAD in seed.
- **Backing:** n/a (direct source argument)

#### `RNGF-ADV-17` — level quest type roll (rollLevelQuest type select)
- **Invariant:** Level quest TYPE is a weighted select of the frozen daily word; onlyGame-gated (Quests :2014). decAllowed (_canRollDecimatorWindow) only reshapes the type-weight space, not the seed; type is global, not per-player-selectable.
- **Frozen inputs:** entropy=rngWord frozen daily word; onlyGame gate Quests :2014; levelQuestVersion monotonic counter :2017
- **Live inputs (scale-not-select):** decAllowed (_canRollDecimatorQuest) live decimator-window read; reshapes type-weight space (pool membership), not seed bits, and is advance-controlled protocol state (decWindowOpen set _finalizeRngRequest :1739) — cannot bias selection for a player
- **Verification recipe:** grep function rollLevelQuest Quests :2014 (onlyGame); quests.rollLevelQuest AdvanceModule :543 with rngWord; decWindowOpen set _finalizeRngRequest :1739/:1743.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias the level quest TYPE via the live decimator-window read (decAllowed) or steer the seed. Defeated: rollLevelQuest onlyGame; levelQuestType=_bonusQuestType(entropy,type(uint8).max,decAllowed) with entropy=frozen daily word on lastPurchase leg under held rngLockedFlag. decAllowed only RESHAPES pool membership, not seed bits, and is advance-controlled (decWindowOpen :1739/:1743). Type is global, not per-player-selectable.
- **Backing:** n/a (direct source argument)

---

## Cluster: Game-and-getters — DegenerusGame.sol VRF-derivation sites + cross-contract seams

_All 10 cluster consumers exist at HEAD; no removals. The deity-boon SEED commit point is precisely _applyDailyRng @ AdvanceModule:1930. The far-future MEMBERSHIP write-lock that freezes sampleFarFutureTickets is the RngLocked() revert at Storage:621/653/715 + _livenessTriggered() at :618/650/703, spanning rngLockedFlag-set@:1702 through _unlockRng:1805. traitBurnTicket membership is written ONLY by the advance-stack MintModule._raritySymbolBatch — no player-callable mid-resolution writer. The deity boon stays the sole MUTABLE-INPUT (recipient/slot selection from a fixed daily menu, the word frozen). VERIFY caveat: the committed word at P is rawWord+totalFlipReversals (nudge), still FROZEN-AT-COMMIT (reverseFlip gated by rngLockedFlag before the word exists) — entries DEITYDATA-01/BAFTRAIT-05/BAFFAR-06 omit the nudge term (INFO doc gap, not a freeze break)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-DEITYDATA-01` | contracts/DegenerusGame.sol:890 (dailySeed=rngWordByDay[day]; day=_simulatedDayIndex() :882) | AdvanceModule.sol:1930 (_applyDailyRng: rngWordByDay[day]=finalWord) — per-wall-clock-day word commit P. | FROZEN-AT-COMMIT | yes | INFO | yes |
| `RNGF-DEITYISSUE-02` | contracts/modules/DegenerusGameLootboxModule.sol:2322 (_deityBoonForSlot: seed=keccak256(rngWord,deity,day,slot); roll=seed%total :2327), from DegenerusGame.sol:898 issueDeityBoon -> LootboxModule.issueDeityBoon:1135 (rngWord=rngWordByDay[day] :1142, boonType :1161) | AdvanceModule.sol:1930 (_applyDailyRng: rngWordByDay[day]=finalWord) — same per-day word commit P; deity word read at issueDeityBoon:1142, rejected if zero :1143. | MUTABLE-INPUT | yes | INFO | yes |
| `RNGF-DEITYVIEW-03` | contracts/DeityBoonViewer.sol:118 (seed=keccak256(abi.encode(dailySeed,deity,d,i)); slots[i]=_boonFromRoll(seed%total,...)) | AdvanceModule.sol:1930 (rngWordByDay[day]=finalWord); dailySeed fetched cross-contract via IDeityBoonDataSource(game).deityBoonData(deity) DeityBoonViewer.sol:105 | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-RNGGETTER-04` | contracts/DegenerusGame.sol:2090 (return rngWordByDay[day]) — value consumed in cross-contract callers (RNGF-REDEEMSEAM-08, RNGF-SUBMITGATE-10) | AdvanceModule.sol:1930 (rngWordByDay[day]=finalWord) for the queried day; for gap days _backfillGapDays writes rngWordByDay[gapDay] — both single-shot. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAFTRAIT-05` | contracts/DegenerusGame.sol:2318-2329 (traitSel=uint8(entropy>>24); arr=traitBurnTicket[targetLvl][traitSel]; start=(entropy>>40)%len; tickets[i]=arr[(start+i)%len]) — consumed DegenerusJackpots.sol:389 in runBafJackpot slice E | AdvanceModule.sol:1930 (rngWordByDay[day]=finalWord BAF seed) AND trait-bucket MEMBERSHIP commitment: traitBurnTicket[lvl] written only by the advance ticket-processing stack MintModule.sol:794-813 (_raritySymbolBatch), keyed at resolution level; no player adds entries between word-knowability and the sampler. | CROSS-CONTRACT-SEAM | yes | INFO | yes |
| `RNGF-BAFFAR-06` | contracts/DegenerusGame.sol:2347-2357 (word=keccak256(word,s); candidate=currentLvl+5+word%95; queue=ticketQueue[_tqFarFutureKey(candidate)]; idx=(word>>32)%len; winner=queue[idx]) — consumed DegenerusJackpots.sol:318 in runBafJackpot far-future slices D/D2 | AdvanceModule.sol:1930 (rngWordByDay[day]=finalWord BAF seed) AND far-future ticketQueue membership: far-future pushes revert RngLocked() while rngLockedFlag set (Storage:621/653/715) and under _livenessTriggered (Storage:618/650/703). rngLockedFlag set at VRF REQUEST (AdvanceModule:1702) before the word, cleared only at _unlockRng:1805. | CROSS-CONTRACT-SEAM | yes | INFO | yes |
| `RNGF-BOXPROBE-07` | contracts/DegenerusGame.sol:1419 (if (lootboxRngWordByIndex[idx]==0) return false) — equality-to-zero probe inside boxesPending | AdvanceModule.sol _finalizeLootboxRng / rawFulfillRandomWords (lootboxRngWordByIndex[index]=word) — per-index single-shot word landing at the request-advanced LR_INDEX-1 frontier | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-REDEEMSEAM-08` | contracts/sDGNRS.sol:916-920 (rngWord=game.rngWordForDay(day+1) :916; entropy=EntropyLib.hash2(rngWord,uint160(player)) :917; game.resolveRedemptionLootbox{...}(player,lootboxEth,entropy,actScore) :920) | Game-side: AdvanceModule.sol:1930 writes rngWordByDay[day+1] when the advance settling day+1 runs (resolving word). sDGNRS-side: (player,day) claim slot + lootboxEth base sealed at submit (_submitGamblingClaimFrom sDGNRS:1021+); submit pins currentPeriod==dailyIdx (drawn day) so day+1 NOT drawn at submit (sDGNRS:1030-1036). | CROSS-CONTRACT-SEAM | yes | HIGH | yes |
| `RNGF-FLIPESCROW-09` | contracts/sDGNRS.sol:888-893 (read (rewardPercent,flipWon)=coinflip.getCoinflipDayResult(day+1) :888; payout principal + principal*rewardPercent% via creditFlip :891-893) | contracts/Coinflip.sol processCoinflipPayouts (rngWord-derived win bit + reward-percent stored into coinflipDayResultPacked for the resolved day) under onlyDegenerusGameContract during the advance settling day+1; (player,day) claim + flipEscrow slice sealed at submit (sDGNRS:1021+). | CROSS-CONTRACT-SEAM | yes | HIGH | yes |
| `RNGF-SUBMITGATE-10` | contracts/sDGNRS.sol:1036 (if (game.rngWordForDay(currentPeriod)==0) revert BurnsBlockedBeforeDailyRng()) | AdvanceModule.sol:1930 (rngWordByDay[currentPeriod]=finalWord) — the gate admits only once the current wall-clock day's word is committed; currentPeriod=GameTimeLib.currentDayIndex() (sDGNRS:1028, pure timestamp). | FROZEN-AT-COMMIT | yes | n/a | yes |

#### `RNGF-DEITYDATA-01` — deityBoonData daily-boon seed read (view)
- **Invariant:** deityBoonData returns dailySeed=rngWordByDay[day] for day=_simulatedDayIndex(). Pure view, derives NO outcome; value fully determined at P (:1930); day pure block.timestamp, not caller-selectable; dailySeed 0 until P for that wall-clock day.
- **Frozen inputs:** dailySeed == rngWordByDay[day] single-shot written once at :1930, never overwritten (rngGate :1209-1210); day == _simulatedDayIndex() = GameTimeLib.currentDayIndex() pure block.timestamp; deity param indexes deityBoonPacked used-mask only; does not enter dailySeed
- **Live inputs (scale-not-select):** usedMask/decimatorOpen/deityPassAvailable read live but are state-display fields, not part of the consumed VRF word; reshape downstream weight space (RNGF-DEITYISSUE-02), not the seed
- **Verification recipe:** grep function deityBoonData :869; day=_simulatedDayIndex() :882, dailySeed=rngWordByDay[day] :890 (no keccak); sole write rngWordByDay[day] AdvanceModule :1930; _simulatedDayIndex Storage :1429 (pure timestamp); rngGate short-circuit :1210.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Refutation: the committed word at P is NOT pure VRF — finalWord=rawWord+totalFlipReversals (actor-purchased nudge). Defeated: reverseFlip gated `if(rngLockedFlag) revert` (DegenerusGame.sol:1818) and the word only becomes knowable inside the locked callback branch (:1844-1846); rngLockedFlag set at request :1702, cleared only at _unlockRng :1805 AFTER commit and consumption — no window where the word is knowable AND a nudge can be added; nudge frozen blind before word exists. deityBoonData pure view of single-shot rngWordByDay[day]. Freeze holds. INFO: the entry's invariant/frozenInputs text mischaracterizes the committed word as pure VRF and omits the nudge term (same FROZEN-AT-COMMIT class, should be documented as a contributing frozen input).
- **Backing:** test/fuzz/RngLockDeterminism.t.sol; test/fuzz/V55FreezeDeterminism.t.sol

#### `RNGF-DEITYISSUE-02` — issueDeityBoon -> _deityBoonForSlot deity boon-type derivation
- **Invariant:** For fixed (deity,day,slot) the boon TYPE is a pure deterministic boonType=_boonFromRoll(keccak256(rngWord,deity,day,slot)%total). rngWord determined at P; deity bound to the deity-pass holder (not spoofable); day wall-clock; per-slot mask + per-recipient-per-day dedup make each (deity,day,slot) issuable once. NO actor input between P and consumption changes WHICH word seeds the roll or the mapping. Residual mutability is only recipient/slot selection from the fixed daily menu — cannot re-roll a slot's type.
- **Frozen inputs:** rngWord == rngWordByDay[day] single-shot at :1930; non-zero-gated :1143; deity _resolvePlayer-resolved holder (:903), deity-pass bit enforced :1139 — not spoofable; day _simulatedDayIndex() wall-clock :1141; slot caller-chosen 0..2 bounded :1138 one-shot per (deity,day,slot) via mask :1152-1156; recipient dedup'd per day :1150,:1157; not in seed
- **Live inputs (scale-not-select):** decimatorAllowed (_isDecimatorWindow :1159) and deityPassAvailable (deityPassOwners.length<MAX :1160): live reads reshape `total` and the boon-type mapping — monotone/structural game-state, not flippable on demand to steer a favorable boon
- **Verification recipe:** grep function _deityBoonForSlot LootboxModule :2314; seed=keccak256(abi.encode(rngWord,deity,day,slot)) :2322, roll=seed%total :2327; issueDeityBoon rngWord=rngWordByDay[day] :1142 revert if 0 :1143, deity-pass bit :1139, slot bound :1138, per-slot mask :1152-1156, per-recipient dedup :1150; Game entry deity=_resolvePlayer(deity) :903.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Recipe reproduces exactly. Refutation: `total` depends on the LIVE deityPassAvailable=deityPassOwners.length<32 and issueDeityBoon has NO rngLockedFlag guard; flipping true->false yields total=1498->1458, a different roll -> possibly a different boonType; deity times issuance around the flip to pick the better of two. Defeated: (1) the flip is a one-way one-time GLOBAL latch — deityPassOwners only ever grows (sole writer WhaleModule:604, no pop), capped 32, crosses 31->32 once forever; (2) the deity cannot self-trigger (needs the 32nd pass bought by a different actor; HAS_DEITY_PASS guard WhaleModule:551 blocks a 2nd pass per address); (3) no atomic compose/multicall lets the deity sandwich their own issuance around a third party's purchase; (4) at most a binary pick between two pre-determined boon types at a single irreversible boundary, not a seed re-roll. Word-freeze HOLDS; correctly MUTABLE-INPUT. INFO: the 31->32 boundary presents a one-shot two-way menu pick to a deity opportunistically issuing at that exact boundary — bounded/de-duped, third-party-dependent, once-ever.
- **Backing:** test/fuzz/RngFreezeAndRemovalProofs.t.sol

#### `RNGF-DEITYVIEW-03` — DeityBoonViewer.deityBoonSlots preview re-derivation (view mirror)
- **Invariant:** READ-ONLY preview reproducing RNGF-DEITYISSUE-02's mapping: keccak256(dailySeed,deity,day,i)%total. Derives no on-chain outcome, pays nothing. dailySeed = Game's committed rngWordByDay[day]; if 0 returns empty menu (:112) mirroring the issuance gate. Faithful pure mirror; no actor input can desync it from the frozen word.
- **Frozen inputs:** dailySeed cross-contract read of Game.deityBoonData -> rngWordByDay[day], P-frozen at :1930; deity param and d (returned day) fixed per the deityBoonData call; i loop index 0..DEITY_DAILY_BOON_COUNT-1, not actor-chosen
- **Live inputs (scale-not-select):** decimatorOpen / deityPassAvailable returned by deityBoonData feed `total` :114-115 — same structural reshape as issuance; preview-only, no value derived
- **Verification recipe:** grep deityBoonData DeityBoonViewer :105 (dailySeed from Game); seed=keccak256(abi.encode(dailySeed,deity,d,i)) :118; empty-return on dailySeed==0 :112; diff preimage tuple vs _deityBoonForSlot:2322 — same fields, same _boonFromRoll.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Recipe reproduces exactly. DeityBoonViewer.deityBoonSlots reads IDeityBoonDataSource(game).deityBoonData(deity) :105, returns empty on dailySeed==0 :112, computes seed=keccak256(abi.encode(dailySeed,deity,d,i)) :118 — structurally identical preimage to _deityBoonForSlot's keccak256(abi.encode(rngWord,deity,day,slot)). _boonFromRoll private pure mirror. External view: no mutation, no payout. Desync defeated — same field tuple, same total computation, same dailySeed (= rngWordByDay[day], P-frozen). Freeze trivially holds (read-only).
- **Backing:** n/a (direct source argument)

#### `RNGF-RNGGETTER-04` — rngWordForDay public getter (cross-contract VRF-word accessor)
- **Invariant:** rngWordForDay(day) returns rngWordByDay[day], a PAST committed word (or 0). Getter derives nothing; manipulability lives in the CALLER's choice of which frozen day to read, constrained by the consumer (sDGNRS pins day to a drawn day at submit and reads day+1 undrawn at submit). Returned word immutable thereafter (rngGate :1210).
- **Frozen inputs:** rngWordByDay[day] single-shot per day at :1930 (or _backfillGapDays); never re-written (:1210); day arg caller-supplied but only indexes a frozen mapping; in sDGNRS the caller cannot read a future undrawn word to advantage (RNGF-REDEEMSEAM-08/10)
- **Verification recipe:** grep function rngWordForDay :2089-2091 single `return rngWordByDay[day]`; sole writers _applyDailyRng :1930 + _backfillGapDays (gap-gated :1224); consumers grep rngWordForDay sDGNRS :916 (day+1) and :1036 (presence gate).
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Recipe reproduces exactly: single `return rngWordByDay[day]` (:2089-2091), no derivation. Sole writers _applyDailyRng (:1930) and _backfillGapDays (:1880, disjoint gap days), single-shot, never re-written (:1210). Refutation: caller-supplied `day` could read a future undrawn word. Defeated in sDGNRS: reads at :916 (day+1, resolving word) and :1036 (presence gate); submit gate pins currentPeriod to a drawn day so day+1 is undrawn at submit and frozen by claim. Getter returns an immutable past word; manipulability lives in the constrained caller-day, discharged by the seam entries.
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/FlipRedeemWindow.t.sol

#### `RNGF-BAFTRAIT-05` — sampleTraitTicketsAtLevel BAF trait-ticket scatter sampler
- **Invariant:** entropy = EntropyLib.hash2(rngWord,salt) chained off the daily BAF word; traitSel, start, 4-ticket window are pure functions of that entropy, frozen at P. Sampled pool traitBurnTicket[targetLvl][traitSel] membership frozen because (a) BAF resolution runs in the SAME advanceGame tx as word application (atomic), (b) bucket pushes occur only inside the advance ticket-processing stack (MintModule:810), (c) ticket queueing reverts under _livenessTriggered. No actor input changes which word (onlyGame caller) or which buckets exist.
- **Frozen inputs:** entropy=hash2(rngWordByDay[day],salt) chain, P-frozen :1930; salt in-resolution counter; targetLvl chosen by onlyGame caller from frozen word; level monotone set only at request (Storage:237 + AdvanceModule:1733); traitBurnTicket[targetLvl][*] membership written only by MintModule._raritySymbolBatch:810 within the advance stack; rngWord BAF seed single-shot :1930; BAF fires only on (rngWord&1)==1 :928 same tx
- **Live inputs (scale-not-select):** arr.length read live but membership-frozen for the resolution (no player push reachable within the atomic advance tx); len bounds the modulo, cannot be inflated to bias selection
- **Verification recipe:** grep function sampleTraitTicketsAtLevel :2314; traitSel=uint8(entropy>>24) :2318, start=(entropy>>40)%len :2327, window loop :2328-2333; caller DegenerusJackpots :389 entropy=hash2 chain off rngWord :374,:298 onlyGame :252; traitBurnTicket sole push MintModule _raritySymbolBatch :794-813; runBafJackpot dispatched AdvanceModule :932 same tx; liveness gate Storage :618/:650/:703.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Recipe reproduces. Refutation: (a) push to a trait bucket between word becoming public (callback :1846) and the sampler? No — all player queueing paths pass rngBypass=false and revert under RngLocked (Storage:621/653/715) and _livenessTriggered across the whole rngLockedFlag window (:1702->:1805) that spans the BAF run; bucket pushes require ticket processing which runs only inside the advance. (b) rngBypass=true bypass? rngBypass true only at JackpotModule:647/906/2121 — jackpot-WINNER awards inside resolution (delegatecall targets), an output not a player input. entropy and bucket membership frozen; freeze holds. INFO: same nudge omission as DEITYDATA-01 (BAF rngWord is the nudged daily word, still frozen-at-commit, not noted in the entry).
- **Backing:** test/fuzz/BafFarFutureTickets.t.sol; test/fuzz/BafRebuyReconciliation.t.sol; test/fuzz/TicketRouting.t.sol

#### `RNGF-BAFFAR-06` — sampleFarFutureTickets BAF far-future ticketQueue sampler
- **Invariant:** Internal re-roll word starts from entropy=hash2(BAF daily word,salt) and is keccak-chained per attempt; candidate level walk and per-queue index are pure functions of that chain, frozen at P. Far-future queue writes doubly gated: RngLocked() while rngLockedFlag set, and _livenessTriggered — lock spans request-time (:1702) through _unlockRng (:1805), covering the whole word-public window. currentLvl read live but monotone and frozen for the resolution (set only at request :1733).
- **Frozen inputs:** entropy=hash2(rngWordByDay[day],salt) far-future chain P-frozen :1930; ticketQueue[_tqFarFutureKey(candidate)] membership blocked by RngLocked() (Storage:621/653/715) across request->unlock and by _livenessTriggered; rngWord BAF seed single-shot :1930; BAF fires (rngWord&1)==1 same tx :928
- **Live inputs (scale-not-select):** currentLvl=level (:2344) read live but level monotone non-decreasing written ONLY at the rngGate request path (:1733) — request-frozen for the in-flight resolution; queue.length read live but far-future membership write-locked across the window
- **Verification recipe:** grep function sampleFarFutureTickets :2341; word=keccak256(word,s) :2350, candidate=currentLvl+5+word%95 :2351, idx=(word>>32)%len :2356; caller DegenerusJackpots :318 entropy=hash2(entropy,salt) :317 onlyGame :252; isFarFuture && rngLockedFlag Storage :621/:653/:715; rngLockedFlag=true :1702 cleared :1805; level sole write :1733.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Recipe reproduces. Refutation: (1) the far-future write-lock carries a `!rngBypass` exception — set rngBypass=true to push far-future entries while word is public? Defeated: rngBypass is a function parameter; every player-facing caller passes false (MintModule/WhaleModule/GameAfkingModule/DecimatorModule/LootboxModule/DegenerusGame/Storage); rngBypass=true only at JackpotModule:647/906/2121 (internal jackpot-winner awards). (2) advance level mid-window to shift the candidate band? No — `level` has exactly one writer AdvanceModule:1733 (RNG-request path, monotone) + Storage:237 init; request-frozen for the in-flight resolution. Lock spans :1702->:1805. Far-future membership and candidate band frozen; selection pure over the P-frozen chained word. INFO: same nudge-omission note.
- **Backing:** test/fuzz/BafFarFutureTickets.t.sol; test/fuzz/TqFarFutureKey.t.sol; test/fuzz/TicketRouting.t.sol

#### `RNGF-BOXPROBE-07` — boxesPending frontier readiness probe (no outcome derived)
- **Invariant:** boxesPending derives NO outcome: a boolean discovery hint comparing the frontier index's word to zero. Reads lootboxRngWordByIndex[idx] only for !=0 presence, returns false during rngLockedFlag/liveness (:1413). Probed idx is the monotonic boxCursorIndex frontier clamped to LR_INDEX-1 — request-advanced, not player-selectable.
- **Frozen inputs:** lootboxRngWordByIndex[idx] per-index single-shot word; idx=monotonic boxCursorIndex frontier :1417, not selectable; LR_INDEX active request-advanced via _lrAdvanceIndexClearPending :1694; probe uses LR_INDEX-1 :1416
- **Live inputs (scale-not-select):** rngLockedFlag / _livenessTriggered() live-read to fail closed (return false) during freeze :1413 — safety clamp; boxPlayers[idx].length vs boxCursor live counts, presence-only
- **Verification recipe:** grep function boxesPending :1412; returns bool only, lootboxRngWordByIndex[idx]==0 check :1419, rngLocked/liveness guard :1413, idx=boxCursorIndex frontier :1417, active=LR_INDEX :1414; no arithmetic on the word.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Recipe reproduces exactly: boxesPending returns bool only; the word is touched solely for presence via `if (lootboxRngWordByIndex[idx]==0) return false` :1419 — no keccak/modulo/shift anywhere in the body. Fails closed during the freeze (:1413). Probed idx is the monotonic boxCursorIndex frontier clamped to finalized-1, request-advanced, not caller-selectable. Refutation: could the boolean leak exploitable timing of an undrawn word? Only reveals drawn-vs-not for a non-selectable frontier index, gated off during lock/liveness; no value derived. Freeze trivially holds.
- **Backing:** n/a (direct source argument)

#### `RNGF-REDEEMSEAM-08` — sDGNRS redemption lootbox seed via rngWordForDay(day+1)
- **Invariant:** Lootbox draw entropy = hash2(rngWordForDay(day+1), uint160(player)). Resolving word rngWordByDay[day+1] undrawn (==0) at submit because submit admits only when rngWordForDay(currentPeriod)!=0 (currentPeriod==dailyIdx), pinning resolution to the NEXT day's draw. player folded at submit, not re-chosen at claim. At submit the seed is unknowable; by claim it is the single resolving word every holder's backing rides.
- **Frozen inputs:** rngWord=rngWordForDay(day+1) resolving word ==0 at submit (currentPeriod!=0 admit gate sDGNRS:1036), single-shot :1930; player bound to (player,day) claim slot at submit; self-claim enforced; lootboxEth from claim.ethValueOwed (sealed at submit :1069) * redemption roll :742; frozen before this site; day resolved-day index pinned at submit (currentPeriod==dailyIdx) via GameTimeLib pure timestamp
- **Live inputs (scale-not-select):** actScore=claimActivityScore-1 (sDGNRS:911) snapshot at submit, scopes the lootbox EV cap downstream — not a seed input, not re-chooseable at claim
- **Verification recipe:** grep rngWordForDay(day+1) sDGNRS :916; entropy=hash2(rngWord,uint160(player)) :917; resolveRedemptionLootbox(...entropy...) :920; BurnsBlockedBeforeDailyRng submit gate :1036; resolveRedemptionPeriod day-keyed :742 single-pool sentinel _pendingResolveDay PriorDayUnresolved :1042; day+1 single-shot Game-side :1930 + short-circuit :1210.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`HIGH`. Recipe reproduces (cosmetic: source widens player to uint256, ledger wrote uint160 — semantically identical). Submit gate confirmed sDGNRS:1036 with currentPeriod=GameTimeLib.currentDayIndex() (:1028, pure timestamp). Prime suspect — selecting a revealed word — defeated: (1) `day` is the composite key into pendingRedemptions[player][day], written at submit as [beneficiary][currentPeriod], attacker can only claim a slot they submitted, and submit pins day=currentPeriod to a DRAWN day so day+1 undrawn; (2) claim requires redemptionPeriods[day]!=0 set only by resolveRedemptionPeriod (GAME-only :742-743) on the advance that settles day+1 — so day+1's word is already frozen before claim is callable; (3) player bound to the claim slot, live-game claim permissionless with all value to player (no timing edge), post-gameOver self-claim only; (4) roll=uint16(((currentWord>>8)%151)+25) derived from the committed daily word inside the lock; single-pool sentinel blocks a second unresolved day. day+1 word ==0 at submit, becomes the unique resolving word by claim. Freeze holds.
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/FlipRedeemWindow.t.sol; test/fuzz/StakedStonkRedemption.t.sol; test/fuzz/RedemptionEdgeCases.t.sol

#### `RNGF-FLIPESCROW-09` — sDGNRS FLIP-escrow leg via getCoinflipDayResult(day+1)
- **Invariant:** FLIP-escrow leg consumes coinflipDayResultPacked[(day+1)] — the stored 3-state byte processCoinflipPayouts wrote from the day+1 VRF word inside the advance stack. getCoinflipDayResult is a pure read of an already-resolved frozen result. claim.flipEscrow and the (player,day) slot frozen at submit; day+1 undrawn at submit (same currentPeriod==dailyIdx pin), so no submit-time pre-knowledge of win/rewardPercent and no claim-time steering.
- **Frozen inputs:** rewardPercent/flipWon from coinflipDayResultPacked[day+1] written once by processCoinflipPayouts from the day+1 VRF word; stored past result at claim (Coinflip :367); claim.flipEscrow whole-token slice sealed at submit (sDGNRS:1021+); player/day bound to claim slot at submit; day pinned to a drawn day so day+1 is the resolving coinflip
- **Live inputs (scale-not-select):** isGameOver guard :883 live game-state; in gameOver the FLIP leg is skipped (worthless), not a seed input
- **Verification recipe:** grep getCoinflipDayResult(day+1) sDGNRS :888 flipWon-gated payout :889-893; function getCoinflipDayResult Coinflip :367 -> _dayResult (pure stored byte); processCoinflipPayouts writes the lane from the daily VRF word under onlyDegenerusGameContract; day+1 undrawn at submit via sDGNRS:1036 admit gate.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`HIGH`. Recipe reproduces. getCoinflipDayResult is a pure read: _dayResult(day) reads the coinflipDayResultPacked lane (Coinflip:367-369,1193-1197). Lane written once by processCoinflipPayouts (onlyDegenerusGameContract, :853-857) where win=(rngWord&1)==1 :886 and rewardPercent from keccak256(rngWord,epoch)%20/range :859-883, rngWord = committed daily word from the advance (:1240 live day, :1883 disjoint gap days). Refutation: pick a `day` whose day+1 coinflip is known at submit, or grind day+1 — defeated identically to REDEEMSEAM-08: day is the pendingRedemptions composite key fixed at submit; submit pins day=currentPeriod to a drawn day so day+1's coinflip is unresolved (lane==0->flipWon=false) at submit; by claim the lane is a frozen past result; claim.flipEscrow sealed at submit. isGameOver guard only skips the worthless post-gameOver leg. Freeze holds.
- **Backing:** test/fuzz/CoinflipCarryClaim.t.sol; test/fuzz/FlipRedeemWindow.t.sol; test/fuzz/StakedStonkRedemption.t.sol

#### `RNGF-SUBMITGATE-10` — sDGNRS submit-window VRF-word presence gate rngWordForDay(currentPeriod)==0
- **Invariant:** Consumes only the PRESENCE (drawn vs not) of rngWordForDay(currentPeriod), never the word VALUE in any derived outcome. Admits a gambling-burn submit only when the current day's word is recorded (==dailyIdx), structurally guaranteeing the pool resolves on the NEXT day's draw and the lootbox/FLIP legs read day+1 = a word undrawn at submit. currentPeriod pure block.timestamp; no actor input changes the gate's meaning or the value it reads.
- **Frozen inputs:** currentPeriod=GameTimeLib.currentDayIndex() pure block.timestamp (sDGNRS:1028); rngWordForDay(currentPeriod): only non-zeroness consumed; value not derived into any outcome
- **Verification recipe:** grep BurnsBlockedBeforeDailyRng sDGNRS :1036 `== 0` presence-only (no value stored/derived); GameTimeLib.currentDayIndex() :1028; gate pins currentPeriod==dailyIdx feeding the day+1 freeze of RNGF-REDEEMSEAM-08/09.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Recipe reproduces exactly: sDGNRS:1036 presence-only (==0) read; the word VALUE never stored or derived. currentPeriod=GameTimeLib.currentDayIndex() (:1028, pure block.timestamp). Refutation: choose currentPeriod, or leak the value into an outcome? No — currentPeriod is pure timestamp (not caller-selectable beyond bounded miner drift, which cannot create or alter a committed word), and only non-zeroness consumed. The gate structurally establishes the load-bearing freeze precondition for the seams (day+1 undrawn at submit). No outcome derived; freeze trivially holds; the rngLocked guard on burn()/burnWrapped() covers the request->fulfilment sub-window.
- **Backing:** test/fuzz/FlipRedeemWindow.t.sol; test/fuzz/RedemptionEdgeCases.t.sol

---

## Cluster: GameAfkingModule — afking-box open word read + cover-box index bind

_Both v66 afking consumers exist at HEAD; MIDRNG-02 did not touch the afking open path. RNGF-AFK-02 is a producer-side BIND (no lootboxRngWordByIndex SLOAD in _recordAfkingCoverBox — only LR_INDEX read + enqueue); the word VALUE is consumed downstream in the human-box open leg. FROZEN-AT-COMMIT holds via the LR_INDEX producer-guard (bind binds the CURRENT index whose word is always 0; all word writes target LR_INDEX-1 or lower)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-AFK-01` | contracts/modules/DegenerusGameLootboxModule.sol:1087 (seed=keccak256(abi.encode(rngWord,player,day,amount)) inside resolveAfkingBox:1075-1117); rngWord from the open leg SLOAD GameAfkingModule.sol:1539 (word=rngWordByDay[stampDay]) threaded via _openAfkingBox:1465-1489 | AdvanceModule.sol:1930 (_applyDailyRng writes rngWordByDay[day], from rngGate:1239). Seed DAY label committed earlier at the STAGE stamp GameAfkingModule.sol:938 (sub.lastAutoBoughtDay=processDay) + sub.score:906 + sub.amount:907, all written by _deliverAfkingBuy BEFORE rngGate. P two-part: DAY/score/amount frozen at STAGE (AdvanceModule:336, before rngGate :380); WORD VALUE determined at :1930. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-AFK-02` | downstream word VALUE consumed by LootboxModule._openLootBoxLegWith (DegenerusGameLootboxModule.sol:525/:560, v66 #22) reading lootboxRngWordByIndex[index]; within THIS cluster the freeze-relevant act is the irrevocable BIND GameAfkingModule.sol:1006 (boxPlayers[index].push(player)) + :1037-1038 (lootboxEth[index][player]=_packLootbox(...)) inside _recordAfkingCoverBox:970-1054, index from lootboxRngPacked LR_INDEX :981-982 | GameAfkingModule.sol:982 — index=uint48((lr>>LR_INDEX_SHIFT)&LR_INDEX_MASK) reads CURRENT lootboxRngPacked LR_INDEX. Cover-box committed to THIS index (boxPlayers[index].push :1006). Word determined LATER: next fresh RNG request advances LR_INDEX (AdvanceModule:1666-1675 _lrAdvanceIndexClearPending from _finalizeRngRequest:1694) and sets rngLockedFlag=true :1702; word written at _finalizeLootboxRng:1290 (index=LR_INDEX-1) on fulfillment. P = the bind at :982/:1006. | FROZEN-AT-COMMIT | yes | n/a | yes |

#### `RNGF-AFK-01` — afking-box open word read (rngWordByDay[stampDay]) — Sub-stamp box materialization
- **Invariant:** Word consumed at resolveAfkingBox is rngWordByDay[sub.lastAutoBoughtDay]; lastAutoBoughtDay, score, amount all written during the pre-RNG STAGE (AdvanceModule:329-368) strictly before rngGate writes rngWordByDay[day]. At the instant the seed day is fixed, rngWordByDay[stampDay] is still 0. No actor input between stamp (P) and open (C) changes WHICH word (seed day re-read identically in the open-leg cache) or HOW the outcome derives (preimage exactly {rngWord,player,day,amount}; level live at :1092 but auto-open via _subOpenCursor removes timing; EV-cap RMW is a benign monotonic down-clamp).
- **Frozen inputs:** rngWord == rngWordByDay[stampDay] written once at :1930, immutable (rngGate :1209-1210); player msg.sender subscriber resolved at subscribe :323, read from _subscribers[cursor] :1526; day=stampDay=sub.lastAutoBoughtDay stamped at :938 with boundary-pinned processDay; STAGE runs before rngGate; amount=sub.amount stamped :907 after the funded-debit gate; subscriber set frozen across [request->unlock] (subscribe reverts during rngLockedFlag :319)
- **Live inputs (scale-not-select):** level (LootboxModule:1092) read LIVE at open, cannot bias WHICH word or seed; only shifts the rolled target level base; auto-open via _subOpenCursor removes timing; EV-cap accumulator lootboxEvCapPacked (LootboxModule:1100->:474-510) read LIVE but monotonic DOWN-clamp; reduces scaled AMOUNT only, never touches the seed
- **Verification recipe:** grep rngWordByDay[stampDay] GameAfkingModule -> only :1539 (cache :1532-1542); seed preimage keccak256(abi.encode(rngWord,player,day,amount)) LootboxModule :1087; P-ordering _runSubscriberStage :336 before rngGate :380 -> rngWordByDay[day]=finalWord :1930; subscribe-freeze if(rngLockedFlag) revert :319; _applyEvMultiplierWithCap :474-510 monotonic down-clamp.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: four biasing vectors all defeated. (a) Day-cache cross-box seed swap: cache keyed on each box's OWN stampDay; non-matching falls to else-branch re-SLOAD; _openAfkingBox always pairs day=lastAutoBoughtDay with that word — no cross-day mismatch. (b) Live level steering the draw: _rollTargetLevel derives near/far branch + offset ENTIRELY from the frozen seed; currentLevel only shifts the absolute target LABEL, never the win/loss draw. (c) Player timing own open: opens walk the shared advancing _subOpenCursor, permissionless and open-bounty-incentivized (mintFlip:1598-1606), and even with perfect level timing the seed is frozen. (d) EV-cap RMW: returns <= amount on every branch (monotonic), never reads/writes rngWord, seed derived at :1087 before the call. P-ordering verified: _runSubscriberStage :336 before rngGate :380 -> :1930, so rngWordByDay[stampDay] provably 0 when seed day fixed. Subscribe-freeze :319. Recipe reproduces; freeze holds.
- **Backing:** test/fuzz/V56FreezeSolvency.t.sol; test/fuzz/V55RevertFreeEvCap.t.sol

#### `RNGF-AFK-02` — afking cover-box bind to lootboxRngWordByIndex (subscribe-time grounding INDEXED box)
- **Invariant:** subscribe runs AFTER the current day's word is public, so a rngWordByDay[stampDay]-keyed box would let a player-timed subscribe select a revealed seed — avoided. Cover-box binds to lootboxRngWordByIndex[index] where index is the CURRENT LR_INDEX, a slot whose word is NOT yet written. No actor input between bind (P) and word write (AdvanceModule:1290) changes WHICH index the box rides (advances only via _lrAdvanceIndexClearPending on the next request) or pre-reveals that index's word (future VRF word; subscribe frozen out during rngLockedFlag).
- **Frozen inputs:** index from CURRENT LR_INDEX :982; buyer enqueues boxPlayers[index] :1006; cannot choose a different/future index; player msg.sender subscriber :323 folded into per-(index,player) lootboxEth; amount cover-buy spend confirmed funded before buy; packed lootboxEth :1037-1038; score/activityScore computed once at first deposit :876-887, frozen into the packed word :990/:1013; word value lootboxRngWordByIndex[index] written only at _finalizeLootboxRng :1290 on VRF fulfillment — future word never knowable at subscribe
- **Live inputs (scale-not-select):** live game level at cover-buy (capKey=currentLevel+1 :983) a label/EV-cap key, not a seed input; presale flag (_psRead :1052) only tags the event
- **Verification recipe:** grep lootboxRngPacked GameAfkingModule -> :981 (lr) :982 (index field); lootboxRngWordByIndex GameAfkingModule -> only doc-comment hits, NO SLOAD; index advances only protocol-side _lrAdvanceIndexClearPending AdvanceModule:1666 from _finalizeRngRequest:1694, rngLockedFlag=true :1702; word finalized later lootboxRngWordByIndex[index]=rngWord :1290 (index=LR_INDEX-1); subscribe frozen out during request :319.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bind the cover-box to an index whose VRF word is already revealed (the only grind, since subscribe runs outside the freeze with rngLockedFlag==false). Defeated by a structural producer-guard: the bind reads CURRENT LR_INDEX (:981/:982) and enqueues boxPlayers[index].push(player):1006 + lootboxEth write:1037-1038 — and _recordAfkingCoverBox performs NO lootboxRngWordByIndex SLOAD (grep confirms only doc-comment hits). ALL word writes target LR_INDEX-1 or lower and NEVER the current index (daily :1288/:1290 write-once :1289, mid-day :1849-1850, pre-RNG drain :277-288, gameover :1329/1364, orphan backfill scans i=idx-1 down :1900-1907). LR_INDEX advances only via private _lrAdvanceIndexClearPending (nextIndex=current+1 :1668) from _finalizeRngRequest:1694/requestLootboxRng:1146 — protocol-driven, never buyer-selectable, monotonic. So lootboxRngWordByIndex[bind_index]==0 at every bind, and that index's word is written only AFTER LR_INDEX advanced past it. Both windows checked (mid-day in-flight, post-callback-pre-next-request) — buyer can never bind back to revealed N. FREEZE-01 set-freeze (:319). Freeze holds.
- **Backing:** n/a (direct source argument); test/fuzz/V56SecUnmanipulable.t.sol (adjacent cover-buy mechanics)

---

## Cluster: LootboxModule — box-open ETH/presale legs, direct/redemption/afking resolve, boon rolls

_All seven v66 cluster consumers (rows 22-28) exist at HEAD; line anchors re-mapped. MIDRNG-02 confirmed: the open path is INSENSITIVE to daily-vs-mid-day (reads lootboxRngWordByIndex[index] either way; index request-advanced). The redemption day+1 seam (LBX-04) rests on sDGNRS gates that exist at HEAD; v66 cross-day re-roll confirmed remediated. RNGF-LBX-07 (deity boon) is the one MUTABLE-INPUT; RNGF-LBX-06 (_rollLootboxBoons) is NEEDS-PROOF — the roll VALUE is a frozen bit-slice (seed>>120), but the roll->boonType MAPPING live-reads the boon weight space, so a holder can open-time to reshape WHICH bounded boon a fixed roll maps to (LOW, by-design, bounded EV)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-LBX-01` | contracts/modules/DegenerusGameLootboxModule.sol:560 (seed=keccak256(abi.encode(rngWord,player,amount))); outcome at :561 _rollTargetLevel + :580 _resolveLootboxCommon | AdvanceModule.sol:1290 (_finalizeLootboxRng: lootboxRngWordByIndex[index]=rngWord), :288 (mid-day preIdx), :1850 (rawFulfillRandomWords), :1907 (backfill). Per-index word P fixed at VRF fulfill; consumed rngWord loaded :525 / :681->:705. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-LBX-02` | contracts/modules/DegenerusGameLootboxModule.sol:751 (seed=keccak256(abi.encodePacked(rngWord,keccak256("PRESALE_BOX"),player,amount))); FLIP/DGNRS/WWXRP 50/40/10 split :757-:838 | Same per-index VRF word as LBX-01: AdvanceModule:1290/:288/:1850/:1907. rngWord loaded :630/:708. presaleBoxEth[index][player] packed at buy. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-LBX-03` | contracts/modules/DegenerusGameLootboxModule.sol:886 (seed=EntropyLib.hash2(rngWord,uint256(uint160(player)))); outcome :887 _rollTargetLevel + :896 _resolveLootboxCommon | Caller-domain-separated, frozen at the caller. (a) Decimator DecimatorModule:277 round.rngWord=uint32(rngWord) passed :676. (b) Degenerette DegeneretteModule:652 rngWord=lootboxRngWordByIndex[index] (RngNotReady gate :653), hash2(rngWord,betId) :791. (c) ETH-spin recirc hash2(seed,BOX_RECIRC_TAG). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-LBX-04` | contracts/modules/DegenerusGameLootboxModule.sol:973 (seed=keccak256(abi.encode(rngWord,player,amount))); chunked resolveRedemptionLootbox:954-959 with per-chunk rngWord=EntropyLib.hash1(rngWord) :958; outcome :974 + :981 | CROSS-CONTRACT: entropy frozen at sDGNRS submit/resolve. redemptionPeriods[day] written once by resolveRedemptionPeriod (sDGNRS:759) during the daily advance settling day+1; entropy=hash2(rngWordForDay(day+1),uint160(player)) computed sDGNRS:917 (day+1 undrawn at submit); activityScore snapshot at submit (claim.activityScore :1141); amount=(ethValueOwed*roll)/2 frozen. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-LBX-05` | contracts/modules/DegenerusGameLootboxModule.sol:1087 (seed=keccak256(abi.encode(rngWord,player,day,amount))); outcome :1093 _rollTargetLevel + :1102 _resolveLootboxCommon | GameAfkingModule stamps seed inputs at the pre-RNG STAGE before the day's word lands: sub.lastAutoBoughtDay=processDay (GameAfkingModule:938) BEFORE rngGate writes rngWordByDay[day]. Word committed at rngGate/_applyDailyRng write of rngWordByDay[stampDay] (single-shot). Open leg _openAfkingBox reads day=lastAutoBoughtDay :1467 and word=rngWordByDay[stampDay] (_autoOpen :1539), passing both at :1480. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-LBX-06` | contracts/modules/DegenerusGameLootboxModule.sol:1459 (roll=uint32(seed>>120) % BOON_PPM_SCALE); mapped at :1462 _boonFromRoll((roll*totalWeight)/totalChance,...) applied :1468 | Seed passed to _rollLootboxBoons (at _resolveLootboxCommon:1279) is the SAME per-resolution seed already frozen by the parent consumer (LBX-01/03/04/05, derived once at entry from the committed rngWord). Boon roll bits[120..151] are a fixed slice of that frozen seed. | NEEDS-PROOF | yes | LOW | yes |
| `RNGF-LBX-07` | contracts/modules/DegenerusGameLootboxModule.sol:2322 (seed=keccak256(abi.encode(rngWord,deity,day,slot)); roll=seed%total :2327, mapped :2328). Issued issueDeityBoon:1161 then _applyBoon :1162. | rngWord=rngWordByDay[day] read at issueDeityBoon:1142 with day=_simulatedDayIndex() :1141; nonzero-gated :1143. Day's word committed by the daily advance (single-shot). P = that daily-word write. | MUTABLE-INPUT | yes | LOW | yes |

#### `RNGF-LBX-01` — ETH-lootbox-leg box-open seed (_openLootBoxLegWith)
- **Invariant:** Word consumed at :560 is lootboxRngWordByIndex[index], determined once at P (single-shot: every writer guards !=0). Seed inputs are rngWord (frozen at P), player (queue entry fixed at deposit), amount (LB_AMOUNT_MASK packed at deposit). index request-advanced so no actor selects a revealed index. No block.*/live enters the seed.
- **Frozen inputs:** rngWord lootboxRngWordByIndex[index] committed at P by VRF fulfill, single-shot (!=0 guard :1289); player boxPlayers[index] queue entry / resolved owner, fixed at enqueue; amount LB_AMOUNT_MASK packed at deposit, cleared to 0 at :579 before downstream; index lootboxRngPacked LR_INDEX advanced at REQUEST (:1666), never selectable at open
- **Live inputs (scale-not-select):** currentLevel=level+1 read live :547 feeds _rollTargetLevel base NOT the seed; permissionless gas-bounded auto-open :656 prevents holder level-timing; score/evMultiplierBps from FROZEN packed score, scales AMOUNT not seed/target; _applyEvMultiplierWithCap NOT called on this leg (frozen branch inlined :571-573)
- **Verification recipe:** grep keccak256(abi.encode(rngWord,player,amount)) — only rngWord/player/amount (no block.*, level, day); rngWord from lootboxRngWordByIndex[index] :525/:705; every write site (:288,:1290,:1850,:1907) guarded !=0 single-shot; LR_INDEX advances at request (_lrAdvanceIndexClearPending :1666) not at open; :579 zeroes lootboxEth before _resolveLootboxCommon.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: write a box at an index whose VRF word already landed (read the word, choose the seed). Defeated: the buy path writes lootboxEth[lbIndex] at lbIndex=LR_INDEX (the CURRENT active PRE-incremented index — MintModule:1585/:1949), while the word lands at LR_INDEX-1 (openHumanBoxes:663 'finalized=active-1'; _finalizeLootboxRng :1288 writes index=LR_INDEX-1). LR_INDEX advances ONLY at fresh request (:1694, !isRetry), never at open. Seed = keccak256(abi.encode(rngWord,player,amount)) — only 3 inputs. rngWord single-shot across ALL writers (:1289 early-return, :280 pre-check, :1850 requestId guard :1839, :1907 backfill stops at first filled :1901). _applyEvMultiplierWithCap NOT called (frozen branch :571-573). Recipe reproduces exactly. FREEZE HOLDS.
- **Backing:** test/fuzz/LootboxRngLifecycle.t.sol::test_wordWriteIdempotent + index-advance + entropy-unique tests; structural single-shot guard AdvanceModule:1289

#### `RNGF-LBX-02` — Presale-box leg seed (_resolvePresaleBox)
- **Invariant:** Presale-box outcome at :751 is domain-separated off the SAME per-index committed word with a fixed keccak256("PRESALE_BOX") tag + player + amount (immutable buy data). presaleBoxEth zeroed BEFORE resolution. A write at an already-worded index is unreachable from the buy path. soldBefore is buy-time cumulative selecting only the DGNRS tier, not the seed.
- **Frozen inputs:** rngWord lootboxRngWordByIndex[index] committed at P (single-shot :1289); player presaleBoxEth queue owner fixed at buy; amount PRESALE_BOX_AMOUNT_MASK packed at buy, dequeued to 0 at :632/:707; soldBefore PRESALE_BOX_SOLD_SHIFT packed at buy — feeds DGNRS tier not seed; fixed domain tag keccak256("PRESALE_BOX") compile-time constant
- **Live inputs (scale-not-select):** PriceLookupLib.priceForLevel(level+1) :773 scales FLIP token COUNT not the bucket; dgnrs.poolBalance(PresaleBox) snapshot :824 scales DGNRS magnitude not seed
- **Verification recipe:** grep PRESALE_BOX literal domain tag; seed=keccak256(rngWord,tag,player,amount); rngWord lootboxRngWordByIndex[index] :630/:708; presaleBoxEth=0 at :632/:707 before _resolvePresaleBox; uint16(seed)%100 :757 selects bucket; level read :773 and pool read :824 scale COUNT not bucket; same LR_INDEX request-advance.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: same index-selection vector as LBX-01 plus a soldBefore/tier-grind. Defeated: presaleBoxEth[index] written at the CURRENT active LR_INDEX (MintModule:1960 from :1949=_lrRead(LR_INDEX)), word lands one behind, so no already-worded-index targeting. Seed = keccak256(abi.encodePacked(rngWord,keccak256("PRESALE_BOX"),player,amount)). presaleBoxEth zeroed BEFORE resolution :632/:707. Bucket fixed by uint16(seed)%100 :757; only level read :773 and pool read :822-826 scale COUNT not bucket; soldBefore is FROZEN buy-time cumulative feeding _presaleBoxDgnrsTierTenths only. FREEZE HOLDS.
- **Backing:** shares LBX-01 per-index commitment backing (LootboxRngLifecycle.t.sol); n/a for presale-specific tag/buy-data freeze

#### `RNGF-LBX-03` — resolveLootboxDirect seed (decimator / degenerette-win / ETH-spin recirc auto-resolve)
- **Invariant:** resolveLootboxDirect derives outcome ONLY from the caller-passed rngWord and player. rngWord frozen at the caller's commitment (per-level decimator word / per-bet lootbox word + immutable betId / box recirc seed), domain-separated so the same daily word cannot collide. No live post-word input enters the seed at :886; no live day. Delegatecall-only guard :877 confines msg.value.
- **Frozen inputs:** rngWord caller param frozen at the caller's commitment (DecimatorModule:277 / DegeneretteModule:652 under RngNotReady / box recirc seed); player caller param, fixed at the bet/burn; betId player's own bet nonce fixed at placement, mixed hash2(rngWord,betId) :791; no day term (:885 confirms no _simulatedDayIndex in the seed)
- **Live inputs (scale-not-select):** currentLevel=level+1 :880 base for _rollTargetLevel only; auto-resolve callers not player-open-timed; _applyEvMultiplierWithCap :890 SOLE live read-into-write: benign monotonic DOWN-clamp; evMultiplierBps from FROZEN caller-passed activityScore :889; scales AMOUNT, cannot bias word/target
- **Verification recipe:** grep EntropyLib.hash2(rngWord,uint256(uint160(player))) — rngWord+player only; callers DecimatorModule:673 (rngWord=round.rngWord) DegeneretteModule:928 (hash2(rngWord,betId)); round.rngWord written once :277; lootboxRngWordByIndex[index] under RngNotReady gate :652-653; _applyEvMultiplierWithCap only reduces :489/:493; address(this)==GAME guard :877.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-roll via claim timing / a futurePrizePool nudge, or a grindable caller-supplied rngWord. Defeated: seed=EntropyLib.hash2(rngWord,uint256(uint160(player))) — only rngWord+player (hash2 pure keccak EntropyLib.sol:23). Callers domain-separate AND freeze the word (DecimatorModule per-round round.rngWord; DegeneretteModule lootboxRngWordByIndex[index] under RngNotReady + immutable betId). No live post-word input/day in the seed. address(this)==GAME guard :877 confines msg.value to the redemption recirc (direct call hits amount==0 early-return). _applyEvMultiplierWithCap :890 monotonic DOWN-clamp, scales amount only. FREEZE HOLDS.
- **Backing:** test/fuzz/DegeneretteFreezeResolution.t.sol; decimator per-round freeze direct source argument (DecimatorModule:277)

#### `RNGF-LBX-04` — resolveRedemptionLootbox -> _resolveRedemptionChunk seed (sDGNRS gambling-burn claim)
- **Invariant:** Chunk seed at :973 derives ONLY from caller-passed rngWord (= hash2 over the day+1 word, frozen because day+1 was undrawn when burn submitted — BurnsBlockedBeforeDailyRng :1036 + _pendingResolveDay single-pool sentinel :1042), player, amount. Per-chunk hash1 advance deterministic from the frozen seed. No live day. (player,day) slot deleted at sDGNRS:871 (CEI) before the cross-call.
- **Frozen inputs:** rngWord param hash2(rngWordForDay(day+1),uint160(player)) :917 — day+1 single-shot and undrawn at submit (:1036); player (player,day) key fixed at submit; amount lootboxEth=(ethValueOwed*roll)/2 — ethValueOwed frozen at submit, roll once at resolveRedemptionPeriod :759; activityScore claim.activityScore snapshot once :1141; per-chunk hash1(rngWord) :958 pure deterministic chaining
- **Live inputs (scale-not-select):** currentLevel=level+1 :969 base for _rollTargetLevel only; resolving word and roll already fixed; _applyEvMultiplierWithCap :977 benign monotonic down-clamp; evMultiplierBps from FROZEN submit snapshot
- **Verification recipe:** grep keccak256(abi.encode(rngWord,player,amount)) :973 — no day; caller sDGNRS:920 game.resolveRedemptionLootbox{value}(player,lootboxEth,entropy,actScore); entropy=hash2(rngWordForDay(day+1),player) :916-917; submit gate sDGNRS:1036 + _pendingResolveDay sentinel :1041-1043 PriorDayUnresolved; redemptionPeriods[day] once :759 sentinel cleared :767; claim.activityScore once :1141 slot deleted :871 (CEI); per-chunk hash1 :958.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation (the seam): grind the resolving day's word by submitting the burn AFTER day+1 is knowable, or replay/re-resolve the roll. Defeated: chunk seed at :973 = keccak256(abi.encode(rngWord,player,amount)) (no day); rngWord = hash2(game.rngWordForDay(day+1),player) :916-917; the claim's period key 'day' = currentPeriod = the SUBMIT day (:1028; slot pendingRedemptions[beneficiary][currentPeriod] :1129); entropy uses day+1, undrawn at submit. Submit gate sDGNRS:1036 pins the submit to a drawn current day; burn()/burnWrapped() block the request->fulfil window via rngLocked; _pendingResolveDay sentinel blocks re-arming. Roll redemptionPeriods[day] VRF-derived (AdvanceModule:1262) written ONCE per day at resolveRedemptionPeriod:759 (sentinel cleared :767). claim.activityScore once; (player,day) slot deleted :871 (CEI). Per-chunk hash1(rngWord) pure. Claim permissionless but word+roll already fixed by claim-time. v66 cross-day re-roll confirmed remediated. FREEZE HOLDS across the seam.
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/RedemptionEdgeCases.t.sol/RedemptionSplit.t.sol/StakedStonkRedemption.t.sol; test/repro/V62RedemptionReentrancy.t.sol (CEI)

#### `RNGF-LBX-05` — resolveAfkingBox seed (afking-subscription box, frozen-stamp-day word, live-level twin)
- **Invariant:** Seed derives ONLY from caller-passed rngWord (= rngWordByDay[stampDay], frozen because stampDay committed by the STAGE before that day's word existed), player, FROZEN stamped process day, stamped amount. The day MUST stay frozen — live _simulatedDayIndex() would let a self-keepering player grind via open-timing; the code deliberately passes the frozen stamp day. No-double-open: leg runs only while lastOpenedDay<lastAutoBoughtDay and sets lastOpenedDay=lastAutoBoughtDay BEFORE the resolve. ZERO block.* entropy.
- **Frozen inputs:** rngWord rngWordByDay[lastAutoBoughtDay] frozen stamp day's word; stamp day committed (:938) BEFORE rngGate writes that day's word; player subscriber fixed at subscribe; day sub.lastAutoBoughtDay boundary-pinned FROZEN process day (NOT live); amount stamped spend (boons OFF => amount==spend); activityScore stamped bps, FROZEN EV input
- **Live inputs (scale-not-select):** currentLevel=level+1 :1092 base for _rollTargetLevel; auto-open removes level-timing; _applyEvMultiplierWithCap :1100 benign monotonic down-clamp on the shared per-[player][level] budget; evMultiplierBps from FROZEN stamped activityScore
- **Verification recipe:** grep keccak256(abi.encode(rngWord,player,day,amount)) :1087 — FROZEN day param (distinct from resolveLootboxDirect); rngWord is a CALLER param; caller _autoOpen stampDay=lastAutoBoughtDay :1532, word=rngWordByDay[stampDay] :1539 readiness gate word==0 :1544; _openAfkingBox lastOpenedDay=lastAutoBoughtDay BEFORE resolve :1470, day+word :1467/:1480; lastAutoBoughtDay=processDay :938 (pre-RNG); _autoOpen entry-gate rngLockedFlag/_livenessTriggered :1507; no block.* in seed.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: a self-keepering subscriber stamps the box into a day whose word is already known (grindable seed), or open-times to re-derive. Defeated: seed at :1087 = keccak256(abi.encode(rngWord,player,day,amount)) where 'day' is the FROZEN stamp param (NOT live) and rngWord=rngWordByDay[stampDay] read by the caller. Load-bearing ordering: the afking process STAGE (_runSubscriberStage(day) -> stamps lastAutoBoughtDay=processDay :938) runs STRICTLY BEFORE rngGate writes that day's word — AdvanceModule executes the STAGE at :336 and reaches rngGate only at :380. So the stamp can NEVER target a day whose word exists. No-double-open: _autoOpen skips lastOpenedDay>=stampDay :1533, sets lastOpenedDay=lastAutoBoughtDay BEFORE resolve :1470. Day-cache never crosses a box to another day's word. EV-cap benign. No block.* in seed. FREEZE HOLDS.
- **Backing:** test/fuzz/AfKingSubscription.t.sol/AfKingConcurrency.t.sol/AfKingFundingWaterfall.t.sol/OverpayToAfking.t.sol; seed day-freeze direct source argument :1087

#### `RNGF-LBX-06` — _rollLootboxBoons boon roll (boon-type award off the box seed)
- **Invariant:** Roll value at :1459 is bits[120..151] of the parent's frozen per-resolution seed — fully determined at P, no fresh entropy. The roll is frozen; what is LIVE-read is the boon WEIGHT SPACE and chance-gate denominators which reshape the roll->boonType MAPPING but cannot change the roll value or which word was consumed. Boon-eligibility live reads are SCORING-AGAINST, not seed inputs.
- **Frozen inputs:** seed param the parent per-resolution seed frozen at P (rngWord-derived); bits[120..151] sliced inline; originalAmount param -> boonBudget fixed BPS (_lootboxBoonBudget :1200); currentLevel param threaded from parent
- **Live inputs (scale-not-select):** decWindowOpen _isDecimatorWindow() :1431 adds decimator weights — protocol-wide window flag; deityPassOwners.length :1437 protocol-supply gate; lazyPassValue _lazyPassPriceForLevel :1429 gates lazy-pass weights; mintPacked_[player] deity-pass bit :1436 only short-circuits the owner-count SLOAD; currentDay :1427 boon start day (expiry bookkeeping), not a seed/roll input; checkAndClearExpiredBoon delegatecall :1421 no effect on the roll
- **Verification recipe:** grep uint32(seed>>120) % BOON_PPM_SCALE :1459 — pure bit-slice of the parent seed; seed is the parent's frozen seed threaded :1279; enumerate live reads (decWindowOpen :1431, deityPassOwners.length :1437, lazyPassValue :1429, mintPacked_ :1436, currentDay :1427) and verify each feeds _boonPoolStats/_boonFromRoll weights or _applyBoon bookkeeping — none XORed/added into seed or roll; chance gate :1455-1460 uses frozen boonBudget.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Refutation partially LANDED at the DERIVATION level; the WORD/ROLL freeze (under test) HOLDS. The roll VALUE at :1459 = uint32(seed>>120)%BOON_PPM_SCALE is a FROZEN bit-slice of the parent's committed seed — no live read XORed/added into seed or roll. HOWEVER the boon->type mapping _boonFromRoll(...) :1462 and the boon-gate (roll>=totalChance) :1460 DO depend on live reads (decWindowOpen :1431, lazyPassValue :1429/_boonPoolStats :1511, deityEligible/deityPassOwners.length :1436-1438). Since manual openBox is permissionlessly callable and the frozen roll is publicly computable once the index word lands, a box holder CAN read the frozen roll and choose open-timing/conditions to reshape WHICH deterministic boon a fixed roll maps to. This understates the ledger's 'scoring-against' framing. BUT not a freeze break: roll/word cannot be re-rolled (fixed at P); the level driving decWindowOpen/price advances ~1/qualifying-day and is a deterministic narrow band, so a holder can only passively WAIT for the protocol to cross a window, not summon it; flipping deityEligible costs ~24+ ETH and only trades eligibility; the boon is a haircut-funded side-reward (10% boonBudget capped 1 ETH), bounded EV. Same bounded post-reveal MUTABLE-INPUT class as LBX-07. FREEZE-OF-WORD HOLDS; residual is a known by-design bounded derivation-timing selection, INFO/LOW.
- **Backing:** test/fuzz/LootboxBoonCoexistence.t.sol; roll-is-seed-slice direct source argument :1459

#### `RNGF-LBX-07` — issueDeityBoon -> _deityBoonForSlot deity boon roll
- **Invariant:** boon-type at :2322 is a pure function of (rngWordByDay[day],deity,day,slot) — the day's word committed at P and public/frozen. MUTABLE-INPUT: the deity chooses recipient and slot (0-2) AFTER the public day word is drawn, can read rngWordByDay[day] off-chain and pick the slot they prefer. But (1) the word cannot be re-rolled (single committed daily word, day-keyed, fixed at P); (2) rate-limited to DEITY_DAILY_BOON_COUNT(=3) slots/day, one recipient per recipient per day; (3) gift to a recipient (deity!=recipient), not self-payout. WORD frozen at P; only WHICH of 3 deterministic per-slot outcomes is gifted is deity-selectable, one-shot-per-slot.
- **Frozen inputs:** rngWord rngWordByDay[day] committed at P (single-shot), nonzero-gated :1143; deity msg.sender deity-pass holder gated :1139; day _simulatedDayIndex() :1141 wall-clock (not selectable)
- **Live inputs (scale-not-select):** slot param 0-2 caller-chosen AFTER the word is public — the MUTABLE-INPUT; bounded to 3, each consumable once (slotMask :1152-1153, deityBoonPacked write :1154-1156); recipient param affects WHO gets the boon (per-recipient one-per-day :1150,:1157), NOT the boonType (recipient not in seed); decimatorAllowed :1159 / deityPassAvailable :1160 reshape `total` and the weight mapping; protocol-state, not deity-set per-call
- **Verification recipe:** grep keccak256(abi.encode(rngWord,deity,day,slot)) :2322 — seed inputs rngWord/deity/day/slot, recipient NOT in seed; rngWord=rngWordByDay[day] :1142 day=_simulatedDayIndex() :1141 nonzero gate :1143; rate-limits deity-pass gate :1139 slot<3 :1138 per-recipient-day :1150/:1157 per-slot used-mask :1148-1156; gift-only deity!=recipient :1137; view mirror DeityBoonViewer:99/:118.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Refutation: exceed the 3-slot bound, self-target, or re-roll the word; confirmed the acknowledged bounded MUTABLE-INPUT (word frozen, slot/recipient post-reveal selectable). Source: seed=keccak256(abi.encode(rngWord,deity,day,slot)); rngWord=rngWordByDay[day] :1142 day=_simulatedDayIndex() :1141 nonzero-gated :1143. recipient NOT in seed, so the deity cannot grind across recipients for a better boonType. The deity reads the public day word then picks slot 0-2 — at most 3 deterministic per-slot outcomes — the residual mutable input. Bounds verified one-shot: slot<3 :1138, per-slot used-mask :1152, per-recipient-day gate :1150/:1157, deity-pass gate :1139, gift-only :1137. Word frozen-at-commit; only the bounded post-reveal slot/recipient selection is mutable, exactly as classified (known v66 by-design MUTABLE-INPUT). FREEZE-OF-WORD HOLDS; residual by-design.
- **Backing:** n/a (direct source argument); word frozen-at-commit (rngWordByDay[day] single-shot)

---

## Cluster: DegeneretteModule — per-spin draws, FLIP survival flip, box-spins, sDGNRS reward

_All 6 v66 consumers (rows 29-34) exist at HEAD; MIDRNG-02 added a third valid commitment write (rawFulfillRandomWords mid-day finalize :1850) but did not change the consumption sites. Box-spins synthesize both player ticket and hero quadrant from the box seed (no player-chosen customTicket). RNGF-DEGN-34 (_awardDegeneretteDgnrs) is the one CROSS-CONTRACT-SEAM: VRF win-gate (s>=7) frozen at P, only the magnitude reads the drainable sDGNRS Reward pool (v66 D-28). VERIFY notes test-backing defects: DEGN-30 mis-cites a Coinflip test for the Degenerette path; DEGN-34's testDgnrsAwardStaysPerSpin is currently vm.skip(true) — neither breaks the source freeze._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-DEGN-29` | contracts/modules/DegenerusGameDegeneretteModule.sol:676-692 (resultSeed=keccak256(rngWord,index,[spinIdx],QUICK_PLAY_SALT) :676-689; resultTicket=DegenerusTraitUtils.packedTraitsDegenerette(resultSeed) :690-692; scored :698) | AdvanceModule.sol:1290 (_finalizeLootboxRng) OR :1850 (rawFulfillRandomWords mid-day) OR :1907 (orphan backfill). Consumed slot read DegeneretteModule:652 (rngWord=lootboxRngWordByIndex[index]); index request-advanced (LR_INDEX) and frozen INTO the packed bet at placement :531-555. | FROZEN-AT-COMMIT | yes | INFO | yes |
| `RNGF-DEGN-30` | contracts/modules/DegenerusGameDegeneretteModule.sol:772-780 (if currency==FLIP && totalPayout!=0: EntropyLib.hash2(rngWord,betId)&1==1 -> double else zero; mint :774/:777) | AdvanceModule.sol:1290/:1850/:1907 (lootboxRngWordByIndex[index] committed). betId is the player's own monotonic nonce fixed at placement (DegeneretteModule:557-561 degeneretteBetNonce), read as the loop key :629. | FROZEN-AT-COMMIT | yes | INFO | yes |
| `RNGF-DEGN-31` | contracts/modules/DegenerusGameDegeneretteModule.sol:1305-1318 (playerTicket=packedTraitsDegenerette(seed) :1305; resultTicket=packedTraitsDegenerette(hash2(seed,1)) :1306-1308; hero=uint8(seed&MASK_2) :1309; s=_score :1309; payout=_fullTicketPayout :1310-1317; mint :1319) | Box `seed` committed transitively from lootboxRngWordByIndex[index]: AdvanceModule:1290/:288/:1850/:1907 -> LootboxModule:560 (keccak(rngWord,player,amount)) OR :886 (hash2(rngWord,uint160(player))) -> WWXRP-spin sub-seed=EntropyLib.hash2(seed,BOX_WWXRP_SPIN_TAG) LootboxModule:2010 -> _callWwxrpSpin :2095-2111 delegatecall. P = the per-index VRF finalize. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-DEGN-32` | contracts/modules/DegenerusGameDegeneretteModule.sol:1362-1393 (loop i 0..2: ss=hash2(seed,i) :1363; playerTicket=packedTraitsDegenerette(ss) :1364; resultTicket=packedTraitsDegenerette(hash2(ss,1)) :1365-1367; s=_score(...,uint8(ss&MASK_2)) :1368; survival=total!=0 && hash2(seed,BOX_SURVIVAL_TAG)&1==1 :1384-1385; mint :1387) | Same transitive root as DEGN-31: P=lootboxRngWordByIndex[index] :1290/:1850/:1907; box-seed=keccak(W,player,amount) :560 or hash2(W,player) :886; FLIP-spins sub-seed=EntropyLib.hash2(box-seed,BOX_FLIP_SPIN_TAG) :2025 -> _callFlipSpins :2114-2130. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-DEGN-33` | contracts/modules/DegenerusGameDegeneretteModule.sol:1414-1463 (playerTicket=packedTraitsDegenerette(seed) :1414; resultTicket=packedTraitsDegenerette(hash2(seed,1)) :1415-1417; hero=uint8(seed&MASK_2) :1418; s=_score :1418; payout=_fullTicketPayout :1419-1426; _distributePayout :1436; recirc seed=hash2(seed,BOX_RECIRC_TAG) :1459) | Same transitive root: P=lootboxRngWordByIndex[index] :1290/:288/:1850/:1907; box-seed :560/:886; ETH-spin sub-seed=EntropyLib.hash2(box-seed,BOX_ETH_SPIN_TAG) :2042 -> _callEthSpin :2133-2148. ETH-spin fires only from a DIRECT box open (allowEthSpin=true :2034); recirc boxes (allowEthSpin=false) award tickets :2046-2052 (no nested cascade). | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-DEGN-34` | contracts/modules/DegenerusGameDegeneretteModule.sol:1210-1234 (gated by VRF-derived score s>=7; from _resolveFullTicketBet:738 and resolveEthSpinFromBox:1437; reward=poolBalance(Reward)*bps*cappedBet/(10000*1e18) :1226; sdgnrs.transferFromPool(Reward,player,reward) :1229) | The s>=7 GATE is VRF-derived and FROZEN at P (lootboxRngWordByIndex[index] :1290/:1850/:1907 -> resultTicket -> _score s). The reward MAGNITUDE is NOT VRF-derived: reads sdgnrs.poolBalance(Reward) LIVE cross-contract (sDGNRS.sol:520) at :1220 and the frozen betWei. transferFromPool onlyGame (sDGNRS.sol:559). | CROSS-CONTRACT-SEAM | yes | LOW | yes |

#### `RNGF-DEGN-29` — _resolveFullTicketBet per-spin result-ticket draws (Degenerette spins)
- **Invariant:** For a bet at index I, consumed word W=lootboxRngWordByIndex[I] determined once at P and immutable (single-shot :1289). Placement gate :533 rejects any bet whose index word is ALREADY committed, so the bettor commits customTicket+heroQuadrant+index+activityScore BEFORE W exists. Between P and C no actor input changes WHICH word (index frozen in the packed bet :648) or HOW the result derives (packedTraitsDegenerette pure of seed only). customTicket/heroQuadrant feed only _score :698, SCORED AGAINST the VRF-derived resultTicket, never into resultSeed.
- **Frozen inputs:** rngWord=lootboxRngWordByIndex[index] committed :1290/:1850/:1907 single-shot; read :652; index packed at placement (FT_INDEX_SHIFT :965) decoded :648; equals LR_INDEX at place; advanced only by VRF request; spinIdx pure loop local 0..ticketCount-1 :673; QUICK_PLAY_SALT compile-time constant 0x51 :260; customTicket/heroQuadrant frozen in the packed bet :642/:650; only enter _score; placement gate :533 forbids placing on an already-worded index
- **Live inputs (scale-not-select):** level read live at S==9 WWXRP whale-pass branch :750 does NOT affect THIS per-spin draw; only buckets a deduped whale halfpass; activityScore decoded from the packed bet :649 — a placement SNAPSHOT, scales roiBps/EV only
- **Verification recipe:** grep lootboxRngWordByIndex[index]!=0 revert RngNotReady — placement guard :533 AND resolution guard :653; resultSeed preimage :676-689 NO live SLOAD; index decoded :648 never reassigned; 3 commitment writes (:1290,:1850,:1907) single-shot :1289; _score :1001 takes customTicket/heroQuadrant as scoring args only.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Refutation: grind by placing many bets at one index then resolving only winners, OR re-arm the seed after W knowable. Defeated. resultSeed preimage :676-689 = keccak256(rngWord,index,[spinIdx],QUICK_PLAY_SALT) — NO live-state token (no level/poolBalance/futurePrizePool/balanceOf/block.*/timestamp). index decoded once :648 and NEVER reassigned. Placement gate :533 rejects an already-worded index; the lifecycle is sound (requestLootboxRng advances LR_INDEX :1146, finalize writes LR_INDEX-1 :1288/:1849, word lands on the index a bet froze BEFORE the request). customTicket/heroQuadrant feed _score only. Recipe reproduces (minor: resolution guard at :653 is the equivalent 'if(rngWord==0) revert'). NOTE: ledger missed a 4th commitment write at AdvanceModule:288 (single-shot via :280 ==0 check, VRF-rooted) — omission does not break freeze. INFO: placement packs uint32(index) :552 while the gate key is uint48 — a 32-bit truncation aliasing only after ~4.3e9 advances (physically unreachable, not actor-forcible).
- **Backing:** test/fuzz/RngFreezeAndRemovalProofs.t.sol; test/fuzz/DegeneretteFreezeResolution.t.sol

#### `RNGF-DEGN-30` — _resolveFullTicketBet FLIP survival flip (double-or-nothing)
- **Invariant:** Survival outcome bit = hash2(W,betId)&1 fully determined at P because BOTH inputs frozen before W lands: W is the committed index word (placement gate :533 forbids a known-word index), betId the player's nonce assigned at placement (:557-561), immutable. A losing bet pays zero whether resolved or abandoned, so selective resolution earns nothing — the bit is fixed at fulfillment, not at resolve-call time. Accumulator (:774 adds again to double; :777 subtracts to zero) is EV-neutral x2-at-50/50.
- **Frozen inputs:** rngWord=lootboxRngWordByIndex[index] committed :1290/:1850/:1907; read :652; betId player's degeneretteBetNonce, stored at placement :557-561; resolve loop key :439-440->:629; currency/totalPayout gate frozen in packed bet :644
- **Verification recipe:** read :772-780 bit=EntropyLib.hash2(rngWord,betId)&1 with rngWord from :652 and betId param :637; betId originates degeneretteBetNonce at placement :557-561; hash2 pure; only place(write)/resolve(delete :655) touch the bet.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Refutation: place many bets (distinct betIds) at one index, observe W when it lands, then selectively resolve only winners. Defeated: the bit at :773 is fully determined at P because BOTH inputs freeze before W — W is the index word (placement gate :533 forbids a known-word index) and betId is the monotonic degeneretteBetNonce assigned only at placement (:557-561, no betId mutation between place and the delete at :655). hash2 pure keccak. A losing survival flip subtracts the payout back out (:777) and pays zero; an abandoned losing bet also pays zero — selective resolution earns nothing. Recipe reproduces. BACKING DEFECT (no freeze impact): the ledger cites testWinLossRngPathByteUnmodified (RngFreezeAndRemovalProofs.t.sol:751) which asserts on Coinflip.sol source occurrences, NOT the Degenerette FLIP survival path — mis-cited; live backing is testFlipSurvivalFlipLossZeroesMint (DegeneretteFreezeResolution.t.sol:449).
- **Backing:** test/fuzz/DegeneretteFreezeResolution.t.sol::testFlipSurvivalFlipLossZeroesMint:449

#### `RNGF-DEGN-31` — resolveWwxrpSpinFromBox spin reels + S==9 bracket whale-halfpass
- **Invariant:** Spin `seed` is a pure hash2/keccak chain rooted at the committed per-index word W and frozen box-deposit data (player, amount) — no live post-reveal state enters the chain. All reels and the score derive from {seed, hash2(seed,1), seed&MASK_2}; hero quadrant SYNTHESIZED from the seed (:1309), so no player-chosen ticket steers it (box-spins have no customTicket). address(this)==GAME :1298 plus delegatecall-only reach mean the seed is never attacker-supplied.
- **Frozen inputs:** seed=hash2(box-seed,BOX_WWXRP_SPIN_TAG) :2010; box-seed=keccak(W,player,amount) :560 or hash2(W,player) :886; W committed :1290/:1850/:1907; player box owner fixed at deposit; amount packed in lootboxEth at deposit, zeroed at :579 on open; activityScore frozen score snapshot, scales ROI only; BOX_WWXRP_SPIN_TAG / hash2(seed,1) counter compile-time constants
- **Live inputs (scale-not-select):** level read live :1324 gates ONLY the deduped one-per-10-level whale-halfpass (wwxrpJackpotWhalePassBracketAwarded[bracket]); not in spin seed/reels/payout; auto-open removes timing
- **Verification recipe:** trace BOX_WWXRP_SPIN_TAG LootboxModule:2010 hash2(seed,...); box-seed :560/:886 keccak/hash2 of {W,player,amount}; address(this)==GAME :1298; playerTicket/resultTicket/hero all from seed :1305-1309 no SLOAD; live level read :1324 only the bracket flag.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: inject a live read into the box-spin seed or steer reels via a player-chosen ticket. Defeated. seed is a pure keccak/hash2 chain: box-seed=keccak256(abi.encode(rngWord,player,amount)) :560 or hash2(rngWord,uint160(player)) :886; WWXRP sub-seed=hash2(box-seed,BOX_WWXRP_SPIN_TAG) :2010. The dispatch branch (roll=uint16(seed>>40)%20 :1983) is also seed-derived — no live SLOAD steers which spin fires. resolveWwxrpSpinFromBox derives playerTicket, resultTicket (hash2(seed,1)) AND the hero quadrant (uint8(seed&MASK_2) :1309) all from the seed — NO player-chosen customTicket on box-spins. address(this)==GAME :1298 + delegatecall-only. Live `level` :1324 only gates the deduped bracket flag. Recipe reproduces.
- **Backing:** n/a (direct source argument); shares per-spin primitives backed by DegeneretteFreezeResolution.t.sol + DegeneretteHeroScore.t.sol:test_HERO_ScoreFormula

#### `RNGF-DEGN-32` — resolveFlipSpinsFromBox 3 FLIP spins + one survival flip
- **Invariant:** All three reels + the single survival flip derive deterministically from the committed `seed` (hash2(seed,i) per spin, hash2(seed,BOX_SURVIVAL_TAG)&1). seed pure keccak chain rooted at W and frozen {player,totalStake}; mint-only — no pool/ETH/recirc read. Survival flip on a seed bit the three spins never consume (counter-tagged), independent and fixed at P.
- **Frozen inputs:** seed=hash2(box-seed,BOX_FLIP_SPIN_TAG) :2025; box-seed rooted at committed W; player box owner fixed at deposit; totalStake=_largeFlipOut(amount,targetPrice,seed) :2019/:2074-2092 pure from frozen amount + same seed; split /BOX_FLIP_SPINS :1355; activityScore deposit snapshot scales ROI only; BOX_FLIP_SPINS/BOX_SURVIVAL_TAG/per-spin counters constants
- **Verification recipe:** grep BOX_FLIP_SPIN_TAG LootboxModule:2025 forwarded _callFlipSpins :2114; address(this)==GAME :1353; per-spin ss=hash2(seed,i) :1363 and survival=hash2(seed,BOX_SURVIVAL_TAG)&1 :1385 only entropy reads; coin.mintForGame :1387 sole effect.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias one of the 3 reels or the survival bit via a counter collision or a live read. Defeated: each reel ss=hash2(seed,i) :1363 for i in 0..2, resultTicket=hash2(ss,1) :1366, survival=hash2(seed,BOX_SURVIVAL_TAG)&1 :1385 — distinct tagged chunks, no collision (i in {0,1,2} vs the large BOX_SURVIVAL_TAG). seed=hash2(box-seed,BOX_FLIP_SPIN_TAG) :2025 pure chain rooted at W. No SLOAD in the loop; coin.mintForGame :1387 sole effect. address(this)==GAME :1353. Recipe reproduces.
- **Backing:** n/a (direct source argument); survival-flip zero-on-loss shares DegeneretteFreezeResolution.t.sol:449

#### `RNGF-DEGN-33` — resolveEthSpinFromBox spin + 3-tier ETH split + recirc
- **Invariant:** Reel + score derive purely from the committed `seed` (hash2 chain rooted at W + frozen {player,amount}); recirc box's seed=hash2(seed,BOX_RECIRC_TAG) is a deterministic descendant. The ONLY live reads are pool magnitudes inside _distributePayout (futurePrizePool for the 10% ETH_WIN_CAP_BPS cap and the running decrement) — SCORING the payout against the live pool, not steering which reel won (already fixed at P). Cap is a monotonic down-clamp. Pool/claimable writes flushed BEFORE recirc but the recirc SEED is the frozen hash2(seed,BOX_RECIRC_TAG).
- **Frozen inputs:** seed=hash2(box-seed,BOX_ETH_SPIN_TAG) :2042; box-seed rooted at committed W; player box owner fixed at deposit; betAmount/stake=_ticketBudget(amount,...)*_ticketVarianceBps(seed)/10000 :2035-2036 from frozen amount + same seed; activityScore deposit snapshot scales ROI only; BOX_ETH_SPIN_TAG / BOX_RECIRC_TAG constants
- **Live inputs (scale-not-select):** futurePrizePool (_distributePayout :873/:890) scores the payout AGAINST the pool, does not steer which spin won; cap monotonic down-clamp (:892, excess->lootboxShare); flush-before-recirc :1439-1447 but recirc seed :1459 is the frozen hash2(seed,BOX_RECIRC_TAG), pool-independent
- **Verification recipe:** grep BOX_ETH_SPIN_TAG LootboxModule:2042 inside allowEthSpin branch :2034; else branch :2046-2052 awards tickets; address(this)==GAME :1408; reel derivation from seed :1414-1418; recirc seed :1459 = hash2(seed,BOX_RECIRC_TAG) NOT re-read from flushed storage; _distributePayout pool reads :873/:890 only size ethShare/lootboxShare (cap :891-896).
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: use the live futurePrizePool reads to steer the reel, or re-roll the recirc box from flushed storage. Defeated. The reel + score derive purely from the committed seed (playerTicket=packedTraitsDegenerette(seed) :1414, resultTicket=hash2(seed,1) :1416, hero=uint8(seed&MASK_2) :1418); seed=hash2(box-seed,BOX_ETH_SPIN_TAG) :2042. Pool reads only PARTITION an already-fixed payout (ETH_WIN_CAP_BPS monotonic down-clamp :891-896, excess->lootboxShare). recirc seed=hash2(seed,BOX_RECIRC_TAG) :1459 is the frozen descendant, NOT re-read from flushed pool/claimable storage. allowEthSpin=false on the recirc entry (else-branch :2046-2052 awards tickets) prevents a cascade. address(this)==GAME :1408. Recipe reproduces.
- **Backing:** test/fuzz/DegeneretteFreezeResolution.t.sol::testEthCapBindsOnIdenticalSpin_Tier2:509 + testBatchedPayoutEqualsPerSpinExpectation_Tier1

#### `RNGF-DEGN-34` — _awardDegeneretteDgnrs sDGNRS Reward-pool grant (S>=7 ETH spins, also box ETH-spin)
- **Invariant:** WHETHER this award fires is fully determined at P: gated solely on the VRF-derived score s>=7 and currency==ETH, both frozen. The award MAGNITUDE is intentionally a live read of the cross-contract Reward pool (percentage-of-remaining-pool model), NOT frozen at P — but no actor input between P and C changes WHICH word/score gates the award. A third party draining the Reward pool between P and C changes only the SIZE of an already-earned reward (down-scales; ==0 -> early return), never whether the player won. betWei capped at 1 ether :1225. (v66 D-28 documented seam: magnitude reads a drainable pool.)
- **Frozen inputs:** s (score gate s>=7) VRF-derived from W via resultTicket+_score; frozen at P; currency==CURRENCY_ETH frozen in packed bet :644; betWei frozen in packed bet :645 / box stake; capped 1 ether :1225; bps (4%/8%/15% for s=7/8/9) compile-time constants :228-230, selected by frozen s
- **Live inputs (scale-not-select):** sdgnrs.poolBalance(Reward) live cross-contract SLOAD :1220 sets only reward magnitude, not the win gate; drainable but only shrinks an already-earned grant (==0->no-op); cannot manufacture/steer a win or change which word was consumed
- **Verification recipe:** grep _awardDegeneretteDgnrs both call sites gate on s>=7 (:737,:1437); seed/score NOT re-derived here (s is a frozen param); live sdgnrs.poolBalance(Reward) :1220 feeds only reward magnitude :1226 with ==0 early-return :1227; transferFromPool onlyGame sDGNRS.sol:559.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Refutation: manufacture or steer the award via the cross-contract Reward-pool read, or bias the s>=7 gate. WIN GATE is VRF-frozen: _awardDegeneretteDgnrs takes `s` frozen; both call sites gate on s>=7 (:737,:1437) where s=_score(playerTicket, resultTicket=VRF-derived, heroQuadrant) — no seed re-derivation; bps a const by frozen s. Seam by-design: sdgnrs.poolBalance(Reward) (live SLOAD :1220, view) sets only magnitude :1226; ==0 early-returns :1227; betWei capped 1 ether :1225; transferFromPool onlyGame (one-directional pull). Draining the pool only DOWN-SCALES an already-earned grant. Freeze of the VRF outcome holds; magnitude intentionally pool-relative (CROSS-CONTRACT-SEAM). BACKING DEFECT (no freeze impact): the cited testDgnrsAwardStaysPerSpin (DegeneretteFreezeResolution.t.sol:719) is vm.skip(true) at :720 — a DISABLED test presented as magnitude backing; the gate-freeze is source-provable independent of it.
- **Backing:** test/fuzz/DegeneretteFreezeResolution.t.sol (testDgnrsAwardStaysPerSpin currently vm.skip); DegeneretteHeroScore.t.sol:test_HERO_DgnrsThresholdsRemapped

---

## Cluster: JackpotModule — terminal/daily/purchase/early-bird/FLIP/BAF jackpot consumers + BAF sampler seams

_All v66 JackpotModule rows (35-48) + BAF sampler/dispatch rows (20-21,49-50) exist at HEAD; no removals. Single commitment for all daily/purchase/early-bird/FLIP/BAF words is _applyDailyRng:1930 (terminal via _gameOverEntropy:1309). Hero-override freshness: consumed dailyHeroWagers[dailyIdx] read at the FROZEN index (sole writer _unlockRng:1804) while the writer keys wall-clock _simulatedDayIndex() — disjoint slot during day-D resolution. Trait-pool freeze: traitBurnTicket written ONLY by the advance-stack _raritySymbolBatch (VRF-labelled). Far-future freeze: rngLockedFlag-gated writes spanning request->unlock. JKPT-39/43 are NEEDS-PROOF (rest on unlock-discipline / dailyIdx-advance; RngReuseJackpotStraddle 3/3 PASS is the operative regression — recommend CI gate). VERIFY caveat: the JS hero-override harness (HeroOverrideDayIndex 8-fail, HeroOverrideWeightedRoll 2-fail) is stale (wagerUnit /1e12 vs contract /1e14; garbage dailyIdx slot), NOT a contract regression — Foundry backing (RngReuse 3/3, JackpotCapBound 2/2, BafFarFutureTickets 6/6, DegeneretteHeroScore 6/6) all PASS._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-JKPT-35` | contracts/modules/DegenerusGameJackpotModule.sol:239 (_rollWinningTraits(rngWord,false)) and :243-246 (_soloAdjustedEntropy(traitIds,EntropyLib.hash2(rngWord,targetLvl))); winner draw :259 _processDailyEth -> :1147 _randTraitTicket idx=keccak(randomWord,trait,salt,i)%effectiveLen :1500-1502 | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord (via _gameOverEntropy:1309 _applyDailyRng) — written inside _handleGameOverPath BEFORE handleGameOverDrain (AdvanceModule:644-704). Word re-read GameOverModule:93. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-36` | contracts/modules/DegenerusGameJackpotModule.sol:302 _rollWinningTraitsPair(randWord); :331 _dailyCurrentPoolBps(counter,randWord); :369-379 keccak(randWord,DAILY_CARRYOVER_SOURCE_TAG,counter); :411-414 _soloAdjustedEntropy(traitIdsDaily,hash2(randWord,lvl)); winners :433 _processDailyEth -> _randTraitTicket :1500 | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord (rngGate:1239). randWord passed AdvanceModule:567 payDailyJackpot(true,lvl,rngWord) = rngGate return == rngWordByDay[day] == rngWordCurrent. | FROZEN-AT-COMMIT | yes | LOW | yes |
| `RNGF-JKPT-37` | contracts/modules/DegenerusGameJackpotModule.sol:468 unpackWinningTraits(from :302 _rollWinningTraitsPair); :469-472 _soloAdjustedEntropy(traitIds,hash2(randWord,lvl)); :515 _processDailyEth; :533-540 _distributeLootboxAndTickets(...,randWord,5000) -> _distributeTicketJackpot entropy=hash2(randWord,lvl) :774 | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord (rngGate:1239). randWord passed AdvanceModule:487 payDailyJackpot(false,purchaseLevel,rngWord). | FROZEN-AT-COMMIT | yes | LOW | yes |
| `RNGF-JKPT-38` | contracts/modules/DegenerusGameJackpotModule.sol:632-634 _rollWinningTraits(rngWord,true) -> bonusTraits; :642 _randTraitTicket(bucket,rngWord,traitId,25,t) winner draw idx=keccak(randomWord,trait,salt,i)%effectiveLen :1500 | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. Called from payDailyJackpot:342 _runEarlyBirdLootboxJackpot(lvl+1,randWord) only on isEarlyBirdDay (counter==0), within the same delegatecall. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-39` | contracts/modules/DegenerusGameJackpotModule.sol:571 _rollWinningTraitsPair(randWord); :574 _runFlipJackpot(...,randWord); :584 _distributeTicketJackpot(...,hash2(randWord,lvl),...); :602 _distributeTicketJackpot(...,hash2(randWord,sourceLevel),...) | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. randWord passed AdvanceModule:555 payDailyJackpotCoinAndTickets(rngWord); the contract requires randWord == rngWordCurrent from Phase 1 (the same day's committed word). | NEEDS-PROOF | yes | MED | yes |
| `RNGF-JKPT-40` | contracts/modules/DegenerusGameJackpotModule.sol:1629-1631 lvlPrime=minLevel+keccak(randomWord,FLIP_LEVEL_TAG,i)%range; :1644-1646 idx=keccak(randomWord,trait_i,lvlPrime,i)%effectiveLen; winner=traitBurnTicket[lvlPrime][trait_i][idx] | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. Reached via _runFlipJackpot from payDailyJackpotCoinAndTickets:574 (randWord) or payDailyFlipJackpot:1526 (randWord) — threaded from rngGate. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-41` | contracts/modules/DegenerusGameJackpotModule.sol:1692-1694 entropy=keccak(rngWord,lvl,FAR_FUTURE_FLIP_TAG); :1702 entropy=hash2(entropy,s); :1705 candidate=lvl+5+entropy%95; :1711 winner=queue[(entropy>>32)%len] over ticketQueue[_tqFarFutureKey(candidate)] | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. Reached via _runFlipJackpot:1548 from the same advance-stack callers. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-42` | contracts/modules/DegenerusGameJackpotModule.sol:1525 _rollWinningTraits(randWord,true) -> bonusTraitsPacked; :1526 _runFlipJackpot(lvl,level,minLevel,maxLevel,bonusTraitsPacked,randWord) -> near-future (JKPT-40) + far-future (JKPT-41) draws | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. Via the internal _payDailyCoinJackpot wrapper (AdvanceModule:479/485/488 at purchaseLevel==1, :1071 selector dispatch payDailyFlipJackpot) passing rngWord/saltedRng. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-43` | contracts/modules/DegenerusGameJackpotModule.sol:1396-1398 pick=uint64(keccak(abi.encode(entropy,day))%effectiveTotal); cumulative-weight walk :1400-1412 returns (quadrant,symbol) | Two-part: (P1) consumed weights dailyHeroWagers[day] settled at the previous day's _unlockRng boundary (AdvanceModule:1804 advances dailyIdx); (P2) VRF entropy rngWordByDay[day] frozen at AdvanceModule:1930. Caller passes day=dailyIdx (via _applyHeroOverride:1309 and _rollWinningTraitsPair:1785). | NEEDS-PROOF | yes | MED | yes |
| `RNGF-JKPT-44` | contracts/modules/DegenerusGameJackpotModule.sol:1326-1334 heroColor=(randomWord>>{0,3,6,9})&7 by quadrant; :1336-1340 writes w[heroQuadrant] | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. randomWord is the per-roll word (randWord main, EntropyLib.hash2(randWord,BONUS_TRAITS_TAG) bonus), both frozen-word descendants. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-JKPT-45` | contracts/modules/DegenerusGameJackpotModule.sol:1096-1098 entropyState=keccak(entropyState,traitIdx,share) chained; :1102 _processBucket -> :1147 _randTraitTicket -> :1500-1502 idx=keccak(randomWord,trait,salt,i)%effectiveLen; bucket counts/shares :1072-1075 JackpotBucketLib | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord (daily/purchase) or :1309 (terminal). The `entropy` passed into _processDailyEth is _soloAdjustedEntropy(traitIds,hash2(rngWord,lvl/targetLvl)) — a frozen-word descendant computed at the caller (runTerminalJackpot:243, payDailyJackpot:411/469). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-JKPT-46` | contracts/modules/DegenerusGameJackpotModule.sol:1839-1844 seed=keccak(abi.encodePacked(randWord,DAILY_CURRENT_BPS_TAG,counter)); return DAILY_CURRENT_BPS_MIN+seed%range | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. Called from payDailyJackpot:331 only on !isFinalPhysicalDay. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-JKPT-47` | contracts/modules/DegenerusGameJackpotModule.sol:1806-1809 coinEntropy=keccak(randWord,lvl,FLIP_JACKPOT_TAG); bonusTargetLevel=lvl+1+coinEntropy%4; emit DailyWinningTraits. External emitDailyWinningTraits:1571-1573 _rollWinningTraits(randWord,true) + saltedRng bonus traits. | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. emitDailyWinningTraits dispatched AdvanceModule:474 (purchaseLevel==1) via IDegenerusGame(address(this)).emitDailyWinningTraits with the rngWord; _emitDailyWinningTraits called inline from payDailyJackpot:455/474. | FROZEN-AT-COMMIT | yes | INFO | yes |
| `RNGF-JKPT-48` | contracts/modules/DegenerusGameJackpotModule.sol:1918-1919 jackpots.runBafJackpot(poolWei,lvl,rngWord) returns winners; recirc roll _jackpotTicketRoll:2083 entropy=hash2(entropy,entropy), level select :2086-2101 (entropy%100,/100%4,/100%46), Bernoulli round-up :2115 uint16(entropy>>200)%TICKET_SCALE | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. BAF fires from _consolidatePoolsAndRewardJackpots:932 only when (rngWord&1)==1 (bit-0 gate :928); rngWord is the rngGate-returned committed word for the transition advance. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-20` | contracts/DegenerusGame.sol:2318 traitSel=uint8(entropy>>24); :2327 start=(entropy>>40)%len; :2329 tickets[i]=arr[(start+i)%len] over traitBurnTicket[targetLvl][traitSel]. Consumed DegenerusJackpots.runBafJackpot:389. | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. The entropy arg is the DegenerusJackpots BAF hash2-chain off rngWord (:374); targetLvl DegenerusJackpots-chosen from the frozen word (:380-387). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-21` | contracts/DegenerusGame.sol:2350 word=keccak(word,s); :2351 candidate=currentLvl+5+word%95; :2356-2357 idx=(word>>32)%len; winner=queue[idx] over ticketQueue[_tqFarFutureKey(candidate)]. Consumed DegenerusJackpots.runBafJackpot:318. | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord. The entropy arg is the DegenerusJackpots BAF hash2-chain off rngWord (:317). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-49` | contracts/DegenerusJackpots.sol:300 pick=2+uint8(entropy&1) (_bafTop slot); :380-387 targetLvl=lvl +- entropy%{3,4,99} scatter; entropy chain :298/:317/:374 = hash2(entropy,salt) off rngWord | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord — passed through Game.runBafJackpot self-call (AdvanceModule:932 -> DegenerusGame:1013 delegatecall -> JackpotModule:1918 jackpots.runBafJackpot(poolWei,lvl,rngWord)). Bracket epoch read once DegenerusJackpots:266 bafLevel[lvl].epoch. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-50` | contracts/DegenerusJackpots.sol:318 farTickets=degenerusGame.sampleFarFutureTickets(entropy) then best/second by _bafScore :329-342; :284 coin.coinflipTopLastDay() (deterministic top-bettor read for slice A2) | AdvanceModule.sol:1930 rngWordByDay[day]=finalWord (entropy seed). The coinflip leaderboard (coinflipTopByDay) written by Coinflip._updateTopDayBettor during coinflip bets and read for a settled prior-day window. | CROSS-CONTRACT-SEAM | yes | n/a | yes |

#### `RNGF-JKPT-35` — runTerminalJackpot — winning-trait roll + solo-adjusted entropy + bucket counts (x00 terminal, GameOver)
- **Invariant:** rngWord passed to runTerminalJackpot is rngWordByDay[day], determined at :1930 before _handleGameOverPath reaches handleGameOverDrain :698. (a) gameOver latched TRUE GameOverModule:135 INSIDE handleGameOverDrain; runTerminalJackpot access-gated msg.sender==GAME :237, reachable only via the GameOver self-call :191; (b) winner pool traitBurnTicket[targetLvl] membership fixed at purchase/burn and VRF-labelled in the advance drain (_raritySymbolBatch MintModule:744-799); (c) _queueTickets reverts once _livenessTriggered() Storage:618. No actor input changes which word or how winners derive.
- **Frozen inputs:** rngWord==rngWordByDay[day] single-shot/immutable (:1209-1210/:1305 short-circuit); written once :1930; targetLvl lvl+1 computed by GameOverModule:191 from storage level, not caller-supplied; poolWei `remaining` GameOverModule:165-191 magnitude only; traitBurnTicket[targetLvl] membership pushed by the advance-stack ticket drain; trait label VRF-derived LCG, not player-selectable; gameOver latch true GameOverModule:135 before the terminal jackpot call
- **Live inputs (scale-not-select):** deityBySymbol[fullSymId] (_randTraitTicket :1459 / _computeBucketCounts :954) virtual-entry augmentation; deity identity admin/issuance state, fixed virtual holder, scored against the word not steering it
- **Verification recipe:** grep function runTerminalJackpot :237 OnlyGame `msg.sender!=ContractAddresses.GAME revert OnlyGame()`; sole caller GameOverModule:191 preceded by gameOver=true :135 and rngWord=rngWordByDay[day] :93; _handleGameOverPath writes rngWordByDay[day] via _gameOverEntropy->_applyDailyRng:1930 :644-651 BEFORE handleGameOverDrain :698-702; short-circuit :1209/:1305; _queueTickets revert _livenessTriggered() Storage:618.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: supply a chosen rngWord to runTerminalJackpot to control the winning-trait roll/winner draw. Defeated: Game.runTerminalJackpot (DegenerusGame.sol:1104) reverts unless msg.sender==address(this); only caller GameOverModule:191 self-call with rngWord=rngWordByDay[day] (:93), committed once at _applyDailyRng:1930 / _gameOverEntropy:1305-1309. Module guard msg.sender==GAME :237. Winner pool traitBurnTicket[targetLvl] written ONLY by _raritySymbolBatch (inline-asm push :783) with trait label = LCG of keccak(baseKey,entropyWord,groupIdx) :745-758 — VRF/address-derived, player cannot pick the bucket; no external .push exists. New tickets blocked once _livenessTriggered() (Storage:618). _rollWinningTraits/getRandomTraits and JackpotBucketLib are pure functions of the frozen word. JackpotCapBound.t.sol 2/2 PASS.
- **Backing:** test/fuzz/JackpotCapBound.t.sol; test/halmos/Arithmetic.t.sol; test/integration/JackpotSoloSplit.test.js; test/unit/JackpotSoloPicker.test.js

#### `RNGF-JKPT-36` — payDailyJackpot (jackpot-phase) — winning-trait pair + hero override + carryover + daily-BPS + solo entropy
- **Invariant:** randWord the committed daily word fixed at :1930. (a) hero-override pool dailyHeroWagers[dailyIdx] read at a FROZEN index — dailyIdx written only by _unlockRng :1804 at the END of the previous day's cycle, so during day-D resolution dailyIdx==D-1 while a fresh bet keys to _simulatedDayIndex()==D (Degenerette:569), a DIFFERENT slot; (b) traitBurnTicket[lvl] membership fixed pre-word and VRF-labelled; (c) far-future ticketQueue writes revert under rngLockedFlag (Storage:621). counter/jackpotCounter advance-driven.
- **Frozen inputs:** randWord==rngWordByDay[day] single-shot :1930; payDailyJackpot reachable only via the advance self-stack; lvl storage level advance-controlled; dailyIdx hero-read key written ONLY by _unlockRng :1804; frozen at D-1 across the rng-lock window; jackpotCounter/counter advance-driven; traitBurnTicket[lvl] membership fixed at purchase/burn; VRF-labelled
- **Live inputs (scale-not-select):** dailyHeroWagers[dailyIdx][q] (_rollHeroSymbol :1366) a SETTLED prior-cycle-day pool; read index frozen by design at the previous day; current-window bets key a different slot; scored-against-not-steering; curPool/futPool magnitudes pool sizes only, no preimage
- **Verification recipe:** grep payDailyJackpot internal wrapper AdvanceModule:1018 payDailyJackpot.selector; _rollHeroSymbol reads dailyHeroWagers[dailyIdx] via _applyHeroOverride:1309/_rollWinningTraitsPair:1785; dailyIdx sole writer _unlockRng:1804; _simulatedDayIndex Storage:1429 == GameTimeLib.currentDayIndex() wall-clock; Degenerette writer keys dailyHeroWagers[_simulatedDayIndex()] :569,573,581.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Refutation: a bettor who has seen day-D's word injects into the consumed hero pool to bias the hero-override winner. Defeated: consumer reads dailyHeroWagers[dailyIdx] (:1309/:1785), dailyIdx sole writer _unlockRng :1804, so during day-D resolution dailyIdx==D-1; the Degenerette writer keys dailyHeroWagers[_simulatedDayIndex()]=GameTimeLib.currentDayIndex() (wall-clock, :569) which is >= D during the lock window — a DISJOINT slot. randWord frozen :1930. payDailyJackpot NOT externally exposed (only internal wrapper AdvanceModule:1018 delegatecalls it). RngReuseJackpotStraddle 3/3 + DegeneretteHeroScore 6/6 PASS. CAVEAT (LOW if relied on): the cited JS backing test/edge/HeroOverrideDayIndex.test.js does NOT reproduce green (8/8 failing) — stale-harness (expects MIN_BET/1e12 while the contract computes wagerUnit=totalBet/1e14 at DegeneretteModule:571; reads dailyIdx from a stale storage-slot offset yielding garbage), NOT a contract freeze regression. The source freeze argument and the Foundry backing hold.
- **Backing:** test/edge/HeroOverrideDayIndex.test.js (STALE JS); test/fuzz/RngReuseJackpotStraddle.t.sol; test/fuzz/DegeneretteHeroScore.t.sol

#### `RNGF-JKPT-37` — payDailyJackpot (purchase-phase) — winning traits + hero override + solo-adjusted ETH + lootbox/ticket bonuses
- **Invariant:** Identical freeze to RNGF-JKPT-36 on the purchase-phase branch: randWord committed at :1930; hero-override consumes the frozen dailyIdx pool; traitBurnTicket[lvl] membership pre-word fixed and VRF-labelled; far-future writes rngLockedFlag-gated. Additionally distributes lootbox tickets via _distributeTicketJackpot with entropy=hash2(randWord,lvl) — pure keccak of the frozen word; no live player input enters the winner-index keccak.
- **Frozen inputs:** randWord==rngWordByDay[day] frozen :1930; purchaseLevel/lvl advance-controlled; dailyIdx hero read key _unlockRng-only writer, frozen at D-1; winningTraitsPacked from _rollWinningTraitsPair(randWord) deterministic from frozen word + frozen hero pool; traitBurnTicket[lvl] membership pre-word fixed, VRF-labelled
- **Live inputs (scale-not-select):** dailyHeroWagers[dailyIdx] settled prior-cycle pool; frozen read index; futureBal magnitude (_getFuturePrizePool :483) 1% slice size only
- **Verification recipe:** confirm the purchase-phase branch after `return;` :464 reads winningTraitsPacked from the same :302; _distributeLootboxAndTickets passes hash2(randWord,lvl) :774 and _randTraitTicket idx=keccak(randomWord,trait,salt,i)%effectiveLen :1500-1502 no mutable-player SLOAD; same dailyIdx/wall-clock disjointness.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Refutation: same hero-pool injection on the purchase-phase branch, plus biasing _distributeTicketJackpot winner index. Defeated: identical hero freeze to RNGF-JKPT-36 (read dailyIdx != write wall-clock). Ticket entropy = EntropyLib.hash2(randWord,lvl) :774; winner idx = keccak(randomWord,trait,salt,i)%effectiveLen :1500-1502 — pure keccak descendants of the frozen word, no mutable-player SLOAD in the index derivation. traitBurnTicket membership VRF-labelled, no external push. Same JS-backing caveat as JKPT-36 (HeroOverrideDayIndex stale-harness fail; Foundry backing JackpotSingleCallCorrectness/RngReuse pass).
- **Backing:** test/edge/HeroOverrideDayIndex.test.js (STALE JS); test/fuzz/JackpotSingleCallCorrectness.t.sol; test/halmos/Arithmetic.t.sol

#### `RNGF-JKPT-38` — _runEarlyBirdLootboxJackpot — bonus-trait winner draw (day-1 jackpot-phase only)
- **Invariant:** rngWord committed at :1930. Bonus traits derive from EntropyLib.hash2(rngWord,BONUS_TRAITS_TAG) and the per-quadrant hero override over the frozen dailyIdx pool. Winners drawn from traitBurnTicket[lvl+1], membership fixed pre-word and VRF-labelled in the advance drain. No actor input selects which bonus traits roll or which holder wins.
- **Frozen inputs:** rngWord==rngWordByDay[day] frozen :1930; reachable only inside payDailyJackpot delegatecall; lvl outer level+1 advance-controlled :342; bonusTraits _rollWinningTraits(rngWord,true) keccak(rngWord,BONUS_TRAITS_TAG)-derived, hero-overridden against the frozen dailyIdx pool; traitBurnTicket[lvl] membership pre-word fixed, VRF-labelled; ticketCount/ticketPrice PriceLookupLib.priceForLevel + futureBal magnitude :623-629
- **Live inputs (scale-not-select):** deityBySymbol[fullSymId] (virtual entries _randTraitTicket :1459) admin/issuance state, fixed virtual holder; futureBal magnitude (_getPrizePools :622) budget size only
- **Verification recipe:** grep _runEarlyBirdLootboxJackpot sole caller payDailyJackpot:342 (isEarlyBirdDay); bonusTraits=unpackWinningTraits(_rollWinningTraits(rngWord,true)) :632-634; winner idx keccak :1500-1502; bucket=traitBurnTicket[lvl] :635, lvl==outer+1.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: select which bonus trait / holder wins the 25-per-trait early-bird draw. Defeated: sole caller payDailyJackpot:342 (isEarlyBirdDay, counter==0) inside the locked advance; rngWord frozen :1930. bonusTraits = unpackWinningTraits(_rollWinningTraits(rngWord,true)) :632-634 = keccak(rngWord,BONUS_TRAITS_TAG)-derived; winner idx keccak :1500-1502 over traitBurnTicket[lvl] (bucket=traitBurnTicket[lvl] :635, lvl==outer+1). _queueTickets uses rngBypass=true :647 but is unreachable by a player. Trait membership VRF-labelled. No actor input between P and C.
- **Backing:** test/stat/PerPullEmptyBucketSkip.test.js; test/fuzz/JackpotSingleCallCorrectness.t.sol; test/halmos/Arithmetic.t.sol

#### `RNGF-JKPT-39` — payDailyJackpotCoinAndTickets (Phase-2) — coin jackpot + daily/carryover ticket distribution
- **Invariant:** randWord must equal the Phase-1 committed daily word (rngWordCurrent == rngWordByDay[day]). dailyJackpotCoinTicketsPending gates re-entry :556. The day-index race is closed: this Phase-2 call runs in a LATER advance that re-derives traits from the same frozen randWord and reads the frozen dailyIdx hero pool. Ticket entropy hash2(randWord,lvl)/hash2(randWord,sourceLevel); far-future coin draw inside _runFlipJackpot reads ticketQueue (writes rngLockedFlag-gated). The RngReuse straddle is the failure mode this freeze depends on NOT occurring — guarded by rngGate gap-backfill + dailyIdx advance discipline.
- **Frozen inputs:** randWord must==rngWordCurrent/rngWordByDay[day] from Phase 1; AdvanceModule:555 passes the rngGate return for the same advance; lvl storage level :567; dailyTicketBudgetsPacked/counterStep/carryoverSourceOffset written by Phase-1 payDailyJackpot:399, advance-controlled; dailyIdx hero read key _unlockRng-only writer, frozen at D-1; traitBurnTicket[lvl]/[sourceLevel] + far-future ticketQueue pre-word fixed; far-future rngLockedFlag-gated
- **Live inputs (scale-not-select):** dailyHeroWagers[dailyIdx] settled prior-cycle pool, frozen read index; jackpotCounter :590 advance-driven; levelPrizePool[lvl-1] magnitude (_calcDailyCoinBudget :1828) budget only
- **Verification recipe:** grep payDailyJackpotCoinAndTickets internal wrapper AdvanceModule:1040 dispatched only when dailyJackpotCoinTicketsPending :554; module early-returns on !pending :556 and re-derives traits from the SAME randWord :571; ticket entropy hash2(randWord,lvl) :584 / hash2(randWord,sourceLevel) :602; RngReuseJackpotStraddle.t.sol wall-day straddle.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`MED`. Refutation (NEEDS-PROOF class): wall-day straddle reuse — make Phase-2 consume a re-revealed prior word, or inject into the hero pool between Phase-1 and Phase-2. Defeated by source + direct regression: payDailyJackpotCoinAndTickets early-returns on !dailyJackpotCoinTicketsPending :556 and re-derives traits from the SAME randWord :571; ticket entropy = hash2(randWord,lvl)/hash2(randWord,sourceLevel) :584/:602. The RNGREUSE guard at AdvanceModule:184-186 clamps day to dIdx+1 so a NEW wall-day never resolves with a prior unsealed word. test/fuzz/RngReuseJackpotStraddle.t.sol 3/3 PASS (testFix_PendingStraddle_DPlus1GetsFreshWord, testFix_TransitionStraddle_DPlus1GetsFreshWord, testControl). Hero pool frozen at dailyIdx (JKPT-36). Recommend promoting RngReuseJackpotStraddle to a CI gate.
- **Backing:** test/fuzz/RngReuseJackpotStraddle.t.sol (DIRECT 3/3 PASS); test/edge/HeroOverrideDayIndex.test.js; test/edge/CompressedJackpot.test.js

#### `RNGF-JKPT-40` — _runFlipJackpot / _awardDailyCoinToTraitWinners — near-future FLIP winner draw (per-pull level sampling)
- **Invariant:** randomWord committed at :1930. Both the per-pull level sample (lvlPrime) and the holder index are pure keccak descendants of randomWord with loop-fixed salts (FLIP_LEVEL_TAG,trait_i,i). Winner pool traitBurnTicket[lvlPrime][trait_i] membership fixed pre-word and VRF-labelled; deity virtual entries admin state. No actor input alters which level/trait/holder is selected.
- **Frozen inputs:** randomWord==rngWordByDay[day] frozen :1930; minLevel/maxLevel/range passed by caller from advance-controlled level (:574 lvl+1..lvl+4; :1526 caller minLevel/maxLevel); winningTraitsPacked (trait_i) _rollWinningTraits-derived from the frozen word; traitBurnTicket[lvlPrime][trait_i] membership pre-word fixed, VRF-labelled; deityCache[traitIdx] read once at loop entry :1604-1611 from deityBySymbol admin state
- **Live inputs (scale-not-select):** deityBySymbol (deityCache) admin state, fixed virtual holder scored against the word; coinBudget magnitude (_calcDailyCoinBudget) per-pull baseAmount size only
- **Verification recipe:** grep _awardDailyCoinToTraitWinners/_runFlipJackpot/payDailyFlipJackpot callers :1552 (from _runFlipJackpot <- :574,:1526); lvlPrime keccak :1629-1631 and holder-idx keccak :1644-1646 take only (randomWord,fixed tags,loop i,lvlPrime); holders=traitBurnTicket[lvlPrime][trait_i] :1633.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: add membership to traitBurnTicket[lvlPrime][trait_i] for a sampled near-future level (lvl+1..lvl+4) after the word is knowable, to land in the per-pull draw. Defeated: lvlPrime = minLevel + keccak(randomWord,FLIP_LEVEL_TAG,i)%range :1629-1631 and holder idx = keccak(randomWord,trait_i,lvlPrime,i)%effectiveLen :1644-1646 are pure frozen-word keccaks. holders = traitBurnTicket[lvlPrime][trait_i] :1633; membership VRF-labelled in the advance drain (only writer _raritySymbolBatch), trait LABEL not player-choosable. deityCache read once :1604-1611 from admin state. No mutable player SLOAD enters either keccak.
- **Backing:** test/unit/JackpotNearFutureCoinFloor.test.js; test/stat/JackpotTicketRollSeedUniqueness.test.js

#### `RNGF-JKPT-41` — _awardFarFutureCoinJackpot — far-future ticketQueue FLIP winner draw
- **Invariant:** rngWord committed at :1930. Sampled levels and winner indices are pure keccak/hash2 descendants of rngWord with loop-fixed salt s. far-future ticketQueue[_tqFarFutureKey(candidate)] structurally protected: writes revert under rngLockedFlag (Storage:621/653/715), so during the rng-lock window of the resolving day no player can add/remove far-future membership. Consumed pool frozen before the word is knowable (lock set _requestRng :1702, cleared _unlockRng :1805).
- **Frozen inputs:** rngWord==rngWordByDay[day] frozen :1930; lvl advance-controlled level; ticketQueue[_tqFarFutureKey(candidate)] membership FAR-FUTURE writes rngLockedFlag-gated (Storage:621,653,715) — frozen across the lock window bracketing the reveal
- **Verification recipe:** grep _awardFarFutureCoinJackpot caller _runFlipJackpot:1548; entropy chain :1692/:1702 winner index :1711 take only (rngWord,lvl,FAR_FUTURE_FLIP_TAG,s); isFarFuture && rngLockedFlag Storage:621/:653/:715 revert RngLocked; rngLockedFlag set _requestRng :1702 cleared _unlockRng :1805.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: add/remove far-future ticketQueue membership after the word is knowable to be the queue[idx] winner. Defeated: entropy=keccak(rngWord,lvl,FAR_FUTURE_FLIP_TAG) :1692-1694, candidate=lvl+5+entropy%95 :1705, winner=queue[(entropy>>32)%len] :1711 — pure frozen-word descendants. ticketQueue[_tqFarFutureKey(candidate)] far-future writes revert RngLocked when rngLockedFlag set (Storage:621/653/715); lock set _requestRng:1702, cleared only _unlockRng:1805 — brackets the rngWordByDay[day] write. Boundary check: candidate==lvl+5 reads _tqFarFutureKey(lvl+5) but writes to targetLevel==level+5 go to _tqWriteKey (isFarFuture strictly targetLevel>level+5), a DIFFERENT key — no unlocked write populates the sampled bucket. test/fuzz/BafFarFutureTickets.t.sol 6/6 PASS.
- **Backing:** test/unit/JackpotFarFutureCoinFloor.test.js; test/fuzz/BafFarFutureTickets.t.sol

#### `RNGF-JKPT-42` — payDailyFlipJackpot (purchaseLevel==1 path) — bonus traits + FLIP jackpot
- **Invariant:** randWord committed at :1930 (or, on the second day-1 call, keccak(rngWord,BONUS_TRAITS_TAG) of it — a deterministic salt, still a frozen-word descendant). Both the near-future trait pool and far-future ticketQueue freeze exactly as JKPT-40/41. Access gated: dispatch only from the advance self-stack. No actor input alters the word or winner pools.
- **Frozen inputs:** randWord (and saltedRng=keccak(rngWord,BONUS_TRAITS_TAG)) frozen-word descendants :1930; lvl/level/minLevel/maxLevel advance-controlled (purchaseLevel==1 -> minLevel/maxLevel=1 or 2..5 :479/:485); bonusTraitsPacked _rollWinningTraits(randWord,true) frozen-word derived; traitBurnTicket + far-future ticketQueue membership pre-word fixed; far-future rngLockedFlag-gated
- **Live inputs (scale-not-select):** deityBySymbol (deityCache near-future leg) admin state; levelPrizePool/coinBudget magnitude budget only
- **Verification recipe:** grep payDailyFlipJackpot/_payDailyCoinJackpot AdvanceModule:479/485/488 (purchaseLevel==1), :1071 selector dispatch; payDailyFlipJackpot:1524 derives bonusTraitsPacked from randWord and forwards to _runFlipJackpot:1526; inherit JKPT-40/41 recipes.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias the day-1 FLIP jackpot near/far legs. Defeated: payDailyFlipJackpot:1524-1526 derives bonusTraitsPacked from _rollWinningTraits(randWord,true) (frozen-word descendant) and forwards to _runFlipJackpot; both legs inherit JKPT-40 (frozen-word keccak + VRF-labelled pool) and JKPT-41 (frozen-word keccak + rngLockedFlag-gated far-future queue). Reachable only via internal _payDailyCoinJackpot wrapper (AdvanceModule:479/485/1061, private/internal) inside the locked advance; not externally exposed. randWord and saltedRng=keccak(rngWord,BONUS_TRAITS_TAG) are frozen-word descendants.
- **Backing:** test/unit/JackpotNearFutureCoinFloor.test.js + JackpotFarFutureCoinFloor.test.js; test/fuzz/BafFarFutureTickets.t.sol

#### `RNGF-JKPT-43` — _rollHeroSymbol — weighted hero (quadrant,symbol) roll over dailyHeroWagers
- **Invariant:** The hero roll consumes (a) entropy = the frozen daily word and (b) weights = dailyHeroWagers[dailyIdx], read at the FROZEN index dailyIdx (writer _unlockRng only). During day-D resolution dailyIdx==D-1; a bettor who has observed day-D's word can only write to dailyHeroWagers[wall-clock-day==D] (Degenerette:569,581) — a DISJOINT slot. Neither the entropy nor the weight pool consumed is actor-controllable after day-D's word becomes knowable. The leaderBonus (maxAmount/2) is a deterministic function of the frozen weights.
- **Frozen inputs:** day==dailyIdx written only by _unlockRng :1804; frozen across the day-D rng-lock window; entropy==rngWordByDay[day] frozen :1930; dailyHeroWagers[day][q] weights written by placeDegeneretteBet at dailyHeroWagers[_simulatedDayIndex()] (:569,581); for the consumed index dailyIdx==D-1 the slot was closed at the D-1->D boundary
- **Live inputs (scale-not-select):** dailyHeroWagers[dailyIdx][q] (live SLOAD :1366) a settled prior-cycle pool; read index frozen-by-design at the previous day; current-window bets land in a different slot; scored-against (weighted draw), not steering
- **Verification recipe:** grep _rollHeroSymbol callers _applyHeroOverride:1309 (passes dailyIdx) and _rollWinningTraitsPair:1785; pick keccak(entropy,day) :1396-1398; dailyIdx sole writer _unlockRng:1804; dailyHeroWagers writer keys _simulatedDayIndex() (Degenerette:569) reader keys dailyIdx (:1366); _simulatedDayIndex()==GameTimeLib.currentDayIndex() Storage:1429-1431.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`MED`. Refutation (NEEDS-PROOF class): weight the hero (quadrant,symbol) roll by re-staking after day-D's word is knowable. Defeated: pick=keccak(entropy,day)%effectiveTotal :1396-1398 with entropy=frozen word; weights=dailyHeroWagers[day=dailyIdx] read at the FROZEN index (callers pass dailyIdx :1309/:1785). dailyIdx sole writer _unlockRng:1804; writer placeDegeneretteBet keys _simulatedDayIndex()=currentDayIndex() (wall-clock) :569,581. During day-D resolution dailyIdx==D-1 != wall-clock>=D, disjoint slot. leaderBonus=maxAmount/2 is a deterministic function of the frozen weights. Foundry DegeneretteHeroScore.t.sol 6/6 PASS + HeroOverrideWeightedRoll chi2/leader-bonus/single-bettor stats PASS. CAVEAT (LOW): the cited JS backing HeroOverrideDayIndex.test.js (8 fail) + HeroOverrideWeightedRoll (2 fail) do NOT reproduce green — stale harness (wagerUnit /1e12 vs contract /1e14; garbage dailyIdx slot read), not a freeze break.
- **Backing:** test/edge/HeroOverrideDayIndex.test.js (STALE JS); test/edge/HeroOverrideWeightedRoll.test.js (STALE JS); test/fuzz/DegeneretteHeroScore.t.sol (PASS)

#### `RNGF-JKPT-44` — _applyHeroResult — hero-color bit-slice sampling
- **Invariant:** heroColor is a pure bit-slice of the frozen randomWord; heroQuadrant/heroSymbol arrive from _rollHeroSymbol (already frozen on entropy + frozen dailyIdx pool, JKPT-43). The function is private pure — reads no storage. No actor input.
- **Frozen inputs:** randomWord per-roll frozen-word descendant (randWord or hash2(randWord,BONUS_TRAITS_TAG)); heroQuadrant/heroSymbol outputs of _rollHeroSymbol over frozen inputs (JKPT-43); hasHeroWinner boolean from _rollHeroSymbol over the frozen pool
- **Verification recipe:** confirm _applyHeroResult `private pure` :1322 no SLOAD; heroColor only from randomWord bit-slices :1326-1334; callers pass frozen-word descendants _rollWinningTraits:1768, _rollWinningTraitsPair:1788/1793.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: steer hero-color via storage. Defeated: _applyHeroResult is private pure :1316-1322 — reads NO storage; heroColor is a pure bit-slice of randomWord :1326-1334; heroQuadrant/heroSymbol come from _rollHeroSymbol over frozen inputs (JKPT-43). Callers pass frozen-word descendants (r=randWord or hash2(randWord,BONUS_TRAITS_TAG)) at _rollWinningTraits:1768 / _rollWinningTraitsPair:1788/1793. No actor input.
- **Backing:** test/edge/HeroOverrideWeightedRoll.test.js; test/helpers/rollHeroSymbolRef.mjs

#### `RNGF-JKPT-45` — _processDailyEth / _processBucket / _randTraitTicket — ETH winner selection (all ETH jackpot paths)
- **Invariant:** Winner-selection entropy is a keccak descendant of the frozen committed word; the per-bucket entropyState is chained by keccak(entropyState,traitIdx,share) where traitIdx/share are deterministic functions of the frozen word and frozen pool magnitudes. Winner index = keccak(randomWord,trait,salt,i)%effectiveLen over traitBurnTicket[lvl] whose membership is pre-word fixed and VRF-labelled. bucketCounts from JackpotBucketLib.bucketCountsForPoolCap (deterministic in ethPool+entropy, capped).
- **Frozen inputs:** entropy/effectiveEntropy _soloAdjustedEntropy(traitIds,hash2(rngWord,lvl)) frozen-word descendant; traitIds _rollWinningTraits(Pair)-derived from frozen word + frozen hero pool; lvl/targetLvl advance/GameOver-controlled; bucketCounts/shareBps JackpotBucketLib pure functions of (ethPool,entropy) capped at DAILY_ETH_MAX_WINNERS; traitBurnTicket[lvl] membership pre-word fixed, VRF-labelled
- **Live inputs (scale-not-select):** deityBySymbol[fullSymId] (virtual entries _randTraitTicket :1459 / _computeBucketCounts :954) admin/issuance state, fixed virtual holder; ethPool/share magnitudes pool sizes only, drive perWinner amount not winner identity
- **Verification recipe:** confirm all three callers compute entropy=_soloAdjustedEntropy over hash2(<frozen word>,lvl): :243-246,:411-414,:469-472; _processDailyEth chains entropyState=keccak(entropyState,traitIdx,share) :1096-1098 and _randTraitTicket idx keccak :1500-1502; JackpotBucketLib.bucketCountsForPoolCap pure; JackpotCapBound.t.sol total<=maxTotal+4.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias ETH winner selection across all ETH paths via the entropy chain or bucket counts. Defeated: all 3 callers compute entropy=_soloAdjustedEntropy(traitIds, hash2(<frozen word>,lvl)) (runTerminalJackpot:243-246, payDailyJackpot:411-414 and :469-472) — frozen-word descendant; _processDailyEth chains entropyState=keccak(entropyState,traitIdx,share) :1096-1098; _randTraitTicket idx=keccak(randomWord,trait,salt,i)%effectiveLen :1500-1502. JackpotBucketLib.bucketCountsForPoolCap/bucketShares/soloBucketIndex are pure in (poolWei,entropy). traitBurnTicket membership VRF-labelled. deityBySymbol is admin/issuance state (fixed virtual holder scored-against). JackpotCapBound.t.sol 2/2 PASS.
- **Backing:** test/fuzz/JackpotCapBound.t.sol; test/fuzz/JackpotSingleCallCorrectness.t.sol; test/fuzz/JackpotCombinedPool.t.sol; test/halmos/Arithmetic.t.sol; test/integration/JackpotSoloSplit.test.js

#### `RNGF-JKPT-46` — _dailyCurrentPoolBps — random 6%-14% daily-pool percentage
- **Invariant:** bps is a pure keccak of the frozen daily word, the fixed DAILY_CURRENT_BPS_TAG salt, and counter (jackpotCounter, advance-driven). private pure — no storage read. No actor input; bps is a budget-percentage, not a winner selector.
- **Frozen inputs:** randWord==rngWordByDay[day] frozen :1930; counter jackpotCounter snapshot :310 advance-driven; DAILY_CURRENT_BPS_MIN/MAX/range compile-time constants
- **Verification recipe:** confirm _dailyCurrentPoolBps `private pure` :1837; seed=keccak(randWord,DAILY_CURRENT_BPS_TAG,counter) :1839-1842; sole caller payDailyJackpot:331 gated !isFinalPhysicalDay.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: grind the 6%-14% daily-pool bps. Defeated: _dailyCurrentPoolBps is private pure :1834-1837 — no storage read; seed=keccak(randWord,DAILY_CURRENT_BPS_TAG,counter) :1839-1844 with randWord frozen :1930 and counter=jackpotCounter (advance-driven). Sole caller payDailyJackpot:331 gated on !isFinalPhysicalDay. bps is a budget-percentage, not a winner selector. Halmos check_bps_split_bounded backs the [600,1400] bound.
- **Backing:** test/halmos/Arithmetic.t.sol; test/edge/CompressedJackpot.test.js

#### `RNGF-JKPT-47` — _emitDailyWinningTraits / emitDailyWinningTraits — bonus-target-level + bonus trait rolls (event-only)
- **Invariant:** All derived values (bonusTargetLevel, main/bonus traits) are keccak descendants of the frozen daily word with fixed tags (FLIP_JACKPOT_TAG,BONUS_TRAITS_TAG) and advance-controlled lvl. External entry access-gated msg.sender==GAME :1569. The function only emits an event — no fund/winner outcome derived, so even the hero-overridden traits here are informational. No actor input.
- **Frozen inputs:** randWord==rngWordByDay[day] frozen :1930; lvl/bonusTargetLevel arg advance-controlled; FLIP_JACKPOT_TAG / BONUS_TRAITS_TAG compile-time constants; dailyIdx (questDay=_simulatedDayIndex(), hero pool inside _rollWinningTraits) wall-clock day for the event label; hero pool read at frozen dailyIdx
- **Live inputs (scale-not-select):** dailyHeroWagers[dailyIdx] (inside _rollWinningTraits -> _applyHeroOverride) event-only, frozen read index
- **Verification recipe:** confirm emitDailyWinningTraits external guard msg.sender==GAME :1569; bonusTargetLevel=lvl+1+keccak(randWord,lvl,FLIP_JACKPOT_TAG)%4 :1806-1809; the only effect is emit DailyWinningTraits :1810,:1574 — no _creditClaimable/_queueTickets/coinflip call.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`INFO`. Refutation: manipulate the emitted bonusTargetLevel / bonus traits. Defeated and low-stakes: both _emitDailyWinningTraits (inline, :1799-1816) and external emitDailyWinningTraits (:1568-1575, guarded msg.sender==GAME :1569; Game wrapper DegenerusGame:1121 self-call-only) derive bonusTargetLevel=lvl+1+keccak(randWord,lvl,FLIP_JACKPOT_TAG)%4 :1806-1809 and traits from frozen-word keccaks. The ONLY effect is `emit DailyWinningTraits` :1810/:1574 — no _creditClaimable / _queueTickets / coinflip call, so no fund or winner outcome is derived. Hero pool read at frozen dailyIdx (event-only). Confirmed the external entrypoint is distinct (purchaseLevel==1 path AdvanceModule:474) and correctly access-gated.
- **Backing:** n/a (direct source argument); test/edge/HeroOverrideDayIndex.test.js for trait-roll determinism

#### `RNGF-JKPT-48` — runBafJackpot (Game-side dispatch) + ticket-recirc roll (_awardJackpotTickets / _jackpotTicketRoll)
- **Invariant:** rngWord committed at :1930. Winner SET computed cross-contract in DegenerusJackpots.runBafJackpot (RNGF-BAF-49/50). The Game-side recirc legs derive ticket levels/counts purely from keccak/hash2 descendants of the frozen rngWord. Access gated msg.sender==address(this) :1916, reachable only via the self-call :932. BAF fire gated on bit-0 of the frozen word; phaseTransitionActive lets recirc far-future rolls bypass the lock without opening player writes. No actor input changes the word, fire decision, or recirc derivation.
- **Frozen inputs:** rngWord==rngWordByDay[day] frozen :1930; BAF fire gated (rngWord&1)==1 :928; poolWei/lvl advance-controlled (bafPoolWei=baseMemFuture*pct/100 :929-930, lvl storage level); recirc level/count keccak descendants of the frozen rngWord (_jackpotTicketRoll entropy=hash2(entropy,entropy) :2083); winner SET returned by DegenerusJackpots.runBafJackpot (BAF-49/50)
- **Live inputs (scale-not-select):** PriceLookupLib.priceForLevel(targetLevel) :2104 deterministic price table, not player-set; futurePrizePool magnitude (memFuture) budget size only
- **Verification recipe:** confirm Game.runBafJackpot guard msg.sender==address(this) (DegenerusGame.sol:1018) and module guard :1916; sole on-chain caller AdvanceModule:932 inside _consolidate, gated (rngWord&1)==1 :928; _jackpotTicketRoll entropy/level/round-up :2083-2115 uses only the threaded entropy; phaseTransitionActive around _runRewardJackpots.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: flip the BAF fire decision or bias the recirc ticket roll. Defeated: Game.runBafJackpot reverts unless msg.sender==address(this) (DegenerusGame.sol:1018); module guard :1916; sole caller AdvanceModule:932 inside _consolidate, BAF fire gated (rngWord&1)==1 :928 — bit-0 of the frozen word. recirc _jackpotTicketRoll entropy=hash2(entropy,entropy) :2083, level select :2090-2101, Bernoulli round-up bits[200..215] :2115 — all frozen-word descendants; _queueTickets uses rngBypass=true :2121 but is private/unreachable by a player. Winner SET delegated to DegenerusJackpots (BAF-49/50). test/fuzz/BafFarFutureTickets.t.sol 6/6 PASS.
- **Backing:** test/fuzz/BafFarFutureTickets.t.sol; test/edge/BafCreditRouting.test.js; test/stat/JackpotTicketRollBernoulliEv.test.js

#### `RNGF-BAF-20` — sampleTraitTicketsAtLevel — BAF scatter trait-ticket sampler (Game view, consumed by DegenerusJackpots)
- **Invariant:** Pure view selecting trait and offset purely from the entropy bits (>>24 trait, >>40 offset) of a frozen-word keccak descendant. Candidate pool traitBurnTicket[targetLvl][traitSel] membership fixed at purchase/burn and trait label VRF-assigned by the advance ticket-drain. A player cannot choose which trait bucket their burned tickets land in (the LCG trait assignment consumes the committed entropyWord), nor add membership after the word (BAF runs inside the advance tx holding rngLockedFlag; the only trait-write path is the deterministic advance drain). Membership ordering append-only at burn time, pre-word.
- **Frozen inputs:** entropy DegenerusJackpots hash2-chain off the frozen rngWord :374; targetLvl DegenerusJackpots-computed from the frozen word :380-387, not player-supplied; traitBurnTicket[targetLvl][traitSel] membership fixed at burn/purchase; trait label VRF-assigned by _raritySymbolBatch (MintModule:744-799); arr ordering append-only push at burn, fixed before the consolidation advance
- **Verification recipe:** grep function sampleTraitTicketsAtLevel :2314 external view; traitSel=uint8(entropy>>24) :2318, start=(entropy>>40)%len :2327; caller DegenerusJackpots:389 entropy frozen-word hash2 chain; traitBurnTicket sole writer _raritySymbolBatch (MintModule:783-799 advance-stack); trait label VRF-derived LCG traitFromWord(s) :758.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: choose which trait bucket your burned tickets land in, or add membership after the word, to win the scatter sampler. Defeated: sampleTraitTicketsAtLevel is external view (:2314); traitSel=uint8(entropy>>24) and start=(entropy>>40)%len are pure entropy bit-slices (:2318,:2327) of the DegenerusJackpots frozen-word hash2 chain (:374). traitBurnTicket is written ONLY by _raritySymbolBatch (inline-asm push :783) with trait label = LCG of keccak(baseKey,entropyWord,groupIdx) :745-758 — player's address is mixed in but the VRF entropyWord dominates and is unpredictable pre-commit; assignment runs in the advance drain after commit. No direct external .push exists. Player controls WHETHER (by burning) not WHICH bucket nor the post-word draw.
- **Backing:** test/unit/DegenerusJackpots.test.js; test/fuzz/BafRebuyReconciliation.t.sol

#### `RNGF-BAF-21` — sampleFarFutureTickets — BAF far-future ticketQueue sampler (Game view, consumed by DegenerusJackpots)
- **Invariant:** Pure view selecting candidate level and queue index purely from keccak descendants of a frozen-word entropy chain. far-future ticketQueue[_tqFarFutureKey(candidate)] structurally frozen: far-future writes revert under rngLockedFlag (Storage:621/653/715). Because BAF runs inside the consolidation advance during the rng-lock window (lock set _requestRng:1702, cleared _unlockRng:1805), no player can add/remove far-future membership between the word's commitment and BAF consumption. currentLvl read live but monotonic advance-state.
- **Frozen inputs:** entropy DegenerusJackpots hash2-chain off the frozen rngWord :317; ticketQueue[_tqFarFutureKey(candidate)] membership far-future writes rngLockedFlag-gated (Storage:621,653,715) — frozen across the lock window; queue ordering append-only push at purchase (Storage:629,661,721), pre-word and lock-protected
- **Live inputs (scale-not-select):** currentLvl=level :2344 monotonic advance-state; selects the candidate-level base window, not a per-call player-steerable value (level only changes via the advance which holds the lock)
- **Verification recipe:** grep function sampleFarFutureTickets :2341 external view; candidate/index pure keccak descendants :2350-2357; caller DegenerusJackpots:318 entropy frozen-word hash2 chain; isFarFuture && rngLockedFlag Storage:621/:653/:715; lock set _requestRng:1702 cleared _unlockRng:1805.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: add to the far-future ticketQueue after the word to be the BAF far-future winner. Defeated: sampleFarFutureTickets is external view (:2341); candidate=currentLvl+5+word%95 and idx=(word>>32)%len are pure keccak descendants (:2350-2357) of the frozen-word chain (DegenerusJackpots:317). Far-future ticketQueue writes revert RngLocked when rngLockedFlag set (Storage:621/653/715); lock brackets the word (set _requestRng:1702, cleared _unlockRng:1805). BAF runs inside the consolidation advance holding the lock. currentLvl=level :2344 monotonic advance-state, changes only via the lock-holding advance. Same candidate==currentLvl+5 boundary as JKPT-41: writes to that level go to the non-far-future key, not the sampled pool. BafFarFutureTickets.t.sol 6/6 PASS.
- **Backing:** test/fuzz/BafFarFutureTickets.t.sol; test/unit/DegenerusJackpots.test.js

#### `RNGF-BAF-49` — DegenerusJackpots.runBafJackpot — 3rd/4th-slot pick + scatter level targeting
- **Invariant:** rngWord committed at :1930. All slot picks and scatter targets are keccak/hash2 descendants of rngWord with a monotonically-incrementing salt fixed per pass. Candidate pool = bafTop leaderboard (PlayerScore[4]) + the Game samplers (BAF-20/21). Player-controllable input = BAF coinflip stake (bafPlayer.total) which RANKS a player but is scored AGAINST the frozen word's draw; bracket epoch (currentEpoch :266) read once and threaded so a mid-resolution re-stake cannot change scores within the resolution. onlyGame :252.
- **Frozen inputs:** rngWord/entropy frozen committed word :1930, threaded via the onlyGame BAF dispatch; lvl/poolWei advance-controlled (Game self-call args); currentEpoch=bafLevel[lvl].epoch read once :266 and threaded through every _bafScore — frozen for the whole resolution; bafTop[lvl] PlayerScore[4] leaderboard snapshot of stakes accumulated before this resolution; rank-membership fixed pre-word
- **Live inputs (scale-not-select):** _bafScore(cand,lvl,currentEpoch) :331,:402 reads player BAF stake at the FROZEN epoch — a score not a steering input; cannot be changed for the current epoch mid-resolution (currentEpoch pinned :266)
- **Verification recipe:** grep function runBafJackpot DegenerusJackpots:245 onlyGame; entropy chain hash2(entropy,salt) :298/:317/:374 and pick/targetLvl :300/:380-387 use only entropy; currentEpoch read once :266 passed to every _bafScore :331,:402; sole caller Game BAF self-stack JackpotModule:1918.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-stake BAF score mid-resolution to steer the 3rd/4th-slot pick or scatter ranking. Defeated: runBafJackpot onlyGame (:245-252, msg.sender==GAME), entered only via the Game BAF self-stack (JackpotModule:1918, Game:1018 self-call). entropy chain hash2(entropy,salt) :298/:317/:374; pick=2+uint8(entropy&1) :300; scatter targetLvl derivations :380-387 use only entropy — frozen-word descendants. currentEpoch=bafLevel[lvl].epoch read ONCE :266 and threaded to every _bafScore :331,:402; _bafScore returns 0 on stale epoch :548. Candidate SET from the frozen-word samplers; score only RANKS within it (scored-against). recordBafFlip is onlyCoin and cannot reenter atomically between the epoch read and the score reads inside the advance tx.
- **Backing:** test/unit/DegenerusJackpots.test.js; test/fuzz/BafRebuyReconciliation.t.sol; test/edge/BafCreditRouting.test.js

#### `RNGF-BAF-50` — DegenerusJackpots.runBafJackpot — far-future BAF draws (slices D/D2) via sampleFarFutureTickets + coinflip-leaderboard slice
- **Invariant:** far-future draw entropy is a frozen-word hash2 descendant; the sampled ticketQueue is lock-frozen (BAF-21). The coinflipTopLastDay() slice reads a SETTLED 24h leaderboard window keyed by flipsClaimableDay (the VRF-resolved epoch) — membership determined by past coinflip stakes scored against the frozen word, not selectable at resolution. _bafScore ranking epoch-pinned (currentEpoch :266). No actor input changes the word, the lock-frozen far-future pool, or the settled coinflip leaderboard window.
- **Frozen inputs:** entropy frozen-word hash2 chain :317; far-future ticketQueue rngLockedFlag-gated, frozen across the lock window (BAF-21); coinflipTopByDay[flipsClaimableDay] settled-day leaderboard; flipsClaimableDay set from the VRF-resolved epoch (Coinflip side), membership from past stakes; currentEpoch (_bafScore) pinned :266
- **Live inputs (scale-not-select):** _bafScore(cand,lvl,currentEpoch) epoch-pinned score (scored-against); coinflipTopLastDay() top player membership is whoever out-staked over the settled window — a fixed past-window fact
- **Verification recipe:** confirm farTickets draw :318 with entropy from the frozen chain; ranking by _bafScore :329-342; coin.coinflipTopLastDay() :284 reads a settled-day leaderboard; far-future queue lock per BAF-21; currentEpoch pinned :266; coinflipTopByDay write _targetFlipDay (Coinflip:672,679) vs read flipsClaimableDay (:915).
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: inject into the coinflip-leaderboard slice or far-future BAF draw at resolution. Defeated: far-future entropy is a frozen-word hash2 chain (:317); sampled ticketQueue lock-frozen (BAF-21); ranking by _bafScore epoch-pinned :266. coinflipTopLastDay() (Coinflip:1103-1112) reads coinflipTopByDay[flipsClaimableDay] — a SETTLED day (flipsClaimableDay set to the VRF-resolved epoch :915), while new stakes write coinflipTopByDay[_targetFlipDay()] = the NEXT window (Coinflip:672,679) — a disjoint future day. So a bettor cannot inject into the consumed slice after the BAF word is knowable. coinflipTopLastDay is external view. All membership/score inputs are settled past facts or lock-frozen pools.
- **Backing:** test/fuzz/BafFarFutureTickets.t.sol; test/unit/DegenerusJackpots.test.js; test/fuzz/BafRebuyReconciliation.t.sol

---

## Cluster: DecimatorModule — regular/terminal decimator winner-select + claim-time lootbox seed

_v66 codex-found #68 (regular) and #69 (terminal) decimator consumers exist at HEAD; #68 split into DEC-01 (winner-select) + DEC-02 (claim-time lootbox seed round.rngWord). MIDRNG-02 outside this cluster. _decSubbucketFor is a PURE keccak(player,lvl,bucket) placement (a frozen INPUT, not a VRF consumer). TEST-COVERAGE GAP: RngLockDeterminism.t.sol sec4/sec13 determinism fuzz are vm.skip(true) (the byte-identical-under-mid-lock property is NOT enforced for either decimator path) — recommend flipping to strict assertions. VERIFY found a doc GAP on DEC-03: the entry's invariant claims 'terminal burns blocked once gameOver/liveness reached', but recordTerminalDecBurn is gated only by daysRemaining<=7 + terminalDecWindow(), NOT _livenessTriggered; in the VRF-grace corner the actual protection is prevrandao unpredictability of the fallback word (LOW; correct the invariant text)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-DEC-01` | contracts/modules/DegenerusGameDecimatorModule.sol:245 (loop _decWinningSubbucket(decSeed,denom)) -> :501 (uint8(uint256(keccak256(abi.encodePacked(entropy,denom)))%denom)); decSeed=rngWord :242; packed decBucketOffsetPacked[lvl] :269 via _packDecWinningSubbucket :246/:510 | AdvanceModule.sol:1239 rngWord=_applyDailyRng(day,currentWord) writes rngWordByDay[day] BEFORE _consolidatePoolsAndRewardJackpots :525 threads it (param :832) to runDecimatorJackpot :955. Word request-committed earlier: _requestRng sets rngLockedFlag=true / rngWordCurrent=0 :1700-1702. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-DEC-02` | contracts/modules/DegenerusGameLootboxModule.sol:886 seed=EntropyLib.hash2(rngWord,uint256(uint160(player))) inside resolveLootboxDirect:874, via DecimatorModule _awardDecimatorLootbox :669-680 (rngWord=round.rngWord) <- _creditDecJackpotClaimCore :463 <- _claimDecimatorJackpotFor :407-412 | DecimatorModule.sol:277 round.rngWord=uint32(rngWord) — stored once during runDecimatorJackpot (same daily-VRF snapshot as DEC-01); idempotent per level (early-return :233-235 if round.poolWei!=0), so the 32-bit seed is write-once. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-DEC-03` | contracts/modules/DegenerusGameDecimatorModule.sol:996 (loop _decWinningSubbucket(decSeed,denom)) -> :501 keccak256(abi.encodePacked(entropy,denom))%denom; bucketKey=keccak256(abi.encode(lvl,denom,winningSub)) :1003 reads terminalDecBucketBurnTotal; packed decBucketOffsetPacked[lvl+1] :1024. decSeed=rngWord :994. | contracts/modules/DegenerusGameGameOverModule.sol:93 rngWord=rngWordByDay[day] — read inside handleGameOverDrain BEFORE runTerminalDecimatorJackpot :177. rngWordByDay[day] written upstream by AdvanceModule._gameOverEntropy/_applyDailyRng (gameover-day word or _getHistoricalRngFallback) BEFORE the drain delegatecall (:645/:700). gameOver=true latched GameOverModule:135 before :177. | CROSS-CONTRACT-SEAM | yes | LOW | yes |

#### `RNGF-DEC-01` — Regular decimator winning-subbucket selection (per-denom winner-select)
- **Invariant:** Winning subbucket for each denom 2..12 at lvl is keccak256(rngWord,denom)%denom where rngWord=rngWordByDay[day] determined at :1239 before runDecimatorJackpot is reachable. No actor input between the level-transition VRF request that closes the decimator window and the consumption changes which word (single-shot :1209-1210) or HOW the winner derives (denom fixed [2,12] loop constant; the only other input is the player-keyed subbucket placement frozen at burn).
- **Frozen inputs:** rngWord=rngWordByDay[day] written once :1239, immutable (rngGate :1209-1210); denom pure loop constant 2..12 :243; subbucket placement e.subBucket set only in recordDecBurn first-burn :160 / migration :167 via _decSubbucketFor=keccak256(player,lvl,bucket)%bucket :636 — VRF-independent; decBucketBurnTotal[lvl][denom][sub] accumulated only by recordDecBurn during the open window; window close decWindowOpen=false :1743 BEFORE the resolving VRF word; FLIP.decimatorBurn reverts NotDecimatorWindow :649; level keying alignment: window-open burns key decBurn[level+1] :652 == decBurn[lvl] consolidation arg :955
- **Verification recipe:** grep _decWinningSubbucket body :496-502 = uint8(uint256(keccak256(abi.encodePacked(entropy,denom)))%denom); call site :245 decSeed=rngWord :242; provenance runDecimatorJackpot :955 <- _consolidate :525 <- rngGate :380; rngWordByDay[day] write+short-circuit :1239/:1209-1210; window-close-before-word decWindowOpen=false :1743 + rngLockedFlag=true :1702; subbucket placement _decSubbucketFor :633-639 hashes (player,lvl,bucket) only; burn gate FLIP.sol:649 + keying level()+1 :652.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: grind the daily word via reverseFlip() nudges AFTER the raw word becomes knowable. Defeated: reverseFlip() reverts when rngLockedFlag (DegenerusGame.sol:1818); the daily/level-transition request sets rngLockedFlag=true :1702 BEFORE the VRF callback delivers rngWordCurrent; rawFulfillRandomWords :1844-1846 stores the daily word with rngLockedFlag STILL true, so no nudge can be added across request->callback->consumption; consumed at :1239. Second refutation: add a decimator burn into the winning subbucket after the word is knowable — defeated structurally: the same isTicketJackpotDay branch sets level=lvl :1733 and closes the window :1743 atomically with the lock, FLIP.decimatorBurn reverts NotDecimatorWindow :649 keyed level()+1 :652. Subbucket placement _decSubbucketFor :634-638 is pure keccak(player,lvl,bucket). All ledger anchors reproduce exactly.
- **Backing:** test/repro/DecimatorOffsetIsolation.t.sol; NOTE coverage gap: sec4/sec13 RngLockDeterminism.t.sol fuzz are vm.skip(true)

#### `RNGF-DEC-02` — Regular decimator claim-time lootbox seed (round.rngWord -> resolveLootboxDirect)
- **Invariant:** Claim-time lootbox outcome for a winning claimant = seed=hash2(round.rngWord, player), round.rngWord frozen at runDecimatorJackpot snapshot, player value-fixed. No claim-time input alters which seed or how it derives: claim is permissionless (all value to player :293-297), removing exclusive timing; the activity-score EV input is the FROZEN min-score of the winning bucket, not a live read.
- **Frozen inputs:** round.rngWord (uint32) written once :277; idempotent snapshot :233; 32-bit seed gates only the claim-time lootbox draw (winners already selected from the FULL word) combined via keccak with frozen inputs only; player winner address; (lvl,player) DecEntry written at burn; claim permissionless self-or-other :293; amount pro-rata = poolWei*entryBurn/totalBurn :559, all snapshotted :273-274; evScore _minScoreForBucket(e.bucket,lvl) :411,:689 sealed when the winning burn was bucketed; frozen, NOT a live activityScore; currentLevel=level+1 :880 deterministic monotonic
- **Live inputs (scale-not-select):** _applyEvMultiplierWithCap :890 live per-level EV budget but evScore is the FROZEN bucket-min-score (monotonic down-clamp against a frozen budget); scores against the outcome, cannot steer the seed; futurePrizePool recirc path reads live pool only for the 10% distribute cap, never as a seed input (:881-885)
- **Verification recipe:** grep round.rngWord store :277 (uint32) read :410; _creditDecJackpotClaimCore :463 -> _awardDecimatorLootbox :645 -> delegatecall resolveLootboxDirect.selector :669-680; seed=hash2(rngWord,uint256(uint160(player))) :886, activityScore only into _lootboxEvMultiplierFromScore :889; evScore frozen _minScoreForBucket :689; permissionless claim :293-297; idempotent snapshot :233-235.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: at permissionless claim time, steer the lootbox via a live read or by re-arming round.rngWord. Defeated: seed=EntropyLib.hash2(rngWord,uint256(uint160(player))) :886, rngWord=round.rngWord frozen once :277 (uint32 snapshot; runDecimatorJackpot idempotent via round.poolWei!=0 :233-235) and player fixed. The activityScore feeds ONLY _lootboxEvMultiplierFromScore :889 which scales AMOUNT via the FROZEN _minScoreForBucket(winBucket,lvl) :411,:689 — never the seed. The lone live read _applyEvMultiplierWithCap :890 is a monotonic down-clamp against a frozen per-level cap. Permissionless self-or-other claim :293-297 removes timing. All anchors reproduce.
- **Backing:** direct source argument + DecClaimRound.rngWord NatSpec DegenerusGameStorage.sol:1785-1791; NOTE sec13 DecimatorAwardLootbox determinism fuzz vm.skip

#### `RNGF-DEC-03` — Terminal decimator winning-subbucket selection (GAMEOVER death-bet winner-select)
- **Invariant:** Terminal winning subbucket for each denom 2..12 = keccak256(rngWord,denom)%denom where rngWord=rngWordByDay[day] for the gameover day, determined at GameOverModule:93 before runTerminalDecimatorJackpot. Resolution single-shot, GAME-gated; one-time boost (boostTerminalDecimator) gated by !_livenessTriggered() so its placement provably commits BEFORE the gameover word exists.
- **Frozen inputs:** rngWord=rngWordByDay[day] (GameOverModule:93) written before the drain delegatecall; single-shot (:1209-1210); denom pure loop constant 2..12 :995; terminalDecBucketBurnTotal accumulated only by recordTerminalDecBurn during the open window; daysRemaining<=7 reverts :790; FLIP terminal gate :724-725; e.subBucket placement _decSubbucketFor :823/:926 pure (player,lvl,bucket) keccak; boost timing: boostTerminalDecimator reverts if _livenessTriggered() :883 and requires daysRemaining==0 :884; lvl from storage level GameOverModule:76; decBucketOffsetPacked[lvl+1] target avoids aliasing a live regular round's [lvl] :1017-1024; gameOver latch true :135 before :177
- **Live inputs (scale-not-select):** boostTerminalDecimator recomputes the bucket from LIVE playerActivityScore :913 and promotes only if strictly LOWER bucket :923 — monotonic-improvement clamp gated by !_livenessTriggered, commits before the gameover word; scores against, not into, the seed; terminal CLAIM :1042 reads NO live score and has NO lootbox leg — pure ETH claimable :1045
- **Verification recipe:** grep function runTerminalDecimatorJackpot :978 decSeed=rngWord :994 loop :996 offset decBucketOffsetPacked[lvl+1] :1024; handleGameOverDrain rngWord=rngWordByDay[day] :93 runTerminalDecimatorJackpot :177 after gameOver=true :135; AdvanceModule _gameOverEntropy :645 + handleGameOverDrain.selector :700; burn gating FLIP.sol:724-725 + daysRemaining<=7 :789-790; boost pre-word freeze _livenessTriggered revert :883 daysRemaining!=0 :884 promote only if liveBucket<oldBucket :923; offset isolation :1017-1024; terminal claim pure _creditClaimable :1042-1047.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`LOW`. Recipe reproduces (decSeed=rngWord :994, loop :996, offset decBucketOffsetPacked[lvl+1] :1024; rngWord=rngWordByDay[day] :93 before :177 after gameOver=true :135; boost gated :883/:884; FLIP gate :724-725; offset isolation live-tested). GAP found: the ledger asserts 'terminal burns are blocked once gameOver/liveness is reached' as a frozenInput, but recordTerminalDecBurn (:782-790) is gated ONLY by daysRemaining<=7 (and terminalDecWindow()=!gameOver&&!lastPurchaseDay) — NOT by _livenessTriggered() nor rngLockedFlag (only boostTerminalDecimator carries the liveness gate). In the VRF-grace liveness path (_livenessTriggered true when rngRequestTime!=0 for >=14 days), liveness can fire while daysRemaining>7, and the gameover word is then committed via the historical-fallback branch _gameOverEntropy :1333-1368 while gameOver is still false and terminal burns remain OPEN. So the freeze in that corner does NOT rest on the claimed structural burn-block. The net freeze still HOLDS because the fallback word=keccak256(combined,currentDay,block.prevrandao) :1415 is unpredictable to a non-validator placing a burn in a prior block; the only steering is the documented/accepted 1-bit prevrandao validator bias over an 11-denom keccak selection — negligible. Severity LOW: the ledger's structural justification is inaccurate; the actual protection for the burn path in the VRF-dead corner is prevrandao unpredictability, which the entry's invariant/frozenInputs do not name. Recommend correcting the DEC-03 invariant text and flipping the skipped sec4/sec13 oracles.
- **Backing:** test/repro/DecimatorOffsetIsolation.t.sol (LIVE); sec4/sec13 RngLockDeterminism.t.sol:677/1083 vm.skip(true)

---

## Cluster: sDGNRS-Coinflip — redemption-lootbox / FLIP-escrow / submit-gate / coinflip result seams

_All 7 v66 cluster consumers (#52-#58) exist at HEAD; line numbers stable. Added RNGF-SEAM-RESOLVE (the redemption-roll derivation feeding #52's amount). MIDRNG-02 mid-day path is NOT in this cluster (redemption reads the DAILY word rngWordForDay(day+1), never lootboxRngWordByIndex). V-184 cross-day re-roll structurally removed (redemptionPeriods day-keyed single-write; redemptionPeriodIndex/pendingRedemptionEthBase grep-empty). ONE FLAGGED: RNGF-SEAM-RESOLVE freezeHolds=false on the gameover prevrandao-fallback twin only (1-bit validator MAGNITUDE nudge on a gameover-terminal self-claim roll after a forced VRF stall; normal + gameover-normal paths fully frozen) — bounded LOW, the v66-disclosed NEEDS-PROOF residual._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-SEAM-52` | contracts/sDGNRS.sol:916-920 (rngWord=game.rngWordForDay(day+1) :916; entropy=EntropyLib.hash2(rngWord,uint160(player)) :917; game.resolveRedemptionLootbox :920) -> consumed LootboxModule._resolveRedemptionChunk:973 (seed=keccak256(rngWord,player,amount)) -> _rollTargetLevel:974 | AdvanceModule.sol:_applyDailyRng:1929-1930 — rngWordByDay[day+1] written (resolving day's word) on the advance resolving `day`; until that SSTORE day+1 word is 0. (player,day) claim slot frozen at sDGNRS:_submitGamblingClaimFrom:1129-1142. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-SEAM-RESOLVE` | contracts/modules/DegenerusGameAdvanceModule.sol:rngGate:1261-1264 (redemptionRoll=uint16(((currentWord>>8)%151)+25); sdgnrs.resolveRedemptionPeriod(redemptionRoll,toResolve)) — gameover normal :1320-1326, gameover prevrandao-fallback :1356-1361. Applied as the percentage sDGNRS._claimRedemptionFor:842 after store by resolveRedemptionPeriod:759. | AdvanceModule.sol:_applyDailyRng:1929-1930 — currentWord=rngWordByDay[day] set immediately before the roll is derived from its bits [8..]. Pool named by sdgnrs.pendingResolveDay() :1259 stamped at sDGNRS:_submitGamblingClaimFrom:1043. | CROSS-CONTRACT-SEAM | **NO** | LOW | yes |
| `RNGF-SEAM-53` | contracts/sDGNRS.sol:_claimRedemptionFor:888-894 (read (rewardPercent,flipWon)=coinflip.getCoinflipDayResult(day+1) :888; if flipWon: flipPaid=principal+principal*rewardPercent/100 :891-892; coinflip.creditFlip(player,flipPaid) :893). principal=claim.flipEscrow*1e18 :891. | contracts/Coinflip.sol:processCoinflipPayouts:886-889 — win bit (rngWord&1) and rewardPercent derived from the day+1 VRF word and stored via _storeDayResult:1206 into coinflipDayResultPacked on the advance resolving day+1. Escrow magnitude claim.flipEscrow frozen at sDGNRS:_submitGamblingClaimFrom:1135-1136 (removed via coinflip.withdrawRedeemedFlip :1124). | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-SEAM-GATE-54` | contracts/sDGNRS.sol:_submitGamblingClaimFrom:1036 (if (game.rngWordForDay(currentPeriod)==0) revert BurnsBlockedBeforeDailyRng) — presence-only. Plus burn:639/burnWrapped:663 (if (game.rngLocked()) revert BurnsBlockedDuringRng) and the single-pool sentinel :1041-1043 (PriorDayUnresolved). | Not a value-consumption site: the FREEZE-ENFORCING gate establishing the commitment-ordering invariant for SEAM-52/53. Pins the burn stamp to a drawn day (currentPeriod==dailyIdx) so rngWordForDay(day+1) is NOT-yet-drawn at submit. currentPeriod=GameTimeLib.currentDayIndex() :1028, pure block.timestamp. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-SEAM-55` | contracts/Coinflip.sol:processCoinflipPayouts:859-889 (seedWord=keccak256(rngWord,epoch) :859; roll=seedWord%20 :864; rewardPercent :866-883; win=(rngWord&1)==1 :886; _storeDayResult(epoch,...) :889). Bounty credit :902-909. | P = the call itself: rngWord arrives as a parameter from the Game advanceGame/VRF-callback stack (rngGate:1240 / _gameOverEntropy:1312,1347 / _backfillGapDays:1883), = the post-_applyDailyRng word for `epoch` (fixed AdvanceModule:1930). epoch is the resolved day index. Result stored _storeDayResult:1206, never rewritten for a past day. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-SEAM-56` | contracts/Coinflip.sol:_processClaimDays:522 (read (rewardPercent,win)=_dayResult(cursor)); payout/carry :541-578; winningBafCredit :551-552. Mirrored carry/auto-rebuy read :1140. | P = processCoinflipPayouts._storeDayResult:1206 for each cursor day (past resolved day). Per-day stake _flipStake(cursor,player) written at deposit on a FUTURE day (_targetFlipDay=currentDayIndex()+1, Coinflip:1252-1253) before that day's word existed. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-SEAM-57` | contracts/Coinflip.sol:getCoinflipDayResult:367-369 -> _dayResult:1193-1197 (b=uint8(coinflipDayResultPacked[day>>5]>>((day&31)*8)); rewardPercent=b; win=b>=50). External caller: sDGNRS:_claimRedemptionFor:888 (RNGF-SEAM-53). | P = processCoinflipPayouts._storeDayResult:1206 for the queried day (written once when resolved). The view re-derives nothing — re-reads the already-frozen stored byte. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-SEAM-58` | contracts/Coinflip.sol:coinflipTopLastDay:1103-1112 (reads coinflipTopByDay[lastDay] :1110). Written by _updateTopDayBettor:1284-1295. Consumed VRF-side externally by DegenerusJackpots.runBafJackpot (coinflip-leaderboard slice, v66 #51). | P (membership): _updateTopDayBettor:1292 SSTORE of coinflipTopByDay[day] — set at deposit time on the target day (a deposit lands the stake at _targetFlipDay=currentDayIndex()+1 and updates that day's leader). The day cursor consumed (flipsClaimableDay = the VRF-resolved epoch, set processCoinflipPayouts:915) advances only on resolution. The VRF winner-select consuming this membership is in DegenerusJackpots.runBafJackpot. | CROSS-CONTRACT-SEAM | yes | n/a | yes |

#### `RNGF-SEAM-52` — Redemption-lootbox seed (hash2 over rngWordForDay(day+1)) -> resolveRedemptionLootbox
- **Invariant:** Lootbox draw for claim (player,day) derived from W=rngWordByDay[day+1] fully determined at P. Between P and C no actor input changes WHICH word (consumed day index fixed = day+1, day stamped at submit not re-selectable at claim) nor HOW (preimage (W,player,amount); amount=totalRolledEth/2 from ethValueOwed frozen at submit * roll frozen at resolve). W UNDRAWN at submit (BurnsBlockedBeforeDailyRng pins submit to currentPeriod==dailyIdx so day+1 is the NEXT not-yet-drawn day).
- **Frozen inputs:** rngWord=game.rngWordForDay(day+1) determined at P :1930, single-shot/immutable (:1209-1210, AdvanceModule:644); player composite mapping key pendingRedemptions[player][day] :1129; fixed at submit; day+1 resolving-day selector: claim key day = wall-clock submit day :1028 pinned to dailyIdx by BurnsBlockedBeforeDailyRng :1036; amount=lootboxEth: totalRolledEth=ethValueOwed (frozen at submit :1134) * roll (frozen at resolve) /100 :842,851-852; activityScore claim.activityScore-1 snapshot :1140-1141, read :911
- **Live inputs (scale-not-select):** currentLevel=level+1 in _resolveRedemptionChunk :969 feeds _rollTargetLevel and the EV cap NOT the seed; level monotone-up, can't be steered to bias the seed; comment :970-972 no live day in the seed; _applyEvMultiplierWithCap :977 live EV-budget down-clamp on a frozen budget; bounds payout, does not alter selection
- **Verification recipe:** grep rngWordForDay(day+1) sDGNRS :916; seed LootboxModule:973 keccak256(abi.encode(rngWord,player,amount)) NO block.timestamp/currentDay/level in preimage; rngWordByDay[day]=finalWord :1930 single-shot short-circuit :1209-1210; only _applyDailyRng / _backfillGapDays:1880 / _getHistoricalRngFallback land a new value, each gated; claim's day+1 selector not re-tunable (mapping key day immutable post-submit).
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: observe rngWordForDay(day+1) after it is drawn (set during the resolving advance :1239/:1880), then steer the seed. Defeated: seed at LootboxModule:973 = keccak256(abi.encode(rngWord,player,amount)) with NO live-day/level/timestamp term (level+1 :969 feeds only _rollTargetLevel and the EV cap, comment :970-972). Every preimage component frozen before the word is knowable: rngWord=rngWordForDay(day+1) undrawn at submit (gate :1036 requires rngWordForDay(currentPeriod)!=0 while day+1 is the NEXT day); player is the immutable composite mapping key (sole write :1129, delete :871, no re-write); amount=lootboxEth=totalRolledEth/2 with ethValueOwed frozen at submit :1134 and roll frozen at resolve. day is the wall-clock submit day (currentDayIndex(), timestamp-pure) not re-selectable at claim. resolveRedemptionLootbox onlyifSDGNRS (LootboxModule:930). testFuzz_ClaimReadsCorrectDay PASS 10000.
- **Backing:** test/fuzz/StakedStonkRedemption.t.sol; test/fuzz/RedemptionEdgeCases.t.sol

#### `RNGF-SEAM-RESOLVE` — Redemption-roll derivation ((currentWord>>8)%151+25) -> resolveRedemptionPeriod write
- **Invariant:** Roll applied to the resolving pool is roll=((W>>8)%151)+25 in [25,175], W=the day's word fixed at P. Between P and apply C no actor input changes the roll: word bits frozen at P, modulo/offset fixed, pool selector toResolve is the single-pool sentinel (set once per burn-day at submit, cleared on resolve :767) — cannot select WHICH pool a word resolves nor re-resolve (redemptionPeriods[day] keyed by day, never overwritten; V-184 cross-day re-roll structurally removed).
- **Frozen inputs:** currentWord the day's VRF word frozen at P :1930; toResolve=sdgnrs.pendingResolveDay() :1259 single-pool sentinel stamped at submit :1043, at-most-one unresolved day; roll formula (>>8,%151,+25) fixed pure arithmetic; pool.ethBase accumulated only by submit-time increments :1106 which are rngLocked- and BurnsBlockedBeforeDailyRng-gated; future-day re-arm blocked by PriorDayUnresolved :1042
- **Verification recipe:** grep % 151) + 25 AdvanceModule three sites :1262, :1320(area), :1356 all derive from currentWord/fallbackWord and pass sdgnrs.pendingResolveDay(); redemptionPeriods[dayToResolve]=roll sDGNRS:759 plain uint16 keyed by day :301, NO redemptionPeriodIndex/pendingRedemptionEthBase (grep zero); resolveRedemptionPeriod onlyGAME :743 clears sentinel only when it matches :767; roll in [25,175] (Halmos); a resolved day can't be re-resolved (pendingByDay deleted :764).
- **Re-verification verdict:** freezeHolds=`false`, recipeReproducible=`true`, severityIfBroken=`LOW`. REFUTATION (the entry's own gameover-fallback twin): the frozenInputs claims 'currentWord frozen at P', but the third roll site (_gameOverEntropy prevrandao-fallback :1358-1359) derives redemptionRoll=((fallbackWord>>8)%151)+25 where fallbackWord=keccak256(combined,currentDay,block.prevrandao) (_getHistoricalRngFallback, called :1337). block.prevrandao is validator-controllable (~1 bit) on a reachable path: gameover + 3-day VRF stall (elapsed>=GAMEOVER_RNG_FALLBACK_DELAY :1335) leaves a sentinel-stamped redemption pool, and that proposer biases the [25,175] roll MAGNITUDE that a 100%-direct gameover self-claim (sDGNRS:848-849 isGameOver -> ethDirect=totalRolledEth) pays to themselves. So an actor-controllable input DOES change the roll's derivation on this path. BOUNDED (not catastrophe): a magnitude nudge, not an index/word SELECTION; roll stays in [25,175]; gameover-terminal-only (no lootbox leg, no live-game holders); reachable only after a forced VRF stall. The normal path (:1261-1264) and gameover-normal path (:1320-1326) ARE fully frozen and hold. V-184 cross-day re-roll structurally removed (redemptionPeriods day-keyed single-write :759; redemptionPeriodIndex/pendingRedemptionEthBase grep-empty; testFuzz_EDGE_07 PASS). This is the v66-flagged NEEDS-PROOF residual the cluster notes disclose. Roll bounds [25,175] sound by source arithmetic (Halmos symbolic run timed out at 240s but property trivially closed).
- **Backing:** test/halmos/RedemptionRoll.t.sol; test/fuzz/RedemptionEdgeCases.t.sol:testFuzz_EDGE_07_V184AttackReproductionStructuralClosure

#### `RNGF-SEAM-53` — FLIP-escrow contingent leg (getCoinflipDayResult(day+1) win/rewardPercent)
- **Invariant:** Contingent FLIP payout reads the ABSOLUTE day+1 coinflip result byte R=coinflipDayResultPacked lane (3-state: 0 unresolved / 1 loss / 50..156 win-reward%). R determined at P. Between P and C no claim-time input changes R or the payout: consumed day index fixed (day+1, day immutable mapping key), win/rewardPercent read straight from the stored byte (_dayResult pure), claim.flipEscrow frozen at submit, creditFlip makes no callback into sDGNRS (CEI: slot deleted at :871 before the call). IDENTICAL day+1 coinflip every non-redeeming holder's backing rides — no exclusive timing edge.
- **Frozen inputs:** (rewardPercent,win) day+1 result byte frozen at P :1206 inside processCoinflipPayouts:889; never rewritten for a past day (epoch monotone, flipsClaimableDay=epoch :915); claim.flipEscrow whole-token slice removed from sDGNRS backing at submit :1124, packed :1136; fixed at submit; player composite mapping key :1129; day+1 selector resolving day deterministic from immutable submit-day key
- **Verification recipe:** grep getCoinflipDayResult(day+1) sDGNRS :888 inside if(!isGameOver && claim.flipEscrow!=0); Coinflip._dayResult:1193-1196 pure SLOAD+shift+mask (win=b>=50, rewardPercent=b) NO word read at view time; win=(rngWord&1)==1 Coinflip:886; processCoinflipPayouts onlyDegenerusGameContract :857 modifier :218-221; result byte written once per epoch=day+1 (advance-driven), immutable.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-entrancy via creditFlip to mutate the claim slot/escrow after reading the day+1 result, or grind flipEscrow after the result is knowable. Defeated: sDGNRS deletes the (player,day) slot at :871 BEFORE getCoinflipDayResult(day+1) :888 and creditFlip :893 (CEI), and creditFlip (Coinflip:970-976) only calls _addDailyFlip — no callback into sDGNRS. The consumed result byte is read by the pure _dayResult (Coinflip:1193-1196, SLOAD+shift+mask, win=b>=50, no word/keccak/block); written exactly once at _storeDayResult :1206 under onlyDegenerusGameContract, never rewritten (epoch monotone). claim.flipEscrow frozen at submit :1135-1136; player immutable; day+1 deterministic. Unresolved day+1 reads b==0 -> flipWon=false -> pays nothing (safe default). Recipe reproduces.
- **Backing:** test/fuzz/StakedStonkRedemption.t.sol; test/fuzz/FlipEmissionSeeds.t.sol; test/fuzz/CoinflipWinClassificationFloor.t.sol

#### `RNGF-SEAM-GATE-54` — Submit-window VRF-presence gate (rngWordForDay(currentPeriod)==0 reverts BurnsBlockedBeforeDailyRng) + rngLocked burn gate + PriorDayUnresolved sentinel
- **Invariant:** No VRF-derived VALUE consumed at :1036 — only the PRESENCE (drawn vs not) of the current day's word gates admission. Admits gambling burns only after today's word exists, so the pool resolves on the NEXT day's draw (day+1) and the lootbox/FLIP legs read a word that did not exist when the player committed. currentPeriod timestamp-pure (not player-selectable beyond waiting for wall-clock, which cannot reveal a future word). With rngLocked (blocks request->fulfilment) and PriorDayUnresolved (one unresolved pool), the admission window is the post-draw/pre-next-request slice.
- **Frozen inputs:** currentPeriod=GameTimeLib.currentDayIndex() :1028 pure timestamp function; gate reads ONLY non-zeroness of rngWordForDay(currentPeriod) — value never enters any derived outcome; _pendingResolveDay sentinel :240 stamped :1043 cleared :767; enforces at-most-one unresolved pool :1042
- **Live inputs (scale-not-select):** game.rngWordForDay(currentPeriod) presence: live read of whether the current day is drawn — gates admission, does not feed an outcome; the value-relevant word is always day+1, undrawn at this moment
- **Verification recipe:** grep BurnsBlockedBeforeDailyRng error :126 revert :1036 read `== 0` presence only; currentPeriod :1028 GameTimeLib.currentDayIndex(); rngLocked() burn:639/burnWrapped:663; PriorDayUnresolved :1042; every gambling-burn entry passes :1036 and :1041-1043.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: feed a VRF VALUE through the admission gate, or bypass it to land a burn in a day whose day+1 word is already drawn. Defeated: :1036 reads ONLY game.rngWordForDay(currentPeriod)==0 (presence, not value). currentPeriod=GameTimeLib.currentDayIndex() :1028 pure block.timestamp; advancing wall-clock cannot reveal a future word. The rngLocked gate (burn:639/burnWrapped:663) blocks the request->fulfilment window; PriorDayUnresolved :1041-1043 enforces at-most-one unresolved pool, so a burn lands only in a drawn current day with day+1 still undrawn. Every gambling-burn entry passes :1036 and :1041-1043. Oracle test_GamblingBurnRevertsBeforeDailyRng PASS (reinjecting pre-gate code flips it to FAIL); testFuzz_EDGE_11 (rngLocked) and EDGE_01 PASS 10000.
- **Backing:** test/fuzz/StakedStonkRedemption.t.sol:240-256 test_GamblingBurnRevertsBeforeDailyRng; test/fuzz/RedemptionEdgeCases.t.sol EDGE_11/EDGE_01/EDGE_05

#### `RNGF-SEAM-55` — Coinflip per-day result derivation (win bit + reward-percent roll) in processCoinflipPayouts
- **Invariant:** For day=epoch the result byte (win,rewardPercent) is a deterministic pure function of (rngWord,epoch,bonus): seedWord=keccak(rngWord,epoch), roll=seedWord%20, rewardPercent from fixed branches + bonus, win=rngWord&1. rngWord the VRF word committed at the producing _applyDailyRng SSTORE; epoch advance-controlled; bonus caller-precomputed from FROZEN protocol state (level/first-jackpot-day flags), NOT player-flippable. onlyDegenerusGameContract so neither rngWord nor epoch is attacker-supplied. bountyOwedTo latched earlier under !rngLocked :686.
- **Frozen inputs:** rngWord VRF word forwarded from the advance stack (= rngWordByDay[epoch] :1240/1930); not player-supplied :857; epoch resolved day index advance-controlled; bonus precomputed from frozen protocol state (level 0 / first-jackpot-day / x0-level) :877-880; not player-flippable; bountyOwedTo latched at :703 only when !game.rngLocked() :686; frozen for this resolution
- **Verification recipe:** grep win=(rngWord&1)==1 Coinflip:886; seedWord=keccak256(abi.encodePacked(rngWord,epoch)) :859; onlyDegenerusGameContract :857 checks msg.sender==GAME :218-220; !game.rngLocked() bounty latch :686; no block.prevrandao/timestamp/player-storage in win or rewardPercent.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: supply attacker-chosen rngWord/epoch/bonus, or flip the win bit / steal the bounty after the word is in flight. Defeated: processCoinflipPayouts onlyDegenerusGameContract (:857, modifier :218-220) so rngWord/epoch/bonus arrive from the advanceGame stack (rngWord=post-_applyDailyRng word for epoch :1240/1930), never player-supplied. win=(rngWord&1)==1 :886 and rewardPercent from seedWord=keccak(rngWord,epoch) via fixed branches + frozen bonus :859-883 — no block.prevrandao/timestamp/player-storage. bonus caller-precomputed from frozen protocol state :877-880. bountyOwedTo latched only when !game.rngLocked() (deposit path :686), read/cleared deterministically :895-911. Result byte stored once per epoch :1206. Win floor: wins span [50,156], never colliding with loss sentinel 1 — test_RewardFloorsNeverEnterMisreadBand PASS. Recipe reproduces.
- **Backing:** test/fuzz/CoinflipRngSpineBehavioral.t.sol; test/fuzz/CoinflipWinClassificationFloor.t.sol; test/fuzz/FlipEmissionSeeds.t.sol

#### `RNGF-SEAM-56` — Win/loss claim consumer (_dayResult per cursor day) in _processClaimDays
- **Invariant:** For each resolved day in the claim window the consumed result byte = coinflipDayResultPacked lane frozen at P. The player's stake committed at deposit (a future day, before the word). No claim-time input steers win/rewardPercent: cursor walks resolved days monotonically, player is the claimant (own stake), payout=stake+stake*rewardPercent/100 reads only frozen state. Unresolved days (byte==0) skipped. Stake freshly SLOADed per day, cleared on read :538.
- **Frozen inputs:** (rewardPercent,win)=_dayResult(cursor) frozen at P for each past resolved day; pure SLOAD+mask :1193-1196; _flipStake(cursor,player) written at deposit on _targetFlipDay=currentDayIndex()+1 :1252, a future day BEFORE that day's word; frozen for the resolved day; cursor monotone day walk over already-resolved days; player the claimant
- **Live inputs (scale-not-select):** carry (auto-rebuy rolling stake) added only when rebuyActive :532-534; accumulated prior-day winnings, itself frozen per-day-result-derived, not claim-time-selectable; bafResolvedDay=jackpots.getLastBafResolvedDay() :548 monotone protocol cursor gating winningBafCredit attribution; does not alter win/rewardPercent
- **Verification recipe:** grep _dayResult(cursor) :522 and :1140 both read the stored byte via pure _dayResult; _targetFlipDay :1252-1253 returns currentDayIndex()+1 (stake placed one day AHEAD); loop skips byte==0 :525 and clears stake :538; payout :544-546 reads only stake + frozen rewardPercent.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: place or grow a stake for a day whose word is already revealed, or re-derive the day result inside the claim loop. Defeated: stake committed at deposit on _targetFlipDay()=currentDayIndex()+1 :672,1252-1253 — a future day before its word exists; _processClaimDays :521-586 reads (rewardPercent,win)=_dayResult(cursor) (frozen byte, pure SLOAD+mask) for already-resolved cursor days only, skips unresolved b==0 :525, freshly SLOADs stake per day :530, clears it on read :538. Payout :544-546 = stake + stake*rewardPercent/100 reads only frozen state; no msg.sender-tunable term re-derives the day's result. carry is accumulated prior-day winnings (frozen-result-derived), bafResolvedDay a monotone cursor gating attribution only :548-552 — neither alters win/rewardPercent. Result byte immutable post-_storeDayResult :1206. Recipe reproduces.
- **Backing:** test/fuzz/CoinflipCarryClaim.t.sol; test/gas/CoinflipDeepClaimWorstCaseGas.t.sol; test/fuzz/FlipEmissionSeeds.t.sol

#### `RNGF-SEAM-57` — getCoinflipDayResult external view (cross-contract day-result accessor)
- **Invariant:** Returns the stored 3-state byte for the caller-supplied `day`, which only indexes an already-resolved frozen result (or 0). No VRF value re-derived live; the day argument is caller-supplied but selects among immutable past results — cannot change WHICH word produced a given day's byte (each day's byte written once per epoch) nor HOW it is decoded (fixed mask/shift, win=b>=50).
- **Frozen inputs:** coinflipDayResultPacked[day>>5] lane frozen at P :1206 per day; never overwritten for a past day (epoch monotone); day argument caller-supplied index into frozen results; in SEAM-53 it is the immutable claim key day+1
- **Verification recipe:** grep function getCoinflipDayResult :367 delegates to _dayResult; _dayResult :1193-1196 SLOAD+shift+mask+(b>=50) — no word read, no keccak, no block context; view, derives no new randomness; only write to its store is the single-shot _storeDayResult at P.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: pass a day argument that re-derives randomness or decodes a different word, or read a result that can still change. Defeated: getCoinflipDayResult :367-369 is view and delegates to _dayResult :1193-1197, which performs only SLOAD + shift + mask + (b>=50) — no word read, no keccak, no block context. The caller-supplied day only indexes immutable past results in coinflipDayResultPacked, whose sole write is the single-shot _storeDayResult at P :1206 per epoch; it cannot change WHICH word produced a given day's byte nor HOW it is decoded. In SEAM-53 the day is the immutable claim key day+1. Recipe reproduces; exercised by testFuzz_ClaimReadsCorrectDay PASS 10000.
- **Backing:** test/fuzz/StakedStonkRedemption.t.sol; test/fuzz/FlipEmissionSeeds.t.sol

#### `RNGF-SEAM-58` — Coinflip leaderboard read (coinflipTopByDay via coinflipTopLastDay) — membership preimage to external BAF winner-select
- **Invariant:** coinflipTopByDay[day] is NOT itself a VRF-derived value — leaderboard MEMBERSHIP. The leaderboard for a resolved day is fully determined by deposits placed for that day (each at currentDayIndex()+1, i.e. before that day's BAF word), and the read keys off flipsClaimableDay (the VRF-resolved epoch). A player can steer WHO is top by out-staking, but only on a FUTURE day's leaderboard before that day's word is drawn; once the day is the resolving epoch membership is frozen. The downstream VRF winner-select samples this frozen membership against the day's word.
- **Frozen inputs:** coinflipTopByDay[lastDay] written at deposit-time :1292 for that day; deposits target currentDayIndex()+1 :1252 so a resolved day's leaderboard sealed before that day's word existed; lastDay=flipsClaimableDay (:1110 area) VRF-resolved epoch advanced only by processCoinflipPayouts:915; score=_score96(stake) whole-token stake snapshot at deposit
- **Live inputs (scale-not-select):** leaderboard membership player-controllable (out-stake to become top.player) — but ONLY for a future, not-yet-resolved day; cannot be changed for a day whose word is being consumed
- **Verification recipe:** grep coinflipTopByDay read :1110 (coinflipTopLastDay) write :1292 (_updateTopDayBettor); _updateTopDayBettor called from the deposit path with targetDay :679 = _targetFlipDay (currentDayIndex()+1); coinflipTopLastDay keys flipsClaimableDay set :915.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: out-stake to overwrite coinflipTopByDay for a day whose BAF word is being consumed, biasing the downstream winner-select against a revealed word. Defeated: all writes to coinflipTopByDay[d] occur in _updateTopDayBettor :1290-1293 called only from the deposit/credit path _addDailyFlip with targetDay=_targetFlipDay()=currentDayIndex()+1 :672,679,1252. The read keys off flipsClaimableDay (resolved epoch :1108-1110, advanced only by processCoinflipPayouts:915). Since the advance processes day d<=currentDayIndex(), flipsClaimableDay<currentDayIndex()+1=targetDay, so every leaderboard write lands strictly AHEAD of the consumed epoch — membership for a consumed day was sealed before that day's word existed. Even credit paths (creditFlip/creditFlipBatch via _addDailyFlip) still target the future day. The player-controllable input (out-staking) is committed pre-word. Recipe reproduces.
- **Backing:** test/edge/BafCreditRouting.test.js; deposit-day-ahead property structural (Coinflip:1252)

---

## Cluster: Jackpots-BAF — runBafJackpot slot/scatter/far-future winner-select + fire-gate

_All v66 BAF-cluster consumers exist at HEAD and were re-anchored; nothing removed by MIDRNG-02 (a different cluster). The load-bearing freeze is the resolving-bracket recordBafFlip RngLocked guard (Coinflip:609-616) + the far-future ticketQueue RngLocked push guard (Storage:621/653/715) + traitBurnTicket written only by the advance-stack _raritySymbolBatch — all spanning rngLockedFlag-set@:1702 through _unlockRng:1805. RNGF-BAF-08 (coinflip top-bettor slice) consumes NO VRF value (deterministic resolved-window leaderboard; membership a pre-resolution stake commitment). No MUTABLE-INPUT remains in this cluster. VERIFY: BAF-07 recipe cites a wrong single-shot line anchor (:644 game-over path vs the actual rngGate :1209-1210) — recipeReproducible=false, but the freeze invariant holds (reverseFlip gated by rngLockedFlag across fulfillment; nudge frozen at P)._

| id | consumptionSite | commitmentPoint (P) | freezeClass | freezeHolds | sev | recipeRepro |
|---|---|---|---|---|---|---|
| `RNGF-BAF-01` | contracts/DegenerusJackpots.sol:300 (pick=2+uint8(entropy&1); _bafTop(lvl,pick) :301; credited :303) | AdvanceModule.sol:1702 (rngLockedFlag=true at the daily VRF request, rngWordCurrent==0). Consumed word = bracket word at rawFulfillRandomWords->_applyDailyRng write of rngWordByDay[day]; entropy=EntropyLib.hash2(hash2(rngWord,1)) chained DegenerusJackpots:298 from the rngWord param threaded from AdvanceModule._consolidatePoolsAndRewardJackpots:935. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-02` | contracts/DegenerusJackpots.sol:318 (degenerusGame.sampleFarFutureTickets(entropy)); best/second by _bafScore :331-340; credited :344,:349 — loop pass 0/1 :315 | AdvanceModule.sol:1702 (rngLockedFlag=true at daily VRF request) for entropy; Storage:621 (if isFarFuture && rngLockedFlag && !rngBypass revert RngLocked) commits far-future ticketQueue membership at lock engage — no far-future push lands after P. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-03` | contracts/DegenerusJackpots.sol:382-386 (targetLvl=lvl +- entropy%{3,4,99}); :389 (degenerusGame.sampleTraitTicketsAtLevel(targetLvl,entropy)); best/second by _bafScore :402-411; bucketed :418-429 | AdvanceModule.sol:1702 (rngLockedFlag=true at daily VRF request); per-round entropy re-chained :374 (++salt; entropy=hash2(entropy,salt)). traitBurnTicket[targetLvl] bucket membership committed by the advance-stack ticket processing (MintModule._raritySymbolBatch:794-813) for past/current levels BEFORE the BAF word; new burns route to future buckets, advance-gated. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-04` | contracts/DegenerusGame.sol:2318 (traitSel=uint8(entropy>>24)); :2327 (start=(entropy>>40)%len); :2329 (tickets[i]=arr[(start+i)%len]) — entry :2314 | AdvanceModule.sol:1702 (rngLockedFlag=true); entropy param the same frozen keccak-chained daily word from DegenerusJackpots.runBafJackpot:389; sampled bucket traitBurnTicket[targetLvl][traitSel] committed by _raritySymbolBatch (MintModule:794-813) for resolved levels before the word. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-05` | contracts/DegenerusGame.sol:2350 (word=keccak(word,s)); :2351 (candidate=currentLvl+5+word%95); :2356 (idx=(word>>32)%len); :2357 (winner=queue[idx]) — entry :2341 | AdvanceModule.sol:1702 (rngLockedFlag=true) for entropy; Storage:621/:653 commits far-future ticketQueue membership when the lock engages; sellFarFutureTickets reverts under lock MintModule:1247. | CROSS-CONTRACT-SEAM | yes | n/a | yes |
| `RNGF-BAF-06` | contracts/modules/DegenerusGameJackpotModule.sol:2083 (entropy=EntropyLib.hash2(entropy,entropy)); :2087 (roll=entropy-entropyDiv100*100, path/level select); :2115 (uint16(entropy>>200)%TICKET_SCALE<frac, Bernoulli round-up) — from runBafJackpot:1911->_awardJackpotTickets:2010 | AdvanceModule.sol:1702 (rngLockedFlag=true). Recirc seed = the same rngWord param threaded into Game.runBafJackpot AdvanceModule:935; the winner SET/amounts come back from DegenerusJackpots.runBafJackpot (frozen samples above); recirc roll re-derives entropy off that frozen word via hash2. | FROZEN-AT-COMMIT | yes | n/a | yes |
| `RNGF-BAF-07` | contracts/modules/DegenerusGameAdvanceModule.sol:928 ((rngWord&1)==1 -> fire BAF); :932-936 (runBafJackpot(bafPoolWei,lvl,rngWord)); :940 (markBafSkipped on losing flip) — inside _consolidatePoolsAndRewardJackpots:828 | AdvanceModule.sol:1702 (rngLockedFlag=true at daily VRF request, rngWordCurrent==0). rngWord = rngGate return = rngWordByDay[day] (threaded rngGate :380 -> _consolidate :529); written single-shot by _applyDailyRng (rngGate short-circuit :644). | FROZEN-AT-COMMIT | yes | INFO | **no** |
| `RNGF-BAF-08` | contracts/DegenerusJackpots.sol:284 (coin.coinflipTopLastDay()); credited :285 — consumes a deterministic leaderboard view | contracts/Coinflip.sol:915 (flipsClaimableDay=epoch in processCoinflipPayouts) and :1292 (_updateTopDayBettor writes coinflipTopByDay[day]); BAF resolution reads the resolved window via coinflipTopLastDay:1108-1110. | CROSS-CONTRACT-SEAM | yes | n/a | yes |

#### `RNGF-BAF-01` — runBafJackpot Slice B — 3rd/4th BAF leaderboard slot pick (pseudo-random tie-break)
- **Invariant:** The 1 bit consumed by `entropy&1` is a full-diffusion keccak output of rngWordByDay[day], determined at fulfillment and immutable. Candidate set bafTop[lvl] (PlayerScore[4]); currentEpoch read once :266. Between P (lock set :1702) and C (:300), recordBafFlip on the resolving bracket reverts (Coinflip:609-616), so no actor can move a player onto/within bafTop[lvl]; lvl and pick computed from the frozen word.
- **Frozen inputs:** rngWord=rngWordByDay[day] written by rawFulfillRandomWords/_applyDailyRng AFTER the lock :1702; single-shot (:644); entropy (&1 bit) hash2(hash2(rngWord,1)) :298 pure keccak; no actor input; bafTop[lvl] board: resolving-bracket recordBafFlip RngLocked-blocked (Coinflip:609-616); VAULT/sDGNRS off the board; currentEpoch read once :266; bumped only at end of this resolution :480; lvl passed by AdvanceModule; pick (2 or 3) from the frozen entropy bit
- **Verification recipe:** grep pick=2+uint8(entropy&1) :300, entropy reassigned :298 via EntropyLib.hash2; entropy=rngWord :262 from onlyGame param; rngLockedFlag=true :1702 preceded by rngWordCurrent=0 :1700; recordBafFlip Coinflip:624 read :609-616 lastPurchaseDay_&&rngLocked_&&cachedLevel%10==0->revert RngLocked; onlyGame runBafJackpot :252.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: move a player onto/within bafTop[lvl] for the resolving bracket between P and C so the entropy&1 tie-break picks a fresh entry. Defeated: the only writer of bafTop[lvl] is recordBafFlip (:191/203), called from exactly one guarded site (Coinflip:624). During BAF resolution inJackpot=false, lastPurchaseDay=true, rngLocked=true (the _consolidate call AdvanceModule:525 is on the !inJackpot last-purchase path; rngLockedFlag :1702 NOT cleared before consumption — _unlockRng only after jackpot processing). For lvl%10==0 cachedLevel=lvl so guard Coinflip:609-616 fires. recordBafFlip onlyCoin :163; runBafJackpot onlyGame :170 via advance self-delegatecall :1916. entropy keccak of the frozen word. Recipe reproduces.
- **Backing:** test/fuzz/TicketRouting.t.sol RNG-02; recordBafFlip resolving-bracket guard

#### `RNGF-BAF-02` — runBafJackpot Slices D/D2 — two far-future ticketQueue draws ranked by BAF score
- **Invariant:** entropy at each pass = EntropyLib.hash2(prev,++salt) :316-317, keccak chain off the frozen daily word; salt sequence deterministic. Candidate pool from ticketQueue[_tqFarFutureKey(candidate)] :2353; far-future queue writes RngLocked-gated (Storage:621,653) and sellFarFutureTickets reverts under lock (MintModule:1247). Ranking _bafScore(cand,lvl,currentEpoch) — same frozen epoch/board. No actor input changes which queue indices, candidate, or score ranking.
- **Frozen inputs:** rngWord/entropy frozen daily VRF word keccak-chained :317 + internal keccak :2350; ticketQueue[_tqFarFutureKey] membership far-future pushes RngLocked-blocked Storage:621/:653; sellFarFutureTickets reverts MintModule:1247; drained only in advance; _bafScore inputs resolving-bracket recordBafFlip RngLocked-blocked; currentEpoch :266; salt sequence ++salt :316 deterministic; currentLvl read live in sampler :2344 only sets the band; advance-controlled
- **Live inputs (scale-not-select):** level :2344 advance-stack controlled, fixed for the resolution; defines candidate band but cannot be steered between P and C
- **Verification recipe:** grep sampleFarFutureTickets :318 inside for(pass<2) :315 with ++salt;entropy=hash2(entropy,salt) :316-317; DegenerusGame.sol:2341-2377 pool ticketQueue[_tqFarFutureKey(candidate)] :2353 + internal keccak :2350; isFarFuture && rngLockedFlag Storage:621/:653; sellFarFutureTickets revert MintModule:1247; _bafScore :546-550 keys currentEpoch :266.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: push a winning candidate into ticketQueue[_tqFarFutureKey(candidate)] after the daily word is knowable, or drain the queue to reshape membership. Defeated: every far-future push reverts under the lock — _queueTickets:621, _queueTicketsScaled:653, _queueTicketsRange:715 all (isFarFuture && rngLockedFlag && !rngBypass -> revert RngLocked); sellFarFutureTickets reverts MintModule:1247. rngBypass=true only inside the advance stack. The only drain (processFutureTicketBatch) runs deterministically inside the same advance tx. The entropy chain (++salt; hash2 :316-317) is keccak of the frozen word. TicketRouting.t.sol RNG-02 :159-208 (expectRevert RngLocked, rngBypass exempt :171-175).
- **Backing:** test/fuzz/TicketRouting.t.sol RNG-02/RNG-02-Scaled/RNG-02-Range

#### `RNGF-BAF-03` — runBafJackpot Slice E — scatter level targeting + trait-ticket sampling (50 rounds x top-2 by BAF score)
- **Invariant:** Each round's entropy is a keccak-chained derivative of the frozen daily word (independent per round via ++salt). targetLvl deterministic from frozen entropy. sampleTraitTicketsAtLevel selects traitSel=uint8(entropy>>24), start=(entropy>>40)%len over traitBurnTicket[targetLvl][traitSel] — all from the frozen word; bucket membership for the sampled (past/current) levels fixed (those levels' ticket processing already ran), recordBafFlip on the resolving bracket RngLocked-blocked. No actor input changes targetLvl, traitSel, sampled holders, or _bafScore ranking.
- **Frozen inputs:** rngWord/entropy frozen daily VRF word per-round keccak chain :374; traitBurnTicket[targetLvl][traitSel] membership/order populated by _raritySymbolBatch (MintModule:794-813) inside the advance stack; claimBingo reads only :118-119; player burns route through the advance-gated mint pipeline; isCentury=(lvl%100==0) pure :367; BAF_SCATTER_ROUNDS=50 + loop counter :133,:370; _bafScore currentEpoch :266; resolving-bracket recordBafFlip RngLocked-blocked; traitSel/start uint8(entropy>>24),(entropy>>40)%len :2318,:2327
- **Live inputs (scale-not-select):** arr.length / queue len inside the samplers :2320,:2325 the modulus base; fixed for the sampled bucket during the lock window
- **Verification recipe:** read DegenerusJackpots:359-462 Slice E: ++salt;entropy=hash2(entropy,salt) :371-374, targetLvl :380-387, sampleTraitTicketsAtLevel :389, _bafScore :402, bucketing :418-429; DegenerusGame:2314-2334 traitSel=uint8(entropy>>24), start=(entropy>>40)%len, pool traitBurnTicket[targetLvl][traitSel] view-only; sstore(dst,player) MintModule:810 only population; traitBurnTicket BingoModule READ-ONLY :118-119; runBafJackpot onlyGame :252.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: burn/mint a trait ticket into traitBurnTicket[targetLvl][traitSel] between P and C (scatter band includes past levels lvl-1-entropy%99) so the sampled holder set contains a chosen address; or pre-bias by selecting targetLvl. Defeated: traitBurnTicket's sole writer is the assembly loop in MintModule._raritySymbolBatch (sstore :810), called only from processTicketBatch:991 and the queue-drain :635, both reached exclusively via advance-stack delegatecall (AdvanceModule:674/1554 processTicketBatch.selector). processTicketBatch is NOT exposed through any Game public function or interface and the Game has no generic fallback delegatecall dispatch (only receive() :2481), so no external actor can invoke it. claimBingo reads the bucket read-only. targetLvl and traitSel (uint8(entropy>>24)) and window start are pure functions of the per-round keccak-chained frozen word :374. _bafScore keys on currentEpoch :266; resolving-bracket score writes RngLocked-blocked (Coinflip:609-616).
- **Backing:** test/fuzz/TicketRouting.t.sol; direct source argument (traitBurnTicket written only by advance-stack _raritySymbolBatch)

#### `RNGF-BAF-04` — Game-side sampleTraitTicketsAtLevel — trait-ticket sampler (cross-contract leg of RNGF-BAF-03)
- **Invariant:** Pure view: derives traitSel and a window start solely from the entropy argument and reads traitBurnTicket[targetLvl][traitSel]. Word in entropy frozen at P; the only state read is the trait bucket array whose membership cannot change between P and C (resolved-level buckets immutable; new burns advance-gated). targetLvl supplied by the onlyGame caller from the frozen word.
- **Frozen inputs:** entropy param frozen daily VRF word keccak-chained in DegenerusJackpots :2316; targetLvl param computed by the onlyGame caller from the frozen word; traitBurnTicket[targetLvl][traitSel] holders written only by _raritySymbolBatch :810; read-only here (view); traitSel/start uint8(entropy>>24),(entropy>>40)%len
- **Live inputs (scale-not-select):** len=arr.length :2320 modulus base; fixed for the sampled bucket during the lock window
- **Verification recipe:** grep function sampleTraitTicketsAtLevel :2314 external view; traitSel uint8(entropy>>24) :2318, start (entropy>>40)%len :2327, read arr[(start+i)%len] :2329 no SSTORE; sole writer MintModule._raritySymbolBatch sstore :810 via advance delegatecall; only caller DegenerusJackpots:389.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: bias the returned holders by mutating traitBurnTicket[targetLvl][traitSel] or by influencing traitSel/start through a live read. Defeated: external view :2317, traitSel=uint8(entropy>>24) :2318, start=(entropy>>40)%len :2327, read arr[(start+i)%len] :2329 — no SSTORE, entropy a frozen param (no live word read). The only state read, traitBurnTicket[targetLvl][traitSel], has its sole writer in the advance-stack-only _raritySymbolBatch :810; resolved-level buckets immutable during the lock window. Sole caller DegenerusJackpots.runBafJackpot:389 (onlyGame, self-delegatecall-gated upstream). len only sets the modulus base.
- **Backing:** n/a (direct source argument)

#### `RNGF-BAF-05` — Game-side sampleFarFutureTickets — far-future ticketQueue sampler (cross-contract leg of RNGF-BAF-02)
- **Invariant:** Pure view: derives a candidate-level walk and queue index purely from the entropy argument (internal keccak re-roll word=keccak(word,s) :2350) over ticketQueue[_tqFarFutureKey(candidate)]. Entropy word frozen at P; far-future queue membership RngLocked-frozen at P (no push lands while the lock is held; drained only by the advance stack). currentLvl read live only to set the [+5,+99] band, advance-stack-fixed for the resolution.
- **Frozen inputs:** entropy param frozen daily VRF word keccak-chained in DegenerusJackpots :2342; ticketQueue[_tqFarFutureKey(candidate)] membership RngLocked-gated (Storage:621,653); sellFarFutureTickets reverts MintModule:1247; drained only in advance; write-frozen during the window (Storage:784); candidate/idx currentLvl+5+word%95,(word>>32)%len slices of the frozen keccak-chained word; s loop bound (10) / found cap (4) :2349
- **Live inputs (scale-not-select):** currentLvl=level :2344 sets only the candidate band; advance-stack-controlled, fixed during the resolution; len=queue.length :2354 modulus base; frozen for the sampled far-future key during the lock window
- **Verification recipe:** grep function sampleFarFutureTickets :2341 external view; word=keccak(word,s) :2350, candidate :2351, idx :2356, read queue[idx] :2357 no SSTORE; isFarFuture && rngLockedFlag Storage:621/:653 + _tqFarFutureKey note :782-787; sellFarFutureTickets revert MintModule:1247; sole caller DegenerusJackpots:318.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: insert into or reorder ticketQueue[_tqFarFutureKey(candidate)] between P and C so the (word>>32)%len index selects a chosen winner; or steer currentLvl to shift the candidate band onto a favorable queue. Defeated: external view :2343; internal re-roll word=keccak(word,s) :2350, candidate=currentLvl+5+word%95 :2351, idx=(word>>32)%len :2356, queue[idx] :2357 — no SSTORE. Far-future queue membership RngLocked-frozen (Storage:621/653/715; MintModule:1247) and drained only by the deterministic advance stack. currentLvl=level advance-stack-fixed for the resolution and only sets the [+5,+99] band; an actor cannot move the game level between P and C. TicketRouting.t.sol RNG-02 :159-208.
- **Backing:** test/fuzz/TicketRouting.t.sol RNG-02

#### `RNGF-BAF-06` — Game-side runBafJackpot dispatch + ticket-recirc roll (_awardJackpotTickets / _jackpotTicketRoll)
- **Invariant:** Winner addresses/amounts computed in DegenerusJackpots (BAF-01..05, frozen). The recirc ticket roll evolves entropy=hash2(entropy,entropy) :2083 — a keccak of the frozen word — and reads only the frozen winner address + amount; path/level select (bits[0..12]) and Bernoulli round-up (bits[200..215]) are slices of that frozen word, statistically independent (180+ bits apart). priceForLevel pure lookup. No actor input changes which tickets are queued or to whom.
- **Frozen inputs:** rngWord/entropy frozen daily VRF word from AdvanceModule:935; keccak-evolved :2083; winner/amount returned from DegenerusJackpots.runBafJackpot (frozen-sample winners) iterated :1930-1932; minTargetLevel(lvl) advance-stack level; TICKET_SCALE/LOOTBOX_CLAIM_THRESHOLD/HALF_WHALE_PASS_PRICE constants; targetLevel/roundedUp pure functions of frozen entropy slices :2087-2120; priceForLevel pure :2104
- **Verification recipe:** grep function runBafJackpot JackpotModule:1911 self-only msg.sender!=address(this) revert E() :1916; jackpots.runBafJackpot(poolWei,lvl,rngWord) :1918-1919 returns winners/amounts; _jackpotTicketRoll :2077-2136 entropy=hash2(entropy,entropy) :2083, roll :2087, Bernoulli uint16(entropy>>200)%TICKET_SCALE :2115; _queueTickets(...,true) bypass :2121 advance-stack; caller AdvanceModule:932-936.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: re-enter the recirc roll, or change the winner set / queued ticket recipients between the frozen sample and the Bernoulli round-up. Defeated: runBafJackpot :1911 self-only (msg.sender != address(this) revert E() :1916). Winners/amounts come from jackpots.runBafJackpot (frozen samples BAF-01..05). _jackpotTicketRoll evolves entropy=EntropyLib.hash2(entropy,entropy) :2083 (keccak of the frozen word), path/level select :2087 (bits 0..12), Bernoulli round-up uint16(entropy>>200)%TICKET_SCALE :2115 (bits 200..215) — disjoint slices of one frozen word; priceForLevel pure :2104. _queueTickets(winner,targetLevel,whole,true) :2121 uses rngBypass=true (advance-stack). Every recirc input is the frozen word or a frozen winner/amount/constant.
- **Backing:** direct source argument; comment-asserted bit-independence JackpotModule:2060-2070

#### `RNGF-BAF-07` — AdvanceModule BAF fire-gate (bit 0) + rngWord source identity for the whole BAF cluster
- **Invariant:** rngWord passed to runBafJackpot is exactly rngWordByDay[day] — the same VRF word whose bit 0 fires/skips the BAF — so the entire BAF cluster and the fire decision share one frozen word. bafPoolWei deterministic percentage (10/20%) of baseMemFuture (pool magnitude). On a losing flip markBafSkipped bumps lastBafResolvedDay (DegenerusJackpots:497-503), gating out pre-skip winning-flip BAF credit (Coinflip:551). No actor input changes the fire bit, pool size, or word identity.
- **Frozen inputs:** rngWord=rngWordByDay[day] rngGate return :380 -> :529->:935; single-shot, RngLocked held :1702 through resolution until _unlockRng :1805; bafPoolWei (baseMemFuture*{10,20})/100 :930 pool-magnitude only; lvl/prevMod10/prevMod100 advance-stack arithmetic :919-920; fire bit rngWord&1 :928 single frozen-word bit; lastBafResolvedDay bump on skip GameTimeLib.currentDayIndex() :500 filters pre-skip credit Coinflip:551
- **Verification recipe:** grep (rngWord&1)==1 :928; runBafJackpot(bafPoolWei,lvl,rngWord) :932-936; markBafSkipped(lvl) :940; rngGate( :380 returns rngWord passed to _consolidate :529 (param :828); rngGate short-circuit if(rngWordByDay[day]==0) request-only :644 [NOTE: actual rngGate single-shot is :1209-1210]; rngLockedFlag=true :1702 cleared :1805; cursor>=bafResolvedDay Coinflip:551.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`false`, severityIfBroken=`INFO`. Refutation (sharpest): after the VRF callback delivers the raw word (rawFulfillRandomWords sets rngWordCurrent=word :1846 while rngLockedFlag=true) but BEFORE _applyDailyRng folds it in (:1925/1930), call reverseFlip() to add nudges (totalFlipReversals) and bias bit 0 with knowledge of the raw word. Defeated: reverseFlip() reverts at DegenerusGame:1818 (if(rngLockedFlag) revert); rngLockedFlag set at request :1702 before any word is knowable and stays set across fulfillment and _applyDailyRng until _unlockRng:1805. No nudge can be queued where the raw word is readable; totalFlipReversals frozen at P. rngWord threaded to runBafJackpot (:935) is the same _consolidate param (:832) whose bit 0 fires BAF (:928). bafPoolWei deterministic. RECIPE LINE IMPRECISION (does NOT break the invariant): step 3 cites the single-shot short-circuit at :644 (the game-over entropy path), but the actual rngGate single-shot is :1209-1210; _applyDailyRng writes rngWordByDay[day] single-shot :1930. Invariant holds; one cited line anchor is wrong (recipeReproducible=false).
- **Backing:** test/edge/RngStall.test.js; test/fuzz/RngFreezeAndRemovalProofs.t.sol

#### `RNGF-BAF-08` — runBafJackpot Slice A2 — top coinflip bettor (24h) leaderboard read (deterministic, NOT a VRF value draw)
- **Invariant:** Does NOT derive an outcome from a VRF word — credits 5% to the deterministic top-bettor of the resolved last-day window. Winner is whoever held the highest stakeScore for flipsClaimableDay, written during that day's flips. flipsClaimableDay advanced to the VRF-resolved epoch :915, so the read window is the just-resolved day, fixed before BAF runs in the same advance. The only actor influence is out-staking to be top-bettor DURING that day — pre-commitment, not post-word steering; no VRF value consumed.
- **Frozen inputs:** coinflipTopByDay[flipsClaimableDay] PlayerScore written by _updateTopDayBettor :1292 during the resolved day's flips; not re-writable for a past resolved day; flipsClaimableDay set to the VRF-resolved epoch :915; read in coinflipTopLastDay :1108; topPrize=P/20 deterministic pool fraction :283
- **Live inputs (scale-not-select):** coinflipTopLastDay() :1103-1112 a view of an already-resolved window; no VRF word enters; membership is the only player-controllable factor and is a pre-day-resolution stake commitment
- **Verification recipe:** grep coinflipTopLastDay :284 NOT chained off entropy (no EntropyLib feeds it) credits topPrize=P/20 :283; Coinflip:1103-1112 lastDay=flipsClaimableDay :1108 top=coinflipTopByDay[lastDay] :1110 pure view; flipsClaimableDay=epoch :915 + _updateTopDayBettor write :1292; IDegenerusCoinJackpotView.coinflipTopLastDay view :27.
- **Re-verification verdict:** freezeHolds=`true`, recipeReproducible=`true`, severityIfBroken=`n/a`. Refutation: after the daily word is knowable, out-stake to become coinflipTopByDay[flipsClaimableDay] and capture the 5% top-bettor slice. Defeated: this slice consumes NO VRF word (coinflipTopLastDay :1103-1112 is a pure leaderboard view; topPrize=P/20 :283 not chained off entropy). Every flip stake applies to _targetFlipDay()=currentDayIndex()+1 :1252-1253, so a stake can only build the leaderboard for a FUTURE day. When day D resolves (flipsClaimableDay=D :915), currentDayIndex() is already D, so new stakes target D+1 — coinflipTopByDay[D] (written by _updateTopDayBettor :1292 during stakes that targeted D while D was the future) is sealed before D's word is consumed. Membership is a pre-resolution stake commitment, not post-word selection. Recipe reproduces.
- **Backing:** test/edge/BafCreditRouting.test.js; deposit-day-ahead property structural (Coinflip:1252)

---

## Residual / flagged

Every entry where `freezeHolds=false` OR `severityIfBroken>=LOW` OR the recipe did not reproduce verbatim. None is a confirmed catastrophe or a confirmed exploitable freeze break on a live-game path; all are bounded residuals, by-design mutable inputs, doc/test-backing gaps, or a single magnitude-nudge on a gameover-terminal fallback after a forced VRF stall.

| id | cluster | freezeClass | freezeHolds | sev | recipeRepro | reason | one-line finding |
|---|---|---|---|---|---|---|---|
| `RNGF-REDEEMSEAM-08` | Game-and-getters | CROSS-CONTRACT-SEAM | yes | HIGH | yes | sev>=LOW | Cross-contract redemption-lootbox seed over rngWordForDay(day+1); HIGH-if-broken but day+1 is undrawn at submit (BurnsBlockedBeforeDailyRng pins to a drawn day) and the resolving word is frozen before claim is callable — FREEZE HOLDS. |
| `RNGF-FLIPESCROW-09` | Game-and-getters | CROSS-CONTRACT-SEAM | yes | HIGH | yes | sev>=LOW | Cross-contract FLIP-escrow leg over getCoinflipDayResult(day+1); HIGH-if-broken but day+1 coinflip is unresolved at submit and a frozen past result by claim; flipEscrow sealed at submit — FREEZE HOLDS. |
| `RNGF-JKPT-39` | JackpotModule | NEEDS-PROOF | yes | MED | yes | sev>=LOW | Phase-2 coin/ticket distribution: NEEDS-PROOF/MED — freeze rests on unlock-discipline (RNGREUSE clamp :184-186); RngReuseJackpotStraddle 3/3 PASS is the operative regression. Recommend promoting it to a CI gate. |
| `RNGF-JKPT-43` | JackpotModule | NEEDS-PROOF | yes | MED | yes | sev>=LOW | _rollHeroSymbol weighted hero roll: NEEDS-PROOF/MED — freeze rests on the dailyIdx-advance disjoint-slot invariant; Foundry DegeneretteHeroScore 6/6 PASS; the cited JS harness is stale (not a freeze break). |
| `RNGF-SEAM-RESOLVE` | sDGNRS-Coinflip | CROSS-CONTRACT-SEAM | **NO** | LOW | yes | freezeHolds=false, sev>=LOW | freezeHolds=FALSE on the gameover prevrandao-fallback twin ONLY: a validator's ~1-bit block.prevrandao biases the [25,175] redemption-roll MAGNITUDE on a gameover-terminal self-claim after a forced >=14d VRF stall. Bounded (magnitude not selection); normal + gameover-normal paths fully frozen. The v66-disclosed NEEDS-PROOF residual. |
| `RNGF-DEC-03` | DecimatorModule | CROSS-CONTRACT-SEAM | yes | LOW | yes | sev>=LOW | Terminal decimator winner-select: net freeze HOLDS, but the entry's invariant text inaccurately claims terminal burns are liveness-blocked; in the VRF-grace corner the actual protection is prevrandao unpredictability of the fallback word (1-bit, 11-denom keccak). Correct the invariant text; flip the skipped sec4/sec13 oracles. |
| `RNGF-LBX-06` | LootboxModule | NEEDS-PROOF | yes | LOW | yes | sev>=LOW | _rollLootboxBoons: roll VALUE is a frozen seed bit-slice (HOLDS), but the roll->boonType MAPPING live-reads the boon weight space, so a holder can open-time to reshape WHICH bounded boon a fixed roll maps to — by-design, bounded EV (10% boonBudget capped 1 ETH). |
| `RNGF-LBX-07` | LootboxModule | MUTABLE-INPUT | yes | LOW | yes | sev>=LOW | Deity boon: word frozen-at-commit, but the deity picks slot/recipient AFTER the public day word — bounded to 3 one-shot slots/day, gift-only (deity!=recipient); the acknowledged by-design MUTABLE-INPUT. |
| `RNGF-DEGN-34` | DegeneretteModule | CROSS-CONTRACT-SEAM | yes | LOW | yes | sev>=LOW | _awardDegeneretteDgnrs: VRF win-gate (s>=7) frozen at P; only the reward MAGNITUDE reads the drainable sDGNRS Reward pool (down-scales an earned grant, cannot manufacture/steer a win). Backing test currently vm.skip. |
| `RNGF-JKPT-36` | JackpotModule | FROZEN-AT-COMMIT | yes | LOW | yes | sev>=LOW | Jackpot-phase hero-override: freeze HOLDS (read dailyIdx != write wall-clock); LOW only because the cited JS harness (HeroOverrideDayIndex) is stale (wagerUnit /1e12 vs /1e14), not a contract regression; Foundry backing PASS. |
| `RNGF-JKPT-37` | JackpotModule | FROZEN-AT-COMMIT | yes | LOW | yes | sev>=LOW | Purchase-phase hero-override + ticket distribution: freeze HOLDS (identical disjoint-slot argument + frozen-word keccak winner index); same stale-JS-harness caveat as JKPT-36. |
| `RNGF-BAF-07` | Jackpots-BAF | FROZEN-AT-COMMIT | yes | INFO | **no** | recipe-not-reproduced | BAF fire-gate freeze HOLDS (reverseFlip gated by rngLockedFlag across fulfillment; nudge frozen at P), but the recipe cites a wrong single-shot line anchor (:644 game-over path vs the actual rngGate :1209-1210) — recipeReproducible=false, doc fix only. |

### Concrete findings (flagged entries)

#### `RNGF-REDEEMSEAM-08` (Game-and-getters, CROSS-CONTRACT-SEAM, sev `HIGH`, freezeHolds=`true`, recipeReproducible=`true`)
Recipe reproduces (cosmetic: source widens player to uint256, ledger wrote uint160 — semantically identical). Submit gate confirmed sDGNRS:1036 with currentPeriod=GameTimeLib.currentDayIndex() (:1028, pure timestamp). Prime suspect — selecting a revealed word — defeated: (1) `day` is the composite key into pendingRedemptions[player][day], written at submit as [beneficiary][currentPeriod], attacker can only claim a slot they submitted, and submit pins day=currentPeriod to a DRAWN day so day+1 undrawn; (2) claim requires redemptionPeriods[day]!=0 set only by resolveRedemptionPeriod (GAME-only :742-743) on the advance that settles day+1 — so day+1's word is already frozen before claim is callable; (3) player bound to the claim slot, live-game claim permissionless with all value to player (no timing edge), post-gameOver self-claim only; (4) roll=uint16(((currentWord>>8)%151)+25) derived from the committed daily word inside the lock; single-pool sentinel blocks a second unresolved day. day+1 word ==0 at submit, becomes the unique resolving word by claim. Freeze holds.

#### `RNGF-FLIPESCROW-09` (Game-and-getters, CROSS-CONTRACT-SEAM, sev `HIGH`, freezeHolds=`true`, recipeReproducible=`true`)
Recipe reproduces. getCoinflipDayResult is a pure read: _dayResult(day) reads the coinflipDayResultPacked lane (Coinflip:367-369,1193-1197). Lane written once by processCoinflipPayouts (onlyDegenerusGameContract, :853-857) where win=(rngWord&1)==1 :886 and rewardPercent from keccak256(rngWord,epoch)%20/range :859-883, rngWord = committed daily word from the advance (:1240 live day, :1883 disjoint gap days). Refutation: pick a `day` whose day+1 coinflip is known at submit, or grind day+1 — defeated identically to REDEEMSEAM-08: day is the pendingRedemptions composite key fixed at submit; submit pins day=currentPeriod to a drawn day so day+1's coinflip is unresolved (lane==0->flipWon=false) at submit; by claim the lane is a frozen past result; claim.flipEscrow sealed at submit. isGameOver guard only skips the worthless post-gameOver leg. Freeze holds.

#### `RNGF-JKPT-39` (JackpotModule, NEEDS-PROOF, sev `MED`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation (NEEDS-PROOF class): wall-day straddle reuse — make Phase-2 consume a re-revealed prior word, or inject into the hero pool between Phase-1 and Phase-2. Defeated by source + direct regression: payDailyJackpotCoinAndTickets early-returns on !dailyJackpotCoinTicketsPending :556 and re-derives traits from the SAME randWord :571; ticket entropy = hash2(randWord,lvl)/hash2(randWord,sourceLevel) :584/:602. The RNGREUSE guard at AdvanceModule:184-186 clamps day to dIdx+1 so a NEW wall-day never resolves with a prior unsealed word. test/fuzz/RngReuseJackpotStraddle.t.sol 3/3 PASS (testFix_PendingStraddle_DPlus1GetsFreshWord, testFix_TransitionStraddle_DPlus1GetsFreshWord, testControl). Hero pool frozen at dailyIdx (JKPT-36). Recommend promoting RngReuseJackpotStraddle to a CI gate.

#### `RNGF-JKPT-43` (JackpotModule, NEEDS-PROOF, sev `MED`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation (NEEDS-PROOF class): weight the hero (quadrant,symbol) roll by re-staking after day-D's word is knowable. Defeated: pick=keccak(entropy,day)%effectiveTotal :1396-1398 with entropy=frozen word; weights=dailyHeroWagers[day=dailyIdx] read at the FROZEN index (callers pass dailyIdx :1309/:1785). dailyIdx sole writer _unlockRng:1804; writer placeDegeneretteBet keys _simulatedDayIndex()=currentDayIndex() (wall-clock) :569,581. During day-D resolution dailyIdx==D-1 != wall-clock>=D, disjoint slot. leaderBonus=maxAmount/2 is a deterministic function of the frozen weights. Foundry DegeneretteHeroScore.t.sol 6/6 PASS + HeroOverrideWeightedRoll chi2/leader-bonus/single-bettor stats PASS. CAVEAT (LOW): the cited JS backing HeroOverrideDayIndex.test.js (8 fail) + HeroOverrideWeightedRoll (2 fail) do NOT reproduce green — stale harness (wagerUnit /1e12 vs contract /1e14; garbage dailyIdx slot read), not a freeze break.

#### `RNGF-SEAM-RESOLVE` (sDGNRS-Coinflip, CROSS-CONTRACT-SEAM, sev `LOW`, freezeHolds=`false`, recipeReproducible=`true`)
REFUTATION (the entry's own gameover-fallback twin): the frozenInputs claims 'currentWord frozen at P', but the third roll site (_gameOverEntropy prevrandao-fallback :1358-1359) derives redemptionRoll=((fallbackWord>>8)%151)+25 where fallbackWord=keccak256(combined,currentDay,block.prevrandao) (_getHistoricalRngFallback, called :1337). block.prevrandao is validator-controllable (~1 bit) on a reachable path: gameover + 3-day VRF stall (elapsed>=GAMEOVER_RNG_FALLBACK_DELAY :1335) leaves a sentinel-stamped redemption pool, and that proposer biases the [25,175] roll MAGNITUDE that a 100%-direct gameover self-claim (sDGNRS:848-849 isGameOver -> ethDirect=totalRolledEth) pays to themselves. So an actor-controllable input DOES change the roll's derivation on this path. BOUNDED (not catastrophe): a magnitude nudge, not an index/word SELECTION; roll stays in [25,175]; gameover-terminal-only (no lootbox leg, no live-game holders); reachable only after a forced VRF stall. The normal path (:1261-1264) and gameover-normal path (:1320-1326) ARE fully frozen and hold. V-184 cross-day re-roll structurally removed (redemptionPeriods day-keyed single-write :759; redemptionPeriodIndex/pendingRedemptionEthBase grep-empty; testFuzz_EDGE_07 PASS). This is the v66-flagged NEEDS-PROOF residual the cluster notes disclose. Roll bounds [25,175] sound by source arithmetic (Halmos symbolic run timed out at 240s but property trivially closed).

#### `RNGF-DEC-03` (DecimatorModule, CROSS-CONTRACT-SEAM, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Recipe reproduces (decSeed=rngWord :994, loop :996, offset decBucketOffsetPacked[lvl+1] :1024; rngWord=rngWordByDay[day] :93 before :177 after gameOver=true :135; boost gated :883/:884; FLIP gate :724-725; offset isolation live-tested). GAP found: the ledger asserts 'terminal burns are blocked once gameOver/liveness is reached' as a frozenInput, but recordTerminalDecBurn (:782-790) is gated ONLY by daysRemaining<=7 (and terminalDecWindow()=!gameOver&&!lastPurchaseDay) — NOT by _livenessTriggered() nor rngLockedFlag (only boostTerminalDecimator carries the liveness gate). In the VRF-grace liveness path (_livenessTriggered true when rngRequestTime!=0 for >=14 days), liveness can fire while daysRemaining>7, and the gameover word is then committed via the historical-fallback branch _gameOverEntropy :1333-1368 while gameOver is still false and terminal burns remain OPEN. So the freeze in that corner does NOT rest on the claimed structural burn-block. The net freeze still HOLDS because the fallback word=keccak256(combined,currentDay,block.prevrandao) :1415 is unpredictable to a non-validator placing a burn in a prior block; the only steering is the documented/accepted 1-bit prevrandao validator bias over an 11-denom keccak selection — negligible. Severity LOW: the ledger's structural justification is inaccurate; the actual protection for the burn path in the VRF-dead corner is prevrandao unpredictability, which the entry's invariant/frozenInputs do not name. Recommend correcting the DEC-03 invariant text and flipping the skipped sec4/sec13 oracles.

#### `RNGF-LBX-06` (LootboxModule, NEEDS-PROOF, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation partially LANDED at the DERIVATION level; the WORD/ROLL freeze (under test) HOLDS. The roll VALUE at :1459 = uint32(seed>>120)%BOON_PPM_SCALE is a FROZEN bit-slice of the parent's committed seed — no live read XORed/added into seed or roll. HOWEVER the boon->type mapping _boonFromRoll(...) :1462 and the boon-gate (roll>=totalChance) :1460 DO depend on live reads (decWindowOpen :1431, lazyPassValue :1429/_boonPoolStats :1511, deityEligible/deityPassOwners.length :1436-1438). Since manual openBox is permissionlessly callable and the frozen roll is publicly computable once the index word lands, a box holder CAN read the frozen roll and choose open-timing/conditions to reshape WHICH deterministic boon a fixed roll maps to. This understates the ledger's 'scoring-against' framing. BUT not a freeze break: roll/word cannot be re-rolled (fixed at P); the level driving decWindowOpen/price advances ~1/qualifying-day and is a deterministic narrow band, so a holder can only passively WAIT for the protocol to cross a window, not summon it; flipping deityEligible costs ~24+ ETH and only trades eligibility; the boon is a haircut-funded side-reward (10% boonBudget capped 1 ETH), bounded EV. Same bounded post-reveal MUTABLE-INPUT class as LBX-07. FREEZE-OF-WORD HOLDS; residual is a known by-design bounded derivation-timing selection, INFO/LOW.

#### `RNGF-LBX-07` (LootboxModule, MUTABLE-INPUT, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation: exceed the 3-slot bound, self-target, or re-roll the word; confirmed the acknowledged bounded MUTABLE-INPUT (word frozen, slot/recipient post-reveal selectable). Source: seed=keccak256(abi.encode(rngWord,deity,day,slot)); rngWord=rngWordByDay[day] :1142 day=_simulatedDayIndex() :1141 nonzero-gated :1143. recipient NOT in seed, so the deity cannot grind across recipients for a better boonType. The deity reads the public day word then picks slot 0-2 — at most 3 deterministic per-slot outcomes — the residual mutable input. Bounds verified one-shot: slot<3 :1138, per-slot used-mask :1152, per-recipient-day gate :1150/:1157, deity-pass gate :1139, gift-only :1137. Word frozen-at-commit; only the bounded post-reveal slot/recipient selection is mutable, exactly as classified (known v66 by-design MUTABLE-INPUT). FREEZE-OF-WORD HOLDS; residual by-design.

#### `RNGF-DEGN-34` (DegeneretteModule, CROSS-CONTRACT-SEAM, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation: manufacture or steer the award via the cross-contract Reward-pool read, or bias the s>=7 gate. WIN GATE is VRF-frozen: _awardDegeneretteDgnrs takes `s` frozen; both call sites gate on s>=7 (:737,:1437) where s=_score(playerTicket, resultTicket=VRF-derived, heroQuadrant) — no seed re-derivation; bps a const by frozen s. Seam by-design: sdgnrs.poolBalance(Reward) (live SLOAD :1220, view) sets only magnitude :1226; ==0 early-returns :1227; betWei capped 1 ether :1225; transferFromPool onlyGame (one-directional pull). Draining the pool only DOWN-SCALES an already-earned grant. Freeze of the VRF outcome holds; magnitude intentionally pool-relative (CROSS-CONTRACT-SEAM). BACKING DEFECT (no freeze impact): the cited testDgnrsAwardStaysPerSpin (DegeneretteFreezeResolution.t.sol:719) is vm.skip(true) at :720 — a DISABLED test presented as magnitude backing; the gate-freeze is source-provable independent of it.

#### `RNGF-JKPT-36` (JackpotModule, FROZEN-AT-COMMIT, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation: a bettor who has seen day-D's word injects into the consumed hero pool to bias the hero-override winner. Defeated: consumer reads dailyHeroWagers[dailyIdx] (:1309/:1785), dailyIdx sole writer _unlockRng :1804, so during day-D resolution dailyIdx==D-1; the Degenerette writer keys dailyHeroWagers[_simulatedDayIndex()]=GameTimeLib.currentDayIndex() (wall-clock, :569) which is >= D during the lock window — a DISJOINT slot. randWord frozen :1930. payDailyJackpot NOT externally exposed (only internal wrapper AdvanceModule:1018 delegatecalls it). RngReuseJackpotStraddle 3/3 + DegeneretteHeroScore 6/6 PASS. CAVEAT (LOW if relied on): the cited JS backing test/edge/HeroOverrideDayIndex.test.js does NOT reproduce green (8/8 failing) — stale-harness (expects MIN_BET/1e12 while the contract computes wagerUnit=totalBet/1e14 at DegeneretteModule:571; reads dailyIdx from a stale storage-slot offset yielding garbage), NOT a contract freeze regression. The source freeze argument and the Foundry backing hold.

#### `RNGF-JKPT-37` (JackpotModule, FROZEN-AT-COMMIT, sev `LOW`, freezeHolds=`true`, recipeReproducible=`true`)
Refutation: same hero-pool injection on the purchase-phase branch, plus biasing _distributeTicketJackpot winner index. Defeated: identical hero freeze to RNGF-JKPT-36 (read dailyIdx != write wall-clock). Ticket entropy = EntropyLib.hash2(randWord,lvl) :774; winner idx = keccak(randomWord,trait,salt,i)%effectiveLen :1500-1502 — pure keccak descendants of the frozen word, no mutable-player SLOAD in the index derivation. traitBurnTicket membership VRF-labelled, no external push. Same JS-backing caveat as JKPT-36 (HeroOverrideDayIndex stale-harness fail; Foundry backing JackpotSingleCallCorrectness/RngReuse pass).

#### `RNGF-BAF-07` (Jackpots-BAF, FROZEN-AT-COMMIT, sev `INFO`, freezeHolds=`true`, recipeReproducible=`false`)
Refutation (sharpest): after the VRF callback delivers the raw word (rawFulfillRandomWords sets rngWordCurrent=word :1846 while rngLockedFlag=true) but BEFORE _applyDailyRng folds it in (:1925/1930), call reverseFlip() to add nudges (totalFlipReversals) and bias bit 0 with knowledge of the raw word. Defeated: reverseFlip() reverts at DegenerusGame:1818 (if(rngLockedFlag) revert); rngLockedFlag set at request :1702 before any word is knowable and stays set across fulfillment and _applyDailyRng until _unlockRng:1805. No nudge can be queued where the raw word is readable; totalFlipReversals frozen at P. rngWord threaded to runBafJackpot (:935) is the same _consolidate param (:832) whose bit 0 fires BAF (:928). bafPoolWei deterministic. RECIPE LINE IMPRECISION (does NOT break the invariant): step 3 cites the single-shot short-circuit at :644 (the game-over entropy path), but the actual rngGate single-shot is :1209-1210; _applyDailyRng writes rngWordByDay[day] single-shot :1930. Invariant holds; one cited line anchor is wrong (recipeReproducible=false).

### Recommended follow-on (carried, non-blocking)

1. **RNGF-SEAM-RESOLVE / ADV-10 / ADV-12 / DEC-03 prevrandao residual** — the gameover-only, post-14d-VRF-death fallback word mixes 1 bit of `block.prevrandao`; it nudges a magnitude/roll on a terminal self-claim, never a word/index SELECTION. Source-documented accepted trade-off; no live-game path. Optionally fold prevrandao bias into a documented INFO ledger row rather than a NEEDS-PROOF residual.
2. **Decimator determinism oracles** — `test/fuzz/RngLockDeterminism.t.sol` sec4 (RunTerminalDecimatorJackpot) and sec13 (DecimatorAwardLootbox) are `vm.skip(true)`; flip to strict byte-identical-under-mid-lock assertions (recalibrated to current storage slots) to discharge DEC-01/02/03 with a determinism oracle.
3. **JKPT-39 RngReuseJackpotStraddle -> CI gate** — the wall-day-straddle word-reuse failure mode is guarded by a passing regression (3/3) that is not yet a CI gate; promote it.
4. **Stale JS hero-override harness** — `test/edge/HeroOverrideDayIndex.test.js` (8 fail) and `test/edge/HeroOverrideWeightedRoll.test.js` (2 fail) are stale (wagerUnit `/1e12` vs contract `/1e14` at DegeneretteModule:571; garbage `dailyIdx` from a stale storage-slot offset). The Foundry backing for the same invariants passes; repair or retire the JS harness so the documented backing is green.
5. **DEGN-34 magnitude backing** — `testDgnrsAwardStaysPerSpin` is `vm.skip(true)`; the gate-freeze is source-provable, but the magnitude-replay assertion is untested. Un-skip or replace.
6. **Doc corrections (no logic change)** — DEITYDATA-01/BAFTRAIT-05/BAFFAR-06 should name the `totalFlipReversals` nudge as a contributing frozen input; DEC-03 invariant text should name prevrandao-unpredictability (not a liveness burn-block) as the VRF-grace-corner protection; BAF-07 recipe should cite the rngGate single-shot at `:1209-1210` (not `:644`).
